# eclipse-collections 
 
# Bad smells
I found 2389 bad smells with 1 repairable:
| ruleID | number | fixable |
| --- | --- | --- |
| UNCHECKED_WARNING | 924 | false |
| JavadocReference | 347 | false |
| DuplicatedCode | 263 | false |
| NullableProblems | 238 | false |
| Deprecation | 163 | false |
| OptionalGetWithoutIsPresent | 55 | false |
| ConstantValue | 46 | false |
| RefusedBequest | 39 | false |
| DataFlowIssue | 31 | false |
| EqualsWhichDoesntCheckParameterClass | 29 | false |
| SimplifyStreamApiCallChains | 26 | false |
| IgnoreResultOfCall | 25 | false |
| EqualsWithItself | 24 | false |
| TypeParameterHidesVisibleType | 22 | false |
| DeprecatedIsStillUsed | 21 | false |
| SuspiciousMethodCalls | 18 | false |
| ShiftOutOfRange | 12 | false |
| RedundantCast | 12 | false |
| MismatchedJavadocCode | 11 | false |
| CastCanBeRemovedNarrowingVariableType | 10 | false |
| SynchronizationOnLocalVariableOrMethodParameter | 9 | false |
| SuspiciousSystemArraycopy | 8 | false |
| RedundantComparatorComparing | 8 | false |
| ManualArrayToCollectionCopy | 7 | false |
| DanglingJavadoc | 6 | false |
| SortedCollectionWithNonComparableKeys | 4 | false |
| IntegerMultiplicationImplicitCastToLong | 4 | false |
| UnusedAssignment | 4 | false |
| SimplifiableBooleanExpression | 3 | false |
| ParameterCanBeLocal | 2 | false |
| SimplifiableAssertion | 2 | false |
| AssertBetweenInconvertibleTypes | 2 | false |
| RedundantStreamOptionalCall | 2 | false |
| ThrowFromFinallyBlock | 2 | false |
| PointlessBitwiseExpression | 2 | false |
| ToArrayCallWithZeroLengthArrayArgument | 1 | true |
| RedundantCollectionOperation | 1 | false |
| IOStreamConstructor | 1 | false |
| ListIndexOfReplaceableByContains | 1 | false |
| FunctionalExpressionCanBeFolded | 1 | false |
| MismatchedCollectionQueryUpdate | 1 | false |
| RedundantExplicitClose | 1 | false |
| NullArgumentToVariableArgMethod | 1 | false |
## RuleId[id=ToArrayCallWithZeroLengthArrayArgument]
### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Map.Entry\[map.entrySet().size()\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/immutable/ImmutableMapFactoryImpl.java`
#### Snippet
```java
        }

        Map.Entry<K, V>[] entries = map.entrySet().toArray(new Map.Entry[map.entrySet().size()]);
        switch (entries.length)
        {
```

## RuleId[id=EqualsWithItself]
### EqualsWithItself
`equals()` called on itself
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/ListEqualTest.java`
#### Snippet
```java
    public void ec()
    {
        if (!this.integersEC1.equals(this.integersEC1))
        {
            throw new AssertionError();
```

### EqualsWithItself
`equals()` called on itself
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/ListEqualTest.java`
#### Snippet
```java
    public void jdk()
    {
        if (!this.integersJDK1.equals(this.integersJDK1))
        {
            throw new AssertionError();
```

### EqualsWithItself
`assertSame()` called on itself
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/FunctionalInterfaceSetTest.java`
#### Snippet
```java
        org.eclipse.collections.api.block.function.Function<Integer, String> function3 = each ->
        {
            Assert.assertSame(each, each);
            return String.valueOf(each);
        };
```

### EqualsWithItself
`assertSame()` called on itself
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/FunctionalInterfaceSetTest.java`
#### Snippet
```java

        org.eclipse.collections.api.block.function.Function<String, Integer> function4 = each -> {
            Assert.assertSame(each, each);
            return Integer.valueOf(each);
        };
```

### EqualsWithItself
`assertSame()` called on itself
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/FunctionalInterfaceSetTest.java`
#### Snippet
```java
            Function<Integer, String> mapper3 = each ->
            {
                Assert.assertSame(each, each);
                return String.valueOf(each);
            };
```

### EqualsWithItself
`assertSame()` called on itself
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/FunctionalInterfaceSetTest.java`
#### Snippet
```java

            Function<String, Integer> mapper4 = each -> {
                Assert.assertSame(each, each);
                return Integer.valueOf(each);
            };
```

### EqualsWithItself
`assertEquals()` called on itself
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AggregateByTest.java`
#### Snippet
```java
        MapIterable<String, ImmutableMarketValueStatistics> actual = this.aggregateByCategory_parallel_lazy_ec();
        MapIterable<String, ImmutableMarketValueStatistics> expected = this.aggregateByCategory_serial_lazy_ec();
        Assert.assertEquals(expected, expected);
        Verify.assertMapsEqual((Map<String, ImmutableMarketValueStatistics>) expected, (Map<String, ImmutableMarketValueStatistics>) actual);
    }
```

### EqualsWithItself
`assertEquals()` called on itself
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AggregateByTest.java`
#### Snippet
```java
        MapIterable<Account, ImmutableMarketValueStatistics> actual = this.aggregateByAccount_parallel_lazy_ec();
        MapIterable<Account, ImmutableMarketValueStatistics> expected = this.aggregateByAccount_serial_lazy_ec();
        Assert.assertEquals(expected, expected);
        Verify.assertMapsEqual((Map<Account, ImmutableMarketValueStatistics>) expected, (Map<Account, ImmutableMarketValueStatistics>) actual);
    }
```

### EqualsWithItself
`assertEquals()` called on itself
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AggregateByTest.java`
#### Snippet
```java
        MapIterable<Product, MarketValueStatistics> actual = this.aggregateInPlaceByProduct_parallel_lazy_ec();
        MapIterable<Product, MarketValueStatistics> expected = this.aggregateInPlaceByProduct_serial_eager_ec();
        Assert.assertEquals(expected, expected);
        Verify.assertMapsEqual((Map<Product, MarketValueStatistics>) expected, (Map<Product, MarketValueStatistics>) actual);
    }
```

### EqualsWithItself
`assertEquals()` called on itself
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AggregateByTest.java`
#### Snippet
```java
        MapIterable<Account, MarketValueStatistics> actual = this.aggregateInPlaceByAccount_parallel_lazy_ec();
        MapIterable<Account, MarketValueStatistics> expected = this.aggregateInPlaceByAccount_serial_eager_ec();
        Assert.assertEquals(expected, expected);
        Verify.assertMapsEqual((Map<Account, MarketValueStatistics>) expected, (Map<Account, MarketValueStatistics>) actual);
    }
```

### EqualsWithItself
`assertEquals()` called on itself
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AggregateByTest.java`
#### Snippet
```java
        MapIterable<String, MarketValueStatistics> actual = this.aggregateInPlaceByCategory_parallel_lazy_ec();
        MapIterable<String, MarketValueStatistics> expected = this.aggregateInPlaceByCategory_serial_eager_ec();
        Assert.assertEquals(expected, expected);
        Verify.assertMapsEqual((Map<String, MarketValueStatistics>) expected, (Map<String, MarketValueStatistics>) actual);
    }
```

### EqualsWithItself
`assertEquals()` called on itself
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AggregateByTest.java`
#### Snippet
```java
        MapIterable<Product, ImmutableMarketValueStatistics> actual = this.aggregateByProduct_parallel_lazy_ec();
        MapIterable<Product, ImmutableMarketValueStatistics> expected = this.aggregateByProduct_serial_lazy_ec();
        Assert.assertEquals(expected, expected);
        Verify.assertMapsEqual((Map<Product, ImmutableMarketValueStatistics>) expected, (Map<Product, ImmutableMarketValueStatistics>) actual);
    }
```

### EqualsWithItself
`assertSame()` called on itself
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/FunctionalInterfaceTest.java`
#### Snippet
```java
        org.eclipse.collections.api.block.function.Function<Integer, String> function3 = each ->
        {
            Assert.assertSame(each, each);
            return String.valueOf(each);
        };
```

### EqualsWithItself
`assertSame()` called on itself
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/FunctionalInterfaceTest.java`
#### Snippet
```java

        org.eclipse.collections.api.block.function.Function<String, Integer> function4 = each -> {
            Assert.assertSame(each, each);
            return Integer.valueOf(each);
        };
```

### EqualsWithItself
`assertSame()` called on itself
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/FunctionalInterfaceTest.java`
#### Snippet
```java
            Function<Integer, String> mapper3 = each ->
            {
                Assert.assertSame(each, each);
                return String.valueOf(each);
            };
```

### EqualsWithItself
`assertSame()` called on itself
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/FunctionalInterfaceTest.java`
#### Snippet
```java

            Function<String, Integer> mapper4 = each -> {
                Assert.assertSame(each, each);
                return Integer.valueOf(each);
            };
```

### EqualsWithItself
`assertEquals()` called on itself
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/CountSetTest.java`
#### Snippet
```java
        {
            this.integersEC.asLazy().forEach(Procedures.cast(Assert::assertNotNull));
            this.integersEC.asLazy().forEach(Procedures.cast(each -> Assert.assertEquals(each, each)));
            this.integersEC.asLazy().forEach(new CountProcedure<>());

```

### EqualsWithItself
`assertEquals()` called on itself
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/CountSetTest.java`
#### Snippet
```java

            this.integersEC.asParallel(this.executorService, BATCH_SIZE).forEach(Assert::assertNotNull);
            this.integersEC.asParallel(this.executorService, BATCH_SIZE).forEach(each -> Assert.assertEquals(each, each));
            this.integersEC.asParallel(this.executorService, BATCH_SIZE).forEach(new CountProcedure<>());

```

### EqualsWithItself
`assertEquals()` called on itself
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/CountSetTest.java`
#### Snippet
```java

            this.integersJDK.stream().forEach(Assert::assertNotNull);
            this.integersJDK.stream().forEach(each -> Assert.assertEquals(each, each));

            this.integersJDK.parallelStream().forEach(Assert::assertNotNull);
```

### EqualsWithItself
`assertEquals()` called on itself
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/CountSetTest.java`
#### Snippet
```java

            this.integersJDK.parallelStream().forEach(Assert::assertNotNull);
            this.integersJDK.parallelStream().forEach(each -> Assert.assertEquals(each, each));
        }

```

### EqualsWithItself
`assertEquals()` called on itself
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/CountTest.java`
#### Snippet
```java
        {
            this.integersEC.asLazy().forEach(Procedures.cast(Assert::assertNotNull));
            this.integersEC.asLazy().forEach(Procedures.cast(each -> Assert.assertEquals(each, each)));
            this.integersEC.asLazy().forEach(new CountProcedure<>());

```

### EqualsWithItself
`assertEquals()` called on itself
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/CountTest.java`
#### Snippet
```java

            this.integersEC.asParallel(this.executorService, BATCH_SIZE).forEach(Assert::assertNotNull);
            this.integersEC.asParallel(this.executorService, BATCH_SIZE).forEach(each -> Assert.assertEquals(each, each));
            this.integersEC.asParallel(this.executorService, BATCH_SIZE).forEach(new CountProcedure<>());

```

### EqualsWithItself
`assertEquals()` called on itself
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/CountTest.java`
#### Snippet
```java

            this.integersJDK.stream().forEach(Assert::assertNotNull);
            this.integersJDK.stream().forEach(each -> Assert.assertEquals(each, each));

            this.integersJDK.parallelStream().forEach(Assert::assertNotNull);
```

### EqualsWithItself
`assertEquals()` called on itself
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/CountTest.java`
#### Snippet
```java

            this.integersJDK.parallelStream().forEach(Assert::assertNotNull);
            this.integersJDK.parallelStream().forEach(each -> Assert.assertEquals(each, each));
        }

```

## RuleId[id=UNCHECKED_WARNING]
### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections-api/src/main/java/org/eclipse/collections/api/map/MapIterable.java`
#### Snippet
```java
    default V getOrDefault(Object key, V defaultValue)
    {
        return this.getIfAbsentValue((K) key, defaultValue);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'IV\[\]'
in `eclipse-collections-api/src/main/java/org/eclipse/collections/api/map/MapIterable.java`
#### Snippet
```java
            Function3<? super IV, ? super K, ? super V, ? extends IV> function)
    {
        IV[] result = (IV[]) new Object[]{injectedValue};
        this.forEachKeyValue((key, value) -> result[0] = function.value(result[0], key, value));
        return result[0];
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections-api/src/main/java/org/eclipse/collections/api/map/ConcurrentMutableMap.java`
#### Snippet
```java
    default V getOrDefault(Object key, V defaultValue)
    {
        return this.getIfAbsentValue((K) key, defaultValue);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections-api/src/main/java/org/eclipse/collections/api/map/MutableMapIterable.java`
#### Snippet
```java
    default V getOrDefault(Object key, V defaultValue)
    {
        return this.getIfAbsentValue((K) key, defaultValue);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections-api/src/main/java/org/eclipse/collections/api/RichIterable.java`
#### Snippet
```java
    {
        boolean[] seenOne = new boolean[1];
        T[] result = (T[]) new Object[1];
        this.each(each ->
        {
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.block.SerializableComparator\>' to 'org.eclipse.collections.api.block.SerializableComparator'
in `eclipse-collections-api/src/main/java/org/eclipse/collections/api/block/factory/SerializableComparators.java`
#### Snippet
```java
    public static <T> SerializableComparator<T> naturalOrder()
    {
        return (SerializableComparator<T>) NATURAL_ORDER_COMPARATOR;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.block.SerializableComparator\>' to 'org.eclipse.collections.api.block.SerializableComparator'
in `eclipse-collections-api/src/main/java/org/eclipse/collections/api/block/factory/SerializableComparators.java`
#### Snippet
```java
    public static <T> SerializableComparator<T> reverseNaturalOrder()
    {
        return (SerializableComparator<T>) REVERSE_NATURAL_ORDER_COMPARATOR;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.list.ListIterable' to 'java.util.List\>'
in `eclipse-collections-api/src/main/java/org/eclipse/collections/api/list/ListIterable.java`
#### Snippet
```java
    default int binarySearch(T key)
    {
        return Collections.binarySearch((List<? extends Comparable<? super T>>) this, key);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.list.ListIterable' to 'java.util.List'
in `eclipse-collections-api/src/main/java/org/eclipse/collections/api/list/ListIterable.java`
#### Snippet
```java
    default int binarySearch(T key, Comparator<? super T> comparator)
    {
        return Collections.binarySearch((List<? extends T>) this, key, comparator);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections-api/src/main/java/org/eclipse/collections/api/factory/list/MutableListFactory.java`
#### Snippet
```java
    default <T> MutableList<T> wrapCopy(T... array)
    {
        T[] newArray = (T[]) new Object[array.length];
        System.arraycopy(array, 0, newArray, 0, array.length);
        return this.with(newArray);
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.list.MutableList\>' to 'org.eclipse.collections.api.list.MutableList'
in `eclipse-collections-api/src/main/java/org/eclipse/collections/api/factory/list/ImmutableListFactory.java`
#### Snippet
```java
    default <T> ImmutableList<T> withAllSorted(Comparator<? super T> comparator, RichIterable<? extends T> items)
    {
        MutableList<T> list = (MutableList<T>) items.toSortedList(comparator);
        return list.toImmutable();
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.list.MutableList\>' to 'org.eclipse.collections.api.list.MutableList'
in `eclipse-collections-api/src/main/java/org/eclipse/collections/api/factory/list/ImmutableListFactory.java`
#### Snippet
```java
    default <T> ImmutableList<T> withAllSorted(RichIterable<? extends T> items)
    {
        MutableList<T> list = (MutableList<T>) items.toSortedList();
        return list.toImmutable();
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Class\>' to 'java.lang.Class'
in `eclipse-collections-api/src/main/java/org/eclipse/collections/api/factory/ServiceLoaderUtils.java`
#### Snippet
```java
        try
        {
            Class<T> fallbackClass = (Class<T>) Class.forName(fallbackName, true, loader);
            return fallbackClass.newInstance();
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.map.MapIterable' to 'java.util.Map'
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AggregateByTest.java`
#### Snippet
```java
        MapIterable<String, ImmutableMarketValueStatistics> expected = this.aggregateByCategory_serial_lazy_ec();
        Assert.assertEquals(expected, expected);
        Verify.assertMapsEqual((Map<String, ImmutableMarketValueStatistics>) expected, (Map<String, ImmutableMarketValueStatistics>) actual);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.map.MapIterable' to 'java.util.Map'
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AggregateByTest.java`
#### Snippet
```java
        MapIterable<String, ImmutableMarketValueStatistics> expected = this.aggregateByCategory_serial_lazy_ec();
        Assert.assertEquals(expected, expected);
        Verify.assertMapsEqual((Map<String, ImmutableMarketValueStatistics>) expected, (Map<String, ImmutableMarketValueStatistics>) actual);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.map.MapIterable' to 'java.util.Map'
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AggregateByTest.java`
#### Snippet
```java
        MapIterable<Account, ImmutableMarketValueStatistics> expected = this.aggregateByAccount_serial_lazy_ec();
        Assert.assertEquals(expected, expected);
        Verify.assertMapsEqual((Map<Account, ImmutableMarketValueStatistics>) expected, (Map<Account, ImmutableMarketValueStatistics>) actual);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.map.MapIterable' to 'java.util.Map'
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AggregateByTest.java`
#### Snippet
```java
        MapIterable<Account, ImmutableMarketValueStatistics> expected = this.aggregateByAccount_serial_lazy_ec();
        Assert.assertEquals(expected, expected);
        Verify.assertMapsEqual((Map<Account, ImmutableMarketValueStatistics>) expected, (Map<Account, ImmutableMarketValueStatistics>) actual);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.map.MapIterable' to 'java.util.Map'
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AggregateByTest.java`
#### Snippet
```java
        MapIterable<Product, MarketValueStatistics> expected = this.aggregateInPlaceByProduct_serial_eager_ec();
        Assert.assertEquals(expected, expected);
        Verify.assertMapsEqual((Map<Product, MarketValueStatistics>) expected, (Map<Product, MarketValueStatistics>) actual);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.map.MapIterable' to 'java.util.Map'
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AggregateByTest.java`
#### Snippet
```java
        MapIterable<Product, MarketValueStatistics> expected = this.aggregateInPlaceByProduct_serial_eager_ec();
        Assert.assertEquals(expected, expected);
        Verify.assertMapsEqual((Map<Product, MarketValueStatistics>) expected, (Map<Product, MarketValueStatistics>) actual);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.map.MapIterable' to 'java.util.Map'
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AggregateByTest.java`
#### Snippet
```java
        MapIterable<Account, MarketValueStatistics> expected = this.aggregateInPlaceByAccount_serial_eager_ec();
        Assert.assertEquals(expected, expected);
        Verify.assertMapsEqual((Map<Account, MarketValueStatistics>) expected, (Map<Account, MarketValueStatistics>) actual);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.map.MapIterable' to 'java.util.Map'
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AggregateByTest.java`
#### Snippet
```java
        MapIterable<Account, MarketValueStatistics> expected = this.aggregateInPlaceByAccount_serial_eager_ec();
        Assert.assertEquals(expected, expected);
        Verify.assertMapsEqual((Map<Account, MarketValueStatistics>) expected, (Map<Account, MarketValueStatistics>) actual);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.map.MapIterable' to 'java.util.Map'
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AggregateByTest.java`
#### Snippet
```java
        MapIterable<String, MarketValueStatistics> expected = this.aggregateInPlaceByCategory_serial_eager_ec();
        Assert.assertEquals(expected, expected);
        Verify.assertMapsEqual((Map<String, MarketValueStatistics>) expected, (Map<String, MarketValueStatistics>) actual);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.map.MapIterable' to 'java.util.Map'
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AggregateByTest.java`
#### Snippet
```java
        MapIterable<String, MarketValueStatistics> expected = this.aggregateInPlaceByCategory_serial_eager_ec();
        Assert.assertEquals(expected, expected);
        Verify.assertMapsEqual((Map<String, MarketValueStatistics>) expected, (Map<String, MarketValueStatistics>) actual);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.map.MapIterable' to 'java.util.Map'
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AggregateByTest.java`
#### Snippet
```java
        MapIterable<Product, ImmutableMarketValueStatistics> expected = this.aggregateByProduct_serial_lazy_ec();
        Assert.assertEquals(expected, expected);
        Verify.assertMapsEqual((Map<Product, ImmutableMarketValueStatistics>) expected, (Map<Product, ImmutableMarketValueStatistics>) actual);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.map.MapIterable' to 'java.util.Map'
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AggregateByTest.java`
#### Snippet
```java
        MapIterable<Product, ImmutableMarketValueStatistics> expected = this.aggregateByProduct_serial_lazy_ec();
        Assert.assertEquals(expected, expected);
        Verify.assertMapsEqual((Map<Product, ImmutableMarketValueStatistics>) expected, (Map<Product, ImmutableMarketValueStatistics>) actual);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.impl.EmptyIterator\>' to 'org.eclipse.collections.impl.EmptyIterator'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/EmptyIterator.java`
#### Snippet
```java
    public static <T> EmptyIterator<T> getInstance()
    {
        return (EmptyIterator<T>) INSTANCE;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.api.RichIterable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/SynchronizedRichIterable.java`
#### Snippet
```java
        public void readExternal(ObjectInput in) throws IOException, ClassNotFoundException
        {
            this.richIterable = (RichIterable<T>) in.readObject();
        }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'E\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/AbstractRichIterable.java`
#### Snippet
```java
        int size = this.size();
        E[] result = array.length < size
                ? (E[]) Array.newInstance(array.getClass().getComponentType(), size)
                : array;

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Comparator'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/sorted/immutable/ImmutableSortedBagSerializationProxy.java`
#### Snippet
```java
    {
        int size = in.readInt();
        Comparator<T> comparator = (Comparator<T>) in.readObject();
        MutableSortedBag<T> deserializedBag = new TreeBag<>(comparator);

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/sorted/immutable/ImmutableSortedBagSerializationProxy.java`
#### Snippet
```java
        for (int i = 0; i < size; i++)
        {
            deserializedBag.addOccurrences((T) in.readObject(), in.readInt());
        }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Iterable\>' to 'org.eclipse.collections.api.bag.sorted.ImmutableSortedBag'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/sorted/immutable/ImmutableSortedBagFactoryImpl.java`
#### Snippet
```java
        if (items instanceof ImmutableSortedBag<?>)
        {
            return (ImmutableSortedBag<T>) items;
        }
        if (Iterate.isEmpty(items))
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.bag.sorted.ImmutableSortedBag\>' to 'org.eclipse.collections.api.bag.sorted.ImmutableSortedBag'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/sorted/immutable/ImmutableSortedBagFactoryImpl.java`
#### Snippet
```java
    public <T> ImmutableSortedBag<T> empty()
    {
        return (ImmutableSortedBag<T>) ImmutableEmptySortedBag.INSTANCE;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Comparator\>' to 'java.util.Comparator'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/sorted/mutable/AbstractMutableSortedBag.java`
#### Snippet
```java
    public <S> MutableSortedBag<S> selectInstancesOf(Class<S> clazz)
    {
        Comparator<? super S> comparator = (Comparator<? super S>) this.comparator();
        MutableSortedBag<S> result = SortedBags.mutable.empty(comparator);
        this.forEachWithOccurrences((each, occurrences) -> {
```

### UNCHECKED_WARNING
Unchecked cast: 'R' to 'org.eclipse.collections.api.bag.MutableBagIterable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/AbstractBag.java`
#### Snippet
```java
        if (target instanceof MutableBagIterable<?>)
        {
            MutableBagIterable<V> targetBag = (MutableBagIterable<V>) target;

            this.forEachWithOccurrences((each, occurrences) -> targetBag.addOccurrences(function.valueOf(each), occurrences));
```

### UNCHECKED_WARNING
Unchecked cast: 'R' to 'org.eclipse.collections.api.bag.MutableBagIterable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/AbstractBag.java`
#### Snippet
```java
        if (target instanceof MutableBagIterable<?>)
        {
            MutableBagIterable<T> targetBag = (MutableBagIterable<T>) target;

            this.forEachWithOccurrences((each, occurrences) -> {
```

### UNCHECKED_WARNING
Unchecked cast: 'R' to 'org.eclipse.collections.api.bag.MutableBagIterable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/AbstractBag.java`
#### Snippet
```java
        if (target instanceof MutableBagIterable<?>)
        {
            MutableBagIterable<T> targetBag = (MutableBagIterable<T>) target;

            this.forEachWithOccurrences((each, occurrences) -> {
```

### UNCHECKED_WARNING
Unchecked cast: 'R' to 'org.eclipse.collections.api.bag.MutableBagIterable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/AbstractBag.java`
#### Snippet
```java
        if (target instanceof MutableBagIterable<?>)
        {
            MutableBagIterable<V> targetBag = (MutableBagIterable<V>) target;

            this.forEachWithOccurrences((each, occurrences) -> targetBag.addOccurrences(function.value(each, parameter), occurrences));
```

### UNCHECKED_WARNING
Unchecked cast: 'R' to 'org.eclipse.collections.api.bag.MutableBagIterable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/AbstractBag.java`
#### Snippet
```java
        if (target instanceof MutableBagIterable<?>)
        {
            MutableBagIterable<T> targetBag = (MutableBagIterable<T>) target;

            this.forEachWithOccurrences((each, occurrences) -> {
```

### UNCHECKED_WARNING
Unchecked cast: 'R' to 'org.eclipse.collections.api.bag.MutableBagIterable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/AbstractBag.java`
#### Snippet
```java
        if (target instanceof MutableBagIterable<?>)
        {
            MutableBagIterable<V> targetBag = (MutableBagIterable<V>) target;

            this.forEachWithOccurrences((each, occurrences) -> {
```

### UNCHECKED_WARNING
Unchecked cast: 'R' to 'org.eclipse.collections.api.bag.MutableBagIterable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/AbstractBag.java`
#### Snippet
```java
        if (target instanceof MutableBagIterable<?>)
        {
            MutableBagIterable<T> targetBag = (MutableBagIterable<T>) target;

            this.forEachWithOccurrences((each, occurrences) -> {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'IV\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/AbstractBag.java`
#### Snippet
```java
    public <IV> IV injectInto(IV injectedValue, Function2<? super IV, ? super T, ? extends IV> function)
    {
        IV[] result = (IV[]) new Object[]{injectedValue};
        this.forEachWithOccurrences((each, occurrences) -> {
            for (int i = 0; i < occurrences; i++)
```

### UNCHECKED_WARNING
Unchecked cast: 'R' to 'org.eclipse.collections.api.bag.MutableBagIterable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/AbstractBag.java`
#### Snippet
```java
        if (target instanceof MutableBagIterable<?>)
        {
            MutableBagIterable<V> targetBag = (MutableBagIterable<V>) target;

            this.forEachWithOccurrences((each, occurrences) -> {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'IV\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/AbstractBag.java`
#### Snippet
```java
    public <IV, P> IV injectIntoWith(IV injectedValue, Function3<? super IV, ? super T, ? super P, ? extends IV> function, P parameter)
    {
        IV[] result = (IV[]) new Object[]{injectedValue};
        this.forEachWithOccurrences((each, occurrences) -> {
            for (int i = 0; i < occurrences; i++)
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Iterable\>' to 'org.eclipse.collections.api.bag.Bag'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/mutable/HashBag.java`
#### Snippet
```java
        if (source instanceof Bag)
        {
            return HashBag.newBag((Bag<E>) source);
        }
        return HashBag.newBagWith((E[]) Iterate.toArray(source));
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'E\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/mutable/HashBag.java`
#### Snippet
```java
            return HashBag.newBag((Bag<E>) source);
        }
        return HashBag.newBagWith((E[]) Iterate.toArray(source));
    }

```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/sorted/immutable/ImmutableEmptySortedBag.java`
#### Snippet
```java
    public ImmutableSortedBag<T> newWith(T element)
    {
        return SortedBags.immutable.with(this.comparator, element);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.impl.bag.sorted.immutable.ImmutableEmptySortedBag' to 'org.eclipse.collections.api.bag.sorted.ImmutableSortedBag'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/sorted/immutable/ImmutableEmptySortedBag.java`
#### Snippet
```java
    public <S> ImmutableSortedBag<S> selectInstancesOf(Class<S> clazz)
    {
        return (ImmutableSortedBag<S>) this;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Comparator\>' to 'java.util.Comparator\>'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/sorted/immutable/ImmutableEmptySortedBag.java`
#### Snippet
```java
    public ImmutableSortedSet<Pair<T, Integer>> zipWithIndex()
    {
        return SortedSets.immutable.with((Comparator<? super Pair<T, Integer>>) this.comparator);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'E\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/sorted/immutable/ImmutableSortedBagImpl.java`
#### Snippet
```java
        if (array1.length < this.size)
        {
            array1 = (E[]) Array.newInstance(array1.getClass().getComponentType(), this.size);
        }
        T[] items = (T[]) this.toArray();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/sorted/immutable/ImmutableSortedBagImpl.java`
#### Snippet
```java
            array1 = (E[]) Array.newInstance(array1.getClass().getComponentType(), this.size);
        }
        T[] items = (T[]) this.toArray();
        System.arraycopy(items, 0, array1, 0, this.size);
        if (array1.length > this.size)
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/sorted/immutable/ImmutableSortedBagImpl.java`
#### Snippet
```java
    public int occurrencesOf(Object item)
    {
        int index = Arrays.binarySearch(this.elements, (T) item, this.comparator);
        if (index > -1)
        {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/sorted/immutable/ImmutableSortedBagImpl.java`
#### Snippet
```java
        int insertionPoint = (index + 1) * -1;

        T[] elements = (T[]) new Object[this.elements.length + 1];
        int[] occurrences = new int[this.occurrences.length + 1];

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.ordered.OrderedIterable' to 'java.util.List'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/sorted/immutable/ImmutableSortedBagImpl.java`
#### Snippet
```java
        if (other instanceof RandomAccess)
        {
            List<S> otherList = (List<S>) other;
            int otherListIndex = 0;
            for (int i = 0; i < this.elements.length; i++)
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/sorted/immutable/ImmutableSortedBagImpl.java`
#### Snippet
```java
        }
        this.comparator = sortedBag.comparator();
        this.elements = (T[]) new Object[sortedBag.sizeDistinct()];
        this.occurrences = new int[sortedBag.sizeDistinct()];
        this.size = sortedBag.size();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/sorted/immutable/ImmutableSortedBagImpl.java`
#### Snippet
```java
    public boolean contains(Object object)
    {
        return Arrays.binarySearch(this.elements, (T) object, this.comparator) >= 0;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/sorted/immutable/ImmutableSortedBagImpl.java`
#### Snippet
```java
        }

        T[] elements = (T[]) new Object[this.elements.length - 1];
        int[] occurrences = new int[this.occurrences.length - 1];

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/sorted/mutable/TreeBag.java`
#### Snippet
```java
        if (this.items.containsKey(object))
        {
            long result = this.items.headMap((T) object).values().sumOfInt(Counter.TO_COUNT);
            if (result > Integer.MAX_VALUE)
            {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Comparator'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/sorted/mutable/TreeBag.java`
#### Snippet
```java
    public void readExternal(ObjectInput in) throws IOException, ClassNotFoundException
    {
        this.items = new TreeSortedMap<>((Comparator<T>) in.readObject());
        int size = in.readInt();
        for (int i = 0; i < size; i++)
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/sorted/mutable/TreeBag.java`
#### Snippet
```java
        for (int i = 0; i < size; i++)
        {
            this.addOccurrences((T) in.readObject(), in.readInt());
        }
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Iterable\>' to 'org.eclipse.collections.api.bag.sorted.SortedBag'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/sorted/mutable/TreeBag.java`
#### Snippet
```java
        if (source instanceof SortedBag<?>)
        {
            return new TreeBag<>((SortedBag<E>) source);
        }
        return Iterate.addAllTo(source, TreeBag.newBag());
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Iterable\>' to 'java.lang.Iterable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/mutable/MultiReaderMutableBagFactory.java`
#### Snippet
```java
    public <T> MultiReaderBag<T> withAll(Iterable<? extends T> iterable)
    {
        return MultiReaderHashBag.newBag((Iterable<T>) iterable);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/mutable/AbstractHashBag.java`
#### Snippet
```java
            for (Object each : iterable)
            {
                int removed = this.items.removeKeyIfAbsent((T) each, 0);
                this.size -= removed;
            }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/mutable/AbstractHashBag.java`
#### Snippet
```java
        }

        int newValue = this.items.updateValue((T) item, 0, IntToIntFunctions.subtract(occurrences));

        if (newValue <= 0)
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/mutable/AbstractHashBag.java`
#### Snippet
```java
    public boolean remove(Object item)
    {
        int newValue = this.items.updateValue((T) item, 0, IntToIntFunctions.decrement());
        if (newValue <= 0)
        {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/mutable/AbstractHashBag.java`
#### Snippet
```java
        if (newValue <= 0)
        {
            this.items.removeKey((T) item);
            if (newValue == -1)
            {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.api.block.HashingStrategy'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/strategy/mutable/HashBagWithHashingStrategySerializationProxy.java`
#### Snippet
```java
    public void readExternal(ObjectInput in) throws IOException, ClassNotFoundException
    {
        HashingStrategy<T> hashingStrategy = (HashingStrategy<T>) in.readObject();
        int size = in.readInt();
        HashBagWithHashingStrategy<T> deserializedBag = new HashBagWithHashingStrategy<>(hashingStrategy, size);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/strategy/mutable/HashBagWithHashingStrategySerializationProxy.java`
#### Snippet
```java
        for (int i = 0; i < size; i++)
        {
            deserializedBag.addOccurrences((T) in.readObject(), in.readInt());
        }

```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/strategy/mutable/MutableHashingStrategyBagFactoryImpl.java`
#### Snippet
```java
    public <T> MutableBag<T> with(HashingStrategy<? super T> hashingStrategy)
    {
        return HashBagWithHashingStrategy.newBagWith(hashingStrategy);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Iterable\>' to 'org.eclipse.collections.api.bag.Bag'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/strategy/mutable/HashBagWithHashingStrategy.java`
#### Snippet
```java
        if (source instanceof Bag)
        {
            return HashBagWithHashingStrategy.newBag(hashingStrategy, (Bag<E>) source);
        }
        return HashBagWithHashingStrategy.newBagWith(hashingStrategy, (E[]) Iterate.toArray(source));
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'E\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/strategy/mutable/HashBagWithHashingStrategy.java`
#### Snippet
```java
            return HashBagWithHashingStrategy.newBag(hashingStrategy, (Bag<E>) source);
        }
        return HashBagWithHashingStrategy.newBagWith(hashingStrategy, (E[]) Iterate.toArray(source));
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Iterable\>' to 'org.eclipse.collections.api.bag.Bag'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/mutable/AbstractMutableBagIterable.java`
#### Snippet
```java
        if (iterable instanceof Bag)
        {
            return this.addAllBag((Bag<T>) iterable);
        }
        int oldSize = this.size();
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.bag.ImmutableBag\>' to 'org.eclipse.collections.api.bag.ImmutableBag'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/immutable/ImmutableBagFactoryImpl.java`
#### Snippet
```java
    public <T> ImmutableBag<T> empty()
    {
        return (ImmutableBag<T>) ImmutableEmptyBag.INSTANCE;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Iterable\>' to 'org.eclipse.collections.api.bag.ImmutableBag'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/immutable/ImmutableBagFactoryImpl.java`
#### Snippet
```java
        if (items instanceof ImmutableBag<?>)
        {
            return (ImmutableBag<T>) items;
        }
        if (items instanceof Bag<?>)
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Iterable\>' to 'org.eclipse.collections.api.bag.Bag'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/immutable/ImmutableBagFactoryImpl.java`
#### Snippet
```java
        if (items instanceof Bag<?>)
        {
            Bag<T> bag = (Bag<T>) items;
            if (bag.size() == 1)
            {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/immutable/ImmutableBagFactoryImpl.java`
#### Snippet
```java
            return new ImmutableHashBag<>(bag);
        }
        return this.of((T[]) Iterate.toArray(items));
    }
}
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.collection.MutableCollection\>' to 'org.eclipse.collections.api.bag.MutableBag'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/mutable/SynchronizedBag.java`
#### Snippet
```java
    public <P, V> MutableBag<V> collectWith(Function2<? super T, ? super P, ? extends V> function, P parameter)
    {
        return (MutableBag<V>) super.collectWith(function, parameter);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/immutable/ImmutableBagSerializationProxy.java`
#### Snippet
```java
        for (int i = 0; i < size; i++)
        {
            deserializedBag.addOccurrences((T) in.readObject(), in.readInt());
        }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.api.bag.MutableBag'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/mutable/UnmodifiableBag.java`
#### Snippet
```java
        public void readExternal(ObjectInput in) throws IOException, ClassNotFoundException
        {
            this.mutableBag = (MutableBag<T>) in.readObject();
        }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Comparator\>' to 'java.util.Comparator'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/sorted/immutable/AbstractImmutableSortedBag.java`
#### Snippet
```java
    public <S> ImmutableSortedBag<S> selectInstancesOf(Class<S> clazz)
    {
        Comparator<? super S> comparator = (Comparator<? super S>) this.comparator();
        MutableSortedBag<S> result = SortedBags.mutable.empty(comparator);
        this.forEachWithOccurrences((each, occurrences) ->
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/sorted/immutable/AbstractImmutableSortedBag.java`
#### Snippet
```java

        MutableList<RichIterable<T>> result = Lists.mutable.empty();
        T[] objects = (T[]) this.toArray();
        MutableCollection<T> batch = SortedBags.mutable.empty(this.comparator());
        int j = 0;
```

### UNCHECKED_WARNING
Unchecked cast: 'R' to 'org.eclipse.collections.api.bag.MutableBag'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/sorted/immutable/AbstractImmutableSortedBag.java`
#### Snippet
```java
        if (target instanceof MutableBag)
        {
            MutableBag<S> targetBag = (MutableBag<S>) target;
            this.forEachWithOccurrences((each, occurrences) ->
            {
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.tuple.Pair' to 'S'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/sorted/immutable/AbstractImmutableSortedBag.java`
#### Snippet
```java
                if (iterator.hasNext())
                {
                    targetBag.addOccurrences((S) Tuples.pair(each, iterator.next()), occurrences);
                }
            });
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Iterable\>' to 'java.util.List'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/immutable/AbstractImmutableBagIterable.java`
#### Snippet
```java
        else if (elements instanceof List)
        {
            List<T> toBeRemoved = (List<T>) elements;
            if (this.size() * toBeRemoved.size() > 10000)
            {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/AbstractMapIterable.java`
#### Snippet
```java
    public V getOrDefault(Object key, V defaultValue)
    {
        return this.getIfAbsentValue((K) key, defaultValue);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.bag.mutable.HashBag'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/mutable/MultiReaderHashBag.java`
#### Snippet
```java
    public void readExternal(ObjectInput in) throws IOException, ClassNotFoundException
    {
        this.delegate = (HashBag<T>) in.readObject();
        this.lock = new ReentrantReadWriteLock();
        this.lockWrapper = new ReadWriteLockWrapper(this.lock);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/fixed/EmptyMap.java`
#### Snippet
```java
            return false;
        }
        Map<K, V> that = (Map<K, V>) other;
        return that.size() == this.size();
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.map.FixedSizeMap,capture\>' to 'org.eclipse.collections.api.map.FixedSizeMap'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/fixed/FixedSizeMapFactoryImpl.java`
#### Snippet
```java
    public <K, V> FixedSizeMap<K, V> empty()
    {
        return (FixedSizeMap<K, V>) EMPTY_MAP;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/fixed/SingletonMap.java`
#### Snippet
```java
            return false;
        }
        Map<K, V> that = (Map<K, V>) other;
        return that.size() == this.size() && this.keyAndValueEquals(this.key1, this.value1, that);
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/fixed/SingletonMap.java`
#### Snippet
```java
    public void readExternal(ObjectInput in) throws IOException, ClassNotFoundException
    {
        this.key1 = (K) in.readObject();
        this.value1 = (V) in.readObject();
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/fixed/SingletonMap.java`
#### Snippet
```java
    {
        this.key1 = (K) in.readObject();
        this.value1 = (V) in.readObject();
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.bag.ImmutableBag\>' to 'org.eclipse.collections.api.bag.ImmutableBag'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/immutable/ImmutableEmptyBag.java`
#### Snippet
```java
            Function<? super T, ? extends V> function)
    {
        return (ImmutableBag<V>) INSTANCE;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.bag.ImmutableBag\>' to 'org.eclipse.collections.api.bag.ImmutableBag'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/immutable/ImmutableEmptyBag.java`
#### Snippet
```java
    public <S> ImmutableBag<S> selectInstancesOf(Class<S> clazz)
    {
        return (ImmutableBag<S>) INSTANCE;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.partition.bag.PartitionImmutableBag' to 'org.eclipse.collections.api.partition.bag.PartitionImmutableBag'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/immutable/ImmutableEmptyBag.java`
#### Snippet
```java
    public PartitionImmutableBag<T> partition(Predicate<? super T> predicate)
    {
        return (PartitionImmutableBag<T>) IMMUTABLE_EMPTY_PARTITION;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.bag.ImmutableBag\>' to 'org.eclipse.collections.api.bag.ImmutableBag'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/immutable/ImmutableEmptyBag.java`
#### Snippet
```java
    public <P, V> ImmutableBag<V> collectWith(Function2<? super T, ? super P, ? extends V> function, P parameter)
    {
        return (ImmutableBag<V>) INSTANCE;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.partition.bag.PartitionImmutableBag' to 'org.eclipse.collections.api.partition.bag.PartitionImmutableBag'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/immutable/ImmutableEmptyBag.java`
#### Snippet
```java
    public <P> PartitionImmutableBag<T> partitionWith(Predicate2<? super T, ? super P> predicate, P parameter)
    {
        return (PartitionImmutableBag<T>) IMMUTABLE_EMPTY_PARTITION;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.bag.ImmutableBag\>' to 'org.eclipse.collections.api.bag.ImmutableBag'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/immutable/ImmutableEmptyBag.java`
#### Snippet
```java
    public <V> ImmutableBag<V> flatCollect(Function<? super T, ? extends Iterable<V>> function)
    {
        return (ImmutableBag<V>) INSTANCE;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.bag.ImmutableBag\>' to 'org.eclipse.collections.api.bag.ImmutableBag'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/immutable/ImmutableEmptyBag.java`
#### Snippet
```java
    public <V> ImmutableBag<V> collect(Function<? super T, ? extends V> function)
    {
        return (ImmutableBag<V>) INSTANCE;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.LazyIterable\>' to 'org.eclipse.collections.api.LazyIterable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/immutable/ImmutableEmptyBag.java`
#### Snippet
```java
    public LazyIterable<T> asLazy()
    {
        return (LazyIterable<T>) LAZY_ITERABLE;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/immutable/ImmutableArrayBag.java`
#### Snippet
```java
    private ImmutableBag<T> newArrayBagWith(T element, int elementIndex, int distinctItemCount)
    {
        T[] newKeys = (T[]) new Object[distinctItemCount];
        int[] newCounts = new int[distinctItemCount];
        System.arraycopy(this.keys, 0, newKeys, 0, this.keys.length);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/immutable/ImmutableArrayBag.java`
#### Snippet
```java
        {
            int distinctItemCount = this.sizeDistinct() - (this.counts[elementIndex] == 1 ? 1 : 0);
            T[] newKeys = (T[]) new Object[distinctItemCount];
            int[] newCounts = new int[distinctItemCount];
            if (distinctItemCount == this.sizeDistinct())
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/immutable/ImmutableArrayBag.java`
#### Snippet
```java
    {
        int distinctItemCount = bag.sizeDistinct();
        T[] newKeys = (T[]) new Object[distinctItemCount];
        int[] newCounts = new int[distinctItemCount];
        bag.forEachWithOccurrences(new ObjectIntProcedure<T>()
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'org.eclipse.collections.api.map.MutableMap', required 'org.eclipse.collections.api.map.MutableMap'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/fixed/AbstractMemoryEfficientMutableMap.java`
#### Snippet
```java

    @Override
    public MutableMap<K, V> newEmpty(int capacity)
    {
        return UnifiedMap.newMap(capacity);
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.impl.bag.immutable.ImmutableSingletonBag' to 'org.eclipse.collections.api.bag.ImmutableBag'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/immutable/ImmutableSingletonBag.java`
#### Snippet
```java
    {
        return clazz.isInstance(this.value)
                ? (ImmutableBag<S>) this
                : Bags.immutable.empty();
    }
```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/immutable/ImmutableSingletonBag.java`
#### Snippet
```java
    public ImmutableBag<T> newWith(T element)
    {
        return Bags.immutable.with(this.value, element);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/fixed/TripletonMap.java`
#### Snippet
```java
    public void readExternal(ObjectInput in) throws IOException, ClassNotFoundException
    {
        this.key1 = (K) in.readObject();
        this.value1 = (V) in.readObject();
        this.key2 = (K) in.readObject();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/fixed/TripletonMap.java`
#### Snippet
```java
    {
        this.key1 = (K) in.readObject();
        this.value1 = (V) in.readObject();
        this.key2 = (K) in.readObject();
        this.value2 = (V) in.readObject();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/fixed/TripletonMap.java`
#### Snippet
```java
        this.key1 = (K) in.readObject();
        this.value1 = (V) in.readObject();
        this.key2 = (K) in.readObject();
        this.value2 = (V) in.readObject();
        this.key3 = (K) in.readObject();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/fixed/TripletonMap.java`
#### Snippet
```java
        this.value1 = (V) in.readObject();
        this.key2 = (K) in.readObject();
        this.value2 = (V) in.readObject();
        this.key3 = (K) in.readObject();
        this.value3 = (V) in.readObject();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/fixed/TripletonMap.java`
#### Snippet
```java
        this.key2 = (K) in.readObject();
        this.value2 = (V) in.readObject();
        this.key3 = (K) in.readObject();
        this.value3 = (V) in.readObject();
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/fixed/TripletonMap.java`
#### Snippet
```java
        this.value2 = (V) in.readObject();
        this.key3 = (K) in.readObject();
        this.value3 = (V) in.readObject();
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/fixed/TripletonMap.java`
#### Snippet
```java
            return false;
        }
        Map<K, V> that = (Map<K, V>) other;
        return that.size() == this.size()
                && this.keyAndValueEquals(this.key1, this.value1, that)
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/fixed/DoubletonMap.java`
#### Snippet
```java
    public void readExternal(ObjectInput in) throws IOException, ClassNotFoundException
    {
        this.key1 = (K) in.readObject();
        this.value1 = (V) in.readObject();
        this.key2 = (K) in.readObject();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/fixed/DoubletonMap.java`
#### Snippet
```java
    {
        this.key1 = (K) in.readObject();
        this.value1 = (V) in.readObject();
        this.key2 = (K) in.readObject();
        this.value2 = (V) in.readObject();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/fixed/DoubletonMap.java`
#### Snippet
```java
        this.key1 = (K) in.readObject();
        this.value1 = (V) in.readObject();
        this.key2 = (K) in.readObject();
        this.value2 = (V) in.readObject();
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/fixed/DoubletonMap.java`
#### Snippet
```java
        this.value1 = (V) in.readObject();
        this.key2 = (K) in.readObject();
        this.value2 = (V) in.readObject();
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/fixed/DoubletonMap.java`
#### Snippet
```java
            return false;
        }
        Map<K, V> that = (Map<K, V>) other;
        return that.size() == this.size()
                && this.keyAndValueEquals(this.key1, this.value1, that)
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Comparator'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/sorted/immutable/ImmutableSortedMapSerializationProxy.java`
#### Snippet
```java
    public void readExternal(ObjectInput in) throws IOException, ClassNotFoundException
    {
        Comparator<? super K> comparator = (Comparator<? super K>) in.readObject();
        int size = in.readInt();
        MutableSortedMap<K, V> deserializedMap = TreeSortedMap.newMap(comparator);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/sorted/immutable/ImmutableSortedMapSerializationProxy.java`
#### Snippet
```java
        for (int i = 0; i < size; i++)
        {
            deserializedMap.put((K) in.readObject(), (V) in.readObject());
        }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/sorted/immutable/ImmutableSortedMapSerializationProxy.java`
#### Snippet
```java
        for (int i = 0; i < size; i++)
        {
            deserializedMap.put((K) in.readObject(), (V) in.readObject());
        }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.SortedMap' to 'org.eclipse.collections.api.map.sorted.ImmutableSortedMap'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/sorted/immutable/ImmutableSortedMapFactoryImpl.java`
#### Snippet
```java
        if (map instanceof ImmutableSortedMap)
        {
            return (ImmutableSortedMap<K, V>) map;
        }
        if (map.isEmpty())
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.map.sorted.ImmutableSortedMap,capture\>' to 'org.eclipse.collections.api.map.sorted.ImmutableSortedMap'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/sorted/immutable/ImmutableSortedMapFactoryImpl.java`
#### Snippet
```java
    public <K, V> ImmutableSortedMap<K, V> empty()
    {
        return (ImmutableSortedMap<K, V>) ImmutableEmptySortedMap.INSTANCE;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Map,capture\>' to 'java.util.SortedMap'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/sorted/mutable/TreeSortedMap.java`
#### Snippet
```java
        if (map instanceof SortedMap<?, ?>)
        {
            return new TreeSortedMap<>((SortedMap<K, V>) map);
        }
        return new TreeSortedMap<>(map);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Comparator'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/sorted/mutable/TreeSortedMap.java`
#### Snippet
```java
    public void readExternal(ObjectInput in) throws IOException, ClassNotFoundException
    {
        this.treeMap = new TreeMap<>((Comparator<? super K>) in.readObject());
        int size = in.readInt();
        for (int i = 0; i < size; ++i)
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/sorted/mutable/TreeSortedMap.java`
#### Snippet
```java
        for (int i = 0; i < size; ++i)
        {
            this.treeMap.put((K) in.readObject(), (V) in.readObject());
        }
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/sorted/mutable/TreeSortedMap.java`
#### Snippet
```java
        for (int i = 0; i < size; ++i)
        {
            this.treeMap.put((K) in.readObject(), (V) in.readObject());
        }
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/sorted/immutable/ImmutableEmptySortedMap.java`
#### Snippet
```java
        }

        return ((Map<K, V>) other).isEmpty();
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/sorted/immutable/ImmutableTreeMap.java`
#### Snippet
```java
        {
            T[] result = a.length < this.size()
                    ? (T[]) Array.newInstance(a.getClass().getComponentType(), this.size())
                    : a;

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/sorted/immutable/ImmutableTreeMap.java`
#### Snippet
```java
    public V get(Object key)
    {
        int index = Arrays.binarySearch(this.keys, (K) key, this.comparator);
        if (index >= 0)
        {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/sorted/immutable/ImmutableTreeMap.java`
#### Snippet
```java
    public boolean containsKey(Object key)
    {
        return Arrays.binarySearch(this.keys, (K) key, this.comparator) >= 0;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'K\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/sorted/immutable/ImmutableTreeMap.java`
#### Snippet
```java
        }
        this.comparator = sortedMap.comparator();
        K[] keysCopy = (K[]) new Object[sortedMap.size()];
        V[] valuesCopy = (V[]) new Object[sortedMap.size()];
        int index = 0;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'V\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/sorted/immutable/ImmutableTreeMap.java`
#### Snippet
```java
        this.comparator = sortedMap.comparator();
        K[] keysCopy = (K[]) new Object[sortedMap.size()];
        V[] valuesCopy = (V[]) new Object[sortedMap.size()];
        int index = 0;
        for (Entry<K, V> entry : sortedMap.entrySet())
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.RichIterable\>' to 'org.eclipse.collections.api.list.MutableList'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/sorted/mutable/SynchronizedSortedMap.java`
#### Snippet
```java
            Function<? super V, ? extends R> function)
    {
        return (MutableList<R>) super.collectIf(predicate, function);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.map.MutableMapIterable,V\>' to 'org.eclipse.collections.api.map.MutableMap'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/sorted/mutable/SynchronizedSortedMap.java`
#### Snippet
```java
    public <VV> MutableMap<VV, V> groupByUniqueKey(Function<? super V, ? extends VV> function)
    {
        return (MutableMap<VV, V>) super.groupByUniqueKey(function);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.RichIterable\>' to 'org.eclipse.collections.api.list.MutableList'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/sorted/mutable/SynchronizedSortedMap.java`
#### Snippet
```java
    public <R> MutableList<R> collect(Function<? super V, ? extends R> function)
    {
        return (MutableList<R>) super.collect(function);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.api.bimap.MutableBiMap'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/SynchronizedBiMapSerializationProxy.java`
#### Snippet
```java
    public void readExternal(ObjectInput in) throws IOException, ClassNotFoundException
    {
        this.map = (MutableBiMap<K, V>) in.readObject();
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.api.map.MutableMapIterable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/SynchronizedMapSerializationProxy.java`
#### Snippet
```java
    public void readExternal(ObjectInput in) throws IOException, ClassNotFoundException
    {
        this.map = (MutableMapIterable<K, V>) in.readObject();
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.map.MutableMapIterable,V2\>' to 'org.eclipse.collections.api.map.MutableMap'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/SynchronizedMutableMap.java`
#### Snippet
```java
            Function2<? super V2, ? super V1, ? extends V2> nonMutatingAggregator)
    {
        return (MutableMap<K1, V2>) super.aggregateBy(keyFunction, valueFunction, zeroValueFactory, nonMutatingAggregator);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.map.MutableMapIterable,V\>' to 'org.eclipse.collections.api.map.MutableMap'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/SynchronizedMutableMap.java`
#### Snippet
```java
    public <VV> MutableMap<VV, V> groupByUniqueKey(Function<? super V, ? extends VV> function)
    {
        return (MutableMap<VV, V>) super.groupByUniqueKey(function);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.map.MutableMapIterable,VV\>' to 'org.eclipse.collections.api.map.MutableMap'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/SynchronizedMutableMap.java`
#### Snippet
```java
            Function2<? super VV, ? super V, ? extends VV> nonMutatingAggregator)
    {
        return (MutableMap<KK, VV>) super.aggregateBy(groupBy, zeroValueFactory, nonMutatingAggregator);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.RichIterable\>' to 'org.eclipse.collections.api.bag.MutableBag'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/SynchronizedMutableMap.java`
#### Snippet
```java
    public <A> MutableBag<A> collect(Function<? super V, ? extends A> function)
    {
        return (MutableBag<A>) super.collect(function);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/immutable/ImmutableEmptyMapWithHashingStrategy.java`
#### Snippet
```java
        }

        return ((Map<K, V>) other).isEmpty();
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.api.block.HashingStrategy'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/immutable/ImmutableMapWithHashingStrategySerializationProxy.java`
#### Snippet
```java
    public void readExternal(ObjectInput in) throws IOException, ClassNotFoundException
    {
        HashingStrategy<? super K> strategy = (HashingStrategy<? super K>) in.readObject();
        int size = in.readInt();
        MutableMap<K, V> deserializedMap = UnifiedMapWithHashingStrategy.newMap(strategy);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/immutable/ImmutableMapWithHashingStrategySerializationProxy.java`
#### Snippet
```java
        for (int i = 0; i < size; i++)
        {
            deserializedMap.put((K) in.readObject(), (V) in.readObject());
        }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/immutable/ImmutableMapWithHashingStrategySerializationProxy.java`
#### Snippet
```java
        for (int i = 0; i < size; i++)
        {
            deserializedMap.put((K) in.readObject(), (V) in.readObject());
        }

```

### UNCHECKED_WARNING
Unchecked cast: 'capture' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/immutable/ImmutableEntryWithHashingStrategy.java`
#### Snippet
```java
        {
            Entry<?, ?> that = (Entry<?, ?>) object;
            return this.hashingStrategy.equals(this.key, (K) that.getKey())
                    && Objects.equals(this.value, that.getValue());
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/primitive/ObjectBooleanHashMapWithHashingStrategy.java`
#### Snippet
```java
    private K toNonSentinel(Object key)
    {
        return key == NULL_KEY ? null : (K) key;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/primitive/ObjectBooleanHashMapWithHashingStrategy.java`
#### Snippet
```java
                    this.position++;
                }
                K result = (K) ObjectBooleanHashMapWithHashingStrategy.this.keys[this.position];
                this.count++;
                this.position++;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/primitive/ObjectBooleanHashMapWithHashingStrategy.java`
#### Snippet
```java
                this.position++;
            }
            this.currentKey = (K) ObjectBooleanHashMapWithHashingStrategy.this.keys[this.position];
            this.isCurrentKeySet = true;
            this.position++;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/primitive/ObjectBooleanHashMapWithHashingStrategy.java`
#### Snippet
```java
            for (Object object : collection)
            {
                ObjectBooleanHashMapWithHashingStrategy.this.removeKey((K) object);
            }
            return oldSize != ObjectBooleanHashMapWithHashingStrategy.this.size();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/primitive/ObjectBooleanHashMapWithHashingStrategy.java`
#### Snippet
```java
        {
            int oldSize = ObjectBooleanHashMapWithHashingStrategy.this.size();
            ObjectBooleanHashMapWithHashingStrategy.this.removeKey((K) key);
            return oldSize != ObjectBooleanHashMapWithHashingStrategy.this.size();
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/primitive/ObjectBooleanHashMapWithHashingStrategy.java`
#### Snippet
```java
    public void remove(Object key)
    {
        this.removeKey((K) key);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/primitive/ObjectBooleanHashMapWithHashingStrategy.java`
#### Snippet
```java
            if (result.length < size)
            {
                result = (T[]) Array.newInstance(result.getClass().getComponentType(), size);
            }
            this.copyKeys(result);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.api.block.HashingStrategy'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/primitive/ObjectBooleanHashMapWithHashingStrategy.java`
#### Snippet
```java
    public void readExternal(ObjectInput in) throws IOException, ClassNotFoundException
    {
        this.hashingStrategy = (HashingStrategy<? super K>) in.readObject();
        int size = in.readInt();
        int capacity = this.smallestPowerOfTwoGreaterThan(this.fastCeil(size * OCCUPIED_DATA_RATIO));
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/primitive/ObjectBooleanHashMapWithHashingStrategy.java`
#### Snippet
```java
        for (int i = 0; i < size; i++)
        {
            this.put((K) in.readObject(), in.readBoolean());
        }
    }
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.eclipse.collections.impl.map.mutable.primitive.ObjectBooleanHashMapWithHashingStrategy' to 'org.eclipse.collections.impl.map.mutable.primitive.ObjectBooleanHashMapWithHashingStrategy'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/primitive/ObjectBooleanHashMapWithHashingStrategy.java`
#### Snippet
```java
    {
        //noinspection rawtypes
        return new ObjectBooleanHashMapWithHashingStrategy<>(map.hashingStrategy, map);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/immutable/ImmutableMapSerializationProxy.java`
#### Snippet
```java
        for (int i = 0; i < size; i++)
        {
            if (deserializedMap.put((K) in.readObject(), (V) in.readObject()) != null)
            {
                throw new IllegalStateException();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/immutable/ImmutableMapSerializationProxy.java`
#### Snippet
```java
        for (int i = 0; i < size; i++)
        {
            if (deserializedMap.put((K) in.readObject(), (V) in.readObject()) != null)
            {
                throw new IllegalStateException();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/primitive/ObjectBooleanHashMap.java`
#### Snippet
```java
            if (result.length < size)
            {
                result = (T[]) Array.newInstance(result.getClass().getComponentType(), size);
            }
            this.copyKeys(result);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/primitive/ObjectBooleanHashMap.java`
#### Snippet
```java
            for (Object object : collection)
            {
                ObjectBooleanHashMap.this.removeKey((K) object);
            }
            return oldSize != ObjectBooleanHashMap.this.size();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/primitive/ObjectBooleanHashMap.java`
#### Snippet
```java
                this.position++;
            }
            this.currentKey = (K) ObjectBooleanHashMap.this.keys[this.position];
            this.isCurrentKeySet = true;
            this.position++;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/primitive/ObjectBooleanHashMap.java`
#### Snippet
```java
                    this.position++;
                }
                K result = (K) ObjectBooleanHashMap.this.keys[this.position];
                this.count++;
                this.position++;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/primitive/ObjectBooleanHashMap.java`
#### Snippet
```java
        {
            int oldSize = ObjectBooleanHashMap.this.size();
            ObjectBooleanHashMap.this.removeKey((K) key);
            return oldSize != ObjectBooleanHashMap.this.size();
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/primitive/ObjectBooleanHashMap.java`
#### Snippet
```java
    private K toNonSentinel(Object key)
    {
        return key == NULL_KEY ? null : (K) key;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/primitive/ObjectBooleanHashMap.java`
#### Snippet
```java
    public void remove(Object key)
    {
        this.removeKey((K) key);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/primitive/ObjectBooleanHashMap.java`
#### Snippet
```java
        for (int i = 0; i < size; i++)
        {
            this.put((K) in.readObject(), in.readBoolean());
        }
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/immutable/ImmutableEmptyMap.java`
#### Snippet
```java
        }

        return ((Map<K, V>) other).isEmpty();
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            return false;
        }
        if (cur != CHAINED_KEY && this.nonNullTableObjectEquals(cur, (K) key))
        {
            return true;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            return true;
        }
        return cur == CHAINED_KEY && this.chainContainsKey((Object[]) this.table[index + 1], (K) key);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.map.MutableMap' to 'org.eclipse.collections.impl.map.mutable.UnifiedMap'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
    public <R> MutableMap<K, R> collectValues(Function2<? super K, ? super V, ? extends R> function)
    {
        UnifiedMap<K, R> target = (UnifiedMap<K, R>) this.newEmpty();
        target.loadFactor = this.loadFactor;
        target.occupied = this.occupied;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
                    {
                        chainedTargetTable[j] = chainedTable[j];
                        chainedTargetTable[j + 1] = function.value(this.nonSentinel(chainedTable[j]), (V) chainedTable[j + 1]);
                    }
                }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            else if (this.table[i] != null)
            {
                target.table[i + 1] = function.value(this.nonSentinel(this.table[i]), (V) this.table[i + 1]);
            }
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
                return;
            }
            procedure.value((V) chain[i + 1], parameter);
        }
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
                    if (chain[j] != null)
                    {
                        this.put(this.nonSentinel(chain[j]), (V) chain[j + 1]);
                    }
                }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            else if (cur != null)
            {
                this.put(this.nonSentinel(cur), (V) old[i + 1]);
            }
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
                else if (cur != null)
                {
                    procedure.value(ImmutableEntry.of(UnifiedMap.this.nonSentinel(cur), (V) UnifiedMap.this.table[i + 1]));
                }
            }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            else if (cur != null)
            {
                this.put(this.nonSentinel(cur), (V) unifiedMap.table[i + 1]);
            }
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
                    {
                        K key = this.nonSentinel(chainedTable[j]);
                        V value = (V) chainedTable[j + 1];
                        if (predicate.accept(key, value))
                        {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            {
                K key = this.nonSentinel(this.table[i]);
                V value = (V) this.table[i + 1];

                if (predicate.accept(key, value))
```

### UNCHECKED_WARNING
Unchecked cast: 'capture' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            }
            Entry<?, ?> entry = (Entry<?, ?>) e;
            K key = (K) entry.getKey();
            V value = (V) entry.getValue();

```

### UNCHECKED_WARNING
Unchecked cast: 'capture' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            Entry<?, ?> entry = (Entry<?, ?>) e;
            K key = (K) entry.getKey();
            V value = (V) entry.getValue();

            int index = UnifiedMap.this.index(key);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
                    if (chainedTable[j] != null)
                    {
                        V value = (V) chainedTable[j + 1];
                        if (predicate.accept(value))
                        {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            else if (this.table[i] != null)
            {
                V value = (V) this.table[i + 1];

                if (predicate.accept(value))
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'org.eclipse.collections.api.map.MutableMap', required 'org.eclipse.collections.api.map.MutableMap'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java

    @Override
    public MutableMap<K, V> newEmpty(int capacity)
    {
        return new UnifiedMap<>(capacity, this.loadFactor);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
                return;
            }
            procedure.value(this.nonSentinel(cur), (V) chain[i + 1]);
        }
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
                else if (cur != null)
                {
                    procedure.value(ImmutableEntry.of(UnifiedMap.this.nonSentinel(cur), (V) map[i + 1]));
                }
            }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
                    if (chainedTable[j] != null)
                    {
                        V value = (V) chainedTable[j + 1];
                        if (predicate.accept(value, parameter))
                        {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            else if (this.table[i] != null)
            {
                V value = (V) this.table[i + 1];

                if (predicate.accept(value, parameter))
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            }
            K key = this.nonSentinel(cur);
            V value = (V) chain[i + 1];
            Object otherValue = other.get(key);
            if (!UnifiedMap.nullSafeEquals(otherValue, value) || (value == null && otherValue == null && !other.containsKey(key)))
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
                            }
                            result[count++] =
                                    new WeakBoundEntry<>(UnifiedMap.this.nonSentinel(cur), (V) chain[j + 1], this.holder);
                        }
                    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
                    else
                    {
                        result[count++] = new WeakBoundEntry<>(UnifiedMap.this.nonSentinel(x), (V) table[i + 1], this.holder);
                    }
                }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
                    if (UnifiedMap.nullSafeEquals(value, curValue))
                    {
                        return ImmutableEntry.of(UnifiedMap.this.nonSentinel(cur), (V) curValue);
                    }
                }
```

### UNCHECKED_WARNING
Unchecked cast: 'capture' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
        private Entry<K, V> getEntry(Entry<?, ?> entry)
        {
            K key = (K) entry.getKey();
            V value = (V) entry.getValue();
            int index = UnifiedMap.this.index(key);
```

### UNCHECKED_WARNING
Unchecked cast: 'capture' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
        {
            K key = (K) entry.getKey();
            V value = (V) entry.getValue();
            int index = UnifiedMap.this.index(key);

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
                if (UnifiedMap.nullSafeEquals(value, curValue))
                {
                    return ImmutableEntry.of(UnifiedMap.this.nonSentinel(cur), (V) curValue);
                }
            }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
                if (this.nonNullTableObjectEquals(chain[i], key))
                {
                    V result = (V) chain[i + 1];
                    chain[i + 1] = value;
                    return result;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
                else if (cur != null)
                {
                    replace.add((V) UnifiedMap.this.table[i + 1]);
                }
            }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            if (result.length < size)
            {
                result = (T[]) Array.newInstance(result.getClass().getComponentType(), size);
            }
            this.copyKeys(result);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
                    if (chainedTable[j] != null)
                    {
                        V value = (V) chainedTable[j + 1];
                        if (predicate.accept(value, parameter) == expected)
                        {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            else if (this.table[i] != null)
            {
                V value = (V) this.table[i + 1];

                if (predicate.accept(value, parameter) == expected)
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            if (result.length < size)
            {
                result = (T[]) Array.newInstance(result.getClass().getComponentType(), size);
            }
            this.copyValues(result);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            this.position += 2;
            this.lastReturned = true;
            return (V) val;
        }
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
                if (UnifiedMap.this.nonNullTableObjectEquals(k, key))
                {
                    other.put(UnifiedMap.this.nonSentinel(k), (V) chain[i + 1]);
                }
            }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            if (this.nonNullTableObjectEquals(k, key))
            {
                V val = (V) chain[i + 1];
                this.overwriteWithLastElementFromChain(chain, index, i);
                return val;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            this.position += 2;
            this.lastReturned = true;
            return new WeakBoundEntry<>(UnifiedMap.this.nonSentinel(cur), (V) value, this.holder);
        }
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
        if (cur != CHAINED_KEY && this.nonNullTableObjectEquals(cur, key))
        {
            return (V) this.table[index + 1];
        }
        return this.chainedGetIfAbsentPutWith(key, index, function, parameter);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            {
                K key = this.nonSentinel(cur);
                V value = (V) this.table[i + 1];
                Object otherValue = other.get(key);
                if (!UnifiedMap.nullSafeEquals(otherValue, value) || (value == null && otherValue == null && !other.containsKey(key)))
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
        if (cur != CHAINED_KEY && this.nonNullTableObjectEquals(cur, key))
        {
            V oldValue = (V) this.table[index + 1];
            V newValue = function.valueOf(oldValue);
            this.table[index + 1] = newValue;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
                    if (chainedTable[j] != null)
                    {
                        V value = (V) chainedTable[j + 1];
                        if (predicate.accept(value))
                        {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            else if (this.table[i] != null)
            {
                V value = (V) this.table[i + 1];

                if (predicate.accept(value))
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Map,capture\>' to 'org.eclipse.collections.impl.map.mutable.UnifiedMap'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
        if (map instanceof UnifiedMap<?, ?>)
        {
            this.copyMap((UnifiedMap<K, V>) map);
        }
        else if (map instanceof UnsortedMapIterable)
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Map,capture\>' to 'org.eclipse.collections.api.map.MapIterable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
        else if (map instanceof UnsortedMapIterable)
        {
            MapIterable<K, V> mapIterable = (MapIterable<K, V>) map;
            mapIterable.forEachKeyValue(this::put);
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
                    return;
                }
                replace.add((V) chain[i + 1]);
            }
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
        {
            Object[] chain = (Object[]) UnifiedMap.this.table[this.position + 1];
            V val = (V) chain[this.chainPosition + 1];
            this.chainPosition += 2;
            if (this.chainPosition >= chain.length
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            else if (value == null && this.table[i - 1] != null || value != null)
            {
                procedure.value((V) value);
            }
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            }
            this.lastReturned = true;
            return new WeakBoundEntry<>(UnifiedMap.this.nonSentinel(cur), (V) value, this.holder);
        }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            else if (cur != null)
            {
                procedure.value((V) this.table[i + 1], parameter);
            }
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
                return index;
            }
            objectIntProcedure.value((V) chain[i + 1], index++);
        }
        return index;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            else if (cur != null)
            {
                procedure.value(this.nonSentinel(cur), (V) this.table[i + 1]);
            }
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
                    return;
                }
                procedure.value(ImmutableEntry.of(UnifiedMap.this.nonSentinel(cur), (V) chain[i + 1]));
            }
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            if (cur == CHAINED_KEY)
            {
                return this.getFromChain((Object[]) val, (K) key);
            }
            if (this.nonNullTableObjectEquals(cur, (K) key))
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
                return this.getFromChain((Object[]) val, (K) key);
            }
            if (this.nonNullTableObjectEquals(cur, (K) key))
            {
                return (V) val;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            if (this.nonNullTableObjectEquals(cur, (K) key))
            {
                return (V) val;
            }
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
                    if (chainedTable[j] != null)
                    {
                        V value = (V) chainedTable[j + 1];
                        if (predicate.accept(value))
                        {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            else if (this.table[i] != null)
            {
                V value = (V) this.table[i + 1];

                if (predicate.accept(value))
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
        if (cur != CHAINED_KEY && this.nonNullTableObjectEquals(cur, key))
        {
            return (V) this.table[index + 1];
        }
        return this.chainedGetIfAbsentPut(key, index, function);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
        if (cur != CHAINED_KEY && this.nonNullTableObjectEquals(cur, key))
        {
            V oldValue = (V) this.table[index + 1];
            V newValue = function.value(oldValue, parameter);
            this.table[index + 1] = newValue;
```

### UNCHECKED_WARNING
Unchecked cast: 'capture' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            {
                Entry<?, ?> other = (Entry<?, ?>) obj;
                K otherKey = (K) other.getKey();
                V otherValue = (V) other.getValue();
                return UnifiedMap.nullSafeEquals(this.key, otherKey)
```

### UNCHECKED_WARNING
Unchecked cast: 'capture' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
                Entry<?, ?> other = (Entry<?, ?>) obj;
                K otherKey = (K) other.getKey();
                V otherValue = (V) other.getValue();
                return UnifiedMap.nullSafeEquals(this.key, otherKey)
                        && UnifiedMap.nullSafeEquals(this.value, otherValue);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            if (this.table[i] == CHAINED_KEY)
            {
                if (this.chainedContainsValue((Object[]) this.table[i + 1], (V) value))
                {
                    return true;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
                    {
                        K key = this.nonSentinel(chainedTable[j]);
                        V value = (V) chainedTable[j + 1];
                        if (predicate.accept(key, value))
                        {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            {
                K key = this.nonSentinel(this.table[i]);
                V value = (V) this.table[i + 1];

                if (predicate.accept(key, value))
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
                if (UnifiedMap.this.nonNullTableObjectEquals(k, key))
                {
                    V val = (V) chain[i + 1];
                    if (UnifiedMap.nullSafeEquals(val, value))
                    {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
                if (this.nonNullTableObjectEquals(chain[i], key))
                {
                    result = (V) chain[i + 1];
                    break;
                }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
                    if (chainedTable[j] != null)
                    {
                        V value = (V) chainedTable[j + 1];
                        if (predicate.accept(value, parameter))
                        {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            else if (this.table[i] != null)
            {
                V value = (V) this.table[i + 1];

                if (predicate.accept(value, parameter))
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            {
                Object[] chain = (Object[]) this.table[i + 1];
                return (V) chain[1];
            }
            if (cur != null)
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            if (cur != null)
            {
                return (V) this.table[i + 1];
            }
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
                    }
                    K key = this.nonSentinel(chain[chIndex]);
                    V value = (V) chain[chIndex + 1];
                    if (predicate.accept(key, value))
                    {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            {
                K key = this.nonSentinel(cur);
                V value = (V) this.table[index + 1];
                if (predicate.accept(key, value))
                {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
                if (this.nonNullTableObjectEquals(chain[i], key))
                {
                    V oldValue = (V) chain[i + 1];
                    V result = function.value(oldValue, parameter);
                    chain[i + 1] = result;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
    private K nonSentinel(Object key)
    {
        return key == NULL_KEY ? null : (K) key;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
        for (int i = 0; i < size; i++)
        {
            this.put((K) in.readObject(), (V) in.readObject());
        }
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
        for (int i = 0; i < size; i++)
        {
            this.put((K) in.readObject(), (V) in.readObject());
        }
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
        if (cur != CHAINED_KEY && this.nonNullTableObjectEquals(cur, key))
        {
            V result = (V) this.table[index + 1];
            this.table[index + 1] = value;
            return result;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            if (cur == CHAINED_KEY)
            {
                return this.removeFromChain((Object[]) val, (K) key, index);
            }
            if (this.nonNullTableObjectEquals(cur, (K) key))
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
                return this.removeFromChain((Object[]) val, (K) key, index);
            }
            if (this.nonNullTableObjectEquals(cur, (K) key))
            {
                this.table[index] = null;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
                this.table[index + 1] = null;
                this.occupied--;
                return (V) val;
            }
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
                if (this.nonNullTableObjectEquals(chain[i], key))
                {
                    V oldValue = (V) chain[i + 1];
                    V result = function.valueOf(oldValue);
                    chain[i + 1] = result;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
        if (cur != CHAINED_KEY && this.nonNullTableObjectEquals(cur, key))
        {
            return (V) this.table[index + 1];
        }
        return this.chainedGetIfAbsentPut(key, index, value);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
                if (cur == CHAINED_KEY)
                {
                    this.putIfFoundFromChain((Object[]) val, (K) key, other);
                    return;
                }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
                    return;
                }
                if (UnifiedMap.this.nonNullTableObjectEquals(cur, (K) key))
                {
                    other.put(UnifiedMap.this.nonSentinel(cur), (V) val);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
                if (UnifiedMap.this.nonNullTableObjectEquals(cur, (K) key))
                {
                    other.put(UnifiedMap.this.nonSentinel(cur), (V) val);
                }
            }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
                break;
            }
            this.put(this.nonSentinel(cur), (V) chain[j + 1]);
        }
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            else if (cur != null)
            {
                procedure.value((V) this.table[i + 1]);
            }
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
                if (this.nonNullTableObjectEquals(chain[i], key))
                {
                    result = (V) chain[i + 1];
                    break;
                }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
                    if (chainedTable[j] != null)
                    {
                        V value = (V) chainedTable[j + 1];
                        if (predicate.accept(value, parameter))
                        {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            else if (this.table[i] != null)
            {
                V value = (V) this.table[i + 1];

                if (predicate.accept(value, parameter))
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            if (result.length < size)
            {
                result = (T[]) Array.newInstance(result.getClass().getComponentType(), size);
            }
            this.copyEntries(result);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
                    if (chainedTable[j] != null)
                    {
                        V value = (V) chainedTable[j + 1];
                        if (predicate.accept(value) == expected)
                        {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            else if (this.table[i] != null)
            {
                V value = (V) this.table[i + 1];

                if (predicate.accept(value) == expected)
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            if (this.nonNullTableObjectEquals(k, key))
            {
                return (V) chain[i + 1];
            }
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
                    if (cur != null)
                    {
                        this.putForTrim((K) cur, (V) chain[i + 1], j, mask);
                    }
                }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
                    if (cur != null)
                    {
                        this.putForTrim((K) cur, (V) chain[i + 1], j, mask);
                    }
                }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            else if (key != null)
            {
                this.putForTrim((K) key, (V) temp[j + 1], j, mask);
            }
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            else if (key != null)
            {
                this.putForTrim((K) key, (V) temp[j + 1], j, mask);
            }
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            else if (cur != null)
            {
                objectIntProcedure.value((V) this.table[i + 1], index++);
            }
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
                return;
            }
            procedure.value((V) chain[i + 1]);
        }
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
                if (this.nonNullTableObjectEquals(chain[i], key))
                {
                    result = (V) chain[i + 1];
                    break;
                }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/immutable/ImmutableSingletonMap.java`
#### Snippet
```java
            return false;
        }
        Map<K, V> that = (Map<K, V>) other;
        return that.size() == this.size() && this.keyAndValueEquals(this.key1, this.value1, that);
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                return false;
            }
            Map.Entry<K, V> e = (Map.Entry<K, V>) o;
            Entry<K, V> candidate = ConcurrentHashMap.this.getEntry(e.getKey());
            return e.equals(candidate);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMap.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
            else
            {
                Entry<K, V> e = (Entry<K, V>) o;
                while (e != null)
                {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMap.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                    key = keyTransformer.value(key, newValue);
                }
                Entry<K, V> newEntry = new Entry<>(key, newValue, (Entry<K, V>) o);
                if (currentArray.compareAndSet(index, o, newEntry))
                {
```

### UNCHECKED_WARNING
Unchecked call to 'compareAndSet(int, E, E)' as a member of raw type 'java.util.concurrent.atomic.AtomicReferenceArray'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                }
                Entry<K, V> newEntry = new Entry<>(key, newValue, (Entry<K, V>) o);
                if (currentArray.compareAndSet(index, o, newEntry))
                {
                    this.incrementSizeAndPossiblyResize(currentArray, length, o);
```

### UNCHECKED_WARNING
Unchecked call to 'set(int, E)' as a member of raw type 'java.util.concurrent.atomic.AtomicReferenceArray'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                if (oldTable.get(end) == null)
                {
                    oldTable.set(end, RESIZE_SENTINEL);
                    if (this.partitionedSize == null && newSize >= PARTITIONED_SIZE_THRESHOLD)
                    {
```

### UNCHECKED_WARNING
Unchecked call to 'set(int, E)' as a member of raw type 'java.util.concurrent.atomic.AtomicReferenceArray'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                    }
                    resizeContainer = new ResizeContainer(new AtomicReferenceArray(newSize), oldTable.length() - 1);
                    oldTable.set(end, resizeContainer);
                    ownResize = true;
                }
```

### UNCHECKED_WARNING
Unchecked call to 'compareAndSet(int, E, E)' as a member of raw type 'java.util.concurrent.atomic.AtomicReferenceArray'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
            V result = function.valueOf(factory.value());
            Entry<K, V> newEntry = new Entry<>(key, result, null);
            if (currentArray.compareAndSet(index, null, newEntry))
            {
                this.addToSize(1);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMap.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                throw new ConcurrentModificationException("can't iterate while resizing!");
            }
            Entry<K, V> e = (Entry<K, V>) o;
            while (e != null)
            {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
            {
                Object value = e.getValue();
                block.value((V) value);
                e = e.getNext();
            }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                return false;
            }
            Map.Entry<K, V> e = (Map.Entry<K, V>) o;
            K k1 = this.key;
            Object k2 = e.getKey();
```

### UNCHECKED_WARNING
Unchecked call to 'compareAndSet(int, E, E)' as a member of raw type 'java.util.concurrent.atomic.AtomicReferenceArray'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                    if (o == null)
                    {
                        if (src.compareAndSet(j, null, RESIZED))
                        {
                            j--;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMap.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                    else
                    {
                        Entry<K, V> e = (Entry<K, V>) o;
                        if (src.compareAndSet(j, o, RESIZING))
                        {
```

### UNCHECKED_WARNING
Unchecked call to 'compareAndSet(int, E, E)' as a member of raw type 'java.util.concurrent.atomic.AtomicReferenceArray'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                    {
                        Entry<K, V> e = (Entry<K, V>) o;
                        if (src.compareAndSet(j, o, RESIZING))
                        {
                            while (e != null)
```

### UNCHECKED_WARNING
Unchecked call to 'set(int, E)' as a member of raw type 'java.util.concurrent.atomic.AtomicReferenceArray'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                                e = e.getNext();
                            }
                            src.set(j, RESIZED);
                            j--;
                        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMap.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                throw new ConcurrentModificationException("can't iterate while resizing!");
            }
            Entry<K, V> e = (Entry<K, V>) o;
            while (e != null)
            {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                Object key = e.getKey();
                Object value = e.getValue();
                block.value((K) key, (V) value);
                e = e.getNext();
            }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                Object key = e.getKey();
                Object value = e.getValue();
                block.value((K) key, (V) value);
                e = e.getNext();
            }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Map,capture\>' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMap'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
        if (map instanceof ConcurrentHashMap<?, ?> && chunks > 1 && map.size() > 50000)
        {
            ConcurrentHashMap<K, V> incoming = (ConcurrentHashMap<K, V>) map;
            AtomicReferenceArray currentArray = incoming.table;
            FutureTask<?>[] futures = new FutureTask<?>[chunks];
```

### UNCHECKED_WARNING
Unchecked call to 'FutureTask(Runnable, V)' as a member of raw type 'java.util.concurrent.FutureTask'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                int start = i * chunkSize;
                int end = Math.min((i + 1) * chunkSize, currentArray.length());
                futures[i] = new FutureTask(() -> this.sequentialPutAll(currentArray, start, end), null);
                executor.execute(futures[i]);
            }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMap.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
            else
            {
                Entry<K, V> e = (Entry<K, V>) o;
                while (e != null)
                {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/immutable/ImmutableTripletonMap.java`
#### Snippet
```java
            return false;
        }
        Map<K, V> that = (Map<K, V>) other;
        return that.size() == this.size()
                && this.keyAndValueEquals(this.key1, this.value1, that)
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMap.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                    if (candidate.equals(key))
                    {
                        Entry<K, V> replacement = this.createReplacementChainForRemoval((Entry<K, V>) o, e);
                        if (currentArray.compareAndSet(index, o, replacement))
                        {
```

### UNCHECKED_WARNING
Unchecked call to 'compareAndSet(int, E, E)' as a member of raw type 'java.util.concurrent.atomic.AtomicReferenceArray'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                    {
                        Entry<K, V> replacement = this.createReplacementChainForRemoval((Entry<K, V>) o, e);
                        if (currentArray.compareAndSet(index, o, replacement))
                        {
                            this.addToSize(-1);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMap.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
            else
            {
                Entry<K, V> e = (Entry<K, V>) o;
                while (e != null)
                {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMap.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                    {
                        V oldValue = e.getValue();
                        Entry<K, V> newEntry = new Entry<>(e.getKey(), value, this.createReplacementChainForRemoval((Entry<K, V>) o, e));
                        if (!currentArray.compareAndSet(index, o, newEntry))
                        {
```

### UNCHECKED_WARNING
Unchecked call to 'compareAndSet(int, E, E)' as a member of raw type 'java.util.concurrent.atomic.AtomicReferenceArray'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                        V oldValue = e.getValue();
                        Entry<K, V> newEntry = new Entry<>(e.getKey(), value, this.createReplacementChainForRemoval((Entry<K, V>) o, e));
                        if (!currentArray.compareAndSet(index, o, newEntry))
                        {
                            //noinspection ContinueStatementWithLabel
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMap.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
            else
            {
                Entry<K, V> e = (Entry<K, V>) o;
                while (e != null)
                {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMap.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                        if (oldValue == e.getValue() || (oldValue != null && oldValue.equals(e.getValue())))
                        {
                            Entry<K, V> replacement = this.createReplacementChainForRemoval((Entry<K, V>) o, e);
                            Entry<K, V> newEntry = new Entry<>(key, newValue, replacement);
                            if (currentArray.compareAndSet(index, o, newEntry))
```

### UNCHECKED_WARNING
Unchecked call to 'compareAndSet(int, E, E)' as a member of raw type 'java.util.concurrent.atomic.AtomicReferenceArray'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                            Entry<K, V> replacement = this.createReplacementChainForRemoval((Entry<K, V>) o, e);
                            Entry<K, V> newEntry = new Entry<>(key, newValue, replacement);
                            if (currentArray.compareAndSet(index, o, newEntry))
                            {
                                return true;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMap.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
            else
            {
                Entry<K, V> e = (Entry<K, V>) o;
                while (e != null)
                {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMap.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                else
                {
                    newEntry = new Entry<>(toCopyEntry.getKey(), toCopyEntry.getValue(), (Entry<K, V>) o);
                }
                if (currentArray.compareAndSet(index, o, newEntry))
```

### UNCHECKED_WARNING
Unchecked call to 'compareAndSet(int, E, E)' as a member of raw type 'java.util.concurrent.atomic.AtomicReferenceArray'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                    newEntry = new Entry<>(toCopyEntry.getKey(), toCopyEntry.getValue(), (Entry<K, V>) o);
                }
                if (currentArray.compareAndSet(index, o, newEntry))
                {
                    return;
```

### UNCHECKED_WARNING
Unchecked call to 'compareAndSet(int, E, E)' as a member of raw type 'java.util.concurrent.atomic.AtomicReferenceArray'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
            if (o == null)
            {
                if (src.compareAndSet(j, null, RESIZED))
                {
                    j++;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMap.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
            else
            {
                Entry<K, V> e = (Entry<K, V>) o;
                if (src.compareAndSet(j, o, RESIZING))
                {
```

### UNCHECKED_WARNING
Unchecked call to 'compareAndSet(int, E, E)' as a member of raw type 'java.util.concurrent.atomic.AtomicReferenceArray'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
            {
                Entry<K, V> e = (Entry<K, V>) o;
                if (src.compareAndSet(j, o, RESIZING))
                {
                    while (e != null)
```

### UNCHECKED_WARNING
Unchecked call to 'set(int, E)' as a member of raw type 'java.util.concurrent.atomic.AtomicReferenceArray'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                        e = e.getNext();
                    }
                    src.set(j, RESIZED);
                    j++;
                }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMap.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
            else
            {
                Entry<K, V> e = (Entry<K, V>) o;
                while (e != null)
                {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMap.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                        V oldValue = e.getValue();
                        V newValue = function.valueOf(oldValue);
                        Entry<K, V> newEntry = new Entry<>(e.getKey(), newValue, this.createReplacementChainForRemoval((Entry<K, V>) o, e));
                        if (!currentArray.compareAndSet(index, o, newEntry))
                        {
```

### UNCHECKED_WARNING
Unchecked call to 'compareAndSet(int, E, E)' as a member of raw type 'java.util.concurrent.atomic.AtomicReferenceArray'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                        V newValue = function.valueOf(oldValue);
                        Entry<K, V> newEntry = new Entry<>(e.getKey(), newValue, this.createReplacementChainForRemoval((Entry<K, V>) o, e));
                        if (!currentArray.compareAndSet(index, o, newEntry))
                        {
                            //noinspection ContinueStatementWithLabel
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMap.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                }
                V result = function.valueOf(factory.value());
                Entry<K, V> newEntry = new Entry<>(key, result, (Entry<K, V>) o);
                if (currentArray.compareAndSet(index, o, newEntry))
                {
```

### UNCHECKED_WARNING
Unchecked call to 'compareAndSet(int, E, E)' as a member of raw type 'java.util.concurrent.atomic.AtomicReferenceArray'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                V result = function.valueOf(factory.value());
                Entry<K, V> newEntry = new Entry<>(key, result, (Entry<K, V>) o);
                if (currentArray.compareAndSet(index, o, newEntry))
                {
                    this.incrementSizeAndPossiblyResize(currentArray, length, o);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMap.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                else if (o != null)
                {
                    Entry<K, V> e = (Entry<K, V>) o;
                    if (currentArray.compareAndSet(i, o, null))
                    {
```

### UNCHECKED_WARNING
Unchecked call to 'compareAndSet(int, E, E)' as a member of raw type 'java.util.concurrent.atomic.AtomicReferenceArray'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                {
                    Entry<K, V> e = (Entry<K, V>) o;
                    if (currentArray.compareAndSet(i, o, null))
                    {
                        int removedEntries = 0;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMap.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
            else
            {
                Entry<K, V> e = (Entry<K, V>) o;
                while (e != null)
                {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMap.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                    newValue = factory.value();
                }
                Entry<K, V> newEntry = new Entry<>(key, newValue, (Entry<K, V>) o);
                if (currentArray.compareAndSet(index, o, newEntry))
                {
```

### UNCHECKED_WARNING
Unchecked call to 'compareAndSet(int, E, E)' as a member of raw type 'java.util.concurrent.atomic.AtomicReferenceArray'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                }
                Entry<K, V> newEntry = new Entry<>(key, newValue, (Entry<K, V>) o);
                if (currentArray.compareAndSet(index, o, newEntry))
                {
                    this.incrementSizeAndPossiblyResize(currentArray, length, o);
```

### UNCHECKED_WARNING
Unchecked call to 'FutureTask(Runnable, V)' as a member of raw type 'java.util.concurrent.FutureTask'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                int end = Math.min((i + 1) * chunkSize, currentArray.length() - 1);
                Procedure<V> block = blocks.get(i);
                futures[i] = new FutureTask(() -> this.sequentialForEachValue(block, currentArray, start, end), null);
                executor.execute(futures[i]);
            }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMap.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
            else
            {
                Entry<K, V> e = (Entry<K, V>) o;
                while (e != null)
                {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
        for (int i = 0; i < size; i++)
        {
            this.put((K) in.readObject(), (V) in.readObject());
        }
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
        for (int i = 0; i < size; i++)
        {
            this.put((K) in.readObject(), (V) in.readObject());
        }
    }
```

### UNCHECKED_WARNING
Unchecked call to 'compareAndSet(int, E, E)' as a member of raw type 'java.util.concurrent.atomic.AtomicReferenceArray'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
            Entry<K, V> newEntry = new Entry<>(key, value, null);
            this.addToSize(1);
            if (currentArray.compareAndSet(index, null, newEntry))
            {
                return null;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMap.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
            else
            {
                Entry<K, V> e = (Entry<K, V>) o;
                while (e != null)
                {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMap.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                    {
                        V oldValue = e.getValue();
                        Entry<K, V> newEntry = new Entry<>(e.getKey(), value, this.createReplacementChainForRemoval((Entry<K, V>) o, e));
                        if (!currentArray.compareAndSet(index, o, newEntry))
                        {
```

### UNCHECKED_WARNING
Unchecked call to 'compareAndSet(int, E, E)' as a member of raw type 'java.util.concurrent.atomic.AtomicReferenceArray'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                        V oldValue = e.getValue();
                        Entry<K, V> newEntry = new Entry<>(e.getKey(), value, this.createReplacementChainForRemoval((Entry<K, V>) o, e));
                        if (!currentArray.compareAndSet(index, o, newEntry))
                        {
                            //noinspection ContinueStatementWithLabel
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMap.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                    e = e.getNext();
                }
                Entry<K, V> newEntry = new Entry<>(key, value, (Entry<K, V>) o);
                if (currentArray.compareAndSet(index, o, newEntry))
                {
```

### UNCHECKED_WARNING
Unchecked call to 'compareAndSet(int, E, E)' as a member of raw type 'java.util.concurrent.atomic.AtomicReferenceArray'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                }
                Entry<K, V> newEntry = new Entry<>(key, value, (Entry<K, V>) o);
                if (currentArray.compareAndSet(index, o, newEntry))
                {
                    this.incrementSizeAndPossiblyResize(currentArray, length, o);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMap.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
            else
            {
                Entry<K, V> e = (Entry<K, V>) o;
                while (e != null)
                {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMap.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                    if (candidate.equals(key) && this.nullSafeEquals(e.getValue(), value))
                    {
                        Entry<K, V> replacement = this.createReplacementChainForRemoval((Entry<K, V>) o, e);
                        if (currentArray.compareAndSet(index, o, replacement))
                        {
```

### UNCHECKED_WARNING
Unchecked call to 'compareAndSet(int, E, E)' as a member of raw type 'java.util.concurrent.atomic.AtomicReferenceArray'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                    {
                        Entry<K, V> replacement = this.createReplacementChainForRemoval((Entry<K, V>) o, e);
                        if (currentArray.compareAndSet(index, o, replacement))
                        {
                            this.addToSize(-1);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMap.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
            else
            {
                Entry<K, V> e = (Entry<K, V>) o;
                while (e != null)
                {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMap.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                    e = e.getNext();
                }
                Entry<K, V> newEntry = new Entry<>(key, value, (Entry<K, V>) o);
                if (currentArray.compareAndSet(index, o, newEntry))
                {
```

### UNCHECKED_WARNING
Unchecked call to 'compareAndSet(int, E, E)' as a member of raw type 'java.util.concurrent.atomic.AtomicReferenceArray'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                }
                Entry<K, V> newEntry = new Entry<>(key, value, (Entry<K, V>) o);
                if (currentArray.compareAndSet(index, o, newEntry))
                {
                    this.incrementSizeAndPossiblyResize(currentArray, length, o);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMap.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
            return this.slowRemove(key, hash, currentArray);
        }
        Entry<K, V> e = (Entry<K, V>) o;
        while (e != null)
        {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMap.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
            if (candidate.equals(key))
            {
                Entry<K, V> replacement = this.createReplacementChainForRemoval((Entry<K, V>) o, e);
                if (currentArray.compareAndSet(index, o, replacement))
                {
```

### UNCHECKED_WARNING
Unchecked call to 'compareAndSet(int, E, E)' as a member of raw type 'java.util.concurrent.atomic.AtomicReferenceArray'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
            {
                Entry<K, V> replacement = this.createReplacementChainForRemoval((Entry<K, V>) o, e);
                if (currentArray.compareAndSet(index, o, replacement))
                {
                    this.addToSize(-1);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMap.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                throw new ConcurrentModificationException("can't iterate while resizing!");
            }
            Entry<K, V> e = (Entry<K, V>) o;
            while (e != null)
            {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                Object key = e.getKey();
                Object value = e.getValue();
                this.put((K) key, (V) value);
                e = e.getNext();
            }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                Object key = e.getKey();
                Object value = e.getValue();
                this.put((K) key, (V) value);
                e = e.getNext();
            }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMap.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
            else
            {
                Entry<K, V> e = (Entry<K, V>) o;
                while (e != null)
                {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMap.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                    e = e.getNext();
                }
                Entry<K, V> newEntry = new Entry<>(key, value, (Entry<K, V>) o);
                if (currentArray.compareAndSet(index, o, newEntry))
                {
```

### UNCHECKED_WARNING
Unchecked call to 'compareAndSet(int, E, E)' as a member of raw type 'java.util.concurrent.atomic.AtomicReferenceArray'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                }
                Entry<K, V> newEntry = new Entry<>(key, value, (Entry<K, V>) o);
                if (currentArray.compareAndSet(index, o, newEntry))
                {
                    this.incrementSizeAndPossiblyResize(currentArray, length, o);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMap.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                throw new ConcurrentModificationException("Can't serialize while resizing!");
            }
            Entry<K, V> e = (Entry<K, V>) o;
            while (e != null)
            {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMap.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
            return this.slowReplace(key, oldValue, newValue, hash, currentArray);
        }
        Entry<K, V> e = (Entry<K, V>) o;
        while (e != null)
        {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMap.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                if (oldValue == e.getValue() || (oldValue != null && oldValue.equals(e.getValue())))
                {
                    Entry<K, V> replacement = this.createReplacementChainForRemoval((Entry<K, V>) o, e);
                    Entry<K, V> newEntry = new Entry<>(key, newValue, replacement);
                    return currentArray.compareAndSet(index, o, newEntry) || this.slowReplace(key, oldValue, newValue, hash, currentArray);
```

### UNCHECKED_WARNING
Unchecked call to 'compareAndSet(int, E, E)' as a member of raw type 'java.util.concurrent.atomic.AtomicReferenceArray'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                    Entry<K, V> replacement = this.createReplacementChainForRemoval((Entry<K, V>) o, e);
                    Entry<K, V> newEntry = new Entry<>(key, newValue, replacement);
                    return currentArray.compareAndSet(index, o, newEntry) || this.slowReplace(key, oldValue, newValue, hash, currentArray);
                }
                return false;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
            return false;
        }
        Map<K, V> m = (Map<K, V>) o;
        if (m.size() != this.size())
        {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMap.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                else if (o != null)
                {
                    this.next = (Entry<K, V>) o;
                    this.index++;
                    break;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMap.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
            else
            {
                Entry<K, V> e = (Entry<K, V>) o;
                while (e != null)
                {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMap.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                        V oldValue = e.getValue();
                        V newValue = function.value(oldValue, parameter);
                        Entry<K, V> newEntry = new Entry<>(e.getKey(), newValue, this.createReplacementChainForRemoval((Entry<K, V>) o, e));
                        if (!currentArray.compareAndSet(index, o, newEntry))
                        {
```

### UNCHECKED_WARNING
Unchecked call to 'compareAndSet(int, E, E)' as a member of raw type 'java.util.concurrent.atomic.AtomicReferenceArray'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                        V newValue = function.value(oldValue, parameter);
                        Entry<K, V> newEntry = new Entry<>(e.getKey(), newValue, this.createReplacementChainForRemoval((Entry<K, V>) o, e));
                        if (!currentArray.compareAndSet(index, o, newEntry))
                        {
                            //noinspection ContinueStatementWithLabel
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMap.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                }
                V result = function.value(factory.value(), parameter);
                Entry<K, V> newEntry = new Entry<>(key, result, (Entry<K, V>) o);
                if (currentArray.compareAndSet(index, o, newEntry))
                {
```

### UNCHECKED_WARNING
Unchecked call to 'compareAndSet(int, E, E)' as a member of raw type 'java.util.concurrent.atomic.AtomicReferenceArray'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                V result = function.value(factory.value(), parameter);
                Entry<K, V> newEntry = new Entry<>(key, result, (Entry<K, V>) o);
                if (currentArray.compareAndSet(index, o, newEntry))
                {
                    this.incrementSizeAndPossiblyResize(currentArray, length, o);
```

### UNCHECKED_WARNING
Unchecked call to 'FutureTask(Runnable, V)' as a member of raw type 'java.util.concurrent.FutureTask'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                int end = Math.min((i + 1) * chunkSize, currentArray.length());
                Procedure2<K, V> block = blocks.get(i);
                futures[i] = new FutureTask(() -> this.sequentialForEachKeyValue(block, currentArray, start, end), null);
                executor.execute(futures[i]);
            }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                return false;
            }
            Map.Entry<K, V> e = (Map.Entry<K, V>) o;
            return ConcurrentHashMap.this.remove(e.getKey(), e.getValue());
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMap.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                throw new ConcurrentModificationException("can't compute hashcode while resizing!");
            }
            Entry<K, V> e = (Entry<K, V>) o;
            while (e != null)
            {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMap.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                else if (o != null)
                {
                    Entry<K, V> e = (Entry<K, V>) o;
                    while (e != null)
                    {
```

### UNCHECKED_WARNING
Unchecked call to 'compareAndSet(int, E, E)' as a member of raw type 'java.util.concurrent.atomic.AtomicReferenceArray'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
            V result = function.value(factory.value(), parameter);
            Entry<K, V> newEntry = new Entry<>(key, result, null);
            if (currentArray.compareAndSet(index, null, newEntry))
            {
                this.addToSize(1);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMap.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
            return this.slowGet(key, hash, index, currentArray);
        }
        for (Entry<K, V> e = (Entry<K, V>) o; e != null; e = e.getNext())
        {
            Object k;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMap.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
            else
            {
                Entry<K, V> e = (Entry<K, V>) o;
                while (e != null)
                {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMap.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                    newValue = function.valueOf(parameter);
                }
                Entry<K, V> newEntry = new Entry<>(key, newValue, (Entry<K, V>) o);
                if (currentArray.compareAndSet(index, o, newEntry))
                {
```

### UNCHECKED_WARNING
Unchecked call to 'compareAndSet(int, E, E)' as a member of raw type 'java.util.concurrent.atomic.AtomicReferenceArray'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                }
                Entry<K, V> newEntry = new Entry<>(key, newValue, (Entry<K, V>) o);
                if (currentArray.compareAndSet(index, o, newEntry))
                {
                    this.incrementSizeAndPossiblyResize(currentArray, length, o);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMapUnsafe.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
                else
                {
                    newEntry = new Entry<>(toCopyEntry.getKey(), toCopyEntry.getValue(), (Entry<K, V>) o);
                }
                if (ConcurrentHashMapUnsafe.casArrayAt(currentArray, index, o, newEntry))
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMapUnsafe.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
            else
            {
                Entry<K, V> e = (Entry<K, V>) o;
                while (e != null)
                {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMapUnsafe.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
                    if (candidate.equals(key) && this.nullSafeEquals(e.getValue(), value))
                    {
                        Entry<K, V> replacement = this.createReplacementChainForRemoval((Entry<K, V>) o, e);
                        if (ConcurrentHashMapUnsafe.casArrayAt(currentArray, index, o, replacement))
                        {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
                return false;
            }
            Map.Entry<K, V> e = (Map.Entry<K, V>) o;
            Entry<K, V> candidate = ConcurrentHashMapUnsafe.this.getEntry(e.getKey());
            return e.equals(candidate);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMapUnsafe.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
            else
            {
                Entry<K, V> e = (Entry<K, V>) o;
                while (e != null)
                {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMapUnsafe.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
                throw new ConcurrentModificationException("can't iterate while resizing!");
            }
            Entry<K, V> e = (Entry<K, V>) o;
            while (e != null)
            {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
                Object key = e.getKey();
                Object value = e.getValue();
                block.value((K) key, (V) value);
                e = e.getNext();
            }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
                Object key = e.getKey();
                Object value = e.getValue();
                block.value((K) key, (V) value);
                e = e.getNext();
            }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMapUnsafe.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
                else if (o != null)
                {
                    this.next = (Entry<K, V>) o;
                    this.index++;
                    break;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
                return false;
            }
            Map.Entry<K, V> e = (Map.Entry<K, V>) o;
            return ConcurrentHashMapUnsafe.this.remove(e.getKey(), e.getValue());
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMapUnsafe.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
            else
            {
                Entry<K, V> e = (Entry<K, V>) o;
                while (e != null)
                {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMapUnsafe.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
                    newValue = factory.value();
                }
                Entry<K, V> newEntry = new Entry<>(key, newValue, (Entry<K, V>) o);
                if (ConcurrentHashMapUnsafe.casArrayAt(currentArray, index, o, newEntry))
                {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMapUnsafe.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
            else
            {
                Entry<K, V> e = (Entry<K, V>) o;
                while (e != null)
                {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMapUnsafe.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
                    newValue = function.valueOf(parameter);
                }
                Entry<K, V> newEntry = new Entry<>(key, newValue, (Entry<K, V>) o);
                if (ConcurrentHashMapUnsafe.casArrayAt(currentArray, index, o, newEntry))
                {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMapUnsafe.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
            else
            {
                Entry<K, V> e = (Entry<K, V>) o;
                while (e != null)
                {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMapUnsafe.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
                    if (candidate.equals(key))
                    {
                        Entry<K, V> replacement = this.createReplacementChainForRemoval((Entry<K, V>) o, e);
                        if (ConcurrentHashMapUnsafe.casArrayAt(currentArray, index, o, replacement))
                        {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
                return false;
            }
            Map.Entry<K, V> e = (Map.Entry<K, V>) o;
            K k1 = this.key;
            Object k2 = e.getKey();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMapUnsafe.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
            else
            {
                Entry<K, V> e = (Entry<K, V>) o;
                while (e != null)
                {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMapUnsafe.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
                    {
                        V oldValue = e.getValue();
                        Entry<K, V> newEntry = new Entry<>(e.getKey(), value, this.createReplacementChainForRemoval((Entry<K, V>) o, e));
                        if (!ConcurrentHashMapUnsafe.casArrayAt(currentArray, index, o, newEntry))
                        {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMapUnsafe.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
                    e = e.getNext();
                }
                Entry<K, V> newEntry = new Entry<>(key, value, (Entry<K, V>) o);
                if (ConcurrentHashMapUnsafe.casArrayAt(currentArray, index, o, newEntry))
                {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Map,capture\>' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMapUnsafe'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
        if (map instanceof ConcurrentHashMapUnsafe<?, ?> && chunks > 1 && map.size() > 50000)
        {
            ConcurrentHashMapUnsafe<K, V> incoming = (ConcurrentHashMapUnsafe<K, V>) map;
            Object[] currentArray = incoming.table;
            FutureTask<?>[] futures = new FutureTask<?>[chunks];
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMapUnsafe.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
            else
            {
                Entry<K, V> e = (Entry<K, V>) o;
                while (e != null)
                {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMapUnsafe.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
                        V oldValue = e.getValue();
                        V newValue = function.valueOf(oldValue);
                        Entry<K, V> newEntry = new Entry<>(e.getKey(), newValue, this.createReplacementChainForRemoval((Entry<K, V>) o, e));
                        if (!ConcurrentHashMapUnsafe.casArrayAt(currentArray, index, o, newEntry))
                        {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMapUnsafe.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
                }
                V result = function.valueOf(factory.value());
                Entry<K, V> newEntry = new Entry<>(key, result, (Entry<K, V>) o);
                if (ConcurrentHashMapUnsafe.casArrayAt(currentArray, index, o, newEntry))
                {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
        for (int i = 0; i < size; i++)
        {
            this.put((K) in.readObject(), (V) in.readObject());
        }
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
        for (int i = 0; i < size; i++)
        {
            this.put((K) in.readObject(), (V) in.readObject());
        }
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMapUnsafe.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
            else
            {
                Entry<K, V> e = (Entry<K, V>) o;
                while (e != null)
                {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMapUnsafe.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
                    e = e.getNext();
                }
                Entry<K, V> newEntry = new Entry<>(key, value, (Entry<K, V>) o);
                if (ConcurrentHashMapUnsafe.casArrayAt(currentArray, index, o, newEntry))
                {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMapUnsafe.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
            else
            {
                Entry<K, V> e = (Entry<K, V>) o;
                while (e != null)
                {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMapUnsafe.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
                        V oldValue = e.getValue();
                        V newValue = function.value(oldValue, parameter);
                        Entry<K, V> newEntry = new Entry<>(e.getKey(), newValue, this.createReplacementChainForRemoval((Entry<K, V>) o, e));
                        if (!ConcurrentHashMapUnsafe.casArrayAt(currentArray, index, o, newEntry))
                        {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMapUnsafe.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
                }
                V result = function.value(factory.value(), parameter);
                Entry<K, V> newEntry = new Entry<>(key, result, (Entry<K, V>) o);
                if (ConcurrentHashMapUnsafe.casArrayAt(currentArray, index, o, newEntry))
                {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMapUnsafe.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
            else
            {
                Entry<K, V> e = (Entry<K, V>) o;
                if (ConcurrentHashMapUnsafe.casArrayAt(src, j, o, RESIZING))
                {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMapUnsafe.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
                throw new ConcurrentModificationException("can't compute hashcode while resizing!");
            }
            Entry<K, V> e = (Entry<K, V>) o;
            while (e != null)
            {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMapUnsafe.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
            return this.slowReplace(key, oldValue, newValue, hash, currentArray);
        }
        Entry<K, V> e = (Entry<K, V>) o;
        while (e != null)
        {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMapUnsafe.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
                if (oldValue == e.getValue() || (oldValue != null && oldValue.equals(e.getValue())))
                {
                    Entry<K, V> replacement = this.createReplacementChainForRemoval((Entry<K, V>) o, e);
                    Entry<K, V> newEntry = new Entry<>(key, newValue, replacement);
                    return ConcurrentHashMapUnsafe.casArrayAt(currentArray, index, o, newEntry) || this.slowReplace(key, oldValue, newValue, hash, currentArray);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMapUnsafe.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
                else if (o != null)
                {
                    Entry<K, V> e = (Entry<K, V>) o;
                    while (e != null)
                    {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMapUnsafe.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
            else
            {
                Entry<K, V> e = (Entry<K, V>) o;
                while (e != null)
                {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMapUnsafe.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
                    key = keyTransformer.value(key, newValue);
                }
                Entry<K, V> newEntry = new Entry<>(key, newValue, (Entry<K, V>) o);
                if (ConcurrentHashMapUnsafe.casArrayAt(currentArray, index, o, newEntry))
                {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMapUnsafe.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
            if (o instanceof Entry)
            {
                Entry<K, V> e = (Entry<K, V>) o;
                while (e != null)
                {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMapUnsafe.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
            else
            {
                Entry<K, V> e = (Entry<K, V>) o;
                while (e != null)
                {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMapUnsafe.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
                    e = e.getNext();
                }
                Entry<K, V> newEntry = new Entry<>(key, value, (Entry<K, V>) o);
                if (ConcurrentHashMapUnsafe.casArrayAt(currentArray, index, o, newEntry))
                {
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.map.ImmutableMap,capture\>' to 'org.eclipse.collections.api.map.ImmutableMap'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/immutable/ImmutableMapFactoryImpl.java`
#### Snippet
```java
    public <K, V> ImmutableMap<K, V> empty()
    {
        return (ImmutableMap<K, V>) ImmutableEmptyMap.INSTANCE;
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Map.Entry\[\]' to 'java.util.Map.Entry\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/immutable/ImmutableMapFactoryImpl.java`
#### Snippet
```java
        }

        Map.Entry<K, V>[] entries = map.entrySet().toArray(new Map.Entry[map.entrySet().size()]);
        switch (entries.length)
        {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMapUnsafe.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
                throw new ConcurrentModificationException("can't iterate while resizing!");
            }
            Entry<K, V> e = (Entry<K, V>) o;
            while (e != null)
            {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
            {
                Object value = e.getValue();
                block.value((V) value);
                e = e.getNext();
            }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMapUnsafe.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
                throw new ConcurrentModificationException("Can't serialize while resizing!");
            }
            Entry<K, V> e = (Entry<K, V>) o;
            while (e != null)
            {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMapUnsafe.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
            else
            {
                Entry<K, V> e = (Entry<K, V>) o;
                while (e != null)
                {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMapUnsafe.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
                    newValue = factory.valueOf(key);
                }
                Entry<K, V> newEntry = new Entry<>(key, newValue, (Entry<K, V>) o);
                if (ConcurrentHashMapUnsafe.casArrayAt(currentArray, index, o, newEntry))
                {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMapUnsafe.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
                else if (o != null)
                {
                    Entry<K, V> e = (Entry<K, V>) o;
                    if (ConcurrentHashMapUnsafe.casArrayAt(currentArray, i, o, null))
                    {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMapUnsafe.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
                    else
                    {
                        Entry<K, V> e = (Entry<K, V>) o;
                        if (ConcurrentHashMapUnsafe.casArrayAt(src, j, o, RESIZING))
                        {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMapUnsafe.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
                throw new ConcurrentModificationException("can't iterate while resizing!");
            }
            Entry<K, V> e = (Entry<K, V>) o;
            while (e != null)
            {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
                Object key = e.getKey();
                Object value = e.getValue();
                this.put((K) key, (V) value);
                e = e.getNext();
            }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
                Object key = e.getKey();
                Object value = e.getValue();
                this.put((K) key, (V) value);
                e = e.getNext();
            }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
            return false;
        }
        Map<K, V> m = (Map<K, V>) o;
        if (m.size() != this.size())
        {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMapUnsafe.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
            return this.slowGet(key, hash, index, currentArray);
        }
        for (Entry<K, V> e = (Entry<K, V>) o; e != null; e = e.getNext())
        {
            Object k;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMapUnsafe.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
            return this.slowRemove(key, hash, currentArray);
        }
        Entry<K, V> e = (Entry<K, V>) o;
        while (e != null)
        {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMapUnsafe.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
            if (candidate == key || candidate.equals(key))
            {
                Entry<K, V> replacement = this.createReplacementChainForRemoval((Entry<K, V>) o, e);
                if (ConcurrentHashMapUnsafe.casArrayAt(currentArray, index, o, replacement))
                {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMapUnsafe.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
            else
            {
                Entry<K, V> e = (Entry<K, V>) o;
                while (e != null)
                {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMapUnsafe.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
                        if (oldValue == e.getValue() || (oldValue != null && oldValue.equals(e.getValue())))
                        {
                            Entry<K, V> replacement = this.createReplacementChainForRemoval((Entry<K, V>) o, e);
                            Entry<K, V> newEntry = new Entry<>(key, newValue, replacement);
                            if (ConcurrentHashMapUnsafe.casArrayAt(currentArray, index, o, newEntry))
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMapUnsafe.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
            else
            {
                Entry<K, V> e = (Entry<K, V>) o;
                while (e != null)
                {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMapUnsafe.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
            else
            {
                Entry<K, V> e = (Entry<K, V>) o;
                while (e != null)
                {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.map.mutable.ConcurrentHashMapUnsafe.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
                    {
                        V oldValue = e.getValue();
                        Entry<K, V> newEntry = new Entry<>(e.getKey(), value, this.createReplacementChainForRemoval((Entry<K, V>) o, e));
                        if (!ConcurrentHashMapUnsafe.casArrayAt(currentArray, index, o, newEntry))
                        {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/immutable/ImmutableDoubletonMap.java`
#### Snippet
```java
            return false;
        }
        Map<K, V> that = (Map<K, V>) other;
        return that.size() == this.size()
                && this.keyAndValueEquals(this.key1, this.value1, that)
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/immutable/ImmutableQuadrupletonMap.java`
#### Snippet
```java
            return false;
        }
        Map<K, V> that = (Map<K, V>) other;
        return that.size() == this.size()
                && this.keyAndValueEquals(this.key1, this.value1, that)
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/fixed/SingletonSet.java`
#### Snippet
```java
    public void readExternal(ObjectInput in) throws IOException, ClassNotFoundException
    {
        this.element1 = (T) in.readObject();
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/fixed/TripletonSet.java`
#### Snippet
```java
    public void readExternal(ObjectInput in) throws IOException, ClassNotFoundException
    {
        this.element1 = (T) in.readObject();
        this.element2 = (T) in.readObject();
        this.element3 = (T) in.readObject();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/fixed/TripletonSet.java`
#### Snippet
```java
    {
        this.element1 = (T) in.readObject();
        this.element2 = (T) in.readObject();
        this.element3 = (T) in.readObject();
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/fixed/TripletonSet.java`
#### Snippet
```java
        this.element1 = (T) in.readObject();
        this.element2 = (T) in.readObject();
        this.element3 = (T) in.readObject();
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.set.FixedSizeSet\>' to 'org.eclipse.collections.api.set.FixedSizeSet'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/fixed/FixedSizeSetFactoryImpl.java`
#### Snippet
```java
    public <T> FixedSizeSet<T> empty()
    {
        return (FixedSizeSet<T>) FixedSizeSetFactoryImpl.EMPTY_SET;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/fixed/FixedSizeSetFactoryImpl.java`
#### Snippet
```java
                return new EmptySet<>();
            case 1:
                itemArray = (T[]) set.toArray();
                return new SingletonSet<>(itemArray[0]);
            case 2:
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/fixed/FixedSizeSetFactoryImpl.java`
#### Snippet
```java
                return new SingletonSet<>(itemArray[0]);
            case 2:
                itemArray = (T[]) set.toArray();
                return new DoubletonSet<>(itemArray[0], itemArray[1]);
            case 3:
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/fixed/FixedSizeSetFactoryImpl.java`
#### Snippet
```java
                return new DoubletonSet<>(itemArray[0], itemArray[1]);
            case 3:
                itemArray = (T[]) set.toArray();
                return new TripletonSet<>(itemArray[0], itemArray[1], itemArray[2]);
            case 4:
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/fixed/FixedSizeSetFactoryImpl.java`
#### Snippet
```java
                return new TripletonSet<>(itemArray[0], itemArray[1], itemArray[2]);
            case 4:
                itemArray = (T[]) set.toArray();
                return new QuadrupletonSet<>(itemArray[0], itemArray[1], itemArray[2], itemArray[3]);
            default:
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/fixed/FixedSizeSetFactoryImpl.java`
#### Snippet
```java
                return new EmptySet<>();
            case 1:
                itemArray = (T[]) set.toArray();
                return new SingletonSet<>(itemArray[0]);
            case 2:
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/fixed/FixedSizeSetFactoryImpl.java`
#### Snippet
```java
                return new SingletonSet<>(itemArray[0]);
            case 2:
                itemArray = (T[]) set.toArray();
                return new DoubletonSet<>(itemArray[0], itemArray[1]);
            case 3:
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/fixed/FixedSizeSetFactoryImpl.java`
#### Snippet
```java
                return new DoubletonSet<>(itemArray[0], itemArray[1]);
            case 3:
                itemArray = (T[]) set.toArray();
                return new TripletonSet<>(itemArray[0], itemArray[1], itemArray[2]);
            case 4:
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/fixed/FixedSizeSetFactoryImpl.java`
#### Snippet
```java
                return new TripletonSet<>(itemArray[0], itemArray[1], itemArray[2]);
            case 4:
                itemArray = (T[]) set.toArray();
                return new QuadrupletonSet<>(itemArray[0], itemArray[1], itemArray[2], itemArray[3]);
            default:
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/fixed/QuadrupletonSet.java`
#### Snippet
```java
    public void readExternal(ObjectInput in) throws IOException, ClassNotFoundException
    {
        this.element1 = (T) in.readObject();
        this.element2 = (T) in.readObject();
        this.element3 = (T) in.readObject();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/fixed/QuadrupletonSet.java`
#### Snippet
```java
    {
        this.element1 = (T) in.readObject();
        this.element2 = (T) in.readObject();
        this.element3 = (T) in.readObject();
        this.element4 = (T) in.readObject();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/fixed/QuadrupletonSet.java`
#### Snippet
```java
        this.element1 = (T) in.readObject();
        this.element2 = (T) in.readObject();
        this.element3 = (T) in.readObject();
        this.element4 = (T) in.readObject();
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/fixed/QuadrupletonSet.java`
#### Snippet
```java
        this.element2 = (T) in.readObject();
        this.element3 = (T) in.readObject();
        this.element4 = (T) in.readObject();
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/fixed/DoubletonSet.java`
#### Snippet
```java
    public void readExternal(ObjectInput in) throws IOException, ClassNotFoundException
    {
        this.element1 = (T) in.readObject();
        this.element2 = (T) in.readObject();
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/fixed/DoubletonSet.java`
#### Snippet
```java
    {
        this.element1 = (T) in.readObject();
        this.element2 = (T) in.readObject();
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.set.MutableSet\>' to 'org.eclipse.collections.api.set.MutableSet\>'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/AbstractUnifiedSet.java`
#### Snippet
```java
    public MutableSet<UnsortedSetIterable<T>> powerSet()
    {
        return (MutableSet<UnsortedSetIterable<T>>) (MutableSet<?>) SetIterables.powerSet(this);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Iterable\>' to 'org.eclipse.collections.api.set.sorted.ImmutableSortedSet'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/immutable/ImmutableSortedSetFactoryImpl.java`
#### Snippet
```java
        if (items instanceof ImmutableSortedSet<?>)
        {
            return (ImmutableSortedSet<T>) items;
        }
        if (Iterate.isEmpty(items))
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.SortedSet' to 'org.eclipse.collections.api.set.sorted.ImmutableSortedSet'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/immutable/ImmutableSortedSetFactoryImpl.java`
#### Snippet
```java
        if (set instanceof ImmutableSortedSet)
        {
            return (ImmutableSortedSet<T>) set;
        }
        if (set.isEmpty())
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.set.sorted.ImmutableSortedSet\>' to 'org.eclipse.collections.api.set.sorted.ImmutableSortedSet'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/immutable/ImmutableSortedSetFactoryImpl.java`
#### Snippet
```java
    public <T> ImmutableSortedSet<T> empty()
    {
        return (ImmutableSortedSet<T>) ImmutableEmptySortedSet.INSTANCE;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Comparator'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/immutable/ImmutableSortedSetSerializationProxy.java`
#### Snippet
```java
    public void readExternal(ObjectInput in) throws IOException, ClassNotFoundException
    {
        Comparator<T> comparator = (Comparator<T>) in.readObject();
        int size = in.readInt();
        MutableSortedSet<T> deserializedSet = new TreeSortedSet<>(comparator);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/immutable/ImmutableSortedSetSerializationProxy.java`
#### Snippet
```java
        for (int i = 0; i < size; i++)
        {
            deserializedSet.add((T) in.readObject());
        }

```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/immutable/ImmutableEmptySortedSet.java`
#### Snippet
```java
    public ImmutableSortedSet<T> newWith(T element)
    {
        return SortedSets.immutable.with(this.comparator, element);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.impl.set.sorted.immutable.ImmutableEmptySortedSet' to 'org.eclipse.collections.api.set.sorted.ImmutableSortedSet'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/immutable/ImmutableEmptySortedSet.java`
#### Snippet
```java
    public <S> ImmutableSortedSet<S> selectInstancesOf(Class<S> clazz)
    {
        return (ImmutableSortedSet<S>) this;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
        if (cur != CHAINED_KEY && this.nonNullTableObjectEquals(cur, key))
        {
            V result = (V) this.table[index + 1];
            this.table[index + 1] = value;
            return result;
```

### UNCHECKED_WARNING
Unchecked cast: 'capture' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
        private Entry<K, V> getEntry(Entry<?, ?> entry)
        {
            K key = (K) entry.getKey();
            V value = (V) entry.getValue();
            int index = UnifiedMapWithHashingStrategy.this.index(key);
```

### UNCHECKED_WARNING
Unchecked cast: 'capture' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
        {
            K key = (K) entry.getKey();
            V value = (V) entry.getValue();
            int index = UnifiedMapWithHashingStrategy.this.index(key);

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
                if (UnifiedMapWithHashingStrategy.nullSafeEquals(value, curValue))
                {
                    return ImmutableEntry.of(UnifiedMapWithHashingStrategy.this.nonSentinel(cur), (V) curValue);
                }
            }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Map,capture\>' to 'org.eclipse.collections.impl.map.strategy.mutable.UnifiedMapWithHashingStrategy'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
        if (map instanceof UnifiedMapWithHashingStrategy<?, ?>)
        {
            this.copyMap((UnifiedMapWithHashingStrategy<K, V>) map);
        }
        else if (map instanceof UnsortedMapIterable)
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Map,capture\>' to 'org.eclipse.collections.api.map.MapIterable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
        else if (map instanceof UnsortedMapIterable)
        {
            MapIterable<K, V> mapIterable = (MapIterable<K, V>) map;
            mapIterable.forEachKeyValue(this::put);
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
        if (cur != CHAINED_KEY && this.nonNullTableObjectEquals(cur, key))
        {
            V oldValue = (V) this.table[index + 1];
            V newValue = function.value(oldValue, parameter);
            this.table[index + 1] = newValue;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
            this.position += 2;
            this.lastReturned = true;
            return new WeakBoundEntry<>(UnifiedMapWithHashingStrategy.this.nonSentinel(cur), (V) value, this.holder,
                    UnifiedMapWithHashingStrategy.this.hashingStrategy);
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
                if (this.nonNullTableObjectEquals(chain[i], key))
                {
                    V result = (V) chain[i + 1];
                    chain[i + 1] = value;
                    return result;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
                    return;
                }
                replace.add((V) chain[i + 1]);
            }
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
                return;
            }
            procedure.value((V) chain[i + 1]);
        }
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
            this.position += 2;
            this.lastReturned = true;
            return (V) val;
        }
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
    public V remove(Object key)
    {
        int index = this.index((K) key);
        Object cur = this.table[index];
        if (cur != null)
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
            if (cur == CHAINED_KEY)
            {
                return this.removeFromChain((Object[]) val, (K) key, index);
            }
            if (this.nonNullTableObjectEquals(cur, (K) key))
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
                return this.removeFromChain((Object[]) val, (K) key, index);
            }
            if (this.nonNullTableObjectEquals(cur, (K) key))
            {
                this.table[index] = null;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
                this.table[index + 1] = null;
                this.occupied--;
                return (V) val;
            }
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
            if (result.length < size)
            {
                result = (T[]) Array.newInstance(result.getClass().getComponentType(), size);
            }
            this.copyKeys(result);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
                if (this.nonNullTableObjectEquals(chain[i], key))
                {
                    V oldValue = (V) chain[i + 1];
                    V result = function.value(oldValue, parameter);
                    chain[i + 1] = result;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
                if (this.nonNullTableObjectEquals(chain[i], key))
                {
                    result = (V) chain[i + 1];
                    break;
                }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
                            }
                            result[count++] =
                                    new WeakBoundEntry<>(UnifiedMapWithHashingStrategy.this.nonSentinel(cur), (V) chain[j + 1], this.holder,
                                            UnifiedMapWithHashingStrategy.this.hashingStrategy);
                        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
                    else
                    {
                        result[count++] = new WeakBoundEntry<>(UnifiedMapWithHashingStrategy.this.nonSentinel(x), (V) table[i + 1], this.holder,
                                UnifiedMapWithHashingStrategy.this.hashingStrategy);
                    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
    private K nonSentinel(Object key)
    {
        return key == NULL_KEY ? null : (K) key;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.api.block.HashingStrategy'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
    public void readExternal(ObjectInput in) throws IOException, ClassNotFoundException
    {
        this.hashingStrategy = (HashingStrategy<? super K>) in.readObject();
        int size = in.readInt();
        this.loadFactor = in.readFloat();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
        for (int i = 0; i < size; i++)
        {
            this.put((K) in.readObject(), (V) in.readObject());
        }
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
        for (int i = 0; i < size; i++)
        {
            this.put((K) in.readObject(), (V) in.readObject());
        }
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
            {
                Object[] chain = (Object[]) this.table[i + 1];
                return (V) chain[1];
            }
            if (cur != null)
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
            if (cur != null)
            {
                return (V) this.table[i + 1];
            }
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
        if (cur != CHAINED_KEY && this.nonNullTableObjectEquals(cur, key))
        {
            return (V) this.table[index + 1];
        }
        return this.chainedGetIfAbsentPut(key, index, value);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
            else if (cur != null)
            {
                procedure.value(this.nonSentinel(cur), (V) this.table[i + 1]);
            }
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
                if (this.nonNullTableObjectEquals(chain[i], key))
                {
                    V oldValue = (V) chain[i + 1];
                    V result = function.valueOf(oldValue);
                    chain[i + 1] = result;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
                if (UnifiedMapWithHashingStrategy.this.nonNullTableObjectEquals(k, key))
                {
                    V val = (V) chain[i + 1];
                    if (UnifiedMapWithHashingStrategy.nullSafeEquals(val, value))
                    {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
            if (this.table[i] == CHAINED_KEY)
            {
                if (this.chainedContainsValue((Object[]) this.table[i + 1], (V) value))
                {
                    return true;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
            if (result.length < size)
            {
                result = (T[]) Array.newInstance(result.getClass().getComponentType(), size);
            }
            this.copyValues(result);
```

### UNCHECKED_WARNING
Unchecked cast: 'capture' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
            }
            Entry<?, ?> entry = (Entry<?, ?>) e;
            K key = (K) entry.getKey();
            V value = (V) entry.getValue();

```

### UNCHECKED_WARNING
Unchecked cast: 'capture' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
            Entry<?, ?> entry = (Entry<?, ?>) e;
            K key = (K) entry.getKey();
            V value = (V) entry.getValue();

            int index = UnifiedMapWithHashingStrategy.this.index(key);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
            else if (cur != null)
            {
                objectIntProcedure.value((V) this.table[i + 1], index++);
            }
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
                break;
            }
            this.put(this.nonSentinel(cur), (V) chain[j + 1]);
        }
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
                return;
            }
            procedure.value((V) chain[i + 1], parameter);
        }
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
                if (this.nonNullTableObjectEquals(chain[i], key))
                {
                    result = (V) chain[i + 1];
                    break;
                }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
        {
            Object[] chain = (Object[]) UnifiedMapWithHashingStrategy.this.table[this.position + 1];
            V val = (V) chain[this.chainPosition + 1];
            this.chainPosition += 2;
            if (this.chainPosition >= chain.length
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
            if (this.nonNullTableObjectEquals(k, key))
            {
                return (V) chain[i + 1];
            }
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
        if (cur != CHAINED_KEY && this.nonNullTableObjectEquals(cur, key))
        {
            return (V) this.table[index + 1];
        }
        return this.chainedGetIfAbsentPutWith(key, index, function, parameter);
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'org.eclipse.collections.api.map.MutableMap', required 'org.eclipse.collections.api.map.MutableMap'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java

    @Override
    public MutableMap<K, V> newEmpty(int capacity)
    {
        return new UnifiedMapWithHashingStrategy<>(this.hashingStrategy, capacity, this.loadFactor);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
                    return;
                }
                procedure.value(ImmutableEntry.of(UnifiedMapWithHashingStrategy.this.nonSentinel(cur), (V) chain[i + 1]));
            }
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
                else if (cur != null)
                {
                    procedure.value(ImmutableEntry.of(UnifiedMapWithHashingStrategy.this.nonSentinel(cur), (V) UnifiedMapWithHashingStrategy.this.table[i + 1]));
                }
            }
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.map.MutableMap' to 'org.eclipse.collections.impl.map.strategy.mutable.UnifiedMapWithHashingStrategy'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
    public <R> MutableMap<K, R> collectValues(Function2<? super K, ? super V, ? extends R> function)
    {
        UnifiedMapWithHashingStrategy<K, R> target = (UnifiedMapWithHashingStrategy<K, R>) this.newEmpty();
        target.loadFactor = this.loadFactor;
        target.occupied = this.occupied;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
                    {
                        chainedTargetTable[j] = chainedTable[j];
                        chainedTargetTable[j + 1] = function.value(this.nonSentinel(chainedTable[j]), (V) chainedTable[j + 1]);
                    }
                }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
            else if (this.table[i] != null)
            {
                target.table[i + 1] = function.value(this.nonSentinel(this.table[i]), (V) this.table[i + 1]);
            }
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
                if (UnifiedMapWithHashingStrategy.this.nonNullTableObjectEquals(k, key))
                {
                    other.put(UnifiedMapWithHashingStrategy.this.nonSentinel(k), (V) chain[i + 1]);
                }
            }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
                    if (chainedTable[j] != null)
                    {
                        V value = (V) chainedTable[j + 1];
                        if (predicate.accept(value) == expected)
                        {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
            else if (this.table[i] != null)
            {
                V value = (V) this.table[i + 1];

                if (predicate.accept(value) == expected)
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
                return;
            }
            procedure.value(this.nonSentinel(cur), (V) chain[i + 1]);
        }
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
    public V get(Object key)
    {
        int index = this.index((K) key);
        Object cur = this.table[index];
        if (cur != null)
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
            if (cur == CHAINED_KEY)
            {
                return this.getFromChain((Object[]) val, (K) key);
            }
            if (this.nonNullTableObjectEquals(cur, (K) key))
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
                return this.getFromChain((Object[]) val, (K) key);
            }
            if (this.nonNullTableObjectEquals(cur, (K) key))
            {
                return (V) val;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
            if (this.nonNullTableObjectEquals(cur, (K) key))
            {
                return (V) val;
            }
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
                else if (cur != null)
                {
                    procedure.value(ImmutableEntry.of(UnifiedMapWithHashingStrategy.this.nonSentinel(cur), (V) map[i + 1]));
                }
            }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
        public void putIfFound(Object key, Map<K, V> other)
        {
            int index = UnifiedMapWithHashingStrategy.this.index((K) key);
            Object cur = UnifiedMapWithHashingStrategy.this.table[index];
            if (cur != null)
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
                if (cur == CHAINED_KEY)
                {
                    this.putIfFoundFromChain((Object[]) val, (K) key, other);
                    return;
                }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
                    return;
                }
                if (UnifiedMapWithHashingStrategy.this.nonNullTableObjectEquals(cur, (K) key))
                {
                    other.put(UnifiedMapWithHashingStrategy.this.nonSentinel(cur), (V) val);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
                if (UnifiedMapWithHashingStrategy.this.nonNullTableObjectEquals(cur, (K) key))
                {
                    other.put(UnifiedMapWithHashingStrategy.this.nonSentinel(cur), (V) val);
                }
            }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
                return index;
            }
            objectIntProcedure.value((V) chain[i + 1], index++);
        }
        return index;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
            if (result.length < size)
            {
                result = (T[]) Array.newInstance(result.getClass().getComponentType(), size);
            }
            this.copyEntries(result);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
            else if (cur != null)
            {
                procedure.value((V) this.table[i + 1], parameter);
            }
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
    public boolean containsKey(Object key)
    {
        int index = this.index((K) key);
        Object cur = this.table[index];
        if (cur == null)
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
            return false;
        }
        if (cur != CHAINED_KEY && this.nonNullTableObjectEquals(cur, (K) key))
        {
            return true;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
            return true;
        }
        return cur == CHAINED_KEY && this.chainContainsKey((Object[]) this.table[index + 1], (K) key);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
            }
            this.lastReturned = true;
            return new WeakBoundEntry<>(UnifiedMapWithHashingStrategy.this.nonSentinel(cur), (V) value, this.holder,
                    UnifiedMapWithHashingStrategy.this.hashingStrategy);
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
                    if (chainedTable[j] != null)
                    {
                        V value = (V) chainedTable[j + 1];
                        if (predicate.accept(value, parameter) == expected)
                        {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
            else if (this.table[i] != null)
            {
                V value = (V) this.table[i + 1];

                if (predicate.accept(value, parameter) == expected)
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
                if (this.nonNullTableObjectEquals(chain[i], key))
                {
                    result = (V) chain[i + 1];
                    break;
                }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
            else if (cur != null)
            {
                procedure.value((V) this.table[i + 1]);
            }
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
            {
                K key = this.nonSentinel(cur);
                V value = (V) this.table[i + 1];
                Object otherValue = other.get(key);
                if (!UnifiedMapWithHashingStrategy.nullSafeEquals(otherValue, value) || (value == null && otherValue == null && !other.containsKey(key)))
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
            else if (cur != null)
            {
                this.put(this.nonSentinel(cur), (V) unifiedMap.table[i + 1]);
            }
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
                else if (cur != null)
                {
                    replace.add((V) UnifiedMapWithHashingStrategy.this.table[i + 1]);
                }
            }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
            else if (value == null && this.table[i - 1] != null || value != null)
            {
                procedure.value((V) value);
            }
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
                    if (UnifiedMapWithHashingStrategy.nullSafeEquals(value, curValue))
                    {
                        return ImmutableEntry.of(UnifiedMapWithHashingStrategy.this.nonSentinel(cur), (V) curValue);
                    }
                }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
            }
            K key = this.nonSentinel(cur);
            V value = (V) chain[i + 1];
            Object otherValue = other.get(key);
            if (!UnifiedMapWithHashingStrategy.nullSafeEquals(otherValue, value) || (value == null && otherValue == null && !other.containsKey(key)))
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
        if (cur != CHAINED_KEY && this.nonNullTableObjectEquals(cur, key))
        {
            return (V) this.table[index + 1];
        }
        return this.chainedGetIfAbsentPut(key, index, function);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
        if (cur != CHAINED_KEY && this.nonNullTableObjectEquals(cur, key))
        {
            V oldValue = (V) this.table[index + 1];
            V newValue = function.valueOf(oldValue);
            this.table[index + 1] = newValue;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
                    if (cur != null)
                    {
                        this.putForTrim((K) cur, (V) chain[i + 1], j, mask);
                    }
                }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
                    if (cur != null)
                    {
                        this.putForTrim((K) cur, (V) chain[i + 1], j, mask);
                    }
                }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
            else if (key != null)
            {
                this.putForTrim((K) key, (V) temp[j + 1], j, mask);
            }
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
            else if (key != null)
            {
                this.putForTrim((K) key, (V) temp[j + 1], j, mask);
            }
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'capture' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
            {
                Entry<?, ?> other = (Entry<?, ?>) obj;
                K otherKey = (K) other.getKey();
                V otherValue = (V) other.getValue();
                return this.hashingStrategy.equals(this.key, otherKey)
```

### UNCHECKED_WARNING
Unchecked cast: 'capture' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
                Entry<?, ?> other = (Entry<?, ?>) obj;
                K otherKey = (K) other.getKey();
                V otherValue = (V) other.getValue();
                return this.hashingStrategy.equals(this.key, otherKey)
                        && UnifiedMapWithHashingStrategy.nullSafeEquals(this.value, otherValue);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
            if (this.nonNullTableObjectEquals(k, key))
            {
                V val = (V) chain[i + 1];
                this.overwriteWithLastElementFromChain(chain, index, i);
                return val;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
                    if (chain[j] != null)
                    {
                        this.put(this.nonSentinel(chain[j]), (V) chain[j + 1]);
                    }
                }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
            else if (cur != null)
            {
                this.put(this.nonSentinel(cur), (V) old[i + 1]);
            }
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Iterable\>' to 'java.lang.Iterable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/MultiReaderMutableSetFactory.java`
#### Snippet
```java
    public <T> MultiReaderSet<T> withAll(Iterable<? extends T> iterable)
    {
        return MultiReaderUnifiedSet.newSet((Iterable<T>) iterable);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/mutable/TreeSortedSet.java`
#### Snippet
```java
        if (this.treeSet.contains(object))
        {
            return this.treeSet.headSet((T) object).size();
        }
        return -1;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Iterable\>' to 'java.util.SortedSet'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/mutable/TreeSortedSet.java`
#### Snippet
```java
        if (source instanceof SortedSet<?>)
        {
            return new TreeSortedSet<>((SortedSet<T>) source);
        }
        TreeSortedSet<T> sortedSet = TreeSortedSet.newSet();
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.impl.set.sorted.mutable.TreeSortedSet' to 'org.eclipse.collections.impl.set.sorted.mutable.TreeSortedSet'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/mutable/TreeSortedSet.java`
#### Snippet
```java
    public <S> TreeSortedSet<S> selectInstancesOf(Class<S> clazz)
    {
        TreeSortedSet<S> result = (TreeSortedSet<S>) this.newEmpty();
        this.forEach(new SelectInstancesOfProcedure<>(clazz, result));
        return result;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Comparator'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/mutable/TreeSortedSet.java`
#### Snippet
```java
    public void readExternal(ObjectInput in) throws IOException, ClassNotFoundException
    {
        this.treeSet = new TreeSet<>((Comparator<T>) in.readObject());
        int size = in.readInt();
        for (int i = 0; i < size; i++)
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/mutable/TreeSortedSet.java`
#### Snippet
```java
        for (int i = 0; i < size; i++)
        {
            this.add((T) in.readObject());
        }
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.impl.set.sorted.mutable.TreeSortedSet'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/mutable/TreeSortedSet.java`
#### Snippet
```java
        try
        {
            TreeSortedSet<T> clone = (TreeSortedSet<T>) super.clone();
            clone.treeSet = (TreeSet<T>) this.treeSet.clone();
            return clone;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.TreeSet'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/mutable/TreeSortedSet.java`
#### Snippet
```java
        {
            TreeSortedSet<T> clone = (TreeSortedSet<T>) super.clone();
            clone.treeSet = (TreeSet<T>) this.treeSet.clone();
            return clone;
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.set.sorted.MutableSortedSet\>' to 'org.eclipse.collections.api.set.sorted.MutableSortedSet\>'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/mutable/TreeSortedSet.java`
#### Snippet
```java
    public MutableSortedSet<SortedSetIterable<T>> powerSet()
    {
        return (MutableSortedSet<SortedSetIterable<T>>) (MutableSortedSet<?>) SortedSetIterables.powerSet(this);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'T' to 'java.lang.Comparable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/immutable/ImmutableTreeSet.java`
#### Snippet
```java
    {
        return this.comparator == null
                ? ((Comparable<T>) o1).compareTo(o2)
                : this.comparator.compare(o1, o2);
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/immutable/ImmutableTreeSet.java`
#### Snippet
```java
    public boolean contains(Object object)
    {
        return Arrays.binarySearch(this.delegate, (T) object, this.comparator) >= 0;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'T' to 'java.lang.Comparable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/immutable/ImmutableTreeSet.java`
#### Snippet
```java
            {
                int compare = inputComparator == null
                        ? ((Comparable<? super T>) input[i - 1]).compareTo(input[i])
                        : inputComparator.compare(input[i - 1], input[i]);
                if (compare >= 0)
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/immutable/ImmutableTreeSet.java`
#### Snippet
```java
            {
                Arrays.sort(input, inputComparator);
                T[] unique = (T[]) new Object[input.length];
                unique[0] = input[0];

```

### UNCHECKED_WARNING
Unchecked cast: 'T' to 'java.lang.Comparable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/immutable/ImmutableTreeSet.java`
#### Snippet
```java
                {
                    int compare = inputComparator == null
                            ? ((Comparable<? super T>) unique[uniqueCount - 1]).compareTo(input[i])
                            : inputComparator.compare(unique[uniqueCount - 1], input[i]);
                    if (compare < 0)
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/immutable/ImmutableTreeSet.java`
#### Snippet
```java
    public static <T> ImmutableSortedSet<T> newSetFromIterable(Iterable<? extends T> iterable)
    {
        return new ImmutableTreeSet<>((T[]) Iterate.toArray(iterable), null, false);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/immutable/ImmutableTreeSet.java`
#### Snippet
```java
    public static <T> ImmutableSortedSet<T> newSetFromIterable(Comparator<? super T> comparator, Iterable<? extends T> iterable)
    {
        return new ImmutableTreeSet<>((T[]) Iterate.toArray(iterable), comparator, false);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/immutable/ImmutableTreeSet.java`
#### Snippet
```java
    public static <T> ImmutableSortedSet<T> newSet(SortedSet<? super T> set)
    {
        return new ImmutableTreeSet<>((T[]) set.toArray(), set.comparator(), true);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.set.sorted.ImmutableSortedSet\>' to 'org.eclipse.collections.api.set.sorted.ImmutableSortedSet\>'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/immutable/AbstractImmutableSortedSet.java`
#### Snippet
```java
    public ImmutableSortedSet<SortedSetIterable<T>> powerSet()
    {
        return (ImmutableSortedSet<SortedSetIterable<T>>) (ImmutableSortedSet<?>) SortedSetIterables.immutablePowerSet(this);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Comparator\>' to 'java.util.Comparator'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/immutable/AbstractImmutableSortedSet.java`
#### Snippet
```java
    public <S> ImmutableSortedSet<S> selectInstancesOf(Class<S> clazz)
    {
        TreeSortedSet<S> result = TreeSortedSet.newSet((Comparator<? super S>) this.comparator());
        this.forEach(new SelectInstancesOfProcedure<>(clazz, result));
        return result.toImmutable();
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.set.MutableSet' to 'org.eclipse.collections.api.set.MutableSet'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/AbstractMutableSet.java`
#### Snippet
```java
    public <S> MutableSet<S> selectInstancesOf(Class<S> clazz)
    {
        MutableSet<S> result = (MutableSet<S>) this.newEmpty();
        this.forEach(new SelectInstancesOfProcedure<>(clazz, result));
        return result;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.api.set.MutableSet'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/AbstractMutableSet.java`
#### Snippet
```java
        try
        {
            return (MutableSet<T>) super.clone();
        }
        catch (CloneNotSupportedException e)
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.set.MutableSet\>' to 'org.eclipse.collections.api.set.MutableSet\>'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/AbstractMutableSet.java`
#### Snippet
```java
    public MutableSet<UnsortedSetIterable<T>> powerSet()
    {
        return (MutableSet<UnsortedSetIterable<T>>) (MutableSet<?>) SetIterables.powerSet(this);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.set.MutableSet\>' to 'org.eclipse.collections.api.set.MutableSet\>'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/SetAdapter.java`
#### Snippet
```java
    public MutableSet<UnsortedSetIterable<T>> powerSet()
    {
        return (MutableSet<UnsortedSetIterable<T>>) (MutableSet<?>) SetIterables.powerSet(this);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/mutable/SortedSetAdapter.java`
#### Snippet
```java
        if (this.delegate.contains(object))
        {
            return this.delegate.headSet((T) object).size();
        }
        return -1;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Comparator\>' to 'java.util.Comparator'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/mutable/SortedSetAdapter.java`
#### Snippet
```java
    public <S> MutableSortedSet<S> selectInstancesOf(Class<S> clazz)
    {
        TreeSortedSet<S> result = TreeSortedSet.newSet((Comparator<? super S>) this.comparator());
        this.forEach(new SelectInstancesOfProcedure<>(clazz, result));
        return result;
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.set.sorted.MutableSortedSet\>' to 'org.eclipse.collections.api.set.sorted.MutableSortedSet\>'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/mutable/SortedSetAdapter.java`
#### Snippet
```java
    public MutableSortedSet<SortedSetIterable<T>> powerSet()
    {
        return (MutableSortedSet<SortedSetIterable<T>>) (MutableSortedSet<?>) SortedSetIterables.powerSet(this);
    }

```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/strategy/mutable/MutableHashingStrategySetFactoryImpl.java`
#### Snippet
```java
    public <T> MutableSet<T> with(HashingStrategy<? super T> hashingStrategy)
    {
        return UnifiedSetWithHashingStrategy.newSetWith(hashingStrategy);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/strategy/immutable/ImmutableHashingStrategySetFactoryImpl.java`
#### Snippet
```java
    public <T> ImmutableSet<T> withAll(HashingStrategy<? super T> hashingStrategy, Iterable<? extends T> items)
    {
        return this.with(hashingStrategy, (T[]) Iterate.toArray(items));
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.api.block.HashingStrategy'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/strategy/immutable/ImmutableSetWithHashingStrategySerializationProxy.java`
#### Snippet
```java
    public void readExternal(ObjectInput in) throws IOException, ClassNotFoundException
    {
        HashingStrategy<? super T> strategy = (HashingStrategy<? super T>) in.readObject();
        int size = in.readInt();
        UnifiedSetWithHashingStrategy<T> deserializedSet = UnifiedSetWithHashingStrategy.newSet(strategy);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/strategy/immutable/ImmutableSetWithHashingStrategySerializationProxy.java`
#### Snippet
```java
        for (int i = 0; i < size; i++)
        {
            deserializedSet.put((T) in.readObject());
        }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        if (this.nonNullTableObjectEquals(cur, key))
        {
            return (T) cur;
        }
        return null;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        for (Object key : iterable)
        {
            this.addIfFound((T) key, retainedCopy);
        }
        if (retainedCopy.size() < this.size())
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        for (int i = 0; i < size; i++)
        {
            this.add((T) in.readObject());
        }
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Iterable\>' to 'java.util.List'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        if (iterable instanceof List && iterable instanceof RandomAccess)
        {
            List<T> list = (List<T>) iterable;
            for (int i = 0; i < size; i++)
            {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Iterable\>' to 'org.eclipse.collections.impl.set.mutable.UnifiedSet'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        if (source instanceof UnifiedSet)
        {
            return new UnifiedSet<>((UnifiedSet<K>) source);
        }
        if (source instanceof Collection)
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Iterable\>' to 'java.util.Collection'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        if (source instanceof Collection)
        {
            return new UnifiedSet<>((Collection<K>) source);
        }
        if (source == null)
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        if (cur instanceof ChainedBucket)
        {
            return this.removeFromChain((ChainedBucket) cur, (T) key, index);
        }
        if (this.nonNullTableObjectEquals(cur, (T) key))
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
            return this.removeFromChain((ChainedBucket) cur, (T) key, index);
        }
        if (this.nonNullTableObjectEquals(cur, (T) key))
        {
            this.table[index] = null;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        if (cur instanceof ChainedBucket)
        {
            return this.chainContains((ChainedBucket) cur, (T) key);
        }
        return this.nonNullTableObjectEquals(cur, (T) key);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
            return this.chainContains((ChainedBucket) cur, (T) key);
        }
        return this.nonNullTableObjectEquals(cur, (T) key);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        int size = this.size();
        T[] result = array.length < size
                ? (T[]) Array.newInstance(array.getClass().getComponentType(), size)
                : array;

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
    private T nonSentinel(Object key)
    {
        return key == NULL_KEY ? null : (T) key;
    }

```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/strategy/immutable/ImmutableEmptySetWithHashingStrategy.java`
#### Snippet
```java
    public ImmutableSet<T> newWith(T element)
    {
        return HashingStrategySets.immutable.with(this.hashingStrategy, element);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Iterable\>' to 'org.eclipse.collections.api.set.ImmutableSet'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/immutable/ImmutableSetFactoryImpl.java`
#### Snippet
```java
        if (items instanceof ImmutableSet<?>)
        {
            return (ImmutableSet<T>) items;
        }
        return this.with((T[]) Iterate.toArray(items));
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/immutable/ImmutableSetFactoryImpl.java`
#### Snippet
```java
            return (ImmutableSet<T>) items;
        }
        return this.with((T[]) Iterate.toArray(items));
    }
}
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.set.ImmutableSet\>' to 'org.eclipse.collections.api.set.ImmutableSet'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/immutable/ImmutableSetFactoryImpl.java`
#### Snippet
```java
    public <T> ImmutableSet<T> empty()
    {
        return (ImmutableSet<T>) ImmutableEmptySet.INSTANCE;
    }

```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/immutable/ImmutableQuadrupletonSet.java`
#### Snippet
```java
        if (!this.contains(element))
        {
            return Sets.immutable.with(this.element1, this.element2, this.element3, this.element4, element);
        }
        return this;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/immutable/ImmutableSetSerializationProxy.java`
#### Snippet
```java
        for (int i = 0; i < size; i++)
        {
            deserializedSet.add((T) in.readObject());
        }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.partition.set.PartitionImmutableSet\>' to 'org.eclipse.collections.api.partition.set.PartitionImmutableSet'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/immutable/ImmutableEmptySet.java`
#### Snippet
```java
    public PartitionImmutableSet<T> partition(Predicate<? super T> predicate)
    {
        return (PartitionImmutableSet<T>) EMPTY;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.api.set.MutableSet'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/MultiReaderUnifiedSet.java`
#### Snippet
```java
    public void readExternal(ObjectInput in) throws IOException, ClassNotFoundException
    {
        this.delegate = (MutableSet<T>) in.readObject();
        this.lock = new ReentrantReadWriteLock();
        this.lockWrapper = new ReadWriteLockWrapper(this.lock);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Iterable\>' to 'java.lang.Iterable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/lazy/SelectInstancesOfIterable.java`
#### Snippet
```java
    public Optional<T> detectOptional(Predicate<? super T> predicate)
    {
        return Iterate.detectOptional((Iterable<T>) this.adapted, Predicates.and(Predicates.instanceOf(this.clazz), predicate));
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Iterable\>' to 'java.lang.Iterable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/lazy/SelectInstancesOfIterable.java`
#### Snippet
```java
    public <P> void forEachWith(Procedure2<? super T, ? super P> procedure, P parameter)
    {
        Iterate.forEachWith((Iterable<T>) this.adapted, new IfProcedureWith<>(Predicates.instanceOf(this.clazz), procedure), parameter);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Iterable\>' to 'java.lang.Iterable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/lazy/SelectInstancesOfIterable.java`
#### Snippet
```java
    public void forEachWithIndex(ObjectIntProcedure<? super T> objectIntProcedure)
    {
        Iterate.forEach((Iterable<T>) this.adapted, new IfObjectIntProcedure<>(Predicates.instanceOf(this.clazz), objectIntProcedure));
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Iterable\>' to 'java.lang.Iterable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/lazy/SelectInstancesOfIterable.java`
#### Snippet
```java
    public boolean allSatisfy(Predicate<? super T> predicate)
    {
        return Iterate.allSatisfy((Iterable<T>) this.adapted, new AllSatisfyPredicate<>(Predicates.instanceOf(this.clazz), predicate));
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Iterable\>' to 'java.lang.Iterable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/lazy/SelectInstancesOfIterable.java`
#### Snippet
```java
    public boolean noneSatisfy(Predicate<? super T> predicate)
    {
        return Iterate.noneSatisfy((Iterable<T>) this.adapted, new AllSatisfyPredicate<>(Predicates.instanceOf(this.clazz), predicate));
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Iterable\>' to 'java.lang.Iterable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/lazy/SelectInstancesOfIterable.java`
#### Snippet
```java
    public boolean anySatisfy(Predicate<? super T> predicate)
    {
        return Iterate.anySatisfy((Iterable<T>) this.adapted, Predicates.and(Predicates.instanceOf(this.clazz), predicate));
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Iterable\>' to 'java.lang.Iterable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/lazy/SelectInstancesOfIterable.java`
#### Snippet
```java
    public void each(Procedure<? super T> procedure)
    {
        Iterate.forEach((Iterable<T>) this.adapted, new IfProcedure<>(Predicates.instanceOf(this.clazz), procedure));
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Iterable\>' to 'java.lang.Iterable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/lazy/SelectInstancesOfIterable.java`
#### Snippet
```java
    public T detect(Predicate<? super T> predicate)
    {
        return Iterate.detect((Iterable<T>) this.adapted, Predicates.and(Predicates.instanceOf(this.clazz), predicate));
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'V\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/lazy/FlatCollectIterable.java`
#### Snippet
```java
    public Optional<V> detectOptional(Predicate<? super V> predicate)
    {
        V[] result = (V[]) new Object[1];
        Iterate.anySatisfy(this.adapted, each ->
        {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'V\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/lazy/FlatCollectIterable.java`
#### Snippet
```java
    public V detect(Predicate<? super V> predicate)
    {
        V[] result = (V[]) new Object[1];
        Iterate.anySatisfy(this.adapted, each ->
        {
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.set.ImmutableSet\>' to 'org.eclipse.collections.api.set.ImmutableSet\>'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/immutable/AbstractImmutableSet.java`
#### Snippet
```java
    public ImmutableSet<UnsortedSetIterable<T>> powerSet()
    {
        return (ImmutableSet<UnsortedSetIterable<T>>) (ImmutableSet<?>) SetIterables.immutablePowerSet(this);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/lazy/iterator/SelectIterator.java`
#### Snippet
```java
            Object temp = this.next;
            this.next = NULL;
            return (T) temp;
        }
        throw new NoSuchElementException();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/lazy/iterator/SelectInstancesOfIterator.java`
#### Snippet
```java
            Object temp = this.next;
            this.next = NULL;
            return (T) temp;
        }
        throw new NoSuchElementException();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/lazy/iterator/TakeWhileIterator.java`
#### Snippet
```java
            Object temp = this.next;
            this.next = NULL;
            return (T) temp;
        }
        throw new NoSuchElementException();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/lazy/iterator/DistinctIterator.java`
#### Snippet
```java
            Object temp = this.next;
            this.next = NULL;
            return (T) temp;
        }
        throw new NoSuchElementException();
```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/lazy/AbstractLazyIterable.java`
#### Snippet
```java
    public LazyIterable<T> concatenate(Iterable<T> iterable)
    {
        return LazyIterate.concatenate(this, iterable);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/lazy/AbstractLazyIterable.java`
#### Snippet
```java
    public T getLast()
    {
        T[] result = (T[]) new Object[1];
        this.each(each -> result[0] = each);
        return result[0];
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.ParallelIterable' to 'org.eclipse.collections.api.ParallelIterable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/lazy/parallel/AbstractParallelIterableImpl.java`
#### Snippet
```java
    public <S> ParallelIterable<S> selectInstancesOf(Class<S> clazz)
    {
        return (ParallelIterable<S>) this.select(Predicates.instanceOf(clazz));
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.bag.ParallelUnsortedBag' to 'org.eclipse.collections.api.bag.ParallelUnsortedBag'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/lazy/parallel/bag/AbstractParallelUnsortedBag.java`
#### Snippet
```java
    public <S> ParallelUnsortedBag<S> selectInstancesOf(Class<S> clazz)
    {
        return (ParallelUnsortedBag<S>) this.select(Predicates.instanceOf(clazz));
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/strategy/mutable/UnifiedSetWithHashingStrategy.java`
#### Snippet
```java
    public boolean contains(Object key)
    {
        int index = this.index((T) key);
        Object cur = this.table[index];
        if (cur == null)
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/strategy/mutable/UnifiedSetWithHashingStrategy.java`
#### Snippet
```java
        if (cur instanceof ChainedBucket)
        {
            return this.chainContains((ChainedBucket) cur, (T) key);
        }
        return this.nonNullTableObjectEquals(cur, (T) key);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/strategy/mutable/UnifiedSetWithHashingStrategy.java`
#### Snippet
```java
            return this.chainContains((ChainedBucket) cur, (T) key);
        }
        return this.nonNullTableObjectEquals(cur, (T) key);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/strategy/mutable/UnifiedSetWithHashingStrategy.java`
#### Snippet
```java
        {
            this.table[index] = UnifiedSetWithHashingStrategy.toSentinelIfNull(key);
            return (T) cur;
        }
        this.chainedAdd(key, index);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/strategy/mutable/UnifiedSetWithHashingStrategy.java`
#### Snippet
```java
        for (Object key : iterable)
        {
            this.addIfFound((T) key, retainedCopy);
        }
        if (retainedCopy.size() < this.size())
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/strategy/mutable/UnifiedSetWithHashingStrategy.java`
#### Snippet
```java
        if (this.nonNullTableObjectEquals(cur, key))
        {
            return (T) cur;
        }
        return null;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/strategy/mutable/UnifiedSetWithHashingStrategy.java`
#### Snippet
```java
    public boolean remove(Object key)
    {
        int index = this.index((T) key);

        Object cur = this.table[index];
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/strategy/mutable/UnifiedSetWithHashingStrategy.java`
#### Snippet
```java
        if (cur instanceof ChainedBucket)
        {
            return this.removeFromChain((ChainedBucket) cur, (T) key, index);
        }
        if (this.nonNullTableObjectEquals(cur, (T) key))
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/strategy/mutable/UnifiedSetWithHashingStrategy.java`
#### Snippet
```java
            return this.removeFromChain((ChainedBucket) cur, (T) key, index);
        }
        if (this.nonNullTableObjectEquals(cur, (T) key))
        {
            this.table[index] = null;
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.impl.set.strategy.mutable.UnifiedSetWithHashingStrategy' to 'org.eclipse.collections.impl.set.strategy.mutable.UnifiedSetWithHashingStrategy'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/strategy/mutable/UnifiedSetWithHashingStrategy.java`
#### Snippet
```java
    public <S> UnifiedSetWithHashingStrategy<S> selectInstancesOf(Class<S> clazz)
    {
        UnifiedSetWithHashingStrategy<S> result = (UnifiedSetWithHashingStrategy<S>) this.newEmpty();
        this.forEach(new SelectInstancesOfProcedure<>(clazz, result));
        return result;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.api.block.HashingStrategy'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/strategy/mutable/UnifiedSetWithHashingStrategy.java`
#### Snippet
```java
    public void readExternal(ObjectInput in) throws IOException, ClassNotFoundException
    {
        this.hashingStrategy = (HashingStrategy<? super T>) in.readObject();
        int size = in.readInt();
        this.loadFactor = in.readFloat();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/strategy/mutable/UnifiedSetWithHashingStrategy.java`
#### Snippet
```java
        for (int i = 0; i < size; i++)
        {
            this.add((T) in.readObject());
        }
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/strategy/mutable/UnifiedSetWithHashingStrategy.java`
#### Snippet
```java
    private T nonSentinel(Object key)
    {
        return key == NULL_KEY ? null : (T) key;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Iterable\>' to 'org.eclipse.collections.impl.set.strategy.mutable.UnifiedSetWithHashingStrategy'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/strategy/mutable/UnifiedSetWithHashingStrategy.java`
#### Snippet
```java
        if (source instanceof UnifiedSetWithHashingStrategy<?>)
        {
            return new UnifiedSetWithHashingStrategy<>(hashingStrategy, (UnifiedSetWithHashingStrategy<K>) source);
        }
        if (source instanceof Collection<?>)
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Iterable\>' to 'java.util.Collection'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/strategy/mutable/UnifiedSetWithHashingStrategy.java`
#### Snippet
```java
        if (source instanceof Collection<?>)
        {
            return new UnifiedSetWithHashingStrategy<>(hashingStrategy, (Collection<K>) source);
        }
        if (source == null)
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/strategy/mutable/UnifiedSetWithHashingStrategy.java`
#### Snippet
```java
        int size = this.size();
        T[] result = array.length < size
                ? (T[]) Array.newInstance(array.getClass().getComponentType(), size)
                : array;

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.set.ParallelUnsortedSetIterable' to 'org.eclipse.collections.api.set.ParallelUnsortedSetIterable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/lazy/parallel/set/AbstractParallelUnsortedSetIterable.java`
#### Snippet
```java
    public <S> ParallelUnsortedSetIterable<S> selectInstancesOf(Class<S> clazz)
    {
        return (ParallelUnsortedSetIterable<S>) this.select(Predicates.instanceOf(clazz));
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.set.sorted.ParallelSortedSetIterable' to 'org.eclipse.collections.api.set.sorted.ParallelSortedSetIterable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/lazy/parallel/set/sorted/AbstractParallelSortedSetIterable.java`
#### Snippet
```java
    public <S> ParallelSortedSetIterable<S> selectInstancesOf(Class<S> clazz)
    {
        return (ParallelSortedSetIterable<S>) this.select(Predicates.instanceOf(clazz));
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.list.ParallelListIterable' to 'org.eclipse.collections.api.list.ParallelListIterable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/lazy/parallel/list/AbstractParallelListIterable.java`
#### Snippet
```java
    public <S> ParallelListIterable<S> selectInstancesOf(Class<S> clazz)
    {
        return (ParallelListIterable<S>) this.select(Predicates.instanceOf(clazz));
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/TripletonList.java`
#### Snippet
```java
    public void readExternal(ObjectInput in) throws IOException, ClassNotFoundException
    {
        this.element1 = (T) in.readObject();
        this.element2 = (T) in.readObject();
        this.element3 = (T) in.readObject();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/TripletonList.java`
#### Snippet
```java
    {
        this.element1 = (T) in.readObject();
        this.element2 = (T) in.readObject();
        this.element3 = (T) in.readObject();
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/TripletonList.java`
#### Snippet
```java
        this.element1 = (T) in.readObject();
        this.element2 = (T) in.readObject();
        this.element3 = (T) in.readObject();
    }
}
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/QuadrupletonList.java`
#### Snippet
```java
    public void readExternal(ObjectInput in) throws IOException, ClassNotFoundException
    {
        this.element1 = (T) in.readObject();
        this.element2 = (T) in.readObject();
        this.element3 = (T) in.readObject();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/QuadrupletonList.java`
#### Snippet
```java
    {
        this.element1 = (T) in.readObject();
        this.element2 = (T) in.readObject();
        this.element3 = (T) in.readObject();
        this.element4 = (T) in.readObject();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/QuadrupletonList.java`
#### Snippet
```java
        this.element1 = (T) in.readObject();
        this.element2 = (T) in.readObject();
        this.element3 = (T) in.readObject();
        this.element4 = (T) in.readObject();
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/QuadrupletonList.java`
#### Snippet
```java
        this.element2 = (T) in.readObject();
        this.element3 = (T) in.readObject();
        this.element4 = (T) in.readObject();
    }
}
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/SingletonList.java`
#### Snippet
```java
    public void readExternal(ObjectInput in) throws IOException, ClassNotFoundException
    {
        this.element1 = (T) in.readObject();
    }
}
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/QuintupletonList.java`
#### Snippet
```java
    public void readExternal(ObjectInput in) throws IOException, ClassNotFoundException
    {
        this.element1 = (T) in.readObject();
        this.element2 = (T) in.readObject();
        this.element3 = (T) in.readObject();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/QuintupletonList.java`
#### Snippet
```java
    {
        this.element1 = (T) in.readObject();
        this.element2 = (T) in.readObject();
        this.element3 = (T) in.readObject();
        this.element4 = (T) in.readObject();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/QuintupletonList.java`
#### Snippet
```java
        this.element1 = (T) in.readObject();
        this.element2 = (T) in.readObject();
        this.element3 = (T) in.readObject();
        this.element4 = (T) in.readObject();
        this.element5 = (T) in.readObject();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/QuintupletonList.java`
#### Snippet
```java
        this.element2 = (T) in.readObject();
        this.element3 = (T) in.readObject();
        this.element4 = (T) in.readObject();
        this.element5 = (T) in.readObject();
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/QuintupletonList.java`
#### Snippet
```java
        this.element3 = (T) in.readObject();
        this.element4 = (T) in.readObject();
        this.element5 = (T) in.readObject();
    }
}
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/DoubletonList.java`
#### Snippet
```java
    public void readExternal(ObjectInput in) throws IOException, ClassNotFoundException
    {
        this.element1 = (T) in.readObject();
        this.element2 = (T) in.readObject();
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/DoubletonList.java`
#### Snippet
```java
    {
        this.element1 = (T) in.readObject();
        this.element2 = (T) in.readObject();
    }
}
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'E\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/ArrayAdapter.java`
#### Snippet
```java
    public static <E> ArrayAdapter<E> newArray(Iterable<? extends E> source)
    {
        return new ArrayAdapter<>((E[]) Iterate.toArray(source));
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'E\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/ArrayAdapter.java`
#### Snippet
```java
    public static <E> ArrayAdapter<E> newArrayWith(E one, E two, E three)
    {
        return new ArrayAdapter<>((E[]) new Object[]{one, two, three});
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'E\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/ArrayAdapter.java`
#### Snippet
```java
    {
        int oldSize = Iterate.sizeOf(iterable);
        E[] array = (E[]) new Object[oldSize + 1];
        Iterate.toArray(iterable, array);
        array[oldSize] = itemToAdd;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'E\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/ArrayAdapter.java`
#### Snippet
```java
    public static <E> ArrayAdapter<E> newArrayWith(E one, E two, E three, E four, E five)
    {
        return new ArrayAdapter<>((E[]) new Object[]{one, two, three, four, five});
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'E\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/ArrayAdapter.java`
#### Snippet
```java
    public static <E> ArrayAdapter<E> newArrayWith(E one, E two, E three, E four)
    {
        return new ArrayAdapter<>((E[]) new Object[]{one, two, three, four});
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'E\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/ArrayAdapter.java`
#### Snippet
```java
    public static <E> ArrayAdapter<E> newArrayWith(E one)
    {
        return new ArrayAdapter<>((E[]) new Object[]{one});
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'E\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/ArrayAdapter.java`
#### Snippet
```java
    public static <E> ArrayAdapter<E> newArrayWith(E one, E two, E three, E four, E five, E six)
    {
        return new ArrayAdapter<>((E[]) new Object[]{one, two, three, four, five, six});
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/ArrayAdapter.java`
#### Snippet
```java
        // Read in array length and allocate array
        int arrayLength = objectInputStream.readInt();
        this.items = (T[]) new Object[arrayLength];
        Object[] localItems = this.items;

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'E\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/ArrayAdapter.java`
#### Snippet
```java
    public static <E> ArrayAdapter<E> newArrayWith(E one, E two, E three, E four, E five, E six, E seven)
    {
        return new ArrayAdapter<>((E[]) new Object[]{one, two, three, four, five, six, seven});
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'E\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/ArrayAdapter.java`
#### Snippet
```java
    public static <E> ArrayAdapter<E> newArrayWith(E one, E two)
    {
        return new ArrayAdapter<>((E[]) new Object[]{one, two});
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'E\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/ArrayAdapter.java`
#### Snippet
```java
    public static <E> ArrayAdapter<E> newArray()
    {
        return ArrayAdapter.newArrayWith((E[]) EMPTY_ARRAY);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.list.FixedSizeList\>' to 'org.eclipse.collections.api.list.FixedSizeList'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/FixedSizeListFactoryImpl.java`
#### Snippet
```java
    public <T> FixedSizeList<T> empty()
    {
        return (FixedSizeList<T>) FixedSizeListFactoryImpl.EMPTY_LIST;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/FixedSizeListFactoryImpl.java`
#### Snippet
```java
    public <T> FixedSizeList<T> fromStream(Stream<? extends T> stream)
    {
        return this.of((T[]) stream.toArray());
    }
}
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/FixedSizeListFactoryImpl.java`
#### Snippet
```java
    public <T> FixedSizeList<T> withAll(Iterable<? extends T> items)
    {
        return this.of((T[]) Iterate.toArray(items));
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/SextupletonList.java`
#### Snippet
```java
    public void readExternal(ObjectInput in) throws IOException, ClassNotFoundException
    {
        this.element1 = (T) in.readObject();
        this.element2 = (T) in.readObject();
        this.element3 = (T) in.readObject();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/SextupletonList.java`
#### Snippet
```java
    {
        this.element1 = (T) in.readObject();
        this.element2 = (T) in.readObject();
        this.element3 = (T) in.readObject();
        this.element4 = (T) in.readObject();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/SextupletonList.java`
#### Snippet
```java
        this.element1 = (T) in.readObject();
        this.element2 = (T) in.readObject();
        this.element3 = (T) in.readObject();
        this.element4 = (T) in.readObject();
        this.element5 = (T) in.readObject();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/SextupletonList.java`
#### Snippet
```java
        this.element2 = (T) in.readObject();
        this.element3 = (T) in.readObject();
        this.element4 = (T) in.readObject();
        this.element5 = (T) in.readObject();
        this.element6 = (T) in.readObject();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/SextupletonList.java`
#### Snippet
```java
        this.element3 = (T) in.readObject();
        this.element4 = (T) in.readObject();
        this.element5 = (T) in.readObject();
        this.element6 = (T) in.readObject();
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/SextupletonList.java`
#### Snippet
```java
        this.element4 = (T) in.readObject();
        this.element5 = (T) in.readObject();
        this.element6 = (T) in.readObject();
    }
}
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.api.list.MutableList'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/AbstractListAdapter.java`
#### Snippet
```java
        try
        {
            return (MutableList<T>) super.clone();
        }
        catch (CloneNotSupportedException e)
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.ArrayList'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/ArrayListAdapter.java`
#### Snippet
```java
    public ArrayListAdapter<T> clone()
    {
        return new ArrayListAdapter<>((ArrayList<T>) this.delegate.clone());
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'E\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/AbstractArrayAdapter.java`
#### Snippet
```java
        int size = this.size();
        E[] result = array.length < size
                ? (E[]) Array.newInstance(array.getClass().getComponentType(), size)
                : array;

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/AbstractArrayAdapter.java`
#### Snippet
```java
    protected AbstractArrayAdapter()
    {
        this.items = (T[]) OBJECTS;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'L' to 'org.eclipse.collections.api.list.MutableList'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/SynchronizedMutableList.java`
#### Snippet
```java
    {
        MutableList<E> mutableList =
                list instanceof MutableList ? (MutableList<E>) list : ListAdapter.adapt(list);
        return new SynchronizedMutableList<>(mutableList, lock);
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.impl.list.mutable.SynchronizedMutableList' to 'java.util.List\>'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/SynchronizedMutableList.java`
#### Snippet
```java
        synchronized (this.getLock())
        {
            return Collections.binarySearch((List<? extends Comparable<? super T>>) this, key);
        }
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'L' to 'org.eclipse.collections.api.list.MutableList'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/SynchronizedMutableList.java`
#### Snippet
```java
    {
        MutableList<E> mutableList =
                list instanceof MutableList ? (MutableList<E>) list : ListAdapter.adapt(list);
        return new SynchronizedMutableList<>(mutableList);
    }
```

### UNCHECKED_WARNING
Unchecked assignment: 'java.util.Iterator\[\]' to 'java.util.Iterator\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/CompositeFastList.java`
#### Snippet
```java
        private CompositeIterator(FastList<FastList<E>> newLists)
        {
            this.iterators = new Iterator[newLists.size()];
            for (int i = 0; i < newLists.size(); ++i)
            {
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.lang.Object\[\]', required 'E\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/CompositeFastList.java`
#### Snippet
```java

    @Override
    public Object[] toArray(Object[] array)
    {
        int size = this.size();
```

### UNCHECKED_WARNING
Unchecked overriding: return type requires unchecked conversion. Found 'java.lang.Object\[\]', required 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/CompositeFastList.java`
#### Snippet
```java

    @Override
    public Object[] toArray(Object[] array)
    {
        int size = this.size();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Collection\>' to 'org.eclipse.collections.impl.list.mutable.FastList'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/CompositeFastList.java`
#### Snippet
```java
        }
        this.size += collection.size();
        this.lists.add((FastList<E>) collection);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.impl.list.mutable.MultiReaderFastList' to 'java.util.List\>'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/MultiReaderFastList.java`
#### Snippet
```java
        try (LockWrapper wrapper = this.lockWrapper.acquireReadLock())
        {
            return Collections.binarySearch((List<? extends Comparable<? super T>>) this, key);
        }
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.api.list.MutableList'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/MultiReaderFastList.java`
#### Snippet
```java
    public void readExternal(ObjectInput in) throws IOException, ClassNotFoundException
    {
        this.delegate = (MutableList<T>) in.readObject();
        this.lock = new ReentrantReadWriteLock();
        this.lockWrapper = new ReadWriteLockWrapper(this.lock);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/immutable/ImmutableListFactoryImpl.java`
#### Snippet
```java
            return this.with(items.getOnly());
        }
        T[] array = (T[]) items.toArray();
        Arrays.sort(array);
        return new ImmutableArrayList<>(array);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Iterable\>' to 'org.eclipse.collections.api.list.ImmutableList'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/immutable/ImmutableListFactoryImpl.java`
#### Snippet
```java
        if (items instanceof ImmutableList<?>)
        {
            return (ImmutableList<T>) items;
        }
        if (items instanceof List && items instanceof RandomAccess)
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Iterable\>' to 'java.util.List'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/immutable/ImmutableListFactoryImpl.java`
#### Snippet
```java
        if (items instanceof List && items instanceof RandomAccess)
        {
            return this.withList((List<T>) items);
        }
        return new ImmutableArrayList<>((T[]) Iterate.toArray(items));
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/immutable/ImmutableListFactoryImpl.java`
#### Snippet
```java
            return this.withList((List<T>) items);
        }
        return new ImmutableArrayList<>((T[]) Iterate.toArray(items));
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/immutable/ImmutableListFactoryImpl.java`
#### Snippet
```java
            return this.with(items.getOnly());
        }
        T[] array = (T[]) items.toArray();
        Arrays.sort(array, comparator);
        return new ImmutableArrayList<>(array);
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.list.ImmutableList\>' to 'org.eclipse.collections.api.list.ImmutableList'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/immutable/ImmutableListFactoryImpl.java`
#### Snippet
```java
    public <T> ImmutableList<T> empty()
    {
        return (ImmutableList<T>) ImmutableEmptyList.INSTANCE;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.partition.list.PartitionImmutableList\>' to 'org.eclipse.collections.api.partition.list.PartitionImmutableList'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/immutable/ImmutableEmptyList.java`
#### Snippet
```java
    public PartitionImmutableList<T> partitionWhile(Predicate<? super T> predicate)
    {
        return (PartitionImmutableList<T>) EMPTY;
    }
}
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.List'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/immutable/ImmutableEmptyList.java`
#### Snippet
```java
            return false;
        }
        List<T> list = (List<T>) otherList;
        return list.isEmpty();
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.partition.list.PartitionImmutableList\>' to 'org.eclipse.collections.api.partition.list.PartitionImmutableList'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/immutable/ImmutableEmptyList.java`
#### Snippet
```java
    public PartitionImmutableList<T> partition(Predicate<? super T> predicate)
    {
        return (PartitionImmutableList<T>) EMPTY;
    }

```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/immutable/ImmutableDecapletonList.java`
#### Snippet
```java
    public ImmutableList<T> newWith(T newItem)
    {
        return Lists.immutable.with(
                this.element1,
                this.element2,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/immutable/ImmutableArrayList.java`
#### Snippet
```java
        int startIndex = this.detectNotIndex(predicate);
        int resultSize = this.size() - startIndex;
        T[] result = (T[]) new Object[resultSize];
        System.arraycopy(this.items, startIndex, result, 0, resultSize);
        return new ImmutableArrayList<>(result);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/immutable/ImmutableArrayList.java`
#### Snippet
```java
        int partitionIndex = this.detectNotIndex(predicate);
        int rejectedSize = this.size() - partitionIndex;
        T[] selectedArray = (T[]) new Object[partitionIndex];
        T[] rejectedArray = (T[]) new Object[rejectedSize];
        System.arraycopy(this.items, 0, selectedArray, 0, partitionIndex);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/immutable/ImmutableArrayList.java`
#### Snippet
```java
        int rejectedSize = this.size() - partitionIndex;
        T[] selectedArray = (T[]) new Object[partitionIndex];
        T[] rejectedArray = (T[]) new Object[rejectedSize];
        System.arraycopy(this.items, 0, selectedArray, 0, partitionIndex);
        System.arraycopy(this.items, partitionIndex, rejectedArray, 0, rejectedSize);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'E\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/immutable/ImmutableArrayList.java`
#### Snippet
```java
        if (a.length < size)
        {
            a = (E[]) Array.newInstance(a.getClass().getComponentType(), size);
        }
        System.arraycopy(this.items, 0, a, 0, size);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/immutable/ImmutableArrayList.java`
#### Snippet
```java
    {
        int oldSize = this.size();
        T[] array = (T[]) new Object[oldSize + 1];
        this.toArray(array);
        array[oldSize] = newItem;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'E\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/immutable/ImmutableArrayList.java`
#### Snippet
```java
    public static <E> ImmutableArrayList<E> newList(Iterable<? extends E> iterable)
    {
        return new ImmutableArrayList<>((E[]) Iterate.toArray(iterable));
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/immutable/ImmutableArrayList.java`
#### Snippet
```java
    {
        int endIndex = this.detectNotIndex(predicate);
        T[] result = (T[]) new Object[endIndex];
        System.arraycopy(this.items, 0, result, 0, endIndex);
        return new ImmutableArrayList<>(result);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.api.list.MutableList'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/AbstractMutableList.java`
#### Snippet
```java
        try
        {
            return (MutableList<T>) super.clone();
        }
        catch (CloneNotSupportedException e)
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/FastList.java`
#### Snippet
```java
    {
        this.size = in.readInt();
        this.items = (T[]) new Object[this.size];
        for (int i = 0; i < this.size; i++)
        {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/FastList.java`
#### Snippet
```java
        for (int i = 0; i < this.size; i++)
        {
            this.items[i] = (T) in.readObject();
        }
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/FastList.java`
#### Snippet
```java
        int startIndex = this.detectNotIndex(predicate);
        int resultSize = this.size() - startIndex;
        T[] result = (T[]) new Object[resultSize];
        System.arraycopy(this.items, startIndex, result, 0, resultSize);
        return FastList.newListWith(result);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Collection\>' to 'org.eclipse.collections.impl.list.mutable.FastList'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/FastList.java`
#### Snippet
```java
        if (source.getClass() == FastList.class)
        {
            this.addAllFastListAtIndex((FastList<T>) source, index);
        }
        else if (source.getClass() == ArrayList.class)
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Collection\>' to 'java.util.ArrayList'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/FastList.java`
#### Snippet
```java
        else if (source.getClass() == ArrayList.class)
        {
            this.addAllArrayListAtIndex((ArrayList<T>) source, index);
        }
        else if (source instanceof List && source instanceof RandomAccess)
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Collection\>' to 'java.util.List'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/FastList.java`
#### Snippet
```java
        else if (source instanceof List && source instanceof RandomAccess)
        {
            this.addAllRandomAccessListAtIndex((List<T>) source, index);
        }
        else
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/FastList.java`
#### Snippet
```java

    protected int size;
    protected transient T[] items = (T[]) DEFAULT_SIZED_EMPTY_ARRAY;

    public FastList()
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/FastList.java`
#### Snippet
```java
    private void transferItemsToNewArrayWithCapacity(int newCapacity)
    {
        this.items = (T[]) this.copyItemsWithNewCapacity(newCapacity);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'E\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/FastList.java`
#### Snippet
```java
        if (array.length < this.size)
        {
            array = (E[]) Array.newInstance(array.getClass().getComponentType(), this.size);
        }
        System.arraycopy(this.items, 0, array, 0, this.size);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/FastList.java`
#### Snippet
```java
            throw new IllegalArgumentException("Expected initial capacity is greater than or equal to 0. Was: " + initialCapacity);
        }
        this.items = initialCapacity == 0 ? (T[]) ZERO_SIZED_ARRAY : (T[]) new Object[initialCapacity];
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/FastList.java`
#### Snippet
```java
            throw new IllegalArgumentException("Expected initial capacity is greater than or equal to 0. Was: " + initialCapacity);
        }
        this.items = initialCapacity == 0 ? (T[]) ZERO_SIZED_ARRAY : (T[]) new Object[initialCapacity];
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'E\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/FastList.java`
#### Snippet
```java
    public <E> E[] toArray(int sourceFromIndex, int sourceToIndex)
    {
        return this.toArray((E[]) new Object[sourceToIndex - sourceFromIndex + 1], sourceFromIndex, sourceToIndex, 0);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/FastList.java`
#### Snippet
```java
        if (this.items == DEFAULT_SIZED_EMPTY_ARRAY)
        {
            this.items = (T[]) new Object[10];
        }
        else
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/FastList.java`
#### Snippet
```java
    public T[] toTypedArray(Class<T> clazz)
    {
        T[] array = (T[]) Array.newInstance(clazz, this.size);
        System.arraycopy(this.items, 0, array, 0, this.size);
        return array;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Collection\>' to 'org.eclipse.collections.impl.list.mutable.FastList'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/FastList.java`
#### Snippet
```java
        if (source.getClass() == FastList.class)
        {
            this.addAllFastList((FastList<T>) source);
        }
        else if (source.getClass() == ArrayList.class)
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Collection\>' to 'java.util.ArrayList'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/FastList.java`
#### Snippet
```java
        else if (source.getClass() == ArrayList.class)
        {
            this.addAllArrayList((ArrayList<T>) source);
        }
        else if (source instanceof List && source instanceof RandomAccess)
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Collection\>' to 'java.util.List'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/FastList.java`
#### Snippet
```java
        else if (source instanceof List && source instanceof RandomAccess)
        {
            this.addAllRandomAccessList((List<T>) source);
        }
        else
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/FastList.java`
#### Snippet
```java
        if (this.items.length == oldSize)
        {
            T[] newItems = (T[]) new Object[this.sizePlusFiftyPercent(oldSize)];
            if (index > 0)
            {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'E\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/FastList.java`
#### Snippet
```java
    public static <E> FastList<E> newList(Iterable<? extends E> source)
    {
        return FastList.newListWith((E[]) Iterate.toArray(source));
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'E\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/FastList.java`
#### Snippet
```java
    public static <E> FastList<E> wrapCopy(E... array)
    {
        E[] newArray = (E[]) new Object[array.length];
        System.arraycopy(array, 0, newArray, 0, array.length);
        return new FastList<>(newArray);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/FastList.java`
#### Snippet
```java
    {
        int endIndex = this.detectNotIndex(predicate);
        T[] result = (T[]) new Object[endIndex];
        System.arraycopy(this.items, 0, result, 0, endIndex);
        return FastList.newListWith(result);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/FastList.java`
#### Snippet
```java
    public FastList(Collection<? extends T> source)
    {
        this.items = (T[]) source.toArray();
        this.size = this.items.length;
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/immutable/AbstractImmutableList.java`
#### Snippet
```java
        {
            int oldSize = this.size();
            T[] array = (T[]) new Object[oldSize + 1];
            this.toArray(array);
            array[oldSize] = newItem;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/immutable/AbstractImmutableList.java`
#### Snippet
```java
    public ImmutableList<T> newWithoutAll(Iterable<? extends T> elements)
    {
        FastList<T> result = FastList.newListWith((T[]) this.toArray());
        this.removeAllFrom(elements, result);
        return result.toImmutable();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/immutable/AbstractImmutableList.java`
#### Snippet
```java
            return this;
        }
        T[] results = (T[]) new Object[this.size() - 1];
        int currentIndex = 0;
        for (int i = 0; i < this.size(); i++)
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/immutable/AbstractImmutableList.java`
#### Snippet
```java
        int oldSize = this.size();
        int newSize = Iterate.sizeOf(elements);
        T[] array = (T[]) new Object[oldSize + newSize];
        this.toArray(array);
        Iterate.forEachWithIndex(elements, (each, index) -> array[oldSize + index] = each);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bimap/immutable/ImmutableBiMapSerializationProxy.java`
#### Snippet
```java
        for (int i = 0; i < size; i++)
        {
            if (deserializedBiMap.put((K) in.readObject(), (V) in.readObject()) != null)
            {
                throw new IllegalStateException();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bimap/immutable/ImmutableBiMapSerializationProxy.java`
#### Snippet
```java
        for (int i = 0; i < size; i++)
        {
            if (deserializedBiMap.put((K) in.readObject(), (V) in.readObject()) != null)
            {
                throw new IllegalStateException();
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.map.ImmutableMap' to 'org.eclipse.collections.api.bimap.ImmutableBiMap'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bimap/immutable/ImmutableBiMapFactoryImpl.java`
#### Snippet
```java
        if (immutableMap instanceof ImmutableBiMap)
        {
            return (ImmutableBiMap<K, V>) immutableMap;
        }
        return new ImmutableHashBiMap<>(immutableMap, immutableMap.flipUniqueValues());
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Map' to 'org.eclipse.collections.api.bimap.ImmutableBiMap'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bimap/immutable/ImmutableBiMapFactoryImpl.java`
#### Snippet
```java
        if (map instanceof ImmutableBiMap<?, ?>)
        {
            return (ImmutableBiMap<K, V>) map;
        }
        if (map instanceof MutableBiMap<?, ?>)
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.impl.bimap.immutable.ImmutableHashBiMap,capture\>' to 'org.eclipse.collections.api.bimap.ImmutableBiMap'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bimap/immutable/ImmutableBiMapFactoryImpl.java`
#### Snippet
```java
    public <K, V> ImmutableBiMap<K, V> empty()
    {
        return (ImmutableBiMap<K, V>) EMPTY_INSTANCE;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.map.MutableMapIterable,V\>' to 'org.eclipse.collections.api.bimap.MutableBiMap'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bimap/mutable/SynchronizedBiMap.java`
#### Snippet
```java
    public <VV> MutableBiMap<VV, V> groupByUniqueKey(Function<? super V, ? extends VV> function)
    {
        return (MutableBiMap<VV, V>) super.groupByUniqueKey(function);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.block.function.Function0\>' to 'org.eclipse.collections.api.block.function.Function0\>'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Functions0.java`
#### Snippet
```java
    public static <T> Function0<MutableBag<T>> newHashBag()
    {
        return (Function0<MutableBag<T>>) (Function0<?>) NEW_HASH_BAG_FUNCTION;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.block.function.Function0\>' to 'org.eclipse.collections.api.block.function.Function0\>'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Functions0.java`
#### Snippet
```java
    public static <T> Function0<MutableSet<T>> newUnifiedSet()
    {
        return (Function0<MutableSet<T>>) (Function0<?>) NEW_UNIFIED_SET_FUNCTION;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.block.function.Function0\>' to 'org.eclipse.collections.api.block.function.Function0\>'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Functions0.java`
#### Snippet
```java
    public static <T> Function0<MutableList<T>> newFastList()
    {
        return (Function0<MutableList<T>>) (Function0<?>) NEW_FAST_LIST_FUNCTION;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.impl.block.factory.Functions0.NullFunction\>' to 'org.eclipse.collections.api.block.function.Function0'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Functions0.java`
#### Snippet
```java
    public static <T> Function0<T> nullValue()
    {
        return (Function0<T>) NULL_FUNCTION;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.block.function.Function0\>' to 'org.eclipse.collections.api.block.function.Function0\>'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Functions0.java`
#### Snippet
```java
    public static <K, V> Function0<MutableMap<K, V>> newUnifiedMap()
    {
        return (Function0<MutableMap<K, V>>) (Function0<?>) NEW_UNIFIED_MAP_FUNCTION;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.impl.block.factory.Procedures.NoopProcedure' to 'org.eclipse.collections.api.block.procedure.Procedure'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Procedures.java`
#### Snippet
```java
    public static <T> Procedure<T> noop()
    {
        return (Procedure<T>) NoopProcedure.INSTANCE;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bimap/AbstractBiMap.java`
#### Snippet
```java
    public V getOrDefault(Object key, V defaultValue)
    {
        return this.getIfAbsentValue((K) key, defaultValue);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.block.procedure.Procedure2,capture\>' to 'org.eclipse.collections.api.block.procedure.Procedure2\>'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Procedures2.java`
#### Snippet
```java
    public static <T> Procedure2<T, Collection<T>> addToCollection()
    {
        return (Procedure2<T, Collection<T>>) ADD_TO_COLLECTION;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.block.procedure.Procedure2,capture\>' to 'org.eclipse.collections.api.block.procedure.Procedure2\>'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Procedures2.java`
#### Snippet
```java
    public static <T> Procedure2<T, Collection<T>> removeFromCollection()
    {
        return (Procedure2<T, Collection<T>>) REMOVE_FROM_COLLECTION;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.impl.block.factory.Predicates2,capture\>' to 'org.eclipse.collections.impl.block.factory.Predicates2'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Predicates2.java`
#### Snippet
```java
    public static <T extends Comparable<T>> Predicates2<T, T> lessThanOrEqualTo()
    {
        return (Predicates2<T, T>) LESS_THAN_OR_EQUAL;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.impl.block.factory.Predicates2,capture\>' to 'org.eclipse.collections.impl.block.factory.Predicates2'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Predicates2.java`
#### Snippet
```java
    public static <T extends Comparable<T>> Predicates2<T, T> greaterThanOrEqualTo()
    {
        return (Predicates2<T, T>) GREATER_THAN_OR_EQUAL;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.impl.block.factory.Predicates2,capture\>' to 'org.eclipse.collections.api.block.predicate.Predicate2'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Predicates2.java`
#### Snippet
```java
            Function<? super T, ? extends P> function)
    {
        return new AttributePredicates2<>(function, (Predicate2<P, P>) LESS_THAN_OR_EQUAL);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.impl.block.factory.Predicates2,capture\>' to 'org.eclipse.collections.api.block.predicate.Predicate2'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Predicates2.java`
#### Snippet
```java
    public static <T, P extends Comparable<? super P>> Predicates2<T, P> attributeGreaterThan(Function<? super T, ? extends P> function)
    {
        return new AttributePredicates2<>(function, (Predicate2<P, P>) GREATER_THAN);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.impl.block.factory.Predicates2,capture\>' to 'org.eclipse.collections.api.block.predicate.Predicate2'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Predicates2.java`
#### Snippet
```java
    public static <T, P extends Comparable<? super P>> Predicates2<T, P> attributeLessThan(Function<? super T, ? extends P> function)
    {
        return new AttributePredicates2<>(function, (Predicate2<P, P>) LESS_THAN);
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'org.eclipse.collections.impl.block.factory.Predicates2.AttributePredicates2' to 'org.eclipse.collections.impl.block.factory.Predicates2\>'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Predicates2.java`
#### Snippet
```java
    {
        //noinspection rawtypes
        return new AttributePredicates2(function, Predicates2.notIn());
    }

```

### UNCHECKED_WARNING
Unchecked call to 'AttributePredicates2(Function, Predicate2)' as a member of raw type 'org.eclipse.collections.impl.block.factory.Predicates2.AttributePredicates2'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Predicates2.java`
#### Snippet
```java
    {
        //noinspection rawtypes
        return new AttributePredicates2(function, Predicates2.notIn());
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.impl.block.factory.Predicates2,capture\>' to 'org.eclipse.collections.impl.block.factory.Predicates2'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Predicates2.java`
#### Snippet
```java
    public static <T extends Comparable<T>> Predicates2<T, T> lessThan()
    {
        return (Predicates2<T, T>) LESS_THAN;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.impl.block.factory.Predicates2,capture\>' to 'org.eclipse.collections.api.block.predicate.Predicate2'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Predicates2.java`
#### Snippet
```java
            Function<? super T, ? extends P> function)
    {
        return new AttributePredicates2<>(function, (Predicate2<P, P>) GREATER_THAN_OR_EQUAL);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.impl.block.factory.Predicates2,capture\>' to 'org.eclipse.collections.impl.block.factory.Predicates2'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Predicates2.java`
#### Snippet
```java
    public static <T extends Comparable<T>> Predicates2<T, T> greaterThan()
    {
        return (Predicates2<T, T>) GREATER_THAN;
    }

```

### UNCHECKED_WARNING
Unchecked assignment: 'org.eclipse.collections.impl.block.factory.Predicates2.AttributePredicates2' to 'org.eclipse.collections.impl.block.factory.Predicates2\>'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Predicates2.java`
#### Snippet
```java
    {
        //noinspection rawtypes
        return new AttributePredicates2(function, Predicates2.in());
    }

```

### UNCHECKED_WARNING
Unchecked call to 'AttributePredicates2(Function, Predicate2)' as a member of raw type 'org.eclipse.collections.impl.block.factory.Predicates2.AttributePredicates2'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Predicates2.java`
#### Snippet
```java
    {
        //noinspection rawtypes
        return new AttributePredicates2(function, Predicates2.in());
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bimap/mutable/AbstractMutableBiMap.java`
#### Snippet
```java
            if (result.length < size)
            {
                result = (T[]) Array.newInstance(result.getClass().getComponentType(), size);
            }
            this.copyEntries(result);
```

### UNCHECKED_WARNING
Unchecked cast: 'capture' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bimap/mutable/AbstractMutableBiMap.java`
#### Snippet
```java
            }
            Map.Entry<?, ?> entry = (Map.Entry<?, ?>) e;
            K key = (K) entry.getKey();
            V value = (V) entry.getValue();

```

### UNCHECKED_WARNING
Unchecked cast: 'capture' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bimap/mutable/AbstractMutableBiMap.java`
#### Snippet
```java
            Map.Entry<?, ?> entry = (Map.Entry<?, ?>) e;
            K key = (K) entry.getKey();
            V value = (V) entry.getValue();

            V actualValue = AbstractMutableBiMap.this.delegate.get(key);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Map.Entry'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bimap/mutable/AbstractMutableBiMap.java`
#### Snippet
```java
                return false;
            }
            Map.Entry<K, V> entry = (Map.Entry<K, V>) o;
            K key = entry.getKey();
            V actualValue = AbstractMutableBiMap.this.get(key);
```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/HashingStrategies.java`
#### Snippet
```java
    public static <T, V1, V2> HashingStrategy<T> fromFunctions(Function<? super T, ? extends V1> one, Function<? super T, ? extends V2> two)
    {
        return HashingStrategies.chain(
                HashingStrategies.fromFunction(one),
                HashingStrategies.fromFunction(two));
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.block.HashingStrategy' to 'org.eclipse.collections.api.block.HashingStrategy'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/HashingStrategies.java`
#### Snippet
```java
    public static <T> HashingStrategy<T> defaultStrategy()
    {
        return (HashingStrategy<T>) DEFAULT_HASHING_STRATEGY;
    }

```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/HashingStrategies.java`
#### Snippet
```java
    public static <T, V1, V2, V3> HashingStrategy<T> fromFunctions(Function<? super T, ? extends V1> one, Function<? super T, ? extends V2> two, Function<? super T, ? extends V3> three)
    {
        return HashingStrategies.chain(
                HashingStrategies.fromFunction(one),
                HashingStrategies.fromFunction(two),
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.block.function.Function,capture\>' to 'org.eclipse.collections.api.block.function.Function,V2\>'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Functions.java`
#### Snippet
```java
    public static <V2> Function<Pair<?, V2>, V2> secondOfPair()
    {
        return (Function<Pair<?, V2>, V2>) (Function<?, ?>) SECOND_OF_PAIR_FUNCTION;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.block.function.Function,capture\>' to 'org.eclipse.collections.api.block.function.Function,V1\>'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Functions.java`
#### Snippet
```java
    public static <V1> Function<Pair<V1, ?>, V1> firstOfPair()
    {
        return (Function<Pair<V1, ?>, V1>) (Function<?, ?>) FIRST_OF_PAIR_FUNCTION;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.block.function.Function,capture\>' to 'org.eclipse.collections.api.block.function.Function'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Functions.java`
#### Snippet
```java
    public static <T> Function<T, T> identity()
    {
        return (Function<T, T>) PASS_THRU_FUNCTION;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.block.function.Function,capture\>' to 'org.eclipse.collections.api.block.function.Function'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Functions.java`
#### Snippet
```java
    public static Function<String, String> getStringPassThru()
    {
        return (Function<String, String>) PASS_THRU_FUNCTION;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.block.function.Function,capture\>' to 'org.eclipse.collections.api.block.function.Function,org.eclipse.collections.api.tuple.Pair\>'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Functions.java`
#### Snippet
```java
    public static <S, T> Function<Pair<S, T>, Pair<T, S>> swappedPair()
    {
        return (Function<Pair<S, T>, Pair<T, S>>) (Function<?, ?>) SWAPPED_PAIR_FUNCTION;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.block.function.Function,capture\>' to 'org.eclipse.collections.api.block.function.Function'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Functions.java`
#### Snippet
```java
    public static Function<Number, Number> getNumberPassThru()
    {
        return (Function<Number, Number>) PASS_THRU_FUNCTION;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.block.function.Function,capture\>' to 'org.eclipse.collections.api.block.function.Function,V\>'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Functions.java`
#### Snippet
```java
    public static <V> Function<Map.Entry<?, V>, V> getValueFunction()
    {
        return (Function<Map.Entry<?, V>, V>) MAP_VALUE_FUNCTION;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.block.function.Function,capture\>' to 'org.eclipse.collections.api.block.function.Function,K\>'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Functions.java`
#### Snippet
```java
    public static <K> Function<Map.Entry<K, ?>, K> getKeyFunction()
    {
        return (Function<Map.Entry<K, ?>, K>) MAP_KEY_FUNCTION;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/procedure/SelectInstancesOfProcedure.java`
#### Snippet
```java
        if (this.clazz.isInstance(object))
        {
            this.collection.add((T) object);
        }
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.block.SerializableComparator\>' to 'org.eclipse.collections.api.block.SerializableComparator\>'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Comparators.java`
#### Snippet
```java
    public static <T> SerializableComparator<SortedSetIterable<T>> powerSet()
    {
        return (SerializableComparator<SortedSetIterable<T>>) POWER_SET_COMPARATOR;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.block.SerializableComparator\>' to 'org.eclipse.collections.api.block.SerializableComparator'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Comparators.java`
#### Snippet
```java
    public static <T> SerializableComparator<T> comparableComparator()
    {
        return (SerializableComparator<T>) COMPARABLE_COMPARATOR;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.block.SerializableComparator\>' to 'org.eclipse.collections.api.block.SerializableComparator'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Comparators.java`
#### Snippet
```java
    public static <T> SerializableComparator<T> originalNaturalOrder()
    {
        return (SerializableComparator<T>) NATURAL_ORDER_COMPARATOR;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'T' to 'java.lang.Comparable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Comparators.java`
#### Snippet
```java
        public int compare(T one, T two)
        {
            return ((Comparable<T>) one).compareTo(two);
        }
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.block.SerializableComparator\>' to 'org.eclipse.collections.api.block.SerializableComparator'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Comparators.java`
#### Snippet
```java
    public static <T> SerializableComparator<T> originalReverseNaturalOrder()
    {
        return (SerializableComparator<T>) REVERSE_NATURAL_ORDER_COMPARATOR;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'T' to 'java.lang.Comparable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/procedure/MinProcedure.java`
#### Snippet
```java
        if (this.visitedAtLeastOnce)
        {
            if (((Comparable<T>) each).compareTo(this.result) < 0)
            {
                this.result = each;
```

### UNCHECKED_WARNING
Unchecked cast: 'T' to 'java.lang.Comparable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/procedure/MaxProcedure.java`
#### Snippet
```java
        if (this.visitedAtLeastOnce)
        {
            if (((Comparable<T>) each).compareTo(this.result) > 0)
            {
                this.result = each;
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.block.function.Function' to 'org.eclipse.collections.api.block.function.Function'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/procedure/MapCollectProcedure.java`
#### Snippet
```java
    public MapCollectProcedure(Map<K, V> newMap, Function<? super T, ? extends K> newKeyFunction)
    {
        this(newMap, newKeyFunction, (Function<T, V>) Functions.identity());
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/stack/immutable/ImmutableStackSerializationProxy.java`
#### Snippet
```java
        for (int i = 0; i < size; i++)
        {
            deserializedDelegate.add((T) in.readObject());
        }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.stack.ImmutableStack\>' to 'org.eclipse.collections.api.stack.ImmutableStack'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/stack/immutable/ImmutableStackFactoryImpl.java`
#### Snippet
```java
    public <T> ImmutableStack<T> empty()
    {
        return (ImmutableStack<T>) ImmutableEmptyStack.INSTANCE;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/stack/immutable/ImmutableStackFactoryImpl.java`
#### Snippet
```java
    public <T> ImmutableStack<T> withAllReversed(Iterable<? extends T> items)
    {
        return this.withReversed((T[]) Iterate.toArray(items));
    }
}
```

### UNCHECKED_WARNING
Unchecked cast: 'T1' to 'java.lang.Comparable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/tuple/PairImpl.java`
#### Snippet
```java
    public int compareTo(Pair<T1, T2> other)
    {
        int i = ((Comparable<T1>) this.one).compareTo(other.getOne());
        if (i != 0)
        {
```

### UNCHECKED_WARNING
Unchecked cast: 'T2' to 'java.lang.Comparable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/tuple/PairImpl.java`
#### Snippet
```java
            return i;
        }
        return ((Comparable<T2>) this.two).compareTo(other.getTwo());
    }
}
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.block.function.Function,capture\>' to 'org.eclipse.collections.api.block.function.Function,org.eclipse.collections.api.tuple.Pair\>'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/tuple/AbstractImmutableEntry.java`
#### Snippet
```java
    public static <K, V> Function<Map.Entry<K, V>, Pair<K, V>> getPairFunction()
    {
        return (Function<Map.Entry<K, V>, Pair<K, V>>) (Function<?, ?>) TO_PAIR;
    }

```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/tuple/Tuples.java`
#### Snippet
```java
    public static <T> MutableList<T> pairToList(Pair<T, T> pair)
    {
        return Lists.mutable.with(pair.getOne(), pair.getTwo());
    }

```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/tuple/Tuples.java`
#### Snippet
```java
    public static <T> MutableList<T> tripleToList(Triple<T, T, T> triple)
    {
        return Lists.mutable.with(triple.getOne(), triple.getTwo(), triple.getThree());
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'T1' to 'java.lang.Comparable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/tuple/TripleImpl.java`
#### Snippet
```java
    public int compareTo(Triple<T1, T2, T3> other)
    {
        int i = ((Comparable<T1>) this.one).compareTo(other.getOne());
        if (i != 0)
        {
```

### UNCHECKED_WARNING
Unchecked cast: 'T2' to 'java.lang.Comparable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/tuple/TripleImpl.java`
#### Snippet
```java
            return i;
        }
        int j = ((Comparable<T2>) this.two).compareTo(other.getTwo());
        if (j != 0)
        {
```

### UNCHECKED_WARNING
Unchecked cast: 'T3' to 'java.lang.Comparable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/tuple/TripleImpl.java`
#### Snippet
```java
            return j;
        }
        return ((Comparable<T3>) this.three).compareTo(other.getThree());
    }
}
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.partition.list.PartitionImmutableList\>' to 'org.eclipse.collections.api.partition.stack.PartitionImmutableStack'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/stack/immutable/ImmutableEmptyStack.java`
#### Snippet
```java
    public <P> PartitionImmutableStack<T> partitionWith(Predicate2<? super T, ? super P> predicate, P parameter)
    {
        return (PartitionImmutableStack<T>) EMPTY;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.partition.list.PartitionImmutableList\>' to 'org.eclipse.collections.api.partition.stack.PartitionImmutableStack'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/stack/immutable/ImmutableEmptyStack.java`
#### Snippet
```java
    public PartitionImmutableStack<T> partition(Predicate<? super T> predicate)
    {
        return (PartitionImmutableStack<T>) EMPTY;
    }

```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/factory/Sets.java`
#### Snippet
```java
            Set<? extends E> setB)
    {
        return intersectInto(newSet(setA, setB), setA, setB);
    }

```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/factory/Sets.java`
#### Snippet
```java
        return unionInto(
                targetSet,
                differenceInto(newSet(setA, setB), setA, setB),
                differenceInto(newSet(setA, setB), setB, setA));
    }
```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/factory/Sets.java`
#### Snippet
```java
                targetSet,
                differenceInto(newSet(setA, setB), setA, setB),
                differenceInto(newSet(setA, setB), setB, setA));
    }

```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/factory/Sets.java`
#### Snippet
```java
            Set<? extends E> setB)
    {
        return setA.size() < setB.size() ? fillSet(targetSet, Sets.retainAllProcedure(), setA, setB)
                : fillSet(targetSet, Sets.retainAllProcedure(), setB, setA);
    }
```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/factory/Sets.java`
#### Snippet
```java
    {
        return setA.size() < setB.size() ? fillSet(targetSet, Sets.retainAllProcedure(), setA, setB)
                : fillSet(targetSet, Sets.retainAllProcedure(), setB, setA);
    }

```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/factory/Sets.java`
#### Snippet
```java
    public static <T> MutableSet<MutableSet<T>> powerSet(Set<T> set)
    {
        MutableSet<MutableSet<T>> seed = UnifiedSet.newSetWith(UnifiedSet.newSet());
        return Iterate.injectInto(seed, set, (accumulator, element) -> Sets.union(accumulator, accumulator.collect(innerSet -> innerSet.toSet().with(element))));
    }
```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/factory/Sets.java`
#### Snippet
```java
            Set<? extends E> setB)
    {
        return setA.size() > setB.size() ? fillSet(targetSet, Sets.addAllProcedure(), setA, setB)
                : fillSet(targetSet, Sets.addAllProcedure(), setB, setA);
    }
```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/factory/Sets.java`
#### Snippet
```java
    {
        return setA.size() > setB.size() ? fillSet(targetSet, Sets.addAllProcedure(), setA, setB)
                : fillSet(targetSet, Sets.addAllProcedure(), setB, setA);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Set\>' to 'java.util.SortedSet'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/factory/Sets.java`
#### Snippet
```java
            return null;
        }
        SortedSet<E> sortedSetWithComparator = (SortedSet<E>) Iterate.detect(sortedSetCollection, HAS_NON_NULL_COMPARATOR);
        if (sortedSetWithComparator != null)
        {
```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/factory/Sets.java`
#### Snippet
```java
            Set<? extends E> subtrahendSet)
    {
        return fillSet(targetSet, Sets.removeAllProcedure(), minuendSet, subtrahendSet);
    }

```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/factory/Sets.java`
#### Snippet
```java
            Set<? extends E> setB)
    {
        return symmetricDifferenceInto(newSet(setA, setB), setA, setB);
    }

```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/factory/Sets.java`
#### Snippet
```java
            Set<? extends E> setB)
    {
        return unionInto(newSet(setA, setB), setA, setB);
    }

```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/factory/Sets.java`
#### Snippet
```java
            Set<? extends E> subtrahendSet)
    {
        return differenceInto(newSet(minuendSet, subtrahendSet), minuendSet, subtrahendSet);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/stack/mutable/ArrayStack.java`
#### Snippet
```java
    {
        int size = in.readInt();
        T[] array = (T[]) new Object[size];
        for (int i = size - 1; i >= 0; i--)
        {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/stack/mutable/ArrayStack.java`
#### Snippet
```java
        for (int i = size - 1; i >= 0; i--)
        {
            array[i] = (T) in.readObject();
        }
        this.delegate = FastList.newListWith(array);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Iterable\>' to 'java.lang.Iterable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/stack/mutable/ArrayStack.java`
#### Snippet
```java
    public static <T> ArrayStack<T> newStack(Iterable<? extends T> items)
    {
        return new ArrayStack<>((Iterable<T>) items);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.ordered.OrderedIterable' to 'java.util.List'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/OrderedIterate.java`
#### Snippet
```java
        if (o1 instanceof RandomAccess)
        {
            return RandomAccessListIterate.corresponds((List<T>) o1, o2, predicate);
        }
        if (o2 instanceof RandomAccess)
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.ordered.OrderedIterable' to 'java.util.List'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/OrderedIterate.java`
#### Snippet
```java
        if (o2 instanceof RandomAccess)
        {
            List<S> otherList = (List<S>) o2;
            Iterator<T> iterator = o1.iterator();
            for (int index = 0; index < otherList.size(); index++)
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/stack/immutable/ImmutableArrayStack.java`
#### Snippet
```java
            for (int i = 0; i < size; i++)
            {
                deserializedDelegate.add((T) in.readObject());
            }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'E\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/stack/immutable/ImmutableArrayStack.java`
#### Snippet
```java
    public static <E> ImmutableArrayStack<E> newStack(Iterable<? extends E> iterable)
    {
        return new ImmutableArrayStack<>((E[]) Iterate.toArray(iterable));
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.LazyIterable\>' to 'org.eclipse.collections.api.LazyIterable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/LazyIterate.java`
#### Snippet
```java
    public static <T> LazyIterable<T> empty()
    {
        return (LazyIterable<T>) EMPTY_ITERABLE;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.multimap.set.ImmutableSetMultimap' to 'org.eclipse.collections.api.multimap.set.ImmutableSetMultimap'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/factory/Multimaps.java`
#### Snippet
```java
            public <K, V> ImmutableSetMultimap<K, V> empty()
            {
                return (ImmutableSetMultimap<K, V>) EMPTY;
            }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.multimap.list.ImmutableListMultimap' to 'org.eclipse.collections.api.multimap.list.ImmutableListMultimap'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/factory/Multimaps.java`
#### Snippet
```java
            public <K, V> ImmutableListMultimap<K, V> empty()
            {
                return (ImmutableListMultimap<K, V>) EMPTY;
            }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.multimap.bag.ImmutableBagMultimap' to 'org.eclipse.collections.api.multimap.bag.ImmutableBagMultimap'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/factory/Multimaps.java`
#### Snippet
```java
            public <K, V> ImmutableBagMultimap<K, V> empty()
            {
                return (ImmutableBagMultimap<K, V>) EMPTY;
            }

```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/factory/Multimaps.java`
#### Snippet
```java
            public <K, V> ImmutableSortedSetMultimap<K, V> with(Comparator<? super V> comparator, K key, V value)
            {
                return new ImmutableSortedSetMultimapImpl<>(Maps.immutable.with(key, SortedSets.immutable.with(comparator, value)), comparator);
            }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Collection\>' to 'org.eclipse.collections.api.collection.MutableCollection'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/DefaultSpeciesNewStrategy.java`
#### Snippet
```java
        if (collection instanceof MutableCollection)
        {
            return ((MutableCollection<T>) collection).newEmpty();
        }
        if (collection instanceof Proxy)
```

### UNCHECKED_WARNING
Unchecked cast: 'capture' to 'java.util.Collection'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/DefaultSpeciesNewStrategy.java`
#### Snippet
```java
        if (ReflectionHelper.hasDefaultConstructor(collection.getClass()))
        {
            return (Collection<T>) ReflectionHelper.newInstance(collection.getClass());
        }
        return DefaultSpeciesNewStrategy.createNewInstanceForCollectionType(collection);
```

### UNCHECKED_WARNING
Unchecked cast: 'capture' to 'java.util.Collection'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/DefaultSpeciesNewStrategy.java`
#### Snippet
```java
        if (constructor != null)
        {
            return (Collection<T>) ReflectionHelper.newInstance(constructor, size);
        }
        return this.speciesNew(collection);
```

### UNCHECKED_WARNING
Unchecked cast: 'T' to 'java.lang.Comparable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/SortedSetIterables.java`
#### Snippet
```java
            if (itrB.hasNext())
            {
                int val = ((Comparable<T>) itrA.next()).compareTo(itrB.next());
                if (val != 0)
                {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Map' to 'org.eclipse.collections.api.map.ImmutableMap'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/MapIterate.java`
#### Snippet
```java
            if (map instanceof ImmutableMap)
            {
                entries = ((ImmutableMap<K, V>) map).keyValuesView();
            }
            else
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Map' to 'org.eclipse.collections.api.map.MapIterable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/MapIterate.java`
#### Snippet
```java
            if (map instanceof UnsortedMapIterable)
            {
                ((MapIterable<K, V>) map).forEachKeyValue(procedure);
            }
            else
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Map' to 'org.eclipse.collections.api.map.MapIterable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/MapIterate.java`
#### Snippet
```java
            if (map instanceof UnsortedMapIterable)
            {
                ((MapIterable<K, V>) map).forEachValue(procedure);
            }
            else
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Map' to 'org.eclipse.collections.api.map.MapIterable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/MapIterate.java`
#### Snippet
```java
            if (map instanceof UnsortedMapIterable)
            {
                ((MapIterable<K, V>) map).forEachKey(procedure);
            }
            else
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Map' to 'org.eclipse.collections.api.map.MapIterable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/MapIterate.java`
#### Snippet
```java
        if (map instanceof UnsortedMapIterable)
        {
            return ((MapIterable<K, V>) map).ifPresentApply(key, function);
        }
        V result = map.get(key);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Map' to 'org.eclipse.collections.api.map.MapIterable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/MapIterate.java`
#### Snippet
```java
        if (map instanceof UnsortedMapIterable)
        {
            return ((MapIterable<K, V>) map).getIfAbsent(key, instanceBlock);
        }
        V result = map.get(key);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Map' to 'org.eclipse.collections.api.map.MapIterable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/MapIterate.java`
#### Snippet
```java
        if (map instanceof UnsortedMapIterable)
        {
            return ((MapIterable<K, V>) map).getIfAbsentWith(key, function, parameter);
        }
        V result = map.get(key);
```

### UNCHECKED_WARNING
Unchecked cast: 'T' to 'java.lang.Comparable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/SortedBagIterables.java`
#### Snippet
```java
            if (itrB.hasNext())
            {
                int val = ((Comparable<T>) itrA.next()).compareTo(itrB.next());
                if (val != 0)
                {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayListIterate.java`
#### Snippet
```java
        try
        {
            return (T[]) ArrayListIterate.ELEMENT_DATA_FIELD.get(list);
        }
        catch (IllegalAccessException e)
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayListIterate.java`
#### Snippet
```java
                if (clazz.isInstance(element))
                {
                    result.add((T) element);
                }
            }
```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/ReflectionHelper.java`
#### Snippet
```java
     * Mapping of iterator wrapper classes to iterator types
     */
    private static final ImmutableMap<Class<?>, Class<?>> WRAPPER_TO_PRIMATIVES = UnifiedMap.<Class<?>, Class<?>>newMapWith(
            Tuples.twin(Short.class, short.class),
            Tuples.twin(Boolean.class, boolean.class),
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.reflect.Constructor\>' to 'java.lang.reflect.Constructor'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/ReflectionHelper.java`
#### Snippet
```java
            if (ReflectionHelper.parameterTypesMatch(candidate.getParameterTypes(), constructorParameterTypes))
            {
                return (Constructor<T>) candidate;
            }
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.api.multimap.MutableMultimap'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/multimap/SynchronizedMultimapSerializationProxy.java`
#### Snippet
```java
    public void readExternal(ObjectInput in) throws IOException, ClassNotFoundException
    {
        this.multimap = (MutableMultimap<K, V>) in.readObject();
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.collection.MutableCollection' to 'C'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/multimap/AbstractSynchronizedPutMultimap.java`
#### Snippet
```java
            }

            return (C) existingValues.asUnmodifiable();
        }
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'T' to 'java.lang.Comparable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/RandomAccessListIterate.java`
#### Snippet
```java
        {
            T item = list.get(i);
            if (((Comparable<? super T>) item).compareTo(min) < 0)
            {
                min = item;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/RandomAccessListIterate.java`
#### Snippet
```java
            if (clazz.isInstance(item))
            {
                result.add((T) item);
            }
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'T' to 'java.lang.Comparable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/RandomAccessListIterate.java`
#### Snippet
```java
        {
            T item = list.get(i);
            if (((Comparable<T>) item).compareTo(max) > 0)
            {
                max = item;
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.ordered.OrderedIterable

' to 'java.util.List

'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/RandomAccessListIterate.java`
#### Snippet
```java
        if (other instanceof RandomAccess)
        {
            List<P> otherList = (List<P>) other;
            for (int index = 0; index < size; index++)
            {
```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/SetIterables.java`
#### Snippet
```java
    public static <T> MutableSet<MutableSet<T>> powerSet(Set<T> set)
    {
        MutableSet<MutableSet<T>> seed = UnifiedSet.newSetWith(Sets.mutable.empty());
        return powerSetWithSeed(set, seed);
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.set.SetIterable\>' to 'org.eclipse.collections.api.set.SetIterable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/SetIterables.java`
#### Snippet
```java
            R targetSet)
    {
        SetIterable<E> one = (SetIterable<E>) minuendSet;
        return one.reject(subtrahendSet::contains, targetSet);
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.set.SetIterable\>' to 'org.eclipse.collections.api.set.SetIterable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/SetIterables.java`
#### Snippet
```java
            setB = tmp;
        }
        SetIterable<E> one = (SetIterable<E>) setA;
        return one.select(setB::contains, targetSet);
    }
```

### UNCHECKED_WARNING
Unchecked generics array creation for varargs parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/SetIterables.java`
#### Snippet
```java
    public static <T> MutableSet<MutableSet<T>> powerSet(UnifiedSetWithHashingStrategy<T> set)
    {
        MutableSet<MutableSet<T>> seed = UnifiedSet.newSetWith(set.newEmpty());
        return powerSetWithSeed(set, seed);
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.map.MutableMap,capture\>' to 'org.eclipse.collections.api.map.MutableMap\>'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/multimap/AbstractImmutableMultimap.java`
#### Snippet
```java
    public MutableMap<K, RichIterable<V>> toMap()
    {
        return (MutableMap<K, RichIterable<V>>) (MutableMap<?, ?>) this.map.toMap();
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.collection.MutableCollection' to 'C'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/multimap/AbstractMutableMultimap.java`
#### Snippet
```java
        collection = collection == null ? this.createCollection() : collection;
        this.subtractFromTotalSize(collection.size());
        return (C) collection.asUnmodifiable();
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/multimap/AbstractMutableMultimap.java`
#### Snippet
```java
        for (int k = 0; k < keyCount; k++)
        {
            K key = (K) in.readObject();
            int valuesSize = in.readInt();
            C values = this.createCollection();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/multimap/AbstractMutableMultimap.java`
#### Snippet
```java
            for (int v = 0; v < valuesSize; v++)
            {
                values.add((V) in.readObject());
            }
            this.addToTotalSize(valuesSize);
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.collection.MutableCollection' to 'C'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/multimap/AbstractMutableMultimap.java`
#### Snippet
```java
        oldValues = oldValues == null ? this.createCollection() : oldValues;
        this.addToTotalSize(newValues.size() - oldValues.size());
        return (C) oldValues.asUnmodifiable();
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.map.MutableMap,capture\>' to 'org.eclipse.collections.api.map.MutableMap\>'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/multimap/AbstractMutableMultimap.java`
#### Snippet
```java
    public MutableMap<K, RichIterable<V>> toMap()
    {
        MutableMap<K, RichIterable<V>> result = (MutableMap<K, RichIterable<V>>) (MutableMap<?, ?>) this.map.newEmpty();
        this.map.forEachKeyValue((key, collection) -> {
            MutableCollection<V> mutableCollection = collection.newEmpty();
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.collection.MutableCollection' to 'C'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/multimap/AbstractMutableMultimap.java`
#### Snippet
```java
    public C get(K key)
    {
        return (C) this.map.getIfAbsentWith(key, this.createCollectionBlock(), this).asUnmodifiable();
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.collection.MutableCollection' to 'C'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/multimap/AbstractMutableMultimap.java`
#### Snippet
```java
        }

        return (C) existingValues.asUnmodifiable();
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.map.MutableMap' to 'org.eclipse.collections.api.map.MutableMap'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/multimap/AbstractMutableMultimap.java`
#### Snippet
```java
    public <R extends Collection<V>> MutableMap<K, R> toMap(Function0<R> collectionFactory)
    {
        MutableMap<K, R> result = (MutableMap<K, R>) this.createMapWithKeyCount(this.map.size());
        this.map.forEachKeyValue((key, collection) -> {
            R mutableCollection = collectionFactory.value();
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.set.SetIterable' to 'java.util.Set'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/multimap/AbstractSynchronizedMultimap.java`
#### Snippet
```java
        synchronized (this.lock)
        {
            return SynchronizedMutableSet.of((Set<K>) this.getDelegate().keySet(), this.lock);
        }
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IteratorIterate.java`
#### Snippet
```java
            if (clazz.isInstance(item))
            {
                targetCollection.add((T) item);
            }
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/multimap/bag/ImmutableBagMultimapImpl.java`
#### Snippet
```java
            for (int i = 0; i < keyCount; i++)
            {
                K key = (K) in.readObject();
                int valuesSize = in.readInt();
                MutableBag<V> bag = Bags.mutable.empty();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/multimap/bag/ImmutableBagMultimapImpl.java`
#### Snippet
```java
                for (int j = 0; j < valuesSize; j++)
                {
                    V value = (V) in.readObject();
                    int count = in.readInt();

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/multimap/bag/AbstractMutableBagMultimap.java`
#### Snippet
```java
        for (int i = 0; i < keyCount; i++)
        {
            K key = (K) in.readObject();
            int valuesSize = in.readInt();
            MutableBag<V> bag = this.createCollection();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/multimap/bag/AbstractMutableBagMultimap.java`
#### Snippet
```java
            for (int j = 0; j < valuesSize; j++)
            {
                V value = (V) in.readObject();
                int count = in.readInt();

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.map.MutableMap,capture\>' to 'org.eclipse.collections.api.map.MutableMap\>'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/multimap/bag/AbstractMutableBagMultimap.java`
#### Snippet
```java
    public ImmutableBagMultimap<K, V> toImmutable()
    {
        MutableMap<K, ImmutableBag<V>> result = (MutableMap<K, ImmutableBag<V>>) (MutableMap<?, ?>) this.createMapWithKeyCount(this.map.size());

        this.map.forEachKeyValue((key, bag) -> result.put(key, bag.toImmutable()));
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Comparator'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/multimap/bag/TreeBagMultimap.java`
#### Snippet
```java
    public void readExternal(ObjectInput in) throws IOException, ClassNotFoundException
    {
        this.comparator = (Comparator<? super V>) in.readObject();
        super.readExternal(in);
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.multimap.Multimap,capture\>' to 'org.eclipse.collections.api.multimap.sortedbag.SortedBagMultimap'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/multimap/bag/TreeBagMultimap.java`
#### Snippet
```java
        super(Math.max(multimap.keysView().size() * 2, 16));
        this.comparator = multimap instanceof SortedBagMultimap<?, ?>
                ? ((SortedBagMultimap<K, V>) multimap).comparator()
                : null;
        this.putAll(multimap);
```

### UNCHECKED_WARNING
Unchecked cast: 'T' to 'java.lang.Comparable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/InternalArrayIterate.java`
#### Snippet
```java
        {
            T item = array[i];
            if (((Comparable<T>) item).compareTo(max) > 0)
            {
                max = item;
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.ordered.OrderedIterable

' to 'java.util.List

'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/InternalArrayIterate.java`
#### Snippet
```java
        if (other instanceof RandomAccess)
        {
            List<P> otherList = (List<P>) other;
            for (int index = 0; index < size; index++)
            {
```

### UNCHECKED_WARNING
Unchecked cast: 'T' to 'java.lang.Comparable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/InternalArrayIterate.java`
#### Snippet
```java
        {
            T item = array[i];
            if (((Comparable<? super T>) item).compareTo(min) < 0)
            {
                min = item;
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.block.procedure.Procedure\>' to 'org.eclipse.collections.impl.block.procedure.FastListSelectProcedure'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/InternalArrayIterate.java`
#### Snippet
```java
        if (procedure instanceof FastListSelectProcedure)
        {
            InternalArrayIterate.batchFastListSelect(array, start, end, (FastListSelectProcedure<T>) procedure);
        }
        else if (procedure instanceof FastListCollectProcedure)
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Comparator'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/multimap/bag/sorted/TreeBagMultimap.java`
#### Snippet
```java
    public void readExternal(ObjectInput in) throws IOException, ClassNotFoundException
    {
        this.comparator = (Comparator<? super V>) in.readObject();
        super.readExternal(in);
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.multimap.Multimap,capture\>' to 'org.eclipse.collections.api.multimap.sortedbag.SortedBagMultimap'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/multimap/bag/sorted/TreeBagMultimap.java`
#### Snippet
```java
        super(Math.max(multimap.keysView().size() * 2, 16));
        this.comparator = multimap instanceof SortedBagMultimap<?, ?>
                ? ((SortedBagMultimap<K, V>) multimap).comparator()
                : null;
        this.putAll(multimap);
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.block.procedure.Procedure\>' to 'org.eclipse.collections.impl.block.procedure.FastListCollectProcedure'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/InternalArrayIterate.java`
#### Snippet
```java
        else if (procedure instanceof FastListCollectProcedure)
        {
            InternalArrayIterate.batchFastListCollect(array, start, end, (FastListCollectProcedure<T, ?>) procedure);
        }
        else if (procedure instanceof FastListCollectIfProcedure)
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.block.procedure.Procedure\>' to 'org.eclipse.collections.impl.block.procedure.FastListCollectIfProcedure'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/InternalArrayIterate.java`
#### Snippet
```java
        else if (procedure instanceof FastListCollectIfProcedure)
        {
            InternalArrayIterate.batchFastListCollectIf(array, start, end, (FastListCollectIfProcedure<T, ?>) procedure);
        }
        else if (procedure instanceof CountProcedure)
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.block.procedure.Procedure\>' to 'org.eclipse.collections.impl.block.procedure.CountProcedure'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/InternalArrayIterate.java`
#### Snippet
```java
        else if (procedure instanceof CountProcedure)
        {
            InternalArrayIterate.batchCount(array, start, end, (CountProcedure<T>) procedure);
        }
        else if (procedure instanceof FastListRejectProcedure)
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.block.procedure.Procedure\>' to 'org.eclipse.collections.impl.block.procedure.FastListRejectProcedure'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/InternalArrayIterate.java`
#### Snippet
```java
        else if (procedure instanceof FastListRejectProcedure)
        {
            InternalArrayIterate.batchReject(array, start, end, (FastListRejectProcedure<T>) procedure);
        }
        else if (procedure instanceof MultimapPutProcedure)
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.block.procedure.Procedure\>' to 'org.eclipse.collections.impl.block.procedure.MultimapPutProcedure'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/InternalArrayIterate.java`
#### Snippet
```java
        else if (procedure instanceof MultimapPutProcedure)
        {
            InternalArrayIterate.batchGroupBy(array, start, end, (MultimapPutProcedure<?, T>) procedure);
        }
        else
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/InternalArrayIterate.java`
#### Snippet
```java
            if (clazz.isInstance(each))
            {
                results.add((T) each);
            }
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.api.block.HashingStrategy'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/multimap/bag/strategy/HashBagMultimapWithHashingStrategy.java`
#### Snippet
```java
    public void readExternal(ObjectInput in) throws IOException, ClassNotFoundException
    {
        this.hashingStrategy = (HashingStrategy<? super K>) in.readObject();
        super.readExternal(in);
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Comparator'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/multimap/bag/sorted/mutable/TreeBagMultimap.java`
#### Snippet
```java
    public void readExternal(ObjectInput in) throws IOException, ClassNotFoundException
    {
        this.comparator = (Comparator<? super V>) in.readObject();
        super.readExternal(in);
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.multimap.Multimap,capture\>' to 'org.eclipse.collections.api.multimap.sortedbag.SortedBagMultimap'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/multimap/bag/sorted/mutable/TreeBagMultimap.java`
#### Snippet
```java
        super(Math.max(multimap.keysView().size() * 2, 16));
        this.comparator = multimap instanceof SortedBagMultimap<?, ?>
                ? ((SortedBagMultimap<K, V>) multimap).comparator()
                : null;
        this.putAll(multimap);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Comparator'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/multimap/bag/sorted/immutable/ImmutableSortedBagMultimapImpl.java`
#### Snippet
```java
        public void readExternal(ObjectInput in) throws IOException, ClassNotFoundException
        {
            this.comparator = (Comparator<? super V>) in.readObject();
            super.readExternal(in);
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.multimap.Multimap,capture\>' to 'org.eclipse.collections.api.multimap.sortedset.SortedSetMultimap'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/multimap/set/sorted/TreeSortedSetMultimap.java`
#### Snippet
```java
        super(Math.max(multimap.sizeDistinct() * 2, 16));
        this.comparator = multimap instanceof SortedSetMultimap<?, ?>
                ? ((SortedSetMultimap<K, V>) multimap).comparator()
                : null;
        this.putAll(multimap);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Comparator'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/multimap/set/sorted/TreeSortedSetMultimap.java`
#### Snippet
```java
    public void readExternal(ObjectInput in) throws IOException, ClassNotFoundException
    {
        this.comparator = (Comparator<? super V>) in.readObject();
        super.readExternal(in);
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Comparator'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/multimap/set/sorted/SynchronizedPutTreeSortedSetMultimap.java`
#### Snippet
```java
    public void readExternal(ObjectInput in) throws IOException, ClassNotFoundException
    {
        this.comparator = (Comparator<? super V>) in.readObject();
        super.readExternal(in);
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.multimap.Multimap,capture\>' to 'org.eclipse.collections.api.multimap.sortedset.SortedSetMultimap'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/multimap/set/sorted/SynchronizedPutTreeSortedSetMultimap.java`
#### Snippet
```java
    {
        this.comparator = multimap instanceof SortedSetMultimap<?, ?>
                ? ((SortedSetMultimap<K, V>) multimap).comparator()
                : null;
        this.putAll(multimap);
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.api.block.HashingStrategy'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/multimap/set/strategy/UnifiedSetWithHashingStrategyMultimap.java`
#### Snippet
```java
    public void readExternal(ObjectInput in) throws IOException, ClassNotFoundException
    {
        this.hashingStrategy = (HashingStrategy<? super V>) in.readObject();
        super.readExternal(in);
    }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'java.util.Comparator'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/multimap/set/sorted/ImmutableSortedSetMultimapImpl.java`
#### Snippet
```java
        public void readExternal(ObjectInput in) throws IOException, ClassNotFoundException
        {
            this.comparator = (Comparator<? super V>) in.readObject();
            super.readExternal(in);
        }
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Iterable\>' to 'java.util.Collection'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/Iterate.java`
#### Snippet
```java
        if (iterable instanceof Collection)
        {
            return ((Collection<T>) iterable).toArray(target);
        }
        if (iterable instanceof RichIterable)
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Iterable\>' to 'org.eclipse.collections.api.RichIterable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/Iterate.java`
#### Snippet
```java
        if (iterable instanceof RichIterable)
        {
            return ((RichIterable<T>) iterable).toArray(target);
        }
        MutableList<T> result = Lists.mutable.empty();
```

### UNCHECKED_WARNING
Unchecked cast: 'L' to 'org.eclipse.collections.api.list.MutableList'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/Iterate.java`
#### Snippet
```java
        if (list instanceof MutableList<?>)
        {
            ((MutableList<T>) list).sortThis();
        }
        else if (list instanceof ArrayList)
```

### UNCHECKED_WARNING
Unchecked cast: 'L' to 'java.util.ArrayList'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/Iterate.java`
#### Snippet
```java
        else if (list instanceof ArrayList)
        {
            ArrayListIterate.sortThis((ArrayList<T>) list);
        }
        else
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Iterable' to 'org.eclipse.collections.impl.parallel.BatchIterable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/Iterate.java`
#### Snippet
```java
        else if (iterable instanceof BatchIterable)
        {
            ((BatchIterable<T>) iterable).forEach(procedure);
        }
        else if (iterable instanceof ArrayList)
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Iterable\>' to 'java.util.Collection'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/Iterate.java`
#### Snippet
```java
        if (iterable instanceof Collection<?>)
        {
            return targetCollection.addAll((Collection<T>) iterable);
        }
        int oldSize = targetCollection.size();
```

### UNCHECKED_WARNING
Unchecked cast: 'L' to 'org.eclipse.collections.api.list.MutableList'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/Iterate.java`
#### Snippet
```java
        if (list instanceof MutableList)
        {
            ((MutableList<T>) list).sortThis(comparator);
        }
        else if (list instanceof ArrayList)
```

### UNCHECKED_WARNING
Unchecked cast: 'L' to 'java.util.ArrayList'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/Iterate.java`
#### Snippet
```java
        else if (list instanceof ArrayList)
        {
            ArrayListIterate.sortThis((ArrayList<T>) list, comparator);
        }
        else
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Iterable\>' to 'java.util.Collection'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/Iterate.java`
#### Snippet
```java
        if (iterable instanceof Collection<?>)
        {
            return targetCollection.removeAll((Collection<T>) iterable);
        }
        int oldSize = targetCollection.size();
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.List' to 'org.eclipse.collections.api.list.ListIterable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/parallel/ProcedureFJTask.java`
#### Snippet
```java
            if (this.list instanceof ListIterable)
            {
                ((ListIterable<T>) this.list).forEach(this.start, stop, this.procedure);
            }
            else if (this.list instanceof ArrayList)
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.eclipse.collections.impl.parallel.BatchIterableProcedureFJTask\[\]' to 'org.eclipse.collections.impl.parallel.BatchIterableProcedureFJTask\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/parallel/BatchIterableProcedureFJTaskRunner.java`
#### Snippet
```java
    private void createAndExecuteTasks(Executor executor, ProcedureFactory<BT> procedureFactory, BatchIterable<T> batchIterable)
    {
        this.procedures = new BatchIterableProcedureFJTask[this.taskCount];
        for (int batchNumber = 0; batchNumber < this.taskCount; batchNumber++)
        {
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.eclipse.collections.impl.parallel.ArrayProcedureFJTask\[\]' to 'org.eclipse.collections.impl.parallel.ArrayProcedureFJTask\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/parallel/ArrayProcedureFJTaskRunner.java`
#### Snippet
```java
    private void createAndExecuteTasks(Executor executor, ProcedureFactory<BT> procedureFactory, T[] array)
    {
        this.procedures = new ArrayProcedureFJTask[this.taskCount];
        int sectionSize = array.length / this.taskCount;
        for (int index = 0; index < this.taskCount; index++)
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Iterable\>' to 'org.eclipse.collections.api.set.sorted.SortedSetIterable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/parallel/AbstractPredicateBasedCombiner.java`
#### Snippet
```java
        if (sourceCollection instanceof SortedSetIterable)
        {
            return TreeSortedSet.newSet(((SortedSetIterable<T>) sourceCollection).comparator());
        }
        if (sourceCollection instanceof SetIterable)
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.eclipse.collections.impl.parallel.ProcedureFJTask\[\]' to 'org.eclipse.collections.impl.parallel.ProcedureFJTask\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/parallel/ProcedureFJTaskRunner.java`
#### Snippet
```java
    private void createAndExecuteTasks(Executor executor, ProcedureFactory<BT> procedureFactory, List<T> list)
    {
        this.procedures = new ProcedureFJTask[this.taskCount];
        int sectionSize = list.size() / this.taskCount;
        for (int index = 0; index < this.taskCount; index++)
```

### UNCHECKED_WARNING
Unchecked assignment: 'org.eclipse.collections.impl.parallel.ObjectIntProcedureFJTask\[\]' to 'org.eclipse.collections.impl.parallel.ObjectIntProcedureFJTask\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/parallel/ObjectIntProcedureFJTaskRunner.java`
#### Snippet
```java
    private void createAndExecuteTasks(Executor executor, ObjectIntProcedureFactory<BT> procedureFactory, List<T> list)
    {
        this.procedures = new ObjectIntProcedureFJTask[this.taskCount];

        int sectionSize = list.size() / this.taskCount;
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.api.collection.MutableCollection'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/UnmodifiableCollectionSerializationProxy.java`
#### Snippet
```java
    public void readExternal(ObjectInput in) throws IOException, ClassNotFoundException
    {
        this.mutableCollection = (MutableCollection<T>) in.readObject();
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'org.eclipse.collections.api.collection.MutableCollection'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/SynchronizedCollectionSerializationProxy.java`
#### Snippet
```java
    public void readExternal(ObjectInput in) throws IOException, ClassNotFoundException
    {
        this.mutableCollection = (MutableCollection<T>) in.readObject();
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Iterable\>' to 'java.util.List'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/immutable/AbstractImmutableCollection.java`
#### Snippet
```java
        else if (elements instanceof List)
        {
            List<T> toBeRemoved = (List<T>) elements;
            if (this.size() * toBeRemoved.size() > 10000)
            {
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Iterable\>' to 'java.util.List'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractMutableCollection.java`
#### Snippet
```java
        if (iterable instanceof List && iterable instanceof RandomAccess)
        {
            List<T> list = (List<T>) iterable;
            int size = list.size();
            for (int i = 0; i < size; i++)
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.multimap.Multimap,T\>' to 'org.eclipse.collections.api.multimap.MutableMultimap'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractSynchronizedMutableCollection.java`
#### Snippet
```java
    public <V> MutableMultimap<V, T> groupBy(Function<? super T, ? extends V> function)
    {
        return (MutableMultimap<V, T>) super.groupBy(function);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.RichIterable\>' to 'org.eclipse.collections.api.collection.MutableCollection'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractSynchronizedMutableCollection.java`
#### Snippet
```java
    public <V> MutableCollection<V> collectIf(Predicate<? super T> predicate, Function<? super T, ? extends V> function)
    {
        return (MutableCollection<V>) super.collectIf(predicate, function);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.bag.Bag\>' to 'org.eclipse.collections.api.bag.MutableBag'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractSynchronizedMutableCollection.java`
#### Snippet
```java
    public <V> MutableBag<V> countBy(Function<? super T, ? extends V> function)
    {
        return (MutableBag<V>) super.countBy(function);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.bag.Bag\>' to 'org.eclipse.collections.api.bag.MutableBag'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractSynchronizedMutableCollection.java`
#### Snippet
```java
    public <V, P> MutableBag<V> countByWith(Function2<? super T, ? super P, ? extends V> function, P parameter)
    {
        return (MutableBag<V>) super.countByWith(function, parameter);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.List' to 'org.eclipse.collections.api.list.ListIterable'
in `eclipse-collections-forkjoin/src/main/java/org/eclipse/collections/impl/forkjoin/FJListObjectIntProcedureTask.java`
#### Snippet
```java
            if (this.list instanceof ListIterable)
            {
                ((ListIterable<T>) this.list).forEachWithIndex(this.start, this.end, this.procedure);
            }
            else if (this.list instanceof ArrayList)
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.List' to 'org.eclipse.collections.api.list.ListIterable'
in `eclipse-collections-forkjoin/src/main/java/org/eclipse/collections/impl/forkjoin/FJListProcedureTask.java`
#### Snippet
```java
            if (this.list instanceof ListIterable)
            {
                ((ListIterable<T>) this.list).forEach(this.start, this.end, this.procedure);
            }
            else if (this.list instanceof ArrayList)
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Collection' to 'R'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/parallel/ParallelIterate.java`
#### Snippet
```java
                taskCount,
                executor);
        return (R) combiner.getResult();
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Iterable' to 'org.eclipse.collections.impl.parallel.BatchIterable'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/parallel/ParallelIterate.java`
#### Snippet
```java
            {
                ParallelIterate.forEachInBatchWithExecutor(
                        (BatchIterable<T>) iterable,
                        procedureFactory,
                        combiner,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/parallel/ParallelIterate.java`
#### Snippet
```java
            {
                ParallelArrayIterate.forEachOn(
                        (T[]) Iterate.toArray(iterable),
                        procedureFactory,
                        combiner,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Collection' to 'R'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/parallel/ParallelIterate.java`
#### Snippet
```java
                ParallelIterate.calculateTaskCount(iterable, batchSize),
                executor);
        return (R) combiner.getResult();
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Collection' to 'R'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/parallel/ParallelIterate.java`
#### Snippet
```java
                ParallelIterate.calculateTaskCount(iterable, batchSize),
                executor);
        return (R) combiner.getResult();
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Collection' to 'R'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/parallel/ParallelIterate.java`
#### Snippet
```java
                ParallelIterate.calculateTaskCount(iterable, batchSize),
                executor);
        return (R) combiner.getResult();
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Collection' to 'R'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/parallel/ParallelIterate.java`
#### Snippet
```java
                taskCount,
                executor);
        return (R) combiner.getResult();
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/parallel/ParallelIterate.java`
#### Snippet
```java
            {
                ParallelIterate.forEachWithIndexInListOnExecutor(
                        ArrayAdapter.adapt((T[]) Iterate.toArray(iterable)),
                        procedureFactory,
                        combiner,
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.collection.MutableCollection\>' to 'org.eclipse.collections.api.collection.MutableCollection'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractUnmodifiableMutableCollection.java`
#### Snippet
```java
    protected MutableCollection<T> getMutableCollection()
    {
        return (MutableCollection<T>) this.collection;
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object' to 'T'
in `eclipse-collections-testutils/src/main/java/org/eclipse/collections/impl/test/SerializeTestHelper.java`
#### Snippet
```java
    {
        byte[] pileOfBytes = serialize(sourceObject);
        return (T) deserialize(pileOfBytes);
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Collection' to 'R'
in `eclipse-collections-forkjoin/src/main/java/org/eclipse/collections/impl/forkjoin/FJIterate.java`
#### Snippet
```java
        int taskCount = FJIterate.calculateTaskCount(iterable, batchSize);
        FJIterate.forEach(iterable, procedureFactory, combiner, batchSize, taskCount, executor);
        return (R) combiner.getResult();
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Iterable' to 'org.eclipse.collections.impl.parallel.BatchIterable'
in `eclipse-collections-forkjoin/src/main/java/org/eclipse/collections/impl/forkjoin/FJIterate.java`
#### Snippet
```java
            {
                FJIterate.forEachInBatchWithExecutor(
                        (BatchIterable<T>) iterable,
                        procedureFactory,
                        combiner,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections-forkjoin/src/main/java/org/eclipse/collections/impl/forkjoin/FJIterate.java`
#### Snippet
```java
            {
                ParallelArrayIterate.forEachOn(
                        (T[]) Iterate.toArray(iterable),
                        procedureFactory,
                        combiner,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Collection' to 'R'
in `eclipse-collections-forkjoin/src/main/java/org/eclipse/collections/impl/forkjoin/FJIterate.java`
#### Snippet
```java
                FJIterate.calculateTaskCount(iterable, batchSize),
                executor);
        return (R) combiner.getResult();
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Collection' to 'R'
in `eclipse-collections-forkjoin/src/main/java/org/eclipse/collections/impl/forkjoin/FJIterate.java`
#### Snippet
```java
                taskCount,
                executor);
        return (R) combiner.getResult();
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.lang.Object\[\]' to 'T\[\]'
in `eclipse-collections-forkjoin/src/main/java/org/eclipse/collections/impl/forkjoin/FJIterate.java`
#### Snippet
```java
            {
                FJIterate.forEachWithIndexInListOnExecutor(
                        ArrayAdapter.adapt((T[]) Iterate.toArray(iterable)),
                        procedureFactory,
                        combiner,
```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Collection' to 'R'
in `eclipse-collections-forkjoin/src/main/java/org/eclipse/collections/impl/forkjoin/FJIterate.java`
#### Snippet
```java
        int taskCount = FJIterate.calculateTaskCount(size, batchSize);
        FJIterate.forEach(iterable, procedureFactory, combiner, batchSize, taskCount, executor);
        return (R) combiner.getResult();
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'java.util.Collection' to 'R'
in `eclipse-collections-forkjoin/src/main/java/org/eclipse/collections/impl/forkjoin/FJIterate.java`
#### Snippet
```java
        int taskCount = FJIterate.calculateTaskCount(iterable, batchSize);
        FJIterate.forEach(iterable, procedureFactory, combiner, batchSize, taskCount, executor);
        return (R) combiner.getResult();
    }

```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.list.ListIterable' to 'java.util.List'
in `eclipse-collections-testutils/src/main/java/org/eclipse/collections/impl/test/Verify.java`
#### Snippet
```java
            for (K key : expectedListMultimap.keysView())
            {
                Verify.assertListsEqual(multimapName + " value list for key:" + key, (List<V>) expectedListMultimap.get(key), (List<V>) actualListMultimap.get(key));
            }
            Assert.assertEquals(multimapName, expectedListMultimap, actualListMultimap);
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.list.ListIterable' to 'java.util.List'
in `eclipse-collections-testutils/src/main/java/org/eclipse/collections/impl/test/Verify.java`
#### Snippet
```java
            for (K key : expectedListMultimap.keysView())
            {
                Verify.assertListsEqual(multimapName + " value list for key:" + key, (List<V>) expectedListMultimap.get(key), (List<V>) actualListMultimap.get(key));
            }
            Assert.assertEquals(multimapName, expectedListMultimap, actualListMultimap);
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.set.SetIterable' to 'java.util.Set'
in `eclipse-collections-testutils/src/main/java/org/eclipse/collections/impl/test/Verify.java`
#### Snippet
```java
            for (K key : expectedSetMultimap.keysView())
            {
                Verify.assertSetsEqual(multimapName + " value set for key:" + key, (Set<V>) expectedSetMultimap.get(key), (Set<V>) actualSetMultimap.get(key));
            }
            Assert.assertEquals(multimapName, expectedSetMultimap, actualSetMultimap);
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.set.SetIterable' to 'java.util.Set'
in `eclipse-collections-testutils/src/main/java/org/eclipse/collections/impl/test/Verify.java`
#### Snippet
```java
            for (K key : expectedSetMultimap.keysView())
            {
                Verify.assertSetsEqual(multimapName + " value set for key:" + key, (Set<V>) expectedSetMultimap.get(key), (Set<V>) actualSetMultimap.get(key));
            }
            Assert.assertEquals(multimapName, expectedSetMultimap, actualSetMultimap);
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.set.sorted.SortedSetIterable' to 'java.util.SortedSet'
in `eclipse-collections-testutils/src/main/java/org/eclipse/collections/impl/test/Verify.java`
#### Snippet
```java
            for (K key : expectedSortedSetMultimap.keysView())
            {
                Verify.assertSortedSetsEqual(multimapName + " value set for key:" + key, (SortedSet<V>) expectedSortedSetMultimap.get(key), (SortedSet<V>) actualSortedSetMultimap.get(key));
            }
            Assert.assertEquals(multimapName, expectedSortedSetMultimap, actualSortedSetMultimap);
```

### UNCHECKED_WARNING
Unchecked cast: 'org.eclipse.collections.api.set.sorted.SortedSetIterable' to 'java.util.SortedSet'
in `eclipse-collections-testutils/src/main/java/org/eclipse/collections/impl/test/Verify.java`
#### Snippet
```java
            for (K key : expectedSortedSetMultimap.keysView())
            {
                Verify.assertSortedSetsEqual(multimapName + " value set for key:" + key, (SortedSet<V>) expectedSortedSetMultimap.get(key), (SortedSet<V>) actualSortedSetMultimap.get(key));
            }
            Assert.assertEquals(multimapName, expectedSortedSetMultimap, actualSortedSetMultimap);
```

## RuleId[id=JavadocReference]
### JavadocReference
Cannot resolve symbol `ObjectIntProcedure`
in `eclipse-collections-api/src/main/java/org/eclipse/collections/api/InternalIterable.java`
#### Snippet
```java
     * </pre>
     *
     * @deprecated in 6.0. Use {@link OrderedIterable#forEachWithIndex(ObjectIntProcedure)} instead.
     */
    @Deprecated
```

### JavadocReference
Cannot resolve symbol `ShortFunction`
in `eclipse-collections-api/src/main/java/org/eclipse/collections/api/RichIterable.java`
#### Snippet
```java

    /**
     * Same as {@link #collectShort(ShortFunction)}, except that the results are gathered into the specified {@code target}
     * collection.
     * <p>
```

### JavadocReference
Cannot resolve symbol `ShortFunction`
in `eclipse-collections-api/src/main/java/org/eclipse/collections/api/RichIterable.java`
#### Snippet
```java
     * </pre>
     *
     * @param shortFunction a {@link ShortFunction} to use as the collect transformation function
     * @param target        the MutableShortCollection to append to for all elements in this {@code RichIterable}
     * @return {@code target}, which contains appended elements as a result of the collect transformation
```

### JavadocReference
Cannot resolve symbol `IntFunction`
in `eclipse-collections-api/src/main/java/org/eclipse/collections/api/RichIterable.java`
#### Snippet
```java

    /**
     * Same as {@link #collectInt(IntFunction)}, except that the results are gathered into the specified {@code target}
     * collection.
     * <p>
```

### JavadocReference
Cannot resolve symbol `IntFunction`
in `eclipse-collections-api/src/main/java/org/eclipse/collections/api/RichIterable.java`
#### Snippet
```java
     * </pre>
     *
     * @param intFunction a {@link IntFunction} to use as the collect transformation function
     * @param target      the MutableIntCollection to append to for all elements in this {@code RichIterable}
     * @return {@code target}, which contains appended elements as a result of the collect transformation
```

### JavadocReference
Cannot resolve symbol `CharFunction`
in `eclipse-collections-api/src/main/java/org/eclipse/collections/api/RichIterable.java`
#### Snippet
```java

    /**
     * Same as {@link #collectChar(CharFunction)}, except that the results are gathered into the specified {@code target}
     * collection.
     * <p>
```

### JavadocReference
Cannot resolve symbol `CharFunction`
in `eclipse-collections-api/src/main/java/org/eclipse/collections/api/RichIterable.java`
#### Snippet
```java
     * </pre>
     *
     * @param charFunction a {@link CharFunction} to use as the collect transformation function
     * @param target       the MutableCharCollection to append to for all elements in this {@code RichIterable}
     * @return {@code target}, which contains appended elements as a result of the collect transformation
```

### JavadocReference
Cannot resolve symbol `DoubleFunction`
in `eclipse-collections-api/src/main/java/org/eclipse/collections/api/RichIterable.java`
#### Snippet
```java

    /**
     * Same as {@link #collectDouble(DoubleFunction)}, except that the results are gathered into the specified {@code target}
     * collection.
     * <p>
```

### JavadocReference
Cannot resolve symbol `DoubleFunction`
in `eclipse-collections-api/src/main/java/org/eclipse/collections/api/RichIterable.java`
#### Snippet
```java
     * </pre>
     *
     * @param doubleFunction a {@link DoubleFunction} to use as the collect transformation function
     * @param target         the MutableDoubleCollection to append to for all elements in this {@code RichIterable}
     * @return {@code target}, which contains appended elements as a result of the collect transformation
```

### JavadocReference
Cannot resolve symbol `ByteFunction`
in `eclipse-collections-api/src/main/java/org/eclipse/collections/api/RichIterable.java`
#### Snippet
```java

    /**
     * Same as {@link #collectByte(ByteFunction)}, except that the results are gathered into the specified {@code target}
     * collection.
     * <p>
```

### JavadocReference
Cannot resolve symbol `ByteFunction`
in `eclipse-collections-api/src/main/java/org/eclipse/collections/api/RichIterable.java`
#### Snippet
```java
     * </pre>
     *
     * @param byteFunction a {@link ByteFunction} to use as the collect transformation function
     * @param target       the MutableByteCollection to append to for all elements in this {@code RichIterable}
     * @return {@code target}, which contains appended elements as a result of the collect transformation
```

### JavadocReference
Cannot resolve symbol `BooleanFunction`
in `eclipse-collections-api/src/main/java/org/eclipse/collections/api/RichIterable.java`
#### Snippet
```java

    /**
     * Same as {@link #collectBoolean(BooleanFunction)}, except that the results are gathered into the specified {@code target}
     * collection.
     * <p>
```

### JavadocReference
Cannot resolve symbol `BooleanFunction`
in `eclipse-collections-api/src/main/java/org/eclipse/collections/api/RichIterable.java`
#### Snippet
```java
     * </pre>
     *
     * @param booleanFunction a {@link BooleanFunction} to use as the collect transformation function
     * @param target          the MutableBooleanCollection to append to for all elements in this {@code RichIterable}
     * @return {@code target}, which contains appended elements as a result of the collect transformation
```

### JavadocReference
Cannot resolve symbol `LongFunction`
in `eclipse-collections-api/src/main/java/org/eclipse/collections/api/RichIterable.java`
#### Snippet
```java

    /**
     * Same as {@link #collectLong(LongFunction)}, except that the results are gathered into the specified {@code target}
     * collection.
     * <p>
```

### JavadocReference
Cannot resolve symbol `LongFunction`
in `eclipse-collections-api/src/main/java/org/eclipse/collections/api/RichIterable.java`
#### Snippet
```java
     * </pre>
     *
     * @param longFunction a {@link LongFunction} to use as the collect transformation function
     * @param target       the MutableLongCollection to append to for all elements in this {@code RichIterable}
     * @return {@code target}, which contains appended elements as a result of the collect transformation
```

### JavadocReference
Cannot resolve symbol `FloatFunction`
in `eclipse-collections-api/src/main/java/org/eclipse/collections/api/RichIterable.java`
#### Snippet
```java

    /**
     * Same as {@link #collectFloat(FloatFunction)}, except that the results are gathered into the specified {@code target}
     * collection.
     * <p>
```

### JavadocReference
Cannot resolve symbol `FloatFunction`
in `eclipse-collections-api/src/main/java/org/eclipse/collections/api/RichIterable.java`
#### Snippet
```java
     * </pre>
     *
     * @param floatFunction a {@link FloatFunction} to use as the collect transformation function
     * @param target        the MutableFloatCollection to append to for all elements in this {@code RichIterable}
     * @return {@code target}, which contains appended elements as a result of the collect transformation
```

### JavadocReference
Cannot resolve symbol `org.eclipse.collections.api.block.procedure.primitive.ObjectIntProcedure`
in `eclipse-collections-api/src/main/java/org/eclipse/collections/api/block/procedure/ObjectIntProcedure.java`
#### Snippet
```java
 * which is usually the index of the current element of a collection.
 *
 * @deprecated since 3.0 use {@link org.eclipse.collections.api.block.procedure.primitive.ObjectIntProcedure} instead.
 */
@FunctionalInterface
```

### JavadocReference
Cannot resolve symbol `ObjectIntPair`
in `eclipse-collections-api/src/main/java/org/eclipse/collections/api/factory/bag/ImmutableBagFactory.java`
#### Snippet
```java

    /**
     * Same as {@link #withOccurrences(ObjectIntPair[])}
     *
     * @since 10.3
```

### JavadocReference
Cannot resolve symbol `ObjectIntPair`
in `eclipse-collections-api/src/main/java/org/eclipse/collections/api/factory/bag/MutableBagFactory.java`
#### Snippet
```java

    /**
     *  Same as {@link #withOccurrences(ObjectIntPair[])}.
     *
     *  @since 10.3
```

### JavadocReference
Symbol `org.eclipse.collections.impl.set.immutable.ImmutableEmptySet` is inaccessible from here
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/strategy/immutable/package-info.java`
#### Snippet
```java
 * <ul>
 *     <li>
 *          {@link org.eclipse.collections.impl.set.strategy.immutable.ImmutableEmptySetWithHashingStrategy} - an {@link org.eclipse.collections.impl.set.immutable.ImmutableEmptySet} with user defined hashing strategy.
 *     </li>
 *     <li>
```

### JavadocReference
Symbol `org.eclipse.collections.impl.set.immutable.ImmutableUnifiedSet` is inaccessible from here
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/strategy/immutable/package-info.java`
#### Snippet
```java
 *     </li>
 *     <li>
 *          {@link org.eclipse.collections.impl.set.strategy.immutable.ImmutableUnifiedSetWithHashingStrategy} - an {@link org.eclipse.collections.impl.set.immutable.ImmutableUnifiedSet} with user defined hashing strategy.
 *     </li>
 * </ul>
```

### JavadocReference
Cannot resolve symbol `ImmutableBooleanSet`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/immutable/primitive/ImmutableBooleanSetFactoryImpl.java`
#### Snippet
```java

/**
 * ImmutableBooleanSetFactoryImpl is a factory implementation which creates instances of type {@link ImmutableBooleanSet}.
 *
 * @since 4.0.
```

### JavadocReference
Cannot resolve symbol `org.eclipse.collections.api.list.primitive.ImmutableIntList`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/immutable/primitive/package-info.java`
#### Snippet
```java
 * This package contains implementations of immutable primitive list interfaces and immutable primitive list factory interfaces.
 * <p>
 *     An {@link org.eclipse.collections.api.list.primitive.ImmutableIntList} is the non-modifiable equivalent interface of {@link org.eclipse.collections.api.list.primitive.MutableIntList}.
 */
package org.eclipse.collections.impl.list.immutable.primitive;
```

### JavadocReference
Cannot resolve symbol `org.eclipse.collections.api.list.primitive.MutableIntList`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/immutable/primitive/package-info.java`
#### Snippet
```java
 * This package contains implementations of immutable primitive list interfaces and immutable primitive list factory interfaces.
 * <p>
 *     An {@link org.eclipse.collections.api.list.primitive.ImmutableIntList} is the non-modifiable equivalent interface of {@link org.eclipse.collections.api.list.primitive.MutableIntList}.
 */
package org.eclipse.collections.impl.list.immutable.primitive;
```

### JavadocReference
Cannot resolve symbol `ObjectIntProcedure`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/ObjectIntProcedures.java`
#### Snippet
```java

/**
 * Contains factory methods for creating {@link ObjectIntProcedure} instances.
 */
public final class ObjectIntProcedures
```

### JavadocReference
Cannot resolve symbol `CharToCharFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/function/primitive/CharFunction.java`
#### Snippet
```java
 * A CharFunction can be used to convert one character to another.
 *
 * @deprecated since 3.0. Use {@link CharToCharFunction} instead.
 */
@FunctionalInterface
```

### JavadocReference
Cannot resolve symbol `org.eclipse.collections.api.block.predicate.primitive.CharPredicate`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/predicate/primitive/CharPredicate.java`
#### Snippet
```java
 * A Predicate that accepts a char value
 *
 * @deprecated since 3.0. Use {@link org.eclipse.collections.api.block.predicate.primitive.CharPredicate} instead.
 */
@FunctionalInterface
```

### JavadocReference
Cannot resolve symbol `org.eclipse.collections.api.block.procedure.primitive.CharProcedure`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/procedure/primitive/CharProcedure.java`
#### Snippet
```java

/**
 * @deprecated since 3.0 use {@link org.eclipse.collections.api.block.procedure.primitive.CharProcedure}
 */
@FunctionalInterface
```

### JavadocReference
Cannot resolve symbol `org.eclipse.collections.api.block.procedure.primitive.IntIntProcedure`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/procedure/primitive/IntIntProcedure.java`
#### Snippet
```java
 * second arguments.
 *
 * @deprecated since 3.0 use {@link org.eclipse.collections.api.block.procedure.primitive.IntIntProcedure}
 */
@FunctionalInterface
```

### JavadocReference
Cannot resolve symbol `org.eclipse.collections.api.block.procedure.primitive.IntObjectProcedure`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/procedure/primitive/IntObjectProcedure.java`
#### Snippet
```java

/**
 * @deprecated since 3.0 use {@link org.eclipse.collections.api.block.procedure.primitive.IntObjectProcedure}
 */
@FunctionalInterface
```

### JavadocReference
Cannot resolve symbol `org.eclipse.collections.api.block.procedure.primitive.IntProcedure`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/procedure/primitive/IntProcedure.java`
#### Snippet
```java

/**
 * @deprecated since 3.0 use {@link org.eclipse.collections.api.block.procedure.primitive.IntProcedure}
 */
@FunctionalInterface
```

### JavadocReference
Cannot resolve symbol `CharPredicate`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/StringIterate.java`
#### Snippet
```java
    /**
     * @return a new string with all the characters that return true for the specified {@code predicate}.
     * @deprecated since 7.0. Use {@link #selectChar(String, CharPredicate)} instead.
     */
    @Deprecated
```

### JavadocReference
Cannot resolve symbol `CharProcedure`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/StringIterate.java`
#### Snippet
```java

    /**
     * For each char in the {@code string}, execute the {@link CharProcedure}.
     *
     * @since 7.0
```

### JavadocReference
Cannot resolve symbol `CharProcedure`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/StringIterate.java`
#### Snippet
```java

    /**
     * For each character in the {@code string}, execute the {@link CharProcedure}.
     *
     * @deprecated since 7.0. Use {@link #forEachChar(String, CharProcedure)} instead.
```

### JavadocReference
Cannot resolve symbol `CharProcedure`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/StringIterate.java`
#### Snippet
```java
     * For each character in the {@code string}, execute the {@link CharProcedure}.
     *
     * @deprecated since 7.0. Use {@link #forEachChar(String, CharProcedure)} instead.
     */
    @Deprecated
```

### JavadocReference
Cannot resolve symbol `CharProcedure`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/StringIterate.java`
#### Snippet
```java

    /**
     * For each character in the {@code string}, execute the {@link CharProcedure}.
     *
     * @deprecated since 3.0. Use {@link #forEach(String, CharProcedure)} instead.
```

### JavadocReference
Cannot resolve symbol `CharProcedure`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/StringIterate.java`
#### Snippet
```java
     * For each character in the {@code string}, execute the {@link CharProcedure}.
     *
     * @deprecated since 3.0. Use {@link #forEach(String, CharProcedure)} instead.
     */
    @Deprecated
```

### JavadocReference
Cannot resolve symbol `CharPredicate`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/StringIterate.java`
#### Snippet
```java
    /**
     * @return true if any of the characters in the {@code string} answer true for the specified {@code predicate}.
     * @deprecated since 7.0. Use {@link #anySatisfyChar(String, CharPredicate)} instead.
     */
    @Deprecated
```

### JavadocReference
Cannot resolve symbol `CharProcedure`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/StringIterate.java`
#### Snippet
```java

    /**
     * For each char in the {@code string} in reverse order, execute the {@link CharProcedure}.
     *
     * @since 7.0
```

### JavadocReference
Cannot resolve symbol `CharToCharFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/StringIterate.java`
#### Snippet
```java

    /**
     * @deprecated since 3.0. Use {@link #collect(String, CharToCharFunction)} instead.
     */
    @Deprecated
```

### JavadocReference
Cannot resolve symbol `CharToCharFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/StringIterate.java`
#### Snippet
```java
     * Transform the char elements to a new string using the specified function {@code function}.
     *
     * @deprecated since 7.0. Use {@link #collectChar(String, CharToCharFunction)} instead.
     */
    @Deprecated
```

### JavadocReference
Cannot resolve symbol `CharPredicate`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/StringIterate.java`
#### Snippet
```java
    /**
     * @return a new string excluding all the characters that return true for the specified {@code predicate}.
     * @deprecated since 7.0. Use {@link #rejectChar(String, CharPredicate)} instead.
     */
    @Deprecated
```

### JavadocReference
Cannot resolve symbol `CharPredicate`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/StringIterate.java`
#### Snippet
```java
     * no value is found.
     *
     * @deprecated since 7.0. Use {@link #detectCharIfNone(String, CharPredicate, char)} instead.
     */
    @Deprecated
```

### JavadocReference
Cannot resolve symbol `CharPredicate`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/StringIterate.java`
#### Snippet
```java
    /**
     * @return true if all the characters in the {@code string} answer true for the specified {@code predicate}.
     * @deprecated since 7.0. Use {@link #allSatisfyChar(String, CharPredicate)} instead.
     */
    @Deprecated
```

### JavadocReference
Cannot resolve symbol `CharPredicate`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/StringIterate.java`
#### Snippet
```java
     * default string if no value is found.
     *
     * @deprecated since 7.0. Use {@link #detectCharIfNone(String, CharPredicate, String)} instead.
     */
    @Deprecated
```

### JavadocReference
Cannot resolve symbol `CharPredicate`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/StringIterate.java`
#### Snippet
```java
    /**
     * @return true if none of the characters in the {@code string} answer true for the specified {@code predicate}.
     * @deprecated since 7.0. Use {@link #noneSatisfyChar(String, CharPredicate)} instead.
     */
    @Deprecated
```

### JavadocReference
Cannot resolve symbol `CharPredicate`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/StringIterate.java`
#### Snippet
```java
     * Find the first element that returns true for the specified {@code predicate}.
     *
     * @deprecated since 7.0. Use {@link #detectChar(String, CharPredicate)} instead.
     */
    @Deprecated
```

### JavadocReference
Cannot resolve symbol `CharPredicate`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/StringIterate.java`
#### Snippet
```java
     * Count the number of elements that return true for the specified {@code predicate}.
     *
     * @deprecated since 7.0. Use {@link #countChar(String, CharPredicate)} instead.
     */
    @Deprecated
```

### JavadocReference
Cannot resolve symbol `CharProcedure`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/StringIterate.java`
#### Snippet
```java

    /**
     * For each char in the {@code string} in reverse order, execute the {@link CharProcedure}.
     *
     * @deprecated since 7.0. Use {@link #reverseForEachChar(String, CharProcedure)} instead.
```

### JavadocReference
Cannot resolve symbol `CharProcedure`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/StringIterate.java`
#### Snippet
```java
     * For each char in the {@code string} in reverse order, execute the {@link CharProcedure}.
     *
     * @deprecated since 7.0. Use {@link #reverseForEachChar(String, CharProcedure)} instead.
     */
    @Deprecated
```

### JavadocReference
Cannot resolve symbol `ObjectIntProcedure`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#forEachWithIndex(Iterable, ObjectIntProcedure)
     */
    public static <T> void forEachWithIndex(List<T> list, ObjectIntProcedure<? super T> objectIntProcedure)
```

### JavadocReference
Cannot resolve symbol `FloatFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectFloat(Iterable, FloatFunction)
     */
    public static <T> MutableFloatList collectFloat(
```

### JavadocReference
Cannot resolve symbol `FloatObjectToFloatFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#injectInto(float, Iterable, FloatObjectToFloatFunction)
     */
    public static <T> float injectInto(float injectValue, List<T> list, FloatObjectToFloatFunction<? super T> function)
```

### JavadocReference
Cannot resolve symbol `collectFloat(Iterable, FloatFunction, MutableFloatCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectFloat(Iterable, FloatFunction, MutableFloatCollection)
     */
    public static <T, R extends MutableFloatCollection> R collectFloat(
```

### JavadocReference
Cannot resolve symbol `FloatFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectFloat(Iterable, FloatFunction, MutableFloatCollection)
     */
    public static <T, R extends MutableFloatCollection> R collectFloat(
```

### JavadocReference
Cannot resolve symbol `MutableFloatCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectFloat(Iterable, FloatFunction, MutableFloatCollection)
     */
    public static <T, R extends MutableFloatCollection> R collectFloat(
```

### JavadocReference
Cannot resolve symbol `collectBoolean(Iterable, BooleanFunction, MutableBooleanCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectBoolean(Iterable, BooleanFunction, MutableBooleanCollection)
     */
    public static <T, R extends MutableBooleanCollection> R collectBoolean(
```

### JavadocReference
Cannot resolve symbol `BooleanFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectBoolean(Iterable, BooleanFunction, MutableBooleanCollection)
     */
    public static <T, R extends MutableBooleanCollection> R collectBoolean(
```

### JavadocReference
Cannot resolve symbol `MutableBooleanCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectBoolean(Iterable, BooleanFunction, MutableBooleanCollection)
     */
    public static <T, R extends MutableBooleanCollection> R collectBoolean(
```

### JavadocReference
Cannot resolve symbol `collectInt(Iterable, IntFunction, MutableIntCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectInt(Iterable, IntFunction, MutableIntCollection)
     */
    public static <T, R extends MutableIntCollection> R collectInt(
```

### JavadocReference
Cannot resolve symbol `IntFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectInt(Iterable, IntFunction, MutableIntCollection)
     */
    public static <T, R extends MutableIntCollection> R collectInt(
```

### JavadocReference
Cannot resolve symbol `MutableIntCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectInt(Iterable, IntFunction, MutableIntCollection)
     */
    public static <T, R extends MutableIntCollection> R collectInt(
```

### JavadocReference
Cannot resolve symbol `LongFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectLong(Iterable, LongFunction)
     */
    public static <T> MutableLongList collectLong(
```

### JavadocReference
Cannot resolve symbol `CharFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectChar(Iterable, CharFunction)
     */
    public static <T> MutableCharList collectChar(
```

### JavadocReference
Cannot resolve symbol `collectShort(Iterable, ShortFunction, MutableShortCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectShort(Iterable, ShortFunction, MutableShortCollection)
     */
    public static <T, R extends MutableShortCollection> R collectShort(
```

### JavadocReference
Cannot resolve symbol `ShortFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectShort(Iterable, ShortFunction, MutableShortCollection)
     */
    public static <T, R extends MutableShortCollection> R collectShort(
```

### JavadocReference
Cannot resolve symbol `MutableShortCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectShort(Iterable, ShortFunction, MutableShortCollection)
     */
    public static <T, R extends MutableShortCollection> R collectShort(
```

### JavadocReference
Cannot resolve symbol `DoubleFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectDouble(Iterable, DoubleFunction)
     */
    public static <T> MutableDoubleList collectDouble(
```

### JavadocReference
Cannot resolve symbol `ShortFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectShort(Iterable, ShortFunction)
     */
    public static <T> MutableShortList collectShort(
```

### JavadocReference
Cannot resolve symbol `ByteFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectByte(Iterable, ByteFunction)
     */
    public static <T> MutableByteList collectByte(
```

### JavadocReference
Cannot resolve symbol `IntObjectToIntFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#injectInto(int, Iterable, IntObjectToIntFunction)
     */
    public static <T> int injectInto(int injectValue, List<T> list, IntObjectToIntFunction<? super T> function)
```

### JavadocReference
Cannot resolve symbol `collectDouble(Iterable, DoubleFunction, MutableDoubleCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectDouble(Iterable, DoubleFunction, MutableDoubleCollection)
     */
    public static <T, R extends MutableDoubleCollection> R collectDouble(
```

### JavadocReference
Cannot resolve symbol `DoubleFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectDouble(Iterable, DoubleFunction, MutableDoubleCollection)
     */
    public static <T, R extends MutableDoubleCollection> R collectDouble(
```

### JavadocReference
Cannot resolve symbol `MutableDoubleCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectDouble(Iterable, DoubleFunction, MutableDoubleCollection)
     */
    public static <T, R extends MutableDoubleCollection> R collectDouble(
```

### JavadocReference
Cannot resolve symbol `collectLong(Iterable, LongFunction, MutableLongCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectLong(Iterable, LongFunction, MutableLongCollection)
     */
    public static <T, R extends MutableLongCollection> R collectLong(
```

### JavadocReference
Cannot resolve symbol `LongFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectLong(Iterable, LongFunction, MutableLongCollection)
     */
    public static <T, R extends MutableLongCollection> R collectLong(
```

### JavadocReference
Cannot resolve symbol `MutableLongCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectLong(Iterable, LongFunction, MutableLongCollection)
     */
    public static <T, R extends MutableLongCollection> R collectLong(
```

### JavadocReference
Cannot resolve symbol `LongObjectToLongFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#injectInto(long, Iterable, LongObjectToLongFunction)
     */
    public static <T> long injectInto(long injectValue, List<T> list, LongObjectToLongFunction<? super T> function)
```

### JavadocReference
Cannot resolve symbol `collectChar(Iterable, CharFunction, MutableCharCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectChar(Iterable, CharFunction, MutableCharCollection)
     */
    public static <T, R extends MutableCharCollection> R collectChar(
```

### JavadocReference
Cannot resolve symbol `CharFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectChar(Iterable, CharFunction, MutableCharCollection)
     */
    public static <T, R extends MutableCharCollection> R collectChar(
```

### JavadocReference
Cannot resolve symbol `MutableCharCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectChar(Iterable, CharFunction, MutableCharCollection)
     */
    public static <T, R extends MutableCharCollection> R collectChar(
```

### JavadocReference
Cannot resolve symbol `collectByte(Iterable, ByteFunction, MutableByteCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectByte(Iterable, ByteFunction, MutableByteCollection)
     */
    public static <T, R extends MutableByteCollection> R collectByte(
```

### JavadocReference
Cannot resolve symbol `ByteFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectByte(Iterable, ByteFunction, MutableByteCollection)
     */
    public static <T, R extends MutableByteCollection> R collectByte(
```

### JavadocReference
Cannot resolve symbol `MutableByteCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectByte(Iterable, ByteFunction, MutableByteCollection)
     */
    public static <T, R extends MutableByteCollection> R collectByte(
```

### JavadocReference
Cannot resolve symbol `BooleanFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectBoolean(Iterable, BooleanFunction)
     */
    public static <T> MutableBooleanList collectBoolean(
```

### JavadocReference
Cannot resolve symbol `IntFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectInt(Iterable, IntFunction)
     */
    public static <T> MutableIntList collectInt(
```

### JavadocReference
Cannot resolve symbol `DoubleObjectToDoubleFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#injectInto(double, Iterable, DoubleObjectToDoubleFunction)
     */
    public static <T> double injectInto(double injectValue, List<T> list, DoubleObjectToDoubleFunction<? super T> function)
```

### JavadocReference
Cannot resolve symbol `DoubleFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IterableIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#sumOfDouble(Iterable, DoubleFunction)
     */
    public static <T> double sumOfDouble(Iterable<T> iterable, DoubleFunction<? super T> function)
```

### JavadocReference
Cannot resolve symbol `FloatObjectToFloatFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IterableIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#injectInto(float, Iterable, FloatObjectToFloatFunction)
     */
    public static <T> float injectInto(
```

### JavadocReference
Cannot resolve symbol `DoubleFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IterableIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectDouble(Iterable, DoubleFunction)
     */
    public static <T> MutableDoubleCollection collectDouble(
```

### JavadocReference
Cannot resolve symbol `collectInt(Iterable, IntFunction, MutableIntCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IterableIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectInt(Iterable, IntFunction, MutableIntCollection)
     */
    public static <T, R extends MutableIntCollection> R collectInt(Iterable<T> iterable, IntFunction<? super T> intFunction, R target)
```

### JavadocReference
Cannot resolve symbol `IntFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IterableIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectInt(Iterable, IntFunction, MutableIntCollection)
     */
    public static <T, R extends MutableIntCollection> R collectInt(Iterable<T> iterable, IntFunction<? super T> intFunction, R target)
```

### JavadocReference
Cannot resolve symbol `MutableIntCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IterableIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectInt(Iterable, IntFunction, MutableIntCollection)
     */
    public static <T, R extends MutableIntCollection> R collectInt(Iterable<T> iterable, IntFunction<? super T> intFunction, R target)
```

### JavadocReference
Cannot resolve symbol `collectByte(Iterable, ByteFunction, MutableByteCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IterableIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectByte(Iterable, ByteFunction, MutableByteCollection)
     */
    public static <T, R extends MutableByteCollection> R collectByte(Iterable<T> iterable, ByteFunction<? super T> byteFunction, R target)
```

### JavadocReference
Cannot resolve symbol `ByteFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IterableIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectByte(Iterable, ByteFunction, MutableByteCollection)
     */
    public static <T, R extends MutableByteCollection> R collectByte(Iterable<T> iterable, ByteFunction<? super T> byteFunction, R target)
```

### JavadocReference
Cannot resolve symbol `MutableByteCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IterableIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectByte(Iterable, ByteFunction, MutableByteCollection)
     */
    public static <T, R extends MutableByteCollection> R collectByte(Iterable<T> iterable, ByteFunction<? super T> byteFunction, R target)
```

### JavadocReference
Cannot resolve symbol `FloatFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IterableIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#sumOfFloat(Iterable, FloatFunction)
     */
    public static <T> double sumOfFloat(Iterable<T> iterable, FloatFunction<? super T> function)
```

### JavadocReference
Cannot resolve symbol `IntFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IterableIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#sumOfInt(Iterable, IntFunction)
     */
    public static <T> long sumOfInt(Iterable<T> iterable, IntFunction<? super T> function)
```

### JavadocReference
Cannot resolve symbol `FloatFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IterableIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectFloat(Iterable, FloatFunction)
     */
    public static <T> MutableFloatCollection collectFloat(
```

### JavadocReference
Cannot resolve symbol `collectFloat(FloatFunction, MutableFloatCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/MapIterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#collectFloat(FloatFunction, MutableFloatCollection)
     */
    public static <K, V, R extends MutableFloatCollection> R collectFloat(
```

### JavadocReference
Cannot resolve symbol `collectChar(Iterable, CharFunction, MutableCharCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IterableIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectChar(Iterable, CharFunction, MutableCharCollection)
     */
    public static <T, R extends MutableCharCollection> R collectChar(Iterable<T> iterable, CharFunction<? super T> charFunction, R target)
```

### JavadocReference
Cannot resolve symbol `CharFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IterableIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectChar(Iterable, CharFunction, MutableCharCollection)
     */
    public static <T, R extends MutableCharCollection> R collectChar(Iterable<T> iterable, CharFunction<? super T> charFunction, R target)
```

### JavadocReference
Cannot resolve symbol `MutableCharCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IterableIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectChar(Iterable, CharFunction, MutableCharCollection)
     */
    public static <T, R extends MutableCharCollection> R collectChar(Iterable<T> iterable, CharFunction<? super T> charFunction, R target)
```

### JavadocReference
Cannot resolve symbol `FloatFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/MapIterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#collectFloat(FloatFunction, MutableFloatCollection)
     */
    public static <K, V, R extends MutableFloatCollection> R collectFloat(
```

### JavadocReference
Cannot resolve symbol `DoubleObjectToDoubleFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IterableIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#injectInto(double, Iterable, DoubleObjectToDoubleFunction)
     */
    public static <T> double injectInto(
```

### JavadocReference
Cannot resolve symbol `collectDouble(Iterable, DoubleFunction, MutableDoubleCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IterableIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectDouble(Iterable, DoubleFunction, MutableDoubleCollection)
     */
    public static <T, R extends MutableDoubleCollection> R collectDouble(Iterable<T> iterable, DoubleFunction<? super T> doubleFunction, R target)
```

### JavadocReference
Cannot resolve symbol `MutableFloatCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/MapIterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#collectFloat(FloatFunction, MutableFloatCollection)
     */
    public static <K, V, R extends MutableFloatCollection> R collectFloat(
```

### JavadocReference
Cannot resolve symbol `IntFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/MapIterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#collectInt(IntFunction)
     */
    public static <K, V> MutableIntCollection collectInt(
```

### JavadocReference
Cannot resolve symbol `BooleanFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/MapIterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#collectBoolean(BooleanFunction)
     */
    public static <K, V> MutableBooleanCollection collectBoolean(
```

### JavadocReference
Cannot resolve symbol `ShortFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/MapIterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#collectShort(ShortFunction)
     */
    public static <K, V> MutableShortCollection collectShort(
```

### JavadocReference
Cannot resolve symbol `DoubleFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/MapIterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#collectDouble(DoubleFunction)
     */
    public static <K, V> MutableDoubleCollection collectDouble(
```

### JavadocReference
Cannot resolve symbol `DoubleFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IterableIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectDouble(Iterable, DoubleFunction, MutableDoubleCollection)
     */
    public static <T, R extends MutableDoubleCollection> R collectDouble(Iterable<T> iterable, DoubleFunction<? super T> doubleFunction, R target)
```

### JavadocReference
Cannot resolve symbol `MutableDoubleCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IterableIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectDouble(Iterable, DoubleFunction, MutableDoubleCollection)
     */
    public static <T, R extends MutableDoubleCollection> R collectDouble(Iterable<T> iterable, DoubleFunction<? super T> doubleFunction, R target)
```

### JavadocReference
Cannot resolve symbol `collectLong(Iterable, LongFunction, MutableLongCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IterableIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectLong(Iterable, LongFunction, MutableLongCollection)
     */
    public static <T, R extends MutableLongCollection> R collectLong(Iterable<T> iterable, LongFunction<? super T> longFunction, R target)
```

### JavadocReference
Cannot resolve symbol `LongFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IterableIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectLong(Iterable, LongFunction, MutableLongCollection)
     */
    public static <T, R extends MutableLongCollection> R collectLong(Iterable<T> iterable, LongFunction<? super T> longFunction, R target)
```

### JavadocReference
Cannot resolve symbol `MutableLongCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IterableIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectLong(Iterable, LongFunction, MutableLongCollection)
     */
    public static <T, R extends MutableLongCollection> R collectLong(Iterable<T> iterable, LongFunction<? super T> longFunction, R target)
```

### JavadocReference
Cannot resolve symbol `LongFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IterableIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#sumOfLong(Iterable, LongFunction)
     */
    public static <T> long sumOfLong(Iterable<T> iterable, LongFunction<? super T> function)
```

### JavadocReference
Cannot resolve symbol `collectBoolean(Iterable, BooleanFunction, MutableBooleanCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IterableIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectBoolean(Iterable, BooleanFunction, MutableBooleanCollection)
     */
    public static <T, R extends MutableBooleanCollection> R collectBoolean(Iterable<T> iterable, BooleanFunction<? super T> booleanFunction, R target)
```

### JavadocReference
Cannot resolve symbol `BooleanFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IterableIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectBoolean(Iterable, BooleanFunction, MutableBooleanCollection)
     */
    public static <T, R extends MutableBooleanCollection> R collectBoolean(Iterable<T> iterable, BooleanFunction<? super T> booleanFunction, R target)
```

### JavadocReference
Cannot resolve symbol `MutableBooleanCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IterableIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectBoolean(Iterable, BooleanFunction, MutableBooleanCollection)
     */
    public static <T, R extends MutableBooleanCollection> R collectBoolean(Iterable<T> iterable, BooleanFunction<? super T> booleanFunction, R target)
```

### JavadocReference
Cannot resolve symbol `CharFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IterableIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectChar(Iterable, CharFunction)
     */
    public static <T> MutableCharCollection collectChar(
```

### JavadocReference
Cannot resolve symbol `LongFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IterableIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectLong(Iterable, LongFunction)
     */
    public static <T> MutableLongCollection collectLong(
```

### JavadocReference
Cannot resolve symbol `ObjectIntProcedure`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IterableIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#forEachWithIndex(Iterable, ObjectIntProcedure)
     */
    public static <T> void forEachWithIndex(Iterable<T> iterable, ObjectIntProcedure<? super T> objectIntProcedure)
```

### JavadocReference
Cannot resolve symbol `IntObjectToIntFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IterableIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#injectInto(int, Iterable, IntObjectToIntFunction)
     */
    public static <T> int injectInto(
```

### JavadocReference
Cannot resolve symbol `LongObjectToLongFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IterableIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#injectInto(long, Iterable, LongObjectToLongFunction)
     */
    public static <T> long injectInto(
```

### JavadocReference
Cannot resolve symbol `ByteFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IterableIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectByte(Iterable, ByteFunction)
     */
    public static <T> MutableByteCollection collectByte(
```

### JavadocReference
Cannot resolve symbol `BooleanFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IterableIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectBoolean(Iterable, BooleanFunction)
     */
    public static <T> MutableBooleanCollection collectBoolean(
```

### JavadocReference
Cannot resolve symbol `ShortFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IterableIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectShort(Iterable, ShortFunction)
     */
    public static <T> MutableShortCollection collectShort(
```

### JavadocReference
Cannot resolve symbol `IntFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IterableIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectInt(Iterable, IntFunction)
     */
    public static <T> MutableIntCollection collectInt(
```

### JavadocReference
Cannot resolve symbol `collectFloat(Iterable, FloatFunction, MutableFloatCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IterableIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectFloat(Iterable, FloatFunction, MutableFloatCollection)
     */
    public static <T, R extends MutableFloatCollection> R collectFloat(Iterable<T> iterable, FloatFunction<? super T> floatFunction, R target)
```

### JavadocReference
Cannot resolve symbol `FloatFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IterableIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectFloat(Iterable, FloatFunction, MutableFloatCollection)
     */
    public static <T, R extends MutableFloatCollection> R collectFloat(Iterable<T> iterable, FloatFunction<? super T> floatFunction, R target)
```

### JavadocReference
Cannot resolve symbol `MutableFloatCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IterableIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectFloat(Iterable, FloatFunction, MutableFloatCollection)
     */
    public static <T, R extends MutableFloatCollection> R collectFloat(Iterable<T> iterable, FloatFunction<? super T> floatFunction, R target)
```

### JavadocReference
Cannot resolve symbol `collectShort(Iterable, ShortFunction, MutableShortCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IterableIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectShort(Iterable, ShortFunction, MutableShortCollection)
     */
    public static <T, R extends MutableShortCollection> R collectShort(Iterable<T> iterable, ShortFunction<? super T> shortFunction, R target)
```

### JavadocReference
Cannot resolve symbol `ShortFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IterableIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectShort(Iterable, ShortFunction, MutableShortCollection)
     */
    public static <T, R extends MutableShortCollection> R collectShort(Iterable<T> iterable, ShortFunction<? super T> shortFunction, R target)
```

### JavadocReference
Cannot resolve symbol `ByteFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/MapIterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#collectByte(ByteFunction)
     */
    public static <K, V> MutableByteCollection collectByte(
```

### JavadocReference
Cannot resolve symbol `MutableShortCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IterableIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectShort(Iterable, ShortFunction, MutableShortCollection)
     */
    public static <T, R extends MutableShortCollection> R collectShort(Iterable<T> iterable, ShortFunction<? super T> shortFunction, R target)
```

### JavadocReference
Cannot resolve symbol `collectBoolean(BooleanFunction, MutableBooleanCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/MapIterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#collectBoolean(BooleanFunction, MutableBooleanCollection)
     */
    public static <K, V, R extends MutableBooleanCollection> R collectBoolean(
```

### JavadocReference
Cannot resolve symbol `BooleanFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/MapIterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#collectBoolean(BooleanFunction, MutableBooleanCollection)
     */
    public static <K, V, R extends MutableBooleanCollection> R collectBoolean(
```

### JavadocReference
Cannot resolve symbol `MutableBooleanCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/MapIterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#collectBoolean(BooleanFunction, MutableBooleanCollection)
     */
    public static <K, V, R extends MutableBooleanCollection> R collectBoolean(
```

### JavadocReference
Cannot resolve symbol `collectChar(CharFunction, MutableCharCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/MapIterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#collectChar(CharFunction, MutableCharCollection)
     */
    public static <K, V, R extends MutableCharCollection> R collectChar(
```

### JavadocReference
Cannot resolve symbol `CharFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/MapIterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#collectChar(CharFunction, MutableCharCollection)
     */
    public static <K, V, R extends MutableCharCollection> R collectChar(
```

### JavadocReference
Cannot resolve symbol `MutableCharCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/MapIterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#collectChar(CharFunction, MutableCharCollection)
     */
    public static <K, V, R extends MutableCharCollection> R collectChar(
```

### JavadocReference
Cannot resolve symbol `collectByte(ByteFunction, MutableByteCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/MapIterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#collectByte(ByteFunction, MutableByteCollection)
     */
    public static <K, V, R extends MutableByteCollection> R collectByte(
```

### JavadocReference
Cannot resolve symbol `ByteFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/MapIterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#collectByte(ByteFunction, MutableByteCollection)
     */
    public static <K, V, R extends MutableByteCollection> R collectByte(
```

### JavadocReference
Cannot resolve symbol `MutableByteCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/MapIterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#collectByte(ByteFunction, MutableByteCollection)
     */
    public static <K, V, R extends MutableByteCollection> R collectByte(
```

### JavadocReference
Cannot resolve symbol `collectLong(LongFunction, MutableLongCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/MapIterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#collectLong(LongFunction, MutableLongCollection)
     */
    public static <K, V, R extends MutableLongCollection> R collectLong(
```

### JavadocReference
Cannot resolve symbol `LongFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/MapIterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#collectLong(LongFunction, MutableLongCollection)
     */
    public static <K, V, R extends MutableLongCollection> R collectLong(
```

### JavadocReference
Cannot resolve symbol `MutableLongCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/MapIterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#collectLong(LongFunction, MutableLongCollection)
     */
    public static <K, V, R extends MutableLongCollection> R collectLong(
```

### JavadocReference
Cannot resolve symbol `collectInt(IntFunction, MutableIntCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/MapIterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#collectInt(IntFunction, MutableIntCollection)
     */
    public static <K, V, R extends MutableIntCollection> R collectInt(
```

### JavadocReference
Cannot resolve symbol `IntFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/MapIterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#collectInt(IntFunction, MutableIntCollection)
     */
    public static <K, V, R extends MutableIntCollection> R collectInt(
```

### JavadocReference
Cannot resolve symbol `MutableIntCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/MapIterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#collectInt(IntFunction, MutableIntCollection)
     */
    public static <K, V, R extends MutableIntCollection> R collectInt(
```

### JavadocReference
Cannot resolve symbol `collectDouble(DoubleFunction, MutableDoubleCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/MapIterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#collectDouble(DoubleFunction, MutableDoubleCollection)
     */
    public static <K, V, R extends MutableDoubleCollection> R collectDouble(
```

### JavadocReference
Cannot resolve symbol `DoubleFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/MapIterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#collectDouble(DoubleFunction, MutableDoubleCollection)
     */
    public static <K, V, R extends MutableDoubleCollection> R collectDouble(
```

### JavadocReference
Cannot resolve symbol `MutableDoubleCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/MapIterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#collectDouble(DoubleFunction, MutableDoubleCollection)
     */
    public static <K, V, R extends MutableDoubleCollection> R collectDouble(
```

### JavadocReference
Cannot resolve symbol `FloatFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/MapIterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#collectFloat(FloatFunction)
     */
    public static <K, V> MutableFloatCollection collectFloat(
```

### JavadocReference
Cannot resolve symbol `CharFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/MapIterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#collectChar(CharFunction)
     */
    public static <K, V> MutableCharCollection collectChar(
```

### JavadocReference
Cannot resolve symbol `collectShort(ShortFunction, MutableShortCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/MapIterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#collectShort(ShortFunction, MutableShortCollection)
     */
    public static <K, V, R extends MutableShortCollection> R collectShort(
```

### JavadocReference
Cannot resolve symbol `ShortFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/MapIterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#collectShort(ShortFunction, MutableShortCollection)
     */
    public static <K, V, R extends MutableShortCollection> R collectShort(
```

### JavadocReference
Cannot resolve symbol `MutableShortCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/MapIterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#collectShort(ShortFunction, MutableShortCollection)
     */
    public static <K, V, R extends MutableShortCollection> R collectShort(
```

### JavadocReference
Cannot resolve symbol `LongFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/MapIterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#collectLong(LongFunction)
     */
    public static <K, V> MutableLongCollection collectLong(
```

### JavadocReference
Cannot resolve symbol `ByteFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectByte(Iterable, ByteFunction)
     */
    public static <T> MutableByteList collectByte(
```

### JavadocReference
Cannot resolve symbol `IntObjectToIntFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#injectInto(int, Iterable, IntObjectToIntFunction)
     */
    public static <T> int injectInto(
```

### JavadocReference
Cannot resolve symbol `FloatObjectToFloatFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#injectInto(float, Iterable, FloatObjectToFloatFunction)
     */
    public static <T> float injectInto(
```

### JavadocReference
Cannot resolve symbol `LongObjectToLongFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#injectInto(long, Iterable, LongObjectToLongFunction)
     */
    public static <T> long injectInto(
```

### JavadocReference
Cannot resolve symbol `ObjectIntProcedure`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#forEachWithIndex(Iterable, ObjectIntProcedure)
     */
    public static <T> void forEachWithIndex(ArrayList<T> list, ObjectIntProcedure<? super T> objectIntProcedure)
```

### JavadocReference
Cannot resolve symbol `collectFloat(Iterable, FloatFunction, MutableFloatCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectFloat(Iterable, FloatFunction, MutableFloatCollection)
     */
    public static <T, R extends MutableFloatCollection> R collectFloat(ArrayList<T> list, FloatFunction<? super T> floatFunction, R target)
```

### JavadocReference
Cannot resolve symbol `FloatFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectFloat(Iterable, FloatFunction, MutableFloatCollection)
     */
    public static <T, R extends MutableFloatCollection> R collectFloat(ArrayList<T> list, FloatFunction<? super T> floatFunction, R target)
```

### JavadocReference
Cannot resolve symbol `MutableFloatCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectFloat(Iterable, FloatFunction, MutableFloatCollection)
     */
    public static <T, R extends MutableFloatCollection> R collectFloat(ArrayList<T> list, FloatFunction<? super T> floatFunction, R target)
```

### JavadocReference
Cannot resolve symbol `collectChar(Iterable, CharFunction, MutableCharCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectChar(Iterable, CharFunction, MutableCharCollection)
     */
    public static <T, R extends MutableCharCollection> R collectChar(ArrayList<T> list, CharFunction<? super T> charFunction, R target)
```

### JavadocReference
Cannot resolve symbol `CharFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectChar(Iterable, CharFunction, MutableCharCollection)
     */
    public static <T, R extends MutableCharCollection> R collectChar(ArrayList<T> list, CharFunction<? super T> charFunction, R target)
```

### JavadocReference
Cannot resolve symbol `MutableCharCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectChar(Iterable, CharFunction, MutableCharCollection)
     */
    public static <T, R extends MutableCharCollection> R collectChar(ArrayList<T> list, CharFunction<? super T> charFunction, R target)
```

### JavadocReference
Cannot resolve symbol `collectLong(Iterable, LongFunction, MutableLongCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectLong(Iterable, LongFunction, MutableLongCollection)
     */
    public static <T, R extends MutableLongCollection> R collectLong(ArrayList<T> list, LongFunction<? super T> longFunction, R target)
```

### JavadocReference
Cannot resolve symbol `LongFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectLong(Iterable, LongFunction, MutableLongCollection)
     */
    public static <T, R extends MutableLongCollection> R collectLong(ArrayList<T> list, LongFunction<? super T> longFunction, R target)
```

### JavadocReference
Cannot resolve symbol `MutableLongCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectLong(Iterable, LongFunction, MutableLongCollection)
     */
    public static <T, R extends MutableLongCollection> R collectLong(ArrayList<T> list, LongFunction<? super T> longFunction, R target)
```

### JavadocReference
Cannot resolve symbol `collectInt(Iterable, IntFunction, MutableIntCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectInt(Iterable, IntFunction, MutableIntCollection)
     */
    public static <T, R extends MutableIntCollection> R collectInt(ArrayList<T> list, IntFunction<? super T> intFunction, R target)
```

### JavadocReference
Cannot resolve symbol `IntFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectInt(Iterable, IntFunction, MutableIntCollection)
     */
    public static <T, R extends MutableIntCollection> R collectInt(ArrayList<T> list, IntFunction<? super T> intFunction, R target)
```

### JavadocReference
Cannot resolve symbol `MutableIntCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectInt(Iterable, IntFunction, MutableIntCollection)
     */
    public static <T, R extends MutableIntCollection> R collectInt(ArrayList<T> list, IntFunction<? super T> intFunction, R target)
```

### JavadocReference
Cannot resolve symbol `collectDouble(Iterable, DoubleFunction, MutableDoubleCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectDouble(Iterable, DoubleFunction, MutableDoubleCollection)
     */
    public static <T, R extends MutableDoubleCollection> R collectDouble(ArrayList<T> list, DoubleFunction<? super T> doubleFunction, R target)
```

### JavadocReference
Cannot resolve symbol `DoubleFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectDouble(Iterable, DoubleFunction, MutableDoubleCollection)
     */
    public static <T, R extends MutableDoubleCollection> R collectDouble(ArrayList<T> list, DoubleFunction<? super T> doubleFunction, R target)
```

### JavadocReference
Cannot resolve symbol `MutableDoubleCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectDouble(Iterable, DoubleFunction, MutableDoubleCollection)
     */
    public static <T, R extends MutableDoubleCollection> R collectDouble(ArrayList<T> list, DoubleFunction<? super T> doubleFunction, R target)
```

### JavadocReference
Cannot resolve symbol `collectBoolean(Iterable, BooleanFunction, MutableBooleanCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectBoolean(Iterable, BooleanFunction, MutableBooleanCollection)
     */
    public static <T, R extends MutableBooleanCollection> R collectBoolean(ArrayList<T> list, BooleanFunction<? super T> booleanFunction, R target)
```

### JavadocReference
Cannot resolve symbol `BooleanFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectBoolean(Iterable, BooleanFunction, MutableBooleanCollection)
     */
    public static <T, R extends MutableBooleanCollection> R collectBoolean(ArrayList<T> list, BooleanFunction<? super T> booleanFunction, R target)
```

### JavadocReference
Cannot resolve symbol `MutableBooleanCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectBoolean(Iterable, BooleanFunction, MutableBooleanCollection)
     */
    public static <T, R extends MutableBooleanCollection> R collectBoolean(ArrayList<T> list, BooleanFunction<? super T> booleanFunction, R target)
```

### JavadocReference
Cannot resolve symbol `LongFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectLong(Iterable, LongFunction)
     */
    public static <T> MutableLongList collectLong(
```

### JavadocReference
Cannot resolve symbol `ShortFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectShort(Iterable, ShortFunction)
     */
    public static <T> MutableShortList collectShort(
```

### JavadocReference
Cannot resolve symbol `IntFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectInt(Iterable, IntFunction)
     */
    public static <T> MutableIntList collectInt(
```

### JavadocReference
Cannot resolve symbol `collectByte(Iterable, ByteFunction, MutableByteCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectByte(Iterable, ByteFunction, MutableByteCollection)
     */
    public static <T, R extends MutableByteCollection> R collectByte(ArrayList<T> list, ByteFunction<? super T> byteFunction, R target)
```

### JavadocReference
Cannot resolve symbol `ByteFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectByte(Iterable, ByteFunction, MutableByteCollection)
     */
    public static <T, R extends MutableByteCollection> R collectByte(ArrayList<T> list, ByteFunction<? super T> byteFunction, R target)
```

### JavadocReference
Cannot resolve symbol `MutableByteCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectByte(Iterable, ByteFunction, MutableByteCollection)
     */
    public static <T, R extends MutableByteCollection> R collectByte(ArrayList<T> list, ByteFunction<? super T> byteFunction, R target)
```

### JavadocReference
Cannot resolve symbol `collectShort(Iterable, ShortFunction, MutableShortCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectShort(Iterable, ShortFunction, MutableShortCollection)
     */
    public static <T, R extends MutableShortCollection> R collectShort(ArrayList<T> list, ShortFunction<? super T> shortFunction, R target)
```

### JavadocReference
Cannot resolve symbol `ShortFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectShort(Iterable, ShortFunction, MutableShortCollection)
     */
    public static <T, R extends MutableShortCollection> R collectShort(ArrayList<T> list, ShortFunction<? super T> shortFunction, R target)
```

### JavadocReference
Cannot resolve symbol `MutableShortCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectShort(Iterable, ShortFunction, MutableShortCollection)
     */
    public static <T, R extends MutableShortCollection> R collectShort(ArrayList<T> list, ShortFunction<? super T> shortFunction, R target)
```

### JavadocReference
Cannot resolve symbol `BooleanFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectBoolean(Iterable, BooleanFunction)
     */
    public static <T> MutableBooleanList collectBoolean(
```

### JavadocReference
Cannot resolve symbol `FloatFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectFloat(Iterable, FloatFunction)
     */
    public static <T> MutableFloatList collectFloat(
```

### JavadocReference
Cannot resolve symbol `DoubleFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectDouble(Iterable, DoubleFunction)
     */
    public static <T> MutableDoubleList collectDouble(
```

### JavadocReference
Cannot resolve symbol `CharFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectChar(Iterable, CharFunction)
     */
    public static <T> MutableCharList collectChar(
```

### JavadocReference
Cannot resolve symbol `collectShort(Iterable, ShortFunction, MutableShortCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectShort(Iterable, ShortFunction, MutableShortCollection)
     */
    public static <T, R extends MutableShortCollection> R collectShort(
```

### JavadocReference
Cannot resolve symbol `ShortFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectShort(Iterable, ShortFunction, MutableShortCollection)
     */
    public static <T, R extends MutableShortCollection> R collectShort(
```

### JavadocReference
Cannot resolve symbol `MutableShortCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectShort(Iterable, ShortFunction, MutableShortCollection)
     */
    public static <T, R extends MutableShortCollection> R collectShort(
```

### JavadocReference
Cannot resolve symbol `ShortFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectShort(Iterable, ShortFunction)
     */
    public static <T> MutableShortList collectShort(
```

### JavadocReference
Cannot resolve symbol `DoubleFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#sumByDouble(Iterable, Function, DoubleFunction)
     * @since 6.0
     */
```

### JavadocReference
Cannot resolve symbol `FloatFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#sumOfFloat(Iterable, FloatFunction)
     * @since 6.0
     */
```

### JavadocReference
Cannot resolve symbol `FloatObjectToFloatFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#injectInto(float, Iterable, FloatObjectToFloatFunction)
     * @since 6.0
     */
```

### JavadocReference
Cannot resolve symbol `FloatFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectFloat(Iterable, FloatFunction)
     */
    public static <T> MutableFloatList collectFloat(T[] objectArray, FloatFunction<? super T> floatFunction)
```

### JavadocReference
Cannot resolve symbol `CharFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectChar(Iterable, CharFunction)
     */
    public static <T> MutableCharList collectChar(T[] objectArray, CharFunction<? super T> charFunction)
```

### JavadocReference
Cannot resolve symbol `collectByte(Iterable, ByteFunction, MutableByteCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectByte(Iterable, ByteFunction, MutableByteCollection)
     */
    public static <T, R extends MutableByteCollection> R collectByte(
```

### JavadocReference
Cannot resolve symbol `ByteFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectByte(Iterable, ByteFunction, MutableByteCollection)
     */
    public static <T, R extends MutableByteCollection> R collectByte(
```

### JavadocReference
Cannot resolve symbol `MutableByteCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectByte(Iterable, ByteFunction, MutableByteCollection)
     */
    public static <T, R extends MutableByteCollection> R collectByte(
```

### JavadocReference
Cannot resolve symbol `DoubleFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#sumOfDouble(Iterable, DoubleFunction)
     * @since 6.0
     */
```

### JavadocReference
Cannot resolve symbol `collectDouble(Iterable, DoubleFunction, MutableDoubleCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectDouble(Iterable, DoubleFunction, MutableDoubleCollection)
     */
    public static <T, R extends MutableDoubleCollection> R collectDouble(
```

### JavadocReference
Cannot resolve symbol `DoubleFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectDouble(Iterable, DoubleFunction, MutableDoubleCollection)
     */
    public static <T, R extends MutableDoubleCollection> R collectDouble(
```

### JavadocReference
Cannot resolve symbol `MutableDoubleCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectDouble(Iterable, DoubleFunction, MutableDoubleCollection)
     */
    public static <T, R extends MutableDoubleCollection> R collectDouble(
```

### JavadocReference
Cannot resolve symbol `LongFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#sumByLong(Iterable, Function, LongFunction)
     * @since 6.0
     */
```

### JavadocReference
Cannot resolve symbol `BooleanFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectBoolean(Iterable, BooleanFunction)
     */
    public static <T> MutableBooleanList collectBoolean(T[] objectArray, BooleanFunction<? super T> booleanFunction)
```

### JavadocReference
Cannot resolve symbol `IntFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectInt(Iterable, IntFunction)
     */
    public static <T> MutableIntList collectInt(
```

### JavadocReference
Cannot resolve symbol `collectFloat(Iterable, FloatFunction, MutableFloatCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectFloat(Iterable, FloatFunction, MutableFloatCollection)
     */
    public static <T, R extends MutableFloatCollection> R collectFloat(
```

### JavadocReference
Cannot resolve symbol `FloatFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectFloat(Iterable, FloatFunction, MutableFloatCollection)
     */
    public static <T, R extends MutableFloatCollection> R collectFloat(
```

### JavadocReference
Cannot resolve symbol `DoubleObjectToDoubleFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#injectInto(double, Iterable, DoubleObjectToDoubleFunction)
     */
    public static <T> double injectInto(
```

### JavadocReference
Cannot resolve symbol `MutableFloatCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectFloat(Iterable, FloatFunction, MutableFloatCollection)
     */
    public static <T, R extends MutableFloatCollection> R collectFloat(
```

### JavadocReference
Cannot resolve symbol `IntObjectToIntFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#injectInto(int, Iterable, IntObjectToIntFunction)
     */
    public static <T> int injectInto(
```

### JavadocReference
Cannot resolve symbol `ObjectIntProcedure`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#forEachWithIndex(Iterable, ObjectIntProcedure)
     */
    public static <T> void forEachWithIndex(T[] objectArray, ObjectIntProcedure<? super T> objectIntProcedure)
```

### JavadocReference
Cannot resolve symbol `collectBoolean(Iterable, BooleanFunction, MutableBooleanCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectBoolean(Iterable, BooleanFunction, MutableBooleanCollection)
     */
    public static <T, R extends MutableBooleanCollection> R collectBoolean(
```

### JavadocReference
Cannot resolve symbol `BooleanFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectBoolean(Iterable, BooleanFunction, MutableBooleanCollection)
     */
    public static <T, R extends MutableBooleanCollection> R collectBoolean(
```

### JavadocReference
Cannot resolve symbol `MutableBooleanCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectBoolean(Iterable, BooleanFunction, MutableBooleanCollection)
     */
    public static <T, R extends MutableBooleanCollection> R collectBoolean(
```

### JavadocReference
Cannot resolve symbol `collectChar(Iterable, CharFunction, MutableCharCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectChar(Iterable, CharFunction, MutableCharCollection)
     */
    public static <T, R extends MutableCharCollection> R collectChar(
```

### JavadocReference
Cannot resolve symbol `CharFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectChar(Iterable, CharFunction, MutableCharCollection)
     */
    public static <T, R extends MutableCharCollection> R collectChar(
```

### JavadocReference
Cannot resolve symbol `MutableCharCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectChar(Iterable, CharFunction, MutableCharCollection)
     */
    public static <T, R extends MutableCharCollection> R collectChar(
```

### JavadocReference
Cannot resolve symbol `LongFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectLong(Iterable, LongFunction)
     */
    public static <T> MutableLongList collectLong(
```

### JavadocReference
Cannot resolve symbol `FloatFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#sumByFloat(Iterable, Function, FloatFunction)
     * @since 6.0
     */
```

### JavadocReference
Cannot resolve symbol `DoubleObjectToDoubleFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#injectInto(double, Iterable, DoubleObjectToDoubleFunction)
     */
    public static <T> double injectInto(
```

### JavadocReference
Cannot resolve symbol `IntFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#sumOfInt(Iterable, IntFunction)
     * @since 6.0
     */
```

### JavadocReference
Cannot resolve symbol `DoubleFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectDouble(Iterable, DoubleFunction)
     */
    public static <T> MutableDoubleList collectDouble(T[] objectArray, DoubleFunction<? super T> doubleFunction)
```

### JavadocReference
Cannot resolve symbol `LongFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#sumOfLong(Iterable, LongFunction)
     * @since 6.0
     */
```

### JavadocReference
Cannot resolve symbol `LongObjectToLongFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#injectInto(long, Iterable, LongObjectToLongFunction)
     */
    public static <T> long injectInto(
```

### JavadocReference
Cannot resolve symbol `ByteFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectByte(Iterable, ByteFunction)
     */
    public static <T> MutableByteList collectByte(T[] objectArray, ByteFunction<? super T> byteFunction)
```

### JavadocReference
Cannot resolve symbol `IntFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#sumByInt(Iterable, Function, IntFunction)
     * @since 6.0
     */
```

### JavadocReference
Cannot resolve symbol `collectLong(Iterable, LongFunction, MutableLongCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectLong(Iterable, LongFunction, MutableLongCollection)
     */
    public static <T, R extends MutableLongCollection> R collectLong(
```

### JavadocReference
Cannot resolve symbol `LongFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectLong(Iterable, LongFunction, MutableLongCollection)
     */
    public static <T, R extends MutableLongCollection> R collectLong(
```

### JavadocReference
Cannot resolve symbol `MutableLongCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectLong(Iterable, LongFunction, MutableLongCollection)
     */
    public static <T, R extends MutableLongCollection> R collectLong(
```

### JavadocReference
Cannot resolve symbol `collectInt(Iterable, IntFunction, MutableIntCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectInt(Iterable, IntFunction, MutableIntCollection)
     */
    public static <T, R extends MutableIntCollection> R collectInt(
```

### JavadocReference
Cannot resolve symbol `IntFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectInt(Iterable, IntFunction, MutableIntCollection)
     */
    public static <T, R extends MutableIntCollection> R collectInt(
```

### JavadocReference
Cannot resolve symbol `MutableIntCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectInt(Iterable, IntFunction, MutableIntCollection)
     */
    public static <T, R extends MutableIntCollection> R collectInt(
```

### JavadocReference
Cannot resolve symbol `ShortFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/RandomAccessListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectShort(Iterable, ShortFunction)
     */
    public static <T> MutableShortList collectShort(
```

### JavadocReference
Cannot resolve symbol `FloatFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/RandomAccessListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectFloat(Iterable, FloatFunction)
     */
    public static <T, R extends MutableFloatCollection> R collectFloat(
```

### JavadocReference
Cannot resolve symbol `LongFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/RandomAccessListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectLong(Iterable, LongFunction)
     */
    public static <T> MutableLongList collectLong(
```

### JavadocReference
Cannot resolve symbol `IntFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/RandomAccessListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectInt(Iterable, IntFunction)
     */
    public static <T, R extends MutableIntCollection> R collectInt(
```

### JavadocReference
Cannot resolve symbol `ByteFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/RandomAccessListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectByte(Iterable, ByteFunction)
     */
    public static <T> MutableByteList collectByte(
```

### JavadocReference
Cannot resolve symbol `ByteFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/RandomAccessListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectByte(Iterable, ByteFunction)
     */
    public static <T, R extends MutableByteCollection> R collectByte(
```

### JavadocReference
Cannot resolve symbol `CharFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/RandomAccessListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectChar(Iterable, CharFunction)
     */
    public static <T, R extends MutableCharCollection> R collectChar(
```

### JavadocReference
Cannot resolve symbol `ShortFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/RandomAccessListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectShort(Iterable, ShortFunction)
     */
    public static <T, R extends MutableShortCollection> R collectShort(
```

### JavadocReference
Cannot resolve symbol `DoubleFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/RandomAccessListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectDouble(Iterable, DoubleFunction)
     */
    public static <T> MutableDoubleList collectDouble(
```

### JavadocReference
Cannot resolve symbol `DoubleFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/RandomAccessListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectDouble(Iterable, DoubleFunction)
     */
    public static <T, R extends MutableDoubleCollection> R collectDouble(
```

### JavadocReference
Cannot resolve symbol `FloatFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/RandomAccessListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectFloat(Iterable, FloatFunction)
     */
    public static <T> MutableFloatList collectFloat(
```

### JavadocReference
Cannot resolve symbol `BooleanFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/RandomAccessListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectBoolean(Iterable, BooleanFunction)
     */
    public static <T, R extends MutableBooleanCollection> R collectBoolean(
```

### JavadocReference
Cannot resolve symbol `CharFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/RandomAccessListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectChar(Iterable, CharFunction)
     */
    public static <T> MutableCharList collectChar(
```

### JavadocReference
Cannot resolve symbol `BooleanFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/RandomAccessListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectBoolean(Iterable, BooleanFunction)
     */
    public static <T> MutableBooleanList collectBoolean(
```

### JavadocReference
Cannot resolve symbol `LongFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/RandomAccessListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectLong(Iterable, LongFunction)
     */
    public static <T, R extends MutableLongCollection> R collectLong(
```

### JavadocReference
Cannot resolve symbol `IntFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/RandomAccessListIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectInt(Iterable, IntFunction)
     */
    public static <T> MutableIntList collectInt(
```

### JavadocReference
Cannot resolve symbol `IntFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IteratorIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectInt(Iterable, IntFunction)
     */
    public static <T> MutableIntCollection collectInt(
```

### JavadocReference
Cannot resolve symbol `FloatObjectToFloatFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IteratorIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#injectInto(float, Iterable, FloatObjectToFloatFunction)
     */
    public static <T> float injectInto(
```

### JavadocReference
Cannot resolve symbol `DoubleFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IteratorIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectDouble(Iterable, DoubleFunction)
     */
    public static <T> MutableDoubleCollection collectDouble(
```

### JavadocReference
Cannot resolve symbol `CharFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IteratorIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectChar(Iterable, CharFunction)
     */
    public static <T> MutableCharCollection collectChar(
```

### JavadocReference
Cannot resolve symbol `ShortFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IteratorIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectShort(Iterable, ShortFunction)
     */
    public static <T> MutableShortCollection collectShort(
```

### JavadocReference
Cannot resolve symbol `collectLong(Iterable, LongFunction, MutableLongCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IteratorIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectLong(Iterable, LongFunction, MutableLongCollection)
     */
    public static <T, R extends MutableLongCollection> R collectLong(
```

### JavadocReference
Cannot resolve symbol `LongFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IteratorIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectLong(Iterable, LongFunction, MutableLongCollection)
     */
    public static <T, R extends MutableLongCollection> R collectLong(
```

### JavadocReference
Cannot resolve symbol `MutableLongCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IteratorIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectLong(Iterable, LongFunction, MutableLongCollection)
     */
    public static <T, R extends MutableLongCollection> R collectLong(
```

### JavadocReference
Cannot resolve symbol `BooleanFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IteratorIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectBoolean(Iterable, BooleanFunction)
     */
    public static <T> MutableBooleanCollection collectBoolean(
```

### JavadocReference
Cannot resolve symbol `collectBoolean(Iterable, BooleanFunction, MutableBooleanCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IteratorIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectBoolean(Iterable, BooleanFunction, MutableBooleanCollection)
     */
    public static <T, R extends MutableBooleanCollection> R collectBoolean(
```

### JavadocReference
Cannot resolve symbol `BooleanFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IteratorIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectBoolean(Iterable, BooleanFunction, MutableBooleanCollection)
     */
    public static <T, R extends MutableBooleanCollection> R collectBoolean(
```

### JavadocReference
Cannot resolve symbol `MutableBooleanCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IteratorIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectBoolean(Iterable, BooleanFunction, MutableBooleanCollection)
     */
    public static <T, R extends MutableBooleanCollection> R collectBoolean(
```

### JavadocReference
Cannot resolve symbol `ByteFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IteratorIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectByte(Iterable, ByteFunction)
     */
    public static <T> MutableByteCollection collectByte(
```

### JavadocReference
Cannot resolve symbol `collectShort(Iterable, ShortFunction, MutableShortCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IteratorIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectShort(Iterable, ShortFunction, MutableShortCollection)
     */
    public static <T, R extends MutableShortCollection> R collectShort(
```

### JavadocReference
Cannot resolve symbol `ShortFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IteratorIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectShort(Iterable, ShortFunction, MutableShortCollection)
     */
    public static <T, R extends MutableShortCollection> R collectShort(
```

### JavadocReference
Cannot resolve symbol `MutableShortCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IteratorIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectShort(Iterable, ShortFunction, MutableShortCollection)
     */
    public static <T, R extends MutableShortCollection> R collectShort(
```

### JavadocReference
Cannot resolve symbol `collectDouble(Iterable, DoubleFunction, MutableDoubleCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IteratorIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectDouble(Iterable, DoubleFunction, MutableDoubleCollection)
     */
    public static <T, R extends MutableDoubleCollection> R collectDouble(
```

### JavadocReference
Cannot resolve symbol `DoubleFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IteratorIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectDouble(Iterable, DoubleFunction, MutableDoubleCollection)
     */
    public static <T, R extends MutableDoubleCollection> R collectDouble(
```

### JavadocReference
Cannot resolve symbol `MutableDoubleCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IteratorIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectDouble(Iterable, DoubleFunction, MutableDoubleCollection)
     */
    public static <T, R extends MutableDoubleCollection> R collectDouble(
```

### JavadocReference
Cannot resolve symbol `ObjectIntProcedure`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IteratorIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#forEachWithIndex(Iterable, ObjectIntProcedure)
     */
    public static <T> void forEachWithIndex(Iterator<T> iterator, ObjectIntProcedure<? super T> objectIntProcedure)
```

### JavadocReference
Cannot resolve symbol `DoubleObjectToDoubleFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IteratorIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#injectInto(double, Iterable, DoubleObjectToDoubleFunction)
     */
    public static <T> double injectInto(
```

### JavadocReference
Cannot resolve symbol `collectFloat(Iterable, FloatFunction, MutableFloatCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IteratorIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectFloat(Iterable, FloatFunction, MutableFloatCollection)
     */
    public static <T, R extends MutableFloatCollection> R collectFloat(
```

### JavadocReference
Cannot resolve symbol `FloatFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IteratorIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectFloat(Iterable, FloatFunction, MutableFloatCollection)
     */
    public static <T, R extends MutableFloatCollection> R collectFloat(
```

### JavadocReference
Cannot resolve symbol `MutableFloatCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IteratorIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectFloat(Iterable, FloatFunction, MutableFloatCollection)
     */
    public static <T, R extends MutableFloatCollection> R collectFloat(
```

### JavadocReference
Cannot resolve symbol `LongObjectToLongFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IteratorIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#injectInto(long, Iterable, LongObjectToLongFunction)
     */
    public static <T> long injectInto(
```

### JavadocReference
Cannot resolve symbol `LongFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IteratorIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectLong(Iterable, LongFunction)
     */
    public static <T> MutableLongCollection collectLong(
```

### JavadocReference
Cannot resolve symbol `IntObjectToIntFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IteratorIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#injectInto(int, Iterable, IntObjectToIntFunction)
     */
    public static <T> int injectInto(
```

### JavadocReference
Cannot resolve symbol `collectChar(Iterable, CharFunction, MutableCharCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IteratorIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectChar(Iterable, CharFunction, MutableCharCollection)
     */
    public static <T, R extends MutableCharCollection> R collectChar(
```

### JavadocReference
Cannot resolve symbol `CharFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IteratorIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectChar(Iterable, CharFunction, MutableCharCollection)
     */
    public static <T, R extends MutableCharCollection> R collectChar(
```

### JavadocReference
Cannot resolve symbol `MutableCharCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IteratorIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectChar(Iterable, CharFunction, MutableCharCollection)
     */
    public static <T, R extends MutableCharCollection> R collectChar(
```

### JavadocReference
Cannot resolve symbol `collectInt(Iterable, IntFunction, MutableIntCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IteratorIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectInt(Iterable, IntFunction, MutableIntCollection)
     */
    public static <T, R extends MutableIntCollection> R collectInt(
```

### JavadocReference
Cannot resolve symbol `IntFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IteratorIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectInt(Iterable, IntFunction, MutableIntCollection)
     */
    public static <T, R extends MutableIntCollection> R collectInt(
```

### JavadocReference
Cannot resolve symbol `MutableIntCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IteratorIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectInt(Iterable, IntFunction, MutableIntCollection)
     */
    public static <T, R extends MutableIntCollection> R collectInt(
```

### JavadocReference
Cannot resolve symbol `collectByte(Iterable, ByteFunction, MutableByteCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IteratorIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectByte(Iterable, ByteFunction, MutableByteCollection)
     */
    public static <T, R extends MutableByteCollection> R collectByte(
```

### JavadocReference
Cannot resolve symbol `ByteFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IteratorIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectByte(Iterable, ByteFunction, MutableByteCollection)
     */
    public static <T, R extends MutableByteCollection> R collectByte(
```

### JavadocReference
Cannot resolve symbol `MutableByteCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IteratorIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectByte(Iterable, ByteFunction, MutableByteCollection)
     */
    public static <T, R extends MutableByteCollection> R collectByte(
```

### JavadocReference
Cannot resolve symbol `FloatFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/IteratorIterate.java`
#### Snippet
```java

    /**
     * @see Iterate#collectFloat(Iterable, FloatFunction)
     */
    public static <T> MutableFloatCollection collectFloat(
```

### JavadocReference
Cannot resolve symbol `DoubleFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/Iterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#sumOfDouble(DoubleFunction)
     */
    public static <T> double sumOfDouble(Iterable<T> iterable, DoubleFunction<? super T> function)
```

### JavadocReference
Cannot resolve symbol `FloatFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/Iterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#sumOfFloat(FloatFunction)
     */
    public static <T, V> ObjectDoubleMap<V> sumByFloat(Iterable<T> iterable, Function<T, V> groupBy, FloatFunction<? super T> function)
```

### JavadocReference
Cannot resolve symbol `DoubleFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/Iterate.java`
#### Snippet
```java

    /**
     * Same as {@link #collectDouble(Iterable, DoubleFunction)}, except that the results are gathered into the specified {@code target}
     * collection.
     * <p>
```

### JavadocReference
Cannot resolve symbol `IntFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/Iterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#sumByInt(Function, IntFunction)
     */
    public static <T, V> ObjectLongMap<V> sumByInt(Iterable<T> iterable, Function<T, V> groupBy, IntFunction<? super T> function)
```

### JavadocReference
Cannot resolve symbol `LongObjectToLongFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/Iterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#injectInto(long, LongObjectToLongFunction)
     */
    public static <T> long injectInto(
```

### JavadocReference
Cannot resolve symbol `ShortFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/Iterate.java`
#### Snippet
```java

    /**
     * Same as {@link #collectShort(Iterable, ShortFunction)}, except that the results are gathered into the specified {@code target}
     * collection.
     * <p>
```

### JavadocReference
Cannot resolve symbol `FloatFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/Iterate.java`
#### Snippet
```java

    /**
     * Same as {@link #collectFloat(Iterable, FloatFunction)}, except that the results are gathered into the specified {@code target}
     * collection.
     * <p>
```

### JavadocReference
Cannot resolve symbol `IntFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/Iterate.java`
#### Snippet
```java

    /**
     * Same as {@link #collectInt(Iterable, IntFunction)}, except that the results are gathered into the specified {@code target}
     * collection.
     * <p>
```

### JavadocReference
Cannot resolve symbol `DoubleObjectToDoubleFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/Iterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#injectInto(double, DoubleObjectToDoubleFunction)
     */
    public static <T> double injectInto(
```

### JavadocReference
Cannot resolve symbol `LongFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/Iterate.java`
#### Snippet
```java

    /**
     * Same as {@link #collectLong(Iterable, LongFunction)}, except that the results are gathered into the specified {@code target}
     * collection.
     * <p>
```

### JavadocReference
Cannot resolve symbol `BooleanFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/Iterate.java`
#### Snippet
```java

    /**
     * Same as {@link #collectBoolean(Iterable, BooleanFunction)}, except that the results are gathered into the specified {@code target}
     * collection.
     * <p>
```

### JavadocReference
Cannot resolve symbol `DoubleFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/Iterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#sumOfDouble(DoubleFunction)
     */
    public static <T, V> ObjectDoubleMap<V> sumByDouble(Iterable<T> iterable, Function<T, V> groupBy, DoubleFunction<? super T> function)
```

### JavadocReference
Cannot resolve symbol `FloatFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/Iterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#sumOfFloat(FloatFunction)
     */
    public static <T> double sumOfFloat(Iterable<T> iterable, FloatFunction<? super T> function)
```

### JavadocReference
Cannot resolve symbol `CharFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/Iterate.java`
#### Snippet
```java

    /**
     * Same as {@link #collectChar(Iterable, CharFunction)}, except that the results are gathered into the specified {@code target}
     * collection.
     *
```

### JavadocReference
Cannot resolve symbol `ByteFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/Iterate.java`
#### Snippet
```java

    /**
     * Same as {@link #collectByte(Iterable, ByteFunction)}, except that the results are gathered into the specified {@code target}
     * collection.
     * <p>
```

### JavadocReference
Cannot resolve symbol `LongFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/Iterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#sumByLong(Function, LongFunction)
     */
    public static <T, V> ObjectLongMap<V> sumByLong(Iterable<T> iterable, Function<T, V> groupBy, LongFunction<? super T> function)
```

### JavadocReference
Cannot resolve symbol `IntFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/Iterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#sumOfInt(IntFunction)
     */
    public static <T> long sumOfInt(Iterable<T> iterable, IntFunction<? super T> function)
```

### JavadocReference
Cannot resolve symbol `FloatObjectToFloatFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/Iterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#injectInto(float, FloatObjectToFloatFunction)
     */
    public static <T> float injectInto(
```

### JavadocReference
Cannot resolve symbol `IntObjectToIntFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/Iterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#injectInto(int, IntObjectToIntFunction)
     */
    public static <T> int injectInto(
```

### JavadocReference
Cannot resolve symbol `LongFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/Iterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#sumOfLong(LongFunction)
     */
    public static <T> long sumOfLong(Iterable<T> iterable, LongFunction<? super T> function)
```

### JavadocReference
Symbol `org.eclipse.collections.impl.set.immutable.ImmutableUnifiedSet` is inaccessible from here
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/partition/set/package-info.java`
#### Snippet
```java
 *     <ul>
 *         <li>
 *             {@link org.eclipse.collections.impl.partition.set.PartitionImmutableSetImpl} - Result of splitting a {@link org.eclipse.collections.impl.set.immutable.ImmutableUnifiedSet} into two based on a Predicate.
 *         </li>
 *         <li>
```

### JavadocReference
Cannot resolve symbol `ObjectIntProcedure`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/parallel/ParallelIterate.java`
#### Snippet
```java
     * @param minForkSize Only run in parallel if input collection is longer than this.
     * @param taskCount   How many parallel tasks to submit to the executor.
     * @see #forEachWithIndex(Iterable, ObjectIntProcedure)
     */
    public static <T, BT extends ObjectIntProcedure<? super T>> void forEachWithIndex(
```

### JavadocReference
Cannot resolve symbol `ObjectIntProcedure`
in `eclipse-collections-forkjoin/src/main/java/org/eclipse/collections/impl/forkjoin/FJIterate.java`
#### Snippet
```java
     * @param minForkSize Only run in parallel if input collection is longer than this.
     * @param taskCount   The number of parallel tasks to submit to the executor.
     * @see #forEachWithIndex(Iterable, ObjectIntProcedure)
     */
    public static <T, PT extends ObjectIntProcedure<? super T>> void forEachWithIndex(
```

### JavadocReference
Cannot resolve symbol `collectInt(IntFunction, MutableIntCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collector/Collectors2.java`
#### Snippet
```java
     * Interval.oneTo(10).reduceInPlace(Collectors2.collectInt(each -> each, IntLists.mutable::empty));}<br>
     * <p>
     * Equivalent to using @{@link RichIterable#collectInt(IntFunction, MutableIntCollection)}
     * </p>
     * {@code MutableIntList collect =
```

### JavadocReference
Cannot resolve symbol `IntFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collector/Collectors2.java`
#### Snippet
```java
     * Interval.oneTo(10).reduceInPlace(Collectors2.collectInt(each -> each, IntLists.mutable::empty));}<br>
     * <p>
     * Equivalent to using @{@link RichIterable#collectInt(IntFunction, MutableIntCollection)}
     * </p>
     * {@code MutableIntList collect =
```

### JavadocReference
Cannot resolve symbol `MutableIntCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collector/Collectors2.java`
#### Snippet
```java
     * Interval.oneTo(10).reduceInPlace(Collectors2.collectInt(each -> each, IntLists.mutable::empty));}<br>
     * <p>
     * Equivalent to using @{@link RichIterable#collectInt(IntFunction, MutableIntCollection)}
     * </p>
     * {@code MutableIntList collect =
```

### JavadocReference
Cannot resolve symbol `collectBoolean(BooleanFunction, MutableBooleanCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collector/Collectors2.java`
#### Snippet
```java
     * Interval.oneTo(10).reduceInPlace(Collectors2.collectBoolean(each -> each % 2 == 0, BooleanLists.mutable::empty));}<br>
     * <p>
     * Equivalent to using @{@link RichIterable#collectBoolean(BooleanFunction, MutableBooleanCollection)}
     * </p>
     * {@code MutableBooleanList collect =
```

### JavadocReference
Cannot resolve symbol `BooleanFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collector/Collectors2.java`
#### Snippet
```java
     * Interval.oneTo(10).reduceInPlace(Collectors2.collectBoolean(each -> each % 2 == 0, BooleanLists.mutable::empty));}<br>
     * <p>
     * Equivalent to using @{@link RichIterable#collectBoolean(BooleanFunction, MutableBooleanCollection)}
     * </p>
     * {@code MutableBooleanList collect =
```

### JavadocReference
Cannot resolve symbol `MutableBooleanCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collector/Collectors2.java`
#### Snippet
```java
     * Interval.oneTo(10).reduceInPlace(Collectors2.collectBoolean(each -> each % 2 == 0, BooleanLists.mutable::empty));}<br>
     * <p>
     * Equivalent to using @{@link RichIterable#collectBoolean(BooleanFunction, MutableBooleanCollection)}
     * </p>
     * {@code MutableBooleanList collect =
```

### JavadocReference
Cannot resolve symbol `DoubleFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collector/Collectors2.java`
#### Snippet
```java
     * Interval.oneTo(10).reduceInPlace(Collectors2.sumByDouble(each -> ((int)each % 2), Integer::doubleValue));}<br>
     * <p>
     * Equivalent to using @{@link RichIterable#sumByDouble(Function, DoubleFunction)}
     * </p>
     * {@code ObjectDoubleMap<Integer> sumBy =
```

### JavadocReference
Cannot resolve symbol `collectLong(LongFunction, MutableLongCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collector/Collectors2.java`
#### Snippet
```java
     * Interval.oneTo(10).reduceInPlace(Collectors2.collectLong(each -> (long) each, LongLists.mutable::empty));}<br>
     * <p>
     * Equivalent to using @{@link RichIterable#collectLong(LongFunction, MutableLongCollection)}
     * </p>
     * {@code MutableLongList collect =
```

### JavadocReference
Cannot resolve symbol `LongFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collector/Collectors2.java`
#### Snippet
```java
     * Interval.oneTo(10).reduceInPlace(Collectors2.collectLong(each -> (long) each, LongLists.mutable::empty));}<br>
     * <p>
     * Equivalent to using @{@link RichIterable#collectLong(LongFunction, MutableLongCollection)}
     * </p>
     * {@code MutableLongList collect =
```

### JavadocReference
Cannot resolve symbol `MutableLongCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collector/Collectors2.java`
#### Snippet
```java
     * Interval.oneTo(10).reduceInPlace(Collectors2.collectLong(each -> (long) each, LongLists.mutable::empty));}<br>
     * <p>
     * Equivalent to using @{@link RichIterable#collectLong(LongFunction, MutableLongCollection)}
     * </p>
     * {@code MutableLongList collect =
```

### JavadocReference
Cannot resolve symbol `collectShort(ShortFunction, MutableShortCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collector/Collectors2.java`
#### Snippet
```java
     * Interval.oneTo(10).reduceInPlace(Collectors2.collectShort(each -> (short) (each % Short.MAX_VALUE), ShortLists.mutable::empty));}<br>
     * <p>
     * Equivalent to using @{@link RichIterable#collectShort(ShortFunction, MutableShortCollection)}
     * </p>
     * {@code MutableShortList collect =
```

### JavadocReference
Cannot resolve symbol `ShortFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collector/Collectors2.java`
#### Snippet
```java
     * Interval.oneTo(10).reduceInPlace(Collectors2.collectShort(each -> (short) (each % Short.MAX_VALUE), ShortLists.mutable::empty));}<br>
     * <p>
     * Equivalent to using @{@link RichIterable#collectShort(ShortFunction, MutableShortCollection)}
     * </p>
     * {@code MutableShortList collect =
```

### JavadocReference
Cannot resolve symbol `MutableShortCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collector/Collectors2.java`
#### Snippet
```java
     * Interval.oneTo(10).reduceInPlace(Collectors2.collectShort(each -> (short) (each % Short.MAX_VALUE), ShortLists.mutable::empty));}<br>
     * <p>
     * Equivalent to using @{@link RichIterable#collectShort(ShortFunction, MutableShortCollection)}
     * </p>
     * {@code MutableShortList collect =
```

### JavadocReference
Cannot resolve symbol `collectByte(ByteFunction, MutableByteCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collector/Collectors2.java`
#### Snippet
```java
     * Interval.oneTo(10).reduceInPlace(Collectors2.collectByte(each -> (byte) (each % Byte.MAX_VALUE), ByteLists.mutable::empty));}<br>
     * <p>
     * Equivalent to using @{@link RichIterable#collectByte(ByteFunction, MutableByteCollection)}
     * </p>
     * {@code MutableByteList collect =
```

### JavadocReference
Cannot resolve symbol `ByteFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collector/Collectors2.java`
#### Snippet
```java
     * Interval.oneTo(10).reduceInPlace(Collectors2.collectByte(each -> (byte) (each % Byte.MAX_VALUE), ByteLists.mutable::empty));}<br>
     * <p>
     * Equivalent to using @{@link RichIterable#collectByte(ByteFunction, MutableByteCollection)}
     * </p>
     * {@code MutableByteList collect =
```

### JavadocReference
Cannot resolve symbol `MutableByteCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collector/Collectors2.java`
#### Snippet
```java
     * Interval.oneTo(10).reduceInPlace(Collectors2.collectByte(each -> (byte) (each % Byte.MAX_VALUE), ByteLists.mutable::empty));}<br>
     * <p>
     * Equivalent to using @{@link RichIterable#collectByte(ByteFunction, MutableByteCollection)}
     * </p>
     * {@code MutableByteList collect =
```

### JavadocReference
Cannot resolve symbol `collectChar(CharFunction, MutableCharCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collector/Collectors2.java`
#### Snippet
```java
     * Interval.oneTo(10).reduceInPlace(Collectors2.collectChar(each -> (char) (each % Character.MAX_VALUE), CharLists.mutable::empty));}<br>
     * <p>
     * Equivalent to using @{@link RichIterable#collectChar(CharFunction, MutableCharCollection)}
     * </p>
     * {@code MutableCharList collect =
```

### JavadocReference
Cannot resolve symbol `CharFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collector/Collectors2.java`
#### Snippet
```java
     * Interval.oneTo(10).reduceInPlace(Collectors2.collectChar(each -> (char) (each % Character.MAX_VALUE), CharLists.mutable::empty));}<br>
     * <p>
     * Equivalent to using @{@link RichIterable#collectChar(CharFunction, MutableCharCollection)}
     * </p>
     * {@code MutableCharList collect =
```

### JavadocReference
Cannot resolve symbol `MutableCharCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collector/Collectors2.java`
#### Snippet
```java
     * Interval.oneTo(10).reduceInPlace(Collectors2.collectChar(each -> (char) (each % Character.MAX_VALUE), CharLists.mutable::empty));}<br>
     * <p>
     * Equivalent to using @{@link RichIterable#collectChar(CharFunction, MutableCharCollection)}
     * </p>
     * {@code MutableCharList collect =
```

### JavadocReference
Cannot resolve symbol `FloatFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collector/Collectors2.java`
#### Snippet
```java
     * Interval.oneTo(10).reduceInPlace(Collectors2.sumByFloat(each -> ((int)each % 2), Integer::floatValue));}<br>
     * <p>
     * Equivalent to using @{@link RichIterable#sumByFloat(Function, FloatFunction)}
     * </p>
     * {@code ObjectDoubleMap<Integer> sumBy =
```

### JavadocReference
Cannot resolve symbol `IntFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collector/Collectors2.java`
#### Snippet
```java
     * Interval.oneTo(10).reduceInPlace(Collectors2.sumByInt(each -> Integer.valueOf(each % 2), Integer::intValue));}<br>
     * <p>
     * Equivalent to using @{@link RichIterable#sumByInt(Function, IntFunction)}
     * </p>
     * {@code ObjectLongMap<Integer> sumBy =
```

### JavadocReference
Cannot resolve symbol `collectDouble(DoubleFunction, MutableDoubleCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collector/Collectors2.java`
#### Snippet
```java
     * Interval.oneTo(10).reduceInPlace(Collectors2.collectDouble(each -> (double) each, DoubleLists.mutable::empty));}<br>
     * <p>
     * Equivalent to using @{@link RichIterable#collectDouble(DoubleFunction, MutableDoubleCollection)}
     * </p>
     * {@code MutableDoubleList collect =
```

### JavadocReference
Cannot resolve symbol `DoubleFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collector/Collectors2.java`
#### Snippet
```java
     * Interval.oneTo(10).reduceInPlace(Collectors2.collectDouble(each -> (double) each, DoubleLists.mutable::empty));}<br>
     * <p>
     * Equivalent to using @{@link RichIterable#collectDouble(DoubleFunction, MutableDoubleCollection)}
     * </p>
     * {@code MutableDoubleList collect =
```

### JavadocReference
Cannot resolve symbol `MutableDoubleCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collector/Collectors2.java`
#### Snippet
```java
     * Interval.oneTo(10).reduceInPlace(Collectors2.collectDouble(each -> (double) each, DoubleLists.mutable::empty));}<br>
     * <p>
     * Equivalent to using @{@link RichIterable#collectDouble(DoubleFunction, MutableDoubleCollection)}
     * </p>
     * {@code MutableDoubleList collect =
```

### JavadocReference
Cannot resolve symbol `collectFloat(FloatFunction, MutableFloatCollection)`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collector/Collectors2.java`
#### Snippet
```java
     * Interval.oneTo(10).reduceInPlace(Collectors2.collectFloat(each -> (float) each, FloatLists.mutable::empty));}<br>
     * <p>
     * Equivalent to using @{@link RichIterable#collectFloat(FloatFunction, MutableFloatCollection)}
     * </p>
     * {@code MutableFloatList collect =
```

### JavadocReference
Cannot resolve symbol `FloatFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collector/Collectors2.java`
#### Snippet
```java
     * Interval.oneTo(10).reduceInPlace(Collectors2.collectFloat(each -> (float) each, FloatLists.mutable::empty));}<br>
     * <p>
     * Equivalent to using @{@link RichIterable#collectFloat(FloatFunction, MutableFloatCollection)}
     * </p>
     * {@code MutableFloatList collect =
```

### JavadocReference
Cannot resolve symbol `MutableFloatCollection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collector/Collectors2.java`
#### Snippet
```java
     * Interval.oneTo(10).reduceInPlace(Collectors2.collectFloat(each -> (float) each, FloatLists.mutable::empty));}<br>
     * <p>
     * Equivalent to using @{@link RichIterable#collectFloat(FloatFunction, MutableFloatCollection)}
     * </p>
     * {@code MutableFloatList collect =
```

### JavadocReference
Cannot resolve symbol `LongFunction`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collector/Collectors2.java`
#### Snippet
```java
     * Interval.oneTo(10).reduceInPlace(Collectors2.sumByLong(each -> Long.valueOf(each % 2), Integer::longValue));}<br>
     * <p>
     * Equivalent to using @{@link RichIterable#sumByLong(Function, LongFunction)}
     * </p>
     * {@code ObjectLongMap<Long> sumBy =
```

## RuleId[id=ManualArrayToCollectionCopy]
### ManualArrayToCollectionCopy
Manual array to collection copy
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/immutable/ImmutableTreeSet.java`
#### Snippet
```java
        MutableSortedSet<T> output = SortedSets.mutable.of(this.comparator());

        for (int i = 0; i < count; i++)
        {
            output.add(this.delegate[i]);
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayListIterate.java`
#### Snippet
```java
            T[] elements = ArrayListIterate.getInternalArray(list);

            for (int i = count; i < size; i++)
            {
                targetList.add(elements[i]);
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayIterate.java`
#### Snippet
```java
            throw new IllegalArgumentException("Count must be greater than zero, but was: " + count);
        }
        for (int i = count; i < array.length; i++)
        {
            target.add(array[i]);
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayIterate.java`
#### Snippet
```java
        }
        int end = Math.min(array.length, count);
        for (int i = 0; i < end; i++)
        {
            target.add(array[i]);
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayListIterate.java`
#### Snippet
```java
                    MutableList<T> rejected = result.getRejected();
                    rejected.add(each);
                    for (int j = i + 1; j < size; j++)
                    {
                        rejected.add(elements[j]);
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayListIterate.java`
#### Snippet
```java
                {
                    result.add(element);
                    for (int j = i + 1; j < size; j++)
                    {
                        T eachNotDropped = elements[j];
```

### ManualArrayToCollectionCopy
Manual array to collection copy
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayListIterate.java`
#### Snippet
```java

            int end = Math.min(size, count);
            for (int i = 0; i < end; i++)
            {
                targetList.add(elements[i]);
```

## RuleId[id=SuspiciousSystemArraycopy]
### SuspiciousSystemArraycopy
Source parameter type 'T\[\]' is not assignable to destination parameter `array1` of type 'E\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/sorted/immutable/ImmutableSortedBagImpl.java`
#### Snippet
```java
        }
        T[] items = (T[]) this.toArray();
        System.arraycopy(items, 0, array1, 0, this.size);
        if (array1.length > this.size)
        {
```

### SuspiciousSystemArraycopy
Source parameter type 'K\[\]' is not assignable to destination parameter `result` of type 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/sorted/immutable/ImmutableTreeMap.java`
#### Snippet
```java
                    : a;

            System.arraycopy(ImmutableTreeMap.this.keys, 0, result, 0, ImmutableTreeMap.this.keys.length);

            if (result.length > this.size())
```

### SuspiciousSystemArraycopy
Source parameter type 'T\[\]' is not assignable to destination parameter `result` of type 'E\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/AbstractArrayAdapter.java`
#### Snippet
```java
                : array;

        System.arraycopy(this.items, 0, result, 0, size);
        if (result.length > size)
        {
```

### SuspiciousSystemArraycopy
`this.items` is not of an array type
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/primitive/BooleanArrayList.java`
#### Snippet
```java
            target = new boolean[this.size];
        }
        System.arraycopy(this.items, 0, target, 0, this.size);
        return target;
    }
```

### SuspiciousSystemArraycopy
Source parameter type 'T\[\]' is not assignable to destination parameter `a` of type 'E\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/immutable/ImmutableArrayList.java`
#### Snippet
```java
            a = (E[]) Array.newInstance(a.getClass().getComponentType(), size);
        }
        System.arraycopy(this.items, 0, a, 0, size);
        if (a.length > size)
        {
```

### SuspiciousSystemArraycopy
Source parameter type 'T\[\]' is not assignable to destination parameter `array` of type 'E\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/FastList.java`
#### Snippet
```java
    public <E> E[] toArray(E[] array, int sourceFromIndex, int sourceToIndex, int destinationIndex)
    {
        System.arraycopy(this.items, sourceFromIndex, array, destinationIndex, sourceToIndex - sourceFromIndex + 1);
        return array;
    }
```

### SuspiciousSystemArraycopy
Source parameter type 'java.lang.Object\[\]' is not assignable to destination parameter `this.items` of type 'T\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/FastList.java`
#### Snippet
```java
        this.shiftElementsAtIndex(index, sourceSize);
        this.size = newSize;
        System.arraycopy(newItems, 0, this.items, index, sourceSize);
    }

```

### SuspiciousSystemArraycopy
Source parameter type 'T\[\]' is not assignable to destination parameter `array` of type 'E\[\]'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/FastList.java`
#### Snippet
```java
            array = (E[]) Array.newInstance(array.getClass().getComponentType(), this.size);
        }
        System.arraycopy(this.items, 0, array, 0, this.size);
        if (array.length > this.size)
        {
```

## RuleId[id=DataFlowIssue]
### DataFlowIssue
Unboxing of `this.map.get(1)` may produce `NullPointerException`
in `jcstress-tests/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapEntrySetRemoveIfTest.java`
#### Snippet
```java
    public void after(Z_Result r)
    {
        r.r1 = this.map.containsKey(1) && this.map.get(1);
    }
}
```

### DataFlowIssue
Unboxing of `this.map.get(1)` may produce `NullPointerException`
in `jcstress-tests/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapValuesRemoveIfTest.java`
#### Snippet
```java
    public void after(Z_Result r)
    {
        r.r1 = this.map.containsKey(1) && this.map.get(1);
    }
}
```

### DataFlowIssue
Array index is out of bounds
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/AbstractRichIterable.java`
#### Snippet
```java
                : array;

        this.forEachWithIndex((each, index) -> result[index] = (E) each);
        if (result.length > size)
        {
```

### DataFlowIssue
Array index is out of bounds
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/AbstractRichIterable.java`
#### Snippet
```java
    {
        Object[] result = new Object[this.size()];
        this.forEachWithIndex((each, index) -> result[index] = each);
        return result;
    }
```

### DataFlowIssue
Method invocation `toSet` will produce `NullPointerException`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/mutable/primitive/BooleanHashBag.java`
#### Snippet
```java
    {
        int oldSize = this.size();
        BooleanSet set = elements instanceof BooleanSet ? (BooleanSet) elements : elements.toSet();
        if (!set.contains(true) && this.containsTrue())
        {
```

### DataFlowIssue
Method invocation `toSet` will produce `NullPointerException`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/primitive/ObjectBooleanHashMapWithHashingStrategy.java`
#### Snippet
```java
        {
            int oldSize = ObjectBooleanHashMapWithHashingStrategy.this.size();
            BooleanSet sourceSet = source instanceof BooleanSet ? (BooleanSet) source : source.toSet();
            ObjectBooleanHashMapWithHashingStrategy<K> retained = ObjectBooleanHashMapWithHashingStrategy.this.select((object, value) -> sourceSet.contains(value));
            if (retained.size() != oldSize)
```

### DataFlowIssue
Method invocation `toSet` will produce `NullPointerException`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/primitive/ObjectBooleanHashMap.java`
#### Snippet
```java
        {
            int oldSize = ObjectBooleanHashMap.this.size();
            BooleanSet sourceSet = source instanceof BooleanSet ? (BooleanSet) source : source.toSet();
            ObjectBooleanHashMap<K> retained = ObjectBooleanHashMap.this.select((object, value) -> sourceSet.contains(value));
            if (retained.size() != oldSize)
```

### DataFlowIssue
Argument `this.nonSentinel(chainedDetect)` might be null
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
                if (chainedDetect != null)
                {
                    return Optional.of(this.nonSentinel(chainedDetect));
                }
            }
```

### DataFlowIssue
Argument `each` might be null
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
                if (predicate.accept(each))
                {
                    return Optional.of(each);
                }
            }
```

### DataFlowIssue
Method invocation `toArray` will produce `NullPointerException`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/primitive/BooleanHashSet.java`
#### Snippet
```java
        }

        return BooleanHashSet.newSetWith(source.toArray());
    }

```

### DataFlowIssue
Method invocation `toArray` will produce `NullPointerException`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/primitive/ByteHashSet.java`
#### Snippet
```java
        }

        return ByteHashSet.newSetWith(source.toArray());
    }

```

### DataFlowIssue
Method invocation `toSet` will produce `NullPointerException`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/primitive/ByteHashSet.java`
#### Snippet
```java
    {
        int oldSize = this.size();
        ByteSet sourceSet = source instanceof ByteSet ? (ByteSet) source : source.toSet();

        ByteHashSet retained = this.select(sourceSet::contains);
```

### DataFlowIssue
Method invocation `toArray` will produce `NullPointerException`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/immutable/primitive/ImmutableBooleanSetFactoryImpl.java`
#### Snippet
```java
            return (ImmutableBooleanSet) items;
        }
        return this.with(items.toArray());
    }

```

### DataFlowIssue
Array index is out of bounds
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/lazy/TakeIterable.java`
#### Snippet
```java
    {
        Object[] result = new Object[this.count];
        this.forEachWithIndex((each, index) -> result[index] = each);
        return result;
    }
```

### DataFlowIssue
Argument `this.nonSentinel(chainedDetect)` might be null
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/strategy/mutable/UnifiedSetWithHashingStrategy.java`
#### Snippet
```java
                if (chainedDetect != null)
                {
                    return Optional.of(this.nonSentinel(chainedDetect));
                }
            }
```

### DataFlowIssue
Argument `each` might be null
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/strategy/mutable/UnifiedSetWithHashingStrategy.java`
#### Snippet
```java
                if (predicate.accept(each))
                {
                    return Optional.of(each);
                }
            }
```

### DataFlowIssue
Array index is out of bounds
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/lazy/primitive/CollectBooleanIterable.java`
#### Snippet
```java
    {
        boolean[] array = new boolean[this.size()];
        this.iterable.forEachWithIndex((each, index) -> array[index] = this.function.booleanValueOf(each));
        return array;
    }
```

### DataFlowIssue
Array index is out of bounds
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/Interval.java`
#### Snippet
```java
    {
        Integer[] result = new Integer[this.size()];
        this.forEachWithIndex((ObjectIntProcedure<Integer>) (each, index) -> result[index] = each);
        return result;
    }
```

### DataFlowIssue
Array index is out of bounds
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/Interval.java`
#### Snippet
```java
    {
        int[] result = new int[this.size()];
        this.forEachWithIndex((IntIntProcedure) (each, index) -> result[index] = each);
        return result;
    }
```

### DataFlowIssue
Array index is out of bounds
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/CompositeFastList.java`
#### Snippet
```java
                : (Object[]) Array.newInstance(array.getClass().getComponentType(), size);

        this.forEachWithIndex((each, index) -> result[index] = each);

        if (result.length > size)
```

### DataFlowIssue
Array index is out of bounds
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/CompositeFastList.java`
#### Snippet
```java
    {
        Object[] result = new Object[this.size()];
        this.forEachWithIndex((each, index) -> result[index] = each);
        return result;
    }
```

### DataFlowIssue
Method invocation `toSet` will produce `NullPointerException`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/primitive/BooleanArrayList.java`
#### Snippet
```java
    {
        int oldSize = this.size();
        BooleanSet sourceSet = source instanceof BooleanSet ? (BooleanSet) source : source.toSet();
        BooleanArrayList retained = this.select(sourceSet::contains);

```

### DataFlowIssue
Array index is out of bounds
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/primitive/LongInterval.java`
#### Snippet
```java
    {
        long[] result = new long[this.size()];
        this.forEachWithIndex((each, index) -> result[index] = each);
        return result;
    }
```

### DataFlowIssue
Array index is out of bounds
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/primitive/LongInterval.java`
#### Snippet
```java
        }
        long[] finalBypass = result;
        this.forEachWithIndex((each, index) -> finalBypass[index] = each);
        return result;
    }
```

### DataFlowIssue
Array index is out of bounds
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/primitive/IntInterval.java`
#### Snippet
```java
        }
        int[] finalBypass = result;
        this.forEachWithIndex((each, index) -> finalBypass[index] = each);
        return result;
    }
```

### DataFlowIssue
Array index is out of bounds
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/primitive/IntInterval.java`
#### Snippet
```java
    {
        int[] result = new int[this.size()];
        this.forEachWithIndex((each, index) -> result[index] = each);
        return result;
    }
```

### DataFlowIssue
Condition `function instanceof BooleanFunction` is redundant and can be replaced with a null check
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Comparators.java`
#### Snippet
```java
    private static <T, V extends Comparable<? super V>> SerializableComparator<T> getPrimitiveFunctionComparator(Function<? super T, ? extends V> function)
    {
        if (function instanceof BooleanFunction)
        {
            return Functions.toBooleanComparator((BooleanFunction<T>) function);
```

### DataFlowIssue
Method invocation `toArray` will produce `NullPointerException`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/string/immutable/CodePointList.java`
#### Snippet
```java
            return new CodePointList((ImmutableIntList) iterable);
        }
        return new CodePointList(iterable.toArray());
    }

```

### DataFlowIssue
Method invocation `makeString` will produce `NullPointerException`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/string/immutable/CharAdapter.java`
#### Snippet
```java
            return new CharAdapter(iterable.toString());
        }
        return new CharAdapter(iterable.makeString(""));
    }

```

### DataFlowIssue
Method invocation `injectInto` will produce `NullPointerException`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/string/immutable/CodePointAdapter.java`
#### Snippet
```java
            return new CodePointAdapter(iterable.toString());
        }
        StringBuilder builder = iterable.injectInto(new StringBuilder(), StringBuilder::appendCodePoint);
        return new CodePointAdapter(builder.toString());
    }
```

### DataFlowIssue
Result of 'min' is the same as the second argument making the call meaningless
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/RandomAccessListIterate.java`
#### Snippet
```java
            return targetList;
        }
        int start = Math.min(list.size(), count);
        targetList.addAll(list.subList(start, list.size()));
        return targetList;
```

## RuleId[id=SimplifyStreamApiCallChains]
### SimplifyStreamApiCallChains
'collect(maxBy())' can be replaced with 'max()' (may change semantics)
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MaxByIntTest.java`
#### Snippet
```java
    public Position maxByQuantity_parallel_lazy_collect_methodref_jdk()
    {
        return this.positions.getJdkPositions().parallelStream().collect(
                Collectors.maxBy(QUANTITY_COMPARATOR_METHODREF)).get();
    }

```

### SimplifyStreamApiCallChains
'collect(maxBy())' can be replaced with 'max()' (may change semantics)
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MaxByIntTest.java`
#### Snippet
```java
    public Position maxByQuantity_serial_lazy_collect_lambda_jdk()
    {
        return this.positions.getJdkPositions().stream().collect(
                Collectors.maxBy(QUANTITY_COMPARATOR_LAMBDA)).get();
    }

```

### SimplifyStreamApiCallChains
'collect(maxBy())' can be replaced with 'max()' (may change semantics)
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MaxByIntTest.java`
#### Snippet
```java
    public Position maxByQuantity_parallel_lazy_collect_lambda_jdk()
    {
        return this.positions.getJdkPositions().parallelStream().collect(
                Collectors.maxBy(QUANTITY_COMPARATOR_LAMBDA)).get();
    }

```

### SimplifyStreamApiCallChains
'collect(maxBy())' can be replaced with 'max()' (may change semantics)
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MaxByIntTest.java`
#### Snippet
```java
    public Position maxByQuantity_serial_lazy_collect_methodref_jdk()
    {
        return this.positions.getJdkPositions().stream().collect(
                Collectors.maxBy(QUANTITY_COMPARATOR_METHODREF)).get();
    }

```

### SimplifyStreamApiCallChains
Can be replaced with '.values().stream()'
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AnagramSetTest.java`
#### Snippet
```java
        groupBy.entrySet()
                .stream()
                .map(Map.Entry::getValue)
                .filter(list -> list.size() >= SIZE_THRESHOLD)
                .sorted(Comparator.<Set<String>>comparingInt(Set::size).reversed())
```

### SimplifyStreamApiCallChains
Can be replaced with '.values().stream()'
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AnagramSetTest.java`
#### Snippet
```java
        groupBy.entrySet()
                .stream()
                .map(Map.Entry::getValue)
                .filter(list -> list.size() >= SIZE_THRESHOLD)
                .sorted(Comparator.<Set<String>>comparingInt(Set::size).reversed())
```

### SimplifyStreamApiCallChains
'collect(minBy())' can be replaced with 'min()' (may change semantics)
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MinByDoubleTest.java`
#### Snippet
```java
    public Position minByMarketValue_serial_lazy_collect_methodref_jdk()
    {
        return this.positions.getJdkPositions().stream().collect(
                Collectors.minBy(MARKET_VALUE_COMPARATOR_METHODREF)).get();
    }

```

### SimplifyStreamApiCallChains
'collect(minBy())' can be replaced with 'min()' (may change semantics)
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MinByDoubleTest.java`
#### Snippet
```java
    public Position minByMarketValue_serial_lazy_collect_lambda_jdk()
    {
        return this.positions.getJdkPositions().stream().collect(
                Collectors.minBy(MARKET_VALUE_COMPARATOR_LAMBDA)).get();
    }

```

### SimplifyStreamApiCallChains
'collect(minBy())' can be replaced with 'min()' (may change semantics)
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MinByDoubleTest.java`
#### Snippet
```java
    public Position minByMarketValue_parallel_lazy_collect_lambda_jdk()
    {
        return this.positions.getJdkPositions().parallelStream().collect(
                Collectors.minBy(MARKET_VALUE_COMPARATOR_LAMBDA)).get();
    }

```

### SimplifyStreamApiCallChains
'collect(minBy())' can be replaced with 'min()' (may change semantics)
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MinByDoubleTest.java`
#### Snippet
```java
    public Position minByMarketValue_parallel_lazy_collect_methodref_jdk()
    {
        return this.positions.getJdkPositions().parallelStream().collect(
                Collectors.minBy(MARKET_VALUE_COMPARATOR_METHODREF)).get();
    }

```

### SimplifyStreamApiCallChains
Can be replaced with '.values().stream()'
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AnagramListTest.java`
#### Snippet
```java
        groupBy.entrySet()
                .stream()
                .map(Map.Entry::getValue)
                .filter(list -> list.size() >= SIZE_THRESHOLD)
                .sorted(Comparator.<List<String>>comparingInt(List::size).reversed())
```

### SimplifyStreamApiCallChains
Can be replaced with '.values().stream()'
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AnagramListTest.java`
#### Snippet
```java
        groupBy.entrySet()
                .stream()
                .map(Map.Entry::getValue)
                .filter(list -> list.size() >= SIZE_THRESHOLD)
                .sorted(Comparator.<List<String>>comparingInt(List::size).reversed())
```

### SimplifyStreamApiCallChains
'collect(minBy())' can be replaced with 'min()' (may change semantics)
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MinByIntTest.java`
#### Snippet
```java
    public Position minByQuantity_parallel_lazy_collect_lambda_jdk()
    {
        return this.positions.getJdkPositions().parallelStream().collect(
                Collectors.minBy(QUANTITY_COMPARATOR_LAMBDA)).get();
    }

```

### SimplifyStreamApiCallChains
'collect(minBy())' can be replaced with 'min()' (may change semantics)
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MinByIntTest.java`
#### Snippet
```java
    public Position minByQuantity_serial_lazy_collect_methodref_jdk()
    {
        return this.positions.getJdkPositions().stream().collect(
                Collectors.minBy(QUANTITY_COMPARATOR_METHODREF)).get();
    }

```

### SimplifyStreamApiCallChains
'collect(minBy())' can be replaced with 'min()' (may change semantics)
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MinByIntTest.java`
#### Snippet
```java
    public Position minByQuantity_serial_lazy_collect_lambda_jdk()
    {
        return this.positions.getJdkPositions().stream().collect(
                Collectors.minBy(QUANTITY_COMPARATOR_LAMBDA)).get();
    }

```

### SimplifyStreamApiCallChains
'collect(minBy())' can be replaced with 'min()' (may change semantics)
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MinByIntTest.java`
#### Snippet
```java
    public Position minByQuantity_parallel_lazy_collect_methodref_jdk()
    {
        return this.positions.getJdkPositions().parallelStream().collect(
                Collectors.minBy(QUANTITY_COMPARATOR_METHODREF)).get();
    }

```

### SimplifyStreamApiCallChains
'collect(maxBy())' can be replaced with 'max()' (may change semantics)
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MaxByDoubleTest.java`
#### Snippet
```java
    public Position maxByMarketValue_serial_lazy_collect_lambda_jdk()
    {
        return this.positions.getJdkPositions().stream().collect(
                Collectors.maxBy(MARKET_VALUE_COMPARATOR_LAMBDA)).get();
    }

```

### SimplifyStreamApiCallChains
'collect(maxBy())' can be replaced with 'max()' (may change semantics)
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MaxByDoubleTest.java`
#### Snippet
```java
    public Position maxByMarketValue_parallel_lazy_collect_methodref_jdk()
    {
        return this.positions.getJdkPositions().parallelStream().collect(
                Collectors.maxBy(MARKET_VALUE_COMPARATOR_METHODREF)).get();
    }

```

### SimplifyStreamApiCallChains
'collect(maxBy())' can be replaced with 'max()' (may change semantics)
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MaxByDoubleTest.java`
#### Snippet
```java
    public Position maxByMarketValue_serial_lazy_collect_methodref_jdk()
    {
        return this.positions.getJdkPositions().stream().collect(
                Collectors.maxBy(MARKET_VALUE_COMPARATOR_METHODREF)).get();
    }

```

### SimplifyStreamApiCallChains
'collect(maxBy())' can be replaced with 'max()' (may change semantics)
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MaxByDoubleTest.java`
#### Snippet
```java
    public Position maxByMarketValue_parallel_lazy_collect_lambda_jdk()
    {
        return this.positions.getJdkPositions().parallelStream().collect(
                Collectors.maxBy(MARKET_VALUE_COMPARATOR_LAMBDA)).get();
    }

```

### SimplifyStreamApiCallChains
''stream().forEach()'' can be replaced with 'forEach()'' (may change semantics)
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/CountSetTest.java`
#### Snippet
```java
            this.integersEC.asParallel(this.executorService, BATCH_SIZE).forEach(new CountProcedure<>());

            this.integersJDK.stream().forEach(Assert::assertNotNull);
            this.integersJDK.stream().forEach(each -> Assert.assertEquals(each, each));

```

### SimplifyStreamApiCallChains
''stream().forEach()'' can be replaced with 'forEach()'' (may change semantics)
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/CountSetTest.java`
#### Snippet
```java

            this.integersJDK.stream().forEach(Assert::assertNotNull);
            this.integersJDK.stream().forEach(each -> Assert.assertEquals(each, each));

            this.integersJDK.parallelStream().forEach(Assert::assertNotNull);
```

### SimplifyStreamApiCallChains
Can be replaced with '.values().stream()'
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AnagramBagTest.java`
#### Snippet
```java
        groupBy.entrySet()
                .stream()
                .map(Map.Entry::getValue)
                .filter(list -> list.size() >= SIZE_THRESHOLD)
                .sorted(Comparator.<List<String>>comparingInt(List::size).reversed())
```

### SimplifyStreamApiCallChains
Can be replaced with '.values().stream()'
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AnagramBagTest.java`
#### Snippet
```java
        groupBy.entrySet()
                .stream()
                .map(Map.Entry::getValue)
                .filter(list -> list.size() >= SIZE_THRESHOLD)
                .sorted(Comparator.<List<String>>comparingInt(List::size).reversed())
```

### SimplifyStreamApiCallChains
''stream().forEach()'' can be replaced with 'forEach()'' (may change semantics)
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/CountTest.java`
#### Snippet
```java
            this.integersEC.asParallel(this.executorService, BATCH_SIZE).forEach(new CountProcedure<>());

            this.integersJDK.stream().forEach(Assert::assertNotNull);
            this.integersJDK.stream().forEach(each -> Assert.assertEquals(each, each));

```

### SimplifyStreamApiCallChains
''stream().forEach()'' can be replaced with 'forEach()'' (may change semantics)
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/CountTest.java`
#### Snippet
```java

            this.integersJDK.stream().forEach(Assert::assertNotNull);
            this.integersJDK.stream().forEach(each -> Assert.assertEquals(each, each));

            this.integersJDK.parallelStream().forEach(Assert::assertNotNull);
```

## RuleId[id=ParameterCanBeLocal]
### ParameterCanBeLocal
Parameter can be converted to a local variable
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
    }

    private V slowGet(Object key, int hash, int index, AtomicReferenceArray currentArray)
    {
        while (true)
```

### ParameterCanBeLocal
Parameter can be converted to a local variable
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
    }

    private V slowGet(Object key, int hash, int index, Object[] currentArray)
    {
        while (true)
```

## RuleId[id=TypeParameterHidesVisibleType]
### TypeParameterHidesVisibleType
Type parameter `T` hides type parameter 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/UnmodifiableRichIterable.java`
#### Snippet
```java

    @Override
    public <T> T[] toArray(T[] a)
    {
        return this.iterable.toArray(a);
```

### TypeParameterHidesVisibleType
Type parameter `T` hides type parameter 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/immutable/ImmutableHashBag.java`
#### Snippet
```java

    @Override
    public <T> T[] toArray(T[] a)
    {
        return this.delegate.toArray(a);
```

### TypeParameterHidesVisibleType
Type parameter `T` hides type parameter 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/immutable/ImmutableEmptyBag.java`
#### Snippet
```java

    @Override
    public <T> T[] toArray(T[] a)
    {
        if (a.length > 0)
```

### TypeParameterHidesVisibleType
Type parameter `K` hides type parameter 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/MapAdapter.java`
#### Snippet
```java

    @Override
    public <K, V> MutableMap<K, V> newEmpty(int capacity)
    {
        return UnifiedMap.newMap(capacity);
```

### TypeParameterHidesVisibleType
Type parameter `V` hides type parameter 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/MapAdapter.java`
#### Snippet
```java

    @Override
    public <K, V> MutableMap<K, V> newEmpty(int capacity)
    {
        return UnifiedMap.newMap(capacity);
```

### TypeParameterHidesVisibleType
Type parameter `K` hides type parameter 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentMutableHashMap.java`
#### Snippet
```java

    @Override
    public <K, V> MutableMap<K, V> newEmpty(int capacity)
    {
        return ConcurrentMutableHashMap.newMap();
```

### TypeParameterHidesVisibleType
Type parameter `V` hides type parameter 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentMutableHashMap.java`
#### Snippet
```java

    @Override
    public <K, V> MutableMap<K, V> newEmpty(int capacity)
    {
        return ConcurrentMutableHashMap.newMap();
```

### TypeParameterHidesVisibleType
Type parameter `K` hides type parameter 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/AbstractMutableMap.java`
#### Snippet
```java
     * Creates a new instance of the same type, using the given capacity and the default growth parameters.
     */
    public abstract <K, V> MutableMap<K, V> newEmpty(int capacity);

    @Override
```

### TypeParameterHidesVisibleType
Type parameter `V` hides type parameter 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/AbstractMutableMap.java`
#### Snippet
```java
     * Creates a new instance of the same type, using the given capacity and the default growth parameters.
     */
    public abstract <K, V> MutableMap<K, V> newEmpty(int capacity);

    @Override
```

### TypeParameterHidesVisibleType
Type parameter `K` hides type parameter 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/primitive/ObjectBooleanHashMapWithHashingStrategy.java`
#### Snippet
```java
        }

        public class InternalKeysViewIterator<K> implements Iterator<K>
        {
            private int count;
```

### TypeParameterHidesVisibleType
Type parameter `K` hides type parameter 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/primitive/ObjectBooleanHashMap.java`
#### Snippet
```java
        }

        public class InternalKeysViewIterator<K> implements Iterator<K>
        {
            private int count;
```

### TypeParameterHidesVisibleType
Type parameter `K` hides type parameter 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java

    @Override
    public <K, V> MutableMap<K, V> newEmpty(int capacity)
    {
        return ConcurrentHashMap.newMap();
```

### TypeParameterHidesVisibleType
Type parameter `V` hides type parameter 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java

    @Override
    public <K, V> MutableMap<K, V> newEmpty(int capacity)
    {
        return ConcurrentHashMap.newMap();
```

### TypeParameterHidesVisibleType
Type parameter `K` hides type parameter 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java

    @Override
    public <K, V> MutableMap<K, V> newEmpty(int capacity)
    {
        return ConcurrentHashMapUnsafe.newMap();
```

### TypeParameterHidesVisibleType
Type parameter `V` hides type parameter 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java

    @Override
    public <K, V> MutableMap<K, V> newEmpty(int capacity)
    {
        return ConcurrentHashMapUnsafe.newMap();
```

### TypeParameterHidesVisibleType
Type parameter `T` hides type parameter 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java

    @Override
    public <T> T[] toArray(T[] array)
    {
        int size = this.size();
```

### TypeParameterHidesVisibleType
Type parameter `T` hides type parameter 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/strategy/mutable/UnifiedSetWithHashingStrategy.java`
#### Snippet
```java

    @Override
    public <T> T[] toArray(T[] array)
    {
        int size = this.size();
```

### TypeParameterHidesVisibleType
Type parameter `T` hides type parameter 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/stack/mutable/SynchronizedStack.java`
#### Snippet
```java

    @Override
    public <T> T[] toArray(T[] a)
    {
        synchronized (this.lock)
```

### TypeParameterHidesVisibleType
Type parameter `T` hides type parameter 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/stack/mutable/UnmodifiableStack.java`
#### Snippet
```java

    @Override
    public <T> T[] toArray(T[] a)
    {
        return this.mutableStack.toArray(a);
```

### TypeParameterHidesVisibleType
Type parameter `T` hides type parameter 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/stack/mutable/ArrayStack.java`
#### Snippet
```java

    @Override
    public <T> T[] toArray(T[] a)
    {
        return this.delegate.asReversed().toArray(a);
```

### TypeParameterHidesVisibleType
Type parameter `T` hides type parameter 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/stack/immutable/ImmutableArrayStack.java`
#### Snippet
```java

    @Override
    public <T> T[] toArray(T[] a)
    {
        return this.delegate.asReversed().toArray(a);
```

### TypeParameterHidesVisibleType
Type parameter `T` hides type parameter 'T'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/AbstractSynchronizedRichIterable.java`
#### Snippet
```java

    @Override
    public <T> T[] toArray(T[] a)
    {
        synchronized (this.lock)
```

## RuleId[id=DeprecatedIsStillUsed]
### DeprecatedIsStillUsed
Deprecated member 'ConcurrentMutableHashMap' is still used
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentMutableHashMap.java`
#### Snippet
```java
 */
@Deprecated
public final class ConcurrentMutableHashMap<K, V>
        extends AbstractMutableMap<K, V>
        implements ConcurrentMutableMap<K, V>, Serializable
```

### DeprecatedIsStillUsed
Deprecated member 'DEFAULT_LOAD_FACTOR' is still used
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/primitive/ObjectBooleanHashMap.java`
#### Snippet
```java
     */
    @Deprecated
    private static final float DEFAULT_LOAD_FACTOR = 0.5f;
    private static final int OCCUPIED_DATA_RATIO = 2;
    private static final int OCCUPIED_SENTINEL_RATIO = 4;
```

### DeprecatedIsStillUsed
Deprecated member 'DropIterator' is still used
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/lazy/iterator/DropIterator.java`
#### Snippet
```java
 */
@Deprecated
public final class DropIterator<T> implements Iterator<T>
{
    private final Iterator<T> delegateIterator;
```

### DeprecatedIsStillUsed
Deprecated member 'CharPredicate' is still used
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/predicate/primitive/CharPredicate.java`
#### Snippet
```java
@FunctionalInterface
@Deprecated
public interface CharPredicate
        extends Serializable
{
```

### DeprecatedIsStillUsed
Deprecated member 'originalNaturalOrder' is still used
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Comparators.java`
#### Snippet
```java
     */
    @Deprecated
    public static <T> SerializableComparator<T> originalNaturalOrder()
    {
        return (SerializableComparator<T>) NATURAL_ORDER_COMPARATOR;
```

### DeprecatedIsStillUsed
Deprecated member 'originalByFunction' is still used
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Comparators.java`
#### Snippet
```java
     */
    @Deprecated
    public static <T, V extends Comparable<? super V>> SerializableComparator<T> originalByFunction(Function<? super T, ? extends V> function)
    {
        SerializableComparator<T> comparator = Comparators.getPrimitiveFunctionComparator(function);
```

### DeprecatedIsStillUsed
Deprecated member 'originalByFunction' is still used
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Comparators.java`
#### Snippet
```java
     */
    @Deprecated
    public static <T, V> SerializableComparator<T> originalByFunction(Function<? super T, ? extends V> function, Comparator<V> comparator)
    {
        return new FunctionComparator<>(function, comparator);
```

### DeprecatedIsStillUsed
Deprecated member 'originalReverseNaturalOrder' is still used
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Comparators.java`
#### Snippet
```java
     */
    @Deprecated
    public static <T> SerializableComparator<T> originalReverseNaturalOrder()
    {
        return (SerializableComparator<T>) REVERSE_NATURAL_ORDER_COMPARATOR;
```

### DeprecatedIsStillUsed
Deprecated member 'getKeyFunction' is still used
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/tuple/AbstractImmutableEntry.java`
#### Snippet
```java
     */
    @Deprecated
    public static <K> Function<Map.Entry<K, ?>, K> getKeyFunction()
    {
        return Functions.getKeyFunction();
```

### DeprecatedIsStillUsed
Deprecated member 'getValueFunction' is still used
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/tuple/AbstractImmutableEntry.java`
#### Snippet
```java
     */
    @Deprecated
    public static <V> Function<Map.Entry<?, V>, V> getValueFunction()
    {
        return Functions.getValueFunction();
```

### DeprecatedIsStillUsed
Deprecated member 'ImmutableArrayStack' is still used
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/stack/immutable/ImmutableArrayStack.java`
#### Snippet
```java
 */
@Deprecated
final class ImmutableArrayStack<T> implements ImmutableStack<T>, Serializable
{
    private static final long serialVersionUID = 1L;
```

### DeprecatedIsStillUsed
Deprecated member 'asUppercaseSet' is still used
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/StringIterate.java`
#### Snippet
```java
     */
    @Deprecated
    public static MutableSet<Character> asUppercaseSet(String string)
    {
        return StringIterate.toUppercaseSet(string);
```

### DeprecatedIsStillUsed
Deprecated member 'csvTokensToSet' is still used
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/StringIterate.java`
#### Snippet
```java
     */
    @Deprecated
    public static MutableSet<String> csvTokensToSet(String string)
    {
        return StringIterate.tokensToSet(string, ",");
```

### DeprecatedIsStillUsed
Deprecated member 'csvTokensToSortedList' is still used
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/StringIterate.java`
#### Snippet
```java
     */
    @Deprecated
    public static MutableList<String> csvTokensToSortedList(String string)
    {
        return StringIterate.tokensToSortedList(string, ",");
```

### DeprecatedIsStillUsed
Deprecated member 'csvTokensToReverseSortedList' is still used
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/StringIterate.java`
#### Snippet
```java
     */
    @Deprecated
    public static MutableList<String> csvTokensToReverseSortedList(String string)
    {
        return StringIterate.tokensToReverseSortedList(string, ",");
```

### DeprecatedIsStillUsed
Deprecated member 'asLowercaseSet' is still used
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/StringIterate.java`
#### Snippet
```java
     */
    @Deprecated
    public static MutableSet<Character> asLowercaseSet(String string)
    {
        return StringIterate.toLowercaseSet(string);
```

### DeprecatedIsStillUsed
Deprecated member 'csvTokensToList' is still used
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/StringIterate.java`
#### Snippet
```java
     */
    @Deprecated
    public static MutableList<String> csvTokensToList(String string)
    {
        return StringIterate.tokensToList(string, ",");
```

### DeprecatedIsStillUsed
Deprecated member 'csvTrimmedTokensToList' is still used
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/StringIterate.java`
#### Snippet
```java
     */
    @Deprecated
    public static MutableList<String> csvTrimmedTokensToList(String string)
    {
        return StringIterate.trimmedTokensToList(string, ",");
```

### DeprecatedIsStillUsed
Deprecated member 'csvTrimmedTokensToSortedList' is still used
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/StringIterate.java`
#### Snippet
```java
     */
    @Deprecated
    public static MutableList<String> csvTrimmedTokensToSortedList(String string)
    {
        return StringIterate.trimmedTokensToSortedList(string, ",");
```

### DeprecatedIsStillUsed
Deprecated member 'EMPTY_CLASS_ARRAY' is still used
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/ReflectionHelper.java`
#### Snippet
```java
    @SuppressWarnings("rawtypes")
    @Deprecated
    public static final Class[] EMPTY_CLASS_ARRAY = {};

    /**
```

### DeprecatedIsStillUsed
Deprecated member 'assertShallowClone' is still used
in `eclipse-collections-testutils/src/main/java/org/eclipse/collections/impl/test/Verify.java`
#### Snippet
```java
     */
    @Deprecated
    public static void assertShallowClone(Cloneable object)
    {
        try
```

## RuleId[id=SortedCollectionWithNonComparableKeys]
### SortedCollectionWithNonComparableKeys
Construction of sorted collection with non-comparable elements
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/sorted/mutable/TreeSortedMap.java`
#### Snippet
```java
    public TreeSortedMap(Pair<K, V>... pairs)
    {
        this.treeMap = new TreeMap<>();
        ArrayIterate.forEach(pairs, new MapCollectProcedure<Pair<K, V>, K, V>(
                this.treeMap,
```

### SortedCollectionWithNonComparableKeys
Construction of sorted collection with non-comparable elements
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/sorted/mutable/TreeSortedMap.java`
#### Snippet
```java
    public TreeSortedMap()
    {
        this.treeMap = new TreeMap<>();
    }

```

### SortedCollectionWithNonComparableKeys
Construction of sorted collection with non-comparable elements
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/mutable/TreeSortedSet.java`
#### Snippet
```java
    public TreeSortedSet(Iterable<? extends T> iterable)
    {
        this.treeSet = new TreeSet<>();
        this.addAllIterable(iterable);
    }
```

### SortedCollectionWithNonComparableKeys
Construction of sorted collection with non-comparable elements
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/mutable/TreeSortedSet.java`
#### Snippet
```java
    public TreeSortedSet()
    {
        this.treeSet = new TreeSet<>();
    }

```

## RuleId[id=RedundantComparatorComparing]
### RedundantComparatorComparing
Comparator method can be simplified if 'min()' call is replaced with 'max()'
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MinTest.java`
#### Snippet
```java
    public int serial_lazy_reverse_streams_ec()
    {
        return this.integersEC.stream().min(Comparator.reverseOrder()).get();
    }

```

### RedundantComparatorComparing
Comparator method can be simplified if 'min()' call is replaced with 'max()'
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MinTest.java`
#### Snippet
```java
    public int serial_lazy_reverse_jdk()
    {
        return this.integersJDK.stream().min(Comparator.reverseOrder()).get();
    }

```

### RedundantComparatorComparing
Comparator method can be simplified if 'min()' call is replaced with 'max()'
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MinTest.java`
#### Snippet
```java
    public int parallel_lazy_reverse_streams_ec()
    {
        return this.integersEC.parallelStream().min(Comparator.reverseOrder()).get();
    }

```

### RedundantComparatorComparing
Comparator method can be simplified if 'min()' call is replaced with 'max()'
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MinTest.java`
#### Snippet
```java
    public int parallel_lazy_reverse_jdk()
    {
        return this.integersJDK.parallelStream().min(Comparator.reverseOrder()).get();
    }

```

### RedundantComparatorComparing
Comparator method can be simplified if 'max()' call is replaced with 'min()'
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MaxTest.java`
#### Snippet
```java
    public int parallel_lazy_reverse_streams_ec()
    {
        return this.integersEC.parallelStream().max(Comparator.reverseOrder()).get();
    }

```

### RedundantComparatorComparing
Comparator method can be simplified if 'max()' call is replaced with 'min()'
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MaxTest.java`
#### Snippet
```java
    public int parallel_lazy_reverse_jdk()
    {
        return this.integersJDK.parallelStream().max(Comparator.reverseOrder()).get();
    }

```

### RedundantComparatorComparing
Comparator method can be simplified if 'max()' call is replaced with 'min()'
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MaxTest.java`
#### Snippet
```java
    public int serial_lazy_reverse_streams_ec()
    {
        return this.integersEC.stream().max(Comparator.reverseOrder()).get();
    }

```

### RedundantComparatorComparing
Comparator method can be simplified if 'max()' call is replaced with 'min()'
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MaxTest.java`
#### Snippet
```java
    public int serial_lazy_reverse_jdk()
    {
        return this.integersJDK.stream().max(Comparator.reverseOrder()).get();
    }

```

## RuleId[id=RefusedBequest]
### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/sorted/mutable/UnmodifiableSortedBag.java`
#### Snippet
```java

    @Override
    public UnmodifiableSortedBag<T> clone()
    {
        return this;
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/sorted/mutable/SynchronizedSortedBag.java`
#### Snippet
```java

    @Override
    public MutableSortedBag<T> clone()
    {
        synchronized (this.getLock())
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/sorted/mutable/SynchronizedSortedMap.java`
#### Snippet
```java

    @Override
    public MutableSortedMap<K, V> clone()
    {
        synchronized (this.lock)
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/sorted/mutable/UnmodifiableTreeMap.java`
#### Snippet
```java

    @Override
    public MutableSortedMap<K, V> clone()
    {
        return this;
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/SynchronizedMutableMap.java`
#### Snippet
```java

    @Override
    public MutableMap<K, V> clone()
    {
        synchronized (this.lock)
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnmodifiableMutableMap.java`
#### Snippet
```java

    @Override
    public MutableMap<K, V> clone()
    {
        return this;
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/fixed/SingletonSet.java`
#### Snippet
```java
    // Weird implementation of clone() is ok on final classes
    @Override
    public SingletonSet<T> clone()
    {
        return new SingletonSet<>(this.element1);
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/fixed/TripletonSet.java`
#### Snippet
```java
    // Weird implementation of clone() is ok on final classes
    @Override
    public TripletonSet<T> clone()
    {
        return new TripletonSet<>(this.element1, this.element2, this.element3);
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/fixed/QuadrupletonSet.java`
#### Snippet
```java
    // Weird implementation of clone() is ok on final classes
    @Override
    public QuadrupletonSet<T> clone()
    {
        return new QuadrupletonSet<>(this.element1, this.element2, this.element3, this.element4);
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/fixed/EmptySet.java`
#### Snippet
```java

    @Override
    public EmptySet<T> clone()
    {
        return this;
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/fixed/DoubletonSet.java`
#### Snippet
```java
    // Weird implementation of clone() is ok on final classes
    @Override
    public DoubletonSet<T> clone()
    {
        return new DoubletonSet<>(this.element1, this.element2);
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/mutable/SynchronizedSortedSet.java`
#### Snippet
```java

    @Override
    public MutableSortedSet<T> clone()
    {
        synchronized (this.getLock())
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/mutable/UnmodifiableSortedSet.java`
#### Snippet
```java

    @Override
    public UnmodifiableSortedSet<T> clone()
    {
        return this;
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/SetAdapter.java`
#### Snippet
```java

    @Override
    public MutableSet<T> clone()
    {
        return Sets.mutable.withAll(this.delegate);
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/SynchronizedMutableSet.java`
#### Snippet
```java

    @Override
    public MutableSet<T> clone()
    {
        synchronized (this.getLock())
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/mutable/SortedSetAdapter.java`
#### Snippet
```java

    @Override
    public MutableSortedSet<T> clone()
    {
        return SortedSets.mutable.withAll(this.delegate);
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnmodifiableMutableSet.java`
#### Snippet
```java

    @Override
    public UnmodifiableMutableSet<T> clone()
    {
        return this;
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/MultiReaderUnifiedSet.java`
#### Snippet
```java

        @Override
        public MutableSet<T> clone()
        {
            return this.getDelegate().clone();
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/MultiReaderUnifiedSet.java`
#### Snippet
```java

    @Override
    public MultiReaderSet<T> clone()
    {
        try (LockWrapper wrapper = this.lockWrapper.acquireReadLock())
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/EmptyList.java`
#### Snippet
```java

    @Override
    public EmptyList<T> clone()
    {
        return this;
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/TripletonList.java`
#### Snippet
```java

    @Override
    public TripletonList<T> clone()
    {
        return new TripletonList<>(this.element1, this.element2, this.element3);
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/QuadrupletonList.java`
#### Snippet
```java

    @Override
    public QuadrupletonList<T> clone()
    {
        return new QuadrupletonList<>(this.element1, this.element2, this.element3, this.element4);
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/SingletonList.java`
#### Snippet
```java

    @Override
    public SingletonList<T> clone()
    {
        return new SingletonList<>(this.element1);
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/QuintupletonList.java`
#### Snippet
```java

    @Override
    public QuintupletonList<T> clone()
    {
        return new QuintupletonList<>(this.element1, this.element2, this.element3, this.element4, this.element5);
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/DoubletonList.java`
#### Snippet
```java

    @Override
    public DoubletonList<T> clone()
    {
        return new DoubletonList<>(this.element1, this.element2);
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/ArrayAdapter.java`
#### Snippet
```java

    @Override
    public ArrayAdapter<T> clone()
    {
        return new ArrayAdapter<>(this.items.clone());
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/SextupletonList.java`
#### Snippet
```java

    @Override
    public SextupletonList<T> clone()
    {
        return new SextupletonList<>(
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/ArrayListAdapter.java`
#### Snippet
```java

    @Override
    public ArrayListAdapter<T> clone()
    {
        return new ArrayListAdapter<>((ArrayList<T>) this.delegate.clone());
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/ListAdapter.java`
#### Snippet
```java

    @Override
    public MutableList<T> clone()
    {
        return Lists.mutable.withAll(this.delegate);
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/SynchronizedMutableList.java`
#### Snippet
```java

    @Override
    public MutableList<T> clone()
    {
        synchronized (this.getLock())
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/RandomAccessListAdapter.java`
#### Snippet
```java

    @Override
    public MutableList<T> clone()
    {
        return Lists.mutable.withAll(this.delegate);
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/UnmodifiableMutableList.java`
#### Snippet
```java

    @Override
    public UnmodifiableMutableList<T> clone()
    {
        return this;
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/UnmodifiableMutableList.java`
#### Snippet
```java

        @Override
        public RandomAccessUnmodifiableMutableList<T> clone()
        {
            return this;
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/MultiReaderFastList.java`
#### Snippet
```java

    @Override
    public MultiReaderList<T> clone()
    {
        try (LockWrapper wrapper = this.lockWrapper.acquireReadLock())
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/MultiReaderFastList.java`
#### Snippet
```java

        @Override
        public MutableList<T> clone()
        {
            return this.getDelegate().clone();
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/AbstractMutableList.java`
#### Snippet
```java

        @Override
        public MutableList<T> clone()
        {
            return new FastList<>(this);
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bimap/mutable/SynchronizedBiMap.java`
#### Snippet
```java

    @Override
    public MutableBiMap<K, V> clone()
    {
        synchronized (this.lock)
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bimap/mutable/UnmodifiableBiMap.java`
#### Snippet
```java

    @Override
    public MutableBiMap<K, V> clone()
    {
        return this;
```

### RefusedBequest
Method `clone()` does not call 'super.clone()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bimap/mutable/AbstractMutableBiMap.java`
#### Snippet
```java

    @Override
    public MutableBiMap<K, V> clone()
    {
        return new HashBiMap<>(this);
```

## RuleId[id=SimplifiableAssertion]
### SimplifiableAssertion
`assertFalse()` can be simplified to 'assertNotEquals()'
in `eclipse-collections-testutils/src/main/java/org/eclipse/collections/impl/test/Verify.java`
#### Snippet
```java
            }

            Assert.assertFalse("Neither item should equal null", objectA.equals(null));
            Assert.assertFalse("Neither item should equal null", objectB.equals(null));
            Assert.assertNotEquals("Neither item should equal new Object()", objectA.equals(new Object()));
```

### SimplifiableAssertion
`assertFalse()` can be simplified to 'assertNotEquals()'
in `eclipse-collections-testutils/src/main/java/org/eclipse/collections/impl/test/Verify.java`
#### Snippet
```java

            Assert.assertFalse("Neither item should equal null", objectA.equals(null));
            Assert.assertFalse("Neither item should equal null", objectB.equals(null));
            Assert.assertNotEquals("Neither item should equal new Object()", objectA.equals(new Object()));
            Assert.assertNotEquals("Neither item should equal new Object()", objectB.equals(new Object()));
```

## RuleId[id=ShiftOutOfRange]
### ShiftOutOfRange
Shift operation `<<` by out-of-bounds value {64..127}
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/primitive/ByteHashSet.java`
#### Snippet
```java
            long initial = this.bitGroup1;

            this.bitGroup1 |= 1L << (byte) ((element + 1) * -1);

            if (this.bitGroup1 != initial)
```

### ShiftOutOfRange
Shift operation `<<` by out-of-bounds value {64..127}
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/primitive/ByteHashSet.java`
#### Snippet
```java
            long initial = this.bitGroup4;

            this.bitGroup4 |= 1L << element;

            if (this.bitGroup4 != initial)
```

### ShiftOutOfRange
Shift operation `<<` by out-of-bounds value {64..127}
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/primitive/ByteHashSet.java`
#### Snippet
```java
        {
            long initial = this.bitGroup1;
            this.bitGroup1 &= ~(1L << (byte) ((value + 1) * -1));
            if (this.bitGroup1 == initial)
            {
```

### ShiftOutOfRange
Shift operation `<<` by out-of-bounds value {64..127}
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/primitive/ByteHashSet.java`
#### Snippet
```java

        long initial = this.bitGroup4;
        this.bitGroup4 &= ~(1L << value);
        if (this.bitGroup4 == initial)
        {
```

### ShiftOutOfRange
Shift operation `<<` by out-of-bounds value {-128, 64..127}
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/primitive/ByteHashSet.java`
#### Snippet
```java
            byte value = (byte) Long.numberOfTrailingZeros(bitGroup1);
            procedure.value((byte) ((value + 65) * -1));
            bitGroup1 &= ~(1L << (byte) (value + 64));
        }

```

### ShiftOutOfRange
Shift operation `<<` by out-of-bounds value {-128, 64..127}
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/primitive/ByteHashSet.java`
#### Snippet
```java
            byte value = (byte) Long.numberOfTrailingZeros(bitGroup4);
            procedure.value((byte) (value + 64));
            bitGroup4 &= ~(1L << (byte) (value + 64));
        }
    }
```

### ShiftOutOfRange
Shift operation `>>>` by out-of-bounds value {64..127}
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/primitive/ByteHashSet.java`
#### Snippet
```java
        if (value <= MAX_BYTE_GROUP_1)
        {
            return ((this.bitGroup1 >>> (byte) ((value + 1) * -1)) & 1L) != 0;
        }
        if (value <= MAX_BYTE_GROUP_2)
```

### ShiftOutOfRange
Shift operation `>>>` by out-of-bounds value {64..127}
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/primitive/ByteHashSet.java`
#### Snippet
```java
        }

        return ((this.bitGroup4 >>> value) & 1L) != 0;
    }

```

### ShiftOutOfRange
Shift operation `>>>` by out-of-bounds value {64..127}
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/primitive/ByteHashSet.java`
#### Snippet
```java
            if (value <= MAX_BYTE_GROUP_1)
            {
                return ((this.bitGroup1 >>> (byte) ((value + 1) * -1)) & 1L) != 0;
            }
            if (value <= MAX_BYTE_GROUP_2)
```

### ShiftOutOfRange
Shift operation `>>>` by out-of-bounds value {64..127}
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/primitive/ByteHashSet.java`
#### Snippet
```java
            }

            return ((this.bitGroup4 >>> value) & 1L) != 0;
        }

```

### ShiftOutOfRange
Shift operation `<<` by out-of-bounds value {-128, 64..127}
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/primitive/ByteHashSet.java`
#### Snippet
```java
                byte value = (byte) Long.numberOfTrailingZeros(bitGroup1);
                procedure.value((byte) ((value + 65) * -1));
                bitGroup1 &= ~(1L << (byte) (value + 64));
            }

```

### ShiftOutOfRange
Shift operation `<<` by out-of-bounds value {-128, 64..127}
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/primitive/ByteHashSet.java`
#### Snippet
```java
                byte value = (byte) Long.numberOfTrailingZeros(bitGroup4);
                procedure.value((byte) (value + 64));
                bitGroup4 &= ~(1L << (byte) (value + 64));
            }
        }
```

## RuleId[id=DuplicatedCode]
### DuplicatedCode
Duplicated code
in `eclipse-collections-api/src/main/java/org/eclipse/collections/api/RichIterable.java`
#### Snippet
```java
            RichIterable<?> outside = this;
            RichIterable<?> inside = (RichIterable<?>) source;
            if (this.size() < inside.size())
            {
                outside = inside;
                inside = this;
            }
            if (outside instanceof SetIterable)
            {
                RichIterable<?> temp = outside;
                outside = inside;
                inside = temp;
            }
            else if (inside.size() > 32 && !(inside instanceof SetIterable))
            {
                inside = inside.toSet();
            }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections-api/src/main/java/org/eclipse/collections/api/RichIterable.java`
#### Snippet
```java
        MutableMap<K, V> map = Maps.mutable.empty();
        this.forEach(each ->
        {
            K key = groupBy.valueOf(each);
            V value = map.getIfAbsentPut(key, zeroValueFactory);
            mutatingAggregator.value(value, each);
        });
        return map;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections-api/src/main/java/org/eclipse/collections/api/bimap/ImmutableBiMap.java`
#### Snippet
```java
        MutableMap<KK, VV> map = Maps.mutable.empty();
        this.forEach(each ->
        {
            KK key = groupBy.valueOf(each);
            VV value = map.getIfAbsentPut(key, zeroValueFactory);
            mutatingAggregator.value(value, each);
        });
        return map.toImmutable();
```

### DuplicatedCode
Duplicated code
in `eclipse-collections-api/src/main/java/org/eclipse/collections/api/collection/ImmutableCollection.java`
#### Snippet
```java
        MutableMap<K, V> map = Maps.mutable.empty();
        this.forEach(each ->
        {
            K key = groupBy.valueOf(each);
            V value = map.getIfAbsentPut(key, zeroValueFactory);
            mutatingAggregator.value(value, each);
        });
        return map.toImmutable();
```

### DuplicatedCode
Duplicated code
in `eclipse-collections-api/src/main/java/org/eclipse/collections/api/map/MutableMap.java`
#### Snippet
```java
        MutableMap<KK, VV> map = Maps.mutable.empty();
        this.forEach(each ->
        {
            KK key = groupBy.valueOf(each);
            VV value = map.getIfAbsentPut(key, zeroValueFactory);
            mutatingAggregator.value(value, each);
        });
        return map;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections-forkjoin/src/main/java/org/eclipse/collections/impl/forkjoin/FJBatchIterableProcedureRunner.java`
#### Snippet
```java
        FastList<ForkJoinTask<PT>> tasks = this.createAndExecuteTasks(executor, procedureFactory, list);
        if (this.combiner.useCombineOne())
        {
            this.join();
        }
        if (this.error != null)
        {
            throw new RuntimeException("One or more parallel tasks failed", this.error);
        }
        if (!this.combiner.useCombineOne())
        {
            this.combiner.combineAll(tasks.asLazy().collect(new ProcedureExtractor()));
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections-forkjoin/src/main/java/org/eclipse/collections/impl/forkjoin/FJListObjectIntProcedureRunner.java`
#### Snippet
```java
        FastList<ForkJoinTask<PT>> tasks = FastList.newList(this.taskCount);
        int sectionSize = list.size() / this.taskCount;
        int taskCountMinusOne = this.taskCount - 1;
        for (int index = 0; index < this.taskCount; index++)
        {
            ForkJoinTask<PT> task = this.createTask(procedureFactory, list, sectionSize, taskCountMinusOne, index);
            tasks.add(task);
            executor.execute(task);
        }
        return tasks;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/AbstractBag.java`
#### Snippet
```java
            for (int i = 0; i < occurrences; i++)
            {
                double adjustedValue = f - compensation[0];
                double nextSum = sum[0] + adjustedValue;
                compensation[0] = nextSum - sum[0] - adjustedValue;
                sum[0] = nextSum;
            }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/AbstractBag.java`
#### Snippet
```java
        int keySize = Math.min(n, this.sizeDistinct());
        MutableList<ObjectIntPair<T>> sorted = this.toListWithOccurrences().sortThisByInt(function);
        MutableList<ObjectIntPair<T>> results = sorted.subList(0, keySize).toList();
        while (keySize < sorted.size() && results.getLast().getTwo() == sorted.get(keySize).getTwo())
        {
            results.add(sorted.get(keySize));
            keySize++;
        }
        return results;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/immutable/AbstractImmutableBag.java`
#### Snippet
```java
        PartitionMutableBag<T> partitionMutableBag = new PartitionHashBag<>();
        this.forEachWithOccurrences((each, occurrences) -> {
            MutableBag<T> bucket = predicate.accept(each, parameter)
                    ? partitionMutableBag.getSelected()
                    : partitionMutableBag.getRejected();
            bucket.addOccurrences(each, occurrences);
        });
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/immutable/AbstractImmutableBagIterable.java`
#### Snippet
```java
        if (elements instanceof Set)
        {
            result.removeAll((Set<?>) elements);
        }
        else if (elements instanceof List)
        {
            List<T> toBeRemoved = (List<T>) elements;
            if (this.size() * toBeRemoved.size() > 10000)
            {
                result.removeAll(Sets.mutable.withAll(elements));
            }
            else
            {
                result.removeAll(toBeRemoved);
            }
        }
        else
        {
            result.removeAll(Sets.mutable.withAll(elements));
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/immutable/ImmutableBagSerializationProxy.java`
#### Snippet
```java
        try
        {
            this.bag.forEachWithOccurrences(new CheckedObjectIntProcedure<T>()
            {
                public void safeValue(T object, int index) throws IOException
                {
                    out.writeObject(object);
                    out.writeInt(index);
                }
            });
        }
        catch (RuntimeException e)
        {
            if (e.getCause() instanceof IOException)
            {
                throw (IOException) e.getCause();
            }
            throw e;
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/mutable/AbstractMutableBagIterable.java`
#### Snippet
```java
        if (size <= 0)
        {
            throw new IllegalArgumentException("Size for groups must be positive but was: " + size);
        }

        Iterator<T> iterator = this.iterator();
        MutableList<RichIterable<T>> result = Lists.mutable.empty();
        while (iterator.hasNext())
        {
            MutableCollection<T> batch = this.newEmpty();
            for (int i = 0; i < size && iterator.hasNext(); i++)
            {
                batch.add(iterator.next());
            }
            result.add(batch);
        }
        return result;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/mutable/primitive/BooleanHashBag.java`
#### Snippet
```java
                BooleanIterator iterator = this.booleanIterator();
                while (iterator.hasNext())
                {
                    MutableBooleanBag batch = BooleanBags.mutable.empty();
                    for (int i = 0; i < size && iterator.hasNext(); i++)
                    {
                        batch.add(iterator.next());
                    }
                    result.add(batch);
                }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/sorted/immutable/AbstractImmutableSortedBag.java`
#### Snippet
```java
        PartitionMutableSortedBag<T> result = new PartitionTreeBag<>(this.comparator());
        this.forEachWithOccurrences((each, index) ->
        {
            MutableSortedBag<T> bucket = predicate.accept(each) ? result.getSelected() : result.getRejected();
            bucket.addOccurrences(each, index);
        });
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/sorted/immutable/AbstractImmutableSortedBag.java`
#### Snippet
```java
        PartitionMutableSortedBag<T> result = new PartitionTreeBag<>(this.comparator());
        this.forEachWithOccurrences((each, index) ->
        {
            MutableSortedBag<T> bucket = predicate.accept(each, parameter)
                    ? result.getSelected()
                    : result.getRejected();
            bucket.addOccurrences(each, index);
        });
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/sorted/immutable/AbstractImmutableSortedBag.java`
#### Snippet
```java
        Comparator<? super S> comparator = (Comparator<? super S>) this.comparator();
        MutableSortedBag<S> result = SortedBags.mutable.empty(comparator);
        this.forEachWithOccurrences((each, occurrences) ->
        {
            if (clazz.isInstance(each))
            {
                result.addOccurrences(clazz.cast(each), occurrences);
            }
        });
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/sorted/mutable/TreeBag.java`
#### Snippet
```java
        ListIterate.rangeCheck(fromIndex, toIndex, this.size);
        if (fromIndex > toIndex)
        {
            throw new IllegalArgumentException("fromIndex must not be greater than toIndex");
        }

        Iterator<Map.Entry<T, Counter>> iterator = this.items.entrySet().iterator();
        int i = 0;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bimap/mutable/AbstractMutableBiMap.java`
#### Snippet
```java
        V value = this.delegate.get(key);

        if (value != null || this.delegate.containsKey(key))
        {
            return value;
        }

        V newValue = function.valueOf(parameter);
        this.put(key, newValue);
        return newValue;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/primitive/BooleanPredicates.java`
#### Snippet
```java
        if (one == IS_TRUE_BOOLEAN_PREDICATE && two == IS_TRUE_BOOLEAN_PREDICATE)
        {
            return IS_TRUE_BOOLEAN_PREDICATE;
        }
        if (one == IS_FALSE_BOOLEAN_PREDICATE && two == IS_TRUE_BOOLEAN_PREDICATE || one == IS_TRUE_BOOLEAN_PREDICATE && two == IS_FALSE_BOOLEAN_PREDICATE)
        {
            return FALSE_PREDICATE;
        }
        if (one == IS_FALSE_BOOLEAN_PREDICATE && two == IS_FALSE_BOOLEAN_PREDICATE)
        {
            return IS_FALSE_BOOLEAN_PREDICATE;
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collector/BigDecimalSummaryStatistics.java`
#### Snippet
```java
        this.count++;
        if (each != null)
        {
            this.sum = this.sum.add(each);
            this.min = this.min == null ? each : this.min.min(each);
            this.max = this.max == null ? each : this.max.max(each);
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collector/BigDecimalSummaryStatistics.java`
#### Snippet
```java
        this.count += summaryStatistics.count;
        this.sum = this.sum.add(summaryStatistics.sum);
        if (summaryStatistics.min != null)
        {
            this.min = this.min == null ? summaryStatistics.min : this.min.min(summaryStatistics.min);
        }
        if (summaryStatistics.max != null)
        {
            this.max = this.max == null ? summaryStatistics.max : this.max.max(summaryStatistics.max);
        }
        return this;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/Interval.java`
#### Snippet
```java
        if (from % 2 != 0)
        {
            if (from < to)
            {
                from++;
            }
            else
            {
                from--;
            }
        }
        if (to % 2 != 0)
        {
            if (to > from)
            {
                to--;
            }
            else
            {
                to++;
            }
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/Interval.java`
#### Snippet
```java
        if (from % 2 == 0)
        {
            if (from < to)
            {
                from++;
            }
            else
            {
                from--;
            }
        }
        if (to % 2 == 0)
        {
            if (to > from)
            {
                to--;
            }
            else
            {
                to++;
            }
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/Interval.java`
#### Snippet
```java
        int index = 0;
        if (this.goForward())
        {
            for (long i = this.from; i <= this.to; i += this.step)
            {
                procedure.value((int) i, index++);
            }
        }
        else
        {
            for (long i = this.from; i >= this.to; i += this.step)
            {
                procedure.value((int) i, index++);
            }
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/primitive/LongInterval.java`
#### Snippet
```java
        if (this.goForward())
        {
            for (long i = this.from; i <= this.to; i += this.step)
            {
                procedure.value((int) i, index++);
            }
        }
        else
        {
            for (long i = this.from; i >= this.to; i += this.step)
            {
                procedure.value((int) i, index++);
            }
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/Interval.java`
#### Snippet
```java
        if (this.goForward())
        {
            for (long i = this.from; i <= this.to; i += this.step)
            {
                procedure.value((int) i);
            }
        }
        else
        {
            for (long i = this.from; i >= this.to; i += this.step)
            {
                procedure.value((int) i);
            }
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/DoubletonList.java`
#### Snippet
```java
        switch (index)
        {
            case 0:
                return this.element1;
            case 1:
                return this.element2;
            default:
                throw new IndexOutOfBoundsException("Index: " + index + ", Size: " + this.size());
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/QuadrupletonList.java`
#### Snippet
```java
        switch (index)
        {
            case 0:
                return this.element1;
            case 1:
                return this.element2;
            case 2:
                return this.element3;
            case 3:
                return this.element4;
            default:
                throw new IndexOutOfBoundsException("Index: " + index + ", Size: " + this.size());
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/QuintupletonList.java`
#### Snippet
```java
        switch (index)
        {
            case 0:
                return this.element1;
            case 1:
                return this.element2;
            case 2:
                return this.element3;
            case 3:
                return this.element4;
            case 4:
                return this.element5;
            default:
                throw new IndexOutOfBoundsException("Index: " + index + ", Size: " + this.size());
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/QuintupletonList.java`
#### Snippet
```java
        this.element1 = operator.apply(this.element1);
        this.element2 = operator.apply(this.element2);
        this.element3 = operator.apply(this.element3);
        this.element4 = operator.apply(this.element4);
        this.element5 = operator.apply(this.element5);
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/SextupletonList.java`
#### Snippet
```java
        switch (index)
        {
            case 0:
                return this.element1;
            case 1:
                return this.element2;
            case 2:
                return this.element3;
            case 3:
                return this.element4;
            case 4:
                return this.element5;
            case 5:
                return this.element6;
            default:
                throw new IndexOutOfBoundsException("Index: " + index + ", Size: " + this.size());
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/TripletonList.java`
#### Snippet
```java
        switch (index)
        {
            case 0:
                return this.element1;
            case 1:
                return this.element2;
            case 2:
                return this.element3;
            default:
                throw new IndexOutOfBoundsException("Index: " + index + ", Size: " + this.size());
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/immutable/AbstractImmutableList.java`
#### Snippet
```java
            if (fromIndex < 0)
            {
                throw new IndexOutOfBoundsException("fromIndex = " + fromIndex);
            }
            if (toIndex > list.size())
            {
                throw new IndexOutOfBoundsException("toIndex = " + toIndex);
            }
            if (fromIndex > toIndex)
            {
                throw new IllegalArgumentException("fromIndex(" + fromIndex + ") > toIndex(" + toIndex + ')');
            }
            this.original = list;
            this.offset = fromIndex;
            this.size = toIndex - fromIndex;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/immutable/ImmutableDecapletonList.java`
#### Snippet
```java
        procedure.value(this.element1);
        procedure.value(this.element2);
        procedure.value(this.element3);
        procedure.value(this.element4);
        procedure.value(this.element5);
        procedure.value(this.element6);
        procedure.value(this.element7);
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/immutable/primitive/ImmutableBooleanArrayList.java`
#### Snippet
```java
        MutableBooleanList target = new BooleanArrayList();
        MutableBooleanSet seenSoFar = new BooleanHashSet();
        for (int i = 0; i < this.size; i++)
        {
            boolean each = this.get(i);
            if (seenSoFar.add(each))
            {
                target.add(each);
            }
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/immutable/primitive/ImmutableBooleanArrayList.java`
#### Snippet
```java
        try
        {
            appendable.append(start);
            for (int i = 0; i < this.size; i++)
            {
                if (i > 0)
                {
                    appendable.append(separator);
                }
                boolean value = this.items.get(i);
                appendable.append(String.valueOf(value));
            }
            appendable.append(end);
        }
        catch (IOException e)
        {
            throw new RuntimeException(e);
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/primitive/IntInterval.java`
#### Snippet
```java
        T result = injectedValue;
        if (this.goForward())
        {
            for (long i = this.from; i <= this.to; i += this.step)
            {
                result = function.valueOf(result, (int) i);
            }
        }
        else
        {
            for (long i = this.from; i >= this.to; i += this.step)
            {
                result = function.valueOf(result, (int) i);
            }
        }
        return result;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/primitive/IntInterval.java`
#### Snippet
```java
        T result = injectedValue;
        int index = 0;

        if (this.goForward())
        {
            for (long i = this.from; i <= this.to; i += this.step)
            {
                result = function.valueOf(result, (int) i, index);
                index++;
            }
        }
        else
        {
            for (long i = this.from; i >= this.to; i += this.step)
            {
                result = function.valueOf(result, (int) i, index);
                index++;
            }
        }
        return result;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/primitive/IntInterval.java`
#### Snippet
```java
                    for (long i = innerFrom; i <= this.to && batch.size() < size; i += this.step)
                    {
                        batch.add((int) i);
                        lastUpdated = (int) i;
                    }
                    result.add(batch);
                    innerFrom = lastUpdated + this.step;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/primitive/IntInterval.java`
#### Snippet
```java
                    for (long i = innerFrom; i >= this.to && batch.size() < size; i += this.step)
                    {
                        batch.add((int) i);
                        lastUpdated = (int) i;
                    }
                    result.add(batch);
                    innerFrom = lastUpdated + this.step;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/fixed/DoubletonMap.java`
#### Snippet
```java
        if (predicate.accept(this.key1, this.value1))
        {
            return Tuples.pair(this.key1, this.value1);
        }
        if (predicate.accept(this.key2, this.value2))
        {
            return Tuples.pair(this.key2, this.value2);
        }
        return null;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/fixed/FixedSizeMapFactoryImpl.java`
#### Snippet
```java
        if (Objects.equals(key1, key2) && Objects.equals(key2, key3))
        {
            return this.of(key1, value3);
        }
        if (Objects.equals(key1, key2))
        {
            return this.of(key1, value2, key3, value3);
        }
        if (Objects.equals(key1, key3))
        {
            return this.of(key2, value2, key1, value3);
        }
        if (Objects.equals(key2, key3))
        {
            return this.of(key1, value1, key2, value3);
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/fixed/TripletonMap.java`
#### Snippet
```java
        if (Objects.equals(this.key3, key))
        {
            return this.value3;
        }
        if (Objects.equals(this.key2, key))
        {
            return this.value2;
        }
        if (Objects.equals(this.key1, key))
        {
            return this.value1;
        }
        return null;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/fixed/TripletonMap.java`
#### Snippet
```java
        Pair<K2, V2> pair1 = function.value(this.key1, this.value1);
        Pair<K2, V2> pair2 = function.value(this.key2, this.value2);
        Pair<K2, V2> pair3 = function.value(this.key3, this.value3);
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/fixed/TripletonMap.java`
#### Snippet
```java
        if (predicate.accept(this.key1, this.value1))
        {
            return Tuples.pair(this.key1, this.value1);
        }
        if (predicate.accept(this.key2, this.value2))
        {
            return Tuples.pair(this.key2, this.value2);
        }
        if (predicate.accept(this.key3, this.value3))
        {
            return Tuples.pair(this.key3, this.value3);
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/fixed/TripletonMap.java`
#### Snippet
```java
        int result = 0;

        if (predicate.accept(this.key1, this.value1))
        {
            result |= 1;
        }
        if (predicate.accept(this.key2, this.value2))
        {
            result |= 2;
        }
        if (predicate.accept(this.key3, this.value3))
        {
            result |= 4;
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/immutable/ImmutableMapFactoryImpl.java`
#### Snippet
```java
        if (Objects.equals(key1, key2))
        {
            return this.of(key1, value2, key3, value3, key4, value4);
        }
        if (Objects.equals(key1, key3))
        {
            return this.of(key2, value2, key1, value3, key4, value4);
        }
        if (Objects.equals(key1, key4))
        {
            return this.of(key2, value2, key3, value3, key1, value4);
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/immutable/ImmutableMapSerializationProxy.java`
#### Snippet
```java
        out.writeInt(this.map.size());
        try
        {
            this.map.forEachKeyValue(new CheckedProcedure2<K, V>()
            {
                public void safeValue(K key, V value) throws IOException
                {
                    out.writeObject(key);
                    out.writeObject(value);
                }
            });
        }
        catch (RuntimeException e)
        {
            if (e.getCause() instanceof IOException)
            {
                throw (IOException) e.getCause();
            }
            throw e;
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                Entry<K, V> e = (Entry<K, V>) o;
                while (e != null)
                {
                    K candidate = e.getKey();
                    if (candidate.equals(key))
                    {
                        return e.getValue();
                    }
                    e = e.getNext();
                }
                Entry<K, V> newEntry = new Entry<>(key, value, (Entry<K, V>) o);
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
        this.addToSize(1);
        if (prev != null)
        {
            int localSize = this.size();
            int threshold = (length >> 1) + (length >> 2); // threshold = length * 0.75
            if (localSize + 1 > threshold)
            {
                this.resize(currentArray);
            }
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                Entry<K, V> newEntry;
                if (o == null)
                {
                    if (toCopyEntry.getNext() == null)
                    {
                        newEntry = toCopyEntry; // no need to duplicate
                    }
                    else
                    {
                        newEntry = new Entry<>(toCopyEntry.getKey(), toCopyEntry.getValue());
                    }
                }
                else
                {
                    newEntry = new Entry<>(toCopyEntry.getKey(), toCopyEntry.getValue(), (Entry<K, V>) o);
                }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                Entry<K, V> e = (Entry<K, V>) o;
                while (e != null)
                {
                    Object candidate = e.getKey();
                    if (candidate.equals(key))
                    {
                        return e.getValue();
                    }
                    e = e.getNext();
                }
                if (!createdValue)
                {
                    createdValue = true;
                    newValue = factory.value();
                }
                Entry<K, V> newEntry = new Entry<>(key, newValue, (Entry<K, V>) o);
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                Entry<K, V> e = (Entry<K, V>) o;
                while (e != null)
                {
                    Object candidate = e.getKey();
                    if (candidate.equals(key))
                    {
                        return e.getValue();
                    }
                    e = e.getNext();
                }
                Entry<K, V> newEntry = new Entry<>(key, value, (Entry<K, V>) o);
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                Entry<K, V> e = (Entry<K, V>) o;
                while (e != null)
                {
                    Object candidate = e.getKey();
                    if (candidate.equals(key))
                    {
                        return e.getValue();
                    }
                    e = e.getNext();
                }
                if (!createdValue)
                {
                    createdValue = true;
                    newValue = factory.value(param1, param2, key);
                    if (newValue == null)
                    {
                        return null; // null value means no mapping is required
                    }
                    key = keyTransformer.value(key, newValue);
                }
                Entry<K, V> newEntry = new Entry<>(key, newValue, (Entry<K, V>) o);
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                else if (o != null)
                {
                    Entry<K, V> e = (Entry<K, V>) o;
                    while (e != null)
                    {
                        Object v = e.getValue();
                        if (this.nullSafeEquals(v, value))
                        {
                            return true;
                        }
                        e = e.getNext();
                    }
                }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
        if (o == RESIZED || o == RESIZING)
        {
            return this.slowGet(key, hash, index, currentArray);
        }
        for (Entry<K, V> e = (Entry<K, V>) o; e != null; e = e.getNext())
        {
            Object k;
            if ((k = e.key) == key || key.equals(k))
            {
                return e.value;
            }
        }
        return null;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
            if (o == RESIZED || o == RESIZING)
            {
                currentArray = this.helpWithResizeWhileCurrentIndex(currentArray, index);
            }
            else
            {
                Entry<K, V> e = (Entry<K, V>) o;
                while (e != null)
                {
                    Object candidate = e.getKey();
                    if (candidate.equals(key))
                    {
                        return e.getValue();
                    }
                    e = e.getNext();
                }
                return null;
            }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
            if (o == RESIZED || o == RESIZING)
            {
                currentArray = this.helpWithResizeWhileCurrentIndex(currentArray, index);
            }
            else
            {
                Entry<K, V> e = (Entry<K, V>) o;
                while (e != null)
                {
                    Object candidate = e.getKey();
                    if (candidate.equals(key))
                    {
                        return e;
                    }
                    e = e.getNext();
                }
                return null;
            }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                Entry<K, V> e = (Entry<K, V>) o;
                while (e != null)
                {
                    Object candidate = e.getKey();
                    if (candidate.equals(key))
                    {
                        V oldValue = e.getValue();
                        Entry<K, V> newEntry = new Entry<>(e.getKey(), value, this.createReplacementChainForRemoval((Entry<K, V>) o, e));
                        if (!currentArray.compareAndSet(index, o, newEntry))
                        {
                            //noinspection ContinueStatementWithLabel
                            continue outer;
                        }
                        return oldValue;
                    }
                    e = e.getNext();
                }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
        if (this.size() == 0)
        {
            int threshold = map.size();
            threshold += threshold >> 1; // threshold = length * 0.75

            int capacity = 1;
            while (capacity < threshold)
            {
                capacity <<= 1;
            }
            this.resize(this.table, capacity + 1);
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
        if (original == toRemove)
        {
            return original.getNext();
        }
        Entry<K, V> replacement = null;
        Entry<K, V> e = original;
        while (e != null)
        {
            if (e != toRemove)
            {
                replacement = new Entry<>(e.getKey(), e.getValue(), replacement);
            }
            e = e.getNext();
        }
        return replacement;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
        if (this.isEmpty())
        {
            return "{}";
        }
        Iterator<Map.Entry<K, V>> iterator = this.entrySet().iterator();

        StringBuilder sb = new StringBuilder();
        sb.append('{');
        while (true)
        {
            Map.Entry<K, V> e = iterator.next();
            K key = e.getKey();
            V value = e.getValue();
            sb.append(key == this ? "(this Map)" : key);
            sb.append('=');
            sb.append(value == this ? "(this Map)" : value);
            if (!iterator.hasNext())
            {
                return sb.append('}').toString();
            }
            sb.append(", ");
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
            if (o == RESIZED || o == RESIZING)
            {
                throw new ConcurrentModificationException("Can't serialize while resizing!");
            }
            Entry<K, V> e = (Entry<K, V>) o;
            while (e != null)
            {
                count++;
                out.writeObject(e.getKey());
                out.writeObject(e.getValue());
                e = e.getNext();
            }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
            if (this.next == null && this.index == this.currentState.end && this.todo != null && !this.todo.isEmpty())
            {
                this.currentState = this.todo.remove(this.todo.size() - 1);
                this.index = this.currentState.start;
                this.findNext();
            }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
            Entry<K, V> e = this.next;
            if (e == null)
            {
                throw new NoSuchElementException();
            }

            if ((this.next = e.getNext()) == null)
            {
                this.findNext();
            }
            this.current = e;
            return e;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
            Objects.requireNonNull(col);
            boolean removed = false;
            ValueIterator itr = new ValueIterator();
            while (itr.hasNext())
            {
                if (col.contains(itr.next()))
                {
                    removed |= itr.removeByKeyValue();
                }
            }
            return removed;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
            Objects.requireNonNull(filter);
            boolean removed = false;
            ValueIterator itr = new ValueIterator();
            while (itr.hasNext())
            {
                if (filter.test(itr.next()))
                {
                    removed |= itr.removeByKeyValue();
                }
            }
            return removed;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
            Objects.requireNonNull(col);
            boolean removed = false;

            if (this.size() > col.size())
            {
                for (Object o : col)
                {
                    removed |= this.remove(o);
                }
            }
            else
            {
                for (EntryIterator itr = new EntryIterator(); itr.hasNext(); )
                {
                    if (col.contains(itr.next()))
                    {
                        removed |= itr.removeByKeyValue();
                    }
                }
            }
            return removed;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
            Objects.requireNonNull(filter);
            boolean removed = false;
            EntryIterator itr = new EntryIterator();
            while (itr.hasNext())
            {
                if (filter.test(itr.next()))
                {
                    removed |= itr.removeByKeyValue();
                }
            }
            return removed;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
            if (this.resizers.get() > 0)
            {
                for (int i = 0; i < 16; i++)
                {
                    if (this.resizers.get() == 0)
                    {
                        break;
                    }
                }
                for (int i = 0; i < 16; i++)
                {
                    if (this.resizers.get() == 0)
                    {
                        break;
                    }
                    Thread.yield();
                }
            }
            if (this.resizers.get() > 0)
            {
                synchronized (this)
                {
                    while (this.resizers.get() > 0)
                    {
                        try
                        {
                            this.wait();
                        }
                        catch (InterruptedException e)
                        {
                            // ignore
                        }
                    }
                }
            }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                Entry<K, V> e = (Entry<K, V>) o;
                while (e != null)
                {
                    Object candidate = e.getKey();
                    if (candidate.equals(key))
                    {
                        return e.getValue();
                    }
                    e = e.getNext();
                }
                if (!createdValue)
                {
                    createdValue = true;
                    newValue = function.valueOf(parameter);
                }
                Entry<K, V> newEntry = new Entry<>(key, newValue, (Entry<K, V>) o);
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
                if (!createdValue)
                {
                    createdValue = true;
                    newValue = factory.valueOf(key);
                }
                Entry<K, V> newEntry = new Entry<>(key, newValue, (Entry<K, V>) o);
                if (ConcurrentHashMapUnsafe.casArrayAt(currentArray, index, o, newEntry))
                {
                    this.incrementSizeAndPossiblyResize(currentArray, length, o);
                    return newValue;
                }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
                Entry<K, V> e = (Entry<K, V>) o;
                while (e != null)
                {
                    Object candidate = e.getKey();
                    if (candidate.equals(key))
                    {
                        V oldValue = e.getValue();
                        Entry<K, V> newEntry = new Entry<>(e.getKey(), value, this.createReplacementChainForRemoval((Entry<K, V>) o, e));
                        if (!ConcurrentHashMapUnsafe.casArrayAt(currentArray, index, o, newEntry))
                        {
                            //noinspection ContinueStatementWithLabel
                            continue outer;
                        }
                        return oldValue;
                    }
                    e = e.getNext();
                }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            Object[] newChain = new Object[chain.length + 4];
            System.arraycopy(chain, 0, newChain, 0, chain.length);
            this.table[index + 1] = newChain;
            newChain[chain.length] = UnifiedMap.toSentinelIfNull(key);
            newChain[chain.length + 1] = value;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
        Object[] newChain = new Object[4];
        newChain[0] = this.table[index];
        newChain[1] = this.table[index + 1];
        newChain[2] = UnifiedMap.toSentinelIfNull(key);
        newChain[3] = value;
        this.table[index] = CHAINED_KEY;
        this.table[index + 1] = newChain;
        if (++this.occupied > this.maxSize)
        {
            this.rehash(this.table.length);
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            newChain[2] = UnifiedMap.toSentinelIfNull(key);
            newChain[3] = result;
            this.table[index] = CHAINED_KEY;
            this.table[index + 1] = newChain;
            if (++this.occupied > this.maxSize)
            {
                this.rehash(this.table.length);
            }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
        if (cur != CHAINED_KEY && this.nonNullTableObjectEquals(cur, key))
        {
            V oldValue = (V) this.table[index + 1];
            V newValue = function.valueOf(oldValue);
            this.table[index + 1] = newValue;
            return newValue;
        }
        return this.chainedUpdateValue(key, index, factory, function);
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
        V result = function.valueOf(factory.value());
        newChain[3] = result;
        this.table[index] = CHAINED_KEY;
        this.table[index + 1] = newChain;
        if (++this.occupied > this.maxSize)
        {
            this.rehash(this.table.length);
        }
        return result;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
        if (cur != CHAINED_KEY && this.nonNullTableObjectEquals(cur, key))
        {
            V oldValue = (V) this.table[index + 1];
            V newValue = function.value(oldValue, parameter);
            this.table[index + 1] = newValue;
            return newValue;
        }
        return this.chainedUpdateValueWith(key, index, factory, function, parameter);
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
        V result = function.value(factory.value(), parameter);
        newChain[3] = result;
        this.table[index] = CHAINED_KEY;
        this.table[index + 1] = newChain;
        if (++this.occupied > this.maxSize)
        {
            this.rehash(this.table.length);
        }
        return result;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
                Object[] newChain = new Object[chain.length + 4];
                System.arraycopy(chain, 0, newChain, 0, chain.length);
                newChain[i] = UnifiedMap.toSentinelIfNull(key);
                newChain[i + 1] = value;
                this.table[index + 1] = newChain;
                if (++this.occupied > this.maxSize)
                {
                    this.rehash(this.table.length);
                }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
        int headerSize = 2;
        int sizeInWords = this.table.length + headerSize;
        for (int i = 0; i < this.table.length; i += 2)
        {
            if (this.table[i] == CHAINED_KEY)
            {
                sizeInWords += headerSize + ((Object[]) this.table[i + 1]).length;
            }
        }
        return sizeInWords;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
        int oldLength = this.table.length;
        Object[] old = this.table;
        this.allocate(newCapacity);
        this.occupied = 0;

        for (int i = 0; i < oldLength; i += 2)
        {
            Object cur = old[i];
            if (cur == CHAINED_KEY)
            {
                Object[] chain = (Object[]) old[i + 1];
                for (int j = 0; j < chain.length; j += 2)
                {
                    if (chain[j] != null)
                    {
                        this.put(this.nonSentinel(chain[j]), (V) chain[j + 1]);
                    }
                }
            }
            else if (cur != null)
            {
                this.put(this.nonSentinel(cur), (V) old[i + 1]);
            }
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
        if (cur != null)
        {
            Object val = this.table[index + 1];
            if (cur == CHAINED_KEY)
            {
                return this.getFromChain((Object[]) val, (K) key);
            }
            if (this.nonNullTableObjectEquals(cur, (K) key))
            {
                return (V) val;
            }
        }
        return null;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
        for (int i = 0; i < chain.length; i += 2)
        {
            Object k = chain[i];
            if (k == null)
            {
                return null;
            }
            if (this.nonNullTableObjectEquals(k, key))
            {
                return (V) chain[i + 1];
            }
        }
        return null;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
        if (cur == null)
        {
            return false;
        }
        if (cur != CHAINED_KEY && this.nonNullTableObjectEquals(cur, (K) key))
        {
            return true;
        }
        return cur == CHAINED_KEY && this.chainContainsKey((Object[]) this.table[index + 1], (K) key);
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
        for (int i = 0; i < chain.length; i += 2)
        {
            Object k = chain[i];
            if (k == null)
            {
                return false;
            }
            if (this.nonNullTableObjectEquals(k, key))
            {
                return true;
            }
        }
        return false;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
        for (int i = 0; i < this.table.length; i += 2)
        {
            Object cur = this.table[i];
            if (cur == CHAINED_KEY)
            {
                this.chainedForEachEntry((Object[]) this.table[i + 1], procedure);
            }
            else if (cur != null)
            {
                procedure.value(this.nonSentinel(cur), (V) this.table[i + 1]);
            }
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
        for (int i = 0; i < this.table.length; i += 2)
        {
            Object cur = this.table[i];
            if (cur == CHAINED_KEY)
            {
                Object[] chain = (Object[]) this.table[i + 1];
                return (V) chain[1];
            }
            if (cur != null)
            {
                return (V) this.table[i + 1];
            }
        }
        return null;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
        int sectionSize = this.table.length / sectionCount;
        int start = sectionIndex * sectionSize;
        int end = sectionIndex == sectionCount - 1 ? this.table.length : start + sectionSize;
        if (start % 2 == 0)
        {
            start++;
        }
        for (int i = start; i < end; i += 2)
        {
            Object value = this.table[i];
            if (value instanceof Object[])
            {
                this.chainedForEachValue((Object[]) value, procedure);
            }
            else if (value == null && this.table[i - 1] != null || value != null)
            {
                procedure.value((V) value);
            }
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
        for (int i = 0; i < this.table.length; i += 2)
        {
            Object cur = this.table[i];
            if (cur == CHAINED_KEY)
            {
                this.chainedForEachKey((Object[]) this.table[i + 1], procedure);
            }
            else if (cur != null)
            {
                procedure.value(this.nonSentinel(cur));
            }
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
        for (int i = 0; i < this.table.length; i += 2)
        {
            Object cur = this.table[i];
            if (cur == CHAINED_KEY)
            {
                this.chainedForEachValue((Object[]) this.table[i + 1], procedure);
            }
            else if (cur != null)
            {
                procedure.value((V) this.table[i + 1]);
            }
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
        else if (map instanceof UnsortedMapIterable)
        {
            MapIterable<K, V> mapIterable = (MapIterable<K, V>) map;
            mapIterable.forEachKeyValue(this::put);
        }
        else
        {
            Iterator<? extends Entry<? extends K, ? extends V>> iterator = this.getEntrySetFrom(map).iterator();
            while (iterator.hasNext())
            {
                Entry<? extends K, ? extends V> entry = iterator.next();
                this.put(entry.getKey(), entry.getValue());
            }
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
        Set<? extends Entry<? extends K, ? extends V>> entries = map.entrySet();
        if (entries != null)
        {
            return entries;
        }
        if (map.isEmpty())
        {
            return Sets.immutable.<Entry<K, V>>of().castToSet();
        }
        throw new IllegalStateException("Entry set was null and size was non-zero");
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
        for (int i = 0; i < unifiedMap.table.length; i += 2)
        {
            Object cur = unifiedMap.table[i];
            if (cur == CHAINED_KEY)
            {
                this.copyChain((Object[]) unifiedMap.table[i + 1]);
            }
            else if (cur != null)
            {
                this.put(this.nonSentinel(cur), (V) unifiedMap.table[i + 1]);
            }
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
        if (cur != null)
        {
            Object val = this.table[index + 1];
            if (cur == CHAINED_KEY)
            {
                return this.removeFromChain((Object[]) val, (K) key, index);
            }
            if (this.nonNullTableObjectEquals(cur, (K) key))
            {
                this.table[index] = null;
                this.table[index + 1] = null;
                this.occupied--;
                return (V) val;
            }
        }
        return null;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
        for (int i = 0; i < chain.length; i += 2)
        {
            Object k = chain[i];
            if (k == null)
            {
                return null;
            }
            if (this.nonNullTableObjectEquals(k, key))
            {
                V val = (V) chain[i + 1];
                this.overwriteWithLastElementFromChain(chain, index, i);
                return val;
            }
        }
        return null;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
        int j = chain.length - 2;
        for (; j > i; j -= 2)
        {
            if (chain[j] != null)
            {
                chain[i] = chain[j];
                chain[i + 1] = chain[j + 1];
                break;
            }
        }
        chain[j] = null;
        chain[j + 1] = null;
        if (j == 0)
        {
            this.table[index] = null;
            this.table[index + 1] = null;
        }
        this.occupied--;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
        if (this.table.length <= this.fastCeil(this.occupied / this.loadFactor) << 2)
        {
            return false;
        }

        Object[] temp = this.table;
        this.init(this.fastCeil(this.occupied / this.loadFactor));
        if (this.isEmpty())
        {
            return true;
        }

        int mask = this.table.length - 1;
        for (int j = 0; j < temp.length; j += 2)
        {
            Object key = temp[j];
            if (key == CHAINED_KEY)
            {
                Object[] chain = (Object[]) temp[j + 1];
                for (int i = 0; i < chain.length; i += 2)
                {
                    Object cur = chain[i];
                    if (cur != null)
                    {
                        this.putForTrim((K) cur, (V) chain[i + 1], j, mask);
                    }
                }
            }
            else if (key != null)
            {
                this.putForTrim((K) key, (V) temp[j + 1], j, mask);
            }
        }
        return true;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
        int index = oldIndex & mask;
        Object cur = this.table[index];
        if (cur == null)
        {
            this.table[index] = key;
            this.table[index + 1] = value;
            return;
        }
        this.chainedPutForTrim(key, index, value);
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            Object[] chain = (Object[]) this.table[index + 1];
            for (int i = 0; i < chain.length; i += 2)
            {
                if (chain[i] == null)
                {
                    chain[i] = key;
                    chain[i + 1] = value;
                    return;
                }
            }
            Object[] newChain = new Object[chain.length + 4];
            System.arraycopy(chain, 0, newChain, 0, chain.length);
            this.table[index + 1] = newChain;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
        Object[] newChain = new Object[4];
        newChain[0] = this.table[index];
        newChain[1] = this.table[index + 1];
        newChain[2] = key;
        newChain[3] = value;
        this.table[index] = CHAINED_KEY;
        this.table[index + 1] = newChain;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
        int size = in.readInt();
        this.loadFactor = in.readFloat();
        this.init(Math.max(
                (int) (size / this.loadFactor) + 1,
                DEFAULT_INITIAL_CAPACITY));
        for (int i = 0; i < size; i++)
        {
            this.put((K) in.readObject(), (V) in.readObject());
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
        out.writeInt(this.size());
        out.writeFloat(this.loadFactor);
        for (int i = 0; i < this.table.length; i += 2)
        {
            Object o = this.table[i];
            if (o != null)
            {
                if (o == CHAINED_KEY)
                {
                    this.writeExternalChain(out, (Object[]) this.table[i + 1]);
                }
                else
                {
                    out.writeObject(this.nonSentinel(o));
                    out.writeObject(this.table[i + 1]);
                }
            }
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
        for (int i = 0; i < this.table.length; i += 2)
        {
            Object o = this.table[i];
            if (o != null)
            {
                if (o == CHAINED_KEY)
                {
                    this.writeExternalChain(out, (Object[]) this.table[i + 1]);
                }
                else
                {
                    out.writeObject(this.nonSentinel(o));
                    out.writeObject(this.table[i + 1]);
                }
            }
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
        for (int i = 0; i < chain.length; i += 2)
        {
            Object cur = chain[i];
            if (cur == null)
            {
                return;
            }
            out.writeObject(this.nonSentinel(cur));
            out.writeObject(chain[i + 1]);
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
        int index = 0;
        for (int i = 0; i < this.table.length; i += 2)
        {
            Object cur = this.table[i];
            if (cur == CHAINED_KEY)
            {
                index = this.chainedForEachValueWithIndex((Object[]) this.table[i + 1], objectIntProcedure, index);
            }
            else if (cur != null)
            {
                objectIntProcedure.value((V) this.table[i + 1], index++);
            }
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
        for (int i = 0; i < chain.length; i += 2)
        {
            Object cur = chain[i];
            if (cur == null)
            {
                return index;
            }
            objectIntProcedure.value((V) chain[i + 1], index++);
        }
        return index;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
        for (int i = 0; i < this.table.length; i += 2)
        {
            Object cur = this.table[i];
            if (cur == CHAINED_KEY)
            {
                this.chainedForEachValueWith((Object[]) this.table[i + 1], procedure, parameter);
            }
            else if (cur != null)
            {
                procedure.value((V) this.table[i + 1], parameter);
            }
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
        target.loadFactor = this.loadFactor;
        target.occupied = this.occupied;
        target.allocate(this.table.length >> 1);

        for (int i = 0; i < target.table.length; i += 2)
        {
            target.table[i] = this.table[i];

            if (this.table[i] == CHAINED_KEY)
            {
                Object[] chainedTable = (Object[]) this.table[i + 1];
                Object[] chainedTargetTable = new Object[chainedTable.length];
                for (int j = 0; j < chainedTargetTable.length; j += 2)
                {
                    if (chainedTable[j] != null)
                    {
                        chainedTargetTable[j] = chainedTable[j];
                        chainedTargetTable[j + 1] = function.value(this.nonSentinel(chainedTable[j]), (V) chainedTable[j + 1]);
                    }
                }
                target.table[i + 1] = chainedTargetTable;
            }
            else if (this.table[i] != null)
            {
                target.table[i + 1] = function.value(this.nonSentinel(this.table[i]), (V) this.table[i + 1]);
            }
        }

        return target;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
        for (int i = 0; i < this.table.length; i += 2)
        {
            if (this.table[i] == CHAINED_KEY)
            {
                Object[] chainedTable = (Object[]) this.table[i + 1];
                for (int j = 0; j < chainedTable.length; j += 2)
                {
                    if (chainedTable[j] != null)
                    {
                        V value = (V) chainedTable[j + 1];
                        if (predicate.accept(value))
                        {
                            return value;
                        }
                    }
                }
            }
            else if (this.table[i] != null)
            {
                V value = (V) this.table[i + 1];

                if (predicate.accept(value))
                {
                    return value;
                }
            }
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
        for (int i = 0; i < this.table.length; i += 2)
        {
            if (this.table[i] == CHAINED_KEY)
            {
                Object[] chainedTable = (Object[]) this.table[i + 1];
                for (int j = 0; j < chainedTable.length; j += 2)
                {
                    if (chainedTable[j] != null)
                    {
                        V value = (V) chainedTable[j + 1];
                        if (predicate.accept(value, parameter))
                        {
                            return value;
                        }
                    }
                }
            }
            else if (this.table[i] != null)
            {
                V value = (V) this.table[i + 1];

                if (predicate.accept(value, parameter))
                {
                    return value;
                }
            }
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
        for (int i = 0; i < this.table.length; i += 2)
        {
            if (this.table[i] == CHAINED_KEY)
            {
                Object[] chainedTable = (Object[]) this.table[i + 1];
                for (int j = 0; j < chainedTable.length; j += 2)
                {
                    if (chainedTable[j] != null)
                    {
                        V value = (V) chainedTable[j + 1];
                        if (predicate.accept(value) == expected)
                        {
                            return onShortCircuit;
                        }
                    }
                }
            }
            else if (this.table[i] != null)
            {
                V value = (V) this.table[i + 1];

                if (predicate.accept(value) == expected)
                {
                    return onShortCircuit;
                }
            }
        }

        return atEnd;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
        for (int i = 0; i < this.table.length; i += 2)
        {
            if (this.table[i] == CHAINED_KEY)
            {
                Object[] chainedTable = (Object[]) this.table[i + 1];
                for (int j = 0; j < chainedTable.length; j += 2)
                {
                    if (chainedTable[j] != null)
                    {
                        V value = (V) chainedTable[j + 1];
                        if (predicate.accept(value, parameter) == expected)
                        {
                            return onShortCircuit;
                        }
                    }
                }
            }
            else if (this.table[i] != null)
            {
                V value = (V) this.table[i + 1];

                if (predicate.accept(value, parameter) == expected)
                {
                    return onShortCircuit;
                }
            }
        }

        return atEnd;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            Object[] map = UnifiedMap.this.table;
            int sectionSize = map.length / sectionCount;
            int start = sectionIndex * sectionSize;
            int end = sectionIndex == sectionCount - 1 ? map.length : start + sectionSize;
            if (start % 2 != 0)
            {
                start++;
            }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            StringBuilder stringBuilder = new StringBuilder();
            stringBuilder.append('[');
            Procedure<K> appendStringProcedure = new AppendStringProcedure<>(stringBuilder, ", ");
            this.forEach(appendStringProcedure);
            stringBuilder.append(']');
            return stringBuilder.toString();
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            int pos = this.chainPosition - 2;
            int replacePos = this.chainPosition;
            while (replacePos < chain.length - 2 && chain[replacePos + 2] != null)
            {
                replacePos += 2;
            }
            chain[pos] = chain[replacePos];
            chain[pos + 1] = chain[replacePos + 1];
            chain[replacePos] = null;
            chain[replacePos + 1] = null;
            this.chainPosition = pos;
            this.lastReturned = false;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            if (!this.hasNext())
            {
                throw new NoSuchElementException("next() called, but the iterator is exhausted");
            }
            this.count++;
            Object[] table = UnifiedMap.this.table;
            if (this.chainPosition != 0)
            {
                return this.nextFromChain();
            }
            while (table[this.position] == null)
            {
                this.position += 2;
            }
            Object cur = table[this.position];
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            for (Object obj : collection)
            {
                if (obj instanceof Entry)
                {
                    Entry<?, ?> otherEntry = (Entry<?, ?>) obj;
                    Entry<K, V> thisEntry = this.getEntry(otherEntry);
                    if (thisEntry != null)
                    {
                        retainedCopy.put(thisEntry.getKey(), thisEntry.getValue());
                    }
                }
            }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            StringBuilder stringBuilder = new StringBuilder();
            stringBuilder.append('[');
            Procedure<Entry<K, V>> appendStringProcedure = new AppendStringProcedure<>(stringBuilder, ", ");
            this.forEach(appendStringProcedure);
            stringBuilder.append(']');
            return stringBuilder.toString();
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            Object value = chain[this.chainPosition + 1];
            this.chainPosition += 2;
            if (this.chainPosition >= chain.length
                    || chain[this.chainPosition] == null)
            {
                this.chainPosition = 0;
                this.position += 2;
            }
            this.lastReturned = true;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            if (o == null)
            {
                for (Iterator<V> it = this.iterator(); it.hasNext(); )
                {
                    if (it.next() == null)
                    {
                        it.remove();
                        return true;
                    }
                }
            }
            else
            {
                for (Iterator<V> it = this.iterator(); it.hasNext(); )
                {
                    V o2 = it.next();
                    if (o == o2 || o2.equals(o))
                    {
                        it.remove();
                        return true;
                    }
                }
            }
            return false;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            boolean modified = false;
            Iterator<V> e = this.iterator();
            while (e.hasNext())
            {
                if (!collection.contains(e.next()))
                {
                    e.remove();
                    modified = true;
                }
            }
            return modified;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            StringBuilder stringBuilder = new StringBuilder();
            stringBuilder.append('[');
            Procedure<V> appendStringProcedure = new AppendStringProcedure<>(stringBuilder, ", ");
            this.forEach(appendStringProcedure);
            stringBuilder.append(']');
            return stringBuilder.toString();
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
            V val = (V) chain[this.chainPosition + 1];
            this.chainPosition += 2;
            if (this.chainPosition >= chain.length
                    || chain[this.chainPosition] == null)
            {
                this.chainPosition = 0;
                this.position += 2;
            }
            this.lastReturned = true;
            return val;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/primitive/AbstractMutableBooleanValuesMap.java`
#### Snippet
```java
        if (size <= 0)
        {
            throw new IllegalArgumentException("Size for groups must be positive but was: " + size);
        }
        MutableList<BooleanIterable> result = Lists.mutable.empty();
        if (this.notEmpty())
        {
            BooleanIterator iterator = this.booleanIterator();
            while (iterator.hasNext())
            {
                MutableBooleanBag batch = BooleanBags.mutable.empty();
                for (int i = 0; i < size && iterator.hasNext(); i++)
                {
                    batch.add(iterator.next());
                }
                result.add(batch);
            }
        }
        return result;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/primitive/AbstractMutableBooleanValuesMap.java`
#### Snippet
```java
        if (this.getSentinelValues() != null)
        {
            if (this.getSentinelValues().containsZeroKey)
            {
                array[index] = this.getSentinelValues().zeroValue;
                index++;
            }
            if (this.getSentinelValues().containsOneKey)
            {
                array[index] = this.getSentinelValues().oneValue;
                index++;
            }
        }
        for (int i = 0; i < this.getTableSize(); i++)
        {
            if (this.isNonSentinelAtIndex(i))
            {
                array[index] = this.getValueAtIndex(i);
                index++;
            }
        }

        return array;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/primitive/ObjectBooleanHashMap.java`
#### Snippet
```java
        int index = this.probe(key);
        if (ObjectBooleanHashMap.isNonSentinel(this.keys[index]) && ObjectBooleanHashMap.nullSafeEquals(this.toNonSentinel(this.keys[index]), key))
        {
            return this.values.get(index);
        }
        boolean value = function.booleanValueOf(key);
        this.addKeyValueAtIndex(key, value, index);
        return value;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/primitive/ObjectBooleanHashMapWithHashingStrategy.java`
#### Snippet
```java
        int index = this.probe(key);
        if (ObjectBooleanHashMapWithHashingStrategy.isNonSentinel(this.keys[index]) && this.nullSafeEquals(this.toNonSentinel(this.keys[index]), key))
        {
            return this.values.get(index);
        }
        boolean value = function.booleanValueOf(key);
        this.addKeyValueAtIndex(key, value, index);
        return value;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/ordered/mutable/OrderedMapAdapter.java`
#### Snippet
```java
        Iterator<Entry<K, V>> iterator = this.delegate.entrySet().iterator();
        int countCopy = count;
        while (iterator.hasNext() && countCopy-- > 0)
        {
            Entry<K, V> next = iterator.next();
            output.put(next.getKey(), next.getValue());
        }
        return output;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/ordered/mutable/OrderedMapAdapter.java`
#### Snippet
```java
        Iterator<Entry<K, V>> iterator = this.delegate.entrySet().iterator();
        int start = Math.min(count, this.size());
        if (start == this.size())
        {
            return output;
        }
        int i = 0;
        while (iterator.hasNext())
        {
            if (i >= start)
            {
                Entry<K, V> next = iterator.next();
                output.put(next.getKey(), next.getValue());
            }
            else
            {
                iterator.next();
            }
            i++;
        }
        return output;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/sorted/mutable/SortedMapAdapter.java`
#### Snippet
```java
        int start = Math.min(count, this.size());
        if (start == this.size())
        {
            return output;
        }
        int i = 0;
        while (iterator.hasNext())
        {
            if (i >= start)
            {
                Entry<K, V> next = iterator.next();
                output.put(next.getKey(), next.getValue());
            }
            else
            {
                iterator.next();
            }
            i++;
        }
        return output;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/sorted/mutable/SortedMapAdapter.java`
#### Snippet
```java
        if (count < 0)
        {
            throw new IllegalArgumentException("Count must be greater than zero, but was: " + count);
        }

        MutableSortedMap<K, V> output = SortedMaps.mutable.of(this.comparator());
        Iterator<Entry<K, V>> iterator = this.delegate.entrySet().iterator();
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
            Object[] newChain = new Object[chain.length + 4];
            System.arraycopy(chain, 0, newChain, 0, chain.length);
            this.table[index + 1] = newChain;
            newChain[chain.length] = UnifiedMapWithHashingStrategy.toSentinelIfNull(key);
            newChain[chain.length + 1] = value;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
        Object[] newChain = new Object[4];
        newChain[0] = this.table[index];
        newChain[1] = this.table[index + 1];
        newChain[2] = UnifiedMapWithHashingStrategy.toSentinelIfNull(key);
        newChain[3] = value;
        this.table[index] = CHAINED_KEY;
        this.table[index + 1] = newChain;
        if (++this.occupied > this.maxSize)
        {
            this.rehash(this.table.length);
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
            newChain[2] = UnifiedMapWithHashingStrategy.toSentinelIfNull(key);
            newChain[3] = result;
            this.table[index] = CHAINED_KEY;
            this.table[index + 1] = newChain;
            if (++this.occupied > this.maxSize)
            {
                this.rehash(this.table.length);
            }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
                Object[] newChain = new Object[chain.length + 4];
                System.arraycopy(chain, 0, newChain, 0, chain.length);
                newChain[i] = UnifiedMapWithHashingStrategy.toSentinelIfNull(key);
                newChain[i + 1] = value;
                this.table[index + 1] = newChain;
                if (++this.occupied > this.maxSize)
                {
                    this.rehash(this.table.length);
                }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
            Object[] map = UnifiedMapWithHashingStrategy.this.table;
            int sectionSize = map.length / sectionCount;
            int start = sectionIndex * sectionSize;
            int end = sectionIndex == sectionCount - 1 ? map.length : start + sectionSize;
            if (start % 2 != 0)
            {
                start++;
            }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
            if (!this.hasNext())
            {
                throw new NoSuchElementException("next() called, but the iterator is exhausted");
            }
            this.count++;
            Object[] table = UnifiedMapWithHashingStrategy.this.table;
            if (this.chainPosition != 0)
            {
                return this.nextFromChain();
            }
            while (table[this.position] == null)
            {
                this.position += 2;
            }
            Object cur = table[this.position];
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/parallel/ArrayProcedureFJTaskRunner.java`
#### Snippet
```java
        try
        {
            if (this.combiner.useCombineOne())
            {
                int remainingTaskCount = this.taskCount;
                while (remainingTaskCount > 0)
                {
                    this.combiner.combineOne(this.outputQueue.take());
                    remainingTaskCount--;
                }
            }
            else
            {
                this.latch.await();
            }
        }
        catch (InterruptedException e)
        {
            throw new RuntimeException("Combine failed", e);
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/parallel/ParallelIterate.java`
#### Snippet
```java
            if (this.result.isEmpty())
            {
                thingToCombine.getResult().forEachKeyValue((each, sumCompensation) -> {
                    this.result.put(each, sumCompensation.getOne());
                    this.compensation.put(each, sumCompensation.getTwo());
                });
            }
            else
            {
                thingToCombine.getResult().forEachKeyValue((each, sumCompensation) -> {
                    double sum = this.result.get(each);
                    double currentCompensation = this.compensation.getIfAbsentPut(each, () -> 0.0d) + sumCompensation.getTwo();

                    double adjustedValue = sumCompensation.getOne() - currentCompensation;
                    double nextSum = sum + adjustedValue;
                    this.compensation.put(each, nextSum - sum - adjustedValue);
                    this.result.put(each, nextSum);
                });
            }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/fixed/FixedSizeSetFactoryImpl.java`
#### Snippet
```java
        if (Objects.equals(one, two))
        {
            return this.of(one, three);
        }
        if (Objects.equals(one, three))
        {
            return this.of(one, two);
        }
        if (Objects.equals(two, three))
        {
            return this.of(one, two);
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/fixed/FixedSizeSetFactoryImpl.java`
#### Snippet
```java
        if (Objects.equals(one, two))
        {
            return this.of(one, three, four);
        }
        if (Objects.equals(one, three))
        {
            return this.of(one, two, four);
        }
        if (Objects.equals(one, four))
        {
            return this.of(one, two, three);
        }
        if (Objects.equals(two, three))
        {
            return this.of(one, two, four);
        }
        if (Objects.equals(two, four))
        {
            return this.of(one, two, three);
        }
        if (Objects.equals(three, four))
        {
            return this.of(one, two, three);
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/fixed/FixedSizeSetFactoryImpl.java`
#### Snippet
```java
        T[] itemArray;
        switch (set.size())
        {
            case 0:
                return new EmptySet<>();
            case 1:
                itemArray = (T[]) set.toArray();
                return new SingletonSet<>(itemArray[0]);
            case 2:
                itemArray = (T[]) set.toArray();
                return new DoubletonSet<>(itemArray[0], itemArray[1]);
            case 3:
                itemArray = (T[]) set.toArray();
                return new TripletonSet<>(itemArray[0], itemArray[1], itemArray[2]);
            case 4:
                itemArray = (T[]) set.toArray();
                return new QuadrupletonSet<>(itemArray[0], itemArray[1], itemArray[2], itemArray[3]);
            default:
                return set;
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/immutable/ImmutableSetSerializationProxy.java`
#### Snippet
```java
        out.writeInt(this.set.size());
        try
        {
            this.set.forEach(new CheckedProcedure<T>()
            {
                public void safeValue(T value) throws IOException
                {
                    out.writeObject(value);
                }
            });
        }
        catch (RuntimeException e)
        {
            if (e.getCause() instanceof IOException)
            {
                throw (IOException) e.getCause();
            }
            throw e;
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/immutable/primitive/ImmutableTrueFalseSet.java`
#### Snippet
```java
            switch (this.currentIndex)
            {
                case 0:
                    this.currentIndex++;
                    return false;
                case 1:
                    this.currentIndex++;
                    return true;
                default:
                    throw new NoSuchElementException();
            }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
                if (this.nonNullTableObjectEquals(bucket.zero, key))
                {
                    return false;
                }
                if (bucket.one == null)
                {
                    bucket.one = realKey;
                    if (++this.occupied > this.maxSize)
                    {
                        this.rehash();
                    }
                    return true;
                }
                if (this.nonNullTableObjectEquals(bucket.one, key))
                {
                    return false;
                }
                if (bucket.two == null)
                {
                    bucket.two = realKey;
                    if (++this.occupied > this.maxSize)
                    {
                        this.rehash();
                    }
                    return true;
                }
                if (this.nonNullTableObjectEquals(bucket.two, key))
                {
                    return false;
                }
                if (bucket.three instanceof ChainedBucket)
                {
                    bucket = (ChainedBucket) bucket.three;
                    continue;
                }
                if (bucket.three == null)
                {
                    bucket.three = realKey;
                    if (++this.occupied > this.maxSize)
                    {
                        this.rehash();
                    }
                    return true;
                }
                if (this.nonNullTableObjectEquals(bucket.three, key))
                {
                    return false;
                }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        int oldLength = this.table.length;
        Object[] old = this.table;
        this.allocate(newCapacity);
        this.occupied = 0;

        for (int i = 0; i < oldLength; i++)
        {
            Object oldKey = old[i];
            if (oldKey instanceof ChainedBucket)
            {
                ChainedBucket bucket = (ChainedBucket) oldKey;
                do
                {
                    if (bucket.zero != null)
                    {
                        this.add(this.nonSentinel(bucket.zero));
                    }
                    if (bucket.one == null)
                    {
                        break;
                    }
                    this.add(this.nonSentinel(bucket.one));
                    if (bucket.two == null)
                    {
                        break;
                    }
                    this.add(this.nonSentinel(bucket.two));
                    if (bucket.three != null)
                    {
                        if (bucket.three instanceof ChainedBucket)
                        {
                            bucket = (ChainedBucket) bucket.three;
                            continue;
                        }
                        this.add(this.nonSentinel(bucket.three));
                    }
                    break;
                }
                while (true);
            }
            else if (oldKey != null)
            {
                this.add(this.nonSentinel(oldKey));
            }
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        do
        {
            if (this.nonNullTableObjectEquals(bucket.zero, key))
            {
                return true;
            }
            if (bucket.one == null)
            {
                return false;
            }
            if (this.nonNullTableObjectEquals(bucket.one, key))
            {
                return true;
            }
            if (bucket.two == null)
            {
                return false;
            }
            if (this.nonNullTableObjectEquals(bucket.two, key))
            {
                return true;
            }
            if (bucket.three == null)
            {
                return false;
            }
            if (bucket.three instanceof ChainedBucket)
            {
                bucket = (ChainedBucket) bucket.three;
                continue;
            }
            return this.nonNullTableObjectEquals(bucket.three, key);
        }
        while (true);
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        Object[] set = this.table;
        int sectionSize = set.length / sectionCount;
        int start = sectionSize * sectionIndex;
        int end = sectionIndex == sectionCount - 1 ? set.length : start + sectionSize;
        for (int i = start; i < end; i++)
        {
            Object cur = set[i];
            if (cur != null)
            {
                if (cur instanceof ChainedBucket)
                {
                    this.chainedForEach((ChainedBucket) cur, procedure);
                }
                else
                {
                    procedure.value(this.nonSentinel(cur));
                }
            }
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        do
        {
            procedure.value(this.nonSentinel(bucket.zero));
            if (bucket.one == null)
            {
                return;
            }
            procedure.value(this.nonSentinel(bucket.one));
            if (bucket.two == null)
            {
                return;
            }
            procedure.value(this.nonSentinel(bucket.two));
            if (bucket.three == null)
            {
                return;
            }
            if (bucket.three instanceof ChainedBucket)
            {
                bucket = (ChainedBucket) bucket.three;
                continue;
            }
            procedure.value(this.nonSentinel(bucket.three));
            return;
        }
        while (true);
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        for (int i = 0; i < this.table.length; i++)
        {
            Object cur = this.table[i];
            if (cur instanceof ChainedBucket)
            {
                this.chainedForEachWith((ChainedBucket) cur, procedure, parameter);
            }
            else if (cur != null)
            {
                procedure.value(this.nonSentinel(cur), parameter);
            }
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        do
        {
            procedure.value(this.nonSentinel(bucket.zero), parameter);
            if (bucket.one == null)
            {
                return;
            }
            procedure.value(this.nonSentinel(bucket.one), parameter);
            if (bucket.two == null)
            {
                return;
            }
            procedure.value(this.nonSentinel(bucket.two), parameter);
            if (bucket.three == null)
            {
                return;
            }
            if (bucket.three instanceof ChainedBucket)
            {
                bucket = (ChainedBucket) bucket.three;
                continue;
            }
            procedure.value(this.nonSentinel(bucket.three), parameter);
            return;
        }
        while (true);
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        int count = 0;
        for (int i = 0; i < this.table.length; i++)
        {
            Object cur = this.table[i];
            if (cur instanceof ChainedBucket)
            {
                count = this.chainedForEachWithIndex((ChainedBucket) cur, objectIntProcedure, count);
            }
            else if (cur != null)
            {
                objectIntProcedure.value(this.nonSentinel(cur), count++);
            }
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        do
        {
            procedure.value(this.nonSentinel(bucket.zero), count++);
            if (bucket.one == null)
            {
                return count;
            }
            procedure.value(this.nonSentinel(bucket.one), count++);
            if (bucket.two == null)
            {
                return count;
            }
            procedure.value(this.nonSentinel(bucket.two), count++);
            if (bucket.three == null)
            {
                return count;
            }
            if (bucket.three instanceof ChainedBucket)
            {
                bucket = (ChainedBucket) bucket.three;
                continue;
            }
            procedure.value(this.nonSentinel(bucket.three), count++);
            return count;
        }
        while (true);
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        for (int i = 0; i < this.table.length; i++)
        {
            Object cur = this.table[i];
            if (cur instanceof ChainedBucket)
            {
                return this.nonSentinel(((ChainedBucket) cur).zero);
            }
            if (cur != null)
            {
                return this.nonSentinel(cur);
            }
        }
        return null;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        for (int i = this.table.length - 1; i >= 0; i--)
        {
            Object cur = this.table[i];
            if (cur instanceof ChainedBucket)
            {
                return this.getLast((ChainedBucket) cur);
            }
            if (cur != null)
            {
                return this.nonSentinel(cur);
            }
        }
        return null;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        while (bucket.three instanceof ChainedBucket)
        {
            bucket = (ChainedBucket) bucket.three;
        }

        if (bucket.three != null)
        {
            return this.nonSentinel(bucket.three);
        }
        if (bucket.two != null)
        {
            return this.nonSentinel(bucket.two);
        }
        if (bucket.one != null)
        {
            return this.nonSentinel(bucket.one);
        }
        assert bucket.zero != null;
        return this.nonSentinel(bucket.zero);
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        MutableList<T> positiveResult = Lists.mutable.empty();
        MutableList<T> negativeResult = Lists.mutable.empty();
        this.forEachWith((each, parm) -> (predicate.accept(each, parm) ? positiveResult : negativeResult).add(each), parameter);
        return Tuples.twin(positiveResult, negativeResult);
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        for (int i = start; i < end; i++)
        {
            Object cur = this.table[i];
            if (cur instanceof ChainedBucket)
            {
                Object chainedDetect = this.chainedDetect((ChainedBucket) cur, predicate);
                if (chainedDetect != null)
                {
                    return this.nonSentinel(chainedDetect);
                }
            }
            else if (cur != null)
            {
                T each = this.nonSentinel(cur);
                if (predicate.accept(each))
                {
                    return each;
                }
            }
        }
        return null;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        for (int i = start; i < end; i++)
        {
            Object cur = this.table[i];
            if (cur instanceof ChainedBucket)
            {
                Object chainedDetect = this.chainedDetect((ChainedBucket) cur, predicate);
                if (chainedDetect != null)
                {
                    return Optional.of(this.nonSentinel(chainedDetect));
                }
            }
            else if (cur != null)
            {
                T each = this.nonSentinel(cur);
                if (predicate.accept(each))
                {
                    return Optional.of(each);
                }
            }
        }
        return Optional.empty();
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        do
        {
            if (predicate.accept(this.nonSentinel(bucket.zero)))
            {
                return bucket.zero;
            }
            if (bucket.one == null)
            {
                return null;
            }
            if (predicate.accept(this.nonSentinel(bucket.one)))
            {
                return bucket.one;
            }
            if (bucket.two == null)
            {
                return null;
            }
            if (predicate.accept(this.nonSentinel(bucket.two)))
            {
                return bucket.two;
            }
            if (bucket.three == null)
            {
                return null;
            }
            if (bucket.three instanceof ChainedBucket)
            {
                bucket = (ChainedBucket) bucket.three;
                continue;
            }
            if (predicate.accept(this.nonSentinel(bucket.three)))
            {
                return bucket.three;
            }
            return null;
        }
        while (true);
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        for (int i = start; i < end; i++)
        {
            Object cur = this.table[i];
            if (cur instanceof ChainedBucket)
            {
                if (this.chainedShortCircuit((ChainedBucket) cur, predicate, expected))
                {
                    return onShortCircuit;
                }
            }
            else if (cur != null)
            {
                T each = this.nonSentinel(cur);
                if (predicate.accept(each) == expected)
                {
                    return onShortCircuit;
                }
            }
        }
        return atEnd;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        do
        {
            if (predicate.accept(this.nonSentinel(bucket.zero)) == expected)
            {
                return true;
            }
            if (bucket.one == null)
            {
                return false;
            }
            if (predicate.accept(this.nonSentinel(bucket.one)) == expected)
            {
                return true;
            }
            if (bucket.two == null)
            {
                return false;
            }
            if (predicate.accept(this.nonSentinel(bucket.two)) == expected)
            {
                return true;
            }
            if (bucket.three == null)
            {
                return false;
            }
            if (bucket.three instanceof ChainedBucket)
            {
                bucket = (ChainedBucket) bucket.three;
                continue;
            }
            return predicate.accept(this.nonSentinel(bucket.three)) == expected;
        }
        while (true);
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        for (int i = 0; i < this.table.length; i++)
        {
            Object cur = this.table[i];
            if (cur instanceof ChainedBucket)
            {
                if (this.chainedShortCircuitWith((ChainedBucket) cur, predicate2, parameter, expected))
                {
                    return onShortCircuit;
                }
            }
            else if (cur != null)
            {
                T each = this.nonSentinel(cur);
                if (predicate2.accept(each, parameter) == expected)
                {
                    return onShortCircuit;
                }
            }
        }
        return atEnd;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        do
        {
            if (predicate.accept(this.nonSentinel(bucket.zero), parameter) == expected)
            {
                return true;
            }
            if (bucket.one == null)
            {
                return false;
            }
            if (predicate.accept(this.nonSentinel(bucket.one), parameter) == expected)
            {
                return true;
            }
            if (bucket.two == null)
            {
                return false;
            }
            if (predicate.accept(this.nonSentinel(bucket.two), parameter) == expected)
            {
                return true;
            }
            if (bucket.three == null)
            {
                return false;
            }
            if (bucket.three instanceof ChainedBucket)
            {
                bucket = (ChainedBucket) bucket.three;
                continue;
            }
            return predicate.accept(this.nonSentinel(bucket.three), parameter) == expected;
        }
        while (true);
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        if (size > this.maxSize)
        {
            size = (int) (size / this.loadFactor) + 1;
            int capacity = Integer.highestOneBit(size);
            if (size != capacity)
            {
                capacity <<= 1;
            }
            this.rehash(capacity);
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        boolean changed = false;
        for (int i = 0; i < unifiedset.table.length; i++)
        {
            Object cur = unifiedset.table[i];
            if (cur instanceof ChainedBucket)
            {
                changed |= this.copyChain((ChainedBucket) cur);
            }
            else if (cur != null)
            {
                changed |= this.add(this.nonSentinel(cur));
            }
        }
        return changed;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        boolean changed = false;
        do
        {
            changed |= this.add(this.nonSentinel(bucket.zero));
            if (bucket.one == null)
            {
                return changed;
            }
            changed |= this.add(this.nonSentinel(bucket.one));
            if (bucket.two == null)
            {
                return changed;
            }
            changed |= this.add(this.nonSentinel(bucket.two));
            if (bucket.three == null)
            {
                return changed;
            }
            if (bucket.three instanceof ChainedBucket)
            {
                bucket = (ChainedBucket) bucket.three;
                continue;
            }
            changed |= this.add(this.nonSentinel(bucket.three));
            return changed;
        }
        while (true);
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        if (cur instanceof ChainedBucket)
        {
            return this.removeFromChain((ChainedBucket) cur, (T) key, index);
        }
        if (this.nonNullTableObjectEquals(cur, (T) key))
        {
            this.table[index] = null;
            this.occupied--;
            return true;
        }
        return false;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        if (this.nonNullTableObjectEquals(bucket.zero, key))
        {
            bucket.zero = bucket.removeLast(0);
            if (bucket.zero == null)
            {
                this.table[index] = null;
            }
            this.occupied--;
            return true;
        }
        if (bucket.one == null)
        {
            return false;
        }
        if (this.nonNullTableObjectEquals(bucket.one, key))
        {
            bucket.one = bucket.removeLast(1);
            this.occupied--;
            return true;
        }
        if (bucket.two == null)
        {
            return false;
        }
        if (this.nonNullTableObjectEquals(bucket.two, key))
        {
            bucket.two = bucket.removeLast(2);
            this.occupied--;
            return true;
        }
        if (bucket.three == null)
        {
            return false;
        }
        if (bucket.three instanceof ChainedBucket)
        {
            return this.removeDeepChain(bucket, key);
        }
        if (this.nonNullTableObjectEquals(bucket.three, key))
        {
            bucket.three = bucket.removeLast(3);
            this.occupied--;
            return true;
        }
        return false;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        do
        {
            ChainedBucket bucket = (ChainedBucket) oldBucket.three;
            if (this.nonNullTableObjectEquals(bucket.zero, key))
            {
                bucket.zero = bucket.removeLast(0);
                if (bucket.zero == null)
                {
                    oldBucket.three = null;
                }
                this.occupied--;
                return true;
            }
            if (bucket.one == null)
            {
                return false;
            }
            if (this.nonNullTableObjectEquals(bucket.one, key))
            {
                bucket.one = bucket.removeLast(1);
                this.occupied--;
                return true;
            }
            if (bucket.two == null)
            {
                return false;
            }
            if (this.nonNullTableObjectEquals(bucket.two, key))
            {
                bucket.two = bucket.removeLast(2);
                this.occupied--;
                return true;
            }
            if (bucket.three == null)
            {
                return false;
            }
            if (bucket.three instanceof ChainedBucket)
            {
                oldBucket = bucket;
                continue;
            }
            if (this.nonNullTableObjectEquals(bucket.three, key))
            {
                bucket.three = bucket.removeLast(3);
                this.occupied--;
                return true;
            }
            return false;
        }
        while (true);
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        if (this.table.length <= (this.fastCeil(this.occupied / this.loadFactor) << 1))
        {
            return false;
        }

        Object[] temp = this.table;
        this.init(this.fastCeil(this.occupied / this.loadFactor));
        if (this.isEmpty())
        {
            return true;
        }

        int mask = this.table.length - 1;
        for (int j = 0; j < temp.length; j++)
        {
            Object cur = temp[j];
            if (cur instanceof ChainedBucket)
            {
                ChainedBucket bucket = (ChainedBucket) cur;
                this.chainedTrimToSize(bucket, j, mask);
            }
            else if (cur != null)
            {
                this.addForTrim(cur, j, mask);
            }
        }
        return true;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        do
        {
            this.addForTrim(bucket.zero, oldIndex, mask);
            if (bucket.one == null)
            {
                return;
            }
            this.addForTrim(bucket.one, oldIndex, mask);
            if (bucket.two == null)
            {
                return;
            }
            this.addForTrim(bucket.two, oldIndex, mask);
            if (bucket.three == null)
            {
                return;
            }
            if (bucket.three instanceof ChainedBucket)
            {
                bucket = (ChainedBucket) bucket.three;
                continue;
            }
            this.addForTrim(bucket.three, oldIndex, mask);
            return;
        }
        while (true);
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        if (this.table[index] instanceof ChainedBucket)
        {
            ChainedBucket bucket = (ChainedBucket) this.table[index];
            do
            {
                if (bucket.one == null)
                {
                    bucket.one = key;
                    return;
                }
                if (bucket.two == null)
                {
                    bucket.two = key;
                    return;
                }
                if (bucket.three instanceof ChainedBucket)
                {
                    bucket = (ChainedBucket) bucket.three;
                    continue;
                }
                if (bucket.three == null)
                {
                    bucket.three = key;
                    return;
                }
                bucket.three = new ChainedBucket(bucket.three, key);
                return;
            }
            while (true);
        }
        ChainedBucket newBucket = new ChainedBucket(this.table[index], key);
        this.table[index] = newBucket;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        int size = in.readInt();
        this.loadFactor = in.readFloat();
        this.init(Math.max((int) (size / this.loadFactor) + 1, DEFAULT_INITIAL_CAPACITY));
        for (int i = 0; i < size; i++)
        {
            this.add((T) in.readObject());
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        out.writeInt(this.size());
        out.writeFloat(this.loadFactor);
        for (int i = 0; i < this.table.length; i++)
        {
            Object o = this.table[i];
            if (o != null)
            {
                if (o instanceof ChainedBucket)
                {
                    this.writeExternalChain(out, (ChainedBucket) o);
                }
                else
                {
                    out.writeObject(this.nonSentinel(o));
                }
            }
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/strategy/mutable/UnifiedSetWithHashingStrategy.java`
#### Snippet
```java
        for (int i = 0; i < this.table.length; i++)
        {
            Object o = this.table[i];
            if (o != null)
            {
                if (o instanceof ChainedBucket)
                {
                    this.writeExternalChain(out, (ChainedBucket) o);
                }
                else
                {
                    out.writeObject(this.nonSentinel(o));
                }
            }
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        do
        {
            out.writeObject(this.nonSentinel(bucket.zero));
            if (bucket.one == null)
            {
                return;
            }
            out.writeObject(this.nonSentinel(bucket.one));
            if (bucket.two == null)
            {
                return;
            }
            out.writeObject(this.nonSentinel(bucket.two));
            if (bucket.three == null)
            {
                return;
            }
            if (bucket.three instanceof ChainedBucket)
            {
                bucket = (ChainedBucket) bucket.three;
                continue;
            }
            out.writeObject(this.nonSentinel(bucket.three));
            return;
        }
        while (true);
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        int index = this.index(key);

        Object cur = this.table[index];
        if (cur == null)
        {
            return;
        }
        if (cur instanceof ChainedBucket)
        {
            this.addIfFoundFromChain((ChainedBucket) cur, key, other);
            return;
        }
        if (this.nonNullTableObjectEquals(cur, key))
        {
            other.add(this.nonSentinel(cur));
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        do
        {
            if (this.nonNullTableObjectEquals(bucket.zero, key))
            {
                other.add(this.nonSentinel(bucket.zero));
                return;
            }
            if (bucket.one == null)
            {
                return;
            }
            if (this.nonNullTableObjectEquals(bucket.one, key))
            {
                other.add(this.nonSentinel(bucket.one));
                return;
            }
            if (bucket.two == null)
            {
                return;
            }
            if (this.nonNullTableObjectEquals(bucket.two, key))
            {
                other.add(this.nonSentinel(bucket.two));
                return;
            }
            if (bucket.three == null)
            {
                return;
            }
            if (bucket.three instanceof ChainedBucket)
            {
                bucket = (ChainedBucket) bucket.three;
                continue;
            }
            if (this.nonNullTableObjectEquals(bucket.three, key))
            {
                other.add(this.nonSentinel(bucket.three));
                return;
            }
            return;
        }
        while (true);
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        for (Object key : iterable)
        {
            this.addIfFound((T) key, retainedCopy);
        }
        if (retainedCopy.size() < this.size())
        {
            this.maxSize = retainedCopy.maxSize;
            this.occupied = retainedCopy.occupied;
            this.table = retainedCopy.table;
            return true;
        }
        return false;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        boolean result = false;
        Iterator<T> e = this.iterator();
        while (e.hasNext())
        {
            if (!collection.contains(e.next()))
            {
                e.remove();
                result = true;
            }
        }
        return result;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        Object[] table = this.table;
        int count = 0;
        for (int i = 0; i < table.length; i++)
        {
            Object cur = table[i];
            if (cur != null)
            {
                if (cur instanceof ChainedBucket)
                {
                    ChainedBucket bucket = (ChainedBucket) cur;
                    count = this.copyBucketToArray(result, bucket, count);
                }
                else
                {
                    result[count++] = this.nonSentinel(cur);
                }
            }
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        do
        {
            result[count++] = this.nonSentinel(bucket.zero);
            if (bucket.one == null)
            {
                break;
            }
            result[count++] = this.nonSentinel(bucket.one);
            if (bucket.two == null)
            {
                break;
            }
            result[count++] = this.nonSentinel(bucket.two);
            if (bucket.three == null)
            {
                break;
            }
            if (bucket.three instanceof ChainedBucket)
            {
                bucket = (ChainedBucket) bucket.three;
                continue;
            }
            result[count++] = this.nonSentinel(bucket.three);
            break;
        }
        while (true);
        return count;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        int size = this.size();
        T[] result = array.length < size
                ? (T[]) Array.newInstance(array.getClass().getComponentType(), size)
                : array;

        this.copyToArray(result);
        if (size < result.length)
        {
            result[size] = null;
        }
        return result;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
            if (this.chainPosition != 0)
            {
                return this.nextFromChain();
            }
            while (table[this.position] == null)
            {
                this.position++;
            }
            Object cur = table[this.position];
            if (cur instanceof ChainedBucket)
            {
                return this.nextFromChain();
            }
            this.position++;
            this.lastReturned = true;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
            if (i > 3)
            {
                this.removeLongChain(this, i - 3);
            }
            else
            {
                switch (i)
                {
                    case 0:
                        this.zero = this.removeLast(0);
                        return;
                    case 1:
                        this.one = this.removeLast(1);
                        return;
                    case 2:
                        this.two = this.removeLast(2);
                        return;
                    case 3:
                        if (this.three instanceof ChainedBucket)
                        {
                            this.removeLongChain(this, i - 3);
                            return;
                        }
                        this.three = null;
                        return;
                    default:
                        throw new AssertionError();
                }
            }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
            do
            {
                ChainedBucket bucket = (ChainedBucket) oldBucket.three;
                switch (i)
                {
                    case 0:
                        bucket.zero = bucket.removeLast(0);
                        return;
                    case 1:
                        bucket.one = bucket.removeLast(1);
                        return;
                    case 2:
                        bucket.two = bucket.removeLast(2);
                        return;
                    case 3:
                        if (bucket.three instanceof ChainedBucket)
                        {
                            i -= 3;
                            oldBucket = bucket;
                            continue;
                        }
                        bucket.three = null;
                        return;
                    default:
                        if (bucket.three instanceof ChainedBucket)
                        {
                            i -= 3;
                            oldBucket = bucket;
                            continue;
                        }
                        throw new AssertionError();
                }
            }
            while (true);
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
            ChainedBucket bucket = this;
            while (i > 3 && bucket.three instanceof ChainedBucket)
            {
                bucket = (ChainedBucket) bucket.three;
                i -= 3;
            }
            do
            {
                switch (i)
                {
                    case 0:
                        return bucket.zero;
                    case 1:
                        return bucket.one;
                    case 2:
                        return bucket.two;
                    case 3:
                        if (bucket.three instanceof ChainedBucket)
                        {
                            i -= 3;
                            bucket = (ChainedBucket) bucket.three;
                            continue;
                        }
                        return bucket.three;
                    case 4:
                        return null; // this happens when a bucket is exactly full, and we're iterating
                    default:
                        throw new AssertionError();
                }
            }
            while (true);
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
            if (this.three instanceof ChainedBucket)
            {
                return this.removeLast(this);
            }
            if (this.three != null)
            {
                Object result = this.three;
                this.three = null;
                return cur == 3 ? null : result;
            }
            if (this.two != null)
            {
                Object result = this.two;
                this.two = null;
                return cur == 2 ? null : result;
            }
            if (this.one != null)
            {
                Object result = this.one;
                this.one = null;
                return cur == 1 ? null : result;
            }
            this.zero = null;
            return null;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
            do
            {
                ChainedBucket bucket = (ChainedBucket) oldBucket.three;
                if (bucket.three instanceof ChainedBucket)
                {
                    oldBucket = bucket;
                    continue;
                }
                if (bucket.three != null)
                {
                    Object result = bucket.three;
                    bucket.three = null;
                    return result;
                }
                if (bucket.two != null)
                {
                    Object result = bucket.two;
                    bucket.two = null;
                    return result;
                }
                if (bucket.one != null)
                {
                    Object result = bucket.one;
                    bucket.one = null;
                    return result;
                }
                Object result = bucket.zero;
                oldBucket.three = null;
                return result;
            }
            while (true);
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
            ChainedBucket result = new ChainedBucket();
            ChainedBucket dest = result;
            ChainedBucket src = this;
            do
            {
                dest.zero = src.zero;
                dest.one = src.one;
                dest.two = src.two;
                if (src.three instanceof ChainedBucket)
                {
                    dest.three = new ChainedBucket();
                    src = (ChainedBucket) src.three;
                    dest = (ChainedBucket) dest.three;
                    continue;
                }
                dest.three = src.three;
                return result;
            }
            while (true);
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        int index = this.index(key);
        Object cur = this.table[index];

        if (cur == null)
        {
            return null;
        }
        if (cur instanceof ChainedBucket)
        {
            return this.chainedGet(key, (ChainedBucket) cur);
        }
        if (this.nonNullTableObjectEquals(cur, key))
        {
            return (T) cur;
        }
        return null;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        do
        {
            if (this.nonNullTableObjectEquals(bucket.zero, key))
            {
                return this.nonSentinel(bucket.zero);
            }
            if (bucket.one == null)
            {
                return null;
            }
            if (this.nonNullTableObjectEquals(bucket.one, key))
            {
                return this.nonSentinel(bucket.one);
            }
            if (bucket.two == null)
            {
                return null;
            }
            if (this.nonNullTableObjectEquals(bucket.two, key))
            {
                return this.nonSentinel(bucket.two);
            }
            if (bucket.three instanceof ChainedBucket)
            {
                bucket = (ChainedBucket) bucket.three;
                continue;
            }
            if (bucket.three == null)
            {
                return null;
            }
            if (this.nonNullTableObjectEquals(bucket.three, key))
            {
                return this.nonSentinel(bucket.three);
            }
            return null;
        }
        while (true);
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
                if (this.nonNullTableObjectEquals(bucket.zero, key))
                {
                    return this.nonSentinel(bucket.zero);
                }
                if (bucket.one == null)
                {
                    bucket.one = realKey;
                    if (++this.occupied > this.maxSize)
                    {
                        this.rehash();
                    }
                    return key;
                }
                if (this.nonNullTableObjectEquals(bucket.one, key))
                {
                    return this.nonSentinel(bucket.one);
                }
                if (bucket.two == null)
                {
                    bucket.two = realKey;
                    if (++this.occupied > this.maxSize)
                    {
                        this.rehash();
                    }
                    return key;
                }
                if (this.nonNullTableObjectEquals(bucket.two, key))
                {
                    return this.nonSentinel(bucket.two);
                }
                if (bucket.three instanceof ChainedBucket)
                {
                    bucket = (ChainedBucket) bucket.three;
                    continue;
                }
                if (bucket.three == null)
                {
                    bucket.three = realKey;
                    if (++this.occupied > this.maxSize)
                    {
                        this.rehash();
                    }
                    return key;
                }
                if (this.nonNullTableObjectEquals(bucket.three, key))
                {
                    return this.nonSentinel(bucket.three);
                }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/strategy/mutable/UnifiedSetWithHashingStrategy.java`
#### Snippet
```java
            if (bucket.three instanceof ChainedBucket)
            {
                bucket = (ChainedBucket) bucket.three;
                continue;
            }
            if (bucket.three == null)
            {
                bucket.three = realKey;
                if (++this.occupied > this.maxSize)
                {
                    this.rehash();
                }
                return key;
            }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        int index = this.index(key);
        Object cur = this.table[index];
        if (cur == null)
        {
            return null;
        }
        if (cur instanceof ChainedBucket)
        {
            return this.removeFromChainForPool((ChainedBucket) cur, key, index);
        }
        if (this.nonNullTableObjectEquals(cur, key))
        {
            this.table[index] = null;
            this.occupied--;
            return this.nonSentinel(cur);
        }
        return null;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        if (this.nonNullTableObjectEquals(bucket.zero, key))
        {
            Object result = bucket.zero;
            bucket.zero = bucket.removeLast(0);
            if (bucket.zero == null)
            {
                this.table[index] = null;
            }
            this.occupied--;
            return this.nonSentinel(result);
        }
        if (bucket.one == null)
        {
            return null;
        }
        if (this.nonNullTableObjectEquals(bucket.one, key))
        {
            Object result = bucket.one;
            bucket.one = bucket.removeLast(1);
            this.occupied--;
            return this.nonSentinel(result);
        }
        if (bucket.two == null)
        {
            return null;
        }
        if (this.nonNullTableObjectEquals(bucket.two, key))
        {
            Object result = bucket.two;
            bucket.two = bucket.removeLast(2);
            this.occupied--;
            return this.nonSentinel(result);
        }
        if (bucket.three == null)
        {
            return null;
        }
        if (bucket.three instanceof ChainedBucket)
        {
            return this.removeDeepChainForPool(bucket, key);
        }
        if (this.nonNullTableObjectEquals(bucket.three, key))
        {
            Object result = bucket.three;
            bucket.three = bucket.removeLast(3);
            this.occupied--;
            return this.nonSentinel(result);
        }
        return null;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
        do
        {
            ChainedBucket bucket = (ChainedBucket) oldBucket.three;
            if (this.nonNullTableObjectEquals(bucket.zero, key))
            {
                Object result = bucket.zero;
                bucket.zero = bucket.removeLast(0);
                if (bucket.zero == null)
                {
                    oldBucket.three = null;
                }
                this.occupied--;
                return this.nonSentinel(result);
            }
            if (bucket.one == null)
            {
                return null;
            }
            if (this.nonNullTableObjectEquals(bucket.one, key))
            {
                Object result = bucket.one;
                bucket.one = bucket.removeLast(1);
                this.occupied--;
                return this.nonSentinel(result);
            }
            if (bucket.two == null)
            {
                return null;
            }
            if (this.nonNullTableObjectEquals(bucket.two, key))
            {
                Object result = bucket.two;
                bucket.two = bucket.removeLast(2);
                this.occupied--;
                return this.nonSentinel(result);
            }
            if (bucket.three == null)
            {
                return null;
            }
            if (bucket.three instanceof ChainedBucket)
            {
                oldBucket = bucket;
                continue;
            }
            if (this.nonNullTableObjectEquals(bucket.three, key))
            {
                Object result = bucket.three;
                bucket.three = bucket.removeLast(3);
                this.occupied--;
                return this.nonSentinel(result);
            }
            return null;
        }
        while (true);
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/primitive/ByteHashSet.java`
#### Snippet
```java
        if (value <= MAX_BYTE_GROUP_1)
        {
            return ((this.bitGroup1 >>> (byte) ((value + 1) * -1)) & 1L) != 0;
        }
        if (value <= MAX_BYTE_GROUP_2)
        {
            return ((this.bitGroup2 >>> (byte) ((value + 1) * -1)) & 1L) != 0;
        }
        if (value <= MAX_BYTE_GROUP_3)
        {
            return ((this.bitGroup3 >>> value) & 1L) != 0;
        }

        return ((this.bitGroup4 >>> value) & 1L) != 0;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/primitive/ByteHashSet.java`
#### Snippet
```java
        byte[] array = new byte[this.size()];
        int index = 0;

        ByteIterator iterator = this.byteIterator();

        while (iterator.hasNext())
        {
            byte nextByte = iterator.next();
            array[index] = nextByte;
            index++;
        }

        return array;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/primitive/ByteHashSet.java`
#### Snippet
```java
        if (array.length < this.size())
        {
            array = new byte[this.size()];
        }
        int index = 0;

        ByteIterator iterator = this.byteIterator();

        while (iterator.hasNext())
        {
            byte nextByte = iterator.next();
            array[index] = nextByte;
            index++;
        }

        return array;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/primitive/ByteHashSet.java`
#### Snippet
```java
        long bitGroup1 = this.bitGroup1;
        while (bitGroup1 != 0L)
        {
            byte value = (byte) Long.numberOfTrailingZeros(bitGroup1);
            procedure.value((byte) ((value + 65) * -1));
            bitGroup1 &= ~(1L << (byte) (value + 64));
        }

        long bitGroup2 = this.bitGroup2;
        while (bitGroup2 != 0L)
        {
            byte value = (byte) Long.numberOfTrailingZeros(bitGroup2);
            procedure.value((byte) ((value + 1) * -1));
            bitGroup2 &= ~(1L << value);
        }

        long bitGroup3 = this.bitGroup3;
        while (bitGroup3 != 0L)
        {
            byte value = (byte) Long.numberOfTrailingZeros(bitGroup3);
            procedure.value(value);
            bitGroup3 &= ~(1L << value);
        }

        long bitGroup4 = this.bitGroup4;
        while (bitGroup4 != 0L)
        {
            byte value = (byte) Long.numberOfTrailingZeros(bitGroup4);
            procedure.value((byte) (value + 64));
            bitGroup4 &= ~(1L << (byte) (value + 64));
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/primitive/ByteHashSet.java`
#### Snippet
```java
            byte max = 0;

            if (this.bitGroup4 != 0L)
            {
                //the highest has to be from this
                max = (byte) (127 - Long.numberOfLeadingZeros(this.bitGroup4));
            }
            else if (this.bitGroup3 != 0L)
            {
                max = (byte) (63 - Long.numberOfLeadingZeros(this.bitGroup3));
            }
            else if (this.bitGroup2 != 0L)
            {
                max = (byte) ((Long.numberOfTrailingZeros(this.bitGroup2) + 1) * -1);
            }
            else if (this.bitGroup1 != 0L)
            {
                max = (byte) ((Long.numberOfTrailingZeros(this.bitGroup1) + 65) * -1);
            }

            return max;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/primitive/ByteHashSet.java`
#### Snippet
```java
        if (this.bitGroup4 != 0L)
        {
            //the highest has to be from this
            max = (byte) (127 - Long.numberOfLeadingZeros(this.bitGroup4));
        }
        else if (this.bitGroup3 != 0L)
        {
            max = (byte) (63 - Long.numberOfLeadingZeros(this.bitGroup3));
        }
        else if (this.bitGroup2 != 0L)
        {
            max = (byte) ((Long.numberOfTrailingZeros(this.bitGroup2) + 1) * -1);
        }
        else if (this.bitGroup1 != 0L)
        {
            max = (byte) ((Long.numberOfTrailingZeros(this.bitGroup1) + 65) * -1);
        }

        return max;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/primitive/ByteHashSet.java`
#### Snippet
```java
            byte min = 0;

            if (this.bitGroup1 != 0L)
            {
                //the minimum has to be from this
                min = (byte) (128 - Long.numberOfLeadingZeros(this.bitGroup1));
                min *= -1;
            }
            else if (this.bitGroup2 != 0L)
            {
                min = (byte) ((64 - Long.numberOfLeadingZeros(this.bitGroup2)) * -1);
            }
            else if (this.bitGroup3 != 0L)
            {
                min = (byte) Long.numberOfTrailingZeros(this.bitGroup3);
            }
            else if (this.bitGroup4 != 0L)
            {
                min = (byte) (Long.numberOfTrailingZeros(this.bitGroup4) + 64);
            }

            return min;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/primitive/ByteHashSet.java`
#### Snippet
```java
        if (this.bitGroup1 != 0L)
        {
            //the minimum has to be from this
            min = (byte) (128 - Long.numberOfLeadingZeros(this.bitGroup1));
            min *= -1;
        }
        else if (this.bitGroup2 != 0L)
        {
            min = (byte) ((64 - Long.numberOfLeadingZeros(this.bitGroup2)) * -1);
        }
        else if (this.bitGroup3 != 0L)
        {
            min = (byte) Long.numberOfTrailingZeros(this.bitGroup3);
        }
        else if (this.bitGroup4 != 0L)
        {
            min = (byte) (Long.numberOfTrailingZeros(this.bitGroup4) + 64);
        }

        return min;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/primitive/ByteHashSet.java`
#### Snippet
```java
        byte[] sortedArray = this.toSortedArray();
        int middleIndex = sortedArray.length >> 1;
        if (sortedArray.length > 1 && (sortedArray.length & 1) == 0)
        {
            byte first = sortedArray[middleIndex];
            byte second = sortedArray[middleIndex - 1];
            return ((double) first + (double) second) / 2.0;
        }
        return (double) sortedArray[middleIndex];
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/mutable/SortedSetAdapter.java`
#### Snippet
```java
        ListIterate.rangeCheck(fromIndex, toIndex, this.size());

        if (fromIndex > toIndex)
        {
            throw new IllegalArgumentException("fromIndex must not be greater than toIndex");
        }

        Iterator<T> iterator = this.iterator();
        int i = 0;
        while (iterator.hasNext() && i <= toIndex)
        {
            T each = iterator.next();
            if (i >= fromIndex)
            {
                procedure.value(each);
            }
            i++;
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/mutable/SortedSetAdapter.java`
#### Snippet
```java
        ListIterate.rangeCheck(fromIndex, toIndex, this.size());

        if (fromIndex > toIndex)
        {
            throw new IllegalArgumentException("fromIndex must not be greater than toIndex");
        }

        Iterator<T> iterator = this.iterator();
        int i = 0;
        while (iterator.hasNext() && i <= toIndex)
        {
            T each = iterator.next();
            if (i >= fromIndex)
            {
                objectIntProcedure.value(each, i);
            }
            i++;
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/strategy/mutable/UnifiedSetWithHashingStrategy.java`
#### Snippet
```java
        int index = this.index(key);
        Object cur = this.table[index];
        if (cur == null)
        {
            this.table[index] = UnifiedSetWithHashingStrategy.toSentinelIfNull(key);
            if (++this.occupied > this.maxSize)
            {
                this.rehash();
            }
            return key;
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/stack/immutable/ImmutableArrayStack.java`
#### Snippet
```java
            out.writeInt(this.stack.size());
            try
            {
                this.stack.forEach(new CheckedProcedure<T>()
                {
                    public void safeValue(T object) throws IOException
                    {
                        out.writeObject(object);
                    }
                });
            }
            catch (RuntimeException e)
            {
                if (e.getCause() instanceof IOException)
                {
                    throw (IOException) e.getCause();
                }
                throw e;
            }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/stack/mutable/ArrayStack.java`
#### Snippet
```java
        if (this.checkZeroCount(count))
        {
            return result;
        }
        this.checkEmptyStack();
        this.checkSizeLessThanCount(count);
        while (count > 0)
        {
            result.add(this.pop());
            count--;
        }
        return result;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/string/immutable/CharAdapter.java`
#### Snippet
```java
        if (this.isEmpty())
        {
            throw new NoSuchElementException();
        }
        char max = this.get(0);
        int size = this.size();
        for (int i = 1; i < size; i++)
        {
            char value = this.get(i);
            if (max < value)
            {
                max = value;
            }
        }
        return max;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/string/immutable/CodePointAdapter.java`
#### Snippet
```java
        int currentIndex = 0;
        for (int i = 0; i < this.adapted.length(); )
        {
            int codePoint = this.adapted.codePointAt(i);
            if (index == currentIndex)
            {
                return codePoint;
            }
            i += Character.charCount(codePoint);
            currentIndex++;
        }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/string/immutable/CodePointAdapter.java`
#### Snippet
```java
        if (this.isEmpty())
        {
            throw new NoSuchElementException();
        }
        int max = this.get(0);
        for (int i = 0; i < this.adapted.length(); )
        {
            int codePoint = this.adapted.codePointAt(i);
            if (max < codePoint)
            {
                max = codePoint;
            }
            i += Character.charCount(codePoint);
        }
        return max;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/string/immutable/CodePointAdapter.java`
#### Snippet
```java
                if (appendable instanceof StringBuilder)
                {
                    ((StringBuilder) appendable).appendCodePoint(codePoint);
                }
                else if (appendable instanceof StringBuffer)
                {
                    ((StringBuffer) appendable).appendCodePoint(codePoint);
                }
                else
                {
                    char[] chars = Character.toChars(codePoint);
                    for (char aChar : chars)
                    {
                        appendable.append(aChar);
                    }
                }
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/string/immutable/CodePointAdapter.java`
#### Snippet
```java
        int size = this.size();
        int othersize = iterable.size();
        MutableList<IntIntPair> target = Lists.mutable.withInitialCapacity(Math.min(size, othersize));
        IntIterator iterator = iterable.intIterator();
        for (int i = 0; i < size && i < othersize; i++)
        {
            target.add(PrimitiveTuples.pair(this.get(i), iterator.next()));
        }
        return target.toImmutable();
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/MapIterate.java`
#### Snippet
```java
        MutableMap<K, V> resultMap = Maps.mutable.empty();
        Procedure2<K, V> mapTransferProcedure = new MapPutProcedure<>(resultMap);
        Procedure2<K, V> procedure = (key, value) -> {
            if (predicate.accept(key))
            {
                mapTransferProcedure.value(key, value);
            }
        };
        MapIterate.forEachKeyValue(map, procedure);
        return resultMap;
```

### DuplicatedCode
Duplicated code
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/internal/SortedBagIterables.java`
#### Snippet
```java
        Iterator<T> itrA = bagA.iterator();
        Iterator<T> itrB = bagB.iterator();
        if (bagA.comparator() != null)
        {
            Comparator<? super T> comparator = bagA.comparator();
            while (itrA.hasNext())
            {
                if (itrB.hasNext())
                {
                    int val = comparator.compare(itrA.next(), itrB.next());
                    if (val != 0)
                    {
                        return val;
                    }
                }
                else
                {
                    return 1;
                }
            }
            return itrB.hasNext() ? -1 : 0;
        }

        while (itrA.hasNext())
        {
            if (itrB.hasNext())
            {
                int val = ((Comparable<T>) itrA.next()).compareTo(itrB.next());
                if (val != 0)
                {
                    return val;
                }
            }
            else
            {
                return 1;
            }
        }
        return itrB.hasNext() ? -1 : 0;
```

### DuplicatedCode
Duplicated code
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AnagramBagTest.java`
#### Snippet
```java
        MutableMultimap<Alphagram, String> groupBy = ParallelIterate.groupBy(this.ecWords, Alphagram::new);
        groupBy.multiValuesView()
                .select(iterable -> iterable.size() >= SIZE_THRESHOLD)
                .toSortedList(Comparators.byIntFunction(RichIterable::size))
                .asReversed()
                .collect(iterable -> iterable.size() + ": " + iterable)
                .forEach(Procedures.cast(e -> Assert.assertFalse(e.isEmpty())));
```

### DuplicatedCode
Duplicated code
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AnagramBagTest.java`
#### Snippet
```java
        MutableMultimap<Alphagram, String> groupBy = FJIterate.groupBy(this.ecWords, Alphagram::new);
        groupBy.multiValuesView()
                .select(iterable -> iterable.size() >= SIZE_THRESHOLD)
                .toSortedList(Comparators.byIntFunction(RichIterable::size))
                .asReversed()
                .collect(iterable -> iterable.size() + ": " + iterable)
                .forEach(Procedures.cast(e -> Assert.assertFalse(e.isEmpty())));
```

### DuplicatedCode
Duplicated code
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AnagramBagTest.java`
#### Snippet
```java
        groupBy.entrySet()
                .stream()
                .map(Map.Entry::getValue)
                .filter(list -> list.size() >= SIZE_THRESHOLD)
                .sorted(Comparator.<List<String>>comparingInt(List::size).reversed())
                .map(list -> list.size() + ": " + list)
                .forEach(e -> Assert.assertFalse(e.isEmpty()));
```

### DuplicatedCode
Duplicated code
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AnagramBagTest.java`
#### Snippet
```java
        Map<Alphagram, List<String>> groupBy = this.ecWords.stream().collect(Collectors.groupingBy(Alphagram::new));
        groupBy.entrySet()
                .stream()
                .map(Map.Entry::getValue)
                .filter(list -> list.size() >= SIZE_THRESHOLD)
                .sorted(Comparator.<List<String>>comparingInt(List::size).reversed())
                .map(list -> list.size() + ": " + list)
                .forEach(e -> Assert.assertFalse(e.isEmpty()));
```

### DuplicatedCode
Duplicated code
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AnagramBagTest.java`
#### Snippet
```java
        groupBy.entrySet()
                .parallelStream()
                .map(Map.Entry::getValue)
                .filter(list -> list.size() >= SIZE_THRESHOLD)
                .sorted(Comparator.<List<String>>comparingInt(List::size).reversed())
                .map(list -> list.size() + ": " + list)
                .forEach(e -> Assert.assertFalse(e.isEmpty()));
```

### DuplicatedCode
Duplicated code
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AnagramBagTest.java`
#### Snippet
```java
        Map<Alphagram, List<String>> groupBy = this.ecWords.parallelStream().collect(Collectors.groupingBy(Alphagram::new));
        groupBy.entrySet()
                .parallelStream()
                .map(Map.Entry::getValue)
                .filter(list -> list.size() >= SIZE_THRESHOLD)
                .sorted(Comparator.<List<String>>comparingInt(List::size).reversed())
                .map(list -> list.size() + ": " + list)
                .forEach(e -> Assert.assertFalse(e.isEmpty()));
```

### DuplicatedCode
Duplicated code
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AnagramSetTest.java`
#### Snippet
```java
        groupBy.entrySet()
                .stream()
                .map(Map.Entry::getValue)
                .filter(list -> list.size() >= SIZE_THRESHOLD)
                .sorted(Comparator.<Set<String>>comparingInt(Set::size).reversed())
                .map(list -> list.size() + ": " + list)
                .forEach(e -> Assert.assertFalse(e.isEmpty()));
```

### DuplicatedCode
Duplicated code
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AnagramSetTest.java`
#### Snippet
```java
        groupBy.entrySet()
                .parallelStream()
                .map(Map.Entry::getValue)
                .filter(list -> list.size() >= SIZE_THRESHOLD)
                .sorted(Comparator.<Set<String>>comparingInt(Set::size).reversed())
                .parallel()
                .map(list -> list.size() + ": " + list)
                .forEach(e -> Assert.assertFalse(e.isEmpty()));
```

### DuplicatedCode
Duplicated code
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/CountSetTest.java`
#### Snippet
```java
if (this.megamorphicWarmupLevel > 0)
        {
            // serial, lazy, JDK
            {
                long evens = this.integersJDK.stream().filter(each -> each % 2 == 0).count();
                Assert.assertEquals(SIZE / 2, evens);
                long odds = this.integersJDK.stream().filter(each -> each % 2 == 1).count();
                Assert.assertEquals(SIZE / 2, odds);
                long evens2 = this.integersJDK.stream().filter(each -> (each & 1) == 0).count();
                Assert.assertEquals(SIZE / 2, evens2);
            }

            // parallel, lazy, JDK
            {
                long evens = this.integersJDK.parallelStream().filter(each -> each % 2 == 0).count();
                Assert.assertEquals(SIZE / 2, evens);
                long odds = this.integersJDK.parallelStream().filter(each -> each % 2 == 1).count();
                Assert.assertEquals(SIZE / 2, odds);
                long evens2 = this.integersJDK.parallelStream().filter(each -> (each & 1) == 0).count();
                Assert.assertEquals(SIZE / 2, evens2);
            }

            // serial, lazy, EC
            {
                long evens = this.integersEC.asLazy().count(each -> each % 2 == 0);
                Assert.assertEquals(SIZE / 2, evens);
                long odds = this.integersEC.asLazy().count(each -> each % 2 == 1);
                Assert.assertEquals(SIZE / 2, odds);
                long evens2 = this.integersEC.asLazy().count(each -> (each & 1) == 0);
                Assert.assertEquals(SIZE / 2, evens2);
            }

            // parallel, lazy, EC
            {
                long evens = this.integersEC.asParallel(this.executorService, BATCH_SIZE).count(each -> each % 2 == 0);
                Assert.assertEquals(SIZE / 2, evens);
                long odds = this.integersEC.asParallel(this.executorService, BATCH_SIZE).count(each -> each % 2 == 1);
                Assert.assertEquals(SIZE / 2, odds);
                long evens2 = this.integersEC.asParallel(this.executorService, BATCH_SIZE).count(each -> (each & 1) == 0);
                Assert.assertEquals(SIZE / 2, evens2);
            }

            // serial, eager, EC
            {
                long evens = this.integersEC.count(each -> each % 2 == 0);
                Assert.assertEquals(SIZE / 2, evens);
                long odds = this.integersEC.count(each -> each % 2 == 1);
                Assert.assertEquals(SIZE / 2, odds);
                long evens2 = this.integersEC.count(each -> (each & 1) == 0);
                Assert.assertEquals(SIZE / 2, evens2);
            }

            // parallel, eager, EC
            long evens = ParallelIterate.count(this.integersEC, each -> each % 2 == 0);
            Assert.assertEquals(SIZE / 2, evens);
            long odds = ParallelIterate.count(this.integersEC, each -> each % 2 == 1);
            Assert.assertEquals(SIZE / 2, odds);
            long evens2 = ParallelIterate.count(this.integersEC, each -> (each & 1) == 0);
            Assert.assertEquals(SIZE / 2, evens2);
        }

        if (this.megamorphicWarmupLevel > 1)
        {
            // stream().mapToLong().reduce()
            Assert.assertEquals(
                    500001500000L,
                    this.integersJDK.stream().mapToLong(each -> each + 1).reduce(0, (accum, each) -> accum + each));

            Assert.assertEquals(
                    500002500000L,
                    this.integersJDK.stream().mapToLong(each -> each + 2).reduce(0, (accum, each) -> {
                        Assert.assertTrue(each >= 0);
                        return accum + each;
                    }));

            Assert.assertEquals(
                    500003500000L,
                    this.integersJDK.stream().mapToLong(each -> each + 3).reduce(0, (accum, each) -> {
                        Assert.assertTrue(each >= 0);
                        return accum + each;
                    }));

            // parallelStream().mapToLong().reduce()
            Assert.assertEquals(
                    500001500000L,
                    this.integersJDK.parallelStream().mapToLong(each -> each + 1).reduce(0, (accum, each) -> accum + each));

            Assert.assertEquals(
                    500002500000L,
                    this.integersJDK.parallelStream().mapToLong(each -> each + 2).reduce(0, (accum, each) -> {
                        Assert.assertTrue(each >= 0);
                        return accum + each;
                    }));

            Assert.assertEquals(
                    500003500000L,
                    this.integersJDK.parallelStream().mapToLong(each -> each + 3).reduce(0, (accum, each) -> {
                        Assert.assertTrue(each >= 0);
                        return accum + each;
                    }));
        }

        if (this.megamorphicWarmupLevel > 2)
        {
            this.integersEC.asLazy().forEach(Procedures.cast(Assert::assertNotNull));
            this.integersEC.asLazy().forEach(Procedures.cast(each -> Assert.assertEquals(each, each)));
            this.integersEC.asLazy().forEach(new CountProcedure<>());

            this.integersEC.asParallel(this.executorService, BATCH_SIZE).forEach(Assert::assertNotNull);
            this.integersEC.asParallel(this.executorService, BATCH_SIZE).forEach(each -> Assert.assertEquals(each, each));
            this.integersEC.asParallel(this.executorService, BATCH_SIZE).forEach(new CountProcedure<>());

            this.integersJDK.stream().forEach(Assert::assertNotNull);
            this.integersJDK.stream().forEach(each -> Assert.assertEquals(each, each));

            this.integersJDK.parallelStream().forEach(Assert::assertNotNull);
            this.integersJDK.parallelStream().forEach(each -> Assert.assertEquals(each, each));
        }
```

### DuplicatedCode
Duplicated code
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/FunctionalInterfaceSetTest.java`
#### Snippet
```java
this.setUp();

        org.eclipse.collections.api.block.predicate.Predicate<Integer> predicate1 = each -> (each + 2) % 10_000 != 0;
        org.eclipse.collections.api.block.predicate.Predicate<Integer> predicate2 = each -> (each + 3) % 10_000 != 0;
        org.eclipse.collections.api.block.predicate.Predicate<Integer> predicate3 = each -> (each + 4) % 10_000 != 0;
        org.eclipse.collections.api.block.predicate.Predicate<Integer> predicate4 = each -> (each + 5) % 10_000 != 0;

        org.eclipse.collections.api.block.function.Function<Integer, String> function1 = each ->
        {
            Assert.assertNotNull(each);
            return String.valueOf(each);
        };

        org.eclipse.collections.api.block.function.Function<String, Integer> function2 = each -> {
            Assert.assertNotNull(each);
            return Integer.valueOf(each);
        };

        org.eclipse.collections.api.block.function.Function<Integer, String> function3 = each ->
        {
            Assert.assertSame(each, each);
            return String.valueOf(each);
        };

        org.eclipse.collections.api.block.function.Function<String, Integer> function4 = each -> {
            Assert.assertSame(each, each);
            return Integer.valueOf(each);
        };

        if (this.megamorphicWarmupLevel > 0)
        {
            Predicate<Integer> predicateJDK1 = each -> (each + 2) % 10_000 != 0;
            Predicate<Integer> predicateJDK2 = each -> (each + 3) % 10_000 != 0;
            Predicate<Integer> predicateJDK3 = each -> (each + 4) % 10_000 != 0;
            Predicate<Integer> predicateJDK4 = each -> (each + 5) % 10_000 != 0;

            Function<Integer, String> mapper1 = each ->
            {
                Assert.assertNotNull(each);
                return String.valueOf(each);
            };

            Function<String, Integer> mapper2 = each -> {
                Assert.assertNotNull(each);
                return Integer.valueOf(each);
            };

            Function<Integer, String> mapper3 = each ->
            {
                Assert.assertSame(each, each);
                return String.valueOf(each);
            };

            Function<String, Integer> mapper4 = each -> {
                Assert.assertSame(each, each);
                return Integer.valueOf(each);
            };

            // serial, lazy, JDK
            {
                Set<Integer> set = this.integersJDK.stream()
                        .filter(predicateJDK1)
                        .map(mapper1)
                        .map(mapper2)
                        .filter(predicateJDK2)
                        .collect(Collectors.toSet());
                Verify.assertSize(999_800, set);

                List<Integer> collection = this.integersJDK.stream()
                        .filter(predicateJDK3)
                        .map(mapper3)
                        .map(mapper4)
                        .filter(predicateJDK4)
                        .collect(Collectors.toCollection(ArrayList::new));
                Verify.assertSize(999_800, collection);
            }

            // parallel, lazy, JDK
            {
                Set<Integer> set = this.integersJDK.parallelStream()
                        .filter(predicateJDK1)
                        .map(mapper1)
                        .map(mapper2)
                        .filter(predicateJDK2)
                        .collect(Collectors.toSet());
                Verify.assertSize(999_800, set);

                List<Integer> collection = this.integersJDK.parallelStream()
                        .filter(predicateJDK3)
                        .map(mapper3)
                        .map(mapper4)
                        .filter(predicateJDK4)
                        .collect(Collectors.toCollection(ArrayList::new));
                Verify.assertSize(999_800, collection);
            }

            // serial, lazy, EC
            {
                MutableSet<Integer> set = this.integersEC.asLazy()
                        .select(predicate1)
                        .collect(function1)
                        .collect(function2)
                        .select(predicate2)
                        .toSet();
                Verify.assertSize(999_800, set);

                MutableBag<Integer> bag = this.integersEC.asLazy()
                        .select(predicate3)
                        .collect(function3)
                        .collect(function4)
                        .select(predicate4)
                        .toBag();
                Verify.assertIterableSize(999_800, bag);
            }

            // parallel, lazy, EC
            {
                MutableSet<Integer> set = this.integersEC.asParallel(this.executorService, BATCH_SIZE)
                        .select(predicate1)
                        .collect(function1)
                        .collect(function2)
                        .select(predicate2)
                        .toSet();
                Verify.assertSize(999_800, set);

                MutableBag<Integer> bag = this.integersEC.asParallel(this.executorService, BATCH_SIZE)
                        .select(predicate3)
                        .collect(function3)
                        .collect(function4)
                        .select(predicate4)
                        .toBag();
                Verify.assertIterableSize(999_800, bag);
            }

            // serial, eager, EC
            MutableSet<Integer> set = this.integersEC
                    .select(predicate1)
                    .collect(function1)
                    .collect(function2)
                    .select(predicate2)
                    .toSet();
            Verify.assertSize(999_800, set);

            MutableBag<Integer> bag = this.integersEC
                    .select(predicate3)
                    .collect(function3)
                    .collect(function4)
                    .select(predicate4)
                    .toBag();
            Verify.assertIterableSize(999_800, bag);
        }

        if (this.megamorphicWarmupLevel > 1)
        {
            // parallel, eager, EC
            Collection<Integer> select1 = ParallelIterate.select(this.integersEC, predicate1, new UnifiedSet<>(), true);
            Collection<String> collect1 = ParallelIterate.collect(select1, function1, new UnifiedSet<>(), true);
            Collection<Integer> collect2 = ParallelIterate.collect(collect1, function2, new UnifiedSet<>(), true);
            UnifiedSet<Integer> set = ParallelIterate.select(collect2, predicate2, new UnifiedSet<>(), true);
            Verify.assertSize(999_800, set);

            Collection<Integer> select3 = ParallelIterate.select(this.integersEC, predicate3, new HashBag<>(), true);
            Collection<String> collect3 = ParallelIterate.collect(select3, function3, new HashBag<>(), true);
            Collection<Integer> collect4 = ParallelIterate.collect(collect3, function4, new HashBag<>(), true);
            HashBag<Integer> bag = ParallelIterate.select(collect4, predicate4, new HashBag<>(), true);
            Verify.assertSize(999_800, bag);
        }

        if (this.megamorphicWarmupLevel > 2)
        {
            // parallel, eager, EC, executorService
            UnifiedSet<Integer> select1 = ParallelIterate.select(this.integersEC, predicate1, new UnifiedSet<>(), BATCH_SIZE, this.executorService, true);
            UnifiedSet<String> collect1 = ParallelIterate.collect(select1, function1, new UnifiedSet<>(), BATCH_SIZE, this.executorService, true);
            UnifiedSet<Integer> collect2 = ParallelIterate.collect(collect1, function2, new UnifiedSet<>(), BATCH_SIZE, this.executorService, true);
            UnifiedSet<Integer> set = ParallelIterate.select(collect2, predicate2, new UnifiedSet<>(), BATCH_SIZE, this.executorService, true);
            Verify.assertSize(999_800, set);

            HashBag<Integer> select3 = ParallelIterate.select(this.integersEC, predicate3, new HashBag<>(), BATCH_SIZE, this.executorService, true);
            HashBag<String> collect3 = ParallelIterate.collect(select3, function3, new HashBag<>(), BATCH_SIZE, this.executorService, true);
            HashBag<Integer> collect4 = ParallelIterate.collect(collect3, function4, new HashBag<>(), BATCH_SIZE, this.executorService, true);
            HashBag<Integer> bag = ParallelIterate.select(collect4, predicate4, new HashBag<>(), BATCH_SIZE, this.executorService, true);
            Verify.assertSize(999_800, bag);
        }

        FunctionalInterfaceScalaTest.megamorphic(this.megamorphicWarmupLevel);
```

### DuplicatedCode
Duplicated code
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/GroupBySetTest.java`
#### Snippet
```java
        Set<Integer> odds = multimap.get(false);
        Set<Integer> evens = multimap.get(true);
        Verify.assertSetsEqual(Interval.fromToBy(0, 999_999, 2).toSet(), evens);
        Verify.assertSetsEqual(Interval.fromToBy(1, 999_999, 2).toSet(), odds);
```

### DuplicatedCode
Duplicated code
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/IntIntMapLargeStressTest.java`
#### Snippet
```java
        this.intIntKoloboke = HashIntIntMaps.newMutableMap(MAP_SIZE);
        this.intIntEc = new IntIntHashMap(MAP_SIZE);
        this.integerIntegerJdk = new HashMap<>(MAP_SIZE);

        Random random = new Random(0x123456789ABCDL);

        int[] randomNumbersForMap = this.getRandomKeys(random).toArray();

        int number = 23;
        int lower = Integer.MIN_VALUE;
        int upper = Integer.MAX_VALUE;
        this.kolobokeIntKeysForMap = this.fullyRandom
                ? randomNumbersForMap
                : this.getKolobokeArray(number, lower, upper, random);
        this.ecIntKeysForMap = this.fullyRandom
                ? randomNumbersForMap
                : this.getECArray(number, lower, upper, random);
```

### DuplicatedCode
Duplicated code
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/IntIntMapLargeStressTest.java`
#### Snippet
```java
        for (int i = 0; i < KEY_COUNT; i++)
        {
            this.intIntKoloboke.put(this.kolobokeIntKeysForMap[i], 5);
            this.intIntEc.put(this.ecIntKeysForMap[i], 5);
            this.integerIntegerJdk.put(this.jdkIntKeysForMap[i], 5);
        }

        this.shuffle(this.ecIntKeysForMap, random);
        this.shuffle(this.kolobokeIntKeysForMap, random);
        this.shuffle(this.jdkIntKeysForMap, random);
```

### DuplicatedCode
Duplicated code
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/IntIntMapLargeStressTest.java`
#### Snippet
```java
        for (int i = lower; i < upper && ecCollidingNumbers.size() < KEY_COUNT; i++)
        {
            int index = this.ecIndex(i);
            if (index >= number && index <= number + 100)
            {
                ecCollidingNumbers.add(i);
            }
        }
        return ecCollidingNumbers;
```

### DuplicatedCode
Duplicated code
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/IntIntMapLargeStressTest.java`
#### Snippet
```java
        MutableIntSet indices = new IntHashSet();
        for (int i = lower; i < upper && jdkCollidingNumbers.size() < KEY_COUNT; i++)
        {
            int index = this.jdkIndex(i);
            if (indices.size() < slots)
            {
                indices.add(index);
                jdkCollidingNumbers.add(i);
            }
            else if (indices.contains(index))
            {
                jdkCollidingNumbers.add(i);
            }
        }
        return jdkCollidingNumbers;
```

### DuplicatedCode
Duplicated code
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/IntIntMapLargeStressTest.java`
#### Snippet
```java
        MutableIntList kolobokeCollidingNumbers = new IntArrayList();
        for (int i = lower; i < upper && kolobokeCollidingNumbers.size() < KEY_COUNT; i++)
        {
            int index = this.kolobokeIndex(i);
            if (index >= number && index <= number + 100)
            {
                kolobokeCollidingNumbers.add(i);
            }
        }
        return kolobokeCollidingNumbers;
```

### DuplicatedCode
Duplicated code
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/IntIntMapLargeStressTest.java`
#### Snippet
```java
        for (int j = 0; j < LOOP_COUNT; j++)
        {
            for (int i = 0; i < KEY_COUNT; i++)
            {
                if (this.integerIntegerJdk.get(this.jdkIntKeysForMap[i]) == null)
                {
                    throw new AssertionError(this.jdkIntKeysForMap[i] + " not in map");
                }
            }
            if (this.integerIntegerJdk.size() != KEY_COUNT)
            {
                throw new AssertionError("size is " + this.integerIntegerJdk.size());
            }
        }
```

### DuplicatedCode
Duplicated code
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/IntIntMapLargeStressTest.java`
#### Snippet
```java
        for (int j = 0; j < LOOP_COUNT; j++)
        {
            for (int i = 0; i < KEY_COUNT; i++)
            {
                if (this.intIntKoloboke.get(this.kolobokeIntKeysForMap[i]) == this.intIntKoloboke.defaultValue())
                {
                    throw new AssertionError(this.kolobokeIntKeysForMap[i] + " not in map");
                }
            }
            if (this.intIntKoloboke.size() != KEY_COUNT)
            {
                throw new AssertionError("size is " + this.intIntKoloboke.size());
            }
        }
```

### DuplicatedCode
Duplicated code
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/IntIntMapLargeStressTest.java`
#### Snippet
```java
        for (int j = 0; j < LOOP_COUNT; j++)
        {
            for (int i = 0; i < KEY_COUNT; i++)
            {
                if (this.intIntEc.get(this.ecIntKeysForMap[i]) == 0)
                {
                    throw new AssertionError(this.ecIntKeysForMap[i] + " not in map");
                }
            }
            if (this.intIntEc.size() != KEY_COUNT)
            {
                throw new AssertionError("size is " + this.intIntEc.size());
            }
        }
```

### DuplicatedCode
Duplicated code
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/IntIntMapLargeStressTest.java`
#### Snippet
```java
        for (int j = 0; j < LOOP_COUNT; j++)
        {
            Map<Integer, Integer> newMap = new HashMap<>(MAP_SIZE);
            for (int i = 0; i < KEY_COUNT; i++)
            {
                newMap.put(this.jdkIntKeysForMap[i], 4);
            }
            if (newMap.size() != KEY_COUNT)
            {
                throw new AssertionError("size is " + newMap.size());
            }
        }
```

### DuplicatedCode
Duplicated code
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/IntIntMapLargeStressTest.java`
#### Snippet
```java
        for (int j = 0; j < LOOP_COUNT; j++)
        {
            IntIntMap newMap = HashIntIntMaps.newMutableMap(MAP_SIZE);
            for (int i = 0; i < KEY_COUNT; i++)
            {
                newMap.put(this.kolobokeIntKeysForMap[i], 4);
            }
            if (newMap.size() != KEY_COUNT)
            {
                throw new AssertionError("size is " + newMap.size());
            }
        }
```

### DuplicatedCode
Duplicated code
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/IntIntMapLargeStressTest.java`
#### Snippet
```java
        for (int j = 0; j < LOOP_COUNT; j++)
        {
            MutableIntIntMap newMap = new IntIntHashMap(MAP_SIZE);
            for (int i = 0; i < KEY_COUNT; i++)
            {
                newMap.put(this.ecIntKeysForMap[i], 4);
            }
            if (newMap.size() != KEY_COUNT)
            {
                throw new AssertionError("size is " + newMap.size());
            }
        }
```

### DuplicatedCode
Duplicated code
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/IntIntMapLargeStressTest.java`
#### Snippet
```java
        for (int j = 0; j < LOOP_COUNT; j++)
        {
            MutableIntIntMap newMap = new IntIntHashMap(this.intIntEc);
            for (int i = 0; i < KEY_COUNT; i++)
            {
                newMap.remove(this.ecIntKeysForMap[i]);
            }
            if (newMap.notEmpty())
            {
                throw new AssertionError("size is " + newMap.size());
            }
        }
```

### DuplicatedCode
Duplicated code
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/IntIntMapLargeStressTest.java`
#### Snippet
```java
        for (int j = 0; j < LOOP_COUNT; j++)
        {
            Map<Integer, Integer> newMap = new HashMap<>(this.integerIntegerJdk);
            for (int i = 0; i < KEY_COUNT; i++)
            {
                newMap.remove(this.jdkIntKeysForMap[i]);
            }
            if (!newMap.isEmpty())
            {
                throw new AssertionError("size is " + newMap.size());
            }
        }
```

### DuplicatedCode
Duplicated code
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/IntIntMapLargeStressTest.java`
#### Snippet
```java
        for (int j = 0; j < LOOP_COUNT; j++)
        {
            IntIntMap newMap = HashIntIntMaps.newMutableMap(this.intIntKoloboke);
            for (int i = 0; i < KEY_COUNT; i++)
            {
                newMap.remove(this.kolobokeIntKeysForMap[i]);
            }
            if (!newMap.isEmpty())
            {
                throw new AssertionError("size is " + newMap.size());
            }
        }
```

### DuplicatedCode
Duplicated code
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/IntIntMapSmallStressTest.java`
#### Snippet
```java
        for (int i = lower; i < upper && ecCollidingNumbers.size() < KEY_COUNT; i++)
        {
            if (this.ecIndex(i) - this.ecIndex(number) >= 0
                    && this.ecIndex(i) - this.ecIndex(number) < 10
                    && (this.ecIndexTwo(i) - this.ecIndexTwo(number) >= 0)
                    && (this.ecIndexTwo(i) - this.ecIndexTwo(number) < 10))
            {
                ecCollidingNumbers.add(i);
            }
        }
        return ecCollidingNumbers;
```

### DuplicatedCode
Duplicated code
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/LongLongMapLargeStressTest.java`
#### Snippet
```java
        this.longLongKoloboke = HashLongLongMaps.newMutableMap(MAP_SIZE);
        this.longLongEc = new LongLongHashMap(MAP_SIZE);

        Random random = new Random(0x123456789ABCDL);

        int number = 23;
        int lower = Integer.MIN_VALUE;
        int upper = Integer.MAX_VALUE;

        long[] randomNumbersForMap = this.getRandomKeys(random).toArray();

        this.ecLongKeysForMap = this.fullyRandom
                ? randomNumbersForMap
                : this.getECArray(number, lower, upper, random);
        this.kolobokeLongKeysForMap = this.fullyRandom
                ? randomNumbersForMap
                : this.getKolobokeArray(number, lower, upper, random);

        for (int i = 0; i < KEY_COUNT; i++)
        {
            this.longLongKoloboke.put(this.kolobokeLongKeysForMap[i], 5);
            this.longLongEc.put(this.ecLongKeysForMap[i], 5);
        }

        this.shuffle(this.ecLongKeysForMap, random);
        this.shuffle(this.kolobokeLongKeysForMap, random);
```

### DuplicatedCode
Duplicated code
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/LongLongMapLargeStressTest.java`
#### Snippet
```java
        MutableLongList kolobokeCollidingNumbers = new LongArrayList();
        for (int i = lower; i < upper && kolobokeCollidingNumbers.size() < KEY_COUNT; i++)
        {
            int index = this.kolobokeIndex(i);
            if (index >= number && index <= number + 100)
            {
                kolobokeCollidingNumbers.add(i);
            }
        }
        return kolobokeCollidingNumbers;
```

### DuplicatedCode
Duplicated code
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/LongLongMapLargeStressTest.java`
#### Snippet
```java
        for (int j = 0; j < LOOP_COUNT; j++)
        {
            for (int i = 0; i < KEY_COUNT; i++)
            {
                if (this.longLongKoloboke.get(this.kolobokeLongKeysForMap[i]) == this.longLongKoloboke.defaultValue())
                {
                    throw new AssertionError(this.kolobokeLongKeysForMap[i] + " not in map");
                }
            }
            if (this.longLongKoloboke.size() != KEY_COUNT)
            {
                throw new AssertionError("size is " + this.longLongKoloboke.size());
            }
        }
```

### DuplicatedCode
Duplicated code
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/LongLongMapLargeStressTest.java`
#### Snippet
```java
        for (int j = 0; j < LOOP_COUNT; j++)
        {
            for (int i = 0; i < KEY_COUNT; i++)
            {
                if (this.longLongEc.get(this.ecLongKeysForMap[i]) == 0)
                {
                    throw new AssertionError(this.ecLongKeysForMap[i] + " not in map");
                }
            }
            if (this.longLongEc.size() != KEY_COUNT)
            {
                throw new AssertionError("size is " + this.longLongEc.size());
            }
        }
```

### DuplicatedCode
Duplicated code
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/LongLongMapLargeStressTest.java`
#### Snippet
```java
        for (int j = 0; j < LOOP_COUNT; j++)
        {
            LongLongMap newMap = HashLongLongMaps.newMutableMap(MAP_SIZE);
            for (int i = 0; i < KEY_COUNT; i++)
            {
                newMap.put(this.kolobokeLongKeysForMap[i], 4);
            }
            if (newMap.size() != KEY_COUNT)
            {
                throw new AssertionError("size is " + newMap.size());
            }
        }
```

### DuplicatedCode
Duplicated code
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/LongLongMapLargeStressTest.java`
#### Snippet
```java
        for (int j = 0; j < LOOP_COUNT; j++)
        {
            MutableLongLongMap newMap = new LongLongHashMap(MAP_SIZE);
            for (int i = 0; i < KEY_COUNT; i++)
            {
                newMap.put(this.ecLongKeysForMap[i], 4);
            }
            if (newMap.size() != KEY_COUNT)
            {
                throw new AssertionError("size is " + newMap.size());
            }
        }
```

### DuplicatedCode
Duplicated code
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/LongLongMapLargeStressTest.java`
#### Snippet
```java
        for (int j = 0; j < LOOP_COUNT; j++)
        {
            MutableLongLongMap newMap = new LongLongHashMap(this.longLongEc);
            for (int i = 0; i < KEY_COUNT; i++)
            {
                newMap.remove(this.ecLongKeysForMap[i]);
            }
            if (newMap.notEmpty())
            {
                throw new AssertionError("size is " + newMap.size());
            }
        }
```

### DuplicatedCode
Duplicated code
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/LongLongMapLargeStressTest.java`
#### Snippet
```java
        for (int j = 0; j < LOOP_COUNT; j++)
        {
            LongLongMap newMap = HashLongLongMaps.newMutableMap(this.longLongKoloboke);
            for (int i = 0; i < KEY_COUNT; i++)
            {
                newMap.remove(this.kolobokeLongKeysForMap[i]);
            }
            if (!newMap.isEmpty())
            {
                throw new AssertionError("size is " + newMap.size());
            }
        }
```

### DuplicatedCode
Duplicated code
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/list/ListIterationTest.java`
#### Snippet
```java
        int count = this.ecMutable
                .asLazy()
                .select(each -> each % 10_000 != 0)
                .collect(String::valueOf)
                .collect(Integer::valueOf)
                .count(each -> (each + 1) % 10_000 != 0);
        if (count != 999_800)
        {
            throw new AssertionError();
        }
```

### DuplicatedCode
Duplicated code
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/list/ListIterationTest.java`
#### Snippet
```java
        int count = this.ecImmutable
                .asLazy()
                .select(each -> each % 10_000 != 0)
                .collect(String::valueOf)
                .collect(Integer::valueOf)
                .count(each -> (each + 1) % 10_000 != 0);
        if (count != 999_800)
        {
            throw new AssertionError();
        }
```

### DuplicatedCode
Duplicated code
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/list/ListIterationTest.java`
#### Snippet
```java
        int count = this.ecMutable
                .asParallel(this.executorService, BATCH_SIZE)
                .select(each -> each % 10_000 != 0)
                .collect(String::valueOf)
                .collect(Integer::valueOf)
                .count(each -> (each + 1) % 10_000 != 0);
        if (count != 999_800)
        {
            throw new AssertionError();
        }
```

### DuplicatedCode
Duplicated code
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/list/ListIterationTest.java`
#### Snippet
```java
        int count = this.ecImmutable
                .asParallel(this.executorService, BATCH_SIZE)
                .select(each -> each % 10_000 != 0)
                .collect(String::valueOf)
                .collect(Integer::valueOf)
                .count(each -> (each + 1) % 10_000 != 0);
        if (count != 999_800)
        {
            throw new AssertionError();
        }
```

### DuplicatedCode
Duplicated code
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/map/ChainMapPutTest.java`
#### Snippet
```java
        this.elements = new String[this.size];

        Random random = new Random(123456789012345L);
        for (int i = 0; i < this.size; i++)
        {
            this.elements[i] = RandomStringUtils.random(RANDOM_COUNT, 0, 0, false, true, null, random);
        }
```

### DuplicatedCode
Duplicated code
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/map/ScalaImmutableMapGetTest.java`
#### Snippet
```java
        int localSize = this.size;
        String[] localElements = this.elements;
        Map<String, String> localScalaMap = this.scalaMap;

        for (int i = 0; i < localSize; i++)
        {
            if (!localScalaMap.get(localElements[i]).isDefined())
            {
                throw new AssertionError(i);
            }
        }
```

### DuplicatedCode
Duplicated code
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/set/sorted/SortedSetContainsTest.java`
#### Snippet
```java
        for (int i = 0; i < size; i += 2)
        {
            if (!localEcMutable.contains(i))
            {
                throw new AssertionError(i);
            }
        }

        for (int i = 1; i < size; i += 2)
        {
            if (localEcMutable.contains(i))
            {
                throw new AssertionError(i);
            }
        }
```

## RuleId[id=RedundantCollectionOperation]
### RedundantCollectionOperation
`iterator` can be replaced with 'Collections.emptyIterator()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/CompositeFastList.java`
#### Snippet
```java
        if (this.lists.isEmpty())
        {
            return Collections.<E>emptyList().iterator();
        }
        return new CompositeIterator(this.lists);
```

## RuleId[id=MismatchedJavadocCode]
### MismatchedJavadocCode
Method is specified to return 'false' but its return type is not boolean
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/StringPredicates2.java`
#### Snippet
```java

    /**
     * Returns false if a String passed to the accept method ends with the string specified on the predicate.
     *
     * @since 5.0
```

### MismatchedJavadocCode
Method is specified to return 'true' but its return type is not boolean
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/StringPredicates2.java`
#### Snippet
```java

    /**
     * Returns true if a String specified on the predicate is contained within a String passed to the accept
     * method.
     *
```

### MismatchedJavadocCode
Method is specified to return 'false' but its return type is not boolean
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/StringPredicates2.java`
#### Snippet
```java

    /**
     * Returns false if a String passed to the accept method starts with the string specified on the predicate.
     *
     * @since 5.0
```

### MismatchedJavadocCode
Method is specified to return 'true' but its return type is not boolean
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/StringPredicates2.java`
#### Snippet
```java

    /**
     * Returns true if a String passed to the accept method starts with the string specified on the predicate.
     */
    public static Predicates2<String, String> startsWith()
```

### MismatchedJavadocCode
Method is specified to return 'true' but its return type is not boolean
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/StringPredicates2.java`
#### Snippet
```java

    /**
     * Returns true if a String passed to the accept method ends with the string specified on the predicate.
     */
    public static Predicates2<String, String> endsWith()
```

### MismatchedJavadocCode
Method is specified to return 'true' but its return type is not boolean
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/StringPredicates2.java`
#### Snippet
```java

    /**
     * Returns true if a String specified on the predicate is contained within a String passed to the accept
     * method.
     */
```

### MismatchedJavadocCode
Method is specified to return 'true' but its return type is not boolean
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/StringPredicates.java`
#### Snippet
```java

    /**
     * Returns true if a char specified on the predicate is contained within a String passed to the accept
     * method.
     */
```

### MismatchedJavadocCode
Method is specified to return 'true' but its return type is not boolean
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/StringPredicates.java`
#### Snippet
```java

    /**
     * Returns true if a String passed to the accept method starts with the string specified on the predicate.
     */
    public static Predicates<String> startsWith(String substring)
```

### MismatchedJavadocCode
Method is specified to return 'true' but its return type is not boolean
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/StringPredicates.java`
#### Snippet
```java

    /**
     * Returns true if a String specified on the predicate is contained within a String passed to the accept
     * method.
     */
```

### MismatchedJavadocCode
Method is specified to return 'true' but its return type is not boolean
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/StringPredicates.java`
#### Snippet
```java

    /**
     * Returns true if a String specified on the predicate is contained within a String passed to the accept
     * method.
     *
```

### MismatchedJavadocCode
Method is specified to return 'true' but its return type is not boolean
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/StringPredicates.java`
#### Snippet
```java

    /**
     * Returns true if a String passed to the accept method ends with the string specified on the predicate.
     */
    public static Predicates<String> endsWith(String substring)
```

## RuleId[id=IOStreamConstructor]
### IOStreamConstructor
'InputStream' can be constructed using 'Files.newInputStream()'
in `eclipse-collections-code-generator/src/main/java/org/eclipse/collections/codegenerator/tools/FileUtils.java`
#### Snippet
```java
            if (file.getName().endsWith(".jar"))
            {
                try (JarInputStream stream = new JarInputStream(new FileInputStream(file)))
                {
                    processJar(stream, files, templateDirectory);
```

## RuleId[id=Deprecation]
### Deprecation
Overrides deprecated method in 'org.junit.runners.ParentRunner'
in `junit-trait-runner/src/main/java/org/eclipse/collections/impl/test/junit/Java8Runner.java`
#### Snippet
```java

    @Override
    protected Java8TestClass createTestClass(Class<?> testClass)
    {
        return new Java8TestClass(testClass);
```

### Deprecation
'injectInto(long, LongObjectToLongFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/UnmodifiableRichIterable.java`
#### Snippet
```java
    public long injectInto(long injectedValue, LongObjectToLongFunction<? super T> function)
    {
        return this.iterable.injectInto(injectedValue, function);
    }

```

### Deprecation
'injectInto(float, FloatObjectToFloatFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/UnmodifiableRichIterable.java`
#### Snippet
```java
    public float injectInto(float injectedValue, FloatObjectToFloatFunction<? super T> function)
    {
        return this.iterable.injectInto(injectedValue, function);
    }

```

### Deprecation
'injectInto(int, IntObjectToIntFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/UnmodifiableRichIterable.java`
#### Snippet
```java
    public int injectInto(int injectedValue, IntObjectToIntFunction<? super T> function)
    {
        return this.iterable.injectInto(injectedValue, function);
    }

```

### Deprecation
'getLast()' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/UnmodifiableRichIterable.java`
#### Snippet
```java
    public T getLast()
    {
        return this.iterable.getLast();
    }

```

### Deprecation
'zipWithIndex()' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/UnmodifiableRichIterable.java`
#### Snippet
```java
    public RichIterable<Pair<T, Integer>> zipWithIndex()
    {
        return this.iterable.zipWithIndex();
    }

```

### Deprecation
'zip(java.lang.Iterable)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/UnmodifiableRichIterable.java`
#### Snippet
```java
    public <S> RichIterable<Pair<T, S>> zip(Iterable<S> that)
    {
        return this.iterable.zip(that);
    }

```

### Deprecation
'zipWithIndex(R)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/UnmodifiableRichIterable.java`
#### Snippet
```java
    public <R extends Collection<Pair<T, Integer>>> R zipWithIndex(R target)
    {
        return this.iterable.zipWithIndex(target);
    }

```

### Deprecation
'forEachWithIndex(ObjectIntProcedure)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/UnmodifiableRichIterable.java`
#### Snippet
```java
    public void forEachWithIndex(ObjectIntProcedure<? super T> objectIntProcedure)
    {
        this.iterable.forEachWithIndex(objectIntProcedure);
    }

```

### Deprecation
'getFirst()' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/UnmodifiableRichIterable.java`
#### Snippet
```java
    public T getFirst()
    {
        return this.iterable.getFirst();
    }

```

### Deprecation
'zip(java.lang.Iterable, R)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/UnmodifiableRichIterable.java`
#### Snippet
```java
    public <S, R extends Collection<Pair<T, S>>> R zip(Iterable<S> that, R target)
    {
        return this.iterable.zip(that, target);
    }

```

### Deprecation
'injectInto(double, DoubleObjectToDoubleFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/UnmodifiableRichIterable.java`
#### Snippet
```java
    public double injectInto(double injectedValue, DoubleObjectToDoubleFunction<? super T> function)
    {
        return this.iterable.injectInto(injectedValue, function);
    }

```

### Deprecation
'getFirst()' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/mutable/AbstractMutableBagIterable.java`
#### Snippet
```java
    public T getFirst()
    {
        return this.getKeysView().getFirst();
    }

```

### Deprecation
'getLast()' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/mutable/AbstractMutableBagIterable.java`
#### Snippet
```java
    public T getLast()
    {
        return this.getKeysView().getLast();
    }

```

### Deprecation
'getFirst()' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/immutable/ImmutableBagFactoryImpl.java`
#### Snippet
```java
            if (bag.size() == 1)
            {
                return this.with(bag.getFirst());
            }
            if (bag.sizeDistinct() < ImmutableArrayBag.MAXIMUM_USEFUL_ARRAY_BAG_SIZE)
```

### Deprecation
'injectInto(int, IntObjectToIntFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/immutable/ImmutableHashBag.java`
#### Snippet
```java
    public int injectInto(int injectedValue, IntObjectToIntFunction<? super T> function)
    {
        return this.delegate.injectInto(injectedValue, function);
    }

```

### Deprecation
'getFirst()' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/immutable/ImmutableHashBag.java`
#### Snippet
```java
    public T getFirst()
    {
        return this.delegate.getFirst();
    }

```

### Deprecation
'injectInto(long, LongObjectToLongFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/immutable/ImmutableHashBag.java`
#### Snippet
```java
    public long injectInto(long injectedValue, LongObjectToLongFunction<? super T> function)
    {
        return this.delegate.injectInto(injectedValue, function);
    }

```

### Deprecation
'injectInto(float, FloatObjectToFloatFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/immutable/ImmutableHashBag.java`
#### Snippet
```java
    public float injectInto(float injectedValue, FloatObjectToFloatFunction<? super T> function)
    {
        return this.delegate.injectInto(injectedValue, function);
    }

```

### Deprecation
'injectInto(double, DoubleObjectToDoubleFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/immutable/ImmutableHashBag.java`
#### Snippet
```java
    public double injectInto(double injectedValue, DoubleObjectToDoubleFunction<? super T> function)
    {
        return this.delegate.injectInto(injectedValue, function);
    }

```

### Deprecation
'zipWithIndex(R)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/immutable/ImmutableHashBag.java`
#### Snippet
```java
    public <R extends Collection<Pair<T, Integer>>> R zipWithIndex(R target)
    {
        return this.delegate.zipWithIndex(target);
    }

```

### Deprecation
'forEachWithIndex(ObjectIntProcedure)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/immutable/ImmutableHashBag.java`
#### Snippet
```java
    public void forEachWithIndex(ObjectIntProcedure<? super T> objectIntProcedure)
    {
        this.delegate.forEachWithIndex(objectIntProcedure);
    }

```

### Deprecation
'zip(java.lang.Iterable, R)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/immutable/ImmutableHashBag.java`
#### Snippet
```java
    public <S, R extends Collection<Pair<T, S>>> R zip(Iterable<S> that, R target)
    {
        return this.delegate.zip(that, target);
    }

```

### Deprecation
'getLast()' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/immutable/ImmutableHashBag.java`
#### Snippet
```java
    public T getLast()
    {
        return this.delegate.getLast();
    }

```

### Deprecation
'forEachWithIndex(ObjectIntProcedure)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/AbstractMapIterable.java`
#### Snippet
```java
    public void forEachWithIndex(ObjectIntProcedure<? super V> objectIntProcedure)
    {
        this.valuesView().forEachWithIndex(objectIntProcedure);
    }

```

### Deprecation
'getFirst()' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/AbstractMapIterable.java`
#### Snippet
```java
    public V getFirst()
    {
        return this.valuesView().getFirst();
    }

```

### Deprecation
'getLast()' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/AbstractMapIterable.java`
#### Snippet
```java
    public V getLast()
    {
        return this.valuesView().getLast();
    }

```

### Deprecation
Overrides deprecated method in 'org.eclipse.collections.api.map.sorted.MutableSortedMap'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/sorted/mutable/SortedMapAdapter.java`
#### Snippet
```java

    @Override
    public MutableSortedMap<K, V> with(Pair<K, V>... pairs)
    {
        ArrayIterate.forEach(pairs, new MapCollectProcedure<Pair<K, V>, K, V>(this, Functions.firstOfPair(), Functions.secondOfPair()));
```

### Deprecation
Overrides deprecated method in 'org.eclipse.collections.api.map.sorted.MutableSortedMap'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/sorted/mutable/TreeSortedMap.java`
#### Snippet
```java

    @Override
    public TreeSortedMap<K, V> with(Pair<K, V>... pairs)
    {
        ArrayIterate.forEach(pairs, new MapCollectProcedure<Pair<K, V>, K, V>(this, Functions.firstOfPair(), Functions.secondOfPair()));
```

### Deprecation
Overrides deprecated method in 'org.eclipse.collections.api.map.sorted.MutableSortedMap'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/sorted/mutable/SynchronizedSortedMap.java`
#### Snippet
```java
    @Override
    @Deprecated
    public MutableSortedMap<K, V> with(Pair<K, V>... pairs)
    {
        return this.withAllKeyValueArguments(pairs);
```

### Deprecation
'injectInto(double, DoubleObjectToDoubleFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/sorted/mutable/UnmodifiableTreeMap.java`
#### Snippet
```java
    public double injectInto(double injectedValue, DoubleObjectToDoubleFunction<? super V> function)
    {
        return this.getMutableSortedMap().injectInto(injectedValue, function);
    }

```

### Deprecation
'injectInto(int, IntObjectToIntFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/sorted/mutable/UnmodifiableTreeMap.java`
#### Snippet
```java
    public int injectInto(int injectedValue, IntObjectToIntFunction<? super V> function)
    {
        return this.getMutableSortedMap().injectInto(injectedValue, function);
    }

```

### Deprecation
'injectInto(long, LongObjectToLongFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/sorted/mutable/UnmodifiableTreeMap.java`
#### Snippet
```java
    public long injectInto(long injectedValue, LongObjectToLongFunction<? super V> function)
    {
        return this.getMutableSortedMap().injectInto(injectedValue, function);
    }

```

### Deprecation
'injectInto(float, FloatObjectToFloatFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/sorted/mutable/UnmodifiableTreeMap.java`
#### Snippet
```java
    public float injectInto(float injectedValue, FloatObjectToFloatFunction<? super V> function)
    {
        return this.getMutableSortedMap().injectInto(injectedValue, function);
    }

```

### Deprecation
Overrides deprecated method in 'org.eclipse.collections.api.map.sorted.MutableSortedMap'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/sorted/mutable/UnmodifiableTreeMap.java`
#### Snippet
```java

    @Override
    public MutableSortedMap<K, V> with(Pair<K, V>... pairs)
    {
        throw new UnsupportedOperationException("Cannot call with() on " + this.getClass().getSimpleName());
```

### Deprecation
'injectInto(long, LongObjectToLongFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnmodifiableMutableMap.java`
#### Snippet
```java
    public long injectInto(long injectedValue, LongObjectToLongFunction<? super V> function)
    {
        return this.getMutableMap().injectInto(injectedValue, function);
    }

```

### Deprecation
'getFirst()' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnmodifiableMutableMap.java`
#### Snippet
```java
    public V getFirst()
    {
        return this.getMutableMap().getFirst();
    }

```

### Deprecation
'injectInto(int, IntObjectToIntFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnmodifiableMutableMap.java`
#### Snippet
```java
    public int injectInto(int injectedValue, IntObjectToIntFunction<? super V> function)
    {
        return this.getMutableMap().injectInto(injectedValue, function);
    }

```

### Deprecation
'injectInto(float, FloatObjectToFloatFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnmodifiableMutableMap.java`
#### Snippet
```java
    public float injectInto(float injectedValue, FloatObjectToFloatFunction<? super V> function)
    {
        return this.getMutableMap().injectInto(injectedValue, function);
    }

```

### Deprecation
'getLast()' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnmodifiableMutableMap.java`
#### Snippet
```java
    public V getLast()
    {
        return this.getMutableMap().getLast();
    }

```

### Deprecation
'forEachWithIndex(ObjectIntProcedure)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnmodifiableMutableMap.java`
#### Snippet
```java
    public void forEachWithIndex(ObjectIntProcedure<? super V> objectIntProcedure)
    {
        this.getMutableMap().forEachWithIndex(objectIntProcedure);
    }

```

### Deprecation
'zip(java.lang.Iterable, R)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnmodifiableMutableMap.java`
#### Snippet
```java
    public <S, R extends Collection<Pair<V, S>>> R zip(Iterable<S> that, R target)
    {
        return this.getMutableMap().zip(that, target);
    }

```

### Deprecation
'zipWithIndex(R)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnmodifiableMutableMap.java`
#### Snippet
```java
    public <R extends Collection<Pair<V, Integer>>> R zipWithIndex(R target)
    {
        return this.getMutableMap().zipWithIndex(target);
    }

```

### Deprecation
'injectInto(double, DoubleObjectToDoubleFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnmodifiableMutableMap.java`
#### Snippet
```java
    public double injectInto(double injectedValue, DoubleObjectToDoubleFunction<? super V> function)
    {
        return this.getMutableMap().injectInto(injectedValue, function);
    }

```

### Deprecation
'injectInto(float, FloatObjectToFloatFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/ordered/mutable/UnmodifiableMutableOrderedMap.java`
#### Snippet
```java
            FloatObjectToFloatFunction<? super V> function)
    {
        return this.delegate.injectInto(injectedValue, function);
    }

```

### Deprecation
'injectInto(double, DoubleObjectToDoubleFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/ordered/mutable/UnmodifiableMutableOrderedMap.java`
#### Snippet
```java
            DoubleObjectToDoubleFunction<? super V> function)
    {
        return this.delegate.injectInto(injectedValue, function);
    }

```

### Deprecation
'injectInto(long, LongObjectToLongFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/ordered/mutable/UnmodifiableMutableOrderedMap.java`
#### Snippet
```java
            LongObjectToLongFunction<? super V> function)
    {
        return this.delegate.injectInto(injectedValue, function);
    }

```

### Deprecation
'injectInto(int, IntObjectToIntFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/ordered/mutable/UnmodifiableMutableOrderedMap.java`
#### Snippet
```java
            IntObjectToIntFunction<? super V> function)
    {
        return this.delegate.injectInto(injectedValue, function);
    }

```

### Deprecation
Overrides deprecated method in 'org.eclipse.collections.impl.set.mutable.AbstractMutableSet'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/fixed/EmptySet.java`
#### Snippet
```java
    @Deprecated
    @Override
    public <S> MutableSet<Pair<T, S>> zip(Iterable<S> that)
    {
        return Sets.fixedSize.of();
```

### Deprecation
Overrides deprecated method in 'org.eclipse.collections.impl.set.mutable.AbstractMutableSet'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/fixed/EmptySet.java`
#### Snippet
```java
    @Deprecated
    @Override
    public MutableSet<Pair<T, Integer>> zipWithIndex()
    {
        return Sets.fixedSize.of();
```

### Deprecation
'zipWithIndex(R)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnmodifiableMutableSet.java`
#### Snippet
```java
    public <R extends Collection<Pair<T, Integer>>> R zipWithIndex(R target)
    {
        return this.getMutableSet().zipWithIndex(target);
    }

```

### Deprecation
'zip(java.lang.Iterable, R)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnmodifiableMutableSet.java`
#### Snippet
```java
    public <S, R extends Collection<Pair<T, S>>> R zip(Iterable<S> that, R target)
    {
        return this.getMutableSet().zip(that, target);
    }

```

### Deprecation
Overrides deprecated method in 'org.eclipse.collections.impl.set.immutable.AbstractImmutableSet'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/immutable/ImmutableEmptySet.java`
#### Snippet
```java
    @Deprecated
    @Override
    public <S> ImmutableSet<Pair<T, S>> zip(Iterable<S> that)
    {
        return Sets.immutable.empty();
```

### Deprecation
Overrides deprecated method in 'org.eclipse.collections.impl.set.immutable.AbstractImmutableSet'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/immutable/ImmutableEmptySet.java`
#### Snippet
```java
    @Deprecated
    @Override
    public ImmutableSet<Pair<T, Integer>> zipWithIndex()
    {
        return Sets.immutable.empty();
```

### Deprecation
'forEachWithIndex(ObjectIntProcedure)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/lazy/primitive/CollectBooleanIterable.java`
#### Snippet
```java
    {
        boolean[] array = new boolean[this.size()];
        this.iterable.forEachWithIndex((each, index) -> array[index] = this.function.booleanValueOf(each));
        return array;
    }
```

### Deprecation
'selectAndRejectWith(T\[\], int, org.eclipse.collections.api.block.predicate.Predicate2, P)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/AbstractArrayAdapter.java`
#### Snippet
```java
            P parameter)
    {
        return InternalArrayIterate.selectAndRejectWith(this.items, this.items.length, predicate, parameter);
    }

```

### Deprecation
'selectAndRejectWith(T\[\], int, org.eclipse.collections.api.block.predicate.Predicate2, P)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/FastList.java`
#### Snippet
```java
            P parameter)
    {
        return InternalArrayIterate.selectAndRejectWith(this.items, this.size, predicate, parameter);
    }

```

### Deprecation
'injectInto(float, FloatObjectToFloatFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bimap/AbstractBiMap.java`
#### Snippet
```java
    public float injectInto(float injectedValue, FloatObjectToFloatFunction<? super V> function)
    {
        return this.getDelegate().injectInto(injectedValue, function);
    }

```

### Deprecation
'injectInto(double, DoubleObjectToDoubleFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bimap/AbstractBiMap.java`
#### Snippet
```java
    public double injectInto(double injectedValue, DoubleObjectToDoubleFunction<? super V> function)
    {
        return this.getDelegate().injectInto(injectedValue, function);
    }

```

### Deprecation
'getLast()' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bimap/AbstractBiMap.java`
#### Snippet
```java
    public V getLast()
    {
        return this.getDelegate().getLast();
    }

```

### Deprecation
'getFirst()' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bimap/AbstractBiMap.java`
#### Snippet
```java
    public V getFirst()
    {
        return this.getDelegate().getFirst();
    }

```

### Deprecation
'forEachWithIndex(ObjectIntProcedure)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bimap/AbstractBiMap.java`
#### Snippet
```java
    public void forEachWithIndex(ObjectIntProcedure<? super V> objectIntProcedure)
    {
        this.getDelegate().forEachWithIndex(objectIntProcedure);
    }

```

### Deprecation
'injectInto(int, IntObjectToIntFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bimap/AbstractBiMap.java`
#### Snippet
```java
    public int injectInto(int injectedValue, IntObjectToIntFunction<? super V> function)
    {
        return this.getDelegate().injectInto(injectedValue, function);
    }

```

### Deprecation
'injectInto(long, LongObjectToLongFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bimap/AbstractBiMap.java`
#### Snippet
```java
    public long injectInto(long injectedValue, LongObjectToLongFunction<? super V> function)
    {
        return this.getDelegate().injectInto(injectedValue, function);
    }

```

### Deprecation
'zip(java.lang.Iterable)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bimap/mutable/UnmodifiableBiMap.java`
#### Snippet
```java
    public <S> MutableSet<Pair<V, S>> zip(Iterable<S> that)
    {
        return this.delegate.zip(that);
    }

```

### Deprecation
'injectInto(float, FloatObjectToFloatFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bimap/mutable/UnmodifiableBiMap.java`
#### Snippet
```java
    public float injectInto(float injectedValue, FloatObjectToFloatFunction<? super V> function)
    {
        return this.delegate.injectInto(injectedValue, function);
    }

```

### Deprecation
'injectInto(long, LongObjectToLongFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bimap/mutable/UnmodifiableBiMap.java`
#### Snippet
```java
    public long injectInto(long injectedValue, LongObjectToLongFunction<? super V> function)
    {
        return this.delegate.injectInto(injectedValue, function);
    }

```

### Deprecation
'zipWithIndex(R)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bimap/mutable/UnmodifiableBiMap.java`
#### Snippet
```java
    public <R extends Collection<Pair<V, Integer>>> R zipWithIndex(R target)
    {
        return this.delegate.zipWithIndex(target);
    }

```

### Deprecation
'getFirst()' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bimap/mutable/UnmodifiableBiMap.java`
#### Snippet
```java
    public V getFirst()
    {
        return this.delegate.getFirst();
    }

```

### Deprecation
'injectInto(int, IntObjectToIntFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bimap/mutable/UnmodifiableBiMap.java`
#### Snippet
```java
    public int injectInto(int injectedValue, IntObjectToIntFunction<? super V> function)
    {
        return this.delegate.injectInto(injectedValue, function);
    }

```

### Deprecation
'injectInto(double, DoubleObjectToDoubleFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bimap/mutable/UnmodifiableBiMap.java`
#### Snippet
```java
    public double injectInto(double injectedValue, DoubleObjectToDoubleFunction<? super V> function)
    {
        return this.delegate.injectInto(injectedValue, function);
    }

```

### Deprecation
'forEachWithIndex(ObjectIntProcedure)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bimap/mutable/UnmodifiableBiMap.java`
#### Snippet
```java
    public void forEachWithIndex(ObjectIntProcedure<? super V> objectIntProcedure)
    {
        this.delegate.forEachWithIndex(objectIntProcedure);
    }

```

### Deprecation
'zipWithIndex()' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bimap/mutable/UnmodifiableBiMap.java`
#### Snippet
```java
    public MutableSet<Pair<V, Integer>> zipWithIndex()
    {
        return this.delegate.zipWithIndex();
    }

```

### Deprecation
'getLast()' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bimap/mutable/UnmodifiableBiMap.java`
#### Snippet
```java
    public V getLast()
    {
        return this.delegate.getLast();
    }

```

### Deprecation
'zip(java.lang.Iterable, R)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bimap/mutable/UnmodifiableBiMap.java`
#### Snippet
```java
    public <S, R extends Collection<Pair<V, S>>> R zip(Iterable<S> that, R target)
    {
        return this.delegate.zip(that, target);
    }

```

### Deprecation
'allSatisfy(java.lang.String, org.eclipse.collections.impl.block.predicate.CodePointPredicate)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/StringPredicates.java`
#### Snippet
```java
        public boolean accept(String anObject)
        {
            return StringIterate.allSatisfy(anObject, CodePointPredicate.IS_DIGIT);
        }

```

### Deprecation
'anySatisfy(java.lang.String, org.eclipse.collections.impl.block.predicate.CodePointPredicate)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/StringPredicates.java`
#### Snippet
```java
        public boolean accept(String anObject)
        {
            return StringIterate.anySatisfy(anObject, CodePointPredicate.IS_WHITESPACE);
        }

```

### Deprecation
'anySatisfy(java.lang.String, org.eclipse.collections.impl.block.predicate.CodePointPredicate)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/StringPredicates.java`
#### Snippet
```java
        public boolean accept(String anObject)
        {
            return StringIterate.anySatisfy(anObject, CodePointPredicate.IS_DIGIT);
        }

```

### Deprecation
'anySatisfy(java.lang.String, org.eclipse.collections.impl.block.predicate.CodePointPredicate)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/StringPredicates.java`
#### Snippet
```java
        public boolean accept(String anObject)
        {
            return StringIterate.anySatisfy(anObject, CodePointPredicate.IS_UNDEFINED);
        }

```

### Deprecation
'anySatisfy(java.lang.String, org.eclipse.collections.impl.block.predicate.CodePointPredicate)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/StringPredicates.java`
#### Snippet
```java
        public boolean accept(String anObject)
        {
            return StringIterate.anySatisfy(anObject, CodePointPredicate.IS_LETTER_OR_DIGIT);
        }

```

### Deprecation
'allSatisfy(java.lang.String, org.eclipse.collections.impl.block.predicate.CodePointPredicate)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/StringPredicates.java`
#### Snippet
```java
        public boolean accept(String anObject)
        {
            return StringIterate.allSatisfy(anObject, CodePointPredicate.IS_LETTER_OR_DIGIT);
        }

```

### Deprecation
'anySatisfy(java.lang.String, org.eclipse.collections.impl.block.predicate.CodePointPredicate)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/StringPredicates.java`
#### Snippet
```java
        public boolean accept(String anObject)
        {
            return StringIterate.anySatisfy(anObject, CodePointPredicate.IS_UPPERCASE);
        }

```

### Deprecation
'anySatisfy(java.lang.String, org.eclipse.collections.impl.block.predicate.CodePointPredicate)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/StringPredicates.java`
#### Snippet
```java
        public boolean accept(String anObject)
        {
            return StringIterate.anySatisfy(anObject, CodePointPredicate.IS_LETTER);
        }

```

### Deprecation
'anySatisfy(java.lang.String, org.eclipse.collections.impl.block.predicate.CodePointPredicate)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/StringPredicates.java`
#### Snippet
```java
        public boolean accept(String anObject)
        {
            return StringIterate.anySatisfy(anObject, CodePointPredicate.IS_LOWERCASE);
        }

```

### Deprecation
'allSatisfy(java.lang.String, org.eclipse.collections.impl.block.predicate.CodePointPredicate)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/StringPredicates.java`
#### Snippet
```java
        public boolean accept(String anObject)
        {
            return anObject != null && StringIterate.allSatisfy(anObject, CodePointPredicate.IS_LETTER);
        }

```

### Deprecation
'org.eclipse.collections.impl.stack.immutable.ImmutableArrayStack' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/stack/immutable/package-info.java`
#### Snippet
```java
 * <ul>
 *     <li>
 *          {@link org.eclipse.collections.impl.stack.immutable.ImmutableArrayStack} - an ImmutableStack backed by a FastList.
 *     </li>
 * </ul>
```

### Deprecation
'injectInto(int, IntObjectToIntFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/stack/mutable/SynchronizedStack.java`
#### Snippet
```java
        synchronized (this.lock)
        {
            return this.delegate.injectInto(injectedValue, intObjectToIntFunction);
        }
    }
```

### Deprecation
'injectInto(double, DoubleObjectToDoubleFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/stack/mutable/SynchronizedStack.java`
#### Snippet
```java
        synchronized (this.lock)
        {
            return this.delegate.injectInto(injectedValue, doubleObjectToDoubleFunction);
        }
    }
```

### Deprecation
'injectInto(long, LongObjectToLongFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/stack/mutable/SynchronizedStack.java`
#### Snippet
```java
        synchronized (this.lock)
        {
            return this.delegate.injectInto(injectedValue, longObjectToLongFunction);
        }
    }
```

### Deprecation
'injectInto(float, FloatObjectToFloatFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/stack/mutable/SynchronizedStack.java`
#### Snippet
```java
        synchronized (this.lock)
        {
            return this.delegate.injectInto(injectedValue, floatObjectToFloatFunction);
        }
    }
```

### Deprecation
'injectInto(double, DoubleObjectToDoubleFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/stack/mutable/UnmodifiableStack.java`
#### Snippet
```java
    public double injectInto(double injectedValue, DoubleObjectToDoubleFunction<? super T> doubleObjectToDoubleFunction)
    {
        return this.mutableStack.injectInto(injectedValue, doubleObjectToDoubleFunction);
    }

```

### Deprecation
'injectInto(long, LongObjectToLongFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/stack/mutable/UnmodifiableStack.java`
#### Snippet
```java
    public long injectInto(long injectedValue, LongObjectToLongFunction<? super T> longObjectToLongFunction)
    {
        return this.mutableStack.injectInto(injectedValue, longObjectToLongFunction);
    }

```

### Deprecation
'injectInto(int, IntObjectToIntFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/stack/mutable/UnmodifiableStack.java`
#### Snippet
```java
    public int injectInto(int injectedValue, IntObjectToIntFunction<? super T> intObjectToIntFunction)
    {
        return this.mutableStack.injectInto(injectedValue, intObjectToIntFunction);
    }

```

### Deprecation
'injectInto(float, FloatObjectToFloatFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/stack/mutable/UnmodifiableStack.java`
#### Snippet
```java
    public float injectInto(float injectedValue, FloatObjectToFloatFunction<? super T> floatObjectToFloatFunction)
    {
        return this.mutableStack.injectInto(injectedValue, floatObjectToFloatFunction);
    }

```

### Deprecation
'zipWithIndex(R)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/Iterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#zipWithIndex(Collection)
     */
    public static <T, R extends Collection<Pair<T, Integer>>> R zipWithIndex(
```

### Deprecation
'zipWithIndex(R)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/Iterate.java`
#### Snippet
```java
        if (iterable instanceof RichIterable)
        {
            return ((RichIterable<T>) iterable).zipWithIndex(targetCollection);
        }
        if (iterable instanceof ArrayList)
```

### Deprecation
'zip(java.lang.Iterable, R)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/Iterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#zip(Iterable, Collection)
     */
    public static <X, Y, R extends Collection<Pair<X, Y>>> R zip(
```

### Deprecation
'zip(java.lang.Iterable, R)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/Iterate.java`
#### Snippet
```java
        if (xs instanceof RichIterable)
        {
            return ((RichIterable<X>) xs).zip(ys, targetCollection);
        }
        if (xs instanceof ArrayList)
```

### Deprecation
'selectAndRejectWith(org.eclipse.collections.api.block.predicate.Predicate2, P)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/Iterate.java`
#### Snippet
```java
        if (iterable instanceof MutableCollection)
        {
            return ((MutableCollection<T>) iterable).selectAndRejectWith(predicate, injectedValue);
        }
        if (iterable instanceof ArrayList)
```

### Deprecation
'forEachWithIndex(ObjectIntProcedure)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/Iterate.java`
#### Snippet
```java
        if (iterable instanceof InternalIterable)
        {
            ((InternalIterable<T>) iterable).forEachWithIndex(objectIntProcedure);
        }
        else if (iterable instanceof ArrayList)
```

### Deprecation
'zip(java.lang.Iterable)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/Iterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#zip(Iterable)
     */
    public static <X, Y> Collection<Pair<X, Y>> zip(Iterable<X> xs, Iterable<Y> ys)
```

### Deprecation
'zip(java.lang.Iterable)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/Iterate.java`
#### Snippet
```java
        if (xs instanceof MutableCollection)
        {
            return ((MutableCollection<X>) xs).zip(ys);
        }
        if (xs instanceof ArrayList)
```

### Deprecation
'injectInto(long, LongObjectToLongFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/Iterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#injectInto(long, LongObjectToLongFunction)
     */
    public static <T> long injectInto(
```

### Deprecation
'injectInto(long, LongObjectToLongFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/Iterate.java`
#### Snippet
```java
        if (iterable instanceof RichIterable)
        {
            return ((RichIterable<T>) iterable).injectInto(injectValue, function);
        }
        if (iterable instanceof ArrayList)
```

### Deprecation
'getFirst()' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/Iterate.java`
#### Snippet
```java
        if (iterable instanceof RichIterable)
        {
            return ((RichIterable<T>) iterable).getFirst();
        }
        if (iterable instanceof List)
```

### Deprecation
'getLast()' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/Iterate.java`
#### Snippet
```java
        if (iterable instanceof RichIterable)
        {
            return ((RichIterable<T>) iterable).getLast();
        }
        if (iterable instanceof RandomAccess)
```

### Deprecation
'injectInto(double, DoubleObjectToDoubleFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/Iterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#injectInto(double, DoubleObjectToDoubleFunction)
     */
    public static <T> double injectInto(
```

### Deprecation
'injectInto(double, DoubleObjectToDoubleFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/Iterate.java`
#### Snippet
```java
        if (iterable instanceof RichIterable)
        {
            return ((RichIterable<T>) iterable).injectInto(injectValue, function);
        }
        if (iterable instanceof ArrayList)
```

### Deprecation
'injectInto(float, FloatObjectToFloatFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/Iterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#injectInto(float, FloatObjectToFloatFunction)
     */
    public static <T> float injectInto(
```

### Deprecation
'injectInto(float, FloatObjectToFloatFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/Iterate.java`
#### Snippet
```java
        if (iterable instanceof RichIterable)
        {
            return ((RichIterable<T>) iterable).injectInto(injectValue, function);
        }
        if (iterable instanceof ArrayList)
```

### Deprecation
'injectInto(int, IntObjectToIntFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/Iterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#injectInto(int, IntObjectToIntFunction)
     */
    public static <T> int injectInto(
```

### Deprecation
'injectInto(int, IntObjectToIntFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/Iterate.java`
#### Snippet
```java
        if (iterable instanceof RichIterable)
        {
            return ((RichIterable<T>) iterable).injectInto(injectValue, function);
        }
        if (iterable instanceof ArrayList)
```

### Deprecation
'zipWithIndex()' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/Iterate.java`
#### Snippet
```java

    /**
     * @see RichIterable#zipWithIndex()
     */
    public static <T> Collection<Pair<T, Integer>> zipWithIndex(Iterable<T> iterable)
```

### Deprecation
'zipWithIndex()' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/Iterate.java`
#### Snippet
```java
        if (iterable instanceof MutableCollection)
        {
            return ((MutableCollection<T>) iterable).zipWithIndex();
        }
        if (iterable instanceof ArrayList)
```

### Deprecation
'selectAndRejectWith(org.eclipse.collections.api.block.predicate.Predicate2, P)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractSynchronizedMutableCollection.java`
#### Snippet
```java
        synchronized (this.lock)
        {
            return this.getDelegate().selectAndRejectWith(predicate, parameter);
        }
    }
```

### Deprecation
'injectInto(int, IntObjectToIntFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractUnmodifiableMutableCollection.java`
#### Snippet
```java
    public int injectInto(int injectedValue, IntObjectToIntFunction<? super T> function)
    {
        return this.getMutableCollection().injectInto(injectedValue, function);
    }

```

### Deprecation
'getFirst()' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractUnmodifiableMutableCollection.java`
#### Snippet
```java
    public T getFirst()
    {
        return this.getMutableCollection().getFirst();
    }

```

### Deprecation
'zipWithIndex()' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractUnmodifiableMutableCollection.java`
#### Snippet
```java
    public MutableCollection<Pair<T, Integer>> zipWithIndex()
    {
        return this.getMutableCollection().zipWithIndex();
    }

```

### Deprecation
'injectInto(float, FloatObjectToFloatFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractUnmodifiableMutableCollection.java`
#### Snippet
```java
    public float injectInto(float injectedValue, FloatObjectToFloatFunction<? super T> function)
    {
        return this.getMutableCollection().injectInto(injectedValue, function);
    }

```

### Deprecation
'getLast()' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractUnmodifiableMutableCollection.java`
#### Snippet
```java
    public T getLast()
    {
        return this.getMutableCollection().getLast();
    }

```

### Deprecation
'forEachWithIndex(ObjectIntProcedure)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractUnmodifiableMutableCollection.java`
#### Snippet
```java
    public void forEachWithIndex(ObjectIntProcedure<? super T> objectIntProcedure)
    {
        this.getMutableCollection().forEachWithIndex(objectIntProcedure);
    }

```

### Deprecation
'zip(java.lang.Iterable)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractUnmodifiableMutableCollection.java`
#### Snippet
```java
    public <S> MutableCollection<Pair<T, S>> zip(Iterable<S> that)
    {
        return this.getMutableCollection().zip(that);
    }

```

### Deprecation
'zipWithIndex(R)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractUnmodifiableMutableCollection.java`
#### Snippet
```java
    public <R extends Collection<Pair<T, Integer>>> R zipWithIndex(R target)
    {
        return this.getMutableCollection().zipWithIndex(target);
    }

```

### Deprecation
'zip(java.lang.Iterable, R)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractUnmodifiableMutableCollection.java`
#### Snippet
```java
    public <S, R extends Collection<Pair<T, S>>> R zip(Iterable<S> that, R target)
    {
        return this.getMutableCollection().zip(that, target);
    }

```

### Deprecation
'selectAndRejectWith(org.eclipse.collections.api.block.predicate.Predicate2, P)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractUnmodifiableMutableCollection.java`
#### Snippet
```java
            P parameter)
    {
        return this.getMutableCollection().selectAndRejectWith(predicate, parameter);
    }

```

### Deprecation
'injectInto(double, DoubleObjectToDoubleFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractUnmodifiableMutableCollection.java`
#### Snippet
```java
    public double injectInto(double injectedValue, DoubleObjectToDoubleFunction<? super T> function)
    {
        return this.getMutableCollection().injectInto(injectedValue, function);
    }

```

### Deprecation
'injectInto(long, LongObjectToLongFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractUnmodifiableMutableCollection.java`
#### Snippet
```java
    public long injectInto(long injectedValue, LongObjectToLongFunction<? super T> function)
    {
        return this.getMutableCollection().injectInto(injectedValue, function);
    }

```

### Deprecation
'zipWithIndex()' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/AbstractSynchronizedRichIterable.java`
#### Snippet
```java
        synchronized (this.lock)
        {
            return this.delegate.zipWithIndex();
        }
    }
```

### Deprecation
'injectInto(long, LongObjectToLongFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/AbstractSynchronizedRichIterable.java`
#### Snippet
```java
        synchronized (this.lock)
        {
            return this.delegate.injectInto(injectedValue, function);
        }
    }
```

### Deprecation
'getLast()' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/AbstractSynchronizedRichIterable.java`
#### Snippet
```java
        synchronized (this.lock)
        {
            return this.delegate.getLast();
        }
    }
```

### Deprecation
'getFirst()' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/AbstractSynchronizedRichIterable.java`
#### Snippet
```java
        synchronized (this.lock)
        {
            return this.delegate.getFirst();
        }
    }
```

### Deprecation
'zip(java.lang.Iterable, R)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/AbstractSynchronizedRichIterable.java`
#### Snippet
```java
        synchronized (this.lock)
        {
            return this.delegate.zip(that, target);
        }
    }
```

### Deprecation
'injectInto(int, IntObjectToIntFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/AbstractSynchronizedRichIterable.java`
#### Snippet
```java
        synchronized (this.lock)
        {
            return this.delegate.injectInto(injectedValue, function);
        }
    }
```

### Deprecation
'zipWithIndex(R)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/AbstractSynchronizedRichIterable.java`
#### Snippet
```java
        synchronized (this.lock)
        {
            return this.delegate.zipWithIndex(target);
        }
    }
```

### Deprecation
'injectInto(double, DoubleObjectToDoubleFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/AbstractSynchronizedRichIterable.java`
#### Snippet
```java
        synchronized (this.lock)
        {
            return this.delegate.injectInto(injectedValue, function);
        }
    }
```

### Deprecation
'injectInto(float, FloatObjectToFloatFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/AbstractSynchronizedRichIterable.java`
#### Snippet
```java
        synchronized (this.lock)
        {
            return this.delegate.injectInto(injectedValue, function);
        }
    }
```

### Deprecation
'zip(java.lang.Iterable)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/AbstractSynchronizedRichIterable.java`
#### Snippet
```java
        synchronized (this.lock)
        {
            return this.delegate.zip(that);
        }
    }
```

### Deprecation
'forEachWithIndex(ObjectIntProcedure)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/AbstractSynchronizedRichIterable.java`
#### Snippet
```java
        synchronized (this.lock)
        {
            this.delegate.forEachWithIndex(objectIntProcedure);
        }
    }
```

### Deprecation
'injectInto(long, LongObjectToLongFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractMultiReaderMutableCollection.java`
#### Snippet
```java
        try (LockWrapper wrapper = this.lockWrapper.acquireReadLock())
        {
            return this.getDelegate().injectInto(injectedValue, function);
        }
    }
```

### Deprecation
'zip(java.lang.Iterable, R)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractMultiReaderMutableCollection.java`
#### Snippet
```java
        public <S, R extends Collection<Pair<T, S>>> R zip(Iterable<S> that, R target)
        {
            return this.delegate.zip(that, target);
        }

```

### Deprecation
'zip(java.lang.Iterable, R)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractMultiReaderMutableCollection.java`
#### Snippet
```java
        try (LockWrapper wrapper = this.lockWrapper.acquireReadLock())
        {
            return this.getDelegate().zip(that, target);
        }
    }
```

### Deprecation
'injectInto(int, IntObjectToIntFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractMultiReaderMutableCollection.java`
#### Snippet
```java
        try (LockWrapper wrapper = this.lockWrapper.acquireReadLock())
        {
            return this.getDelegate().injectInto(injectedValue, function);
        }
    }
```

### Deprecation
'injectInto(float, FloatObjectToFloatFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractMultiReaderMutableCollection.java`
#### Snippet
```java
        public float injectInto(float injectedValue, FloatObjectToFloatFunction<? super T> function)
        {
            return this.delegate.injectInto(injectedValue, function);
        }

```

### Deprecation
'getLast()' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractMultiReaderMutableCollection.java`
#### Snippet
```java
        try (LockWrapper wrapper = this.lockWrapper.acquireReadLock())
        {
            return this.getDelegate().getLast();
        }
    }
```

### Deprecation
'getFirst()' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractMultiReaderMutableCollection.java`
#### Snippet
```java
        public T getFirst()
        {
            return this.delegate.getFirst();
        }

```

### Deprecation
'zipWithIndex(R)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractMultiReaderMutableCollection.java`
#### Snippet
```java
        public <R extends Collection<Pair<T, Integer>>> R zipWithIndex(R target)
        {
            return this.delegate.zipWithIndex(target);
        }

```

### Deprecation
'injectInto(double, DoubleObjectToDoubleFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractMultiReaderMutableCollection.java`
#### Snippet
```java
        public double injectInto(double injectedValue, DoubleObjectToDoubleFunction<? super T> function)
        {
            return this.delegate.injectInto(injectedValue, function);
        }

```

### Deprecation
'selectAndRejectWith(org.eclipse.collections.api.block.predicate.Predicate2, P)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractMultiReaderMutableCollection.java`
#### Snippet
```java
        try (LockWrapper wrapper = this.lockWrapper.acquireReadLock())
        {
            return this.getDelegate().selectAndRejectWith(predicate, parameter);
        }
    }
```

### Deprecation
'injectInto(long, LongObjectToLongFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractMultiReaderMutableCollection.java`
#### Snippet
```java
        public long injectInto(long injectedValue, LongObjectToLongFunction<? super T> function)
        {
            return this.delegate.injectInto(injectedValue, function);
        }

```

### Deprecation
'getLast()' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractMultiReaderMutableCollection.java`
#### Snippet
```java
        public T getLast()
        {
            return this.delegate.getLast();
        }

```

### Deprecation
'zipWithIndex(R)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractMultiReaderMutableCollection.java`
#### Snippet
```java
        try (LockWrapper wrapper = this.lockWrapper.acquireReadLock())
        {
            return this.getDelegate().zipWithIndex(target);
        }
    }
```

### Deprecation
'forEachWithIndex(ObjectIntProcedure)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractMultiReaderMutableCollection.java`
#### Snippet
```java
        try (LockWrapper wrapper = this.lockWrapper.acquireReadLock())
        {
            this.getDelegate().forEachWithIndex(objectIntProcedure);
        }
    }
```

### Deprecation
'selectAndRejectWith(org.eclipse.collections.api.block.predicate.Predicate2, P)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractMultiReaderMutableCollection.java`
#### Snippet
```java
                P parameter)
        {
            return this.delegate.selectAndRejectWith(predicate, parameter);
        }

```

### Deprecation
'forEachWithIndex(ObjectIntProcedure)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractMultiReaderMutableCollection.java`
#### Snippet
```java
        public void forEachWithIndex(ObjectIntProcedure<? super T> objectIntProcedure)
        {
            this.delegate.forEachWithIndex(objectIntProcedure);
        }

```

### Deprecation
'injectInto(double, DoubleObjectToDoubleFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractMultiReaderMutableCollection.java`
#### Snippet
```java
        try (LockWrapper wrapper = this.lockWrapper.acquireReadLock())
        {
            return this.getDelegate().injectInto(injectedValue, function);
        }
    }
```

### Deprecation
'injectInto(int, IntObjectToIntFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractMultiReaderMutableCollection.java`
#### Snippet
```java
        public int injectInto(int injectedValue, IntObjectToIntFunction<? super T> function)
        {
            return this.delegate.injectInto(injectedValue, function);
        }

```

### Deprecation
'injectInto(float, FloatObjectToFloatFunction)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractMultiReaderMutableCollection.java`
#### Snippet
```java
        try (LockWrapper wrapper = this.lockWrapper.acquireReadLock())
        {
            return this.getDelegate().injectInto(injectedValue, function);
        }
    }
```

### Deprecation
'getFirst()' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractMultiReaderMutableCollection.java`
#### Snippet
```java
        try (LockWrapper wrapper = this.lockWrapper.acquireReadLock())
        {
            return this.getDelegate().getFirst();
        }
    }
```

### Deprecation
'junit.framework.Assert' is deprecated
in `eclipse-collections-testutils/src/main/java/org/eclipse/collections/impl/test/Verify.java`
#### Snippet
```java
                    {
                        //noinspection UseOfObsoleteAssert
                        junit.framework.Assert.failNotEquals(iterableName + " first differed at element [" + index + "];", eachExpected, eachActual);
                    }
                    index++;
```

### Deprecation
'junit.framework.Assert' is deprecated
in `eclipse-collections-testutils/src/main/java/org/eclipse/collections/impl/test/Verify.java`
#### Snippet
```java
                if (!Objects.equals(eachExpected, eachActual))
                {
                    junit.framework.Assert.failNotEquals(listName + " first differed at element [" + index + "];", eachExpected, eachActual);
                }
            }
```

### Deprecation
'junit.framework.Assert' is deprecated
in `eclipse-collections-testutils/src/main/java/org/eclipse/collections/impl/test/Verify.java`
#### Snippet
```java

                //noinspection UseOfObsoleteAssert
                junit.framework.Assert.failNotEquals(message, inExpectedOnlySet, inActualOnlySet);
            }
        }
```

### Deprecation
'zipWithIndex()' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collector/Collectors2.java`
#### Snippet
```java
     * {@code MutableList<ObjectIntPair<Integer>> zipWithIndex2 = Interval.oneTo(10).reduceInPlace(Collectors2.zipWithIndex());}
     * <p>
     * Equivalent to using @{@link RichIterable#zipWithIndex()}
     * </p>
     * {@code LazyIterable<Pair<Integer, Integer>> zipWithIndex = Interval.oneTo(10).zipWithIndex();}
```

### Deprecation
'zip(java.lang.Iterable)' is deprecated
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collector/Collectors2.java`
#### Snippet
```java
     * {@code MutableList<Pair<Integer, Integer>> zipped2 = Interval.oneTo(10).reduceInPlace(Collectors2.zip(Interval.oneTo(10)));}<br>
     * <p>
     * Equivalent to using @{@link RichIterable#zip(Iterable)}
     * </p>
     * {@code LazyIterable<Pair<Integer, Integer>> zip = Interval.oneTo(10).zip(Interval.oneTo(10));}
```

## RuleId[id=SuspiciousMethodCalls]
### SuspiciousMethodCalls
Suspicious call to 'Map.remove()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/sorted/mutable/TreeBag.java`
#### Snippet
```java
        for (Object each : iterable)
        {
            Counter removed = this.items.remove(each);
            if (removed != null)
            {
```

### SuspiciousMethodCalls
Suspicious call to 'Map.remove()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/sorted/mutable/TreeBag.java`
#### Snippet
```java
        if (occurrences >= startCount)
        {
            this.items.remove(item);
            this.size -= startCount;
            return true;
```

### SuspiciousMethodCalls
'MutableCollection' may not contain objects of type 'capture of ?'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/immutable/AbstractImmutableBagIterable.java`
#### Snippet
```java
        if (elements instanceof Set)
        {
            result.removeAll((Set<?>) elements);
        }
        else if (elements instanceof List)
```

### SuspiciousMethodCalls
Suspicious call to 'TreeSet.contains()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/mutable/TreeSortedSet.java`
#### Snippet
```java
    public int indexOf(Object object)
    {
        if (this.treeSet.contains(object))
        {
            return this.treeSet.headSet((T) object).size();
```

### SuspiciousMethodCalls
Suspicious call to 'Set.contains()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/mutable/SortedSetAdapter.java`
#### Snippet
```java
    public int indexOf(Object object)
    {
        if (this.delegate.contains(object))
        {
            return this.delegate.headSet((T) object).size();
```

### SuspiciousMethodCalls
Suspicious call to 'UnifiedMap.containsKey()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bimap/mutable/AbstractMutableBiMap.java`
#### Snippet
```java
        public boolean contains(Object key)
        {
            return AbstractMutableBiMap.this.delegate.containsKey(key);
        }

```

### SuspiciousMethodCalls
Suspicious call to 'UnifiedMap.containsKey()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bimap/mutable/AbstractMutableBiMap.java`
#### Snippet
```java
            for (Object key : collection)
            {
                if (!AbstractMutableBiMap.this.inverse.delegate.containsKey(key))
                {
                    return false;
```

### SuspiciousMethodCalls
Suspicious call to 'UnifiedMap.containsKey()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bimap/mutable/AbstractMutableBiMap.java`
#### Snippet
```java
            for (Object key : source)
            {
                if (!AbstractMutableBiMap.this.delegate.containsKey(key))
                {
                    return false;
```

### SuspiciousMethodCalls
Suspicious call to 'UnifiedMap.containsKey()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bimap/mutable/AbstractMutableBiMap.java`
#### Snippet
```java
        public boolean contains(Object key)
        {
            return AbstractMutableBiMap.this.inverse.delegate.containsKey(key);
        }

```

### SuspiciousMethodCalls
Suspicious call to 'UnifiedMap.containsKey()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bimap/mutable/AbstractMutableBiMap.java`
#### Snippet
```java
    public V remove(Object key)
    {
        if (!this.delegate.containsKey(key))
        {
            return null;
```

### SuspiciousMethodCalls
'Set' may not contain objects of type 'capture of ? extends E'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/factory/Sets.java`
#### Snippet
```java
    {
        return candidateSubset.size() < candidateSuperset.size()
                && candidateSuperset.containsAll(candidateSubset);
    }

```

### SuspiciousMethodCalls
'Set' may not contain objects of type 'capture of ? extends E'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/factory/Sets.java`
#### Snippet
```java
    {
        return candidateSubset.size() <= candidateSuperset.size()
                && candidateSuperset.containsAll(candidateSubset);
    }

```

### SuspiciousMethodCalls
Suspicious call to 'Map.remove()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/multimap/AbstractMutableMultimap.java`
#### Snippet
```java
    public C removeAll(Object key)
    {
        C collection = this.map.remove(key);
        collection = collection == null ? this.createCollection() : collection;
        this.subtractFromTotalSize(collection.size());
```

### SuspiciousMethodCalls
Suspicious call to 'Collection.remove()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/multimap/AbstractMutableMultimap.java`
#### Snippet
```java
        }

        boolean changed = collection.remove(value);
        if (changed)
        {
```

### SuspiciousMethodCalls
Suspicious call to 'Map.remove()'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/multimap/AbstractMutableMultimap.java`
#### Snippet
```java
            if (collection.isEmpty())
            {
                this.map.remove(key);
            }
        }
```

### SuspiciousMethodCalls
'MutableCollection' may not contain objects of type 'capture of ?'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/immutable/AbstractImmutableCollection.java`
#### Snippet
```java
        if (elements instanceof Set)
        {
            result.removeAll((Set<?>) elements);
        }
        else if (elements instanceof List)
```

### SuspiciousMethodCalls
'MutableSet' may not contain objects of type 'capture of ?'
in `eclipse-collections-testutils/src/main/java/org/eclipse/collections/impl/test/Verify.java`
#### Snippet
```java
            {
                MutableSet<?> inExpectedOnlySet = UnifiedSet.newSet(expectedSet);
                inExpectedOnlySet.removeAll(actualSet);

                int numberDifferences = inExpectedOnlySet.size();
```

### SuspiciousMethodCalls
'MutableSet' may not contain objects of type 'capture of ?'
in `eclipse-collections-testutils/src/main/java/org/eclipse/collections/impl/test/Verify.java`
#### Snippet
```java

                MutableSet<?> inActualOnlySet = UnifiedSet.newSet(actualSet);
                inActualOnlySet.removeAll(expectedSet);

                //noinspection UseOfObsoleteAssert
```

## RuleId[id=DanglingJavadoc]
### DanglingJavadoc
Dangling Javadoc comment
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/map/ChainMapPutTest.java`
#### Snippet
```java
        String[] localElements = this.elements;

        /**
         * @see HashMap#DEFAULT_INITIAL_CAPACITY
         */
```

### DanglingJavadoc
Dangling Javadoc comment
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/map/ChainMapPutTest.java`
#### Snippet
```java
        String[] localElements = this.elements;

        /**
         * @see HashTable#initialSize()
         */
```

### DanglingJavadoc
Dangling Javadoc comment
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/map/ChainMapPutTest.java`
#### Snippet
```java
        float localLoadFactor = this.loadFactor;
        String[] localElements = this.elements;
        /**
         * @see UnifiedMap#DEFAULT_INITIAL_CAPACITY
         */
```

### DanglingJavadoc
Dangling Javadoc comment
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/map/MapGrowthTest.java`
#### Snippet
```java
    {
        int localSize = this.size;
        /**
         * @see UnifiedMap#DEFAULT_LOAD_FACTOR
         */
```

### DanglingJavadoc
Dangling Javadoc comment
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/primitive/ObjectBooleanHashMap.java`
#### Snippet
```java
    {
        out.writeInt(this.size());
        /**
         * @deprecated in 5.1.0.
         */
```

### DanglingJavadoc
Dangling Javadoc comment
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/primitive/ObjectBooleanHashMap.java`
#### Snippet
```java
    {
        int size = in.readInt();
        /**
         * @deprecated in 5.1.0.
         */
```

## RuleId[id=NullableProblems]
### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections-api/src/main/java/org/eclipse/collections/api/map/FixedSizeMap.java`
#### Snippet
```java
     */
    @Override
    void putAll(Map<? extends K, ? extends V> map);

    /**
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections-api/src/main/java/org/eclipse/collections/api/collection/FixedSizeCollection.java`
#### Snippet
```java
     */
    @Override
    boolean addAll(Collection<? extends T> collection);

    /**
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections-api/src/main/java/org/eclipse/collections/api/collection/FixedSizeCollection.java`
#### Snippet
```java
     */
    @Override
    boolean removeAll(Collection<?> collection);

    /**
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections-api/src/main/java/org/eclipse/collections/api/collection/FixedSizeCollection.java`
#### Snippet
```java
     */
    @Override
    boolean retainAll(Collection<?> collection);

    /**
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/UnmodifiableMap.java`
#### Snippet
```java

    @Override
    public void putAll(Map<? extends K, ? extends V> t)
    {
        throw new UnsupportedOperationException("Cannot call putAll() on " + this.getClass().getSimpleName());
```

### NullableProblems
Non-annotated method 'toArray' from 'AbstractRichIterable' implements non-null method from 'Collection'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/AbstractBag.java`
#### Snippet
```java
public abstract class AbstractBag<T>
        extends AbstractRichIterable<T>
        implements Collection<T>, Bag<T>
{
    @Override
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/sorted/mutable/UnmodifiableSortedBag.java`
#### Snippet
```java

    @Override
    public int compareTo(SortedBag<T> o)
    {
        return this.getSortedBag().compareTo(o);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/sorted/mutable/SynchronizedSortedBag.java`
#### Snippet
```java

    @Override
    public int compareTo(SortedBag<T> o)
    {
        synchronized (this.getLock())
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/sorted/immutable/ImmutableSortedBagImpl.java`
#### Snippet
```java

    @Override
    public <E> E[] toArray(E[] array)
    {
        E[] array1 = array;
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/sorted/immutable/ImmutableSortedBagImpl.java`
#### Snippet
```java

    @Override
    public int compareTo(SortedBag<T> otherBag)
    {
        return SortedBagIterables.compare(this, otherBag);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/sorted/mutable/TreeBag.java`
#### Snippet
```java

    @Override
    public int compareTo(SortedBag<T> otherBag)
    {
        return SortedBagIterables.compare(this, otherBag);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/mutable/AbstractMutableBagIterable.java`
#### Snippet
```java

    @Override
    public boolean removeAll(Collection<?> collection)
    {
        return this.removeAllIterable(collection);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/mutable/AbstractMutableBagIterable.java`
#### Snippet
```java

    @Override
    public boolean addAll(Collection<? extends T> source)
    {
        return this.addAllIterable(source);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/mutable/AbstractMutableBagIterable.java`
#### Snippet
```java

    @Override
    public boolean retainAll(Collection<?> collection)
    {
        return this.retainAllIterable(collection);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/immutable/AbstractImmutableBagIterable.java`
#### Snippet
```java

    @Override
    public boolean removeAll(Collection<?> collection)
    {
        throw new UnsupportedOperationException("Cannot call removeAll() on " + this.getClass().getSimpleName());
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/immutable/AbstractImmutableBagIterable.java`
#### Snippet
```java

    @Override
    public boolean addAll(Collection<? extends T> collection)
    {
        throw new UnsupportedOperationException("Cannot call addAll() on " + this.getClass().getSimpleName());
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/immutable/AbstractImmutableBagIterable.java`
#### Snippet
```java

    @Override
    public boolean retainAll(Collection<?> collection)
    {
        throw new UnsupportedOperationException("Cannot call retainAll() on " + this.getClass().getSimpleName());
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/immutable/ImmutableHashBag.java`
#### Snippet
```java

    @Override
    public <T> T[] toArray(T[] a)
    {
        return this.delegate.toArray(a);
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/mutable/MultiReaderHashBag.java`
#### Snippet
```java

        @Override
        public Iterator<T> iterator()
        {
            UntouchableIterator<T> iterator = new UntouchableIterator<>(this.delegate.iterator());
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/immutable/ImmutableEmptyBag.java`
#### Snippet
```java

    @Override
    public <T> T[] toArray(T[] a)
    {
        if (a.length > 0)
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/fixed/AbstractMemoryEfficientMutableMap.java`
#### Snippet
```java

    @Override
    public void putAll(Map<? extends K, ? extends V> map)
    {
        throw new UnsupportedOperationException("Cannot call putAll() on " + this.getClass().getSimpleName());
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/immutable/ImmutableSingletonBag.java`
#### Snippet
```java

    @Override
    public Iterator<T> iterator()
    {
        return new SingletonIterator();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/AbstractSynchronizedMapIterable.java`
#### Snippet
```java

    @Override
    public void putAll(Map<? extends K, ? extends V> map)
    {
        synchronized (this.lock)
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/sorted/mutable/SortedMapAdapter.java`
#### Snippet
```java

    @Override
    public void putAll(Map<? extends K, ? extends V> map)
    {
        this.delegate.putAll(map);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/sorted/mutable/TreeSortedMap.java`
#### Snippet
```java

    @Override
    public void putAll(Map<? extends K, ? extends V> map)
    {
        this.treeMap.putAll(map);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/sorted/immutable/ImmutableTreeMap.java`
#### Snippet
```java

        @Override
        public boolean retainAll(Collection<?> collection)
        {
            throw new UnsupportedOperationException("Cannot call retainAll() on " + this.getClass().getSimpleName());
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/sorted/immutable/ImmutableTreeMap.java`
#### Snippet
```java

        @Override
        public boolean containsAll(Collection<?> collection)
        {
            return Iterate.allSatisfyWith(collection, Predicates2.in(), this);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/sorted/immutable/ImmutableTreeMap.java`
#### Snippet
```java

        @Override
        public boolean addAll(Collection<? extends K> collection)
        {
            throw new UnsupportedOperationException("Cannot call addAll() on " + this.getClass().getSimpleName());
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/MapAdapter.java`
#### Snippet
```java

    @Override
    public void putAll(Map<? extends K, ? extends V> map)
    {
        this.delegate.putAll(map);
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentMutableHashMap.java`
#### Snippet
```java

    @Override
    public Set<Entry<K, V>> entrySet()
    {
        return this.delegate.entrySet();
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentMutableHashMap.java`
#### Snippet
```java

    @Override
    public Collection<V> values()
    {
        return this.delegate.values();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentMutableHashMap.java`
#### Snippet
```java

    @Override
    public V replace(K key, V value)
    {
        return this.delegate.replace(key, value);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentMutableHashMap.java`
#### Snippet
```java

    @Override
    public V replace(K key, V value)
    {
        return this.delegate.replace(key, value);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentMutableHashMap.java`
#### Snippet
```java

    @Override
    public void putAll(Map<? extends K, ? extends V> map)
    {
        this.delegate.putAll(map);
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentMutableHashMap.java`
#### Snippet
```java

    @Override
    public Set<K> keySet()
    {
        return this.delegate.keySet();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentMutableHashMap.java`
#### Snippet
```java

    @Override
    public boolean replace(K key, V oldValue, V newValue)
    {
        return this.delegate.replace(key, oldValue, newValue);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentMutableHashMap.java`
#### Snippet
```java

    @Override
    public boolean replace(K key, V oldValue, V newValue)
    {
        return this.delegate.replace(key, oldValue, newValue);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentMutableHashMap.java`
#### Snippet
```java

    @Override
    public boolean replace(K key, V oldValue, V newValue)
    {
        return this.delegate.replace(key, oldValue, newValue);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentMutableHashMap.java`
#### Snippet
```java

    @Override
    public V putIfAbsent(K key, V value)
    {
        return this.delegate.putIfAbsent(key, value);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentMutableHashMap.java`
#### Snippet
```java

    @Override
    public boolean remove(Object key, Object value)
    {
        return this.delegate.remove(key, value);
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentMutableHashMap.java`
#### Snippet
```java

    @Override
    public Iterator<V> iterator()
    {
        return this.delegate.values().iterator();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/AbstractMutableMap.java`
#### Snippet
```java

        @Override
        public boolean addAll(Collection<? extends V> collection)
        {
            throw new UnsupportedOperationException("Cannot call addAll() on " + this.getClass().getSimpleName());
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/sorted/immutable/AbstractImmutableSortedMap.java`
#### Snippet
```java

    @Override
    public void putAll(Map<? extends K, ? extends V> map)
    {
        throw new UnsupportedOperationException("Cannot call putAll() on " + this.getClass().getSimpleName());
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/primitive/ObjectBooleanHashMapWithHashingStrategy.java`
#### Snippet
```java

        @Override
        public boolean addAll(Collection<? extends K> collection)
        {
            throw new UnsupportedOperationException("Cannot call addAll() on " + this.getClass().getSimpleName());
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/primitive/ObjectBooleanHashMapWithHashingStrategy.java`
#### Snippet
```java

        @Override
        public Iterator<K> iterator()
        {
            return new KeySetIterator();
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/primitive/ObjectBooleanHashMapWithHashingStrategy.java`
#### Snippet
```java

        @Override
        public <T> T[] toArray(T[] array)
        {
            T[] result = array;
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/primitive/ObjectBooleanHashMapWithHashingStrategy.java`
#### Snippet
```java

        @Override
        public <T> T[] toArray(T[] array)
        {
            T[] result = array;
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/primitive/ObjectBooleanHashMapWithHashingStrategy.java`
#### Snippet
```java

        @Override
        public boolean retainAll(Collection<?> collection)
        {
            int oldSize = ObjectBooleanHashMapWithHashingStrategy.this.size();
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/primitive/ObjectBooleanHashMapWithHashingStrategy.java`
#### Snippet
```java

        @Override
        public Iterator<K> iterator()
        {
            return new InternalKeysViewIterator<>();
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/primitive/ObjectBooleanHashMapWithHashingStrategy.java`
#### Snippet
```java

        @Override
        public Object[] toArray()
        {
            int size = ObjectBooleanHashMapWithHashingStrategy.this.size();
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/primitive/ObjectBooleanHashMapWithHashingStrategy.java`
#### Snippet
```java

        @Override
        public Iterator<ObjectBooleanPair<K>> iterator()
        {
            return new InternalKeyValuesIterator();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/ordered/mutable/OrderedMapAdapter.java`
#### Snippet
```java

    @Override
    public void putAll(Map<? extends K, ? extends V> map)
    {
        this.delegate.putAll(map);
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/primitive/ObjectBooleanHashMap.java`
#### Snippet
```java

        @Override
        public <T> T[] toArray(T[] result)
        {
            int size = ObjectBooleanHashMap.this.size();
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/primitive/ObjectBooleanHashMap.java`
#### Snippet
```java

        @Override
        public Iterator<ObjectBooleanPair<K>> iterator()
        {
            return new InternalKeyValuesIterator();
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/primitive/ObjectBooleanHashMap.java`
#### Snippet
```java

        @Override
        public Iterator<K> iterator()
        {
            return new InternalKeysViewIterator<>();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/primitive/ObjectBooleanHashMap.java`
#### Snippet
```java

        @Override
        public boolean retainAll(Collection<?> collection)
        {
            int oldSize = ObjectBooleanHashMap.this.size();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/primitive/ObjectBooleanHashMap.java`
#### Snippet
```java

        @Override
        public boolean addAll(Collection<? extends K> collection)
        {
            throw new UnsupportedOperationException("Cannot call addAll() on " + this.getClass().getSimpleName());
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/primitive/ObjectBooleanHashMap.java`
#### Snippet
```java

        @Override
        public Object[] toArray()
        {
            int size = ObjectBooleanHashMap.this.size();
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/primitive/ObjectBooleanHashMap.java`
#### Snippet
```java

        @Override
        public Iterator<K> iterator()
        {
            return new KeySetIterator();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/ordered/mutable/UnmodifiableMutableOrderedMap.java`
#### Snippet
```java

    @Override
    public void putAll(Map<? extends K, ? extends V> t)
    {
        throw new UnsupportedOperationException("Cannot call putAll() on " + this.getClass().getSimpleName());
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java

    @Override
    public void putAll(Map<? extends K, ? extends V> map)
    {
        if (map instanceof UnifiedMap<?, ?>)
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java

        @Override
        public boolean retainAll(Collection<?> collection)
        {
            boolean modified = false;
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java

        @Override
        public boolean addAll(Collection<? extends Entry<K, V>> collection)
        {
            throw new UnsupportedOperationException("Cannot call addAll() on " + this.getClass().getSimpleName());
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java

        @Override
        public boolean addAll(Collection<? extends K> collection)
        {
            throw new UnsupportedOperationException("Cannot call addAll() on " + this.getClass().getSimpleName());
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java

        @Override
        public boolean containsAll(Collection<?> collection)
        {
            // todo: this is N^2. if c is large, we should copy the values to a set.
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java

        @Override
        public boolean removeAll(Collection<?> col)
        {
            Objects.requireNonNull(col);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java

    @Override
    public V putIfAbsent(K key, V value)
    {
        int hash = this.hash(key);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java

    @Override
    public boolean replace(K key, V oldValue, V newValue)
    {
        int hash = this.hash(key);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java

    @Override
    public boolean replace(K key, V oldValue, V newValue)
    {
        int hash = this.hash(key);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java

    @Override
    public boolean replace(K key, V oldValue, V newValue)
    {
        int hash = this.hash(key);
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java

    @Override
    public Iterator<V> iterator()
    {
        return this.values().iterator();
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java

    @Override
    public Set<K> keySet()
    {
        return new KeySet();
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java

    @Override
    public Collection<V> values()
    {
        return new Values();
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
    {
        @Override
        public Iterator<Map.Entry<K, V>> iterator()
        {
            return new EntryIterator();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java

    @Override
    public V replace(K key, V value)
    {
        int hash = this.hash(key);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java

    @Override
    public V replace(K key, V value)
    {
        int hash = this.hash(key);
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java

    @Override
    public Set<Map.Entry<K, V>> entrySet()
    {
        return new EntrySet();
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
    {
        @Override
        public Iterator<K> iterator()
        {
            return new KeyIterator();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java

    @Override
    public void putAll(Map<? extends K, ? extends V> map)
    {
        MapIterate.forEachKeyValue(map, this::put);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java

    @Override
    public boolean remove(Object key, Object value)
    {
        int hash = this.hash(key);
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
    {
        @Override
        public Iterator<V> iterator()
        {
            return new ValueIterator();
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
    {
        @Override
        public Iterator<Map.Entry<K, V>> iterator()
        {
            return new EntryIterator();
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
    {
        @Override
        public Iterator<K> iterator()
        {
            return new KeyIterator();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java

    @Override
    public boolean replace(K key, V oldValue, V newValue)
    {
        int hash = this.hash(key);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java

    @Override
    public boolean replace(K key, V oldValue, V newValue)
    {
        int hash = this.hash(key);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java

    @Override
    public boolean replace(K key, V oldValue, V newValue)
    {
        int hash = this.hash(key);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java

    @Override
    public V replace(K key, V value)
    {
        int hash = this.hash(key);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java

    @Override
    public V replace(K key, V value)
    {
        int hash = this.hash(key);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java

        @Override
        public boolean removeAll(Collection<?> col)
        {
            Objects.requireNonNull(col);
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
    {
        @Override
        public Iterator<V> iterator()
        {
            return new ValueIterator();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java

    @Override
    public boolean remove(Object key, Object value)
    {
        int hash = this.hash(key);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java

    @Override
    public V putIfAbsent(K key, V value)
    {
        int hash = this.hash(key);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java

    @Override
    public void putAll(Map<? extends K, ? extends V> map)
    {
        MapIterate.forEachKeyValue(map, this::put);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/immutable/AbstractImmutableMap.java`
#### Snippet
```java

    @Override
    public void putAll(Map<? extends K, ? extends V> map)
    {
        throw new UnsupportedOperationException("Cannot call putAll() on " + this.getClass().getSimpleName());
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/fixed/TripletonSet.java`
#### Snippet
```java

    @Override
    public Iterator<T> iterator()
    {
        return new TripletonSetIterator();
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/fixed/QuadrupletonSet.java`
#### Snippet
```java

    @Override
    public Iterator<T> iterator()
    {
        return new QuadrupletonSetIterator();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/fixed/AbstractMemoryEfficientMutableSet.java`
#### Snippet
```java

    @Override
    public boolean addAll(Collection<? extends T> collection)
    {
        throw new UnsupportedOperationException("Cannot add to a fixed size set: " + this.getClass());
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/fixed/AbstractMemoryEfficientMutableSet.java`
#### Snippet
```java

    @Override
    public boolean retainAll(Collection<?> collection)
    {
        throw new UnsupportedOperationException("Cannot remove from a fixed size set: " + this.getClass());
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/fixed/AbstractMemoryEfficientMutableSet.java`
#### Snippet
```java

    @Override
    public boolean removeAll(Collection<?> collection)
    {
        throw new UnsupportedOperationException("Cannot remove from a fixed size set: " + this.getClass());
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/fixed/DoubletonSet.java`
#### Snippet
```java

    @Override
    public Iterator<T> iterator()
    {
        return new DoubletonSetIterator();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/AbstractUnifiedSet.java`
#### Snippet
```java

    @Override
    public boolean retainAll(Collection<?> collection)
    {
        return this.retainAllIterable(collection);
```

### NullableProblems
Non-annotated method 'toArray' from 'AbstractRichIterable' implements non-null method from 'Set'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/AbstractUnifiedSet.java`
#### Snippet
```java
public abstract class AbstractUnifiedSet<T>
        extends AbstractMutableCollection<T>
        implements MutableSet<T>, Pool<T>, BatchIterable<T>
{
    protected static final float DEFAULT_LOAD_FACTOR = 0.75f;
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java

        @Override
        public boolean containsAll(Collection<?> collection)
        {
            // todo: this is N^2. if c is large, we should copy the values to a set.
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java

        @Override
        public boolean addAll(Collection<? extends K> collection)
        {
            throw new UnsupportedOperationException("Cannot call addAll() on " + this.getClass().getSimpleName());
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java

    @Override
    public void putAll(Map<? extends K, ? extends V> map)
    {
        if (map instanceof UnifiedMapWithHashingStrategy<?, ?>)
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java

        @Override
        public boolean addAll(Collection<? extends Entry<K, V>> collection)
        {
            throw new UnsupportedOperationException("Cannot call addAll() on " + this.getClass().getSimpleName());
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java

        @Override
        public boolean retainAll(Collection<?> collection)
        {
            boolean modified = false;
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/mutable/SynchronizedSortedSet.java`
#### Snippet
```java

    @Override
    public int compareTo(SortedSetIterable<T> o)
    {
        synchronized (this.getLock())
```

### NullableProblems
Non-annotated method 'toArray' from 'AbstractSynchronizedRichIterable' implements non-null method from 'Set'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/mutable/SynchronizedSortedSet.java`
#### Snippet
```java
public class SynchronizedSortedSet<T>
        extends AbstractSynchronizedMutableCollection<T>
        implements MutableSortedSet<T>, Serializable
{
    private static final long serialVersionUID = 2L;
```

### NullableProblems
Non-annotated method 'toArray' from 'AbstractRichIterable' implements non-null method from 'Set'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/mutable/TreeSortedSet.java`
#### Snippet
```java

public class TreeSortedSet<T> extends AbstractMutableCollection<T>
        implements Externalizable, MutableSortedSet<T>
{
    private static final long serialVersionUID = 1L;
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/mutable/TreeSortedSet.java`
#### Snippet
```java

    @Override
    public int compareTo(SortedSetIterable<T> otherSet)
    {
        return SortedSetIterables.compare(this, otherSet);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/mutable/TreeSortedSet.java`
#### Snippet
```java

    @Override
    public boolean containsAll(Collection<?> collection)
    {
        return Iterate.allSatisfyWith(collection, Predicates2.in(), this.treeSet);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/mutable/TreeSortedSet.java`
#### Snippet
```java

    @Override
    public boolean addAll(Collection<? extends T> collection)
    {
        return this.treeSet.addAll(collection);
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/immutable/ImmutableTreeSet.java`
#### Snippet
```java

    @Override
    public Iterator<T> iterator()
    {
        return FastList.newListWith(this.delegate).asUnmodifiable().iterator();
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/immutable/ImmutableTreeSet.java`
#### Snippet
```java

            @Override
            public Iterator<RootSortedSetBatch<T>> iterator()
            {
                return new SortedSetIterableParallelBatchIterator();
```

### NullableProblems
Non-annotated method 'toArray' from 'AbstractRichIterable' implements non-null method from 'Set'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/immutable/AbstractImmutableSortedSet.java`
#### Snippet
```java
 */
abstract class AbstractImmutableSortedSet<T> extends AbstractImmutableCollection<T>
        implements ImmutableSortedSet<T>, SortedSet<T>
{
    @Override
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/mutable/UnmodifiableSortedSet.java`
#### Snippet
```java

    @Override
    public int compareTo(SortedSetIterable<T> o)
    {
        return this.getSortedSet().compareTo(o);
```

### NullableProblems
Non-annotated parameter 'a' in method 'toArray' from 'AbstractUnmodifiableMutableCollection' should not override non-null parameter from 'Set'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/mutable/UnmodifiableSortedSet.java`
#### Snippet
```java
public class UnmodifiableSortedSet<T>
        extends AbstractUnmodifiableMutableCollection<T>
        implements MutableSortedSet<T>, Serializable
{
    UnmodifiableSortedSet(MutableSortedSet<? extends T> sortedSet)
```

### NullableProblems
Non-annotated method 'toArray' from 'AbstractRichIterable' implements non-null method from 'Set'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/AbstractMutableSet.java`
#### Snippet
```java
public abstract class AbstractMutableSet<T>
        extends AbstractMutableCollection<T>
        implements MutableSet<T>
{
    @Override
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/SetAdapter.java`
#### Snippet
```java

    @Override
    public boolean containsAll(Collection<?> collection)
    {
        return this.delegate.containsAll(collection);
```

### NullableProblems
Non-annotated parameter 'a' in method 'toArray' from 'AbstractCollectionAdapter' should not override non-null parameter from 'Set'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/SetAdapter.java`
#### Snippet
```java
public final class SetAdapter<T>
        extends AbstractCollectionAdapter<T>
        implements Serializable, MutableSet<T>
{
    private static final long serialVersionUID = 1L;
```

### NullableProblems
Non-annotated method 'toArray' from 'AbstractSynchronizedRichIterable' implements non-null method from 'Set'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/SynchronizedMutableSet.java`
#### Snippet
```java
public class SynchronizedMutableSet<T>
        extends AbstractSynchronizedMutableCollection<T>
        implements MutableSet<T>, Serializable
{
    private static final long serialVersionUID = 2L;
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/mutable/SortedSetAdapter.java`
#### Snippet
```java

    @Override
    public MutableSortedSet<T> subSet(T fromElement, T toElement)
    {
        return SortedSetAdapter.adapt(this.delegate.subSet(fromElement, toElement));
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/mutable/SortedSetAdapter.java`
#### Snippet
```java

    @Override
    public int compareTo(SortedSetIterable<T> o)
    {
        return SortedSetIterables.compare(this, o);
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/mutable/SortedSetAdapter.java`
#### Snippet
```java

    @Override
    public MutableSortedSet<T> tailSet(T fromElement)
    {
        return SortedSetAdapter.adapt(this.delegate.tailSet(fromElement));
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/mutable/SortedSetAdapter.java`
#### Snippet
```java

    @Override
    public boolean containsAll(Collection<?> collection)
    {
        return this.delegate.containsAll(collection);
```

### NullableProblems
Non-annotated parameter 'a' in method 'toArray' from 'AbstractCollectionAdapter' should not override non-null parameter from 'Set'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/mutable/SortedSetAdapter.java`
#### Snippet
```java
public final class SortedSetAdapter<T>
        extends AbstractCollectionAdapter<T>
        implements Serializable, MutableSortedSet<T>
{
    private static final long serialVersionUID = 1L;
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/mutable/SortedSetAdapter.java`
#### Snippet
```java

    @Override
    public MutableSortedSet<T> headSet(T toElement)
    {
        return SortedSetAdapter.adapt(this.delegate.headSet(toElement));
```

### NullableProblems
Non-annotated parameter 'a' in method 'toArray' from 'AbstractUnmodifiableMutableCollection' should not override non-null parameter from 'Set'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnmodifiableMutableSet.java`
#### Snippet
```java
public class UnmodifiableMutableSet<T>
        extends AbstractUnmodifiableMutableCollection<T>
        implements MutableSet<T>, Serializable
{
    UnmodifiableMutableSet(MutableSet<? extends T> mutableSet)
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/strategy/immutable/ImmutableUnifiedSetWithHashingStrategy.java`
#### Snippet
```java

    @Override
    public Iterator<T> iterator()
    {
        return new UnmodifiableIteratorAdapter<>(this.delegate.iterator());
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/immutable/ImmutableUnifiedSet.java`
#### Snippet
```java

    @Override
    public Iterator<T> iterator()
    {
        return new UnmodifiableIteratorAdapter<>(this.delegate.iterator());
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java

            @Override
            public Iterator<RootUnsortedSetBatch<T>> iterator()
            {
                return new UnifiedSetParallelSplitIterator();
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/immutable/ImmutableDoubletonSet.java`
#### Snippet
```java

    @Override
    public Iterator<T> iterator()
    {
        return new DoubletonSetIterator();
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/immutable/ImmutableQuadrupletonSet.java`
#### Snippet
```java

    @Override
    public Iterator<T> iterator()
    {
        return new QuadrupletonSetIterator();
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/immutable/ImmutableSingletonSet.java`
#### Snippet
```java

    @Override
    public Iterator<T> iterator()
    {
        return new SingletonSetIterator();
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/immutable/ImmutableTripletonSet.java`
#### Snippet
```java

    @Override
    public Iterator<T> iterator()
    {
        return new TripletonSetIterator();
```

### NullableProblems
Non-annotated parameter 'a' in method 'toArray' from 'UntouchableMutableCollection' should not override non-null parameter from 'Set'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/MultiReaderUnifiedSet.java`
#### Snippet
```java
    static final class UntouchableMutableSet<T>
            extends UntouchableMutableCollection<T>
            implements MutableSet<T>
    {
        private final MutableList<UntouchableIterator<T>> requestedIterators = mList();
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/MultiReaderUnifiedSet.java`
#### Snippet
```java

        @Override
        public Iterator<T> iterator()
        {
            UntouchableIterator<T> iterator = new UntouchableIterator<>(this.delegate.iterator());
```

### NullableProblems
Non-annotated parameter 'a' in method 'toArray' from 'AbstractMultiReaderMutableCollection' should not override non-null parameter from 'Set'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/MultiReaderUnifiedSet.java`
#### Snippet
```java
public final class MultiReaderUnifiedSet<T>
        extends AbstractMultiReaderMutableCollection<T>
        implements RandomAccess, Externalizable, MultiReaderSet<T>
{
    private static final long serialVersionUID = 1L;
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/lazy/CompositeIterable.java`
#### Snippet
```java

    @Override
    public Iterator<E> iterator()
    {
        return new CompositeIterator(this.iterables);
```

### NullableProblems
Non-annotated method 'toArray' from 'AbstractRichIterable' implements non-null method from 'Set'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/immutable/AbstractImmutableSet.java`
#### Snippet
```java
 */
public abstract class AbstractImmutableSet<T> extends AbstractImmutableCollection<T>
        implements ImmutableSet<T>, Set<T>
{
    @Override
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/strategy/mutable/UnifiedSetWithHashingStrategy.java`
#### Snippet
```java

            @Override
            public Iterator<RootUnsortedSetBatch<T>> iterator()
            {
                return new UnifiedSetParallelSplitIterator();
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/lazy/parallel/list/ListIterableParallelIterable.java`
#### Snippet
```java

        @Override
        public Iterator<RootListBatch<T>> iterator()
        {
            return new ListIterableParallelBatchIterator();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/AbstractMemoryEfficientMutableList.java`
#### Snippet
```java

    @Override
    public boolean retainAll(Collection<?> collection)
    {
        throw new UnsupportedOperationException("Cannot remove from a fixed size list: " + this.getClass());
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/AbstractMemoryEfficientMutableList.java`
#### Snippet
```java

    @Override
    public boolean removeAll(Collection<?> collection)
    {
        throw new UnsupportedOperationException("Cannot remove from a fixed size list: " + this.getClass());
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/AbstractMemoryEfficientMutableList.java`
#### Snippet
```java

    @Override
    public boolean addAll(int index, Collection<? extends T> collection)
    {
        throw new UnsupportedOperationException("Cannot add to a fixed size list: " + this.getClass());
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/AbstractMemoryEfficientMutableList.java`
#### Snippet
```java

    @Override
    public boolean addAll(Collection<? extends T> collection)
    {
        throw new UnsupportedOperationException("Cannot add to a fixed size list: " + this.getClass());
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/AbstractListAdapter.java`
#### Snippet
```java

    @Override
    public boolean addAll(int index, Collection<? extends T> collection)
    {
        return this.getDelegate().addAll(index, collection);
```

### NullableProblems
Non-annotated parameter 'a' in method 'toArray' from 'AbstractCollectionAdapter' should not override non-null parameter from 'List'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/AbstractListAdapter.java`
#### Snippet
```java
public abstract class AbstractListAdapter<T>
        extends AbstractCollectionAdapter<T>
        implements MutableList<T>
{
    @Override
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/Interval.java`
#### Snippet
```java

    @Override
    public Integer[] toArray()
    {
        Integer[] result = new Integer[this.size()];
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/Interval.java`
#### Snippet
```java
    @Override
    @SuppressWarnings("TypeParameterExtendsFinalClass")
    public boolean addAll(Collection<? extends Integer> collection)
    {
        throw new UnsupportedOperationException("Cannot call addAll() on " + this.getClass().getSimpleName());
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/Interval.java`
#### Snippet
```java

    @Override
    public ListIterator<Integer> listIterator()
    {
        return new MutableListIterator<>(this, 0);
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/Interval.java`
#### Snippet
```java

    @Override
    public Iterator<Integer> iterator()
    {
        return new IntegerIterator();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/Interval.java`
#### Snippet
```java

    @Override
    public boolean removeAll(Collection<?> collection)
    {
        throw new UnsupportedOperationException("Cannot call removeAll() on " + this.getClass().getSimpleName());
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/Interval.java`
#### Snippet
```java

    @Override
    public Interval subList(int fromIndex, int toIndex)
    {
        return Interval.fromToBy(this.get(fromIndex), this.get(toIndex - 1), this.step);
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/Interval.java`
#### Snippet
```java

    @Override
    public ListIterator<Integer> listIterator(int index)
    {
        return new MutableListIterator<>(this, index);
```

### NullableProblems
Non-annotated method 'toArray' from 'AbstractLazyIterable' implements non-null method from 'List'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/Interval.java`
#### Snippet
```java
public final class Interval
        extends AbstractLazyIterable<Integer>
        implements List<Integer>, Serializable, RandomAccess
{
    private static final long serialVersionUID = 1L;
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/Interval.java`
#### Snippet
```java

    @Override
    public boolean retainAll(Collection<?> collection)
    {
        throw new UnsupportedOperationException("Cannot call retainAll() on " + this.getClass().getSimpleName());
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/Interval.java`
#### Snippet
```java
    @Override
    @SuppressWarnings("TypeParameterExtendsFinalClass")
    public boolean addAll(int index, Collection<? extends Integer> collection)
    {
        throw new UnsupportedOperationException("Cannot call addAll() on " + this.getClass().getSimpleName());
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/AbstractArrayAdapter.java`
#### Snippet
```java

    @Override
    public boolean addAll(Collection<? extends T> collection)
    {
        throw new UnsupportedOperationException("Cannot call addAll() on " + this.getClass().getSimpleName());
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/fixed/AbstractArrayAdapter.java`
#### Snippet
```java

    @Override
    public boolean addAll(int index, Collection<? extends T> collection)
    {
        throw new UnsupportedOperationException("Cannot call addAll() on " + this.getClass().getSimpleName());
```

### NullableProblems
Non-annotated method 'toArray' from 'AbstractSynchronizedRichIterable' implements non-null method from 'List'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/SynchronizedMutableList.java`
#### Snippet
```java
public class SynchronizedMutableList<T>
        extends AbstractSynchronizedMutableCollection<T>
        implements MutableList<T>, Serializable
{
    private static final long serialVersionUID = 2L;
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/SynchronizedMutableList.java`
#### Snippet
```java

    @Override
    public boolean addAll(int index, Collection<? extends T> collection)
    {
        synchronized (this.getLock())
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/UnmodifiableMutableList.java`
#### Snippet
```java

    @Override
    public boolean addAll(int index, Collection<? extends T> collection)
    {
        throw new UnsupportedOperationException("Cannot call addAll() on " + this.getClass().getSimpleName());
```

### NullableProblems
Non-annotated parameter 'a' in method 'toArray' from 'AbstractUnmodifiableMutableCollection' should not override non-null parameter from 'List'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/UnmodifiableMutableList.java`
#### Snippet
```java
public class UnmodifiableMutableList<T>
        extends AbstractUnmodifiableMutableCollection<T>
        implements MutableList<T>, Serializable
{
    UnmodifiableMutableList(MutableList<? extends T> mutableList)
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/CompositeFastList.java`
#### Snippet
```java

    @Override
    public Iterator<E> iterator()
    {
        if (this.lists.isEmpty())
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/CompositeFastList.java`
#### Snippet
```java

    @Override
    public boolean addAll(int index, Collection<? extends E> collection)
    {
        throw new UnsupportedOperationException(this.getClass().getSimpleName() + ".addAll(index, collection) not implemented yet");
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/CompositeFastList.java`
#### Snippet
```java
     */
    @Override
    public ListIterator<E> listIterator()
    {
        return this.listIterator(0);
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/CompositeFastList.java`
#### Snippet
```java

    @Override
    public Object[] toArray(Object[] array)
    {
        int size = this.size();
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/CompositeFastList.java`
#### Snippet
```java
     */
    @Override
    public ListIterator<E> listIterator(int index)
    {
        if (this.lists.size() > 1 || this.lists.isEmpty())
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/CompositeFastList.java`
#### Snippet
```java

    @Override
    public Object[] toArray()
    {
        Object[] result = new Object[this.size()];
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/MultiReaderFastList.java`
#### Snippet
```java

    @Override
    public boolean addAll(int index, Collection<? extends T> collection)
    {
        try (LockWrapper wrapper = this.lockWrapper.acquireWriteLock())
```

### NullableProblems
Non-annotated parameter 'a' in method 'toArray' from 'UntouchableMutableCollection' should not override non-null parameter from 'List'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/MultiReaderFastList.java`
#### Snippet
```java
    static final class UntouchableMutableList<T>
            extends UntouchableMutableCollection<T>
            implements MutableList<T>
    {
        private final MutableList<UntouchableListIterator<T>> requestedIterators = mList();
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/MultiReaderFastList.java`
#### Snippet
```java

        @Override
        public MutableList<T> subList(int fromIndex, int toIndex)
        {
            UntouchableMutableList<T> subList = new UntouchableMutableList<>(
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/MultiReaderFastList.java`
#### Snippet
```java

        @Override
        public Iterator<T> iterator()
        {
            UntouchableListIterator<T> iterator = new UntouchableListIterator<>(this.delegate.iterator());
```

### NullableProblems
Non-annotated parameter 'a' in method 'toArray' from 'AbstractMultiReaderMutableCollection' should not override non-null parameter from 'List'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/MultiReaderFastList.java`
#### Snippet
```java
public final class MultiReaderFastList<T>
        extends AbstractMultiReaderMutableCollection<T>
        implements RandomAccess, Externalizable, MultiReaderList<T>
{
    private static final long serialVersionUID = 1L;
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/MultiReaderFastList.java`
#### Snippet
```java

    @Override
    public MultiReaderList<T> subList(int fromIndex, int toIndex)
    {
        try (LockWrapper wrapper = this.lockWrapper.acquireReadLock())
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/MultiReaderFastList.java`
#### Snippet
```java

        @Override
        public boolean addAll(int index, Collection<? extends T> collection)
        {
            return this.getDelegate().addAll(index, collection);
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/MultiReaderFastList.java`
#### Snippet
```java

        @Override
        public ListIterator<T> listIterator(int index)
        {
            UntouchableListIterator<T> iterator = new UntouchableListIterator<>(this.getDelegate().listIterator(index));
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/MultiReaderFastList.java`
#### Snippet
```java

        @Override
        public ListIterator<T> listIterator()
        {
            UntouchableListIterator<T> iterator = new UntouchableListIterator<>(this.getDelegate().listIterator());
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/immutable/ImmutableArrayList.java`
#### Snippet
```java

    @Override
    public boolean containsAll(Collection<?> collection)
    {
        return Iterate.allSatisfy(collection, Predicates.in(this.items));
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/immutable/ImmutableArrayList.java`
#### Snippet
```java

    @Override
    public <E> E[] toArray(E[] a)
    {
        int size = this.size();
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/immutable/ImmutableArrayList.java`
#### Snippet
```java

    @Override
    public Iterator<T> iterator()
    {
        return Arrays.asList(this.items).iterator();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/AbstractMutableList.java`
#### Snippet
```java

        @Override
        public boolean addAll(Collection<? extends T> collection)
        {
            return this.addAll(this.size, collection);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/AbstractMutableList.java`
#### Snippet
```java

        @Override
        public boolean addAll(int index, Collection<? extends T> collection)
        {
            if (index < 0 || index > this.size)
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/AbstractMutableList.java`
#### Snippet
```java

    @Override
    public boolean retainAll(Collection<?> collection)
    {
        int currentSize = this.size();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/AbstractMutableList.java`
#### Snippet
```java

    @Override
    public boolean removeAll(Collection<?> collection)
    {
        int currentSize = this.size();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/AbstractMutableList.java`
#### Snippet
```java

    @Override
    public boolean containsAll(Collection<?> source)
    {
        return Iterate.allSatisfyWith(source, Predicates2.in(), this);
```

### NullableProblems
Non-annotated method 'toArray' from 'AbstractRichIterable' implements non-null method from 'List'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/AbstractMutableList.java`
#### Snippet
```java
public abstract class AbstractMutableList<T>
        extends AbstractMutableCollection<T>
        implements MutableList<T>
{
    private static final IntObjectToIntFunction<?> HASH_CODE_FUNCTION = (hashCode, item) -> 31 * hashCode + (item == null ? 0 : item.hashCode());
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/FastList.java`
#### Snippet
```java

    @Override
    public boolean addAll(int index, Collection<? extends T> source)
    {
        if (index > this.size || index < 0)
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/immutable/AbstractImmutableList.java`
#### Snippet
```java

    @Override
    public boolean addAll(int index, Collection<? extends T> collection)
    {
        throw new UnsupportedOperationException("Cannot call addAll() on " + this.getClass().getSimpleName());
```

### NullableProblems
Non-annotated method 'toArray' from 'AbstractRichIterable' implements non-null method from 'List'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/immutable/AbstractImmutableList.java`
#### Snippet
```java
abstract class AbstractImmutableList<T>
        extends AbstractImmutableCollection<T>
        implements ImmutableList<T>, List<T>
{
    @Override
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bimap/mutable/UnmodifiableBiMap.java`
#### Snippet
```java

    @Override
    public void putAll(Map<? extends K, ? extends V> m)
    {
        throw new UnsupportedOperationException("Cannot call putAll() on " + this.getClass().getSimpleName());
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bimap/immutable/AbstractImmutableBiMap.java`
#### Snippet
```java

    @Override
    public void putAll(Map<? extends K, ? extends V> map)
    {
        throw new UnsupportedOperationException("Cannot call putAll() on " + this.getClass().getSimpleName());
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bimap/mutable/AbstractMutableBiMap.java`
#### Snippet
```java

        @Override
        public boolean addAll(Collection<? extends Map.Entry<K, V>> collection)
        {
            throw new UnsupportedOperationException("Cannot call addAll() on " + this.getClass().getSimpleName());
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bimap/mutable/AbstractMutableBiMap.java`
#### Snippet
```java

        @Override
        public boolean retainAll(Collection<?> collection)
        {
            int oldSize = AbstractMutableBiMap.this.size();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bimap/mutable/AbstractMutableBiMap.java`
#### Snippet
```java

        @Override
        public boolean retainAll(Collection<?> collection)
        {
            int oldSize = AbstractMutableBiMap.this.size();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bimap/mutable/AbstractMutableBiMap.java`
#### Snippet
```java

        @Override
        public <T> T[] toArray(T[] a)
        {
            return AbstractMutableBiMap.this.delegate.values().toArray(a);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bimap/mutable/AbstractMutableBiMap.java`
#### Snippet
```java

        @Override
        public boolean addAll(Collection<? extends K> source)
        {
            throw new UnsupportedOperationException("Cannot call addAll() on " + this.getClass().getSimpleName());
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bimap/mutable/AbstractMutableBiMap.java`
#### Snippet
```java

        @Override
        public boolean retainAll(Collection<?> collection)
        {
            int oldSize = AbstractMutableBiMap.this.size();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bimap/mutable/AbstractMutableBiMap.java`
#### Snippet
```java

        @Override
        public <T> T[] toArray(T[] a)
        {
            return AbstractMutableBiMap.this.delegate.keySet().toArray(a);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bimap/mutable/AbstractMutableBiMap.java`
#### Snippet
```java

        @Override
        public boolean addAll(Collection<? extends V> collection)
        {
            throw new UnsupportedOperationException("Cannot call addAll() on " + this.getClass().getSimpleName());
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/stack/immutable/ImmutableNotEmptyStack.java`
#### Snippet
```java

    @Override
    public Iterator<T> iterator()
    {
        return new ImmutableStackIterator<>(this);
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/stack/mutable/SynchronizedStack.java`
#### Snippet
```java

    @Override
    public Iterator<T> iterator()
    {
        return new UnmodifiableIteratorAdapter<>(this.delegate.iterator());
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/stack/mutable/UnmodifiableStack.java`
#### Snippet
```java

    @Override
    public Iterator<T> iterator()
    {
        return new UnmodifiableIteratorAdapter<>(this.mutableStack.iterator());
```

### NullableProblems
Not annotated method overrides method annotated with @NotNull
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/stack/immutable/ImmutableArrayStack.java`
#### Snippet
```java

    @Override
    public Iterator<T> iterator()
    {
        return this.delegate.asReversed().iterator();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/parallel/CollectionsThreadFactory.java`
#### Snippet
```java

    @Override
    public Thread newThread(Runnable r)
    {
        Thread t = new Thread(this.group, r, this.namePrefix + this.threadNumber.getAndIncrement(), 0);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/immutable/AbstractImmutableCollection.java`
#### Snippet
```java

    @Override
    public boolean removeAll(Collection<?> collection)
    {
        throw new UnsupportedOperationException("Cannot call removeAll() on " + this.getClass().getSimpleName());
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/immutable/AbstractImmutableCollection.java`
#### Snippet
```java

    @Override
    public boolean retainAll(Collection<?> collection)
    {
        throw new UnsupportedOperationException("Cannot call retainAll() on " + this.getClass().getSimpleName());
```

### NullableProblems
Non-annotated method 'toArray' from 'AbstractRichIterable' implements non-null method from 'Collection'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/immutable/AbstractImmutableCollection.java`
#### Snippet
```java
public abstract class AbstractImmutableCollection<T>
        extends AbstractRichIterable<T>
        implements ImmutableCollection<T>, Collection<T>
{
    protected abstract MutableCollection<T> newMutable(int size);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/immutable/AbstractImmutableCollection.java`
#### Snippet
```java

    @Override
    public boolean addAll(Collection<? extends T> collection)
    {
        throw new UnsupportedOperationException("Cannot call addAll() on " + this.getClass().getSimpleName());
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractMutableCollection.java`
#### Snippet
```java

    @Override
    public boolean removeAll(Collection<?> source)
    {
        return this.removeAllIterable(source);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractMutableCollection.java`
#### Snippet
```java

    @Override
    public boolean addAll(Collection<? extends T> source)
    {
        return this.addAllIterable(source);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractMutableCollection.java`
#### Snippet
```java

    @Override
    public boolean retainAll(Collection<?> source)
    {
        int oldSize = this.size();
```

### NullableProblems
Non-annotated method 'toArray' from 'AbstractRichIterable' implements non-null method from 'Collection'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractMutableCollection.java`
#### Snippet
```java
public abstract class AbstractMutableCollection<T>
        extends AbstractRichIterable<T>
        implements MutableCollection<T>
{
    @Override
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractSynchronizedMutableCollection.java`
#### Snippet
```java

    @Override
    public boolean addAll(Collection<? extends T> coll)
    {
        synchronized (this.getLock())
```

### NullableProblems
Non-annotated method 'toArray' from 'AbstractSynchronizedRichIterable' implements non-null method from 'Collection'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractSynchronizedMutableCollection.java`
#### Snippet
```java

public abstract class AbstractSynchronizedMutableCollection<T>
        extends AbstractSynchronizedRichIterable<T> implements MutableCollection<T>
{
    protected AbstractSynchronizedMutableCollection(MutableCollection<T> delegate)
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractSynchronizedMutableCollection.java`
#### Snippet
```java

    @Override
    public boolean removeAll(Collection<?> coll)
    {
        synchronized (this.getLock())
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractSynchronizedMutableCollection.java`
#### Snippet
```java

    @Override
    public boolean retainAll(Collection<?> coll)
    {
        synchronized (this.getLock())
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractUnmodifiableMutableCollection.java`
#### Snippet
```java

    @Override
    public <S> S[] toArray(S[] a)
    {
        return this.collection.toArray(a);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractUnmodifiableMutableCollection.java`
#### Snippet
```java

    @Override
    public boolean removeAll(Collection<?> c)
    {
        throw new UnsupportedOperationException("Cannot call removeAll() on " + this.getClass().getSimpleName());
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractUnmodifiableMutableCollection.java`
#### Snippet
```java

    @Override
    public boolean retainAll(Collection<?> c)
    {
        throw new UnsupportedOperationException("Cannot call retainAll() on " + this.getClass().getSimpleName());
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractUnmodifiableMutableCollection.java`
#### Snippet
```java

    @Override
    public boolean containsAll(Collection<?> c)
    {
        return this.collection.containsAll(c);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractUnmodifiableMutableCollection.java`
#### Snippet
```java

    @Override
    public boolean addAll(Collection<? extends T> c)
    {
        throw new UnsupportedOperationException("Cannot call addAll() on " + this.getClass().getSimpleName());
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractCollectionAdapter.java`
#### Snippet
```java

    @Override
    public boolean containsAll(Collection<?> collection)
    {
        return this.getDelegate().containsAll(collection);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractCollectionAdapter.java`
#### Snippet
```java

    @Override
    public boolean retainAll(Collection<?> collection)
    {
        int currentSize = this.size();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractCollectionAdapter.java`
#### Snippet
```java

    @Override
    public boolean removeAll(Collection<?> collection)
    {
        int currentSize = this.size();
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractCollectionAdapter.java`
#### Snippet
```java

    @Override
    public <E> E[] toArray(E[] a)
    {
        return this.getDelegate().toArray(a);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractMultiReaderMutableCollection.java`
#### Snippet
```java

        @Override
        public boolean addAll(Collection<? extends T> collection)
        {
            return this.delegate.addAll(collection);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractMultiReaderMutableCollection.java`
#### Snippet
```java

        @Override
        public <E> E[] toArray(E[] a)
        {
            return this.delegate.toArray(a);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractMultiReaderMutableCollection.java`
#### Snippet
```java

        @Override
        public boolean removeAll(Collection<?> collection)
        {
            return this.delegate.removeAll(collection);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractMultiReaderMutableCollection.java`
#### Snippet
```java

    @Override
    public boolean retainAll(Collection<?> collection)
    {
        try (LockWrapper wrapper = this.lockWrapper.acquireWriteLock())
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractMultiReaderMutableCollection.java`
#### Snippet
```java

    @Override
    public <E> E[] toArray(E[] a)
    {
        try (LockWrapper wrapper = this.lockWrapper.acquireReadLock())
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractMultiReaderMutableCollection.java`
#### Snippet
```java

    @Override
    public boolean removeAll(Collection<?> collection)
    {
        try (LockWrapper wrapper = this.lockWrapper.acquireWriteLock())
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractMultiReaderMutableCollection.java`
#### Snippet
```java

    @Override
    public boolean containsAll(Collection<?> collection)
    {
        try (LockWrapper wrapper = this.lockWrapper.acquireReadLock())
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractMultiReaderMutableCollection.java`
#### Snippet
```java

        @Override
        public boolean retainAll(Collection<?> collection)
        {
            return this.delegate.retainAll(collection);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractMultiReaderMutableCollection.java`
#### Snippet
```java

        @Override
        public boolean containsAll(Collection<?> collection)
        {
            return this.delegate.containsAll(collection);
```

### NullableProblems
Not annotated parameter overrides @NotNull parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractMultiReaderMutableCollection.java`
#### Snippet
```java

    @Override
    public boolean addAll(Collection<? extends T> collection)
    {
        try (LockWrapper wrapper = this.lockWrapper.acquireWriteLock())
```

## RuleId[id=IgnoreResultOfCall]
### IgnoreResultOfCall
Result of `ExecutorService.awaitTermination()` is ignored
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MaxByIntTest.java`
#### Snippet
```java
    {
        this.executorService.shutdownNow();
        this.executorService.awaitTermination(1L, TimeUnit.SECONDS);
    }

```

### IgnoreResultOfCall
Result of `ExecutorService.awaitTermination()` is ignored
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/SumOfDoubleTest.java`
#### Snippet
```java
    {
        this.executorService.shutdownNow();
        this.executorService.awaitTermination(1L, TimeUnit.SECONDS);
    }

```

### IgnoreResultOfCall
Result of `ExecutorService.awaitTermination()` is ignored
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MinTest.java`
#### Snippet
```java
    {
        this.executorService.shutdownNow();
        this.executorService.awaitTermination(1L, TimeUnit.SECONDS);
    }

```

### IgnoreResultOfCall
Result of `ExecutorService.awaitTermination()` is ignored
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/NoneSatisfyTest.java`
#### Snippet
```java
    {
        this.executorService.shutdownNow();
        this.executorService.awaitTermination(1L, TimeUnit.SECONDS);
    }

```

### IgnoreResultOfCall
Result of `ExecutorService.awaitTermination()` is ignored
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/SummarizeIntTest.java`
#### Snippet
```java
    {
        this.executorService.shutdownNow();
        this.executorService.awaitTermination(1L, TimeUnit.SECONDS);
    }

```

### IgnoreResultOfCall
Result of `ExecutorService.awaitTermination()` is ignored
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/CollectTest.java`
#### Snippet
```java
    {
        this.executorService.shutdownNow();
        this.executorService.awaitTermination(1L, TimeUnit.SECONDS);
    }

```

### IgnoreResultOfCall
Result of `ExecutorService.awaitTermination()` is ignored
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AllSatisfyTest.java`
#### Snippet
```java
    {
        this.executorService.shutdownNow();
        this.executorService.awaitTermination(1L, TimeUnit.SECONDS);
    }

```

### IgnoreResultOfCall
Result of `ExecutorService.awaitTermination()` is ignored
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/CollectorJoiningTest.java`
#### Snippet
```java
    {
        this.executorService.shutdownNow();
        this.executorService.awaitTermination(1L, TimeUnit.SECONDS);
    }

```

### IgnoreResultOfCall
Result of `ExecutorService.awaitTermination()` is ignored
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/FunctionalInterfaceSetTest.java`
#### Snippet
```java
    {
        this.executorService.shutdownNow();
        this.executorService.awaitTermination(1L, TimeUnit.SECONDS);
    }

```

### IgnoreResultOfCall
Result of `ExecutorService.awaitTermination()` is ignored
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AggregateByTest.java`
#### Snippet
```java
    {
        this.executorService.shutdownNow();
        this.executorService.awaitTermination(1L, TimeUnit.SECONDS);
    }

```

### IgnoreResultOfCall
Result of `ExecutorService.awaitTermination()` is ignored
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AnySatisfyTest.java`
#### Snippet
```java
    {
        this.executorService.shutdownNow();
        this.executorService.awaitTermination(1L, TimeUnit.SECONDS);
    }

```

### IgnoreResultOfCall
Result of `ExecutorService.awaitTermination()` is ignored
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AnagramSetTest.java`
#### Snippet
```java
    {
        this.executorService.shutdownNow();
        this.executorService.awaitTermination(1L, TimeUnit.SECONDS);
    }

```

### IgnoreResultOfCall
Result of `ExecutorService.awaitTermination()` is ignored
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MinByDoubleTest.java`
#### Snippet
```java
    {
        this.executorService.shutdownNow();
        this.executorService.awaitTermination(1L, TimeUnit.SECONDS);
    }

```

### IgnoreResultOfCall
Result of `ExecutorService.awaitTermination()` is ignored
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AnagramListTest.java`
#### Snippet
```java
    {
        this.executorService.shutdownNow();
        this.executorService.awaitTermination(1L, TimeUnit.SECONDS);
    }

```

### IgnoreResultOfCall
Result of `ExecutorService.awaitTermination()` is ignored
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/SumOfIntTest.java`
#### Snippet
```java
    {
        this.executorService.shutdownNow();
        this.executorService.awaitTermination(1L, TimeUnit.SECONDS);
    }

```

### IgnoreResultOfCall
Result of `ExecutorService.awaitTermination()` is ignored
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/FunctionalInterfaceTest.java`
#### Snippet
```java
    {
        this.executorService.shutdownNow();
        this.executorService.awaitTermination(1L, TimeUnit.SECONDS);
    }

```

### IgnoreResultOfCall
Result of `ExecutorService.awaitTermination()` is ignored
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MinByIntTest.java`
#### Snippet
```java
    {
        this.executorService.shutdownNow();
        this.executorService.awaitTermination(1L, TimeUnit.SECONDS);
    }

```

### IgnoreResultOfCall
Result of `ExecutorService.awaitTermination()` is ignored
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MaxByDoubleTest.java`
#### Snippet
```java
    {
        this.executorService.shutdownNow();
        this.executorService.awaitTermination(1L, TimeUnit.SECONDS);
    }

```

### IgnoreResultOfCall
Result of `ExecutorService.awaitTermination()` is ignored
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MaxTest.java`
#### Snippet
```java
    {
        this.executorService.shutdownNow();
        this.executorService.awaitTermination(1L, TimeUnit.SECONDS);
    }

```

### IgnoreResultOfCall
Result of `ExecutorService.awaitTermination()` is ignored
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/CountSetTest.java`
#### Snippet
```java
    {
        this.executorService.shutdownNow();
        this.executorService.awaitTermination(1L, TimeUnit.SECONDS);
    }

```

### IgnoreResultOfCall
Result of `ExecutorService.awaitTermination()` is ignored
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AnagramBagTest.java`
#### Snippet
```java
    {
        this.executorService.shutdownNow();
        this.executorService.awaitTermination(1L, TimeUnit.SECONDS);
    }

```

### IgnoreResultOfCall
Result of `ExecutorService.awaitTermination()` is ignored
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/GroupBySetTest.java`
#### Snippet
```java
    {
        this.executorService.shutdownNow();
        this.executorService.awaitTermination(1L, TimeUnit.SECONDS);
    }

```

### IgnoreResultOfCall
Result of `ExecutorService.awaitTermination()` is ignored
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/CountTest.java`
#### Snippet
```java
    {
        this.executorService.shutdownNow();
        this.executorService.awaitTermination(1L, TimeUnit.SECONDS);
    }

```

### IgnoreResultOfCall
Result of `ExecutorService.awaitTermination()` is ignored
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/set/sorted/SortedSetIterationTest.java`
#### Snippet
```java
    {
        this.executorService.shutdownNow();
        this.executorService.awaitTermination(1L, TimeUnit.SECONDS);
    }

```

### IgnoreResultOfCall
Result of `ExecutorService.awaitTermination()` is ignored
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/list/ListIterationTest.java`
#### Snippet
```java
    {
        this.executorService.shutdownNow();
        this.executorService.awaitTermination(1L, TimeUnit.SECONDS);
    }

```

## RuleId[id=ListIndexOfReplaceableByContains]
### ListIndexOfReplaceableByContains
`this.indexOf(object) > -1` can be replaced with 'this.contains(object)'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/AbstractMutableList.java`
#### Snippet
```java
    public boolean contains(Object object)
    {
        return this.indexOf(object) > -1;
    }

```

## RuleId[id=FunctionalExpressionCanBeFolded]
### FunctionalExpressionCanBeFolded
Method reference can be replaced with qualifier
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/lazy/parallel/bag/FlatCollectUnsortedBagBatch.java`
#### Snippet
```java
    public void forEach(Procedure<? super V> procedure)
    {
        this.unsortedBagBatch.forEach(each -> Iterate.forEach(this.function.valueOf(each), procedure::value));
    }

```

## RuleId[id=AssertBetweenInconvertibleTypes]
### AssertBetweenInconvertibleTypes
Possibly redundant assertion: incompatible types are compared 'String' and 'boolean'
in `eclipse-collections-testutils/src/main/java/org/eclipse/collections/impl/test/Verify.java`
#### Snippet
```java
            Assert.assertFalse("Neither item should equal null", objectA.equals(null));
            Assert.assertFalse("Neither item should equal null", objectB.equals(null));
            Assert.assertNotEquals("Neither item should equal new Object()", objectA.equals(new Object()));
            Assert.assertNotEquals("Neither item should equal new Object()", objectB.equals(new Object()));
            Assert.assertEquals("Expected " + itemNames + " to be equal.", objectA, objectA);
```

### AssertBetweenInconvertibleTypes
Possibly redundant assertion: incompatible types are compared 'String' and 'boolean'
in `eclipse-collections-testutils/src/main/java/org/eclipse/collections/impl/test/Verify.java`
#### Snippet
```java
            Assert.assertFalse("Neither item should equal null", objectB.equals(null));
            Assert.assertNotEquals("Neither item should equal new Object()", objectA.equals(new Object()));
            Assert.assertNotEquals("Neither item should equal new Object()", objectB.equals(new Object()));
            Assert.assertEquals("Expected " + itemNames + " to be equal.", objectA, objectA);
            Assert.assertEquals("Expected " + itemNames + " to be equal.", objectB, objectB);
```

## RuleId[id=RedundantStreamOptionalCall]
### RedundantStreamOptionalCall
Redundant 'parallel()' call: the stream was created via 'parallelStream()', so it's already parallel
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AnagramSetTest.java`
#### Snippet
```java
                .filter(list -> list.size() >= SIZE_THRESHOLD)
                .sorted(Comparator.<Set<String>>comparingInt(Set::size).reversed())
                .parallel()
                .map(list -> list.size() + ": " + list)
                .forEach(e -> Assert.assertFalse(e.isEmpty()));
```

### RedundantStreamOptionalCall
Redundant 'parallel()' call: the stream was created via 'parallelStream()', so it's already parallel
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AnagramSetTest.java`
#### Snippet
```java
                .filter(list -> list.size() >= SIZE_THRESHOLD)
                .sorted(Comparator.<Set<String>>comparingInt(Set::size).reversed())
                .parallel()
                .map(list -> list.size() + ": " + list)
                .forEach(e -> Assert.assertFalse(e.isEmpty()));
```

## RuleId[id=IntegerMultiplicationImplicitCastToLong]
### IntegerMultiplicationImplicitCastToLong
this.mapSizeDividedBy16000 \* 64: integer multiplication implicitly cast to long
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/LongIntMapTest.java`
#### Snippet
```java
            }
        }
        this.randomIntegersForMap = new Random(0x123456789ABCDL).ints().limit((long) (this.mapSizeDividedBy16000 * 64)).toArray();

        this.longIntMap = new LongIntHashMap();
```

### IntegerMultiplicationImplicitCastToLong
this.mapSizeDividedBy64 \* 64: integer multiplication implicitly cast to long
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/IntIntMapTest.java`
#### Snippet
```java
            }
        }
        this.randomIntsForValues = new Random(0x123456789ABCDL).ints().limit((long) (this.mapSizeDividedBy64 * 64)).toArray();

        this.intIntMap = new IntIntHashMap();
```

### IntegerMultiplicationImplicitCastToLong
i \* 2: integer multiplication implicitly cast to long
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/IntListJMHTest.java`
#### Snippet
```java
                .parallel()
                .filter(i -> i % 2 == 0)
                .mapToLong(i -> (long) (i * 2))
                .sum();
    }
```

### IntegerMultiplicationImplicitCastToLong
i \* 2: integer multiplication implicitly cast to long
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/IntListJMHTest.java`
#### Snippet
```java
        return IntStream.of(this.ints)
                .filter(i -> i % 2 == 0)
                .mapToLong(i -> (long) (i * 2))
                .sum();
    }
```

## RuleId[id=ThrowFromFinallyBlock]
### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `eclipse-collections-code-generator/src/main/java/org/eclipse/collections/codegenerator/tools/FileUtils.java`
#### Snippet
```java
                catch (IOException e)
                {
                    throw new RuntimeException("Could not close filewriter: " + e);
                }
            }
```

### ThrowFromFinallyBlock
`throw` inside 'finally' block
in `eclipse-collections-code-generator/src/main/java/org/eclipse/collections/codegenerator/tools/FileUtils.java`
#### Snippet
```java
                catch (IOException e)
                {
                    throw new RuntimeException("Could not close bufferedwriter: " + e);
                }
            }
```

## RuleId[id=RedundantCast]
### RedundantCast
Casting `this.size()` to `long` is redundant
in `eclipse-collections-api/src/main/java/org/eclipse/collections/api/map/MapIterable.java`
#### Snippet
```java
    default Spliterator<V> spliterator()
    {
        return Spliterators.spliterator(this.iterator(), (long) this.size(), 0);
    }

```

### RedundantCast
Casting `this.size()` to `long` is redundant
in `eclipse-collections-api/src/main/java/org/eclipse/collections/api/map/primitive/PrimitiveObjectMap.java`
#### Snippet
```java
    default Spliterator<V> spliterator()
    {
        return Spliterators.spliterator(this.iterator(), (long) this.size(), 0);
    }
}
```

### RedundantCast
Casting `this.size()` to `long` is redundant
in `eclipse-collections-api/src/main/java/org/eclipse/collections/api/collection/ImmutableCollection.java`
#### Snippet
```java
    default Spliterator<T> spliterator()
    {
        return Spliterators.spliterator(this.iterator(), (long) this.size(), 0);
    }

```

### RedundantCast
Casting `(this.mapSizeDividedBy16000 * 64)` to `long` is redundant
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/LongIntMapTest.java`
#### Snippet
```java
            }
        }
        this.randomIntegersForMap = new Random(0x123456789ABCDL).ints().limit((long) (this.mapSizeDividedBy16000 * 64)).toArray();

        this.longIntMap = new LongIntHashMap();
```

### RedundantCast
Casting `this.mapSizeDividedBy64` to `long` is redundant
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/IntIntMapTest.java`
#### Snippet
```java
        }

        int[] randomSeeds = new Random(0x123456789ABCDL).ints().limit((long) this.mapSizeDividedBy64).toArray();

        this.randomIntsForKeys = new int[this.mapSizeDividedBy64 * 64];
```

### RedundantCast
Casting `(this.mapSizeDividedBy64 * 64)` to `long` is redundant
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/IntIntMapTest.java`
#### Snippet
```java
            }
        }
        this.randomIntsForValues = new Random(0x123456789ABCDL).ints().limit((long) (this.mapSizeDividedBy64 * 64)).toArray();

        this.intIntMap = new IntIntHashMap();
```

### RedundantCast
Casting `sortedArray[middleIndex]` to `double` is redundant
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/primitive/ByteHashSet.java`
#### Snippet
```java
            return ((double) first + (double) second) / 2.0;
        }
        return (double) sortedArray[middleIndex];
    }

```

### RedundantCast
Casting `sortedArray[middleIndex]` to `double` is redundant
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/primitive/ByteHashSet.java`
#### Snippet
```java
                return ((double) first + (double) second) / 2.0;
            }
            return (double) sortedArray[middleIndex];
        }

```

### RedundantCast
Casting `HASH_CODE_FUNCTION` to `IntObjectToIntFunction` is redundant
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/AbstractMutableList.java`
#### Snippet
```java
    {
        // Optimize injectInto in subclasses if necessary
        return this.injectInto(1, (IntObjectToIntFunction<T>) HASH_CODE_FUNCTION);
    }

```

### RedundantCast
Casting `this.to` to `long` is redundant
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/primitive/LongInterval.java`
#### Snippet
```java
        public long estimateSize()
        {
            return ((long) this.to - (long) this.current) / (long) this.step + 1;
        }

```

### RedundantCast
Casting `this.current` to `long` is redundant
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/primitive/LongInterval.java`
#### Snippet
```java
        public long estimateSize()
        {
            return ((long) this.to - (long) this.current) / (long) this.step + 1;
        }

```

### RedundantCast
Casting `this.step` to `long` is redundant
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/primitive/LongInterval.java`
#### Snippet
```java
        public long estimateSize()
        {
            return ((long) this.to - (long) this.current) / (long) this.step + 1;
        }

```

## RuleId[id=SimplifiableBooleanExpression]
### SimplifiableBooleanExpression
`value == null && this.table[i - 1] != null || value != null` can be simplified to 'value != null\|\|this.table\[i - 1\] != null'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
                this.chainedForEachValue((Object[]) value, procedure);
            }
            else if (value == null && this.table[i - 1] != null || value != null)
            {
                procedure.value((V) value);
```

### SimplifiableBooleanExpression
`value == null && this.table[i - 1] != null || value != null` can be simplified to 'value != null\|\|this.table\[i - 1\] != null'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
                this.chainedForEachValue((Object[]) value, procedure);
            }
            else if (value == null && this.table[i - 1] != null || value != null)
            {
                procedure.value((V) value);
```

### SimplifiableBooleanExpression
`distinct && this.predicate.accept(each) || !distinct` can be simplified to '!distinct\|\|this.predicate.accept(each)'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/lazy/parallel/list/ParallelDistinctListIterable.java`
#### Snippet
```java
        {
            boolean distinct = this.distinct.put(each, true) == null;
            return distinct && this.predicate.accept(each) || !distinct;
        }
    }
```

## RuleId[id=MismatchedCollectionQueryUpdate]
### MismatchedCollectionQueryUpdate
Contents of collection `result` are updated, but never queried
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/SetAddAllTest.java`
#### Snippet
```java
    public void jdk()
    {
        Set<Integer> result = new HashSet<>();
        for (int i = 0; i < 1000; i++)
        {
```

## RuleId[id=PointlessBitwiseExpression]
### PointlessBitwiseExpression
`~(ResizeContainer.QUEUE_INCREMENT - 1)` can be replaced with '-ResizeContainer.QUEUE_INCREMENT'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
            else if (o == RESIZED || o == RESIZING)
            {
                j = (j & ~(ResizeContainer.QUEUE_INCREMENT - 1)) + ResizeContainer.QUEUE_INCREMENT;
                if (resizeContainer.resizers.get() == 1)
                {
```

### PointlessBitwiseExpression
`~(ResizeContainer.QUEUE_INCREMENT - 1)` can be replaced with '-ResizeContainer.QUEUE_INCREMENT'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
            else if (o == RESIZED || o == RESIZING)
            {
                j = (j & ~(ResizeContainer.QUEUE_INCREMENT - 1)) + ResizeContainer.QUEUE_INCREMENT;
                if (resizeContainer.resizers.get() == 1)
                {
```

## RuleId[id=RedundantExplicitClose]
### RedundantExplicitClose
Redundant 'close()'
in `eclipse-collections-testutils/src/main/java/org/eclipse/collections/impl/test/SerializeTestHelper.java`
#### Snippet
```java
            objectOutputStream.writeObject(sourceObject);
            objectOutputStream.flush();
            objectOutputStream.close();
        }
    }
```

## RuleId[id=SynchronizationOnLocalVariableOrMethodParameter]
### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `oldTable`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
        if (last == null || last == RESIZE_SENTINEL)
        {
            synchronized (oldTable) // allocating a new array is too expensive to make this an atomic operation
            {
                if (oldTable.get(end) == null)
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `oldTable`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
        if (last == null || last == RESIZE_SENTINEL)
        {
            synchronized (oldTable) // allocating a new array is too expensive to make this an atomic operation
            {
                if (ConcurrentHashMapUnsafe.arrayAt(oldTable, end) == null)
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `each`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Procedures.java`
#### Snippet
```java
            else
            {
                synchronized (each)
                {
                    this.procedure.value(each);
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `each`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Functions.java`
#### Snippet
```java
        public V valueOf(T each)
        {
            synchronized (each)
            {
                return this.function.valueOf(each);
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `each`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Predicates.java`
#### Snippet
```java
        public boolean accept(T each)
        {
            synchronized (each)
            {
                return this.predicate.accept(each);
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `existingValues`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/multimap/AbstractSynchronizedPutMultimap.java`
#### Snippet
```java
        {
            C existingValues = this.getIfAbsentPutCollection(key);
            synchronized (existingValues)
            {
                this.addAll(existingValues, values);
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `existingValues`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/multimap/AbstractSynchronizedPutMultimap.java`
#### Snippet
```java

        C existingValues = this.getIfAbsentPutCollection(key);
        synchronized (existingValues)
        {
            if (existingValues.isEmpty())
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `collection`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/multimap/AbstractSynchronizedPutMultimap.java`
#### Snippet
```java
    {
        MutableCollection<V> collection = this.getIfAbsentPutCollection(key);
        synchronized (collection)
        {
            if (collection.add(value))
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `bag`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/multimap/bag/SynchronizedPutHashBagMultimap.java`
#### Snippet
```java
            MutableBag<V> bag = this.map.getIfAbsentPutWith(key, this.createCollectionBlock(), this);

            synchronized (bag)
            {
                bag.addOccurrences(value, occurrences);
```

## RuleId[id=NullArgumentToVariableArgMethod]
### NullArgumentToVariableArgMethod
Confusing argument `this.items`, unclear if a varargs or non-varargs call is desired
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/immutable/ImmutableArrayList.java`
#### Snippet
```java
    public boolean containsAll(Collection<?> collection)
    {
        return Iterate.allSatisfy(collection, Predicates.in(this.items));
    }

```

## RuleId[id=UnusedAssignment]
### UnusedAssignment
Variable `remainingOccurrences` initializer `-1` is redundant
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/immutable/ImmutableArrayBag.java`
#### Snippet
```java
    {
        private int position;
        private int remainingOccurrences = -1;

        private ArrayBagIterator()
```

### UnusedAssignment
Variable `data` initializer `null` is redundant
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayListIterate.java`
#### Snippet
```java
    static
    {
        Field data = null;
        Field size = null;
        try
```

### UnusedAssignment
Variable `size` initializer `null` is redundant
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/utility/ArrayListIterate.java`
#### Snippet
```java
    {
        Field data = null;
        Field size = null;
        try
        {
```

### UnusedAssignment
Variable `isDaemon` initializer `true` is redundant
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/parallel/CollectionsThreadFactory.java`
#### Snippet
```java
    private final AtomicInteger threadNumber = new AtomicInteger(1);
    private final String namePrefix;
    private boolean isDaemon = true;

    CollectionsThreadFactory(String poolPrefix, boolean useDaemonThreads)
```

## RuleId[id=EqualsWhichDoesntCheckParameterClass]
### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/UnmodifiableMap.java`
#### Snippet
```java

    @Override
    public boolean equals(Object o)
    {
        return o == this || this.delegate.equals(o);
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/sorted/mutable/UnmodifiableSortedBag.java`
#### Snippet
```java

    @Override
    public boolean equals(Object obj)
    {
        return this.getSortedBag().equals(obj);
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/mutable/UnmodifiableBag.java`
#### Snippet
```java

    @Override
    public boolean equals(Object obj)
    {
        return this.getMutableBag().equals(obj);
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/immutable/ImmutableHashBag.java`
#### Snippet
```java

    @Override
    public boolean equals(Object obj)
    {
        return this.delegate.equals(obj);
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/sorted/mutable/SortedMapAdapter.java`
#### Snippet
```java

    @Override
    public boolean equals(Object o)
    {
        return this.delegate.equals(o);
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/sorted/mutable/TreeSortedMap.java`
#### Snippet
```java

    @Override
    public boolean equals(Object o)
    {
        return this.treeMap.equals(o);
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/MapAdapter.java`
#### Snippet
```java

    @Override
    public boolean equals(Object o)
    {
        return this.delegate.equals(o);
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentMutableHashMap.java`
#### Snippet
```java

    @Override
    public boolean equals(Object o)
    {
        return this.delegate.equals(o);
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/sorted/mutable/UnmodifiableTreeMap.java`
#### Snippet
```java

    @Override
    public boolean equals(Object obj)
    {
        return this.getMutableSortedMap().equals(obj);
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnmodifiableMutableMap.java`
#### Snippet
```java

    @Override
    public boolean equals(Object obj)
    {
        return this.getMutableMap().equals(obj);
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/ordered/mutable/OrderedMapAdapter.java`
#### Snippet
```java

    @Override
    public boolean equals(Object o)
    {
        return this.delegate.equals(o);
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/ordered/mutable/UnmodifiableMutableOrderedMap.java`
#### Snippet
```java

    @Override
    public boolean equals(Object o)
    {
        return this.delegate.equals(o);
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/immutable/ImmutableUnifiedMapWithHashingStrategy.java`
#### Snippet
```java

    @Override
    public boolean equals(Object o)
    {
        return this.delegate.equals(o);
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/immutable/ImmutableUnifiedMap.java`
#### Snippet
```java

    @Override
    public boolean equals(Object o)
    {
        return this.delegate.equals(o);
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/mutable/TreeSortedSet.java`
#### Snippet
```java

    @Override
    public boolean equals(Object object)
    {
        if (this == object)
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/mutable/UnmodifiableSortedSet.java`
#### Snippet
```java

    @Override
    public boolean equals(Object obj)
    {
        if (obj == this)
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/SetAdapter.java`
#### Snippet
```java

    @Override
    public boolean equals(Object obj)
    {
        if (obj == this)
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/sorted/mutable/SortedSetAdapter.java`
#### Snippet
```java

    @Override
    public boolean equals(Object obj)
    {
        if (obj == this)
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnmodifiableMutableSet.java`
#### Snippet
```java

    @Override
    public boolean equals(Object obj)
    {
        if (obj == this)
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/strategy/immutable/ImmutableUnifiedSetWithHashingStrategy.java`
#### Snippet
```java

    @Override
    public boolean equals(Object other)
    {
        if (other == this)
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/immutable/ImmutableUnifiedSet.java`
#### Snippet
```java

    @Override
    public boolean equals(Object other)
    {
        if (other == this)
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/AbstractListAdapter.java`
#### Snippet
```java

    @Override
    public boolean equals(Object o)
    {
        return this.getDelegate().equals(o);
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/mutable/UnmodifiableMutableList.java`
#### Snippet
```java

    @Override
    public boolean equals(Object obj)
    {
        return this.getMutableList().equals(obj);
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bimap/mutable/UnmodifiableBiMap.java`
#### Snippet
```java

    @Override
    public boolean equals(Object o)
    {
        return this.delegate.equals(o);
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bimap/mutable/AbstractMutableBiMap.java`
#### Snippet
```java
    {
        @Override
        public boolean equals(Object obj)
        {
            return AbstractMutableBiMap.this.delegate.keySet().equals(obj);
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/string/immutable/CodePointList.java`
#### Snippet
```java

    @Override
    public boolean equals(Object otherList)
    {
        return this.codePoints.equals(otherList);
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/stack/mutable/UnmodifiableStack.java`
#### Snippet
```java

    @Override
    public boolean equals(Object obj)
    {
        return this.mutableStack.equals(obj);
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/AbstractSynchronizedRichIterable.java`
#### Snippet
```java

    @Override
    public boolean equals(Object obj)
    {
        if (obj == this || obj == this.delegate)
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/collection/mutable/AbstractMultiReaderMutableCollection.java`
#### Snippet
```java

        @Override
        public boolean equals(Object o)
        {
            return this.delegate.equals(o);
```

## RuleId[id=ConstantValue]
### ConstantValue
Result of `e.isEmpty()` is always 'false'
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AnagramSetTest.java`
#### Snippet
```java
                .parallel()
                .map(list -> list.size() + ": " + list)
                .forEach(e -> Assert.assertFalse(e.isEmpty()));
    }

```

### ConstantValue
Result of `e.isEmpty()` is always 'false'
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AnagramSetTest.java`
#### Snippet
```java
                .sorted(Comparator.<Set<String>>comparingInt(Set::size).reversed())
                .map(list -> list.size() + ": " + list)
                .forEach(e -> Assert.assertFalse(e.isEmpty()));
    }

```

### ConstantValue
Result of `e.isEmpty()` is always 'false'
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AnagramSetTest.java`
#### Snippet
```java
                .parallel()
                .map(list -> list.size() + ": " + list)
                .forEach(e -> Assert.assertFalse(e.isEmpty()));
    }

```

### ConstantValue
Result of `e.isEmpty()` is always 'false'
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AnagramSetTest.java`
#### Snippet
```java
                .sorted(Comparator.<Set<String>>comparingInt(Set::size).reversed())
                .map(list -> list.size() + ": " + list)
                .forEach(e -> Assert.assertFalse(e.isEmpty()));
    }

```

### ConstantValue
Result of `e.isEmpty()` is always 'false'
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AnagramListTest.java`
#### Snippet
```java
                .sorted(Comparator.<List<String>>comparingInt(List::size).reversed())
                .map(list -> list.size() + ": " + list)
                .forEach(e -> Assert.assertFalse(e.isEmpty()));
    }

```

### ConstantValue
Result of `e.isEmpty()` is always 'false'
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AnagramListTest.java`
#### Snippet
```java
                .sorted(Comparator.<List<String>>comparingInt(List::size).reversed())
                .map(list -> list.size() + ": " + list)
                .forEach(e -> Assert.assertFalse(e.isEmpty()));
    }

```

### ConstantValue
Result of `e.isEmpty()` is always 'false'
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AnagramListTest.java`
#### Snippet
```java
                .sorted(Comparator.<List<String>>comparingInt(List::size).reversed())
                .map(list -> list.size() + ": " + list)
                .forEach(e -> Assert.assertFalse(e.isEmpty()));
    }

```

### ConstantValue
Result of `e.isEmpty()` is always 'false'
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AnagramListTest.java`
#### Snippet
```java
                .sorted(Comparator.<List<String>>comparingInt(List::size).reversed())
                .map(list -> list.size() + ": " + list)
                .forEach(e -> Assert.assertFalse(e.isEmpty()));
    }

```

### ConstantValue
Result of `e.isEmpty()` is always 'false'
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AnagramBagTest.java`
#### Snippet
```java
                .sorted(Comparator.<List<String>>comparingInt(List::size).reversed())
                .map(list -> list.size() + ": " + list)
                .forEach(e -> Assert.assertFalse(e.isEmpty()));
    }

```

### ConstantValue
Result of `e.isEmpty()` is always 'false'
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AnagramBagTest.java`
#### Snippet
```java
                .sorted(Comparator.<List<String>>comparingInt(List::size).reversed())
                .map(list -> list.size() + ": " + list)
                .forEach(e -> Assert.assertFalse(e.isEmpty()));
    }

```

### ConstantValue
Result of `e.isEmpty()` is always 'false'
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AnagramBagTest.java`
#### Snippet
```java
                .sorted(Comparator.<List<String>>comparingInt(List::size).reversed())
                .map(list -> list.size() + ": " + list)
                .forEach(e -> Assert.assertFalse(e.isEmpty()));
    }

```

### ConstantValue
Result of `e.isEmpty()` is always 'false'
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/AnagramBagTest.java`
#### Snippet
```java
                .sorted(Comparator.<List<String>>comparingInt(List::size).reversed())
                .map(list -> list.size() + ": " + list)
                .forEach(e -> Assert.assertFalse(e.isEmpty()));
    }

```

### ConstantValue
Value `source` is always 'null'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/mutable/primitive/BooleanHashBag.java`
#### Snippet
```java
        }

        return new BooleanHashBag(source);
    }

```

### ConstantValue
Value `this.next` is always 'false'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/bag/immutable/ImmutableSingletonBag.java`
#### Snippet
```java
                return ImmutableSingletonBag.this.value;
            }
            throw new NoSuchElementException("i=" + this.next);
        }

```

### ConstantValue
Condition `entries != null` is always `true`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/UnifiedMap.java`
#### Snippet
```java
    {
        Set<? extends Entry<? extends K, ? extends V>> entries = map.entrySet();
        if (entries != null)
        {
            return entries;
```

### ConstantValue
Condition `entries != null` is always `true`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/strategy/mutable/UnifiedMapWithHashingStrategy.java`
#### Snippet
```java
    {
        Set<? extends Entry<? extends K, ? extends V>> entries = map.entrySet();
        if (entries != null)
        {
            return entries;
```

### ConstantValue
Result of `i - 3` is always '0'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/UnifiedSet.java`
#### Snippet
```java
                        if (this.three instanceof ChainedBucket)
                        {
                            this.removeLongChain(this, i - 3);
                            return;
                        }
```

### ConstantValue
Condition `this.minusOneTwentyEightToPlusOneTwentySeven <= 127` is always `true`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/primitive/ByteHashSet.java`
#### Snippet
```java
                this.count++;

                while (this.minusOneTwentyEightToPlusOneTwentySeven <= 127)
                {
                    if (ImmutableByteHashSet.this.contains(this.minusOneTwentyEightToPlusOneTwentySeven))
```

### ConstantValue
Condition `this.minusOneTwentyEightToPlusOneTwentySeven <= 127` is always `true`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/mutable/primitive/ByteHashSet.java`
#### Snippet
```java
            this.count++;

            while (this.minusOneTwentyEightToPlusOneTwentySeven <= 127)
            {
                if (ByteHashSet.this.contains(this.minusOneTwentyEightToPlusOneTwentySeven))
```

### ConstantValue
Result of `i - 3` is always '0'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/set/strategy/mutable/UnifiedSetWithHashingStrategy.java`
#### Snippet
```java
                        if (this.three instanceof ChainedBucket)
                        {
                            this.removeLongChain(this, i - 3);
                            return;
                        }
```

### ConstantValue
Condition `otherList instanceof LongInterval` is always `true`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/list/primitive/LongInterval.java`
#### Snippet
```java
        }

        if (otherList instanceof LongInterval)
        {
            LongInterval otherInterval = (LongInterval) otherList;
```

### ConstantValue
Condition `function instanceof ByteFunction` is always `false`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Comparators.java`
#### Snippet
```java
            return Functions.toBooleanComparator((BooleanFunction<T>) function);
        }
        if (function instanceof ByteFunction)
        {
            return Functions.toByteComparator((ByteFunction<T>) function);
```

### ConstantValue
Value `function` is always 'null'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Comparators.java`
#### Snippet
```java
            return Functions.toBooleanComparator((BooleanFunction<T>) function);
        }
        if (function instanceof ByteFunction)
        {
            return Functions.toByteComparator((ByteFunction<T>) function);
```

### ConstantValue
Condition `function instanceof CharFunction` is always `false`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Comparators.java`
#### Snippet
```java
            return Functions.toByteComparator((ByteFunction<T>) function);
        }
        if (function instanceof CharFunction)
        {
            return Functions.toCharComparator((CharFunction<T>) function);
```

### ConstantValue
Value `function` is always 'null'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Comparators.java`
#### Snippet
```java
            return Functions.toByteComparator((ByteFunction<T>) function);
        }
        if (function instanceof CharFunction)
        {
            return Functions.toCharComparator((CharFunction<T>) function);
```

### ConstantValue
Condition `function instanceof DoubleFunction` is always `false`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Comparators.java`
#### Snippet
```java
            return Functions.toCharComparator((CharFunction<T>) function);
        }
        if (function instanceof DoubleFunction)
        {
            return Functions.toDoubleComparator((DoubleFunction<T>) function);
```

### ConstantValue
Value `function` is always 'null'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Comparators.java`
#### Snippet
```java
            return Functions.toCharComparator((CharFunction<T>) function);
        }
        if (function instanceof DoubleFunction)
        {
            return Functions.toDoubleComparator((DoubleFunction<T>) function);
```

### ConstantValue
Condition `function instanceof FloatFunction` is always `false`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Comparators.java`
#### Snippet
```java
            return Functions.toDoubleComparator((DoubleFunction<T>) function);
        }
        if (function instanceof FloatFunction)
        {
            return Functions.toFloatComparator((FloatFunction<T>) function);
```

### ConstantValue
Value `function` is always 'null'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Comparators.java`
#### Snippet
```java
            return Functions.toDoubleComparator((DoubleFunction<T>) function);
        }
        if (function instanceof FloatFunction)
        {
            return Functions.toFloatComparator((FloatFunction<T>) function);
```

### ConstantValue
Condition `function instanceof IntFunction` is always `false`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Comparators.java`
#### Snippet
```java
            return Functions.toFloatComparator((FloatFunction<T>) function);
        }
        if (function instanceof IntFunction)
        {
            return Functions.toIntComparator((IntFunction<T>) function);
```

### ConstantValue
Value `function` is always 'null'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Comparators.java`
#### Snippet
```java
            return Functions.toFloatComparator((FloatFunction<T>) function);
        }
        if (function instanceof IntFunction)
        {
            return Functions.toIntComparator((IntFunction<T>) function);
```

### ConstantValue
Condition `function instanceof LongFunction` is always `false`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Comparators.java`
#### Snippet
```java
            return Functions.toIntComparator((IntFunction<T>) function);
        }
        if (function instanceof LongFunction)
        {
            return Functions.toLongComparator((LongFunction<T>) function);
```

### ConstantValue
Value `function` is always 'null'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Comparators.java`
#### Snippet
```java
            return Functions.toIntComparator((IntFunction<T>) function);
        }
        if (function instanceof LongFunction)
        {
            return Functions.toLongComparator((LongFunction<T>) function);
```

### ConstantValue
Condition `function instanceof ShortFunction` is always `false`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Comparators.java`
#### Snippet
```java
            return Functions.toLongComparator((LongFunction<T>) function);
        }
        if (function instanceof ShortFunction)
        {
            return Functions.toShortComparator((ShortFunction<T>) function);
```

### ConstantValue
Value `function` is always 'null'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/block/factory/Comparators.java`
#### Snippet
```java
            return Functions.toLongComparator((LongFunction<T>) function);
        }
        if (function instanceof ShortFunction)
        {
            return Functions.toShortComparator((ShortFunction<T>) function);
```

### ConstantValue
Condition `otherList instanceof IntList` is always `false`
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/string/immutable/CodePointAdapter.java`
#### Snippet
```java
            return this.equalsCodePointAdapter((CodePointAdapter) otherList);
        }
        if (otherList instanceof IntList)
        {
            return this.equalsIntList((IntList) otherList);
```

### ConstantValue
Result of `objectA.equals(null)` is always 'false'
in `eclipse-collections-testutils/src/main/java/org/eclipse/collections/impl/test/Verify.java`
#### Snippet
```java
            }

            Assert.assertFalse("Neither item should equal null", objectA.equals(null));
            Assert.assertFalse("Neither item should equal null", objectB.equals(null));
            Assert.assertNotEquals("Neither item should equal new Object()", objectA.equals(new Object()));
```

### ConstantValue
Result of `objectB.equals(null)` is always 'false'
in `eclipse-collections-testutils/src/main/java/org/eclipse/collections/impl/test/Verify.java`
#### Snippet
```java

            Assert.assertFalse("Neither item should equal null", objectA.equals(null));
            Assert.assertFalse("Neither item should equal null", objectB.equals(null));
            Assert.assertNotEquals("Neither item should equal new Object()", objectA.equals(new Object()));
            Assert.assertNotEquals("Neither item should equal new Object()", objectB.equals(new Object()));
```

### ConstantValue
Condition `!actualMutableMapIterable.values().isEmpty()` is always `false`
in `eclipse-collections-testutils/src/main/java/org/eclipse/collections/impl/test/Verify.java`
#### Snippet
```java
                Assert.fail(mutableMapIterableName + " should be empty; actual size:<" + actualMutableMapIterable.keySet().size() + '>');
            }
            if (!actualMutableMapIterable.values().isEmpty())
            {
                Assert.fail(mutableMapIterableName + " should be empty; actual size:<" + actualMutableMapIterable.values().size() + '>');
```

### ConstantValue
Condition `!actualMutableMapIterable.entrySet().isEmpty()` is always `false`
in `eclipse-collections-testutils/src/main/java/org/eclipse/collections/impl/test/Verify.java`
#### Snippet
```java
                Assert.fail(mutableMapIterableName + " should be empty; actual size:<" + actualMutableMapIterable.values().size() + '>');
            }
            if (!actualMutableMapIterable.entrySet().isEmpty())
            {
                Assert.fail(mutableMapIterableName + " should be empty; actual size:<" + actualMutableMapIterable.entrySet().size() + '>');
```

### ConstantValue
Condition `valuesSize != expectedSize` is always `false`
in `eclipse-collections-testutils/src/main/java/org/eclipse/collections/impl/test/Verify.java`
#### Snippet
```java
            }
            int valuesSize = mutableMapIterable.values().size();
            if (valuesSize != expectedSize)
            {
                Assert.fail("Incorrect size for " + mapName + ".values(); expected:<" + expectedSize + "> but was:<" + actualSize + '>');
```

### ConstantValue
Condition `entrySetSize != expectedSize` is always `false`
in `eclipse-collections-testutils/src/main/java/org/eclipse/collections/impl/test/Verify.java`
#### Snippet
```java
            }
            int entrySetSize = mutableMapIterable.entrySet().size();
            if (entrySetSize != expectedSize)
            {
                Assert.fail("Incorrect size for " + mapName + ".entrySet(); expected:<" + expectedSize + "> but was:<" + actualSize + '>');
```

### ConstantValue
Condition `actualMap.size() != 0` is always `false`
in `eclipse-collections-testutils/src/main/java/org/eclipse/collections/impl/test/Verify.java`
#### Snippet
```java
                Assert.fail(mapName + " should be empty; actual size:<" + actualMap.size() + '>');
            }
            if (actualMap.size() != 0)
            {
                Assert.fail(mapName + " should be empty; actual size:<" + actualMap.size() + '>');
```

### ConstantValue
Condition `!actualMap.keySet().isEmpty()` is always `false`
in `eclipse-collections-testutils/src/main/java/org/eclipse/collections/impl/test/Verify.java`
#### Snippet
```java
                Assert.fail(mapName + " should be empty; actual size:<" + actualMap.size() + '>');
            }
            if (!actualMap.keySet().isEmpty())
            {
                Assert.fail(mapName + " should be empty; actual size:<" + actualMap.keySet().size() + '>');
```

### ConstantValue
Condition `!actualMap.values().isEmpty()` is always `false`
in `eclipse-collections-testutils/src/main/java/org/eclipse/collections/impl/test/Verify.java`
#### Snippet
```java
                Assert.fail(mapName + " should be empty; actual size:<" + actualMap.keySet().size() + '>');
            }
            if (!actualMap.values().isEmpty())
            {
                Assert.fail(mapName + " should be empty; actual size:<" + actualMap.values().size() + '>');
```

### ConstantValue
Condition `!actualMap.entrySet().isEmpty()` is always `false`
in `eclipse-collections-testutils/src/main/java/org/eclipse/collections/impl/test/Verify.java`
#### Snippet
```java
                Assert.fail(mapName + " should be empty; actual size:<" + actualMap.values().size() + '>');
            }
            if (!actualMap.entrySet().isEmpty())
            {
                Assert.fail(mapName + " should be empty; actual size:<" + actualMap.entrySet().size() + '>');
```

## RuleId[id=OptionalGetWithoutIsPresent]
### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MaxByIntTest.java`
#### Snippet
```java
    {
        return this.positions.getJdkPositions().parallelStream().max(
                QUANTITY_COMPARATOR_LAMBDA).get();
    }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MaxByIntTest.java`
#### Snippet
```java
    {
        return this.positions.getJdkPositions().parallelStream().collect(
                Collectors.maxBy(QUANTITY_COMPARATOR_METHODREF)).get();
    }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MaxByIntTest.java`
#### Snippet
```java
    {
        return this.positions.getJdkPositions().parallelStream().max(
                QUANTITY_COMPARATOR_METHODREF).get();
    }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MaxByIntTest.java`
#### Snippet
```java
    {
        return this.positions.getJdkPositions().stream().collect(
                Collectors.maxBy(QUANTITY_COMPARATOR_LAMBDA)).get();
    }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MaxByIntTest.java`
#### Snippet
```java
    {
        return this.positions.getJdkPositions().parallelStream().collect(
                Collectors.maxBy(QUANTITY_COMPARATOR_LAMBDA)).get();
    }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MaxByIntTest.java`
#### Snippet
```java
    public Position maxByQuantity_serial_lazy_direct_methodref_jdk()
    {
        return this.positions.getJdkPositions().stream().max(QUANTITY_COMPARATOR_METHODREF).get();
    }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MaxByIntTest.java`
#### Snippet
```java
    {
        return this.positions.getJdkPositions().stream().collect(
                Collectors.maxBy(QUANTITY_COMPARATOR_METHODREF)).get();
    }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MaxByIntTest.java`
#### Snippet
```java
    public Position maxByQuantity_serial_lazy_direct_lambda_jdk()
    {
        return this.positions.getJdkPositions().stream().max(QUANTITY_COMPARATOR_LAMBDA).get();
    }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MinTest.java`
#### Snippet
```java
    public int parallel_lazy_streams_ec()
    {
        return this.integersEC.parallelStream().min(Comparator.naturalOrder()).get();
    }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MinTest.java`
#### Snippet
```java
    public int serial_lazy_jdk()
    {
        return this.integersJDK.stream().min(Comparator.naturalOrder()).get();
    }

```

### OptionalGetWithoutIsPresent
`OptionalInt.getAsInt()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MinTest.java`
#### Snippet
```java
    public int parallel_lazy_intstream_jdk()
    {
        return this.integersJDK.parallelStream().mapToInt(Integer::intValue).min().getAsInt();
    }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MinTest.java`
#### Snippet
```java
    public int parallel_lazy_jdk()
    {
        return this.integersJDK.parallelStream().min(Comparator.naturalOrder()).get();
    }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MinTest.java`
#### Snippet
```java
    public int serial_lazy_reverse_streams_ec()
    {
        return this.integersEC.stream().min(Comparator.reverseOrder()).get();
    }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MinTest.java`
#### Snippet
```java
    public int serial_lazy_streams_ec()
    {
        return this.integersEC.stream().min(Comparator.naturalOrder()).get();
    }

```

### OptionalGetWithoutIsPresent
`OptionalInt.getAsInt()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MinTest.java`
#### Snippet
```java
    public int serial_lazy_intstream_jdk()
    {
        return this.integersJDK.stream().mapToInt(Integer::intValue).min().getAsInt();
    }

```

### OptionalGetWithoutIsPresent
`OptionalInt.getAsInt()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MinTest.java`
#### Snippet
```java
    public int serial_lazy_intstream_streams_ec()
    {
        return this.integersEC.stream().mapToInt(Integer::intValue).min().getAsInt();
    }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MinTest.java`
#### Snippet
```java
    public int serial_lazy_reverse_jdk()
    {
        return this.integersJDK.stream().min(Comparator.reverseOrder()).get();
    }

```

### OptionalGetWithoutIsPresent
`OptionalInt.getAsInt()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MinTest.java`
#### Snippet
```java
    public int parallel_lazy_intstream_streams_ec()
    {
        return this.integersEC.parallelStream().mapToInt(Integer::intValue).min().getAsInt();
    }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MinTest.java`
#### Snippet
```java
    public int parallel_lazy_reverse_streams_ec()
    {
        return this.integersEC.parallelStream().min(Comparator.reverseOrder()).get();
    }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MinTest.java`
#### Snippet
```java
    public int parallel_lazy_reverse_jdk()
    {
        return this.integersJDK.parallelStream().min(Comparator.reverseOrder()).get();
    }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MinByDoubleTest.java`
#### Snippet
```java
    {
        return this.positions.getJdkPositions().stream().collect(
                Collectors.minBy(MARKET_VALUE_COMPARATOR_METHODREF)).get();
    }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MinByDoubleTest.java`
#### Snippet
```java
    {
        return this.positions.getJdkPositions().stream().collect(
                Collectors.minBy(MARKET_VALUE_COMPARATOR_LAMBDA)).get();
    }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MinByDoubleTest.java`
#### Snippet
```java
    {
        return this.positions.getJdkPositions().parallelStream().collect(
                Collectors.minBy(MARKET_VALUE_COMPARATOR_LAMBDA)).get();
    }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MinByDoubleTest.java`
#### Snippet
```java
    {
        return this.positions.getJdkPositions().parallelStream().min(
                MARKET_VALUE_COMPARATOR_METHODREF).get();
    }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MinByDoubleTest.java`
#### Snippet
```java
    {
        return this.positions.getJdkPositions().parallelStream().collect(
                Collectors.minBy(MARKET_VALUE_COMPARATOR_METHODREF)).get();
    }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MinByDoubleTest.java`
#### Snippet
```java
    {
        return this.positions.getJdkPositions().parallelStream().min(
                MARKET_VALUE_COMPARATOR_LAMBDA).get();
    }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MinByDoubleTest.java`
#### Snippet
```java
    public Position minByMarketValue_serial_lazy_direct_lambda_jdk()
    {
        return this.positions.getJdkPositions().stream().min(MARKET_VALUE_COMPARATOR_LAMBDA).get();
    }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MinByDoubleTest.java`
#### Snippet
```java
    public Position minByMarketValue_serial_lazy_direct_methodref_jdk()
    {
        return this.positions.getJdkPositions().stream().min(MARKET_VALUE_COMPARATOR_METHODREF).get();
    }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MinByIntTest.java`
#### Snippet
```java
    public Position minByQuantity_serial_lazy_direct_lambda_jdk()
    {
        return this.positions.getJdkPositions().stream().min(QUANTITY_COMPARATOR_LAMBDA).get();
    }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MinByIntTest.java`
#### Snippet
```java
    {
        return this.positions.getJdkPositions().parallelStream().collect(
                Collectors.minBy(QUANTITY_COMPARATOR_LAMBDA)).get();
    }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MinByIntTest.java`
#### Snippet
```java
    public Position minByQuantity_serial_lazy_direct_methodref_jdk()
    {
        return this.positions.getJdkPositions().stream().min(QUANTITY_COMPARATOR_METHODREF).get();
    }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MinByIntTest.java`
#### Snippet
```java
    {
        return this.positions.getJdkPositions().parallelStream().min(
                QUANTITY_COMPARATOR_LAMBDA).get();
    }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MinByIntTest.java`
#### Snippet
```java
    {
        return this.positions.getJdkPositions().stream().collect(
                Collectors.minBy(QUANTITY_COMPARATOR_METHODREF)).get();
    }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MinByIntTest.java`
#### Snippet
```java
    {
        return this.positions.getJdkPositions().stream().collect(
                Collectors.minBy(QUANTITY_COMPARATOR_LAMBDA)).get();
    }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MinByIntTest.java`
#### Snippet
```java
    {
        return this.positions.getJdkPositions().parallelStream().collect(
                Collectors.minBy(QUANTITY_COMPARATOR_METHODREF)).get();
    }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MinByIntTest.java`
#### Snippet
```java
    {
        return this.positions.getJdkPositions().parallelStream().min(
                QUANTITY_COMPARATOR_METHODREF).get();
    }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MaxByDoubleTest.java`
#### Snippet
```java
    {
        return this.positions.getJdkPositions().stream().collect(
                Collectors.maxBy(MARKET_VALUE_COMPARATOR_LAMBDA)).get();
    }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MaxByDoubleTest.java`
#### Snippet
```java
    {
        return this.positions.getJdkPositions().parallelStream().max(
                MARKET_VALUE_COMPARATOR_METHODREF).get();
    }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MaxByDoubleTest.java`
#### Snippet
```java
    {
        return this.positions.getJdkPositions().parallelStream().collect(
                Collectors.maxBy(MARKET_VALUE_COMPARATOR_METHODREF)).get();
    }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MaxByDoubleTest.java`
#### Snippet
```java
    {
        return this.positions.getJdkPositions().stream().collect(
                Collectors.maxBy(MARKET_VALUE_COMPARATOR_METHODREF)).get();
    }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MaxByDoubleTest.java`
#### Snippet
```java
    {
        return this.positions.getJdkPositions().parallelStream().collect(
                Collectors.maxBy(MARKET_VALUE_COMPARATOR_LAMBDA)).get();
    }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MaxByDoubleTest.java`
#### Snippet
```java
    {
        return this.positions.getJdkPositions().parallelStream().max(
                MARKET_VALUE_COMPARATOR_LAMBDA).get();
    }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MaxByDoubleTest.java`
#### Snippet
```java
    public Position maxByMarketValue_serial_lazy_direct_methodref_jdk()
    {
        return this.positions.getJdkPositions().stream().max(MARKET_VALUE_COMPARATOR_METHODREF).get();
    }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MaxByDoubleTest.java`
#### Snippet
```java
    public Position maxByMarketValue_serial_lazy_direct_lambda_jdk()
    {
        return this.positions.getJdkPositions().stream().max(MARKET_VALUE_COMPARATOR_LAMBDA).get();
    }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MaxTest.java`
#### Snippet
```java
    public int parallel_lazy_streams_ec()
    {
        return this.integersEC.parallelStream().max(Comparator.naturalOrder()).get();
    }

```

### OptionalGetWithoutIsPresent
`OptionalInt.getAsInt()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MaxTest.java`
#### Snippet
```java
    public int parallel_lazy_intstream_jdk()
    {
        return this.integersJDK.parallelStream().mapToInt(Integer::intValue).max().getAsInt();
    }

```

### OptionalGetWithoutIsPresent
`OptionalInt.getAsInt()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MaxTest.java`
#### Snippet
```java
    public int serial_lazy_intstream_streams_ec()
    {
        return this.integersEC.stream().mapToInt(Integer::intValue).max().getAsInt();
    }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MaxTest.java`
#### Snippet
```java
    public int serial_lazy_jdk()
    {
        return this.integersJDK.stream().max(Comparator.naturalOrder()).get();
    }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MaxTest.java`
#### Snippet
```java
    public int parallel_lazy_reverse_streams_ec()
    {
        return this.integersEC.parallelStream().max(Comparator.reverseOrder()).get();
    }

```

### OptionalGetWithoutIsPresent
`OptionalInt.getAsInt()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MaxTest.java`
#### Snippet
```java
    public int serial_lazy_intstream_jdk()
    {
        return this.integersJDK.stream().mapToInt(Integer::intValue).max().getAsInt();
    }

```

### OptionalGetWithoutIsPresent
`OptionalInt.getAsInt()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MaxTest.java`
#### Snippet
```java
    public int parallel_lazy_intstream_streams_ec()
    {
        return this.integersEC.parallelStream().mapToInt(Integer::intValue).max().getAsInt();
    }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MaxTest.java`
#### Snippet
```java
    public int parallel_lazy_reverse_jdk()
    {
        return this.integersJDK.parallelStream().max(Comparator.reverseOrder()).get();
    }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MaxTest.java`
#### Snippet
```java
    public int serial_lazy_reverse_streams_ec()
    {
        return this.integersEC.stream().max(Comparator.reverseOrder()).get();
    }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MaxTest.java`
#### Snippet
```java
    public int serial_lazy_reverse_jdk()
    {
        return this.integersJDK.stream().max(Comparator.reverseOrder()).get();
    }

```

### OptionalGetWithoutIsPresent
`Optional.get()` without 'isPresent()' check
in `jmh-tests/src/main/java/org/eclipse/collections/impl/jmh/MaxTest.java`
#### Snippet
```java
    public int parallel_lazy_jdk()
    {
        return this.integersJDK.parallelStream().max(Comparator.naturalOrder()).get();
    }

```

## RuleId[id=CastCanBeRemovedNarrowingVariableType]
### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'key' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                Object key = e.getKey();
                Object value = e.getValue();
                this.put((K) key, (V) value);
                e = e.getNext();
            }
```

### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'value' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                Object key = e.getKey();
                Object value = e.getValue();
                this.put((K) key, (V) value);
                e = e.getNext();
            }
```

### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'key' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                Object key = e.getKey();
                Object value = e.getValue();
                block.value((K) key, (V) value);
                e = e.getNext();
            }
```

### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'value' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
                Object key = e.getKey();
                Object value = e.getValue();
                block.value((K) key, (V) value);
                e = e.getNext();
            }
```

### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'value' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMap.java`
#### Snippet
```java
            {
                Object value = e.getValue();
                block.value((V) value);
                e = e.getNext();
            }
```

### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'key' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
                Object key = e.getKey();
                Object value = e.getValue();
                block.value((K) key, (V) value);
                e = e.getNext();
            }
```

### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'value' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
                Object key = e.getKey();
                Object value = e.getValue();
                block.value((K) key, (V) value);
                e = e.getNext();
            }
```

### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'key' to 'K'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
                Object key = e.getKey();
                Object value = e.getValue();
                this.put((K) key, (V) value);
                e = e.getNext();
            }
```

### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'value' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
                Object key = e.getKey();
                Object value = e.getValue();
                this.put((K) key, (V) value);
                e = e.getNext();
            }
```

### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'value' to 'V'
in `eclipse-collections/src/main/java/org/eclipse/collections/impl/map/mutable/ConcurrentHashMapUnsafe.java`
#### Snippet
```java
            {
                Object value = e.getValue();
                block.value((V) value);
                e = e.getNext();
            }
```

