# iotdb 
 
# Bad smells
I found 5618 bad smells with 393 repairable:
| ruleID | number | fixable |
| --- | --- | --- |
| RuleId[ruleID=BoundedWildcard] | 661 | false |
| RuleId[ruleID=RedundantFieldInitialization] | 660 | false |
| RuleId[ruleID=ReturnNull] | 637 | false |
| RuleId[ruleID=SystemOutErr] | 292 | false |
| RuleId[ruleID=AssignmentToMethodParameter] | 290 | false |
| RuleId[ruleID=SizeReplaceableByIsEmpty] | 181 | true |
| RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized] | 178 | false |
| RuleId[ruleID=UtilityClassWithoutPrivateConstructor] | 174 | false |
| RuleId[ruleID=PublicFieldAccessedInSynchronizedContext] | 145 | false |
| RuleId[ruleID=UnusedAssignment] | 120 | false |
| RuleId[ruleID=NonProtectedConstructorInAbstractClass] | 120 | true |
| RuleId[ruleID=ThrowablePrintStackTrace] | 117 | false |
| RuleId[ruleID=DataFlowIssue] | 110 | false |
| RuleId[ruleID=ZeroLengthArrayInitialization] | 108 | false |
| RuleId[ruleID=SynchronizeOnThis] | 105 | false |
| RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment] | 99 | false |
| RuleId[ruleID=UnnecessaryFullyQualifiedName] | 98 | false |
| RuleId[ruleID=IgnoreResultOfCall] | 86 | false |
| RuleId[ruleID=UnnecessarySuperQualifier] | 79 | false |
| RuleId[ruleID=NestedAssignment] | 76 | false |
| RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern] | 76 | false |
| RuleId[ruleID=RedundantMethodOverride] | 64 | false |
| RuleId[ruleID=EnumSwitchStatementWhichMissesCases] | 55 | false |
| RuleId[ruleID=UnstableApiUsage] | 50 | false |
| RuleId[ruleID=EmptyMethod] | 43 | false |
| RuleId[ruleID=TypeParameterExtendsObject] | 40 | false |
| RuleId[ruleID=CommentedOutCode] | 39 | false |
| RuleId[ruleID=KeySetIterationMayUseEntrySet] | 38 | false |
| RuleId[ruleID=UnnecessaryBoxing] | 35 | false |
| RuleId[ruleID=ConstantValue] | 34 | false |
| RuleId[ruleID=StringEqualsEmptyString] | 33 | false |
| RuleId[ruleID=CodeBlock2Expr] | 30 | false |
| RuleId[ruleID=UnnecessaryToStringCall] | 30 | true |
| RuleId[ruleID=CallToStringConcatCanBeReplacedByOperator] | 29 | false |
| RuleId[ruleID=UnnecessaryLocalVariable] | 27 | true |
| RuleId[ruleID=AssignmentToForLoopParameter] | 21 | false |
| RuleId[ruleID=EqualsAndHashcode] | 20 | false |
| RuleId[ruleID=ObsoleteCollection] | 20 | false |
| RuleId[ruleID=AssignmentToStaticFieldFromInstanceMethod] | 19 | false |
| RuleId[ruleID=IntegerMultiplicationImplicitCastToLong] | 19 | false |
| RuleId[ruleID=UnnecessarySemicolon] | 19 | false |
| RuleId[ruleID=MethodOverridesStaticMethod] | 19 | false |
| RuleId[ruleID=BusyWait] | 18 | false |
| RuleId[ruleID=InnerClassMayBeStatic] | 17 | true |
| RuleId[ruleID=SynchronizationOnLocalVariableOrMethodParameter] | 16 | false |
| RuleId[ruleID=FieldMayBeStatic] | 16 | false |
| RuleId[ruleID=CStyleArrayDeclaration] | 15 | false |
| RuleId[ruleID=AssignmentToLambdaParameter] | 14 | false |
| RuleId[ruleID=SynchronizeOnNonFinalField] | 14 | false |
| RuleId[ruleID=MethodOverloadsParentMethod] | 13 | false |
| RuleId[ruleID=RefusedBequest] | 13 | false |
| RuleId[ruleID=DeprecatedIsStillUsed] | 13 | false |
| RuleId[ruleID=StringOperationCanBeSimplified] | 12 | false |
| RuleId[ruleID=InstanceofCatchParameter] | 12 | false |
| RuleId[ruleID=RedundantStringFormatCall] | 12 | false |
| RuleId[ruleID=DuplicateBranchesInSwitch] | 11 | false |
| RuleId[ruleID=NonShortCircuitBoolean] | 11 | false |
| RuleId[ruleID=UseOfPropertiesAsHashtable] | 11 | false |
| RuleId[ruleID=SwitchStatementWithConfusingDeclaration] | 9 | false |
| RuleId[ruleID=RedundantImplements] | 9 | false |
| RuleId[ruleID=NonFinalFieldOfException] | 9 | false |
| RuleId[ruleID=IOResource] | 9 | false |
| RuleId[ruleID=NestedSynchronizedStatement] | 9 | false |
| RuleId[ruleID=UnnecessaryUnboxing] | 8 | false |
| RuleId[ruleID=NonStrictComparisonCanBeEquality] | 8 | true |
| RuleId[ruleID=SuspiciousSystemArraycopy] | 8 | false |
| RuleId[ruleID=DuplicateExpressions] | 8 | false |
| RuleId[ruleID=IfStatementWithIdenticalBranches] | 8 | false |
| RuleId[ruleID=ObjectNotify] | 7 | false |
| RuleId[ruleID=OptionalContainsCollection] | 7 | false |
| RuleId[ruleID=Java8ListReplaceAll] | 6 | false |
| RuleId[ruleID=UnstableTypeUsedInSignature] | 6 | false |
| RuleId[ruleID=OptionalUsedAsFieldOrParameterType] | 6 | false |
| RuleId[ruleID=AbstractMethodCallInConstructor] | 6 | false |
| RuleId[ruleID=CaughtExceptionImmediatelyRethrown] | 5 | false |
| RuleId[ruleID=NonSerializableFieldInSerializableClass] | 5 | false |
| RuleId[ruleID=SetReplaceableByEnumSet] | 5 | false |
| RuleId[ruleID=CopyConstructorMissesField] | 5 | false |
| RuleId[ruleID=UnnecessaryModifier] | 4 | true |
| RuleId[ruleID=PointlessArithmeticExpression] | 4 | false |
| RuleId[ruleID=MagicConstant] | 4 | false |
| RuleId[ruleID=NullableProblems] | 4 | false |
| RuleId[ruleID=MismatchedCollectionQueryUpdate] | 4 | false |
| RuleId[ruleID=RedundantSuppression] | 4 | false |
| RuleId[ruleID=ArrayHashCode] | 4 | false |
| RuleId[ruleID=UseCompareMethod] | 4 | false |
| RuleId[ruleID=UseBulkOperation] | 4 | false |
| RuleId[ruleID=StaticCallOnSubclass] | 4 | false |
| RuleId[ruleID=Convert2MethodRef] | 4 | false |
| RuleId[ruleID=MismatchedJavadocCode] | 4 | false |
| RuleId[ruleID=CatchMayIgnoreException] | 4 | false |
| RuleId[ruleID=SuspiciousMethodCalls] | 4 | false |
| RuleId[ruleID=UnnecessaryContinue] | 4 | false |
| RuleId[ruleID=WrapperTypeMayBePrimitive] | 3 | false |
| RuleId[ruleID=EmptyStatementBody] | 3 | false |
| RuleId[ruleID=SortedCollectionWithNonComparableKeys] | 3 | false |
| RuleId[ruleID=TrivialStringConcatenation] | 3 | false |
| RuleId[ruleID=ClassNameSameAsAncestorName] | 3 | false |
| RuleId[ruleID=NonSynchronizedMethodOverridesSynchronizedMethod] | 3 | false |
| RuleId[ruleID=UtilityClassWithPublicConstructor] | 3 | false |
| RuleId[ruleID=ReplaceAllDot] | 3 | false |
| RuleId[ruleID=StringBufferReplaceableByString] | 3 | false |
| RuleId[ruleID=AbstractClassNeverImplemented] | 3 | false |
| RuleId[ruleID=InfiniteLoopStatement] | 3 | false |
| RuleId[ruleID=NullArgumentToVariableArgMethod] | 3 | false |
| RuleId[ruleID=RedundantCollectionOperation] | 3 | false |
| RuleId[ruleID=WhileCanBeForeach] | 2 | false |
| RuleId[ruleID=RegExpRedundantEscape] | 2 | false |
| RuleId[ruleID=FinalPrivateMethod] | 2 | false |
| RuleId[ruleID=DoubleBraceInitialization] | 2 | false |
| RuleId[ruleID=IndexOfReplaceableByContains] | 2 | false |
| RuleId[ruleID=FinallyBlockCannotCompleteNormally] | 2 | false |
| RuleId[ruleID=CollectionContainsUrl] | 2 | false |
| RuleId[ruleID=ProtectedMemberInFinalClass] | 2 | true |
| RuleId[ruleID=ConditionalBreakInInfiniteLoop] | 2 | false |
| RuleId[ruleID=RegExpSimplifiable] | 2 | false |
| RuleId[ruleID=UnnecessaryReturn] | 2 | true |
| RuleId[ruleID=ConstantMathCall] | 2 | false |
| RuleId[ruleID=ManualMinMaxCalculation] | 2 | false |
| RuleId[ruleID=WhileLoopSpinsOnField] | 2 | false |
| RuleId[ruleID=ArrayEquality] | 2 | false |
| RuleId[ruleID=StringBufferReplaceableByStringBuilder] | 2 | false |
| RuleId[ruleID=OptionalGetWithoutIsPresent] | 2 | false |
| RuleId[ruleID=StringConcatenationInsideStringBufferAppend] | 2 | false |
| RuleId[ruleID=RedundantLengthCheck] | 2 | false |
| RuleId[ruleID=RegExpSingleCharAlternation] | 2 | false |
| RuleId[ruleID=ToArrayCallWithZeroLengthArrayArgument] | 1 | true |
| RuleId[ruleID=EqualsWithItself] | 1 | false |
| RuleId[ruleID=MismatchedArrayReadWrite] | 1 | false |
| RuleId[ruleID=FinalStaticMethod] | 1 | false |
| RuleId[ruleID=AccessStaticViaInstance] | 1 | false |
| RuleId[ruleID=RedundantExplicitClose] | 1 | false |
| RuleId[ruleID=Contract] | 1 | false |
| RuleId[ruleID=SimplifyStreamApiCallChains] | 1 | false |
| RuleId[ruleID=JavaReflectionMemberAccess] | 1 | false |
| RuleId[ruleID=UnusedLabel] | 1 | true |
| RuleId[ruleID=PrimitiveArrayArgumentToVariableArgMethod] | 1 | false |
| RuleId[ruleID=WaitNotInLoop] | 1 | false |
| RuleId[ruleID=StringEquality] | 1 | false |
| RuleId[ruleID=LoopStatementsThatDontLoop] | 1 | false |
| RuleId[ruleID=UnnecessaryStringEscape] | 1 | false |
| RuleId[ruleID=RedundantArrayCreation] | 1 | false |
| RuleId[ruleID=ThrowFromFinallyBlock] | 1 | false |
| RuleId[ruleID=ExceptionNameDoesntEndWithException] | 1 | false |
| RuleId[ruleID=FuseStreamOperations] | 1 | false |
| RuleId[ruleID=ThreadStartInConstruction] | 1 | false |
| RuleId[ruleID=BigDecimalMethodWithoutRoundingCalled] | 1 | false |
| RuleId[ruleID=CastCanBeRemovedNarrowingVariableType] | 1 | false |
## RuleId[ruleID=ToArrayCallWithZeroLengthArrayArgument]
### RuleId[ruleID=ToArrayCallWithZeroLengthArrayArgument]
Call to `toArray()` with pre-sized array argument 'new Procedure\[subProcList.size()\]'
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/impl/statemachine/StateMachineProcedure.java`
#### Snippet
```java

      if (subProcList != null && !subProcList.isEmpty()) {
        Procedure[] subProcedures = subProcList.toArray(new Procedure[subProcList.size()]);
        subProcList = null;
        return subProcedures;
```

## RuleId[ruleID=EqualsWithItself]
### RuleId[ruleID=EqualsWithItself]
`equalsIgnoreCase()` called on itself
in `confignode/src/main/java/org/apache/iotdb/confignode/consensus/request/read/template/GetSchemaTemplatePlan.java`
#### Snippet
```java
    if (o == null || getClass() != o.getClass()) return false;
    GetSchemaTemplatePlan that = (GetSchemaTemplatePlan) o;
    return this.templateName.equalsIgnoreCase(this.templateName);
  }

```

## RuleId[ruleID=WrapperTypeMayBePrimitive]
### RuleId[ruleID=WrapperTypeMayBePrimitive]
Type may be primitive
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/cross/utils/InplaceCompactionEstimator.java`
#### Snippet
```java
      throws IOException {
    long cost = 0;
    Long fileCost = unseqMeasurement.measure(unseqResource);
    cost += fileCost;

```

### RuleId[ruleID=WrapperTypeMayBePrimitive]
Type may be primitive
in `library-udf/src/main/java/org/apache/iotdb/library/drepair/util/ValueRepair.java`
#### Snippet
```java
    while (dataIterator.hasNextRow()) {
      Row row = dataIterator.next();
      Double v = Util.getValueAsDouble(row);
      timeList.add(row.getTime());
      if (!Double.isFinite(v)) {
```

### RuleId[ruleID=WrapperTypeMayBePrimitive]
Type may be primitive
in `library-udf/src/main/java/org/apache/iotdb/library/drepair/util/ValueFill.java`
#### Snippet
```java
    while (dataIterator.hasNextRow()) {
      Row row = dataIterator.next();
      Double v = Util.getValueAsDouble(row);
      timeList.add(row.getTime());
      if (!Double.isFinite(v)) {
```

## RuleId[ruleID=MismatchedArrayReadWrite]
### RuleId[ruleID=MismatchedArrayReadWrite]
Contents of array `byteToChars` are written to, but never read
in `jdbc/src/main/java/org/apache/iotdb/jdbc/StringUtils.java`
#### Snippet
```java
  private static byte[] allBytes = new byte[256];

  private static char[] byteToChars = new char[256];

  private static Method toPlainStringMethod;
```

## RuleId[ruleID=UnnecessaryModifier]
### RuleId[ruleID=UnnecessaryModifier]
Modifier `public` is redundant for interface members
in `node-commons/src/main/java/org/apache/iotdb/commons/auth/user/IUserAccessor.java`
#### Snippet
```java
   * @return userDirPath
   */
  public String getDirPath();
}

```

### RuleId[ruleID=UnnecessaryModifier]
Modifier `public` is redundant for interface members
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/IMPPDataExchangeManagerCallback.java`
#### Snippet
```java

public interface IMPPDataExchangeManagerCallback<T> {
  public void call(T argument);
}

```

### RuleId[ruleID=UnnecessaryModifier]
Modifier `public` is redundant for interface members
in `server/src/main/java/org/apache/iotdb/db/metadata/idtable/entry/IDeviceID.java`
#### Snippet
```java
   * @return string format device id
   */
  public String toStringID();

  public void serialize(ByteBuffer byteBuffer);
```

### RuleId[ruleID=UnnecessaryModifier]
Modifier `public` is redundant for interface members
in `server/src/main/java/org/apache/iotdb/db/metadata/idtable/entry/IDeviceID.java`
#### Snippet
```java
  public String toStringID();

  public void serialize(ByteBuffer byteBuffer);
}

```

## RuleId[ruleID=PointlessArithmeticExpression]
### RuleId[ruleID=PointlessArithmeticExpression]
`1 * 1024 * 1024` can be replaced with '1024 \* 1024'
in `server/src/main/java/org/apache/iotdb/db/sync/transport/client/IoTDBSyncClient.java`
#### Snippet
```java
  private static final IoTDBConfig config = IoTDBDescriptor.getInstance().getConfig();

  private static final int TRANSFER_BUFFER_SIZE_IN_BYTES = 1 * 1024 * 1024;

  private TTransport transport = null;
```

### RuleId[ruleID=PointlessArithmeticExpression]
`addCostRatio * 1` can be replaced with 'addCostRatio'
in `library-udf/src/main/java/org/apache/iotdb/library/drepair/util/TimestampRepair.java`
#### Snippet
```java
          // addition or deletion
          if (f[i - 1][j] < f[i][j - 1]) {
            f[i][j] = f[i - 1][j] + addCostRatio * 1;
            steps[i][j] = 1;
          } else {
```

### RuleId[ruleID=PointlessArithmeticExpression]
`addCostRatio * 1` can be replaced with 'addCostRatio'
in `library-udf/src/main/java/org/apache/iotdb/library/drepair/util/TimestampRepair.java`
#### Snippet
```java
            steps[i][j] = 1;
          } else {
            f[i][j] = f[i][j - 1] + addCostRatio * 1;
            steps[i][j] = 2;
          }
```

### RuleId[ruleID=PointlessArithmeticExpression]
`0 + offset` can be replaced with 'offset'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BytesUtils.java`
#### Snippet
```java
      throw new IllegalArgumentException("Invalid input: desc.length - offset < 4");
    }
    desc[0 + offset] = (byte) ((i >> 24) & 0xFF);
    desc[1 + offset] = (byte) ((i >> 16) & 0xFF);
    desc[2 + offset] = (byte) ((i >> 8) & 0xFF);
```

## RuleId[ruleID=MagicConstant]
### RuleId[ruleID=MagicConstant]
Should be one of: ResultSetMetaData.columnNoNulls, ResultSetMetaData.columnNullable, ...
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBResultMetadata.java`
#### Snippet
```java
  @Override
  public int isNullable(int arg0) throws SQLException {
    return 1;
  }

```

### RuleId[ruleID=MagicConstant]
Should be one of: ParameterMetaData.parameterModeIn, ParameterMetaData.parameterModeOut, ...
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
      @Override
      public int getParameterMode(int param) {
        return 0;
      }

```

### RuleId[ruleID=MagicConstant]
Should be one of: DatabaseMetaData.sqlStateXOpen, DatabaseMetaData.sqlStateSQL, DatabaseMetaData.sqlStateSQL99
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
  @Override
  public int getSQLStateType() {
    return 0;
  }

```

### RuleId[ruleID=MagicConstant]
Should be one of: ResultSetMetaData.columnNoNulls, ResultSetMetaData.columnNullable, ...
in `integration-test/src/main/java/org/apache/iotdb/itbase/runtime/ClusterResultSetMetaData.java`
#### Snippet
```java
      delegate.addRequest(() -> rs.isNullable(column));
    }
    return delegate.requestAllAndCompare();
  }

```

## RuleId[ruleID=EmptyStatementBody]
### RuleId[ruleID=EmptyStatementBody]
`for` statement has empty body
in `server/src/main/java/org/apache/iotdb/db/service/metrics/ProcessMetrics.java`
#### Snippet
```java
  private int getThreadsCount() {
    ThreadGroup parentThread;
    for (parentThread = Thread.currentThread().getThreadGroup();
        parentThread.getParent() != null;
        parentThread = parentThread.getParent()) {}
```

### RuleId[ruleID=EmptyStatementBody]
`while` statement has empty body
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/MTreeReleaseTaskManager.java`
#### Snippet
```java
    if (releaseTaskExecutor != null) {
      releaseTaskExecutor.shutdown();
      while (!releaseTaskExecutor.isTerminated()) ;
      releaseTaskExecutor = null;
    }
```

### RuleId[ruleID=EmptyStatementBody]
`while` statement has empty body
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/MTreeFlushTaskManager.java`
#### Snippet
```java
    if (flushTaskExecutor != null) {
      flushTaskExecutor.shutdown();
      while (!flushTaskExecutor.isTerminated()) ;
      flushTaskExecutor = null;
    }
```

## RuleId[ruleID=WhileCanBeForeach]
### RuleId[ruleID=WhileCanBeForeach]
`while` loop can be replaced with enhanced 'for'
in `server/src/main/java/org/apache/iotdb/db/sync/externalpipe/ExtPipePlugin.java`
#### Snippet
```java
        Set<String> sgSet = pipeOpManager.getSgSet();
        Iterator<String> iter = sgSet.iterator();
        while (iter.hasNext()) {
          String sgName = iter.next();
          if (threadIndex != getThreadIndex(sgName)) {
```

### RuleId[ruleID=WhileCanBeForeach]
`while` loop can be replaced with enhanced 'for'
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/traverser/Traverser.java`
#### Snippet
```java
    }
    Iterator<IMNode> nodes = traverseContext.iterator();
    while (nodes.hasNext()) {
      IMNode node = nodes.next();
      if (node.isStorageGroup()) {
```

## RuleId[ruleID=SortedCollectionWithNonComparableKeys]
### RuleId[ruleID=SortedCollectionWithNonComparableKeys]
Construction of sorted collection with non-comparable elements
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/ConfigMTree.java`
#### Snippet
```java
            }
          };
      collector.setResultSet(new TreeSet<>());
      collector.traverse();

```

### RuleId[ruleID=SortedCollectionWithNonComparableKeys]
Construction of sorted collection with non-comparable elements
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/MTreeBelowSGMemoryImpl.java`
#### Snippet
```java
            }
          };
      collector.setResultSet(new TreeSet<>());
      collector.traverse();
      return collector.getResult();
```

### RuleId[ruleID=SortedCollectionWithNonComparableKeys]
Construction of sorted collection with non-comparable elements
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/MTreeBelowSGCachedImpl.java`
#### Snippet
```java
            }
          };
      collector.setResultSet(new TreeSet<>());
      collector.traverse();
      return collector.getResult();
```

## RuleId[ruleID=CommentedOutCode]
### RuleId[ruleID=CommentedOutCode]
Commented out code (7 lines)
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/fragment/FragmentInstanceContext.java`
#### Snippet
```java
  private SessionInfo sessionInfo;

  //    private final GcMonitor gcMonitor;
  //    private final AtomicLong startNanos = new AtomicLong();
  //    private final AtomicLong startFullGcCount = new AtomicLong(-1);
```

### RuleId[ruleID=CommentedOutCode]
Commented out code (3 lines)
in `server/src/main/java/org/apache/iotdb/db/conf/IoTDBConfig.java`
#### Snippet
```java
  }

  //  private String addHomeDir(String dir) {
  //    return addDirPrefix(System.getProperty(IoTDBConstant.IOTDB_HOME, null), dir);
  //  }
```

### RuleId[ruleID=CommentedOutCode]
Commented out code (13 lines)
in `server/src/main/java/org/apache/iotdb/db/sync/externalpipe/ExtPipePlugin.java`
#### Snippet
```java
              writer.insertText(sgName, path, timestampInMs, tvPair.getValue().getStringValue());
              break;
              //            case VECTOR:
              //              writer.insertVector(
              //                  sgName,
```

### RuleId[ruleID=CommentedOutCode]
Commented out code (4 lines)
in `session/src/main/java/org/apache/iotdb/session/pool/SessionPool.java`
#### Snippet
```java
      this.notify();
      // comment the following codes as putBack is too frequently called.
      //      if (logger.isTraceEnabled()) {
      //        logger.trace("put a session back and notify others..., queue.size = {}",
      // queue.size());
```

### RuleId[ruleID=CommentedOutCode]
Commented out code (9 lines)
in `server/src/main/java/org/apache/iotdb/db/query/control/QueryTimeManager.java`
#### Snippet
```java
   */
  public static boolean checkQueryAlive(long queryId) {
    //    QueryContext queryContext = getInstance().getQueryContext(queryId);
    //    if (queryContext == null) {
    //      return false;
```

### RuleId[ruleID=CommentedOutCode]
Commented out code (6 lines)
in `server/src/main/java/org/apache/iotdb/db/query/control/SessionManager.java`
#### Snippet
```java
    releaseSessionResource(session, releaseByQueryId);
    // TODO we only need to do so when query is killed by time out
    //    // close the socket.
    //    // currently, we only focus on RPC service.
    //    // TODO do we need to consider MQTT ClientSession and Internal Client?
```

### RuleId[ruleID=CommentedOutCode]
Commented out code (4 lines)
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/QuickSort.java`
#### Snippet
```java
  }

  //    default void insertion_sort(int lo, int hi){
  //        for(int i = lo + 1; i <= hi; i++) {
  //        }
```

### RuleId[ruleID=CommentedOutCode]
Commented out code (3 lines)
in `server/src/main/java/org/apache/iotdb/db/service/SettleService.java`
#### Snippet
```java
        }
      }
      //      while (!StorageEngine.getInstance().isAllSgReady()) {
      //        // wait for all sg ready
      //      }
```

### RuleId[ruleID=CommentedOutCode]
Commented out code (14 lines)
in `server/src/main/java/org/apache/iotdb/db/service/SettleService.java`
#### Snippet
```java
      List<TsFileResource> seqResourcesToBeSettled = new ArrayList<>();
      List<TsFileResource> unseqResourcesToBeSettled = new ArrayList<>();
      //      for (Map.Entry<PartialPath, List<String>> entry : tmpSgResourcesMap.entrySet()) {
      //        try {
      //          StorageEngine.getInstance()
```

### RuleId[ruleID=CommentedOutCode]
Commented out code (2 lines)
in `server/src/main/java/org/apache/iotdb/db/service/UpgradeSevice.java`
#### Snippet
```java

  private static void countUpgradeFiles() {
    //    cntUpgradeFileNum.addAndGet(StorageEngine.getInstance().countUpgradeFiles());
    //    logger.info("finish counting upgrading files, total num:{}", cntUpgradeFileNum);
  }
```

### RuleId[ruleID=CommentedOutCode]
Commented out code (5 lines)
in `server/src/main/java/org/apache/iotdb/db/service/UpgradeSevice.java`
#### Snippet
```java

  private static void upgradeAll() {
    //    try {
    //      StorageEngine.getInstance().upgradeAll();
    //    } catch (StorageEngineException e) {
```

### RuleId[ruleID=CommentedOutCode]
Commented out code (2 lines)
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/SchemaFile.java`
#### Snippet
```java
    this.isEntity = sgNode.isEntity();
    // todo record the templateId generated in configNode
    //    this.templateHash =
    //        sgNode.getSchemaTemplate() == null ? 0 : sgNode.getSchemaTemplate().hashCode();
    updateHeader();
```

### RuleId[ruleID=CommentedOutCode]
Commented out code (8 lines)
in `example/session/src/main/java/org/apache/iotdb/AlignedTimeseriesSessionExample.java`
#### Snippet
```java

    insertAlignedRecord();
    //    insertAlignedRecords();
    //    insertAlignedRecordsOfOneDevice();

```

### RuleId[ruleID=CommentedOutCode]
Commented out code (5 lines)
in `example/session/src/main/java/org/apache/iotdb/AlignedTimeseriesSessionExample.java`
#### Snippet
```java
    selectWithLastTestWithoutValueFilter();

    //    selectWithValueFilterTest();
    //    selectWithGroupByTest();
    //    selectWithLastTest();
```

### RuleId[ruleID=CommentedOutCode]
Commented out code (3 lines)
in `library-udf/src/main/java/org/apache/iotdb/library/drepair/util/TimestampRepair.java`
#### Snippet
```java
        repaired[i - 1] = ps;
        repairedValue[i - 1] = original[j - 1];
        /*
        if(logger.isDebugEnabled()){
          logger.debug(time[j - 1] + "," + ps + "," + original[j - 1]);
```

### RuleId[ruleID=CommentedOutCode]
Commented out code (3 lines)
in `library-udf/src/main/java/org/apache/iotdb/library/drepair/util/TimestampRepair.java`
#### Snippet
```java
        repairedValue[i - 1] = Double.NaN;
        unionSet += 1;
        /*
        if(logger.isDebugEnabled()){
          logger.debug("add, " + ps + "," + original[j - 1]);
```

### RuleId[ruleID=CommentedOutCode]
Commented out code (3 lines)
in `library-udf/src/main/java/org/apache/iotdb/library/drepair/util/TimestampRepair.java`
#### Snippet
```java
        // delete points
        unionSet += 1;
        /*
        if(logger.isDebugEnabled()){
          logger.debug(time[j - 1] + ",delete" + "," + original[j - 1]);
```

### RuleId[ruleID=CommentedOutCode]
Commented out code (4 lines)
in `library-udf/src/main/java/org/apache/iotdb/library/drepair/util/TimestampRepair.java`
#### Snippet
```java
      }
    }
    /*
    if(logger.isDebugEnabled()) {
      logger.debug(joinSet / unionSet);
```

### RuleId[ruleID=CommentedOutCode]
Commented out code (15 lines)
in `example/session/src/main/java/org/apache/iotdb/SessionExample.java`
#### Snippet
```java
    insertRecord();
    insertTablet();
    //    insertTabletWithNullValues();
    //    insertTablets();
    //    insertRecords();
```

### RuleId[ruleID=CommentedOutCode]
Commented out code (2 lines)
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/KLLSketchForQuantile.java`
#### Snippet
```java
    for (int i = 0; i < cntLevel; i++) {
      approxRank += findRankInLevel(i, v);
      //      for (int j = levelPos[i]; j < levelPos[i + 1]; j++)
      //        if (num[j] < v) approxRank += 1 << i;
    }
```

### RuleId[ruleID=CommentedOutCode]
Commented out code (4 lines)
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/KLLSketchForQuantile.java`
#### Snippet
```java
    level0Sorted = false;

    //    boolean flag=false;
    //    for(int i=0;i<cntLevel;i++)if(levelPos[i+1]-levelPos[i]>levelMaxSize[i])flag=true;
    //    if(flag)compact();
```

### RuleId[ruleID=CommentedOutCode]
Commented out code (6 lines)
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/HeapLongKLLSketch.java`
#### Snippet
```java
  public void mergeWithTempSpace(
      List<org.apache.iotdb.library.dprofile.util.KLLSketchForQuantile> otherList) {
    //    System.out.println("[MERGE]");
    //    show();
    //
```

### RuleId[ruleID=CommentedOutCode]
Commented out code (2 lines)
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/HeapLongKLLSketch.java`
#### Snippet
```java
        otherN += another.getN();
      }
    //    System.out.println();
    //    System.out.println("[mergeWithTempSpace]\totherNumLen:"+otherNumLen);
    if (getNumLen() + otherNumLen <= maxMemoryNum) {
```

### RuleId[ruleID=CommentedOutCode]
Commented out code (2 lines)
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/HeapLongKLLSketch.java`
#### Snippet
```java
      levelPos[cntLevel] = numLen;
      this.N += otherN;
      //    System.out.println("-------------------------------.............---------");
      //      show();System.out.println("\t?\t"+levelPos[0]);
      while (getNumLen() > maxMemoryNum) compact();
```

### RuleId[ruleID=CommentedOutCode]
Commented out code (2 lines)
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/HeapLongKLLSketch.java`
#### Snippet
```java
      //      show();System.out.println("\t?\t"+levelPos[0]);
      while (getNumLen() > maxMemoryNum) compact();
      //      show();System.out.println("\t?\t"+levelPos[0]);
      //    System.out.println("\t\t??\t\t"+Arrays.toString(num));
      int newPos0 = maxMemoryNum - getNumLen();
```

### RuleId[ruleID=CommentedOutCode]
Commented out code (6 lines)
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/HeapLongKLLSketch.java`
#### Snippet
```java
      num = oldNum;
    }
    //    System.out.println("\t\t??\t\t"+Arrays.toString(num));
    //    System.out.println("\t\t??\t\t"+Arrays.toString(levelPos));
    //    System.out.println("-------------------------------.............---------");
```

### RuleId[ruleID=CommentedOutCode]
Commented out code (3 lines)
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/HeapLongKLLSketch.java`
#### Snippet
```java
  public void mergeWithTempSpace(
      org.apache.iotdb.library.dprofile.util.KLLSketchForQuantile another) {
    //    System.out.println("[MERGE]");
    //    show();
    //    another.show();
```

### RuleId[ruleID=CommentedOutCode]
Commented out code (4 lines)
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/HeapLongKLLSketch.java`
#### Snippet
```java
      for (int i = 0; i < cntLevel; i++) {
        int tmpL = cntPos;
        //        if(i<oldCntLevel)
        //
        // System.out.println("\t\t\t\t"+tmpL+"\t\t"+levelPos[i]+"\t"+oldLevelPos[i]+"---"+oldLevelPos[i+1]);
```

### RuleId[ruleID=CommentedOutCode]
Commented out code (2 lines)
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/HeapLongKLLSketch.java`
#### Snippet
```java
      }
      levelPos[cntLevel] = numLen;
      //    System.out.println("-------------------------------.............---------");
      //    show();
      while (getNumLen() > maxMemoryNum) compact();
```

### RuleId[ruleID=CommentedOutCode]
Commented out code (2 lines)
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/HeapLongKLLSketch.java`
#### Snippet
```java
      //    show();
      while (getNumLen() > maxMemoryNum) compact();
      //    show();
      //    System.out.println("\t\t??\t\t"+Arrays.toString(num));
      int newPos0 = maxMemoryNum - getNumLen();
```

### RuleId[ruleID=CommentedOutCode]
Commented out code (6 lines)
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/HeapLongKLLSketch.java`
#### Snippet
```java
      this.N += another.N;
    }
    //    System.out.println("\t\t??\t\t"+Arrays.toString(num));
    //    System.out.println("\t\t??\t\t"+Arrays.toString(levelPos));
    //    System.out.println("-------------------------------.............---------");
```

### RuleId[ruleID=CommentedOutCode]
Commented out code (3 lines)
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/HeapLongKLLSketch.java`
#### Snippet
```java

  public void merge(org.apache.iotdb.library.dprofile.util.KLLSketchForQuantile another) {
    //    System.out.println("[MERGE]");
    //    show();
    //    another.show();
```

### RuleId[ruleID=CommentedOutCode]
Commented out code (4 lines)
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/HeapLongKLLSketch.java`
#### Snippet
```java
        //        System.out.println("\t\t"+levelPos[0]);show();showLevelMaxSize();
        if (levelPos[0] == 0) compact();
        //        if(levelPos[0]==0){
        //          show();
        //          showLevelMaxSize();
```

### RuleId[ruleID=CommentedOutCode]
Commented out code (3 lines)
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/HeapLongKLLSketch.java`
#### Snippet
```java
    }
    this.N += another.N;
    //    System.out.println("[MERGE result]");
    //    show();
    //    System.out.println();
```

### RuleId[ruleID=CommentedOutCode]
Commented out code (2 lines)
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/SchemaRegionSchemaFileImpl.java`
#### Snippet
```java
  private boolean usingMLog = true;
  // the log file seriesPath
  //  private String logFilePath;
  //  private File logFile;
  private SchemaLogWriter<ISchemaRegionPlan> logWriter;
```

### RuleId[ruleID=CommentedOutCode]
Commented out code (13 lines)
in `example/ext-pipe-plugin-example/src/main/java/org/apache/iotdb/extpipe/ExtPipeSinkWriterImpl.java`
#### Snippet
```java
  }

  //  @Override
  //  public void createTimeSeries(String[] path, DataType dataType) {
  //    //== Here, handle create TimeSeries operation.
```

### RuleId[ruleID=CommentedOutCode]
Commented out code (2 lines)
in `example/ext-pipe-plugin-example/src/main/java/org/apache/iotdb/extpipe/ExtPipeSinkWriterImpl.java`
#### Snippet
```java
    Map<String, String> extendedFields = new HashMap<>();
    // extendedFields.put("AverageSpeed", Long.toString(extSession.getAvgSpeed());
    // extendedFields.put("Speed", Long.toString(extSession.getSpeed()));
    // extendedFields.put("SessionId", extSession == null ? "N/A" : extSession.getId());
    status.setExtendedFields(extendedFields);
```

### RuleId[ruleID=CommentedOutCode]
Commented out code (16 lines)
in `external-pipe-api/src/main/java/org/apache/iotdb/pipe/external/api/IExternalPipeSinkWriter.java`
#### Snippet
```java
  void insertText(String sgName, String[] path, long time, String value) throws IOException;

  //  /**
  //   * Insert a vector data point to the sink.
  //   *
```

### RuleId[ruleID=CommentedOutCode]
Commented out code (22 lines)
in `external-pipe-api/src/main/java/org/apache/iotdb/pipe/external/api/IExternalPipeSinkWriter.java`
#### Snippet
```java
  void delete(String sgName, String delPath, long startTime, long endTime) throws IOException;

  //  /**
  //   * Handle the creation of a timeseries.
  //   *
```

## RuleId[ruleID=CStyleArrayDeclaration]
### RuleId[ruleID=CStyleArrayDeclaration]
C-style array declaration of parameter `b`
in `server/src/main/java/org/apache/iotdb/db/qp/utils/EmptyOutputStream.java`
#### Snippet
```java

  @Override
  public void write(byte b[]) throws IOException {}
}

```

### RuleId[ruleID=CStyleArrayDeclaration]
C-style array declaration of parameter `b`
in `server/src/main/java/org/apache/iotdb/db/qp/utils/EmptyOutputStream.java`
#### Snippet
```java

  @Override
  public void write(byte b[], int off, int len) throws IOException {}

  @Override
```

### RuleId[ruleID=CStyleArrayDeclaration]
C-style array declaration of parameter `args`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/TestRPCClient.java`
#### Snippet
```java
  }

  public static void main(String args[]) {
    TestRPCClient client = new TestRPCClient();
    //    client.removeRegionPeer();
```

### RuleId[ruleID=CStyleArrayDeclaration]
C-style array declaration of parameter `cbuf`
in `server/src/main/java/org/apache/iotdb/db/engine/modification/utils/TracedBufferedReader.java`
#### Snippet
```java
  /** {@link BufferedReader#read(char[], int, int)} */
  @Override
  public int read(char cbuf[], int off, int len) throws IOException {
    synchronized (lock) {
      ensureOpen();
```

### RuleId[ruleID=CStyleArrayDeclaration]
C-style array declaration of field `cb`
in `server/src/main/java/org/apache/iotdb/db/engine/modification/utils/TracedBufferedReader.java`
#### Snippet
```java
  private Reader in;

  private char cb[];
  private int nChars, nextChar;

```

### RuleId[ruleID=CStyleArrayDeclaration]
C-style array declaration of local variable `ncb`
in `server/src/main/java/org/apache/iotdb/db/engine/modification/utils/TracedBufferedReader.java`
#### Snippet
```java
        } else {
          /* Reallocate buffer to accommodate read-ahead limit */
          char ncb[] = new char[readAheadLimit];
          System.arraycopy(cb, markedChar, ncb, 0, delta);
          cb = ncb;
```

### RuleId[ruleID=CStyleArrayDeclaration]
C-style array declaration of local variable `x`
in `library-udf/src/main/java/org/apache/iotdb/library/drepair/util/ScreenFill.java`
#### Snippet
```java
      }
    }
    double x[] = new double[2 * count];
    int temp_count = 0;
    for (int i = 1; i <= m; i++) {
```

### RuleId[ruleID=CStyleArrayDeclaration]
C-style array declaration of parameter `a`
in `library-udf/src/main/java/org/apache/iotdb/library/frequency/util/FFTUtil.java`
#### Snippet
```java
  }

  public void add(PointCollector collector, double a[], int i) throws Exception {
    double ans = 0;
    switch (result) {
```

### RuleId[ruleID=CStyleArrayDeclaration]
C-style array declaration of parameter `a`
in `library-udf/src/main/java/org/apache/iotdb/library/frequency/util/FFTUtil.java`
#### Snippet
```java
  }

  public void outputUncompressed(PointCollector collector, double a[]) throws Exception {
    int n = a.length / 2;
    for (int i = 0; i < n; i++) {
```

### RuleId[ruleID=CStyleArrayDeclaration]
C-style array declaration of parameter `a`
in `library-udf/src/main/java/org/apache/iotdb/library/frequency/util/FFTUtil.java`
#### Snippet
```java
  }

  public void outputCompressed(PointCollector collector, double a[]) throws Exception {
    int n = a.length / 2;
    // calculate total energy
```

### RuleId[ruleID=CStyleArrayDeclaration]
C-style array declaration of local variable `a`
in `library-udf/src/main/java/org/apache/iotdb/library/frequency/UDTFIFFT.java`
#### Snippet
```java
  public void terminate(PointCollector collector) throws Exception {
    int n = time.get(time.size() - 1) + 1;
    double a[] = new double[n * 2];
    for (int i = 0; i < time.size(); i++) {
      int k = time.get(i);
```

### RuleId[ruleID=CStyleArrayDeclaration]
C-style array declaration of field `MASKS`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BitReader.java`
#### Snippet
```java

  private static final int BITS_IN_A_BYTE = 8;
  private static final byte MASKS[] = {(byte) 0xff, 0x7f, 0x3f, 0x1f, 0x0f, 0x07, 0x03, 0x01};
  private final ByteBuffer buffer;
  private int bitCnt = BITS_IN_A_BYTE;
```

### RuleId[ruleID=CStyleArrayDeclaration]
C-style array declaration of field `data`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/decoder/FreqDecoder.java`
#### Snippet
```java
public class FreqDecoder extends Decoder {

  private double data[];

  private int readTotalCount = 0;
```

### RuleId[ruleID=CStyleArrayDeclaration]
C-style array declaration of local variable `value`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/decoder/FreqDecoder.java`
#### Snippet
```java
    long min = reader.next(bits);
    // Decode all values
    long value[] = new long[m];
    int symbol;
    for (int i = 0; i < m; i++) {
```

### RuleId[ruleID=CStyleArrayDeclaration]
C-style array declaration of parameter `a`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/encoder/FreqEncoder.java`
#### Snippet
```java
  }

  private ArrayList<Point> selectPoints(double a[]) {
    // Keep the components with priority queue in the descending order of energy
    double sum2 = 0;
```

## RuleId[ruleID=AssignmentToStaticFieldFromInstanceMethod]
### RuleId[ruleID=AssignmentToStaticFieldFromInstanceMethod]
Assignment to static field `sslContextBuilder` from instance context
in `example/rest-java-example/src/main/java/org/apache/iotdb/SSLClient.java`
#### Snippet
```java
  private SSLClient() {
    try {
      sslContextBuilder =
          new SSLContextBuilder()
              .loadTrustMaterial(
```

### RuleId[ruleID=AssignmentToStaticFieldFromInstanceMethod]
Assignment to static field `plainsf` from instance context
in `example/rest-java-example/src/main/java/org/apache/iotdb/SSLClient.java`
#### Snippet
```java
                    }
                  });
      plainsf = PlainConnectionSocketFactory.getSocketFactory();
      sslConnectionSocketFactory =
          new SSLConnectionSocketFactory(
```

### RuleId[ruleID=AssignmentToStaticFieldFromInstanceMethod]
Assignment to static field `sslConnectionSocketFactory` from instance context
in `example/rest-java-example/src/main/java/org/apache/iotdb/SSLClient.java`
#### Snippet
```java
                  });
      plainsf = PlainConnectionSocketFactory.getSocketFactory();
      sslConnectionSocketFactory =
          new SSLConnectionSocketFactory(
              sslContextBuilder.build(),
```

### RuleId[ruleID=AssignmentToStaticFieldFromInstanceMethod]
Assignment to static field `poolingHttpClientConnectionManager` from instance context
in `example/rest-java-example/src/main/java/org/apache/iotdb/SSLClient.java`
#### Snippet
```java
              .register("https", sslConnectionSocketFactory)
              .build();
      poolingHttpClientConnectionManager = new PoolingHttpClientConnectionManager(registryBuilder);
      poolingHttpClientConnectionManager.setMaxTotal(10);
    } catch (NoSuchAlgorithmException | KeyStoreException | KeyManagementException e) {
```

### RuleId[ruleID=AssignmentToStaticFieldFromInstanceMethod]
Assignment to static field `pool` from instance context
in `example/kafka/src/main/java/org/apache/iotdb/kafka/Consumer.java`
#### Snippet
```java
  private Consumer(List<KafkaConsumer<String, String>> consumerList) {
    this.consumerList = consumerList;
    pool =
        new SessionPool.Builder()
            .host(Constant.IOTDB_CONNECTION_HOST)
```

### RuleId[ruleID=AssignmentToStaticFieldFromInstanceMethod]
Assignment to static field `this.ascending` from instance context
in `server/src/main/java/org/apache/iotdb/db/query/executor/LastQueryExecutor.java`
#### Snippet
```java
    this.dataTypes = lastQueryPlan.getDeduplicatedDataTypes();
    this.expression = lastQueryPlan.getExpression();
    this.ascending = lastQueryPlan.isAscending();
  }

```

### RuleId[ruleID=AssignmentToStaticFieldFromInstanceMethod]
Assignment to static field `currentTaskNum` from instance context
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/CompactionTaskManager.java`
#### Snippet
```java
      init = true;
    }
    currentTaskNum = new AtomicInteger(0);
    init = true;
    logger.info("Compaction task manager started.");
```

### RuleId[ruleID=AssignmentToStaticFieldFromInstanceMethod]
Assignment to static field `currentTaskNum` from instance context
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/CompactionTaskManager.java`
#### Snippet
```java
            || config.isEnableCrossSpaceCompaction())) {
      initThreadPool();
      currentTaskNum = new AtomicInteger(0);
      candidateCompactionTaskQueue.regsitPollLastHook(
          AbstractCompactionTask::resetCompactionCandidateStatusForAllSourceFiles);
```

### RuleId[ruleID=AssignmentToStaticFieldFromInstanceMethod]
Assignment to static field `serviceProvider` from instance context
in `server/src/main/java/org/apache/iotdb/db/service/IoTDB.java`
#### Snippet
```java
  private void initServiceProvider() throws QueryProcessException {
    if (!config.isClusterMode()) {
      serviceProvider = new StandaloneServiceProvider();
    }
  }
```

### RuleId[ruleID=AssignmentToStaticFieldFromInstanceMethod]
Assignment to static field `serviceProvider` from instance context
in `server/src/main/java/org/apache/iotdb/db/service/DataNode.java`
#### Snippet
```java

  private void initServiceProvider() throws QueryProcessException {
    serviceProvider = new StandaloneServiceProvider();
  }

```

### RuleId[ruleID=AssignmentToStaticFieldFromInstanceMethod]
Assignment to static field `urlClassLoader` from instance context
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/RSchemaRegionLoader.java`
#### Snippet
```java
        }
      }
      urlClassLoader = new URLClassLoader(dependentJars.toArray(new URL[] {}));
    }
  }
```

### RuleId[ruleID=AssignmentToStaticFieldFromInstanceMethod]
Assignment to static field `server` from instance context
in `server/src/main/java/org/apache/iotdb/db/protocol/rest/RestService.java`
#### Snippet
```java

  private void startNonSSL(int port) {
    server = new Server(port);
    server.setHandler(constructServletContextHandler());
    serverStart();
```

### RuleId[ruleID=AssignmentToStaticFieldFromInstanceMethod]
Assignment to static field `server` from instance context
in `server/src/main/java/org/apache/iotdb/db/protocol/rest/RestService.java`
#### Snippet
```java
      int idleTime,
      boolean clientAuth) {
    server = new Server();

    HttpConfiguration httpsConfig = new HttpConfiguration();
```

### RuleId[ruleID=AssignmentToStaticFieldFromInstanceMethod]
Assignment to static field `intDic` from instance context
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/UDAFPercentile.java`
#### Snippet
```java
    switch (dataType) {
      case INT32:
        intDic = new HashMap<>();
        break;
      case INT64:
```

### RuleId[ruleID=AssignmentToStaticFieldFromInstanceMethod]
Assignment to static field `longDic` from instance context
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/UDAFPercentile.java`
#### Snippet
```java
        break;
      case INT64:
        longDic = new HashMap<>();
        break;
      case FLOAT:
```

### RuleId[ruleID=AssignmentToStaticFieldFromInstanceMethod]
Assignment to static field `floatDic` from instance context
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/UDAFPercentile.java`
#### Snippet
```java
        break;
      case FLOAT:
        floatDic = new HashMap<>();
        break;
      case DOUBLE:
```

### RuleId[ruleID=AssignmentToStaticFieldFromInstanceMethod]
Assignment to static field `doubleDic` from instance context
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/UDAFPercentile.java`
#### Snippet
```java
        break;
      case DOUBLE:
        doubleDic = new HashMap<>();
        break;
      default:
```

### RuleId[ruleID=AssignmentToStaticFieldFromInstanceMethod]
Assignment to static field `Config.rpcThriftCompressionEnable` from instance context
in `zeppelin-interpreter/src/main/java/org/apache/zeppelin/iotdb/IoTDBInterpreter.java`
#### Snippet
```java
          properties.getProperty(IOTDB_TIME_DISPLAY_TYPE, DEFAULT_TIME_DISPLAY_TYPE).trim();
      this.timeFormat = setTimeFormat(timeDisplayType);
      Config.rpcThriftCompressionEnable =
          "true"
              .equalsIgnoreCase(
```

### RuleId[ruleID=AssignmentToStaticFieldFromInstanceMethod]
Assignment to static field `session` from instance context
in `example/rocketmq/src/main/java/org/apache/iotdb/rocketmq/RocketMQConsumer.java`
#### Snippet
```java
    user = (user == null ? Constant.IOTDB_CONNECTION_USER : user);
    password = (password == null ? Constant.IOTDB_CONNECTION_PASSWORD : password);
    session = new Session(host, port, user, password);
    session.open();
    for (String storageGroup : Constant.STORAGE_GROUP) {
```

## RuleId[ruleID=RegExpRedundantEscape]
### RuleId[ruleID=RegExpRedundantEscape]
Redundant character escape `\\}` in RegExp
in `metrics/dropwizard-metrics/src/main/java/org/apache/iotdb/metrics/dropwizard/DropwizardMetricNameTool.java`
#### Snippet
```java
    String name = metricInfo.getName();
    Map<String, String> tags = metricInfo.getTags();
    return name.replaceAll("\\{|\\}", "")
        + "{"
        + tags.entrySet().stream()
```

### RuleId[ruleID=RegExpRedundantEscape]
Redundant character escape `\\}` in RegExp
in `metrics/dropwizard-metrics/src/main/java/org/apache/iotdb/metrics/dropwizard/DropwizardMetricNameTool.java`
#### Snippet
```java
                        + t.getValue().replace(TAG_SEPARATOR, ""))
            .collect(Collectors.joining(TAG_SEPARATOR))
            .replaceAll("\\{|\\}", "")
        + "}";
  }
```

## RuleId[ruleID=Java8ListReplaceAll]
### RuleId[ruleID=Java8ListReplaceAll]
The loop can be replaced with 'List.replaceAll'
in `server/src/main/java/org/apache/iotdb/db/conf/directories/DirectoryManager.java`
#### Snippet
```java
      List<String> sequenceFileFolders =
          new ArrayList<>(Arrays.asList(IoTDBDescriptor.getInstance().getConfig().getDataDirs()));
      for (int i = 0; i < sequenceFileFolders.size(); i++) {
        sequenceFileFolders.set(
            i, sequenceFileFolders.get(i) + File.separator + IoTDBConstant.SEQUENCE_FLODER_NAME);
```

### RuleId[ruleID=Java8ListReplaceAll]
The loop can be replaced with 'List.replaceAll'
in `server/src/main/java/org/apache/iotdb/db/conf/directories/DirectoryManager.java`
#### Snippet
```java
      List<String> unsequenceFileFolders =
          new ArrayList<>(Arrays.asList(IoTDBDescriptor.getInstance().getConfig().getDataDirs()));
      for (int i = 0; i < unsequenceFileFolders.size(); i++) {
        unsequenceFileFolders.set(
            i,
```

### RuleId[ruleID=Java8ListReplaceAll]
The loop can be replaced with 'List.replaceAll'
in `server/src/main/java/org/apache/iotdb/db/conf/directories/DirectoryManager.java`
#### Snippet
```java
    sequenceFileFolders =
        new ArrayList<>(Arrays.asList(IoTDBDescriptor.getInstance().getConfig().getDataDirs()));
    for (int i = 0; i < sequenceFileFolders.size(); i++) {
      sequenceFileFolders.set(
          i, sequenceFileFolders.get(i) + File.separator + IoTDBConstant.SEQUENCE_FLODER_NAME);
```

### RuleId[ruleID=Java8ListReplaceAll]
The loop can be replaced with 'List.replaceAll'
in `server/src/main/java/org/apache/iotdb/db/conf/directories/DirectoryManager.java`
#### Snippet
```java
    unsequenceFileFolders =
        new ArrayList<>(Arrays.asList(IoTDBDescriptor.getInstance().getConfig().getDataDirs()));
    for (int i = 0; i < unsequenceFileFolders.size(); i++) {
      unsequenceFileFolders.set(
          i, unsequenceFileFolders.get(i) + File.separator + IoTDBConstant.UNSEQUENCE_FLODER_NAME);
```

### RuleId[ruleID=Java8ListReplaceAll]
The loop can be replaced with 'List.replaceAll'
in `server/src/main/java/org/apache/iotdb/db/conf/directories/DirectoryManager.java`
#### Snippet
```java
    sequenceFileFolders =
        new ArrayList<>(Arrays.asList(IoTDBDescriptor.getInstance().getConfig().getDataDirs()));
    for (int i = 0; i < sequenceFileFolders.size(); i++) {
      sequenceFileFolders.set(
          i, sequenceFileFolders.get(i) + File.separator + IoTDBConstant.SEQUENCE_FLODER_NAME);
```

### RuleId[ruleID=Java8ListReplaceAll]
The loop can be replaced with 'List.replaceAll'
in `server/src/main/java/org/apache/iotdb/db/conf/directories/DirectoryManager.java`
#### Snippet
```java
    unsequenceFileFolders =
        new ArrayList<>(Arrays.asList(IoTDBDescriptor.getInstance().getConfig().getDataDirs()));
    for (int i = 0; i < unsequenceFileFolders.size(); i++) {
      unsequenceFileFolders.set(
          i, unsequenceFileFolders.get(i) + File.separator + IoTDBConstant.UNSEQUENCE_FLODER_NAME);
```

## RuleId[ruleID=KeySetIterationMayUseEntrySet]
### RuleId[ruleID=KeySetIterationMayUseEntrySet]
Iteration over `schemaMap.keySet()` may be replaced with 'entrySet()' iteration
in `cli/src/main/java/org/apache/iotdb/tool/ExportTsFile.java`
#### Snippet
```java
      }
      List<Tablet> tabletList = new ArrayList<>();
      for (String deviceId : schemaMap.keySet()) {
        List<MeasurementSchema> schemaList = schemaMap.get(deviceId);
        Tablet tablet = new Tablet(deviceId, schemaList);
```

### RuleId[ruleID=KeySetIterationMayUseEntrySet]
Iteration over `tags.keySet()` may be replaced with 'entrySet()' iteration
in `schema-engine-tag/src/main/java/org/apache/iotdb/db/metadata/tagSchemaRegion/utils/PathTagConverterUtils.java`
#### Snippet
```java
  public static String tagsToPath(String storageGroupFullPath, Map<String, String> tags) {
    StringBuilder stringBuilder = new StringBuilder(storageGroupFullPath);
    for (String tagKey : tags.keySet()) {
      stringBuilder.append(".").append(tagKey).append(".").append(tags.get(tagKey));
    }
```

### RuleId[ruleID=KeySetIterationMayUseEntrySet]
Iteration over `measurementMap.keySet()` may be replaced with 'entrySet()' iteration
in `schema-engine-tag/src/main/java/org/apache/iotdb/db/metadata/tagSchemaRegion/TagSchemaRegion.java`
#### Snippet
```java
      if (deviceEntry != null) {
        Map<String, SchemaEntry> measurementMap = deviceEntry.getMeasurementMap();
        for (String m : measurementMap.keySet()) {
          SchemaEntry schemaEntry = measurementMap.get(m);
          ShowTimeSeriesResults.add(
```

### RuleId[ruleID=KeySetIterationMayUseEntrySet]
Iteration over `measurementMap.keySet()` may be replaced with 'values()' iteration
in `schema-engine-tag/src/main/java/org/apache/iotdb/db/metadata/tagSchemaRegion/TagSchemaRegion.java`
#### Snippet
```java
        idTableWithDeviceIDList.getDeviceEntry(deviceID.toStringID()).getMeasurementMap();
    if (deviceID instanceof SHA256DeviceID) {
      for (String m : measurementMap.keySet()) {
        SchemaEntry schemaEntry = measurementMap.get(m);
        List<SchemaEntry> schemaEntries = new ArrayList<>();
```

### RuleId[ruleID=KeySetIterationMayUseEntrySet]
Iteration over `measurementMap.keySet()` may be replaced with 'entrySet()' iteration
in `schema-engine-tag/src/main/java/org/apache/iotdb/db/metadata/tagSchemaRegion/TagSchemaRegion.java`
#### Snippet
```java
      }
    } else {
      for (String m : measurementMap.keySet()) {
        SchemaEntry schemaEntry = measurementMap.get(m);
        ShowTimeSeriesResults.add(
```

### RuleId[ruleID=KeySetIterationMayUseEntrySet]
Iteration over `deviceAndMeasurementNames.keySet()` may be replaced with 'entrySet()' iteration
in `cli/src/main/java/org/apache/iotdb/tool/ImportCsv.java`
#### Snippet
```java
          boolean isFail = false;

          for (String deviceId : deviceAndMeasurementNames.keySet()) {
            ArrayList<TSDataType> types = new ArrayList<>();
            ArrayList<Object> values = new ArrayList<>();
```

### RuleId[ruleID=KeySetIterationMayUseEntrySet]
Iteration over `alignedPrefix.keySet()` may be replaced with 'entrySet()' iteration
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/parser/StatementGenerator.java`
#### Snippet
```java
    }

    for (String prefix : alignedPrefix.keySet()) {
      List<String> thisMeasurements = new ArrayList<>();
      List<TSDataType> thisDataTypes = new ArrayList<>();
```

### RuleId[ruleID=KeySetIterationMayUseEntrySet]
Iteration over `alignedPrefix.keySet()` may be replaced with 'entrySet()' iteration
in `server/src/main/java/org/apache/iotdb/db/qp/physical/sys/CreateTemplatePlan.java`
#### Snippet
```java
    }

    for (String prefix : alignedPrefix.keySet()) {
      List<String> thisMeasurements = new ArrayList<>();
      List<TSDataType> thisDataTypes = new ArrayList<>();
```

### RuleId[ruleID=KeySetIterationMayUseEntrySet]
Iteration over `deviceToSourceTargetPathPairListMap.keySet()` may be replaced with 'entrySet()' iteration
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/PlanGraphPrinter.java`
#### Snippet
```java
    Map<String, List<Pair<String, PartialPath>>> deviceToSourceTargetPathPairListMap =
        descriptor.getDeviceToSourceTargetPathPairListMap();
    for (String deviceName : deviceToSourceTargetPathPairListMap.keySet()) {
      boxValue.add(String.format("Device [%s]:", deviceName));
      drawSourceTargetPath(
```

### RuleId[ruleID=KeySetIterationMayUseEntrySet]
Iteration over `caseChangeMap.keySet()` may be replaced with 'entrySet()' iteration
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/AnalyzeVisitor.java`
#### Snippet
```java
      caseChangeMap.put(key.toLowerCase(Locale.ROOT), key);
    }
    for (String lowerCaseKey : caseChangeMap.keySet()) {
      if (!ALLOWED_SCHEMA_PROPS.contains(lowerCaseKey)) {
        throw new SemanticException(
```

### RuleId[ruleID=KeySetIterationMayUseEntrySet]
Iteration over `groupByLevelExpressions.keySet()` may be replaced with 'entrySet()' iteration
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/AnalyzeVisitor.java`
#### Snippet
```java
  private void checkDataTypeConsistencyInGroupByLevel(
      Analysis analysis, Map<Expression, Set<Expression>> groupByLevelExpressions) {
    for (Expression groupedAggregationExpression : groupByLevelExpressions.keySet()) {
      TSDataType checkedDataType = analysis.getType(groupedAggregationExpression);
      for (Expression rawAggregationExpression :
```

### RuleId[ruleID=KeySetIterationMayUseEntrySet]
Iteration over `deviceToSourceTransformExpressions.keySet()` may be replaced with 'entrySet()' iteration
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/AnalyzeVisitor.java`
#### Snippet
```java
    Map<String, Set<Expression>> deviceToSourceTransformExpressions =
        analysis.getDeviceToSourceTransformExpressions();
    for (String deviceName : deviceToSourceTransformExpressions.keySet()) {
      Set<Expression> sourceTransformExpressions =
          deviceToSourceTransformExpressions.get(deviceName);
```

### RuleId[ruleID=KeySetIterationMayUseEntrySet]
Iteration over `deviceToWhereExpression.keySet()` may be replaced with 'entrySet()' iteration
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/AnalyzeVisitor.java`
#### Snippet
```java
    if (queryStatement.hasWhere()) {
      Map<String, Expression> deviceToWhereExpression = analysis.getDeviceToWhereExpression();
      for (String deviceName : deviceToWhereExpression.keySet()) {
        Expression whereExpression = deviceToWhereExpression.get(deviceName);
        deviceToSourceExpressions
```

### RuleId[ruleID=KeySetIterationMayUseEntrySet]
Iteration over `deviceToOutputExpressions.keySet()` may be replaced with 'entrySet()' iteration
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/AnalyzeVisitor.java`
#### Snippet
```java
            ? analysis.getDeviceToAggregationExpressions()
            : analysis.getDeviceToSourceTransformExpressions();
    for (String deviceName : deviceToOutputExpressions.keySet()) {
      Set<Expression> outputExpressionsUnderDevice = deviceToOutputExpressions.get(deviceName);
      Set<String> outputColumns = new LinkedHashSet<>();
```

### RuleId[ruleID=KeySetIterationMayUseEntrySet]
Iteration over `deviceToOutputColumnsMap.keySet()` may be replaced with 'entrySet()' iteration
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/AnalyzeVisitor.java`
#### Snippet
```java

    Map<String, List<Integer>> deviceViewInputIndexesMap = new HashMap<>();
    for (String deviceName : deviceToOutputColumnsMap.keySet()) {
      List<String> outputsUnderDevice = new ArrayList<>(deviceToOutputColumnsMap.get(deviceName));
      List<Integer> indexes = new ArrayList<>();
```

### RuleId[ruleID=KeySetIterationMayUseEntrySet]
Iteration over `deviceToSelectExpressions.keySet()` may be replaced with 'entrySet()' iteration
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/AnalyzeVisitor.java`
#### Snippet
```java
    Map<String, Set<Expression>> deviceToSelectExpressions =
        analysis.getDeviceToSelectExpressions();
    for (String deviceName : deviceToSelectExpressions.keySet()) {
      Set<Expression> selectExpressions = deviceToSelectExpressions.get(deviceName);
      Set<Expression> aggregationExpressions = new LinkedHashSet<>();
```

### RuleId[ruleID=KeySetIterationMayUseEntrySet]
Iteration over `deviceToAggregationExpressions.keySet()` may be replaced with 'entrySet()' iteration
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/AnalyzeVisitor.java`
#### Snippet
```java
      Map<String, Set<Expression>> deviceToAggregationExpressions =
          analysis.getDeviceToAggregationExpressions();
      for (String deviceName : deviceToAggregationExpressions.keySet()) {
        Set<Expression> aggregationExpressions = deviceToAggregationExpressions.get(deviceName);
        Set<Expression> sourceTransformExpressions = new LinkedHashSet<>();
```

### RuleId[ruleID=KeySetIterationMayUseEntrySet]
Iteration over `measurementToDeviceSelectExpressions.keySet()` may be replaced with 'entrySet()' iteration
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/AnalyzeVisitor.java`
#### Snippet
```java
      }

      for (Expression measurementExpression : measurementToDeviceSelectExpressions.keySet()) {
        if (paginationController.hasCurOffset()) {
          paginationController.consumeOffset();
```

### RuleId[ruleID=KeySetIterationMayUseEntrySet]
Iteration over `deviceToSelectExpressionsOfOneMeasurement.keySet()` may be replaced with 'entrySet()' iteration
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/AnalyzeVisitor.java`
#### Snippet
```java

          // add deviceToSelectExpressions
          for (String deviceName : deviceToSelectExpressionsOfOneMeasurement.keySet()) {
            Expression expression = deviceToSelectExpressionsOfOneMeasurement.get(deviceName);
            Expression expressionWithoutAlias =
```

### RuleId[ruleID=KeySetIterationMayUseEntrySet]
Iteration over `groupByLevelExpressions.keySet()` may be replaced with 'entrySet()' iteration
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/LogicalPlanBuilder.java`
#### Snippet
```java
      Ordering scanOrder) {
    List<CrossSeriesAggregationDescriptor> groupByLevelDescriptors = new ArrayList<>();
    for (Expression groupedExpression : groupByLevelExpressions.keySet()) {
      groupByLevelDescriptors.add(
          new CrossSeriesAggregationDescriptor(
```

### RuleId[ruleID=KeySetIterationMayUseEntrySet]
Iteration over `tagValuesToGroupedTimeseriesOperands.keySet()` may be replaced with 'entrySet()' iteration
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/LogicalPlanBuilder.java`
#### Snippet
```java
    Map<List<String>, List<CrossSeriesAggregationDescriptor>> tagValuesToAggregationDescriptors =
        new HashMap<>();
    for (List<String> tagValues : tagValuesToGroupedTimeseriesOperands.keySet()) {
      LinkedHashMap<Expression, List<Expression>> groupedTimeseriesOperands =
          tagValuesToGroupedTimeseriesOperands.get(tagValues);
```

### RuleId[ruleID=KeySetIterationMayUseEntrySet]
Iteration over `targetPathToSourceInputLocationMap.keySet()` may be replaced with 'entrySet()' iteration
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/AbstractIntoOperator.java`
#### Snippet
```java
    List<InsertTabletStatementGenerator> insertTabletStatementGenerators =
        new ArrayList<>(targetPathToSourceInputLocationMap.size());
    for (PartialPath targetDevice : targetPathToSourceInputLocationMap.keySet()) {
      InsertTabletStatementGenerator generator =
          new InsertTabletStatementGenerator(
```

### RuleId[ruleID=KeySetIterationMayUseEntrySet]
Iteration over `pathToAggrIndexesMap.keySet()` may be replaced with 'entrySet()' iteration
in `server/src/main/java/org/apache/iotdb/db/query/dataset/groupby/GroupByWithValueFilterDataSet.java`
#### Snippet
```java

    // init non-aligned series reader
    for (PartialPath path : pathToAggrIndexesMap.keySet()) {
      IReaderByTimestamp seriesReaderByTimestamp = getReaderByTime(path, groupByTimePlan, context);
      readerToAggrIndexesMap.put(
```

### RuleId[ruleID=KeySetIterationMayUseEntrySet]
Iteration over `alignedPathToAggrIndexesMap.keySet()` may be replaced with 'entrySet()' iteration
in `server/src/main/java/org/apache/iotdb/db/query/dataset/groupby/GroupByWithValueFilterDataSet.java`
#### Snippet
```java
    pathToAggrIndexesMap = null;
    // init aligned series reader
    for (PartialPath alignedPath : alignedPathToAggrIndexesMap.keySet()) {
      IReaderByTimestamp seriesReaderByTimestamp =
          getReaderByTime(alignedPath, groupByTimePlan, context);
```

### RuleId[ruleID=KeySetIterationMayUseEntrySet]
Iteration over `pathToAggrIndexesMap.keySet()` may be replaced with 'entrySet()' iteration
in `server/src/main/java/org/apache/iotdb/db/query/executor/AggregationExecutor.java`
#### Snippet
```java
    }

    for (PartialPath path : pathToAggrIndexesMap.keySet()) {
      IReaderByTimestamp seriesReaderByTimestamp =
          getReaderByTime(path, queryPlan, path.getSeriesType(), context);
```

### RuleId[ruleID=KeySetIterationMayUseEntrySet]
Iteration over `alignedPathToAggrIndexesMap.keySet()` may be replaced with 'entrySet()' iteration
in `server/src/main/java/org/apache/iotdb/db/query/executor/AggregationExecutor.java`
#### Snippet
```java
    // assign null to be friendly for GC
    pathToAggrIndexesMap = null;
    for (AlignedPath vectorPath : alignedPathToAggrIndexesMap.keySet()) {
      IReaderByTimestamp seriesReaderByTimestamp =
          getReaderByTime(vectorPath, queryPlan, vectorPath.getSeriesType(), context);
```

### RuleId[ruleID=KeySetIterationMayUseEntrySet]
Iteration over `deviceIteratorMap.keySet()` may be replaced with 'entrySet()' iteration
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/inner/utils/MultiTsFileDeviceIterator.java`
#### Snippet
```java
    List<TsFileResource> toBeRemovedResources = new LinkedList<>();
    Pair<String, Boolean> minDevice = null;
    for (TsFileResource resource : deviceIteratorMap.keySet()) {
      TsFileDeviceIterator deviceIterator = deviceIteratorMap.get(resource);
      if (deviceIterator.current() == null || deviceIterator.current().equals(currentDevice)) {
```

### RuleId[ruleID=KeySetIterationMayUseEntrySet]
Iteration over `pathToChunkMetadataListMap.keySet()` may be replaced with 'entrySet()' iteration
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileResource.java`
#### Snippet
```java

  private void generatePathToTimeSeriesMetadataMap() throws IOException {
    for (PartialPath path : pathToChunkMetadataListMap.keySet()) {
      pathToTimeSeriesMetadataMap.put(
          path,
```

### RuleId[ruleID=KeySetIterationMayUseEntrySet]
Iteration over `nodeCache.keySet()` may be replaced with 'entrySet()' iteration
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/cache/PlainCacheManager.java`
#### Snippet
```java
  @Override
  protected IMNode getPotentialNodeTobeEvicted() {
    for (CacheEntry cacheEntry : nodeCache.keySet()) {
      if (!cacheEntry.isPinned()) {
        return nodeCache.get(cacheEntry);
```

### RuleId[ruleID=KeySetIterationMayUseEntrySet]
Iteration over `pathToAggregations.keySet()` may be replaced with 'entrySet()' iteration
in `server/src/main/java/org/apache/iotdb/db/metadata/utils/MetaUtils.java`
#### Snippet
```java
    Map<PartialPath, List<AggregationDescriptor>> result = new HashMap<>();
    Map<String, List<MeasurementPath>> deviceToAlignedPathsMap = new HashMap<>();
    for (PartialPath path : pathToAggregations.keySet()) {
      MeasurementPath measurementPath = (MeasurementPath) path;
      if (!measurementPath.isUnderAlignedEntity()) {
```

### RuleId[ruleID=KeySetIterationMayUseEntrySet]
Iteration over `pathSetTemplateMap.keySet()` may be replaced with 'entrySet()' iteration
in `server/src/main/java/org/apache/iotdb/db/metadata/template/ClusterTemplateManager.java`
#### Snippet
```java
    readWriteLock.readLock().lock();
    try {
      for (PartialPath templateSetPath : pathSetTemplateMap.keySet()) {
        if (path.startsWith(templateSetPath.getNodes())) {
          return new Pair<>(
```

### RuleId[ruleID=KeySetIterationMayUseEntrySet]
Iteration over `map.keySet()` may be replaced with 'entrySet()' iteration
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/handler/NewQueryHandler.java`
#### Snippet
```java
              NewInfluxDBServiceImpl.executeStatement(functionSql, sessionid);
          Map<String, Object> map = QueryResultUtils.getColumnNameAndValue(tsExecuteStatementResp);
          for (String colume : map.keySet()) {
            Object o = map.get(colume);
            String fullPath = colume.substring(functionName.length() + 1, colume.length() - 1);
```

### RuleId[ruleID=KeySetIterationMayUseEntrySet]
Iteration over `tagsMap.keySet()` may be replaced with 'entrySet()' iteration
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/meta/TagInfluxDBMetaManager.java`
#### Snippet
```java
    tagsMap.put("measurement", measurement);
    StringBuilder devicePath = new StringBuilder("root." + database);
    for (String tagKey : tagsMap.keySet()) {
      devicePath.append(".").append(tagKey).append(".").append(tagsMap.get(tagKey));
    }
```

### RuleId[ruleID=KeySetIterationMayUseEntrySet]
Iteration over `recoveredLocalSchemaRegionInfo.keySet()` may be replaced with 'entrySet()' iteration
in `server/src/main/java/org/apache/iotdb/db/localconfignode/LocalSchemaPartitionTable.java`
#### Snippet
```java
    table = new ConcurrentHashMap<>();
    schemaRegionIdGenerator = new AtomicInteger(0);
    for (PartialPath storageGroup : recoveredLocalSchemaRegionInfo.keySet()) {
      List<SchemaRegionId> schemaRegionIdList = new CopyOnWriteArrayList<>();
      table.put(storageGroup, schemaRegionIdList);
```

### RuleId[ruleID=KeySetIterationMayUseEntrySet]
Iteration over `dnlToSgMap .keySet()` may be replaced with 'Map.forEach()' iteration
in `confignode/src/main/java/org/apache/iotdb/confignode/manager/ClusterSchemaManager.java`
#### Snippet
```java
    AsyncClientHandler<TSetTTLReq, TSStatus> clientHandler =
        new AsyncClientHandler<>(DataNodeRequestType.SET_TTL);
    dnlToSgMap
        .keySet()
        .forEach(
            dataNodeId -> {
```

### RuleId[ruleID=KeySetIterationMayUseEntrySet]
Iteration over `regionReplicaSetMap.keySet()` may be replaced with 'entrySet()' iteration
in `confignode/src/main/java/org/apache/iotdb/confignode/manager/load/balancer/router/leader/GreedyLeaderBalancer.java`
#### Snippet
```java

    /* Ensure all RegionGroups' leader are not inside disabled DataNodes */
    for (TConsensusGroupId regionGroupId : regionReplicaSetMap.keySet()) {
      int leaderId = regionLeaderMap.get(regionGroupId);
      if (disabledDataNodeSet.contains(leaderId)) {
```

### RuleId[ruleID=KeySetIterationMayUseEntrySet]
Iteration over `regionReplicaSetMap.keySet()` may be replaced with 'entrySet()' iteration
in `confignode/src/main/java/org/apache/iotdb/confignode/manager/load/balancer/router/leader/GreedyLeaderBalancer.java`
#### Snippet
```java
    /* Double keyword sort */
    List<WeightEntry> weightList = new ArrayList<>();
    for (TConsensusGroupId regionGroupId : regionReplicaSetMap.keySet()) {
      int leaderId = regionLeaderMap.get(regionGroupId);
      int leaderWeight = leaderCounter.get(regionLeaderMap.get(regionGroupId)).get();
```

### RuleId[ruleID=KeySetIterationMayUseEntrySet]
Iteration over `chunkMetadataMap.keySet()` may be replaced with 'entrySet()' iteration
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/writer/TsFileIOWriter.java`
#### Snippet
```java
      }
    }
    for (String device : chunkMetadataMap.keySet()) {
      Map<String, List<IChunkMetadata>> seriesToChunkMetadataMap = chunkMetadataMap.get(device);
      for (Map.Entry<String, List<IChunkMetadata>> entry : seriesToChunkMetadataMap.entrySet()) {
```

## RuleId[ruleID=ObjectNotify]
### RuleId[ruleID=ObjectNotify]
`notify` should probably be replaced with 'notifyAll()'
in `server/src/main/java/org/apache/iotdb/db/sync/transport/client/SenderManager.java`
#### Snippet
```java
        logger.info("Reconnect to {} successfully.", pipeSink);
        synchronized (object) {
          object.notify();
        }
        while (!blockingQueue.isEmpty()) {
```

### RuleId[ruleID=ObjectNotify]
`notify` should probably be replaced with 'notifyAll()'
in `server/src/main/java/org/apache/iotdb/db/sync/transport/client/SenderManager.java`
#### Snippet
```java
          object = blockingQueue.take();
          synchronized (object) {
            object.notify();
          }
        }
```

### RuleId[ruleID=ObjectNotify]
`notify` should probably be replaced with 'notifyAll()'
in `session/src/main/java/org/apache/iotdb/session/pool/SessionPool.java`
#### Snippet
```java
        }
        queue.push(session);
        this.notify();
      }
    } catch (IoTDBConnectionException e) {
```

### RuleId[ruleID=ObjectNotify]
`notify` should probably be replaced with 'notifyAll()'
in `session/src/main/java/org/apache/iotdb/session/pool/SessionPool.java`
#### Snippet
```java
        size--;
        // we do not need to notifyAll as any waited thread can continue to work after waked up.
        this.notify();
        if (logger.isDebugEnabled()) {
          logger.debug("open session failed, reduce the count and notify others...");
```

### RuleId[ruleID=ObjectNotify]
`notify` should probably be replaced with 'notifyAll()'
in `session/src/main/java/org/apache/iotdb/session/pool/SessionPool.java`
#### Snippet
```java
          size--;
          // we do not need to notifyAll as any waited thread can continue to work after waked up.
          this.notify();
          if (logger.isDebugEnabled()) {
            logger.debug("open session failed, reduce the count and notify others...");
```

### RuleId[ruleID=ObjectNotify]
`notify` should probably be replaced with 'notifyAll()'
in `session/src/main/java/org/apache/iotdb/session/pool/SessionPool.java`
#### Snippet
```java
    synchronized (this) {
      // we do not need to notifyAll as any waited thread can continue to work after waked up.
      this.notify();
      // comment the following codes as putBack is too frequently called.
      //      if (logger.isTraceEnabled()) {
```

### RuleId[ruleID=ObjectNotify]
`notify` should probably be replaced with 'notifyAll()'
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/impl/statemachine/RegionMigrateProcedure.java`
#### Snippet
```java
        migrateResult = migrateStatus.toString();
      }
      regionMigrateLock.notify();
    }
  }
```

## RuleId[ruleID=FinalPrivateMethod]
### RuleId[ruleID=FinalPrivateMethod]
'private' method declared `final`
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
  }

  private final char getSuccessor(char c, int n) {
    return ((c == 'y') && (n == 2))
        ? 'X'
```

### RuleId[ruleID=FinalPrivateMethod]
'private' method declared `final`
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
  }

  private final String getDateTimePattern(String dt, boolean toTime) throws Exception {
    //
    // Special case
```

## RuleId[ruleID=SizeReplaceableByIsEmpty]
### RuleId[ruleID=SizeReplaceableByIsEmpty]
`Wi.size() == 0` can be replaced with 'Wi.isEmpty()'
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/utils/MasterRepairUtil.java`
#### Snippet
```java
  public ArrayList<ArrayList<Double>> calC(int i, ArrayList<Integer> Wi) {
    ArrayList<ArrayList<Double>> Ci = new ArrayList<>();
    if (Wi.size() == 0) {
      Ci.add(this.kdTreeUtil.query(this.td.get(i), std));
    } else {
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`((String) target).length() > 0` can be replaced with '!((String) target).isEmpty()'
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/builtin/String/UDTFStrLocate.java`
#### Snippet
```java
        .validateInputSeriesDataType(0, Type.TEXT)
        .validate(
            target -> ((String) target).length() > 0,
            "target should not be empty",
            validator.getParameters().getString("target"));
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`stack.size() != 0` can be replaced with '!stack.isEmpty()'
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/utils/KDTreeUtil.java`
#### Snippet
```java
    Node node;
    double tdis;
    while (stack.size() != 0) {
      node = stack.pop();
      if (node.isLeaf) {
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`kNearest.size() == 0` can be replaced with 'kNearest.isEmpty()'
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/utils/KDTreeUtil.java`
#### Snippet
```java
      kNearest.add(findNearest(input, nearest, std));
    }
    if (kNearest.size() == 0) {
      kNearest.add(node.value);
    }
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`data.size() == 0` can be replaced with 'data.isEmpty()'
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/utils/KDTreeUtil.java`
#### Snippet
```java

  private void buildDetail(Node node, ArrayList<ArrayList<Double>> data, int dimensions) {
    if (data.size() == 0) {
      return;
    }
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`left.size() == 0` can be replaced with 'left.isEmpty()'
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/utils/KDTreeUtil.java`
#### Snippet
```java
    for (ArrayList<Double> d : data) {
      if (d.get(node.partitionDimension) == node.partitionValue) {
        if (left.size() == 0) {
          left.add(d);
        } else {
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`stack.size() != 0` can be replaced with '!stack.isEmpty()'
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/utils/KDTreeUtil.java`
#### Snippet
```java
    Node node;
    double tdis;
    while (stack.size() != 0) {
      node = stack.pop();
      if (node.isLeaf) {
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`homeDir.length() > 0` can be replaced with '!homeDir.isEmpty()'
in `node-commons/src/main/java/org/apache/iotdb/commons/conf/CommonConfig.java`
#### Snippet
```java

  private String addHomeDir(String dir, String homeDir) {
    if (!new File(dir).isAbsolute() && homeDir != null && homeDir.length() > 0) {
      if (!homeDir.endsWith(File.separator)) {
        dir = homeDir + File.separatorChar + dir;
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`stack.size() != 0` can be replaced with '!stack.isEmpty()'
in `session/src/main/java/org/apache/iotdb/session/template/Template.java`
#### Snippet
```java
    }

    while (stack.size() != 0) {
      Pair<String, TemplateNode> cur = stack.pop();
      String prefix = cur.left;
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`sgColumns.size() > 0` can be replaced with '!sgColumns.isEmpty()'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBNonAlignJDBCResultSet.java`
#### Snippet
```java
    for (int i = 0; i < columnNameList.size(); i++) {
      String name = "";
      if (sgColumns != null && sgColumns.size() > 0) {
        name = sgColumns.get(i) + "." + columnNameList.get(i);
        newSgColumns.add(sgColumns.get(i));
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`catalog.length() > 0` can be replaced with '!catalog.isEmpty()'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java

    String sql = "SHOW timeseries";
    if (catalog != null && catalog.length() > 0) {
      if (catalog.contains("%")) {
        catalog = catalog.replace("%", "*");
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`schemaPattern.length() > 0` can be replaced with '!schemaPattern.isEmpty()'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      }
      sql = sql + " " + catalog;
    } else if (schemaPattern != null && schemaPattern.length() > 0) {
      if (schemaPattern.contains("%")) {
        schemaPattern = schemaPattern.replace("%", "*");
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`catalog.length() > 0` can be replaced with '!catalog.isEmpty()'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      sql = sql + " " + schemaPattern;
    }
    if (((catalog != null && catalog.length() > 0)
            || schemaPattern != null && schemaPattern.length() > 0)
        && tableNamePattern != null
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`schemaPattern.length() > 0` can be replaced with '!schemaPattern.isEmpty()'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
    }
    if (((catalog != null && catalog.length() > 0)
            || schemaPattern != null && schemaPattern.length() > 0)
        && tableNamePattern != null
        && tableNamePattern.length() > 0) {
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`tableNamePattern.length() > 0` can be replaced with '!tableNamePattern.isEmpty()'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
            || schemaPattern != null && schemaPattern.length() > 0)
        && tableNamePattern != null
        && tableNamePattern.length() > 0) {
      if (tableNamePattern.contains("%")) {
        tableNamePattern = tableNamePattern.replace("%", "*");
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`result.length() > 0` can be replaced with '!result.isEmpty()'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      }
      result = str.toString();
      if (result.length() > 0) {
        result = result.substring(0, result.length() - 1);
      }
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`result.length() > 0` can be replaced with '!result.isEmpty()'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      }
      result = str.toString();
      if (result.length() > 0) {
        result = result.substring(0, result.length() - 1);
      }
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`catalog.length() > 0` can be replaced with '!catalog.isEmpty()'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java

    String sql = "SHOW DATABASES";
    if (catalog != null && catalog.length() > 0) {
      if (catalog.contains("%")) {
        catalog = catalog.replace("%", "*");
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`schemaPattern.length() > 0` can be replaced with '!schemaPattern.isEmpty()'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      }
      sql = sql + " " + catalog;
    } else if (schemaPattern != null && schemaPattern.length() > 0) {
      if (schemaPattern.contains("%")) {
        schemaPattern = schemaPattern.replace("%", "*");
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`catalog.length() > 0` can be replaced with '!catalog.isEmpty()'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      sql = sql + " " + schemaPattern;
    }
    if (((catalog != null && catalog.length() > 0)
            || schemaPattern != null && schemaPattern.length() > 0)
        && tableNamePattern != null
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`schemaPattern.length() > 0` can be replaced with '!schemaPattern.isEmpty()'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
    }
    if (((catalog != null && catalog.length() > 0)
            || schemaPattern != null && schemaPattern.length() > 0)
        && tableNamePattern != null
        && tableNamePattern.length() > 0) {
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`tableNamePattern.length() > 0` can be replaced with '!tableNamePattern.isEmpty()'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
            || schemaPattern != null && schemaPattern.length() > 0)
        && tableNamePattern != null
        && tableNamePattern.length() > 0) {
      if (tableNamePattern.contains("%")) {
        tableNamePattern = tableNamePattern.replace("%", "*");
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`catalog.length() > 0` can be replaced with '!catalog.isEmpty()'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
    }

    if (((catalog != null && catalog.length() > 0)
            || schemaPattern != null && schemaPattern.length() > 0)
        && tableNamePattern != null
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`schemaPattern.length() > 0` can be replaced with '!schemaPattern.isEmpty()'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java

    if (((catalog != null && catalog.length() > 0)
            || schemaPattern != null && schemaPattern.length() > 0)
        && tableNamePattern != null
        && tableNamePattern.length() > 0
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`tableNamePattern.length() > 0` can be replaced with '!tableNamePattern.isEmpty()'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
            || schemaPattern != null && schemaPattern.length() > 0)
        && tableNamePattern != null
        && tableNamePattern.length() > 0
        && columnNamePattern != null
        && columnNamePattern.length() > 0) {
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`columnNamePattern.length() > 0` can be replaced with '!columnNamePattern.isEmpty()'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
        && tableNamePattern.length() > 0
        && columnNamePattern != null
        && columnNamePattern.length() > 0) {
      sql = sql + "." + columnNamePattern;
    }
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`catalog.length() > 0` can be replaced with '!catalog.isEmpty()'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java

    String sql = "SHOW DATABASES";
    if (catalog != null && catalog.length() > 0) {
      if (catalog.contains("%")) {
        catalog = catalog.replace("%", "*");
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`schemaPattern.length() > 0` can be replaced with '!schemaPattern.isEmpty()'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      }
      sql = sql + " " + catalog;
    } else if (schemaPattern != null && schemaPattern.length() > 0) {
      if (schemaPattern.contains("%")) {
        schemaPattern = schemaPattern.replace("%", "*");
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`catalog.length() > 0` can be replaced with '!catalog.isEmpty()'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      sql = sql + " " + schemaPattern;
    }
    if (((catalog != null && catalog.length() > 0)
            || schemaPattern != null && schemaPattern.length() > 0)
        && tableNamePattern != null
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`schemaPattern.length() > 0` can be replaced with '!schemaPattern.isEmpty()'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
    }
    if (((catalog != null && catalog.length() > 0)
            || schemaPattern != null && schemaPattern.length() > 0)
        && tableNamePattern != null
        && tableNamePattern.length() > 0) {
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`tableNamePattern.length() > 0` can be replaced with '!tableNamePattern.isEmpty()'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
            || schemaPattern != null && schemaPattern.length() > 0)
        && tableNamePattern != null
        && tableNamePattern.length() > 0) {
      if (tableNamePattern.contains("%")) {
        tableNamePattern = tableNamePattern.replace("%", "*");
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`catalog.length() > 0` can be replaced with '!catalog.isEmpty()'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java

    String sql = "SHOW DATABASES";
    if (catalog != null && catalog.length() > 0) {
      if (catalog.contains("%")) {
        catalog = catalog.replace("%", "*");
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`schemaPattern.length() > 0` can be replaced with '!schemaPattern.isEmpty()'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      }
      sql = sql + " " + catalog;
    } else if (schemaPattern != null && schemaPattern.length() > 0) {
      if (schemaPattern.contains("%")) {
        schemaPattern = schemaPattern.replace("%", "*");
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`catalog.length() > 0` can be replaced with '!catalog.isEmpty()'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      sql = sql + " " + schemaPattern;
    }
    if (((catalog != null && catalog.length() > 0)
            || schemaPattern != null && schemaPattern.length() > 0)
        && tableNamePattern != null
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`schemaPattern.length() > 0` can be replaced with '!schemaPattern.isEmpty()'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
    }
    if (((catalog != null && catalog.length() > 0)
            || schemaPattern != null && schemaPattern.length() > 0)
        && tableNamePattern != null
        && tableNamePattern.length() > 0) {
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`tableNamePattern.length() > 0` can be replaced with '!tableNamePattern.isEmpty()'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
            || schemaPattern != null && schemaPattern.length() > 0)
        && tableNamePattern != null
        && tableNamePattern.length() > 0) {
      if (tableNamePattern.contains("%")) {
        tableNamePattern = tableNamePattern.replace("%", "*");
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`catalog.length() > 0` can be replaced with '!catalog.isEmpty()'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
    }

    if (((catalog != null && catalog.length() > 0)
            || schemaPattern != null && schemaPattern.length() > 0)
        && tableNamePattern != null
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`schemaPattern.length() > 0` can be replaced with '!schemaPattern.isEmpty()'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java

    if (((catalog != null && catalog.length() > 0)
            || schemaPattern != null && schemaPattern.length() > 0)
        && tableNamePattern != null
        && tableNamePattern.length() > 0
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`tableNamePattern.length() > 0` can be replaced with '!tableNamePattern.isEmpty()'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
            || schemaPattern != null && schemaPattern.length() > 0)
        && tableNamePattern != null
        && tableNamePattern.length() > 0
        && columnNamePattern != null
        && columnNamePattern.length() > 0) {
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`columnNamePattern.length() > 0` can be replaced with '!columnNamePattern.isEmpty()'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
        && tableNamePattern.length() > 0
        && columnNamePattern != null
        && columnNamePattern.length() > 0) {
      sql = sql + "." + columnNamePattern;
    }
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`levelProcessorClassNames.size() > 0` can be replaced with '!levelProcessorClassNames.isEmpty()'
in `schema-engine-tag/src/main/java/org/apache/iotdb/lsm/engine/LSMEngineBuilder.java`
#### Snippet
```java
    LevelProcessorChain<T, R, C> levelProcessChain = new LevelProcessorChain<>();
    try {
      if (levelProcessorClassNames.size() > 0) {
        ILevelProcessor iLevelProcess =
            levelProcessChain.nextLevel(generateLevelProcessor(levelProcessorClassNames.get(0)));
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`ids.size() > 0` can be replaced with '!ids.isEmpty()'
in `schema-engine-tag/src/main/java/org/apache/iotdb/db/metadata/tagSchemaRegion/TagSchemaRegion.java`
#### Snippet
```java
        List<IDeviceID> IDS = new LinkedList<>();
        List<Integer> ids = getDeviceIDsFromInvertedIndex(devicePath);
        if (ids.size() > 0) {
          for (int id : ids) {
            IDS.add(idTableWithDeviceIDList.get(id));
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`plan.getMeasurements().size() == 0` can be replaced with 'plan.getMeasurements().isEmpty()'
in `schema-engine-tag/src/main/java/org/apache/iotdb/db/metadata/tagSchemaRegion/TagSchemaRegion.java`
#### Snippet
```java
      } else {
        filterExistingMeasurements(plan, deviceEntry.getMeasurementMap().keySet());
        if (plan.getMeasurements().size() == 0)
          throw new PathAlreadyExistException(devicePath.getFullPath());
      }
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`sgList.size() > 0` can be replaced with '!sgList.isEmpty()'
in `service-rpc/src/main/java/org/apache/iotdb/rpc/IoTDBRpcDataSet.java`
#### Snippet
```java
        String name;
        if (sgList != null
            && sgList.size() > 0
            && (aliasColumnMap == null || !aliasColumnMap.get(i))) {
          name = sgList.get(i) + "." + columnNameList.get(i);
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`sgList.size() > 0` can be replaced with '!sgList.isEmpty()'
in `service-rpc/src/main/java/org/apache/iotdb/rpc/IoTDBJDBCDataSet.java`
#### Snippet
```java
        String name = "";
        if (sgList != null
            && sgList.size() > 0
            && (aliasColumnMap == null || !aliasColumnMap.get(i))) {
          name = sgList.get(i) + "." + columnNameList.get(i);
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`times.size() != 0` can be replaced with '!times.isEmpty()'
in `cli/src/main/java/org/apache/iotdb/tool/ImportCsv.java`
#### Snippet
```java
          }
        });
    if (times.size() != 0) {
      writeAndEmptyDataSet(deviceName.get(), times, typesList, valuesList, measurementsList, 3);
      pointSize.set(0);
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`nodeNameList.size() == 0` can be replaced with 'nodeNameList.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/qp/physical/sys/AuthorPlan.java`
#### Snippet
```java
    }
    List<String> nodeNameList = ReadWriteIOUtils.readStringList(buffer);
    if (nodeNameList.size() == 0) {
      this.nodeNameList = null;
    } else {
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`sql.length() > 0` can be replaced with '!sql.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/Coordinator.java`
#### Snippet
```java
    QueryId globalQueryId = queryIdGenerator.createNextQueryId();
    try (SetThreadName queryName = new SetThreadName(globalQueryId.getId())) {
      if (sql != null && sql.length() > 0) {
        LOGGER.debug("[QueryStart] sql: {}", sql);
      }
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`actualPaths.size() == 0` can be replaced with 'actualPaths.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/ExpressionAnalyzer.java`
#### Snippet
```java
      for (PartialPath concatPath : concatPaths) {
        List<MeasurementPath> actualPaths = schemaTree.searchMeasurementPaths(concatPath).left;
        if (actualPaths.size() == 0) {
          return Collections.singletonList(new NullOperand());
        }
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`noStarPaths.size() == 0` can be replaced with 'noStarPaths.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/ExpressionAnalyzer.java`
#### Snippet
```java

      List<MeasurementPath> noStarPaths = schemaTree.searchMeasurementPaths(concatPath).left;
      if (noStarPaths.size() == 0) {
        return Collections.singletonList(new NullOperand());
      }
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`concatedExpression.size() != 0` can be replaced with '!concatedExpression.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/ExpressionAnalyzer.java`
#### Snippet
```java
        List<Expression> concatedExpression =
            concatDeviceAndRemoveWildcard(suffixExpression, devicePath, schemaTree);
        if (concatedExpression != null && concatedExpression.size() != 0) {
          extendedExpressions.add(concatedExpression);
        }
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`replaced.length() == 0` can be replaced with 'replaced.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/parser/ASTVisitor.java`
#### Snippet
```java
        // replace "" with "
        String replaced = unWrappedString.replace("\"\"", "\"");
        return replaced.length() == 0 ? "" : replaced;
      }
      if ((src.charAt(0) == '\'' && src.charAt(src.length() - 1) == '\'')) {
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`replaced.length() == 0` can be replaced with 'replaced.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/parser/ASTVisitor.java`
#### Snippet
```java
        // replace '' with '
        String replaced = unWrappedString.replace("''", "'");
        return replaced.length() == 0 ? "" : replaced;
      }
    }
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`props.size() > 0` can be replaced with '!props.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/parser/ASTVisitor.java`
#### Snippet
```java
    }

    if (props.size() > 0) {
      throw new SQLParserException("schema template: property is not supported yet.");
    }
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`props.size() > 0` can be replaced with '!props.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/parser/ASTVisitor.java`
#### Snippet
```java
    }

    if (props.size() > 0) {
      throw new SQLParserException("create aligned timeseries: property is not supported yet.");
    }
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`0 == dataPartitionQueryParam.getTimePartitionSlotList().size()` can be replaced with 'dataPartitionQueryParam.getTimePartitionSlotList().isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/cache/PartitionCache.java`
#### Snippet
```java
        seriesSlotToTimePartitionMap.computeIfAbsent(seriesPartitionSlot, k -> new HashMap<>());
    // Notice: when query all time partition, then miss
    if (0 == dataPartitionQueryParam.getTimePartitionSlotList().size()) {
      return false;
    }
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`storageGroupToQueryParamsMap.size() == 0` can be replaced with 'storageGroupToQueryParamsMap.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/cache/PartitionCache.java`
#### Snippet
```java
    dataPartitionCacheLock.readLock().lock();
    try {
      if (storageGroupToQueryParamsMap.size() == 0) {
        CacheMetricsRecorder.record(false, DATA_PARTITION_CACHE_NAME);
        return null;
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`0 == entry.getValue().size()` can be replaced with 'entry.getValue().isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/cache/PartitionCache.java`
#### Snippet
```java
          storageGroupToQueryParamsMap.entrySet()) {
        if (null == entry.getValue()
            || 0 == entry.getValue().size()
            || !getStorageGroupDataPartition(dataPartitionMap, entry.getKey(), entry.getValue())) {
          CacheMetricsRecorder.record(false, DATA_PARTITION_CACHE_NAME);
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`0 == cacheConsensusGroupId.size()` can be replaced with 'cacheConsensusGroupId.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/cache/PartitionCache.java`
#### Snippet
```java
    List<TConsensusGroupId> cacheConsensusGroupId = cachedTimePartitionSlot.get(timePartitionSlot);
    if (null == cacheConsensusGroupId
        || 0 == cacheConsensusGroupId.size()
        || null == timePartitionSlot) {
      logger.debug(
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`storageGroupToDeviceMap.size() == 0` can be replaced with 'storageGroupToDeviceMap.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/cache/PartitionCache.java`
#### Snippet
```java
    schemaPartitionCacheLock.readLock().lock();
    try {
      if (storageGroupToDeviceMap.size() == 0) {
        CacheMetricsRecorder.record(false, SCHEMA_PARTITION_CACHE_NAME);
        return null;
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`0 != analysis.getDataPartitionInfo().getDataPartitionMap().size()` can be replaced with '!analysis.getDataPartitionInfo().getDataPartitionMap().isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/LogicalPlanVisitor.java`
#### Snippet
```java
    if (showTimeSeriesStatement.isOrderByHeat()
        && null != analysis.getDataPartitionInfo()
        && 0 != analysis.getDataPartitionInfo().getDataPartitionMap().size()) {
      PlanNode lastPlanNode =
          new LogicalPlanBuilder(analysis, context)
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`childBoxString.size() > 0` can be replaced with '!childBoxString.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/PlanGraphPrinter.java`
#### Snippet
```java
      int childrenWidth = 0;
      for (List<String> childBoxString : childBoxStrings) {
        Validate.isTrue(childBoxString.size() > 0, "Lines of box string should be greater than 0");
        childrenWidth += childBoxString.get(0).length();
      }
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`children.size() == 0` can be replaced with 'children.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/PlanGraphPrinter.java`
#### Snippet
```java
    box.lines.add(printBoxEdge(box, false));

    if (children.size() == 0) {
      return box.lines;
    }
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`sortItemList.size() > 0` can be replaced with '!sortItemList.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/AnalyzeVisitor.java`
#### Snippet
```java
    Set<Expression> sourceExpressions;
    List<SortItem> sortItemList = analysis.getMergeOrderParameter().getSortItemList();
    if (sortItemList.size() > 0) {
      checkState(
          sortItemList.size() == 1 && sortItemList.get(0).getSortKey() == SortKey.TIMESERIES,
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`schemaNodeManagementPartition.getSchemaPartition().getSchemaPartitionMap().size() == 0` can be replaced with 'schemaNodeManagementPartition.getSchemaPartition().getSchemaPartitionMap().isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/AnalyzeVisitor.java`
#### Snippet
```java
    }
    if (!schemaNodeManagementPartition.getMatchedNode().isEmpty()
        && schemaNodeManagementPartition.getSchemaPartition().getSchemaPartitionMap().size() == 0) {
      analysis.setFinishQueryAfterAnalyze(true);
    }
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`schemaNodeManagementPartition.getSchemaPartition().getSchemaPartitionMap().size() == 0` can be replaced with 'schemaNodeManagementPartition.getSchemaPartition().getSchemaPartitionMap().isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/AnalyzeVisitor.java`
#### Snippet
```java
    }
    if (!schemaNodeManagementPartition.getMatchedNode().isEmpty()
        && schemaNodeManagementPartition.getSchemaPartition().getSchemaPartitionMap().size() == 0) {
      analysis.setFinishQueryAfterAnalyze(true);
    }
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`children.size() > 0` can be replaced with '!children.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/metedata/read/AbstractSchemaMergeNode.java`
#### Snippet
```java
  @Override
  public List<String> getOutputColumnNames() {
    if (children.size() > 0) {
      return children.get(0).getOutputColumnNames();
    }
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`sourceLocations.size() > 0` can be replaced with '!sourceLocations.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/parameter/OutputColumn.java`
#### Snippet
```java
  public OutputColumn(List<InputLocation> sourceLocations, boolean overlapped) {
    checkArgument(
        sourceLocations != null && sourceLocations.size() > 0,
        "size of sourceLocations should be larger than 0");
    this.sourceLocations = sourceLocations;
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`availableDataNodes.size() == 0` can be replaced with 'availableDataNodes.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/distribution/SimpleFragmentParallelPlanner.java`
#### Snippet
```java
    List<TDataNodeLocation> availableDataNodes =
        filterAvailableTDataNode(regionReplicaSet.getDataNodeLocations());
    if (availableDataNodes.size() == 0) {
      String errorMsg =
          String.format(
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`descriptorExpressions.size() == 0` can be replaced with 'descriptorExpressions.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/distribution/SourceRewriter.java`
#### Snippet
```java
          }
        }
        if (descriptorExpressions.size() == 0) {
          continue;
        }
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`sources.size() > 0` can be replaced with '!sources.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/distribution/SourceRewriter.java`
#### Snippet
```java
                d, context.queryContext.getTypeProvider()));
    checkArgument(
        sources.size() > 0, "Aggregation sources should not be empty when distribution planning");
    SeriesAggregationSourceNode seed = sources.get(0);
    AggregationNode aggregationNode =
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`instanceInfo.getFailureInfoList().size() == 0` can be replaced with 'instanceInfo.getFailureInfoList().isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/scheduler/FixedRateFragInsStateTracker.java`
#### Snippet
```java
    if (instanceInfo.getState().isFailed()) {
      if (instanceInfo.getFailureInfoList() == null
          || instanceInfo.getFailureInfoList().size() == 0) {
        stateMachine.transitionToFailed(
            new RuntimeException(String.format("FragmentInstance[%s] is failed.", instanceId)));
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`insertRowStatementList.size() == 0` can be replaced with 'insertRowStatementList.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/statement/crud/InsertRowsOfOneDeviceStatement.java`
#### Snippet
```java

    // set device path, measurements, and data types
    if (insertRowStatementList == null || insertRowStatementList.size() == 0) {
      return;
    }
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`id.length() == 0` can be replaced with 'id.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/mpp/common/QueryId.java`
#### Snippet
```java
  // Check if the string matches [_a-z0-9]+ , but without the overhead of regex
  private static boolean isValidId(String id) {
    if (id.length() == 0) {
      return false;
    }
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`root.getChildren().size() == 0` can be replaced with 'root.getChildren().isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/mpp/common/schematree/ClusterSchemaTree.java`
#### Snippet
```java
  @Override
  public boolean isEmpty() {
    return root.getChildren() == null || root.getChildren().size() == 0;
  }
}
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`children.size() > 0` can be replaced with '!children.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/join/RowBasedTimeJoinOperator.java`
#### Snippet
```java
      TimeComparator comparator) {
    checkArgument(
        children != null && children.size() > 0,
        "child size of TimeJoinOperator should be larger than 0");
    this.operatorContext = operatorContext;
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`children.size() > 0` can be replaced with '!children.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/join/TimeJoinOperator.java`
#### Snippet
```java
      TimeComparator comparator) {
    checkArgument(
        children != null && children.size() > 0,
        "child size of TimeJoinOperator should be larger than 0");
    this.operatorContext = operatorContext;
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`insertNodes.size() != 0` can be replaced with '!insertNodes.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/wal/node/WALNode.java`
#### Snippet
```java

      // update iterator
      if (insertNodes.size() != 0) {
        itr = insertNodes.iterator();
        return true;
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`startNumbers.size() != 0` can be replaced with '!startNumbers.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/sync/pipedata/queue/BufferedPipeDataQueue.java`
#### Snippet
```java
        startNumbers.add(SyncPathUtil.getSerialNumberFromPipeLogName(file.getName()));
      }
    if (startNumbers.size() != 0) {
      Collections.sort(startNumbers);
      for (Long startTime : startNumbers) {
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`filePipeSerialNumberSet.size() > 0` can be replaced with '!filePipeSerialNumberSet.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/PipeOpManager.java`
#### Snippet
```java
    }

    if (filePipeSerialNumberSet.size() > 0) {
      if (filePipeSerialNumberSet.first() > minNum) {
        filePipe.commit(filePipeSerialNumberSet.first() - 1);
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`prefix.length() > 0` can be replaced with '!prefix.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/conf/IoTDBConfig.java`
#### Snippet
```java

  private String addDirPrefix(String prefix, String dir) {
    if (!new File(dir).isAbsolute() && prefix != null && prefix.length() > 0) {
      if (!prefix.endsWith(File.separator)) {
        dir = prefix + File.separatorChar + dir;
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`tvPairList.size() > 0` can be replaced with '!tvPairList.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/TsFileOpBlock.java`
#### Snippet
```java

      if (!sensorFullPath.equals(lastSensorFullPath)) {
        if ((tvPairList != null) && (tvPairList.size() > 0)) {
          insertToDataList(dataList, lastSensorFullPath, tvPairList);
          tvPairList = null;
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`tvPairList.size() > 0` can be replaced with '!tvPairList.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/TsFileOpBlock.java`
#### Snippet
```java
    }

    if ((tvPairList != null) && (tvPairList.size() > 0)) {
      insertToDataList(dataList, lastSensorFullPath, tvPairList);
    }
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`text.length() == 0` can be replaced with 'text.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/query/dataset/ShowTimeseriesDataSet.java`
#### Snippet
```java
            .collect(Collectors.joining(","));

    updateRecord(record, text.length() == 0 ? null : "{" + text + "}");
  }
}
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`valuesList.size() == 0` can be replaced with 'valuesList.isEmpty()'
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
      }
    }
    if (valuesList.size() == 0) {
      throw new NoValidValueException("All inserted data is null.");
    }
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`valuesList.size() == 0` can be replaced with 'valuesList.isEmpty()'
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
      }
    }
    if (valuesList.size() == 0) {
      logger.info("All values of the {} are null,null values are {}", deviceId, nullMap.toString());
      return true;
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`valuesList.size() == 0` can be replaced with 'valuesList.isEmpty()'
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
      }
    }
    if (valuesList.size() == 0) {
      throw new NoValidValueException("All inserted data is null.");
    }
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`valuesList.size() == 0` can be replaced with 'valuesList.isEmpty()'
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
      }
    }
    if (valuesList.size() == 0) {
      logger.info("All values of the {} are null,null values are {}", deviceId, nullMap.toString());
      return true;
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`valuesList.size() == 0` can be replaced with 'valuesList.isEmpty()'
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
      }
    }
    if (valuesList.size() == 0) {
      throw new NoValidValueException("All inserted data is null.");
    }
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`valuesList.size() == 0` can be replaced with 'valuesList.isEmpty()'
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
      }
    }
    if (valuesList.size() == 0) {
      throw new NoValidValueException("All inserted data is null.");
    }
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`newTsFileResources.size() == 0` can be replaced with 'newTsFileResources.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/tools/settle/TsFileAndModSettleTool.java`
#### Snippet
```java
                + File.separator
                + oldTsFileResource.getTimePartition());
    if (newTsFileResources.size() == 0) { // if the oldTsFile has no mods, it should not be deleted.
      if (oldTsFileResource.isDeleted()) {
        oldTsFileResource.remove();
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`queue.size() != 0` can be replaced with '!queue.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/FixedPriorityBlockingQueue.java`
#### Snippet
```java
    lock.lock();
    try {
      while (queue.size() != 0) {
        this.pollLast();
      }
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`queue.size() == 0` can be replaced with 'queue.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/FixedPriorityBlockingQueue.java`
#### Snippet
```java
    lock.lockInterruptibly();
    try {
      while (queue.size() == 0) {
        notEmpty.await();
      }
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`queue.size() == 0` can be replaced with 'queue.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/FixedPriorityBlockingQueue.java`
#### Snippet
```java
    lock.lockInterruptibly();
    try {
      while (queue.size() == 0) {
        notEmpty.await();
      }
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`seqDataDirList.size() == 0` can be replaced with 'seqDataDirList.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/tools/validate/TsFileValidationTool.java`
#### Snippet
```java
        }
      }
      if (seqDataDirList.size() == 0 && fileList.size() == 0) {
        System.out.println(
            "Please input correct param, which is [path of data dir] [-pd = print details or not] [-f = path of outFile]. Eg: xxx/iotdb/data/data -pd=true -f=xxx/TsFile_validation_view.txt");
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`fileList.size() == 0` can be replaced with 'fileList.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/tools/validate/TsFileValidationTool.java`
#### Snippet
```java
        }
      }
      if (seqDataDirList.size() == 0 && fileList.size() == 0) {
        System.out.println(
            "Please input correct param, which is [path of data dir] [-pd = print details or not] [-f = path of outFile]. Eg: xxx/iotdb/data/data -pd=true -f=xxx/TsFile_validation_view.txt");
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`selectedUnseqFiles.size() == 0` can be replaced with 'selectedUnseqFiles.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/cross/rewrite/RewriteCrossSpaceCompactionSelector.java`
#### Snippet
```java

  private boolean updateSelectedFiles(long newCost, TsFileResource unseqFile) {
    if (selectedUnseqFiles.size() == 0
        || (seqSelectedNum + selectedUnseqFiles.size() + 1 + tmpSelectedSeqFiles.size()
                <= maxCrossCompactionFileNum
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`mergeFiles[0].size() > 0` can be replaced with '!mergeFiles\[0\].isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/cross/rewrite/RewriteCrossSpaceCompactionSelector.java`
#### Snippet
```java
          mergeFiles[1]);

      if (mergeFiles[0].size() > 0 && mergeFiles[1].size() > 0) {
        LOGGER.info(
            "{} [Compaction] submit a task with {} sequence file and {} unseq files",
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`mergeFiles[1].size() > 0` can be replaced with '!mergeFiles\[1\].isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/cross/rewrite/RewriteCrossSpaceCompactionSelector.java`
#### Snippet
```java
          mergeFiles[1]);

      if (mergeFiles[0].size() > 0 && mergeFiles[1].size() > 0) {
        LOGGER.info(
            "{} [Compaction] submit a task with {} sequence file and {} unseq files",
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`readerAndChunkMetadataList.size() > 0` can be replaced with '!readerAndChunkMetadataList.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/inner/utils/AlignedSeriesCompactionExecutor.java`
#### Snippet
```java

  public void execute() throws IOException {
    while (readerAndChunkMetadataList.size() > 0) {
      Pair<TsFileSequenceReader, List<AlignedChunkMetadata>> readerListPair =
          readerAndChunkMetadataList.removeFirst();
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`targetTsFileList.size() > 0` can be replaced with '!targetTsFileList.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/inner/InnerSpaceCompactionTask.java`
#### Snippet
```java
      }
      // inner space compaction task has only one target file
      if (targetTsFileList.size() > 0) {
        // if the target tsfile is empty file, it will be removed
        TsFileMetricManager.getInstance()
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`readerAndChunkMetadataList.size() > 0` can be replaced with '!readerAndChunkMetadataList.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/inner/utils/SingleSeriesCompactionExecutor.java`
#### Snippet
```java
   */
  public void execute() throws IOException {
    while (readerAndChunkMetadataList.size() > 0) {
      Pair<TsFileSequenceReader, List<ChunkMetadata>> readerListPair =
          readerAndChunkMetadataList.removeFirst();
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`lostFiles.size() == 0` can be replaced with 'lostFiles.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/CompactionExceptionHandler.java`
#### Snippet
```java
      }
    }
    return lostFiles.size() == 0;
  }

```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`taskPriorityQueue.size() > 0` can be replaced with '!taskPriorityQueue.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/inner/sizetiered/SizeTieredCompactionSelector.java`
#### Snippet
```java
      }
      List<List<TsFileResource>> taskList = new LinkedList<>();
      while (taskPriorityQueue.size() > 0) {
        List<TsFileResource> resources = taskPriorityQueue.poll().left;
        taskList.add(resources);
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`chunkMetadataListMap.size() == 0` can be replaced with 'chunkMetadataListMap.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/inner/utils/MultiTsFileDeviceIterator.java`
#### Snippet
```java
        TsFileSequenceReader reader = readerMap.get(resource);
        Map<String, List<ChunkMetadata>> chunkMetadataListMap = chunkMetadataCacheMap.get(reader);
        if (chunkMetadataListMap.size() == 0) {
          if (chunkMetadataIteratorMap.get(resource).hasNext()) {
            chunkMetadataListMap = chunkMetadataIteratorMap.get(resource).next();
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`tempCollectedSeries.size() > 0` can be replaced with '!tempCollectedSeries.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/inner/utils/MultiTsFileDeviceIterator.java`
#### Snippet
```java
        tempCollectedSeries.addAll(chunkMetadataListMap.keySet());
      }
      if (tempCollectedSeries.size() > 0) {
        if (!hasRemainingSeries()) {
          lastSeries = Collections.max(tempCollectedSeries);
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`alignedChunkMetadataList.size() > 0` can be replaced with '!alignedChunkMetadataList.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/inner/utils/MultiTsFileDeviceIterator.java`
#### Snippet
```java
      List<AlignedChunkMetadata> alignedChunkMetadataList =
          reader.getAlignedChunkMetadata(currentDevice.left);
      if (alignedChunkMetadataList.size() > 0) {
        alignedChunkMetadataList.forEach(x -> x.setFilePath(tsFileResource.getTsFilePath()));
      }
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`seriesInThisIteration.size() == 0` can be replaced with 'seriesInThisIteration.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/inner/utils/MultiTsFileDeviceIterator.java`
#### Snippet
```java

    public boolean hasNextSeries() {
      if (seriesInThisIteration.size() == 0 && !collectSeries()) {
        return false;
      } else {
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`modificationForCurrentSeries.size() != 0` can be replaced with '!modificationForCurrentSeries.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/inner/utils/MultiTsFileDeviceIterator.java`
#### Snippet
```java

          // if there are modifications of current series, apply them to the chunk metadata
          if (modificationForCurrentSeries.size() != 0) {
            QueryUtils.modifyChunkMetaData(
                chunkMetadataListInThisResource, modificationForCurrentSeries);
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`reportedStorageGroupMemCostMap.size() == 0` can be replaced with 'reportedStorageGroupMemCostMap.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/rescon/SystemInfo.java`
#### Snippet
```java
  private boolean chooseMemTablesToMarkFlush(TsFileProcessor currentTsFileProcessor) {
    // If invoke flush by replaying logs, do not flush now!
    if (reportedStorageGroupMemCostMap.size() == 0) {
      return false;
    }
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`triggerNameList.size() != 0` can be replaced with '!triggerNameList.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/trigger/executor/TriggerFireVisitor.java`
#### Snippet
```java
    boolean isAllEmpty = true;
    for (List<String> triggerNameList : triggerNameLists) {
      if (triggerNameList.size() != 0) {
        isAllEmpty = false;
        break;
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`result.size() == 0` can be replaced with 'result.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/service/thrift/impl/ClientRPCServiceImpl.java`
#### Snippet
```java
        List<ByteBuffer> result = pair.left;
        finished = pair.right;
        boolean hasResultSet = !(result.size() == 0);
        resp.setHasResultSet(hasResultSet);
        resp.setIsAlign(true);
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`cur.getChildren().size() == 0` can be replaced with 'cur.getChildren().isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/ConfigMTree.java`
#### Snippet
```java

    // delete node a while retain root.a.sg2
    while (cur.getParent() != null && cur.getChildren().size() == 0) {
      cur.getParent().deleteChild(cur.getName());
      cur = cur.getParent();
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`TsFileAndModSettleTool.getInstance().recoverSettleFileMap.size() != 0` can be replaced with '!TsFileAndModSettleTool.getInstance().recoverSettleFileMap.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/DataRegion.java`
#### Snippet
```java
    try {
      TsFileAndModSettleTool.moveNewTsFile(oldTsFileResource, newTsFileResources);
      if (TsFileAndModSettleTool.getInstance().recoverSettleFileMap.size() != 0) {
        TsFileAndModSettleTool.getInstance()
            .recoverSettleFileMap
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`tsFilePaths.size() == 0` can be replaced with 'tsFilePaths.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/DataRegion.java`
#### Snippet
```java
      List<TsFileResource> unseqResourcesToBeSettled,
      List<String> tsFilePaths) {
    if (tsFilePaths.size() == 0) {
      for (TsFileResource resource : tsFileManager.getTsFileList(true)) {
        if (!resource.isClosed()) {
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`keyAddressList.size() == 0` can be replaced with 'keyAddressList.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/Segment.java`
#### Snippet
```java
    }

    if (keyAddressList.size() == 0) {
      throw new MetadataException("Segment can not be split with no records.");
    }
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`list.size() == 0` can be replaced with 'list.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/Segment.java`
#### Snippet
```java
  protected static <T> int binaryInsertPairList(List<Pair<String, T>> list, String key)
      throws RecordDuplicatedException {
    if (list.size() == 0) {
      return 0;
    }
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`cascadePages.size() != 0` can be replaced with '!cascadePages.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/pagemgr/BTreePageManager.java`
#### Snippet
```java
        }

        while (cascadePages.size() != 0) {
          if (dirtyPages.size() > SchemaFileConfig.PAGE_CACHE_SIZE) {
            flushDirtyPages();
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`children.size() > 0` can be replaced with '!children.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/pagemgr/BTreePageManager.java`
#### Snippet
```java
      @Override
      public boolean hasNext() {
        if (children.size() > 0) {
          return true;
        }
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`children.size() == 0` can be replaced with 'children.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/pagemgr/BTreePageManager.java`
#### Snippet
```java
        try {
          ISchemaPage nPage;
          while (children.size() == 0 && nextSeg >= 0) {
            nPage = getPageInstance(getPageIndex(nextSeg));
            children = nPage.getAsSegmentedPage().getChildren(getSegIndex(nextSeg));
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`children.size() > 0` can be replaced with '!children.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/pagemgr/BTreePageManager.java`
#### Snippet
```java
        }

        return children.size() > 0;
      }

```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`children.size() != 0` can be replaced with '!children.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/pagemgr/BTreePageManager.java`
#### Snippet
```java
    IMNode child;
    // TODO: inefficient to build B+Tree up-to-bottom, improve further
    while (children.size() != 0 || nextAddr != -1L) {
      if (children.size() == 0) {
        cursorPage = getPageInstance(getPageIndex(nextAddr));
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`children.size() == 0` can be replaced with 'children.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/pagemgr/BTreePageManager.java`
#### Snippet
```java
    // TODO: inefficient to build B+Tree up-to-bottom, improve further
    while (children.size() != 0 || nextAddr != -1L) {
      if (children.size() == 0) {
        cursorPage = getPageInstance(getPageIndex(nextAddr));
        nextAddr = cursorPage.getAsSegmentedPage().getNextSegAddress((short) 0);
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`dirtyPages.size() == 0` can be replaced with 'dirtyPages.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/pagemgr/PageManager.java`
#### Snippet
```java
  @Override
  public void flushDirtyPages() throws IOException {
    if (dirtyPages.size() == 0) {
      return;
    }
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`res.size() != 0` can be replaced with '!res.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/pagemgr/PageManager.java`
#### Snippet
```java

    // complete log file
    if (res.size() != 0) {
      FileOutputStream outputStream = new FileOutputStream(logPath, true);
      outputStream.write(new byte[] {SchemaFileConfig.SF_COMMIT_MARK});
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`astack.size() != 0` can be replaced with '!astack.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/metadata/template/Template.java`
#### Snippet
```java
    Deque<IMNode> astack = new ArrayDeque<>();
    astack.push(cur);
    while (astack.size() != 0) {
      IMNode top = astack.pop();
      if (!top.isMeasurement()) {
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`checkSet.size() == 0` can be replaced with 'checkSet.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/metadata/template/Template.java`
#### Snippet
```java
      }

      if (checkSet.size() == 0) {
        checkSet.add(prefix);
      }
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`traverseChildren.size() != 0` can be replaced with '!traverseChildren.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/metadata/template/Template.java`
#### Snippet
```java
    Deque<IMNode> traverseChildren = new ArrayDeque<>();
    directNodes.values().forEach(traverseChildren::push);
    while (traverseChildren.size() != 0) {
      IMNode cur = traverseChildren.pop();
      if (cur.getChildren().size() != 0) {
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`cur.getChildren().size() != 0` can be replaced with '!cur.getChildren().isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/metadata/template/Template.java`
#### Snippet
```java
    while (traverseChildren.size() != 0) {
      IMNode cur = traverseChildren.pop();
      if (cur.getChildren().size() != 0) {
        cur.getChildren().values().forEach(traverseChildren::push);
      }
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`stack.size() != 0` can be replaced with '!stack.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/metadata/template/Template.java`
#### Snippet
```java
    Deque<IMNode> stack = new ArrayDeque<>();
    stack.push(cur);
    while (stack.size() != 0) {
      cur = stack.pop();
      if (cur.isMeasurement()) {
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`requestBody.size() > 0` can be replaced with '!requestBody.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/protocol/rest/impl/GrafanaApiServiceImpl.java`
#### Snippet
```java
      throws NotFoundException {
    try {
      if (requestBody != null && requestBody.size() > 0) {
        PartialPath path = new PartialPath(Joiner.on(".").join(requestBody));
        String sql = "show child paths " + path;
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`numbers.size() == 0` can be replaced with 'numbers.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/function/aggregator/InfluxStddevFunction.java`
#### Snippet
```java
  public InfluxFunctionValue calculateBruteForce() {
    return new InfluxFunctionValue(
        numbers.size() == 0 ? numbers : MathUtils.popStdDev(numbers), 0L);
  }

```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`numbers.size() == 0` can be replaced with 'numbers.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/function/aggregator/InfluxMeanFunction.java`
#### Snippet
```java
  @Override
  public InfluxFunctionValue calculateBruteForce() {
    return new InfluxFunctionValue(numbers.size() == 0 ? numbers : MathUtils.mean(numbers), 0L);
  }

```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`numbers.size() == 0` can be replaced with 'numbers.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/function/aggregator/InfluxSumFunction.java`
#### Snippet
```java
  @Override
  public InfluxFunctionValue calculateBruteForce() {
    return new InfluxFunctionValue(numbers.size() == 0 ? numbers : MathUtils.sum(numbers), 0L);
  }

```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`value.size() == 0` can be replaced with 'value.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/handler/AbstractQueryHandler.java`
#### Snippet
```java
          updateByIoTDBFunc(database, measurement, function, sessionid);
      //      InfluxFunctionValue functionValue = function.calculateByIoTDBFunc();
      if (value.size() == 0) {
        value.add(functionValue.getTimestamp());
      } else {
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`value.size() == 0` can be replaced with 'value.isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/handler/AbstractQueryHandler.java`
#### Snippet
```java
        // If there are no common queries, they are all function queries
        for (InfluxFunction function : functions) {
          if (value.size() == 0) {
            value.add(function.calculateBruteForce().getTimestamp());
          } else {
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`((String) regex).length() > 0` can be replaced with '!((String) regex).isEmpty()'
in `library-udf/src/main/java/org/apache/iotdb/library/string/UDTFRegexMatch.java`
#### Snippet
```java
        .validateInputSeriesDataType(0, Type.TEXT)
        .validate(
            regex -> ((String) regex).length() > 0,
            "regexp has to be a valid regular expression.",
            validator.getParameters().getStringOrDefault("regex", ""))
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`((String) regex).length() > 0` can be replaced with '!((String) regex).isEmpty()'
in `library-udf/src/main/java/org/apache/iotdb/library/string/UDTFRegexSplit.java`
#### Snippet
```java
        .validateInputSeriesDataType(0, Type.TEXT)
        .validate(
            regex -> ((String) regex).length() > 0,
            "regexp has to be a valid regular expression.",
            validator.getParameters().getStringOrDefault("regex", ""))
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`((String) regex).length() > 0` can be replaced with '!((String) regex).isEmpty()'
in `library-udf/src/main/java/org/apache/iotdb/library/string/UDTFRegexReplace.java`
#### Snippet
```java
        .validateInputSeriesDataType(0, Type.TEXT)
        .validate(
            regex -> ((String) regex).length() > 0,
            "regex should not be empty",
            validator.getParameters().getString("regex"))
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`((String) target).length() > 0` can be replaced with '!((String) target).isEmpty()'
in `library-udf/src/main/java/org/apache/iotdb/library/string/UDTFStrReplace.java`
#### Snippet
```java
        .validateInputSeriesDataType(0, Type.TEXT)
        .validate(
            target -> ((String) target).length() > 0,
            "target should not be empty",
            validator.getParameters().getString("target"))
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`list2.size() == 0` can be replaced with 'list2.isEmpty()'
in `library-udf/src/main/java/org/apache/iotdb/library/frequency/UDTFDeconv.java`
#### Snippet
```java
  @Override
  public void terminate(PointCollector collector) throws Exception {
    if (list2.size() == 0) { // Exception: divided by zero
      throw new Exception("Divided by zero.");
    } else if (list2.size() > list1.size()) { // order of divisor is larger than dividend
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`meterRegistrySet.size() == 0` can be replaced with 'meterRegistrySet.isEmpty()'
in `metrics/micrometer-metrics/src/main/java/org/apache/iotdb/metrics/micrometer/reporter/MicrometerIoTDBReporter.java`
#### Snippet
```java
              .collect(Collectors.toSet());
      IoTDBMeterRegistry ioTDBMeterRegistry;
      if (meterRegistrySet.size() == 0) {
        ioTDBMeterRegistry = new IoTDBMeterRegistry(IoTDBRegistryConfig.DEFAULT, Clock.SYSTEM);
        Metrics.addRegistry(ioTDBMeterRegistry);
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`meterRegistrySet.size() == 0` can be replaced with 'meterRegistrySet.isEmpty()'
in `metrics/micrometer-metrics/src/main/java/org/apache/iotdb/metrics/micrometer/reporter/MicrometerJmxReporter.java`
#### Snippet
```java
              .filter(reporter -> reporter instanceof JmxMeterRegistry)
              .collect(Collectors.toSet());
      if (meterRegistrySet.size() == 0) {
        Metrics.addRegistry(new JmxMeterRegistry(JmxConfig.DEFAULT, Clock.SYSTEM));
      }
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`meterRegistrySet.size() == 0` can be replaced with 'meterRegistrySet.isEmpty()'
in `metrics/micrometer-metrics/src/main/java/org/apache/iotdb/metrics/micrometer/reporter/MicrometerPrometheusReporter.java`
#### Snippet
```java
            .collect(Collectors.toSet());
    PrometheusMeterRegistry prometheusMeterRegistry;
    if (meterRegistrySet.size() == 0) {
      prometheusMeterRegistry = new PrometheusMeterRegistry(PrometheusConfig.DEFAULT);
      prometheusMeterRegistry.throwExceptionOnRegistrationFailure();
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`homeDir.length() > 0` can be replaced with '!homeDir.isEmpty()'
in `confignode/src/main/java/org/apache/iotdb/confignode/conf/ConfigNodeConfig.java`
#### Snippet
```java
  private String addHomeDir(String dir) {
    String homeDir = System.getProperty(ConfigNodeConstant.CONFIGNODE_HOME, null);
    if (!new File(dir).isAbsolute() && homeDir != null && homeDir.length() > 0) {
      if (!homeDir.endsWith(File.separator)) {
        dir = homeDir + File.separatorChar + dir;
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`readerAndChunkMetadataList.size() > 0` can be replaced with '!readerAndChunkMetadataList.isEmpty()'
in `rewrite-tsfile-tool/src/main/java/org/apache/iotdb/RewriteTsFileTool.java`
#### Snippet
```java
        collectSchemaFromAlignedChunkMetadataList(readerAndChunkMetadataList);
    iSchemaList.forEach(x -> schemaList.add((MeasurementSchema) x));
    while (readerAndChunkMetadataList.size() > 0) {
      Pair<TsFileSequenceReader, List<AlignedChunkMetadata>> readerListPair =
          readerAndChunkMetadataList.removeFirst();
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`slidingWindow.size() > 0` can be replaced with '!slidingWindow.isEmpty()'
in `confignode/src/main/java/org/apache/iotdb/confignode/manager/node/heartbeat/ConfigNodeHeartbeatCache.java`
#### Snippet
```java
    long lastSendTime = 0;
    synchronized (slidingWindow) {
      if (slidingWindow.size() > 0) {
        lastSendTime = slidingWindow.getLast().getSendTimestamp();
      }
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`slidingWindow.size() == 0` can be replaced with 'slidingWindow.isEmpty()'
in `confignode/src/main/java/org/apache/iotdb/confignode/manager/node/heartbeat/BaseNodeCache.java`
#### Snippet
```java
      // Only sequential heartbeats are accepted.
      // And un-sequential heartbeats will be discarded.
      if (slidingWindow.size() == 0
          || slidingWindow.getLast().getSendTimestamp() < newHeartbeatSample.getSendTimestamp()) {
        slidingWindow.add(newHeartbeatSample);
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`slidingWindow.size() > 0` can be replaced with '!slidingWindow.isEmpty()'
in `confignode/src/main/java/org/apache/iotdb/confignode/manager/node/heartbeat/DataNodeHeartbeatCache.java`
#### Snippet
```java
    NodeHeartbeatSample lastSample = null;
    synchronized (slidingWindow) {
      if (slidingWindow.size() > 0) {
        lastSample = slidingWindow.getLast();
      }
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`slidingWindow.size() == 0` can be replaced with 'slidingWindow.isEmpty()'
in `confignode/src/main/java/org/apache/iotdb/confignode/manager/partition/heartbeat/RegionCache.java`
#### Snippet
```java
      // Only sequential HeartbeatSamples are accepted.
      // And un-sequential HeartbeatSamples will be discarded.
      if (slidingWindow.size() == 0
          || getLastSample().getSendTimestamp() < newHeartbeatSample.getSendTimestamp()) {
        slidingWindow.add(newHeartbeatSample);
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`configManager.getConsensusManager().getConsensusImpl().getAllConsensusGroupIds().size() ...` can be replaced with '!configManager.getConsensusManager().getConsensusImpl().getAllConsensusGroupIds().isEmpty()'
in `confignode/src/main/java/org/apache/iotdb/confignode/service/ConfigNode.java`
#### Snippet
```java
      boolean isJoinedCluster = false;
      for (int retry = 0; retry < SCHEDULE_WAITING_RETRY_NUM; retry++) {
        if (configManager.getConsensusManager().getConsensusImpl().getAllConsensusGroupIds().size()
            > 0) {
          isJoinedCluster = true;
          break;
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`migratedFailedRegions.size() > 0` can be replaced with '!migratedFailedRegions.isEmpty()'
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/impl/node/RemoveDataNodeProcedure.java`
#### Snippet
```java
            .map(TRegionReplicaSet::getRegionId)
            .collect(Collectors.toList());
    if (migratedFailedRegions.size() > 0) {
      LOG.warn(
          "{}, Some regions are migrated failed, the StopDataNode process should not be executed, migratedFailedRegions: {}",
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`removedDataNodes.size() == 0` can be replaced with 'removedDataNodes.isEmpty()'
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/env/DataNodeRemoveHandler.java`
#### Snippet
```java
              dataNodeLocation);
        }
        if (removedDataNodes.size() == 0) {
          status.setCode(TSStatusCode.NO_ENOUGH_DATANODE.getStatusCode());
          status.setMessage("Failed to remove all requested data nodes");
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`registeredDataNodes.size() > 0` can be replaced with '!registeredDataNodes.isEmpty()'
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/env/ConfigNodeProcedureEnv.java`
#### Snippet
```java
            registeredDataNodes);

    if (registeredDataNodes.size() > 0) {
      LOG.info(
          "Begin to broadcast the latest configNodeGroup to DataNodes, ConfigNodeGroups: {}, DataNodes: {}",
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`runnables.size() > 0` can be replaced with '!runnables.isEmpty()'
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/scheduler/SimpleProcedureScheduler.java`
#### Snippet
```java
  @Override
  public boolean queueHasRunnables() {
    return runnables.size() > 0;
  }

```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`batchList.size() > 0` can be replaced with '!batchList.isEmpty()'
in `flink-iotdb-connector/src/main/java/org/apache/iotdb/flink/IoTDBSink.java`
#### Snippet
```java
    if (batchSize > 0) {
      synchronized (batchList) {
        if (batchList.size() > 0) {
          List<String> deviceIds = new ArrayList<>();
          List<Long> timestamps = new ArrayList<>();
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`matchedPaths.size() != 0` can be replaced with '!matchedPaths.isEmpty()'
in `confignode/src/main/java/org/apache/iotdb/confignode/persistence/schema/ClusterSchemaInfo.java`
#### Snippet
```java
      PartialPath patternPath = new PartialPath(plan.getStorageGroupPathPattern());
      List<PartialPath> matchedPaths = mTree.getBelongedStorageGroups(patternPath);
      if (matchedPaths.size() != 0) {
        for (PartialPath path : matchedPaths) {
          mTree
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`plan.getPartitionSlotsMap().size() == 0` can be replaced with 'plan.getPartitionSlotsMap().isEmpty()'
in `confignode/src/main/java/org/apache/iotdb/confignode/persistence/partition/PartitionInfo.java`
#### Snippet
```java
    Map<String, SchemaPartitionTable> schemaPartition = new ConcurrentHashMap<>();

    if (plan.getPartitionSlotsMap().size() == 0) {
      // Return all SchemaPartitions when the queried PartitionSlots are empty
      storageGroupPartitionTables.forEach(
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`selectedSeries.size() == 0` can be replaced with 'selectedSeries.isEmpty()'
in `spark-tsfile/src/main/java/org/apache/iotdb/spark/tsfile/qp/optimizer/PhysicalOptimizer.java`
#### Snippet
```java

    // query all measurements from TSFile
    if (selectedSeries.size() == 0) {
      selectedSeries.addAll(allMeasurementsInFile.keySet());
    } else {
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`leafDeviceNodeOffsetList.size() == 0` can be replaced with 'leafDeviceNodeOffsetList.isEmpty()'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/TsFileDeviceIterator.java`
#### Snippet
```java
    if (!queue.isEmpty()) {
      return true;
    } else if (leafDeviceNodeOffsetList.size() == 0) {
      // device queue is empty and all device leaf node has been read
      return false;
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`chunkMetadataList.size() != 0` can be replaced with '!chunkMetadataList.isEmpty()'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/query/executor/ExecutorWithTimeGenerator.java`
#### Snippet
```java

      List<IChunkMetadata> chunkMetadataList = metadataQuerier.getChunkMetaDataList(selectedPath);
      if (chunkMetadataList.size() != 0) {
        dataTypes.add(chunkMetadataList.get(0).getDataType());
        if (cachedValue) {
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`timeHeap.size() > 0` can be replaced with '!timeHeap.isEmpty()'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/query/dataset/DataSetWithoutTimeGenerator.java`
#### Snippet
```java
  @Override
  public boolean hasNextWithoutConstraint() {
    return timeHeap.size() > 0;
  }

```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`pathSc.length() > 0` can be replaced with '!pathSc.isEmpty()'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/Path.java`
#### Snippet
```java
      fullPath = pathSc;
    } else {
      if (pathSc.length() > 0) {
        String[] nodes = PathNodesGenerator.splitPathToNodes(pathSc);
        device = "";
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`tags.size() > 0` can be replaced with '!tags.isEmpty()'
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/RSchemaUtils.java`
#### Snippet
```java
    }

    if (tags != null && tags.size() > 0) {
      flag = (byte) (flag | FLAG_HAS_TAGS);
    }
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`attributes.size() > 0` can be replaced with '!attributes.isEmpty()'
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/RSchemaUtils.java`
#### Snippet
```java
    }

    if (attributes != null && attributes.size() > 0) {
      flag = (byte) (flag | FLAG_HAS_ATTRIBUTES);
    }
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`tags.size() > 0` can be replaced with '!tags.isEmpty()'
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/RSchemaUtils.java`
#### Snippet
```java
    }

    if (tags != null && tags.size() > 0) {
      ReadWriteIOUtils.write(DATA_BLOCK_TYPE_TAGS, outputStream);
      ReadWriteIOUtils.write(tags, outputStream);
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`attributes.size() > 0` can be replaced with '!attributes.isEmpty()'
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/RSchemaUtils.java`
#### Snippet
```java
    }

    if (attributes != null && attributes.size() > 0) {
      ReadWriteIOUtils.write(DATA_BLOCK_TYPE_TAGS, outputStream);
      ReadWriteIOUtils.write(tags, outputStream);
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`selectedSeries.size() == 0` can be replaced with 'selectedSeries.isEmpty()'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/expression/util/ExpressionOptimizer.java`
#### Snippet
```java
      GlobalTimeExpression timeFilter, List<Path> selectedSeries)
      throws QueryFilterOptimizationException {
    if (selectedSeries.size() == 0) {
      throw new QueryFilterOptimizationException("size of selectSeries could not be 0");
    }
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`filePath.length() > 0` can be replaced with '!filePath.isEmpty()'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/FilePathUtils.java`
#### Snippet
```java
   */
  public static String regularizePath(String filePath) {
    if (filePath.length() > 0 && filePath.charAt(filePath.length() - 1) != File.separatorChar) {
      filePath = filePath + File.separatorChar;
    }
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`endPosForEachDevice.size() > 0` can be replaced with '!endPosForEachDevice.isEmpty()'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/writer/tsmiterator/DiskTSMIterator.java`
#### Snippet
```java
      currentDevice = ReadWriteIOUtils.readString(input.wrapAsInputStream());
      nextEndPosForDevice =
          endPosForEachDevice.size() > 0 ? endPosForEachDevice.removeFirst() : fileLength;
    }
    // deserialize public info for measurement
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`iChunkMetadataList.size() > 0` can be replaced with '!iChunkMetadataList.isEmpty()'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/writer/TsFileIOWriter.java`
#### Snippet
```java
      ReadWriteIOUtils.write(seriesPath.getDevice(), tempOutput.wrapAsStream());
    }
    if (isNewPath && iChunkMetadataList.size() > 0) {
      // serialize the public info of this measurement
      ReadWriteIOUtils.writeVar(seriesPath.getMeasurement(), tempOutput.wrapAsStream());
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`values.size() == 0` can be replaced with 'values.isEmpty()'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/encoder/LongZigzagEncoder.java`
#### Snippet
```java
    ByteArrayOutputStream byteCache = new ByteArrayOutputStream();
    int len = values.size();
    if (values.size() == 0) {
      return;
    }
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`values.size() == 0` can be replaced with 'values.isEmpty()'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/encoder/IntZigzagEncoder.java`
#### Snippet
```java
    ByteArrayOutputStream byteCache = new ByteArrayOutputStream();
    int len = values.size();
    if (values.size() == 0) {
      return;
    }
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`type.trim().length() != 0` can be replaced with '!type.trim().isEmpty()'
in `metrics/interface/src/main/java/org/apache/iotdb/metrics/config/MetricConfig.java`
#### Snippet
```java
    this.metricReporterList = new ArrayList<>();
    for (String type : metricReporterList.split(",")) {
      if (type.trim().length() != 0) {
        this.metricReporterList.add(ReporterType.valueOf(type));
      }
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`tsRecord.dataPointList.size() > 0` can be replaced with '!tsRecord.dataPointList.isEmpty()'
in `flink-tsfile-connector/src/main/java/org/apache/iotdb/flink/tsfile/RowTSRecordConverter.java`
#### Snippet
```java
    }
    for (TSRecord tsRecord : reuse) {
      if (tsRecord.dataPointList.size() > 0) {
        tsRecord.setTime(timestamp);
        collector.collect(tsRecord);
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`0 == tagsPart.length()` can be replaced with 'tagsPart.isEmpty()'
in `metrics/dropwizard-metrics/src/main/java/org/apache/iotdb/metrics/dropwizard/DropwizardMetricNameTool.java`
#### Snippet
```java
      name = flatString.substring(0, firstIndex).replaceAll("[^a-zA-Z0-9:_\\]\\[]", "_");
      String tagsPart = flatString.substring(firstIndex + 1, lastIndex);
      if (0 == tagsPart.length()) {
        metricInfo = new MetricInfo(metricType, name);
      } else {
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`mNode.getTags().size() > 0` can be replaced with '!mNode.getTags().isEmpty()'
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/RSchemaRegion.java`
#### Snippet
```java
                  new RMeasurementMNode(path.getFullPath(), originValue, readWriteHandler);
              boolean hasTags = false;
              if (mNode.getTags() != null && mNode.getTags().size() > 0) {
                hasTags = true;
                for (Map.Entry<String, String> entry : tagsMap.entrySet()) {
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`labels.size() > 0` can be replaced with '!labels.isEmpty()'
in `metrics/dropwizard-metrics/src/main/java/org/apache/iotdb/metrics/dropwizard/reporter/PrometheusTextWriter.java`
#### Snippet
```java
      throws IOException {
    write(name);
    if (labels.size() > 0) {
      write('{');
      for (Map.Entry<String, String> entry : labels.entrySet()) {
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`exception.getMessage().length() > 0` can be replaced with '!exception.getMessage().isEmpty()'
in `consensus/src/main/java/org/apache/iotdb/consensus/common/response/ConsensusWriteResponse.java`
#### Snippet
```java
    if (exception != null
        && exception.getMessage() != null
        && exception.getMessage().length() > 0) {
      return exception.getMessage();
    }
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`pendingBatches.size() > 0` can be replaced with '!pendingBatches.isEmpty()'
in `consensus/src/main/java/org/apache/iotdb/consensus/multileader/logdispatcher/SyncStatus.java`
#### Snippet
```java
    synchronized (this) {
      batch.setSynced(true);
      if (pendingBatches.size() > 0 && pendingBatches.get(0).equals(batch)) {
        Iterator<PendingBatch> iterator = pendingBatches.iterator();
        PendingBatch current = iterator.next();
```

### RuleId[ruleID=SizeReplaceableByIsEmpty]
`group.getPeers().size() == 0` can be replaced with 'group.getPeers().isEmpty()'
in `consensus/src/main/java/org/apache/iotdb/consensus/ratis/RatisConsensus.java`
#### Snippet
```java
    RatisClient client = null;
    try {
      if (group.getPeers().size() == 0) {
        client = getRaftClient(RaftGroup.valueOf(group.getGroupId(), server));
      } else {
```

## RuleId[ruleID=TrivialStringConcatenation]
### RuleId[ruleID=TrivialStringConcatenation]
Empty string used in concatenation
in `integration/src/main/java/org/apache/iotdb/integration/env/ClusterEnvBase.java`
#### Snippet
```java
    do {
      StringBuilder port =
          new StringBuilder("" + rpcPortStart++)
              .append("|")
              .append(metaPortStart++)
```

### RuleId[ruleID=TrivialStringConcatenation]
Empty string used in concatenation
in `server/src/main/java/org/apache/iotdb/db/qp/utils/DateTimeUtils.java`
#### Snippet
```java
        temp += (ch - '0');
      } else {
        String unit = duration.charAt(i) + "";
        // This is to identify units with two letters.
        if (i + 1 < duration.length() && !Character.isDigit(duration.charAt(i + 1))) {
```

### RuleId[ruleID=TrivialStringConcatenation]
Empty string used in concatenation
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/chunk/AlignedChunkGroupWriterImpl.java`
#### Snippet
```java
              + deviceId
              + TsFileConstant.PATH_SEPARATOR
              + ""
              + ", time should later than "
              + lastTime);
```

## RuleId[ruleID=FinalStaticMethod]
### RuleId[ruleID=FinalStaticMethod]
'static' method declared `final`
in `jdbc/src/main/java/org/apache/iotdb/jdbc/StringUtils.java`
#### Snippet
```java
  }

  public static final String fixDecimalExponent(String dString) {
    int ePos = dString.indexOf("E");
    if (ePos == -1) {
```

## RuleId[ruleID=NullableProblems]
### RuleId[ruleID=NullableProblems]
Constructor parameter for @Nullable field might be annotated @Nullable itself
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/process/GroupByLevelNode.java`
#### Snippet
```java
      PlanNodeId id,
      List<CrossSeriesAggregationDescriptor> groupByLevelDescriptors,
      GroupByTimeParameter groupByTimeParameter,
      Ordering scanOrder) {
    super(id);
```

### RuleId[ruleID=NullableProblems]
Constructor parameter for @Nullable field might be annotated @Nullable itself
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/process/GroupByLevelNode.java`
#### Snippet
```java
      List<PlanNode> children,
      List<CrossSeriesAggregationDescriptor> groupByLevelDescriptors,
      GroupByTimeParameter groupByTimeParameter,
      Ordering scanOrder) {
    super(id, children);
```

### RuleId[ruleID=NullableProblems]
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `flink-tsfile-connector/src/main/java/org/apache/iotdb/flink/tsfile/TsFileOutputFormat.java`
#### Snippet
```java

  protected Schema schema;
  @Nullable protected TSFileConfig config;

  protected transient Configuration hadoopConf = null;
```

### RuleId[ruleID=NullableProblems]
The generated code will use '@org.jetbrains.annotations.Nullable' instead of '@javax.annotation.Nullable'
in `flink-tsfile-connector/src/main/java/org/apache/iotdb/flink/tsfile/TsFileInputFormat.java`
#### Snippet
```java
  private final QueryExpression expression;
  private final RowRecordParser<T> parser;
  @Nullable private final TSFileConfig config;

  private transient org.apache.hadoop.conf.Configuration hadoopConf = null;
```

## RuleId[ruleID=IgnoreResultOfCall]
### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.mkdirs()` is ignored
in `node-commons/src/main/java/org/apache/iotdb/commons/auth/role/LocalFileRoleAccessor.java`
#### Snippet
```java
    File roleDir = new File(roleDirPath);
    if (!roleDir.exists()) {
      roleDir.mkdirs();
    }
    try (BufferedOutputStream outputStream =
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.mkdirs()` is ignored
in `node-commons/src/main/java/org/apache/iotdb/commons/sync/utils/SyncPathUtil.java`
#### Snippet
```java
  public static boolean createFile(File file) throws IOException {
    if (!file.getParentFile().exists()) {
      file.getParentFile().mkdirs();
    }
    return file.createNewFile();
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.mkdirs()` is ignored
in `node-commons/src/main/java/org/apache/iotdb/commons/sync/persistence/SyncLogWriter.java`
#### Snippet
```java
      File logFile = new File(dir, fileName);
      if (!logFile.getParentFile().exists()) {
        logFile.getParentFile().mkdirs();
      }
      outputStream = new FileOutputStream(logFile, true);
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.mkdirs()` is ignored
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/JVMCommonUtils.java`
#### Snippet
```java
  public static long getUsableSpace(String dir) {
    File dirFile = FSFactoryProducer.getFSFactory().getFile(dir);
    dirFile.mkdirs();
    return dirFile.getFreeSpace();
  }
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `DataInputStream.read()` is ignored
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/IOUtils.java`
#### Snippet
```java
      }

      inputStream.read(strBuffer, 0, length);
      return new String(strBuffer, 0, length, encoding);
    }
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `ZoneId.of()` is ignored
in `jdbc/src/main/java/org/apache/iotdb/jdbc/Utils.java`
#### Snippet
```java
          try {
            // Check the validity of the time zone string.
            ZoneId.of(value);
          } catch (DateTimeException e) {
            return false;
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.delete()` is ignored
in `cli/src/main/java/org/apache/iotdb/tool/ExportTsFile.java`
#### Snippet
```java
    File f = FSFactoryProducer.getFSFactory().getFile(filePath);
    if (f.exists()) {
      f.delete();
    }
    HashSet<String> deviceFilterSet = new HashSet<>();
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.delete()` is ignored
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileWriteWithTSRecord.java`
#### Snippet
```java
      File f = FSFactoryProducer.getFSFactory().getFile(path);
      if (f.exists()) {
        f.delete();
      }

```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.mkdirs()` is ignored
in `schema-engine-tag/src/main/java/org/apache/iotdb/lsm/manager/WALManager.java`
#### Snippet
```java
  private void initFile(String walDirPath, String walFileName) throws IOException {
    File schemaDir = new File(walDirPath);
    schemaDir.mkdirs();
    walFile = new File(this.walDirPath, walFileName);
    if (!walFile.exists()) {
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.createNewFile()` is ignored
in `schema-engine-tag/src/main/java/org/apache/iotdb/lsm/manager/WALManager.java`
#### Snippet
```java
    walFile = new File(this.walDirPath, walFileName);
    if (!walFile.exists()) {
      walFile.createNewFile();
    }
  }
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.setExecutable()` is ignored
in `integration/src/main/java/org/apache/iotdb/integration/env/ClusterNode.java`
#### Snippet
```java
    try {
      FileUtils.copyDirectoryToDirectory(new File(srcPath), new File(this.path));
      new File(scriptPath).setExecutable(true);
    } catch (IOException ex) {
      fail("Copy cluster node dir failed. " + ex.getMessage());
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.mkdir()` is ignored
in `cli/src/main/java/org/apache/iotdb/tool/ImportCsv.java`
#### Snippet
```java
      File file = new File(failedFileDirectory);
      if (!file.isDirectory()) {
        file.mkdir();
        failedFileDirectory = file.getAbsolutePath() + File.separator;
      }
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `Math.toIntExact()` is ignored
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/statement/literal/LongLiteral.java`
#### Snippet
```java
    if (dataType == TSDataType.INT32) {
      try {
        Math.toIntExact(value);
        return true;
      } catch (ArithmeticException e) {
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.delete()` is ignored
in `server/src/main/java/org/apache/iotdb/db/wal/recover/WALNodeRecoverTask.java`
#### Snippet
```java
      File[] checkpointFiles = CheckpointFileUtils.listAllCheckpointFiles(logDirectory);
      for (File checkpointFile : checkpointFiles) {
        checkpointFile.delete();
      }
      // recover version id and search index
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.delete()` is ignored
in `server/src/main/java/org/apache/iotdb/db/wal/checkpoint/CheckpointManager.java`
#### Snippet
```java
                  logDirectory,
                  CheckpointFileUtils.getLogFileName(currentCheckPointFileVersion - 1));
          oldFile.delete();
        }
      } catch (IOException e) {
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.createNewFile()` is ignored
in `server/src/main/java/org/apache/iotdb/db/conf/adapter/CompressionRatio.java`
#### Snippet
```java
    checkDirectoryExist();
    if (!oldFile.exists()) {
      newFile.createNewFile();
      LOGGER.debug(
          "Old ratio file {} doesn't exist, force create ratio file {}",
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.delete()` is ignored
in `server/src/main/java/org/apache/iotdb/db/conf/adapter/CompressionRatio.java`
#### Snippet
```java
      for (int i = 0; i < ratioFiles.length; i++) {
        if (i != maxRatioIndex) {
          ratioFiles[i].delete();
        }
      }
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.mkdirs()` is ignored
in `server/src/main/java/org/apache/iotdb/db/sync/sender/recovery/TsFilePipeLogger.java`
#### Snippet
```java
    File link = new File(tsFileDir, getRelativeFilePath(file));
    if (!link.getParentFile().exists()) {
      link.getParentFile().mkdirs();
    }

```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `Double.parseDouble()` is ignored
in `server/src/main/java/org/apache/iotdb/db/conf/IoTDBDescriptor.java`
#### Snippet
```java
        ((double) (proportionForMemTable)
            / (double) (proportionForMemTable + proportionForTimePartitionInfo));
    Double.parseDouble(properties.getProperty("flush_time_memory_proportion", "0.05"));
    double timePartitionInfoForWrite =
        ((double) (proportionForTimePartitionInfo)
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.mkdirs()` is ignored
in `server/src/main/java/org/apache/iotdb/db/sync/transport/server/ReceiverManager.java`
#### Snippet
```java

    if (!new File(SyncPathUtil.getFileDataDirPath(identityInfo)).exists()) {
      new File(SyncPathUtil.getFileDataDirPath(identityInfo)).mkdirs();
    }
    createConnection(identityInfo);
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.renameTo()` is ignored
in `server/src/main/java/org/apache/iotdb/db/sync/transport/server/ReceiverManager.java`
#### Snippet
```java
                    .substring(
                        0, targetFile.getName().length() - SyncConstant.PATCH_SUFFIX.length()));
        targetFile.renameTo(newFile);
      }
    }
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.delete()` is ignored
in `server/src/main/java/org/apache/iotdb/db/query/externalsort/serialize/impl/FixLengthTimeValuePairSerializer.java`
#### Snippet
```java
    File file = new File(tmpFilePath);
    if (file.exists()) {
      file.delete();
    }
    if (file.getParentFile() != null) {
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.mkdirs()` is ignored
in `server/src/main/java/org/apache/iotdb/db/query/externalsort/serialize/impl/FixLengthTimeValuePairSerializer.java`
#### Snippet
```java
    }
    if (file.getParentFile() != null) {
      file.getParentFile().mkdirs();
    }
    file.createNewFile();
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.createNewFile()` is ignored
in `server/src/main/java/org/apache/iotdb/db/query/externalsort/serialize/impl/FixLengthTimeValuePairSerializer.java`
#### Snippet
```java
      file.getParentFile().mkdirs();
    }
    file.createNewFile();
  }

```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.delete()` is ignored
in `server/src/main/java/org/apache/iotdb/db/tools/settle/TsFileAndModSettleTool.java`
#### Snippet
```java
            "meet error when reading settle log, log path:{}", SettleLog.getSettleLogPath(), e);
      } finally {
        FSFactoryProducer.getFSFactory().getFile(SettleLog.getSettleLogPath()).delete();
      }
    }
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.delete()` is ignored
in `server/src/main/java/org/apache/iotdb/db/tools/settle/TsFileAndModSettleTool.java`
#### Snippet
```java
      }
      if (newPartitionDir.exists()) {
        newPartitionDir.delete();
      }
      return;
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.delete()` is ignored
in `server/src/main/java/org/apache/iotdb/db/tools/settle/TsFileAndModSettleTool.java`
#### Snippet
```java
    File oldTsFile = oldTsFileResource.getTsFile();
    boolean isOldFileExisted = oldTsFile.exists();
    oldTsFile.delete();
    for (TsFileResource newTsFileResource : newTsFileResources) {
      newPartitionDir =
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.delete()` is ignored
in `server/src/main/java/org/apache/iotdb/db/tools/settle/TsFileAndModSettleTool.java`
#### Snippet
```java
                newPartitionDir, newTsFile.getName() + TsFileResource.RESOURCE_SUFFIX);
        if (tmpResourceFile.exists()) {
          tmpResourceFile.delete();
        }
      }
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.delete()` is ignored
in `server/src/main/java/org/apache/iotdb/db/tools/settle/TsFileAndModSettleTool.java`
#### Snippet
```java
      // if the newPartition folder is empty, then it will be deleted
      if (newPartitionDir.exists()) {
        newPartitionDir.delete();
      }
    }
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.mkdirs()` is ignored
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileSplitByPartitionTool.java`
#### Snippet
```java
                  .getFile(oldTsFile.getParent() + File.separator + partition);
          if (!partitionDir.exists()) {
            partitionDir.mkdirs();
          }
          File newFile =
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.delete()` is ignored
in `server/src/main/java/org/apache/iotdb/db/utils/UpgradeUtils.java`
#### Snippet
```java
            e);
      } finally {
        FSFactoryProducer.getFSFactory().getFile(UpgradeLog.getUpgradeLogPath()).delete();
      }
    }
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.mkdir()` is ignored
in `server/src/main/java/org/apache/iotdb/db/utils/UpgradeUtils.java`
#### Snippet
```java
      File partitionDir = fsFactory.getFile(virtualStorageGroupDir, String.valueOf(partition));
      if (!partitionDir.exists()) {
        partitionDir.mkdir();
      }
      // move upgraded TsFile
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `DataInputStream.read()` is ignored
in `server/src/main/java/org/apache/iotdb/db/utils/QueryDataSetUtils.java`
#### Snippet
```java
            int binarySize = stream.readInt();
            byte[] binaryValue = new byte[binarySize];
            stream.read(binaryValue);
            binaryValues[index] = new Binary(binaryValue);
          }
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.mkdirs()` is ignored
in `server/src/main/java/org/apache/iotdb/db/engine/settle/SettleLog.java`
#### Snippet
```java
    try {
      if (!settleLogPath.getParentFile().exists()) {
        settleLogPath.getParentFile().mkdirs();
      }
      settleLogPath.createNewFile();
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.createNewFile()` is ignored
in `server/src/main/java/org/apache/iotdb/db/engine/settle/SettleLog.java`
#### Snippet
```java
        settleLogPath.getParentFile().mkdirs();
      }
      settleLogPath.createNewFile();
      settleLogWriter = new BufferedWriter(new FileWriter(getSettleLogPath(), true));
      return true;
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.mkdirs()` is ignored
in `server/src/main/java/org/apache/iotdb/db/engine/upgrade/UpgradeLog.java`
#### Snippet
```java
    try {
      if (!upgradeLogPath.getParentFile().exists()) {
        upgradeLogPath.getParentFile().mkdirs();
      }
      upgradeLogPath.createNewFile();
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.createNewFile()` is ignored
in `server/src/main/java/org/apache/iotdb/db/engine/upgrade/UpgradeLog.java`
#### Snippet
```java
        upgradeLogPath.getParentFile().mkdirs();
      }
      upgradeLogPath.createNewFile();
      upgradeLogWriter = new BufferedWriter(new FileWriter(getUpgradeLogPath(), true));
      return true;
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.mkdirs()` is ignored
in `server/src/main/java/org/apache/iotdb/db/engine/version/SimpleFileVersionController.java`
#### Snippet
```java
    File directory = SystemFileFactory.INSTANCE.getFile(directoryPath);
    if (!directory.exists()) {
      directory.mkdirs();
    }
    File[] versionFiles = directory.listFiles((dir, name) -> name.startsWith(FILE_PREFIX));
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.delete()` is ignored
in `server/src/main/java/org/apache/iotdb/db/engine/version/SimpleFileVersionController.java`
#### Snippet
```java
      for (int i = 0; i < versionFiles.length; i++) {
        if (i != maxVersionIndex) {
          versionFiles[i].delete();
        }
      }
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.delete()` is ignored
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/task/CompactionRecoverTask.java`
#### Snippet
```java
        File tmpTargetFile = targetFileIdentifier.getFileFromDataDirs();
        if (tmpTargetFile != null) {
          tmpTargetFile.delete();
          File chunkMetadataTempFile =
              new File(
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.delete()` is ignored
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/task/CompactionRecoverTask.java`
#### Snippet
```java
                  tmpTargetFile.getAbsolutePath() + TsFileIOWriter.CHUNK_METADATA_TEMP_FILE_SUFFIX);
          if (chunkMetadataTempFile.exists()) {
            chunkMetadataTempFile.delete();
          }
        }
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.delete()` is ignored
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/writer/InnerSpaceCompactionWriter.java`
#### Snippet
```java
    fileWriter.endFile();
    if (isEmptyFile) {
      fileWriter.getFile().delete();
    }
  }
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.delete()` is ignored
in `server/src/main/java/org/apache/iotdb/db/engine/modification/ModificationFile.java`
#### Snippet
```java
  public void remove() throws IOException {
    close();
    FSFactoryProducer.getFSFactory().getFile(filePath).delete();
  }

```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.delete()` is ignored
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/writer/CrossSpaceCompactionWriter.java`
#### Snippet
```java
      // delete empty target file
      if (isEmptyFile[i]) {
        fileWriterList.get(i).getFile().delete();
      }
    }
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.mkdirs()` is ignored
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileNameGenerator.java`
#### Snippet
```java
    String tsFileDir =
        generateTsFileDir(sequence, logicalStorageGroup, virtualStorageGroup, timePartitionId);
    fsFactory.getFile(tsFileDir).mkdirs();
    return tsFileDir
        + File.separator
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `InputStream.skip()` is ignored
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/timeindex/DeviceTimeIndex.java`
#### Snippet
```java
  public static Set<String> getDevices(InputStream inputStream) throws IOException {
    int deviceNum = ReadWriteIOUtils.readInt(inputStream);
    inputStream.skip(2L * deviceNum * ReadWriteIOUtils.LONG_LEN);
    Set<String> devices = new HashSet<>();
    for (int i = 0; i < deviceNum; i++) {
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `InputStream.skip()` is ignored
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/timeindex/DeviceTimeIndex.java`
#### Snippet
```java
    for (int i = 0; i < deviceNum; i++) {
      String path = ReadWriteIOUtils.readString(inputStream).intern();
      inputStream.skip(ReadWriteIOUtils.INT_LEN);
      devices.add(path);
    }
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.delete()` is ignored
in `server/src/main/java/org/apache/iotdb/db/metadata/tag/TagManager.java`
#### Snippet
```java
    File tagFile = SystemFileFactory.INSTANCE.getFile(sgSchemaDirPath, MetadataConstant.TAG_LOG);
    if (tagFile.exists()) {
      tagFile.delete();
    }

```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.delete()` is ignored
in `server/src/main/java/org/apache/iotdb/db/metadata/tag/TagManager.java`
#### Snippet
```java
      return new TagManager(sgSchemaDirPath);
    } catch (IOException e) {
      tagFile.delete();
      throw e;
    }
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.delete()` is ignored
in `server/src/main/java/org/apache/iotdb/db/metadata/tag/TagManager.java`
#### Snippet
```java
            tagLogSnapshotTmp.getName(),
            tagLogSnapshot.getName());
        tagLogSnapshot.delete();
        return false;
      }
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.delete()` is ignored
in `server/src/main/java/org/apache/iotdb/db/metadata/tag/TagManager.java`
#### Snippet
```java
    } catch (IOException e) {
      logger.error("Failed to create tagManager snapshot due to {}", e.getMessage(), e);
      tagLogSnapshot.delete();
      return false;
    } finally {
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.delete()` is ignored
in `server/src/main/java/org/apache/iotdb/db/metadata/tag/TagManager.java`
#### Snippet
```java
      return false;
    } finally {
      tagLogSnapshotTmp.delete();
    }
  }
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.mkdirs()` is ignored
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/DataRegion.java`
#### Snippet
```java
    // move file from sync dir to data dir
    if (!targetFile.getParentFile().exists()) {
      targetFile.getParentFile().mkdirs();
    }
    try {
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.mkdirs()` is ignored
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/DataRegion.java`
#### Snippet
```java
                SystemFileFactory.INSTANCE.getFile(storageGroupSysDir, String.valueOf(partitionId));
            if (!directory.exists()) {
              directory.mkdirs();
            }
            File versionFile =
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.delete()` is ignored
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/DataRegion.java`
#### Snippet
```java
        File originResource = fsFactory.getFile(tempResource.getPath().replace(suffix, ""));
        if (originResource.exists()) {
          tempResource.delete();
        } else {
          tempResource.renameTo(originResource);
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.renameTo()` is ignored
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/DataRegion.java`
#### Snippet
```java
          tempResource.delete();
        } else {
          tempResource.renameTo(originResource);
        }
      }
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.delete()` is ignored
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/SchemaFile.java`
#### Snippet
```java
    } catch (IOException e) {
      logger.error("Failed to create SchemaFile snapshot due to {}", e.getMessage(), e);
      schemaFileSnapshot.delete();
      return false;
    }
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.createNewFile()` is ignored
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/SchemaFile.java`
#### Snippet
```java
      Files.delete(Paths.get(pmtFile.toURI()));
    }
    pmtFile.createNewFile();

    channel = new RandomAccessFile(pmtFile, "rw").getChannel();
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.createNewFile()` is ignored
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/SchemaFile.java`
#### Snippet
```java
          String.format("Schema File [%s] will be overwritten since already exists.", filePath));
      Files.delete(Paths.get(pmtFile.toURI()));
      pmtFile.createNewFile();
    }

```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.mkdirs()` is ignored
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/SchemaFile.java`
#### Snippet
```java
    if (!pmtFile.exists() || !pmtFile.isFile()) {
      File dir = SystemFileFactory.INSTANCE.getFile(dirPath);
      dir.mkdirs();
      pmtFile.createNewFile();
    }
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.createNewFile()` is ignored
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/SchemaFile.java`
#### Snippet
```java
      File dir = SystemFileFactory.INSTANCE.getFile(dirPath);
      dir.mkdirs();
      pmtFile.createNewFile();
    }

```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `FileInputStream.read()` is ignored
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/log/SchemaFileLogReader.java`
#### Snippet
```java
    byte[] tempBytes = new byte[SchemaFileConfig.PAGE_LENGTH];
    while (inputStream.available() > 0) {
      inputStream.read(tempBytes, 0, 1);

      if (tempBytes[0] == SchemaFileConfig.SF_COMMIT_MARK) {
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `FileInputStream.read()` is ignored
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/log/SchemaFileLogReader.java`
#### Snippet
```java

        // remove what is collected if commit mark followed, throw exception otherwise
        inputStream.read(tempBytes, 0, 1);
        if (tempBytes[0] == SchemaFileConfig.SF_COMMIT_MARK) {
          colBuffers.clear();
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.delete()` is ignored
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/snapshot/MemMTreeSnapshotUtil.java`
#### Snippet
```java
            snapshotTmp.getName(),
            snapshot.getName());
        snapshot.delete();
        return false;
      }
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.delete()` is ignored
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/snapshot/MemMTreeSnapshotUtil.java`
#### Snippet
```java
    } catch (IOException e) {
      logger.error("Failed to create mtree snapshot due to {}", e.getMessage(), e);
      snapshot.delete();
      return false;
    } finally {
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.delete()` is ignored
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/snapshot/MemMTreeSnapshotUtil.java`
#### Snippet
```java
      return false;
    } finally {
      snapshotTmp.delete();
    }
  }
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.delete()` is ignored
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/SchemaRegionUtils.java`
#### Snippet
```java
    final File storageGroupDir = schemaRegionDir.getParentFile();
    if (Objects.requireNonNull(storageGroupDir.listFiles()).length == 0) {
      storageGroupDir.delete();
    }
  }
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.createNewFile()` is ignored
in `confignode/src/main/java/org/apache/iotdb/confignode/consensus/statemachine/ConfigNodeRegionStateMachine.java`
#### Snippet
```java
    logFile = SystemFileFactory.INSTANCE.getFile(progressFilePath + endIndex);
    try {
      logFile.createNewFile();
      logWriter = new LogWriter(logFile, false);
      LOGGER.info("Create ConfigNode SimpleConsensusFile: {}", logFile.getAbsolutePath());
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.mkdirs()` is ignored
in `confignode/src/main/java/org/apache/iotdb/confignode/consensus/statemachine/ConfigNodeRegionStateMachine.java`
#### Snippet
```java
  private void initStandAloneConfigNode() {
    File dir = new File(currentFileDir);
    dir.mkdirs();
    String[] list = new File(currentFileDir).list();
    if (list != null && list.length != 0) {
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.delete()` is ignored
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/store/ProcedureWAL.java`
#### Snippet
```java
    } catch (IOException e) {
      LOG.error("Load {} failed, it will be deleted.", walFilePath, e);
      walFilePath.toFile().delete();
    }
  }
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `InputStream.skip()` is ignored
in `tsfile/src/main/java/org/apache/iotdb/tsfile/file/header/ChunkHeader.java`
#### Snippet
```java
      InputStream inputStream) throws IOException {
    ReadWriteForEncodingUtils.readUnsignedVarInt(inputStream);
    inputStream.skip(Byte.BYTES); // skip Data type
    CompressionType type = ReadWriteIOUtils.readCompressionType(inputStream);
    TSEncoding encoding = ReadWriteIOUtils.readEncoding(inputStream);
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.delete()` is ignored
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/TsFileGeneratorUtils.java`
#### Snippet
```java
    File file = fsFactory.getFile(filePath);
    if (file.exists()) {
      file.delete();
    }
    if (chunkGroupSize > 0)
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.delete()` is ignored
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/TsFileGeneratorUtils.java`
#### Snippet
```java
    File file = fsFactory.getFile(filePath);
    if (file.exists()) {
      file.delete();
    }
    int originGroupSize = TSFileDescriptor.getInstance().getConfig().getGroupSizeInByte();
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.delete()` is ignored
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/TsFileGeneratorUtils.java`
#### Snippet
```java
    File file = fsFactory.getFile(filePath);
    if (file.exists()) {
      file.delete();
    }
    if (chunkGroupSize > 0)
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.delete()` is ignored
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/TsFileGeneratorUtils.java`
#### Snippet
```java
    File file = fsFactory.getFile(filePath);
    if (file.exists()) {
      file.delete();
    }
    if (chunkGroupSize > 0)
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.delete()` is ignored
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/TsFileGeneratorUtils.java`
#### Snippet
```java
    File file = fsFactory.getFile(filePath);
    if (file.exists()) {
      file.delete();
    }
    if (chunkGroupSize > 0)
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.mkdirs()` is ignored
in `tsfile/src/main/java/org/apache/iotdb/tsfile/fileSystem/fsFactory/LocalFSFactory.java`
#### Snippet
```java
    File res = new File(pathname);
    if (!res.exists()) {
      res.getParentFile().mkdirs();
    }
    return res;
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.mkdirs()` is ignored
in `tsfile/src/main/java/org/apache/iotdb/tsfile/fileSystem/fileOutputFactory/LocalFSOutputFactory.java`
#### Snippet
```java
      File parentDirectory = file.getParentFile();
      if (parentDirectory != null) {
        file.getParentFile().mkdirs();
      }
      return new LocalTsFileOutput(new FileOutputStream(file, append));
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.mkdirs()` is ignored
in `tsfile/src/main/java/org/apache/iotdb/tsfile/fileSystem/fsFactory/HDFSFactory.java`
#### Snippet
```java
      File res = (File) constructorWithPathname.newInstance(pathname);
      if (!res.exists()) {
        res.getParentFile().mkdirs();
      }
      return res;
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.renameTo()` is ignored
in `integration-test/src/main/java/org/apache/iotdb/it/env/DataNodeWrapper.java`
#### Snippet
```java
    String newLogFilePath = getLogDirPath() + File.separator + getId() + ".log";
    File oldLogFile = new File(oldLogFilePath);
    oldLogFile.renameTo(new File(newLogFilePath));

    // rename node dir
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.renameTo()` is ignored
in `integration-test/src/main/java/org/apache/iotdb/it/env/DataNodeWrapper.java`
#### Snippet
```java
    String newNodeDirPath = getNodePath();
    File oldNodeDir = new File(oldNodeDirPath);
    oldNodeDir.renameTo(new File(newNodeDirPath));
  }

```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.renameTo()` is ignored
in `integration-test/src/main/java/org/apache/iotdb/it/env/ConfigNodeWrapper.java`
#### Snippet
```java
    File oldLogFile =
        new File(getLogDirPath() + File.separator + configNodeName + portList[0] + ".log");
    oldLogFile.renameTo(new File(getLogDirPath() + File.separator + getId() + ".log"));

    // rename node dir
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.renameTo()` is ignored
in `integration-test/src/main/java/org/apache/iotdb/it/env/ConfigNodeWrapper.java`
#### Snippet
```java
                + configNodeName
                + portList[0]);
    oldNodeDir.renameTo(new File(getNodePath()));
  }

```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.delete()` is ignored
in `example/hadoop/src/main/java/org/apache/iotdb/hadoop/tsfile/TsFileHelper.java`
#### Snippet
```java
    File file = new File(filePath);
    if (file.exists()) {
      file.delete();
    }
    writeTsFile(filePath);
```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.delete()` is ignored
in `example/hadoop/src/main/java/org/apache/iotdb/hadoop/tsfile/TsFileHelper.java`
#### Snippet
```java

      if (file.exists()) {
        file.delete();
      }

```

### RuleId[ruleID=IgnoreResultOfCall]
Result of `File.mkdirs()` is ignored
in `consensus/src/main/java/org/apache/iotdb/consensus/ratis/ApplicationStateMachineProxy.java`
#### Snippet
```java
    FileUtils.deleteFully(snapshotTmpDir);

    snapshotTmpDir.mkdirs();
    if (!snapshotTmpDir.isDirectory()) {
      logger.error("Unable to create temp snapshotDir at {}", snapshotTmpDir);
```

## RuleId[ruleID=AccessStaticViaInstance]
### RuleId[ruleID=AccessStaticViaInstance]
Static member 'org.apache.iotdb.db.query.executor.LastQueryExecutor.ascending' accessed via instance reference
in `server/src/main/java/org/apache/iotdb/db/query/executor/LastQueryExecutor.java`
#### Snippet
```java
    this.dataTypes = lastQueryPlan.getDeduplicatedDataTypes();
    this.expression = lastQueryPlan.getExpression();
    this.ascending = lastQueryPlan.isAscending();
  }

```

## RuleId[ruleID=UnnecessaryUnboxing]
### RuleId[ruleID=UnnecessaryUnboxing]
Unnecessary unboxing
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
      for (count = 0; count < maxvecs; count++) {
        v = vec.get(count);
        n = ((Integer) v[2]).intValue();
        c = getSuccessor(((Character) v[0]).charValue(), n);

```

### RuleId[ruleID=UnnecessaryUnboxing]
Unnecessary unboxing
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
        v = vec.get(count);
        n = ((Integer) v[2]).intValue();
        c = getSuccessor(((Character) v[0]).charValue(), n);

        if (!Character.isLetterOrDigit(separator)) {
```

### RuleId[ruleID=UnnecessaryUnboxing]
Unnecessary unboxing
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java

        if (!Character.isLetterOrDigit(separator)) {
          if ((c == ((Character) v[0]).charValue()) && (c != 'S')) {
            vecRemovelist.add(v);
          } else {
```

### RuleId[ruleID=UnnecessaryUnboxing]
Unnecessary unboxing
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
          ((StringBuilder) v[1]).append(c);

          if (c == ((Character) v[0]).charValue()) {
            v[2] = Integer.valueOf(n + 1);
          } else {
```

### RuleId[ruleID=UnnecessaryUnboxing]
Unnecessary unboxing
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
    for (int i = 0; i < size; i++) {
      v = vec.get(i);
      c = ((Character) v[0]).charValue();
      n = ((Integer) v[2]).intValue();

```

### RuleId[ruleID=UnnecessaryUnboxing]
Unnecessary unboxing
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
      v = vec.get(i);
      c = ((Character) v[0]).charValue();
      n = ((Integer) v[2]).intValue();

      boolean bk = getSuccessor(c, n) != c;
```

### RuleId[ruleID=UnnecessaryUnboxing]
Unnecessary unboxing
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
    if (parameterObj instanceof Boolean) {
      parameterAsNum =
          ((Boolean) parameterObj).booleanValue() ? Integer.valueOf(1) : Integer.valueOf(0);
    } else if (parameterObj instanceof String) {
      switch (targetSqlType) {
```

### RuleId[ruleID=UnnecessaryUnboxing]
Unnecessary unboxing
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
          case Types.BOOLEAN:
            if (parameterObj instanceof Boolean) {
              setBoolean(parameterIndex, ((Boolean) parameterObj).booleanValue());
              break;
            } else if (parameterObj instanceof String) {
```

## RuleId[ruleID=RedundantMethodOverride]
### RuleId[ruleID=RedundantMethodOverride]
Method `getPaths()` is identical to its super method
in `server/src/main/java/org/apache/iotdb/db/qp/physical/sys/MeasurementMNodePlan.java`
#### Snippet
```java

  @Override
  public List<PartialPath> getPaths() {
    return new ArrayList<>();
  }
```

### RuleId[ruleID=RedundantMethodOverride]
Method `getPaths()` is identical to its super method
in `server/src/main/java/org/apache/iotdb/db/qp/physical/sys/StorageGroupMNodePlan.java`
#### Snippet
```java

  @Override
  public List<PartialPath> getPaths() {
    return new ArrayList<>();
  }
```

### RuleId[ruleID=RedundantMethodOverride]
Method `visitSort()` only delegates to its super method
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/OperatorTreeGenerator.java`
#### Snippet
```java

  @Override
  public Operator visitSort(SortNode node, LocalExecutionPlanContext context) {
    return super.visitSort(node, context);
  }
```

### RuleId[ruleID=RedundantMethodOverride]
Method `getScanOrder()` is identical to its super method
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/source/AlignedSeriesAggregationScanNode.java`
#### Snippet
```java
  }

  public Ordering getScanOrder() {
    return scanOrder;
  }
```

### RuleId[ruleID=RedundantMethodOverride]
Method `getTimeFilter()` is identical to its super method
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/source/AlignedSeriesAggregationScanNode.java`
#### Snippet
```java

  @Nullable
  public Filter getTimeFilter() {
    return timeFilter;
  }
```

### RuleId[ruleID=RedundantMethodOverride]
Method `getGroupByTimeParameter()` is identical to its super method
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/source/AlignedSeriesAggregationScanNode.java`
#### Snippet
```java

  @Nullable
  public GroupByTimeParameter getGroupByTimeParameter() {
    return groupByTimeParameter;
  }
```

### RuleId[ruleID=RedundantMethodOverride]
Method `getScanOrder()` is identical to its super method
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/source/SeriesAggregationScanNode.java`
#### Snippet
```java
  }

  public Ordering getScanOrder() {
    return scanOrder;
  }
```

### RuleId[ruleID=RedundantMethodOverride]
Method `getTimeFilter()` is identical to its super method
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/source/SeriesAggregationScanNode.java`
#### Snippet
```java

  @Nullable
  public Filter getTimeFilter() {
    return timeFilter;
  }
```

### RuleId[ruleID=RedundantMethodOverride]
Method `getGroupByTimeParameter()` is identical to its super method
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/source/SeriesAggregationScanNode.java`
#### Snippet
```java

  @Nullable
  public GroupByTimeParameter getGroupByTimeParameter() {
    return groupByTimeParameter;
  }
```

### RuleId[ruleID=RedundantMethodOverride]
Method `setChildren()` is identical to its super method
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/process/last/LastQueryMergeNode.java`
#### Snippet
```java
  }

  public void setChildren(List<PlanNode> children) {
    this.children = children;
  }
```

### RuleId[ruleID=RedundantMethodOverride]
Method `addChild()` is identical to its super method
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/process/last/LastQueryCollectNode.java`
#### Snippet
```java

  @Override
  public void addChild(PlanNode child) {
    children.add(child);
  }
```

### RuleId[ruleID=RedundantMethodOverride]
Method `allowedChildCount()` is identical to its super method
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/process/last/LastQueryCollectNode.java`
#### Snippet
```java

  @Override
  public int allowedChildCount() {
    return CHILD_COUNT_NO_LIMIT;
  }
```

### RuleId[ruleID=RedundantMethodOverride]
Method `setChildren()` is identical to its super method
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/process/last/LastQueryCollectNode.java`
#### Snippet
```java
  }

  public void setChildren(List<PlanNode> children) {
    this.children = children;
  }
```

### RuleId[ruleID=RedundantMethodOverride]
Method `equals()` only delegates to its super method
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/process/last/LastQueryCollectNode.java`
#### Snippet
```java

  @Override
  public boolean equals(Object o) {
    return super.equals(o);
  }
```

### RuleId[ruleID=RedundantMethodOverride]
Method `getChildren()` is identical to its super method
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/process/last/LastQueryCollectNode.java`
#### Snippet
```java

  @Override
  public List<PlanNode> getChildren() {
    return children;
  }
```

### RuleId[ruleID=RedundantMethodOverride]
Method `addChild()` is identical to its super method
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/process/last/LastQueryMergeNode.java`
#### Snippet
```java

  @Override
  public void addChild(PlanNode child) {
    children.add(child);
  }
```

### RuleId[ruleID=RedundantMethodOverride]
Method `allowedChildCount()` is identical to its super method
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/process/last/LastQueryMergeNode.java`
#### Snippet
```java

  @Override
  public int allowedChildCount() {
    return CHILD_COUNT_NO_LIMIT;
  }
```

### RuleId[ruleID=RedundantMethodOverride]
Method `getChildren()` is identical to its super method
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/process/last/LastQueryMergeNode.java`
#### Snippet
```java

  @Override
  public List<PlanNode> getChildren() {
    return children;
  }
```

### RuleId[ruleID=RedundantMethodOverride]
Method `addChild()` is identical to its super method
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/process/last/LastQueryNode.java`
#### Snippet
```java

  @Override
  public void addChild(PlanNode child) {
    children.add(child);
  }
```

### RuleId[ruleID=RedundantMethodOverride]
Method `setChildren()` is identical to its super method
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/process/last/LastQueryNode.java`
#### Snippet
```java
  }

  public void setChildren(List<PlanNode> children) {
    this.children = children;
  }
```

### RuleId[ruleID=RedundantMethodOverride]
Method `getChildren()` is identical to its super method
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/process/last/LastQueryNode.java`
#### Snippet
```java

  @Override
  public List<PlanNode> getChildren() {
    return children;
  }
```

### RuleId[ruleID=RedundantMethodOverride]
Method `allowedChildCount()` is identical to its super method
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/process/last/LastQueryNode.java`
#### Snippet
```java

  @Override
  public int allowedChildCount() {
    return CHILD_COUNT_NO_LIMIT;
  }
```

### RuleId[ruleID=RedundantMethodOverride]
Method `addChild()` is identical to its super method
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/metedata/read/SchemaQueryOrderByHeatNode.java`
#### Snippet
```java

  @Override
  public void addChild(PlanNode child) {
    children.add(child);
  }
```

### RuleId[ruleID=RedundantMethodOverride]
Method `getChildren()` is identical to its super method
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/metedata/read/SchemaQueryOrderByHeatNode.java`
#### Snippet
```java

  @Override
  public List<PlanNode> getChildren() {
    return children;
  }
```

### RuleId[ruleID=RedundantMethodOverride]
Method `allowedChildCount()` is identical to its super method
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/metedata/read/SchemaQueryOrderByHeatNode.java`
#### Snippet
```java

  @Override
  public int allowedChildCount() {
    return CHILD_COUNT_NO_LIMIT;
  }
```

### RuleId[ruleID=RedundantMethodOverride]
Method `getPaths()` is identical to its super method
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/statement/metadata/ShowTriggersStatement.java`
#### Snippet
```java

  @Override
  public List<PartialPath> getPaths() {
    return Collections.emptyList();
  }
```

### RuleId[ruleID=RedundantMethodOverride]
Method `getPaths()` is identical to its super method
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/statement/metadata/ShowContinuousQueriesStatement.java`
#### Snippet
```java

  @Override
  public List<PartialPath> getPaths() {
    return Collections.emptyList();
  }
```

### RuleId[ruleID=RedundantMethodOverride]
Method `getPaths()` is identical to its super method
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/statement/metadata/ShowFunctionsStatement.java`
#### Snippet
```java

  @Override
  public List<PartialPath> getPaths() {
    return Collections.emptyList();
  }
```

### RuleId[ruleID=RedundantMethodOverride]
Method `isBlocked()` only delegates to its super method
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/SortOperator.java`
#### Snippet
```java

  @Override
  public ListenableFuture<?> isBlocked() {
    return ProcessOperator.super.isBlocked();
  }
```

### RuleId[ruleID=RedundantMethodOverride]
Method `close()` only delegates to its super method
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/SortOperator.java`
#### Snippet
```java

  @Override
  public void close() throws Exception {
    ProcessOperator.super.close();
  }
```

### RuleId[ruleID=RedundantMethodOverride]
Method `reset()` only delegates to its super method
in `server/src/main/java/org/apache/iotdb/db/mpp/aggregation/LastValueDescAccumulator.java`
#### Snippet
```java

  @Override
  public void reset() {
    super.reset();
  }
```

### RuleId[ruleID=RedundantMethodOverride]
Method `close()` only delegates to its super method
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/DeletionOpBlock.java`
#### Snippet
```java
  /** release the current class object's resource */
  @Override
  public void close() {
    super.close();
  }
```

### RuleId[ruleID=RedundantMethodOverride]
Method `stop()` is identical to its super method
in `server/src/main/java/org/apache/iotdb/db/query/pool/RawQueryReadTaskPoolManager.java`
#### Snippet
```java

  @Override
  public void stop() {
    if (pool != null) {
      close();
```

### RuleId[ruleID=RedundantMethodOverride]
Method `stop()` is identical to its super method
in `server/src/main/java/org/apache/iotdb/db/query/pool/QueryTaskManager.java`
#### Snippet
```java

  @Override
  public void stop() {
    if (pool != null) {
      close();
```

### RuleId[ruleID=RedundantMethodOverride]
Method `getAllSatisfiedPageData()` only delegates to its super method
in `server/src/main/java/org/apache/iotdb/db/query/reader/chunk/MemAlignedPageReader.java`
#### Snippet
```java

  @Override
  public BatchData getAllSatisfiedPageData() throws IOException {
    return IPageReader.super.getAllSatisfiedPageData();
  }
```

### RuleId[ruleID=RedundantMethodOverride]
Method `getDataCount()` is identical to its super method
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/TsFileOpBlock.java`
#### Snippet
```java
   */
  @Override
  public long getDataCount() {
    return dataCount;
  }
```

### RuleId[ruleID=RedundantMethodOverride]
Method `isEmpty()` is identical to its super method
in `server/src/main/java/org/apache/iotdb/db/engine/querycontext/AlignedReadOnlyMemChunk.java`
#### Snippet
```java

  @Override
  public boolean isEmpty() throws IOException {
    return tsBlock.isEmpty();
  }
```

### RuleId[ruleID=RedundantMethodOverride]
Method `createContext()` only delegates to its super method
in `server/src/main/java/org/apache/iotdb/db/service/thrift/handler/InfluxDBServiceThriftHandler.java`
#### Snippet
```java

  @Override
  public ServerContext createContext(TProtocol in, TProtocol out) {
    // nothing
    return super.createContext(in, out);
```

### RuleId[ruleID=RedundantMethodOverride]
Method `moveDataToNewMNode()` only delegates to its super method
in `server/src/main/java/org/apache/iotdb/db/metadata/mnode/StorageGroupMNode.java`
#### Snippet
```java

  @Override
  public void moveDataToNewMNode(IMNode newMNode) {
    super.moveDataToNewMNode(newMNode);
  }
```

### RuleId[ruleID=RedundantMethodOverride]
Method `getFullPath()` is identical to its super method
in `server/src/main/java/org/apache/iotdb/db/metadata/mnode/StorageGroupEntityMNode.java`
#### Snippet
```java

  @Override
  public String getFullPath() {
    if (fullPath == null) {
      fullPath = concatFullPath().intern();
```

### RuleId[ruleID=RedundantMethodOverride]
Method `moveDataToNewMNode()` only delegates to its super method
in `server/src/main/java/org/apache/iotdb/db/metadata/mnode/StorageGroupEntityMNode.java`
#### Snippet
```java

  @Override
  public void moveDataToNewMNode(IMNode newMNode) {
    super.moveDataToNewMNode(newMNode);
  }
```

### RuleId[ruleID=RedundantMethodOverride]
Method `getSchemaTemplateId()` is identical to its super method
in `server/src/main/java/org/apache/iotdb/db/metadata/mnode/EntityMNode.java`
#### Snippet
```java
   */
  @Override
  public int getSchemaTemplateId() {
    return schemaTemplateId >= -1 ? schemaTemplateId : -schemaTemplateId - 2;
  }
```

### RuleId[ruleID=RedundantMethodOverride]
Method `shouldRetry()` only delegates to its super method
in `server/src/main/java/org/apache/iotdb/db/consensus/statemachine/SchemaRegionStateMachine.java`
#### Snippet
```java

  @Override
  public boolean shouldRetry(TSStatus writeResult) {
    // TODO implement this
    return super.shouldRetry(writeResult);
```

### RuleId[ruleID=RedundantMethodOverride]
Method `getSleepTime()` only delegates to its super method
in `server/src/main/java/org/apache/iotdb/db/consensus/statemachine/SchemaRegionStateMachine.java`
#### Snippet
```java

  @Override
  public long getSleepTime() {
    // TODO implement this
    return super.getSleepTime();
```

### RuleId[ruleID=RedundantMethodOverride]
Method `updateResult()` only delegates to its super method
in `server/src/main/java/org/apache/iotdb/db/consensus/statemachine/SchemaRegionStateMachine.java`
#### Snippet
```java

  @Override
  public TSStatus updateResult(TSStatus previousResult, TSStatus retryResult) {
    // TODO implement this
    return super.updateResult(previousResult, retryResult);
```

### RuleId[ruleID=RedundantMethodOverride]
Method `updateResult()` only delegates to its super method
in `server/src/main/java/org/apache/iotdb/db/consensus/statemachine/DataRegionStateMachine.java`
#### Snippet
```java

  @Override
  public TSStatus updateResult(TSStatus previousResult, TSStatus retryResult) {
    // TODO implement this
    return super.updateResult(previousResult, retryResult);
```

### RuleId[ruleID=RedundantMethodOverride]
Method `shouldRetry()` only delegates to its super method
in `server/src/main/java/org/apache/iotdb/db/consensus/statemachine/DataRegionStateMachine.java`
#### Snippet
```java

  @Override
  public boolean shouldRetry(TSStatus writeResult) {
    // TODO implement this
    return super.shouldRetry(writeResult);
```

### RuleId[ruleID=RedundantMethodOverride]
Method `getSleepTime()` only delegates to its super method
in `server/src/main/java/org/apache/iotdb/db/consensus/statemachine/DataRegionStateMachine.java`
#### Snippet
```java

  @Override
  public long getSleepTime() {
    // TODO implement this
    return super.getSleepTime();
```

### RuleId[ruleID=RedundantMethodOverride]
Method `getModificationsForMemtable()` is identical to its super method
in `server/src/main/java/org/apache/iotdb/db/metadata/utils/ResourceByPathUtils.java`
#### Snippet
```java
  /** get modifications from a memtable. */
  @Override
  protected List<Modification> getModificationsForMemtable(
      IMemTable memTable, List<Pair<Modification, IMemTable>> modsToMemtable) {
    List<Modification> modifications = new ArrayList<>();
```

### RuleId[ruleID=RedundantMethodOverride]
Method `terminate()` is identical to its super method
in `library-udf/src/main/java/org/apache/iotdb/library/anomaly/UDTFTwoSidedFilter.java`
#### Snippet
```java

  @Override
  public void terminate(PointCollector collector) throws Exception {}
}

```

### RuleId[ruleID=RedundantMethodOverride]
Method `terminate()` is identical to its super method
in `library-udf/src/main/java/org/apache/iotdb/library/anomaly/UDTFLOF.java`
#### Snippet
```java

  @Override
  public void terminate(PointCollector collector) throws Exception {}
}

```

### RuleId[ruleID=RedundantMethodOverride]
Method `terminate()` is identical to its super method
in `library-udf/src/main/java/org/apache/iotdb/library/anomaly/UDTFRange.java`
#### Snippet
```java

  @Override
  public void terminate(PointCollector collector) throws Exception {}
}

```

### RuleId[ruleID=RedundantMethodOverride]
Method `terminate()` is identical to its super method
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/UDTFMvAvg.java`
#### Snippet
```java

  @Override
  public void terminate(PointCollector collector) throws Exception {}
}

```

### RuleId[ruleID=RedundantMethodOverride]
Method `getTime()` only delegates to its super method
in `library-udf/src/main/java/org/apache/iotdb/library/drepair/util/ScreenFill.java`
#### Snippet
```java

  @Override
  public long[] getTime() {
    return super.getTime();
  }
```

### RuleId[ruleID=RedundantMethodOverride]
Method `getFilled()` only delegates to its super method
in `library-udf/src/main/java/org/apache/iotdb/library/drepair/util/ScreenFill.java`
#### Snippet
```java

  @Override
  public double[] getFilled() {
    return super.getFilled();
  }
```

### RuleId[ruleID=RedundantMethodOverride]
Method `calMeanAndVar()` only delegates to its super method
in `library-udf/src/main/java/org/apache/iotdb/library/drepair/util/ScreenFill.java`
#### Snippet
```java

  @Override
  public void calMeanAndVar() throws UDFException {
    super.calMeanAndVar();
  }
```

### RuleId[ruleID=RedundantMethodOverride]
Method `getSleepTime()` only delegates to its super method
in `confignode/src/main/java/org/apache/iotdb/confignode/consensus/statemachine/ConfigNodeRegionStateMachine.java`
#### Snippet
```java

  @Override
  public long getSleepTime() {
    // TODO implement this
    return RetryPolicy.super.getSleepTime();
```

### RuleId[ruleID=RedundantMethodOverride]
Method `updateResult()` only delegates to its super method
in `confignode/src/main/java/org/apache/iotdb/confignode/consensus/statemachine/ConfigNodeRegionStateMachine.java`
#### Snippet
```java

  @Override
  public TSStatus updateResult(TSStatus previousResult, TSStatus retryResult) {
    // TODO implement this
    return RetryPolicy.super.updateResult(previousResult, retryResult);
```

### RuleId[ruleID=RedundantMethodOverride]
Method `shouldRetry()` only delegates to its super method
in `confignode/src/main/java/org/apache/iotdb/confignode/consensus/statemachine/ConfigNodeRegionStateMachine.java`
#### Snippet
```java

  @Override
  public boolean shouldRetry(TSStatus writeResult) {
    // TODO implement this
    return RetryPolicy.super.shouldRetry(writeResult);
```

### RuleId[ruleID=RedundantMethodOverride]
Method `isRollbackSupported()` is identical to its super method
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/impl/node/RemoveDataNodeProcedure.java`
#### Snippet
```java

  @Override
  protected boolean isRollbackSupported(RemoveDataNodeState state) {
    return false;
  }
```

### RuleId[ruleID=RedundantMethodOverride]
Method `isRollbackSupported()` is identical to its super method
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/impl/statemachine/RegionMigrateProcedure.java`
#### Snippet
```java

  @Override
  protected boolean isRollbackSupported(RegionTransitionState state) {
    return false;
  }
```

### RuleId[ruleID=RedundantMethodOverride]
Method `getSinglePath()` is identical to its super method
in `spark-tsfile/src/main/java/org/apache/iotdb/spark/tsfile/qp/common/BasicOperator.java`
#### Snippet
```java

  @Override
  public String getSinglePath() {
    return singlePath;
  }
```

### RuleId[ruleID=RedundantMethodOverride]
Method `isMeasurement()` is identical to its super method
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/mnode/RStorageGroupMNode.java`
#### Snippet
```java

  @Override
  public boolean isMeasurement() {
    return false;
  }
```

### RuleId[ruleID=RedundantMethodOverride]
Method `isEntity()` is identical to its super method
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/mnode/RStorageGroupMNode.java`
#### Snippet
```java

  @Override
  public boolean isEntity() {
    return false;
  }
```

## RuleId[ruleID=ClassNameSameAsAncestorName]
### RuleId[ruleID=ClassNameSameAsAncestorName]
Class name `WALManager` is the same as one of its superclass' names
in `schema-engine-tag/src/main/java/org/apache/iotdb/db/metadata/tagSchemaRegion/tagIndex/wal/WALManager.java`
#### Snippet
```java

/** Manage wal entry writes and reads */
public class WALManager extends org.apache.iotdb.lsm.manager.WALManager {

  private static final Logger logger = LoggerFactory.getLogger(WALManager.class);
```

### RuleId[ruleID=ClassNameSameAsAncestorName]
Class name `Factory` is the same as one of its superclass' names
in `service-rpc/src/main/java/org/apache/iotdb/rpc/TSnappyElasticFramedTransport.java`
#### Snippet
```java
public class TSnappyElasticFramedTransport extends TCompressedElasticFramedTransport {

  public static class Factory extends TElasticFramedTransport.Factory {

    public Factory() {
```

### RuleId[ruleID=ClassNameSameAsAncestorName]
Class name `JmxConfig` is the same as one of its superclass' names
in `metrics/micrometer-metrics/src/main/java/org/apache/iotdb/metrics/micrometer/reporter/JmxConfig.java`
#### Snippet
```java
package org.apache.iotdb.metrics.micrometer.reporter;

public interface JmxConfig extends io.micrometer.jmx.JmxConfig {
  JmxConfig DEFAULT = k -> null;

```

## RuleId[ruleID=IntegerMultiplicationImplicitCastToLong]
### RuleId[ruleID=IntegerMultiplicationImplicitCastToLong]
(end - start) \* insertTabletNode.getDataTypes()\[i\].getDataTypeSize(): integer multiplication implicitly cast to long
in `server/src/main/java/org/apache/iotdb/db/utils/MemUtils.java`
#### Snippet
```java
        }
      } else {
        memSize += (end - start) * insertTabletNode.getDataTypes()[i].getDataTypeSize();
      }
    }
```

### RuleId[ruleID=IntegerMultiplicationImplicitCastToLong]
(end - start) \* RamUsageEstimator.NUM_BYTES_OBJECT_HEADER: integer multiplication implicitly cast to long
in `server/src/main/java/org/apache/iotdb/db/utils/MemUtils.java`
#### Snippet
```java
  public static long getBinaryColumnSize(Binary[] column, int start, int end) {
    long memSize = 0;
    memSize += (end - start) * RamUsageEstimator.NUM_BYTES_OBJECT_HEADER;
    for (int i = start; i < end; i++) {
      memSize += RamUsageEstimator.sizeOf(column[i].getValues());
```

### RuleId[ruleID=IntegerMultiplicationImplicitCastToLong]
str.length() \* 2: integer multiplication implicitly cast to long
in `server/src/main/java/org/apache/iotdb/db/utils/MemUtils.java`
#### Snippet
```java
  public static long getStringMem(String str) {
    // wide char (2 bytes each) and 64B String overhead
    return str.length() * 2 + 64L;
  }

```

### RuleId[ruleID=IntegerMultiplicationImplicitCastToLong]
TSFileConfig.MAGIC_STRING.getBytes().length \* 2: integer multiplication implicitly cast to long
in `tsfile/src/main/java/org/apache/iotdb/tsfile/v2/read/TsFileSequenceReaderForV2.java`
#### Snippet
```java
  public boolean isComplete() throws IOException {
    return tsFileInput.size()
            >= TSFileConfig.MAGIC_STRING.getBytes().length * 2
                + TSFileConfig.VERSION_NUMBER_V2.getBytes().length
        && (readTailMagic().equals(readHeadMagic()));
```

### RuleId[ruleID=IntegerMultiplicationImplicitCastToLong]
(bytes\[length - 1 - i\] \& 0xFF) \<\< (i \* 8): integer shift implicitly cast to long
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BytesUtils.java`
#### Snippet
```java
    long r = 0;
    for (int i = 0; i < length; i++) {
      r += ((bytes[length - 1 - i] & 0xFF) << (i * 8));
    }

```

### RuleId[ruleID=IntegerMultiplicationImplicitCastToLong]
TSFileConfig.MAGIC_STRING.getBytes().length \* 2: integer multiplication implicitly cast to long
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/TsFileSequenceReader.java`
#### Snippet
```java
    // TSFileConfig.MAGIC_STRING.getBytes().length * 2 for two magic string
    // Byte.BYTES for the file version number
    if (size >= TSFileConfig.MAGIC_STRING.getBytes().length * 2 + Byte.BYTES) {
      String tailMagic = readTailMagic();
      String headMagic = readHeadMagic();
```

### RuleId[ruleID=IntegerMultiplicationImplicitCastToLong]
((values.size() + 7) / 8 + 4) \* values.size(): integer multiplication implicitly cast to long
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/encoder/BitmapEncoder.java`
#### Snippet
```java
  public long getMaxByteSize() {
    // byteCacheSize + byteDictSize + (byte array + array length) * byteDictSize
    return (long) 4 + 4 + ((values.size() + 7) / 8 + 4) * values.size();
  }
}
```

### RuleId[ruleID=IntegerMultiplicationImplicitCastToLong]
values.size() \* 4: integer multiplication implicitly cast to long
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/encoder/LongZigzagEncoder.java`
#### Snippet
```java
    }
    // try to caculate max value
    return (long) 8 + values.size() * 4;
  }
}
```

### RuleId[ruleID=IntegerMultiplicationImplicitCastToLong]
writeIndex \* 4: integer multiplication implicitly cast to long
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/encoder/DeltaBinaryEncoder.java`
#### Snippet
```java
    public long getMaxByteSize() {
      // The meaning of 24 is: index(4)+width(4)+minDeltaBase(4)+firstValue(4)
      return (long) 24 + writeIndex * 4;
    }
  }
```

### RuleId[ruleID=IntegerMultiplicationImplicitCastToLong]
writeIndex \* 8: integer multiplication implicitly cast to long
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/encoder/DeltaBinaryEncoder.java`
#### Snippet
```java
    public long getMaxByteSize() {
      // The meaning of 24 is: index(4)+width(4)+minDeltaBase(8)+firstValue(8)
      return (long) 24 + writeIndex * 8;
    }

```

### RuleId[ruleID=IntegerMultiplicationImplicitCastToLong]
values.size() \* 4: integer multiplication implicitly cast to long
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/encoder/IntZigzagEncoder.java`
#### Snippet
```java
    }
    // try to caculate max value
    return (long) 8 + values.size() * 4;
  }
}
```

### RuleId[ruleID=IntegerMultiplicationImplicitCastToLong]
values.size() \* 8: integer multiplication implicitly cast to long
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/encoder/LongRleEncoder.java`
#### Snippet
```java
    // try to caculate max value
    int groupNum = (values.size() / 8 + 1) / 63 + 1;
    return (long) 8 + groupNum * 5 + values.size() * 8;
  }
}
```

### RuleId[ruleID=IntegerMultiplicationImplicitCastToLong]
values.size() \* 4: integer multiplication implicitly cast to long
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/encoder/IntRleEncoder.java`
#### Snippet
```java
    // try to caculate max value
    int groupNum = (values.size() / 8 + 1) / 63 + 1;
    return (long) 8 + groupNum * 5 + values.size() * 4;
  }
}
```

### RuleId[ruleID=IntegerMultiplicationImplicitCastToLong]
1 \<\< totalBits: integer shift implicitly cast to long
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/bitpacking/IntPacker.java`
#### Snippet
```java
        valueIdx++;
        totalBits -= width;
        buffer = buffer & ((1 << totalBits) - 1);
      }
    }
```

### RuleId[ruleID=IntegerMultiplicationImplicitCastToLong]
writeIndex \* 2: integer multiplication implicitly cast to long
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/encoder/RegularDataEncoder.java`
#### Snippet
```java
      // The meaning of 20 is:
      // identifier(4)+bitmapLength(4)+index(4)+minDeltaBase(8)+firstValue(8)
      return (long) 28 + (writeIndex * 2 / 8) + (writeIndex * 8);
    }

```

### RuleId[ruleID=IntegerMultiplicationImplicitCastToLong]
writeIndex \* 8: integer multiplication implicitly cast to long
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/encoder/RegularDataEncoder.java`
#### Snippet
```java
      // The meaning of 20 is:
      // identifier(4)+bitmapLength(4)+index(4)+minDeltaBase(8)+firstValue(8)
      return (long) 28 + (writeIndex * 2 / 8) + (writeIndex * 8);
    }

```

### RuleId[ruleID=IntegerMultiplicationImplicitCastToLong]
writeIndex \* 2: integer multiplication implicitly cast to long
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/encoder/RegularDataEncoder.java`
#### Snippet
```java
      // The meaning of 20 is:
      // identifier(4)+bitmapLength(4)+index(4)+minDeltaBase(4)+firstValue(4)
      return (long) 20 + (writeIndex * 2 / 8) + (writeIndex * 4);
    }

```

### RuleId[ruleID=IntegerMultiplicationImplicitCastToLong]
writeIndex \* 4: integer multiplication implicitly cast to long
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/encoder/RegularDataEncoder.java`
#### Snippet
```java
      // The meaning of 20 is:
      // identifier(4)+bitmapLength(4)+index(4)+minDeltaBase(4)+firstValue(4)
      return (long) 20 + (writeIndex * 2 / 8) + (writeIndex * 4);
    }

```

### RuleId[ruleID=IntegerMultiplicationImplicitCastToLong]
13 \* writeIndex: integer multiplication implicitly cast to long
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/encoder/FreqEncoder.java`
#### Snippet
```java
  @Override
  public long getMaxByteSize() {
    return 8 + 13 * writeIndex;
  }

```

## RuleId[ruleID=NestedAssignment]
### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `node-commons/src/main/java/org/apache/iotdb/commons/sync/persistence/SyncLogReader.java`
#### Snippet
```java
  private void recoverPipe(InputStream inputStream) throws IOException {
    byte nextByte;
    while ((nextByte = ReadWriteIOUtils.readByte(inputStream)) != -1) {
      SyncOperation operationType = SyncOperation.values()[nextByte];
      switch (operationType) {
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/FileUtils.java`
#### Snippet
```java
          byte[] bytes = new byte[bufferSize];
          int size = 0;
          while ((size = in.read(bytes)) > 0) {
            out.write(bytes, 0, size);
          }
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
    }

    while ((z = reader.read()) != -1) {
      separator = (char) z;
      maxvecs = vec.size();
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `cli/src/main/java/org/apache/iotdb/tool/ExportCsv.java`
#### Snippet
```java
      String sql;
      int index = 0;
      while ((sql = reader.readLine()) != null) {
        dumpResult(sql, index);
        index++;
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `cli/src/main/java/org/apache/iotdb/tool/ExportTsFile.java`
#### Snippet
```java
      String sql;
      int i = 0;
      while ((sql = reader.readLine()) != null) {
        legalCheck(sql);
        dumpResult(sql, i++);
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileSequenceRead.java`
#### Snippet
```java
      int pageIndex = 0;
      byte marker;
      while ((marker = reader.readMarker()) != MetaMarker.SEPARATOR) {
        switch (marker) {
          case MetaMarker.CHUNK_HEADER:
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `integration/src/main/java/org/apache/iotdb/integration/env/ClusterEnvBase.java`
#### Snippet
```java
        BufferedReader br = new BufferedReader(new InputStreamReader(proc.getInputStream()));
        String line;
        while ((line = br.readLine()) != null) {
          System.out.println(line);
        }
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/source/AlignedSeriesScanOperator.java`
#### Snippet
```java
  @Override
  public boolean isFinished() {
    return finished || (finished = !hasNext());
  }

```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/source/AbstractSeriesAggregationScanOperator.java`
#### Snippet
```java
  @Override
  public boolean isFinished() {
    return finished || (finished = !hasNext());
  }

```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/source/SeriesScanOperator.java`
#### Snippet
```java
  @Override
  public boolean isFinished() {
    return finished || (finished = !hasNext());
  }

```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/TransformOperator.java`
#### Snippet
```java
    // If keepNull is false, we must iterate the reader until a non-null row is returned.
    YieldableState yieldableState;
    while ((yieldableState = reader.yield()) == YieldableState.YIELDABLE) {
      if (reader.isCurrentNull() && !keepNull) {
        reader.readyForNext();
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/input/QueryDataSetInputLayer.java`
#### Snippet
```java
      YieldableState yieldableState;
      while (YieldableState.YIELDABLE.equals(
          yieldableState = queryDataSet.canYieldNextRowInObjects())) {
        Object[] rowRecordCandidate = queryDataSet.nextRowInObjects();
        rowRecordList.put(rowRecordCandidate);
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `server/src/main/java/org/apache/iotdb/db/sync/pipedata/queue/BufferedPipeDataQueue.java`
#### Snippet
```java
    try {
      synchronized (waitLock) {
        while ((pipeData = pullOnePipeData(commitSerialNumber)) == null) {
          waitLock.wait();
          waitLock.notifyAll();
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
    if (enableRedirection
        && !deviceIdToEndpoint.isEmpty()
        && (endPoint = deviceIdToEndpoint.get(deviceId)) != null) {
      return endPointToSessionConnection.get(endPoint);
    } else {
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `server/src/main/java/org/apache/iotdb/db/tools/watermark/WatermarkDetector.java`
#### Snippet
```java
        throw new IOException("columnIndex is out of range.");
      }
      while ((line = reader.readLine()) != null) {
        items = line.split(",");
        long timestamp = parseTimestamp(items[0]);
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `server/src/main/java/org/apache/iotdb/db/tools/settle/TsFileAndModSettleTool.java`
#### Snippet
```java
                  FSFactoryProducer.getFSFactory().getFile(SettleLog.getSettleLogPath())))) {
        String line = null;
        while ((line = settleLogReader.readLine()) != null && !"".equals(line)) {
          String oldFilePath = line.split(SettleLog.COMMA_SEPERATOR)[0];
          int settleCheckStatus = Integer.parseInt(line.split(SettleLog.COMMA_SEPERATOR)[1]);
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `server/src/main/java/org/apache/iotdb/db/tools/upgrade/TsFileOnlineUpgradeTool.java`
#### Snippet
```java
    long chunkHeaderOffset;
    try {
      while ((marker = reader.readMarker()) != MetaMarker.SEPARATOR) {
        switch (marker) {
          case MetaMarker.CHUNK_HEADER:
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileSplitByPartitionTool.java`
#### Snippet
```java
    long chunkHeaderOffset;
    try {
      while ((marker = reader.readMarker()) != MetaMarker.SEPARATOR) {
        switch (marker) {
          case MetaMarker.CHUNK_GROUP_HEADER:
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `server/src/main/java/org/apache/iotdb/db/utils/OpenFileNumUtil.java`
#### Snippet
```java

      try (BufferedReader in = new BufferedReader(new InputStreamReader(pro.getInputStream()))) {
        while ((line = in.readLine()) != null) {
          lineCount++;
          countOneFile(line, pid, resultMap);
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `server/src/main/java/org/apache/iotdb/db/utils/OpenFileNumUtil.java`
#### Snippet
```java
        BufferedReader in1 = new BufferedReader(new InputStreamReader(pro1.getInputStream()));
        String line;
        while ((line = in1.readLine()) != null) {
          line = line.trim();
          String[] temp = line.split("\\s+");
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `server/src/main/java/org/apache/iotdb/db/utils/UpgradeUtils.java`
#### Snippet
```java
                  FSFactoryProducer.getFSFactory().getFile(UpgradeLog.getUpgradeLogPath())))) {
        String line = null;
        while ((line = upgradeLogReader.readLine()) != null) {
          String oldFilePath = line.split(COMMA_SEPERATOR)[0];
          String oldFileName = new File(oldFilePath).getName();
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `server/src/main/java/org/apache/iotdb/db/tools/validate/TsFileValidationTool.java`
#### Snippet
```java

          // start reading data points in sequence
          while ((marker = reader.readMarker()) != MetaMarker.SEPARATOR) {
            switch (marker) {
              case MetaMarker.CHUNK_HEADER:
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `server/src/main/java/org/apache/iotdb/db/engine/load/TsFileSplitter.java`
#### Snippet
```java
      getChunkMetadata(reader, offset2ChunkMetadata);
      byte marker;
      while ((marker = reader.readMarker()) != MetaMarker.SEPARATOR) {
        switch (marker) {
          case MetaMarker.CHUNK_HEADER:
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `server/src/main/java/org/apache/iotdb/db/engine/snapshot/SnapshotLogAnalyzer.java`
#### Snippet
```java
      snapshotId = reader.readLine();
      String line;
      while ((line = reader.readLine()) != null && !line.equals(SnapshotLogger.END_FLAG)) {
        fileInfoSet.add(line);
      }
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/log/CompactionLogAnalyzer.java`
#### Snippet
```java
    boolean isSeqSource = true;
    try (BufferedReader bufferedReader = new BufferedReader(new FileReader(logFile))) {
      while ((currLine = bufferedReader.readLine()) != null) {
        if (currLine.equals(STR_UNSEQ_FILES_FROM_OLD)) {
          isSeqSource = false;
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/log/CompactionLogAnalyzer.java`
#### Snippet
```java
    String currLine;
    try (BufferedReader bufferedReader = new BufferedReader(new FileReader(logFile))) {
      while ((currLine = bufferedReader.readLine()) != null) {
        switch (currLine) {
          case STR_SOURCE_FILES_FROM_OLD:
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/log/CompactionLogAnalyzer.java`
#### Snippet
```java
    String currLine;
    try (BufferedReader bufferedReader = new BufferedReader(new FileReader(logFile))) {
      while ((currLine = bufferedReader.readLine()) != null) {
        String fileInfo;
        if (currLine.startsWith(STR_SOURCE_FILES)) {
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `server/src/main/java/org/apache/iotdb/db/engine/modification/io/LocalTextModificationAccessor.java`
#### Snippet
```java
    List<Modification> modificationList = new ArrayList<>();
    try (TracedBufferedReader reader = new TracedBufferedReader(new FileReader(file))) {
      while ((line = reader.readLine()) != null) {
        if (line.equals(ABORT_MARK) && !modificationList.isEmpty()) {
          modificationList.remove(modificationList.size() - 1);
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `server/src/main/java/org/apache/iotdb/db/engine/modification/utils/TracedBufferedReader.java`
#### Snippet
```java
          dst = delta;
        }
        nextChar = nChars = delta;
      }
    }
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `server/src/main/java/org/apache/iotdb/db/engine/modification/utils/TracedBufferedReader.java`
#### Snippet
```java
    this.in = in;
    cb = new char[sz];
    nextChar = nChars = 0;
  }

```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/MemMTreeStore.java`
#### Snippet
```java

  private void releaseMemory(int size) {
    localMemoryUsage.getAndUpdate(v -> v -= size);
    memoryStatistics.releaseMemory(size);
  }
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/MemMTreeStore.java`
#### Snippet
```java
  private void requestMemory(int size) {
    memoryStatistics.requestMemory(size);
    localMemoryUsage.getAndUpdate(v -> v += size);
  }

```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/cache/LRUCacheManager.java`
#### Snippet
```java
    private void moveToFirst(LRUCacheEntry entry) {
      if (first == null || last == null) { // empty linked list
        first = last = entry;
        return;
      }
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/memcontrol/MemManagerNodeEstimatedSizeBasedImpl.java`
#### Snippet
```java
    int size = estimator.estimateSize(node);
    memoryStatistics.requestMemory(size);
    pinnedSize.getAndUpdate(v -> v += size);
  }

```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/memcontrol/MemManagerNodeEstimatedSizeBasedImpl.java`
#### Snippet
```java
      memoryStatistics.releaseMemory(deltaSize);
    }
    pinnedSize.getAndUpdate(v -> v += deltaSize);
  }

```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/memcontrol/MemManagerNodeEstimatedSizeBasedImpl.java`
#### Snippet
```java
  public void releasePinnedMemResource(IMNode node) {
    int size = estimator.estimateSize(node);
    this.size.getAndUpdate(v -> v += size);
    pinnedSize.getAndUpdate(v -> v -= size);
  }
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/memcontrol/MemManagerNodeEstimatedSizeBasedImpl.java`
#### Snippet
```java
    int size = estimator.estimateSize(node);
    this.size.getAndUpdate(v -> v += size);
    pinnedSize.getAndUpdate(v -> v -= size);
  }

```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/memcontrol/MemManagerNodeEstimatedSizeBasedImpl.java`
#### Snippet
```java
    }
    int finalSize = size;
    this.size.getAndUpdate(v -> v -= finalSize);
    memoryStatistics.releaseMemory(size);
  }
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/memcontrol/MemManagerNodeEstimatedSizeBasedImpl.java`
#### Snippet
```java
  public void upgradeMemResource(IMNode node) {
    int size = estimator.estimateSize(node);
    pinnedSize.getAndUpdate(v -> v += size);
    this.size.getAndUpdate(v -> v -= size);
  }
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/memcontrol/MemManagerNodeEstimatedSizeBasedImpl.java`
#### Snippet
```java
    int size = estimator.estimateSize(node);
    pinnedSize.getAndUpdate(v -> v += size);
    this.size.getAndUpdate(v -> v -= size);
  }

```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/memcontrol/MemManagerNodeEstimatedSizeBasedImpl.java`
#### Snippet
```java
  public void releaseMemResource(IMNode node) {
    int size = estimator.estimateSize(node);
    this.size.getAndUpdate(v -> v -= size);
    memoryStatistics.releaseMemory(size);
  }
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/memcontrol/MemManagerNodeNumBasedImpl.java`
#### Snippet
```java
  @Override
  public void releaseMemResource(List<IMNode> evictedNodes) {
    size.getAndUpdate(value -> value -= evictedNodes.size());
  }

```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `server/src/main/java/org/apache/iotdb/db/metadata/rescon/MemoryStatistics.java`
#### Snippet
```java

  public void requestMemory(long size) {
    memoryUsage.getAndUpdate(v -> v += size);
    if (memoryUsage.get() >= memoryCapacity) {
      logger.warn("Current series number {} is too large...", memoryUsage);
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `server/src/main/java/org/apache/iotdb/db/metadata/rescon/MemoryStatistics.java`
#### Snippet
```java

  public void releaseMemory(long size) {
    memoryUsage.getAndUpdate(v -> v -= size);
    if (!allowToCreateNewSeries && memoryUsage.get() < memoryCapacity) {
      logger.info("Current series number {} come back to normal level", memoryUsage);
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/pagemgr/PageManager.java`
#### Snippet
```java
      } else if (alias != null && alias.compareTo(oldAlias) != 0) {
        // remove and (re)insert: both not null, but not equals
        insertNewSubEntry = removeOldSubEntry = true;
      } else {
        // do nothing: both null, or both not null but equals
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/pagemgr/PageManager.java`
#### Snippet
```java
      } else {
        // do nothing: both null, or both not null but equals
        insertNewSubEntry = removeOldSubEntry = false;
      }

```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `example/session/src/main/java/org/apache/iotdb/TabletExample.java`
#### Snippet
```java

      String line;
      while ((line = reader.readLine()) != null) {
        String[] items = line.split(",");
        for (Entry<String, TSDataType> entry : measureTSTypeInfos.entrySet()) {
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `library-udf/src/main/java/org/apache/iotdb/library/util/CircularQueue.java`
#### Snippet
```java

  public CircularQueue(int capacity) {
    head = tail = size = 0;
    data = (E[]) new Object[capacity];
    minLen = Math.max(INITCAP, capacity);
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `library-udf/src/main/java/org/apache/iotdb/library/util/CircularQueue.java`
#### Snippet
```java

  public CircularQueue(int capacity) {
    head = tail = size = 0;
    data = (E[]) new Object[capacity];
    minLen = Math.max(INITCAP, capacity);
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `library-udf/src/main/java/org/apache/iotdb/library/util/LongCircularQueue.java`
#### Snippet
```java

  public LongCircularQueue(int capacity) {
    head = tail = size = 0;
    data = new long[capacity];
    minLen = Math.max(INITCAP, capacity);
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `library-udf/src/main/java/org/apache/iotdb/library/util/LongCircularQueue.java`
#### Snippet
```java

  public LongCircularQueue(int capacity) {
    head = tail = size = 0;
    data = new long[capacity];
    minLen = Math.max(INITCAP, capacity);
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `library-udf/src/main/java/org/apache/iotdb/library/util/BooleanCircularQueue.java`
#### Snippet
```java

  public BooleanCircularQueue(int capacity) {
    head = tail = size = 0;
    data = new boolean[capacity];
    minLen = Math.max(INITCAP, capacity);
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `library-udf/src/main/java/org/apache/iotdb/library/util/BooleanCircularQueue.java`
#### Snippet
```java

  public BooleanCircularQueue(int capacity) {
    head = tail = size = 0;
    data = new boolean[capacity];
    minLen = Math.max(INITCAP, capacity);
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `library-udf/src/main/java/org/apache/iotdb/library/util/DoubleCircularQueue.java`
#### Snippet
```java

  public DoubleCircularQueue(int capacity) {
    head = tail = size = 0;
    data = new double[capacity];
    minLen = Math.max(INITCAP, capacity);
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `library-udf/src/main/java/org/apache/iotdb/library/util/DoubleCircularQueue.java`
#### Snippet
```java

  public DoubleCircularQueue(int capacity) {
    head = tail = size = 0;
    data = new double[capacity];
    minLen = Math.max(INITCAP, capacity);
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `library-udf/src/main/java/org/apache/iotdb/library/dmatch/UDAFDtw.java`
#### Snippet
```java
    dp = new double[m + 1][m + 1];
    for (int i = 1; i <= m; i++) {
      dp[0][i] = dp[i][0] = Double.MAX_VALUE;
    }
    dp[0][0] = 0;
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `library-udf/src/main/java/org/apache/iotdb/library/series/util/ConsecutiveUtil.java`
#### Snippet
```java
        collector.putInt(first, count);
      }
      first = last = -gap;
      count = 0;
    } else {
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `library-udf/src/main/java/org/apache/iotdb/library/series/util/ConsecutiveUtil.java`
#### Snippet
```java
          collector.putInt(first, count);
        }
        first = last = time;
        count = 1;
      }
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `library-udf/src/main/java/org/apache/iotdb/library/series/util/ConsecutiveUtil.java`
#### Snippet
```java
      return;
    }
    first = last = -gap;
    for (Pair<Long, Boolean> p : window) {
      process(p.getLeft(), p.getRight(), collector);
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `library-udf/src/main/java/org/apache/iotdb/library/frequency/util/DWTUtil.java`
#### Snippet
```java
    }
    ncof = cc.length;
    ioff = joff = -(ncof >> 1);
    cr = new double[ncof];
    double sig = -1.0;
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `rewrite-tsfile-tool/src/main/java/org/apache/iotdb/RewriteTsFileTool.java`
#### Snippet
```java
      List<Long> timeForAlignedSeries = new ArrayList<>();
      List<MeasurementSchema> schemaForAlignedSeries = new ArrayList<>();
      while ((marker = reader.readMarker()) != MetaMarker.SEPARATOR) {
        switch (marker) {
          case MetaMarker.CHUNK_HEADER:
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `confignode/src/main/java/org/apache/iotdb/confignode/manager/load/balancer/router/leader/MinCostFlowLeaderBalancer.java`
#### Snippet
```java
      int currentFlow;
      System.arraycopy(nodeHeadEdge, 0, nodeCurrentEdge, 0, maxNode);
      while ((currentFlow = dfsAugmentation(sNode, INFINITY)) > 0) {
        maximumFlow += currentFlow;
      }
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/impl/sync/OperatePipeProcedureRollbackProcessor.java`
#### Snippet
```java
            dataNodeId, nodeManager.getRegisteredDataNodeLocations().get(dataNodeId));
        TOperatePipeOnDataNodeReq request;
        while ((request = entry.getValue().peek()) != null) {
          AsyncClientHandler<TOperatePipeOnDataNodeReq, TSStatus> clientHandler =
              new AsyncClientHandler<>(
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `spark-tsfile/src/main/java/org/apache/iotdb/spark/tsfile/qp/common/BasicOperator.java`
#### Snippet
```java
  public BasicOperator(int tokenIntType, String path, String value) {
    super(tokenIntType);
    this.seriesPath = this.singlePath = path;
    this.seriesValue = value;
    this.isLeaf = true;
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/ReadWriteForEncodingUtils.java`
#### Snippet
```java
    int i = 0;
    int b = 0;
    while (buffer.hasRemaining() && ((b = buffer.get()) & 0x80) != 0) {
      value |= (b & 0x7F) << i;
      i += 7;
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/RamUsageEstimator.java`
#### Snippet
```java
      int slot = rehash(e) & mask;
      Object existing;
      while ((existing = keys[slot]) != null) {
        if (e == existing) {
          return true;
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/RamUsageEstimator.java`
#### Snippet
```java
      int slot = rehash(e) & mask;
      Object existing;
      while ((existing = keys[slot]) != null) {
        if (e == existing) {
          return false; // already found.
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/RamUsageEstimator.java`
#### Snippet
```java
          ClassCache cachedInfo = classCache.get(obClazz);
          if (cachedInfo == null) {
            classCache.put(obClazz, cachedInfo = createCacheEntry(obClazz));
          }

```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/ReadWriteIOUtils.java`
#### Snippet
```java
    int length = 0;
    int read;
    while (buffer.hasRemaining() && (read = input.read(buffer, position)) != NO_BYTE_TO_READ) {
      length += read;
      position += read;
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/ReadWriteIOUtils.java`
#### Snippet
```java
    while (length < len
        && target.hasRemaining()
        && (read = input.read(target, offset)) != NO_BYTE_TO_READ) {
      length += read;
      offset += read;
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/ReadWriteIOUtils.java`
#### Snippet
```java
    int len;
    while (bytes.length - offset > 0
        && (len = inputStream.read(bytes, offset, bytes.length - offset)) != NO_BYTE_TO_READ) {
      offset += len;
    }
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/ReadWriteIOUtils.java`
#### Snippet
```java
    int length = 0;
    int read;
    while (buffer.hasRemaining() && (read = input.read(buffer)) != NO_BYTE_TO_READ) {
      length += read;
    }
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `tsfile/src/main/java/org/apache/iotdb/tsfile/compress/ICompressor.java`
#### Snippet
```java
      byte[] buffer = new byte[256];
      int n;
      while ((n = ungzip.read(buffer)) > 0) {
        out.write(buffer, 0, n);
      }
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/decoder/LongZigzagDecoder.java`
#### Snippet
```java
    long b = 0;

    while (byteCache.hasRemaining() && ((b = byteCache.get()) & 0x80) != 0) {
      n |= (b & 0x7F) << i;
      i += 7;
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/TsFileSequenceReader.java`
#### Snippet
```java
    List<IMeasurementSchema> measurementSchemaList = new ArrayList<>();
    try {
      while ((marker = this.readMarker()) != MetaMarker.SEPARATOR) {
        switch (marker) {
          case MetaMarker.CHUNK_HEADER:
```

### RuleId[ruleID=NestedAssignment]
Result of assignment expression used
in `consensus/src/main/java/org/apache/iotdb/consensus/multileader/logdispatcher/LogDispatcher.java`
#### Snippet
```java
        while (!Thread.interrupted() && !stopped) {
          long startTime = System.currentTimeMillis();
          while ((batch = getBatch()).isEmpty()) {
            // we may block here if there is no requests in the queue
            IndexedConsensusRequest request =
```

## RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`temp = temp / std[pos]` could be simplified to 'temp /= std\[pos\]'
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/utils/MasterRepairUtil.java`
#### Snippet
```java
    for (int pos = 0; pos < columnCnt; pos++) {
      double temp = tTuple.get(pos) - mTuple.get(pos);
      temp = temp / std[pos];
      distance += temp * temp;
    }
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`maxSize = maxSize - count` could be simplified to 'maxSize -= count'
in `cli/src/main/java/org/apache/iotdb/cli/utils/IoTPrinter.java`
#### Snippet
```java
        if (remain > 0) {
          paddingStr = padding(remain);
          maxSize = maxSize - count;
          element = paddingStr.append(element).toString();
        } else if (remain == 0) {
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`maxSize = maxSize - count` could be simplified to 'maxSize -= count'
in `cli/src/main/java/org/apache/iotdb/cli/utils/IoTPrinter.java`
#### Snippet
```java
          element = paddingStr.append(element).toString();
        } else if (remain == 0) {
          maxSize = maxSize - count;
        }
      }
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`time = time * 1000` could be simplified to 'time \*= 1000'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
          break;
        case "us":
          time = time * 1000;
          break;
        case "ns":
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`time = time * 1000000` could be simplified to 'time \*= 1000000'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
          break;
        case "ns":
          time = time * 1000000;
          break;
        default:
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`d = d - (long) d` could be simplified to 'd -= (long) d'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
          double d = Double.parseDouble(parameters.get(param));
          if (d >= 1) { // we only need the fraction digits
            d = d - (long) d;
          }
          if (d == 0) { // nothing to count
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`time = time * 1000` could be simplified to 'time \*= 1000'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
          break;
        case "us":
          time = time * 1000;
          break;
        case "ns":
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`time = time * 1000000` could be simplified to 'time \*= 1000000'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
          break;
        case "ns":
          time = time * 1000000;
          break;
        default:
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`bitmap[k] = (bitmap[k] << 1)` could be simplified to 'bitmap\[k\] \<\<= 1'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
          DataOutputStream dataOutputStream = dataOutputStreams[2 * k + 1]; // DO NOT FORGET +1
          if (field == null || field.getDataType() == null) {
            bitmap[k] = (bitmap[k] << 1);
          } else {
            bitmap[k] = (bitmap[k] << 1) | 0x01;
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`valueOccupation[k] = valueOccupation[k] + 4 + field.getBinaryV().getLength()` could be simplified to 'valueOccupation\[k\] += 4 + field.getBinaryV().getLength()'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
                dataOutputStream.writeInt(field.getBinaryV().getLength());
                dataOutputStream.write(field.getBinaryV().getValues());
                valueOccupation[k] = valueOccupation[k] + 4 + field.getBinaryV().getLength();
                break;
              default:
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`i = i + 2` could be simplified to 'i += 2'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/parser/ASTVisitor.java`
#### Snippet
```java
    long time;
    time = parseDateFormat(ctx.getChild(0).getText(), currentTime);
    for (int i = 1; i < ctx.getChildCount(); i = i + 2) {
      if ("+".equals(ctx.getChild(i).getText())) {
        time += DateTimeUtils.convertDurationStrToLong(time, ctx.getChild(i + 1).getText());
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`i = i + 2` could be simplified to 'i += 2'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/parser/ASTVisitor.java`
#### Snippet
```java
    long time;
    time = parseDateFormat(ctx.getChild(0).getText());
    for (int i = 1; i < ctx.getChildCount(); i = i + 2) {
      if ("+".equals(ctx.getChild(i).getText())) {
        time += DateTimeUtils.convertDurationStrToLong(time, ctx.getChild(i + 1).getText());
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`interval = interval / MS_TO_MONTH` could be simplified to 'interval /= MS_TO_MONTH'
in `server/src/main/java/org/apache/iotdb/db/query/dataset/groupby/GroupByTimeDataSet.java`
#### Snippet
```java

    if (isIntervalByMonth) {
      interval = interval / MS_TO_MONTH;
    }

```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`slidingStep = slidingStep / MS_TO_MONTH` could be simplified to 'slidingStep /= MS_TO_MONTH'
in `server/src/main/java/org/apache/iotdb/db/query/dataset/groupby/GroupByTimeDataSet.java`
#### Snippet
```java

    if (isSlidingStepByMonth) {
      slidingStep = slidingStep / MS_TO_MONTH;
    }

```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`currentBitmapList[bufferIndex] = (currentBitmapList[bufferIndex] << 1)` could be simplified to 'currentBitmapList\[bufferIndex\] \<\<= 1'
in `server/src/main/java/org/apache/iotdb/db/query/dataset/RawQueryDataSetWithoutValueFilter.java`
#### Snippet
```java
                  i < ((AlignedPath) paths.get(seriesIndex)).getMeasurementList().size();
                  i++) {
                currentBitmapList[bufferIndex] = (currentBitmapList[bufferIndex] << 1);
                bufferIndex++;
              }
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`currentBitmapList[bufferIndex] = (currentBitmapList[bufferIndex] << 1)` could be simplified to 'currentBitmapList\[bufferIndex\] \<\<= 1'
in `server/src/main/java/org/apache/iotdb/db/query/dataset/RawQueryDataSetWithoutValueFilter.java`
#### Snippet
```java
              }
            } else {
              currentBitmapList[bufferIndex] = (currentBitmapList[bufferIndex] << 1);
              bufferIndex++;
            }
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`currentBitmapList[bufferIndex] = (currentBitmapList[bufferIndex] << 1)` could be simplified to 'currentBitmapList\[bufferIndex\] \<\<= 1'
in `server/src/main/java/org/apache/iotdb/db/query/dataset/RawQueryDataSetWithoutValueFilter.java`
#### Snippet
```java
                for (TsPrimitiveType primitiveVal : cachedBatchDataArray[seriesIndex].getVector()) {
                  if (primitiveVal == null) {
                    currentBitmapList[bufferIndex] = (currentBitmapList[bufferIndex] << 1);
                    bufferIndex++;
                    continue;
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`currentBitmapList[i] = (currentBitmapList[i] << 1)` could be simplified to 'currentBitmapList\[i\] \<\<= 1'
in `server/src/main/java/org/apache/iotdb/db/query/dataset/UDTFAlignByTimeDataSet.java`
#### Snippet
```java
        if (!reader.next() || reader.currentTime() != minTime) {
          if (rowOffset == 0) {
            currentBitmapList[i] = (currentBitmapList[i] << 1);
          }
          continue;
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`currentBitmapList[i] = (currentBitmapList[i] << 1)` could be simplified to 'currentBitmapList\[i\] \<\<= 1'
in `server/src/main/java/org/apache/iotdb/db/query/dataset/UDTFAlignByTimeDataSet.java`
#### Snippet
```java
          } else {
            // there's no data in the current field, so put a null placeholder 0x00
            currentBitmapList[i] = (currentBitmapList[i] << 1);
          }
        }
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`sum = sum + datum` could be simplified to 'sum += datum'
in `server/src/main/java/org/apache/iotdb/db/utils/MathUtils.java`
#### Snippet
```java
    double sum = 0;
    for (Double datum : data) {
      sum = sum + datum;
    }
    return sum;
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`variance = variance + (Math.pow((data.get(i) - mean(data)), 2))` could be simplified to 'variance += (Math.pow((data.get(i) - mean(data)), 2))'
in `server/src/main/java/org/apache/iotdb/db/utils/MathUtils.java`
#### Snippet
```java
    double variance = 0;
    for (int i = 0; i < data.size(); i++) {
      variance = variance + (Math.pow((data.get(i) - mean(data)), 2));
    }
    variance = variance / data.size();
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`variance = variance / data.size()` could be simplified to 'variance /= data.size()'
in `server/src/main/java/org/apache/iotdb/db/utils/MathUtils.java`
#### Snippet
```java
      variance = variance + (Math.pow((data.get(i) - mean(data)), 2));
    }
    variance = variance / data.size();
    return variance;
  }
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`unbrokenLogsSize = unbrokenLogsSize + logSize + LEAST_LOG_SIZE` could be simplified to 'unbrokenLogsSize += logSize + LEAST_LOG_SIZE'
in `server/src/main/java/org/apache/iotdb/db/utils/writelog/SingleFileLogReader.java`
#### Snippet
```java
      batchLogReader = new BatchLogReader(ByteBuffer.wrap(buffer));
      if (!batchLogReader.isFileCorrupted()) {
        unbrokenLogsSize = unbrokenLogsSize + logSize + LEAST_LOG_SIZE;
      } else {
        truncateBrokenLogs();
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`bitmap[k] = (bitmap[k] << 1)` could be simplified to 'bitmap\[k\] \<\<= 1'
in `server/src/main/java/org/apache/iotdb/db/utils/QueryDataSetUtils.java`
#### Snippet
```java
          DataOutputStream dataOutputStream = dataOutputStreams[2 * k + 1]; // DO NOT FORGET +1
          if (field == null || field.getDataType() == null) {
            bitmap[k] = (bitmap[k] << 1);
          } else {
            bitmap[k] = (bitmap[k] << 1) | FLAG;
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`valueOccupation[k] = valueOccupation[k] + 4 + field.getBinaryV().getLength()` could be simplified to 'valueOccupation\[k\] += 4 + field.getBinaryV().getLength()'
in `server/src/main/java/org/apache/iotdb/db/utils/QueryDataSetUtils.java`
#### Snippet
```java
                dataOutputStream.writeInt(field.getBinaryV().getLength());
                dataOutputStream.write(field.getBinaryV().getValues());
                valueOccupation[k] = valueOccupation[k] + 4 + field.getBinaryV().getLength();
                break;
              default:
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`bitmaps[k] = bitmaps[k] << 1` could be simplified to 'bitmaps\[k\] \<\<= 1'
in `server/src/main/java/org/apache/iotdb/db/utils/QueryDataSetUtils.java`
#### Snippet
```java
              rowCount++;
              if (column.isNull(i)) {
                bitmaps[k] = bitmaps[k] << 1;
              } else {
                bitmaps[k] = (bitmaps[k] << 1) | FLAG;
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`bitmaps[k] = bitmaps[k] << 1` could be simplified to 'bitmaps\[k\] \<\<= 1'
in `server/src/main/java/org/apache/iotdb/db/utils/QueryDataSetUtils.java`
#### Snippet
```java
              rowCount++;
              if (column.isNull(i)) {
                bitmaps[k] = bitmaps[k] << 1;
              } else {
                bitmaps[k] = (bitmaps[k] << 1) | FLAG;
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`bitmaps[k] = bitmaps[k] << 1` could be simplified to 'bitmaps\[k\] \<\<= 1'
in `server/src/main/java/org/apache/iotdb/db/utils/QueryDataSetUtils.java`
#### Snippet
```java
              rowCount++;
              if (column.isNull(i)) {
                bitmaps[k] = bitmaps[k] << 1;
              } else {
                bitmaps[k] = (bitmaps[k] << 1) | FLAG;
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`bitmaps[k] = bitmaps[k] << 1` could be simplified to 'bitmaps\[k\] \<\<= 1'
in `server/src/main/java/org/apache/iotdb/db/utils/QueryDataSetUtils.java`
#### Snippet
```java
              rowCount++;
              if (column.isNull(i)) {
                bitmaps[k] = bitmaps[k] << 1;
              } else {
                bitmaps[k] = (bitmaps[k] << 1) | FLAG;
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`bitmaps[k] = bitmaps[k] << 1` could be simplified to 'bitmaps\[k\] \<\<= 1'
in `server/src/main/java/org/apache/iotdb/db/utils/QueryDataSetUtils.java`
#### Snippet
```java
              rowCount++;
              if (column.isNull(i)) {
                bitmaps[k] = bitmaps[k] << 1;
              } else {
                bitmaps[k] = (bitmaps[k] << 1) | FLAG;
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`bitmaps[k] = bitmaps[k] << 1` could be simplified to 'bitmaps\[k\] \<\<= 1'
in `server/src/main/java/org/apache/iotdb/db/utils/QueryDataSetUtils.java`
#### Snippet
```java
              rowCount++;
              if (column.isNull(i)) {
                bitmaps[k] = bitmaps[k] << 1;
              } else {
                bitmaps[k] = (bitmaps[k] << 1) | FLAG;
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`valueOccupation[k] = valueOccupation[k] + 4 + binary.getLength()` could be simplified to 'valueOccupation\[k\] += 4 + binary.getLength()'
in `server/src/main/java/org/apache/iotdb/db/utils/QueryDataSetUtils.java`
#### Snippet
```java
                dataOutputStream.writeInt(binary.getLength());
                dataOutputStream.write(binary.getValues());
                valueOccupation[k] = valueOccupation[k] + 4 + binary.getLength();
              }
              if (rowCount != 0 && rowCount % 8 == 0) {
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`tIdx = tIdx - nullCnt` could be simplified to 'tIdx -= nullCnt'
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/BooleanTVList.java`
#### Snippet
```java
      }
      // update maxTime and sorted
      tIdx = tIdx - nullCnt;
      inPutMinTime = Math.min(inPutMinTime, time[tIdx]);
      maxTime = Math.max(maxTime, time[tIdx]);
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`tIdx = tIdx - nullCnt` could be simplified to 'tIdx -= nullCnt'
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/DoubleTVList.java`
#### Snippet
```java
      }
      // update maxTime and sorted
      tIdx = tIdx - nullCnt;
      inPutMinTime = Math.min(inPutMinTime, time[tIdx]);
      maxTime = Math.max(maxTime, time[tIdx]);
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`tIdx = tIdx - nullCnt` could be simplified to 'tIdx -= nullCnt'
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/IntTVList.java`
#### Snippet
```java
      }
      // update maxTime and sorted
      tIdx = tIdx - nullCnt;
      inPutMinTime = Math.min(inPutMinTime, time[tIdx]);
      maxTime = Math.max(maxTime, time[tIdx]);
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`tIdx = tIdx - nullCnt` could be simplified to 'tIdx -= nullCnt'
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/FloatTVList.java`
#### Snippet
```java
      }
      // update maxTime and sorted
      tIdx = tIdx - nullCnt;
      inPutMinTime = Math.min(inPutMinTime, time[tIdx]);
      maxTime = Math.max(maxTime, time[tIdx]);
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`tIdx = tIdx - nullCnt` could be simplified to 'tIdx -= nullCnt'
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/BinaryTVList.java`
#### Snippet
```java
      }
      // update maxTime and sorted
      tIdx = tIdx - nullCnt;
      inPutMinTime = Math.min(inPutMinTime, time[tIdx]);
      maxTime = Math.max(maxTime, time[tIdx]);
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`tIdx = tIdx - nullCnt` could be simplified to 'tIdx -= nullCnt'
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/LongTVList.java`
#### Snippet
```java
      }
      // update maxTime and sorted
      tIdx = tIdx - nullCnt;
      inPutMinTime = Math.min(inPutMinTime, time[tIdx]);
      maxTime = Math.max(maxTime, time[tIdx]);
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`totalFilledBytesNum = totalFilledBytesNum + n` could be simplified to 'totalFilledBytesNum += n'
in `server/src/main/java/org/apache/iotdb/db/engine/modification/utils/TracedBufferedReader.java`
#### Snippet
```java
      nChars = dst + n;
      nextChar = dst;
      totalFilledBytesNum = totalFilledBytesNum + n;
    }
  }
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`freeAddr = (short) (freeAddr - newLen)` could be simplified to 'freeAddr -= newLen'
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/WrappedSegment.java`
#### Snippet
```java
      }

      freeAddr = (short) (freeAddr - newLen);
      // it will not mark old record as expired
      this.buffer.position(freeAddr);
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`i = i + 2` could be simplified to 'i += 2'
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/sql/InfluxDBSqlVisitor.java`
#### Snippet
```java
    long time;
    time = parseTimeFormat(ctx.getChild(0).getText());
    for (int i = 1; i < ctx.getChildCount(); i = i + 2) {
      if (ctx.getChild(i).getText().equals("+")) {
        time += DateTimeUtils.convertDurationStrToLong(time, ctx.getChild(i + 1).getText());
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`this.sumX1 = this.sumX1 + value` could be simplified to 'this.sumX1 += value'
in `library-udf/src/main/java/org/apache/iotdb/library/anomaly/UDTFKSigma.java`
#### Snippet
```java
        }
        t.push(timestamp);
        this.sumX1 = this.sumX1 + value;
        this.sumX2 = this.sumX2 + value * value;
        this.mean = this.sumX1 / v.getSize();
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`this.sumX2 = this.sumX2 + value * value` could be simplified to 'this.sumX2 += value \* value'
in `library-udf/src/main/java/org/apache/iotdb/library/anomaly/UDTFKSigma.java`
#### Snippet
```java
        t.push(timestamp);
        this.sumX1 = this.sumX1 + value;
        this.sumX2 = this.sumX2 + value * value;
        this.mean = this.sumX1 / v.getSize();
        this.var = this.sumX2 / v.getSize() - this.mean * this.mean;
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`correlation = correlation / length` could be simplified to 'correlation /= length'
in `library-udf/src/main/java/org/apache/iotdb/library/dmatch/util/CrossCorrelation.java`
#### Snippet
```java
        }
      }
      correlation = correlation / length;
      correlationArrayList.add(correlation);
    }
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`valueBefore = valueBefore / len` could be simplified to 'valueBefore /= len'
in `library-udf/src/main/java/org/apache/iotdb/library/anomaly/util/WindowDetect.java`
#### Snippet
```java
        }
      }
      valueBefore = valueBefore / len;
      valueAfter = valueAfter / len;
      double p1 = Math.abs(valueBefore - curValue) / (Math.max(valueBefore, curValue) + 1);
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`valueAfter = valueAfter / len` could be simplified to 'valueAfter /= len'
in `library-udf/src/main/java/org/apache/iotdb/library/anomaly/util/WindowDetect.java`
#### Snippet
```java
      }
      valueBefore = valueBefore / len;
      valueAfter = valueAfter / len;
      double p1 = Math.abs(valueBefore - curValue) / (Math.max(valueBefore, curValue) + 1);
      double p2 = Math.abs(valueAfter - curValue) / (Math.max(valueAfter, curValue) + 1);
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`v1 = v1 / (time[index + 1] - time[index])` could be simplified to 'v1 /= (time\[index + 1\] - time\[index\])'
in `library-udf/src/main/java/org/apache/iotdb/library/drepair/util/LsGreedy.java`
#### Snippet
```java
      this.index = index;
      double v1 = repaired[index + 1] - repaired[index];
      v1 = v1 / (time[index + 1] - time[index]);
      double v2 = repaired[index] - repaired[index - 1];
      v2 = v2 / (time[index] - time[index - 1]);
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`v2 = v2 / (time[index] - time[index - 1])` could be simplified to 'v2 /= (time\[index\] - time\[index - 1\])'
in `library-udf/src/main/java/org/apache/iotdb/library/drepair/util/LsGreedy.java`
#### Snippet
```java
      v1 = v1 / (time[index + 1] - time[index]);
      double v2 = repaired[index] - repaired[index - 1];
      v2 = v2 / (time[index] - time[index - 1]);
      this.u = v1 - v2;
    }
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`correlation = correlation / n` could be simplified to 'correlation /= n'
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/UDTFQLB.java`
#### Snippet
```java
        }
      }
      correlation = correlation / n;
      collector.putDouble((long) n + shift, correlation);
      qlb += correlation * correlation / (n - shift) * n * (n + 2);
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`ret = ret / (source.get(1).time - source.get(0).time)` could be simplified to 'ret /= (source.get(1).time - source.get(0).time)'
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/Resampler.java`
#### Snippet
```java
            source.get(0).value * (source.get(1).time - t)
                + source.get(1).value * (t - source.get(0).time);
        ret = ret / (source.get(1).time - source.get(0).time);
        break;
      default:
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`correlation = correlation / length` could be simplified to 'correlation /= length'
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/CrossCorrelation.java`
#### Snippet
```java
        }
      }
      correlation = correlation / length;
      correlationArrayList.add(correlation);
    }
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`executionTime = executionTime + everyInterval` could be simplified to 'executionTime += everyInterval'
in `confignode/src/main/java/org/apache/iotdb/confignode/manager/cq/CQScheduleTask.java`
#### Snippet
```java
  private void updateExecutionTime() {
    if (timeoutPolicy == TimeoutPolicy.BLOCKED) {
      executionTime = executionTime + everyInterval;
    } else if (timeoutPolicy == TimeoutPolicy.DISCARD) {
      long now = System.currentTimeMillis();
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`executionTime = executionTime + ((now - executionTime - 1) / everyInterval + 1) * everyInt...` could be simplified to 'executionTime += ((now - executionTime - 1) / everyInterval + 1) \* everyInterval'
in `confignode/src/main/java/org/apache/iotdb/confignode/manager/cq/CQScheduleTask.java`
#### Snippet
```java
    } else if (timeoutPolicy == TimeoutPolicy.DISCARD) {
      long now = System.currentTimeMillis();
      executionTime =
          executionTime + ((now - executionTime - 1) / everyInterval + 1) * everyInterval;
    } else {
      throw new IllegalArgumentException("Unknown TimeoutPolicy: " + timeoutPolicy);
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`unbrokenLogsSize = unbrokenLogsSize + logSize + LEAST_LOG_SIZE` could be simplified to 'unbrokenLogsSize += logSize + LEAST_LOG_SIZE'
in `confignode/src/main/java/org/apache/iotdb/confignode/writelog/io/SingleFileLogReader.java`
#### Snippet
```java
      batchLogReader = new BatchLogReader(ByteBuffer.wrap(buffer));
      if (!batchLogReader.isFileCorrupted()) {
        unbrokenLogsSize = unbrokenLogsSize + logSize + LEAST_LOG_SIZE;
      } else {
        truncateBrokenLogs();
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`endIndex = endIndex + 1` could be simplified to 'endIndex += 1'
in `confignode/src/main/java/org/apache/iotdb/confignode/consensus/statemachine/ConfigNodeRegionStateMachine.java`
#### Snippet
```java
      buffer.position(buffer.limit());
      logWriter.write(buffer);
      endIndex = endIndex + 1;
      File tmpLogFile = new File(progressFilePath + endIndex);
      Files.move(logFile.toPath(), tmpLogFile.toPath(), StandardCopyOption.ATOMIC_MOVE);
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`startIndex = startIndex + 1` could be simplified to 'startIndex += 1'
in `confignode/src/main/java/org/apache/iotdb/confignode/consensus/statemachine/ConfigNodeRegionStateMachine.java`
#### Snippet
```java
      endIndex = 0;
    }
    startIndex = startIndex + 1;
    createLogFile(endIndex);
  }
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`key[0] = (byte) (key[0] + '0')` could be simplified to 'key\[0\] += '0''
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/RSchemaReadWriteHandler.java`
#### Snippet
```java
        while (iterator.isValid()) {
          byte[] key = iterator.key();
          key[0] = (byte) (key[0] + '0');
          outputStream.write(key);
          outputStream.write(" -> ".getBytes());
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`out = out + ch` could be simplified to 'out += ch'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/filter/operator/Like.java`
#### Snippet
```java
          String nextChar = String.valueOf(value.charAt(i + 1));
          if ("%".equals(nextChar) || "_".equals(nextChar) || "\\".equals(nextChar)) {
            out = out + ch;
          }
          if ("\\".equals(nextChar)) {
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`out = out + ch` could be simplified to 'out += ch'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/filter/operator/Like.java`
#### Snippet
```java
        }
      } else {
        out = out + ch;
      }
    }
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`bitmap[i / 8] = (byte) (bitmap[i / 8] & (~(MASK >>> shift)))` could be simplified to 'bitmap\[i / 8\] \&= (\~(MASK \>\>\> shift))'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/reader/page/AlignedPageReader.java`
#### Snippet
```java
          if (isDeleted[columnIndex][i]) {
            int shift = i % 8;
            bitmap[i / 8] = (byte) (bitmap[i / 8] & (~(MASK >>> shift)));
          }
        }
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`ret = ret << m` could be simplified to 'ret \<\<= m'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BitReader.java`
#### Snippet
```java
      int m = len + bitCnt >= BITS_IN_A_BYTE ? BITS_IN_A_BYTE - bitCnt : len;
      len -= m;
      ret = ret << m;
      byte y = (byte) (cache & MASKS[bitCnt]); // Truncate the low bits with &
      y = (byte) ((y & 0xff) >>> (BITS_IN_A_BYTE - bitCnt - m)); // Logical shift right
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`ret = ret | (y & 0xff)` could be simplified to 'ret \|= (y \& 0xff)'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BitReader.java`
#### Snippet
```java
      byte y = (byte) (cache & MASKS[bitCnt]); // Truncate the low bits with &
      y = (byte) ((y & 0xff) >>> (BITS_IN_A_BYTE - bitCnt - m)); // Logical shift right
      ret = ret | (y & 0xff);
      bitCnt += m;
    }
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`flag = (byte) (flag | FLAG_HAS_ALIAS)` could be simplified to 'flag \|= FLAG_HAS_ALIAS'
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/RSchemaUtils.java`
#### Snippet
```java
    byte flag = DEFAULT_FLAG;
    if (alias != null) {
      flag = (byte) (flag | FLAG_HAS_ALIAS);
    }

```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`flag = (byte) (flag | FLAG_HAS_TAGS)` could be simplified to 'flag \|= FLAG_HAS_TAGS'
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/RSchemaUtils.java`
#### Snippet
```java

    if (tags != null && tags.size() > 0) {
      flag = (byte) (flag | FLAG_HAS_TAGS);
    }

```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`flag = (byte) (flag | FLAG_HAS_ATTRIBUTES)` could be simplified to 'flag \|= FLAG_HAS_ATTRIBUTES'
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/RSchemaUtils.java`
#### Snippet
```java

    if (attributes != null && attributes.size() > 0) {
      flag = (byte) (flag | FLAG_HAS_ATTRIBUTES);
    }

```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`flag = (byte) (flag | FLAG_HAS_SCHEMA)` could be simplified to 'flag \|= FLAG_HAS_SCHEMA'
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/RSchemaUtils.java`
#### Snippet
```java

    if (schema != null) {
      flag = (byte) (flag | FLAG_HAS_SCHEMA);
    }

```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`origin[1] = (byte) (origin[1] | FLAG_SET_TTL)` could be simplified to 'origin\[1\] \|= FLAG_SET_TTL'
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/RSchemaUtils.java`
#### Snippet
```java
      ttlBlock[0] = DATA_BLOCK_TYPE_TTL;
      BytesUtils.longToBytes(ttl, ttlBlock, 1);
      origin[1] = (byte) (origin[1] | FLAG_SET_TTL);
      return BytesUtils.concatByteArray(origin, ttlBlock);
    } else {
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`x = x & ~(ALL_MASK << len)` could be simplified to 'x \&= \~(ALL_MASK \<\< len)'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BitConstructor.java`
#### Snippet
```java

  public void add(long x, int len) {
    x = x & ~(ALL_MASK << len); // Make sure that all bits expect the lowest len bits of x are 0
    while (len > 0) {
      // Number of bits inserted into cache
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`y = (byte) (y << (BITS_IN_A_BYTE - cnt))` could be simplified to 'y \<\<= (BITS_IN_A_BYTE - cnt)'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BitConstructor.java`
#### Snippet
```java
      cnt += m;
      byte y = (byte) (x >> len);
      y = (byte) (y << (BITS_IN_A_BYTE - cnt));
      cache = (byte) (cache | y);
      x = x & ~(ALL_MASK << len);
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`cache = (byte) (cache | y)` could be simplified to 'cache \|= y'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BitConstructor.java`
#### Snippet
```java
      byte y = (byte) (x >> len);
      y = (byte) (y << (BITS_IN_A_BYTE - cnt));
      cache = (byte) (cache | y);
      x = x & ~(ALL_MASK << len);
      if (cnt == BITS_IN_A_BYTE) {
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`x = x & ~(ALL_MASK << len)` could be simplified to 'x \&= \~(ALL_MASK \<\< len)'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BitConstructor.java`
#### Snippet
```java
      y = (byte) (y << (BITS_IN_A_BYTE - cnt));
      cache = (byte) (cache | y);
      x = x & ~(ALL_MASK << len);
      if (cnt == BITS_IN_A_BYTE) {
        pad();
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`temp = temp >> 8` could be simplified to 'temp \>\>= 8'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BytesUtils.java`
#### Snippet
```java
    for (int i = b.length - 1; i >= 0; i--) {
      b[i] = (byte) temp;
      temp = temp >> 8;
    }

```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`value = value >> 8` could be simplified to 'value \>\>= 8'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BytesUtils.java`
#### Snippet
```java
    for (int i = 7; i >= 0; i--) {
      bytes[offset + i] = (byte) value;
      value = value >> 8;
    }
  }
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`value = value << 8` could be simplified to 'value \<\<= 8'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BytesUtils.java`
#### Snippet
```java
    int value = 0;
    value |= ret[0];
    value = value << 8;
    value |= ret[1];
    return value;
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`y = (byte) (y << (8 - cnt))` could be simplified to 'y \<\<= (8 - cnt)'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BytesUtils.java`
#### Snippet
```java
        cnt += m;
        byte y = (byte) (srcNum >>> width);
        y = (byte) (y << (8 - cnt));
        mask = ~(mask - (1 << (8 - cnt)));
        result[index] = (byte) (result[index] & mask | y);
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`srcNum = srcNum & ~(-1L << width)` could be simplified to 'srcNum \&= \~(-1L \<\< width)'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BytesUtils.java`
#### Snippet
```java
        mask = ~(mask - (1 << (8 - cnt)));
        result[index] = (byte) (result[index] & mask | y);
        srcNum = srcNum & ~(-1L << width);
        if (cnt == 8) {
          index++;
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`l = l >> 8` could be simplified to 'l \>\>= 8'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BytesUtils.java`
#### Snippet
```java
    for (int i = 3; i >= 0; i--) {
      b[i] = (byte) l;
      l = l >> 8;
    }
    return b;
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`y = (byte) (y << (8 - cnt))` could be simplified to 'y \<\<= (8 - cnt)'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BytesUtils.java`
#### Snippet
```java
        cnt += m;
        byte y = (byte) (srcNum >>> width);
        y = (byte) (y << (8 - cnt));
        mask = ~(mask - (1 << (8 - cnt)));
        result[index] = (byte) (result[index] & mask | y);
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`srcNum = srcNum & ~(-1 << width)` could be simplified to 'srcNum \&= \~(-1 \<\< width)'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BytesUtils.java`
#### Snippet
```java
        mask = ~(mask - (1 << (8 - cnt)));
        result[index] = (byte) (result[index] & mask | y);
        srcNum = srcNum & ~(-1 << width);
        if (cnt == 8) {
          index++;
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`ret = ret << m` could be simplified to 'ret \<\<= m'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BytesUtils.java`
#### Snippet
```java
      int m = width + cnt >= 8 ? 8 - cnt : width;
      width -= m;
      ret = ret << m;
      byte y = (byte) (result[index] & (0xff >> cnt));
      y = (byte) ((y & 0xff) >>> (8 - cnt - m));
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`ret = ret | (y & 0xff)` could be simplified to 'ret \|= (y \& 0xff)'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BytesUtils.java`
#### Snippet
```java
      byte y = (byte) (result[index] & (0xff >> cnt));
      y = (byte) ((y & 0xff) >>> (8 - cnt - m));
      ret = ret | (y & 0xff);
      cnt += m;
      if (cnt == 8) {
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`l = l >> 8` could be simplified to 'l \>\>= 8'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BytesUtils.java`
#### Snippet
```java
    for (int i = 3 + offset; i >= offset; i--) {
      desc[i] = (byte) l;
      l = l >> 8;
    }
  }
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`ret = ret << m` could be simplified to 'ret \<\<= m'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BytesUtils.java`
#### Snippet
```java
      int m = width + cnt >= 8 ? 8 - cnt : width;
      width -= m;
      ret = ret << m;
      byte y = (byte) (result[index] & (0xff >> cnt));
      y = (byte) ((y & 0xff) >>> (8 - cnt - m));
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`ret = ret | (y & 0xff)` could be simplified to 'ret \|= (y \& 0xff)'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BytesUtils.java`
#### Snippet
```java
      byte y = (byte) (result[index] & (0xff >> cnt));
      y = (byte) ((y & 0xff) >>> (8 - cnt - m));
      ret = ret | (y & 0xff);
      cnt += m;
      if (cnt == 8) {
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`value = value >> 8` could be simplified to 'value \>\>= 8'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BytesUtils.java`
#### Snippet
```java
    for (int i = 7; i >= 0; i--) {
      bytes[i] = (byte) value;
      value = value >> 8;
    }
    return bytes;
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`filePath = filePath + File.separatorChar` could be simplified to 'filePath += File.separatorChar'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/FilePathUtils.java`
#### Snippet
```java
  public static String regularizePath(String filePath) {
    if (filePath.length() > 0 && filePath.charAt(filePath.length() - 1) != File.separatorChar) {
      filePath = filePath + File.separatorChar;
    }
    return filePath;
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`n = n | (b << i)` could be simplified to 'n \|= (b \<\< i)'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/decoder/LongZigzagDecoder.java`
#### Snippet
```java
      i += 7;
    }
    n = n | (b << i);
    currentCount--;
    return (n >>> 1) ^ -(n & 1); // back to two's-complement
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`deltaBlockBuffer[i] = deltaBlockBuffer[i] - minDeltaBase` could be simplified to 'deltaBlockBuffer\[i\] -= minDeltaBase'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/encoder/DeltaBinaryEncoder.java`
#### Snippet
```java
    @Override
    protected void calcTwoDiff(int i) {
      deltaBlockBuffer[i] = deltaBlockBuffer[i] - minDeltaBase;
    }

```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`deltaBlockBuffer[i] = deltaBlockBuffer[i] - minDeltaBase` could be simplified to 'deltaBlockBuffer\[i\] -= minDeltaBase'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/encoder/DeltaBinaryEncoder.java`
#### Snippet
```java
    @Override
    protected void calcTwoDiff(int i) {
      deltaBlockBuffer[i] = deltaBlockBuffer[i] - minDeltaBase;
    }

```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`values[valueIdx] = values[valueIdx] << leftBits` could be simplified to 'values\[valueIdx\] \<\<= leftBits'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/bitpacking/LongPacker.java`
#### Snippet
```java
        if (width - totalBits >= leftBits) {
          // then put left bits in current byte to current long value
          values[valueIdx] = values[valueIdx] << leftBits;
          values[valueIdx] = values[valueIdx] | (((1L << leftBits) - 1) & buf[byteIdx]);
          totalBits += leftBits;
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`values[valueIdx] = values[valueIdx] | (((1L << leftBits) - 1) & buf[byteIdx])` could be simplified to 'values\[valueIdx\] \|= (((1L \<\< leftBits) - 1) \& buf\[byteIdx\])'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/bitpacking/LongPacker.java`
#### Snippet
```java
          // then put left bits in current byte to current long value
          values[valueIdx] = values[valueIdx] << leftBits;
          values[valueIdx] = values[valueIdx] | (((1L << leftBits) - 1) & buf[byteIdx]);
          totalBits += leftBits;
          // get next byte
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`values[valueIdx] = values[valueIdx] << t` could be simplified to 'values\[valueIdx\] \<\<= t'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/bitpacking/LongPacker.java`
#### Snippet
```java
          // numbers of bits to be take
          int t = width - totalBits;
          values[valueIdx] = values[valueIdx] << t;
          values[valueIdx] =
              values[valueIdx] | (((1L << leftBits) - 1) & buf[byteIdx]) >>> (leftBits - t);
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`values[valueIdx] = values[valueIdx] | (((1L << leftBits) - 1) & buf[byteIdx]) >>> (lef...` could be simplified to 'values\[valueIdx\] \|= (((1L \<\< leftBits) - 1) \& buf\[byteIdx\]) \>\>\> (leftBits - t)'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/bitpacking/LongPacker.java`
#### Snippet
```java
          int t = width - totalBits;
          values[valueIdx] = values[valueIdx] << t;
          values[valueIdx] =
              values[valueIdx] | (((1L << leftBits) - 1) & buf[byteIdx]) >>> (leftBits - t);
          leftBits -= t;
          totalBits += t;
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`buffer = buffer & ((1 << totalBits) - 1)` could be simplified to 'buffer \&= ((1 \<\< totalBits) - 1)'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/bitpacking/IntPacker.java`
#### Snippet
```java
        valueIdx++;
        totalBits -= width;
        buffer = buffer & ((1 << totalBits) - 1);
      }
    }
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`systemError = systemError - point.getPower()` could be simplified to 'systemError -= point.getPower()'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/encoder/FreqEncoder.java`
#### Snippet
```java
        }
        keepList.add(point);
        systemError = systemError - point.getPower();
        roundingError = Math.pow(2, this.beta * 2) * keepList.size();
      }
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`ans = ans * 2` could be simplified to 'ans \*= 2'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/encoder/FreqEncoder.java`
#### Snippet
```java
    if (x > 1) {
      while (ans * 2 <= x) {
        ans = ans * 2;
        exponent++;
      }
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`ans = ans / 2` could be simplified to 'ans /= 2'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/encoder/FreqEncoder.java`
#### Snippet
```java
    } else {
      while (ans > x) {
        ans = ans / 2;
        exponent--;
      }
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`doubleValue = doubleValue + 0.1` could be simplified to 'doubleValue += 0.1'
in `example/hadoop/src/main/java/org/apache/iotdb/hadoop/tsfile/TsFileHelper.java`
#### Snippet
```java

      try {
        for (int r = 0; r < rowNum; r++, value++, doubleValue = doubleValue + 0.1) {
          int row = tablet.rowSize++;
          timestamps[row] = timestamp++;
```

### RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
`sum = sum + value.get()` could be simplified to 'sum += value.get()'
in `example/hadoop/src/main/java/org/apache/iotdb/hadoop/tsfile/TSFMRReadExample.java`
#### Snippet
```java
      double sum = 0;
      for (DoubleWritable value : values) {
        sum = sum + value.get();
      }
      context.write(key, new DoubleWritable(sum));
```

## RuleId[ruleID=MismatchedCollectionQueryUpdate]
### RuleId[ruleID=MismatchedCollectionQueryUpdate]
Contents of collection `regionCountPerSeries` are updated, but never queried
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/distribution/SourceRewriter.java`
#### Snippet
```java
    // Step 1: split SeriesAggregationSourceNode according to data partition
    List<SeriesAggregationSourceNode> sources = new ArrayList<>();
    Map<PartialPath, Integer> regionCountPerSeries = new HashMap<>();
    for (SeriesAggregationSourceNode child : rawSources) {
      List<TRegionReplicaSet> dataDistribution =
```

### RuleId[ruleID=MismatchedCollectionQueryUpdate]
Contents of collection `queryScheduledTaskMap` are updated, but never queried
in `server/src/main/java/org/apache/iotdb/db/query/control/QueryTimeManager.java`
#### Snippet
```java
  private ScheduledExecutorService executorService;

  private Map<Long, ScheduledFuture<?>> queryScheduledTaskMap;

  private QueryTimeManager() {
```

### RuleId[ruleID=MismatchedCollectionQueryUpdate]
Contents of collection `newTsFileResources` are updated, but never queried
in `server/src/main/java/org/apache/iotdb/db/tools/settle/TsFileAndModSettleTool.java`
#### Snippet
```java
  public static void settleTsFilesAndMods(Map<String, TsFileResource> resourcesToBeSettled) {
    int successCount = 0;
    Map<String, List<TsFileResource>> newTsFileResources = new HashMap<>();
    SettleLog.createSettleLog();
    for (Map.Entry<String, TsFileResource> entry : resourcesToBeSettled.entrySet()) {
```

### RuleId[ruleID=MismatchedCollectionQueryUpdate]
Contents of collection `targetFileResources` are updated, but never queried
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/task/CompactionRecoverTask.java`
#### Snippet
```java
                    + File.separator
                    + IoTDBConstant.COMPACTION_MODIFICATION_FILE_NAME_FROM_OLD);
        List<TsFileResource> targetFileResources = new ArrayList<>();
        for (int i = 0; i < sourceFileIdentifiers.size(); i++) {
          TsFileIdentifier sourceFileIdentifier = sourceFileIdentifiers.get(i);
```

## RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `INSTANCE` is accessed in both synchronized and unsynchronized contexts
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/service/UDFExecutableManager.java`
#### Snippet
```java
  /////////////////////////////////////////////////////////////////////////////////////////////////

  private static UDFExecutableManager INSTANCE = null;

  public static synchronized UDFExecutableManager setupAndGetInstance(
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `INSTANCE` is accessed in both synchronized and unsynchronized contexts
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/service/UDFClassLoaderManager.java`
#### Snippet
```java
  /////////////////////////////////////////////////////////////////////////////////////////////////

  private static UDFClassLoaderManager INSTANCE = null;

  public static synchronized UDFClassLoaderManager setupAndGetInstance(String libRoot) {
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `accessor` is accessed in both synchronized and unsynchronized contexts
in `node-commons/src/main/java/org/apache/iotdb/commons/auth/role/BasicRoleManager.java`
#### Snippet
```java

  protected Map<String, Role> roleMap;
  protected IRoleAccessor accessor;
  protected HashLock lock;

```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `roleMap` is accessed in both synchronized and unsynchronized contexts
in `node-commons/src/main/java/org/apache/iotdb/commons/auth/role/BasicRoleManager.java`
#### Snippet
```java
public abstract class BasicRoleManager implements IRoleManager {

  protected Map<String, Role> roleMap;
  protected IRoleAccessor accessor;
  protected HashLock lock;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `userMap` is accessed in both synchronized and unsynchronized contexts
in `node-commons/src/main/java/org/apache/iotdb/commons/auth/user/BasicUserManager.java`
#### Snippet
```java
  private static final String NO_SUCH_USER_ERROR = "No such user %s";

  protected Map<String, User> userMap;
  protected IUserAccessor accessor;
  protected HashLock lock;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `accessor` is accessed in both synchronized and unsynchronized contexts
in `node-commons/src/main/java/org/apache/iotdb/commons/auth/user/BasicUserManager.java`
#### Snippet
```java

  protected Map<String, User> userMap;
  protected IUserAccessor accessor;
  protected HashLock lock;

```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `outputStream` is accessed in both synchronized and unsynchronized contexts
in `node-commons/src/main/java/org/apache/iotdb/commons/sync/persistence/SyncLogWriter.java`
#### Snippet
```java
public class SyncLogWriter implements AutoCloseable {
  // record pipe meta info
  private OutputStream outputStream;
  private final File dir;
  private final String fileName;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `INSTANCE` is accessed in both synchronized and unsynchronized contexts
in `node-commons/src/main/java/org/apache/iotdb/commons/trigger/service/TriggerExecutableManager.java`
#### Snippet
```java
  /////////////////////////////////////////////////////////////////////////////////////////////////

  private static TriggerExecutableManager INSTANCE = null;

  public static synchronized TriggerExecutableManager setupAndGetInstance(
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `threadStopLatch` is accessed in both synchronized and unsynchronized contexts
in `node-commons/src/main/java/org/apache/iotdb/commons/service/AbstractThriftServiceThread.java`
#### Snippet
```java
  private TServerTransport serverTransport;
  private TServer poolServer;
  private CountDownLatch threadStopLatch;

  private ExecutorService executorService;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `serverTransport` is accessed in both synchronized and unsynchronized contexts
in `node-commons/src/main/java/org/apache/iotdb/commons/service/AbstractThriftServiceThread.java`
#### Snippet
```java

  private static final Logger logger = LoggerFactory.getLogger(AbstractThriftServiceThread.class);
  private TServerTransport serverTransport;
  private TServer poolServer;
  private CountDownLatch threadStopLatch;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `poolServer` is accessed in both synchronized and unsynchronized contexts
in `node-commons/src/main/java/org/apache/iotdb/commons/service/AbstractThriftServiceThread.java`
#### Snippet
```java
  private static final Logger logger = LoggerFactory.getLogger(AbstractThriftServiceThread.class);
  private TServerTransport serverTransport;
  private TServer poolServer;
  private CountDownLatch threadStopLatch;

```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `EXECUTOR` is accessed in both synchronized and unsynchronized contexts
in `node-commons/src/main/java/org/apache/iotdb/commons/partition/executor/SeriesPartitionExecutor.java`
#### Snippet
```java
  // The params, executorName and seriesPartitionSlotNum, are global unique during system running.
  // Therefore, one executor instance is enough for usage.
  protected static SeriesPartitionExecutor EXECUTOR;

  protected final int seriesPartitionSlotNum;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `transport` is accessed in both synchronized and unsynchronized contexts
in `influxdb-protocol/src/main/java/org/apache/iotdb/influxdb/session/InfluxDBSession.java`
#### Snippet
```java
  private static final Logger logger = LoggerFactory.getLogger(InfluxDBSession.class);

  private TTransport transport;
  private InfluxDBService.Iface client;
  private List<InfluxEndPoint> endPointList = new ArrayList<>();
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `defaultEndPoint` is accessed in both synchronized and unsynchronized contexts
in `influxdb-protocol/src/main/java/org/apache/iotdb/influxdb/session/InfluxDBSession.java`
#### Snippet
```java
  protected int fetchSize;
  protected ZoneId zoneId;
  protected InfluxEndPoint defaultEndPoint;
  protected int thriftDefaultBufferSize;
  protected int thriftMaxFrameSize;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `endPointList` is accessed in both synchronized and unsynchronized contexts
in `influxdb-protocol/src/main/java/org/apache/iotdb/influxdb/session/InfluxDBSession.java`
#### Snippet
```java
  private TTransport transport;
  private InfluxDBService.Iface client;
  private List<InfluxEndPoint> endPointList = new ArrayList<>();
  private long sessionId;

```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `sessionId` is accessed in both synchronized and unsynchronized contexts
in `influxdb-protocol/src/main/java/org/apache/iotdb/influxdb/session/InfluxDBSession.java`
#### Snippet
```java
  private InfluxDBService.Iface client;
  private List<InfluxEndPoint> endPointList = new ArrayList<>();
  private long sessionId;

  protected String username;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `client` is accessed in both synchronized and unsynchronized contexts
in `influxdb-protocol/src/main/java/org/apache/iotdb/influxdb/session/InfluxDBSession.java`
#### Snippet
```java

  private TTransport transport;
  private InfluxDBService.Iface client;
  private List<InfluxEndPoint> endPointList = new ArrayList<>();
  private long sessionId;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `connectionTimeoutInMs` is accessed in both synchronized and unsynchronized contexts
in `influxdb-protocol/src/main/java/org/apache/iotdb/influxdb/session/InfluxDBSession.java`
#### Snippet
```java
  private boolean isClosed = true;
  protected boolean enableRPCCompression;
  protected int connectionTimeoutInMs;

  public InfluxDBSession(String host, int rpcPort, String username, String password) {
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `deviceIDS` is accessed in both synchronized and unsynchronized contexts
in `schema-engine-tag/src/main/java/org/apache/iotdb/db/metadata/tagSchemaRegion/idtable/IDTableWithDeviceIDListImpl.java`
#### Snippet
```java

  // use a deviceID list manager device id -> INT32 id
  private List<IDeviceID> deviceIDS;

  public IDTableWithDeviceIDListImpl(File storageGroupDir) {
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `lsmEngine` is accessed in both synchronized and unsynchronized contexts
in `schema-engine-tag/src/main/java/org/apache/iotdb/db/metadata/tagSchemaRegion/tagIndex/TagInvertedIndex.java`
#### Snippet
```java
  // Directly use the lsm engine that comes with the lsm framework to implement the tag inverted
  // index
  LSMEngine<MemTableGroup> lsmEngine;

  /**
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `blocked` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/SharedTsBlockQueue.java`
#### Snippet
```java
  private final Queue<TsBlock> queue = new LinkedList<>();

  private SettableFuture<Void> blocked = SettableFuture.create();

  private ListenableFuture<Void> blockedOnMemory;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `state` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/StateMachine.java`
#### Snippet
```java
  @SuppressWarnings("java:S3077")
  @GuardedBy("lock")
  private T state;

  @GuardedBy("lock")
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `reservedBytes` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/memory/MemoryPool.java`
#### Snippet
```java
  private final long maxBytesPerQuery;

  private long reservedBytes = 0L;
  private final Map<String, Long> queryMemoryReservations = new HashMap<>();
  private final Queue<MemoryReservationFuture<Void>> memoryReservationFutures = new LinkedList<>();
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `closed` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/LocalSourceHandle.java`
#### Snippet
```java
  private boolean aborted = false;

  private boolean closed = false;

  private int currSequenceId;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `aborted` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/LocalSourceHandle.java`
#### Snippet
```java
  private final SourceHandleListener sourceHandleListener;
  private final SharedTsBlockQueue queue;
  private boolean aborted = false;

  private boolean closed = false;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `aborted` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/LocalSinkHandle.java`
#### Snippet
```java
  private final SharedTsBlockQueue queue;
  private volatile ListenableFuture<Void> blocked = immediateFuture(null);
  private boolean aborted = false;
  private boolean closed = false;

```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `noMoreTsBlocks` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/SinkHandle.java`
#### Snippet
```java
  private boolean closed = false;

  private boolean noMoreTsBlocks = false;

  public SinkHandle(
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `nextSequenceId` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/SinkHandle.java`
#### Snippet
```java

  private volatile ListenableFuture<Void> blocked;
  private int nextSequenceId = 0;
  /** The actual buffered memory in bytes, including the amount of memory being reserved. */
  private long bufferRetainedSizeInBytes;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `bufferRetainedSizeInBytes` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/SourceHandle.java`
#### Snippet
```java

  /** The actual buffered memory in bytes, including the amount of memory being reserved. */
  private long bufferRetainedSizeInBytes = 0L;

  private int currSequenceId = 0;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `aborted` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/SourceHandle.java`
#### Snippet
```java
  private int nextSequenceId = 0;
  private int lastSequenceId = Integer.MAX_VALUE;
  private boolean aborted = false;

  private boolean closed = false;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `compressionRatio` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/conf/adapter/CompressionRatio.java`
#### Snippet
```java
  private static final double DEFAULT_COMPRESSION_RATIO = 2.0;

  private AtomicDouble compressionRatio = new AtomicDouble(DEFAULT_COMPRESSION_RATIO);

  /** The total sum of all compression ratios. */
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `directory` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/conf/adapter/CompressionRatio.java`
#### Snippet
```java
  private long calcTimes;

  private File directory;

  private CompressionRatio() {
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `compressionRatioSum` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/conf/adapter/CompressionRatio.java`
#### Snippet
```java

  /** The total sum of all compression ratios. */
  private double compressionRatioSum;

  /** The number of compression ratios. */
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `calcTimes` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/conf/adapter/CompressionRatio.java`
#### Snippet
```java

  /** The number of compression ratios. */
  private long calcTimes;

  private File directory;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `isCollectFinished` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/sync/sender/pipe/TsFilePipe.java`
#### Snippet
```java

  /* whether finish collect history file. If false, no need to collect realtime file*/
  private boolean isCollectFinished;
  /**
   * Write lock needs to be added to block the real-time data collection process when collecting
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `transport` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/sync/transport/client/IoTDBSyncClient.java`
#### Snippet
```java
  private static final int TRANSFER_BUFFER_SIZE_IN_BYTES = 1 * 1024 * 1024;

  private TTransport transport = null;
  private volatile IClientRPCService.Client serviceClient = null;

```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `outputStream` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/sync/pipedata/queue/BufferedPipeDataQueue.java`
#### Snippet
```java

  private BlockingDeque<Long> pipeLogStartNumber;
  private DataOutputStream outputStream;
  private long currentPipeLogSize;

```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `commitSerialNumber` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/sync/pipedata/queue/BufferedPipeDataQueue.java`
#### Snippet
```java

  private long pullSerialNumber;
  private long commitSerialNumber;
  private DataOutputStream commitLogWriter;
  private long currentCommitLogSize;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `outputDeque` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/sync/pipedata/queue/BufferedPipeDataQueue.java`
#### Snippet
```java
  private final Object waitLock = new Object();

  private BlockingDeque<PipeData> outputDeque;

  private long pullSerialNumber;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `currentPipeLogSize` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/sync/pipedata/queue/BufferedPipeDataQueue.java`
#### Snippet
```java
  private BlockingDeque<Long> pipeLogStartNumber;
  private DataOutputStream outputStream;
  private long currentPipeLogSize;

  /** output */
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `pipeLogStartNumber` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/sync/pipedata/queue/BufferedPipeDataQueue.java`
#### Snippet
```java
  private BlockingDeque<PipeData> inputDeque;

  private BlockingDeque<Long> pipeLogStartNumber;
  private DataOutputStream outputStream;
  private long currentPipeLogSize;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `inputDeque` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/sync/pipedata/queue/BufferedPipeDataQueue.java`
#### Snippet
```java
  private long lastMaxSerialNumber;

  private BlockingDeque<PipeData> inputDeque;

  private BlockingDeque<Long> pipeLogStartNumber;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `lostConnectionTime` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/sync/transport/client/SenderManager.java`
#### Snippet
```java

  protected long lastReportTime = 0;
  protected long lostConnectionTime = Long.MAX_VALUE;

  private boolean isRunning;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `modificationList` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/TsFileOpBlock.java`
#### Snippet
```java
  // Save all modifications that are from .mods file.
  // (modificationList == null) means no .mods file or .mods file is empty.
  Collection<Modification> modificationList;
  // HashMap: measurement FullPath => DeletionGroup(save deletion info)
  private Map<String, DeletionGroup> fullPathToDeletionMap;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `fullPathToDeletionMap` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/TsFileOpBlock.java`
#### Snippet
```java
  Collection<Modification> modificationList;
  // HashMap: measurement FullPath => DeletionGroup(save deletion info)
  private Map<String, DeletionGroup> fullPathToDeletionMap;

  // LRUMap: startIndexOfPage => PageData
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `tsFileName` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/TsFileOpBlock.java`
#### Snippet
```java

  // tsFile name
  private String tsFileName;
  private String modsFileName;
  private TsFileFullReader tsFileFullSeqReader;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `fullTsMetadataMap` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/TsFileOpBlock.java`
#### Snippet
```java

  // full Timeseries Metadata TreeMap : FileOffset => Pair(MeasurementFullPath, TimeseriesMetadata)
  private Map<Long, Pair<Path, TimeseriesMetadata>> fullTsMetadataMap;
  // TreeMap: IndexInFile => ChunkInfo (measurementFullPath, ChunkOffset, PointCount, deletedFlag)
  private TreeMap<Long, ChunkInfo> indexToChunkInfoMap;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `dataReady` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/TsFileOpBlock.java`
#### Snippet
```java
  private LRUCache<Long, List<TimeValuePair>> pageCache;

  private boolean dataReady = false;

  Decoder timeDecoder =
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `tsFileFullSeqReader` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/TsFileOpBlock.java`
#### Snippet
```java
  private String tsFileName;
  private String modsFileName;
  private TsFileFullReader tsFileFullSeqReader;

  // full Timeseries Metadata TreeMap : FileOffset => Pair(MeasurementFullPath, TimeseriesMetadata)
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `modsFileName` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/TsFileOpBlock.java`
#### Snippet
```java
  // tsFile name
  private String tsFileName;
  private String modsFileName;
  private TsFileFullReader tsFileFullSeqReader;

```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `indexToChunkInfoMap` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/TsFileOpBlock.java`
#### Snippet
```java
  private Map<Long, Pair<Path, TimeseriesMetadata>> fullTsMetadataMap;
  // TreeMap: IndexInFile => ChunkInfo (measurementFullPath, ChunkOffset, PointCount, deletedFlag)
  private TreeMap<Long, ChunkInfo> indexToChunkInfoMap;

  // Save all modifications that are from .mods file.
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `maxSize` is accessed in both synchronized and unsynchronized contexts
in `session/src/main/java/org/apache/iotdb/session/pool/SessionPool.java`
#### Snippet
```java
  private final ConcurrentMap<Session, Session> occupied = new ConcurrentHashMap<>();
  private int size = 0;
  private int maxSize = 0;
  private final long waitToGetSessionTimeoutInMs;

```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `closed` is accessed in both synchronized and unsynchronized contexts
in `session/src/main/java/org/apache/iotdb/session/pool/SessionPool.java`
#### Snippet
```java

  // whether the queue is closed.
  private boolean closed;

  // Redirect-able SessionPool
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `blockingQueueArray` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/query/dataset/NonAlignEngineDataSet.java`
#### Snippet
```java

  // Blocking queue list for each time value buffer pair
  private BlockingQueue<Pair<ByteBuffer, ByteBuffer>>[] blockingQueueArray;

  private boolean initialized = false;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `offsetArray` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/query/dataset/NonAlignEngineDataSet.java`
#### Snippet
```java
  private boolean initialized = false;

  private AtomicIntegerArray offsetArray;

  private int limit;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `fetchSize` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/query/dataset/NonAlignEngineDataSet.java`
#### Snippet
```java
  private boolean[] noMoreDataInQueueArray;

  private int fetchSize;

  // capacity for blocking queue
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `seriesReaderWithoutValueFilterList` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/query/dataset/NonAlignEngineDataSet.java`
#### Snippet
```java
  }

  private List<ManagedSeriesReader> seriesReaderWithoutValueFilterList;

  // Blocking queue list for each time value buffer pair
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `alreadyReturnedRowNumArray` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/query/dataset/NonAlignEngineDataSet.java`
#### Snippet
```java
  private int limit;

  private AtomicIntegerArray alreadyReturnedRowNumArray;

  private BatchData[] cachedBatchData;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `limit` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/query/dataset/NonAlignEngineDataSet.java`
#### Snippet
```java
  private AtomicIntegerArray offsetArray;

  private int limit;

  private AtomicIntegerArray alreadyReturnedRowNumArray;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `cachedBatchData` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/query/dataset/NonAlignEngineDataSet.java`
#### Snippet
```java
  private AtomicIntegerArray alreadyReturnedRowNumArray;

  private BatchData[] cachedBatchData;

  // indicate that there is no more batch data in the corresponding queue
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `seriesReaderList` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/query/dataset/RawQueryDataSetWithoutValueFilter.java`
#### Snippet
```java
  }

  protected List<ManagedSeriesReader> seriesReaderList;

  protected TimeSelector timeHeap;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `defaultSessionConnection` is accessed in both synchronized and unsynchronized contexts
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java

  protected TEndPoint defaultEndPoint;
  protected SessionConnection defaultSessionConnection;
  private boolean isClosed = true;

```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `enableQueryRedirection` is accessed in both synchronized and unsynchronized contexts
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
  protected volatile Map<TEndPoint, SessionConnection> endPointToSessionConnection;

  protected boolean enableQueryRedirection = false;

  // The version number of the client which used for compatibility in the server
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `enableRedirection` is accessed in both synchronized and unsynchronized contexts
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java

  // Cluster version cache
  protected boolean enableRedirection;
  protected volatile Map<String, TEndPoint> deviceIdToEndpoint;
  protected volatile Map<TEndPoint, SessionConnection> endPointToSessionConnection;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `zoneId` is accessed in both synchronized and unsynchronized contexts
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
  protected boolean enableRPCCompression;
  protected int connectionTimeoutInMs;
  protected ZoneId zoneId;

  protected int thriftDefaultBufferSize;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `data` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/utils/CopyOnReadLinkedList.java`
#### Snippet
```java
public class CopyOnReadLinkedList<T> {

  LinkedList<T> data = new LinkedList<>();
  List<T> readCopy;

```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `tsFileProcessorQueue` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/engine/flush/FlushManager.java`
#### Snippet
```java
  private final IoTDBConfig config = IoTDBDescriptor.getInstance().getConfig();

  private ConcurrentLinkedDeque<TsFileProcessor> tsFileProcessorQueue =
      new ConcurrentLinkedDeque<>();

```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `flushPool` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/engine/flush/FlushManager.java`
#### Snippet
```java
      new ConcurrentLinkedDeque<>();

  private FlushTaskPoolManager flushPool = FlushTaskPoolManager.getInstance();

  @Override
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `client` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/engine/trigger/sink/alertmanager/AlertManagerHandler.java`
#### Snippet
```java
public class AlertManagerHandler implements Handler<AlertManagerConfiguration, AlertManagerEvent> {

  private static CloseableHttpClient client;
  private static int referenceCount;

```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `client` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/engine/trigger/sink/forward/http/HTTPForwardHandler.java`
#### Snippet
```java
  private static final Logger LOGGER = LoggerFactory.getLogger(HTTPForwardHandler.class);

  private static CloseableHttpClient client;
  private static int referenceCount;

```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `prevVersion` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/engine/version/SimpleFileVersionController.java`
#### Snippet
```java
  private long timePartitionId;

  private long prevVersion;
  private long currVersion;
  private String directoryPath;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `currVersion` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/engine/version/SimpleFileVersionController.java`
#### Snippet
```java

  private long prevVersion;
  private long currVersion;
  private String directoryPath;

```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `saveInterval` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/engine/version/SimpleFileVersionController.java`
#### Snippet
```java
   * by saveInterval to avoid conflicts.
   */
  private static long saveInterval = 100;
  /** time partition id to dividing time series into different database */
  private long timePartitionId;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `list` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/engine/memtable/WritableMemChunk.java`
#### Snippet
```java

  private IMeasurementSchema schema;
  private TVList list;
  private static final String UNSUPPORTED_TYPE = "Unsupported data type:";
  private static final Logger LOGGER = LoggerFactory.getLogger(WritableMemChunk.class);
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `subCompactionTaskExecutionPool` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/CompactionTaskManager.java`
#### Snippet
```java

  // The thread pool that executes the sub compaction task.
  private WrappedThreadPoolExecutor subCompactionTaskExecutionPool;

  public static volatile AtomicInteger currentTaskNum = new AtomicInteger(0);
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `taskExecutionPool` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/CompactionTaskManager.java`
#### Snippet
```java
  // The thread pool that executes the compaction task. The default number of threads for this pool
  // is 10.
  private WrappedThreadPoolExecutor taskExecutionPool;

  // The thread pool that executes the sub compaction task.
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `list` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/engine/memtable/AlignedWritableMemChunk.java`
#### Snippet
```java
  private final Map<String, Integer> measurementIndexMap;
  private final List<IMeasurementSchema> schemaList;
  private AlignedTVList list;

  private static final int maxNumberOfPointsInPage =
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `chunkWriters` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/writer/AbstractCompactionWriter.java`
#### Snippet
```java
  // Each sub task has its own chunk writer.
  // The index of the array corresponds to subTaskId.
  protected IChunkWriter[] chunkWriters = new IChunkWriter[subTaskNum];

  protected boolean isAlign;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `totalFilledBytesNum` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/engine/modification/utils/TracedBufferedReader.java`
#### Snippet
```java

  /** the total bytes number already filled into cb */
  private long totalFilledBytesNum = 0;

  /**
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `nextChar` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/engine/modification/utils/TracedBufferedReader.java`
#### Snippet
```java

  private char cb[];
  private int nChars, nextChar;

  private static final int INVALIDATED = -2;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `nChars` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/engine/modification/utils/TracedBufferedReader.java`
#### Snippet
```java

  private char cb[];
  private int nChars, nextChar;

  private static final int INVALIDATED = -2;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `timePartitionInfoMemoryThreshold` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TimePartitionManager.java`
#### Snippet
```java

  long memCost = 0;
  long timePartitionInfoMemoryThreshold =
      IoTDBDescriptor.getInstance().getConfig().getAllocateMemoryForTimePartitionInfo();

```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `pool` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/rescon/AbstractPoolManager.java`
#### Snippet
```java
  private static final int WAIT_TIMEOUT = 2000;

  protected ExecutorService pool;

  /** Block new flush submits and exit when all RUNNING THREADS AND TASKS IN THE QUEUE end. */
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `currentMemtableNumber` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/rescon/MemTableManager.java`
#### Snippet
```java
  private static final int WAIT_TIME = 100;
  public static final int MEMTABLE_NUM_FOR_EACH_PARTITION = 4;
  private int currentMemtableNumber = 0;

  private MemTableManager() {}
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `limitUpdateThreshold` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/rescon/PrimitiveArrayManager.java`
#### Snippet
```java

  /** LIMITS should be updated if (TOTAL_ALLOCATION_REQUEST_COUNT.get() > limitUpdateThreshold) */
  private static long limitUpdateThreshold;

  /** TSDataType#serialize() -> count of allocation requests, VECTOR is ignored */
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `TIME_INDEX_MEMORY_THRESHOLD` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/rescon/TsFileResourceManager.java`
#### Snippet
```java

  /** threshold total memory for all TimeIndex */
  private double TIME_INDEX_MEMORY_THRESHOLD = CONFIG.getAllocateMemoryForTimeIndex();

  /** store the sealed TsFileResource, sorted by priority of TimeIndex */
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `compactionMemoryCost` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/rescon/SystemInfo.java`
#### Snippet
```java

  private long flushingMemTablesCost = 0L;
  private AtomicLong compactionMemoryCost = new AtomicLong(0L);

  private ExecutorService flushTaskSubmitThreadPool =
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `memorySizeForMemtable` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/rescon/SystemInfo.java`
#### Snippet
```java
  private volatile boolean rejected = false;

  private long memorySizeForMemtable;
  private long memorySizeForCompaction;
  private Map<DataRegionInfo, Long> reportedStorageGroupMemCostMap = new HashMap<>();
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `FLUSH_THERSHOLD` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/rescon/SystemInfo.java`
#### Snippet
```java
  private ExecutorService flushTaskSubmitThreadPool =
      IoTDBThreadPoolFactory.newSingleThreadExecutor(ThreadName.FLUSH_TASK_SUBMIT.getName());
  private double FLUSH_THERSHOLD = memorySizeForMemtable * config.getFlushProportion();
  private double REJECT_THERSHOLD = memorySizeForMemtable * config.getRejectProportion();

```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `REJECT_THERSHOLD` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/rescon/SystemInfo.java`
#### Snippet
```java
      IoTDBThreadPoolFactory.newSingleThreadExecutor(ThreadName.FLUSH_TASK_SUBMIT.getName());
  private double FLUSH_THERSHOLD = memorySizeForMemtable * config.getFlushProportion();
  private double REJECT_THERSHOLD = memorySizeForMemtable * config.getRejectProportion();

  private volatile boolean isEncodingFasterThanIo = true;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `reportedStorageGroupMemCostMap` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/rescon/SystemInfo.java`
#### Snippet
```java
  private long memorySizeForMemtable;
  private long memorySizeForCompaction;
  private Map<DataRegionInfo, Long> reportedStorageGroupMemCostMap = new HashMap<>();

  private long flushingMemTablesCost = 0L;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `totalStorageGroupMemCost` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/rescon/SystemInfo.java`
#### Snippet
```java
  private static final Logger logger = LoggerFactory.getLogger(SystemInfo.class);

  private long totalStorageGroupMemCost = 0L;
  private volatile boolean rejected = false;

```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `regionMigratePool` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/service/RegionMigrateService.java`
#### Snippet
```java
  private static final int SLEEP_MILLIS = 5000;

  private RegionMigratePool regionMigratePool;

  private RegionMigrateService() {}
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `dataRegionInfo` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileProcessor.java`
#### Snippet
```java

  /** database info for mem control */
  private DataRegionInfo dataRegionInfo;
  /** tsfile processor info for mem control */
  private TsFileProcessorInfo tsFileProcessorInfo;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `writer` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileProcessor.java`
#### Snippet
```java

  /** writer for restore tsfile and flushing */
  private RestorableTsFileIOWriter writer;

  /** tsfile resource for index this tsfile */
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `maxPlanIndex` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileResource.java`
#### Snippet
```java

  /** Maximum index of plans executed within this TsFile. */
  protected long maxPlanIndex = Long.MIN_VALUE;

  /** Minimum index of plans executed within this TsFile. */
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `file` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileResource.java`
#### Snippet
```java

  /** this tsfile */
  private File file;

  public static final String RESOURCE_SUFFIX = ".resource";
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `timeIndex` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileResource.java`
#### Snippet
```java

  /** time index */
  protected ITimeIndex timeIndex;

  private volatile ModificationFile modFile;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `minPlanIndex` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileResource.java`
#### Snippet
```java

  /** Minimum index of plans executed within this TsFile. */
  protected long minPlanIndex = Long.MAX_VALUE;

  private long version = 0;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `fsFactory` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileResource.java`
#### Snippet
```java
  private boolean isSeq;

  private FSFactory fsFactory = FSFactoryProducer.getFSFactory();

  /** generated upgraded TsFile ResourceList used for upgrading v0.11.x/v2 -> 0.12/v3 */
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `lastCacheValue` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/metadata/cache/lastCache/container/LastCacheContainer.java`
#### Snippet
```java
public class LastCacheContainer implements ILastCacheContainer {

  ILastCacheValue lastCacheValue;

  @Override
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `tagLogFile` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/metadata/tag/TagManager.java`
#### Snippet
```java

  private String sgSchemaDirPath;
  private TagLogFile tagLogFile;
  // tag key -> tag value -> LeafMNode
  private Map<String, Map<String, Set<IMeasurementMNode>>> tagIndex = new ConcurrentHashMap<>();
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `nodeBuffer` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/cache/CacheManager.java`
#### Snippet
```java

  // The nodeBuffer helps to quickly locate the volatile subtree
  private NodeBuffer nodeBuffer = new NodeBuffer();

  public void initRootStatus(IMNode root) {
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `memManager` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/cache/CacheManager.java`
#### Snippet
```java
public abstract class CacheManager implements ICacheManager {

  private IMemManager memManager = MemManagerHolder.getMemManagerInstance();

  // The nodeBuffer helps to quickly locate the volatile subtree
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `storageGroupName` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/DataRegion.java`
#### Snippet
```java
  private String dataRegionId;
  /** database name */
  private String storageGroupName;
  /** database system directory */
  private File storageGroupSysDir;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `closingUnSequenceTsFileProcessor` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/DataRegion.java`
#### Snippet
```java

  /** unsequence tsfile processors which are closing */
  private CopyOnReadLinkedList<TsFileProcessor> closingUnSequenceTsFileProcessor =
      new CopyOnReadLinkedList<>();

```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `tsFileManager` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/DataRegion.java`
#### Snippet
```java
  private File storageGroupSysDir;
  /** manage seqFileList and unSeqFileList */
  private TsFileManager tsFileManager;

  /** manage tsFileResource degrade */
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `dataTTL` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/DataRegion.java`
#### Snippet
```java
   * eventually removed.
   */
  private long dataTTL = Long.MAX_VALUE;
  /** file system factory (local or hdfs) */
  private FSFactory fsFactory = FSFactoryProducer.getFSFactory();
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `dataRegionId` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/DataRegion.java`
#### Snippet
```java

  /** data region id */
  private String dataRegionId;
  /** database name */
  private String storageGroupName;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `closingSequenceTsFileProcessor` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/DataRegion.java`
#### Snippet
```java
  private List<TsFileResource> upgradeSeqFileList = new LinkedList<>();
  /** sequence tsfile processors which are closing */
  private CopyOnReadLinkedList<TsFileProcessor> closingSequenceTsFileProcessor =
      new CopyOnReadLinkedList<>();
  // upgrading unsequence TsFile resource list
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `fileChannel` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/metadata/tag/TagLogFile.java`
#### Snippet
```java
  private static final Logger logger = LoggerFactory.getLogger(TagLogFile.class);
  private File tagFile;
  private FileChannel fileChannel;
  private static final String LENGTH_EXCEED_MSG =
      "Tag/Attribute exceeds the max length limit. "
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `firstLeaf` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/InternalPage.java`
#### Snippet
```java
  protected static int COMPOUND_POINT_LENGTH = 8;

  private long firstLeaf;
  private int subIndexPage;

```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `subIndexPage` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/InternalPage.java`
#### Snippet
```java

  private long firstLeaf;
  private int subIndexPage;

  private transient String penultKey, lastKey;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `COMPOUND_POINT_LENGTH` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/InternalPage.java`
#### Snippet
```java
public class InternalPage extends SchemaPage implements ISegment<Integer, Integer> {

  protected static int COMPOUND_POINT_LENGTH = 8;

  private long firstLeaf;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `aliasKeyList` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/WrappedSegment.java`
#### Snippet
```java

  // reconstruct every initiation after keyAddressList but not write into buffer
  private List<Pair<String, String>> aliasKeyList;

  /**
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `childCache` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/CachedMNodeContainer.java`
#### Snippet
```java
  private long segmentAddress = -1;

  private Map<String, IMNode> childCache = null;
  private Map<String, IMNode> newChildBuffer = null;
  private Map<String, IMNode> updatedChildBuffer = null;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `updatedChildBuffer` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/CachedMNodeContainer.java`
#### Snippet
```java
  private Map<String, IMNode> childCache = null;
  private Map<String, IMNode> newChildBuffer = null;
  private Map<String, IMNode> updatedChildBuffer = null;

  @Override
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `newChildBuffer` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/CachedMNodeContainer.java`
#### Snippet
```java

  private Map<String, IMNode> childCache = null;
  private Map<String, IMNode> newChildBuffer = null;
  private Map<String, IMNode> updatedChildBuffer = null;

```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `idTableMap` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/metadata/idtable/IDTableManager.java`
#### Snippet
```java

  /** database path -> id table */
  HashMap<String, IDTable> idTableMap;

  /** system dir */
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `IDiskSchemaManager` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/metadata/idtable/IDTableHashmapImpl.java`
#### Snippet
```java

  /** disk schema manager to manage disk schema entry */
  private IDiskSchemaManager IDiskSchemaManager;
  /** iotdb config */
  protected static IoTDBConfig config = IoTDBDescriptor.getInstance().getConfig();
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `md` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/metadata/idtable/entry/SHA256DeviceID.java`
#### Snippet
```java

  /** using lots of message digest for improving parallelism */
  private static MessageDigest[] md;

  /** number of message digest, for improve parallelism */
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `readBytes` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/metadata/logfile/SchemaLogReader.java`
#### Snippet
```java
    private final InputStream inputStream;

    private long readBytes = 0;

    private long mark;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `logWriter` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/metadata/logfile/MLogWriter.java`
#### Snippet
```java

  private final File logFile;
  private LogWriter logWriter;
  private int logNum;
  private final ByteBuffer mlogBuffer =
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `mtree` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/metadata/storagegroup/StorageGroupSchemaManager.java`
#### Snippet
```java
  private StorageGroupLogWriter logWriter;

  private ConfigMTree mtree;

  private boolean isRecover = true;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `logWriter` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/metadata/storagegroup/StorageGroupSchemaManager.java`
#### Snippet
```java
  private IoTDBConfig config = IoTDBDescriptor.getInstance().getConfig();

  private StorageGroupLogWriter logWriter;

  private ConfigMTree mtree;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `config` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/metadata/storagegroup/StorageGroupSchemaManager.java`
#### Snippet
```java
  private static final Logger logger = LoggerFactory.getLogger(StorageGroupSchemaManager.class);

  private IoTDBConfig config = IoTDBDescriptor.getInstance().getConfig();

  private StorageGroupLogWriter logWriter;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `isRecover` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/metadata/storagegroup/StorageGroupSchemaManager.java`
#### Snippet
```java
  private ConfigMTree mtree;

  private boolean isRecover = true;

  private static class StorageGroupManagerHolder {
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `store` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/MTreeBelowSGMemoryImpl.java`
#### Snippet
```java

  // this implementation is based on memory, thus only MTree write operation must invoke MTreeStore
  private MemMTreeStore store;
  private volatile IStorageGroupMNode storageGroupMNode;
  private final Function<IMeasurementMNode, Map<String, String>> tagGetter;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `schemaRegionMap` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/SchemaEngine.java`
#### Snippet
```java
  private ConfigMTree sharedPrefixTree;

  private Map<SchemaRegionId, ISchemaRegion> schemaRegionMap;
  private SchemaEngineMode schemaRegionStoredMode;

```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `sharedPrefixTree` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/SchemaEngine.java`
#### Snippet
```java
  private static final IoTDBConfig config = IoTDBDescriptor.getInstance().getConfig();

  private ConfigMTree sharedPrefixTree;

  private Map<SchemaRegionId, ISchemaRegion> schemaRegionMap;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `schemaMap` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/metadata/template/Template.java`
#### Snippet
```java
  private boolean isDirectAligned;
  private int measurementsCount;
  private Map<String, IMeasurementSchema> schemaMap;

  // accelerate template query and check
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `isDirectAligned` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/metadata/template/Template.java`
#### Snippet
```java
  private String name;
  private Map<String, IMNode> directNodes;
  private boolean isDirectAligned;
  private int measurementsCount;
  private Map<String, IMeasurementSchema> schemaMap;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `measurementsCount` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/metadata/template/Template.java`
#### Snippet
```java
  private Map<String, IMNode> directNodes;
  private boolean isDirectAligned;
  private int measurementsCount;
  private Map<String, IMeasurementSchema> schemaMap;

```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `directNodes` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/metadata/template/Template.java`
#### Snippet
```java
  private int id;
  private String name;
  private Map<String, IMNode> directNodes;
  private boolean isDirectAligned;
  private int measurementsCount;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `database2Measurement2TagOrders` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/meta/AbstractInfluxDBMetaManager.java`
#### Snippet
```java

  // TODO avoid OOM
  protected static Map<String, Map<String, Map<String, Integer>>> database2Measurement2TagOrders =
      new HashMap<>();

```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `INSTANCE` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/consensus/DataRegionConsensusImpl.java`
#### Snippet
```java
  private static final IoTDBConfig conf = IoTDBDescriptor.getInstance().getConfig();

  private static IConsensus INSTANCE = null;

  private DataRegionConsensusImpl() {}
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `INSTANCE` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/consensus/SchemaRegionConsensusImpl.java`
#### Snippet
```java
  private static final IoTDBConfig conf = IoTDBDescriptor.getInstance().getConfig();

  private static IConsensus INSTANCE = null;

  private SchemaRegionConsensusImpl() {}
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `schemaRegionDirPath` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/SchemaRegionMemoryImpl.java`
#### Snippet
```java

  private String storageGroupDirPath;
  private String schemaRegionDirPath;
  private String storageGroupFullPath;
  private SchemaRegionId schemaRegionId;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `usingMLog` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/SchemaRegionMemoryImpl.java`
#### Snippet
```java

  // the log file writer
  private boolean usingMLog = true;
  private SchemaLogWriter<ISchemaRegionPlan> logWriter;

```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `mNodeCache` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/SchemaRegionMemoryImpl.java`
#### Snippet
```java
  private MTreeBelowSGMemoryImpl mtree;
  // device -> DeviceMNode
  private LoadingCache<PartialPath, IMNode> mNodeCache;
  private TagManager tagManager;

```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `schemaStatisticsManager` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/SchemaRegionMemoryImpl.java`
#### Snippet
```java
  private SchemaLogWriter<ISchemaRegionPlan> logWriter;

  private SchemaStatisticsManager schemaStatisticsManager = SchemaStatisticsManager.getInstance();
  private MemoryStatistics memoryStatistics = MemoryStatistics.getInstance();

```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `logWriter` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/SchemaRegionMemoryImpl.java`
#### Snippet
```java
  // the log file writer
  private boolean usingMLog = true;
  private SchemaLogWriter<ISchemaRegionPlan> logWriter;

  private SchemaStatisticsManager schemaStatisticsManager = SchemaStatisticsManager.getInstance();
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `schemaRegionId` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/SchemaRegionMemoryImpl.java`
#### Snippet
```java
  private String schemaRegionDirPath;
  private String storageGroupFullPath;
  private SchemaRegionId schemaRegionId;

  // the log file writer
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `storageGroupFullPath` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/SchemaRegionMemoryImpl.java`
#### Snippet
```java
  private String storageGroupDirPath;
  private String schemaRegionDirPath;
  private String storageGroupFullPath;
  private SchemaRegionId schemaRegionId;

```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `tagManager` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/SchemaRegionMemoryImpl.java`
#### Snippet
```java
  // device -> DeviceMNode
  private LoadingCache<PartialPath, IMNode> mNodeCache;
  private TagManager tagManager;

  // seriesNumberMonitor may be null
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `mtree` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/SchemaRegionMemoryImpl.java`
#### Snippet
```java

  private final IStorageGroupMNode storageGroupMNode;
  private MTreeBelowSGMemoryImpl mtree;
  // device -> DeviceMNode
  private LoadingCache<PartialPath, IMNode> mNodeCache;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `isRecovering` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/SchemaRegionMemoryImpl.java`
#### Snippet
```java
  protected static IoTDBConfig config = IoTDBDescriptor.getInstance().getConfig();

  private boolean isRecovering = true;
  private volatile boolean initialized = false;

```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `config` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/SchemaRegionMemoryImpl.java`
#### Snippet
```java
  private static final Logger logger = LoggerFactory.getLogger(SchemaRegionMemoryImpl.class);

  protected static IoTDBConfig config = IoTDBDescriptor.getInstance().getConfig();

  private boolean isRecovering = true;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `regionIds` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/localconfignode/LocalDataPartitionTable.java`
#### Snippet
```java
  private String storageGroupName;
  private final int regionNum;
  private DataRegionId[] regionIds;

  public LocalDataPartitionTable(String storageGroupName, List<DataRegionId> regions) {
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `table` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/localconfignode/LocalSchemaPartitionTable.java`
#### Snippet
```java
  private AtomicInteger schemaRegionIdGenerator;

  private Map<PartialPath, List<SchemaRegionId>> table;

  private static class LocalSchemaPartitionTableHolder {
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `partitionTableMap` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/localconfignode/LocalDataPartitionInfo.java`
#### Snippet
```java

  // storageGroup -> LocalDataPartitionTable
  private Map<PartialPath, LocalDataPartitionTable> partitionTableMap;

  private static class LocalDataPartitionTableHolder {
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `executor` is accessed in both synchronized and unsynchronized contexts
in `confignode/src/main/java/org/apache/iotdb/confignode/manager/ProcedureManager.java`
#### Snippet
```java

  private final ConfigManager configManager;
  private ProcedureExecutor<ConfigNodeProcedureEnv> executor;
  private ProcedureScheduler scheduler;
  private IProcedureStore store;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `mtree` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/SchemaRegionSchemaFileImpl.java`
#### Snippet
```java

  private final IStorageGroupMNode storageGroupMNode;
  private MTreeBelowSGCachedImpl mtree;
  // device -> DeviceMNode
  private final LoadingCache<PartialPath, IMNode> mNodeCache;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `isClearing` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/SchemaRegionSchemaFileImpl.java`
#### Snippet
```java
  private boolean isRecovering = true;
  private volatile boolean initialized = false;
  private boolean isClearing = false;

  private final String storageGroupDirPath;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `logWriter` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/SchemaRegionSchemaFileImpl.java`
#### Snippet
```java
  //  private String logFilePath;
  //  private File logFile;
  private SchemaLogWriter<ISchemaRegionPlan> logWriter;

  private final SchemaStatisticsManager schemaStatisticsManager =
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `isRecovering` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/SchemaRegionSchemaFileImpl.java`
#### Snippet
```java
  protected static IoTDBConfig config = IoTDBDescriptor.getInstance().getConfig();

  private boolean isRecovering = true;
  private volatile boolean initialized = false;
  private boolean isClearing = false;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `tagManager` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/SchemaRegionSchemaFileImpl.java`
#### Snippet
```java
  // device -> DeviceMNode
  private final LoadingCache<PartialPath, IMNode> mNodeCache;
  private TagManager tagManager;

  // seriesNumberMonitor may be null
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `usingMLog` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/SchemaRegionSchemaFileImpl.java`
#### Snippet
```java

  // the log file writer
  private boolean usingMLog = true;
  // the log file seriesPath
  //  private String logFilePath;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `config` is accessed in both synchronized and unsynchronized contexts
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/SchemaRegionSchemaFileImpl.java`
#### Snippet
```java
  private static final Logger logger = LoggerFactory.getLogger(SchemaRegionSchemaFileImpl.class);

  protected static IoTDBConfig config = IoTDBDescriptor.getInstance().getConfig();

  private boolean isRecovering = true;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `startIndex` is accessed in both synchronized and unsynchronized contexts
in `confignode/src/main/java/org/apache/iotdb/confignode/consensus/statemachine/ConfigNodeRegionStateMachine.java`
#### Snippet
```java
  private LogWriter logWriter;
  private File logFile;
  private int startIndex;
  private int endIndex;

```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `logFile` is accessed in both synchronized and unsynchronized contexts
in `confignode/src/main/java/org/apache/iotdb/confignode/consensus/statemachine/ConfigNodeRegionStateMachine.java`
#### Snippet
```java
  private ConfigManager configManager;
  private LogWriter logWriter;
  private File logFile;
  private int startIndex;
  private int endIndex;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `logWriter` is accessed in both synchronized and unsynchronized contexts
in `confignode/src/main/java/org/apache/iotdb/confignode/consensus/statemachine/ConfigNodeRegionStateMachine.java`
#### Snippet
```java
  private final ConfigPlanExecutor executor;
  private ConfigManager configManager;
  private LogWriter logWriter;
  private File logFile;
  private int startIndex;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `endIndex` is accessed in both synchronized and unsynchronized contexts
in `confignode/src/main/java/org/apache/iotdb/confignode/consensus/statemachine/ConfigNodeRegionStateMachine.java`
#### Snippet
```java
  private File logFile;
  private int startIndex;
  private int endIndex;

  private static final String currentFileDir =
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `state` is accessed in both synchronized and unsynchronized contexts
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/Procedure.java`
#### Snippet
```java
  private long submittedTime;

  private ProcedureState state = ProcedureState.INITIALIZING;
  private int childrenLatch = 0;
  private ProcedureException exception;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `stackIndexes` is accessed in both synchronized and unsynchronized contexts
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/Procedure.java`
#### Snippet
```java
  private boolean lockedWhenLoading = false;

  private int[] stackIndexes = null;

  private boolean persist = true;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `exception` is accessed in both synchronized and unsynchronized contexts
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/Procedure.java`
#### Snippet
```java
  private ProcedureState state = ProcedureState.INITIALIZING;
  private int childrenLatch = 0;
  private ProcedureException exception;

  private volatile long timeout = NO_TIMEOUT;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `migrateResult` is accessed in both synchronized and unsynchronized contexts
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/impl/statemachine/RegionMigrateProcedure.java`
#### Snippet
```java
  private boolean migrateSuccess = true;

  private String migrateResult = "";

  public RegionMigrateProcedure() {
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `tsFileWriter` is accessed in both synchronized and unsynchronized contexts
in `spark-tsfile/src/main/java/org/apache/iotdb/spark/tsfile/io/TsFileRecordWriter.java`
#### Snippet
```java
public class TsFileRecordWriter extends RecordWriter<NullWritable, TSRecord> {

  private TsFileWriter tsFileWriter = null;

  public TsFileRecordWriter(TaskAttemptContext job, Path file, Schema schema) throws IOException {
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `pool` is accessed in both synchronized and unsynchronized contexts
in `flink-iotdb-connector/src/main/java/org/apache/iotdb/flink/IoTDBSink.java`
#### Snippet
```java
  private IoTSerializationSchema<IN> serializationSchema;
  private Map<String, IoTDBSinkOptions.TimeseriesOption> timeseriesOptionMap;
  private transient SessionPool pool;
  private transient ScheduledExecutorService scheduledExecutor;

```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `batchSize` is accessed in both synchronized and unsynchronized contexts
in `flink-iotdb-connector/src/main/java/org/apache/iotdb/flink/IoTDBSink.java`
#### Snippet
```java
  private transient ScheduledExecutorService scheduledExecutor;

  private int batchSize = 0;
  private int flushIntervalMs = 3000;
  private List<Event> batchList;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `position` is accessed in both synchronized and unsynchronized contexts
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/writer/LocalTsFileOutput.java`
#### Snippet
```java
  private FileOutputStream outputStream;
  private BufferedOutputStream bufferedStream;
  private long position;

  public LocalTsFileOutput(FileOutputStream outputStream) {
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `bufferedStream` is accessed in both synchronized and unsynchronized contexts
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/writer/LocalTsFileOutput.java`
#### Snippet
```java

  private FileOutputStream outputStream;
  private BufferedOutputStream bufferedStream;
  private long position;

```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `fileMetadataSize` is accessed in both synchronized and unsynchronized contexts
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/TsFileSequenceReader.java`
#### Snippet
```java
  protected TsFileInput tsFileInput;
  protected long fileMetadataPos;
  protected int fileMetadataSize;
  private ByteBuffer markerBuffer = ByteBuffer.allocate(Byte.BYTES);
  protected volatile TsFileMetadata tsFileMetaData;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `fileMetadataPos` is accessed in both synchronized and unsynchronized contexts
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/TsFileSequenceReader.java`
#### Snippet
```java
  protected String file;
  protected TsFileInput tsFileInput;
  protected long fileMetadataPos;
  protected int fileMetadataSize;
  private ByteBuffer markerBuffer = ByteBuffer.allocate(Byte.BYTES);
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `writer` is accessed in both synchronized and unsynchronized contexts
in `hadoop/src/main/java/org/apache/iotdb/hadoop/tsfile/TSFRecordWriter.java`
#### Snippet
```java
  private static final Logger logger = LoggerFactory.getLogger(TSFRecordWriter.class);

  private TsFileWriter writer;

  public TSFRecordWriter(TaskAttemptContext job, Path path, Schema schema) throws IOException {
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `fs` is accessed in both synchronized and unsynchronized contexts
in `hadoop/src/main/java/org/apache/iotdb/hadoop/fileSystem/HDFSInput.java`
#### Snippet
```java

  private FSDataInputStream fsDataInputStream;
  private FileSystem fs;
  private Path path;

```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `fsDataInputStream` is accessed in both synchronized and unsynchronized contexts
in `hadoop/src/main/java/org/apache/iotdb/hadoop/fileSystem/HDFSInput.java`
#### Snippet
```java
public class HDFSInput implements TsFileInput {

  private FSDataInputStream fsDataInputStream;
  private FileSystem fs;
  private Path path;
```

### RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
Field `executorService` is accessed in both synchronized and unsynchronized contexts
in `consensus/src/main/java/org/apache/iotdb/consensus/multileader/logdispatcher/LogDispatcher.java`
#### Snippet
```java
  private final String selfPeerId;
  private final IClientManager<TEndPoint, AsyncMultiLeaderServiceClient> clientManager;
  private ExecutorService executorService;

  private boolean stopped = false;
```

## RuleId[ruleID=EqualsAndHashcode]
### RuleId[ruleID=EqualsAndHashcode]
Class has `equals()` defined but does not define `hashCode()`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/metedata/write/CreateTimeSeriesNode.java`
#### Snippet
```java
import java.util.TreeMap;

public class CreateTimeSeriesNode extends WritePlanNode implements ICreateTimeSeriesPlan {
  private PartialPath path;
  private TSDataType dataType;
```

### RuleId[ruleID=EqualsAndHashcode]
Class has `equals()` defined but does not define `hashCode()`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/fragment/FragmentInstanceFailureInfo.java`
#### Snippet
```java
 * href="https://github.com/trinodb/trino/blob/master/core/trino-main/src/main/java/io/trino/execution/ExecutionFailureInfo.java">...</a>
 */
public class FragmentInstanceFailureInfo implements Serializable {
  private static final Pattern STACK_TRACE_PATTERN =
      Pattern.compile("(.*)\\.(.*)\\(([^:]*)(?::(.*))?\\)");
```

### RuleId[ruleID=EqualsAndHashcode]
Class has `equals()` defined but does not define `hashCode()`
in `server/src/main/java/org/apache/iotdb/db/wal/buffer/WALEntry.java`
#### Snippet
```java
 * value(physical plan or memTable snapshot).
 */
public abstract class WALEntry implements SerializedSize {
  private static final Logger logger = LoggerFactory.getLogger(WALEntry.class);

```

### RuleId[ruleID=EqualsAndHashcode]
Class has `equals()` defined but does not define `hashCode()`
in `server/src/main/java/org/apache/iotdb/db/wal/checkpoint/MemTableInfo.java`
#### Snippet
```java
 * file version id of its first {@link WALEntry}.
 */
public class MemTableInfo implements SerializedSize {
  /** memTable id 8 bytes, first version id 8 bytes */
  private static final int FIXED_SERIALIZED_SIZE = Long.BYTES * 2;
```

### RuleId[ruleID=EqualsAndHashcode]
Class has `equals()` defined but does not define `hashCode()`
in `server/src/main/java/org/apache/iotdb/db/wal/checkpoint/Checkpoint.java`
#### Snippet
```java
 * brief information of each memTable.
 */
public class Checkpoint implements SerializedSize {
  /** checkpoint type 1 byte, checkpoint number 4 bytes */
  private static final int FIXED_SERIALIZED_SIZE = Byte.BYTES + Integer.BYTES;
```

### RuleId[ruleID=EqualsAndHashcode]
Class has `equals()` defined but does not define `hashCode()`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/task/AbstractCompactionTask.java`
#### Snippet
```java
 * finished. The future returns the {@link CompactionTaskSummary} of this task execution.
 */
public abstract class AbstractCompactionTask {
  private static final Logger LOGGER =
      LoggerFactory.getLogger(IoTDBConstant.COMPACTION_LOGGER_NAME);
```

### RuleId[ruleID=EqualsAndHashcode]
Class has `equals()` defined but does not define `hashCode()`
in `server/src/main/java/org/apache/iotdb/db/metadata/mnode/container/MNodeContainers.java`
#### Snippet
```java
  }

  private static class EmptyContainer extends AbstractMap<String, IMNode>
      implements IMNodeContainer {

```

### RuleId[ruleID=EqualsAndHashcode]
Class has `hashCode()` defined but does not define `equals()`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/cache/LRUCacheManager.java`
#### Snippet
```java
  }

  private static class LRUCacheEntry extends CacheEntry {

    // although the node instance may be replaced, the name and full path of the node won't be
```

### RuleId[ruleID=EqualsAndHashcode]
Class has `equals()` defined but does not define `hashCode()`
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/impl/node/RemoveConfigNodeProcedure.java`
#### Snippet
```java

/** remove config node procedure */
public class RemoveConfigNodeProcedure extends AbstractNodeProcedure<RemoveConfigNodeState> {
  private static final Logger LOG = LoggerFactory.getLogger(RemoveConfigNodeProcedure.class);
  private static final int retryThreshold = 5;
```

### RuleId[ruleID=EqualsAndHashcode]
Class has `equals()` defined but does not define `hashCode()`
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/impl/node/RemoveDataNodeProcedure.java`
#### Snippet
```java

/** remove data node procedure */
public class RemoveDataNodeProcedure extends AbstractNodeProcedure<RemoveDataNodeState> {
  private static final Logger LOG = LoggerFactory.getLogger(RemoveDataNodeProcedure.class);
  private static final int RETRY_THRESHOLD = 5;
```

### RuleId[ruleID=EqualsAndHashcode]
Class has `equals()` defined but does not define `hashCode()`
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/impl/node/AddConfigNodeProcedure.java`
#### Snippet
```java

/** add config node procedure */
public class AddConfigNodeProcedure extends AbstractNodeProcedure<AddConfigNodeState> {
  private static final Logger LOG = LoggerFactory.getLogger(AddConfigNodeProcedure.class);
  private static final int RETRY_THRESHOLD = 5;
```

### RuleId[ruleID=EqualsAndHashcode]
Class has `equals()` defined but does not define `hashCode()`
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/impl/schema/DeleteStorageGroupProcedure.java`
#### Snippet
```java
import java.util.List;

public class DeleteStorageGroupProcedure
    extends StateMachineProcedure<ConfigNodeProcedureEnv, DeleteStorageGroupState> {
  private static final Logger LOG = LoggerFactory.getLogger(DeleteStorageGroupProcedure.class);
```

### RuleId[ruleID=EqualsAndHashcode]
Class has `equals()` defined but does not define `hashCode()`
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/impl/trigger/DropTriggerProcedure.java`
#### Snippet
```java

/** drop trigger procedure */
public class DropTriggerProcedure extends AbstractNodeProcedure<DropTriggerState> {
  private static final Logger LOG = LoggerFactory.getLogger(DropTriggerProcedure.class);
  private static final int retryThreshold = 5;
```

### RuleId[ruleID=EqualsAndHashcode]
Class has `equals()` defined but does not define `hashCode()`
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/impl/trigger/CreateTriggerProcedure.java`
#### Snippet
```java

/** create trigger procedure */
public class CreateTriggerProcedure extends AbstractNodeProcedure<CreateTriggerState> {
  private static final Logger LOG = LoggerFactory.getLogger(CreateTriggerProcedure.class);
  private static final int retryThreshold = 5;
```

### RuleId[ruleID=EqualsAndHashcode]
Class has `equals()` defined but does not define `hashCode()`
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/impl/statemachine/RegionMigrateProcedure.java`
#### Snippet
```java

/** region migrate procedure */
public class RegionMigrateProcedure
    extends StateMachineProcedure<ConfigNodeProcedureEnv, RegionTransitionState> {
  private static final Logger LOG = LoggerFactory.getLogger(RegionMigrateProcedure.class);
```

### RuleId[ruleID=EqualsAndHashcode]
Class has `equals()` defined but does not define `hashCode()`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/filter/operator/In.java`
#### Snippet
```java
 * @param <T> comparable data type
 */
public class In<T extends Comparable<T>> implements Filter {

  private static final long serialVersionUID = 8572705136773595399L;
```

### RuleId[ruleID=EqualsAndHashcode]
Class has `equals()` defined but does not define `hashCode()`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/filter/operator/Regexp.java`
#### Snippet
```java
 * @param <T> comparable data type
 */
public class Regexp<T extends Comparable<T>> implements Filter {

  protected String value;
```

### RuleId[ruleID=EqualsAndHashcode]
Class has `equals()` defined but does not define `hashCode()`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/record/Tablet.java`
#### Snippet
```java
 * <p>Notice: The tablet should not have empty cell, please use BitMap to denote null value
 */
public class Tablet {

  private static final int DEFAULT_SIZE = 1024;
```

### RuleId[ruleID=EqualsAndHashcode]
Class has `hashCode()` defined but does not define `equals()`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/TsFileSequenceReader.java`
#### Snippet
```java
import java.util.stream.Collectors;

public class TsFileSequenceReader implements AutoCloseable {

  private static final Logger logger = LoggerFactory.getLogger(TsFileSequenceReader.class);
```

### RuleId[ruleID=EqualsAndHashcode]
Class has `equals()` defined but does not define `hashCode()`
in `metrics/interface/src/main/java/org/apache/iotdb/metrics/config/MetricConfig.java`
#### Snippet
```java
import java.util.Objects;

public class MetricConfig {

  /** Is the statistic of operation performance enabled */
```

## RuleId[ruleID=RedundantExplicitClose]
### RuleId[ruleID=RedundantExplicitClose]
Redundant 'close()'
in `confignode/src/main/java/org/apache/iotdb/confignode/persistence/schema/TemplateTable.java`
#### Snippet
```java
      deserialize(bufferedInputStream);
      bufferedInputStream.close();
      fileInputStream.close();
    } finally {
      templateReadWriteLock.writeLock().unlock();
```

## RuleId[ruleID=CallToStringConcatCanBeReplacedByOperator]
### RuleId[ruleID=CallToStringConcatCanBeReplacedByOperator]
Call to `concat()` can be replaced with '+' expression
in `session/src/main/java/org/apache/iotdb/session/SessionConnection.java`
#### Snippet
```java
      urls.add(url.toString());
    }
    return MSG_RECONNECTION_FAIL.concat(urls.toString());
  }

```

### RuleId[ruleID=CallToStringConcatCanBeReplacedByOperator]
Call to `concat()` can be replaced with '+' expression
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/source/AlignedSeriesScanNode.java`
#### Snippet
```java
    String deviceName = alignedPath.getDevice();
    for (String measurement : alignedPath.getMeasurementList()) {
      outputColumnNames.add(deviceName.concat(TsFileConstant.PATH_SEPARATOR + measurement));
    }
    return outputColumnNames;
```

### RuleId[ruleID=CallToStringConcatCanBeReplacedByOperator]
Call to `concat()` can be replaced with '+' expression
in `server/src/main/java/org/apache/iotdb/db/constant/TestConstant.java`
#### Snippet
```java
            + tsFileVersion
            + "-0-0.tsfile";
    return filePath.concat(fileName);
  }

```

### RuleId[ruleID=CallToStringConcatCanBeReplacedByOperator]
Call to `concat()` can be replaced with '+' expression
in `server/src/main/java/org/apache/iotdb/db/constant/TestConstant.java`
#### Snippet
```java
public class TestConstant {

  public static final String BASE_OUTPUT_PATH = "target".concat(File.separator);
  public static final String OUTPUT_DATA_DIR =
      BASE_OUTPUT_PATH.concat("data").concat(File.separator);
```

### RuleId[ruleID=CallToStringConcatCanBeReplacedByOperator]
Call to `concat()` can be replaced with '+' expression
in `server/src/main/java/org/apache/iotdb/db/constant/TestConstant.java`
#### Snippet
```java
  public static final String BASE_OUTPUT_PATH = "target".concat(File.separator);
  public static final String OUTPUT_DATA_DIR =
      BASE_OUTPUT_PATH.concat("data").concat(File.separator);
  public static final String PARTIAL_PATH_STRING =
      "%s" + File.separator + "%d" + File.separator + "%d" + File.separator;
```

### RuleId[ruleID=CallToStringConcatCanBeReplacedByOperator]
Call to `concat()` can be replaced with '+' expression
in `server/src/main/java/org/apache/iotdb/db/constant/TestConstant.java`
#### Snippet
```java
  public static final String BASE_OUTPUT_PATH = "target".concat(File.separator);
  public static final String OUTPUT_DATA_DIR =
      BASE_OUTPUT_PATH.concat("data").concat(File.separator);
  public static final String PARTIAL_PATH_STRING =
      "%s" + File.separator + "%d" + File.separator + "%d" + File.separator;
```

### RuleId[ruleID=CallToStringConcatCanBeReplacedByOperator]
Call to `concat()` can be replaced with '+' expression
in `server/src/main/java/org/apache/iotdb/db/constant/TestConstant.java`
#### Snippet
```java
      "%s" + File.separator + "%d" + File.separator + "%d" + File.separator;
  public static final String TEST_TSFILE_PATH =
      BASE_OUTPUT_PATH + "testTsFile".concat(File.separator) + PARTIAL_PATH_STRING;

  public static final String d0 = "root.vehicle.d0";
```

### RuleId[ruleID=CallToStringConcatCanBeReplacedByOperator]
Call to `concat()` can be replaced with '+' expression
in `library-udf/src/main/java/org/apache/iotdb/library/string/UDTFRegexReplace.java`
#### Snippet
```java
      result =
          prefix
              .concat(
                  origin
                      .substring(prefix.length(), origin.length() - suffix.length())
```

### RuleId[ruleID=CallToStringConcatCanBeReplacedByOperator]
Call to `concat()` can be replaced with '+' expression
in `library-udf/src/main/java/org/apache/iotdb/library/string/UDTFRegexReplace.java`
#### Snippet
```java
                      .substring(prefix.length(), origin.length() - suffix.length())
                      .replaceAll(regex, replace))
              .concat(suffix);
    } else {
      IntArrayList fromIndexList = new IntArrayList();
```

### RuleId[ruleID=CallToStringConcatCanBeReplacedByOperator]
Call to `concat()` can be replaced with '+' expression
in `library-udf/src/main/java/org/apache/iotdb/library/string/UDTFRegexReplace.java`
#### Snippet
```java
      result =
          prefix
              .concat(
                  origin
                      .substring(prefix.length(), origin.length() - suffix.length())
```

### RuleId[ruleID=CallToStringConcatCanBeReplacedByOperator]
Call to `concat()` can be replaced with '+' expression
in `library-udf/src/main/java/org/apache/iotdb/library/string/UDTFRegexReplace.java`
#### Snippet
```java
                      .substring(prefix.length(), origin.length() - suffix.length())
                      .replaceAll(regex, replace))
              .concat(suffix);
    }
    collector.putString(row.getTime(), result);
```

### RuleId[ruleID=CallToStringConcatCanBeReplacedByOperator]
Call to `concat()` can be replaced with '+' expression
in `library-udf/src/main/java/org/apache/iotdb/library/string/UDTFStrReplace.java`
#### Snippet
```java
      result =
          prefix
              .concat(
                  origin
                      .substring(endIndex, origin.length() - suffix.length())
```

### RuleId[ruleID=CallToStringConcatCanBeReplacedByOperator]
Call to `concat()` can be replaced with '+' expression
in `library-udf/src/main/java/org/apache/iotdb/library/string/UDTFStrReplace.java`
#### Snippet
```java
                      .substring(endIndex, origin.length() - suffix.length())
                      .replace(target, replace))
              .concat(suffix);
    } else {
      int fromIndex = 0;
```

### RuleId[ruleID=CallToStringConcatCanBeReplacedByOperator]
Call to `concat()` can be replaced with '+' expression
in `library-udf/src/main/java/org/apache/iotdb/library/string/UDTFStrReplace.java`
#### Snippet
```java
      result =
          prefix
              .concat(origin.substring(prefix.length(), fromIndex).replace(target, replace))
              .concat(suffix);
    }
```

### RuleId[ruleID=CallToStringConcatCanBeReplacedByOperator]
Call to `concat()` can be replaced with '+' expression
in `library-udf/src/main/java/org/apache/iotdb/library/string/UDTFStrReplace.java`
#### Snippet
```java
          prefix
              .concat(origin.substring(prefix.length(), fromIndex).replace(target, replace))
              .concat(suffix);
    }
    collector.putString(row.getTime(), result);
```

### RuleId[ruleID=CallToStringConcatCanBeReplacedByOperator]
Call to `concat()` can be replaced with '+' expression
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/mnode/RInternalMNode.java`
#### Snippet
```java
  @Override
  public boolean hasChild(String name) {
    String childPathName = fullPath.concat(RSchemaConstants.PATH_SEPARATOR).concat(name);
    IMNode node = getNodeBySpecifiedPath(childPathName);
    return node != null;
```

### RuleId[ruleID=CallToStringConcatCanBeReplacedByOperator]
Call to `concat()` can be replaced with '+' expression
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/mnode/RInternalMNode.java`
#### Snippet
```java
  @Override
  public boolean hasChild(String name) {
    String childPathName = fullPath.concat(RSchemaConstants.PATH_SEPARATOR).concat(name);
    IMNode node = getNodeBySpecifiedPath(childPathName);
    return node != null;
```

### RuleId[ruleID=CallToStringConcatCanBeReplacedByOperator]
Call to `concat()` can be replaced with '+' expression
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/mnode/RInternalMNode.java`
#### Snippet
```java
  @Override
  public IMNode deleteChild(String name) {
    String childPathName = fullPath.concat(RSchemaConstants.PATH_SEPARATOR).concat(name);
    int nodeNameMaxLevel = RSchemaUtils.getLevelByPartialPath(childPathName);
    for (RMNodeType type : RMNodeType.values()) {
```

### RuleId[ruleID=CallToStringConcatCanBeReplacedByOperator]
Call to `concat()` can be replaced with '+' expression
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/mnode/RInternalMNode.java`
#### Snippet
```java
  @Override
  public IMNode deleteChild(String name) {
    String childPathName = fullPath.concat(RSchemaConstants.PATH_SEPARATOR).concat(name);
    int nodeNameMaxLevel = RSchemaUtils.getLevelByPartialPath(childPathName);
    for (RMNodeType type : RMNodeType.values()) {
```

### RuleId[ruleID=CallToStringConcatCanBeReplacedByOperator]
Call to `concat()` can be replaced with '+' expression
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/mnode/RInternalMNode.java`
#### Snippet
```java
  @Override
  public IMNode getChild(String name) {
    String childPathName = fullPath.concat(RSchemaConstants.PATH_SEPARATOR).concat(name);
    return getNodeBySpecifiedPath(childPathName);
  }
```

### RuleId[ruleID=CallToStringConcatCanBeReplacedByOperator]
Call to `concat()` can be replaced with '+' expression
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/mnode/RInternalMNode.java`
#### Snippet
```java
  @Override
  public IMNode getChild(String name) {
    String childPathName = fullPath.concat(RSchemaConstants.PATH_SEPARATOR).concat(name);
    return getNodeBySpecifiedPath(childPathName);
  }
```

### RuleId[ruleID=CallToStringConcatCanBeReplacedByOperator]
Call to `concat()` can be replaced with '+' expression
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/mnode/RInternalMNode.java`
#### Snippet
```java
  public IMNode addChild(String name, IMNode child) {
    child.setParent(this);
    String childName = fullPath.concat(RSchemaConstants.PATH_SEPARATOR).concat(name);
    int childNameMaxLevel = RSchemaUtils.getLevelByPartialPath(childName);
    try {
```

### RuleId[ruleID=CallToStringConcatCanBeReplacedByOperator]
Call to `concat()` can be replaced with '+' expression
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/mnode/RInternalMNode.java`
#### Snippet
```java
  public IMNode addChild(String name, IMNode child) {
    child.setParent(this);
    String childName = fullPath.concat(RSchemaConstants.PATH_SEPARATOR).concat(name);
    int childNameMaxLevel = RSchemaUtils.getLevelByPartialPath(childName);
    try {
```

### RuleId[ruleID=CallToStringConcatCanBeReplacedByOperator]
Call to `concat()` can be replaced with '+' expression
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/TsFileGeneratorUtils.java`
#### Snippet
```java
            + tsFileVersion
            + "-0-0.tsfile";
    return fileParentPath.concat(fileName);
  }

```

### RuleId[ruleID=CallToStringConcatCanBeReplacedByOperator]
Call to `concat()` can be replaced with '+' expression
in `integration-test/src/main/java/org/apache/iotdb/itbase/constant/TestConstant.java`
#### Snippet
```java
            + tsFileVersion
            + "-0-0.tsfile";
    return filePath.concat(fileName);
  }

```

### RuleId[ruleID=CallToStringConcatCanBeReplacedByOperator]
Call to `concat()` can be replaced with '+' expression
in `integration-test/src/main/java/org/apache/iotdb/itbase/constant/TestConstant.java`
#### Snippet
```java
  public static final String BASE_OUTPUT_PATH = "target".concat(File.separator);
  public static final String OUTPUT_DATA_DIR =
      BASE_OUTPUT_PATH.concat("data").concat(File.separator);
  public static final String PARTIAL_PATH_STRING =
      "%s" + File.separator + "%d" + File.separator + "%d" + File.separator;
```

### RuleId[ruleID=CallToStringConcatCanBeReplacedByOperator]
Call to `concat()` can be replaced with '+' expression
in `integration-test/src/main/java/org/apache/iotdb/itbase/constant/TestConstant.java`
#### Snippet
```java
  public static final String BASE_OUTPUT_PATH = "target".concat(File.separator);
  public static final String OUTPUT_DATA_DIR =
      BASE_OUTPUT_PATH.concat("data").concat(File.separator);
  public static final String PARTIAL_PATH_STRING =
      "%s" + File.separator + "%d" + File.separator + "%d" + File.separator;
```

### RuleId[ruleID=CallToStringConcatCanBeReplacedByOperator]
Call to `concat()` can be replaced with '+' expression
in `integration-test/src/main/java/org/apache/iotdb/itbase/constant/TestConstant.java`
#### Snippet
```java
      "%s" + File.separator + "%d" + File.separator + "%d" + File.separator;
  public static final String TEST_TSFILE_PATH =
      BASE_OUTPUT_PATH + "testTsFile".concat(File.separator) + PARTIAL_PATH_STRING;

  public static final String d0 = "root.vehicle.d0";
```

### RuleId[ruleID=CallToStringConcatCanBeReplacedByOperator]
Call to `concat()` can be replaced with '+' expression
in `integration-test/src/main/java/org/apache/iotdb/itbase/constant/TestConstant.java`
#### Snippet
```java
public class TestConstant {

  public static final String BASE_OUTPUT_PATH = "target".concat(File.separator);
  public static final String OUTPUT_DATA_DIR =
      BASE_OUTPUT_PATH.concat("data").concat(File.separator);
```

## RuleId[ruleID=SynchronizeOnThis]
### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `node-commons/src/main/java/org/apache/iotdb/commons/auth/role/BasicRoleManager.java`
#### Snippet
```java
  @Override
  public void replaceAllRoles(Map<String, Role> roles) throws AuthException {
    synchronized (this) {
      reset();
      roleMap = roles;
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `node-commons/src/main/java/org/apache/iotdb/commons/auth/user/BasicUserManager.java`
#### Snippet
```java
  @Override
  public void replaceAllUsers(Map<String, User> users) throws AuthException {
    synchronized (this) {
      reset();
      userMap = users;
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `node-commons/src/main/java/org/apache/iotdb/commons/client/AsyncBaseClientFactory.java`
#### Snippet
```java
      String threadName) {
    super(clientManager, clientFactoryProperty);
    synchronized (this) {
      tManagers = new TAsyncClientManager[clientFactoryProperty.getSelectorNumOfAsyncClientPool()];
      for (int i = 0; i < tManagers.length; i++) {
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `node-commons/src/main/java/org/apache/iotdb/commons/service/metric/MetricService.java`
#### Snippet
```java
  @Override
  public void reloadService(ReloadLevel reloadLevel) {
    synchronized (this) {
      switch (reloadLevel) {
        case RESTART_METRIC:
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on a class may have unforeseen side-effects
in `node-commons/src/main/java/org/apache/iotdb/commons/security/encrypt/AsymmetricEncryptFactory.java`
#### Snippet
```java
      String providerClassName, String providerParameter) {
    if (asymmetricEncrypt == null) {
      synchronized (AsymmetricEncrypt.class) {
        if (asymmetricEncrypt == null) {

```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/LocalExecutionPlanner.java`
#### Snippet
```java
    long estimatedMemorySize = root.calculateMaxPeekMemory();

    synchronized (this) {
      if (estimatedMemorySize > freeMemoryForOperators) {
        throw new MemoryNotEnoughException(
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/LocalExecutionPlanner.java`
#### Snippet
```java
            try (SetThreadName fragmentInstanceName =
                new SetThreadName(stateMachine.getFragmentInstanceId().getFullId())) {
              synchronized (this) {
                this.freeMemoryForOperators += estimatedMemorySize;
                LOGGER.debug(
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/scheduler/FixedRateFragInsStateTracker.java`
#### Snippet
```java
      try (SetThreadName threadName = new SetThreadName(instance.getId().getFullId())) {
        FragmentInstanceInfo instanceInfo = fetchInstanceInfo(instance);
        synchronized (this) {
          InstanceStateMetrics metrics =
              instanceStateMap.computeIfAbsent(
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/SharedTsBlockQueue.java`
#### Snippet
```java
      blockedOnMemory.addListener(
          () -> {
            synchronized (this) {
              queue.add(tsBlock);
              if (!blocked.isDone()) {
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/memory/MemoryPool.java`
#### Snippet
```java

    ListenableFuture<Void> result;
    synchronized (this) {
      if (maxBytes - reservedBytes < bytes
          || maxBytesPerQuery - queryMemoryReservations.getOrDefault(queryId, 0L) < bytes) {
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/memory/MemoryPool.java`
#### Snippet
```java
      return false;
    }
    synchronized (this) {
      if (maxBytes - reservedBytes < bytes
          || maxBytesPerQuery - queryMemoryReservations.getOrDefault(queryId, 0L) < bytes) {
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/memory/MemoryPool.java`
#### Snippet
```java
  public void free(String queryId, long bytes) {
    List<MemoryReservationFuture<Void>> futureList = new ArrayList<>();
    synchronized (this) {
      Validate.notNull(queryId);
      Validate.isTrue(bytes > 0L);
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/LocalSourceHandle.java`
#### Snippet
```java
      logger.debug("[StartAbortLocalSourceHandle]");
      synchronized (queue) {
        synchronized (this) {
          if (aborted || closed) {
            return;
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/LocalSourceHandle.java`
#### Snippet
```java
      logger.debug("[StartCloseLocalSourceHandle]");
      synchronized (queue) {
        synchronized (this) {
          if (aborted || closed) {
            return;
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/LocalSourceHandle.java`
#### Snippet
```java
      logger.debug("[StartAbortLocalSourceHandle]");
      synchronized (queue) {
        synchronized (this) {
          if (aborted || closed) {
            return;
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/LocalSourceHandle.java`
#### Snippet
```java
        // There are two locks need to invoke this method. One is lock of SharedTsBlockQueue,
        // the other is lock of LocalSourceHandle.
        synchronized (this) {
          sourceHandleListener.onFinished(this);
        }
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/LocalSinkHandle.java`
#### Snippet
```java
  public void setNoMoreTsBlocks() {
    synchronized (queue) {
      synchronized (this) {
        logger.debug("[StartSetNoMoreTsBlocksOnLocal]");
        if (aborted || closed) {
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/LocalSinkHandle.java`
#### Snippet
```java
    logger.debug("[StartCloseLocalSinkHandle]");
    synchronized (queue) {
      synchronized (this) {
        if (aborted || closed) {
          return;
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/LocalSinkHandle.java`
#### Snippet
```java
    synchronized (queue) {
      if (isFinished()) {
        synchronized (this) {
          sinkHandleListener.onFinish(this);
        }
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/LocalSinkHandle.java`
#### Snippet
```java
    logger.debug("[StartAbortLocalSinkHandle]");
    synchronized (queue) {
      synchronized (this) {
        if (aborted || closed) {
          return;
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/LocalSinkHandle.java`
#### Snippet
```java
  public void send(TsBlock tsBlock) {
    Validate.notNull(tsBlock, "tsBlocks is null");
    synchronized (this) {
      checkState();
      if (!blocked.isDone()) {
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/LocalSinkHandle.java`
#### Snippet
```java
      }
      logger.debug("[StartSendTsBlockOnLocal]");
      synchronized (this) {
        blocked = queue.add(tsBlock);
      }
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/SinkHandle.java`
#### Snippet
```java
  void acknowledgeTsBlock(int startSequenceId, int endSequenceId) {
    long freedBytes = 0L;
    synchronized (this) {
      if (aborted || closed) {
        return;
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/SourceHandle.java`
#### Snippet
```java
            executorService.submit(
                new SendAcknowledgeDataBlockEventTask(startSequenceId, endSequenceId));
            synchronized (SourceHandle.this) {
              if (aborted || closed) {
                return;
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/SourceHandle.java`
#### Snippet
```java

    private void fail(Throwable t) {
      synchronized (SourceHandle.this) {
        if (aborted || closed) {
          return;
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/SourceHandle.java`
#### Snippet
```java
                e);
            if (attempt == MAX_ATTEMPT_TIMES) {
              synchronized (SourceHandle.this) {
                sourceHandleListener.onFailure(SourceHandle.this, e);
              }
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/SourceHandle.java`
#### Snippet
```java
            } catch (InterruptedException ex) {
              Thread.currentThread().interrupt();
              synchronized (SourceHandle.this) {
                sourceHandleListener.onFailure(SourceHandle.this, e);
              }
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/fragment/FragmentInstanceStateMachine.java`
#### Snippet
```java
  public void sourceTaskFailed(FragmentInstanceId instanceId, Throwable failure) {
    List<FragmentInstanceFailureListener> listeners;
    synchronized (this) {
      sourceInstanceFailures.putIfAbsent(instanceId, failure);
      listeners = ImmutableList.copyOf(sourceInstanceFailureListeners);
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/fragment/FragmentInstanceStateMachine.java`
#### Snippet
```java
  public void addSourceTaskFailureListener(FragmentInstanceFailureListener listener) {
    Map<FragmentInstanceId, Throwable> failures;
    synchronized (this) {
      sourceInstanceFailureListeners.add(listener);
      failures = ImmutableMap.copyOf(sourceInstanceFailures);
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/schedule/queue/IndexedBlockingQueue.java`
#### Snippet
```java
  public synchronized E poll() throws InterruptedException {
    while (isEmpty()) {
      this.wait();
    }
    E output = pollFirst();
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/schedule/queue/IndexedBlockingQueue.java`
#### Snippet
```java
    pushToQueue(element);
    size++;
    this.notifyAll();
  }

```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/wal/utils/listener/AbstractResultListener.java`
#### Snippet
```java
      while (status == Status.RUNNING) {
        try {
          this.wait();
        } catch (InterruptedException e) {
          logger.warn("Interrupted when waiting for result.", e);
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/wal/utils/listener/AbstractResultListener.java`
#### Snippet
```java
    cause = e;
    if (wait) {
      this.notifyAll();
    }
    return this;
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/wal/utils/listener/AbstractResultListener.java`
#### Snippet
```java
    status = Status.SUCCESS;
    if (wait) {
      this.notifyAll();
    }
    return this;
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/query/control/FileReaderManager.java`
#### Snippet
```java
   */
  public void decreaseFileReaderReference(TsFileResource tsFile, boolean isClosed) {
    synchronized (this) {
      if (!isClosed && unclosedReferenceMap.containsKey(tsFile.getTsFilePath())) {
        if (unclosedReferenceMap.get(tsFile.getTsFilePath()).decrementAndGet() == 0) {
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/query/control/FileReaderManager.java`
#### Snippet
```java
        isClosed ? closedFileReaderMap : unclosedFileReaderMap;
    Map<String, AtomicInteger> refMap = isClosed ? closedReferenceMap : unclosedReferenceMap;
    synchronized (this) {
      // check ref num again
      if (refMap.get(tsFilePath).get() != 0) {
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/query/control/FileReaderManager.java`
#### Snippet
```java
  public void increaseFileReaderReference(TsFileResource tsFile, boolean isClosed) {
    tsFile.readLock();
    synchronized (this) {
      if (!isClosed) {
        unclosedReferenceMap
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `session/src/main/java/org/apache/iotdb/session/pool/SessionPool.java`
#### Snippet
```java
      session.open(enableCompression, connectionTimeoutInMs);
      // avoid someone has called close() the session pool
      synchronized (this) {
        if (closed) {
          // have to release the connection...
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `session/src/main/java/org/apache/iotdb/session/pool/SessionPool.java`
#### Snippet
```java
        }
        queue.push(session);
        this.notify();
      }
    } catch (IoTDBConnectionException e) {
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `session/src/main/java/org/apache/iotdb/session/pool/SessionPool.java`
#### Snippet
```java
      }
    } catch (IoTDBConnectionException e) {
      synchronized (this) {
        size--;
        // we do not need to notifyAll as any waited thread can continue to work after waked up.
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `session/src/main/java/org/apache/iotdb/session/pool/SessionPool.java`
#### Snippet
```java
        size--;
        // we do not need to notifyAll as any waited thread can continue to work after waked up.
        this.notify();
        if (logger.isDebugEnabled()) {
          logger.debug("open session failed, reduce the count and notify others...");
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `session/src/main/java/org/apache/iotdb/session/pool/SessionPool.java`
#### Snippet
```java
    long start = System.currentTimeMillis();
    while (session == null) {
      synchronized (this) {
        if (size < maxSize) {
          // we can create more session
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `session/src/main/java/org/apache/iotdb/session/pool/SessionPool.java`
#### Snippet
```java
            logger.debug("no more sessions can be created, wait... queue.size={}", queue.size());
          }
          this.wait(1000);
          long timeOut = Math.min(waitToGetSessionTimeoutInMs, 60_000);
          if (System.currentTimeMillis() - start > timeOut) {
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `session/src/main/java/org/apache/iotdb/session/pool/SessionPool.java`
#### Snippet
```java
        session.open(enableCompression, connectionTimeoutInMs);
        // avoid someone has called close() the session pool
        synchronized (this) {
          if (closed) {
            // have to release the connection...
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `session/src/main/java/org/apache/iotdb/session/pool/SessionPool.java`
#### Snippet
```java
        // if exception, we will throw the exception.
        // Meanwhile, we have to set size--
        synchronized (this) {
          size--;
          // we do not need to notifyAll as any waited thread can continue to work after waked up.
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `session/src/main/java/org/apache/iotdb/session/pool/SessionPool.java`
#### Snippet
```java
          size--;
          // we do not need to notifyAll as any waited thread can continue to work after waked up.
          this.notify();
          if (logger.isDebugEnabled()) {
            logger.debug("open session failed, reduce the count and notify others...");
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `session/src/main/java/org/apache/iotdb/session/pool/SessionPool.java`
#### Snippet
```java
  private void putBack(Session session) {
    queue.push(session);
    synchronized (this) {
      // we do not need to notifyAll as any waited thread can continue to work after waked up.
      this.notify();
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `session/src/main/java/org/apache/iotdb/session/pool/SessionPool.java`
#### Snippet
```java
    synchronized (this) {
      // we do not need to notifyAll as any waited thread can continue to work after waked up.
      this.notify();
      // comment the following codes as putBack is too frequently called.
      //      if (logger.isTraceEnabled()) {
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on a class may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/engine/trigger/utils/HTTPConnectionPool.java`
#### Snippet
```java
  public static PoolingHttpClientConnectionManager getInstance() {
    if (clientConnectionManager == null) {
      synchronized (HTTPConnectionPool.class) {
        if (clientConnectionManager == null) {
          clientConnectionManager = new PoolingHttpClientConnectionManager();
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/engine/modification/ModificationFile.java`
#### Snippet
```java

  private void init() {
    synchronized (this) {
      modifications = (List<Modification>) reader.read();
    }
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/engine/modification/ModificationFile.java`
#### Snippet
```java
   */
  public void write(Modification mod) throws IOException {
    synchronized (this) {
      writer.write(mod);
      if (modifications != null) {
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/engine/modification/ModificationFile.java`
#### Snippet
```java
  @Override
  public void close() throws IOException {
    synchronized (this) {
      writer.close();
      modifications = null;
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/engine/modification/ModificationFile.java`
#### Snippet
```java

  public void abort() throws IOException {
    synchronized (this) {
      writer.abort();
      if (modifications != null && !modifications.isEmpty()) {
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/engine/modification/ModificationFile.java`
#### Snippet
```java
   */
  public Collection<Modification> getModifications() {
    synchronized (this) {
      checkInit();
      return new ArrayList<>(modifications);
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileLock.java`
#### Snippet
```java

  public boolean tryReadLock() {
    synchronized (this) {
      if (writeCnt > 0) {
        return false;
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileLock.java`
#### Snippet
```java

  public boolean tryWriteLock() {
    synchronized (this) {
      if (writeCnt > 0 || readCnt > 0) {
        return false;
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileLock.java`
#### Snippet
```java

  public void writeUnlock() {
    synchronized (this) {
      if (writeCnt > 0) {
        writeCnt--;
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileLock.java`
#### Snippet
```java
      if (writeCnt > 0) {
        writeCnt--;
        this.notifyAll();
      }
    }
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileLock.java`
#### Snippet
```java

  public void readUnlock() {
    synchronized (this) {
      if (readCnt > 0) {
        readCnt--;
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileLock.java`
#### Snippet
```java
      if (readCnt > 0) {
        readCnt--;
        this.notifyAll();
      }
    }
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileLock.java`
#### Snippet
```java

  public void readLock() {
    synchronized (this) {
      while (writeCnt > 0) {
        try {
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileLock.java`
#### Snippet
```java
      while (writeCnt > 0) {
        try {
          this.wait(1);
        } catch (InterruptedException e) {
          Thread.currentThread().interrupt();
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileLock.java`
#### Snippet
```java

  public void writeLock() {
    synchronized (this) {
      try {
        while (writeCnt > 0 || readCnt > 0) {
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileLock.java`
#### Snippet
```java
      try {
        while (writeCnt > 0 || readCnt > 0) {
          this.wait(1);
        }
      } catch (InterruptedException e) {
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/rescon/MemTableManager.java`
#### Snippet
```java
  public synchronized void decreaseMemtableNumber() {
    currentMemtableNumber--;
    notifyAll();
  }

```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/rescon/MemTableManager.java`
#### Snippet
```java
      }
      try {
        wait(WAIT_TIME);
      } catch (InterruptedException e) {
        logger.error("{} fails to wait for memtables {}, continue to wait", storageGroup, e);
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/rescon/MemTableManager.java`
#### Snippet
```java
        MEMTABLE_NUM_FOR_EACH_PARTITION * CONFIG.getConcurrentWritingTimePartition() * diff;
    CONFIG.setMaxMemtableNumber(maxMemTableNum);
    notifyAll();
  }

```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileResource.java`
#### Snippet
```java

  public void setModFile(ModificationFile modFile) {
    synchronized (this) {
      this.modFile = modFile;
    }
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileResource.java`
#### Snippet
```java
    if (isClosed()) {
      if (tsFileSize == -1) {
        synchronized (this) {
          if (tsFileSize == -1) {
            tsFileSize = file.length();
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileResource.java`
#### Snippet
```java
  public ModificationFile getModFile() {
    if (modFile == null) {
      synchronized (this) {
        if (modFile == null) {
          modFile = ModificationFile.getNormalMods(this);
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileResource.java`
#### Snippet
```java
  public ModificationFile getCompactionModFile() {
    if (compactionModFile == null) {
      synchronized (this) {
        if (compactionModFile == null) {
          compactionModFile = ModificationFile.getCompactionMods(this);
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileResource.java`
#### Snippet
```java
  public void resetModFile() {
    if (modFile != null) {
      synchronized (this) {
        modFile = null;
      }
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/metadata/cache/SchemaCacheEntry.java`
#### Snippet
```java
  public ILastCacheContainer getLastCacheContainer() {
    if (lastCacheContainer == null) {
      synchronized (this) {
        if (lastCacheContainer == null) {
          lastCacheContainer = new LastCacheContainer();
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/metadata/mnode/InternalMNode.java`
#### Snippet
```java
    if (children == null) {
      // double check, children is volatile
      synchronized (this) {
        if (children == null) {
          children = MNodeContainers.getNewMNodeContainer();
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/metadata/mnode/InternalMNode.java`
#### Snippet
```java
    if (children == null) {
      // double check, children is volatile
      synchronized (this) {
        if (children == null) {
          children = MNodeContainers.getNewMNodeContainer();
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/metadata/mnode/EntityMNode.java`
#### Snippet
```java
    if (aliasChildren == null) {
      // double check, alias children volatile
      synchronized (this) {
        if (aliasChildren == null) {
          aliasChildren = new ConcurrentHashMap<>();
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/ConfigMTree.java`
#### Snippet
```java
    // synchronize check and add, we need addChild operation be atomic.
    // only write operations on mtree will be synchronized
    synchronized (this) {
      if (cur.hasChild(nodeNames[i])) {
        // node b has child sg
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/MTreeBelowSGMemoryImpl.java`
#### Snippet
```java
    IEntityMNode entityMNode;

    synchronized (this) {
      for (String measurement : template.getSchemaMap().keySet()) {
        if (cur.hasChild(measurement)) {
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/MTreeBelowSGMemoryImpl.java`
#### Snippet
```java
    // synchronize check and replace, we need replaceChild become atomic operation
    // only write on mtree will be synchronized
    synchronized (this) {
      IEntityMNode entityMNode = store.setToEntity(node);
      if (entityMNode.isStorageGroup()) {
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/MTreeBelowSGMemoryImpl.java`
#### Snippet
```java
    // synchronize check and add, we need addChild operation be atomic.
    // only write operations on mtree will be synchronized
    synchronized (this) {
      IMNode device = checkAndAutoCreateDeviceNode(devicePath.getTailNode(), deviceParent);

```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/MTreeBelowSGMemoryImpl.java`
#### Snippet
```java

      if (!hasMeasurement) {
        synchronized (this) {
          curNode = store.setToInternal(entityMNode);
          if (curNode.isStorageGroup()) {
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/MTreeBelowSGMemoryImpl.java`
#### Snippet
```java
    // synchronize check and add, we need addChild and add Alias become atomic operation
    // only write on mtree will be synchronized
    synchronized (this) {
      IMNode device = checkAndAutoCreateDeviceNode(devicePath.getTailNode(), deviceParent);

```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/metadata/template/Template.java`
#### Snippet
```java
    }

    synchronized (this) {
      if (prefix.equals("")) {
        isDirectAligned = true;
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/metadata/template/Template.java`
#### Snippet
```java
    IMNode cur = constructEntityPath(path);

    synchronized (this) {
      IMeasurementMNode leafNode =
          MeasurementMNode.getMeasurementMNode(
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/MTreeBelowSGCachedImpl.java`
#### Snippet
```java
        pinnedNodes.add(cur);
      }
      synchronized (this) {
        for (String measurement : template.getSchemaMap().keySet()) {
          if (store.hasChild(cur, measurement)) {
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/MTreeBelowSGCachedImpl.java`
#### Snippet
```java
      // synchronize check and add, we need addChild and add Alias become atomic operation
      // only write on mtree will be synchronized
      synchronized (this) {
        IMNode device = checkAndAutoCreateDeviceNode(devicePath.getTailNode(), deviceParent);

```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/MTreeBelowSGCachedImpl.java`
#### Snippet
```java

      if (!hasMeasurement) {
        synchronized (this) {
          curNode = store.setToInternal(entityMNode);
          if (curNode.isStorageGroup()) {
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/MTreeBelowSGCachedImpl.java`
#### Snippet
```java
      // synchronize check and add, we need addChild operation be atomic.
      // only write operations on mtree will be synchronized
      synchronized (this) {
        IMNode device = checkAndAutoCreateDeviceNode(devicePath.getTailNode(), deviceParent);

```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/MTreeBelowSGCachedImpl.java`
#### Snippet
```java
    // synchronize check and replace, we need replaceChild become atomic operation
    // only write on mtree will be synchronized
    synchronized (this) {
      IEntityMNode entityMNode = store.setToEntity(node);
      if (entityMNode.isStorageGroup()) {
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `confignode/src/main/java/org/apache/iotdb/confignode/manager/ProcedureManager.java`
#### Snippet
```java
      String queryId, Map<PartialPath, List<Template>> templateSetInfo) {
    long procedureId = -1;
    synchronized (this) {
      boolean hasOverlappedTask = false;
      ProcedureType type;
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `confignode/src/main/java/org/apache/iotdb/confignode/manager/ProcedureManager.java`
#### Snippet
```java
  public TSStatus unsetSchemaTemplate(String queryId, Template template, PartialPath path) {
    long procedureId = -1;
    synchronized (this) {
      boolean hasOverlappedTask = false;
      ProcedureType type;
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `confignode/src/main/java/org/apache/iotdb/confignode/manager/ProcedureManager.java`
#### Snippet
```java
        PathPatternTree.deserialize(ByteBuffer.wrap(req.getPathPatternTree()));
    long procedureId = -1;
    synchronized (this) {
      boolean hasOverlappedTask = false;
      ProcedureType type;
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `confignode/src/main/java/org/apache/iotdb/confignode/manager/partition/PartitionManager.java`
#### Snippet
```java
    // by the number of SeriesPartitionSlots,
    // the number of serialized CreateDataPartitionReqs is acceptable.
    synchronized (this) {
      // Filter unassigned DataPartitionSlots
      Map<String, Map<TSeriesPartitionSlot, List<TTimePartitionSlot>>>
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `confignode/src/main/java/org/apache/iotdb/confignode/manager/partition/PartitionManager.java`
#### Snippet
```java
    // by the number of SeriesPartitionSlots,
    // the number of serialized CreateSchemaPartitionReqs is acceptable.
    synchronized (this) {
      // Filter unassigned SchemaPartitionSlots
      Map<String, List<TSeriesPartitionSlot>> unassignedSchemaPartitionSlotsMap =
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/TsFileSequenceReader.java`
#### Snippet
```java
    try {
      if (tsFileMetaData == null) {
        synchronized (this) {
          if (tsFileMetaData == null) {
            tsFileMetaData =
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `integration-test/src/main/java/org/apache/iotdb/it/framework/IoTDBTestListener.java`
#### Snippet
```java
  public synchronized void testRunFinished(Result result) throws IOException {
    StringBuilder sb = new StringBuilder();
    synchronized (this) {
      for (IoTDBTestStat stat : stats) {
        sb.append(stat).append("\n");
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `example/trigger/src/main/java/org/apache/iotdb/trigger/old/TriggerExample.java`
#### Snippet
```java
  private void tryOpenSinksFirstOnFire() throws Exception {
    if (!isSinksOpenedAfterCreation) {
      synchronized (this) {
        if (!isSinksOpenedAfterCreation) {
          openSinkHandlers();
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `consensus/src/main/java/org/apache/iotdb/consensus/ratis/RequestMessage.java`
#### Snippet
```java
  public ByteString getContent() {
    if (serializedContent == null) {
      synchronized (this) {
        if (serializedContent == null) {
          serializedContent =
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `consensus/src/main/java/org/apache/iotdb/consensus/ratis/ResponseMessage.java`
#### Snippet
```java
  public ByteString getContent() {
    if (serializedData == null) {
      synchronized (this) {
        if (serializedData == null) {
          assert contentHolder instanceof TSStatus;
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `consensus/src/main/java/org/apache/iotdb/consensus/multileader/logdispatcher/SyncStatus.java`
#### Snippet
```java
  /** we may block here if the synchronization pipeline is full */
  public void addNextBatch(PendingBatch batch) throws InterruptedException {
    synchronized (this) {
      while (pendingBatches.size() >= config.getReplication().getMaxPendingBatch()
          || !multiLeaderMemoryManager.reserve(batch.getSerializedSize(), false)) {
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `consensus/src/main/java/org/apache/iotdb/consensus/multileader/logdispatcher/SyncStatus.java`
#### Snippet
```java
      while (pendingBatches.size() >= config.getReplication().getMaxPendingBatch()
          || !multiLeaderMemoryManager.reserve(batch.getSerializedSize(), false)) {
        wait();
      }
      pendingBatches.add(batch);
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `consensus/src/main/java/org/apache/iotdb/consensus/multileader/logdispatcher/SyncStatus.java`
#### Snippet
```java
   */
  public void removeBatch(PendingBatch batch) {
    synchronized (this) {
      batch.setSynced(true);
      if (pendingBatches.size() > 0 && pendingBatches.get(0).equals(batch)) {
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `consensus/src/main/java/org/apache/iotdb/consensus/multileader/logdispatcher/SyncStatus.java`
#### Snippet
```java
        }
        // wake up logDispatcherThread that might be blocked
        notifyAll();
      }
    }
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `consensus/src/main/java/org/apache/iotdb/consensus/multileader/logdispatcher/SyncStatus.java`
#### Snippet
```java
  public long getNextSendingIndex() {
    // we do not use ReentrantReadWriteLock because there will be only one thread reading this field
    synchronized (this) {
      return 1
          + (pendingBatches.isEmpty()
```

### RuleId[ruleID=SynchronizeOnThis]
Lock operations on 'this' may have unforeseen side-effects
in `consensus/src/main/java/org/apache/iotdb/consensus/multileader/logdispatcher/LogDispatcher.java`
#### Snippet
```java

  public void offer(IndexedConsensusRequest request) {
    synchronized (this) {
      threads.forEach(
          thread -> {
```

## RuleId[ruleID=DoubleBraceInitialization]
### RuleId[ruleID=DoubleBraceInitialization]
Double brace initialization
in `node-commons/src/main/java/org/apache/iotdb/commons/conf/IoTDBConstant.java`
#### Snippet
```java
  public static final int MIN_SUPPORTED_JDK_VERSION = 8;
  public static final Set<String> reservedWords =
      new HashSet<String>() {
        {
          add("TIME");
```

### RuleId[ruleID=DoubleBraceInitialization]
Double brace initialization
in `metrics/interface/src/main/java/org/apache/iotdb/metrics/metricsets/jvm/JvmGcMetrics.java`
#### Snippet
```java

    private static Map<String, GcGenerationAge> knownCollectors =
        new HashMap<String, GcGenerationAge>() {
          {
            put("ConcurrentMarkSweep", OLD);
```

## RuleId[ruleID=UnstableTypeUsedInSignature]
### RuleId[ruleID=UnstableTypeUsedInSignature]
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'io.airlift.stats.CounterStat'
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/fragment/FragmentInstanceExecution.java`
#### Snippet
```java
  private long lastHeartbeat;

  public static FragmentInstanceExecution createFragmentInstanceExecution(
      IDriverScheduler scheduler,
      FragmentInstanceId instanceId,
```

### RuleId[ruleID=UnstableTypeUsedInSignature]
Field must be marked with '@com.google.common.annotations.Beta' annotation because its type references unstable type 'io.airlift.stats.CounterStat'
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/fragment/FragmentInstanceManager.java`
#### Snippet
```java

  // record failed instances count
  private final CounterStat failedInstances = new CounterStat();

  private static final long QUERY_TIMEOUT_MS =
```

### RuleId[ruleID=UnstableTypeUsedInSignature]
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'io.airlift.stats.CounterStat'
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/fragment/FragmentInstanceManager.java`
#### Snippet
```java
  }

  public CounterStat getFailedInstances() {
    return failedInstances;
  }
```

### RuleId[ruleID=UnstableTypeUsedInSignature]
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'com.google.common.util.concurrent.RateLimiter'
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/CompactionTaskManager.java`
#### Snippet
```java
  }

  public RateLimiter getMergeWriteRateLimiter() {
    setWriteMergeRate(
        IoTDBDescriptor.getInstance().getConfig().getCompactionWriteThroughputMbPerSec());
```

### RuleId[ruleID=UnstableTypeUsedInSignature]
Method must be marked with '@com.google.common.annotations.Beta' annotation because its signature references unstable type 'com.google.common.util.concurrent.RateLimiter'
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/CompactionTaskManager.java`
#### Snippet
```java
  }
  /** wait by throughoutMbPerSec limit to avoid continuous Write Or Read */
  public static void mergeRateLimiterAcquire(RateLimiter limiter, long bytesLength) {
    while (bytesLength >= Integer.MAX_VALUE) {
      limiter.acquire(Integer.MAX_VALUE);
```

### RuleId[ruleID=UnstableTypeUsedInSignature]
Field must be marked with '@com.google.common.annotations.Beta' annotation because its type references unstable type 'com.google.common.util.concurrent.RateLimiter'
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/CompactionTaskManager.java`
#### Snippet
```java
  private final AtomicInteger finishedTaskNum = new AtomicInteger(0);

  private final RateLimiter mergeWriteRateLimiter = RateLimiter.create(Double.MAX_VALUE);

  private final IoTDBConfig config = IoTDBDescriptor.getInstance().getConfig();
```

## RuleId[ruleID=UnusedAssignment]
### RuleId[ruleID=UnusedAssignment]
Variable `result` initializer `true` is redundant
in `node-commons/src/main/java/org/apache/iotdb/commons/auth/role/LocalFileRoleAccessor.java`
#### Snippet
```java
        systemFileFactory.getFile(roleSnapshotDir.getAbsolutePath() + "-" + UUID.randomUUID());

    boolean result = true;
    try {
      result = FileUtils.copyDir(roleFolder, roleTmpSnapshotDir);
```

### RuleId[ruleID=UnusedAssignment]
Variable `result` initializer `true` is redundant
in `node-commons/src/main/java/org/apache/iotdb/commons/auth/user/LocalFileUserAccessor.java`
#### Snippet
```java
        systemFileFactory.getFile(userSnapshotDir.getAbsolutePath() + "-" + UUID.randomUUID());

    boolean result = true;
    try {
      result = FileUtils.copyDir(userFolder, userTmpSnapshotDir);
```

### RuleId[ruleID=UnusedAssignment]
Variable `size` initializer `0` is redundant
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/FileUtils.java`
#### Snippet
```java
            BufferedOutputStream out = new BufferedOutputStream(new FileOutputStream(targetFile))) {
          byte[] bytes = new byte[bufferSize];
          int size = 0;
          while ((size = in.read(bytes)) > 0) {
            out.write(bytes, 0, size);
```

### RuleId[ruleID=UnusedAssignment]
Variable `queryTimeout` initializer `-1` is redundant
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBStatement.java`
#### Snippet
```java
   * configuration of server. And value 0 will disable the function of query timeout.
   */
  private int queryTimeout = -1;

  protected IClientRPCService.Iface client;
```

### RuleId[ruleID=UnusedAssignment]
Variable `nonAlign` initializer `false` is redundant
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBResultMetadata.java`
#### Snippet
```java
  private List<String> sgColumns;
  private String operationType = "";
  private boolean nonAlign = false;

  /** Constructor of IoTDBResultMetadata. */
```

### RuleId[ruleID=UnusedAssignment]
Variable `operationType` initializer `""` is redundant
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBResultMetadata.java`
#### Snippet
```java
  private boolean ignoreTimestamp;
  private List<String> sgColumns;
  private String operationType = "";
  private boolean nonAlign = false;

```

### RuleId[ruleID=UnusedAssignment]
Variable `markRate` initializer `2` is redundant
in `jdbc/src/main/java/org/apache/iotdb/jdbc/GroupedLSBWatermarkEncoder.java`
#### Snippet
```java
  private String secretKey;
  private String bitString;
  private int markRate = 2;
  private int groupNumber;
  private int maxBitPosition = 5;
```

### RuleId[ruleID=UnusedAssignment]
Variable `maxBitPosition` initializer `5` is redundant
in `jdbc/src/main/java/org/apache/iotdb/jdbc/GroupedLSBWatermarkEncoder.java`
#### Snippet
```java
  private int markRate = 2;
  private int groupNumber;
  private int maxBitPosition = 5;
  private int minBitPosition = 0;

```

### RuleId[ruleID=UnusedAssignment]
The value `new ArrayList<>()` assigned to `ioTDBRpcDataSet.columnTypeDeduplicatedList` is never used
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBNonAlignJDBCResultSet.java`
#### Snippet
```java
    ioTDBRpcDataSet.columnOrdinalMap = new HashMap<>();
    ioTDBRpcDataSet.columnOrdinalMap.put(TIMESTAMP_STR, 1);
    ioTDBRpcDataSet.columnTypeDeduplicatedList = new ArrayList<>();
    ioTDBRpcDataSet.columnTypeDeduplicatedList = new ArrayList<>(columnNameIndex.size());
    for (int i = 0; i < columnNameIndex.size(); i++) {
```

### RuleId[ruleID=UnusedAssignment]
Variable `name` initializer `""` is redundant
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBNonAlignJDBCResultSet.java`
#### Snippet
```java
    List<String> newSgColumns = new ArrayList<>();
    for (int i = 0; i < columnNameList.size(); i++) {
      String name = "";
      if (sgColumns != null && sgColumns.size() > 0) {
        name = sgColumns.get(i) + "." + columnNameList.get(i);
```

### RuleId[ruleID=UnusedAssignment]
Variable `sgColumns` initializer `null` is redundant
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBNonAlignJDBCResultSet.java`
#### Snippet
```java
  private TSQueryNonAlignDataSet tsQueryNonAlignDataSet;
  private byte[][] times; // used for disable align
  private List<String> sgColumns = null;
  // for disable align clause
  IoTDBNonAlignJDBCResultSet(
```

### RuleId[ruleID=UnusedAssignment]
Variable `operationType` initializer `""` is redundant
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBNonAlignJDBCResultSet.java`
#### Snippet
```java
  private static final int TIMESTAMP_STR_LENGTH = 4;
  private static final String EMPTY_STR = "";
  private String operationType = "";
  private TSQueryNonAlignDataSet tsQueryNonAlignDataSet;
  private byte[][] times; // used for disable align
```

### RuleId[ruleID=UnusedAssignment]
Variable `openResp` initializer `null` is redundant
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBConnection.java`
#### Snippet
```java
    openReq.putToConfiguration("version", params.getVersion().toString());

    TSOpenSessionResp openResp = null;
    try {
      openResp = client.openSession(openReq);
```

### RuleId[ruleID=UnusedAssignment]
Variable `zonedDateTime` initializer `null` is redundant
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
  public void setTime(int parameterIndex, Time x, Calendar cal) throws SQLException {
    try {
      ZonedDateTime zonedDateTime = null;
      long time = x.getTime();
      String timeprecision = client.getProperties().getTimestampPrecision();
```

### RuleId[ruleID=UnusedAssignment]
Variable `scaledBigDecimal` initializer `null` is redundant
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
      case Types.NUMERIC:
        if (parameterAsNum instanceof java.math.BigDecimal) {
          BigDecimal scaledBigDecimal = null;

          try {
```

### RuleId[ruleID=UnusedAssignment]
Variable `zonedDateTime` initializer `null` is redundant
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
  @Override
  public void setTimestamp(int parameterIndex, Timestamp x, Calendar cal) throws SQLException {
    ZonedDateTime zonedDateTime = null;
    if (cal != null) {
      zonedDateTime =
```

### RuleId[ruleID=UnusedAssignment]
Variable `pageIndex` initializer `0` is redundant
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileSequenceRead.java`
#### Snippet
```java
      System.out.println("position: " + reader.position());
      List<long[]> timeBatch = new ArrayList<>();
      int pageIndex = 0;
      byte marker;
      while ((marker = reader.readMarker()) != MetaMarker.SEPARATOR) {
```

### RuleId[ruleID=UnusedAssignment]
The value `null` assigned to `rs` is never used
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      }
    } catch (Exception ex) {
      rs = null;
    }
    try {
```

### RuleId[ruleID=UnusedAssignment]
The value `null` assigned to `stmt` is never used
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      }
    } catch (Exception ex) {
      stmt = null;
    }
  }
```

### RuleId[ruleID=UnusedAssignment]
Variable `maxcount` initializer `0` is redundant
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
  @Override
  public int getMaxConnections() {
    int maxcount = 0;
    try {
      maxcount = client.getProperties().getMaxConcurrentClientNum();
```

### RuleId[ruleID=UnusedAssignment]
Variable `name` initializer `""` is redundant
in `service-rpc/src/main/java/org/apache/iotdb/rpc/IoTDBJDBCDataSet.java`
#### Snippet
```java
      }
      for (int i = 0; i < columnNameList.size(); i++) {
        String name = "";
        if (sgList != null
            && sgList.size() > 0
```

### RuleId[ruleID=UnusedAssignment]
Variable `emptyResultSet` initializer `false` is redundant
in `service-rpc/src/main/java/org/apache/iotdb/rpc/IoTDBJDBCDataSet.java`
#### Snippet
```java
  public int fetchSize;
  public final long timeout;
  public boolean emptyResultSet = false;
  public boolean hasCachedRecord = false;
  public boolean lastReadWasNull;
```

### RuleId[ruleID=UnusedAssignment]
Variable `tsQueryDataSet` initializer `null` is redundant
in `service-rpc/src/main/java/org/apache/iotdb/rpc/IoTDBJDBCDataSet.java`
#### Snippet
```java
  public int rowsIndex = 0; // used to record the row index in current TSQueryDataSet

  public TSQueryDataSet tsQueryDataSet = null;
  public byte[] time; // used to cache the current time value
  public byte[] currentBitmap; // used to cache the current bitmap for every column
```

### RuleId[ruleID=UnusedAssignment]
Variable `sessionDataSet` initializer `null` is redundant
in `cli/src/main/java/org/apache/iotdb/tool/ImportCsv.java`
#### Snippet
```java
      throws IoTDBConnectionException {
    String sql = "select * from " + deviceNames + " limit 1";
    SessionDataSet sessionDataSet = null;
    try {
      sessionDataSet = session.executeQueryStatement(sql);
```

### RuleId[ruleID=UnusedAssignment]
Variable `failedFilePath` initializer `null` is redundant
in `cli/src/main/java/org/apache/iotdb/tool/ImportCsv.java`
#### Snippet
```java
          return;
        }
        String failedFilePath = null;
        if (failedFileDirectory == null) {
          failedFilePath = file.getAbsolutePath() + ".failed";
```

### RuleId[ruleID=UnusedAssignment]
Variable `queryId` initializer `-1` is redundant
in `server/src/main/java/org/apache/iotdb/db/qp/physical/sys/KillQueryPlan.java`
#### Snippet
```java
public class KillQueryPlan extends PhysicalPlan {

  private long queryId = -1;

  public KillQueryPlan(long queryId) {
```

### RuleId[ruleID=UnusedAssignment]
The value `endTime` assigned to `startTime` is never used
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/write/InsertTabletNode.java`
#### Snippet
```java
        // next init
        startLoc = i;
        startTime = endTime;
        endTime =
            (times[i] / TimePartitionUtils.timePartitionInterval + 1)
```

### RuleId[ruleID=UnusedAssignment]
Variable `level` initializer `-1` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/metedata/read/NodePathsSchemaScanNode.java`
#### Snippet
```java
  // the path could be a prefix path with wildcard
  private PartialPath prefixPath;
  private int level = -1;

  public NodePathsSchemaScanNode(PlanNodeId id, PartialPath prefixPath, int level) {
```

### RuleId[ruleID=UnusedAssignment]
Variable `devicePath` initializer `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/metedata/write/CreateAlignedTimeSeriesNode.java`
#### Snippet
```java
  public static CreateAlignedTimeSeriesNode deserialize(ByteBuffer byteBuffer) {
    String id;
    PartialPath devicePath = null;
    List<String> measurements;
    List<TSDataType> dataTypes;
```

### RuleId[ruleID=UnusedAssignment]
Variable `compressors` initializer `new ArrayList<>()` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/statement/internal/InternalCreateTimeSeriesStatement.java`
#### Snippet
```java
  private List<TSDataType> tsDataTypes;
  private List<TSEncoding> encodings = new ArrayList<>();
  private List<CompressionType> compressors = new ArrayList<>();

  private boolean isAligned;
```

### RuleId[ruleID=UnusedAssignment]
Variable `encodings` initializer `new ArrayList<>()` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/statement/internal/InternalCreateTimeSeriesStatement.java`
#### Snippet
```java

  private List<TSDataType> tsDataTypes;
  private List<TSEncoding> encodings = new ArrayList<>();
  private List<CompressionType> compressors = new ArrayList<>();

```

### RuleId[ruleID=UnusedAssignment]
Variable `tags` initializer `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/metedata/write/CreateTimeSeriesNode.java`
#### Snippet
```java
  private String alias;
  private Map<String, String> props = null;
  private Map<String, String> tags = null;
  private Map<String, String> attributes = null;

```

### RuleId[ruleID=UnusedAssignment]
Variable `attributes` initializer `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/metedata/write/CreateTimeSeriesNode.java`
#### Snippet
```java
  private Map<String, String> props = null;
  private Map<String, String> tags = null;
  private Map<String, String> attributes = null;

  // only used inside schemaRegion to be serialized to mlog, no need to be serialized for mpp
```

### RuleId[ruleID=UnusedAssignment]
Variable `path` initializer `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/metedata/write/CreateTimeSeriesNode.java`
#### Snippet
```java
  public static CreateTimeSeriesNode deserialize(ByteBuffer byteBuffer) {
    String id;
    PartialPath path = null;
    TSDataType dataType;
    TSEncoding encoding;
```

### RuleId[ruleID=UnusedAssignment]
Variable `bufferRetainedSizeInBytes` initializer `0L` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/SourceHandle.java`
#### Snippet
```java

  /** The actual buffered memory in bytes, including the amount of memory being reserved. */
  private long bufferRetainedSizeInBytes = 0L;

  private int currSequenceId = 0;
```

### RuleId[ruleID=UnusedAssignment]
Variable `count` initializer `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/schema/DevicesCountOperator.java`
#### Snippet
```java
    isFinished = true;
    TsBlockBuilder tsBlockBuilder = new TsBlockBuilder(outputDataTypes);
    long count = 0;
    try {
      count =
```

### RuleId[ruleID=UnusedAssignment]
Variable `count` initializer `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/schema/TimeSeriesCountOperator.java`
#### Snippet
```java
    isFinished = true;
    TsBlockBuilder tsBlockBuilder = new TsBlockBuilder(outputDataTypes);
    long count = 0;
    try {
      if (key != null && value != null) {
```

### RuleId[ruleID=UnusedAssignment]
Variable `path` initializer `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/metedata/write/AlterTimeSeriesNode.java`
#### Snippet
```java
  public static AlterTimeSeriesNode deserialize(ByteBuffer byteBuffer) {
    String id;
    PartialPath path = null;
    AlterType alterType = null;
    String alias = null;
```

### RuleId[ruleID=UnusedAssignment]
Variable `alterType` initializer `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/metedata/write/AlterTimeSeriesNode.java`
#### Snippet
```java
    String id;
    PartialPath path = null;
    AlterType alterType = null;
    String alias = null;
    Map<String, String> alterMap = null;
```

### RuleId[ruleID=UnusedAssignment]
Variable `timeRangeToExpand` initializer `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/aggregation/timerangeiterator/PreAggrWindowWithNaturalMonthIterator.java`
#### Snippet
```java

  private void tryToExpandHeap() {
    TimeRange timeRangeToExpand = null;
    while (aggrWindowIterator.hasNextTimeRange() && timeBoundaryHeap.size() < HEAP_MAX_SIZE) {
      timeRangeToExpand = aggrWindowIterator.nextTimeRange();
```

### RuleId[ruleID=UnusedAssignment]
Variable `value` initializer `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/wal/buffer/WALEntry.java`
#### Snippet
```java
    // handle info
    long memTableId = stream.readLong();
    WALEntryValue value = null;
    switch (type) {
      case MEMORY_TABLE_SNAPSHOT:
```

### RuleId[ruleID=UnusedAssignment]
Variable `beginIndex` initializer `-1` is redundant
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/AbstractOpBlock.java`
#### Snippet
```java

  // record First Entry's index. It is the index of all data of 1 SG.
  protected long beginIndex = -1;
  // data number of this OpBlock
  protected long dataCount = -1;
```

### RuleId[ruleID=UnusedAssignment]
Variable `operation` initializer `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/sync/externalpipe/ExtPipePlugin.java`
#### Snippet
```java
          }

          Operation operation = null;
          try {
            operation =
```

### RuleId[ruleID=UnusedAssignment]
Variable `tsFileReader` initializer `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/query/control/FileReaderManager.java`
#### Snippet
```java
      }

      TsFileSequenceReader tsFileReader = null;
      // check if the file is old version
      if (!isClosed) {
```

### RuleId[ruleID=UnusedAssignment]
Variable `maxSize` initializer `0` is redundant
in `session/src/main/java/org/apache/iotdb/session/pool/SessionPool.java`
#### Snippet
```java
  private final ConcurrentMap<Session, Session> occupied = new ConcurrentHashMap<>();
  private int size = 0;
  private int maxSize = 0;
  private final long waitToGetSessionTimeoutInMs;

```

### RuleId[ruleID=UnusedAssignment]
The value `null` assigned to `pathToAggrIndexesMap` is never used
in `server/src/main/java/org/apache/iotdb/db/query/dataset/groupby/GroupByWithValueFilterDataSet.java`
#### Snippet
```java
    }
    // assign null to be friendly for GC
    pathToAggrIndexesMap = null;
    // init aligned series reader
    for (PartialPath alignedPath : alignedPathToAggrIndexesMap.keySet()) {
```

### RuleId[ruleID=UnusedAssignment]
The value `null` assigned to `alignedPathToAggrIndexesMap` is never used
in `server/src/main/java/org/apache/iotdb/db/query/dataset/groupby/GroupByWithValueFilterDataSet.java`
#### Snippet
```java
    }
    // assign null to be friendly for GC
    alignedPathToAggrIndexesMap = null;

    preAggregateResults = new AggregateResult[paths.size()];
```

### RuleId[ruleID=UnusedAssignment]
Variable `cnt` initializer `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/query/aggregation/impl/AvgAggrResult.java`
#### Snippet
```java
  private TSDataType seriesDataType;
  private double avg = 0.0;
  private long cnt = 0;

  public AvgAggrResult(TSDataType seriesDataType) {
```

### RuleId[ruleID=UnusedAssignment]
Variable `avg` initializer `0.0` is redundant
in `server/src/main/java/org/apache/iotdb/db/query/aggregation/impl/AvgAggrResult.java`
#### Snippet
```java

  private TSDataType seriesDataType;
  private double avg = 0.0;
  private long cnt = 0;

```

### RuleId[ruleID=UnusedAssignment]
Variable `timeseriesMetadataMap` initializer `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileSelfCheckTool.java`
#### Snippet
```java
  private Map<Long, Pair<Path, TimeseriesMetadata>> getTimeseriesMetadataMapWithReader(
      TsFileSelfCheckToolReader reader) throws Exception {
    Map<Long, Pair<Path, TimeseriesMetadata>> timeseriesMetadataMap = null;
    timeseriesMetadataMap = reader.getAllTimeseriesMetadataWithOffset();
    return timeseriesMetadataMap;
```

### RuleId[ruleID=UnusedAssignment]
Variable `timeseriesMetadataMap` initializer `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileSelfCheckTool.java`
#### Snippet
```java
      throws IOException, TsFileTimeseriesMetadataException {
    TsFileSelfCheckToolReader reader = new TsFileSelfCheckToolReader(filename);
    Map<Long, Pair<Path, TimeseriesMetadata>> timeseriesMetadataMap = null;
    try {
      timeseriesMetadataMap = getTimeseriesMetadataMapWithReader(reader);
```

### RuleId[ruleID=UnusedAssignment]
Variable `timeseriesMetadataMap` initializer `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileSelfCheckTool.java`
#### Snippet
```java
    logger.info("file path: " + filename);
    TsFileSelfCheckToolReader reader = new TsFileSelfCheckToolReader(filename);
    Map<Long, Pair<Path, TimeseriesMetadata>> timeseriesMetadataMap = null;
    long res = -1;
    try {
```

### RuleId[ruleID=UnusedAssignment]
Variable `res` initializer `-1` is redundant
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileSelfCheckTool.java`
#### Snippet
```java
    TsFileSelfCheckToolReader reader = new TsFileSelfCheckToolReader(filename);
    Map<Long, Pair<Path, TimeseriesMetadata>> timeseriesMetadataMap = null;
    long res = -1;
    try {
      timeseriesMetadataMap = getTimeseriesMetadataMapWithReader(reader);
```

### RuleId[ruleID=UnusedAssignment]
Variable `line` initializer `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/tools/settle/TsFileAndModSettleTool.java`
#### Snippet
```java
              new FileReader(
                  FSFactoryProducer.getFSFactory().getFile(SettleLog.getSettleLogPath())))) {
        String line = null;
        while ((line = settleLogReader.readLine()) != null && !"".equals(line)) {
          String oldFilePath = line.split(SettleLog.COMMA_SEPERATOR)[0];
```

### RuleId[ruleID=UnusedAssignment]
Variable `currentDeletion` initializer `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileSplitByPartitionTool.java`
#### Snippet
```java
    if (oldModification != null) {
      modsIterator = oldModification.iterator();
      Deletion currentDeletion = null;
      while (modsIterator.hasNext()) {
        currentDeletion = (Deletion) modsIterator.next();
```

### RuleId[ruleID=UnusedAssignment]
Variable `currentDeletion` initializer `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileSplitByPartitionTool.java`
#### Snippet
```java
      ChunkMetadata chunkMetadata = new ChunkMetadata();
      modsIterator = oldModification.iterator();
      Deletion currentDeletion = null;
      while (modsIterator.hasNext()) {
        currentDeletion = (Deletion) modsIterator.next();
```

### RuleId[ruleID=UnusedAssignment]
Variable `line` initializer `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/utils/UpgradeUtils.java`
#### Snippet
```java
              new FileReader(
                  FSFactoryProducer.getFSFactory().getFile(UpgradeLog.getUpgradeLogPath())))) {
        String line = null;
        while ((line = upgradeLogReader.readLine()) != null) {
          String oldFilePath = line.split(COMMA_SEPERATOR)[0];
```

### RuleId[ruleID=UnusedAssignment]
The value `null` assigned to `pathToAggrIndexesMap` is never used
in `server/src/main/java/org/apache/iotdb/db/query/executor/AggregationExecutor.java`
#### Snippet
```java
    }
    // assign null to be friendly for GC
    pathToAggrIndexesMap = null;
    for (AlignedPath vectorPath : alignedPathToAggrIndexesMap.keySet()) {
      IReaderByTimestamp seriesReaderByTimestamp =
```

### RuleId[ruleID=UnusedAssignment]
The value `null` assigned to `alignedPathToAggrIndexesMap` is never used
in `server/src/main/java/org/apache/iotdb/db/query/executor/AggregationExecutor.java`
#### Snippet
```java
    }
    // assign null to be friendly for GC
    alignedPathToAggrIndexesMap = null;

    for (int i = 0; i < selectedSeries.size(); i++) {
```

### RuleId[ruleID=UnusedAssignment]
Variable `timeRangeToExpand` initializer `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/utils/timerangeiterator/PreAggrWindowWithNaturalMonthIterator.java`
#### Snippet
```java

  private void tryToExpandHeap() {
    TimeRange timeRangeToExpand = null;
    while (aggrWindowIterator.hasNextTimeRange() && timeBoundaryHeap.size() < HEAP_MAX_SIZE) {
      timeRangeToExpand = aggrWindowIterator.nextTimeRange();
```

### RuleId[ruleID=UnusedAssignment]
Variable `element` initializer `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/FixedPriorityBlockingQueue.java`
#### Snippet
```java
    final ReentrantLock lock = this.lock;
    lock.lock();
    T element = null;
    try {
      element = queue.pollLast();
```

### RuleId[ruleID=UnusedAssignment]
Variable `ioMessage` initializer `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/engine/flush/MemTableFlushTask.java`
#### Snippet
```java
            writer.getFile().getName());
        while (true) {
          Object ioMessage = null;
          try {
            ioMessage = ioTaskQueue.take();
```

### RuleId[ruleID=UnusedAssignment]
Variable `task` initializer `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/CompactionWorker.java`
#### Snippet
```java
    while (!Thread.currentThread().isInterrupted()) {
      try {
        AbstractCompactionTask task = null;
        try {
          task = compactionTaskQueue.take();
```

### RuleId[ruleID=UnusedAssignment]
Variable `innerSpaceCompactionSelector` initializer `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/CompactionScheduler.java`
#### Snippet
```java
    }

    ICompactionSelector innerSpaceCompactionSelector = null;
    if (sequence) {
      innerSpaceCompactionSelector =
```

### RuleId[ruleID=UnusedAssignment]
Variable `snapshotComplete` initializer `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/engine/snapshot/SnapshotLoader.java`
#### Snippet
```java

  private DataRegion loadSnapshotWithLog(File logFile) {
    boolean snapshotComplete = false;
    try {
      logAnalyzer = new SnapshotLogAnalyzer(logFile);
```

### RuleId[ruleID=UnusedAssignment]
Variable `seqFileCost` initializer `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/cross/utils/ReadPointCrossCompactionEstimator.java`
#### Snippet
```java
      // else is sub compaction task num.
      int concurrentSeriesNum = fileInfo[2] == -1 ? subCompactionTaskNum : fileInfo[2];
      long seqFileCost = 0;
      if (fileInfo[0] == 0) { // If totalChunkNum ==0, i.e. this seq tsFile has no chunk.
        logger.warn(
```

### RuleId[ruleID=UnusedAssignment]
Variable `memoryCost` initializer `0L` is redundant
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/cross/CrossSpaceCompactionTask.java`
#### Snippet
```java
  protected double selectedSeqFileSize = 0;
  protected double selectedUnseqFileSize = 0;
  protected long memoryCost = 0L;

  public CrossSpaceCompactionTask(
```

### RuleId[ruleID=UnusedAssignment]
Variable `targetFile` initializer `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/task/CompactionRecoverTask.java`
#### Snippet
```java
          if (sourceFileIdentifier.isSequence()) {
            File tmpTargetFile = targetFileIdentifiers.get(i).getFileFromDataDirs();
            File targetFile = null;

            // move tmp target file to target file if not exist
```

### RuleId[ruleID=UnusedAssignment]
Variable `handleSuccess` initializer `true` is redundant
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/CompactionExceptionHandler.java`
#### Snippet
```java
          unseqResourceList);

      boolean handleSuccess = true;

      List<TsFileResource> lostSourceFiles = new ArrayList<>();
```

### RuleId[ruleID=UnusedAssignment]
Variable `sgPath` initializer `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/service/SettleService.java`
#### Snippet
```java

  public PartialPath getSGByFilePath(String tsFilePath) throws WriteProcessException {
    PartialPath sgPath = null;
    try {
      sgPath =
```

### RuleId[ruleID=UnusedAssignment]
Variable `isDataNode` initializer `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/service/metrics/SystemMetrics.java`
#### Snippet
```java
  private final ScheduledExecutorService service = Executors.newSingleThreadScheduledExecutor();
  private final Set<FileStore> fileStores = new HashSet<>();
  private boolean isDataNode = false;
  private long systemDiskTotalSpace = 0L;
  private long systemDiskFreeSpace = 0L;
```

### RuleId[ruleID=UnusedAssignment]
The value `size` assigned to `v` is never used
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/MemMTreeStore.java`
#### Snippet
```java

  private void releaseMemory(int size) {
    localMemoryUsage.getAndUpdate(v -> v -= size);
    memoryStatistics.releaseMemory(size);
  }
```

### RuleId[ruleID=UnusedAssignment]
The value `size` assigned to `v` is never used
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/MemMTreeStore.java`
#### Snippet
```java
  private void requestMemory(int size) {
    memoryStatistics.requestMemory(size);
    localMemoryUsage.getAndUpdate(v -> v += size);
  }

```

### RuleId[ruleID=UnusedAssignment]
The value `size` assigned to `v` is never used
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/memcontrol/MemManagerNodeEstimatedSizeBasedImpl.java`
#### Snippet
```java
    int size = estimator.estimateSize(node);
    memoryStatistics.requestMemory(size);
    pinnedSize.getAndUpdate(v -> v += size);
  }

```

### RuleId[ruleID=UnusedAssignment]
The value `deltaSize` assigned to `v` is never used
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/memcontrol/MemManagerNodeEstimatedSizeBasedImpl.java`
#### Snippet
```java
      memoryStatistics.releaseMemory(deltaSize);
    }
    pinnedSize.getAndUpdate(v -> v += deltaSize);
  }

```

### RuleId[ruleID=UnusedAssignment]
The value `size` assigned to `v` is never used
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/memcontrol/MemManagerNodeEstimatedSizeBasedImpl.java`
#### Snippet
```java
  public void releasePinnedMemResource(IMNode node) {
    int size = estimator.estimateSize(node);
    this.size.getAndUpdate(v -> v += size);
    pinnedSize.getAndUpdate(v -> v -= size);
  }
```

### RuleId[ruleID=UnusedAssignment]
The value `size` assigned to `v` is never used
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/memcontrol/MemManagerNodeEstimatedSizeBasedImpl.java`
#### Snippet
```java
    int size = estimator.estimateSize(node);
    this.size.getAndUpdate(v -> v += size);
    pinnedSize.getAndUpdate(v -> v -= size);
  }

```

### RuleId[ruleID=UnusedAssignment]
The value `finalSize` assigned to `v` is never used
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/memcontrol/MemManagerNodeEstimatedSizeBasedImpl.java`
#### Snippet
```java
    }
    int finalSize = size;
    this.size.getAndUpdate(v -> v -= finalSize);
    memoryStatistics.releaseMemory(size);
  }
```

### RuleId[ruleID=UnusedAssignment]
The value `size` assigned to `v` is never used
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/memcontrol/MemManagerNodeEstimatedSizeBasedImpl.java`
#### Snippet
```java
  public void upgradeMemResource(IMNode node) {
    int size = estimator.estimateSize(node);
    pinnedSize.getAndUpdate(v -> v += size);
    this.size.getAndUpdate(v -> v -= size);
  }
```

### RuleId[ruleID=UnusedAssignment]
The value `size` assigned to `v` is never used
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/memcontrol/MemManagerNodeEstimatedSizeBasedImpl.java`
#### Snippet
```java
    int size = estimator.estimateSize(node);
    pinnedSize.getAndUpdate(v -> v += size);
    this.size.getAndUpdate(v -> v -= size);
  }

```

### RuleId[ruleID=UnusedAssignment]
The value `size` assigned to `v` is never used
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/memcontrol/MemManagerNodeEstimatedSizeBasedImpl.java`
#### Snippet
```java
  public void releaseMemResource(IMNode node) {
    int size = estimator.estimateSize(node);
    this.size.getAndUpdate(v -> v -= size);
    memoryStatistics.releaseMemory(size);
  }
```

### RuleId[ruleID=UnusedAssignment]
Variable `cacheEntry` initializer `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/cache/CacheManager.java`
#### Snippet
```java
  public synchronized boolean evict() {
    IMNode node = null;
    CacheEntry cacheEntry = null;
    List<IMNode> evictedMNodes = new ArrayList<>();
    boolean isSuccess = false;
```

### RuleId[ruleID=UnusedAssignment]
Variable `childNum` initializer `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/ConfigMTree.java`
#### Snippet
```java

    String name = storageGroupMNode.getName();
    int childNum = 0;

    while (!PATH_ROOT.equals(name)) {
```

### RuleId[ruleID=UnusedAssignment]
The value `0` assigned to `childNum` is never used
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/ConfigMTree.java`
#### Snippet
```java
        case STORAGE_GROUP_MNODE_TYPE:
          storageGroupMNode = deserializeStorageGroupMNode(inputStream);
          childNum = 0;
          stack.push(storageGroupMNode);
          name = storageGroupMNode.getName();
```

### RuleId[ruleID=UnusedAssignment]
The value `evictedNodes.size()` assigned to `value` is never used
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/memcontrol/MemManagerNodeNumBasedImpl.java`
#### Snippet
```java
  @Override
  public void releaseMemResource(List<IMNode> evictedNodes) {
    size.getAndUpdate(value -> value -= evictedNodes.size());
  }

```

### RuleId[ruleID=UnusedAssignment]
The value `size` assigned to `v` is never used
in `server/src/main/java/org/apache/iotdb/db/metadata/rescon/MemoryStatistics.java`
#### Snippet
```java

  public void requestMemory(long size) {
    memoryUsage.getAndUpdate(v -> v += size);
    if (memoryUsage.get() >= memoryCapacity) {
      logger.warn("Current series number {} is too large...", memoryUsage);
```

### RuleId[ruleID=UnusedAssignment]
The value `size` assigned to `v` is never used
in `server/src/main/java/org/apache/iotdb/db/metadata/rescon/MemoryStatistics.java`
#### Snippet
```java

  public void releaseMemory(long size) {
    memoryUsage.getAndUpdate(v -> v -= size);
    if (!allowToCreateNewSeries && memoryUsage.get() < memoryCapacity) {
      logger.info("Current series number {} come back to normal level", memoryUsage);
```

### RuleId[ruleID=UnusedAssignment]
Variable `removeOldSubEntry` initializer `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/pagemgr/PageManager.java`
#### Snippet
```java
  @Override
  public void writeUpdatedChildren(IMNode node) throws MetadataException, IOException {
    boolean removeOldSubEntry = false, insertNewSubEntry = false;
    int subIndex;
    long curSegAddr = getNodeAddress(node);
```

### RuleId[ruleID=UnusedAssignment]
Variable `insertNewSubEntry` initializer `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/pagemgr/PageManager.java`
#### Snippet
```java
  @Override
  public void writeUpdatedChildren(IMNode node) throws MetadataException, IOException {
    boolean removeOldSubEntry = false, insertNewSubEntry = false;
    int subIndex;
    long curSegAddr = getNodeAddress(node);
```

### RuleId[ruleID=UnusedAssignment]
Variable `region` initializer `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/RSchemaRegionLoader.java`
#### Snippet
```java
  public ISchemaRegion loadRSchemaRegion(
      PartialPath storageGroup, SchemaRegionId schemaRegionId, IStorageGroupMNode node) {
    ISchemaRegion region = null;
    LOGGER.info("Creating instance for schema-engine-rocksdb");
    try {
```

### RuleId[ruleID=UnusedAssignment]
Variable `device` initializer `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/MTreeBelowSGMemoryImpl.java`
#### Snippet
```java
  public Map<Integer, MetadataException> checkMeasurementExistence(
      PartialPath devicePath, List<String> measurementList, List<String> aliasList) {
    IMNode device = null;
    try {
      device = getNodeByPath(devicePath);
```

### RuleId[ruleID=UnusedAssignment]
Variable `device` initializer `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/MTreeBelowSGCachedImpl.java`
#### Snippet
```java
  public Map<Integer, MetadataException> checkMeasurementExistence(
      PartialPath devicePath, List<String> measurementList, List<String> aliasList) {
    IMNode device = null;
    try {
      device = getNodeByPath(devicePath);
```

### RuleId[ruleID=UnusedAssignment]
Variable `idx` initializer `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/SchemaRegionMemoryImpl.java`
#### Snippet
```java
    // init the metadata from the operation log
    if (logFile.exists()) {
      int idx = 0;
      try (SchemaLogReader<ISchemaRegionPlan> mLogReader =
          new SchemaLogReader<>(
```

### RuleId[ruleID=UnusedAssignment]
Variable `ans` initializer `0` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/util/Util.java`
#### Snippet
```java
   */
  public static double getValueAsDouble(Row row, int index) throws Exception {
    double ans = 0;
    try {
      switch (row.getDataType(index)) {
```

### RuleId[ruleID=UnusedAssignment]
The value `cnt_epsilon` assigned to `var_epsilon` is never used
in `library-udf/src/main/java/org/apache/iotdb/library/drepair/util/ARFill.java`
#### Snippet
```java
    }
    mean_epsilon /= cnt_epsilon;
    var_epsilon /= cnt_epsilon;
    for (int i = 0; i < original.length; i++) {
      double yt = original[i];
```

### RuleId[ruleID=UnusedAssignment]
The value `-1` assigned to `w_nr` is never used
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/Segment.java`
#### Snippet
```java
      w_nr = cur_nr + in_window_size;
      if (input_df.length <= w_nr) {
        w_nr = -1;
        last_run = true;
        w = Arrays.copyOfRange(input_df, cur_nr, input_df.length);
```

### RuleId[ruleID=UnusedAssignment]
Variable `ans` initializer `0` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/frequency/util/FFTUtil.java`
#### Snippet
```java

  public void add(PointCollector collector, double a[], int i) throws Exception {
    double ans = 0;
    switch (result) {
      case "real":
```

### RuleId[ruleID=UnusedAssignment]
Variable `pageIndex` initializer `0` is redundant
in `rewrite-tsfile-tool/src/main/java/org/apache/iotdb/RewriteTsFileTool.java`
#### Snippet
```java
      reader.position((long) TSFileConfig.MAGIC_STRING.getBytes().length + 1);
      List<long[]> timeBatch = new ArrayList<>();
      int pageIndex = 0;
      byte marker;
      String currentDevice = null;
```

### RuleId[ruleID=UnusedAssignment]
Variable `modifications` initializer `null` is redundant
in `rewrite-tsfile-tool/src/main/java/org/apache/iotdb/RewriteTsFileTool.java`
#### Snippet
```java
  private static void writeModification(String filename, Session session)
      throws IoTDBConnectionException, StatementExecutionException {
    List<Modification> modifications = null;
    if (FSFactoryProducer.getFSFactory()
        .getFile(filename + ModificationFile.FILE_SUFFIX)
```

### RuleId[ruleID=UnusedAssignment]
Variable `idx` initializer `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/SchemaRegionSchemaFileImpl.java`
#### Snippet
```java
    // init the metadata from the operation log
    if (logFile.exists()) {
      int idx = 0;
      try (SchemaLogReader<ISchemaRegionPlan> mLogReader =
          new SchemaLogReader<>(
```

### RuleId[ruleID=UnusedAssignment]
Variable `rollbackException` initializer `null` is redundant
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/impl/schema/UnsetTemplateProcedure.java`
#### Snippet
```java
    }
    alreadyRollback = true;
    ProcedureException rollbackException = null;
    try {
      executeRollbackInvalidateCache(env);
```

### RuleId[ruleID=UnusedAssignment]
Variable `activeCount` initializer `activeExecutorCount.incrementAndGet()` is redundant
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/ProcedureExecutor.java`
#### Snippet
```java
          }
          this.activeProcedure = procedure;
          int activeCount = activeExecutorCount.incrementAndGet();
          startTime.set(System.currentTimeMillis());
          executeProcedure(procedure);
```

### RuleId[ruleID=UnusedAssignment]
Variable `tsFileWriter` initializer `null` is redundant
in `spark-tsfile/src/main/java/org/apache/iotdb/spark/tsfile/io/TsFileRecordWriter.java`
#### Snippet
```java
public class TsFileRecordWriter extends RecordWriter<NullWritable, TSRecord> {

  private TsFileWriter tsFileWriter = null;

  public TsFileRecordWriter(TaskAttemptContext job, Path file, Schema schema) throws IOException {
```

### RuleId[ruleID=UnusedAssignment]
Variable `paths` initializer `new ArrayList<>()` is redundant
in `spark-tsfile/src/main/java/org/apache/iotdb/spark/tsfile/qp/common/TSQueryPlan.java`
#### Snippet
```java
public class TSQueryPlan {

  private List<String> paths = new ArrayList<>();
  private FilterOperator timeFilterOperator;
  private FilterOperator valueFilterOperator;
```

### RuleId[ruleID=UnusedAssignment]
Variable `midRet` initializer `null` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/expression/util/ExpressionOptimizer.java`
#### Snippet
```java
          IExpression regularLeft = optimize(left, selectedSeries);
          IExpression regularRight = optimize(right, selectedSeries);
          IBinaryExpression midRet = null;
          if (relation == ExpressionType.AND) {
            midRet = BinaryExpression.and(regularLeft, regularRight);
```

### RuleId[ruleID=UnusedAssignment]
Variable `is64Bit` initializer `false` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/RamUsageEstimator.java`
#### Snippet
```java

    static {
      boolean is64Bit = false;
      try {
        final Class<?> unsafeClass = Class.forName("sun.misc.Unsafe");
```

### RuleId[ruleID=UnusedAssignment]
Variable `dataPoint` initializer `null` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/record/datapoint/DataPoint.java`
#### Snippet
```java
   */
  public static DataPoint getDataPoint(TSDataType dataType, String measurementId, String value) {
    DataPoint dataPoint = null;
    try {
      switch (dataType) {
```

### RuleId[ruleID=UnusedAssignment]
Variable `fileLength` initializer `0` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/writer/tsmiterator/DiskTSMIterator.java`
#### Snippet
```java
  private File cmtFile;
  private LocalTsFileInput input;
  private long fileLength = 0;
  private long currentPos = 0;
  private long nextEndPosForDevice = 0;
```

### RuleId[ruleID=UnusedAssignment]
Variable `nextEndPosForDevice` initializer `0` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/writer/tsmiterator/DiskTSMIterator.java`
#### Snippet
```java
  private long fileLength = 0;
  private long currentPos = 0;
  private long nextEndPosForDevice = 0;
  private String currentDevice;
  private boolean remainsInFile = true;
```

### RuleId[ruleID=UnusedAssignment]
Variable `currentDevice` initializer `null` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/writer/TsFileIOWriter.java`
#### Snippet
```java
    Map<String, MetadataIndexNode> deviceMetadataIndexMap = new TreeMap<>();
    Queue<MetadataIndexNode> measurementMetadataIndexQueue = new ArrayDeque<>();
    String currentDevice = null;
    String prevDevice = null;
    Path currentPath = null;
```

### RuleId[ruleID=UnusedAssignment]
Variable `currentPath` initializer `null` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/writer/TsFileIOWriter.java`
#### Snippet
```java
    String currentDevice = null;
    String prevDevice = null;
    Path currentPath = null;
    MetadataIndexNode currentIndexNode =
        new MetadataIndexNode(MetadataIndexNodeType.LEAF_MEASUREMENT);
```

### RuleId[ruleID=UnusedAssignment]
Variable `compressed` initializer `null` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/compress/ICompressor.java`
#### Snippet
```java
      byte[] maxCompressed = new byte[getMaxBytesForCompression(length)];
      int compressedSize = Snappy.compress(data, offset, length, maxCompressed, 0);
      byte[] compressed = null;
      if (compressedSize < maxCompressed.length) {
        compressed = new byte[compressedSize];
```

### RuleId[ruleID=UnusedAssignment]
Variable `measurementMetadataIndexPair` initializer `metadataIndexPair` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/TsFileSequenceReader.java`
#### Snippet
```java
    // the content of next Layer MeasurementNode of the specific device's DeviceNode
    ByteBuffer buffer = readData(metadataIndexPair.left.getOffset(), metadataIndexPair.right);
    Pair<MetadataIndexEntry, Long> measurementMetadataIndexPair = metadataIndexPair;
    List<TimeseriesMetadata> timeseriesMetadataList = new ArrayList<>();

```

### RuleId[ruleID=UnusedAssignment]
Variable `tscheckStatus` initializer `TsFileCheckStatus.COMPLETE_FILE` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/TsFileSequenceReader.java`
#### Snippet
```java
      Statistics<? extends Serializable> chunkMetadatasSta = Statistics.getStatsByType(dataType);
      for (IChunkMetadata chunkMetadata : getChunkMetadataList(entry.getValue().left)) {
        long tscheckStatus = TsFileCheckStatus.COMPLETE_FILE;
        try {
          tscheckStatus = checkChunkAndPagesStatistics(chunkMetadata);
```

### RuleId[ruleID=UnusedAssignment]
Variable `threshold` initializer `1e-4` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/encoder/FreqEncoder.java`
#### Snippet
```java
  private int blockSize;
  protected int writeIndex = 0;
  private double threshold = 1e-4;
  private int beta;
  private double[] dataBuffer;
```

### RuleId[ruleID=UnusedAssignment]
Variable `isSuccess` initializer `false` is redundant
in `example/hadoop/src/main/java/org/apache/iotdb/hadoop/tsfile/TSMRWriteExample.java`
#### Snippet
```java
    }; // configure reading which measurementIds
    TSFInputFormat.setReadMeasurementIds(job, measurementIds);
    boolean isSuccess = false;
    try {
      isSuccess = job.waitForCompletion(true);
```

### RuleId[ruleID=UnusedAssignment]
Variable `isSuccess` initializer `false` is redundant
in `example/hadoop/src/main/java/org/apache/iotdb/hadoop/tsfile/TSFMRReadExample.java`
#### Snippet
```java
    }; // configure reading which measurementIds
    TSFInputFormat.setReadMeasurementIds(job, measurementIds);
    boolean isSuccess = false;
    try {
      isSuccess = job.waitForCompletion(true);
```

### RuleId[ruleID=UnusedAssignment]
Variable `result` initializer `""` is redundant
in `metrics/dropwizard-metrics/src/main/java/org/apache/iotdb/metrics/dropwizard/reporter/DropwizardPrometheusReporter.java`
#### Snippet
```java
    DropwizardMetricsExporter dropwizardMetricsExporter =
        new DropwizardMetricsExporter(metricRegistry, prometheusTextWriter);
    String result = "";
    try {
      dropwizardMetricsExporter.scrape();
```

### RuleId[ruleID=UnusedAssignment]
Variable `fileInfo` initializer `null` is redundant
in `consensus/src/main/java/org/apache/iotdb/consensus/ratis/SnapshotStorage.java`
#### Snippet
```java
        continue;
      }
      FileInfo fileInfo = null;
      try {
        fileInfo = new FileInfo(file.toRealPath(), null);
```

### RuleId[ruleID=UnusedAssignment]
Variable `applicationRequest` initializer `null` is redundant
in `consensus/src/main/java/org/apache/iotdb/consensus/ratis/ApplicationStateMachineProxy.java`
#### Snippet
```java
    updateLastAppliedTermIndex(log.getTerm(), log.getIndex());

    IConsensusRequest applicationRequest = null;

    // if this server is leader
```

## RuleId[ruleID=IndexOfReplaceableByContains]
### RuleId[ruleID=IndexOfReplaceableByContains]
`x.indexOf("64") != -1` can be replaced with 'x.contains("64")'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/RamUsageEstimator.java`
#### Snippet
```java
        final String x = System.getProperty("sun.arch.data.model");
        if (x != null) {
          is64Bit = x.indexOf("64") != -1;
        } else {
          if (OS_ARCH != null && OS_ARCH.indexOf("64") != -1) {
```

### RuleId[ruleID=IndexOfReplaceableByContains]
`OS_ARCH.indexOf("64") != -1` can be replaced with 'OS_ARCH.contains("64")'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/RamUsageEstimator.java`
#### Snippet
```java
          is64Bit = x.indexOf("64") != -1;
        } else {
          if (OS_ARCH != null && OS_ARCH.indexOf("64") != -1) {
            is64Bit = true;
          } else {
```

## RuleId[ruleID=Contract]
### RuleId[ruleID=Contract]
Contract clause '_ -\> true' is violated
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileResourceList.java`
#### Snippet
```java
    if (newNode.prev != null || newNode.next != null) {
      // this node already in a list
      return false;
    }
    if (tail == null) {
```

## RuleId[ruleID=UnnecessarySemicolon]
### RuleId[ruleID=UnnecessarySemicolon]
Unnecessary semicolon `;`
in `node-commons/src/main/java/org/apache/iotdb/commons/trigger/TriggerInformation.java`
#### Snippet
```java
  private String jarFileMD5;

  public TriggerInformation() {};

  public TriggerInformation(
```

### RuleId[ruleID=UnnecessarySemicolon]
Unnecessary semicolon `;`
in `example/kafka/src/main/java/org/apache/iotdb/kafka/ConsumerThread.java`
#### Snippet
```java
    List<String> deviceIds = new ArrayList<>(size);
    List<Long> times = new ArrayList<>(size);
    ;
    List<List<String>> measurementsList = new ArrayList<>(size);
    ;
```

### RuleId[ruleID=UnnecessarySemicolon]
Unnecessary semicolon `;`
in `example/kafka/src/main/java/org/apache/iotdb/kafka/ConsumerThread.java`
#### Snippet
```java
    ;
    List<List<String>> measurementsList = new ArrayList<>(size);
    ;
    List<List<TSDataType>> typesList = new ArrayList<>(size);
    ;
```

### RuleId[ruleID=UnnecessarySemicolon]
Unnecessary semicolon `;`
in `example/kafka/src/main/java/org/apache/iotdb/kafka/ConsumerThread.java`
#### Snippet
```java
    ;
    List<List<TSDataType>> typesList = new ArrayList<>(size);
    ;
    List<List<Object>> valuesList = new ArrayList<>(size);
    ;
```

### RuleId[ruleID=UnnecessarySemicolon]
Unnecessary semicolon `;`
in `example/kafka/src/main/java/org/apache/iotdb/kafka/ConsumerThread.java`
#### Snippet
```java
    ;
    List<List<Object>> valuesList = new ArrayList<>(size);
    ;
    for (String data : datas) {
      String[] dataArray = data.split(",");
```

### RuleId[ruleID=UnnecessarySemicolon]
Unnecessary semicolon `;`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/QueryType.java`
#### Snippet
```java
public enum QueryType {
  WRITE,
  READ;
}

```

### RuleId[ruleID=UnnecessarySemicolon]
Unnecessary semicolon `;`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/cache/StorageGroupCacheResult.java`
#### Snippet
```java
  private List<String> missedDevices = new ArrayList<>();
  /** result map, Notice: this map will be empty when failed */
  protected Map<String, V> map = new HashMap<>();;

  public boolean isSuccess() {
```

### RuleId[ruleID=UnnecessarySemicolon]
Unnecessary semicolon `;`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/statement/metadata/CreateContinuousQueryStatement.java`
#### Snippet
```java
    sqlBuilder.append('\t').append("EVERY ").append(everyInterval).append("ms\n");
    sqlBuilder.append('\t').append("BOUNDARY ").append(boundaryTime).append("\n");
    ;
    sqlBuilder.append('\t').append("RANGE ").append(startTimeOffset).append("ms");
    if (endTimeOffset != 0) {
```

### RuleId[ruleID=UnnecessarySemicolon]
Unnecessary semicolon `;`
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/AbstractOpBlock.java`
#### Snippet
```java
  public void close() {
    closed = true;
  };

  public boolean isClosed() {
```

### RuleId[ruleID=UnnecessarySemicolon]
Unnecessary semicolon `;`
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/AbstractOpBlock.java`
#### Snippet
```java
  public boolean isClosed() {
    return closed;
  };

  public long getPipeDataSerialNumber() {
```

### RuleId[ruleID=UnnecessarySemicolon]
Unnecessary semicolon `;`
in `server/src/main/java/org/apache/iotdb/db/sync/externalpipe/operation/Operation.java`
#### Snippet
```java
  public enum OperationType {
    INSERT,
    DELETE;
  }

```

### RuleId[ruleID=UnnecessarySemicolon]
Unnecessary semicolon `;`
in `server/src/main/java/org/apache/iotdb/db/metadata/cache/DataNodeSchemaCache.java`
#### Snippet
```java
  public void takeWriteLock() {
    readWriteLock.writeLock().lock();
    ;
  }

```

### RuleId[ruleID=UnnecessarySemicolon]
Unnecessary semicolon `;`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/SchemaPage.java`
#### Snippet
```java
    ReadWriteIOUtils.write(spareSize, pageBuffer);
    ReadWriteIOUtils.write(memberNum, pageBuffer);
  };

  @Override
```

### RuleId[ruleID=UnnecessarySemicolon]
Unnecessary semicolon `;`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/SchemaPage.java`
#### Snippet
```java
  public WrappedSegment getSegmentOnTest(short idx) throws SegmentNotFoundException {
    return null;
  };

  @Override
```

### RuleId[ruleID=UnnecessarySemicolon]
Unnecessary semicolon `;`
in `server/src/main/java/org/apache/iotdb/db/localconfignode/LocalSchemaPartitionTable.java`
#### Snippet
```java
    private static final LocalSchemaPartitionTable INSTANCE = new LocalSchemaPartitionTable();

    private LocalSchemaPartitionTableHolder() {};
  }

```

### RuleId[ruleID=UnnecessarySemicolon]
Unnecessary semicolon `;`
in `library-udf/src/main/java/org/apache/iotdb/library/drepair/util/ValueFill.java`
#### Snippet
```java
  public double[] getFilled() {
    return repaired;
  };

  public void calMeanAndVar() throws UDFException {
```

### RuleId[ruleID=UnnecessarySemicolon]
Unnecessary semicolon `;`
in `confignode/src/main/java/org/apache/iotdb/confignode/client/ConfigNodeRequestType.java`
#### Snippet
```java
  REMOVE_CONFIG_NODE,
  DELETE_CONFIG_NODE_PEER,
  STOP_CONFIG_NODE;
}

```

### RuleId[ruleID=UnnecessarySemicolon]
Unnecessary semicolon `;`
in `external-api/src/main/java/org/apache/iotdb/external/api/thrift/JudgableServerContext.java`
#### Snippet
```java
  default boolean isWrapperFor(Class<?> iface) {
    return false;
  };
}

```

### RuleId[ruleID=UnnecessarySemicolon]
Unnecessary semicolon `;`
in `consensus/src/main/java/org/apache/iotdb/consensus/IStateMachine.java`
#### Snippet
```java
    default long getSleepTime() {
      return 100;
    };
  }

```

## RuleId[ruleID=SimplifyStreamApiCallChains]
### RuleId[ruleID=SimplifyStreamApiCallChains]
'Arrays.asList().stream()' can be replaced with 'Arrays.stream()'
in `server/src/main/java/org/apache/iotdb/db/tools/validate/TsFileValidationTool.java`
#### Snippet
```java
          // get time partition dirs and sort them
          List<File> timePartitionDirs =
              Arrays.asList(Objects.requireNonNull(dataRegionDir.listFiles())).stream()
                  .filter(file -> Pattern.compile("[0-9]*").matcher(file.getName()).matches())
                  .collect(Collectors.toList());
```

## RuleId[ruleID=StringOperationCanBeSimplified]
### RuleId[ruleID=StringOperationCanBeSimplified]
`toLowerCase()` call can be replaced with 'equalsIgnoreCase()'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBResultMetadata.java`
#### Snippet
```java
      return system_user;
    } else if ("QUERY".equals(operationType)) {
      if (("time".equals(columnName.toLowerCase()) && columnInfoList.size() != 2)
          || "timeseries".equals(columnName.toLowerCase())
          || "device".equals(columnName.toLowerCase())) {
```

### RuleId[ruleID=StringOperationCanBeSimplified]
`toLowerCase()` call can be replaced with 'equalsIgnoreCase()'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBResultMetadata.java`
#### Snippet
```java
    } else if ("QUERY".equals(operationType)) {
      if (("time".equals(columnName.toLowerCase()) && columnInfoList.size() != 2)
          || "timeseries".equals(columnName.toLowerCase())
          || "device".equals(columnName.toLowerCase())) {
        return system_null;
```

### RuleId[ruleID=StringOperationCanBeSimplified]
`toLowerCase()` call can be replaced with 'equalsIgnoreCase()'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBResultMetadata.java`
#### Snippet
```java
      if (("time".equals(columnName.toLowerCase()) && columnInfoList.size() != 2)
          || "timeseries".equals(columnName.toLowerCase())
          || "device".equals(columnName.toLowerCase())) {
        return system_null;
      } else if (columnInfoList.size() >= 2
```

### RuleId[ruleID=StringOperationCanBeSimplified]
`toLowerCase()` call can be replaced with 'equalsIgnoreCase()'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBResultMetadata.java`
#### Snippet
```java
        return system_null;
      } else if (columnInfoList.size() >= 2
          && "time".equals(columnInfoList.get(0).toLowerCase())
          && "device".equals(columnInfoList.get(1).toLowerCase())) {
        return system_null;
```

### RuleId[ruleID=StringOperationCanBeSimplified]
`toLowerCase()` call can be replaced with 'equalsIgnoreCase()'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBResultMetadata.java`
#### Snippet
```java
      } else if (columnInfoList.size() >= 2
          && "time".equals(columnInfoList.get(0).toLowerCase())
          && "device".equals(columnInfoList.get(1).toLowerCase())) {
        return system_null;
      }
```

### RuleId[ruleID=StringOperationCanBeSimplified]
Call to `substring()` is redundant
in `jdbc/src/main/java/org/apache/iotdb/jdbc/StringUtils.java`
#### Snippet
```java
      if (maybeMinusChar != '-' && maybeMinusChar != '+') {
        StringBuffer buf = new StringBuffer(dString.length() + 1);
        buf.append(dString.substring(0, ePos + 1));
        buf.append('+');
        buf.append(dString.substring(ePos + 1, dString.length()));
```

### RuleId[ruleID=StringOperationCanBeSimplified]
Unnecessary string length argument
in `jdbc/src/main/java/org/apache/iotdb/jdbc/StringUtils.java`
#### Snippet
```java
        buf.append(dString.substring(0, ePos + 1));
        buf.append('+');
        buf.append(dString.substring(ePos + 1, dString.length()));
        dString = buf.toString();
      }
```

### RuleId[ruleID=StringOperationCanBeSimplified]
Unnecessary empty string argument
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
    try {
      statement = connection.createStatement();
      StringBuilder str = new StringBuilder("");
      resultSet = statement.executeQuery("show functions");
      while (resultSet.next()) {
```

### RuleId[ruleID=StringOperationCanBeSimplified]
Unnecessary empty string argument
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
    try {
      statement = connection.createStatement();
      StringBuilder str = new StringBuilder("");
      resultSet = statement.executeQuery("show functions");
      List<String> listfunction = Arrays.asList("MAX_TIME", "MIN_TIME", "TIME_DIFFERENCE", "NOW");
```

### RuleId[ruleID=StringOperationCanBeSimplified]
Unnecessary empty string argument
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/WrappedSegment.java`
#### Snippet
```java
  public String toString() {
    ByteBuffer bufferR = this.buffer.asReadOnlyBuffer();
    StringBuilder builder = new StringBuilder("");
    builder.append(
        String.format(
```

### RuleId[ruleID=StringOperationCanBeSimplified]
Unnecessary empty string argument
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/WrappedSegment.java`
#### Snippet
```java
    // Measurement presents as (name, data_type, encoding, compressor, alias_if_exist)
    ByteBuffer bufferR = this.buffer.asReadOnlyBuffer();
    StringBuilder builder = new StringBuilder("");
    builder.append(
        String.format(
```

### RuleId[ruleID=StringOperationCanBeSimplified]
Call to `toString()` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/filter/operator/Like.java`
#### Snippet
```java
      this.pattern = Pattern.compile(patternStrBuild.toString());
    } catch (PatternSyntaxException e) {
      throw new PatternSyntaxException("Regular expression error", value.toString(), e.getIndex());
    }
  }
```

## RuleId[ruleID=MethodOverloadsParentMethod]
### RuleId[ruleID=MethodOverloadsParentMethod]
Method `get()` overloads a compatible method of a superclass, when overriding might have been intended
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/datastructure/tv/ElasticSerializableTVList.java`
#### Snippet
```java
    }

    BatchData get(int targetIndex) throws IOException {
      if (!containsKey(targetIndex)) {
        if (cacheCapacity <= size()) {
```

### RuleId[ruleID=MethodOverloadsParentMethod]
Method `get()` overloads a compatible method of a superclass, when overriding might have been intended
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/datastructure/row/ElasticSerializableRowRecordList.java`
#### Snippet
```java
    }

    SerializableRowRecordList get(int targetIndex) throws IOException {
      if (!containsKey(targetIndex)) {
        if (cacheCapacity <= size()) {
```

### RuleId[ruleID=MethodOverloadsParentMethod]
Method `submit()` overloads a compatible method of a superclass, when overriding might have been intended
in `server/src/main/java/org/apache/iotdb/db/utils/windowing/runtime/WindowEvaluationTaskPoolManager.java`
#### Snippet
```java
  }

  public void submit(WindowEvaluationTask task) {
    try {
      super.submit(task);
```

### RuleId[ruleID=MethodOverloadsParentMethod]
Method `setSelectComponent()` overloads a compatible method of a superclass, when overriding might have been intended
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/operator/InfluxQueryOperator.java`
#### Snippet
```java
  }

  public void setSelectComponent(InfluxSelectComponent influxSelectComponent) {
    this.influxSelectComponent = influxSelectComponent;
  }
```

### RuleId[ruleID=MethodOverloadsParentMethod]
Method `satisfy()` overloads a compatible method of a superclass, when overriding might have been intended
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/filter/ValueFilter.java`
#### Snippet
```java
    }

    public boolean satisfy(long time, TsPrimitiveType[] values) {
      Object v = filterType == FilterType.TIME_FILTER ? time : values[index].getValue();
      return this.value.compareTo((T) v) > 0;
```

### RuleId[ruleID=MethodOverloadsParentMethod]
Method `satisfy()` overloads a compatible method of a superclass, when overriding might have been intended
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/filter/ValueFilter.java`
#### Snippet
```java
    }

    public boolean satisfy(long time, TsPrimitiveType[] values) {
      Object v = filterType == FilterType.TIME_FILTER ? time : values[index].getValue();
      return this.value.compareTo((T) v) < 0;
```

### RuleId[ruleID=MethodOverloadsParentMethod]
Method `satisfy()` overloads a compatible method of a superclass, when overriding might have been intended
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/filter/ValueFilter.java`
#### Snippet
```java
    }

    public boolean satisfy(long time, TsPrimitiveType[] values) {
      Object v = filterType == FilterType.TIME_FILTER ? time : values[index].getValue();
      return this.value.equals(v);
```

### RuleId[ruleID=MethodOverloadsParentMethod]
Method `satisfy()` overloads a compatible method of a superclass, when overriding might have been intended
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/filter/ValueFilter.java`
#### Snippet
```java
    }

    public boolean satisfy(long time, TsPrimitiveType[] values) {
      Object v = filterType == FilterType.TIME_FILTER ? time : values[index].getValue();
      return !this.value.equals(v);
```

### RuleId[ruleID=MethodOverloadsParentMethod]
Method `satisfy()` overloads a compatible method of a superclass, when overriding might have been intended
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/filter/ValueFilter.java`
#### Snippet
```java
    }

    public boolean satisfy(long time, TsPrimitiveType[] values) {
      Object v = filterType == FilterType.TIME_FILTER ? time : values[index].getValue();
      return this.values.contains(v) != not;
```

### RuleId[ruleID=MethodOverloadsParentMethod]
Method `satisfy()` overloads a compatible method of a superclass, when overriding might have been intended
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/filter/ValueFilter.java`
#### Snippet
```java
    }

    public boolean satisfy(long time, TsPrimitiveType[] values) {
      Object v = filterType == FilterType.TIME_FILTER ? time : values[index].getValue();
      return this.value.equals(v);
```

### RuleId[ruleID=MethodOverloadsParentMethod]
Method `satisfy()` overloads a compatible method of a superclass, when overriding might have been intended
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/filter/ValueFilter.java`
#### Snippet
```java
    }

    public boolean satisfy(long time, TsPrimitiveType[] values) {
      Object v = filterType == FilterType.TIME_FILTER ? time : values[index].getValue();
      return this.value.equals(v);
```

### RuleId[ruleID=MethodOverloadsParentMethod]
Method `satisfy()` overloads a compatible method of a superclass, when overriding might have been intended
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/filter/ValueFilter.java`
#### Snippet
```java
    }

    public boolean satisfy(long time, TsPrimitiveType[] values) {
      Object v = filterType == FilterType.TIME_FILTER ? time : values[index].getValue();
      return this.value.compareTo((T) v) >= 0;
```

### RuleId[ruleID=MethodOverloadsParentMethod]
Method `satisfy()` overloads a compatible method of a superclass, when overriding might have been intended
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/filter/ValueFilter.java`
#### Snippet
```java
    }

    public boolean satisfy(long time, TsPrimitiveType[] values) {
      Object v = filterType == FilterType.TIME_FILTER ? time : values[index].getValue();
      return this.value.compareTo((T) v) <= 0;
```

## RuleId[ruleID=OptionalContainsCollection]
### RuleId[ruleID=OptionalContainsCollection]
'Optional' contains collection `List`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/execution/config/metadata/template/ShowSchemaTemplateTask.java`
#### Snippet
```java
            .collect(Collectors.toList());
    TsBlockBuilder builder = new TsBlockBuilder(outputDataTypes);
    Optional<List<Template>> optional = Optional.ofNullable(templateList);
    optional
        .orElse(new ArrayList<>())
```

### RuleId[ruleID=OptionalContainsCollection]
'Optional' contains array `boolean[]`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/block/column/DoubleColumn.java`
#### Snippet
```java
  private final long retainedSizeInBytes;

  public DoubleColumn(int positionCount, Optional<boolean[]> valueIsNull, double[] values) {
    this(0, positionCount, valueIsNull.orElse(null), values);
  }
```

### RuleId[ruleID=OptionalContainsCollection]
'Optional' contains array `boolean[]`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/block/column/IntColumn.java`
#### Snippet
```java
  private final long retainedSizeInBytes;

  public IntColumn(int positionCount, Optional<boolean[]> valueIsNull, int[] values) {
    this(0, positionCount, valueIsNull.orElse(null), values);
  }
```

### RuleId[ruleID=OptionalContainsCollection]
'Optional' contains array `boolean[]`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/block/column/BooleanColumn.java`
#### Snippet
```java
  private final long retainedSizeInBytes;

  public BooleanColumn(int positionCount, Optional<boolean[]> valueIsNull, boolean[] values) {
    this(0, positionCount, valueIsNull.orElse(null), values);
  }
```

### RuleId[ruleID=OptionalContainsCollection]
'Optional' contains array `boolean[]`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/block/column/BinaryColumn.java`
#### Snippet
```java
  private final long retainedSizeInBytes;

  public BinaryColumn(int positionCount, Optional<boolean[]> valueIsNull, Binary[] values) {
    this(0, positionCount, valueIsNull.orElse(null), values);
  }
```

### RuleId[ruleID=OptionalContainsCollection]
'Optional' contains array `boolean[]`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/block/column/FloatColumn.java`
#### Snippet
```java
  private final long retainedSizeInBytes;

  public FloatColumn(int positionCount, Optional<boolean[]> valueIsNull, float[] values) {
    this(0, positionCount, valueIsNull.orElse(null), values);
  }
```

### RuleId[ruleID=OptionalContainsCollection]
'Optional' contains array `boolean[]`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/block/column/LongColumn.java`
#### Snippet
```java
  private final long retainedSizeInBytes;

  public LongColumn(int positionCount, Optional<boolean[]> valueIsNull, long[] values) {
    this(0, positionCount, valueIsNull.orElse(null), values);
  }
```

## RuleId[ruleID=JavaReflectionMemberAccess]
### RuleId[ruleID=JavaReflectionMemberAccess]
Cannot resolve method 'getDiagnosticMXBean'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/RamUsageEstimator.java`
#### Snippet
```java
        try {
          Class<?> sunMF = Class.forName("sun.management.ManagementFactory");
          hotSpotBean = sunMF.getMethod("getDiagnosticMXBean").invoke(null);
        } catch (Exception e2) {
          // Last resort option is an attempt to get it from ManagementFactory's server anyway (may
```

## RuleId[ruleID=NonSynchronizedMethodOverridesSynchronizedMethod]
### RuleId[ruleID=NonSynchronizedMethodOverridesSynchronizedMethod]
Unsynchronized method `writeTo()` overrides synchronized method
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/PublicBAOS.java`
#### Snippet
```java
  @Override
  @SuppressWarnings("squid:S3551")
  public void writeTo(OutputStream out) throws IOException {
    out.write(buf, 0, count);
  }
```

### RuleId[ruleID=NonSynchronizedMethodOverridesSynchronizedMethod]
Unsynchronized method `size()` overrides synchronized method
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/PublicBAOS.java`
#### Snippet
```java
  @Override
  @SuppressWarnings("squid:S3551")
  public int size() {
    return count;
  }
```

### RuleId[ruleID=NonSynchronizedMethodOverridesSynchronizedMethod]
Unsynchronized method `reset()` overrides synchronized method
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/PublicBAOS.java`
#### Snippet
```java
  @Override
  @SuppressWarnings("squid:S3551")
  public void reset() {
    count = 0;
  }
```

## RuleId[ruleID=FinallyBlockCannotCompleteNormally]
### RuleId[ruleID=FinallyBlockCannotCompleteNormally]
`finally` block can not complete normally
in `server/src/main/java/org/apache/iotdb/db/service/thrift/impl/DataNodeInternalRPCServiceImpl.java`
#### Snippet
```java
              } catch (InterruptedException e) {
                LOGGER.error("Meets InterruptedException in stopDataNode RPC method");
              } finally {
                LOGGER.info("Executing system.exit(0) in stopDataNode RPC method after 20 seconds");
                System.exit(0);
```

### RuleId[ruleID=FinallyBlockCannotCompleteNormally]
`finally` block can not complete normally
in `confignode/src/main/java/org/apache/iotdb/confignode/service/thrift/ConfigNodeRPCServiceProcessor.java`
#### Snippet
```java
              } catch (IOException e) {
                LOGGER.error("Meet error when stop ConfigNode!", e);
              } finally {
                System.exit(0);
              }
```

## RuleId[ruleID=CollectionContainsUrl]
### RuleId[ruleID=CollectionContainsUrl]
Set `urlSet` may contain URL objects
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/Loader.java`
#### Snippet
```java
  /** function for getting resources. */
  public static Set<URL> getResources(String resource, ClassLoader classLoader) throws IOException {
    Set<URL> urlSet = new HashSet<>();
    Enumeration<URL> urlEnum = classLoader.getResources(resource);
    while (urlEnum.hasMoreElements()) {
```

### RuleId[ruleID=CollectionContainsUrl]
Set `urlSet` may contain URL objects
in `tsfile/src/main/java/org/apache/iotdb/tsfile/common/conf/TSFileDescriptor.java`
#### Snippet
```java
  private void multiplicityWarning(String resource, ClassLoader classLoader) {
    try {
      Set<URL> urlSet = Loader.getResources(resource, classLoader);
      if (urlSet != null && urlSet.size() > 1) {
        logger.warn("Resource [{}] occurs multiple times on the classpath", resource);
```

## RuleId[ruleID=ProtectedMemberInFinalClass]
### RuleId[ruleID=ProtectedMemberInFinalClass]
Class member declared `protected` in 'final' class
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/RamUsageEstimator.java`
#### Snippet
```java

    /** Round the capacity to the next allowed value. */
    protected int roundCapacity(int requestedCapacity) {
      // Maximum positive integer that is a power of two.
      if (requestedCapacity > (0x80000000 >>> 1)) {
```

### RuleId[ruleID=ProtectedMemberInFinalClass]
Class member declared `protected` in 'final' class
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/RamUsageEstimator.java`
#### Snippet
```java

    /** Return the next possible capacity, counting from the current buffers' size. */
    protected int nextCapacity(int current) {
      assert current > 0 && Long.bitCount(current) == 1 : "Capacity must be a power of two.";
      assert ((current << 1) > 0) : "Maximum capacity exceeded (" + (0x80000000 >>> 1) + ").";
```

## RuleId[ruleID=SwitchStatementWithConfusingDeclaration]
### RuleId[ruleID=SwitchStatementWithConfusingDeclaration]
Local variable `pipeName` declared in one 'switch' branch and used in another
in `node-commons/src/main/java/org/apache/iotdb/commons/sync/persistence/SyncLogReader.java`
#### Snippet
```java
          break;
        case STOP_PIPE:
          String pipeName = ReadWriteIOUtils.readString(inputStream);
          pipes.get(pipeName).setStatus(PipeStatus.STOP);
          break;
```

### RuleId[ruleID=SwitchStatementWithConfusingDeclaration]
Local variable `chunkOffset` declared in one 'switch' branch and used in another
in `server/src/main/java/org/apache/iotdb/db/engine/load/TsFileSplitter.java`
#### Snippet
```java
          case MetaMarker.ONLY_ONE_PAGE_CHUNK_HEADER:
          case MetaMarker.ONLY_ONE_PAGE_TIME_CHUNK_HEADER:
            long chunkOffset = reader.position();
            consumeAllAlignedChunkData(chunkOffset, pageIndex2ChunkData);
            handleModification(offset2Deletions, chunkOffset);
```

### RuleId[ruleID=SwitchStatementWithConfusingDeclaration]
Local variable `header` declared in one 'switch' branch and used in another
in `server/src/main/java/org/apache/iotdb/db/engine/load/TsFileSplitter.java`
#### Snippet
```java
            handleModification(offset2Deletions, chunkOffset);

            ChunkHeader header = reader.readChunkHeader(marker);
            String measurementId = header.getMeasurementID();
            if (header.getDataSize() == 0) {
```

### RuleId[ruleID=SwitchStatementWithConfusingDeclaration]
Local variable `chunkMetadata` declared in one 'switch' branch and used in another
in `server/src/main/java/org/apache/iotdb/db/engine/load/TsFileSplitter.java`
#### Snippet
```java
                ((header.getChunkType() & TsFileConstant.TIME_COLUMN_MASK)
                    == TsFileConstant.TIME_COLUMN_MASK);
            IChunkMetadata chunkMetadata = offset2ChunkMetadata.get(chunkOffset - Byte.BYTES);
            TTimePartitionSlot timePartitionSlot =
                TimePartitionUtils.getTimePartition(chunkMetadata.getStartTime());
```

### RuleId[ruleID=SwitchStatementWithConfusingDeclaration]
Local variable `valueDecoder` declared in one 'switch' branch and used in another
in `server/src/main/java/org/apache/iotdb/db/engine/load/TsFileSplitter.java`
#### Snippet
```java
                    TSEncoding.valueOf(TSFileDescriptor.getInstance().getConfig().getTimeEncoder()),
                    TSDataType.INT64);
            Decoder valueDecoder =
                Decoder.getDecoderByType(header.getEncodingType(), header.getDataType());
            int dataSize = header.getDataSize();
```

### RuleId[ruleID=SwitchStatementWithConfusingDeclaration]
Local variable `dataSize` declared in one 'switch' branch and used in another
in `server/src/main/java/org/apache/iotdb/db/engine/load/TsFileSplitter.java`
#### Snippet
```java
            Decoder valueDecoder =
                Decoder.getDecoderByType(header.getEncodingType(), header.getDataType());
            int dataSize = header.getDataSize();
            int pageIndex = 0;
            if (isAligned) {
```

### RuleId[ruleID=SwitchStatementWithConfusingDeclaration]
Local variable `pageIndex` declared in one 'switch' branch and used in another
in `server/src/main/java/org/apache/iotdb/db/engine/load/TsFileSplitter.java`
#### Snippet
```java
                Decoder.getDecoderByType(header.getEncodingType(), header.getDataType());
            int dataSize = header.getDataSize();
            int pageIndex = 0;
            if (isAligned) {
              isTimeChunkNeedDecode = true;
```

### RuleId[ruleID=SwitchStatementWithConfusingDeclaration]
Local variable `length` declared in one 'switch' branch and used in another
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/ReadWriteIOUtils.java`
#### Snippet
```java
        return buffer.getInt();
      case BINARY:
        int length = buffer.getInt();
        byte[] bytes = new byte[length];
        buffer.get(bytes);
```

### RuleId[ruleID=SwitchStatementWithConfusingDeclaration]
Local variable `bytes` declared in one 'switch' branch and used in another
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/ReadWriteIOUtils.java`
#### Snippet
```java
      case BINARY:
        int length = buffer.getInt();
        byte[] bytes = new byte[length];
        buffer.get(bytes);
        return new Binary(bytes);
```

## RuleId[ruleID=UnusedLabel]
### RuleId[ruleID=UnusedLabel]
Unused label `bufferLoop`
in `server/src/main/java/org/apache/iotdb/db/engine/modification/utils/TracedBufferedReader.java`
#### Snippet
```java
      boolean omitLF = ignoreLF || skipLF;

      bufferLoop:
      for (; ; ) {

```

## RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `roleMap` accessed in synchronized context
in `node-commons/src/main/java/org/apache/iotdb/commons/auth/role/BasicRoleManager.java`
#### Snippet
```java
    synchronized (this) {
      reset();
      roleMap = roles;

      for (Entry<String, Role> entry : roleMap.entrySet()) {
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `roleMap` accessed in synchronized context
in `node-commons/src/main/java/org/apache/iotdb/commons/auth/role/BasicRoleManager.java`
#### Snippet
```java
      roleMap = roles;

      for (Entry<String, Role> entry : roleMap.entrySet()) {
        Role role = entry.getValue();
        try {
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `accessor` accessed in synchronized context
in `node-commons/src/main/java/org/apache/iotdb/commons/auth/role/BasicRoleManager.java`
#### Snippet
```java
        Role role = entry.getValue();
        try {
          accessor.saveRole(role);
        } catch (IOException e) {
          throw new AuthException(e);
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `userMap` accessed in synchronized context
in `node-commons/src/main/java/org/apache/iotdb/commons/auth/user/BasicUserManager.java`
#### Snippet
```java
    synchronized (this) {
      reset();
      userMap = users;

      for (Entry<String, User> entry : userMap.entrySet()) {
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `userMap` accessed in synchronized context
in `node-commons/src/main/java/org/apache/iotdb/commons/auth/user/BasicUserManager.java`
#### Snippet
```java
      userMap = users;

      for (Entry<String, User> entry : userMap.entrySet()) {
        User user = entry.getValue();
        try {
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `accessor` accessed in synchronized context
in `node-commons/src/main/java/org/apache/iotdb/commons/auth/user/BasicUserManager.java`
#### Snippet
```java
        User user = entry.getValue();
        try {
          accessor.saveUser(user);
        } catch (IOException e) {
          throw new AuthException(e);
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `tManagers` accessed in synchronized context
in `node-commons/src/main/java/org/apache/iotdb/commons/client/AsyncBaseClientFactory.java`
#### Snippet
```java
    super(clientManager, clientFactoryProperty);
    synchronized (this) {
      tManagers = new TAsyncClientManager[clientFactoryProperty.getSelectorNumOfAsyncClientPool()];
      for (int i = 0; i < tManagers.length; i++) {
        try {
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `tManagers` accessed in synchronized context
in `node-commons/src/main/java/org/apache/iotdb/commons/client/AsyncBaseClientFactory.java`
#### Snippet
```java
    synchronized (this) {
      tManagers = new TAsyncClientManager[clientFactoryProperty.getSelectorNumOfAsyncClientPool()];
      for (int i = 0; i < tManagers.length; i++) {
        try {
          tManagers[i] = new TAsyncClientManager();
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `tManagers` accessed in synchronized context
in `node-commons/src/main/java/org/apache/iotdb/commons/client/AsyncBaseClientFactory.java`
#### Snippet
```java
      for (int i = 0; i < tManagers.length; i++) {
        try {
          tManagers[i] = new TAsyncClientManager();
        } catch (IOException e) {
          logger.error("Cannot create Async client factory", e);
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `EXECUTOR` accessed in synchronized context
in `node-commons/src/main/java/org/apache/iotdb/commons/partition/executor/SeriesPartitionExecutor.java`
#### Snippet
```java
  private static synchronized void initStaticSeriesPartitionExecutor(
      String executorName, int seriesPartitionSlotNum) {
    if (EXECUTOR == null) {
      try {
        Class<?> executor = Class.forName(executorName);
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `EXECUTOR` accessed in synchronized context
in `node-commons/src/main/java/org/apache/iotdb/commons/partition/executor/SeriesPartitionExecutor.java`
#### Snippet
```java
        Class<?> executor = Class.forName(executorName);
        Constructor<?> executorConstructor = executor.getConstructor(int.class);
        EXECUTOR =
            (SeriesPartitionExecutor) executorConstructor.newInstance(seriesPartitionSlotNum);
      } catch (ClassNotFoundException
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `this.enableRPCCompression` accessed in synchronized context
in `influxdb-protocol/src/main/java/org/apache/iotdb/influxdb/session/InfluxDBSession.java`
#### Snippet
```java
      return;
    }
    this.enableRPCCompression = enableRPCCompression;
    this.connectionTimeoutInMs = connectionTimeoutInMs;
    this.endPointList.add(defaultEndPoint);
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `this.connectionTimeoutInMs` accessed in synchronized context
in `influxdb-protocol/src/main/java/org/apache/iotdb/influxdb/session/InfluxDBSession.java`
#### Snippet
```java
    }
    this.enableRPCCompression = enableRPCCompression;
    this.connectionTimeoutInMs = connectionTimeoutInMs;
    this.endPointList.add(defaultEndPoint);

```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `defaultEndPoint` accessed in synchronized context
in `influxdb-protocol/src/main/java/org/apache/iotdb/influxdb/session/InfluxDBSession.java`
#### Snippet
```java
    this.enableRPCCompression = enableRPCCompression;
    this.connectionTimeoutInMs = connectionTimeoutInMs;
    this.endPointList.add(defaultEndPoint);

    init();
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `lsmEngine` accessed in synchronized context
in `schema-engine-tag/src/main/java/org/apache/iotdb/db/metadata/tagSchemaRegion/tagIndex/TagInvertedIndex.java`
#### Snippet
```java
      InsertionRequest insertionRequest =
          new InsertionRequest(generateKeys(tag.getKey(), tag.getValue()), id);
      lsmEngine.insert(insertionRequest);
    }
  }
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `lsmEngine` accessed in synchronized context
in `schema-engine-tag/src/main/java/org/apache/iotdb/db/metadata/tagSchemaRegion/tagIndex/TagInvertedIndex.java`
#### Snippet
```java
      DeletionRequest deletionRequest =
          new DeletionRequest(generateKeys(tag.getKey(), tag.getValue()), id);
      lsmEngine.delete(deletionRequest);
    }
  }
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `scheduledExecutor` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/scheduler/FixedRateFragInsStateTracker.java`
#### Snippet
```java
    trackTask =
        ScheduledExecutorUtil.safelyScheduleAtFixedRate(
            scheduledExecutor,
            this::fetchStateAndUpdate,
            0,
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `status` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/wal/utils/listener/AbstractResultListener.java`
#### Snippet
```java
  public synchronized Status waitForResult() {
    if (wait) {
      while (status == Status.RUNNING) {
        try {
          this.wait();
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `status` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/wal/utils/listener/AbstractResultListener.java`
#### Snippet
```java
      }
    }
    return status;
  }

```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `status` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/wal/utils/listener/AbstractResultListener.java`
#### Snippet
```java
  /** Set status to failure and notify all threads waiting for the result. */
  public synchronized AbstractResultListener fail(Exception e) {
    status = Status.FAILURE;
    cause = e;
    if (wait) {
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `cause` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/wal/utils/listener/AbstractResultListener.java`
#### Snippet
```java
  public synchronized AbstractResultListener fail(Exception e) {
    status = Status.FAILURE;
    cause = e;
    if (wait) {
      this.notifyAll();
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `status` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/wal/utils/listener/AbstractResultListener.java`
#### Snippet
```java
  /** Set status to success and notify all threads waiting for the result. */
  public synchronized AbstractResultListener succeed() {
    status = Status.SUCCESS;
    if (wait) {
      this.notifyAll();
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `lostConnectionTime` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/sync/transport/client/SenderManager.java`
#### Snippet
```java
            // successfully reconnect to receiver.
            logger.error("Connect to receiver {} error, because {}.", pipeSink, e.getMessage(), e);
            lostConnectionTime = Math.min(lostConnectionTime, System.currentTimeMillis());
            blockingQueue.offer(lock);
            lock.wait();
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `lostConnectionTime` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/sync/transport/client/SenderManager.java`
#### Snippet
```java
            // successfully reconnect to receiver.
            logger.error("Connect to receiver {} error, because {}.", pipeSink, e.getMessage(), e);
            lostConnectionTime = Math.min(lostConnectionTime, System.currentTimeMillis());
            blockingQueue.offer(lock);
            lock.wait();
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `modificationList` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/TsFileOpBlock.java`
#### Snippet
```java
    }

    if ((fullPathToDeletionMap == null) && (modificationList != null)) {
      fullPathToDeletionMap = new HashMap<>();
    }
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `seriesReaderList` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/query/dataset/RawQueryDataSetWithoutValueFilter.java`
#### Snippet
```java
      cachedBatchDataArray[seriesIndex] = batchData;

      synchronized (seriesReaderList.get(seriesIndex)) {
        // we only need to judge whether to submit another task when the queue is not full
        if (blockingQueueArray[seriesIndex].remainingCapacity() > 0) {
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `seriesReaderList` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/query/dataset/RawQueryDataSetWithoutValueFilter.java`
#### Snippet
```java
        // we only need to judge whether to submit another task when the queue is not full
        if (blockingQueueArray[seriesIndex].remainingCapacity() > 0) {
          ManagedSeriesReader reader = seriesReaderList.get(seriesIndex);
          // if the reader isn't being managed and still has more data,
          // that means this read task leave the pool before because the queue has no more space
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `defaultSessionConnection` accessed in synchronized context
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
  public synchronized void setTimeZone(String zoneId)
      throws StatementExecutionException, IoTDBConnectionException {
    defaultSessionConnection.setTimeZone(zoneId);
    this.zoneId = ZoneId.of(zoneId);
  }
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `this.zoneId` accessed in synchronized context
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
      throws StatementExecutionException, IoTDBConnectionException {
    defaultSessionConnection.setTimeZone(zoneId);
    this.zoneId = ZoneId.of(zoneId);
  }

```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `defaultSessionConnection` accessed in synchronized context
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
  @Override
  public synchronized String getTimeZone() {
    return defaultSessionConnection.getTimeZone();
  }

```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `enableRedirection` accessed in synchronized context
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
    }
    try {
      if (enableRedirection) {
        for (SessionConnection sessionConnection : endPointToSessionConnection.values()) {
          sessionConnection.close();
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `endPointToSessionConnection` accessed in synchronized context
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
    try {
      if (enableRedirection) {
        for (SessionConnection sessionConnection : endPointToSessionConnection.values()) {
          sessionConnection.close();
        }
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `defaultSessionConnection` accessed in synchronized context
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
        }
      } else {
        defaultSessionConnection.close();
      }
    } finally {
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `this.enableRPCCompression` accessed in synchronized context
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
    }

    this.enableRPCCompression = enableRPCCompression;
    this.connectionTimeoutInMs = connectionTimeoutInMs;
    defaultSessionConnection = constructSessionConnection(this, defaultEndPoint, zoneId);
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `this.connectionTimeoutInMs` accessed in synchronized context
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java

    this.enableRPCCompression = enableRPCCompression;
    this.connectionTimeoutInMs = connectionTimeoutInMs;
    defaultSessionConnection = constructSessionConnection(this, defaultEndPoint, zoneId);
    defaultSessionConnection.setEnableRedirect(enableQueryRedirection);
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `defaultSessionConnection` accessed in synchronized context
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
    this.enableRPCCompression = enableRPCCompression;
    this.connectionTimeoutInMs = connectionTimeoutInMs;
    defaultSessionConnection = constructSessionConnection(this, defaultEndPoint, zoneId);
    defaultSessionConnection.setEnableRedirect(enableQueryRedirection);
    isClosed = false;
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `defaultEndPoint` accessed in synchronized context
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
    this.enableRPCCompression = enableRPCCompression;
    this.connectionTimeoutInMs = connectionTimeoutInMs;
    defaultSessionConnection = constructSessionConnection(this, defaultEndPoint, zoneId);
    defaultSessionConnection.setEnableRedirect(enableQueryRedirection);
    isClosed = false;
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `zoneId` accessed in synchronized context
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
    this.enableRPCCompression = enableRPCCompression;
    this.connectionTimeoutInMs = connectionTimeoutInMs;
    defaultSessionConnection = constructSessionConnection(this, defaultEndPoint, zoneId);
    defaultSessionConnection.setEnableRedirect(enableQueryRedirection);
    isClosed = false;
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `defaultSessionConnection` accessed in synchronized context
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
    this.connectionTimeoutInMs = connectionTimeoutInMs;
    defaultSessionConnection = constructSessionConnection(this, defaultEndPoint, zoneId);
    defaultSessionConnection.setEnableRedirect(enableQueryRedirection);
    isClosed = false;
    if (enableRedirection || enableQueryRedirection) {
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `enableQueryRedirection` accessed in synchronized context
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
    this.connectionTimeoutInMs = connectionTimeoutInMs;
    defaultSessionConnection = constructSessionConnection(this, defaultEndPoint, zoneId);
    defaultSessionConnection.setEnableRedirect(enableQueryRedirection);
    isClosed = false;
    if (enableRedirection || enableQueryRedirection) {
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `enableRedirection` accessed in synchronized context
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
    defaultSessionConnection.setEnableRedirect(enableQueryRedirection);
    isClosed = false;
    if (enableRedirection || enableQueryRedirection) {
      deviceIdToEndpoint = new ConcurrentHashMap<>();
      endPointToSessionConnection = new ConcurrentHashMap<>();
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `enableQueryRedirection` accessed in synchronized context
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
    defaultSessionConnection.setEnableRedirect(enableQueryRedirection);
    isClosed = false;
    if (enableRedirection || enableQueryRedirection) {
      deviceIdToEndpoint = new ConcurrentHashMap<>();
      endPointToSessionConnection = new ConcurrentHashMap<>();
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `deviceIdToEndpoint` accessed in synchronized context
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
    isClosed = false;
    if (enableRedirection || enableQueryRedirection) {
      deviceIdToEndpoint = new ConcurrentHashMap<>();
      endPointToSessionConnection = new ConcurrentHashMap<>();
      endPointToSessionConnection.put(defaultEndPoint, defaultSessionConnection);
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `endPointToSessionConnection` accessed in synchronized context
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
    if (enableRedirection || enableQueryRedirection) {
      deviceIdToEndpoint = new ConcurrentHashMap<>();
      endPointToSessionConnection = new ConcurrentHashMap<>();
      endPointToSessionConnection.put(defaultEndPoint, defaultSessionConnection);
    }
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `endPointToSessionConnection` accessed in synchronized context
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
      deviceIdToEndpoint = new ConcurrentHashMap<>();
      endPointToSessionConnection = new ConcurrentHashMap<>();
      endPointToSessionConnection.put(defaultEndPoint, defaultSessionConnection);
    }
  }
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `defaultEndPoint` accessed in synchronized context
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
      deviceIdToEndpoint = new ConcurrentHashMap<>();
      endPointToSessionConnection = new ConcurrentHashMap<>();
      endPointToSessionConnection.put(defaultEndPoint, defaultSessionConnection);
    }
  }
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `defaultSessionConnection` accessed in synchronized context
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
      deviceIdToEndpoint = new ConcurrentHashMap<>();
      endPointToSessionConnection = new ConcurrentHashMap<>();
      endPointToSessionConnection.put(defaultEndPoint, defaultSessionConnection);
    }
  }
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `data` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/utils/CopyOnReadLinkedList.java`
#### Snippet
```java

  public synchronized void remove(T d) {
    data.remove(d);
  }

```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `data` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/utils/CopyOnReadLinkedList.java`
#### Snippet
```java

  public synchronized boolean contains(T d) {
    return data.contains(d);
  }

```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `readCopy` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/utils/CopyOnReadLinkedList.java`
#### Snippet
```java

  public synchronized List<T> cloneList() {
    if (readCopy == null) {
      readCopy = new ArrayList<>(data);
    }
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `readCopy` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/utils/CopyOnReadLinkedList.java`
#### Snippet
```java
  public synchronized List<T> cloneList() {
    if (readCopy == null) {
      readCopy = new ArrayList<>(data);
    }
    return readCopy;
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `data` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/utils/CopyOnReadLinkedList.java`
#### Snippet
```java
  public synchronized List<T> cloneList() {
    if (readCopy == null) {
      readCopy = new ArrayList<>(data);
    }
    return readCopy;
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `readCopy` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/utils/CopyOnReadLinkedList.java`
#### Snippet
```java
      readCopy = new ArrayList<>(data);
    }
    return readCopy;
  }

```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `readCopy` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/utils/CopyOnReadLinkedList.java`
#### Snippet
```java

  public synchronized void reset() {
    readCopy = null;
  }

```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `readCopy` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/utils/CopyOnReadLinkedList.java`
#### Snippet
```java

  public synchronized Iterator<T> iterator() {
    readCopy = new ArrayList<>(data);
    return readCopy.iterator();
  }
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `data` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/utils/CopyOnReadLinkedList.java`
#### Snippet
```java

  public synchronized Iterator<T> iterator() {
    readCopy = new ArrayList<>(data);
    return readCopy.iterator();
  }
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `readCopy` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/utils/CopyOnReadLinkedList.java`
#### Snippet
```java
  public synchronized Iterator<T> iterator() {
    readCopy = new ArrayList<>(data);
    return readCopy.iterator();
  }

```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `data` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/utils/CopyOnReadLinkedList.java`
#### Snippet
```java

  public synchronized void add(T d) {
    data.add(d);
  }

```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `currentTaskNum` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/CompactionTaskManager.java`
#### Snippet
```java
    // add metrics
    CompactionMetricsRecorder.recordTaskInfo(
        task, CompactionTaskStatus.FINISHED, currentTaskNum.get());
    finishedTaskNum.incrementAndGet();
  }
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `currentTaskNum` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/CompactionTaskManager.java`
#### Snippet
```java
            || config.isEnableCrossSpaceCompaction())) {
      initThreadPool();
      currentTaskNum = new AtomicInteger(0);
      candidateCompactionTaskQueue.regsitPollLastHook(
          AbstractCompactionTask::resetCompactionCandidateStatusForAllSourceFiles);
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `chunkWriters` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/writer/AbstractCompactionWriter.java`
#### Snippet
```java
    writeRateLimit(chunkWriters[subTaskId].estimateMaxSeriesMemSize());
    synchronized (targetWriter) {
      chunkWriters[subTaskId].writeToFileWriter(targetWriter);
    }
  }
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `lock` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/engine/modification/utils/TracedBufferedReader.java`
#### Snippet
```java
      throw new IllegalArgumentException("skip value is negative");
    }
    synchronized (lock) {
      ensureOpen();
      long r = n;
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `lock` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/engine/modification/utils/TracedBufferedReader.java`
#### Snippet
```java
    int startChar;

    synchronized (lock) {
      ensureOpen();
      boolean omitLF = ignoreLF || skipLF;
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `lock` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/engine/modification/utils/TracedBufferedReader.java`
#### Snippet
```java
  @Override
  public int read(char cbuf[], int off, int len) throws IOException {
    synchronized (lock) {
      ensureOpen();
      if ((off < 0)
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `lock` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/engine/modification/utils/TracedBufferedReader.java`
#### Snippet
```java
      throw new IllegalArgumentException("Read-ahead limit < 0");
    }
    synchronized (lock) {
      ensureOpen();
      this.readAheadLimit = readAheadLimit;
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `lock` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/engine/modification/utils/TracedBufferedReader.java`
#### Snippet
```java
  @Override
  public void close() throws IOException {
    synchronized (lock) {
      if (in == null) {
        return;
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `lock` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/engine/modification/utils/TracedBufferedReader.java`
#### Snippet
```java
  @Override
  public void reset() throws IOException {
    synchronized (lock) {
      ensureOpen();
      if (markedChar < 0) {
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `lock` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/engine/modification/utils/TracedBufferedReader.java`
#### Snippet
```java
  @Override
  public int read() throws IOException {
    synchronized (lock) {
      ensureOpen();
      for (; ; ) {
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `lock` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/engine/modification/utils/TracedBufferedReader.java`
#### Snippet
```java
  @Override
  public boolean ready() throws IOException {
    synchronized (lock) {
      ensureOpen();

```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `memCost` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TimePartitionManager.java`
#### Snippet
```java
    synchronized (timePartitionInfoMap) {
      timePartitionInfoMap.clear();
      memCost = 0;
    }
  }
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `memCost` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TimePartitionManager.java`
#### Snippet
```java
        if (timePartitionInfo != null) {
          timePartitionInfoMapForDataRegion.remove(partitionId);
          memCost -= timePartitionInfo.memSize;
        }
      }
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `memCost` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TimePartitionManager.java`
#### Snippet
```java
      if (timePartitionInfo != null) {
        timePartitionInfo.lastSystemFlushTime = systemFlushTime;
        memCost += memSize - timePartitionInfo.memSize;
        timePartitionInfo.memSize = memSize;
        timePartitionInfo.isActive = isActive;
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `memCost` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TimePartitionManager.java`
#### Snippet
```java
        timePartitionInfo.memSize = memSize;
        timePartitionInfo.isActive = isActive;
        if (memCost > timePartitionInfoMemoryThreshold) {
          evictOldPartition();
        }
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `timePartitionInfoMemoryThreshold` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TimePartitionManager.java`
#### Snippet
```java
        timePartitionInfo.memSize = memSize;
        timePartitionInfo.isActive = isActive;
        if (memCost > timePartitionInfoMemoryThreshold) {
          evictOldPartition();
        }
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `memCost` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TimePartitionManager.java`
#### Snippet
```java
      }

      while (memCost > timePartitionInfoMemoryThreshold) {
        TimePartitionInfo timePartitionInfo = treeSet.first();
        memCost -= timePartitionInfo.memSize;
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `timePartitionInfoMemoryThreshold` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TimePartitionManager.java`
#### Snippet
```java
      }

      while (memCost > timePartitionInfoMemoryThreshold) {
        TimePartitionInfo timePartitionInfo = treeSet.first();
        memCost -= timePartitionInfo.memSize;
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `memCost` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TimePartitionManager.java`
#### Snippet
```java
      while (memCost > timePartitionInfoMemoryThreshold) {
        TimePartitionInfo timePartitionInfo = treeSet.first();
        memCost -= timePartitionInfo.memSize;
        DataRegion dataRegion =
            StorageEngineV2.getInstance().getDataRegion(timePartitionInfo.dataRegionId);
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `pool` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/rescon/AbstractPoolManager.java`
#### Snippet
```java

  public synchronized <T> Future<T> submit(Callable<T> task) {
    return pool.submit(task);
  }

```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `pool` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/rescon/AbstractPoolManager.java`
#### Snippet
```java

  public synchronized Future<?> submit(Runnable task) {
    return pool.submit(task);
  }

```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `timeIndex` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileResource.java`
#### Snippet
```java
        fsFactory.getBufferedOutputStream(file + RESOURCE_SUFFIX + TEMP_SUFFIX)) {
      ReadWriteIOUtils.write(VERSION_NUMBER, outputStream);
      timeIndex.serialize(outputStream);

      ReadWriteIOUtils.write(maxPlanIndex, outputStream);
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `maxPlanIndex` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileResource.java`
#### Snippet
```java
      timeIndex.serialize(outputStream);

      ReadWriteIOUtils.write(maxPlanIndex, outputStream);
      ReadWriteIOUtils.write(minPlanIndex, outputStream);

```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `minPlanIndex` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileResource.java`
#### Snippet
```java

      ReadWriteIOUtils.write(maxPlanIndex, outputStream);
      ReadWriteIOUtils.write(minPlanIndex, outputStream);

      if (modFile != null && modFile.exists()) {
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `lastCacheValue` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/metadata/cache/lastCache/container/LastCacheContainer.java`
#### Snippet
```java
  @Override
  public synchronized void resetLastCache() {
    lastCacheValue = null;
  }

```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `lastCacheValue` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/metadata/cache/lastCache/container/LastCacheContainer.java`
#### Snippet
```java
    }

    if (lastCacheValue == null) {
      // If no cached last, (1) a last query (2) an unseq insertion or (3) a seq insertion will
      // update cache.
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `lastCacheValue` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/metadata/cache/lastCache/container/LastCacheContainer.java`
#### Snippet
```java
      // update cache.
      if (!highPriorityUpdate || latestFlushedTime <= timeValuePair.getTimestamp()) {
        lastCacheValue = new LastCacheValue(timeValuePair.getTimestamp(), timeValuePair.getValue());
      }
    } else if (timeValuePair.getTimestamp() > lastCacheValue.getTimestamp()
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `lastCacheValue` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/metadata/cache/lastCache/container/LastCacheContainer.java`
#### Snippet
```java
        lastCacheValue = new LastCacheValue(timeValuePair.getTimestamp(), timeValuePair.getValue());
      }
    } else if (timeValuePair.getTimestamp() > lastCacheValue.getTimestamp()
        || (timeValuePair.getTimestamp() == lastCacheValue.getTimestamp() && highPriorityUpdate)) {
      lastCacheValue.setTimestamp(timeValuePair.getTimestamp());
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `lastCacheValue` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/metadata/cache/lastCache/container/LastCacheContainer.java`
#### Snippet
```java
      }
    } else if (timeValuePair.getTimestamp() > lastCacheValue.getTimestamp()
        || (timeValuePair.getTimestamp() == lastCacheValue.getTimestamp() && highPriorityUpdate)) {
      lastCacheValue.setTimestamp(timeValuePair.getTimestamp());
      lastCacheValue.setValue(timeValuePair.getValue());
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `lastCacheValue` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/metadata/cache/lastCache/container/LastCacheContainer.java`
#### Snippet
```java
    } else if (timeValuePair.getTimestamp() > lastCacheValue.getTimestamp()
        || (timeValuePair.getTimestamp() == lastCacheValue.getTimestamp() && highPriorityUpdate)) {
      lastCacheValue.setTimestamp(timeValuePair.getTimestamp());
      lastCacheValue.setValue(timeValuePair.getValue());
    }
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `lastCacheValue` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/metadata/cache/lastCache/container/LastCacheContainer.java`
#### Snippet
```java
        || (timeValuePair.getTimestamp() == lastCacheValue.getTimestamp() && highPriorityUpdate)) {
      lastCacheValue.setTimestamp(timeValuePair.getTimestamp());
      lastCacheValue.setValue(timeValuePair.getValue());
    }
  }
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `children` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/metadata/mnode/InternalMNode.java`
#### Snippet
```java
      // double check, children is volatile
      synchronized (this) {
        if (children == null) {
          children = MNodeContainers.getNewMNodeContainer();
        }
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `children` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/metadata/mnode/InternalMNode.java`
#### Snippet
```java
      synchronized (this) {
        if (children == null) {
          children = MNodeContainers.getNewMNodeContainer();
        }
      }
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `children` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/metadata/mnode/InternalMNode.java`
#### Snippet
```java
    oldChildNode.moveDataToNewMNode(newChildNode);

    children.replace(newChildNode.getName(), newChildNode);
  }

```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `children` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/metadata/mnode/InternalMNode.java`
#### Snippet
```java
      // double check, children is volatile
      synchronized (this) {
        if (children == null) {
          children = MNodeContainers.getNewMNodeContainer();
        }
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `children` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/metadata/mnode/InternalMNode.java`
#### Snippet
```java
      synchronized (this) {
        if (children == null) {
          children = MNodeContainers.getNewMNodeContainer();
        }
      }
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `COMPOUND_POINT_LENGTH` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/InternalPage.java`
#### Snippet
```java
    synchronized (pageBuffer) {
      this.pageBuffer.limit(this.pageBuffer.capacity());
      this.pageBuffer.position(PAGE_HEADER_SIZE + index * COMPOUND_POINT_LENGTH);
      return ReadWriteIOUtils.readLong(this.pageBuffer);
    }
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `freeAddr` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/WrappedSegment.java`
#### Snippet
```java
    buf.clear();

    int recordStartAddr = freeAddr - buf.capacity();

    int newPairLength = pairLength + key.getBytes().length + 4 + 2;
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `pairLength` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/WrappedSegment.java`
#### Snippet
```java
    int recordStartAddr = freeAddr - buf.capacity();

    int newPairLength = pairLength + key.getBytes().length + 4 + 2;
    if (recordStartAddr < SchemaFileConfig.SEG_HEADER_SIZE + newPairLength) {
      return -1;
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `pairLength` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/WrappedSegment.java`
#### Snippet
```java
      return -1;
    }
    pairLength = (short) newPairLength;

    int tarIdx = binaryInsertPairList(keyAddressList, key);
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `keyAddressList` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/WrappedSegment.java`
#### Snippet
```java
    pairLength = (short) newPairLength;

    int tarIdx = binaryInsertPairList(keyAddressList, key);

    // update aliasKeyList
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `keyAddressList` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/WrappedSegment.java`
#### Snippet
```java
    this.buffer.position(recordStartAddr);
    this.buffer.put(buf);
    keyAddressList.add(tarIdx, new Pair<>(key, (short) recordStartAddr));
    this.freeAddr = (short) recordStartAddr;
    this.recordNum++;
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `this.freeAddr` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/WrappedSegment.java`
#### Snippet
```java
    this.buffer.put(buf);
    keyAddressList.add(tarIdx, new Pair<>(key, (short) recordStartAddr));
    this.freeAddr = (short) recordStartAddr;
    this.recordNum++;

```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `this.recordNum` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/WrappedSegment.java`
#### Snippet
```java
    keyAddressList.add(tarIdx, new Pair<>(key, (short) recordStartAddr));
    this.freeAddr = (short) recordStartAddr;
    this.recordNum++;

    penuKey = lastKey;
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `penuKey` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/WrappedSegment.java`
#### Snippet
```java
    this.recordNum++;

    penuKey = lastKey;
    lastKey = key;
    return recordStartAddr - pairLength - SchemaFileConfig.SEG_HEADER_SIZE;
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `lastKey` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/WrappedSegment.java`
#### Snippet
```java
    this.recordNum++;

    penuKey = lastKey;
    lastKey = key;
    return recordStartAddr - pairLength - SchemaFileConfig.SEG_HEADER_SIZE;
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `lastKey` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/WrappedSegment.java`
#### Snippet
```java

    penuKey = lastKey;
    lastKey = key;
    return recordStartAddr - pairLength - SchemaFileConfig.SEG_HEADER_SIZE;
  }
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `pairLength` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/WrappedSegment.java`
#### Snippet
```java
    penuKey = lastKey;
    lastKey = key;
    return recordStartAddr - pairLength - SchemaFileConfig.SEG_HEADER_SIZE;
  }

```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `pageIndex` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/SegmentedPage.java`
#### Snippet
```java

    if (newSeg == null) {
      throw new SchemaPageOverflowException(pageIndex);
    }

```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `memberNum` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/SegmentedPage.java`
#### Snippet
```java
    }

    pageBuffer.position(SchemaFileConfig.PAGE_LENGTH - memberNum * SchemaFileConfig.SEG_OFF_DIG);
    for (short offset : segOffsetLst) {
      ReadWriteIOUtils.write(offset, pageBuffer);
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `spareSize` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/SegmentedPage.java`
#### Snippet
```java
  @Override
  public synchronized void deleteSegment(short segId) throws SegmentNotFoundException {
    spareSize += getSegmentSize(segId);
    getSegment(segId).delete();
    segCacheMap.remove(segId);
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `pageIndex` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/SegmentedPage.java`
#### Snippet
```java
    if (segCacheMap.containsKey(thisIndex)) {
      throw new MetadataException(
          String.format("Segment cache map inconsistent with segment list in page %d.", pageIndex));
    }

```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `spareOffset` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/SegmentedPage.java`
#### Snippet
```java

    segCacheMap.put(thisIndex, seg);
    segOffsetLst.add(spareOffset);

    spareOffset += seg.size();
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `spareOffset` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/SegmentedPage.java`
#### Snippet
```java
    segOffsetLst.add(spareOffset);

    spareOffset += seg.size();
    spareSize -= seg.size() + 2;
    memberNum += 1;
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `spareSize` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/SegmentedPage.java`
#### Snippet
```java

    spareOffset += seg.size();
    spareSize -= seg.size() + 2;
    memberNum += 1;

```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `memberNum` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/SegmentedPage.java`
#### Snippet
```java
    spareOffset += seg.size();
    spareSize -= seg.size() + 2;
    memberNum += 1;

    return thisIndex;
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `spareOffset` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/SegmentedPage.java`
#### Snippet
```java
    this.pageBuffer.clear();

    spareOffset = SchemaFileConfig.PAGE_HEADER_SIZE;

    short offset, len;
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `spareOffset` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/SegmentedPage.java`
#### Snippet
```java
        mirrorPage.limit(offset + len);

        this.segOffsetLst.set(i, spareOffset);
        this.pageBuffer.position(spareOffset);
        this.pageBuffer.put(mirrorPage);
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `spareOffset` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/SegmentedPage.java`
#### Snippet
```java

        this.segOffsetLst.set(i, spareOffset);
        this.pageBuffer.position(spareOffset);
        this.pageBuffer.put(mirrorPage);
        spareOffset += len;
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `spareOffset` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/SegmentedPage.java`
#### Snippet
```java
        this.pageBuffer.position(spareOffset);
        this.pageBuffer.put(mirrorPage);
        spareOffset += len;
      }
    }
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `spareOffset` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/SegmentedPage.java`
#### Snippet
```java
    if (idx >= 0) {
      this.pageBuffer.clear();
      this.pageBuffer.position(spareOffset);

      mirrorPage.clear();
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `spareOffset` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/SegmentedPage.java`
#### Snippet
```java

      this.pageBuffer.put(mirrorPage);
      segOffsetLst.set(idx, spareOffset);
      spareOffset += len;
    }
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `spareOffset` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/SegmentedPage.java`
#### Snippet
```java
      this.pageBuffer.put(mirrorPage);
      segOffsetLst.set(idx, spareOffset);
      spareOffset += len;
    }

```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `spareSize` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/SegmentedPage.java`
#### Snippet
```java
    }

    spareSize =
        (short)
            (this.pageBuffer.capacity() - spareOffset - memberNum * SchemaFileConfig.SEG_OFF_DIG);
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `spareOffset` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/SegmentedPage.java`
#### Snippet
```java
    spareSize =
        (short)
            (this.pageBuffer.capacity() - spareOffset - memberNum * SchemaFileConfig.SEG_OFF_DIG);
  }

```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `memberNum` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/SegmentedPage.java`
#### Snippet
```java
    spareSize =
        (short)
            (this.pageBuffer.capacity() - spareOffset - memberNum * SchemaFileConfig.SEG_OFF_DIG);
  }

```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `idTableMap` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/metadata/idtable/IDTableManager.java`
#### Snippet
```java
  public synchronized IMeasurementSchema getSeriesSchema(String deviceName, String measurementName)
      throws MetadataException {
    for (IDTable idTable : idTableMap.values()) {
      IMeasurementSchema measurementSchema = idTable.getSeriesSchema(deviceName, measurementName);
      if (measurementSchema != null) {
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `idTableMap` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/metadata/idtable/IDTableManager.java`
#### Snippet
```java
  public synchronized IDTable getIDTable(PartialPath devicePath) {
    try {
      return idTableMap.computeIfAbsent(
          IoTDB.schemaProcessor.getStorageGroupNodeByPath(devicePath).getFullPath(),
          storageGroupPath ->
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `logger` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/metadata/idtable/IDTableManager.java`
#### Snippet
```java
                      systemDir + File.separator + storageGroupPath)));
    } catch (MetadataException e) {
      logger.error("get id table failed, path is: " + devicePath + ". caused by: " + e);
    }

```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `idTableMap` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/metadata/idtable/IDTableManager.java`
#### Snippet
```java
   */
  public synchronized IDTable getIDTableDirectly(String sgPath) {
    return idTableMap.computeIfAbsent(
        sgPath,
        storageGroupPath ->
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `database2Measurement2TagOrders` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/meta/AbstractInfluxDBMetaManager.java`
#### Snippet
```java
    if (newTagInfoRecords != null) {
      updateTagInfoRecords(newTagInfoRecords, sessionID);
      database2Measurement2TagOrders.get(database).put(measurement, newTagKeyToLayerOrders);
    }

```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `database2Measurement2TagOrders` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/meta/AbstractInfluxDBMetaManager.java`
#### Snippet
```java
      String database, long sessionID) {
    Map<String, Map<String, Integer>> measurement2TagOrders =
        database2Measurement2TagOrders.get(database);
    if (measurement2TagOrders != null) {
      return measurement2TagOrders;
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `database2Measurement2TagOrders` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/meta/AbstractInfluxDBMetaManager.java`
#### Snippet
```java
    setStorageGroup(database, sessionID);
    measurement2TagOrders = new HashMap<>();
    database2Measurement2TagOrders.put(database, measurement2TagOrders);
    return measurement2TagOrders;
  }
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `config` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/SchemaRegionMemoryImpl.java`
#### Snippet
```java
              storageGroupMNode, tagManager::readTags, schemaRegionId.getId());

      if (!(config.isClusterMode()
          && config
              .getSchemaRegionConsensusProtocolClass()
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `config` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/SchemaRegionMemoryImpl.java`
#### Snippet
```java

      if (!(config.isClusterMode()
          && config
              .getSchemaRegionConsensusProtocolClass()
              .equals(ConsensusFactory.RATIS_CONSENSUS))) {
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `config` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/SchemaRegionSchemaFileImpl.java`
#### Snippet
```java
              storageGroupMNode, tagManager::readTags, schemaRegionId.getId());

      if (!(config.isClusterMode()
          && config
              .getSchemaRegionConsensusProtocolClass()
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `config` accessed in synchronized context
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/SchemaRegionSchemaFileImpl.java`
#### Snippet
```java

      if (!(config.isClusterMode()
          && config
              .getSchemaRegionConsensusProtocolClass()
              .equals(ConsensusFactory.RATIS_CONSENSUS))) {
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `cache` accessed in synchronized context
in `tsfile/src/main/java/org/apache/iotdb/tsfile/common/cache/LRUCache.java`
#### Snippet
```java
  @Override
  public synchronized T get(K key) throws IOException {
    if (cache.containsKey(key)) {
      return cache.get(key);
    } else {
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `cache` accessed in synchronized context
in `tsfile/src/main/java/org/apache/iotdb/tsfile/common/cache/LRUCache.java`
#### Snippet
```java
  public synchronized T get(K key) throws IOException {
    if (cache.containsKey(key)) {
      return cache.get(key);
    } else {
      T value = loadObjectByKey(key);
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `cache` accessed in synchronized context
in `tsfile/src/main/java/org/apache/iotdb/tsfile/common/cache/LRUCache.java`
#### Snippet
```java
      T value = loadObjectByKey(key);
      if (value != null) {
        cache.put(key, value);
      }
      return value;
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `cache` accessed in synchronized context
in `tsfile/src/main/java/org/apache/iotdb/tsfile/common/cache/LRUCache.java`
#### Snippet
```java

  public synchronized void removeItem(K key) {
    cache.remove(key);
  }
}
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `cache` accessed in synchronized context
in `tsfile/src/main/java/org/apache/iotdb/tsfile/common/cache/LRUCache.java`
#### Snippet
```java

  public synchronized void put(K key, T value) {
    cache.put(key, value);
  }

```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `cache` accessed in synchronized context
in `tsfile/src/main/java/org/apache/iotdb/tsfile/common/cache/LRUCache.java`
#### Snippet
```java
  @Override
  public synchronized void clear() {
    cache.clear();
  }

```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `tsFileMetaData` accessed in synchronized context
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/TsFileSequenceReader.java`
#### Snippet
```java
      if (tsFileMetaData == null) {
        synchronized (this) {
          if (tsFileMetaData == null) {
            tsFileMetaData =
                TsFileMetadata.deserializeFrom(readData(fileMetadataPos, fileMetadataSize));
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `tsFileMetaData` accessed in synchronized context
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/TsFileSequenceReader.java`
#### Snippet
```java
        synchronized (this) {
          if (tsFileMetaData == null) {
            tsFileMetaData =
                TsFileMetadata.deserializeFrom(readData(fileMetadataPos, fileMetadataSize));
          }
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `fileMetadataPos` accessed in synchronized context
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/TsFileSequenceReader.java`
#### Snippet
```java
          if (tsFileMetaData == null) {
            tsFileMetaData =
                TsFileMetadata.deserializeFrom(readData(fileMetadataPos, fileMetadataSize));
          }
        }
```

### RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
Non-private field `fileMetadataSize` accessed in synchronized context
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/TsFileSequenceReader.java`
#### Snippet
```java
          if (tsFileMetaData == null) {
            tsFileMetaData =
                TsFileMetadata.deserializeFrom(readData(fileMetadataPos, fileMetadataSize));
          }
        }
```

## RuleId[ruleID=RedundantSuppression]
### RuleId[ruleID=RedundantSuppression]
Redundant suppression
in `server/src/main/java/org/apache/iotdb/db/metadata/mnode/container/MNodeContainers.java`
#### Snippet
```java

  @SuppressWarnings("rawtypes")
  private static final IMNodeContainer EMPTY_CONTAINER = new EmptyContainer();

  @SuppressWarnings("unchecked")
```

### RuleId[ruleID=RedundantSuppression]
Redundant suppression
in `server/src/main/java/org/apache/iotdb/db/metadata/mnode/container/MNodeContainers.java`
#### Snippet
```java

  @SuppressWarnings("unchecked")
  public static IMNodeContainer emptyMNodeContainer() {
    return EMPTY_CONTAINER;
  }
```

### RuleId[ruleID=RedundantSuppression]
Redundant suppression
in `server/src/main/java/org/apache/iotdb/db/query/reader/universal/PriorityMergeReader.java`
#### Snippet
```java
/** This class implements {@link IPointReader} for data sources with different priorities. */
@SuppressWarnings("ConstantConditions") // heap is ensured by hasNext non-empty
public class PriorityMergeReader implements IPointReader {

  // max time of all added readers in PriorityMergeReader
```

### RuleId[ruleID=RedundantSuppression]
Redundant suppression
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileSplitByPartitionTool.java`
#### Snippet
```java
   */
  @SuppressWarnings({"squid:S3776", "deprecation"}) // Suppress high Cognitive Complexity warning
  public void parseAndRewriteFile(List<TsFileResource> rewrittenResources)
      throws IOException, WriteProcessException, IllegalPathException {
    // check if the TsFile has correct header
```

## RuleId[ruleID=UtilityClassWithPublicConstructor]
### RuleId[ruleID=UtilityClassWithPublicConstructor]
Class `AbstractTsFileTool` has only 'static' members, and a 'public' constructor
in `cli/src/main/java/org/apache/iotdb/tool/AbstractTsFileTool.java`
#### Snippet
```java
import java.io.IOException;

public abstract class AbstractTsFileTool {

  protected static final String HOST_ARGS = "h";
```

### RuleId[ruleID=UtilityClassWithPublicConstructor]
Class `AbstractCsvTool` has only 'static' members, and a 'public' constructor
in `cli/src/main/java/org/apache/iotdb/tool/AbstractCsvTool.java`
#### Snippet
```java
import java.util.List;

public abstract class AbstractCsvTool {

  protected static final String HOST_ARGS = "h";
```

### RuleId[ruleID=UtilityClassWithPublicConstructor]
Class `SQLConstant` has only 'static' members, and a 'public' constructor
in `server/src/main/java/org/apache/iotdb/db/qp/constant/SQLConstant.java`
#### Snippet
```java
/** this class contains several constants used in SQL. */
// some fields are for future features
public class SQLConstant {

  public SQLConstant() throws InstantiationException {
```

## RuleId[ruleID=OptionalUsedAsFieldOrParameterType]
### RuleId[ruleID=OptionalUsedAsFieldOrParameterType]
`Optional` used as type for parameter 'valueIsNull'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/block/column/DoubleColumn.java`
#### Snippet
```java
  private final long retainedSizeInBytes;

  public DoubleColumn(int positionCount, Optional<boolean[]> valueIsNull, double[] values) {
    this(0, positionCount, valueIsNull.orElse(null), values);
  }
```

### RuleId[ruleID=OptionalUsedAsFieldOrParameterType]
`Optional` used as type for parameter 'valueIsNull'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/block/column/IntColumn.java`
#### Snippet
```java
  private final long retainedSizeInBytes;

  public IntColumn(int positionCount, Optional<boolean[]> valueIsNull, int[] values) {
    this(0, positionCount, valueIsNull.orElse(null), values);
  }
```

### RuleId[ruleID=OptionalUsedAsFieldOrParameterType]
`Optional` used as type for parameter 'valueIsNull'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/block/column/BooleanColumn.java`
#### Snippet
```java
  private final long retainedSizeInBytes;

  public BooleanColumn(int positionCount, Optional<boolean[]> valueIsNull, boolean[] values) {
    this(0, positionCount, valueIsNull.orElse(null), values);
  }
```

### RuleId[ruleID=OptionalUsedAsFieldOrParameterType]
`Optional` used as type for parameter 'valueIsNull'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/block/column/BinaryColumn.java`
#### Snippet
```java
  private final long retainedSizeInBytes;

  public BinaryColumn(int positionCount, Optional<boolean[]> valueIsNull, Binary[] values) {
    this(0, positionCount, valueIsNull.orElse(null), values);
  }
```

### RuleId[ruleID=OptionalUsedAsFieldOrParameterType]
`Optional` used as type for parameter 'valueIsNull'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/block/column/FloatColumn.java`
#### Snippet
```java
  private final long retainedSizeInBytes;

  public FloatColumn(int positionCount, Optional<boolean[]> valueIsNull, float[] values) {
    this(0, positionCount, valueIsNull.orElse(null), values);
  }
```

### RuleId[ruleID=OptionalUsedAsFieldOrParameterType]
`Optional` used as type for parameter 'valueIsNull'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/block/column/LongColumn.java`
#### Snippet
```java
  private final long retainedSizeInBytes;

  public LongColumn(int positionCount, Optional<boolean[]> valueIsNull, long[] values) {
    this(0, positionCount, valueIsNull.orElse(null), values);
  }
```

## RuleId[ruleID=NonStrictComparisonCanBeEquality]
### RuleId[ruleID=NonStrictComparisonCanBeEquality]
Can be replaced with equality
in `node-commons/src/main/java/org/apache/iotdb/commons/path/PartialPath.java`
#### Snippet
```java

  public String getTailNode() {
    if (nodes.length <= 0) {
      return "";
    }
```

### RuleId[ruleID=NonStrictComparisonCanBeEquality]
Can be replaced with equality
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/PipeOpManager.java`
#### Snippet
```java
   */
  private void commitFilePipe(List<Long> filePipeSerialNumberList) {
    if (filePipeSerialNumberList.size() <= 0) {
      return;
    }
```

### RuleId[ruleID=NonStrictComparisonCanBeEquality]
Can be replaced with equality
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/PipeOpManager.java`
#### Snippet
```java
    }

    if (filePipeSerialNumberSet.size() <= 0) {
      logger.error("commitFilePipe(), filePipeSerialNumberSet should not be empty.");
      return;
```

### RuleId[ruleID=NonStrictComparisonCanBeEquality]
Can be replaced with equality
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/PipeOpManager.java`
#### Snippet
```java
   */
  public boolean isEmpty() {
    return (filePipeSerialNumberSet.size() <= 0);
  }

```

### RuleId[ruleID=NonStrictComparisonCanBeEquality]
Can be replaced with equality
in `server/src/main/java/org/apache/iotdb/db/sync/externalpipe/ExtPipePluginManager.java`
#### Snippet
```java
    extPipePluginMap.remove(pipeTypeName);

    if (extPipePluginMap.size() <= 0) {
      stopAllThreadPool();

```

### RuleId[ruleID=NonStrictComparisonCanBeEquality]
Can be replaced with equality
in `server/src/main/java/org/apache/iotdb/db/sync/externalpipe/ExtPipePluginManager.java`
#### Snippet
```java
    alive = true;
    ThreadPoolExecutor tpe = ((ThreadPoolExecutor) monitorService);
    if ((tpe.getActiveCount() <= 0) && (tpe.getQueue().size() <= 0)) {
      monitorService.submit(this::monitorPipeData);
    }
```

### RuleId[ruleID=NonStrictComparisonCanBeEquality]
Can be replaced with equality
in `server/src/main/java/org/apache/iotdb/db/engine/trigger/sink/mqtt/MQTTHandler.java`
#### Snippet
```java

  private static String arrayToJson(Object[] array) throws SinkException {
    if (array.length <= 0) {
      throw new SinkException("The number of measurements should be positive.");
    }
```

### RuleId[ruleID=NonStrictComparisonCanBeEquality]
Can be replaced with equality
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/RSchemaRegion.java`
#### Snippet
```java
              if (didAnyUpdate) {
                try (WriteBatch batch = new WriteBatch()) {
                  if (tagLen > 0 && mNode.getTags().size() <= 0) {
                    batch.delete(
                        readWriteHandler.getColumnFamilyHandleByName(TABLE_NAME_TAGS), key);
```

## RuleId[ruleID=SystemOutErr]
### RuleId[ruleID=SystemOutErr]
Uses of `System.err` should probably be replaced with more robust logging
in `node-commons/src/main/java/org/apache/iotdb/commons/ServerCommandLine.java`
#### Snippet
```java
  protected void usage(String message) {
    if (message != null) {
      System.err.println(message);
      System.err.println();
    }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.err` should probably be replaced with more robust logging
in `node-commons/src/main/java/org/apache/iotdb/commons/ServerCommandLine.java`
#### Snippet
```java
    if (message != null) {
      System.err.println(message);
      System.err.println();
    }

```

### RuleId[ruleID=SystemOutErr]
Uses of `System.err` should probably be replaced with more robust logging
in `node-commons/src/main/java/org/apache/iotdb/commons/ServerCommandLine.java`
#### Snippet
```java
    }

    System.err.println(getUsage());
  }

```

### RuleId[ruleID=SystemOutErr]
Uses of `System.err` should probably be replaced with more robust logging
in `node-commons/src/main/java/org/apache/iotdb/commons/conf/IoTDBConstant.java`
#### Snippet
```java
      }
    } catch (Exception e) {
      System.err.println("get git.properties error: " + e.getMessage());
    }
    BUILD_INFO = finalBuildInfo;
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/rest-java-example/src/main/java/org/apache/iotdb/HttpExample.java`
#### Snippet
```java
      String message = EntityUtils.toString(responseEntity, "utf-8");
      JsonObject result = JsonParser.parseString(message).getAsJsonObject();
      System.out.println(result);
    } catch (IOException e) {
      e.printStackTrace();
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/rest-java-example/src/main/java/org/apache/iotdb/HttpExample.java`
#### Snippet
```java
      String message = EntityUtils.toString(responseEntity, "utf-8");
      JsonObject result = JsonParser.parseString(message).getAsJsonObject();
      System.out.println(result);
    } catch (IOException e) {
      e.printStackTrace();
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/rest-java-example/src/main/java/org/apache/iotdb/HttpExample.java`
#### Snippet
```java
      ObjectMapper mapper = new ObjectMapper();
      Map map = mapper.readValue(message, Map.class);
      System.out.println(map);
    } catch (IOException e) {
      e.printStackTrace();
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/rest-java-example/src/main/java/org/apache/iotdb/HttpsExample.java`
#### Snippet
```java
      ObjectMapper mapper = new ObjectMapper();
      Map map = mapper.readValue(message, Map.class);
      System.out.println(map);
    } catch (IOException e) {
      e.printStackTrace();
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/rest-java-example/src/main/java/org/apache/iotdb/HttpsExample.java`
#### Snippet
```java
      String message = EntityUtils.toString(responseEntity, "utf-8");
      JsonObject result = JsonParser.parseString(message).getAsJsonObject();
      System.out.println(result);
    } catch (IOException e) {
      e.printStackTrace();
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/rest-java-example/src/main/java/org/apache/iotdb/HttpsExample.java`
#### Snippet
```java
      String message = EntityUtils.toString(responseEntity, "utf-8");
      JsonObject result = JsonParser.parseString(message).getAsJsonObject();
      System.out.println(result);

    } catch (IOException e) {
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/cli/utils/IoTPrinter.java`
#### Snippet
```java

public class IoTPrinter {
  private static final PrintStream SCREEN_PRINTER = new PrintStream(System.out);

  public static void printf(String format, Object... args) {
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/AbstractTsFileTool.java`
#### Snippet
```java
    if (str == null) {
      String msg = String.format("Required values for option '%s' not provided", name);
      System.out.println(msg);
      System.out.println("Use -help for more information");
      throw new ArgsErrorException(msg);
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/AbstractTsFileTool.java`
#### Snippet
```java
      String msg = String.format("Required values for option '%s' not provided", name);
      System.out.println(msg);
      System.out.println("Use -help for more information");
      throw new ArgsErrorException(msg);
    }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/AbstractCsvTool.java`
#### Snippet
```java
      }
    }
    System.out.printf(
        "Input time format %s is not supported, "
            + "please input like yyyy-MM-dd\\ HH:mm:ss.SSS or yyyy-MM-dd'T'HH:mm:ss.SSS%n",
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/AbstractCsvTool.java`
#### Snippet
```java
    if (str == null) {
      String msg = String.format("Required values for option '%s' not provided", name);
      System.out.println(msg);
      System.out.println("Use -help for more information");
      throw new ArgsErrorException(msg);
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/AbstractCsvTool.java`
#### Snippet
```java
      String msg = String.format("Required values for option '%s' not provided", name);
      System.out.println(msg);
      System.out.println("Use -help for more information");
      throw new ArgsErrorException(msg);
    }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/ExportCsv.java`
#### Snippet
```java
      writeCsvFile(sessionDataSet, path, headers, linesPerFile);
      sessionDataSet.closeOperationHandle();
      System.out.println("Export completely!");
    } catch (StatementExecutionException | IoTDBConnectionException | IOException e) {
      System.out.println("Cannot dump result because: " + e.getMessage());
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/ExportCsv.java`
#### Snippet
```java
      System.out.println("Export completely!");
    } catch (StatementExecutionException | IoTDBConnectionException | IOException e) {
      System.out.println("Cannot dump result because: " + e.getMessage());
    }
  }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/ExportCsv.java`
#### Snippet
```java

    if (args == null || args.length == 0) {
      System.out.println("Too few params input, please check the following hint.");
      hf.printHelp(TSFILEDB_CLI_PREFIX, options, true);
      System.exit(CODE_ERROR);
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/ExportCsv.java`
#### Snippet
```java
      commandLine = parser.parse(options, args);
    } catch (ParseException e) {
      System.out.println(e.getMessage());
      hf.printHelp(TSFILEDB_CLI_PREFIX, options, true);
      System.exit(CODE_ERROR);
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/ExportCsv.java`
#### Snippet
```java
          LineReader lineReader = JlineUtils.getLineReader(username, host, port);
          sql = lineReader.readLine(TSFILEDB_CLI_PREFIX + "> please input query: ");
          System.out.println(sql);
          String[] values = sql.trim().split(";");
          for (int i = 0; i < values.length; i++) {
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/ExportCsv.java`
#### Snippet
```java

    } catch (IOException e) {
      System.out.println("Failed to operate on file, because " + e.getMessage());
      exitCode = CODE_ERROR;
    } catch (ArgsErrorException e) {
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/ExportCsv.java`
#### Snippet
```java
      exitCode = CODE_ERROR;
    } catch (ArgsErrorException e) {
      System.out.println("Invalid args: " + e.getMessage());
      exitCode = CODE_ERROR;
    } catch (IoTDBConnectionException | StatementExecutionException e) {
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/ExportCsv.java`
#### Snippet
```java
      exitCode = CODE_ERROR;
    } catch (IoTDBConnectionException | StatementExecutionException e) {
      System.out.println("Connect failed because " + e.getMessage());
      exitCode = CODE_ERROR;
    } catch (TException e) {
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/ExportCsv.java`
#### Snippet
```java
      exitCode = CODE_ERROR;
    } catch (TException e) {
      System.out.println(
          "Can not get the timestamp precision from server because " + e.getMessage());
      exitCode = CODE_ERROR;
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/ExportCsv.java`
#### Snippet
```java
        } catch (IoTDBConnectionException e) {
          exitCode = CODE_ERROR;
          System.out.println(
              "Encounter an error when closing session, error is: " + e.getMessage());
        }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/ExportTsFile.java`
#### Snippet
```java
      }
      if (tabletList.isEmpty()) {
        System.out.println("!!!Warning:Tablet is empty,no data can be exported.");
        System.exit(CODE_ERROR);
      }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/ExportTsFile.java`
#### Snippet
```java
      long end = System.currentTimeMillis();
      sessionDataSet.closeOperationHandle();
      System.out.println("Export completely!cost:" + (end - start) + "ms.");
    } catch (StatementExecutionException
        | IoTDBConnectionException
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/ExportTsFile.java`
#### Snippet
```java
        | IOException
        | WriteProcessException e) {
      System.out.println("Cannot dump result because: " + e.getMessage());
    }
  }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/ExportTsFile.java`
#### Snippet
```java
        || sqlLower.contains("max_time(")
        || sqlLower.contains("min_time(")) {
      System.out.println("The sql you entered is invalid, please don't use aggregate query.");
      System.exit(CODE_ERROR);
    }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/ExportTsFile.java`
#### Snippet
```java

    if (args == null || args.length == 0) {
      System.out.println("Too few params input, please check the following hint.");
      hf.printHelp(TSFILEDB_CLI_PREFIX, options, true);
      System.exit(CODE_ERROR);
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/ExportTsFile.java`
#### Snippet
```java
      commandLine = parser.parse(options, args);
    } catch (ParseException e) {
      System.out.println(e.getMessage());
      hf.printHelp(TSFILEDB_CLI_PREFIX, options, true);
      System.exit(CODE_ERROR);
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/ExportTsFile.java`
#### Snippet
```java
          LineReader lineReader = JlineUtils.getLineReader(username, host, port);
          sql = lineReader.readLine(TSFILEDB_CLI_PREFIX + "> please input query: ");
          System.out.println(sql);
          String[] values = sql.trim().split(";");
          for (int i = 0; i < values.length; i++) {
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/ExportTsFile.java`
#### Snippet
```java

    } catch (IOException e) {
      System.out.println("Failed to operate on file, because " + e.getMessage());
      exitCode = CODE_ERROR;
    } catch (ArgsErrorException e) {
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/ExportTsFile.java`
#### Snippet
```java
      exitCode = CODE_ERROR;
    } catch (ArgsErrorException e) {
      System.out.println("Invalid args: " + e.getMessage());
      exitCode = CODE_ERROR;
    } catch (IoTDBConnectionException e) {
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/ExportTsFile.java`
#### Snippet
```java
      exitCode = CODE_ERROR;
    } catch (IoTDBConnectionException e) {
      System.out.println("Connect failed because " + e.getMessage());
      exitCode = CODE_ERROR;
    } finally {
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/ExportTsFile.java`
#### Snippet
```java
        } catch (IoTDBConnectionException e) {
          exitCode = CODE_ERROR;
          System.out.println(
              "Encounter an error when closing session, error is: " + e.getMessage());
        }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileWriteWithTSRecord.java`
#### Snippet
```java
    } catch (Throwable e) {
      e.printStackTrace();
      System.out.println(e.getMessage());
    }
  }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileRead.java`
#### Snippet
```java
    QueryDataSet queryDataSet = readTsFile.query(queryExpression);
    while (queryDataSet.hasNext()) {
      System.out.println(queryDataSet.next());
    }
    System.out.println("----------------");
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileRead.java`
#### Snippet
```java
      System.out.println(queryDataSet.next());
    }
    System.out.println("----------------");
  }

```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileSequenceRead.java`
#### Snippet
```java
    }
    try (TsFileSequenceReader reader = new TsFileSequenceReader(filename)) {
      System.out.println(
          "file length: " + FSFactoryProducer.getFSFactory().getFile(filename).length());
      System.out.println("file magic head: " + reader.readHeadMagic());
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileSequenceRead.java`
#### Snippet
```java
      System.out.println(
          "file length: " + FSFactoryProducer.getFSFactory().getFile(filename).length());
      System.out.println("file magic head: " + reader.readHeadMagic());
      System.out.println("file magic tail: " + reader.readTailMagic());
      System.out.println("Level 1 metadata position: " + reader.getFileMetadataPos());
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileSequenceRead.java`
#### Snippet
```java
          "file length: " + FSFactoryProducer.getFSFactory().getFile(filename).length());
      System.out.println("file magic head: " + reader.readHeadMagic());
      System.out.println("file magic tail: " + reader.readTailMagic());
      System.out.println("Level 1 metadata position: " + reader.getFileMetadataPos());
      System.out.println("Level 1 metadata size: " + reader.getTsFileMetadataSize());
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileSequenceRead.java`
#### Snippet
```java
      System.out.println("file magic head: " + reader.readHeadMagic());
      System.out.println("file magic tail: " + reader.readTailMagic());
      System.out.println("Level 1 metadata position: " + reader.getFileMetadataPos());
      System.out.println("Level 1 metadata size: " + reader.getTsFileMetadataSize());
      // Sequential reading of one ChunkGroup now follows this order:
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileSequenceRead.java`
#### Snippet
```java
      System.out.println("file magic tail: " + reader.readTailMagic());
      System.out.println("Level 1 metadata position: " + reader.getFileMetadataPos());
      System.out.println("Level 1 metadata size: " + reader.getTsFileMetadataSize());
      // Sequential reading of one ChunkGroup now follows this order:
      // first the CHUNK_GROUP_HEADER, then SeriesChunks (headers and data) in one ChunkGroup
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileSequenceRead.java`
#### Snippet
```java
      // marker) ahead and judge accordingly.
      reader.position((long) TSFileConfig.MAGIC_STRING.getBytes().length + 1);
      System.out.println("position: " + reader.position());
      List<long[]> timeBatch = new ArrayList<>();
      int pageIndex = 0;
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileSequenceRead.java`
#### Snippet
```java
          case MetaMarker.ONLY_ONE_PAGE_TIME_CHUNK_HEADER:
          case MetaMarker.ONLY_ONE_PAGE_VALUE_CHUNK_HEADER:
            System.out.println("\t[Chunk]");
            System.out.println("\tchunk type: " + marker);
            System.out.println("\tposition: " + reader.position());
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileSequenceRead.java`
#### Snippet
```java
          case MetaMarker.ONLY_ONE_PAGE_VALUE_CHUNK_HEADER:
            System.out.println("\t[Chunk]");
            System.out.println("\tchunk type: " + marker);
            System.out.println("\tposition: " + reader.position());
            ChunkHeader header = reader.readChunkHeader(marker);
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileSequenceRead.java`
#### Snippet
```java
            System.out.println("\t[Chunk]");
            System.out.println("\tchunk type: " + marker);
            System.out.println("\tposition: " + reader.position());
            ChunkHeader header = reader.readChunkHeader(marker);
            System.out.println("\tMeasurement: " + header.getMeasurementID());
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileSequenceRead.java`
#### Snippet
```java
            System.out.println("\tposition: " + reader.position());
            ChunkHeader header = reader.readChunkHeader(marker);
            System.out.println("\tMeasurement: " + header.getMeasurementID());
            if (header.getDataSize() == 0) {
              // empty value chunk
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileSequenceRead.java`
#### Snippet
```java
            if (header.getDataSize() == 0) {
              // empty value chunk
              System.out.println("\t-- Empty Chunk ");
              break;
            }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileSequenceRead.java`
#### Snippet
```java
              break;
            }
            System.out.println(
                "\tChunk Size: " + (header.getDataSize() + header.getSerializedSize()));
            Decoder defaultTimeDecoder =
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileSequenceRead.java`
#### Snippet
```java
            while (dataSize > 0) {
              valueDecoder.reset();
              System.out.println(
                  "\t\t[Page" + pageIndex + "]\n \t\tPage head position: " + reader.position());
              PageHeader pageHeader =
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileSequenceRead.java`
#### Snippet
```java
                      header.getDataType(),
                      (header.getChunkType() & 0x3F) == MetaMarker.CHUNK_HEADER);
              System.out.println("\t\tPage data position: " + reader.position());
              ByteBuffer pageData = reader.readPage(pageHeader, header.getCompressionType());
              System.out.println(
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileSequenceRead.java`
#### Snippet
```java
              System.out.println("\t\tPage data position: " + reader.position());
              ByteBuffer pageData = reader.readPage(pageHeader, header.getCompressionType());
              System.out.println(
                  "\t\tUncompressed page data size: " + pageHeader.getUncompressedSize());
              System.out.println(
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileSequenceRead.java`
#### Snippet
```java
              System.out.println(
                  "\t\tUncompressed page data size: " + pageHeader.getUncompressedSize());
              System.out.println(
                  "\t\tCompressed page data size: " + pageHeader.getCompressedSize());
              if ((header.getChunkType() & (byte) TsFileConstant.TIME_COLUMN_MASK)
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileSequenceRead.java`
#### Snippet
```java
                    new TimePageReader(pageHeader, pageData, defaultTimeDecoder);
                timeBatch.add(timePageReader.getNextTimeBatch());
                System.out.println("\t\tpoints in the page: " + timeBatch.get(pageIndex).length);
                if (printDetail) {
                  for (int i = 0; i < timeBatch.get(pageIndex).length; i++) {
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileSequenceRead.java`
#### Snippet
```java
                if (printDetail) {
                  for (int i = 0; i < timeBatch.get(pageIndex).length; i++) {
                    System.out.println("\t\t\ttime: " + timeBatch.get(pageIndex)[i]);
                  }
                }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileSequenceRead.java`
#### Snippet
```java
                    valuePageReader.nextValueBatch(timeBatch.get(pageIndex));
                if (valueBatch.length == 0) {
                  System.out.println("\t\t-- Empty Page ");
                } else {
                  System.out.println("\t\tpoints in the page: " + valueBatch.length);
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileSequenceRead.java`
#### Snippet
```java
                  System.out.println("\t\t-- Empty Page ");
                } else {
                  System.out.println("\t\tpoints in the page: " + valueBatch.length);
                }
                if (printDetail) {
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileSequenceRead.java`
#### Snippet
```java
                if (printDetail) {
                  for (TsPrimitiveType batch : valueBatch) {
                    System.out.println("\t\t\tvalue: " + batch);
                  }
                }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileSequenceRead.java`
#### Snippet
```java
                BatchData batchData = pageReader.getAllSatisfiedPageData();
                if (header.getChunkType() == MetaMarker.CHUNK_HEADER) {
                  System.out.println("\t\tpoints in the page: " + pageHeader.getNumOfValues());
                } else {
                  System.out.println("\t\tpoints in the page: " + batchData.length());
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileSequenceRead.java`
#### Snippet
```java
                  System.out.println("\t\tpoints in the page: " + pageHeader.getNumOfValues());
                } else {
                  System.out.println("\t\tpoints in the page: " + batchData.length());
                }
                if (printDetail) {
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileSequenceRead.java`
#### Snippet
```java
                if (printDetail) {
                  while (batchData.hasCurrent()) {
                    System.out.println(
                        "\t\t\ttime, value: "
                            + batchData.currentTime()
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileSequenceRead.java`
#### Snippet
```java
            break;
          case MetaMarker.CHUNK_GROUP_HEADER:
            System.out.println("[Chunk Group]");
            System.out.println("Chunk Group Header position: " + reader.position());
            ChunkGroupHeader chunkGroupHeader = reader.readChunkGroupHeader();
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileSequenceRead.java`
#### Snippet
```java
          case MetaMarker.CHUNK_GROUP_HEADER:
            System.out.println("[Chunk Group]");
            System.out.println("Chunk Group Header position: " + reader.position());
            ChunkGroupHeader chunkGroupHeader = reader.readChunkGroupHeader();
            System.out.println("device: " + chunkGroupHeader.getDeviceID());
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileSequenceRead.java`
#### Snippet
```java
            System.out.println("Chunk Group Header position: " + reader.position());
            ChunkGroupHeader chunkGroupHeader = reader.readChunkGroupHeader();
            System.out.println("device: " + chunkGroupHeader.getDeviceID());
            break;
          case MetaMarker.OPERATION_INDEX_RANGE:
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileSequenceRead.java`
#### Snippet
```java
          case MetaMarker.OPERATION_INDEX_RANGE:
            reader.readPlanIndex();
            System.out.println("minPlanIndex: " + reader.getMinPlanIndex());
            System.out.println("maxPlanIndex: " + reader.getMaxPlanIndex());
            break;
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileSequenceRead.java`
#### Snippet
```java
            reader.readPlanIndex();
            System.out.println("minPlanIndex: " + reader.getMinPlanIndex());
            System.out.println("maxPlanIndex: " + reader.getMaxPlanIndex());
            break;
          default:
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileSequenceRead.java`
#### Snippet
```java
        }
      }
      System.out.println("[Metadata]");
      for (String device : reader.getAllDevices()) {
        Map<String, List<ChunkMetadata>> seriesMetaData = reader.readChunkMetadataInDevice(device);
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileSequenceRead.java`
#### Snippet
```java
      for (String device : reader.getAllDevices()) {
        Map<String, List<ChunkMetadata>> seriesMetaData = reader.readChunkMetadataInDevice(device);
        System.out.printf(
            "\t[Device]Device %s, Number of Measurements %d%n", device, seriesMetaData.size());
        for (Map.Entry<String, List<ChunkMetadata>> serie : seriesMetaData.entrySet()) {
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileSequenceRead.java`
#### Snippet
```java
            "\t[Device]Device %s, Number of Measurements %d%n", device, seriesMetaData.size());
        for (Map.Entry<String, List<ChunkMetadata>> serie : seriesMetaData.entrySet()) {
          System.out.println("\t\tMeasurement:" + serie.getKey());
          for (ChunkMetadata chunkMetadata : serie.getValue()) {
            System.out.println("\t\tFile offset:" + chunkMetadata.getOffsetOfChunkHeader());
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileSequenceRead.java`
#### Snippet
```java
          System.out.println("\t\tMeasurement:" + serie.getKey());
          for (ChunkMetadata chunkMetadata : serie.getValue()) {
            System.out.println("\t\tFile offset:" + chunkMetadata.getOffsetOfChunkHeader());
          }
        }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/jdbc/src/main/java/org/apache/iotdb/PrepareStatementDemo.java`
#### Snippet
```java
            builder.append(resultSet.getString(i)).append(",");
          }
          System.out.println(builder);
        }
        for (int i = 1; i <= resultSetMetaData.getColumnCount(); i++) {
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/jdbc/src/main/java/org/apache/iotdb/PrepareStatementDemo.java`
#### Snippet
```java
        }
        for (int i = 1; i <= resultSetMetaData.getColumnCount(); i++) {
          System.out.println(
              resultSetMetaData.getColumnType(i) + "-" + resultSetMetaData.getColumnName(i));
        }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/jdbc/src/main/java/org/apache/iotdb/SyntaxConventionRelatedExample.java`
#### Snippet
```java
  private static void outputResult(ResultSet resultSet) throws SQLException {
    if (resultSet != null) {
      System.out.println("--------------------------");
      final ResultSetMetaData metaData = resultSet.getMetaData();
      final int columnCount = metaData.getColumnCount();
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/jdbc/src/main/java/org/apache/iotdb/SyntaxConventionRelatedExample.java`
#### Snippet
```java
      final int columnCount = metaData.getColumnCount();
      for (int i = 0; i < columnCount; i++) {
        System.out.print(metaData.getColumnLabel(i + 1) + " ");
      }
      System.out.println();
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/jdbc/src/main/java/org/apache/iotdb/SyntaxConventionRelatedExample.java`
#### Snippet
```java
        System.out.print(metaData.getColumnLabel(i + 1) + " ");
      }
      System.out.println();
      while (resultSet.next()) {
        for (int i = 1; ; i++) {
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/jdbc/src/main/java/org/apache/iotdb/SyntaxConventionRelatedExample.java`
#### Snippet
```java
      while (resultSet.next()) {
        for (int i = 1; ; i++) {
          System.out.print(resultSet.getString(i));
          if (i < columnCount) {
            System.out.print(", ");
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/jdbc/src/main/java/org/apache/iotdb/SyntaxConventionRelatedExample.java`
#### Snippet
```java
          System.out.print(resultSet.getString(i));
          if (i < columnCount) {
            System.out.print(", ");
          } else {
            System.out.println();
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/jdbc/src/main/java/org/apache/iotdb/SyntaxConventionRelatedExample.java`
#### Snippet
```java
            System.out.print(", ");
          } else {
            System.out.println();
            break;
          }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/jdbc/src/main/java/org/apache/iotdb/SyntaxConventionRelatedExample.java`
#### Snippet
```java
        }
      }
      System.out.println("--------------------------\n");
    }
  }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/jdbc/src/main/java/org/apache/iotdb/SyntaxConventionRelatedExample.java`
#### Snippet
```java
                ROOT_SG1_SPECIAL_CHARACTER_EXAMPLE));
      } catch (IoTDBSQLException e) {
        System.out.println(e.getMessage());
      }

```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/jdbc/src/main/java/org/apache/iotdb/SyntaxConventionRelatedExample.java`
#### Snippet
```java
      }
    } catch (IoTDBSQLException e) {
      System.out.println(e.getMessage());
    }
  }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/jdbc/src/main/java/org/apache/iotdb/JDBCExample.java`
#### Snippet
```java
  private static void outputResult(ResultSet resultSet) throws SQLException {
    if (resultSet != null) {
      System.out.println("--------------------------");
      final ResultSetMetaData metaData = resultSet.getMetaData();
      final int columnCount = metaData.getColumnCount();
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/jdbc/src/main/java/org/apache/iotdb/JDBCExample.java`
#### Snippet
```java
      final int columnCount = metaData.getColumnCount();
      for (int i = 0; i < columnCount; i++) {
        System.out.print(metaData.getColumnLabel(i + 1) + " ");
      }
      System.out.println();
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/jdbc/src/main/java/org/apache/iotdb/JDBCExample.java`
#### Snippet
```java
        System.out.print(metaData.getColumnLabel(i + 1) + " ");
      }
      System.out.println();
      while (resultSet.next()) {
        for (int i = 1; ; i++) {
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/jdbc/src/main/java/org/apache/iotdb/JDBCExample.java`
#### Snippet
```java
      while (resultSet.next()) {
        for (int i = 1; ; i++) {
          System.out.print(resultSet.getString(i));
          if (i < columnCount) {
            System.out.print(", ");
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/jdbc/src/main/java/org/apache/iotdb/JDBCExample.java`
#### Snippet
```java
          System.out.print(resultSet.getString(i));
          if (i < columnCount) {
            System.out.print(", ");
          } else {
            System.out.println();
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/jdbc/src/main/java/org/apache/iotdb/JDBCExample.java`
#### Snippet
```java
            System.out.print(", ");
          } else {
            System.out.println();
            break;
          }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/jdbc/src/main/java/org/apache/iotdb/JDBCExample.java`
#### Snippet
```java
        }
      }
      System.out.println("--------------------------\n");
    }
  }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/jdbc/src/main/java/org/apache/iotdb/JDBCExample.java`
#### Snippet
```java
            "CREATE TIMESERIES root.sg1.d1.s3 WITH DATATYPE=INT64, ENCODING=RLE, COMPRESSOR=SNAPPY");
      } catch (IoTDBSQLException e) {
        System.out.println(e.getMessage());
      }

```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/jdbc/src/main/java/org/apache/iotdb/JDBCExample.java`
#### Snippet
```java
      outputResult(resultSet);
    } catch (IoTDBSQLException e) {
      System.out.println(e.getMessage());
    }
  }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `integration/src/main/java/org/apache/iotdb/integration/env/ClusterEnvBase.java`
#### Snippet
```java
        String line;
        while ((line = br.readLine()) != null) {
          System.out.println(line);
        }
        System.out.println();
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `integration/src/main/java/org/apache/iotdb/integration/env/ClusterEnvBase.java`
#### Snippet
```java
          System.out.println(line);
        }
        System.out.println();
        if (proc.waitFor() == 1) {
          flag = false;
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/ImportCsv.java`
#### Snippet
```java
          session.open();
        } catch (IoTDBConnectionException ex) {
          System.out.println("Meet error when insert csv because " + e.getMessage());
        }
        writeAndEmptyDataSet(device, times, typesList, valuesList, measurementsList, --retryTime);
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/ImportCsv.java`
#### Snippet
```java
      }
    } catch (StatementExecutionException e) {
      System.out.println("Meet error when insert csv because " + e.getMessage());
    } finally {
      times.clear();
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/ImportCsv.java`
#### Snippet
```java
                    headerTypeMap.put(header, type);
                  } else {
                    System.out.printf(
                        "Line '%s', column '%s': '%s' unknown type%n",
                        record.getRecordNumber(), header, value);
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/ImportCsv.java`
#### Snippet
```java
                  if (valueTrans == null) {
                    isFail = true;
                    System.out.printf(
                        "Line '%s', column '%s': '%s' can't convert to '%s'%n",
                        record.getRecordNumber(), header, value, type);
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/ImportCsv.java`
#### Snippet
```java
    }
    if (hasStarted.get()) {
      System.out.println("Import completely!");
    } else {
      System.out.println("No records!");
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/ImportCsv.java`
#### Snippet
```java
      System.out.println("Import completely!");
    } else {
      System.out.println("No records!");
    }
  }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/ImportCsv.java`
#### Snippet
```java
                    headerTypeMap.put(headerNameWithoutType, type);
                  } else {
                    System.out.printf(
                        "Line '%s', column '%s': '%s' unknown type%n",
                        record.getRecordNumber(), headerNameWithoutType, value);
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/ImportCsv.java`
#### Snippet
```java
                if (valueTrans == null) {
                  isFail.set(true);
                  System.out.printf(
                      "Line '%s', column '%s': '%s' can't convert to '%s'%n",
                      record.getRecordNumber(), headerNameWithoutType, value, type);
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/ImportCsv.java`
#### Snippet
```java
      writeFailedLinesFile(headerNames, failedFilePath, failedRecords);
    }
    System.out.println("Import completely!");
  }

```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/ImportCsv.java`
#### Snippet
```java

    if (args == null || args.length == 0) {
      System.out.println("Too few params input, please check the following hint.");
      hf.printHelp(TSFILEDB_CLI_PREFIX, options, true);
      System.exit(CODE_ERROR);
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/ImportCsv.java`
#### Snippet
```java
      commandLine = parser.parse(options, args);
    } catch (org.apache.commons.cli.ParseException e) {
      System.out.println("Parse error: " + e.getMessage());
      hf.printHelp(TSFILEDB_CLI_PREFIX, options, true);
      System.exit(CODE_ERROR);
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/ImportCsv.java`
#### Snippet
```java
      parseSpecialParams(commandLine);
    } catch (ArgsErrorException e) {
      System.out.println("Args error: " + e.getMessage());
      System.exit(CODE_ERROR);
    } catch (Exception e) {
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/ImportCsv.java`
#### Snippet
```java
      System.exit(CODE_ERROR);
    } catch (Exception e) {
      System.out.println("Encounter an error, because: " + e.getMessage());
      System.exit(CODE_ERROR);
    }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/ImportCsv.java`
#### Snippet
```java
        }
      } else {
        System.out.println("File not found!");
        return CODE_ERROR;
      }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/ImportCsv.java`
#### Snippet
```java
      }
    } catch (IoTDBConnectionException | StatementExecutionException e) {
      System.out.println("Encounter an error when connecting to server, because " + e.getMessage());
      return CODE_ERROR;
    } finally {
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/ImportCsv.java`
#### Snippet
```java
      sessionDataSet = session.executeQueryStatement(sql);
    } catch (StatementExecutionException e) {
      System.out.println("Meet error when query the type of timeseries because " + e.getMessage());
      return false;
    }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/ImportCsv.java`
#### Snippet
```java
        Stream<CSVRecord> records = csvRecords.stream();
        if (headerNames.isEmpty()) {
          System.out.println("Empty file!");
          return;
        }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/ImportCsv.java`
#### Snippet
```java
        }
        if (!timeColumn.equalsIgnoreCase(headerNames.get(0))) {
          System.out.println("No headers!");
          return;
        }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/ImportCsv.java`
#### Snippet
```java
        }
      } catch (IOException | IllegalPathException e) {
        System.out.println("CSV file read exception because: " + e.getMessage());
      }
    } else {
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/ImportCsv.java`
#### Snippet
```java
      }
    } else {
      System.out.println("The file name must end with \"csv\" or \"txt\"!");
    }
  }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/ImportCsv.java`
#### Snippet
```java
          session.open();
        } catch (IoTDBConnectionException ex) {
          System.out.println("Meet error when insert csv because " + e.getMessage());
        }
        writeAndEmptyDataSet(
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/ImportCsv.java`
#### Snippet
```java
      }
    } catch (StatementExecutionException e) {
      System.out.println("Meet error when insert csv because " + e.getMessage());
    } finally {
      deviceIds.clear();
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/TestRPCClient.java`
#### Snippet
```java
              new TTriggerSnapshotLoadReq(
                  new DataRegionId(1).convertToTConsensusGroupId(), "snapshot_1_1662370255552"));
      System.out.println(res.status);
    } catch (IOException | TException e) {
      throw new RuntimeException(e);
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/TestRPCClient.java`
#### Snippet
```java
          client.inactivatePeer(
              new TInactivatePeerReq(new DataRegionId(1).convertToTConsensusGroupId()));
      System.out.println(res.status);
    } catch (IOException | TException e) {
      throw new RuntimeException(e);
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/TestRPCClient.java`
#### Snippet
```java
      req.setRegionReplicaSet(regionReplicaSet);
      TSStatus res = client.createDataRegion(req);
      System.out.println(res.code + " " + res.message);

    } catch (IOException | TException e) {
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/PlanNodeUtil.java`
#### Snippet
```java
  private static void printPlanNodeWithLevel(PlanNode root, int level) {
    printTab(level);
    System.out.println(root.toString());
    for (PlanNode child : root.getChildren()) {
      printPlanNodeWithLevel(child, level + 1);
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/PlanNodeUtil.java`
#### Snippet
```java
  private static void printTab(int count) {
    for (int i = 0; i < count; i++) {
      System.out.print("\t");
    }
  }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/PlanGraphPrinter.java`
#### Snippet
```java
    List<String> lines = getGraph(node);
    for (String line : lines) {
      System.out.println(line);
    }
  }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/tools/IoTDBDataDirViewer.java`
#### Snippet
```java

  private static void printlnBoth(PrintWriter pw, String str) {
    System.out.println(str);
    pw.println(str);
  }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/tools/IoTDBDataDirViewer.java`
#### Snippet
```java
      throw new IOException("Invalid parameters. Please check the user guide.");
    }
    System.out.println("output save path:" + outFile);
    System.out.println("data dir num:" + data_dir.length);
    try (PrintWriter pw = new PrintWriter(new FileWriter(outFile))) {
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/tools/IoTDBDataDirViewer.java`
#### Snippet
```java
    }
    System.out.println("output save path:" + outFile);
    System.out.println("data dir num:" + data_dir.length);
    try (PrintWriter pw = new PrintWriter(new FileWriter(outFile))) {
      for (String dir : data_dir) {
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileResourcePrinter.java`
#### Snippet
```java
        printResource(file.getAbsolutePath());
      }
      System.out.println("Analyzing the resource file folder " + folder + " finished.");
    } else {
      // analyze a tsfile.resource file
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileResourcePrinter.java`
#### Snippet
```java
      // analyze a tsfile.resource file
      printResource(folderFile.getAbsolutePath());
      System.out.println("Analyzing the resource file " + folder + " finished.");
    }
  }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileResourcePrinter.java`
#### Snippet
```java
    filename = filename.substring(0, filename.length() - 9);
    TsFileResource resource = new TsFileResource(SystemFileFactory.INSTANCE.getFile(filename));
    System.out.printf("Analyzing %s ...%n", filename);
    System.out.println();
    resource.deserialize();
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileResourcePrinter.java`
#### Snippet
```java
    TsFileResource resource = new TsFileResource(SystemFileFactory.INSTANCE.getFile(filename));
    System.out.printf("Analyzing %s ...%n", filename);
    System.out.println();
    resource.deserialize();

```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileResourcePrinter.java`
#### Snippet
```java
    resource.deserialize();

    System.out.printf(
        "Resource plan index range [%d, %d]%n",
        resource.getMinPlanIndex(), resource.getMaxPlanIndex());
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileResourcePrinter.java`
#### Snippet
```java

    for (String device : resource.getDevices()) {
      System.out.printf(
          "device %s, start time %d (%s), end time %d (%s)%n",
          device,
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileResourcePrinter.java`
#### Snippet
```java
          DateTimeUtils.convertMillsecondToZonedDateTime(resource.getEndTime(device)));
    }
    System.out.println();
  }
}
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/tools/schema/SchemaFileSketchTool.java`
#### Snippet
```java
    try {
      String res = ((SchemaFile) sf).inspect(pw);
      System.out.println(res);
    } finally {
      sf.close();
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/tools/watermark/WatermarkDetector.java`
#### Snippet
```java
      String dataType)
      throws LogicalOperatorException, IOException {
    System.out.println("-----Watermark detection begins-----");
    int[] trueNums = new int[watermarkBitString.length()]; // for majority vote
    int[] falseNums = new int[watermarkBitString.length()]; // for majority vote
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/tools/watermark/WatermarkDetector.java`
#### Snippet
```java

    int b = calMin(cnt, alpha);
    System.out.println(
        String.format("total counted number = %d, detected hit number = %d", cnt, hit_cnt));
    System.out.println(
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/tools/watermark/WatermarkDetector.java`
#### Snippet
```java
    System.out.println(
        String.format("total counted number = %d, detected hit number = %d", cnt, hit_cnt));
    System.out.println(
        String.format(
            "To reach the significant level %f, the hit number should be not smaller than: %d",
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/tools/watermark/WatermarkDetector.java`
#### Snippet
```java
    boolean isWatermarked;
    if (hit_cnt >= b) {
      System.out.println("Therefore the detection result is: watermarked");
      isWatermarked = true;
    } else {
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/tools/watermark/WatermarkDetector.java`
#### Snippet
```java
      isWatermarked = true;
    } else {
      System.out.println("Therefore the detection result is: not watermarked");
      isWatermarked = false;
    }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/tools/watermark/WatermarkDetector.java`
#### Snippet
```java
      isWatermarked = false;
    }
    System.out.println("-----Watermark detection finishes-----");
    return isWatermarked;
  }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/tools/watermark/WatermarkDetector.java`
#### Snippet
```java
    b++;
    if (b > l) {
      System.out.println(
          "The total counted number or the alpha is too small to find b "
              + "that meets the formula: (C(l,b)+C(l,b+1)+C(l,b+2)+...+C(l,l))/2^l < alpha");
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/tools/settle/TsFileAndModSettleTool.java`
#### Snippet
```java
      try {
        TsFileAndModSettleTool tsFileAndModSettleTool = TsFileAndModSettleTool.getInstance();
        System.out.println("Start settling for tsFile : " + resourceToBeSettled.getTsFilePath());
        if (tsFileAndModSettleTool.isSettledFileGenerated(resourceToBeSettled)) {
          settledTsFileResources = tsFileAndModSettleTool.findSettledFile(resourceToBeSettled);
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/tools/settle/TsFileAndModSettleTool.java`
#### Snippet
```java
                + SettleLog.COMMA_SEPERATOR
                + SettleCheckStatus.SETTLE_SUCCESS);
        System.out.println(
            "Finish settling successfully for tsFile : " + resourceToBeSettled.getTsFilePath());
        successCount++;
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/tools/settle/TsFileAndModSettleTool.java`
#### Snippet
```java
        successCount++;
      } catch (Exception e) {
        System.out.println(
            "Meet error while settling the tsFile : " + resourceToBeSettled.getTsFilePath());
        e.printStackTrace();
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/tools/settle/TsFileAndModSettleTool.java`
#### Snippet
```java
    if (resourcesToBeSettled.size() == successCount) {
      SettleLog.closeLogWriter();
      System.out.println("Finish settling all tsfiles Successfully!");
    } else {
      System.out.println(
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/tools/settle/TsFileAndModSettleTool.java`
#### Snippet
```java
      System.out.println("Finish settling all tsfiles Successfully!");
    } else {
      System.out.println(
          "Finish Settling, "
              + (resourcesToBeSettled.size() - successCount)
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/tools/settle/TsFileAndModSettleTool.java`
#### Snippet
```java
      }
    }
    System.out.println(
        "Totally find "
            + oldTsFileResources.size()
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.err` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/utils/CommonUtils.java`
#### Snippet
```java

  private static void err(Throwable e) {
    System.err.println("error: " + e.getMessage());
    System.err.println("-- StackTrace --");
    System.err.println(Throwables.getStackTraceAsString(e));
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.err` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/utils/CommonUtils.java`
#### Snippet
```java
  private static void err(Throwable e) {
    System.err.println("error: " + e.getMessage());
    System.err.println("-- StackTrace --");
    System.err.println(Throwables.getStackTraceAsString(e));
  }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.err` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/utils/CommonUtils.java`
#### Snippet
```java
    System.err.println("error: " + e.getMessage());
    System.err.println("-- StackTrace --");
    System.err.println(Throwables.getStackTraceAsString(e));
  }
}
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/utils/CommonUtils.java`
#### Snippet
```java

  private static void badUse(Exception e) {
    System.out.println("node-tool: " + e.getMessage());
    System.out.println("See 'node-tool help' or 'node-tool help <command>'.");
  }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/utils/CommonUtils.java`
#### Snippet
```java
  private static void badUse(Exception e) {
    System.out.println("node-tool: " + e.getMessage());
    System.out.println("See 'node-tool help' or 'node-tool help <command>'.");
  }

```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileSketchTool.java`
#### Snippet
```java

  private void printlnBoth(PrintWriter pw, String str) {
    System.out.println(str);
    pw.println(str);
  }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileSketchTool.java`
#### Snippet
```java
    String filename = fileNames.left;
    String outFile = fileNames.right;
    System.out.println("TsFile path:" + filename);
    System.out.println("Sketch save path:" + outFile);
    new TsFileSketchTool(filename, outFile).run();
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileSketchTool.java`
#### Snippet
```java
    String outFile = fileNames.right;
    System.out.println("TsFile path:" + filename);
    System.out.println("Sketch save path:" + outFile);
    new TsFileSketchTool(filename, outFile).run();
  }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/tools/validate/TsFileValidationTool.java`
#### Snippet
```java

  private static void printBoth(String msg) {
    System.out.println(msg);
    if (printToFile) {
      pw.println(msg);
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/tools/validate/TsFileValidationTool.java`
#### Snippet
```java
  private static boolean checkArgs(String[] args) {
    if (args.length < 1) {
      System.out.println(
          "Please input correct param, which is [path of data dir] [-pd = print details or not] [-f = path of outFile]. Eg: xxx/iotdb/data/data -pd=true -f=xxx/TsFile_validation_view.txt");
      return false;
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/tools/validate/TsFileValidationTool.java`
#### Snippet
```java
            fileList.add(f);
          } else {
            System.out.println(arg + " is not a correct data directory or tsfile of IOTDB.");
            return false;
          }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/tools/validate/TsFileValidationTool.java`
#### Snippet
```java
      }
      if (seqDataDirList.size() == 0 && fileList.size() == 0) {
        System.out.println(
            "Please input correct param, which is [path of data dir] [-pd = print details or not] [-f = path of outFile]. Eg: xxx/iotdb/data/data -pd=true -f=xxx/TsFile_validation_view.txt");
        return false;
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/service/GetVersion.java`
#### Snippet
```java
public class GetVersion {
  public static void main(String[] args) {
    System.out.println(IoTDBDescriptor.getInstance().getConfig().getIoTDBVersion());
  }
}
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/SchemaFile.java`
#### Snippet
```java
            lastPageIndex + 1);
    if (pw == null) {
      pw = new PrintWriter(System.out);
    }
    pw.print(header);
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/SessionPoolExample.java`
#### Snippet
```java
              // get DataIterator like JDBC
              DataIterator dataIterator = wrapper.iterator();
              System.out.println(wrapper.getColumnNames());
              System.out.println(wrapper.getColumnTypes());
              while (dataIterator.next()) {
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/SessionPoolExample.java`
#### Snippet
```java
              DataIterator dataIterator = wrapper.iterator();
              System.out.println(wrapper.getColumnNames());
              System.out.println(wrapper.getColumnTypes());
              while (dataIterator.next()) {
                StringBuilder builder = new StringBuilder();
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/SessionPoolExample.java`
#### Snippet
```java
                  builder.append(dataIterator.getString(columnName) + " ");
                }
                System.out.println(builder);
              }
            } catch (IoTDBConnectionException | StatementExecutionException e) {
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/SessionPoolExample.java`
#### Snippet
```java
            try {
              wrapper = sessionPool.executeQueryStatement("select * from root.sg1.d1");
              System.out.println(wrapper.getColumnNames());
              System.out.println(wrapper.getColumnTypes());
              while (wrapper.hasNext()) {
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/SessionPoolExample.java`
#### Snippet
```java
              wrapper = sessionPool.executeQueryStatement("select * from root.sg1.d1");
              System.out.println(wrapper.getColumnNames());
              System.out.println(wrapper.getColumnTypes());
              while (wrapper.hasNext()) {
                System.out.println(wrapper.next());
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/SessionPoolExample.java`
#### Snippet
```java
              System.out.println(wrapper.getColumnTypes());
              while (wrapper.hasNext()) {
                System.out.println(wrapper.next());
              }
            } catch (IoTDBConnectionException | StatementExecutionException e) {
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/HybridTimeseriesSessionExample.java`
#### Snippet
```java
  private static void selectTest() throws StatementExecutionException, IoTDBConnectionException {
    SessionDataSet dataSet = session.executeQueryStatement("select ** from root.sg_1");
    System.out.println(dataSet.getColumnNames());
    while (dataSet.hasNext()) {
      System.out.println(dataSet.next());
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/HybridTimeseriesSessionExample.java`
#### Snippet
```java
    System.out.println(dataSet.getColumnNames());
    while (dataSet.hasNext()) {
      System.out.println(dataSet.next());
    }

```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/DataMigrationExample.java`
#### Snippet
```java

      } catch (Exception e) {
        System.out.println(
            "Loading the " + i + "-th timeseries: " + series + " failed " + e.getMessage());
        return null;
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/DataMigrationExample.java`
#### Snippet
```java
      }

      System.out.println("Loading the " + i + "-th timeseries: " + series + " success");
      return null;
    }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/DataMigrationExample.java`
#### Snippet
```java
    if (schemaIter.next()) {
      total = schemaIter.getInt(1);
      System.out.println("Total timeseries: " + total);
    } else {
      System.out.println("Can not get timeseries schema");
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/DataMigrationExample.java`
#### Snippet
```java
      System.out.println("Total timeseries: " + total);
    } else {
      System.out.println("Can not get timeseries schema");
      System.exit(1);
    }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/SyntaxConventionRelatedExample.java`
#### Snippet
```java
        session.executeRawDataQuery(paths, startTime, endTime, timeOut)) {

      System.out.println(dataSet1.getColumnNames());
      dataSet1.setFetchSize(1024);
      while (dataSet1.hasNext()) {
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/SyntaxConventionRelatedExample.java`
#### Snippet
```java
      dataSet1.setFetchSize(1024);
      while (dataSet1.hasNext()) {
        System.out.println(dataSet1.next());
      }
    }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/TabletExample.java`
#### Snippet
```java
    measureTSTypeInfos.forEach((mea, type) -> schemas.add(new MeasurementSchema(mea, type)));

    System.out.println(
        String.format(
            "Test Java: csv file name: %s, row: %d, col: %d", dataFileName, rowSize, colSize));
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/TabletExample.java`
#### Snippet
```java
        String.format(
            "Test Java: csv file name: %s, row: %d, col: %d", dataFileName, rowSize, colSize));
    System.out.println(String.format("Total points: %d", rowSize * colSize * schemas.size()));

    // test start
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/TabletExample.java`
#### Snippet
```java
    session.close();

    System.out.println(String.format("load cost: %.3f", ((float) loadCost / 1000_000_000)));
    System.out.println(
        String.format(
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/TabletExample.java`
#### Snippet
```java

    System.out.println(String.format("load cost: %.3f", ((float) loadCost / 1000_000_000)));
    System.out.println(
        String.format(
            "construct tablet cost: %.3f",
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/TabletExample.java`
#### Snippet
```java
            "construct tablet cost: %.3f",
            ((float) (allEnd - allStart - insertCost - loadCost) / 1000_000_000)));
    System.out.println(
        String.format("insert tablet cost: %.3f", ((float) insertCost / 1000_000_000)));
    System.out.println(
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/TabletExample.java`
#### Snippet
```java
    System.out.println(
        String.format("insert tablet cost: %.3f", ((float) insertCost / 1000_000_000)));
    System.out.println(
        String.format("total cost: %.3f", ((float) (allEnd - allStart) / 1000_000_000)));
    System.out.println(String.format("%.3f", ((float) loadCost / 1000_000_000)));
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/TabletExample.java`
#### Snippet
```java
    System.out.println(
        String.format("total cost: %.3f", ((float) (allEnd - allStart) / 1000_000_000)));
    System.out.println(String.format("%.3f", ((float) loadCost / 1000_000_000)));
  }
}
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/AlignedTimeseriesSessionExample.java`
#### Snippet
```java
    SessionDataSet dataSet =
        session.executeQueryStatement("select s1 from root.sg_1.d1 where s1 > 3 and time < 9");
    System.out.println(dataSet.getColumnNames());
    while (dataSet.hasNext()) {
      System.out.println(dataSet.next());
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/AlignedTimeseriesSessionExample.java`
#### Snippet
```java
    System.out.println(dataSet.getColumnNames());
    while (dataSet.hasNext()) {
      System.out.println(dataSet.next());
    }

```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/AlignedTimeseriesSessionExample.java`
#### Snippet
```java
        session.executeQueryStatement(
            "select * from root.sg_1.d1 where time < 8 and s1 > 3 and s2 > 5");
    System.out.println(dataSet.getColumnNames());
    while (dataSet.hasNext()) {
      System.out.println(dataSet.next());
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/AlignedTimeseriesSessionExample.java`
#### Snippet
```java
    System.out.println(dataSet.getColumnNames());
    while (dataSet.hasNext()) {
      System.out.println(dataSet.next());
    }

```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/AlignedTimeseriesSessionExample.java`
#### Snippet
```java
  private static void selectTest() throws StatementExecutionException, IoTDBConnectionException {
    SessionDataSet dataSet = session.executeQueryStatement("select s1 from root.sg_1.d1");
    System.out.println(dataSet.getColumnNames());
    while (dataSet.hasNext()) {
      System.out.println(dataSet.next());
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/AlignedTimeseriesSessionExample.java`
#### Snippet
```java
    System.out.println(dataSet.getColumnNames());
    while (dataSet.hasNext()) {
      System.out.println(dataSet.next());
    }

```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/AlignedTimeseriesSessionExample.java`
#### Snippet
```java
    dataSet.closeOperationHandle();
    dataSet = session.executeQueryStatement("select * from root.sg_1.d1");
    System.out.println(dataSet.getColumnNames());
    while (dataSet.hasNext()) {
      System.out.println(dataSet.next());
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/AlignedTimeseriesSessionExample.java`
#### Snippet
```java
    System.out.println(dataSet.getColumnNames());
    while (dataSet.hasNext()) {
      System.out.println(dataSet.next());
    }

```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/AlignedTimeseriesSessionExample.java`
#### Snippet
```java
        session.executeQueryStatement(
            "select count(s1) from root.sg_1.d1.vector GROUP BY ([1, 100), 20ms)");
    System.out.println(dataSet.getColumnNames());
    while (dataSet.hasNext()) {
      System.out.println(dataSet.next());
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/AlignedTimeseriesSessionExample.java`
#### Snippet
```java
    System.out.println(dataSet.getColumnNames());
    while (dataSet.hasNext()) {
      System.out.println(dataSet.next());
    }

```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/AlignedTimeseriesSessionExample.java`
#### Snippet
```java
            "select count(*) from root.sg_1.d1.vector where time > 50 and s1 > 0 and s2 > 10000"
                + " GROUP BY ([50, 100), 10ms)");
    System.out.println(dataSet.getColumnNames());
    while (dataSet.hasNext()) {
      System.out.println(dataSet.next());
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/AlignedTimeseriesSessionExample.java`
#### Snippet
```java
    System.out.println(dataSet.getColumnNames());
    while (dataSet.hasNext()) {
      System.out.println(dataSet.next());
    }

```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/AlignedTimeseriesSessionExample.java`
#### Snippet
```java
    SessionDataSet dataSet =
        session.executeQueryStatement("select last s1 from root.sg_1.d1 where time >= 5");
    System.out.println(dataSet.getColumnNames());
    while (dataSet.hasNext()) {
      System.out.println(dataSet.next());
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/AlignedTimeseriesSessionExample.java`
#### Snippet
```java
    System.out.println(dataSet.getColumnNames());
    while (dataSet.hasNext()) {
      System.out.println(dataSet.next());
    }

```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/AlignedTimeseriesSessionExample.java`
#### Snippet
```java

    dataSet = session.executeQueryStatement("select last * from root.sg_1.d1 where time >= 5");
    System.out.println(dataSet.getColumnNames());
    while (dataSet.hasNext()) {
      System.out.println(dataSet.next());
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/AlignedTimeseriesSessionExample.java`
#### Snippet
```java
    System.out.println(dataSet.getColumnNames());
    while (dataSet.hasNext()) {
      System.out.println(dataSet.next());
    }
    dataSet.closeOperationHandle();
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/AlignedTimeseriesSessionExample.java`
#### Snippet
```java

    dataSet = session.executeQueryStatement("select last * from root.sg_1.d1 where time >= 20");
    System.out.println(dataSet.getColumnNames());
    while (dataSet.hasNext()) {
      System.out.println(dataSet.next());
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/AlignedTimeseriesSessionExample.java`
#### Snippet
```java
    System.out.println(dataSet.getColumnNames());
    while (dataSet.hasNext()) {
      System.out.println(dataSet.next());
    }
    dataSet.closeOperationHandle();
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/AlignedTimeseriesSessionExample.java`
#### Snippet
```java
    SessionDataSet dataSet =
        session.executeQueryStatement("select * from root.sg_1 align by device");
    System.out.println(dataSet.getColumnNames());
    while (dataSet.hasNext()) {
      System.out.println(dataSet.next());
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/AlignedTimeseriesSessionExample.java`
#### Snippet
```java
    System.out.println(dataSet.getColumnNames());
    while (dataSet.hasNext()) {
      System.out.println(dataSet.next());
    }

```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/AlignedTimeseriesSessionExample.java`
#### Snippet
```java
      throws StatementExecutionException, IoTDBConnectionException {
    SessionDataSet dataSet = session.executeQueryStatement("select count(s1) from root.sg_1.d1");
    System.out.println(dataSet.getColumnNames());
    while (dataSet.hasNext()) {
      System.out.println(dataSet.next());
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/AlignedTimeseriesSessionExample.java`
#### Snippet
```java
    System.out.println(dataSet.getColumnNames());
    while (dataSet.hasNext()) {
      System.out.println(dataSet.next());
    }

```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/AlignedTimeseriesSessionExample.java`
#### Snippet
```java
    dataSet.closeOperationHandle();
    dataSet = session.executeQueryStatement("select count(*) from root.sg_1.d1");
    System.out.println(dataSet.getColumnNames());
    while (dataSet.hasNext()) {
      System.out.println(dataSet.next());
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/AlignedTimeseriesSessionExample.java`
#### Snippet
```java
    System.out.println(dataSet.getColumnNames());
    while (dataSet.hasNext()) {
      System.out.println(dataSet.next());
    }

```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/AlignedTimeseriesSessionExample.java`
#### Snippet
```java
        session.executeQueryStatement(
            "select sum(*) from root.sg_1.d1.vector where time > 50 and s1 > 0 and s2 > 10000");
    System.out.println(dataSet.getColumnNames());
    while (dataSet.hasNext()) {
      System.out.println(dataSet.next());
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/AlignedTimeseriesSessionExample.java`
#### Snippet
```java
    System.out.println(dataSet.getColumnNames());
    while (dataSet.hasNext()) {
      System.out.println(dataSet.next());
    }

```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/AlignedTimeseriesSessionExample.java`
#### Snippet
```java
      throws StatementExecutionException, IoTDBConnectionException {
    SessionDataSet dataSet = session.executeQueryStatement("select last s1 from root.sg_1.d1");
    System.out.println(dataSet.getColumnNames());
    while (dataSet.hasNext()) {
      System.out.println(dataSet.next());
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/AlignedTimeseriesSessionExample.java`
#### Snippet
```java
    System.out.println(dataSet.getColumnNames());
    while (dataSet.hasNext()) {
      System.out.println(dataSet.next());
    }

```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/AlignedTimeseriesSessionExample.java`
#### Snippet
```java
    dataSet.closeOperationHandle();
    dataSet = session.executeQueryStatement("select last * from root.sg_1.d1");
    System.out.println(dataSet.getColumnNames());
    while (dataSet.hasNext()) {
      System.out.println(dataSet.next());
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/AlignedTimeseriesSessionExample.java`
#### Snippet
```java
    System.out.println(dataSet.getColumnNames());
    while (dataSet.hasNext()) {
      System.out.println(dataSet.next());
    }

```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/AlignedTimeseriesSessionExample.java`
#### Snippet
```java
    selectWithLastTestWithoutValueFilter();
    session.executeNonQueryStatement("delete from root.sg_1.d1.s1 where time <= 5");
    System.out.println("execute sql delete from root.sg_1.d1.s1 where time <= 5");
    selectTest();
    selectWithValueFilterTest();
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/AlignedTimeseriesSessionExample.java`
#### Snippet
```java
    selectWithLastTestWithoutValueFilter();
    session.executeNonQueryStatement("delete from root.sg_1.d1.s2 where time <= 3");
    System.out.println("execute sql delete from root.sg_1.d1.s2 where time <= 3");

    selectTest();
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/AlignedTimeseriesSessionExample.java`
#### Snippet
```java
    selectWithLastTestWithoutValueFilter();
    session.executeNonQueryStatement("delete from root.sg_1.d1.s1 where time <= 10");
    System.out.println("execute sql delete from root.sg_1.d1.s1 where time <= 10");
    selectTest();
    selectWithValueFilterTest();
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/SessionExample.java`
#### Snippet
```java
    session.insertTablet(tablet);
    try (SessionDataSet dataSet = session.executeQueryStatement("select s1, s2 from " + device)) {
      System.out.println(dataSet.getColumnNames());
      while (dataSet.hasNext()) {
        System.out.println(dataSet.next());
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/SessionExample.java`
#### Snippet
```java
      System.out.println(dataSet.getColumnNames());
      while (dataSet.hasNext()) {
        System.out.println(dataSet.next());
      }
    }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/SessionExample.java`
#### Snippet
```java
    try (SessionDataSet dataSet =
        session.executeQueryStatement("select into_s1, into_s2, into_s3 from root.sg1.d1")) {
      System.out.println(dataSet.getColumnNames());
      while (dataSet.hasNext()) {
        System.out.println(dataSet.next());
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/SessionExample.java`
#### Snippet
```java
      System.out.println(dataSet.getColumnNames());
      while (dataSet.hasNext()) {
        System.out.println(dataSet.next());
      }
    }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/SessionExample.java`
#### Snippet
```java

      DataIterator iterator = dataSet.iterator();
      System.out.println(dataSet.getColumnNames());
      dataSet.setFetchSize(1024); // default is 10000
      while (iterator.next()) {
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/SessionExample.java`
#### Snippet
```java
        }

        System.out.println(builder);
      }
    }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/SessionExample.java`
#### Snippet
```java
    paths.add(ROOT_SG1_D1_S3);
    try (SessionDataSet sessionDataSet = session.executeLastDataQuery(paths, 3, 60000)) {
      System.out.println(sessionDataSet.getColumnNames());
      sessionDataSet.setFetchSize(1024);
      while (sessionDataSet.hasNext()) {
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/SessionExample.java`
#### Snippet
```java
      sessionDataSet.setFetchSize(1024);
      while (sessionDataSet.hasNext()) {
        System.out.println(sessionDataSet.next());
      }
    }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/SessionExample.java`
#### Snippet
```java
          sessionEnableRedirect.executeQueryStatement(selectPrefix + i + ".d1")) {

        System.out.println(dataSet.getColumnNames());
        dataSet.setFetchSize(1024); // default is 10000
        while (dataSet.hasNext()) {
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/SessionExample.java`
#### Snippet
```java
        dataSet.setFetchSize(1024); // default is 10000
        while (dataSet.hasNext()) {
          System.out.println(dataSet.next());
        }
      }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/SessionExample.java`
#### Snippet
```java
              selectPrefix + i + ".d1 where time >= 1 and time < 10")) {

        System.out.println(dataSet.getColumnNames());
        dataSet.setFetchSize(1024); // default is 10000
        while (dataSet.hasNext()) {
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/SessionExample.java`
#### Snippet
```java
        dataSet.setFetchSize(1024); // default is 10000
        while (dataSet.hasNext()) {
          System.out.println(dataSet.next());
        }
      }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/SessionExample.java`
#### Snippet
```java
              selectPrefix + i + ".d1 where time >= 1 and time < 10 align by device")) {

        System.out.println(dataSet.getColumnNames());
        dataSet.setFetchSize(1024); // default is 10000
        while (dataSet.hasNext()) {
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/SessionExample.java`
#### Snippet
```java
        dataSet.setFetchSize(1024); // default is 10000
        while (dataSet.hasNext()) {
          System.out.println(dataSet.next());
        }
      }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/SessionExample.java`
#### Snippet
```java
                  + i
                  + ".d1.s1 > 1")) {
        System.out.println(dataSet.getColumnNames());
        dataSet.setFetchSize(1024); // default is 10000
        while (dataSet.hasNext()) {
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/SessionExample.java`
#### Snippet
```java
        dataSet.setFetchSize(1024); // default is 10000
        while (dataSet.hasNext()) {
          System.out.println(dataSet.next());
        }
      }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/SessionExample.java`
#### Snippet
```java
    try (SessionDataSet dataSet = session.executeRawDataQuery(paths, startTime, endTime, timeOut)) {

      System.out.println(dataSet.getColumnNames());
      dataSet.setFetchSize(1024);
      while (dataSet.hasNext()) {
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/SessionExample.java`
#### Snippet
```java
      dataSet.setFetchSize(1024);
      while (dataSet.hasNext()) {
        System.out.println(dataSet.next());
      }
    }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/SessionExample.java`
#### Snippet
```java
    try (SessionDataSet dataSet =
        session.executeQueryStatement("select * from root.sg1.d1", 2000)) {
      System.out.println(dataSet.getColumnNames());
      dataSet.setFetchSize(1024); // default is 10000
      while (dataSet.hasNext()) {
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/SessionExample.java`
#### Snippet
```java
      dataSet.setFetchSize(1024); // default is 10000
      while (dataSet.hasNext()) {
        System.out.println(dataSet.next());
      }
    }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/SessionExample.java`
#### Snippet
```java
  private static void query() throws IoTDBConnectionException, StatementExecutionException {
    try (SessionDataSet dataSet = session.executeQueryStatement("select * from root.sg1.d1")) {
      System.out.println(dataSet.getColumnNames());
      dataSet.setFetchSize(1024); // default is 10000
      while (dataSet.hasNext()) {
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/SessionExample.java`
#### Snippet
```java
      dataSet.setFetchSize(1024); // default is 10000
      while (dataSet.hasNext()) {
        System.out.println(dataSet.next());
      }
    }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/KLLSketchForQuantile.java`
#### Snippet
```java
  public void showNum() {
    for (int i = 0; i < cntLevel; i++) {
      System.out.print("\t|");
      for (int j = levelPos[i]; j < levelPos[i + 1]; j++) System.out.print(num[j] + ",");
      System.out.print("|\t");
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/KLLSketchForQuantile.java`
#### Snippet
```java
    for (int i = 0; i < cntLevel; i++) {
      System.out.print("\t|");
      for (int j = levelPos[i]; j < levelPos[i + 1]; j++) System.out.print(num[j] + ",");
      System.out.print("|\t");
    }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/KLLSketchForQuantile.java`
#### Snippet
```java
      System.out.print("\t|");
      for (int j = levelPos[i]; j < levelPos[i + 1]; j++) System.out.print(num[j] + ",");
      System.out.print("|\t");
    }
    System.out.println();
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/KLLSketchForQuantile.java`
#### Snippet
```java
      System.out.print("|\t");
    }
    System.out.println();
  }

```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/KLLSketchForQuantile.java`
#### Snippet
```java
  public void show() {
    for (int i = 0; i < cntLevel; i++) {
      System.out.print("\t");
      System.out.print("[" + (levelPos[i + 1] - levelPos[i]) + "]");
      System.out.print("\t");
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/KLLSketchForQuantile.java`
#### Snippet
```java
    for (int i = 0; i < cntLevel; i++) {
      System.out.print("\t");
      System.out.print("[" + (levelPos[i + 1] - levelPos[i]) + "]");
      System.out.print("\t");
    }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/KLLSketchForQuantile.java`
#### Snippet
```java
      System.out.print("\t");
      System.out.print("[" + (levelPos[i + 1] - levelPos[i]) + "]");
      System.out.print("\t");
    }
    System.out.println();
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/KLLSketchForQuantile.java`
#### Snippet
```java
      System.out.print("\t");
    }
    System.out.println();
  }

```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/HeapLongKLLSketch.java`
#### Snippet
```java
  public void showLevelMaxSize() {
    int numLEN = levelPos[cntLevel] - levelPos[0];
    System.out.println("\t\t//maxMemNum:" + maxMemoryNum + "\t//N:" + N);
    for (int i = 0; i < cntLevel; i++) System.out.print("\t\t" + levelMaxSize[i] + "\t");
    System.out.println();
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/HeapLongKLLSketch.java`
#### Snippet
```java
    int numLEN = levelPos[cntLevel] - levelPos[0];
    System.out.println("\t\t//maxMemNum:" + maxMemoryNum + "\t//N:" + N);
    for (int i = 0; i < cntLevel; i++) System.out.print("\t\t" + levelMaxSize[i] + "\t");
    System.out.println();
    System.out.println("-------------------------------------------------------");
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/HeapLongKLLSketch.java`
#### Snippet
```java
    System.out.println("\t\t//maxMemNum:" + maxMemoryNum + "\t//N:" + N);
    for (int i = 0; i < cntLevel; i++) System.out.print("\t\t" + levelMaxSize[i] + "\t");
    System.out.println();
    System.out.println("-------------------------------------------------------");
  }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/HeapLongKLLSketch.java`
#### Snippet
```java
    for (int i = 0; i < cntLevel; i++) System.out.print("\t\t" + levelMaxSize[i] + "\t");
    System.out.println();
    System.out.println("-------------------------------------------------------");
  }

```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/influxdb-protocol-example/src/main/java/org/apache/iotdb/influxdb/InfluxDBExample.java`
#### Snippet
```java
            database);
    result = influxDB.query(query);
    System.out.println("query1 result:" + result.getResults().get(0).getSeries().get(0).toString());

    query =
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/influxdb-protocol-example/src/main/java/org/apache/iotdb/influxdb/InfluxDBExample.java`
#### Snippet
```java
            database);
    result = influxDB.query(query);
    System.out.println("query2 result:" + result.getResults().get(0).getSeries().get(0).toString());
  }
}
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/util/QueryResultUtils.java`
#### Snippet
```java
    }
    if (!checkSameQueryResult(queryResult1, queryResult2)) {
      System.out.println("QueryResult1 and QueryResult2 is not same attribute");
      return queryResult1;
    }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/util/QueryResultUtils.java`
#### Snippet
```java
    }
    if (!checkSameQueryResult(queryResult1, queryResult2)) {
      System.out.println("QueryResult1 and QueryResult2 is not same attribute");
      return queryResult1;
    }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/flink/src/main/java/org/apache/iotdb/flink/FlinkTsFileBatchSource.java`
#### Snippet
```java
    List<String> result = source.map(Row::toString).collect();
    for (String s : result) {
      System.out.println(s);
    }
  }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/flink/src/main/java/org/apache/iotdb/flink/FlinkTsFileStreamSource.java`
#### Snippet
```java
    Iterator<String> result = DataStreamUtils.collect(rowString);
    while (result.hasNext()) {
      System.out.println(result.next());
    }
  }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/flink/src/main/java/org/apache/iotdb/flink/FlinkTsFileBatchSink.java`
#### Snippet
```java
    String[] result = TsFileUtils.readTsFile(path, paths);
    for (String row : result) {
      System.out.println(row);
    }
  }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/flink/src/main/java/org/apache/iotdb/flink/FlinkTsFileStreamSink.java`
#### Snippet
```java
    String[] result = TsFileUtils.readTsFile(path, paths);
    for (String row : result) {
      System.out.println(row);
    }
  }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `rewrite-tsfile-tool/src/main/java/org/apache/iotdb/RewriteTsFileTool.java`
#### Snippet
```java
            } catch (Throwable t) {
              // this is a broken aligned chunk, skip it
              System.out.println("Skip aligned chunk " + device);
            }
          } else {
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `rewrite-tsfile-tool/src/main/java/org/apache/iotdb/RewriteTsFileTool.java`
#### Snippet
```java
            break;
          default:
            System.out.printf(
                "Cannot handle marker %d in position %d, stop reading %s%n",
                marker, reader.position(), filename);
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `rewrite-tsfile-tool/src/main/java/org/apache/iotdb/RewriteTsFileTool.java`
#### Snippet
```java
          // this is a broken chunk, skip it
          t.printStackTrace();
          System.out.printf("Skip broken chunk in device %s.%s%n", device, p.getMeasurement());
        }
      }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `rewrite-tsfile-tool/src/main/java/org/apache/iotdb/RewriteTsFileTool.java`
#### Snippet
```java
    for (int i = 0; i < files.size(); i++) {
      File file = files.get(i);
      System.out.printf("Loading %s(%d/%d)...", file.getPath(), i + 1, size);
      try {
        seqWriteSingleTsFile(file.getPath(), session);
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `rewrite-tsfile-tool/src/main/java/org/apache/iotdb/RewriteTsFileTool.java`
#### Snippet
```java
        session.executeNonQueryStatement("FLUSH");
      } catch (Exception e) {
        System.out.println(
            "------------------------------Error Message------------------------------");
        e.printStackTrace();
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `rewrite-tsfile-tool/src/main/java/org/apache/iotdb/RewriteTsFileTool.java`
#### Snippet
```java
            "------------------------------Error Message------------------------------");
        e.printStackTrace();
        System.out.println(
            "------------------------------End Message------------------------------");
        unloadTsFiles.add(file);
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `rewrite-tsfile-tool/src/main/java/org/apache/iotdb/RewriteTsFileTool.java`
#### Snippet
```java
        continue;
      }
      System.out.println("Done");
    }
    System.out.printf(
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `rewrite-tsfile-tool/src/main/java/org/apache/iotdb/RewriteTsFileTool.java`
#### Snippet
```java
      System.out.println("Done");
    }
    System.out.printf(
        "Load %d TsFiles successfully, %d TsFiles not loaded.%n",
        size - unloadTsFiles.size(), unloadTsFiles.size());
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `rewrite-tsfile-tool/src/main/java/org/apache/iotdb/RewriteTsFileTool.java`
#### Snippet
```java
        size - unloadTsFiles.size(), unloadTsFiles.size());
    if (!unloadTsFiles.isEmpty()) {
      System.out.println("Load Error TsFiles list");
      for (File file : unloadTsFiles) {
        System.out.println(file.getPath());
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `rewrite-tsfile-tool/src/main/java/org/apache/iotdb/RewriteTsFileTool.java`
#### Snippet
```java
      System.out.println("Load Error TsFiles list");
      for (File file : unloadTsFiles) {
        System.out.println(file.getPath());
      }
    }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `rewrite-tsfile-tool/src/main/java/org/apache/iotdb/RewriteTsFileTool.java`
#### Snippet
```java
  public static void writeToIoTDB(List<File> files, Session session) {
    sortTsFiles(files);
    System.out.printf("Collect TsFiles successfully, %d files to be loaded.%n", files.size());
    System.out.println("Start Loading TsFiles...");
    if (readMode.equals("s")) {
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `rewrite-tsfile-tool/src/main/java/org/apache/iotdb/RewriteTsFileTool.java`
#### Snippet
```java
    sortTsFiles(files);
    System.out.printf("Collect TsFiles successfully, %d files to be loaded.%n", files.size());
    System.out.println("Start Loading TsFiles...");
    if (readMode.equals("s")) {
      writeTsFileSequentially(files, session);
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `rewrite-tsfile-tool/src/main/java/org/apache/iotdb/RewriteTsFileTool.java`
#### Snippet
```java
          | StatementExecutionException
          | NoMeasurementException e) {
        System.out.println(
            "------------------------------Error Message------------------------------");
        e.printStackTrace();
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `rewrite-tsfile-tool/src/main/java/org/apache/iotdb/RewriteTsFileTool.java`
#### Snippet
```java
            "------------------------------Error Message------------------------------");
        e.printStackTrace();
        System.out.println(
            "------------------------------End Message------------------------------");
      }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `rewrite-tsfile-tool/src/main/java/org/apache/iotdb/RewriteTsFileTool.java`
#### Snippet
```java
      }
    }
    System.out.println("Finish Loading TsFiles");
  }

```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `rewrite-tsfile-tool/src/main/java/org/apache/iotdb/RewriteTsFileTool.java`
#### Snippet
```java
      ignoreBrokenChunk = commandLine.hasOption("ig");
    } catch (ParseException e) {
      System.out.printf("Parse Args Error. %s%n", e.getMessage());
      priHelp(options);
    }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `rewrite-tsfile-tool/src/main/java/org/apache/iotdb/RewriteTsFileTool.java`
#### Snippet
```java
      session = new Session(host, port, user, password);
      session.open();
      System.out.printf("Connect to IoTDB %s:%s successfully.%n", host, port);
      writeToIoTDB(collectTsFiles(new File(filePath)), session);
    } catch (IoTDBConnectionException e) {
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `rewrite-tsfile-tool/src/main/java/org/apache/iotdb/RewriteTsFileTool.java`
#### Snippet
```java
      writeToIoTDB(collectTsFiles(new File(filePath)), session);
    } catch (IoTDBConnectionException e) {
      System.out.printf("Can not connect to IoTDB. %s%n", e.getMessage());
      e.printStackTrace();
    } catch (Exception e) {
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `rewrite-tsfile-tool/src/main/java/org/apache/iotdb/RewriteTsFileTool.java`
#### Snippet
```java
      e.printStackTrace();
    } catch (Exception e) {
      System.out.printf("Load Error. %s%n", e.getMessage());
      e.printStackTrace();
    } finally {
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `rewrite-tsfile-tool/src/main/java/org/apache/iotdb/RewriteTsFileTool.java`
#### Snippet
```java
          session.close();
        } catch (IoTDBConnectionException e) {
          System.out.printf("Can not connect to IoTDB. %s%n", e.getMessage());
          e.printStackTrace();
        }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/expression/util/ExpressionPrinter.java`
#### Snippet
```java
  private static void print(IExpression expression, int level) {
    if (expression instanceof IUnaryExpression) {
      System.out.println(getPrefix(level) + expression);
    } else {
      System.out.println(getPrefix(level) + expression.getType() + ":");
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/expression/util/ExpressionPrinter.java`
#### Snippet
```java
      System.out.println(getPrefix(level) + expression);
    } else {
      System.out.println(getPrefix(level) + expression.getType() + ":");
      print(((IBinaryExpression) expression).getLeft(), level + 1);
      print(((IBinaryExpression) expression).getRight(), level + 1);
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `integration-test/src/main/java/org/apache/iotdb/it/env/ConfigFactory.java`
#### Snippet
```java
          break;
        default:
          System.out.println("Unknown env type: " + env);
          System.exit(-1);
          break;
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `integration-test/src/main/java/org/apache/iotdb/it/env/EnvFactory.java`
#### Snippet
```java
            break;
          default:
            System.out.println("Unknown env type: " + envType);
            System.exit(-1);
            break;
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/hadoop/src/main/java/org/apache/iotdb/hadoop/tsfile/TsFileHelper.java`
#### Snippet
```java
    } catch (Throwable e) {
      e.printStackTrace();
      System.out.println(e.getMessage());
    }
  }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/hadoop/src/main/java/org/apache/iotdb/hadoop/tsfile/TSMRWriteExample.java`
#### Snippet
```java

    if (args.length != 3) {
      System.out.println("Please give hdfs url, input path, output path");
      return;
    }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/hadoop/src/main/java/org/apache/iotdb/hadoop/tsfile/TSMRWriteExample.java`
#### Snippet
```java
    }
    if (isSuccess) {
      System.out.println("Execute successfully");
    } else {
      System.out.println("Execute unsuccessfully");
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/hadoop/src/main/java/org/apache/iotdb/hadoop/tsfile/TSMRWriteExample.java`
#### Snippet
```java
      System.out.println("Execute successfully");
    } else {
      System.out.println("Execute unsuccessfully");
    }
  }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/hadoop/src/main/java/org/apache/iotdb/hadoop/tsfile/TSFMRReadExample.java`
#### Snippet
```java

    if (args.length != 3) {
      System.out.println("Please give hdfs url, input path, output path");
      return;
    }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/hadoop/src/main/java/org/apache/iotdb/hadoop/tsfile/TSFMRReadExample.java`
#### Snippet
```java
    }
    if (isSuccess) {
      System.out.println("Execute successfully");
    } else {
      System.out.println("Execute unsuccessfully");
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/hadoop/src/main/java/org/apache/iotdb/hadoop/tsfile/TSFMRReadExample.java`
#### Snippet
```java
      System.out.println("Execute successfully");
    } else {
      System.out.println("Execute unsuccessfully");
    }
  }
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/trigger/src/main/java/org/apache/iotdb/trigger/SimpleTrigger.java`
#### Snippet
```java
  @Override
  public boolean fire(Tablet tablet) {
    System.out.println("receive a tablet, device name is " + tablet.deviceId);
    System.out.println("measurements are: ");
    tablet
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/trigger/src/main/java/org/apache/iotdb/trigger/SimpleTrigger.java`
#### Snippet
```java
  public boolean fire(Tablet tablet) {
    System.out.println("receive a tablet, device name is " + tablet.deviceId);
    System.out.println("measurements are: ");
    tablet
        .getSchemas()
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/trigger/src/main/java/org/apache/iotdb/trigger/SimpleTrigger.java`
#### Snippet
```java
    tablet
        .getSchemas()
        .forEach(measurementSchema -> System.out.println(measurementSchema.getMeasurementId()));
    System.out.println("time are: " + Arrays.toString(tablet.timestamps));
    return true;
```

### RuleId[ruleID=SystemOutErr]
Uses of `System.out` should probably be replaced with more robust logging
in `example/trigger/src/main/java/org/apache/iotdb/trigger/SimpleTrigger.java`
#### Snippet
```java
        .getSchemas()
        .forEach(measurementSchema -> System.out.println(measurementSchema.getMeasurementId()));
    System.out.println("time are: " + Arrays.toString(tablet.timestamps));
    return true;
  }
```

## RuleId[ruleID=ReplaceAllDot]
### RuleId[ruleID=ReplaceAllDot]
Suspicious regex expression "." in call to 'split()'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
    try {
      String version = client.getProperties().getVersion();
      String[] versions = version.split(".");
      if (versions.length >= 2) {
        major_version = Integer.valueOf(versions[0]);
```

### RuleId[ruleID=ReplaceAllDot]
Suspicious regex expression "." in call to 'split()'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
    try {
      String version = client.getProperties().getVersion();
      String[] versions = version.split(".");
      if (versions.length >= 2) {
        minor_version = Integer.valueOf(versions[1]);
```

### RuleId[ruleID=ReplaceAllDot]
File.separator is used as a regex; will not work on Windows
in `server/src/main/java/org/apache/iotdb/db/engine/snapshot/SnapshotTaker.java`
#### Snippet
```java
    // ... data (un)sequence sgName dataRegionId timePartition tsFileName
    String[] splittedPath =
        tsFile.getAbsolutePath().split(File.separator.equals("\\") ? "\\\\" : File.separator);
    // snapshot dir will be like
    // ... data snapshot snapshotId (un)sequence sgName dataRegionId timePartition
```

## RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `node-commons/src/main/java/org/apache/iotdb/commons/auth/role/LocalFileRoleAccessor.java`
#### Snippet
```java
      Set<String> set = new HashSet<>();
      for (String fileName : names) {
        set.add(fileName.replace(IoTDBConstant.PROFILE_SUFFIX, "").replace(TEMP_SUFFIX, ""));
      }
      retList.addAll(set);
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `node-commons/src/main/java/org/apache/iotdb/commons/auth/role/LocalFileRoleAccessor.java`
#### Snippet
```java
      Set<String> set = new HashSet<>();
      for (String fileName : names) {
        set.add(fileName.replace(IoTDBConstant.PROFILE_SUFFIX, "").replace(TEMP_SUFFIX, ""));
      }
      retList.addAll(set);
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `node-commons/src/main/java/org/apache/iotdb/commons/auth/user/LocalFileUserAccessor.java`
#### Snippet
```java
      Set<String> set = new HashSet<>();
      for (String fileName : names) {
        set.add(fileName.replace(IoTDBConstant.PROFILE_SUFFIX, "").replace(TEMP_SUFFIX, ""));
      }
      retList.addAll(set);
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `node-commons/src/main/java/org/apache/iotdb/commons/auth/user/LocalFileUserAccessor.java`
#### Snippet
```java
      Set<String> set = new HashSet<>();
      for (String fileName : names) {
        set.add(fileName.replace(IoTDBConstant.PROFILE_SUFFIX, "").replace(TEMP_SUFFIX, ""));
      }
      retList.addAll(set);
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/PathUtils.java`
#### Snippet
```java

  private static boolean checkBackQuotes(String src) {
    int num = src.length() - src.replace("`", "").length();
    if (num % 2 == 1) {
      return false;
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/PathUtils.java`
#### Snippet
```java
      return false;
    }
    return src.length() == (src.replace("``", "").length() + num);
  }
}
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `node-commons/src/main/java/org/apache/iotdb/commons/path/PartialPath.java`
#### Snippet
```java
    } else {
      if (patternNode.contains(ONE_LEVEL_PATH_WILDCARD)) {
        if (Pattern.matches(patternNode.replace("*", ".*"), pathNode)) {
          isMatch = matchPath(pathNodes, pathIndex + 1, patternIndex + 1, false, pathIsPrefix);
        }
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `node-commons/src/main/java/org/apache/iotdb/commons/schema/tree/AbstractTreeVisitor.java`
#### Snippet
```java
        optimizedNodes.add(MULTI_LEVEL_PATH_WILDCARD);
      } else if (rawNode.contains(ONE_LEVEL_PATH_WILDCARD)) {
        optimizedNodes.add(rawNode.replace("*", ".*"));
      } else {
        optimizedNodes.add(rawNode);
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`matches()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
                  || "N".equalsIgnoreCase((String) parameterObj)) {
                setBoolean(parameterIndex, false);
              } else if (((String) parameterObj).matches("-?\\d+\\.?\\d*")) {
                setBoolean(parameterIndex, !((String) parameterObj).matches("-?[0]+[.]*[0]*"));
              } else {
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`matches()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
                setBoolean(parameterIndex, false);
              } else if (((String) parameterObj).matches("-?\\d+\\.?\\d*")) {
                setBoolean(parameterIndex, !((String) parameterObj).matches("-?[0]+[.]*[0]*"));
              } else {
                throw new SQLException(
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
    if (catalog != null && catalog.length() > 0) {
      if (catalog.contains("%")) {
        catalog = catalog.replace("%", "*");
      }
      sql = sql + " " + catalog;
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
    } else if (schemaPattern != null && schemaPattern.length() > 0) {
      if (schemaPattern.contains("%")) {
        schemaPattern = schemaPattern.replace("%", "*");
      }
      sql = sql + " " + schemaPattern;
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
        && tableNamePattern.length() > 0) {
      if (tableNamePattern.contains("%")) {
        tableNamePattern = tableNamePattern.replace("%", "*");
      }
      sql = sql + "." + tableNamePattern;
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
    try {
      String version = client.getProperties().getVersion();
      String[] versions = version.split(".");
      if (versions.length >= 2) {
        major_version = Integer.valueOf(versions[0]);
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
    if (catalog != null && catalog.length() > 0) {
      if (catalog.contains("%")) {
        catalog = catalog.replace("%", "*");
      }
      sql = sql + " " + catalog;
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
    } else if (schemaPattern != null && schemaPattern.length() > 0) {
      if (schemaPattern.contains("%")) {
        schemaPattern = schemaPattern.replace("%", "*");
      }
      sql = sql + " " + schemaPattern;
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
        && tableNamePattern.length() > 0) {
      if (tableNamePattern.contains("%")) {
        tableNamePattern = tableNamePattern.replace("%", "*");
      }
      sql = sql + "." + tableNamePattern;
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
    try {
      String version = client.getProperties().getVersion();
      String[] versions = version.split(".");
      if (versions.length >= 2) {
        minor_version = Integer.valueOf(versions[1]);
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
    if (catalog != null && catalog.length() > 0) {
      if (catalog.contains("%")) {
        catalog = catalog.replace("%", "*");
      }
      sql = sql + " " + catalog;
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
    } else if (schemaPattern != null && schemaPattern.length() > 0) {
      if (schemaPattern.contains("%")) {
        schemaPattern = schemaPattern.replace("%", "*");
      }
      sql = sql + " " + schemaPattern;
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
        && tableNamePattern.length() > 0) {
      if (tableNamePattern.contains("%")) {
        tableNamePattern = tableNamePattern.replace("%", "*");
      }
      sql = sql + "." + tableNamePattern;
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
    if (catalog != null && catalog.length() > 0) {
      if (catalog.contains("%")) {
        catalog = catalog.replace("%", "*");
      }
      sql = sql + " " + catalog;
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
    } else if (schemaPattern != null && schemaPattern.length() > 0) {
      if (schemaPattern.contains("%")) {
        schemaPattern = schemaPattern.replace("%", "*");
      }
      sql = sql + " " + schemaPattern;
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
        && tableNamePattern.length() > 0) {
      if (tableNamePattern.contains("%")) {
        tableNamePattern = tableNamePattern.replace("%", "*");
      }
      sql = sql + "." + tableNamePattern;
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `cli/src/main/java/org/apache/iotdb/tool/ImportCsv.java`
#### Snippet
```java
      if (matcher.find()) {
        type = matcher.group();
        headerNameWithoutType = headerName.replace("(" + type + ")", "").replaceAll("\\s+", "");
        headerNameMap.put(headerNameWithoutType, headerName);
        headerTypeMap.put(headerNameWithoutType, getType(type));
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `server/src/main/java/org/apache/iotdb/db/qp/strategy/SQLParseError.java`
#### Snippet
```java
      IntervalSet expectedTokens = ((Parser) recognizer).getExpectedTokens();
      String expectedTokensString = expectedTokens.toString(recognizer.getVocabulary());
      String trimmed = expectedTokensString.replace(" ", "");
      Set<String> expectedTokenNames =
          new HashSet<>(Arrays.asList(trimmed.substring(1, trimmed.length() - 1).split(",")));
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/parser/ASTVisitor.java`
#### Snippet
```java
      if (src.charAt(0) == '\"' && src.charAt(src.length() - 1) == '\"') {
        // replace "" with "
        String replaced = unWrappedString.replace("\"\"", "\"");
        return replaced.length() == 0 ? "" : replaced;
      }
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/parser/ASTVisitor.java`
#### Snippet
```java
      if ((src.charAt(0) == '\'' && src.charAt(src.length() - 1) == '\'')) {
        // replace '' with '
        String replaced = unWrappedString.replace("''", "'");
        return replaced.length() == 0 ? "" : replaced;
      }
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/parser/ASTVisitor.java`
#### Snippet
```java
        && src.endsWith(TsFileConstant.BACK_QUOTE_STRING)) {
      return src.substring(1, src.length() - 1)
          .replace(TsFileConstant.DOUBLE_BACK_QUOTE_STRING, TsFileConstant.BACK_QUOTE_STRING);
    }
    checkIdentifier(src);
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/statement/literal/StringLiteral.java`
#### Snippet
```java
  @Override
  public String toString() {
    return String.format("\"%s\"", value.replace("\"", "'"));
  }
}
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/expression/unary/LikeExpression.java`
#### Snippet
```java
                      patternBuilder.substring(
                          patternBuilder.length() - 2, patternBuilder.length()))) {
        patternBuilder.append(ch.replace("%", ".*?").replace("_", "."));
      } else {
        patternBuilder.append(ch);
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/expression/unary/LikeExpression.java`
#### Snippet
```java
                      patternBuilder.substring(
                          patternBuilder.length() - 2, patternBuilder.length()))) {
        patternBuilder.append(ch.replace("%", ".*?").replace("_", "."));
      } else {
        patternBuilder.append(ch);
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `server/src/main/java/org/apache/iotdb/db/auth/ClusterAuthorityFetcher.java`
#### Snippet
```java
  private PathPrivilege toPathPrivilege(String path, String privilege) {
    PathPrivilege pathPrivilege = new PathPrivilege();
    String[] privileges = privilege.replace(" ", "").split(",");
    Set<Integer> privilegeIds = new HashSet<>();
    for (String p : privileges) {
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `server/src/main/java/org/apache/iotdb/db/tools/upgrade/TsFileOnlineUpgradeTool.java`
#### Snippet
```java
  @Override
  public String upgradeTsFileName(String oldTsFileName) {
    String[] name = oldTsFileName.split(TsFileConstant.TSFILE_SUFFIX);
    return name[0] + "-0" + TsFileConstant.TSFILE_SUFFIX;
  }
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `server/src/main/java/org/apache/iotdb/db/utils/OpenFileNumUtil.java`
#### Snippet
```java
  private static void countOneFile(
      String line, int pid, EnumMap<OpenFileNumStatistics, Integer> resultMap) {
    String[] temp = line.split("\\s+");
    if (!line.contains(Integer.toString(pid)) || temp.length <= 8) {
      return;
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `server/src/main/java/org/apache/iotdb/db/utils/OpenFileNumUtil.java`
#### Snippet
```java
        while ((line = in1.readLine()) != null) {
          line = line.trim();
          String[] temp = line.split("\\s+");
          if (temp.length > 1 && isNumeric(temp[1])) {
            iotdbPid = Integer.parseInt(temp[1]);
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `server/src/main/java/org/apache/iotdb/db/engine/snapshot/SnapshotFileSet.java`
#### Snippet
```java
  public static final String[] DATA_FILE_SUFFIX =
      new String[] {
        TsFileConstant.TSFILE_SUFFIX.replace(".", ""),
        TsFileResource.RESOURCE_SUFFIX.replace(".", ""),
        ModificationFile.FILE_SUFFIX.replace(".", ""),
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `server/src/main/java/org/apache/iotdb/db/engine/snapshot/SnapshotFileSet.java`
#### Snippet
```java
      new String[] {
        TsFileConstant.TSFILE_SUFFIX.replace(".", ""),
        TsFileResource.RESOURCE_SUFFIX.replace(".", ""),
        ModificationFile.FILE_SUFFIX.replace(".", ""),
      };
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `server/src/main/java/org/apache/iotdb/db/engine/snapshot/SnapshotFileSet.java`
#### Snippet
```java
        TsFileConstant.TSFILE_SUFFIX.replace(".", ""),
        TsFileResource.RESOURCE_SUFFIX.replace(".", ""),
        ModificationFile.FILE_SUFFIX.replace(".", ""),
      };

```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/log/CompactionLogAnalyzer.java`
#### Snippet
```java
    if (isSeqSource) {
      String targetFilePath =
          oldFilePath.replace(
              TsFileConstant.TSFILE_SUFFIX,
              TsFileConstant.TSFILE_SUFFIX
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/log/CompactionLogAnalyzer.java`
#### Snippet
```java
        String fileInfo;
        if (currLine.startsWith(STR_SOURCE_FILES)) {
          fileInfo = currLine.replace(STR_SOURCE_FILES + TsFileIdentifier.INFO_SEPARATOR, "");
          analyzeFilePath(false, fileInfo);
        } else {
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/log/CompactionLogAnalyzer.java`
#### Snippet
```java
          analyzeFilePath(false, fileInfo);
        } else {
          fileInfo = currLine.replace(STR_TARGET_FILES + TsFileIdentifier.INFO_SEPARATOR, "");
          analyzeFilePath(true, fileInfo);
        }
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/task/CompactionRecoverTask.java`
#### Snippet
```java
                  tmpTargetFile
                      .getPath()
                      .replace(
                          TsFileConstant.TSFILE_SUFFIX
                              + IoTDBConstant.CROSS_COMPACTION_TMP_FILE_SUFFIX_FROM_OLD,
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/task/CompactionRecoverTask.java`
#### Snippet
```java
                              .get(i)
                              .getFilePath()
                              .replace(
                                  TsFileConstant.TSFILE_SUFFIX
                                      + IoTDBConstant.CROSS_COMPACTION_TMP_FILE_SUFFIX_FROM_OLD,
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileManager.java`
#### Snippet
```java
  // ({systemTime}-{versionNum}-{innerCompactionNum}-{crossCompactionNum}.tsfile)
  public static int compareFileName(File o1, File o2) {
    String[] items1 = o1.getName().replace(TSFILE_SUFFIX, "").split(FILE_NAME_SEPARATOR);
    String[] items2 = o2.getName().replace(TSFILE_SUFFIX, "").split(FILE_NAME_SEPARATOR);
    long ver1 = Long.parseLong(items1[0]);
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileManager.java`
#### Snippet
```java
  public static int compareFileName(File o1, File o2) {
    String[] items1 = o1.getName().replace(TSFILE_SUFFIX, "").split(FILE_NAME_SEPARATOR);
    String[] items2 = o2.getName().replace(TSFILE_SUFFIX, "").split(FILE_NAME_SEPARATOR);
    long ver1 = Long.parseLong(items1[0]);
    long ver2 = Long.parseLong(items2[0]);
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileResource.java`
#### Snippet
```java
  public static int compareFileName(TsFileResource o1, TsFileResource o2) {
    String[] items1 =
        o1.getTsFile().getName().replace(TSFILE_SUFFIX, "").split(FILE_NAME_SEPARATOR);
    String[] items2 =
        o2.getTsFile().getName().replace(TSFILE_SUFFIX, "").split(FILE_NAME_SEPARATOR);
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileResource.java`
#### Snippet
```java
        o1.getTsFile().getName().replace(TSFILE_SUFFIX, "").split(FILE_NAME_SEPARATOR);
    String[] items2 =
        o2.getTsFile().getName().replace(TSFILE_SUFFIX, "").split(FILE_NAME_SEPARATOR);
    long ver1 = Long.parseLong(items1[0]);
    long ver2 = Long.parseLong(items2[0]);
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `server/src/main/java/org/apache/iotdb/db/service/thrift/impl/DataNodeInternalRPCServiceImpl.java`
#### Snippet
```java
        s.getGroupByTimeComponent().setLeftCRightO(true);
      }
      executedSQL = String.join(" ", s.constructFormattedSQL().split("\n")).replaceAll(" +", " ");

      QUERY_FREQUENCY_RECORDER.incrementAndGet();
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/DataRegion.java`
#### Snippet
```java
  /** check if the tsfile's time is smaller than system current time */
  private void checkTsFileTime(File tsFile) throws DataRegionException {
    String[] items = tsFile.getName().replace(TSFILE_SUFFIX, "").split(FILE_NAME_SEPARATOR);
    long fileTime = Long.parseLong(items[0]);
    long currentTime = System.currentTimeMillis();
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/DataRegion.java`
#### Snippet
```java
  // ({systemTime}-{versionNum}-{mergeNum}.tsfile)
  private int compareFileName(File o1, File o2) {
    String[] items1 = o1.getName().replace(TSFILE_SUFFIX, "").split(FILE_NAME_SEPARATOR);
    String[] items2 = o2.getName().replace(TSFILE_SUFFIX, "").split(FILE_NAME_SEPARATOR);
    long ver1 = Long.parseLong(items1[0]);
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/DataRegion.java`
#### Snippet
```java
  private int compareFileName(File o1, File o2) {
    String[] items1 = o1.getName().replace(TSFILE_SUFFIX, "").split(FILE_NAME_SEPARATOR);
    String[] items2 = o2.getName().replace(TSFILE_SUFFIX, "").split(FILE_NAME_SEPARATOR);
    long ver1 = Long.parseLong(items1[0]);
    long ver2 = Long.parseLong(items2[0]);
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/traverser/Traverser.java`
#### Snippet
```java
  protected void processOneLevelWildcard(IMNode node, int idx, int level) throws MetadataException {
    boolean multiLevelWildcard = nodes[idx].equals(MULTI_LEVEL_PATH_WILDCARD);
    String targetNameRegex = nodes[idx + 1].replace("*", ".*");

    if (isInTemplate) {
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `server/src/main/java/org/apache/iotdb/db/protocol/rest/filter/AuthorizationFilter.java`
#### Snippet
```java
      ContainerRequestContext containerRequestContext, String authorizationHeader) {

    String decoded = Base64.decodeAsString(authorizationHeader.replace("Basic ", ""));
    // todo: support special chars in username and password
    String[] split = decoded.split(":");
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/util/FilterUtils.java`
#### Snippet
```java
    String filterString = filter.toString();
    if (filter instanceof ValueFilter.ValueEq) {
      return filterString.split("== ")[1];
    } else if (filter instanceof ValueFilter.ValueNotEq) {
      return filterString.split("!= ")[1];
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/util/FilterUtils.java`
#### Snippet
```java
      return filterString.split("== ")[1];
    } else if (filter instanceof ValueFilter.ValueNotEq) {
      return filterString.split("!= ")[1];
    } else if (filter instanceof ValueFilter.ValueLtEq) {
      return filterString.split("<= ")[1];
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/util/FilterUtils.java`
#### Snippet
```java
      return filterString.split("!= ")[1];
    } else if (filter instanceof ValueFilter.ValueLtEq) {
      return filterString.split("<= ")[1];
    } else if (filter instanceof ValueFilter.ValueLt) {
      return filterString.split("< ")[1];
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/util/FilterUtils.java`
#### Snippet
```java
      return filterString.split("<= ")[1];
    } else if (filter instanceof ValueFilter.ValueLt) {
      return filterString.split("< ")[1];
    } else if (filter instanceof ValueFilter.ValueGtEq) {
      return filterString.split(">= ")[1];
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/util/FilterUtils.java`
#### Snippet
```java
      return filterString.split("< ")[1];
    } else if (filter instanceof ValueFilter.ValueGtEq) {
      return filterString.split(">= ")[1];
    } else if (filter instanceof ValueFilter.ValueGt) {
      return filterString.split("> ")[1];
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/util/FilterUtils.java`
#### Snippet
```java
      return filterString.split(">= ")[1];
    } else if (filter instanceof ValueFilter.ValueGt) {
      return filterString.split("> ")[1];
    } else {
      throw new UnSupportedDataTypeException("Unsupported filter :" + filter);
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `library-udf/src/main/java/org/apache/iotdb/library/util/Util.java`
#### Snippet
```java
    long unit = 0;
    s = s.toLowerCase();
    s = s.replaceAll(" ", "");
    if (s.endsWith("ms")) {
      unit = 1;
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/parser/PathParseError.java`
#### Snippet
```java
      IntervalSet expectedTokens = ((Parser) recognizer).getExpectedTokens();
      String expectedTokensString = expectedTokens.toString(recognizer.getVocabulary());
      String trimmed = expectedTokensString.replace(" ", "");
      Set<String> expectedTokenNames =
          new HashSet<>(Arrays.asList(trimmed.substring(1, trimmed.length() - 1).split(",")));
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/filter/operator/Like.java`
#### Snippet
```java
                        patternStrBuild.substring(
                            patternStrBuild.length() - 2, patternStrBuild.length())))) {
          String replaceStr = ch.replace("%", ".*?").replace("_", ".");
          patternStrBuild.append(replaceStr);
        } else {
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/filter/operator/Like.java`
#### Snippet
```java
                        patternStrBuild.substring(
                            patternStrBuild.length() - 2, patternStrBuild.length())))) {
          String replaceStr = ch.replace("%", ".*?").replace("_", ".");
          patternStrBuild.append(replaceStr);
        } else {
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/RSchemaUtils.java`
#### Snippet
```java
  public static PartialPath getPartialPathFromInnerPath(String path, char level) {
    String pathWithoutLevel = path.replace(PATH_SEPARATOR + level, PATH_SEPARATOR);
    String[] nodes = pathWithoutLevel.split(ESCAPE_PATH_SEPARATOR);
    nodes[0] = IoTDBConstant.PATH_ROOT;
    return new PartialPath(nodes);
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/RSchemaUtils.java`
#### Snippet
```java
        new String(
            Objects.requireNonNull(BytesUtils.subBytes(rocksdbKey, 1, rocksdbKey.length - 1)));
    String[] nodes = rawKey.split(ESCAPE_PATH_SEPARATOR);
    nodes[0] = ROOT_STRING;
    for (int i = 1; i < nodes.length; i++) {
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `metrics/interface/src/main/java/org/apache/iotdb/metrics/metricsets/jvm/JvmThreadMetrics.java`
#### Snippet
```java

  private static String getStateTagValue(Thread.State state) {
    return state.name().toLowerCase().replace("_", "-");
  }
}
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `metrics/dropwizard-metrics/src/main/java/org/apache/iotdb/metrics/dropwizard/DropwizardMetricNameTool.java`
#### Snippet
```java
    int lastIndex = flatString.indexOf("}");
    if (firstIndex == -1 || lastIndex == -1) {
      name = flatString.replaceAll("[^a-zA-Z0-9:_\\]\\[]", "_");
      metricInfo = new MetricInfo(metricType, name);
    } else {
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `metrics/dropwizard-metrics/src/main/java/org/apache/iotdb/metrics/dropwizard/DropwizardMetricNameTool.java`
#### Snippet
```java
      metricInfo = new MetricInfo(metricType, name);
    } else {
      name = flatString.substring(0, firstIndex).replaceAll("[^a-zA-Z0-9:_\\]\\[]", "_");
      String tagsPart = flatString.substring(firstIndex + 1, lastIndex);
      if (0 == tagsPart.length()) {
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `metrics/dropwizard-metrics/src/main/java/org/apache/iotdb/metrics/dropwizard/DropwizardMetricNameTool.java`
#### Snippet
```java
    String name = metricInfo.getName();
    Map<String, String> tags = metricInfo.getTags();
    return name.replaceAll("\\{|\\}", "")
        + "{"
        + tags.entrySet().stream()
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `metrics/dropwizard-metrics/src/main/java/org/apache/iotdb/metrics/dropwizard/DropwizardMetricNameTool.java`
#### Snippet
```java
            .map(
                t ->
                    t.getKey().replace(TAG_SEPARATOR, "")
                        + TAG_SEPARATOR
                        + t.getValue().replace(TAG_SEPARATOR, ""))
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `metrics/dropwizard-metrics/src/main/java/org/apache/iotdb/metrics/dropwizard/DropwizardMetricNameTool.java`
#### Snippet
```java
                    t.getKey().replace(TAG_SEPARATOR, "")
                        + TAG_SEPARATOR
                        + t.getValue().replace(TAG_SEPARATOR, ""))
            .collect(Collectors.joining(TAG_SEPARATOR))
            .replaceAll("\\{|\\}", "")
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `metrics/dropwizard-metrics/src/main/java/org/apache/iotdb/metrics/dropwizard/DropwizardMetricNameTool.java`
#### Snippet
```java
                        + t.getValue().replace(TAG_SEPARATOR, ""))
            .collect(Collectors.joining(TAG_SEPARATOR))
            .replaceAll("\\{|\\}", "")
        + "}";
  }
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replace()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `grafana-connector/src/main/java/org/apache/iotdb/web/grafana/interceptor/LoginInterceptor.java`
#### Snippet
```java
          new String(
              Base64Utils.decode(
                  authorization.replace("Basic ", "").getBytes(StandardCharsets.UTF_8)));
      String[] authorizationHeader = authorization.split(":");
      if (authorizationHeader.length != 2
```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `grafana-connector/src/main/java/org/apache/iotdb/web/grafana/dao/impl/BasicDaoImpl.java`
#### Snippet
```java
        timestampRadioX = 1L;
    }
    logger.info("Use timestamp precision {}", timestampPrecision.replaceAll("[\n\r\t]", "_"));
  }

```

### RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `grafana-connector/src/main/java/org/apache/iotdb/web/grafana/controller/DatabaseConnectController.java`
#### Snippet
```java
      return result.toString();
    } catch (Exception e) {
      logger.error("/query failed, request body is {}", json.replaceAll("[\n\r\t]", "_"), e);
    }
    return null;
```

## RuleId[ruleID=NonProtectedConstructorInAbstractClass]
### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `BasicUserManager()` of an abstract class should not be declared 'public'
in `node-commons/src/main/java/org/apache/iotdb/commons/auth/user/BasicUserManager.java`
#### Snippet
```java
   * @throws AuthException Authentication Exception
   */
  public BasicUserManager(IUserAccessor accessor) throws AuthException {
    this.userMap = new HashMap<>();
    this.accessor = accessor;
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `PipeInfo()` of an abstract class should not be declared 'public'
in `node-commons/src/main/java/org/apache/iotdb/commons/sync/pipe/PipeInfo.java`
#### Snippet
```java
  }

  public PipeInfo(String pipeName, String pipeSinkName, PipeStatus status, long createTime) {
    this.pipeName = pipeName;
    this.pipeSinkName = pipeSinkName;
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `PipeInfo()` of an abstract class should not be declared 'public'
in `node-commons/src/main/java/org/apache/iotdb/commons/sync/pipe/PipeInfo.java`
#### Snippet
```java
  protected PipeInfo() {}

  public PipeInfo(String pipeName, String pipeSinkName, long createTime) {
    this.pipeName = pipeName;
    this.pipeSinkName = pipeSinkName;
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `AbstractThriftServiceThread()` of an abstract class should not be declared 'public'
in `node-commons/src/main/java/org/apache/iotdb/commons/service/AbstractThriftServiceThread.java`
#### Snippet
```java
  /** for asynced ThriftService. */
  @SuppressWarnings("squid:S107")
  public AbstractThriftServiceThread(
      TBaseAsyncProcessor processor,
      String serviceName,
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `AbstractThriftServiceThread()` of an abstract class should not be declared 'public'
in `node-commons/src/main/java/org/apache/iotdb/commons/service/AbstractThriftServiceThread.java`
#### Snippet
```java
  /** for synced ThriftServiceThread */
  @SuppressWarnings("squid:S107")
  public AbstractThriftServiceThread(
      TProcessor processor,
      String serviceName,
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `Partition()` of an abstract class should not be declared 'public'
in `node-commons/src/main/java/org/apache/iotdb/commons/partition/Partition.java`
#### Snippet
```java
  private final SeriesPartitionExecutor executor;

  public Partition(String seriesSlotExecutorName, int seriesPartitionSlotNum) {
    this.seriesSlotExecutorName = seriesSlotExecutorName;
    this.seriesPartitionSlotNum = seriesPartitionSlotNum;
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `SeriesPartitionExecutor()` of an abstract class should not be declared 'public'
in `node-commons/src/main/java/org/apache/iotdb/commons/partition/executor/SeriesPartitionExecutor.java`
#### Snippet
```java
  protected final int seriesPartitionSlotNum;

  public SeriesPartitionExecutor(int seriesPartitionSlotNum) {
    this.seriesPartitionSlotNum = seriesPartitionSlotNum;
  }
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `TemplateNode()` of an abstract class should not be declared 'public'
in `session/src/main/java/org/apache/iotdb/session/template/TemplateNode.java`
#### Snippet
```java
  private String name;

  public TemplateNode(String name) {
    this.name = name;
  }
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `AbstractTsFileTool()` of an abstract class should not be declared 'public'
in `cli/src/main/java/org/apache/iotdb/tool/AbstractTsFileTool.java`
#### Snippet
```java
  protected static Session session;

  public AbstractTsFileTool() {}

  protected static String checkRequiredArg(String arg, String name, CommandLine commandLine)
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `AbstractCsvTool()` of an abstract class should not be declared 'public'
in `cli/src/main/java/org/apache/iotdb/tool/AbstractCsvTool.java`
#### Snippet
```java
  protected static Session session;

  public AbstractCsvTool() {}

  protected static String checkRequiredArg(String arg, String name, CommandLine commandLine)
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `AbstractIoTDBJDBCResultSet()` of an abstract class should not be declared 'public'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/AbstractIoTDBJDBCResultSet.java`
#### Snippet
```java
  private BitSet aliasColumnMap;

  public AbstractIoTDBJDBCResultSet(
      Statement statement,
      List<String> columnNameList,
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `AbstractIoTDBJDBCResultSet()` of an abstract class should not be declared 'public'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/AbstractIoTDBJDBCResultSet.java`
#### Snippet
```java
  }

  public AbstractIoTDBJDBCResultSet(
      Statement statement,
      List<String> columnNameList,
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `BasicLSMManager()` of an abstract class should not be declared 'public'
in `schema-engine-tag/src/main/java/org/apache/iotdb/lsm/manager/BasicLSMManager.java`
#### Snippet
```java
  LevelProcessorChain<T, R, C> levelProcessChain;

  public BasicLSMManager() {}

  /**
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `WALManager()` of an abstract class should not be declared 'public'
in `schema-engine-tag/src/main/java/org/apache/iotdb/lsm/manager/WALManager.java`
#### Snippet
```java
  private boolean recover;

  public WALManager(String schemaDirPath) {
    this.walDirPath = schemaDirPath;
  }
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `WALManager()` of an abstract class should not be declared 'public'
in `schema-engine-tag/src/main/java/org/apache/iotdb/lsm/manager/WALManager.java`
#### Snippet
```java
  }

  public WALManager(
      String walDirPath,
      String walFileName,
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `QueryPlan()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/qp/physical/crud/QueryPlan.java`
#### Snippet
```java
  private boolean withoutAllNull;

  public QueryPlan() {
    super(Operator.OperatorType.QUERY);
    setQuery(true);
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `PlanNode()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/PlanNode.java`
#### Snippet
```java
  private PlanNodeId id;

  public PlanNode(PlanNodeId id) {
    requireNonNull(id, "id is null");
    this.id = id;
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `SinkNode()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/sink/SinkNode.java`
#### Snippet
```java
public abstract class SinkNode extends PlanNode implements AutoCloseable {

  public SinkNode(PlanNodeId id) {
    super(id);
  }
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `SourceNode()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/source/SourceNode.java`
#### Snippet
```java
public abstract class SourceNode extends PlanNode implements AutoCloseable, IPartitionRelatedNode {

  public SourceNode(PlanNodeId id) {
    super(id);
  }
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `SeriesAggregationSourceNode()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/source/SeriesAggregationSourceNode.java`
#### Snippet
```java
  @Nullable protected GroupByTimeParameter groupByTimeParameter;

  public SeriesAggregationSourceNode(
      PlanNodeId id, List<AggregationDescriptor> aggregationDescriptorList) {
    super(id);
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `SeriesSourceNode()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/source/SeriesSourceNode.java`
#### Snippet
```java

public abstract class SeriesSourceNode extends SourceNode {
  public SeriesSourceNode(PlanNodeId id) {
    super(id);
  }
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `SingleChildProcessNode()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/process/SingleChildProcessNode.java`
#### Snippet
```java
  protected PlanNode child;

  public SingleChildProcessNode(PlanNodeId id) {
    super(id);
  }
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `SingleChildProcessNode()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/process/SingleChildProcessNode.java`
#### Snippet
```java
  }

  public SingleChildProcessNode(PlanNodeId id, PlanNode child) {
    super(id);
    this.child = child;
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `MultiChildProcessNode()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/process/MultiChildProcessNode.java`
#### Snippet
```java
  }

  public MultiChildProcessNode(PlanNodeId id) {
    super(id);
    this.children = new ArrayList<>();
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `MultiChildProcessNode()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/process/MultiChildProcessNode.java`
#### Snippet
```java
  protected List<PlanNode> children;

  public MultiChildProcessNode(PlanNodeId id, List<PlanNode> children) {
    super(id);
    this.children = children;
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `ProcessNode()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/process/ProcessNode.java`
#### Snippet
```java
public abstract class ProcessNode extends PlanNode {

  public ProcessNode(PlanNodeId id) {
    super(id);
  }
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `AbstractSchemaMergeNode()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/metedata/read/AbstractSchemaMergeNode.java`
#### Snippet
```java
  private final List<PlanNode> children;

  public AbstractSchemaMergeNode(PlanNodeId id) {
    super(id);
    children = new ArrayList<>();
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `AbstractFragInsStateTracker()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/scheduler/AbstractFragInsStateTracker.java`
#### Snippet
```java
      internalServiceClientManager;

  public AbstractFragInsStateTracker(
      QueryStateMachine stateMachine,
      ScheduledExecutorService scheduledExecutor,
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `AbstractIntoIterator()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/statement/component/IntoComponent.java`
#### Snippet
```java
    protected int measurementIndex;

    public AbstractIntoIterator(
        List<IntoItem> intoItems,
        boolean isDeviceExistPlaceholder,
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `SchemaNode()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/mpp/common/schematree/node/SchemaNode.java`
#### Snippet
```java
  protected final String name;

  public SchemaNode(String name) {
    this.name = name;
  }
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `SchemaTreeVisitor()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/mpp/common/schematree/visitor/SchemaTreeVisitor.java`
#### Snippet
```java
    extends AbstractTreeVisitorWithLimitOffset<SchemaNode, R> {

  public SchemaTreeVisitor(
      SchemaNode root, PartialPath pathPattern, int limit, int offset, boolean isPrefixMatch) {
    super(root, pathPattern, limit, offset, isPrefixMatch);
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `Driver()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/driver/Driver.java`
#### Snippet
```java
  }

  public Driver(Operator root, ISinkHandle sinkHandle, DriverContext driverContext) {
    checkNotNull(root, "root Operator should not be null");
    checkNotNull(sinkHandle, "SinkHandle should not be null");
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `AbstractSeriesAggregationScanOperator()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/source/AbstractSeriesAggregationScanOperator.java`
#### Snippet
```java
  private final long maxReturnSize;

  public AbstractSeriesAggregationScanOperator(
      PlanNodeId sourceId,
      OperatorContext context,
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `SingleInputAggregationOperator()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/SingleInputAggregationOperator.java`
#### Snippet
```java
  protected final long maxReturnSize;

  public SingleInputAggregationOperator(
      OperatorContext operatorContext,
      List<Aggregator> aggregators,
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `AbstractIntoOperator()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/AbstractIntoOperator.java`
#### Snippet
```java
  private DataNodeInternalClient client;

  public AbstractIntoOperator(
      OperatorContext operatorContext,
      Operator child,
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `AbstractDriverThread()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/schedule/AbstractDriverThread.java`
#### Snippet
```java
  private volatile boolean closed;

  public AbstractDriverThread(
      String workerId,
      ThreadGroup tg,
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `IndexedBlockingQueue()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/schedule/queue/IndexedBlockingQueue.java`
#### Snippet
```java
   * @throws IllegalArgumentException if maxCapacity <= 0.
   */
  public IndexedBlockingQueue(int maxCapacity, E queryHolder) {
    this.MAX_CAPACITY = maxCapacity;
    this.queryHolder = queryHolder;
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `SlidingWindowAggregator()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/mpp/aggregation/slidingwindow/SlidingWindowAggregator.java`
#### Snippet
```java
  protected TimeRange curTimeRange;

  public SlidingWindowAggregator(
      Accumulator accumulator, List<InputLocation[]> inputLocationList, AggregationStep step) {
    super(accumulator, step, inputLocationList);
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `ColumnTransformer()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/column/ColumnTransformer.java`
#### Snippet
```java
  protected int referenceCount;

  public ColumnTransformer(Type returnType) {
    this.returnType = returnType;
    this.columnCache = new ColumnCache();
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `LeafColumnTransformer()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/column/leaf/LeafColumnTransformer.java`
#### Snippet
```java

public abstract class LeafColumnTransformer extends ColumnTransformer {
  public LeafColumnTransformer(Type returnType) {
    super(returnType);
  }
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `UnaryColumnTransformer()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/column/unary/UnaryColumnTransformer.java`
#### Snippet
```java
  protected ColumnTransformer childColumnTransformer;

  public UnaryColumnTransformer(Type returnType, ColumnTransformer childColumnTransformer) {
    super(returnType);
    this.childColumnTransformer = childColumnTransformer;
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `ArithmeticBinaryColumnTransformer()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/column/binary/ArithmeticBinaryColumnTransformer.java`
#### Snippet
```java

public abstract class ArithmeticBinaryColumnTransformer extends BinaryColumnTransformer {
  public ArithmeticBinaryColumnTransformer(
      Type returnType, ColumnTransformer leftTransformer, ColumnTransformer rightTransformer) {
    super(returnType, leftTransformer, rightTransformer);
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `CompareBinaryColumnTransformer()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/column/binary/CompareBinaryColumnTransformer.java`
#### Snippet
```java
public abstract class CompareBinaryColumnTransformer extends BinaryColumnTransformer {

  public CompareBinaryColumnTransformer(
      Type returnType, ColumnTransformer leftTransformer, ColumnTransformer rightTransformer) {
    super(returnType, leftTransformer, rightTransformer);
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `BinaryColumnTransformer()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/column/binary/BinaryColumnTransformer.java`
#### Snippet
```java
  protected final ColumnTransformer rightTransformer;

  public BinaryColumnTransformer(
      Type returnType, ColumnTransformer leftTransformer, ColumnTransformer rightTransformer) {
    super(returnType);
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `LogicBinaryColumnTransformer()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/column/binary/LogicBinaryColumnTransformer.java`
#### Snippet
```java

public abstract class LogicBinaryColumnTransformer extends BinaryColumnTransformer {
  public LogicBinaryColumnTransformer(
      Type returnType, ColumnTransformer leftTransformer, ColumnTransformer rightTransformer) {
    super(returnType, leftTransformer, rightTransformer);
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `TernaryColumnTransformer()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/column/ternary/TernaryColumnTransformer.java`
#### Snippet
```java
  protected ColumnTransformer thirdColumnTransformer;

  public TernaryColumnTransformer(
      Type returnType,
      ColumnTransformer firstColumnTransformer,
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `CompareTernaryColumnTransformer()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/column/ternary/CompareTernaryColumnTransformer.java`
#### Snippet
```java

public abstract class CompareTernaryColumnTransformer extends TernaryColumnTransformer {
  public CompareTernaryColumnTransformer(
      Type returnType,
      ColumnTransformer firstColumnTransformer,
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `UnaryTransformer()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/transformer/unary/UnaryTransformer.java`
#### Snippet
```java
  protected final boolean isLayerPointReaderConstant;

  public UnaryTransformer(LayerPointReader layerPointReader) {
    this.layerPointReader = layerPointReader;
    layerPointReaderDataType = layerPointReader.getDataType();
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `LogWriter()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/wal/io/LogWriter.java`
#### Snippet
```java
  protected long size;

  public LogWriter(File logFile) throws FileNotFoundException {
    this.logFile = logFile;
    this.logStream = new FileOutputStream(logFile, true);
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `AbstractResultListener()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/wal/utils/listener/AbstractResultListener.java`
#### Snippet
```java
  protected volatile Exception cause;

  public AbstractResultListener(boolean wait) {
    this.wait = wait;
    this.status = Status.RUNNING;
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `AbstractWALBuffer()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/wal/buffer/AbstractWALBuffer.java`
#### Snippet
```java
  protected volatile WALWriter currentWALFileWriter;

  public AbstractWALBuffer(
      String identifier, String logDirectory, long startFileVersion, long startSearchIndex)
      throws FileNotFoundException {
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `WALEntry()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/wal/buffer/WALEntry.java`
#### Snippet
```java
  protected final WALFlushListener walFlushListener;

  public WALEntry(long memTableId, WALEntryValue value, boolean wait) {
    this.memTableId = memTableId;
    this.value = value;
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `AbstractTsFileRecoverPerformer()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/wal/recover/file/AbstractTsFileRecoverPerformer.java`
#### Snippet
```java
  protected RestorableTsFileIOWriter writer;

  public AbstractTsFileRecoverPerformer(TsFileResource tsFileResource) {
    this.tsFileResource = tsFileResource;
  }
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `AbstractNodeAllocationStrategy()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/wal/allocation/AbstractNodeAllocationStrategy.java`
#### Snippet
```java
  protected FolderManager folderManager;

  public AbstractNodeAllocationStrategy() {
    try {
      folderManager =
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `PipeData()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/sync/pipedata/PipeData.java`
#### Snippet
```java
  protected long serialNumber;

  public PipeData() {}

  public PipeData(long serialNumber) {
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `PipeData()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/sync/pipedata/PipeData.java`
#### Snippet
```java
  public PipeData() {}

  public PipeData(long serialNumber) {
    this.serialNumber = serialNumber;
  }
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `AbstractOpBlock()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/AbstractOpBlock.java`
#### Snippet
```java
  protected long dataCount = -1;

  public AbstractOpBlock(String storageGroupName, long pipeDataSerialNumber, long beginIndex) {
    this.storageGroup = storageGroupName;
    this.pipeDataSerialNumber = pipeDataSerialNumber;
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `Operation()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/sync/externalpipe/operation/Operation.java`
#### Snippet
```java
  private long endIndex;

  public Operation(
      OperationType operationType, String storageGroup, long startIndex, long endIndex) {
    this.operationType = operationType;
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `GroupByTimeEngineDataSet()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/query/dataset/groupby/GroupByTimeEngineDataSet.java`
#### Snippet
```java
public abstract class GroupByTimeEngineDataSet extends GroupByTimeDataSet {

  public GroupByTimeEngineDataSet() {}

  public GroupByTimeEngineDataSet(QueryContext context, GroupByTimePlan groupByTimePlan) {
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `GroupByTimeEngineDataSet()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/query/dataset/groupby/GroupByTimeEngineDataSet.java`
#### Snippet
```java
  public GroupByTimeEngineDataSet() {}

  public GroupByTimeEngineDataSet(QueryContext context, GroupByTimePlan groupByTimePlan) {
    super(context, groupByTimePlan);
  }
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `GroupByTimeDataSet()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/query/dataset/groupby/GroupByTimeDataSet.java`
#### Snippet
```java
  protected SlidingWindowGroupByExecutor[] slidingWindowGroupByExecutors;

  public GroupByTimeDataSet() {}

  /** groupBy query. */
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `GroupByTimeDataSet()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/query/dataset/groupby/GroupByTimeDataSet.java`
#### Snippet
```java

  /** groupBy query. */
  public GroupByTimeDataSet(QueryContext context, GroupByTimePlan groupByTimePlan) {
    super(
        new ArrayList<>(groupByTimePlan.getDeduplicatedPaths()),
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `UDTFDataSet()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/query/dataset/UDTFDataSet.java`
#### Snippet
```java
  }

  public UDTFDataSet(QueryContext queryContext, UDTFPlan udtfPlan, IUDFInputDataSet dataSet)
      throws QueryProcessException, IOException {
    queryId = queryContext.getQueryId();
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `IFill()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/query/executor/fill/IFill.java`
#### Snippet
```java
  }

  public IFill() {}

  public abstract IFill copy();
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `IFill()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/query/executor/fill/IFill.java`
#### Snippet
```java
  protected long afterRange = 0;

  public IFill(TSDataType dataType, long queryStartTime) {
    this.dataType = dataType;
    this.queryStartTime = queryStartTime;
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `SlidingWindowGroupByExecutor()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/query/executor/groupby/SlidingWindowGroupByExecutor.java`
#### Snippet
```java
  protected AggregateResult aggregateResult;

  public SlidingWindowGroupByExecutor(TSDataType dataType, String aggrFuncName, boolean ascending) {
    this.aggregateResult =
        AggregateResultFactory.getAggrResultByName(aggrFuncName, dataType, ascending);
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `AggregateResult()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/query/aggregation/AggregateResult.java`
#### Snippet
```java
   * @param resultDataType result data type.
   */
  public AggregateResult(TSDataType resultDataType, AggregationType aggregationType) {
    this.aggregationType = aggregationType;
    this.resultDataType = resultDataType;
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `ExternalSortJobPart()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/query/externalsort/ExternalSortJobPart.java`
#### Snippet
```java
  private ExternalSortJobPartType type;

  public ExternalSortJobPart(ExternalSortJobPartType type) {
    this.type = type;
  }
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `TVList()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/TVList.java`
#### Snippet
```java
  private long version;

  public TVList() {
    timestamps = new ArrayList<>();
    rowCount = 0;
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `AbstractCompactionTask()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/task/AbstractCompactionTask.java`
#### Snippet
```java
  protected long serialId;

  public AbstractCompactionTask(
      String storageGroupName,
      String dataRegionId,
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `AbstractMemTable()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/engine/memtable/AbstractMemTable.java`
#### Snippet
```java
  }

  public AbstractMemTable(Map<IDeviceID, IWritableMemChunkGroup> memTableMap) {
    this.memTableMap = memTableMap;
  }
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `AbstractMemTable()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/engine/memtable/AbstractMemTable.java`
#### Snippet
```java
  private static final String METRIC_POINT_IN = "pointsIn";

  public AbstractMemTable() {
    this.memTableMap = new HashMap<>();
  }
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `ServiceProvider()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/service/basic/ServiceProvider.java`
#### Snippet
```java
  }

  public ServiceProvider(PlanExecutor executor) throws QueryProcessException {
    planner = new Planner();
    this.executor = executor;
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `MNode()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/metadata/mnode/MNode.java`
#### Snippet
```java

  /** Constructor of MNode. */
  public MNode(IMNode parent, String name) {
    this.parent = parent;
    this.name = name;
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `CounterTraverser()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/traverser/counter/CounterTraverser.java`
#### Snippet
```java
  protected long count;

  public CounterTraverser(IMNode startNode, PartialPath path, IMTreeStore store)
      throws MetadataException {
    super(startNode, path, store);
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `EntityCollector()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/traverser/collector/EntityCollector.java`
#### Snippet
```java
  }

  public EntityCollector(
      IMNode startNode, PartialPath path, IMTreeStore store, int limit, int offset)
      throws MetadataException {
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `EntityCollector()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/traverser/collector/EntityCollector.java`
#### Snippet
```java
public abstract class EntityCollector<T> extends CollectorTraverser<T> {

  public EntityCollector(IMNode startNode, PartialPath path, IMTreeStore store)
      throws MetadataException {
    super(startNode, path, store);
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `Traverser()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/traverser/Traverser.java`
#### Snippet
```java
   * @throws MetadataException
   */
  public Traverser(IMNode startNode, PartialPath path, IMTreeStore store) throws MetadataException {
    String[] nodes = path.getNodes();
    if (nodes.length == 0 || !nodes[0].equals(PATH_ROOT)) {
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `StorageGroupCollector()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/traverser/collector/StorageGroupCollector.java`
#### Snippet
```java
  protected boolean collectInternal = false;

  public StorageGroupCollector(IMNode startNode, PartialPath path, IMTreeStore store)
      throws MetadataException {
    super(startNode, path, store);
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `MNodeAboveSGCollector()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/traverser/collector/MNodeAboveSGCollector.java`
#### Snippet
```java
  protected Set<PartialPath> involvedStorageGroupMNodes = new HashSet<>();

  public MNodeAboveSGCollector(IMNode startNode, PartialPath path, IMTreeStore store)
      throws MetadataException {
    super(startNode, path, store);
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `MeasurementCollector()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/traverser/collector/MeasurementCollector.java`
#### Snippet
```java
  }

  public MeasurementCollector(
      IMNode startNode, PartialPath path, IMTreeStore store, int limit, int offset)
      throws MetadataException {
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `MeasurementCollector()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/traverser/collector/MeasurementCollector.java`
#### Snippet
```java
public abstract class MeasurementCollector<T> extends CollectorTraverser<T> {

  public MeasurementCollector(IMNode startNode, PartialPath path, IMTreeStore store)
      throws MetadataException {
    super(startNode, path, store);
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `MNodeCollector()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/traverser/collector/MNodeCollector.java`
#### Snippet
```java
  private Set<IMNode> processedNodes = new HashSet<>();

  public MNodeCollector(IMNode startNode, PartialPath path, IMTreeStore store)
      throws MetadataException {
    super(startNode, path, store);
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `CollectorTraverser()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/traverser/collector/CollectorTraverser.java`
#### Snippet
```java
  }

  public CollectorTraverser(
      IMNode startNode, PartialPath path, IMTreeStore store, int limit, int offset)
      throws MetadataException {
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `CollectorTraverser()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/traverser/collector/CollectorTraverser.java`
#### Snippet
```java
  protected T resultSet;

  public CollectorTraverser(IMNode startNode, PartialPath path, IMTreeStore store)
      throws MetadataException {
    super(startNode, path, store);
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `InfluxFunction()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/function/InfluxFunction.java`
#### Snippet
```java
  private List<Expression> expressionList;

  public InfluxFunction(List<Expression> expressionList) {
    this.expressionList = expressionList;
  }
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `InfluxFunction()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/function/InfluxFunction.java`
#### Snippet
```java
  }

  public InfluxFunction() {}

  public List<Expression> getExpressions() {
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `InfluxSelector()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/function/selector/InfluxSelector.java`
#### Snippet
```java
  private List<Object> relatedValues;

  public InfluxSelector(List<Expression> expressionList) {
    super(expressionList);
  }
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `InfluxAggregator()` of an abstract class should not be declared 'public'
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/function/aggregator/InfluxAggregator.java`
#### Snippet
```java
public abstract class InfluxAggregator extends InfluxFunction {

  public InfluxAggregator(List<Expression> expressionList) {
    super(expressionList);
  }
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `ValueRepair()` of an abstract class should not be declared 'public'
in `library-udf/src/main/java/org/apache/iotdb/library/drepair/util/ValueRepair.java`
#### Snippet
```java
  protected double[] repaired;

  public ValueRepair(RowIterator dataIterator) throws Exception {
    ArrayList<Long> timeList = new ArrayList<>();
    ArrayList<Double> originList = new ArrayList<>();
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `ValueFill()` of an abstract class should not be declared 'public'
in `library-udf/src/main/java/org/apache/iotdb/library/drepair/util/ValueFill.java`
#### Snippet
```java
  protected int not_nan_number = 0;

  public ValueFill(RowIterator dataIterator) throws Exception {
    ArrayList<Long> timeList = new ArrayList<>();
    ArrayList<Double> originList = new ArrayList<>();
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `KLLSketchForQuantile()` of an abstract class should not be declared 'public'
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/KLLSketchForQuantile.java`
#### Snippet
```java
  //  Random test_random = new Random();

  public KLLSketchForQuantile() {}

  protected abstract int calcMaxMemoryNum(int maxMemoryByte);
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `AbstractAsyncRPCHandler()` of an abstract class should not be declared 'public'
in `confignode/src/main/java/org/apache/iotdb/confignode/client/async/handlers/rpc/AbstractAsyncRPCHandler.java`
#### Snippet
```java
  protected final String formattedTargetLocation;

  public AbstractAsyncRPCHandler(
      DataNodeRequestType requestType,
      int requestId,
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `ConfigPhysicalPlan()` of an abstract class should not be declared 'public'
in `confignode/src/main/java/org/apache/iotdb/confignode/consensus/request/ConfigPhysicalPlan.java`
#### Snippet
```java
  private final ConfigPhysicalPlanType type;

  public ConfigPhysicalPlan(ConfigPhysicalPlanType type) {
    this.type = type;
  }
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `ConfigNodeException()` of an abstract class should not be declared 'public'
in `confignode/src/main/java/org/apache/iotdb/confignode/exception/ConfigNodeException.java`
#### Snippet
```java
public abstract class ConfigNodeException extends Exception {

  public ConfigNodeException(String message) {
    super(message);
  }
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `InternalProcedure()` of an abstract class should not be declared 'public'
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/InternalProcedure.java`
#### Snippet
```java
 */
public abstract class InternalProcedure<Env> extends Procedure<Env> {
  public InternalProcedure(long toMillis) {
    setTimeout(toMillis);
  }
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `StoppableThread()` of an abstract class should not be declared 'public'
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/StoppableThread.java`
#### Snippet
```java
  private static final Logger LOG = LoggerFactory.getLogger(StoppableThread.class);

  public StoppableThread(ThreadGroup threadGroup, String name) {
    super(threadGroup, name);
  }
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `RegionMaintainTask()` of an abstract class should not be declared 'public'
in `confignode/src/main/java/org/apache/iotdb/confignode/persistence/partition/maintainer/RegionMaintainTask.java`
#### Snippet
```java
  protected TDataNodeLocation targetDataNode;

  public RegionMaintainTask(RegionMaintainType type) {
    this.type = type;
  }
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `RMNode()` of an abstract class should not be declared 'public'
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/mnode/RMNode.java`
#### Snippet
```java

  /** Constructor of MNode. */
  public RMNode(String fullPath, RSchemaReadWriteHandler readWriteHandler) {
    this.fullPath = fullPath.intern();
    this.name = fullPath.substring(fullPath.lastIndexOf(RSchemaConstants.PATH_SEPARATOR) + 1);
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `QueryDataSet()` of an abstract class should not be declared 'public'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/query/dataset/QueryDataSet.java`
#### Snippet
```java
  }

  public QueryDataSet() {}

  public QueryDataSet(List<Path> paths, List<TSDataType> dataTypes) {
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `QueryDataSet()` of an abstract class should not be declared 'public'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/query/dataset/QueryDataSet.java`
#### Snippet
```java
  public QueryDataSet() {}

  public QueryDataSet(List<Path> paths, List<TSDataType> dataTypes) {
    initQueryDataSetFields(paths, dataTypes, true);
  }
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `QueryDataSet()` of an abstract class should not be declared 'public'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/query/dataset/QueryDataSet.java`
#### Snippet
```java
  }

  public QueryDataSet(List<Path> paths, List<TSDataType> dataTypes, boolean ascending) {
    initQueryDataSetFields(paths, dataTypes, ascending);
  }
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `BinaryFilter()` of an abstract class should not be declared 'public'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/filter/basic/BinaryFilter.java`
#### Snippet
```java
  protected Filter right;

  public BinaryFilter() {}

  protected BinaryFilter(Filter left, Filter right) {
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `UnaryFilter()` of an abstract class should not be declared 'public'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/filter/basic/UnaryFilter.java`
#### Snippet
```java
  protected FilterType filterType;

  public UnaryFilter() {}

  protected UnaryFilter(T value, FilterType filterType) {
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `AbstractFileSeriesReader()` of an abstract class should not be declared 'public'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/reader/series/AbstractFileSeriesReader.java`
#### Snippet
```java

  /** constructor of FileSeriesReader. */
  public AbstractFileSeriesReader(
      IChunkLoader chunkLoader, List<IChunkMetadata> chunkMetadataList, Filter filter) {
    this.chunkLoader = chunkLoader;
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `DataPoint()` of an abstract class should not be declared 'public'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/record/datapoint/DataPoint.java`
#### Snippet
```java
   * @param measurementId measurementId of this DataPoint
   */
  public DataPoint(TSDataType type, String measurementId) {
    this.type = type;
    this.measurementId = measurementId;
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `LRUCache()` of an abstract class should not be declared 'public'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/common/cache/LRUCache.java`
#### Snippet
```java
  protected Map<K, T> cache;

  public LRUCache(int cacheSize) {
    this.cache =
        new LinkedHashMap<K, T>(cacheSize, 0.75f, true) {
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `Decoder()` of an abstract class should not be declared 'public'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/decoder/Decoder.java`
#### Snippet
```java
  private TSEncoding type;

  public Decoder(TSEncoding type) {
    this.type = type;
  }
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `RegularDataDecoder()` of an abstract class should not be declared 'public'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/decoder/RegularDataDecoder.java`
#### Snippet
```java
  protected int packNum;

  public RegularDataDecoder() {
    super(TSEncoding.REGULAR);
  }
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `DeltaBinaryDecoder()` of an abstract class should not be declared 'public'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/decoder/DeltaBinaryDecoder.java`
#### Snippet
```java
  protected int encodingLength;

  public DeltaBinaryDecoder() {
    super(TSEncoding.TS_2DIFF);
  }
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `Encoder()` of an abstract class should not be declared 'public'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/encoder/Encoder.java`
#### Snippet
```java
  }

  public Encoder(TSEncoding type) {
    this.type = type;
  }
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `DeltaBinaryEncoder()` of an abstract class should not be declared 'public'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/encoder/DeltaBinaryEncoder.java`
#### Snippet
```java
   * @param size - the number how many numbers to be packed into a block.
   */
  public DeltaBinaryEncoder(int size) {
    super(TSEncoding.TS_2DIFF);
    blockSize = size;
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `RegularDataEncoder()` of an abstract class should not be declared 'public'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/encoder/RegularDataEncoder.java`
#### Snippet
```java
   * @param size - the number how many numbers to be packed into a block.
   */
  public RegularDataEncoder(int size) {
    super(TSEncoding.REGULAR);
    blockSize = size;
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `TSEncodingBuilder()` of an abstract class should not be declared 'public'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/encoder/TSEncodingBuilder.java`
#### Snippet
```java
  protected final TSFileConfig conf;

  public TSEncodingBuilder() {
    this.conf = TSFileDescriptor.getInstance().getConfig();
  }
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `AbstractMetricManager()` of an abstract class should not be declared 'public'
in `metrics/interface/src/main/java/org/apache/iotdb/metrics/AbstractMetricManager.java`
#### Snippet
```java
  protected Map<MetricInfo, IMetric> metrics;

  public AbstractMetricManager() {
    nameToMetaInfo = new ConcurrentHashMap<>();
    metrics = new ConcurrentHashMap<>();
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `AbstractMetricService()` of an abstract class should not be declared 'public'
in `metrics/interface/src/main/java/org/apache/iotdb/metrics/AbstractMetricService.java`
#### Snippet
```java
  protected List<IMetricSet> metricSets = new ArrayList<>();

  public AbstractMetricService() {}

  /** Start metric service */
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `AbstractNodeWrapper()` of an abstract class should not be declared 'public'
in `integration-test/src/main/java/org/apache/iotdb/it/env/AbstractNodeWrapper.java`
#### Snippet
```java
  private int node_port;

  public AbstractNodeWrapper(String testClassName, String testMethodName, int[] portList) {
    this.testClassName = testClassName;
    this.testMethodName = testMethodName;
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `RequestDelegate()` of an abstract class should not be declared 'public'
in `integration-test/src/main/java/org/apache/iotdb/itbase/runtime/RequestDelegate.java`
#### Snippet
```java
   * @param endpoints the list of endpoints.
   */
  public RequestDelegate(List<String> endpoints) {
    this.endpoints = endpoints;
  }
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `TsFileOutputFormat()` of an abstract class should not be declared 'public'
in `flink-tsfile-connector/src/main/java/org/apache/iotdb/flink/tsfile/TsFileOutputFormat.java`
#### Snippet
```java
  protected transient TsFileWriter writer = null;

  public TsFileOutputFormat(String path, Schema schema, TSFileConfig config) {
    super(path == null ? null : new Path(path));
    this.schema = Preconditions.checkNotNull(schema);
```

### RuleId[ruleID=NonProtectedConstructorInAbstractClass]
Constructor `ConsensusResponse()` of an abstract class should not be declared 'public'
in `consensus/src/main/java/org/apache/iotdb/consensus/common/response/ConsensusResponse.java`
#### Snippet
```java
  protected final ConsensusException exception;

  public ConsensusResponse(ConsensusException exception) {
    this.exception = exception;
  }
```

## RuleId[ruleID=CaughtExceptionImmediatelyRethrown]
### RuleId[ruleID=CaughtExceptionImmediatelyRethrown]
Caught exception `e` is immediately rethrown
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/TsFileOpBlock.java`
#### Snippet
```java
          }
        }
      } catch (BufferOverflowException e) {
        throw e;
      }
```

### RuleId[ruleID=CaughtExceptionImmediatelyRethrown]
Caught exception `e` is immediately rethrown
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileSelfCheckTool.java`
#### Snippet
```java
          }
        }
      } catch (BufferOverflowException e) {
        throw e;
      }
```

### RuleId[ruleID=CaughtExceptionImmediatelyRethrown]
Caught exception `e` is immediately rethrown
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileSketchTool.java`
#### Snippet
```java
          }
        }
      } catch (BufferOverflowException e) {
        throw e;
      }
```

### RuleId[ruleID=CaughtExceptionImmediatelyRethrown]
Caught exception `e` is immediately rethrown
in `flink-iotdb-connector/src/main/java/org/apache/iotdb/flink/IoTDBSource.java`
#### Snippet
```java
    try {
      dataSet.closeOperationHandle();
    } catch (StatementExecutionException | IoTDBConnectionException e) {
      throw e;
    } finally {
```

### RuleId[ruleID=CaughtExceptionImmediatelyRethrown]
Caught exception `e` is immediately rethrown
in `example/ext-pipe-plugin-example/src/main/java/org/apache/iotdb/extpipe/ExtPipeSinkWriterFactory.java`
#### Snippet
```java
      // == Here, do init work of IExternalPipeSinkWriterFactory instance.
      // ...
    } catch (Exception e) {
      // LOGGER.error("Failed to init extPipeSink ..." , e);
      throw e;
```

## RuleId[ruleID=AssignmentToMethodParameter]
### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `distance`
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/utils/KDTreeUtil.java`
#### Snippet
```java
        tdis = UtilZ.distance(input, node.value, std);
        if (tdis < distance) {
          distance = tdis;
          nearest = node.value;
        }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `distance`
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/utils/KDTreeUtil.java`
#### Snippet
```java
          tdis = UtilZ.distance(input, node.value, std);
          if (tdis < distance) {
            distance = tdis;
            nearest = node.value;
          }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `distance`
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/utils/KDTreeUtil.java`
#### Snippet
```java
        tdis = UtilZ.distance(input, node.value, std);
        if (tdis < distance) {
          distance = tdis;
          nearest.add(node.value);
        }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `distance`
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/utils/KDTreeUtil.java`
#### Snippet
```java
          tdis = UtilZ.distance(input, node.value, std);
          if (tdis < distance) {
            distance = tdis;
            nearest.add(node.value);
          }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `dir`
in `node-commons/src/main/java/org/apache/iotdb/commons/conf/CommonConfig.java`
#### Snippet
```java
    if (!new File(dir).isAbsolute() && homeDir != null && homeDir.length() > 0) {
      if (!homeDir.endsWith(File.separator)) {
        dir = homeDir + File.separatorChar + dir;
      } else {
        dir = homeDir + dir;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `dir`
in `node-commons/src/main/java/org/apache/iotdb/commons/conf/CommonConfig.java`
#### Snippet
```java
        dir = homeDir + File.separatorChar + dir;
      } else {
        dir = homeDir + dir;
      }
    }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `curNode`
in `node-commons/src/main/java/org/apache/iotdb/commons/path/PathPatternTree.java`
#### Snippet
```java
          new PathPatternNode<>(pathNodes[i], VoidSerializer.getInstance());
      curNode.addChild(newNode);
      curNode = newNode;
    }
    curNode.markPathPattern(true);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `t`
in `node-commons/src/main/java/org/apache/iotdb/commons/concurrent/threadpool/WrappedThreadPoolExecutor.java`
#### Snippet
```java
        ((Future<?>) r).get();
      } catch (CancellationException ce) {
        t = ce;
      } catch (ExecutionException ee) {
        t = ee.getCause();
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `t`
in `node-commons/src/main/java/org/apache/iotdb/commons/concurrent/threadpool/WrappedThreadPoolExecutor.java`
#### Snippet
```java
        t = ce;
      } catch (ExecutionException ee) {
        t = ee.getCause();
      } catch (InterruptedException ie) {
        Thread.currentThread().interrupt();
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `dString`
in `jdbc/src/main/java/org/apache/iotdb/jdbc/StringUtils.java`
#### Snippet
```java
        buf.append('+');
        buf.append(dString.substring(ePos + 1, dString.length()));
        dString = buf.toString();
      }
    }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `args`
in `cli/src/main/java/org/apache/iotdb/cli/AbstractCli.java`
#### Snippet
```java
      // is true.
      continuePrint = true;
      args = Arrays.copyOfRange(args, 0, index);
      return args;
    }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `startValue`
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileWriteAlignedWithTablet.java`
#### Snippet
```java
    long sensorNum = schemas.size();

    for (long r = 0; r < rowNum; r++, startValue++) {
      int row = tablet.rowSize++;
      timestamps[row] = startTime++;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `startTime`
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileWriteAlignedWithTablet.java`
#### Snippet
```java
    for (long r = 0; r < rowNum; r++, startValue++) {
      int row = tablet.rowSize++;
      timestamps[row] = startTime++;
      for (int i = 0; i < sensorNum; i++) {
        Binary[] textSensor = (Binary[]) values[i];
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `startValue`
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileWriteAlignedWithTSRecord.java`
#### Snippet
```java
      TSRecord tsRecord = new TSRecord(time, deviceId);
      for (IMeasurementSchema schema : schemas) {
        DataPoint dPoint = new LongDataPoint(schema.getMeasurementId(), startValue++);
        tsRecord.addTuple(dPoint);
      }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `startValue`
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileWriteWithTSRecord.java`
#### Snippet
```java
      TSRecord tsRecord = new TSRecord(time, deviceId);
      for (IMeasurementSchema schema : schemas) {
        DataPoint dPoint = new LongDataPoint(schema.getMeasurementId(), startValue++);
        tsRecord.addTuple(dPoint);
      }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `startValue`
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileWriteWithTablet.java`
#### Snippet
```java
    long sensorNum = schemas.size();

    for (long r = 0; r < rowNum; r++, startValue++) {
      int row = tablet.rowSize++;
      timestamps[row] = startTime++;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `startTime`
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileWriteWithTablet.java`
#### Snippet
```java
    for (long r = 0; r < rowNum; r++, startValue++) {
      int row = tablet.rowSize++;
      timestamps[row] = startTime++;
      for (int i = 0; i < sensorNum; i++) {
        Binary[] textSensor = (Binary[]) values[i];
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `catalog`
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
    if (catalog != null && catalog.length() > 0) {
      if (catalog.contains("%")) {
        catalog = catalog.replace("%", "*");
      }
      sql = sql + " " + catalog;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `schemaPattern`
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
    } else if (schemaPattern != null && schemaPattern.length() > 0) {
      if (schemaPattern.contains("%")) {
        schemaPattern = schemaPattern.replace("%", "*");
      }
      sql = sql + " " + schemaPattern;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `tableNamePattern`
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
        && tableNamePattern.length() > 0) {
      if (tableNamePattern.contains("%")) {
        tableNamePattern = tableNamePattern.replace("%", "*");
      }
      sql = sql + "." + tableNamePattern;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `rs`
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      }
    } catch (Exception ex) {
      rs = null;
    }
    try {
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `stmt`
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      }
    } catch (Exception ex) {
      stmt = null;
    }
  }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `catalog`
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
    if (catalog != null && catalog.length() > 0) {
      if (catalog.contains("%")) {
        catalog = catalog.replace("%", "*");
      }
      sql = sql + " " + catalog;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `schemaPattern`
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
    } else if (schemaPattern != null && schemaPattern.length() > 0) {
      if (schemaPattern.contains("%")) {
        schemaPattern = schemaPattern.replace("%", "*");
      }
      sql = sql + " " + schemaPattern;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `tableNamePattern`
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
        && tableNamePattern.length() > 0) {
      if (tableNamePattern.contains("%")) {
        tableNamePattern = tableNamePattern.replace("%", "*");
      }
      sql = sql + "." + tableNamePattern;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `catalog`
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
    if (catalog != null && catalog.length() > 0) {
      if (catalog.contains("%")) {
        catalog = catalog.replace("%", "*");
      }
      sql = sql + " " + catalog;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `schemaPattern`
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
    } else if (schemaPattern != null && schemaPattern.length() > 0) {
      if (schemaPattern.contains("%")) {
        schemaPattern = schemaPattern.replace("%", "*");
      }
      sql = sql + " " + schemaPattern;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `tableNamePattern`
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
        && tableNamePattern.length() > 0) {
      if (tableNamePattern.contains("%")) {
        tableNamePattern = tableNamePattern.replace("%", "*");
      }
      sql = sql + "." + tableNamePattern;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `catalog`
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
    if (catalog != null && catalog.length() > 0) {
      if (catalog.contains("%")) {
        catalog = catalog.replace("%", "*");
      }
      sql = sql + " " + catalog;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `schemaPattern`
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
    } else if (schemaPattern != null && schemaPattern.length() > 0) {
      if (schemaPattern.contains("%")) {
        schemaPattern = schemaPattern.replace("%", "*");
      }
      sql = sql + " " + schemaPattern;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `tableNamePattern`
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
        && tableNamePattern.length() > 0) {
      if (tableNamePattern.contains("%")) {
        tableNamePattern = tableNamePattern.replace("%", "*");
      }
      sql = sql + "." + tableNamePattern;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `path`
in `example/jdbc/src/main/java/org/apache/iotdb/SyntaxConventionRelatedExample.java`
#### Snippet
```java
  private static String prepareInsertStatement(int time, String path) {
    // remove device root.sg1
    path = removeDevice(path);
    return String.format(
        "insert into root.sg1(timestamp, %s) values(" + time + "," + 1 + ")", path);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `encoding`
in `schema-engine-tag/src/main/java/org/apache/iotdb/db/metadata/tagSchemaRegion/TagSchemaRegion.java`
#### Snippet
```java
      throws MetadataException {
    if (encoding == null) {
      encoding = getDefaultEncoding(dataType);
    }
    if (compressor == null) {
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `compressor`
in `schema-engine-tag/src/main/java/org/apache/iotdb/db/metadata/tagSchemaRegion/TagSchemaRegion.java`
#### Snippet
```java
    }
    if (compressor == null) {
      compressor = TSFileDescriptor.getInstance().getConfig().getCompressor();
    }
    createTimeseries(path, dataType, encoding, compressor, Collections.emptyMap());
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `devicePath`
in `schema-engine-tag/src/main/java/org/apache/iotdb/db/metadata/tagSchemaRegion/TagSchemaRegion.java`
#### Snippet
```java
    if (path.endsWith(TAIL)) {
      path = path.substring(0, path.length() - TAIL.length());
      devicePath = new PartialPath(path);
    }
    synchronized (idTableWithDeviceIDList) {
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `operator`
in `server/src/main/java/org/apache/iotdb/db/qp/Planner.java`
#### Snippet
```java
    LogicalChecker.check(operator);
    // optimize the logical operator
    operator = logicalOptimize(operator);
    // from logical operator to physical plan
    return generatePhysicalPlanFromOperator(operator);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `root`
in `server/src/main/java/org/apache/iotdb/db/qp/Planner.java`
#### Snippet
```java
  private QueryOperator optimizeQueryOperator(QueryOperator root)
      throws LogicalOperatorException, PathNumOverLimitException {
    root = (QueryOperator) new ConcatPathOptimizer().transform(root);

    WhereComponent whereComponent = root.getWhereComponent();
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `timestamp`
in `server/src/main/java/org/apache/iotdb/db/qp/utils/DateTimeUtils.java`
#### Snippet
```java
    switch (timePrecision) {
      case "ns":
        timestamp /= 1000_000_000;
        break;
      case "us":
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `timestamp`
in `server/src/main/java/org/apache/iotdb/db/qp/utils/DateTimeUtils.java`
#### Snippet
```java
        break;
      case "us":
        timestamp /= 1000_000;
        break;
    }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `expression`
in `server/src/main/java/org/apache/iotdb/db/qp/logical/crud/GroupByQueryOperator.java`
#### Snippet
```java

    if (expression == null) {
      expression = timeExpression;
    } else {
      expression = BinaryExpression.and(expression, timeExpression);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `expression`
in `server/src/main/java/org/apache/iotdb/db/qp/logical/crud/GroupByQueryOperator.java`
#### Snippet
```java
      expression = timeExpression;
    } else {
      expression = BinaryExpression.and(expression, timeExpression);
    }

```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `retryTime`
in `cli/src/main/java/org/apache/iotdb/tool/ImportCsv.java`
#### Snippet
```java
          System.out.println("Meet error when insert csv because " + e.getMessage());
        }
        writeAndEmptyDataSet(device, times, typesList, valuesList, measurementsList, --retryTime);
      }
    } catch (StatementExecutionException e) {
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `retryTime`
in `cli/src/main/java/org/apache/iotdb/tool/ImportCsv.java`
#### Snippet
```java
        }
        writeAndEmptyDataSet(
            deviceIds, times, typesList, valuesList, measurementsList, --retryTime);
      }
    } catch (StatementExecutionException e) {
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `msg`
in `server/src/main/java/org/apache/iotdb/db/qp/strategy/SQLParseError.java`
#### Snippet
```java
        // node name
        if (expectedTokenNames.contains("*") && expectedTokenNames.contains("**")) {
          msg = msg.replace(expectedTokensString, "{ID, QUOTED_ID, *, **}");
        } else {
          msg = msg.replace(expectedTokensString, "{ID, QUOTED_ID}");
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `msg`
in `server/src/main/java/org/apache/iotdb/db/qp/strategy/SQLParseError.java`
#### Snippet
```java
          msg = msg.replace(expectedTokensString, "{ID, QUOTED_ID, *, **}");
        } else {
          msg = msg.replace(expectedTokensString, "{ID, QUOTED_ID}");
        }
      }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `i`
in `server/src/main/java/org/apache/iotdb/db/qp/strategy/optimizer/MergeSingleFilterOptimizer.java`
#### Snippet
```java
      List<FilterOperator> ret, FilterOperator filter, int i, PartialPath childPath) {
    List<FilterOperator> children = filter.getChildren();
    for (; i < children.size(); i++) {
      ret.add(children.get(i));
    }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `indexOfMissingMeasurements`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/ClusterSchemaFetcher.java`
#### Snippet
```java
          }
        }
        indexOfMissingMeasurements = recheckedIndexOfMissingMeasurements;
        for (Map.Entry<String, IMeasurementSchema> entry : template.getSchemaMap().entrySet()) {
          schemaTree.appendSingleMeasurement(
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `queryStatement`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/AnalyzeVisitor.java`
#### Snippet
```java
      // concat path and construct path pattern tree
      PathPatternTree patternTree = new PathPatternTree();
      queryStatement =
          (QueryStatement) new ConcatPathRewriter().rewrite(queryStatement, patternTree);
      analysis.setStatement(queryStatement);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `sgLevel`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/AnalyzeVisitor.java`
#### Snippet
```java
  private void autoCreateSg(int sgLevel, Map<String, Map<MeasurementSchema, File>> device2Schemas)
      throws VerifyMetadataException, LoadFileException, IllegalPathException {
    sgLevel += 1; // e.g. "root.sg" means sgLevel = 1, "root.sg.test" means sgLevel=2
    Set<PartialPath> sgSet = new HashSet<>();
    for (String device : device2Schemas.keySet()) {
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `curStep`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/LogicalPlanBuilder.java`
#### Snippet
```java
    if (curStep.isOutputPartial()) {
      if (groupByTimeParameter != null && groupByTimeParameter.hasOverlap()) {
        curStep =
            crossGroupByExpressions != null ? AggregationStep.INTERMEDIATE : AggregationStep.FINAL;

```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `curStep`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/LogicalPlanBuilder.java`
#### Snippet
```java

        if (crossGroupByExpressions != null) {
          curStep = AggregationStep.FINAL;
          if (tagKeys != null) {
            this.root =
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `curStep`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/LogicalPlanBuilder.java`
#### Snippet
```java
      } else {
        if (tagKeys != null) {
          curStep = AggregationStep.FINAL;
          this.root =
              createGroupByTagNode(
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `curStep`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/LogicalPlanBuilder.java`
#### Snippet
```java
                  scanOrder);
        } else if (crossGroupByExpressions != null) {
          curStep = AggregationStep.FINAL;
          this.root =
              createGroupByTLevelNode(
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `i`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/rewriter/MergeSingleFilterOptimizer.java`
#### Snippet
```java
      List<QueryFilter> ret, QueryFilter filter, int i, PartialPath childPath) {
    List<QueryFilter> children = filter.getChildren();
    for (; i < children.size(); i++) {
      ret.add(children.get(i));
    }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `thisNode`
in `server/src/main/java/org/apache/iotdb/db/mpp/common/schematree/ClusterSchemaTree.java`
#### Snippet
```java
            entityNode = new SchemaEntityNode(thisNode.getName());
            thisParent.replaceChild(thisNode.getName(), entityNode);
            thisNode = entityNode;
          }

```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `inFlightException`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/driver/Driver.java`
#### Snippet
```java
    if (newException instanceof Error) {
      if (inFlightException == null) {
        inFlightException = newException;
      } else {
        // Self-suppression not permitted
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `inputTsBlock`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/AggregationUtil.java`
#### Snippet
```java
      if ((ascending && inputTsBlock.getStartTime() < curTimeRange.getMin())
          || (!ascending && inputTsBlock.getStartTime() > curTimeRange.getMax())) {
        inputTsBlock = skipPointsOutOfTimeRange(inputTsBlock, curTimeRange, ascending);
      }

```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `inputTsBlock`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/AggregationUtil.java`
#### Snippet
```java
      }
      if (lastReadRowIndex >= inputTsBlock.getPositionCount()) {
        inputTsBlock = null;
      } else {
        inputTsBlock = inputTsBlock.subTsBlock(lastReadRowIndex);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `inputTsBlock`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/AggregationUtil.java`
#### Snippet
```java
        inputTsBlock = null;
      } else {
        inputTsBlock = inputTsBlock.subTsBlock(lastReadRowIndex);
      }
    }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `lastReadIndex`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/AbstractIntoOperator.java`
#### Snippet
```java

        ++rowCount;
        ++lastReadIndex;
        if (rowCount == TABLET_ROW_LIMIT) {
          break;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `interval`
in `server/src/main/java/org/apache/iotdb/db/mpp/aggregation/timerangeiterator/TimeRangeIteratorFactory.java`
#### Snippet
```java
    long originInterval = interval;
    long originSlidingStep = slidingStep;
    interval = isIntervalByMonth ? interval / MS_TO_MONTH : interval;
    slidingStep = isSlidingStepByMonth ? slidingStep / MS_TO_MONTH : slidingStep;

```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `slidingStep`
in `server/src/main/java/org/apache/iotdb/db/mpp/aggregation/timerangeiterator/TimeRangeIteratorFactory.java`
#### Snippet
```java
    long originSlidingStep = slidingStep;
    interval = isIntervalByMonth ? interval / MS_TO_MONTH : interval;
    slidingStep = isSlidingStepByMonth ? slidingStep / MS_TO_MONTH : slidingStep;

    if (outputPartialTimeWindow && originInterval > originSlidingStep) {
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `cacheSize`
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/datastructure/tv/ElasticSerializableTVList.java`
#### Snippet
```java
    internalTVListCapacity = allocatableCapacity / cacheSize;
    if (internalTVListCapacity == 0) {
      cacheSize = 1;
      internalTVListCapacity = allocatableCapacity;
    }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `numCacheBlock`
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/datastructure/row/ElasticSerializableRowRecordList.java`
#### Snippet
```java
    internalRowRecordListCapacity = allocatableCapacity / numCacheBlock;
    if (internalRowRecordListCapacity == 0) {
      numCacheBlock = 1;
      internalRowRecordListCapacity = allocatableCapacity;
    }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `type`
in `server/src/main/java/org/apache/iotdb/db/sync/sender/pipe/PipeSinkFactory.java`
#### Snippet
```java
public class PipeSinkFactory {
  public static PipeSink createPipeSink(String type, String name) {
    type = type.toLowerCase();

    if (PipeSink.PipeSinkType.IoTDB.name().toLowerCase().equals(type)) {
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `file`
in `server/src/main/java/org/apache/iotdb/db/sync/sender/recovery/TsFilePipeLogger.java`
#### Snippet
```java
    while (!file.getName().equals(IoTDBConstant.SEQUENCE_FLODER_NAME)
        && !file.getName().equals(IoTDBConstant.UNSEQUENCE_FLODER_NAME)) {
      file = file.getParentFile();
      builder =
          new StringBuilder(file.getName())
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `startTime`
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/DeletionGroup.java`
#### Snippet
```java

    if ((startEntry != null) && (startTime <= startEntry.getValue())) {
      startTime = startEntry.getKey();
    }
    if ((endEntry != null) && (endTime < endEntry.getValue())) {
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `endTime`
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/DeletionGroup.java`
#### Snippet
```java
    }
    if ((endEntry != null) && (endTime < endEntry.getValue())) {
      endTime = endEntry.getValue();
    }

```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `dir`
in `server/src/main/java/org/apache/iotdb/db/conf/IoTDBConfig.java`
#### Snippet
```java
    if (!new File(dir).isAbsolute() && prefix != null && prefix.length() > 0) {
      if (!prefix.endsWith(File.separator)) {
        dir = prefix + File.separatorChar + dir;
      } else {
        dir = prefix + dir;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `dir`
in `server/src/main/java/org/apache/iotdb/db/conf/IoTDBConfig.java`
#### Snippet
```java
        dir = prefix + File.separatorChar + dir;
      } else {
        dir = prefix + dir;
      }
    }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `deviceId`
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/TsFileOpBlock.java`
#### Snippet
```java
        } else { // deviceId should be determined by LEAF_DEVICE node
          if (type.equals(MetadataIndexNodeType.LEAF_DEVICE)) {
            deviceId = metadataIndex.getName();
          }
          MetadataIndexNode metadataIndexNode = MetadataIndexNode.deserializeFrom(buffer);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `startPriority`
in `server/src/main/java/org/apache/iotdb/db/query/reader/universal/PriorityMergeReader.java`
#### Snippet
```java
            });
    for (IPointReader reader : prioritySeriesReaders) {
      addReader(reader, startPriority++);
    }
  }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `timeArrayLength`
in `server/src/main/java/org/apache/iotdb/db/query/dataset/groupby/GroupByWithValueFilterDataSet.java`
#### Snippet
```java
      }
      if (groupByTimePlan.isAscending() && lastTimestamp < curEndTime) {
        timestampArray[timeArrayLength++] = lastTimestamp;
      } else if (!groupByTimePlan.isAscending() && lastTimestamp >= curStartTime) {
        timestampArray[timeArrayLength++] = lastTimestamp;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `timeArrayLength`
in `server/src/main/java/org/apache/iotdb/db/query/dataset/groupby/GroupByWithValueFilterDataSet.java`
#### Snippet
```java
        timestampArray[timeArrayLength++] = lastTimestamp;
      } else if (!groupByTimePlan.isAscending() && lastTimestamp >= curStartTime) {
        timestampArray[timeArrayLength++] = lastTimestamp;
      } else {
        // may lastTimestamp get from cache
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `values`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
      boolean isAligned) {
    if (hasNull(values)) {
      values = changeToArrayListWithStringType(values);
      measurements = changeToArrayListWithStringType(measurements);
      prefixPaths = new ArrayList<>(prefixPaths);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `measurements`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
    if (hasNull(values)) {
      values = changeToArrayListWithStringType(values);
      measurements = changeToArrayListWithStringType(measurements);
      prefixPaths = new ArrayList<>(prefixPaths);
      time = new ArrayList<>(time);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `prefixPaths`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
      values = changeToArrayListWithStringType(values);
      measurements = changeToArrayListWithStringType(measurements);
      prefixPaths = new ArrayList<>(prefixPaths);
      time = new ArrayList<>(time);
      filterNullValueAndMeasurementWithStringType(prefixPaths, time, measurements, values);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `time`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
      measurements = changeToArrayListWithStringType(measurements);
      prefixPaths = new ArrayList<>(prefixPaths);
      time = new ArrayList<>(time);
      filterNullValueAndMeasurementWithStringType(prefixPaths, time, measurements, values);
    }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `values`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
  private List<List<TSDataType>> changeToArrayListWithTSDataType(List<List<TSDataType>> values) {
    if (!(values instanceof ArrayList)) {
      values = new ArrayList<>(values);
    }
    for (int i = 0; i < values.size(); i++) {
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `values`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
  private List<List<String>> changeToArrayListWithStringType(List<List<String>> values) {
    if (!(values instanceof ArrayList)) {
      values = new ArrayList<>(values);
    }
    for (int i = 0; i < values.size(); i++) {
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `measurements`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
      List<String> values) {
    if (hasNull(values)) {
      measurements = new ArrayList<>(measurements);
      values = new ArrayList<>(values);
      boolean isAllValueNull =
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `values`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
    if (hasNull(values)) {
      measurements = new ArrayList<>(measurements);
      values = new ArrayList<>(values);
      boolean isAllValueNull =
          filterNullValueAndMeasurementWithStringType(values, deviceId, measurements);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `measurementsList`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
      throws IoTDBConnectionException, BatchExecutionException {
    if (hasNull(valuesList)) {
      measurementsList = changeToArrayListWithStringType(measurementsList);
      valuesList = changeToArrayList(valuesList);
      typesList = changeToArrayListWithTSDataType(typesList);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `valuesList`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
    if (hasNull(valuesList)) {
      measurementsList = changeToArrayListWithStringType(measurementsList);
      valuesList = changeToArrayList(valuesList);
      typesList = changeToArrayListWithTSDataType(typesList);
      times = new ArrayList<>(times);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `typesList`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
      measurementsList = changeToArrayListWithStringType(measurementsList);
      valuesList = changeToArrayList(valuesList);
      typesList = changeToArrayListWithTSDataType(typesList);
      times = new ArrayList<>(times);
      filterNullValueAndMeasurementOfOneDevice(
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `times`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
      valuesList = changeToArrayList(valuesList);
      typesList = changeToArrayListWithTSDataType(typesList);
      times = new ArrayList<>(times);
      filterNullValueAndMeasurementOfOneDevice(
          prefixPath, times, measurementsList, typesList, valuesList);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `values`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
  private List<List<Object>> changeToArrayList(List<List<Object>> values) {
    if (!(values instanceof ArrayList)) {
      values = new ArrayList<>(values);
    }
    for (int i = 0; i < values.size(); i++) {
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `measurementsList`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
      times.sort(Long::compareTo);
      // sort measurementsList
      measurementsList = sortList(measurementsList, index);
      // sort valuesList
      valuesList = sortList(valuesList, index);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `valuesList`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
      measurementsList = sortList(measurementsList, index);
      // sort valuesList
      valuesList = sortList(valuesList, index);
    }

```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `measurements`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
      boolean isAligned) {
    if (hasNull(values)) {
      measurements = new ArrayList<>(measurements);
      values = new ArrayList<>(values);
      boolean isAllValueNull =
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `values`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
    if (hasNull(values)) {
      measurements = new ArrayList<>(measurements);
      values = new ArrayList<>(values);
      boolean isAllValueNull =
          filterNullValueAndMeasurementWithStringType(values, prefixPath, measurements);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `measurementsList`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
      throws IoTDBConnectionException {
    if (hasNull(valuesList)) {
      measurementsList = changeToArrayListWithStringType(measurementsList);
      valuesList = changeToArrayList(valuesList);
      deviceIds = new ArrayList<>(deviceIds);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `valuesList`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
    if (hasNull(valuesList)) {
      measurementsList = changeToArrayListWithStringType(measurementsList);
      valuesList = changeToArrayList(valuesList);
      deviceIds = new ArrayList<>(deviceIds);
      times = new ArrayList<>(times);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `deviceIds`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
      measurementsList = changeToArrayListWithStringType(measurementsList);
      valuesList = changeToArrayList(valuesList);
      deviceIds = new ArrayList<>(deviceIds);
      times = new ArrayList<>(times);
      typesList = changeToArrayListWithTSDataType(typesList);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `times`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
      valuesList = changeToArrayList(valuesList);
      deviceIds = new ArrayList<>(deviceIds);
      times = new ArrayList<>(times);
      typesList = changeToArrayListWithTSDataType(typesList);
      filterNullValueAndMeasurement(deviceIds, times, measurementsList, valuesList, typesList);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `typesList`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
      deviceIds = new ArrayList<>(deviceIds);
      times = new ArrayList<>(times);
      typesList = changeToArrayListWithTSDataType(typesList);
      filterNullValueAndMeasurement(deviceIds, times, measurementsList, valuesList, typesList);
    }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `measurements`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
      throws IoTDBConnectionException {
    if (hasNull(values)) {
      measurements = new ArrayList<>(measurements);
      values = new ArrayList<>(values);
      types = new ArrayList<>(types);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `values`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
    if (hasNull(values)) {
      measurements = new ArrayList<>(measurements);
      values = new ArrayList<>(values);
      types = new ArrayList<>(types);
      boolean isAllValuesNull =
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `types`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
      measurements = new ArrayList<>(measurements);
      values = new ArrayList<>(values);
      types = new ArrayList<>(types);
      boolean isAllValuesNull =
          filterNullValueAndMeasurement(prefixPath, measurements, types, values);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `measurementsList`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
      boolean isAligned) {
    if (hasNull(valuesList)) {
      measurementsList = changeToArrayListWithStringType(measurementsList);
      valuesList = changeToArrayListWithStringType(valuesList);
      times = new ArrayList<>(times);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `valuesList`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
    if (hasNull(valuesList)) {
      measurementsList = changeToArrayListWithStringType(measurementsList);
      valuesList = changeToArrayListWithStringType(valuesList);
      times = new ArrayList<>(times);
      filterNullValueAndMeasurementWithStringTypeOfOneDevice(
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `times`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
      measurementsList = changeToArrayListWithStringType(measurementsList);
      valuesList = changeToArrayListWithStringType(valuesList);
      times = new ArrayList<>(times);
      filterNullValueAndMeasurementWithStringTypeOfOneDevice(
          times, prefixPath, measurementsList, valuesList);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `measurementsList`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
      times.sort(Long::compareTo);
      // sort measurementList
      measurementsList = sortList(measurementsList, index);
      // sort typesList
      typesList = sortList(typesList, index);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `typesList`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
      measurementsList = sortList(measurementsList, index);
      // sort typesList
      typesList = sortList(typesList, index);
      // sort values
      valuesList = sortList(valuesList, index);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `valuesList`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
      typesList = sortList(typesList, index);
      // sort values
      valuesList = sortList(valuesList, index);
    }

```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `measurements`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
      throws IoTDBConnectionException {
    if (hasNull(values)) {
      measurements = new ArrayList<>(measurements);
      types = new ArrayList<>(types);
      values = new ArrayList<>(values);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `types`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
    if (hasNull(values)) {
      measurements = new ArrayList<>(measurements);
      types = new ArrayList<>(types);
      values = new ArrayList<>(values);
      boolean isAllValuesNull =
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `values`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
      measurements = new ArrayList<>(measurements);
      types = new ArrayList<>(types);
      values = new ArrayList<>(values);
      boolean isAllValuesNull =
          filterNullValueAndMeasurement(deviceId, measurements, types, values);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `extVal`
in `server/src/main/java/org/apache/iotdb/db/query/aggregation/impl/ExtremeAggrResult.java`
#### Snippet
```java
      Comparable<Object> absCurrentValue = (Comparable<Object>) getAbsValue(currentValue);
      if (extVal == null) {
        extVal = currentValue;
      } else {
        Comparable<Object> absExtVal = (Comparable<Object>) getAbsValue(extVal);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `extVal`
in `server/src/main/java/org/apache/iotdb/db/query/aggregation/impl/ExtremeAggrResult.java`
#### Snippet
```java
        if (absExtVal.compareTo(absCurrentValue) < 0
            || (absExtVal.compareTo(absCurrentValue) == 0 && extVal.compareTo(currentValue) < 0)) {
          extVal = currentValue;
        }
      }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `timeFilter`
in `server/src/main/java/org/apache/iotdb/db/query/executor/groupby/impl/LocalGroupByExecutor.java`
#### Snippet
```java
        QueryResourceManager.getInstance().getQueryDataSource(path, context, timeFilter, ascending);
    // update filter by TTL
    timeFilter = queryDataSource.updateFilterUsingTTL(timeFilter);
    // init SeriesAggregateReader for non-aligned series
    reader =
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `timeFilter`
in `server/src/main/java/org/apache/iotdb/db/query/executor/groupby/impl/LocalAlignedGroupByExecutor.java`
#### Snippet
```java
        QueryResourceManager.getInstance().getQueryDataSource(path, context, timeFilter, ascending);
    // update filter by TTL
    timeFilter = queryDataSource.updateFilterUsingTTL(timeFilter);

    // init AlignedSeriesAggregateReader for aligned series
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `lBoundTime`
in `server/src/main/java/org/apache/iotdb/db/query/executor/fill/LastPointReader.java`
#### Snippet
```java
      if (!timeseriesMetadata.isModified()) {
        if (endtimeContainedByTimeFilter(timeseriesMetadata.getStatistics())) {
          lBoundTime = Math.max(lBoundTime, timeseriesMetadata.getStatistics().getEndTime());
        } else {
          lBoundTime = Math.max(lBoundTime, timeseriesMetadata.getStatistics().getStartTime());
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `lBoundTime`
in `server/src/main/java/org/apache/iotdb/db/query/executor/fill/LastPointReader.java`
#### Snippet
```java
          lBoundTime = Math.max(lBoundTime, timeseriesMetadata.getStatistics().getEndTime());
        } else {
          lBoundTime = Math.max(lBoundTime, timeseriesMetadata.getStatistics().getStartTime());
        }
      }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `filename`
in `server/src/main/java/org/apache/iotdb/db/tools/IoTDBDataDirViewer.java`
#### Snippet
```java

  private static void printResource(String filename, PrintWriter pw) throws IOException {
    filename = filename.substring(0, filename.length() - 9);
    TsFileResource resource = new TsFileResource(SystemFileFactory.INSTANCE.getFile(filename));
    resource.deserialize();
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `filename`
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileResourcePrinter.java`
#### Snippet
```java
  @SuppressWarnings("squid:S106")
  public static void printResource(String filename) throws IOException {
    filename = filename.substring(0, filename.length() - 9);
    TsFileResource resource = new TsFileResource(SystemFileFactory.INSTANCE.getFile(filename));
    System.out.printf("Analyzing %s ...%n", filename);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `deviceId`
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileSelfCheckTool.java`
#### Snippet
```java
          // deviceId should be determined by LEAF_DEVICE node
          if (type.equals(MetadataIndexNodeType.LEAF_DEVICE)) {
            deviceId = metadataIndex.getName();
          }
          MetadataIndexNode metadataIndexNode = MetadataIndexNode.deserializeFrom(buffer);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `e`
in `server/src/main/java/org/apache/iotdb/db/utils/ErrorHandlingUtils.java`
#### Snippet
```java
  public static Throwable getRootCause(Throwable e) {
    while (e.getCause() != null) {
      e = e.getCause();
    }
    return e;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `value`
in `server/src/main/java/org/apache/iotdb/db/utils/CommonUtils.java`
#### Snippet
```java
      case INT32:
        if (destDataType == TSDataType.INT64) {
          value = Arrays.stream((int[]) value).mapToLong(Long::valueOf).toArray();
        } else if (destDataType == TSDataType.FLOAT) {
          int[] tmp = (int[]) value;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `value`
in `server/src/main/java/org/apache/iotdb/db/utils/CommonUtils.java`
#### Snippet
```java
            result[i] = (float) tmp[i];
          }
          value = result;
        } else if (destDataType == TSDataType.DOUBLE) {
          value = Arrays.stream((int[]) value).mapToDouble(Double::valueOf).toArray();
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `value`
in `server/src/main/java/org/apache/iotdb/db/utils/CommonUtils.java`
#### Snippet
```java
          value = result;
        } else if (destDataType == TSDataType.DOUBLE) {
          value = Arrays.stream((int[]) value).mapToDouble(Double::valueOf).toArray();
        }
        break;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `value`
in `server/src/main/java/org/apache/iotdb/db/utils/CommonUtils.java`
#### Snippet
```java
      case INT64:
        if (destDataType == TSDataType.DOUBLE) {
          value = Arrays.stream((long[]) value).mapToDouble(Double::valueOf).toArray();
        }
        break;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `value`
in `server/src/main/java/org/apache/iotdb/db/utils/CommonUtils.java`
#### Snippet
```java
            result[i] = tmp[i];
          }
          value = result;
        }
        break;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `value`
in `server/src/main/java/org/apache/iotdb/db/utils/CommonUtils.java`
#### Snippet
```java

  private static boolean parseBoolean(String value) throws QueryProcessException {
    value = value.toLowerCase();
    if (SQLConstant.BOOLEAN_FALSE_NUM.equals(value) || SQLConstant.BOOLEAN_FALSE.equals(value)) {
      return false;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `value`
in `server/src/main/java/org/apache/iotdb/db/utils/CommonUtils.java`
#### Snippet
```java
      case INT32:
        if (destDataType == TSDataType.INT64) {
          value = (long) ((int) value);
        } else if (destDataType == TSDataType.FLOAT) {
          value = (float) ((int) value);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `value`
in `server/src/main/java/org/apache/iotdb/db/utils/CommonUtils.java`
#### Snippet
```java
          value = (long) ((int) value);
        } else if (destDataType == TSDataType.FLOAT) {
          value = (float) ((int) value);
        } else if (destDataType == TSDataType.DOUBLE) {
          value = (double) ((int) value);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `value`
in `server/src/main/java/org/apache/iotdb/db/utils/CommonUtils.java`
#### Snippet
```java
          value = (float) ((int) value);
        } else if (destDataType == TSDataType.DOUBLE) {
          value = (double) ((int) value);
        }
        break;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `value`
in `server/src/main/java/org/apache/iotdb/db/utils/CommonUtils.java`
#### Snippet
```java
      case INT64:
        if (destDataType == TSDataType.DOUBLE) {
          value = (double) ((long) value);
        }
        break;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `value`
in `server/src/main/java/org/apache/iotdb/db/utils/CommonUtils.java`
#### Snippet
```java
      case FLOAT:
        if (destDataType == TSDataType.DOUBLE) {
          value = (double) ((float) value);
        }
        break;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `deviceId`
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileSketchTool.java`
#### Snippet
```java
          // deviceId should be determined by LEAF_DEVICE node
          if (type.equals(MetadataIndexNodeType.LEAF_DEVICE)) {
            deviceId = metadataIndex.getName();
          }
          MetadataIndexNode metadataIndexNode = MetadataIndexNode.deserializeFrom(buffer);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `currEx`
in `server/src/main/java/org/apache/iotdb/db/utils/SchemaUtils.java`
#### Snippet
```java
        break;
      }
      currEx = currEx.getCause();
    }
    return null;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `hi`
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/TimBinaryTVList.java`
#### Snippet
```java
  @Override
  public void reverseRange(int lo, int hi) {
    hi--;
    while (lo < hi) {
      long loT = getTime(lo);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `lo`
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/TimBinaryTVList.java`
#### Snippet
```java
      long hiT = getTime(hi);
      Binary hiV = getBinary(hi);
      set(lo++, hiT, hiV);
      set(hi--, loT, loV);
    }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `hi`
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/TimBinaryTVList.java`
#### Snippet
```java
      Binary hiV = getBinary(hi);
      set(lo++, hiT, hiV);
      set(hi--, loT, loV);
    }
  }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `hi`
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/TimFloatTVList.java`
#### Snippet
```java
  @Override
  public void reverseRange(int lo, int hi) {
    hi--;
    while (lo < hi) {
      long loT = getTime(lo);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `lo`
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/TimFloatTVList.java`
#### Snippet
```java
      long hiT = getTime(hi);
      float hiV = getFloat(hi);
      set(lo++, hiT, hiV);
      set(hi--, loT, loV);
    }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `hi`
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/TimFloatTVList.java`
#### Snippet
```java
      float hiV = getFloat(hi);
      set(lo++, hiT, hiV);
      set(hi--, loT, loV);
    }
  }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `hi`
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/TimLongTVList.java`
#### Snippet
```java
  @Override
  public void reverseRange(int lo, int hi) {
    hi--;
    while (lo < hi) {
      long loT = getTime(lo);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `lo`
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/TimLongTVList.java`
#### Snippet
```java
      long hiT = getTime(hi);
      long hiV = getLong(hi);
      set(lo++, hiT, hiV);
      set(hi--, loT, loV);
    }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `hi`
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/TimLongTVList.java`
#### Snippet
```java
      long hiV = getLong(hi);
      set(lo++, hiT, hiV);
      set(hi--, loT, loV);
    }
  }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `hi`
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/TimAlignedTVList.java`
#### Snippet
```java
  @Override
  public void reverseRange(int lo, int hi) {
    hi--;
    while (lo < hi) {
      long loT = getTime(lo);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `lo`
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/TimAlignedTVList.java`
#### Snippet
```java
      long hiT = getTime(hi);
      int hiV = getValueIndex(hi);
      set(lo++, hiT, hiV);
      set(hi--, loT, loV);
    }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `hi`
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/TimAlignedTVList.java`
#### Snippet
```java
      int hiV = getValueIndex(hi);
      set(lo++, hiT, hiV);
      set(hi--, loT, loV);
    }
  }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `hi`
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/TimIntTVList.java`
#### Snippet
```java
  @Override
  public void reverseRange(int lo, int hi) {
    hi--;
    while (lo < hi) {
      long loT = getTime(lo);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `lo`
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/TimIntTVList.java`
#### Snippet
```java
      long hiT = getTime(hi);
      int hiV = getInt(hi);
      set(lo++, hiT, hiV);
      set(hi--, loT, loV);
    }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `hi`
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/TimIntTVList.java`
#### Snippet
```java
      int hiV = getInt(hi);
      set(lo++, hiT, hiV);
      set(hi--, loT, loV);
    }
  }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `time`
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/BooleanTVList.java`
#### Snippet
```java
      long[] clonedTime = new long[end - start];
      System.arraycopy(time, start, clonedTime, 0, end - start);
      time = clonedTime;
      timeIdxOffset = start;
      // drop null at the end of value array
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `end`
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/BooleanTVList.java`
#### Snippet
```java
      int nullCnt =
          dropNullValThenUpdateMaxTimeAndSorted(time, value, bitMap, start, end, timeIdxOffset);
      end -= nullCnt;
    } else {
      updateMaxTimeAndSorted(time, start, end);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `time`
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/DoubleTVList.java`
#### Snippet
```java
      long[] clonedTime = new long[end - start];
      System.arraycopy(time, start, clonedTime, 0, end - start);
      time = clonedTime;
      timeIdxOffset = start;
      // drop null at the end of value array
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `end`
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/DoubleTVList.java`
#### Snippet
```java
      int nullCnt =
          dropNullValThenUpdateMaxTimeAndSorted(time, value, bitMap, start, end, timeIdxOffset);
      end -= nullCnt;
    } else {
      updateMaxTimeAndSorted(time, start, end);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `hi`
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/TimBooleanTVList.java`
#### Snippet
```java
  @Override
  public void reverseRange(int lo, int hi) {
    hi--;
    while (lo < hi) {
      long loT = getTime(lo);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `lo`
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/TimBooleanTVList.java`
#### Snippet
```java
      long hiT = getTime(hi);
      boolean hiV = getBoolean(hi);
      set(lo++, hiT, hiV);
      set(hi--, loT, loV);
    }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `hi`
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/TimBooleanTVList.java`
#### Snippet
```java
      boolean hiV = getBoolean(hi);
      set(lo++, hiT, hiV);
      set(hi--, loT, loV);
    }
  }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `remainingToCalculate`
in `server/src/main/java/org/apache/iotdb/db/query/executor/AggregationExecutor.java`
#### Snippet
```java
        if (aggregateResult.hasFinalResult()) {
          isCalculatedArray[i] = true;
          remainingToCalculate--;
          if (remainingToCalculate == 0) {
            return newRemainingToCalculate;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `remainingToCalculate`
in `server/src/main/java/org/apache/iotdb/db/query/executor/AggregationExecutor.java`
#### Snippet
```java
      if (seriesReader.canUseCurrentPageStatistics()) {
        Statistics pageStatistic = seriesReader.currentPageStatistics();
        remainingToCalculate =
            aggregateStatistics(
                aggregateResultList, isCalculatedArray, remainingToCalculate, pageStatistic);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `remainingToCalculate`
in `server/src/main/java/org/apache/iotdb/db/query/executor/AggregationExecutor.java`
#### Snippet
```java
      }
      IBatchDataIterator batchDataIterator = seriesReader.nextPage().getBatchDataIterator();
      remainingToCalculate =
          aggregateBatchData(
              aggregateResultList, isCalculatedArray, remainingToCalculate, batchDataIterator);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `remainingToCalculate`
in `server/src/main/java/org/apache/iotdb/db/query/executor/AggregationExecutor.java`
#### Snippet
```java
        while (seriesReader.hasNextSubSeries()) {
          Statistics pageStatistic = seriesReader.currentPageStatistics();
          remainingToCalculate =
              aggregateStatistics(
                  aggregateResultList.get(seriesReader.getCurIndex()),
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `remainingToCalculate`
in `server/src/main/java/org/apache/iotdb/db/query/executor/AggregationExecutor.java`
#### Snippet
```java
        int subIndex = seriesReader.getCurIndex();
        IBatchDataIterator batchIterator = nextOverlappedPageData.getBatchDataIterator(subIndex);
        remainingToCalculate =
            aggregateBatchData(
                aggregateResultList.get(subIndex),
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `timeFilter`
in `server/src/main/java/org/apache/iotdb/db/query/executor/AggregationExecutor.java`
#### Snippet
```java
    }
    // update filter by TTL
    timeFilter = queryDataSource.updateFilterUsingTTL(timeFilter);

    if (!isAggregateResultEmpty(ascAggregateResultList)) {
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `timeFilter`
in `server/src/main/java/org/apache/iotdb/db/query/executor/AggregationExecutor.java`
#### Snippet
```java
    }
    // update filter by TTL
    timeFilter = queryDataSource.updateFilterUsingTTL(timeFilter);

    if (ascAggregateResultList != null && !ascAggregateResultList.isEmpty()) {
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `time`
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/IntTVList.java`
#### Snippet
```java
      long[] clonedTime = new long[end - start];
      System.arraycopy(time, start, clonedTime, 0, end - start);
      time = clonedTime;
      timeIdxOffset = start;
      // drop null at the end of value array
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `end`
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/IntTVList.java`
#### Snippet
```java
      int nullCnt =
          dropNullValThenUpdateMaxTimeAndSorted(time, value, bitMap, start, end, timeIdxOffset);
      end -= nullCnt;
    } else {
      updateMaxTimeAndSorted(time, start, end);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `time`
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/FloatTVList.java`
#### Snippet
```java
      long[] clonedTime = new long[end - start];
      System.arraycopy(time, start, clonedTime, 0, end - start);
      time = clonedTime;
      timeIdxOffset = start;
      // drop null at the end of value array
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `end`
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/FloatTVList.java`
#### Snippet
```java
      int nullCnt =
          dropNullValThenUpdateMaxTimeAndSorted(time, value, bitMap, start, end, timeIdxOffset);
      end -= nullCnt;
    } else {
      updateMaxTimeAndSorted(time, start, end);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `hi`
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/TimDoubleTVList.java`
#### Snippet
```java
  @Override
  public void reverseRange(int lo, int hi) {
    hi--;
    while (lo < hi) {
      long loT = getTime(lo);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `lo`
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/TimDoubleTVList.java`
#### Snippet
```java
      long hiT = getTime(hi);
      double hiV = getDouble(hi);
      set(lo++, hiT, hiV);
      set(hi--, loT, loV);
    }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `hi`
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/TimDoubleTVList.java`
#### Snippet
```java
      double hiV = getDouble(hi);
      set(lo++, hiT, hiV);
      set(hi--, loT, loV);
    }
  }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `deleteCursor`
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/TVList.java`
#### Snippet
```java
        return true;
      } else if (deletionList.get(deleteCursor).getMax() < timestamp) {
        deleteCursor++;
      } else {
        return false;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `start`
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/TimSort.java`
#### Snippet
```java
    assert lo <= start && start <= hi;
    if (start == lo) {
      start++;
    }
    for (; start < hi; start++) {
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `start`
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/TimSort.java`
#### Snippet
```java
      start++;
    }
    for (; start < hi; start++) {

      saveAsPivot(start);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `time`
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/BinaryTVList.java`
#### Snippet
```java
      long[] clonedTime = new long[end - start];
      System.arraycopy(time, start, clonedTime, 0, end - start);
      time = clonedTime;
      timeIdxOffset = start;
      // drop null at the end of value array
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `end`
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/BinaryTVList.java`
#### Snippet
```java
      int nullCnt =
          dropNullValThenUpdateMaxTimeAndSorted(time, value, bitMap, start, end, timeIdxOffset);
      end -= nullCnt;
    } else {
      updateMaxTimeAndSorted(time, start, end);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `time`
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/LongTVList.java`
#### Snippet
```java
      long[] clonedTime = new long[end - start];
      System.arraycopy(time, start, clonedTime, 0, end - start);
      time = clonedTime;
      timeIdxOffset = start;
      // drop null at the end of value array
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `end`
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/LongTVList.java`
#### Snippet
```java
      int nullCnt =
          dropNullValThenUpdateMaxTimeAndSorted(time, value, bitMap, start, end, timeIdxOffset);
      end -= nullCnt;
    } else {
      updateMaxTimeAndSorted(time, start, end);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `bytesLength`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/CompactionTaskManager.java`
#### Snippet
```java
    while (bytesLength >= Integer.MAX_VALUE) {
      limiter.acquire(Integer.MAX_VALUE);
      bytesLength -= Integer.MAX_VALUE;
    }
    if (bytesLength > 0) {
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `filter`
in `server/src/main/java/org/apache/iotdb/db/engine/querycontext/QueryDataSource.java`
#### Snippet
```java
    if (dataTTL != Long.MAX_VALUE) {
      if (filter != null) {
        filter = new AndFilter(filter, TimeFilter.gtEq(DateTimeUtils.currentTime() - dataTTL));
      } else {
        filter = TimeFilter.gtEq(DateTimeUtils.currentTime() - dataTTL);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `filter`
in `server/src/main/java/org/apache/iotdb/db/engine/querycontext/QueryDataSource.java`
#### Snippet
```java
        filter = new AndFilter(filter, TimeFilter.gtEq(DateTimeUtils.currentTime() - dataTTL));
      } else {
        filter = TimeFilter.gtEq(DateTimeUtils.currentTime() - dataTTL);
      }
    }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `memTableIncrement`
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileProcessor.java`
#### Snippet
```java
      long memTableIncrement, long chunkMetadataIncrement, long textDataIncrement)
      throws WriteProcessException {
    memTableIncrement += textDataIncrement;
    dataRegionInfo.addStorageGroupMemCost(memTableIncrement);
    tsFileProcessorInfo.addTSPMemCost(chunkMetadataIncrement);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `node`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/CachedMTreeStore.java`
#### Snippet
```java
    while (!node.isStorageGroup()) {
      unPin(node);
      node = node.getParent();
    }
  }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `dataTTL`
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/DataRegion.java`
#### Snippet
```java
  public void setDataTTLWithTimePrecisionCheck(long dataTTL) {
    if (dataTTL != Long.MAX_VALUE) {
      dataTTL =
          DateTimeUtils.convertMilliTimeWithPrecision(
              dataTTL, IoTDBDescriptor.getInstance().getConfig().getTimestampPrecision());
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `position`
in `server/src/main/java/org/apache/iotdb/db/metadata/tag/TagLogFile.java`
#### Snippet
```java
    if (position < 0) {
      // append the record to file tail
      position = fileChannel.size();
    }
    fileChannel.write(byteBuffer, position);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `pw`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/SchemaFile.java`
#### Snippet
```java
            lastPageIndex + 1);
    if (pw == null) {
      pw = new PrintWriter(System.out);
    }
    pw.print(header);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `node`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/traverser/counter/MNodeLevelCounter.java`
#### Snippet
```java
    Iterator<IMNode> iterator = traverseContext.iterator();
    while (level > targetLevel && iterator.hasNext()) {
      node = iterator.next();
      level--;
    }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `level`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/traverser/counter/MNodeLevelCounter.java`
#### Snippet
```java
    while (level > targetLevel && iterator.hasNext()) {
      node = iterator.next();
      level--;
    }

```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `node`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/traverser/collector/MNodeCollector.java`
#### Snippet
```java
      Deque<IMNode> stack = new ArrayDeque<>();
      while (level > targetLevel) {
        node = traverseContext.pop();
        stack.push(node);
        level--;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `level`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/traverser/collector/MNodeCollector.java`
#### Snippet
```java
        node = traverseContext.pop();
        stack.push(node);
        level--;
      }
      // record processed node so they will not be processed twice
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `deviceID`
in `server/src/main/java/org/apache/iotdb/db/metadata/idtable/entry/SHA256DeviceID.java`
#### Snippet
```java
  private void fromSHA256String(String deviceID) {
    if (deviceID.startsWith("`") && deviceID.endsWith("`")) {
      deviceID = deviceID.substring(1, deviceID.length() - 1);
    }
    String[] part = deviceID.split(SEPARATOR);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `offset`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/MTreeBelowSGMemoryImpl.java`
#### Snippet
```java
    collector.setPrefixMatch(isPrefixMatch);
    collector.traverse();
    offset = collector.getCurOffset() + 1;
    return new Pair<>(result, offset);
  }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `offset`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/MTreeBelowSGCachedImpl.java`
#### Snippet
```java
    collector.setPrefixMatch(isPrefixMatch);
    collector.traverse();
    offset = collector.getCurOffset() + 1;
    return new Pair<>(result, offset);
  }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `precision`
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/input/InfluxLineParser.java`
#### Snippet
```java
  public static List<Point> parserRecordsToPoints(String records, TimeUnit precision) {
    if (precision == null) {
      precision = TimeUnit.NANOSECONDS;
    }
    ArrayList<Point> points = new ArrayList<>();
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `precision`
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/input/InfluxLineParser.java`
#### Snippet
```java
  public static Point parseToPoint(String input, TimeUnit precision) {
    if (precision == null) {
      precision = TimeUnit.NANOSECONDS;
    }
    CharStream charStream = new ANTLRInputStream(input);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `offset`
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/SchemaRegionMemoryImpl.java`
#### Snippet
```java
        if ((plan.getTags() != null && !plan.getTags().isEmpty())
            || (plan.getAttributes() != null && !plan.getAttributes().isEmpty())) {
          offset = tagManager.writeTagFile(plan.getTags(), plan.getAttributes());
        }
        plan.setTagOffset(offset);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `right`
in `library-udf/src/main/java/org/apache/iotdb/library/anomaly/UDTFLOF.java`
#### Snippet
```java
    while (left < right) {
      while (left < right && a[right][1] >= key) {
        right--;
      }
      if (left < right) {
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `left`
in `library-udf/src/main/java/org/apache/iotdb/library/anomaly/UDTFLOF.java`
#### Snippet
```java
      }
      while (left < right && a[left][1] <= key) {
        left++;
      }
      if (left < right) {
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `s`
in `library-udf/src/main/java/org/apache/iotdb/library/util/Util.java`
#### Snippet
```java
  public static long parseTime(String s) {
    long unit = 0;
    s = s.toLowerCase();
    s = s.replaceAll(" ", "");
    if (s.endsWith("ms")) {
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `s`
in `library-udf/src/main/java/org/apache/iotdb/library/util/Util.java`
#### Snippet
```java
    long unit = 0;
    s = s.toLowerCase();
    s = s.replaceAll(" ", "");
    if (s.endsWith("ms")) {
      unit = 1;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `s`
in `library-udf/src/main/java/org/apache/iotdb/library/util/Util.java`
#### Snippet
```java
    if (s.endsWith("ms")) {
      unit = 1;
      s = s.substring(0, s.length() - 2);
    } else if (s.endsWith("s")) {
      unit = 1000;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `s`
in `library-udf/src/main/java/org/apache/iotdb/library/util/Util.java`
#### Snippet
```java
    } else if (s.endsWith("s")) {
      unit = 1000;
      s = s.substring(0, s.length() - 1);
    } else if (s.endsWith("m")) {
      unit = 60 * 1000L;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `s`
in `library-udf/src/main/java/org/apache/iotdb/library/util/Util.java`
#### Snippet
```java
    } else if (s.endsWith("m")) {
      unit = 60 * 1000L;
      s = s.substring(0, s.length() - 1);
    } else if (s.endsWith("h")) {
      unit = 60 * 60 * 1000L;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `s`
in `library-udf/src/main/java/org/apache/iotdb/library/util/Util.java`
#### Snippet
```java
    } else if (s.endsWith("h")) {
      unit = 60 * 60 * 1000L;
      s = s.substring(0, s.length() - 1);
    } else if (s.endsWith("d")) {
      unit = 24 * 60 * 60 * 1000L;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `s`
in `library-udf/src/main/java/org/apache/iotdb/library/util/Util.java`
#### Snippet
```java
    } else if (s.endsWith("d")) {
      unit = 24 * 60 * 60 * 1000L;
      s = s.substring(0, s.length() - 1);
    }
    double v = Double.parseDouble(s);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `result`
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/UDAFQuantile.java`
#### Snippet
```java
        return (double) (result);
      case FLOAT:
        result = (result >>> 31) == 0 ? result : result ^ Long.MAX_VALUE;
        return Float.intBitsToFloat((int) (result));
      case INT64:
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `result`
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/UDAFQuantile.java`
#### Snippet
```java
        return (double) (result);
      case DOUBLE:
        result = (result >>> 63) == 0 ? result : result ^ Long.MAX_VALUE;
        return Double.longBitsToDouble(result);
      default:
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `v`
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/MADSketch.java`
#### Snippet
```java
  public void insert(double v, double[] bounds) {
    if (v < bounds[0]) {
      v = bounds[0];
    } else if (v > bounds[1] && v < bounds[2]) {
      v = bounds[2];
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `v`
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/MADSketch.java`
#### Snippet
```java
      v = bounds[0];
    } else if (v > bounds[1] && v < bounds[2]) {
      v = bounds[2];
    } else if (v > bounds[3] && v < bounds[4]) {
      v = bounds[4];
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `v`
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/MADSketch.java`
#### Snippet
```java
      v = bounds[2];
    } else if (v > bounds[3] && v < bounds[4]) {
      v = bounds[4];
    } else if (v > bounds[5]) {
      v = bounds[5];
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `v`
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/MADSketch.java`
#### Snippet
```java
      v = bounds[4];
    } else if (v > bounds[5]) {
      v = bounds[5];
    }
    insert(v);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `pos`
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/HeapLongKLLSketch.java`
#### Snippet
```java
      int p1 = L1, p2 = L2;
      while (p1 < R1 || p2 < R2)
        if (p1 < R1 && (p2 == R2 || a1[p1] < a2[p2])) a3[pos++] = a1[p1++];
        else a3[pos++] = a2[p2++];
    }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `pos`
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/HeapLongKLLSketch.java`
#### Snippet
```java
      while (p1 < R1 || p2 < R2)
        if (p1 < R1 && (p2 == R2 || a1[p1] < a2[p2])) a3[pos++] = a1[p1++];
        else a3[pos++] = a2[p2++];
    }
    return R1 - L1 + R2 - L2;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `dir`
in `confignode/src/main/java/org/apache/iotdb/confignode/conf/ConfigNodeConfig.java`
#### Snippet
```java
    if (!new File(dir).isAbsolute() && homeDir != null && homeDir.length() > 0) {
      if (!homeDir.endsWith(File.separator)) {
        dir = homeDir + File.separatorChar + dir;
      } else {
        dir = homeDir + dir;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `dir`
in `confignode/src/main/java/org/apache/iotdb/confignode/conf/ConfigNodeConfig.java`
#### Snippet
```java
        dir = homeDir + File.separatorChar + dir;
      } else {
        dir = homeDir + dir;
      }
    }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `functionName`
in `confignode/src/main/java/org/apache/iotdb/confignode/manager/UDFManager.java`
#### Snippet
```java

  public TSStatus dropFunction(String functionName) {
    functionName = functionName.toUpperCase();
    udfInfo.acquireUDFTableLock();
    try {
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `inputFlow`
in `confignode/src/main/java/org/apache/iotdb/confignode/manager/load/balancer/router/leader/MinCostFlowLeaderBalancer.java`
#### Snippet
```java
        minCostFlowEdges.get(currentEdge ^ 1).capacity += subOutputFlow;

        inputFlow -= subOutputFlow;
        outputFlow += subOutputFlow;

```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `offset`
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/SchemaRegionSchemaFileImpl.java`
#### Snippet
```java
          if ((plan.getTags() != null && !plan.getTags().isEmpty())
              || (plan.getAttributes() != null && !plan.getAttributes().isEmpty())) {
            offset = tagManager.writeTagFile(plan.getTags(), plan.getAttributes());
          }
          plan.setTagOffset(offset);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `proc`
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/Procedure.java`
#### Snippet
```java
  protected static long getRootProcedureId(Map<Long, Procedure> procedures, Procedure proc) {
    while (proc.hasParent()) {
      proc = procedures.get(proc.getParentProcId());
      if (proc == null) {
        return NO_PROC_ID;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `username`
in `confignode/src/main/java/org/apache/iotdb/confignode/persistence/AuthorInfo.java`
#### Snippet
```java
        // Bring this user's permission information back to the datanode for caching
        if (authorizer instanceof OpenIdAuthorizer) {
          username = ((OpenIdAuthorizer) authorizer).getIoTDBUserName(username);
          result = getUserPermissionInfo(username);
          result.getUserInfo().setIsOpenIdUser(true);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `host`
in `example/rocketmq/src/main/java/org/apache/iotdb/rocketmq/RocketMQConsumer.java`
#### Snippet
```java
      throws IoTDBConnectionException, StatementExecutionException {
    if (host == null) {
      host = Constant.IOTDB_CONNECTION_HOST;
      port = Constant.IOTDB_CONNECTION_PORT;
    }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `port`
in `example/rocketmq/src/main/java/org/apache/iotdb/rocketmq/RocketMQConsumer.java`
#### Snippet
```java
    if (host == null) {
      host = Constant.IOTDB_CONNECTION_HOST;
      port = Constant.IOTDB_CONNECTION_PORT;
    }
    user = (user == null ? Constant.IOTDB_CONNECTION_USER : user);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `user`
in `example/rocketmq/src/main/java/org/apache/iotdb/rocketmq/RocketMQConsumer.java`
#### Snippet
```java
      port = Constant.IOTDB_CONNECTION_PORT;
    }
    user = (user == null ? Constant.IOTDB_CONNECTION_USER : user);
    password = (password == null ? Constant.IOTDB_CONNECTION_PASSWORD : password);
    session = new Session(host, port, user, password);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `password`
in `example/rocketmq/src/main/java/org/apache/iotdb/rocketmq/RocketMQConsumer.java`
#### Snippet
```java
    }
    user = (user == null ? Constant.IOTDB_CONNECTION_USER : user);
    password = (password == null ? Constant.IOTDB_CONNECTION_PASSWORD : password);
    session = new Session(host, port, user, password);
    session.open();
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `filter`
in `spark-tsfile/src/main/java/org/apache/iotdb/spark/tsfile/qp/QueryProcessor.java`
#### Snippet
```java
    if (filter != null) {
      RemoveNotOptimizer removeNot = new RemoveNotOptimizer();
      filter = removeNot.optimize(filter);

      DNFFilterOptimizer dnf = new DNFFilterOptimizer();
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `filter`
in `spark-tsfile/src/main/java/org/apache/iotdb/spark/tsfile/qp/QueryProcessor.java`
#### Snippet
```java

      DNFFilterOptimizer dnf = new DNFFilterOptimizer();
      filter = dnf.optimize(filter);

      // merge different query path
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `filter`
in `spark-tsfile/src/main/java/org/apache/iotdb/spark/tsfile/qp/QueryProcessor.java`
#### Snippet
```java
      // => or (or (sensor_1 > 20, sensor_1 < 10), sensor_2 > 10)
      MergeSingleFilterOptimizer merge = new MergeSingleFilterOptimizer();
      filter = merge.optimize(filter);

      List<FilterOperator> filterOperators = splitFilter(filter);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `chunkHeaderSize`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/file/header/ChunkHeader.java`
#### Snippet
```java
    CompressionType type = ReadWriteIOUtils.readCompressionType(buffer);
    TSEncoding encoding = ReadWriteIOUtils.readEncoding(buffer);
    chunkHeaderSize =
        chunkHeaderSize - Integer.BYTES - 1 + ReadWriteForEncodingUtils.uVarIntSize(dataSize);
    return new ChunkHeader(
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `deviceId`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/v2/read/TsFileSequenceReaderForV2.java`
#### Snippet
```java
      case LEAF_DEVICE:
      case INTERNAL_MEASUREMENT:
        deviceId = metadataIndex.getName();
        MetadataIndexNode metadataIndexNode = MetadataIndexNodeV2.deserializeFrom(buffer);
        int metadataIndexListSize = metadataIndexNode.getChildren().size();
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `msg`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/parser/PathParseError.java`
#### Snippet
```java
        // node name
        if (expectedTokenNames.contains("*") && expectedTokenNames.contains("**")) {
          msg = msg.replace(expectedTokensString, "{ID, QUOTED_ID, *, **}");
        } else {
          msg = msg.replace(expectedTokensString, "{ID, QUOTED_ID}");
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `msg`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/parser/PathParseError.java`
#### Snippet
```java
          msg = msg.replace(expectedTokensString, "{ID, QUOTED_ID, *, **}");
        } else {
          msg = msg.replace(expectedTokensString, "{ID, QUOTED_ID}");
        }
      }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `str`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/parser/PathVisitor.java`
#### Snippet
```java
        return false;
      } else {
        str = removeSign;
      }
    }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `len`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BitReader.java`
#### Snippet
```java
      // Number of bits read from the current byte
      int m = len + bitCnt >= BITS_IN_A_BYTE ? BITS_IN_A_BYTE - bitCnt : len;
      len -= m;
      ret = ret << m;
      byte y = (byte) (cache & MASKS[bitCnt]); // Truncate the low bits with &
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `errorPercent`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BloomFilter.java`
#### Snippet
```java
   */
  public static BloomFilter getEmptyBloomFilter(double errorPercent, int numOfString) {
    errorPercent = Math.max(errorPercent, TSFileConfig.MIN_BLOOM_FILTER_ERROR_RATE);
    errorPercent = Math.min(errorPercent, TSFileConfig.MAX_BLOOM_FILTER_ERROR_RATE);

```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `errorPercent`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BloomFilter.java`
#### Snippet
```java
  public static BloomFilter getEmptyBloomFilter(double errorPercent, int numOfString) {
    errorPercent = Math.max(errorPercent, TSFileConfig.MIN_BLOOM_FILTER_ERROR_RATE);
    errorPercent = Math.min(errorPercent, TSFileConfig.MAX_BLOOM_FILTER_ERROR_RATE);

    double ln2 = Math.log(2);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `offset`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/Murmur128Hash.java`
#### Snippet
```java
    // tail
    // Advance offset to the unprocessed tail of the data.
    offset += nblocks * 16;
    long k1 = 0;
    long k2 = 0;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `k`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/Murmur128Hash.java`
#### Snippet
```java

  private static long fmix(long k) {
    k ^= k >>> 33;
    k *= 0xff51afd7ed558ccdL;
    k ^= k >>> 33;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `k`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/Murmur128Hash.java`
#### Snippet
```java
  private static long fmix(long k) {
    k ^= k >>> 33;
    k *= 0xff51afd7ed558ccdL;
    k ^= k >>> 33;
    k *= 0xc4ceb9fe1a85ec53L;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `k`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/Murmur128Hash.java`
#### Snippet
```java
    k ^= k >>> 33;
    k *= 0xff51afd7ed558ccdL;
    k ^= k >>> 33;
    k *= 0xc4ceb9fe1a85ec53L;
    k ^= k >>> 33;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `k`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/Murmur128Hash.java`
#### Snippet
```java
    k *= 0xff51afd7ed558ccdL;
    k ^= k >>> 33;
    k *= 0xc4ceb9fe1a85ec53L;
    k ^= k >>> 33;
    return k;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `k`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/Murmur128Hash.java`
#### Snippet
```java
    k ^= k >>> 33;
    k *= 0xc4ceb9fe1a85ec53L;
    k ^= k >>> 33;
    return k;
  }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `value`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/ReadWriteForEncodingUtils.java`
#### Snippet
```java
    while ((value & 0xFFFFFF80) != 0L) {
      length++;
      value >>>= 7;
    }
    length++;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `value`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/ReadWriteForEncodingUtils.java`
#### Snippet
```java

    byte[] res = new byte[length];
    value = preValue;
    int i = 0;
    while ((value & 0xFFFFFF80) != 0L) {
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `value`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/ReadWriteForEncodingUtils.java`
#### Snippet
```java
    while ((value & 0xFFFFFF80) != 0L) {
      res[i] = (byte) ((value & 0x7F) | 0x80);
      value >>>= 7;
      i++;
    }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `value`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/ReadWriteForEncodingUtils.java`
#### Snippet
```java
    while ((value & 0xFFFFFF80) != 0L) {
      out.write((value & 0x7F) | 0x80);
      value >>>= 7;
      position++;
    }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `value`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/ReadWriteForEncodingUtils.java`
#### Snippet
```java
    int position = 1;
    while ((value & 0xFFFFFF80) != 0L) {
      value >>>= 7;
      position++;
    }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `value`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/ReadWriteForEncodingUtils.java`
#### Snippet
```java
    while ((value & 0xFFFFFF80) != 0L) {
      out.write((value & 0x7F) | 0x80);
      value >>>= 7;
      position++;
    }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `value`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/ReadWriteForEncodingUtils.java`
#### Snippet
```java
    while ((value & 0xFFFFFF80) != 0L) {
      buffer.put((byte) ((value & 0x7F) | 0x80));
      value >>>= 7;
      position++;
    }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `x`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BitConstructor.java`
#### Snippet
```java

  public void add(long x, int len) {
    x = x & ~(ALL_MASK << len); // Make sure that all bits expect the lowest len bits of x are 0
    while (len > 0) {
      // Number of bits inserted into cache
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `len`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BitConstructor.java`
#### Snippet
```java
      // Number of bits inserted into cache
      int m = len + cnt >= BITS_IN_A_BYTE ? BITS_IN_A_BYTE - cnt : len;
      len -= m;
      cnt += m;
      byte y = (byte) (x >> len);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `x`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BitConstructor.java`
#### Snippet
```java
      y = (byte) (y << (BITS_IN_A_BYTE - cnt));
      cache = (byte) (cache | y);
      x = x & ~(ALL_MASK << len);
      if (cnt == BITS_IN_A_BYTE) {
        pad();
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `width`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BytesUtils.java`
#### Snippet
```java
      while (width > 0) {
        int m = width + cnt >= 8 ? 8 - cnt : width;
        width -= m;
        int mask = 1 << (8 - cnt);
        cnt += m;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `srcNum`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BytesUtils.java`
#### Snippet
```java
        mask = ~(mask - (1 << (8 - cnt)));
        result[index] = (byte) (result[index] & mask | y);
        srcNum = srcNum & ~(-1L << width);
        if (cnt == 8) {
          index++;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `offset`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BytesUtils.java`
#### Snippet
```java
   */
  public static int getByteN(byte data, int offset) {
    offset %= 8;
    if (((0xff & data) & (1 << (7 - offset))) != 0) {
      return 1;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `width`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BytesUtils.java`
#### Snippet
```java
      while (width > 0) {
        int m = width + cnt >= 8 ? 8 - cnt : width;
        width -= m;
        int mask = 1 << (8 - cnt);
        cnt += m;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `srcNum`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BytesUtils.java`
#### Snippet
```java
        mask = ~(mask - (1 << (8 - cnt)));
        result[index] = (byte) (result[index] & mask | y);
        srcNum = srcNum & ~(-1 << width);
        if (cnt == 8) {
          index++;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `width`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BytesUtils.java`
#### Snippet
```java
    while (width > 0) {
      int m = width + cnt >= 8 ? 8 - cnt : width;
      width -= m;
      ret = ret << m;
      byte y = (byte) (result[index] & (0xff >> cnt));
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `width`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BytesUtils.java`
#### Snippet
```java
    while (width > 0) {
      int m = width + cnt >= 8 ? 8 - cnt : width;
      width -= m;
      ret = ret << m;
      byte y = (byte) (result[index] & (0xff >> cnt));
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `offset`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BytesUtils.java`
#### Snippet
```java
   */
  public static byte setByteN(byte data, int offset, int value) {
    offset %= 8;
    if (value == 1) {
      return (byte) ((0xff & data) | (1 << (7 - offset)));
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `offset`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BytesUtils.java`
#### Snippet
```java
   */
  public static int getIntN(int data, int offset) {
    offset %= 32;
    if ((data & (1 << (offset))) != 0) {
      return 1;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `offset`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BytesUtils.java`
#### Snippet
```java
   */
  public static int getLongN(long data, int offset) {
    offset %= 64;
    if ((data & (1L << (offset))) != 0) {
      return 1;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `offset`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BytesUtils.java`
#### Snippet
```java
   */
  public static int setIntN(int data, int offset, int value) {
    offset %= 32;
    if (value == 1) {
      return (data | (1 << (offset)));
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `offset`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BytesUtils.java`
#### Snippet
```java
   */
  public static long setLongN(long data, int offset, int value) {
    offset %= 64;
    if (value == 1) {
      return (data | (1L << (offset)));
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `leftExpression`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/expression/util/ExpressionOptimizer.java`
#### Snippet
```java
      IExpression leftChild = ((BinaryExpression) rightExpression).getLeft();
      IExpression rightChild = ((BinaryExpression) rightExpression).getRight();
      leftExpression = mergeSecondTreeToFirstTree(leftExpression, leftChild);
      leftExpression = mergeSecondTreeToFirstTree(leftExpression, rightChild);
      return leftExpression;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `leftExpression`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/expression/util/ExpressionOptimizer.java`
#### Snippet
```java
      IExpression rightChild = ((BinaryExpression) rightExpression).getRight();
      leftExpression = mergeSecondTreeToFirstTree(leftExpression, leftChild);
      leftExpression = mergeSecondTreeToFirstTree(leftExpression, rightChild);
      return leftExpression;
    } else {
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `filePath`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/FilePathUtils.java`
#### Snippet
```java
  public static String regularizePath(String filePath) {
    if (filePath.length() > 0 && filePath.charAt(filePath.length() - 1) != File.separatorChar) {
      filePath = filePath + File.separatorChar;
    }
    return filePath;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `batchSize`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/chunk/ChunkWriterImpl.java`
#### Snippet
```java
  public void write(long[] timestamps, int[] values, int batchSize) {
    if (isSdtEncoding) {
      batchSize = sdtEncoder.encode(timestamps, values, batchSize);
    }
    pageWriter.write(timestamps, values, batchSize);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `batchSize`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/chunk/ChunkWriterImpl.java`
#### Snippet
```java
  public void write(long[] timestamps, double[] values, int batchSize) {
    if (isSdtEncoding) {
      batchSize = sdtEncoder.encode(timestamps, values, batchSize);
    }
    pageWriter.write(timestamps, values, batchSize);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `batchSize`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/chunk/ChunkWriterImpl.java`
#### Snippet
```java
  public void write(long[] timestamps, float[] values, int batchSize) {
    if (isSdtEncoding) {
      batchSize = sdtEncoder.encode(timestamps, values, batchSize);
    }
    pageWriter.write(timestamps, values, batchSize);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `batchSize`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/chunk/ChunkWriterImpl.java`
#### Snippet
```java
  public void write(long[] timestamps, long[] values, int batchSize) {
    if (isSdtEncoding) {
      batchSize = sdtEncoder.encode(timestamps, values, batchSize);
    }
    pageWriter.write(timestamps, values, batchSize);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `startValue`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/TsFileGeneratorUtils.java`
#### Snippet
```java
      boolean isAligned)
      throws IOException, WriteProcessException {
    for (long time = startTime; time < rowSize + startTime; time++, startValue++) {
      // construct TsRecord
      TSRecord tsRecord = new TSRecord(time, deviceId);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `startValue`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/TsFileGeneratorUtils.java`
#### Snippet
```java
    long sensorNum = schemas.size();

    for (long r = 0; r < rowNum; r++, startValue++) {
      int row = tablet.rowSize++;
      timestamps[row] = startTime++;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `startTime`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/TsFileGeneratorUtils.java`
#### Snippet
```java
    for (long r = 0; r < rowNum; r++, startValue++) {
      int row = tablet.rowSize++;
      timestamps[row] = startTime++;
      for (int i = 0; i < sensorNum; i++) {
        long[] sensor = (long[]) values[i];
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `size`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/RamUsageEstimator.java`
#### Snippet
```java
  /** Aligns an object size to be the next multiple of {@link #NUM_BYTES_OBJECT_ALIGNMENT}. */
  public static long alignObjectSize(long size) {
    size += (long) NUM_BYTES_OBJECT_ALIGNMENT - 1L;
    return size - (size % NUM_BYTES_OBJECT_ALIGNMENT);
  }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `initialCapacity`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/RamUsageEstimator.java`
#### Snippet
```java
    /** Creates a hash set with the given capacity and load factor. */
    public IdentityHashSet(int initialCapacity, float loadFactor) {
      initialCapacity = Math.max(MIN_CAPACITY, initialCapacity);

      assert initialCapacity > 0
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `clazz`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/RamUsageEstimator.java`
#### Snippet
```java

    // Walk type hierarchy
    for (; clazz != null; clazz = clazz.getSuperclass()) {
      final Field[] fields = clazz.getDeclaredFields();
      for (Field f : fields) {
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `current`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/RamUsageEstimator.java`
#### Snippet
```java

      if (current < MIN_CAPACITY / 2) {
        current = MIN_CAPACITY / 2;
      }
      return current << 1;
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `position`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/ReadWriteIOUtils.java`
#### Snippet
```java
    while (buffer.hasRemaining() && (read = input.read(buffer, position)) != NO_BYTE_TO_READ) {
      length += read;
      position += read;
      input.read(buffer, position);
    }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `offset`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/ReadWriteIOUtils.java`
#### Snippet
```java
        && (read = input.read(target, offset)) != NO_BYTE_TO_READ) {
      length += read;
      offset += read;
    }
    target.limit(limit);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `bits`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/decoder/GorillaDecoderV2.java`
#### Snippet
```java
        byte d = (byte) (buffer & ((1 << bitsLeft) - 1));
        value = (value << bitsLeft) + (d & 0xFF);
        bits -= bitsLeft;
        bitsLeft = 0;
      } else {
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `bits`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/decoder/GorillaDecoderV2.java`
#### Snippet
```java
        value = (value << bits) + (d & 0xFF);
        bitsLeft -= bits;
        bits = 0;
      }
      flipByte(in);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `position`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/TsFileSequenceReader.java`
#### Snippet
```java
                  allocateSize, actualReadSize, position));
        }
        position += allocateSize;
      }
    }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `deviceId`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/TsFileSequenceReader.java`
#### Snippet
```java
    try {
      if (type.equals(MetadataIndexNodeType.LEAF_DEVICE)) {
        deviceId = metadataIndex.getName();
      }
      MetadataIndexNode metadataIndexNode = MetadataIndexNode.deserializeFrom(buffer);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `deviceId`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/TsFileSequenceReader.java`
#### Snippet
```java
        // deviceId should be determined by LEAF_DEVICE node
        if (type.equals(MetadataIndexNodeType.LEAF_DEVICE)) {
          deviceId = metadataIndex.getName();
        }
        MetadataIndexNode metadataIndexNode = MetadataIndexNode.deserializeFrom(buffer);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `n`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/encoder/LongZigzagEncoder.java`
#### Snippet
```java
  /** encoding and bit packing */
  private byte[] encodeLong(long n) {
    n = (n << 1) ^ (n >> 63);
    int idx = 0;
    if ((n & ~0x7F) != 0) {
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `n`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/encoder/LongZigzagEncoder.java`
#### Snippet
```java
    if ((n & ~0x7F) != 0) {
      buf[idx++] = (byte) ((n | 0x80) & 0xFF);
      n >>>= 7;
      while (n > 0x7F) {
        buf[idx++] = (byte) ((n | 0x80) & 0xFF);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `n`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/encoder/LongZigzagEncoder.java`
#### Snippet
```java
      while (n > 0x7F) {
        buf[idx++] = (byte) ((n | 0x80) & 0xFF);
        n >>>= 7;
      }
    }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `n`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/encoder/IntZigzagEncoder.java`
#### Snippet
```java
  /** encoding and bit packing */
  private byte[] encodeInt(int n) {
    n = (n << 1) ^ (n >> 31);
    int idx = 0;
    if ((n & ~0x7F) != 0) {
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `n`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/encoder/IntZigzagEncoder.java`
#### Snippet
```java
    if ((n & ~0x7F) != 0) {
      buf[idx++] = (byte) ((n | 0x80) & 0xFF);
      n >>>= 7;
      while (n > 0x7F) {
        buf[idx++] = (byte) ((n | 0x80) & 0xFF);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `n`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/encoder/IntZigzagEncoder.java`
#### Snippet
```java
      while (n > 0x7F) {
        buf[idx++] = (byte) ((n | 0x80) & 0xFF);
        n >>>= 7;
      }
    }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `bits`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/encoder/GorillaEncoderV2.java`
#### Snippet
```java
      if (shift >= 0) {
        buffer |= (byte) ((value >> shift) & ((1 << bitsLeft) - 1));
        bits -= bitsLeft;
        bitsLeft = 0;
      } else {
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `bits`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/encoder/GorillaEncoderV2.java`
#### Snippet
```java
        buffer |= (byte) (value << shift);
        bitsLeft -= bits;
        bits = 0;
      }
      flipByte(out);
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `snr`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/encoder/FreqEncoder.java`
#### Snippet
```java
    this.transformer = new DoubleDCT_1D(blockSize);
    this.dataBuffer = new double[blockSize];
    snr = Math.max(snr, 0);
    this.threshold = Math.pow(10, -snr / 10);
  }
```

### RuleId[ruleID=AssignmentToMethodParameter]
Assignment to method parameter `value`
in `metrics/dropwizard-metrics/src/main/java/org/apache/iotdb/metrics/dropwizard/reporter/IoTDBReporter.java`
#### Snippet
```java
      } else {
        dataTypes.add(TSDataType.TEXT);
        value = value.toString();
      }
      List<Object> values = Collections.singletonList(value);
```

## RuleId[ruleID=PrimitiveArrayArgumentToVariableArgMethod]
### RuleId[ruleID=PrimitiveArrayArgumentToVariableArgMethod]
Confusing primitive array argument to varargs method
in `confignode/src/main/java/org/apache/iotdb/confignode/consensus/request/write/template/CreateSchemaTemplatePlan.java`
#### Snippet
```java
  @Override
  public int hashCode() {
    return Objects.hash(templateData);
  }
}
```

## RuleId[ruleID=SynchronizationOnLocalVariableOrMethodParameter]
### RuleId[ruleID=SynchronizationOnLocalVariableOrMethodParameter]
Synchronization on local variable `memTable`
in `server/src/main/java/org/apache/iotdb/db/wal/node/WALNode.java`
#### Snippet
```java
      try {
        // make sure snapshot is made before memTable flush operation
        synchronized (memTable) {
          if (memTable.getFlushStatus() != FlushStatus.WORKING) {
            return;
```

### RuleId[ruleID=SynchronizationOnLocalVariableOrMethodParameter]
Synchronization on local variable `lock`
in `server/src/main/java/org/apache/iotdb/db/sync/transport/client/SenderManager.java`
#### Snippet
```java
    try {
      Object lock = new Object();
      synchronized (lock) {
        while (!Thread.currentThread().isInterrupted()) {
          try {
```

### RuleId[ruleID=SynchronizationOnLocalVariableOrMethodParameter]
Synchronization on local variable `object`
in `server/src/main/java/org/apache/iotdb/db/sync/transport/client/SenderManager.java`
#### Snippet
```java
        lostConnectionTime = Long.MAX_VALUE;
        logger.info("Reconnect to {} successfully.", pipeSink);
        synchronized (object) {
          object.notify();
        }
```

### RuleId[ruleID=SynchronizationOnLocalVariableOrMethodParameter]
Synchronization on local variable `object`
in `server/src/main/java/org/apache/iotdb/db/sync/transport/client/SenderManager.java`
#### Snippet
```java
        while (!blockingQueue.isEmpty()) {
          object = blockingQueue.take();
          synchronized (object) {
            object.notify();
          }
```

### RuleId[ruleID=SynchronizationOnLocalVariableOrMethodParameter]
Synchronization on method parameter `tsFileProcessor`
in `server/src/main/java/org/apache/iotdb/db/engine/flush/FlushManager.java`
#### Snippet
```java
  @SuppressWarnings("squid:S2445")
  public void registerTsFileProcessor(TsFileProcessor tsFileProcessor) {
    synchronized (tsFileProcessor) {
      if (tsFileProcessor.isManagedByFlushManager()) {
        LOGGER.debug(
```

### RuleId[ruleID=SynchronizationOnLocalVariableOrMethodParameter]
Synchronization on method parameter `targetWriter`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/writer/AbstractCompactionWriter.java`
#### Snippet
```java
      throws IOException {
    writeRateLimit(chunkWriters[subTaskId].estimateMaxSeriesMemSize());
    synchronized (targetWriter) {
      chunkWriters[subTaskId].writeToFileWriter(targetWriter);
    }
```

### RuleId[ruleID=SynchronizationOnLocalVariableOrMethodParameter]
Synchronization on local variable `tmpMemTable`
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileProcessor.java`
#### Snippet
```java
    }

    synchronized (tmpMemTable) {
      try {
        long startWait = System.currentTimeMillis();
```

### RuleId[ruleID=SynchronizationOnLocalVariableOrMethodParameter]
Synchronization on method parameter `memTable`
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileProcessor.java`
#### Snippet
```java
  /** This method will synchronize the memTable and release its flushing resources */
  private void syncReleaseFlushedMemTable(IMemTable memTable) {
    synchronized (memTable) {
      releaseFlushedMemTable(memTable);
      memTable.notifyAll();
```

### RuleId[ruleID=SynchronizationOnLocalVariableOrMethodParameter]
Synchronization on method parameter `parent`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/CachedMTreeStore.java`
#### Snippet
```java

  private IMNode loadChildFromDiskToParent(IMNode parent, IMNode node) {
    synchronized (parent) {
      IMNode nodeAlreadyLoaded = parent.getChild(node.getName());
      if (nodeAlreadyLoaded != null) {
```

### RuleId[ruleID=SynchronizationOnLocalVariableOrMethodParameter]
Synchronization on local variable `cacheEntry`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/cache/CacheManager.java`
#### Snippet
```java
      // give that root.sg.d.s, if sg and d have been persisted and s are volatile, then d
      // will be added to nodeBuffer
      synchronized (cacheEntry) {
        if (!cacheEntry.isVolatile()) {
          nodeBuffer.put(cacheEntry, parent);
```

### RuleId[ruleID=SynchronizationOnLocalVariableOrMethodParameter]
Synchronization on local variable `cacheEntry`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/cache/CacheManager.java`
#### Snippet
```java

    // the operation that changes the node's cache status should be synchronized
    synchronized (cacheEntry) {
      if (getCacheEntry(node) == null) {
        throw new MNodeNotCachedException();
```

### RuleId[ruleID=SynchronizationOnLocalVariableOrMethodParameter]
Synchronization on local variable `cacheEntry`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/cache/CacheManager.java`
#### Snippet
```java
    }

    synchronized (cacheEntry) {
      cacheEntry = getCacheEntry(node);
      if (cacheEntry == null || !cacheEntry.isPinned()) {
```

### RuleId[ruleID=SynchronizationOnLocalVariableOrMethodParameter]
Synchronization on local variable `cacheEntry`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/cache/CacheManager.java`
#### Snippet
```java
      // the cacheEntry may be set to volatile concurrently, the unVolatile node should not be added
      // to nodeBuffer, which prevent the duplicated collecting on subTree
      synchronized (cacheEntry) {
        if (!cacheEntry.isVolatile()) {
          nodeBuffer.put(cacheEntry, parent);
```

### RuleId[ruleID=SynchronizationOnLocalVariableOrMethodParameter]
Synchronization on local variable `cacheEntry`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/cache/CacheManager.java`
#### Snippet
```java

    boolean isPinStatusChanged = false;
    synchronized (cacheEntry) {
      cacheEntry.unPin();
      if (!cacheEntry.isPinned()) {
```

### RuleId[ruleID=SynchronizationOnLocalVariableOrMethodParameter]
Synchronization on local variable `cacheEntry`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/cache/CacheManager.java`
#### Snippet
```java
    CacheEntry cacheEntry = getCacheEntry(node);
    if (!cacheEntry.isVolatile()) {
      synchronized (cacheEntry) {
        // the status change affects the subTre collect in nodeBuffer
        cacheEntry.setVolatile(true);
```

### RuleId[ruleID=SynchronizationOnLocalVariableOrMethodParameter]
Synchronization on local variable `cacheEntry`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/cache/CacheManager.java`
#### Snippet
```java
      cacheEntry = getCacheEntry(node);
      // the operation that may change the cache status of a node should be synchronized
      synchronized (cacheEntry) {
        if (!cacheEntry.isPinned() && isInNodeCache(cacheEntry)) {
          getBelongedContainer(node).evictMNode(node.getName());
```

## RuleId[ruleID=ReturnNull]
### RuleId[ruleID=ReturnNull]
Return of `null`
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/utils/UDFBinaryTransformer.java`
#### Snippet
```java
  public static org.apache.iotdb.udf.api.type.Binary transformToUDFBinary(
      org.apache.iotdb.tsfile.utils.Binary binary) {
    return binary == null ? null : new org.apache.iotdb.udf.api.type.Binary(binary.getValues());
  }
}
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/utils/UDFBinaryTransformer.java`
#### Snippet
```java
  public static org.apache.iotdb.tsfile.utils.Binary transformToBinary(
      org.apache.iotdb.udf.api.type.Binary binary) {
    return binary == null ? null : new org.apache.iotdb.tsfile.utils.Binary(binary.getValues());
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/utils/UDFDataTypeTransformer.java`
#### Snippet
```java

  public static TSDataType transformToTsDataType(Type type) {
    return type == null ? null : TSDataType.getTsDataType(type.getType());
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/utils/UDFDataTypeTransformer.java`
#### Snippet
```java
  public static List<TSDataType> transformToTsDataTypeList(List<Type> typeList) {
    return typeList == null
        ? null
        : typeList.stream()
            .map(UDFDataTypeTransformer::transformToTsDataType)
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/utils/UDFDataTypeTransformer.java`
#### Snippet
```java
  public static List<Type> transformToUDFDataTypeList(List<TSDataType> tsDataTypeList) {
    return tsDataTypeList == null
        ? null
        : tsDataTypeList.stream()
            .map(UDFDataTypeTransformer::transformToUDFDataType)
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/utils/UDFDataTypeTransformer.java`
#### Snippet
```java

  public static Type transformToUDFDataType(TSDataType tsDataType) {
    return tsDataType == null ? null : getUDFDataType(tsDataType.getType());
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/builtin/UDTFAbs.java`
#### Snippet
```java
  public Object transform(Row row) throws IOException {
    if (row.isNull(0)) {
      return null;
    }
    switch (dataType) {
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/builtin/UDTFOnOff.java`
#### Snippet
```java
  public Object transform(Row row) throws IOException {
    if (row.isNull(0)) {
      return null;
    }
    switch (dataType) {
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/builtin/UDTFMath.java`
#### Snippet
```java
  public Object transform(Row row) throws IOException {
    if (row.isNull(0)) {
      return null;
    }
    switch (dataType) {
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/builtin/String/UDTFLower.java`
#### Snippet
```java
  public Object transform(Row row) throws IOException {
    if (row.isNull(0)) {
      return null;
    }
    return Binary.valueOf(row.getString(0).toLowerCase());
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/builtin/String/UDTFEndsWith.java`
#### Snippet
```java
  public Object transform(Row row) throws IOException {
    if (row.isNull(0)) {
      return null;
    }
    return row.getString(0).endsWith(target);
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/builtin/String/UDTFUpper.java`
#### Snippet
```java
  public Object transform(Row row) throws IOException {
    if (row.isNull(0)) {
      return null;
    }
    return Binary.valueOf(row.getString(0).toUpperCase());
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `node-commons/src/main/java/org/apache/iotdb/commons/auth/role/LocalFileRoleAccessor.java`
#### Snippet
```java
        roleProfile = backProfile;
      } else {
        return null;
      }
    }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `node-commons/src/main/java/org/apache/iotdb/commons/auth/user/LocalFileUserAccessor.java`
#### Snippet
```java
        userProfile = newProfile;
      } else {
        return null;
      }
    }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `node-commons/src/main/java/org/apache/iotdb/commons/auth/authorizer/OpenIdAuthorizer.java`
#### Snippet
```java
      }
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `node-commons/src/main/java/org/apache/iotdb/commons/enums/RegionMigrateState.java`
#### Snippet
```java
      }
    }
    return null;
  }
}
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `node-commons/src/main/java/org/apache/iotdb/commons/enums/DataNodeRemoveState.java`
#### Snippet
```java
      }
    }
    return null;
  }
}
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/BasicStructureSerDeUtil.java`
#### Snippet
```java
    int size = readInt(buffer);
    if (size < 0) {
      return null;
    }
    List<String> stringList = new ArrayList<>();
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/BasicStructureSerDeUtil.java`
#### Snippet
```java
    int strLength = readInt(buffer);
    if (strLength < 0) {
      return null;
    } else if (strLength == 0) {
      return "";
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/BasicStructureSerDeUtil.java`
#### Snippet
```java
    int length = readInt(buffer);
    if (length == -1) {
      return null;
    }
    Map<String, List<String>> map = new HashMap<>(length);
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/BasicStructureSerDeUtil.java`
#### Snippet
```java
    int length = readInt(buffer);
    if (length == -1) {
      return null;
    }
    Map<Integer, List<Integer>> map = new HashMap<>(length);
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/BasicStructureSerDeUtil.java`
#### Snippet
```java
    int length = readInt(buffer);
    if (length == -1) {
      return null;
    }
    Map<String, String> map = new HashMap<>(length);
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/IOUtils.java`
#### Snippet
```java
      return new String(strBuffer, 0, length, encoding);
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/SerializeUtils.java`
#### Snippet
```java
  public static Object deserializeObject(ByteBuffer buffer) {
    if (buffer == null || buffer.limit() == 0) {
      return null;
    }
    return ReadWriteIOUtils.readObject(buffer);
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/SerializeUtils.java`
#### Snippet
```java
  public static BatchData deserializeBatchData(ByteBuffer buffer) {
    if (buffer == null || (buffer.limit() - buffer.position()) == 0) {
      return null;
    }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/SerializeUtils.java`
#### Snippet
```java
  public static TimeValuePair deserializeTVPair(ByteBuffer buffer) {
    if (buffer == null || (buffer.limit() - buffer.position() == 0)) {
      return null;
    }
    TSDataType dataType = TSDataType.values()[buffer.get()];
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/SerializeUtils.java`
#### Snippet
```java
        return new TimeValuePair(time, primitiveType);
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `node-commons/src/main/java/org/apache/iotdb/commons/trigger/TriggerTable.java`
#### Snippet
```java
  public TDataNodeLocation getTriggerLocation(String triggerName) {
    TriggerInformation triggerInformation = triggerTable.get(triggerName);
    return triggerInformation == null ? null : triggerInformation.getDataNodeLocation();
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `node-commons/src/main/java/org/apache/iotdb/commons/partition/SchemaPartition.java`
#### Snippet
```java
    }
    // TODO: (xingtanzjr) how to handle this exception in IoTDB
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `node-commons/src/main/java/org/apache/iotdb/commons/partition/DataPartitionTable.java`
#### Snippet
```java
          .getPrecededDataPartition(timePartitionSlot, timePartitionInterval);
    } else {
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `node-commons/src/main/java/org/apache/iotdb/commons/partition/SeriesPartitionTable.java`
#### Snippet
```java
    if (timePartitionSlot.getStartTime() < timePartitionInterval) {
      // The first DataPartition doesn't have predecessor
      return null;
    } else {
      TTimePartitionSlot predecessorSlot =
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `node-commons/src/main/java/org/apache/iotdb/commons/partition/DataPartition.java`
#### Snippet
```java
    }
    // TODO: (xingtanzjr) how to handle this exception in IoTDB
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `session/src/main/java/org/apache/iotdb/session/template/TemplateNode.java`
#### Snippet
```java

  public Map<String, TemplateNode> getChildren() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `session/src/main/java/org/apache/iotdb/session/SessionDataSet.java`
#### Snippet
```java
  public RowRecord next() throws StatementExecutionException, IoTDBConnectionException {
    if (!ioTDBRpcDataSet.hasCachedRecord && !hasNext()) {
      return null;
    }
    ioTDBRpcDataSet.hasCachedRecord = false;
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDataSource.java`
#### Snippet
```java
  @Override
  public <T> T unwrap(Class<T> aClass) {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDataSource.java`
#### Snippet
```java
      e.printStackTrace();
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDataSource.java`
#### Snippet
```java
  @Override
  public java.util.logging.Logger getParentLogger() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDataSource.java`
#### Snippet
```java
  @Override
  public PrintWriter getLogWriter() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDataSource.java`
#### Snippet
```java
      e.printStackTrace();
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBResultMetadata.java`
#### Snippet
```java
      return typeString;
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBResultMetadata.java`
#### Snippet
```java
        break;
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBNonAlignJDBCResultSet.java`
#### Snippet
```java
      int index = ioTDBRpcDataSet.columnOrdinalMap.get(column) - START_INDEX;
      if (times[index] == null || times[index].length == 0) {
        return null;
      }
      return String.valueOf(BytesUtils.bytesToLong(times[index]));
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBNonAlignJDBCResultSet.java`
#### Snippet
```java
        || ioTDBRpcDataSet.values[index] == null
        || ioTDBRpcDataSet.values[index].length < 1) {
      return null;
    }
    return ioTDBRpcDataSet.getString(
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBNonAlignJDBCResultSet.java`
#### Snippet
```java
      int index = ioTDBRpcDataSet.columnOrdinalMap.get(column) - START_INDEX;
      if (times[index] == null || times[index].length == 0) {
        return null;
      }
      return BytesUtils.bytesToLong(times[index]);
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBNonAlignJDBCResultSet.java`
#### Snippet
```java
        || ioTDBRpcDataSet.values[index] == null
        || ioTDBRpcDataSet.values[index].length < 1) {
      return null;
    }
    return ioTDBRpcDataSet.getObject(
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDataSourceFactory.java`
#### Snippet
```java
  @Override
  public XADataSource createXADataSource(Properties properties) {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDataSourceFactory.java`
#### Snippet
```java
  @Override
  public ConnectionPoolDataSource createConnectionPoolDataSource(Properties properties) {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDriver.java`
#### Snippet
```java
  public Connection connect(String url, Properties info) throws SQLException {
    try {
      return acceptsURL(url) ? new IoTDBConnection(url, info) : null;
    } catch (TTransportException e) {
      throw new SQLException(
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `jdbc/src/main/java/org/apache/iotdb/jdbc/StringUtils.java`
#### Snippet
```java
  public static String consistentToString(BigDecimal decimal) {
    if (decimal == null) {
      return null;
    }
    if (toPlainStringMethod != null) {
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
      @Override
      public String getParameterTypeName(int param) {
        return null;
      }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
      @Override
      public <T> T unwrap(Class<T> iface) {
        return null;
      }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
      @Override
      public String getParameterClassName(int param) {
        return null;
      }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `schema-engine-tag/src/main/java/org/apache/iotdb/db/metadata/tagSchemaRegion/tagIndex/wal/WALManager.java`
#### Snippet
```java
      }
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `schema-engine-tag/src/main/java/org/apache/iotdb/db/metadata/tagSchemaRegion/tagIndex/query/MemChunkQuery.java`
#### Snippet
```java
  public List<Object> getChildren(
      MemChunk memNode, QueryRequest queryRequest, QueryRequestContext context) {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `schema-engine-tag/src/main/java/org/apache/iotdb/db/metadata/tagSchemaRegion/tagIndex/deletion/MemChunkDeletion.java`
#### Snippet
```java
  public List<Object> getChildren(
      MemChunk memNode, DeletionRequest request, DeleteRequestContext context) {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `jdbc/src/main/java/org/apache/iotdb/jdbc/AbstractIoTDBJDBCResultSet.java`
#### Snippet
```java
      return new BigDecimal(value);
    } else {
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `schema-engine-tag/src/main/java/org/apache/iotdb/db/metadata/tagSchemaRegion/tagIndex/insertion/MemChunkInsertion.java`
#### Snippet
```java
  public List<Object> getChildren(
      MemChunk memNode, InsertionRequest insertionRequest, InsertRequestContext context) {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
    TsBlock tsBlock = tsBlockBuilder.build();
    if (tsBlock == null) {
      return null;
    } else {
      return serde.serialize(tsBlock);
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBJDBCResultSet.java`
#### Snippet
```java
      return new BigDecimal(value);
    } else {
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `schema-engine-tag/src/main/java/org/apache/iotdb/lsm/request/IQueryRequest.java`
#### Snippet
```java
  @Override
  default Object getValue() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `service-rpc/src/main/java/org/apache/iotdb/rpc/AutoScalingBufferReadTransport.java`
#### Snippet
```java
  public TConfiguration getConfiguration() {
    // should never call this method.
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `service-rpc/src/main/java/org/apache/iotdb/rpc/TSocketWrapper.java`
#### Snippet
```java
    } catch (TTransportException e) {
      // never happen
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `service-rpc/src/main/java/org/apache/iotdb/rpc/TSocketWrapper.java`
#### Snippet
```java
    } catch (TTransportException e) {
      // never happen
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `service-rpc/src/main/java/org/apache/iotdb/rpc/TSocketWrapper.java`
#### Snippet
```java
    } catch (TTransportException e) {
      // never happen
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `service-rpc/src/main/java/org/apache/iotdb/rpc/TSocketWrapper.java`
#### Snippet
```java
    } catch (TTransportException e) {
      // never happen
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `service-rpc/src/main/java/org/apache/iotdb/rpc/AutoScalingBufferWriteTransport.java`
#### Snippet
```java
  public TConfiguration getConfiguration() {
    // should never call this method.
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `service-rpc/src/main/java/org/apache/iotdb/rpc/TNonblockingSocketWrapper.java`
#### Snippet
```java
    } catch (TTransportException e) {
      // never happen
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `service-rpc/src/main/java/org/apache/iotdb/rpc/TNonblockingSocketWrapper.java`
#### Snippet
```java
    } catch (TTransportException e) {
      // never happen
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `service-rpc/src/main/java/org/apache/iotdb/rpc/TNonblockingSocketWrapper.java`
#### Snippet
```java
    } catch (TTransportException e) {
      // never happen
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `schema-engine-tag/src/main/java/org/apache/iotdb/db/metadata/tagSchemaRegion/TagSchemaRegion.java`
#### Snippet
```java
      PartialPath pathPattern, Map<Integer, Template> templateMap, boolean withTags)
      throws MetadataException {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `schema-engine-tag/src/main/java/org/apache/iotdb/db/metadata/tagSchemaRegion/TagSchemaRegion.java`
#### Snippet
```java
  private SchemaEntry getSchemaEntry(String devicePath, String measurementName) {
    DeviceEntry deviceEntry = idTableWithDeviceIDList.getDeviceEntry(devicePath);
    if (deviceEntry == null) return null;
    return deviceEntry.getSchemaEntry(measurementName);
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/qp/logical/sys/ShowPipeOperator.java`
#### Snippet
```java
  public PhysicalPlan generatePhysicalPlan(PhysicalGenerator generator)
      throws QueryProcessException {
    return null;
  }
}
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/qp/utils/GroupByLevelController.java`
#### Snippet
```java
    return columnToAliasMap != null && columnToAliasMap.get(originName) != null
        ? columnToAliasMap.get(originName)
        : null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `service-rpc/src/main/java/org/apache/iotdb/rpc/IoTDBRpcDataSet.java`
#### Snippet
```java
    if (index < 0 || index >= columnTypeDeduplicatedList.size() || isNull(index, tsBlockIndex)) {
      lastReadWasNull = true;
      return null;
    }
    lastReadWasNull = false;
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `service-rpc/src/main/java/org/apache/iotdb/rpc/IoTDBRpcDataSet.java`
#### Snippet
```java
    if (index < 0 || index >= columnTypeDeduplicatedList.size() || isNull(index, tsBlockIndex)) {
      lastReadWasNull = true;
      return null;
    }
    lastReadWasNull = false;
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `service-rpc/src/main/java/org/apache/iotdb/rpc/IoTDBRpcDataSet.java`
#### Snippet
```java
    } else {
      lastReadWasNull = true;
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `service-rpc/src/main/java/org/apache/iotdb/rpc/IoTDBRpcDataSet.java`
#### Snippet
```java
        return curTsBlock.getColumn(index).getBinary(tsBlockIndex).getStringValue();
      default:
        return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `service-rpc/src/main/java/org/apache/iotdb/rpc/IoTDBJDBCDataSet.java`
#### Snippet
```java
        return new String(values[index], StandardCharsets.UTF_8);
      default:
        return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `service-rpc/src/main/java/org/apache/iotdb/rpc/IoTDBJDBCDataSet.java`
#### Snippet
```java
    if (index < 0 || index >= values.length || isNull(index, rowsIndex - 1)) {
      lastReadWasNull = true;
      return null;
    }
    lastReadWasNull = false;
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `service-rpc/src/main/java/org/apache/iotdb/rpc/IoTDBJDBCDataSet.java`
#### Snippet
```java
    if (index < 0 || index >= values.length || isNull(index, rowsIndex - 1)) {
      lastReadWasNull = true;
      return null;
    }
    lastReadWasNull = false;
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `service-rpc/src/main/java/org/apache/iotdb/rpc/IoTDBJDBCDataSet.java`
#### Snippet
```java
        return new String(values[index], StandardCharsets.UTF_8);
      default:
        return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/qp/logical/sys/DropPipeSinkOperator.java`
#### Snippet
```java
  public PhysicalPlan generatePhysicalPlan(PhysicalGenerator generator)
      throws QueryProcessException {
    return null;
  }
}
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/qp/logical/sys/CreatePipeSinkOperator.java`
#### Snippet
```java
  public PhysicalPlan generatePhysicalPlan(PhysicalGenerator generator)
      throws QueryProcessException {
    return null;
  }
}
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/qp/logical/sys/ShowPipeSinkTypeOperator.java`
#### Snippet
```java
  public PhysicalPlan generatePhysicalPlan(PhysicalGenerator generator)
      throws QueryProcessException {
    return null;
  }
}
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/qp/logical/sys/StopPipeOperator.java`
#### Snippet
```java
  public PhysicalPlan generatePhysicalPlan(PhysicalGenerator generator)
      throws QueryProcessException {
    return null;
  }
}
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/qp/logical/sys/DeletePartitionOperator.java`
#### Snippet
```java
  public PhysicalPlan generatePhysicalPlan(PhysicalGenerator generator)
      throws QueryProcessException {
    return null;
  }
}
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/qp/logical/sys/DropPipeOperator.java`
#### Snippet
```java
  public PhysicalPlan generatePhysicalPlan(PhysicalGenerator generator)
      throws QueryProcessException {
    return null;
  }
}
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/qp/logical/sys/CreatePipeOperator.java`
#### Snippet
```java
  public PhysicalPlan generatePhysicalPlan(PhysicalGenerator generator)
      throws QueryProcessException {
    return null;
  }
}
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/qp/logical/sys/ShowPipeSinkOperator.java`
#### Snippet
```java
  public PhysicalPlan generatePhysicalPlan(PhysicalGenerator generator)
      throws QueryProcessException {
    return null;
  }
}
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/qp/logical/sys/StartPipeOperator.java`
#### Snippet
```java
  public PhysicalPlan generatePhysicalPlan(PhysicalGenerator generator)
      throws QueryProcessException {
    return null;
  }
}
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/qp/logical/sys/AuthorOperator.java`
#### Snippet
```java
          return LIST_ROLE_PRIVILEGE;
        default:
          return null;
      }
    }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/qp/logical/crud/DeleteDataOperator.java`
#### Snippet
```java
  public PhysicalPlan generatePhysicalPlan(PhysicalGenerator generator)
      throws QueryProcessException {
    return null;
  }
}
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/qp/logical/crud/InsertOperator.java`
#### Snippet
```java
  public PhysicalPlan generatePhysicalPlan(PhysicalGenerator generator)
      throws QueryProcessException {
    return null;
  }
}
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/qp/logical/crud/BasicFunctionOperator.java`
#### Snippet
```java
    } catch (SQLParserException e) {
      logger.error("error copy:", e);
      return null;
    }
    ret.isLeaf = isLeaf;
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/qp/physical/sys/SetTemplatePlan.java`
#### Snippet
```java
  @Override
  public List<PartialPath> getPaths() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `cli/src/main/java/org/apache/iotdb/tool/ImportCsv.java`
#### Snippet
```java
        case BOOLEAN:
          if (!"true".equalsIgnoreCase(value) && !"false".equalsIgnoreCase(value)) {
            return null;
          }
          return Boolean.parseBoolean(value);
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `cli/src/main/java/org/apache/iotdb/tool/ImportCsv.java`
#### Snippet
```java
          return Double.parseDouble(value);
        default:
          return null;
      }
    } catch (NumberFormatException e) {
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `cli/src/main/java/org/apache/iotdb/tool/ImportCsv.java`
#### Snippet
```java
      }
    } catch (NumberFormatException e) {
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `cli/src/main/java/org/apache/iotdb/tool/ImportCsv.java`
#### Snippet
```java
      }
    } else if ("null".equals(strValue) || "NULL".equals(strValue)) {
      return null;
      // "NaN" is returned if the NaN Literal is given in Parser
    } else if ("NaN".equals(strValue)) {
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `cli/src/main/java/org/apache/iotdb/tool/ImportCsv.java`
#### Snippet
```java
        return DOUBLE;
      default:
        return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/qp/logical/crud/QueryOperator.java`
#### Snippet
```java
  protected IExpression transformFilterOperatorToExpression() throws QueryProcessException {
    if (whereComponent == null) {
      return null;
    }
    FilterOperator filterOperator = whereComponent.getFilterOperator();
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/qp/logical/crud/QueryOperator.java`
#### Snippet
```java
    try {
      return expression == null
          ? null
          : ExpressionOptimizer.getInstance()
              .optimize(expression, new ArrayList<>(queryPlan.getDeduplicatedPaths()));
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/qp/physical/sys/PruneTemplatePlan.java`
#### Snippet
```java
  @Override
  public List<PartialPath> getPaths() {
    return null;
  }
}
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/qp/physical/sys/UnsetTemplatePlan.java`
#### Snippet
```java
  @Override
  public List<PartialPath> getPaths() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/qp/physical/sys/ShowTTLPlan.java`
#### Snippet
```java
  @Override
  public List<PartialPath> getPaths() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/qp/physical/sys/ActivateTemplatePlan.java`
#### Snippet
```java
  @Override
  public List<PartialPath> getPaths() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/qp/physical/sys/AppendTemplatePlan.java`
#### Snippet
```java
  @Override
  public List<PartialPath> getPaths() {
    return null;
  }
}
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/qp/physical/sys/DropTemplatePlan.java`
#### Snippet
```java
  @Override
  public List<PartialPath> getPaths() {
    return null;
  }
}
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/qp/strategy/optimizer/MergeSingleFilterOptimizer.java`
#### Snippet
```java
      filter.setIsSingle(false);
      filter.setChildren(ret);
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/parser/StatementGenerator.java`
#### Snippet
```java
            new PartialPath(SQLConstant.getSingleRootArray()), req.getName());
      default:
        return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/ExpressionUtils.java`
#### Snippet
```java
      }
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/qp/physical/sys/CreateTemplatePlan.java`
#### Snippet
```java
      return Arrays.asList(schemaNames);
    } else {
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/qp/physical/sys/CreateTemplatePlan.java`
#### Snippet
```java
  @Override
  public List<PartialPath> getPaths() {
    return null;
  }
}
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/ExpressionAnalyzer.java`
#### Snippet
```java
      return getDeviceNameInSourceExpression(expression.getExpressions().get(0));
    } else if (expression instanceof ConstantOperand || expression instanceof TimestampOperand) {
      return null;
    } else {
      throw new IllegalArgumentException(
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/Analysis.java`
#### Snippet
```java
    // NULL_Operand needn't check
    if (expression.getExpressionType() == ExpressionType.NULL) {
      return null;
    }
    TSDataType type = expressionTypes.get(NodeRef.of(expression));
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/ExpressionTypeAnalyzer.java`
#### Snippet
```java
    @Override
    public TSDataType visitNullOperand(NullOperand nullOperand, Void context) {
      return null;
    }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/GroupByLevelController.java`
#### Snippet
```java

  public String getAlias(String columnName) {
    return columnToAliasMap.get(columnName) != null ? columnToAliasMap.get(columnName) : null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/cache/PartitionCache.java`
#### Snippet
```java
      if (storageGroupToQueryParamsMap.size() == 0) {
        CacheMetricsRecorder.record(false, DATA_PARTITION_CACHE_NAME);
        return null;
      }
      Map<String, Map<TSeriesPartitionSlot, Map<TTimePartitionSlot, List<TRegionReplicaSet>>>>
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/cache/PartitionCache.java`
#### Snippet
```java
            || !getStorageGroupDataPartition(dataPartitionMap, entry.getKey(), entry.getValue())) {
          CacheMetricsRecorder.record(false, DATA_PARTITION_CACHE_NAME);
          return null;
        }
      }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/cache/PartitionCache.java`
#### Snippet
```java
      }
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/cache/PartitionCache.java`
#### Snippet
```java
      if (storageGroupToDeviceMap.size() == 0) {
        CacheMetricsRecorder.record(false, SCHEMA_PARTITION_CACHE_NAME);
        return null;
      }
      Map<String, Map<TSeriesPartitionSlot, TRegionReplicaSet>> schemaPartitionMap =
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/cache/PartitionCache.java`
#### Snippet
```java
              storageGroupName);
          CacheMetricsRecorder.record(false, SCHEMA_PARTITION_CACHE_NAME);
          return null;
        }
        Map<TSeriesPartitionSlot, TConsensusGroupId> map =
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/cache/PartitionCache.java`
#### Snippet
```java
                "[{} Cache] miss when search device {}", SCHEMA_PARTITION_CACHE_NAME, device);
            CacheMetricsRecorder.record(false, SCHEMA_PARTITION_CACHE_NAME);
            return null;
          }
          TConsensusGroupId consensusGroupId = map.get(seriesPartitionSlot);
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/StandaloneSchemaFetcher.java`
#### Snippet
```java
  @Override
  public Map<Integer, Template> checkAllRelatedTemplate(PartialPath pathPattern) {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/StandaloneSchemaFetcher.java`
#### Snippet
```java
  @Override
  public Pair<Template, PartialPath> checkTemplateSetInfo(PartialPath path) {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/StandaloneSchemaFetcher.java`
#### Snippet
```java
  @Override
  public Pair<Template, List<PartialPath>> getAllPathsSetTemplate(String templateName) {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/PlanFragment.java`
#### Snippet
```java
      }
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/PlanFragment.java`
#### Snippet
```java
      }
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/load/LoadSingleTsFileNode.java`
#### Snippet
```java
  @Override
  public List<PlanNode> getChildren() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/load/LoadSingleTsFileNode.java`
#### Snippet
```java
  @Override
  public List<String> getOutputColumnNames() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/load/LoadSingleTsFileNode.java`
#### Snippet
```java
  @Override
  public TRegionReplicaSet getRegionReplicaSet() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/load/LoadTsFilePieceNode.java`
#### Snippet
```java
    } catch (IOException | PageException | IllegalPathException e) {
      logger.error(String.format("Deserialize %s error.", LoadTsFilePieceNode.class.getName()), e);
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/load/LoadTsFilePieceNode.java`
#### Snippet
```java
  @Override
  public List<String> getOutputColumnNames() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/load/LoadTsFilePieceNode.java`
#### Snippet
```java
  @Override
  public TRegionReplicaSet getRegionReplicaSet() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/load/LoadTsFilePieceNode.java`
#### Snippet
```java
  @Override
  public List<PlanNode> getChildren() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/load/LoadTsFileNode.java`
#### Snippet
```java
  @Override
  public TRegionReplicaSet getRegionReplicaSet() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/load/LoadTsFileNode.java`
#### Snippet
```java
  @Override
  public List<String> getOutputColumnNames() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/load/LoadTsFileNode.java`
#### Snippet
```java
  @Override
  public List<PlanNode> getChildren() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/write/InsertRowsNode.java`
#### Snippet
```java
  @Override
  public List<String> getOutputColumnNames() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/write/InsertRowsNode.java`
#### Snippet
```java
  @Override
  public List<PlanNode> getChildren() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/OperatorTreeGenerator.java`
#### Snippet
```java
            node, context, node.getSeriesPath().getMeasurementPath());
      } else { // otherwise, we just ignore it and return null
        return null;
      }
    } else { //  cached last value is satisfied, put it into LastCacheScanOperator
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/OperatorTreeGenerator.java`
#### Snippet
```java
    } else { //  cached last value is satisfied, put it into LastCacheScanOperator
      context.addCachedLastValue(timeValuePair, seriesPath.getFullPath());
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/OperatorTreeGenerator.java`
#### Snippet
```java
        return createUpdateLastCacheOperator(node, context, node.getSeriesPath());
      } else { // otherwise, we just ignore it and return null
        return null;
      }
    } else { //  cached last value is satisfied, put it into LastCacheScanOperator
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/OperatorTreeGenerator.java`
#### Snippet
```java
    } else { //  cached last value is satisfied, put it into LastCacheScanOperator
      context.addCachedLastValue(timeValuePair, seriesPath.getFullPath());
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/write/InsertMultiTabletsNode.java`
#### Snippet
```java
  @Override
  public List<String> getOutputColumnNames() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/write/InsertMultiTabletsNode.java`
#### Snippet
```java
  @Override
  public List<PlanNode> getChildren() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/write/InsertRowNode.java`
#### Snippet
```java
  @Override
  public List<String> getOutputColumnNames() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/write/InsertRowNode.java`
#### Snippet
```java
  public TimeValuePair composeTimeValuePair(int columnIndex) {
    if (columnIndex >= values.length) {
      return null;
    }
    Object value = values[columnIndex];
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/write/InsertRowNode.java`
#### Snippet
```java
  @Override
  public List<PlanNode> getChildren() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/write/DeleteDataNode.java`
#### Snippet
```java
  @Override
  public List<String> getOutputColumnNames() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/write/InsertRowsOfOneDeviceNode.java`
#### Snippet
```java
  @Override
  public List<String> getOutputColumnNames() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/write/InsertRowsOfOneDeviceNode.java`
#### Snippet
```java
  @Override
  public List<PlanNode> getChildren() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/source/AlignedLastQueryScanNode.java`
#### Snippet
```java
  @Override
  public Filter getPartitionTimeFilter() {
    return null;
  }
}
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/source/LastQueryScanNode.java`
#### Snippet
```java
  @Override
  public Filter getPartitionTimeFilter() {
    return null;
  }
}
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/write/InsertTabletNode.java`
#### Snippet
```java
  public TimeValuePair composeLastTimeValuePair(int measurementIndex) {
    if (measurementIndex >= columns.length) {
      return null;
    }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/write/InsertTabletNode.java`
#### Snippet
```java
    }
    if (lastIdx < 0) {
      return null;
    }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/write/InsertTabletNode.java`
#### Snippet
```java
  @Override
  public List<PlanNode> getChildren() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/write/InsertTabletNode.java`
#### Snippet
```java
  @Override
  public List<String> getOutputColumnNames() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/metedata/write/ConstructSchemaBlackListNode.java`
#### Snippet
```java
  @Override
  public List<String> getOutputColumnNames() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/metedata/write/ConstructSchemaBlackListNode.java`
#### Snippet
```java
  @Override
  public List<PlanNode> getChildren() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/metedata/write/InternalCreateTimeSeriesNode.java`
#### Snippet
```java
  @Override
  public List<String> getOutputColumnNames() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/metedata/write/DeleteTimeSeriesNode.java`
#### Snippet
```java
  @Override
  public List<PlanNode> getChildren() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/metedata/write/DeleteTimeSeriesNode.java`
#### Snippet
```java
  @Override
  public List<String> getOutputColumnNames() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/metedata/write/RollbackSchemaBlackListNode.java`
#### Snippet
```java
  @Override
  public List<PlanNode> getChildren() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/metedata/write/RollbackSchemaBlackListNode.java`
#### Snippet
```java
  @Override
  public List<String> getOutputColumnNames() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/metedata/write/DeactivateTemplateNode.java`
#### Snippet
```java
  @Override
  public List<PlanNode> getChildren() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/metedata/write/DeactivateTemplateNode.java`
#### Snippet
```java
  @Override
  public List<String> getOutputColumnNames() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/metedata/write/ActivateTemplateNode.java`
#### Snippet
```java
  @Override
  public List<String> getOutputColumnNames() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/metedata/write/InvalidateSchemaCacheNode.java`
#### Snippet
```java
  @Override
  public List<PlanNode> getChildren() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/metedata/write/InvalidateSchemaCacheNode.java`
#### Snippet
```java
  @Override
  public List<String> getOutputColumnNames() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/metedata/write/PreDeactivateTemplateNode.java`
#### Snippet
```java
  @Override
  public List<String> getOutputColumnNames() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/metedata/write/PreDeactivateTemplateNode.java`
#### Snippet
```java
  @Override
  public List<PlanNode> getChildren() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/metedata/write/RollbackPreDeactivateTemplateNode.java`
#### Snippet
```java
  @Override
  public List<String> getOutputColumnNames() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/metedata/write/RollbackPreDeactivateTemplateNode.java`
#### Snippet
```java
  @Override
  public List<PlanNode> getChildren() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/metedata/write/CreateMultiTimeSeriesNode.java`
#### Snippet
```java
  @Override
  public List<String> getOutputColumnNames() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/rewriter/MergeSingleFilterOptimizer.java`
#### Snippet
```java
      filter.setIsSingle(false);
      filter.setChildren(ret);
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/metedata/write/CreateAlignedTimeSeriesNode.java`
#### Snippet
```java
  @Override
  public List<String> getOutputColumnNames() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/execution/memory/MemorySourceHandle.java`
#### Snippet
```java
    hasNext = false;
    if (result.isEmpty()) {
      return null;
    } else {
      try {
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/scheduler/StandaloneScheduler.java`
#### Snippet
```java
  @Override
  public Duration getTotalCpuTime() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/scheduler/StandaloneScheduler.java`
#### Snippet
```java
  @Override
  public FragmentInfo getFragmentInfo() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/scheduler/ClusterScheduler.java`
#### Snippet
```java
  @Override
  public FragmentInfo getFragmentInfo() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/scheduler/ClusterScheduler.java`
#### Snippet
```java
  @Override
  public Duration getTotalCpuTime() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/scheduler/load/LoadTsFileScheduler.java`
#### Snippet
```java
  @Override
  public FragmentInfo getFragmentInfo() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/scheduler/load/LoadTsFileScheduler.java`
#### Snippet
```java
  @Override
  public Duration getTotalCpuTime() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/expression/Expression.java`
#### Snippet
```java
    public Expression next() {
      if (!hasNext()) {
        return null;
      }
      Expression current = queue.pop();
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/statement/metadata/template/CreateSchemaTemplateStatement.java`
#### Snippet
```java
  @Override
  public List<? extends PartialPath> getPaths() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/common/filter/BasicFunctionFilter.java`
#### Snippet
```java
    } catch (SQLParserException e) {
      logger.error("error copy:", e);
      return null;
    }
    ret.isLeaf = isLeaf;
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/common/schematree/node/SchemaNode.java`
#### Snippet
```java

  public SchemaNode getChild(String name) {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/common/schematree/DeviceSchemaInfo.java`
#### Snippet
```java
        .map(
            measurementSchemaInfo ->
                measurementSchemaInfo == null ? null : measurementSchemaInfo.getSchema())
        .collect(Collectors.toList());
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/common/schematree/DeviceSchemaInfo.java`
#### Snippet
```java
              measurementInfo -> {
                if (measurementInfo == null) {
                  return null;
                }
                MeasurementPath measurementPath =
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/common/schematree/ClusterSchemaTree.java`
#### Snippet
```java
    for (int i = 1; i < nodes.length; i++) {
      if (cur == null) {
        return null;
      }
      cur = cur.getChild(nodes[i]);
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/common/schematree/ClusterSchemaTree.java`
#### Snippet
```java

    if (cur == null) {
      return null;
    }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/common/schematree/node/SchemaEntityNode.java`
#### Snippet
```java
      return node;
    }
    return aliasChildren == null ? null : aliasChildren.get(name);
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/metedata/write/CreateTimeSeriesNode.java`
#### Snippet
```java
  @Override
  public List<String> getOutputColumnNames() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/LocalSourceHandle.java`
#### Snippet
```java
      }
    } else {
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/MPPDataExchangeServiceThriftHandler.java`
#### Snippet
```java
  public ServerContext createContext(TProtocol tProtocol, TProtocol tProtocol1) {
    thriftConnectionNumber.incrementAndGet();
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/fragment/FragmentInstanceManager.java`
#### Snippet
```java
      return context.getInstanceInfo();
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/fragment/FragmentInstanceManager.java`
#### Snippet
```java
      return context.getInstanceInfo();
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/fragment/FragmentInstanceManager.java`
#### Snippet
```java
    FragmentInstanceContext context = instanceContext.get(instanceId);
    if (context == null) {
      return null;
    }
    return context.getInstanceInfo();
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/SourceHandle.java`
#### Snippet
```java
      return serde.deserialize(tsBlock);
    } else {
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/SourceHandle.java`
#### Snippet
```java
      ByteBuffer tsBlock = sequenceIdToTsBlock.remove(currSequenceId);
      if (tsBlock == null) {
        return null;
      }
      long retainedSize = sequenceIdToDataBlockSize.remove(currSequenceId);
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/fragment/FragmentInstanceFailureInfo.java`
#### Snippet
```java
  public static FragmentInstanceFailureInfo toFragmentInstanceFailureInfo(Throwable throwable) {
    if (throwable == null) {
      return null;
    }
    return new FragmentInstanceFailureInfo(
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/fragment/FragmentInstanceFailureInfo.java`
#### Snippet
```java
  private static FailureException toException(FragmentInstanceFailureInfo failureInfo) {
    if (failureInfo == null) {
      return null;
    }
    FailureException failure =
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/schema/SchemaQueryMergeOperator.java`
#### Snippet
```java
    } else {
      currentIndex++;
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/schema/SchemaQueryOrderByHeatOperator.java`
#### Snippet
```java
      return resultTsBlockList.get(currentIndex - 1);
    } else {
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/schema/CountMergeOperator.java`
#### Snippet
```java
      return resultTsBlockList.get(currentIndex - 1);
    } else {
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/schema/NodePathsConvertOperator.java`
#### Snippet
```java
    TsBlock block = child.next();
    if (block == null || block.isEmpty()) {
      return null;
    }
    TsBlockBuilder tsBlockBuilder = new TsBlockBuilder(outputDataTypes);
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/schema/SchemaFetchMergeOperator.java`
#### Snippet
```java
    } else {
      currentIndex++;
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/schema/NodePathsCountOperator.java`
#### Snippet
```java
      ListenableFuture<?> blocked = child.isBlocked();
      if (!blocked.isDone()) {
        return null;
      }
      if (child.hasNext()) {
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/schema/NodeManageMemoryMergeOperator.java`
#### Snippet
```java
      TsBlock block = child.next();
      if (block == null) {
        return null;
      }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/schema/TimeSeriesSchemaScanOperator.java`
#### Snippet
```java
  private String mapToString(Map<String, String> map) {
    if (map == null || map.isEmpty()) {
      return null;
    }
    String content =
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/source/AbstractSeriesAggregationScanOperator.java`
#### Snippet
```java
      return resultTsBlock;
    } else {
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/source/AlignedSeriesScanUtil.java`
#### Snippet
```java
  protected Statistics currentPageTimeStatistics() throws IOException {
    if (firstPageReader == null) {
      return null;
    }
    return firstPageReader.getTimeStatistics();
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/source/AlignedSeriesScanUtil.java`
#### Snippet
```java
  protected Statistics currentPageStatistics(int index) throws IOException {
    if (firstPageReader == null) {
      return null;
    }
    return firstPageReader.getStatistics(index);
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/OffsetOperator.java`
#### Snippet
```java
    TsBlock block = child.next();
    if (block == null) {
      return null;
    }
    if (remainingOffset > 0) {
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/FillOperator.java`
#### Snippet
```java
    TsBlock block = child.next();
    if (block == null) {
      return null;
    }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/SortOperator.java`
#### Snippet
```java
  @Override
  public TsBlock next() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/SortOperator.java`
#### Snippet
```java
  @Override
  public OperatorContext getOperatorContext() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/LimitOperator.java`
#### Snippet
```java
    TsBlock block = child.next();
    if (block == null) {
      return null;
    }
    TsBlock res = block;
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/metedata/write/AlterTimeSeriesNode.java`
#### Snippet
```java
  @Override
  public List<String> getOutputColumnNames() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/metedata/write/AlterTimeSeriesNode.java`
#### Snippet
```java
  @Override
  public List<PlanNode> getChildren() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/DeviceViewOperator.java`
#### Snippet
```java
    if (!getCurDeviceOperator().hasNext()) {
      deviceIndex++;
      return null;
    }
    TsBlock tsBlock = getCurDeviceOperator().next();
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/DeviceViewOperator.java`
#### Snippet
```java
    TsBlock tsBlock = getCurDeviceOperator().next();
    if (tsBlock == null) {
      return null;
    }
    List<Integer> indexes = getCurDeviceIndexes();
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/LinearFillOperator.java`
#### Snippet
```java
              // there is still more TsBlock, so current calculation is not finished, and we just
              // return null
              return null;
            }
          }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/DeviceMergeOperator.java`
#### Snippet
```java
  private String getDeviceNameFromTsBlock(TsBlock tsBlock) {
    if (tsBlock == null || tsBlock.getPositionCount() == 0 || tsBlock.getColumn(0).isNull(0)) {
      return null;
    }
    return tsBlock.getColumn(0).getBinary(0).toString();
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/DeviceMergeOperator.java`
#### Snippet
```java
        inputTsBlocks[i] = deviceOperators.get(i).next();
        if (inputTsBlocks[i] == null || inputTsBlocks[i].isEmpty()) {
          return null;
        }
        deviceOfInputTsBlocks[i] = getDeviceNameFromTsBlock(inputTsBlocks[i]);
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/TransformOperator.java`
#### Snippet
```java
      YieldableState yieldableState = iterateAllColumnsToNextValid();
      if (yieldableState == YieldableState.NOT_YIELDABLE_WAITING_FOR_DATA) {
        return null;
      }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/SingleInputAggregationOperator.java`
#### Snippet
```java
      return resultTsBlock;
    } else {
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/FilterAndProjectOperator.java`
#### Snippet
```java
    TsBlock input = inputOperator.next();
    if (input == null) {
      return null;
    }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/AggregationOperator.java`
#### Snippet
```java
      return resultTsBlock;
    } else {
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/IntoOperator.java`
#### Snippet
```java

    if (child.hasNext()) {
      return null;
    } else {
      insertMultiTabletsInternally(false);
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/last/UpdateLastCacheOperator.java`
#### Snippet
```java
    TsBlock res = child.next();
    if (res == null) {
      return null;
    }
    if (res.isEmpty()) {
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/last/LastQueryCollectOperator.java`
#### Snippet
```java
    } else {
      currentIndex++;
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/DeviceViewIntoOperator.java`
#### Snippet
```java

    if (child.hasNext()) {
      return null;
    } else {
      insertMultiTabletsInternally(false);
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/last/LastQueryOperator.java`
#### Snippet
```java
        TsBlock tsBlock = children.get(currentIndex).next();
        if (tsBlock == null) {
          return null;
        } else if (!tsBlock.isEmpty()) {
          LastQueryUtil.appendLastValue(tsBlockBuilder, tsBlock, 0);
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/last/LastQuerySortOperator.java`
#### Snippet
```java
          TsBlock tsBlock = children.get(currentIndex).next();
          if (tsBlock == null) {
            return null;
          } else if (!tsBlock.isEmpty()) {
            if (canUseDataFromCachedTsBlock(tsBlock)) {
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/source/SeriesScanUtil.java`
#### Snippet
```java
  Statistics currentPageStatistics() {
    if (firstPageReader == null) {
      return null;
    }
    return firstPageReader.getStatistics();
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/join/RowBasedTimeJoinOperator.java`
#### Snippet
```java
            // the data that is not empty, but this will cause the execution time of the while loop
            // to be uncontrollable and may exceed all allocated time slice
            return null;
          }
        } else { // no more tsBlock
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/join/TimeJoinOperator.java`
#### Snippet
```java
            // the data that is not empty, but this will cause the execution time of the while loop
            // to be uncontrollable and may exceed all allocated time slice
            return null;
          }
        } else { // no more tsBlock
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/schedule/queue/IndexedBlockingQueue.java`
#### Snippet
```java
    E output = remove(queryHolder);
    if (output == null) {
      return null;
    }
    size--;
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/schedule/task/DriverTask.java`
#### Snippet
```java
    @Override
    public ISinkHandle getSinkHandle() {
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/schedule/task/DriverTask.java`
#### Snippet
```java
    @Override
    public ListenableFuture<?> processFor(Duration duration) {
      return null;
    }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/last/LastQueryMergeOperator.java`
#### Snippet
```java
            // the data that is not empty, but this will cause the execution time of the while loop
            // to be uncontrollable and may exceed all allocated time slice
            return null;
          }
        } else { // no more tsBlock
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/aggregation/timerangeiterator/SingleTimeWindowIterator.java`
#### Snippet
```java
      return curTimeRange;
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/aggregation/timerangeiterator/AggrWindowIterator.java`
#### Snippet
```java
      return getFinalTimeRange(curTimeRange, leftCRightO);
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/aggregation/timerangeiterator/PreAggrWindowIterator.java`
#### Snippet
```java
      return getFinalTimeRange(curTimeRange, leftCRightO);
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/mpp/aggregation/timerangeiterator/PreAggrWindowWithNaturalMonthIterator.java`
#### Snippet
```java
      return getFinalTimeRange(curTimeRange, leftCRightO);
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/wal/utils/WALFileStatus.java`
#### Snippet
```java
      }
    }
    return null;
  }
}
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/wal/recover/WALRecoverManager.java`
#### Snippet
```java
    if (hasStarted) {
      logger.error("Cannot recover tsfile from wal because wal recovery has already started");
      return null;
    } else {
      try {
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/wal/recover/WALRecoverManager.java`
#### Snippet
```java
      logger.error("Fail to remove recover performer for file {}", file, e);
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/wal/checkpoint/CheckpointType.java`
#### Snippet
```java
      }
    }
    return null;
  }
}
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/wal/checkpoint/CheckpointManager.java`
#### Snippet
```java
    }
    if (memTableInfos.isEmpty()) {
      return null;
    }
    MemTableInfo oldestMemTableInfo = memTableInfos.get(0);
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/conf/rest/IoTDBRestServiceDescriptor.java`
#### Snippet
```java
            IoTDBRestServiceConfig.CONFIG_NAME);
        // update all data seriesPath
        return null;
      }
    }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/conf/rest/IoTDBRestServiceDescriptor.java`
#### Snippet
```java
      return new URL(urlString);
    } catch (MalformedURLException e) {
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/sync/common/ClusterSyncInfoFetcher.java`
#### Snippet
```java
  @Override
  public TSStatus recordMsg(String pipeName, PipeMessage message) {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/sync/SyncService.java`
#### Snippet
```java
  @TestOnly
  public SenderManager getSenderManager() {
    return null;
  }
}
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/sync/sender/pipe/TsFilePipe.java`
#### Snippet
```java
    try {
      if (pipeLog.isHardlinkExist(tsFile)) {
        return null;
      }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/sync/sender/pipe/TsFilePipe.java`
#### Snippet
```java
    } catch (IOException e) {
      logger.error(logFormat("Create hardlink for history tsfile %s error.", tsFile.getPath()), e);
      return null;
    } finally {
      collectRealTimeDataLock.unlock();
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/sync/pipedata/queue/BufferedPipeDataQueue.java`
#### Snippet
```java
    } else if (outputDeque != inputDeque) {
      if (pipeLogStartNumber.isEmpty() || lastSerialNumber == Long.MIN_VALUE) {
        return null;
      }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/sync/pipedata/queue/BufferedPipeDataQueue.java`
#### Snippet
```java

      if (serialNumber > pipeLogStartNumber.peekLast()) {
        return null;
      } else if (serialNumber == pipeLogStartNumber.peekLast() && inputDeque != null) {
        outputDeque = inputDeque;
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/sync/pipedata/queue/BufferedPipeDataQueue.java`
#### Snippet
```java
      return outputDeque.poll();
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/conf/IoTDBDescriptor.java`
#### Snippet
```java
        // update all data seriesPath
        conf.updatePath();
        return null;
      }
    }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/conf/IoTDBDescriptor.java`
#### Snippet
```java
      return new URL(urlString);
    } catch (MalformedURLException e) {
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/PipeOpSgManager.java`
#### Snippet
```java
  public synchronized Operation getOperation(long beginIndex, long length) throws IOException {
    if (beginIndex >= (this.beginIndex + dataCount)) {
      return null;
    }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/DeletionOpBlock.java`
#### Snippet
```java

    if (index != beginIndex) {
      return null;
    }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/PipeOpManager.java`
#### Snippet
```java
    if (pipeOpSgManager == null) {
      logger.error("getOperation(), invalid sgName={}. continue.", sgName);
      return null;
    }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/sync/externalpipe/ExtPipePluginConfiguration.java`
#### Snippet
```java
      logger.error(
          "getBucketSgInfoMap(), error. pipeName = {}, threadIndex = {}.", pipeName, threadIndex);
      return null;
    }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/sync/transport/server/ReceiverManager.java`
#### Snippet
```java
      return connectionIdToIdentityInfoMap.get(id);
    } else {
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/conf/IoTDBConfig.java`
#### Snippet
```java
      return m.group(1);
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/sync/externalpipe/ExtPipePluginManager.java`
#### Snippet
```java
    ExtPipePlugin extPipePlugin = extPipePluginMap.get(extPipeTypeName);
    if (extPipePlugin == null) {
      return null;
    }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/query/reader/chunk/ChunkReaderWrap.java`
#### Snippet
```java
      return chunkMetaData.getMeasurementUid();
    } else {
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/TsFileOpBlock.java`
#### Snippet
```java
          @Override
          public List<TimeValuePair> loadObjectByKey(Long key) throws IOException {
            return null;
          }
        };
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/TsFileOpBlock.java`
#### Snippet
```java
      logger.error("TsFileOpBlock.getOperation(), Error: index {} is out of range.", index);
      // throw new IOException("index is out of range.");
      return null;
    }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/TsFileOpBlock.java`
#### Snippet
```java
    // (fullPathToDeletionMap == null) means modificationList is null or empty
    if (fullPathToDeletionMap == null) {
      return null;
    }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/sync/externalpipe/ExtPipePlugin.java`
#### Snippet
```java
      }

      return null;
    }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `session/src/main/java/org/apache/iotdb/session/pool/SessionPool.java`
#### Snippet
```java
      }
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `session/src/main/java/org/apache/iotdb/session/pool/SessionPool.java`
#### Snippet
```java
      }
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `session/src/main/java/org/apache/iotdb/session/pool/SessionPool.java`
#### Snippet
```java
      }
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `session/src/main/java/org/apache/iotdb/session/pool/SessionPool.java`
#### Snippet
```java
    }
    // never go here
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `session/src/main/java/org/apache/iotdb/session/pool/SessionPool.java`
#### Snippet
```java
      }
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `session/src/main/java/org/apache/iotdb/session/pool/SessionPool.java`
#### Snippet
```java
      }
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `session/src/main/java/org/apache/iotdb/session/pool/SessionPool.java`
#### Snippet
```java
      }
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `session/src/main/java/org/apache/iotdb/session/pool/SessionPool.java`
#### Snippet
```java
    }
    // never go here
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `session/src/main/java/org/apache/iotdb/session/pool/SessionPool.java`
#### Snippet
```java
    }
    // never go here
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `session/src/main/java/org/apache/iotdb/session/pool/SessionPool.java`
#### Snippet
```java
    }
    // never go here
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `session/src/main/java/org/apache/iotdb/session/pool/SessionPool.java`
#### Snippet
```java
    }
    // never go here
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/query/control/SessionManager.java`
#### Snippet
```java
          e, OperationType.CHECK_AUTHORITY.getName(), TSStatusCode.EXECUTE_STATEMENT_ERROR);
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/query/reader/series/SeriesReaderByTimestamp.java`
#### Snippet
```java
  public Object[] getValuesInTimestamps(long[] timestamps, int length) throws IOException {
    if (length <= 0) {
      return null;
    }
    Object[] results = new Object[length];
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/query/reader/series/SeriesReader.java`
#### Snippet
```java
  Statistics currentPageTimeStatistics() throws IOException {
    if (firstPageReader == null) {
      return null;
    }
    if (!(firstPageReader.isAlignedPageReader())) {
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/query/reader/series/SeriesReader.java`
#### Snippet
```java
  Statistics currentPageStatistics(int index) throws IOException {
    if (firstPageReader == null) {
      return null;
    }
    if (!(firstPageReader.isAlignedPageReader())) {
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/query/reader/series/SeriesReader.java`
#### Snippet
```java
  Statistics currentPageStatistics() {
    if (firstPageReader == null) {
      return null;
    }
    return firstPageReader.getStatistics();
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/query/dataset/NonAlignEngineDataSet.java`
#### Snippet
```java
  @Override
  public RowRecord nextWithoutConstraint() {
    return null;
  }
}
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/query/dataset/RawQueryDataSetWithValueFilter.java`
#### Snippet
```java
  public RowRecord nextWithoutConstraint() throws IOException {
    if (cachedRowRecords.isEmpty() && !cacheRowRecords()) {
      return null;
    }
    return cachedRowRecords.remove(cachedRowRecords.size() - 1);
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/query/executor/fill/ValueFill.java`
#### Snippet
```java
          return new TimeValuePair(queryStartTime, new TsPrimitiveType.TsBoolean(false));
        } else {
          return null;
        }
      case INT32:
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/query/executor/fill/ValueFill.java`
#### Snippet
```java
      }
    } else {
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/query/aggregation/impl/SumAggrResult.java`
#### Snippet
```java
  @Override
  public Double getResult() {
    return hasCandidateResult() ? getDoubleValue() : null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/query/aggregation/impl/ExtremeAggrResult.java`
#### Snippet
```java
  @Override
  public Object getResult() {
    return hasCandidateResult() ? getValue() : null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/query/executor/groupby/impl/LocalGroupByExecutor.java`
#### Snippet
```java
        List<AggregateResult> aggregateResults = calcResult(nextStartTime, nextEndTime);
        if (aggregateResults == null || aggregateResults.get(0).getResult() == null) {
          return null;
        }
        // restore context
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/query/executor/groupby/impl/LocalGroupByExecutor.java`
#### Snippet
```java
        List<AggregateResult> aggregateResults = calcResult(nextStartTime, nextEndTime);
        if (aggregateResults == null || aggregateResults.get(0).getResult() == null) {
          return null;
        }
        // restore context
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/query/aggregation/impl/AvgAggrResult.java`
#### Snippet
```java
      setDoubleValue(avg);
    }
    return hasCandidateResult() ? getDoubleValue() : null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/query/aggregation/impl/MaxTimeAggrResult.java`
#### Snippet
```java
  @Override
  public Long getResult() {
    return hasCandidateResult() ? getLongValue() : null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/query/aggregation/impl/MaxValueAggrResult.java`
#### Snippet
```java
  @Override
  public Object getResult() {
    return hasCandidateResult() ? getValue() : null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/query/aggregation/impl/MinTimeAggrResult.java`
#### Snippet
```java
  @Override
  public Long getResult() {
    return hasCandidateResult() ? getLongValue() : null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/query/aggregation/impl/FirstValueAggrResult.java`
#### Snippet
```java
  @Override
  public Object getResult() {
    return hasCandidateResult() ? getValue() : null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/query/aggregation/impl/LastValueAggrResult.java`
#### Snippet
```java
  @Override
  public Object getResult() {
    return hasCandidateResult() ? getValue() : null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/query/aggregation/impl/MinValueAggrResult.java`
#### Snippet
```java
  @Override
  public Object getResult() {
    return hasCandidateResult() ? getValue() : null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/query/executor/RawDataQueryExecutor.java`
#### Snippet
```java
  protected QueryDataSet needRedirect(QueryContext context, boolean hasValueFilter)
      throws StorageEngineException, QueryProcessException {
    return null;
  }
}
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/query/timegenerator/ServerTimeGenerator.java`
#### Snippet
```java
          return FilterFactory.or(leftTimeFilter, rightTimeFilter);
        } else {
          return null;
        }
      }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/query/timegenerator/ServerTimeGenerator.java`
#### Snippet
```java
      }
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/tools/settle/TsFileAndModSettleTool.java`
#### Snippet
```java
    List<File> files = new ArrayList<>();
    if (args.length == 0) {
      return null;
    } else {
      for (String arg : args) {
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/query/executor/LastQueryExecutor.java`
#### Snippet
```java

    public TimeValuePair read() {
      return null;
    }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/query/executor/LastQueryExecutor.java`
#### Snippet
```java
    @Override
    public TimeValuePair read() {
      return null;
    }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/utils/QueryUtils.java`
#### Snippet
```java
  public static ValueIterator generateValueIterator(Object[] values) {
    if (values == null) {
      return null;
    }
    // find the first element that is not NULL
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/utils/QueryUtils.java`
#### Snippet
```java
    if (index == values.length) {
      // all elements are NULL
      return null;
    }
    if (values[index] instanceof TsPrimitiveType[]) {
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/utils/IExpressionDeserializeUtil.java`
#### Snippet
```java
        return GlobalTimeExpression.deserialize(byteBuffer);
      case TRUE:
        return null;
      default:
        throw new UnsupportedOperationException("unSupport expressionType");
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileSplitByPartitionTool.java`
#### Snippet
```java
      return chunkMetadata.getDeleteIntervalList();
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/utils/ErrorHandlingUtils.java`
#### Snippet
```java
      return RpcUtils.getStatus(((IoTDBException) e).getErrorCode(), rootCause.getMessage());
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/utils/ErrorHandlingUtils.java`
#### Snippet
```java
    }

    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/utils/CommonUtils.java`
#### Snippet
```java
    try {
      if ("null".equals(value) || "NULL".equals(value)) {
        return null;
      }
      switch (dataType) {
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/utils/FileLoaderUtils.java`
#### Snippet
```java
      if (alignedTimeSeriesMetadata.getTimeseriesMetadata().getStatistics().getStartTime()
          > alignedTimeSeriesMetadata.getTimeseriesMetadata().getStatistics().getEndTime()) {
        return null;
      }
      if (filter != null
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/utils/FileLoaderUtils.java`
#### Snippet
```java
              alignedTimeSeriesMetadata.getTimeseriesMetadata().getStatistics().getStartTime(),
              alignedTimeSeriesMetadata.getTimeseriesMetadata().getStatistics().getEndTime())) {
        return null;
      }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/utils/FileLoaderUtils.java`
#### Snippet
```java
      if (timeSeriesMetadata.getStatistics().getStartTime()
          > timeSeriesMetadata.getStatistics().getEndTime()) {
        return null;
      }
      if (filter != null
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/utils/FileLoaderUtils.java`
#### Snippet
```java
              timeSeriesMetadata.getStatistics().getStartTime(),
              timeSeriesMetadata.getStatistics().getEndTime())) {
        return null;
      }
    }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/utils/AlignedValueIterator.java`
#### Snippet
```java
  public Object get(int index) {
    if (values[index] == null || ((TsPrimitiveType[]) values[index])[subMeasurementIndex] == null) {
      return null;
    }
    return ((TsPrimitiveType[]) values[index])[subMeasurementIndex].getValue();
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/utils/TypeInferenceUtils.java`
#### Snippet
```java
        }
      } else if ("null".equals(strValue) || "NULL".equals(strValue)) {
        return null;
        // "NaN" is returned if the NaN Literal is given in Parser
      } else if ("NaN".equals(strValue)) {
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/utils/SchemaUtils.java`
#### Snippet
```java
      currEx = currEx.getCause();
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/utils/SchemaUtils.java`
#### Snippet
```java
  public static TSDataType getAggregationType(String aggregation) {
    if (aggregation == null) {
      return null;
    }
    switch (aggregation.toLowerCase()) {
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/utils/SchemaUtils.java`
#### Snippet
```java
      case SQLConstant.MAX_VALUE:
      default:
        return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/utils/QueryDataSetUtils.java`
#### Snippet
```java
  public static BitMap[] readBitMapsFromBuffer(ByteBuffer buffer, int columns, int size) {
    if (!buffer.hasRemaining()) {
      return null;
    }
    BitMap[] bitMaps = new BitMap[columns];
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/utils/QueryDataSetUtils.java`
#### Snippet
```java
      throws IOException {
    if (stream.available() <= 0) {
      return null;
    }
    BitMap[] bitMaps = new BitMap[columns];
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/TVList.java`
#### Snippet
```java
        break;
    }
    return null;
  }
}
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/TVList.java`
#### Snippet
```java
        break;
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/utils/timerangeiterator/PreAggrWindowWithNaturalMonthIterator.java`
#### Snippet
```java
      return curTimeRange;
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/utils/timerangeiterator/AggrWindowIterator.java`
#### Snippet
```java
      return curTimeRange;
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/utils/timerangeiterator/PreAggrWindowIterator.java`
#### Snippet
```java
      return curTimeRange;
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/engine/StorageEngine.java`
#### Snippet
```java

  public String getStorageGroupPath(PartialPath selectedPath) {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/engine/StorageEngine.java`
#### Snippet
```java
  private DataRegion getStorageGroupProcessorByPath(
      PartialPath devicePath, IStorageGroupMNode storageGroupMNode) throws DataRegionException {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/engine/cache/TimeSeriesMetadataCache.java`
#### Snippet
```java
      if (bloomFilter != null
          && !bloomFilter.contains(key.device + IoTDBConstant.PATH_SEPARATOR + key.measurement)) {
        return null;
      }
      TimeseriesMetadata timeseriesMetadata =
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/engine/cache/TimeSeriesMetadataCache.java`
#### Snippet
```java
              new Path(key.device, key.measurement, true), ignoreNotExists);
      return (timeseriesMetadata == null || timeseriesMetadata.getStatistics().getCount() == 0)
          ? null
          : timeseriesMetadata;
    }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/engine/cache/TimeSeriesMetadataCache.java`
#### Snippet
```java
                DEBUG_LOGGER.info("TimeSeries meta data {} is filter by bloomFilter!", key);
              }
              return null;
            }
          }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/engine/cache/TimeSeriesMetadataCache.java`
#### Snippet
```java
        DEBUG_LOGGER.info("The file doesn't have this time series {}.", key);
      }
      return null;
    } else {
      if (debug) {
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/engine/flush/NotifyFlushMemTable.java`
#### Snippet
```java
  @Override
  public IMemTable copy() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/AlignedTVList.java`
#### Snippet
```java
        return cloneB;
      default:
        return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/engine/memtable/IWritableMemChunk.java`
#### Snippet
```java

  default TVList getTVList() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/engine/trigger/sink/alertmanager/AlertManagerEvent.java`
#### Snippet
```java
  private static String fillTemplate(Map<String, String> map, String template) {
    if (template == null || map == null) {
      return null;
    }
    StringBuffer sb = new StringBuffer();
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/log/TsFileIdentifier.java`
#### Snippet
```java
      }
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/engine/snapshot/SnapshotLoader.java`
#### Snippet
```java
    } catch (Exception e) {
      LOGGER.error("Exception occurs while load snapshot from {}", snapshotPath, e);
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/engine/snapshot/SnapshotLoader.java`
#### Snippet
```java
      }
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/engine/snapshot/SnapshotLoader.java`
#### Snippet
```java
      } catch (IOException e) {
        LOGGER.error("Failed to remove origin data files", e);
        return null;
      }
      LOGGER.info("Moving snapshot file to data dirs");
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/engine/snapshot/SnapshotLoader.java`
#### Snippet
```java
        LOGGER.info("Remove all data files in original data dir");
      } catch (IOException e) {
        return null;
      }
      createLinksFromSnapshotDirToDataDirWithoutLog(new File(snapshotPath));
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/engine/snapshot/SnapshotLoader.java`
#### Snippet
```java
      LOGGER.error(
          "Exception occurs when loading snapshot for {}-{}", storageGroupName, dataRegionId, e);
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/engine/snapshot/SnapshotLoader.java`
#### Snippet
```java
    } catch (Exception e) {
      LOGGER.error("Exception occurs when reading snapshot file", e);
      return null;
    }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/engine/snapshot/SnapshotLoader.java`
#### Snippet
```java
      // Do not load this snapshot because it's not complete.
      LOGGER.error("This snapshot is not complete, cannot load it");
      return null;
    }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/engine/snapshot/SnapshotLoader.java`
#### Snippet
```java
      } catch (IOException e) {
        LOGGER.error("Failed to remove origin data files", e);
        return null;
      }
    } finally {
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/CompactionTaskManager.java`
#### Snippet
```java
      return subCompactionTaskExecutionPool.submit(subCompactionTask);
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/engine/memtable/AlignedWritableMemChunk.java`
#### Snippet
```java
  @Override
  public IMeasurementSchema getSchema() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/task/CompactionRecoverTask.java`
#### Snippet
```java
      }
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/engine/modification/ModificationFile.java`
#### Snippet
```java
  public ModificationFile createHardlink() {
    if (!exists()) {
      return null;
    }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/engine/modification/ModificationFile.java`
#### Snippet
```java
      } catch (IOException e) {
        logger.error("Cannot create hardlink for {}", filePath, e);
        return null;
      }
    }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/engine/querycontext/QueryDataSource.java`
#### Snippet
```java
      return seqResources.get(curIndex);
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/engine/querycontext/QueryDataSource.java`
#### Snippet
```java
      return unseqResources.get(actualIndex);
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/engine/modification/io/LocalTextModificationAccessor.java`
#### Snippet
```java
      return encodeDeletion((Deletion) mod);
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/engine/modification/utils/TracedBufferedReader.java`
#### Snippet
```java
            return s.toString();
          } else {
            return null;
          }
        }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/inner/utils/MultiTsFileDeviceIterator.java`
#### Snippet
```java
      getReaderAndChunkMetadataForCurrentAlignedSeries() throws IOException {
    if (currentDevice == null || !currentDevice.right) {
      return null;
    }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/inner/utils/MultiTsFileDeviceIterator.java`
#### Snippet
```java
    public String nextSeries() {
      if (!hasNextSeries()) {
        return null;
      } else {
        currentCompactingSeries = seriesInThisIteration.removeFirst();
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/inner/utils/MultiTsFileDeviceIterator.java`
#### Snippet
```java
        getMetadataListForCurrentSeries() throws IllegalPathException {
      if (currentCompactingSeries == null) {
        return null;
      }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TimePartitionManager.java`
#### Snippet
```java
          timePartitionInfoMap.get(dataRegionId);
      if (timePartitionInfoMapForDataRegion == null) {
        return null;
      }
      return timePartitionInfoMapForDataRegion.get(timePartitionId);
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/timeindex/DeviceTimeIndex.java`
#### Snippet
```java
    Integer index = deviceToIndex.get(deviceId);
    if (index == null) {
      return null;
    } else {
      int i = index;
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/timeindex/DeviceTimeIndex.java`
#### Snippet
```java
    }

    return hasMatchedDevice ? new Pair<>(startTime, endTime) : null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/service/thrift/handler/InternalServiceThriftHandler.java`
#### Snippet
```java
  public ServerContext createContext(TProtocol tProtocol, TProtocol tProtocol1) {
    thriftConnectionNumber.incrementAndGet();
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileResource.java`
#### Snippet
```java
      return pathToTimeSeriesMetadataMap.get(seriesPath);
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileResource.java`
#### Snippet
```java
  public TsFileResource createHardlink() {
    if (!file.exists()) {
      return null;
    }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileResource.java`
#### Snippet
```java
    } catch (IOException e) {
      LOGGER.error("Cannot create hardlink for {}", file, e);
      return null;
    }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileResource.java`
#### Snippet
```java
      } catch (IOException e) {
        LOGGER.error("Cannot create hardlink for {}", file, e);
        return null;
      }
    }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/trigger/service/TriggerManagementService.java`
#### Snippet
```java
      return triggerInformation.getDataNodeLocation();
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/service/thrift/impl/TSServiceImpl.java`
#### Snippet
```java
  @Override
  public TSExecuteStatementResp executeLastDataQueryV2(TSLastDataQueryReq req) throws TException {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/service/thrift/impl/TSServiceImpl.java`
#### Snippet
```java

  public TSStatus insertTabletsInternally(TSInsertTabletsReq req) throws MetadataException {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/service/thrift/impl/TSServiceImpl.java`
#### Snippet
```java
  @Override
  public TSStatus insertRecord(TSInsertRecordReq req) {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/service/thrift/impl/TSServiceImpl.java`
#### Snippet
```java
  @Override
  public TSFetchResultsResp fetchResultsV2(TSFetchResultsReq req) throws TException {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/service/thrift/impl/TSServiceImpl.java`
#### Snippet
```java
  @Override
  public TSExecuteStatementResp executeStatementV2(TSExecuteStatementReq req) throws TException {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/service/thrift/impl/TSServiceImpl.java`
#### Snippet
```java
  @Override
  public TSStatus insertStringRecordsOfOneDevice(TSInsertStringRecordsOfOneDeviceReq req) {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/service/thrift/impl/TSServiceImpl.java`
#### Snippet
```java
  @Override
  public TSExecuteStatementResp executeRawDataQueryV2(TSRawDataQueryReq req) throws TException {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/service/thrift/impl/TSServiceImpl.java`
#### Snippet
```java
  public TSExecuteStatementResp executeUpdateStatementV2(TSExecuteStatementReq req)
      throws TException {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/service/thrift/impl/TSServiceImpl.java`
#### Snippet
```java
    @Override
    public TSExecuteStatementResp call() throws Exception {
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/service/thrift/impl/TSServiceImpl.java`
#### Snippet
```java
  @Override
  public TSStatus executeBatchStatement(TSExecuteBatchStatementReq req) {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/service/thrift/impl/TSServiceImpl.java`
#### Snippet
```java
  @Override
  public TSQueryTemplateResp querySchemaTemplate(TSQueryTemplateReq req) {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/service/thrift/impl/TSServiceImpl.java`
#### Snippet
```java
  @Override
  public TSStatus insertRecords(TSInsertRecordsReq req) {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/service/thrift/impl/TSServiceImpl.java`
#### Snippet
```java
  @Override
  public TSStatus insertRecordsOfOneDevice(TSInsertRecordsOfOneDeviceReq req) {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/service/thrift/impl/TSServiceImpl.java`
#### Snippet
```java
    @Override
    public TSFetchResultsResp call() throws Exception {
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/service/thrift/impl/TSServiceImpl.java`
#### Snippet
```java
  public TSExecuteStatementResp executeQueryStatementV2(TSExecuteStatementReq req)
      throws TException {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/service/thrift/impl/TSServiceImpl.java`
#### Snippet
```java
  @Override
  public TSStatus deleteData(TSDeleteDataReq req) {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/service/thrift/impl/TSServiceImpl.java`
#### Snippet
```java
  @Override
  public TSStatus insertStringRecord(TSInsertStringRecordReq req) {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/service/thrift/impl/TSServiceImpl.java`
#### Snippet
```java
  @Override
  public TSStatus insertTablet(TSInsertTabletReq req) {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/service/thrift/impl/TSServiceImpl.java`
#### Snippet
```java
  @Override
  public TSStatus insertStringRecords(TSInsertStringRecordsReq req) {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/service/thrift/impl/ClientRPCServiceImpl.java`
#### Snippet
```java
      resp.setStatus(
          onQueryException(e, "\"" + statement + "\". " + OperationType.EXECUTE_STATEMENT));
      return null;
    } finally {
      addOperationLatency(Operation.EXECUTE_QUERY, startTime);
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/cache/DataNodeLastCacheManager.java`
#### Snippet
```java
  public static TimeValuePair getLastCache(SchemaCacheEntry entry) {
    if (!CACHE_ENABLED || null == entry) {
      return null;
    }
    ILastCacheContainer lastCacheContainer = entry.getLastCacheContainer();
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/cache/lastCache/container/LastCacheContainer.java`
#### Snippet
```java
  @Override
  public TimeValuePair getCachedLast() {
    return lastCacheValue == null ? null : lastCacheValue.getTimeValuePair();
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/cache/DataNodeSchemaCache.java`
#### Snippet
```java
    SchemaCacheEntry entry = cache.getIfPresent(seriesPath);
    if (null == entry) {
      return null;
    }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/mnode/StorageGroupEntityMNode.java`
#### Snippet
```java
  @Override
  public TStorageGroupSchema getStorageGroupSchema() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/LocalSchemaProcessor.java`
#### Snippet
```java
    }

    return failedNames.isEmpty() ? null : String.join(",", failedNames);
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/mnode/InternalMNode.java`
#### Snippet
```java
      return children.remove(name);
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/mnode/EntityMNode.java`
#### Snippet
```java
      return child;
    }
    return aliasChildren == null ? null : aliasChildren.get(name);
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/mnode/MeasurementMNode.java`
#### Snippet
```java
  @Override
  public IMNode deleteChild(String name) {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/mnode/MeasurementMNode.java`
#### Snippet
```java
  @Override
  public IMNode addChild(IMNode child) {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/mnode/MeasurementMNode.java`
#### Snippet
```java
  public IEntityMNode getParent() {
    if (parent == null) {
      return null;
    }
    return parent.getAsEntityMNode();
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/mnode/MeasurementMNode.java`
#### Snippet
```java
  public IMNode addChild(String name, IMNode child) {
    // Do nothing
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/mnode/container/MNodeContainers.java`
#### Snippet
```java

    public IMNode get(Object key) {
      return null;
    }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/cache/PlainCacheManager.java`
#### Snippet
```java
      }
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/cache/LRUCacheManager.java`
#### Snippet
```java
        }

        return target == null ? null : target.getNode();
      } finally {
        lock.unlock();
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/InternalPage.java`
#### Snippet
```java
  @Override
  public Integer getRecordByAlias(String alias) {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/WrappedSegment.java`
#### Snippet
```java

    if (ix < 0) {
      return null;
    }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/WrappedSegment.java`
#### Snippet
```java
      throws RecordDuplicatedException {
    if (buffer == null) {
      return null;
    }
    return new WrappedSegment(buffer, true);
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/WrappedSegment.java`
#### Snippet
```java

    if (index < 0) {
      return null;
    }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/WrappedSegment.java`
#### Snippet
```java
      return this.buffer.slice();
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/WrappedSegment.java`
#### Snippet
```java
      throws RecordDuplicatedException {
    if (buffer == null) {
      return null;
    }
    return new WrappedSegment(buffer, false);
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/MockSchemaFile.java`
#### Snippet
```java
  static IMNode cloneMNode(IMNode node) {
    if (node == null) {
      return null;
    }
    if (node.isMeasurement()) {
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/SchemaPage.java`
#### Snippet
```java
  @TestOnly
  public WrappedSegment getSegmentOnTest(short idx) throws SegmentNotFoundException {
    return null;
  };

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/SchemaPage.java`
#### Snippet
```java
  @Override
  public ISegment<Integer, Integer> getAsInternalPage() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/SchemaPage.java`
#### Snippet
```java
  @Override
  public ByteBuffer getEntireSegmentSlice() throws MetadataException {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/SchemaPage.java`
#### Snippet
```java
  @Override
  public ISegment<String, String> getAsAliasIndexPage() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/SchemaPage.java`
#### Snippet
```java
  @Override
  public ISegmentedPage getAsSegmentedPage() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/AliasIndexPage.java`
#### Snippet
```java
  @Override
  public ByteBuffer resetBuffer(int ptr) {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/AliasIndexPage.java`
#### Snippet
```java

    if (memberNum <= 0 || pos > memberNum) {
      return null;
    }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/AliasIndexPage.java`
#### Snippet
```java
    }

    return key.equals(getKeyByIndex(pos)) ? getNameByIndex(pos) : null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/SegmentedPage.java`
#### Snippet
```java
        < size + SchemaFileConfig.SEG_OFF_DIG) {
      // since this may occur frequently, throw exception here may be inefficient
      return null;
    }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/CachedMNodeContainer.java`
#### Snippet
```java

  private IMNode replace(Map<String, IMNode> map, String key, IMNode value) {
    return map == null ? null : map.replace(key, value);
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/CachedMNodeContainer.java`
#### Snippet
```java

  private IMNode get(Map<String, IMNode> map, Object key) {
    return map == null ? null : map.get(key);
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/CachedMNodeContainer.java`
#### Snippet
```java

  private IMNode remove(Map<String, IMNode> map, Object key) {
    return map == null ? null : map.remove(key);
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/RecordUtils.java`
#### Snippet
```java
    if (ReadWriteIOUtils.readByte(recBuf) != MEASUREMENT_TYPE) {
      recBuf.position(oriPos);
      return null;
    }
    recBuf.position(oriPos + ALIAS_OFFSET);
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/traverser/Traverser.java`
#### Snippet
```java
      if (skipPreDeletedSchema && node.getAsEntityMNode().isPreDeactivateTemplate()) {
        // skip this pre deactivated template, the invoker will skip this
        return null;
      }
      return templateMap.get(node.getSchemaTemplateId());
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/traverser/Traverser.java`
#### Snippet
```java
      }
    }
    return null;
  }
}
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/pagemgr/BTreePageManager.java`
#### Snippet
```java

    if (page.getAsInternalPage() == null || page.getSubIndex() < 0) {
      return null;
    }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/pagemgr/BTreePageManager.java`
#### Snippet
```java

    if (name == null) {
      return null;
    }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/idtable/IDTableManager.java`
#### Snippet
```java
    }

    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/idtable/entry/InsertMeasurementMNode.java`
#### Snippet
```java
  @Override
  public CacheEntry getCacheEntry() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/idtable/entry/InsertMeasurementMNode.java`
#### Snippet
```java
  @Override
  public IEntityMNode getParent() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/idtable/IDTableHashmapImpl.java`
#### Snippet
```java
    DeviceEntry deviceEntry = getDeviceEntry(deviceName);
    if (deviceEntry == null) {
      return null;
    }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/idtable/IDTableHashmapImpl.java`
#### Snippet
```java
    SchemaEntry schemaEntry = deviceEntry.getSchemaEntry(measurementName);
    if (schemaEntry == null) {
      return null;
    }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/logfile/MLogWriter.java`
#### Snippet
```java
        logger.error("unknown cmd {}", str);
    }
    return null;
  }
}
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/template/ClusterTemplateManager.java`
#### Snippet
```java
        }
      }
      return null;
    } finally {
      readWriteLock.readLock().unlock();
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/template/ClusterTemplateManager.java`
#### Snippet
```java
    try {
      if (!templateNameMap.containsKey(templateName)) {
        return null;
      }
      Template template = templateIdMap.get(templateNameMap.get(templateName));
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/visitor/SchemaExecutionVisitor.java`
#### Snippet
```java
  @Override
  public TSStatus visitPlan(PlanNode node, ISchemaRegion context) {
    return null;
  }
}
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/RSchemaRegionLoader.java`
#### Snippet
```java
        | RuntimeException e) {
      LOGGER.error("Cannot initialize RSchemaRegion", e);
      return null;
    }
    return region;
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/MTreeBelowSGMemoryImpl.java`
#### Snippet
```java
      curNode = curNode.getParent();
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/MTreeBelowSGMemoryImpl.java`
#### Snippet
```java
    MetaFormatUtils.checkTimeseries(devicePath);
    if (nodeNames.length == levelOfSG + 1) {
      return null;
    }
    IMNode cur = storageGroupMNode;
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/template/Template.java`
#### Snippet
```java
  private IMNode getPathNodeInTemplate(String[] pathNodes) {
    if (pathNodes.length == 0) {
      return null;
    }
    IMNode cur = directNodes.getOrDefault(pathNodes[0], null);
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/template/Template.java`
#### Snippet
```java
        cur = cur.getChild(pathNodes[i]);
      } else {
        return null;
      }
    }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/template/Template.java`
#### Snippet
```java
    String[] pathNodes = PathUtils.splitPathToDetachedNodes(path);
    if (pathNodes.length == 1) {
      return null;
    }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/protocol/mqtt/JSONPayloadFormatter.java`
#### Snippet
```java
  public List<Message> format(ByteBuf payload) {
    if (payload == null) {
      return null;
    }
    String txt = payload.toString(StandardCharsets.UTF_8);
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/protocol/rest/handler/AuthorizationHandler.java`
#### Snippet
```java
      return Response.ok().entity(ExceptionHandler.tryCatchException(e)).build();
    }
    return null;
  }
}
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/protocol/rest/filter/AuthorizationFilter.java`
#### Snippet
```java
              .build();
      containerRequestContext.abortWith(resp);
      return null;
    }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/protocol/rest/filter/AuthorizationFilter.java`
#### Snippet
```java
                .build();
        containerRequestContext.abortWith(resp);
        return null;
      }
    } catch (ConfigNodeConnectionException e) {
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/protocol/rest/filter/AuthorizationFilter.java`
#### Snippet
```java
              .build();
      containerRequestContext.abortWith(resp);
      return null;
    }
    return user;
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/MTreeBelowSGCachedImpl.java`
#### Snippet
```java
    MetaFormatUtils.checkTimeseries(devicePath);
    if (nodeNames.length == levelOfSG + 1) {
      return null;
    }
    IMNode cur = storageGroupMNode;
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/MTreeBelowSGCachedImpl.java`
#### Snippet
```java
    }
    unPinMNode(curNode);
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/util/FieldUtils.java`
#### Snippet
```java
  public static Object iotdbFieldConvert(Field field) {
    if (field.getDataType() == null) {
      return null;
    }
    switch (field.getDataType()) {
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/util/FieldUtils.java`
#### Snippet
```java
        return field.getBoolV();
      default:
        return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/util/JacksonUtils.java`
#### Snippet
```java
      return mapper.readValue(jsonStr, objClass);
    } catch (IOException e) {
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/util/JacksonUtils.java`
#### Snippet
```java
      return mapper.writeValueAsString(obj);
    } catch (JsonProcessingException e) {
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/consensus/statemachine/SchemaRegionStateMachine.java`
#### Snippet
```java
    } catch (IllegalArgumentException e) {
      logger.error(e.getMessage());
      return null;
    }
    logger.debug(
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/consensus/statemachine/visitor/DataExecutionVisitor.java`
#### Snippet
```java
  @Override
  public TSStatus visitPlan(PlanNode node, DataRegion context) {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/consensus/statemachine/DataRegionStateMachine.java`
#### Snippet
```java
          region.getDataRegionId(),
          e);
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/consensus/statemachine/DataRegionStateMachine.java`
#### Snippet
```java
      } catch (IllegalArgumentException e) {
        logger.error(e.getMessage());
        return null;
      }
      return QUERY_INSTANCE_MANAGER.execDataQueryFragmentInstance(fragmentInstance, region);
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/consensus/statemachine/DataRegionStateMachine.java`
#### Snippet
```java
    } catch (IOException e) {
      logger.warn("{}: cannot get the canonical file of {} due to {}", this, snapshotDir, e);
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/SchemaRegionMemoryImpl.java`
#### Snippet
```java
    IMNode result = deviceMNode.getChild(measurementName);
    if (result == null) {
      return null;
    }
    if (result.isMeasurement()) {
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/localconfignode/LocalSchemaPartitionTable.java`
#### Snippet
```java
  public SchemaRegionId getSchemaRegionId(PartialPath storageGroup, PartialPath path) {
    if (!table.containsKey(storageGroup)) {
      return null;
    }
    return calculateSchemaRegionId(storageGroup, path);
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/localconfignode/LocalDataPartitionInfo.java`
#### Snippet
```java
  public DataRegionId getDataRegionId(PartialPath storageGroup, PartialPath path) {
    if (!partitionTableMap.containsKey(storageGroup)) {
      return null;
    }
    LocalDataPartitionTable table = partitionTableMap.get(storageGroup);
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/utils/ResourceByPathUtils.java`
#### Snippet
```java
    if (!memTableMap.containsKey(deviceID)
        || !memTableMap.get(deviceID).contains(partialPath.getMeasurement())) {
      return null;
    }
    IWritableMemChunk memChunk =
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/utils/ResourceByPathUtils.java`
#### Snippet
```java
    // check If memtable contains this path
    if (!memTableMap.containsKey(deviceID)) {
      return null;
    }
    AlignedWritableMemChunk alignedMemChunk =
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/utils/ResourceByPathUtils.java`
#### Snippet
```java
    }
    if (!containsMeasurement) {
      return null;
    }
    // get sorted tv list is synchronized so different query can get right sorted list reference
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/localconfignode/LocalConfigNode.java`
#### Snippet
```java
    DataRegionId dataRegionId = dataPartitionInfo.getDataRegionId(storageGroup, path);
    if (dataRegionId == null) {
      return null;
    }
    DataRegion dataRegion = storageEngine.getDataRegion(dataRegionId);
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `metrics/micrometer-metrics/src/main/java/org/apache/iotdb/metrics/micrometer/reporter/IoTDBRegistryConfig.java`
#### Snippet
```java
        @Override
        public String get(String key) {
          return null;
        }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `trigger-api/src/main/java/org/apache/iotdb/trigger/api/TriggerAttributes.java`
#### Snippet
```java
  public Long getLong(String key) {
    String value = attributes.get(key);
    return value == null ? null : Long.parseLong(value);
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `trigger-api/src/main/java/org/apache/iotdb/trigger/api/TriggerAttributes.java`
#### Snippet
```java
  public Float getFloat(String key) {
    String value = attributes.get(key);
    return value == null ? null : Float.parseFloat(value);
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `trigger-api/src/main/java/org/apache/iotdb/trigger/api/TriggerAttributes.java`
#### Snippet
```java
  public Boolean getBoolean(String key) {
    String value = attributes.get(key);
    return value == null ? null : Boolean.parseBoolean(value);
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `trigger-api/src/main/java/org/apache/iotdb/trigger/api/TriggerAttributes.java`
#### Snippet
```java
  public Integer getInt(String key) {
    String value = attributes.get(key);
    return value == null ? null : Integer.parseInt(value);
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `trigger-api/src/main/java/org/apache/iotdb/trigger/api/TriggerAttributes.java`
#### Snippet
```java
  public Double getDouble(String key) {
    String value = attributes.get(key);
    return value == null ? null : Double.parseDouble(value);
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `zeppelin-interpreter/src/main/java/org/apache/zeppelin/iotdb/IoTDBInterpreter.java`
#### Snippet
```java
  @Override
  public ZeppelinContext getZeppelinContext() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `confignode/src/main/java/org/apache/iotdb/confignode/client/sync/SyncConfigNodeClientPool.java`
#### Snippet
```java
          case NOTIFY_REGISTER_SUCCESS:
            client.notifyRegisterSuccess();
            return null;
          case REMOVE_CONFIG_NODE:
            return removeConfigNode((TConfigNodeLocation) req, client);
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `confignode/src/main/java/org/apache/iotdb/confignode/conf/ConfigNodeDescriptor.java`
#### Snippet
```java
        // and CONFIGNODE_HOME will be set. But we didn't set these two in developer mode.
        // Thus, just return null and use default Configuration in developer mode.
        return null;
      }
    }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `confignode/src/main/java/org/apache/iotdb/confignode/conf/ConfigNodeDescriptor.java`
#### Snippet
```java
      return new URL(urlString);
    } catch (MalformedURLException e) {
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `confignode/src/main/java/org/apache/iotdb/confignode/manager/ConsensusManager.java`
#### Snippet
```java
      }
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/SchemaRegionSchemaFileImpl.java`
#### Snippet
```java
    IMNode result = mtree.getChildFromPinnedMNode(deviceMNode, measurementName);
    if (result == null) {
      return null;
    }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `confignode/src/main/java/org/apache/iotdb/confignode/manager/ConfigManager.java`
#### Snippet
```java
      return errorStatus.setMessage(errorPrefix + "least_data_region_group_num" + errorSuffix);
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `confignode/src/main/java/org/apache/iotdb/confignode/manager/ConfigManager.java`
#### Snippet
```java
      }
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `confignode/src/main/java/org/apache/iotdb/confignode/service/thrift/ConfigNodeRPCServiceHandler.java`
#### Snippet
```java
  public ServerContext createContext(TProtocol arg0, TProtocol arg1) {
    thriftConnectionNumber.incrementAndGet();
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `confignode/src/main/java/org/apache/iotdb/confignode/manager/node/NodeManager.java`
#### Snippet
```java
  public NodeStatus getNodeStatusByNodeId(int nodeId) {
    BaseNodeCache baseNodeCache = nodeCacheMap.get(nodeId);
    return baseNodeCache == null ? null : baseNodeCache.getNodeStatus();
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/TimeoutExecutorThread.java`
#### Snippet
```java
    } catch (InterruptedException e) {
      currentThread().interrupt();
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `confignode/src/main/java/org/apache/iotdb/confignode/consensus/statemachine/ConfigNodeRegionStateMachine.java`
#### Snippet
```java
      } catch (Throwable e) {
        LOGGER.error("Deserialization error for write plan : {}", request);
        return null;
      }
    } else if (request instanceof ConfigPhysicalPlan) {
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `confignode/src/main/java/org/apache/iotdb/confignode/consensus/statemachine/ConfigNodeRegionStateMachine.java`
#### Snippet
```java
    } else {
      LOGGER.error("Unexpected read plan : {}", request);
      return null;
    }
    return read(plan);
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/RootProcedureStack.java`
#### Snippet
```java
      }
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/RootProcedureStack.java`
#### Snippet
```java
  protected synchronized long[] getSubprocedureIds() {
    if (subprocs == null) {
      return null;
    }
    return subprocs.stream().mapToLong(Procedure::getProcId).toArray();
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/env/DataNodeRemoveHandler.java`
#### Snippet
```java
      status = new TSStatus(TSStatusCode.MIGRATE_REGION_ERROR.getStatusCode());
      status.setMessage("Cannot find region replica nodes, region: " + regionId);
      return null;
    }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/env/DataNodeRemoveHandler.java`
#### Snippet
```java
    if (!newNode.isPresent()) {
      LOGGER.warn("No enough Data node to migrate region: {}", regionId);
      return null;
    }
    return newNode.get();
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/impl/statemachine/StateMachineProcedure.java`
#### Snippet
```java

      if (!hasMoreState() || isFailed()) {
        return null;
      }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/impl/statemachine/StateMachineProcedure.java`
#### Snippet
```java
        return subProcedures;
      }
      return (isWaiting() || isFailed() || !hasMoreState()) ? null : new Procedure[] {this};
    } finally {
      updateTimestamp();
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/store/ProcedureFactory.java`
#### Snippet
```java
      return ProcedureType.UNSET_TEMPLATE_PROCEDURE;
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/scheduler/AbstractProcedureScheduler.java`
#### Snippet
```java
      if (!running) {
        LOG.debug("the scheduler is not running");
        return null;
      }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/scheduler/AbstractProcedureScheduler.java`
#### Snippet
```java
        }
        if (!queueHasRunnables()) {
          return null;
        }
      }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/scheduler/AbstractProcedureScheduler.java`
#### Snippet
```java
    } catch (InterruptedException e) {
      Thread.currentThread().interrupt();
      return null;
    } finally {
      schedUnlock();
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/impl/schema/DeleteTimeSeriesProcedure.java`
#### Snippet
```java
    fetchSchemaBlackListTask.execute();
    if (isFailed()) {
      return null;
    }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/ProcedureExecutor.java`
#### Snippet
```java
    CompletedProcedureContainer retainer = completed.get(procId);
    if (retainer == null) {
      return null;
    } else {
      return retainer.getProcedure();
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/ProcedureExecutor.java`
#### Snippet
```java
        String errMsg = "subproc[" + i + "] is null, aborting procedure";
        proc.setFailure(new ProcedureException((errMsg), new IllegalArgumentException(errMsg)));
        return null;
      }
      subproc.setParentProcId(proc.getProcId());
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `external-api/src/main/java/org/apache/iotdb/external/api/thrift/JudgableServerContext.java`
#### Snippet
```java
  @Override
  default <T> T unwrap(Class<T> iface) {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `flink-iotdb-connector/src/main/java/org/apache/iotdb/flink/DefaultIoTSerializationSchema.java`
#### Snippet
```java
  public Event serialize(Map<String, String> tuple) {
    if (tuple == null) {
      return null;
    }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `confignode/src/main/java/org/apache/iotdb/confignode/persistence/partition/PartitionInfo.java`
#### Snippet
```java
          .getPrecededDataPartition(seriesPartitionSlot, timePartitionSlot, timePartitionInterval);
    } else {
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `spark-tsfile/src/main/java/org/apache/iotdb/spark/tsfile/qp/optimizer/MergeSingleFilterOptimizer.java`
#### Snippet
```java
      filter.setIsSingle(false);
      filter.setChildrenList(ret);
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/mnode/RStorageGroupMNode.java`
#### Snippet
```java
  @Override
  public TStorageGroupSchema getStorageGroupSchema() {
    return null;
  }
}
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/mnode/RInternalMNode.java`
#### Snippet
```java
        if (readWriteHandler.keyExist(childInnerName)) {
          readWriteHandler.deleteByKey(childInnerName);
          return null;
        }
      } catch (RocksDBException e) {
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/mnode/RInternalMNode.java`
#### Snippet
```java
    // When based on Rocksdb, mNodes are not held in memory for long periods
    // Therefore, the return value here is meaningless
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `example/mqtt-customize/src/main/java/org/apache/iotdb/mqtt/server/CustomizedJsonPayloadFormatter.java`
#### Snippet
```java
    // Suppose the payload is a json format
    if (payload == null) {
      return null;
    }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `spark-tsfile/src/main/java/org/apache/iotdb/spark/tsfile/qp/QueryProcessor.java`
#### Snippet
```java

    if (singleFilterList == null) {
      return null;
    }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `spark-tsfile/src/main/java/org/apache/iotdb/spark/tsfile/qp/optimizer/PhysicalOptimizer.java`
#### Snippet
```java
  private Pair<String, Set<String>> mergeColumn(FilterOperator columnFilterOperator) {
    if (columnFilterOperator == null) {
      return null;
    }
    if (columnFilterOperator.isLeaf()) {
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `spark-tsfile/src/main/java/org/apache/iotdb/spark/tsfile/qp/optimizer/PhysicalOptimizer.java`
#### Snippet
```java
      // special case : not equal
      if (columnFilterOperator.getTokenIntType() == SQLConstant.NOTEQUAL) {
        return null;
      }
      //
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `spark-tsfile/src/main/java/org/apache/iotdb/spark/tsfile/qp/optimizer/PhysicalOptimizer.java`
#### Snippet
```java
    Pair<String, Set<String>> ret = mergeColumn(children.get(0));
    if (ret == null) {
      return null;
    }
    for (int i = 1; i < children.size(); i++) {
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `spark-tsfile/src/main/java/org/apache/iotdb/spark/tsfile/qp/optimizer/PhysicalOptimizer.java`
#### Snippet
```java
      Pair<String, Set<String>> temp = mergeColumn(children.get(i));
      if (temp == null) {
        return null;
      }
      switch (columnFilterOperator.getTokenIntType()) {
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/mnode/RMNode.java`
#### Snippet
```java
      return new PartialPath(fullPath);
    } catch (IllegalPathException ignored) {
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/mnode/RMNode.java`
#### Snippet
```java
      }
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `hive-connector/src/main/java/org/apache/iotdb/hive/TsFileSerDe.java`
#### Snippet
```java
  public SerDeStats getSerDeStats() {
    // Not support yet
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `hive-connector/src/main/java/org/apache/iotdb/hive/TsFileSerDe.java`
#### Snippet
```java
  public Writable serialize(Object obj, ObjectInspector objInspector) {
    // Not support yet
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/mnode/RMeasurementMNode.java`
#### Snippet
```java
  @Override
  public IMNode addChild(IMNode child) {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/mnode/RMeasurementMNode.java`
#### Snippet
```java
  public IMNode deleteChild(String name) {
    // Do nothing
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/mnode/RMeasurementMNode.java`
#### Snippet
```java
  public IEntityMNode getParent() {
    if (super.getParent() == null) {
      return null;
    }
    return parent.getAsEntityMNode();
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `hive-connector/src/main/java/org/apache/iotdb/hive/TsFileDeserializer.java`
#### Snippet
```java
    MapWritable mapWritable = (MapWritable) writable;
    if (!Objects.equals(mapWritable.get(new Text("device_id")).toString(), deviceId)) {
      return null;
    }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/file/metadata/AlignedChunkMetadata.java`
#### Snippet
```java
  public Statistics getStatistics(int index) {
    IChunkMetadata v = valueChunkMetadataList.get(index);
    return v == null ? null : v.getStatistics();
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/RSchemaReadWriteHandler.java`
#### Snippet
```java
      }
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/file/metadata/MetadataIndexNode.java`
#### Snippet
```java
    int index = binarySearchInChildren(key, exactSearch);
    if (index == -1) {
      return null;
    }
    long childEndOffset;
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/file/metadata/MetadataIndexNode.java`
#### Snippet
```java
  MetadataIndexEntry peek() {
    if (children.isEmpty()) {
      return null;
    }
    return children.get(0);
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/file/metadata/TimeseriesMetadata.java`
#### Snippet
```java
    if (excludedMeasurements.contains(measurementID)) {
      buffer.position(buffer.position() + chunkMetaDataListDataSize);
      return null;
    }
    TimeseriesMetadata timeseriesMetaData = new TimeseriesMetadata();
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/file/metadata/AlignedTimeSeriesMetadata.java`
#### Snippet
```java
  public Statistics getStatistics(int index) {
    TimeseriesMetadata v = valueTimeseriesMetadataList.get(index);
    return v == null ? null : v.getStatistics();
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/query/dataset/EmptyDataSet.java`
#### Snippet
```java
  @Override
  public RowRecord nextWithoutConstraint() {
    return null;
  }
}
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/v2/read/TsFileSequenceReaderForV2.java`
#### Snippet
```java
    if (metadataIndexPair == null) {
      if (ignoreNotExists) {
        return null;
      }
      return null;
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/v2/read/TsFileSequenceReaderForV2.java`
#### Snippet
```java
        return null;
      }
      return null;
    }
    ByteBuffer buffer = readData(metadataIndexPair.left.getOffset(), metadataIndexPair.right);
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/v2/read/TsFileSequenceReaderForV2.java`
#### Snippet
```java
    }
    if (metadataIndexPair == null) {
      return null;
    }
    List<TimeseriesMetadata> timeseriesMetadataList = new ArrayList<>();
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/v2/read/TsFileSequenceReaderForV2.java`
#### Snippet
```java
    int searchResult =
        binarySearchInTimeseriesMetadataList(timeseriesMetadataList, path.getMeasurement());
    return searchResult >= 0 ? timeseriesMetadataList.get(searchResult) : null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/query/timegenerator/TsFileTimeGenerator.java`
#### Snippet
```java
  @Override
  public Filter getTimeFilter() {
    return null;
  }
}
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/DescReadBatchData.java`
#### Snippet
```java
        return value;
      } else {
        return null;
      }
    }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/DescReadBatchData.java`
#### Snippet
```java
      }
    }
    return null;
  }
}
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/Field.java`
#### Snippet
```java
  public static Field getField(Object value, TSDataType dataType) {
    if (value == null) {
      return null;
    }
    Field field = new Field(dataType);
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/Field.java`
#### Snippet
```java
  public Object getObjectValue(TSDataType dataType) {
    if (this.dataType == null) {
      return null;
    }
    switch (dataType) {
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/block/column/TimeColumn.java`
#### Snippet
```java
  public boolean[] isNull() {
    // todo
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/BatchData.java`
#### Snippet
```java
        return value;
      } else {
        return null;
      }
    }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/BatchData.java`
#### Snippet
```java
      }
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/BatchData.java`
#### Snippet
```java
        return new TsVector(getVector());
      default:
        return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/BatchData.java`
#### Snippet
```java
        return getVector();
      default:
        return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/BatchData.java`
#### Snippet
```java
    public Object currentValue() {
      TsPrimitiveType v = getVector()[subIndex];
      return v == null ? null : v.getValue();
    }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/block/column/ColumnEncoder.java`
#### Snippet
```java
    boolean mayHaveNull = input.get() != 0;
    if (!mayHaveNull) {
      return null;
    }
    return deserializeBooleanArray(input, positionCount);
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/reader/LocalTsFileInput.java`
#### Snippet
```java
      int strLength = ReadWriteForEncodingUtils.readVarInt(byteBuffer);
      if (strLength < 0) {
        return null;
      } else if (strLength == 0) {
        return "";
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/reader/LocalTsFileInput.java`
#### Snippet
```java
      logger.warn(
          "Current thread is interrupted by another thread when it is blocked in an I/O operation upon a channel.");
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/reader/series/FileSeriesReaderByTimestamp.java`
#### Snippet
```java
    if (chunkReader == null) {
      if (!constructNextSatisfiedChunkReader()) {
        return null;
      }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/reader/series/FileSeriesReaderByTimestamp.java`
#### Snippet
```java
        data = chunkReader.nextPageData();
      } else {
        return null;
      }
    }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/reader/series/FileSeriesReaderByTimestamp.java`
#### Snippet
```java
          return value;
        }
        return null;
      } else {
        if (chunkReader.hasNextSatisfiedPage()) {
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/reader/series/FileSeriesReaderByTimestamp.java`
#### Snippet
```java
          data = chunkReader.nextPageData();
        } else if (!constructNextSatisfiedChunkReader()) {
          return null;
        }
      }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/reader/series/FileSeriesReaderByTimestamp.java`
#### Snippet
```java
    }

    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/reader/page/ValuePageReader.java`
#### Snippet
```java
    TsPrimitiveType resultValue = null;
    if (valueBuffer == null || ((bitmap[timeIndex / 8] & 0xFF) & (MASK >>> (timeIndex % 8))) == 0) {
      return null;
    }
    switch (dataType) {
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/reader/page/AlignedPageReader.java`
#### Snippet
```java
  public Statistics getStatistics(int index) {
    ValuePageReader valuePageReader = valuePageReaderList.get(index);
    return valuePageReader == null ? null : valuePageReader.getStatistics();
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/reader/chunk/AlignedChunkReader.java`
#### Snippet
```java
    }
    if (!exist) {
      return null;
    }
    AlignedPageReader alignedPageReader =
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/controller/MetadataQuerierByFileImpl.java`
#### Snippet
```java
    if (tsFileReader.getChunkMetadataList(path) == null
        || tsFileReader.getChunkMetadataList(path).isEmpty()) {
      return null;
    }
    return tsFileReader.getChunkMetadataList(path).get(0).getDataType();
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BytesUtils.java`
#### Snippet
```java
  public static byte[] subBytes(byte[] src, int start, int length) {
    if ((start + length) > src.length) {
      return null;
    }
    if (length <= 0) {
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BytesUtils.java`
#### Snippet
```java
    }
    if (length <= 0) {
      return null;
    }
    byte[] result = new byte[length];
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/FilePathUtils.java`
#### Snippet
```java
  public static String getTsFilePrefixPath(String tsFileAbsolutePath) {
    if (tsFileAbsolutePath == null) {
      return null;
    }
    String[] pathSegments = splitTsFilePath(tsFileAbsolutePath);
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/RamUsageEstimator.java`
#### Snippet
```java
          }

          return (pos >= keys.length ? null : keys[pos]);
        }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/ReadWriteIOUtils.java`
#### Snippet
```java
    int length = readInt(buffer);
    if (length == NO_BYTE_TO_READ) {
      return null;
    }
    Map<String, String> map = new HashMap<>(length);
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/ReadWriteIOUtils.java`
#### Snippet
```java
    int length = readInt(buffer);
    if (length == NO_BYTE_TO_READ) {
      return null;
    }
    LinkedHashMap<String, String> map = new LinkedHashMap<>(length);
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/ReadWriteIOUtils.java`
#### Snippet
```java
    int length = readInt(inputStream);
    if (length == NO_BYTE_TO_READ) {
      return null;
    }
    Map<String, String> map = new HashMap<>(length);
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/ReadWriteIOUtils.java`
#### Snippet
```java
    int strLength = ReadWriteForEncodingUtils.readVarInt(buffer);
    if (strLength < 0) {
      return null;
    } else if (strLength == 0) {
      return "";
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/ReadWriteIOUtils.java`
#### Snippet
```java
        return new Binary(bytes);
      case NULL:
        return null;
      case STRING:
      default:
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/ReadWriteIOUtils.java`
#### Snippet
```java
    int strLength = readInt(buffer);
    if (strLength < 0) {
      return null;
    } else if (strLength == 0) {
      return "";
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/ReadWriteIOUtils.java`
#### Snippet
```java
    int strLength = ReadWriteForEncodingUtils.readVarInt(inputStream);
    if (strLength < 0) {
      return null;
    } else if (strLength == 0) {
      return "";
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/ReadWriteIOUtils.java`
#### Snippet
```java
    int strLength = readInt(inputStream);
    if (strLength <= 0) {
      return null;
    }
    byte[] bytes = new byte[strLength];
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/writer/tsmiterator/DiskTSMIterator.java`
#### Snippet
```java
    } catch (IOException e) {
      LOG.error("Meets IOException when reading timeseries metadata from disk", e);
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/common/conf/TSFileDescriptor.java`
#### Snippet
```java
    } else {
      logger.warn("not found {}, use the default configs.", TSFileConfig.CONFIG_FILE_NAME);
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/common/conf/TSFileDescriptor.java`
#### Snippet
```java
    URL u = getResource(TSFileConfig.CONFIG_FILE_NAME, classLoader);
    if (u == null) {
      return null;
    } else {
      multiplicityWarning(TSFileConfig.CONFIG_FILE_NAME, classLoader);
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/common/conf/TSFileDescriptor.java`
#### Snippet
```java
    } catch (FileNotFoundException e) {
      logger.warn("Fail to find config file {}", filePath);
      return null;
    } catch (IOException e) {
      logger.warn("read file ({}) failure, please check the access permissions.", filePath);
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/common/conf/TSFileDescriptor.java`
#### Snippet
```java
    } catch (IOException e) {
      logger.warn("read file ({}) failure, please check the access permissions.", filePath);
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/record/Tablet.java`
#### Snippet
```java
  public static BitMap[] readBitMapsFromBuffer(ByteBuffer buffer, int columns, int size) {
    if (!buffer.hasRemaining()) {
      return null;
    }
    BitMap[] bitMaps = new BitMap[columns];
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/writer/TsFileIOWriter.java`
#### Snippet
```java
        } catch (IOException e) {
          logger.error("Failed to get device timeseries metadata map", e);
          return null;
        }
      }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/TsFileSequenceReader.java`
#### Snippet
```java
    // Not aligned timeseries
    if (!"".equals(measurementNode.getChildren().get(0).getName())) {
      return null;
    }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/TsFileSequenceReader.java`
#### Snippet
```java
      return tryToGetFirstTimeseriesMetadata(metadataIndexNode);
    }
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/TsFileSequenceReader.java`
#### Snippet
```java
    if (metadataIndexPair == null) {
      if (ignoreNotExists) {
        return null;
      }
      throw new IOException("Device {" + path.getDevice() + "} is not in tsFileMetaData");
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/TsFileSequenceReader.java`
#### Snippet
```java

    if (metadataIndexPair == null) {
      return null;
    }
    List<TimeseriesMetadata> timeseriesMetadataList = new ArrayList<>();
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/TsFileSequenceReader.java`
#### Snippet
```java
      }
    } else {
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/TsFileSequenceReader.java`
#### Snippet
```java
    if (metadataIndexPair == null) {
      if (ignoreNotExists) {
        return null;
      }
      throw new IOException("Device {" + path.getDevice() + "} is not in tsFileMetaData");
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/TsFileSequenceReader.java`
#### Snippet
```java
    }
    if (metadataIndexPair == null) {
      return null;
    }
    List<TimeseriesMetadata> timeseriesMetadataList = new ArrayList<>();
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/TsFileSequenceReader.java`
#### Snippet
```java
    int searchResult =
        binarySearchInTimeseriesMetadataList(timeseriesMetadataList, path.getMeasurement());
    return searchResult >= 0 ? timeseriesMetadataList.get(searchResult) : null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/TsFileSequenceReader.java`
#### Snippet
```java
      throws IOException {
    if (chunkMetadataList.isEmpty()) {
      return null;
    }
    IChunkMetadata lastChunkMetadata = chunkMetadataList.get(chunkMetadataList.size() - 1);
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/TsFileSequenceReader.java`
#### Snippet
```java
        getMetadataAndEndOffset(deviceMetadataIndexNode, path.getDevice(), true, true);
    if (metadataIndexPair == null) {
      return null;
    }
    ByteBuffer buffer = readData(metadataIndexPair.left.getOffset(), metadataIndexPair.right);
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/fileSystem/fileInputFactory/LocalFSInputFactory.java`
#### Snippet
```java
    } catch (IOException e) {
      logger.error("Failed to get TsFile input of file: {}, ", filePath, e);
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/fileSystem/fileInputFactory/HDFSInputFactory.java`
#### Snippet
```java
          filePath,
          e);
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/fileSystem/fsFactory/LocalFSFactory.java`
#### Snippet
```java
    } catch (IOException e) {
      logger.error("Failed to get buffered reader for {}. ", filePath, e);
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/fileSystem/fsFactory/LocalFSFactory.java`
#### Snippet
```java
    } catch (IOException e) {
      logger.error("Failed to get buffered input stream for {}. ", filePath, e);
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/fileSystem/fsFactory/LocalFSFactory.java`
#### Snippet
```java
    } catch (IOException e) {
      logger.error("Failed to get buffered writer for {}. ", filePath, e);
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/fileSystem/fsFactory/LocalFSFactory.java`
#### Snippet
```java
    } catch (IOException e) {
      logger.error("Failed to get buffered output stream for {}. ", filePath, e);
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/fileSystem/fileOutputFactory/LocalFSOutputFactory.java`
#### Snippet
```java
    } catch (IOException e) {
      logger.error("Failed to get TsFile output of file: {}, ", filePath, e);
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/fileSystem/fileOutputFactory/HDFSOutputFactory.java`
#### Snippet
```java
          filePath,
          e);
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/fileSystem/fsFactory/HDFSFactory.java`
#### Snippet
```java
          filePath,
          e);
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/fileSystem/fsFactory/HDFSFactory.java`
#### Snippet
```java
          filePath,
          e);
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/fileSystem/fsFactory/HDFSFactory.java`
#### Snippet
```java
      logger.error(
          "Failed to get file: {}. Please check your dependency of Hadoop module.", pathname, e);
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/fileSystem/fsFactory/HDFSFactory.java`
#### Snippet
```java
          prefix,
          e);
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/fileSystem/fsFactory/HDFSFactory.java`
#### Snippet
```java
          suffix,
          e);
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/fileSystem/fsFactory/HDFSFactory.java`
#### Snippet
```java
          filePath,
          e);
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/fileSystem/fsFactory/HDFSFactory.java`
#### Snippet
```java
      logger.error(
          "Failed to get file: {}. Please check your dependency of Hadoop module.", uri, e);
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/fileSystem/fsFactory/HDFSFactory.java`
#### Snippet
```java
          parent.getAbsolutePath() + File.separator + child,
          e);
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/fileSystem/fsFactory/HDFSFactory.java`
#### Snippet
```java
          filePath,
          e);
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/fileSystem/fsFactory/HDFSFactory.java`
#### Snippet
```java
          parent + File.separator + child,
          e);
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/fileSystem/fsFactory/HDFSFactory.java`
#### Snippet
```java
      logger.error(
          "Failed to get file: {}. Please check your dependency of Hadoop module.", pathname, e);
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `metrics/interface/src/main/java/org/apache/iotdb/metrics/metricsets/jvm/JvmUtils.java`
#### Snippet
```java
      // MemoryPoolMXBean.getUsage() should return null, not throwing InternalError, so it seems to
      // be a JVM bug.
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `integration-test/src/main/java/org/apache/iotdb/it/env/RemoteServerEnv.java`
#### Snippet
```java
  @Override
  public List<ConfigNodeWrapper> getConfigNodeWrapperList() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `integration-test/src/main/java/org/apache/iotdb/it/env/RemoteServerEnv.java`
#### Snippet
```java
  @Override
  public List<DataNodeWrapper> getDataNodeWrapperList() {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `flink-tsfile-connector/src/main/java/org/apache/iotdb/flink/tsfile/RowRowRecordParser.java`
#### Snippet
```java
  private Object toSqlValue(Field field) {
    if (field == null) {
      return null;
    } else if (field.getDataType() == null) {
      return null;
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `flink-tsfile-connector/src/main/java/org/apache/iotdb/flink/tsfile/RowRowRecordParser.java`
#### Snippet
```java
      return null;
    } else if (field.getDataType() == null) {
      return null;
    } else {
      switch (field.getDataType()) {
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `hadoop/src/main/java/org/apache/iotdb/hadoop/fileSystem/HDFSFile.java`
#### Snippet
```java
    } catch (IOException e) {
      logger.error("Failed to get buffered writer for {}. ", filePath, e);
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `hadoop/src/main/java/org/apache/iotdb/hadoop/fileSystem/HDFSFile.java`
#### Snippet
```java
    } catch (IOException e) {
      logger.error("Failed to get buffered output stream for {}. ", filePath, e);
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `hadoop/src/main/java/org/apache/iotdb/hadoop/fileSystem/HDFSFile.java`
#### Snippet
```java
    } catch (IOException e) {
      logger.error("Failed to get buffered input stream for {}. ", filePath, e);
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `hadoop/src/main/java/org/apache/iotdb/hadoop/fileSystem/HDFSFile.java`
#### Snippet
```java
    } catch (IOException e) {
      logger.error("Failed to get buffered reader for {}. ", filePath, e);
      return null;
    }
  }
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `udf-api/src/main/java/org/apache/iotdb/udf/api/type/Binary.java`
#### Snippet
```java
  public String getStringValue() {
    if (values == null) {
      return null;
    }
    if (stringCache == null) {
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/RSchemaRegion.java`
#### Snippet
```java
    }
    if (j < level) {
      return null;
    }
    return stringBuilder.toString();
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `udf-api/src/main/java/org/apache/iotdb/udf/api/customizer/parameter/UDFParameters.java`
#### Snippet
```java
  public Double getDouble(String key) {
    String value = attributes.get(key);
    return value == null ? null : Double.parseDouble(value);
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `udf-api/src/main/java/org/apache/iotdb/udf/api/customizer/parameter/UDFParameters.java`
#### Snippet
```java
  public Float getFloat(String key) {
    String value = attributes.get(key);
    return value == null ? null : Float.parseFloat(value);
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `udf-api/src/main/java/org/apache/iotdb/udf/api/customizer/parameter/UDFParameters.java`
#### Snippet
```java
  public Integer getInt(String key) {
    String value = attributes.get(key);
    return value == null ? null : Integer.parseInt(value);
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `udf-api/src/main/java/org/apache/iotdb/udf/api/customizer/parameter/UDFParameters.java`
#### Snippet
```java
  public Boolean getBoolean(String key) {
    String value = attributes.get(key);
    return value == null ? null : Boolean.parseBoolean(value);
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `udf-api/src/main/java/org/apache/iotdb/udf/api/customizer/parameter/UDFParameters.java`
#### Snippet
```java
  public Long getLong(String key) {
    String value = attributes.get(key);
    return value == null ? null : Long.parseLong(value);
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `consensus/src/main/java/org/apache/iotdb/consensus/IStateMachine.java`
#### Snippet
```java
   */
  default File getSnapshotRoot() {
    return null;
  }
}
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `consensus/src/main/java/org/apache/iotdb/consensus/ratis/SnapshotStorage.java`
#### Snippet
```java
    Path[] snapshots = getSortedSnapshotDirPaths();
    if (snapshots == null || snapshots.length == 0) {
      return null;
    }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `consensus/src/main/java/org/apache/iotdb/consensus/ratis/SnapshotStorage.java`
#### Snippet
```java
    } catch (IOException exception) {
      logger.warn("cannot construct snapshot directory stream ", exception);
      return null;
    }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `consensus/src/main/java/org/apache/iotdb/consensus/ratis/SnapshotStorage.java`
#### Snippet
```java
    File latestSnapshotDir = findLatestSnapshotDir();
    if (latestSnapshotDir == null) {
      return null;
    }
    TermIndex snapshotTermIndex = Utils.getTermIndexFromDir(latestSnapshotDir);
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `consensus/src/main/java/org/apache/iotdb/consensus/ratis/SnapshotStorage.java`
#### Snippet
```java
    List<Path> actualSnapshotFiles = applicationStateMachine.getSnapshotFiles(latestSnapshotDir);
    if (actualSnapshotFiles == null) {
      return null;
    }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `consensus/src/main/java/org/apache/iotdb/consensus/ratis/SnapshotStorage.java`
#### Snippet
```java
      } catch (IOException e) {
        logger.warn("{} cannot resolve real path of {} due to {}", this, file, e);
        return null;
      }
      fileInfos.add(fileInfo);
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `consensus/src/main/java/org/apache/iotdb/consensus/ratis/SnapshotStorage.java`
#### Snippet
```java
  @Override
  public File getTmpDir() {
    return getSnapshotDir() == null ? null : new File(getSnapshotDir(), TMP_PREFIX);
  }
}
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `consensus/src/main/java/org/apache/iotdb/consensus/common/request/BatchIndexedConsensusRequest.java`
#### Snippet
```java
  @Override
  public ByteBuffer serializeToByteBuffer() {
    return null;
  }
}
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `consensus/src/main/java/org/apache/iotdb/consensus/simple/SimpleConsensus.java`
#### Snippet
```java
  public Peer getLeader(ConsensusGroupId groupId) {
    if (!stateMachineMap.containsKey(groupId)) {
      return null;
    }
    return new Peer(groupId, thisNodeId, thisNode);
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `consensus/src/main/java/org/apache/iotdb/consensus/multileader/service/MultiLeaderRPCServiceHandler.java`
#### Snippet
```java
  @Override
  public ServerContext createContext(TProtocol input, TProtocol output) {
    return null;
  }

```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `consensus/src/main/java/org/apache/iotdb/consensus/multileader/MultiLeaderConsensus.java`
#### Snippet
```java
  public Peer getLeader(ConsensusGroupId groupId) {
    if (!stateMachineMap.containsKey(groupId)) {
      return null;
    }
    return new Peer(groupId, thisNodeId, thisNode);
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `consensus/src/main/java/org/apache/iotdb/consensus/ratis/RatisConsensus.java`
#### Snippet
```java
    } catch (IOException e) {
      logger.warn("fetch division info for group " + groupId + " failed due to: ", e);
      return null;
    }
    if (leaderId == null) {
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `consensus/src/main/java/org/apache/iotdb/consensus/ratis/RatisConsensus.java`
#### Snippet
```java
    }
    if (leaderId == null) {
      return null;
    }
    int nodeId = Utils.fromRaftPeerIdToNodeId(leaderId);
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `grafana-connector/src/main/java/org/apache/iotdb/web/grafana/controller/DatabaseConnectController.java`
#### Snippet
```java
      JsonObject jsonObject = GSON.fromJson(json, JsonObject.class);
      if (Objects.isNull(jsonObject)) {
        return null;
      }
      Pair<ZonedDateTime, ZonedDateTime> timeRange = getTimeFromAndTo(jsonObject);
```

### RuleId[ruleID=ReturnNull]
Return of `null`
in `grafana-connector/src/main/java/org/apache/iotdb/web/grafana/controller/DatabaseConnectController.java`
#### Snippet
```java
      logger.error("/query failed, request body is {}", json.replaceAll("[\n\r\t]", "_"), e);
    }
    return null;
  }

```

## RuleId[ruleID=AssignmentToLambdaParameter]
### RuleId[ruleID=AssignmentToLambdaParameter]
Assignment to lambda parameter `v`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/MemMTreeStore.java`
#### Snippet
```java

  private void releaseMemory(int size) {
    localMemoryUsage.getAndUpdate(v -> v -= size);
    memoryStatistics.releaseMemory(size);
  }
```

### RuleId[ruleID=AssignmentToLambdaParameter]
Assignment to lambda parameter `v`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/MemMTreeStore.java`
#### Snippet
```java
  private void requestMemory(int size) {
    memoryStatistics.requestMemory(size);
    localMemoryUsage.getAndUpdate(v -> v += size);
  }

```

### RuleId[ruleID=AssignmentToLambdaParameter]
Assignment to lambda parameter `v`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/memcontrol/MemManagerNodeEstimatedSizeBasedImpl.java`
#### Snippet
```java
    int size = estimator.estimateSize(node);
    memoryStatistics.requestMemory(size);
    pinnedSize.getAndUpdate(v -> v += size);
  }

```

### RuleId[ruleID=AssignmentToLambdaParameter]
Assignment to lambda parameter `v`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/memcontrol/MemManagerNodeEstimatedSizeBasedImpl.java`
#### Snippet
```java
      memoryStatistics.releaseMemory(deltaSize);
    }
    pinnedSize.getAndUpdate(v -> v += deltaSize);
  }

```

### RuleId[ruleID=AssignmentToLambdaParameter]
Assignment to lambda parameter `v`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/memcontrol/MemManagerNodeEstimatedSizeBasedImpl.java`
#### Snippet
```java
  public void releasePinnedMemResource(IMNode node) {
    int size = estimator.estimateSize(node);
    this.size.getAndUpdate(v -> v += size);
    pinnedSize.getAndUpdate(v -> v -= size);
  }
```

### RuleId[ruleID=AssignmentToLambdaParameter]
Assignment to lambda parameter `v`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/memcontrol/MemManagerNodeEstimatedSizeBasedImpl.java`
#### Snippet
```java
    int size = estimator.estimateSize(node);
    this.size.getAndUpdate(v -> v += size);
    pinnedSize.getAndUpdate(v -> v -= size);
  }

```

### RuleId[ruleID=AssignmentToLambdaParameter]
Assignment to lambda parameter `v`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/memcontrol/MemManagerNodeEstimatedSizeBasedImpl.java`
#### Snippet
```java
    }
    int finalSize = size;
    this.size.getAndUpdate(v -> v -= finalSize);
    memoryStatistics.releaseMemory(size);
  }
```

### RuleId[ruleID=AssignmentToLambdaParameter]
Assignment to lambda parameter `v`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/memcontrol/MemManagerNodeEstimatedSizeBasedImpl.java`
#### Snippet
```java
  public void upgradeMemResource(IMNode node) {
    int size = estimator.estimateSize(node);
    pinnedSize.getAndUpdate(v -> v += size);
    this.size.getAndUpdate(v -> v -= size);
  }
```

### RuleId[ruleID=AssignmentToLambdaParameter]
Assignment to lambda parameter `v`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/memcontrol/MemManagerNodeEstimatedSizeBasedImpl.java`
#### Snippet
```java
    int size = estimator.estimateSize(node);
    pinnedSize.getAndUpdate(v -> v += size);
    this.size.getAndUpdate(v -> v -= size);
  }

```

### RuleId[ruleID=AssignmentToLambdaParameter]
Assignment to lambda parameter `v`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/memcontrol/MemManagerNodeEstimatedSizeBasedImpl.java`
#### Snippet
```java
  public void releaseMemResource(IMNode node) {
    int size = estimator.estimateSize(node);
    this.size.getAndUpdate(v -> v -= size);
    memoryStatistics.releaseMemory(size);
  }
```

### RuleId[ruleID=AssignmentToLambdaParameter]
Assignment to lambda parameter `value`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/memcontrol/MemManagerNodeNumBasedImpl.java`
#### Snippet
```java
  @Override
  public void releaseMemResource(List<IMNode> evictedNodes) {
    size.getAndUpdate(value -> value -= evictedNodes.size());
  }

```

### RuleId[ruleID=AssignmentToLambdaParameter]
Assignment to lambda parameter `v`
in `server/src/main/java/org/apache/iotdb/db/metadata/rescon/MemoryStatistics.java`
#### Snippet
```java

  public void requestMemory(long size) {
    memoryUsage.getAndUpdate(v -> v += size);
    if (memoryUsage.get() >= memoryCapacity) {
      logger.warn("Current series number {} is too large...", memoryUsage);
```

### RuleId[ruleID=AssignmentToLambdaParameter]
Assignment to lambda parameter `v`
in `server/src/main/java/org/apache/iotdb/db/metadata/rescon/MemoryStatistics.java`
#### Snippet
```java

  public void releaseMemory(long size) {
    memoryUsage.getAndUpdate(v -> v -= size);
    if (!allowToCreateNewSeries && memoryUsage.get() < memoryCapacity) {
      logger.info("Current series number {} come back to normal level", memoryUsage);
```

### RuleId[ruleID=AssignmentToLambdaParameter]
Assignment to lambda parameter `procedure`
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/ProcedureExecutor.java`
#### Snippet
```java
                }
                stack.push(procedure);
                procedure = procedures.get(procedure.getParentProcId());
              }
            });
```

## RuleId[ruleID=UnnecessaryLocalVariable]
### RuleId[ruleID=UnnecessaryLocalVariable]
Local variable `httpClient` is redundant
in `example/rest-java-example/src/main/java/org/apache/iotdb/SSLClient.java`
#### Snippet
```java

  public CloseableHttpClient getHttpClient() {
    CloseableHttpClient httpClient =
        HttpClients.custom()
            .setSSLSocketFactory(sslConnectionSocketFactory)
```

### RuleId[ruleID=UnnecessaryLocalVariable]
Local variable `result` is redundant
in `schema-engine-tag/src/main/java/org/apache/iotdb/lsm/engine/LSMEngineBuilder.java`
#### Snippet
```java
              InstantiationException, IllegalAccessException {
    Class c = Class.forName(className);
    ILevelProcessor<T, ?, R, C> result =
        (ILevelProcessor<T, ?, R, C>) c.getDeclaredConstructor().newInstance();
    return result;
```

### RuleId[ruleID=UnnecessaryLocalVariable]
Local variable `result` is redundant
in `schema-engine-tag/src/main/java/org/apache/iotdb/db/metadata/tagSchemaRegion/TagSchemaRegion.java`
#### Snippet
```java
      throws MetadataException {
    List<MeasurementPath> res = getMeasurementPaths(pathPattern, isPrefixMatch, false);
    Pair<List<MeasurementPath>, Integer> result = new Pair<>(res, 0);
    return result;
  }
```

### RuleId[ruleID=UnnecessaryLocalVariable]
Local variable `aggregatePath` is redundant
in `server/src/main/java/org/apache/iotdb/db/qp/physical/crud/AggregationPlan.java`
#### Snippet
```java
      PartialPath path = paths.get(pathIndex);
      String functionName = aggregations.get(pathIndex);
      String aggregatePath =
          groupByLevelController.getGroupedPath(
              String.format("%s(%s)", functionName, path.getFullPath()));
```

### RuleId[ruleID=UnnecessaryLocalVariable]
Local variable `regexpFilter` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/common/filter/RegexpFilter.java`
#### Snippet
```java
    QueryFilter queryFilter = QueryFilter.deserialize(byteBuffer);
    String value = ReadWriteIOUtils.readString(byteBuffer);
    RegexpFilter regexpFilter =
        new RegexpFilter(queryFilter.filterType, queryFilter.singlePath, value);
    return regexpFilter;
```

### RuleId[ruleID=UnnecessaryLocalVariable]
Local variable `likeFilter` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/common/filter/LikeFilter.java`
#### Snippet
```java
  public static LikeFilter deserialize(ByteBuffer byteBuffer) {
    QueryFilter queryFilter = QueryFilter.deserialize(byteBuffer);
    LikeFilter likeFilter =
        new LikeFilter(
            queryFilter.filterType,
```

### RuleId[ruleID=UnnecessaryLocalVariable]
Local variable `writer` is redundant
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileSplitByPartitionTool.java`
#### Snippet
```java
              logger.error("Create new TsFile {} failed because it exists", newFile);
            }
            TsFileIOWriter writer = new TsFileIOWriter(newFile);
            return writer;
          } catch (IOException e) {
```

### RuleId[ruleID=UnnecessaryLocalVariable]
Local variable `timePartitions` is redundant
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileManager.java`
#### Snippet
```java
    readLock();
    try {
      Set<Long> timePartitions = new HashSet<>(sequenceFiles.keySet());
      return timePartitions;
    } finally {
```

### RuleId[ruleID=UnnecessaryLocalVariable]
Local variable `tsFileName` is redundant
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileNameGenerator.java`
#### Snippet
```java
    if (matcher.find()) {
      try {
        TsFileName tsFileName =
            new TsFileName(
                Long.parseLong(matcher.group(1)),
```

### RuleId[ruleID=UnnecessaryLocalVariable]
Local variable `executeStatementResp` is redundant
in `server/src/main/java/org/apache/iotdb/db/service/thrift/impl/NewInfluxDBServiceImpl.java`
#### Snippet
```java
        NewInfluxDBServiceImpl.getClientRPCService().requestStatementId(sessionId));
    tsExecuteStatementReq.setFetchSize(InfluxConstant.DEFAULT_FETCH_SIZE);
    TSExecuteStatementResp executeStatementResp =
        NewInfluxDBServiceImpl.getClientRPCService().executeStatement(tsExecuteStatementReq);
    return executeStatementResp;
```

### RuleId[ruleID=UnnecessaryLocalVariable]
Local variable `ret` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/util/CircularQueue.java`
#### Snippet
```java
      throw new IllegalArgumentException("Error: Queue is Empty!");
    }
    E ret = data[head];
    return ret;
  }
```

### RuleId[ruleID=UnnecessaryLocalVariable]
Local variable `ret` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/util/LongCircularQueue.java`
#### Snippet
```java
      throw new IllegalArgumentException("Error: Queue is Empty!");
    }
    long ret = data[head];
    return ret;
  }
```

### RuleId[ruleID=UnnecessaryLocalVariable]
Local variable `ret` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/util/BooleanCircularQueue.java`
#### Snippet
```java
      throw new IllegalArgumentException("Error: Queue is Empty!");
    }
    boolean ret = data[head];
    return ret;
  }
```

### RuleId[ruleID=UnnecessaryLocalVariable]
Local variable `ret` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/util/DoubleCircularQueue.java`
#### Snippet
```java
      throw new IllegalArgumentException("Error: Queue is Empty!");
    }
    double ret = data[head];
    return ret;
  }
```

### RuleId[ruleID=UnnecessaryLocalVariable]
Local variable `deviceId` is redundant
in `example/session/src/main/java/org/apache/iotdb/SessionExample.java`
#### Snippet
```java
  private static void insertStrRecord()
      throws IoTDBConnectionException, StatementExecutionException {
    String deviceId = ROOT_SG1_D1;
    List<String> measurements = new ArrayList<>();
    measurements.add("s1");
```

### RuleId[ruleID=UnnecessaryLocalVariable]
Local variable `deviceId` is redundant
in `example/session/src/main/java/org/apache/iotdb/SessionExample.java`
#### Snippet
```java
  private static void insertRecordInObject()
      throws IoTDBConnectionException, StatementExecutionException {
    String deviceId = ROOT_SG1_D1;
    List<String> measurements = new ArrayList<>();
    List<TSDataType> types = new ArrayList<>();
```

### RuleId[ruleID=UnnecessaryLocalVariable]
Local variable `path` is redundant
in `example/session/src/main/java/org/apache/iotdb/SessionExample.java`
#### Snippet
```java

  private static void deleteData() throws IoTDBConnectionException, StatementExecutionException {
    String path = ROOT_SG1_D1_S1;
    long deleteTime = 99;
    session.deleteData(path, deleteTime);
```

### RuleId[ruleID=UnnecessaryLocalVariable]
Local variable `deviceId` is redundant
in `example/session/src/main/java/org/apache/iotdb/SessionExample.java`
#### Snippet
```java

  private static void insertRecords() throws IoTDBConnectionException, StatementExecutionException {
    String deviceId = ROOT_SG1_D1;
    List<String> measurements = new ArrayList<>();
    measurements.add("s1");
```

### RuleId[ruleID=UnnecessaryLocalVariable]
Local variable `deviceId` is redundant
in `example/session/src/main/java/org/apache/iotdb/SessionExample.java`
#### Snippet
```java

  private static void insertRecord() throws IoTDBConnectionException, StatementExecutionException {
    String deviceId = ROOT_SG1_D1;
    List<String> measurements = new ArrayList<>();
    List<TSDataType> types = new ArrayList<>();
```

### RuleId[ruleID=UnnecessaryLocalVariable]
Local variable `res_df1` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/Segment.java`
#### Snippet
```java
  public static ArrayList<double[]> swab_alg(
      double[] df, long[] timestamp, double max_error, int windowsize) throws Exception {
    ArrayList<double[]> res_df1 = swab(df, max_error, 10, windowsize);
    return res_df1;
  }
```

### RuleId[ruleID=UnnecessaryLocalVariable]
Local variable `procedure` is redundant
in `confignode/src/main/java/org/apache/iotdb/confignode/consensus/request/write/procedure/UpdateProcedurePlan.java`
#### Snippet
```java
  @Override
  protected void deserializeImpl(ByteBuffer buffer) throws IOException {
    Procedure procedure = ProcedureFactory.getInstance().create(buffer);
    this.procedure = procedure;
  }
```

### RuleId[ruleID=UnnecessaryLocalVariable]
Local variable `pollResult` is redundant
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/scheduler/AbstractProcedureScheduler.java`
#### Snippet
```java
        }
      }
      final Procedure pollResult = dequeue();

      return pollResult;
```

### RuleId[ruleID=UnnecessaryLocalVariable]
Local variable `ts` is redundant
in `example/mqtt-customize/src/main/java/org/apache/iotdb/mqtt/server/CustomizedJsonPayloadFormatter.java`
#### Snippet
```java
    // this is just an example, so we just generate some Messages directly
    for (int i = 0; i < 2; i++) {
      long ts = i;
      Message message = new Message();
      message.setDevice("d" + i);
```

### RuleId[ruleID=UnnecessaryLocalVariable]
Local variable `s` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BytesUtils.java`
#### Snippet
```java
    short s1 = (short) (b[0] & 0xff);
    s1 <<= 8;
    short s = (short) (s0 | s1);
    return s;
  }
```

### RuleId[ruleID=UnnecessaryLocalVariable]
Local variable `cachedInfo` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/RamUsageEstimator.java`
#### Snippet
```java
    }

    ClassCache cachedInfo =
        new ClassCache(alignObjectSize(shallowInstanceSize), referenceFields.toArray(new Field[0]));
    return cachedInfo;
```

### RuleId[ruleID=UnnecessaryLocalVariable]
Local variable `timeseriesMetadata` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/writer/tsmiterator/TSMIterator.java`
#### Snippet
```java
    }

    TimeseriesMetadata timeseriesMetadata =
        new TimeseriesMetadata(
            (byte)
```

### RuleId[ruleID=UnnecessaryLocalVariable]
Local variable `pool` is redundant
in `integration-test/src/main/java/org/apache/iotdb/itbase/env/BaseEnv.java`
#### Snippet
```java
      int thriftDefaultBufferSize,
      int thriftMaxFrameSize) {
    SessionPool pool =
        new SessionPool(
            host,
```

## RuleId[ruleID=ArrayHashCode]
### RuleId[ruleID=ArrayHashCode]
Array passed to 'Objects.hash()' should be wrapped in 'Arrays.hashcode()'
in `confignode/src/main/java/org/apache/iotdb/confignode/consensus/request/write/storagegroup/SetTTLPlan.java`
#### Snippet
```java
  @Override
  public int hashCode() {
    return Objects.hash(storageGroupPathPattern, TTL);
  }
}
```

### RuleId[ruleID=ArrayHashCode]
Array passed to 'Objects.hash()' should be wrapped in 'Arrays.hashcode()'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BloomFilter.java`
#### Snippet
```java
  @Override
  public int hashCode() {
    return Objects.hash(size, hashFunctionSize, bits, func);
  }

```

### RuleId[ruleID=ArrayHashCode]
Array passed to 'Objects.hash()' should be wrapped in 'Arrays.hashcode()'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/schema/VectorMeasurementSchema.java`
#### Snippet
```java
  @Override
  public int hashCode() {
    return Objects.hash(deviceId, types, encodings, compressor);
  }

```

### RuleId[ruleID=ArrayHashCode]
Array passed to 'Objects.hash()' should be wrapped in 'Arrays.hashcode()'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/schema/VectorMeasurementSchema.java`
#### Snippet
```java
  @Override
  public int hashCode() {
    return Objects.hash(deviceId, types, encodings, compressor);
  }

```

## RuleId[ruleID=UseCompareMethod]
### RuleId[ruleID=UseCompareMethod]
Expression can be replaced with 'Long.compare'
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/AbstractOpBlock.java`
#### Snippet
```java
  @Override
  public int compareTo(AbstractOpBlock o) {
    return beginIndex > o.beginIndex ? 1 : (beginIndex == o.beginIndex ? 0 : -1);
  }

```

### RuleId[ruleID=UseCompareMethod]
Expression can be replaced with 'Integer.compare'
in `library-udf/src/main/java/org/apache/iotdb/library/anomaly/util/MissDetector.java`
#### Snippet
```java
    @Override
    public int compareTo(MissingSubSeries o) {
      if (this.getLength() > o.getLength()) {
        return -1;
      } else if (this.getLength() == o.getLength()) {
```

### RuleId[ruleID=UseCompareMethod]
Expression can be replaced with 'Double.compare'
in `library-udf/src/main/java/org/apache/iotdb/library/drepair/util/LsGreedy.java`
#### Snippet
```java
      double u1 = Math.abs(this.u - center);
      double u2 = Math.abs(o.u - center);
      if (u1 > u2) {
        return -1;
      } else if (u1 == u2) {
```

### RuleId[ruleID=UseCompareMethod]
Expression can be replaced with 'Long.compare'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/TimeRange.java`
#### Snippet
```java
      return -1;
    } else {
      if (this.max > r.max) {
        return 1;
      } else if (this.max < r.max) {
```

## RuleId[ruleID=BusyWait]
### RuleId[ruleID=BusyWait]
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `node-commons/src/main/java/org/apache/iotdb/commons/service/ThriftService.java`
#### Snippet
```java
      while (!thriftServiceThread.isServing()) {
        // sleep 100ms for waiting the rpc server start.
        Thread.sleep(100);
      }
    } catch (InterruptedException
```

### RuleId[ruleID=BusyWait]
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `integration/src/main/java/org/apache/iotdb/integration/env/ClusterEnvBase.java`
#### Snippet
```java

    do {
      Thread.sleep(1000);

      try (IoTDBConnection connection = getConnection(60);
```

### RuleId[ruleID=BusyWait]
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `integration/src/main/java/org/apache/iotdb/integration/env/ClusterNode.java`
#### Snippet
```java
    while (this.instance.isAlive()) {
      try {
        Thread.sleep(100);
      } catch (InterruptedException e) {
        e.printStackTrace();
```

### RuleId[ruleID=BusyWait]
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `server/src/main/java/org/apache/iotdb/db/wal/WALManager.java`
#### Snippet
```java
      while (!walNode.isAllWALEntriesConsumed()) {
        try {
          Thread.sleep(50);
        } catch (InterruptedException e) {
          logger.error("Interrupted when waiting for all write-ahead logs flushed.");
```

### RuleId[ruleID=BusyWait]
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `server/src/main/java/org/apache/iotdb/db/wal/node/WALNode.java`
#### Snippet
```java
        while (memTable.getFlushStatus() != FlushStatus.FLUSHED) {
          try {
            Thread.sleep(1_000);
            sleepTime += 1_000;
            if (sleepTime > 10_000) {
```

### RuleId[ruleID=BusyWait]
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `server/src/main/java/org/apache/iotdb/db/sync/externalpipe/ExtPipePlugin.java`
#### Snippet
```java
        // Backoff
        try {
          Thread.sleep(configuration.getBackOffInterval());
        } catch (InterruptedException ignored) {
        }
```

### RuleId[ruleID=BusyWait]
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `server/src/main/java/org/apache/iotdb/db/sync/externalpipe/ExtPipePlugin.java`
#### Snippet
```java
          // Backoff
          try {
            Thread.sleep(configuration.getBackOffInterval());
          } catch (InterruptedException ignored) {
          }
```

### RuleId[ruleID=BusyWait]
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/CompactionTaskManager.java`
#### Snippet
```java
    while (!storageGroupTasks.containsKey(regionWithSG)
        || !storageGroupTasks.get(regionWithSG).containsKey(task)) {
      Thread.sleep(10);
      if (System.currentTimeMillis() - startTime > 20_000) {
        throw new TimeoutException("Timeout when waiting for task future");
```

### RuleId[ruleID=BusyWait]
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/CompactionTaskManager.java`
#### Snippet
```java
    while (!taskExecutionPool.isTerminated()) {
      try {
        Thread.sleep(200);
      } catch (InterruptedException e) {
        Thread.currentThread().interrupt();
```

### RuleId[ruleID=BusyWait]
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/CompactionTaskManager.java`
#### Snippet
```java
        if (totalSize > 0) {
          try {
            Thread.sleep(100);
          } catch (InterruptedException e) {
            logger.error("Interrupted when waiting all task finish", e);
```

### RuleId[ruleID=BusyWait]
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `server/src/main/java/org/apache/iotdb/db/rescon/SystemInfo.java`
#### Snippet
```java
    while (originSize + memoryCost > memorySizeForCompaction
        || !compactionMemoryCost.compareAndSet(originSize, originSize + memoryCost)) {
      Thread.sleep(100);
      originSize = this.compactionMemoryCost.get();
    }
```

### RuleId[ruleID=BusyWait]
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `server/src/main/java/org/apache/iotdb/db/service/NewIoTDB.java`
#### Snippet
```java
    while (!StorageEngineV2.getInstance().isAllSgReady()) {
      try {
        Thread.sleep(1000);
      } catch (InterruptedException e) {
        logger.warn("IoTDB failed to set up.", e);
```

### RuleId[ruleID=BusyWait]
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `server/src/main/java/org/apache/iotdb/db/service/DataNode.java`
#### Snippet
```java
    while (!StorageEngineV2.getInstance().isAllSgReady()) {
      try {
        Thread.sleep(1000);
      } catch (InterruptedException e) {
        logger.warn("IoTDB DataNode failed to set up.", e);
```

### RuleId[ruleID=BusyWait]
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `example/flink/src/main/java/org/apache/iotdb/flink/FlinkIoTDBSink.java`
#### Snippet
```java

        context.collect(tuple);
        Thread.sleep(1000);
      }
    }
```

### RuleId[ruleID=BusyWait]
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `integration-test/src/main/java/org/apache/iotdb/it/env/AbstractNodeWrapper.java`
#### Snippet
```java
    while (this.instance.isAlive()) {
      try {
        Thread.sleep(100);
      } catch (InterruptedException e) {
        logger.error("Waiting node to shutdown." + e);
```

### RuleId[ruleID=BusyWait]
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `consensus/src/main/java/org/apache/iotdb/consensus/ratis/RatisConsensus.java`
#### Snippet
```java
    try {
      while (divisionInfo.isLeader() && !divisionInfo.isLeaderReady()) {
        Thread.sleep(10);
        long consumedTime = System.currentTimeMillis() - startTime;
        if (consumedTime >= DEFAULT_WAIT_LEADER_READY_TIMEOUT) {
```

### RuleId[ruleID=BusyWait]
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `consensus/src/main/java/org/apache/iotdb/consensus/multileader/logdispatcher/LogDispatcher.java`
#### Snippet
```java
              // If write pressure is low, we simply sleep a little to reduce the number of RPC
              if (pendingRequest.size() <= config.getReplication().getMaxRequestNumPerBatch()) {
                Thread.sleep(config.getReplication().getMaxWaitingTimeForAccumulatingBatchInMs());
              }
            }
```

### RuleId[ruleID=BusyWait]
Call to `Thread.sleep()` in a loop, probably busy-waiting
in `consensus/src/main/java/org/apache/iotdb/consensus/multileader/MultiLeaderServerImpl.java`
#### Snippet
```java
            res.searchIndex,
            res.safeIndex);
        Thread.sleep(checkIntervalInMs);
      }
    } catch (IOException | TException e) {
```

## RuleId[ruleID=WaitNotInLoop]
### RuleId[ruleID=WaitNotInLoop]
Call to `wait()` is not in loop
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/impl/statemachine/RegionMigrateProcedure.java`
#### Snippet
```java
      try {
        // TODO set timeOut?
        regionMigrateLock.wait();

        if (!migrateSuccess) {
```

## RuleId[ruleID=UseBulkOperation]
### RuleId[ruleID=UseBulkOperation]
Iteration can be replaced with bulk 'Collection.addAll()' call
in `server/src/main/java/org/apache/iotdb/db/localconfignode/LocalConfigNode.java`
#### Snippet
```java
      }
      for (String roleN : user.getRoleList()) {
        roleList.add(roleN);
      }
    }
```

### RuleId[ruleID=UseBulkOperation]
Iteration can be replaced with bulk 'Collection.addAll()' call
in `confignode/src/main/java/org/apache/iotdb/confignode/persistence/AuthorInfo.java`
#### Snippet
```java
      }
      for (String roleN : user.getRoleList()) {
        roleList.add(roleN);
      }
    }
```

### RuleId[ruleID=UseBulkOperation]
Iteration can be replaced with bulk 'Collection.addAll()' call
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/StringContainer.java`
#### Snippet
```java
    StringContainer ret = new StringContainer(joinSeparator);
    for (String s : sequenceList) {
      ret.sequenceList.add(s);
    }
    for (String s : reverseList) {
```

### RuleId[ruleID=UseBulkOperation]
Iteration can be replaced with bulk 'Collection.addAll()' call
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/StringContainer.java`
#### Snippet
```java
    }
    for (String s : reverseList) {
      ret.reverseList.add(s);
    }
    ret.totalLength = totalLength;
```

## RuleId[ruleID=UnstableApiUsage]
### RuleId[ruleID=UnstableApiUsage]
'io.airlift.stats.CounterStat' is marked unstable with @Beta
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/fragment/FragmentInstanceExecution.java`
#### Snippet
```java
      IDriver driver,
      FragmentInstanceStateMachine stateMachine,
      CounterStat failedInstances,
      long timeOut) {
    FragmentInstanceExecution execution =
```

### RuleId[ruleID=UnstableApiUsage]
'io.airlift.stats.CounterStat' is marked unstable with @Beta
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/fragment/FragmentInstanceExecution.java`
#### Snippet
```java

  // this is a separate method to ensure that the `this` reference is not leaked during construction
  private void initialize(CounterStat failedInstances, IDriverScheduler scheduler) {
    requireNonNull(failedInstances, "failedInstances is null");
    stateMachine.addStateChangeListener(
```

### RuleId[ruleID=UnstableApiUsage]
'update(long)' is declared in unstable class 'io.airlift.stats.CounterStat' marked with @Beta
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/fragment/FragmentInstanceExecution.java`
#### Snippet
```java
            // Update failed tasks counter
            if (newState == FAILED) {
              failedInstances.update(1);
            }

```

### RuleId[ruleID=UnstableApiUsage]
'io.airlift.stats.CounterStat' is marked unstable with @Beta
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/fragment/FragmentInstanceManager.java`
#### Snippet
```java

  // record failed instances count
  private final CounterStat failedInstances = new CounterStat();

  private static final long QUERY_TIMEOUT_MS =
```

### RuleId[ruleID=UnstableApiUsage]
'CounterStat()' is declared in unstable class 'io.airlift.stats.CounterStat' marked with @Beta
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/fragment/FragmentInstanceManager.java`
#### Snippet
```java

  // record failed instances count
  private final CounterStat failedInstances = new CounterStat();

  private static final long QUERY_TIMEOUT_MS =
```

### RuleId[ruleID=UnstableApiUsage]
'io.airlift.stats.CounterStat' is marked unstable with @Beta
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/fragment/FragmentInstanceManager.java`
#### Snippet
```java

  // record failed instances count
  private final CounterStat failedInstances = new CounterStat();

  private static final long QUERY_TIMEOUT_MS =
```

### RuleId[ruleID=UnstableApiUsage]
'io.airlift.stats.CounterStat' is marked unstable with @Beta
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/fragment/FragmentInstanceManager.java`
#### Snippet
```java
  }

  public CounterStat getFailedInstances() {
    return failedInstances;
  }
```

### RuleId[ruleID=UnstableApiUsage]
'successfulAsList(java.lang.Iterable\>)' is marked unstable with @Beta
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/schema/SchemaQueryOrderByHeatOperator.java`
#### Snippet
```java
      }
    }
    return listenableFutureList.isEmpty() ? NOT_BLOCKED : successfulAsList(listenableFutureList);
  }

```

### RuleId[ruleID=UnstableApiUsage]
'successfulAsList(java.lang.Iterable\>)' is marked unstable with @Beta
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/schema/CountMergeOperator.java`
#### Snippet
```java
    }

    return listenableFutureList.isEmpty() ? NOT_BLOCKED : successfulAsList(listenableFutureList);
  }

```

### RuleId[ruleID=UnstableApiUsage]
'successfulAsList(java.lang.Iterable\>)' is marked unstable with @Beta
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/DeviceMergeOperator.java`
#### Snippet
```java
      }
    }
    return listenableFutures.isEmpty() ? NOT_BLOCKED : successfulAsList(listenableFutures);
  }

```

### RuleId[ruleID=UnstableApiUsage]
'successfulAsList(java.lang.Iterable\>)' is marked unstable with @Beta
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/TagAggregationOperator.java`
#### Snippet
```java
      }
    }
    return listenableFutures.isEmpty() ? NOT_BLOCKED : Futures.successfulAsList(listenableFutures);
  }

```

### RuleId[ruleID=UnstableApiUsage]
'successfulAsList(java.lang.Iterable\>)' is marked unstable with @Beta
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/AggregationOperator.java`
#### Snippet
```java
      }
    }
    return listenableFutures.isEmpty() ? NOT_BLOCKED : successfulAsList(listenableFutures);
  }

```

### RuleId[ruleID=UnstableApiUsage]
'successfulAsList(java.lang.Iterable\>)' is marked unstable with @Beta
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/last/LastQueryOperator.java`
#### Snippet
```java
        }
      }
      return listenableFutures.isEmpty() ? NOT_BLOCKED : successfulAsList(listenableFutures);
    } else {
      return Futures.immediateVoidFuture();
```

### RuleId[ruleID=UnstableApiUsage]
'successfulAsList(java.lang.Iterable\>)' is marked unstable with @Beta
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/last/LastQuerySortOperator.java`
#### Snippet
```java
        }
      }
      return listenableFutures.isEmpty() ? NOT_BLOCKED : successfulAsList(listenableFutures);
    } else {
      return Futures.immediateVoidFuture();
```

### RuleId[ruleID=UnstableApiUsage]
'successfulAsList(java.lang.Iterable\>)' is marked unstable with @Beta
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/join/RowBasedTimeJoinOperator.java`
#### Snippet
```java
      }
    }
    return listenableFutures.isEmpty() ? NOT_BLOCKED : successfulAsList(listenableFutures);
  }

```

### RuleId[ruleID=UnstableApiUsage]
'successfulAsList(java.lang.Iterable\>)' is marked unstable with @Beta
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/join/TimeJoinOperator.java`
#### Snippet
```java
      }
    }
    return listenableFutures.isEmpty() ? NOT_BLOCKED : successfulAsList(listenableFutures);
  }

```

### RuleId[ruleID=UnstableApiUsage]
'successfulAsList(java.lang.Iterable\>)' is marked unstable with @Beta
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/last/LastQueryMergeOperator.java`
#### Snippet
```java
      }
    }
    return listenableFutures.isEmpty() ? NOT_BLOCKED : successfulAsList(listenableFutures);
  }

```

### RuleId[ruleID=UnstableApiUsage]
'com.google.common.collect.MinMaxPriorityQueue' is marked unstable with @Beta
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/FixedPriorityBlockingQueue.java`
#### Snippet
```java
  private int maxSize;
  private Comparator<T> comparator;
  private MinMaxPriorityQueue<T> queue;

  private ReentrantLock lock = new ReentrantLock();
```

### RuleId[ruleID=UnstableApiUsage]
'pollLast()' is declared in unstable class 'com.google.common.collect.MinMaxPriorityQueue' marked with @Beta
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/FixedPriorityBlockingQueue.java`
#### Snippet
```java
    T element = null;
    try {
      element = queue.pollLast();
    } finally {
      lock.unlock();
```

### RuleId[ruleID=UnstableApiUsage]
'size()' is declared in unstable class 'com.google.common.collect.MinMaxPriorityQueue' marked with @Beta
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/FixedPriorityBlockingQueue.java`
#### Snippet
```java
    lock.lock();
    try {
      while (queue.size() != 0) {
        this.pollLast();
      }
```

### RuleId[ruleID=UnstableApiUsage]
'size()' is declared in unstable class 'com.google.common.collect.MinMaxPriorityQueue' marked with @Beta
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/FixedPriorityBlockingQueue.java`
#### Snippet
```java
    lock.lockInterruptibly();
    try {
      while (queue.size() == 0) {
        notEmpty.await();
      }
```

### RuleId[ruleID=UnstableApiUsage]
'pollLast()' is declared in unstable class 'com.google.common.collect.MinMaxPriorityQueue' marked with @Beta
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/FixedPriorityBlockingQueue.java`
#### Snippet
```java
        notEmpty.await();
      }
      return queue.pollLast();
    } finally {
      this.lock.unlock();
```

### RuleId[ruleID=UnstableApiUsage]
'size()' is declared in unstable class 'com.google.common.collect.MinMaxPriorityQueue' marked with @Beta
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/FixedPriorityBlockingQueue.java`
#### Snippet
```java
    lock.lockInterruptibly();
    try {
      while (queue.size() == 0) {
        notEmpty.await();
      }
```

### RuleId[ruleID=UnstableApiUsage]
'pollFirst()' is declared in unstable class 'com.google.common.collect.MinMaxPriorityQueue' marked with @Beta
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/FixedPriorityBlockingQueue.java`
#### Snippet
```java
        notEmpty.await();
      }
      return queue.pollFirst();
    } finally {
      this.lock.unlock();
```

### RuleId[ruleID=UnstableApiUsage]
'add(E)' is declared in unstable class 'com.google.common.collect.MinMaxPriorityQueue' marked with @Beta
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/FixedPriorityBlockingQueue.java`
#### Snippet
```java
    lock.lockInterruptibly();
    try {
      queue.add(element);
      if (queue.size() > maxSize) {
        this.pollLast();
```

### RuleId[ruleID=UnstableApiUsage]
'size()' is declared in unstable class 'com.google.common.collect.MinMaxPriorityQueue' marked with @Beta
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/FixedPriorityBlockingQueue.java`
#### Snippet
```java
    try {
      queue.add(element);
      if (queue.size() > maxSize) {
        this.pollLast();
      }
```

### RuleId[ruleID=UnstableApiUsage]
'size()' is declared in unstable class 'com.google.common.collect.MinMaxPriorityQueue' marked with @Beta
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/FixedPriorityBlockingQueue.java`
#### Snippet
```java
    lock.lock();
    try {
      return queue.size();
    } finally {
      lock.unlock();
```

### RuleId[ruleID=UnstableApiUsage]
'com.google.common.collect.MinMaxPriorityQueue' is marked unstable with @Beta
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/FixedPriorityBlockingQueue.java`
#### Snippet
```java
    this.maxSize = maxSize;
    this.comparator = comparator;
    this.queue = MinMaxPriorityQueue.orderedBy(comparator).maximumSize(maxSize + 1).create();
  }

```

### RuleId[ruleID=UnstableApiUsage]
'orderedBy(java.util.Comparator**)' is declared in unstable class 'com.google.common.collect.MinMaxPriorityQueue' marked with @Beta**
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/FixedPriorityBlockingQueue.java`
#### Snippet
```java
    this.maxSize = maxSize;
    this.comparator = comparator;
    this.queue = MinMaxPriorityQueue.orderedBy(comparator).maximumSize(maxSize + 1).create();
  }

```

### RuleId[ruleID=UnstableApiUsage]
'maximumSize(int)' is declared in unstable class 'com.google.common.collect.MinMaxPriorityQueue.Builder' marked with @Beta
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/FixedPriorityBlockingQueue.java`
#### Snippet
```java
    this.maxSize = maxSize;
    this.comparator = comparator;
    this.queue = MinMaxPriorityQueue.orderedBy(comparator).maximumSize(maxSize + 1).create();
  }

```

### RuleId[ruleID=UnstableApiUsage]
'create()' is declared in unstable class 'com.google.common.collect.MinMaxPriorityQueue.Builder' marked with @Beta
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/FixedPriorityBlockingQueue.java`
#### Snippet
```java
    this.maxSize = maxSize;
    this.comparator = comparator;
    this.queue = MinMaxPriorityQueue.orderedBy(comparator).maximumSize(maxSize + 1).create();
  }

```

### RuleId[ruleID=UnstableApiUsage]
'getRate()' is declared in unstable class 'com.google.common.util.concurrent.RateLimiter' marked with @Beta
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/CompactionTaskManager.java`
#### Snippet
```java
      throughout = Double.MAX_VALUE;
    }
    if (mergeWriteRateLimiter.getRate() != throughout) {
      mergeWriteRateLimiter.setRate(throughout);
    }
```

### RuleId[ruleID=UnstableApiUsage]
'setRate(double)' is declared in unstable class 'com.google.common.util.concurrent.RateLimiter' marked with @Beta
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/CompactionTaskManager.java`
#### Snippet
```java
    }
    if (mergeWriteRateLimiter.getRate() != throughout) {
      mergeWriteRateLimiter.setRate(throughout);
    }
  }
```

### RuleId[ruleID=UnstableApiUsage]
'com.google.common.util.concurrent.RateLimiter' is marked unstable with @Beta
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/CompactionTaskManager.java`
#### Snippet
```java
  }

  public RateLimiter getMergeWriteRateLimiter() {
    setWriteMergeRate(
        IoTDBDescriptor.getInstance().getConfig().getCompactionWriteThroughputMbPerSec());
```

### RuleId[ruleID=UnstableApiUsage]
'com.google.common.util.concurrent.RateLimiter' is marked unstable with @Beta
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/CompactionTaskManager.java`
#### Snippet
```java
  }
  /** wait by throughoutMbPerSec limit to avoid continuous Write Or Read */
  public static void mergeRateLimiterAcquire(RateLimiter limiter, long bytesLength) {
    while (bytesLength >= Integer.MAX_VALUE) {
      limiter.acquire(Integer.MAX_VALUE);
```

### RuleId[ruleID=UnstableApiUsage]
'acquire(int)' is declared in unstable class 'com.google.common.util.concurrent.RateLimiter' marked with @Beta
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/CompactionTaskManager.java`
#### Snippet
```java
  public static void mergeRateLimiterAcquire(RateLimiter limiter, long bytesLength) {
    while (bytesLength >= Integer.MAX_VALUE) {
      limiter.acquire(Integer.MAX_VALUE);
      bytesLength -= Integer.MAX_VALUE;
    }
```

### RuleId[ruleID=UnstableApiUsage]
'acquire(int)' is declared in unstable class 'com.google.common.util.concurrent.RateLimiter' marked with @Beta
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/CompactionTaskManager.java`
#### Snippet
```java
    }
    if (bytesLength > 0) {
      limiter.acquire((int) bytesLength);
    }
  }
```

### RuleId[ruleID=UnstableApiUsage]
'com.google.common.util.concurrent.RateLimiter' is marked unstable with @Beta
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/CompactionTaskManager.java`
#### Snippet
```java
  private final AtomicInteger finishedTaskNum = new AtomicInteger(0);

  private final RateLimiter mergeWriteRateLimiter = RateLimiter.create(Double.MAX_VALUE);

  private final IoTDBConfig config = IoTDBDescriptor.getInstance().getConfig();
```

### RuleId[ruleID=UnstableApiUsage]
'com.google.common.util.concurrent.RateLimiter' is marked unstable with @Beta
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/CompactionTaskManager.java`
#### Snippet
```java
  private final AtomicInteger finishedTaskNum = new AtomicInteger(0);

  private final RateLimiter mergeWriteRateLimiter = RateLimiter.create(Double.MAX_VALUE);

  private final IoTDBConfig config = IoTDBDescriptor.getInstance().getConfig();
```

### RuleId[ruleID=UnstableApiUsage]
'create(double)' is declared in unstable class 'com.google.common.util.concurrent.RateLimiter' marked with @Beta
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/CompactionTaskManager.java`
#### Snippet
```java
  private final AtomicInteger finishedTaskNum = new AtomicInteger(0);

  private final RateLimiter mergeWriteRateLimiter = RateLimiter.create(Double.MAX_VALUE);

  private final IoTDBConfig config = IoTDBDescriptor.getInstance().getConfig();
```

### RuleId[ruleID=UnstableApiUsage]
'com.google.common.util.concurrent.RateLimiter' is marked unstable with @Beta
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/inner/utils/AlignedSeriesCompactionExecutor.java`
#### Snippet
```java
  private final List<IMeasurementSchema> schemaList;
  private long remainingPointInChunkWriter = 0L;
  private final RateLimiter rateLimiter =
      CompactionTaskManager.getInstance().getMergeWriteRateLimiter();

```

### RuleId[ruleID=UnstableApiUsage]
'com.google.common.util.concurrent.RateLimiter' is marked unstable with @Beta
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/inner/utils/SingleSeriesCompactionExecutor.java`
#### Snippet
```java
  private Chunk cachedChunk;
  private ChunkMetadata cachedChunkMetadata;
  private RateLimiter compactionRateLimiter =
      CompactionTaskManager.getInstance().getMergeWriteRateLimiter();
  // record the min time and max time to update the target resource
```

### RuleId[ruleID=UnstableApiUsage]
'com.google.common.math.Quantiles' is marked unstable with @Beta
in `library-udf/src/main/java/org/apache/iotdb/library/anomaly/UDTFIQR.java`
#### Snippet
```java
  public void terminate(PointCollector collector) throws Exception {
    if (compute.equalsIgnoreCase("batch")) {
      q1 = Quantiles.quartiles().index(1).compute(value);
      q3 = Quantiles.quartiles().index(3).compute(value);
      iqr = q3 - q1;
```

### RuleId[ruleID=UnstableApiUsage]
'quartiles()' is declared in unstable class 'com.google.common.math.Quantiles' marked with @Beta
in `library-udf/src/main/java/org/apache/iotdb/library/anomaly/UDTFIQR.java`
#### Snippet
```java
  public void terminate(PointCollector collector) throws Exception {
    if (compute.equalsIgnoreCase("batch")) {
      q1 = Quantiles.quartiles().index(1).compute(value);
      q3 = Quantiles.quartiles().index(3).compute(value);
      iqr = q3 - q1;
```

### RuleId[ruleID=UnstableApiUsage]
'index(int)' is declared in unstable class 'com.google.common.math.Quantiles' marked with @Beta
in `library-udf/src/main/java/org/apache/iotdb/library/anomaly/UDTFIQR.java`
#### Snippet
```java
  public void terminate(PointCollector collector) throws Exception {
    if (compute.equalsIgnoreCase("batch")) {
      q1 = Quantiles.quartiles().index(1).compute(value);
      q3 = Quantiles.quartiles().index(3).compute(value);
      iqr = q3 - q1;
```

### RuleId[ruleID=UnstableApiUsage]
'compute(java.util.Collection)' is declared in unstable class 'com.google.common.math.Quantiles' marked with @Beta
in `library-udf/src/main/java/org/apache/iotdb/library/anomaly/UDTFIQR.java`
#### Snippet
```java
  public void terminate(PointCollector collector) throws Exception {
    if (compute.equalsIgnoreCase("batch")) {
      q1 = Quantiles.quartiles().index(1).compute(value);
      q3 = Quantiles.quartiles().index(3).compute(value);
      iqr = q3 - q1;
```

### RuleId[ruleID=UnstableApiUsage]
'com.google.common.math.Quantiles' is marked unstable with @Beta
in `library-udf/src/main/java/org/apache/iotdb/library/anomaly/UDTFIQR.java`
#### Snippet
```java
    if (compute.equalsIgnoreCase("batch")) {
      q1 = Quantiles.quartiles().index(1).compute(value);
      q3 = Quantiles.quartiles().index(3).compute(value);
      iqr = q3 - q1;
    }
```

### RuleId[ruleID=UnstableApiUsage]
'quartiles()' is declared in unstable class 'com.google.common.math.Quantiles' marked with @Beta
in `library-udf/src/main/java/org/apache/iotdb/library/anomaly/UDTFIQR.java`
#### Snippet
```java
    if (compute.equalsIgnoreCase("batch")) {
      q1 = Quantiles.quartiles().index(1).compute(value);
      q3 = Quantiles.quartiles().index(3).compute(value);
      iqr = q3 - q1;
    }
```

### RuleId[ruleID=UnstableApiUsage]
'index(int)' is declared in unstable class 'com.google.common.math.Quantiles' marked with @Beta
in `library-udf/src/main/java/org/apache/iotdb/library/anomaly/UDTFIQR.java`
#### Snippet
```java
    if (compute.equalsIgnoreCase("batch")) {
      q1 = Quantiles.quartiles().index(1).compute(value);
      q3 = Quantiles.quartiles().index(3).compute(value);
      iqr = q3 - q1;
    }
```

### RuleId[ruleID=UnstableApiUsage]
'compute(java.util.Collection)' is declared in unstable class 'com.google.common.math.Quantiles' marked with @Beta
in `library-udf/src/main/java/org/apache/iotdb/library/anomaly/UDTFIQR.java`
#### Snippet
```java
    if (compute.equalsIgnoreCase("batch")) {
      q1 = Quantiles.quartiles().index(1).compute(value);
      q3 = Quantiles.quartiles().index(3).compute(value);
      iqr = q3 - q1;
    }
```

## RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (newStatus) { case ReadOnly: logger.error( "Change system status to...` statement on enum type 'org.apache.iotdb.commons.cluster.NodeStatus' misses cases: 'Running', and 'Unknown'
in `node-commons/src/main/java/org/apache/iotdb/commons/conf/CommonConfig.java`
#### Snippet
```java
    this.statusReason = null;

    switch (newStatus) {
      case ReadOnly:
        logger.error(
            "Change system status to ReadOnly! Only query statements are permitted!",
            new RuntimeException("System mode is set to READ_ONLY"));
        break;
      case Removing:
        logger.info(
            "Change system status to Removing! The current Node is being removed from cluster!");
        break;
    }
  }

```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (pipeInfo.getStatus()) { case RUNNING: writer.operatePipe(pipeInfo.getP...` statement on enum type 'org.apache.iotdb.commons.sync.pipe.PipeStatus' misses cases: 'DROP', 'PARTIAL_CREATE', 'PARTIAL_START', ...
in `node-commons/src/main/java/org/apache/iotdb/commons/sync/metadata/SyncMetadata.java`
#### Snippet
```java
      for (PipeInfo pipeInfo : pipes.values()) {
        writer.addPipe(pipeInfo);
        switch (pipeInfo.getStatus()) {
          case RUNNING:
            writer.operatePipe(pipeInfo.getPipeName(), SyncOperation.START_PIPE);
            break;
          case STOP:
            writer.operatePipe(pipeInfo.getPipeName(), SyncOperation.STOP_PIPE);
            break;
        }
      }
    }
```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (dataType) { case DOUBLE: deserializeDoubleTVPairs(buffer, ret, size, dataType)...` statement on enum type 'org.apache.iotdb.tsfile.file.metadata.enums.TSDataType' misses case 'VECTOR'
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/SerializeUtils.java`
#### Snippet
```java
    int size = buffer.getInt();
    List<TimeValuePair> ret = new ArrayList<>(size);
    switch (dataType) {
      case DOUBLE:
        deserializeDoubleTVPairs(buffer, ret, size, dataType);
        break;
      case FLOAT:
        deserializeFloatTVPairs(buffer, ret, size, dataType);
        break;
      case INT32:
        deserializeIntTVPairs(buffer, ret, size, dataType);
        break;
      case INT64:
        deserializeLongTVPairs(buffer, ret, size, dataType);
        break;
      case BOOLEAN:
        deserializeBooleanTVPairs(buffer, ret, size, dataType);
        break;
      case TEXT:
        deserializeTextTVPairs(buffer, ret, size, dataType);
        break;
    }
    return ret;
  }
```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (TSDataType.values()[buffer.get()]) { case BOOLEAN: values[...` statement on enum type 'org.apache.iotdb.tsfile.file.metadata.enums.TSDataType' misses case 'VECTOR'
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/SerializeUtils.java`
#### Snippet
```java
            boolean notNull = (buffer.get() == 1);
            if (notNull) {
              switch (TSDataType.values()[buffer.get()]) {
                case BOOLEAN:
                  values[j] = new TsPrimitiveType.TsBoolean(buffer.get() == 1);
                  break;
                case DOUBLE:
                  values[j] = new TsPrimitiveType.TsDouble(buffer.getDouble());
                  break;
                case FLOAT:
                  values[j] = new TsPrimitiveType.TsFloat(buffer.getFloat());
                  break;
                case TEXT:
                  int len = buffer.getInt();
                  byte[] bytes = new byte[len];
                  buffer.get(bytes);
                  values[j] = new TsPrimitiveType.TsBinary(new Binary(bytes));
                  break;
                case INT64:
                  values[j] = new TsPrimitiveType.TsLong(buffer.getLong());
                  break;
                case INT32:
                  values[j] = new TsPrimitiveType.TsInt(buffer.getInt());
                  break;
              }
            }
          }
```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (dataType) { case DOUBLE: return new TimeValuePair(time, TsPrimitiveType.getByT...` statement on enum type 'org.apache.iotdb.tsfile.file.metadata.enums.TSDataType' misses case 'VECTOR'
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/SerializeUtils.java`
#### Snippet
```java
      return new TimeValuePair(time, null);
    }
    switch (dataType) {
      case DOUBLE:
        return new TimeValuePair(time, TsPrimitiveType.getByType(dataType, buffer.getDouble()));
      case FLOAT:
        return new TimeValuePair(time, TsPrimitiveType.getByType(dataType, buffer.getFloat()));
      case INT32:
        return new TimeValuePair(time, TsPrimitiveType.getByType(dataType, buffer.getInt()));
      case INT64:
        return new TimeValuePair(time, TsPrimitiveType.getByType(dataType, buffer.getLong()));
      case BOOLEAN:
        return new TimeValuePair(time, TsPrimitiveType.getByType(dataType, buffer.get() == 1));
      case TEXT:
        int bytesLen = buffer.getInt();
        byte[] bytes = new byte[bytesLen];
        buffer.get(bytes);
        TsPrimitiveType primitiveType = TsPrimitiveType.getByType(dataType, new Binary(bytes));
        return new TimeValuePair(time, primitiveType);
    }
    return null;
  }
```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (dataType) { case TEXT: dataOutputStream.writeLong(timeValuePair.getTimesta...` statement on enum type 'org.apache.iotdb.tsfile.file.metadata.enums.TSDataType' misses case 'VECTOR'
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/SerializeUtils.java`
#### Snippet
```java
    try {
      dataOutputStream.write(dataType.ordinal());
      switch (dataType) {
        case TEXT:
          dataOutputStream.writeLong(timeValuePair.getTimestamp());
          if (timeValuePair.getTimestamp() != Long.MIN_VALUE) {
            dataOutputStream.writeInt(timeValuePair.getValue().getBinary().getLength());
            dataOutputStream.write(timeValuePair.getValue().getBinary().getValues());
          }
          break;
        case BOOLEAN:
          dataOutputStream.writeLong(timeValuePair.getTimestamp());
          if (timeValuePair.getTimestamp() != Long.MIN_VALUE) {
            dataOutputStream.writeBoolean(timeValuePair.getValue().getBoolean());
          }
          break;
        case INT64:
          dataOutputStream.writeLong(timeValuePair.getTimestamp());
          if (timeValuePair.getTimestamp() != Long.MIN_VALUE) {
            dataOutputStream.writeLong(timeValuePair.getValue().getLong());
          }
          break;
        case INT32:
          dataOutputStream.writeLong(timeValuePair.getTimestamp());
          if (timeValuePair.getTimestamp() != Long.MIN_VALUE) {
            dataOutputStream.writeInt(timeValuePair.getValue().getInt());
          }
          break;
        case FLOAT:
          dataOutputStream.writeLong(timeValuePair.getTimestamp());
          if (timeValuePair.getTimestamp() != Long.MIN_VALUE) {
            dataOutputStream.writeFloat(timeValuePair.getValue().getFloat());
          }
          break;
        case DOUBLE:
          dataOutputStream.writeLong(timeValuePair.getTimestamp());
          if (timeValuePair.getTimestamp() != Long.MIN_VALUE) {
            dataOutputStream.writeDouble(timeValuePair.getValue().getDouble());
          }
      }
    } catch (IOException e) {
      // unreachable
```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (timeValuePairs.get(0).getValue().getDataType()) { case TEXT: serializeText...` statement on enum type 'org.apache.iotdb.tsfile.file.metadata.enums.TSDataType' misses case 'VECTOR'
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/SerializeUtils.java`
#### Snippet
```java
      dataOutputStream.write(dataType.ordinal());
      dataOutputStream.writeInt(timeValuePairs.size());
      switch (timeValuePairs.get(0).getValue().getDataType()) {
        case TEXT:
          serializeTextTVPairs(timeValuePairs, dataOutputStream);
          break;
        case BOOLEAN:
          serializeBooleanTVPairs(timeValuePairs, dataOutputStream);
          break;
        case INT64:
          serializeLongTVPairs(timeValuePairs, dataOutputStream);
          break;
        case INT32:
          serializeIntTVPairs(timeValuePairs, dataOutputStream);
          break;
        case FLOAT:
          serializeFloatTVPairs(timeValuePairs, dataOutputStream);
          break;
        case DOUBLE:
          serializeDoubleTVPairs(timeValuePairs, dataOutputStream);
      }
    } catch (IOException ignored) {
      // unreachable
```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (types.get(i)) { case INT64: values.add(Long.parseLong(valuesStr[i])); ...` statement on enum type 'org.apache.iotdb.tsfile.file.metadata.enums.TSDataType' misses case 'VECTOR'
in `example/pulsar/src/main/java/org/apache/iotdb/pulsar/PulsarConsumerThread.java`
#### Snippet
```java
      String[] valuesStr = dataArray[4].split(":");
      for (int i = 0; i < valuesStr.length; i++) {
        switch (types.get(i)) {
          case INT64:
            values.add(Long.parseLong(valuesStr[i]));
            break;
          case DOUBLE:
            values.add(Double.parseDouble(valuesStr[i]));
            break;
          case INT32:
            values.add(Integer.parseInt(valuesStr[i]));
            break;
          case TEXT:
            values.add(valuesStr[i]);
            break;
          case FLOAT:
            values.add(Float.parseFloat(valuesStr[i]));
            break;
          case BOOLEAN:
            values.add(Boolean.parseBoolean(valuesStr[i]));
            break;
        }
      }
      deviceIds.add(device);
```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (types.get(i)) { case INT64: values.add(Long.parseLong(valuesStr[i])); ...` statement on enum type 'org.apache.iotdb.tsfile.file.metadata.enums.TSDataType' misses case 'VECTOR'
in `example/pulsar/src/main/java/org/apache/iotdb/pulsar/PulsarConsumerThread.java`
#### Snippet
```java
    String[] valuesStr = dataArray[4].split(":");
    for (int i = 0; i < valuesStr.length; i++) {
      switch (types.get(i)) {
        case INT64:
          values.add(Long.parseLong(valuesStr[i]));
          break;
        case DOUBLE:
          values.add(Double.parseDouble(valuesStr[i]));
          break;
        case INT32:
          values.add(Integer.parseInt(valuesStr[i]));
          break;
        case TEXT:
          values.add(valuesStr[i]);
          break;
        case FLOAT:
          values.add(Float.parseFloat(valuesStr[i]));
          break;
        case BOOLEAN:
          values.add(Boolean.parseBoolean(valuesStr[i]));
          break;
      }
    }

```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (types.get(i)) { case INT64: values.add(Long.parseLong(valuesStr[i])); ...` statement on enum type 'org.apache.iotdb.tsfile.file.metadata.enums.TSDataType' misses case 'VECTOR'
in `example/kafka/src/main/java/org/apache/iotdb/kafka/ConsumerThread.java`
#### Snippet
```java
      String[] valuesStr = dataArray[4].split(":");
      for (int i = 0; i < valuesStr.length; i++) {
        switch (types.get(i)) {
          case INT64:
            values.add(Long.parseLong(valuesStr[i]));
            break;
          case DOUBLE:
            values.add(Double.parseDouble(valuesStr[i]));
            break;
          case INT32:
            values.add(Integer.parseInt(valuesStr[i]));
            break;
          case TEXT:
            values.add(valuesStr[i]);
            break;
          case FLOAT:
            values.add(Float.parseFloat(valuesStr[i]));
            break;
          case BOOLEAN:
            values.add(Boolean.parseBoolean(valuesStr[i]));
            break;
        }
      }
      deviceIds.add(device);
```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (types.get(i)) { case INT64: values.add(Long.parseLong(valuesStr[i])); ...` statement on enum type 'org.apache.iotdb.tsfile.file.metadata.enums.TSDataType' misses case 'VECTOR'
in `example/kafka/src/main/java/org/apache/iotdb/kafka/ConsumerThread.java`
#### Snippet
```java
    String[] valuesStr = dataArray[4].split(":");
    for (int i = 0; i < valuesStr.length; i++) {
      switch (types.get(i)) {
        case INT64:
          values.add(Long.parseLong(valuesStr[i]));
          break;
        case DOUBLE:
          values.add(Double.parseDouble(valuesStr[i]));
          break;
        case INT32:
          values.add(Integer.parseInt(valuesStr[i]));
          break;
        case TEXT:
          values.add(valuesStr[i]);
          break;
        case FLOAT:
          values.add(Float.parseFloat(valuesStr[i]));
          break;
        case BOOLEAN:
          values.add(Boolean.parseBoolean(valuesStr[i]));
          break;
      }
    }

```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (columnType) { case TEXT: field.setBinaryV(new Binary(val.toString()));...` statement on enum type 'org.apache.iotdb.tsfile.file.metadata.enums.TSDataType' misses case 'VECTOR'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
        org.apache.iotdb.tsfile.read.common.Field field =
            new org.apache.iotdb.tsfile.read.common.Field(columnType);
        switch (columnType) {
          case TEXT:
            field.setBinaryV(new Binary(val.toString()));
            break;
          case FLOAT:
            field.setFloatV(((float) val));
            break;
          case INT32:
            field.setIntV(((int) val));
            break;
          case INT64:
            field.setLongV(((long) val));
            break;
          case DOUBLE:
            field.setDoubleV(((double) val));
            break;
          case BOOLEAN:
            field.setBoolV(((boolean) val));
            break;
        }
        record.addField(field);
        if (i == 0) {
```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (columnType) { case TEXT: tsBlockBuilder.getColumnBuilder(j).writeBinar...` statement on enum type 'org.apache.iotdb.tsfile.file.metadata.enums.TSDataType' misses case 'VECTOR'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      for (int j = 0; j < tsDataTypeList.size(); j++) {
        TSDataType columnType = tsDataTypeList.get(j);
        switch (columnType) {
          case TEXT:
            tsBlockBuilder.getColumnBuilder(j).writeBinary((Binary) valuesInRow.get(j));
            break;
          case FLOAT:
            tsBlockBuilder.getColumnBuilder(j).writeFloat((float) valuesInRow.get(j));
            break;
          case INT32:
            tsBlockBuilder.getColumnBuilder(j).writeInt((int) valuesInRow.get(j));
            break;
          case INT64:
            tsBlockBuilder.getColumnBuilder(j).writeLong((long) valuesInRow.get(j));
            break;
          case DOUBLE:
            tsBlockBuilder.getColumnBuilder(j).writeDouble((double) valuesInRow.get(j));
            break;
          case BOOLEAN:
            tsBlockBuilder.getColumnBuilder(j).writeBoolean((boolean) valuesInRow.get(j));
            break;
        }
      }
      tsBlockBuilder.declarePosition();
```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (rowWindow.getDataType(0)) { case INT32: accumulator += rowIterator.next()....` statement on enum type 'org.apache.iotdb.udf.api.type.Type' misses cases: 'BOOLEAN', 'INT64', 'FLOAT', and 'TEXT'
in `integration/src/main/java/org/apache/iotdb/db/query/udf/example/Accumulator.java`
#### Snippet
```java
    RowIterator rowIterator = rowWindow.getRowIterator();
    while (rowIterator.hasNextRow()) {
      switch (rowWindow.getDataType(0)) {
        case INT32:
          accumulator += rowIterator.next().getInt(0);
          break;
        case DOUBLE:
          accumulator += (int) rowIterator.next().getDouble(0);
          break;
      }
    }
    if (rowWindow.windowSize() != 0) {
```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (dataTypes[i]) { case BOOLEAN: size += Byte.BYTES; break; ...` statement on enum type 'org.apache.iotdb.tsfile.file.metadata.enums.TSDataType' misses case 'VECTOR'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/write/InsertRowNode.java`
#### Snippet
```java
      }
      size += Byte.BYTES;
      switch (dataTypes[i]) {
        case BOOLEAN:
          size += Byte.BYTES;
          break;
        case INT32:
          size += Integer.BYTES;
          break;
        case INT64:
          size += Long.BYTES;
          break;
        case FLOAT:
          size += Float.BYTES;
          break;
        case DOUBLE:
          size += Double.BYTES;
          break;
        case TEXT:
          size += ReadWriteIOUtils.sizeToWrite((Binary) values[i]);
          break;
      }
    }

```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (dataTypes[i]) { case TEXT: values[i] = new Binary[rowSize]; brea...` statement on enum type 'org.apache.iotdb.tsfile.file.metadata.enums.TSDataType' misses case 'VECTOR'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/write/InsertTabletNode.java`
#### Snippet
```java
    Object[] values = new Object[columnSize];
    for (int i = 0; i < values.length; i++) {
      switch (dataTypes[i]) {
        case TEXT:
          values[i] = new Binary[rowSize];
          break;
        case FLOAT:
          values[i] = new float[rowSize];
          break;
        case INT32:
          values[i] = new int[rowSize];
          break;
        case INT64:
          values[i] = new long[rowSize];
          break;
        case DOUBLE:
          values[i] = new double[rowSize];
          break;
        case BOOLEAN:
          values[i] = new boolean[rowSize];
          break;
      }
    }
    return values;
```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (dataType) { case INT32: size += Integer.BYTES * (end - start); break; ...` statement on enum type 'org.apache.iotdb.tsfile.file.metadata.enums.TSDataType' misses case 'VECTOR'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/write/InsertTabletNode.java`
#### Snippet
```java
  private int getColumnSize(TSDataType dataType, Object column, int start, int end) {
    int size = 0;
    switch (dataType) {
      case INT32:
        size += Integer.BYTES * (end - start);
        break;
      case INT64:
        size += Long.BYTES * (end - start);
        break;
      case FLOAT:
        size += Float.BYTES * (end - start);
        break;
      case DOUBLE:
        size += Double.BYTES * (end - start);
        break;
      case BOOLEAN:
        size += Byte.BYTES * (end - start);
        break;
      case TEXT:
        Binary[] binaryValues = (Binary[]) column;
        for (int j = start; j < end; j++) {
          size += ReadWriteIOUtils.sizeToWrite(binaryValues[j]);
        }
        break;
    }
    return size;
  }
```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (status) { case SUCCESS: walFlushListener.succeed(); break; case ...` statement on enum type 'org.apache.iotdb.db.wal.utils.listener.AbstractResultListener.Status' misses case 'RUNNING'
in `server/src/main/java/org/apache/iotdb/db/wal/node/WALFakeNode.java`
#### Snippet
```java
  private WALFlushListener getResult() {
    WALFlushListener walFlushListener = new WALFlushListener(false);
    switch (status) {
      case SUCCESS:
        walFlushListener.succeed();
        break;
      case FAILURE:
        walFlushListener.fail(cause);
        break;
    }
    return walFlushListener;
  }
```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (type) { case CLOSE_SIGNAL: case ROLL_WAL_LOG_WRITER_SIGNAL: case WAL_FILE_...` statement on enum type 'org.apache.iotdb.db.wal.buffer.WALEntryType' misses cases: 'INSERT_ROW_PLAN', 'INSERT_TABLET_PLAN', ...
in `server/src/main/java/org/apache/iotdb/db/wal/buffer/WALEntry.java`
#### Snippet
```java

    // handle signal
    switch (type) {
      case CLOSE_SIGNAL:
      case ROLL_WAL_LOG_WRITER_SIGNAL:
      case WAL_FILE_INFO_END_MARKER:
        return new WALSignalEntry(type);
    }

    // handle info
```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (src) { case INT32: if (dest == TSDataType.INT64 || dest == TSDataType.FLOAT ||...` statement on enum type 'org.apache.iotdb.tsfile.file.metadata.enums.TSDataType' misses cases: 'BOOLEAN', 'DOUBLE', 'TEXT', and 'VECTOR'
in `server/src/main/java/org/apache/iotdb/db/utils/CommonUtils.java`
#### Snippet
```java

  public static boolean checkCanCastType(TSDataType src, TSDataType dest) {
    switch (src) {
      case INT32:
        if (dest == TSDataType.INT64 || dest == TSDataType.FLOAT || dest == TSDataType.DOUBLE) {
          return true;
        }
      case INT64:
        if (dest == TSDataType.DOUBLE) {
          return true;
        }
      case FLOAT:
        if (dest == TSDataType.DOUBLE) {
          return true;
        }
    }
    return false;
  }
```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (srcDataType) { case INT32: if (destDataType == TSDataType.INT64) { v...` statement on enum type 'org.apache.iotdb.tsfile.file.metadata.enums.TSDataType' misses cases: 'BOOLEAN', 'DOUBLE', 'TEXT', and 'VECTOR'
in `server/src/main/java/org/apache/iotdb/db/utils/CommonUtils.java`
#### Snippet
```java

  public static Object castArray(TSDataType srcDataType, TSDataType destDataType, Object value) {
    switch (srcDataType) {
      case INT32:
        if (destDataType == TSDataType.INT64) {
          value = Arrays.stream((int[]) value).mapToLong(Long::valueOf).toArray();
        } else if (destDataType == TSDataType.FLOAT) {
          int[] tmp = (int[]) value;
          float[] result = new float[tmp.length];
          for (int i = 0; i < tmp.length; i++) {
            result[i] = (float) tmp[i];
          }
          value = result;
        } else if (destDataType == TSDataType.DOUBLE) {
          value = Arrays.stream((int[]) value).mapToDouble(Double::valueOf).toArray();
        }
        break;
      case INT64:
        if (destDataType == TSDataType.DOUBLE) {
          value = Arrays.stream((long[]) value).mapToDouble(Double::valueOf).toArray();
        }
        break;
      case FLOAT:
        if (destDataType == TSDataType.DOUBLE) {
          float[] tmp = (float[]) value;
          double[] result = new double[tmp.length];
          for (int i = 0; i < tmp.length; i++) {
            result[i] = tmp[i];
          }
          value = result;
        }
        break;
    }
    return value;
  }
```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (srcDataType) { case INT32: if (destDataType == TSDataType.INT64) { v...` statement on enum type 'org.apache.iotdb.tsfile.file.metadata.enums.TSDataType' misses cases: 'BOOLEAN', 'DOUBLE', 'TEXT', and 'VECTOR'
in `server/src/main/java/org/apache/iotdb/db/utils/CommonUtils.java`
#### Snippet
```java

  public static Object castValue(TSDataType srcDataType, TSDataType destDataType, Object value) {
    switch (srcDataType) {
      case INT32:
        if (destDataType == TSDataType.INT64) {
          value = (long) ((int) value);
        } else if (destDataType == TSDataType.FLOAT) {
          value = (float) ((int) value);
        } else if (destDataType == TSDataType.DOUBLE) {
          value = (double) ((int) value);
        }
        break;
      case INT64:
        if (destDataType == TSDataType.DOUBLE) {
          value = (double) ((long) value);
        }
        break;
      case FLOAT:
        if (destDataType == TSDataType.DOUBLE) {
          value = (double) ((float) value);
        }
        break;
    }
    return value;
  }
```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (TemplateQueryType.values()[req.getQueryType()]) { case SHOW_MEASUREMENTS: ...` statement on enum type 'org.apache.iotdb.db.metadata.template.TemplateQueryType' misses cases: 'COUNT_MEASUREMENTS', 'IS_MEASUREMENT', ...
in `server/src/main/java/org/apache/iotdb/db/service/thrift/impl/ClientRPCServiceImpl.java`
#### Snippet
```java
        return resp;
      }
      switch (TemplateQueryType.values()[req.getQueryType()]) {
        case SHOW_MEASUREMENTS:
          resp.setQueryType(TemplateQueryType.SHOW_MEASUREMENTS.ordinal());
          break;
        case SHOW_TEMPLATES:
          resp.setQueryType(TemplateQueryType.SHOW_TEMPLATES.ordinal());
          break;
        case SHOW_SET_TEMPLATES:
          resp.setQueryType(TemplateQueryType.SHOW_SET_TEMPLATES.ordinal());
          break;
        case SHOW_USING_TEMPLATES:
          resp.setQueryType(TemplateQueryType.SHOW_USING_TEMPLATES.ordinal());
          break;
      }
      return executeTemplateQueryStatement(statement, req, resp);
    } catch (Exception e) {
```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (dataType) { case BOOLEAN: ((boolean[]) tablet.values[0])[row] = da...` statement on enum type 'org.apache.iotdb.tsfile.file.metadata.enums.TSDataType' misses case 'VECTOR'
in `example/session/src/main/java/org/apache/iotdb/DataMigrationExample.java`
#### Snippet
```java
          int row = tablet.rowSize++;
          tablet.timestamps[row] = dataIter.getLong(1);
          switch (dataType) {
            case BOOLEAN:
              ((boolean[]) tablet.values[0])[row] = dataIter.getBoolean(2);
              break;
            case INT32:
              ((int[]) tablet.values[0])[row] = dataIter.getInt(2);
              break;
            case INT64:
              ((long[]) tablet.values[0])[row] = dataIter.getLong(2);
              break;
            case FLOAT:
              ((float[]) tablet.values[0])[row] = dataIter.getFloat(2);
              break;
            case DOUBLE:
              ((double[]) tablet.values[0])[row] = dataIter.getDouble(2);
              break;
            case TEXT:
              ((Binary[]) tablet.values[0])[row] = new Binary(dataIter.getString(2));
              break;
          }
          if (tablet.rowSize == tablet.getMaxRowNumber()) {
            writerPool.insertTablet(tablet, true);
```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (dataType) { case INT32: v.push(row.getInt(0)); break; ...` statement on enum type 'org.apache.iotdb.tsfile.file.metadata.enums.TSDataType' misses cases: 'BOOLEAN', 'TEXT', and 'VECTOR'
in `library-udf/src/main/java/org/apache/iotdb/library/anomaly/UDTFKSigma.java`
#### Snippet
```java
      if (v.isFull()) {
        double frontValue = Double.parseDouble(v.pop().toString());
        switch (dataType) {
          case INT32:
            v.push(row.getInt(0));
            break;
          case INT64:
            v.push(row.getLong(0));
            break;
          case DOUBLE:
            v.push(row.getDouble(0));
            break;
          case FLOAT:
            v.push(row.getFloat(0));
            break;
        }
        t.pop();
        t.push(timestamp);
```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (dataType) { case INT32: v.push(row.getInt(0)); break; ...` statement on enum type 'org.apache.iotdb.tsfile.file.metadata.enums.TSDataType' misses cases: 'BOOLEAN', 'TEXT', and 'VECTOR'
in `library-udf/src/main/java/org/apache/iotdb/library/anomaly/UDTFKSigma.java`
#### Snippet
```java
        }
      } else {
        switch (dataType) {
          case INT32:
            v.push(row.getInt(0));
            break;
          case INT64:
            v.push(row.getLong(0));
            break;
          case DOUBLE:
            v.push(row.getDouble(0));
            break;
          case FLOAT:
            v.push(row.getFloat(0));
            break;
        }
        t.push(timestamp);
        this.sumX1 = this.sumX1 + value;
```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (type) { case INT32: pc.putInt(t, (Integer) o); break; case INT64...` statement on enum type 'org.apache.iotdb.tsfile.file.metadata.enums.TSDataType' misses cases: 'TEXT', and 'VECTOR'
in `library-udf/src/main/java/org/apache/iotdb/library/util/Util.java`
#### Snippet
```java
  public static void putValue(PointCollector pc, TSDataType type, long t, Object o)
      throws Exception {
    switch (type) {
      case INT32:
        pc.putInt(t, (Integer) o);
        break;
      case INT64:
        pc.putLong(t, (Long) o);
        break;
      case FLOAT:
        pc.putFloat(t, (Float) o);
        break;
      case DOUBLE:
        pc.putDouble(t, (Double) o);
        break;
      case BOOLEAN:
        pc.putBoolean(t, (Boolean) o);
    }
  }

```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (dataType) { case INT32: collector.putInt(0, (int) res); break; ...` statement on enum type 'org.apache.iotdb.tsfile.file.metadata.enums.TSDataType' misses cases: 'BOOLEAN', 'TEXT', and 'VECTOR'
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/UDAFPercentile.java`
#### Snippet
```java
    } else {
      double res = sketch.query(rank);
      switch (dataType) {
        case INT32:
          collector.putInt(0, (int) res);
          break;
        case INT64:
          collector.putLong(0, (long) res);
          break;
        case FLOAT:
          collector.putFloat(0, (float) res);
          break;
        case DOUBLE:
          collector.putDouble(0, res);
      }
    }
  }
```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (dataType) { case INT32: transformInt(row, pc); break; case INT64...` statement on enum type 'org.apache.iotdb.tsfile.file.metadata.enums.TSDataType' misses cases: 'BOOLEAN', 'TEXT', and 'VECTOR'
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/UDAFSpread.java`
#### Snippet
```java
  @Override
  public void transform(Row row, PointCollector pc) throws Exception {
    switch (dataType) {
      case INT32:
        transformInt(row, pc);
        break;
      case INT64:
        transformLong(row, pc);
        break;
      case FLOAT:
        transformFloat(row, pc);
        break;
      case DOUBLE:
        transformDouble(row, pc);
        break;
    }
  }

```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (dataType) { case INT32: collector.putInt(0, (int) res); break; c...` statement on enum type 'org.apache.iotdb.tsfile.file.metadata.enums.TSDataType' misses cases: 'BOOLEAN', 'TEXT', and 'VECTOR'
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/UDAFQuantile.java`
#### Snippet
```java
    long result = sketch.findMinValueWithRank((long) (rank * sketch.getN()));
    double res = longToResult(result);
    switch (dataType) {
      case INT32:
        collector.putInt(0, (int) res);
        break;
      case INT64:
        collector.putLong(0, (long) res);
        break;
      case FLOAT:
        collector.putFloat(0, (float) res);
        break;
      case DOUBLE:
        collector.putDouble(0, res);
    }
  }

```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (dataType) { case INT32: intMap = new IntIntHashMap(); itMap = new IntL...` statement on enum type 'org.apache.iotdb.tsfile.file.metadata.enums.TSDataType' misses case 'VECTOR'
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/UDAFMode.java`
#### Snippet
```java
        .setOutputDataType(parameters.getDataType(0));
    dataType = UDFDataTypeTransformer.transformToTsDataType(parameters.getDataType(0));
    switch (dataType) {
      case INT32:
        intMap = new IntIntHashMap();
        itMap = new IntLongHashMap();
        break;
      case INT64:
        longMap = new LongIntHashMap();
        ltMap = new LongLongHashMap();
        break;
      case FLOAT:
        floatMap = new FloatIntHashMap();
        ftMap = new FloatLongHashMap();
        break;
      case DOUBLE:
        doubleMap = new DoubleIntHashMap();
        dtMap = new DoubleLongHashMap();
        break;
      case TEXT:
        stringMap = new HashMap<>();
        stMap = new HashMap<>();
        break;
      case BOOLEAN:
        booleanCnt = 0;
    }
  }

```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (dataType) { case INT32: intMap.forEachKeyValue(max::insert); int im = ...` statement on enum type 'org.apache.iotdb.tsfile.file.metadata.enums.TSDataType' misses case 'VECTOR'
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/UDAFMode.java`
#### Snippet
```java
  public void terminate(PointCollector pc) throws Exception {
    MaxSelector max = new MaxSelector();
    switch (dataType) {
      case INT32:
        intMap.forEachKeyValue(max::insert);
        int im = max.getInt();
        pc.putInt(itMap.get(im), im);
        break;
      case INT64:
        longMap.forEachKeyValue(max::insert);
        long lm = max.getLong();
        pc.putLong(ltMap.get(lm), lm);
        break;
      case FLOAT:
        floatMap.forEachKeyValue(max::insert);
        float fm = max.getFloat();
        pc.putFloat(ftMap.get(fm), fm);
        break;
      case DOUBLE:
        doubleMap.forEachKeyValue(max::insert);
        double dm = max.getDouble();
        pc.putDouble(dtMap.get(dm), dm);
        break;
      case TEXT:
        int maxTimes = 0;
        String s = null;
        for (Map.Entry<String, Integer> entry : stringMap.entrySet()) {
          String key = entry.getKey();
          Integer value = entry.getValue();
          if (value > maxTimes) {
            maxTimes = value;
            s = key;
          }
        }
        pc.putString(stMap.get(s), s);
        break;
      case BOOLEAN:
        pc.putBoolean(0, booleanCnt > 0);
    }
  }
}
```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (dataType) { case INT32: intMap.addToValue(row.getInt(0), 1); if (!itMa...` statement on enum type 'org.apache.iotdb.tsfile.file.metadata.enums.TSDataType' misses case 'VECTOR'
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/UDAFMode.java`
#### Snippet
```java
  @Override
  public void transform(Row row, PointCollector pc) throws Exception {
    switch (dataType) {
      case INT32:
        intMap.addToValue(row.getInt(0), 1);
        if (!itMap.containsKey(row.getInt(0))) {
          itMap.addToValue(row.getInt(0), row.getTime());
        }
        break;
      case INT64:
        longMap.addToValue(row.getLong(0), 1);
        if (!ltMap.containsKey(row.getLong(0))) {
          ltMap.addToValue(row.getLong(0), row.getTime());
        }
        break;
      case FLOAT:
        floatMap.addToValue(row.getFloat(0), 1);
        if (!ftMap.containsKey(row.getFloat(0))) {
          ftMap.addToValue(row.getFloat(0), row.getTime());
        }
        break;
      case DOUBLE:
        doubleMap.addToValue(row.getDouble(0), 1);
        if (!dtMap.containsKey(row.getDouble(0))) {
          dtMap.addToValue(row.getDouble(0), row.getTime());
        }
        break;
      case TEXT:
        stringMap.put(row.getString(0), stringMap.getOrDefault(row.getString(0), 0) + 1);
        if (!stMap.containsKey(row.getString(0))) {
          stMap.put(row.getString(0), row.getTime());
        }
        break;
      case BOOLEAN:
        boolean v = row.getBoolean(0);
        booleanCnt = v ? booleanCnt + 1 : booleanCnt - 1;
    }
  }

```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (dataType) { case INT32: intSet = new IntHashSet(); break; case I...` statement on enum type 'org.apache.iotdb.tsfile.file.metadata.enums.TSDataType' misses case 'VECTOR'
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/UDTFDistinct.java`
#### Snippet
```java
        .setOutputDataType(parameters.getDataType(0));
    dataType = UDFDataTypeTransformer.transformToTsDataType(parameters.getDataType(0));
    switch (dataType) {
      case INT32:
        intSet = new IntHashSet();
        break;
      case INT64:
        longSet = new LongHashSet();
        break;
      case FLOAT:
        floatSet = new FloatHashSet();
        break;
      case DOUBLE:
        doubleSet = new DoubleHashSet();
        break;
      case TEXT:
        stringSet = new HashSet<>();
        break;
      case BOOLEAN:
        booleanSet = new BooleanHashSet();
    }
  }

```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (dataType) { case INT32: MutableIntIterator intIterator = intSet.intIterator();...` statement on enum type 'org.apache.iotdb.tsfile.file.metadata.enums.TSDataType' misses case 'VECTOR'
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/UDTFDistinct.java`
#### Snippet
```java
  public void terminate(PointCollector pc) throws Exception {
    int i = 0;
    switch (dataType) {
      case INT32:
        MutableIntIterator intIterator = intSet.intIterator();
        while (intIterator.hasNext()) {
          pc.putInt(i, intIterator.next());
          i++;
        }
        break;
      case INT64:
        MutableLongIterator longIterator = longSet.longIterator();
        while (longIterator.hasNext()) {
          pc.putLong(i, longIterator.next());
          i++;
        }
        break;
      case FLOAT:
        MutableFloatIterator floatIterator = floatSet.floatIterator();
        while (floatIterator.hasNext()) {
          pc.putFloat(i, floatIterator.next());
          i++;
        }
        break;
      case DOUBLE:
        MutableDoubleIterator doubleIterator = doubleSet.doubleIterator();
        while (doubleIterator.hasNext()) {
          pc.putDouble(i, doubleIterator.next());
          i++;
        }
        break;
      case TEXT:
        for (String s : stringSet) {
          pc.putString(i, s);
          i++;
        }
        break;
      case BOOLEAN:
        MutableBooleanIterator booleanIterator = booleanSet.booleanIterator();
        while (booleanIterator.hasNext()) {
          pc.putBoolean(i, booleanIterator.next());
          i++;
        }
    }
  }

```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (dataType) { case INT32: intSet.clear(); break; case INT64: ...` statement on enum type 'org.apache.iotdb.tsfile.file.metadata.enums.TSDataType' misses case 'VECTOR'
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/UDTFDistinct.java`
#### Snippet
```java
  @Override
  public void beforeDestroy() {
    switch (dataType) {
      case INT32:
        intSet.clear();
        break;
      case INT64:
        longSet.clear();
        break;
      case FLOAT:
        floatSet.clear();
        break;
      case DOUBLE:
        doubleSet.clear();
        break;
      case TEXT:
        stringSet.clear();
        break;
      case BOOLEAN:
        booleanSet.clear();
    }
  }
}
```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (dataType) { case INT32: intSet.add(row.getInt(0)); break; case I...` statement on enum type 'org.apache.iotdb.tsfile.file.metadata.enums.TSDataType' misses case 'VECTOR'
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/UDTFDistinct.java`
#### Snippet
```java
  @Override
  public void transform(Row row, PointCollector pc) throws Exception {
    switch (dataType) {
      case INT32:
        intSet.add(row.getInt(0));
        break;
      case INT64:
        longSet.add(row.getLong(0));
        break;
      case FLOAT:
        floatSet.add(row.getFloat(0));
        break;
      case DOUBLE:
        doubleSet.add(row.getDouble(0));
        break;
      case TEXT:
        stringSet.add(row.getString(0));
        break;
      case BOOLEAN:
        booleanSet.add(row.getBoolean(0));
    }
  }

```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (valueForAlignedSeries.get(j).get(i).getDataType()) { case INT32: ...` statement on enum type 'org.apache.iotdb.tsfile.file.metadata.enums.TSDataType' misses case 'VECTOR'
in `rewrite-tsfile-tool/src/main/java/org/apache/iotdb/RewriteTsFileTool.java`
#### Snippet
```java
switch (valueForAlignedSeries.get(j).get(i).getDataType()) {
                    case INT32:
                      tablet.addValue(
                          schemaForAlignedSeries.get(j).getMeasurementId(),
                          tablet.rowSize,
                          valueForAlignedSeries.get(j).get(i).getInt());
                      break;
                    case INT64:
                      tablet.addValue(
                          schemaForAlignedSeries.get(j).getMeasurementId(),
                          tablet.rowSize,
                          valueForAlignedSeries.get(j).get(i).getLong());
                      break;
                    case TEXT:
                      tablet.addValue(
                          schemaForAlignedSeries.get(j).getMeasurementId(),
                          tablet.rowSize,
                          valueForAlignedSeries.get(j).get(i).getStringValue());
                      break;
                    case BOOLEAN:
                      tablet.addValue(
                          schemaForAlignedSeries.get(j).getMeasurementId(),
                          tablet.rowSize,
                          valueForAlignedSeries.get(j).get(i).getBoolean());
                      break;
                    case FLOAT:
                      tablet.addValue(
                          schemaForAlignedSeries.get(j).getMeasurementId(),
                          tablet.rowSize,
                          valueForAlignedSeries.get(j).get(i).getFloat());
                      break;
                    case DOUBLE:
                      tablet.addValue(
                          schemaForAlignedSeries.get(j).getMeasurementId(),
                          tablet.rowSize,
                          valueForAlignedSeries.get(j).get(i).getDouble());
                      break;
                  }
```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (valueForAlignedSeries.get(j).get(i).getDataType()) { case INT32: ...` statement on enum type 'org.apache.iotdb.tsfile.file.metadata.enums.TSDataType' misses case 'VECTOR'
in `rewrite-tsfile-tool/src/main/java/org/apache/iotdb/RewriteTsFileTool.java`
#### Snippet
```java
              continue;
            }
            switch (valueForAlignedSeries.get(j).get(i).getDataType()) {
              case INT32:
                tablet.addValue(
                    schemaForAlignedSeries.get(j).getMeasurementId(),
                    tablet.rowSize,
                    valueForAlignedSeries.get(j).get(i).getInt());
                break;
              case INT64:
                tablet.addValue(
                    schemaForAlignedSeries.get(j).getMeasurementId(),
                    tablet.rowSize,
                    valueForAlignedSeries.get(j).get(i).getLong());
                break;
              case TEXT:
                tablet.addValue(
                    schemaForAlignedSeries.get(j).getMeasurementId(),
                    tablet.rowSize,
                    valueForAlignedSeries.get(j).get(i).getStringValue());
                break;
              case BOOLEAN:
                tablet.addValue(
                    schemaForAlignedSeries.get(j).getMeasurementId(),
                    tablet.rowSize,
                    valueForAlignedSeries.get(j).get(i).getBoolean());
                break;
              case FLOAT:
                tablet.addValue(
                    schemaForAlignedSeries.get(j).getMeasurementId(),
                    tablet.rowSize,
                    valueForAlignedSeries.get(j).get(i).getFloat());
                break;
              case DOUBLE:
                tablet.addValue(
                    schemaForAlignedSeries.get(j).getMeasurementId(),
                    tablet.rowSize,
                    valueForAlignedSeries.get(j).get(i).getDouble());
                break;
            }
          }
          tablet.rowSize++;
```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (timeValuePair.getValue().getDataType()) { case TEXT: tablet.addValue( ...` statement on enum type 'org.apache.iotdb.tsfile.file.metadata.enums.TSDataType' misses case 'VECTOR'
in `rewrite-tsfile-tool/src/main/java/org/apache/iotdb/RewriteTsFileTool.java`
#### Snippet
```java
        TimeValuePair timeValuePair = batchIterator.nextTimeValuePair();
        tablet.addTimestamp(tablet.rowSize, timeValuePair.getTimestamp());
        switch (timeValuePair.getValue().getDataType()) {
          case TEXT:
            tablet.addValue(
                p.getMeasurement(), tablet.rowSize++, timeValuePair.getValue().getStringValue());
            break;
          case BOOLEAN:
            tablet.addValue(
                p.getMeasurement(), tablet.rowSize++, timeValuePair.getValue().getBoolean());
            break;
          case DOUBLE:
            tablet.addValue(
                p.getMeasurement(), tablet.rowSize++, timeValuePair.getValue().getDouble());
            break;
          case FLOAT:
            tablet.addValue(
                p.getMeasurement(), tablet.rowSize++, timeValuePair.getValue().getFloat());
            break;
          case INT64:
            tablet.addValue(
                p.getMeasurement(), tablet.rowSize++, timeValuePair.getValue().getLong());
            break;
          case INT32:
            tablet.addValue(
                p.getMeasurement(), tablet.rowSize++, timeValuePair.getValue().getInt());
            break;
        }
        if (tablet.rowSize >= MAX_TABLET_LENGTH) {
          session.insertTablet(tablet);
```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (pointsData[i].getDataType()) { case INT32: tablet.addValue( ...` statement on enum type 'org.apache.iotdb.tsfile.file.metadata.enums.TSDataType' misses case 'VECTOR'
in `rewrite-tsfile-tool/src/main/java/org/apache/iotdb/RewriteTsFileTool.java`
#### Snippet
```java
              continue;
            }
            switch (pointsData[i].getDataType()) {
              case INT32:
                tablet.addValue(
                    schemaList.get(i).getMeasurementId(), tablet.rowSize, pointsData[i].getInt());
                break;
              case INT64:
                tablet.addValue(
                    schemaList.get(i).getMeasurementId(), tablet.rowSize, pointsData[i].getLong());
                break;
              case FLOAT:
                tablet.addValue(
                    schemaList.get(i).getMeasurementId(), tablet.rowSize, pointsData[i].getFloat());
                break;
              case DOUBLE:
                tablet.addValue(
                    schemaList.get(i).getMeasurementId(),
                    tablet.rowSize,
                    pointsData[i].getDouble());
                break;
              case BOOLEAN:
                tablet.addValue(
                    schemaList.get(i).getMeasurementId(),
                    tablet.rowSize,
                    pointsData[i].getBoolean());
                break;
              case TEXT:
                tablet.addValue(
                    schemaList.get(i).getMeasurementId(),
                    tablet.rowSize,
                    pointsData[i].getStringValue());
                break;
            }
          }
          tablet.rowSize++;
```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (state) { case CREATE_PEER: case ADD_PEER: return true; }` statement on enum type 'org.apache.iotdb.confignode.procedure.state.AddConfigNodeState' misses cases: 'ADD_CONFIG_NODE_PREPARE', and 'REGISTER_SUCCESS'
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/impl/node/AddConfigNodeProcedure.java`
#### Snippet
```java
  @Override
  protected boolean isRollbackSupported(AddConfigNodeState state) {
    switch (state) {
      case CREATE_PEER:
      case ADD_PEER:
        return true;
    }
    return false;
  }
```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (state) { case CREATE_PEER: env.deleteConfigNodePeer(tConfigNodeLocation); ...` statement on enum type 'org.apache.iotdb.confignode.procedure.state.AddConfigNodeState' misses cases: 'ADD_CONFIG_NODE_PREPARE', and 'REGISTER_SUCCESS'
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/impl/node/AddConfigNodeProcedure.java`
#### Snippet
```java
  protected void rollbackState(ConfigNodeProcedureEnv env, AddConfigNodeState state)
      throws ProcedureException {
    switch (state) {
      case CREATE_PEER:
        env.deleteConfigNodePeer(tConfigNodeLocation);
        LOG.info("Rollback CREATE_PEER for: {}", tConfigNodeLocation);
        break;
      case ADD_PEER:
        env.removeConfigNodePeer(tConfigNodeLocation);
        LOG.info("Rollback ADD_PEER for: {}", tConfigNodeLocation);
        break;
    }
  }

```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (state) { case OPERATE_CHECK: case PRE_OPERATE_PIPE_CONFIGNODE: case OPERAT...` statement on enum type 'org.apache.iotdb.confignode.procedure.state.sync.OperatePipeState' misses case 'OPERATE_PIPE_CONFIGNODE'
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/impl/sync/CreatePipeProcedure.java`
#### Snippet
```java
  @Override
  protected boolean isRollbackSupported(OperatePipeState state) {
    switch (state) {
      case OPERATE_CHECK:
      case PRE_OPERATE_PIPE_CONFIGNODE:
      case OPERATE_PIPE_DATANODE:
        return true;
    }
    return false;
  }
```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (state) { case OPERATE_CHECK: case PRE_OPERATE_PIPE_CONFIGNODE: case OPERAT...` statement on enum type 'org.apache.iotdb.confignode.procedure.state.sync.OperatePipeState' misses case 'OPERATE_PIPE_CONFIGNODE'
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/impl/sync/StopPipeProcedure.java`
#### Snippet
```java
  @Override
  protected boolean isRollbackSupported(OperatePipeState state) {
    switch (state) {
      case OPERATE_CHECK:
      case PRE_OPERATE_PIPE_CONFIGNODE:
      case OPERATE_PIPE_DATANODE:
        return true;
    }
    return false;
  }
```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (state) { case OPERATE_CHECK: case PRE_OPERATE_PIPE_CONFIGNODE: case OPERAT...` statement on enum type 'org.apache.iotdb.confignode.procedure.state.sync.OperatePipeState' misses case 'OPERATE_PIPE_CONFIGNODE'
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/impl/sync/StartPipeProcedure.java`
#### Snippet
```java
  @Override
  protected boolean isRollbackSupported(OperatePipeState state) {
    switch (state) {
      case OPERATE_CHECK:
      case PRE_OPERATE_PIPE_CONFIGNODE:
      case OPERATE_PIPE_DATANODE:
        return true;
    }
    return false;
  }
```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (state) { case DELETE_PRE: case INVALIDATE_CACHE: return true; }` statement on enum type 'org.apache.iotdb.confignode.procedure.state.schema.DeleteStorageGroupState' misses cases: 'DELETE_STORAGE_GROUP_PREPARE', ...
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/impl/schema/DeleteStorageGroupProcedure.java`
#### Snippet
```java
  @Override
  protected boolean isRollbackSupported(DeleteStorageGroupState state) {
    switch (state) {
      case DELETE_PRE:
      case INVALIDATE_CACHE:
        return true;
    }
    return false;
  }
```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (state) { case DELETE_PRE: case INVALIDATE_CACHE: LOG.info("Rollback preD...` statement on enum type 'org.apache.iotdb.confignode.procedure.state.schema.DeleteStorageGroupState' misses cases: 'DELETE_STORAGE_GROUP_PREPARE', ...
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/impl/schema/DeleteStorageGroupProcedure.java`
#### Snippet
```java
  protected void rollbackState(ConfigNodeProcedureEnv env, DeleteStorageGroupState state)
      throws IOException, InterruptedException {
    switch (state) {
      case DELETE_PRE:
      case INVALIDATE_CACHE:
        LOG.info("Rollback preDeleted:{}", deleteSgSchema.getName());
        env.preDelete(PreDeleteStorageGroupPlan.PreDeleteType.ROLLBACK, deleteSgSchema.getName());
        break;
    }
  }

```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (types.get(i)) { case INT64: values.add(Long.parseLong(valuesStr[i])); ...` statement on enum type 'org.apache.iotdb.tsfile.file.metadata.enums.TSDataType' misses case 'VECTOR'
in `example/rocketmq/src/main/java/org/apache/iotdb/rocketmq/RocketMQConsumer.java`
#### Snippet
```java
    String[] valuesStr = dataArray[4].split(":");
    for (int i = 0; i < valuesStr.length; i++) {
      switch (types.get(i)) {
        case INT64:
          values.add(Long.parseLong(valuesStr[i]));
          break;
        case DOUBLE:
          values.add(Double.parseDouble(valuesStr[i]));
          break;
        case INT32:
          values.add(Integer.parseInt(valuesStr[i]));
          break;
        case TEXT:
          values.add(valuesStr[i]);
          break;
        case FLOAT:
          values.add(Float.parseFloat(valuesStr[i]));
          break;
        case BOOLEAN:
          values.add(Boolean.parseBoolean(valuesStr[i]));
          break;
      }
    }

```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (types.get(i)) { case INT64: values.add(Long.parseLong(valuesStr[i])); ...` statement on enum type 'org.apache.iotdb.tsfile.file.metadata.enums.TSDataType' misses case 'VECTOR'
in `flink-iotdb-connector/src/main/java/org/apache/iotdb/flink/DefaultIoTSerializationSchema.java`
#### Snippet
```java
    String[] valuesStr = tuple.get(fieldValues).split(separator);
    for (int i = 0; i < valuesStr.length; i++) {
      switch (types.get(i)) {
        case INT64:
          values.add(Long.parseLong(valuesStr[i]));
          break;
        case DOUBLE:
          values.add(Double.parseDouble(valuesStr[i]));
          break;
        case INT32:
          values.add(Integer.parseInt(valuesStr[i]));
          break;
        case TEXT:
          values.add(valuesStr[i]);
          break;
        case FLOAT:
          values.add(Float.parseFloat(valuesStr[i]));
          break;
        case BOOLEAN:
          values.add(Boolean.parseBoolean(valuesStr[i]));
          break;
      }
    }

```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (value.getDataType()) { case BOOLEAN: outputStream.writeBoo...` statement on enum type 'org.apache.iotdb.tsfile.file.metadata.enums.TSDataType' misses case 'VECTOR'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/DescReadWriteBatchData.java`
#### Snippet
```java
              outputStream.write(1);
              outputStream.write(value.getDataType().serialize());
              switch (value.getDataType()) {
                case BOOLEAN:
                  outputStream.writeBoolean(value.getBoolean());
                  break;
                case DOUBLE:
                  outputStream.writeDouble(value.getDouble());
                  break;
                case FLOAT:
                  outputStream.writeFloat(value.getFloat());
                  break;
                case TEXT:
                  Binary binary = value.getBinary();
                  outputStream.writeInt(binary.getLength());
                  outputStream.write(binary.getValues());
                  break;
                case INT64:
                  outputStream.writeLong(value.getLong());
                  break;
                case INT32:
                  outputStream.writeInt(value.getInt());
                  break;
              }
            }
          }
```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (value.getDataType()) { case BOOLEAN: outputStream.writeBoo...` statement on enum type 'org.apache.iotdb.tsfile.file.metadata.enums.TSDataType' misses case 'VECTOR'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/BatchData.java`
#### Snippet
```java
              outputStream.write(1);
              outputStream.write(value.getDataType().serialize());
              switch (value.getDataType()) {
                case BOOLEAN:
                  outputStream.writeBoolean(value.getBoolean());
                  break;
                case DOUBLE:
                  outputStream.writeDouble(value.getDouble());
                  break;
                case FLOAT:
                  outputStream.writeFloat(value.getFloat());
                  break;
                case TEXT:
                  Binary binary = value.getBinary();
                  outputStream.writeInt(binary.getLength());
                  outputStream.write(binary.getValues());
                  break;
                case INT64:
                  outputStream.writeLong(value.getLong());
                  break;
                case INT32:
                  outputStream.writeInt(value.getInt());
                  break;
              }
            }
          }
```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (writer.getDataType()) { case INT64: writer.write(time, point != null ? poi...` statement on enum type 'org.apache.iotdb.tsfile.file.metadata.enums.TSDataType' misses case 'VECTOR'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/chunk/AlignedChunkWriterImpl.java`
#### Snippet
```java
    for (TsPrimitiveType point : points) {
      ValueChunkWriter writer = valueChunkWriterList.get(valueIndex++);
      switch (writer.getDataType()) {
        case INT64:
          writer.write(time, point != null ? point.getLong() : Long.MAX_VALUE, point == null);
          break;
        case INT32:
          writer.write(time, point != null ? point.getInt() : Integer.MAX_VALUE, point == null);
          break;
        case FLOAT:
          writer.write(time, point != null ? point.getFloat() : Float.MAX_VALUE, point == null);
          break;
        case DOUBLE:
          writer.write(time, point != null ? point.getDouble() : Double.MAX_VALUE, point == null);
          break;
        case BOOLEAN:
          writer.write(time, point != null ? point.getBoolean() : false, point == null);
          break;
        case TEXT:
          writer.write(
              time,
              point != null ? point.getBinary() : new Binary("".getBytes(StandardCharsets.UTF_8)),
              point == null);
          break;
      }
    }
    write(time);
```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (types.get(i)) { case INT64: values.add(Long.parseLong(valuesStr[i])); ...` statement on enum type 'org.apache.iotdb.tsfile.file.metadata.enums.TSDataType' misses case 'VECTOR'
in `example/rabbitmq/src/main/java/org/apache/iotdb/rabbitmq/RabbitMQConsumer.java`
#### Snippet
```java
    String[] valuesStr = dataArray[4].split(":");
    for (int i = 0; i < valuesStr.length; i++) {
      switch (types.get(i)) {
        case INT64:
          values.add(Long.parseLong(valuesStr[i]));
          break;
        case DOUBLE:
          values.add(Double.parseDouble(valuesStr[i]));
          break;
        case INT32:
          values.add(Integer.parseInt(valuesStr[i]));
          break;
        case TEXT:
          values.add(valuesStr[i]);
          break;
        case FLOAT:
          values.add(Float.parseFloat(valuesStr[i]));
          break;
        case BOOLEAN:
          values.add(Boolean.parseBoolean(valuesStr[i]));
          break;
      }
    }

```

### RuleId[ruleID=EnumSwitchStatementWhichMissesCases]
`switch (rowWindow.getDataType(0)) { case INT32: accumulator += rowIterator.next()....` statement on enum type 'org.apache.iotdb.udf.api.type.Type' misses cases: 'BOOLEAN', 'INT64', 'FLOAT', and 'TEXT'
in `integration-test/src/main/java/org/apache/iotdb/db/query/udf/example/Accumulator.java`
#### Snippet
```java
    RowIterator rowIterator = rowWindow.getRowIterator();
    while (rowIterator.hasNextRow()) {
      switch (rowWindow.getDataType(0)) {
        case INT32:
          accumulator += rowIterator.next().getInt(0);
          break;
        case DOUBLE:
          accumulator += (int) rowIterator.next().getDouble(0);
          break;
      }
    }
    if (rowWindow.windowSize() != 0) {
```

## RuleId[ruleID=StringEquality]
### RuleId[ruleID=StringEquality]
String values are compared using `==`, not 'equals()'
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/builtin/UDTFBottomK.java`
#### Snippet
```java
                  final String cs2 = pairA.right;

                  if (Objects.requireNonNull(cs1) == Objects.requireNonNull(cs2)) {
                    return 0;
                  }
```

## RuleId[ruleID=ConditionalBreakInInfiniteLoop]
### RuleId[ruleID=ConditionalBreakInInfiniteLoop]
Conditional break inside loop
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/DeletionGroup.java`
#### Snippet
```java
    }

    while (true) {
      if (ts < intervalCursor.startTime) {
        return false;
```

### RuleId[ruleID=ConditionalBreakInInfiniteLoop]
Conditional break inside loop
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/RSchemaRegion.java`
#### Snippet
```java

        // delete parent without child after timeseries deleted
        while (true) {
          if (parentNeedsToCheck.isEmpty()) {
            break;
```

## RuleId[ruleID=LoopStatementsThatDontLoop]
### RuleId[ruleID=LoopStatementsThatDontLoop]
`for` statement does not loop
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/RSchemaReadWriteHandler.java`
#### Snippet
```java
      byte[] key = RSchemaUtils.toRocksDBKey(siblingPrefix, type);
      try (RocksIterator iterator = rocksDB.newIterator()) {
        for (iterator.seek(key); iterator.isValid(); iterator.next()) {
          if (!RSchemaUtils.prefixMatch(iterator.key(), key)) {
            break;
```

## RuleId[ruleID=StaticCallOnSubclass]
### RuleId[ruleID=StaticCallOnSubclass]
Static method `setInputPaths()` declared in class 'org.apache.hadoop.mapreduce.lib.input.FileInputFormat' but referenced via subclass 'org.apache.iotdb.hadoop.tsfile.TSFInputFormat'
in `example/hadoop/src/main/java/org/apache/iotdb/hadoop/tsfile/TSMRWriteExample.java`
#### Snippet
```java

    // set input file path
    TSFInputFormat.setInputPaths(job, inputPath);
    // set output file path
    TSFOutputFormat.setOutputPath(job, outputPath);
```

### RuleId[ruleID=StaticCallOnSubclass]
Static method `setOutputPath()` declared in class 'org.apache.hadoop.mapreduce.lib.output.FileOutputFormat' but referenced via subclass 'org.apache.iotdb.hadoop.tsfile.TSFOutputFormat'
in `example/hadoop/src/main/java/org/apache/iotdb/hadoop/tsfile/TSMRWriteExample.java`
#### Snippet
```java
    TSFInputFormat.setInputPaths(job, inputPath);
    // set output file path
    TSFOutputFormat.setOutputPath(job, outputPath);

    /** special configuration for reading tsfile with TSFInputFormat */
```

### RuleId[ruleID=StaticCallOnSubclass]
Static method `setInputPaths()` declared in class 'org.apache.hadoop.mapreduce.lib.input.FileInputFormat' but referenced via subclass 'org.apache.iotdb.hadoop.tsfile.TSFInputFormat'
in `example/hadoop/src/main/java/org/apache/iotdb/hadoop/tsfile/TSFMRReadExample.java`
#### Snippet
```java
    job.setOutputValueClass(DoubleWritable.class);
    // set input file path
    TSFInputFormat.setInputPaths(job, inputPath);
    // set output file path
    TSFOutputFormat.setOutputPath(job, outputPath);
```

### RuleId[ruleID=StaticCallOnSubclass]
Static method `setOutputPath()` declared in class 'org.apache.hadoop.mapreduce.lib.output.FileOutputFormat' but referenced via subclass 'org.apache.iotdb.hadoop.tsfile.TSFOutputFormat'
in `example/hadoop/src/main/java/org/apache/iotdb/hadoop/tsfile/TSFMRReadExample.java`
#### Snippet
```java
    TSFInputFormat.setInputPaths(job, inputPath);
    // set output file path
    TSFOutputFormat.setOutputPath(job, outputPath);

    /** special configuration for reading tsfile with TSFInputFormat */
```

## RuleId[ruleID=SuspiciousSystemArraycopy]
### RuleId[ruleID=SuspiciousSystemArraycopy]
`columns[k]` is not of an array type
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/write/InsertTabletNode.java`
#### Snippet
```java
        System.arraycopy(times, start, subTimes, destLoc, end - start);
        for (int k = 0; k < values.length; k++) {
          System.arraycopy(columns[k], start, values[k], destLoc, end - start);
          if (bitMaps != null && this.bitMaps[k] != null) {
            BitMap.copyOfRange(this.bitMaps[k], start, bitMaps[k], destLoc, end - start);
```

### RuleId[ruleID=SuspiciousSystemArraycopy]
`values[k]` is not of an array type
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/write/InsertTabletNode.java`
#### Snippet
```java
        System.arraycopy(times, start, subTimes, destLoc, end - start);
        for (int k = 0; k < values.length; k++) {
          System.arraycopy(columns[k], start, values[k], destLoc, end - start);
          if (bitMaps != null && this.bitMaps[k] != null) {
            BitMap.copyOfRange(this.bitMaps[k], start, bitMaps[k], destLoc, end - start);
```

### RuleId[ruleID=SuspiciousSystemArraycopy]
`value[columnIndexArray[i]]` is not of an array type
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/AlignedTVList.java`
#### Snippet
```java
        case TEXT:
          Binary[] arrayT = ((Binary[]) columnValues.get(arrayIndex));
          System.arraycopy(value[columnIndexArray[i]], idx, arrayT, elementIndex, remaining);

          // update raw size of Text chunk
```

### RuleId[ruleID=SuspiciousSystemArraycopy]
`value[columnIndexArray[i]]` is not of an array type
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/AlignedTVList.java`
#### Snippet
```java
        case FLOAT:
          float[] arrayF = ((float[]) columnValues.get(arrayIndex));
          System.arraycopy(value[columnIndexArray[i]], idx, arrayF, elementIndex, remaining);
          break;
        case INT32:
```

### RuleId[ruleID=SuspiciousSystemArraycopy]
`value[columnIndexArray[i]]` is not of an array type
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/AlignedTVList.java`
#### Snippet
```java
        case INT32:
          int[] arrayI = ((int[]) columnValues.get(arrayIndex));
          System.arraycopy(value[columnIndexArray[i]], idx, arrayI, elementIndex, remaining);
          break;
        case INT64:
```

### RuleId[ruleID=SuspiciousSystemArraycopy]
`value[columnIndexArray[i]]` is not of an array type
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/AlignedTVList.java`
#### Snippet
```java
        case INT64:
          long[] arrayL = ((long[]) columnValues.get(arrayIndex));
          System.arraycopy(value[columnIndexArray[i]], idx, arrayL, elementIndex, remaining);
          break;
        case DOUBLE:
```

### RuleId[ruleID=SuspiciousSystemArraycopy]
`value[columnIndexArray[i]]` is not of an array type
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/AlignedTVList.java`
#### Snippet
```java
        case DOUBLE:
          double[] arrayD = ((double[]) columnValues.get(arrayIndex));
          System.arraycopy(value[columnIndexArray[i]], idx, arrayD, elementIndex, remaining);
          break;
        case BOOLEAN:
```

### RuleId[ruleID=SuspiciousSystemArraycopy]
`value[columnIndexArray[i]]` is not of an array type
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/AlignedTVList.java`
#### Snippet
```java
        case BOOLEAN:
          boolean[] arrayB = ((boolean[]) columnValues.get(arrayIndex));
          System.arraycopy(value[columnIndexArray[i]], idx, arrayB, elementIndex, remaining);
          break;
        default:
```

## RuleId[ruleID=UnnecessaryStringEscape]
### RuleId[ruleID=UnnecessaryStringEscape]
`\'` is unnecessarily escaped
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
  @Override
  public String getIdentifierQuoteString() {
    return "\' or \"";
  }

```

## RuleId[ruleID=ObsoleteCollection]
### RuleId[ruleID=ObsoleteCollection]
Obsolete collection type `Stack` used
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/utils/KDTreeUtil.java`
#### Snippet
```java
  public ArrayList<Double> query(ArrayList<Double> input, double[] std) {
    Node node = kdTree;
    Stack<Node> stack = new Stack<>();
    while (!node.isLeaf) {
      if (input.get(node.partitionDimension) < node.partitionValue) {
```

### RuleId[ruleID=ObsoleteCollection]
Obsolete collection type `Stack<>` used
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/utils/KDTreeUtil.java`
#### Snippet
```java
  public ArrayList<Double> query(ArrayList<Double> input, double[] std) {
    Node node = kdTree;
    Stack<Node> stack = new Stack<>();
    while (!node.isLeaf) {
      if (input.get(node.partitionDimension) < node.partitionValue) {
```

### RuleId[ruleID=ObsoleteCollection]
Obsolete collection type `Stack` used
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/utils/KDTreeUtil.java`
#### Snippet
```java

  public ArrayList<Double> queryRec(
      ArrayList<Double> input, double distance, Stack<Node> stack, double[] std) {
    ArrayList<Double> nearest = null;
    Node node;
```

### RuleId[ruleID=ObsoleteCollection]
Obsolete collection type `Stack` used
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/utils/KDTreeUtil.java`
#### Snippet
```java
    ArrayList<ArrayList<Double>> kNearest = new ArrayList<>();
    Node node = kdTree;
    Stack<Node> stack = new Stack<>();
    while (!node.isLeaf) {
      if (input.get(node.partitionDimension) < node.partitionValue) {
```

### RuleId[ruleID=ObsoleteCollection]
Obsolete collection type `Stack<>` used
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/utils/KDTreeUtil.java`
#### Snippet
```java
    ArrayList<ArrayList<Double>> kNearest = new ArrayList<>();
    Node node = kdTree;
    Stack<Node> stack = new Stack<>();
    while (!node.isLeaf) {
      if (input.get(node.partitionDimension) < node.partitionValue) {
```

### RuleId[ruleID=ObsoleteCollection]
Obsolete collection type `Stack` used
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/utils/KDTreeUtil.java`
#### Snippet
```java

  public ArrayList<ArrayList<Double>> queryRecKNN(
      ArrayList<Double> input, double distance, Stack<Node> stack, double[] std) {
    ArrayList<ArrayList<Double>> nearest = new ArrayList<>();
    Node node;
```

### RuleId[ruleID=ObsoleteCollection]
Obsolete collection type `Vector<>` used
in `node-commons/src/main/java/org/apache/iotdb/commons/partition/SchemaPartitionTable.java`
#### Snippet
```java
  public List<TSeriesPartitionSlot> filterUnassignedSchemaPartitionSlots(
      List<TSeriesPartitionSlot> partitionSlots) {
    List<TSeriesPartitionSlot> result = new Vector<>();

    partitionSlots.forEach(
```

### RuleId[ruleID=ObsoleteCollection]
Obsolete collection type `Vector<>` used
in `node-commons/src/main/java/org/apache/iotdb/commons/partition/SeriesPartitionTable.java`
#### Snippet
```java

      int consensusGroupIdNum = ReadWriteIOUtils.readInt(inputStream);
      List<TConsensusGroupId> consensusGroupIds = new Vector<>();
      for (int j = 0; j < consensusGroupIdNum; j++) {
        TConsensusGroupId consensusGroupId = new TConsensusGroupId();
```

### RuleId[ruleID=ObsoleteCollection]
Obsolete collection type `Vector<>` used
in `node-commons/src/main/java/org/apache/iotdb/commons/partition/SeriesPartitionTable.java`
#### Snippet
```java

      int consensusGroupIdNum = buffer.getInt();
      List<TConsensusGroupId> consensusGroupIds = new Vector<>();
      for (int j = 0; j < consensusGroupIdNum; j++) {
        consensusGroupIds.add(ThriftCommonsSerDeUtils.deserializeTConsensusGroupId(buffer));
```

### RuleId[ruleID=ObsoleteCollection]
Obsolete collection type `Vector<>` used
in `node-commons/src/main/java/org/apache/iotdb/commons/partition/SeriesPartitionTable.java`
#### Snippet
```java
  public synchronized List<TTimePartitionSlot> filterUnassignedDataPartitionSlots(
      List<TTimePartitionSlot> partitionSlots) {
    List<TTimePartitionSlot> result = new Vector<>();

    partitionSlots.forEach(
```

### RuleId[ruleID=ObsoleteCollection]
Obsolete collection type `Vector<>` used
in `node-commons/src/main/java/org/apache/iotdb/commons/partition/SeriesPartitionTable.java`
#### Snippet
```java
        .forEach(
            ((timePartitionSlot, consensusGroupIds) -> {
              seriesPartitionMap.put(timePartitionSlot, new Vector<>(consensusGroupIds));
              consensusGroupIds.forEach(
                  consensusGroupId ->
```

### RuleId[ruleID=ObsoleteCollection]
Obsolete collection type `Hashtable` used
in `jdbc/src/main/java/org/apache/iotdb/jdbc/Activator.java`
#### Snippet
```java
  public void start(BundleContext context) {
    IoTDBDataSourceFactory dsf = new IoTDBDataSourceFactory();
    Dictionary<String, String> props = new Hashtable<String, String>();
    props.put(DataSourceFactory.OSGI_JDBC_DRIVER_CLASS, IoTDBDriver.class.getName());
    props.put(DataSourceFactory.OSGI_JDBC_DRIVER_NAME, "iotdb");
```

### RuleId[ruleID=ObsoleteCollection]
Obsolete collection type `Stack` used
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/ConfigMTree.java`
#### Snippet
```java
    InternalMNode internalMNode;

    Stack<InternalMNode> stack = new Stack<>();
    stack.push(storageGroupMNode);

```

### RuleId[ruleID=ObsoleteCollection]
Obsolete collection type `Stack<>` used
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/ConfigMTree.java`
#### Snippet
```java
    InternalMNode internalMNode;

    Stack<InternalMNode> stack = new Stack<>();
    stack.push(storageGroupMNode);

```

### RuleId[ruleID=ObsoleteCollection]
Obsolete collection type `Vector<>` used
in `confignode/src/main/java/org/apache/iotdb/confignode/manager/load/balancer/router/priority/GreedyPriorityBalancer.java`
#### Snippet
```java

    // List<Pair<loadScore, TDataNodeLocation>> for sorting
    List<Pair<Long, TDataNodeLocation>> sortList = new Vector<>();
    replicaSet
        .getDataNodeLocations()
```

### RuleId[ruleID=ObsoleteCollection]
Obsolete collection type `Vector<>` used
in `confignode/src/main/java/org/apache/iotdb/confignode/consensus/request/write/region/OfferRegionMaintainTasksPlan.java`
#### Snippet
```java
  public OfferRegionMaintainTasksPlan() {
    super(ConfigPhysicalPlanType.OfferRegionMaintainTasks);
    this.regionMaintainTaskList = new Vector<>();
  }

```

### RuleId[ruleID=ObsoleteCollection]
Obsolete collection type `Vector<>` used
in `confignode/src/main/java/org/apache/iotdb/confignode/persistence/partition/PartitionInfo.java`
#### Snippet
```java
  public DataSet getRegionInfoList(GetRegionInfoListPlan regionsInfoPlan) {
    RegionInfoListResp regionResp = new RegionInfoListResp();
    List<TRegionInfo> regionInfoList = new Vector<>();
    if (storageGroupPartitionTables.isEmpty()) {
      regionResp.setStatus(RpcUtils.getStatus(TSStatusCode.SUCCESS_STATUS));
```

### RuleId[ruleID=ObsoleteCollection]
Obsolete collection type `Vector<>` used
in `confignode/src/main/java/org/apache/iotdb/confignode/persistence/partition/StorageGroupPartitionTable.java`
#### Snippet
```java

  private List<TRegionInfo> buildRegionInfoList(RegionGroup regionGroup) {
    List<TRegionInfo> regionInfoList = new Vector<>();
    final TConsensusGroupId regionId = regionGroup.getId();

```

### RuleId[ruleID=ObsoleteCollection]
Obsolete collection type `Vector<>` used
in `confignode/src/main/java/org/apache/iotdb/confignode/persistence/partition/StorageGroupPartitionTable.java`
#### Snippet
```java
   */
  public List<Pair<Long, TConsensusGroupId>> getRegionGroupSlotsCounter(TConsensusGroupType type) {
    List<Pair<Long, TConsensusGroupId>> result = new Vector<>();

    regionGroupMap.forEach(
```

### RuleId[ruleID=ObsoleteCollection]
Obsolete collection type `Vector<>` used
in `confignode/src/main/java/org/apache/iotdb/confignode/persistence/partition/StorageGroupPartitionTable.java`
#### Snippet
```java

  public List<TRegionInfo> getRegionInfoList(GetRegionInfoListPlan regionsInfoPlan) {
    List<TRegionInfo> regionInfoList = new Vector<>();
    final TShowRegionReq showRegionReq = regionsInfoPlan.getShowRegionReq();

```

## RuleId[ruleID=RefusedBequest]
### RuleId[ruleID=RefusedBequest]
Method `clone()` does not call 'super.clone()'
in `node-commons/src/main/java/org/apache/iotdb/commons/path/AlignedPath.java`
#### Snippet
```java

  @Override
  public AlignedPath clone() {
    AlignedPath alignedPath = null;
    try {
```

### RuleId[ruleID=RefusedBequest]
Method `clone()` does not call 'super.clone()'
in `node-commons/src/main/java/org/apache/iotdb/commons/path/MeasurementPath.java`
#### Snippet
```java

  @Override
  public MeasurementPath clone() {
    MeasurementPath newMeasurementPath = null;
    try {
```

### RuleId[ruleID=RefusedBequest]
Method `clone()` does not call 'super.clone()'
in `node-commons/src/main/java/org/apache/iotdb/commons/path/PartialPath.java`
#### Snippet
```java

  @Override
  public PartialPath clone() {
    return new PartialPath(this.getNodes().clone());
  }
```

### RuleId[ruleID=RefusedBequest]
Method `clone()` does not call 'super.clone()'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/PlanNodeUtil.java`
#### Snippet
```java
    }

    public PrintContext clone() {
      return new PrintContext(isLast, level, new TreeMap<>(codeMap));
    }
```

### RuleId[ruleID=RefusedBequest]
Method `clone()` does not call 'super.clone()'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/process/FilterNode.java`
#### Snippet
```java

  @Override
  public PlanNode clone() {
    return new FilterNode(
        getPlanNodeId(), outputExpressions, predicate, keepNull, zoneId, scanOrder);
```

### RuleId[ruleID=RefusedBequest]
Method `clone()` does not call 'super.clone()'
in `server/src/main/java/org/apache/iotdb/db/engine/modification/Deletion.java`
#### Snippet
```java

  @Override
  public Deletion clone() {
    return new Deletion(getPath(), getFileOffset(), getStartTime(), getEndTime());
  }
```

### RuleId[ruleID=RefusedBequest]
Method `clone()` does not call 'super.clone()'
in `spark-tsfile/src/main/java/org/apache/iotdb/spark/tsfile/qp/common/BasicOperator.java`
#### Snippet
```java

  @Override
  public BasicOperator clone() {
    BasicOperator ret = new BasicOperator(this.tokenIntType, seriesPath, seriesValue);
    ret.tokenSymbol = tokenSymbol;
```

### RuleId[ruleID=RefusedBequest]
Method `clone()` does not call 'super.clone()'
in `spark-tsfile/src/main/java/org/apache/iotdb/spark/tsfile/qp/common/FilterOperator.java`
#### Snippet
```java

  @Override
  public FilterOperator clone() {
    FilterOperator ret = new FilterOperator(this.tokenIntType);
    ret.tokenSymbol = tokenSymbol;
```

### RuleId[ruleID=RefusedBequest]
Method `clone()` does not call 'super.clone()'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/Path.java`
#### Snippet
```java

  @Override
  public Path clone() {
    return new Path(fullPath);
  }
```

### RuleId[ruleID=RefusedBequest]
Method `clone()` does not call 'super.clone()'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/expression/impl/SingleSeriesExpression.java`
#### Snippet
```java

  @Override
  public IExpression clone() {
    return new SingleSeriesExpression(seriesPath.clone(), filter.copy());
  }
```

### RuleId[ruleID=RefusedBequest]
Method `clone()` does not call 'super.clone()'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/expression/impl/GlobalTimeExpression.java`
#### Snippet
```java

  @Override
  public IExpression clone() {
    return new GlobalTimeExpression(filter.copy());
  }
```

### RuleId[ruleID=RefusedBequest]
Method `clone()` does not call 'super.clone()'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BitMap.java`
#### Snippet
```java

  @Override
  public BitMap clone() {
    byte[] cloneBytes = new byte[this.bits.length];
    System.arraycopy(this.bits, 0, cloneBytes, 0, this.bits.length);
```

### RuleId[ruleID=RefusedBequest]
Method `clone()` does not call 'super.clone()'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/StringContainer.java`
#### Snippet
```java

  @Override
  public StringContainer clone() {
    StringContainer ret = new StringContainer(joinSeparator);
    for (String s : sequenceList) {
```

## RuleId[ruleID=RedundantArrayCreation]
### RuleId[ruleID=RedundantArrayCreation]
Redundant array creation for calling varargs method
in `jdbc/src/main/java/org/apache/iotdb/jdbc/StringUtils.java`
#### Snippet
```java
    }
    try {
      toPlainStringMethod = BigDecimal.class.getMethod("toPlainString", new Class[0]);
    } catch (NoSuchMethodException nsme) {
    }
```

## RuleId[ruleID=DuplicateBranchesInSwitch]
### RuleId[ruleID=DuplicateBranchesInSwitch]
Branch in 'switch' is a duplicate of the default branch
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
      switch (timeprecision.toLowerCase()) {
        case "ms":
          break;
        case "us":
          time = time * 1000;
```

### RuleId[ruleID=DuplicateBranchesInSwitch]
Duplicate branch in 'switch'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java

      case Types.FLOAT:
        setFloat(parameterIndex, parameterAsNum.floatValue());
        break;
      case Types.DOUBLE:
```

### RuleId[ruleID=DuplicateBranchesInSwitch]
Branch in 'switch' is a duplicate of the default branch
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
          case Types.LONGVARBINARY:
          case Types.BLOB:
            throw new SQLException(Constant.PARAMETER_SUPPORTED);
          case Types.DATE:
          case Types.TIMESTAMP:
```

### RuleId[ruleID=DuplicateBranchesInSwitch]
Branch in 'switch' is a duplicate of the default branch
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java

          case Types.OTHER:
            throw new SQLException(Constant.PARAMETER_SUPPORTED); //
          default:
            throw new SQLException(Constant.PARAMETER_SUPPORTED); //
```

### RuleId[ruleID=DuplicateBranchesInSwitch]
Branch in 'switch' is a duplicate of the default branch
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
      switch (timeprecision.toLowerCase()) {
        case "ms":
          break;
        case "us":
          time = time * 1000;
```

### RuleId[ruleID=DuplicateBranchesInSwitch]
Branch in 'switch' is a duplicate of the default branch
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/log/CompactionLogAnalyzer.java`
#### Snippet
```java
          case SEQUENCE_NAME_FROM_OLD:
          case UNSEQUENCE_NAME_FROM_OLD:
            break;
          default:
            break;
```

### RuleId[ruleID=DuplicateBranchesInSwitch]
Branch in 'switch' is a duplicate of the default branch
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/UDAFQuantile.java`
#### Snippet
```java
        return (float) data >= 0f ? result : result ^ Long.MAX_VALUE;
      case INT64:
        return (long) data;
      case DOUBLE:
        result = Double.doubleToLongBits((double) data);
```

### RuleId[ruleID=DuplicateBranchesInSwitch]
Branch in 'switch' is a duplicate of the default branch
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/UDAFQuantile.java`
#### Snippet
```java
    switch (dataType) {
      case INT32:
        return (double) (result);
      case FLOAT:
        result = (result >>> 31) == 0 ? result : result ^ Long.MAX_VALUE;
```

### RuleId[ruleID=DuplicateBranchesInSwitch]
Branch in 'switch' is a duplicate of the default branch
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/UDAFQuantile.java`
#### Snippet
```java
        return Float.intBitsToFloat((int) (result));
      case INT64:
        return (double) (result);
      case DOUBLE:
        result = (result >>> 63) == 0 ? result : result ^ Long.MAX_VALUE;
```

### RuleId[ruleID=DuplicateBranchesInSwitch]
Duplicate branch in 'switch'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/encoder/PlainEncoder.java`
#### Snippet
```java
        return 8;
      case FLOAT:
        return 4;
      case DOUBLE:
        return 8;
```

### RuleId[ruleID=DuplicateBranchesInSwitch]
Duplicate branch in 'switch'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/encoder/PlainEncoder.java`
#### Snippet
```java
        return 4;
      case DOUBLE:
        return 8;
      case TEXT:
        // refer to encode(Binary,ByteArrayOutputStream)
```

## RuleId[ruleID=RegExpSimplifiable]
### RuleId[ruleID=RegExpSimplifiable]
`[0]` can be simplified to '0'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
                setBoolean(parameterIndex, false);
              } else if (((String) parameterObj).matches("-?\\d+\\.?\\d*")) {
                setBoolean(parameterIndex, !((String) parameterObj).matches("-?[0]+[.]*[0]*"));
              } else {
                throw new SQLException(
```

### RuleId[ruleID=RegExpSimplifiable]
`[0]` can be simplified to '0'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
                setBoolean(parameterIndex, false);
              } else if (((String) parameterObj).matches("-?\\d+\\.?\\d*")) {
                setBoolean(parameterIndex, !((String) parameterObj).matches("-?[0]+[.]*[0]*"));
              } else {
                throw new SQLException(
```

## RuleId[ruleID=DuplicateExpressions]
### RuleId[ruleID=DuplicateExpressions]
Multiple occurrences of `(catalog != null && catalog.length() > 0) || schemaPattern != null && schemaPattern.leng...`
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      sql = sql + " " + schemaPattern;
    }
    if (((catalog != null && catalog.length() > 0)
            || schemaPattern != null && schemaPattern.length() > 0)
        && tableNamePattern != null
        && tableNamePattern.length() > 0) {
```

### RuleId[ruleID=DuplicateExpressions]
Multiple occurrences of `(catalog != null && catalog.length() > 0) || schemaPattern != null && schemaPattern.leng...`
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
    }

    if (((catalog != null && catalog.length() > 0)
            || schemaPattern != null && schemaPattern.length() > 0)
        && tableNamePattern != null
        && tableNamePattern.length() > 0
```

### RuleId[ruleID=DuplicateExpressions]
Multiple occurrences of `(catalog != null && catalog.length() > 0) || schemaPattern != null && schemaPattern.leng...`
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      sql = sql + " " + schemaPattern;
    }
    if (((catalog != null && catalog.length() > 0)
            || schemaPattern != null && schemaPattern.length() > 0)
        && tableNamePattern != null
        && tableNamePattern.length() > 0) {
```

### RuleId[ruleID=DuplicateExpressions]
Multiple occurrences of `(catalog != null && catalog.length() > 0) || schemaPattern != null && schemaPattern.leng...`
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
    }

    if (((catalog != null && catalog.length() > 0)
            || schemaPattern != null && schemaPattern.length() > 0)
        && tableNamePattern != null
        && tableNamePattern.length() > 0
```

### RuleId[ruleID=DuplicateExpressions]
Multiple occurrences of `oldLevelPos[i + 1] - oldLevelPos[i]`
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/HeapLongKLLSketch.java`
#### Snippet
```java
        levelPos[i] = cntPos;
        if (i < oldCntLevel) {
          System.arraycopy(num, oldLevelPos[i], num, cntPos, oldLevelPos[i + 1] - oldLevelPos[i]);
          cntPos += oldLevelPos[i + 1] - oldLevelPos[i];
        }
```

### RuleId[ruleID=DuplicateExpressions]
Multiple occurrences of `oldLevelPos[i + 1] - oldLevelPos[i]`
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/HeapLongKLLSketch.java`
#### Snippet
```java
        if (i < oldCntLevel) {
          System.arraycopy(num, oldLevelPos[i], num, cntPos, oldLevelPos[i + 1] - oldLevelPos[i]);
          cntPos += oldLevelPos[i + 1] - oldLevelPos[i];
        }
        for (org.apache.iotdb.library.dprofile.util.KLLSketchForQuantile another : otherList)
```

### RuleId[ruleID=DuplicateExpressions]
Multiple occurrences of `oldLevelPos[i + 1] - oldLevelPos[i]`
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/HeapLongKLLSketch.java`
#### Snippet
```java
          //          System.out.println("\t\t\t"+oldNum[oldLevelPos[i + 1]-1]);
          System.arraycopy(
              oldNum, oldLevelPos[i], num, numLen, oldLevelPos[i + 1] - oldLevelPos[i]);
          numLen += oldLevelPos[i + 1] - oldLevelPos[i];
        }
```

### RuleId[ruleID=DuplicateExpressions]
Multiple occurrences of `oldLevelPos[i + 1] - oldLevelPos[i]`
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/HeapLongKLLSketch.java`
#### Snippet
```java
          System.arraycopy(
              oldNum, oldLevelPos[i], num, numLen, oldLevelPos[i + 1] - oldLevelPos[i]);
          numLen += oldLevelPos[i + 1] - oldLevelPos[i];
        }
        for (org.apache.iotdb.library.dprofile.util.KLLSketchForQuantile another : otherList)
```

## RuleId[ruleID=StringBufferReplaceableByString]
### RuleId[ruleID=StringBufferReplaceableByString]
`StringBuffer buf` can be replaced with 'String'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/StringUtils.java`
#### Snippet
```java
      char maybeMinusChar = dString.charAt(ePos + 1);
      if (maybeMinusChar != '-' && maybeMinusChar != '+') {
        StringBuffer buf = new StringBuffer(dString.length() + 1);
        buf.append(dString.substring(0, ePos + 1));
        buf.append('+');
```

### RuleId[ruleID=StringBufferReplaceableByString]
`StringBuilder stringBuilder` can be replaced with 'String'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/TimeValuePair.java`
#### Snippet
```java
  @Override
  public String toString() {
    StringBuilder stringBuilder = new StringBuilder();
    stringBuilder.append(timestamp).append(" : ").append(getValue());
    return stringBuilder.toString();
```

### RuleId[ruleID=StringBufferReplaceableByString]
`StringBuilder stringBuilder` can be replaced with 'String'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/expression/QueryExpression.java`
#### Snippet
```java
  @Override
  public String toString() {
    StringBuilder stringBuilder =
        new StringBuilder("\n\t[Selected Series]:")
            .append(selectedSeries)
```

## RuleId[ruleID=UnnecessaryReturn]
### RuleId[ruleID=UnnecessaryReturn]
`return` is unnecessary as the last statement in a 'void' method
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/AuthUtils.java`
#### Snippet
```java
          return;
        default:
          return;
      }
    }
```

### RuleId[ruleID=UnnecessaryReturn]
`return` is unnecessary as the last statement in a 'void' method
in `node-commons/src/main/java/org/apache/iotdb/commons/security/encrypt/MessageDigestEncrypt.java`
#### Snippet
```java
  @Override
  public void init(String providerParameters) {
    return;
  }

```

## RuleId[ruleID=NonShortCircuitBoolean]
### RuleId[ruleID=NonShortCircuitBoolean]
Non-short-circuit boolean expression `result &= roleTmpSnapshotDir.renameTo(roleSnapshotDir)`
in `node-commons/src/main/java/org/apache/iotdb/commons/auth/role/LocalFileRoleAccessor.java`
#### Snippet
```java
    try {
      result = FileUtils.copyDir(roleFolder, roleTmpSnapshotDir);
      result &= roleTmpSnapshotDir.renameTo(roleSnapshotDir);
    } finally {
      if (roleTmpSnapshotDir.exists() && !roleTmpSnapshotDir.delete()) {
```

### RuleId[ruleID=NonShortCircuitBoolean]
Non-short-circuit boolean expression `result &= userTmpSnapshotDir.renameTo(userSnapshotDir)`
in `node-commons/src/main/java/org/apache/iotdb/commons/auth/user/LocalFileUserAccessor.java`
#### Snippet
```java
    try {
      result = FileUtils.copyDir(userFolder, userTmpSnapshotDir);
      result &= userTmpSnapshotDir.renameTo(userSnapshotDir);
    } finally {
      if (userTmpSnapshotDir.exists() && !userTmpSnapshotDir.delete()) {
```

### RuleId[ruleID=NonShortCircuitBoolean]
Non-short-circuit boolean expression `userManager.processTakeSnapshot(snapshotDir) & roleManager.processTakeSnapshot(snapshotDir)`
in `node-commons/src/main/java/org/apache/iotdb/commons/auth/authorizer/BasicAuthorizer.java`
#### Snippet
```java
  @Override
  public boolean processTakeSnapshot(File snapshotDir) throws TException, IOException {
    return userManager.processTakeSnapshot(snapshotDir)
        & roleManager.processTakeSnapshot(snapshotDir);
  }

```

### RuleId[ruleID=NonShortCircuitBoolean]
Non-short-circuit boolean expression `result &= copyDir(file.getAbsoluteFile(), targetFile)`
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/FileUtils.java`
#### Snippet
```java
      File targetFile = new File(targetDir, file.getName());
      if (file.isDirectory()) {
        result &= copyDir(file.getAbsoluteFile(), targetFile);
      } else {
        // copy file
```

### RuleId[ruleID=NonShortCircuitBoolean]
Non-short-circuit boolean expression `dp[i][j] |= dp[i - 1][j - 1]`
in `node-commons/src/main/java/org/apache/iotdb/commons/path/PartialPath.java`
#### Snippet
```java
              || nodes1[i - 1].equals(nodes2[j - 1])) {
            // if nodes1[i-1] and nodes[2] is matched, dp[i][j] = dp[i-1][j-1]
            dp[i][j] |= dp[i - 1][j - 1];
          }
        }
```

### RuleId[ruleID=NonShortCircuitBoolean]
Non-short-circuit boolean expression `newDp[j] |= dp[j - 1]`
in `node-commons/src/main/java/org/apache/iotdb/commons/path/PartialPath.java`
#### Snippet
```java
                  || lNodes[i - 1].equals(rNodes[j - 1]))) {
            // if nodes1[i-1] includes rNodes[j-1], dp[i][j] = dp[i-1][j-1]
            newDp[j] |= dp[j - 1];
          }
        }
```

### RuleId[ruleID=NonShortCircuitBoolean]
Non-short-circuit boolean expression `isClosed &= heartbeatExecutorService.awaitTermination( SyncConstant.DEFAULT_...`
in `server/src/main/java/org/apache/iotdb/db/sync/transport/client/SenderManager.java`
#### Snippet
```java
              SyncConstant.DEFAULT_WAITING_FOR_STOP_MILLISECONDS, TimeUnit.MILLISECONDS);
      heartbeatExecutorService.shutdownNow();
      isClosed &=
          heartbeatExecutorService.awaitTermination(
              SyncConstant.DEFAULT_WAITING_FOR_STOP_MILLISECONDS, TimeUnit.MILLISECONDS);
      if (!isClosed) {
        throw new PipeException(
```

### RuleId[ruleID=NonShortCircuitBoolean]
Non-short-circuit boolean expression `flushFlag |= memChunk.writeWithFlushCheck( times, columns[i], ...`
in `server/src/main/java/org/apache/iotdb/db/engine/memtable/WritableMemChunkGroup.java`
#### Snippet
```java
      }
      IWritableMemChunk memChunk = createMemChunkIfNotExistAndGet(schemaList.get(i));
      flushFlag |=
          memChunk.writeWithFlushCheck(
              times,
              columns[i],
              bitMaps == null ? null : bitMaps[i],
              schemaList.get(i).getType(),
              start,
              end);
    }
    return flushFlag;
```

### RuleId[ruleID=NonShortCircuitBoolean]
Non-short-circuit boolean expression `flushFlag |= memChunk.writeWithFlushCheck(insertTime, objectValue[i])`
in `server/src/main/java/org/apache/iotdb/db/engine/memtable/WritableMemChunkGroup.java`
#### Snippet
```java
      }
      IWritableMemChunk memChunk = createMemChunkIfNotExistAndGet(schemaList.get(i));
      flushFlag |= memChunk.writeWithFlushCheck(insertTime, objectValue[i]);
    }
    return flushFlag;
```

### RuleId[ruleID=NonShortCircuitBoolean]
Non-short-circuit boolean expression `updateRegionLeaderMap() | updateRegionPriorityMap()`
in `confignode/src/main/java/org/apache/iotdb/confignode/manager/load/balancer/RouteBalancer.java`
#### Snippet
```java
  public boolean updateRegionRouteMap() {
    synchronized (regionRouteMap) {
      return updateRegionLeaderMap() | updateRegionPriorityMap();
    }
  }
```

### RuleId[ruleID=NonShortCircuitBoolean]
Non-short-circuit boolean expression `abortRollback |= !isRunning() || !store.isRunning()`
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/ProcedureExecutor.java`
#### Snippet
```java

      boolean abortRollback = lockState != ProcedureLockState.LOCK_ACQUIRED;
      abortRollback |= !isRunning() || !store.isRunning();
      if (abortRollback) {
        return lockState;
```

## RuleId[ruleID=ConstantMathCall]
### RuleId[ruleID=ConstantMathCall]
Constant call to `sqrt()` can be simplified
in `library-udf/src/main/java/org/apache/iotdb/library/frequency/util/DWTUtil.java`
#### Snippet
```java
    this.layer = layer;
    if (method.equalsIgnoreCase("Haar") || method.equalsIgnoreCase("DB2")) {
      cc = new double[] {1 / Math.sqrt(2), 1 / Math.sqrt(2)};
    } else if (method.equalsIgnoreCase("DB4")) {
      cc =
```

### RuleId[ruleID=ConstantMathCall]
Constant call to `sqrt()` can be simplified
in `library-udf/src/main/java/org/apache/iotdb/library/frequency/util/DWTUtil.java`
#### Snippet
```java
    this.layer = layer;
    if (method.equalsIgnoreCase("Haar") || method.equalsIgnoreCase("DB2")) {
      cc = new double[] {1 / Math.sqrt(2), 1 / Math.sqrt(2)};
    } else if (method.equalsIgnoreCase("DB4")) {
      cc =
```

## RuleId[ruleID=ManualMinMaxCalculation]
### RuleId[ruleID=ManualMinMaxCalculation]
Can be replaced with 'Math.min()' call
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/UDAFSpread.java`
#### Snippet
```java
    double v = row.getDouble(0);
    if (Double.isFinite(v)) {
      doubleMin = doubleMin < v ? doubleMin : v;
      doubleMax = doubleMax > v ? doubleMax : v;
    }
```

### RuleId[ruleID=ManualMinMaxCalculation]
Can be replaced with 'Math.max()' call
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/UDAFSpread.java`
#### Snippet
```java
    if (Double.isFinite(v)) {
      doubleMin = doubleMin < v ? doubleMin : v;
      doubleMax = doubleMax > v ? doubleMax : v;
    }
  }
```

## RuleId[ruleID=AbstractClassNeverImplemented]
### RuleId[ruleID=AbstractClassNeverImplemented]
Abstract class `FlushLevelProcessor` has no concrete subclass
in `schema-engine-tag/src/main/java/org/apache/iotdb/lsm/levelProcess/FlushLevelProcessor.java`
#### Snippet
```java

/** indicates the flush method of each layer of memory nodes */
public abstract class FlushLevelProcessor<I, O>
    extends BasicLevelProcessor<I, O, Object, FlushRequestContext> {

```

### RuleId[ruleID=AbstractClassNeverImplemented]
Abstract class `BaseManagedSeriesReader` has no concrete subclass
in `server/src/main/java/org/apache/iotdb/db/query/reader/series/BaseManagedSeriesReader.java`
#### Snippet
```java
package org.apache.iotdb.db.query.reader.series;

public abstract class BaseManagedSeriesReader implements ManagedSeriesReader {

  protected volatile boolean managedByPool;
```

### RuleId[ruleID=AbstractClassNeverImplemented]
Abstract class `AbstractInnerSpaceEstimator` has no concrete subclass
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/inner/AbstractInnerSpaceEstimator.java`
#### Snippet
```java
 * its corresponding implementation.
 */
public abstract class AbstractInnerSpaceEstimator extends AbstractCompactionEstimator {
  public abstract long estimateInnerCompactionMemory(List<TsFileResource> resources);

```

## RuleId[ruleID=BoundedWildcard]
### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends ArrayList`
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/utils/KDTreeUtil.java`
#### Snippet
```java
    }

    static double median(ArrayList<ArrayList<Double>> data, int dimension) {
      ArrayList<Double> d = new ArrayList<>();
      for (ArrayList<Double> k : data) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends ArrayList`
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/utils/KDTreeUtil.java`
#### Snippet
```java
    }

    static ArrayList<ArrayList<Double>> maxMin(ArrayList<ArrayList<Double>> data, int dimensions) {
      ArrayList<ArrayList<Double>> mm = new ArrayList<>();
      ArrayList<Double> min_v = new ArrayList<>();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends ArrayList`
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/utils/KDTreeUtil.java`
#### Snippet
```java

  public ArrayList<Double> findNearest(
      ArrayList<Double> input, ArrayList<ArrayList<Double>> nearest, double[] std) {
    double min_dis = Double.MAX_VALUE;
    int min_index = 0;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends ArrayList`
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/utils/KDTreeUtil.java`
#### Snippet
```java
  private static class UtilZ {

    static double variance(ArrayList<ArrayList<Double>> data, int dimension) {
      double sum = 0d;
      for (ArrayList<Double> d : data) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Pair`
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/builtin/UDTFEqualSizeBucketOutlierSample.java`
#### Snippet
```java

  public void putPQValueFloat(
      PriorityQueue<Pair<Integer, Double>> pq, RowWindow rowWindow, PointCollector collector)
      throws IOException {
    int[] arr = new int[number];
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Pair`
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/builtin/UDTFEqualSizeBucketOutlierSample.java`
#### Snippet
```java

  public void putPQValueLong(
      PriorityQueue<Pair<Integer, Double>> pq, RowWindow rowWindow, PointCollector collector)
      throws IOException {
    int[] arr = new int[number];
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Pair`
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/builtin/UDTFEqualSizeBucketOutlierSample.java`
#### Snippet
```java

  public void putPQValueDouble(
      PriorityQueue<Pair<Integer, Double>> pq, RowWindow rowWindow, PointCollector collector)
      throws IOException {
    int[] arr = new int[number];
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Pair`
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/builtin/UDTFEqualSizeBucketOutlierSample.java`
#### Snippet
```java

  public void putPQValueInt(
      PriorityQueue<Pair<Integer, Double>> pq, RowWindow rowWindow, PointCollector collector)
      throws IOException {
    int[] arr = new int[number];
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super V`
in `node-commons/src/main/java/org/apache/iotdb/commons/path/PathPatternNode.java`
#### Snippet
```java
  }

  public void deleteValue(V value, BiConsumer<V, Set<V>> remappingFunction) {
    remappingFunction.accept(value, valueSet);
  }
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Set`
in `node-commons/src/main/java/org/apache/iotdb/commons/path/PathPatternNode.java`
#### Snippet
```java
  }

  public void deleteValue(V value, BiConsumer<V, Set<V>> remappingFunction) {
    remappingFunction.accept(value, valueSet);
  }
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super V`
in `node-commons/src/main/java/org/apache/iotdb/commons/path/PathPatternNode.java`
#### Snippet
```java
  }

  public void appendValue(V value, BiConsumer<V, Set<V>> remappingFunction) {
    remappingFunction.accept(value, valueSet);
  }
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Set`
in `node-commons/src/main/java/org/apache/iotdb/commons/path/PathPatternNode.java`
#### Snippet
```java
  }

  public void appendValue(V value, BiConsumer<V, Set<V>> remappingFunction) {
    remappingFunction.accept(value, valueSet);
  }
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends IMeasurementSchema`
in `node-commons/src/main/java/org/apache/iotdb/commons/path/AlignedPath.java`
#### Snippet
```java
  }

  public void addSchemas(List<IMeasurementSchema> schemas) {
    this.schemaList.addAll(schemas);
  }
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super PartialPath`
in `node-commons/src/main/java/org/apache/iotdb/commons/path/PathPatternTree.java`
#### Snippet
```java
      PathPatternNode<Void, VoidSerializer> node,
      Deque<String> ancestors,
      List<PartialPath> fullPaths) {
    if (node.isPathPattern()) {
      fullPaths.add(convertNodesToPartialPath(node, ancestors));
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super String`
in `node-commons/src/main/java/org/apache/iotdb/commons/path/PathPatternTree.java`
#### Snippet
```java

  private void searchDevicePattern(
      PathPatternNode<Void, VoidSerializer> curNode, List<String> nodes, Set<String> results) {
    nodes.add(curNode.getName());
    if (curNode.isPathPattern()) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super V`
in `node-commons/src/main/java/org/apache/iotdb/commons/path/PatternTreeMap.java`
#### Snippet
```java
   */
  private void searchOverlapped(
      PathPatternNode<V, VSerializer> node, String[] pathNodes, int pos, Set<V> resultSet) {
    if (pos == pathNodes.length - 1) {
      resultSet.addAll(node.getValues());
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Set`
in `node-commons/src/main/java/org/apache/iotdb/commons/path/PatternTreeMap.java`
#### Snippet
```java
      int pos,
      List<String> measurements,
      List<Set<V>> resultSet) {
    if (pos == deviceNodes.length - 1) {
      for (int i = 0; i < measurements.size(); i++) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/PathUtils.java`
#### Snippet
```java
   * (more than one node name), in template?
   */
  public static void isLegalMeasurementLists(List<List<String>> measurementLists)
      throws IllegalPathException {
    if (measurementLists == null) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/PathUtils.java`
#### Snippet
```java
   * single node name
   */
  public static void isLegalSingleMeasurementLists(List<List<String>> measurementLists)
      throws MetadataException {
    if (measurementLists == null) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PathPrivilege`
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/AuthUtils.java`
#### Snippet
```java
   */
  public static boolean hasPrivilege(
      String path, int privilegeId, List<PathPrivilege> privilegeList) {
    for (PathPrivilege pathPrivilege : privilegeList) {
      if (pathPrivilege.getPath().equals(path)
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PathPrivilege`
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/AuthUtils.java`
#### Snippet
```java
   */
  public static void removePrivilege(
      String path, int privilegeId, List<PathPrivilege> privilegeList) {
    PathPrivilege emptyPrivilege = null;
    for (PathPrivilege pathPrivilege : privilegeList) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PathPrivilege`
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/AuthUtils.java`
#### Snippet
```java
   */
  public static boolean checkPrivilege(
      String path, int privilegeId, List<PathPrivilege> privilegeList) throws AuthException {
    if (privilegeList == null) {
      return false;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PathPrivilege`
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/AuthUtils.java`
#### Snippet
```java
   * @return The privileges granted to the role.
   */
  public static Set<Integer> getPrivileges(String path, List<PathPrivilege> privilegeList)
      throws AuthException {
    if (privilegeList == null) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super List`
in `node-commons/src/main/java/org/apache/iotdb/commons/path/PartialPath.java`
#### Snippet
```java
   */
  private boolean alterPrefixPathInternal(
      List<String> prefix, List<String> path, List<String> current, List<List<String>> results) {
    if (prefix.isEmpty()) {
      current.addAll(path);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PartialPath`
in `node-commons/src/main/java/org/apache/iotdb/commons/path/PartialPath.java`
#### Snippet
```java
  }

  public static List<String> toStringList(List<PartialPath> pathList) {
    List<String> ret = new ArrayList<>();
    for (PartialPath path : pathList) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TimeValuePair`
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/SerializeUtils.java`
#### Snippet
```java

  private static void serializeFloatTVPairs(
      List<TimeValuePair> timeValuePairs, DataOutputStream dataOutputStream) throws IOException {
    for (TimeValuePair timeValuePair : timeValuePairs) {
      dataOutputStream.writeLong(timeValuePair.getTimestamp());
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TimeValuePair`
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/SerializeUtils.java`
#### Snippet
```java

  private static void serializeIntTVPairs(
      List<TimeValuePair> timeValuePairs, DataOutputStream dataOutputStream) throws IOException {
    for (TimeValuePair timeValuePair : timeValuePairs) {
      dataOutputStream.writeLong(timeValuePair.getTimestamp());
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super TimeValuePair`
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/SerializeUtils.java`
#### Snippet
```java

  private static void deserializeBooleanTVPairs(
      ByteBuffer buffer, List<TimeValuePair> ret, int size, TSDataType dataType) {
    for (int i = 0; i < size; i++) {
      long time = buffer.getLong();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TimeValuePair`
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/SerializeUtils.java`
#### Snippet
```java

  private static void serializeDoubleTVPairs(
      List<TimeValuePair> timeValuePairs, DataOutputStream dataOutputStream) throws IOException {
    for (TimeValuePair timeValuePair : timeValuePairs) {
      dataOutputStream.writeLong(timeValuePair.getTimestamp());
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TimeValuePair`
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/SerializeUtils.java`
#### Snippet
```java

  private static void serializeTextTVPairs(
      List<TimeValuePair> timeValuePairs, DataOutputStream dataOutputStream) throws IOException {
    for (TimeValuePair timeValuePair : timeValuePairs) {
      dataOutputStream.writeLong(timeValuePair.getTimestamp());
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Integer`
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/SerializeUtils.java`
#### Snippet
```java
  }

  public static void deserializeIntList(List<Integer> ints, ByteBuffer buffer) {
    int length = buffer.getInt();
    for (int i = 0; i < length; i++) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super TimeValuePair`
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/SerializeUtils.java`
#### Snippet
```java

  private static void deserializeIntTVPairs(
      ByteBuffer buffer, List<TimeValuePair> ret, int size, TSDataType dataType) {
    for (int i = 0; i < size; i++) {
      long time = buffer.getLong();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super TimeValuePair`
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/SerializeUtils.java`
#### Snippet
```java

  private static void deserializeDoubleTVPairs(
      ByteBuffer buffer, List<TimeValuePair> ret, int size, TSDataType dataType) {
    for (int i = 0; i < size; i++) {
      long time = buffer.getLong();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Integer`
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/SerializeUtils.java`
#### Snippet
```java
  }

  public static void deserializeIntSet(Set<Integer> ints, ByteBuffer buffer) {
    int length = buffer.getInt();
    for (int i = 0; i < length; i++) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super TimeValuePair`
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/SerializeUtils.java`
#### Snippet
```java

  private static void deserializeTextTVPairs(
      ByteBuffer buffer, List<TimeValuePair> ret, int size, TSDataType dataType) {
    for (int i = 0; i < size; i++) {
      long time = buffer.getLong();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super TimeValuePair`
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/SerializeUtils.java`
#### Snippet
```java

  private static void deserializeFloatTVPairs(
      ByteBuffer buffer, List<TimeValuePair> ret, int size, TSDataType dataType) {
    for (int i = 0; i < size; i++) {
      long time = buffer.getLong();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TimeValuePair`
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/SerializeUtils.java`
#### Snippet
```java

  private static void serializeLongTVPairs(
      List<TimeValuePair> timeValuePairs, DataOutputStream dataOutputStream) throws IOException {
    for (TimeValuePair timeValuePair : timeValuePairs) {
      dataOutputStream.writeLong(timeValuePair.getTimestamp());
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super TimeValuePair`
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/SerializeUtils.java`
#### Snippet
```java

  private static void deserializeLongTVPairs(
      ByteBuffer buffer, List<TimeValuePair> ret, int size, TSDataType dataType) {
    for (int i = 0; i < size; i++) {
      long time = buffer.getLong();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TimeValuePair`
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/SerializeUtils.java`
#### Snippet
```java

  private static void serializeBooleanTVPairs(
      List<TimeValuePair> timeValuePairs, DataOutputStream dataOutputStream) throws IOException {
    for (TimeValuePair timeValuePair : timeValuePairs) {
      dataOutputStream.writeLong(timeValuePair.getTimestamp());
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Consumer`
in `example/pulsar/src/main/java/org/apache/iotdb/pulsar/PulsarConsumer.java`
#### Snippet
```java
  private static final Logger logger = LoggerFactory.getLogger(PulsarConsumer.class);

  public PulsarConsumer(List<Consumer<?>> consumerList) {
    this.consumerList = consumerList;
  }
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends KafkaConsumer`
in `example/kafka/src/main/java/org/apache/iotdb/kafka/Consumer.java`
#### Snippet
```java
  private static SessionPool pool;

  private Consumer(List<KafkaConsumer<String, String>> consumerList) {
    this.consumerList = consumerList;
    pool =
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super RowRecord`
in `jdbc/src/main/java/org/apache/iotdb/jdbc/ListDataSet.java`
#### Snippet
```java
  }

  public void sort(Comparator<RowRecord> c) {
    records.sort(c);
  }
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `cli/src/main/java/org/apache/iotdb/cli/utils/IoTPrinter.java`
#### Snippet
```java
  }

  public static void printRow(List<List<String>> lists, int i, List<Integer> maxSizeList) {
    printf("|");
    int count;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `cli/src/main/java/org/apache/iotdb/tool/AbstractCsvTool.java`
#### Snippet
```java
   */
  public static Boolean writeCsvFile(
      List<String> headerNames, List<List<Object>> records, String filePath) {
    try {
      final CSVPrinterWrapper csvPrinterWrapper = new CSVPrinterWrapper(filePath);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends IMeasurementSchema`
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileWriteAlignedWithTSRecord.java`
#### Snippet
```java
      TsFileWriter tsFileWriter,
      String deviceId,
      List<IMeasurementSchema> schemas,
      long rowSize,
      long startTime,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends IMeasurementSchema`
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileWriteWithTSRecord.java`
#### Snippet
```java
      TsFileWriter tsFileWriter,
      String deviceId,
      List<IMeasurementSchema> schemas,
      long rowSize,
      long startTime,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
  }

  private void addToDataSet(List<List<Map>> listbigPaths, ListDataSet dataSet) {
    List<TSDataType> listType = new ArrayList<>();
    int i = 0;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java

  public static ByteBuffer convertTsBlock(
      List<List<Object>> valuesList, List<TSDataType> tsDataTypeList) throws IOException {
    TsBlockBuilder tsBlockBuilder = new TsBlockBuilder(tsDataTypeList);
    for (List<Object> valuesInRow : valuesList) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super String`
in `schema-engine-tag/src/main/java/org/apache/iotdb/lsm/context/applicationcontext/ApplicationContextGenerator.java`
#### Snippet
```java

  private static void setLevelProcessors(
      List<String> levelProcessorClass, Class<?> clz, int level) {
    if (level < levelProcessorClass.size()) {
      levelProcessorClass.set(level, clz.getName());
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super T`
in `schema-engine-tag/src/main/java/org/apache/iotdb/lsm/manager/BasicLSMManager.java`
#### Snippet
```java
   * @param levelProcessorsChain level processors chain
   */
  public void setLevelProcessorsChain(LevelProcessorChain<T, R, C> levelProcessorsChain) {
    this.levelProcessChain = levelProcessorsChain;
  }
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super R`
in `schema-engine-tag/src/main/java/org/apache/iotdb/lsm/manager/BasicLSMManager.java`
#### Snippet
```java
   * @param levelProcessorsChain level processors chain
   */
  public void setLevelProcessorsChain(LevelProcessorChain<T, R, C> levelProcessorsChain) {
    this.levelProcessChain = levelProcessorsChain;
  }
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super C`
in `schema-engine-tag/src/main/java/org/apache/iotdb/lsm/manager/BasicLSMManager.java`
#### Snippet
```java
   * @param levelProcessorsChain level processors chain
   */
  public void setLevelProcessorsChain(LevelProcessorChain<T, R, C> levelProcessorsChain) {
    this.levelProcessChain = levelProcessorsChain;
  }
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super T`
in `schema-engine-tag/src/main/java/org/apache/iotdb/lsm/levelProcess/LevelProcessorChain.java`
#### Snippet
```java
  ILevelProcessor<T, ?, R, C> headLevelProcess;

  public <O> ILevelProcessor<T, O, R, C> nextLevel(ILevelProcessor<T, O, R, C> next) {
    this.headLevelProcess = next;
    return next;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super R`
in `schema-engine-tag/src/main/java/org/apache/iotdb/lsm/levelProcess/LevelProcessorChain.java`
#### Snippet
```java
  ILevelProcessor<T, ?, R, C> headLevelProcess;

  public <O> ILevelProcessor<T, O, R, C> nextLevel(ILevelProcessor<T, O, R, C> next) {
    this.headLevelProcess = next;
    return next;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super C`
in `schema-engine-tag/src/main/java/org/apache/iotdb/lsm/levelProcess/LevelProcessorChain.java`
#### Snippet
```java
  ILevelProcessor<T, ?, R, C> headLevelProcess;

  public <O> ILevelProcessor<T, O, R, C> nextLevel(ILevelProcessor<T, O, R, C> next) {
    this.headLevelProcess = next;
    return next;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Integer`
in `schema-engine-tag/src/main/java/org/apache/iotdb/db/metadata/tagSchemaRegion/TagSchemaRegion.java`
#### Snippet
```java
      PartialPath devicePath,
      String[] measurements,
      Function<Integer, TSDataType> getDataType,
      TSEncoding[] encodings,
      CompressionType[] compressionTypes,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends IDeviceID`
in `schema-engine-tag/src/main/java/org/apache/iotdb/db/metadata/tagSchemaRegion/TagSchemaRegion.java`
#### Snippet
```java
  }

  private List<String> getDevicePaths(List<IDeviceID> deviceIDS) {
    List<String> devicePaths = new ArrayList<>();
    if (config.getDeviceIDTransformationMethod().equals("SHA256")) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends IDeviceID`
in `schema-engine-tag/src/main/java/org/apache/iotdb/db/metadata/tagSchemaRegion/TagSchemaRegion.java`
#### Snippet
```java
  }

  private List<SchemaEntry> getSchemaEntries(List<IDeviceID> deviceIDS) {
    List<SchemaEntry> schemaEntries = new ArrayList<>();
    for (IDeviceID deviceID : deviceIDS) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super ShowTimeSeriesResult`
in `schema-engine-tag/src/main/java/org/apache/iotdb/db/metadata/tagSchemaRegion/TagSchemaRegion.java`
#### Snippet
```java

  private void getTimeSeriesResultOfDeviceFromIDTable(
      List<ShowTimeSeriesResult> ShowTimeSeriesResults, IDeviceID deviceID) {
    Map<String, SchemaEntry> measurementMap =
        idTableWithDeviceIDList.getDeviceEntry(deviceID.toStringID()).getMeasurementMap();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Expression`
in `server/src/main/java/org/apache/iotdb/db/qp/utils/WildcardsRemover.java`
#### Snippet
```java
  }

  public List<List<Expression>> removeWildcardsFrom(List<Expression> expressions)
      throws LogicalOptimizeException {
    // One by one, remove the wildcards from the input expressions. In most cases, an expression
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PartialPath`
in `server/src/main/java/org/apache/iotdb/db/qp/logical/crud/SelectIntoOperator.java`
#### Snippet
```java
  }

  private void checkWildcardsInPartialPaths(List<PartialPath> paths)
      throws LogicalOperatorException {
    for (PartialPath path : paths) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super String`
in `cli/src/main/java/org/apache/iotdb/tool/ImportCsv.java`
#### Snippet
```java
   */
  private static boolean queryType(
      String deviceNames, HashMap<String, TSDataType> headerTypeMap, String alignedType)
      throws IoTDBConnectionException {
    String sql = "select * from " + deviceNames + " limit 1";
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super TSDataType`
in `cli/src/main/java/org/apache/iotdb/tool/ImportCsv.java`
#### Snippet
```java
   */
  private static boolean queryType(
      String deviceNames, HashMap<String, TSDataType> headerTypeMap, String alignedType)
      throws IoTDBConnectionException {
    String sql = "select * from " + deviceNames + " limit 1";
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super String`
in `cli/src/main/java/org/apache/iotdb/tool/ImportCsv.java`
#### Snippet
```java
  private static void parseHeaders(
      List<String> headerNames,
      @Nullable HashMap<String, List<String>> deviceAndMeasurementNames,
      HashMap<String, TSDataType> headerTypeMap,
      HashMap<String, String> headerNameMap)
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super String`
in `cli/src/main/java/org/apache/iotdb/tool/ImportCsv.java`
#### Snippet
```java
      List<String> headerNames,
      @Nullable HashMap<String, List<String>> deviceAndMeasurementNames,
      HashMap<String, TSDataType> headerTypeMap,
      HashMap<String, String> headerNameMap)
      throws IllegalPathException {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super TSDataType`
in `cli/src/main/java/org/apache/iotdb/tool/ImportCsv.java`
#### Snippet
```java
      List<String> headerNames,
      @Nullable HashMap<String, List<String>> deviceAndMeasurementNames,
      HashMap<String, TSDataType> headerTypeMap,
      HashMap<String, String> headerNameMap)
      throws IllegalPathException {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super String`
in `cli/src/main/java/org/apache/iotdb/tool/ImportCsv.java`
#### Snippet
```java
      @Nullable HashMap<String, List<String>> deviceAndMeasurementNames,
      HashMap<String, TSDataType> headerTypeMap,
      HashMap<String, String> headerNameMap)
      throws IllegalPathException {
    String regex = "(?<=\\()\\S+(?=\\))";
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super String`
in `cli/src/main/java/org/apache/iotdb/tool/ImportCsv.java`
#### Snippet
```java
      @Nullable HashMap<String, List<String>> deviceAndMeasurementNames,
      HashMap<String, TSDataType> headerTypeMap,
      HashMap<String, String> headerNameMap)
      throws IllegalPathException {
    String regex = "(?<=\\()\\S+(?=\\))";
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super PartialPath`
in `server/src/main/java/org/apache/iotdb/db/qp/logical/crud/QueryOperator.java`
#### Snippet
```java

  private void concatFilterPath(
      PartialPath prefix, FilterOperator operator, Set<PartialPath> filterPaths)
      throws LogicalOptimizeException, MetadataException {
    if (!operator.isLeaf()) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PartialPath`
in `server/src/main/java/org/apache/iotdb/db/qp/logical/crud/QueryOperator.java`
#### Snippet
```java
  }

  private List<PartialPath> removeStarsInDeviceWithUnique(List<PartialPath> paths)
      throws LogicalOptimizeException {
    List<PartialPath> retDevices;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PartialPath`
in `server/src/main/java/org/apache/iotdb/db/qp/executor/PlanExecutor.java`
#### Snippet
```java
  }

  private ListDataSet executeListUserPrivileges(String userName, List<PartialPath> nodeNameList)
      throws AuthException {
    User user = authorizerManager.getUser(userName);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PartialPath`
in `server/src/main/java/org/apache/iotdb/db/qp/executor/PlanExecutor.java`
#### Snippet
```java
  }

  private ListDataSet executeListRolePrivileges(String roleName, List<PartialPath> nodeNameList)
      throws AuthException {
    Role role = authorizerManager.getRole(roleName);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PartialPath`
in `server/src/main/java/org/apache/iotdb/db/qp/executor/PlanExecutor.java`
#### Snippet
```java

  private void addLockInfoToDataSet(
      List<PartialPath> paths, List<String> lockHolderList, ListDataSet dataSet) {
    for (int i = 0; i < paths.size(); i++) {
      RowRecord record = new RowRecord(0);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PartialPath`
in `server/src/main/java/org/apache/iotdb/db/qp/executor/PlanExecutor.java`
#### Snippet
```java
  }

  private void addToDataSet(Collection<PartialPath> paths, ListDataSet dataSet) {
    for (PartialPath s : paths) {
      RowRecord record = new RowRecord(0);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PartialPath`
in `server/src/main/java/org/apache/iotdb/db/qp/physical/sys/FlushPlan.java`
#### Snippet
```java
  }

  public FlushPlan(Boolean isSeq, List<PartialPath> storageGroups) {
    super(OperatorType.FLUSH);
    if (storageGroups == null) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Expression`
in `server/src/main/java/org/apache/iotdb/db/qp/physical/crud/AlignByDevicePlan.java`
#### Snippet
```java
  }

  public void calcWithoutNullColumnIndex(List<Expression> withoutNullColumns)
      throws QueryProcessException {
    // record specified without null columns, include alias
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super String`
in `server/src/main/java/org/apache/iotdb/db/qp/physical/crud/QueryPlan.java`
#### Snippet
```java

  public List<TSDataType> getWideQueryHeaders(
      List<String> respColumns, List<String> respSgColumns, boolean isJdbcQuery, BitSet aliasList)
      throws TException, MetadataException {
    List<TSDataType> seriesTypes = new ArrayList<>();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super String`
in `server/src/main/java/org/apache/iotdb/db/qp/physical/crud/QueryPlan.java`
#### Snippet
```java

  public List<TSDataType> getWideQueryHeaders(
      List<String> respColumns, List<String> respSgColumns, boolean isJdbcQuery, BitSet aliasList)
      throws TException, MetadataException {
    List<TSDataType> seriesTypes = new ArrayList<>();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PartialPath`
in `server/src/main/java/org/apache/iotdb/db/qp/physical/crud/QueryPlan.java`
#### Snippet
```java

  @Override
  public void setPaths(List<PartialPath> paths) {
    List<MeasurementPath> measurementPaths = new ArrayList<>();
    for (PartialPath path : paths) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super String`
in `server/src/main/java/org/apache/iotdb/db/qp/physical/crud/UDTFPlan.java`
#### Snippet
```java
  @Override
  public List<TSDataType> getWideQueryHeaders(
      List<String> respColumns, List<String> respSgColumns, boolean isJdbcQuery, BitSet aliasList) {
    List<TSDataType> seriesTypes = new ArrayList<>();
    for (int i = 0; i < paths.size(); i++) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super String`
in `server/src/main/java/org/apache/iotdb/db/qp/physical/crud/AggregationPlan.java`
#### Snippet
```java
  @Override
  public List<TSDataType> getWideQueryHeaders(
      List<String> respColumns, List<String> respSgColumns, boolean isJdbcQuery, BitSet aliasList)
      throws MetadataException {
    List<TSDataType> seriesTypes = new ArrayList<>();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends AggregateResult`
in `server/src/main/java/org/apache/iotdb/db/qp/physical/crud/AggregationPlan.java`
#### Snippet
```java

  public Map<String, AggregateResult> groupAggResultByLevel(
      List<AggregateResult> aggregateResults) {
    if (!groupPathsResultMap.isEmpty()) {
      groupPathsResultMap.clear();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super FilterOperator`
in `server/src/main/java/org/apache/iotdb/db/qp/strategy/optimizer/DnfFilterOptimizer.java`
#### Snippet
```java
   * @throws LogicalOptimizeException exception in DNF optimizing
   */
  private void addChildOpInAnd(FilterOperator operator, List<FilterOperator> newChildrenList)
      throws LogicalOptimizeException {
    if (operator.isLeaf()) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super FilterOperator`
in `server/src/main/java/org/apache/iotdb/db/qp/strategy/optimizer/DnfFilterOptimizer.java`
#### Snippet
```java
   * @param newChildrenList new children list
   */
  private void addChildOpInOr(FilterOperator operator, List<FilterOperator> newChildrenList) {
    if (operator.isLeaf() || operator.getFilterType() == FilterType.KW_AND) {
      newChildrenList.add(operator);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends FilterOperator`
in `server/src/main/java/org/apache/iotdb/db/qp/strategy/optimizer/DnfFilterOptimizer.java`
#### Snippet
```java

  private void dealWithLeftAndRightAndChildren(
      List<FilterOperator> leftAndChildren,
      List<FilterOperator> rightAndChildren,
      List<FilterOperator> newChildrenList)
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends FilterOperator`
in `server/src/main/java/org/apache/iotdb/db/qp/strategy/optimizer/DnfFilterOptimizer.java`
#### Snippet
```java
  private void dealWithLeftAndRightAndChildren(
      List<FilterOperator> leftAndChildren,
      List<FilterOperator> rightAndChildren,
      List<FilterOperator> newChildrenList)
      throws LogicalOptimizeException {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super FilterOperator`
in `server/src/main/java/org/apache/iotdb/db/qp/strategy/optimizer/DnfFilterOptimizer.java`
#### Snippet
```java
      List<FilterOperator> leftAndChildren,
      List<FilterOperator> rightAndChildren,
      List<FilterOperator> newChildrenList)
      throws LogicalOptimizeException {
    for (FilterOperator leftAndChild : leftAndChildren) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super FilterOperator`
in `server/src/main/java/org/apache/iotdb/db/qp/strategy/optimizer/MergeSingleFilterOptimizer.java`
#### Snippet
```java

  @SuppressWarnings("squid:S3776") // Suppress high Cognitive Complexity warning
  private int mergeSingleFilters(List<FilterOperator> ret, FilterOperator filter) {
    List<FilterOperator> children = filter.getChildren();
    List<FilterOperator> tempExtrNode = null;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends FilterOperator`
in `server/src/main/java/org/apache/iotdb/db/qp/strategy/optimizer/MergeSingleFilterOptimizer.java`
#### Snippet
```java
  }

  private boolean allIsBasic(List<FilterOperator> children) {
    for (FilterOperator child : children) {
      if (!(child instanceof BasicFunctionOperator)) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PartialPath`
in `server/src/main/java/org/apache/iotdb/db/qp/strategy/optimizer/ConcatPathOptimizer.java`
#### Snippet
```java

  private FilterOperator constructBinaryFilterTreeWithAnd(
      List<PartialPath> noStarPaths, FilterOperator operator) throws LogicalOptimizeException {
    FilterOperator filterBinaryTree = new FilterOperator(FilterType.KW_AND);
    FilterOperator currentNode = filterBinaryTree;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `server/src/main/java/org/apache/iotdb/db/qp/strategy/optimizer/ConcatPathOptimizer.java`
#### Snippet
```java

  public static <T> void cartesianProduct(
      List<List<T>> dimensionValue, List<List<T>> resultList, int layer, List<T> currentList) {
    if (layer < dimensionValue.size() - 1) {
      if (dimensionValue.get(layer).isEmpty()) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super List`
in `server/src/main/java/org/apache/iotdb/db/qp/strategy/optimizer/ConcatPathOptimizer.java`
#### Snippet
```java

  public static <T> void cartesianProduct(
      List<List<T>> dimensionValue, List<List<T>> resultList, int layer, List<T> currentList) {
    if (layer < dimensionValue.size() - 1) {
      if (dimensionValue.get(layer).isEmpty()) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PartialPath`
in `server/src/main/java/org/apache/iotdb/db/qp/strategy/optimizer/ConcatPathOptimizer.java`
#### Snippet
```java

  private FilterOperator concatFilterAndRemoveWildcards(
      List<PartialPath> fromPaths,
      FilterOperator operator,
      Set<PartialPath> filterPaths,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super PartialPath`
in `server/src/main/java/org/apache/iotdb/db/qp/strategy/optimizer/ConcatPathOptimizer.java`
#### Snippet
```java
      List<PartialPath> fromPaths,
      FilterOperator operator,
      Set<PartialPath> filterPaths,
      boolean isPrefixMatch)
      throws LogicalOptimizeException {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PartialPath`
in `server/src/main/java/org/apache/iotdb/db/qp/strategy/optimizer/ConcatPathOptimizer.java`
#### Snippet
```java

  private List<PartialPath> removeWildcardsInConcatPaths(
      List<PartialPath> originalPaths, boolean isPrefixMatch) throws LogicalOptimizeException {
    HashSet<PartialPath> actualPaths = new HashSet<>();
    try {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Expression`
in `server/src/main/java/org/apache/iotdb/db/qp/strategy/optimizer/ConcatPathOptimizer.java`
#### Snippet
```java
      List<PartialPath> prefixPaths,
      Expression expression,
      List<Expression> withoutNullColumns,
      Set<String> aliasSet)
      throws LogicalOptimizeException {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Expression`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/ExpressionUtils.java`
#### Snippet
```java
  }

  private static Expression constructBinaryFilterTreeWithAnd(List<Expression> expressions) {
    // TODO: consider AVL tree
    if (expressions.size() == 2) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/ExpressionUtils.java`
#### Snippet
```java

  public static List<Expression> reconstructFunctionExpressions(
      FunctionExpression expression, List<List<Expression>> childExpressionsList) {
    List<Expression> resultExpressions = new ArrayList<>();
    for (List<Expression> functionExpressions : childExpressionsList) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Expression`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/ExpressionUtils.java`
#### Snippet
```java
  public static List<Expression> reconstructBinaryExpressions(
      ExpressionType expressionType,
      List<Expression> leftExpressions,
      List<Expression> rightExpressions) {
    List<Expression> resultExpressions = new ArrayList<>();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Expression`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/ExpressionUtils.java`
#### Snippet
```java
      ExpressionType expressionType,
      List<Expression> leftExpressions,
      List<Expression> rightExpressions) {
    List<Expression> resultExpressions = new ArrayList<>();
    for (Expression le : leftExpressions) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Expression`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/ExpressionUtils.java`
#### Snippet
```java

  public static List<Expression> reconstructUnaryExpressions(
      UnaryExpression expression, List<Expression> childExpressions) {
    List<Expression> resultExpressions = new ArrayList<>();
    for (Expression childExpression : childExpressions) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/ExpressionUtils.java`
#### Snippet
```java

  public static <T> void cartesianProduct(
      List<List<T>> dimensionValue, List<List<T>> resultList, int layer, List<T> currentList) {
    if (layer < dimensionValue.size() - 1) {
      if (dimensionValue.get(layer).isEmpty()) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super List`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/ExpressionUtils.java`
#### Snippet
```java

  public static <T> void cartesianProduct(
      List<List<T>> dimensionValue, List<List<T>> resultList, int layer, List<T> currentList) {
    if (layer < dimensionValue.size() - 1) {
      if (dimensionValue.get(layer).isEmpty()) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Expression`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/ExpressionUtils.java`
#### Snippet
```java
  public static List<Expression> reconstructTernaryExpressions(
      Expression expression,
      List<Expression> firstExpressions,
      List<Expression> secondExpressions,
      List<Expression> thirdExpressions) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Expression`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/ExpressionUtils.java`
#### Snippet
```java
      Expression expression,
      List<Expression> firstExpressions,
      List<Expression> secondExpressions,
      List<Expression> thirdExpressions) {
    List<Expression> resultExpressions = new ArrayList<>();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Expression`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/ExpressionUtils.java`
#### Snippet
```java
      List<Expression> firstExpressions,
      List<Expression> secondExpressions,
      List<Expression> thirdExpressions) {
    List<Expression> resultExpressions = new ArrayList<>();
    for (Expression fe : firstExpressions) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `server/src/main/java/org/apache/iotdb/db/qp/physical/sys/CreateTemplatePlan.java`
#### Snippet
```java
  public CreateTemplatePlan(
      String name,
      List<List<String>> measurements,
      List<List<TSDataType>> dataTypes,
      List<List<TSEncoding>> encodings,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `server/src/main/java/org/apache/iotdb/db/qp/physical/sys/CreateTemplatePlan.java`
#### Snippet
```java
      String name,
      List<List<String>> measurements,
      List<List<TSDataType>> dataTypes,
      List<List<TSEncoding>> encodings,
      List<List<CompressionType>> compressors) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `server/src/main/java/org/apache/iotdb/db/qp/physical/sys/CreateTemplatePlan.java`
#### Snippet
```java
      List<List<String>> measurements,
      List<List<TSDataType>> dataTypes,
      List<List<TSEncoding>> encodings,
      List<List<CompressionType>> compressors) {
    // New constructor for tree-structured template where aligned measurements get individual
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `server/src/main/java/org/apache/iotdb/db/qp/physical/sys/CreateTemplatePlan.java`
#### Snippet
```java
      List<List<TSDataType>> dataTypes,
      List<List<TSEncoding>> encodings,
      List<List<CompressionType>> compressors) {
    // New constructor for tree-structured template where aligned measurements get individual
    // compressors
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends DataPartitionQueryParam`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/ClusterPartitionFetcher.java`
#### Snippet
```java
  /** split data partition query param by database */
  private Map<String, List<DataPartitionQueryParam>> splitDataPartitionQueryParam(
      List<DataPartitionQueryParam> dataPartitionQueryParams, boolean isAutoCreate) {
    List<String> devicePaths = new ArrayList<>();
    for (DataPartitionQueryParam dataPartitionQueryParam : dataPartitionQueryParams) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends DataPartitionQueryParam`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/StandalonePartitionFetcher.java`
#### Snippet
```java
  /** Split data partition query param by database. */
  private Map<String, List<DataPartitionQueryParam>> splitDataPartitionQueryParam(
      List<DataPartitionQueryParam> dataPartitionQueryParams, boolean isAutoCreate)
      throws MetadataException {
    List<String> devicePaths = new ArrayList<>();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PartialPath`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/ExpressionAnalyzer.java`
#### Snippet
```java
  public static List<Expression> removeWildcardInFilter(
      Expression predicate,
      List<PartialPath> prefixPaths,
      ISchemaTree schemaTree,
      boolean isWhere) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PartialPath`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/ExpressionAnalyzer.java`
#### Snippet
```java
   */
  public static List<Expression> concatExpressionWithSuffixPaths(
      Expression expression, List<PartialPath> prefixPaths, PathPatternTree patternTree) {
    if (expression instanceof TernaryExpression) {
      List<Expression> firstExpressions =
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PartialPath`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/ExpressionAnalyzer.java`
#### Snippet
```java
   */
  public static void constructPatternTreeFromExpression(
      Expression predicate, List<PartialPath> prefixPaths, PathPatternTree patternTree) {
    if (predicate instanceof TernaryExpression) {
      constructPatternTreeFromExpression(
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PartialPath`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/ExpressionAnalyzer.java`
#### Snippet
```java
   */
  public static List<PartialPath> concatExpressionWithSuffixPaths(
      Expression expression, List<PartialPath> prefixPaths) {
    Set<PartialPath> resultPaths = new HashSet<>();
    if (expression instanceof TernaryExpression) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Expression`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/SelectIntoUtils.java`
#### Snippet
```java
  }

  public static boolean checkIsAllRawSeriesQuery(List<Expression> expressions) {
    for (Expression expression : expressions) {
      if (!(expression instanceof TimeSeriesOperand)) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super PartialPath`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/parser/ASTVisitor.java`
#### Snippet
```java
  }

  private void checkGrantRevokePrivileges(String[] privileges, List<PartialPath> nodeNameList) {
    if (nodeNameList.isEmpty()) {
      nodeNameList.add(new PartialPath(ALL_RESULT_NODES));
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends NodeRef`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/Analysis.java`
#### Snippet
```java
  }

  public void addTypes(Map<NodeRef<Expression>, TSDataType> types) {
    this.expressionTypes.putAll(types);
  }
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super NodeRef`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/ExpressionTypeAnalyzer.java`
#### Snippet
```java

  public static void analyzeExpression(
      Map<NodeRef<Expression>, TSDataType> types, Expression expression) {
    ExpressionTypeAnalyzer analyzer = new ExpressionTypeAnalyzer();
    analyzer.analyze(expression);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super TSDataType`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/ExpressionTypeAnalyzer.java`
#### Snippet
```java

  public static void analyzeExpression(
      Map<NodeRef<Expression>, TSDataType> types, Expression expression) {
    ExpressionTypeAnalyzer analyzer = new ExpressionTypeAnalyzer();
    analyzer.analyze(expression);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PlanOptimizer`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/LogicalPlanner.java`
#### Snippet
```java
  private final List<PlanOptimizer> optimizers;

  public LogicalPlanner(MPPQueryContext context, List<PlanOptimizer> optimizers) {
    this.context = context;
    this.optimizers = optimizers;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PartialPath`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/StandaloneSchemaFetcher.java`
#### Snippet
```java
  @Override
  public ISchemaTree fetchSchemaListWithAutoCreate(
      List<PartialPath> devicePathList,
      List<String[]> measurementsList,
      List<TSDataType[]> tsDataTypesList,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Expression`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/LogicalPlanVisitor.java`
#### Snippet
```java
  }

  private boolean needTransform(Set<Expression> expressions) {
    for (Expression expression : expressions) {
      if (ExpressionAnalyzer.checkIsNeedTransform(expression)) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super String`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/ClusterSchemaFetcher.java`
#### Snippet
```java

  private void parseFetchedData(
      Binary data, ClusterSchemaTree resultSchemaTree, Set<String> databaseSet) {
    InputStream inputStream = new ByteArrayInputStream(data.getValues());
    try {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Integer`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/ClusterSchemaFetcher.java`
#### Snippet
```java
      List<Integer> indexOfMissingMeasurements,
      String[] measurements,
      Function<Integer, TSDataType> getDataType,
      TSEncoding[] encodings,
      CompressionType[] compressionTypes,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PartialPath`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/ClusterSchemaFetcher.java`
#### Snippet
```java
  @Override
  public ISchemaTree fetchSchemaListWithAutoCreate(
      List<PartialPath> devicePathList,
      List<String[]> measurementsList,
      List<TSDataType[]> tsDataTypesList,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/load/LoadTsFileNode.java`
#### Snippet
```java
  }

  public LoadTsFileNode(PlanNodeId id, List<TsFileResource> resources) {
    super(id);
    this.resources = resources;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PlanNode`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/sink/FragmentSinkNode.java`
#### Snippet
```java

  @Override
  public PlanNode cloneWithChildren(List<PlanNode> children) {
    Validate.isTrue(
        children == null || children.size() == 1,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super PartialPath`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/OperatorTreeGenerator.java`
#### Snippet
```java
  private void processTargetPathToSourceMap(
      Map<PartialPath, Map<String, String>> targetPathToSourceMap,
      Map<PartialPath, Map<String, InputLocation>> targetPathToSourceInputLocationMap,
      Map<PartialPath, Map<String, TSDataType>> targetPathToDataTypeMap,
      Map<String, InputLocation> sourceColumnToInputLocationMap,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Map`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/OperatorTreeGenerator.java`
#### Snippet
```java
  private void processTargetPathToSourceMap(
      Map<PartialPath, Map<String, String>> targetPathToSourceMap,
      Map<PartialPath, Map<String, InputLocation>> targetPathToSourceInputLocationMap,
      Map<PartialPath, Map<String, TSDataType>> targetPathToDataTypeMap,
      Map<String, InputLocation> sourceColumnToInputLocationMap,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super PartialPath`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/OperatorTreeGenerator.java`
#### Snippet
```java
      Map<PartialPath, Map<String, String>> targetPathToSourceMap,
      Map<PartialPath, Map<String, InputLocation>> targetPathToSourceInputLocationMap,
      Map<PartialPath, Map<String, TSDataType>> targetPathToDataTypeMap,
      Map<String, InputLocation> sourceColumnToInputLocationMap,
      TypeProvider typeProvider) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Map`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/OperatorTreeGenerator.java`
#### Snippet
```java
      Map<PartialPath, Map<String, String>> targetPathToSourceMap,
      Map<PartialPath, Map<String, InputLocation>> targetPathToSourceInputLocationMap,
      Map<PartialPath, Map<String, TSDataType>> targetPathToDataTypeMap,
      Map<String, InputLocation> sourceColumnToInputLocationMap,
      TypeProvider typeProvider) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends InputLocation`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/OperatorTreeGenerator.java`
#### Snippet
```java
      Map<PartialPath, Map<String, InputLocation>> targetPathToSourceInputLocationMap,
      Map<PartialPath, Map<String, TSDataType>> targetPathToDataTypeMap,
      Map<String, InputLocation> sourceColumnToInputLocationMap,
      TypeProvider typeProvider) {
    for (Map.Entry<PartialPath, Map<String, String>> entry : targetPathToSourceMap.entrySet()) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends OutputColumn`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/OperatorTreeGenerator.java`
#### Snippet
```java

  private List<ColumnMerger> createColumnMergers(
      List<OutputColumn> outputColumns, TimeComparator timeComparator) {
    List<ColumnMerger> mergers = new ArrayList<>(outputColumns.size());
    for (OutputColumn outputColumn : outputColumns) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/OperatorTreeGenerator.java`
#### Snippet
```java

  private List<InputLocation[]> calcInputLocationList(
      AggregationDescriptor descriptor, Map<String, List<InputLocation>> layout) {
    List<List<String>> inputColumnNames = descriptor.getInputColumnNamesList();
    List<InputLocation[]> inputLocationList = new ArrayList<>();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/PlanGraphPrinter.java`
#### Snippet
```java
  }

  private String getLine(List<List<String>> children, int child, int line) {
    if (line < children.get(child).size()) {
      return children.get(child).get(line);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super String`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/PlanGraphPrinter.java`
#### Snippet
```java

  private void drawSourceTargetPath(
      List<String> boxValue,
      List<Pair<String, PartialPath>> sourceTargetPathPairList,
      Map<String, Boolean> targetDeviceToAlignedMap) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Pair`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/PlanGraphPrinter.java`
#### Snippet
```java
  private void drawSourceTargetPath(
      List<String> boxValue,
      List<Pair<String, PartialPath>> sourceTargetPathPairList,
      Map<String, Boolean> targetDeviceToAlignedMap) {
    for (Pair<String, PartialPath> sourceTargetPathPair : sourceTargetPathPairList) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/PlanGraphPrinter.java`
#### Snippet
```java
  }

  private static int getChildMidPosition(List<List<String>> children, int idx) {
    int left = 0;
    for (int i = 0; i < idx; i++) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/PlanGraphPrinter.java`
#### Snippet
```java
    }

    public void calculateBoxParams(List<List<String>> childBoxStrings) {
      int childrenWidth = 0;
      for (List<String> childBoxString : childBoxStrings) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/PlanGraphPrinter.java`
#### Snippet
```java
  }

  private int getChildrenLineCount(List<List<String>> children) {
    int count = 0;
    for (List<String> child : children) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends AggregationDescriptor`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/process/AggregationNode.java`
#### Snippet
```java
   */
  public static List<AggregationDescriptor> getDeduplicatedDescriptors(
      List<AggregationDescriptor> aggregationDescriptors) {
    Map<String, Integer> columnToIndexMap = new HashMap<>();
    boolean[] removedIndexes = new boolean[aggregationDescriptors.size()];
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Pair`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/AnalyzeVisitor.java`
#### Snippet
```java
      Analysis analysis,
      QueryStatement queryStatement,
      List<Pair<Expression, String>> outputExpressions) {
    if (queryStatement.isSelectInto()) {
      analysis.setRespDatasetHeader(
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Pair`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/AnalyzeVisitor.java`
#### Snippet
```java
      Analysis analysis,
      QueryStatement queryStatement,
      List<Pair<Expression, String>> outputExpressions) {
    Expression deviceExpression =
        new TimeSeriesOperand(new MeasurementPath(new PartialPath(DEVICE, false), TSDataType.TEXT));
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`>
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/AnalyzeVisitor.java`
#### Snippet
```java
      Analysis analysis,
      QueryStatement queryStatement,
      Map<Integer, List<Pair<Expression, String>>> outputExpressionMap,
      List<Pair<Expression, String>> outputExpressions) {
    if (!queryStatement.isGroupByLevel()) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Pair`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/AnalyzeVisitor.java`
#### Snippet
```java
      QueryStatement queryStatement,
      Map<Integer, List<Pair<Expression, String>>> outputExpressionMap,
      List<Pair<Expression, String>> outputExpressions) {
    if (!queryStatement.isGroupByLevel()) {
      return;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super String`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/AnalyzeVisitor.java`
#### Snippet
```java
      LoadTsFileStatement statement,
      File tsFile,
      Map<String, Long> device2MinTime,
      Map<String, Long> device2MaxTime,
      Map<String, Map<MeasurementSchema, File>> device2Schemas,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super String`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/AnalyzeVisitor.java`
#### Snippet
```java
      File tsFile,
      Map<String, Long> device2MinTime,
      Map<String, Long> device2MaxTime,
      Map<String, Map<MeasurementSchema, File>> device2Schemas,
      Map<String, Pair<Boolean, File>> device2IsAligned)
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super String`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/AnalyzeVisitor.java`
#### Snippet
```java
      Map<String, Long> device2MinTime,
      Map<String, Long> device2MaxTime,
      Map<String, Map<MeasurementSchema, File>> device2Schemas,
      Map<String, Pair<Boolean, File>> device2IsAligned)
      throws IOException, VerifyMetadataException {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super String`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/AnalyzeVisitor.java`
#### Snippet
```java
      Map<String, Long> device2MaxTime,
      Map<String, Map<MeasurementSchema, File>> device2Schemas,
      Map<String, Pair<Boolean, File>> device2IsAligned)
      throws IOException, VerifyMetadataException {
    try (TsFileSequenceReader reader = new TsFileSequenceReader(tsFile.getAbsolutePath())) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Pair`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/AnalyzeVisitor.java`
#### Snippet
```java
      ISchemaTree schemaTree,
      Map<String, Map<MeasurementSchema, File>> device2Schemas,
      Map<String, Pair<Boolean, File>> device2IsAligned)
      throws VerifyMetadataException, IllegalPathException {
    for (Map.Entry<String, Map<MeasurementSchema, File>> entry : device2Schemas.entrySet()) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends MeasurementPath`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/AnalyzeVisitor.java`
#### Snippet
```java

  private Analysis analyzeLast(
      Analysis analysis, List<MeasurementPath> allSelectedPath, ISchemaTree schemaTree) {
    Set<Expression> sourceExpressions;
    List<SortItem> sortItemList = analysis.getMergeOrderParameter().getSortItemList();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Expression`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/AnalyzeVisitor.java`
#### Snippet
```java

  private void checkDataTypeConsistencyInGroupByLevel(
      Analysis analysis, Map<Expression, Set<Expression>> groupByLevelExpressions) {
    for (Expression groupedAggregationExpression : groupByLevelExpressions.keySet()) {
      TSDataType checkedDataType = analysis.getType(groupedAggregationExpression);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Set`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/AnalyzeVisitor.java`
#### Snippet
```java

  private void checkDataTypeConsistencyInGroupByLevel(
      Analysis analysis, Map<Expression, Set<Expression>> groupByLevelExpressions) {
    for (Expression groupedAggregationExpression : groupByLevelExpressions.keySet()) {
      TSDataType checkedDataType = analysis.getType(groupedAggregationExpression);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Map`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/AnalyzeVisitor.java`
#### Snippet
```java
  }

  private void analyzeSchemaProps(List<Map<String, String>> propsList) {
    if (propsList == null) {
      return;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Expression`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/AnalyzeVisitor.java`
#### Snippet
```java
      Analysis analysis,
      Expression expression,
      Map<Expression, Set<Expression>> groupByLevelExpressions,
      Map<Expression, Set<Expression>> groupedExpressionToRawExpressionsMap) {
    for (Expression groupedAggregationExpression :
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Set`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/AnalyzeVisitor.java`
#### Snippet
```java
      Expression expression,
      Map<Expression, Set<Expression>> groupByLevelExpressions,
      Map<Expression, Set<Expression>> groupedExpressionToRawExpressionsMap) {
    for (Expression groupedAggregationExpression :
        ExpressionAnalyzer.searchAggregationExpressions(expression)) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PartialPath`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/AnalyzeVisitor.java`
#### Snippet
```java
      Analysis analysis,
      QueryStatement queryStatement,
      Set<PartialPath> deviceSet,
      List<Pair<Expression, String>> outputExpressions) {
    if (!queryStatement.isSelectInto()) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Pair`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/AnalyzeVisitor.java`
#### Snippet
```java
      QueryStatement queryStatement,
      Set<PartialPath> deviceSet,
      List<Pair<Expression, String>> outputExpressions) {
    if (!queryStatement.isSelectInto()) {
      return;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super String`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/AnalyzeVisitor.java`
#### Snippet
```java
      Analysis analysis,
      QueryStatement queryStatement,
      Map<String, Set<Expression>> deviceToAggregationExpressions) {
    if (!queryStatement.isAggregationQuery()) {
      return;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Pair`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/AnalyzeVisitor.java`
#### Snippet
```java
  private ISchemaTree autoCreateSchema(
      Map<String, Map<MeasurementSchema, File>> device2Schemas,
      Map<String, Pair<Boolean, File>> device2IsAligned)
      throws IllegalPathException {
    List<PartialPath> deviceList = new ArrayList<>();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Expression`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/AnalyzeVisitor.java`
#### Snippet
```java
   */
  private void checkDataTypeConsistencyInAlignByDevice(
      Analysis analysis, List<Expression> expressions) {
    TSDataType checkedDataType = analysis.getType(expressions.get(0));
    for (Expression expression : expressions) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Pair`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/AnalyzeVisitor.java`
#### Snippet
```java
      Analysis analysis,
      QueryStatement queryStatement,
      List<Pair<Expression, String>> outputExpressions) {
    if (!queryStatement.isSelectInto()) {
      return;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PartialPath`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/AnalyzeVisitor.java`
#### Snippet
```java
      QueryStatement queryStatement,
      ISchemaTree schemaTree,
      Set<PartialPath> deviceSet,
      Map<String, Set<Expression>> deviceToAggregationExpressions) {
    if (!queryStatement.hasHaving()) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super String`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/AnalyzeVisitor.java`
#### Snippet
```java
      ISchemaTree schemaTree,
      Set<PartialPath> deviceSet,
      Map<String, Set<Expression>> deviceToAggregationExpressions) {
    if (!queryStatement.hasHaving()) {
      return;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PartialPath`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/AnalyzeVisitor.java`
#### Snippet
```java
      QueryStatement queryStatement,
      ISchemaTree schemaTree,
      Set<PartialPath> deviceSet) {
    List<Pair<Expression, String>> outputExpressions = new ArrayList<>();
    Map<String, Set<Expression>> deviceToSelectExpressions = new HashMap<>();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PartialPath`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/metedata/read/SchemaQueryScanNode.java`
#### Snippet
```java
  }

  public void setPathPatternList(List<PartialPath> pathPatternList) {
    if (pathPatternList.size() == 1) {
      this.path = pathPatternList.get(0);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Set`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/LogicalPlanBuilder.java`
#### Snippet
```java
  private PlanNode createGroupByTLevelNode(
      List<PlanNode> children,
      Map<Expression, Set<Expression>> groupByLevelExpressions,
      AggregationStep curStep,
      GroupByTimeParameter groupByTimeParameter,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super PartialPath`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/LogicalPlanBuilder.java`
#### Snippet
```java
      Ordering scanOrder,
      boolean needCheckAscending,
      Map<PartialPath, List<AggregationDescriptor>> ascendingAggregations,
      Map<PartialPath, List<AggregationDescriptor>> descendingAggregations) {
    AggregationDescriptor aggregationDescriptor =
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super PartialPath`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/LogicalPlanBuilder.java`
#### Snippet
```java
      boolean needCheckAscending,
      Map<PartialPath, List<AggregationDescriptor>> ascendingAggregations,
      Map<PartialPath, List<AggregationDescriptor>> descendingAggregations) {
    AggregationDescriptor aggregationDescriptor =
        new AggregationDescriptor(
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/LogicalPlanBuilder.java`
#### Snippet
```java
  private PlanNode createGroupByTagNode(
      List<String> tagKeys,
      Map<List<String>, LinkedHashMap<Expression, List<Expression>>>
          tagValuesToGroupedTimeseriesOperands,
      Collection<Expression> groupByTagOutputExpressions,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends LinkedHashMap`>
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/LogicalPlanBuilder.java`
#### Snippet
```java
  private PlanNode createGroupByTagNode(
      List<String> tagKeys,
      Map<List<String>, LinkedHashMap<Expression, List<Expression>>>
          tagValuesToGroupedTimeseriesOperands,
      Collection<Expression> groupByTagOutputExpressions,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Expression`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/LogicalPlanBuilder.java`
#### Snippet
```java

  private List<AggregationDescriptor> constructAggregationDescriptorList(
      Set<Expression> aggregationExpressions, AggregationStep curStep) {
    return aggregationExpressions.stream()
        .map(
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Expression`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/LogicalPlanBuilder.java`
#### Snippet
```java
  }

  private void updateTypeProvider(Collection<Expression> expressions) {
    if (expressions == null) {
      return;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PartialPath`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/metedata/write/CreateMultiTimeSeriesNode.java`
#### Snippet
```java
  public CreateMultiTimeSeriesNode(
      PlanNodeId id,
      List<PartialPath> paths,
      List<TSDataType> dataTypes,
      List<TSEncoding> encodings,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Map`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/metedata/write/CreateMultiTimeSeriesNode.java`
#### Snippet
```java
      List<TSEncoding> encodings,
      List<CompressionType> compressors,
      List<Map<String, String>> propsList,
      List<String> aliasList,
      List<Map<String, String>> tagsList,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Map`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/metedata/write/CreateMultiTimeSeriesNode.java`
#### Snippet
```java
      List<Map<String, String>> propsList,
      List<String> aliasList,
      List<Map<String, String>> tagsList,
      List<Map<String, String>> attributesList) {
    super(id);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Map`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/metedata/write/CreateMultiTimeSeriesNode.java`
#### Snippet
```java
      List<String> aliasList,
      List<Map<String, String>> tagsList,
      List<Map<String, String>> attributesList) {
    super(id);
    measurementGroupMap = new HashMap<>();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super QueryFilter`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/rewriter/DnfFilterOptimizer.java`
#### Snippet
```java
   * @throws StatementAnalyzeException exception in DNF optimizing
   */
  private void addChildOpInAnd(QueryFilter operator, List<QueryFilter> newChildrenList)
      throws StatementAnalyzeException {
    if (operator.isLeaf()) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends QueryFilter`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/rewriter/DnfFilterOptimizer.java`
#### Snippet
```java

  private void dealWithLeftAndRightAndChildren(
      List<QueryFilter> leftAndChildren,
      List<QueryFilter> rightAndChildren,
      List<QueryFilter> newChildrenList)
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends QueryFilter`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/rewriter/DnfFilterOptimizer.java`
#### Snippet
```java
  private void dealWithLeftAndRightAndChildren(
      List<QueryFilter> leftAndChildren,
      List<QueryFilter> rightAndChildren,
      List<QueryFilter> newChildrenList)
      throws StatementAnalyzeException {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super QueryFilter`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/rewriter/DnfFilterOptimizer.java`
#### Snippet
```java
      List<QueryFilter> leftAndChildren,
      List<QueryFilter> rightAndChildren,
      List<QueryFilter> newChildrenList)
      throws StatementAnalyzeException {
    for (QueryFilter leftAndChild : leftAndChildren) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super QueryFilter`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/rewriter/DnfFilterOptimizer.java`
#### Snippet
```java
   * @param newChildrenList new children list
   */
  private void addChildOpInOr(QueryFilter operator, List<QueryFilter> newChildrenList) {
    if (operator.isLeaf() || operator.getFilterType() == FilterType.KW_AND) {
      newChildrenList.add(operator);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PlanNode`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/distribution/ExchangeNodeAdder.java`
#### Snippet
```java

  private TRegionReplicaSet calculateSchemaRegionByChildren(
      List<PlanNode> children, NodeGroupContext context) {
    // We always make the schemaRegion of MetaMergeNode to be the same as its first child.
    return context.getNodeDistribution(children.get(0).getPlanNodeId()).region;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super QueryFilter`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/rewriter/MergeSingleFilterOptimizer.java`
#### Snippet
```java

  @SuppressWarnings("squid:S3776") // Suppress high Cognitive Complexity warning
  private int mergeSingleFilters(List<QueryFilter> ret, QueryFilter filter) {
    List<QueryFilter> children = filter.getChildren();
    List<QueryFilter> tempExtrNode = null;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends QueryFilter`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/rewriter/MergeSingleFilterOptimizer.java`
#### Snippet
```java
  }

  private boolean allIsBasic(List<QueryFilter> children) {
    for (QueryFilter child : children) {
      if (!(child instanceof BasicFunctionFilter)) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PipeSink`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/execution/config/sys/sync/ShowPipeSinkTask.java`
#### Snippet
```java

  public static void buildTSBlockByPipeSink(
      List<PipeSink> pipeSinkList, SettableFuture<ConfigTaskResult> future) {
    List<TSDataType> outputDataTypes =
        ColumnHeaderConstant.showPipeSinkColumnHeaders.stream()
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super ConfigTaskResult`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/execution/config/sys/sync/ShowPipeSinkTask.java`
#### Snippet
```java

  public static void buildTSBlockByPipeSink(
      List<PipeSink> pipeSinkList, SettableFuture<ConfigTaskResult> future) {
    List<TSDataType> outputDataTypes =
        ColumnHeaderConstant.showPipeSinkColumnHeaders.stream()
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/distribution/SourceRewriter.java`
#### Snippet
```java
      GroupByTagNode root,
      SlidingWindowAggregationNode slidingWindowNode,
      Map<TRegionReplicaSet, List<SeriesAggregationSourceNode>> sourceGroup,
      DistributionPlanContext context) {
    GroupByTagNode newRoot = (GroupByTagNode) root.clone();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/distribution/SourceRewriter.java`
#### Snippet
```java
      GroupByLevelNode root,
      SlidingWindowAggregationNode slidingWindowNode,
      Map<TRegionReplicaSet, List<SeriesAggregationSourceNode>> sourceGroup,
      DistributionPlanContext context) {
    GroupByLevelNode newRoot = (GroupByLevelNode) root.clone();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/distribution/SourceRewriter.java`
#### Snippet
```java
  private GroupByLevelNode groupSourcesForGroupByLevel(
      GroupByLevelNode root,
      Map<TRegionReplicaSet, List<SeriesAggregationSourceNode>> sourceGroup,
      DistributionPlanContext context) {
    GroupByLevelNode newRoot = (GroupByLevelNode) root.clone();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/distribution/SourceRewriter.java`
#### Snippet
```java
  private GroupByTagNode groupSourcesForGroupByTag(
      GroupByTagNode root,
      Map<TRegionReplicaSet, List<SeriesAggregationSourceNode>> sourceGroup,
      DistributionPlanContext context) {
    GroupByTagNode newRoot = (GroupByTagNode) root.clone();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends FragmentInstance`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/execution/QueryExecution.java`
#### Snippet
```java
  }

  private String printFragmentInstances(List<FragmentInstance> instances) {
    StringBuilder ret = new StringBuilder();
    for (FragmentInstance instance : instances) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super ConfigTaskResult`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/execution/config/metadata/CountStorageGroupTask.java`
#### Snippet
```java
  }

  public static void buildTSBlock(int storageGroupNum, SettableFuture<ConfigTaskResult> future) {
    TsBlockBuilder builder = new TsBlockBuilder(Collections.singletonList(TSDataType.INT32));
    builder.getTimeColumnBuilder().writeLong(0L);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super ConfigTaskResult`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/execution/config/metadata/ShowTTLTask.java`
#### Snippet
```java

  public static void buildTSBlock(
      Map<String, Long> storageGroupToTTL, SettableFuture<ConfigTaskResult> future) {
    List<TSDataType> outputDataTypes =
        ColumnHeaderConstant.showTTLColumnHeaders.stream()
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends ByteBuffer`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/execution/config/metadata/ShowFunctionsTask.java`
#### Snippet
```java

  public static void buildTsBlock(
      List<ByteBuffer> allUDFInformation, SettableFuture<ConfigTaskResult> future) {
    List<TSDataType> outputDataTypes =
        ColumnHeaderConstant.showFunctionsColumnHeaders.stream()
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super ConfigTaskResult`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/execution/config/metadata/ShowFunctionsTask.java`
#### Snippet
```java

  public static void buildTsBlock(
      List<ByteBuffer> allUDFInformation, SettableFuture<ConfigTaskResult> future) {
    List<TSDataType> outputDataTypes =
        ColumnHeaderConstant.showFunctionsColumnHeaders.stream()
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PartialPath`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/execution/config/metadata/template/ShowPathSetTemplateTask.java`
#### Snippet
```java

  public static void buildTSBlock(
      List<PartialPath> listPath, SettableFuture<ConfigTaskResult> future) {
    List<TSDataType> outputDataTypes =
        ColumnHeaderConstant.showPathSetTemplateHeaders.stream()
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super ConfigTaskResult`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/execution/config/metadata/template/ShowPathSetTemplateTask.java`
#### Snippet
```java

  public static void buildTSBlock(
      List<PartialPath> listPath, SettableFuture<ConfigTaskResult> future) {
    List<TSDataType> outputDataTypes =
        ColumnHeaderConstant.showPathSetTemplateHeaders.stream()
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super ConfigTaskResult`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/execution/config/metadata/template/ShowNodesInSchemaTemplateTask.java`
#### Snippet
```java
  }

  public static void buildTSBlock(Template template, SettableFuture<ConfigTaskResult> future) {
    List<TSDataType> outputDataTypes =
        ColumnHeaderConstant.showNodesInSchemaTemplateHeaders.stream()
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super ConfigTaskResult`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/execution/config/metadata/template/ShowSchemaTemplateTask.java`
#### Snippet
```java

  public static void buildTSBlock(
      List<Template> templateList, SettableFuture<ConfigTaskResult> future) {
    List<TSDataType> outputDataTypes =
        ColumnHeaderConstant.showSchemaTemplateHeaders.stream()
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends FragmentInstance`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/scheduler/FragmentInstanceDispatcherImpl.java`
#### Snippet
```java
  }

  private Future<FragInstanceDispatchResult> dispatchWriteSync(List<FragmentInstance> instances) {
    for (FragmentInstance instance : instances) {
      try (SetThreadName threadName = new SetThreadName(instance.getId().getFullId())) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends FragmentInstance`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/scheduler/FragmentInstanceDispatcherImpl.java`
#### Snippet
```java
  //  unsafe for current FragmentInstance scheduler framework. We need to implement the
  //  topological dispatch according to dependency relations between FragmentInstances
  private Future<FragInstanceDispatchResult> dispatchRead(List<FragmentInstance> instances) {
    return executor.submit(
        () -> {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PartialPath`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/execution/config/executor/ClusterConfigTaskExecutor.java`
#### Snippet
```java
  }

  private ByteBuffer serializePatternListToByteBuffer(List<PartialPath> patternList) {
    PathPatternTree patternTree = new PathPatternTree();
    for (PartialPath pathPattern : patternList) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends FragmentInstance`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/scheduler/load/LoadTsFileDispatcherImpl.java`
#### Snippet
```java

  @Override
  public Future<FragInstanceDispatchResult> dispatch(List<FragmentInstance> instances) {
    return executor.submit(
        () -> {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends File`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/statement/crud/LoadTsFileStatement.java`
#### Snippet
```java
  }

  private void sortTsFiles(List<File> files) {
    files.sort(
        (o1, o2) -> {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Expression`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/expression/leaf/TimestampOperand.java`
#### Snippet
```java

  @Override
  public void concat(List<PartialPath> prefixPaths, List<Expression> resultExpressions) {
    resultExpressions.add(this);
  }
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Expression`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/expression/leaf/TimestampOperand.java`
#### Snippet
```java
  public void removeWildcards(
      org.apache.iotdb.db.qp.utils.WildcardsRemover wildcardsRemover,
      List<Expression> resultExpressions)
      throws LogicalOptimizeException {
    resultExpressions.add(this);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super PartialPath`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/expression/leaf/TimestampOperand.java`
#### Snippet
```java

  @Override
  public void collectPaths(Set<PartialPath> pathSet) {
    pathSet.add(TIMESTAMP_PARTIAL_PATH);
  }
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super ResultColumn`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/expression/ResultColumn.java`
#### Snippet
```java
   */
  public void removeWildcards(
      WildcardsRemover wildcardsRemover, List<ResultColumn> resultColumns, boolean needAliasCheck)
      throws LogicalOptimizeException {
    List<Expression> resultExpressions = new ArrayList<>();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super ResultColumn`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/expression/ResultColumn.java`
#### Snippet
```java
   */
  public void concat(
      List<PartialPath> prefixPaths, List<ResultColumn> resultColumns, boolean needAliasCheck)
      throws LogicalOptimizeException {
    List<Expression> resultExpressions = new ArrayList<>();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Expression`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/expression/leaf/ConstantOperand.java`
#### Snippet
```java
  public void removeWildcards(
      org.apache.iotdb.db.qp.utils.WildcardsRemover wildcardsRemover,
      List<Expression> resultExpressions) {
    resultExpressions.add(this);
  }
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Expression`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/expression/leaf/ConstantOperand.java`
#### Snippet
```java

  @Override
  public void concat(List<PartialPath> prefixPaths, List<Expression> resultExpressions) {
    resultExpressions.add(this);
  }
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super PartialPath`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/expression/leaf/TimeSeriesOperand.java`
#### Snippet
```java

  @Override
  public void collectPaths(Set<PartialPath> pathSet) {
    pathSet.add(path);
  }
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Expression`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/expression/leaf/TimeSeriesOperand.java`
#### Snippet
```java
  public void removeWildcards(
      org.apache.iotdb.db.qp.utils.WildcardsRemover wildcardsRemover,
      List<Expression> resultExpressions)
      throws LogicalOptimizeException {
    for (PartialPath actualPath : wildcardsRemover.removeWildcardFrom(path)) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/expression/leaf/TimeSeriesOperand.java`
#### Snippet
```java
  @Override
  public void bindInputLayerColumnIndexWithExpression(
      Map<String, List<InputLocation>> inputLocations) {
    final String digest = toString();

```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PartialPath`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/expression/leaf/TimeSeriesOperand.java`
#### Snippet
```java

  @Override
  public void concat(List<PartialPath> prefixPaths, List<Expression> resultExpressions) {
    for (PartialPath prefixPath : prefixPaths) {
      resultExpressions.add(new TimeSeriesOperand(prefixPath.concatPath(path)));
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Expression`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/expression/leaf/TimeSeriesOperand.java`
#### Snippet
```java

  @Override
  public void concat(List<PartialPath> prefixPaths, List<Expression> resultExpressions) {
    for (PartialPath prefixPath : prefixPaths) {
      resultExpressions.add(new TimeSeriesOperand(prefixPath.concatPath(path)));
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Expression`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/expression/leaf/NullOperand.java`
#### Snippet
```java

  @Override
  public void removeWildcards(WildcardsRemover wildcardsRemover, List<Expression> resultExpressions)
      throws LogicalOptimizeException {
    resultExpressions.add(this);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Expression`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/expression/leaf/NullOperand.java`
#### Snippet
```java

  @Override
  public void concat(List<PartialPath> prefixPaths, List<Expression> resultExpressions) {
    resultExpressions.add(this);
  }
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Expression`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/expression/unary/UnaryExpression.java`
#### Snippet
```java

  @Override
  public final void concat(List<PartialPath> prefixPaths, List<Expression> resultExpressions) {
    List<Expression> resultExpressionsForRecursion = new ArrayList<>();
    expression.concat(prefixPaths, resultExpressionsForRecursion);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Expression`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/expression/unary/UnaryExpression.java`
#### Snippet
```java
  @Override
  public final void removeWildcards(
      WildcardsRemover wildcardsRemover, List<Expression> resultExpressions)
      throws LogicalOptimizeException {
    List<Expression> resultExpressionsForRecursion = new ArrayList<>();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Expression`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/expression/binary/BinaryExpression.java`
#### Snippet
```java

  private void reconstruct(
      List<Expression> leftExpressions,
      List<Expression> rightExpressions,
      List<Expression> resultExpressions) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Expression`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/expression/binary/BinaryExpression.java`
#### Snippet
```java
  private void reconstruct(
      List<Expression> leftExpressions,
      List<Expression> rightExpressions,
      List<Expression> resultExpressions) {
    for (Expression le : leftExpressions) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Expression`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/expression/binary/BinaryExpression.java`
#### Snippet
```java
      List<Expression> leftExpressions,
      List<Expression> rightExpressions,
      List<Expression> resultExpressions) {
    for (Expression le : leftExpressions) {
      for (Expression re : rightExpressions) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Expression`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/expression/multi/FunctionExpression.java`
#### Snippet
```java

  @Override
  public void concat(List<PartialPath> prefixPaths, List<Expression> resultExpressions) {
    List<List<Expression>> resultExpressionsForRecursionList = new ArrayList<>();

```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super String`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/expression/multi/FunctionExpression.java`
#### Snippet
```java
  @Override
  public void constructUdfExecutors(
      Map<String, UDTFExecutor> expressionName2Executor, ZoneId zoneId) {
    String expressionString = getExpressionString();
    if (expressionName2Executor.containsKey(expressionString)) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super UDTFExecutor`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/expression/multi/FunctionExpression.java`
#### Snippet
```java
  @Override
  public void constructUdfExecutors(
      Map<String, UDTFExecutor> expressionName2Executor, ZoneId zoneId) {
    String expressionString = getExpressionString();
    if (expressionName2Executor.containsKey(expressionString)) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Expression`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/expression/multi/FunctionExpression.java`
#### Snippet
```java
  public void removeWildcards(
      org.apache.iotdb.db.qp.utils.WildcardsRemover wildcardsRemover,
      List<Expression> resultExpressions)
      throws LogicalOptimizeException {
    for (List<Expression> functionExpression : wildcardsRemover.removeWildcardsFrom(expressions)) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Expression`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/expression/ternary/TernaryExpression.java`
#### Snippet
```java

  private void reconstruct(
      List<Expression> firstExpressions,
      List<Expression> secondExpressions,
      List<Expression> thirdExpressions,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Expression`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/expression/ternary/TernaryExpression.java`
#### Snippet
```java
  private void reconstruct(
      List<Expression> firstExpressions,
      List<Expression> secondExpressions,
      List<Expression> thirdExpressions,
      List<Expression> resultExpressions) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Expression`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/expression/ternary/TernaryExpression.java`
#### Snippet
```java
      List<Expression> firstExpressions,
      List<Expression> secondExpressions,
      List<Expression> thirdExpressions,
      List<Expression> resultExpressions) {
    for (Expression fe : firstExpressions) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Expression`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/expression/ternary/TernaryExpression.java`
#### Snippet
```java
      List<Expression> secondExpressions,
      List<Expression> thirdExpressions,
      List<Expression> resultExpressions) {
    for (Expression fe : firstExpressions) {
      for (Expression se : secondExpressions)
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/statement/metadata/template/CreateSchemaTemplateStatement.java`
#### Snippet
```java
  public CreateSchemaTemplateStatement(
      String name,
      List<List<String>> measurements,
      List<List<TSDataType>> dataTypes,
      List<List<TSEncoding>> encodings,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/statement/metadata/template/CreateSchemaTemplateStatement.java`
#### Snippet
```java
      String name,
      List<List<String>> measurements,
      List<List<TSDataType>> dataTypes,
      List<List<TSEncoding>> encodings,
      List<List<CompressionType>> compressors) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/statement/metadata/template/CreateSchemaTemplateStatement.java`
#### Snippet
```java
      List<List<String>> measurements,
      List<List<TSDataType>> dataTypes,
      List<List<TSEncoding>> encodings,
      List<List<CompressionType>> compressors) {
    this();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/statement/metadata/template/CreateSchemaTemplateStatement.java`
#### Snippet
```java
      List<List<TSDataType>> dataTypes,
      List<List<TSEncoding>> encodings,
      List<List<CompressionType>> compressors) {
    this();
    this.name = name;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends MeasurementSchemaInfo`
in `server/src/main/java/org/apache/iotdb/db/mpp/common/schematree/DeviceSchemaInfo.java`
#### Snippet
```java
      PartialPath devicePath,
      boolean isAligned,
      List<MeasurementSchemaInfo> measurementSchemaInfoList) {
    this.devicePath = devicePath;
    this.isAligned = isAligned;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends MeasurementPath`
in `server/src/main/java/org/apache/iotdb/db/mpp/common/schematree/ClusterSchemaTree.java`
#### Snippet
```java
  }

  public void appendMeasurementPaths(List<MeasurementPath> measurementPathList) {
    for (MeasurementPath measurementPath : measurementPathList) {
      appendSingleMeasurementPath(measurementPath);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super T`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/StateMachine.java`
#### Snippet
```java
  private void fireStateChanged(
      T newState,
      FutureStateChange<T> futureStateChange,
      List<StateChangeListener<T>> stateChangeListeners) {
    checkState(!Thread.holdsLock(lock), "Cannot fire state change event while holding the lock");
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends StateChangeListener`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/StateMachine.java`
#### Snippet
```java
      T newState,
      FutureStateChange<T> futureStateChange,
      List<StateChangeListener<T>> stateChangeListeners) {
    checkState(!Thread.holdsLock(lock), "Cannot fire state change event while holding the lock");
    requireNonNull(newState, "newState is null");
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super T`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/StateMachine.java`
#### Snippet
```java
  }

  private void fireStateChangedListener(T newState, StateChangeListener<T> stateChangeListener) {
    try {
      stateChangeListener.stateChanged(newState);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super T`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/StateMachine.java`
#### Snippet
```java
   * @return true if the state is set
   */
  public boolean setIf(T newState, Predicate<T> predicate) {
    checkState(!Thread.holdsLock(lock), "Cannot set state while holding the lock");
    requireNonNull(newState, "newState is null");
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends T`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/driver/Driver.java`
#### Snippet
```java
   */
  private <T> Optional<T> tryWithLock(
      long timeout, TimeUnit unit, boolean interruptOnClose, Supplier<T> task) {
    checkLockNotHeld("Lock cannot be reacquired");

```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Throwable`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/MPPDataExchangeManager.java`
#### Snippet
```java
    public SinkHandleListenerImpl(
        FragmentInstanceContext context,
        IMPPDataExchangeManagerCallback<Throwable> onFailureCallback) {
      this.context = context;
      this.onFailureCallback = onFailureCallback;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Throwable`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/MPPDataExchangeManager.java`
#### Snippet
```java
    private final IMPPDataExchangeManagerCallback<Throwable> onFailureCallback;

    public SourceHandleListenerImpl(IMPPDataExchangeManagerCallback<Throwable> onFailureCallback) {
      this.onFailureCallback = onFailureCallback;
    }
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Aggregator`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/AggregationUtil.java`
#### Snippet
```java
  }

  public static boolean isAllAggregatorsHasFinalResult(List<Aggregator> aggregators) {
    for (Aggregator aggregator : aggregators) {
      if (!aggregator.hasFinalResult()) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Aggregator`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/AggregationUtil.java`
#### Snippet
```java

  public static long calculateMaxAggregationResultSizeForLastQuery(
      List<Aggregator> aggregators, PartialPath inputSeriesPath) {
    long timeValueColumnsSizePerLine = TimeColumn.SIZE_IN_BYTES_PER_POSITION;
    List<TSDataType> outPutDataTypes =
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Operator`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/schema/SchemaQueryMergeOperator.java`
#### Snippet
```java

  public SchemaQueryMergeOperator(
      PlanNodeId planNodeId, OperatorContext operatorContext, List<Operator> children) {
    this.planNodeId = planNodeId;
    this.operatorContext = operatorContext;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Operator`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/schema/SchemaQueryOrderByHeatOperator.java`
#### Snippet
```java
  private int currentIndex = 0;

  public SchemaQueryOrderByHeatOperator(OperatorContext operatorContext, List<Operator> operators) {
    this.operatorContext = requireNonNull(operatorContext, "operatorContext is null");
    this.operators = operators;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Operator`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/schema/CountMergeOperator.java`
#### Snippet
```java

  public CountMergeOperator(
      PlanNodeId planNodeId, OperatorContext operatorContext, List<Operator> children) {
    this.planNodeId = planNodeId;
    this.operatorContext = operatorContext;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Operator`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/schema/SchemaFetchMergeOperator.java`
#### Snippet
```java

  public SchemaFetchMergeOperator(
      OperatorContext operatorContext, List<Operator> children, List<String> storageGroupList) {
    this.operatorContext = operatorContext;
    this.children = children;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PartialPath`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/schema/PathsUsingTemplateScanOperator.java`
#### Snippet
```java
      PlanNodeId planNodeId,
      OperatorContext context,
      List<PartialPath> pathPatternList,
      int templateId) {
    super(planNodeId, context, 0, 0, null, false);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends T`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/schema/SchemaTsBlockUtil.java`
#### Snippet
```java

  public static <T> List<TsBlock> transferSchemaResultToTsBlockList(
      Iterator<T> schemaRegionResultIterator,
      List<TSDataType> outputDataTypes,
      BiConsumer<T, TsBlockBuilder> consumer) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super T`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/schema/SchemaTsBlockUtil.java`
#### Snippet
```java
      Iterator<T> schemaRegionResultIterator,
      List<TSDataType> outputDataTypes,
      BiConsumer<T, TsBlockBuilder> consumer) {
    List<TsBlock> result = new ArrayList<>();
    TsBlockBuilder tsBlockBuilder = new TsBlockBuilder(outputDataTypes);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super TsBlockBuilder`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/schema/SchemaTsBlockUtil.java`
#### Snippet
```java
      Iterator<T> schemaRegionResultIterator,
      List<TSDataType> outputDataTypes,
      BiConsumer<T, TsBlockBuilder> consumer) {
    List<TsBlock> result = new ArrayList<>();
    TsBlockBuilder tsBlockBuilder = new TsBlockBuilder(outputDataTypes);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Operator`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/DeviceViewOperator.java`
#### Snippet
```java
      OperatorContext operatorContext,
      List<String> devices,
      List<Operator> deviceOperators,
      List<List<Integer>> deviceColumnIndex,
      List<TSDataType> dataTypes) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/DeviceViewOperator.java`
#### Snippet
```java
      List<String> devices,
      List<Operator> deviceOperators,
      List<List<Integer>> deviceColumnIndex,
      List<TSDataType> dataTypes) {
    this.operatorContext = operatorContext;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Operator`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/DeviceMergeOperator.java`
#### Snippet
```java
      OperatorContext operatorContext,
      List<String> devices,
      List<Operator> deviceOperators,
      List<TSDataType> dataTypes,
      TimeSelector selector,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends InsertTabletStatementGenerator`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/AbstractIntoOperator.java`
#### Snippet
```java

  private boolean existFullStatement(
      List<InsertTabletStatementGenerator> insertTabletStatementGenerators) {
    for (InsertTabletStatementGenerator generator : insertTabletStatementGenerators) {
      if (generator.isFull()) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends InsertTabletStatementGenerator`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/AbstractIntoOperator.java`
#### Snippet
```java

  private boolean existNonEmptyStatement(
      List<InsertTabletStatementGenerator> insertTabletStatementGenerators) {
    if (insertTabletStatementGenerators == null) {
      return false;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PartialPath`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/AbstractIntoOperator.java`
#### Snippet
```java

  protected static List<InsertTabletStatementGenerator> constructInsertTabletStatementGenerators(
      Map<PartialPath, Map<String, InputLocation>> targetPathToSourceInputLocationMap,
      Map<PartialPath, Map<String, TSDataType>> targetPathToDataTypeMap,
      Map<String, Boolean> targetDeviceToAlignedMap) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Map`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/AbstractIntoOperator.java`
#### Snippet
```java

  protected static List<InsertTabletStatementGenerator> constructInsertTabletStatementGenerators(
      Map<PartialPath, Map<String, InputLocation>> targetPathToSourceInputLocationMap,
      Map<PartialPath, Map<String, TSDataType>> targetPathToDataTypeMap,
      Map<String, Boolean> targetDeviceToAlignedMap) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Map`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/AbstractIntoOperator.java`
#### Snippet
```java
  protected static List<InsertTabletStatementGenerator> constructInsertTabletStatementGenerators(
      Map<PartialPath, Map<String, InputLocation>> targetPathToSourceInputLocationMap,
      Map<PartialPath, Map<String, TSDataType>> targetPathToDataTypeMap,
      Map<String, Boolean> targetDeviceToAlignedMap) {
    List<InsertTabletStatementGenerator> insertTabletStatementGenerators =
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends LeafColumnTransformer`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/FilterAndProjectOperator.java`
#### Snippet
```java
      Operator inputOperator,
      List<TSDataType> filterOutputDataTypes,
      List<LeafColumnTransformer> filterLeafColumnTransformerList,
      ColumnTransformer filterOutputTransformer,
      List<ColumnTransformer> commonTransformerList,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends ColumnTransformer`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/FilterAndProjectOperator.java`
#### Snippet
```java
      List<LeafColumnTransformer> filterLeafColumnTransformerList,
      ColumnTransformer filterOutputTransformer,
      List<ColumnTransformer> commonTransformerList,
      List<LeafColumnTransformer> projectLeafColumnTransformerList,
      List<ColumnTransformer> projectOutputTransformerList,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends LeafColumnTransformer`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/FilterAndProjectOperator.java`
#### Snippet
```java
      ColumnTransformer filterOutputTransformer,
      List<ColumnTransformer> commonTransformerList,
      List<LeafColumnTransformer> projectLeafColumnTransformerList,
      List<ColumnTransformer> projectOutputTransformerList,
      boolean hasNonMappableUDF,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends ColumnTransformer`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/FilterAndProjectOperator.java`
#### Snippet
```java
      List<ColumnTransformer> commonTransformerList,
      List<LeafColumnTransformer> projectLeafColumnTransformerList,
      List<ColumnTransformer> projectOutputTransformerList,
      boolean hasNonMappableUDF,
      boolean hasFilter) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Aggregator`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/AggregationOperator.java`
#### Snippet
```java
  public AggregationOperator(
      OperatorContext operatorContext,
      List<Aggregator> aggregators,
      ITimeRangeIterator timeRangeIterator,
      List<Operator> children,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Operator`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/AggregationOperator.java`
#### Snippet
```java
      List<Aggregator> aggregators,
      ITimeRangeIterator timeRangeIterator,
      List<Operator> children,
      long maxReturnSize) {
    this.operatorContext = operatorContext;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Pair`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/IntoOperator.java`
#### Snippet
```java
      Map<PartialPath, Map<String, TSDataType>> targetPathToDataTypeMap,
      Map<String, Boolean> targetDeviceToAlignedMap,
      List<Pair<String, PartialPath>> sourceTargetPathPairList,
      Map<String, InputLocation> sourceColumnToInputLocationMap) {
    super(
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends InputLocation`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/join/merge/NonOverlappedMultiColumnMerger.java`
#### Snippet
```java
   */
  public NonOverlappedMultiColumnMerger(
      List<InputLocation> inputLocations, TimeComparator comparator) {
    this.inputLocations = inputLocations;
    this.comparator = comparator;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends InputLocation`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/join/merge/MultiColumnMerger.java`
#### Snippet
```java
  private final List<InputLocation> inputLocations;

  public MultiColumnMerger(List<InputLocation> inputLocations) {
    this.inputLocations = inputLocations;
  }
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Operator`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/last/LastQueryCollectOperator.java`
#### Snippet
```java
  private int currentIndex;

  public LastQueryCollectOperator(OperatorContext operatorContext, List<Operator> children) {
    this.operatorContext = operatorContext;
    this.children = children;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Map`>
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/DeviceViewIntoOperator.java`
#### Snippet
```java
      OperatorContext operatorContext,
      Operator child,
      Map<String, Map<PartialPath, Map<String, InputLocation>>>
          deviceToTargetPathSourceInputLocationMap,
      Map<String, Map<PartialPath, Map<String, TSDataType>>> deviceToTargetPathDataTypeMap,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Map`>
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/DeviceViewIntoOperator.java`
#### Snippet
```java
      Map<String, Map<PartialPath, Map<String, InputLocation>>>
          deviceToTargetPathSourceInputLocationMap,
      Map<String, Map<PartialPath, Map<String, TSDataType>>> deviceToTargetPathDataTypeMap,
      Map<String, Boolean> targetDeviceToAlignedMap,
      Map<String, List<Pair<String, PartialPath>>> deviceToSourceTargetPathPairListMap,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`>
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/DeviceViewIntoOperator.java`
#### Snippet
```java
      Map<String, Map<PartialPath, Map<String, TSDataType>>> deviceToTargetPathDataTypeMap,
      Map<String, Boolean> targetDeviceToAlignedMap,
      Map<String, List<Pair<String, PartialPath>>> deviceToSourceTargetPathPairListMap,
      Map<String, InputLocation> sourceColumnToInputLocationMap) {
    super(operatorContext, child, null, sourceColumnToInputLocationMap);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends UpdateLastCacheOperator`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/last/LastQueryOperator.java`
#### Snippet
```java
  public LastQueryOperator(
      OperatorContext operatorContext,
      List<UpdateLastCacheOperator> children,
      TsBlockBuilder builder) {
    this.operatorContext = operatorContext;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends UpdateLastCacheOperator`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/last/LastQuerySortOperator.java`
#### Snippet
```java
      OperatorContext operatorContext,
      TsBlock cachedTsBlock,
      List<UpdateLastCacheOperator> children,
      Comparator<Binary> timeSeriesComparator) {
    this.cachedTsBlock = cachedTsBlock;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Binary`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/last/LastQueryUtil.java`
#### Snippet
```java

  public static int compareTimeSeries(
      TsBlock a, int indexA, TsBlock b, int indexB, Comparator<Binary> comparator) {
    return comparator.compare(a.getColumn(0).getBinary(indexA), b.getColumn(0).getBinary(indexB));
  }
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/source/SeriesScanUtil.java`
#### Snippet
```java
  }

  private LinkedList<TsFileResource> sortUnSeqFileResources(List<TsFileResource> tsFileResources) {
    return tsFileResources.stream()
        .sorted(orderUtils.comparingLong(tsFileResource -> orderUtils.getOrderTime(tsFileResource)))
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Operator`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/join/RowBasedTimeJoinOperator.java`
#### Snippet
```java
  public RowBasedTimeJoinOperator(
      OperatorContext operatorContext,
      List<Operator> children,
      Ordering mergeOrder,
      List<TSDataType> dataTypes,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends ColumnMerger`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/join/RowBasedTimeJoinOperator.java`
#### Snippet
```java
      Ordering mergeOrder,
      List<TSDataType> dataTypes,
      List<ColumnMerger> mergers,
      TimeComparator comparator) {
    checkArgument(
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Operator`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/join/TimeJoinOperator.java`
#### Snippet
```java
  public TimeJoinOperator(
      OperatorContext operatorContext,
      List<Operator> children,
      Ordering mergeOrder,
      List<TSDataType> dataTypes,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends ColumnMerger`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/join/TimeJoinOperator.java`
#### Snippet
```java
      Ordering mergeOrder,
      List<TSDataType> dataTypes,
      List<ColumnMerger> mergers,
      TimeComparator comparator) {
    checkArgument(
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super E`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/schedule/queue/L2PriorityQueue.java`
#### Snippet
```java
   * @throws IllegalArgumentException if maxCapacity <= 0.
   */
  public L2PriorityQueue(int maxCapacity, Comparator<E> comparator, E queryHolder) {
    super(maxCapacity, queryHolder);
    this.workingSortedElements = new TreeSet<>(comparator);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Operator`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/last/LastQueryMergeOperator.java`
#### Snippet
```java

  public LastQueryMergeOperator(
      OperatorContext operatorContext, List<Operator> children, Comparator<Binary> comparator) {
    this.operatorContext = operatorContext;
    this.children = children;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Binary`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/last/LastQueryMergeOperator.java`
#### Snippet
```java

  public LastQueryMergeOperator(
      OperatorContext operatorContext, List<Operator> children, Comparator<Binary> comparator) {
    this.operatorContext = operatorContext;
    this.children = children;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends IDriver`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/schedule/DriverScheduler.java`
#### Snippet
```java

  @Override
  public void submitDrivers(QueryId queryId, List<IDriver> instances, long timeOut) {
    List<DriverTask> tasks =
        instances.stream()
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Column`
in `server/src/main/java/org/apache/iotdb/db/mpp/aggregation/slidingwindow/MonotonicQueueSlidingWindowAggregator.java`
#### Snippet
```java
      List<InputLocation[]> inputLocationList,
      AggregationStep step,
      Comparator<Column> comparator) {
    super(accumulator, inputLocationList, step);
    this.comparator = comparator;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends ResultColumn`
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/udf/UDTFContext.java`
#### Snippet
```java
  }

  public void constructUdfExecutors(List<ResultColumn> resultColumns) {
    for (ResultColumn resultColumn : resultColumns) {
      resultColumn.getExpression().constructUdfExecutors(expressionName2Executor, zoneId);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `server/src/main/java/org/apache/iotdb/db/auth/AuthorizerManager.java`
#### Snippet
```java
  /** build TSBlock */
  public void buildTSBlock(
      Map<String, List<String>> authorizerInfo, SettableFuture<ConfigTaskResult> future) {
    List<TSDataType> types = new ArrayList<>();
    for (int i = 0; i < authorizerInfo.size(); i++) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super ConfigTaskResult`
in `server/src/main/java/org/apache/iotdb/db/auth/AuthorizerManager.java`
#### Snippet
```java
  /** build TSBlock */
  public void buildTSBlock(
      Map<String, List<String>> authorizerInfo, SettableFuture<ConfigTaskResult> future) {
    List<TSDataType> types = new ArrayList<>();
    for (int i = 0; i < authorizerInfo.size(); i++) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Long`
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/TsFileOpBlock.java`
#### Snippet
```java
        String deviceId,
        MetadataIndexNodeType type,
        Map<Long, Pair<Path, TimeseriesMetadata>> timeseriesMetadataMap,
        boolean needChunkMetadata)
        throws IOException {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Pair`
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/TsFileOpBlock.java`
#### Snippet
```java
        String deviceId,
        MetadataIndexNodeType type,
        Map<Long, Pair<Path, TimeseriesMetadata>> timeseriesMetadataMap,
        boolean needChunkMetadata)
        throws IOException {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Pair`>
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/TsFileOpBlock.java`
#### Snippet
```java
   */
  private void insertToDataList(
      List<Pair<MeasurementPath, List<TimeValuePair>>> dataList,
      String sensorFullPath,
      List<TimeValuePair> tvPairList)
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Chunk`
in `server/src/main/java/org/apache/iotdb/db/query/reader/resource/CachedUnseqResourceMergeReader.java`
#### Snippet
```java
public class CachedUnseqResourceMergeReader extends CachedPriorityMergeReader {

  public CachedUnseqResourceMergeReader(List<Chunk> chunks, TSDataType dataType)
      throws IOException {
    super(dataType);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends IPointReader`
in `server/src/main/java/org/apache/iotdb/db/query/reader/universal/PriorityMergeReader.java`
#### Snippet
```java

  // only used in external sort, need to refactor later
  public PriorityMergeReader(List<IPointReader> prioritySeriesReaders, int startPriority)
      throws IOException {
    heap =
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Modification`
in `server/src/main/java/org/apache/iotdb/db/query/context/QueryContext.java`
#### Snippet
```java
  }

  private List<Modification> sortAndMerge(List<Modification> modifications) {
    modifications.sort(
        (o1, o2) -> {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Long`
in `server/src/main/java/org/apache/iotdb/db/query/control/SessionManager.java`
#### Snippet
```java
  }

  private void releaseSessionResource(IClientSession session, Consumer<Long> releaseQueryResource) {
    Iterable<Long> statementIds = session.getStatementIds();
    if (statementIds != null) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Long`
in `server/src/main/java/org/apache/iotdb/db/query/control/SessionManager.java`
#### Snippet
```java

  public void closeStatement(
      IClientSession session, long statementId, Consumer<Long> releaseByQueryId) {
    Set<Long> queryIdSet = session.removeStatementId(statementId);
    if (queryIdSet != null) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Long`
in `server/src/main/java/org/apache/iotdb/db/query/control/SessionManager.java`
#### Snippet
```java

  public void closeDataset(
      IClientSession session, Long statementId, Long queryId, Consumer<Long> releaseByQueryId) {
    releaseByQueryId.accept(queryId);
    session.removeQueryId(statementId, queryId);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/query/control/tracing/TracingInfo.java`
#### Snippet
```java
  }

  public void addTsFileSet(List<TsFileResource> seqResources, List<TsFileResource> unSeqResources) {
    this.seqFileSet.addAll(seqResources);
    this.unSeqFileSet.addAll(unSeqResources);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/query/control/tracing/TracingInfo.java`
#### Snippet
```java
  }

  public void addTsFileSet(List<TsFileResource> seqResources, List<TsFileResource> unSeqResources) {
    this.seqFileSet.addAll(seqResources);
    this.unSeqFileSet.addAll(unSeqResources);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PartialPath`
in `server/src/main/java/org/apache/iotdb/db/query/dataset/SingleDataSet.java`
#### Snippet
```java
  private int i = 0;

  public SingleDataSet(List<PartialPath> paths, List<TSDataType> dataTypes) {
    super(new ArrayList<>(paths), dataTypes);
    // SingleDataSet's column number is different from other datasets
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PartialPath`
in `server/src/main/java/org/apache/iotdb/db/query/dataset/ListDataSet.java`
#### Snippet
```java
  private int index = 0;

  public ListDataSet(List<PartialPath> paths, List<TSDataType> dataTypes) {
    super(new ArrayList<>(paths), dataTypes);
  }
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super RowRecord`
in `server/src/main/java/org/apache/iotdb/db/query/dataset/ListDataSet.java`
#### Snippet
```java
  }

  public void sort(Comparator<RowRecord> c) {
    records.sort(c);
  }
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/query/reader/series/SeriesReader.java`
#### Snippet
```java
  }

  private LinkedList<TsFileResource> sortUnSeqFileResources(List<TsFileResource> tsFileResources) {
    return tsFileResources.stream()
        .sorted(orderUtils.comparingLong(tsFileResource -> orderUtils.getOrderTime(tsFileResource)))
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Pair`
in `server/src/main/java/org/apache/iotdb/db/query/dataset/NonAlignEngineDataSet.java`
#### Snippet
```java
    public ReadTask(
        ManagedSeriesReader reader,
        BlockingQueue<Pair<ByteBuffer, ByteBuffer>> blockingQueue,
        WatermarkEncoder encoder,
        int index) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super ByteBuffer`
in `server/src/main/java/org/apache/iotdb/db/query/dataset/RawQueryDataSetWithoutValueFilter.java`
#### Snippet
```java

  private void putPBOSToBuffer(
      PublicBAOS[] bitmapBAOSList, List<ByteBuffer> bitmapBufferList, int tsIndex) {
    ByteBuffer bitmapBuffer = ByteBuffer.allocate(bitmapBAOSList[tsIndex].size());
    bitmapBuffer.put(bitmapBAOSList[tsIndex].getBuf(), 0, bitmapBAOSList[tsIndex].size());
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super BatchData`
in `server/src/main/java/org/apache/iotdb/db/query/dataset/RawQueryDataSetWithoutValueFilter.java`
#### Snippet
```java
    public ReadTask(
        ManagedSeriesReader reader,
        BlockingQueue<BatchData> blockingQueue,
        String pathName,
        int[] batchDataLengthList,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super ByteBuffer`
in `server/src/main/java/org/apache/iotdb/db/query/dataset/UDTFAlignByTimeDataSet.java`
#### Snippet
```java

  protected void putPBOSToBuffer(
      PublicBAOS[] bitmapBAOSList, List<ByteBuffer> bitmapBufferList, int tsIndex) {
    ByteBuffer bitmapBuffer = ByteBuffer.allocate(bitmapBAOSList[tsIndex].size());
    bitmapBuffer.put(bitmapBAOSList[tsIndex].getBuf(), 0, bitmapBAOSList[tsIndex].size());
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends IReaderByTimestamp`
in `server/src/main/java/org/apache/iotdb/db/query/dataset/RawQueryDataSetWithValueFilter.java`
#### Snippet
```java
      List<TSDataType> dataTypes,
      TimeGenerator timeGenerator,
      List<IReaderByTimestamp> readers,
      List<List<Integer>> readerToIndexList,
      List<Boolean> cached,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `server/src/main/java/org/apache/iotdb/db/query/dataset/RawQueryDataSetWithValueFilter.java`
#### Snippet
```java
      TimeGenerator timeGenerator,
      List<IReaderByTimestamp> readers,
      List<List<Integer>> readerToIndexList,
      List<Boolean> cached,
      boolean ascending) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends SessionConnection`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
   */
  private <T> void insertByGroup(
      Map<SessionConnection, T> recordsGroup, InsertConsumer<T> insertConsumer)
      throws IoTDBConnectionException, StatementExecutionException {
    List<CompletableFuture<Void>> completableFutures =
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends T`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
   */
  private <T> void insertByGroup(
      Map<SessionConnection, T> recordsGroup, InsertConsumer<T> insertConsumer)
      throws IoTDBConnectionException, StatementExecutionException {
    List<CompletableFuture<Void>> completableFutures =
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super T`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
   */
  private <T> void insertByGroup(
      Map<SessionConnection, T> recordsGroup, InsertConsumer<T> insertConsumer)
      throws IoTDBConnectionException, StatementExecutionException {
    List<CompletableFuture<Void>> completableFutures =
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
      List<String> prefixPaths,
      List<Long> times,
      List<List<String>> measurementsList,
      List<List<String>> valuesList) {
    for (int i = valuesList.size() - 1; i >= 0; i--) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
      List<Long> times,
      List<List<String>> measurementsList,
      List<List<String>> valuesList) {
    for (int i = valuesList.size() - 1; i >= 0; i--) {
      List<String> values = valuesList.get(i);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
      List<String> deviceIds,
      List<Long> times,
      List<List<String>> measurementsList,
      List<List<Object>> valuesList,
      List<List<TSDataType>> typesList) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
      List<Long> times,
      List<List<String>> measurementsList,
      List<List<Object>> valuesList,
      List<List<TSDataType>> typesList) {
    for (int i = valuesList.size() - 1; i >= 0; i--) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
      List<List<String>> measurementsList,
      List<List<Object>> valuesList,
      List<List<TSDataType>> typesList) {
    for (int i = valuesList.size() - 1; i >= 0; i--) {
      List<Object> values = valuesList.get(i);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
      String name,
      List<String> schemaNames,
      List<List<String>> measurements,
      List<List<TSDataType>> dataTypes,
      List<List<TSEncoding>> encodings,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
      List<String> schemaNames,
      List<List<String>> measurements,
      List<List<TSDataType>> dataTypes,
      List<List<TSEncoding>> encodings,
      List<CompressionType> compressors)
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
      List<List<String>> measurements,
      List<List<TSDataType>> dataTypes,
      List<List<TSEncoding>> encodings,
      List<CompressionType> compressors)
      throws IoTDBConnectionException, StatementExecutionException {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends T`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
   * @return ordered list
   */
  private static <T> List<T> sortList(List<T> source, Integer[] index) {
    return Arrays.stream(index).map(source::get).collect(Collectors.toList());
  }
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
      String deviceId,
      List<Long> times,
      List<List<String>> measurementsList,
      List<List<TSDataType>> typesList,
      List<List<Object>> valuesList) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
      List<Long> times,
      List<List<String>> measurementsList,
      List<List<TSDataType>> typesList,
      List<List<Object>> valuesList) {
    for (int i = valuesList.size() - 1; i >= 0; i--) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
      List<List<String>> measurementsList,
      List<List<TSDataType>> typesList,
      List<List<Object>> valuesList) {
    for (int i = valuesList.size() - 1; i >= 0; i--) {
      List<Object> values = valuesList.get(i);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java

  private List<ByteBuffer> objectValuesListToByteBufferList(
      List<List<Object>> valuesList, List<List<TSDataType>> typesList)
      throws IoTDBConnectionException {
    List<ByteBuffer> buffersList = new ArrayList<>();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java

  private List<ByteBuffer> objectValuesListToByteBufferList(
      List<List<Object>> valuesList, List<List<TSDataType>> typesList)
      throws IoTDBConnectionException {
    List<ByteBuffer> buffersList = new ArrayList<>();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
      List<Long> times,
      String deviceId,
      List<List<String>> measurementsList,
      List<List<String>> valuesList) {
    for (int i = valuesList.size() - 1; i >= 0; i--) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
      String deviceId,
      List<List<String>> measurementsList,
      List<List<String>> valuesList) {
    for (int i = valuesList.size() - 1; i >= 0; i--) {
      List<String> values = valuesList.get(i);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super AggregateResult`
in `server/src/main/java/org/apache/iotdb/db/query/executor/groupby/impl/MonotonicQueueSlidingWindowGroupByExecutor.java`
#### Snippet
```java
      String aggrFuncName,
      boolean ascending,
      Comparator<AggregateResult> comparator) {
    super(dataType, aggrFuncName, ascending);
    this.comparator = comparator;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends AggregateResult`
in `server/src/main/java/org/apache/iotdb/db/query/executor/groupby/impl/LocalAlignedGroupByExecutor.java`
#### Snippet
```java
  }

  private void calcFromStatistics(Statistics statistics, List<AggregateResult> aggregateResultList)
      throws QueryProcessException {
    // statistics may be null for aligned time series
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends ExternalSortJobPart`
in `server/src/main/java/org/apache/iotdb/db/query/externalsort/MultiSourceExternalSortJobPart.java`
#### Snippet
```java

  public MultiSourceExternalSortJobPart(
      long queryId, String tmpFilePath, List<ExternalSortJobPart> source) {
    super(ExternalSortJobPartType.MULTIPLE_SOURCE);
    this.source = source;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends ExternalSortJobPart`
in `server/src/main/java/org/apache/iotdb/db/query/externalsort/ExternalSortJob.java`
#### Snippet
```java
  private List<ExternalSortJobPart> partList;

  ExternalSortJob(List<ExternalSortJobPart> partList) {
    this.partList = partList;
  }
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/query/executor/fill/LastPointReader.java`
#### Snippet
```java

  private PriorityQueue<TsFileResource> sortUnSeqFileResourcesInDecendingOrder(
      List<TsFileResource> tsFileResources) {
    PriorityQueue<TsFileResource> unseqTsFilesSet =
        new PriorityQueue<>(
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super PartialPath`
in `server/src/main/java/org/apache/iotdb/db/query/timegenerator/ServerTimeGenerator.java`
#### Snippet
```java
   */
  private Filter getPathListAndConstructTimeFilterFromExpression(
      IExpression expression, List<PartialPath> pathList) {
    if (expression.getType() == ExpressionType.SERIES) {
      SingleSeriesExpression seriesExpression = (SingleSeriesExpression) expression;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends ChunkReaderWrap`
in `server/src/main/java/org/apache/iotdb/db/query/externalsort/SimpleExternalSortEngine.java`
#### Snippet
```java
  /** init IReaderByTimestamp with ChunkReaderWrap. */
  private List<IReaderByTimestamp> generateIReaderByTimestamp(
      List<ChunkReaderWrap> readerWraps, final int start, final int size) throws IOException {
    List<IReaderByTimestamp> readerByTimestampList = new ArrayList<>();
    for (int i = start; i < start + size; i++) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends IPointReader`
in `server/src/main/java/org/apache/iotdb/db/query/externalsort/SimpleExternalSortEngine.java`
#### Snippet
```java
   * @return reader list that implements IReaderByTimestamp
   */
  private List<IReaderByTimestamp> convert(List<IPointReader> pointReaderList) {
    List<IReaderByTimestamp> readerByTimestampList = new ArrayList<>();
    for (IPointReader pointReader : pointReaderList) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends ChunkReaderWrap`
in `server/src/main/java/org/apache/iotdb/db/query/externalsort/SimpleExternalSortEngine.java`
#### Snippet
```java
  /** init IPointReader with ChunkReaderWrap. */
  private List<IPointReader> generateIPointReader(
      List<ChunkReaderWrap> readerWraps, final int start, final int size) throws IOException {
    List<IPointReader> pointReaderList = new ArrayList<>();
    for (int i = start; i < start + size; i++) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends ChunkReaderWrap`
in `server/src/main/java/org/apache/iotdb/db/query/externalsort/SimpleExternalSortEngine.java`
#### Snippet
```java

  @Override
  public ExternalSortJob createJob(long queryId, List<ChunkReaderWrap> readerWrapList) {
    long jobId = scheduler.genJobId();
    List<ExternalSortJobPart> ret = new ArrayList<>();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Long`
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileSelfCheckTool.java`
#### Snippet
```java
        String deviceId,
        MetadataIndexNodeType type,
        Map<Long, Pair<Path, TimeseriesMetadata>> timeseriesMetadataMap,
        boolean needChunkMetadata)
        throws IOException {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Pair`
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileSelfCheckTool.java`
#### Snippet
```java
        String deviceId,
        MetadataIndexNodeType type,
        Map<Long, Pair<Path, TimeseriesMetadata>> timeseriesMetadataMap,
        boolean needChunkMetadata)
        throws IOException {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/tools/settle/TsFileAndModSettleTool.java`
#### Snippet
```java
   */
  public static void moveNewTsFile(
      TsFileResource oldTsFileResource, List<TsFileResource> newTsFileResources)
      throws IOException {
    // delete old mods
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends LastCacheAccessor`
in `server/src/main/java/org/apache/iotdb/db/query/executor/LastQueryExecutor.java`
#### Snippet
```java
   */
  private static List<TimeValuePair> readLastPairsFromCache(
      List<PartialPath> seriesPaths, List<LastCacheAccessor> cacheAccessors, boolean debugOn) {
    List<TimeValuePair> ret = new ArrayList<>();
    for (int i = 0; i < cacheAccessors.size(); i++) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/tools/upgrade/TsFileOnlineUpgradeTool.java`
#### Snippet
```java
  /** upgrade file resource */
  @SuppressWarnings({"squid:S3776", "deprecation"}) // Suppress high Cognitive Complexity warning
  private void upgradeFile(List<TsFileResource> upgradedResources)
      throws IOException, WriteProcessException {

```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Modification`
in `server/src/main/java/org/apache/iotdb/db/utils/QueryUtils.java`
#### Snippet
```java
  @SuppressWarnings("squid:S3776") // Suppress high Cognitive Complexity warning
  public static void modifyChunkMetaData(
      List<? extends IChunkMetadata> chunkMetaData, List<Modification> modifications) {
    for (IChunkMetadata metaData : chunkMetaData) {
      for (Modification modification : modifications) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends AlignedChunkMetadata`
in `server/src/main/java/org/apache/iotdb/db/utils/QueryUtils.java`
#### Snippet
```java

  public static void modifyAlignedChunkMetaData(
      List<AlignedChunkMetadata> chunkMetaData, List<List<Modification>> modifications) {
    for (AlignedChunkMetadata metaData : chunkMetaData) {
      List<IChunkMetadata> valueChunkMetadataList = metaData.getValueChunkMetadataList();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `server/src/main/java/org/apache/iotdb/db/utils/QueryUtils.java`
#### Snippet
```java

  public static void modifyAlignedChunkMetaData(
      List<AlignedChunkMetadata> chunkMetaData, List<List<Modification>> modifications) {
    for (AlignedChunkMetadata metaData : chunkMetaData) {
      List<IChunkMetadata> valueChunkMetadataList = metaData.getValueChunkMetadataList();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileSplitByPartitionTool.java`
#### Snippet
```java
   */
  @SuppressWarnings({"squid:S3776", "deprecation"}) // Suppress high Cognitive Complexity warning
  public void parseAndRewriteFile(List<TsFileResource> rewrittenResources)
      throws IOException, WriteProcessException, IllegalPathException {
    // check if the TsFile has correct header
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Long`
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileSplitByPartitionTool.java`
#### Snippet
```java
      BatchData batchData,
      MeasurementSchema schema,
      Map<Long, ChunkWriterImpl> partitionChunkWriterMap) {
    while (batchData.hasCurrent()) {
      long time = batchData.currentTime();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Long`
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileSplitByPartitionTool.java`
#### Snippet
```java
      PageHeader pageHeader,
      ByteBuffer pageData,
      Map<Long, ChunkWriterImpl> partitionChunkWriterMap)
      throws PageException {
    long partitionId = StorageEngineV2.getTimePartition(pageHeader.getStartTime());
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PageHeader`
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileSplitByPartitionTool.java`
#### Snippet
```java
      boolean firstChunkInChunkGroup,
      MeasurementSchema schema,
      List<PageHeader> pageHeadersInChunk,
      List<ByteBuffer> pageDataInChunk,
      List<Boolean> needToDecodeInfoInChunk,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends ByteBuffer`
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileSplitByPartitionTool.java`
#### Snippet
```java
      MeasurementSchema schema,
      List<PageHeader> pageHeadersInChunk,
      List<ByteBuffer> pageDataInChunk,
      List<Boolean> needToDecodeInfoInChunk,
      long chunkHeaderOffset)
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super OpenFileNumStatistics`
in `server/src/main/java/org/apache/iotdb/db/utils/OpenFileNumUtil.java`
#### Snippet
```java

  private static void countOneFile(
      String line, int pid, EnumMap<OpenFileNumStatistics, Integer> resultMap) {
    String[] temp = line.split("\\s+");
    if (!line.contains(Integer.toString(pid)) || temp.length <= 8) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Path`
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileSketchTool.java`
#### Snippet
```java
  }

  private void printTimeseriesIndex(long pos, Pair<Path, TimeseriesMetadata> timeseriesMetadata) {
    try {
      printlnBoth(
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TimeseriesMetadata`
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileSketchTool.java`
#### Snippet
```java
  }

  private void printTimeseriesIndex(long pos, Pair<Path, TimeseriesMetadata> timeseriesMetadata) {
    try {
      printlnBoth(
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Long`
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileSketchTool.java`
#### Snippet
```java
        String deviceId,
        MetadataIndexNodeType type,
        Map<Long, Pair<Path, TimeseriesMetadata>> timeseriesMetadataMap,
        boolean needChunkMetadata)
        throws IOException {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Pair`
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileSketchTool.java`
#### Snippet
```java
        String deviceId,
        MetadataIndexNodeType type,
        Map<Long, Pair<Path, TimeseriesMetadata>> timeseriesMetadataMap,
        boolean needChunkMetadata)
        throws IOException {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends ChunkGroupMetadata`
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileSketchTool.java`
#### Snippet
```java
  }

  private void printChunk(List<ChunkGroupMetadata> allChunkGroupMetadata) {
    try {
      long nextChunkGroupHeaderPos =
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Long`
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileSketchTool.java`
#### Snippet
```java
  private void loadIndexTree(
      MetadataIndexNode metadataIndexNode,
      TreeMap<Long, MetadataIndexNode> metadataIndexNodeMap,
      List<String> treeOutputStringBuffer,
      int deep)
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super MetadataIndexNode`
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileSketchTool.java`
#### Snippet
```java
  private void loadIndexTree(
      MetadataIndexNode metadataIndexNode,
      TreeMap<Long, MetadataIndexNode> metadataIndexNodeMap,
      List<String> treeOutputStringBuffer,
      int deep)
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super String`
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileSketchTool.java`
#### Snippet
```java
      MetadataIndexNode metadataIndexNode,
      TreeMap<Long, MetadataIndexNode> metadataIndexNodeMap,
      List<String> treeOutputStringBuffer,
      int deep)
      throws IOException {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends MeasurementPath`
in `server/src/main/java/org/apache/iotdb/db/utils/SchemaUtils.java`
#### Snippet
```java

  public static List<TSDataType> getSeriesTypesByPaths(
      List<MeasurementPath> paths, List<String> aggregations) {
    List<TSDataType> tsDataTypes = new ArrayList<>();
    for (int i = 0; i < paths.size(); i++) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends AggregateResult`
in `server/src/main/java/org/apache/iotdb/db/query/executor/AggregationExecutor.java`
#### Snippet
```java

  private static int aggregateBatchData(
      List<AggregateResult> aggregateResultList,
      boolean[] isCalculatedArray,
      int remainingToCalculate,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `server/src/main/java/org/apache/iotdb/db/query/executor/AggregationExecutor.java`
#### Snippet
```java
  private static int aggregateAlignedPages(
      AlignedSeriesAggregateReader seriesReader,
      List<List<AggregateResult>> aggregateResultList,
      List<boolean[]> isCalculatedArray,
      int remainingToCalculate)
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `server/src/main/java/org/apache/iotdb/db/query/executor/AggregationExecutor.java`
#### Snippet
```java
  }

  private static boolean isAggregateResultEmpty(List<List<AggregateResult>> resultList) {
    for (List<AggregateResult> result : resultList) {
      if (!result.isEmpty()) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `server/src/main/java/org/apache/iotdb/db/query/executor/AggregationExecutor.java`
#### Snippet
```java
  protected void aggregateOneAlignedSeries(
      AlignedPath alignedPath,
      List<List<Integer>> subIndexes,
      Set<String> allMeasurementsInDevice,
      Filter timeFilter)
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends AggregateResult`
in `server/src/main/java/org/apache/iotdb/db/query/executor/AggregationExecutor.java`
#### Snippet
```java
  /** Aggregate each result in the list with the statistics */
  private static int aggregateStatistics(
      List<AggregateResult> aggregateResultList,
      boolean[] isCalculatedArray,
      int remainingToCalculate,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TimeRange`
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/TVList.java`
#### Snippet
```java

  protected boolean isPointDeleted(
      long timestamp, List<TimeRange> deletionList, Integer deleteCursor) {
    while (deletionList != null && deleteCursor < deletionList.size()) {
      if (deletionList.get(deleteCursor).contains(timestamp)) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends DataRegion`
in `server/src/main/java/org/apache/iotdb/db/engine/StorageEngine.java`
#### Snippet
```java

  /** unlock all merge lock of the storage group processor related to the query */
  public void mergeUnLock(List<DataRegion> list) {
    list.forEach(DataRegion::readUnlock);
  }
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PartialPath`
in `server/src/main/java/org/apache/iotdb/db/engine/StorageEngine.java`
#### Snippet
```java
  /** get all merge lock of the storage group processor related to the query */
  public Pair<List<DataRegion>, Map<DataRegion, List<PartialPath>>> mergeLock(
      List<PartialPath> pathList) throws StorageEngineException {
    Map<DataRegion, List<PartialPath>> map = new HashMap<>();
    for (PartialPath path : pathList) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends File`
in `server/src/main/java/org/apache/iotdb/db/tools/validate/TsFileValidationTool.java`
#### Snippet
```java
  }

  public static void findUncorrectFiles(List<File> tsFiles) {
    for (File tsFile : tsFiles) {
      List<String> previousBadFileMsgs = new ArrayList<>();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends DataRegionId`
in `server/src/main/java/org/apache/iotdb/db/engine/StorageEngineV2.java`
#### Snippet
```java
  }

  public void setTTL(List<DataRegionId> dataRegionIdList, long dataTTL) {
    for (DataRegionId dataRegionId : dataRegionIdList) {
      DataRegion dataRegion = dataRegionMap.get(dataRegionId);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Future`
in `server/src/main/java/org/apache/iotdb/db/engine/StorageEngineV2.java`
#### Snippet
```java
  }

  private <V> void checkResults(List<Future<V>> tasks, String errorMsg) {
    for (Future<V> task : tasks) {
      try {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Future`
in `server/src/main/java/org/apache/iotdb/db/engine/StorageEngineV2.java`
#### Snippet
```java
  }

  private void asyncRecover(List<Future<Void>> futures) {
    Map<String, List<DataRegionId>> localDataRegionInfo = getLocalDataRegionInfo();
    localDataRegionInfo.values().forEach(list -> recoverDataRegionNum += list.size());
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends MQTTForwardEvent`
in `server/src/main/java/org/apache/iotdb/db/engine/trigger/sink/forward/mqtt/MQTTForwardHandler.java`
#### Snippet
```java

  @Override
  public void onEvent(List<MQTTForwardEvent> events) throws SinkException {
    StringBuilder sb = new StringBuilder().append("[");
    for (MQTTForwardEvent event : events) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/AlignedTVList.java`
#### Snippet
```java
  /** Build TsBlock by column. */
  public TsBlock buildTsBlock(
      int floatPrecision, List<TSEncoding> encodingList, List<List<TimeRange>> deletionList) {
    TsBlockBuilder builder = new TsBlockBuilder(dataTypes);
    // Time column
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends T`
in `server/src/main/java/org/apache/iotdb/db/engine/trigger/utils/BatchHandlerQueue.java`
#### Snippet
```java
    ArrayBlockingQueue<T> queue;

    public ForwardQueueConsumer(String name, ArrayBlockingQueue<T> queue) {
      super(name);
      this.queue = queue;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends HTTPForwardEvent`
in `server/src/main/java/org/apache/iotdb/db/engine/trigger/sink/forward/http/HTTPForwardHandler.java`
#### Snippet
```java

  @Override
  public void onEvent(List<HTTPForwardEvent> events) throws SinkException {
    CloseableHttpResponse response = null;
    try {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Long`
in `server/src/main/java/org/apache/iotdb/db/engine/load/TsFileSplitter.java`
#### Snippet
```java

  private void getChunkMetadata(
      TsFileSequenceReader reader, Map<Long, IChunkMetadata> offset2ChunkMetadata)
      throws IOException {
    Map<String, List<TimeseriesMetadata>> device2Metadata = reader.getAllTimeseriesMetadata(true);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super IChunkMetadata`
in `server/src/main/java/org/apache/iotdb/db/engine/load/TsFileSplitter.java`
#### Snippet
```java

  private void getChunkMetadata(
      TsFileSequenceReader reader, Map<Long, IChunkMetadata> offset2ChunkMetadata)
      throws IOException {
    Map<String, List<TimeseriesMetadata>> device2Metadata = reader.getAllTimeseriesMetadata(true);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Long`
in `server/src/main/java/org/apache/iotdb/db/engine/load/TsFileSplitter.java`
#### Snippet
```java
  }

  private void getAllModification(Map<Long, List<Deletion>> offset2Deletions) throws IOException {
    try (ModificationFile modificationFile =
        new ModificationFile(tsFile.getAbsolutePath() + ModificationFile.FILE_SUFFIX)) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super TsFileData`
in `server/src/main/java/org/apache/iotdb/db/engine/load/TsFileSplitter.java`
#### Snippet
```java
  private final Function<TsFileData, Boolean> consumer;

  public TsFileSplitter(File tsFile, Function<TsFileData, Boolean> consumer) {
    this.tsFile = tsFile;
    this.consumer = consumer;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `server/src/main/java/org/apache/iotdb/db/engine/load/TsFileSplitter.java`
#### Snippet
```java

  private void handleModification(
      TreeMap<Long, List<Deletion>> offset2Deletions, long chunkOffset) {
    while (!offset2Deletions.isEmpty() && offset2Deletions.firstEntry().getKey() <= chunkOffset) {
      offset2Deletions
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends IMeasurementSchema`
in `server/src/main/java/org/apache/iotdb/db/engine/memtable/WritableMemChunkGroup.java`
#### Snippet
```java
      Object[] columns,
      BitMap[] bitMaps,
      List<IMeasurementSchema> schemaList,
      int start,
      int end) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends IMeasurementSchema`
in `server/src/main/java/org/apache/iotdb/db/engine/memtable/WritableMemChunkGroup.java`
#### Snippet
```java
  @Override
  public boolean writeWithFlushCheck(
      long insertTime, Object[] objectValue, List<IMeasurementSchema> schemaList) {
    boolean flushFlag = false;
    for (int i = 0; i < objectValue.length; i++) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/snapshot/SnapshotTaker.java`
#### Snippet
```java
  }

  private boolean createSnapshot(List<TsFileResource> resources, String snapshotId) {
    try {
      for (TsFileResource resource : resources) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends AbstractCompactionTask`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/CompactionWorker.java`
#### Snippet
```java

  public CompactionWorker(
      int threadId, FixedPriorityBlockingQueue<AbstractCompactionTask> compactionTaskQueue) {
    this.threadId = threadId;
    this.compactionTaskQueue = compactionTaskQueue;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/CompactionUtils.java`
#### Snippet
```java

  public static void deleteCompactionModsFile(
      List<TsFileResource> selectedSeqTsFileResourceList,
      List<TsFileResource> selectedUnSeqTsFileResourceList)
      throws IOException {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/CompactionUtils.java`
#### Snippet
```java
  public static void deleteCompactionModsFile(
      List<TsFileResource> selectedSeqTsFileResourceList,
      List<TsFileResource> selectedUnSeqTsFileResourceList)
      throws IOException {
    for (TsFileResource seqFile : selectedSeqTsFileResourceList) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/CompactionUtils.java`
#### Snippet
```java
   */
  public static void moveTargetFile(
      List<TsFileResource> targetResources, boolean isInnerSpace, String fullStorageGroupName)
      throws IOException, WriteProcessException {
    String fileSuffix;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/CompactionUtils.java`
#### Snippet
```java

  public static boolean deleteTsFilesInDisk(
      Collection<TsFileResource> mergeTsFiles, String storageGroupName) {
    logger.info("{} [Compaction] Compaction starts to delete real file ", storageGroupName);
    boolean result = true;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/CompactionUtils.java`
#### Snippet
```java
   */
  public static void combineModsInCrossCompaction(
      List<TsFileResource> seqResources,
      List<TsFileResource> unseqResources,
      List<TsFileResource> targetResources)
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/CompactionUtils.java`
#### Snippet
```java
  public static void combineModsInCrossCompaction(
      List<TsFileResource> seqResources,
      List<TsFileResource> unseqResources,
      List<TsFileResource> targetResources)
      throws IOException {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/CompactionUtils.java`
#### Snippet
```java
      List<TsFileResource> seqResources,
      List<TsFileResource> unseqResources,
      List<TsFileResource> targetResources)
      throws IOException {
    // target file may less than source seq files, so we should find each target file with its
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/CompactionUtils.java`
#### Snippet
```java
  /** Delete all modification files for source files */
  public static void deleteModificationForSourceFile(
      Collection<TsFileResource> sourceFiles, String storageGroupName) throws IOException {
    logger.info("{} [Compaction] Start to delete modifications of source files", storageGroupName);
    for (TsFileResource tsFileResource : sourceFiles) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/CompactionUtils.java`
#### Snippet
```java
   */
  public static void combineModsInInnerCompaction(
      Collection<TsFileResource> sourceFiles, TsFileResource targetTsFile) throws IOException {
    Set<Modification> modifications = new HashSet<>();
    for (TsFileResource mergeTsFile : sourceFiles) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Modification`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/CompactionUtils.java`
#### Snippet
```java

  private static void updateOneTargetMods(
      TsFileResource targetFile, Set<Modification> modifications) throws IOException {
    if (!modifications.isEmpty()) {
      try (ModificationFile modificationFile = ModificationFile.getNormalMods(targetFile)) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/log/CompactionLogger.java`
#### Snippet
```java
  }

  public void logFiles(List<TsFileResource> tsFiles, String flag) throws IOException {
    for (TsFileResource tsFileResource : tsFiles) {
      logStream.write(
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends AbstractCompactionTask`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/CompactionTaskManager.java`
#### Snippet
```java
  }

  public boolean isAnyTaskInListStillRunning(List<AbstractCompactionTask> compactionTasks) {
    boolean anyTaskRunning = false;
    for (AbstractCompactionTask task : compactionTasks) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends IMeasurementSchema`
in `server/src/main/java/org/apache/iotdb/db/engine/memtable/AbstractMemTable.java`
#### Snippet
```java

  private IWritableMemChunkGroup createAlignedMemChunkGroupIfNotExistAndGet(
      IDeviceID deviceId, List<IMeasurementSchema> schemaList) {
    IWritableMemChunkGroup memChunkGroup =
        memTableMap.computeIfAbsent(
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends IMeasurementSchema`
in `server/src/main/java/org/apache/iotdb/db/engine/memtable/AbstractMemTable.java`
#### Snippet
```java
   */
  private IWritableMemChunkGroup createMemChunkGroupIfNotExistAndGet(
      IDeviceID deviceId, List<IMeasurementSchema> schemaList) {
    IWritableMemChunkGroup memChunkGroup =
        memTableMap.computeIfAbsent(deviceId, k -> new WritableMemChunkGroup());
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/cross/utils/ReadPointCrossCompactionEstimator.java`
#### Snippet
```java
   */
  private long calculatingWritingTargetFiles(
      List<TsFileResource> seqResources, TsFileResource unseqResource) throws IOException {
    long cost = 0;
    for (TsFileResource seqResource : seqResources) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/cross/utils/ReadPointCrossCompactionEstimator.java`
#### Snippet
```java
   * one seq file will be queried at the same time.
   */
  private long calculateReadingSeqFiles(List<TsFileResource> seqResources) throws IOException {
    long cost = 0;
    for (TsFileResource seqResource : seqResources) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/cross/CrossSpaceCompactionTask.java`
#### Snippet
```java
  }

  private boolean addReadLock(List<TsFileResource> tsFileResourceList) {
    if (!tsFileManager.isAllowCompaction()) {
      return false;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/cross/CrossSpaceCompactionTask.java`
#### Snippet
```java
  }

  private void deleteOldFiles(List<TsFileResource> tsFileResourceList) throws IOException {
    for (TsFileResource tsFileResource : tsFileResourceList) {
      FileReaderManager.getInstance().closeFileAndRemoveReader(tsFileResource.getTsFilePath());
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/cross/CrossSpaceCompactionTask.java`
#### Snippet
```java
  }

  private void releaseReadAndLockWrite(List<TsFileResource> tsFileResourceList) {
    for (TsFileResource tsFileResource : tsFileResourceList) {
      tsFileResource.readUnlock();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends MeasurementSchema`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/cross/rewrite/task/ReadPointPerformerSubTask.java`
#### Snippet
```java
      QueryDataSource queryDataSource,
      AbstractCompactionWriter compactionWriter,
      Map<String, MeasurementSchema> schemaMap,
      int taskId) {
    this.device = device;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/cross/rewrite/CrossSpaceCompactionResource.java`
#### Snippet
```java

  public CrossSpaceCompactionResource(
      List<TsFileResource> seqFiles, List<TsFileResource> unseqFiles) {
    this.seqFiles = seqFiles.stream().filter(this::filterSeqResource).collect(Collectors.toList());
    filterUnseqResource(unseqFiles);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/cross/rewrite/CrossSpaceCompactionResource.java`
#### Snippet
```java
   * under a time partition in a VSG.
   */
  private void filterUnseqResource(List<TsFileResource> unseqResources) {
    for (TsFileResource resource : unseqResources) {
      if (resource.getStatus() != TsFileResourceStatus.CLOSED) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/cross/rewrite/CrossSpaceCompactionResource.java`
#### Snippet
```java

  public CrossSpaceCompactionResource(
      Collection<TsFileResource> seqFiles, List<TsFileResource> unseqFiles, long ttlLowerBound) {
    this.ttlLowerBound = ttlLowerBound;
    this.seqFiles = seqFiles.stream().filter(this::filterSeqResource).collect(Collectors.toList());
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends IMeasurementSchema`
in `server/src/main/java/org/apache/iotdb/db/engine/memtable/AlignedWritableMemChunk.java`
#### Snippet
```java
   * @return columnIndexArray: schemaList[i] is schema of columns[columnIndexArray[i]]
   */
  private int[] checkColumnsInInsertPlan(List<IMeasurementSchema> schemaListInInsertPlan) {
    Map<String, Integer> measurementIdsInInsertPlan = new HashMap<>();
    for (int i = 0; i < schemaListInInsertPlan.size(); i++) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends IMeasurementSchema`
in `server/src/main/java/org/apache/iotdb/db/engine/memtable/AlignedWritableMemChunk.java`
#### Snippet
```java

  @Override
  public synchronized TVList getSortedTvListForQuery(List<IMeasurementSchema> schemaList) {
    sortTVList();
    // increase reference count
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/cross/utils/InplaceCompactionEstimator.java`
#### Snippet
```java
  private long calculateMemoryCost(
      TsFileResource unseqResource,
      List<TsFileResource> seqResources,
      IFileQueryMemMeasurement unseqMeasurement,
      IFileQueryMemMeasurement seqMeasurement)
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileIdentifier`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/task/CompactionRecoverTask.java`
#### Snippet
```java
    /** Delete tmp target file and compaction mods file. */
    private boolean handleCrossCompactionWithAllSourceFilesExistBefore013(
        List<TsFileIdentifier> targetFileIdentifiers) {
      // delete tmp target file
      for (TsFileIdentifier targetFileIdentifier : targetFileIdentifiers) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileIdentifier`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/task/CompactionRecoverTask.java`
#### Snippet
```java
   */
  private boolean handleWithAllSourceFilesExist(
      List<TsFileIdentifier> targetFileIdentifiers, List<TsFileIdentifier> sourceFileIdentifiers) {
    LOGGER.info(
        "{} [Compaction][Recover] all source files exists, delete all target files.",
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileIdentifier`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/task/CompactionRecoverTask.java`
#### Snippet
```java
   */
  private boolean handleWithAllSourceFilesExist(
      List<TsFileIdentifier> targetFileIdentifiers, List<TsFileIdentifier> sourceFileIdentifiers) {
    LOGGER.info(
        "{} [Compaction][Recover] all source files exists, delete all target files.",
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileIdentifier`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/task/CompactionRecoverTask.java`
#### Snippet
```java
     */
    private boolean handleCrossCompactionWithSomeSourceFilesLostBefore013(
        List<TsFileIdentifier> targetFileIdentifiers,
        List<TsFileIdentifier> sourceFileIdentifiers) {
      try {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileIdentifier`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/task/CompactionRecoverTask.java`
#### Snippet
```java
    private boolean handleCrossCompactionWithSomeSourceFilesLostBefore013(
        List<TsFileIdentifier> targetFileIdentifiers,
        List<TsFileIdentifier> sourceFileIdentifiers) {
      try {
        File compactionModsFileFromOld =
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileIdentifier`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/task/CompactionRecoverTask.java`
#### Snippet
```java
   */
  private boolean handleWithSomeSourceFilesLost(
      List<TsFileIdentifier> targetFileIdentifiers, List<TsFileIdentifier> sourceFileIdentifiers)
      throws IOException {
    // some source files have been deleted, while target file must exist and complete.
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileIdentifier`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/task/CompactionRecoverTask.java`
#### Snippet
```java
  }

  private boolean checkIsTargetFilesComplete(List<TsFileIdentifier> targetFileIdentifiers)
      throws IOException {
    for (TsFileIdentifier targetFileIdentifier : targetFileIdentifiers) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Pair`>
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/inner/utils/AlignedSeriesCompactionExecutor.java`
#### Snippet
```java
   */
  private List<IMeasurementSchema> collectSchemaFromAlignedChunkMetadataList(
      LinkedList<Pair<TsFileSequenceReader, List<AlignedChunkMetadata>>> readerAndChunkMetadataList)
      throws IOException {
    Set<MeasurementSchema> schemaSet = new HashSet<>();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Pair`>
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/inner/utils/AlignedSeriesCompactionExecutor.java`
#### Snippet
```java
      String device,
      TsFileResource targetResource,
      LinkedList<Pair<TsFileSequenceReader, List<AlignedChunkMetadata>>> readerAndChunkMetadataList,
      TsFileIOWriter writer)
      throws IOException {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Pair`>
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/inner/utils/SingleSeriesCompactionExecutor.java`
#### Snippet
```java
  public SingleSeriesCompactionExecutor(
      PartialPath series,
      LinkedList<Pair<TsFileSequenceReader, List<ChunkMetadata>>> readerAndChunkMetadataList,
      TsFileIOWriter fileWriter,
      TsFileResource targetResource) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Pair`>
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/inner/utils/SingleSeriesCompactionExecutor.java`
#### Snippet
```java
      PartialPath series,
      IMeasurementSchema measurementSchema,
      LinkedList<Pair<TsFileSequenceReader, List<ChunkMetadata>>> readerAndChunkMetadataList,
      TsFileIOWriter fileWriter,
      TsFileResource targetResource) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/CompactionExceptionHandler.java`
#### Snippet
```java

  private static boolean checkIsTargetFilesComplete(
      List<TsFileResource> targetResources,
      List<TsFileResource> lostSourceResources,
      String fullStorageGroupName)
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/CompactionExceptionHandler.java`
#### Snippet
```java

  private static boolean checkAllSourceFileExists(
      List<TsFileResource> tsFileResources, List<TsFileResource> lostFiles) {
    for (TsFileResource tsFileResource : tsFileResources) {
      if (!tsFileResource.getTsFile().exists() || !tsFileResource.resourceFileExists()) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/CompactionExceptionHandler.java`
#### Snippet
```java

  private static boolean checkAllSourceFileExists(
      List<TsFileResource> tsFileResources, List<TsFileResource> lostFiles) {
    for (TsFileResource tsFileResource : tsFileResources) {
      if (!tsFileResource.getTsFile().exists() || !tsFileResource.resourceFileExists()) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/CompactionExceptionHandler.java`
#### Snippet
```java
   */
  private static boolean handleWhenAllSourceFilesExist(
      List<TsFileResource> targetResourceList,
      List<TsFileResource> sourceSeqResourceList,
      List<TsFileResource> sourceUnseqResourceList,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Pair`, Long>
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/inner/sizetiered/SizeTieredCompactionSelector.java`
#### Snippet
```java
   */
  private boolean selectLevelTask(
      int level, PriorityQueue<Pair<List<TsFileResource>, Long>> taskPriorityQueue)
      throws IOException {
    boolean shouldContinueToSearch = true;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/writer/CrossSpaceCompactionWriter.java`
#### Snippet
```java

  public CrossSpaceCompactionWriter(
      List<TsFileResource> targetResources, List<TsFileResource> seqFileResources)
      throws IOException {
    this.targetTsFileResources = targetResources;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/writer/CrossSpaceCompactionWriter.java`
#### Snippet
```java

  public CrossSpaceCompactionWriter(
      List<TsFileResource> targetResources, List<TsFileResource> seqFileResources)
      throws IOException {
    this.targetTsFileResources = targetResources;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileSequenceReader`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/inner/utils/MultiTsFileDeviceIterator.java`
#### Snippet
```java

    private MeasurementIterator(
        Map<TsFileResource, TsFileSequenceReader> readerMap,
        String device,
        boolean needDeserializeTimeseries)
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Pair`>
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/performer/impl/ReadChunkCompactionPerformer.java`
#### Snippet
```java

  private boolean checkAlignedSeriesExists(
      LinkedList<Pair<TsFileSequenceReader, List<AlignedChunkMetadata>>>
          readerAndChunkMetadataList) {
    for (Pair<TsFileSequenceReader, List<AlignedChunkMetadata>> readerListPair :
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/performer/impl/ReadChunkCompactionPerformer.java`
#### Snippet
```java

  @Override
  public void setTargetFiles(List<TsFileResource> targetFiles) {
    if (targetFiles.size() != 1) {
      throw new RuntimeException(
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileManager.java`
#### Snippet
```java
  }

  public void removeAll(List<TsFileResource> tsFileResourceList, boolean sequence) {
    writeLock("removeAll");
    try {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super File`
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileManager.java`
#### Snippet
```java

  private void collectTsFile(
      List<File> historyTsFiles,
      List<TsFileResource> tsFileResources,
      ISyncManager syncManager,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileManager.java`
#### Snippet
```java
  private void collectTsFile(
      List<File> historyTsFiles,
      List<TsFileResource> tsFileResources,
      ISyncManager syncManager,
      long dataStartTime) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileManager.java`
#### Snippet
```java
  /** This method is called after compaction to update memory. */
  public void replace(
      List<TsFileResource> seqFileResources,
      List<TsFileResource> unseqFileResources,
      List<TsFileResource> targetFileResources,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileManager.java`
#### Snippet
```java
  public void replace(
      List<TsFileResource> seqFileResources,
      List<TsFileResource> unseqFileResources,
      List<TsFileResource> targetFileResources,
      long timePartition,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileManager.java`
#### Snippet
```java
      List<TsFileResource> seqFileResources,
      List<TsFileResource> unseqFileResources,
      List<TsFileResource> targetFileResources,
      long timePartition,
      boolean isTargetSequence)
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileManager.java`
#### Snippet
```java
  }

  public void addAll(List<TsFileResource> tsFileResourceList, boolean sequence) {
    writeLock("add");
    try {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileNameGenerator.java`
#### Snippet
```java
   */
  public static List<TsFileResource> getCrossCompactionTargetFileResources(
      List<TsFileResource> seqResources) throws IOException {
    List<TsFileResource> targetFileResources = new ArrayList<>();
    for (TsFileResource resource : seqResources) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileNameGenerator.java`
#### Snippet
```java
   */
  public static TsFileResource getInnerCompactionTargetFileResource(
      List<TsFileResource> tsFileResources, boolean sequence) throws IOException {
    long minTime = Long.MAX_VALUE;
    long maxTime = Long.MIN_VALUE;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/service/SettleService.java`
#### Snippet
```java

  public void startSettling(
      List<TsFileResource> seqResourcesToBeSettled, List<TsFileResource> unseqResourcesToBeSettled)
      throws WriteProcessException {
    filesToBeSettledCount.addAndGet(
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/service/SettleService.java`
#### Snippet
```java

  public void startSettling(
      List<TsFileResource> seqResourcesToBeSettled, List<TsFileResource> unseqResourcesToBeSettled)
      throws WriteProcessException {
    filesToBeSettledCount.addAndGet(
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/performer/impl/ReadPointCompactionPerformer.java`
#### Snippet
```java

  private static void updateDeviceStartTimeAndEndTime(
      List<TsFileResource> targetResources, AbstractCompactionWriter compactionWriter) {
    List<TsFileIOWriter> targetFileWriters = compactionWriter.getFileIOWriter();
    for (int i = 0; i < targetFileWriters.size(); i++) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/performer/impl/ReadPointCompactionPerformer.java`
#### Snippet
```java

  private static void updatePlanIndexes(
      List<TsFileResource> targetResources,
      List<TsFileResource> seqResources,
      List<TsFileResource> unseqResources) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/performer/impl/ReadPointCompactionPerformer.java`
#### Snippet
```java
  private static void updatePlanIndexes(
      List<TsFileResource> targetResources,
      List<TsFileResource> seqResources,
      List<TsFileResource> unseqResources) {
    // as the new file contains data of other files, track their plan indexes in the new file
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/performer/impl/ReadPointCompactionPerformer.java`
#### Snippet
```java
      List<TsFileResource> targetResources,
      List<TsFileResource> seqResources,
      List<TsFileResource> unseqResources) {
    // as the new file contains data of other files, track their plan indexes in the new file
    // so that we will be able to compare data across different IoTDBs that share the same index
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends CloseFileListener`
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileProcessor.java`
#### Snippet
```java
  }

  public void addCloseFileListeners(Collection<CloseFileListener> listeners) {
    closeFileListeners.addAll(listeners);
  }
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PartialPath`
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileProcessor.java`
#### Snippet
```java
   * <p>Delete data in both working MemTable and flushing MemTables.
   */
  public void deleteDataInMemory(Deletion deletion, Set<PartialPath> devicePaths) {
    flushQueryLock.writeLock().lock();
    if (logger.isDebugEnabled()) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PartialPath`
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileProcessor.java`
#### Snippet
```java
   */
  public void query(
      List<PartialPath> seriesPaths,
      QueryContext context,
      List<TsFileResource> tsfileResourcesForQuery)
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileProcessor.java`
#### Snippet
```java
      List<PartialPath> seriesPaths,
      QueryContext context,
      List<TsFileResource> tsfileResourcesForQuery)
      throws IOException {
    Map<PartialPath, List<IChunkMetadata>> pathToChunkMetadataListMap = new HashMap<>();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends FlushListener`
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileProcessor.java`
#### Snippet
```java
  }

  public void addFlushListeners(Collection<FlushListener> listeners) {
    flushListeners.addAll(listeners);
  }
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends ByteBuffer`
in `server/src/main/java/org/apache/iotdb/db/service/DataNode.java`
#### Snippet
```java
  }

  private void getTriggerInformationList(List<ByteBuffer> allTriggerInformation) {
    if (allTriggerInformation != null && !allTriggerInformation.isEmpty()) {
      List<TriggerInformation> list = new ArrayList<>();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends ByteBuffer`
in `server/src/main/java/org/apache/iotdb/db/service/DataNode.java`
#### Snippet
```java
  }

  private void getUDFInformationList(List<ByteBuffer> allUDFInformation) {
    if (allUDFInformation != null && !allUDFInformation.isEmpty()) {
      List<UDFInformation> list = new ArrayList<>();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super PartialPath`
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileResource.java`
#### Snippet
```java
  /** unsealed TsFile, for query */
  public TsFileResource(
      Map<PartialPath, List<ReadOnlyMemChunk>> pathToReadOnlyMemChunkMap,
      Map<PartialPath, List<IChunkMetadata>> pathToChunkMetadataListMap,
      TsFileResource originTsFileResource)
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `server/src/main/java/org/apache/iotdb/db/service/thrift/impl/DataNodeInternalRPCServiceImpl.java`
#### Snippet
```java

  private Map<PartialPath, List<Integer>> transformTemplateSetInfo(
      Map<String, List<Integer>> rawTemplateSetInfo) {
    Map<PartialPath, List<Integer>> result = new HashMap<>();
    rawTemplateSetInfo.forEach(
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PartialPath`
in `server/src/main/java/org/apache/iotdb/db/service/thrift/impl/DataNodeInternalRPCServiceImpl.java`
#### Snippet
```java

  private Map<PartialPath, List<Integer>> filterTemplateSetInfo(
      Map<PartialPath, List<Integer>> templateSetInfo, TConsensusGroupId consensusGroupId) {

    PartialPath storageGroupPath = getStorageGroupPath(consensusGroupId);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `server/src/main/java/org/apache/iotdb/db/service/thrift/impl/DataNodeInternalRPCServiceImpl.java`
#### Snippet
```java

  private Map<PartialPath, List<Integer>> filterTemplateSetInfo(
      Map<PartialPath, List<Integer>> templateSetInfo, TConsensusGroupId consensusGroupId) {

    PartialPath storageGroupPath = getStorageGroupPath(consensusGroupId);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends IMNode`
in `server/src/main/java/org/apache/iotdb/db/metadata/mnode/iterator/MNodeIterator.java`
#### Snippet
```java
  private Iterator<IMNode> iterator;

  public MNodeIterator(Iterator<IMNode> iterator) {
    this.iterator = iterator;
  }
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends IMNode`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/memcontrol/MemManagerNodeEstimatedSizeBasedImpl.java`
#### Snippet
```java

  @Override
  public void releaseMemResource(List<IMNode> evictedNodes) {
    int size = 0;
    for (IMNode node : evictedNodes) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super IMNode`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/cache/CacheManager.java`
#### Snippet
```java
    }

    void forEachNode(Consumer<IMNode> action) {
      for (Map<CacheEntry, IMNode> map : maps) {
        for (IMNode node : map.values()) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super IMNode`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/cache/CacheManager.java`
#### Snippet
```java
  }

  private void collectEvictedMNodes(IMNode node, List<IMNode> evictedMNodes) {
    for (IMNode child : node.getChildren().values()) {
      removeFromNodeCache(getCacheEntry(child));
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super IMNode`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/cache/CacheManager.java`
#### Snippet
```java
  }

  private void collectVolatileNodes(IMNode node, List<IMNode> nodesToPersist) {
    Iterator<IMNode> bufferIterator = getCachedMNodeContainer(node).getChildrenBufferIterator();

```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/DataRegion.java`
#### Snippet
```java
  // may remove the iterator's data
  private void removePartitions(
      TimePartitionFilter filter, Iterator<TsFileResource> iterator, boolean sequence) {
    while (iterator.hasNext()) {
      TsFileResource tsFileResource = iterator.next();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Long`
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/DataRegion.java`
#### Snippet
```java
   */
  private TsFileProcessor getOrCreateTsFileProcessorIntern(
      long timeRangeId, TreeMap<Long, TsFileProcessor> tsFileProcessorTreeMap, boolean sequence)
      throws IOException, DiskSpaceInsufficientException {

```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/DataRegion.java`
#### Snippet
```java
   */
  private int findInsertionPosition(
      TsFileResource newTsFileResource, List<TsFileResource> sequenceList) {

    int insertPos = -1;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/DataRegion.java`
#### Snippet
```java

  private Map<Long, List<TsFileResource>> splitResourcesByPartition(
      List<TsFileResource> resources) {
    Map<Long, List<TsFileResource>> ret = new TreeMap<>();
    for (TsFileResource resource : resources) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/DataRegion.java`
#### Snippet
```java

  private boolean isFileAlreadyExistInClosed(
      TsFileResource tsFileResource, long partitionNum, Collection<TsFileResource> existingFiles) {
    for (TsFileResource resource : existingFiles) {
      if (resource.getTimePartition() == partitionNum
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/DataRegion.java`
#### Snippet
```java
   */
  private List<TsFileResource> getFileResourceListForQuery(
      Collection<TsFileResource> tsFileResources,
      List<TsFileResource> upgradeTsFileResources,
      List<PartialPath> pathList,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/DataRegion.java`
#### Snippet
```java
  private List<TsFileResource> getFileResourceListForQuery(
      Collection<TsFileResource> tsFileResources,
      List<TsFileResource> upgradeTsFileResources,
      List<PartialPath> pathList,
      String singleDeviceId,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PartialPath`
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/DataRegion.java`
#### Snippet
```java
  private boolean canSkipDelete(
      TsFileResource tsFileResource,
      Set<PartialPath> devicePaths,
      long deleteStart,
      long deleteEnd,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/DataRegion.java`
#### Snippet
```java
      int insertIndex,
      TsFileResource newTsFileResource,
      List<TsFileResource> sequenceList) {
    long timePartitionId = newTsFileResource.getTimePartition();
    if (tsFileType == LoadTsFileType.LOAD_UNSEQUENCE || insertIndex == sequenceList.size() - 1) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/DataRegion.java`
#### Snippet
```java

  public void addSettleFilesToList(
      List<TsFileResource> seqResourcesToBeSettled,
      List<TsFileResource> unseqResourcesToBeSettled,
      List<String> tsFilePaths) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/DataRegion.java`
#### Snippet
```java
  public void addSettleFilesToList(
      List<TsFileResource> seqResourcesToBeSettled,
      List<TsFileResource> unseqResourcesToBeSettled,
      List<String> tsFilePaths) {
    if (tsFilePaths.size() == 0) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileProcessor`
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/DataRegion.java`
#### Snippet
```java
      TsFileResource tsFileResource,
      long partitionNum,
      Collection<TsFileProcessor> workingProcessors) {
    for (TsFileProcessor workingProcesssor : workingProcessors) {
      if (workingProcesssor.getTimeRangeId() == partitionNum) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/DataRegion.java`
#### Snippet
```java
  }

  private LoadTsFileType getLoadingTsFileType(int insertPos, List<TsFileResource> sequenceList) {
    if (insertPos == POS_OVERLAP) {
      return LoadTsFileType.LOAD_UNSEQUENCE;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/DataRegion.java`
#### Snippet
```java
      long partitionId,
      DataRegionRecoveryContext context,
      List<TsFileResource> resourceList,
      boolean isSeq,
      boolean isLatestPartition) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/DataRegion.java`
#### Snippet
```java
  /** Seperate tsfiles in TsFileManager to sealedList and unsealedList. */
  private void separateTsFile(
      List<TsFileResource> sealedResource, List<TsFileResource> unsealedResource) {
    tsFileManager
        .getTsFileList(true)
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/DataRegion.java`
#### Snippet
```java
  /** Seperate tsfiles in TsFileManager to sealedList and unsealedList. */
  private void separateTsFile(
      List<TsFileResource> sealedResource, List<TsFileResource> unsealedResource) {
    tsFileManager
        .getTsFileList(true)
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/DataRegion.java`
#### Snippet
```java
  }

  private void loadUpgradedResources(List<TsFileResource> resources, boolean isseq) {
    if (resources.isEmpty()) {
      return;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TsFileResource`
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/DataRegion.java`
#### Snippet
```java

  private void deleteDataInFiles(
      Collection<TsFileResource> tsFileResourceList,
      Deletion deletion,
      Set<PartialPath> devicePaths,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super ModificationFile`
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/DataRegion.java`
#### Snippet
```java
      Deletion deletion,
      Set<PartialPath> devicePaths,
      List<ModificationFile> updatedModFiles,
      TimePartitionFilter timePartitionFilter)
      throws IOException {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends IMNode`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/MockSchemaFile.java`
#### Snippet
```java
  }

  private void write(long address, Map<String, IMNode> nodeMap) {
    for (IMNode node : nodeMap.values()) {
      if (!node.isMeasurement()) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends IMNode`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/MockSchemaFile.java`
#### Snippet
```java
    Iterator<IMNode> iterator;

    MockSchemaFileIterator(Iterator<IMNode> iterator) {
      this.iterator = iterator;
    }
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super String`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/CachedMNodeContainer.java`
#### Snippet
```java
  }

  private IMNode replace(Map<String, IMNode> map, String key, IMNode value) {
    return map == null ? null : map.replace(key, value);
  }
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends IMNode`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/CachedMNodeContainer.java`
#### Snippet
```java
  }

  private IMNode get(Map<String, IMNode> map, Object key) {
    return map == null ? null : map.get(key);
  }
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends IMNode`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/CachedMNodeContainer.java`
#### Snippet
```java
  }

  private IMNode remove(Map<String, IMNode> map, Object key) {
    return map == null ? null : map.remove(key);
  }
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends IMNode`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/CachedMNodeContainer.java`
#### Snippet
```java

  @Override
  public synchronized void loadChildrenFromDisk(Map<String, IMNode> children) {
    if (childCache == null) {
      childCache = new ConcurrentHashMap<>();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Pair`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/Segment.java`
#### Snippet
```java
   * @throws RecordDuplicatedException
   */
  protected static <T> int binaryInsertPairList(List<Pair<String, T>> list, String key)
      throws RecordDuplicatedException {
    if (list.size() == 0) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Pair`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/Segment.java`
#### Snippet
```java
  protected abstract short getRecordLength();

  protected static <T> int binarySearchPairList(List<Pair<String, T>> list, String key) {
    int head = 0;
    int tail = list.size() - 1;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Integer`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/snapshot/MemMTreeSnapshotUtil.java`
#### Snippet
```java
  private static void deserializeMNode(
      Deque<IMNode> ancestors,
      Deque<Integer> restChildrenNum,
      MNodeDeserializer deserializer,
      InputStream inputStream,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super IMeasurementMNode`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/snapshot/MemMTreeSnapshotUtil.java`
#### Snippet
```java
      MNodeDeserializer deserializer,
      InputStream inputStream,
      Consumer<IMeasurementMNode> measurementProcess)
      throws IOException {
    byte type = ReadWriteIOUtils.readByte(inputStream);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends SchemaEntry`
in `server/src/main/java/org/apache/iotdb/db/metadata/idtable/IDTableHashmapImpl.java`
#### Snippet
```java
  @Override
  @TestOnly
  public synchronized List<DiskSchemaEntry> getDiskSchemaEntries(List<SchemaEntry> schemaEntries) {
    List<Long> offsets = new ArrayList<>(schemaEntries.size());
    for (SchemaEntry schemaEntry : schemaEntries) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PartialPath`
in `server/src/main/java/org/apache/iotdb/db/metadata/idtable/IDTableHashmapImpl.java`
#### Snippet
```java
   */
  @Override
  public synchronized Pair<Integer, Set<String>> deleteTimeseries(List<PartialPath> fullPaths)
      throws MetadataException {
    int deletedNum = 0;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends T`
in `server/src/main/java/org/apache/iotdb/db/metadata/logfile/FakeCRC32Deserializer.java`
#### Snippet
```java
      new ConfigurableDataInputStream(EMPTY_INPUT_STREAM_PLACE_HOLDER);

  public FakeCRC32Deserializer(IDeserializer<T> deserializer) {
    this.deserializer = deserializer;
  }
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super T`
in `server/src/main/java/org/apache/iotdb/db/metadata/logfile/BufferedSerializer.java`
#### Snippet
```java
  private final ISerializer<T> serializer;

  public BufferedSerializer(ISerializer<T> serializer) {
    this.serializer = serializer;
  }
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends T`
in `server/src/main/java/org/apache/iotdb/db/metadata/logfile/SchemaLogReader.java`
#### Snippet
```java
  }

  public SchemaLogReader(String logFilePath, IDeserializer<T> deserializer) throws IOException {
    logFile = SystemFileFactory.INSTANCE.getFile(logFilePath);
    inputStream = new RecordableInputStream(new BufferedInputStream(new FileInputStream(logFile)));
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends T`
in `server/src/main/java/org/apache/iotdb/db/metadata/logfile/SchemaLogReader.java`
#### Snippet
```java
  private boolean isFileCorrupted = false;

  public SchemaLogReader(String schemaDir, String logFileName, IDeserializer<T> deserializer)
      throws IOException {
    logFile = SystemFileFactory.INSTANCE.getFile(schemaDir + File.separator + logFileName);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super T`
in `server/src/main/java/org/apache/iotdb/db/metadata/logfile/FakeCRC32Serializer.java`
#### Snippet
```java
  private final ISerializer<T> serializer;

  public FakeCRC32Serializer(ISerializer<T> serializer) {
    this.serializer = serializer;
  }
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super T`
in `server/src/main/java/org/apache/iotdb/db/metadata/logfile/SchemaLogWriter.java`
#### Snippet
```java

  public SchemaLogWriter(
      String schemaDir, String logFileName, ISerializer<T> serializer, boolean forceEachWrite)
      throws IOException {
    File dir = SystemFileFactory.INSTANCE.getFile(schemaDir);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super T`
in `server/src/main/java/org/apache/iotdb/db/metadata/logfile/SchemaLogWriter.java`
#### Snippet
```java
  }

  public SchemaLogWriter(String logFilePath, ISerializer<T> serializer, boolean forceEachWrite)
      throws IOException {
    logFile = SystemFileFactory.INSTANCE.getFile(logFilePath);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PartialPath`
in `server/src/main/java/org/apache/iotdb/db/metadata/utils/MetaUtils.java`
#### Snippet
```java
   *     vector1 (s1,s2) would be returned once.
   */
  public static List<PartialPath> groupAlignedPaths(List<PartialPath> fullPaths) {
    List<PartialPath> result = new LinkedList<>();
    AlignedPath alignedPath = null;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PartialPath`
in `server/src/main/java/org/apache/iotdb/db/metadata/utils/MetaUtils.java`
#### Snippet
```java

  public static Map<PartialPath, List<AggregationDescriptor>> groupAlignedAggregations(
      Map<PartialPath, List<AggregationDescriptor>> pathToAggregations) {
    Map<PartialPath, List<AggregationDescriptor>> result = new HashMap<>();
    Map<String, List<MeasurementPath>> deviceToAlignedPathsMap = new HashMap<>();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `server/src/main/java/org/apache/iotdb/db/metadata/utils/MetaUtils.java`
#### Snippet
```java

  public static Map<PartialPath, List<AggregationDescriptor>> groupAlignedAggregations(
      Map<PartialPath, List<AggregationDescriptor>> pathToAggregations) {
    Map<PartialPath, List<AggregationDescriptor>> result = new HashMap<>();
    Map<String, List<MeasurementPath>> deviceToAlignedPathsMap = new HashMap<>();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PartialPath`
in `server/src/main/java/org/apache/iotdb/db/metadata/utils/MetaUtils.java`
#### Snippet
```java
   */
  public static Map<AlignedPath, List<List<Integer>>> groupAlignedSeriesWithAggregations(
      Map<PartialPath, List<Integer>> pathToAggrIndexesMap) {
    Map<AlignedPath, List<List<Integer>>> alignedPathToAggrIndexesMap = new HashMap<>();
    Map<String, AlignedPath> temp = new HashMap<>();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `server/src/main/java/org/apache/iotdb/db/metadata/utils/MetaUtils.java`
#### Snippet
```java
   */
  public static Map<AlignedPath, List<List<Integer>>> groupAlignedSeriesWithAggregations(
      Map<PartialPath, List<Integer>> pathToAggrIndexesMap) {
    Map<AlignedPath, List<List<Integer>>> alignedPathToAggrIndexesMap = new HashMap<>();
    Map<String, AlignedPath> temp = new HashMap<>();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PartialPath`
in `server/src/main/java/org/apache/iotdb/db/metadata/utils/MetaUtils.java`
#### Snippet
```java
  }

  public static List<PartialPath> groupAlignedSeries(List<PartialPath> fullPaths) {
    List<PartialPath> result = new ArrayList<>();
    Map<String, AlignedPath> deviceToAlignedPathMap = new HashMap<>();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super IMNode`
in `server/src/main/java/org/apache/iotdb/db/metadata/utils/MetaUtils.java`
#### Snippet
```java

  @TestOnly
  public static void collectLastNode(IMNode node, List<IMNode> lastNodeList) {
    if (node != null) {
      Map<String, IMNode> children = node.getChildren();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super IMeasurementMNode`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/MTreeBelowSGMemoryImpl.java`
#### Snippet
```java
      MemMTreeStore store,
      IStorageGroupMNode storageGroupMNode,
      Function<IMeasurementMNode, Map<String, String>> tagGetter,
      int schemaRegionId) {
    this.store = store;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Map`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/MTreeBelowSGMemoryImpl.java`
#### Snippet
```java
      MemMTreeStore store,
      IStorageGroupMNode storageGroupMNode,
      Function<IMeasurementMNode, Map<String, String>> tagGetter,
      int schemaRegionId) {
    this.store = store;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super IMeasurementMNode`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/MTreeBelowSGMemoryImpl.java`
#### Snippet
```java
  public MTreeBelowSGMemoryImpl(
      IStorageGroupMNode storageGroupMNode,
      Function<IMeasurementMNode, Map<String, String>> tagGetter,
      int schemaRegionId) {
    PartialPath storageGroup = storageGroupMNode.getPartialPath();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Map`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/MTreeBelowSGMemoryImpl.java`
#### Snippet
```java
  public MTreeBelowSGMemoryImpl(
      IStorageGroupMNode storageGroupMNode,
      Function<IMeasurementMNode, Map<String, String>> tagGetter,
      int schemaRegionId) {
    PartialPath storageGroup = storageGroupMNode.getPartialPath();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super IMeasurementMNode`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/MTreeBelowSGCachedImpl.java`
#### Snippet
```java
  public MTreeBelowSGCachedImpl(
      IStorageGroupMNode storageGroupMNode,
      Function<IMeasurementMNode, Map<String, String>> tagGetter,
      int schemaRegionId)
      throws MetadataException, IOException {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Map`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/MTreeBelowSGCachedImpl.java`
#### Snippet
```java
  public MTreeBelowSGCachedImpl(
      IStorageGroupMNode storageGroupMNode,
      Function<IMeasurementMNode, Map<String, String>> tagGetter,
      int schemaRegionId)
      throws MetadataException, IOException {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super IMeasurementMNode`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/MTreeBelowSGCachedImpl.java`
#### Snippet
```java
      IStorageGroupMNode storageGroupMNode,
      Consumer<IMeasurementMNode> measurementProcess,
      Function<IMeasurementMNode, Map<String, String>> tagGetter)
      throws MetadataException {
    this.store = store;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Map`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/MTreeBelowSGCachedImpl.java`
#### Snippet
```java
      IStorageGroupMNode storageGroupMNode,
      Consumer<IMeasurementMNode> measurementProcess,
      Function<IMeasurementMNode, Map<String, String>> tagGetter)
      throws MetadataException {
    this.store = store;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends InsertNode`
in `server/src/main/java/org/apache/iotdb/db/consensus/statemachine/DataRegionStateMachine.java`
#### Snippet
```java
   * upper layer.
   */
  private InsertNode mergeInsertNodes(List<InsertNode> insertNodes) {
    int size = insertNodes.size();
    if (size == 0) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Integer`
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/SchemaRegionMemoryImpl.java`
#### Snippet
```java
      PartialPath devicePath,
      String[] measurements,
      Function<Integer, TSDataType> getDataType,
      TSEncoding[] encodings,
      CompressionType[] compressionTypes,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends ISchemaRegionPlan`
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/SchemaRegionMemoryImpl.java`
#### Snippet
```java
  }

  private int applyMLog(SchemaLogReader<ISchemaRegionPlan> mLogReader) {
    int idx = 0;
    ISchemaRegionPlan plan;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PartialPath`
in `server/src/main/java/org/apache/iotdb/db/localconfignode/LocalSchemaPartitionTable.java`
#### Snippet
```java

  public synchronized void init(
      Map<PartialPath, List<SchemaRegionId>> recoveredLocalSchemaRegionInfo)
      throws MetadataException {
    table = new ConcurrentHashMap<>();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `server/src/main/java/org/apache/iotdb/db/localconfignode/LocalSchemaPartitionTable.java`
#### Snippet
```java

  public synchronized void init(
      Map<PartialPath, List<SchemaRegionId>> recoveredLocalSchemaRegionInfo)
      throws MetadataException {
    table = new ConcurrentHashMap<>();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends IExpression`
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/handler/AbstractQueryHandler.java`
#### Snippet
```java
   */
  public QueryResult queryByConditions(
      List<IExpression> expressions,
      String database,
      String measurement,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends IExpression`
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/handler/TagQueryHandler.java`
#### Snippet
```java
  @Override
  public QueryResult queryByConditions(
      List<IExpression> expressions,
      String database,
      String measurement,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Pair`
in `server/src/main/java/org/apache/iotdb/db/metadata/utils/ResourceByPathUtils.java`
#### Snippet
```java
  @Override
  protected List<Modification> getModificationsForMemtable(
      IMemTable memTable, List<Pair<Modification, IMemTable>> modsToMemtable) {
    List<Modification> modifications = new ArrayList<>();
    boolean foundMemtable = false;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Pair`
in `server/src/main/java/org/apache/iotdb/db/metadata/utils/ResourceByPathUtils.java`
#### Snippet
```java
  /** get modifications from a memtable. */
  protected List<Modification> getModificationsForMemtable(
      IMemTable memTable, List<Pair<Modification, IMemTable>> modsToMemtable) {
    List<Modification> modifications = new ArrayList<>();
    boolean foundMemtable = false;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends ReadOnlyMemChunk`
in `server/src/main/java/org/apache/iotdb/db/metadata/utils/ResourceByPathUtils.java`
#### Snippet
```java
  @Override
  public ITimeSeriesMetadata generateTimeSeriesMetadata(
      List<ReadOnlyMemChunk> readOnlyMemChunk, List<IChunkMetadata> chunkMetadataList)
      throws IOException {
    TimeseriesMetadata timeSeriesMetadata = new TimeseriesMetadata();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends IChunkMetadata`
in `server/src/main/java/org/apache/iotdb/db/metadata/utils/ResourceByPathUtils.java`
#### Snippet
```java
  @Override
  public ITimeSeriesMetadata generateTimeSeriesMetadata(
      List<ReadOnlyMemChunk> readOnlyMemChunk, List<IChunkMetadata> chunkMetadataList)
      throws IOException {
    TimeseriesMetadata timeSeriesMetadata = new TimeseriesMetadata();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends ReadOnlyMemChunk`
in `server/src/main/java/org/apache/iotdb/db/metadata/utils/ResourceByPathUtils.java`
#### Snippet
```java
  @Override
  public AlignedTimeSeriesMetadata generateTimeSeriesMetadata(
      List<ReadOnlyMemChunk> readOnlyMemChunk, List<IChunkMetadata> chunkMetadataList)
      throws IOException {
    TimeseriesMetadata timeTimeSeriesMetadata = new TimeseriesMetadata();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends IChunkMetadata`
in `server/src/main/java/org/apache/iotdb/db/metadata/utils/ResourceByPathUtils.java`
#### Snippet
```java
  @Override
  public AlignedTimeSeriesMetadata generateTimeSeriesMetadata(
      List<ReadOnlyMemChunk> readOnlyMemChunk, List<IChunkMetadata> chunkMetadataList)
      throws IOException {
    TimeseriesMetadata timeTimeSeriesMetadata = new TimeseriesMetadata();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends SchemaRegionId`
in `server/src/main/java/org/apache/iotdb/db/localconfignode/LocalConfigNode.java`
#### Snippet
```java

  private void deleteSchemaRegionsInStorageGroup(
      PartialPath storageGroup, List<SchemaRegionId> schemaRegionIdSet) throws MetadataException {
    for (SchemaRegionId schemaRegionId : schemaRegionIdSet) {
      schemaEngine.deleteSchemaRegion(schemaRegionId);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PartialPath`
in `server/src/main/java/org/apache/iotdb/db/localconfignode/LocalConfigNode.java`
#### Snippet
```java
   * @param storageGroups list of paths to be deleted.
   */
  public void deleteStorageGroups(List<PartialPath> storageGroups) throws MetadataException {
    for (PartialPath storageGroup : storageGroups) {
      deleteStorageGroup(storageGroup);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends DataRegionId`
in `server/src/main/java/org/apache/iotdb/db/localconfignode/LocalConfigNode.java`
#### Snippet
```java
  }

  private void deleteDataRegionsInStorageGroup(List<DataRegionId> dataRegionIdSet) {
    for (DataRegionId dataRegionId : dataRegionIdSet) {
      storageEngine.deleteDataRegion(dataRegionId);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Pair`
in `library-udf/src/main/java/org/apache/iotdb/library/drepair/util/Screen.java`
#### Snippet
```java
  }

  private double getMedian(ArrayList<Pair<Long, Double>> list, int index) {
    int m = 0;
    while (index + m + 1 < list.size()
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Pair`
in `library-udf/src/main/java/org/apache/iotdb/library/drepair/util/Screen.java`
#### Snippet
```java
  }

  private double getRepairedValue(ArrayList<Pair<Long, Double>> list, int index, double mid) {
    double xmin =
        list.get(index - 1).getRight()
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Pair`
in `library-udf/src/main/java/org/apache/iotdb/library/drepair/util/ScreenFill.java`
#### Snippet
```java
  }

  private double getRepairedValue(ArrayList<Pair<Long, Double>> list, int index, double mid) {
    double xmin =
        list.get(index - 1).getRight()
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Pair`
in `library-udf/src/main/java/org/apache/iotdb/library/drepair/util/ScreenFill.java`
#### Snippet
```java
  }

  private double getMedian(ArrayList<Pair<Long, Double>> list, int index) {
    int m = 0;
    while (index + m + 1 < list.size()
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends org.apache.iotdb.library.dprofile.util.KLLSketchForQuantile`
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/HeapLongKLLSketch.java`
#### Snippet
```java

  public void mergeWithTempSpace(
      List<org.apache.iotdb.library.dprofile.util.KLLSketchForQuantile> otherList) {
    //    System.out.println("[MERGE]");
    //    show();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super T`
in `metrics/micrometer-metrics/src/main/java/org/apache/iotdb/metrics/micrometer/type/MicrometerAutoGauge.java`
#### Snippet
```java
      String metricName,
      T object,
      ToLongFunction<T> mapper,
      String... tags) {
    LoggerFactory.getLogger(MicrometerAutoGauge.class).info("{},{}", metricName, tags);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Tag`
in `metrics/micrometer-metrics/src/main/java/org/apache/iotdb/metrics/micrometer/reporter/IoTDBMeterRegistry.java`
#### Snippet
```java
  }

  private Map<String, String> tagsConvertToMap(List<Tag> tags) {
    Map<String, String> labels = new HashMap<>();
    for (Tag tag : tags) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends File`
in `rewrite-tsfile-tool/src/main/java/org/apache/iotdb/RewriteTsFileTool.java`
#### Snippet
```java
   * @throws NoMeasurementException
   */
  public static void reverseWriteTsFile(List<File> files, Session session)
      throws IOException, IllegalPathException, IoTDBConnectionException,
          StatementExecutionException, NoMeasurementException {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super MeasurementSchema`
in `rewrite-tsfile-tool/src/main/java/org/apache/iotdb/RewriteTsFileTool.java`
#### Snippet
```java
  private static void readAndSendValuePage(
      boolean addSchema,
      List<MeasurementSchema> schemaForAlignedSeries,
      ChunkHeader header,
      PageHeader pageHeader,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super TsPrimitiveType`
in `rewrite-tsfile-tool/src/main/java/org/apache/iotdb/RewriteTsFileTool.java`
#### Snippet
```java
      List<long[]> timeBatch,
      int pageIndex,
      List<TsPrimitiveType> valueList) {
    if (!addSchema && header.getDataType() != TSDataType.VECTOR) {
      schemaForAlignedSeries.add(
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends File`
in `rewrite-tsfile-tool/src/main/java/org/apache/iotdb/RewriteTsFileTool.java`
#### Snippet
```java
  }

  private static void writeTsFileSequentially(List<File> files, Session session) {
    int size = files.size();
    List<File> unloadTsFiles = new ArrayList<>();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super MeasurementSchema`
in `rewrite-tsfile-tool/src/main/java/org/apache/iotdb/RewriteTsFileTool.java`
#### Snippet
```java
  private static void readAndSendTimePage(
      boolean addSchema,
      List<MeasurementSchema> schemaForAlignedSeries,
      ChunkHeader header,
      PageHeader pageHeader,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Long`
in `rewrite-tsfile-tool/src/main/java/org/apache/iotdb/RewriteTsFileTool.java`
#### Snippet
```java
      List<long[]> timeBatch,
      int pageIndex,
      List<Long> timeForAlignedSeries)
      throws IOException {
    if (!addSchema && header.getDataType() != TSDataType.VECTOR) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Pair`>
in `rewrite-tsfile-tool/src/main/java/org/apache/iotdb/RewriteTsFileTool.java`
#### Snippet
```java
  /** Collect the schema list for an aligned device. */
  private static List<IMeasurementSchema> collectSchemaFromAlignedChunkMetadataList(
      LinkedList<Pair<TsFileSequenceReader, List<AlignedChunkMetadata>>> readerAndChunkMetadataList)
      throws IOException {
    Set<MeasurementSchema> schemaSet = new HashSet<>();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends File`
in `rewrite-tsfile-tool/src/main/java/org/apache/iotdb/RewriteTsFileTool.java`
#### Snippet
```java
  }

  private static void sortTsFiles(List<File> files) {
    Map<File, Long> file2Timestamp = new HashMap<>();
    Map<File, Long> file2Version = new HashMap<>();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends RegionStatistics`
in `confignode/src/main/java/org/apache/iotdb/confignode/manager/partition/heartbeat/RegionGroupCache.java`
#### Snippet
```java

  private RegionGroupStatus updateRegionGroupStatus(
      Map<Integer, RegionStatistics> regionStatisticsMap) {
    int unknownCount = 0;
    for (RegionStatistics regionStatistics : regionStatisticsMap.values()) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends RegionHeartbeatSample`
in `confignode/src/main/java/org/apache/iotdb/confignode/manager/partition/heartbeat/RegionGroupCache.java`
#### Snippet
```java
   *     RegionGroupStatistics
   */
  public void forceUpdate(Map<Integer, RegionHeartbeatSample> newHeartbeatSamples) {
    newHeartbeatSamples.forEach(this::cacheHeartbeatSample);
    updateCurrentStatistics();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends ISchemaRegionPlan`
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/SchemaRegionSchemaFileImpl.java`
#### Snippet
```java
  }

  private int applyMLog(SchemaLogReader<ISchemaRegionPlan> mLogReader) {
    int idx = 0;
    ISchemaRegionPlan plan;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PartialPath`
in `confignode/src/main/java/org/apache/iotdb/confignode/manager/ConfigManager.java`
#### Snippet
```java
   * @return List of PartialPath the databases that not exist
   */
  public List<PartialPath> checkStorageGroupExist(List<PartialPath> storageGroups) {
    List<PartialPath> noExistSg = new ArrayList<>();
    if (storageGroups == null) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Procedure`
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/Procedure.java`
#### Snippet
```java

  /** Helper to lookup the root Procedure ID given a specified procedure. */
  protected static long getRootProcedureId(Map<Long, Procedure> procedures, Procedure proc) {
    while (proc.hasParent()) {
      proc = procedures.get(proc.getParentProcId());
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Procedure`
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/store/ProcedureWAL.java`
#### Snippet
```java
   * @param procedureList procedure list
   */
  public void load(List<Procedure> procedureList) {
    Procedure procedure = null;
    try (FileInputStream fis = new FileInputStream(walFilePath.toFile());
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends AtomicLong`
in `confignode/src/main/java/org/apache/iotdb/confignode/persistence/partition/RegionGroup.java`
#### Snippet
```java

  /** @param deltaMap Map<TSeriesPartitionSlot, Delta TTimePartitionSlot Count> */
  public void updateSlotCountMap(Map<TSeriesPartitionSlot, AtomicLong> deltaMap) {
    deltaMap.forEach(
        ((seriesPartitionSlot, delta) -> {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Env`
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/ProcedureExecutor.java`
#### Snippet
```java
  }

  private ProcedureLockState acquireLock(Procedure<Env> proc) {
    if (proc.hasLock()) {
      return ProcedureLockState.LOCK_ACQUIRED;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Long`
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/ProcedureExecutor.java`
#### Snippet
```java
  }

  private void restoreLock(Procedure procedure, Set<Long> restored) {
    procedure.restoreLock(environment);
    restored.add(procedure.getProcId());
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Env`
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/ProcedureExecutor.java`
#### Snippet
```java
  }

  private void releaseLock(Procedure<Env> procedure, boolean force) {
    if (force || !procedure.holdLock(this.environment) || procedure.isFinished()) {
      procedure.doReleaseLock(this.environment, store);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Procedure`
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/ProcedureExecutor.java`
#### Snippet
```java
  }

  private void restoreLocks(Deque<Procedure<Env>> stack, Set<Long> restored) {
    while (!stack.isEmpty()) {
      restoreLock(stack.pop(), restored);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super IN`
in `flink-iotdb-connector/src/main/java/org/apache/iotdb/flink/IoTDBSink.java`
#### Snippet
```java
  private int sessionPoolSize = 2;

  public IoTDBSink(IoTDBSinkOptions options, IoTSerializationSchema<IN> schema) {
    this.options = options;
    this.serializationSchema = schema;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PartialPath`
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/impl/schema/DeactivateTemplateProcedure.java`
#### Snippet
```java
  }

  private void prepareDataNodeRequest(Map<PartialPath, List<Template>> templateSetInfo) {
    Map<String, List<Integer>> dataNodeRequest = new HashMap<>();
    templateSetInfo.forEach(
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/impl/schema/DeactivateTemplateProcedure.java`
#### Snippet
```java
  }

  private void prepareDataNodeRequest(Map<PartialPath, List<Template>> templateSetInfo) {
    Map<String, List<Integer>> dataNodeRequest = new HashMap<>();
    templateSetInfo.forEach(
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends QueryExpression`
in `spark-tsfile/src/main/java/org/apache/iotdb/spark/tsfile/qp/Executor.java`
#### Snippet
```java

  public static List<QueryDataSet> query(
      TsFileReader reader, List<QueryExpression> queryExpressions, long start, long end) {
    List<QueryDataSet> dataSets = new ArrayList<>();
    try {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super FilterOperator`
in `spark-tsfile/src/main/java/org/apache/iotdb/spark/tsfile/qp/optimizer/DNFFilterOptimizer.java`
#### Snippet
```java
   * @param newChildrenList
   */
  private void addChildOpInOr(FilterOperator child, List<FilterOperator> newChildrenList) {
    if (child.isLeaf() || child.getTokenIntType() == KW_AND) {
      newChildrenList.add(child);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super FilterOperator`
in `spark-tsfile/src/main/java/org/apache/iotdb/spark/tsfile/qp/optimizer/DNFFilterOptimizer.java`
#### Snippet
```java
   * @throws DNFOptimizeException
   */
  private void addChildOpInAnd(FilterOperator child, List<FilterOperator> newChildrenList)
      throws DNFOptimizeException {
    if (child.isLeaf()) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends FilterOperator`
in `spark-tsfile/src/main/java/org/apache/iotdb/spark/tsfile/qp/optimizer/MergeSingleFilterOptimizer.java`
#### Snippet
```java
  }

  private boolean allIsBasic(List<FilterOperator> children) {
    for (FilterOperator child : children) {
      if (!(child instanceof BasicOperator)) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends FilterOperator`
in `spark-tsfile/src/main/java/org/apache/iotdb/spark/tsfile/qp/optimizer/PhysicalOptimizer.java`
#### Snippet
```java
  }

  private Map<String, Set<String>> mergeColumns(List<FilterOperator> columnFilterOperators) {
    Map<String, Set<String>> column_values_map = new HashMap<>();
    for (FilterOperator filterOperator : columnFilterOperators) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Set`
in `spark-tsfile/src/main/java/org/apache/iotdb/spark/tsfile/qp/optimizer/PhysicalOptimizer.java`
#### Snippet
```java
  private void combination(
      List<String> actualDeltaObjects,
      Map<String, Set<String>> columnValues,
      Object[] columns,
      int beginIndex,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TypeInfo`
in `hive-connector/src/main/java/org/apache/iotdb/hive/TsFileDeserializer.java`
#### Snippet
```java
  @SuppressWarnings("squid:S3776") // Suppress high Cognitive Complexity warning
  public Object deserialize(
      List<String> columnNames, List<TypeInfo> columnTypes, Writable writable, String deviceId)
      throws TsFileSerDeException {
    if (!(writable instanceof MapWritable)) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super String`
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/RSchemaReadWriteHandler.java`
#### Snippet
```java
  }

  public void scanAllKeysRecursively(Set<String> seeds, int level, Function<String, Boolean> op) {
    if (seeds == null || seeds.isEmpty()) {
      return;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super String`
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/RSchemaReadWriteHandler.java`
#### Snippet
```java
  }

  public void getKeyByPrefix(String innerName, Function<String, Boolean> function) {
    try (RocksIterator iterator = rocksDB.newIterator()) {
      for (iterator.seek(innerName.getBytes()); iterator.isValid(); iterator.next()) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends ChunkMetadata`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/file/metadata/TimeseriesMetadata.java`
#### Snippet
```java

  // For reading version-2 only
  public void setChunkMetadataList(ArrayList<ChunkMetadata> chunkMetadataList) {
    this.chunkMetadataList = new ArrayList<>(chunkMetadataList);
  }
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Path`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/file/metadata/TsFileMetadata.java`
#### Snippet
```java
   * @return bloom filter
   */
  private BloomFilter buildBloomFilter(Set<Path> paths) {
    BloomFilter filter =
        BloomFilter.getEmptyBloomFilter(
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends IChunkMetadata`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/file/metadata/AlignedTimeSeriesMetadata.java`
#### Snippet
```java

  private List<AlignedChunkMetadata> getAlignedChunkMetadata(
      List<IChunkMetadata> timeChunkMetadata, List<List<IChunkMetadata>> valueChunkMetadataList) {
    List<AlignedChunkMetadata> res = new ArrayList<>();
    for (int i = 0; i < timeChunkMetadata.size(); i++) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/file/metadata/AlignedTimeSeriesMetadata.java`
#### Snippet
```java

  private List<AlignedChunkMetadata> getAlignedChunkMetadata(
      List<IChunkMetadata> timeChunkMetadata, List<List<IChunkMetadata>> valueChunkMetadataList) {
    List<AlignedChunkMetadata> res = new ArrayList<>();
    for (int i = 0; i < timeChunkMetadata.size(); i++) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super MetadataIndexNode`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/file/metadata/MetadataIndexConstructor.java`
#### Snippet
```java
  public static void addCurrentIndexNodeToQueue(
      MetadataIndexNode currentIndexNode,
      Queue<MetadataIndexNode> metadataIndexNodeQueue,
      TsFileOutput out)
      throws IOException {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends AlignedChunkMetadata`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/TsFileAlignedSeriesReaderIterator.java`
#### Snippet
```java
  public TsFileAlignedSeriesReaderIterator(
      TsFileSequenceReader reader,
      List<AlignedChunkMetadata> alignedChunkMetadataList,
      List<IMeasurementSchema> schemaList) {
    this.reader = reader;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends IMeasurementSchema`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/TsFileAlignedSeriesReaderIterator.java`
#### Snippet
```java
      TsFileSequenceReader reader,
      List<AlignedChunkMetadata> alignedChunkMetadataList,
      List<IMeasurementSchema> schemaList) {
    this.reader = reader;
    this.alignedChunkMetadataList = alignedChunkMetadataList;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super String`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/v2/read/TsFileSequenceReaderForV2.java`
#### Snippet
```java
      String deviceId,
      MetadataIndexNodeType type,
      Map<String, List<TimeseriesMetadata>> timeseriesMetadataMap)
      throws IOException {
    switch (type) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends ChunkMetadata`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/v2/read/TsFileSequenceReaderForV2.java`
#### Snippet
```java
  }

  private void applyVersion(List<ChunkMetadata> chunkMetadataList) {
    if (versionInfo == null || versionInfo.isEmpty()) {
      return;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends FileSeriesReaderByTimestamp`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/query/dataset/DataSetWithTimeGenerator.java`
#### Snippet
```java
      List<TSDataType> dataTypes,
      TimeGenerator timeGenerator,
      List<FileSeriesReaderByTimestamp> readers) {
    super(paths, dataTypes);
    this.cached = cached;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Path`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/query/executor/ExecutorWithTimeGenerator.java`
#### Snippet
```java
  }

  private static void getAllFilteredPaths(IExpression expression, HashSet<Path> paths) {
    if (expression instanceof BinaryExpression) {
      getAllFilteredPaths(((BinaryExpression) expression).getLeft(), paths);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Path`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/query/executor/ExecutorWithTimeGenerator.java`
#### Snippet
```java

  public static List<Boolean> markFilterdPaths(
      IExpression expression, List<Path> selectedPaths, boolean hasOrNode) {
    List<Boolean> cached = new ArrayList<>();
    if (hasOrNode) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Long`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/query/timegenerator/node/AndNode.java`
#### Snippet
```java
  }

  private boolean fillNextCache(BiPredicate<Long, Long> seekRight) throws IOException {
    long leftValue = leftChild.next();
    long rightValue = rightChild.next();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Long`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/query/timegenerator/node/AndNode.java`
#### Snippet
```java
  }

  private boolean fillNextCache(BiPredicate<Long, Long> seekRight) throws IOException {
    long leftValue = leftChild.next();
    long rightValue = rightChild.next();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends AbstractFileSeriesReader`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/query/dataset/DataSetWithoutTimeGenerator.java`
#### Snippet
```java
   */
  public DataSetWithoutTimeGenerator(
      List<Path> paths, List<TSDataType> dataTypes, List<AbstractFileSeriesReader> readers)
      throws IOException {
    super(paths, dataTypes);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TimeRange`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/TimeRange.java`
#### Snippet
```java
   */
  @SuppressWarnings("squid:S3776") // Suppress high Cognitive Complexity warning
  public List<TimeRange> getRemains(List<TimeRange> timeRangesPrev) {
    List<TimeRange> remains = new ArrayList<>();

```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/reader/page/AlignedPageReader.java`
#### Snippet
```java
  }

  public void setDeleteIntervalList(List<List<TimeRange>> list) {
    for (int i = 0; i < valueCount; i++) {
      if (valuePageReaderList.get(i) != null) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Statistics`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/reader/chunk/AlignedChunkReader.java`
#### Snippet
```java
  /** construct all the page readers in this chunk */
  private void initAllPageReaders(
      Statistics timeChunkStatistics, List<Statistics> valueChunkStatisticsList)
      throws IOException {
    // construct next satisfied page header
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TimeRange`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/reader/chunk/AlignedChunkReader.java`
#### Snippet
```java

  /** used for value page filter */
  protected boolean pageSatisfied(PageHeader pageHeader, List<TimeRange> valueDeleteInterval) {
    if (currentTimestamp > pageHeader.getEndTime()) {
      // used for chunk reader by timestamp
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PageHeader`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/reader/chunk/AlignedChunkReader.java`
#### Snippet
```java

  private void skipBytesInStreamByLength(
      PageHeader timePageHeader, List<PageHeader> valuePageHeader) {
    timeChunkDataBuffer.position(
        timeChunkDataBuffer.position() + timePageHeader.getCompressedSize());
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends PageHeader`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/reader/chunk/AlignedChunkReader.java`
#### Snippet
```java

  private AlignedPageReader constructPageReaderForNextPage(
      PageHeader timePageHeader, List<PageHeader> valuePageHeader) throws IOException {
    PageInfo timePageInfo = new PageInfo();
    getPageInfo(timePageHeader, timeChunkDataBuffer, timeChunkHeader, timePageInfo);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Path`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/controller/MetadataQuerierByFileImpl.java`
#### Snippet
```java
  @Override
  @SuppressWarnings("squid:S3776") // Suppress high Cognitive Complexity warning
  public void loadChunkMetaDatas(List<Path> paths) throws IOException {
    // group measurements by device
    TreeMap<String, Set<String>> deviceMeasurementsMap = new TreeMap<>();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Path`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/controller/MetadataQuerierByFileImpl.java`
#### Snippet
```java

  @Override
  public Map<Path, List<IChunkMetadata>> getChunkMetaDataMap(List<Path> paths) throws IOException {
    Map<Path, List<IChunkMetadata>> chunkMetaDatas = new HashMap<>();
    for (Path path : paths) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Path`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/controller/MetadataQuerierByFileImpl.java`
#### Snippet
```java
  @SuppressWarnings("squid:S3776") // Suppress high Cognitive Complexity warning
  public List<TimeRange> convertSpace2TimePartition(
      List<Path> paths, long spacePartitionStartPos, long spacePartitionEndPos) throws IOException {
    if (spacePartitionStartPos > spacePartitionEndPos) {
      throw new IllegalArgumentException(
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends MeasurementSchema`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/MeasurementGroup.java`
#### Snippet
```java
  }

  public MeasurementGroup(boolean isAligned, List<MeasurementSchema> measurementSchemas) {
    this.isAligned = isAligned;
    measurementSchemaMap = new HashMap<>();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Path`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/expression/util/ExpressionOptimizer.java`
#### Snippet
```java
   */
  private IExpression pushGlobalTimeFilterToAllSeries(
      GlobalTimeExpression timeFilter, List<Path> selectedSeries)
      throws QueryFilterOptimizationException {
    if (selectedSeries.size() == 0) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends DataPoint`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/chunk/NonAlignedChunkGroupWriterImpl.java`
#### Snippet
```java

  @Override
  public int write(long time, List<DataPoint> data) throws IOException, WriteProcessException {
    int pointCount = 0;
    for (DataPoint point : data) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends MeasurementSchema`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/chunk/NonAlignedChunkGroupWriterImpl.java`
#### Snippet
```java

  @Override
  public void tryToAddSeriesWriter(List<MeasurementSchema> schemas) {
    for (IMeasurementSchema schema : schemas) {
      if (!chunkWriters.containsKey(schema.getMeasurementId())) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends MeasurementSchema`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/TsFileGeneratorUtils.java`
#### Snippet
```java
      TsFileWriter tsFileWriter,
      String deviceId,
      List<MeasurementSchema> schemas,
      long rowSize,
      long startTime,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends MeasurementSchema`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/TsFileWriter.java`
#### Snippet
```java
   * @throws WriteProcessException
   */
  public void registerAlignedTimeseries(Path devicePath, List<MeasurementSchema> measurementSchemas)
      throws WriteProcessException {
    if (schema.containsDevice(devicePath)) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends MeasurementSchema`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/TsFileWriter.java`
#### Snippet
```java
   * @param measurementSchemas
   */
  public void registerTimeseries(Path devicePath, List<MeasurementSchema> measurementSchemas) {
    for (MeasurementSchema schema : measurementSchemas) {
      try {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends DataPoint`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/TsFileWriter.java`
#### Snippet
```java
   */
  private List<MeasurementSchema> checkIsAllMeasurementsInGroup(
      List<DataPoint> dataPoints, MeasurementGroup measurementGroup, boolean isAligned)
      throws NoMeasurementException {
    if (isAligned && !measurementGroup.isAligned()) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends MeasurementSchema`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/TsFileWriter.java`
#### Snippet
```java
  private void checkIsAllMeasurementsInGroup(
      MeasurementGroup measurementGroup,
      List<MeasurementSchema> measurementSchemas,
      boolean isAligned)
      throws NoMeasurementException {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends IMeasurementSchema`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/chunk/AlignedChunkWriterImpl.java`
#### Snippet
```java
  }

  public AlignedChunkWriterImpl(List<IMeasurementSchema> schemaList) {
    TSEncoding timeEncoding =
        TSEncoding.valueOf(TSFileDescriptor.getInstance().getConfig().getTimeEncoder());
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends DataPoint`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/chunk/AlignedChunkGroupWriterImpl.java`
#### Snippet
```java

  @Override
  public int write(long time, List<DataPoint> data) throws WriteProcessException, IOException {
    checkIsHistoryData(time);
    List<ValueChunkWriter> emptyValueChunkWriters = new ArrayList<>();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends MeasurementSchema`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/chunk/AlignedChunkGroupWriterImpl.java`
#### Snippet
```java

  @Override
  public void tryToAddSeriesWriter(List<MeasurementSchema> measurementSchemas) throws IOException {
    for (MeasurementSchema schema : measurementSchemas) {
      if (!valueChunkWriterMap.containsKey(schema.getMeasurementId())) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends ValueChunkWriter`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/chunk/AlignedChunkGroupWriterImpl.java`
#### Snippet
```java
  }

  private void writeEmptyDataInOneRow(List<ValueChunkWriter> valueChunkWriterList) {
    for (ValueChunkWriter valueChunkWriter : valueChunkWriterList) {
      TSDataType dataType = valueChunkWriter.getDataType();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Map`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/ReadWriteIOUtils.java`
#### Snippet
```java
  }

  public static void write(List<Map<String, String>> maps, OutputStream stream) throws IOException {
    for (Map<String, String> map : maps) {
      write(map, stream);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Map`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/ReadWriteIOUtils.java`
#### Snippet
```java
  }

  public static void write(List<Map<String, String>> maps, ByteBuffer buffer) {
    for (Map<String, String> map : maps) {
      write(map, buffer);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super T`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/common/conf/TSFileDescriptor.java`
#### Snippet
```java

    private <T> void set(
        Consumer<T> setter, String propertyKey, Function<String, T> propertyValueConverter) {
      String value = this.properties.getProperty(propertyKey);

```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super String`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/common/conf/TSFileDescriptor.java`
#### Snippet
```java

    private <T> void set(
        Consumer<T> setter, String propertyKey, Function<String, T> propertyValueConverter) {
      String value = this.properties.getProperty(propertyKey);

```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends T`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/common/conf/TSFileDescriptor.java`
#### Snippet
```java

    private <T> void set(
        Consumer<T> setter, String propertyKey, Function<String, T> propertyValueConverter) {
      String value = this.properties.getProperty(propertyKey);

```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Integer`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/common/conf/TSFileDescriptor.java`
#### Snippet
```java
    }

    public void setInt(Consumer<Integer> setter, String propertyKey) {
      set(setter, propertyKey, Integer::parseInt);
    }
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Double`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/common/conf/TSFileDescriptor.java`
#### Snippet
```java
    }

    public void setDouble(Consumer<Double> setter, String propertyKey) {
      set(setter, propertyKey, Double::parseDouble);
    }
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Path`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/writer/TsFileIOWriter.java`
#### Snippet
```java

  /** Remove such ChunkMetadata that its startTime is not in chunkStartTimes */
  public void filterChunks(Map<Path, List<Long>> chunkStartTimes) {
    Map<Path, Integer> startTimeIdxes = new HashMap<>();
    chunkStartTimes.forEach((p, t) -> startTimeIdxes.put(p, 0));
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/writer/TsFileIOWriter.java`
#### Snippet
```java

  /** Remove such ChunkMetadata that its startTime is not in chunkStartTimes */
  public void filterChunks(Map<Path, List<Long>> chunkStartTimes) {
    Map<Path, Integer> startTimeIdxes = new HashMap<>();
    chunkStartTimes.forEach((p, t) -> startTimeIdxes.put(p, 0));
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends IChunkMetadata`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/writer/TsFileIOWriter.java`
#### Snippet
```java

  private void writeChunkMetadataToTempFile(
      List<IChunkMetadata> iChunkMetadataList, Path seriesPath, boolean isNewPath)
      throws IOException {
    // [DeviceId] measurementId datatype size chunkMetadataBuffer
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends ByteBuffer`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/decoder/BitmapDecoder.java`
#### Snippet
```java
   * @return List(Pair of ( length, bitmap index) )
   */
  public List<Pair<Integer, byte[]>> decodeAll(int target, List<ByteBuffer> pageList) {
    List<Pair<Integer, byte[]>> resultList = new ArrayList<>();
    for (ByteBuffer bufferPage : pageList) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends IChunkMetadata`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/writer/tsmiterator/TSMIterator.java`
#### Snippet
```java

  public static TimeseriesMetadata constructOneTimeseriesMetadata(
      String measurementId, List<IChunkMetadata> chunkMetadataList) throws IOException {
    // create TimeseriesMetaData
    PublicBAOS publicBAOS = new PublicBAOS();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends ChunkGroupMetadata`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/writer/tsmiterator/TSMIterator.java`
#### Snippet
```java

  public static List<Pair<Path, List<IChunkMetadata>>> sortChunkMetadata(
      List<ChunkGroupMetadata> chunkGroupMetadataList,
      String currentDevice,
      List<ChunkMetadata> chunkMetadataList) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends ChunkMetadata`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/writer/tsmiterator/TSMIterator.java`
#### Snippet
```java
      List<ChunkGroupMetadata> chunkGroupMetadataList,
      String currentDevice,
      List<ChunkMetadata> chunkMetadataList) {
    Map<String, Map<Path, List<IChunkMetadata>>> chunkMetadataMap = new TreeMap<>();
    List<Pair<Path, List<IChunkMetadata>>> sortedChunkMetadataList = new LinkedList<>();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends List`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/TsFileSequenceReader.java`
#### Snippet
```java
   */
  private boolean hasDataInPartition(
      Map<String, List<ChunkMetadata>> seriesMetadataMap, long start, long end) {
    for (List<ChunkMetadata> chunkMetadataList : seriesMetadataMap.values()) {
      for (ChunkMetadata chunkMetadata : chunkMetadataList) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Pair`>
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/TsFileSequenceReader.java`
#### Snippet
```java
      String deviceId,
      MetadataIndexNodeType type,
      Queue<Pair<String, Pair<Long, Long>>> queue)
      throws IOException {
    try {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super String`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/TsFileSequenceReader.java`
#### Snippet
```java
      String deviceId,
      MetadataIndexNodeType type,
      Map<String, List<TimeseriesMetadata>> timeseriesMetadataMap,
      boolean needChunkMetadata)
      throws IOException {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super TimeseriesMetadata`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/TsFileSequenceReader.java`
#### Snippet
```java
   */
  public void getDeviceTimeseriesMetadata(
      List<TimeseriesMetadata> timeseriesMetadataList,
      MetadataIndexNode measurementNode,
      Set<String> excludedMeasurementIds,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Pair`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/TsFileSequenceReader.java`
#### Snippet
```java

  private void collectEachLeafMeasurementNodeOffsetRange(
      ByteBuffer buffer, Queue<Pair<Long, Long>> queue) throws IOException {
    try {
      final MetadataIndexNode metadataIndexNode = MetadataIndexNode.deserializeFrom(buffer);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends IChunkMetadata`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/TsFileSequenceReader.java`
#### Snippet
```java

  /** Get measurement schema by chunkMetadatas. */
  public MeasurementSchema getMeasurementSchema(List<IChunkMetadata> chunkMetadataList)
      throws IOException {
    if (chunkMetadataList.isEmpty()) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Path`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/TsFileSequenceReader.java`
#### Snippet
```java
  @SuppressWarnings("squid:S3776") // Suppress high Cognitive Complexity warning
  public long selfCheck(
      Map<Path, IMeasurementSchema> newSchema,
      List<ChunkGroupMetadata> chunkGroupMetadataList,
      boolean fastFinish)
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super IMeasurementSchema`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/TsFileSequenceReader.java`
#### Snippet
```java
  @SuppressWarnings("squid:S3776") // Suppress high Cognitive Complexity warning
  public long selfCheck(
      Map<Path, IMeasurementSchema> newSchema,
      List<ChunkGroupMetadata> chunkGroupMetadataList,
      boolean fastFinish)
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super ChunkGroupMetadata`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/TsFileSequenceReader.java`
#### Snippet
```java
  public long selfCheck(
      Map<Path, IMeasurementSchema> newSchema,
      List<ChunkGroupMetadata> chunkGroupMetadataList,
      boolean fastFinish)
      throws IOException {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends TimeseriesMetadata`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/TsFileSequenceReader.java`
#### Snippet
```java

  protected int binarySearchInTimeseriesMetadataList(
      List<TimeseriesMetadata> timeseriesMetadataList, String key) {
    int low = 0;
    int high = timeseriesMetadataList.size() - 1;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super Pair`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/TsFileSequenceReader.java`
#### Snippet
```java
   */
  private void getDevicesOfLeafNode(
      MetadataIndexNode deviceLeafNode, Queue<Pair<String, long[]>> measurementNodeOffsetQueue) {
    if (!deviceLeafNode.getNodeType().equals(MetadataIndexNodeType.LEAF_DEVICE)) {
      throw new RuntimeException("the first param should be device leaf node.");
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Point`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/encoder/FreqEncoder.java`
#### Snippet
```java
   * @return Estimated number of bits
   */
  private double estimateIncreaseBits(ArrayList<Point> list, int m) {
    double bits = 0;
    double eps = Math.pow(2, beta);
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Point`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/encoder/FreqEncoder.java`
#### Snippet
```java
  }

  private byte[] encodeBlock(ArrayList<Point> list) {
    // Quantification
    int m = list.size();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super MemoryUsage`
in `metrics/interface/src/main/java/org/apache/iotdb/metrics/metricsets/jvm/JvmUtils.java`
#### Snippet
```java
public class JvmUtils {
  static double getUsageValue(
      MemoryPoolMXBean memoryPoolMXBean, ToLongFunction<MemoryUsage> getter) {
    MemoryUsage usage = getUsage(memoryPoolMXBean);
    if (usage == null) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Pair`
in `metrics/interface/src/main/java/org/apache/iotdb/metrics/reporter/InternalReporter.java`
#### Snippet
```java
   * @param gauges the map of autoGauge
   */
  public void addAutoGauge(Map<Pair<String, String[]>, AutoGauge> gauges) {
    autoGauges.putAll(gauges);
  }
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends AutoGauge`
in `metrics/interface/src/main/java/org/apache/iotdb/metrics/reporter/InternalReporter.java`
#### Snippet
```java
   * @param gauges the map of autoGauge
   */
  public void addAutoGauge(Map<Pair<String, String[]>, AutoGauge> gauges) {
    autoGauges.putAll(gauges);
  }
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super String`
in `integration-test/src/main/java/org/apache/iotdb/it/env/DataNodeWrapper.java`
#### Snippet
```java

  @Override
  protected void addStartCmdParams(List<String> params) {
    final String workDir = getNodePath();
    final String confDir = workDir + File.separator + "conf";
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends MemoryUsage`
in `metrics/interface/src/main/java/org/apache/iotdb/metrics/metricsets/jvm/JvmGcMetrics.java`
#### Snippet
```java

  private void countPoolSizeDelta(
      Map<String, MemoryUsage> before,
      Map<String, MemoryUsage> after,
      Counter counter,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends MemoryUsage`
in `metrics/interface/src/main/java/org/apache/iotdb/metrics/metricsets/jvm/JvmGcMetrics.java`
#### Snippet
```java
  private void countPoolSizeDelta(
      Map<String, MemoryUsage> before,
      Map<String, MemoryUsage> after,
      Counter counter,
      AtomicLong previousPoolSize,
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super String`
in `integration-test/src/main/java/org/apache/iotdb/it/env/ConfigNodeWrapper.java`
#### Snippet
```java

  @Override
  protected void addStartCmdParams(List<String> params) {
    final String workDir = getNodePath();
    final String confDir = workDir + File.separator + "conf";
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends ResultSetMetaData`
in `integration-test/src/main/java/org/apache/iotdb/itbase/runtime/ClusterResultSetMetaData.java`
#### Snippet
```java
  private final List<String> endpoints;

  public ClusterResultSetMetaData(List<ResultSetMetaData> metadataList, List<String> endpoints) {
    this.metaDataList = metadataList;
    this.endpoints = endpoints;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends FileStatus`
in `hadoop/src/main/java/org/apache/iotdb/hadoop/tsfile/TSFInputFormat.java`
#### Snippet
```java

  public static List<TSFInputSplit> getTSFInputSplit(
      Configuration configuration, List<FileStatus> listFileStatus, Logger logger)
      throws IOException {
    BlockLocation[] blockLocations;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Path`
in `flink-tsfile-connector/src/main/java/org/apache/iotdb/flink/tsfile/RowRowRecordParser.java`
#### Snippet
```java
   */
  public static RowRowRecordParser create(
      RowTypeInfo outputRowTypeInfo, List<Path> selectedSeries) {
    List<String> selectedSeriesNames =
        selectedSeries.stream().map(Path::toString).collect(Collectors.toList());
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super TSRecord`
in `flink-tsfile-connector/src/main/java/org/apache/iotdb/flink/tsfile/RowTSRecordConverter.java`
#### Snippet
```java

  @Override
  public void convert(Row input, Collector<TSRecord> collector) {
    long timestamp = (long) input.getField(timeIndex);
    for (TSRecord tsRecord : reuse) {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super QueryDataSet`
in `hadoop/src/main/java/org/apache/iotdb/hadoop/tsfile/TSFRecordReader.java`
#### Snippet
```java
      Configuration configuration,
      IReaderSet readerSet,
      List<QueryDataSet> dataSetList,
      List<String> deviceIdList)
      throws IOException {
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super String`
in `hadoop/src/main/java/org/apache/iotdb/hadoop/tsfile/TSFRecordReader.java`
#### Snippet
```java
      IReaderSet readerSet,
      List<QueryDataSet> dataSetList,
      List<String> deviceIdList)
      throws IOException {
    org.apache.hadoop.fs.Path path = split.getPath();
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Field`
in `hadoop/src/main/java/org/apache/iotdb/hadoop/tsfile/TSFRecordReader.java`
#### Snippet
```java
   */
  public static void readFieldsValue(
      MapWritable mapWritable, List<Field> fields, List<String> measurementIds)
      throws InterruptedException {
    int index = 0;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super PartialPath`
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/RSchemaRegion.java`
#### Snippet
```java

  private void getMatchedPathByNodeType(
      String[] nodes, Character[] nodetype, Collection<PartialPath> collection)
      throws IllegalPathException {
    List<String> allResult = Collections.synchronizedList(new ArrayList<>());
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? super T`
in `metrics/dropwizard-metrics/src/main/java/org/apache/iotdb/metrics/dropwizard/type/DropwizardAutoGauge.java`
#### Snippet
```java
  private final ToLongFunction<T> mapper;

  public DropwizardAutoGauge(T obj, ToLongFunction<T> mapper) {
    this.refObject = new WeakReference<>(obj);
    this.mapper = mapper;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends RatisConfig.RatisConsensus`
in `consensus/src/main/java/org/apache/iotdb/consensus/ratis/RatisClient.java`
#### Snippet
```java
        RaftProperties raftProperties,
        RaftClientRpc clientRpc,
        Supplier<RatisConfig.RatisConsensus> config) {
      super(clientManager, clientPoolProperty);
      this.raftProperties = raftProperties;
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends Peer`
in `consensus/src/main/java/org/apache/iotdb/consensus/ratis/Utils.java`
#### Snippet
```java
  }

  public static List<RaftPeer> fromPeersAndPriorityToRaftPeers(List<Peer> peers, int priority) {
    return peers.stream()
        .map(peer -> Utils.fromPeerAndPriorityToRaftPeer(peer, priority))
```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends RaftClientReply`
in `consensus/src/main/java/org/apache/iotdb/consensus/ratis/RatisConsensus.java`
#### Snippet
```java
  }
  /** launch a consensus write with retry mechanism */
  private RaftClientReply writeWithRetry(CheckedSupplier<RaftClientReply, IOException> caller)
      throws IOException {

```

### RuleId[ruleID=BoundedWildcard]
Can generalize to `? extends IOException`
in `consensus/src/main/java/org/apache/iotdb/consensus/ratis/RatisConsensus.java`
#### Snippet
```java
  }
  /** launch a consensus write with retry mechanism */
  private RaftClientReply writeWithRetry(CheckedSupplier<RaftClientReply, IOException> caller)
      throws IOException {

```

## RuleId[ruleID=WhileLoopSpinsOnField]
### RuleId[ruleID=WhileLoopSpinsOnField]
`while` loop spins on field
in `cli/src/main/java/org/apache/iotdb/cli/AbstractCli.java`
#### Snippet
```java
          long costTime = System.currentTimeMillis() - startTime;
          println(String.format("It costs %.3fs", costTime / 1000.0));
          while (!isReachEnd) {
            if (continuePrint) {
              maxSizeList = new ArrayList<>(columnLength);
```

### RuleId[ruleID=WhileLoopSpinsOnField]
`while` loop spins on field
in `example/flink/src/main/java/org/apache/iotdb/flink/FlinkIoTDBSink.java`
#### Snippet
```java
    @Override
    public void run(SourceContext context) throws Exception {
      while (running) {
        Map<String, String> tuple = new HashMap();
        tuple.put("device", "root.sg.d1");
```

## RuleId[ruleID=ThrowFromFinallyBlock]
### RuleId[ruleID=ThrowFromFinallyBlock]
`throw` inside 'finally' block
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/cross/rewrite/RewriteCrossSpaceCompactionSelector.java`
#### Snippet
```java
        compactionEstimator.clear();
      } catch (IOException e) {
        throw new MergeException(e);
      }
    }
```

## RuleId[ruleID=IfStatementWithIdenticalBranches]
### RuleId[ruleID=IfStatementWithIdenticalBranches]
Common part can be extracted from 'if'
in `server/src/main/java/org/apache/iotdb/db/qp/strategy/optimizer/MergeSingleFilterOptimizer.java`
#### Snippet
```java
        // not more same, add existing nodes in tempExtrNode into a new node
        // prevent make a node which has only one child.
        if (tempExtrNode.size() == 1) {
          ret.add(tempExtrNode.get(0));
          // use exist Object directly for efficiency
```

### RuleId[ruleID=IfStatementWithIdenticalBranches]
Common part can be extracted from 'if'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/rewriter/MergeSingleFilterOptimizer.java`
#### Snippet
```java
        // not more same, add existing nodes in tempExtrNode into a new node
        // prevent make a node which has only one child.
        if (tempExtrNode.size() == 1) {
          ret.add(tempExtrNode.get(0));
          // use exist Object directly for efficiency
```

### RuleId[ruleID=IfStatementWithIdenticalBranches]
Common part can be extracted removing branch
in `server/src/main/java/org/apache/iotdb/db/query/reader/universal/CachedPriorityMergeReader.java`
#### Snippet
```java
  @Override
  public TimeValuePair currentTimeValuePair() throws IOException {
    if (0 <= cacheIdx && cacheIdx < cacheLimit) {
      return timeValuePairCache[cacheIdx];
    } else {
```

### RuleId[ruleID=IfStatementWithIdenticalBranches]
Common part can be extracted removing branch
in `server/src/main/java/org/apache/iotdb/db/query/reader/universal/CachedPriorityMergeReader.java`
#### Snippet
```java
  public TimeValuePair nextTimeValuePair() throws IOException {
    TimeValuePair ret;
    if (cacheIdx < cacheLimit) {
      ret = timeValuePairCache[cacheIdx++];
    } else {
```

### RuleId[ruleID=IfStatementWithIdenticalBranches]
Common part can be extracted from 'if'
in `server/src/main/java/org/apache/iotdb/db/query/reader/series/SeriesReader.java`
#### Snippet
```java
    this.valueFilter = valueFilter;
    this.fileFilter = fileFilter;
    if (ascending) {
      this.orderUtils = new AscTimeOrderUtils();
      mergeReader = getPriorityMergeReader();
```

### RuleId[ruleID=IfStatementWithIdenticalBranches]
Common part can be extracted from 'if'
in `server/src/main/java/org/apache/iotdb/db/query/reader/series/SeriesReader.java`
#### Snippet
```java
    this.valueFilter = valueFilter;
    this.fileFilter = null;
    if (ascending) {
      this.orderUtils = new AscTimeOrderUtils();
      mergeReader = getPriorityMergeReader();
```

### RuleId[ruleID=IfStatementWithIdenticalBranches]
Common part can be extracted from 'if'
in `server/src/main/java/org/apache/iotdb/db/engine/modification/utils/TracedBufferedReader.java`
#### Snippet
```java
        dst = 0;
      } else {
        if (readAheadLimit <= cb.length) {
          /* Shuffle in the current buffer */
          System.arraycopy(cb, markedChar, cb, 0, delta);
```

### RuleId[ruleID=IfStatementWithIdenticalBranches]
Common part can be extracted from 'if'
in `spark-tsfile/src/main/java/org/apache/iotdb/spark/tsfile/qp/optimizer/MergeSingleFilterOptimizer.java`
#### Snippet
```java
        // not more same, add exist nodes in tempExtrNode into a new node
        // prevent make a node which has only one child.
        if (tempExtrNode.size() == 1) {
          ret.add(tempExtrNode.get(0));
          // use exist Object directly for efficiency
```

## RuleId[ruleID=UnnecessarySuperQualifier]
### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/service/UDFClassLoader.java`
#### Snippet
```java
    URL[] urls = FileUtils.toURLs(fileSet.toArray(new File[0]));
    for (URL url : urls) {
      super.addURL(url);
    }
  }
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `node-commons/src/main/java/org/apache/iotdb/commons/auth/authorizer/OpenIdAuthorizer.java`
#### Snippet
```java
    // Create User if not exists
    String iotdbUsername = getUsername(claims);
    if (!super.listAllUsers().contains(iotdbUsername)) {
      logger.info("User {} logs in for first time, storing it locally!", iotdbUsername);
      // We give the user a random password so that no one could hijack them via local login
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `node-commons/src/main/java/org/apache/iotdb/commons/path/AlignedPath.java`
#### Snippet
```java
  public void serialize(OutputStream stream) throws IOException {
    PathType.Aligned.serialize(stream);
    super.serializeWithoutType(stream);
    ReadWriteIOUtils.write(measurementList.size(), stream);
    for (String measurement : measurementList) {
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `node-commons/src/main/java/org/apache/iotdb/commons/path/AlignedPath.java`
#### Snippet
```java
  public void serialize(ByteBuffer byteBuffer) {
    PathType.Aligned.serialize(byteBuffer);
    super.serializeWithoutType(byteBuffer);
    ReadWriteIOUtils.write(measurementList.size(), byteBuffer);
    for (String measurement : measurementList) {
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `node-commons/src/main/java/org/apache/iotdb/commons/path/MeasurementPath.java`
#### Snippet
```java
  public void serialize(ByteBuffer byteBuffer) {
    PathType.Measurement.serialize(byteBuffer);
    super.serializeWithoutType(byteBuffer);
    if (measurementSchema == null) {
      ReadWriteIOUtils.write((byte) 0, byteBuffer);
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `node-commons/src/main/java/org/apache/iotdb/commons/path/MeasurementPath.java`
#### Snippet
```java
  public void serialize(OutputStream stream) throws IOException {
    PathType.Measurement.serialize(stream);
    super.serializeWithoutType(stream);
    if (measurementSchema == null) {
      ReadWriteIOUtils.write((byte) 0, stream);
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
                Instant.ofEpochMilli(time), ZoneId.of(cal.getTimeZone().getID()));
      } else {
        zonedDateTime = ZonedDateTime.ofInstant(Instant.ofEpochMilli(time), super.zoneId);
      }
      this.parameters.put(
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
  @Override
  public boolean execute() throws SQLException {
    return super.execute(createCompleteSql(sql, parameters));
  }

```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
  @Override
  public int executeUpdate() throws SQLException {
    return super.executeUpdate(createCompleteSql(sql, parameters));
  }

```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
              Instant.ofEpochMilli(x.getTime()), ZoneId.of(cal.getTimeZone().getID()));
    } else {
      zonedDateTime = ZonedDateTime.ofInstant(Instant.ofEpochMilli(x.getTime()), super.zoneId);
    }
    this.parameters.put(
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
  @Override
  public ResultSet executeQuery() throws SQLException {
    return super.executeQuery(createCompleteSql(sql, parameters));
  }

```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
  public void setTimestamp(int parameterIndex, Timestamp x) {
    ZonedDateTime zonedDateTime =
        ZonedDateTime.ofInstant(Instant.ofEpochMilli(x.getTime()), super.zoneId);
    this.parameters.put(
        parameterIndex, zonedDateTime.format(DateTimeFormatter.ISO_LOCAL_DATE_TIME));
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `server/src/main/java/org/apache/iotdb/db/qp/logical/crud/LastQueryOperator.java`
#### Snippet
```java
  public PhysicalPlan generatePhysicalPlan(PhysicalGenerator generator)
      throws QueryProcessException {
    return super.generateRawDataQueryPlan(generator, new LastQueryPlan());
  }
}
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `server/src/main/java/org/apache/iotdb/db/qp/logical/crud/UDTFQueryOperator.java`
#### Snippet
```java
  public PhysicalPlan generatePhysicalPlan(PhysicalGenerator generator)
      throws QueryProcessException {
    return super.generateRawDataQueryPlan(generator, initUDTFPlan());
  }

```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `server/src/main/java/org/apache/iotdb/db/qp/logical/crud/GroupByQueryOperator.java`
#### Snippet
```java
        isAlignByDevice()
            ? this.generateAlignByDevicePlan(generator)
            : super.generateRawDataQueryPlan(generator, initGroupByTimePlan(new GroupByTimePlan()));

    AggregationPlan aggregationPlan =
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `server/src/main/java/org/apache/iotdb/db/qp/logical/crud/AggregationQueryOperator.java`
#### Snippet
```java
        isAlignByDevice()
            ? this.generateAlignByDevicePlan(generator)
            : super.generateRawDataQueryPlan(generator, initAggregationPlan(new AggregationPlan()));

    AggregationPlan aggregationPlan =
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `server/src/main/java/org/apache/iotdb/db/qp/logical/crud/UDAFQueryOperator.java`
#### Snippet
```java
    if (!isAlignByDevice()) {
      physicalPlan =
          super.generateRawDataQueryPlan(generator, new UDAFPlan(selectComponent.getZoneId()));
      UDAFPlan udafPlan = (UDAFPlan) physicalPlan;
      udafPlan.setInnerAggregationPlan(innerAggregationPlan);
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `server/src/main/java/org/apache/iotdb/db/qp/logical/crud/GroupByFillQueryOperator.java`
#### Snippet
```java
      throws QueryProcessException {
    GroupByTimeFillPlan groupByTimeFillPlan =
        (GroupByTimeFillPlan) super.initGroupByTimePlan(queryPlan);
    GroupByFillClauseComponent groupByFillClauseComponent =
        (GroupByFillClauseComponent) specialClauseComponent;
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `server/src/main/java/org/apache/iotdb/db/qp/logical/crud/GroupByFillQueryOperator.java`
#### Snippet
```java
        isAlignByDevice()
            ? this.generateAlignByDevicePlan(generator)
            : super.generateRawDataQueryPlan(
                generator, initGroupByTimeFillPlan(new GroupByTimeFillPlan()));

```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `server/src/main/java/org/apache/iotdb/db/qp/logical/crud/FillQueryOperator.java`
#### Snippet
```java
    return isAlignByDevice()
        ? this.generateAlignByDevicePlan(generator)
        : super.generateRawDataQueryPlan(generator, initFillQueryPlan());
  }

```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `server/src/main/java/org/apache/iotdb/db/qp/physical/sys/DataAuthPlan.java`
#### Snippet
```java
  @Override
  public void serialize(DataOutputStream stream) throws IOException {
    int type = this.getPlanType(super.getOperatorType());
    stream.writeByte((byte) type);
    stream.writeInt(users.size());
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `server/src/main/java/org/apache/iotdb/db/qp/physical/sys/DataAuthPlan.java`
#### Snippet
```java
  @Override
  public void serializeImpl(ByteBuffer buffer) {
    int type = this.getPlanType(super.getOperatorType());
    buffer.put((byte) type);
    buffer.putInt(users.size());
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `server/src/main/java/org/apache/iotdb/db/qp/physical/sys/AuthorPlan.java`
#### Snippet
```java
  @Override
  public void serializeImpl(ByteBuffer buffer) {
    int type = this.getPlanType(super.getOperatorType());
    buffer.put((byte) type);
    buffer.putInt(authorType.ordinal());
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `server/src/main/java/org/apache/iotdb/db/qp/physical/sys/AuthorPlan.java`
#### Snippet
```java
  @Override
  public void serialize(DataOutputStream stream) throws IOException {
    int type = this.getPlanType(super.getOperatorType());
    stream.writeByte((byte) type);
    stream.writeInt(authorType.ordinal());
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `server/src/main/java/org/apache/iotdb/db/mpp/common/filter/BasicFunctionFilter.java`
#### Snippet
```java
  public void serialize(ByteBuffer byteBuffer) {
    FilterTypes.BasicFunction.serialize(byteBuffer);
    super.serializeWithoutType(byteBuffer);
    ReadWriteIOUtils.write(value, byteBuffer);
    ReadWriteIOUtils.write(funcToken.ordinal(), byteBuffer);
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `server/src/main/java/org/apache/iotdb/db/mpp/common/filter/RegexpFilter.java`
#### Snippet
```java
  public void serialize(ByteBuffer byteBuffer) {
    FilterTypes.Regexp.serialize(byteBuffer);
    super.serializeWithoutType(byteBuffer);
    ReadWriteIOUtils.write(value, byteBuffer);
  }
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `server/src/main/java/org/apache/iotdb/db/mpp/common/filter/FunctionFilter.java`
#### Snippet
```java
  public void serialize(ByteBuffer byteBuffer) {
    FilterTypes.Function.serialize(byteBuffer);
    super.serializeWithoutType(byteBuffer);
  }

```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `server/src/main/java/org/apache/iotdb/db/mpp/common/filter/LikeFilter.java`
#### Snippet
```java
  public void serialize(ByteBuffer byteBuffer) {
    FilterTypes.Like.serialize(byteBuffer);
    super.serializeWithoutType(byteBuffer);
    ReadWriteIOUtils.write(value, byteBuffer);
  }
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `server/src/main/java/org/apache/iotdb/db/mpp/common/filter/InFilter.java`
#### Snippet
```java
  public void serialize(ByteBuffer byteBuffer) {
    FilterTypes.In.serialize(byteBuffer);
    super.serializeWithoutType(byteBuffer);
    ReadWriteIOUtils.write(not, byteBuffer);
    ReadWriteIOUtils.write(values.size(), byteBuffer);
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `server/src/main/java/org/apache/iotdb/db/wal/recover/file/SealedTsFileRecoverPerformer.java`
#### Snippet
```java
   */
  public void recover() throws DataRegionException, IOException {
    super.recoverWithWriter();

    if (hasCrashed()) {
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `server/src/main/java/org/apache/iotdb/db/wal/recover/file/UnsealedTsFileRecoverPerformer.java`
#### Snippet
```java
   */
  public void startRecovery() throws DataRegionException, IOException {
    super.recoverWithWriter();

    if (hasCrashed()) {
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `server/src/main/java/org/apache/iotdb/db/query/reader/universal/DescPriorityMergeReader.java`
#### Snippet
```java
    if (reader.hasNextTimeValuePair()) {
      heap.add(new Element(reader, reader.nextTimeValuePair(), priority));
      super.currentReadStopTime = Math.min(currentReadStopTime, endTime);
    } else {
      reader.close();
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `server/src/main/java/org/apache/iotdb/db/query/reader/universal/DescPriorityMergeReader.java`
#### Snippet
```java

  public DescPriorityMergeReader() {
    super.heap =
        new PriorityQueue<>(
            (o1, o2) -> {
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `server/src/main/java/org/apache/iotdb/db/query/dataset/SingleDataSet.java`
#### Snippet
```java
    // TODO I don't know whether it's right or not in AlignedPath, remember to check here while
    // adapting SingleDataSet query for new vector
    super.columnNum = dataTypes.size();
  }

```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `server/src/main/java/org/apache/iotdb/db/query/dataset/AlignByDeviceDataSet.java`
#### Snippet
```java
    // TODO I don't know whether it's right or not in AlignedPath, remember to check here while
    // adapting AlignByDevice query for new vector
    super.columnNum = alignByDevicePlan.getMeasurements().size() + 1; // + 1 for 'device'
    this.measurements = alignByDevicePlan.getMeasurements();
    this.paths = alignByDevicePlan.getDeduplicatePaths();
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `server/src/main/java/org/apache/iotdb/db/query/dataset/NonAlignEngineDataSet.java`
#### Snippet
```java
  private void init(WatermarkEncoder encoder, int fetchSize) {
    QueryTimeManager.checkQueryAlive(queryId);
    initLimit(super.rowOffset, super.rowLimit, seriesReaderWithoutValueFilterList.size());
    this.fetchSize = fetchSize;
    for (int i = 0; i < seriesReaderWithoutValueFilterList.size(); i++) {
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `server/src/main/java/org/apache/iotdb/db/query/dataset/NonAlignEngineDataSet.java`
#### Snippet
```java
  private void init(WatermarkEncoder encoder, int fetchSize) {
    QueryTimeManager.checkQueryAlive(queryId);
    initLimit(super.rowOffset, super.rowLimit, seriesReaderWithoutValueFilterList.size());
    this.fetchSize = fetchSize;
    for (int i = 0; i < seriesReaderWithoutValueFilterList.size(); i++) {
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `server/src/main/java/org/apache/iotdb/db/query/dataset/UDFRawQueryInputDataSetWithoutValueFilter.java`
#### Snippet
```java
          && cachedBatchDataArray[seriesIndex].currentTime() == minTime) {
        rowInObjects[seriesIndex] = cachedBatchDataArray[seriesIndex].currentValue();
        super.cacheNext(seriesIndex);
      }
    }
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `server/src/main/java/org/apache/iotdb/db/query/dataset/groupby/GroupByLevelDataSet.java`
#### Snippet
```java
    // TODO I don't know whether it's right or not in AlignedPath, remember to check here while
    // adapting GroupByLevel query for new vector
    super.columnNum = dataTypes.size();
  }

```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `server/src/main/java/org/apache/iotdb/db/tools/upgrade/TsFileOnlineUpgradeTool.java`
#### Snippet
```java
        || StorageEngineV2.getTimePartition(pageHeader.getStartTime())
            != StorageEngineV2.getTimePartition(pageHeader.getEndTime())
        || super.checkIfNeedToDecode(schema, deviceId, pageHeader, chunkHeaderOffset);
  }

```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `server/src/main/java/org/apache/iotdb/db/trigger/service/TriggerClassLoader.java`
#### Snippet
```java
    URL[] urls = FileUtils.toURLs(fileSet.toArray(new File[0]));
    for (URL url : urls) {
      super.addURL(url);
    }
  }
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `server/src/main/java/org/apache/iotdb/db/metadata/mnode/MeasurementMNode.java`
#### Snippet
```java
  @Override
  public MeasurementPath getMeasurementPath() {
    MeasurementPath result = new MeasurementPath(super.getPartialPath(), schema);
    result.setUnderAlignedEntity(getParent().isAligned());
    return result;
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `confignode/src/main/java/org/apache/iotdb/confignode/service/thrift/ConfigNodeRPCService.java`
#### Snippet
```java
        (ConfigNodeRPCServiceProcessor) configNodeRPCServerProcessor;

    super.mbeanName =
        String.format(
            "%s:%s=%s", this.getClass().getPackage(), IoTDBConstant.JMX_TYPE, getID().getJmxName());
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `spark-tsfile/src/main/java/org/apache/iotdb/spark/tsfile/qp/common/FilterOperator.java`
#### Snippet
```java

  public void setTokenIntType(int intType) {
    super.tokenIntType = intType;
    super.tokenSymbol = SQLConstant.tokenSymbol.get(tokenIntType);
  }
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `spark-tsfile/src/main/java/org/apache/iotdb/spark/tsfile/qp/common/FilterOperator.java`
#### Snippet
```java
  public void setTokenIntType(int intType) {
    super.tokenIntType = intType;
    super.tokenSymbol = SQLConstant.tokenSymbol.get(tokenIntType);
  }

```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `hive-connector/src/main/java/org/apache/iotdb/hive/TSFHiveInputFormat.java`
#### Snippet
```java
  public InputSplit[] getSplits(JobConf job, int numSplits) throws IOException {
    job.setBoolean(INPUT_DIR_RECURSIVE, true);
    return TSFInputFormat.getTSFInputSplit(job, Arrays.asList(super.listStatus(job)), logger)
        .toArray(new InputSplit[0]);
  }
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/mnode/RMeasurementMNode.java`
#### Snippet
```java
  @Override
  public MeasurementPath getMeasurementPath() {
    MeasurementPath result = new MeasurementPath(super.getPartialPath(), schema);
    result.setUnderAlignedEntity(getParent().isAligned());
    if (alias != null && !alias.isEmpty()) {
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/query/timegenerator/TsFileTimeGenerator.java`
#### Snippet
```java
    this.metadataQuerier = metadataQuerier;

    super.constructNode(iexpression);
  }

```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/DescReadBatchData.java`
#### Snippet
```java
  @Override
  public boolean hasCurrent() {
    return super.readCurListIndex >= 0 && super.readCurArrayIndex >= 0;
  }

```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/DescReadBatchData.java`
#### Snippet
```java
  @Override
  public boolean hasCurrent() {
    return super.readCurListIndex >= 0 && super.readCurArrayIndex >= 0;
  }

```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/DescReadBatchData.java`
#### Snippet
```java
  @Override
  public BatchData flip() {
    super.readCurArrayIndex = writeCurArrayIndex - 1;
    super.readCurListIndex = writeCurListIndex;
    return this;
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/DescReadBatchData.java`
#### Snippet
```java
  public BatchData flip() {
    super.readCurArrayIndex = writeCurArrayIndex - 1;
    super.readCurListIndex = writeCurListIndex;
    return this;
  }
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/DescReadBatchData.java`
#### Snippet
```java
  @Override
  public void next() {
    super.readCurArrayIndex--;
    if (super.readCurArrayIndex == -1) {
      super.readCurArrayIndex = capacity - 1;
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/DescReadBatchData.java`
#### Snippet
```java
  public void next() {
    super.readCurArrayIndex--;
    if (super.readCurArrayIndex == -1) {
      super.readCurArrayIndex = capacity - 1;
      super.readCurListIndex--;
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/DescReadBatchData.java`
#### Snippet
```java
    super.readCurArrayIndex--;
    if (super.readCurArrayIndex == -1) {
      super.readCurArrayIndex = capacity - 1;
      super.readCurListIndex--;
    }
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/DescReadBatchData.java`
#### Snippet
```java
    if (super.readCurArrayIndex == -1) {
      super.readCurArrayIndex = capacity - 1;
      super.readCurListIndex--;
    }
  }
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/DescReadBatchData.java`
#### Snippet
```java
  @Override
  public void resetBatchData() {
    super.readCurArrayIndex = writeCurArrayIndex - 1;
    super.readCurListIndex = writeCurListIndex;
  }
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/DescReadBatchData.java`
#### Snippet
```java
  public void resetBatchData() {
    super.readCurArrayIndex = writeCurArrayIndex - 1;
    super.readCurListIndex = writeCurListIndex;
  }

```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/DescReadWriteBatchData.java`
#### Snippet
```java
  @Override
  public BatchData flip() {
    super.readCurArrayIndex = capacity - 1;
    super.readCurListIndex = writeCurListIndex;
    return this;
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/DescReadWriteBatchData.java`
#### Snippet
```java
  public BatchData flip() {
    super.readCurArrayIndex = capacity - 1;
    super.readCurListIndex = writeCurListIndex;
    return this;
  }
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/DescReadWriteBatchData.java`
#### Snippet
```java
  @Override
  public void next() {
    super.readCurArrayIndex--;
    if ((readCurListIndex == 0 && readCurArrayIndex <= writeCurArrayIndex)
        || readCurArrayIndex == -1) {
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/DescReadWriteBatchData.java`
#### Snippet
```java
    if ((readCurListIndex == 0 && readCurArrayIndex <= writeCurArrayIndex)
        || readCurArrayIndex == -1) {
      super.readCurListIndex--;
      super.readCurArrayIndex = capacity - 1;
    }
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/DescReadWriteBatchData.java`
#### Snippet
```java
        || readCurArrayIndex == -1) {
      super.readCurListIndex--;
      super.readCurArrayIndex = capacity - 1;
    }
  }
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/DescReadWriteBatchData.java`
#### Snippet
```java
  @Override
  public void resetBatchData() {
    super.readCurArrayIndex = capacity - 1;
    super.readCurListIndex = writeCurListIndex;
  }
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/DescReadWriteBatchData.java`
#### Snippet
```java
  public void resetBatchData() {
    super.readCurArrayIndex = capacity - 1;
    super.readCurListIndex = writeCurListIndex;
  }

```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/BatchData.java`
#### Snippet
```java
          break;
        }
        super.next();
      }
      return BatchData.this.hasCurrent();
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/BatchData.java`
#### Snippet
```java
    public boolean hasNext() {
      while (BatchData.this.hasCurrent() && currentValue() == null) {
        super.next();
      }
      return BatchData.this.hasCurrent();
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/RamUsageEstimator.java`
#### Snippet
```java
    @Override
    public String toString() {
      return super.name() + " (" + description + ")";
    }
  }
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `integration-test/src/main/java/org/apache/iotdb/it/env/Cluster1Env.java`
#### Snippet
```java
  public void initBeforeClass() throws InterruptedException {
    logger.debug("=======start init class=======");
    super.initEnvironment(1, 3);
  }

```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `integration-test/src/main/java/org/apache/iotdb/it/env/Cluster1Env.java`
#### Snippet
```java
  public void initBeforeTest() throws InterruptedException {
    logger.debug("=======start init test=======");
    super.initEnvironment(1, 3);
  }
}
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `integration-test/src/main/java/org/apache/iotdb/it/env/Cluster1Env.java`
#### Snippet
```java
  public void initClusterEnvironment(int configNodesNum, int dataNodesNum) {
    logger.debug("=======start init cluster environment=======");
    super.initEnvironment(configNodesNum, dataNodesNum);
  }

```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `integration-test/src/main/java/org/apache/iotdb/it/env/DataNodeWrapper.java`
#### Snippet
```java
  @Override
  protected void updateConfig(Properties properties) {
    properties.setProperty(IoTDBConstant.DN_RPC_ADDRESS, super.getIp());
    properties.setProperty(IoTDBConstant.DN_RPC_PORT, String.valueOf(super.getPort()));
    properties.setProperty(IoTDBConstant.DN_INTERNAL_ADDRESS, this.internal_address);
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `integration-test/src/main/java/org/apache/iotdb/it/env/DataNodeWrapper.java`
#### Snippet
```java
  protected void updateConfig(Properties properties) {
    properties.setProperty(IoTDBConstant.DN_RPC_ADDRESS, super.getIp());
    properties.setProperty(IoTDBConstant.DN_RPC_PORT, String.valueOf(super.getPort()));
    properties.setProperty(IoTDBConstant.DN_INTERNAL_ADDRESS, this.internal_address);
    properties.setProperty(IoTDBConstant.DN_INTERNAL_PORT, String.valueOf(this.internalPort));
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `integration-test/src/main/java/org/apache/iotdb/it/env/DataNodeWrapper.java`
#### Snippet
```java
        "dn_schema_region_consensus_port", String.valueOf(this.schemaRegionConsensusPort));
    properties.setProperty("dn_join_cluster_retry_interval_ms", "1000");
    properties.setProperty("mqtt_host", super.getIp());
    properties.setProperty("mqtt_port", String.valueOf(this.mqttPort));
    properties.setProperty("connection_timeout_ms", "30000");
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `integration-test/src/main/java/org/apache/iotdb/it/env/DataNodeWrapper.java`
#### Snippet
```java
    super(testClassName, testMethodName, portList);
    this.targetConfigNode = targetConfigNode;
    this.internal_address = super.getIp();
    this.mppDataExchangePort = portList[1];
    this.internalPort = portList[2];
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `integration-test/src/main/java/org/apache/iotdb/it/env/SimpleEnv.java`
#### Snippet
```java
  public void initBeforeClass() throws InterruptedException {
    logger.debug("=======start init class=======");
    super.initEnvironment(1, 1);
  }

```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `integration-test/src/main/java/org/apache/iotdb/it/env/ConfigNodeWrapper.java`
#### Snippet
```java
  @Override
  protected void updateConfig(Properties properties) {
    properties.setProperty(IoTDBConstant.CN_INTERNAL_ADDRESS, super.getIp());
    properties.setProperty(IoTDBConstant.CN_INTERNAL_PORT, String.valueOf(getPort()));
    properties.setProperty(IoTDBConstant.CN_CONSENSUS_PORT, String.valueOf(this.consensusPort));
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `hadoop/src/main/java/org/apache/iotdb/hadoop/tsfile/TSFInputFormat.java`
#### Snippet
```java
  public List<InputSplit> getSplits(JobContext job) throws IOException {
    job.getConfiguration().setBoolean(INPUT_DIR_RECURSIVE, true);
    List<FileStatus> listFileStatus = super.listStatus(job);
    return new ArrayList<>(getTSFInputSplit(job.getConfiguration(), listFileStatus, logger));
  }
```

### RuleId[ruleID=UnnecessarySuperQualifier]
Qualifier `super` is unnecessary in this context
in `consensus/src/main/java/org/apache/iotdb/consensus/multileader/service/MultiLeaderRPCService.java`
#### Snippet
```java
    this.multiLeaderRPCServiceProcessor =
        (MultiLeaderRPCServiceProcessor) multiLeaderRPCServiceProcessor;
    super.mbeanName =
        String.format(
            "%s:%s=%s", this.getClass().getPackage(), IoTDBConstant.JMX_TYPE, getID().getJmxName());
```

## RuleId[ruleID=InfiniteLoopStatement]
### RuleId[ruleID=InfiniteLoopStatement]
`do` statement cannot complete without throwing an exception
in `example/pulsar/src/main/java/org/apache/iotdb/pulsar/PulsarConsumerThread.java`
#### Snippet
```java
  public void run() {
    try {
      do {
        Messages<?> messages = consumer.batchReceive();
        List<String> datas = new ArrayList<>(messages.size());
```

### RuleId[ruleID=InfiniteLoopStatement]
`do` statement cannot complete without throwing an exception
in `example/kafka/src/main/java/org/apache/iotdb/kafka/ConsumerThread.java`
#### Snippet
```java
  public void run() {
    try {
      do {
        ConsumerRecords<String, String> records = consumer.poll(Duration.ofSeconds(1));
        List<String> datas = new ArrayList<>(records.count());
```

### RuleId[ruleID=InfiniteLoopStatement]
`while` statement cannot complete without throwing an exception
in `server/src/main/java/org/apache/iotdb/db/sync/pipedata/queue/BufferedPipeDataQueue.java`
#### Snippet
```java
    List<PipeData> pipeData = new ArrayList<>();
    try (DataInputStream inputStream = new DataInputStream(new FileInputStream(file))) {
      while (true) {
        pipeData.add(PipeData.createPipeData(inputStream));
      }
```

## RuleId[ruleID=CodeBlock2Expr]
### RuleId[ruleID=CodeBlock2Expr]
Statement lambda can be replaced with expression lambda
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/SubPlan.java`
#### Snippet
```java
    result.add(this.planFragment);
    this.children.forEach(
        child -> {
          result.addAll(child.getPlanFragmentList());
        });
```

### RuleId[ruleID=CodeBlock2Expr]
Statement lambda can be replaced with expression lambda
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/LogicalPlanBuilder.java`
#### Snippet
```java

    ColumnHeaderConstant.selectIntoColumnHeaders.forEach(
        columnHeader -> {
          updateTypeProviderWithConstantType(
              columnHeader.getColumnName(), columnHeader.getColumnType());
```

### RuleId[ruleID=CodeBlock2Expr]
Statement lambda can be replaced with expression lambda
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/LogicalPlanBuilder.java`
#### Snippet
```java

    ColumnHeaderConstant.selectIntoAlignByDeviceColumnHeaders.forEach(
        columnHeader -> {
          updateTypeProviderWithConstantType(
              columnHeader.getColumnName(), columnHeader.getColumnType());
```

### RuleId[ruleID=CodeBlock2Expr]
Statement lambda can be replaced with expression lambda
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/distribution/ExchangeNodeAdder.java`
#### Snippet
```java
    node.getChildren()
        .forEach(
            child -> {
              visitedChildren.add(visit(child, context));
            });
```

### RuleId[ruleID=CodeBlock2Expr]
Statement lambda can be replaced with expression lambda
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/distribution/ExchangeNodeAdder.java`
#### Snippet
```java
    node.getChildren()
        .forEach(
            child -> {
              visit(child, context);
            });
```

### RuleId[ruleID=CodeBlock2Expr]
Statement lambda can be replaced with expression lambda
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/distribution/SourceRewriter.java`
#### Snippet
```java
        .getSchemaPartitionMap()
        .forEach(
            (storageGroup, deviceGroup) -> {
              deviceGroup.forEach(
                  (deviceGroupId, schemaRegionReplicaSet) ->
```

### RuleId[ruleID=CodeBlock2Expr]
Statement lambda can be replaced with expression lambda
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/distribution/SourceRewriter.java`
#### Snippet
```java
          .getSchemaPartitionMap()
          .forEach(
              (storageGroup, deviceGroup) -> {
                deviceGroup.forEach(
                    (deviceGroupId, schemaRegionReplicaSet) ->
```

### RuleId[ruleID=CodeBlock2Expr]
Statement lambda can be replaced with expression lambda
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/distribution/SourceRewriter.java`
#### Snippet
```java
          .getSchemaPartitionMap()
          .forEach(
              (storageGroup, deviceGroup) -> {
                deviceGroup.forEach(
                    (deviceGroupId, schemaRegionReplicaSet) ->
```

### RuleId[ruleID=CodeBlock2Expr]
Statement lambda can be replaced with expression lambda
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/distribution/SourceRewriter.java`
#### Snippet
```java
                  n.getAggregationDescriptorList()
                      .forEach(
                          v -> {
                            childDescriptors.add(
                                new AggregationDescriptor(
```

### RuleId[ruleID=CodeBlock2Expr]
Statement lambda can be replaced with expression lambda
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/distribution/SourceRewriter.java`
#### Snippet
```java
          .getAggregationDescriptorList()
          .forEach(
              descriptor -> {
                rootAggDescriptorList.add(
                    new AggregationDescriptor(
```

### RuleId[ruleID=CodeBlock2Expr]
Statement lambda can be replaced with expression lambda
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/distribution/SourceRewriter.java`
#### Snippet
```java
    node.getAggregationDescriptorList()
        .forEach(
            descriptor -> {
              leafAggDescriptorList.add(
                  new AggregationDescriptor(
```

### RuleId[ruleID=CodeBlock2Expr]
Statement lambda can be replaced with expression lambda
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/distribution/SourceRewriter.java`
#### Snippet
```java
    node.getAggregationDescriptorList()
        .forEach(
            descriptor -> {
              rootAggDescriptorList.add(
                  new AggregationDescriptor(
```

### RuleId[ruleID=CodeBlock2Expr]
Statement lambda can be replaced with expression lambda
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/fragment/FragmentInstanceStateMachine.java`
#### Snippet
```java
    }
    executor.execute(
        () -> {
          failures.forEach(listener::onTaskFailed);
        });
```

### RuleId[ruleID=CodeBlock2Expr]
Statement lambda can be replaced with expression lambda
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileSplitByPartitionTool.java`
#### Snippet
```java
        .values()
        .forEach(
            writer -> {
              writer.sealCurrentPage();
            });
```

### RuleId[ruleID=CodeBlock2Expr]
Statement lambda can be replaced with expression lambda
in `server/src/main/java/org/apache/iotdb/db/tools/validate/TsFileValidationTool.java`
#### Snippet
```java
                              deviceID,
                              k -> {
                                return new Pair<>("", Long.MIN_VALUE);
                              })
                          .right) {
```

### RuleId[ruleID=CodeBlock2Expr]
Statement lambda can be replaced with expression lambda
in `server/src/main/java/org/apache/iotdb/db/tools/validate/TsFileValidationTool.java`
#### Snippet
```java
                                deviceID,
                                k -> {
                                  return new Pair<>("", Long.MIN_VALUE);
                                })
                            .right) {
```

### RuleId[ruleID=CodeBlock2Expr]
Statement lambda can be replaced with expression lambda
in `server/src/main/java/org/apache/iotdb/db/rescon/SystemInfo.java`
#### Snippet
```java
      selectedTsFileProcessor.setWorkMemTableShouldFlush();
      flushTaskSubmitThreadPool.submit(
          () -> {
            selectedTsFileProcessor.submitAFlushTask();
          });
```

### RuleId[ruleID=CodeBlock2Expr]
Statement lambda can be replaced with expression lambda
in `server/src/main/java/org/apache/iotdb/db/service/thrift/impl/DataNodeRegionManager.java`
#### Snippet
```java
        .getAllSchemaRegions()
        .forEach(
            schemaRegion -> {
              schemaRegionLockMap.put(
                  schemaRegion.getSchemaRegionId(), new ReentrantReadWriteLock(false));
```

### RuleId[ruleID=CodeBlock2Expr]
Statement lambda can be replaced with expression lambda
in `server/src/main/java/org/apache/iotdb/db/service/thrift/impl/DataNodeInternalRPCServiceImpl.java`
#### Snippet
```java
          .getAllConsensusGroupIds()
          .forEach(
              groupId -> {
                result.put(
                    groupId.convertToTConsensusGroupId(),
```

### RuleId[ruleID=CodeBlock2Expr]
Statement lambda can be replaced with expression lambda
in `server/src/main/java/org/apache/iotdb/db/service/thrift/impl/DataNodeInternalRPCServiceImpl.java`
#### Snippet
```java
          .getAllConsensusGroupIds()
          .forEach(
              groupId -> {
                result.put(
                    groupId.convertToTConsensusGroupId(),
```

### RuleId[ruleID=CodeBlock2Expr]
Statement lambda can be replaced with expression lambda
in `metrics/micrometer-metrics/src/main/java/org/apache/iotdb/metrics/micrometer/reporter/IoTDBMeterRegistry.java`
#### Snippet
```java
                Map<String, String> labels = tagsConvertToMap(tags);
                meter.use(
                    gauge -> {
                      updateValue(name, labels, gauge.value(), time);
                    },
```

### RuleId[ruleID=CodeBlock2Expr]
Statement lambda can be replaced with expression lambda
in `metrics/micrometer-metrics/src/main/java/org/apache/iotdb/metrics/micrometer/reporter/IoTDBMeterRegistry.java`
#### Snippet
```java
                      updateValue(name, labels, gauge.value(), time);
                    },
                    counter -> {
                      updateValue(name, labels, counter.count(), time);
                    },
```

### RuleId[ruleID=CodeBlock2Expr]
Statement lambda can be replaced with expression lambda
in `metrics/micrometer-metrics/src/main/java/org/apache/iotdb/metrics/micrometer/reporter/IoTDBMeterRegistry.java`
#### Snippet
```java
                      updateValue(name, labels, counter.count(), time);
                    },
                    timer -> {
                      writeSnapshotAndCount(name, labels, timer.takeSnapshot(), time);
                    },
```

### RuleId[ruleID=CodeBlock2Expr]
Statement lambda can be replaced with expression lambda
in `metrics/micrometer-metrics/src/main/java/org/apache/iotdb/metrics/micrometer/reporter/IoTDBMeterRegistry.java`
#### Snippet
```java
                      writeSnapshotAndCount(name, labels, timer.takeSnapshot(), time);
                    },
                    summary -> {
                      writeSnapshotAndCount(name, labels, summary.takeSnapshot(), time);
                    },
```

### RuleId[ruleID=CodeBlock2Expr]
Statement lambda can be replaced with expression lambda
in `metrics/micrometer-metrics/src/main/java/org/apache/iotdb/metrics/micrometer/reporter/IoTDBMeterRegistry.java`
#### Snippet
```java
                      writeSnapshotAndCount(name, labels, summary.takeSnapshot(), time);
                    },
                    longTaskTimer -> {
                      updateValue(name, labels, (double) longTaskTimer.activeTasks(), time);
                    },
```

### RuleId[ruleID=CodeBlock2Expr]
Statement lambda can be replaced with expression lambda
in `metrics/micrometer-metrics/src/main/java/org/apache/iotdb/metrics/micrometer/reporter/IoTDBMeterRegistry.java`
#### Snippet
```java
                      updateValue(name, labels, (double) longTaskTimer.activeTasks(), time);
                    },
                    timeGauge -> {
                      updateValue(name, labels, timeGauge.value(getBaseTimeUnit()), time);
                    },
```

### RuleId[ruleID=CodeBlock2Expr]
Statement lambda can be replaced with expression lambda
in `metrics/micrometer-metrics/src/main/java/org/apache/iotdb/metrics/micrometer/reporter/IoTDBMeterRegistry.java`
#### Snippet
```java
                      updateValue(name, labels, timeGauge.value(getBaseTimeUnit()), time);
                    },
                    functionCounter -> {
                      updateValue(name, labels, functionCounter.count(), time);
                    },
```

### RuleId[ruleID=CodeBlock2Expr]
Statement lambda can be replaced with expression lambda
in `metrics/micrometer-metrics/src/main/java/org/apache/iotdb/metrics/micrometer/reporter/IoTDBMeterRegistry.java`
#### Snippet
```java
                      updateValue(name, labels, functionCounter.count(), time);
                    },
                    functionTimer -> {
                      updateValue(name, labels, functionTimer.count(), time);
                    },
```

### RuleId[ruleID=CodeBlock2Expr]
Statement lambda can be replaced with expression lambda
in `metrics/micrometer-metrics/src/main/java/org/apache/iotdb/metrics/micrometer/reporter/IoTDBMeterRegistry.java`
#### Snippet
```java
                      updateValue(name, labels, functionTimer.count(), time);
                    },
                    m -> {
                      logger.debug("unknown meter:" + meter);
                    });
```

### RuleId[ruleID=CodeBlock2Expr]
Statement lambda can be replaced with expression lambda
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/TsFileWriter.java`
#### Snippet
```java
    MeasurementGroup measurementGroup = new MeasurementGroup(true);
    measurementSchemas.forEach(
        measurementSchema -> {
          measurementGroup
              .getMeasurementSchemaMap()
```

## RuleId[ruleID=UseOfPropertiesAsHashtable]
### RuleId[ruleID=UseOfPropertiesAsHashtable]
Call to `Hashtable.put()` on properties object
in `example/kafka/src/main/java/org/apache/iotdb/kafka/Producer.java`
#### Snippet
```java

    Properties props = new Properties();
    props.put(ProducerConfig.BOOTSTRAP_SERVERS_CONFIG, Constant.KAFKA_SERVICE_URL);
    props.put(ProducerConfig.KEY_SERIALIZER_CLASS_CONFIG, StringSerializer.class);
    props.put(ProducerConfig.VALUE_SERIALIZER_CLASS_CONFIG, StringSerializer.class);
```

### RuleId[ruleID=UseOfPropertiesAsHashtable]
Call to `Hashtable.put()` on properties object
in `example/kafka/src/main/java/org/apache/iotdb/kafka/Producer.java`
#### Snippet
```java
    Properties props = new Properties();
    props.put(ProducerConfig.BOOTSTRAP_SERVERS_CONFIG, Constant.KAFKA_SERVICE_URL);
    props.put(ProducerConfig.KEY_SERIALIZER_CLASS_CONFIG, StringSerializer.class);
    props.put(ProducerConfig.VALUE_SERIALIZER_CLASS_CONFIG, StringSerializer.class);
    producer = new KafkaProducer<>(props);
```

### RuleId[ruleID=UseOfPropertiesAsHashtable]
Call to `Hashtable.put()` on properties object
in `example/kafka/src/main/java/org/apache/iotdb/kafka/Producer.java`
#### Snippet
```java
    props.put(ProducerConfig.BOOTSTRAP_SERVERS_CONFIG, Constant.KAFKA_SERVICE_URL);
    props.put(ProducerConfig.KEY_SERIALIZER_CLASS_CONFIG, StringSerializer.class);
    props.put(ProducerConfig.VALUE_SERIALIZER_CLASS_CONFIG, StringSerializer.class);
    producer = new KafkaProducer<>(props);
  }
```

### RuleId[ruleID=UseOfPropertiesAsHashtable]
Call to `Hashtable.put()` on properties object
in `example/kafka/src/main/java/org/apache/iotdb/kafka/Consumer.java`
#### Snippet
```java
      Properties props = new Properties();

      props.put(ConsumerConfig.BOOTSTRAP_SERVERS_CONFIG, Constant.KAFKA_SERVICE_URL);
      /** serializer class */
      props.put(ConsumerConfig.KEY_DESERIALIZER_CLASS_CONFIG, StringDeserializer.class);
```

### RuleId[ruleID=UseOfPropertiesAsHashtable]
Call to `Hashtable.put()` on properties object
in `example/kafka/src/main/java/org/apache/iotdb/kafka/Consumer.java`
#### Snippet
```java
      props.put(ConsumerConfig.BOOTSTRAP_SERVERS_CONFIG, Constant.KAFKA_SERVICE_URL);
      /** serializer class */
      props.put(ConsumerConfig.KEY_DESERIALIZER_CLASS_CONFIG, StringDeserializer.class);
      props.put(ConsumerConfig.VALUE_DESERIALIZER_CLASS_CONFIG, StringDeserializer.class);
      /**
```

### RuleId[ruleID=UseOfPropertiesAsHashtable]
Call to `Hashtable.put()` on properties object
in `example/kafka/src/main/java/org/apache/iotdb/kafka/Consumer.java`
#### Snippet
```java
      /** serializer class */
      props.put(ConsumerConfig.KEY_DESERIALIZER_CLASS_CONFIG, StringDeserializer.class);
      props.put(ConsumerConfig.VALUE_DESERIALIZER_CLASS_CONFIG, StringDeserializer.class);
      /**
       * What to do when there is no initial offset in ZooKeeper or if an offset is out of range
```

### RuleId[ruleID=UseOfPropertiesAsHashtable]
Call to `Hashtable.put()` on properties object
in `example/kafka/src/main/java/org/apache/iotdb/kafka/Consumer.java`
#### Snippet
```java
       * the offset to the latest offset
       */
      props.put(ConsumerConfig.AUTO_OFFSET_RESET_CONFIG, "earliest");
      /** There is only one consumer in a group */
      props.put(ConsumerConfig.GROUP_ID_CONFIG, Constant.TOPIC);
```

### RuleId[ruleID=UseOfPropertiesAsHashtable]
Call to `Hashtable.put()` on properties object
in `example/kafka/src/main/java/org/apache/iotdb/kafka/Consumer.java`
#### Snippet
```java
      props.put(ConsumerConfig.AUTO_OFFSET_RESET_CONFIG, "earliest");
      /** There is only one consumer in a group */
      props.put(ConsumerConfig.GROUP_ID_CONFIG, Constant.TOPIC);

      KafkaConsumer<String, String> consumer = new KafkaConsumer<>(props);
```

### RuleId[ruleID=UseOfPropertiesAsHashtable]
Call to `Hashtable.put()` on properties object
in `jdbc/src/main/java/org/apache/iotdb/jdbc/Utils.java`
#### Snippet
```java
        case Config.VERSION:
        case Config.NETWORK_TIMEOUT:
          info.put(key, value);
          break;
        case Config.TIME_ZONE:
```

### RuleId[ruleID=UseOfPropertiesAsHashtable]
Call to `Hashtable.put()` on properties object
in `jdbc/src/main/java/org/apache/iotdb/jdbc/Utils.java`
#### Snippet
```java
            return false;
          }
          info.put(key, value);
          break;
        default:
```

### RuleId[ruleID=UseOfPropertiesAsHashtable]
Call to `Hashtable.get()` on properties object
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBConnection.java`
#### Snippet
```java
    params = Utils.parseUrl(url, info);
    this.url = url;
    this.userName = info.get("user").toString();
    this.networkTimeout = params.getNetworkTimeout();
    this.zoneId = ZoneId.of(params.getTimeZone());
```

## RuleId[ruleID=EmptyMethod]
### RuleId[ruleID=EmptyMethod]
Method only calls its super
in `confignode/src/main/java/org/apache/iotdb/confignode/consensus/statemachine/ConfigNodeRegionStateMachine.java`
#### Snippet
```java

  @Override
  public long getSleepTime() {
    // TODO implement this
    return RetryPolicy.super.getSleepTime();
```

### RuleId[ruleID=EmptyMethod]
The method is empty
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/Procedure.java`
#### Snippet
```java
   * @param env environment
   */
  protected final void beforeRecover(Env env) {
    // no op
  }
```

### RuleId[ruleID=EmptyMethod]
The method is empty
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/Procedure.java`
#### Snippet
```java
   * @param env environment
   */
  protected final void afterRecover(Env env) {
    // no op
  }
```

### RuleId[ruleID=EmptyMethod]
The method is empty
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/Procedure.java`
#### Snippet
```java
   * @param env environment
   */
  protected void completionCleanup(Env env) {
    // no op
  }
```

### RuleId[ruleID=EmptyMethod]
The method is empty
in `consensus/src/main/java/org/apache/iotdb/consensus/multileader/service/MultiLeaderRPCServiceProcessor.java`
#### Snippet
```java
  }

  public void handleClientExit() {}
}

```

### RuleId[ruleID=EmptyMethod]
All implementations of this method are empty
in `external-pipe-api/src/main/java/org/apache/iotdb/pipe/external/api/IExternalPipeSinkWriter.java`
#### Snippet
```java
   * @param value Value of the data point.
   */
  void insertBoolean(String sgName, String[] path, long time, boolean value) throws IOException;

  /**
```

### RuleId[ruleID=EmptyMethod]
All implementations of this method are empty
in `external-pipe-api/src/main/java/org/apache/iotdb/pipe/external/api/IExternalPipeSinkWriter.java`
#### Snippet
```java
   * @param value Value of the data point.
   */
  void insertInt32(String sgName, String[] path, long time, int value) throws IOException;

  /**
```

### RuleId[ruleID=EmptyMethod]
All implementations of this method are empty
in `external-pipe-api/src/main/java/org/apache/iotdb/pipe/external/api/IExternalPipeSinkWriter.java`
#### Snippet
```java
   * @param value Value of the data point.
   */
  void insertInt64(String sgName, String[] path, long time, long value) throws IOException;

  /**
```

### RuleId[ruleID=EmptyMethod]
All implementations of this method are empty
in `external-pipe-api/src/main/java/org/apache/iotdb/pipe/external/api/IExternalPipeSinkWriter.java`
#### Snippet
```java
   * @param value Value of the data point.
   */
  void insertFloat(String sgName, String[] path, long time, float value) throws IOException;

  /**
```

### RuleId[ruleID=EmptyMethod]
All implementations of this method are empty
in `external-pipe-api/src/main/java/org/apache/iotdb/pipe/external/api/IExternalPipeSinkWriter.java`
#### Snippet
```java
   * @param value Value of the data point.
   */
  void insertDouble(String sgName, String[] path, long time, double value) throws IOException;

  /**
```

### RuleId[ruleID=EmptyMethod]
All implementations of this method are empty
in `external-pipe-api/src/main/java/org/apache/iotdb/pipe/external/api/IExternalPipeSinkWriter.java`
#### Snippet
```java
   * @param value Value of the data point.
   */
  void insertText(String sgName, String[] path, long time, String value) throws IOException;

  //  /**
```

### RuleId[ruleID=EmptyMethod]
All implementations of this method are empty
in `external-pipe-api/src/main/java/org/apache/iotdb/pipe/external/api/IExternalPipeSinkWriter.java`
#### Snippet
```java
   * @param endTime Ending timestamp of the deleted data points. Unit ms.
   */
  void delete(String sgName, String delPath, long startTime, long endTime) throws IOException;

  //  /**
```

### RuleId[ruleID=EmptyMethod]
All implementations of this method are empty
in `external-pipe-api/src/main/java/org/apache/iotdb/pipe/external/api/IExternalPipeSinkWriter.java`
#### Snippet
```java
   * <p>The framework will retry if this method throws an {@link IOException}.
   */
  void flush() throws IOException;

  /** Get the status of this writer. This method should NOT throw any exception. */
```

### RuleId[ruleID=EmptyMethod]
Method only calls its super
in `library-udf/src/main/java/org/apache/iotdb/library/drepair/util/ScreenFill.java`
#### Snippet
```java

  @Override
  public long[] getTime() {
    return super.getTime();
  }
```

### RuleId[ruleID=EmptyMethod]
Method only calls its super
in `library-udf/src/main/java/org/apache/iotdb/library/drepair/util/ScreenFill.java`
#### Snippet
```java

  @Override
  public double[] getFilled() {
    return super.getFilled();
  }
```

### RuleId[ruleID=EmptyMethod]
Method only calls its super
in `library-udf/src/main/java/org/apache/iotdb/library/drepair/util/ScreenFill.java`
#### Snippet
```java

  @Override
  public void calMeanAndVar() throws UDFException {
    super.calMeanAndVar();
  }
```

### RuleId[ruleID=EmptyMethod]
All implementations of this method are empty
in `node-commons/src/main/java/org/apache/iotdb/commons/security/encrypt/AsymmetricEncrypt.java`
#### Snippet
```java
   * @param providerParameters encrypt need some parameters
   */
  void init(String providerParameters);

  /**
```

### RuleId[ruleID=EmptyMethod]
All implementations of this method are empty
in `schema-engine-tag/src/main/java/org/apache/iotdb/lsm/manager/ILSMManager.java`
#### Snippet
```java
   * @param context request context
   */
  void postProcess(T root, R request, C context);

  /**
```

### RuleId[ruleID=EmptyMethod]
Method only calls its super
in `server/src/main/java/org/apache/iotdb/db/consensus/statemachine/DataRegionStateMachine.java`
#### Snippet
```java

  @Override
  public long getSleepTime() {
    // TODO implement this
    return super.getSleepTime();
```

### RuleId[ruleID=EmptyMethod]
Method only calls its super
in `server/src/main/java/org/apache/iotdb/db/consensus/statemachine/SchemaRegionStateMachine.java`
#### Snippet
```java

  @Override
  public long getSleepTime() {
    // TODO implement this
    return super.getSleepTime();
```

### RuleId[ruleID=EmptyMethod]
All implementations of this method are empty
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/reader/IDataBlockReader.java`
#### Snippet
```java
  TsBlock nextBatch() throws IOException;

  void close() throws IOException;
}

```

### RuleId[ruleID=EmptyMethod]
Method only calls its super
in `server/src/main/java/org/apache/iotdb/db/mpp/aggregation/LastValueDescAccumulator.java`
#### Snippet
```java

  @Override
  public void reset() {
    super.reset();
  }
```

### RuleId[ruleID=EmptyMethod]
Method only calls its super
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/SortOperator.java`
#### Snippet
```java

  @Override
  public ListenableFuture<?> isBlocked() {
    return ProcessOperator.super.isBlocked();
  }
```

### RuleId[ruleID=EmptyMethod]
Method only calls its super
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/SortOperator.java`
#### Snippet
```java

  @Override
  public void close() throws Exception {
    ProcessOperator.super.close();
  }
```

### RuleId[ruleID=EmptyMethod]
All implementations of this method are empty
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/window/IWindow.java`
#### Snippet
```java
   * this window
   */
  void mergeOnePoint();

  /**
```

### RuleId[ruleID=EmptyMethod]
All implementations of this method are empty
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/sink/SinkNode.java`
#### Snippet
```java
  }

  public abstract void send();
}

```

### RuleId[ruleID=EmptyMethod]
All implementations of this method are empty
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/source/SourceNode.java`
#### Snippet
```java
  }

  public abstract void open() throws Exception;

  public abstract void setRegionReplicaSet(TRegionReplicaSet regionReplicaSet);
```

### RuleId[ruleID=EmptyMethod]
The method is empty
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/scheduler/ClusterScheduler.java`
#### Snippet
```java

  // Send the instances to other nodes
  private void sendFragmentInstances() {}

  // After sending, start to collect the states of these fragment instances
```

### RuleId[ruleID=EmptyMethod]
The method is empty
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/scheduler/ClusterScheduler.java`
#### Snippet
```java

  // After sending, start to collect the states of these fragment instances
  private void startMonitorInstances() {}
}

```

### RuleId[ruleID=EmptyMethod]
All implementations of this method are empty
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/scheduler/IFragInstanceDispatcher.java`
#### Snippet
```java
  Future<FragInstanceDispatchResult> dispatch(List<FragmentInstance> instances);

  void abort();
}

```

### RuleId[ruleID=EmptyMethod]
All implementations of this method are empty
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/scheduler/IScheduler.java`
#### Snippet
```java
  FragmentInfo getFragmentInfo();

  void abortFragmentInstance(FragmentInstanceId instanceId, Throwable failureCause);

  void cancelFragment(PlanFragmentId planFragmentId);
```

### RuleId[ruleID=EmptyMethod]
All implementations of this method are empty
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/scheduler/IScheduler.java`
#### Snippet
```java
  void abortFragmentInstance(FragmentInstanceId instanceId, Throwable failureCause);

  void cancelFragment(PlanFragmentId planFragmentId);
}

```

### RuleId[ruleID=EmptyMethod]
All implementations of this method are empty
in `server/src/main/java/org/apache/iotdb/db/query/executor/LastQueryExecutor.java`
#### Snippet
```java
    TimeValuePair read();

    void write(TimeValuePair pair);
  }

```

### RuleId[ruleID=EmptyMethod]
The method is empty
in `server/src/main/java/org/apache/iotdb/db/query/executor/LastQueryExecutor.java`
#### Snippet
```java
  }

  public static void clear() {}
}

```

### RuleId[ruleID=EmptyMethod]
Method only calls its super
in `server/src/main/java/org/apache/iotdb/db/query/reader/chunk/MemAlignedPageReader.java`
#### Snippet
```java

  @Override
  public BatchData getAllSatisfiedPageData() throws IOException {
    return IPageReader.super.getAllSatisfiedPageData();
  }
```

### RuleId[ruleID=EmptyMethod]
The method is empty
in `server/src/main/java/org/apache/iotdb/db/service/IoTDB.java`
#### Snippet
```java
  }

  public static void initInfluxDBMManager() {}

  private void initServiceProvider() throws QueryProcessException {
```

### RuleId[ruleID=EmptyMethod]
The method is empty
in `server/src/main/java/org/apache/iotdb/db/service/UpgradeSevice.java`
#### Snippet
```java
  }

  private static void countUpgradeFiles() {
    //    cntUpgradeFileNum.addAndGet(StorageEngine.getInstance().countUpgradeFiles());
    //    logger.info("finish counting upgrading files, total num:{}", cntUpgradeFileNum);
```

### RuleId[ruleID=EmptyMethod]
The method is empty
in `server/src/main/java/org/apache/iotdb/db/service/UpgradeSevice.java`
#### Snippet
```java
  }

  private static void upgradeAll() {
    //    try {
    //      StorageEngine.getInstance().upgradeAll();
```

### RuleId[ruleID=EmptyMethod]
The method is empty
in `server/src/main/java/org/apache/iotdb/db/service/thrift/impl/DataNodeInternalRPCServiceImpl.java`
#### Snippet
```java
  }

  public void handleClientExit() {}
}

```

### RuleId[ruleID=EmptyMethod]
Method only calls its super
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/DeletionOpBlock.java`
#### Snippet
```java
  /** release the current class object's resource */
  @Override
  public void close() {
    super.close();
  }
```

### RuleId[ruleID=EmptyMethod]
All implementations of this method are empty
in `server/src/main/java/org/apache/iotdb/db/sync/sender/manager/ISyncManager.java`
#### Snippet
```java
  File createHardlink(File tsFile, long modsOffset);

  void delete();
}

```

### RuleId[ruleID=EmptyMethod]
The method is empty
in `server/src/main/java/org/apache/iotdb/db/sync/transport/client/SenderManager.java`
#### Snippet
```java
  /** test */
  @TestOnly
  public void setSyncClient(ISyncClient syncClient) {
    // TODO: update test env
  }
```

### RuleId[ruleID=EmptyMethod]
All implementations of this method are empty
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/reader/IChunkReader.java`
#### Snippet
```java
  BatchData nextPageData() throws IOException;

  void close() throws IOException;

  List<IPageReader> loadPageReaderList() throws IOException;
```

## RuleId[ruleID=RedundantFieldInitialization]
### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/builtin/UDTFTimeDifference.java`
#### Snippet
```java
public class UDTFTimeDifference implements UDTF {

  private boolean hasPrevious = false;

  private long previousTime = 0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/builtin/UDTFTimeDifference.java`
#### Snippet
```java
  private boolean hasPrevious = false;

  private long previousTime = 0;

  @Override
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/builtin/UDTFValueTrend.java`
#### Snippet
```java
  protected int previousInt = 0;
  protected long previousLong = 0;
  protected float previousFloat = 0;
  protected double previousDouble = 0;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/builtin/UDTFValueTrend.java`
#### Snippet
```java
  protected boolean hasPrevious = false;

  protected int previousInt = 0;
  protected long previousLong = 0;
  protected float previousFloat = 0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/builtin/UDTFValueTrend.java`
#### Snippet
```java
public abstract class UDTFValueTrend implements UDTF {

  protected boolean hasPrevious = false;

  protected int previousInt = 0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/builtin/UDTFValueTrend.java`
#### Snippet
```java
  protected long previousLong = 0;
  protected float previousFloat = 0;
  protected double previousDouble = 0;

  protected TSDataType dataType;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/builtin/UDTFValueTrend.java`
#### Snippet
```java

  protected int previousInt = 0;
  protected long previousLong = 0;
  protected float previousFloat = 0;
  protected double previousDouble = 0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/utils/KDTreeUtil.java`
#### Snippet
```java
    double partitionValue;
    ArrayList<Double> value;
    boolean isLeaf = false;
    Node left;
    Node right;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/service/UDFExecutableManager.java`
#### Snippet
```java
  /////////////////////////////////////////////////////////////////////////////////////////////////

  private static UDFExecutableManager INSTANCE = null;

  public static synchronized UDFExecutableManager setupAndGetInstance(
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/service/UDFClassLoaderManager.java`
#### Snippet
```java
  /////////////////////////////////////////////////////////////////////////////////////////////////

  private static UDFClassLoaderManager INSTANCE = null;

  public static synchronized UDFClassLoaderManager setupAndGetInstance(String libRoot) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `node-commons/src/main/java/org/apache/iotdb/commons/auth/entity/User.java`
#### Snippet
```java
  private List<PathPrivilege> privilegeList;
  private List<String> roleList;
  private boolean isOpenIdUser = false; // default NO openIdUser

  private boolean useWaterMark = false; // default NO watermark
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `node-commons/src/main/java/org/apache/iotdb/commons/auth/entity/User.java`
#### Snippet
```java
  private boolean isOpenIdUser = false; // default NO openIdUser

  private boolean useWaterMark = false; // default NO watermark

  /**
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `node-commons/src/main/java/org/apache/iotdb/commons/conf/CommonConfig.java`
#### Snippet
```java
  private volatile NodeStatus status = NodeStatus.Running;

  private volatile String statusReason = null;

  /** Disk Monitor */
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `node-commons/src/main/java/org/apache/iotdb/commons/conf/CommonConfig.java`
#### Snippet
```java

  /** whether to use thrift compression. */
  private boolean isCnRpcThriftCompressionEnabled = false;

  /** What will the system do when unrecoverable error occurs. */
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `node-commons/src/main/java/org/apache/iotdb/commons/path/PathPatternNode.java`
#### Snippet
```java
   * path pattern. In PatternTreeMap, it can be replaced by the size of the valueSet
   */
  private boolean mark = false;

  private final VSerializer serializer;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `node-commons/src/main/java/org/apache/iotdb/commons/path/MeasurementPath.java`
#### Snippet
```java
  private Map<String, String> tagMap;

  private boolean isUnderAlignedEntity = false;

  // alias of measurement, null pointer cannot be serialized in thrift so empty string is instead
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `node-commons/src/main/java/org/apache/iotdb/commons/schema/tree/AbstractTreeVisitorWithLimitOffset.java`
#### Snippet
```java
  protected final boolean hasLimit;

  protected int count = 0;
  protected int curOffset = -1;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `node-commons/src/main/java/org/apache/iotdb/commons/trigger/service/TriggerExecutableManager.java`
#### Snippet
```java
  /////////////////////////////////////////////////////////////////////////////////////////////////

  private static TriggerExecutableManager INSTANCE = null;

  public static synchronized TriggerExecutableManager setupAndGetInstance(
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `node-commons/src/main/java/org/apache/iotdb/commons/service/ThriftService.java`
#### Snippet
```java
  }

  boolean setSyncedImpl = false;
  boolean setAsyncedImpl = false;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `node-commons/src/main/java/org/apache/iotdb/commons/service/ThriftService.java`
#### Snippet
```java

  boolean setSyncedImpl = false;
  boolean setAsyncedImpl = false;

  public void initSyncedServiceImpl(Object serviceImpl) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `node-commons/src/main/java/org/apache/iotdb/commons/exception/IoTDBException.java`
#### Snippet
```java
   * the full stack of the exception
   */
  protected boolean isUserException = false;

  public IoTDBException(String message, int errorCode) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `example/rest-java-example/src/main/java/org/apache/iotdb/SSLClient.java`
#### Snippet
```java
public class SSLClient {

  private static SSLConnectionSocketFactory sslConnectionSocketFactory = null;
  private static PoolingHttpClientConnectionManager poolingHttpClientConnectionManager = null;
  private static SSLContextBuilder sslContextBuilder = null;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `example/rest-java-example/src/main/java/org/apache/iotdb/SSLClient.java`
#### Snippet
```java
  private static PoolingHttpClientConnectionManager poolingHttpClientConnectionManager = null;
  private static SSLContextBuilder sslContextBuilder = null;
  private static ConnectionSocketFactory plainsf = null;

  private static class SSLClientInstance {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `example/rest-java-example/src/main/java/org/apache/iotdb/SSLClient.java`
#### Snippet
```java
  private static SSLConnectionSocketFactory sslConnectionSocketFactory = null;
  private static PoolingHttpClientConnectionManager poolingHttpClientConnectionManager = null;
  private static SSLContextBuilder sslContextBuilder = null;
  private static ConnectionSocketFactory plainsf = null;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `example/rest-java-example/src/main/java/org/apache/iotdb/SSLClient.java`
#### Snippet
```java

  private static SSLConnectionSocketFactory sslConnectionSocketFactory = null;
  private static PoolingHttpClientConnectionManager poolingHttpClientConnectionManager = null;
  private static SSLContextBuilder sslContextBuilder = null;
  private static ConnectionSocketFactory plainsf = null;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `session/src/main/java/org/apache/iotdb/session/SessionConnection.java`
#### Snippet
```java
  private TEndPoint endPoint;
  private List<TEndPoint> endPointList = new ArrayList<>();
  private boolean enableRedirect = false;

  // TestOnly
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `jdbc/src/main/java/org/apache/iotdb/jdbc/ListDataSet.java`
#### Snippet
```java
public class ListDataSet extends QueryDataSet {
  private final List<RowRecord> records = new ArrayList<>();
  private int index = 0;

  @Override
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `jdbc/src/main/java/org/apache/iotdb/jdbc/Config.java`
#### Snippet
```java
  public static final String JDBC_DRIVER_NAME = "org.apache.iotdb.jdbc.IoTDBDriver";

  public static boolean rpcThriftCompressionEnable = false;

  /** key of thrift default buffer size */
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBStatement.java`
#### Snippet
```java

  /** Keep state so we can fail certain calls made after cancel(). */
  private boolean isCancelled = false;

  /** Add SQLWarnings to the warningChain if needed. */
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBStatement.java`
#### Snippet
```java

  ZoneId zoneId;
  private ResultSet resultSet = null;
  private IoTDBConnection connection;
  private int fetchSize;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBStatement.java`
#### Snippet
```java
  private IoTDBConnection connection;
  private int fetchSize;
  private int maxRows = 0;

  /**
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBStatement.java`
#### Snippet
```java

  /** Add SQLWarnings to the warningChain if needed. */
  private SQLWarning warningChain = null;

  private long sessionId;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBStatement.java`
#### Snippet
```java
  private static final String NOT_SUPPORT_EXECUTE_UPDATE = "Not support executeUpdate";
  /** Keep state so we can fail certain calls made after close(). */
  private boolean isClosed = false;

  /** Keep state so we can fail certain calls made after cancel(). */
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBResultMetadata.java`
#### Snippet
```java
  private List<String> sgColumns;
  private String operationType = "";
  private boolean nonAlign = false;

  /** Constructor of IoTDBResultMetadata. */
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `jdbc/src/main/java/org/apache/iotdb/jdbc/GroupedLSBWatermarkEncoder.java`
#### Snippet
```java
  private int groupNumber;
  private int maxBitPosition = 5;
  private int minBitPosition = 0;

  public GroupedLSBWatermarkEncoder(
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBNonAlignJDBCResultSet.java`
#### Snippet
```java
  private TSQueryNonAlignDataSet tsQueryNonAlignDataSet;
  private byte[][] times; // used for disable align
  private List<String> sgColumns = null;
  // for disable align clause
  IoTDBNonAlignJDBCResultSet(
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBConnection.java`
#### Snippet
```java
  private IoTDBConnectionParams params;
  private boolean isClosed = true;
  private SQLWarning warningChain = null;
  private TTransport transport;
  /**
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBConnection.java`
#### Snippet
```java
   * will use server configuration.
   */
  private int queryTimeout = 0;

  /**
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBConnection.java`
#### Snippet
```java
  private static final String NOT_SUPPORT_PREPARE_CALL = "Does not support prepareCall";
  private static final String NOT_SUPPORT_PREPARE_STATEMENT = "Does not support prepareStatement";
  private IClientRPCService.Iface client = null;
  private long sessionId = -1;
  private IoTDBConnectionParams params;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `cli/src/main/java/org/apache/iotdb/cli/AbstractCli.java`
#### Snippet
```java
  private static boolean continuePrint = false;

  private static int lineCount = 0;
  private static final String SUCCESS_MESSAGE = "The statement is executed successfully.";

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `cli/src/main/java/org/apache/iotdb/cli/AbstractCli.java`
#### Snippet
```java
  static int maxPrintRowCount = 1000;
  private static int fetchSize = 1000;
  static int queryTimeout = 0;
  static String timestampPrecision = "ms";
  static String timeFormat = RpcUtils.DEFAULT_TIME_FORMAT;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `cli/src/main/java/org/apache/iotdb/cli/AbstractCli.java`
#### Snippet
```java
  private static final String SUCCESS_MESSAGE = "The statement is executed successfully.";

  private static boolean isReachEnd = false;

  static String host = "127.0.0.1";
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `cli/src/main/java/org/apache/iotdb/cli/AbstractCli.java`
#### Snippet
```java
  static Set<String> keywordSet = new HashSet<>();

  static ServerProperties properties = null;

  private static boolean cursorBeforeFirst = true;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `cli/src/main/java/org/apache/iotdb/cli/AbstractCli.java`
#### Snippet
```java
  static String password;
  static String execute;
  static boolean hasExecuteSQL = false;

  static Set<String> keywordSet = new HashSet<>();
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `cli/src/main/java/org/apache/iotdb/cli/AbstractCli.java`
#### Snippet
```java
  static String timestampPrecision = "ms";
  static String timeFormat = RpcUtils.DEFAULT_TIME_FORMAT;
  private static boolean continuePrint = false;

  private static int lineCount = 0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileSequenceRead.java`
#### Snippet
```java
public class TsFileSequenceRead {
  // if you wanna print detailed datas in pages, then turn it true.
  private static boolean printDetail = false;

  @SuppressWarnings({
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `jdbc/src/main/java/org/apache/iotdb/jdbc/AbstractIoTDBJDBCResultSet.java`
#### Snippet
```java

  protected Statement statement;
  protected SQLWarning warningChain = null;
  protected List<String> columnTypeList;
  protected IoTDBJDBCDataSet ioTDBRpcDataSet;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `schema-engine-tag/src/main/java/org/apache/iotdb/lsm/wal/WALReader.java`
#### Snippet
```java
  // next wal record
  private IWALRecord nextRecord;
  private boolean fileCorrupted = false;

  public WALReader(File logFile, IWALRecord prototype) throws IOException {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBJDBCResultSet.java`
#### Snippet
```java
  private String operationType = "";
  private List<String> columns = null;
  private List<String> sgColumns = null;

  public IoTDBJDBCResultSet(
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBJDBCResultSet.java`
#### Snippet
```java

  protected Statement statement;
  protected SQLWarning warningChain = null;
  protected List<String> columnTypeList;
  protected IoTDBRpcDataSet ioTDBRpcDataSet;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBJDBCResultSet.java`
#### Snippet
```java
  protected IoTDBTracingInfo ioTDBRpcTracingInfo;
  private String operationType = "";
  private List<String> columns = null;
  private List<String> sgColumns = null;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `service-rpc/src/main/java/org/apache/iotdb/rpc/AutoScalingBufferReadTransport.java`
#### Snippet
```java
  private final AutoResizingBuffer buf;
  private int pos = 0;
  private int limit = 0;

  public AutoScalingBufferReadTransport(int initialCapacity) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `service-rpc/src/main/java/org/apache/iotdb/rpc/AutoScalingBufferReadTransport.java`
#### Snippet
```java

  private final AutoResizingBuffer buf;
  private int pos = 0;
  private int limit = 0;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `service-rpc/src/main/java/org/apache/iotdb/rpc/RpcTransportFactory.java`
#### Snippet
```java

  // TODO: make it a config
  public static boolean USE_SNAPPY = false;
  public static RpcTransportFactory INSTANCE;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `integration/src/main/java/org/apache/iotdb/db/engine/trigger/example/Accumulator.java`
#### Snippet
```java
  public static final int BASE = 7713;

  private double accumulator = 0;
  private boolean isStopped = true;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `integration/src/main/java/org/apache/iotdb/db/engine/trigger/example/Counter.java`
#### Snippet
```java
  public static final int BASE = 1377;

  private int counter = 0;
  private boolean isStopped = true;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/qp/utils/WildcardsRemover.java`
#### Snippet
```java
public class WildcardsRemover {

  private int soffset = 0;
  private int currentOffset = 0;
  private int currentLimit =
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/qp/utils/WildcardsRemover.java`
#### Snippet
```java

  /** Records the path number that the SchemaProcessor totally returned. */
  private int consumed = 0;

  /**
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/qp/utils/WildcardsRemover.java`
#### Snippet
```java

  private int soffset = 0;
  private int currentOffset = 0;
  private int currentLimit =
      IoTDBDescriptor.getInstance().getConfig().getMaxQueryDeduplicatedPathNum() + 1;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/qp/logical/Operator.java`
#### Snippet
```java
   * DML use prefixMatch.
   */
  protected boolean isPrefixMatchPath = false;

  protected Operator(int tokenIntType) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/qp/logical/sys/SettleOperator.java`
#### Snippet
```java
  PartialPath sgPath;
  String tsFilePath;
  boolean isSgPath = false;

  public SettleOperator(int tokenIntType) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/qp/utils/GroupByLevelController.java`
#### Snippet
```java
  Set<String> offsetPaths;
  private final int[] levels;
  int prevSize = 0;
  /** count(root.sg.d1.s1) with level = 1 -> count(root.*.d1.s1) */
  private Map<String, String> groupedPathMap;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `service-rpc/src/main/java/org/apache/iotdb/rpc/IoTDBRpcDataSet.java`
#### Snippet
```java
  public int fetchSize;
  public final long timeout;
  public boolean hasCachedRecord = false;
  public boolean lastReadWasNull;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `service-rpc/src/main/java/org/apache/iotdb/rpc/IoTDBRpcDataSet.java`
#### Snippet
```java
  public static final int START_INDEX = 2;
  public String sql;
  public boolean isClosed = false;
  public IClientRPCService.Iface client;
  public List<String> columnNameList; // no deduplication
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/qp/logical/sys/ShowDevicesOperator.java`
#### Snippet
```java
  private PartialPath path;
  private int limit = 0;
  private int offset = 0;
  private boolean hasSgCol;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/qp/logical/sys/ShowDevicesOperator.java`
#### Snippet
```java

  private PartialPath path;
  private int limit = 0;
  private int offset = 0;
  private boolean hasSgCol;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `service-rpc/src/main/java/org/apache/iotdb/rpc/IoTDBJDBCDataSet.java`
#### Snippet
```java
  public int fetchSize;
  public final long timeout;
  public boolean emptyResultSet = false;
  public boolean hasCachedRecord = false;
  public boolean lastReadWasNull;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `service-rpc/src/main/java/org/apache/iotdb/rpc/IoTDBJDBCDataSet.java`
#### Snippet
```java
  public final long timeout;
  public boolean emptyResultSet = false;
  public boolean hasCachedRecord = false;
  public boolean lastReadWasNull;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `service-rpc/src/main/java/org/apache/iotdb/rpc/IoTDBJDBCDataSet.java`
#### Snippet
```java
  public int rowsIndex = 0; // used to record the row index in current TSQueryDataSet

  public TSQueryDataSet tsQueryDataSet = null;
  public byte[] time; // used to cache the current time value
  public byte[] currentBitmap; // used to cache the current bitmap for every column
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `service-rpc/src/main/java/org/apache/iotdb/rpc/IoTDBJDBCDataSet.java`
#### Snippet
```java
  public static final int START_INDEX = 2;
  public String sql;
  public boolean isClosed = false;
  public IClientRPCService.Iface client;
  public List<String> columnNameList; // no deduplication
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `service-rpc/src/main/java/org/apache/iotdb/rpc/IoTDBJDBCDataSet.java`
#### Snippet
```java
  public boolean ignoreTimeStamp;

  public int rowsIndex = 0; // used to record the row index in current TSQueryDataSet

  public TSQueryDataSet tsQueryDataSet = null;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/qp/logical/sys/CreateTimeSeriesOperator.java`
#### Snippet
```java
  private CompressionType compressor;
  private Map<String, String> props = null;
  private Map<String, String> attributes = null;
  private Map<String, String> tags = null;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/qp/logical/sys/CreateTimeSeriesOperator.java`
#### Snippet
```java
  private Map<String, String> props = null;
  private Map<String, String> attributes = null;
  private Map<String, String> tags = null;

  public CreateTimeSeriesOperator(int tokenIntType) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/qp/logical/sys/CreateTimeSeriesOperator.java`
#### Snippet
```java
  private TSEncoding encoding;
  private CompressionType compressor;
  private Map<String, String> props = null;
  private Map<String, String> attributes = null;
  private Map<String, String> tags = null;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/qp/logical/sys/ShowTimeSeriesOperator.java`
#### Snippet
```java
  private String key;
  private String value;
  private int limit = 0;
  private int offset = 0;
  // if is true, the result will be sorted according to the inserting frequency of the timeseries
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/qp/logical/sys/ShowTimeSeriesOperator.java`
#### Snippet
```java
  private String value;
  private int limit = 0;
  private int offset = 0;
  // if is true, the result will be sorted according to the inserting frequency of the timeseries
  private final boolean orderByHeat;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/qp/logical/sys/CreateAlignedTimeSeriesOperator.java`
#### Snippet
```java
  private List<Map<String, String>> tagsList = new ArrayList<>();
  private List<Map<String, String>> attributesList = new ArrayList<>();
  private List<Long> tagOffsets = null;

  public CreateAlignedTimeSeriesOperator(int tokenIntType) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/qp/logical/crud/GroupByClauseComponent.java`
#### Snippet
```java
  // sliding step
  private long slidingStep;
  private boolean isIntervalByMonth = false;
  private boolean isSlidingStepByMonth = false;
  // if it is left close and right open interval
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/qp/logical/crud/GroupByClauseComponent.java`
#### Snippet
```java
  private long slidingStep;
  private boolean isIntervalByMonth = false;
  private boolean isSlidingStepByMonth = false;
  // if it is left close and right open interval
  private boolean leftCRightO;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/qp/logical/crud/SpecialClauseComponent.java`
#### Snippet
```java
public class SpecialClauseComponent {

  protected int rowLimit = 0;
  protected int rowOffset = 0;
  protected int seriesLimit = 0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/qp/logical/crud/SpecialClauseComponent.java`
#### Snippet
```java
  protected int[] levels;

  protected boolean isAlignByDevice = false;
  protected boolean isAlignByTime = true;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/qp/logical/crud/SpecialClauseComponent.java`
#### Snippet
```java

  protected int rowLimit = 0;
  protected int rowOffset = 0;
  protected int seriesLimit = 0;
  protected int seriesOffset = 0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/qp/logical/crud/SpecialClauseComponent.java`
#### Snippet
```java
  protected int rowOffset = 0;
  protected int seriesLimit = 0;
  protected int seriesOffset = 0;

  protected boolean ascending = true;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/qp/logical/crud/SpecialClauseComponent.java`
#### Snippet
```java
  protected int rowLimit = 0;
  protected int rowOffset = 0;
  protected int seriesLimit = 0;
  protected int seriesOffset = 0;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/qp/logical/crud/FilterOperator.java`
#### Snippet
```java
  boolean isSingle = false;
  // if isSingle = false, singlePath must be null
  PartialPath singlePath = null;
  // all paths involved in this filter
  Set<PartialPath> pathSet;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/qp/logical/crud/FilterOperator.java`
#### Snippet
```java
  protected boolean isLeaf = false;
  // isSingle being true means all recursive children of this filter belong to one seriesPath.
  boolean isSingle = false;
  // if isSingle = false, singlePath must be null
  PartialPath singlePath = null;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/qp/logical/crud/FilterOperator.java`
#### Snippet
```java
  // leaf filter operator means it doesn't have left and right child filterOperator. Leaf filter
  // should set FunctionOperator.
  protected boolean isLeaf = false;
  // isSingle being true means all recursive children of this filter belong to one seriesPath.
  boolean isSingle = false;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/qp/logical/crud/SelectComponent.java`
#### Snippet
```java
  private final ZoneId zoneId;

  private boolean hasPlainAggregationFunction = false;
  private boolean hasTimeSeriesGeneratingFunction = false;
  private boolean hasUserDefinedAggregationFunction = false;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/qp/logical/crud/SelectComponent.java`
#### Snippet
```java

  private boolean hasPlainAggregationFunction = false;
  private boolean hasTimeSeriesGeneratingFunction = false;
  private boolean hasUserDefinedAggregationFunction = false;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/qp/logical/crud/SelectComponent.java`
#### Snippet
```java
  private boolean hasPlainAggregationFunction = false;
  private boolean hasTimeSeriesGeneratingFunction = false;
  private boolean hasUserDefinedAggregationFunction = false;

  protected List<ResultColumn> resultColumns = new ArrayList<>();
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `cli/src/main/java/org/apache/iotdb/tool/ImportCsv.java`
#### Snippet
```java

  private static String targetPath;
  private static String failedFileDirectory = null;
  private static int linesPerFailedFile = 10000;
  private static Boolean aligned = false;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/qp/physical/PhysicalPlan.java`
#### Snippet
```java
   * DML use prefixMatch.
   */
  private boolean isPrefixMatch = false;

  /** whether the plan can be split into more than one Plans. Only used in the cluster mode. */
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/qp/physical/PhysicalPlan.java`
#### Snippet
```java
  private static final String SERIALIZATION_UNIMPLEMENTED = "serialization unimplemented";

  private boolean isQuery = false;

  private Operator.OperatorType operatorType;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/qp/physical/sys/ShowPlan.java`
#### Snippet
```java
  private ShowContentType showContentType;
  protected int limit = 0;
  protected int offset = 0;
  protected PartialPath path;
  private boolean hasLimit;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/qp/physical/sys/ShowPlan.java`
#### Snippet
```java

  private ShowContentType showContentType;
  protected int limit = 0;
  protected int offset = 0;
  protected PartialPath path;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/qp/physical/crud/RawDataQueryPlan.java`
#### Snippet
```java
  private List<PartialPath> deduplicatedPaths = new ArrayList<>();

  private IExpression expression = null;
  private Map<String, Set<String>> deviceToMeasurements = new HashMap<>();

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/qp/physical/sys/CreateMultiTimeSeriesPlan.java`
#### Snippet
```java
  private List<String> alias = null;
  private List<Map<String, String>> props = null;
  private List<Map<String, String>> tags = null;
  private List<Map<String, String>> attributes = null;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/qp/physical/sys/CreateMultiTimeSeriesPlan.java`
#### Snippet
```java
  private List<Map<String, String>> props = null;
  private List<Map<String, String>> tags = null;
  private List<Map<String, String>> attributes = null;

  boolean[] isExecuted;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/qp/physical/sys/CreateMultiTimeSeriesPlan.java`
#### Snippet
```java
  private List<TSEncoding> encodings;
  private List<CompressionType> compressors;
  private List<String> alias = null;
  private List<Map<String, String>> props = null;
  private List<Map<String, String>> tags = null;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/qp/physical/sys/CreateMultiTimeSeriesPlan.java`
#### Snippet
```java
  private List<CompressionType> compressors;
  private List<String> alias = null;
  private List<Map<String, String>> props = null;
  private List<Map<String, String>> tags = null;
  private List<Map<String, String>> attributes = null;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/qp/physical/crud/QueryPlan.java`
#### Snippet
```java

  private int rowLimit = 0;
  private int rowOffset = 0;

  private boolean ascending = true;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/qp/physical/crud/QueryPlan.java`
#### Snippet
```java

  protected List<ResultColumn> resultColumns = null;
  protected List<MeasurementPath> paths = null;

  private boolean alignByTime = true; // for disable align sql
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/qp/physical/crud/QueryPlan.java`
#### Snippet
```java
      withoutNullColumnsIndex; // index set that withoutNullColumns for output data columns

  private boolean enableRedirect = false;
  private boolean enableTracing = false;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/qp/physical/crud/QueryPlan.java`
#### Snippet
```java

  private boolean enableRedirect = false;
  private boolean enableTracing = false;

  // if true, we don't need the row whose any column is null
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/qp/physical/crud/QueryPlan.java`
#### Snippet
```java
  private boolean alignByTime = true; // for disable align sql

  private int rowLimit = 0;
  private int rowOffset = 0;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/qp/physical/crud/QueryPlan.java`
#### Snippet
```java
      "The without null columns don't match the columns queried. If there is an alias, please use the alias.";

  protected List<ResultColumn> resultColumns = null;
  protected List<MeasurementPath> paths = null;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/qp/physical/sys/CreateAlignedTimeSeriesPlan.java`
#### Snippet
```java
  private List<Map<String, String>> tagsList;
  private List<Map<String, String>> attributesList;
  private List<Long> tagOffsets = null;

  public CreateAlignedTimeSeriesPlan() {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/Analysis.java`
#### Snippet
```java

  // indicate is there a value filter
  private boolean hasValueFilter = false;

  // a global time filter used in `initQueryDataSource` and filter push down
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/ColumnPaginationController.java`
#### Snippet
```java

  // records the path number that the SchemaProcessor totally returned
  private int consumed = 0;

  // for ALIGN BY DEVICE / DISABLE ALIGN / GROUP BY LEVEL / LAST, controller does is disabled
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/LocalExecutionPlanContext.java`
#### Snippet
```java
  private ISinkHandle sinkHandle;

  private int nextOperatorId = 0;

  private final TypeProvider typeProvider;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/qp/physical/sys/CreateTimeSeriesPlan.java`
#### Snippet
```java
  private CompressionType compressor;
  private String alias;
  private Map<String, String> props = null;
  private Map<String, String> tags = null;
  private Map<String, String> attributes = null;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/qp/physical/sys/CreateTimeSeriesPlan.java`
#### Snippet
```java
  private String alias;
  private Map<String, String> props = null;
  private Map<String, String> tags = null;
  private Map<String, String> attributes = null;
  private long tagOffset = -1;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/qp/physical/sys/CreateTimeSeriesPlan.java`
#### Snippet
```java
  private Map<String, String> props = null;
  private Map<String, String> tags = null;
  private Map<String, String> attributes = null;
  private long tagOffset = -1;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/write/InsertRowNode.java`
#### Snippet
```java
  private Object[] values;

  private boolean isNeedInferType = false;

  public InsertRowNode(PlanNodeId id) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/write/InsertTabletNode.java`
#### Snippet
```java
  private Object[] columns;

  private int rowCount = 0;

  // when this plan is sub-plan split from another InsertTabletNode, this indicates the original
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/parameter/GroupByTimeParameter.java`
#### Snippet
```java
  // if it is expressed by natural month. eg, 1mo
  private boolean isIntervalByMonth = false;
  private boolean isSlidingStepByMonth = false;

  // if it is left close and right open interval
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/parameter/GroupByTimeParameter.java`
#### Snippet
```java

  // if it is expressed by natural month. eg, 1mo
  private boolean isIntervalByMonth = false;
  private boolean isSlidingStepByMonth = false;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/distribution/DistributionPlanner.java`
#### Snippet
```java
  private LogicalQueryPlan logicalPlan;

  private int planFragmentIndex = 0;

  public DistributionPlanner(Analysis analysis, LogicalQueryPlan logicalPlan) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/metedata/write/CreateAlignedTimeSeriesNode.java`
#### Snippet
```java
  // only used inside schemaRegion to be serialized to mlog, no need to be serialized for mpp
  // transport
  private List<Long> tagOffsets = null;

  private TRegionReplicaSet regionReplicaSet;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/execution/QueryExecution.java`
#### Snippet
```java
  private static final int MAX_RETRY_COUNT = 3;
  private static final long RETRY_INTERVAL_IN_MS = 2000;
  private int retryCount = 0;
  private final MPPQueryContext context;
  private IScheduler scheduler;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/statement/crud/InsertRowStatement.java`
#### Snippet
```java
  private long time;
  private Object[] values;
  private boolean isNeedInferType = false;

  public InsertRowStatement() {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/statement/crud/QueryStatement.java`
#### Snippet
```java
  private int seriesLimit = 0;
  // series offset for result set. The default value is 0
  private int seriesOffset = 0;

  private FillComponent fillComponent;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/statement/crud/QueryStatement.java`
#### Snippet
```java
  private int rowLimit = 0;
  // row offset for result set. The default value is 0
  private int rowOffset = 0;

  // series limit and offset for result set. The default value is 0, which means no limit
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/statement/crud/QueryStatement.java`
#### Snippet
```java

  // series limit and offset for result set. The default value is 0, which means no limit
  private int seriesLimit = 0;
  // series offset for result set. The default value is 0
  private int seriesOffset = 0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/statement/crud/QueryStatement.java`
#### Snippet
```java

  // row limit and offset for result set. The default value is 0, which means no limit
  private int rowLimit = 0;
  // row offset for result set. The default value is 0
  private int rowOffset = 0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/statement/crud/InsertTabletStatement.java`
#### Snippet
```java
  private Object[] columns;

  private int rowCount = 0;

  public InsertTabletStatement() {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/statement/metadata/ShowClusterStatement.java`
#### Snippet
```java
public class ShowClusterStatement extends ShowStatement implements IConfigStatement {

  private boolean isDetails = false;

  @Override
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/statement/metadata/SetStorageGroupStatement.java`
#### Snippet
```java
  private Long ttl = null;
  private Integer schemaReplicationFactor = null;
  private Integer dataReplicationFactor = null;
  private Long timePartitionInterval = null;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/statement/metadata/SetStorageGroupStatement.java`
#### Snippet
```java
public class SetStorageGroupStatement extends Statement implements IConfigStatement {
  private PartialPath storageGroupPath;
  private Long ttl = null;
  private Integer schemaReplicationFactor = null;
  private Integer dataReplicationFactor = null;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/statement/metadata/SetStorageGroupStatement.java`
#### Snippet
```java
  private Integer schemaReplicationFactor = null;
  private Integer dataReplicationFactor = null;
  private Long timePartitionInterval = null;

  public SetStorageGroupStatement() {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/statement/metadata/SetStorageGroupStatement.java`
#### Snippet
```java
  private PartialPath storageGroupPath;
  private Long ttl = null;
  private Integer schemaReplicationFactor = null;
  private Integer dataReplicationFactor = null;
  private Long timePartitionInterval = null;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/statement/metadata/CreateTimeSeriesStatement.java`
#### Snippet
```java
  private CompressionType compressor;
  private Map<String, String> props = null;
  private Map<String, String> attributes = null;
  private Map<String, String> tags = null;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/statement/metadata/CreateTimeSeriesStatement.java`
#### Snippet
```java
  private TSEncoding encoding;
  private CompressionType compressor;
  private Map<String, String> props = null;
  private Map<String, String> attributes = null;
  private Map<String, String> tags = null;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/statement/metadata/CreateTimeSeriesStatement.java`
#### Snippet
```java
  private Map<String, String> props = null;
  private Map<String, String> attributes = null;
  private Map<String, String> tags = null;

  public CreateTimeSeriesStatement() {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/statement/metadata/CreateContinuousQueryStatement.java`
#### Snippet
```java

  // The end time of each query execution, default value is 0.
  private long endTimeOffset = 0;

  // Specify how we deal with the cq task whose previous time interval execution is not finished
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/statement/metadata/CreateContinuousQueryStatement.java`
#### Snippet
```java

  // A date that represents the execution time of a certain cq task, default value is 0.
  private long boundaryTime = 0;

  // The start time of each query execution, default value is every_interval
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/statement/metadata/ShowStatement.java`
#### Snippet
```java

  int limit = 0;
  int offset = 0;

  protected boolean isPrefixPath;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/statement/metadata/ShowStatement.java`
#### Snippet
```java
public class ShowStatement extends Statement {

  int limit = 0;
  int offset = 0;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/statement/metadata/ShowTTLStatement.java`
#### Snippet
```java
public class ShowTTLStatement extends ShowStatement implements IConfigStatement {
  private List<PartialPath> pathPatterns = new ArrayList<>();
  private boolean isAll = false;

  public boolean isAll() {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/statement/component/GroupByTimeComponent.java`
#### Snippet
```java
  // if it is expressed by natural month. eg, 1mo
  private boolean isIntervalByMonth = false;
  private boolean isSlidingStepByMonth = false;

  // if it is left close and right open interval
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/statement/component/GroupByTimeComponent.java`
#### Snippet
```java

  // if it is expressed by natural month. eg, 1mo
  private boolean isIntervalByMonth = false;
  private boolean isSlidingStepByMonth = false;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/statement/component/OrderByComponent.java`
#### Snippet
```java
  private final List<SortItem> sortItemList;

  private boolean orderByTime = false;
  private int timeOrderPriority = -1;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/statement/component/OrderByComponent.java`
#### Snippet
```java
  private int timeseriesOrderPriority = -1;

  private boolean orderByDevice = false;
  private int deviceOrderPriority = -1;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/statement/component/OrderByComponent.java`
#### Snippet
```java
  private int timeOrderPriority = -1;

  private boolean orderByTimeseries = false;
  private int timeseriesOrderPriority = -1;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/statement/component/SelectComponent.java`
#### Snippet
```java
  private boolean hasLast = false;

  private boolean hasBuiltInAggregationFunction = false;

  private List<ResultColumn> resultColumns = new ArrayList<>();
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/statement/component/SelectComponent.java`
#### Snippet
```java
  private final ZoneId zoneId;

  private boolean hasLast = false;

  private boolean hasBuiltInAggregationFunction = false;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/expression/Expression.java`
#### Snippet
```java
  /////////////////////////////////////////////////////////////////////////////////////////////////

  protected Boolean isConstantOperandCache = null;

  /** If this expression and all of its sub-expressions are {@link ConstantOperand}. */
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/expression/Expression.java`
#### Snippet
```java
  /////////////////////////////////////////////////////////////////////////////////////////////////

  protected Integer inputColumnIndex = null;

  // TODO: remove after MPP finish
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/common/filter/QueryFilter.java`
#### Snippet
```java
  boolean isSingle = false;
  // if isSingle = false, singlePath must be null
  PartialPath singlePath = null;
  // all paths involved in this filter
  Set<PartialPath> pathSet;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/common/filter/QueryFilter.java`
#### Snippet
```java
  protected boolean isLeaf = false;
  // isSingle being true means all recursive children of this filter belong to one seriesPath.
  boolean isSingle = false;
  // if isSingle = false, singlePath must be null
  PartialPath singlePath = null;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/common/filter/QueryFilter.java`
#### Snippet
```java
  // leaf filter operator means it doesn't have left and right child filterOperator. Leaf filter
  // should set FunctionOperator.
  protected boolean isLeaf = false;
  // isSingle being true means all recursive children of this filter belong to one seriesPath.
  boolean isSingle = false;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/metedata/write/CreateTimeSeriesNode.java`
#### Snippet
```java
  private String alias;
  private Map<String, String> props = null;
  private Map<String, String> tags = null;
  private Map<String, String> attributes = null;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/metedata/write/CreateTimeSeriesNode.java`
#### Snippet
```java
  private CompressionType compressor;
  private String alias;
  private Map<String, String> props = null;
  private Map<String, String> tags = null;
  private Map<String, String> attributes = null;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/metedata/write/CreateTimeSeriesNode.java`
#### Snippet
```java
  private Map<String, String> props = null;
  private Map<String, String> tags = null;
  private Map<String, String> attributes = null;

  // only used inside schemaRegion to be serialized to mlog, no need to be serialized for mpp
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0L` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/SharedTsBlockQueue.java`
#### Snippet
```java
  private boolean noMoreTsBlocks = false;

  private long bufferRetainedSizeInBytes = 0L;

  private final Queue<TsBlock> queue = new LinkedList<>();
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/SharedTsBlockQueue.java`
#### Snippet
```java
  private ListenableFuture<Void> blockedOnMemory;

  private boolean closed = false;

  private LocalSourceHandle sourceHandle;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/SharedTsBlockQueue.java`
#### Snippet
```java
  private final LocalMemoryManager localMemoryManager;

  private boolean noMoreTsBlocks = false;

  private long bufferRetainedSizeInBytes = 0L;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0L` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/memory/MemoryPool.java`
#### Snippet
```java
  private final long maxBytesPerQuery;

  private long reservedBytes = 0L;
  private final Map<String, Long> queryMemoryReservations = new HashMap<>();
  private final Queue<MemoryReservationFuture<Void>> memoryReservationFutures = new LinkedList<>();
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/LocalSourceHandle.java`
#### Snippet
```java
  private boolean aborted = false;

  private boolean closed = false;

  private int currSequenceId;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/LocalSourceHandle.java`
#### Snippet
```java
  private final SourceHandleListener sourceHandleListener;
  private final SharedTsBlockQueue queue;
  private boolean aborted = false;

  private boolean closed = false;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/LocalSinkHandle.java`
#### Snippet
```java
  private final SharedTsBlockQueue queue;
  private volatile ListenableFuture<Void> blocked = immediateFuture(null);
  private boolean aborted = false;
  private boolean closed = false;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/LocalSinkHandle.java`
#### Snippet
```java
  private volatile ListenableFuture<Void> blocked = immediateFuture(null);
  private boolean aborted = false;
  private boolean closed = false;

  public LocalSinkHandle(
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/SinkHandle.java`
#### Snippet
```java
  private boolean aborted = false;

  private boolean closed = false;

  private boolean noMoreTsBlocks = false;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/SinkHandle.java`
#### Snippet
```java
  private boolean closed = false;

  private boolean noMoreTsBlocks = false;

  public SinkHandle(
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/SinkHandle.java`
#### Snippet
```java

  private volatile ListenableFuture<Void> blocked;
  private int nextSequenceId = 0;
  /** The actual buffered memory in bytes, including the amount of memory being reserved. */
  private long bufferRetainedSizeInBytes;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/SinkHandle.java`
#### Snippet
```java
  private long bufferRetainedSizeInBytes;

  private boolean aborted = false;

  private boolean closed = false;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/SourceHandle.java`
#### Snippet
```java
  private boolean aborted = false;

  private boolean closed = false;

  public SourceHandle(
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0L` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/SourceHandle.java`
#### Snippet
```java

  /** The actual buffered memory in bytes, including the amount of memory being reserved. */
  private long bufferRetainedSizeInBytes = 0L;

  private int currSequenceId = 0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/SourceHandle.java`
#### Snippet
```java

  private int currSequenceId = 0;
  private int nextSequenceId = 0;
  private int lastSequenceId = Integer.MAX_VALUE;
  private boolean aborted = false;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/SourceHandle.java`
#### Snippet
```java
  private long bufferRetainedSizeInBytes = 0L;

  private int currSequenceId = 0;
  private int nextSequenceId = 0;
  private int lastSequenceId = Integer.MAX_VALUE;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/SourceHandle.java`
#### Snippet
```java
  private int nextSequenceId = 0;
  private int lastSequenceId = Integer.MAX_VALUE;
  private boolean aborted = false;

  private boolean closed = false;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/schema/SchemaQueryOrderByHeatOperator.java`
#### Snippet
```java
  private final boolean[] noMoreTsBlocks;
  private List<TsBlock> resultTsBlockList;
  private int currentIndex = 0;

  public SchemaQueryOrderByHeatOperator(OperatorContext operatorContext, List<Operator> operators) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/schema/CountMergeOperator.java`
#### Snippet
```java

  private List<TsBlock> resultTsBlockList;
  private int currentIndex = 0;

  private final List<Operator> children;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/schema/LevelTimeSeriesCountOperator.java`
#### Snippet
```java

  private List<TsBlock> tsBlockList;
  private int currentIndex = 0;

  private final List<TSDataType> outputDataTypes;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/schema/SchemaQueryScanOperator.java`
#### Snippet
```java
  protected OperatorContext operatorContext;
  protected List<TsBlock> tsBlockList;
  protected int currentIndex = 0;

  protected int limit;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/schema/SchemaFetchScanOperator.java`
#### Snippet
```java
  private final boolean withTags;

  private boolean isFinished = false;

  public SchemaFetchScanOperator(
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/source/AlignedSeriesScanOperator.java`
#### Snippet
```java
  private final PlanNodeId sourceId;
  private TsBlock tsBlock;
  private boolean hasCachedTsBlock = false;
  private boolean finished = false;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/source/AlignedSeriesScanOperator.java`
#### Snippet
```java
  private TsBlock tsBlock;
  private boolean hasCachedTsBlock = false;
  private boolean finished = false;

  private final long maxReturnSize;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/source/AbstractSeriesAggregationScanOperator.java`
#### Snippet
```java
  protected final TsBlockBuilder resultTsBlockBuilder;

  protected boolean finished = false;

  private final long maxRetainedSize;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/source/SeriesScanOperator.java`
#### Snippet
```java
  private TsBlock tsBlock;
  private boolean hasCachedTsBlock = false;
  private boolean finished = false;

  private final long maxReturnSize;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/source/SeriesScanOperator.java`
#### Snippet
```java
  private final PlanNodeId sourceId;
  private TsBlock tsBlock;
  private boolean hasCachedTsBlock = false;
  private boolean finished = false;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/LinearFillOperator.java`
#### Snippet
```java
  private final List<Long> cachedRowIndex;

  private long currentRowIndex = 0;
  // next TsBlock Index for each Column
  private final int[] nextTsBlockIndex;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/AbstractIntoOperator.java`
#### Snippet
```java
    private final InputLocation[] inputLocations;

    private int rowCount = 0;

    private long[] times;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/last/LastQueryMergeOperator.java`
#### Snippet
```java
  private final boolean[] noMoreTsBlocks;

  private boolean finished = false;

  private final Comparator<Binary> comparator;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/aggregation/MinValueAccumulator.java`
#### Snippet
```java
  private TSDataType seriesDataType;
  private TsPrimitiveType minResult;
  private boolean initResult = false;

  public MinValueAccumulator(TSDataType seriesDataType) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/aggregation/SumAccumulator.java`
#### Snippet
```java
  private TSDataType seriesDataType;
  private double sumValue = 0;
  private boolean initResult = false;

  public SumAccumulator(TSDataType seriesDataType) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/aggregation/SumAccumulator.java`
#### Snippet
```java

  private TSDataType seriesDataType;
  private double sumValue = 0;
  private boolean initResult = false;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/aggregation/AvgAccumulator.java`
#### Snippet
```java
  private long countValue;
  private double sumValue;
  private boolean initResult = false;

  public AvgAccumulator(TSDataType seriesDataType) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/aggregation/MaxTimeAccumulator.java`
#### Snippet
```java

  protected long maxTime = Long.MIN_VALUE;
  protected boolean initResult = false;

  public MaxTimeAccumulator() {}
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/aggregation/CountAccumulator.java`
#### Snippet
```java
public class CountAccumulator implements Accumulator {

  private long countValue = 0;

  public CountAccumulator() {}
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/aggregation/LastValueAccumulator.java`
#### Snippet
```java
  protected TsPrimitiveType lastValue;
  protected long maxTime = Long.MIN_VALUE;
  protected boolean initResult = false;

  public LastValueAccumulator(TSDataType seriesDataType) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/aggregation/timerangeiterator/PreAggrWindowIterator.java`
#### Snippet
```java
  private long curSlidingStep;
  private boolean isIntervalCyclicChange = false;
  private int intervalCnt = 0;

  private TimeRange curTimeRange;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/aggregation/timerangeiterator/PreAggrWindowIterator.java`
#### Snippet
```java
  private long curInterval;
  private long curSlidingStep;
  private boolean isIntervalCyclicChange = false;
  private int intervalCnt = 0;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/datastructure/util/ValueRecorder.java`
#### Snippet
```java
  float windowFirstValueFloat = 0;
  double windowFirstValueDouble = 0;
  boolean windowFirstValueBoolean = false;
  String windowFirstValueString = "";

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/datastructure/util/ValueRecorder.java`
#### Snippet
```java
public class ValueRecorder {

  boolean recorded = false;

  int windowFirstValueInt = 0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/datastructure/util/ValueRecorder.java`
#### Snippet
```java
  long windowFirstValueLong = 0;
  float windowFirstValueFloat = 0;
  double windowFirstValueDouble = 0;
  boolean windowFirstValueBoolean = false;
  String windowFirstValueString = "";
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/datastructure/util/ValueRecorder.java`
#### Snippet
```java

  int windowFirstValueInt = 0;
  long windowFirstValueLong = 0;
  float windowFirstValueFloat = 0;
  double windowFirstValueDouble = 0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/datastructure/util/ValueRecorder.java`
#### Snippet
```java
  int windowFirstValueInt = 0;
  long windowFirstValueLong = 0;
  float windowFirstValueFloat = 0;
  double windowFirstValueDouble = 0;
  boolean windowFirstValueBoolean = false;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/datastructure/util/ValueRecorder.java`
#### Snippet
```java
  boolean recorded = false;

  int windowFirstValueInt = 0;
  long windowFirstValueLong = 0;
  float windowFirstValueFloat = 0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/intermediate/MultiInputColumnIntermediateLayer.java`
#### Snippet
```java
      private boolean hasAtLeastOneRow = false;

      private boolean hasCached = false;
      private long nextWindowTimeBegin = strategy.getDisplayWindowBegin();
      private int nextIndexBegin = 0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/intermediate/MultiInputColumnIntermediateLayer.java`
#### Snippet
```java
          new ElasticSerializableRowRecordListBackedMultiColumnRow(dataTypes);

      private boolean hasCached = false;
      private boolean currentNull = false;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/intermediate/MultiInputColumnIntermediateLayer.java`
#### Snippet
```java

      private boolean hasCached = false;
      private boolean currentNull = false;

      @Override
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/intermediate/MultiInputColumnIntermediateLayer.java`
#### Snippet
```java
      private boolean hasCached = false;
      private long nextWindowTimeBegin = strategy.getDisplayWindowBegin();
      private int nextIndexBegin = 0;

      @Override
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/intermediate/MultiInputColumnIntermediateLayer.java`
#### Snippet
```java
          new ElasticSerializableRowRecordListBackedMultiColumnWindow(rowRecordList);

      private boolean hasCached = false;
      private int beginIndex = -slidingStep;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/intermediate/MultiInputColumnIntermediateLayer.java`
#### Snippet
```java
      private long nextWindowTimeBegin = displayWindowBegin;
      private long nextWindowTimeEnd = 0;
      private int nextIndexBegin = 0;
      private int nextIndexEnd = 1;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/intermediate/MultiInputColumnIntermediateLayer.java`
#### Snippet
```java

      private boolean isFirstIteration = true;
      private boolean hasAtLeastOneRow = false;

      private boolean hasCached = false;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/intermediate/MultiInputColumnIntermediateLayer.java`
#### Snippet
```java

  private boolean isFirstIteration = true;
  private Object[] cachedRow = null;

  public MultiInputColumnIntermediateLayer(
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/intermediate/MultiInputColumnIntermediateLayer.java`
#### Snippet
```java

      private boolean isFirstIteration = true;
      private boolean hasAtLeastOneRow = false;

      private long nextWindowTimeBegin = displayWindowBegin;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/intermediate/MultiInputColumnIntermediateLayer.java`
#### Snippet
```java

      private long nextWindowTimeBegin = displayWindowBegin;
      private long nextWindowTimeEnd = 0;
      private int nextIndexBegin = 0;
      private int nextIndexEnd = 1;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/intermediate/SingleInputColumnMultiReferenceIntermediateLayer.java`
#### Snippet
```java

      private long nextWindowTimeBegin = displayWindowBegin;
      private long nextWindowTimeEnd = 0;
      private int nextIndexBegin = 0;
      private int nextIndexEnd = 1;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/intermediate/SingleInputColumnSingleReferenceIntermediateLayer.java`
#### Snippet
```java
      private long nextWindowTimeBegin = displayWindowBegin;
      private long nextWindowTimeEnd = 0;
      private int nextIndexBegin = 0;
      private int nextIndexEnd = 1;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/intermediate/SingleInputColumnSingleReferenceIntermediateLayer.java`
#### Snippet
```java
      private boolean hasCached = false;
      private long nextWindowTimeBegin = strategy.getDisplayWindowBegin();
      private int nextIndexBegin = 0;

      @Override
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/intermediate/SingleInputColumnSingleReferenceIntermediateLayer.java`
#### Snippet
```java

      private boolean hasCached = false;
      private boolean isCurrentNull = false;

      @Override
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/intermediate/SingleInputColumnSingleReferenceIntermediateLayer.java`
#### Snippet
```java

      private boolean isFirstIteration = true;
      private boolean hasAtLeastOneRow = false;

      private long nextWindowTimeBegin = displayWindowBegin;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/intermediate/SingleInputColumnSingleReferenceIntermediateLayer.java`
#### Snippet
```java
      private boolean hasAtLeastOneRow = false;

      private boolean hasCached = false;
      private long nextWindowTimeBegin = strategy.getDisplayWindowBegin();
      private int nextIndexBegin = 0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/intermediate/SingleInputColumnSingleReferenceIntermediateLayer.java`
#### Snippet
```java

      private boolean isFirstIteration = true;
      private boolean hasAtLeastOneRow = false;

      private boolean hasCached = false;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/intermediate/SingleInputColumnMultiReferenceIntermediateLayer.java`
#### Snippet
```java
          new ElasticSerializableTVListBackedSingleColumnWindow(tvList);

      private boolean hasCached = false;
      private int beginIndex = -slidingStep;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/intermediate/SingleInputColumnSingleReferenceIntermediateLayer.java`
#### Snippet
```java

      private boolean isFirstIteration = true;
      private boolean hasAtLeastOneRow = false;

      private long nextWindowTimeBegin = displayWindowBegin;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/intermediate/SingleInputColumnMultiReferenceIntermediateLayer.java`
#### Snippet
```java

      private boolean isFirstIteration = true;
      private boolean hasAtLeastOneRow = false;

      private long nextWindowTimeBegin = displayWindowBegin;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/intermediate/SingleInputColumnSingleReferenceIntermediateLayer.java`
#### Snippet
```java
          new ElasticSerializableTVListBackedSingleColumnWindow(tvList);

      private boolean hasCached = false;
      private int beginIndex = -slidingStep;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/intermediate/SingleInputColumnSingleReferenceIntermediateLayer.java`
#### Snippet
```java

      private long nextWindowTimeBegin = displayWindowBegin;
      private long nextWindowTimeEnd = 0;
      private int nextIndexBegin = 0;
      private int nextIndexEnd = 1;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/intermediate/SingleInputColumnSingleReferenceIntermediateLayer.java`
#### Snippet
```java
      private long nextWindowTimeBegin = displayWindowBegin;
      private long nextWindowTimeEnd = 0;
      private int nextIndexBegin = 0;
      private int nextIndexEnd = 1;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/intermediate/SingleInputColumnSingleReferenceIntermediateLayer.java`
#### Snippet
```java
      private final Row row = new LayerPointReaderBackedSingleColumnRow(parentLayerPointReader);

      private boolean hasCached = false;
      private boolean isCurrentNull = false;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/intermediate/SingleInputColumnMultiReferenceIntermediateLayer.java`
#### Snippet
```java
          new ElasticSerializableTVListBackedSingleColumnRow(tvList, -1);

      private boolean hasCached = false;
      private int currentRowIndex = -1;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/intermediate/SingleInputColumnSingleReferenceIntermediateLayer.java`
#### Snippet
```java

      private long nextWindowTimeBegin = displayWindowBegin;
      private long nextWindowTimeEnd = 0;
      private int nextIndexBegin = 0;
      private int nextIndexEnd = 1;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/intermediate/SingleInputColumnMultiReferenceIntermediateLayer.java`
#### Snippet
```java

      private boolean isFirstIteration = true;
      private boolean hasAtLeastOneRow = false;

      private long nextWindowTimeBegin = displayWindowBegin;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/intermediate/SingleInputColumnMultiReferenceIntermediateLayer.java`
#### Snippet
```java
      private long nextWindowTimeBegin = displayWindowBegin;
      private long nextWindowTimeEnd = 0;
      private int nextIndexBegin = 0;
      private int nextIndexEnd = 1;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/intermediate/SingleInputColumnMultiReferenceIntermediateLayer.java`
#### Snippet
```java
      private final SafetyPile safetyPile = safetyLine.addSafetyPile();

      private boolean hasCached = false;
      private int currentPointIndex = -1;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/intermediate/SingleInputColumnMultiReferenceIntermediateLayer.java`
#### Snippet
```java
      private long nextWindowTimeBegin = displayWindowBegin;
      private long nextWindowTimeEnd = 0;
      private int nextIndexBegin = 0;
      private int nextIndexEnd = 1;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/intermediate/SingleInputColumnMultiReferenceIntermediateLayer.java`
#### Snippet
```java
      private boolean hasCached = false;
      private long nextWindowTimeBegin = nextWindowTimeBeginGivenByStrategy;
      private int nextIndexBegin = 0;
      private boolean hasAtLeastOneRow;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/intermediate/SingleInputColumnMultiReferenceIntermediateLayer.java`
#### Snippet
```java

      private long nextWindowTimeBegin = displayWindowBegin;
      private long nextWindowTimeEnd = 0;
      private int nextIndexBegin = 0;
      private int nextIndexEnd = 1;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/intermediate/SingleInputColumnMultiReferenceIntermediateLayer.java`
#### Snippet
```java

      private boolean isFirstIteration = true;
      private boolean hasCached = false;
      private long nextWindowTimeBegin = nextWindowTimeBeginGivenByStrategy;
      private int nextIndexBegin = 0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/wal/io/WALReader.java`
#### Snippet
```java
  private final DataInputStream logStream;
  private WALEntry nextEntry;
  private boolean fileCorrupted = false;

  public WALReader(File logFile) throws IOException {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/wal/node/WALNode.java`
#### Snippet
```java
    private long nextSearchIndex;
    /** files to search */
    private File[] filesToSearch = null;
    /** index of current searching file in the filesToSearch */
    private int currentFileIndex = -1;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/wal/node/WALNode.java`
#### Snippet
```java
    private long firstValidVersionId;
    /** recursion time of calling deletion */
    private int recursionTime = 0;

    @Override
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/wal/node/WALNode.java`
#### Snippet
```java
    private final List<IndexedConsensusRequest> insertNodes = new LinkedList<>();
    /** iterator of insertNodes */
    private Iterator<IndexedConsensusRequest> itr = null;

    public PlanNodeIterator(long startIndex) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/wal/recover/WALRecoverManager.java`
#### Snippet
```java

  /** true when the recover procedure has started */
  private volatile boolean hasStarted = false;
  /** start recovery after all data regions have submitted unsealed zero-level TsFiles */
  private volatile CountDownLatch allDataRegionScannedLatch;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/wal/buffer/WALBuffer.java`
#### Snippet
```java
   */
  private class ByteBufferView implements IWALByteBufferView {
    private int flushedBytesNum = 0;

    private void ensureEnoughSpace(int bytesNum) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/wal/buffer/WALBuffer.java`
#### Snippet
```java
    final WALMetaData metaData = new WALMetaData();
    final List<WALFlushListener> fsyncListeners = new LinkedList<>();
    WALFlushListener rollWALFileWriterListener = null;
  }

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/wal/buffer/WALBuffer.java`
#### Snippet
```java
    private final ByteBufferView byteBufferVew = new ByteBufferView();
    private final SerializeInfo info = new SerializeInfo();
    private int batchSize = 0;

    @Override
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/wal/buffer/WALBuffer.java`
#### Snippet
```java

  /** whether close method is called */
  private volatile boolean isClosed = false;
  /** WALEntries */
  private final BlockingQueue<WALEntry> walEntries = new ArrayBlockingQueue<>(QUEUE_CAPACITY);
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/wal/checkpoint/CheckpointManager.java`
#### Snippet
```java
  private long maxMemTableId = 0;
  /** current checkpoint file version id, only updated by fsyncAndDeleteThread */
  private int currentCheckPointFileVersion = 0;
  /** current checkpoint file log writer, only updated by fsyncAndDeleteThread */
  private ILogWriter currentLogWriter;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/wal/checkpoint/CheckpointManager.java`
#### Snippet
```java
  private volatile ByteBuffer cachedByteBuffer;
  /** max memTable id */
  private long maxMemTableId = 0;
  /** current checkpoint file version id, only updated by fsyncAndDeleteThread */
  private int currentCheckPointFileVersion = 0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/conf/rest/IoTDBRestServiceConfig.java`
#### Snippet
```java

  /** Is client authentication required */
  private boolean clientAuth = false;

  public boolean isClientAuth() {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/conf/rest/IoTDBRestServiceConfig.java`
#### Snippet
```java

  /** enable the REST Service ssl. */
  private boolean enableHttps = false;

  /** ssl key Store Path */
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/conf/rest/IoTDBRestServiceConfig.java`
#### Snippet
```java

  /** Whether to display rest service interface information through swagger */
  private boolean enableSwagger = false;

  /** enable the REST Service ssl. */
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/conf/rest/IoTDBRestServiceConfig.java`
#### Snippet
```java
  static final String CONFIG_NAME = "iotdb-common.properties";
  /** if the enableRestService is true, we will start REST Service */
  private boolean enableRestService = false;

  /** set the REST Service port. */
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/sync/transport/client/IoTDBSyncClient.java`
#### Snippet
```java
  private static final int TRANSFER_BUFFER_SIZE_IN_BYTES = 1 * 1024 * 1024;

  private TTransport transport = null;
  private volatile IClientRPCService.Client serviceClient = null;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/sync/transport/client/IoTDBSyncClient.java`
#### Snippet
```java

  private TTransport transport = null;
  private volatile IClientRPCService.Client serviceClient = null;

  /* remote IP address*/
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/sync/transport/client/SenderManager.java`
#### Snippet
```java
  protected ScheduledExecutorService heartbeatExecutorService;

  protected long lastReportTime = 0;
  protected long lostConnectionTime = Long.MAX_VALUE;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/PipeOpSgManager.java`
#### Snippet
```java
  private long beginIndex = 0;
  // Operators number of this StorageGroup
  private long dataCount = 0;
  private long committedIndex = -1;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/PipeOpSgManager.java`
#### Snippet
```java

  // record First Operator's index
  private long beginIndex = 0;
  // Operators number of this StorageGroup
  private long dataCount = 0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/AbstractOpBlock.java`
#### Snippet
```java
  long pipeDataSerialNumber;

  boolean closed = false;

  // record First Entry's index. It is the index of all data of 1 SG.
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/DeletionGroup.java`
#### Snippet
```java
  public static class IntervalCursor {
    Iterator<Map.Entry<Long, Long>> iter = null;
    boolean subsequentNoDelete = false;
    public long startTime;
    public long endTime;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/DeletionGroup.java`
#### Snippet
```java

  public static class IntervalCursor {
    Iterator<Map.Entry<Long, Long>> iter = null;
    boolean subsequentNoDelete = false;
    public long startTime;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/PipeOpManager.java`
#### Snippet
```java
  }
  // Used to notify consumers that new data arrived.
  private NewDataEventHandler newDataEventHandler = null;

  public PipeOpManager(TsFilePipe filePipe) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/conf/IoTDBConfig.java`
#### Snippet
```java

  /** indicate whether current mode is cluster */
  private boolean isClusterMode = false;

  /**
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/conf/IoTDBConfig.java`
#### Snippet
```java
  private long recoveryLogIntervalInMs = 5_000L;

  private boolean enableDiscardOutOfOrderData = false;

  /** the method to transform device path to device id, can be 'Plain' or 'SHA256' */
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/conf/IoTDBConfig.java`
#### Snippet
```java
   * whether create mapping file of id table. This file can map device id in tsfile to device path
   */
  private boolean enableIDTableLogFile = false;

  /** whether to use persistent schema mode */
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/conf/IoTDBConfig.java`
#### Snippet
```java
   * iotdb-common.properties
   */
  private boolean enableInfluxDBRpcService = false;

  /** the size of ioTaskQueue */
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/conf/IoTDBConfig.java`
#### Snippet
```java

  /** Is the write ahead log enable. */
  private boolean enableIndex = false;

  /** How many threads can concurrently build index. When <= 0, use CPU core number. */
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/conf/IoTDBConfig.java`
#### Snippet
```java

  /** whether use chunkBufferPool. */
  private boolean chunkBufferPoolEnable = false;

  /** Switch of watermark function */
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/conf/IoTDBConfig.java`
#### Snippet
```java

  /** Memory allocated for PartitionCache */
  private long allocateMemoryForPartitionCache = 0;

  /** Memory allocated for LastCache */
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0L` is redundant
in `server/src/main/java/org/apache/iotdb/db/conf/IoTDBConfig.java`
#### Snippet
```java

  /** When a sequence TsFile's file size (in byte) exceed this, the TsFile is forced closed. */
  private long seqTsFileSize = 0L;

  /** When a memTable's size (in byte) exceeds this, the memtable is flushed to disk. Unit: byte */
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/conf/IoTDBConfig.java`
#### Snippet
```java

  /** whether to enable the mqtt service. */
  private boolean enableMQTTService = false;

  /** the mqtt service binding host. */
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/conf/IoTDBConfig.java`
#### Snippet
```java

  /** Set true to enable writing monitor time series. */
  private boolean enableMonitorSeriesWrite = false;

  /** Cache size of {@code checkAndGetDataTypeCache} in {@link LocalSchemaProcessor}. */
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/conf/IoTDBConfig.java`
#### Snippet
```java

  private boolean dataRatisConsensusLogUnsafeFlushEnable = false;
  private boolean schemaRatisConsensusLogUnsafeFlushEnable = false;

  private long dataRatisConsensusLogSegmentSizeMax = 24 * 1024 * 1024L;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/conf/IoTDBConfig.java`
#### Snippet
```java
  private long schemaRatisConsensusSnapshotTriggerThreshold = 400000L;

  private boolean dataRatisConsensusLogUnsafeFlushEnable = false;
  private boolean schemaRatisConsensusLogUnsafeFlushEnable = false;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0L` is redundant
in `server/src/main/java/org/apache/iotdb/db/conf/IoTDBConfig.java`
#### Snippet
```java

  /** When a unSequence TsFile's file size (in byte) exceed this, the TsFile is forced closed. */
  private long unSeqTsFileSize = 0L;

  /** When a sequence TsFile's file size (in byte) exceed this, the TsFile is forced closed. */
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/conf/IoTDBConfig.java`
#### Snippet
```java

  /** Whether to timed flush sequence tsfiles' memtables. */
  private boolean enableTimedFlushSeqMemtable = false;

  /**
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/conf/IoTDBConfig.java`
#### Snippet
```java

  /** whether to use id table. ATTENTION: id table is not compatible with alias */
  private boolean enableIDTable = false;

  /**
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/conf/IoTDBConfig.java`
#### Snippet
```java

  /** Switch of watermark function */
  private boolean enableWatermark = false;

  /** Secret key for watermark */
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/conf/IoTDBConfig.java`
#### Snippet
```java

  /** the minimum size (in bytes) of segment inside a schema file page */
  private short minimumSegmentInSchemaFile = 0;

  /** cache size for pages in one schema file */
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/conf/IoTDBConfig.java`
#### Snippet
```java

  /** Set true to enable statistics monitor service, false to disable statistics service. */
  private boolean enableStatMonitor = false;

  /** Set true to enable writing monitor time series. */
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/conf/IoTDBConfig.java`
#### Snippet
```java

  /** whether use kerberos to authenticate hdfs */
  private boolean useKerberos = false;

  /** full path of kerberos keytab file */
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/conf/IoTDBConfig.java`
#### Snippet
```java

  /** the max time to live of a session in ms. Unit: millisecond */
  private int sessionTimeoutThreshold = 0;

  /** Replace implementation class of JDBC service */
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/conf/IoTDBConfig.java`
#### Snippet
```java

  /** Max number of wal nodes, each node corresponds to one wal directory */
  private int maxWalNodesNum = 0;

  /** Duration a wal flush operation will wait before calling fsync. Unit: millisecond */
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/conf/IoTDBConfig.java`
#### Snippet
```java

  /** Maximum MemTable number. Invalid when enableMemControl is true. */
  private int maxMemtableNumber = 0;

  /** The amount of data iterate each time in server */
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/conf/IoTDBConfig.java`
#### Snippet
```java

  /** whether to use thrift compression. */
  private boolean rpcThriftCompressionEnable = false;

  /** whether to use Snappy compression before sending data through the network */
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0L` is redundant
in `server/src/main/java/org/apache/iotdb/db/conf/IoTDBConfig.java`
#### Snippet
```java
   * proper merge candidates can be found). Unit: second.
   */
  private long mergeIntervalSec = 0L;

  /** The limit of compaction merge can reach per second */
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/conf/IoTDBConfig.java`
#### Snippet
```java

  /** Strategy of multiple directories. */
  private String multiDirStrategyClassName = null;

  private String ratisDataRegionSnapshotDir =
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/conf/IoTDBConfig.java`
#### Snippet
```java

  /** whether to use Snappy compression before sending data through the network */
  private boolean rpcAdvancedCompressionEnable = false;

  /** Port which the JDBC server listens to. */
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/sync/externalpipe/ExtPipePluginRegister.java`
#### Snippet
```java

  private static class ExtPipePluginRegisterHolder {
    private static ExtPipePluginRegister INSTANCE = null;

    static {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/sync/externalpipe/ExtPipePluginManager.java`
#### Snippet
```java
  private ExecutorService monitorService =
      IoTDBThreadPoolFactory.newFixedThreadPool(1, "ExtPipePluginManager-monitor");
  boolean alive = false;

  private long lastPipeDataSerialNumber = Long.MIN_VALUE;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0L` is redundant
in `server/src/main/java/org/apache/iotdb/db/sync/externalpipe/ExtPipePluginManager.java`
#### Snippet
```java
  // condition-lock of commit and also record commit number
  private byte[] commitTriggerLocker = new byte[0];
  private long commitTriggerCounter = 0L;

  public ExtPipePluginManager(TsFilePipe tsFilePipe) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/TsFileOpBlock.java`
#### Snippet
```java
    public long chunkOffsetInFile = -1;
    public long startIndexInFile = -1;
    public long pointCount = 0;
    public boolean isTimeAligned = false;
    public long timeChunkOffsetInFile = -1;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/TsFileOpBlock.java`
#### Snippet
```java
  private LRUCache<Long, List<TimeValuePair>> pageCache;

  private boolean dataReady = false;

  Decoder timeDecoder =
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/TsFileOpBlock.java`
#### Snippet
```java
    public long startIndexInFile = -1;
    public long pointCount = 0;
    public boolean isTimeAligned = false;
    public long timeChunkOffsetInFile = -1;
    public DeletionGroup.DeletedType deletedFlag = NO_DELETED;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/query/reader/series/SeriesRawDataBatchReader.java`
#### Snippet
```java

  private BatchData batchData;
  private boolean hasCachedBatchData = false;

  public SeriesRawDataBatchReader(SeriesReader seriesReader) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/query/reader/series/AlignedSeriesAggregateReader.java`
#### Snippet
```java
   * if we have more sub series in one loop. And use nextSeries() method to move to next sub series.
   */
  private int curIndex = 0;

  private final int subSensorSize;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/query/reader/universal/CachedPriorityMergeReader.java`
#### Snippet
```java
  private int cacheIdx = 0;

  private Long lastTimestamp = null;

  public CachedPriorityMergeReader(TSDataType dataType) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/query/reader/universal/CachedPriorityMergeReader.java`
#### Snippet
```java
  private TimeValuePair[] timeValuePairCache = new TimeValuePair[CACHE_SIZE];
  private int cacheLimit = 0;
  private int cacheIdx = 0;

  private Long lastTimestamp = null;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/query/reader/universal/CachedPriorityMergeReader.java`
#### Snippet
```java

  private TimeValuePair[] timeValuePairCache = new TimeValuePair[CACHE_SIZE];
  private int cacheLimit = 0;
  private int cacheIdx = 0;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/sync/externalpipe/ExtPipePlugin.java`
#### Snippet
```java
  private ExtPipePluginConfiguration configuration;

  private volatile boolean alive = false;
  // ArrayList: save DataTransmissionTasks' info
  private List<DataTransmissionTask> dataTransmissionTasks;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0L` is redundant
in `server/src/main/java/org/apache/iotdb/db/sync/externalpipe/ExtPipePlugin.java`
#### Snippet
```java
    // condition-lock for notifying new data arriving
    private byte[] newDataLocker = new byte[0];
    private long newDataCounter = 0L;

    DataTransmissionTask(
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/query/context/QueryContext.java`
#### Snippet
```java

  private boolean debug;
  private boolean enableTracing = false;

  /**
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/query/context/QueryContext.java`
#### Snippet
```java
  private long timeout;

  private volatile boolean isInterrupted = false;

  public QueryContext() {}
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `session/src/main/java/org/apache/iotdb/session/pool/SessionPool.java`
#### Snippet
```java
  private final ConcurrentMap<Session, Session> occupied = new ConcurrentHashMap<>();
  private int size = 0;
  private int maxSize = 0;
  private final long waitToGetSessionTimeoutInMs;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `session/src/main/java/org/apache/iotdb/session/pool/SessionPool.java`
#### Snippet
```java
  // for session whose resultSet is not released.
  private final ConcurrentMap<Session, Session> occupied = new ConcurrentHashMap<>();
  private int size = 0;
  private int maxSize = 0;
  private final long waitToGetSessionTimeoutInMs;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `session/src/main/java/org/apache/iotdb/session/pool/SessionPool.java`
#### Snippet
```java
  private ZoneId zoneId;
  private boolean enableRedirection;
  private boolean enableQueryRedirection = false;

  private int thriftDefaultBufferSize;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `session/src/main/java/org/apache/iotdb/session/pool/SessionPool.java`
#### Snippet
```java
    private String host = SessionConfig.DEFAULT_HOST;
    private int port = SessionConfig.DEFAULT_PORT;
    private List<String> nodeUrls = null;
    private int maxSize = SessionConfig.DEFAULT_SESSION_POOL_MAX_SIZE;
    private String user = SessionConfig.DEFAULT_USER;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `session/src/main/java/org/apache/iotdb/session/pool/SessionPool.java`
#### Snippet
```java
    private int thriftDefaultBufferSize = SessionConfig.DEFAULT_INITIAL_BUFFER_CAPACITY;
    private int thriftMaxFrameSize = SessionConfig.DEFAULT_MAX_FRAME_SIZE;
    private boolean enableCompression = false;
    private ZoneId zoneId = null;
    private boolean enableRedirection = SessionConfig.DEFAULT_REDIRECTION_MODE;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `session/src/main/java/org/apache/iotdb/session/pool/SessionPool.java`
#### Snippet
```java
    private int thriftMaxFrameSize = SessionConfig.DEFAULT_MAX_FRAME_SIZE;
    private boolean enableCompression = false;
    private ZoneId zoneId = null;
    private boolean enableRedirection = SessionConfig.DEFAULT_REDIRECTION_MODE;
    private int connectionTimeoutInMs = SessionConfig.DEFAULT_CONNECTION_TIMEOUT_MS;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/query/control/clientsession/IClientSession.java`
#### Snippet
```java
  private String username;

  private boolean login = false;

  private long logInTime;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/query/control/tracing/TracingInfo.java`
#### Snippet
```java

  private int totalPageNum = 0;
  private int overlappedPageNum = 0;

  private final List<Pair<String, Long>> activityList = new ArrayList<>();
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/query/control/tracing/TracingInfo.java`
#### Snippet
```java
  private long startTime;

  private int seriesPathNum = 0;

  private final Set<TsFileResource> seqFileSet = new HashSet<>();
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/query/control/tracing/TracingInfo.java`
#### Snippet
```java
  private long unsequenceChunkPointNum = 0;

  private int totalPageNum = 0;
  private int overlappedPageNum = 0;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/query/control/tracing/TracingInfo.java`
#### Snippet
```java
  private int sequenceChunkNum = 0;
  private long sequenceChunkPointNum = 0;
  private int unsequenceChunkNum = 0;
  private long unsequenceChunkPointNum = 0;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/query/control/tracing/TracingInfo.java`
#### Snippet
```java

  private int sequenceChunkNum = 0;
  private long sequenceChunkPointNum = 0;
  private int unsequenceChunkNum = 0;
  private long unsequenceChunkPointNum = 0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/query/control/tracing/TracingInfo.java`
#### Snippet
```java
  private final Set<TsFileResource> unSeqFileSet = new HashSet<>();

  private int sequenceChunkNum = 0;
  private long sequenceChunkPointNum = 0;
  private int unsequenceChunkNum = 0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/query/control/tracing/TracingInfo.java`
#### Snippet
```java
  private long sequenceChunkPointNum = 0;
  private int unsequenceChunkNum = 0;
  private long unsequenceChunkPointNum = 0;

  private int totalPageNum = 0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/query/dataset/SingleDataSet.java`
#### Snippet
```java

  private RowRecord record;
  private int i = 0;

  public SingleDataSet(List<PartialPath> paths, List<TSDataType> dataTypes) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/query/dataset/ShowDataSet.java`
#### Snippet
```java
  protected ShowPlan plan;
  private List<RowRecord> result = new ArrayList<>();
  private int index = 0;
  protected boolean hasLimit;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/query/dataset/ListDataSet.java`
#### Snippet
```java

  private final List<RowRecord> records = new ArrayList<>();
  private int index = 0;

  public ListDataSet(List<PartialPath> paths, List<TSDataType> dataTypes) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/query/dataset/AlignByDeviceDataSet.java`
#### Snippet
```java
  private String currentDevice;
  private List<String> executeColumns;
  private int pathsNum = 0;

  public AlignByDeviceDataSet(
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/query/dataset/NonAlignEngineDataSet.java`
#### Snippet
```java
  private BlockingQueue<Pair<ByteBuffer, ByteBuffer>>[] blockingQueueArray;

  private boolean initialized = false;

  private AtomicIntegerArray offsetArray;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/query/dataset/NonAlignEngineDataSet.java`
#### Snippet
```java
  private final long queryId;
  /** flag that main thread is interrupted or not */
  private volatile boolean interrupted = false;

  private static final RawQueryReadTaskPoolManager pool = RawQueryReadTaskPoolManager.getInstance();
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/query/dataset/groupby/GroupByLevelDataSet.java`
#### Snippet
```java
  private static final Logger logger = LoggerFactory.getLogger(GroupByLevelDataSet.class);
  private List<RowRecord> records = new ArrayList<>();
  private int index = 0;

  public GroupByLevelDataSet(GroupByTimePlan plan, GroupByTimeEngineDataSet dataSet)
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/query/dataset/groupby/GroupByTimeDataSet.java`
#### Snippet
```java
  protected boolean leftCRightO;
  protected boolean isIntervalByMonth = false;
  protected boolean isSlidingStepByMonth = false;

  ITimeRangeIterator aggrWindowIterator;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/query/dataset/groupby/GroupByTimeDataSet.java`
#### Snippet
```java

  protected boolean leftCRightO;
  protected boolean isIntervalByMonth = false;
  protected boolean isSlidingStepByMonth = false;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
  protected volatile Map<TEndPoint, SessionConnection> endPointToSessionConnection;

  protected boolean enableQueryRedirection = false;

  // The version number of the client which used for compatibility in the server
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
    private long timeOut = SessionConfig.DEFAULT_QUERY_TIME_OUT;

    private List<String> nodeUrls = null;

    public Builder host(String host) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
    private String password = SessionConfig.DEFAULT_PASSWORD;
    private int fetchSize = SessionConfig.DEFAULT_FETCH_SIZE;
    private ZoneId zoneId = null;
    private int thriftDefaultBufferSize = SessionConfig.DEFAULT_INITIAL_BUFFER_CAPACITY;
    private int thriftMaxFrameSize = SessionConfig.DEFAULT_MAX_FRAME_SIZE;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/query/executor/fill/IFill.java`
#### Snippet
```java
  protected TSDataType dataType;

  protected boolean isBeforeByMonth = false;
  protected long beforeRange = 0;
  protected boolean isAfterByMonth = false;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/query/executor/fill/IFill.java`
#### Snippet
```java
  protected boolean isBeforeByMonth = false;
  protected long beforeRange = 0;
  protected boolean isAfterByMonth = false;
  protected long afterRange = 0;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/query/executor/fill/IFill.java`
#### Snippet
```java

  protected boolean isBeforeByMonth = false;
  protected long beforeRange = 0;
  protected boolean isAfterByMonth = false;
  protected long afterRange = 0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/query/executor/fill/IFill.java`
#### Snippet
```java
  protected long beforeRange = 0;
  protected boolean isAfterByMonth = false;
  protected long afterRange = 0;

  public IFill(TSDataType dataType, long queryStartTime) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/query/aggregation/impl/AvgAggrResult.java`
#### Snippet
```java
  private TSDataType seriesDataType;
  private double avg = 0.0;
  private long cnt = 0;

  public AvgAggrResult(TSDataType seriesDataType) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0.0` is redundant
in `server/src/main/java/org/apache/iotdb/db/query/aggregation/impl/AvgAggrResult.java`
#### Snippet
```java

  private TSDataType seriesDataType;
  private double avg = 0.0;
  private long cnt = 0;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/query/externalsort/ExternalSortJobScheduler.java`
#### Snippet
```java
public class ExternalSortJobScheduler {

  private long queryId = 0;

  private ExternalSortJobScheduler() {}
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/tools/watermark/GroupedLSBWatermarkEncoder.java`
#### Snippet
```java
  private int groupNumber;
  private int maxBitPosition = 5;
  private int minBitPosition = 0;

  public GroupedLSBWatermarkEncoder(String secretKey, String bitString) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/utils/ValueIterator.java`
#### Snippet
```java
  // Object: TsPrimitiveType[] or common data type
  protected Object[] values;
  protected int curPos = 0;

  public ValueIterator(Object[] values) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/utils/OpenFileNumUtil.java`
#### Snippet
```java
  private static DirectoryManager directoryManager = DirectoryManager.getInstance();
  private static final String[] COMMAND_TEMPLATE = {"/bin/bash", "-c", ""};
  private static boolean isOutputValid = false;
  private int pid;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/utils/writelog/MultiFileLogReader.java`
#### Snippet
```java
  private SingleFileLogReader currentReader;
  private File[] files;
  private int fileIdx = 0;

  public MultiFileLogReader(File[] files) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/utils/writelog/SingleFileLogReader.java`
#### Snippet
```java
  private int idx;
  // used to truncate the broken logs
  private long unbrokenLogsSize = 0;

  private BatchLogReader batchLogReader;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/utils/writelog/SingleFileLogReader.java`
#### Snippet
```java
  private BatchLogReader batchLogReader;

  private boolean fileCorrupted = false;

  public SingleFileLogReader(File logFile) throws FileNotFoundException {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/utils/writelog/BatchLogReader.java`
#### Snippet
```java
  private Iterator<PhysicalPlan> planIterator;

  private boolean fileCorrupted = false;

  BatchLogReader(ByteBuffer buffer) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/utils/windowing/window/WindowImpl.java`
#### Snippet
```java
  private float[] floatValues = null;
  private double[] doubleValues = null;
  private boolean[] booleanValues = null;
  private Binary[] binaryValues = null;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/utils/windowing/window/WindowImpl.java`
#### Snippet
```java
  private int[] intValues = null;
  private long[] longValues = null;
  private float[] floatValues = null;
  private double[] doubleValues = null;
  private boolean[] booleanValues = null;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/utils/windowing/window/WindowImpl.java`
#### Snippet
```java
  private long[] timestamps = null;

  private int[] intValues = null;
  private long[] longValues = null;
  private float[] floatValues = null;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/utils/windowing/window/WindowImpl.java`
#### Snippet
```java
  private final TSDataType dataType;

  private long[] timestamps = null;

  private int[] intValues = null;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/utils/windowing/window/WindowImpl.java`
#### Snippet
```java
  private double[] doubleValues = null;
  private boolean[] booleanValues = null;
  private Binary[] binaryValues = null;

  public WindowImpl(EvictableBatchList list, int begin, int size) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/utils/windowing/window/WindowImpl.java`
#### Snippet
```java

  private int[] intValues = null;
  private long[] longValues = null;
  private float[] floatValues = null;
  private double[] doubleValues = null;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/utils/windowing/window/WindowImpl.java`
#### Snippet
```java
  private long[] longValues = null;
  private float[] floatValues = null;
  private double[] doubleValues = null;
  private boolean[] booleanValues = null;
  private Binary[] binaryValues = null;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/BackBooleanTVList.java`
#### Snippet
```java
  private final List<long[]> tmpTimestamps = new ArrayList<>();
  private final List<boolean[]> tmpValues = new ArrayList<>();
  private int tmpLength = 0;

  @Override
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/BackDoubleTVList.java`
#### Snippet
```java
  private final List<long[]> tmpTimestamps = new ArrayList<>();
  private final List<double[]> tmpValues = new ArrayList<>();
  private int tmpLength = 0;

  @Override
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/BackLongTVList.java`
#### Snippet
```java
  private final List<long[]> tmpTimestamps = new ArrayList<>();
  private final List<long[]> tmpValues = new ArrayList<>();
  private int tmpLength = 0;

  @Override
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/BackBinaryTVList.java`
#### Snippet
```java
  private final List<long[]> tmpTimestamps = new ArrayList<>();
  private final List<Binary[]> tmpValues = new ArrayList<>();
  private int tmpLength = 0;

  @Override
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/BackFloatTVList.java`
#### Snippet
```java
  private final List<long[]> tmpTimestamps = new ArrayList<>();
  private final List<float[]> tmpValues = new ArrayList<>();
  private int tmpLength = 0;

  @Override
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/BackIntTVList.java`
#### Snippet
```java
  private final List<long[]> tmpTimestamps = new ArrayList<>();
  private final List<int[]> tmpValues = new ArrayList<>();
  private int tmpLength = 0;

  @Override
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/BackAlignedTVList.java`
#### Snippet
```java
  private final List<long[]> tmpTimestamps = new ArrayList<>();
  private final List<int[]> tmpIndices = new ArrayList<>();
  private int tmpLength = 0;

  BackAlignedTVList(List<TSDataType> types) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/utils/timerangeiterator/PreAggrWindowIterator.java`
#### Snippet
```java
  private long curInterval;
  private long curSlidingStep;
  private boolean isIntervalCyclicChange = false;
  private int intervalCnt = 0;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/utils/timerangeiterator/PreAggrWindowIterator.java`
#### Snippet
```java
  private long curSlidingStep;
  private boolean isIntervalCyclicChange = false;
  private int intervalCnt = 0;

  private TimeRange curTimeRange;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/tools/validate/TsFileValidationTool.java`
#### Snippet
```java
  private static final List<File> seqDataDirList = new ArrayList<>();
  private static final List<File> fileList = new ArrayList<>();
  public static int badFileNum = 0;

  // measurementID -> <fileName, [lastTime, endTimeInLastFile]>
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/tools/validate/TsFileValidationTool.java`
#### Snippet
```java

  // print to local file or not
  private static boolean printToFile = false;

  private static String outFilePath = "TsFile_validation_view.txt";
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/tools/validate/TsFileValidationTool.java`
#### Snippet
```java
  private static String outFilePath = "TsFile_validation_view.txt";

  private static PrintWriter pw = null;

  private static final Logger logger = LoggerFactory.getLogger(TsFileValidationTool.class);
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/tools/validate/TsFileValidationTool.java`
#### Snippet
```java
public class TsFileValidationTool {
  // print detail type of overlap or not
  private static boolean printDetails = false;

  // print to local file or not
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0L` is redundant
in `server/src/main/java/org/apache/iotdb/db/engine/flush/MemTableFlushTask.java`
#### Snippet
```java

  private volatile long memSerializeTime = 0L;
  private volatile long ioTime = 0L;

  /**
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0L` is redundant
in `server/src/main/java/org/apache/iotdb/db/engine/flush/MemTableFlushTask.java`
#### Snippet
```java
  private IMemTable memTable;

  private volatile long memSerializeTime = 0L;
  private volatile long ioTime = 0L;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/engine/StorageEngineV2.java`
#### Snippet
```java
  private List<CloseFileListener> customCloseFileListeners = new ArrayList<>();
  private List<FlushListener> customFlushListeners = new ArrayList<>();
  private int recoverDataRegionNum = 0;

  private LoadTsFileManager loadTsFileManager = new LoadTsFileManager();
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/client/ConfigNodeClient.java`
#### Snippet
```java
  private TEndPoint configNode;

  private int cursor = 0;

  private final IoTDBConfig config = IoTDBDescriptor.getInstance().getConfig();
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0L` is redundant
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/task/CompactionTaskSummary.java`
#### Snippet
```java
/** The summary of one {@link AbstractCompactionTask} execution */
public class CompactionTaskSummary {
  private long timeCost = 0L;
  private volatile Status status = Status.NOT_STARTED;
  private long startTime = -1L;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/log/CompactionLogAnalyzer.java`
#### Snippet
```java
  private final List<TsFileIdentifier> sourceFileInfos = new ArrayList<>();
  private final List<TsFileIdentifier> targetFileInfos = new ArrayList<>();
  private boolean isLogFromOld = false;

  public CompactionLogAnalyzer(File logFile) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/CompactionTaskManager.java`
#### Snippet
```java

  private final IoTDBConfig config = IoTDBDescriptor.getInstance().getConfig();
  private volatile boolean init = false;

  public static CompactionTaskManager getInstance() {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/engine/memtable/AbstractMemTable.java`
#### Snippet
```java
   * including TEXT values
   */
  private long tvListRamCost = 0;

  private int seriesNumber = 0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/engine/memtable/AbstractMemTable.java`
#### Snippet
```java
  private int seriesNumber = 0;

  private long totalPointsNum = 0;

  private long totalPointsNumThreshold = 0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/engine/memtable/AbstractMemTable.java`
#### Snippet
```java
  private long tvListRamCost = 0;

  private int seriesNumber = 0;

  private long totalPointsNum = 0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/engine/memtable/AbstractMemTable.java`
#### Snippet
```java
  private long totalPointsNum = 0;

  private long totalPointsNumThreshold = 0;

  private long maxPlanIndex = Long.MIN_VALUE;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/engine/memtable/AbstractMemTable.java`
#### Snippet
```java
      IoTDBDescriptor.getInstance().getConfig().getAvgSeriesPointNumberThreshold();
  /** memory size of data points, including TEXT values */
  private long memSize = 0;
  /**
   * memory usage of all TVLists memory usage regardless of whether these TVLists are full,
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/engine/memtable/AbstractMemTable.java`
#### Snippet
```java
  protected boolean disableMemControl = true;

  private boolean shouldFlush = false;
  private volatile FlushStatus flushStatus = FlushStatus.WORKING;
  private final int avgSeriesPointNumThreshold =
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/cross/CrossSpaceCompactionTask.java`
#### Snippet
```java
  protected List<TsFileResource> holdWriteLockList = new ArrayList<>();
  protected double selectedSeqFileSize = 0;
  protected double selectedUnseqFileSize = 0;
  protected long memoryCost = 0L;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/cross/CrossSpaceCompactionTask.java`
#### Snippet
```java
  protected List<TsFileResource> holdReadLockList = new ArrayList<>();
  protected List<TsFileResource> holdWriteLockList = new ArrayList<>();
  protected double selectedSeqFileSize = 0;
  protected double selectedUnseqFileSize = 0;
  protected long memoryCost = 0L;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0L` is redundant
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/cross/CrossSpaceCompactionTask.java`
#### Snippet
```java
  protected double selectedSeqFileSize = 0;
  protected double selectedUnseqFileSize = 0;
  protected long memoryCost = 0L;

  public CrossSpaceCompactionTask(
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/reader/SeriesDataBlockReader.java`
#### Snippet
```java
  private TsBlock tsBlock;

  private boolean hasCachedBatchData = false;

  public SeriesDataBlockReader(
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0L` is redundant
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/inner/utils/AlignedSeriesCompactionExecutor.java`
#### Snippet
```java
  private final AlignedChunkWriterImpl chunkWriter;
  private final List<IMeasurementSchema> schemaList;
  private long remainingPointInChunkWriter = 0L;
  private final RateLimiter rateLimiter =
      CompactionTaskManager.getInstance().getMergeWriteRateLimiter();
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/inner/utils/SingleSeriesCompactionExecutor.java`
#### Snippet
```java
  private long minStartTimestamp = Long.MAX_VALUE;
  private long maxEndTimestamp = Long.MIN_VALUE;
  private long pointCountInChunkWriter = 0;

  private final long targetChunkSize =
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/engine/modification/utils/TracedBufferedReader.java`
#### Snippet
```java

  /** The skipLF flag when the mark was set */
  private boolean markedSkipLF = false;

  private static int defaultCharBufferSize = 8192;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/engine/modification/utils/TracedBufferedReader.java`
#### Snippet
```java

  /** the total bytes number already filled into cb */
  private long totalFilledBytesNum = 0;

  /**
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/engine/modification/utils/TracedBufferedReader.java`
#### Snippet
```java
  private static final int UNMARKED = -1;
  private int markedChar = UNMARKED;
  private int readAheadLimit = 0; /* Valid only when markedChar > 0 */

  /** If the next character is a line feed, skip it */
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/engine/modification/utils/TracedBufferedReader.java`
#### Snippet
```java
    Iterator<String> iter =
        new Iterator<String>() {
          String nextLine = null;

          @Override
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/engine/modification/utils/TracedBufferedReader.java`
#### Snippet
```java

  /** If the next character is a line feed, skip it */
  private boolean skipLF = false;

  /** The skipLF flag when the mark was set */
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/inner/utils/MultiTsFileDeviceIterator.java`
#### Snippet
```java
    private Map<TsFileResource, TsFileSequenceReader> readerMap;
    private String device;
    private String currentCompactingSeries = null;
    private LinkedList<String> seriesInThisIteration = new LinkedList<>();
    // tsfile sequence reader -> series -> list<ChunkMetadata>
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/inner/utils/MultiTsFileDeviceIterator.java`
#### Snippet
```java
  private final Map<TsFileResource, TsFileDeviceIterator> deviceIteratorMap = new HashMap<>();
  private final Map<TsFileResource, List<Modification>> modificationCache = new HashMap<>();
  private Pair<String, Boolean> currentDevice = null;

  /** Used for inner space compaction. */
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileResourceList.java`
#### Snippet
```java
  private TsFileResource tail;
  private final ReentrantReadWriteLock lock = new ReentrantReadWriteLock();
  private int count = 0;

  /**
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileResourceList.java`
#### Snippet
```java
  private class TsFileIterator implements Iterator<TsFileResource> {
    List<TsFileResource> tsFileResourceList;
    int currentIndex = 0;

    public TsFileIterator() {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TimePartitionManager.java`
#### Snippet
```java
  final Map<DataRegionId, Map<Long, TimePartitionInfo>> timePartitionInfoMap;

  long memCost = 0;
  long timePartitionInfoMemoryThreshold =
      IoTDBDescriptor.getInstance().getConfig().getAllocateMemoryForTimePartitionInfo();
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/rescon/MemTableManager.java`
#### Snippet
```java
  private static final int WAIT_TIME = 100;
  public static final int MEMTABLE_NUM_FOR_EACH_PARTITION = 4;
  private int currentMemtableNumber = 0;

  private MemTableManager() {}
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/rescon/SystemInfo.java`
#### Snippet
```java

  private long totalStorageGroupMemCost = 0L;
  private volatile boolean rejected = false;

  private long memorySizeForMemtable;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0L` is redundant
in `server/src/main/java/org/apache/iotdb/db/rescon/SystemInfo.java`
#### Snippet
```java
  private Map<DataRegionInfo, Long> reportedStorageGroupMemCostMap = new HashMap<>();

  private long flushingMemTablesCost = 0L;
  private AtomicLong compactionMemoryCost = new AtomicLong(0L);

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0L` is redundant
in `server/src/main/java/org/apache/iotdb/db/rescon/SystemInfo.java`
#### Snippet
```java
  private static final Logger logger = LoggerFactory.getLogger(SystemInfo.class);

  private long totalStorageGroupMemCost = 0L;
  private volatile boolean rejected = false;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/service/thrift/handler/BaseServerContextHandler.java`
#### Snippet
```java

public class BaseServerContextHandler {
  private static ServerContextFactory factory = null;
  private static final Logger logger = LoggerFactory.getLogger(BaseServerContextHandler.class);

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0L` is redundant
in `server/src/main/java/org/apache/iotdb/db/service/metrics/FileMetrics.java`
#### Snippet
```java
  private long sequenceFileTotalCount = 0L;
  private long unsequenceFileTotalSize = 0L;
  private long unsequenceFileTotalCount = 0L;

  @Override
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0L` is redundant
in `server/src/main/java/org/apache/iotdb/db/service/metrics/FileMetrics.java`
#### Snippet
```java
  private long walFileTotalCount = 0L;
  private long sequenceFileTotalSize = 0L;
  private long sequenceFileTotalCount = 0L;
  private long unsequenceFileTotalSize = 0L;
  private long unsequenceFileTotalCount = 0L;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0L` is redundant
in `server/src/main/java/org/apache/iotdb/db/service/metrics/FileMetrics.java`
#### Snippet
```java
  private Future<?> currentServiceFuture;
  private final ScheduledExecutorService service = Executors.newSingleThreadScheduledExecutor();
  private long walFileTotalSize = 0L;
  private long walFileTotalCount = 0L;
  private long sequenceFileTotalSize = 0L;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0L` is redundant
in `server/src/main/java/org/apache/iotdb/db/service/metrics/FileMetrics.java`
#### Snippet
```java
  private long sequenceFileTotalSize = 0L;
  private long sequenceFileTotalCount = 0L;
  private long unsequenceFileTotalSize = 0L;
  private long unsequenceFileTotalCount = 0L;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0L` is redundant
in `server/src/main/java/org/apache/iotdb/db/service/metrics/FileMetrics.java`
#### Snippet
```java
  private final ScheduledExecutorService service = Executors.newSingleThreadScheduledExecutor();
  private long walFileTotalSize = 0L;
  private long walFileTotalCount = 0L;
  private long sequenceFileTotalSize = 0L;
  private long sequenceFileTotalCount = 0L;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0L` is redundant
in `server/src/main/java/org/apache/iotdb/db/service/metrics/FileMetrics.java`
#### Snippet
```java
  private long walFileTotalSize = 0L;
  private long walFileTotalCount = 0L;
  private long sequenceFileTotalSize = 0L;
  private long sequenceFileTotalCount = 0L;
  private long unsequenceFileTotalSize = 0L;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0L` is redundant
in `server/src/main/java/org/apache/iotdb/db/service/metrics/SystemMetrics.java`
#### Snippet
```java
  private boolean isDataNode = false;
  private long systemDiskTotalSpace = 0L;
  private long systemDiskFreeSpace = 0L;

  public SystemMetrics(boolean isDataNode) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/service/metrics/SystemMetrics.java`
#### Snippet
```java
  private final ScheduledExecutorService service = Executors.newSingleThreadScheduledExecutor();
  private final Set<FileStore> fileStores = new HashSet<>();
  private boolean isDataNode = false;
  private long systemDiskTotalSpace = 0L;
  private long systemDiskFreeSpace = 0L;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0L` is redundant
in `server/src/main/java/org/apache/iotdb/db/service/metrics/SystemMetrics.java`
#### Snippet
```java
  private final Set<FileStore> fileStores = new HashSet<>();
  private boolean isDataNode = false;
  private long systemDiskTotalSpace = 0L;
  private long systemDiskFreeSpace = 0L;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileResource.java`
#### Snippet
```java
  protected long minPlanIndex = Long.MAX_VALUE;

  private long version = 0;

  private long ramSize;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/plan/schemaregion/impl/CreateTimeSeriesPlanImpl.java`
#### Snippet
```java
  private CompressionType compressor;
  private String alias;
  private Map<String, String> props = null;
  private Map<String, String> tags = null;
  private Map<String, String> attributes = null;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/plan/schemaregion/impl/CreateTimeSeriesPlanImpl.java`
#### Snippet
```java
  private String alias;
  private Map<String, String> props = null;
  private Map<String, String> tags = null;
  private Map<String, String> attributes = null;
  private long tagOffset = -1;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/plan/schemaregion/impl/CreateTimeSeriesPlanImpl.java`
#### Snippet
```java
  private Map<String, String> props = null;
  private Map<String, String> tags = null;
  private Map<String, String> attributes = null;
  private long tagOffset = -1;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/plan/schemaregion/impl/CreateAlignedTimeSeriesPlanImpl.java`
#### Snippet
```java
  private List<Map<String, String>> tagsList;
  private List<Map<String, String>> attributesList;
  private List<Long> tagOffsets = null;

  public CreateAlignedTimeSeriesPlanImpl() {}
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/cache/SchemaCacheEntry.java`
#### Snippet
```java
  private final boolean isAligned;

  private volatile ILastCacheContainer lastCacheContainer = null;

  SchemaCacheEntry(
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/mnode/InternalMNode.java`
#### Snippet
```java
  protected int schemaTemplateId = NON_TEMPLATE;

  private volatile boolean useTemplate = false;

  /** Constructor of MNode. */
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/mnode/InternalMNode.java`
#### Snippet
```java
   */
  @SuppressWarnings("squid:S3077")
  protected transient volatile IMNodeContainer children = null;

  /**
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/mnode/EntityMNode.java`
#### Snippet
```java
  private transient volatile Map<String, IMeasurementMNode> aliasChildren = null;

  private volatile boolean isAligned = false;

  @Override
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/mnode/EntityMNode.java`
#### Snippet
```java
   */
  @SuppressWarnings("squid:S3077")
  private transient volatile Map<String, IMeasurementMNode> aliasChildren = null;

  private volatile boolean isAligned = false;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/mnode/MeasurementMNode.java`
#### Snippet
```java
  private IMeasurementSchema schema;
  /** whether this measurement is pre deleted and considered in black list */
  private boolean preDeleted = false;

  /**
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/CachedMTreeStore.java`
#### Snippet
```java
  private final MTreeReleaseTaskManager releaseTaskManager = MTreeReleaseTaskManager.getInstance();
  private volatile boolean hasReleaseTask;
  private int releaseCount = 0;

  private final ReadWriteLock readWriteLock =
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/CachedMTreeStore.java`
#### Snippet
```java

  private final MTreeFlushTaskManager flushTaskManager = MTreeFlushTaskManager.getInstance();
  private int flushCount = 0;
  private volatile boolean hasFlushTask;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/cache/LRUCacheManager.java`
#### Snippet
```java
    private volatile LRUCacheEntry pre = null;

    private volatile LRUCacheEntry next = null;

    public LRUCacheEntry(IMNode node) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/cache/LRUCacheManager.java`
#### Snippet
```java
    protected volatile IMNode node;

    private volatile LRUCacheEntry pre = null;

    private volatile LRUCacheEntry next = null;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/cache/CacheEntry.java`
#### Snippet
```java
public class CacheEntry {

  private volatile boolean isVolatile = false;

  private final AtomicInteger semaphore = new AtomicInteger();
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/DataRegion.java`
#### Snippet
```java
  private final Condition deletedCondition = insertLock.writeLock().newCondition();
  /** data region has been deleted or not */
  private volatile boolean deleted = false;
  /** closeStorageGroupCondition is used to wait for all currently closing TsFiles to be done. */
  private final Object closeStorageGroupCondition = new Object();
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/DataRegion.java`
#### Snippet
```java
  private DataRegionInfo dataRegionInfo = new DataRegionInfo(this);
  /** whether it's ready from recovery */
  private boolean isReady = false;
  /** close file listeners */
  private List<CloseFileListener> customCloseFileListeners = Collections.emptyList();
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/tag/TagLogFile.java`
#### Snippet
```java
  private static final int RECORD_FLUSH_INTERVAL =
      IoTDBDescriptor.getInstance().getConfig().getTagAttributeFlushInterval();
  private int unFlushedRecordNum = 0;

  public TagLogFile(String schemaDir, String logFileName) throws IOException {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/SchemaFileConfig.java`
#### Snippet
```java

  public static boolean DETAIL_SKETCH = true; // whether sketch leaf segment in detail
  public static int INTERNAL_SPLIT_VALVE = 0; // internal segment split at this spare

  // endregion
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/MockSchemaFile.java`
#### Snippet
```java
  private IStorageGroupMNode storageGroupMNode;

  private long fileTail = 0;
  private final Map<Long, Map<String, IMNode>> mockFile = new HashMap<>();

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/SchemaPage.java`
#### Snippet
```java
  protected short memberNum; // amount of the member, definition depends on implementation

  protected volatile boolean dirtyFlag = false; // any modification turns it true

  protected SchemaPage(ByteBuffer pageBuffer) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/AliasIndexPage.java`
#### Snippet
```java
  private static final int OFFSET_LEN = 2;
  long nextPage;
  String penultKey = null, lastKey = null;

  /**
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/AliasIndexPage.java`
#### Snippet
```java
  private static final int OFFSET_LEN = 2;
  long nextPage;
  String penultKey = null, lastKey = null;

  /**
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/CachedMNodeContainer.java`
#### Snippet
```java

    Iterator<IMNode> iterator;
    byte status = 0;

    CachedMNodeContainerIterator() {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/CachedMNodeContainer.java`
#### Snippet
```java
  private long segmentAddress = -1;

  private Map<String, IMNode> childCache = null;
  private Map<String, IMNode> newChildBuffer = null;
  private Map<String, IMNode> updatedChildBuffer = null;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/CachedMNodeContainer.java`
#### Snippet
```java
    Iterator<IMNode> newBufferIterator;
    Iterator<IMNode> updateBufferIterator;
    byte status = 0;

    BufferIterator() {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/CachedMNodeContainer.java`
#### Snippet
```java
  private Map<String, IMNode> childCache = null;
  private Map<String, IMNode> newChildBuffer = null;
  private Map<String, IMNode> updatedChildBuffer = null;

  @Override
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/CachedMNodeContainer.java`
#### Snippet
```java

  private Map<String, IMNode> childCache = null;
  private Map<String, IMNode> newChildBuffer = null;
  private Map<String, IMNode> updatedChildBuffer = null;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/traverser/counter/MeasurementGroupByLevelCounter.java`
#### Snippet
```java
  private Map<PartialPath, Long> result = new HashMap<>();
  private List<String> timeseries = new ArrayList<>();
  private boolean hasTag = false;

  // path representing current branch and matching the pattern and level
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/Segment.java`
#### Snippet
```java

  // assess monotonic
  protected String penuKey = null, lastKey = null;

  /**
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/Segment.java`
#### Snippet
```java

  // assess monotonic
  protected String penuKey = null, lastKey = null;

  /**
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/traverser/counter/MeasurementCounter.java`
#### Snippet
```java

  private List<String> timeseries = new ArrayList<>();
  private boolean hasTag = false;

  public MeasurementCounter(IMNode startNode, PartialPath path, IMTreeStore store)
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/traverser/Traverser.java`
#### Snippet
```java

  // if true, measurement in template should be processed
  protected boolean shouldTraverseTemplate = false;
  protected Map<Integer, Template> templateMap;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/traverser/Traverser.java`
#### Snippet
```java

  // default false means fullPath pattern match
  protected boolean isPrefixMatch = false;

  /**
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/traverser/Traverser.java`
#### Snippet
```java

  // if true, the pre deleted measurement or pre deactivated template won't be processed
  protected boolean skipPreDeletedSchema = false;

  // default false means fullPath pattern match
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/traverser/Traverser.java`
#### Snippet
```java
  protected Deque<IMNode> traverseContext;

  protected boolean isInTemplate = false;

  // if true, measurement in template should be processed
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/traverser/Traverser.java`
#### Snippet
```java
  protected int startIndex;
  protected int startLevel;
  protected boolean isPrefixStart = false;

  // to construct full path or find mounted node on MTree when traverse into template
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/traverser/collector/StorageGroupCollector.java`
#### Snippet
```java
public abstract class StorageGroupCollector<T> extends CollectorTraverser<T> {

  protected boolean collectInternal = false;

  public StorageGroupCollector(IMNode startNode, PartialPath path, IMTreeStore store)
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/traverser/collector/CollectorTraverser.java`
#### Snippet
```java

  protected boolean hasLimit = false;
  protected int count = 0;
  protected int curOffset = -1;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/traverser/collector/CollectorTraverser.java`
#### Snippet
```java
  protected int offset;

  protected boolean hasLimit = false;
  protected int count = 0;
  protected int curOffset = -1;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/logfile/SchemaLogReader.java`
#### Snippet
```java
  private T nextSchemaPlan;

  private long currentIndex = 0;
  private boolean isFileCorrupted = false;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/logfile/SchemaLogReader.java`
#### Snippet
```java

  private long currentIndex = 0;
  private boolean isFileCorrupted = false;

  public SchemaLogReader(String schemaDir, String logFileName, IDeserializer<T> deserializer)
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/logfile/SchemaLogReader.java`
#### Snippet
```java
    private final InputStream inputStream;

    private long readBytes = 0;

    private long mark;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/RSchemaRegionLoader.java`
#### Snippet
```java
public class RSchemaRegionLoader {
  private static final Logger LOGGER = LoggerFactory.getLogger(RSchemaRegionLoader.class);
  private static URLClassLoader urlClassLoader = null;
  private static final String RSCHEMA_REGION_CLASS_NAME =
      "org.apache.iotdb.db.metadata.schemaregion.rocksdb.RSchemaRegion";
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/SchemaEngine.java`
#### Snippet
```java

  // seriesNumberMonitor may be null
  private ISeriesNumerMonitor seriesNumerMonitor = null;

  public TSStatus write(SchemaRegionId schemaRegionId, PlanNode planNode) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/function/selector/InfluxSelector.java`
#### Snippet
```java

  // The timestamp corresponding to the value
  private Long timestamp = null;

  private List<Object> relatedValues;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/function/selector/InfluxMaxFunction.java`
#### Snippet
```java
  private String stringValue = null;
  private boolean isNumber = false;
  private boolean isString = false;

  private Double maxValue = null;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/function/selector/InfluxMaxFunction.java`
#### Snippet
```java
  private Double doubleValue = Double.MIN_VALUE;
  private String stringValue = null;
  private boolean isNumber = false;
  private boolean isString = false;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/function/selector/InfluxMaxFunction.java`
#### Snippet
```java
  private boolean isString = false;

  private Double maxValue = null;

  public InfluxMaxFunction(List<Expression> expressionList) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/function/selector/InfluxMaxFunction.java`
#### Snippet
```java
public class InfluxMaxFunction extends InfluxSelector {
  private Double doubleValue = Double.MIN_VALUE;
  private String stringValue = null;
  private boolean isNumber = false;
  private boolean isString = false;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/function/selector/InfluxMinFunction.java`
#### Snippet
```java
  private boolean isString = false;

  private Double minValue = null;

  public InfluxMinFunction(List<Expression> expressionList) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/function/selector/InfluxMinFunction.java`
#### Snippet
```java
  private Double doubleValue = Double.MAX_VALUE;
  private String stringValue = null;
  private boolean isNumber = false;
  private boolean isString = false;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/function/selector/InfluxMinFunction.java`
#### Snippet
```java
public class InfluxMinFunction extends InfluxSelector {
  private Double doubleValue = Double.MAX_VALUE;
  private String stringValue = null;
  private boolean isNumber = false;
  private boolean isString = false;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/function/selector/InfluxMinFunction.java`
#### Snippet
```java
  private String stringValue = null;
  private boolean isNumber = false;
  private boolean isString = false;

  private Double minValue = null;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/function/aggregator/InfluxModeFunction.java`
#### Snippet
```java
  private final Map<Object, Integer> valueOrders = new HashMap<>();
  private final Map<Object, Long> valueLastTimestamp = new HashMap<>();
  private int maxNumber = 0;
  private Object maxObject = null;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/function/aggregator/InfluxModeFunction.java`
#### Snippet
```java
  private final Map<Object, Long> valueLastTimestamp = new HashMap<>();
  private int maxNumber = 0;
  private Object maxObject = null;

  public InfluxModeFunction(List<Expression> expressionList) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/function/aggregator/InfluxCountFunction.java`
#### Snippet
```java

public class InfluxCountFunction extends InfluxAggregator {
  private int countNum = 0;

  public InfluxCountFunction(List<Expression> expressionList) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/function/aggregator/InfluxSpreadFunction.java`
#### Snippet
```java

public class InfluxSpreadFunction extends InfluxAggregator {
  private Double maxNum = null;
  private Double minNum = null;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/function/aggregator/InfluxSpreadFunction.java`
#### Snippet
```java
public class InfluxSpreadFunction extends InfluxAggregator {
  private Double maxNum = null;
  private Double minNum = null;

  public InfluxSpreadFunction(List<Expression> expressionList) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/operator/InfluxSelectComponent.java`
#### Snippet
```java
  private boolean hasMoreSelectorFunction = false;
  private boolean hasMoreFunction = false;
  private boolean hasFunction = false;
  private boolean hasCommonQuery = false;
  private boolean hasOnlyTraverseFunction = false;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/operator/InfluxSelectComponent.java`
#### Snippet
```java
  private boolean hasSelectorFunction = false;
  private boolean hasMoreSelectorFunction = false;
  private boolean hasMoreFunction = false;
  private boolean hasFunction = false;
  private boolean hasCommonQuery = false;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/operator/InfluxSelectComponent.java`
#### Snippet
```java
    extends org.apache.iotdb.db.qp.logical.crud.SelectComponent {

  private boolean hasAggregationFunction = false;
  private boolean hasSelectorFunction = false;
  private boolean hasMoreSelectorFunction = false;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/operator/InfluxSelectComponent.java`
#### Snippet
```java
  private boolean hasMoreFunction = false;
  private boolean hasFunction = false;
  private boolean hasCommonQuery = false;
  private boolean hasOnlyTraverseFunction = false;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/operator/InfluxSelectComponent.java`
#### Snippet
```java
  private boolean hasFunction = false;
  private boolean hasCommonQuery = false;
  private boolean hasOnlyTraverseFunction = false;

  public InfluxSelectComponent() {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/operator/InfluxSelectComponent.java`
#### Snippet
```java

  private boolean hasAggregationFunction = false;
  private boolean hasSelectorFunction = false;
  private boolean hasMoreSelectorFunction = false;
  private boolean hasMoreFunction = false;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/operator/InfluxSelectComponent.java`
#### Snippet
```java
  private boolean hasAggregationFunction = false;
  private boolean hasSelectorFunction = false;
  private boolean hasMoreSelectorFunction = false;
  private boolean hasMoreFunction = false;
  private boolean hasFunction = false;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/function/aggregator/InfluxMeanFunction.java`
#### Snippet
```java
  private final List<Double> numbers = new ArrayList<>();
  double sum = 0;
  long count = 0;

  public InfluxMeanFunction(List<Expression> expressionList) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/function/aggregator/InfluxMeanFunction.java`
#### Snippet
```java
public class InfluxMeanFunction extends InfluxAggregator {
  private final List<Double> numbers = new ArrayList<>();
  double sum = 0;
  long count = 0;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/function/aggregator/InfluxSumFunction.java`
#### Snippet
```java
public class InfluxSumFunction extends InfluxAggregator {
  private final List<Double> numbers = new ArrayList<>();
  private long sum = 0;

  public InfluxSumFunction(List<Expression> expressionList) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/consensus/DataRegionConsensusImpl.java`
#### Snippet
```java
  private static final IoTDBConfig conf = IoTDBDescriptor.getInstance().getConfig();

  private static IConsensus INSTANCE = null;

  private DataRegionConsensusImpl() {}
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `server/src/main/java/org/apache/iotdb/db/consensus/SchemaRegionConsensusImpl.java`
#### Snippet
```java
  private static final IoTDBConfig conf = IoTDBDescriptor.getInstance().getConfig();

  private static IConsensus INSTANCE = null;

  private SchemaRegionConsensusImpl() {}
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/SchemaRegionMemoryImpl.java`
#### Snippet
```java

  private boolean isRecovering = true;
  private volatile boolean initialized = false;

  private String storageGroupDirPath;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0.0` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/dmatch/UDAFCov.java`
#### Snippet
```java
  private double sum_x = 0.0;
  private double sum_y = 0.0;
  private double sum_xy = 0.0;

  @Override
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/dmatch/UDAFCov.java`
#### Snippet
```java
public class UDAFCov implements UDTF {

  private long count = 0;
  private double sum_x = 0.0;
  private double sum_y = 0.0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0.0` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/dmatch/UDAFCov.java`
#### Snippet
```java

  private long count = 0;
  private double sum_x = 0.0;
  private double sum_y = 0.0;
  private double sum_xy = 0.0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0.0` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/dmatch/UDAFCov.java`
#### Snippet
```java
  private long count = 0;
  private double sum_x = 0.0;
  private double sum_y = 0.0;
  private double sum_xy = 0.0;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0.0` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/dmatch/UDAFPearson.java`
#### Snippet
```java
  private double sum_x = 0.0;
  private double sum_y = 0.0;
  private double sum_xy = 0.0;
  private double sum_xx = 0.0;
  private double sum_yy = 0.0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0.0` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/dmatch/UDAFPearson.java`
#### Snippet
```java
  private double sum_xy = 0.0;
  private double sum_xx = 0.0;
  private double sum_yy = 0.0;

  @Override
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0.0` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/dmatch/UDAFPearson.java`
#### Snippet
```java
  private double sum_y = 0.0;
  private double sum_xy = 0.0;
  private double sum_xx = 0.0;
  private double sum_yy = 0.0;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/dmatch/UDAFPearson.java`
#### Snippet
```java
public class UDAFPearson implements UDTF {

  private long count = 0;
  private double sum_x = 0.0;
  private double sum_y = 0.0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0.0` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/dmatch/UDAFPearson.java`
#### Snippet
```java
  private long count = 0;
  private double sum_x = 0.0;
  private double sum_y = 0.0;
  private double sum_xy = 0.0;
  private double sum_xx = 0.0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0.0` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/dmatch/UDAFPearson.java`
#### Snippet
```java

  private long count = 0;
  private double sum_x = 0.0;
  private double sum_y = 0.0;
  private double sum_xy = 0.0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/series/util/ConsecutiveUtil.java`
#### Snippet
```java
  private long last;
  private long gap;
  private int count = 0;
  private final ArrayList<Pair<Long, Boolean>> window = new ArrayList<>(maxLen);

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0.0` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/anomaly/UDTFKSigma.java`
#### Snippet
```java
public class UDTFKSigma implements UDTF {
  private double mean = 0.0;
  private double var = 0.0;
  private double sumX2 = 0.0;
  private double sumX1 = 0.0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/anomaly/UDTFKSigma.java`
#### Snippet
```java
  private double sumX1 = 0.0;
  private double multipleK;
  private int windowSize = 0;
  private CircularQueue<Object> v;
  private LongCircularQueue t;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0.0` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/anomaly/UDTFKSigma.java`
#### Snippet
```java
  private double var = 0.0;
  private double sumX2 = 0.0;
  private double sumX1 = 0.0;
  private double multipleK;
  private int windowSize = 0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0.0` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/anomaly/UDTFKSigma.java`
#### Snippet
```java
/** This function detects outliers which lies over average +/- k * sigma. */
public class UDTFKSigma implements UDTF {
  private double mean = 0.0;
  private double var = 0.0;
  private double sumX2 = 0.0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0.0` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/anomaly/UDTFKSigma.java`
#### Snippet
```java
  private double mean = 0.0;
  private double var = 0.0;
  private double sumX2 = 0.0;
  private double sumX1 = 0.0;
  private double multipleK;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0.0d` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/anomaly/UDTFIQR.java`
#### Snippet
```java
  double q1 = 0.0d;
  double q3 = 0.0d;
  double iqr = 0.0d;

  public void validate(UDFParameterValidator validator) throws Exception {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0.0d` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/anomaly/UDTFIQR.java`
#### Snippet
```java
  String compute = "batch";
  double q1 = 0.0d;
  double q3 = 0.0d;
  double iqr = 0.0d;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0.0d` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/anomaly/UDTFIQR.java`
#### Snippet
```java
  ArrayList<Long> timestamp = new ArrayList<>();
  String compute = "batch";
  double q1 = 0.0d;
  double q3 = 0.0d;
  double iqr = 0.0d;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/anomaly/util/MissDetector.java`
#### Snippet
```java
  private double lineBoundary = 0.6;
  private long startTime;
  private int windowCnt = 0;
  private double r2 = 0;
  private final ArrayList<MissingSubSeries> anomalyList = new ArrayList<>();
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/anomaly/util/MissDetector.java`
#### Snippet
```java
  private long startTime;
  private int windowCnt = 0;
  private double r2 = 0;
  private final ArrayList<MissingSubSeries> anomalyList = new ArrayList<>();

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/drepair/util/LsGreedy.java`
#### Snippet
```java
public class LsGreedy extends ValueRepair {

  private double center = 0, sigma;
  private final double eps = 1e-12;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/drepair/util/ValueFill.java`
#### Snippet
```java
  protected double mean = 0;
  protected double var = 0;
  protected int not_nan_number = 0;

  public ValueFill(RowIterator dataIterator) throws Exception {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/drepair/util/ValueFill.java`
#### Snippet
```java
  protected double[] repaired;
  protected double mean = 0;
  protected double var = 0;
  protected int not_nan_number = 0;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/drepair/util/ValueFill.java`
#### Snippet
```java
  protected double[] original;
  protected double[] repaired;
  protected double mean = 0;
  protected double var = 0;
  protected int not_nan_number = 0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/localconfignode/LocalConfigNode.java`
#### Snippet
```java
  private static final IoTDBConfig config = IoTDBDescriptor.getInstance().getConfig();
  public static final long STANDALONE_MOCK_TIME_SLOT_START_TIME = 0L;
  private volatile boolean initialized = false;

  private ScheduledExecutorService timedForceMLogThread;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/drepair/util/MAFill.java`
#### Snippet
```java
  double window_sum = 0;
  int window_cnt = 0;
  int l = 0;
  int r = window_size - 1;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/drepair/util/MAFill.java`
#### Snippet
```java
public class MAFill extends ValueFill {
  int window_size = 5;
  double window_sum = 0;
  int window_cnt = 0;
  int l = 0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/drepair/util/MAFill.java`
#### Snippet
```java
  int window_size = 5;
  double window_sum = 0;
  int window_cnt = 0;
  int l = 0;
  int r = window_size - 1;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/UDAFIntegralAvg.java`
#### Snippet
```java
  long lastTime = -1;
  double lastValue = 0;
  double integralValue = 0;

  @Override
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/UDAFIntegralAvg.java`
#### Snippet
```java
  long startTime = -1;
  long lastTime = -1;
  double lastValue = 0;
  double integralValue = 0;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0.0d` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/UDTFMinMax.java`
#### Snippet
```java
  String compute = "batch";
  double min = 0.0d;
  double max = 0.0d;
  boolean flag = true;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0.0d` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/UDTFMinMax.java`
#### Snippet
```java
  ArrayList<Long> timestamp = new ArrayList<>();
  String compute = "batch";
  double min = 0.0d;
  double max = 0.0d;
  boolean flag = true;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0.0` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/UDAFStddev.java`
#### Snippet
```java
  private long count = 0;
  private double sumX2 = 0.0;
  private double sumX1 = 0.0;

  @Override
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/UDAFStddev.java`
#### Snippet
```java
/** This function is used to calculate the population standard deviation. */
public class UDAFStddev implements UDTF {
  private long count = 0;
  private double sumX2 = 0.0;
  private double sumX1 = 0.0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0.0` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/UDAFStddev.java`
#### Snippet
```java
public class UDAFStddev implements UDTF {
  private long count = 0;
  private double sumX2 = 0.0;
  private double sumX1 = 0.0;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/UDAFIntegral.java`
#### Snippet
```java
  long lastTime = -1;
  double lastValue;
  double integralValue = 0;

  @Override
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/UDTFPACF.java`
#### Snippet
```java
  ArrayList<Long> timestamp = new ArrayList<>();
  int lag;
  int n = 0;

  @Override
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/UDTFQLB.java`
#### Snippet
```java

  private final DoubleArrayList valueArrayList = new DoubleArrayList();
  private int m = 0;
  private double qlb = 0.0f;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0.0f` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/UDTFQLB.java`
#### Snippet
```java
  private final DoubleArrayList valueArrayList = new DoubleArrayList();
  private int m = 0;
  private double qlb = 0.0f;

  @Override
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/UDTFSample.java`
#### Snippet
```java
  // These variables occurs in pool sampling
  private Pair<Long, Object>[] samples; // sampled data
  private int num = 0; // number of points already sampled
  private Random random;
  private TSDataType dataType;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0.0d` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/UDTFZScore.java`
#### Snippet
```java
  String compute = "batch";
  double avg = 0.0d;
  double sd = 0.0d;
  double sum = 0.0d;
  double squareSum = 0.0d;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0.0d` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/UDTFZScore.java`
#### Snippet
```java
  ArrayList<Long> timestamp = new ArrayList<>();
  String compute = "batch";
  double avg = 0.0d;
  double sd = 0.0d;
  double sum = 0.0d;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0.0d` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/UDTFZScore.java`
#### Snippet
```java
  double sd = 0.0d;
  double sum = 0.0d;
  double squareSum = 0.0d;

  @Override
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0.0d` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/UDTFZScore.java`
#### Snippet
```java
  double avg = 0.0d;
  double sd = 0.0d;
  double sum = 0.0d;
  double squareSum = 0.0d;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0.0` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/UDAFSkew.java`
#### Snippet
```java
public class UDAFSkew implements UDTF {
  private long count = 0;
  private double sumX3 = 0.0;
  private double sumX2 = 0.0;
  private double sumX1 = 0.0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/UDAFSkew.java`
#### Snippet
```java
/** This function calculates skew of input series. */
public class UDAFSkew implements UDTF {
  private long count = 0;
  private double sumX3 = 0.0;
  private double sumX2 = 0.0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0.0` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/UDAFSkew.java`
#### Snippet
```java
  private double sumX3 = 0.0;
  private double sumX2 = 0.0;
  private double sumX1 = 0.0;

  @Override
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0.0` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/UDAFSkew.java`
#### Snippet
```java
  private long count = 0;
  private double sumX3 = 0.0;
  private double sumX2 = 0.0;
  private double sumX1 = 0.0;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/dquality/util/TimeSeriesQuality.java`
#### Snippet
```java
  private boolean downtime = true; // count for shutdown period
  private int cnt = 0; // total number of points
  private int missCnt = 0; // number of missing points
  private int specialCnt = 0; // number of special values
  private int lateCnt = 0; // number of latency points
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/dquality/util/TimeSeriesQuality.java`
#### Snippet
```java
  private int valueCnt = 0; // number of out of range points
  private int variationCnt = 0; // number of variation out of range points
  private int speedCnt = 0; // number of speed out of range points
  private int speedchangeCnt = 0; // number of speed change(acceleration) out of range points
  private final double[] time; // series without special values
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/dquality/util/TimeSeriesQuality.java`
#### Snippet
```java
  private int lateCnt = 0; // number of latency points
  private int redundancyCnt = 0; // number of redundancy points
  private int valueCnt = 0; // number of out of range points
  private int variationCnt = 0; // number of variation out of range points
  private int speedCnt = 0; // number of speed out of range points
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/dquality/util/TimeSeriesQuality.java`
#### Snippet
```java
  private int cnt = 0; // total number of points
  private int missCnt = 0; // number of missing points
  private int specialCnt = 0; // number of special values
  private int lateCnt = 0; // number of latency points
  private int redundancyCnt = 0; // number of redundancy points
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/dquality/util/TimeSeriesQuality.java`
#### Snippet
```java
  private int specialCnt = 0; // number of special values
  private int lateCnt = 0; // number of latency points
  private int redundancyCnt = 0; // number of redundancy points
  private int valueCnt = 0; // number of out of range points
  private int variationCnt = 0; // number of variation out of range points
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/dquality/util/TimeSeriesQuality.java`
#### Snippet
```java
  private int missCnt = 0; // number of missing points
  private int specialCnt = 0; // number of special values
  private int lateCnt = 0; // number of latency points
  private int redundancyCnt = 0; // number of redundancy points
  private int valueCnt = 0; // number of out of range points
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/dquality/util/TimeSeriesQuality.java`
#### Snippet
```java
  public static final int windowSize = 10;
  private boolean downtime = true; // count for shutdown period
  private int cnt = 0; // total number of points
  private int missCnt = 0; // number of missing points
  private int specialCnt = 0; // number of special values
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/dquality/util/TimeSeriesQuality.java`
#### Snippet
```java
  private int variationCnt = 0; // number of variation out of range points
  private int speedCnt = 0; // number of speed out of range points
  private int speedchangeCnt = 0; // number of speed change(acceleration) out of range points
  private final double[] time; // series without special values
  private final double[] origin; // series without special values
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `library-udf/src/main/java/org/apache/iotdb/library/dquality/util/TimeSeriesQuality.java`
#### Snippet
```java
  private int redundancyCnt = 0; // number of redundancy points
  private int valueCnt = 0; // number of out of range points
  private int variationCnt = 0; // number of variation out of range points
  private int speedCnt = 0; // number of speed out of range points
  private int speedchangeCnt = 0; // number of speed change(acceleration) out of range points
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `zeppelin-interpreter/src/main/java/org/apache/zeppelin/iotdb/IoTDBInterpreter.java`
#### Snippet
```java

  private IoTDBConnectionException connectionException;
  private IoTDBConnection connection = null;
  private int fetchSize;
  private ZoneId zoneId;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `confignode/src/main/java/org/apache/iotdb/confignode/conf/ConfigNodeConfig.java`
#### Snippet
```java

  // TODO: Read from iotdb-confignode.properties
  private int configNodeRegionId = 0;

  /** ConfigNodeGroup consensus protocol */
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `confignode/src/main/java/org/apache/iotdb/confignode/conf/ConfigNodeConfig.java`
#### Snippet
```java

  private boolean configNodeRatisLogUnsafeFlushEnable = false;
  private boolean schemaRegionRatisLogUnsafeFlushEnable = false;

  /** RatisConsensus protocol, max capacity of a single Raft Log segment */
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `confignode/src/main/java/org/apache/iotdb/confignode/conf/ConfigNodeConfig.java`
#### Snippet
```java
  private boolean dataRegionRatisLogUnsafeFlushEnable = false;

  private boolean configNodeRatisLogUnsafeFlushEnable = false;
  private boolean schemaRegionRatisLogUnsafeFlushEnable = false;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `confignode/src/main/java/org/apache/iotdb/confignode/conf/ConfigNodeConfig.java`
#### Snippet
```java

  /** whether to use Snappy compression before sending data through the network */
  private boolean rpcAdvancedCompressionEnable = false;

  /** max frame size */
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `confignode/src/main/java/org/apache/iotdb/confignode/conf/ConfigNodeConfig.java`
#### Snippet
```java
   * previous TimePartitionSlot if set true
   */
  private boolean enableDataPartitionInheritPolicy = false;

  /** Number of SeriesPartitionSlots per StorageGroup */
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `confignode/src/main/java/org/apache/iotdb/confignode/conf/ConfigNodeConfig.java`
#### Snippet
```java

  /** RatisConsensus protocol, allow flushing Raft Log asynchronously */
  private boolean dataRegionRatisLogUnsafeFlushEnable = false;

  private boolean configNodeRatisLogUnsafeFlushEnable = false;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `rewrite-tsfile-tool/src/main/java/org/apache/iotdb/RewriteTsFileTool.java`
#### Snippet
```java
  private static String filePath = "";
  private static String readMode = "s";
  private static boolean ignoreBrokenChunk = false;

  public static void main(String[] args) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `confignode/src/main/java/org/apache/iotdb/confignode/manager/load/balancer/region/CopySetRegionAllocator.java`
#### Snippet
```java
  private static final int maximumRandomNum = 10;

  private int maxId = 0;
  private int intersectionSize = 0;
  private final List<TDataNodeLocation> weightList;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `confignode/src/main/java/org/apache/iotdb/confignode/manager/load/balancer/region/CopySetRegionAllocator.java`
#### Snippet
```java

  private int maxId = 0;
  private int intersectionSize = 0;
  private final List<TDataNodeLocation> weightList;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `confignode/src/main/java/org/apache/iotdb/confignode/manager/load/balancer/router/leader/MinCostFlowLeaderBalancer.java`
#### Snippet
```java
  private int[] nodeMinimumCost;

  private int maximumFlow = 0;
  private int minimumCost = 0;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `confignode/src/main/java/org/apache/iotdb/confignode/manager/load/balancer/router/leader/MinCostFlowLeaderBalancer.java`
#### Snippet
```java

  private int maximumFlow = 0;
  private int minimumCost = 0;

  public MinCostFlowLeaderBalancer() {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `confignode/src/main/java/org/apache/iotdb/confignode/manager/load/balancer/router/leader/MinCostFlowLeaderBalancer.java`
#### Snippet
```java
  /** Graph edges */
  // Maximum index of graph edges
  private int maxEdge = 0;

  private final List<MinCostFlowEdge> minCostFlowEdges;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/SchemaRegionSchemaFileImpl.java`
#### Snippet
```java

  private boolean isRecovering = true;
  private volatile boolean initialized = false;
  private boolean isClearing = false;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/SchemaRegionSchemaFileImpl.java`
#### Snippet
```java
  private boolean isRecovering = true;
  private volatile boolean initialized = false;
  private boolean isClearing = false;

  private final String storageGroupDirPath;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `confignode/src/main/java/org/apache/iotdb/confignode/writelog/io/BatchLogReader.java`
#### Snippet
```java
  private Iterator<ConfigPhysicalPlan> planIterator;

  private boolean fileCorrupted = false;

  BatchLogReader(ByteBuffer buffer) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `confignode/src/main/java/org/apache/iotdb/confignode/writelog/io/SingleFileLogReader.java`
#### Snippet
```java
  private BatchLogReader batchLogReader;

  private boolean fileCorrupted = false;

  public SingleFileLogReader(File logFile) throws FileNotFoundException {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `confignode/src/main/java/org/apache/iotdb/confignode/writelog/io/SingleFileLogReader.java`
#### Snippet
```java
  private int idx;
  // used to truncate the broken logs
  private long unbrokenLogsSize = 0;

  private BatchLogReader batchLogReader;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/Procedure.java`
#### Snippet
```java

  private ProcedureState state = ProcedureState.INITIALIZING;
  private int childrenLatch = 0;
  private ProcedureException exception;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/Procedure.java`
#### Snippet
```java

  private volatile byte[] result = null;
  private volatile boolean locked = false;
  private boolean lockedWhenLoading = false;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/Procedure.java`
#### Snippet
```java
  private boolean lockedWhenLoading = false;

  private int[] stackIndexes = null;

  private boolean persist = true;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/Procedure.java`
#### Snippet
```java
  private volatile byte[] result = null;
  private volatile boolean locked = false;
  private boolean lockedWhenLoading = false;

  private int[] stackIndexes = null;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/Procedure.java`
#### Snippet
```java
  private volatile long lastUpdate;

  private volatile byte[] result = null;
  private volatile boolean locked = false;
  private boolean lockedWhenLoading = false;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/RootProcedureStack.java`
#### Snippet
```java
  }

  private Set<Procedure<Env>> subprocs = null;
  private ArrayList<Procedure<Env>> subprocStack = null;
  private State state = State.RUNNING;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/RootProcedureStack.java`
#### Snippet
```java

  private Set<Procedure<Env>> subprocs = null;
  private ArrayList<Procedure<Env>> subprocStack = null;
  private State state = State.RUNNING;
  private int running = 0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/RootProcedureStack.java`
#### Snippet
```java
  private ArrayList<Procedure<Env>> subprocStack = null;
  private State state = State.RUNNING;
  private int running = 0;

  public synchronized boolean isFailed() {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/impl/schema/UnsetTemplateProcedure.java`
#### Snippet
```java
  private PartialPath path;

  private boolean alreadyRollback = false;

  private transient ByteBuffer addTemplateSetInfo;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/impl/statemachine/StateMachineProcedure.java`
#### Snippet
```java
  private int[] states = null;

  private List<Procedure<Env>> subProcList = null;

  protected final int getCycles() {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/impl/statemachine/StateMachineProcedure.java`
#### Snippet
```java

  private Flow stateFlow = Flow.HAS_MORE_STATE;
  protected int stateCount = 0;
  private int[] states = null;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/impl/statemachine/StateMachineProcedure.java`
#### Snippet
```java
  private Flow stateFlow = Flow.HAS_MORE_STATE;
  protected int stateCount = 0;
  private int[] states = null;

  private List<Procedure<Env>> subProcList = null;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/impl/statemachine/StateMachineProcedure.java`
#### Snippet
```java

  /** Cycles on same state. Good for figuring if we are stuck. */
  private int cycles = 0;

  /** Ordinal of the previous state. So we can tell if we are progressing or not. */
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/scheduler/LockQueue.java`
#### Snippet
```java
  private final ArrayDeque<Procedure<?>> deque = new ArrayDeque<>();

  private Procedure<?> lockOwnerProcedure = null;

  public boolean tryLock(Procedure<?> procedure) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/store/ConfigProcedureStore.java`
#### Snippet
```java
  private static final Logger LOG = LoggerFactory.getLogger(ConfigProcedureStore.class);

  private volatile boolean isRunning = false;
  private final ProcedureInfo procedureInfo;
  private final String procedureWalDir =
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/scheduler/AbstractProcedureScheduler.java`
#### Snippet
```java
  private final ReentrantLock schedulerLock = new ReentrantLock();
  private final Condition schedWaitCond = schedulerLock.newCondition();
  private boolean running = false;

  @Override
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/store/ProcedureStore.java`
#### Snippet
```java
  public static final String PROCEDURE_WAL_SUFFIX = ".proc.wal";
  private final IProcedureFactory procedureFactory;
  private volatile boolean isRunning = false;

  public ProcedureStore(IProcedureFactory procedureFactory) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `spark-tsfile/src/main/java/org/apache/iotdb/spark/tsfile/io/TsFileRecordWriter.java`
#### Snippet
```java
public class TsFileRecordWriter extends RecordWriter<NullWritable, TSRecord> {

  private TsFileWriter tsFileWriter = null;

  public TsFileRecordWriter(TaskAttemptContext job, Path file, Schema schema) throws IOException {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `flink-iotdb-connector/src/main/java/org/apache/iotdb/flink/IoTDBSink.java`
#### Snippet
```java
  private transient ScheduledExecutorService scheduledExecutor;

  private int batchSize = 0;
  private int flushIntervalMs = 3000;
  private List<Event> batchList;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `spark-tsfile/src/main/java/org/apache/iotdb/spark/tsfile/qp/common/FilterOperator.java`
#### Snippet
```java
  protected boolean isSingle = false;
  // if isSingle = false, singlePath must be null
  protected String singlePath = null;

  public FilterOperator(int tokenType) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `spark-tsfile/src/main/java/org/apache/iotdb/spark/tsfile/qp/common/FilterOperator.java`
#### Snippet
```java
  // leaf filter operator means it doesn't have left and right child filterOperator. Leaf filter
  // should be BasicOperator.
  protected boolean isLeaf = false;
  // All recursive children of this filter belong to one series path when isSingle variable is true
  protected boolean isSingle = false;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `spark-tsfile/src/main/java/org/apache/iotdb/spark/tsfile/qp/common/FilterOperator.java`
#### Snippet
```java
  protected boolean isLeaf = false;
  // All recursive children of this filter belong to one series path when isSingle variable is true
  protected boolean isSingle = false;
  // if isSingle = false, singlePath must be null
  protected String singlePath = null;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `confignode/src/main/java/org/apache/iotdb/confignode/persistence/partition/StorageGroupPartitionTable.java`
#### Snippet
```java
  private static final Logger LOGGER = LoggerFactory.getLogger(StorageGroupPartitionTable.class);

  private volatile boolean isPredeleted = false;
  // The name of database
  private String storageGroupName;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/mnode/RInternalMNode.java`
#### Snippet
```java
public class RInternalMNode extends RMNode {

  private volatile boolean useTemplate = false;

  /**
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/mnode/REntityMNode.java`
#### Snippet
```java
public class REntityMNode extends RInternalMNode implements IEntityMNode {

  private volatile boolean isAligned = false;

  /**
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `hive-connector/src/main/java/org/apache/iotdb/hive/TsFileSerDe.java`
#### Snippet
```java
  private List<String> columnNames;
  private List<TypeInfo> columnTypes;
  private TsFileDeserializer tsFileDeserializer = null;
  private ObjectInspector oi;
  private String deviceId;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `hive-connector/src/main/java/org/apache/iotdb/hive/TSFHiveRecordReader.java`
#### Snippet
```java
  private List<String> deviceIdList = new ArrayList<>();
  /** The index of QueryDataSet that is currently processed */
  private int currentIndex = 0;

  private boolean isReadDeviceId;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/file/metadata/ChunkMetadata.java`
#### Snippet
```java
  private Statistics<? extends Serializable> statistics;

  private boolean isFromOldTsFile = false;

  private long ramSize;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/file/metadata/statistics/Statistics.java`
#### Snippet
```java

  /** number of time-value points */
  private int count = 0;

  private long startTime = Long.MAX_VALUE;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/TsFileDeviceIterator.java`
#### Snippet
```java
  // device -> firstMeasurmentNode offset
  private final Queue<Pair<String, long[]>> queue;
  private Pair<String, Boolean> currentDevice = null;
  private MetadataIndexNode measurementNode;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/RowRecord.java`
#### Snippet
```java
  private final List<Field> fields;
  /** if any column is null, this field should be set to true; otherwise false */
  private boolean hasNullField = false;

  /** if any column is not null, this field should be set to false; otherwise true */
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/Chunk.java`
#### Snippet
```java
  private Statistics chunkStatistic;
  private ByteBuffer chunkData;
  private boolean isFromOldFile = false;
  /** A list of deleted intervals. */
  private List<TimeRange> deleteIntervalList;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/query/dataset/QueryDataSet.java`
#### Snippet
```java
  protected int rowLimit = 0; // rowLimit > 0 means the LIMIT constraint exists
  protected int rowOffset = 0;
  protected int alreadyReturnedRowNum = 0;
  protected int fetchSize = 10000;
  protected boolean ascending;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/query/dataset/QueryDataSet.java`
#### Snippet
```java
  protected List<TSDataType> dataTypes;

  protected int rowLimit = 0; // rowLimit > 0 means the LIMIT constraint exists
  protected int rowOffset = 0;
  protected int alreadyReturnedRowNum = 0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/query/dataset/QueryDataSet.java`
#### Snippet
```java
  /** For redirect query. Need keep consistent with EndPoint in rpc.thrift. */
  public static class EndPoint {
    private String ip = null;
    private int port = 0;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/query/dataset/QueryDataSet.java`
#### Snippet
```java

  protected int rowLimit = 0; // rowLimit > 0 means the LIMIT constraint exists
  protected int rowOffset = 0;
  protected int alreadyReturnedRowNum = 0;
  protected int fetchSize = 10000;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/query/dataset/QueryDataSet.java`
#### Snippet
```java
  public static class EndPoint {
    private String ip = null;
    private int port = 0;

    public EndPoint(String ip, int port) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/query/dataset/QueryDataSet.java`
#### Snippet
```java
   *  we need to redirect the query to any data group which has some data to speed up query.
   */
  protected EndPoint endPoint = null;

  /** if any column is null, we don't need that row */
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/TimeRange.java`
#### Snippet
```java

  /** The upper value */
  private long max = 0;

  /**
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/TimeRange.java`
#### Snippet
```java

  /** The lower value */
  private long min = 0;

  /** The upper value */
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/reader/page/PageReader.java`
#### Snippet
```java
  private List<TimeRange> deleteIntervalList;

  private int deleteCursor = 0;

  public PageReader(
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/reader/page/TimePageReader.java`
#### Snippet
```java
  private List<TimeRange> deleteIntervalList;

  private int deleteCursor = 0;

  public TimePageReader(ByteBuffer pageData, Decoder timeDecoder) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/reader/series/FileSeriesReaderByTimestamp.java`
#### Snippet
```java
  protected IChunkLoader chunkLoader;
  protected List<IChunkMetadata> chunkMetadataList;
  private int currentChunkIndex = 0;

  private IChunkReader chunkReader;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/reader/series/FileSeriesReaderByTimestamp.java`
#### Snippet
```java
  private IChunkReader chunkReader;
  private long currentTimestamp;
  private BatchData data = null; // current batch data

  /** init with chunkLoader and chunkMetaDataList. */
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/reader/page/ValuePageReader.java`
#### Snippet
```java
  private List<TimeRange> deleteIntervalList;

  private int deleteCursor = 0;

  public ValuePageReader(
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BitReader.java`
#### Snippet
```java
  private final ByteBuffer buffer;
  private int bitCnt = BITS_IN_A_BYTE;
  private byte cache = 0;

  public BitReader(ByteBuffer buffer) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BitConstructor.java`
#### Snippet
```java
  private final ByteArrayList data;
  private byte cache = 0;
  private int cnt = 0;

  public BitConstructor() {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BitConstructor.java`
#### Snippet
```java
  private static final long ALL_MASK = -1;
  private final ByteArrayList data;
  private byte cache = 0;
  private int cnt = 0;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/StringContainer.java`
#### Snippet
```java
  private int totalLength = 0;
  /** the count of string segments. */
  private int count = 0;

  private boolean isUpdated = true;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/StringContainer.java`
#### Snippet
```java
  private ArrayList<String> reverseList;
  /** the summation length of all string segments. */
  private int totalLength = 0;
  /** the count of string segments. */
  private int count = 0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/TsFileWriter.java`
#### Snippet
```java

  private final int pageSize;
  private long recordCount = 0;

  // deviceId -> measurementIdList
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/TsFileWriter.java`
#### Snippet
```java
   * write only sequential data
   */
  private boolean isUnseq = false;

  private Map<String, IChunkGroupWriter> groupWriters = new HashMap<>();
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/writer/RestorableTsFileIOWriter.java`
#### Snippet
```java
  private Map<Path, IMeasurementSchema> knownSchemas = new HashMap<>();

  private int lastFlushedChunkGroupIndex = 0;

  private boolean crashed;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/common/conf/TSFileConfig.java`
#### Snippet
```java
      "org.apache.hadoop.hdfs.server.namenode.ha.ConfiguredFailoverProxyProvider";
  /** whether use kerberos to authenticate hdfs */
  private boolean useKerberos = false;
  /** full path of kerberos keytab file */
  private String kerberosKeytabFilePath = "/path";
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/writer/tsmiterator/DiskTSMIterator.java`
#### Snippet
```java
  private LocalTsFileInput input;
  private long fileLength = 0;
  private long currentPos = 0;
  private long nextEndPosForDevice = 0;
  private String currentDevice;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/writer/tsmiterator/DiskTSMIterator.java`
#### Snippet
```java
  private File cmtFile;
  private LocalTsFileInput input;
  private long fileLength = 0;
  private long currentPos = 0;
  private long nextEndPosForDevice = 0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/writer/tsmiterator/DiskTSMIterator.java`
#### Snippet
```java
  private long fileLength = 0;
  private long currentPos = 0;
  private long nextEndPosForDevice = 0;
  private String currentDevice;
  private boolean remainsInFile = true;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/decoder/IntGorillaDecoder.java`
#### Snippet
```java
public class IntGorillaDecoder extends GorillaDecoderV2 {

  protected int storedValue = 0;

  @Override
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/decoder/GorillaDecoderV2.java`
#### Snippet
```java

  private byte buffer = 0;
  private int bitsLeft = 0;

  protected GorillaDecoderV2() {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/decoder/GorillaDecoderV2.java`
#### Snippet
```java
  protected boolean firstValueWasRead = false;
  protected int storedLeadingZeros = Integer.MAX_VALUE;
  protected int storedTrailingZeros = 0;
  protected boolean hasNext = true;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/decoder/GorillaDecoderV2.java`
#### Snippet
```java
  protected boolean hasNext = true;

  private byte buffer = 0;
  private int bitsLeft = 0;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/decoder/GorillaDecoderV2.java`
#### Snippet
```java
public abstract class GorillaDecoderV2 extends Decoder {

  protected boolean firstValueWasRead = false;
  protected int storedLeadingZeros = Integer.MAX_VALUE;
  protected int storedTrailingZeros = 0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/writer/TsFileIOWriter.java`
#### Snippet
```java
  private Path lastSerializePath = null;
  protected LinkedList<Long> endPosInCMTForDevice = new LinkedList<>();
  private volatile int chunkMetadataCount = 0;
  public static final String CHUNK_METADATA_TEMP_FILE_SUFFIX = ".meta";

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0L` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/writer/TsFileIOWriter.java`
#### Snippet
```java
  // the following variable is used for memory control
  protected long maxMetadataSize;
  protected long currentChunkMetadataSize = 0L;
  protected File chunkMetadataTempFile;
  protected LocalTsFileOutput tempOutput;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/writer/TsFileIOWriter.java`
#### Snippet
```java
  protected File chunkMetadataTempFile;
  protected LocalTsFileOutput tempOutput;
  protected volatile boolean hasChunkMetadataInDisk = false;
  // record the total num of path in order to make bloom filter
  protected int pathCount = 0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/writer/TsFileIOWriter.java`
#### Snippet
```java
  // record the total num of path in order to make bloom filter
  protected int pathCount = 0;
  protected boolean enableMemoryControl = false;
  private Path lastSerializePath = null;
  protected LinkedList<Long> endPosInCMTForDevice = new LinkedList<>();
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/writer/TsFileIOWriter.java`
#### Snippet
```java
  protected int pathCount = 0;
  protected boolean enableMemoryControl = false;
  private Path lastSerializePath = null;
  protected LinkedList<Long> endPosInCMTForDevice = new LinkedList<>();
  private volatile int chunkMetadataCount = 0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/writer/TsFileIOWriter.java`
#### Snippet
```java
  protected volatile boolean hasChunkMetadataInDisk = false;
  // record the total num of path in order to make bloom filter
  protected int pathCount = 0;
  protected boolean enableMemoryControl = false;
  private Path lastSerializePath = null;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/decoder/LongGorillaDecoder.java`
#### Snippet
```java
public class LongGorillaDecoder extends GorillaDecoderV2 {

  protected long storedValue = 0;

  @Override
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/schema/MeasurementSchema.java`
#### Snippet
```java
  private TSEncodingBuilder encodingConverter;
  private byte compressor;
  private Map<String, String> props = null;

  public MeasurementSchema() {}
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/decoder/RegularDataDecoder.java`
#### Snippet
```java
  protected int readIntTotalCount = 0;

  protected int nextReadIndex = 0;
  /** data number in this pack. */
  protected int packNum;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/decoder/RegularDataDecoder.java`
#### Snippet
```java

  /** the first value in one pack. */
  protected int readIntTotalCount = 0;

  protected int nextReadIndex = 0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/decoder/DeltaBinaryDecoder.java`
#### Snippet
```java

  /** the first value in one pack. */
  protected int readIntTotalCount = 0;

  protected int nextReadIndex = 0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/decoder/DeltaBinaryDecoder.java`
#### Snippet
```java
public abstract class DeltaBinaryDecoder extends Decoder {

  protected long count = 0;
  protected byte[] deltaBuf;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/decoder/DeltaBinaryDecoder.java`
#### Snippet
```java
  protected int readIntTotalCount = 0;

  protected int nextReadIndex = 0;
  /** max bit length of all value in a pack. */
  protected int packWidth;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/decoder/FreqDecoder.java`
#### Snippet
```java
  private int readTotalCount = 0;

  private int nextReadIndex = 0;

  public FreqDecoder() {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/decoder/FreqDecoder.java`
#### Snippet
```java
  private double data[];

  private int readTotalCount = 0;

  private int nextReadIndex = 0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/encoder/DeltaBinaryEncoder.java`
#### Snippet
```java

  protected int writeIndex = -1;
  protected int writeWidth = 0;

  /**
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/encoder/LongGorillaEncoder.java`
#### Snippet
```java
          + 1;

  private long storedValue = 0;

  @Override
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/encoder/GorillaEncoderV2.java`
#### Snippet
```java
  protected boolean firstValueWasWritten = false;
  protected int storedLeadingZeros = Integer.MAX_VALUE;
  protected int storedTrailingZeros = 0;

  private byte buffer = 0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/encoder/GorillaEncoderV2.java`
#### Snippet
```java
public abstract class GorillaEncoderV2 extends Encoder {

  protected boolean firstValueWasWritten = false;
  protected int storedLeadingZeros = Integer.MAX_VALUE;
  protected int storedTrailingZeros = 0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/encoder/GorillaEncoderV2.java`
#### Snippet
```java
  protected int storedTrailingZeros = 0;

  private byte buffer = 0;
  protected int bitsLeft = Byte.SIZE;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/encoder/IntGorillaEncoder.java`
#### Snippet
```java
          + 1;

  private int storedValue = 0;

  @Override
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/encoder/FreqEncoder.java`
#### Snippet
```java
  private static final Logger logger = LoggerFactory.getLogger(FreqEncoder.class);
  private int blockSize;
  protected int writeIndex = 0;
  private double threshold = 1e-4;
  private int beta;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/encoder/TSEncodingBuilder.java`
#### Snippet
```java
  public static class Ts2Diff extends TSEncodingBuilder {

    private int maxPointNumber = 0;

    @Override
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `integration-test/src/main/java/org/apache/iotdb/itbase/runtime/ClusterTestStatement.java`
#### Snippet
```java
  private final List<Statement> readStatements = new ArrayList<>();
  private final List<String> readEndpoints = new ArrayList<>();
  private boolean closed = false;
  private int maxRows = Integer.MAX_VALUE;
  private int queryTimeout = DEFAULT_QUERY_TIMEOUT;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `integration-test/src/main/java/org/apache/iotdb/it/env/AbstractEnv.java`
#### Snippet
```java
  protected List<ConfigNodeWrapper> configNodeWrapperList = Collections.emptyList();
  protected List<DataNodeWrapper> dataNodeWrapperList = Collections.emptyList();
  protected String testMethodName = null;

  protected void initEnvironment(int configNodesNum, int dataNodesNum) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `flink-tsfile-connector/src/main/java/org/apache/iotdb/flink/tsfile/TSRecordOutputFormat.java`
#### Snippet
```java
  private final TSRecordConverter<T> converter;

  private transient TSRecordCollector tsRecordCollector = null;

  public TSRecordOutputFormat(String path, Schema schema, TSRecordConverter<T> converter) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `flink-tsfile-connector/src/main/java/org/apache/iotdb/flink/tsfile/TsFileOutputFormat.java`
#### Snippet
```java
  protected transient Configuration hadoopConf = null;
  private FileOutputStream fos = null;
  protected transient TsFileWriter writer = null;

  public TsFileOutputFormat(String path, Schema schema, TSFileConfig config) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `flink-tsfile-connector/src/main/java/org/apache/iotdb/flink/tsfile/TsFileOutputFormat.java`
#### Snippet
```java

  protected transient Configuration hadoopConf = null;
  private FileOutputStream fos = null;
  protected transient TsFileWriter writer = null;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `flink-tsfile-connector/src/main/java/org/apache/iotdb/flink/tsfile/TsFileOutputFormat.java`
#### Snippet
```java
  @Nullable protected TSFileConfig config;

  protected transient Configuration hadoopConf = null;
  private FileOutputStream fos = null;
  protected transient TsFileWriter writer = null;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `flink-tsfile-connector/src/main/java/org/apache/iotdb/flink/tsfile/TsFileInputFormat.java`
#### Snippet
```java
  private transient org.apache.hadoop.conf.Configuration hadoopConf = null;
  private transient TsFileReader readTsFile = null;
  private transient QueryDataSet queryDataSet = null;

  public TsFileInputFormat(
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `flink-tsfile-connector/src/main/java/org/apache/iotdb/flink/tsfile/TsFileInputFormat.java`
#### Snippet
```java
  @Nullable private final TSFileConfig config;

  private transient org.apache.hadoop.conf.Configuration hadoopConf = null;
  private transient TsFileReader readTsFile = null;
  private transient QueryDataSet queryDataSet = null;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `flink-tsfile-connector/src/main/java/org/apache/iotdb/flink/tsfile/TsFileInputFormat.java`
#### Snippet
```java

  private transient org.apache.hadoop.conf.Configuration hadoopConf = null;
  private transient TsFileReader readTsFile = null;
  private transient QueryDataSet queryDataSet = null;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `hadoop/src/main/java/org/apache/iotdb/hadoop/tsfile/TSFRecordReader.java`
#### Snippet
```java

  private long timestamp = 0;
  private boolean isReadDeviceId = false;
  private boolean isReadTime = false;
  private TsFileSequenceReader reader;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `hadoop/src/main/java/org/apache/iotdb/hadoop/tsfile/TSFRecordReader.java`
#### Snippet
```java
  private List<String> deviceIdList = new ArrayList<>();

  private List<Field> fields = null;
  /** The index of QueryDataSet that is currently processed */
  private int currentIndex = 0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `hadoop/src/main/java/org/apache/iotdb/hadoop/tsfile/TSFRecordReader.java`
#### Snippet
```java
  private long timestamp = 0;
  private boolean isReadDeviceId = false;
  private boolean isReadTime = false;
  private TsFileSequenceReader reader;
  private List<String> measurementIds;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `hadoop/src/main/java/org/apache/iotdb/hadoop/tsfile/TSFRecordReader.java`
#### Snippet
```java
  private int currentIndex = 0;

  private long timestamp = 0;
  private boolean isReadDeviceId = false;
  private boolean isReadTime = false;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `hadoop/src/main/java/org/apache/iotdb/hadoop/tsfile/TSFRecordReader.java`
#### Snippet
```java
  private List<Field> fields = null;
  /** The index of QueryDataSet that is currently processed */
  private int currentIndex = 0;

  private long timestamp = 0;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `example/trigger/src/main/java/org/apache/iotdb/trigger/old/TriggerExample.java`
#### Snippet
```java
  // yet).
  // See IOTDB-2274 for more detail.
  private volatile boolean isSinksOpenedAfterCreation = false;

  private SlidingSizeWindowEvaluationHandler windowEvaluationHandler;
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `metrics/dropwizard-metrics/src/main/java/org/apache/iotdb/metrics/dropwizard/reporter/DropwizardIoTDBReporter.java`
#### Snippet
```java
  private static final Logger LOGGER = LoggerFactory.getLogger(DropwizardIoTDBReporter.class);

  private AbstractMetricManager dropwizardMetricManager = null;
  private IoTDBReporter reporter;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `metrics/dropwizard-metrics/src/main/java/org/apache/iotdb/metrics/dropwizard/reporter/DropwizardJmxReporter.java`
#### Snippet
```java
  private static final Logger LOGGER = LoggerFactory.getLogger(DropwizardJmxReporter.class);

  private AbstractMetricManager dropwizardMetricManager = null;
  private JmxReporter jmxReporter = null;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `metrics/dropwizard-metrics/src/main/java/org/apache/iotdb/metrics/dropwizard/reporter/DropwizardJmxReporter.java`
#### Snippet
```java

  private AbstractMetricManager dropwizardMetricManager = null;
  private JmxReporter jmxReporter = null;

  @Override
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `metrics/dropwizard-metrics/src/main/java/org/apache/iotdb/metrics/dropwizard/reporter/DropwizardPrometheusReporter.java`
#### Snippet
```java

  private AbstractMetricManager dropwizardMetricManager = null;
  private DisposableServer httpServer = null;

  @Override
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `metrics/dropwizard-metrics/src/main/java/org/apache/iotdb/metrics/dropwizard/reporter/DropwizardPrometheusReporter.java`
#### Snippet
```java
  private static final Logger LOGGER = LoggerFactory.getLogger(DropwizardPrometheusReporter.class);

  private AbstractMetricManager dropwizardMetricManager = null;
  private DisposableServer httpServer = null;

```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `null` is redundant
in `consensus/src/main/java/org/apache/iotdb/consensus/ratis/SnapshotStorage.java`
#### Snippet
```java

  private final ReentrantReadWriteLock snapshotCacheGuard = new ReentrantReadWriteLock();
  private SnapshotInfo currentSnapshot = null;

  public SnapshotStorage(IStateMachine applicationStateMachine) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `0` is redundant
in `consensus/src/main/java/org/apache/iotdb/consensus/common/request/IndexedConsensusRequest.java`
#### Snippet
```java
  private final List<IConsensusRequest> requests;
  private final List<ByteBuffer> serializedRequests = new ArrayList<>();
  private long serializedSize = 0;

  public IndexedConsensusRequest(long searchIndex, List<IConsensusRequest> requests) {
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `consensus/src/main/java/org/apache/iotdb/consensus/config/MultiLeaderConfig.java`
#### Snippet
```java
      private int rpcMaxConcurrentClientNum = 65535;
      private int thriftServerAwaitTimeForStopService = 60;
      private boolean isRpcThriftCompressionEnabled = false;
      private int selectorNumOfClientManager = 1;
      private int connectionTimeoutInMs = (int) TimeUnit.SECONDS.toMillis(20);
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `consensus/src/main/java/org/apache/iotdb/consensus/config/RatisConfig.java`
#### Snippet
```java

    public static class Builder {
      private boolean useMemory = false;
      private int queueElementLimit = 4096;
      private SizeInBytes queueByteLimit = SizeInBytes.valueOf("64MB");
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `consensus/src/main/java/org/apache/iotdb/consensus/multileader/logdispatcher/LogDispatcher.java`
#### Snippet
```java
  private ExecutorService executorService;

  private boolean stopped = false;

  public LogDispatcher(
```

### RuleId[ruleID=RedundantFieldInitialization]
Field initialization to `false` is redundant
in `consensus/src/main/java/org/apache/iotdb/consensus/multileader/logdispatcher/LogDispatcher.java`
#### Snippet
```java
    private final MultiLeaderMemoryManager multiLeaderMemoryManager =
        MultiLeaderMemoryManager.getInstance();
    private volatile boolean stopped = false;

    private final ConsensusReqReader.ReqIterator walEntryIterator;
```

## RuleId[ruleID=RedundantImplements]
### RuleId[ruleID=RedundantImplements]
Redundant interface declaration `Comparable`
in `node-commons/src/main/java/org/apache/iotdb/commons/path/PartialPath.java`
#### Snippet
```java
 * module
 */
public class PartialPath extends Path implements Comparable<Path>, Cloneable {

  private static final Logger logger = LoggerFactory.getLogger(PartialPath.class);
```

### RuleId[ruleID=RedundantImplements]
Redundant interface declaration `IPriorityBalancer`
in `confignode/src/main/java/org/apache/iotdb/confignode/manager/load/balancer/router/priority/LeaderPriorityBalancer.java`
#### Snippet
```java

/** The LeaderPriorityBalancer always pick the leader Replica */
public class LeaderPriorityBalancer extends GreedyPriorityBalancer implements IPriorityBalancer {

  public LeaderPriorityBalancer() {
```

### RuleId[ruleID=RedundantImplements]
Redundant interface declaration `AutoCloseable`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/v2/read/TsFileSequenceReaderForV2.java`
#### Snippet
```java
import java.util.stream.Collectors;

public class TsFileSequenceReaderForV2 extends TsFileSequenceReader implements AutoCloseable {

  private List<Pair<Long, Long>> versionInfo;
```

### RuleId[ruleID=RedundantImplements]
Redundant interface declaration `Serializable`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/filter/operator/OrFilter.java`
#### Snippet
```java

/** Either of the left and right operators of AndExpression must satisfy the condition. */
public class OrFilter extends BinaryFilter implements Serializable {

  private static final long serialVersionUID = -968055896528472694L;
```

### RuleId[ruleID=RedundantImplements]
Redundant interface declaration `Serializable`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/expression/impl/SingleSeriesExpression.java`
#### Snippet
```java
import java.util.Objects;

public class SingleSeriesExpression implements IUnaryExpression, Serializable {

  private static final long serialVersionUID = 7131207370394865228L;
```

### RuleId[ruleID=RedundantImplements]
Redundant interface declaration `Serializable`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/expression/impl/GlobalTimeExpression.java`
#### Snippet
```java
import java.util.Objects;

public class GlobalTimeExpression implements IUnaryExpression, Serializable {

  private static final long serialVersionUID = 1146132942359113670L;
```

### RuleId[ruleID=RedundantImplements]
Redundant interface declaration `Serializable`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/expression/impl/BinaryExpression.java`
#### Snippet
```java
import java.util.Objects;

public abstract class BinaryExpression implements IBinaryExpression, Serializable {

  private static final long serialVersionUID = -711801318534904452L;
```

### RuleId[ruleID=RedundantImplements]
Redundant interface declaration `Writable`
in `hadoop/src/main/java/org/apache/iotdb/hadoop/tsfile/TSFInputSplit.java`
#### Snippet
```java
 */
public class TSFInputSplit extends FileSplit
    implements Writable, org.apache.hadoop.mapred.InputSplit {

  public TSFInputSplit() {
```

### RuleId[ruleID=RedundantImplements]
Redundant interface declaration `org.apache.hadoop.mapred.InputSplit`
in `hadoop/src/main/java/org/apache/iotdb/hadoop/tsfile/TSFInputSplit.java`
#### Snippet
```java
 */
public class TSFInputSplit extends FileSplit
    implements Writable, org.apache.hadoop.mapred.InputSplit {

  public TSFInputSplit() {
```

## RuleId[ruleID=ExceptionNameDoesntEndWithException]
### RuleId[ruleID=ExceptionNameDoesntEndWithException]
Exception class name `SchemaFileNotExists` does not end with 'Exception'
in `server/src/main/java/org/apache/iotdb/db/exception/metadata/schemafile/SchemaFileNotExists.java`
#### Snippet
```java
import org.apache.iotdb.rpc.TSStatusCode;

public class SchemaFileNotExists extends MetadataException {

  public SchemaFileNotExists(String fileName) {
```

## RuleId[ruleID=InstanceofCatchParameter]
### RuleId[ruleID=InstanceofCatchParameter]
'instanceof' on 'catch' parameter `t`
in `node-commons/src/main/java/org/apache/iotdb/commons/client/sync/SyncThriftClientWithErrorHandler.java`
#### Snippet
```java
    } catch (Throwable t) {
      Throwable origin = t;
      if (t instanceof InvocationTargetException) {
        origin = ((InvocationTargetException) t).getTargetException();
      }
```

### RuleId[ruleID=InstanceofCatchParameter]
'instanceof' on 'catch' parameter `ex`
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
        }
      } catch (Exception ex) {
        if (ex instanceof SQLException) {
          throw (SQLException) ex;
        }
```

### RuleId[ruleID=InstanceofCatchParameter]
'instanceof' on 'catch' parameter `e`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/AnalyzeVisitor.java`
#### Snippet
```java
        whereExpression = analyzeWhereSplitByDevice(queryStatement, devicePath, schemaTree);
      } catch (SemanticException e) {
        if (e instanceof MeasurementNotExistException) {
          logger.warn(e.getMessage());
          deviceIterator.remove();
```

### RuleId[ruleID=InstanceofCatchParameter]
'instanceof' on 'catch' parameter `e`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/execution/config/ConfigExecution.java`
#### Snippet
```java
          executor);
    } catch (Throwable e) {
      if (e instanceof InterruptedException) {
        Thread.currentThread().interrupt();
      }
```

### RuleId[ruleID=InstanceofCatchParameter]
'instanceof' on 'catch' parameter `e`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/execution/config/ConfigExecution.java`
#### Snippet
```java
      return new ExecutionResult(context.getQueryId(), RpcUtils.getStatus(statusCode, message));
    } catch (InterruptedException | ExecutionException e) {
      if (e instanceof InterruptedException) {
        Thread.currentThread().interrupt();
      }
```

### RuleId[ruleID=InstanceofCatchParameter]
'instanceof' on 'catch' parameter `e`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/execution/QueryExecution.java`
#### Snippet
```java
    } catch (InterruptedException | ExecutionException e) {
      // TODO: (xingtanzjr) use more accurate error handling
      if (e instanceof InterruptedException) {
        Thread.currentThread().interrupt();
      }
```

### RuleId[ruleID=InstanceofCatchParameter]
'instanceof' on 'catch' parameter `e`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/scheduler/ClusterScheduler.java`
#### Snippet
```java
    } catch (InterruptedException | ExecutionException e) {
      // If the dispatch request cannot be sent or TException is caught, we will retry this query.
      if (e instanceof InterruptedException) {
        Thread.currentThread().interrupt();
      }
```

### RuleId[ruleID=InstanceofCatchParameter]
'instanceof' on 'catch' parameter `e`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/scheduler/load/LoadTsFileScheduler.java`
#### Snippet
```java
      }
    } catch (InterruptedException | ExecutionException e) {
      if (e instanceof InterruptedException) {
        Thread.currentThread().interrupt();
      }
```

### RuleId[ruleID=InstanceofCatchParameter]
'instanceof' on 'catch' parameter `e`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/scheduler/load/LoadTsFileScheduler.java`
#### Snippet
```java
      }
    } catch (InterruptedException | ExecutionException | CancellationException e) {
      if (e instanceof InterruptedException) {
        Thread.currentThread().interrupt();
      }
```

### RuleId[ruleID=InstanceofCatchParameter]
'instanceof' on 'catch' parameter `throwable`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/cross/CrossSpaceCompactionTask.java`
#### Snippet
```java
    } catch (Throwable throwable) {
      // catch throwable to handle OOM errors
      if (!(throwable instanceof InterruptedException)) {
        LOGGER.error(
            "{}-{} [Compaction] Meet errors in cross space compaction.",
```

### RuleId[ruleID=InstanceofCatchParameter]
'instanceof' on 'catch' parameter `throwable`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/inner/InnerSpaceCompactionTask.java`
#### Snippet
```java
    } catch (Throwable throwable) {
      // catch throwable to handle OOM errors
      if (!(throwable instanceof InterruptedException)) {
        LOGGER.error(
            "{}-{} [Compaction] Meet errors in inner space compaction.",
```

### RuleId[ruleID=InstanceofCatchParameter]
'instanceof' on 'catch' parameter `metadataException`
in `confignode/src/main/java/org/apache/iotdb/confignode/manager/ClusterSchemaManager.java`
#### Snippet
```java
    } catch (MetadataException metadataException) {
      // Reject if StorageGroup already set
      if (metadataException instanceof IllegalPathException) {
        result = new TSStatus(TSStatusCode.ILLEGAL_PATH.getStatusCode());
      } else {
```

## RuleId[ruleID=ArrayEquality]
### RuleId[ruleID=ArrayEquality]
Array objects are compared using `==`, not 'Arrays.equals()'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/write/InsertTabletNode.java`
#### Snippet
```java

  private boolean equals(Object[] columns) {
    if (this.columns == columns) {
      return true;
    }
```

### RuleId[ruleID=ArrayEquality]
Array objects are compared using `==`, not 'Arrays.equals()'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/ByteArrayList.java`
#### Snippet
```java

  private void ensureCapacityForAdd() {
    if (this.items == DEFAULT_SIZED_EMPTY_ARRAY) {
      this.items = new byte[10];
    } else {
```

## RuleId[ruleID=StringBufferReplaceableByStringBuilder]
### RuleId[ruleID=StringBufferReplaceableByStringBuilder]
`StringBuffer buf` may be declared as 'StringBuilder'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/StringUtils.java`
#### Snippet
```java
      char maybeMinusChar = dString.charAt(ePos + 1);
      if (maybeMinusChar != '-' && maybeMinusChar != '+') {
        StringBuffer buf = new StringBuffer(dString.length() + 1);
        buf.append(dString.substring(0, ePos + 1));
        buf.append('+');
```

### RuleId[ruleID=StringBufferReplaceableByStringBuilder]
`StringBuffer keywordBuf` may be declared as 'StringBuilder'
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      myKeywordMap.remove(it.next());
    }
    StringBuffer keywordBuf = new StringBuffer();
    it = myKeywordMap.keySet().iterator();
    if (it.hasNext()) {
```

## RuleId[ruleID=FuseStreamOperations]
### RuleId[ruleID=FuseStreamOperations]
Stream may be extended replacing 'sort'
in `server/src/main/java/org/apache/iotdb/db/tools/validate/TsFileValidationTool.java`
#### Snippet
```java
              Arrays.asList(Objects.requireNonNull(dataRegionDir.listFiles())).stream()
                  .filter(file -> Pattern.compile("[0-9]*").matcher(file.getName()).matches())
                  .collect(Collectors.toList());
          timePartitionDirs.sort(
              (f1, f2) ->
```

## RuleId[ruleID=RedundantStringFormatCall]
### RuleId[ruleID=RedundantStringFormatCall]
Redundant call to `format()`
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/column/ternary/CompareTernaryColumnTransformer.java`
#### Snippet
```java

    throw new UnsupportedOperationException(
        String.format("The Type of three subExpression should be all Numeric or Text"));
  }

```

### RuleId[ruleID=RedundantStringFormatCall]
Redundant call to `format()`
in `server/src/main/java/org/apache/iotdb/db/tools/watermark/WatermarkDetector.java`
#### Snippet
```java
    int b = calMin(cnt, alpha);
    System.out.println(
        String.format("total counted number = %d, detected hit number = %d", cnt, hit_cnt));
    System.out.println(
        String.format(
```

### RuleId[ruleID=RedundantStringFormatCall]
Redundant call to `format()`
in `server/src/main/java/org/apache/iotdb/db/tools/watermark/WatermarkDetector.java`
#### Snippet
```java
        String.format("total counted number = %d, detected hit number = %d", cnt, hit_cnt));
    System.out.println(
        String.format(
            "To reach the significant level %f, the hit number should be not smaller than: %d",
            alpha, b));
```

### RuleId[ruleID=RedundantStringFormatCall]
Redundant call to `format()`
in `server/src/main/java/org/apache/iotdb/db/utils/sync/SyncPipeUtil.java`
#### Snippet
```java
          ((TsFilePipeInfo) pipeInfo).isSyncDelOp());
    } else {
      throw new PipeException(String.format("Can not recognition pipeInfo type"));
    }
  }
```

### RuleId[ruleID=RedundantStringFormatCall]
Redundant call to `format()`
in `server/src/main/java/org/apache/iotdb/db/exception/metadata/schemafile/SegmentOverflowException.java`
#### Snippet
```java
  public SegmentOverflowException() {
    super(
        String.format("Segment not enough space"),
        TSStatusCode.SEGMENT_OUT_OF_SPACE.getStatusCode(),
        true);
```

### RuleId[ruleID=RedundantStringFormatCall]
Redundant call to `format()`
in `example/session/src/main/java/org/apache/iotdb/TabletExample.java`
#### Snippet
```java

    System.out.println(
        String.format(
            "Test Java: csv file name: %s, row: %d, col: %d", dataFileName, rowSize, colSize));
    System.out.println(String.format("Total points: %d", rowSize * colSize * schemas.size()));
```

### RuleId[ruleID=RedundantStringFormatCall]
Redundant call to `format()`
in `example/session/src/main/java/org/apache/iotdb/TabletExample.java`
#### Snippet
```java
        String.format(
            "Test Java: csv file name: %s, row: %d, col: %d", dataFileName, rowSize, colSize));
    System.out.println(String.format("Total points: %d", rowSize * colSize * schemas.size()));

    // test start
```

### RuleId[ruleID=RedundantStringFormatCall]
Redundant call to `format()`
in `example/session/src/main/java/org/apache/iotdb/TabletExample.java`
#### Snippet
```java
    session.close();

    System.out.println(String.format("load cost: %.3f", ((float) loadCost / 1000_000_000)));
    System.out.println(
        String.format(
```

### RuleId[ruleID=RedundantStringFormatCall]
Redundant call to `format()`
in `example/session/src/main/java/org/apache/iotdb/TabletExample.java`
#### Snippet
```java
    System.out.println(String.format("load cost: %.3f", ((float) loadCost / 1000_000_000)));
    System.out.println(
        String.format(
            "construct tablet cost: %.3f",
            ((float) (allEnd - allStart - insertCost - loadCost) / 1000_000_000)));
```

### RuleId[ruleID=RedundantStringFormatCall]
Redundant call to `format()`
in `example/session/src/main/java/org/apache/iotdb/TabletExample.java`
#### Snippet
```java
            ((float) (allEnd - allStart - insertCost - loadCost) / 1000_000_000)));
    System.out.println(
        String.format("insert tablet cost: %.3f", ((float) insertCost / 1000_000_000)));
    System.out.println(
        String.format("total cost: %.3f", ((float) (allEnd - allStart) / 1000_000_000)));
```

### RuleId[ruleID=RedundantStringFormatCall]
Redundant call to `format()`
in `example/session/src/main/java/org/apache/iotdb/TabletExample.java`
#### Snippet
```java
        String.format("insert tablet cost: %.3f", ((float) insertCost / 1000_000_000)));
    System.out.println(
        String.format("total cost: %.3f", ((float) (allEnd - allStart) / 1000_000_000)));
    System.out.println(String.format("%.3f", ((float) loadCost / 1000_000_000)));
  }
```

### RuleId[ruleID=RedundantStringFormatCall]
Redundant call to `format()`
in `example/session/src/main/java/org/apache/iotdb/TabletExample.java`
#### Snippet
```java
    System.out.println(
        String.format("total cost: %.3f", ((float) (allEnd - allStart) / 1000_000_000)));
    System.out.println(String.format("%.3f", ((float) loadCost / 1000_000_000)));
  }
}
```

## RuleId[ruleID=NonFinalFieldOfException]
### RuleId[ruleID=NonFinalFieldOfException]
Non-final field `parameter` of exception class
in `node-commons/src/main/java/org/apache/iotdb/commons/exception/ConfigurationException.java`
#### Snippet
```java

public class ConfigurationException extends IoTDBException {
  private String parameter;
  private String correctValue;

```

### RuleId[ruleID=NonFinalFieldOfException]
Non-final field `correctValue` of exception class
in `node-commons/src/main/java/org/apache/iotdb/commons/exception/ConfigurationException.java`
#### Snippet
```java
public class ConfigurationException extends IoTDBException {
  private String parameter;
  private String correctValue;

  /**
```

### RuleId[ruleID=NonFinalFieldOfException]
Non-final field `errorCode` of exception class
in `node-commons/src/main/java/org/apache/iotdb/commons/exception/IoTDBException.java`
#### Snippet
```java

  private static final long serialVersionUID = 8480450962311247736L;
  protected int errorCode;
  /**
   * This kind of exception is caused by users' wrong sql, and there is no need for server to print
```

### RuleId[ruleID=NonFinalFieldOfException]
Non-final field `isUserException` of exception class
in `node-commons/src/main/java/org/apache/iotdb/commons/exception/IoTDBException.java`
#### Snippet
```java
   * the full stack of the exception
   */
  protected boolean isUserException = false;

  public IoTDBException(String message, int errorCode) {
```

### RuleId[ruleID=NonFinalFieldOfException]
Non-final field `statusList` of exception class
in `service-rpc/src/main/java/org/apache/iotdb/rpc/BatchExecutionException.java`
#### Snippet
```java
public class BatchExecutionException extends StatementExecutionException {

  private List<TSStatus> statusList;

  public BatchExecutionException(String message) {
```

### RuleId[ruleID=NonFinalFieldOfException]
Non-final field `statusCode` of exception class
in `service-rpc/src/main/java/org/apache/iotdb/rpc/StatementExecutionException.java`
#### Snippet
```java
public class StatementExecutionException extends Exception {

  private int statusCode;

  public StatementExecutionException(TSStatus status) {
```

### RuleId[ruleID=NonFinalFieldOfException]
Non-final field `failureStatus` of exception class
in `server/src/main/java/org/apache/iotdb/db/exception/mpp/FragmentInstanceDispatchException.java`
#### Snippet
```java
public class FragmentInstanceDispatchException extends Exception {

  private TSStatus failureStatus;

  public FragmentInstanceDispatchException(Throwable t) {
```

### RuleId[ruleID=NonFinalFieldOfException]
Non-final field `measurementPath` of exception class
in `server/src/main/java/org/apache/iotdb/db/exception/metadata/MeasurementAlreadyExistException.java`
#### Snippet
```java
public class MeasurementAlreadyExistException extends MetadataException {

  private MeasurementPath measurementPath;

  public MeasurementAlreadyExistException(String path, MeasurementPath measurementPath) {
```

### RuleId[ruleID=NonFinalFieldOfException]
Non-final field `errMsg` of exception class
in `spark-tsfile/src/main/java/org/apache/iotdb/spark/tsfile/qp/exception/QueryProcessorException.java`
#### Snippet
```java
  private static final long serialVersionUID = -8987915921329335088L;

  private String errMsg;

  QueryProcessorException(String msg) {
```

## RuleId[ruleID=ZeroLengthArrayInitialization]
### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/UDFTable.java`
#### Snippet
```java

  public UDFInformation[] getAllUDFInformation() {
    return udfInformationMap.values().toArray(new UDFInformation[0]);
  }

```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/service/UDFClassLoader.java`
#### Snippet
```java
    HashSet<File> fileSet =
        new HashSet<>(FileUtils.listFiles(SystemFileFactory.INSTANCE.getFile(libRoot), null, true));
    URL[] urls = FileUtils.toURLs(fileSet.toArray(new File[0]));
    for (URL url : urls) {
      super.addURL(url);
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/service/UDFClassLoader.java`
#### Snippet
```java

  public UDFClassLoader(String libRoot) throws IOException {
    super(new URL[0]);
    this.libRoot = libRoot;
    activeQueriesCount = new AtomicLong(0);
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `node-commons/src/main/java/org/apache/iotdb/commons/path/PathPatternTree.java`
#### Snippet
```java
    }
    nodeList.add(node.getName());
    return new PartialPath(nodeList.toArray(new String[0]));
  }

```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/StatusUtils.java`
#### Snippet
```java
  public static TSStatus[] getFailingStatus(Map<Integer, TSStatus> statusMap, int size) {
    if (statusMap == null || statusMap.isEmpty()) {
      return new TSStatus[0];
    }
    TSStatus[] failingStatus = new TSStatus[size];
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/PathUtils.java`
#### Snippet
```java
  public static String[] splitPathToDetachedNodes(String path) throws IllegalPathException {
    if ("".equals(path)) {
      return new String[] {};
    }
    try {
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `node-commons/src/main/java/org/apache/iotdb/commons/path/PartialPath.java`
#### Snippet
```java
        Arrays.asList(prefix.getNodes()), Arrays.asList(nodes), new ArrayList<>(), results);
    return results.stream()
        .map(r -> new PartialPath(r.toArray(new String[0])))
        .collect(Collectors.toList());
  }
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `node-commons/src/main/java/org/apache/iotdb/commons/path/PartialPath.java`
#### Snippet
```java
    fullPath = path;
    if ("".equals(path)) {
      this.nodes = new String[] {};
    } else {
      this.nodes = new String[] {path};
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/SerializeUtils.java`
#### Snippet
```java
  public static Object[] deserializeObjects(ByteBuffer buffer) {
    if (buffer == null || buffer.limit() == 0) {
      return new Object[0];
    }
    int size = buffer.getInt();
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `node-commons/src/main/java/org/apache/iotdb/commons/schema/tree/AbstractTreeVisitor.java`
#### Snippet
```java
    }

    return optimizedNodes.toArray(new String[0]);
  }

```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `node-commons/src/main/java/org/apache/iotdb/commons/schema/tree/AbstractTreeVisitor.java`
#### Snippet
```java
    }
    nodeNames.add(node.getName());
    return nodeNames.toArray(new String[0]);
  }

```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDriver.java`
#### Snippet
```java
  public DriverPropertyInfo[] getPropertyInfo(String url, Properties info) {
    // TODO Auto-generated method stub
    return new DriverPropertyInfo[0];
  }

```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `jdbc/src/main/java/org/apache/iotdb/jdbc/StringUtils.java`
#### Snippet
```java
    }
    try {
      toPlainStringMethod = BigDecimal.class.getMethod("toPlainString", new Class[0]);
    } catch (NoSuchMethodException nsme) {
    }
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/qp/utils/GroupByLevelController.java`
#### Snippet
```java
    MeasurementPath groupedPath =
        new MeasurementPath(
            new PartialPath(transformedNodes.toArray(new String[0])),
            ((MeasurementPath) rawPath).getMeasurementSchema());
    if (rawPath.isMeasurementAliasExists()) {
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/qp/physical/sys/PruneTemplatePlan.java`
#### Snippet
```java

    this.name = name;
    this.prunedMeasurements = prunedMeasurements.toArray(new String[0]);
  }

```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/qp/physical/sys/AppendTemplatePlan.java`
#### Snippet
```java
    this.name = name;
    this.isAligned = isAligned;
    this.measurements = measurements.toArray(new String[0]);
    this.dataTypes = dataTypes.toArray(new TSDataType[0]);
    this.encodings = encodings.toArray(new TSEncoding[0]);
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/qp/physical/sys/AppendTemplatePlan.java`
#### Snippet
```java
    this.isAligned = isAligned;
    this.measurements = measurements.toArray(new String[0]);
    this.dataTypes = dataTypes.toArray(new TSDataType[0]);
    this.encodings = encodings.toArray(new TSEncoding[0]);
    this.compressors = compressors.toArray(new CompressionType[0]);
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/qp/physical/sys/AppendTemplatePlan.java`
#### Snippet
```java
    this.measurements = measurements.toArray(new String[0]);
    this.dataTypes = dataTypes.toArray(new TSDataType[0]);
    this.encodings = encodings.toArray(new TSEncoding[0]);
    this.compressors = compressors.toArray(new CompressionType[0]);
  }
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/qp/physical/sys/AppendTemplatePlan.java`
#### Snippet
```java
    this.dataTypes = dataTypes.toArray(new TSDataType[0]);
    this.encodings = encodings.toArray(new TSEncoding[0]);
    this.compressors = compressors.toArray(new CompressionType[0]);
  }

```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/parser/StatementGenerator.java`
#### Snippet
```java
    insertStatement.setDevicePath(new PartialPath(insertRecordReq.getPrefixPath()));
    insertStatement.setTime(insertRecordReq.getTimestamp());
    insertStatement.setMeasurements(insertRecordReq.getMeasurements().toArray(new String[0]));
    insertStatement.setDataTypes(new TSDataType[insertStatement.getMeasurements().length]);
    insertStatement.setValues(insertRecordReq.getValues().toArray(new Object[0]));
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/parser/StatementGenerator.java`
#### Snippet
```java
    insertStatement.setMeasurements(insertRecordReq.getMeasurements().toArray(new String[0]));
    insertStatement.setDataTypes(new TSDataType[insertStatement.getMeasurements().length]);
    insertStatement.setValues(insertRecordReq.getValues().toArray(new Object[0]));
    insertStatement.setNeedInferType(true);
    insertStatement.setAligned(insertRecordReq.isAligned);
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/parser/StatementGenerator.java`
#### Snippet
```java
    insertStatement.setDevicePath(new PartialPath(insertRecordReq.getPrefixPath()));
    insertStatement.setTime(insertRecordReq.getTimestamp());
    insertStatement.setMeasurements(insertRecordReq.getMeasurements().toArray(new String[0]));
    insertStatement.setAligned(insertRecordReq.isAligned);
    insertStatement.fillValues(insertRecordReq.values);
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/parser/StatementGenerator.java`
#### Snippet
```java
      InsertTabletStatement insertTabletStatement = new InsertTabletStatement();
      insertTabletStatement.setDevicePath(new PartialPath(req.prefixPaths.get(i)));
      insertTabletStatement.setMeasurements(req.measurementsList.get(i).toArray(new String[0]));
      insertTabletStatement.setTimes(
          QueryDataSetUtils.readTimesFromBuffer(req.timestampsList.get(i), req.sizeList.get(i)));
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/parser/StatementGenerator.java`
#### Snippet
```java
      InsertRowStatement statement = new InsertRowStatement();
      statement.setDevicePath(insertStatement.getDevicePath());
      statement.setMeasurements(req.measurementsList.get(i).toArray(new String[0]));
      statement.setTime(req.timestamps.get(i));
      statement.fillValues(req.valuesList.get(i));
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/parser/StatementGenerator.java`
#### Snippet
```java
    }

    insertRowStatement.setValues(newValues.toArray(new Object[0]));
    insertRowStatement.setMeasurements(newMeasurements.toArray(new String[0]));
  }
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/parser/StatementGenerator.java`
#### Snippet
```java

    insertRowStatement.setValues(newValues.toArray(new Object[0]));
    insertRowStatement.setMeasurements(newMeasurements.toArray(new String[0]));
  }

```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/parser/StatementGenerator.java`
#### Snippet
```java
    InsertTabletStatement insertStatement = new InsertTabletStatement();
    insertStatement.setDevicePath(new PartialPath(insertTabletReq.getPrefixPath()));
    insertStatement.setMeasurements(insertTabletReq.getMeasurements().toArray(new String[0]));
    insertStatement.setTimes(
        QueryDataSetUtils.readTimesFromBuffer(insertTabletReq.timestamps, insertTabletReq.size));
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/parser/StatementGenerator.java`
#### Snippet
```java
      InsertRowStatement statement = new InsertRowStatement();
      statement.setDevicePath(new PartialPath(req.getPrefixPaths().get(i)));
      statement.setMeasurements(req.getMeasurementsList().get(i).toArray(new String[0]));
      statement.setTime(req.getTimestamps().get(i));
      statement.fillValues(req.valuesList.get(i));
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/qp/physical/sys/CreateTemplatePlan.java`
#### Snippet
```java
    // original constructor
    this(name, measurements, dataTypes, encodings, compressors);
    this.schemaNames = schemaNames.toArray(new String[0]);
  }

```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/SelectIntoUtils.java`
#### Snippet
```java
      targetNodes.add(resNode);
    }
    return new PartialPath(targetNodes.toArray(new String[0]));
  }

```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/parser/ASTVisitor.java`
#### Snippet
```java
      measurementList.add(parseNodeNameWithoutWildCard(measurementName));
    }
    insertStatement.setMeasurementList(measurementList.toArray(new String[0]));
    return (ctx.TIME() == null && ctx.TIMESTAMP() == null);
  }
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/parser/ASTVisitor.java`
#### Snippet
```java
      privileges.add(privilegeValue.getText());
    }
    return privileges.toArray(new String[0]);
  }

```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/parser/ASTVisitor.java`
#### Snippet
```java
        }
      }
      valuesList.add(valueList.toArray(new String[0]));
    }
    insertStatement.setTimes(timeArray);
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/GroupByLevelController.java`
#### Snippet
```java
    MeasurementPath groupedPath =
        new MeasurementPath(
            new PartialPath(transformedNodes.toArray(new String[0])),
            ((MeasurementPath) rawPath).getMeasurementSchema());
    if (rawPath.isMeasurementAliasExists()) {
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/write/InsertRowNode.java`
#### Snippet
```java
    }
    this.measurementSchemas =
        deviceSchemaInfo.getMeasurementSchemaList().toArray(new MeasurementSchema[0]);

    // transfer data types from string values when necessary
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/write/InsertRowsOfOneDeviceNode.java`
#### Snippet
```java
      }
    }
    measurements = measurementList.toArray(new String[0]);
    dataTypes = dataTypeList.toArray(new TSDataType[0]);
  }
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/write/InsertRowsOfOneDeviceNode.java`
#### Snippet
```java
    }
    measurements = measurementList.toArray(new String[0]);
    dataTypes = dataTypeList.toArray(new TSDataType[0]);
  }

```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/write/InsertTabletNode.java`
#### Snippet
```java
    }
    measurementSchemas =
        deviceSchemaInfo.getMeasurementSchemaList().toArray(new MeasurementSchema[0]);

    // validate whether data types are matched
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/AnalyzeVisitor.java`
#### Snippet
```java
      String device = entry.getKey();
      MeasurementSchema[] tsFileSchemas =
          entry.getValue().keySet().toArray(new MeasurementSchema[0]);
      DeviceSchemaInfo schemaInfo =
          schemaTree.searchDeviceSchemaInfo(
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/LogicalPlanBuilder.java`
#### Snippet
```java
            context.getQueryId().genPlanNodeId(),
            this.getRoot(),
            selectExpressions.toArray(new Expression[0]),
            isGroupByTime,
            zoneId,
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/LogicalPlanBuilder.java`
#### Snippet
```java
            context.getQueryId().genPlanNodeId(),
            this.getRoot(),
            selectExpressions.toArray(new Expression[0]),
            filterExpression,
            isGroupByTime,
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/statement/crud/InsertRowsOfOneDeviceStatement.java`
#### Snippet
```java
      }
    }
    measurements = measurementList.toArray(new String[0]);
  }

```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/driver/Driver.java`
#### Snippet
```java
      // machine.
      Exception exception = new Exception("Interrupted By");
      exception.setStackTrace(interrupterStack.toArray(new StackTraceElement[0]));
      RuntimeException newException = new RuntimeException("Driver was interrupted", exception);
      newException.addSuppressed(t);
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/fragment/FragmentInstanceFailureInfo.java`
#### Snippet
```java
    }
    ImmutableList<StackTraceElement> stackTrace = stackTraceBuilder.build();
    failure.setStackTrace(stackTrace.toArray(new StackTraceElement[0]));
    return failure;
  }
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/AbstractIntoOperator.java`
#### Snippet
```java
      this.devicePath = devicePath;
      this.isAligned = isAligned;
      this.measurements = measurementToInputLocationMap.keySet().toArray(new String[0]);
      this.dataTypes = measurementToDataTypeMap.values().toArray(new TSDataType[0]);
      this.inputLocations = measurementToInputLocationMap.values().toArray(new InputLocation[0]);
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/AbstractIntoOperator.java`
#### Snippet
```java
      this.isAligned = isAligned;
      this.measurements = measurementToInputLocationMap.keySet().toArray(new String[0]);
      this.dataTypes = measurementToDataTypeMap.values().toArray(new TSDataType[0]);
      this.inputLocations = measurementToInputLocationMap.values().toArray(new InputLocation[0]);
      this.writtenCounter = new HashMap<>();
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/AbstractIntoOperator.java`
#### Snippet
```java
      this.measurements = measurementToInputLocationMap.keySet().toArray(new String[0]);
      this.dataTypes = measurementToDataTypeMap.values().toArray(new TSDataType[0]);
      this.inputLocations = measurementToInputLocationMap.values().toArray(new InputLocation[0]);
      this.writtenCounter = new HashMap<>();
      for (String measurement : measurements) {
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/FilterAndProjectOperator.java`
#### Snippet
```java
    }
    return TsBlock.wrapBlocksWithoutCopy(
        positionCount, originTimeColumn, resultColumns.toArray(new Column[0]));
  }

```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/input/QueryDataSetInputLayer.java`
#### Snippet
```java
      throws QueryProcessException {
    this.queryDataSet = queryDataSet;
    dataTypes = queryDataSet.getDataTypes().toArray(new TSDataType[0]);
    timestampIndex = dataTypes.length;
    rowRecordList =
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/intermediate/MultiInputColumnIntermediateLayer.java`
#### Snippet
```java
    super(expression, queryId, memoryBudgetInMB);

    layerPointReaders = parentLayerPointReaders.toArray(new LayerPointReader[0]);

    dataTypes = new TSDataType[layerPointReaders.length];
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/sync/externalpipe/ExtPipePluginRegister.java`
#### Snippet
```java
            FileUtils.listFiles(
                SystemFileFactory.INSTANCE.getFile(extPipeDir), new String[] {"jar"}, true));
    return FileUtils.toURLs(fileSet.toArray(new File[0]));
  }

```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/sync/externalpipe/ExtPipePluginManager.java`
#### Snippet
```java

  // condition-lock of commit and also record commit number
  private byte[] commitTriggerLocker = new byte[0];
  private long commitTriggerCounter = 0L;

```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/sync/externalpipe/ExtPipePlugin.java`
#### Snippet
```java

    // condition-lock for notifying new data arriving
    private byte[] newDataLocker = new byte[0];
    private long newDataCounter = 0L;

```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/query/dataset/RawQueryDataSetWithoutValueFilter.java`
#### Snippet
```java
    this.queryId = queryId;
    this.originalRowOffset = 0;
    blockingQueueArray = new BlockingQueue[0];
    timeHeap = new TimeSelector(0, ascending);
  }
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/utils/FilesUtils.java`
#### Snippet
```java
            org.apache.commons.io.FileUtils.listFiles(
                SystemFileFactory.INSTANCE.getFile(folderPath), new String[] {"jar"}, true));
    return org.apache.commons.io.FileUtils.toURLs(fileSet.toArray(new File[0]));
  }
}
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/log/CompactionLogger.java`
#### Snippet
```java
      return timePartitionDir.listFiles((dir, name) -> name.endsWith(compactionLogSuffix));
    } else {
      return new File[0];
    }
  }
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/cross/rewrite/RewriteCrossSpaceCompactionSelector.java`
#### Snippet
```java
      if (selectedUnseqFiles.isEmpty()) {
        LOGGER.debug("No merge candidates are found");
        return new List[0];
      }
    } catch (IOException e) {
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/trigger/service/TriggerClassLoader.java`
#### Snippet
```java

  public TriggerClassLoader(String libRoot) throws IOException {
    super(new URL[0]);
    this.libRoot = libRoot;
    LOGGER.info("Trigger lib root: {}", libRoot);
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/trigger/service/TriggerClassLoader.java`
#### Snippet
```java
    HashSet<File> fileSet =
        new HashSet<>(FileUtils.listFiles(SystemFileFactory.INSTANCE.getFile(libRoot), null, true));
    URL[] urls = FileUtils.toURLs(fileSet.toArray(new File[0]));
    for (URL url : urls) {
      super.addURL(url);
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/metadata/mnode/MNode.java`
#### Snippet
```java
      detachedPath.add(0, temp.getName());
    }
    return new PartialPath(detachedPath.toArray(new String[0]));
  }

```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/traverser/Traverser.java`
#### Snippet
```java
    nodeNames.add(currentNode.getName());

    return nodeNames.toArray(new String[0]);
  }

```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/RSchemaRegionLoader.java`
#### Snippet
```java
        }
      }
      urlClassLoader = new URLClassLoader(dependentJars.toArray(new URL[] {}));
    }
  }
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/protocol/mqtt/MPPPublishHandler.java`
#### Snippet
```java
        statement.setDevicePath(new PartialPath(event.getDevice()));
        statement.setTime(event.getTimestamp());
        statement.setMeasurements(event.getMeasurements().toArray(new String[0]));
        statement.setDataTypes(new TSDataType[event.getMeasurements().size()]);
        statement.setValues(event.getValues().toArray(new Object[0]));
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/protocol/mqtt/MPPPublishHandler.java`
#### Snippet
```java
        statement.setMeasurements(event.getMeasurements().toArray(new String[0]));
        statement.setDataTypes(new TSDataType[event.getMeasurements().size()]);
        statement.setValues(event.getValues().toArray(new Object[0]));
        statement.setNeedInferType(true);
        statement.setAligned(false);
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `server/src/main/java/org/apache/iotdb/db/protocol/rest/handler/StatementConstructionHandler.java`
#### Snippet
```java
    InsertTabletStatement insertStatement = new InsertTabletStatement();
    insertStatement.setDevicePath(new PartialPath(insertTabletRequest.getDevice()));
    insertStatement.setMeasurements(insertTabletRequest.getMeasurements().toArray(new String[0]));
    List<List<Object>> rawData = insertTabletRequest.getValues();
    List<String> rawDataType = insertTabletRequest.getDataTypes();
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/UDTFSpline.java`
#### Snippet
```java
    if (yDouble.size() >= 4 && samplePoints >= 2) { // 4个点以上才进行插值
      asi = new AkimaSplineInterpolator();
      double[] x = ArrayUtils.toPrimitive(xDouble.toArray(new Double[0]));
      double[] y = ArrayUtils.toPrimitive(yDouble.toArray(new Double[0]));
      psf = asi.interpolate(x, y);
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/UDTFSpline.java`
#### Snippet
```java
      asi = new AkimaSplineInterpolator();
      double[] x = ArrayUtils.toPrimitive(xDouble.toArray(new Double[0]));
      double[] y = ArrayUtils.toPrimitive(yDouble.toArray(new Double[0]));
      psf = asi.interpolate(x, y);
      for (int i = 0; i < samplePoints; i++) {
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/UDTFPACF.java`
#### Snippet
```java
      }
      double[] x =
          Arrays.stream(value.toArray(new Double[0])).mapToDouble(Double::valueOf).toArray();
      double xmean = StatUtils.mean(x);
      for (int i = 0; i < x.length; i++) {
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `library-udf/src/main/java/org/apache/iotdb/library/frequency/UDTFDeconv.java`
#### Snippet
```java
    } else { // order of divisor is no larger than dividend
      double[] q = new double[list1.size() - list2.size() + 1];
      Double[] r = list1.toArray(new Double[0]);
      int m = list2.size() - 1;
      for (int i = q.length - 1; i >= 0; i--) {
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `example/flink/src/main/java/org/apache/iotdb/flink/TsFileUtils.java`
#### Snippet
```java
        result.add(rowRecord.getTimestamp() + "," + row);
      }
      return result.toArray(new String[0]);
    }
  }
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `confignode/src/main/java/org/apache/iotdb/confignode/consensus/request/read/storagegroup/CountStorageGroupPlan.java`
#### Snippet
```java
  public CountStorageGroupPlan(List<String> storageGroupPattern) {
    this();
    this.storageGroupPattern = storageGroupPattern.toArray(new String[0]);
  }

```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `confignode/src/main/java/org/apache/iotdb/confignode/consensus/request/read/storagegroup/CountStorageGroupPlan.java`
#### Snippet
```java
  public CountStorageGroupPlan(ConfigPhysicalPlanType type, List<String> storageGroupPattern) {
    super(type);
    this.storageGroupPattern = storageGroupPattern.toArray(new String[0]);
  }

```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `confignode/src/main/java/org/apache/iotdb/confignode/consensus/request/write/storagegroup/SetTTLPlan.java`
#### Snippet
```java
  public SetTTLPlan(List<String> storageGroupPathPattern, long TTL) {
    this();
    this.storageGroupPathPattern = storageGroupPathPattern.toArray(new String[0]);
    this.TTL = TTL;
  }
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/mnode/RInternalMNode.java`
#### Snippet
```java
            childName, childNameMaxLevel, RMNodeType.INTERNAL.getValue());
    try {
      readWriteHandler.updateNode(innerName.getBytes(), new byte[] {});
    } catch (RocksDBException e) {
      throw new MetadataException(e);
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/mnode/REntityMNode.java`
#### Snippet
```java
            childName, childNameMaxLevel, RMNodeType.ENTITY.getValue());
    try {
      readWriteHandler.updateNode(innerName.getBytes(), new byte[] {});
    } catch (RocksDBException e) {
      throw new MetadataException(e);
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `hive-connector/src/main/java/org/apache/iotdb/hive/TSFHiveInputFormat.java`
#### Snippet
```java
    job.setBoolean(INPUT_DIR_RECURSIVE, true);
    return TSFInputFormat.getTSFInputSplit(job, Arrays.asList(super.listStatus(job)), logger)
        .toArray(new InputSplit[0]);
  }
}
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/mnode/RMeasurementMNode.java`
#### Snippet
```java
    // todo all existing attributes of the measurementNode need to be written
    try {
      readWriteHandler.updateNode(innerName.getBytes(), new byte[] {});
    } catch (RocksDBException e) {
      throw new MetadataException(e);
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/block/column/DoubleColumnBuilder.java`
#### Snippet
```java
  // it is assumed that these arrays are the same length
  private boolean[] valueIsNull = new boolean[0];
  private double[] values = new double[0];

  private long retainedSizeInBytes;
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/block/column/DoubleColumnBuilder.java`
#### Snippet
```java

  // it is assumed that these arrays are the same length
  private boolean[] valueIsNull = new boolean[0];
  private double[] values = new double[0];

```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/block/column/BinaryColumnBuilder.java`
#### Snippet
```java
  // it is assumed that these arrays are the same length
  private boolean[] valueIsNull = new boolean[0];
  private Binary[] values = new Binary[0];

  private long arraysRetainedSizeInBytes;
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/block/column/BinaryColumnBuilder.java`
#### Snippet
```java

  // it is assumed that these arrays are the same length
  private boolean[] valueIsNull = new boolean[0];
  private Binary[] values = new Binary[0];

```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/block/column/LongColumnBuilder.java`
#### Snippet
```java

  // it is assumed that these arrays are the same length
  private boolean[] valueIsNull = new boolean[0];
  private long[] values = new long[0];

```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/block/column/LongColumnBuilder.java`
#### Snippet
```java
  // it is assumed that these arrays are the same length
  private boolean[] valueIsNull = new boolean[0];
  private long[] values = new long[0];

  private long retainedSizeInBytes;
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/block/column/BooleanColumnBuilder.java`
#### Snippet
```java
  // it is assumed that these arrays are the same length
  private boolean[] valueIsNull = new boolean[0];
  private boolean[] values = new boolean[0];

  private long retainedSizeInBytes;
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/block/column/TimeColumnBuilder.java`
#### Snippet
```java
  private int positionCount;

  private long[] values = new long[0];

  private long retainedSizeInBytes;
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/block/column/BooleanColumnBuilder.java`
#### Snippet
```java

  // it is assumed that these arrays are the same length
  private boolean[] valueIsNull = new boolean[0];
  private boolean[] values = new boolean[0];

```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/block/column/IntColumnBuilder.java`
#### Snippet
```java
  // it is assumed that these arrays are the same length
  private boolean[] valueIsNull = new boolean[0];
  private int[] values = new int[0];

  private long retainedSizeInBytes;
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/block/column/IntColumnBuilder.java`
#### Snippet
```java

  // it is assumed that these arrays are the same length
  private boolean[] valueIsNull = new boolean[0];
  private int[] values = new int[0];

```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/block/column/FloatColumnBuilder.java`
#### Snippet
```java

  // it is assumed that these arrays are the same length
  private boolean[] valueIsNull = new boolean[0];
  private float[] values = new float[0];

```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/block/column/FloatColumnBuilder.java`
#### Snippet
```java
  // it is assumed that these arrays are the same length
  private boolean[] valueIsNull = new boolean[0];
  private float[] values = new float[0];

  private long retainedSizeInBytes;
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/controller/MetadataQuerierByFileImpl.java`
#### Snippet
```java
      Set<String> selectedMeasurements = deviceMeasurements.getValue();
      List<String> devices = this.tsFileReader.getAllDevices();
      String[] deviceNames = devices.toArray(new String[0]);
      if (Arrays.binarySearch(deviceNames, selectedDevice) < 0) {
        continue;
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/RamUsageEstimator.java`
#### Snippet
```java

    ClassCache cachedInfo =
        new ClassCache(alignObjectSize(shallowInstanceSize), referenceFields.toArray(new Field[0]));
    return cachedInfo;
  }
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `tsfile/src/main/java/org/apache/iotdb/tsfile/compress/IUnCompressor.java`
#### Snippet
```java
    public byte[] uncompress(byte[] bytes) throws IOException {
      if (bytes == null) {
        return new byte[0];
      }

```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `tsfile/src/main/java/org/apache/iotdb/tsfile/compress/IUnCompressor.java`
#### Snippet
```java
    public byte[] uncompress(byte[] bytes) {
      if (bytes == null) {
        return new byte[0];
      }

```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `tsfile/src/main/java/org/apache/iotdb/tsfile/compress/IUnCompressor.java`
#### Snippet
```java
            "tsfile-compression SnappyUnCompressor: errors occurs when uncompress input byte", e);
      }
      return new byte[0];
    }

```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `tsfile/src/main/java/org/apache/iotdb/tsfile/compress/IUnCompressor.java`
#### Snippet
```java
    public byte[] uncompress(byte[] byteArray) throws IOException {
      if (null == byteArray) {
        return new byte[0];
      }

```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `tsfile/src/main/java/org/apache/iotdb/tsfile/compress/ICompressor.java`
#### Snippet
```java
    public byte[] compress(byte[] data) {
      if (data == null) {
        return new byte[0];
      }
      return compressor.compress(data);
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `tsfile/src/main/java/org/apache/iotdb/tsfile/compress/ICompressor.java`
#### Snippet
```java
    public byte[] compress(byte[] data) throws IOException {
      if (data == null) {
        return new byte[0];
      }
      return Snappy.compress(data);
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `tsfile/src/main/java/org/apache/iotdb/tsfile/compress/ICompressor.java`
#### Snippet
```java
    public byte[] compress(byte[] data) throws IOException {
      if (null == data) {
        return new byte[0];
      }

```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/decoder/FreqDecoder.java`
#### Snippet
```java
  private long[] decodeValue(int m, BitReader reader) {
    if (m == 0) {
      return new long[0];
    }
    // Decode the encoded bit width of the first value with 8 bits
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `metrics/interface/src/main/java/org/apache/iotdb/metrics/impl/DoNothingHistogramSnapshot.java`
#### Snippet
```java
  @Override
  public long[] getValues() {
    return new long[0];
  }

```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `metrics/interface/src/main/java/org/apache/iotdb/metrics/utils/MetricInfo.java`
#### Snippet
```java
          allNames.add(v);
        });
    return allNames.toArray(new String[0]);
  }

```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `hadoop/src/main/java/org/apache/iotdb/hadoop/fileSystem/HDFSFile.java`
#### Snippet
```java
        files.add(new HDFSFile(filePath.toUri().toString()));
      }
      return files.toArray(new HDFSFile[0]);
    } catch (IOException e) {
      logger.error("Fail to list files in {}. ", hdfsPath.toUri(), e);
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `hadoop/src/main/java/org/apache/iotdb/hadoop/fileSystem/HDFSFile.java`
#### Snippet
```java
    PathFilter pathFilter = path -> path.toUri().toString().endsWith(suffix);
    List<HDFSFile> files = listFiles(fileFolder, pathFilter);
    return files.toArray(new HDFSFile[0]);
  }

```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `hadoop/src/main/java/org/apache/iotdb/hadoop/fileSystem/HDFSFile.java`
#### Snippet
```java
    PathFilter pathFilter = path -> path.toUri().toString().startsWith(prefix);
    List<HDFSFile> files = listFiles(fileFolder, pathFilter);
    return files.toArray(new HDFSFile[0]);
  }

```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `flink-tsfile-connector/src/main/java/org/apache/iotdb/flink/tsfile/RowTSRecordConverter.java`
#### Snippet
```java
      dataPointIndexMapping[i] = tsRecord.dataPointList.size() - 1;
    }
    outputTemplate = outputTemplateList.toArray(new TSRecord[0]);

    reuse = new TSRecord[outputTemplate.length];
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `consensus/src/main/java/org/apache/iotdb/consensus/ratis/SnapshotStorage.java`
#### Snippet
```java
    }

    Path[] pathArray = snapshotPaths.toArray(new Path[0]);
    Arrays.sort(
        pathArray,
```

### RuleId[ruleID=ZeroLengthArrayInitialization]
Allocation of zero length array
in `consensus/src/main/java/org/apache/iotdb/consensus/multileader/wal/GetConsensusReqReaderPlan.java`
#### Snippet
```java
  public ByteBuffer serializeToByteBuffer() {
    // do not need any data, we will use instanceOf
    return ByteBuffer.wrap(new byte[0]);
  }
}
```

## RuleId[ruleID=NullArgumentToVariableArgMethod]
### RuleId[ruleID=NullArgumentToVariableArgMethod]
Confusing argument `null`, unclear if a varargs or non-varargs call is desired
in `jdbc/src/main/java/org/apache/iotdb/jdbc/StringUtils.java`
#### Snippet
```java
    if (toPlainStringMethod != null) {
      try {
        return (String) toPlainStringMethod.invoke(decimal, null);
      } catch (InvocationTargetException invokeEx) {

```

### RuleId[ruleID=NullArgumentToVariableArgMethod]
Confusing argument `jarURLs`, unclear if a varargs or non-varargs call is desired
in `server/src/main/java/org/apache/iotdb/db/sync/externalpipe/ExtPipePluginRegister.java`
#### Snippet
```java
  private void buildFactoryMap() throws IOException {
    URL[] jarURLs = getPlugInJarURLs();
    logger.debug("ExtPipePluginRegister buildFactoryMap(), ExtPIPE Plugin jarURLs: {}", jarURLs);

    for (URL jarUrl : jarURLs) {
```

### RuleId[ruleID=NullArgumentToVariableArgMethod]
Confusing argument `jarURLs`, unclear if a varargs or non-varargs call is desired
in `server/src/main/java/org/apache/iotdb/db/protocol/mqtt/PayloadFormatManager.java`
#### Snippet
```java

    URL[] jarURLs = FilesUtils.getPluginJarURLs(mqttDir);
    logger.debug("MQTT Plugin jarURLs: {}", jarURLs);

    for (URL jarUrl : jarURLs) {
```

## RuleId[ruleID=TypeParameterExtendsObject]
### RuleId[ruleID=TypeParameterExtendsObject]
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/source/SeriesScanUtil.java`
#### Snippet
```java
    @Override
    public boolean isTakeSeqAsFirst(
        Statistics<? extends Object> seqStatistics, Statistics<? extends Object> unseqStatistics) {
      return seqStatistics.getEndTime() > unseqStatistics.getEndTime();
    }
```

### RuleId[ruleID=TypeParameterExtendsObject]
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/source/SeriesScanUtil.java`
#### Snippet
```java
    @Override
    public boolean isTakeSeqAsFirst(
        Statistics<? extends Object> seqStatistics, Statistics<? extends Object> unseqStatistics) {
      return seqStatistics.getEndTime() > unseqStatistics.getEndTime();
    }
```

### RuleId[ruleID=TypeParameterExtendsObject]
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/source/SeriesScanUtil.java`
#### Snippet
```java
    /** Return true if taking first page reader from seq readers */
    boolean isTakeSeqAsFirst(
        Statistics<? extends Object> seqStatistics, Statistics<? extends Object> unseqStatistics);

    boolean getAscending();
```

### RuleId[ruleID=TypeParameterExtendsObject]
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/source/SeriesScanUtil.java`
#### Snippet
```java
    /** Return true if taking first page reader from seq readers */
    boolean isTakeSeqAsFirst(
        Statistics<? extends Object> seqStatistics, Statistics<? extends Object> unseqStatistics);

    boolean getAscending();
```

### RuleId[ruleID=TypeParameterExtendsObject]
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/source/SeriesScanUtil.java`
#### Snippet
```java
    <T> Comparator<T> comparingLong(ToLongFunction<? super T> keyExtractor);

    long getCurrentEndPoint(long time, Statistics<? extends Object> statistics);

    long getCurrentEndPoint(
```

### RuleId[ruleID=TypeParameterExtendsObject]
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/source/SeriesScanUtil.java`
#### Snippet
```java
    long getOverlapCheckTime(Statistics<? extends Object> range);

    boolean isOverlapped(Statistics<? extends Object> left, Statistics<? extends Object> right);

    boolean isOverlapped(long time, Statistics<? extends Object> right);
```

### RuleId[ruleID=TypeParameterExtendsObject]
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/source/SeriesScanUtil.java`
#### Snippet
```java
    long getOverlapCheckTime(Statistics<? extends Object> range);

    boolean isOverlapped(Statistics<? extends Object> left, Statistics<? extends Object> right);

    boolean isOverlapped(long time, Statistics<? extends Object> right);
```

### RuleId[ruleID=TypeParameterExtendsObject]
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/source/SeriesScanUtil.java`
#### Snippet
```java
    @Override
    public long getCurrentEndPoint(
        Statistics<? extends Object> seqStatistics, Statistics<? extends Object> unseqStatistics) {
      return Math.min(seqStatistics.getEndTime(), unseqStatistics.getEndTime());
    }
```

### RuleId[ruleID=TypeParameterExtendsObject]
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/source/SeriesScanUtil.java`
#### Snippet
```java
    @Override
    public long getCurrentEndPoint(
        Statistics<? extends Object> seqStatistics, Statistics<? extends Object> unseqStatistics) {
      return Math.min(seqStatistics.getEndTime(), unseqStatistics.getEndTime());
    }
```

### RuleId[ruleID=TypeParameterExtendsObject]
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/source/SeriesScanUtil.java`
#### Snippet
```java
    long getOrderTime(TsFileResource fileResource);

    long getOverlapCheckTime(Statistics<? extends Object> range);

    boolean isOverlapped(Statistics<? extends Object> left, Statistics<? extends Object> right);
```

### RuleId[ruleID=TypeParameterExtendsObject]
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/source/SeriesScanUtil.java`
#### Snippet
```java

    @Override
    public long getCurrentEndPoint(long time, Statistics<? extends Object> statistics) {
      return Math.min(time, statistics.getEndTime());
    }
```

### RuleId[ruleID=TypeParameterExtendsObject]
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/source/SeriesScanUtil.java`
#### Snippet
```java
    boolean isOverlapped(Statistics<? extends Object> left, Statistics<? extends Object> right);

    boolean isOverlapped(long time, Statistics<? extends Object> right);

    boolean isOverlapped(long time, TsFileResource right);
```

### RuleId[ruleID=TypeParameterExtendsObject]
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/source/SeriesScanUtil.java`
#### Snippet
```java

    long getCurrentEndPoint(
        Statistics<? extends Object> seqStatistics, Statistics<? extends Object> unseqStatistics);

    boolean isExcessEndpoint(long time, long endpointTime);
```

### RuleId[ruleID=TypeParameterExtendsObject]
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/source/SeriesScanUtil.java`
#### Snippet
```java

    long getCurrentEndPoint(
        Statistics<? extends Object> seqStatistics, Statistics<? extends Object> unseqStatistics);

    boolean isExcessEndpoint(long time, long endpointTime);
```

### RuleId[ruleID=TypeParameterExtendsObject]
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/source/SeriesScanUtil.java`
#### Snippet
```java
  public interface TimeOrderUtils {

    long getOrderTime(Statistics<? extends Object> statistics);

    long getOrderTime(TsFileResource fileResource);
```

### RuleId[ruleID=TypeParameterExtendsObject]
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/source/SeriesScanUtil.java`
#### Snippet
```java

    @Override
    public long getCurrentEndPoint(long time, Statistics<? extends Object> statistics) {
      return Math.max(time, statistics.getStartTime());
    }
```

### RuleId[ruleID=TypeParameterExtendsObject]
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/source/SeriesScanUtil.java`
#### Snippet
```java
    @Override
    public boolean isTakeSeqAsFirst(
        Statistics<? extends Object> seqStatistics, Statistics<? extends Object> unseqStatistics) {
      return seqStatistics.getStartTime() < unseqStatistics.getStartTime();
    }
```

### RuleId[ruleID=TypeParameterExtendsObject]
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/source/SeriesScanUtil.java`
#### Snippet
```java
    @Override
    public boolean isTakeSeqAsFirst(
        Statistics<? extends Object> seqStatistics, Statistics<? extends Object> unseqStatistics) {
      return seqStatistics.getStartTime() < unseqStatistics.getStartTime();
    }
```

### RuleId[ruleID=TypeParameterExtendsObject]
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/source/SeriesScanUtil.java`
#### Snippet
```java
    @Override
    public long getCurrentEndPoint(
        Statistics<? extends Object> seqStatistics, Statistics<? extends Object> unseqStatistics) {
      return Math.max(seqStatistics.getStartTime(), unseqStatistics.getStartTime());
    }
```

### RuleId[ruleID=TypeParameterExtendsObject]
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/source/SeriesScanUtil.java`
#### Snippet
```java
    @Override
    public long getCurrentEndPoint(
        Statistics<? extends Object> seqStatistics, Statistics<? extends Object> unseqStatistics) {
      return Math.max(seqStatistics.getStartTime(), unseqStatistics.getStartTime());
    }
```

### RuleId[ruleID=TypeParameterExtendsObject]
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `server/src/main/java/org/apache/iotdb/db/query/reader/series/SeriesReader.java`
#### Snippet
```java
    @Override
    public boolean isTakeSeqAsFirst(
        Statistics<? extends Object> seqStatistics, Statistics<? extends Object> unseqStatistics) {
      return seqStatistics.getStartTime() < unseqStatistics.getStartTime();
    }
```

### RuleId[ruleID=TypeParameterExtendsObject]
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `server/src/main/java/org/apache/iotdb/db/query/reader/series/SeriesReader.java`
#### Snippet
```java
    @Override
    public boolean isTakeSeqAsFirst(
        Statistics<? extends Object> seqStatistics, Statistics<? extends Object> unseqStatistics) {
      return seqStatistics.getStartTime() < unseqStatistics.getStartTime();
    }
```

### RuleId[ruleID=TypeParameterExtendsObject]
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `server/src/main/java/org/apache/iotdb/db/query/reader/series/SeriesReader.java`
#### Snippet
```java
    @Override
    public boolean isTakeSeqAsFirst(
        Statistics<? extends Object> seqStatistics, Statistics<? extends Object> unseqStatistics) {
      return seqStatistics.getEndTime() > unseqStatistics.getEndTime();
    }
```

### RuleId[ruleID=TypeParameterExtendsObject]
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `server/src/main/java/org/apache/iotdb/db/query/reader/series/SeriesReader.java`
#### Snippet
```java
    @Override
    public boolean isTakeSeqAsFirst(
        Statistics<? extends Object> seqStatistics, Statistics<? extends Object> unseqStatistics) {
      return seqStatistics.getEndTime() > unseqStatistics.getEndTime();
    }
```

### RuleId[ruleID=TypeParameterExtendsObject]
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `server/src/main/java/org/apache/iotdb/db/query/reader/series/SeriesReader.java`
#### Snippet
```java

    long getCurrentEndPoint(
        Statistics<? extends Object> seqStatistics, Statistics<? extends Object> unseqStatistics);

    boolean isExcessEndpoint(long time, long endpointTime);
```

### RuleId[ruleID=TypeParameterExtendsObject]
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `server/src/main/java/org/apache/iotdb/db/query/reader/series/SeriesReader.java`
#### Snippet
```java

    long getCurrentEndPoint(
        Statistics<? extends Object> seqStatistics, Statistics<? extends Object> unseqStatistics);

    boolean isExcessEndpoint(long time, long endpointTime);
```

### RuleId[ruleID=TypeParameterExtendsObject]
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `server/src/main/java/org/apache/iotdb/db/query/reader/series/SeriesReader.java`
#### Snippet
```java
    boolean isOverlapped(Statistics<? extends Object> left, Statistics<? extends Object> right);

    boolean isOverlapped(long time, Statistics<? extends Object> right);

    boolean isOverlapped(long time, TsFileResource right);
```

### RuleId[ruleID=TypeParameterExtendsObject]
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `server/src/main/java/org/apache/iotdb/db/query/reader/series/SeriesReader.java`
#### Snippet
```java
    @Override
    public long getCurrentEndPoint(
        Statistics<? extends Object> seqStatistics, Statistics<? extends Object> unseqStatistics) {
      return Math.max(seqStatistics.getStartTime(), unseqStatistics.getStartTime());
    }
```

### RuleId[ruleID=TypeParameterExtendsObject]
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `server/src/main/java/org/apache/iotdb/db/query/reader/series/SeriesReader.java`
#### Snippet
```java
    @Override
    public long getCurrentEndPoint(
        Statistics<? extends Object> seqStatistics, Statistics<? extends Object> unseqStatistics) {
      return Math.max(seqStatistics.getStartTime(), unseqStatistics.getStartTime());
    }
```

### RuleId[ruleID=TypeParameterExtendsObject]
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `server/src/main/java/org/apache/iotdb/db/query/reader/series/SeriesReader.java`
#### Snippet
```java
    @Override
    public long getCurrentEndPoint(
        Statistics<? extends Object> seqStatistics, Statistics<? extends Object> unseqStatistics) {
      return Math.min(seqStatistics.getEndTime(), unseqStatistics.getEndTime());
    }
```

### RuleId[ruleID=TypeParameterExtendsObject]
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `server/src/main/java/org/apache/iotdb/db/query/reader/series/SeriesReader.java`
#### Snippet
```java
    @Override
    public long getCurrentEndPoint(
        Statistics<? extends Object> seqStatistics, Statistics<? extends Object> unseqStatistics) {
      return Math.min(seqStatistics.getEndTime(), unseqStatistics.getEndTime());
    }
```

### RuleId[ruleID=TypeParameterExtendsObject]
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `server/src/main/java/org/apache/iotdb/db/query/reader/series/SeriesReader.java`
#### Snippet
```java
    <T> Comparator<T> comparingLong(ToLongFunction<? super T> keyExtractor);

    long getCurrentEndPoint(long time, Statistics<? extends Object> statistics);

    long getCurrentEndPoint(
```

### RuleId[ruleID=TypeParameterExtendsObject]
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `server/src/main/java/org/apache/iotdb/db/query/reader/series/SeriesReader.java`
#### Snippet
```java
  public interface TimeOrderUtils {

    long getOrderTime(Statistics<? extends Object> statistics);

    long getOrderTime(TsFileResource fileResource);
```

### RuleId[ruleID=TypeParameterExtendsObject]
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `server/src/main/java/org/apache/iotdb/db/query/reader/series/SeriesReader.java`
#### Snippet
```java
    long getOverlapCheckTime(Statistics<? extends Object> range);

    boolean isOverlapped(Statistics<? extends Object> left, Statistics<? extends Object> right);

    boolean isOverlapped(long time, Statistics<? extends Object> right);
```

### RuleId[ruleID=TypeParameterExtendsObject]
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `server/src/main/java/org/apache/iotdb/db/query/reader/series/SeriesReader.java`
#### Snippet
```java
    long getOverlapCheckTime(Statistics<? extends Object> range);

    boolean isOverlapped(Statistics<? extends Object> left, Statistics<? extends Object> right);

    boolean isOverlapped(long time, Statistics<? extends Object> right);
```

### RuleId[ruleID=TypeParameterExtendsObject]
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `server/src/main/java/org/apache/iotdb/db/query/reader/series/SeriesReader.java`
#### Snippet
```java
    /** Return true if taking first page reader from seq readers */
    boolean isTakeSeqAsFirst(
        Statistics<? extends Object> seqStatistics, Statistics<? extends Object> unseqStatistics);

    boolean getAscending();
```

### RuleId[ruleID=TypeParameterExtendsObject]
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `server/src/main/java/org/apache/iotdb/db/query/reader/series/SeriesReader.java`
#### Snippet
```java
    /** Return true if taking first page reader from seq readers */
    boolean isTakeSeqAsFirst(
        Statistics<? extends Object> seqStatistics, Statistics<? extends Object> unseqStatistics);

    boolean getAscending();
```

### RuleId[ruleID=TypeParameterExtendsObject]
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `server/src/main/java/org/apache/iotdb/db/query/reader/series/SeriesReader.java`
#### Snippet
```java

    @Override
    public long getCurrentEndPoint(long time, Statistics<? extends Object> statistics) {
      return Math.min(time, statistics.getEndTime());
    }
```

### RuleId[ruleID=TypeParameterExtendsObject]
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `server/src/main/java/org/apache/iotdb/db/query/reader/series/SeriesReader.java`
#### Snippet
```java
    long getOrderTime(TsFileResource fileResource);

    long getOverlapCheckTime(Statistics<? extends Object> range);

    boolean isOverlapped(Statistics<? extends Object> left, Statistics<? extends Object> right);
```

### RuleId[ruleID=TypeParameterExtendsObject]
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `server/src/main/java/org/apache/iotdb/db/query/reader/series/SeriesReader.java`
#### Snippet
```java

    @Override
    public long getCurrentEndPoint(long time, Statistics<? extends Object> statistics) {
      return Math.max(time, statistics.getStartTime());
    }
```

## RuleId[ruleID=OptionalGetWithoutIsPresent]
### RuleId[ruleID=OptionalGetWithoutIsPresent]
`Optional.get()` without 'isPresent()' check
in `server/src/main/java/org/apache/iotdb/db/sync/pipedata/queue/BufferedPipeDataQueue.java`
#### Snippet
```java
      } else {
        long nextStartNumber =
            pipeLogStartNumber.stream().filter(o -> o >= serialNumber).findFirst().get();
        List<PipeData> parsePipeData =
            parsePipeLog(new File(pipeLogDir, SyncPathUtil.getPipeLogName(nextStartNumber)));
```

### RuleId[ruleID=OptionalGetWithoutIsPresent]
`Optional.get()` without 'isPresent()' check
in `confignode/src/main/java/org/apache/iotdb/confignode/manager/node/NodeManager.java`
#### Snippet
```java
            .filter(e -> !e.equals(removeConfigNodePlan.getConfigNodeLocation()))
            .findAny()
            .get();
    ConsensusGenericResponse resp =
        getConsensusManager()
```

## RuleId[ruleID=ConstantValue]
### RuleId[ruleID=ConstantValue]
Condition `deviceIDS == null` is always `true`
in `schema-engine-tag/src/main/java/org/apache/iotdb/db/metadata/tagSchemaRegion/idtable/IDTableWithDeviceIDListImpl.java`
#### Snippet
```java
  public IDTableWithDeviceIDListImpl(File storageGroupDir) {
    super(storageGroupDir);
    if (deviceIDS == null) {
      deviceIDS = new ArrayList<>();
    }
```

### RuleId[ruleID=ConstantValue]
Value `childPath` is always 'null'
in `server/src/main/java/org/apache/iotdb/db/qp/strategy/optimizer/MergeSingleFilterOptimizer.java`
#### Snippet
```java

    // add last null child
    return addLastNullChild(ret, filter, firstNonSingleIndex, childPath);
  }

```

### RuleId[ruleID=ConstantValue]
Value `intoPathContext` is always 'null'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/parser/ASTVisitor.java`
#### Snippet
```java
    } else {
      List<IoTDBSqlParser.NodeNameInIntoPathContext> nodeNames =
          ((IoTDBSqlParser.SuffixPathInIntoPathContext) intoPathContext).nodeNameInIntoPath();
      String[] path = new String[nodeNames.size()];
      for (int i = 0; i < nodeNames.size(); i++) {
```

### RuleId[ruleID=ConstantValue]
Value `withTags` is always 'true'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/ClusterSchemaFetcher.java`
#### Snippet
```java

    if (withTags) {
      return executeSchemaFetchQuery(new SchemaFetchStatement(patternTree, templateMap, withTags));
    }

```

### RuleId[ruleID=ConstantValue]
Value `withTags` is always 'false'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/ClusterSchemaFetcher.java`
#### Snippet
```java

    if (fullPathList.isEmpty()) {
      return executeSchemaFetchQuery(new SchemaFetchStatement(patternTree, templateMap, withTags));
    }

```

### RuleId[ruleID=ConstantValue]
Value `withTags` is always 'false'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/ClusterSchemaFetcher.java`
#### Snippet
```java

      schemaTree =
          executeSchemaFetchQuery(new SchemaFetchStatement(patternTree, templateMap, withTags));

      // only cache the schema fetched by full path
```

### RuleId[ruleID=ConstantValue]
Value `childPath` is always 'null'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/rewriter/MergeSingleFilterOptimizer.java`
#### Snippet
```java

    // add last null child
    return addLastNullChild(ret, filter, firstNonSingleIndex, childPath);
  }

```

### RuleId[ruleID=ConstantValue]
Condition `isFinal` is always `false`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/distribution/SourceRewriter.java`
#### Snippet
```java
          .forEach(
              d -> {
                d.setStep(isFinal ? AggregationStep.FINAL : AggregationStep.PARTIAL);
                LogicalPlanBuilder.updateTypeProviderByPartialAggregation(
                    d, context.queryContext.getTypeProvider());
```

### RuleId[ruleID=ConstantValue]
Value `inFlightException` is always 'null'
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/driver/Driver.java`
#### Snippet
```java
      inFlightException =
          addSuppressedException(
              inFlightException,
              t,
              "Error closing operator {} for fragment instance {}",
```

### RuleId[ruleID=ConstantValue]
Value `checkMessage` is always 'null'
in `server/src/main/java/org/apache/iotdb/db/auth/StandaloneAuthorityFetcher.java`
#### Snippet
```java
      return RpcUtils.getStatus(TSStatusCode.SUCCESS_STATUS);
    } else {
      return RpcUtils.getStatus(TSStatusCode.NO_PERMISSION, checkMessage);
    }
  }
```

### RuleId[ruleID=ConstantValue]
Condition `finalCommitIndex >= 0` is always `true` when reached
in `server/src/main/java/org/apache/iotdb/db/sync/externalpipe/ExtPipePluginManager.java`
#### Snippet
```java
      }

      if ((finalCommitIndex < Long.MAX_VALUE) && (finalCommitIndex >= 0)) {
        pipeOpManager.commitData(sgName, finalCommitIndex);
      }
```

### RuleId[ruleID=ConstantValue]
Value `pageHeaderHasStatistic` is always 'true'
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/TsFileOpBlock.java`
#### Snippet
```java
      long pagePosInTsFile = tsFileFullSeqReader.position();
      PageHeader pageHeader =
          tsFileFullSeqReader.readPageHeader(tsDataType, pageHeaderHasStatistic);

      int pageSize = pageHeader.getSerializedPageSize();
```

### RuleId[ruleID=ConstantValue]
Value `ascending` is always 'false'
in `server/src/main/java/org/apache/iotdb/db/query/executor/groupby/SlidingWindowGroupByExecutorFactory.java`
#### Snippet
```java
      case SQLConstant.FIRST_VALUE:
        return !ascending
            ? new EmptyQueueSlidingWindowGroupByExecutor(dataType, aggrFuncName, ascending)
            : new NormalQueueSlidingWindowGroupByExecutor(dataType, aggrFuncName, ascending);
      case SQLConstant.MAX_TIME:
```

### RuleId[ruleID=ConstantValue]
Value `ascending` is always 'true'
in `server/src/main/java/org/apache/iotdb/db/query/executor/groupby/SlidingWindowGroupByExecutorFactory.java`
#### Snippet
```java
        return !ascending
            ? new EmptyQueueSlidingWindowGroupByExecutor(dataType, aggrFuncName, ascending)
            : new NormalQueueSlidingWindowGroupByExecutor(dataType, aggrFuncName, ascending);
      case SQLConstant.MAX_TIME:
      case SQLConstant.LAST_VALUE:
```

### RuleId[ruleID=ConstantValue]
Value `ascending` is always 'false'
in `server/src/main/java/org/apache/iotdb/db/query/executor/groupby/SlidingWindowGroupByExecutorFactory.java`
#### Snippet
```java
      case SQLConstant.LAST_VALUE:
        return !ascending
            ? new NormalQueueSlidingWindowGroupByExecutor(dataType, aggrFuncName, ascending)
            : new EmptyQueueSlidingWindowGroupByExecutor(dataType, aggrFuncName, ascending);
      default:
```

### RuleId[ruleID=ConstantValue]
Value `ascending` is always 'true'
in `server/src/main/java/org/apache/iotdb/db/query/executor/groupby/SlidingWindowGroupByExecutorFactory.java`
#### Snippet
```java
        return !ascending
            ? new NormalQueueSlidingWindowGroupByExecutor(dataType, aggrFuncName, ascending)
            : new EmptyQueueSlidingWindowGroupByExecutor(dataType, aggrFuncName, ascending);
      default:
        throw new IllegalArgumentException("Invalid Aggregation Type: " + aggrFuncName);
```

### RuleId[ruleID=ConstantValue]
Condition `writer != null` is always `true`
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileSplitTool.java`
#### Snippet
```java

          List<ChunkMetadata> chunkMetadataList = reader.getChunkMetadataList(path);
          assert writer != null;

          ChunkMetadata firstChunkMetadata = chunkMetadataList.get(0);
```

### RuleId[ruleID=ConstantValue]
Value `isSeq` is always 'true'
in `server/src/main/java/org/apache/iotdb/db/engine/StorageEngineV2.java`
#### Snippet
```java
                cachedThreadPool.submit(
                    () -> {
                      dataRegion.syncCloseOneTsFileProcessor(isSeq, tsFileProcessor);
                      return null;
                    }));
```

### RuleId[ruleID=ConstantValue]
Value `isSeq` is always 'false'
in `server/src/main/java/org/apache/iotdb/db/engine/StorageEngineV2.java`
#### Snippet
```java
                cachedThreadPool.submit(
                    () -> {
                      dataRegion.syncCloseOneTsFileProcessor(isSeq, tsFileProcessor);
                      return null;
                    }));
```

### RuleId[ruleID=ConstantValue]
Condition `this.sealedTsFileResources != null` is always `true`
in `server/src/main/java/org/apache/iotdb/db/rescon/TsFileResourceManager.java`
#### Snippet
```java
  /** function for clearing TsFileManager */
  public synchronized void clear() {
    if (this.sealedTsFileResources != null) {
      this.sealedTsFileResources.clear();
    }
```

### RuleId[ruleID=ConstantValue]
Condition `memIncrements != null` is always `true` when reached
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileProcessor.java`
#### Snippet
```java
        results[i] = RpcUtils.getStatus(TSStatusCode.INTERNAL_SERVER_ERROR, e.getMessage());
      }
      if (enableMemControl && memIncrements != null) {
        rollbackMemoryInfo(memIncrements);
      }
```

### RuleId[ruleID=ConstantValue]
Condition `memIncrements != null` is always `true` when reached
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileProcessor.java`
#### Snippet
```java
      }
    } catch (Exception e) {
      if (enableMemControl && memIncrements != null) {
        rollbackMemoryInfo(memIncrements);
      }
```

### RuleId[ruleID=ConstantValue]
Value `clientAuth` is always 'true'
in `server/src/main/java/org/apache/iotdb/db/protocol/rest/RestService.java`
#### Snippet
```java
      sslContextFactory.setTrustStorePath(trustStorePath);
      sslContextFactory.setTrustStorePassword(trustStorePwd);
      sslContextFactory.setNeedClientAuth(clientAuth);
    }

```

### RuleId[ruleID=ConstantValue]
Condition `isSuccess` is always `true`
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/SchemaRegionMemoryImpl.java`
#### Snippet
```java

    long mtreeSnapshotStartTime = System.currentTimeMillis();
    isSuccess = isSuccess && mtree.createSnapshot(snapshotDir);
    logger.info(
        "MTree snapshot creation of schemaRegion {} costs {}ms.",
```

### RuleId[ruleID=ConstantValue]
Condition `v == null` is always `false`
in `library-udf/src/main/java/org/apache/iotdb/library/anomaly/util/WindowDetect.java`
#### Snippet
```java
      Double v = Util.getValueAsDouble(row);
      timeList.add(row.getTime());
      if (v == null || !Double.isFinite(v)) {
        originList.add(Double.NaN);
      } else {
```

### RuleId[ruleID=ConstantValue]
Condition `isSuccess` is always `true`
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/SchemaRegionSchemaFileImpl.java`
#### Snippet
```java

    long mtreeSnapshotStartTime = System.currentTimeMillis();
    isSuccess = isSuccess && mtree.createSnapshot(snapshotDir);
    logger.info(
        "MTree snapshot creation of schemaRegion {} costs {}ms.",
```

### RuleId[ruleID=ConstantValue]
Condition `rootProcId == null` is always `false`
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/ProcedureExecutor.java`
#### Snippet
```java
    }
    final Long rootProcId = getRootProcedureId(proc);
    if (rootProcId == null) {
      LOG.warn("Rollback because parent is done/rolledback, proc is {}", proc);
      executeRollback(proc);
```

### RuleId[ruleID=ConstantValue]
Condition `i > 0` is always `true`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/filter/operator/Like.java`
#### Snippet
```java
        }
        if ((i == 0)
            || (i > 0 && !"\\".equals(String.valueOf(unescapeValue.charAt(i - 1))))
            || (i >= 2
                && "\\\\"
```

### RuleId[ruleID=ConstantValue]
Value `obj` is always 'null'
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/RSchemaUtils.java`
#### Snippet
```java
    Object obj = null;
    if (valueType.getFlag() != null && (filter & valueType.getFlag()) == 0) {
      return obj;
    }

```

### RuleId[ruleID=ConstantValue]
Condition `initialCapacity > 0` is always `true`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/RamUsageEstimator.java`
#### Snippet
```java
      initialCapacity = Math.max(MIN_CAPACITY, initialCapacity);

      assert initialCapacity > 0
          : "Initial capacity must be between (0, " + Integer.MAX_VALUE + "].";
      assert loadFactor > 0 && loadFactor < 1 : "Load factor must be between (0, 1).";
```

### RuleId[ruleID=ConstantValue]
Condition `measurementsToIndexMap.size() - 1 >= 0` is always `true`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/schema/VectorMeasurementSchema.java`
#### Snippet
```java
    measurementsToIndexMap.put(measurementId, measurementsToIndexMap.size());
    byte[] typesInByte = new byte[measurementsToIndexMap.size()];
    if (measurementsToIndexMap.size() - 1 >= 0) {
      System.arraycopy(types, 0, typesInByte, 0, measurementsToIndexMap.size() - 1);
    }
```

### RuleId[ruleID=ConstantValue]
Condition `measurementsToIndexMap.size() - 1 >= 0` is always `true`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/schema/VectorMeasurementSchema.java`
#### Snippet
```java
    this.types = typesInByte;
    byte[] encodingsInByte = new byte[measurementsToIndexMap.size()];
    if (measurementsToIndexMap.size() - 1 >= 0) {
      System.arraycopy(encodings, 0, encodingsInByte, 0, measurementsToIndexMap.size() - 1);
    }
```

### RuleId[ruleID=ConstantValue]
Condition `urlSet != null` is always `true`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/common/conf/TSFileDescriptor.java`
#### Snippet
```java
    try {
      Set<URL> urlSet = Loader.getResources(resource, classLoader);
      if (urlSet != null && urlSet.size() > 1) {
        logger.warn("Resource [{}] occurs multiple times on the classpath", resource);
        for (URL url : urlSet) {
```

### RuleId[ruleID=ConstantValue]
Condition `shouldRetry` is always `true`
in `consensus/src/main/java/org/apache/iotdb/consensus/ratis/ApplicationStateMachineProxy.java`
#### Snippet
```java
        }
      }
    } while (shouldRetry);

    return CompletableFuture.completedFuture(ret);
```

## RuleId[ruleID=MethodOverridesStaticMethod]
### RuleId[ruleID=MethodOverridesStaticMethod]
Method `deserialize()` tries to override a static method of a superclass
in `node-commons/src/main/java/org/apache/iotdb/commons/path/AlignedPath.java`
#### Snippet
```java
  }

  public static AlignedPath deserialize(ByteBuffer byteBuffer) {
    PartialPath partialPath = PartialPath.deserialize(byteBuffer);
    AlignedPath alignedPath = new AlignedPath();
```

### RuleId[ruleID=MethodOverridesStaticMethod]
Method `deserialize()` tries to override a static method of a superclass
in `node-commons/src/main/java/org/apache/iotdb/commons/path/MeasurementPath.java`
#### Snippet
```java
  }

  public static MeasurementPath deserialize(ByteBuffer byteBuffer) {
    PartialPath partialPath = PartialPath.deserialize(byteBuffer);
    MeasurementPath measurementPath = new MeasurementPath();
```

### RuleId[ruleID=MethodOverridesStaticMethod]
Method `deserialize()` tries to override a static method of a superclass
in `node-commons/src/main/java/org/apache/iotdb/commons/path/PartialPath.java`
#### Snippet
```java
  // Attention!!! If you want to use serialize and deserialize of partialPath, must invoke
  // PathDeserializeUtil.deserialize
  public static PartialPath deserialize(ByteBuffer byteBuffer) {
    Path path = Path.deserialize(byteBuffer);
    PartialPath partialPath = new PartialPath();
```

### RuleId[ruleID=MethodOverridesStaticMethod]
Method `deserialize()` tries to override a static method of a superclass
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/process/FilterNode.java`
#### Snippet
```java
  }

  public static FilterNode deserialize(ByteBuffer byteBuffer) {
    int outputExpressionsLength = ReadWriteIOUtils.readInt(byteBuffer);
    Expression[] outputExpressions = new Expression[outputExpressionsLength];
```

### RuleId[ruleID=MethodOverridesStaticMethod]
Method `deserialize()` tries to override a static method of a superclass
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/parameter/CrossSeriesAggregationDescriptor.java`
#### Snippet
```java
  }

  public static CrossSeriesAggregationDescriptor deserialize(ByteBuffer byteBuffer) {
    AggregationDescriptor aggregationDescriptor = AggregationDescriptor.deserialize(byteBuffer);
    Expression outputExpression = Expression.deserialize(byteBuffer);
```

### RuleId[ruleID=MethodOverridesStaticMethod]
Method `deserialize()` tries to override a static method of a superclass
in `server/src/main/java/org/apache/iotdb/db/mpp/common/filter/BasicFunctionFilter.java`
#### Snippet
```java
  }

  public static BasicFunctionFilter deserialize(ByteBuffer byteBuffer) {
    QueryFilter queryFilter = QueryFilter.deserialize(byteBuffer);
    BasicFunctionFilter basicFunctionFilter =
```

### RuleId[ruleID=MethodOverridesStaticMethod]
Method `deserialize()` tries to override a static method of a superclass
in `server/src/main/java/org/apache/iotdb/db/mpp/common/filter/RegexpFilter.java`
#### Snippet
```java
  }

  public static RegexpFilter deserialize(ByteBuffer byteBuffer) {
    QueryFilter queryFilter = QueryFilter.deserialize(byteBuffer);
    String value = ReadWriteIOUtils.readString(byteBuffer);
```

### RuleId[ruleID=MethodOverridesStaticMethod]
Method `deserialize()` tries to override a static method of a superclass
in `server/src/main/java/org/apache/iotdb/db/mpp/common/filter/FunctionFilter.java`
#### Snippet
```java
  }

  public static FunctionFilter deserialize(ByteBuffer byteBuffer) {
    int filterTypeIndex = ReadWriteIOUtils.readInt(byteBuffer);
    int childSize = ReadWriteIOUtils.readInt(byteBuffer);
```

### RuleId[ruleID=MethodOverridesStaticMethod]
Method `deserialize()` tries to override a static method of a superclass
in `server/src/main/java/org/apache/iotdb/db/mpp/common/filter/LikeFilter.java`
#### Snippet
```java
  }

  public static LikeFilter deserialize(ByteBuffer byteBuffer) {
    QueryFilter queryFilter = QueryFilter.deserialize(byteBuffer);
    LikeFilter likeFilter =
```

### RuleId[ruleID=MethodOverridesStaticMethod]
Method `deserialize()` tries to override a static method of a superclass
in `server/src/main/java/org/apache/iotdb/db/mpp/common/filter/InFilter.java`
#### Snippet
```java
  }

  public static InFilter deserialize(ByteBuffer byteBuffer) {
    QueryFilter queryFilter = QueryFilter.deserialize(byteBuffer);
    boolean not = ReadWriteIOUtils.readBool(byteBuffer);
```

### RuleId[ruleID=MethodOverridesStaticMethod]
Method `deserialize()` tries to override a static method of a superclass
in `server/src/main/java/org/apache/iotdb/db/mpp/common/schematree/node/SchemaEntityNode.java`
#### Snippet
```java
  }

  public static SchemaEntityNode deserialize(InputStream inputStream) throws IOException {
    String name = ReadWriteIOUtils.readString(inputStream);
    boolean isAligned = ReadWriteIOUtils.readBool(inputStream);
```

### RuleId[ruleID=MethodOverridesStaticMethod]
Method `deserialize()` tries to override a static method of a superclass
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/BooleanTVList.java`
#### Snippet
```java
  }

  public static BooleanTVList deserialize(DataInputStream stream) throws IOException {
    BooleanTVList tvList = BooleanTVList.newList();
    int rowCount = stream.readInt();
```

### RuleId[ruleID=MethodOverridesStaticMethod]
Method `deserialize()` tries to override a static method of a superclass
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/DoubleTVList.java`
#### Snippet
```java
  }

  public static DoubleTVList deserialize(DataInputStream stream) throws IOException {
    DoubleTVList tvList = DoubleTVList.newList();
    int rowCount = stream.readInt();
```

### RuleId[ruleID=MethodOverridesStaticMethod]
Method `deserialize()` tries to override a static method of a superclass
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/IntTVList.java`
#### Snippet
```java
  }

  public static IntTVList deserialize(DataInputStream stream) throws IOException {
    IntTVList tvList = IntTVList.newList();
    int rowCount = stream.readInt();
```

### RuleId[ruleID=MethodOverridesStaticMethod]
Method `deserialize()` tries to override a static method of a superclass
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/FloatTVList.java`
#### Snippet
```java
  }

  public static FloatTVList deserialize(DataInputStream stream) throws IOException {
    FloatTVList tvList = FloatTVList.newList();
    int rowCount = stream.readInt();
```

### RuleId[ruleID=MethodOverridesStaticMethod]
Method `deserialize()` tries to override a static method of a superclass
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/BinaryTVList.java`
#### Snippet
```java
  }

  public static BinaryTVList deserialize(DataInputStream stream) throws IOException {
    BinaryTVList tvList = BinaryTVList.newList();
    int rowCount = stream.readInt();
```

### RuleId[ruleID=MethodOverridesStaticMethod]
Method `deserialize()` tries to override a static method of a superclass
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/LongTVList.java`
#### Snippet
```java
  }

  public static LongTVList deserialize(DataInputStream stream) throws IOException {
    LongTVList tvList = LongTVList.newList();
    int rowCount = stream.readInt();
```

### RuleId[ruleID=MethodOverridesStaticMethod]
Method `deserialize()` tries to override a static method of a superclass
in `server/src/main/java/org/apache/iotdb/db/utils/datastructure/AlignedTVList.java`
#### Snippet
```java
  }

  public static AlignedTVList deserialize(DataInputStream stream) throws IOException {
    int dataTypeNum = stream.readInt();
    List<TSDataType> dataTypes = new ArrayList<>(dataTypeNum);
```

### RuleId[ruleID=MethodOverridesStaticMethod]
Method `convertFromRpcTDataPartitionReq()` tries to override a static method of a superclass
in `confignode/src/main/java/org/apache/iotdb/confignode/consensus/request/read/partition/GetOrCreateDataPartitionPlan.java`
#### Snippet
```java
   * @return GetOrCreateDataPartitionPlan
   */
  public static GetOrCreateDataPartitionPlan convertFromRpcTDataPartitionReq(
      TDataPartitionReq req) {
    return new GetOrCreateDataPartitionPlan(new ConcurrentHashMap<>(req.getPartitionSlotsMap()));
```

## RuleId[ruleID=StringConcatenationInsideStringBufferAppend]
### RuleId[ruleID=StringConcatenationInsideStringBufferAppend]
String concatenation as argument to `StringBuilder.append()` call
in `example/session/src/main/java/org/apache/iotdb/SessionPoolExample.java`
#### Snippet
```java
                StringBuilder builder = new StringBuilder();
                for (String columnName : wrapper.getColumnNames()) {
                  builder.append(dataIterator.getString(columnName) + " ");
                }
                System.out.println(builder);
```

### RuleId[ruleID=StringConcatenationInsideStringBufferAppend]
String concatenation as argument to `StringBuilder.append()` call
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/Procedure.java`
#### Snippet
```java
    }
    if (hasException()) {
      sb.append(", exception=" + getException());
    }

```

## RuleId[ruleID=IOResource]
### RuleId[ruleID=IOResource]
'DataOutputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/SerializeUtils.java`
#### Snippet
```java
    // TODO-Cluster: replace with a no-copy method
    ByteArrayOutputStream byteArrayOutputStream = new ByteArrayOutputStream();
    DataOutputStream dataOutputStream = new DataOutputStream(byteArrayOutputStream);
    try {
      for (long aLong : longs) {
```

### RuleId[ruleID=IOResource]
'DataOutputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
    for (int i = 0; i < columnNumWithTime; i++) {
      byteArrayOutputStreams[i] = new ByteArrayOutputStream();
      dataOutputStreams[i] = new DataOutputStream(byteArrayOutputStreams[i]);
    }
    int rowCount = 0;
```

### RuleId[ruleID=IOResource]
'DataOutputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `server/src/main/java/org/apache/iotdb/db/utils/QueryDataSetUtils.java`
#### Snippet
```java
    for (int i = 0; i < columnNumWithTime; i++) {
      byteArrayOutputStreams[i] = new ByteArrayOutputStream();
      dataOutputStreams[i] = new DataOutputStream(byteArrayOutputStreams[i]);
    }

```

### RuleId[ruleID=IOResource]
'DataOutputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `server/src/main/java/org/apache/iotdb/db/utils/QueryDataSetUtils.java`
#### Snippet
```java
    for (int i = 0; i < columnNumWithTime; i++) {
      byteArrayOutputStreams[i] = new ByteArrayOutputStream();
      dataOutputStreams[i] = new DataOutputStream(byteArrayOutputStreams[i]);
    }

```

### RuleId[ruleID=IOResource]
'DataOutputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `server/src/main/java/org/apache/iotdb/db/metadata/plan/schemaregion/impl/SchemaRegionPlanTxtSerializer.java`
#### Snippet
```java
  @Override
  public void serialize(ISchemaRegionPlan plan, OutputStream outputStream) throws IOException {
    DataOutputStream dataOutputStream = new DataOutputStream(outputStream);
    StringBuilder stringBuilder = new StringBuilder();
    // serialize plan type
```

### RuleId[ruleID=IOResource]
'PrintWriter' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/SchemaFile.java`
#### Snippet
```java
            lastPageIndex + 1);
    if (pw == null) {
      pw = new PrintWriter(System.out);
    }
    pw.print(header);
```

### RuleId[ruleID=IOResource]
'RandomAccessFile' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/SchemaFile.java`
#### Snippet
```java
    pmtFile.createNewFile();

    channel = new RandomAccessFile(pmtFile, "rw").getChannel();
    headerContent = ByteBuffer.allocate(SchemaFileConfig.FILE_HEADER_SIZE);
    initFileHeader();
```

### RuleId[ruleID=IOResource]
'RandomAccessFile' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/SchemaFile.java`
#### Snippet
```java
    }

    this.channel = new RandomAccessFile(pmtFile, "rw").getChannel();
    this.headerContent = ByteBuffer.allocate(SchemaFileConfig.FILE_HEADER_SIZE);
    // will be overwritten if to init
```

### RuleId[ruleID=IOResource]
'RandomAccessFile' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/SchemaFile.java`
#### Snippet
```java
    filePath = pmtFile.getPath();
    logPath = file.getParent() + File.separator + MetadataConstant.SCHEMA_LOG_FILE_NAME;
    channel = new RandomAccessFile(file, "rw").getChannel();
    headerContent = ByteBuffer.allocate(SchemaFileConfig.FILE_HEADER_SIZE);

```

## RuleId[ruleID=RedundantLengthCheck]
### RuleId[ruleID=RedundantLengthCheck]
Redundant array length check
in `cli/src/main/java/org/apache/iotdb/tool/ImportCsv.java`
#### Snippet
```java
    }
    final String[] opTypeInferValues = commandLine.getOptionValues(TYPE_INFER_ARGS);
    if (opTypeInferValues != null && opTypeInferValues.length > 0) {
      for (String opTypeInferValue : opTypeInferValues) {
        if (opTypeInferValue.contains("=")) {
```

### RuleId[ruleID=RedundantLengthCheck]
Redundant array length check
in `consensus/src/main/java/org/apache/iotdb/consensus/multileader/logdispatcher/IndexController.java`
#### Snippet
```java
    File directory = new File(storageDir);
    File[] versionFiles = directory.listFiles((dir, name) -> name.startsWith(prefix));
    if (versionFiles != null && versionFiles.length > 0) {
      for (File versionFile : versionFiles) {
        Files.delete(versionFile.toPath());
```

## RuleId[ruleID=FieldMayBeStatic]
### RuleId[ruleID=FieldMayBeStatic]
Field `TAIL` may be 'static'
in `schema-engine-tag/src/main/java/org/apache/iotdb/db/metadata/tagSchemaRegion/TagSchemaRegion.java`
#### Snippet
```java

  // when a path ends with ".**", it represents batch processing
  private final String TAIL = ".**";

  private final IStorageGroupMNode storageGroupMNode;
```

### RuleId[ruleID=FieldMayBeStatic]
Field `CONFIG_NODE_LIST` may be 'static'
in `server/src/main/java/org/apache/iotdb/db/client/ConfigNodeInfo.java`
#### Snippet
```java
  private static final Logger logger = LoggerFactory.getLogger(ConfigNodeInfo.class);

  private final String CONFIG_NODE_LIST = "config_node_list";

  private static final String PROPERTIES_FILE_NAME = "system.properties";
```

### RuleId[ruleID=FieldMayBeStatic]
Field `SELECT_WARN_THRESHOLD` may be 'static'
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/cross/rewrite/RewriteCrossSpaceCompactionSelector.java`
#### Snippet
```java
      LoggerFactory.getLogger(IoTDBConstant.COMPACTION_LOGGER_NAME);
  private static final IoTDBConfig config = IoTDBDescriptor.getInstance().getConfig();
  private final int SELECT_WARN_THRESHOLD = 10;
  protected String logicalStorageGroupName;
  protected String dataRegionId;
```

### RuleId[ruleID=FieldMayBeStatic]
Field `threshold` may be 'static'
in `library-udf/src/main/java/org/apache/iotdb/library/anomaly/util/StreamMissDetector.java`
#### Snippet
```java
  private final BooleanCircularQueue labelBuffer = new BooleanCircularQueue();
  private final SimpleRegression regression = new SimpleRegression();
  private final double threshold = 0.9999;
  private int minLength;
  private int state;
```

### RuleId[ruleID=FieldMayBeStatic]
Field `eps` may be 'static'
in `library-udf/src/main/java/org/apache/iotdb/library/drepair/util/LsGreedy.java`
#### Snippet
```java

  private double center = 0, sigma;
  private final double eps = 1e-12;

  public LsGreedy(RowIterator dataIterator) throws Exception {
```

### RuleId[ruleID=FieldMayBeStatic]
Field `snapshotFileName` may be 'static'
in `confignode/src/main/java/org/apache/iotdb/confignode/persistence/TriggerInfo.java`
#### Snippet
```java
  private final ReentrantLock triggerTableLock = new ReentrantLock();

  private final String snapshotFileName = "trigger_info.bin";

  public TriggerInfo() throws IOException {
```

### RuleId[ruleID=FieldMayBeStatic]
Field `snapshotFileName` may be 'static'
in `confignode/src/main/java/org/apache/iotdb/confignode/persistence/UDFInfo.java`
#### Snippet
```java
  private final ReentrantLock udfTableLock = new ReentrantLock();

  private final String snapshotFileName = "udf_info.bin";

  public UDFInfo() throws IOException {
```

### RuleId[ruleID=FieldMayBeStatic]
Field `snapshotFileName` may be 'static'
in `confignode/src/main/java/org/apache/iotdb/confignode/persistence/schema/TemplateTable.java`
#### Snippet
```java
  private final Map<Integer, Template> templateIdMap = new ConcurrentHashMap<>();

  private final String snapshotFileName = "template_info.bin";

  public TemplateTable() {
```

### RuleId[ruleID=FieldMayBeStatic]
Field `snapshotFileName` may be 'static'
in `confignode/src/main/java/org/apache/iotdb/confignode/persistence/node/NodeInfo.java`
#### Snippet
```java
  private final Map<Integer, TDataNodeConfiguration> registeredDataNodes;

  private final String snapshotFileName = "node_info.bin";

  public NodeInfo() {
```

### RuleId[ruleID=FieldMayBeStatic]
Field `snapshotFileName` may be 'static'
in `confignode/src/main/java/org/apache/iotdb/confignode/persistence/schema/ClusterSchemaInfo.java`
#### Snippet
```java
  private final ConfigMTree mTree;

  private final String snapshotFileName = "cluster_schema.bin";

  private final TemplateTable templateTable;
```

### RuleId[ruleID=FieldMayBeStatic]
Field `snapshotFileName` may be 'static'
in `confignode/src/main/java/org/apache/iotdb/confignode/persistence/partition/PartitionInfo.java`
#### Snippet
```java
  private final List<RegionMaintainTask> regionMaintainTaskList;

  private final String snapshotFileName = "partition_info.bin";

  public PartitionInfo() {
```

### RuleId[ruleID=FieldMayBeStatic]
Field `TAB` may be 'static'
in `integration-test/src/main/java/org/apache/iotdb/it/env/AbstractNodeWrapper.java`
#### Snippet
```java
  protected final int[] portList;
  protected final int jmxPort;
  private final String TAB = "  ";
  private Process instance;
  private String node_address;
```

### RuleId[ruleID=FieldMayBeStatic]
Field `NODE_NETWORK_TIMEOUT_MS` may be 'static'
in `integration-test/src/main/java/org/apache/iotdb/it/env/AbstractEnv.java`
#### Snippet
```java
  private final int NODE_START_TIMEOUT = 100;
  private final int PROBE_TIMEOUT_MS = 2000;
  private final int NODE_NETWORK_TIMEOUT_MS = 65_000;
  private final Random rand = new Random();
  protected List<ConfigNodeWrapper> configNodeWrapperList = Collections.emptyList();
```

### RuleId[ruleID=FieldMayBeStatic]
Field `NODE_START_TIMEOUT` may be 'static'
in `integration-test/src/main/java/org/apache/iotdb/it/env/AbstractEnv.java`
#### Snippet
```java
public abstract class AbstractEnv implements BaseEnv {
  private static final Logger logger = IoTDBTestLogger.logger;
  private final int NODE_START_TIMEOUT = 100;
  private final int PROBE_TIMEOUT_MS = 2000;
  private final int NODE_NETWORK_TIMEOUT_MS = 65_000;
```

### RuleId[ruleID=FieldMayBeStatic]
Field `PROBE_TIMEOUT_MS` may be 'static'
in `integration-test/src/main/java/org/apache/iotdb/it/env/AbstractEnv.java`
#### Snippet
```java
  private static final Logger logger = IoTDBTestLogger.logger;
  private final int NODE_START_TIMEOUT = 100;
  private final int PROBE_TIMEOUT_MS = 2000;
  private final int NODE_NETWORK_TIMEOUT_MS = 65_000;
  private final Random rand = new Random();
```

### RuleId[ruleID=FieldMayBeStatic]
Field `TMP_PREFIX` may be 'static'
in `consensus/src/main/java/org/apache/iotdb/consensus/ratis/SnapshotStorage.java`
#### Snippet
```java
  private final IStateMachine applicationStateMachine;

  private final String TMP_PREFIX = ".tmp.";
  private File stateMachineDir;

```

## RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `StepTracker` has only 'static' members, and lacks a 'private' constructor
in `node-commons/src/main/java/org/apache/iotdb/commons/StepTracker.java`
#### Snippet
```java
}

public class StepTracker {
  private static final ThreadLocal<Map<String, Metric>> metrics = new ThreadLocal<>();

```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `PathDeserializeUtil` has only 'static' members, and lacks a 'private' constructor
in `node-commons/src/main/java/org/apache/iotdb/commons/path/PathDeserializeUtil.java`
#### Snippet
```java
import java.nio.ByteBuffer;

public class PathDeserializeUtil {

  public static Path deserialize(ByteBuffer buffer) {
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `SyncConstant` has only 'static' members, and lacks a 'private' constructor
in `node-commons/src/main/java/org/apache/iotdb/commons/sync/utils/SyncConstant.java`
#### Snippet
```java
import org.apache.iotdb.rpc.RpcUtils;

public class SyncConstant {
  /** common */
  public static final String UNKNOWN_IP = "UNKNOWN IP";
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `ProcessIdUtils` has only 'static' members, and lacks a 'private' constructor
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/ProcessIdUtils.java`
#### Snippet
```java
import java.lang.management.ManagementFactory;

public class ProcessIdUtils {
  /**
   * There exists no platform-independent way that can be guaranteed to work in all jvm
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `JVMCommonUtils` has only 'static' members, and lacks a 'private' constructor
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/JVMCommonUtils.java`
#### Snippet
```java
import java.util.stream.Stream;

public class JVMCommonUtils {
  /** Default executor pool maximum size. */
  public static final int MAX_EXECUTOR_POOL_SIZE = Math.max(100, getCpuCores() * 5);
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `NodeUrlUtils` has only 'static' members, and lacks a 'private' constructor
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/NodeUrlUtils.java`
#### Snippet
```java
import java.util.StringJoiner;

public class NodeUrlUtils {

  private static final Logger logger = LoggerFactory.getLogger(NodeUrlUtils.class);
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `PathUtils` has only 'static' members, and lacks a 'private' constructor
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/PathUtils.java`
#### Snippet
```java
import java.util.List;

public class PathUtils {

  /**
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `AsymmetricEncryptFactory` has only 'static' members, and lacks a 'private' constructor
in `node-commons/src/main/java/org/apache/iotdb/commons/security/encrypt/AsymmetricEncryptFactory.java`
#### Snippet
```java
import java.lang.reflect.InvocationTargetException;

public class AsymmetricEncryptFactory {
  private static final Logger LOGGER = LoggerFactory.getLogger(AsymmetricEncryptFactory.class);

```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `Factory` has only 'static' members, and lacks a 'private' constructor
in `node-commons/src/main/java/org/apache/iotdb/commons/consensus/ConsensusGroupId.java`
#### Snippet
```java
  }

  public static class Factory {

    public static ConsensusGroupId create(int type, int id) {
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `ScheduledExecutorUtil` has only 'static' members, and lacks a 'private' constructor
in `node-commons/src/main/java/org/apache/iotdb/commons/concurrent/threadpool/ScheduledExecutorUtil.java`
#### Snippet
```java
import java.util.concurrent.TimeUnit;

public class ScheduledExecutorUtil {

  private static final Logger logger = LoggerFactory.getLogger(ScheduledExecutorUtil.class);
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `SessionConfig` has only 'static' members, and lacks a 'private' constructor
in `session/src/main/java/org/apache/iotdb/session/SessionConfig.java`
#### Snippet
```java
import org.apache.iotdb.session.util.Version;

public class SessionConfig {

  public static final String DEFAULT_HOST = "localhost";
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `ThreadUtils` has only 'static' members, and lacks a 'private' constructor
in `session/src/main/java/org/apache/iotdb/session/util/ThreadUtils.java`
#### Snippet
```java
import java.util.concurrent.atomic.AtomicInteger;

public class ThreadUtils {

  /**
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `ParameterUtils` has only 'static' members, and lacks a 'private' constructor
in `influxdb-protocol/src/main/java/org/apache/iotdb/influxdb/protocol/util/ParameterUtils.java`
#### Snippet
```java
package org.apache.iotdb.influxdb.protocol.util;

public class ParameterUtils {

  /**
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `InfluxDBConstant` has only 'static' members, and lacks a 'private' constructor
in `influxdb-protocol/src/main/java/org/apache/iotdb/influxdb/protocol/constant/InfluxDBConstant.java`
#### Snippet
```java
package org.apache.iotdb.influxdb.protocol.constant;

public class InfluxDBConstant {

  public static final String METHOD_NOT_SUPPORTED = "Method not supported.";
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `Utils` has only 'static' members, and lacks a 'private' constructor
in `jdbc/src/main/java/org/apache/iotdb/jdbc/Utils.java`
#### Snippet
```java

/** Utils to convert between thrift format and TsFile format. */
public class Utils {

  static final Pattern URL_PATTERN = Pattern.compile("([^:]+):([0-9]{1,5})(/|\\?.*=.*(&.*=.*)*)?");
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `SessionUtils` has only 'static' members, and lacks a 'private' constructor
in `session/src/main/java/org/apache/iotdb/session/util/SessionUtils.java`
#### Snippet
```java
import static org.apache.iotdb.session.Session.MSG_UNSUPPORTED_DATA_TYPE;

public class SessionUtils {

  private static final Logger logger = LoggerFactory.getLogger(SessionUtils.class);
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `StringUtils` has only 'static' members, and lacks a 'private' constructor
in `jdbc/src/main/java/org/apache/iotdb/jdbc/StringUtils.java`
#### Snippet
```java
import java.math.BigDecimal;

public class StringUtils {

  private static byte[] allBytes = new byte[256];
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `IoTPrinter` has only 'static' members, and lacks a 'private' constructor
in `cli/src/main/java/org/apache/iotdb/cli/utils/IoTPrinter.java`
#### Snippet
```java
import java.util.List;

public class IoTPrinter {
  private static final PrintStream SCREEN_PRINTER = new PrintStream(System.out);

```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `JlineUtils` has only 'static' members, and lacks a 'private' constructor
in `cli/src/main/java/org/apache/iotdb/cli/utils/JlineUtils.java`
#### Snippet
```java
import java.util.stream.IntStream;

public class JlineUtils {

  public static final Pattern SQL_KEYWORD_PATTERN = Pattern.compile("([A-Z_]+)");
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `TsFileForceAppendWrite` has only 'static' members, and lacks a 'private' constructor
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileForceAppendWrite.java`
#### Snippet
```java
import java.nio.file.Files;

public class TsFileForceAppendWrite {

  private static final Logger logger = LoggerFactory.getLogger(TsFileForceAppendWrite.class);
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `TsFileWriteAlignedWithTablet` has only 'static' members, and lacks a 'private' constructor
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileWriteAlignedWithTablet.java`
#### Snippet
```java
import static org.apache.iotdb.tsfile.Constant.SENSOR_3;

public class TsFileWriteAlignedWithTablet {
  private static final Logger logger = LoggerFactory.getLogger(TsFileWriteAlignedWithTablet.class);

```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `TsFileWriteAlignedWithTSRecord` has only 'static' members, and lacks a 'private' constructor
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileWriteAlignedWithTSRecord.java`
#### Snippet
```java
import static org.apache.iotdb.tsfile.Constant.SENSOR_3;

public class TsFileWriteAlignedWithTSRecord {
  private static final Logger logger =
      LoggerFactory.getLogger(TsFileWriteAlignedWithTSRecord.class);
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `TsFileWriteWithTSRecord` has only 'static' members, and lacks a 'private' constructor
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileWriteWithTSRecord.java`
#### Snippet
```java
 * addMeasurement(MeasurementSchema measurementSchema) throws WriteProcessException
 */
public class TsFileWriteWithTSRecord {

  public static void main(String[] args) {
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `TsFileRead` has only 'static' members, and lacks a 'private' constructor
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileRead.java`
#### Snippet
```java
 * TsFileWriteWithTSRecord or TsFileWriteWithTablet to generate the test.tsfile first
 */
public class TsFileRead {

  private static void queryAndPrint(
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `TsFileWriteWithTablet` has only 'static' members, and lacks a 'private' constructor
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileWriteWithTablet.java`
#### Snippet
```java

/** An example of writing data with Tablet to TsFile */
public class TsFileWriteWithTablet {

  private static final Logger logger = LoggerFactory.getLogger(TsFileWriteWithTablet.class);
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `ShowTimeSeriesResultUtils` has only 'static' members, and lacks a 'private' constructor
in `schema-engine-tag/src/main/java/org/apache/iotdb/db/metadata/tagSchemaRegion/utils/ShowTimeSeriesResultUtils.java`
#### Snippet
```java

/** process show timeSeries result */
public class ShowTimeSeriesResultUtils {

  /**
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `MeasurementPathUtils` has only 'static' members, and lacks a 'private' constructor
in `schema-engine-tag/src/main/java/org/apache/iotdb/db/metadata/tagSchemaRegion/utils/MeasurementPathUtils.java`
#### Snippet
```java

/** process MeasurementPath */
public class MeasurementPathUtils {

  /**
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `PathTagConverterUtils` has only 'static' members, and lacks a 'private' constructor
in `schema-engine-tag/src/main/java/org/apache/iotdb/db/metadata/tagSchemaRegion/utils/PathTagConverterUtils.java`
#### Snippet
```java

/** path and tag converter */
public class PathTagConverterUtils {

  /**
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `TsFileSequenceRead` has only 'static' members, and lacks a 'private' constructor
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileSequenceRead.java`
#### Snippet
```java

/** This tool is used to read TsFile sequentially, including nonAligned or aligned timeseries. */
public class TsFileSequenceRead {
  // if you wanna print detailed datas in pages, then turn it true.
  private static boolean printDetail = false;
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `ApplicationContextGenerator` has only 'static' members, and lacks a 'private' constructor
in `schema-engine-tag/src/main/java/org/apache/iotdb/lsm/context/applicationcontext/ApplicationContextGenerator.java`
#### Snippet
```java

/** Used to generate ApplicationContext object based on annotations or configuration files */
public class ApplicationContextGenerator {

  /**
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `PrepareStatementDemo` has only 'static' members, and lacks a 'private' constructor
in `example/jdbc/src/main/java/org/apache/iotdb/PrepareStatementDemo.java`
#### Snippet
```java
import java.sql.Timestamp;

public class PrepareStatementDemo {

  public static void main(String[] args) throws ClassNotFoundException, SQLException {
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `SyntaxConventionRelatedExample` has only 'static' members, and lacks a 'private' constructor
in `example/jdbc/src/main/java/org/apache/iotdb/SyntaxConventionRelatedExample.java`
#### Snippet
```java
import java.util.List;

public class SyntaxConventionRelatedExample {
  /**
   * if you want to create a time series named root.sg1.select, a possible SQL statement would be
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `JDBCExample` has only 'static' members, and lacks a 'private' constructor
in `example/jdbc/src/main/java/org/apache/iotdb/JDBCExample.java`
#### Snippet
```java
import java.sql.Statement;

public class JDBCExample {

  public static void main(String[] args) throws ClassNotFoundException, SQLException {
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `TSocketWrapper` has only 'static' members, and lacks a 'private' constructor
in `service-rpc/src/main/java/org/apache/iotdb/rpc/TSocketWrapper.java`
#### Snippet
```java
 * exception https://issues.apache.org/jira/browse/THRIFT-5412
 */
public class TSocketWrapper {

  public static TSocket wrap(TConfiguration config, String host, int port, int timeout) {
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `TConfigurationConst` has only 'static' members, and lacks a 'private' constructor
in `service-rpc/src/main/java/org/apache/iotdb/rpc/TConfigurationConst.java`
#### Snippet
```java
import org.apache.thrift.TConfiguration;

public class TConfigurationConst {
  // https://github.com/apache/thrift/blob/master/doc/specs/thrift-tconfiguration.md
  public static TConfiguration defaultTConfiguration =
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `TNonblockingSocketWrapper` has only 'static' members, and lacks a 'private' constructor
in `service-rpc/src/main/java/org/apache/iotdb/rpc/TNonblockingSocketWrapper.java`
#### Snippet
```java
 * screen the exception https://issues.apache.org/jira/browse/THRIFT-5412
 */
public class TNonblockingSocketWrapper {

  public static TNonblockingSocket wrap(String host, int port) throws IOException {
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `ExampleUDFConstant` has only 'static' members, and lacks a 'private' constructor
in `integration/src/main/java/org/apache/iotdb/db/query/udf/example/ExampleUDFConstant.java`
#### Snippet
```java
package org.apache.iotdb.db.query.udf.example;

public class ExampleUDFConstant {
  public static final String ACCESS_STRATEGY_KEY = "access";
  public static final String ACCESS_STRATEGY_ROW_BY_ROW = "row-by-row";
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `EnvFactory` has only 'static' members, and lacks a 'private' constructor
in `integration/src/main/java/org/apache/iotdb/integration/env/EnvFactory.java`
#### Snippet
```java
import org.slf4j.LoggerFactory;

public class EnvFactory {
  private static BaseEnv env;
  private static final Logger logger = LoggerFactory.getLogger(EnvFactory.class);
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `ConfigFactory` has only 'static' members, and lacks a 'private' constructor
in `integration/src/main/java/org/apache/iotdb/integration/env/ConfigFactory.java`
#### Snippet
```java
import org.apache.iotdb.itbase.env.BaseConfig;

public class ConfigFactory {
  private static BaseConfig config;

```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `FilterConstant` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/qp/constant/FilterConstant.java`
#### Snippet
```java
import java.util.Map;

public class FilterConstant {

  public static final Map<Integer, FilterType> lexerToFilterType = new HashMap<>();
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `StatementGenerator` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/parser/StatementGenerator.java`
#### Snippet
```java

/** Convert SQL and RPC requests to {@link Statement}. */
public class StatementGenerator {

  public static Statement createStatement(String sql, ZoneId zoneId) {
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `ExpressionUtils` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/ExpressionUtils.java`
#### Snippet
```java
import java.util.List;

public class ExpressionUtils {

  public static List<Expression> reconstructTimeSeriesOperands(
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `ExpressionAnalyzer` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/ExpressionAnalyzer.java`
#### Snippet
```java
import static org.apache.iotdb.db.mpp.plan.analyze.ExpressionUtils.reconstructUnaryExpressions;

public class ExpressionAnalyzer {
  /**
   * Check if all suffix paths in expression are measurements or one-level wildcards, used in ALIGN
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `SelectIntoUtils` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/SelectIntoUtils.java`
#### Snippet
```java
import static org.apache.iotdb.commons.conf.IoTDBConstant.LEVELED_PATH_TEMPLATE_PATTERN;

public class SelectIntoUtils {

  public static PartialPath constructTargetPath(
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `SchemaValidator` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/SchemaValidator.java`
#### Snippet
```java
import java.util.List;

public class SchemaValidator {

  private static final ISchemaFetcher SCHEMA_FETCHER =
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `SubPlanTypeExtractor` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/SubPlanTypeExtractor.java`
#### Snippet
```java
import java.util.List;

public class SubPlanTypeExtractor {

  public static TypeProvider extractor(PlanNode root, TypeProvider allTypes) {
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `PlanNodeUtil` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/PlanNodeUtil.java`
#### Snippet
```java
import java.util.stream.Collectors;

public class PlanNodeUtil {
  private static final String INDENT = "    ";
  private static final String BRO = "  ├──";
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `FilterConstant` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/constant/FilterConstant.java`
#### Snippet
```java
import java.util.Map;

public class FilterConstant {

  public static final Map<Integer, FilterType> lexerToFilterType = new HashMap<>();
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `DataNodeEndPoints` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/constant/DataNodeEndPoints.java`
#### Snippet
```java
import org.apache.iotdb.db.conf.IoTDBDescriptor;

public class DataNodeEndPoints {
  public static final TEndPoint LOCAL_HOST_DATA_BLOCK_ENDPOINT =
      new TEndPoint(
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `FilterDeserializeUtil` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/mpp/common/filter/FilterDeserializeUtil.java`
#### Snippet
```java
import java.nio.ByteBuffer;

public class FilterDeserializeUtil {

  public static QueryFilter deserialize(ByteBuffer buffer) {
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `DatasetHeaderFactory` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/mpp/common/header/DatasetHeaderFactory.java`
#### Snippet
```java
package org.apache.iotdb.db.mpp.common.header;

public class DatasetHeaderFactory {

  public static DatasetHeader getCountStorageGroupHeader() {
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `ColumnHeaderConstant` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/mpp/common/header/ColumnHeaderConstant.java`
#### Snippet
```java
import java.util.List;

public class ColumnHeaderConstant {
  private static final CommonConfig commonConfig = CommonDescriptor.getInstance().getConfig();

```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `SchemaTsBlockUtil` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/schema/SchemaTsBlockUtil.java`
#### Snippet
```java
import static org.apache.iotdb.tsfile.read.common.block.TsBlockBuilderStatus.DEFAULT_MAX_TSBLOCK_SIZE_IN_BYTES;

public class SchemaTsBlockUtil {

  private static final long MAX_SIZE = DEFAULT_MAX_TSBLOCK_SIZE_IN_BYTES;
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `LastQueryUtil` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/process/last/LastQueryUtil.java`
#### Snippet
```java
import java.util.List;

public class LastQueryUtil {

  private static final boolean CACHE_ENABLED =
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `AccumulatorFactory` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/mpp/aggregation/AccumulatorFactory.java`
#### Snippet
```java
import java.util.List;

public class AccumulatorFactory {

  // TODO: Are we going to create different seriesScanOperator based on order by sequence?
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `SlidingWindowAggregatorFactory` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/mpp/aggregation/slidingwindow/SlidingWindowAggregatorFactory.java`
#### Snippet
```java
import java.util.Map;

public class SlidingWindowAggregatorFactory {

  /** comparators used for MonotonicQueueSlidingWindowAggregator */
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `InputRowUtils` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/util/InputRowUtils.java`
#### Snippet
```java
import org.apache.iotdb.db.mpp.transformation.dag.input.IUDFInputDataSet;

public class InputRowUtils {

  /**
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `TransformUtils` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/util/TransformUtils.java`
#### Snippet
```java
import java.util.Optional;

public class TransformUtils {

  public static int compare(CharSequence cs1, CharSequence cs2) {
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `WALFileUtils` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/wal/utils/WALFileUtils.java`
#### Snippet
```java
import static org.apache.iotdb.commons.conf.IoTDBConstant.WAL_VERSION_ID;

public class WALFileUtils {
  /**
   * versionId is a self-incremented id number, helping to maintain the order of wal files.
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `CheckpointFileUtils` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/wal/utils/CheckpointFileUtils.java`
#### Snippet
```java
import static org.apache.iotdb.commons.conf.IoTDBConstant.WAL_VERSION_ID;

public class CheckpointFileUtils {
  /**
   * versionId is a self-incremented id number, helping to maintain the order of checkpoint files
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `PipeSinkFactory` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/sync/sender/pipe/PipeSinkFactory.java`
#### Snippet
```java

// TODO(Ext-pipe): move to subclass of PipeSink
public class PipeSinkFactory {
  public static PipeSink createPipeSink(String type, String name) {
    type = type.toLowerCase();
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `SyncClientFactory` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/sync/transport/client/SyncClientFactory.java`
#### Snippet
```java
import java.net.UnknownHostException;

public class SyncClientFactory {

  private static final Logger logger = LoggerFactory.getLogger(SyncClientFactory.class);
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `SlidingWindowGroupByExecutorFactory` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/query/executor/groupby/SlidingWindowGroupByExecutorFactory.java`
#### Snippet
```java
import java.util.Map;

public class SlidingWindowGroupByExecutorFactory {

  /** comparators used for MonotonicQueueSlidingWindowGroupByExecutor */
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `IoTDBDataDirViewer` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/tools/IoTDBDataDirViewer.java`
#### Snippet
```java
import java.util.TreeSet;

public class IoTDBDataDirViewer {

  public static void main(String[] args) throws IOException {
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `TsFileResourcePrinter` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileResourcePrinter.java`
#### Snippet
```java
 * tsfile.resource file.
 */
public class TsFileResourcePrinter {

  @SuppressWarnings("squid:S106")
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `SchemaFileSketchTool` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/tools/schema/SchemaFileSketchTool.java`
#### Snippet
```java
 * text
 */
public class SchemaFileSketchTool {

  private static final Logger logger = LoggerFactory.getLogger(SchemaFileSketchTool.class);
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `MLogParser` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/tools/schema/MLogParser.java`
#### Snippet
```java

/** parse the binary mlog or snapshot to text */
public class MLogParser {

  private static final Logger logger = LoggerFactory.getLogger(MLogParser.class);
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `WatermarkDetector` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/tools/watermark/WatermarkDetector.java`
#### Snippet
```java
import java.time.ZoneId;

public class WatermarkDetector {

  public static void main(String[] args) throws IOException, LogicalOperatorException {
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `MmapUtil` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/utils/MmapUtil.java`
#### Snippet
```java
import java.nio.MappedByteBuffer;

public class MmapUtil {

  public static void clean(MappedByteBuffer mappedByteBuffer) {
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `IExpressionDeserializeUtil` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/utils/IExpressionDeserializeUtil.java`
#### Snippet
```java
import java.nio.ByteBuffer;

public class IExpressionDeserializeUtil {

  public static IExpression deserialize(ByteBuffer byteBuffer) {
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `ErrorHandlingUtils` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/utils/ErrorHandlingUtils.java`
#### Snippet
```java
import java.util.concurrent.ExecutionException;

public class ErrorHandlingUtils {

  private static final Logger LOGGER = LoggerFactory.getLogger(ErrorHandlingUtils.class);
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `FilesUtils` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/utils/FilesUtils.java`
#### Snippet
```java
import java.util.HashSet;

public class FilesUtils {

  /**
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `TimePartitionUtils` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/utils/TimePartitionUtils.java`
#### Snippet
```java
import org.apache.iotdb.db.conf.ServerConfigConsistent;

public class TimePartitionUtils {
  @ServerConfigConsistent
  public static long timePartitionInterval =
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `ParameterUtils` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/utils/ParameterUtils.java`
#### Snippet
```java
package org.apache.iotdb.db.utils;

public class ParameterUtils {

  /**
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `ThreadUtils` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/utils/ThreadUtils.java`
#### Snippet
```java

/** the utils for managing thread or thread pool */
public class ThreadUtils {

  private static final Logger logger = LoggerFactory.getLogger(StorageEngine.class);
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `SyncPipeUtil` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/utils/sync/SyncPipeUtil.java`
#### Snippet
```java
import java.util.Map;

public class SyncPipeUtil {

  public static PipeSink parseCreatePipeSinkStatement(
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `UpgradeUtils` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/utils/UpgradeUtils.java`
#### Snippet
```java
import java.util.concurrent.locks.ReentrantReadWriteLock;

public class UpgradeUtils {

  private static final Logger logger = LoggerFactory.getLogger(UpgradeUtils.class);
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `DataTypeUtils` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/utils/DataTypeUtils.java`
#### Snippet
```java
import java.util.List;

public class DataTypeUtils {

  public static final String MSG_UNSUPPORTED_DATA_TYPE = "Unsupported data type:";
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `TsFileValidationTool` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/tools/validate/TsFileValidationTool.java`
#### Snippet
```java
 * <p>Timeseries overlap within one page
 */
public class TsFileValidationTool {
  // print detail type of overlap or not
  private static boolean printDetails = false;
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `SettleLog` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/engine/settle/SettleLog.java`
#### Snippet
```java
import java.util.concurrent.locks.ReentrantReadWriteLock;

public class SettleLog {
  private static final Logger logger = LoggerFactory.getLogger(SettleLog.class);
  public static final String COMMA_SEPERATOR = ",";
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `UpgradeLog` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/engine/upgrade/UpgradeLog.java`
#### Snippet
```java
import java.io.IOException;

public class UpgradeLog {

  private static final Logger logger = LoggerFactory.getLogger(UpgradeLog.class);
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `SnapshotFileSet` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/engine/snapshot/SnapshotFileSet.java`
#### Snippet
```java
import java.util.Set;

public class SnapshotFileSet {
  public static final String[] DATA_FILE_SUFFIX =
      new String[] {
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `CompactionScheduler` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/CompactionScheduler.java`
#### Snippet
```java
 * ICompactionSelector#selectCrossSpaceTask(List, List)}} in selector.
 */
public class CompactionScheduler {
  private static final Logger LOGGER =
      LoggerFactory.getLogger(IoTDBConstant.COMPACTION_LOGGER_NAME);
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `CompactionUtils` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/CompactionUtils.java`
#### Snippet
```java
 * timeseries.
 */
public class CompactionUtils {
  private static final Logger logger =
      LoggerFactory.getLogger(IoTDBConstant.COMPACTION_LOGGER_NAME);
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `CompactionExceptionHandler` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/CompactionExceptionHandler.java`
#### Snippet
```java
import java.util.List;

public class CompactionExceptionHandler {
  private static final Logger LOGGER =
      LoggerFactory.getLogger(IoTDBConstant.COMPACTION_LOGGER_NAME);
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `TsFileNameGenerator` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileNameGenerator.java`
#### Snippet
```java
import static org.apache.iotdb.tsfile.common.constant.TsFileConstant.TSFILE_SUFFIX;

public class TsFileNameGenerator {

  private static FSFactory fsFactory = FSFactoryProducer.getFSFactory();
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `GetVersion` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/service/GetVersion.java`
#### Snippet
```java
import org.apache.iotdb.db.conf.IoTDBDescriptor;

public class GetVersion {
  public static void main(String[] args) {
    System.out.println(IoTDBDescriptor.getInstance().getConfig().getIoTDBVersion());
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `DataNodeMetricsHelper` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/service/metrics/DataNodeMetricsHelper.java`
#### Snippet
```java
import org.apache.iotdb.metrics.metricsets.logback.LogbackMetrics;

public class DataNodeMetricsHelper {
  /** Bind predefined metric sets into DataNode */
  public static void bind() {
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `CacheMetricsRecorder` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/service/metrics/recorder/CacheMetricsRecorder.java`
#### Snippet
```java
import org.apache.iotdb.metrics.utils.MetricLevel;

public class CacheMetricsRecorder {

  /**
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `CompactionMetricsRecorder` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/service/metrics/recorder/CompactionMetricsRecorder.java`
#### Snippet
```java
import java.util.concurrent.TimeUnit;

public class CompactionMetricsRecorder {

  public static void recordWriteInfo(
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `TestConstant` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/constant/TestConstant.java`
#### Snippet
```java
import java.io.File;

public class TestConstant {

  public static final String BASE_OUTPUT_PATH = "target".concat(File.separator);
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `PatternTreeMapFactory` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/metadata/path/PatternTreeMapFactory.java`
#### Snippet
```java
import java.util.HashSet;

public class PatternTreeMapFactory {
  public static PatternTreeMap<String, StringSerializer> getTriggerPatternTreeMap() {
    return new PatternTreeMap<>(
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `DataNodeLastCacheManager` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/metadata/cache/DataNodeLastCacheManager.java`
#### Snippet
```java
import org.slf4j.LoggerFactory;

public class DataNodeLastCacheManager {
  private static final Logger logger = LoggerFactory.getLogger(DataNodeLastCacheManager.class);

```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `MNodeUtils` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/metadata/mnode/MNodeUtils.java`
#### Snippet
```java
package org.apache.iotdb.db.metadata.mnode;

public class MNodeUtils {

  /**
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `MNodeContainers` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/metadata/mnode/container/MNodeContainers.java`
#### Snippet
```java
import static java.util.Collections.emptySet;

public class MNodeContainers {

  @SuppressWarnings("rawtypes")
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `SegmentLock` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/SegmentLock.java`
#### Snippet
```java
 * methods are static
 */
public class SegmentLock {

  /**
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `SchemaFileConfig` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/SchemaFileConfig.java`
#### Snippet
```java

/** All public configs about SchemaFile is stated here. */
public class SchemaFileConfig {

  // region SchemaFile Configuration
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `RecordUtils` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/RecordUtils.java`
#### Snippet
```java
 * stack.
 */
public class RecordUtils {
  // Offsets of IMNode infos in a record buffer
  private static final short INTERNAL_NODE_LENGTH =
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `MemMTreeSnapshotUtil` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/snapshot/MemMTreeSnapshotUtil.java`
#### Snippet
```java
import static org.apache.iotdb.db.metadata.MetadataConstant.STORAGE_GROUP_MNODE_TYPE;

public class MemMTreeSnapshotUtil {

  private static final Logger logger = LoggerFactory.getLogger(MemMTreeSnapshotUtil.class);
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `MetaFormatUtils` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/metadata/utils/MetaFormatUtils.java`
#### Snippet
```java
import static org.apache.iotdb.commons.conf.IoTDBConstant.SDT_COMP_MIN_TIME;

public class MetaFormatUtils {

  private static final Logger logger = LoggerFactory.getLogger(MetaFormatUtils.class);
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `SchemaRegionUtils` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/SchemaRegionUtils.java`
#### Snippet
```java
import java.util.Objects;

public class SchemaRegionUtils {

  public static void deleteSchemaRegionFolder(String schemaRegionDirPath, Logger logger)
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `PayloadFormatManager` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/protocol/mqtt/PayloadFormatManager.java`
#### Snippet
```java

/** PayloadFormatManager loads payload formatter from SPI services. */
public class PayloadFormatManager {
  private static final Logger logger = LoggerFactory.getLogger(PayloadFormatManager.class);

```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `InfluxDBMetaManagerFactory` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/meta/InfluxDBMetaManagerFactory.java`
#### Snippet
```java

/** generate InfluxDBMetaManager object according to configuration */
public class InfluxDBMetaManagerFactory {

  /**
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `FieldUtils` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/util/FieldUtils.java`
#### Snippet
```java
import org.apache.iotdb.tsfile.read.common.Field;

public class FieldUtils {
  /**
   * convert the value of field in iotdb to object
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `CommonUtils` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/util/CommonUtils.java`
#### Snippet
```java
package org.apache.iotdb.db.protocol.influxdb.util;

public class CommonUtils {
  /**
   * judge whether two instances are equal, and allow null check
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `InfluxReqAndRespUtils` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/util/InfluxReqAndRespUtils.java`
#### Snippet
```java
import org.apache.iotdb.service.rpc.thrift.TSOpenSessionResp;

public class InfluxReqAndRespUtils {

  public static TSOpenSessionReq convertOpenSessionReq(InfluxOpenSessionReq influxOpenSessionReq) {
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `FilterUtils` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/util/FilterUtils.java`
#### Snippet
```java
import org.apache.iotdb.tsfile.read.filter.basic.Filter;

public class FilterUtils {

  public static String getFilterStringValue(Filter filter) {
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `StringUtils` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/util/StringUtils.java`
#### Snippet
```java
import java.util.Map;

public class StringUtils {

  /**
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `JacksonUtils` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/util/JacksonUtils.java`
#### Snippet
```java
import java.io.IOException;

public class JacksonUtils {
  private static final ObjectMapper mapper = new ObjectMapper();

```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `QueryHandlerFactory` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/handler/QueryHandlerFactory.java`
#### Snippet
```java

/** Generate the corresponding QueryHandler object according to the configuration */
public class QueryHandlerFactory {

  /**
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `InfluxLineParser` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/input/InfluxLineParser.java`
#### Snippet
```java
import java.util.regex.Pattern;

public class InfluxLineParser {
  private static final Pattern BACKSLASH_PATTERN = Pattern.compile("\\\\\"");
  private static final Pattern IDENTIFIER_PATTERN = Pattern.compile("\\\\([,= ])");
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `InfluxConstant` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/constant/InfluxConstant.java`
#### Snippet
```java
package org.apache.iotdb.db.protocol.influxdb.constant;

public class InfluxConstant {

  public static final String PLACE_HOLDER = "PH";
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `InfluxFunctionFactory` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/function/InfluxFunctionFactory.java`
#### Snippet
```java
import java.util.List;

public class InfluxFunctionFactory {

  public static InfluxFunction generateFunction(
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `SessionPoolExample` has only 'static' members, and lacks a 'private' constructor
in `example/session/src/main/java/org/apache/iotdb/SessionPoolExample.java`
#### Snippet
```java
import java.util.concurrent.Executors;

public class SessionPoolExample {

  private static SessionPool sessionPool;
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `HybridTimeseriesSessionExample` has only 'static' members, and lacks a 'private' constructor
in `example/session/src/main/java/org/apache/iotdb/HybridTimeseriesSessionExample.java`
#### Snippet
```java
 * includes Aligned Timeseries and Normal Timeseries
 */
public class HybridTimeseriesSessionExample {

  private static Session session;
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `DataMigrationExample` has only 'static' members, and lacks a 'private' constructor
in `example/session/src/main/java/org/apache/iotdb/DataMigrationExample.java`
#### Snippet
```java
 * with 6668 port
 */
public class DataMigrationExample {

  // used to read data from the source IoTDB
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `SyntaxConventionRelatedExample` has only 'static' members, and lacks a 'private' constructor
in `example/session/src/main/java/org/apache/iotdb/SyntaxConventionRelatedExample.java`
#### Snippet
```java
 * is an example to help you understand better.
 */
public class SyntaxConventionRelatedExample {
  private static Session session;
  private static final String LOCAL_HOST = "127.0.0.1";
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `TabletExample` has only 'static' members, and lacks a 'private' constructor
in `example/session/src/main/java/org/apache/iotdb/TabletExample.java`
#### Snippet
```java
import java.util.Map.Entry;

public class TabletExample {

  private static final String TIME_STR = "time";
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `SessionConcurrentExample` has only 'static' members, and lacks a 'private' constructor
in `example/session/src/main/java/org/apache/iotdb/SessionConcurrentExample.java`
#### Snippet
```java
import java.util.concurrent.Executors;

public class SessionConcurrentExample {

  private static final int sgNum = 20;
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `AlignedTimeseriesSessionExample` has only 'static' members, and lacks a 'private' constructor
in `example/session/src/main/java/org/apache/iotdb/AlignedTimeseriesSessionExample.java`
#### Snippet
```java

@SuppressWarnings("squid:S106")
public class AlignedTimeseriesSessionExample {

  private static Session session;
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `CrossCorrelation` has only 'static' members, and lacks a 'private' constructor
in `library-udf/src/main/java/org/apache/iotdb/library/dmatch/util/CrossCorrelation.java`
#### Snippet
```java

/** util for UDTFXCorr and UDTFACF */
public class CrossCorrelation {
  public static ArrayList<Double> calculateCrossCorrelation(
      ArrayList<Double> valueArrayList1, ArrayList<Double> valueArrayList2) {
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `Util` has only 'static' members, and lacks a 'private' constructor
in `library-udf/src/main/java/org/apache/iotdb/library/util/Util.java`
#### Snippet
```java

/** This class offers functions of getting and putting values from iotdb interface. */
public class Util {

  /**
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `SessionExample` has only 'static' members, and lacks a 'private' constructor
in `example/session/src/main/java/org/apache/iotdb/SessionExample.java`
#### Snippet
```java

@SuppressWarnings("squid:S106")
public class SessionExample {

  private static Session session;
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `CrossCorrelation` has only 'static' members, and lacks a 'private' constructor
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/CrossCorrelation.java`
#### Snippet
```java

/** util for UDTFXCorr and UDTFACF */
public class CrossCorrelation {
  public static ArrayList<Double> calculateCrossCorrelation(
      ArrayList<Double> valueArrayList1, ArrayList<Double> valueArrayList2) {
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `Segment` has only 'static' members, and lacks a 'private' constructor
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/Segment.java`
#### Snippet
```java

/** Util for segment. */
public class Segment {
  private static double calculate_error(double[] y1, double[] y2) throws Exception {
    double[] y = ArrayUtils.addAll(y1, y2);
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `InfluxDBExample` has only 'static' members, and lacks a 'private' constructor
in `example/influxdb-protocol-example/src/main/java/org/apache/iotdb/influxdb/InfluxDBExample.java`
#### Snippet
```java
import java.util.concurrent.TimeUnit;

public class InfluxDBExample {

  private static InfluxDB influxDB;
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `QueryResultUtils` has only 'static' members, and lacks a 'private' constructor
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/util/QueryResultUtils.java`
#### Snippet
```java
import java.util.stream.Collectors;

public class QueryResultUtils {
  /**
   * update the new values to the query results of influxdb
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `FlinkIoTDBSink` has only 'static' members, and lacks a 'private' constructor
in `example/flink/src/main/java/org/apache/iotdb/flink/FlinkIoTDBSink.java`
#### Snippet
```java
import java.util.Random;

public class FlinkIoTDBSink {
  public static void main(String[] args) throws Exception {
    // run the flink job on local mini cluster
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `FlinkIoTDBSource` has only 'static' members, and lacks a 'private' constructor
in `example/flink/src/main/java/org/apache/iotdb/flink/FlinkIoTDBSource.java`
#### Snippet
```java
import java.util.List;

public class FlinkIoTDBSource {

  static final String LOCAL_HOST = "127.0.0.1";
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `FlinkTsFileBatchSource` has only 'static' members, and lacks a 'private' constructor
in `example/flink/src/main/java/org/apache/iotdb/flink/FlinkTsFileBatchSource.java`
#### Snippet
```java

/** The example of reading TsFile via Flink DataSet API. */
public class FlinkTsFileBatchSource {

  public static void main(String[] args) throws Exception {
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `FlinkTsFileStreamSource` has only 'static' members, and lacks a 'private' constructor
in `example/flink/src/main/java/org/apache/iotdb/flink/FlinkTsFileStreamSource.java`
#### Snippet
```java

/** The example of reading TsFile via Flink DataStream API. */
public class FlinkTsFileStreamSource {

  public static void main(String[] args) {
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `FlinkTsFileBatchSink` has only 'static' members, and lacks a 'private' constructor
in `example/flink/src/main/java/org/apache/iotdb/flink/FlinkTsFileBatchSink.java`
#### Snippet
```java

/** The example of writing to TsFile via Flink DataSet API. */
public class FlinkTsFileBatchSink {

  public static final String DEFAULT_TEMPLATE = "template";
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `FlinkTsFileStreamSink` has only 'static' members, and lacks a 'private' constructor
in `example/flink/src/main/java/org/apache/iotdb/flink/FlinkTsFileStreamSink.java`
#### Snippet
```java

/** The example of writing to TsFile via Flink DataStream API. */
public class FlinkTsFileStreamSink {

  public static final String DEFAULT_TEMPLATE = "template";
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `SystemPropertiesUtils` has only 'static' members, and lacks a 'private' constructor
in `confignode/src/main/java/org/apache/iotdb/confignode/conf/SystemPropertiesUtils.java`
#### Snippet
```java
import java.util.Properties;

public class SystemPropertiesUtils {

  private static final Logger LOGGER = LoggerFactory.getLogger(SystemPropertiesUtils.class);
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `RewriteTsFileTool` has only 'static' members, and lacks a 'private' constructor
in `rewrite-tsfile-tool/src/main/java/org/apache/iotdb/RewriteTsFileTool.java`
#### Snippet
```java
import java.util.Set;

public class RewriteTsFileTool {
  private static final int MAX_TABLET_LENGTH = 1024 * 64;

```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `Constant` has only 'static' members, and lacks a 'private' constructor
in `example/rocketmq/src/main/java/org/apache/iotdb/rocketmq/Constant.java`
#### Snippet
```java
package org.apache.iotdb.rocketmq;

public class Constant {

  public static final String TOPIC = "RocketMQ-Test";
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `Executor` has only 'static' members, and lacks a 'private' constructor
in `spark-tsfile/src/main/java/org/apache/iotdb/spark/tsfile/qp/Executor.java`
#### Snippet
```java

/** This class used to execute Queries on TSFile */
public class Executor {

  public static List<QueryDataSet> query(
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `SQLConstant` has only 'static' members, and lacks a 'private' constructor
in `spark-tsfile/src/main/java/org/apache/iotdb/spark/tsfile/qp/common/SQLConstant.java`
#### Snippet
```java

/** this class contains several constants used in SQL. */
public class SQLConstant {

  public static final String DELTA_OBJECT_NAME = "delta_object_name";
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `RSchemaConstants` has only 'static' members, and lacks a 'private' constructor
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/RSchemaConstants.java`
#### Snippet
```java
package org.apache.iotdb.db.metadata.schemaregion.rocksdb;

public class RSchemaConstants {

  public static final char ZERO = '0';
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `TsFileCheckStatus` has only 'static' members, and lacks a 'private' constructor
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/TsFileCheckStatus.java`
#### Snippet
```java
package org.apache.iotdb.tsfile.read;

public class TsFileCheckStatus {
  public static final long COMPLETE_FILE = 0;
  public static final long FILE_EXISTS_MISTAKES = -1;
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `TypeFactory` has only 'static' members, and lacks a 'private' constructor
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/type/TypeFactory.java`
#### Snippet
```java
import org.apache.iotdb.tsfile.file.metadata.enums.TSDataType;

public class TypeFactory {
  public static Type getType(TSDataType tsDataType) {
    switch (tsDataType) {
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `ColumnEncoderFactory` has only 'static' members, and lacks a 'private' constructor
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/block/column/ColumnEncoderFactory.java`
#### Snippet
```java
import java.util.Map;

public class ColumnEncoderFactory {

  private static final Map<ColumnEncoding, ColumnEncoder> encodingToEncoder = new HashMap<>();
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `PathNodesGenerator` has only 'static' members, and lacks a 'private' constructor
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/common/parser/PathNodesGenerator.java`
#### Snippet
```java

/** convert String path to String[] nodes * */
public class PathNodesGenerator {
  private static PathVisitor pathVisitor = new PathVisitor();

```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `FilterFactory` has only 'static' members, and lacks a 'private' constructor
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/filter/factory/FilterFactory.java`
#### Snippet
```java
import java.nio.ByteBuffer;

public class FilterFactory {

  public static AndFilter and(Filter left, Filter right) {
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `ExpressionPrinter` has only 'static' members, and lacks a 'private' constructor
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/expression/util/ExpressionPrinter.java`
#### Snippet
```java
import org.apache.iotdb.tsfile.read.expression.IUnaryExpression;

public class ExpressionPrinter {

  private static final int MAX_DEPTH = 100;
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `RSchemaUtils` has only 'static' members, and lacks a 'private' constructor
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/RSchemaUtils.java`
#### Snippet
```java
import static org.apache.iotdb.db.metadata.schemaregion.rocksdb.RSchemaConstants.ZERO;

public class RSchemaUtils {
  public static final RMNodeType[] NODE_TYPE_ARRAY = new RMNodeType[NODE_TYPE_ALIAS + 1];

```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `TsFileUtils` has only 'static' members, and lacks a 'private' constructor
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/TsFileUtils.java`
#### Snippet
```java
import java.io.IOException;

public class TsFileUtils {

  /**
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `TsFileGeneratorUtils` has only 'static' members, and lacks a 'private' constructor
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/TsFileGeneratorUtils.java`
#### Snippet
```java

/** This class is only used for generating aligned or nonAligned tsfiles in test. */
public class TsFileGeneratorUtils {
  private static final FSFactory fsFactory = FSFactoryProducer.getFSFactory();
  public static final String testStorageGroup = "root.testsg";
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `TsFileIOWriterEndFileTest` has only 'static' members, and lacks a 'private' constructor
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/writer/TsFileIOWriterEndFileTest.java`
#### Snippet
```java
import java.io.File;

public class TsFileIOWriterEndFileTest {
  public static void main(String[] args) throws Exception {
    TsFileIOWriter writer = new TsFileIOWriter(new File("test.tsfile"));
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `GZIPCompress` has only 'static' members, and lacks a 'private' constructor
in `tsfile/src/main/java/org/apache/iotdb/tsfile/compress/ICompressor.java`
#### Snippet
```java
  }

  class GZIPCompress {
    public static byte[] compress(byte[] data) throws IOException {
      ByteArrayOutputStream out = new ByteArrayOutputStream();
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `FSFactoryProducer` has only 'static' members, and lacks a 'private' constructor
in `tsfile/src/main/java/org/apache/iotdb/tsfile/fileSystem/FSFactoryProducer.java`
#### Snippet
```java
import org.apache.iotdb.tsfile.fileSystem.fsFactory.LocalFSFactory;

public class FSFactoryProducer {

  private static FSType fSType = TSFileDescriptor.getInstance().getConfig().getTSFileStorageFs();
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `JvmUtils` has only 'static' members, and lacks a 'private' constructor
in `metrics/interface/src/main/java/org/apache/iotdb/metrics/metricsets/jvm/JvmUtils.java`
#### Snippet
```java
import java.util.function.ToLongFunction;

public class JvmUtils {
  static double getUsageValue(
      MemoryPoolMXBean memoryPoolMXBean, ToLongFunction<MemoryUsage> getter) {
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `RabbitMQChannelUtils` has only 'static' members, and lacks a 'private' constructor
in `example/rabbitmq/src/main/java/org/apache/iotdb/rabbitmq/RabbitMQChannelUtils.java`
#### Snippet
```java
import java.util.concurrent.TimeoutException;

public class RabbitMQChannelUtils {

  @SuppressWarnings("squid:S2095")
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `IoTDBMetricsUtils` has only 'static' members, and lacks a 'private' constructor
in `metrics/interface/src/main/java/org/apache/iotdb/metrics/utils/IoTDBMetricsUtils.java`
#### Snippet
```java
import java.util.Map;

public class IoTDBMetricsUtils {
  private static final Logger logger = LoggerFactory.getLogger(IoTDBMetricsUtils.class);
  private static final MetricConfig metricConfig =
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `RabbitMQProducer` has only 'static' members, and lacks a 'private' constructor
in `example/rabbitmq/src/main/java/org/apache/iotdb/rabbitmq/RabbitMQProducer.java`
#### Snippet
```java

/** The class is to show how to send data to RabbitMQ. */
public class RabbitMQProducer {

  private static final Logger logger = LoggerFactory.getLogger(RabbitMQProducer.class);
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `ExampleUDFConstant` has only 'static' members, and lacks a 'private' constructor
in `integration-test/src/main/java/org/apache/iotdb/db/query/udf/example/ExampleUDFConstant.java`
#### Snippet
```java
package org.apache.iotdb.db.query.udf.example;

public class ExampleUDFConstant {
  public static final String ACCESS_STRATEGY_KEY = "access";
  public static final String ACCESS_STRATEGY_ROW_BY_ROW = "row-by-row";
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `ConfigFactory` has only 'static' members, and lacks a 'private' constructor
in `integration-test/src/main/java/org/apache/iotdb/it/env/ConfigFactory.java`
#### Snippet
```java
import org.apache.iotdb.itbase.env.BaseConfig;

public class ConfigFactory {
  private static BaseConfig config;

```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `EnvUtils` has only 'static' members, and lacks a 'private' constructor
in `integration-test/src/main/java/org/apache/iotdb/it/env/EnvUtils.java`
#### Snippet
```java
import java.util.stream.IntStream;

public class EnvUtils {
  private static final String lockFilePath =
      System.getProperty("user.dir") + File.separator + "target" + File.separator + "lock-";
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `EnvFactory` has only 'static' members, and lacks a 'private' constructor
in `integration-test/src/main/java/org/apache/iotdb/it/env/EnvFactory.java`
#### Snippet
```java
import org.slf4j.Logger;

public class EnvFactory {
  private static BaseEnv env;
  private static final Logger logger = IoTDBTestLogger.logger;
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `IoTDBTestLogger` has only 'static' members, and lacks a 'private' constructor
in `integration-test/src/main/java/org/apache/iotdb/it/framework/IoTDBTestLogger.java`
#### Snippet
```java
import org.slf4j.LoggerFactory;

public class IoTDBTestLogger {
  public static final Logger logger = LoggerFactory.getLogger(IoTDBTestLogger.class);
}
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `IoTDBTestReporter` has only 'static' members, and lacks a 'private' constructor
in `integration-test/src/main/java/org/apache/iotdb/it/framework/IoTDBTestReporter.java`
#### Snippet
```java
import java.util.stream.Stream;

public class IoTDBTestReporter {

  public static void main(String[] args) throws IOException {
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `UDFTestConstant` has only 'static' members, and lacks a 'private' constructor
in `integration-test/src/main/java/org/apache/iotdb/itbase/constant/UDFTestConstant.java`
#### Snippet
```java
package org.apache.iotdb.itbase.constant;

public class UDFTestConstant {
  public static final String ACCESS_STRATEGY_KEY = "access";
  public static final String ACCESS_STRATEGY_ROW_BY_ROW = "row-by-row";
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `TestConstant` has only 'static' members, and lacks a 'private' constructor
in `integration-test/src/main/java/org/apache/iotdb/itbase/constant/TestConstant.java`
#### Snippet
```java
import java.io.File;

public class TestConstant {

  public static final String BASE_OUTPUT_PATH = "target".concat(File.separator);
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `HDFSConfUtil` has only 'static' members, and lacks a 'private' constructor
in `hadoop/src/main/java/org/apache/iotdb/hadoop/fileSystem/HDFSConfUtil.java`
#### Snippet
```java
import java.net.MalformedURLException;

class HDFSConfUtil {

  private static TSFileConfig tsFileConfig = TSFileDescriptor.getInstance().getConfig();
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `TSFileConfigUtil` has only 'static' members, and lacks a 'private' constructor
in `flink-tsfile-connector/src/main/java/org/apache/iotdb/flink/tsfile/util/TSFileConfigUtil.java`
#### Snippet
```java

/** Utils of setting global TSFileConfig. */
public class TSFileConfigUtil {

  public static void setGlobalTSFileConfig(TSFileConfig config) {
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `TsFileWriteToHDFS` has only 'static' members, and lacks a 'private' constructor
in `example/hadoop/src/main/java/org/apache/iotdb/hadoop/tsfile/TsFileWriteToHDFS.java`
#### Snippet
```java
import java.io.File;

public class TsFileWriteToHDFS {

  private static TSFileConfig config = TSFileDescriptor.getInstance().getConfig();
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `TsFileHelper` has only 'static' members, and lacks a 'private' constructor
in `example/hadoop/src/main/java/org/apache/iotdb/hadoop/tsfile/TsFileHelper.java`
#### Snippet
```java
import java.util.List;

public class TsFileHelper {

  private static final Logger logger = LoggerFactory.getLogger(TsFileHelper.class);
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `TSMRWriteExample` has only 'static' members, and lacks a 'private' constructor
in `example/hadoop/src/main/java/org/apache/iotdb/hadoop/tsfile/TSMRWriteExample.java`
#### Snippet
```java

/** One example for writing TsFile with MapReduce. */
public class TSMRWriteExample {

  public static void main(String[] args)
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `TSFMRReadExample` has only 'static' members, and lacks a 'private' constructor
in `example/hadoop/src/main/java/org/apache/iotdb/hadoop/tsfile/TSFMRReadExample.java`
#### Snippet
```java
 * TsFileHelper</code>.
 */
public class TSFMRReadExample {

  public static void main(String[] args)
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `MQTTClient` has only 'static' members, and lacks a 'private' constructor
in `example/mqtt/src/main/java/org/apache/iotdb/mqtt/MQTTClient.java`
#### Snippet
```java
import java.util.Random;

public class MQTTClient {
  public static void main(String[] args) throws Exception {
    MQTT mqtt = new MQTT();
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `DropwizardMetricNameTool` has only 'static' members, and lacks a 'private' constructor
in `metrics/dropwizard-metrics/src/main/java/org/apache/iotdb/metrics/dropwizard/DropwizardMetricNameTool.java`
#### Snippet
```java
import java.util.stream.Collectors;

public class DropwizardMetricNameTool {
  private static final String TAG_SEPARATOR = ".";

```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `ConsensusFactory` has only 'static' members, and lacks a 'private' constructor
in `consensus/src/main/java/org/apache/iotdb/consensus/ConsensusFactory.java`
#### Snippet
```java
import java.util.Optional;

public class ConsensusFactory {
  public static final String CONSTRUCT_FAILED_MSG =
      "Construct consensusImpl failed, Please check your consensus className %s";
```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `Utils` has only 'static' members, and lacks a 'private' constructor
in `consensus/src/main/java/org/apache/iotdb/consensus/common/Utils.java`
#### Snippet
```java
import java.util.List;

public class Utils {
  private static final Logger logger = LoggerFactory.getLogger(Utils.class);

```

### RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
Class `TsfileWebDemoApplication` has only 'static' members, and lacks a 'private' constructor
in `grafana-connector/src/main/java/org/apache/iotdb/web/grafana/TsfileWebDemoApplication.java`
#### Snippet
```java

@SpringBootApplication
public class TsfileWebDemoApplication {

  public static void main(String[] args) {
```

## RuleId[ruleID=DataFlowIssue]
### RuleId[ruleID=DataFlowIssue]
Argument `ReadWriteIOUtils.readString(byteBuffer)` might be null
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/UDFInformation.java`
#### Snippet
```java
  public static UDFInformation deserialize(ByteBuffer byteBuffer) {
    UDFInformation udfInformation = new UDFInformation();
    udfInformation.setFunctionName(ReadWriteIOUtils.readString(byteBuffer));
    udfInformation.setClassName(ReadWriteIOUtils.readString(byteBuffer));
    udfInformation.setBuiltin(ReadWriteIOUtils.readBool(byteBuffer));
```

### RuleId[ruleID=DataFlowIssue]
Dereference of `floatPQ.peek()` may produce `NullPointerException`
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/builtin/UDTFBottomK.java`
#### Snippet
```java
    if (floatPQ.size() < k) {
      floatPQ.add(new Pair<>(time, value));
    } else if (value < floatPQ.peek().right) {
      floatPQ.poll();
      floatPQ.add(new Pair<>(time, value));
```

### RuleId[ruleID=DataFlowIssue]
Dereference of `doublePQ.peek()` may produce `NullPointerException`
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/builtin/UDTFBottomK.java`
#### Snippet
```java
    if (doublePQ.size() < k) {
      doublePQ.add(new Pair<>(time, value));
    } else if (value < doublePQ.peek().right) {
      doublePQ.poll();
      doublePQ.add(new Pair<>(time, value));
```

### RuleId[ruleID=DataFlowIssue]
Dereference of `intPQ.peek()` may produce `NullPointerException`
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/builtin/UDTFBottomK.java`
#### Snippet
```java
    if (intPQ.size() < k) {
      intPQ.add(new Pair<>(time, value));
    } else if (value < intPQ.peek().right) {
      intPQ.poll();
      intPQ.add(new Pair<>(time, value));
```

### RuleId[ruleID=DataFlowIssue]
Dereference of `longPQ.peek()` may produce `NullPointerException`
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/builtin/UDTFBottomK.java`
#### Snippet
```java
    if (longPQ.size() < k) {
      longPQ.add(new Pair<>(time, value));
    } else if (value < longPQ.peek().right) {
      longPQ.poll();
      longPQ.add(new Pair<>(time, value));
```

### RuleId[ruleID=DataFlowIssue]
Dereference of `stringPQ.peek()` may produce `NullPointerException`
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/builtin/UDTFBottomK.java`
#### Snippet
```java
    if (stringPQ.size() < k) {
      stringPQ.add(new Pair<>(time, value));
    } else if (value.compareTo(stringPQ.peek().right) < 0) {
      stringPQ.poll();
      stringPQ.add(new Pair<>(time, value));
```

### RuleId[ruleID=DataFlowIssue]
Dereference of `intPQ.peek()` may produce `NullPointerException`
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/builtin/UDTFTopK.java`
#### Snippet
```java
    if (intPQ.size() < k) {
      intPQ.add(new Pair<>(time, value));
    } else if (intPQ.peek().right < value) {
      intPQ.poll();
      intPQ.add(new Pair<>(time, value));
```

### RuleId[ruleID=DataFlowIssue]
Dereference of `longPQ.peek()` may produce `NullPointerException`
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/builtin/UDTFTopK.java`
#### Snippet
```java
    if (longPQ.size() < k) {
      longPQ.add(new Pair<>(time, value));
    } else if (longPQ.peek().right < value) {
      longPQ.poll();
      longPQ.add(new Pair<>(time, value));
```

### RuleId[ruleID=DataFlowIssue]
Dereference of `stringPQ.peek()` may produce `NullPointerException`
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/builtin/UDTFTopK.java`
#### Snippet
```java
    if (stringPQ.size() < k) {
      stringPQ.add(new Pair<>(time, value));
    } else if (stringPQ.peek().right.compareTo(value) < 0) {
      stringPQ.poll();
      stringPQ.add(new Pair<>(time, value));
```

### RuleId[ruleID=DataFlowIssue]
Dereference of `floatPQ.peek()` may produce `NullPointerException`
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/builtin/UDTFTopK.java`
#### Snippet
```java
    if (floatPQ.size() < k) {
      floatPQ.add(new Pair<>(time, value));
    } else if (floatPQ.peek().right < value) {
      floatPQ.poll();
      floatPQ.add(new Pair<>(time, value));
```

### RuleId[ruleID=DataFlowIssue]
Dereference of `doublePQ.peek()` may produce `NullPointerException`
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/builtin/UDTFTopK.java`
#### Snippet
```java
    if (doublePQ.size() < k) {
      doublePQ.add(new Pair<>(time, value));
    } else if (doublePQ.peek().right < value) {
      doublePQ.poll();
      doublePQ.add(new Pair<>(time, value));
```

### RuleId[ruleID=DataFlowIssue]
Dereference of `pq.peek()` may produce `NullPointerException`
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/builtin/UDTFEqualSizeBucketOutlierSample.java`
#### Snippet
```java
    int[] arr = new int[number];
    for (int i = 0; i < number; i++) {
      arr[i] = pq.peek().left;
      pq.poll();
    }
```

### RuleId[ruleID=DataFlowIssue]
Dereference of `pq.peek()` may produce `NullPointerException`
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/builtin/UDTFEqualSizeBucketOutlierSample.java`
#### Snippet
```java
    int[] arr = new int[number];
    for (int i = 0; i < number; i++) {
      arr[i] = pq.peek().left;
      pq.poll();
    }
```

### RuleId[ruleID=DataFlowIssue]
Dereference of `pq.peek()` may produce `NullPointerException`
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/builtin/UDTFEqualSizeBucketOutlierSample.java`
#### Snippet
```java
    if (pq.size() < number) {
      pq.add(new Pair<>(i, value));
    } else if (value < pq.peek().right) {
      pq.poll();
      pq.add(new Pair<>(i, value));
```

### RuleId[ruleID=DataFlowIssue]
Dereference of `pq.peek()` may produce `NullPointerException`
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/builtin/UDTFEqualSizeBucketOutlierSample.java`
#### Snippet
```java
    int[] arr = new int[number];
    for (int i = 0; i < number; i++) {
      arr[i] = pq.peek().left;
      pq.poll();
    }
```

### RuleId[ruleID=DataFlowIssue]
Dereference of `pq.peek()` may produce `NullPointerException`
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/builtin/UDTFEqualSizeBucketOutlierSample.java`
#### Snippet
```java
    int[] arr = new int[number];
    for (int i = 0; i < number; i++) {
      arr[i] = pq.peek().left;
      pq.poll();
    }
```

### RuleId[ruleID=DataFlowIssue]
Dereference of `pq.peek()` may produce `NullPointerException`
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/builtin/UDTFEqualSizeBucketOutlierSample.java`
#### Snippet
```java
    if (pq.size() < number) {
      pq.add(new Pair<>(i, value));
    } else if (value > pq.peek().right) {
      pq.poll();
      pq.add(new Pair<>(i, value));
```

### RuleId[ruleID=DataFlowIssue]
Dereference of `list` may produce `NullPointerException`
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/FileUtils.java`
#### Snippet
```java
    if (file.isDirectory()) {
      String[] list = file.list();
      for (String item : list) {
        String subPath = path + File.separator + item;
        sum += getDirSize(subPath);
```

### RuleId[ruleID=DataFlowIssue]
Dereference of `folder.listFiles()` may produce `NullPointerException`
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/FileUtils.java`
#### Snippet
```java
  public static void deleteDirectory(File folder) {
    if (folder.isDirectory()) {
      for (File file : folder.listFiles()) {
        deleteDirectory(file);
      }
```

### RuleId[ruleID=DataFlowIssue]
Dereference of `visitorStack.peek()` may produce `NullPointerException`
in `node-commons/src/main/java/org/apache/iotdb/commons/schema/tree/AbstractTreeVisitor.java`
#### Snippet
```java
          new AncestorStackEntry<>(
              parent,
              visitorStack.peek().patternIndex,
              visitorStack.peek().lastMultiLevelWildcardIndex));
      visitorStack.push(
```

### RuleId[ruleID=DataFlowIssue]
Dereference of `visitorStack.peek()` may produce `NullPointerException`
in `node-commons/src/main/java/org/apache/iotdb/commons/schema/tree/AbstractTreeVisitor.java`
#### Snippet
```java
              parent,
              visitorStack.peek().patternIndex,
              visitorStack.peek().lastMultiLevelWildcardIndex));
      visitorStack.push(
          new VisitorStackEntry<>(
```

### RuleId[ruleID=DataFlowIssue]
Dereference of `visitorStack.peek()` may produce `NullPointerException`
in `node-commons/src/main/java/org/apache/iotdb/commons/schema/tree/AbstractTreeVisitor.java`
#### Snippet
```java
        new AncestorStackEntry<>(
            parent,
            visitorStack.peek().patternIndex,
            visitorStack.peek().lastMultiLevelWildcardIndex));
    visitorStack.push(
```

### RuleId[ruleID=DataFlowIssue]
Dereference of `visitorStack.peek()` may produce `NullPointerException`
in `node-commons/src/main/java/org/apache/iotdb/commons/schema/tree/AbstractTreeVisitor.java`
#### Snippet
```java
            parent,
            visitorStack.peek().patternIndex,
            visitorStack.peek().lastMultiLevelWildcardIndex));
    visitorStack.push(
        new VisitorStackEntry<>(
```

### RuleId[ruleID=DataFlowIssue]
Passing `null` argument to parameter annotated as @NotNull
in `influxdb-protocol/src/main/java/org/apache/iotdb/influxdb/IoTDBInfluxDB.java`
#### Snippet
```java
  @Override
  public void write(final String records) {
    write(null, null, null, null, records);
  }

```

### RuleId[ruleID=DataFlowIssue]
Method invocation `getTSDataType` may produce `NullPointerException`
in `schema-engine-tag/src/main/java/org/apache/iotdb/db/metadata/tagSchemaRegion/TagSchemaRegion.java`
#### Snippet
```java
              new MeasurementSchema(
                  measurements[i],
                  schemaEntry.getTSDataType(),
                  schemaEntry.getTSEncoding(),
                  schemaEntry.getCompressionType()),
```

### RuleId[ruleID=DataFlowIssue]
Argument `authorType` might be null
in `server/src/main/java/org/apache/iotdb/db/qp/logical/sys/AuthorOperator.java`
#### Snippet
```java
    try {
      return new AuthorPlan(
          authorType, userName, roleName, password, newPassword, privilegeList, nodeNameList);
    } catch (AuthException e) {
      throw new QueryProcessException(e.getMessage());
```

### RuleId[ruleID=DataFlowIssue]
Method invocation `nodeNameInIntoPath` may produce `NullPointerException`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/parser/ASTVisitor.java`
#### Snippet
```java
    } else {
      List<IoTDBSqlParser.NodeNameInIntoPathContext> nodeNames =
          ((IoTDBSqlParser.SuffixPathInIntoPathContext) intoPathContext).nodeNameInIntoPath();
      String[] path = new String[nodeNames.size()];
      for (int i = 0; i < nodeNames.size(); i++) {
```

### RuleId[ruleID=DataFlowIssue]
Argument `dataType` might be null
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/parser/ASTVisitor.java`
#### Snippet
```java
    }

    TSEncoding encoding = IoTDBDescriptor.getInstance().getDefaultEncodingByType(dataType);
    if (props.containsKey(IoTDBConstant.COLUMN_TIMESERIES_ENCODING.toLowerCase())) {
      String encodingString =
```

### RuleId[ruleID=DataFlowIssue]
Argument `dataType` might be null
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/parser/ASTVisitor.java`
#### Snippet
```java
    }

    TSEncoding encoding = IoTDBDescriptor.getInstance().getDefaultEncodingByType(dataType);
    if (props.containsKey(IoTDBConstant.COLUMN_TIMESERIES_ENCODING.toLowerCase())) {
      String encodingString =
```

### RuleId[ruleID=DataFlowIssue]
Argument `ReadWriteIOUtils.readString(stream)` might be null
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/load/LoadTsFilePieceNode.java`
#### Snippet
```java
    try {
      ReadWriteIOUtils.readShort(stream); // read PlanNodeType
      File tsFile = new File(ReadWriteIOUtils.readString(stream));
      LoadTsFilePieceNode pieceNode = new LoadTsFilePieceNode(new PlanNodeId(""), tsFile);
      int tsFileDataSize = ReadWriteIOUtils.readInt(stream);
```

### RuleId[ruleID=DataFlowIssue]
Argument `aggregationFuncName` might be null
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/parameter/AggregationDescriptor.java`
#### Snippet
```java
      inputExpressionsSize--;
    }
    return new AggregationDescriptor(aggregationFuncName, step, inputExpressions);
  }

```

### RuleId[ruleID=DataFlowIssue]
Function may return null, but it's not allowed here
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/distribution/ExchangeNodeAdder.java`
#### Snippet
```java
                        return calculateSchemaRegionByChildren(child.getChildren(), context);
                      }
                      return region;
                    },
                    Collectors.counting()));
```

### RuleId[ruleID=DataFlowIssue]
Dereference of `file.listFiles()` may produce `NullPointerException`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/statement/crud/LoadTsFileStatement.java`
#### Snippet
```java

  private void findAllTsFile(File file) {
    for (File nowFile : file.listFiles()) {
      if (nowFile.getName().endsWith(TsFileConstant.TSFILE_SUFFIX)) {
        tsFiles.add(nowFile);
```

### RuleId[ruleID=DataFlowIssue]
Method invocation `getNode` may produce `NullPointerException`
in `server/src/main/java/org/apache/iotdb/db/mpp/common/schematree/visitor/SchemaTreeMeasurementVisitor.java`
#### Snippet
```java
            nextMatchedNode.getAsMeasurementNode().getSchema());
    result.setTagMap(nextMatchedNode.getAsMeasurementNode().getTagMap());
    result.setUnderAlignedEntity(ancestorStack.peek().getNode().getAsEntityNode().isAligned());
    String alias = nextMatchedNode.getAsMeasurementNode().getAlias();
    if (nodes[nodes.length - 1].equals(alias)) {
```

### RuleId[ruleID=DataFlowIssue]
Method invocation `isSeq` may produce `NullPointerException`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/source/SeriesScanUtil.java`
#### Snippet
```java
    while (!cachedChunkMetadata.isEmpty()
        && orderUtils.isOverlapped(endpointTime, cachedChunkMetadata.peek().getStatistics())) {
      if (cachedChunkMetadata.peek().isSeq() && hasMeetSeq) {
        break;
      } else if (cachedChunkMetadata.peek().isSeq()) {
```

### RuleId[ruleID=DataFlowIssue]
Method invocation `isSeq` may produce `NullPointerException`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/source/SeriesScanUtil.java`
#### Snippet
```java
      if (cachedChunkMetadata.peek().isSeq() && hasMeetSeq) {
        break;
      } else if (cachedChunkMetadata.peek().isSeq()) {
        hasMeetSeq = true;
      }
```

### RuleId[ruleID=DataFlowIssue]
Method invocation `getStatistics` may produce `NullPointerException`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/source/SeriesScanUtil.java`
#### Snippet
```java
        || (!unSeqPageReaders.isEmpty()
                && orderUtils.isOverlapped(
                    firstPageReader.getStatistics(), unSeqPageReaders.peek().getStatistics())
            || (mergeReader.hasNextTimeValuePair()
                && orderUtils.isOverlapped(
```

### RuleId[ruleID=DataFlowIssue]
Argument `unSeqPageReaders.poll()` might be null
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/source/SeriesScanUtil.java`
#### Snippet
```java
    while (!unSeqPageReaders.isEmpty()
        && orderUtils.isOverlapped(endpointTime, unSeqPageReaders.peek().data.getStatistics())) {
      putPageReaderToMergeReader(unSeqPageReaders.poll());
    }
    if (firstPageReader != null
```

### RuleId[ruleID=DataFlowIssue]
Method invocation `getStatistics` may produce `NullPointerException`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/source/SeriesScanUtil.java`
#### Snippet
```java
    if (!seqPageReaders.isEmpty() && !unSeqPageReaders.isEmpty()) {
      if (orderUtils.isTakeSeqAsFirst(
          seqPageReaders.get(0).getStatistics(), unSeqPageReaders.peek().getStatistics())) {
        firstPageReader = seqPageReaders.get(0);
      } else {
```

### RuleId[ruleID=DataFlowIssue]
Method invocation `getStatistics` may produce `NullPointerException`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/source/SeriesScanUtil.java`
#### Snippet
```java
          orderUtils.getCurrentEndPoint(
              seqTimeSeriesMetadata.get(0).getStatistics(),
              unSeqTimeSeriesMetadata.peek().getStatistics());
    }

```

### RuleId[ruleID=DataFlowIssue]
Method invocation `getStatistics` may produce `NullPointerException`
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/source/SeriesScanUtil.java`
#### Snippet
```java
      if (orderUtils.isTakeSeqAsFirst(
          seqTimeSeriesMetadata.get(0).getStatistics(),
          unSeqTimeSeriesMetadata.peek().getStatistics())) {
        firstTimeSeriesMetadata = seqTimeSeriesMetadata.remove(0);
      } else {
```

### RuleId[ruleID=DataFlowIssue]
Method invocation `getRoleList` may produce `NullPointerException`
in `server/src/main/java/org/apache/iotdb/db/auth/BasicAuthorityCache.java`
#### Snippet
```java
    if (userName != null) {
      if (userCache.getIfPresent(userName) != null) {
        List<String> roleList = userCache.getIfPresent(userName).getRoleList();
        if (!roleList.isEmpty()) {
          roleCache.invalidateAll(roleList);
```

### RuleId[ruleID=DataFlowIssue]
Dereference of `sgWAL.list()` may produce `NullPointerException`
in `server/src/main/java/org/apache/iotdb/db/conf/IoTDBStartCheck.java`
#### Snippet
```java
          for (File sgWAL : sgWALs) {
            // make sure wal directory of each sg is empty
            if (sgWAL.isDirectory() && sgWAL.list().length != 0) {
              logger.error(
                  "WAL detected, please stop insertion and run 'SET SYSTEM TO READONLY', then run 'flush' on IoTDB {} before upgrading to {}.",
```

### RuleId[ruleID=DataFlowIssue]
Dereference of `fileList` may produce `NullPointerException`
in `server/src/main/java/org/apache/iotdb/db/sync/sender/pipe/TsFilePipe.java`
#### Snippet
```java
    if (dir.exists()) {
      File[] fileList = dir.listFiles();
      for (File file : fileList) {
        String dataRegionId = file.getName();
        BufferedPipeDataQueue historyQueue =
```

### RuleId[ruleID=DataFlowIssue]
Dereference of `fileList` may produce `NullPointerException`
in `server/src/main/java/org/apache/iotdb/db/sync/sender/pipe/TsFilePipe.java`
#### Snippet
```java
    if (dir.exists()) {
      File[] fileList = dir.listFiles();
      for (File file : fileList) {
        String dataRegionId = file.getName();
        BufferedPipeDataQueue realTimeQueue =
```

### RuleId[ruleID=DataFlowIssue]
Unboxing of `pipeLogStartNumber.peekLast()` may produce `NullPointerException`
in `server/src/main/java/org/apache/iotdb/db/sync/pipedata/queue/BufferedPipeDataQueue.java`
#### Snippet
```java
      lastMaxSerialNumber =
          recoverPipeDataSize == 0
              ? pipeLogStartNumber.peekLast() - 1
              : recoverPipeData.get(recoverPipeDataSize - 1).getSerialNumber();
    } catch (IOException e) {
```

### RuleId[ruleID=DataFlowIssue]
Unboxing of `pipeLogStartNumber.poll()` may produce `NullPointerException`
in `server/src/main/java/org/apache/iotdb/db/sync/pipedata/queue/BufferedPipeDataQueue.java`
#### Snippet
```java
      long nowPipeLogStartNumber;
      while (true) {
        nowPipeLogStartNumber = pipeLogStartNumber.poll();
        if (!pipeLogStartNumber.isEmpty() && pipeLogStartNumber.peek() <= commitSerialNumber) {
          try {
```

### RuleId[ruleID=DataFlowIssue]
Dereference of `logDir.listFiles()` may produce `NullPointerException`
in `server/src/main/java/org/apache/iotdb/db/sync/pipedata/queue/BufferedPipeDataQueue.java`
#### Snippet
```java
    List<Long> startNumbers = new ArrayList<>();

    for (File file : logDir.listFiles())
      if (file.getName().endsWith(SyncConstant.PIPE_LOG_NAME_SUFFIX) && file.length() > 0) {
        startNumbers.add(SyncPathUtil.getSerialNumberFromPipeLogName(file.getName()));
```

### RuleId[ruleID=DataFlowIssue]
Argument `pipeData` might be null
in `server/src/main/java/org/apache/iotdb/db/sync/pipedata/queue/BufferedPipeDataQueue.java`
#### Snippet
```java
          String.format("Blocking pull pipe data number %s error.", commitSerialNumber + 1), e);
    }
    outputDeque.addFirst(pipeData);
    pullSerialNumber = pipeData.getSerialNumber();
    return pipeData;
```

### RuleId[ruleID=DataFlowIssue]
Method invocation `hasNext` may produce `NullPointerException`
in `server/src/main/java/org/apache/iotdb/db/query/reader/universal/CachedPriorityMergeReader.java`
#### Snippet
```java
      while (heap.peek() != null && heap.peek().currTime() == lastTimestamp) {
        top = heap.poll();
        if (top.hasNext()) {
          top.next();
          heap.add(top);
```

### RuleId[ruleID=DataFlowIssue]
Method invocation `isSeq` may produce `NullPointerException`
in `server/src/main/java/org/apache/iotdb/db/query/reader/series/SeriesReader.java`
#### Snippet
```java
    while (!cachedChunkMetadata.isEmpty()
        && orderUtils.isOverlapped(endpointTime, cachedChunkMetadata.peek().getStatistics())) {
      if (cachedChunkMetadata.peek().isSeq() && hasMeetSeq) {
        break;
      } else if (cachedChunkMetadata.peek().isSeq()) {
```

### RuleId[ruleID=DataFlowIssue]
Method invocation `isSeq` may produce `NullPointerException`
in `server/src/main/java/org/apache/iotdb/db/query/reader/series/SeriesReader.java`
#### Snippet
```java
      if (cachedChunkMetadata.peek().isSeq() && hasMeetSeq) {
        break;
      } else if (cachedChunkMetadata.peek().isSeq()) {
        hasMeetSeq = true;
      }
```

### RuleId[ruleID=DataFlowIssue]
Method invocation `getStatistics` may produce `NullPointerException`
in `server/src/main/java/org/apache/iotdb/db/query/reader/series/SeriesReader.java`
#### Snippet
```java
    if (!seqPageReaders.isEmpty() && !unSeqPageReaders.isEmpty()) {
      if (orderUtils.isTakeSeqAsFirst(
          seqPageReaders.get(0).getStatistics(), unSeqPageReaders.peek().getStatistics())) {
        firstPageReader = seqPageReaders.get(0);
      } else {
```

### RuleId[ruleID=DataFlowIssue]
Method invocation `getStatistics` may produce `NullPointerException`
in `server/src/main/java/org/apache/iotdb/db/query/reader/series/SeriesReader.java`
#### Snippet
```java
          orderUtils.getCurrentEndPoint(
              seqTimeSeriesMetadata.get(0).getStatistics(),
              unSeqTimeSeriesMetadata.peek().getStatistics());
    }

```

### RuleId[ruleID=DataFlowIssue]
Method invocation `getStatistics` may produce `NullPointerException`
in `server/src/main/java/org/apache/iotdb/db/query/reader/series/SeriesReader.java`
#### Snippet
```java
      if (orderUtils.isTakeSeqAsFirst(
          seqTimeSeriesMetadata.get(0).getStatistics(),
          unSeqTimeSeriesMetadata.peek().getStatistics())) {
        firstTimeSeriesMetadata = seqTimeSeriesMetadata.remove(0);
      } else {
```

### RuleId[ruleID=DataFlowIssue]
Method invocation `getStatistics` may produce `NullPointerException`
in `server/src/main/java/org/apache/iotdb/db/query/reader/series/SeriesReader.java`
#### Snippet
```java
        || (!unSeqPageReaders.isEmpty()
                && orderUtils.isOverlapped(
                    firstPageReader.getStatistics(), unSeqPageReaders.peek().getStatistics())
            || (mergeReader.hasNextTimeValuePair()
                && orderUtils.isOverlapped(
```

### RuleId[ruleID=DataFlowIssue]
Argument `unSeqPageReaders.poll()` might be null
in `server/src/main/java/org/apache/iotdb/db/query/reader/series/SeriesReader.java`
#### Snippet
```java
    while (!unSeqPageReaders.isEmpty()
        && orderUtils.isOverlapped(endpointTime, unSeqPageReaders.peek().data.getStatistics())) {
      putPageReaderToMergeReader(unSeqPageReaders.poll());
    }
    if (firstPageReader != null
```

### RuleId[ruleID=DataFlowIssue]
Variable is already assigned to this value
in `server/src/main/java/org/apache/iotdb/db/query/dataset/RawQueryDataSetWithoutValueFilter.java`
#### Snippet
```java

      if (!hasNull) {
        isAllNull = false;
      }

```

### RuleId[ruleID=DataFlowIssue]
Condition `schema instanceof MeasurementSchema` is redundant and can be replaced with a null check
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
    for (int i = 0; i < tablet.getSchemas().size(); i++) {
      IMeasurementSchema schema = tablet.getSchemas().get(i);
      if (schema instanceof MeasurementSchema) {
        tablet.values[columnIndex] = sortList(tablet.values[columnIndex], schema.getType(), index);
        if (tablet.bitMaps != null && tablet.bitMaps[columnIndex] != null) {
```

### RuleId[ruleID=DataFlowIssue]
Method invocation `getSubMeasurementsList` will produce `NullPointerException`
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
        columnIndex++;
      } else {
        int measurementSize = schema.getSubMeasurementsList().size();
        for (int j = 0; j < measurementSize; j++) {
          tablet.values[columnIndex] =
```

### RuleId[ruleID=DataFlowIssue]
Method invocation `getEndTime` may produce `NullPointerException`
in `server/src/main/java/org/apache/iotdb/db/query/executor/fill/LastPointReader.java`
#### Snippet
```java
    PriorityQueue<IChunkMetadata> sortedChunkMetatdataList = sortUnseqChunkMetadatasByEndtime();
    while (!sortedChunkMetatdataList.isEmpty()
        && resultPoint.getTimestamp() <= sortedChunkMetatdataList.peek().getEndTime()) {
      IChunkMetadata chunkMetadata = sortedChunkMetatdataList.poll();
      TimeValuePair chunkLastPoint = getChunkLastPoint(chunkMetadata);
```

### RuleId[ruleID=DataFlowIssue]
Argument `chunkMetadata` might be null
in `server/src/main/java/org/apache/iotdb/db/query/executor/fill/LastPointReader.java`
#### Snippet
```java
      if (chunkLastPoint.getTimestamp() > resultPoint.getTimestamp()
          || (chunkLastPoint.getTimestamp() == resultPoint.getTimestamp()
              && (cachedLastChunk == null || shouldUpdate(cachedLastChunk, chunkMetadata)))) {
        cachedLastChunk = chunkMetadata;
        resultPoint = chunkLastPoint;
```

### RuleId[ruleID=DataFlowIssue]
Dereference of `tsFiles` may produce `NullPointerException`
in `server/src/main/java/org/apache/iotdb/db/tools/settle/TsFileAndModSettleTool.java`
#### Snippet
```java
    }
    List<File> tsFiles = checkArgs(args);
    for (File file : tsFiles) {
      if (!oldTsFileResources.containsKey(file.getName())) {
        if (new File(file + TsFileResource.RESOURCE_SUFFIX).exists()) {
```

### RuleId[ruleID=DataFlowIssue]
Dereference of `loadDir.listFiles()` may produce `NullPointerException`
in `server/src/main/java/org/apache/iotdb/db/engine/load/LoadTsFileManager.java`
#### Snippet
```java
    }

    for (File taskDir : loadDir.listFiles()) {
      String uuid = taskDir.getName();
      TsFileWriterManager writerManager = new TsFileWriterManager(taskDir);
```

### RuleId[ruleID=DataFlowIssue]
Method invocation `getHeader` may produce `NullPointerException`
in `server/src/main/java/org/apache/iotdb/db/engine/cache/ChunkCache.java`
#### Snippet
```java

    return new Chunk(
        chunk.getHeader(),
        chunk.getData().duplicate(),
        chunkMetaData.getDeleteIntervalList(),
```

### RuleId[ruleID=DataFlowIssue]
Unboxing of `satisfiedLengthQueue.poll()` may produce `NullPointerException`
in `server/src/main/java/org/apache/iotdb/db/engine/load/AlignedChunkData.java`
#### Snippet
```java
    long startTime = timePartitionSlot.getStartTime();
    long endTime = startTime + TimePartitionUtils.getTimePartitionInterval();
    int satisfiedLength = satisfiedLengthQueue.poll();
    dataSize += ReadWriteIOUtils.write(true, stream);
    dataSize += ReadWriteIOUtils.write(satisfiedLength, stream);
```

### RuleId[ruleID=DataFlowIssue]
Argument `dataRegion` might be null
in `server/src/main/java/org/apache/iotdb/db/engine/StorageEngineV2.java`
#### Snippet
```java
                    "Failed to recover data region {}[{}]", sgName, dataRegionId.getId(), e);
              }
              dataRegionMap.put(dataRegionId, dataRegion);
              logger.info(
                  "Data regions have been recovered {}/{}",
```

### RuleId[ruleID=DataFlowIssue]
Dereference of `sgDir.listFiles()` may produce `NullPointerException`
in `server/src/main/java/org/apache/iotdb/db/engine/StorageEngineV2.java`
#### Snippet
```java
      String sgName = sgDir.getName();
      List<DataRegionId> dataRegionIdList = new ArrayList<>();
      for (File dataRegionDir : sgDir.listFiles()) {
        if (!dataRegionDir.isDirectory()) {
          continue;
```

### RuleId[ruleID=DataFlowIssue]
Dereference of `fileFolder.listFiles()` may produce `NullPointerException`
in `server/src/main/java/org/apache/iotdb/db/engine/upgrade/UpgradeTask.java`
#### Snippet
```java
        continue;
      }
      for (File storageGroup : fileFolder.listFiles()) {
        if (!storageGroup.isDirectory()) {
          continue;
```

### RuleId[ruleID=DataFlowIssue]
Dereference of `upgradeFolder.listFiles()` may produce `NullPointerException`
in `server/src/main/java/org/apache/iotdb/db/engine/upgrade/UpgradeTask.java`
#### Snippet
```java
    try {
      File upgradeFolder = upgradeResource.getTsFile().getParentFile();
      for (File tempPartitionDir : upgradeFolder.listFiles()) {
        if (tempPartitionDir.isDirectory()
            && fsFactory
```

### RuleId[ruleID=DataFlowIssue]
Method invocation `get` may produce `NullPointerException`
in `server/src/main/java/org/apache/iotdb/db/engine/load/TsFileSplitter.java`
#### Snippet
```java
                    .writeEntirePage(pageHeader, reader.readCompressedPage(pageHeader));
              } else { // decode page
                long[] times = pageIndex2Times.get(pageIndex);
                TsPrimitiveType[] values =
                    decodeValuePage(reader, header, pageHeader, times, valueDecoder);
```

### RuleId[ruleID=DataFlowIssue]
Variable is already assigned to this value
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/cross/utils/ReadPointCrossCompactionEstimator.java`
#### Snippet
```java
            "calculateReadingSeqFiles(), find 1 empty seq tsFile: {}.",
            seqResource.getTsFilePath());
        seqFileCost = 0;
      } else {
        seqFileCost =
```

### RuleId[ruleID=DataFlowIssue]
Casting `o2` to `InnerSpaceCompactionTask` may produce `ClassCastException`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/comparator/DefaultCompactionTaskComparatorImpl.java`
#### Snippet
```java
    if (o1 instanceof InnerSpaceCompactionTask) {
      return compareInnerSpaceCompactionTask(
          (InnerSpaceCompactionTask) o1, (InnerSpaceCompactionTask) o2);
    } else {
      return compareCrossSpaceCompactionTask(
```

### RuleId[ruleID=DataFlowIssue]
Casting `o1` to `CrossSpaceCompactionTask` may produce `ClassCastException`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/comparator/DefaultCompactionTaskComparatorImpl.java`
#### Snippet
```java
    } else {
      return compareCrossSpaceCompactionTask(
          (CrossSpaceCompactionTask) o1, (CrossSpaceCompactionTask) o2);
    }
  }
```

### RuleId[ruleID=DataFlowIssue]
Casting `o2` to `CrossSpaceCompactionTask` may produce `ClassCastException`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/comparator/DefaultCompactionTaskComparatorImpl.java`
#### Snippet
```java
    } else {
      return compareCrossSpaceCompactionTask(
          (CrossSpaceCompactionTask) o1, (CrossSpaceCompactionTask) o2);
    }
  }
```

### RuleId[ruleID=DataFlowIssue]
Dereference of `currTsFileResource` may produce `NullPointerException`
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileResourceList.java`
#### Snippet
```java
    }
    while (currIndex != index) {
      if (currTsFileResource.next == null) {
        if (currIndex == index - 1) {
          insertAfter(currTsFileResource, element);
```

### RuleId[ruleID=DataFlowIssue]
Method invocation `getWorkMemTableRamCost` may produce `NullPointerException`
in `server/src/main/java/org/apache/iotdb/db/rescon/SystemInfo.java`
#### Snippet
```java
      }
      TsFileProcessor selectedTsFileProcessor = allTsFileProcessors.peek();
      memCost += selectedTsFileProcessor.getWorkMemTableRamCost();
      selectedTsFileProcessor.setWorkMemTableShouldFlush();
      flushTaskSubmitThreadPool.submit(
```

### RuleId[ruleID=DataFlowIssue]
Method invocation `intern` may produce `NullPointerException`
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/timeindex/DeviceTimeIndex.java`
#### Snippet
```java
    Set<String> devices = new HashSet<>();
    for (int i = 0; i < deviceNum; i++) {
      String path = ReadWriteIOUtils.readString(inputStream).intern();
      inputStream.skip(ReadWriteIOUtils.INT_LEN);
      devices.add(path);
```

### RuleId[ruleID=DataFlowIssue]
Method invocation `intern` may produce `NullPointerException`
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/timeindex/DeviceTimeIndex.java`
#### Snippet
```java

    for (int i = 0; i < deviceNum; i++) {
      String path = ReadWriteIOUtils.readString(inputStream).intern();
      int index = ReadWriteIOUtils.readInt(inputStream);
      deviceToIndex.put(path, index);
```

### RuleId[ruleID=DataFlowIssue]
Method invocation `intern` may produce `NullPointerException`
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileResource.java`
#### Snippet
```java
        String path = ReadWriteIOUtils.readString(inputStream);
        long time = ReadWriteIOUtils.readLong(inputStream);
        deviceMap.put(path.intern(), i);
        startTimesArray[i] = time;
      }
```

### RuleId[ruleID=DataFlowIssue]
Switch label `"METADATA_IN_JSON"` is unreachable
in `server/src/main/java/org/apache/iotdb/db/service/thrift/impl/TSServiceImpl.java`
#### Snippet
```java
    try {
      switch (req.getType()) {
        case "METADATA_IN_JSON":
          resp.setMetadataInJson("{}");
          status = RpcUtils.getStatus(TSStatusCode.SUCCESS_STATUS);
```

### RuleId[ruleID=DataFlowIssue]
Switch label `"COLUMN"` is unreachable
in `server/src/main/java/org/apache/iotdb/db/service/thrift/impl/TSServiceImpl.java`
#### Snippet
```java
          status = RpcUtils.getStatus(TSStatusCode.SUCCESS_STATUS);
          break;
        case "COLUMN":
          resp.setDataType(getSeriesTypeByPath(new PartialPath(req.getColumnPath())).toString());
          status = RpcUtils.getStatus(TSStatusCode.SUCCESS_STATUS);
```

### RuleId[ruleID=DataFlowIssue]
Switch label `"ALL_COLUMNS"` is unreachable
in `server/src/main/java/org/apache/iotdb/db/service/thrift/impl/TSServiceImpl.java`
#### Snippet
```java
          status = RpcUtils.getStatus(TSStatusCode.SUCCESS_STATUS);
          break;
        case "ALL_COLUMNS":
          resp.setColumnsList(
              getPaths(new PartialPath(req.getColumnPath())).stream()
```

### RuleId[ruleID=DataFlowIssue]
Argument `resp` might be null
in `server/src/main/java/org/apache/iotdb/db/service/thrift/impl/TSServiceImpl.java`
#### Snippet
```java
    if (newDataSet.getEndPoint() != null && plan.isEnableRedirect()) {
      QueryDataSet.EndPoint endPoint = newDataSet.getEndPoint();
      return redirectQueryToAnotherNode(resp, context, endPoint.getIp(), endPoint.getPort());
    }

```

### RuleId[ruleID=DataFlowIssue]
Dereference of `resources.get(0).getTsFile().getParentFile().listFiles()` may produce `NullPointerException`
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/DataRegion.java`
#### Snippet
```java
    // delete upgrade folder when it is empty
    if (resources.get(0).getTsFile().getParentFile().isDirectory()
        && resources.get(0).getTsFile().getParentFile().listFiles().length == 0) {
      try {
        Files.delete(resources.get(0).getTsFile().getParentFile().toPath());
```

### RuleId[ruleID=DataFlowIssue]
Method invocation `getBytes` may produce `NullPointerException`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/AliasIndexPage.java`
#### Snippet
```java
        // migrate newly insert
        mKey = key;
        recSize = 8 + entry.getBytes().length + mKey.getBytes().length;

        spareOffset -= recSize;
```

### RuleId[ruleID=DataFlowIssue]
Method invocation `getBytes` may produce `NullPointerException`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/Segment.java`
#### Snippet
```java
        recSize = recBuf.capacity();
        mKey = key;
        keySize = 4 + mKey.getBytes().length;

        recBuf.clear();
```

### RuleId[ruleID=DataFlowIssue]
Unboxing of `cascadePages.poll()` may produce `NullPointerException`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/pagemgr/BTreePageManager.java`
#### Snippet
```java
          }

          tarPage = getPageInstance(cascadePages.poll());
          if (tarPage.getAsSegmentedPage() != null) {
            tarPage.getAsSegmentedPage().purgeSegments();
```

### RuleId[ruleID=DataFlowIssue]
Method invocation `getAsEntityMNode` may produce `NullPointerException`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/traverser/collector/MeasurementCollector.java`
#### Snippet
```java
        new MeasurementPath(
            new PartialPath(getCurrentPathNodes(currentNode)), currentNode.getSchema());
    retPath.setUnderAlignedEntity(par.getAsEntityMNode().isAligned());
    return retPath;
  }
```

### RuleId[ruleID=DataFlowIssue]
Method invocation `getAsEntityMNode` may produce `NullPointerException`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/traverser/collector/MeasurementCollector.java`
#### Snippet
```java

  protected boolean isUnderAlignedEntity() {
    return traverseContext.peek().getAsEntityMNode().isAligned();
  }
}
```

### RuleId[ruleID=DataFlowIssue]
Method invocation `addChild` may produce `NullPointerException`
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/snapshot/MemMTreeSnapshotUtil.java`
#### Snippet
```java
    if (!ancestors.isEmpty()) {
      node.setParent(ancestors.peek());
      ancestors.peek().addChild(node);
    }

```

### RuleId[ruleID=DataFlowIssue]
Argument `res.deviceID` might be null
in `server/src/main/java/org/apache/iotdb/db/metadata/idtable/entry/DiskSchemaEntry.java`
#### Snippet
```java
    res.entrySize = ReadWriteIOUtils.readInt(inputStream);
    res.entrySize += Integer.BYTES;
    if (isTombstone(res.deviceID)) res.deviceID = TOMBSTONE;
    return res;
  }
```

### RuleId[ruleID=DataFlowIssue]
Method invocation `equals` may produce `NullPointerException`
in `server/src/main/java/org/apache/iotdb/db/metadata/template/Template.java`
#### Snippet
```java

    synchronized (this) {
      if (prefix.equals("")) {
        isDirectAligned = true;
      }
```

### RuleId[ruleID=DataFlowIssue]
Method invocation `getAsEntityMNode` may produce `NullPointerException`
in `server/src/main/java/org/apache/iotdb/db/metadata/template/Template.java`
#### Snippet
```java
        } else {
          commonPar = constructEntityPath(alignedPaths[0]);
          commonPar.getAsEntityMNode().setAligned(true);
          leafNode =
              MeasurementMNode.getMeasurementMNode(
```

### RuleId[ruleID=DataFlowIssue]
Passing `null` argument to parameter annotated as @NotNull
in `server/src/main/java/org/apache/iotdb/db/protocol/rest/impl/GrafanaApiServiceImpl.java`
#### Snippet
```java
        }
      } else {
        return QueryDataSetHandler.fillGrafanaNodesResult(null);
      }
    } catch (Exception e) {
```

### RuleId[ruleID=DataFlowIssue]
Method invocation `getStartSyncIndex` may produce `NullPointerException`
in `server/src/main/java/org/apache/iotdb/db/consensus/statemachine/DataRegionStateMachine.java`
#### Snippet
```java
              // other thread who hold the peek request. In this scenario, current thread
              // should go into await again and wait until its request becoming peek request
              if (requestCache.peek().getStartSyncIndex()
                  == insertNodeWrapper.getStartSyncIndex()) {
                // current thread hold the peek request thus it can write the peek immediately.
```

### RuleId[ruleID=DataFlowIssue]
Argument `wrapper` might be null
in `example/session/src/main/java/org/apache/iotdb/SessionPoolExample.java`
#### Snippet
```java
            } finally {
              // remember to close data set finally!
              sessionPool.closeResultSet(wrapper);
            }
          });
```

### RuleId[ruleID=DataFlowIssue]
Argument `wrapper` might be null
in `example/session/src/main/java/org/apache/iotdb/SessionPoolExample.java`
#### Snippet
```java
            } finally {
              // remember to close data set finally!
              sessionPool.closeResultSet(wrapper);
            }
          });
```

### RuleId[ruleID=DataFlowIssue]
Argument `dataSet` might be null
in `example/session/src/main/java/org/apache/iotdb/DataMigrationExample.java`
#### Snippet
```java
        return null;
      } finally {
        readerPool.closeResultSet(dataSet);
      }

```

### RuleId[ruleID=DataFlowIssue]
Variable is already assigned to this value
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/Resampler.java`
#### Snippet
```java
    switch (interpolator) {
      case "nan":
        ret = Double.NaN;
        break;
      case "ffill":
```

### RuleId[ruleID=DataFlowIssue]
Dereference of `file.listFiles()` may produce `NullPointerException`
in `rewrite-tsfile-tool/src/main/java/org/apache/iotdb/RewriteTsFileTool.java`
#### Snippet
```java
    }
    List<File> list = new ArrayList<>();
    for (File listFile : file.listFiles()) {
      if (listFile.isDirectory()) {
        list.addAll(collectTsFiles(listFile));
```

### RuleId[ruleID=DataFlowIssue]
Method invocation `getCode` may produce `NullPointerException`
in `confignode/src/main/java/org/apache/iotdb/confignode/service/ConfigNode.java`
#### Snippet
```java
      }

      if (status.getCode() == TSStatusCode.SUCCESS_STATUS.getStatusCode()) {
        /* Always set ConfigNodeId before initConsensusManager */
        CONF.setConfigNodeId(resp.getConfigNodeId());
```

### RuleId[ruleID=DataFlowIssue]
Method invocation `getConfigNodeId` may produce `NullPointerException`
in `confignode/src/main/java/org/apache/iotdb/confignode/service/ConfigNode.java`
#### Snippet
```java
      if (status.getCode() == TSStatusCode.SUCCESS_STATUS.getStatusCode()) {
        /* Always set ConfigNodeId before initConsensusManager */
        CONF.setConfigNodeId(resp.getConfigNodeId());
        configManager.initConsensusManager();
        return;
```

### RuleId[ruleID=DataFlowIssue]
Method invocation `tryRunnable` may produce `NullPointerException`
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/ProcedureExecutor.java`
#### Snippet
```java
      return;
    }
    if (parent.tryRunnable()) {
      // if success, means all its children have completed, move parent to front of the queue.
      store.update(parent);
```

### RuleId[ruleID=DataFlowIssue]
Method invocation `peek` may produce `NullPointerException`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/file/metadata/MetadataIndexConstructor.java`
#### Snippet
```java
        }
        currentIndexNode.addEntry(
            new MetadataIndexEntry(metadataIndexNode.peek().getName(), out.getPosition()));
        metadataIndexNode.serializeTo(out.wrapAsStream());
      }
```

### RuleId[ruleID=DataFlowIssue]
Variable is already assigned to this value
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BytesUtils.java`
#### Snippet
```java
      b[0] = 1;
    } else {
      b[0] = 0;
    }
    return b;
```

### RuleId[ruleID=DataFlowIssue]
Variable is already assigned to this value
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/RamUsageEstimator.java`
#### Snippet
```java
            is64Bit = true;
          } else {
            is64Bit = false;
          }
        }
```

### RuleId[ruleID=DataFlowIssue]
Method invocation `inc` may produce `NullPointerException`
in `metrics/interface/src/main/java/org/apache/iotdb/metrics/metricsets/jvm/JvmGcMetrics.java`
#### Snippet
```java
              final long delta = oldAfter - oldBefore;
              if (delta > 0L) {
                promotedBytes.inc(delta);
              }

```

### RuleId[ruleID=DataFlowIssue]
Unboxing of `input.getField(timeIndex)` may produce `NullPointerException`
in `flink-tsfile-connector/src/main/java/org/apache/iotdb/flink/tsfile/RowTSRecordConverter.java`
#### Snippet
```java
  @Override
  public void convert(Row input, Collector<TSRecord> collector) {
    long timestamp = (long) input.getField(timeIndex);
    for (TSRecord tsRecord : reuse) {
      tsRecord.dataPointList.clear();
```

### RuleId[ruleID=DataFlowIssue]
Variable is already assigned to this value
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/RSchemaRegion.java`
#### Snippet
```java
      if (currentIsFlag) {
        j++;
        currentIsFlag = true;
      }
      if (j <= 0 || lastIsFlag || (currentIsFlag && j > level)) {
```

## RuleId[ruleID=DeprecatedIsStillUsed]
### RuleId[ruleID=DeprecatedIsStillUsed]
Deprecated member 'OldIntermediateLayerVisitor' is still used
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/expression/visitor/OldIntermediateLayerVisitor.java`
#### Snippet
```java
@Deprecated
// remove after finishing MPP
public class OldIntermediateLayerVisitor
    extends ExpressionVisitor<
        IntermediateLayer, OldIntermediateLayerVisitor.OldIntermediateLayerVisitorContext> {
```

### RuleId[ruleID=DeprecatedIsStillUsed]
Deprecated member 'INSERT_TABLET_PLAN' is still used
in `server/src/main/java/org/apache/iotdb/db/wal/buffer/WALEntryType.java`
#### Snippet
```java
  INSERT_ROW_PLAN((byte) 0),
  @Deprecated
  INSERT_TABLET_PLAN((byte) 1),
  @Deprecated
  DELETE_PLAN((byte) 2),
```

### RuleId[ruleID=DeprecatedIsStillUsed]
Deprecated member 'DELETE_PLAN' is still used
in `server/src/main/java/org/apache/iotdb/db/wal/buffer/WALEntryType.java`
#### Snippet
```java
  INSERT_TABLET_PLAN((byte) 1),
  @Deprecated
  DELETE_PLAN((byte) 2),
  MEMORY_TABLE_SNAPSHOT((byte) 3),
  /** {@link org.apache.iotdb.db.mpp.plan.planner.plan.node.write.InsertRowNode} */
```

### RuleId[ruleID=DeprecatedIsStillUsed]
Deprecated member 'INSERT_ROW_PLAN' is still used
in `server/src/main/java/org/apache/iotdb/db/wal/buffer/WALEntryType.java`
#### Snippet
```java
  // region info entry type
  @Deprecated
  INSERT_ROW_PLAN((byte) 0),
  @Deprecated
  INSERT_TABLET_PLAN((byte) 1),
```

### RuleId[ruleID=DeprecatedIsStillUsed]
Deprecated member 'preAllocateSegment' is still used
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/pagemgr/PageManager.java`
#### Snippet
```java
  @Deprecated
  // TODO: improve to remove
  private long preAllocateSegment(short size) throws IOException, MetadataException {
    ISegmentedPage page = getMinApplSegmentedPageInMem((short) (size + SEG_OFF_DIG));
    return SchemaFile.getGlobalIndex(page.getPageIndex(), page.allocNewSegment(size));
```

### RuleId[ruleID=DeprecatedIsStillUsed]
Deprecated member 'ListUserRoles' is still used
in `confignode/src/main/java/org/apache/iotdb/confignode/consensus/request/ConfigPhysicalPlanType.java`
#### Snippet
```java
  ListRolePrivilege((short) 615),
  @Deprecated
  ListUserRoles((short) 616),
  @Deprecated
  ListRoleUsers((short) 617),
```

### RuleId[ruleID=DeprecatedIsStillUsed]
Deprecated member 'ListRoleUsers' is still used
in `confignode/src/main/java/org/apache/iotdb/confignode/consensus/request/ConfigPhysicalPlanType.java`
#### Snippet
```java
  ListUserRoles((short) 616),
  @Deprecated
  ListRoleUsers((short) 617),

  /** Function */
```

### RuleId[ruleID=DeprecatedIsStillUsed]
Deprecated member 'getFilterType' is still used
in `hadoop/src/main/java/org/apache/iotdb/hadoop/tsfile/TSFInputFormat.java`
#### Snippet
```java
  // check if not set the filter type, the result will null or empty
  @Deprecated
  public static String getFilterType(Configuration configuration) {
    return configuration.get(FILTER_TYPE);
  }
```

### RuleId[ruleID=DeprecatedIsStillUsed]
Deprecated member 'setHasFilter' is still used
in `hadoop/src/main/java/org/apache/iotdb/hadoop/tsfile/TSFInputFormat.java`
#### Snippet
```java
   */
  @Deprecated
  public static void setHasFilter(Job job, boolean value) {
    job.getConfiguration().setBoolean(FILTER_EXIST, value);
  }
```

### RuleId[ruleID=DeprecatedIsStillUsed]
Deprecated member 'setFilterType' is still used
in `hadoop/src/main/java/org/apache/iotdb/hadoop/tsfile/TSFInputFormat.java`
#### Snippet
```java
   */
  @Deprecated
  public static void setFilterType(Job job, String value) {
    job.getConfiguration().set(FILTER_TYPE, value);
  }
```

### RuleId[ruleID=DeprecatedIsStillUsed]
Deprecated member 'getFilterExp' is still used
in `hadoop/src/main/java/org/apache/iotdb/hadoop/tsfile/TSFInputFormat.java`
#### Snippet
```java

  @Deprecated
  public static String getFilterExp(Configuration configuration) {
    return configuration.get(FILTER_EXPRESSION);
  }
```

### RuleId[ruleID=DeprecatedIsStillUsed]
Deprecated member 'setFilterExp' is still used
in `hadoop/src/main/java/org/apache/iotdb/hadoop/tsfile/TSFInputFormat.java`
#### Snippet
```java

  @Deprecated
  public static void setFilterExp(Job job, String value) {
    job.getConfiguration().set(FILTER_EXPRESSION, value);
  }
```

### RuleId[ruleID=DeprecatedIsStillUsed]
Deprecated member 'getHasFilter' is still used
in `hadoop/src/main/java/org/apache/iotdb/hadoop/tsfile/TSFInputFormat.java`
#### Snippet
```java
   */
  @Deprecated
  public static boolean getHasFilter(Configuration configuration) {
    return configuration.getBoolean(FILTER_EXIST, false);
  }
```

## RuleId[ruleID=Convert2MethodRef]
### RuleId[ruleID=Convert2MethodRef]
Lambda can be replaced with method reference
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/AnalyzeVisitor.java`
#### Snippet
```java
        outputExpressionMap
            .values()
            .forEach(outputExpressionList -> outputExpressions.addAll(outputExpressionList));
        analyzeHaving(analysis, queryStatement, schemaTree);
        analyzeGroupByLevel(analysis, queryStatement, outputExpressionMap, outputExpressions);
```

### RuleId[ruleID=Convert2MethodRef]
Lambda can be replaced with method reference
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileSplitByPartitionTool.java`
#### Snippet
```java
        .forEach(
            writer -> {
              writer.sealCurrentPage();
            });
  }
```

### RuleId[ruleID=Convert2MethodRef]
Lambda can be replaced with method reference
in `server/src/main/java/org/apache/iotdb/db/rescon/SystemInfo.java`
#### Snippet
```java
      flushTaskSubmitThreadPool.submit(
          () -> {
            selectedTsFileProcessor.submitAFlushTask();
          });
      if (selectedTsFileProcessor == currentTsFileProcessor) {
```

### RuleId[ruleID=Convert2MethodRef]
Lambda can be replaced with method reference
in `consensus/src/main/java/org/apache/iotdb/consensus/ratis/RatisConsensus.java`
#### Snippet
```java
              properties,
              clientRpc,
              MemoizedSupplier.valueOf(() -> config.getRatisConsensus())),
          new ClientPoolProperty.Builder<RatisClient>().build().getConfig());
    }
```

## RuleId[ruleID=NonSerializableFieldInSerializableClass]
### RuleId[ruleID=NonSerializableFieldInSerializableClass]
Non-serializable field 'cacheEntry' in a Serializable class
in `server/src/main/java/org/apache/iotdb/db/metadata/mnode/MNode.java`
#### Snippet
```java
  protected String fullPath;

  protected CacheEntry cacheEntry;

  /** Constructor of MNode. */
```

### RuleId[ruleID=NonSerializableFieldInSerializableClass]
Non-serializable field 'schemaEntry' in a Serializable class
in `server/src/main/java/org/apache/iotdb/db/metadata/idtable/entry/InsertMeasurementMNode.java`
#### Snippet
```java
 */
public class InsertMeasurementMNode implements IMeasurementMNode {
  SchemaEntry schemaEntry;

  IMeasurementSchema schema;
```

### RuleId[ruleID=NonSerializableFieldInSerializableClass]
Non-serializable field 'readWriteHandler' in a Serializable class
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/mnode/RMNode.java`
#### Snippet
```java
  protected String fullPath;

  protected RSchemaReadWriteHandler readWriteHandler;

  protected IMNode parent;
```

### RuleId[ruleID=NonSerializableFieldInSerializableClass]
Non-serializable field 'fos' in a Serializable class
in `flink-tsfile-connector/src/main/java/org/apache/iotdb/flink/tsfile/TsFileOutputFormat.java`
#### Snippet
```java

  protected transient Configuration hadoopConf = null;
  private FileOutputStream fos = null;
  protected transient TsFileWriter writer = null;

```

### RuleId[ruleID=NonSerializableFieldInSerializableClass]
Non-serializable field 'hdfsPath' in a Serializable class
in `hadoop/src/main/java/org/apache/iotdb/hadoop/fileSystem/HDFSFile.java`
#### Snippet
```java

  private static final long serialVersionUID = -8419827359081949547L;
  private Path hdfsPath;
  private FileSystem fs;
  private static final Logger logger = LoggerFactory.getLogger(HDFSFile.class);
```

## RuleId[ruleID=AbstractMethodCallInConstructor]
### RuleId[ruleID=AbstractMethodCallInConstructor]
Call to 'abstract' method `constructTextEvaluator()` during object construction
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/transformer/binary/CompareBinaryTransformer.java`
#### Snippet
```java
    evaluator =
        TSDataType.TEXT.equals(leftPointReaderDataType)
            ? constructTextEvaluator()
            : constructNumberEvaluator();
  }
```

### RuleId[ruleID=AbstractMethodCallInConstructor]
Call to 'abstract' method `constructNumberEvaluator()` during object construction
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/transformer/binary/CompareBinaryTransformer.java`
#### Snippet
```java
        TSDataType.TEXT.equals(leftPointReaderDataType)
            ? constructTextEvaluator()
            : constructNumberEvaluator();
  }

```

### RuleId[ruleID=AbstractMethodCallInConstructor]
Call to 'abstract' method `checkType()` during object construction
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/transformer/binary/BinaryTransformer.java`
#### Snippet
```java
    isRightPointReaderConstant = rightPointReader.isConstantPointReader();
    isCurrentConstant = isLeftPointReaderConstant && isRightPointReaderConstant;
    checkType();
  }

```

### RuleId[ruleID=AbstractMethodCallInConstructor]
Call to 'abstract' method `constructTextEvaluator()` during object construction
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/transformer/ternary/CompareTernaryTransformer.java`
#### Snippet
```java
    evaluator =
        TSDataType.TEXT.equals(firstPointReaderDataType)
            ? constructTextEvaluator()
            : constructNumberEvaluator();
  }
```

### RuleId[ruleID=AbstractMethodCallInConstructor]
Call to 'abstract' method `constructNumberEvaluator()` during object construction
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/transformer/ternary/CompareTernaryTransformer.java`
#### Snippet
```java
        TSDataType.TEXT.equals(firstPointReaderDataType)
            ? constructTextEvaluator()
            : constructNumberEvaluator();
  }

```

### RuleId[ruleID=AbstractMethodCallInConstructor]
Call to 'abstract' method `checkType()` during object construction
in `server/src/main/java/org/apache/iotdb/db/mpp/transformation/dag/transformer/ternary/TernaryTransformer.java`
#### Snippet
```java
    this.isCurrentConstant =
        isFirstPointReaderConstant && isSecondPointReaderConstant && isThirdPointReaderConstant;
    checkType();
  }

```

## RuleId[ruleID=RedundantCollectionOperation]
### RuleId[ruleID=RedundantCollectionOperation]
Unnecessary 'containsKey()' check
in `session/src/main/java/org/apache/iotdb/session/template/InternalNode.java`
#### Snippet
```java
  @Override
  public void deleteChild(TemplateNode node) {
    if (children.containsKey(node.getName())) {
      children.remove(node.getName());
    }
```

### RuleId[ruleID=RedundantCollectionOperation]
Unnecessary 'contains()' check
in `schema-engine-tag/src/main/java/org/apache/iotdb/db/metadata/tagSchemaRegion/tagIndex/deletion/MemTableDeletion.java`
#### Snippet
```java
    if (memNode.isImmutable()) {
      Set<Integer> deletionList = memNode.getDeletionList();
      if (!deletionList.contains(deletionRequest.getValue())) {
        deletionList.add(deletionRequest.getValue());
      }
```

### RuleId[ruleID=RedundantCollectionOperation]
`contains` can be replaced with 'Map.containsKey()'
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/RSchemaRegion.java`
#### Snippet
```java
                logger.warn(e.getMessage());
              }
              if (!measurementPathsAndTags.keySet().contains(partialName)) {
                result.put(path, result.get(path));
              } else {
```

## RuleId[ruleID=MismatchedJavadocCode]
### RuleId[ruleID=MismatchedJavadocCode]
Method is specified to return 'true' but there's no such enum constant in ColumnType
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/ExpressionAnalyzer.java`
#### Snippet
```java
   *
   * @param expression expression to be checked
   * @return true if this expression is valid
   */
  public static ResultColumn.ColumnType identifyOutputColumnType(
```

### RuleId[ruleID=MismatchedJavadocCode]
Method is specified to return set but the return type is array
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/Procedure.java`
#### Snippet
```java
   *
   * @param env the environment passed to the ProcedureExecutor
   * @return a set of sub-procedures to run or ourselves if there is more work to do or null if the
   *     procedure is done.
   * @throws ProcedureYieldException the procedure will be added back to the queue and retried
```

### RuleId[ruleID=MismatchedJavadocCode]
Method is specified to return list but the return type is array
in `tsfile/src/main/java/org/apache/iotdb/tsfile/fileSystem/fsFactory/FSFactory.java`
#### Snippet
```java
   * @param fileFolder file folder
   * @param prefix prefix
   * @return list of files
   */
  File[] listFilesByPrefix(String fileFolder, String prefix);
```

### RuleId[ruleID=MismatchedJavadocCode]
Method is specified to return list but the return type is array
in `tsfile/src/main/java/org/apache/iotdb/tsfile/fileSystem/fsFactory/FSFactory.java`
#### Snippet
```java
   * @param fileFolder file folder
   * @param suffix suffix
   * @return list of files
   */
  File[] listFilesBySuffix(String fileFolder, String suffix);
```

## RuleId[ruleID=CatchMayIgnoreException]
### RuleId[ruleID=CatchMayIgnoreException]
Empty `catch` block
in `jdbc/src/main/java/org/apache/iotdb/jdbc/StringUtils.java`
#### Snippet
```java
    try {
      toPlainStringMethod = BigDecimal.class.getMethod("toPlainString", new Class[0]);
    } catch (NoSuchMethodException nsme) {
    }
  }
```

### RuleId[ruleID=CatchMayIgnoreException]
Empty `catch` block
in `jdbc/src/main/java/org/apache/iotdb/jdbc/StringUtils.java`
#### Snippet
```java
      try {
        return (String) toPlainStringMethod.invoke(decimal, null);
      } catch (InvocationTargetException invokeEx) {

      } catch (IllegalAccessException accessEx) {
```

### RuleId[ruleID=CatchMayIgnoreException]
Empty `catch` block
in `jdbc/src/main/java/org/apache/iotdb/jdbc/StringUtils.java`
#### Snippet
```java
      } catch (InvocationTargetException invokeEx) {

      } catch (IllegalAccessException accessEx) {
      }
    }
```

### RuleId[ruleID=CatchMayIgnoreException]
Empty `catch` block
in `server/src/main/java/org/apache/iotdb/db/sync/pipedata/queue/BufferedPipeDataQueue.java`
#### Snippet
```java
        pipeData.add(PipeData.createPipeData(inputStream));
      }
    } catch (EOFException e) {
    } catch (IllegalPathException e) {
      logger.error(String.format("Parsing pipeLog %s error.", file.getPath()), e);
```

## RuleId[ruleID=UnnecessaryToStringCall]
### RuleId[ruleID=UnnecessaryToStringCall]
Unnecessary `toString()` call
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
        logger.warn(
            "All values are null and this submission is ignored,deviceIds are [{}],times are [{}],measurements are [{}]",
            deviceIds.toString(),
            times.toString(),
            measurementsList.toString());
```

### RuleId[ruleID=UnnecessaryToStringCall]
Unnecessary `toString()` call
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
            "All values are null and this submission is ignored,deviceIds are [{}],times are [{}],measurements are [{}]",
            deviceIds.toString(),
            times.toString(),
            measurementsList.toString());
        return;
```

### RuleId[ruleID=UnnecessaryToStringCall]
Unnecessary `toString()` call
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
            deviceIds.toString(),
            times.toString(),
            measurementsList.toString());
        return;
      }
```

### RuleId[ruleID=UnnecessaryToStringCall]
Unnecessary `toString()` call
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
    }
    if (valuesList.size() == 0) {
      logger.info("All values of the {} are null,null values are {}", deviceId, nullMap.toString());
      return true;
    } else {
```

### RuleId[ruleID=UnnecessaryToStringCall]
Unnecessary `toString()` call
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
      return true;
    } else {
      logger.info("Some values of {} are null,null values are {}", deviceId, nullMap.toString());
    }
    return false;
```

### RuleId[ruleID=UnnecessaryToStringCall]
Unnecessary `toString()` call
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
          "All values are null and this submission is ignored,deviceId is [{}],times are [{}],measurements are [{}]",
          deviceId,
          times.toString(),
          measurementsList.toString());
      return;
```

### RuleId[ruleID=UnnecessaryToStringCall]
Unnecessary `toString()` call
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
          deviceId,
          times.toString(),
          measurementsList.toString());
      return;
    }
```

### RuleId[ruleID=UnnecessaryToStringCall]
Unnecessary `toString()` call
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
          "All values are null and this submission is ignored,deviceId is [{}],times are [{}],measurements are [{}]",
          deviceId,
          times.toString(),
          measurementsList.toString());
      return;
```

### RuleId[ruleID=UnnecessaryToStringCall]
Unnecessary `toString()` call
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
          deviceId,
          times.toString(),
          measurementsList.toString());
      return;
    }
```

### RuleId[ruleID=UnnecessaryToStringCall]
Unnecessary `toString()` call
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
        logger.warn(
            "All values are null and this submission is ignored,deviceIds are [{}],times are [{}],measurements are [{}]",
            deviceIds.toString(),
            times.toString(),
            measurementsList.toString());
```

### RuleId[ruleID=UnnecessaryToStringCall]
Unnecessary `toString()` call
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
            "All values are null and this submission is ignored,deviceIds are [{}],times are [{}],measurements are [{}]",
            deviceIds.toString(),
            times.toString(),
            measurementsList.toString());
        return;
```

### RuleId[ruleID=UnnecessaryToStringCall]
Unnecessary `toString()` call
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
            deviceIds.toString(),
            times.toString(),
            measurementsList.toString());
        return;
      }
```

### RuleId[ruleID=UnnecessaryToStringCall]
Unnecessary `toString()` call
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
        logger.warn(
            "All values are null and this submission is ignored,deviceIds are [{}],times are [{}],measurements are [{}]",
            deviceIds.toString(),
            times.toString(),
            measurementsList.toString());
```

### RuleId[ruleID=UnnecessaryToStringCall]
Unnecessary `toString()` call
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
            "All values are null and this submission is ignored,deviceIds are [{}],times are [{}],measurements are [{}]",
            deviceIds.toString(),
            times.toString(),
            measurementsList.toString());
        return;
```

### RuleId[ruleID=UnnecessaryToStringCall]
Unnecessary `toString()` call
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
            deviceIds.toString(),
            times.toString(),
            measurementsList.toString());
        return;
      }
```

### RuleId[ruleID=UnnecessaryToStringCall]
Unnecessary `toString()` call
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
        logger.warn(
            "All values are null and this submission is ignored,deviceIds are [{}],times are [{}],measurements are [{}]",
            deviceIds.toString(),
            times.toString(),
            measurementsList.toString());
```

### RuleId[ruleID=UnnecessaryToStringCall]
Unnecessary `toString()` call
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
            "All values are null and this submission is ignored,deviceIds are [{}],times are [{}],measurements are [{}]",
            deviceIds.toString(),
            times.toString(),
            measurementsList.toString());
        return;
```

### RuleId[ruleID=UnnecessaryToStringCall]
Unnecessary `toString()` call
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
            deviceIds.toString(),
            times.toString(),
            measurementsList.toString());
        return;
      }
```

### RuleId[ruleID=UnnecessaryToStringCall]
Unnecessary `toString()` call
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
          "All values are null and this submission is ignored,deviceId is [{}],times are [{}],measurements are [{}]",
          deviceId,
          times.toString(),
          measurementsList.toString());
      return;
```

### RuleId[ruleID=UnnecessaryToStringCall]
Unnecessary `toString()` call
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
          deviceId,
          times.toString(),
          measurementsList.toString());
      return;
    }
```

### RuleId[ruleID=UnnecessaryToStringCall]
Unnecessary `toString()` call
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
    }
    if (valuesList.size() == 0) {
      logger.info("All values of the {} are null,null values are {}", deviceId, nullMap.toString());
      return true;
    } else {
```

### RuleId[ruleID=UnnecessaryToStringCall]
Unnecessary `toString()` call
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
      return true;
    } else {
      logger.info("Some values of {} are null,null values are {}", deviceId, nullMap.toString());
    }
    return false;
```

### RuleId[ruleID=UnnecessaryToStringCall]
Unnecessary `toString()` call
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
          "All values are null and this submission is ignored,deviceId is [{}],times are [{}],measurements are [{}]",
          deviceId,
          times.toString(),
          measurementsList.toString());
      return;
```

### RuleId[ruleID=UnnecessaryToStringCall]
Unnecessary `toString()` call
in `session/src/main/java/org/apache/iotdb/session/Session.java`
#### Snippet
```java
          deviceId,
          times.toString(),
          measurementsList.toString());
      return;
    }
```

### RuleId[ruleID=UnnecessaryToStringCall]
Unnecessary `toString()` call
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileSketchTool.java`
#### Snippet
```java
    }
    treeOutputStringBuffer.add(
        tableWriter.toString() + "[MetadataIndex:" + metadataIndexNode.getNodeType() + "]");
    for (int i = 0; i < metadataIndexNode.getChildren().size(); i++) {
      MetadataIndexEntry metadataIndexEntry = metadataIndexNode.getChildren().get(i);
```

### RuleId[ruleID=UnnecessaryToStringCall]
Unnecessary `toString()` call
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileSketchTool.java`
#### Snippet
```java

      treeOutputStringBuffer.add(
          tableWriter.toString()
              + "└──────["
              + metadataIndexEntry.getName()
```

### RuleId[ruleID=UnnecessaryToStringCall]
Unnecessary `toString()` call
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/log/CompactionLogger.java`
#### Snippet
```java
              + TsFileIdentifier.getFileIdentifierFromFilePath(
                      tsFileResource.getTsFile().getAbsolutePath())
                  .toString());
      logStream.newLine();
    }
```

### RuleId[ruleID=UnnecessaryToStringCall]
Unnecessary `toString()` call
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/impl/schema/UnsetTemplateProcedure.java`
#### Snippet
```java
          return Flow.NO_MORE_STATE;
        default:
          setFailure(new ProcedureException("Unrecognized state " + state.toString()));
          return Flow.NO_MORE_STATE;
      }
```

### RuleId[ruleID=UnnecessaryToStringCall]
Unnecessary `toString()` call
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/impl/schema/DeleteTimeSeriesProcedure.java`
#### Snippet
```java
          return Flow.NO_MORE_STATE;
        default:
          setFailure(new ProcedureException("Unrecognized state " + state.toString()));
          return Flow.NO_MORE_STATE;
      }
```

### RuleId[ruleID=UnnecessaryToStringCall]
Unnecessary `toString()` call
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/impl/schema/DeactivateTemplateProcedure.java`
#### Snippet
```java
          return Flow.NO_MORE_STATE;
        default:
          setFailure(new ProcedureException("Unrecognized state " + state.toString()));
          return Flow.NO_MORE_STATE;
      }
```

## RuleId[ruleID=AssignmentToForLoopParameter]
### RuleId[ruleID=AssignmentToForLoopParameter]
Assignment to for-loop parameter `j`
in `influxdb-protocol/src/main/java/org/apache/iotdb/influxdb/session/InfluxDBSession.java`
#### Snippet
```java
          defaultEndPoint = endPointList.get(j);
          if (j == endPointList.size() - 1) {
            j = -1;
          }
          tryHostNum++;
```

### RuleId[ruleID=AssignmentToForLoopParameter]
Assignment to for-loop parameter `j`
in `session/src/main/java/org/apache/iotdb/session/SessionConnection.java`
#### Snippet
```java
          this.endPoint = endPointList.get(j);
          if (j == endPointList.size() - 1) {
            j = -1;
          }
          tryHostNum++;
```

### RuleId[ruleID=AssignmentToForLoopParameter]
Assignment to for-loop parameter `i`
in `server/src/main/java/org/apache/iotdb/db/qp/utils/DateTimeUtils.java`
#### Snippet
```java
        // This is to identify units with two letters.
        if (i + 1 < duration.length() && !Character.isDigit(duration.charAt(i + 1))) {
          i++;
          unit += duration.charAt(i);
        }
```

### RuleId[ruleID=AssignmentToForLoopParameter]
Assignment to for-loop parameter `nodeIndex`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/analyze/SelectIntoUtils.java`
#### Snippet
```java
              "select into: placeholder `::` can only be used at the end of the path.");
        }
        for (; nodeIndex < sourceNodes.length; nodeIndex++) {
          targetNodes.add(sourceNodes[nodeIndex]);
        }
```

### RuleId[ruleID=AssignmentToForLoopParameter]
Assignment to for-loop parameter `i`
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/expression/unary/LikeExpression.java`
#### Snippet
```java
          }
          if ("\\".equals(nextChar)) {
            i++;
          }
        }
```

### RuleId[ruleID=AssignmentToForLoopParameter]
Assignment to for-loop parameter `i`
in `server/src/main/java/org/apache/iotdb/db/auth/ClusterAuthorityFetcher.java`
#### Snippet
```java
    for (int i = 0; i < privilegeList.size(); i++) {
      String path = privilegeList.get(i);
      String privilege = privilegeList.get(++i);
      pathPrivilegeList.add(toPathPrivilege(path, privilege));
    }
```

### RuleId[ruleID=AssignmentToForLoopParameter]
Assignment to for-loop parameter `i`
in `server/src/main/java/org/apache/iotdb/db/auth/ClusterAuthorityFetcher.java`
#### Snippet
```java
    for (int i = 0; i < privilegeList.size(); i++) {
      String path = privilegeList.get(i);
      String privilege = privilegeList.get(++i);
      pathPrivilegeList.add(toPathPrivilege(path, privilege));
    }
```

### RuleId[ruleID=AssignmentToForLoopParameter]
Assignment to for-loop parameter `bufferIndex`
in `server/src/main/java/org/apache/iotdb/db/query/dataset/RawQueryDataSetWithoutValueFilter.java`
#### Snippet
```java
                  i++) {
                currentBitmapList[bufferIndex] = (currentBitmapList[bufferIndex] << 1);
                bufferIndex++;
              }
            } else {
```

### RuleId[ruleID=AssignmentToForLoopParameter]
Assignment to for-loop parameter `bufferIndex`
in `server/src/main/java/org/apache/iotdb/db/query/dataset/RawQueryDataSetWithoutValueFilter.java`
#### Snippet
```java
            } else {
              currentBitmapList[bufferIndex] = (currentBitmapList[bufferIndex] << 1);
              bufferIndex++;
            }
          }
```

### RuleId[ruleID=AssignmentToForLoopParameter]
Assignment to for-loop parameter `bufferIndex`
in `server/src/main/java/org/apache/iotdb/db/query/dataset/RawQueryDataSetWithoutValueFilter.java`
#### Snippet
```java
                }
                ReadWriteIOUtils.write(intValue, valueBAOSList[bufferIndex]);
                bufferIndex++;
                break;
              case INT64:
```

### RuleId[ruleID=AssignmentToForLoopParameter]
Assignment to for-loop parameter `bufferIndex`
in `server/src/main/java/org/apache/iotdb/db/query/dataset/RawQueryDataSetWithoutValueFilter.java`
#### Snippet
```java
                }
                ReadWriteIOUtils.write(longValue, valueBAOSList[bufferIndex]);
                bufferIndex++;
                break;
              case FLOAT:
```

### RuleId[ruleID=AssignmentToForLoopParameter]
Assignment to for-loop parameter `bufferIndex`
in `server/src/main/java/org/apache/iotdb/db/query/dataset/RawQueryDataSetWithoutValueFilter.java`
#### Snippet
```java
                }
                ReadWriteIOUtils.write(floatValue, valueBAOSList[bufferIndex]);
                bufferIndex++;
                break;
              case DOUBLE:
```

### RuleId[ruleID=AssignmentToForLoopParameter]
Assignment to for-loop parameter `bufferIndex`
in `server/src/main/java/org/apache/iotdb/db/query/dataset/RawQueryDataSetWithoutValueFilter.java`
#### Snippet
```java
                }
                ReadWriteIOUtils.write(doubleValue, valueBAOSList[bufferIndex]);
                bufferIndex++;
                break;
              case BOOLEAN:
```

### RuleId[ruleID=AssignmentToForLoopParameter]
Assignment to for-loop parameter `bufferIndex`
in `server/src/main/java/org/apache/iotdb/db/query/dataset/RawQueryDataSetWithoutValueFilter.java`
#### Snippet
```java
                ReadWriteIOUtils.write(
                    cachedBatchDataArray[seriesIndex].getBoolean(), valueBAOSList[bufferIndex]);
                bufferIndex++;
                break;
              case TEXT:
```

### RuleId[ruleID=AssignmentToForLoopParameter]
Assignment to for-loop parameter `bufferIndex`
in `server/src/main/java/org/apache/iotdb/db/query/dataset/RawQueryDataSetWithoutValueFilter.java`
#### Snippet
```java
                ReadWriteIOUtils.write(
                    cachedBatchDataArray[seriesIndex].getBinary(), valueBAOSList[bufferIndex]);
                bufferIndex++;
                break;
              case VECTOR:
```

### RuleId[ruleID=AssignmentToForLoopParameter]
Assignment to for-loop parameter `bufferIndex`
in `server/src/main/java/org/apache/iotdb/db/query/dataset/RawQueryDataSetWithoutValueFilter.java`
#### Snippet
```java
                  if (primitiveVal == null) {
                    currentBitmapList[bufferIndex] = (currentBitmapList[bufferIndex] << 1);
                    bufferIndex++;
                    continue;
                  }
```

### RuleId[ruleID=AssignmentToForLoopParameter]
Assignment to for-loop parameter `bufferIndex`
in `server/src/main/java/org/apache/iotdb/db/query/dataset/RawQueryDataSetWithoutValueFilter.java`
#### Snippet
```java
                          String.format("Data type %s is not supported.", type));
                  }
                  bufferIndex++;
                }
                break;
```

### RuleId[ruleID=AssignmentToForLoopParameter]
Assignment to for-loop parameter `i`
in `server/src/main/java/org/apache/iotdb/db/utils/QueryDataSetUtils.java`
#### Snippet
```java
          // AlreadyReturnedRowNum
          queryDataSet.decreaseAlreadyReturnedRowNum();
          i--;
          continue;
        }
```

### RuleId[ruleID=AssignmentToForLoopParameter]
Assignment to for-loop parameter `i`
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/performer/impl/ReadPointCompactionPerformer.java`
#### Snippet
```java
      // remove the target file been deleted from list
      if (!targetResource.getTsFile().exists()) {
        targetResources.remove(i--);
        continue;
      }
```

### RuleId[ruleID=AssignmentToForLoopParameter]
Assignment to for-loop parameter `j`
in `library-udf/src/main/java/org/apache/iotdb/library/dquality/util/TimeSeriesQuality.java`
#### Snippet
```java
            temp++;
            window.remove(j); // move delayed points
            j--;
            if (temp == (int) Math.round(times - 1)) {
              break; // enough points to fill have been found
```

### RuleId[ruleID=AssignmentToForLoopParameter]
Assignment to for-loop parameter `i`
in `tsfile/src/main/java/org/apache/iotdb/tsfile/read/filter/operator/Like.java`
#### Snippet
```java
          }
          if ("\\".equals(nextChar)) {
            i++;
          }
        }
```

## RuleId[ruleID=SuspiciousMethodCalls]
### RuleId[ruleID=SuspiciousMethodCalls]
'Map' may not contain keys of type 'Object'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/scheduler/load/LoadTsFileScheduler.java`
#### Snippet
```java
            replicaSet2Piece.keySet().stream()
                .sorted(
                    Comparator.comparingLong(o -> replicaSet2Piece.get(o).getDataSize()).reversed())
                .collect(Collectors.toList());

```

### RuleId[ruleID=SuspiciousMethodCalls]
'Map\>' may not contain keys of type ''
in `server/src/main/java/org/apache/iotdb/db/engine/StorageEngineV2.java`
#### Snippet
```java
    List<DataRegionId> dataRegionIdList = new ArrayList<>();
    req.storageGroupPathPattern.forEach(
        storageGroup -> dataRegionIdList.addAll(localDataRegionInfo.get(storageGroup)));
    for (DataRegionId dataRegionId : dataRegionIdList) {
      DataRegion dataRegion = dataRegionMap.get(dataRegionId);
```

### RuleId[ruleID=SuspiciousMethodCalls]
'List' may not contain objects of type 'Object'
in `confignode/src/main/java/org/apache/iotdb/confignode/procedure/env/DataNodeRemoveHandler.java`
#### Snippet
```java
    return configManager.getNodeManager().filterDataNodeThroughStatus(NodeStatus.Running).stream()
        .map(TDataNodeConfiguration::getLocation)
        .filter(e -> !regionReplicaNodes.contains(e))
        .findAny();
  }
```

### RuleId[ruleID=SuspiciousMethodCalls]
'Set' may not contain objects of type 'String'
in `schema-engine-rocksdb/src/main/java/org/apache/iotdb/db/metadata/schemaregion/rocksdb/RSchemaRegion.java`
#### Snippet
```java
                logger.warn(e.getMessage());
              }
              if (!measurementPathsAndTags.keySet().contains(partialName)) {
                result.put(path, result.get(path));
              } else {
```

## RuleId[ruleID=InnerClassMayBeStatic]
### RuleId[ruleID=InnerClassMayBeStatic]
Inner class `TsFileDataManager` may be 'static'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/scheduler/load/LoadTsFileScheduler.java`
#### Snippet
```java
  }

  private class TsFileDataManager {
    private final LoadTsFileScheduler scheduler;
    private final LoadSingleTsFileNode singleTsFileNode;
```

### RuleId[ruleID=InnerClassMayBeStatic]
Inner class `VersionPageReader` may be 'static'
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/operator/source/SeriesScanUtil.java`
#### Snippet
```java
  }

  protected class VersionPageReader {

    protected PriorityMergeReader.MergeReaderPriority version;
```

### RuleId[ruleID=InnerClassMayBeStatic]
Inner class `CheckResult` may be 'static'
in `server/src/main/java/org/apache/iotdb/db/sync/transport/server/ReceiverManager.java`
#### Snippet
```java
  // region Interfaces and Implementation of Index Checker

  private class CheckResult {
    boolean result;
    String index;
```

### RuleId[ruleID=InnerClassMayBeStatic]
Inner class `ChunkInfo` may be 'static'
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/TsFileOpBlock.java`
#### Snippet
```java
          TSDataType.INT64);

  private class ChunkInfo {
    public String measurementFullPath;
    public long chunkOffsetInFile = -1;
```

### RuleId[ruleID=InnerClassMayBeStatic]
Inner class `TsFileFullReader` may be 'static'
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/TsFileOpBlock.java`
#### Snippet
```java

  /** This class is used to read & parse Tsfile */
  private class TsFileFullReader extends TsFileSequenceReader {

    /**
```

### RuleId[ruleID=InnerClassMayBeStatic]
Inner class `VersionPageReader` may be 'static'
in `server/src/main/java/org/apache/iotdb/db/query/reader/series/SeriesReader.java`
#### Snippet
```java
  }

  private class VersionPageReader {

    protected PriorityMergeReader.MergeReaderPriority version;
```

### RuleId[ruleID=InnerClassMayBeStatic]
Inner class `TsFileSelfCheckToolReader` may be 'static'
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileSelfCheckTool.java`
#### Snippet
```java
  }

  private class TsFileSelfCheckToolReader extends TsFileSequenceReader {
    public TsFileSelfCheckToolReader(String file) throws IOException {
      super(file);
```

### RuleId[ruleID=InnerClassMayBeStatic]
Inner class `TsFileSketchToolReader` may be 'static'
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileSketchTool.java`
#### Snippet
```java
  }

  private class TsFileSketchToolReader extends TsFileSequenceReader {

    public TsFileSketchToolReader(String file) throws IOException {
```

### RuleId[ruleID=InnerClassMayBeStatic]
Inner class `DataPartitionInfo` may be 'static'
in `server/src/main/java/org/apache/iotdb/db/engine/load/LoadTsFileManager.java`
#### Snippet
```java
  }

  private class DataPartitionInfo {
    private final DataRegion dataRegion;
    private final TTimePartitionSlot timePartitionSlot;
```

### RuleId[ruleID=InnerClassMayBeStatic]
Inner class `SizeTieredCompactionTaskComparator` may be 'static'
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/inner/sizetiered/SizeTieredCompactionSelector.java`
#### Snippet
```java
  }

  private class SizeTieredCompactionTaskComparator
      implements Comparator<Pair<List<TsFileResource>, Long>> {

```

### RuleId[ruleID=InnerClassMayBeStatic]
Inner class `QueryTask` may be 'static'
in `server/src/main/java/org/apache/iotdb/db/service/thrift/impl/TSServiceImpl.java`
#### Snippet
```java
  private static final SessionManager SESSION_MANAGER = SessionManager.getInstance();

  private class QueryTask implements Callable<TSExecuteStatementResp> {

    private final PhysicalPlan plan;
```

### RuleId[ruleID=InnerClassMayBeStatic]
Inner class `FetchResultsTask` may be 'static'
in `server/src/main/java/org/apache/iotdb/db/service/thrift/impl/TSServiceImpl.java`
#### Snippet
```java
  }

  private class FetchResultsTask implements Callable<TSFetchResultsResp> {

    private final IClientSession session;
```

### RuleId[ruleID=InnerClassMayBeStatic]
Inner class `MockSchemaFileIterator` may be 'static'
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/MockSchemaFile.java`
#### Snippet
```java
  }

  private class MockSchemaFileIterator implements Iterator<IMNode> {

    Iterator<IMNode> iterator;
```

### RuleId[ruleID=InnerClassMayBeStatic]
Inner class `SourceDataPoint` may be 'static'
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/Resampler.java`
#### Snippet
```java
  }

  private class SourceDataPoint {

    long time;
```

### RuleId[ruleID=InnerClassMayBeStatic]
Inner class `HashFunction` may be 'static'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/BloomFilter.java`
#### Snippet
```java
  }

  private class HashFunction {

    private int cap;
```

### RuleId[ruleID=InnerClassMayBeStatic]
Inner class `PropertiesOverWriter` may be 'static'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/common/conf/TSFileDescriptor.java`
#### Snippet
```java
  }

  private class PropertiesOverWriter {

    private final Properties properties;
```

### RuleId[ruleID=InnerClassMayBeStatic]
Inner class `Point` may be 'static'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/encoder/FreqEncoder.java`
#### Snippet
```java
  }

  protected class Point implements Comparable<Point> {

    private final int index;
```

## RuleId[ruleID=SetReplaceableByEnumSet]
### RuleId[ruleID=SetReplaceableByEnumSet]
`HashSet<>` can be replaced with 'EnumSet'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/parser/ASTVisitor.java`
#### Snippet
```java
  private OrderByComponent parseOrderByClause(IoTDBSqlParser.OrderByClauseContext ctx) {
    OrderByComponent orderByComponent = new OrderByComponent();
    Set<SortKey> sortKeySet = new HashSet<>();
    for (IoTDBSqlParser.OrderByAttributeClauseContext orderByAttributeClauseContext :
        ctx.orderByAttributeClause()) {
```

### RuleId[ruleID=SetReplaceableByEnumSet]
`HashSet<>` can be replaced with 'EnumSet'
in `server/src/main/java/org/apache/iotdb/db/utils/SchemaUtils.java`
#### Snippet
```java

  static {
    Set<TSEncoding> booleanSet = new HashSet<>();
    booleanSet.add(TSEncoding.PLAIN);
    booleanSet.add(TSEncoding.RLE);
```

### RuleId[ruleID=SetReplaceableByEnumSet]
`HashSet<>` can be replaced with 'EnumSet'
in `server/src/main/java/org/apache/iotdb/db/utils/SchemaUtils.java`
#### Snippet
```java
    schemaChecker.put(TSDataType.BOOLEAN, booleanSet);

    Set<TSEncoding> intSet = new HashSet<>();
    intSet.add(TSEncoding.PLAIN);
    intSet.add(TSEncoding.RLE);
```

### RuleId[ruleID=SetReplaceableByEnumSet]
`HashSet<>` can be replaced with 'EnumSet'
in `server/src/main/java/org/apache/iotdb/db/utils/SchemaUtils.java`
#### Snippet
```java
    schemaChecker.put(TSDataType.INT64, intSet);

    Set<TSEncoding> floatSet = new HashSet<>();
    floatSet.add(TSEncoding.PLAIN);
    floatSet.add(TSEncoding.RLE);
```

### RuleId[ruleID=SetReplaceableByEnumSet]
`HashSet<>` can be replaced with 'EnumSet'
in `server/src/main/java/org/apache/iotdb/db/utils/SchemaUtils.java`
#### Snippet
```java
    schemaChecker.put(TSDataType.DOUBLE, floatSet);

    Set<TSEncoding> textSet = new HashSet<>();
    textSet.add(TSEncoding.PLAIN);
    textSet.add(TSEncoding.DICTIONARY);
```

## RuleId[ruleID=NestedSynchronizedStatement]
### RuleId[ruleID=NestedSynchronizedStatement]
Nested `synchronized` statement
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/LocalSourceHandle.java`
#### Snippet
```java
      logger.debug("[StartAbortLocalSourceHandle]");
      synchronized (queue) {
        synchronized (this) {
          if (aborted || closed) {
            return;
```

### RuleId[ruleID=NestedSynchronizedStatement]
Nested `synchronized` statement
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/LocalSourceHandle.java`
#### Snippet
```java
      logger.debug("[StartCloseLocalSourceHandle]");
      synchronized (queue) {
        synchronized (this) {
          if (aborted || closed) {
            return;
```

### RuleId[ruleID=NestedSynchronizedStatement]
Nested `synchronized` statement
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/LocalSourceHandle.java`
#### Snippet
```java
      logger.debug("[StartAbortLocalSourceHandle]");
      synchronized (queue) {
        synchronized (this) {
          if (aborted || closed) {
            return;
```

### RuleId[ruleID=NestedSynchronizedStatement]
Nested `synchronized` statement
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/LocalSourceHandle.java`
#### Snippet
```java
        // There are two locks need to invoke this method. One is lock of SharedTsBlockQueue,
        // the other is lock of LocalSourceHandle.
        synchronized (this) {
          sourceHandleListener.onFinished(this);
        }
```

### RuleId[ruleID=NestedSynchronizedStatement]
Nested `synchronized` statement
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/LocalSinkHandle.java`
#### Snippet
```java
  public void setNoMoreTsBlocks() {
    synchronized (queue) {
      synchronized (this) {
        logger.debug("[StartSetNoMoreTsBlocksOnLocal]");
        if (aborted || closed) {
```

### RuleId[ruleID=NestedSynchronizedStatement]
Nested `synchronized` statement
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/LocalSinkHandle.java`
#### Snippet
```java
    logger.debug("[StartCloseLocalSinkHandle]");
    synchronized (queue) {
      synchronized (this) {
        if (aborted || closed) {
          return;
```

### RuleId[ruleID=NestedSynchronizedStatement]
Nested `synchronized` statement
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/LocalSinkHandle.java`
#### Snippet
```java
    synchronized (queue) {
      if (isFinished()) {
        synchronized (this) {
          sinkHandleListener.onFinish(this);
        }
```

### RuleId[ruleID=NestedSynchronizedStatement]
Nested `synchronized` statement
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/LocalSinkHandle.java`
#### Snippet
```java
    logger.debug("[StartAbortLocalSinkHandle]");
    synchronized (queue) {
      synchronized (this) {
        if (aborted || closed) {
          return;
```

### RuleId[ruleID=NestedSynchronizedStatement]
Nested `synchronized` statement
in `server/src/main/java/org/apache/iotdb/db/mpp/execution/exchange/LocalSinkHandle.java`
#### Snippet
```java
      }
      logger.debug("[StartSendTsBlockOnLocal]");
      synchronized (this) {
        blocked = queue.add(tsBlock);
      }
```

## RuleId[ruleID=StringEqualsEmptyString]
### RuleId[ruleID=StringEqualsEmptyString]
`equals("")` can be replaced with 'isEmpty()'
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/builtin/UDTFCast.java`
#### Snippet
```java
        return;
      case BOOLEAN:
        collector.putBoolean(time, !("false".equals(stringValue) || "".equals(stringValue)));
        return;
      case TEXT:
```

### RuleId[ruleID=StringEqualsEmptyString]
`equals("")` can be replaced with 'isEmpty()'
in `node-commons/src/main/java/org/apache/iotdb/commons/utils/PathUtils.java`
#### Snippet
```java
   */
  public static String[] splitPathToDetachedNodes(String path) throws IllegalPathException {
    if ("".equals(path)) {
      return new String[] {};
    }
```

### RuleId[ruleID=StringEqualsEmptyString]
`equals("")` can be replaced with 'isEmpty()'
in `node-commons/src/main/java/org/apache/iotdb/commons/path/PartialPath.java`
#### Snippet
```java
    Validate.isTrue(!needSplit);
    fullPath = path;
    if ("".equals(path)) {
      this.nodes = new String[] {};
    } else {
```

### RuleId[ruleID=StringEqualsEmptyString]
`equals("")` can be replaced with 'isEmpty()'
in `session/src/main/java/org/apache/iotdb/session/template/Template.java`
#### Snippet
```java

      if (!curNode.isMeasurement()) {
        if (!"".equals(prefix)) {
          fullPath.append(TsFileConstant.PATH_SEPARATOR);
        }
```

### RuleId[ruleID=StringEqualsEmptyString]
`equals("")` can be replaced with 'isEmpty()'
in `cli/src/main/java/org/apache/iotdb/cli/AbstractCli.java`
#### Snippet
```java
    String[] cmds = s.trim().split(";");
    for (String cmd : cmds) {
      if (cmd != null && !"".equals(cmd.trim())) {
        OperationResult result = handleInputCmd(cmd, connection);
        switch (result) {
```

### RuleId[ruleID=StringEqualsEmptyString]
`equals("")` can be replaced with 'isEmpty()'
in `cli/src/main/java/org/apache/iotdb/tool/ImportCsv.java`
#### Snippet
```java
              String header = deviceId + "." + measurement;
              String value = record.get(headerNameMap.get(header));
              if (!"".equals(value)) {
                TSDataType type;
                if (!headerTypeMap.containsKey(header)) {
```

### RuleId[ruleID=StringEqualsEmptyString]
`equals("")` can be replaced with 'isEmpty()'
in `cli/src/main/java/org/apache/iotdb/tool/ImportCsv.java`
#### Snippet
```java
            String headerName = headerNameEntry.getValue();
            String value = record.get(headerName);
            if (!"".equals(value)) {
              TSDataType type;
              // Get the data type directly if the CSV column have data type.
```

### RuleId[ruleID=StringEqualsEmptyString]
`equals("")` can be replaced with 'isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/qp/physical/crud/AlignByDevicePlan.java`
#### Snippet
```java
      if (measurementInfoMap.containsKey(measurement)) {
        String alias = measurementInfoMap.get(measurement).getMeasurementAlias();
        if (alias != null && !alias.equals("")) {
          actualColumn = alias;
        }
```

### RuleId[ruleID=StringEqualsEmptyString]
`equals("")` can be replaced with 'isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/parser/StatementGenerator.java`
#### Snippet
```java
        alignedCompressions.get(prefix).add(compressionType);
      } else {
        if ("".equals(prefix)) {
          measurements.add(Collections.singletonList(measurementName));
        } else {
```

### RuleId[ruleID=StringEqualsEmptyString]
`equals("")` can be replaced with 'isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/parser/StatementGenerator.java`
#### Snippet
```java

      for (int i = 0; i < alignedPrefix.get(prefix).size(); i++) {
        if ("".equals(prefix)) {
          thisMeasurements.add(alignedPrefix.get(prefix).get(i));
        } else {
```

### RuleId[ruleID=StringEqualsEmptyString]
`equals("")` can be replaced with 'isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/qp/physical/sys/CreateTemplatePlan.java`
#### Snippet
```java
        alignedCompressions.get(prefix).add(compressionType);
      } else {
        if ("".equals(prefix)) {
          measurements.add(Collections.singletonList(measurementName));
        } else {
```

### RuleId[ruleID=StringEqualsEmptyString]
`equals("")` can be replaced with 'isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/qp/physical/sys/CreateTemplatePlan.java`
#### Snippet
```java

      for (int i = 0; i < alignedPrefix.get(prefix).size(); i++) {
        if ("".equals(prefix)) {
          thisMeasurements.add(alignedPrefix.get(prefix).get(i));
        } else {
```

### RuleId[ruleID=StringEqualsEmptyString]
`equals("")` can be replaced with 'isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/parser/ASTVisitor.java`
#### Snippet
```java

  public long parseDateFormat(String timestampStr) throws SQLParserException {
    if (timestampStr == null || "".equals(timestampStr.trim())) {
      throw new SemanticException("input timestamp cannot be empty");
    }
```

### RuleId[ruleID=StringEqualsEmptyString]
`equals("")` can be replaced with 'isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/parser/ASTVisitor.java`
#### Snippet
```java

  public long parseDateFormat(String timestampStr, long currentTime) throws SQLParserException {
    if (timestampStr == null || "".equals(timestampStr.trim())) {
      throw new SemanticException("input timestamp cannot be empty");
    }
```

### RuleId[ruleID=StringEqualsEmptyString]
`equals("")` can be replaced with 'isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/tools/settle/TsFileAndModSettleTool.java`
#### Snippet
```java
                  FSFactoryProducer.getFSFactory().getFile(SettleLog.getSettleLogPath())))) {
        String line = null;
        while ((line = settleLogReader.readLine()) != null && !"".equals(line)) {
          String oldFilePath = line.split(SettleLog.COMMA_SEPERATOR)[0];
          int settleCheckStatus = Integer.parseInt(line.split(SettleLog.COMMA_SEPERATOR)[1]);
```

### RuleId[ruleID=StringEqualsEmptyString]
`equals("")` can be replaced with 'isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/utils/OpenFileNumUtil.java`
#### Snippet
```java
   */
  private static boolean isNumeric(String str) {
    if (str == null || "".equals(str)) {
      return false;
    } else {
```

### RuleId[ruleID=StringEqualsEmptyString]
`equals("")` can be replaced with 'isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/tools/validate/TsFileValidationTool.java`
#### Snippet
```java
                break;
              case MetaMarker.CHUNK_GROUP_HEADER:
                if (!deviceID.equals("")) {
                  // record the end time of last device in current file
                  if (resource.getEndTime(deviceID)
```

### RuleId[ruleID=StringEqualsEmptyString]
`equals("")` can be replaced with 'isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/engine/compaction/cross/utils/ReadPointCrossCompactionEstimator.java`
#### Snippet
```java
      int deviceChunkNum = 0;
      List<TimeseriesMetadata> deviceTimeseriesMetadata = entry.getValue();
      if (deviceTimeseriesMetadata.get(0).getMeasurementId().equals("")) {
        // aligned device
        maxAlignedSeriesNumInDevice =
```

### RuleId[ruleID=StringEqualsEmptyString]
`equals("")` can be replaced with 'isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/WrappedSegment.java`
#### Snippet
```java
    // update aliasKeyList
    String alias = RecordUtils.getRecordAlias(buf);
    if (alias != null && !alias.equals("")) {
      aliasKeyList.add(binaryInsertPairList(aliasKeyList, alias), new Pair<>(alias, key));
    }
```

### RuleId[ruleID=StringEqualsEmptyString]
`equals("")` can be replaced with 'isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/schemafile/WrappedSegment.java`
#### Snippet
```java
    // update alias-key list accordingly
    String alias = RecordUtils.getRecordAlias(this.buffer);
    if (alias != null && !alias.equals("")) {
      aliasKeyList.remove(binarySearchPairList(aliasKeyList, alias));
    }
```

### RuleId[ruleID=StringEqualsEmptyString]
`equals("")` can be replaced with 'isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/metadata/template/Template.java`
#### Snippet
```java
        String thisPrefix = getFullPathWithoutTemplateName(top);

        if (thisPrefix.equals("")) {
          isDirectAligned = false;
        }
```

### RuleId[ruleID=StringEqualsEmptyString]
`equals("")` can be replaced with 'isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/metadata/template/Template.java`
#### Snippet
```java

    synchronized (this) {
      if (prefix.equals("")) {
        isDirectAligned = true;
      }
```

### RuleId[ruleID=StringEqualsEmptyString]
`equals("")` can be replaced with 'isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/metadata/template/Template.java`
#### Snippet
```java
      for (int i = 0; i <= measurementNames.size() - 1; i++) {
        // find the parent and add nodes to template
        if ("".equals(prefix)) {
          leafNode =
              MeasurementMNode.getMeasurementMNode(null, measurementNames.get(i), schemas[i], null);
```

### RuleId[ruleID=StringEqualsEmptyString]
`equals("")` can be replaced with 'isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/metadata/template/Template.java`
#### Snippet
```java

  public List<String> getMeasurementsUnderPath(String path) throws MetadataException {
    if ("".equals(path)) {
      return getAllMeasurementsPaths();
    }
```

### RuleId[ruleID=StringEqualsEmptyString]
`equals("")` can be replaced with 'isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/metadata/template/Template.java`
#### Snippet
```java
    IMNode targetNode = getPathNodeInTemplate(prefix);
    if ((targetNode != null && !targetNode.getAsEntityMNode().isAligned())
        || (prefix.equals("") && !this.isDirectAligned())) {
      throw new IllegalPathException(prefix, "path already exists but not aligned");
    }
```

### RuleId[ruleID=StringEqualsEmptyString]
`equals("")` can be replaced with 'isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/metadata/template/Template.java`
#### Snippet
```java

  public void deleteAlignedPrefix(String path) throws IllegalPathException {
    if (path.equals("")) {
      isDirectAligned = false;
    }
```

### RuleId[ruleID=StringEqualsEmptyString]
`equals("")` can be replaced with 'isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/metadata/template/Template.java`
#### Snippet
```java
      IMNode parNode = getPathNodeInTemplate(prefix);
      if ((parNode != null && parNode.getAsEntityMNode().isAligned())
          || (prefix.equals("") && this.isDirectAligned())) {
        throw new IllegalPathException(measurements[i], "path already exists and aligned");
      }
```

### RuleId[ruleID=StringEqualsEmptyString]
`equals("")` can be replaced with 'isEmpty()'
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/sql/InfluxDBSqlVisitor.java`
#### Snippet
```java
  /** function for parsing time format. */
  public static long parseTimeFormat(String timestampStr) {
    if (timestampStr == null || timestampStr.trim().equals("")) {
      throw new IllegalArgumentException("input timestamp cannot be empty");
    }
```

### RuleId[ruleID=StringEqualsEmptyString]
`equals("")` can be replaced with 'isEmpty()'
in `library-udf/src/main/java/org/apache/iotdb/library/frequency/UDTFIDWT.java`
#### Snippet
```java
  @Override
  public void terminate(PointCollector pointCollector) throws Exception {
    if (!s.equals("") || !method.equals("")) { // When user offers at least one parameter
      DWTUtil transformer = new DWTUtil(method, s, layer, value);
      transformer.inverse();
```

### RuleId[ruleID=StringEqualsEmptyString]
`equals("")` can be replaced with 'isEmpty()'
in `library-udf/src/main/java/org/apache/iotdb/library/frequency/UDTFIDWT.java`
#### Snippet
```java
  @Override
  public void terminate(PointCollector pointCollector) throws Exception {
    if (!s.equals("") || !method.equals("")) { // When user offers at least one parameter
      DWTUtil transformer = new DWTUtil(method, s, layer, value);
      transformer.inverse();
```

### RuleId[ruleID=StringEqualsEmptyString]
`equals("")` can be replaced with 'isEmpty()'
in `library-udf/src/main/java/org/apache/iotdb/library/frequency/UDTFDWT.java`
#### Snippet
```java
  @Override
  public void terminate(PointCollector pointCollector) throws Exception {
    if (!s.equals("") || !method.equals("")) { // When user offers at least one parameter
      DWTUtil transformer = new DWTUtil(method, s, layer, value);
      transformer.waveletTransform();
```

### RuleId[ruleID=StringEqualsEmptyString]
`equals("")` can be replaced with 'isEmpty()'
in `library-udf/src/main/java/org/apache/iotdb/library/frequency/UDTFDWT.java`
#### Snippet
```java
  @Override
  public void terminate(PointCollector pointCollector) throws Exception {
    if (!s.equals("") || !method.equals("")) { // When user offers at least one parameter
      DWTUtil transformer = new DWTUtil(method, s, layer, value);
      transformer.waveletTransform();
```

### RuleId[ruleID=StringEqualsEmptyString]
`equals("")` can be replaced with 'isEmpty()'
in `grafana-connector/src/main/java/org/apache/iotdb/web/grafana/dao/impl/BasicDaoImpl.java`
#### Snippet
```java

    String intervalLocal = getInterval(hours);
    if (!"".equals(intervalLocal)) {
      sql = "SELECT ?(?) FROM root.? WHERE time > ? and time < ? group by ([?, ?),?)";
      params =
```

## RuleId[ruleID=UnnecessaryBoxing]
### RuleId[ruleID=UnnecessaryBoxing]
Unnecessary boxing
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
    Object[] nv = new Object[3];
    Object[] v;
    nv[0] = Character.valueOf('y');
    nv[1] = new StringBuilder();
    nv[2] = Integer.valueOf(0);
```

### RuleId[ruleID=UnnecessaryBoxing]
Unnecessary boxing
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
    nv[0] = Character.valueOf('y');
    nv[1] = new StringBuilder();
    nv[2] = Integer.valueOf(0);
    vec.add(nv);

```

### RuleId[ruleID=UnnecessaryBoxing]
Unnecessary boxing
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
    if (toTime) {
      nv = new Object[3];
      nv[0] = Character.valueOf('h');
      nv[1] = new StringBuilder();
      nv[2] = Integer.valueOf(0);
```

### RuleId[ruleID=UnnecessaryBoxing]
Unnecessary boxing
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
      nv[0] = Character.valueOf('h');
      nv[1] = new StringBuilder();
      nv[2] = Integer.valueOf(0);
      vec.add(nv);
    }
```

### RuleId[ruleID=UnnecessaryBoxing]
Unnecessary boxing
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java

            if ((c == 'X') || (c == 'Y')) {
              v[2] = Integer.valueOf(4);
            }
          }
```

### RuleId[ruleID=UnnecessaryBoxing]
Unnecessary boxing
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
            nv = new Object[3];
            nv[1] = (new StringBuilder(((StringBuilder) v[1]).toString())).append('M');
            nv[0] = Character.valueOf('M');
            nv[2] = Integer.valueOf(1);
            vec.add(nv);
```

### RuleId[ruleID=UnnecessaryBoxing]
Unnecessary boxing
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
            nv[1] = (new StringBuilder(((StringBuilder) v[1]).toString())).append('M');
            nv[0] = Character.valueOf('M');
            nv[2] = Integer.valueOf(1);
            vec.add(nv);
          } else if (c == 'Y') {
```

### RuleId[ruleID=UnnecessaryBoxing]
Unnecessary boxing
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
            nv = new Object[3];
            nv[1] = (new StringBuilder(((StringBuilder) v[1]).toString())).append('d');
            nv[0] = Character.valueOf('d');
            nv[2] = Integer.valueOf(1);
            vec.add(nv);
```

### RuleId[ruleID=UnnecessaryBoxing]
Unnecessary boxing
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
            nv[1] = (new StringBuilder(((StringBuilder) v[1]).toString())).append('d');
            nv[0] = Character.valueOf('d');
            nv[2] = Integer.valueOf(1);
            vec.add(nv);
          }
```

### RuleId[ruleID=UnnecessaryBoxing]
Unnecessary boxing
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java

          if (c == ((Character) v[0]).charValue()) {
            v[2] = Integer.valueOf(n + 1);
          } else {
            v[0] = Character.valueOf(c);
```

### RuleId[ruleID=UnnecessaryBoxing]
Unnecessary boxing
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
            v[2] = Integer.valueOf(n + 1);
          } else {
            v[0] = Character.valueOf(c);
            v[2] = Integer.valueOf(1);
          }
```

### RuleId[ruleID=UnnecessaryBoxing]
Unnecessary boxing
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
          } else {
            v[0] = Character.valueOf(c);
            v[2] = Integer.valueOf(1);
          }
        }
```

### RuleId[ruleID=UnnecessaryBoxing]
Redundant boxing, `Integer.parseInt()` call can be used instead
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      String[] versions = version.split(".");
      if (versions.length >= 2) {
        major_version = Integer.valueOf(versions[0]);
      }
    } catch (TException e) {
```

### RuleId[ruleID=UnnecessaryBoxing]
Redundant boxing, `Integer.parseInt()` call can be used instead
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      String[] versions = version.split(".");
      if (versions.length >= 2) {
        minor_version = Integer.valueOf(versions[1]);
      }
    } catch (TException e) {
```

### RuleId[ruleID=UnnecessaryBoxing]
Redundant boxing, `Long.parseLong()` call can be used instead
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileResourcePrinter.java`
#### Snippet
```java
          FSFactoryProducer.getFSFactory()
              .listFilesBySuffix(folderFile.getAbsolutePath(), ".tsfile.resource");
      Arrays.sort(files, Comparator.comparingLong(x -> Long.valueOf(x.getName().split("-")[0])));

      for (File file : files) {
```

### RuleId[ruleID=UnnecessaryBoxing]
Redundant boxing, `Integer.parseInt()` call can be used instead
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/DataRegion.java`
#### Snippet
```java
      lastFlushTimeMap.removePartition(partitionId);
      TimePartitionManager.getInstance()
          .removePartition(new DataRegionId(Integer.valueOf(dataRegionId)), partitionId);
      TsFileProcessor processor = longTsFileProcessorEntry.getValue();
      if (filter.satisfy(storageGroupName, partitionId)) {
```

### RuleId[ruleID=UnnecessaryBoxing]
Redundant boxing, `Integer.parseInt()` call can be used instead
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/DataRegion.java`
#### Snippet
```java
        TimePartitionManager.getInstance()
            .removePartition(
                new DataRegionId(Integer.valueOf(dataRegionId)), tsFileResource.getTimePartition());
        logger.debug("{} is removed during deleting partitions", tsFileResource.getTsFilePath());
      }
```

### RuleId[ruleID=UnnecessaryBoxing]
Redundant boxing, `Integer.parseInt()` call can be used instead
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/DataRegion.java`
#### Snippet
```java
      TimePartitionManager.getInstance()
          .updateAfterOpeningTsFileProcessor(
              new DataRegionId(Integer.valueOf(dataRegionId)), timeRangeId);
      res = newTsFileProcessor(sequence, timeRangeId);
      tsFileProcessorTreeMap.put(timeRangeId, res);
```

### RuleId[ruleID=UnnecessaryBoxing]
Redundant boxing, `Integer.parseInt()` call can be used instead
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/DataRegion.java`
#### Snippet
```java
    TimePartitionManager.getInstance()
        .updateAfterFlushing(
            new DataRegionId(Integer.valueOf(dataRegionId)),
            processor.getTimeRangeId(),
            systemFlushTime,
```

### RuleId[ruleID=UnnecessaryBoxing]
Redundant boxing, `Integer.parseInt()` call can be used instead
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/DataRegion.java`
#### Snippet
```java
            .registerTimePartitionInfo(
                new TimePartitionInfo(
                    new DataRegionId(Integer.valueOf(dataRegionId)),
                    timePartitionId,
                    true,
```

### RuleId[ruleID=UnnecessaryBoxing]
Redundant boxing, `Integer.parseInt()` call can be used instead
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/DataRegion.java`
#### Snippet
```java
              .registerTimePartitionInfo(
                  new TimePartitionInfo(
                      new DataRegionId(Integer.valueOf(dataRegionId)),
                      timePartitionId,
                      true,
```

### RuleId[ruleID=UnnecessaryBoxing]
Redundant boxing, `Integer.parseInt()` call can be used instead
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/DataRegion.java`
#### Snippet
```java
    TimePartitionManager.getInstance()
        .updateAfterFlushing(
            new DataRegionId(Integer.valueOf(dataRegionId)),
            processor.getTimeRangeId(),
            systemFlushTime,
```

### RuleId[ruleID=UnnecessaryBoxing]
Redundant boxing, `Integer.parseInt()` call can be used instead
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/DataRegion.java`
#### Snippet
```java
          .registerTimePartitionInfo(
              new TimePartitionInfo(
                  new DataRegionId(Integer.valueOf(dataRegionId)),
                  partitionId,
                  false,
```

### RuleId[ruleID=UnnecessaryBoxing]
Redundant boxing, `Integer.parseInt()` call can be used instead
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/DataRegion.java`
#### Snippet
```java
            .registerTimePartitionInfo(
                new TimePartitionInfo(
                    new DataRegionId(Integer.valueOf(dataRegionId)),
                    beforeTimePartition,
                    true,
```

### RuleId[ruleID=UnnecessaryBoxing]
Redundant boxing, `Float.parseFloat()` call can be used instead
in `server/src/main/java/org/apache/iotdb/db/protocol/rest/handler/StatementConstructionHandler.java`
#### Snippet
```java
              bitMaps[columnIndex].mark(rowIndex);
            } else {
              floatValues[rowIndex] = Float.valueOf(String.valueOf(data));
            }
          }
```

### RuleId[ruleID=UnnecessaryBoxing]
Redundant boxing, `Double.parseDouble()` call can be used instead
in `server/src/main/java/org/apache/iotdb/db/protocol/rest/handler/StatementConstructionHandler.java`
#### Snippet
```java
            } else {
              doubleValues[rowIndex] =
                  Double.valueOf(String.valueOf(rawData.get(columnIndex).get(rowIndex)));
            }
          }
```

### RuleId[ruleID=UnnecessaryBoxing]
Unnecessary boxing
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/RamUsageEstimator.java`
#### Snippet
```java
  static {
    primitiveSizes = new IdentityHashMap<Class<?>, Integer>();
    primitiveSizes.put(boolean.class, Integer.valueOf(NUM_BYTES_BOOLEAN));
    primitiveSizes.put(byte.class, Integer.valueOf(NUM_BYTES_BYTE));
    primitiveSizes.put(char.class, Integer.valueOf(NUM_BYTES_CHAR));
```

### RuleId[ruleID=UnnecessaryBoxing]
Unnecessary boxing
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/RamUsageEstimator.java`
#### Snippet
```java
    primitiveSizes = new IdentityHashMap<Class<?>, Integer>();
    primitiveSizes.put(boolean.class, Integer.valueOf(NUM_BYTES_BOOLEAN));
    primitiveSizes.put(byte.class, Integer.valueOf(NUM_BYTES_BYTE));
    primitiveSizes.put(char.class, Integer.valueOf(NUM_BYTES_CHAR));
    primitiveSizes.put(short.class, Integer.valueOf(NUM_BYTES_SHORT));
```

### RuleId[ruleID=UnnecessaryBoxing]
Unnecessary boxing
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/RamUsageEstimator.java`
#### Snippet
```java
    primitiveSizes.put(boolean.class, Integer.valueOf(NUM_BYTES_BOOLEAN));
    primitiveSizes.put(byte.class, Integer.valueOf(NUM_BYTES_BYTE));
    primitiveSizes.put(char.class, Integer.valueOf(NUM_BYTES_CHAR));
    primitiveSizes.put(short.class, Integer.valueOf(NUM_BYTES_SHORT));
    primitiveSizes.put(int.class, Integer.valueOf(NUM_BYTES_INT));
```

### RuleId[ruleID=UnnecessaryBoxing]
Unnecessary boxing
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/RamUsageEstimator.java`
#### Snippet
```java
    primitiveSizes.put(byte.class, Integer.valueOf(NUM_BYTES_BYTE));
    primitiveSizes.put(char.class, Integer.valueOf(NUM_BYTES_CHAR));
    primitiveSizes.put(short.class, Integer.valueOf(NUM_BYTES_SHORT));
    primitiveSizes.put(int.class, Integer.valueOf(NUM_BYTES_INT));
    primitiveSizes.put(float.class, Integer.valueOf(NUM_BYTES_FLOAT));
```

### RuleId[ruleID=UnnecessaryBoxing]
Unnecessary boxing
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/RamUsageEstimator.java`
#### Snippet
```java
    primitiveSizes.put(char.class, Integer.valueOf(NUM_BYTES_CHAR));
    primitiveSizes.put(short.class, Integer.valueOf(NUM_BYTES_SHORT));
    primitiveSizes.put(int.class, Integer.valueOf(NUM_BYTES_INT));
    primitiveSizes.put(float.class, Integer.valueOf(NUM_BYTES_FLOAT));
    primitiveSizes.put(double.class, Integer.valueOf(NUM_BYTES_DOUBLE));
```

### RuleId[ruleID=UnnecessaryBoxing]
Unnecessary boxing
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/RamUsageEstimator.java`
#### Snippet
```java
    primitiveSizes.put(short.class, Integer.valueOf(NUM_BYTES_SHORT));
    primitiveSizes.put(int.class, Integer.valueOf(NUM_BYTES_INT));
    primitiveSizes.put(float.class, Integer.valueOf(NUM_BYTES_FLOAT));
    primitiveSizes.put(double.class, Integer.valueOf(NUM_BYTES_DOUBLE));
    primitiveSizes.put(long.class, Integer.valueOf(NUM_BYTES_LONG));
```

### RuleId[ruleID=UnnecessaryBoxing]
Unnecessary boxing
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/RamUsageEstimator.java`
#### Snippet
```java
    primitiveSizes.put(int.class, Integer.valueOf(NUM_BYTES_INT));
    primitiveSizes.put(float.class, Integer.valueOf(NUM_BYTES_FLOAT));
    primitiveSizes.put(double.class, Integer.valueOf(NUM_BYTES_DOUBLE));
    primitiveSizes.put(long.class, Integer.valueOf(NUM_BYTES_LONG));
  }
```

### RuleId[ruleID=UnnecessaryBoxing]
Unnecessary boxing
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/RamUsageEstimator.java`
#### Snippet
```java
    primitiveSizes.put(float.class, Integer.valueOf(NUM_BYTES_FLOAT));
    primitiveSizes.put(double.class, Integer.valueOf(NUM_BYTES_DOUBLE));
    primitiveSizes.put(long.class, Integer.valueOf(NUM_BYTES_LONG));
  }

```

### RuleId[ruleID=UnnecessaryBoxing]
Redundant boxing, `Integer.parseInt()` call can be used instead
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/encoder/TSEncodingBuilder.java`
#### Snippet
```java
        maxStringLength = TSFileDescriptor.getInstance().getConfig().getMaxStringLength();
      } else {
        maxStringLength = Integer.valueOf(props.get(Encoder.MAX_STRING_LENGTH));
        if (maxStringLength < 0) {
          maxStringLength = TSFileDescriptor.getInstance().getConfig().getMaxStringLength();
```

## RuleId[ruleID=SynchronizeOnNonFinalField]
### RuleId[ruleID=SynchronizeOnNonFinalField]
Synchronization on a non-final field `commitTriggerLocker`
in `server/src/main/java/org/apache/iotdb/db/sync/externalpipe/ExtPipePluginManager.java`
#### Snippet
```java
   */
  public void triggerCommit(String sgName, long commitIndex) throws IOException {
    synchronized (commitTriggerLocker) {
      commitTriggerCounter++;
      commitTriggerLocker.notifyAll();
```

### RuleId[ruleID=SynchronizeOnNonFinalField]
Synchronization on a non-final field `commitTriggerLocker`
in `server/src/main/java/org/apache/iotdb/db/sync/externalpipe/ExtPipePluginManager.java`
#### Snippet
```java
        }

        synchronized (commitTriggerLocker) {
          if (commitTriggerCounter <= 0L) {
            try {
```

### RuleId[ruleID=SynchronizeOnNonFinalField]
Synchronization on a non-final field `newDataLocker`
in `server/src/main/java/org/apache/iotdb/db/sync/externalpipe/ExtPipePlugin.java`
#### Snippet
```java
        }

        synchronized (newDataLocker) {
          if (newDataCounter <= 0L) {
            try {
```

### RuleId[ruleID=SynchronizeOnNonFinalField]
Synchronization on a non-final field `newDataLocker`
in `server/src/main/java/org/apache/iotdb/db/sync/externalpipe/ExtPipePlugin.java`
#### Snippet
```java
     */
    public void notifyNewDataArrive(String sgName, long newDataBeginIndex, long newDataCount) {
      synchronized (newDataLocker) {
        newDataCounter++;
        newDataLocker.notifyAll();
```

### RuleId[ruleID=SynchronizeOnNonFinalField]
Synchronization on a non-final field `lock`
in `server/src/main/java/org/apache/iotdb/db/engine/modification/utils/TracedBufferedReader.java`
#### Snippet
```java
      throw new IllegalArgumentException("skip value is negative");
    }
    synchronized (lock) {
      ensureOpen();
      long r = n;
```

### RuleId[ruleID=SynchronizeOnNonFinalField]
Synchronization on a non-final field `lock`
in `server/src/main/java/org/apache/iotdb/db/engine/modification/utils/TracedBufferedReader.java`
#### Snippet
```java
    int startChar;

    synchronized (lock) {
      ensureOpen();
      boolean omitLF = ignoreLF || skipLF;
```

### RuleId[ruleID=SynchronizeOnNonFinalField]
Synchronization on a non-final field `lock`
in `server/src/main/java/org/apache/iotdb/db/engine/modification/utils/TracedBufferedReader.java`
#### Snippet
```java
  @Override
  public int read(char cbuf[], int off, int len) throws IOException {
    synchronized (lock) {
      ensureOpen();
      if ((off < 0)
```

### RuleId[ruleID=SynchronizeOnNonFinalField]
Synchronization on a non-final field `lock`
in `server/src/main/java/org/apache/iotdb/db/engine/modification/utils/TracedBufferedReader.java`
#### Snippet
```java
      throw new IllegalArgumentException("Read-ahead limit < 0");
    }
    synchronized (lock) {
      ensureOpen();
      this.readAheadLimit = readAheadLimit;
```

### RuleId[ruleID=SynchronizeOnNonFinalField]
Synchronization on a non-final field `lock`
in `server/src/main/java/org/apache/iotdb/db/engine/modification/utils/TracedBufferedReader.java`
#### Snippet
```java
  @Override
  public void close() throws IOException {
    synchronized (lock) {
      if (in == null) {
        return;
```

### RuleId[ruleID=SynchronizeOnNonFinalField]
Synchronization on a non-final field `lock`
in `server/src/main/java/org/apache/iotdb/db/engine/modification/utils/TracedBufferedReader.java`
#### Snippet
```java
  @Override
  public void reset() throws IOException {
    synchronized (lock) {
      ensureOpen();
      if (markedChar < 0) {
```

### RuleId[ruleID=SynchronizeOnNonFinalField]
Synchronization on a non-final field `lock`
in `server/src/main/java/org/apache/iotdb/db/engine/modification/utils/TracedBufferedReader.java`
#### Snippet
```java
  @Override
  public int read() throws IOException {
    synchronized (lock) {
      ensureOpen();
      for (; ; ) {
```

### RuleId[ruleID=SynchronizeOnNonFinalField]
Synchronization on a non-final field `lock`
in `server/src/main/java/org/apache/iotdb/db/engine/modification/utils/TracedBufferedReader.java`
#### Snippet
```java
  @Override
  public boolean ready() throws IOException {
    synchronized (lock) {
      ensureOpen();

```

### RuleId[ruleID=SynchronizeOnNonFinalField]
Synchronization on a non-final field `batchList`
in `flink-iotdb-connector/src/main/java/org/apache/iotdb/flink/IoTDBSink.java`
#### Snippet
```java
  private void flush() throws Exception {
    if (batchSize > 0) {
      synchronized (batchList) {
        if (batchList.size() > 0) {
          List<String> deviceIds = new ArrayList<>();
```

### RuleId[ruleID=SynchronizeOnNonFinalField]
Synchronization on a non-final field `batchList`
in `flink-iotdb-connector/src/main/java/org/apache/iotdb/flink/IoTDBSink.java`
#### Snippet
```java

    if (batchSize > 0) {
      synchronized (batchList) {
        batchList.add(event);
        if (batchList.size() >= batchSize) {
```

## RuleId[ruleID=UnnecessaryFullyQualifiedName]
### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `org.apache.iotdb.udf.api.type` is unnecessary, and can be replaced with an import
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/utils/UDFBinaryTransformer.java`
#### Snippet
```java
  }

  public static org.apache.iotdb.udf.api.type.Binary transformToUDFBinary(
      org.apache.iotdb.tsfile.utils.Binary binary) {
    return binary == null ? null : new org.apache.iotdb.udf.api.type.Binary(binary.getValues());
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `org.apache.iotdb.tsfile.utils` is unnecessary, and can be replaced with an import
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/utils/UDFBinaryTransformer.java`
#### Snippet
```java

  public static org.apache.iotdb.udf.api.type.Binary transformToUDFBinary(
      org.apache.iotdb.tsfile.utils.Binary binary) {
    return binary == null ? null : new org.apache.iotdb.udf.api.type.Binary(binary.getValues());
  }
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `org.apache.iotdb.udf.api.type` is unnecessary, and can be replaced with an import
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/utils/UDFBinaryTransformer.java`
#### Snippet
```java
  public static org.apache.iotdb.udf.api.type.Binary transformToUDFBinary(
      org.apache.iotdb.tsfile.utils.Binary binary) {
    return binary == null ? null : new org.apache.iotdb.udf.api.type.Binary(binary.getValues());
  }
}
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `org.apache.iotdb.tsfile.utils` is unnecessary, and can be replaced with an import
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/utils/UDFBinaryTransformer.java`
#### Snippet
```java
  private UDFBinaryTransformer() {}

  public static org.apache.iotdb.tsfile.utils.Binary transformToBinary(
      org.apache.iotdb.udf.api.type.Binary binary) {
    return binary == null ? null : new org.apache.iotdb.tsfile.utils.Binary(binary.getValues());
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `org.apache.iotdb.udf.api.type` is unnecessary, and can be replaced with an import
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/utils/UDFBinaryTransformer.java`
#### Snippet
```java

  public static org.apache.iotdb.tsfile.utils.Binary transformToBinary(
      org.apache.iotdb.udf.api.type.Binary binary) {
    return binary == null ? null : new org.apache.iotdb.tsfile.utils.Binary(binary.getValues());
  }
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `org.apache.iotdb.tsfile.utils` is unnecessary, and can be replaced with an import
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/utils/UDFBinaryTransformer.java`
#### Snippet
```java
  public static org.apache.iotdb.tsfile.utils.Binary transformToBinary(
      org.apache.iotdb.udf.api.type.Binary binary) {
    return binary == null ? null : new org.apache.iotdb.tsfile.utils.Binary(binary.getValues());
  }

```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `org.apache.iotdb.tsfile.file.metadata.enums` is unnecessary and can be removed
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/utils/UDFDataTypeTransformer.java`
#### Snippet
```java

/**
 * Transform between {@link org.apache.iotdb.tsfile.file.metadata.enums.TSDataType} and {@link
 * org.apache.iotdb.udf.api.type.Type}
 */
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `org.apache.iotdb.udf.api.type` is unnecessary and can be removed
in `node-commons/src/main/java/org/apache/iotdb/commons/udf/utils/UDFDataTypeTransformer.java`
#### Snippet
```java
/**
 * Transform between {@link org.apache.iotdb.tsfile.file.metadata.enums.TSDataType} and {@link
 * org.apache.iotdb.udf.api.type.Type}
 */
public class UDFDataTypeTransformer {
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `java.util` is unnecessary and can be removed
in `node-commons/src/main/java/org/apache/iotdb/commons/auth/authorizer/OpenIdAuthorizer.java`
#### Snippet
```java
    // Read all data from URL
    String providerInfo;
    try (java.util.Scanner s = new java.util.Scanner(stream)) {
      providerInfo = s.useDelimiter("\\A").hasNext() ? s.next() : "";
    }
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `java.util` is unnecessary and can be removed
in `node-commons/src/main/java/org/apache/iotdb/commons/auth/authorizer/OpenIdAuthorizer.java`
#### Snippet
```java
    // Read all data from URL
    String providerInfo;
    try (java.util.Scanner s = new java.util.Scanner(stream)) {
      providerInfo = s.useDelimiter("\\A").hasNext() ? s.next() : "";
    }
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `java.lang.reflect` is unnecessary, and can be replaced with an import
in `influxdb-protocol/src/main/java/org/apache/iotdb/influxdb/protocol/dto/SessionPoint.java`
#### Snippet
```java

    // Get the property of session by reflection
    for (java.lang.reflect.Field reflectField : session.getClass().getDeclaredFields()) {
      reflectField.setAccessible(true);
      try {
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `java.lang.reflect` is unnecessary, and can be replaced with an import
in `influxdb-protocol/src/main/java/org/apache/iotdb/influxdb/IoTDBInfluxDB.java`
#### Snippet
```java
    TimeUnit precision = TimeUnit.NANOSECONDS;
    // Get the precision of point in influxdb by reflection
    for (java.lang.reflect.Field reflectField : point.getClass().getDeclaredFields()) {
      reflectField.setAccessible(true);
      try {
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `java.util.logging` is unnecessary, and can be replaced with an import
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDataSource.java`
#### Snippet
```java

  @Override
  public java.util.logging.Logger getParentLogger() {
    return null;
  }
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `org.slf4j` is unnecessary, and can be replaced with an import
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDriver.java`
#### Snippet
```java
public class IoTDBDriver implements Driver {
  private static final org.slf4j.Logger logger =
      org.slf4j.LoggerFactory.getLogger(IoTDBDriver.class);
  /** Is this driver JDBC compliant. */
  private static final boolean TSFILE_JDBC_COMPLIANT = false;
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `java.sql` is unnecessary and can be removed
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDriver.java`
#### Snippet
```java
import java.util.regex.Pattern;

@Component(service = java.sql.Driver.class, immediate = true)
public class IoTDBDriver implements Driver {
  private static final org.slf4j.Logger logger =
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `org.jline.reader.impl` is unnecessary, and can be replaced with an import
in `cli/src/main/java/org/apache/iotdb/cli/utils/JlineUtils.java`
#### Snippet
```java
    builder.option(Option.DISABLE_EVENT_EXPANSION, true);

    org.jline.reader.impl.DefaultParser parser = new org.jline.reader.impl.DefaultParser();
    builder.parser(parser);
    LineReader lineReader = builder.build();
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `org.jline.reader.impl` is unnecessary, and can be replaced with an import
in `cli/src/main/java/org/apache/iotdb/cli/utils/JlineUtils.java`
#### Snippet
```java
    builder.option(Option.DISABLE_EVENT_EXPANSION, true);

    org.jline.reader.impl.DefaultParser parser = new org.jline.reader.impl.DefaultParser();
    builder.parser(parser);
    LineReader lineReader = builder.build();
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `java.math` is unnecessary and can be removed
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
        case Types.NUMERIC:
        default:
          parameterAsNum = new java.math.BigDecimal((String) parameterObj);
      }
    } else {
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `java.math` is unnecessary and can be removed
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
      case Types.DECIMAL:
      case Types.NUMERIC:
        if (parameterAsNum instanceof java.math.BigDecimal) {
          BigDecimal scaledBigDecimal = null;

```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `java.math` is unnecessary and can be removed
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java

          try {
            scaledBigDecimal = ((java.math.BigDecimal) parameterAsNum).setScale(scale);
          } catch (ArithmeticException ex) {
            try {
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `java.math` is unnecessary and can be removed
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
            try {
              scaledBigDecimal =
                  ((java.math.BigDecimal) parameterAsNum).setScale(scale, BigDecimal.ROUND_HALF_UP);
            } catch (ArithmeticException arEx) {
              throw new SQLException(
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `java.math` is unnecessary, and can be replaced with an import
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java

          setBigDecimal(parameterIndex, scaledBigDecimal);
        } else if (parameterAsNum instanceof java.math.BigInteger) {
          setBigDecimal(
              parameterIndex,
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `java.math` is unnecessary and can be removed
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
          setBigDecimal(
              parameterIndex,
              new java.math.BigDecimal((java.math.BigInteger) parameterAsNum, scale));
        } else {
          setBigDecimal(parameterIndex, BigDecimal.valueOf(parameterAsNum.doubleValue()));
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `java.math` is unnecessary, and can be replaced with an import
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
          setBigDecimal(
              parameterIndex,
              new java.math.BigDecimal((java.math.BigInteger) parameterAsNum, scale));
        } else {
          setBigDecimal(parameterIndex, BigDecimal.valueOf(parameterAsNum.doubleValue()));
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `java.sql` is unnecessary and can be removed
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
      throws SQLException {
    if (parameterObj == null) {
      setNull(parameterIndex, java.sql.Types.OTHER);
    } else {
      try {
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `java.sql` is unnecessary and can be removed
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java

          case Types.CLOB:
            if (parameterObj instanceof java.sql.Clob) {
              setClob(parameterIndex, (java.sql.Clob) parameterObj);
            } else {
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `java.sql` is unnecessary and can be removed
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
          case Types.CLOB:
            if (parameterObj instanceof java.sql.Clob) {
              setClob(parameterIndex, (java.sql.Clob) parameterObj);
            } else {
              setString(parameterIndex, parameterObj.toString());
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `java.sql` is unnecessary and can be removed
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
            switch (targetSqlType) {
              case Types.DATE:
                if (parameterAsDate instanceof java.sql.Date) {
                  setDate(parameterIndex, (java.sql.Date) parameterAsDate);
                } else {
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `java.sql` is unnecessary and can be removed
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
              case Types.DATE:
                if (parameterAsDate instanceof java.sql.Date) {
                  setDate(parameterIndex, (java.sql.Date) parameterAsDate);
                } else {
                  setDate(parameterIndex, new java.sql.Date(parameterAsDate.getTime()));
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `java.sql` is unnecessary and can be removed
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
                  setDate(parameterIndex, (java.sql.Date) parameterAsDate);
                } else {
                  setDate(parameterIndex, new java.sql.Date(parameterAsDate.getTime()));
                }

```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `java.sql` is unnecessary and can be removed
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java

              case Types.TIMESTAMP:
                if (parameterAsDate instanceof java.sql.Timestamp) {
                  setTimestamp(parameterIndex, (java.sql.Timestamp) parameterAsDate);
                } else {
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `java.sql` is unnecessary and can be removed
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
              case Types.TIMESTAMP:
                if (parameterAsDate instanceof java.sql.Timestamp) {
                  setTimestamp(parameterIndex, (java.sql.Timestamp) parameterAsDate);
                } else {
                  setTimestamp(parameterIndex, new java.sql.Timestamp(parameterAsDate.getTime()));
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `java.sql` is unnecessary and can be removed
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
                  setTimestamp(parameterIndex, (java.sql.Timestamp) parameterAsDate);
                } else {
                  setTimestamp(parameterIndex, new java.sql.Timestamp(parameterAsDate.getTime()));
                }

```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `org.apache.iotdb.jdbc` is unnecessary and can be removed
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
  @Override
  public String getDriverName() {
    return org.apache.iotdb.jdbc.IoTDBDriver.class.getName();
  }

```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `java.lang` is unnecessary and can be removed
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      String catalog,
      String schemaPattern,
      java.lang.String functionNamePattern,
      java.lang.String columnNamePattern)
      throws SQLException {
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `java.lang` is unnecessary and can be removed
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      String schemaPattern,
      java.lang.String functionNamePattern,
      java.lang.String columnNamePattern)
      throws SQLException {
    Statement stmt = connection.createStatement();
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `org.apache.commons.cli` is unnecessary, and can be replaced with an import
in `cli/src/main/java/org/apache/iotdb/tool/ImportCsv.java`
#### Snippet
```java
    try {
      commandLine = parser.parse(options, args);
    } catch (org.apache.commons.cli.ParseException e) {
      System.out.println("Parse error: " + e.getMessage());
      hf.printHelp(TSFILEDB_CLI_PREFIX, options, true);
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `org.apache.iotdb.db.mpp.plan.planner.plan.node` is unnecessary and can be removed
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/node/PlanNode.java`
#### Snippet
```java
  /**
   * Deserialize via {@link
   * org.apache.iotdb.db.mpp.plan.planner.plan.node.PlanNodeType#deserialize(ByteBuffer)}
   */
  @Override
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `org.apache.iotdb.db.qp.utils` is unnecessary, and can be replaced with an import
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/expression/leaf/TimestampOperand.java`
#### Snippet
```java
  @Override
  public void removeWildcards(
      org.apache.iotdb.db.qp.utils.WildcardsRemover wildcardsRemover,
      List<Expression> resultExpressions)
      throws LogicalOptimizeException {
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `org.apache.iotdb.db.qp.utils` is unnecessary, and can be replaced with an import
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/expression/Expression.java`
#### Snippet
```java
  @Deprecated
  public abstract void removeWildcards(
      org.apache.iotdb.db.qp.utils.WildcardsRemover wildcardsRemover,
      List<Expression> resultExpressions)
      throws LogicalOptimizeException;
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `org.apache.iotdb.db.qp.utils` is unnecessary, and can be replaced with an import
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/expression/leaf/ConstantOperand.java`
#### Snippet
```java
  @Override
  public void removeWildcards(
      org.apache.iotdb.db.qp.utils.WildcardsRemover wildcardsRemover,
      List<Expression> resultExpressions) {
    resultExpressions.add(this);
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `org.apache.iotdb.db.qp.utils` is unnecessary, and can be replaced with an import
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/expression/leaf/TimeSeriesOperand.java`
#### Snippet
```java
  @Override
  public void removeWildcards(
      org.apache.iotdb.db.qp.utils.WildcardsRemover wildcardsRemover,
      List<Expression> resultExpressions)
      throws LogicalOptimizeException {
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `org.apache.iotdb.db.qp.utils` is unnecessary, and can be replaced with an import
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/expression/binary/BinaryExpression.java`
#### Snippet
```java
  @Override
  public final void removeWildcards(
      org.apache.iotdb.db.qp.utils.WildcardsRemover wildcardsRemover,
      List<Expression> resultExpressions)
      throws LogicalOptimizeException {
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `org.apache.iotdb.db.qp.utils` is unnecessary, and can be replaced with an import
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/expression/multi/FunctionExpression.java`
#### Snippet
```java
  @Override
  public void removeWildcards(
      org.apache.iotdb.db.qp.utils.WildcardsRemover wildcardsRemover,
      List<Expression> resultExpressions)
      throws LogicalOptimizeException {
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `org.apache.iotdb.db.qp.utils` is unnecessary, and can be replaced with an import
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/expression/ternary/TernaryExpression.java`
#### Snippet
```java
  @Override
  public final void removeWildcards(
      org.apache.iotdb.db.qp.utils.WildcardsRemover wildcardsRemover,
      List<Expression> resultExpressions)
      throws LogicalOptimizeException {
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `org.apache.iotdb.db.wal.buffer` is unnecessary and can be removed
in `server/src/main/java/org/apache/iotdb/db/wal/buffer/WALEntryValue.java`
#### Snippet
```java
public interface WALEntryValue extends SerializedSize {
  /**
   * Serialize using {@link org.apache.iotdb.db.wal.buffer.IWALByteBufferView}, which encapsulates
   * some actions to deal with {@link java.nio.BufferOverflowException} occurs in {@link
   * java.nio.ByteBuffer}.
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `org.apache.iotdb.tsfile.utils` is unnecessary and can be removed
in `server/src/main/java/org/apache/iotdb/db/wal/utils/WALWriteUtils.java`
#### Snippet
```java
import java.util.Map;

/** Like {@link org.apache.iotdb.tsfile.utils.ReadWriteIOUtils} */
public class WALWriteUtils {
  public static final int BOOLEAN_LEN = ReadWriteIOUtils.BOOLEAN_LEN;
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `org.apache.iotdb.db.metadata.mtree.store.disk.schemafile` is unnecessary and can be removed
in `server/src/main/java/org/apache/iotdb/db/tools/schema/SchemaFileSketchTool.java`
#### Snippet
```java

/**
 * parse the {@linkplain org.apache.iotdb.db.metadata.mtree.store.disk.schemafile.SchemaFile} to
 * text
 */
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `org.apache.commons.io` is unnecessary, and can be replaced with an import
in `server/src/main/java/org/apache/iotdb/db/utils/FilesUtils.java`
#### Snippet
```java
    HashSet<File> fileSet =
        new HashSet<>(
            org.apache.commons.io.FileUtils.listFiles(
                SystemFileFactory.INSTANCE.getFile(folderPath), new String[] {"jar"}, true));
    return org.apache.commons.io.FileUtils.toURLs(fileSet.toArray(new File[0]));
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `org.apache.commons.io` is unnecessary, and can be replaced with an import
in `server/src/main/java/org/apache/iotdb/db/utils/FilesUtils.java`
#### Snippet
```java
            org.apache.commons.io.FileUtils.listFiles(
                SystemFileFactory.INSTANCE.getFile(folderPath), new String[] {"jar"}, true));
    return org.apache.commons.io.FileUtils.toURLs(fileSet.toArray(new File[0]));
  }
}
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `java.io` is unnecessary and can be removed
in `server/src/main/java/org/apache/iotdb/db/engine/modification/utils/TracedBufferedReader.java`
#### Snippet
```java

/**
 * Copied from {@link java.io.BufferedReader}, trace the read position by modifying the fill()
 * method.
 */
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `org.apache.commons.io` is unnecessary, and can be replaced with an import
in `server/src/main/java/org/apache/iotdb/db/service/metrics/FileMetrics.java`
#### Snippet
```java
                          try {
                            result +=
                                org.apache.commons.io.FileUtils.listFiles(walFolder, null, true)
                                    .size();
                          } catch (UncheckedIOException exception) {
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `com.sun.management` is unnecessary and can be removed
in `server/src/main/java/org/apache/iotdb/db/service/metrics/SystemMetrics.java`
#### Snippet
```java
public class SystemMetrics implements IMetricSet {
  private static final Logger logger = LoggerFactory.getLogger(SystemMetrics.class);
  private com.sun.management.OperatingSystemMXBean osMXBean;
  private Future<?> currentServiceFuture;
  private final ScheduledExecutorService service = Executors.newSingleThreadScheduledExecutor();
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `com.sun.management` is unnecessary and can be removed
in `server/src/main/java/org/apache/iotdb/db/service/metrics/ProcessMetrics.java`
#### Snippet
```java
  public ProcessMetrics() {
    sunOsMXBean =
        (com.sun.management.OperatingSystemMXBean) ManagementFactory.getOperatingSystemMXBean();
    runtime = Runtime.getRuntime();
  }
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `com.sun.management` is unnecessary and can be removed
in `server/src/main/java/org/apache/iotdb/db/service/metrics/ProcessMetrics.java`
#### Snippet
```java
        MetricLevel.CORE,
        sunOsMXBean,
        com.sun.management.OperatingSystemMXBean::getProcessCpuTime,
        Tag.NAME.toString(),
        "process");
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `javax.servlet` is unnecessary, and can be replaced with an import
in `server/src/main/java/org/apache/iotdb/db/protocol/rest/filter/ApiOriginFilter.java`
#### Snippet
```java
import java.io.IOException;

public class ApiOriginFilter implements javax.servlet.Filter {
  @Override
  public void doFilter(ServletRequest request, ServletResponse response, FilterChain chain)
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `java.lang.reflect` is unnecessary, and can be replaced with an import
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/dto/IoTDBPoint.java`
#### Snippet
```java
    TimeUnit precision = TimeUnit.NANOSECONDS;
    // Get the precision of point in influxdb by reflection
    for (java.lang.reflect.Field reflectField : point.getClass().getDeclaredFields()) {
      reflectField.setAccessible(true);
      try {
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `java.lang.reflect` is unnecessary, and can be replaced with an import
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/dto/IoTDBPoint.java`
#### Snippet
```java
    }
    // Get the property of point in influxdb by reflection
    for (java.lang.reflect.Field reflectField : point.getClass().getDeclaredFields()) {
      reflectField.setAccessible(true);
      try {
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `org.apache.iotdb.db.qp.logical.crud` is unnecessary, and can be replaced with an import
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/operator/InfluxSelectComponent.java`
#### Snippet
```java
/** this class maintains information from select clause. */
public final class InfluxSelectComponent
    extends org.apache.iotdb.db.qp.logical.crud.SelectComponent {

  private boolean hasAggregationFunction = false;
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `com.github.benmanes.caffeine.cache` is unnecessary, and can be replaced with an import
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/SchemaRegionMemoryImpl.java`
#### Snippet
```java
            .maximumSize(cacheSize)
            .build(
                new com.github.benmanes.caffeine.cache.CacheLoader<PartialPath, IMNode>() {
                  @Override
                  public @Nullable IMNode load(@NonNull PartialPath partialPath)
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `org.apache.iotdb.library.dprofile.util` is unnecessary, and can be replaced with an import
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/UDAFQuantile.java`
#### Snippet
```java
/** calculate the approximate percentile */
public class UDAFQuantile implements UDTF {
  private org.apache.iotdb.library.dprofile.util.HeapLongKLLSketch sketch;
  private double rank;
  private TSDataType dataType;
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `org.apache.iotdb.library.dprofile.util` is unnecessary, and can be replaced with an import
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/UDAFQuantile.java`
#### Snippet
```java
    rank = parameters.getDoubleOrDefault("rank", 0.5);

    sketch = new org.apache.iotdb.library.dprofile.util.HeapLongKLLSketch(K * 8);
  }

```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `org.apache.iotdb.library.dprofile.util` is unnecessary and can be removed
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/HeapLongKLLSketch.java`
#### Snippet
```java
// This is an implementation for long data type.
// works only in heap memory. don't need to serialize or deserialize
public class HeapLongKLLSketch extends org.apache.iotdb.library.dprofile.util.KLLSketchForQuantile {

  public HeapLongKLLSketch(int maxMemoryByte) { // maxN=7000 for PAGE, 1.6e6 for CHUNK
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `org.apache.iotdb.library.dprofile.util` is unnecessary and can be removed
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/HeapLongKLLSketch.java`
#### Snippet
```java

  public void mergeWithTempSpace(
      List<org.apache.iotdb.library.dprofile.util.KLLSketchForQuantile> otherList) {
    //    System.out.println("[MERGE]");
    //    show();
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `org.apache.iotdb.library.dprofile.util` is unnecessary and can be removed
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/HeapLongKLLSketch.java`
#### Snippet
```java
    long otherN = 0;
    //    System.out.print("\t\t\t\t[mergeWithTempSpace] others:");
    for (org.apache.iotdb.library.dprofile.util.KLLSketchForQuantile another : otherList)
      if (another != null) {
        //      System.out.print("\t"+another.getN());
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `org.apache.iotdb.library.dprofile.util` is unnecessary and can be removed
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/HeapLongKLLSketch.java`
#### Snippet
```java
          cntPos += oldLevelPos[i + 1] - oldLevelPos[i];
        }
        for (org.apache.iotdb.library.dprofile.util.KLLSketchForQuantile another : otherList)
          if (another != null && i < another.cntLevel) {
            System.arraycopy(
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `org.apache.iotdb.library.dprofile.util` is unnecessary and can be removed
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/HeapLongKLLSketch.java`
#### Snippet
```java
          numLen += oldLevelPos[i + 1] - oldLevelPos[i];
        }
        for (org.apache.iotdb.library.dprofile.util.KLLSketchForQuantile another : otherList)
          if (another != null && i < another.cntLevel) {
            System.arraycopy(
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `org.apache.iotdb.library.dprofile.util` is unnecessary and can be removed
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/HeapLongKLLSketch.java`
#### Snippet
```java

  public void mergeWithTempSpace(
      org.apache.iotdb.library.dprofile.util.KLLSketchForQuantile another) {
    //    System.out.println("[MERGE]");
    //    show();
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `org.apache.iotdb.library.dprofile.util` is unnecessary and can be removed
in `library-udf/src/main/java/org/apache/iotdb/library/dprofile/util/HeapLongKLLSketch.java`
#### Snippet
```java
  }

  public void merge(org.apache.iotdb.library.dprofile.util.KLLSketchForQuantile another) {
    //    System.out.println("[MERGE]");
    //    show();
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `io.micrometer.core.instrument` is unnecessary, and can be replaced with an import
in `metrics/micrometer-metrics/src/main/java/org/apache/iotdb/metrics/micrometer/type/MicrometerAutoGauge.java`
#### Snippet
```java

  public MicrometerAutoGauge(
      io.micrometer.core.instrument.MeterRegistry meterRegistry,
      String metricName,
      T object,
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `io.micrometer.core.instrument` is unnecessary, and can be replaced with an import
in `metrics/micrometer-metrics/src/main/java/org/apache/iotdb/metrics/micrometer/type/MicrometerHistogram.java`
#### Snippet
```java
public class MicrometerHistogram implements Histogram {

  io.micrometer.core.instrument.DistributionSummary distributionSummary;

  public MicrometerHistogram(
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `org.apache.iotdb.metrics.type` is unnecessary, and can be replaced with an import
in `metrics/micrometer-metrics/src/main/java/org/apache/iotdb/metrics/micrometer/type/MicrometerHistogram.java`
#### Snippet
```java

  @Override
  public org.apache.iotdb.metrics.type.HistogramSnapshot takeSnapshot() {
    return new MicrometerHistogramSnapshot(distributionSummary.takeSnapshot());
  }
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `io.micrometer.core.instrument` is unnecessary, and can be replaced with an import
in `metrics/micrometer-metrics/src/main/java/org/apache/iotdb/metrics/micrometer/type/MicrometerHistogram.java`
#### Snippet
```java

  public MicrometerHistogram(
      io.micrometer.core.instrument.DistributionSummary distributionSummary) {
    this.distributionSummary = distributionSummary;
  }
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `io.micrometer.core.instrument` is unnecessary, and can be replaced with an import
in `metrics/micrometer-metrics/src/main/java/org/apache/iotdb/metrics/micrometer/type/MicrometerGauge.java`
#### Snippet
```java

  public MicrometerGauge(
      io.micrometer.core.instrument.MeterRegistry meterRegistry,
      String metricName,
      String... tags) {
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `io.micrometer.core.instrument` is unnecessary, and can be replaced with an import
in `metrics/micrometer-metrics/src/main/java/org/apache/iotdb/metrics/micrometer/MicrometerMetricManager.java`
#### Snippet
```java
public class MicrometerMetricManager extends AbstractMetricManager {

  io.micrometer.core.instrument.MeterRegistry meterRegistry;

  public MicrometerMetricManager() {
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `io.micrometer.core.instrument` is unnecessary, and can be replaced with an import
in `metrics/micrometer-metrics/src/main/java/org/apache/iotdb/metrics/micrometer/MicrometerMetricManager.java`
#### Snippet
```java
  @Override
  public Histogram createHistogram(MetricInfo metricInfo) {
    io.micrometer.core.instrument.DistributionSummary distributionSummary =
        io.micrometer.core.instrument.DistributionSummary.builder(metricInfo.getName())
            .tags(metricInfo.getTagsInArray())
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `io.micrometer.core.instrument` is unnecessary, and can be replaced with an import
in `metrics/micrometer-metrics/src/main/java/org/apache/iotdb/metrics/micrometer/MicrometerMetricManager.java`
#### Snippet
```java
  public Histogram createHistogram(MetricInfo metricInfo) {
    io.micrometer.core.instrument.DistributionSummary distributionSummary =
        io.micrometer.core.instrument.DistributionSummary.builder(metricInfo.getName())
            .tags(metricInfo.getTagsInArray())
            .publishPercentiles(0, 0.25, 0.5, 0.75, 1)
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `confignode/src/main/java/org/apache/iotdb/confignode/manager/ConsensusManager.java`
#### Snippet
```java
                      .setThisNode(
                          new TEndPoint(CONF.getInternalAddress(), CONF.getConsensusPort()))
                      .setStorageDir("target" + java.io.File.separator + "simple")
                      .build(),
                  gid -> stateMachine)
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `com.github.benmanes.caffeine.cache` is unnecessary, and can be replaced with an import
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/SchemaRegionSchemaFileImpl.java`
#### Snippet
```java
                })
            .build(
                new com.github.benmanes.caffeine.cache.CacheLoader<PartialPath, IMNode>() {
                  @Override
                  public @Nullable IMNode load(@NonNull PartialPath partialPath)
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `org.apache.iotdb.hadoop.tsfile` is unnecessary and can be removed
in `hive-connector/src/main/java/org/apache/iotdb/hive/TSFHiveOutputFormat.java`
#### Snippet
```java

/**
 * The function implement is same as {@link org.apache.iotdb.hadoop.tsfile.TSFOutputFormat} and is
 * customized for Hive
 */
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `org.apache.iotdb.hadoop.tsfile` is unnecessary and can be removed
in `hive-connector/src/main/java/org/apache/iotdb/hive/TSFHiveRecordWriter.java`
#### Snippet
```java

/**
 * The function implement is same as {@link org.apache.iotdb.hadoop.tsfile.TSFRecordWriter} and is
 * customized for Hive
 */
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `org.apache.iotdb.hadoop.tsfile` is unnecessary and can be removed
in `hive-connector/src/main/java/org/apache/iotdb/hive/TSFHiveInputFormat.java`
#### Snippet
```java

/**
 * The class implement is same as {@link org.apache.iotdb.hadoop.tsfile.TSFInputFormat} and is
 * customized for Hive to implements JobConfigurable interface.
 */
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `java.io` is unnecessary and can be removed
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/writer/TsFileOutput.java`
#### Snippet
```java
   *
   * @return current position
   * @throws java.io.IOException if an I/O error occurs.
   */
  long getPosition() throws IOException;
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `java.nio.charset` is unnecessary, and can be replaced with an import
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/ReadWriteIOUtils.java`
#### Snippet
```java
  public static String readStringFromDirectByteBuffer(ByteBuffer buffer)
      throws CharacterCodingException {
    return java.nio.charset.StandardCharsets.UTF_8
        .newDecoder()
        .decode(buffer.duplicate())
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `java.nio.charset` is unnecessary, and can be replaced with an import
in `tsfile/src/main/java/org/apache/iotdb/tsfile/utils/ReadWriteIOUtils.java`
#### Snippet
```java

  public static ByteBuffer getByteBuffer(String s) {
    return ByteBuffer.wrap(s.getBytes(java.nio.charset.StandardCharsets.UTF_8));
  }

```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `java.io` is unnecessary and can be removed
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/encoder/BitmapEncoder.java`
#### Snippet
```java
   * @param out OutputStream to write encoded stream
   * @throws IOException cannot encode value
   * @see Encoder#encode(int, java.io.ByteArrayOutputStream)
   */
  @Override
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `java.io` is unnecessary and can be removed
in `tsfile/src/main/java/org/apache/iotdb/tsfile/encoding/encoder/BitmapEncoder.java`
#### Snippet
```java
   * @param out OutputStream to write encoded stream
   * @throws IOException cannot flush to OutputStream
   * @see Encoder#flush(java.io.ByteArrayOutputStream)
   */
  @Override
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `java.lang` is unnecessary and can be removed
in `tsfile/src/main/java/org/apache/iotdb/tsfile/exception/TsFileRuntimeException.java`
#### Snippet
```java
/**
 * This Exception is the parent class for all runtime exceptions.<br>
 * This Exception extends super class {@link java.lang.RuntimeException}
 */
public class TsFileRuntimeException extends RuntimeException {
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `org.apache.iotdb.commons.udf.builtin` is unnecessary, and can be replaced with an import
in `integration-test/src/main/java/org/apache/iotdb/itbase/constant/BuiltinAggregationFunctionEnum.java`
#### Snippet
```java
  private static final Set<String> NATIVE_FUNCTION_NAMES =
      new HashSet<>(
          Arrays.stream(org.apache.iotdb.commons.udf.builtin.BuiltinAggregationFunction.values())
              .map(org.apache.iotdb.commons.udf.builtin.BuiltinAggregationFunction::getFunctionName)
              .collect(Collectors.toList()));
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `org.apache.iotdb.commons.udf.builtin` is unnecessary, and can be replaced with an import
in `integration-test/src/main/java/org/apache/iotdb/itbase/constant/BuiltinAggregationFunctionEnum.java`
#### Snippet
```java
      new HashSet<>(
          Arrays.stream(org.apache.iotdb.commons.udf.builtin.BuiltinAggregationFunction.values())
              .map(org.apache.iotdb.commons.udf.builtin.BuiltinAggregationFunction::getFunctionName)
              .collect(Collectors.toList()));

```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `org.apache.hadoop.mapred` is unnecessary, and can be replaced with an import
in `hadoop/src/main/java/org/apache/iotdb/hadoop/tsfile/TSFInputSplit.java`
#### Snippet
```java
 */
public class TSFInputSplit extends FileSplit
    implements Writable, org.apache.hadoop.mapred.InputSplit {

  public TSFInputSplit() {
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `com.codahale.metrics` is unnecessary, and can be replaced with an import
in `metrics/dropwizard-metrics/src/main/java/org/apache/iotdb/metrics/dropwizard/type/DropwizardRate.java`
#### Snippet
```java
  }

  public DropwizardRate(com.codahale.metrics.Timer timer) {
    this.timer = timer;
    this.meter = null;
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `com.codahale.metrics` is unnecessary, and can be replaced with an import
in `metrics/dropwizard-metrics/src/main/java/org/apache/iotdb/metrics/dropwizard/type/DropwizardRate.java`
#### Snippet
```java
  Meter meter;
  /** read-only meter */
  com.codahale.metrics.Timer timer;

  public DropwizardRate(Meter meter) {
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `com.codahale.metrics` is unnecessary, and can be replaced with an import
in `metrics/dropwizard-metrics/src/main/java/org/apache/iotdb/metrics/dropwizard/type/DropwizardHistogramSnapshot.java`
#### Snippet
```java
public class DropwizardHistogramSnapshot implements HistogramSnapshot {

  com.codahale.metrics.Snapshot snapshot;

  public DropwizardHistogramSnapshot(com.codahale.metrics.Snapshot snapshot) {
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `com.codahale.metrics` is unnecessary, and can be replaced with an import
in `metrics/dropwizard-metrics/src/main/java/org/apache/iotdb/metrics/dropwizard/type/DropwizardHistogramSnapshot.java`
#### Snippet
```java
  com.codahale.metrics.Snapshot snapshot;

  public DropwizardHistogramSnapshot(com.codahale.metrics.Snapshot snapshot) {
    this.snapshot = snapshot;
  }
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `com.codahale.metrics` is unnecessary and can be removed
in `metrics/dropwizard-metrics/src/main/java/org/apache/iotdb/metrics/dropwizard/DropwizardMetricManager.java`
#### Snippet
```java
@SuppressWarnings("common-java:DuplicatedBlocks")
public class DropwizardMetricManager extends AbstractMetricManager {
  com.codahale.metrics.MetricRegistry metricRegistry;

  public DropwizardMetricManager() {
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `com.codahale.metrics` is unnecessary, and can be replaced with an import
in `metrics/dropwizard-metrics/src/main/java/org/apache/iotdb/metrics/dropwizard/type/DropwizardAutoGauge.java`
#### Snippet
```java
import java.util.function.ToLongFunction;

public class DropwizardAutoGauge<T> implements AutoGauge, com.codahale.metrics.Gauge<Long> {

  private final WeakReference<T> refObject;
```

### RuleId[ruleID=UnnecessaryFullyQualifiedName]
Qualifier `com.codahale.metrics` is unnecessary, and can be replaced with an import
in `metrics/dropwizard-metrics/src/main/java/org/apache/iotdb/metrics/dropwizard/type/DropwizardGauge.java`
#### Snippet
```java
  }

  public class DropwizardCachedGauge extends com.codahale.metrics.CachedGauge<Long> {

    protected DropwizardCachedGauge(long timeout, TimeUnit timeoutUnit) {
```

## RuleId[ruleID=RegExpSingleCharAlternation]
### RuleId[ruleID=RegExpSingleCharAlternation]
Single character alternation in RegExp
in `metrics/dropwizard-metrics/src/main/java/org/apache/iotdb/metrics/dropwizard/DropwizardMetricNameTool.java`
#### Snippet
```java
    String name = metricInfo.getName();
    Map<String, String> tags = metricInfo.getTags();
    return name.replaceAll("\\{|\\}", "")
        + "{"
        + tags.entrySet().stream()
```

### RuleId[ruleID=RegExpSingleCharAlternation]
Single character alternation in RegExp
in `metrics/dropwizard-metrics/src/main/java/org/apache/iotdb/metrics/dropwizard/DropwizardMetricNameTool.java`
#### Snippet
```java
                        + t.getValue().replace(TAG_SEPARATOR, ""))
            .collect(Collectors.joining(TAG_SEPARATOR))
            .replaceAll("\\{|\\}", "")
        + "}";
  }
```

## RuleId[ruleID=ThrowablePrintStackTrace]
### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `example/rest-java-example/src/main/java/org/apache/iotdb/SSLClient.java`
#### Snippet
```java
      poolingHttpClientConnectionManager.setMaxTotal(10);
    } catch (NoSuchAlgorithmException | KeyStoreException | KeyManagementException e) {
      e.printStackTrace();
    }
  }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `example/rest-java-example/src/main/java/org/apache/iotdb/HttpExample.java`
#### Snippet
```java
      System.out.println(result);
    } catch (IOException e) {
      e.printStackTrace();

    } finally {
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `example/rest-java-example/src/main/java/org/apache/iotdb/HttpExample.java`
#### Snippet
```java
        }
      } catch (IOException e) {
        e.printStackTrace();
      }
    }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `example/rest-java-example/src/main/java/org/apache/iotdb/HttpExample.java`
#### Snippet
```java
      System.out.println(result);
    } catch (IOException e) {
      e.printStackTrace();

    } finally {
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `example/rest-java-example/src/main/java/org/apache/iotdb/HttpExample.java`
#### Snippet
```java
        }
      } catch (IOException e) {
        e.printStackTrace();
      }
    }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `example/rest-java-example/src/main/java/org/apache/iotdb/HttpExample.java`
#### Snippet
```java
      System.out.println(map);
    } catch (IOException e) {
      e.printStackTrace();
    } finally {
      try {
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `example/rest-java-example/src/main/java/org/apache/iotdb/HttpExample.java`
#### Snippet
```java
        }
      } catch (IOException e) {
        e.printStackTrace();
      }
    }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `example/rest-java-example/src/main/java/org/apache/iotdb/HttpsExample.java`
#### Snippet
```java
      System.out.println(map);
    } catch (IOException e) {
      e.printStackTrace();
    } finally {
      try {
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `example/rest-java-example/src/main/java/org/apache/iotdb/HttpsExample.java`
#### Snippet
```java
        }
      } catch (IOException e) {
        e.printStackTrace();
      }
    }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `example/rest-java-example/src/main/java/org/apache/iotdb/HttpsExample.java`
#### Snippet
```java
      System.out.println(result);
    } catch (IOException e) {
      e.printStackTrace();

    } finally {
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `example/rest-java-example/src/main/java/org/apache/iotdb/HttpsExample.java`
#### Snippet
```java
        }
      } catch (IOException e) {
        e.printStackTrace();
      }
    }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `example/rest-java-example/src/main/java/org/apache/iotdb/HttpsExample.java`
#### Snippet
```java

    } catch (IOException e) {
      e.printStackTrace();

    } finally {
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `example/rest-java-example/src/main/java/org/apache/iotdb/HttpsExample.java`
#### Snippet
```java
        }
      } catch (IOException e) {
        e.printStackTrace();
      }
    }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `influxdb-protocol/src/main/java/org/apache/iotdb/influxdb/session/InfluxDBSession.java`
#### Snippet
```java
      RpcUtils.verifySuccess(client.createDatabase(request));
    } catch (TException e) {
      e.printStackTrace();
      logger.error(e.getMessage());
      if (reconnect()) {
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `influxdb-protocol/src/main/java/org/apache/iotdb/influxdb/session/InfluxDBSession.java`
#### Snippet
```java
      return JacksonUtils.json2Bean(tsQueryResultRsp.resultJsonString, QueryResult.class);
    } catch (TException e) {
      e.printStackTrace();
      logger.error(e.getMessage());
      if (reconnect()) {
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDataSource.java`
#### Snippet
```java
      return new IoTDBConnection(url, properties);
    } catch (TTransportException e) {
      e.printStackTrace();
    }
    return null;
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDataSource.java`
#### Snippet
```java
      return new IoTDBConnection(url, newProp);
    } catch (Exception e) {
      e.printStackTrace();
    }
    return null;
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/AbstractCsvTool.java`
#### Snippet
```java
      return true;
    } catch (IOException e) {
      e.printStackTrace();
      return false;
    }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/AbstractCsvTool.java`
#### Snippet
```java
          csvPrinter = csvFormat.print(new PrintWriter(filePath));
        } catch (IOException e) {
          e.printStackTrace();
        }
      }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/AbstractCsvTool.java`
#### Snippet
```java
        csvPrinter.print(value);
      } catch (IOException e) {
        e.printStackTrace();
      }
    }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
          parameterIndex, zonedDateTime.format(DateTimeFormatter.ISO_LOCAL_DATE_TIME));
    } catch (TException e) {
      e.printStackTrace();
    }
  }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java
      setLong(parameterIndex, time);
    } catch (TException e) {
      e.printStackTrace();
    }
  }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/cli/AbstractCli.java`
#### Snippet
```java
              }
            } catch (IOException e) {
              e.printStackTrace();
              executeStatus = CODE_ERROR;
            }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/cli/AbstractCli.java`
#### Snippet
```java
          connection.getTimeZone());
    } catch (IoTDBConnectionException e) {
      e.printStackTrace();
      return CODE_ERROR;
    }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `example/tsfile/src/main/java/org/apache/iotdb/tsfile/TsFileWriteWithTSRecord.java`
#### Snippet
```java
      }
    } catch (Throwable e) {
      e.printStackTrace();
      System.out.println(e.getMessage());
    }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `jdbc/src/main/java/org/apache/iotdb/jdbc/AbstractIoTDBJDBCResultSet.java`
#### Snippet
```java
      }
    } catch (SQLException throwables) {
      throwables.printStackTrace();
    }
    return new IoTDBResultMetadata(
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      }
    } catch (Exception e) {
      e.printStackTrace();
    } finally {
      close(null, stmt);
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      }
    } catch (Exception e) {
      e.printStackTrace();
    } finally {
      close(null, stmt);
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      tsBlock = convertTsBlock(valuesList, tsDataTypeList);
    } catch (IOException e) {
      e.printStackTrace();
    }
    return new IoTDBJDBCResultSet(
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      }
    } catch (Exception e) {
      e.printStackTrace();
    } finally {
      close(null, stmt);
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      return client.getProperties().isReadOnly;
    } catch (TException e) {
      e.printStackTrace();
    }
    throw new SQLException("Can not get the read-only mode");
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      }
    } catch (Exception e) {
      e.printStackTrace();
    } finally {
      close(null, stmt);
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      maxcount = client.getProperties().getMaxConcurrentClientNum();
    } catch (TException e) {
      e.printStackTrace();
    }
    return maxcount;
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      return client.getProperties().getThriftMaxFrameSize();
    } catch (TException e) {
      e.printStackTrace();
    }
    return 0;
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      }
    } catch (Exception e) {
      e.printStackTrace();
    } finally {
      close(null, stmt);
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      tsBlock = convertTsBlock(valuesList, tsDataTypeList);
    } catch (IOException e) {
      e.printStackTrace();
    } finally {
      close(rs, stmt);
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      tsBlock = convertTsBlock(valuesList, tsDataTypeList);
    } catch (IOException e) {
      e.printStackTrace();
    } finally {
      close(rs, stmt);
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      tsBlock = convertTsBlock(valuesList, tsDataTypeList);
    } catch (IOException e) {
      e.printStackTrace();
    } finally {
      close(null, stmt);
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      }
    } catch (Exception ex) {
      ex.printStackTrace();
    } finally {
      close(resultSet, statement);
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      }
    } catch (Exception e) {
      e.printStackTrace();
    } finally {
      close(null, stmt);
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      }
    } catch (Exception e) {
      e.printStackTrace();
    } finally {
      close(null, stmt);
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      }
    } catch (Exception e) {
      e.printStackTrace();
    } finally {
      close(null, stmt);
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      }
    } catch (TException e) {
      e.printStackTrace();
    }
    return minor_version;
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      tsBlock = convertTsBlock(valuesList, tsDataTypeList);
    } catch (IOException e) {
      e.printStackTrace();
    } finally {
      close(null, stmt);
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      tsBlock = convertTsBlock(valuesList, tsDataTypeList);
    } catch (IOException e) {
      e.printStackTrace();
    } finally {
      close(rs, stmt);
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      }
    } catch (Exception e) {
      e.printStackTrace();
    } finally {
      close(null, stmt);
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      }
    } catch (TException e) {
      e.printStackTrace();
    }
    return major_version;
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      tsBlock = convertTsBlock(Collections.singletonList(value), tsDataTypeList);
    } catch (IOException e) {
      e.printStackTrace();
    } finally {
      close(null, stmt);
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      }
    } catch (Exception e) {
      e.printStackTrace();
    } finally {
      close(null, stmt);
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      }
    } catch (Exception e) {
      e.printStackTrace();
    } finally {
      close(null, stmt);
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      tsBlock = convertTsBlock(valuesList, tsDataTypeList);
    } catch (IOException e) {
      e.printStackTrace();
    } finally {
      close(rs, stmt);
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      tsBlock = convertTsBlock(valuesList, tsDataTypeList);
    } catch (IOException e) {
      e.printStackTrace();
    } finally {
      close(rs, stmt);
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
              client.getProperties().getWatermarkParamMaxRightBit());
    } catch (TException e) {
      e.printStackTrace();
    }
    return groupedLSBWatermarkEncoder;
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      tsBlock = convertTsBlock(valuesList, tsDataTypeList);
    } catch (IOException e) {
      e.printStackTrace();
    } finally {
      close(rs, stmt);
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      }
    } catch (Exception e) {
      e.printStackTrace();
    } finally {
      close(null, stmt);
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      }
    } catch (Exception e) {
      e.printStackTrace();
    } finally {
      close(resultSet, statement);
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      tsBlock = convertTsBlock(valuesList, tsDataTypeList);
    } catch (IOException e) {
      e.printStackTrace();
    } finally {
      close(rs, stmt);
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      tsBlock = convertTsBlock(valuesList, tsDataTypeList);
    } catch (IOException e) {
      e.printStackTrace();
    } finally {
      close(rs, stmt);
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBDatabaseMetadata.java`
#### Snippet
```java
      tsBlock = convertTsBlock(Collections.singletonList(value), tsDataTypeList);
    } catch (IOException e) {
      e.printStackTrace();
    } finally {
      close(null, stmt);
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBJDBCResultSet.java`
#### Snippet
```java
      }
    } catch (SQLException throwables) {
      throwables.printStackTrace();
    }
    return new IoTDBResultMetadata(
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `integration/src/main/java/org/apache/iotdb/integration/env/EnvFactory.java`
#### Snippet
```java
        }
      } catch (ClassNotFoundException | IllegalAccessException | InstantiationException e) {
        e.printStackTrace();
        System.exit(-1);
      }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `integration/src/main/java/org/apache/iotdb/integration/env/ConfigFactory.java`
#### Snippet
```java
        }
      } catch (ClassNotFoundException | IllegalAccessException | InstantiationException e) {
        e.printStackTrace();
        System.exit(-1);
      }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `integration/src/main/java/org/apache/iotdb/integration/env/RemoteServerEnv.java`
#### Snippet
```java
      statement.execute("DELETE DATABASE root;");
    } catch (Exception e) {
      e.printStackTrace();
      fail(e.getMessage());
    }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `integration/src/main/java/org/apache/iotdb/integration/env/RemoteServerEnv.java`
#### Snippet
```java
              password);
    } catch (ClassNotFoundException e) {
      e.printStackTrace();
      fail();
    }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `integration/src/main/java/org/apache/iotdb/integration/env/RemoteServerEnv.java`
#### Snippet
```java
      statement.execute("DELETE DATABASE root;");
    } catch (Exception e) {
      e.printStackTrace();
      fail(e.getMessage());
    }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `integration/src/main/java/org/apache/iotdb/integration/env/RemoteServerEnv.java`
#### Snippet
```java
              Config.IOTDB_URL_PREFIX + ip_addr + ":" + port, user, password);
    } catch (ClassNotFoundException e) {
      e.printStackTrace();
      fail();
    }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `integration/src/main/java/org/apache/iotdb/integration/env/ClusterEnvBase.java`
#### Snippet
```java
              System.getProperty("Password", "root"));
    } catch (ClassNotFoundException e) {
      e.printStackTrace();
      fail();
    }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `integration/src/main/java/org/apache/iotdb/integration/env/ClusterEnvBase.java`
#### Snippet
```java
              System.getProperty("Password", "root"));
    } catch (ClassNotFoundException e) {
      e.printStackTrace();
      fail();
    }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `integration/src/main/java/org/apache/iotdb/integration/env/ClusterEnvBase.java`
#### Snippet
```java
      connection.setQueryTimeout(queryTimeout);
    } catch (ClassNotFoundException e) {
      e.printStackTrace();
      fail();
    }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `integration/src/main/java/org/apache/iotdb/integration/env/ClusterNode.java`
#### Snippet
```java
        Thread.sleep(100);
      } catch (InterruptedException e) {
        e.printStackTrace();
      }
    }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/ImportCsv.java`
#### Snippet
```java
        queryType(devicesStr, headerTypeMap, "Time");
      } catch (IoTDBConnectionException e) {
        e.printStackTrace();
      }
    }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `cli/src/main/java/org/apache/iotdb/tool/ImportCsv.java`
#### Snippet
```java
                    typeQueriedDevice.add(deviceName.get());
                  } catch (IoTDBConnectionException e) {
                    e.printStackTrace();
                  }
                }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/execution/config/metadata/template/ShowPathSetTemplateTask.java`
#### Snippet
```java
      }
    } catch (Exception e) {
      e.printStackTrace();
    }
    DatasetHeader datasetHeader = DatasetHeaderFactory.getShowPathSetTemplateHeader();
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/execution/config/metadata/template/ShowNodesInSchemaTemplateTask.java`
#### Snippet
```java
      }
    } catch (Exception e) {
      e.printStackTrace();
    }
    DatasetHeader datasetHeader = DatasetHeaderFactory.getShowNodesInSchemaTemplateHeader();
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/sync/transport/client/SenderManager.java`
#### Snippet
```java
      }
    } catch (InterruptedException e) {
      e.printStackTrace();
    }
  }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/conf/IoTDBConfig.java`
#### Snippet
```java
        configMessage = configMessage + configField.getName() + "=" + configContent + "; ";
      } catch (Exception e) {
        e.printStackTrace();
      }
    }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/tools/settle/TsFileAndModSettleTool.java`
#### Snippet
```java
        System.out.println(
            "Meet error while settling the tsFile : " + resourceToBeSettled.getTsFilePath());
        e.printStackTrace();
      }
    }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/tools/settle/TsFileAndModSettleTool.java`
#### Snippet
```java
          newTsFileResource.serialize();
        } catch (IOException e) {
          e.printStackTrace();
        }
        File tmpResourceFile =
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileSketchTool.java`
#### Snippet
```java
      printlnBoth(pw, splitStr);
    } catch (IOException e) {
      e.printStackTrace();
    }
  }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileSketchTool.java`
#### Snippet
```java
      }
    } catch (IOException e) {
      e.printStackTrace();
    }
  }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileSketchTool.java`
#### Snippet
```java
              + reader.readTailMagic());
    } catch (IOException e) {
      e.printStackTrace();
    }
  }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileSketchTool.java`
#### Snippet
```java
      }
    } catch (IOException e) {
      e.printStackTrace();
    }
  }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileSketchTool.java`
#### Snippet
```java
              + reader.readVersionNumber());
    } catch (IOException e) {
      e.printStackTrace();
    }
  }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/tools/TsFileSketchTool.java`
#### Snippet
```java
      }
    } catch (IOException e) {
      e.printStackTrace();
    }
  }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/service/SettleService.java`
#### Snippet
```java
      setRecoverFinish(true);
    } catch (WriteProcessException e) {
      e.printStackTrace();
    }
  }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/MTreeReleaseTaskManager.java`
#### Snippet
```java
          } catch (Throwable throwable) {
            logger.error("Something wrong happened during MTree release.", throwable);
            throwable.printStackTrace();
            throw throwable;
          }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/disk/MTreeFlushTaskManager.java`
#### Snippet
```java
          } catch (Throwable throwable) {
            logger.error("Something wrong happened during MTree flush.", throwable);
            throwable.printStackTrace();
            throw throwable;
          }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/metadata/mtree/store/CachedMTreeStore.java`
#### Snippet
```java
      logger.error(
          "Error occurred during MTree flush, current SchemaRegion is {}", root.getFullPath(), e);
      e.printStackTrace();
    } finally {
      writeLock.unlock();
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/metadata/template/ClusterTemplateManager.java`
#### Snippet
```java
                      listPath.add(new PartialPath(item));
                    } catch (IllegalPathException e) {
                      e.printStackTrace();
                    }
                  });
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/SchemaEngine.java`
#### Snippet
```java
      } catch (ExecutionException | InterruptedException | RuntimeException e) {
        logger.error("Something wrong happened during SchemaRegion recovery: " + e.getMessage());
        e.printStackTrace();
      }
    }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/SchemaEngine.java`
#### Snippet
```java
      initForLocalConfigNode();
    } catch (MetadataException e) {
      e.printStackTrace();
      logger.error("Error occurred during SchemaEngine initialization.", e);
    }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/SchemaRegionMemoryImpl.java`
#### Snippet
```java
        return idx;
      } catch (Exception e) {
        e.printStackTrace();
        throw new IOException("Failed to parse " + storageGroupFullPath + " mlog.bin for err:" + e);
      }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/SessionPoolExample.java`
#### Snippet
```java
              }
            } catch (IoTDBConnectionException | StatementExecutionException e) {
              e.printStackTrace();
            } finally {
              // remember to close data set finally!
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/SessionPoolExample.java`
#### Snippet
```java
              }
            } catch (IoTDBConnectionException | StatementExecutionException e) {
              e.printStackTrace();
            } finally {
              // remember to close data set finally!
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/SessionConcurrentExample.java`
#### Snippet
```java
      latch.await();
    } catch (InterruptedException e) {
      e.printStackTrace();
    }
  }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/SessionConcurrentExample.java`
#### Snippet
```java
      session.open(false);
    } catch (IoTDBConnectionException e) {
      e.printStackTrace();
    }

```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/SessionConcurrentExample.java`
#### Snippet
```java
            session, String.format("root.sg_%d.d_%d", currentIndex / parallelDegreeForOneSG, j));
      } catch (IoTDBConnectionException | StatementExecutionException e) {
        e.printStackTrace();
      }
    }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `example/session/src/main/java/org/apache/iotdb/SessionConcurrentExample.java`
#### Snippet
```java
      session.close();
    } catch (IoTDBConnectionException e) {
      e.printStackTrace();
    }

```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/util/QueryResultUtils.java`
#### Snippet
```java
      }
    } catch (Exception e) {
      e.printStackTrace();
    }

```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/protocol/influxdb/util/QueryResultUtils.java`
#### Snippet
```java
      }
    } catch (Exception e) {
      e.printStackTrace();
    }

```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rewrite-tsfile-tool/src/main/java/org/apache/iotdb/RewriteTsFileTool.java`
#### Snippet
```java
        } catch (Throwable t) {
          // this is a broken chunk, skip it
          t.printStackTrace();
          System.out.printf("Skip broken chunk in device %s.%s%n", device, p.getMeasurement());
        }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rewrite-tsfile-tool/src/main/java/org/apache/iotdb/RewriteTsFileTool.java`
#### Snippet
```java
        System.out.println(
            "------------------------------Error Message------------------------------");
        e.printStackTrace();
        System.out.println(
            "------------------------------End Message------------------------------");
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rewrite-tsfile-tool/src/main/java/org/apache/iotdb/RewriteTsFileTool.java`
#### Snippet
```java
        System.out.println(
            "------------------------------Error Message------------------------------");
        e.printStackTrace();
        System.out.println(
            "------------------------------End Message------------------------------");
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rewrite-tsfile-tool/src/main/java/org/apache/iotdb/RewriteTsFileTool.java`
#### Snippet
```java
    } catch (IoTDBConnectionException e) {
      System.out.printf("Can not connect to IoTDB. %s%n", e.getMessage());
      e.printStackTrace();
    } catch (Exception e) {
      System.out.printf("Load Error. %s%n", e.getMessage());
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rewrite-tsfile-tool/src/main/java/org/apache/iotdb/RewriteTsFileTool.java`
#### Snippet
```java
    } catch (Exception e) {
      System.out.printf("Load Error. %s%n", e.getMessage());
      e.printStackTrace();
    } finally {
      if (session != null) {
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `rewrite-tsfile-tool/src/main/java/org/apache/iotdb/RewriteTsFileTool.java`
#### Snippet
```java
        } catch (IoTDBConnectionException e) {
          System.out.printf("Can not connect to IoTDB. %s%n", e.getMessage());
          e.printStackTrace();
        }
      }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `server/src/main/java/org/apache/iotdb/db/metadata/schemaregion/SchemaRegionSchemaFileImpl.java`
#### Snippet
```java
        return idx;
      } catch (Exception e) {
        e.printStackTrace();
        throw new IOException("Failed to parse " + storageGroupFullPath + " mlog.bin for err:" + e);
      }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `confignode/src/main/java/org/apache/iotdb/confignode/persistence/schema/TemplateTable.java`
#### Snippet
```java
      template.serialize(outputStream);
    } catch (IOException e) {
      e.printStackTrace();
    }
  }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `spark-tsfile/src/main/java/org/apache/iotdb/spark/tsfile/io/TsFileRecordWriter.java`
#### Snippet
```java
      tsFileWriter.write(tsRecord);
    } catch (WriteProcessException e) {
      e.printStackTrace();
    }
  }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `spark-tsfile/src/main/java/org/apache/iotdb/spark/tsfile/qp/Executor.java`
#### Snippet
```java
      }
    } catch (IOException e) {
      e.printStackTrace();
    }

```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `integration-test/src/main/java/org/apache/iotdb/it/env/RemoteServerEnv.java`
#### Snippet
```java
              Config.IOTDB_URL_PREFIX + ip_addr + ":" + port, this.user, this.password);
    } catch (ClassNotFoundException e) {
      e.printStackTrace();
      fail();
    }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `integration-test/src/main/java/org/apache/iotdb/it/env/RemoteServerEnv.java`
#### Snippet
```java
      statement.execute("DELETE DATABASE root;");
    } catch (Exception e) {
      e.printStackTrace();
      fail(e.getMessage());
    }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `integration-test/src/main/java/org/apache/iotdb/it/env/RemoteServerEnv.java`
#### Snippet
```java
      statement.execute("DELETE DATABASE root;");
    } catch (Exception e) {
      e.printStackTrace();
      fail(e.getMessage());
    }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `integration-test/src/main/java/org/apache/iotdb/it/env/RemoteServerEnv.java`
#### Snippet
```java
              this.password);
    } catch (ClassNotFoundException e) {
      e.printStackTrace();
      fail();
    }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `integration-test/src/main/java/org/apache/iotdb/it/env/EnvFactory.java`
#### Snippet
```java
        }
      } catch (ClassNotFoundException e) {
        e.printStackTrace();
        System.exit(-1);
      }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `integration-test/src/main/java/org/apache/iotdb/it/env/AbstractNodeWrapper.java`
#### Snippet
```java
      }
    }
    lastException.printStackTrace();
    fail("Delete node dir failed.");
  }
```

### RuleId[ruleID=ThrowablePrintStackTrace]
Call to `printStackTrace()` should probably be replaced with more robust logging
in `example/hadoop/src/main/java/org/apache/iotdb/hadoop/tsfile/TsFileHelper.java`
#### Snippet
```java

    } catch (Throwable e) {
      e.printStackTrace();
      System.out.println(e.getMessage());
    }
```

## RuleId[ruleID=UnnecessaryContinue]
### RuleId[ruleID=UnnecessaryContinue]
`continue` is unnecessary as the last statement in a loop
in `schema-engine-tag/src/main/java/org/apache/iotdb/db/metadata/tagSchemaRegion/tagIndex/TagInvertedIndex.java`
#### Snippet
```java
    for (Map.Entry<String, String> tag : tags.entrySet()) {
      RoaringBitmap rb = getMatchedIDs(tag.getKey(), tag.getValue());
      if (rb == null) continue;
      else {
        if (i == 0) roaringBitmap = rb;
```

### RuleId[ruleID=UnnecessaryContinue]
`continue` is unnecessary as the last statement in a loop
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/DeletionGroup.java`
#### Snippet
```java
        intervalCursor.startTime = entry.getKey();
        intervalCursor.endTime = entry.getValue();
        continue;
      } else {
        intervalCursor.subsequentNoDelete = true;
```

### RuleId[ruleID=UnnecessaryContinue]
`continue` is unnecessary as the last statement in a loop
in `server/src/main/java/org/apache/iotdb/db/sync/externalpipe/ExtPipePluginManager.java`
#### Snippet
```java
                logger.error("monitorPipeData(), Can not append TsFile: {}" + tsFileFullName);
              }
              continue;
            } else if (pipeData instanceof DeletionPipeData) {
              // == handle delete PipeData
```

### RuleId[ruleID=UnnecessaryContinue]
`continue` is unnecessary as the last statement in a loop
in `server/src/main/java/org/apache/iotdb/db/sync/datasource/TsFileOpBlock.java`
#### Snippet
```java
            timeChunkPointCount = chunkInfo.pointCount;
            iter.remove();
            continue;
          } else {
            chunkInfo.isTimeAligned = true;
```

## RuleId[ruleID=CopyConstructorMissesField]
### RuleId[ruleID=CopyConstructorMissesField]
Copy constructor does not copy field 'aliasSet'
in `server/src/main/java/org/apache/iotdb/db/qp/logical/crud/QueryOperator.java`
#### Snippet
```java
  }

  public QueryOperator(QueryOperator queryOperator) {
    this();
    this.selectComponent = queryOperator.getSelectComponent();
```

### RuleId[ruleID=CopyConstructorMissesField]
Copy constructor does not copy field 'parametersString'
in `server/src/main/java/org/apache/iotdb/db/mpp/plan/planner/plan/parameter/AggregationDescriptor.java`
#### Snippet
```java
  }

  public AggregationDescriptor(AggregationDescriptor other) {
    this.aggregationFuncName = other.aggregationFuncName;
    this.aggregationType = other.getAggregationType();
```

### RuleId[ruleID=CopyConstructorMissesField]
Copy constructor does not copy 9 fields
in `server/src/main/java/org/apache/iotdb/db/engine/storagegroup/TsFileResource.java`
#### Snippet
```java
  public TsFileResource() {}

  public TsFileResource(TsFileResource other) throws IOException {
    this.file = other.file;
    this.processor = other.processor;
```

### RuleId[ruleID=CopyConstructorMissesField]
Copy constructor does not copy 5 fields
in `tsfile/src/main/java/org/apache/iotdb/tsfile/file/metadata/TimeseriesMetadata.java`
#### Snippet
```java
  }

  public TimeseriesMetadata(TimeseriesMetadata timeseriesMetadata) {
    this.timeSeriesMetadataType = timeseriesMetadata.timeSeriesMetadataType;
    this.chunkMetaDataListDataSize = timeseriesMetadata.chunkMetaDataListDataSize;
```

### RuleId[ruleID=CopyConstructorMissesField]
Copy constructor does not copy fields 'deleteIntervalList' and 'modified'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/file/metadata/ChunkMetadata.java`
#### Snippet
```java

  // won't clone deleteIntervalList & modified
  public ChunkMetadata(ChunkMetadata other) {
    this.measurementUid = other.measurementUid;
    this.offsetOfChunkHeader = other.offsetOfChunkHeader;
```

## RuleId[ruleID=ThreadStartInConstruction]
### RuleId[ruleID=ThreadStartInConstruction]
Call to `start()` during object construction
in `server/src/main/java/org/apache/iotdb/db/engine/trigger/utils/BatchHandlerQueue.java`
#### Snippet
```java
              queues[i]);
      t.setDaemon(true);
      t.start();
    }
  }
```

## RuleId[ruleID=BigDecimalMethodWithoutRoundingCalled]
### RuleId[ruleID=BigDecimalMethodWithoutRoundingCalled]
'BigDecimal.setScale()' called without a rounding mode argument
in `jdbc/src/main/java/org/apache/iotdb/jdbc/IoTDBPreparedStatement.java`
#### Snippet
```java

          try {
            scaledBigDecimal = ((java.math.BigDecimal) parameterAsNum).setScale(scale);
          } catch (ArithmeticException ex) {
            try {
```

## RuleId[ruleID=CastCanBeRemovedNarrowingVariableType]
### RuleId[ruleID=CastCanBeRemovedNarrowingVariableType]
Cast may be removed by changing the type of 'chunkMetaData' to 'ChunkMetadata'
in `tsfile/src/main/java/org/apache/iotdb/tsfile/write/writer/RestorableTsFileIOWriter.java`
#### Snippet
```java
        // all the stale data.
        if (dataType == null || dataType.equals(chunkMetaData.getDataType())) {
          chunkMetadataList.add((ChunkMetadata) chunkMetaData);
        }
      }
```

