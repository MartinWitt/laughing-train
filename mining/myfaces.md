# myfaces 
 
# Bad smells
I found 5683 bad smells with 879 repairable:
| ruleID | number | fixable |
| --- | --- | --- |
| ReturnNull | 871 | false |
| UnnecessaryFullyQualifiedName | 419 | false |
| MissortedModifiers | 416 | false |
| RedundantFieldInitialization | 377 | false |
| UnnecessaryModifier | 347 | true |
| UnusedAssignment | 294 | false |
| ConstantValue | 230 | false |
| DataFlowIssue | 193 | false |
| CodeBlock2Expr | 168 | false |
| AssignmentToMethodParameter | 163 | false |
| SizeReplaceableByIsEmpty | 149 | true |
| NonProtectedConstructorInAbstractClass | 139 | true |
| DuplicateThrows | 111 | false |
| StaticCallOnSubclass | 88 | false |
| BoundedWildcard | 82 | false |
| ReplaceNullCheck | 81 | false |
| UnnecessaryBoxing | 73 | false |
| AbstractClassNeverImplemented | 71 | false |
| RedundantImplements | 64 | false |
| UtilityClassWithoutPrivateConstructor | 59 | true |
| ToArrayCallWithZeroLengthArrayArgument | 50 | true |
| EmptyMethod | 45 | false |
| WhileCanBeForeach | 44 | false |
| UnnecessaryLocalVariable | 44 | true |
| ConditionCoveredByFurtherCondition | 44 | false |
| AssignmentToForLoopParameter | 41 | false |
| NonSerializableFieldInSerializableClass | 40 | false |
| ProtectedMemberInFinalClass | 37 | true |
| RedundantMethodOverride | 35 | false |
| MarkedForRemoval | 32 | false |
| ObsoleteCollection | 32 | false |
| DynamicRegexReplaceableByCompiledPattern | 30 | false |
| CommentedOutCode | 28 | false |
| RedundantSuppression | 28 | false |
| TypeParameterExtendsObject | 28 | false |
| ExtendsAnnotation | 27 | false |
| RedundantArrayCreation | 27 | false |
| UnnecessarySuperQualifier | 27 | false |
| UnnecessaryToStringCall | 24 | true |
| ReplaceAssignmentWithOperatorAssignment | 23 | false |
| IfStatementWithIdenticalBranches | 23 | false |
| FinalStaticMethod | 22 | false |
| RegExpSimplifiable | 22 | false |
| CatchMayIgnoreException | 22 | false |
| EqualsWhichDoesntCheckParameterClass | 22 | false |
| SynchronizationOnLocalVariableOrMethodParameter | 19 | false |
| Convert2MethodRef | 19 | false |
| FieldAccessedSynchronizedAndUnsynchronized | 18 | false |
| StringOperationCanBeSimplified | 16 | false |
| ZeroLengthArrayInitialization | 15 | false |
| DeprecatedIsStillUsed | 15 | false |
| SynchronizeOnThis | 14 | false |
| CollectionContainsUrl | 14 | false |
| InnerClassMayBeStatic | 13 | true |
| IgnoreResultOfCall | 11 | false |
| NestedAssignment | 11 | false |
| PublicFieldAccessedInSynchronizedContext | 11 | false |
| NonFinalFieldOfException | 11 | false |
| SynchronizeOnNonFinalField | 11 | false |
| Java8MapApi | 11 | false |
| UnnecessaryContinue | 11 | false |
| RegExpRedundantEscape | 10 | false |
| FinalPrivateMethod | 10 | false |
| IndexOfReplaceableByContains | 10 | false |
| InstanceofIncompatibleInterface | 10 | false |
| NullArgumentToVariableArgMethod | 10 | false |
| CastCanBeRemovedNarrowingVariableType | 10 | false |
| StringEqualsEmptyString | 9 | false |
| ClassNameSameAsAncestorName | 8 | false |
| TypeParameterHidesVisibleType | 8 | false |
| PointlessBooleanExpression | 8 | true |
| RedundantCollectionOperation | 8 | false |
| UnnecessaryUnboxing | 7 | false |
| StringBufferReplaceableByString | 7 | false |
| NonShortCircuitBoolean | 7 | false |
| MethodOverridesStaticMethod | 7 | false |
| RegExpSingleCharAlternation | 7 | false |
| SystemOutErr | 6 | false |
| SuspiciousSystemArraycopy | 6 | false |
| UnnecessaryReturn | 6 | true |
| JavaReflectionMemberAccess | 5 | false |
| StringBufferReplaceableByStringBuilder | 5 | false |
| NonStaticFinalLogger | 5 | false |
| RedundantUnmodifiable | 5 | false |
| ThrowablePrintStackTrace | 5 | false |
| CastConflictsWithInstanceof | 4 | false |
| UtilityClassWithPublicConstructor | 4 | false |
| DuplicateBranchesInSwitch | 4 | false |
| Java9CollectionFactory | 4 | false |
| TrivialStringConcatenation | 3 | false |
| MismatchedCollectionQueryUpdate | 3 | false |
| StringRepeatCanBeUsed | 3 | false |
| NonStrictComparisonCanBeEquality | 3 | true |
| IOResource | 3 | false |
| RedundantOperationOnEmptyContainer | 3 | false |
| RedundantLengthCheck | 3 | false |
| MissingDeprecatedAnnotation | 3 | false |
| AssignmentToStaticFieldFromInstanceMethod | 2 | false |
| ObjectNotify | 2 | false |
| DuplicateCondition | 2 | false |
| UnnecessarySemicolon | 2 | false |
| UnnecessaryCallToStringValueOf | 2 | false |
| UnnecessaryInitCause | 2 | false |
| LoopStatementsThatDontLoop | 2 | false |
| AnonymousHasLambdaAlternative | 2 | false |
| IfStatementMissingBreakInLoop | 2 | false |
| JUnitMalformedDeclaration | 2 | false |
| MismatchedJavadocCode | 2 | false |
| Convert2Lambda | 2 | false |
| Finalize | 1 | false |
| WrapperTypeMayBePrimitive | 1 | false |
| PointlessArithmeticExpression | 1 | false |
| EmptyStatementBody | 1 | false |
| EqualsBetweenInconvertibleTypes | 1 | false |
| OptionalAssignedToNull | 1 | false |
| UNUSED_IMPORT | 1 | false |
| HtmlWrongAttributeValue | 1 | false |
| DoubleNegation | 1 | false |
| ManualArrayToCollectionCopy | 1 | false |
| NonSynchronizedMethodOverridesSynchronizedMethod | 1 | false |
| OptionalUsedAsFieldOrParameterType | 1 | false |
| SamePackageImport | 1 | false |
| JavaReflectionInvocation | 1 | false |
| UseCompareMethod | 1 | false |
| UseBulkOperation | 1 | false |
| AssignmentToCatchBlockParameter | 1 | false |
| StringEquality | 1 | false |
| UnnecessaryStringEscape | 1 | false |
| DefaultAnnotationParam | 1 | false |
| SimplifyOptionalCallChains | 1 | false |
| FuseStreamOperations | 1 | false |
| NonExceptionNameEndsWithException | 1 | false |
| StringConcatenationInsideStringBufferAppend | 1 | false |
| OptionalIsPresent | 1 | false |
| FieldMayBeStatic | 1 | false |
| ParameterCanBeLocal | 1 | false |
| ThreadStartInConstruction | 1 | false |
## RuleId[ruleID=ToArrayCallWithZeroLengthArrayArgument]
### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new SelectItem\[selectItems.size()\]'
in `api/src/main/java/jakarta/faces/model/SelectItemGroup.java`
#### Snippet
```java
        super("", label, description, disabled);
        Assert.notNull(selectItems, "selectItems");
        _selectItems = selectItems.toArray(new SelectItem[selectItems.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new SelectItem\[selectItems.size()\]'
in `api/src/main/java/jakarta/faces/model/SelectItemGroup.java`
#### Snippet
```java
    {
        Assert.notNull(selectItems, "selectItems");
        setSelectItems(selectItems.toArray(new SelectItem[selectItems.size()]));
    }
}
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new DataModelListener\[_listeners.size()\]'
in `api/src/main/java/jakarta/faces/model/DataModel.java`
#### Snippet
```java
        if (_cachedListenersArray == null)
        {
            _cachedListenersArray = _listeners.toArray(new DataModelListener[_listeners.size()]);
        }
        return _cachedListenersArray;
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new UIComponent\[_map.size()\]'
in `api/src/main/java/jakarta/faces/component/_ComponentFacetMap.java`
#### Snippet
```java
    public void clear()
    {
        UIComponent[] values = _map.values().toArray(new UIComponent[_map.size()]);
        //remove all elements from underlying map
        _map.clear();
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Validator\[_validatorList.size()\]'
in `api/src/main/java/jakarta/faces/component/UIInput.java`
#### Snippet
```java
                return _validatorList == null
                        ? EMPTY_VALIDATOR_ARRAY
                        : _validatorList.toArray(new Validator[_validatorList.size()]);
            }
        }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Validator\[_validatorList.size()\]'
in `api/src/main/java/jakarta/faces/component/UIInput.java`
#### Snippet
```java
            return _validatorList == null
                    ? EMPTY_VALIDATOR_ARRAY
                    : _validatorList.toArray(new Validator[_validatorList.size()]);
        }
    }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Class\[validationGroupsList.size()\]'
in `api/src/main/java/jakarta/faces/validator/BeanValidator.java`
#### Snippet
```java
            }
                    
            this.validationGroupsArray = validationGroupsList.toArray(new Class[validationGroupsList.size()]);
        }
    }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[namespacesSet.size()\]'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/TagAttributesImpl.java`
#### Snippet
```java


        this.namespaces = namespacesSet.toArray(new String[namespacesSet.size()]);
        Arrays.sort(this.namespaces);
        
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new TagAttribute\[value.size()\]'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/TagAttributesImpl.java`
#### Snippet
```java
            String key = entry.getKey();
            List<TagAttribute> value = entry.getValue();
            this.namespaceAttributes.put(key, value.toArray(new TagAttribute[value.size()]));
        }
    }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Metadata\[_mappers.size()\]'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/MetaRulesetImpl.java`
#### Snippet
```java
        else
        {
            return new MetadataImpl(_mappers.toArray(new Metadata[_mappers.size()]));
        }
    }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new ELText\[text.size()\]'
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ELText.java`
#### Snippet
```java
        else
        {
            ELText[] ta = (ELText[]) text.toArray(new ELText[text.size()]);
            return new ELTextComposite(ta);
        }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new ELText\[text.size()\]'
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ELText.java`
#### Snippet
```java
        else
        {
            ELText[] ta = (ELText[]) text.toArray(new ELText[text.size()]);
            return ta;
        }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[names.size()\]'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/RepeatHandler.java`
#### Snippet
```java
            }
            
            _attrs = names.toArray(new String[names.size()]);
        }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new LegacyParamHandler\[params.size()\]'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/LegacyCompositionHandler.java`
#### Snippet
```java
            else
            {
                _params = (LegacyParamHandler[]) params.toArray(new LegacyParamHandler[params.size()]);
            }
        }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new LegacyParamHandler\[params.size()\]'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/LegacyDecorateHandler.java`
#### Snippet
```java
        else
        {
            _params = (LegacyParamHandler[]) params.toArray(new LegacyParamHandler[params.size()]);
        }
    }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new LegacyParamHandler\[params.size()\]'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/LegacyIncludeHandler.java`
#### Snippet
```java
        else
        {
            _params = (LegacyParamHandler[]) params.toArray(new LegacyParamHandler[params.size()]);
        }
    }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new ParamHandler\[params.size()\]'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/CompositionHandler.java`
#### Snippet
```java
            else
            {
                _params = (ParamHandler[]) params.toArray(new ParamHandler[params.size()]);
            }
        }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new ParamHandler\[params.size()\]'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/IncludeHandler.java`
#### Snippet
```java
        else
        {
            _params = (ParamHandler[]) params.toArray(new ParamHandler[params.size()]);
        }
    }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new ParamHandler\[params.size()\]'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/DecorateHandler.java`
#### Snippet
```java
        else
        {
            _params = (ParamHandler[]) params.toArray(new ParamHandler[params.size()]);
        }
    }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new TreeStructComponent\[structChildList.size()\]'
in `impl/src/main/java/org/apache/myfaces/view/facelets/PartialStateManagementStrategy.java`
#### Snippet
```java
            }
            
            TreeStructComponent[] childArray = structChildList.toArray(new TreeStructComponent[structChildList.size()]);
            structComp.setChildren(childArray);
        }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Object\[structFacetList.size()\]'
in `impl/src/main/java/org/apache/myfaces/view/facelets/PartialStateManagementStrategy.java`
#### Snippet
```java
            }
            
            Object[] facetArray = structFacetList.toArray(new Object[structFacetList.size()]);
            structComp.setFacets(facetArray);
        }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new ChooseWhenHandler\[whenList.size()\]'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/ChooseHandler.java`
#### Snippet
```java
            throw new TagException(this.tag, "Choose Tag must have one or more When Tags");
        }
        this.when = (ChooseWhenHandler[]) whenList.toArray(new ChooseWhenHandler[whenList.size()]);

        ArrayList<ChooseOtherwiseHandler> otherwiseList = 
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Metadata\[_mappers.size()\]'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeMetaRulesetImpl.java`
#### Snippet
```java
        else
        {
            return new MetadataImpl(_mappers.toArray(new Metadata[_mappers.size()]));
        }
    }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new PropertyDescriptor\[_propertyDescriptors.size()\]'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeComponentBeanInfo.java`
#### Snippet
```java
            if (_propertyDescriptorsArray == null)
            {
                _propertyDescriptorsArray = _propertyDescriptors.toArray(
                        new PropertyDescriptor[_propertyDescriptors.size()]); 
            }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new PropertyDescriptor\[_propertyDescriptors.size()\]'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeComponentBeanInfo.java`
#### Snippet
```java
            else if (_propertyDescriptorsArray.length != _propertyDescriptors.size())
            {
                _propertyDescriptorsArray = _propertyDescriptors.toArray(
                        new PropertyDescriptor[_propertyDescriptors.size()]);
            }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[attributeNames.size()\]'
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/TemplateContextImpl.java`
#### Snippet
```java
            }
            
            return new ParameterNameEnumeration(attributeNames.toArray(new String[attributeNames.size()]));
        }
    }    
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[attributeNames.size()\]'
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/TemplateContextImpl.java`
#### Snippet
```java
            }
            
            return new ParameterNameEnumeration(attributeNames.toArray(new String[attributeNames.size()]));
        }
    }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Class\[params.size()\]'
in `impl/src/main/java/org/apache/myfaces/view/facelets/util/FaceletsViewDeclarationLanguageUtils.java`
#### Snippet
```java
        }

        return params.toArray(new Class[params.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new TagAttribute\[attrList.size()\]'
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/CompilationManager.java`
#### Snippet
```java
                attrList.add(attr[i]);
            }
            attr = attrList.toArray(new TagAttribute[attrList.size()]);
            return new Tag(tag.getLocation(), tag.getNamespace(), tag.getLocalName(), tag.getQName(),
                           new TagAttributesImpl(attr));
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new TagDecorator\[this.decorators.size()\]'
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/Compiler.java`
#### Snippet
```java
        {
            return new BaseMultipleTagDecorator(new DefaultTagDecorator(), 
                new CompositeTagDecorator(this.decorators.toArray(
                    new TagDecorator[this.decorators.size()])));
        }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new TagLibrary\[this.libraries.size()\]'
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/Compiler.java`
#### Snippet
```java
        if (this.libraries.size() > 0)
        {
            return new CompositeTagLibrary(this.libraries.toArray(new TagLibrary[this.libraries.size()]));
        }
        return EMPTY_LIBRARY;
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new URL\[all.size()\]'
in `impl/src/main/java/org/apache/myfaces/view/facelets/util/Classpath.java`
#### Snippet
```java
        _searchResource(all, loader, prefix + "MANIFEST.MF", prefix, suffix);

        URL[] urlArray = (URL[]) all.toArray(new URL[all.size()]);

        return urlArray;
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[runtimeConfig.getResourceLibraryContracts().size()\]'
in `impl/src/main/java/org/apache/myfaces/config/FacesConfigurator.java`
#### Snippet
```java
        if (runtimeConfig.getContractMappings().isEmpty())
        {
            String[] contracts = runtimeConfig.getResourceLibraryContracts().toArray(
                new String[runtimeConfig.getResourceLibraryContracts().size()]);
            runtimeConfig.addContractMapping("*", contracts);
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[newMappings.size()\]'
in `impl/src/main/java/org/apache/myfaces/webapp/MyFacesContainerInitializer.java`
#### Snippet
```java
                    Set<String> newMappings = new HashSet<>(Arrays.asList(mappings));
                    newMappings.removeAll(conflictMappings);
                    mappings = newMappings.toArray(new String[newMappings.size()]);
                    servlet.addMapping(mappings);
                }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[attributesList.size()\]'
in `impl/src/main/java/org/apache/myfaces/context/servlet/RequestHeaderValuesMap.java`
#### Snippet
```java
            List<String> attributesList = Collections.list(httpServletRequest.getHeaders(key));

            attributes = attributesList.toArray(new String[attributesList.size()]);
            attributeValueCache.put(key, attributes);
        }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Throwable\[throwableList.size()\]'
in `impl/src/main/java/org/apache/myfaces/context/MyFacesExceptionHandlerWrapperImpl.java`
#### Snippet
```java
                {
                    ErrorPageWriter.handle(facesContext, components,
                                           throwableList.toArray(new Throwable[throwableList.size()]));
                }
            }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new PhaseListener\[phaseListeners.size()\]'
in `impl/src/main/java/org/apache/myfaces/lifecycle/LifecycleImpl.java`
#### Snippet
```java
    public PhaseListener[] getPhaseListeners()
    {
        return phaseListeners.toArray(new PhaseListener[phaseListeners.size()]);
    }
    
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Class\[validationGroupsList.size()\]'
in `impl/src/main/java/org/apache/myfaces/component/validate/WholeBeanValidator.java`
#### Snippet
```java
            }
                    
            this.validationGroupsArray = validationGroupsList.toArray(new Class[validationGroupsList.size()]);
        }
    }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[tokens.size()\]'
in `impl/src/main/java/org/apache/myfaces/component/search/SearchExpressionHandlerImpl.java`
#### Snippet
```java
        tokens.add(buffer.toString());

        return tokens.toArray(new String[tokens.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Object\[argsList.size()\]'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlFormatRenderer.java`
#### Snippet
```java
                }

                args = argsList.toArray(new Object[argsList.size()]);
            }
        }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new TreeStructComponent\[structChildList.size()\]'
in `impl/src/main/java/org/apache/myfaces/application/TreeStructureManager.java`
#### Snippet
```java
            }
            
            TreeStructComponent[] childArray = structChildList.toArray(
                    new TreeStructComponent[structChildList.size()]);
            structComp.setChildren(childArray);
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new Object\[structFacetList.size()\]'
in `impl/src/main/java/org/apache/myfaces/application/TreeStructureManager.java`
#### Snippet
```java
            }
            
            Object[] facetArray = structFacetList.toArray(new Object[structFacetList.size()]);
            structComp.setFacets(facetArray);
        }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[mappingsCollection.size()\]'
in `impl/src/main/java/org/apache/myfaces/application/FacesServletMappingUtils.java`
#### Snippet
```java

            Collection<String> mappingsCollection = registration.getMappings();
            mappings = mappingsCollection.toArray(new String[mappingsCollection.size()]);
            if (mappings == null)
            {
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[tokens.size()\]'
in `test/src/main/java/org/apache/myfaces/test/el/ExpressionTokenizer.java`
#### Snippet
```java
            sep = false;
        }
        return tokens.toArray(new String[tokens.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[names.size()\]'
in `test/src/main/java/org/apache/myfaces/test/el/MockMethodExpression.java`
#### Snippet
```java
                    }
                }
                elements = (String[]) names.toArray(new String[names.size()]);
            }
            else
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new ELContextListener\[_elContextListeners.size()\]'
in `impl/src/main/java/org/apache/myfaces/application/ApplicationImpl.java`
#### Snippet
```java
        // I just have to trust that toArray() with do the right thing if the
        // list is changing (not likely)
        return _elContextListeners.toArray(new ELContextListener[_elContextListeners.size()]);
    }

```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new ELContextListener\[elContextListeners.size()\]'
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication12.java`
#### Snippet
```java

        return (ELContextListener[]) elContextListeners
                .toArray(new ELContextListener[elContextListeners.size()]);

    }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new String\[ret.size()\]'
in `test/src/main/java/org/apache/myfaces/test/mock/_RequestHeaderValuesMap.java`
#### Snippet
```java
        }

        return ret.toArray(new String[ret.size()]);
    }
}
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new _ELText\[text.size()\]'
in `test/src/main/java/org/apache/myfaces/test/mock/_ELText.java`
#### Snippet
```java
        else
        {
            _ELText[] ta = (_ELText[]) text.toArray(new _ELText[text.size()]);
            return new ELTextComposite(ta);
        }
```

### ToArrayCallWithZeroLengthArrayArgument
Call to `toArray()` with pre-sized array argument 'new PhaseListener\[phaseListenerList.size()\]'
in `test/src/main/java/org/apache/myfaces/test/mock/lifecycle/MockLifecycle.java`
#### Snippet
```java
    public PhaseListener[] getPhaseListeners()
    {
        return (PhaseListener[]) phaseListenerList.toArray(new PhaseListener[phaseListenerList.size()]);
    }

```

## RuleId[ruleID=Finalize]
### Finalize
'finalize()' should not be overridden
in `impl/src/main/java/org/apache/myfaces/util/lang/ConcurrentLRUCache.java`
#### Snippet
```java

    @Override
    protected void finalize() throws Throwable
    {
        try
```

## RuleId[ruleID=WrapperTypeMayBePrimitive]
### WrapperTypeMayBePrimitive
Type may be primitive
in `impl/src/main/java/org/apache/myfaces/context/flash/FlashImpl.java`
#### Snippet
```java
    private Boolean _convertToBoolean(Object value)
    {
        Boolean booleanValue;
        if (value instanceof Boolean)
        {
```

## RuleId[ruleID=UnnecessaryModifier]
### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/FacesWrapper.java`
#### Snippet
```java
     * @return the instance of the class being wrapped
     */
    public T getWrapped();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/el/CompositeComponentExpressionHolder.java`
#### Snippet
```java
public interface CompositeComponentExpressionHolder
{
    public ValueExpression getExpression(String name);
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/org/apache/myfaces/core/api/shared/lang/PropertyDescriptorUtils.java`
#### Snippet
```java
    public interface ObjCharConsumer<T extends Object>
    {
        public void accept(T t, char i);
    }

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/org/apache/myfaces/core/api/shared/lang/PropertyDescriptorUtils.java`
#### Snippet
```java
    public interface ObjFloatConsumer<T extends Object>
    {
        public void accept(T t, float i);
    }

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/org/apache/myfaces/core/api/shared/lang/PropertyDescriptorUtils.java`
#### Snippet
```java
    public interface ObjByteConsumer<T extends Object>
    {
        public void accept(T t, byte i);
    }

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/org/apache/myfaces/core/api/shared/lang/PropertyDescriptorUtils.java`
#### Snippet
```java
    public interface ObjShortConsumer<T extends Object>
    {
        public void accept(T t, short i);
    }

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/org/apache/myfaces/core/api/shared/lang/PropertyDescriptorUtils.java`
#### Snippet
```java
    public interface ObjBooleanConsumer<T extends Object>
    {
        public void accept(T t, boolean i);
    }
}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/flow/builder/FlowBuilderParameter.java`
#### Snippet
```java
     * @since 4.0
     */
    public static final class Literal extends AnnotationLiteral<FlowBuilderParameter> implements FlowBuilderParameter
    {
        private static final long serialVersionUID = 1L;
```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `api/src/main/java/jakarta/faces/flow/builder/FlowBuilderParameter.java`
#### Snippet
```java
     * @since 4.0
     */
    public static final class Literal extends AnnotationLiteral<FlowBuilderParameter> implements FlowBuilderParameter
    {
        private static final long serialVersionUID = 1L;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/flow/builder/FlowDefinition.java`
#### Snippet
```java
     * @since 4.0
     */
    public static final class Literal extends AnnotationLiteral<FlowDefinition> implements FlowDefinition
    {

```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `api/src/main/java/jakarta/faces/flow/builder/FlowDefinition.java`
#### Snippet
```java
     * @since 4.0
     */
    public static final class Literal extends AnnotationLiteral<FlowDefinition> implements FlowDefinition
    {

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/push/PushContext.java`
#### Snippet
```java
    public static final String ENABLE_WEBSOCKET_ENDPOINT_PARAM_NAME = "jakarta.faces.ENABLE_WEBSOCKET_ENDPOINT";
    
    public static final String WEBSOCKET_ENDPOINT_PORT_PARAM_NAME = "jakarta.faces.WEBSOCKET_ENDPOINT_PORT";
    
    public static final String URI_PREFIX = "/jakarta.faces.push";
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `api/src/main/java/jakarta/faces/push/PushContext.java`
#### Snippet
```java
    public static final String ENABLE_WEBSOCKET_ENDPOINT_PARAM_NAME = "jakarta.faces.ENABLE_WEBSOCKET_ENDPOINT";
    
    public static final String WEBSOCKET_ENDPOINT_PORT_PARAM_NAME = "jakarta.faces.WEBSOCKET_ENDPOINT_PORT";
    
    public static final String URI_PREFIX = "/jakarta.faces.push";
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/view/BehaviorHolderAttachedObjectTarget.java`
#### Snippet
```java
public interface BehaviorHolderAttachedObjectTarget extends AttachedObjectTarget
{
    public boolean isDefaultEvent();
}

```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `api/src/main/java/jakarta/faces/push/PushContext.java`
#### Snippet
```java
    public static final String ENABLE_WEBSOCKET_ENDPOINT_PARAM_NAME = "jakarta.faces.ENABLE_WEBSOCKET_ENDPOINT";
    
    public static final String WEBSOCKET_ENDPOINT_PORT_PARAM_NAME = "jakarta.faces.WEBSOCKET_ENDPOINT_PORT";
    
    public static final String URI_PREFIX = "/jakarta.faces.push";
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/push/PushContext.java`
#### Snippet
```java
    public <S extends Serializable> Set<Future<Void>> send(Object message, S user);  
    
    public <S extends Serializable> Map<S, Set<Future<Void>>> send(Object message, Collection<S> users);
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/push/PushContext.java`
#### Snippet
```java
    public static final String WEBSOCKET_ENDPOINT_PORT_PARAM_NAME = "jakarta.faces.WEBSOCKET_ENDPOINT_PORT";
    
    public static final String URI_PREFIX = "/jakarta.faces.push";
    
    public Set<Future<Void>> send(Object message);
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `api/src/main/java/jakarta/faces/push/PushContext.java`
#### Snippet
```java
    public static final String WEBSOCKET_ENDPOINT_PORT_PARAM_NAME = "jakarta.faces.WEBSOCKET_ENDPOINT_PORT";
    
    public static final String URI_PREFIX = "/jakarta.faces.push";
    
    public Set<Future<Void>> send(Object message);
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `api/src/main/java/jakarta/faces/push/PushContext.java`
#### Snippet
```java
    public static final String WEBSOCKET_ENDPOINT_PORT_PARAM_NAME = "jakarta.faces.WEBSOCKET_ENDPOINT_PORT";
    
    public static final String URI_PREFIX = "/jakarta.faces.push";
    
    public Set<Future<Void>> send(Object message);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/push/PushContext.java`
#### Snippet
```java
    public Set<Future<Void>> send(Object message);
            
    public <S extends Serializable> Set<Future<Void>> send(Object message, S user);  
    
    public <S extends Serializable> Map<S, Set<Future<Void>>> send(Object message, Collection<S> users);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/push/PushContext.java`
#### Snippet
```java
    public static final String URI_PREFIX = "/jakarta.faces.push";
    
    public Set<Future<Void>> send(Object message);
            
    public <S extends Serializable> Set<Future<Void>> send(Object message, S user);  
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/push/PushContext.java`
#### Snippet
```java
{
    
    public static final String ENABLE_WEBSOCKET_ENDPOINT_PARAM_NAME = "jakarta.faces.ENABLE_WEBSOCKET_ENDPOINT";
    
    public static final String WEBSOCKET_ENDPOINT_PORT_PARAM_NAME = "jakarta.faces.WEBSOCKET_ENDPOINT_PORT";
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `api/src/main/java/jakarta/faces/push/PushContext.java`
#### Snippet
```java
{
    
    public static final String ENABLE_WEBSOCKET_ENDPOINT_PARAM_NAME = "jakarta.faces.ENABLE_WEBSOCKET_ENDPOINT";
    
    public static final String WEBSOCKET_ENDPOINT_PORT_PARAM_NAME = "jakarta.faces.WEBSOCKET_ENDPOINT_PORT";
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `api/src/main/java/jakarta/faces/push/PushContext.java`
#### Snippet
```java
{
    
    public static final String ENABLE_WEBSOCKET_ENDPOINT_PARAM_NAME = "jakarta.faces.ENABLE_WEBSOCKET_ENDPOINT";
    
    public static final String WEBSOCKET_ENDPOINT_PORT_PARAM_NAME = "jakarta.faces.WEBSOCKET_ENDPOINT_PORT";
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/push/Push.java`
#### Snippet
```java
public @interface Push
{
    public @Nonbinding String channel() default "";
    
    /*
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/push/Push.java`
#### Snippet
```java
     * @since 4.0
     */
    public static final class Literal extends AnnotationLiteral<Push> implements Push
    {
        private static final long serialVersionUID = 1L;
```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `api/src/main/java/jakarta/faces/push/Push.java`
#### Snippet
```java
     * @since 4.0
     */
    public static final class Literal extends AnnotationLiteral<Push> implements Push
    {
        private static final long serialVersionUID = 1L;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/view/AttachedObjectTarget.java`
#### Snippet
```java
     * a <code>List&lt;AttachedObjectTarget&gt;</code>.
     */
    public static final String ATTACHED_OBJECT_TARGETS_KEY = "jakarta.faces.view.AttachedObjectTargets";

    public String getName();
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `api/src/main/java/jakarta/faces/view/AttachedObjectTarget.java`
#### Snippet
```java
     * a <code>List&lt;AttachedObjectTarget&gt;</code>.
     */
    public static final String ATTACHED_OBJECT_TARGETS_KEY = "jakarta.faces.view.AttachedObjectTargets";

    public String getName();
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `api/src/main/java/jakarta/faces/view/AttachedObjectTarget.java`
#### Snippet
```java
     * a <code>List&lt;AttachedObjectTarget&gt;</code>.
     */
    public static final String ATTACHED_OBJECT_TARGETS_KEY = "jakarta.faces.view.AttachedObjectTargets";

    public String getName();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/view/AttachedObjectTarget.java`
#### Snippet
```java
    public static final String ATTACHED_OBJECT_TARGETS_KEY = "jakarta.faces.view.AttachedObjectTargets";

    public String getName();

    public List<UIComponent> getTargets(UIComponent topLevelComponent);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/view/AttachedObjectTarget.java`
#### Snippet
```java
    public String getName();

    public List<UIComponent> getTargets(UIComponent topLevelComponent);
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/view/AttachedObjectHandler.java`
#### Snippet
```java
     *            The <code>UIComponent</code> to which this particular attached object must be applied.
     */
    public void applyAttachedObject(FacesContext context, UIComponent parent);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/view/AttachedObjectHandler.java`
#### Snippet
```java
     *         <code>AttachedObjectHandler</code>.
     */
    public String getFor();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/view/BehaviorHolderAttachedObjectHandler.java`
#### Snippet
```java
public interface BehaviorHolderAttachedObjectHandler extends AttachedObjectHandler
{
    public String getEventName();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/view/facelets/ComponentConfig.java`
#### Snippet
```java
    public String getComponentType();
    
    public String getRendererType();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/view/facelets/ComponentConfig.java`
#### Snippet
```java
public interface ComponentConfig extends TagConfig
{
    public String getComponentType();
    
    public String getRendererType();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/view/facelets/TagDecorator.java`
#### Snippet
```java
     * @return a decorated tag, otherwise return null
     */
    public Tag decorate(Tag tag);
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/view/facelets/FacetHandler.java`
#### Snippet
```java
public interface FacetHandler
{
    public String getFacetName(FaceletContext ctx);
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/view/facelets/TagConfig.java`
#### Snippet
```java
    public Tag getTag();
    
    public String getTagId();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/view/facelets/TagConfig.java`
#### Snippet
```java
    public FaceletHandler getNextHandler();
    
    public Tag getTag();
    
    public String getTagId();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/view/facelets/TagConfig.java`
#### Snippet
```java
public interface TagConfig
{
    public FaceletHandler getNextHandler();
    
    public Tag getTag();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/view/facelets/ValidatorConfig.java`
#### Snippet
```java
public interface ValidatorConfig extends TagConfig
{
    public String getValidatorId();
}

```

### UnnecessaryModifier
Modifier `static` is redundant for inner interfaces
in `api/src/main/java/jakarta/faces/view/facelets/FaceletCache.java`
#### Snippet
```java
    }
    
    public static interface MemberFactory<V>
    {
        V newInstance(URL key) throws IOException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/view/facelets/FaceletHandler.java`
#### Snippet
```java
     * @throws jakarta.el.ELException
     */
    public void apply(FaceletContext ctx, UIComponent parent)throws IOException;
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/view/facelets/ConverterConfig.java`
#### Snippet
```java
public interface ConverterConfig extends TagConfig
{
    public String getConverterId();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/view/facelets/AttributeHandler.java`
#### Snippet
```java
public interface AttributeHandler
{
    public String getAttributeName(FaceletContext ctx);
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/view/facelets/TextHandler.java`
#### Snippet
```java
    public String getText();
    
    public String getText(FaceletContext ctx);
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/view/facelets/TextHandler.java`
#### Snippet
```java
public interface TextHandler
{
    public String getText();
    
    public String getText(FaceletContext ctx);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/view/facelets/BehaviorConfig.java`
#### Snippet
```java
public interface BehaviorConfig extends TagConfig
{
    public String getBehaviorId();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/event/NamedEvent.java`
#### Snippet
```java
public @interface NamedEvent
{
    public String shortName() default "";
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/event/SystemEventListenerHolder.java`
#### Snippet
```java
public interface SystemEventListenerHolder
{
    public List<SystemEventListener> getListenersForEventClass(Class<? extends SystemEvent> facesEventClass);
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/event/SystemEventListener.java`
#### Snippet
```java
public interface SystemEventListener extends FacesListener
{
    public boolean isListenerForSource(Object source);

    public void processEvent(SystemEvent event) throws AbortProcessingException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/event/SystemEventListener.java`
#### Snippet
```java
    public boolean isListenerForSource(Object source);

    public void processEvent(SystemEvent event) throws AbortProcessingException;
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/event/ComponentSystemEventListener.java`
#### Snippet
```java
public interface ComponentSystemEventListener extends FacesListener
{
    public void processEvent(ComponentSystemEvent event) throws AbortProcessingException;
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/event/ListenersFor.java`
#### Snippet
```java
public @interface ListenersFor
{
    public ListenerFor[] value();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/event/ListenerFor.java`
#### Snippet
```java
public @interface ListenerFor
{
    public Class<? extends SystemEvent> systemEventClass();

    // FIXME: Spec is not Java 5 compliant, specified signature is Class sourceClass()
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/event/ListenerFor.java`
#### Snippet
```java

    // FIXME: Spec is not Java 5 compliant, specified signature is Class sourceClass()
    public Class sourceClass() default Void.class;
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/event/AjaxBehaviorListener.java`
#### Snippet
```java
public interface AjaxBehaviorListener extends BehaviorListener
{
    public void processAjaxBehavior(AjaxBehaviorEvent event) throws AbortProcessingException;
}

```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `api/src/main/java/jakarta/faces/event/ActionListener.java`
#### Snippet
```java
     * @since 2.2
     */
    static final String TO_FLOW_DOCUMENT_ID_ATTR_NAME = "to-flow-document-id";
    
    void processAction(ActionEvent actionEvent) throws AbortProcessingException;
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `api/src/main/java/jakarta/faces/event/ActionListener.java`
#### Snippet
```java
     * @since 2.2
     */
    static final String TO_FLOW_DOCUMENT_ID_ATTR_NAME = "to-flow-document-id";
    
    void processAction(ActionEvent actionEvent) throws AbortProcessingException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/event/ValueChangeListener.java`
#### Snippet
```java
public interface ValueChangeListener extends FacesListener
{
    public void processValueChange(jakarta.faces.event.ValueChangeEvent event) throws AbortProcessingException;
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/event/WebsocketEvent.java`
#### Snippet
```java
         * @since 4.0
         */
        public static final class Literal extends AnnotationLiteral<Opened> implements Opened
        {
            private static final long serialVersionUID = 1L;
```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `api/src/main/java/jakarta/faces/event/WebsocketEvent.java`
#### Snippet
```java
         * @since 4.0
         */
        public static final class Literal extends AnnotationLiteral<Opened> implements Opened
        {
            private static final long serialVersionUID = 1L;
```

### UnnecessaryModifier
Modifier `static` is redundant for inner interfaces
in `api/src/main/java/jakarta/faces/event/WebsocketEvent.java`
#### Snippet
```java
    @Retention(RetentionPolicy.RUNTIME)
    @Documented
    public static @interface Closed 
    {
        /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/event/WebsocketEvent.java`
#### Snippet
```java
         * @since 4.0
         */
        public static final class Literal extends AnnotationLiteral<Closed> implements Closed
        {
            private static final long serialVersionUID = 1L;
```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `api/src/main/java/jakarta/faces/event/WebsocketEvent.java`
#### Snippet
```java
         * @since 4.0
         */
        public static final class Literal extends AnnotationLiteral<Closed> implements Closed
        {
            private static final long serialVersionUID = 1L;
```

### UnnecessaryModifier
Modifier `static` is redundant for inner interfaces
in `api/src/main/java/jakarta/faces/event/WebsocketEvent.java`
#### Snippet
```java
    @Retention(RetentionPolicy.RUNTIME)
    @Documented
    public static @interface Opened 
    {
        /*
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/model/FacesDataModel.java`
#### Snippet
```java
     * @since 4.0
     */
    public static final class Literal extends AnnotationLiteral<FacesDataModel> implements FacesDataModel
    {
        private static final long serialVersionUID = 1L;
```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `api/src/main/java/jakarta/faces/model/FacesDataModel.java`
#### Snippet
```java
     * @since 4.0
     */
    public static final class Literal extends AnnotationLiteral<FacesDataModel> implements FacesDataModel
    {
        private static final long serialVersionUID = 1L;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/model/FacesDataModel.java`
#### Snippet
```java
public @interface FacesDataModel
{
    public java.lang.Class<?> forClass() default Object.class;

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/render/FacesBehaviorRenderer.java`
#### Snippet
```java
     * {@link RenderKit#getRenderer(java.lang.String, java.lang.String)}.
     */
    public String rendererType();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/render/FacesBehaviorRenderer.java`
#### Snippet
```java
     * of {@link Renderer} must be installed.
     */
    public String renderKitId() default RenderKitFactory.HTML_BASIC_RENDER_KIT;
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/render/FacesRenderer.java`
#### Snippet
```java
     * {@link RenderKit#getRenderer(java.lang.String, java.lang.String)}.
     */
    public String rendererType();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/render/FacesRenderer.java`
#### Snippet
```java
     * of {@link Renderer} must be installed.
     */
    public String renderKitId() default RenderKitFactory.HTML_BASIC_RENDER_KIT;
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/render/FacesRenderer.java`
#### Snippet
```java
     * {@link RenderKit#getRenderer(java.lang.String, java.lang.String)}.
     */
    public String componentFamily();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/convert/Converter.java`
#### Snippet
```java
     */
    @JSFWebConfigParam(defaultValue="false", expectedValues="true, false", since="2.0", group="validation")
    public static final String DATETIMECONVERTER_DEFAULT_TIMEZONE_IS_SYSTEM_TIMEZONE_PARAM_NAME =
            "jakarta.faces.DATETIMECONVERTER_DEFAULT_TIMEZONE_IS_SYSTEM_TIMEZONE";
    
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `api/src/main/java/jakarta/faces/convert/Converter.java`
#### Snippet
```java
     */
    @JSFWebConfigParam(defaultValue="false", expectedValues="true, false", since="2.0", group="validation")
    public static final String DATETIMECONVERTER_DEFAULT_TIMEZONE_IS_SYSTEM_TIMEZONE_PARAM_NAME =
            "jakarta.faces.DATETIMECONVERTER_DEFAULT_TIMEZONE_IS_SYSTEM_TIMEZONE";
    
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `api/src/main/java/jakarta/faces/convert/Converter.java`
#### Snippet
```java
     */
    @JSFWebConfigParam(defaultValue="false", expectedValues="true, false", since="2.0", group="validation")
    public static final String DATETIMECONVERTER_DEFAULT_TIMEZONE_IS_SYSTEM_TIMEZONE_PARAM_NAME =
            "jakarta.faces.DATETIMECONVERTER_DEFAULT_TIMEZONE_IS_SYSTEM_TIMEZONE";
    
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/convert/FacesConverter.java`
#### Snippet
```java
     * @return
     */
    public String value() default "";
    
    public boolean managed() default false;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/convert/FacesConverter.java`
#### Snippet
```java
    public String value() default "";
    
    public boolean managed() default false;

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/convert/FacesConverter.java`
#### Snippet
```java
     * @since 4.0
     */
    public static final class Literal extends AnnotationLiteral<FacesConverter> implements FacesConverter
    {
        private static final long serialVersionUID = 1L;
```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `api/src/main/java/jakarta/faces/convert/FacesConverter.java`
#### Snippet
```java
     * @since 4.0
     */
    public static final class Literal extends AnnotationLiteral<FacesConverter> implements FacesConverter
    {
        private static final long serialVersionUID = 1L;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/convert/FacesConverter.java`
#### Snippet
```java
     * @return The class this converter must be registered for.
     */
    public Class forClass() default Object.class;

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/NamingContainer.java`
#### Snippet
```java
     * </p>
     */
    public static final char SEPARATOR_CHAR = ':';
}

```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `api/src/main/java/jakarta/faces/component/NamingContainer.java`
#### Snippet
```java
     * </p>
     */
    public static final char SEPARATOR_CHAR = ':';
}

```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `api/src/main/java/jakarta/faces/component/NamingContainer.java`
#### Snippet
```java
     * </p>
     */
    public static final char SEPARATOR_CHAR = ':';
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/TransientStateHelper.java`
#### Snippet
```java
     * @since 2.1
     */
    public Object getTransient(Object key);
    
    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/TransientStateHelper.java`
#### Snippet
```java
     * @since 2.1
     */
    public Object putTransient(Object key, Object value);
    
}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/TransientStateHelper.java`
#### Snippet
```java
     * @since 2.1
     */
    public Object getTransient(Object key, Object defaultValue);
    
    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/ActionSource2.java`
#### Snippet
```java
    public MethodExpression getActionExpression();
    
    public void setActionExpression(MethodExpression action);
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/ActionSource2.java`
#### Snippet
```java
{
    
    public MethodExpression getActionExpression();
    
    public void setActionExpression(MethodExpression action);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/StateHolder.java`
#### Snippet
```java
    public boolean isTransient();

    public void setTransient(boolean newTransientValue);
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/StateHolder.java`
#### Snippet
```java
    public java.lang.Object saveState(jakarta.faces.context.FacesContext context);

    public void restoreState(jakarta.faces.context.FacesContext context,
                             java.lang.Object state);

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/StateHolder.java`
#### Snippet
```java
public interface StateHolder
{
    public java.lang.Object saveState(jakarta.faces.context.FacesContext context);

    public void restoreState(jakarta.faces.context.FacesContext context,
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/StateHolder.java`
#### Snippet
```java
                             java.lang.Object state);

    public boolean isTransient();

    public void setTransient(boolean newTransientValue);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/FacesComponent.java`
#### Snippet
```java
public @interface FacesComponent
{
    public static final String NAMESPACE = "jakarta.faces.component";
        
    public String value() default "";
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `api/src/main/java/jakarta/faces/component/FacesComponent.java`
#### Snippet
```java
public @interface FacesComponent
{
    public static final String NAMESPACE = "jakarta.faces.component";
        
    public String value() default "";
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `api/src/main/java/jakarta/faces/component/FacesComponent.java`
#### Snippet
```java
public @interface FacesComponent
{
    public static final String NAMESPACE = "jakarta.faces.component";
        
    public String value() default "";
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/FacesComponent.java`
#### Snippet
```java
    public static final String NAMESPACE = "jakarta.faces.component";
        
    public String value() default "";
    
    public boolean createTag() default false;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/FacesComponent.java`
#### Snippet
```java
    public String tagName() default "";
    
    public String namespace() default NAMESPACE;
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/FacesComponent.java`
#### Snippet
```java
    public boolean createTag() default false;
    
    public String tagName() default "";
    
    public String namespace() default NAMESPACE;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/FacesComponent.java`
#### Snippet
```java
    public String value() default "";
    
    public boolean createTag() default false;
    
    public String tagName() default "";
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/EditableValueHolder.java`
#### Snippet
```java
     * must then check for that.
     */
    public boolean isRequired();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/EditableValueHolder.java`
#### Snippet
```java
     * whose validation has not been executed.
     */
    public boolean isValid();

    public void setValid(boolean valid);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/EditableValueHolder.java`
#### Snippet
```java
    public void addValidator(Validator validator);

    public Validator[] getValidators();

    public void removeValidator(Validator validator);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/EditableValueHolder.java`
#### Snippet
```java
    public void addValueChangeListener(ValueChangeListener listener);

    public ValueChangeListener[] getValueChangeListeners();

    public void removeValueChangeListener(ValueChangeListener listener);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/EditableValueHolder.java`
#### Snippet
```java
    public void removeValidator(Validator validator);

    public void addValueChangeListener(ValueChangeListener listener);

    public ValueChangeListener[] getValueChangeListeners();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/EditableValueHolder.java`
#### Snippet
```java
     * via the value-binding named "value".
     */
    public void setLocalValueSet(boolean localValueSet);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/EditableValueHolder.java`
#### Snippet
```java
     * navigation) without performing validation on any following components.
     */
    public boolean isImmediate();

    public void setImmediate(boolean immediate);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/EditableValueHolder.java`
#### Snippet
```java
     * local "value" property of this component, and the submitted value is reset to null.
     */
    public Object getSubmittedValue();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/EditableValueHolder.java`
#### Snippet
```java
    public void setImmediate(boolean immediate);

    public void addValidator(Validator validator);

    public Validator[] getValidators();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/EditableValueHolder.java`
#### Snippet
```java
     * @since 2.0
     */
    public void resetValue();

}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/EditableValueHolder.java`
#### Snippet
```java
     * object <i>must</i> be able to represent all possible user input values, not just valid ones.
     */
    public void setSubmittedValue(Object submittedValue);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/EditableValueHolder.java`
#### Snippet
```java
    public Validator[] getValidators();

    public void removeValidator(Validator validator);

    public void addValueChangeListener(ValueChangeListener listener);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/EditableValueHolder.java`
#### Snippet
```java
    public ValueChangeListener[] getValueChangeListeners();

    public void removeValueChangeListener(ValueChangeListener listener);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/EditableValueHolder.java`
#### Snippet
```java
    public boolean isImmediate();

    public void setImmediate(boolean immediate);

    public void addValidator(Validator validator);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/EditableValueHolder.java`
#### Snippet
```java
     * selected for this component.
     */
    public void setRequired(boolean required);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/EditableValueHolder.java`
#### Snippet
```java
    public boolean isValid();

    public void setValid(boolean valid);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/EditableValueHolder.java`
#### Snippet
```java
     * value local <i>member</i> and the value <i>property</i> (which may involve a value-binding to the user model).
     */
    public boolean isLocalValueSet();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/StateHelper.java`
#### Snippet
```java
     * @since 4.0
     */
    public Object eval(Serializable key, Supplier<Object> defaultValueSupplier);

    public Object get(Serializable key);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/StateHelper.java`
#### Snippet
```java
    public void add(Serializable key, Object value);

    public Object eval(Serializable key);

    public Object eval(Serializable key, Object defaultValue);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/StateHelper.java`
#### Snippet
```java
    public Object eval(Serializable key, Supplier<Object> defaultValueSupplier);

    public Object get(Serializable key);

    public Object put(Serializable key, Object value);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/StateHelper.java`
#### Snippet
```java
    public Object get(Serializable key);

    public Object put(Serializable key, Object value);

    public Object put(Serializable key, String mapKey, Object value);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/StateHelper.java`
#### Snippet
```java
public interface StateHelper extends StateHolder
{
    public void add(Serializable key, Object value);

    public Object eval(Serializable key);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/StateHelper.java`
#### Snippet
```java
    public Object remove(Serializable key);

    public Object remove(Serializable key, Object valueOrKey);
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/StateHelper.java`
#### Snippet
```java
    public Object put(Serializable key, String mapKey, Object value);

    public Object remove(Serializable key);

    public Object remove(Serializable key, Object valueOrKey);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/StateHelper.java`
#### Snippet
```java
    public Object eval(Serializable key);

    public Object eval(Serializable key, Object defaultValue);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/StateHelper.java`
#### Snippet
```java
    public Object put(Serializable key, Object value);

    public Object put(Serializable key, String mapKey, Object value);

    public Object remove(Serializable key);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/ValueHolder.java`
#### Snippet
```java
    public void setValue(Object value);

    public Converter getConverter();

    public void setConverter(Converter converter);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/ValueHolder.java`
#### Snippet
```java
public interface ValueHolder
{
    public Object getLocalValue();

    public Object getValue();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/ValueHolder.java`
#### Snippet
```java
    public Object getLocalValue();

    public Object getValue();

    public void setValue(Object value);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/ValueHolder.java`
#### Snippet
```java
    public Converter getConverter();

    public void setConverter(Converter converter);
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/ValueHolder.java`
#### Snippet
```java
    public Object getValue();

    public void setValue(Object value);

    public Converter getConverter();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/TransientStateHolder.java`
#### Snippet
```java
     * @since 2.1
     */
    public void restoreTransientState(jakarta.faces.context.FacesContext context,
                             java.lang.Object state);

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/TransientStateHolder.java`
#### Snippet
```java
     * @since 2.1
     */
    public java.lang.Object saveTransientState(jakarta.faces.context.FacesContext context);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/UniqueIdVendor.java`
#### Snippet
```java
public interface UniqueIdVendor
{
    public String createUniqueId(FacesContext context, String seed);
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/ActionSource.java`
#### Snippet
```java
    public boolean isImmediate();

    public void setImmediate(boolean immediate);

    public void addActionListener(jakarta.faces.event.ActionListener listener);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/ActionSource.java`
#### Snippet
```java
public interface ActionSource
{
    public boolean isImmediate();

    public void setImmediate(boolean immediate);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/ActionSource.java`
#### Snippet
```java
    public jakarta.faces.event.ActionListener[] getActionListeners();

    public void removeActionListener(jakarta.faces.event.ActionListener listener);
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/ActionSource.java`
#### Snippet
```java
    public void addActionListener(jakarta.faces.event.ActionListener listener);

    public jakarta.faces.event.ActionListener[] getActionListeners();

    public void removeActionListener(jakarta.faces.event.ActionListener listener);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/ActionSource.java`
#### Snippet
```java
    public void setImmediate(boolean immediate);

    public void addActionListener(jakarta.faces.event.ActionListener listener);

    public jakarta.faces.event.ActionListener[] getActionListeners();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/PartialStateHolder.java`
#### Snippet
```java
    public void clearInitialState();
    
    public boolean initialStateMarked();
    
    public void markInitialState();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/PartialStateHolder.java`
#### Snippet
```java
public interface PartialStateHolder extends StateHolder
{
    public void clearInitialState();
    
    public boolean initialStateMarked();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/PartialStateHolder.java`
#### Snippet
```java
    public boolean initialStateMarked();
    
    public void markInitialState();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/html/_EventProperties.java`
#### Snippet
```java
     */
    @JSFProperty(clientEvent="mouseout")
    public String getOnmouseout();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/html/_EventProperties.java`
#### Snippet
```java
     */
    @JSFProperty(clientEvent="mousedown")
    public String getOnmousedown();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/html/_EventProperties.java`
#### Snippet
```java
     */
    @JSFProperty(clientEvent="keydown")
    public String getOnkeydown();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/html/_EventProperties.java`
#### Snippet
```java
     */
    @JSFProperty(clientEvent="keypress")
    public String getOnkeypress();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/html/_EventProperties.java`
#### Snippet
```java
     */
    @JSFProperty(clientEvent="dblclick")
    public String getOndblclick();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/html/_EventProperties.java`
#### Snippet
```java
     */
    @JSFProperty(clientEvent="click")
    public String getOnclick();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/html/_EventProperties.java`
#### Snippet
```java
     */
    @JSFProperty(clientEvent="mouseover")
    public String getOnmouseover();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/html/_EventProperties.java`
#### Snippet
```java
     */
    @JSFProperty(clientEvent="mousemove")
    public String getOnmousemove();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/html/_EventProperties.java`
#### Snippet
```java
     */
    @JSFProperty(clientEvent="keyup")
    public String getOnkeyup();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/html/_EventProperties.java`
#### Snippet
```java
     */
    @JSFProperty(clientEvent="mouseup")
    public String getOnmouseup();

}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/html/_UniversalProperties.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getLang();

    /**
```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `api/src/main/java/jakarta/faces/component/html/_UniversalProperties.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getLang();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/html/_UniversalProperties.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getTitle();


```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `api/src/main/java/jakarta/faces/component/html/_UniversalProperties.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getTitle();


```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/html/_UniversalProperties.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getDir();

    /**
```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `api/src/main/java/jakarta/faces/component/html/_UniversalProperties.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getDir();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/html/_ChangeProperty.java`
#### Snippet
```java
     */
    @JSFProperty(clientEvent="change")
    public abstract String getOnchange();

}
```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `api/src/main/java/jakarta/faces/component/html/_ChangeProperty.java`
#### Snippet
```java
     */
    @JSFProperty(clientEvent="change")
    public abstract String getOnchange();

}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/html/_DisabledClassEnabledClassProperties.java`
#### Snippet
```java
     */
    @JSFProperty
    public String getEnabledClass();

}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/html/_DisabledClassEnabledClassProperties.java`
#### Snippet
```java
     */
    @JSFProperty
    public String getDisabledClass();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/html/_TypeProperty.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getType();

}
```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `api/src/main/java/jakarta/faces/component/html/_TypeProperty.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getType();

}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/html/_StyleProperties.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getStyleClass();

}
```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `api/src/main/java/jakarta/faces/component/html/_StyleProperties.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getStyleClass();

}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/html/_StyleProperties.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getStyle();

    /**
```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `api/src/main/java/jakarta/faces/component/html/_StyleProperties.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getStyle();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/html/_DisabledReadonlyProperties.java`
#### Snippet
```java
     */
    @JSFProperty(defaultValue="false")
    public abstract boolean isReadonly();
}

```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `api/src/main/java/jakarta/faces/component/html/_DisabledReadonlyProperties.java`
#### Snippet
```java
     */
    @JSFProperty(defaultValue="false")
    public abstract boolean isReadonly();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/html/_DisabledReadonlyProperties.java`
#### Snippet
```java
     */
    @JSFProperty(defaultValue="false")
    public abstract boolean isDisabled();

    /**
```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `api/src/main/java/jakarta/faces/component/html/_DisabledReadonlyProperties.java`
#### Snippet
```java
     */
    @JSFProperty(defaultValue="false")
    public abstract boolean isDisabled();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/html/_RoleProperty.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getRole();
}

```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `api/src/main/java/jakarta/faces/component/html/_RoleProperty.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getRole();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/html/_LinkProperties.java`
#### Snippet
```java
    @JSFProperty
    (defaultValue = "false")
    public abstract boolean isDisabled();

}
```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `api/src/main/java/jakarta/faces/component/html/_LinkProperties.java`
#### Snippet
```java
    @JSFProperty
    (defaultValue = "false")
    public abstract boolean isDisabled();

}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/html/_LinkProperties.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getRel();
    
    /**
```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `api/src/main/java/jakarta/faces/component/html/_LinkProperties.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getRel();
    
    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/html/_LinkProperties.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getHreflang();
    
    /**
```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `api/src/main/java/jakarta/faces/component/html/_LinkProperties.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getHreflang();
    
    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/html/_EscapeProperty.java`
#### Snippet
```java
     */
    @JSFProperty(defaultValue="true")    
    public boolean isEscape();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/html/_LinkProperties.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getShape();
    
    /**
```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `api/src/main/java/jakarta/faces/component/html/_LinkProperties.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getShape();
    
    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/html/_LinkProperties.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getType();

    /**
```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `api/src/main/java/jakarta/faces/component/html/_LinkProperties.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getType();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/html/_LinkProperties.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getTarget();
    
    /**
```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `api/src/main/java/jakarta/faces/component/html/_LinkProperties.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getTarget();
    
    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/html/_LinkProperties.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getCoords();
    
    /**
```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `api/src/main/java/jakarta/faces/component/html/_LinkProperties.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getCoords();
    
    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/html/_LinkProperties.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getCharset();
    
    /**
```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `api/src/main/java/jakarta/faces/component/html/_LinkProperties.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getCharset();
    
    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/html/_LinkProperties.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getRev();
    
    /**
```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `api/src/main/java/jakarta/faces/component/html/_LinkProperties.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getRev();
    
    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/html/_FocusBlurProperties.java`
#### Snippet
```java
     */
    @JSFProperty(clientEvent="focus")
    public String getOnfocus();
    
}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/html/_FocusBlurProperties.java`
#### Snippet
```java
     */
    @JSFProperty(clientEvent="blur")
    public String getOnblur();
    
    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/html/_SelectProperty.java`
#### Snippet
```java
     */
    @JSFProperty(clientEvent="select")
    public abstract String getOnselect();

}
```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `api/src/main/java/jakarta/faces/component/html/_SelectProperty.java`
#### Snippet
```java
     */
    @JSFProperty(clientEvent="select")
    public abstract String getOnselect();

}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/html/_AccesskeyProperty.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getAccesskey();

}
```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `api/src/main/java/jakarta/faces/component/html/_AccesskeyProperty.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getAccesskey();

}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/html/_AltProperty.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getAlt();
}

```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `api/src/main/java/jakarta/faces/component/html/_AltProperty.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getAlt();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/html/_LabelProperty.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getLabel();
}

```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `api/src/main/java/jakarta/faces/component/html/_LabelProperty.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getLabel();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/html/_TabindexProperty.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getTabindex();

}
```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `api/src/main/java/jakarta/faces/component/html/_TabindexProperty.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getTabindex();

}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/html/_MessageProperties.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getWarnStyle();

}
```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `api/src/main/java/jakarta/faces/component/html/_MessageProperties.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getWarnStyle();

}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/html/_MessageProperties.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getFatalClass();

    /**
```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `api/src/main/java/jakarta/faces/component/html/_MessageProperties.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getFatalClass();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/html/_MessageProperties.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getFatalStyle();

    /**
```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `api/src/main/java/jakarta/faces/component/html/_MessageProperties.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getFatalStyle();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/html/_MessageProperties.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getErrorClass();

    /**
```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `api/src/main/java/jakarta/faces/component/html/_MessageProperties.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getErrorClass();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/html/_MessageProperties.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getErrorStyle();

    /**
```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `api/src/main/java/jakarta/faces/component/html/_MessageProperties.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getErrorStyle();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/html/_MessageProperties.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getWarnClass();

    /**
```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `api/src/main/java/jakarta/faces/component/html/_MessageProperties.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getWarnClass();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/html/_MessageProperties.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getInfoClass();

    /**
```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `api/src/main/java/jakarta/faces/component/html/_MessageProperties.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getInfoClass();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/html/_MessageProperties.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getInfoStyle();

    /**
```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `api/src/main/java/jakarta/faces/component/html/_MessageProperties.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getInfoStyle();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/html/_MessageProperties.java`
#### Snippet
```java
     */
    @JSFProperty(defaultValue="false")
    public abstract boolean isTooltip();
    
    /**
```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `api/src/main/java/jakarta/faces/component/html/_MessageProperties.java`
#### Snippet
```java
     */
    @JSFProperty(defaultValue="false")
    public abstract boolean isTooltip();
    
    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/html/_TitleProperty.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getTitle();
}

```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `api/src/main/java/jakarta/faces/component/html/_TitleProperty.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getTitle();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/visit/VisitCallback.java`
#### Snippet
```java
     *   subtree or end the visit.
     */
    public VisitResult visit(VisitContext context, UIComponent target);
}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/behavior/Behavior.java`
#### Snippet
```java
public interface Behavior
{
    public void broadcast(BehaviorEvent event);
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/behavior/ClientBehavior.java`
#### Snippet
```java
    public void decode(FacesContext context, UIComponent component);
    
    public Set<ClientBehaviorHint> getHints();
    
    public String getScript(ClientBehaviorContext behaviorContext);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/behavior/ClientBehavior.java`
#### Snippet
```java
public interface ClientBehavior extends Behavior
{
    public void decode(FacesContext context, UIComponent component);
    
    public Set<ClientBehaviorHint> getHints();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/behavior/ClientBehavior.java`
#### Snippet
```java
    public Set<ClientBehaviorHint> getHints();
    
    public String getScript(ClientBehaviorContext behaviorContext);
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/behavior/ClientBehaviorHolder.java`
#### Snippet
```java
    public Map<String,List<ClientBehavior>> getClientBehaviors();
    
    public String getDefaultEventName();
    
    public Collection<String> getEventNames();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/behavior/ClientBehaviorHolder.java`
#### Snippet
```java
public interface ClientBehaviorHolder
{
    public void addClientBehavior(String eventName, ClientBehavior behavior);
    
    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/behavior/ClientBehaviorHolder.java`
#### Snippet
```java
    public String getDefaultEventName();
    
    public Collection<String> getEventNames();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/behavior/ClientBehaviorHolder.java`
#### Snippet
```java
     * @return
     */
    public Map<String,List<ClientBehavior>> getClientBehaviors();
    
    public String getDefaultEventName();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/behavior/FacesBehavior.java`
#### Snippet
```java
    public String value();
    
    public boolean managed() default false;

    /*
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/behavior/FacesBehavior.java`
#### Snippet
```java
     * @since 4.0
     */
    public static final class Literal extends AnnotationLiteral<FacesBehavior> implements FacesBehavior
    {
        private static final long serialVersionUID = 1L;
```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `api/src/main/java/jakarta/faces/component/behavior/FacesBehavior.java`
#### Snippet
```java
     * @since 4.0
     */
    public static final class Literal extends AnnotationLiteral<FacesBehavior> implements FacesBehavior
    {
        private static final long serialVersionUID = 1L;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/behavior/FacesBehavior.java`
#### Snippet
```java
public @interface FacesBehavior
{
    public String value();
    
    public boolean managed() default false;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/validator/Validator.java`
#### Snippet
```java
{

    public void validate(FacesContext context, UIComponent component, T value) throws ValidatorException;
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/validator/FacesValidator.java`
#### Snippet
```java
     * {@link jakarta.faces.application.Application#createValidator(java.lang.String)}.
     */
    public String value() default "";
    
    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/validator/FacesValidator.java`
#### Snippet
```java
     * @since 4.0
     */
    public static final class Literal extends AnnotationLiteral<FacesValidator> implements FacesValidator
    {
        private static final long serialVersionUID = 1L;
```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `api/src/main/java/jakarta/faces/validator/FacesValidator.java`
#### Snippet
```java
     * @since 4.0
     */
    public static final class Literal extends AnnotationLiteral<FacesValidator> implements FacesValidator
    {
        private static final long serialVersionUID = 1L;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/validator/FacesValidator.java`
#### Snippet
```java
    boolean isDefault() default false;
    
    public boolean managed() default false;

    /*
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/annotation/SessionMap.java`
#### Snippet
```java
public @interface SessionMap
{
    public static final class Literal extends AnnotationLiteral<SessionMap> implements SessionMap
    {
        private static final long serialVersionUID = 1L;
```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `api/src/main/java/jakarta/faces/annotation/SessionMap.java`
#### Snippet
```java
public @interface SessionMap
{
    public static final class Literal extends AnnotationLiteral<SessionMap> implements SessionMap
    {
        private static final long serialVersionUID = 1L;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/annotation/HeaderValuesMap.java`
#### Snippet
```java
public @interface HeaderValuesMap
{
    public static final class Literal extends AnnotationLiteral<HeaderValuesMap> implements HeaderValuesMap
    {
        private static final long serialVersionUID = 1L;
```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `api/src/main/java/jakarta/faces/annotation/HeaderValuesMap.java`
#### Snippet
```java
public @interface HeaderValuesMap
{
    public static final class Literal extends AnnotationLiteral<HeaderValuesMap> implements HeaderValuesMap
    {
        private static final long serialVersionUID = 1L;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/annotation/FlowMap.java`
#### Snippet
```java
public @interface FlowMap
{
    public static final class Literal extends AnnotationLiteral<FlowMap> implements FlowMap
    {
        private static final long serialVersionUID = 1L;
```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `api/src/main/java/jakarta/faces/annotation/FlowMap.java`
#### Snippet
```java
public @interface FlowMap
{
    public static final class Literal extends AnnotationLiteral<FlowMap> implements FlowMap
    {
        private static final long serialVersionUID = 1L;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/annotation/ViewMap.java`
#### Snippet
```java
public @interface ViewMap
{
    public static final class Literal extends AnnotationLiteral<ViewMap> implements ViewMap
    {
        private static final long serialVersionUID = 1L;
```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `api/src/main/java/jakarta/faces/annotation/ViewMap.java`
#### Snippet
```java
public @interface ViewMap
{
    public static final class Literal extends AnnotationLiteral<ViewMap> implements ViewMap
    {
        private static final long serialVersionUID = 1L;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/annotation/InitParameterMap.java`
#### Snippet
```java
public @interface InitParameterMap
{
    public static final class Literal extends AnnotationLiteral<InitParameterMap> implements InitParameterMap
    {
        private static final long serialVersionUID = 1L;
```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `api/src/main/java/jakarta/faces/annotation/InitParameterMap.java`
#### Snippet
```java
public @interface InitParameterMap
{
    public static final class Literal extends AnnotationLiteral<InitParameterMap> implements InitParameterMap
    {
        private static final long serialVersionUID = 1L;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/annotation/ManagedProperty.java`
#### Snippet
```java
    

    public static final class Literal extends AnnotationLiteral<ManagedProperty> implements ManagedProperty
    {
        private static final long serialVersionUID = 1L;
```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `api/src/main/java/jakarta/faces/annotation/ManagedProperty.java`
#### Snippet
```java
    

    public static final class Literal extends AnnotationLiteral<ManagedProperty> implements ManagedProperty
    {
        private static final long serialVersionUID = 1L;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/annotation/ManagedProperty.java`
#### Snippet
```java
public @interface ManagedProperty
{
    public String value();
    

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/annotation/RequestParameterValuesMap.java`
#### Snippet
```java
public @interface RequestParameterValuesMap
{
    public static final class Literal extends AnnotationLiteral<RequestParameterValuesMap>
            implements RequestParameterValuesMap
    {
```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `api/src/main/java/jakarta/faces/annotation/RequestParameterValuesMap.java`
#### Snippet
```java
public @interface RequestParameterValuesMap
{
    public static final class Literal extends AnnotationLiteral<RequestParameterValuesMap>
            implements RequestParameterValuesMap
    {
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/annotation/ApplicationMap.java`
#### Snippet
```java
public @interface ApplicationMap
{
    public static final class Literal extends AnnotationLiteral<ApplicationMap> implements ApplicationMap
    {
        private static final long serialVersionUID = 1L;
```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `api/src/main/java/jakarta/faces/annotation/ApplicationMap.java`
#### Snippet
```java
public @interface ApplicationMap
{
    public static final class Literal extends AnnotationLiteral<ApplicationMap> implements ApplicationMap
    {
        private static final long serialVersionUID = 1L;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/annotation/RequestParameterMap.java`
#### Snippet
```java
public @interface RequestParameterMap
{
    public static final class Literal extends AnnotationLiteral<RequestParameterMap> implements RequestParameterMap
    {
        private static final long serialVersionUID = 1L;
```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `api/src/main/java/jakarta/faces/annotation/RequestParameterMap.java`
#### Snippet
```java
public @interface RequestParameterMap
{
    public static final class Literal extends AnnotationLiteral<RequestParameterMap> implements RequestParameterMap
    {
        private static final long serialVersionUID = 1L;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/annotation/HeaderMap.java`
#### Snippet
```java
public @interface HeaderMap
{
    public static final class Literal extends AnnotationLiteral<HeaderMap> implements HeaderMap
    {
        private static final long serialVersionUID = 1L;
```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `api/src/main/java/jakarta/faces/annotation/HeaderMap.java`
#### Snippet
```java
public @interface HeaderMap
{
    public static final class Literal extends AnnotationLiteral<HeaderMap> implements HeaderMap
    {
        private static final long serialVersionUID = 1L;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/annotation/RequestMap.java`
#### Snippet
```java
public @interface RequestMap
{
    public static final class Literal extends AnnotationLiteral<RequestMap> implements RequestMap
    {
        private static final long serialVersionUID = 1L;
```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `api/src/main/java/jakarta/faces/annotation/RequestMap.java`
#### Snippet
```java
public @interface RequestMap
{
    public static final class Literal extends AnnotationLiteral<RequestMap> implements RequestMap
    {
        private static final long serialVersionUID = 1L;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/annotation/RequestCookieMap.java`
#### Snippet
```java
public @interface RequestCookieMap
{
    public static final class Literal extends AnnotationLiteral<RequestCookieMap> implements RequestCookieMap
    {
        private static final long serialVersionUID = 1L;
```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `api/src/main/java/jakarta/faces/annotation/RequestCookieMap.java`
#### Snippet
```java
public @interface RequestCookieMap
{
    public static final class Literal extends AnnotationLiteral<RequestCookieMap> implements RequestCookieMap
    {
        private static final long serialVersionUID = 1L;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/annotation/View.java`
#### Snippet
```java
     *
     */
    public final static class Literal extends AnnotationLiteral<View> implements View
    {
        public static final Literal INSTANCE = of("");
```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `api/src/main/java/jakarta/faces/annotation/View.java`
#### Snippet
```java
     *
     */
    public final static class Literal extends AnnotationLiteral<View> implements View
    {
        public static final Literal INSTANCE = of("");
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/annotation/FacesConfig.java`
#### Snippet
```java

    @Deprecated(forRemoval = true, since = "4.0")
    public static enum Version 
    {
        JSF_2_3
```

### UnnecessaryModifier
Modifier `static` is redundant for inner enums
in `api/src/main/java/jakarta/faces/annotation/FacesConfig.java`
#### Snippet
```java

    @Deprecated(forRemoval = true, since = "4.0")
    public static enum Version 
    {
        JSF_2_3
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/annotation/FacesConfig.java`
#### Snippet
```java
    }
    
    public static final class Literal extends AnnotationLiteral<FacesConfig> implements FacesConfig
    {
        private static final long serialVersionUID = 1L;
```

### UnnecessaryModifier
Modifier `static` is redundant for inner classes of interfaces
in `api/src/main/java/jakarta/faces/annotation/FacesConfig.java`
#### Snippet
```java
    }
    
    public static final class Literal extends AnnotationLiteral<FacesConfig> implements FacesConfig
    {
        private static final long serialVersionUID = 1L;
```

### UnnecessaryModifier
Modifier `static` is redundant for inner interfaces
in `api/src/main/java/jakarta/faces/component/UIViewRoot.java`
#### Snippet
```java
    }

    private static interface PhaseProcessor
    {
        public void process(FacesContext context, UIViewRoot root);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/component/UIViewRoot.java`
#### Snippet
```java
    private static interface PhaseProcessor
    {
        public void process(FacesContext context, UIViewRoot root);
    }

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/application/ResourceDependency.java`
#### Snippet
```java
public @interface ResourceDependency
{
    public String library() default "";
    public String name();
    public String target() default "";
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/application/ResourceDependency.java`
#### Snippet
```java
    public String library() default "";
    public String name();
    public String target() default "";
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/application/ResourceDependency.java`
#### Snippet
```java
{
    public String library() default "";
    public String name();
    public String target() default "";
}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `api/src/main/java/jakarta/faces/application/ResourceDependencies.java`
#### Snippet
```java
public @interface ResourceDependencies
{
    public ResourceDependency[] value();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/spi/FactoryFinderProvider.java`
#### Snippet
```java
     * @param implName
     */
    public void setFactory(String factoryName, String implName);
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/spi/FactoryFinderProvider.java`
#### Snippet
```java
     * @throws FacesException
     */
    public Object getFactory(String factoryName) throws FacesException;
    
    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/spi/FactoryFinderProvider.java`
#### Snippet
```java
     * @throws FacesException
     */
    public void releaseFactories() throws FacesException;
    
    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/flow/Freezable.java`
#### Snippet
```java
     * Call this method when the object will be made available to the runtime
     */
    public void freeze();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/view/ViewDeclarationLanguageStrategy.java`
#### Snippet
```java
     *         should be used to handle the specified view identifier, <code>false</code> otherwise
     */
    public boolean handles(String viewId);
    
    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/view/ViewDeclarationLanguageStrategy.java`
#### Snippet
```java
     * @return the {@link ViewDeclarationLanguage} represented by this support
     */
    public ViewDeclarationLanguage getViewDeclarationLanguage();
    
    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/view/ViewDeclarationLanguageStrategy.java`
#### Snippet
```java
     * @return 
     */
    public String getMinimalImplicitOutcome(String viewId);
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/view/ResponseSwitch.java`
#### Snippet
```java
     * @return
     */
    public boolean isEnabled();
    
}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/view/ResponseSwitch.java`
#### Snippet
```java
     * @param enabled
     */
    public void setEnabled(boolean enabled);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/view/facelets/TemplateClient.java`
#### Snippet
```java
     * @throws ELException
     */
    public boolean apply(FaceletContext ctx, UIComponent parent, String name) throws IOException, FacesException,
            FaceletException, ELException;
}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/util/lang/StreamCharBuffer.java`
#### Snippet
```java
    public static interface LazyInitializingWriter
    {
        public Writer getWriter() throws IOException;
    }

```

### UnnecessaryModifier
Modifier `static` is redundant for inner interfaces
in `impl/src/main/java/org/apache/myfaces/util/lang/StreamCharBuffer.java`
#### Snippet
```java
     *
     */
    public static interface LazyInitializingWriter
    {
        public Writer getWriter() throws IOException;
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/util/lang/ConcurrentLRUCache.java`
#### Snippet
```java
    public static interface EvictionListener<K, V>
    {
        public void evictedEntry(K key, V value);
    }

```

### UnnecessaryModifier
Modifier `static` is redundant for inner interfaces
in `impl/src/main/java/org/apache/myfaces/util/lang/ConcurrentLRUCache.java`
#### Snippet
```java
    }

    public static interface EvictionListener<K, V>
    {
        public void evictedEntry(K key, V value);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ContextAware.java`
#### Snippet
```java
     * @return expression string, for example "#{bean.actionMethod}" or "success"
     */
    public abstract String getExpressionString();

    /**
```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ContextAware.java`
#### Snippet
```java
     * @return expression string, for example "#{bean.actionMethod}" or "success"
     */
    public abstract String getExpressionString();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ContextAware.java`
#### Snippet
```java
     * @return qName of XML attribute, for example "action" or "value"
     */
    public abstract String getQName();

}
```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ContextAware.java`
#### Snippet
```java
     * @return qName of XML attribute, for example "action" or "value"
     */
    public abstract String getQName();

}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/TagHandlerFactory.java`
#### Snippet
```java
     * @throws ELException
     */
    public TagHandler createHandler(TagConfig cfg) throws FacesException, ELException;
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/TagLibrary.java`
#### Snippet
```java
     * @return true if handled by this library
     */
    public boolean containsTagHandler(String ns, String localName);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/TagLibrary.java`
#### Snippet
```java
     * @return true if handled
     */
    public boolean containsFunction(String ns, String name);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/TagLibrary.java`
#### Snippet
```java
     * @return true if the namespace is used in this library
     */
    public boolean containsNamespace(String ns);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/TagLibrary.java`
#### Snippet
```java
     * @return
     */
    public Method createFunction(String ns, String name);
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/TagLibrary.java`
#### Snippet
```java
     * @throws FacesException
     */
    public TagHandler createTagHandler(String ns, String localName, TagConfig tag) throws FacesException;

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ComponentBuilderHandler.java`
#### Snippet
```java
public interface ComponentBuilderHandler
{
    public UIComponent createComponent(FaceletContext ctx);
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/RelocatableResourceHandler.java`
#### Snippet
```java
public interface RelocatableResourceHandler
{
    public UIComponent findChildByTagId(FaceletContext ctx, UIComponent parent, String id);
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/ClientBehaviorAttachedObjectTarget.java`
#### Snippet
```java
public interface ClientBehaviorAttachedObjectTarget extends BehaviorHolderAttachedObjectTarget
{
    public String getEvent();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/html/DefaultTagDecorator.java`
#### Snippet
```java
    private static interface TagDecoratorExecutor
    {
        public Tag decorate(Tag orig, TagAttributes attributes);
    }
    
```

### UnnecessaryModifier
Modifier `static` is redundant for inner interfaces
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/html/DefaultTagDecorator.java`
#### Snippet
```java
    }

    private static interface TagDecoratorExecutor
    {
        public Tag decorate(Tag orig, TagAttributes attributes);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/Instruction.java`
#### Snippet
```java
    public void write(FacesContext context) throws IOException;

    public Instruction apply(ExpressionFactory factory, ELContext ctx);

    public boolean isLiteral();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/Instruction.java`
#### Snippet
```java
interface Instruction
{
    public void write(FacesContext context) throws IOException;

    public Instruction apply(ExpressionFactory factory, ELContext ctx);
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/Instruction.java`
#### Snippet
```java
    public Instruction apply(ExpressionFactory factory, ELContext ctx);

    public boolean isLiteral();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/view/facelets/component/_ChangeSelectProperties.java`
#### Snippet
```java
     */
    @JSFProperty(clientEvent="select")
    public abstract String getOnselect();

}
```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `impl/src/main/java/org/apache/myfaces/view/facelets/component/_ChangeSelectProperties.java`
#### Snippet
```java
     */
    @JSFProperty(clientEvent="select")
    public abstract String getOnselect();

}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/view/facelets/component/_ChangeSelectProperties.java`
#### Snippet
```java
     */
    @JSFProperty(clientEvent="change")
    public abstract String getOnchange();


```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `impl/src/main/java/org/apache/myfaces/view/facelets/component/_ChangeSelectProperties.java`
#### Snippet
```java
     */
    @JSFProperty(clientEvent="change")
    public abstract String getOnchange();


```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/view/facelets/component/_StyleProperties.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getStyle();

    /**
```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `impl/src/main/java/org/apache/myfaces/view/facelets/component/_StyleProperties.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getStyle();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/view/facelets/component/_StyleProperties.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getStyleClass();

}
```

### UnnecessaryModifier
Modifier `abstract` is redundant for interface methods
in `impl/src/main/java/org/apache/myfaces/view/facelets/component/_StyleProperties.java`
#### Snippet
```java
     */
    @JSFProperty
    public abstract String getStyleClass();

}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/view/facelets/component/_EventProperties.java`
#### Snippet
```java
     */
    @JSFProperty(clientEvent="keydown")
    public String getOnkeydown();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/view/facelets/component/_EventProperties.java`
#### Snippet
```java
     */
    @JSFProperty(clientEvent="dblclick")
    public String getOndblclick();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/view/facelets/component/_EventProperties.java`
#### Snippet
```java
     */
    @JSFProperty(clientEvent="mousemove")
    public String getOnmousemove();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/view/facelets/component/_EventProperties.java`
#### Snippet
```java
     */
    @JSFProperty(clientEvent="keypress")
    public String getOnkeypress();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/view/facelets/component/_EventProperties.java`
#### Snippet
```java
     */
    @JSFProperty(clientEvent="click")
    public String getOnclick();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/view/facelets/component/_EventProperties.java`
#### Snippet
```java
     */
    @JSFProperty(clientEvent="keyup")
    public String getOnkeyup();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/view/facelets/component/_EventProperties.java`
#### Snippet
```java
     */
    @JSFProperty(clientEvent="mouseup")
    public String getOnmouseup();

}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/view/facelets/component/_EventProperties.java`
#### Snippet
```java
     */
    @JSFProperty(clientEvent="mouseover")
    public String getOnmouseover();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/view/facelets/component/_EventProperties.java`
#### Snippet
```java
     */
    @JSFProperty(clientEvent="mouseout")
    public String getOnmouseout();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/view/facelets/component/_EventProperties.java`
#### Snippet
```java
     */
    @JSFProperty(clientEvent="mousedown")
    public String getOnmousedown();

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/view/facelets/component/_FocusBlurProperties.java`
#### Snippet
```java
     */
    @JSFProperty(clientEvent="focus")
    public String getOnfocus();
    
}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/view/facelets/component/_FocusBlurProperties.java`
#### Snippet
```java
     */
    @JSFProperty(clientEvent="blur")
    public String getOnblur();
    
    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/webapp/StartupListener.java`
#### Snippet
```java
     * @param context the ServletContext
     */
    public void preDestroy(ServletContext context);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/webapp/StartupListener.java`
#### Snippet
```java
     * @param context the ServletContext
     */
    public void preInit(ServletContext context);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/webapp/StartupListener.java`
#### Snippet
```java
     * @param context the ServletContext
     */
    public void postInit(ServletContext context);

    /**
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/webapp/StartupListener.java`
#### Snippet
```java
     * @param context the ServletContext
     */
    public void postDestroy(ServletContext context);

}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/context/ReleasableFacesContextFactory.java`
#### Snippet
```java
     * Release resources that the ExternalContext is holding onto.
     */
    public void release();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/context/flash/ReleasableFlash.java`
#### Snippet
```java
public interface ReleasableFlash
{
    public void clearFlashMap(FacesContext facesContext, String clientWindowId, String token);
    
}
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/resource/ContractResource.java`
#### Snippet
```java
     * a resource from an specified contract.
     */
    public static final String CONTRACT_SELECTED = "oam.contract.SELECTED";
    
    public boolean isContractResource();
```

### UnnecessaryModifier
Modifier `static` is redundant for interface fields
in `impl/src/main/java/org/apache/myfaces/resource/ContractResource.java`
#### Snippet
```java
     * a resource from an specified contract.
     */
    public static final String CONTRACT_SELECTED = "oam.contract.SELECTED";
    
    public boolean isContractResource();
```

### UnnecessaryModifier
Modifier `final` is redundant for interface fields
in `impl/src/main/java/org/apache/myfaces/resource/ContractResource.java`
#### Snippet
```java
     * a resource from an specified contract.
     */
    public static final String CONTRACT_SELECTED = "oam.contract.SELECTED";
    
    public boolean isContractResource();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/resource/ContractResource.java`
#### Snippet
```java
    public static final String CONTRACT_SELECTED = "oam.contract.SELECTED";
    
    public boolean isContractResource();
    
    public String getContractName();
```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/resource/ContractResource.java`
#### Snippet
```java
    public boolean isContractResource();
    
    public String getContractName();
}

```

### UnnecessaryModifier
Modifier `public` is redundant for interface members
in `impl/src/main/java/org/apache/myfaces/renderkit/LazyRenderKit.java`
#### Snippet
```java
public interface LazyRenderKit
{
    public void addRenderer(String componentFamily, 
        String rendererType, String rendererClass);
}
```

## RuleId[ruleID=PointlessArithmeticExpression]
### PointlessArithmeticExpression
`0 - (int) (j % radix)` can be replaced with '- (int) (j % radix)'
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/SectionUniqueIdCounter.java`
#### Snippet
```java
        do 
        {
            int ch = 0 - (int) (j % radix);
            if (ch > 9)
            {
```

## RuleId[ruleID=MarkedForRemoval]
### MarkedForRemoval
Overrides method that is deprecated and marked for removal in 'jakarta.faces.annotation.FacesConfig'
in `api/src/main/java/jakarta/faces/annotation/FacesConfig.java`
#### Snippet
```java

        @Override
        public Version version()
        {
            return null; // non binding, so not used
```

### MarkedForRemoval
Overrides method that is deprecated and marked for removal in 'jakarta.el.ELResolver'
in `impl/src/main/java/org/apache/myfaces/el/resolver/ResourceResolver.java`
#### Snippet
```java

    @Override
    public Iterator<FeatureDescriptor> getFeatureDescriptors(
            final ELContext context, final Object base)
    {
```

### MarkedForRemoval
Overrides method that is deprecated and marked for removal in 'jakarta.el.ELResolver'
in `impl/src/main/java/org/apache/myfaces/el/resolver/EmptyStringToNullELResolver.java`
#### Snippet
```java

    @Override
    public Iterator<FeatureDescriptor> getFeatureDescriptors(ELContext context, Object base)
    {
        return null;
```

### MarkedForRemoval
Overrides method that is deprecated and marked for removal in 'jakarta.el.CompositeELResolver'
in `impl/src/main/java/org/apache/myfaces/el/resolver/CompositeELResolver.java`
#### Snippet
```java

    @Override
    public Iterator<FeatureDescriptor> getFeatureDescriptors(final ELContext context, final Object base)
    {
        Collection<ELResolver> resolvers = _elResolvers;
```

### MarkedForRemoval
'getFeatureDescriptors(jakarta.el.ELContext, java.lang.Object)' is deprecated and marked for removal
in `impl/src/main/java/org/apache/myfaces/el/resolver/CompositeELResolver.java`
#### Snippet
```java
                if (_elResolvers.hasNext())
                {
                    _currentFDIter = _elResolvers.next().getFeatureDescriptors(_context, _base);
                }
                else
```

### MarkedForRemoval
'jakarta.faces.annotation.FacesConfig.Version' is deprecated and marked for removal
in `impl/src/main/java/org/apache/myfaces/el/ELResolverBuilder.java`
#### Snippet
```java
            if (holder != null)
            {
                FacesConfig.Version version = holder.getFacesConfigVersion();
                if (version == null)
                {
```

### MarkedForRemoval
'jakarta.faces.annotation.FacesConfig.Version' is deprecated and marked for removal
in `impl/src/main/java/org/apache/myfaces/el/ELResolverBuilder.java`
#### Snippet
```java
                    return false;
                }
                else if (version.ordinal() >= FacesConfig.Version.JSF_2_3.ordinal())
                {
                    return true;
```

### MarkedForRemoval
'jakarta.faces.annotation.FacesConfig.Version' is deprecated and marked for removal
in `impl/src/main/java/org/apache/myfaces/el/ELResolverBuilder.java`
#### Snippet
```java
                    return false;
                }
                else if (version.ordinal() >= FacesConfig.Version.JSF_2_3.ordinal())
                {
                    return true;
```

### MarkedForRemoval
Overrides method that is deprecated and marked for removal in 'jakarta.el.ELResolver'
in `impl/src/main/java/org/apache/myfaces/el/resolver/ScopedAttributeResolver.java`
#### Snippet
```java

    @Override
    public Iterator<FeatureDescriptor> getFeatureDescriptors(final ELContext context, final Object base)
    {

```

### MarkedForRemoval
Overrides method that is deprecated and marked for removal in 'jakarta.el.ELResolver'
in `impl/src/main/java/org/apache/myfaces/el/resolver/ResourceBundleResolver.java`
#### Snippet
```java

    @Override
    public Iterator<FeatureDescriptor> getFeatureDescriptors(final ELContext context, final Object base)
    {
        if (base != null)
```

### MarkedForRemoval
Overrides method that is deprecated and marked for removal in 'jakarta.el.ELResolver'
in `impl/src/main/java/org/apache/myfaces/el/resolver/FlashELResolver.java`
#### Snippet
```java

    @Override
    public Iterator<FeatureDescriptor> getFeatureDescriptors(ELContext context, Object base)
    {
        ArrayList<FeatureDescriptor> descriptors = new ArrayList<>(1);
```

### MarkedForRemoval
Overrides method that is deprecated and marked for removal in 'jakarta.el.ELResolver'
in `impl/src/main/java/org/apache/myfaces/el/resolver/ImportConstantsELResolver.java`
#### Snippet
```java

    @Override
    public Iterator<FeatureDescriptor> getFeatureDescriptors(final ELContext context, final Object base)
    {
        return null;
```

### MarkedForRemoval
Overrides method that is deprecated and marked for removal in 'jakarta.el.ELResolver'
in `impl/src/main/java/org/apache/myfaces/el/resolver/CompositeComponentELResolver.java`
#### Snippet
```java

    @Override
    public Iterator<FeatureDescriptor> getFeatureDescriptors(ELContext context, Object base)
    {
        // Per the spec, do nothing.
```

### MarkedForRemoval
Overrides method that is deprecated and marked for removal in 'jakarta.el.ELResolver'
in `impl/src/main/java/org/apache/myfaces/el/resolver/implicitobject/ImplicitObjectResolver.java`
#### Snippet
```java

    @Override
    public Iterator<FeatureDescriptor> getFeatureDescriptors(ELContext context, Object base)
    {
        if (base != null)
```

### MarkedForRemoval
'jakarta.faces.annotation.FacesConfig.Version' is deprecated and marked for removal
in `impl/src/main/java/org/apache/myfaces/cdi/config/FacesConfigBeanHolder.java`
#### Snippet
```java
    private FacesConfig.Version facesConfigVersion = null;

    public FacesConfig.Version getFacesConfigVersion()
    {
        return facesConfigVersion;
```

### MarkedForRemoval
'jakarta.faces.annotation.FacesConfig.Version' is deprecated and marked for removal
in `impl/src/main/java/org/apache/myfaces/cdi/config/FacesConfigBeanHolder.java`
#### Snippet
```java
    }

    public void setFacesConfigVersion(FacesConfig.Version facesConfigVersion)
    {
        this.facesConfigVersion = facesConfigVersion;
```

### MarkedForRemoval
'jakarta.faces.annotation.FacesConfig.Version' is deprecated and marked for removal
in `impl/src/main/java/org/apache/myfaces/cdi/config/FacesConfigBeanHolder.java`
#### Snippet
```java
public class FacesConfigBeanHolder
{
    private FacesConfig.Version facesConfigVersion = null;

    public FacesConfig.Version getFacesConfigVersion()
```

### MarkedForRemoval
'jakarta.faces.annotation.FacesConfig.Version' is deprecated and marked for removal
in `impl/src/main/java/org/apache/myfaces/cdi/config/FacesConfigExtension.java`
#### Snippet
```java
public class FacesConfigExtension implements Extension
{
    private FacesConfig.Version facesConfigVersion;
    
    void beforeBeanDiscovery(@Observes BeforeBeanDiscovery event, BeanManager beanManager)
```

### MarkedForRemoval
'version' is deprecated and marked for removal
in `impl/src/main/java/org/apache/myfaces/cdi/config/FacesConfigExtension.java`
#### Snippet
```java
            if (facesConfigVersion != null)
            {
                facesConfigVersion = facesConfigVersion.ordinal() < config.version().ordinal() ? 
                        config.version() : facesConfigVersion;
            }
```

### MarkedForRemoval
'version' is deprecated and marked for removal
in `impl/src/main/java/org/apache/myfaces/cdi/config/FacesConfigExtension.java`
#### Snippet
```java
            {
                facesConfigVersion = facesConfigVersion.ordinal() < config.version().ordinal() ? 
                        config.version() : facesConfigVersion;
            }
            else
```

### MarkedForRemoval
'version' is deprecated and marked for removal
in `impl/src/main/java/org/apache/myfaces/cdi/config/FacesConfigExtension.java`
#### Snippet
```java
            else
            {
                facesConfigVersion = config.version();
            }
        }
```

### MarkedForRemoval
'setComment(java.lang.String)' is deprecated and marked for removal
in `impl/src/main/java/org/apache/myfaces/context/servlet/ServletExternalContextImpl.java`
#### Snippet
```java
                if ("comment".equals(propertyKey))
                {
                    cookie.setComment((String) propertyValue);
                    continue;
                }
```

### MarkedForRemoval
Overrides method that is deprecated and marked for removal in 'jakarta.el.ELResolver'
in `impl/src/main/java/org/apache/myfaces/component/validate/CopyBeanInterceptorELResolver.java`
#### Snippet
```java

    @Override
    public Iterator<FeatureDescriptor> getFeatureDescriptors(final ELContext ctx, final Object base)
    {
        return resolver.getFeatureDescriptors(ctx, base);
```

### MarkedForRemoval
'getFeatureDescriptors(jakarta.el.ELContext, java.lang.Object)' is deprecated and marked for removal
in `impl/src/main/java/org/apache/myfaces/component/validate/CopyBeanInterceptorELResolver.java`
#### Snippet
```java
    public Iterator<FeatureDescriptor> getFeatureDescriptors(final ELContext ctx, final Object base)
    {
        return resolver.getFeatureDescriptors(ctx, base);
    }

```

### MarkedForRemoval
Overrides method that is deprecated and marked for removal in 'jakarta.el.ELResolver'
in `test/src/main/java/org/apache/myfaces/test/el/FacesScopedAttributeELResolver.java`
#### Snippet
```java
     * @param base Base object against which this evaluation occurs
     */
    public Iterator getFeatureDescriptors(ELContext context, Object base)
    {

```

### MarkedForRemoval
Overrides method that is deprecated and marked for removal in 'jakarta.el.ELResolver'
in `test/src/main/java/org/apache/myfaces/test/el/FacesVariableResolverChainWrapper.java`
#### Snippet
```java
     * @param base Base object against which this evaluation occurs
     */
    public Iterator getFeatureDescriptors(ELContext context, Object base)
    {

```

### MarkedForRemoval
Overrides method that is deprecated and marked for removal in 'jakarta.el.ELResolver'
in `test/src/main/java/org/apache/myfaces/test/el/ReservedWordsELResolver.java`
#### Snippet
```java

    @Override
    public synchronized Iterator<FeatureDescriptor> getFeatureDescriptors(
        ELContext context, Object base)
    {
```

### MarkedForRemoval
Overrides method that is deprecated and marked for removal in 'jakarta.el.ELResolver'
in `test/src/main/java/org/apache/myfaces/test/el/FacesResourceBundleELResolver.java`
#### Snippet
```java
     * @param base Base object against which this evaluation occurs
     */
    public Iterator getFeatureDescriptors(ELContext context, Object base)
    {

```

### MarkedForRemoval
Overrides method that is deprecated and marked for removal in 'jakarta.el.ELResolver'
in `test/src/main/java/org/apache/myfaces/test/el/FacesImplicitObjectELResolver.java`
#### Snippet
```java
     * @param base Base object against which this evaluation occurs
     */
    public Iterator getFeatureDescriptors(ELContext context, Object base)
    {

```

### MarkedForRemoval
'setComment(java.lang.String)' is deprecated and marked for removal
in `test/src/main/java/org/apache/myfaces/test/mock/MockExternalContext20.java`
#### Snippet
```java
                if ("comment".equals(propertyKey))
                {
                    cookie.setComment((String) propertyValue);
                    continue;
                }
```

### MarkedForRemoval
Overrides method that is deprecated and marked for removal in 'jakarta.servlet.SessionCookieConfig'
in `test/src/main/java/org/apache/myfaces/test/mock/MockServletContext.java`
#### Snippet
```java
            }

            @Override public String getComment()
            {
                return null;
```

### MarkedForRemoval
Overrides method that is deprecated and marked for removal in 'jakarta.servlet.SessionCookieConfig'
in `test/src/main/java/org/apache/myfaces/test/mock/MockServletContext.java`
#### Snippet
```java
            }

            @Override public void setComment(String s)
            {

```

## RuleId[ruleID=EmptyStatementBody]
### EmptyStatementBody
`if` statement has empty body
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlResponseWriterImpl.java`
#### Snippet
```java
        else
        {
            if (!_useStraightXml && isEmptyElement(elementName))
            {

```

## RuleId[ruleID=WhileCanBeForeach]
### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `api/src/main/java/org/apache/myfaces/core/api/shared/lang/CollectionUtils.java`
#### Snippet
```java
            // value is Iterable --> Collection, DataModel,...
            Iterator<?> iterator = ((Iterable<?>) value).iterator();
            while (iterator.hasNext())
            {
                callback.accept(iterator.next());
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `api/src/main/java/jakarta/faces/view/ViewMetadata.java`
#### Snippet
```java
        {
            Iterator<UIComponent> children = metadataFacet.getChildren().iterator();
            while (children.hasNext())
            {
                 UIComponent component = children.next();
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `api/src/main/java/jakarta/faces/view/ViewMetadata.java`
#### Snippet
```java
        {
            Iterator<UIComponent> children = metadataFacet.getChildren().iterator();
            while (children.hasNext())
            {
                 UIComponent component = children.next();
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `api/src/main/java/jakarta/faces/view/ViewMetadata.java`
#### Snippet
```java
        {
            Iterator<UIComponent> children = metadataFacet.getChildren().iterator();
            while (children.hasNext())
            {
                 UIComponent component = children.next();
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `api/src/main/java/jakarta/faces/component/_ComponentChildrenList.java`
#### Snippet
```java
    {
        Iterator<? extends UIComponent> it = collection.iterator();
        while (it.hasNext())
        {
            add(location++, it.next());
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `api/src/main/java/jakarta/faces/component/_ComponentChildrenList.java`
#### Snippet
```java
        boolean result = false;
        Iterator<? extends UIComponent> it = collection.iterator();
        while (it.hasNext())
        {
            if (add(it.next()))
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `api/src/main/java/jakarta/faces/component/_DeltaStateHelper.java`
#### Snippet
```java

        Iterator<Map.Entry<Serializable, Object>> it = serializableMap.entrySet().iterator();
        while (it.hasNext())
        {
            entry = it.next();
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `api/src/main/java/jakarta/faces/component/UIData.java`
#### Snippet
```java
            Iterator<UIComponent> childIterator = parent.getFacets().values().iterator();
            
            while (childIterator.hasNext())
            {
                UIComponent component = childIterator.next();
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `api/src/main/java/jakarta/faces/component/UIData.java`
#### Snippet
```java
            Iterator<UIComponent> childIterator = parent.getFacets().values().iterator();

            while (childIterator.hasNext())
            {
                UIComponent child = childIterator.next();
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `api/src/main/java/jakarta/faces/component/UIData.java`
#### Snippet
```java
            Iterator<UIComponent> childIterator = parent.getFacets().values().iterator();
            
            while (childIterator.hasNext())
            {
                UIComponent component = childIterator.next();
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `impl/src/main/java/org/apache/myfaces/util/lang/SubKeyMap.java`
#### Snippet
```java
        {
            Iterator<String> keys = _base.keySet().iterator();
            while (keys.hasNext())
            {
                String key = keys.next();
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ComponentSupport.java`
#### Snippet
```java
                    {
                        Iterator<UIComponent> itr2 = facet.getFacets().values().iterator();
                        while (itr2.hasNext())
                        {
                            UIComponent child = itr2.next();
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ComponentSupport.java`
#### Snippet
```java
                    {
                        Iterator<UIComponent> itr2 = facet.getFacets().values().iterator();
                        while (itr2.hasNext())
                        {
                            UIComponent child = itr2.next();
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ComponentSupport.java`
#### Snippet
```java
                    {
                        Iterator<UIComponent> itr2 = facet.getFacets().values().iterator();
                        while (itr2.hasNext())
                        {
                            UIComponent child = itr2.next();
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ComponentSupport.java`
#### Snippet
```java
                    {
                        Iterator<UIComponent> itr2 = facet.getFacets().values().iterator();
                        while (itr2.hasNext())
                        {
                            UIComponent child = itr2.next();
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `impl/src/main/java/org/apache/myfaces/config/LogMetaInfUtils.java`
#### Snippet
```java

            Iterator<URL> it = ClassUtils.getResources("META-INF/MANIFEST.MF", LogMetaInfUtils.class).iterator();
            while (it.hasNext())
            {
                URL url = it.next();
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `impl/src/main/java/org/apache/myfaces/view/facelets/component/UIRepeat.java`
#### Snippet
```java
            Iterator<UIComponent> childIterator = parent.getFacets().values().iterator();
            
            while (childIterator.hasNext())
            {
                UIComponent component = childIterator.next();
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `impl/src/main/java/org/apache/myfaces/view/facelets/component/UIRepeat.java`
#### Snippet
```java
            Iterator<UIComponent> childIterator = parent.getFacets().values().iterator();

            while (childIterator.hasNext())
            {
                UIComponent child = childIterator.next();
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `impl/src/main/java/org/apache/myfaces/view/facelets/component/UIRepeat.java`
#### Snippet
```java
            Iterator<UIComponent> childIterator = parent.getFacets().values().iterator();
            
            while (childIterator.hasNext())
            {
                UIComponent component = childIterator.next();
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `impl/src/main/java/org/apache/myfaces/config/util/DirectedAcyclicGraphVerifier.java`
#### Snippet
```java
    {
        Iterator<Vertex<T>> it = vertices.iterator();
        while (it.hasNext())
        {
            it.next().reset();
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `impl/src/main/java/org/apache/myfaces/config/util/DirectedAcyclicGraphVerifier.java`
#### Snippet
```java
        // Assert that all vertices are in the vertices list and resolve each of their orders.
        Iterator<Vertex<T>> it = vertices.iterator();
        while (it.hasNext())
        {
            Vertex<T> v = it.next();
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `impl/src/main/java/org/apache/myfaces/config/util/DirectedAcyclicGraphVerifier.java`
#### Snippet
```java
            //  failed, this would lead to some very hard to track down problems elsewhere.
            Iterator<Vertex<T>> dit = v.getDependencies().iterator();
            while (dit.hasNext())
            {
                Vertex<T> dv =  dit.next();
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `impl/src/main/java/org/apache/myfaces/context/flash/FlashImpl.java`
#### Snippet
```java
            Iterator<MessageEntry> iterMessages = messageList.iterator();

            while (iterMessages.hasNext())
            {
                MessageEntry entry = iterMessages.next();
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `test/src/main/java/org/apache/myfaces/test/el/FacesResourceBundleELResolver.java`
#### Snippet
```java
        Iterator entries = application.getResourceBundles().entrySet()
                .iterator();
        while (entries.hasNext())
        {
            Entry entry = (Entry) entries.next();
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractHtmlUnitTestCase.java`
#### Snippet
```java

        Iterator forms = page.getForms().iterator();
        while (forms.hasNext())
        {
            HtmlForm form = (HtmlForm) forms.next();
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpSession.java`
#### Snippet
```java
            }
            Iterator listeners = attributeListeners.iterator();
            while (listeners.hasNext())
            {
                HttpSessionAttributeListener listener = (HttpSessionAttributeListener) listeners
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpSession.java`
#### Snippet
```java
            }            
            Iterator listeners = attributeListeners.iterator();
            while (listeners.hasNext())
            {
                HttpSessionAttributeListener listener = (HttpSessionAttributeListener) listeners
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpSession.java`
#### Snippet
```java
            }            
            Iterator listeners = attributeListeners.iterator();
            while (listeners.hasNext())
            {
                HttpSessionAttributeListener listener = (HttpSessionAttributeListener) listeners
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication12.java`
#### Snippet
```java
            CompositeELResolver nested = new CompositeELResolver();
            Iterator items = resolvers.iterator();
            while (items.hasNext())
            {
                nested.add((ELResolver) items.next());
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `test/src/main/java/org/apache/myfaces/test/mock/MockFlash.java`
#### Snippet
```java
            Iterator iterMessages = messageList.iterator();

            while (iterMessages.hasNext())
            {
                MessageEntry message = (MessageEntry) iterMessages.next();
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `test/src/main/java/org/apache/myfaces/test/mock/MockFacesContext10.java`
#### Snippet
```java
        ArrayList results = new ArrayList();
        Iterator clientIds = messages.keySet().iterator();
        while (clientIds.hasNext())
        {
            String clientId = (String) clientIds.next();
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `test/src/main/java/org/apache/myfaces/test/mock/MockServletContext.java`
#### Snippet
```java
                    this, key, value);
            Iterator listeners = attributeListeners.iterator();
            while (listeners.hasNext())
            {
                ServletContextAttributeListener listener = (ServletContextAttributeListener) listeners
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `test/src/main/java/org/apache/myfaces/test/mock/MockServletContext.java`
#### Snippet
```java
                    this, key, value);
            Iterator listeners = attributeListeners.iterator();
            while (listeners.hasNext())
            {
                ServletContextAttributeListener listener = (ServletContextAttributeListener) listeners
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `test/src/main/java/org/apache/myfaces/test/mock/MockServletContext.java`
#### Snippet
```java
                    this, key, value);
            Iterator listeners = attributeListeners.iterator();
            while (listeners.hasNext())
            {
                ServletContextAttributeListener listener = (ServletContextAttributeListener) listeners
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletRequest.java`
#### Snippet
```java
        Vector values = new Vector();
        Iterator headers = this.headers.iterator();
        while (headers.hasNext())
        {
            String header = (String) headers.next();
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletRequest.java`
#### Snippet
```java
        String match = name + ':';
        Iterator headers = this.headers.iterator();
        while (headers.hasNext())
        {
            String header = (String) headers.next();
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletRequest.java`
#### Snippet
```java
                getServletContext(), this, key, value);
        Iterator listeners = attributeListeners.iterator();
        while (listeners.hasNext())
        {
            ServletRequestAttributeListener listener = (ServletRequestAttributeListener) listeners
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletRequest.java`
#### Snippet
```java
        String match = name + ':';
        Iterator headers = this.headers.iterator();
        while (headers.hasNext())
        {
            String header = (String) headers.next();
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletRequest.java`
#### Snippet
```java
        Vector values = new Vector();
        Iterator headers = this.headers.iterator();
        while (headers.hasNext())
        {
            String header = (String) headers.next();
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletRequest.java`
#### Snippet
```java
        String match = name + ':';
        Iterator headers = this.headers.iterator();
        while (headers.hasNext())
        {
            String header = (String) headers.next();
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletRequest.java`
#### Snippet
```java
                getServletContext(), this, key, value);
        Iterator listeners = attributeListeners.iterator();
        while (listeners.hasNext())
        {
            ServletRequestAttributeListener listener = (ServletRequestAttributeListener) listeners
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletRequest.java`
#### Snippet
```java
                getServletContext(), this, key, value);
        Iterator listeners = attributeListeners.iterator();
        while (listeners.hasNext())
        {
            ServletRequestAttributeListener listener = (ServletRequestAttributeListener) listeners
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletResponse.java`
#### Snippet
```java
        String match = name + ':';
        Iterator headers = this.headers.iterator();
        while (headers.hasNext())
        {
            String header = (String) headers.next();
```

### WhileCanBeForeach
`while` loop can be replaced with enhanced 'for'
in `test/src/main/java/org/apache/myfaces/test/mock/MockSubKeyMap.java`
#### Snippet
```java
        {
            Iterator<String> keys = _base.keySet().iterator();
            while (keys.hasNext())
            {
                String key = keys.next();
```

## RuleId[ruleID=AssignmentToStaticFieldFromInstanceMethod]
### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `lastUpdate` from instance context
in `impl/src/main/java/org/apache/myfaces/config/FacesConfigurator.java`
#### Snippet
```java
                    // request after this one.
                    //
                    lastUpdate = System.currentTimeMillis();

                    return;
```

### AssignmentToStaticFieldFromInstanceMethod
Assignment to static field `lastUpdate` from instance context
in `impl/src/main/java/org/apache/myfaces/config/FacesConfigurator.java`
#### Snippet
```java
        
        // record the time of update
        lastUpdate = System.currentTimeMillis();
    }

```

## RuleId[ruleID=CommentedOutCode]
### CommentedOutCode
Commented out code (9 lines)
in `api/src/main/java/jakarta/faces/component/UIComponentBase.java`
#### Snippet
```java
        // components without any explicit id (UIViewParameter components and UIOuput resource components) instances.
        // So, this warning is becoming obsolete in this new context and should be removed.
        //if (idWasNull && log.isLoggable(Level.WARNING))
        //{
        //    log.warning("WARNING: Component " + _clientId
```

### CommentedOutCode
Commented out code (20 lines)
in `api/src/main/java/jakarta/faces/component/UIViewRoot.java`
#### Snippet
```java
        UIComponent facet = getFacet(target);

        /*
        // If the facet is not found,
        if (facet == null)
```

### CommentedOutCode
Commented out code (2 lines)
in `api/src/main/java/jakarta/faces/component/UIViewRoot.java`
#### Snippet
```java
                    try
                    {
                        // RequestViewContext requestViewContext = RequestViewContext.getInstance(context);
                        // requestViewContext.setRenderTarget("head", true, componentResource);
                        Object requestViewContext = REQUEST_VIEW_CONTEXT_GET_INSTANCE.invoke(null, context);
```

### CommentedOutCode
Commented out code (5 lines)
in `impl/src/main/java/org/apache/myfaces/spi/impl/AllAnnotationInjectionProvider.java`
#### Snippet
```java
        }
        // not implemented since 1.x, lets ignore it
        /*if (method.isAnnotationPresent(WebServiceRef.class)) {
            WebServiceRef annotation =
                (WebServiceRef) method.getAnnotation(WebServiceRef.class);
```

### CommentedOutCode
Commented out code (5 lines)
in `impl/src/main/java/org/apache/myfaces/spi/impl/AllAnnotationInjectionProvider.java`
#### Snippet
```java
            lookupFieldResource(context, instance, field, annotation.name());
        }
        /*if (field.isAnnotationPresent(WebServiceRef.class)) {
            WebServiceRef annotation =
                (WebServiceRef) field.getAnnotation(WebServiceRef.class);
```

### CommentedOutCode
Commented out code (19 lines)
in `impl/src/main/java/org/apache/myfaces/util/lang/ArrayUtils.java`
#### Snippet
```java
    }

//    public static void main(String[] args)
//    {
//        // test code
```

### CommentedOutCode
Commented out code (2 lines)
in `impl/src/main/java/org/apache/myfaces/util/lang/ConcurrentLRUCache.java`
#### Snippet
```java
                // This log message is useless, because it is not supposed to use
                // thread cleanup strategy for this class.
                //log.severe("ConcurrentLRUCache was not destroyed prior to finalize()," +
                //        " indicates a bug -- POSSIBLE RESOURCE LEAK!!!");
                destroy();
```

### CommentedOutCode
Commented out code (3 lines)
in `impl/src/main/java/org/apache/myfaces/util/lang/ConcurrentLRUCache.java`
#### Snippet
```java
            int eSize = 0;

            // System.out.println("newestEntry="+newestEntry + " oldestEntry="+oldestEntry);
            // System.out.println("items removed:" + numRemoved + " numKept=" + numKept + 
            //    " esetSz="+ eSize + " sz-numRemoved=" + (sz-numRemoved));
```

### CommentedOutCode
Commented out code (2 lines)
in `impl/src/main/java/org/apache/myfaces/util/lang/ConcurrentLRUCache.java`
#### Snippet
```java
                    }
                }
                // System.out.println("items removed:" + numRemoved + " numKept=" + 
                //    numKept + " esetSz="+ eSize + " sz-numRemoved=" + (sz-numRemoved));
            }
```

### CommentedOutCode
Commented out code (2 lines)
in `impl/src/main/java/org/apache/myfaces/util/lang/ConcurrentLRUCache.java`
#### Snippet
```java
                        numKept++;
                        // removal not necessary on last pass.
                        // eset[i] = eset[eSize-1];
                        // eSize--;

```

### CommentedOutCode
Commented out code (2 lines)
in `impl/src/main/java/org/apache/myfaces/util/lang/ConcurrentLRUCache.java`
#### Snippet
```java

                        // removal not necessary on last pass.
                        // eset[i] = eset[eSize-1];
                        // eSize--;
                    }
```

### CommentedOutCode
Commented out code (3 lines)
in `impl/src/main/java/org/apache/myfaces/util/lang/ConcurrentLRUCache.java`
#### Snippet
```java
                }

                // System.out.println("items removed:" + numRemoved + " numKept=" + numKept + 
                //    " initialQueueSize="+ wantToRemove + " finalQueueSize=" + 
                //      queue.size() + " sz-numRemoved=" + (sz-numRemoved));
```

### CommentedOutCode
Commented out code (3 lines)
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ContextAwareTagMethodExpression.java`
#### Snippet
```java
        //Not necessary because NullPointerException by null context never occur and should not be wrapped
        //catch (Exception e)
        //{
        //    throw new ContextAwareException(getLocation(), getLocalExpressionString(), getQName(), e);
        //}
```

### CommentedOutCode
Commented out code (3 lines)
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ContextAwareTagMethodExpression.java`
#### Snippet
```java
        //Not necessary because NullPointerException by null context never occur and should not be wrapped
        //catch (Exception e)
        //{
        //    throw new ContextAwareException(getLocation(), getLocalExpressionString(), getQName(), e);
        //}
```

### CommentedOutCode
Commented out code (3 lines)
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ContextAwareTagValueExpression.java`
#### Snippet
```java
        //Not necessary because NullPointerException by null context never occur and should not be wrapped
        //catch (Exception e)
        //{
        //    throw new ContextAwareException(getLocation(), getLocalExpressionString(), getQName(), e);
        //}
```

### CommentedOutCode
Commented out code (2 lines)
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguage.java`
#### Snippet
```java
                    // we expect PostAddToViewEvent will be propagated from parent to child, and
                    // facelets refreshing algorithm do the opposite.
                    //FaceletViewDeclarationLanguage._publishPreRemoveFromViewEvent(context, view);
                    //FaceletViewDeclarationLanguage._publishPostAddToViewEvent(context, view);
                }
```

### CommentedOutCode
Commented out code (4 lines)
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeComponentRule.java`
#### Snippet
```java
            //((UIComponent) instance).getAttributes().put(_name, _attr.getValueExpression(ctx, _type));
            ((UIComponent) instance).setValueExpression(_name, _attr.getValueExpression(ctx, _type));
            //((UIComponent) instance).setValueExpression(_name, 
            //        ctx.getFacesContext().getApplication().
            //        getExpressionFactory().createValueExpression(
```

### CommentedOutCode
Commented out code (2 lines)
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFacelet.java`
#### Snippet
```java
        // implementation wraps everything that could contain ValueExpression and decode so
        // we can't use it here.
        //DefaultFacelet f = (DefaultFacelet) _factory.getFacelet(resource.getURL());
        //f.apply(ctx.getFacesContext(), parent);
        DefaultFacelet f = (DefaultFacelet) _factory.getFacelet(resource.getURL());
```

### CommentedOutCode
Commented out code (8 lines)
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TextUnit.java`
#### Snippet
```java
            return "";
        }
        /*
        if (i == s.length() - 1)
        {
```

### CommentedOutCode
Commented out code (9 lines)
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/UILeaf.java`
#### Snippet
```java
        // components without any explicit id (UIViewParameter components and UIOuput resource components) instances.
        // So, this warning is becoming obsolete in this new context and should be removed.
        //if (idWasNull && log.isLoggable(Level.WARNING))
        //{
        //    log.warning("WARNING: Component " + _clientId
```

### CommentedOutCode
Commented out code (2 lines)
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TagLibraryConfigUnmarshallerImpl.java`
#### Snippet
```java
                else if ("function-class".equals(qName))
                {
                    //String className = this.captureBuffer();
                    //this.functionClass = createClass(Object.class, className);
                    this.functionClass = this.captureBuffer();
```

### CommentedOutCode
Commented out code (2 lines)
in `impl/src/main/java/org/apache/myfaces/config/annotation/ClassByteCodeAnnotationFilter.java`
#### Snippet
```java

                case CP_INFO_INVOKE_DYNAMIC:    // Ignore
                    // u2 bootstrap_method_attr_index;
                    // u2 name_and_type_index;
                    in.readUnsignedShort();
```

### CommentedOutCode
Commented out code (2 lines)
in `impl/src/main/java/org/apache/myfaces/config/annotation/DefaultAnnotationProvider.java`
#### Snippet
```java
        bcan.add("Ljakarta/faces/render/FacesRenderer;");
        bcan.add("Ljakarta/faces/event/NamedEvent;");
        //bcan.add("Ljakarta/faces/event/ListenerFor;");
        //bcan.add("Ljakarta/faces/event/ListenersFor;");
        bcan.add("Ljakarta/faces/render/FacesBehaviorRenderer;");
```

### CommentedOutCode
Commented out code (4 lines)
in `impl/src/main/java/org/apache/myfaces/resource/TempDirFileCacheResourceLoader.java`
#### Snippet
```java
    }

    /*
    private boolean couldResourceContainValueExpressions(ResourceMeta resourceMeta)
    {
```

### CommentedOutCode
Commented out code (4 lines)
in `impl/src/main/java/org/apache/myfaces/lifecycle/clientwindow/ClientConfig.java`
#### Snippet
```java
     *         which should be sent to the users browser.
     */
    /*
    public String getWindowHandlerResourceLocation()
    {
```

### CommentedOutCode
Commented out code (31 lines)
in `impl/src/main/java/org/apache/myfaces/lifecycle/clientwindow/ClientConfig.java`
#### Snippet
```java
     * @throws IOException
     */
    /*
    public String getWindowHandlerHtml() throws IOException
    {
```

### CommentedOutCode
Commented out code (18 lines)
in `impl/src/main/java/org/apache/myfaces/lifecycle/clientwindow/CODIClientSideWindow.java`
#### Snippet
```java
     * {@inheritDoc}
     */
    /*
    public String restoreWindowId(ExternalContext externalContext)
    {
```

### CommentedOutCode
Commented out code (4 lines)
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/HTML.java`
#### Snippet
```java
            INPUT_PASSTHROUGH_ATTRIBUTES_WITHOUT_DISABLED_AND_EVENTS);

    /*
    String[] MESSAGE_PASSTHROUGH_ATTRIBUTES =
        (String[]) ArrayUtils.concat(
```

## RuleId[ruleID=RegExpRedundantEscape]
### RegExpRedundantEscape
Redundant character escape `\\.` in RegExp
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/CompositeComponentELUtils.java`
#### Snippet
```java
     * A regular expression used to determine if cc is used in an expression String.
     */
    public static final Pattern CC_EXPRESSION_REGEX = Pattern.compile(".*[^\\w\\.]cc[^\\w].*");
    
    /**
```

### RegExpRedundantEscape
Redundant character escape `\\(` in RegExp
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/CompositeComponentELUtils.java`
#### Snippet
```java
     */
    public static final Pattern CC_ATTRS_METHOD_EXPRESSION_REGEX
            = Pattern.compile("[^\\(]*[^\\w\\.\\(]cc\\.attrs\\.[^\\.]*(\\(.*)?");
    
    private static final String CC = "cc";
```

### RegExpRedundantEscape
Redundant character escape `\\.` in RegExp
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/CompositeComponentELUtils.java`
#### Snippet
```java
     */
    public static final Pattern CC_ATTRS_METHOD_EXPRESSION_REGEX
            = Pattern.compile("[^\\(]*[^\\w\\.\\(]cc\\.attrs\\.[^\\.]*(\\(.*)?");
    
    private static final String CC = "cc";
```

### RegExpRedundantEscape
Redundant character escape `\\(` in RegExp
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/CompositeComponentELUtils.java`
#### Snippet
```java
     */
    public static final Pattern CC_ATTRS_METHOD_EXPRESSION_REGEX
            = Pattern.compile("[^\\(]*[^\\w\\.\\(]cc\\.attrs\\.[^\\.]*(\\(.*)?");
    
    private static final String CC = "cc";
```

### RegExpRedundantEscape
Redundant character escape `\\.` in RegExp
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/CompositeComponentELUtils.java`
#### Snippet
```java
     */
    public static final Pattern CC_ATTRS_METHOD_EXPRESSION_REGEX
            = Pattern.compile("[^\\(]*[^\\w\\.\\(]cc\\.attrs\\.[^\\.]*(\\(.*)?");
    
    private static final String CC = "cc";
```

### RegExpRedundantEscape
Redundant character escape `\\-` in RegExp
in `impl/src/main/java/org/apache/myfaces/config/LogMetaInfUtils.java`
#### Snippet
```java
    public static final String REGEX_LIBRARY
        = "(jar|besjar|wsjar|zip)"
        + ":(file:.*/((myfaces|tomahawk|trinidad|tobago|commons\\-el|jsp\\-api)[\\w\\-\\_]+?)"
        + "-(\\d+.*)\\.jar)!/META-INF/MANIFEST.MF";
    private static final Pattern REGEX_LIBRARY_PATTERN = Pattern.compile(REGEX_LIBRARY);
```

### RegExpRedundantEscape
Redundant character escape `\\-` in RegExp
in `impl/src/main/java/org/apache/myfaces/config/LogMetaInfUtils.java`
#### Snippet
```java
    public static final String REGEX_LIBRARY
        = "(jar|besjar|wsjar|zip)"
        + ":(file:.*/((myfaces|tomahawk|trinidad|tobago|commons\\-el|jsp\\-api)[\\w\\-\\_]+?)"
        + "-(\\d+.*)\\.jar)!/META-INF/MANIFEST.MF";
    private static final Pattern REGEX_LIBRARY_PATTERN = Pattern.compile(REGEX_LIBRARY);
```

### RegExpRedundantEscape
Redundant character escape `\\_` in RegExp
in `impl/src/main/java/org/apache/myfaces/config/LogMetaInfUtils.java`
#### Snippet
```java
    public static final String REGEX_LIBRARY
        = "(jar|besjar|wsjar|zip)"
        + ":(file:.*/((myfaces|tomahawk|trinidad|tobago|commons\\-el|jsp\\-api)[\\w\\-\\_]+?)"
        + "-(\\d+.*)\\.jar)!/META-INF/MANIFEST.MF";
    private static final Pattern REGEX_LIBRARY_PATTERN = Pattern.compile(REGEX_LIBRARY);
```

### RegExpRedundantEscape
Redundant character escape `\\.` in RegExp
in `impl/src/main/java/org/apache/myfaces/resource/ResourceELUtils.java`
#### Snippet
```java
    // TODO: check this expression, maybe we can make it simpler, because "resource" implicit object
    // cannot be preceded by anything.
    public static final Pattern RESOURCE_EXPRESSION_REGEX = Pattern.compile(".*[^\\w\\.]resource[^\\w].*");
    
    private static final String RESOURCE = "resource";
```

### RegExpRedundantEscape
Redundant character escape `\\]` in RegExp
in `impl/src/main/java/org/apache/myfaces/renderkit/ErrorPageWriter.java`
#### Snippet
```java
    private static String[] debugParts;

    private static final Pattern REGEX_PATTERN = Pattern.compile(".*?\\Q,Id:\\E\\s*(\\S+)\\s*\\].*?");

    private final static String[] IGNORE = new String[] { "parent", "rendererType" };
```

## RuleId[ruleID=ObjectNotify]
### ObjectNotify
`notify` should probably be replaced with 'notifyAll()'
in `impl/src/main/java/org/apache/myfaces/util/lang/ConcurrentLRUCache.java`
#### Snippet
```java
            {
                stop = true;
                this.notify();
            }
        }
```

### ObjectNotify
`notify` should probably be replaced with 'notifyAll()'
in `impl/src/main/java/org/apache/myfaces/util/lang/ConcurrentLRUCache.java`
#### Snippet
```java
            synchronized (this)
            {
                this.notify();
            }
        }
```

## RuleId[ruleID=FinalPrivateMethod]
### FinalPrivateMethod
'private' method declared `final`
in `impl/src/main/java/org/apache/myfaces/util/lang/PriorityQueue.java`
#### Snippet
```java
    }

    private final void upHeap()
    {
        int i = size;
```

### FinalPrivateMethod
'private' method declared `final`
in `impl/src/main/java/org/apache/myfaces/util/lang/PriorityQueue.java`
#### Snippet
```java
    }

    private final void downHeap()
    {
        int i = 1;
```

### FinalPrivateMethod
'private' method declared `final`
in `impl/src/main/java/org/apache/myfaces/util/lang/StreamCharBuffer.java`
#### Snippet
```java
        }

        private final int getNewChunkMinSize()
        {
            if (chunkMinSize <= 0 || allocBuffer.charsUsed() == 0 || allocBuffer.charsUsed() >= chunkMinSize)
```

### FinalPrivateMethod
'private' method declared `final`
in `impl/src/main/java/org/apache/myfaces/util/lang/StreamCharBuffer.java`
#### Snippet
```java
        }

        private final boolean isNextChunkBigEnough(final int len)
        {
            return (len > getNewChunkMinSize());
```

### FinalPrivateMethod
'private' method declared `final`
in `impl/src/main/java/org/apache/myfaces/util/lang/StreamCharBuffer.java`
#### Snippet
```java
        }

        private final int getDirectChunkMinSize()
        {
            if (!isConnectedMode())
```

### FinalPrivateMethod
'private' method declared `final`
in `impl/src/main/java/org/apache/myfaces/util/lang/StreamCharBuffer.java`
#### Snippet
```java
        }

        private final boolean shouldWriteDirectly(final int len)
        {
            if (!isConnectedMode())
```

### FinalPrivateMethod
'private' method declared `final`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TextUnit.java`
#### Snippet
```java
    }

    private final static String trimRight(String s)
    {
        int i = s.length() - 1;
```

### FinalPrivateMethod
'private' method declared `final`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TagLibraryConfigUnmarshallerImpl.java`
#### Snippet
```java
    }    
    
    private static final SAXParser createSAXParser(LibraryHandler handler, ExternalContext externalContext,
                                                   boolean schemaValidating)
            throws SAXException, ParserConfigurationException
```

### FinalPrivateMethod
'private' method declared `final`
in `impl/src/main/java/org/apache/myfaces/application/ApplicationImpl.java`
#### Snippet
```java
     * @throws FacesException 
     */
    private final UIComponent createComponent(FacesContext facesContext, 
            final String componentType) throws FacesException
    {
```

### FinalPrivateMethod
'private' method declared `final`
in `test/src/main/java/org/apache/myfaces/test/mock/MockServletContext.java`
#### Snippet
```java
    // --------------------------------------------------------- Private Methods

    private final URI resolve(String path)
    {
        if (path == null)
```

## RuleId[ruleID=SizeReplaceableByIsEmpty]
### SizeReplaceableByIsEmpty
`((String)label).length() == 0` can be replaced with '((String)label).isEmpty()'
in `api/src/main/java/org/apache/myfaces/core/api/shared/MessageUtils.java`
#### Snippet
```java
        ValueExpression expression = null;
        if (label != null && 
            label instanceof String && ((String)label).length() == 0 )
        {
            // Note component.getAttributes().get("label") internally try to 
```

### SizeReplaceableByIsEmpty
`locale.getVariant().length() > 0` can be replaced with '!locale.getVariant().isEmpty()'
in `api/src/main/java/org/apache/myfaces/core/api/shared/lang/LocaleUtils.java`
#### Snippet
```java
        {
            list.add(locale);
            if (locale.getVariant().length() > 0)
            {
                list.add(new Locale(locale.getLanguage(), locale.getCountry()));
```

### SizeReplaceableByIsEmpty
`locale.getCountry().length() > 0` can be replaced with '!locale.getCountry().isEmpty()'
in `api/src/main/java/org/apache/myfaces/core/api/shared/lang/LocaleUtils.java`
#### Snippet
```java
                list.add(new Locale(locale.getLanguage(), locale.getCountry()));
            }
            if (locale.getCountry().length() > 0)
            {
                list.add(new Locale(locale.getLanguage(), ""));
```

### SizeReplaceableByIsEmpty
`locale.getCountry().length() != 0` can be replaced with '!locale.getCountry().isEmpty()'
in `api/src/main/java/org/apache/myfaces/core/api/shared/lang/LocaleUtils.java`
#### Snippet
```java
                Locale locale = locales.get(i);
                if (languageCode.equals(locale.getLanguage()) &&
                        locale.getCountry().length() != 0 &&
                        locale.getVariant().length() == 0)
                {
```

### SizeReplaceableByIsEmpty
`locale.getVariant().length() == 0` can be replaced with 'locale.getVariant().isEmpty()'
in `api/src/main/java/org/apache/myfaces/core/api/shared/lang/LocaleUtils.java`
#### Snippet
```java
                if (languageCode.equals(locale.getLanguage()) &&
                        locale.getCountry().length() != 0 &&
                        locale.getVariant().length() == 0)
                {
                    countries.add(locale);
```

### SizeReplaceableByIsEmpty
`locale.getVariant().length() == 0` can be replaced with 'locale.getVariant().isEmpty()'
in `api/src/main/java/org/apache/myfaces/core/api/shared/lang/LocaleUtils.java`
#### Snippet
```java
                Locale locale = locales.get(i);
                if (countryCode.equals(locale.getCountry()) &&
                        locale.getVariant().length() == 0)
                {
                    langs.add(locale);
```

### SizeReplaceableByIsEmpty
`value.length() == 0` can be replaced with 'value.isEmpty()'
in `api/src/main/java/jakarta/faces/convert/EnumConverter.java`
#### Snippet
```java
        }
        value = value.trim();
        if (value.length() == 0)
        {
            return null;
```

### SizeReplaceableByIsEmpty
`pattern.length() > 0` can be replaced with '!pattern.isEmpty()'
in `api/src/main/java/jakarta/faces/convert/DateTimeConverter.java`
#### Snippet
```java
        String type = getType();
        String pattern = getPattern();
        if (pattern != null && pattern.length() > 0)
        {
            Locale locale = getLocale();
```

### SizeReplaceableByIsEmpty
`timeStyle.length() > 0` can be replaced with '!timeStyle.isEmpty()'
in `api/src/main/java/jakarta/faces/convert/DateTimeConverter.java`
#### Snippet
```java
            {
                String timeStyle = getTimeStyle();
                if (timeStyle != null && timeStyle.length() > 0)
                {
                    formatter = DateTimeFormatter.ofLocalizedDateTime(
```

### SizeReplaceableByIsEmpty
`value.length() > 0` can be replaced with '!value.isEmpty()'
in `api/src/main/java/jakarta/faces/convert/DateTimeConverter.java`
#### Snippet
```java
        {
            value = value.trim();
            if (value.length() > 0)
            {
                if (isJava8DateTimeFormatter())
```

### SizeReplaceableByIsEmpty
`((String) value).length() == 0` can be replaced with '((String) value).isEmpty()'
in `api/src/main/java/jakarta/faces/component/UIInput.java`
#### Snippet
```java
    private boolean isEmptyString(Object value)
    {
        return ((value instanceof String) && (((String) value).length() == 0));
    }

```

### SizeReplaceableByIsEmpty
`param.length() == 0` can be replaced with 'param.isEmpty()'
in `api/src/main/java/jakarta/faces/component/UINamingContainer.java`
#### Snippet
```java
            // value of the symbolic constant SEPARATOR_CHAR_PARAM_NAME
            String param = eContext.getInitParameter(SEPARATOR_CHAR_PARAM_NAME);
            if (param == null || param.length() == 0)
            {
                // Otherwise, the value of the symbolic constant NamingContainer.SEPARATOR_CHAR must be returned.
```

### SizeReplaceableByIsEmpty
`expr.length() == 0` can be replaced with 'expr.isEmpty()'
in `api/src/main/java/jakarta/faces/component/UIComponentBase.java`
#### Snippet
```java
        Assert.notNull(expr, "expr");

        if (expr.length() == 0)
        {
            return null;
```

### SizeReplaceableByIsEmpty
`string.length() == 0` can be replaced with 'string.isEmpty()'
in `api/src/main/java/jakarta/faces/component/UIComponentBase.java`
#### Snippet
```java
        // Component identifiers must obey the following syntax restrictions:
        // 1. Must not be a zero-length String.
        if (string.length() == 0)
        {
            throw new IllegalArgumentException("component identifier must not be a zero-length String");
```

### SizeReplaceableByIsEmpty
`((String) value).length() == 0` can be replaced with '((String) value).isEmpty()'
in `api/src/main/java/jakarta/faces/validator/RequiredValidator.java`
#### Snippet
```java
        //Check if the value is empty like UIInput.validateValue
        boolean empty = value == null
                || (value instanceof String && ((String) value).length() == 0);

        if (empty)
```

### SizeReplaceableByIsEmpty
`_anyPhase.size() > 0` can be replaced with '!_anyPhase.isEmpty()'
in `api/src/main/java/jakarta/faces/component/UIViewRoot.java`
#### Snippet
```java
        public boolean hasMoreEvents()
        {
            return (_anyPhase != null && _anyPhase.size() > 0) || (_onPhase != null && _onPhase.size() > 0); 
        }

```

### SizeReplaceableByIsEmpty
`_onPhase.size() > 0` can be replaced with '!_onPhase.isEmpty()'
in `api/src/main/java/jakarta/faces/component/UIViewRoot.java`
#### Snippet
```java
        public boolean hasMoreEvents()
        {
            return (_anyPhase != null && _anyPhase.size() > 0) || (_onPhase != null && _onPhase.size() > 0); 
        }

```

### SizeReplaceableByIsEmpty
`parameters.size() > 0` can be replaced with '!parameters.isEmpty()'
in `api/src/main/java/jakarta/faces/application/_NavigationUtils.java`
#### Snippet
```java
    {
        Map<String,List<String>> evaluatedParameters = null;
        if (parameters != null && parameters.size() > 0)
        {
            evaluatedParameters = new HashMap<>(5, 1f);
```

### SizeReplaceableByIsEmpty
`_condition.length() > 0` can be replaced with '!_condition.isEmpty()'
in `api/src/main/java/jakarta/faces/application/NavigationCase.java`
#### Snippet
```java
    public boolean hasCondition()
    {
        return _condition != null && _condition.length() > 0;
    }

```

### SizeReplaceableByIsEmpty
`name.length() > 0` can be replaced with '!name.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/spi/impl/ResourceAnnotationInjectionProvider.java`
#### Snippet
```java
        Object lookedupResource;

        if ((name != null) && (name.length() > 0))
        {
            // TODO local or global JNDI
```

### SizeReplaceableByIsEmpty
`name.length() > 0` can be replaced with '!name.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/spi/impl/ResourceAnnotationInjectionProvider.java`
#### Snippet
```java
        Object lookedupResource;

        if ((name != null) && (name.length() > 0))
        {
            // TODO local or global JNDI
```

### SizeReplaceableByIsEmpty
`serviceImplName.length() != 0` can be replaced with '!serviceImplName.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/spi/impl/DefaultServiceProviderFinder.java`
#### Snippet
```java
                                serviceImplName = serviceImplName.trim();

                                if (serviceImplName.length() != 0)
                                {
                                    results.add(serviceImplName);
```

### SizeReplaceableByIsEmpty
`pattern.length() == 0` can be replaced with 'pattern.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/util/UrlPatternMatcher.java`
#### Snippet
```java
        {
            pattern = pattern.substring(0, pattern.length() - 2);
            if (pattern.length() == 0)
            {
                return true;  // "/*" is the same as "/"
```

### SizeReplaceableByIsEmpty
`_condition.length() > 0` can be replaced with '!_condition.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/flow/NavigationCaseImpl.java`
#### Snippet
```java
    public boolean hasCondition()
    {
        return _condition != null && _condition.length() > 0;
    }

```

### SizeReplaceableByIsEmpty
`id.length() == 0` can be replaced with 'id.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/flow/FlowHandlerImpl.java`
#### Snippet
```java
            throw new IllegalArgumentException("Flow must have a non null id");
        }
        else if (id.length() == 0)
        {
            throw new IllegalArgumentException("Flow must have a non empty id");
```

### SizeReplaceableByIsEmpty
`currentFlowStack.size() > 0` can be replaced with '!currentFlowStack.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/flow/FlowHandlerImpl.java`
#### Snippet
```java
            return null;
        }
        return currentFlowStack.size() > 0 ? currentFlowStack.remove(currentFlowStack.size()-1) : null;
    }
    
```

### SizeReplaceableByIsEmpty
`currentFlowStack.size() > 0` can be replaced with '!currentFlowStack.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/flow/FlowHandlerImpl.java`
#### Snippet
```java
                return null;
            }
            return currentFlowStack.size() > 0 ? 
                currentFlowStack.get(currentFlowStack.size()-1) : null;
        }
```

### SizeReplaceableByIsEmpty
`param.length() == 0` can be replaced with 'param.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/util/WebConfigParamUtils.java`
#### Snippet
```java

        param = param.trim();
        if (param.length() == 0)
        {
            return defaultValue;
```

### SizeReplaceableByIsEmpty
`param.length() == 0` can be replaced with 'param.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/util/WebConfigParamUtils.java`
#### Snippet
```java

        param = param.trim();
        if (param.length() == 0)
        {
            return defaultValue;
```

### SizeReplaceableByIsEmpty
`((String)label).length() == 0` can be replaced with '((String)label).isEmpty()'
in `impl/src/main/java/org/apache/myfaces/util/MessageUtils.java`
#### Snippet
```java
        ValueExpression expression = null;
        if (label != null && 
            label instanceof String && ((String)label).length() == 0 )
        {
            // Note component.getAttributes().get("label") internally try to 
```

### SizeReplaceableByIsEmpty
`jvmRoute.length() > 0` can be replaced with '!jvmRoute.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/util/token/SessionIdGenerator.java`
#### Snippet
```java
        }

        if (jvmRoute != null && jvmRoute.length() > 0)
        {
            buffer.append('.').append(jvmRoute);
```

### SizeReplaceableByIsEmpty
`path.length() == 0` can be replaced with 'path.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletsVDLUtils.java`
#### Snippet
```java
    {
        // Normalize the argument strings
        if (path == null || path.length() == 0)
        {
            path = "*";
```

### SizeReplaceableByIsEmpty
`pattern.length() == 0` can be replaced with 'pattern.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletsVDLUtils.java`
#### Snippet
```java
            path = "*";
        }
        if (pattern == null || pattern.length() == 0)
        {
            pattern = "*";
```

### SizeReplaceableByIsEmpty
`pattern.length() == 0` can be replaced with 'pattern.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletsVDLUtils.java`
#### Snippet
```java
        {
            pattern = pattern.substring(0, pattern.length() - 2);
            if (pattern.length() == 0)
            {
                return true;  // "/*" is the same as "/"
```

### SizeReplaceableByIsEmpty
`suffix.length() == 0` can be replaced with 'suffix.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguageStrategy.java`
#### Snippet
```java
        {
            suffix = suffix.trim();
            if (suffix.length() == 0)
            {
                suffix = ViewHandler.DEFAULT_FACELETS_SUFFIX;
```

### SizeReplaceableByIsEmpty
`mappings.length() == 0` can be replaced with 'mappings.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguageStrategy.java`
#### Snippet
```java
        // Make sure the mappings contain something
        mappings = mappings.trim();
        if (mappings.length() == 0)
        {
            return null;
```

### SizeReplaceableByIsEmpty
`str.length() > 0` can be replaced with '!str.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/util/lang/StreamCharBuffer.java`
#### Snippet
```java
        String str = in.readUTF();
        reset();
        if (str.length() > 0)
        {
            addChunk(new StringChunk(str, 0, str.length()));
```

### SizeReplaceableByIsEmpty
`str.length() > 0` can be replaced with '!str.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/util/lang/StreamCharBuffer.java`
#### Snippet
```java
            // if there are no readers, the result can be cached
            reset();
            if (str.length() > 0)
            {
                addChunk(new StringChunk(str, 0, str.length()));
```

### SizeReplaceableByIsEmpty
`attribute.getNamespace().length() > 0` can be replaced with '!attribute.getNamespace().isEmpty()'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/MetaRulesetImpl.java`
#### Snippet
```java
                // The fastest check is check if the length is > 0, because
                // most attributes usually has no namespace attached.
                if (attribute.getNamespace().length() > 0 &&
                    (PassThroughLibrary.NAMESPACE.equals(attribute.getNamespace())
                        || PassThroughLibrary.JCP_NAMESPACE.equals(attribute.getNamespace())
```

### SizeReplaceableByIsEmpty
`_passthroughRules.size() > 0` can be replaced with '!_passthroughRules.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/MetaRulesetImpl.java`
#### Snippet
```java

        if (_passthroughAttributes.length > 0 &&
            _passthroughRules.size() > 0)
        {
            if (target == null)
```

### SizeReplaceableByIsEmpty
`in.length() == 0` can be replaced with 'in.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ELText.java`
#### Snippet
```java
    public static ELText parseAllowEmptyString(String in, Location location) throws ELException
    {
        if (in != null && in.length() == 0)
        {
            return new ELText(in);
```

### SizeReplaceableByIsEmpty
`localName.length() > 0` can be replaced with '!localName.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/TagAttributeImpl.java`
#### Snippet
```java
        // localName is null or empty and if that so, assign it from the qName 
        // (if localName is empty it is not prefixed, so it is save to set it directly). 
        this.localName = (localName == null) ? qName : ((localName.length() > 0) ? localName : qName);
        this.qName = qName;
        this.value = value;
```

### SizeReplaceableByIsEmpty
`scopeStr.length() == 0` can be replaced with 'scopeStr.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/LegacySetHandler.java`
#### Snippet
```java

                // Check scope string
                if (scopeStr == null || scopeStr.length() == 0)
                {
                    throw new TagException(tag, "scope must not be empty");
```

### SizeReplaceableByIsEmpty
`scopeStr.length() == 0` can be replaced with 'scopeStr.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/SetHandler.java`
#### Snippet
```java

                // Check scope string
                if (scopeStr == null || scopeStr.length() == 0)
                {
                    throw new TagException(tag, "scope must not be empty");
```

### SizeReplaceableByIsEmpty
`attributeJcpNamespace.length() > 0` can be replaced with '!attributeJcpNamespace.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/html/DefaultTagDecorator.java`
#### Snippet
```java
                     String attributeNS = attributeNamespace;
                     TagAttribute attr = tag.getAttributes().get(attributeNS, attributeLocalName);
                     if (attr == null && attributeJcpNamespace.length() > 0)
                     {
                         attributeNS = attributeJcpNamespace;
```

### SizeReplaceableByIsEmpty
`attributeSunNamespace.length() > 0` can be replaced with '!attributeSunNamespace.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/html/DefaultTagDecorator.java`
#### Snippet
```java
                         attr = tag.getAttributes().get(attributeNS, attributeLocalName);
                     }
                     if (attr == null && attributeSunNamespace.length() > 0)
                     {
                         attributeNS = attributeSunNamespace;
```

### SizeReplaceableByIsEmpty
`tagAttribute.getNamespace().length() == 0` can be replaced with 'tagAttribute.getNamespace().isEmpty()'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/html/DefaultTagDecorator.java`
#### Snippet
```java
                    convertedNamespace, tagAttribute.getLocalName(), qname, tagAttribute.getValue());
            }
            else if (tagAttribute.getNamespace().length() == 0)
            {
                // "... If the current attribute's namespace is empty 
```

### SizeReplaceableByIsEmpty
`mappings.length() == 0` can be replaced with 'mappings.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeResourceLibrary.java`
#### Snippet
```java
        // Make sure the mappings contain something
        mappings = mappings.trim();
        if (mappings.length() == 0)
        {
            return null;
```

### SizeReplaceableByIsEmpty
`suffix.length() == 0` can be replaced with 'suffix.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeResourceLibrary.java`
#### Snippet
```java
        {
            suffix = suffix.trim();
            if (suffix.length() == 0)
            {
                suffix = ViewHandler.DEFAULT_FACELETS_SUFFIX;
```

### SizeReplaceableByIsEmpty
`children.size() > 0` can be replaced with '!children.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeComponentResourceTagHandler.java`
#### Snippet
```java
                            children.add(innerCompositeComponent.getChildren().remove(0));
                        }
                        while (children.size() > 0)
                        {
                            UIComponent child = children.remove(0);
```

### SizeReplaceableByIsEmpty
`children.size() > 0` can be replaced with '!children.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeComponentResourceTagHandler.java`
#### Snippet
```java
                        }
                    }
                    while (children.size() > 0)
                    {
                        UIComponent child = children.remove(0);
```

### SizeReplaceableByIsEmpty
`base.length() > 0` can be replaced with '!base.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/SectionUniqueIdCounter.java`
#### Snippet
```java
        }

        if (base != null && base.length() > 0)
        {
            _builder.append('_');
```

### SizeReplaceableByIsEmpty
`this.namespaces.size() > 0` can be replaced with '!this.namespaces.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/NamespaceManager.java`
#### Snippet
```java
    {
        NamespaceUnit unit = new NamespaceUnit(library);
        if (this.namespaces.size() > 0)
        {
            NS ns = null;
```

### SizeReplaceableByIsEmpty
`suffix.length() == 0` can be replaced with 'suffix.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TagLibraryConfig.java`
#### Snippet
```java
            {
                suffix = suffix.trim();
                if (suffix.length() == 0)
                {
                    suffix = ViewHandler.DEFAULT_FACELETS_SUFFIX;
```

### SizeReplaceableByIsEmpty
`mappings.length() == 0` can be replaced with 'mappings.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TagLibraryConfig.java`
#### Snippet
```java
            // Make sure the mappings contain something
            mappings = mappings.trim();
            if (mappings.length() == 0)
            {
                return null;
```

### SizeReplaceableByIsEmpty
`value.length() == 0` can be replaced with 'value.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/CompilationManager.java`
#### Snippet
```java

        // don't carelessly add empty tags
        if (value.length() == 0)
        {
            return;
```

### SizeReplaceableByIsEmpty
`text.length() == 0` can be replaced with 'text.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/CompilationManager.java`
#### Snippet
```java

        // don't carelessly add empty tags
        if (text.length() == 0)
        {
            return;
```

### SizeReplaceableByIsEmpty
`value.length() == 0` can be replaced with 'value.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/CompilationManager.java`
#### Snippet
```java

        // don't carelessly add empty tags
        if (value.length() == 0)
        {
            return;
```

### SizeReplaceableByIsEmpty
`this.decorators.size() > 0` can be replaced with '!this.decorators.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/Compiler.java`
#### Snippet
```java
    public final TagDecorator createTagDecorator()
    {
        if (this.decorators.size() > 0)
        {
            return new BaseMultipleTagDecorator(new DefaultTagDecorator(), 
```

### SizeReplaceableByIsEmpty
`this.libraries.size() > 0` can be replaced with '!this.libraries.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/Compiler.java`
#### Snippet
```java
    public final TagLibrary createTagLibrary()
    {
        if (this.libraries.size() > 0)
        {
            return new CompositeTagLibrary(this.libraries.toArray(new TagLibrary[this.libraries.size()]));
```

### SizeReplaceableByIsEmpty
`prefix.length() > 0` can be replaced with '!prefix.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/view/facelets/util/Classpath.java`
#### Snippet
```java
        }

        if (!done && prefix.length() > 0)
        {
            // we add '/' at the end since join adds it as well
```

### SizeReplaceableByIsEmpty
`text.length() > 0` can be replaced with '!text.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TextUnit.java`
#### Snippet
```java
            }
            
            if (text != null && text.length() > 0)
            {
                int firstCharLocation = -1;
```

### SizeReplaceableByIsEmpty
`this.tags.size() > 0` can be replaced with '!this.tags.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TextUnit.java`
#### Snippet
```java
    private void finishStartTag()
    {
        if (this.tags.size() > 0 && this.startTagOpen)
        {
            this.buffer.append('>');
```

### SizeReplaceableByIsEmpty
`this.tags.size() > 0` can be replaced with '!this.tags.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TextUnit.java`
#### Snippet
```java
            catch (ELException e)
            {
                if (this.tags.size() > 0)
                {
                    throw new TagException((Tag) this.tags.peek(), e.getMessage());
```

### SizeReplaceableByIsEmpty
`s.length() > 0` can be replaced with '!s.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TextUnit.java`
#### Snippet
```java
                s = trimRight(s);
            }
            if (s.length() > 0)
            {
                if (!compressSpaces)
```

### SizeReplaceableByIsEmpty
`s.length() > 0` can be replaced with '!s.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TextUnit.java`
#### Snippet
```java
                    // First check if the text contains EL before build something, and if contains 
                    // an EL expression, compress it before build the ELText.
                    if (s != null && s.length() > 0)
                    {
                        if (ELText.isLiteral(s))
```

### SizeReplaceableByIsEmpty
`instructionBuffer.size() > 0` can be replaced with '!instructionBuffer.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TextUnit.java`
#### Snippet
```java
                        else
                        {
                            if (instructionBuffer.size() > 0 && 
                                !(instructionBuffer.get(instructionBuffer.size()-1) instanceof LiteralXMLInstruction))
                            {
```

### SizeReplaceableByIsEmpty
`s.length() == 0` can be replaced with 's.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TagLibraryConfigUnmarshallerImpl.java`
#### Snippet
```java
        {
            String s = this.buffer.toString().trim();
            if (s.length() == 0)
            {
                throw new Exception("Value Cannot be Empty");
```

### SizeReplaceableByIsEmpty
`s.length() == 0` can be replaced with 's.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TagLibraryConfigUnmarshallerImpl.java`
#### Snippet
```java
        {
            String s = this.buffer.toString().trim();
            if (s.length() == 0)
            {
                //if is "" just set null instead
```

### SizeReplaceableByIsEmpty
`attrName.length() > 0` can be replaced with '!attrName.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/config/ConfigFilesXmlValidationUtils.java`
#### Snippet
```java
                    String attrName = attributes.getLocalName(i);
                    attrName = attrName != null
                            ? ((attrName.length() > 0) ? attrName : attributes.getQName(i))
                            : attributes.getQName(i);
                    if (attrName.equals("version"))
```

### SizeReplaceableByIsEmpty
`attrName.length() > 0` can be replaced with '!attrName.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/config/ConfigFilesXmlValidationUtils.java`
#### Snippet
```java
                {
                    String attrName = attributes.getLocalName(i);
                    attrName = (attrName != null) ? ((attrName.length() > 0) ? attrName
                            : attributes.getQName(i))
                            : attributes.getQName(i);
```

### SizeReplaceableByIsEmpty
`systemEventListener.getSourceClass().length() > 0` can be replaced with '!systemEventListener.getSourceClass().isEmpty()'
in `impl/src/main/java/org/apache/myfaces/config/FacesConfigurator.java`
#### Snippet
```java
                        ClassUtils.newInstance(systemEventListener.getSystemEventListenerClass());
                _callInjectAndPostConstruct(listener);
                if (systemEventListener.getSourceClass() != null && systemEventListener.getSourceClass().length() > 0)
                {
                    application.subscribeToEvent(
```

### SizeReplaceableByIsEmpty
`faceletsProcessing.getFileExtension().length() > 0` can be replaced with '!faceletsProcessing.getFileExtension().isEmpty()'
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigDispenserImpl.java`
#### Snippet
```java
            for (FaceletsProcessing faceletsProcessing : extension.getFaceletsProcessingList())
            {
                if (faceletsProcessing.getFileExtension() != null && faceletsProcessing.getFileExtension().length() > 0)
                {
                    faceletsProcessingByFileExtension.put(faceletsProcessing.getFileExtension(), faceletsProcessing);
```

### SizeReplaceableByIsEmpty
`target.length() > 0` can be replaced with '!target.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/context/RequestViewMetadata.java`
#### Snippet
```java
            {
                String target = annotation.target();
                if (target != null && target.length() > 0)
                {
                    target = ELText.parseAsString(context.getApplication().getExpressionFactory(),
```

### SizeReplaceableByIsEmpty
`target.length() > 0` can be replaced with '!target.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/context/RequestViewMetadata.java`
#### Snippet
```java
            {
                String target = annotation.target();
                if (target != null && target.length() > 0)
                {
                    target = ELText.parseAsString(context.getApplication().getExpressionFactory(),
```

### SizeReplaceableByIsEmpty
`conv.value().length() > 0` can be replaced with '!conv.value().isEmpty()'
in `impl/src/main/java/org/apache/myfaces/config/annotation/AnnotationConfigurator.java`
#### Snippet
```java
                    //the entry there takes precedence
                    boolean hasForClass = !Object.class.equals(conv.forClass());
                    boolean hasValue = conv.value().length() > 0;
                    if (hasForClass || hasValue)
                    {
```

### SizeReplaceableByIsEmpty
`getLocalePrefix().length() > 0` can be replaced with '!getLocalePrefix().isEmpty()'
in `impl/src/main/java/org/apache/myfaces/resource/AliasResourceMetaImpl.java`
#### Snippet
```java
            StringBuilder builder = new StringBuilder();
            boolean firstSlashAdded = false;
            if (getLocalePrefix() != null && getLocalePrefix().length() > 0)
            {
                builder.append(getLocalePrefix());
```

### SizeReplaceableByIsEmpty
`evalScripts.size() > 0` can be replaced with '!evalScripts.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/context/servlet/PartialViewContextImpl.java`
#### Snippet
```java
                
                List<String> evalScripts = pvc.getEvalScripts();
                if (evalScripts != null && evalScripts.size() > 0)
                {
                    for (String script : evalScripts)
```

### SizeReplaceableByIsEmpty
`clientId.length() > 0` can be replaced with '!clientId.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/context/servlet/PartialViewContextImpl.java`
#### Snippet
```java
                for (String clientId : clientIds)
                {
                    if (clientId.length() > 0)
                    {
                        tempList.add(clientId);
```

### SizeReplaceableByIsEmpty
`clientId.length() > 0` can be replaced with '!clientId.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/context/servlet/PartialViewContextImpl.java`
#### Snippet
```java
                for (String clientId : clientIds)
                {
                    if (clientId.length() > 0)
                    {
                        tempList.add(clientId);
```

### SizeReplaceableByIsEmpty
`p.length() > 0` can be replaced with '!p.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/resource/ResourceLoaderUtils.java`
#### Snippet
```java
        for (String p : paths)
        {
            if (p != null && p.length() > 0)
            {
                depth++;
```

### SizeReplaceableByIsEmpty
`_prefix.length() > 0` can be replaced with '!_prefix.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/resource/ResourceMetaImpl.java`
#### Snippet
```java
            StringBuilder builder = new StringBuilder();
            boolean firstSlashAdded = false;
            if (_prefix != null && _prefix.length() > 0)
            {
                builder.append(_prefix);
```

### SizeReplaceableByIsEmpty
`resourceId.length() > 0` can be replaced with '!resourceId.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/resource/ResourceValidationUtils.java`
#### Snippet
```java
        // Follow the same rules as for resourceName, but check resourceId does not
        // start with '/'
        return resourceId.length() > 0 && resourceId.charAt(0) != '/' && 
            validateResourceName(resourceId, true); 
    }
```

### SizeReplaceableByIsEmpty
`o1.length() > 0` can be replaced with '!o1.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/resource/ResourceLoader.java`
#### Snippet
```java
                if (i1 < 0)
                {
                    if (o1.length() > 0)
                    {
                        p1 = false;
```

### SizeReplaceableByIsEmpty
`o2.length() > 0` can be replaced with '!o2.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/resource/ResourceLoader.java`
#### Snippet
```java
                if (i2 < 0)
                {
                    if (o2.length() > 0)
                    {
                        p2 = false;
```

### SizeReplaceableByIsEmpty
`parameters.size() > 0` can be replaced with '!parameters.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/context/servlet/ServletExternalContextImpl.java`
#### Snippet
```java

        //add/update with new params on the paramMap
        if (parameters != null && parameters.size() > 0)
        {
            for (Map.Entry<String, List<String>> pair : parameters.entrySet())
```

### SizeReplaceableByIsEmpty
`paramMap.size() > 0` can be replaced with '!paramMap.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/context/servlet/ServletExternalContextImpl.java`
#### Snippet
```java
        }

        boolean hasParams = paramMap != null && paramMap.size() > 0;

        if (!hasParams && fragment == null) 
```

### SizeReplaceableByIsEmpty
`token.length() > 0` can be replaced with '!token.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/lifecycle/RestoreViewExecutor.java`
#### Snippet
```java
            String token = (String) facesContext.getExternalContext().getRequestParameterMap().get(
                ResponseStateManager.NON_POSTBACK_VIEW_TOKEN_PARAM);
            if (token != null && token.length() > 0)
            {
                String renderKitId = null;
```

### SizeReplaceableByIsEmpty
`bufferString.length() > 0` can be replaced with '!bufferString.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/component/search/SearchExpressionHandlerImpl.java`
#### Snippet
```java
                    // lets add token inside buffer to our tokens
                    String bufferString = buffer.toString().trim();
                    if (bufferString.length() > 0)
                    {
                        tokens.add(bufferString);
```

### SizeReplaceableByIsEmpty
`((String) encodedState).length() == 0` can be replaced with '((String) encodedState).isEmpty()'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlResponseStateManager.java`
#### Snippet
```java
        Object encodedState = 
            facesContext.getExternalContext().getRequestParameterMap().get(ResponseStateManager.VIEW_STATE_PARAM);
        if(encodedState==null || (((String) encodedState).length() == 0))
        {
            return null;
```

### SizeReplaceableByIsEmpty
`((String) encodedState).length() == 0` can be replaced with '((String) encodedState).isEmpty()'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlResponseStateManager.java`
#### Snippet
```java
            String encodedState = 
                context.getExternalContext().getRequestParameterMap().get(ResponseStateManager.VIEW_STATE_PARAM);
            if (encodedState == null || ((String) encodedState).length() == 0)
            {
                return false;
```

### SizeReplaceableByIsEmpty
`publicValue.length() > 0` can be replaced with '!publicValue.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlDoctypeRenderer.java`
#### Snippet
```java
        writer.write((String) attributes.get("rootElement"));
        String publicValue = (String) attributes.get("public"); 
        if (publicValue != null && publicValue.length() > 0)
        {
            writer.write(" PUBLIC \"");
```

### SizeReplaceableByIsEmpty
`systemValue.length() > 0` can be replaced with '!systemValue.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlDoctypeRenderer.java`
#### Snippet
```java
        }
        String systemValue = (String) attributes.get("system");
        if (systemValue != null && systemValue.length() > 0)
        {
            writer.write(" \"");
```

### SizeReplaceableByIsEmpty
`cmdName.length() > 0` can be replaced with '!cmdName.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlCommandScriptRenderer.java`
#### Snippet
```java
        String cmdName = commandScript.getName();
        String name;
        if (cmdName != null && cmdName.length() > 0)
        {
            name = JavascriptUtils.getValidJavascriptName(cmdName, true);
```

### SizeReplaceableByIsEmpty
`resourceName.length() > 0` can be replaced with '!resourceName.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/renderkit/RendererUtils.java`
#### Snippet
```java
        final Map<String, Object> attributes = component.getAttributes();
        final String resourceName = (String) attributes.get(ComponentAttrs.NAME_ATTR);
        if (resourceName != null && (resourceName.length() > 0))
        {

```

### SizeReplaceableByIsEmpty
`libraryName.length() > 0` can be replaced with '!libraryName.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/renderkit/RendererUtils.java`
#### Snippet
```java

            final String libraryName = (String) component.getAttributes().get(ComponentAttrs.LIBRARY_ATTR);
            if ((libraryName != null) && (libraryName.length() > 0))
            {
                resource = resourceHandler.createResource(resourceName, libraryName);
```

### SizeReplaceableByIsEmpty
`uri.length() == 0` can be replaced with 'uri.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/renderkit/RendererUtils.java`
#### Snippet
```java
        // EL implementation turns null into ""
        // see http://www.irian.at/blog/blogid/unifiedElCoercion/#unifiedElCoercion
        if (uri.length() == 0)
        {
            return null;
```

### SizeReplaceableByIsEmpty
`str.length() > 0` can be replaced with '!str.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlResponseWriterImpl.java`
#### Snippet
```java
        // empty string commonly used to force the start tag to be closed.
        // in such case, do not call down the writer chain
        if (str != null && str.length() > 0)
        {
            // Don't bother encoding anything if chosen character encoding is UTF-8
```

### SizeReplaceableByIsEmpty
`value.length() > 0` can be replaced with '!value.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlCheckboxRendererBase.java`
#### Snippet
```java
        }

        if ((value != null) && (value.length() > 0))
        {
            writer.writeAttribute(HTML.VALUE_ATTR, value, null);
```

### SizeReplaceableByIsEmpty
`group.length() > 0` can be replaced with '!group.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlRadioRendererBase.java`
#### Snippet
```java
        }

        if (group != null && group.length() > 0)
        {
            if (value != null)
```

### SizeReplaceableByIsEmpty
`group.length() > 0` can be replaced with '!group.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlRadioRendererBase.java`
#### Snippet
```java
                    selectItem, disabled);

            if (group != null && group.length() > 0)
            {
                //no op
```

### SizeReplaceableByIsEmpty
`onClick.length() != 0` can be replaced with '!onClick.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlButtonRendererBase.java`
#### Snippet
```java
                String onClick = buildBehaviorizedOnClick(uiComponent, behaviors, facesContext, writer,
                        form, validParams);
                if (onClick.length() != 0)
                {
                    writer.writeAttribute(HTML.ONCLICK_ATTR, onClick, null);
```

### SizeReplaceableByIsEmpty
`onClick.length() != 0` can be replaced with '!onClick.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlButtonRendererBase.java`
#### Snippet
```java
                        ClientBehaviorEvents.CLICK, paramList, ClientBehaviorEvents.ACTION, paramList, behaviors,
                        commandOnclick , null);
                if (onClick.length() != 0)
                {
                    writer.writeAttribute(HTML.ONCLICK_ATTR, onClick, null);
```

### SizeReplaceableByIsEmpty
`fragment.length() > 0` can be replaced with '!fragment.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/OutcomeTargetUtils.java`
#### Snippet
```java
            fragment = fragment.trim();

            if (fragment.length() > 0)
            {
                href += '#' + fragment;
```

### SizeReplaceableByIsEmpty
`uiParams.size() > 0` can be replaced with '!uiParams.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/AjaxScriptBuilder.java`
#### Snippet
```java
                }

                if (uiParams != null && uiParams.size() > 0)
                {
                    for (int i = 0, size = uiParams.size(); i < size; i++)
```

### SizeReplaceableByIsEmpty
`supportedLocale.getCountry().length() == 0` can be replaced with 'supportedLocale.getCountry().isEmpty()'
in `impl/src/main/java/org/apache/myfaces/application/ViewHandlerImpl.java`
#### Snippet
```java
                // that occurs further down (see JSTL Reference 1.0 8.3.1)
                if (requestLocale.getLanguage().equals(supportedLocale.getLanguage())
                        && (supportedLocale.getCountry() == null || supportedLocale.getCountry().length() == 0))
                {
                    return supportedLocale;
```

### SizeReplaceableByIsEmpty
`path.length() > 0` can be replaced with '!path.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/application/ViewHandlerImpl.java`
#### Snippet
```java
        Assert.notNull(facesContext, "facesContext");
        Assert.notNull(path, "path");
        if (path.length() > 0 && path.charAt(0) == '/')
        {
            String contextPath = facesContext.getExternalContext().getRequestContextPath();
```

### SizeReplaceableByIsEmpty
`highlightId.size() > 0` can be replaced with '!highlightId.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/renderkit/ErrorPageWriter.java`
#### Snippet
```java
        if (highlightId != null)
        {
            if ((highlightId.size() > 0))
            {
                String id = c.getId();
```

### SizeReplaceableByIsEmpty
`list.size() > 0` can be replaced with '!list.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/renderkit/ErrorPageWriter.java`
#### Snippet
```java
        }

        if (list != null && list.size() > 0)
        {
            return list;
```

### SizeReplaceableByIsEmpty
`resourceName.length() == 0` can be replaced with 'resourceName.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
        Resource resource = null;
        
        if (resourceName.length() == 0)
        {
            return null;
```

### SizeReplaceableByIsEmpty
`localePrefix.length() == 0` can be replaced with 'localePrefix.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java

        // If some variable is "" set it as null.
        if (localePrefix != null && localePrefix.length() == 0)
        {
            localePrefix = null;
```

### SizeReplaceableByIsEmpty
`libraryName.length() == 0` can be replaced with 'libraryName.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
            localePrefix = null;
        }
        if (libraryName != null && libraryName.length() == 0)
        {
            libraryName = null;
```

### SizeReplaceableByIsEmpty
`libraryVersion.length() == 0` can be replaced with 'libraryVersion.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
            libraryName = null;
        }
        if (libraryVersion != null && libraryVersion.length() == 0)
        {
            libraryVersion = null;
```

### SizeReplaceableByIsEmpty
`resourceName.length() == 0` can be replaced with 'resourceName.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
            libraryVersion = null;
        }
        if (resourceName != null && resourceName.length() == 0)
        {
            resourceName = null;
```

### SizeReplaceableByIsEmpty
`resourceVersion.length() == 0` can be replaced with 'resourceVersion.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
            resourceName = null;
        }
        if (resourceVersion != null && resourceVersion.length() == 0)
        {
            resourceVersion = null;
```

### SizeReplaceableByIsEmpty
`suffix.length() > 0` can be replaced with '!suffix.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
                for (String suffix : validSuffixes)
                {
                    if (suffix != null && suffix.length() > 0 && viewId.endsWith(suffix))
                    {
                        matchSuffix = true;
```

### SizeReplaceableByIsEmpty
`contentType.length() == 0` can be replaced with 'contentType.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java

        // the resource does not provide a content-type --> determine it via mime-type
        if (contentType == null || contentType.length() == 0)
        {
            String resourceName = getWrappedResourceName(resource);
```

### SizeReplaceableByIsEmpty
`localePrefix.length() == 0` can be replaced with 'localePrefix.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java

        // If some variable is "" set it as null.
        if (localePrefix != null && localePrefix.length() == 0)
        {
            localePrefix = null;
```

### SizeReplaceableByIsEmpty
`libraryName.length() == 0` can be replaced with 'libraryName.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
            localePrefix = null;
        }
        if (libraryName != null && libraryName.length() == 0)
        {
            libraryName = null;
```

### SizeReplaceableByIsEmpty
`libraryVersion.length() == 0` can be replaced with 'libraryVersion.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
            libraryName = null;
        }
        if (libraryVersion != null && libraryVersion.length() == 0)
        {
            libraryVersion = null;
```

### SizeReplaceableByIsEmpty
`resourceName.length() == 0` can be replaced with 'resourceName.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
            libraryVersion = null;
        }
        if (resourceName != null && resourceName.length() == 0)
        {
            resourceName = null;
```

### SizeReplaceableByIsEmpty
`resourceVersion.length() == 0` can be replaced with 'resourceVersion.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
            resourceName = null;
        }
        if (resourceVersion != null && resourceVersion.length() == 0)
        {
            resourceVersion = null;
```

### SizeReplaceableByIsEmpty
`temp.length() > 0` can be replaced with '!temp.isEmpty()'
in `test/src/main/java/org/apache/myfaces/test/el/MockMethodExpression.java`
#### Snippet
```java
                        .replaceAll(" ", "");
                List names = new ArrayList();
                while (temp.length() > 0)
                {
                    int period = temp.indexOf('.');
```

### SizeReplaceableByIsEmpty
`path.length() > 0` can be replaced with '!path.isEmpty()'
in `test/src/main/java/org/apache/myfaces/test/base/junit/runners/TestClassLoader.java`
#### Snippet
```java
                    path = path.substring(0, path.length() - 1);
                }
                if (path.length() > 0)
                {
                    fExcluded.addElement(path);
```

### SizeReplaceableByIsEmpty
`parameters.size() > 0` can be replaced with '!parameters.isEmpty()'
in `test/src/main/java/org/apache/myfaces/test/mock/MockExternalContext20.java`
#### Snippet
```java

        //add/update with new params on the paramMap
        if (parameters != null && parameters.size() > 0)
        {
            for (Map.Entry<String, List<String>> pair : parameters.entrySet())
```

### SizeReplaceableByIsEmpty
`pair.getKey().trim().length() != 0` can be replaced with '!pair.getKey().trim().isEmpty()'
in `test/src/main/java/org/apache/myfaces/test/mock/MockExternalContext20.java`
#### Snippet
```java
            for (Map.Entry<String, List<String>> pair : parameters.entrySet())
            {
                if (pair.getKey() != null && pair.getKey().trim().length() != 0)
                {
                    paramMap.put(pair.getKey(), pair.getValue());
```

### SizeReplaceableByIsEmpty
`paramMap.size() > 0` can be replaced with '!paramMap.isEmpty()'
in `test/src/main/java/org/apache/myfaces/test/mock/MockExternalContext20.java`
#### Snippet
```java

        //now add the updated param list onto the url
        if (paramMap.size() > 0)
        {
            boolean isFirstPair = true;
```

### SizeReplaceableByIsEmpty
`clientId.length() > 0` can be replaced with '!clientId.isEmpty()'
in `test/src/main/java/org/apache/myfaces/test/mock/MockPartialViewContext.java`
#### Snippet
```java
                for (String clientId : clientIds)
                {
                    if (clientId.length() > 0)
                    {
                        tempList.add(clientId);
```

### SizeReplaceableByIsEmpty
`clientId.length() > 0` can be replaced with '!clientId.isEmpty()'
in `test/src/main/java/org/apache/myfaces/test/mock/MockPartialViewContext.java`
#### Snippet
```java
                for (String clientId : clientIds)
                {
                    if (clientId.length() > 0)
                    {
                        tempList.add(clientId);
```

### SizeReplaceableByIsEmpty
`param.length() == 0` can be replaced with 'param.isEmpty()'
in `test/src/main/java/org/apache/myfaces/test/mock/MockClientWindowFactory.java`
#### Snippet
```java

        param = param.trim();
        if (param.length() == 0)
        {
            return defaultValue;
```

### SizeReplaceableByIsEmpty
`clientBehaviors.size() > 0` can be replaced with '!clientBehaviors.isEmpty()'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/ClientBehaviorRendererUtils.java`
#### Snippet
```java
        }

        boolean renderClientBehavior = clientBehaviors != null && clientBehaviors.size() > 0;
        if (!renderClientBehavior)
        {
```

### SizeReplaceableByIsEmpty
`param.length() == 0` can be replaced with 'param.isEmpty()'
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication20.java`
#### Snippet
```java
    private void checkEmpty(final String param, final String paramName)
    {
        if (param.length() == 0)
        {
            throw new NullPointerException("String " + paramName
```

### SizeReplaceableByIsEmpty
`name.length() > 0` can be replaced with '!name.isEmpty()'
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication20.java`
#### Snippet
```java
            // target attributes.
            String name = annotation.name();
            if (name != null && name.length() > 0)
            {
                name = _ELText.parse(getExpressionFactory(), context.getELContext(), name).toString(
```

### SizeReplaceableByIsEmpty
`library.length() > 0` can be replaced with '!library.isEmpty()'
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication20.java`
#### Snippet
```java
            // If library is the empty string, let library be null.
            String library = annotation.library();
            if (library != null && library.length() > 0)
            {
                library = _ELText.parse(getExpressionFactory(), context.getELContext(), library).toString(
```

### SizeReplaceableByIsEmpty
`target.length() > 0` can be replaced with '!target.isEmpty()'
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication20.java`
#### Snippet
```java
            // If target is the empty string, let target be null.
            String target = annotation.target();
            if (target != null && target.length() > 0)
            {
                target = _ELText.parse(getExpressionFactory(), context.getELContext(), target).toString(
```

### SizeReplaceableByIsEmpty
`name.length() > 0` can be replaced with '!name.isEmpty()'
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication20.java`
#### Snippet
```java
            // target attributes.
            String name = annotation.name();
            if (name != null && name.length() > 0)
            {
                name = _ELText.parse(getExpressionFactory(), context.getELContext(), name).toString(
```

### SizeReplaceableByIsEmpty
`library.length() > 0` can be replaced with '!library.isEmpty()'
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication20.java`
#### Snippet
```java
            // If library is the empty string, let library be null.
            String library = annotation.library();
            if (library != null && library.length() > 0)
            {
                library = _ELText.parse(getExpressionFactory(), context.getELContext(), library).toString(
```

### SizeReplaceableByIsEmpty
`target.length() > 0` can be replaced with '!target.isEmpty()'
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication20.java`
#### Snippet
```java
            // If target is the empty string, let target be null.
            String target = annotation.target();
            if (target != null && target.length() > 0)
            {
                target = _ELText.parse(getExpressionFactory(), context.getELContext(), target).toString(
```

### SizeReplaceableByIsEmpty
`((String) encodedState).length() == 0` can be replaced with '((String) encodedState).isEmpty()'
in `test/src/main/java/org/apache/myfaces/test/mock/MockResponseStateManager.java`
#### Snippet
```java
        Object encodedState = facesContext.getExternalContext()
                .getRequestParameterMap().get(VIEW_STATE_PARAM);
        if (encodedState == null || (((String) encodedState).length() == 0))
        {
            return null;
```

### SizeReplaceableByIsEmpty
`_prefix.length() > 0` can be replaced with '!_prefix.isEmpty()'
in `test/src/main/java/org/apache/myfaces/test/mock/resource/MockResourceMeta.java`
#### Snippet
```java
        StringBuilder builder = new StringBuilder();
        boolean firstSlashAdded = false;
        if (_prefix != null && _prefix.length() > 0)
        {
            builder.append(_prefix);
```

### SizeReplaceableByIsEmpty
`text.size() == 0` can be replaced with 'text.isEmpty()'
in `test/src/main/java/org/apache/myfaces/test/mock/_ELText.java`
#### Snippet
```java
        }

        if (text.size() == 0)
        {
            return null;
```

### SizeReplaceableByIsEmpty
`_prefix.length() > 0` can be replaced with '!_prefix.isEmpty()'
in `test/src/main/java/org/apache/myfaces/test/mock/resource/MockSimpleResource.java`
#### Snippet
```java
        builder.append('/');
        boolean firstSlashAdded = false;
        if (_prefix != null && _prefix.length() > 0)
        {
            builder.append(_prefix);
```

### SizeReplaceableByIsEmpty
`o1.length() > 0` can be replaced with '!o1.isEmpty()'
in `test/src/main/java/org/apache/myfaces/test/mock/resource/MockResourceLoader.java`
#### Snippet
```java
                if (i1 < 0)
                {
                    if (o1.length() > 0)
                    {
                        p1 = false;
```

### SizeReplaceableByIsEmpty
`o2.length() > 0` can be replaced with '!o2.isEmpty()'
in `test/src/main/java/org/apache/myfaces/test/mock/resource/MockResourceLoader.java`
#### Snippet
```java
                if (i2 < 0)
                {
                    if (o2.length() > 0)
                    {
                        p2 = false;
```

### SizeReplaceableByIsEmpty
`o1.length() > 0` can be replaced with '!o1.isEmpty()'
in `test/src/main/java/org/apache/myfaces/test/mock/resource/MockSimpleResourceHandler.java`
#### Snippet
```java
            if (i1 < 0)
            {
                if (o1.length() > 0)
                {
                    p1 = false;
```

### SizeReplaceableByIsEmpty
`o2.length() > 0` can be replaced with '!o2.isEmpty()'
in `test/src/main/java/org/apache/myfaces/test/mock/resource/MockSimpleResourceHandler.java`
#### Snippet
```java
            if (i2 < 0)
            {
                if (o2.length() > 0)
                {
                    p2 = false;
```

### SizeReplaceableByIsEmpty
`encoding.length() == 0` can be replaced with 'encoding.isEmpty()'
in `api/src/main/java/jakarta/faces/application/ViewHandler.java`
#### Snippet
```java
            String tempEnc = contentType.substring(indexOf); // charset=UTF-8
            encoding = tempEnc.substring(tempEnc.indexOf('=') + 1); // UTF-8
            if (encoding.length() == 0)
            {
                encoding = null;
```

### SizeReplaceableByIsEmpty
`item.getLabel().length() > 0` can be replaced with '!item.getLabel().isEmpty()'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/HtmlRendererUtils.java`
#### Snippet
```java
            writer.writeAttribute("class", labelClass, "labelClass");
        }
        if ((item.getLabel() != null) && (item.getLabel().length() > 0))
        {
            writer.write(HTML.NBSP_ENTITY);
```

### SizeReplaceableByIsEmpty
`item.getLabel().length() > 0` can be replaced with '!item.getLabel().isEmpty()'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/HtmlRendererUtils.java`
#### Snippet
```java
            writer.writeAttribute("class", labelClass, "labelClass");
        }
        if ((item.getLabel() != null) && (item.getLabel().length() > 0))
        {
            writer.write(" ");
```

## RuleId[ruleID=TrivialStringConcatenation]
### TrivialStringConcatenation
Empty string used in concatenation
in `impl/src/main/java/org/apache/myfaces/lifecycle/clientwindow/ClientConfig.java`
#### Snippet
```java
            if (r instanceof HttpServletResponse)
            {
                Cookie cookie = new Cookie(COOKIE_NAME_NOSCRIPT_ENABLED, "" + javaScriptEnabled);
                cookie.setPath("/"); // for all the server
                Object context = facesContext.getExternalContext().getContext();
```

### TrivialStringConcatenation
Empty string used in concatenation
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlRadioRendererBase.java`
#### Snippet
```java
            {
                writer.writeAttribute(HTML.VALUE_ATTR, 
                        clientId + facesContext.getNamingContainerSeparatorChar() + "", null);
            }
        }
```

### TrivialStringConcatenation
Empty string used in concatenation
in `impl/src/main/java/org/apache/myfaces/renderkit/ErrorPageWriter.java`
#### Snippet
```java
                            writer.write("<span>Row: ");
                            int rowIndex = ((UIData) parent.getParent()).getRowIndex();
                            writer.write("" + rowIndex);
                            if (rowIndex == -1)
                            {
```

## RuleId[ruleID=FinalStaticMethod]
### FinalStaticMethod
'static' method declared `final`
in `impl/src/main/java/org/apache/myfaces/util/lang/HashMapUtils.java`
#### Snippet
```java
     * @return initial capacity needed
     */
    public static final int calcCapacity(int size)
    {
        return ((size * 4) + 3) / 3;
```

### FinalStaticMethod
'static' method declared `final`
in `impl/src/main/java/org/apache/myfaces/util/lang/ClassUtils.java`
#### Snippet
```java
    }

    protected static final String paramString(Class<?>... types)
    {
        if (types != null)
```

### FinalStaticMethod
'static' method declared `final`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletFactory.java`
#### Snippet
```java
     * @param factory
     */
    public static final void setInstance(FaceletFactory factory)
    {
        if (factory == null)
```

### FinalStaticMethod
'static' method declared `final`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletFactory.java`
#### Snippet
```java
     * @return
     */
    public static final FaceletFactory getInstance()
    {
        return instance.get();
```

### FinalStaticMethod
'static' method declared `final`
in `impl/src/main/java/org/apache/myfaces/util/lang/StreamCharBuffer.java`
#### Snippet
```java
    }

    protected static final void arrayCopy(char[] src, int srcPos, char[] dest, int destPos, int length)
    {
        if (length == 1)
```

### FinalStaticMethod
'static' method declared `final`
in `impl/src/main/java/org/apache/myfaces/view/facelets/util/FaceletsTemplateMappingUtils.java`
#### Snippet
```java
public class FaceletsTemplateMappingUtils
{
    public static final boolean matchTemplate(RuntimeConfig runtimeConfig, String path)
    {
        List<FaceletsTemplateMapping> mappings = runtimeConfig.getFaceletsTemplateMappings();
```

### FinalStaticMethod
'static' method declared `final`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TextUnit.java`
#### Snippet
```java
     * @return 
     */
    final static int compressSpaces(List<Instruction> instructionBuffer, int size)
    {
        boolean addleftspace = true;
```

### FinalStaticMethod
'static' method declared `final`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TextUnit.java`
#### Snippet
```java
    }
    
    final static String compressELText(String text)
    {
        //int firstCharLocation = getFirstTextCharLocationIgnoringSpacesTabsAndCarriageReturn(text);
```

### FinalStaticMethod
'static' method declared `final`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TextUnit.java`
#### Snippet
```java
    }

    private final static String trimRight(String s)
    {
        int i = s.length() - 1;
```

### FinalStaticMethod
'static' method declared `final`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TagLibraryConfigUnmarshallerImpl.java`
#### Snippet
```java
    }    
    
    private static final SAXParser createSAXParser(LibraryHandler handler, ExternalContext externalContext,
                                                   boolean schemaValidating)
            throws SAXException, ParserConfigurationException
```

### FinalStaticMethod
'static' method declared `final`
in `impl/src/main/java/org/apache/myfaces/config/ConfigFilesXmlValidationUtils.java`
#### Snippet
```java
    }

    public static final String getFaceletTagLibVersion(URL url)
    {
        if (isTaglibDocument20OrLater(url))
```

### FinalStaticMethod
'static' method declared `final`
in `impl/src/main/java/org/apache/myfaces/config/ConfigFilesXmlValidationUtils.java`
#### Snippet
```java
    }

    public static final String getFacesConfigVersion(URL url)
    {
        String result = "4.0";
```

### FinalStaticMethod
'static' method declared `final`
in `impl/src/main/java/org/apache/myfaces/application/viewstate/StateUtils.java`
#### Snippet
```java
    }

    public static final byte[] encode(byte[] bytes)
    {
        return Base64.getEncoder().encode(bytes);
```

### FinalStaticMethod
'static' method declared `final`
in `impl/src/main/java/org/apache/myfaces/application/viewstate/StateUtils.java`
#### Snippet
```java
     * This fires during the Render Response phase, saving state.
     */
    public static final String construct(Object object, ExternalContext ctx)
    {
        byte[] bytes = getAsByteArray(object, ctx);
```

### FinalStaticMethod
'static' method declared `final`
in `impl/src/main/java/org/apache/myfaces/application/viewstate/StateUtils.java`
#### Snippet
```java
    }

    public static final byte[] decompress(byte[] bytes)
    {
        Assert.notNull(bytes, "bytes");
```

### FinalStaticMethod
'static' method declared `final`
in `impl/src/main/java/org/apache/myfaces/application/viewstate/StateUtils.java`
#### Snippet
```java
    }

    public static final byte[] compress(byte[] bytes)
    {
        
```

### FinalStaticMethod
'static' method declared `final`
in `impl/src/main/java/org/apache/myfaces/application/viewstate/StateUtils.java`
#### Snippet
```java
    }

    public static final byte[] decode(byte[] bytes)
    {
        return Base64.getDecoder().decode(bytes);
```

### FinalStaticMethod
'static' method declared `final`
in `impl/src/main/java/org/apache/myfaces/application/viewstate/StateUtils.java`
#### Snippet
```java
     * This fires during the Restore View phase, restoring state.
     */
    public static final Object reconstruct(String string, ExternalContext ctx)
    {
        byte[] bytes;
```

### FinalStaticMethod
'static' method declared `final`
in `impl/src/main/java/org/apache/myfaces/application/viewstate/StateUtils.java`
#### Snippet
```java
     * @return
     */
    public static final Object getAsObject(byte[] bytes, ExternalContext ctx)
    {
        // get the Factory that was instantiated @ startup
```

### FinalStaticMethod
'static' method declared `final`
in `impl/src/main/java/org/apache/myfaces/application/viewstate/StateUtils.java`
#### Snippet
```java
     * @return
     */
    public static final byte[] getAsByteArray(Object object, ExternalContext ctx)
    {
        // get the Factory that was instantiated @ startup
```

### FinalStaticMethod
'static' method declared `final`
in `test/src/main/java/org/apache/myfaces/test/mock/_NullEnumeration.java`
#### Snippet
```java
    private static final _NullEnumeration NULL_ENUMERATION = new _NullEnumeration();

    public static final _NullEnumeration instance()
    {
        return NULL_ENUMERATION;
```

### FinalStaticMethod
'static' method declared `final`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/HtmlRendererUtils.java`
#### Snippet
```java
     * @throws IOException
     */
    public static final boolean renderHTMLStringPreserveEmptyAttribute(ResponseWriter writer,
            UIComponent component, String componentProperty, String htmlAttrName)
            throws IOException
```

## RuleId[ruleID=EqualsBetweenInconvertibleTypes]
### EqualsBetweenInconvertibleTypes
`equals` between objects of inconvertible types 'TemplateManager' and 'Facelet'
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/TemplateContextImpl.java`
#### Snippet
```java
        {
            client = _clients.get(i);
            if (client.equals(owner))
            {
                continue;
```

## RuleId[ruleID=DuplicateCondition]
### DuplicateCondition
Duplicate condition `value instanceof Serializable`
in `api/src/main/java/jakarta/faces/component/_DeltaStateHelper.java`
#### Snippet
```java
            if (value instanceof StateHolder
                    || value instanceof List
                    || !(value instanceof Serializable))
            {
                Object savedValue = UIComponentBase.saveAttachedState(context, value);
```

### DuplicateCondition
Duplicate condition `value instanceof Serializable`
in `api/src/main/java/jakarta/faces/component/_DeltaStateHelper.java`
#### Snippet
```java
                targetMap.put(key, UIComponentBase.restoreAttachedState(context, savedValue));
            }
            else if (!(value instanceof Serializable))
            {
                Object newInstance;
```

## RuleId[ruleID=IgnoreResultOfCall]
### IgnoreResultOfCall
Result of `PushbackInputStream.available()` is ignored
in `impl/src/main/java/org/apache/myfaces/util/lang/DynamicPushbackInputStream.java`
#### Snippet
```java
    public int read(byte[] b, int off, int len) throws IOException
    {
        this.available(); // workaround for a problem in PushbackInputStream, without this, the amount of bytes read
                          // from some streams will be incorrect
        int r = super.read(b, off, len);
```

### IgnoreResultOfCall
Result of `File.listFiles()` is ignored
in `impl/src/main/java/org/apache/myfaces/config/annotation/PackageInfo.java`
#### Snippet
```java
            final File base, final ClassLoader cld, final String pckgname)
    {
        base.listFiles(new FileFilter()
        {
            @Override
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `impl/src/main/java/org/apache/myfaces/resource/TempDirFileCacheResourceLoader.java`
#### Snippet
```java
    protected void createTemporalFileVersion(FacesContext facesContext, ResourceMeta resourceMeta, File target)
    {
        target.mkdirs();  // ensure necessary directories exist
        target.delete();  // remove any existing file

```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `impl/src/main/java/org/apache/myfaces/resource/TempDirFileCacheResourceLoader.java`
#### Snippet
```java
    {
        target.mkdirs();  // ensure necessary directories exist
        target.delete();  // remove any existing file

        InputStream inputStream = null;
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `impl/src/main/java/org/apache/myfaces/resource/TempDirFileCacheResourceLoader.java`
#### Snippet
```java
        if (!imagesDir.exists())
        {
            imagesDir.mkdirs();
        }
        else
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `impl/src/main/java/org/apache/myfaces/resource/TempDirFileCacheResourceLoader.java`
#### Snippet
```java
            //Clear the cache
            deleteDir(imagesDir);
            imagesDir.mkdirs();
        }
        _tempDir = imagesDir;
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `impl/src/main/java/org/apache/myfaces/resource/TempDirFileCacheResourceLoader.java`
#### Snippet
```java
                if (!imagesDir.exists())
                {
                    imagesDir.mkdirs();
                }
                return true;
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `impl/src/main/java/org/apache/myfaces/resource/TempDirFileCacheContractResourceLoader.java`
#### Snippet
```java
    protected void createTemporalFileVersion(FacesContext facesContext, ResourceMeta resourceMeta, File target)
    {
        target.mkdirs();  // ensure necessary directories exist
        target.delete();  // remove any existing file

```

### IgnoreResultOfCall
Result of `File.delete()` is ignored
in `impl/src/main/java/org/apache/myfaces/resource/TempDirFileCacheContractResourceLoader.java`
#### Snippet
```java
    {
        target.mkdirs();  // ensure necessary directories exist
        target.delete();  // remove any existing file

        InputStream inputStream = null;
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `impl/src/main/java/org/apache/myfaces/resource/TempDirFileCacheContractResourceLoader.java`
#### Snippet
```java
        if (!imagesDir.exists())
        {
            imagesDir.mkdirs();
        }
        else
```

### IgnoreResultOfCall
Result of `File.mkdirs()` is ignored
in `impl/src/main/java/org/apache/myfaces/resource/TempDirFileCacheContractResourceLoader.java`
#### Snippet
```java
            //Clear the cache
            deleteDir(imagesDir);
            imagesDir.mkdirs();
        }
        _tempDir = imagesDir;
```

## RuleId[ruleID=UnnecessaryUnboxing]
### UnnecessaryUnboxing
Unnecessary unboxing
in `api/src/main/java/jakarta/faces/validator/LongRangeValidator.java`
#### Snippet
```java
    public long getMinimum()
    {
        return _minimum != null ? _minimum.longValue() : Long.MIN_VALUE;
    }

```

### UnnecessaryUnboxing
Unnecessary unboxing
in `impl/src/main/java/org/apache/myfaces/util/token/CsrfSessionTokenFactoryRandom.java`
#### Snippet
```java
            else
            {
                sequence = Integer.valueOf(sequence.intValue() + 1);
            }
            map.put(RendererUtils.SEQUENCE_PARAM, sequence);
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlResponseWriterImpl.java`
#### Snippet
```java
        if (value instanceof Boolean)
        {
            if (((Boolean) value).booleanValue())
            {
                // name as value for XHTML compatibility
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `test/src/main/java/org/apache/myfaces/test/mock/MockFlash.java`
#### Snippet
```java
                : previousRedirect;

        if (!previousRedirect.booleanValue() && redirect)
        {
            // This request contains a redirect. This condition is in general
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `test/src/main/java/org/apache/myfaces/test/mock/MockFlash.java`
#### Snippet
```java
                : previousKeepMessages;

        if (!previousKeepMessages.booleanValue() && keepMessages)
        {
            externalContext.getSessionMap().put(FLASH_KEEP_MESSAGES,
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `test/src/main/java/org/apache/myfaces/test/mock/resource/MockSimpleResourceHandler.java`
#### Snippet
```java
                .getRequestMap().get(IS_RESOURCE_REQUEST);

        if (value != null && value.booleanValue())
        {
            //return the saved value
```

### UnnecessaryUnboxing
Unnecessary unboxing
in `test/src/main/java/org/apache/myfaces/test/mock/resource/MockSimpleResourceHandler.java`
#### Snippet
```java
        {
            //return the saved value
            return value.booleanValue();
        }
        else
```

## RuleId[ruleID=ClassNameSameAsAncestorName]
### ClassNameSameAsAncestorName
Class name `CompositeELResolver` is the same as one of its superclass' names
in `impl/src/main/java/org/apache/myfaces/el/resolver/CompositeELResolver.java`
#### Snippet
```java
 * @version $Revision$ $Date$
 */
public class CompositeELResolver extends jakarta.el.CompositeELResolver
{
    private Collection<ELResolver> _elResolvers;
```

### ClassNameSameAsAncestorName
Class name `Object` is the same as one of its superclass' names
in `impl/src/main/java/org/apache/myfaces/cdi/view/ViewScopeCDIMap.java`
#### Snippet
```java
    }
    
    private class EntryWrapper<String, Object> implements Entry<String, Object>
    {
        private final Object key;
```

### ClassNameSameAsAncestorName
Class name `Entry` is the same as one of its superclass' names
in `impl/src/main/java/org/apache/myfaces/util/lang/SubKeyMap.java`
#### Snippet
```java
    }

    private class Entry<V> implements Map.Entry<String, V>
    {
        public Entry(String baseKey)
```

### ClassNameSameAsAncestorName
Class name `ClassUtils` is the same as one of its superclass' names
in `impl/src/main/java/org/apache/myfaces/util/lang/ClassUtils.java`
#### Snippet
```java


public final class ClassUtils extends org.apache.myfaces.core.api.shared.lang.ClassUtils
{
    //~ Static fields/initializers -----------------------------------------------------------------
```

### ClassNameSameAsAncestorName
Class name `Entry` is the same as one of its superclass' names
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/MappedValueExpression.java`
#### Snippet
```java
public final class MappedValueExpression extends ValueExpression
{
    private final static class Entry implements Map.Entry, Serializable
    {
        private final Map src;
```

### ClassNameSameAsAncestorName
Class name `FacetHandler` is the same as one of its superclass' names
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/FacetHandler.java`
#### Snippet
```java
        bodyContent = "JSP", 
        tagClass="jakarta.faces.webapp.FacetTag")
public final class FacetHandler extends TagHandler 
    implements jakarta.faces.view.facelets.FacetHandler
{
```

### ClassNameSameAsAncestorName
Class name `AttributeHandler` is the same as one of its superclass' names
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/AttributeHandler.java`
#### Snippet
```java
 */
@JSFFaceletTag(name = "f:attribute", bodyContent = "empty")
public final class AttributeHandler extends TagHandler
    implements jakarta.faces.view.facelets.AttributeHandler
{
```

### ClassNameSameAsAncestorName
Class name `Entry` is the same as one of its superclass' names
in `test/src/main/java/org/apache/myfaces/test/mock/MockSubKeyMap.java`
#### Snippet
```java
    }

    private class Entry<V> implements Map.Entry<String, V>
    {
        public Entry(String baseKey)
```

## RuleId[ruleID=RedundantMethodOverride]
### RedundantMethodOverride
Method `get()` only delegates to its super method
in `api/src/main/java/jakarta/faces/component/_ComponentChildrenList.java`
#### Snippet
```java

    @Override
    public UIComponent get(int index)
    {
        return super.get(index);
```

### RedundantMethodOverride
Method `size()` only delegates to its super method
in `api/src/main/java/jakarta/faces/component/_ComponentChildrenList.java`
#### Snippet
```java

    @Override
    public int size()
    {
        return super.size();
```

### RedundantMethodOverride
Method `broadcast()` only delegates to its super method
in `api/src/main/java/jakarta/faces/component/UIInput.java`
#### Snippet
```java

    @Override
    public void broadcast(FacesEvent event) throws AbortProcessingException
    {
        // invoke standard listeners attached to this component first
```

### RedundantMethodOverride
Method `getExpressionString()` is identical to its super method
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ResourceLocationValueExpression.java`
#### Snippet
```java

    @Override
    public String getExpressionString()
    {
        return delegate.getExpressionString();
```

### RedundantMethodOverride
Method `isLiteralText()` is identical to its super method
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ResourceLocationValueExpression.java`
#### Snippet
```java

    @Override
    public boolean isLiteralText()
    {
        return delegate.isLiteralText();
```

### RedundantMethodOverride
Method `hashCode()` is identical to its super method
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ResourceLocationValueExpression.java`
#### Snippet
```java

    @Override
    public int hashCode()
    {
        return delegate.hashCode();
```

### RedundantMethodOverride
Method `equals()` is identical to its super method
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ResourceLocationValueExpression.java`
#### Snippet
```java

    @Override
    public boolean equals(Object obj)
    {
        return delegate.equals(obj);
```

### RedundantMethodOverride
Method `getWrapped()` is identical to its super method
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ResourceLocationValueExpression.java`
#### Snippet
```java

    @Override
    public ValueExpression getWrapped()
    {
        return delegate;
```

### RedundantMethodOverride
Method `cloneWithWriter()` only delegates to its super method
in `impl/src/main/java/org/apache/myfaces/context/PartialResponseWriterImpl.java`
#### Snippet
```java

    @Override
    public ResponseWriter cloneWithWriter(Writer writer)
    {
        return super.cloneWithWriter(writer);
```

### RedundantMethodOverride
Method `isLeaf()` is identical to its super method
in `impl/src/main/java/org/apache/myfaces/component/search/ParentSearchKeywordResolver.java`
#### Snippet
```java
    
    @Override
    public boolean isLeaf(SearchExpressionContext searchExpressionContext, String keyword)
    {
        return false;
```

### RedundantMethodOverride
Method `isPassthrough()` is identical to its super method
in `impl/src/main/java/org/apache/myfaces/component/search/ParentSearchKeywordResolver.java`
#### Snippet
```java

    @Override
    public boolean isPassthrough(SearchExpressionContext searchExpressionContext, String keyword)
    {
        return false;
```

### RedundantMethodOverride
Method `isLeaf()` is identical to its super method
in `impl/src/main/java/org/apache/myfaces/component/search/FormSearchKeywordResolver.java`
#### Snippet
```java
    
    @Override
    public boolean isLeaf(SearchExpressionContext searchExpressionContext, String keyword)
    {
        return false;
```

### RedundantMethodOverride
Method `isPassthrough()` is identical to its super method
in `impl/src/main/java/org/apache/myfaces/component/search/CompositeComponentParentSearchKeywordResolver.java`
#### Snippet
```java

    @Override
    public boolean isPassthrough(SearchExpressionContext searchExpressionContext, String keyword)
    {
        return false;
```

### RedundantMethodOverride
Method `isLeaf()` is identical to its super method
in `impl/src/main/java/org/apache/myfaces/component/search/CompositeComponentParentSearchKeywordResolver.java`
#### Snippet
```java
    
    @Override
    public boolean isLeaf(SearchExpressionContext searchExpressionContext, String keyword)
    {
        return false;
```

### RedundantMethodOverride
Method `isLeaf()` is identical to its super method
in `impl/src/main/java/org/apache/myfaces/component/search/RootSearchKeywordResolver.java`
#### Snippet
```java
    
    @Override
    public boolean isLeaf(SearchExpressionContext searchExpressionContext, String keyword)
    {
        return false;
```

### RedundantMethodOverride
Method `isPassthrough()` is identical to its super method
in `impl/src/main/java/org/apache/myfaces/component/search/RootSearchKeywordResolver.java`
#### Snippet
```java

    @Override
    public boolean isPassthrough(SearchExpressionContext searchExpressionContext, String keyword)
    {
        return false;
```

### RedundantMethodOverride
Method `isPassthrough()` is identical to its super method
in `impl/src/main/java/org/apache/myfaces/component/search/NamingContainerSearchKeywordResolver.java`
#### Snippet
```java

    @Override
    public boolean isPassthrough(SearchExpressionContext searchExpressionContext, String keyword)
    {
        return false;
```

### RedundantMethodOverride
Method `isLeaf()` is identical to its super method
in `impl/src/main/java/org/apache/myfaces/component/search/ThisSearchKeywordResolver.java`
#### Snippet
```java

    @Override
    public boolean isLeaf(SearchExpressionContext searchExpressionContext, String keyword)
    {
        return false;
```

### RedundantMethodOverride
Method `isLeaf()` is identical to its super method
in `impl/src/main/java/org/apache/myfaces/component/search/NamingContainerSearchKeywordResolver.java`
#### Snippet
```java
    
    @Override
    public boolean isLeaf(SearchExpressionContext searchExpressionContext, String keyword)
    {
        return false;
```

### RedundantMethodOverride
Method `isPassthrough()` is identical to its super method
in `impl/src/main/java/org/apache/myfaces/component/search/ThisSearchKeywordResolver.java`
#### Snippet
```java

    @Override
    public boolean isPassthrough(SearchExpressionContext searchExpressionContext, String keyword)
    {
        return false;
```

### RedundantMethodOverride
Method `isPassthrough()` is identical to its super method
in `impl/src/main/java/org/apache/myfaces/component/search/ChildSearchKeywordResolver.java`
#### Snippet
```java

    @Override
    public boolean isPassthrough(SearchExpressionContext searchExpressionContext, String keyword)
    {
        return false;
```

### RedundantMethodOverride
Method `isLeaf()` is identical to its super method
in `impl/src/main/java/org/apache/myfaces/component/search/ChildSearchKeywordResolver.java`
#### Snippet
```java
    
    @Override
    public boolean isLeaf(SearchExpressionContext searchExpressionContext, String keyword)
    {
        return false;
```

### RedundantMethodOverride
Method `isPassthrough()` is identical to its super method
in `impl/src/main/java/org/apache/myfaces/component/search/PreviousSearchKeywordResolver.java`
#### Snippet
```java

    @Override
    public boolean isPassthrough(SearchExpressionContext searchExpressionContext, String keyword)
    {
        return false;
```

### RedundantMethodOverride
Method `isLeaf()` is identical to its super method
in `impl/src/main/java/org/apache/myfaces/component/search/NextSearchKeywordResolver.java`
#### Snippet
```java
    
    @Override
    public boolean isLeaf(SearchExpressionContext searchExpressionContext, String keyword)
    {
        return false;
```

### RedundantMethodOverride
Method `isLeaf()` is identical to its super method
in `impl/src/main/java/org/apache/myfaces/component/search/PreviousSearchKeywordResolver.java`
#### Snippet
```java
    
    @Override
    public boolean isLeaf(SearchExpressionContext searchExpressionContext, String keyword)
    {
        return false;
```

### RedundantMethodOverride
Method `isPassthrough()` is identical to its super method
in `impl/src/main/java/org/apache/myfaces/component/search/NextSearchKeywordResolver.java`
#### Snippet
```java

    @Override
    public boolean isPassthrough(SearchExpressionContext searchExpressionContext, String keyword)
    {
        return false;
```

### RedundantMethodOverride
Method `isPassthrough()` is identical to its super method
in `impl/src/main/java/org/apache/myfaces/component/search/IdSearchKeywordResolver.java`
#### Snippet
```java

    @Override
    public boolean isPassthrough(SearchExpressionContext searchExpressionContext, String keyword)
    {
        return false;
```

### RedundantMethodOverride
Method `isLeaf()` is identical to its super method
in `impl/src/main/java/org/apache/myfaces/component/search/IdSearchKeywordResolver.java`
#### Snippet
```java

    @Override
    public boolean isLeaf(SearchExpressionContext searchExpressionContext, String keyword)
    {
        return false;
```

### RedundantMethodOverride
Method `afterFormElementsEnd()` only delegates to its super method
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlFormRenderer.java`
#### Snippet
```java
{
    @Override
    protected void afterFormElementsEnd(FacesContext facesContext, UIComponent component) throws IOException
    {
        super.afterFormElementsEnd(facesContext, component);
```

### RedundantMethodOverride
Method `isPostback()` is identical to its super method
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlResponseStateManager.java`
#### Snippet
```java
     */
    @Override
    public boolean isPostback(FacesContext context)
    {
        return context.getExternalContext().getRequestParameterMap().containsKey(ResponseStateManager.VIEW_STATE_PARAM);
```

### RedundantMethodOverride
Method `getHttpServletMapping()` only delegates to its super method
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletRequest.java`
#### Snippet
```java

    @Override
    public HttpServletMapping getHttpServletMapping()
    {
        return HttpServletRequest.super.getHttpServletMapping();
```

### RedundantMethodOverride
Method `getTrailerFields()` only delegates to its super method
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletRequest.java`
#### Snippet
```java

    @Override
    public Map<String, String> getTrailerFields()
    {
        return HttpServletRequest.super.getTrailerFields();
```

### RedundantMethodOverride
Method `isTrailerFieldsReady()` only delegates to its super method
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletRequest.java`
#### Snippet
```java

    @Override
    public boolean isTrailerFieldsReady()
    {
        return HttpServletRequest.super.isTrailerFieldsReady();
```

### RedundantMethodOverride
Method `newPushBuilder()` only delegates to its super method
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletRequest.java`
#### Snippet
```java

    @Override
    public PushBuilder newPushBuilder()
    {
        return HttpServletRequest.super.newPushBuilder();
```

### RedundantMethodOverride
Method `isPostback()` is identical to its super method
in `test/src/main/java/org/apache/myfaces/test/mock/MockResponseStateManager.java`
#### Snippet
```java
    }

    public boolean isPostback(FacesContext context)
    {
        return context.getExternalContext().getRequestParameterMap()
```

## RuleId[ruleID=OptionalAssignedToNull]
### OptionalAssignedToNull
Optional value is compared with null
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFaceletFactory.java`
#### Snippet
```java
    private URL getBaseUrl()
    {
        if (_baseUrl == null)
        {
            FacesContext context = FacesContext.getCurrentInstance();
```

## RuleId[ruleID=UNUSED_IMPORT]
### UNUSED_IMPORT
Unused import `import jakarta.faces.component.Doctype;`
in `api/src/main/java/jakarta/faces/component/html/_HtmlDoctype.java`
#### Snippet
```java
package jakarta.faces.component.html;

import jakarta.faces.component.Doctype;
import jakarta.faces.component.UIOutput;

```

## RuleId[ruleID=NestedAssignment]
### NestedAssignment
Result of assignment expression used
in `impl/src/main/java/org/apache/myfaces/spi/impl/DefaultServiceProviderFinder.java`
#### Snippet
```java
                        {
                            String serviceImplName;
                            while ((serviceImplName = rd.readLine()) != null)
                            {
                                int idx = serviceImplName.indexOf('#');
```

### NestedAssignment
Result of assignment expression used
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/SectionUniqueIdCounter.java`
#### Snippet
```java
            j = -l;
        }
        while ((l /= radix) != 0)
        {
            count++;
```

### NestedAssignment
Result of assignment expression used
in `impl/src/main/java/org/apache/myfaces/config/DefaultFacesConfigurationProvider.java`
#### Snippet
```java
                    byte[] buffer = new byte[1024];
                    int length;
                    while ((length = stream.read(buffer)) != -1)
                    {
                        out.write(buffer, 0, length);
```

### NestedAssignment
Result of assignment expression used
in `impl/src/main/java/org/apache/myfaces/webapp/SourceCodeServlet.java`
#### Snippet
```java
        {
            int ch;
            while ((ch = in.read()) !=-1) 
            {
                out.print((char)ch);
```

### NestedAssignment
Result of assignment expression used
in `impl/src/main/java/org/apache/myfaces/resource/TempDirFileCacheResourceLoader.java`
#### Snippet
```java
        int length;

        while ((length = (in.read(buffer))) >= 0)
        {
            out.write(buffer, 0, length);
```

### NestedAssignment
Result of assignment expression used
in `impl/src/main/java/org/apache/myfaces/resource/TempDirFileCacheContractResourceLoader.java`
#### Snippet
```java
        int length;

        while ((length = (in.read(buffer))) >= 0)
        {
            out.write(buffer, 0, length);
```

### NestedAssignment
Result of assignment expression used
in `impl/src/main/java/org/apache/myfaces/lifecycle/clientwindow/CODIClientSideWindow.java`
#### Snippet
```java
            BufferedReader reader = new BufferedReader(new InputStreamReader(is));
            String line = null;
            while ((line = reader.readLine()) != null)
            {
              sb.append(line).append('\n');
```

### NestedAssignment
Result of assignment expression used
in `impl/src/main/java/org/apache/myfaces/renderkit/ErrorPageWriter.java`
#### Snippet
```java
            byte[] buff = new byte[512];
            int read;
            while ((read = is.read(buff)) != -1)
            {
                baos.write(buff, 0, read);
```

### NestedAssignment
Result of assignment expression used
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
        int length;

        while ((length = (in.read(buffer))) >= 0)
        {
            out.write(buffer, 0, length);
```

### NestedAssignment
Result of assignment expression used
in `impl/src/main/java/org/apache/myfaces/application/viewstate/StateUtils.java`
#### Snippet
```java
                        byte[] buffer = new byte[bytes.length];
                        int length;
                        while ((length = gis.read(buffer)) != -1)
                        {
                            baos.write(buffer, 0, length);
```

### NestedAssignment
Result of assignment expression used
in `test/src/main/java/org/apache/myfaces/test/base/junit/runners/TestClassLoader.java`
#### Snippet
```java
            byte[] b = new byte[1000];
            int n;
            while ((n = stream.read(b)) != -1)
            {
                out.write(b, 0, n);
```

## RuleId[ruleID=ReplaceAssignmentWithOperatorAssignment]
### ReplaceAssignmentWithOperatorAssignment
`_size = _size + 1` could be simplified to '_size += 1'
in `api/src/main/java/jakarta/faces/component/_ArrayMap.java`
#### Snippet
```java
            }

            _size = _size + 1;
        }
        else
```

### ReplaceAssignmentWithOperatorAssignment
`i = i - 2` could be simplified to 'i -= 2'
in `api/src/main/java/jakarta/faces/component/_ArrayMap.java`
#### Snippet
```java
        {
            keyList.add(array[i]);
            i = i - 2;
        }
        return keyList.iterator();
```

### ReplaceAssignmentWithOperatorAssignment
`i = i - 2` could be simplified to 'i -= 2'
in `api/src/main/java/jakarta/faces/component/_ArrayMap.java`
#### Snippet
```java
        {
            valueList.add(array[i]);
            i = i - 2;
        }
        return valueList.iterator();
```

### ReplaceAssignmentWithOperatorAssignment
`_size = _size - 1` could be simplified to '_size -= 1'
in `api/src/main/java/jakarta/faces/component/_ArrayMap.java`
#### Snippet
```java
        {
            remove(array, key, false);
            _size = _size - 1;
        }

```

### ReplaceAssignmentWithOperatorAssignment
`i = i - 2` could be simplified to 'i -= 2'
in `api/src/main/java/jakarta/faces/component/_ArrayMap.java`
#### Snippet
```java
        {
            keyList.add((K) _array[i]);
            i = i - 2;
        }
        return keyList.iterator();
```

### ReplaceAssignmentWithOperatorAssignment
`_index = _index - 1` could be simplified to '_index -= 1'
in `api/src/main/java/jakarta/faces/component/_ArrayMap.java`
#### Snippet
```java

                            // And push the index back one
                            _index = _index - 1;
                        }

```

### ReplaceAssignmentWithOperatorAssignment
`size = size * 2` could be simplified to 'size \*= 2'
in `api/src/main/java/jakarta/faces/component/_ArrayMap.java`
#### Snippet
```java
        if (size > 0)
        {
            size = size * 2;
            for (int i = 0; i < size; i++)
            {
```

### ReplaceAssignmentWithOperatorAssignment
`f = f | toDigit(data[j], j)` could be simplified to 'f \|= toDigit(data\[j\], j)'
in `impl/src/main/java/org/apache/myfaces/util/lang/Hex.java`
#### Snippet
```java
            int f = toDigit(data[j], j) << 4;
            j++;
            f = f | toDigit(data[j], j);
            j++;
            out[i] = (byte) (f & 0xFF);
```

### ReplaceAssignmentWithOperatorAssignment
`j = j >>> 1` could be simplified to 'j \>\>\>= 1'
in `impl/src/main/java/org/apache/myfaces/util/lang/PriorityQueue.java`
#### Snippet
```java
            heap[i] = heap[j]; // shift parents down
            i = j;
            j = j >>> 1;
        }
        heap[i] = node; // install saved node
```

### ReplaceAssignmentWithOperatorAssignment
`lastCharLocation = lastCharLocation+1` could be simplified to 'lastCharLocation += 1'
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TextUnit.java`
#### Snippet
```java
                    if (lastCharLocation+1 < text.length())
                    {
                        lastCharLocation = lastCharLocation+1;
                    }
                    if (firstCharLocation > 0)
```

### ReplaceAssignmentWithOperatorAssignment
`lastCharLocation = lastCharLocation+1` could be simplified to 'lastCharLocation += 1'
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TextUnit.java`
#### Snippet
```java
            if (lastCharLocation+1 < text.length())
            {
                lastCharLocation = lastCharLocation+1;
            }
            if (firstCharLocation == 0)
```

### ReplaceAssignmentWithOperatorAssignment
`len = len-1` could be simplified to 'len -= 1'
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/SAXCompiler.java`
#### Snippet
```java
        if (tagContainParams && attributes.containsKey("params"))
        {
            len = len-1;
        }
        
```

### ReplaceAssignmentWithOperatorAssignment
`path = path + metadata` could be simplified to 'path += metadata'
in `impl/src/main/java/org/apache/myfaces/resource/ResourceImpl.java`
#### Snippet
```java
            {
                metadata = "?ln=" + getLibraryName();
                path = path + metadata;
                useAmp = true;

```

### ReplaceAssignmentWithOperatorAssignment
`url = url + '&'` could be simplified to 'url += '\&''
in `impl/src/main/java/org/apache/myfaces/lifecycle/clientwindow/CODIClientSideWindow.java`
#### Snippet
```java
        if (url.contains("?"))
        {
            url = url + '&';
        }
        else
```

### ReplaceAssignmentWithOperatorAssignment
`url = url + '?'` could be simplified to 'url += '?''
in `impl/src/main/java/org/apache/myfaces/lifecycle/clientwindow/CODIClientSideWindow.java`
#### Snippet
```java
        else
        {
            url = url + '?';
        }
        url = url + NOSCRIPT_PARAMETER + "=true";
```

### ReplaceAssignmentWithOperatorAssignment
`columnStyleIndex = columnStyleIndex + getColumnCountForComponent(facesC...` could be simplified to 'columnStyleIndex += getColumnCountForComponent(facesContext, uiData, child)'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTableRendererBase.java`
#### Snippet
```java
                        afterColumn(facesContext, uiData, columnStyleIndex);
                    }
                    columnStyleIndex = columnStyleIndex + 
                            getColumnCountForComponent(facesContext, uiData, child);
                }
            }
```

### ReplaceAssignmentWithOperatorAssignment
`columnStyleIndex = columnStyleIndex + getColumnCountForComponent(facesC...` could be simplified to 'columnStyleIndex += getColumnCountForComponent(facesContext, uiData, child)'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTableRendererBase.java`
#### Snippet
```java
                            afterColumn(facesContext, uiData, columnStyleIndex);
                        }
                        columnStyleIndex = columnStyleIndex + 
                            getColumnCountForComponent(facesContext, uiData, child);
                    }
                }
```

### ReplaceAssignmentWithOperatorAssignment
`prefix = prefix + '/'` could be simplified to 'prefix += '/''
in `impl/src/main/java/org/apache/myfaces/application/ViewIdSupport.java`
#### Snippet
```java
        {
            // need to make sure its really /faces/* and not /facesPage.xhtml
            prefix = prefix + '/'; 
        }
        
```

### ReplaceAssignmentWithOperatorAssignment
`sequence = sequence + 1` could be simplified to 'sequence += 1'
in `impl/src/main/java/org/apache/myfaces/application/viewstate/KeyFactoryRandom.java`
#### Snippet
```java
            else
            {
                sequence = sequence + 1;
            }
            map.put(RendererUtils.SEQUENCE_PARAM, sequence);
```

### ReplaceAssignmentWithOperatorAssignment
`vscount = vscount - 1` could be simplified to 'vscount -= 1'
in `impl/src/main/java/org/apache/myfaces/application/viewstate/SerializedViewCollection.java`
#### Snippet
```java
                        {
                            Integer vscount = _viewScopeIdCounts.get(oldViewScopeId);
                            vscount = vscount - 1;
                            if (vscount < 1)
                            {
```

### ReplaceAssignmentWithOperatorAssignment
`vscount = vscount - 1` could be simplified to 'vscount -= 1'
in `impl/src/main/java/org/apache/myfaces/application/viewstate/SerializedViewCollection.java`
#### Snippet
```java
                {
                    Integer vscount = _viewScopeIdCounts.get(oldViewScopeId);
                    vscount = vscount - 1;
                    if (vscount < 1)
                    {
```

### ReplaceAssignmentWithOperatorAssignment
`sequence = sequence + 1` could be simplified to 'sequence += 1'
in `impl/src/main/java/org/apache/myfaces/application/viewstate/KeyFactorySecureRandom.java`
#### Snippet
```java
            else
            {
                sequence = sequence + 1;
            }
            map.put(RendererUtils.SEQUENCE_PARAM, sequence);
```

### ReplaceAssignmentWithOperatorAssignment
`f = f | toDigit(data[j], j)` could be simplified to 'f \|= toDigit(data\[j\], j)'
in `test/src/main/java/org/apache/myfaces/test/mock/_Hex.java`
#### Snippet
```java
            int f = toDigit(data[j], j) << 4;
            j++;
            f = f | toDigit(data[j], j);
            j++;
            out[i] = (byte) (f & 0xFF);
```

## RuleId[ruleID=MismatchedCollectionQueryUpdate]
### MismatchedCollectionQueryUpdate
Contents of collection `clientWindowExpirationStack` are updated, but never queried
in `impl/src/main/java/org/apache/myfaces/cdi/clientwindow/ClientWindowScopeContextualStorageHolder.java`
#### Snippet
```java
        implements Serializable
{
    private LRULinkedHashMap<String, String> clientWindowExpirationStack;

    @PostConstruct
```

### MismatchedCollectionQueryUpdate
Contents of collection `clientWindowExpirationStack` are updated, but never queried
in `impl/src/main/java/org/apache/myfaces/flow/cdi/FlowScopeContextualStorageHolder.java`
#### Snippet
```java
    private Map<String, List<String>> activeFlowMapKeys;
    
    private LRULinkedHashMap<String, String> clientWindowExpirationStack;
    
    public static final String CURRENT_FLOW_SCOPE_MAP = "oam.CURRENT_FLOW_SCOPE_MAP";
```

### MismatchedCollectionQueryUpdate
Contents of collection `_faceletTemplates` are updated, but never queried
in `impl/src/main/java/org/apache/myfaces/config/RuntimeConfig.java`
#### Snippet
```java
    private List<SearchKeywordResolver> _searchExpressionResolvers = new ArrayList<>();
    
    private List<String> _faceletTemplates = new ArrayList<>();

    public static RuntimeConfig getCurrentInstance(FacesContext facesContext)
```

## RuleId[ruleID=FieldAccessedSynchronizedAndUnsynchronized]
### FieldAccessedSynchronizedAndUnsynchronized
Field `_elResolvers` is accessed in both synchronized and unsynchronized contexts
in `impl/src/main/java/org/apache/myfaces/el/resolver/CompositeELResolver.java`
#### Snippet
```java
public class CompositeELResolver extends jakarta.el.CompositeELResolver
{
    private Collection<ELResolver> _elResolvers;

    @Override
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `clientWindowExpirationStack` is accessed in both synchronized and unsynchronized contexts
in `impl/src/main/java/org/apache/myfaces/cdi/clientwindow/ClientWindowScopeContextualStorageHolder.java`
#### Snippet
```java
        implements Serializable
{
    private LRULinkedHashMap<String, String> clientWindowExpirationStack;

    @PostConstruct
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `clientWindowExpirationStack` is accessed in both synchronized and unsynchronized contexts
in `impl/src/main/java/org/apache/myfaces/flow/cdi/FlowScopeContextualStorageHolder.java`
#### Snippet
```java
    private Map<String, List<String>> activeFlowMapKeys;
    
    private LRULinkedHashMap<String, String> clientWindowExpirationStack;
    
    public static final String CURRENT_FLOW_SCOPE_MAP = "oam.CURRENT_FLOW_SCOPE_MAP";
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `csrfSessionTokenFactory` is accessed in both synchronized and unsynchronized contexts
in `impl/src/main/java/org/apache/myfaces/push/cdi/WebsocketChannelTokenBuilder.java`
#### Snippet
```java
public class WebsocketChannelTokenBuilder
{
    private CsrfSessionTokenFactory csrfSessionTokenFactory;
    
    private boolean initialized;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `initialized` is accessed in both synchronized and unsynchronized contexts
in `impl/src/main/java/org/apache/myfaces/push/cdi/WebsocketChannelTokenBuilder.java`
#### Snippet
```java
    private CsrfSessionTokenFactory csrfSessionTokenFactory;
    
    private boolean initialized;
    
    public WebsocketChannelTokenBuilder()
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `sessionMap` is accessed in both synchronized and unsynchronized contexts
in `impl/src/main/java/org/apache/myfaces/push/cdi/WebsocketSessionManager.java`
#### Snippet
```java
public class WebsocketSessionManager
{
    private Lazy<ConcurrentLRUCache<String, Collection<Reference<Session>>>> sessionMap;

    private Lazy<ConcurrentHashMap<UserChannelKey, Set<String>>> userMap;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `valueField` is accessed in both synchronized and unsynchronized contexts
in `impl/src/main/java/org/apache/myfaces/util/lang/StringCharArrayAccessor.java`
#### Snippet
```java
    static volatile boolean jdk7String = false;
    
    static Field valueField;
    static Field countField;
    static Field offsetField;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `offsetField` is accessed in both synchronized and unsynchronized contexts
in `impl/src/main/java/org/apache/myfaces/util/lang/StringCharArrayAccessor.java`
#### Snippet
```java
    static Field valueField;
    static Field countField;
    static Field offsetField;

    static
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `stop` is accessed in both synchronized and unsynchronized contexts
in `impl/src/main/java/org/apache/myfaces/util/lang/ConcurrentLRUCache.java`
#### Snippet
```java
        private WeakReference<ConcurrentLRUCache> cache;

        private boolean stop = false;

        public CleanupThread(ConcurrentLRUCache c)
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `_functions` is accessed in both synchronized and unsynchronized contexts
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/DefaultFunctionMapper.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;

    private Map<String, Function> _functions = null;

    /*
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `_renderers` is accessed in both synchronized and unsynchronized contexts
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlRenderKitImpl.java`
#### Snippet
```java
    // ~ Instance fields ----------------------------------------------------------------------------

    private Map<String, Map<String, Renderer>> _renderers;
    private ResponseStateManager _responseStateManager;
    private Map<String, ClientBehaviorRenderer> _clientBehaviorRenderers;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `_lastWindowKeys` is accessed in both synchronized and unsynchronized contexts
in `impl/src/main/java/org/apache/myfaces/application/viewstate/SerializedViewCollection.java`
#### Snippet
```java

    private final Map<SerializedViewKey, SerializedViewKey> _precedence = new HashMap<>();
    private Map<String, SerializedViewKey> _lastWindowKeys = null;

    public void put(FacesContext context, Object state, SerializedViewKey key, SerializedViewKey previousRestoredKey)
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `_wildcardPatterns` is accessed in both synchronized and unsynchronized contexts
in `impl/src/main/java/org/apache/myfaces/application/NavigationHandlerImpl.java`
#### Snippet
```java

    private Map<String, Set<NavigationCase>> _navigationCases = null;
    private List<_WildcardPattern> _wildcardPatterns = new ArrayList<>();
    private Boolean _developmentStage;
    
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `_navigationCases` is accessed in both synchronized and unsynchronized contexts
in `impl/src/main/java/org/apache/myfaces/application/NavigationHandlerImpl.java`
#### Snippet
```java
    private static final String ASTERISK = "*";

    private Map<String, Set<NavigationCase>> _navigationCases = null;
    private List<_WildcardPattern> _wildcardPatterns = new ArrayList<>();
    private Boolean _developmentStage;
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `_elContextListeners` is accessed in both synchronized and unsynchronized contexts
in `impl/src/main/java/org/apache/myfaces/application/ApplicationImpl.java`
#### Snippet
```java
    private FlowHandler _flowHandler;

    private ArrayList<ELContextListener> _elContextListeners;

    // components, converters, and validators can be added at runtime--must
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `fPathItems` is accessed in both synchronized and unsynchronized contexts
in `test/src/main/java/org/apache/myfaces/test/base/junit/runners/TestClassLoader.java`
#### Snippet
```java
{
    /** scanned class path */
    private Vector<String> fPathItems;
    /** default excluded paths */
    private String[] defaultExclusions = { "junit.framework.",
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `_sourceClassMap` is accessed in both synchronized and unsynchronized contexts
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication20.java`
#### Snippet
```java
    {
        private List<SystemEventListener> _lstSystemEventListener;
        private Map<Class<?>, List<SystemEventListener>> _sourceClassMap;

        public SystemListenerEntry()
```

### FieldAccessedSynchronizedAndUnsynchronized
Field `_lstSystemEventListener` is accessed in both synchronized and unsynchronized contexts
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication20.java`
#### Snippet
```java
    private static class SystemListenerEntry
    {
        private List<SystemEventListener> _lstSystemEventListener;
        private Map<Class<?>, List<SystemEventListener>> _sourceClassMap;

```

## RuleId[ruleID=ExtendsAnnotation]
### ExtendsAnnotation
Class 'Literal' implements annotation interface `FlowBuilderParameter`
in `api/src/main/java/jakarta/faces/flow/builder/FlowBuilderParameter.java`
#### Snippet
```java
     * @since 4.0
     */
    public static final class Literal extends AnnotationLiteral<FlowBuilderParameter> implements FlowBuilderParameter
    {
        private static final long serialVersionUID = 1L;
```

### ExtendsAnnotation
Class 'Literal' implements annotation interface `FlowDefinition`
in `api/src/main/java/jakarta/faces/flow/builder/FlowDefinition.java`
#### Snippet
```java
     * @since 4.0
     */
    public static final class Literal extends AnnotationLiteral<FlowDefinition> implements FlowDefinition
    {

```

### ExtendsAnnotation
Class 'Literal' implements annotation interface `Push`
in `api/src/main/java/jakarta/faces/push/Push.java`
#### Snippet
```java
     * @since 4.0
     */
    public static final class Literal extends AnnotationLiteral<Push> implements Push
    {
        private static final long serialVersionUID = 1L;
```

### ExtendsAnnotation
Class 'Literal' implements annotation interface `Opened`
in `api/src/main/java/jakarta/faces/event/WebsocketEvent.java`
#### Snippet
```java
         * @since 4.0
         */
        public static final class Literal extends AnnotationLiteral<Opened> implements Opened
        {
            private static final long serialVersionUID = 1L;
```

### ExtendsAnnotation
Class 'Literal' implements annotation interface `Closed`
in `api/src/main/java/jakarta/faces/event/WebsocketEvent.java`
#### Snippet
```java
         * @since 4.0
         */
        public static final class Literal extends AnnotationLiteral<Closed> implements Closed
        {
            private static final long serialVersionUID = 1L;
```

### ExtendsAnnotation
Class 'Literal' implements annotation interface `FacesDataModel`
in `api/src/main/java/jakarta/faces/model/FacesDataModel.java`
#### Snippet
```java
     * @since 4.0
     */
    public static final class Literal extends AnnotationLiteral<FacesDataModel> implements FacesDataModel
    {
        private static final long serialVersionUID = 1L;
```

### ExtendsAnnotation
Class 'Literal' implements annotation interface `FacesConverter`
in `api/src/main/java/jakarta/faces/convert/FacesConverter.java`
#### Snippet
```java
     * @since 4.0
     */
    public static final class Literal extends AnnotationLiteral<FacesConverter> implements FacesConverter
    {
        private static final long serialVersionUID = 1L;
```

### ExtendsAnnotation
Class 'Literal' implements annotation interface `FacesBehavior`
in `api/src/main/java/jakarta/faces/component/behavior/FacesBehavior.java`
#### Snippet
```java
     * @since 4.0
     */
    public static final class Literal extends AnnotationLiteral<FacesBehavior> implements FacesBehavior
    {
        private static final long serialVersionUID = 1L;
```

### ExtendsAnnotation
Class 'Literal' implements annotation interface `FacesValidator`
in `api/src/main/java/jakarta/faces/validator/FacesValidator.java`
#### Snippet
```java
     * @since 4.0
     */
    public static final class Literal extends AnnotationLiteral<FacesValidator> implements FacesValidator
    {
        private static final long serialVersionUID = 1L;
```

### ExtendsAnnotation
Class 'Literal' implements annotation interface `SessionMap`
in `api/src/main/java/jakarta/faces/annotation/SessionMap.java`
#### Snippet
```java
public @interface SessionMap
{
    public static final class Literal extends AnnotationLiteral<SessionMap> implements SessionMap
    {
        private static final long serialVersionUID = 1L;
```

### ExtendsAnnotation
Class 'Literal' implements annotation interface `HeaderValuesMap`
in `api/src/main/java/jakarta/faces/annotation/HeaderValuesMap.java`
#### Snippet
```java
public @interface HeaderValuesMap
{
    public static final class Literal extends AnnotationLiteral<HeaderValuesMap> implements HeaderValuesMap
    {
        private static final long serialVersionUID = 1L;
```

### ExtendsAnnotation
Class 'Literal' implements annotation interface `FlowMap`
in `api/src/main/java/jakarta/faces/annotation/FlowMap.java`
#### Snippet
```java
public @interface FlowMap
{
    public static final class Literal extends AnnotationLiteral<FlowMap> implements FlowMap
    {
        private static final long serialVersionUID = 1L;
```

### ExtendsAnnotation
Class 'Literal' implements annotation interface `ViewMap`
in `api/src/main/java/jakarta/faces/annotation/ViewMap.java`
#### Snippet
```java
public @interface ViewMap
{
    public static final class Literal extends AnnotationLiteral<ViewMap> implements ViewMap
    {
        private static final long serialVersionUID = 1L;
```

### ExtendsAnnotation
Class 'Literal' implements annotation interface `InitParameterMap`
in `api/src/main/java/jakarta/faces/annotation/InitParameterMap.java`
#### Snippet
```java
public @interface InitParameterMap
{
    public static final class Literal extends AnnotationLiteral<InitParameterMap> implements InitParameterMap
    {
        private static final long serialVersionUID = 1L;
```

### ExtendsAnnotation
Class 'Literal' implements annotation interface `ManagedProperty`
in `api/src/main/java/jakarta/faces/annotation/ManagedProperty.java`
#### Snippet
```java
    

    public static final class Literal extends AnnotationLiteral<ManagedProperty> implements ManagedProperty
    {
        private static final long serialVersionUID = 1L;
```

### ExtendsAnnotation
Class 'Literal' implements annotation interface `RequestParameterValuesMap`
in `api/src/main/java/jakarta/faces/annotation/RequestParameterValuesMap.java`
#### Snippet
```java
{
    public static final class Literal extends AnnotationLiteral<RequestParameterValuesMap>
            implements RequestParameterValuesMap
    {
        private static final long serialVersionUID = 1L;
```

### ExtendsAnnotation
Class 'Literal' implements annotation interface `ApplicationMap`
in `api/src/main/java/jakarta/faces/annotation/ApplicationMap.java`
#### Snippet
```java
public @interface ApplicationMap
{
    public static final class Literal extends AnnotationLiteral<ApplicationMap> implements ApplicationMap
    {
        private static final long serialVersionUID = 1L;
```

### ExtendsAnnotation
Class 'Literal' implements annotation interface `RequestParameterMap`
in `api/src/main/java/jakarta/faces/annotation/RequestParameterMap.java`
#### Snippet
```java
public @interface RequestParameterMap
{
    public static final class Literal extends AnnotationLiteral<RequestParameterMap> implements RequestParameterMap
    {
        private static final long serialVersionUID = 1L;
```

### ExtendsAnnotation
Class 'Literal' implements annotation interface `HeaderMap`
in `api/src/main/java/jakarta/faces/annotation/HeaderMap.java`
#### Snippet
```java
public @interface HeaderMap
{
    public static final class Literal extends AnnotationLiteral<HeaderMap> implements HeaderMap
    {
        private static final long serialVersionUID = 1L;
```

### ExtendsAnnotation
Class 'Literal' implements annotation interface `RequestMap`
in `api/src/main/java/jakarta/faces/annotation/RequestMap.java`
#### Snippet
```java
public @interface RequestMap
{
    public static final class Literal extends AnnotationLiteral<RequestMap> implements RequestMap
    {
        private static final long serialVersionUID = 1L;
```

### ExtendsAnnotation
Class 'Literal' implements annotation interface `RequestCookieMap`
in `api/src/main/java/jakarta/faces/annotation/RequestCookieMap.java`
#### Snippet
```java
public @interface RequestCookieMap
{
    public static final class Literal extends AnnotationLiteral<RequestCookieMap> implements RequestCookieMap
    {
        private static final long serialVersionUID = 1L;
```

### ExtendsAnnotation
Class 'Literal' implements annotation interface `View`
in `api/src/main/java/jakarta/faces/annotation/View.java`
#### Snippet
```java
     *
     */
    public final static class Literal extends AnnotationLiteral<View> implements View
    {
        public static final Literal INSTANCE = of("");
```

### ExtendsAnnotation
Class 'Literal' implements annotation interface `FacesConfig`
in `api/src/main/java/jakarta/faces/annotation/FacesConfig.java`
#### Snippet
```java
    }
    
    public static final class Literal extends AnnotationLiteral<FacesConfig> implements FacesConfig
    {
        private static final long serialVersionUID = 1L;
```

### ExtendsAnnotation
Class 'FlowMapAnnotationLiteral' implements annotation interface `FlowMap`
in `impl/src/main/java/org/apache/myfaces/cdi/FacesArtifactFlowMapProducer.java`
#### Snippet
```java
public class FacesArtifactFlowMapProducer extends AbstractDynamicProducer
{
    class FlowMapAnnotationLiteral extends AnnotationLiteral<FlowMap> implements FlowMap
    {
        private static final long serialVersionUID = -8623640277155878657L;
```

### ExtendsAnnotation
Class 'DefaultLiteral' implements annotation interface `Default`
in `impl/src/main/java/org/apache/myfaces/cdi/util/DefaultLiteral.java`
#### Snippet
```java
 * Literal for the {@link jakarta.enterprise.inject.Default} annotation.
 */
public class DefaultLiteral extends AnnotationLiteral<Default> implements Default
{
    private static final long serialVersionUID = -8623640277155878657L;
```

### ExtendsAnnotation
Class 'AnyLiteral' implements annotation interface `Any`
in `impl/src/main/java/org/apache/myfaces/cdi/util/AnyLiteral.java`
#### Snippet
```java
 * NOTE: Taken from Apache DeltaSpike
 */
public class AnyLiteral extends AnnotationLiteral<Any> implements Any
{
    private static final long serialVersionUID = -8623640277155878657L;
```

### ExtendsAnnotation
Class 'ManagedPropertyAnnotationLiteral' implements annotation interface `ManagedProperty`
in `impl/src/main/java/org/apache/myfaces/cdi/managedproperty/ManagedPropertyProducer.java`
#### Snippet
```java
public class ManagedPropertyProducer extends AbstractDynamicProducer<Object>
{
    class ManagedPropertyAnnotationLiteral extends AnnotationLiteral<ManagedProperty> implements ManagedProperty
    {
        private static final long serialVersionUID = 1L;
```

## RuleId[ruleID=HtmlWrongAttributeValue]
### HtmlWrongAttributeValue
Wrong attribute value
in `log/indexing-diagnostic/project.15375f63/diagnostic-2022-11-25-18-01-56.752.html`
#### Snippet
```java
              <td>0</td>
              <td>0</td>
              <td><textarea rows="10" cols="75" readonly="true" placeholder="empty" style="white-space: pre; border: none">Not collected for refresh</textarea></td>
            </tr>
          </tbody>
```

## RuleId[ruleID=StringRepeatCanBeUsed]
### StringRepeatCanBeUsed
Can be replaced with 'String.repeat()'
in `impl/src/main/java/org/apache/myfaces/webapp/FacesInitializerImpl.java`
#### Snippet
```java
                message.append(" mode.");
                int length = projectStage.name().length();
                for (int i = 0; i < 11 - length; i++)
                {
                    message.append(' ');
```

### StringRepeatCanBeUsed
Can be replaced with 'String.repeat()'
in `impl/src/main/java/org/apache/myfaces/webapp/FacesInitializerImpl.java`
#### Snippet
```java
                message.append(" ***\n");
                message.append("***                                            ");
                for (int i = 0; i < length; i++)
                {
                    message.append('^');
```

### StringRepeatCanBeUsed
Can be replaced with 'String.repeat()'
in `impl/src/main/java/org/apache/myfaces/webapp/FacesInitializerImpl.java`
#### Snippet
```java
                    message.append('^');
                }
                for (int i = 0; i < 18 - length; i++)
                {
                    message.append(' ');
```

## RuleId[ruleID=SynchronizeOnThis]
### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `api/src/main/java/jakarta/faces/validator/BeanValidator.java`
#### Snippet
```java
        else
        {
            synchronized (this)
            {
                if (ExternalSpecifications.isBeanValidationAvailable())
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `impl/src/main/java/org/apache/myfaces/util/lang/Lazy.java`
#### Snippet
```java
        if (result == NOT_INITIALIZED)
        {
            synchronized (this)
            {
                result = value;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `impl/src/main/java/org/apache/myfaces/util/lang/ConcurrentLRUCache.java`
#### Snippet
```java
            while (true)
            {
                synchronized (this)
                {
                    if (stop)
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `impl/src/main/java/org/apache/myfaces/util/lang/ConcurrentLRUCache.java`
#### Snippet
```java
                    try
                    {
                        this.wait();
                    }
                    catch (InterruptedException e)
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `impl/src/main/java/org/apache/myfaces/util/lang/ConcurrentLRUCache.java`
#### Snippet
```java
        void stopThread()
        {
            synchronized (this)
            {
                stop = true;
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `impl/src/main/java/org/apache/myfaces/util/lang/ConcurrentLRUCache.java`
#### Snippet
```java
            {
                stop = true;
                this.notify();
            }
        }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `impl/src/main/java/org/apache/myfaces/util/lang/ConcurrentLRUCache.java`
#### Snippet
```java
        void wakeThread()
        {
            synchronized (this)
            {
                this.notify();
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `impl/src/main/java/org/apache/myfaces/util/lang/ConcurrentLRUCache.java`
#### Snippet
```java
            synchronized (this)
            {
                this.notify();
            }
        }
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/DefaultFunctionMapper.java`
#### Snippet
```java
        
        Function f = new Function(prefix, localName, m);
        synchronized (this)
        {
            _functions.put(prefix + ':' + localName, f);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `impl/src/main/java/org/apache/myfaces/resource/TempDirFileCacheResourceLoader.java`
#### Snippet
```java
            if (creator == null)
            {
                synchronized(this)
                {
                    creator = map.get(identifier);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `impl/src/main/java/org/apache/myfaces/resource/TempDirFileCacheContractResourceLoader.java`
#### Snippet
```java
            if (creator == null)
            {
                synchronized(this)
                {
                    creator = map.get(identifier);
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `impl/src/main/java/org/apache/myfaces/lifecycle/clientwindow/ClientConfig.java`
#### Snippet
```java
        if (javaScriptEnabled == null)
        {
            synchronized(this)
            {
                // double lock checking idiom on volatile variable works since java5
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `impl/src/main/java/org/apache/myfaces/lifecycle/clientwindow/ClientConfig.java`
#### Snippet
```java
        if (userAgent == null)
        {
            synchronized (this)
            {
                if (userAgent == null)
```

### SynchronizeOnThis
Lock operations on 'this' may have unforeseen side-effects
in `impl/src/main/java/org/apache/myfaces/component/validate/WholeBeanValidator.java`
#### Snippet
```java
        else
        {
            synchronized (this)
            {
                if (ExternalSpecifications.isBeanValidationAvailable())
```

## RuleId[ruleID=DuplicateThrows]
### DuplicateThrows
There is a more general exception, 'java.lang.Throwable', in the throws list already.
in `api/src/main/java/org/apache/myfaces/core/api/shared/lang/PropertyDescriptorUtils.java`
#### Snippet
```java
    protected static BiConsumer createSetter(MethodHandles.Lookup lookup, LambdaPropertyDescriptor propertyInfo,
            MethodHandle setterHandle)
            throws LambdaConversionException, Throwable
    {
        Class<?> propertyType = propertyInfo.getPropertyType();
```

### DuplicateThrows
There is a more general exception, 'jakarta.el.ELException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/el/resolver/ImportHandlerResolver.java`
#### Snippet
```java
    @Override
    public Object getValue(final ELContext context, final Object base, final Object property)
        throws NullPointerException, PropertyNotFoundException, ELException
    {
        if (EL_CLASS == null
```

### DuplicateThrows
There is a more general exception, 'jakarta.el.ELException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/el/resolver/ScopedAttributeResolver.java`
#### Snippet
```java
    @Override
    public void setValue(final ELContext context, final Object base, final Object property, final Object value)
        throws NullPointerException, PropertyNotFoundException, PropertyNotWritableException, ELException
    {
        if (base != null)
```

### DuplicateThrows
There is a more general exception, 'jakarta.el.ELException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/el/resolver/ScopedAttributeResolver.java`
#### Snippet
```java
    @Override
    public void setValue(final ELContext context, final Object base, final Object property, final Object value)
        throws NullPointerException, PropertyNotFoundException, PropertyNotWritableException, ELException
    {
        if (base != null)
```

### DuplicateThrows
There is a more general exception, 'jakarta.el.ELException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/el/resolver/ScopedAttributeResolver.java`
#### Snippet
```java
    @Override
    public Object getValue(final ELContext context, final Object base, final Object property)
        throws NullPointerException, PropertyNotFoundException, ELException
    {
        if (base != null)
```

### DuplicateThrows
There is a more general exception, 'jakarta.el.ELException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/el/resolver/ScopedAttributeResolver.java`
#### Snippet
```java
    @Override
    public Class<?> getType(final ELContext context, final Object base, final Object property)
        throws NullPointerException, PropertyNotFoundException, ELException
    {

```

### DuplicateThrows
There is a more general exception, 'jakarta.el.ELException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/el/resolver/ScopedAttributeResolver.java`
#### Snippet
```java
    @Override
    public boolean isReadOnly(final ELContext context, final Object base, final Object property)
        throws NullPointerException, PropertyNotFoundException, ELException
    {
        if (base == null)
```

### DuplicateThrows
There is a more general exception, 'jakarta.el.ELException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/el/resolver/ResourceBundleResolver.java`
#### Snippet
```java
    @Override
    public boolean isReadOnly(final ELContext context, final Object base, final Object property)
        throws NullPointerException, PropertyNotFoundException, ELException
    {
        if (base != null)
```

### DuplicateThrows
There is a more general exception, 'jakarta.el.ELException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/el/resolver/ResourceBundleResolver.java`
#### Snippet
```java
    @Override
    public Class<?> getType(final ELContext context, final Object base, final Object property)
        throws NullPointerException, PropertyNotFoundException, ELException
    {
        if (base != null)
```

### DuplicateThrows
There is a more general exception, 'jakarta.el.ELException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/el/resolver/ResourceBundleResolver.java`
#### Snippet
```java
    @Override
    public Object getValue(final ELContext context, final Object base, final Object property)
        throws NullPointerException, PropertyNotFoundException, ELException
    {
        if (base != null)
```

### DuplicateThrows
There is a more general exception, 'jakarta.el.ELException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/el/resolver/ResourceBundleResolver.java`
#### Snippet
```java
    @Override
    public void setValue(final ELContext context, final Object base, final Object property, final Object value)
        throws NullPointerException, PropertyNotFoundException, PropertyNotWritableException, ELException
    {
        // Faces 2.0 spec section 5.6.1.4
```

### DuplicateThrows
There is a more general exception, 'jakarta.el.ELException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/el/resolver/ResourceBundleResolver.java`
#### Snippet
```java
    @Override
    public void setValue(final ELContext context, final Object base, final Object property, final Object value)
        throws NullPointerException, PropertyNotFoundException, PropertyNotWritableException, ELException
    {
        // Faces 2.0 spec section 5.6.1.4
```

### DuplicateThrows
There is a more general exception, 'jakarta.el.ELException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/el/resolver/FlashELResolver.java`
#### Snippet
```java
    @Override
    public Object getValue(ELContext elContext, Object base, Object property)
            throws NullPointerException, PropertyNotFoundException, ELException
    {

```

### DuplicateThrows
There is a more general exception, 'jakarta.el.ELException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/el/resolver/FlashELResolver.java`
#### Snippet
```java
    @Override
    public Class<?> getType(ELContext context, Object base, Object property)
            throws NullPointerException, PropertyNotFoundException, ELException
    {
        if (property == null)
```

### DuplicateThrows
There is a more general exception, 'jakarta.el.ELException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/el/resolver/FlashELResolver.java`
#### Snippet
```java
    public void setValue(ELContext context, Object base, Object property,
            Object value) throws NullPointerException,
            PropertyNotFoundException, PropertyNotWritableException,
            ELException
    {
```

### DuplicateThrows
There is a more general exception, 'jakarta.el.ELException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/el/resolver/FlashELResolver.java`
#### Snippet
```java
    public void setValue(ELContext context, Object base, Object property,
            Object value) throws NullPointerException,
            PropertyNotFoundException, PropertyNotWritableException,
            ELException
    {
```

### DuplicateThrows
There is a more general exception, 'jakarta.el.ELException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/el/resolver/FlashELResolver.java`
#### Snippet
```java
    @Override
    public boolean isReadOnly(ELContext context, Object base, Object property)
            throws NullPointerException, PropertyNotFoundException, ELException
    {
        if (property == null)
```

### DuplicateThrows
There is a more general exception, 'jakarta.el.ELException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/el/resolver/ImportConstantsELResolver.java`
#### Snippet
```java
    public Object getValue(final ELContext context, final Object base,
            final Object property)
            throws NullPointerException, PropertyNotFoundException, ELException
    {
        if (base != null)
```

### DuplicateThrows
There is a more general exception, 'jakarta.el.ELException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/el/resolver/ImportConstantsELResolver.java`
#### Snippet
```java
    @Override
    public void setValue(ELContext elc, Object o, Object o1, Object o2)
            throws NullPointerException, PropertyNotFoundException, PropertyNotWritableException, ELException
    {
        //No op
```

### DuplicateThrows
There is a more general exception, 'jakarta.el.ELException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/el/resolver/ImportConstantsELResolver.java`
#### Snippet
```java
    @Override
    public void setValue(ELContext elc, Object o, Object o1, Object o2)
            throws NullPointerException, PropertyNotFoundException, PropertyNotWritableException, ELException
    {
        //No op
```

### DuplicateThrows
There is a more general exception, 'jakarta.el.ELException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/el/resolver/ImportConstantsELResolver.java`
#### Snippet
```java
    public Class<?> getType(final ELContext context, final Object base,
            final Object property)
            throws NullPointerException, PropertyNotFoundException, ELException
    {
        return null;
```

### DuplicateThrows
There is a more general exception, 'jakarta.el.ELException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/el/resolver/ImportConstantsELResolver.java`
#### Snippet
```java
    public boolean isReadOnly(final ELContext context, final Object base,
            final Object property)
            throws NullPointerException, PropertyNotFoundException, ELException
    {
        return false;
```

### DuplicateThrows
There is a more general exception, 'jakarta.el.ELException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/el/resolver/implicitobject/ImplicitObjectResolver.java`
#### Snippet
```java
    @Override
    public Class<?> getType(ELContext context, Object base, Object property) throws NullPointerException,
        PropertyNotFoundException, ELException
    {

```

### DuplicateThrows
There is a more general exception, 'jakarta.el.ELException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/el/resolver/implicitobject/ImplicitObjectResolver.java`
#### Snippet
```java
    @Override
    public boolean isReadOnly(ELContext context, Object base, Object property) throws NullPointerException,
        PropertyNotFoundException, ELException
    {

```

### DuplicateThrows
There is a more general exception, 'jakarta.el.ELException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/el/resolver/implicitobject/ImplicitObjectResolver.java`
#### Snippet
```java
    @Override
    public Object getValue(ELContext context, Object base, Object property) throws NullPointerException,
        PropertyNotFoundException, ELException
    {

```

### DuplicateThrows
There is a more general exception, 'jakarta.el.ELException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/el/resolver/implicitobject/ImplicitObjectResolver.java`
#### Snippet
```java
    @Override
    public void setValue(ELContext context, Object base, Object property, Object value) throws NullPointerException,
        PropertyNotFoundException, PropertyNotWritableException, ELException
    {

```

### DuplicateThrows
There is a more general exception, 'jakarta.el.ELException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/el/resolver/implicitobject/ImplicitObjectResolver.java`
#### Snippet
```java
    @Override
    public void setValue(ELContext context, Object base, Object property, Object value) throws NullPointerException,
        PropertyNotFoundException, PropertyNotWritableException, ELException
    {

```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/AbstractFacelet.java`
#### Snippet
```java
    public abstract void applyDynamicComponentHandler(FacesContext facesContext, UIComponent parent,
        String baseKey)
         throws IOException, FacesException, FaceletException, ELException;
    
    public abstract boolean isBuildingCompositeComponentMetadata();
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/AbstractFacelet.java`
#### Snippet
```java
    
    public abstract void applyCompositeComponent(AbstractFaceletContext ctx, UIComponent parent, Resource resource)
            throws IOException, FacesException, FaceletException, ELException;
    
    public abstract void applyDynamicComponentHandler(FacesContext facesContext, UIComponent parent,
```

### DuplicateThrows
There is a more general exception, 'jakarta.el.ELException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/FaceletStateValueExpression.java`
#### Snippet
```java

    @Override
    public boolean isReadOnly(ELContext context) throws NullPointerException, PropertyNotFoundException, ELException
    {
        return getWrapped(context).isReadOnly(context);
```

### DuplicateThrows
There is a more general exception, 'jakarta.el.ELException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/FaceletStateValueExpression.java`
#### Snippet
```java

    @Override
    public Class<?> getType(ELContext context) throws NullPointerException, PropertyNotFoundException, ELException
    {
        return getWrapped(context).getType(context);
```

### DuplicateThrows
There is a more general exception, 'jakarta.el.ELException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/FaceletStateValueExpression.java`
#### Snippet
```java

    @Override
    public Object getValue(ELContext context) throws NullPointerException, PropertyNotFoundException, ELException
    {
        return getWrapped(context).getValue(context);
```

### DuplicateThrows
There is a more general exception, 'jakarta.el.ELException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/FaceletStateValueExpression.java`
#### Snippet
```java
    @Override
    public void setValue(ELContext context, Object value) 
        throws NullPointerException, PropertyNotFoundException, PropertyNotWritableException, ELException
    {
        getWrapped(context).setValue(context, value);
```

### DuplicateThrows
There is a more general exception, 'jakarta.el.ELException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/FaceletStateValueExpression.java`
#### Snippet
```java
    @Override
    public void setValue(ELContext context, Object value) 
        throws NullPointerException, PropertyNotFoundException, PropertyNotWritableException, ELException
    {
        getWrapped(context).setValue(context, value);
```

### DuplicateThrows
There is a more general exception, 'jakarta.el.ELException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/MethodExpressionMethodExpression.java`
#### Snippet
```java
    @Override
    public Object invoke(ELContext context, Object[] params)
            throws NullPointerException, PropertyNotFoundException, MethodNotFoundException, ELException
    {
        try
```

### DuplicateThrows
There is a more general exception, 'jakarta.el.ELException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/MethodExpressionMethodExpression.java`
#### Snippet
```java
    @Override
    public Object invoke(ELContext context, Object[] params)
            throws NullPointerException, PropertyNotFoundException, MethodNotFoundException, ELException
    {
        try
```

### DuplicateThrows
There is a more general exception, 'jakarta.el.ELException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/MethodExpressionMethodExpression.java`
#### Snippet
```java
    @Override
    public MethodInfo getMethodInfo(ELContext context)
            throws NullPointerException, PropertyNotFoundException, MethodNotFoundException, ELException
    {
        try
```

### DuplicateThrows
There is a more general exception, 'jakarta.el.ELException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/MethodExpressionMethodExpression.java`
#### Snippet
```java
    @Override
    public MethodInfo getMethodInfo(ELContext context)
            throws NullPointerException, PropertyNotFoundException, MethodNotFoundException, ELException
    {
        try
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/LegacyUserTagHandler.java`
#### Snippet
```java
     */
    @Override
    public void apply(FaceletContext ctx, UIComponent parent) throws IOException, FacesException, FaceletException,
            ELException
    {
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/LegacyUserTagHandler.java`
#### Snippet
```java
    @Override
    public boolean apply(FaceletContext ctx, UIComponent parent, String name) throws IOException, FacesException,
            FaceletException, ELException
    {
        if (name != null)
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/UserTagHandler.java`
#### Snippet
```java
     */
    @Override
    public void apply(FaceletContext ctx, UIComponent parent) throws IOException, FacesException, FaceletException,
            ELException
    {
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/UserTagHandler.java`
#### Snippet
```java
    @Override
    public boolean apply(FaceletContext ctx, UIComponent parent, String name) throws IOException, FacesException,
            FaceletException, ELException
    {
        if (name != null)
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/InsertHandler.java`
#### Snippet
```java
    @Override
    public boolean apply(FaceletContext ctx, UIComponent parent, String name) throws IOException, FacesException,
            FaceletException, ELException
    {
        if (Objects.equals(this.name, name))
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/InsertHandler.java`
#### Snippet
```java
     */
    @Override
    public void apply(FaceletContext ctx, UIComponent parent) throws IOException, FacesException, FaceletException,
            ELException
    {
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/LegacyCompositionHandler.java`
#### Snippet
```java
     */
    @Override
    public void apply(FaceletContext ctx, UIComponent parent) throws IOException, FacesException, FaceletException,
            ELException
    {
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/LegacyCompositionHandler.java`
#### Snippet
```java
    @Override
    public boolean apply(FaceletContext ctx, UIComponent parent, String name) throws IOException, FacesException,
            FaceletException, ELException
    {
        if (name != null)
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/LegacyDecorateHandler.java`
#### Snippet
```java
    @Override
    public boolean apply(FaceletContext ctx, UIComponent parent, String name) throws IOException, FacesException,
            FaceletException, ELException
    {
        if (name != null)
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/LegacyDecorateHandler.java`
#### Snippet
```java
     */
    @Override
    public void apply(FaceletContext ctx, UIComponent parent) throws IOException, FacesException, FaceletException,
            ELException
    {
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/ParamHandler.java`
#### Snippet
```java
    
    public void apply(FaceletContext ctx, UIComponent parent, String nameStr, ValueExpression valueVE, String uniqueId)
            throws IOException, FacesException, FaceletException, ELException
    {
        AbstractFaceletContext actx = ((AbstractFaceletContext) ctx);
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/ParamHandler.java`
#### Snippet
```java
    
    public void apply(FaceletContext ctx, UIComponent parent, String nameStr, ValueExpression valueVE)
            throws IOException, FacesException, FaceletException, ELException
    {
        AbstractFaceletContext actx = ((AbstractFaceletContext) ctx);
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/ParamHandler.java`
#### Snippet
```java
     */
    @Override
    public void apply(FaceletContext ctx, UIComponent parent) throws IOException, FacesException, FaceletException,
            ELException
    {
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/LegacyParamHandler.java`
#### Snippet
```java
     */
    @Override
    public void apply(FaceletContext ctx, UIComponent parent) throws IOException, FacesException, FaceletException,
            ELException
    {
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/LegacyParamHandler.java`
#### Snippet
```java
    
    public void apply(FaceletContext ctx, UIComponent parent, String nameStr, ValueExpression valueVE)
            throws IOException, FacesException, FaceletException, ELException
    {    
        ctx.getVariableMapper().setVariable(nameStr, valueVE);
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/LegacyIncludeHandler.java`
#### Snippet
```java
     */
    @Override
    public void apply(FaceletContext ctx, UIComponent parent) throws IOException, FacesException, FaceletException,
            ELException
    {
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/CompositionHandler.java`
#### Snippet
```java
     */
    @Override
    public void apply(FaceletContext ctx, UIComponent parent) throws IOException, FacesException, FaceletException,
            ELException
    {
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/CompositionHandler.java`
#### Snippet
```java
    @Override
    public boolean apply(FaceletContext ctx, UIComponent parent, String name) throws IOException, FacesException,
            FaceletException, ELException
    {
        if (name != null)
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/DefineHandler.java`
#### Snippet
```java

    public void applyDefinition(FaceletContext ctx, UIComponent parent) throws IOException, FacesException,
            FaceletException, ELException
    {
        this.nextHandler.apply(ctx, parent);
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/DefineHandler.java`
#### Snippet
```java
     */
    @Override
    public void apply(FaceletContext ctx, UIComponent parent) throws IOException, FacesException, FaceletException,
            ELException
    {
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/IncludeHandler.java`
#### Snippet
```java
     */
    @Override
    public void apply(FaceletContext ctx, UIComponent parent) throws IOException, FacesException, FaceletException,
            ELException
    {
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/CatchHandler.java`
#### Snippet
```java

    @Override
    public void apply(FaceletContext ctx, UIComponent parent) throws IOException, FacesException, FaceletException,
            ELException
    {
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/DecorateHandler.java`
#### Snippet
```java
     */
    @Override
    public void apply(FaceletContext ctx, UIComponent parent) throws IOException, FacesException, FaceletException,
            ELException
    {
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/DecorateHandler.java`
#### Snippet
```java
    @Override
    public boolean apply(FaceletContext ctx, UIComponent parent, String name) throws IOException, FacesException,
            FaceletException, ELException
    {
        if (name != null)
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/ChooseWhenHandler.java`
#### Snippet
```java

    @Override
    public void apply(FaceletContext ctx, UIComponent parent) throws IOException, FacesException, FaceletException,
            ELException
    {
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/LegacySetHandler.java`
#### Snippet
```java

    @Override
    public void apply(FaceletContext ctx, UIComponent parent) throws IOException, FacesException, FaceletException,
            ELException
    {
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/SetHandler.java`
#### Snippet
```java

    @Override
    public void apply(FaceletContext ctx, UIComponent parent) throws IOException, FacesException, FaceletException,
            ELException
    {
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/ChooseOtherwiseHandler.java`
#### Snippet
```java

    @Override
    public void apply(FaceletContext ctx, UIComponent parent) throws IOException, FacesException, FaceletException,
            ELException
    {
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/ChooseHandler.java`
#### Snippet
```java

    @Override
    public void apply(FaceletContext ctx, UIComponent parent) throws IOException, FacesException, FaceletException,
            ELException
    {
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/PassThroughAttributeHandler.java`
#### Snippet
```java
     */
    @Override
    public void apply(FaceletContext ctx, UIComponent parent) throws IOException, FacesException, FaceletException,
            ELException
    {
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/ConvertNumberHandler.java`
#### Snippet
```java
     * jakarta.faces.view.facelets.FaceletContext)
     */
    protected Converter createConverter(FaceletContext ctx) throws FacesException, ELException, FaceletException
    {
        return ctx.getFacesContext().getApplication().createConverter(NumberConverter.CONVERTER_ID);
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/LegacyForEachHandler.java`
#### Snippet
```java

    @Override
    public void apply(FaceletContext ctx, UIComponent parent) throws IOException, FacesException, FaceletException,
            ELException
    {
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/ConvertDateTimeHandler.java`
#### Snippet
```java
     * See jakarta.faces.view.facelets.ConverterHandler#createConverter(jakarta.faces.view.facelets.FaceletContext)
     */
    protected Converter createConverter(FaceletContext ctx) throws FacesException, ELException, FaceletException
    {
        return ctx.getFacesContext().getApplication().createConverter(DateTimeConverter.CONVERTER_ID);
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/LoadBundleHandler.java`
#### Snippet
```java
     */
    @Override
    public void apply(FaceletContext ctx, UIComponent parent) throws IOException, FacesException, FaceletException,
            ELException
    {
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/FacetHandler.java`
#### Snippet
```java
     */
    @Override
    public void apply(FaceletContext ctx, UIComponent parent) throws IOException, FacesException, FaceletException,
            ELException
    {
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/SetPropertyActionListenerHandler.java`
#### Snippet
```java
    @Override
    public void apply(FaceletContext ctx, UIComponent parent)
            throws IOException, FacesException, FaceletException, ELException
    {
        //Apply only if we are creating a new component
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/ViewHandler.java`
#### Snippet
```java
     */
    @Override
    public void apply(FaceletContext ctx, UIComponent parent) throws IOException, FacesException, FaceletException,
            ELException
    {
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/ValueChangeListenerHandler.java`
#### Snippet
```java
     */
    @Override
    public void apply(FaceletContext ctx, UIComponent parent) throws IOException, FacesException, FaceletException,
            ELException
    {
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/PhaseListenerHandler.java`
#### Snippet
```java

    @Override
    public void apply(FaceletContext ctx, UIComponent parent) throws IOException, FacesException, FaceletException,
            ELException
    {
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/AttributeHandler.java`
#### Snippet
```java
     */
    @Override
    public void apply(FaceletContext ctx, UIComponent parent) throws IOException, FacesException, FaceletException,
            ELException
    {
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/ActionListenerHandler.java`
#### Snippet
```java
     */
    @Override
    public void apply(FaceletContext ctx, UIComponent parent) throws IOException, FacesException, FaceletException,
            ELException
    {
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/ConvertDelegateHandler.java`
#### Snippet
```java
     * jakarta.faces.view.facelets.FaceletContext)
     */
    protected Converter createConverter(FaceletContext ctx) throws FacesException, ELException, FaceletException
    {
        return ctx.getFacesContext().getApplication().createConverter(this.getConverterId(ctx));
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/EventHandler.java`
#### Snippet
```java
    @Override
    public void apply(FaceletContext ctx, UIComponent parent)
            throws ELException, FacesException, FaceletException, IOException
    {
        //Apply only if we are creating a new component
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/ResetValuesActionListenerHandler.java`
#### Snippet
```java
     */
    @Override
    public void apply(FaceletContext ctx, UIComponent parent) throws IOException, FacesException, FaceletException,
            ELException
    {
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeComponentResourceTagHandler.java`
#### Snippet
```java
    @Override
    public boolean apply(FaceletContext ctx, UIComponent parent, String name)
            throws IOException, FacesException, FaceletException, ELException
    {
        if (_dynamicCompositeComponent)
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFaceletContext.java`
#### Snippet
```java
        @Override
        public boolean apply(FaceletContext ctx, UIComponent parent, String name)
                throws IOException, FacesException, FaceletException, ELException
        {
            String testName = (name != null) ? name : "facelets._NULL_DEF_";
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFaceletContext.java`
#### Snippet
```java
    @Override
    public boolean includeCompositeComponentDefinition(UIComponent parent, String name)
            throws IOException, FaceletException, FacesException, ELException
    {
        TemplateClient ccClient = _isolatedTemplateContext.get(_currentTemplateContext).getCompositeComponentClient();
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFaceletContext.java`
#### Snippet
```java
    @Override
    public boolean includeDefinition(UIComponent parent, String name)
            throws IOException, FaceletException, FacesException, ELException
    {
        return _isolatedTemplateContext.get(_currentTemplateContext).includeDefinition(
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFaceletContext.java`
#### Snippet
```java
    @Override
    public void applyCompositeComponent(UIComponent parent, Resource resource)
            throws IOException, FaceletException, FacesException, ELException
    {
        _facelet.applyCompositeComponent(this, parent, resource);
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/ForEachHandler.java`
#### Snippet
```java

    @Override
    public void apply(FaceletContext ctx, UIComponent parent) throws IOException, FacesException, FaceletException,
            ELException
    {
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/TemplateContextImpl.java`
#### Snippet
```java
        @Override
        public boolean apply(FaceletContext ctx, UIComponent parent, String name)
                throws IOException, FacesException, FaceletException,
                ELException
        {
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/TemplateContextImpl.java`
#### Snippet
```java
        @Override
        public boolean apply(FaceletContext ctx, UIComponent parent, String name)
                throws IOException, FacesException, FaceletException, ELException
        {
            return false;
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/TemplateContextImpl.java`
#### Snippet
```java
    @Override
    public boolean includeDefinition(FaceletContext ctx, Facelet owner, UIComponent parent, String name)
            throws IOException, FaceletException, FacesException, ELException
    {
        TemplateManager client;
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFaceletFactory.java`
#### Snippet
```java
    @Override
    public Facelet getFacelet(FacesContext facesContext, String uri) 
        throws IOException, FaceletException, FacesException, ELException
    {
        Boolean isManagedFacelet = _managedFacelet.get(uri);
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFaceletFactory.java`
#### Snippet
```java
    @Override
    public Facelet getCompositeComponentMetadataFacelet(URL url) throws IOException,
            FaceletException, FacesException, ELException
    {
        if (_abstractFaceletCache != null)
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFaceletFactory.java`
#### Snippet
```java
    @Override
    public Facelet getViewMetadataFacelet(URL url) throws IOException,
            FaceletException, FacesException, ELException
    {
        if (_abstractFaceletCache != null)
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFaceletFactory.java`
#### Snippet
```java
    @Override
    public Facelet getFacelet(FaceletContext ctx, URL url) 
            throws IOException, FaceletException, FacesException, ELException
    {
        if (_abstractFaceletCache != null)
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFacelet.java`
#### Snippet
```java
    @Override
    public void applyDynamicComponentHandler(FacesContext facesContext, UIComponent parent, String baseKey)
         throws IOException, FacesException, FaceletException, ELException
    {
        FaceletCompositionContext fcctx = null;
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFacelet.java`
#### Snippet
```java
    @Override
    public void apply(FacesContext facesContext, UIComponent parent) throws IOException, FacesException,
            FaceletException, ELException
    {
        FaceletCompositionContext myFaceletContext = FaceletCompositionContext.getCurrentInstance(facesContext);
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFacelet.java`
#### Snippet
```java
    @Override
    public void applyCompositeComponent(AbstractFaceletContext ctx, UIComponent parent, Resource resource)
            throws IOException, FacesException, FaceletException, ELException
    {
        // Here we are creating a facelet using the url provided by the resource.
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/CompilationUnit.java`
#### Snippet
```java
    {
        @Override
        public void apply(FaceletContext ctx, UIComponent parent) throws IOException, FacesException, FaceletException,
                ELException
        {
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/Compiler.java`
#### Snippet
```java

    protected abstract CompilerResult doCompile(URL src, String alias)
            throws IOException, FaceletException, ELException, FacesException;

    protected abstract CompilerResult doCompileViewMetadata(URL src, String alias)
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/Compiler.java`
#### Snippet
```java
    }

    public final CompilerResult compile(URL src, String alias) throws IOException, FaceletException, ELException,
            FacesException
    {
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/Compiler.java`
#### Snippet
```java

    protected abstract CompilerResult doCompileViewMetadata(URL src, String alias)
            throws IOException, FaceletException, ELException, FacesException;
    
    protected abstract CompilerResult doCompileCompositeComponentMetadata(URL src, String alias)
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/Compiler.java`
#### Snippet
```java
    
    public final CompilerResult compileViewMetadata(URL src, String alias)
            throws IOException, FaceletException, ELException, FacesException
    {
        return this.doCompileViewMetadata(src, alias);
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/Compiler.java`
#### Snippet
```java
    
    protected abstract CompilerResult doCompileCompositeComponentMetadata(URL src, String alias)
            throws IOException, FaceletException, ELException, FacesException;
    
    protected abstract CompilerResult doCompileComponent(
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/Compiler.java`
#### Snippet
```java
    
    public final CompilerResult compileCompositeComponentMetadata(URL src, String alias)
            throws IOException, FaceletException, ELException, FacesException
    {
        return this.doCompileCompositeComponentMetadata(src, alias);
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/NamespaceHandler.java`
#### Snippet
```java

    @Override
    public void apply(FaceletContext ctx, UIComponent parent) throws IOException, FacesException, FaceletException,
            ELException
    {
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/UIInstructionHandler.java`
#### Snippet
```java

    @Override
    public void apply(FaceletContext ctx, UIComponent parent) throws IOException, FacesException, FaceletException,
            ELException
    {
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/EncodingHandler.java`
#### Snippet
```java

    @Override
    public void apply(FaceletContext ctx, UIComponent parent) throws IOException, FacesException, FaceletException,
            ELException
    {
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/SAXCompiler.java`
#### Snippet
```java
    @Override
    public CompilerResult doCompile(URL src, String alias)
            throws IOException, FaceletException, ELException, FacesException
    {
        CompilationManager mngr = null;
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/SAXCompiler.java`
#### Snippet
```java
    @Override
    protected CompilerResult doCompileCompositeComponentMetadata(URL src, String alias)
            throws IOException, FaceletException, ELException, FacesException
    {
        CompilationManager mngr = null;
```

### DuplicateThrows
There is a more general exception, 'jakarta.faces.FacesException', in the throws list already.
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/SAXCompiler.java`
#### Snippet
```java
    @Override
    protected CompilerResult doCompileViewMetadata(URL src, String alias)
            throws IOException, FaceletException, ELException, FacesException
    {
        CompilationManager mngr = null;
```

## RuleId[ruleID=CastConflictsWithInstanceof]
### CastConflictsWithInstanceof
Cast to 'Bean' type conflicts with preceding 'instanceof PassivationCapable' check
in `impl/src/main/java/org/apache/myfaces/flow/cdi/FlowScopeContext.java`
#### Snippet
```java
        }
        
        FlowReference reference = flowBeanReferences.get(((Bean) bean).getBeanClass());
        if (reference != null)
        {
```

### CastConflictsWithInstanceof
Cast to 'List' type conflicts with preceding 'instanceof RandomAccess' check
in `impl/src/main/java/org/apache/myfaces/util/lang/ArrayUtils.java`
#### Snippet
```java
        if (toAdd instanceof RandomAccess)
        {
            List<? extends T> randomAccess = (List<? extends T>) toAdd;
            for (int i = 0, size = randomAccess.size(); i < size; i++)
            {
```

### CastConflictsWithInstanceof
Cast to 'UISelectOne' type conflicts with preceding 'instanceof HtmlSelectOneListbox' check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlListboxRendererBase.java`
#### Snippet
```java
        {
            renderListbox(facesContext,
                                            (UISelectOne)uiComponent,
                                            isDisabled(facesContext, uiComponent),
                                            size, getConverter(facesContext, uiComponent));
```

### CastConflictsWithInstanceof
Cast to 'List' type conflicts with preceding 'instanceof RandomAccess' check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/AjaxScriptBuilder.java`
#### Snippet
```java
                    if (params instanceof RandomAccess)
                    {
                        List<ClientBehaviorContext.Parameter> list = (List<ClientBehaviorContext.Parameter>) params;
                        for (int i = 0, size = list.size(); i < size; i++)
                        {
```

## RuleId[ruleID=UnusedAssignment]
### UnusedAssignment
Variable `factoryClassNames` initializer `null` is redundant
in `api/src/main/java/jakarta/faces/FactoryFinder.java`
#### Snippet
```java

        ClassLoader classLoader = ClassUtils.getContextClassLoader();
        Map<String, List<String>> factoryClassNames = null;
        synchronized (registeredFactoryNames)
        {
```

### UnusedAssignment
Variable `implClass` initializer `null` is redundant
in `api/src/main/java/jakarta/faces/FactoryFinder.java`
#### Snippet
```java
            {
                String implClassName = classNamesIterator.next();
                Class<?> implClass = null;
                try
                {
```

### UnusedAssignment
Variable `factoryClassNames` initializer `null` is redundant
in `api/src/main/java/jakarta/faces/FactoryFinder.java`
#### Snippet
```java
        // using update feature each time of myfaces (org.apache.myfaces.CONFIG_REFRESH_PERIOD)
        // In this moment, a concurrency problem could happen
        Map<String, List<String>> factoryClassNames = null;
        Map<String, Object> factoryMap = null;

```

### UnusedAssignment
Variable `factoryMap` initializer `null` is redundant
in `api/src/main/java/jakarta/faces/FactoryFinder.java`
#### Snippet
```java
        // In this moment, a concurrency problem could happen
        Map<String, List<String>> factoryClassNames = null;
        Map<String, Object> factoryMap = null;

        synchronized (registeredFactoryNames)
```

### UnusedAssignment
The value changed at `i++` is never used
in `api/src/main/java/jakarta/faces/event/PhaseId.java`
#### Snippet
```java
        INVOKE_APPLICATION = new PhaseId("INVOKE_APPLICATION", i++);
        list.add(INVOKE_APPLICATION);
        RENDER_RESPONSE = new PhaseId("RENDER_RESPONSE", i++);
        list.add(RENDER_RESPONSE);
        VALUES = Collections.unmodifiableList(list);
```

### UnusedAssignment
Variable `_keyIterator` initializer `null` is redundant
in `api/src/main/java/jakarta/faces/model/ResultSetDataModel.java`
#### Snippet
```java
    private static class WrapResultSetKeysIterator implements Iterator<String>
    {
        private Iterator<String> _keyIterator = null;

        public WrapResultSetKeysIterator(WrapResultSetMap map)
```

### UnusedAssignment
Variable `_keyIterator` initializer `null` is redundant
in `api/src/main/java/jakarta/faces/model/ResultSetDataModel.java`
#### Snippet
```java
    {
        private WrapResultSetMap _wrapMap = null;
        private Iterator<String> _keyIterator = null;

        public WrapResultSetEntriesIterator(WrapResultSetMap wrapMap)
```

### UnusedAssignment
Variable `_wrapMap` initializer `null` is redundant
in `api/src/main/java/jakarta/faces/model/ResultSetDataModel.java`
#### Snippet
```java
    private static class WrapResultSetEntriesIterator implements Iterator<Map.Entry<String, Object>>
    {
        private WrapResultSetMap _wrapMap = null;
        private Iterator<String> _keyIterator = null;

```

### UnusedAssignment
Variable `parsed` initializer `null` is redundant
in `api/src/main/java/jakarta/faces/convert/NumberConverter.java`
#### Snippet
```java
        throws ParseException
    {
        Object parsed = null;
        
        ParsePosition parsePosition = new ParsePosition(0);
```

### UnusedAssignment
Variable `bld` initializer `null` is redundant
in `api/src/main/java/jakarta/faces/component/UIForm.java`
#### Snippet
```java
    public String createUniqueId(FacesContext context, String seed)
    {
        StringBuilder bld = null;
        
        // When prependId is set to false, it is necessary to append an unique
```

### UnusedAssignment
Variable `returnValue` initializer `null` is redundant
in `api/src/main/java/jakarta/faces/component/_DeltaStateHelper.java`
#### Snippet
```java
    public Object put(Serializable key, Object value)
    {
        Object returnValue = null;
        if (_createDeltas(key))
        {
```

### UnusedAssignment
Variable `returnValue` initializer `null` is redundant
in `api/src/main/java/jakarta/faces/component/_DeltaStateHelper.java`
#### Snippet
```java
    public Object remove(Serializable key)
    {
        Object returnValue = null;
        if (_createDeltas(key))
        {
```

### UnusedAssignment
Variable `newArray` initializer `null` is redundant
in `api/src/main/java/jakarta/faces/component/_ArrayMap.java`
#### Snippet
```java
            Object value, int increment)
    {
        Object[] newArray = null;
        if (array != null)
        {
```

### UnusedAssignment
The value `0` assigned to `this.listenerCapability` is never used
in `api/src/main/java/jakarta/faces/component/_EventListenerWrapper.java`
#### Snippet
```java
    private void initListenerCapability()
    {
        this.listenerCapability = 0;
        if (this.listener instanceof UIComponent)
        {
```

### UnusedAssignment
Variable `lst` initializer `null` is redundant
in `api/src/main/java/jakarta/faces/component/_DeltaList.java`
#### Snippet
```java
        if (initialStateMarked())
        {
            Object [] lst = null;
            boolean nullDelta = true;
            if (size > 0)
```

### UnusedAssignment
Variable `lst` initializer `null` is redundant
in `api/src/main/java/jakarta/faces/component/behavior/_DeltaList.java`
#### Snippet
```java
        if (initialStateMarked())
        {
            Object [] lst = null;
            boolean nullDelta = true;
            if (size > 0)
```

### UnusedAssignment
Variable `returnValue` initializer `null` is redundant
in `api/src/main/java/jakarta/faces/component/behavior/_DeltaStateHelper.java`
#### Snippet
```java
    public Object remove(Serializable key)
    {
        Object returnValue = null;
        if (_createDeltas())
        {
```

### UnusedAssignment
Variable `returnValue` initializer `null` is redundant
in `api/src/main/java/jakarta/faces/component/behavior/_DeltaStateHelper.java`
#### Snippet
```java
    public Object put(Serializable key, Object value)
    {
        Object returnValue = null;
        if (_createDeltas())
        {
```

### UnusedAssignment
Variable `returnValue` initializer `null` is redundant
in `api/src/main/java/jakarta/faces/component/behavior/_AjaxBehaviorDeltaStateHelper.java`
#### Snippet
```java
    public Object remove(Serializable key)
    {
        Object returnValue = null;
        if (_createDeltas())
        {
```

### UnusedAssignment
Variable `returnValue` initializer `null` is redundant
in `api/src/main/java/jakarta/faces/component/behavior/_AjaxBehaviorDeltaStateHelper.java`
#### Snippet
```java
    public Object put(Serializable key, Object value)
    {
        Object returnValue = null;
        if (_createDeltas())
        {
```

### UnusedAssignment
Variable `childState` initializer `null` is redundant
in `api/src/main/java/jakarta/faces/component/UIData.java`
#### Snippet
```java
                Object childInitialState = null;
                Object descendantInitialState = null;
                Object childState = null;
                String childId = null;
                childState = (state == null) ? null : state.get(component.getClientId(facesContext));
```

### UnusedAssignment
Variable `childId` initializer `null` is redundant
in `api/src/main/java/jakarta/faces/component/UIData.java`
#### Snippet
```java
                Object descendantInitialState = null;
                Object childState = null;
                String childId = null;
                childState = (state == null) ? null : state.get(component.getClientId(facesContext));

```

### UnusedAssignment
Variable `state` initializer `null` is redundant
in `api/src/main/java/jakarta/faces/component/UIData.java`
#### Snippet
```java
                        : child.getChildren().iterator();
                Object descendantState = saveDescendantInitialComponentStates(facesContext, childsIterator, true);
                Object state = null;
                if (child.initialStateMarked())
                {
```

### UnusedAssignment
Variable `childId` initializer `null` is redundant
in `api/src/main/java/jakarta/faces/component/UIData.java`
#### Snippet
```java
                Object childState = null;
                Object descendantState = null;
                String childId = null;
                if (descendantStateIterator != null && descendantStateIterator.hasNext())
                {
```

### UnusedAssignment
Variable `evaluatedParameters` initializer `null` is redundant
in `api/src/main/java/jakarta/faces/application/_NavigationUtils.java`
#### Snippet
```java
            Map<String, List<String>> parameters)
    {
        Map<String,List<String>> evaluatedParameters = null;
        if (parameters != null && parameters.size() > 0)
        {
```

### UnusedAssignment
Variable `importHandlerClass` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/el/resolver/ImportHandlerResolver.java`
#### Snippet
```java
    {
        Class elClass = null;
        Class importHandlerClass = null;
        Constructor elClassConstructor = null;
        Method getImportHandlerMethod = null;
```

### UnusedAssignment
Variable `constructor` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/cdi/model/FacesDataModelManager.java`
#### Snippet
```java
            }
            
            Constructor constructor = null;
            if (selectedConstructor != null)
            {
```

### UnusedAssignment
Variable `lpf` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/spi/FacesConfigResourceProviderFactory.java`
#### Snippet
```java
            return instance;
        }
        FacesConfigResourceProviderFactory lpf = null;
        try
        {
```

### UnusedAssignment
Variable `lpf` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/spi/AnnotationProviderFactory.java`
#### Snippet
```java
            return instance;
        }
        AnnotationProviderFactory lpf = null;
        try
        {
```

### UnusedAssignment
Variable `lpf` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/spi/InjectionProviderFactory.java`
#### Snippet
```java
            return instance;
        }
        InjectionProviderFactory lpf = null;
        try
        {
```

### UnusedAssignment
Variable `lpf` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/spi/StateCacheProviderFactory.java`
#### Snippet
```java
            return instance;
        }
        StateCacheProviderFactory lpf = null;
        try
        {
```

### UnusedAssignment
Variable `lpf` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/spi/FaceletConfigResourceProviderFactory.java`
#### Snippet
```java
            return instance;
        }
        FaceletConfigResourceProviderFactory lpf = null;
        try
        {
```

### UnusedAssignment
Variable `lpf` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/spi/ResourceLibraryContractsProviderFactory.java`
#### Snippet
```java
            return instance;
        }
        ResourceLibraryContractsProviderFactory lpf = null;
        try
        {
```

### UnusedAssignment
Variable `instanceManagers` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/spi/impl/Tomcat7AnnotationInjectionProvider.java`
#### Snippet
```java
{

    private WeakHashMap<ClassLoader, InstanceManager> instanceManagers = null;

    public Tomcat7AnnotationInjectionProvider(ExternalContext externalContext)
```

### UnusedAssignment
Variable `profiles` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/spi/impl/DefaultServiceProviderFinder.java`
#### Snippet
```java
    {
        // Use LinkedHashSet to preserve iteration order
        Enumeration<URL> profiles = null;
        try
        {
```

### UnusedAssignment
Variable `injectionProvider` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/spi/impl/DefaultInjectionProviderFactory.java`
#### Snippet
```java
    public InjectionProvider getInjectionProvider(ExternalContext externalContext)
    {
        InjectionProvider injectionProvider = null;
        if (externalContext == null)
        {
```

### UnusedAssignment
Variable `flow` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/flow/FlowUtils.java`
#### Snippet
```java
    public static String getFlowMapKey(FacesContext facesContext, FlowReference flowReference)
    {
        Flow flow = null;
        if (flowReference.getDocumentId() == null)
        {
```

### UnusedAssignment
The value `((ServletResponseWrapper) response).getResponse()` assigned to `response` is never used
in `impl/src/main/java/org/apache/myfaces/util/ExternalContextUtils.java`
#### Snippet
```java
            {
                // unwrap
                response = ((ServletResponseWrapper) response).getResponse();
            }
            // no more possibilities to find a HttpServletResponse
```

### UnusedAssignment
The value `null` assigned to `value` is never used
in `impl/src/main/java/org/apache/myfaces/util/lang/SkipMatchIterator.java`
#### Snippet
```java
                if (hasNext())
                {
                    value = null;
                    return this.value;
                }
```

### UnusedAssignment
The value `true` assigned to `outboundCallNodeProcessed` is never used
in `impl/src/main/java/org/apache/myfaces/flow/FlowHandlerImpl.java`
#### Snippet
```java
            Map<String, Object> outboundParameters = doBeforeEnterFlow(context, 
                targetFlow, !outboundCallNodeProcessed ? outboundCallNode : null);
            outboundCallNodeProcessed = true;
            pushFlowReference(context, clientWindow, 
                    new FlowReference(targetFlow.getDefiningDocumentId(), targetFlow.getId()), 
```

### UnusedAssignment
The value `true` assigned to `outboundCallNodeProcessed` is never used
in `impl/src/main/java/org/apache/myfaces/flow/FlowHandlerImpl.java`
#### Snippet
```java
                    Map<String, Object> outboundParameters = doBeforeEnterFlow(context,
                        targetFlow, !outboundCallNodeProcessed ? outboundCallNode : null);
                    outboundCallNodeProcessed = true;
                    pushFlowReference(context, clientWindow, 
                            new FlowReference(targetFlow.getDefiningDocumentId(), targetFlow.getId()), toViewId,
```

### UnusedAssignment
The value `true` assigned to `outboundCallNodeProcessed` is never used
in `impl/src/main/java/org/apache/myfaces/flow/FlowHandlerImpl.java`
#### Snippet
```java
                Map<String, Object> outboundParameters = doBeforeEnterFlow(context, 
                    targetFlow, !outboundCallNodeProcessed ? outboundCallNode : null);
                outboundCallNodeProcessed = true;
                pushFlowReference(context, clientWindow, 
                        new FlowReference(targetFlow.getDefiningDocumentId(), targetFlow.getId()), toViewId,
```

### UnusedAssignment
Variable `node` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/flow/FlowHandlerImpl.java`
#### Snippet
```java
                Flow currentFlow = null;
                FlowCallNode outboundCallNode = null;
                FlowNode node = null;
                if (targetFlow == null)
                {
```

### UnusedAssignment
Variable `locale` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/util/MessageUtils.java`
#### Snippet
```java
    public static FacesMessage getMessageFromBundle(String bundleBaseName, String messageId, Object[] params)
    {
        Locale locale = null;
        FacesContext context = FacesContext.getCurrentInstance();
        if(context != null && context.getViewRoot() != null)
```

### UnusedAssignment
Variable `buf` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/util/lang/FastByteArrayInputStream.java`
#### Snippet
```java
     * Our byte buffer
     */
    protected byte[] buf = null;

    /**
```

### UnusedAssignment
Variable `count` initializer `0` is redundant
in `impl/src/main/java/org/apache/myfaces/util/lang/FastByteArrayInputStream.java`
#### Snippet
```java
     * Number of bytes that we can read from the buffer
     */
    protected int count = 0;

    /**
```

### UnusedAssignment
Variable `buf` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/util/lang/FastByteArrayOutputStream.java`
#### Snippet
```java
public class FastByteArrayOutputStream extends OutputStream
{
    protected byte[] buf = null;
    protected int size = 0;

```

### UnusedAssignment
Variable `size` initializer `0` is redundant
in `impl/src/main/java/org/apache/myfaces/util/lang/FastByteArrayOutputStream.java`
#### Snippet
```java
{
    protected byte[] buf = null;
    protected int size = 0;

    public FastByteArrayOutputStream()
```

### UnusedAssignment
The value `null` assigned to `old` is never used
in `impl/src/main/java/org/apache/myfaces/util/lang/FastByteArrayOutputStream.java`
#### Snippet
```java
            buf = new byte[Math.max(sz, 2 * buf.length)];
            System.arraycopy(old, 0, buf, 0, old.length);
            old = null;
        }
    }
```

### UnusedAssignment
Variable `newCurrent` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/util/lang/ClassUtils.java`
#### Snippet
```java
            {
                // let's check if class supports the decorator pattern
                T newCurrent = null;
                try
                {
```

### UnusedAssignment
Variable `_delegate` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/SwitchableWriter.java`
#### Snippet
```java
class SwitchableWriter extends Writer
{
    Writer _delegate = null;
    ResponseSwitch _responseSwitch = null;

```

### UnusedAssignment
Variable `_responseSwitch` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/SwitchableWriter.java`
#### Snippet
```java
{
    Writer _delegate = null;
    ResponseSwitch _responseSwitch = null;

    public SwitchableWriter(Writer delegate, ResponseSwitch responseSwitch)
```

### UnusedAssignment
Variable `_responseSwitch` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/SwitchableOutputStream.java`
#### Snippet
```java

    ServletOutputStream _delegate = null;
    ResponseSwitch _responseSwitch = null;

    public SwitchableOutputStream(ServletOutputStream delegate, ResponseSwitch responseSwitch)
```

### UnusedAssignment
Variable `_delegate` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/SwitchableOutputStream.java`
#### Snippet
```java
{

    ServletOutputStream _delegate = null;
    ResponseSwitch _responseSwitch = null;

```

### UnusedAssignment
Variable `serializedView` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/FullStateManagementStrategy.java`
#### Snippet
```java
        }
        
        Object serializedView = null;
        ResponseStateManager responseStateManager = facesContext.getRenderKit().getResponseStateManager();

```

### UnusedAssignment
Variable `spaceLeft` initializer `0` is redundant
in `impl/src/main/java/org/apache/myfaces/util/lang/StreamCharBuffer.java`
#### Snippet
```java
    private int appendCharBufferChunk(boolean flushInConnected) throws IOException
    {
        int spaceLeft = 0;
        if (flushInConnected && isConnectedMode())
        {
```

### UnusedAssignment
Variable `lastAccessed` initializer `0` is redundant
in `impl/src/main/java/org/apache/myfaces/util/lang/ConcurrentLRUCache.java`
#### Snippet
```java
        K key;
        V value;
        volatile long lastAccessed = 0;
        long lastAccessedCopy = 0;

```

### UnusedAssignment
The value `-1` assigned to `newNewestEntry` is never used
in `impl/src/main/java/org/apache/myfaces/util/lang/ConcurrentLRUCache.java`
#### Snippet
```java
                newOldestEntry = Long.MAX_VALUE;
                newestEntry = newNewestEntry;
                newNewestEntry = -1;
                wantToKeep = lowerWaterMark - numKept;
                wantToRemove = sz - lowerWaterMark - numRemoved;
```

### UnusedAssignment
Variable `viewStructureMetadata` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/ViewPoolProcessor.java`
#### Snippet
```java
        if (viewPool != null && viewPool.isWorthToRecycleThisView(context, view))
        {
            ViewStructureMetadata viewStructureMetadata = null;
            if (faceletViewState == null)
            {
```

### UnusedAssignment
Variable `t` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/CompositeTagDecorator.java`
#### Snippet
```java
    public Tag decorate(Tag tag)
    {
        Tag t = null;
        for (int i = 0; i < this.decorators.length; i++)
        {
```

### UnusedAssignment
Variable `data` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/MetaRulesetImpl.java`
#### Snippet
```java
            for (Map.Entry<String, TagAttribute> entry : _attributes.entrySet())
            {
                Metadata data = null;

                int i = ruleEnd;
```

### UnusedAssignment
Variable `data` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/MetaRulesetImpl.java`
#### Snippet
```java
            for (TagAttribute passthroughAttribute : _passthroughAttributes)
            {
                Metadata data = null;

                int i = ruleEnd;
```

### UnusedAssignment
Variable `c` initializer `0` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ELText.java`
#### Snippet
```java
        int i = s;
        int len = ca.length;
        char c = 0;
        int str = 0;
        int nest = 0;
```

### UnusedAssignment
Variable `c` initializer `0` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ELText.java`
#### Snippet
```java
        char[] ca = in.toCharArray();
        int i = 0;
        char c = 0;
        int len = ca.length;
        int end = len - 1;
```

### UnusedAssignment
Variable `vlen` initializer `0` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ELText.java`
#### Snippet
```java
        int end = len - 1;
        boolean esc = false;
        int vlen = 0;

        StringBuilder buff = null;
```

### UnusedAssignment
Variable `t` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ELText.java`
#### Snippet
```java
        StringBuilder buff = null;
        List<ELText> text = null;
        ELText t = null;
        ValueExpression ve = null;

```

### UnusedAssignment
Variable `ve` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ELText.java`
#### Snippet
```java
        List<ELText> text = null;
        ELText t = null;
        ValueExpression ve = null;

        while (i < len)
```

### UnusedAssignment
Variable `c` initializer `0` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ELText.java`
#### Snippet
```java
        char[] ca = in.toCharArray();
        int i = 0;
        char c = 0;
        int len = ca.length;
        int end = len - 1;
```

### UnusedAssignment
Variable `c` initializer `0` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ELText.java`
#### Snippet
```java
        char[] ca = in.toCharArray();
        int i = 0;
        char c = 0;
        int len = ca.length;
        int end = len - 1;
```

### UnusedAssignment
Variable `vlen` initializer `0` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ELText.java`
#### Snippet
```java
        int end = len - 1;
        boolean esc = false;
        int vlen = 0;

        StringBuilder buff = null;
```

### UnusedAssignment
Variable `t` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ELText.java`
#### Snippet
```java
        StringBuilder buff = null;
        List<ELText> text = null;
        ELText t = null;
        ValueExpression ve = null;

```

### UnusedAssignment
Variable `ve` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ELText.java`
#### Snippet
```java
        List<ELText> text = null;
        ELText t = null;
        ValueExpression ve = null;

        while (i < len)
```

### UnusedAssignment
Variable `found` initializer `false` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/InsertHandler.java`
#### Snippet
```java
        actx.extendClient(this);

        boolean found = false;
        try
        {
```

### UnusedAssignment
Variable `methodExpression` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/TagAttributeImpl.java`
#### Snippet
```java
        try
        {
            MethodExpression methodExpression = null;
            
            // From this point we can suppose this attribute contains a ELExpression
```

### UnusedAssignment
Variable `b` initializer `false` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/IfHandler.java`
#### Snippet
```java
            fcc.startComponentUniqueIdSection(), tagId);
        Boolean restoredValue = (Boolean) ComponentSupport.restoreInitialTagState(ctx, fcc, parent, uniqueId);
        boolean b = false;
        boolean markInitialState = false;
        try
```

### UnusedAssignment
Variable `src` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/LegacyForEachHandler.java`
#### Snippet
```java

        boolean t = this.getTransient(ctx);
        Object src = null;
        ValueExpression srcVE = null;
        if (this.items != null)
```

### UnusedAssignment
Variable `ve` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/LegacyForEachHandler.java`
#### Snippet
```java
                    String v = this.getVarName(ctx);
                    String vs = this.getVarStatusName(ctx);
                    ValueExpression ve = null;
                    ValueExpression vO = this.capture(v, pctx);
                    ValueExpression vsO = this.capture(vs, pctx);
```

### UnusedAssignment
Variable `mi` initializer `0` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/LegacyForEachHandler.java`
#### Snippet
```java
                    ValueExpression vO = this.capture(v, pctx);
                    ValueExpression vsO = this.capture(vs, pctx);
                    int mi = 0;
                    Object value = null;
                    try
```

### UnusedAssignment
Variable `value` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/LegacyForEachHandler.java`
#### Snippet
```java
                    ValueExpression vsO = this.capture(vs, pctx);
                    int mi = 0;
                    Object value = null;
                    try
                    {
```

### UnusedAssignment
Variable `bundle` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/LoadBundleHandler.java`
#### Snippet
```java
    {
        UIViewRoot root = ComponentSupport.getViewRoot(ctx, parent);
        ResourceBundle bundle = null;
        try
        {
```

### UnusedAssignment
Variable `validator` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ComponentTagHandlerDelegate.java`
#### Snippet
```java
            else
            {
                Validator validator = null;
                // create it
                validator = context.getApplication().createValidator(validatorId);
```

### UnusedAssignment
Variable `c` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ComponentTagHandlerDelegate.java`
#### Snippet
```java
            return _componentBuilderHandlerDelegate.createComponent(ctx);
        }
        UIComponent c = null;
        FacesContext faces = ctx.getFacesContext();
        Application app = faces.getApplication();
```

### UnusedAssignment
Variable `targetAttributeName` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguage.java`
#### Snippet
```java
                    }

                    String targetAttributeName = null;
                    ValueExpression targetAttributeNameVE = (ValueExpression)
                            propertyDescriptor.getValue("targetAttributeName");
```

### UnusedAssignment
Variable `actionListener` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguage.java`
#### Snippet
```java

        // target is ActionSource2
        ActionListener actionListener = null;
        // If it is a redirection, a wrapper is used to locate the right instance and call
        //it properly.
```

### UnusedAssignment
Variable `validator` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguage.java`
#### Snippet
```java

        // target is EditableValueHolder
        Validator validator = null;
        // If it is a redirection, a wrapper is used to locate the right instance and call it properly.
        if (ccAttrMeRedirection)
```

### UnusedAssignment
Variable `facelet` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguage.java`
#### Snippet
```java
                    // the result is stored inside created UIViewRoot, so we can safely take the derived
                    // viewId from there.
                    Facelet facelet = null;
                    try
                    {
```

### UnusedAssignment
Variable `beanInfo` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguage.java`
#### Snippet
```java
    public BeanInfo getComponentMetadata(FacesContext context, Resource componentResource)
    {
        BeanInfo beanInfo = null;

        Assert.notNull(context, "context");
```

### UnusedAssignment
Variable `valueChangeListener` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguage.java`
#### Snippet
```java

        // target is EditableValueHolder
        ValueChangeListener valueChangeListener = null;
        // If it is a redirection, a wrapper is used to locate the right instance and call it properly.
        if (ccAttrMeRedirection)
```

### UnusedAssignment
Variable `targetAttributeName` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguage.java`
#### Snippet
```java
            }

            String targetAttributeName = null;
            ValueExpression targetAttributeNameVE
                    = (ValueExpression) propertyDescriptor.getValue("targetAttributeName");
```

### UnusedAssignment
Variable `methodExpression` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguage.java`
#### Snippet
```java
                    else
                    {
                        MethodExpression methodExpression = null;
                        // composite:attribute targets property only has sense for action, actionListener,
                        // validator or valueChangeListener. This means we have to retarget the method expression
```

### UnusedAssignment
Variable `itr` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ComponentSupport.java`
#### Snippet
```java
    public static String findChildInFacetsByTagId(UIComponent parent, String id)
    {
        Iterator<Map.Entry<String, UIComponent>> itr = null;
        if (parent.getFacetCount() > 0)
        {
```

### UnusedAssignment
Variable `itr` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ComponentSupport.java`
#### Snippet
```java
    public static UIComponent findChildByTagId(UIComponent parent, String id)
    {
        Iterator<UIComponent> itr = null;
        if (parent.getChildCount() > 0)
        {
```

### UnusedAssignment
Variable `method` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ComponentSupport.java`
#### Snippet
```java
    static
    {
        Method method = null;
        try
        {
```

### UnusedAssignment
Variable `eventClass` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/EventHandler.java`
#### Snippet
```java
    private Class<? extends ComponentSystemEvent> getEventClass(FaceletContext context)
    {
        Class<?> eventClass = null;
        String value = null;
        
```

### UnusedAssignment
Variable `value` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/EventHandler.java`
#### Snippet
```java
    {
        Class<?> eventClass = null;
        String value = null;
        
        if (type.isLiteral() && eventClassLiteral != null)
```

### UnusedAssignment
Variable `contextComponent` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/ResetValuesActionListenerHandler.java`
#### Snippet
```java
            
            // Calculate the final clientIds
            UIComponent contextComponent = null;
            if (topCompositeComponentReference != null)
            {
```

### UnusedAssignment
Variable `clientIds` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/ResetValuesActionListenerHandler.java`
#### Snippet
```java
            
            Object value = renderExpression.getValue(faces.getELContext());
            Collection<String> clientIds = null;
            if (value == null)
            {
```

### UnusedAssignment
Variable `contextComponent` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/ResetValuesActionListenerHandler.java`
#### Snippet
```java
            
            // Calculate the final clientIds
            UIComponent contextComponent = null;
            if (topCompositeComponentReference != null)
            {
```

### UnusedAssignment
Variable `listener` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/ResetValuesActionListenerHandler.java`
#### Snippet
```java

        ActionSource as = (ActionSource) parent;
        ActionListener listener = null;
        if (_render.isLiteral())
        {
```

### UnusedAssignment
Variable `innerComponent` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/AttachedObjectTargetImpl.java`
#### Snippet
```java
            {
                int separator = target.indexOf(separatorChar);
                UIComponent innerComponent = null;
                if (separator == -1)
                {
```

### UnusedAssignment
Variable `data` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeMetaRulesetImpl.java`
#### Snippet
```java
            for (Map.Entry<String, TagAttribute> entry : _attributes.entrySet())
            {
                Metadata data = null;

                int i = ruleEnd;
```

### UnusedAssignment
Variable `children` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeComponentResourceTagHandler.java`
#### Snippet
```java
                    // associated tag handler, so in this case we just need to remove and add them in the same order 
                    // we found them
                    List<UIComponent> children = null;
                    if (facetName == null)
                    {
```

### UnusedAssignment
Variable `found` initializer `false` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFaceletContext.java`
#### Snippet
```java
            {
                this._names.add(testName);
                boolean found = false;
                AbstractFaceletContext actx = new DefaultFaceletContext(
                        (DefaultFaceletContext) ctx, this._owner, false);
```

### UnusedAssignment
Variable `src` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/ForEachHandler.java`
#### Snippet
```java
    {
        int e = this.getEnd(ctx);
        Object src = null;
        ValueExpression srcVE = null;
        if (this.items != null)
```

### UnusedAssignment
Variable `mi` initializer `0` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/ForEachHandler.java`
#### Snippet
```java
            ValueExpression vO = this.capture(v, pctx);
            ValueExpression vsO = this.capture(vs, pctx);
            int mi = 0;
            Object value = null;
            int stateIndex = 0;
```

### UnusedAssignment
Variable `value` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/ForEachHandler.java`
#### Snippet
```java
            ValueExpression vsO = this.capture(vs, pctx);
            int mi = 0;
            Object value = null;
            int stateIndex = 0;
            try
```

### UnusedAssignment
Variable `base` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/ForEachHandler.java`
#### Snippet
```java
                    value = itr.next();
                    Object[] stateValue = null; /*restoredSavedOption.getValueList().get(stateIndex);*/
                    String base = null;
                    boolean found = false;

```

### UnusedAssignment
Variable `value` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/ForEachHandler.java`
#### Snippet
```java
        ValueExpression vO = this.capture(v, pctx);
        ValueExpression vsO = this.capture(vs, pctx);
        Object value = null;
        try
        {
```

### UnusedAssignment
Variable `mi` initializer `0` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/ForEachHandler.java`
#### Snippet
```java
            ValueExpression vO = this.capture(v, pctx);
            ValueExpression vsO = this.capture(vs, pctx);
            int mi = 0;
            Object value = null;
            try
```

### UnusedAssignment
Variable `value` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/ForEachHandler.java`
#### Snippet
```java
            ValueExpression vsO = this.capture(vs, pctx);
            int mi = 0;
            Object value = null;
            try
            {
```

### UnusedAssignment
Variable `found` initializer `false` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/TemplateContextImpl.java`
#### Snippet
```java
                }
                this._names.add(testName);
                boolean found = false;
                AbstractFaceletContext actx = new DefaultFaceletContext(
                        (DefaultFaceletContext) ctx, this._owner, false);
```

### UnusedAssignment
Variable `fcctx` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFacelet.java`
#### Snippet
```java
         throws IOException, FacesException, FaceletException, ELException
    {
        FaceletCompositionContext fcctx = null;
        boolean faceletCompositionContextInitialized = false;
        fcctx = FaceletCompositionContext.getCurrentInstance(facesContext);
```

### UnusedAssignment
Variable `cc` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFacelet.java`
#### Snippet
```java
            if (sz > 0)
            {
                UIComponent cc = null;
                List<UIComponent> cl = c.getChildren();
                ApplyToken token;
```

### UnusedAssignment
Variable `id` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/FaceletCompositionContextImpl.java`
#### Snippet
```java
    public void finalizeRelocatableResourcesForDeletion(UIViewRoot root)
    {
        String id = null;
        //Check facets 
        if (root.getFacetCount() > 0)
```

### UnusedAssignment
Variable `_viewRoot` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/FaceletCompositionContextImpl.java`
#### Snippet
```java
    
    private VisitContextFactory _visitContextFactory = null;
    private UIViewRoot _viewRoot = null;
    private MyfacesConfig myfacesConfig;
    
```

### UnusedAssignment
Variable `ns` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/NamespaceManager.java`
#### Snippet
```java
    public String getNamespace(String prefix)
    {
        NS ns = null;
        for (int i = 0; i < this.namespaces.size(); i++)
        {
```

### UnusedAssignment
Variable `ns` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/NamespaceManager.java`
#### Snippet
```java
        if (this.namespaces.size() > 0)
        {
            NS ns = null;
            for (int i = this.namespaces.size() - 1; i >= 0; i--)
            {
```

### UnusedAssignment
Variable `ns` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/NamespaceManager.java`
#### Snippet
```java
    public void popNamespace(String prefix)
    {
        NS ns = null;
        for (int i = 0; i < this.namespaces.size(); i++)
        {
```

### UnusedAssignment
Variable `t` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TagLibraryConfig.java`
#### Snippet
```java
        if (StringUtils.isNotEmpty(faceletTagLibrary.getLibraryClass()))
        {
            TagLibrary t = null;
            Class<?> type;
            try
```

### UnusedAssignment
Variable `p` initializer `0` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/CompilationManager.java`
#### Snippet
```java
        {
            List<TagAttribute> attrList = new ArrayList<TagAttribute>(attr.length);
            int p = 0;
            for (int i = 0; i < attr.length; i++)
            {
```

### UnusedAssignment
Variable `dirExists` initializer `false` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/util/Classpath.java`
#### Snippet
```java
    private static boolean _searchDir(Set<URL> result, File dir, String suffix) throws IOException
    {
        boolean dirExists = false;
        if (System.getSecurityManager() != null)
        {
```

### UnusedAssignment
Variable `leftText` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TextUnit.java`
#### Snippet
```java
                int leftChar = 0; // 0=first char on left 1=\n 2=\r 3=\r\n
                int lenght = text.length();
                String leftText = null;
                for (int j = 0; j < lenght; j++)
                {
```

### UnusedAssignment
Variable `leftText` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TextUnit.java`
#### Snippet
```java
        int leftChar = 0; // 0=first char on left 1=\n 2=\r 3=\r\n
        int lenght = text.length();
        String leftText = null;
        for (int j = 0; j < lenght; j++)
        {
```

### UnusedAssignment
Variable `t` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TagLibraryConfigUnmarshallerImpl.java`
#### Snippet
```java
    {
        InputStream is = null;
        FaceletTagLibrary t = null;
        URLConnection conn = null;
        try
```

### UnusedAssignment
Variable `conn` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TagLibraryConfigUnmarshallerImpl.java`
#### Snippet
```java
        InputStream is = null;
        FaceletTagLibrary t = null;
        URLConnection conn = null;
        try
        {
```

### UnusedAssignment
Variable `mngr` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/SAXCompiler.java`
#### Snippet
```java
            throws IOException, FaceletException, ELException, FacesException
    {
        CompilationManager mngr = null;
        InputStream is = null;
        String encoding = null;
```

### UnusedAssignment
Variable `encoding` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/SAXCompiler.java`
#### Snippet
```java
        CompilationManager mngr = null;
        InputStream is = null;
        String encoding = null;
        try
        {
```

### UnusedAssignment
Variable `mngr` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/SAXCompiler.java`
#### Snippet
```java
            throws IOException, FaceletException, ELException, FacesException
    {
        CompilationManager mngr = null;
        InputStream is = null;
        String encoding = null;
```

### UnusedAssignment
Variable `encoding` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/SAXCompiler.java`
#### Snippet
```java
        CompilationManager mngr = null;
        InputStream is = null;
        String encoding = null;
        try
        {
```

### UnusedAssignment
Variable `mngr` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/SAXCompiler.java`
#### Snippet
```java
            throws IOException, FaceletException, ELException, FacesException
    {
        CompilationManager mngr = null;
        InputStream is = null;
        String encoding = null;
```

### UnusedAssignment
Variable `encoding` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/SAXCompiler.java`
#### Snippet
```java
        CompilationManager mngr = null;
        InputStream is = null;
        String encoding = null;
        try
        {
```

### UnusedAssignment
Variable `_valid` initializer `true` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/component/UIRepeat.java`
#### Snippet
```java
        private boolean _localValueSet;
        private Object _submittedValue;
        private boolean _valid = true;
        private Object _value;

```

### UnusedAssignment
Variable `metadataComplete` initializer `false` is redundant
in `impl/src/main/java/org/apache/myfaces/config/DefaultFacesConfigurationMerger.java`
#### Snippet
```java

        //read metadata-complete attribute on WEB-INF/faces-config.xml
        boolean metadataComplete = false;
        if(webAppFacesConfig != null)
        {
```

### UnusedAssignment
Variable `appFilteredConfigResources` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/config/DefaultFacesConfigurationMerger.java`
#### Snippet
```java
        }

        List<FacesConfig> appFilteredConfigResources = null;

        //1. Pre filtering: Sort nodes according to its weight. The weight is the number of named
```

### UnusedAssignment
Variable `connection` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/config/annotation/PackageInfo.java`
#### Snippet
```java
        {
            URL resource = (URL) resources.nextElement();
            URLConnection connection = null;
            try
            {
```

### UnusedAssignment
Variable `jarFile` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/config/annotation/PackageInfo.java`
#### Snippet
```java
                // iterate trhough all the entries in the jar
                JarURLConnection juc = (JarURLConnection) connection;
                JarFile jarFile = null;
                try
                {
```

### UnusedAssignment
Variable `partialWriter` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/context/AjaxExceptionHandlerImpl.java`
#### Snippet
```java
            if (!throwableList.isEmpty())
            {
                PartialResponseWriter partialWriter = null;
                boolean responseComplete = false;

```

### UnusedAssignment
Variable `couldContainAnnotation` initializer `false` is redundant
in `impl/src/main/java/org/apache/myfaces/config/annotation/DefaultAnnotationProvider.java`
#### Snippet
```java

            DataInputStream in = null;
            boolean couldContainAnnotation = false;
            try
            {
```

### UnusedAssignment
Variable `classes` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/config/annotation/DefaultAnnotationProvider.java`
#### Snippet
```java

        Map<Class<? extends Annotation>, Set<Class<?>>> map = new HashMap<>();
        Collection<Class<?>> classes = null;

        //1. Scan for annotations on /WEB-INF/classes
```

### UnusedAssignment
Variable `path` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/config/annotation/DefaultAnnotationProvider.java`
#### Snippet
```java
        }

        String path = null;

        if (paths.isEmpty())
```

### UnusedAssignment
Variable `couldContainAnnotation` initializer `false` is redundant
in `impl/src/main/java/org/apache/myfaces/config/annotation/DefaultAnnotationProvider.java`
#### Snippet
```java
                {
                    DataInputStream in = null;
                    boolean couldContainAnnotation = false;
                    try
                    {
```

### UnusedAssignment
Variable `_facesContextFactory` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/context/servlet/FacesContextImpl.java`
#### Snippet
```java
    private boolean _validationFailed = false;
    private PartialViewContext _partialViewContext = null;
    private ReleasableFacesContextFactory _facesContextFactory = null;
    
    private PartialViewContextFactory _partialViewContextFactory = null;
```

### UnusedAssignment
Variable `app` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/config/annotation/AnnotationConfigurator.java`
#### Snippet
```java
                    if (val.isDefault())
                    {
                        ApplicationImpl app = null;
                        if (facesConfig.getApplications().isEmpty())
                        {
```

### UnusedAssignment
The value `true` assigned to `firstSlashAdded` is never used
in `impl/src/main/java/org/apache/myfaces/resource/AliasResourceMetaImpl.java`
#### Snippet
```java
                builder.append(getResourceVersion());
                builder.append(getRealResourceName().substring(getRealResourceName().lastIndexOf('.')));
                firstSlashAdded = true;
            }
            resourceIdentifier = builder.toString();
```

### UnusedAssignment
Variable `resourcesDirectory` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/resource/RootExternalContextResourceLoader.java`
#### Snippet
```java
    private String contractsDirectory = null;
    
    private String resourcesDirectory = null;

    public RootExternalContextResourceLoader()
```

### UnusedAssignment
Variable `contractsDirectory` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/resource/RootExternalContextResourceLoader.java`
#### Snippet
```java
    private static final String RESOURCES = "resources";
    
    private String contractsDirectory = null;
    
    private String resourcesDirectory = null;
```

### UnusedAssignment
Variable `path` initializer `""` is redundant
in `impl/src/main/java/org/apache/myfaces/resource/ResourceImpl.java`
#### Snippet
```java
            }
            
            String path = "";
            if (mapping.isExtensionMapping())
            {
```

### UnusedAssignment
Variable `metadata` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/resource/ResourceImpl.java`
#### Snippet
```java
            }

            String metadata = null;
            boolean useAmp = false;
            if (getLibraryName() != null)
```

### UnusedAssignment
The value `true` assigned to `useAmp` is never used
in `impl/src/main/java/org/apache/myfaces/resource/ResourceImpl.java`
#### Snippet
```java
            {
                path = path + (useAmp ? '&' : '?') + "con=" + _resourceMeta.getContractName();
                useAmp = true;
            }
            _requestPath = context.getApplication().getViewHandler().getResourceURL(context, path);
```

### UnusedAssignment
Variable `context` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/context/servlet/PartialViewContextImpl.java`
#### Snippet
```java
            EnumSet.of(VisitHint.EXECUTE_LIFECYCLE, VisitHint.SKIP_UNRENDERED));

    private FacesContext context = null;
    private boolean _released = false;
    // Cached values, since their parent methods could be called
```

### UnusedAssignment
Variable `is` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/resource/ClassLoaderContractResourceLoader.java`
#### Snippet
```java
    public InputStream getResourceInputStream(ResourceMeta resourceMeta)
    {
        InputStream is = null;

        String prefix = getPrefix();
```

### UnusedAssignment
Variable `url` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/resource/ClassLoaderContractResourceLoader.java`
#### Snippet
```java
    public URL getResourceURL(ResourceMeta resourceMeta)
    {
        URL url = null;

        String prefix = getPrefix();
```

### UnusedAssignment
Variable `url` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/resource/ClassLoaderResourceLoader.java`
#### Snippet
```java
    public URL getResourceURL(String resourceId)
    {
        URL url = null;

        String prefix = getPrefix();
```

### UnusedAssignment
Variable `is` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/resource/ClassLoaderResourceLoader.java`
#### Snippet
```java
    public InputStream getResourceInputStream(ResourceMeta resourceMeta)
    {
        InputStream is = null;

        String prefix = getPrefix();
```

### UnusedAssignment
The value `true` assigned to `firstSlashAdded` is never used
in `impl/src/main/java/org/apache/myfaces/resource/ResourceMetaImpl.java`
#### Snippet
```java
                builder.append(_resourceVersion);
                builder.append(_resourceName.substring(_resourceName.lastIndexOf('.')));
                firstSlashAdded = true;
            }

```

### UnusedAssignment
Variable `iterator` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/resource/ClassLoaderResourceLoaderIterator.java`
#### Snippet
```java
        private Deque<String> stack = new LinkedList<String>();
        
        Iterator<String> iterator = null;

        public JarDepthIterator(URL directory, String basePath, int maxDepth, ResourceVisitOption... options)
```

### UnusedAssignment
Variable `queryString` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/context/servlet/ServletExternalContextImpl.java`
#### Snippet
```java

        String fragment = null;
        String queryString = null;
        Map<String, List<String>> paramMap = null;

```

### UnusedAssignment
Variable `renderKitFactory` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/lifecycle/RestoreViewSupport.java`
#### Snippet
```java
    private static final Logger log = Logger.getLogger(RestoreViewSupport.class.getName());

    private RenderKitFactory renderKitFactory = null;
    private VisitContextFactory visitContextFactory = null;

```

### UnusedAssignment
Variable `visitContextFactory` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/lifecycle/RestoreViewSupport.java`
#### Snippet
```java

    private RenderKitFactory renderKitFactory = null;
    private VisitContextFactory visitContextFactory = null;

    public RestoreViewSupport(FacesContext facesContext)
```

### UnusedAssignment
Variable `line` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/lifecycle/clientwindow/CODIClientSideWindow.java`
#### Snippet
```java
        {
            BufferedReader reader = new BufferedReader(new InputStreamReader(is));
            String line = null;
            while ((line = reader.readLine()) != null)
            {
```

### UnusedAssignment
Variable `servletPathIndex` initializer `-1` is redundant
in `impl/src/main/java/org/apache/myfaces/lifecycle/RestoreViewExecutor.java`
#### Snippet
```java
            // In theory path = appContextPath + servletPath + pathInfo. 
            int appContextPathIndex = appContextPath != null ? path.indexOf(appContextPath) : -1;
            int servletPathIndex = -1;
            int pathInfoIndex = -1;
            if (ectx.getRequestServletPath() != null && ectx.getRequestPathInfo() != null)
```

### UnusedAssignment
Variable `expression` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/component/search/SearchExpressionHandlerImpl.java`
#### Snippet
```java
            //Split expression into tokens and apply loop
            String nextExpression = null;
            String expression = null;
            if (topExpression.indexOf(":@") > 0)
            {
```

### UnusedAssignment
Variable `writerContentType` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlRenderKitImpl.java`
#### Snippet
```java
        FacesContext facesContext = FacesContext.getCurrentInstance();
        String selectedContentType = null;
        String writerContentType = null;
        boolean isAjaxRequest = facesContext.getPartialViewContext().isAjaxRequest();
        String contentTypeListStringFromAccept = null;
```

### UnusedAssignment
Variable `behaviors` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlSelectableRendererBase.java`
#### Snippet
```java
        }

        Map<String, List<ClientBehavior>> behaviors = null;
        if (uiComponent instanceof ClientBehaviorHolder)
        {
```

### UnusedAssignment
Variable `valueType` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/RendererUtils.java`
#### Snippet
```java
        // Try to get the type from the actual value or,
        // if value == null, obtain the type from the ValueExpression
        Class<?> valueType = null;
        Object value = ve.getValue(facesContext.getELContext());
        valueType = (value != null) ? value.getClass() : ve.getType(facesContext.getELContext());
```

### UnusedAssignment
Variable `trimmedContent` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlResponseWriterImpl.java`
#### Snippet
```java
    {
        String content = getInternalBuffer().toString();
        String trimmedContent = null;
        
        if(_isXhtmlContentType)
```

### UnusedAssignment
Variable `behaviors` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTextRendererBase.java`
#### Snippet
```java
        RendererUtils.checkParamValidity(facesContext,component,null);
        
        Map<String, List<ClientBehavior>> behaviors = null;
        if (component instanceof ClientBehaviorHolder)
        {
```

### UnusedAssignment
Variable `behaviors` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTextRendererBase.java`
#### Snippet
```java
        renderValue(facesContext, component, writer);

        Map<String, List<ClientBehavior>> behaviors = null;
        if (component instanceof ClientBehaviorHolder)
        {
```

### UnusedAssignment
Variable `behaviors` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTextRendererBase.java`
#### Snippet
```java
      RendererUtils.checkParamValidity(facesContext,component,null);
      
      Map<String, List<ClientBehavior>> behaviors = null;
      if (component instanceof ClientBehaviorHolder)
      {
```

### UnusedAssignment
Variable `span` initializer `false` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessageRendererBase.java`
#### Snippet
```java
        ResponseWriter writer = facesContext.getResponseWriter();

        boolean span = false;

        Map<String, List<ClientBehavior>> behaviors = null;
```

### UnusedAssignment
Variable `bodyrows` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlGridRendererBase.java`
#### Snippet
```java
        {
            // get the row indizes for which a new TBODY element should be created
            Integer[] bodyrows = null;
            String bodyrowsAttr = (String) component.getAttributes().get(ComponentAttrs.BODYROWS_ATTR);
            if(bodyrowsAttr != null && !bodyrowsAttr.isEmpty())
```

### UnusedAssignment
Variable `behaviors` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlFormRendererBase.java`
#### Snippet
```java
        String method = getMethod(facesContext, htmlForm);

        Map<String, List<ClientBehavior>> behaviors = null;
        if (component instanceof ClientBehaviorHolder)
        {
```

### UnusedAssignment
Variable `behaviors` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlCheckboxRendererBase.java`
#### Snippet
```java
        }

        Map<String, List<ClientBehavior>> behaviors = null;
        if (uiComponent instanceof UISelectBoolean)
        {
```

### UnusedAssignment
Variable `behaviors` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMenuRendererBase.java`
#### Snippet
```java
        RendererUtils.checkParamValidity(facesContext, component, null);
        
        Map<String, List<ClientBehavior>> behaviors = null;
        if (component instanceof ClientBehaviorHolder)
        {
```

### UnusedAssignment
Variable `behaviors` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlSecretRendererBase.java`
#### Snippet
```java
      ResponseWriter writer = facesContext.getResponseWriter();
      
      Map<String, List<ClientBehavior>> behaviors = null;
      if (uiComponent instanceof ClientBehaviorHolder)
      {
```

### UnusedAssignment
Variable `style` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessagesRendererBase.java`
#### Snippet
```java
    protected String getComponentStyle(UIComponent messages)
    {
        String style = null;
        if (messages instanceof HtmlMessages)
        {
```

### UnusedAssignment
Variable `_componentMessagesIterator` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessagesRendererBase.java`
#### Snippet
```java
        private Iterator _globalMessagesIterator;
        private Iterator _clientIdsWithMessagesIterator;
        private Iterator _componentMessagesIterator = null;
        private String _clientId = null;
        private boolean _redisplay;
```

### UnusedAssignment
Variable `styleClass` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessagesRendererBase.java`
#### Snippet
```java
    protected String getComponentStyleClass(UIComponent messages)
    {
        String styleClass = null;
        if (messages instanceof HtmlMessages)
        {
```

### UnusedAssignment
Variable `_clientId` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessagesRendererBase.java`
#### Snippet
```java
        private Iterator _clientIdsWithMessagesIterator;
        private Iterator _componentMessagesIterator = null;
        private String _clientId = null;
        private boolean _redisplay;
        private Object _next;
```

### UnusedAssignment
Variable `behaviors` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlInputFileRendererBase.java`
#### Snippet
```java
        writer.writeAttribute(HTML.TYPE_ATTR, HTML.INPUT_TYPE_FILE, null);

        Map<String, List<ClientBehavior>> behaviors = null;
        if (component instanceof ClientBehaviorHolder)
        {
```

### UnusedAssignment
Variable `behaviors` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTextareaRendererBase.java`
#### Snippet
```java
        writer.startElement(HTML.TEXTAREA_ELEM, uiComponent);

        Map<String, List<ClientBehavior>> behaviors = null;
        if (uiComponent instanceof ClientBehaviorHolder)
        {
```

### UnusedAssignment
Variable `behaviors` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTextareaRendererBase.java`
#### Snippet
```java
        RendererUtils.checkParamValidity(facesContext, uiComponent, UIInput.class);

        Map<String, List<ClientBehavior>> behaviors = null;
        if (uiComponent instanceof ClientBehaviorHolder)
        {
```

### UnusedAssignment
Variable `labelClass` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/SelectItemsUtils.java`
#### Snippet
```java
                }

                String labelClass = null;

                if (componentDisabled || disabled)
```

### UnusedAssignment
Variable `behaviors` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlLinkRendererBase.java`
#### Snippet
```java
    {
        ResponseWriter writer = facesContext.getResponseWriter();
        Map<String, List<ClientBehavior>> behaviors = null;

        // h:commandLink can be rendered outside a form, but with warning (jsf 2.0 TCK)
```

### UnusedAssignment
Variable `behaviors` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlLinkRendererBase.java`
#### Snippet
```java
    {
        ResponseWriter writer = facesContext.getResponseWriter();
        Map<String, List<ClientBehavior>> behaviors = null;
        
        //calculate href
```

### UnusedAssignment
Variable `behaviors` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlLinkRendererBase.java`
#### Snippet
```java
    {
        ResponseWriter writer = facesContext.getResponseWriter();
        Map<String, List<ClientBehavior>> behaviors = null;

        if (HtmlRendererUtils.isDisabled(output))
```

### UnusedAssignment
Variable `fragmentAttr` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlLinkRendererBase.java`
#### Snippet
```java
            }
            // check for the fragement attribute
            String fragmentAttr = null;
            if (output instanceof HtmlOutputLink)
            {
```

### UnusedAssignment
Variable `onclick` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlLinkRendererBase.java`
#### Snippet
```java
        String serverEventCode = buildServerOnclick(facesContext, component, clientId, formInfo);
        
        String onclick = null;
        
        if (commandOnclick == null && (behaviors.isEmpty() || 
```

### UnusedAssignment
Variable `currentValue` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlRadioRendererBase.java`
#### Snippet
```java
            {
                Converter converter = HtmlRendererUtils.findUIOutputConverterFailSafe(facesContext, selectOne);
                Object currentValue = null;

                // If the current selectOne radio contains a value expression, 
```

### UnusedAssignment
Variable `behaviors` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlRadioRendererBase.java`
#### Snippet
```java
        }
        
        Map<String, List<ClientBehavior>> behaviors = null;
        if (uiComponent instanceof ClientBehaviorHolder)
        {
```

### UnusedAssignment
Variable `behaviors` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTableRendererBase.java`
#### Snippet
```java
        writer.startElement(HTML.TABLE_ELEM, uiComponent);
        
        Map<String, List<ClientBehavior>> behaviors = null;
        if (uiComponent instanceof ClientBehaviorHolder)
        {
```

### UnusedAssignment
Variable `bodyrows` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTableRendererBase.java`
#### Snippet
```java
    private Integer[] getBodyRows(FacesContext facesContext, UIComponent component)
    {
        Integer[] bodyrows = null;
        String bodyrowsAttr = (String) component.getAttributes().get(ComponentAttrs.BODYROWS_ATTR);
        if (bodyrowsAttr != null && !bodyrowsAttr.isEmpty())
```

### UnusedAssignment
Variable `navigationCase` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/OutcomeTargetUtils.java`
#### Snippet
```java
        
        // fromAction is null because there is no action method that was called to get the outcome
        NavigationCase navigationCase = null;
        if (toFlowDocumentId == null)
        {
```

### UnusedAssignment
The value `processGlobalListeners(facesContext, eventInfos, systemEventClass, source, event, sourceBaseType)` assigned to `event` is never used
in `impl/src/main/java/org/apache/myfaces/application/ApplicationImplEventManager.java`
#### Snippet
```java
            // global listeners
            List<EventInfo> eventInfos = globalListeners.get(systemEventClass);
            event = processGlobalListeners(facesContext, eventInfos, systemEventClass, source, event, sourceBaseType);
        }
        catch (AbortProcessingException e)
```

### UnusedAssignment
Variable `viewId` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/application/ViewIdSupport.java`
#### Snippet
```java
        boolean traceEnabled = log.isLoggable(Level.FINEST);
        
        String viewId = null;
        if (ExternalContextUtils.isPortlet(externalContext))
        {
```

### UnusedAssignment
Variable `next` initializer `false` is redundant
in `impl/src/main/java/org/apache/myfaces/application/ViewResourceIterator.java`
#### Snippet
```java
    public boolean hasNext()
    {
        boolean next = false;
        do 
        {
```

### UnusedAssignment
Variable `loader` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/application/ViewResourceIterator.java`
#### Snippet
```java
            if (currentIterator == null)
            {
                ResourceLoader loader = null;
                String basePath = null;
                do
```

### UnusedAssignment
Variable `basePath` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/application/ViewResourceIterator.java`
#### Snippet
```java
            {
                ResourceLoader loader = null;
                String basePath = null;
                do
                {
```

### UnusedAssignment
Variable `_wildcardKeys` initializer `new ArrayList<_WildcardPattern>()` is redundant
in `impl/src/main/java/org/apache/myfaces/application/_FlowNavigationStructure.java`
#### Snippet
```java
    
    private Map<String, Set<NavigationCase>> _navigationCases = null;
    private List<_WildcardPattern> _wildcardKeys = new ArrayList<_WildcardPattern>();

    public _FlowNavigationStructure(String definingDocumentId, String id, 
```

### UnusedAssignment
Variable `_navigationCases` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/application/_FlowNavigationStructure.java`
#### Snippet
```java
    private String _id;
    
    private Map<String, Set<NavigationCase>> _navigationCases = null;
    private List<_WildcardPattern> _wildcardKeys = new ArrayList<_WildcardPattern>();

```

### UnusedAssignment
Variable `m` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/ErrorPageWriter.java`
#### Snippet
```java
            BeanInfo info = Introspector.getBeanInfo(c.getClass());
            PropertyDescriptor[] pd = info.getPropertyDescriptors();
            Method m = null;
            Object v = null;
            ValueExpression valueExpression = null;
```

### UnusedAssignment
Variable `v` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/ErrorPageWriter.java`
#### Snippet
```java
            PropertyDescriptor[] pd = info.getPropertyDescriptors();
            Method m = null;
            Object v = null;
            ValueExpression valueExpression = null;
            String str = null;
```

### UnusedAssignment
Variable `valueExpression` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/ErrorPageWriter.java`
#### Snippet
```java
            Method m = null;
            Object v = null;
            ValueExpression valueExpression = null;
            String str = null;
            for (int i = 0; i < pd.length; i++)
```

### UnusedAssignment
Variable `str` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/ErrorPageWriter.java`
#### Snippet
```java
            Object v = null;
            ValueExpression valueExpression = null;
            String str = null;
            for (int i = 0; i < pd.length; i++)
            {
```

### UnusedAssignment
Variable `state` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/application/viewstate/StateCacheClientSide.java`
#### Snippet
```java
    public Object encodeSerializedState(FacesContext facesContext, Object serializedView)
    {
        Object[] state = null;
        
        if (clientViewStateTimeout > 0L)
```

### UnusedAssignment
Variable `resourceVersion` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
    {
        ResourceMeta resourceMeta = null;
        String resourceVersion = null;

        //1. Try to locate resource in a localized path
```

### UnusedAssignment
Variable `resourceMeta` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
            String resourceId, String contractName)
    {
        ResourceMeta resourceMeta = null;
        String token = null;
        String localePrefix = null;
```

### UnusedAssignment
Variable `token` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
    {
        ResourceMeta resourceMeta = null;
        String token = null;
        String localePrefix = null;
        String libraryName = null;
```

### UnusedAssignment
Variable `resourceName` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
        String libraryName = null;
        String libraryVersion = null;
        String resourceName = null;
        String resourceVersion = null;

```

### UnusedAssignment
Variable `resourceName` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
        }

        String resourceName = null;
        if (resourceBasePath.startsWith(ResourceHandler.RESOURCE_IDENTIFIER))
        {
```

### UnusedAssignment
Variable `resource` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
        }

        Resource resource = null;
        if (libraryName != null)
        {
```

### UnusedAssignment
Variable `libraryFound` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java

        String pathToLib = null;
        Boolean libraryFound = null;
        if (libraryName != null && !ResourceValidationUtils.isValidLibraryName(
                libraryName, isAllowSlashesLibraryName()))
```

### UnusedAssignment
Variable `resourceVersion` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
    {
        ResourceMeta resourceMeta = null;
        String resourceVersion = null;

        //1. Try to locate resource in a localized path
```

### UnusedAssignment
Variable `resourceVersion` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
            String resourceName, String libraryName, String localePrefix)
    {
        String resourceVersion = null;
        String libraryVersion = null;
        ResourceMeta resourceId = null;
```

### UnusedAssignment
Variable `libraryVersion` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
    {
        String resourceVersion = null;
        String libraryVersion = null;
        ResourceMeta resourceId = null;
        
```

### UnusedAssignment
Variable `resourceVersion` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
            String resourceName, String libraryName, String localePrefix, String contractName)
    {
        String resourceVersion = null;
        String libraryVersion = null;
        ResourceMeta resourceId = null;
```

### UnusedAssignment
Variable `libraryVersion` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
    {
        String resourceVersion = null;
        String libraryVersion = null;
        ResourceMeta resourceId = null;
        
```

### UnusedAssignment
Variable `resourceMeta` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
            String resourceId)
    {
        ResourceMeta resourceMeta = null;
        String token = null;
        String localePrefix = null;
```

### UnusedAssignment
Variable `token` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
    {
        ResourceMeta resourceMeta = null;
        String token = null;
        String localePrefix = null;
        String libraryName = null;
```

### UnusedAssignment
Variable `resourceName` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
        String libraryName = null;
        String libraryVersion = null;
        String resourceName = null;
        String resourceVersion = null;

```

### UnusedAssignment
Variable `locale` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
        if (bundleName != null)
        {
            Locale locale = null;
            
            if (isResourceRequest || context.getViewRoot() == null)
```

### UnusedAssignment
Variable `bytes` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/application/viewstate/StateUtils.java`
#### Snippet
```java
    private static byte[] findMacSecret(String secret, String algorithm)
    {
        byte[] bytes = null;
        
        if (secret == null)
```

### UnusedAssignment
Variable `bytes` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/application/viewstate/StateUtils.java`
#### Snippet
```java
    private static byte[] findSecret(String secret, String algorithm)
    {
        byte[] bytes = null;
        
        if (secret == null)
```

### UnusedAssignment
Variable `set` initializer `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/el/FacesScopedAttributeELResolver.java`
#### Snippet
```java
        List descriptors = new ArrayList();
        Map map = null;
        Set set = null;
        Iterator items = null;
        String key = null;
```

### UnusedAssignment
Variable `items` initializer `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/el/FacesScopedAttributeELResolver.java`
#### Snippet
```java
        Map map = null;
        Set set = null;
        Iterator items = null;
        String key = null;
        Object value = null;
```

### UnusedAssignment
Variable `key` initializer `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/el/FacesScopedAttributeELResolver.java`
#### Snippet
```java
        Set set = null;
        Iterator items = null;
        String key = null;
        Object value = null;

```

### UnusedAssignment
Variable `value` initializer `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/el/FacesScopedAttributeELResolver.java`
#### Snippet
```java
        Iterator items = null;
        String key = null;
        Object value = null;

        // Add feature descriptors for request scoped attributes
```

### UnusedAssignment
Variable `value` initializer `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/el/FacesScopedAttributeELResolver.java`
#### Snippet
```java
                .getContext(FacesContext.class);
        ExternalContext econtext = fcontext.getExternalContext();
        Object value = null;
        value = econtext.getRequestMap().get(property);
        if (value != null)
```

### UnusedAssignment
Variable `object` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/application/viewstate/StateCacheServerSide.java`
#### Snippet
```java
                    final ObjectInputStream in = new MyFacesObjectInputStream(is);
                    ois = in;
                    Object object = null;
                    if (System.getSecurityManager() != null) 
                    {
```

### UnusedAssignment
The value `null` assigned to `ois` is never used
in `impl/src/main/java/org/apache/myfaces/application/viewstate/StateCacheServerSide.java`
#### Snippet
```java
                    {
                        ois.close();
                        ois = null;
                    }
                }
```

### UnusedAssignment
Variable `casesSet` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/application/NavigationHandlerImpl.java`
#### Snippet
```java
            _FlowNavigationStructure flowNavigationStructure, String fromAction, String outcome, String viewId)
    {
        Set<NavigationCase> casesSet = null;
        NavigationCase navigationCase = null;
        
```

### UnusedAssignment
Variable `value` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/application/NavigationHandlerImpl.java`
#### Snippet
```java
    {
        MethodExpression method = methodCallNode.getMethodExpression();
        Object value = null;
        
        if (methodCallNode.getParameters() != null &&
```

### UnusedAssignment
The value `targetFlow` assigned to `sourceFlow` is never used
in `impl/src/main/java/org/apache/myfaces/application/NavigationHandlerImpl.java`
#### Snippet
```java
                    navigationContext.getFlowCallNodes().get(i), 
                    navigationContext.getNavigationCase().getToViewId(facesContext));
                sourceFlow = targetFlow;
            }
        }
```

### UnusedAssignment
Variable `navigationCase` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/application/NavigationHandlerImpl.java`
#### Snippet
```java
    {
        NavigationContext navigationContext = new NavigationContext();
        NavigationCase navigationCase = null;
        try
        {
```

### UnusedAssignment
Variable `implicitViewId` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/application/NavigationHandlerImpl.java`
#### Snippet
```java
    private NavigationCase getOutcomeNavigationCase(FacesContext facesContext, String fromAction, String outcome)
    {
        String implicitViewId = null;
        boolean includeViewParams = false;
        int index;
```

### UnusedAssignment
Variable `viewIdToTestString` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/application/NavigationHandlerImpl.java`
#### Snippet
```java
        
        // Apply normalization 
        String viewIdToTestString = null;
        boolean applyNormalization = false;
        for (int i = 0; i < viewIdToTest.length() - 1; i++)
```

### UnusedAssignment
The value `currentFlow.getNode(outcomeToGo)` assigned to `flowNode` is never used
in `impl/src/main/java/org/apache/myfaces/application/NavigationHandlerImpl.java`
#### Snippet
```java
                                //fromAction = currentFlow.getId();
                                actionToGo = currentFlow.getId();
                                flowNode = currentFlow.getNode(outcomeToGo);
                                continue;
                            }
```

### UnusedAssignment
Variable `instance` initializer `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/el/MockVariableValueExpression.java`
#### Snippet
```java
     * <p>The variable instance being wrapped by this expression.</p>
     */
    private Object instance = null;

    // ------------------------------------------------------ Expression Methods
```

### UnusedAssignment
Variable `expectedType` initializer `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/el/MockVariableValueExpression.java`
#### Snippet
```java
     * <p>The expected result type for <code>getValue()</code> calls.</p>
     */
    private Class expectedType = null;

    /**
```

### UnusedAssignment
Variable `key` initializer `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/el/FacesResourceBundleELResolver.java`
#### Snippet
```java
                .getContext(FacesContext.class);
        MockApplication application = (MockApplication) fcontext.getApplication();
        String key = null;
        Object value = null;

```

### UnusedAssignment
The value `entry.getValue()` assigned to `value` is never used
in `test/src/main/java/org/apache/myfaces/test/el/FacesResourceBundleELResolver.java`
#### Snippet
```java
            Entry entry = (Entry) entries.next();
            key = (String) entry.getKey();
            value = entry.getValue();
            descriptors.add(descriptor(key, key, "Resource Bundle " + key,
                    false, false, true, ResourceBundle.class, true));
```

### UnusedAssignment
Variable `expectedType` initializer `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/el/MockValueExpression.java`
#### Snippet
```java
     * <p>The expected result type for <code>getValue()</code> calls.</p>
     */
    private Class expectedType = null;

    /**
```

### UnusedAssignment
Variable `expression` initializer `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/el/MockValueExpression.java`
#### Snippet
```java
     * <p>The original expression string used to create this expression.</p>
     */
    private String expression = null;

    // ------------------------------------------------------ Expression Methods
```

### UnusedAssignment
Variable `expression` initializer `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/el/MockMethodExpression.java`
#### Snippet
```java
     * <p>The original expression string used to create this expression.</p>
     */
    private String expression = null;

    /**
```

### UnusedAssignment
Variable `signature` initializer `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/el/MockMethodExpression.java`
#### Snippet
```java
     * <p>The method signature of the method to be called.</p>
     */
    private Class[] signature = null;

    // ------------------------------------------------------ Expression Methods
```

### UnusedAssignment
Variable `expectedType` initializer `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/el/MockMethodExpression.java`
#### Snippet
```java
     * <p>The expected result type for <code>getValue()</code> calls.</p>
     */
    private Class expectedType = null;

    /**
```

### UnusedAssignment
Variable `converter` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/application/ApplicationImpl.java`
#### Snippet
```java
                }
                
                Converter<?> converter = null;
                
                if (Boolean.TRUE.equals(_cdiManagedConverterMap.get(converterClass)))
```

### UnusedAssignment
Variable `behavior` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/application/ApplicationImpl.java`
#### Snippet
```java
            boolean managed = _cdiManagedBehaviorMap.get(behaviorClass);

            Behavior behavior = null;
            if (managed)
            {
```

### UnusedAssignment
Variable `stageName` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/application/ApplicationImpl.java`
#### Snippet
```java
        if (_projectStage == null)
        {
            String stageName = null;
            
            // try to obtain the ProjectStage from the system property
```

### UnusedAssignment
Variable `converter` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/application/ApplicationImpl.java`
#### Snippet
```java
            boolean managed = _cdiManagedConverterMap.get(converterClass);

            Converter converter = null;
            if (managed)
            {
```

### UnusedAssignment
Variable `validator` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/application/ApplicationImpl.java`
#### Snippet
```java
            boolean managed = _cdiManagedValidatorMap.get(validatorClass);

            Validator validator = null;
            if (managed)
            {
```

### UnusedAssignment
Variable `name` initializer `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockPrincipal.java`
#### Snippet
```java
     * <p>The name for this Principal intance.</p>
     */
    private String name = null;

    // ----------------------------------------------------- Mock Object Methods
```

### UnusedAssignment
Variable `newMethod` initializer `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/base/junit/runners/TestPerClassLoaderRunner.java`
#### Snippet
```java
    protected Statement methodBlock(FrameworkMethod method)
    {
        FrameworkMethod newMethod = null;
        try
        {
```

### UnusedAssignment
Variable `zipFile` initializer `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/base/junit/runners/TestPerClassLoaderRunner.java`
#### Snippet
```java
            return null;
        }
        ZipFile zipFile = null;

        try
```

### UnusedAssignment
Variable `data` initializer `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/base/junit/runners/TestClassLoader.java`
#### Snippet
```java
            throws ClassNotFoundException
    {
        byte[] data = null;
        for (int i = 0; i < fPathItems.size(); i++)
        {
```

### UnusedAssignment
Variable `zipFile` initializer `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/base/junit/runners/TestClassLoader.java`
#### Snippet
```java
    private byte[] loadJarData(String path, String fileName)
    {
        ZipFile zipFile = null;
        InputStream stream = null;
        File archive = new File(path);
```

### UnusedAssignment
Variable `baos` initializer `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockServletOutputStream.java`
#### Snippet
```java
     * <p>The internal buffer we use to capture output.</p>
     */
    private ByteArrayOutputStream baos = null;

    // --------------------------------------------- ServletOutputStream Methods
```

### UnusedAssignment
Variable `renderKits` initializer `new HashMap()` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockRenderKitFactory.java`
#### Snippet
```java
     * <p>The set of render kits that have been registered here.</p>
     */
    private Map renderKits = new HashMap();

    // ------------------------------------------------ RenderKitFactory Methods
```

### UnusedAssignment
Variable `queryString` initializer `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockExternalContext20.java`
#### Snippet
```java
    {
        String fragment = null;
        String queryString = null;
        Map<String, List<String>> paramMap = new HashMap<String, List<String>>();

```

### UnusedAssignment
Variable `mecConstructor` initializer `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockFacesContextFactory.java`
#### Snippet
```java

        // Select the constructor we wish to call
        Constructor mecConstructor = null;
        try
        {
```

### UnusedAssignment
Variable `externalContext` initializer `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockFacesContextFactory.java`
#### Snippet
```java

        // Construct an appropriate MockExternalContext instance
        MockExternalContext externalContext = null;
        try
        {
```

### UnusedAssignment
Variable `component` initializer `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication12.java`
#### Snippet
```java
    {

        UIComponent component = null;
        try
        {
```

### UnusedAssignment
Variable `expressionFactory` initializer `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication12.java`
#### Snippet
```java
     * <p>Expression factory for this instance.</p>
     */
    private ExpressionFactory expressionFactory = null;

    /**
```

### UnusedAssignment
Variable `map` initializer `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockFlash.java`
#### Snippet
```java
        Object session = external.getSession(true);

        Map<String, Object> map = null;

        // Synchronize on the session object to ensure that
```

### UnusedAssignment
Variable `_facesContext` initializer `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockPartialViewContext.java`
#### Snippet
```java
    private static final String PARTIAL_PROCESS = "partial/process";
    private static final String SOURCE_PARAM_NAME = "jakarta.faces.source";
    private FacesContext _facesContext = null;
    private Boolean _ajaxRequest = null;
    private Collection<String> _executeClientIds = null;
```

### UnusedAssignment
Variable `caw` initializer `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockPrintWriter.java`
#### Snippet
```java
     * <p>The writer we will use for buffering.</p>
     */
    private CharArrayWriter caw = null;

    // ----------------------------------------------------- PrintWriter Methods
```

### UnusedAssignment
Variable `writer` initializer `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockResponseWriter.java`
#### Snippet
```java
    private boolean open = false; // Is an element currently open?
    private UIComponent component;
    private Writer writer = null;

    // ----------------------------------------------------- Mock Object Methods
```

### UnusedAssignment
Variable `characterEncoding` initializer `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockResponseWriter.java`
#### Snippet
```java
    // ------------------------------------------------------ Instance Variables

    private String characterEncoding = null;
    private String contentType = "text/html";
    private boolean open = false; // Is an element currently open?
```

### UnusedAssignment
Variable `contentType` initializer `"text/html"` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockResponseWriter.java`
#### Snippet
```java

    private String characterEncoding = null;
    private String contentType = "text/html";
    private boolean open = false; // Is an element currently open?
    private UIComponent component;
```

### UnusedAssignment
Variable `object` initializer `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockResponseStateManager.java`
#### Snippet
```java
        ByteArrayInputStream input = null;
        ObjectInputStream s = null;
        Object object = null;

        try
```

### UnusedAssignment
The value `null` assigned to `s` is never used
in `test/src/main/java/org/apache/myfaces/test/mock/MockResponseStateManager.java`
#### Snippet
```java
                finally
                {
                    s = null;
                }
            }
```

### UnusedAssignment
The value `null` assigned to `input` is never used
in `test/src/main/java/org/apache/myfaces/test/mock/MockResponseStateManager.java`
#### Snippet
```java
                finally
                {
                    input = null;
                }
            }
```

### UnusedAssignment
Variable `treeStruct` initializer `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockResponseStateManager.java`
#### Snippet
```java
        }

        Object treeStruct = null;
        Object compStates = null;

```

### UnusedAssignment
Variable `compStates` initializer `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockResponseStateManager.java`
#### Snippet
```java

        Object treeStruct = null;
        Object compStates = null;

        if (state instanceof Object[])
```

### UnusedAssignment
Variable `bytes` initializer `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockResponseStateManager.java`
#### Snippet
```java
            throws IOException
    {
        byte[] bytes = null;
        ByteArrayOutputStream baos = null;
        ObjectOutputStream oos = null;
```

### UnusedAssignment
The value `null` assigned to `oos` is never used
in `test/src/main/java/org/apache/myfaces/test/mock/MockResponseStateManager.java`
#### Snippet
```java
                finally
                {
                    oos = null;
                }
            }
```

### UnusedAssignment
The value `null` assigned to `baos` is never used
in `test/src/main/java/org/apache/myfaces/test/mock/MockResponseStateManager.java`
#### Snippet
```java
                finally
                {
                    baos = null;
                }
            }
```

### UnusedAssignment
Variable `converters1` initializer `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication10.java`
#### Snippet
```java
    private Map components = null;
    private Map converters = null; // By id
    private Map converters1 = null; // By type
    private Locale defaultLocale = null;
    private String defaultRenderKitId = null;
```

### UnusedAssignment
Variable `validators` initializer `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication10.java`
#### Snippet
```java
    private StateManager stateManager = null;
    private Collection supportedLocales = null;
    private Map validators = null;
    private ViewHandler viewHandler = null;

```

### UnusedAssignment
Variable `components` initializer `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication10.java`
#### Snippet
```java

    private ActionListener actionListener = null;
    private Map components = null;
    private Map converters = null; // By id
    private Map converters1 = null; // By type
```

### UnusedAssignment
Variable `converters` initializer `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication10.java`
#### Snippet
```java
    private ActionListener actionListener = null;
    private Map components = null;
    private Map converters = null; // By id
    private Map converters1 = null; // By type
    private Locale defaultLocale = null;
```

### UnusedAssignment
The value `true` assigned to `firstSlashAdded` is never used
in `test/src/main/java/org/apache/myfaces/test/mock/resource/MockResourceMeta.java`
#### Snippet
```java
            builder.append(_resourceName.substring(_resourceName
                    .lastIndexOf('.')));
            firstSlashAdded = true;
        }

```

### UnusedAssignment
Variable `c` initializer `0` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/_ELText.java`
#### Snippet
```java
        int i = s;
        int len = ca.length;
        char c = 0;
        int str = 0;
        while (i < len)
```

### UnusedAssignment
Variable `c` initializer `0` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/_ELText.java`
#### Snippet
```java
        char[] ca = in.toCharArray();
        int i = 0;
        char c = 0;
        int len = ca.length;
        int end = len - 1;
```

### UnusedAssignment
Variable `vlen` initializer `0` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/_ELText.java`
#### Snippet
```java
        int end = len - 1;
        boolean esc = false;
        int vlen = 0;

        StringBuffer buff = new StringBuffer(128);
```

### UnusedAssignment
Variable `t` initializer `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/_ELText.java`
#### Snippet
```java
        StringBuffer buff = new StringBuffer(128);
        List<_ELText> text = new ArrayList<_ELText>();
        _ELText t = null;
        ValueExpression ve = null;

```

### UnusedAssignment
Variable `ve` initializer `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/_ELText.java`
#### Snippet
```java
        List<_ELText> text = new ArrayList<_ELText>();
        _ELText t = null;
        ValueExpression ve = null;

        while (i < len)
```

### UnusedAssignment
Variable `lifecycles` initializer `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/lifecycle/MockLifecycleFactory.java`
#### Snippet
```java
     * <p>The set of Lifecycle instances registered with us.</p>
     */
    private Map lifecycles = null;

    // ------------------------------------------------ LifecycleFactory Methods
```

### UnusedAssignment
Variable `resourceVersion` initializer `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/resource/MockResourceHandler.java`
#### Snippet
```java
    {
        String localePrefix = getLocalePrefixForLocateResource();
        String resourceVersion = null;
        String libraryVersion = null;
        MockResourceMeta resourceId = null;
```

### UnusedAssignment
Variable `libraryVersion` initializer `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/resource/MockResourceHandler.java`
#### Snippet
```java
        String localePrefix = getLocalePrefixForLocateResource();
        String resourceVersion = null;
        String libraryVersion = null;
        MockResourceMeta resourceId = null;

```

### UnusedAssignment
Variable `labelClass` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/HtmlRendererUtils.java`
#### Snippet
```java
        writer.startElement(HTML.LABEL_ELEM, null); // component);
        writer.writeAttribute(HTML.FOR_ATTR, forClientId, null);
        String labelClass = null;
        if (disabled)
        {
```

### UnusedAssignment
Variable `labelSelectedClass` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/HtmlRendererUtils.java`
#### Snippet
```java
                    .get(ComponentAttrs.ENABLED_CLASS_ATTR);
        }
        String labelSelectedClass = null;
        if (selected)
        {
```

### UnusedAssignment
Variable `labelClass` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/HtmlRendererUtils.java`
#### Snippet
```java
        writer.startElement(HTML.LABEL_ELEM, null); // component);
        writer.writeAttribute(HTML.FOR_ATTR, forClientId, null);
        String labelClass = null;
        if (disabled)
        {
```

### UnusedAssignment
Variable `labelClass` initializer `null` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/HtmlRendererUtils.java`
#### Snippet
```java
                }

                String labelClass = null;

                if (componentDisabled || disabled)
```

## RuleId[ruleID=IndexOfReplaceableByContains]
### IndexOfReplaceableByContains
`-1 != name.toUpperCase().indexOf(searchString.toUpperCase())` can be replaced with 'name.toUpperCase().contains(searchString.toUpperCase())'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/fn/JstlFunction.java`
#### Snippet
```java
        }
        
        return -1 != name.toUpperCase().indexOf(searchString.toUpperCase());
    }

```

### IndexOfReplaceableByContains
`-1 != name.indexOf(searchString)` can be replaced with 'name.contains(searchString)'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/fn/JstlFunction.java`
#### Snippet
```java
        }

        return -1 != name.indexOf(searchString);
    }

```

### IndexOfReplaceableByContains
`userAgent.indexOf("bot") >= 0` can be replaced with 'userAgent.contains("bot")'
in `impl/src/main/java/org/apache/myfaces/lifecycle/clientwindow/ClientConfig.java`
#### Snippet
```java

        if (userAgent != null &&
            ( userAgent.indexOf("bot")     >= 0 || // Googlebot, etc
              userAgent.indexOf("Bot")     >= 0 || // BingBot, etc
              userAgent.indexOf("Slurp")   >= 0 || // Yahoo Slurp
```

### IndexOfReplaceableByContains
`userAgent.indexOf("Bot") >= 0` can be replaced with 'userAgent.contains("Bot")'
in `impl/src/main/java/org/apache/myfaces/lifecycle/clientwindow/ClientConfig.java`
#### Snippet
```java
        if (userAgent != null &&
            ( userAgent.indexOf("bot")     >= 0 || // Googlebot, etc
              userAgent.indexOf("Bot")     >= 0 || // BingBot, etc
              userAgent.indexOf("Slurp")   >= 0 || // Yahoo Slurp
              userAgent.indexOf("Crawler") >= 0    // various other Crawlers
```

### IndexOfReplaceableByContains
`userAgent.indexOf("Slurp") >= 0` can be replaced with 'userAgent.contains("Slurp")'
in `impl/src/main/java/org/apache/myfaces/lifecycle/clientwindow/ClientConfig.java`
#### Snippet
```java
            ( userAgent.indexOf("bot")     >= 0 || // Googlebot, etc
              userAgent.indexOf("Bot")     >= 0 || // BingBot, etc
              userAgent.indexOf("Slurp")   >= 0 || // Yahoo Slurp
              userAgent.indexOf("Crawler") >= 0    // various other Crawlers
            ) )
```

### IndexOfReplaceableByContains
`userAgent.indexOf("Crawler") >= 0` can be replaced with 'userAgent.contains("Crawler")'
in `impl/src/main/java/org/apache/myfaces/lifecycle/clientwindow/ClientConfig.java`
#### Snippet
```java
              userAgent.indexOf("Bot")     >= 0 || // BingBot, etc
              userAgent.indexOf("Slurp")   >= 0 || // Yahoo Slurp
              userAgent.indexOf("Crawler") >= 0    // various other Crawlers
            ) )
        {
```

### IndexOfReplaceableByContains
`expression.indexOf("${") < 0` can be replaced with '!expression.contains("${")'
in `test/src/main/java/org/apache/myfaces/test/el/MockValueExpression.java`
#### Snippet
```java
    {

        return (expression.indexOf("${") < 0) && (expression.indexOf("#{") < 0);

    }
```

### IndexOfReplaceableByContains
`expression.indexOf("#{") < 0` can be replaced with '!expression.contains("#{")'
in `test/src/main/java/org/apache/myfaces/test/el/MockValueExpression.java`
#### Snippet
```java
    {

        return (expression.indexOf("${") < 0) && (expression.indexOf("#{") < 0);

    }
```

### IndexOfReplaceableByContains
`expression.indexOf("${") < 0` can be replaced with '!expression.contains("${")'
in `test/src/main/java/org/apache/myfaces/test/el/MockMethodExpression.java`
#### Snippet
```java
    {

        return (expression.indexOf("${") < 0) && (expression.indexOf("#{") < 0);

    }
```

### IndexOfReplaceableByContains
`expression.indexOf("#{") < 0` can be replaced with '!expression.contains("#{")'
in `test/src/main/java/org/apache/myfaces/test/el/MockMethodExpression.java`
#### Snippet
```java
    {

        return (expression.indexOf("${") < 0) && (expression.indexOf("#{") < 0);

    }
```

## RuleId[ruleID=DoubleNegation]
### DoubleNegation
Double negation in `!((_capabilities & FLAG_IS_RENDERER_TYPE_SET) != 0)`
in `api/src/main/java/jakarta/faces/component/UIComponentBase.java`
#### Snippet
```java
            if (facesListenersSaved == null && stateHelperSaved == null && 
                behaviorsMapSaved == null && systemEventListenerClassMapSaved == null &&
               !((_capabilities & FLAG_IS_RENDERER_TYPE_SET) != 0))
            {
                return null;
```

## RuleId[ruleID=ManualArrayToCollectionCopy]
### ManualArrayToCollectionCopy
Manual array to collection copy
in `impl/src/main/java/org/apache/myfaces/config/annotation/DefaultAnnotationProvider.java`
#### Snippet
```java
            {
                Class[] classes = PackageInfo.getClasses(scanPackageToken);
                for (Class c : classes)
                {
                    list.add(c);                    
```

## RuleId[ruleID=UnnecessarySemicolon]
### UnnecessarySemicolon
Unnecessary semicolon `;`
in `api/src/main/java/jakarta/faces/component/_UIWebsocket.java`
#### Snippet
```java
    enum PropertyKeys 
    {
        channel, scope, user, onopen, onmessage, onclose, connected;
    }
}
```

### UnnecessarySemicolon
Unnecessary semicolon `;`
in `api/src/main/java/jakarta/faces/component/behavior/ClientBehaviorHint.java`
#### Snippet
```java
public enum ClientBehaviorHint
{
    SUBMITTING;
}

```

## RuleId[ruleID=StringOperationCanBeSimplified]
### StringOperationCanBeSimplified
Call to `intern()` on compile-time constant is unnecessary
in `api/src/main/java/jakarta/faces/view/facelets/FaceletContext.java`
#### Snippet
```java
public abstract class FaceletContext extends ELContext
{
    public static final String FACELET_CONTEXT_KEY = "jakarta.faces.FACELET_CONTEXT".intern();
    
    /**
```

### StringOperationCanBeSimplified
`new String()` is redundant
in `impl/src/main/java/org/apache/myfaces/util/lang/StringCharArrayAccessor.java`
#### Snippet
```java
        }

        String str = new String();
        try
        {
```

### StringOperationCanBeSimplified
Unnecessary string length argument
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ComponentSupport.java`
#### Snippet
```java
            if (s.length() >= 7)
            {
                return new Locale(s.substring(0, 2), s.substring(3, 5).toUpperCase(), s.substring(6, s.length()));
            }

```

### StringOperationCanBeSimplified
Inefficient conversion from ByteArrayOutputStream
in `impl/src/main/java/org/apache/myfaces/config/DefaultFacesConfigurationProvider.java`
#### Snippet
```java
                    out.flush();

                    String content = new String(out.toByteArray());
                    if (content.trim().isEmpty())
                    {
```

### StringOperationCanBeSimplified
`toLowerCase()` call can be replaced with 'equalsIgnoreCase()'
in `impl/src/main/java/org/apache/myfaces/config/FacesConfigurator.java`
#### Snippet
```java
                BeanValidator.DISABLE_DEFAULT_BEAN_VALIDATOR_PARAM_NAME);
        final boolean defaultBeanValidatorDisabled = (beanValidatorDisabled != null
                && beanValidatorDisabled.toLowerCase().equals("true"));
        boolean beanValidatorInstalledProgrammatically = false;
        if (!defaultBeanValidatorDisabled
```

### StringOperationCanBeSimplified
Unnecessary string length argument
in `impl/src/main/java/org/apache/myfaces/resource/ClassLoaderResourceLoaderIterator.java`
#### Snippet
```java
                                }
                                
                                path = entryName.substring(basePath.length(), entryName.length());
    
                                if (path.endsWith("/"))
```

### StringOperationCanBeSimplified
Call to `substring()` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/UnicodeEncoder.java`
#### Snippet
```java
                {
                    sb = new StringBuilder(string.length() + 4);
                    sb.append(string.substring(0,i));
                }
                //encode all non basic latin characters
```

### StringOperationCanBeSimplified
Call to `substring()` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlLinkRendererBase.java`
#### Snippet
```java
                        {
                            buff = new StringBuilder();
                            buff.append(strParamValue.substring(0,i));
                        }
                        buff.append('\\');
```

### StringOperationCanBeSimplified
Unnecessary string length argument
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlLinkRendererBase.java`
#### Snippet
```java
            {
                // remove anchor element and add it again after the parameter are encoded
                anchorString = href.substring(index,href.length());
                href = href.substring(0,index);
            }
```

### StringOperationCanBeSimplified
Call to `substring()` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/JavascriptUtils.java`
#### Snippet
```java
                {
                    buf = new StringBuilder(s.length() + 10);
                    buf.append(s.substring(0, i));
                }

```

### StringOperationCanBeSimplified
Call to `substring()` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlButtonRendererBase.java`
#### Snippet
```java
                        {
                            buff = new StringBuilder();
                            buff.append(strParamValue.substring(0,j));
                        }
                        buff.append('\\');
```

### StringOperationCanBeSimplified
Call to `substring()` is redundant
in `impl/src/main/java/org/apache/myfaces/application/ViewIdSupport.java`
#### Snippet
```java
                    if (viewId.endsWith(contextSuffix))
                    {
                        builder.append(viewId.substring(0, viewId.indexOf(contextSuffix)));
                        builder.append(mapping.getExtension());
                        founded = true;
```

### StringOperationCanBeSimplified
Call to `substring()` is redundant
in `impl/src/main/java/org/apache/myfaces/application/ViewIdSupport.java`
#### Snippet
```java
                    else if(viewId.lastIndexOf('.') != -1 )
                    {
                        builder.append(viewId.substring(0, viewId.lastIndexOf('.')));
                        builder.append(config.getViewSuffix()[0]);
                    }
```

### StringOperationCanBeSimplified
Call to `substring()` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/ErrorPageWriter.java`
#### Snippet
```java
                {
                    int endIndex = head.indexOf("</style>", startIndex) + "</style>".length();
                    builder.append(head.substring(head.indexOf("<style", startIndex), endIndex));
                    startIndex = endIndex;
                }
```

### StringOperationCanBeSimplified
Call to `substring()` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/ErrorPageWriter.java`
#### Snippet
```java
                {
                    int endIndex = head.indexOf("</script>", startIndex) + "</script>".length();
                    builder.append(head.substring(head.indexOf("<script", startIndex), endIndex));
                    startIndex = endIndex;
                }
```

### StringOperationCanBeSimplified
`toLowerCase()` call can be replaced with 'equalsIgnoreCase()'
in `impl/src/main/java/org/apache/myfaces/application/ApplicationImpl.java`
#### Snippet
```java
        String configParam = getFacesContext().getExternalContext().
                getInitParameter(Converter.DATETIMECONVERTER_DEFAULT_TIMEZONE_IS_SYSTEM_TIMEZONE_PARAM_NAME);
        if (configParam != null && configParam.toLowerCase().equals("true"))
        {
            _dateTimeConverterDefaultTimeZoneIsSystemTimeZone = true;
```

## RuleId[ruleID=TypeParameterHidesVisibleType]
### TypeParameterHidesVisibleType
Type parameter `String` hides visible type 'java.lang.String'
in `impl/src/main/java/org/apache/myfaces/cdi/view/ViewScopeCDIMap.java`
#### Snippet
```java
    }
    
    private class EntryWrapper<String, Object> implements Entry<String, Object>
    {
        private final Object key;
```

### TypeParameterHidesVisibleType
Type parameter `Object` hides visible type 'java.lang.Object'
in `impl/src/main/java/org/apache/myfaces/cdi/view/ViewScopeCDIMap.java`
#### Snippet
```java
    }
    
    private class EntryWrapper<String, Object> implements Entry<String, Object>
    {
        private final Object key;
```

### TypeParameterHidesVisibleType
Type parameter `V` hides type parameter 'V'
in `impl/src/main/java/org/apache/myfaces/util/lang/SubKeyMap.java`
#### Snippet
```java
    }

    private class Entry<V> implements Map.Entry<String, V>
    {
        public Entry(String baseKey)
```

### TypeParameterHidesVisibleType
Type parameter `V` hides type parameter 'V'
in `impl/src/main/java/org/apache/myfaces/util/lang/SubKeyMap.java`
#### Snippet
```java
    }

    private class EntryIterator<V> implements Iterator<Map.Entry<String, V>>
    {
        public EntryIterator(Iterator<String> iterator)
```

### TypeParameterHidesVisibleType
Type parameter `V` hides type parameter 'V'
in `impl/src/main/java/org/apache/myfaces/util/lang/SubKeyMap.java`
#### Snippet
```java
    // Set implementation for SubkeyMap.entrySet()
    //
    private class Entries<V> extends AbstractSet<Map.Entry<String, V>>
    {
        public Entries()
```

### TypeParameterHidesVisibleType
Type parameter `V` hides type parameter 'V'
in `test/src/main/java/org/apache/myfaces/test/mock/MockSubKeyMap.java`
#### Snippet
```java
    //

    private class Entries<V> extends AbstractSet<Map.Entry<String, V>>
    {
        public Entries()
```

### TypeParameterHidesVisibleType
Type parameter `V` hides type parameter 'V'
in `test/src/main/java/org/apache/myfaces/test/mock/MockSubKeyMap.java`
#### Snippet
```java
    }

    private class EntryIterator<V> implements Iterator<Map.Entry<String, V>>
    {
        public EntryIterator(Iterator<String> iterator)
```

### TypeParameterHidesVisibleType
Type parameter `V` hides type parameter 'V'
in `test/src/main/java/org/apache/myfaces/test/mock/MockSubKeyMap.java`
#### Snippet
```java
    }

    private class Entry<V> implements Map.Entry<String, V>
    {
        public Entry(String baseKey)
```

## RuleId[ruleID=UnnecessaryCallToStringValueOf]
### UnnecessaryCallToStringValueOf
Unnecessary `String.valueOf()` call
in `impl/src/main/java/org/apache/myfaces/util/lang/StreamCharBuffer.java`
#### Snippet
```java
    public String plus(Object value)
    {
        return toString() + String.valueOf(value);
    }

```

### UnnecessaryCallToStringValueOf
Unnecessary `Long.toString()` call
in `impl/src/main/java/org/apache/myfaces/view/facelets/ViewPoolProcessor.java`
#### Snippet
```java
                Logger.getLogger(ViewPoolProcessor.class.getName()).log(
                    Level.INFO, ViewHandler.FACELETS_REFRESH_PERIOD_PARAM_NAME +
                    " web config parameter is set to \"" + Long.toString(refreshPeriod) +
                    "\". To enable view pooling this param"+
                    " must be set to \"-1\". View Pooling disabled.");
```

## RuleId[ruleID=JavaReflectionMemberAccess]
### JavaReflectionMemberAccess
Cannot resolve field 'count'
in `impl/src/main/java/org/apache/myfaces/util/lang/StringCharArrayAccessor.java`
#### Snippet
```java
            try
            {
                countField = String.class.getDeclaredField("count");
                countField.setAccessible(true);

```

### JavaReflectionMemberAccess
Cannot resolve field 'offset'
in `impl/src/main/java/org/apache/myfaces/util/lang/StringCharArrayAccessor.java`
#### Snippet
```java
                countField.setAccessible(true);

                offsetField = String.class.getDeclaredField("offset");
                offsetField.setAccessible(true);
            }
```

### JavaReflectionMemberAccess
Cannot resolve constructor with specified argument types
in `test/src/main/java/org/apache/myfaces/test/mock/MockFacesContextFactory.java`
#### Snippet
```java
        try
        {
            mecConstructor = clazz.getConstructor(externalContextSignature);
        }
        catch (RuntimeException e)
```

### JavaReflectionMemberAccess
Cannot resolve constructor with specified argument types
in `test/src/main/java/org/apache/myfaces/test/mock/MockFacesContextFactory.java`
#### Snippet
```java
        {
            Class clazz = MockFacesContext.class;
            constructor = clazz.getConstructor(facesContextSignature);
        }
        catch (NoSuchMethodException | SecurityException ex)
```

### JavaReflectionMemberAccess
Cannot resolve method 'getExceptionHandler'
in `test/src/main/java/org/apache/myfaces/test/util/JsfVersion.java`
#### Snippet
```java
            try
            {
                Application.class.getMethod("getExceptionHandler");
                supports20 = true;

```

## RuleId[ruleID=NonSynchronizedMethodOverridesSynchronizedMethod]
### NonSynchronizedMethodOverridesSynchronizedMethod
Unsynchronized method `getCause()` overrides synchronized method
in `api/src/main/java/jakarta/faces/FacesException.java`
#### Snippet
```java

    @Override
    public Throwable getCause()
    {
        return super.getCause();
```

## RuleId[ruleID=CollectionContainsUrl]
### CollectionContainsUrl
Set `urls` may contain URL objects
in `impl/src/main/java/org/apache/myfaces/spi/impl/DefaultServiceProviderFinder.java`
#### Snippet
```java
        if (null != profiles && profiles.hasMoreElements())
        {
            Set<URL> urls = new LinkedHashSet<URL>();
            while (profiles.hasMoreElements())
            {
```

### CollectionContainsUrl
Set `urls` may contain URL objects
in `impl/src/main/java/org/apache/myfaces/spi/impl/DefaultServiceProviderFinder.java`
#### Snippet
```java
        }
        
        Set<URL> urls = getURLs(spiClass);
        if (!urls.isEmpty())
        {
```

### CollectionContainsUrl
Map `newLoc` may contain URL objects
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFaceletFactory.java`
#### Snippet
```java
                else
                {
                    Map<String, URL> newLoc = new HashMap<>(_relativeLocations);
                    newLoc.put(uri, url);
                    _relativeLocations = newLoc;
```

### CollectionContainsUrl
Map `newLoc` may contain URL objects
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFaceletFactory.java`
#### Snippet
```java
                else
                {
                    Map<String, URL> newLoc = new HashMap<>(_relativeLocations);
                    newLoc.put(uri, url);
                    _relativeLocations = newLoc;
```

### CollectionContainsUrl
Map `newLoc` may contain URL objects
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFaceletFactory.java`
#### Snippet
```java
                else
                {
                    Map<String, URL> newLoc = new HashMap<>(_relativeLocations);
                    newLoc.put(uri, url);
                    _relativeLocations = newLoc;
```

### CollectionContainsUrl
Map `_relativeLocations` may contain URL objects
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFaceletFactory.java`
#### Snippet
```java
    private Map<String, DefaultFacelet> _compositeComponentMetadataFacelets;
    private long _refreshPeriod;
    private Map<String, URL> _relativeLocations;
    private Map<String, Boolean> _managedFacelet;
    
```

### CollectionContainsUrl
Map `_relativePaths` may contain URL objects
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFacelet.java`
#### Snippet
```java
    private final long _createTime;
    private final long _refreshPeriod;
    private final Map<String, URL> _relativePaths;
    private final FaceletHandler _root;
    private final URL _src;
```

### CollectionContainsUrl
Set `all` may contain URL objects
in `impl/src/main/java/org/apache/myfaces/view/facelets/util/Classpath.java`
#### Snippet
```java
    public static URL[] search(ClassLoader loader, String prefix, String suffix) throws IOException
    {
        Set<URL> all = new LinkedHashSet<>();

        _searchResource(all, loader, prefix, prefix, suffix);
```

### CollectionContainsUrl
Set `result` may contain URL objects
in `impl/src/main/java/org/apache/myfaces/view/facelets/util/Classpath.java`
#### Snippet
```java
    }

    private static void _searchJar(ClassLoader loader, Set<URL> result, JarFile file, String prefix, String suffix)
            throws IOException
    {
```

### CollectionContainsUrl
Set `result` may contain URL objects
in `impl/src/main/java/org/apache/myfaces/view/facelets/util/Classpath.java`
#### Snippet
```java
     *             for any error
     */
    private static void _searchFromURL(Set<URL> result, String prefix, String suffix, URL url) throws IOException
    {
        boolean done = false;
```

### CollectionContainsUrl
Set `result` may contain URL objects
in `impl/src/main/java/org/apache/myfaces/view/facelets/util/Classpath.java`
#### Snippet
```java
    }

    private static boolean _searchDir(Set<URL> result, File dir, String suffix) throws IOException
    {
        boolean dirExists = false;
```

### CollectionContainsUrl
Set `result` may contain URL objects
in `impl/src/main/java/org/apache/myfaces/view/facelets/util/Classpath.java`
#### Snippet
```java
    }

    private static void _searchResource(Set<URL> result, ClassLoader loader, String resource, String prefix,
                                        String suffix) throws IOException
    {
```

### CollectionContainsUrl
Set `urls` may contain URL objects
in `impl/src/main/java/org/apache/myfaces/config/annotation/DefaultAnnotationProvider.java`
#### Snippet
```java
    }

    protected Collection<Class<?>> getAnnotatedMetaInfClasses(ExternalContext ctx, Set<URL> urls)
    {
        if (urls != null && !urls.isEmpty())
```

### CollectionContainsUrl
Set `urlSet` may contain URL objects
in `impl/src/main/java/org/apache/myfaces/config/annotation/DefaultAnnotationProvider.java`
#### Snippet
```java
        ClassLoader cl = ClassUtils.getCurrentLoader(this);

        Set<URL> urlSet = new HashSet<>();

        //This usually happens when maven-jetty-plugin is used
```

## RuleId[ruleID=ProtectedMemberInFinalClass]
### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `impl/src/main/java/org/apache/myfaces/el/resolver/ResourceBundleResolver.java`
#### Snippet
```java
    }

    protected RuntimeConfig runtimeConfig(ELContext context)
    {
        final FacesContext facesContext = facesContext(context);
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `impl/src/main/java/org/apache/myfaces/util/lang/ClassUtils.java`
#### Snippet
```java
    }

    protected static final String paramString(Class<?>... types)
    {
        if (types != null)
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/LegacyUserTagHandler.java`
#### Snippet
```java
{
    protected final TagAttribute[] _vars;
    protected final URL _location;
    protected final Map<String, DefineHandler> _handlers;

```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/LegacyUserTagHandler.java`
#### Snippet
```java
    protected final TagAttribute[] _vars;
    protected final URL _location;
    protected final Map<String, DefineHandler> _handlers;

    public LegacyUserTagHandler(TagConfig config, URL location)
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/LegacyUserTagHandler.java`
#### Snippet
```java
final class LegacyUserTagHandler extends TagHandler implements TemplateClient, ComponentContainerHandler
{
    protected final TagAttribute[] _vars;
    protected final URL _location;
    protected final Map<String, DefineHandler> _handlers;
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/UserTagHandler.java`
#### Snippet
```java
{

    protected final TagAttribute[] _vars;
    protected final URL _location;
    protected final Map<String, DefineHandler> _handlers;
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/UserTagHandler.java`
#### Snippet
```java

    protected final TagAttribute[] _vars;
    protected final URL _location;
    protected final Map<String, DefineHandler> _handlers;

```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/UserTagHandler.java`
#### Snippet
```java
    protected final TagAttribute[] _vars;
    protected final URL _location;
    protected final Map<String, DefineHandler> _handlers;

    /**
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/LegacyCompositionHandler.java`
#### Snippet
```java
    protected final TagAttribute _template;

    protected final Map<String, DefineHandler> _handlers;
    protected final LegacyParamHandler[] _params;

```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/LegacyCompositionHandler.java`
#### Snippet
```java
     * be used in populating the template specified.
     */
    protected final TagAttribute _template;

    protected final Map<String, DefineHandler> _handlers;
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/LegacyCompositionHandler.java`
#### Snippet
```java

    protected final Map<String, DefineHandler> _handlers;
    protected final LegacyParamHandler[] _params;

    public LegacyCompositionHandler(TagConfig config)
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/CompositionHandler.java`
#### Snippet
```java
            className="jakarta.el.ValueExpression",
            deferredValueType="java.lang.String")
    protected final TagAttribute _template;

    protected final Map<String, DefineHandler> _handlers;
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/CompositionHandler.java`
#### Snippet
```java
    protected final Map<String, DefineHandler> _handlers;

    protected final ParamHandler[] _params;

    public CompositionHandler(TagConfig config)
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/CompositionHandler.java`
#### Snippet
```java
    protected final TagAttribute _template;

    protected final Map<String, DefineHandler> _handlers;

    protected final ParamHandler[] _params;
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/ConvertNumberHandler.java`
#### Snippet
```java
     * jakarta.faces.view.facelets.FaceletContext)
     */
    protected Converter createConverter(FaceletContext ctx) throws FacesException, ELException, FaceletException
    {
        return ctx.getFacesContext().getApplication().createConverter(NumberConverter.CONVERTER_ID);
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/ConvertDateTimeHandler.java`
#### Snippet
```java
     * See jakarta.faces.view.facelets.ConverterHandler#createConverter(jakarta.faces.view.facelets.FaceletContext)
     */
    protected Converter createConverter(FaceletContext ctx) throws FacesException, ELException, FaceletException
    {
        return ctx.getFacesContext().getApplication().createConverter(DateTimeConverter.CONVERTER_ID);
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/LoadBundleHandler.java`
#### Snippet
```java
        {
            protected final String key;
            protected final String value;

            public ResourceEntry(String key, String value)
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/LoadBundleHandler.java`
#### Snippet
```java
        }

        protected final ResourceBundle bundle;

        public ResourceBundleMap(ResourceBundle bundle)
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/LoadBundleHandler.java`
#### Snippet
```java
        private final static class ResourceEntry implements Map.Entry<String, String>
        {
            protected final String key;
            protected final String value;

```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/ValidateDelegateHandler.java`
#### Snippet
```java
     * See jakarta.faces.view.facelets.ValidatorHandler#createValidator(jakarta.faces.view.facelets.FaceletContext)
     */
    protected Validator createValidator(FaceletContext ctx)
    {
        return ctx.getFacesContext().getApplication().createValidator(this.getValidatorId(ctx));
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/FacetHandler.java`
#### Snippet
```java
    public static final String KEY = "facelets.FACET_NAME";

    protected final TagAttribute name;

    public FacetHandler(TagConfig config)
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/ConvertDelegateHandler.java`
#### Snippet
```java
     * jakarta.faces.view.facelets.FaceletContext)
     */
    protected Converter createConverter(FaceletContext ctx) throws FacesException, ELException, FaceletException
    {
        return ctx.getFacesContext().getApplication().createConverter(this.getConverterId(ctx));
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFaceletContext.java`
#### Snippet
```java
        private final AbstractFacelet _owner;

        protected final TemplateClient _target;

        private final Set<String> _names = new HashSet<>();
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFaceletFactory.java`
#### Snippet
```java
     * @return true if it needs to be refreshed
     */
    protected boolean needsToBeRefreshed(DefaultFacelet facelet)
    {
        // if set to 0, constantly reload-- nocache
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFaceletFactory.java`
#### Snippet
```java
    private static final long NO_CACHE_DELAY = 0;
    
    protected static final Logger log = Logger.getLogger(DefaultFaceletFactory.class.getName());

    private Optional<URL> _baseUrl;
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TagLibraryConfig.java`
#### Snippet
```java
public final class TagLibraryConfig
{
    protected final static Logger log = Logger.getLogger(TagLibraryConfig.class.getName());

    private static class TagLibraryImpl extends AbstractTagLibrary
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/CompilationManager.java`
#### Snippet
```java
    }

    protected static boolean isTrimmed(String ns, String name)
    {
        return (CompositionHandler.NAME.equals(name) || ComponentRefHandler.NAME.equals(name)) &&
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/CompilationManager.java`
#### Snippet
```java
    }

    protected static boolean isCompositeComponentImplementation(String ns, String name)
    {
        return ImplementationHandler.NAME.equals(name) && (CompositeLibrary.NAMESPACE.equals(ns)
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/CompilationManager.java`
#### Snippet
```java
    }
    
    protected static boolean isCompositeComponentInterface(String ns, String name)
    {
        return InterfaceHandler.NAME.equals(name) && (CompositeLibrary.NAMESPACE.equals(ns)
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/CompilationManager.java`
#### Snippet
```java
    }

    protected static boolean isRemove(String ns, String name)
    {
        return "remove".equals(name) && (UILibrary.NAMESPACE.equals(ns)
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TextUnit.java`
#### Snippet
```java
    }

    protected void flushBufferToConfig(boolean child)
    {
        this.flushTextBuffer(child);
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/SAXCompiler.java`
#### Snippet
```java
    }
    
    protected FaceletsProcessingInstructions getFaceletsProcessingInstructions(URL src, String alias)
    {
        String processAs = null;
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/SAXCompiler.java`
#### Snippet
```java
    }

    protected static String writeXmlDecl(InputStream is, CompilationManager mngr) throws IOException
    {
        is.mark(128);
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/SAXCompiler.java`
#### Snippet
```java
    }
    
    protected FaceletsProcessingInstructions getDefaultFaceletsProcessingInstructions()
    {
        return FaceletsProcessingInstructions.getProcessingInstructions(FaceletsProcessing.PROCESS_AS_XHTML, false);
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/SAXCompiler.java`
#### Snippet
```java
    }
    
    protected static String getXmlDecl(InputStream is, CompilationManager mngr) throws IOException
    {
        is.mark(128);
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `impl/src/main/java/org/apache/myfaces/context/servlet/ServletExternalContextImpl.java`
#### Snippet
```java
    }
    
    protected FacesContext getCurrentFacesContext()
    {
        if (_currentFacesContext == null)
```

### ProtectedMemberInFinalClass
Class member declared `protected` in 'final' class
in `impl/src/main/java/org/apache/myfaces/context/servlet/ServletExternalContextImpl.java`
#### Snippet
```java
    }

    protected void pushResource(String resourceUrl)
    {
        if (!ExternalSpecifications.isServlet4Available())
```

## RuleId[ruleID=PublicFieldAccessedInSynchronizedContext]
### PublicFieldAccessedInSynchronizedContext
Non-private field `enabled` accessed in synchronized context
in `impl/src/main/java/org/apache/myfaces/util/lang/StringCharArrayAccessor.java`
#### Snippet
```java
    private static synchronized void handleError(Exception e)
    {
        enabled = false;
        valueField = null;
        countField = null;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `valueField` accessed in synchronized context
in `impl/src/main/java/org/apache/myfaces/util/lang/StringCharArrayAccessor.java`
#### Snippet
```java
    {
        enabled = false;
        valueField = null;
        countField = null;
        offsetField = null;
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `countField` accessed in synchronized context
in `impl/src/main/java/org/apache/myfaces/util/lang/StringCharArrayAccessor.java`
#### Snippet
```java
        enabled = false;
        valueField = null;
        countField = null;
        offsetField = null;
    }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `offsetField` accessed in synchronized context
in `impl/src/main/java/org/apache/myfaces/util/lang/StringCharArrayAccessor.java`
#### Snippet
```java
        valueField = null;
        countField = null;
        offsetField = null;
    }

```

### PublicFieldAccessedInSynchronizedContext
Non-private field `valueField` accessed in synchronized context
in `impl/src/main/java/org/apache/myfaces/util/lang/StringCharArrayAccessor.java`
#### Snippet
```java
            synchronized (str)
            {
                valueField.set(str, charBuf);
                if (!jdk7String)
                {
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `jdk7String` accessed in synchronized context
in `impl/src/main/java/org/apache/myfaces/util/lang/StringCharArrayAccessor.java`
#### Snippet
```java
            {
                valueField.set(str, charBuf);
                if (!jdk7String)
                {
                    countField.set(str, charBuf.length);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `countField` accessed in synchronized context
in `impl/src/main/java/org/apache/myfaces/util/lang/StringCharArrayAccessor.java`
#### Snippet
```java
                if (!jdk7String)
                {
                    countField.set(str, charBuf.length);
                }
            }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `created` accessed in synchronized context
in `impl/src/main/java/org/apache/myfaces/resource/TempDirFileCacheResourceLoader.java`
#### Snippet
```java
            ResourceMeta resourceMeta, File file, TempDirFileCacheResourceLoader loader)
        {
            if (!created)
            {
                loader.createTemporalFileVersion(facesContext, resourceMeta, file);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `created` accessed in synchronized context
in `impl/src/main/java/org/apache/myfaces/resource/TempDirFileCacheResourceLoader.java`
#### Snippet
```java
            {
                loader.createTemporalFileVersion(facesContext, resourceMeta, file);
                created = true;
            }
        }
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `created` accessed in synchronized context
in `impl/src/main/java/org/apache/myfaces/resource/TempDirFileCacheContractResourceLoader.java`
#### Snippet
```java
            ResourceMeta resourceMeta, File file, TempDirFileCacheContractResourceLoader loader)
        {
            if (!created)
            {
                loader.createTemporalFileVersion(facesContext, resourceMeta, file);
```

### PublicFieldAccessedInSynchronizedContext
Non-private field `created` accessed in synchronized context
in `impl/src/main/java/org/apache/myfaces/resource/TempDirFileCacheContractResourceLoader.java`
#### Snippet
```java
            {
                loader.createTemporalFileVersion(facesContext, resourceMeta, file);
                created = true;
            }
        }
```

## RuleId[ruleID=RedundantSuppression]
### RedundantSuppression
Redundant suppression
in `api/src/main/java/jakarta/faces/application/Application.java`
#### Snippet
```java
 */
@SuppressWarnings("deprecation")
public abstract class Application
{

```

### RedundantSuppression
Redundant suppression
in `api/src/main/java/jakarta/faces/application/ApplicationWrapper.java`
#### Snippet
```java
 */
@SuppressWarnings("deprecation")
public abstract class ApplicationWrapper extends Application implements FacesWrapper<Application>
{
    private Application delegate;
```

### RedundantSuppression
Redundant suppression
in `api/src/main/java/jakarta/faces/component/UIData.java`
#### Snippet
```java
     */
    @SuppressWarnings("unchecked")
    private void restoreDescendantComponentStates(UIComponent parent, boolean iterateFacets, Object state,
                                                  boolean restoreChildFacets)
    {
```

### RedundantSuppression
Redundant suppression
in `api/src/main/java/jakarta/faces/component/UINamingContainer.java`
#### Snippet
```java
     */
    @SuppressWarnings("deprecation")
    public static char getSeparatorChar(FacesContext context)
    {
        Map<Object, Object> attributes = context.getAttributes();
```

### RedundantSuppression
Redundant suppression
in `impl/src/main/java/org/apache/myfaces/config/DefaultFacesConfigurationProvider.java`
#### Snippet
```java

    @SuppressWarnings("unchecked")
    protected FacesConfigUnmarshaller<? extends FacesConfig> getUnmarshaller(ExternalContext ectx)
    {
        if (_unmarshaller == null)
```

### RedundantSuppression
Redundant suppression
in `impl/src/main/java/org/apache/myfaces/config/FacesConfigurator.java`
#### Snippet
```java
 */
@SuppressWarnings("deprecation")
public class FacesConfigurator
{
    private final Class<?>[] NO_PARAMETER_TYPES = new Class[]{};
```

### RedundantSuppression
Redundant suppression
in `impl/src/main/java/org/apache/myfaces/config/RuntimeConfig.java`
#### Snippet
```java
 */
@SuppressWarnings("deprecation")
public class RuntimeConfig
{
    private static final Logger log = Logger.getLogger(RuntimeConfig.class.getName());
```

### RedundantSuppression
Redundant suppression
in `impl/src/main/java/org/apache/myfaces/context/RequestViewContext.java`
#### Snippet
```java
    
    @SuppressWarnings("unchecked")
    public static RequestViewContext getCurrentInstance(FacesContext ctx, UIViewRoot root)
    {
        return getCurrentInstance(ctx, root, true);
```

### RedundantSuppression
Redundant suppression
in `impl/src/main/java/org/apache/myfaces/el/resolver/LambdaBeanELResolver.java`
#### Snippet
```java
    @SuppressWarnings("unchecked")
    @Override
    public Object getValue(ELContext context, Object base, Object property)
    {        
        Objects.requireNonNull(context);
```

### RedundantSuppression
Redundant suppression
in `impl/src/main/java/org/apache/myfaces/el/resolver/LambdaBeanELResolver.java`
#### Snippet
```java
    @SuppressWarnings("unchecked")
    @Override
    public void setValue(ELContext context, Object base, Object property, Object value)
    {
        Objects.requireNonNull(context);
```

### RedundantSuppression
Redundant suppression
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/HtmlRendererUtils.java`
#### Snippet
```java

    @SuppressWarnings("unchecked")
    public static void renderFormSubmitScript(FacesContext facesContext)
            throws IOException
    {
```

### RedundantSuppression
Redundant suppression
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguage.java`
#### Snippet
```java

    @SuppressWarnings("unchecked")
    private MethodExpression reWrapMethodExpression(MethodExpression createdMethodExpression,
                                                    ValueExpression originalValueExpression)
    {
```

### RedundantSuppression
Redundant suppression
in `impl/src/main/java/org/apache/myfaces/view/facelets/PartialStateManagementStrategy.java`
#### Snippet
```java
    
    @SuppressWarnings("unchecked")
    private void registerOnAddRemoveList(FacesContext facesContext, String clientId)
    {
        UIViewRoot uiViewRoot = facesContext.getViewRoot();
```

### RedundantSuppression
Redundant suppression
in `impl/src/main/java/org/apache/myfaces/view/facelets/PartialStateManagementStrategy.java`
#### Snippet
```java
    
    @SuppressWarnings("unchecked")
    private void registerOnAddList(FacesContext facesContext, String clientId)
    {
        UIViewRoot uiViewRoot = facesContext.getViewRoot();
```

### RedundantSuppression
Redundant suppression
in `impl/src/main/java/org/apache/myfaces/view/facelets/component/UIRepeat.java`
#### Snippet
```java
     */
    @SuppressWarnings("unchecked")
    private void restoreDescendantComponentStates(UIComponent parent, boolean iterateFacets, Object state,
                                                  boolean restoreChildFacets)
    {
```

### RedundantSuppression
Redundant suppression
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFaceletContext.java`
#### Snippet
```java
    @Override
    @SuppressWarnings("unchecked")
    public Object getContext(Class key)
    {
        return _ctx.getContext(key);
```

### RedundantSuppression
Redundant suppression
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFaceletContext.java`
#### Snippet
```java
    @Override
    @SuppressWarnings("unchecked")
    public void putContext(Class key, Object contextObject)
    {
        _ctx.putContext(key, contextObject);
```

### RedundantSuppression
Redundant suppression
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/FaceletCompositionContextImpl.java`
#### Snippet
```java
        @SuppressWarnings("unchecked")
        @Override
        public boolean equals(Object obj)
        {
            if (this == obj)
```

### RedundantSuppression
Redundant suppression
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeComponentResourceTagHandler.java`
#### Snippet
```java
    @SuppressWarnings("unchecked")
    @Override
    public void applyNextHandler(FaceletContext ctx, UIComponent c) throws IOException
    {
        FaceletCompositionContext mctx = FaceletCompositionContext.getCurrentInstance(ctx);
```

### RedundantSuppression
Redundant suppression
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ComponentTagHandlerDelegate.java`
#### Snippet
```java
    @SuppressWarnings("unchecked")
    @Override
    public void apply(FaceletContext ctx, UIComponent parent) throws IOException
    {
        // make sure our parent is not null
```

### RedundantSuppression
Redundant suppression
in `test/src/main/java/org/apache/myfaces/test/mock/_ApplicationMap.java`
#### Snippet
```java
    @Override
    @SuppressWarnings("unchecked")
    protected Enumeration<String> getAttributeNames()
    {
        return _servletContext.getAttributeNames();
```

### RedundantSuppression
Redundant suppression
in `test/src/main/java/org/apache/myfaces/test/mock/_InitParameterMap.java`
#### Snippet
```java
    @Override
    @SuppressWarnings("unchecked")
    protected Enumeration<String> getAttributeNames()
    {
        return _servletContext.getInitParameterNames();
```

### RedundantSuppression
Redundant suppression
in `test/src/main/java/org/apache/myfaces/test/mock/_RequestHeaderMap.java`
#### Snippet
```java
    @Override
    @SuppressWarnings("unchecked")
    protected Enumeration<String> getAttributeNames()
    {
        return _httpServletRequest.getHeaderNames();
```

### RedundantSuppression
Redundant suppression
in `test/src/main/java/org/apache/myfaces/test/mock/_RequestHeaderValuesMap.java`
#### Snippet
```java
    @Override
    @SuppressWarnings("unchecked")
    protected String[] getAttribute(final String key)
    {
        String[] ret = _valueCache.get(key);
```

### RedundantSuppression
Redundant suppression
in `test/src/main/java/org/apache/myfaces/test/mock/_RequestHeaderValuesMap.java`
#### Snippet
```java
    @Override
    @SuppressWarnings("unchecked")
    protected Enumeration<String> getAttributeNames()
    {
        return _httpServletRequest.getHeaderNames();
```

### RedundantSuppression
Redundant suppression
in `test/src/main/java/org/apache/myfaces/test/mock/_RequestMap.java`
#### Snippet
```java
    @Override
    @SuppressWarnings("unchecked")
    protected Enumeration<String> getAttributeNames()
    {
        return _servletRequest.getAttributeNames();
```

### RedundantSuppression
Redundant suppression
in `test/src/main/java/org/apache/myfaces/test/mock/_RequestParameterMap.java`
#### Snippet
```java
    @Override
    @SuppressWarnings("unchecked")
    protected Enumeration<String> getAttributeNames()
    {
        return _servletRequest.getParameterNames();
```

### RedundantSuppression
Redundant suppression
in `test/src/main/java/org/apache/myfaces/test/mock/_RequestParameterValuesMap.java`
#### Snippet
```java
    @Override
    @SuppressWarnings("unchecked")
    protected Enumeration<String> getAttributeNames()
    {
        return _servletRequest.getParameterNames();
```

## RuleId[ruleID=UnnecessaryInitCause]
### UnnecessaryInitCause
Unnecessary `Throwable.initCause()` call
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TagLibraryConfigUnmarshallerImpl.java`
#### Snippet
```java
        {
            IOException ioe = new IOException("Error parsing [" + url + "]: ");
            ioe.initCause(e);
            throw ioe;
        }
```

### UnnecessaryInitCause
Unnecessary `Throwable.initCause()` call
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TagLibraryConfigUnmarshallerImpl.java`
#### Snippet
```java
        {
            IOException ioe = new IOException("Error parsing [" + url + "]: ");
            ioe.initCause(e);
            throw ioe;
        }
```

## RuleId[ruleID=UtilityClassWithPublicConstructor]
### UtilityClassWithPublicConstructor
Class `LocaleUtils` has only 'static' members, and a 'public' constructor
in `api/src/main/java/org/apache/myfaces/core/api/shared/lang/LocaleUtils.java`
#### Snippet
```java
 * @version $Id$
 */
public class LocaleUtils
{

```

### UtilityClassWithPublicConstructor
Class `FilenameUtils` has only 'static' members, and a 'public' constructor
in `impl/src/main/java/org/apache/myfaces/util/lang/FilenameUtils.java`
#### Snippet
```java
 * @since 1.1
 */
public class FilenameUtils
{

```

### UtilityClassWithPublicConstructor
Class `TagLibraryConfig` has only 'static' members, and a 'public' constructor
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TagLibraryConfig.java`
#### Snippet
```java
 * @version $Id$
 */
public final class TagLibraryConfig
{
    protected final static Logger log = Logger.getLogger(TagLibraryConfig.class.getName());
```

### UtilityClassWithPublicConstructor
Class `ErrorPageWriter` has only 'static' members, and a 'public' constructor
in `impl/src/main/java/org/apache/myfaces/renderkit/ErrorPageWriter.java`
#### Snippet
```java
 * @author Jakob Korherr (refactored and moved here from jakarta.faces.webapp._ErrorPageWriter)
 */
public final class ErrorPageWriter
{

```

## RuleId[ruleID=OptionalUsedAsFieldOrParameterType]
### OptionalUsedAsFieldOrParameterType
`Optional` used as type for field '_baseUrl'
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFaceletFactory.java`
#### Snippet
```java
    protected static final Logger log = Logger.getLogger(DefaultFaceletFactory.class.getName());

    private Optional<URL> _baseUrl;
    private Compiler _compiler;
    private Map<String, DefaultFacelet> _compositeComponentMetadataFacelets;
```

## RuleId[ruleID=NonStrictComparisonCanBeEquality]
### NonStrictComparisonCanBeEquality
Can be replaced with equality
in `api/src/main/java/jakarta/faces/component/UIInput.java`
#### Snippet
```java
        else if (value instanceof String)
        {
            if (((String) value).trim().length() <= 0)
            {
                return true;
```

### NonStrictComparisonCanBeEquality
Can be replaced with equality
in `api/src/main/java/jakarta/faces/component/html/_HtmlInputFile.java`
#### Snippet
```java
        else if (value instanceof String)
        {
            if ( ((String)value).trim().length() <= 0 )
            {
                return true;
```

### NonStrictComparisonCanBeEquality
Can be replaced with equality
in `impl/src/main/java/org/apache/myfaces/util/lang/ConcurrentLRUCache.java`
#### Snippet
```java
            // if we didn't remove enough entries, then make more passes
            // over the values we collected, with updated min and max values.
            while (sz - numRemoved > acceptableWaterMark && --numPasses >= 0)
            {

```

## RuleId[ruleID=SystemOutErr]
### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `impl/src/main/java/org/apache/myfaces/application/viewstate/StateUtils.java`
#### Snippet
```java
    {
        byte[] bytes = encode(args[0].getBytes(ZIP_CHARSET));
        System.out.println(new String(bytes, ZIP_CHARSET));
    }

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `test/src/main/java/org/apache/myfaces/test/mock/MockPortletContext.java`
#### Snippet
```java
    {

        System.out.println(message);

    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `test/src/main/java/org/apache/myfaces/test/mock/MockPortletContext.java`
#### Snippet
```java
    {

        System.out.println(message);
        exception.printStackTrace();

```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `test/src/main/java/org/apache/myfaces/test/mock/MockServletContext.java`
#### Snippet
```java
    public void log(String message, Throwable exception)
    {
        System.out.println(message);
        exception.printStackTrace();
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `test/src/main/java/org/apache/myfaces/test/mock/MockServletContext.java`
#### Snippet
```java
    public void log(Exception exception, String message)
    {
        System.out.println(message);
        exception.printStackTrace();
    }
```

### SystemOutErr
Uses of `System.out` should probably be replaced with more robust logging
in `test/src/main/java/org/apache/myfaces/test/mock/MockServletContext.java`
#### Snippet
```java
    public void log(String message)
    {
        System.out.println(message);
    }

```

## RuleId[ruleID=DynamicRegexReplaceableByCompiledPattern]
### DynamicRegexReplaceableByCompiledPattern
`matches()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `api/src/main/java/jakarta/faces/validator/BeanValidator.java`
#### Snippet
```java
    private void postSetValidationGroups()
    {
        if (this.validationGroups == null || this.validationGroups.matches(EMPTY_VALIDATION_GROUPS_PATTERN))
        {
            this.validationGroupsArray = DEFAULT_VALIDATION_GROUPS_ARRAY;
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguageStrategy.java`
#### Snippet
```java

        // Get rid of spaces
        mappings = mappings.replaceAll("\\s", "");

        // Escape '.'
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguageStrategy.java`
#### Snippet
```java

        // Escape '.'
        mappings = mappings.replaceAll("\\.", "\\\\.");

        // Change '*' to '.*' to represent any match
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguageStrategy.java`
#### Snippet
```java

        // Change '*' to '.*' to represent any match
        mappings = mappings.replaceAll("\\*", ".*");

        // Split the mappings by changing ';' to '|'
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguageStrategy.java`
#### Snippet
```java

        // Split the mappings by changing ';' to '|'
        mappings = mappings.replaceAll(";", "|");

        return mappings;
```

### DynamicRegexReplaceableByCompiledPattern
`matches()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ComponentTagHandlerDelegate.java`
#### Snippet
```java
                    String validationGroups =  beanValidator.getValidationGroups();
                    if (validationGroups == null 
                            || validationGroups.matches(BeanValidator.EMPTY_VALIDATION_GROUPS_PATTERN))
                    {
                        // no validationGroups available
```

### DynamicRegexReplaceableByCompiledPattern
`matches()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ComponentTagHandlerDelegate.java`
#### Snippet
```java
            String validationGroups =  beanValidator.getValidationGroups();
            if (validationGroups == null 
                    || validationGroups.matches(BeanValidator.EMPTY_VALIDATION_GROUPS_PATTERN))
            {
                // no validationGroups available
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeResourceLibrary.java`
#### Snippet
```java

        // Get rid of spaces
        mappings = mappings.replaceAll("\\s", "");

        // Escape '.'
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeResourceLibrary.java`
#### Snippet
```java

        // Escape '.'
        mappings = mappings.replaceAll("\\.", "\\\\.");

        // Change '*' to '.*' to represent any match
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeResourceLibrary.java`
#### Snippet
```java

        // Change '*' to '.*' to represent any match
        mappings = mappings.replaceAll("\\*", ".*");

        // Split the mappings by changing ';' to '|'
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeResourceLibrary.java`
#### Snippet
```java

        // Split the mappings by changing ';' to '|'
        mappings = mappings.replaceAll(";", "|");

        return mappings;
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TagLibraryConfig.java`
#### Snippet
```java

            // Get rid of spaces
            mappings = mappings.replaceAll("\\s", "");

            // Escape '.'
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TagLibraryConfig.java`
#### Snippet
```java

            // Escape '.'
            mappings = mappings.replaceAll("\\.", "\\\\.");

            // Change '*' to '.*' to represent any match
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TagLibraryConfig.java`
#### Snippet
```java

            // Change '*' to '.*' to represent any match
            mappings = mappings.replaceAll("\\*", ".*");

            // Split the mappings by changing ';' to '|'
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TagLibraryConfig.java`
#### Snippet
```java

            // Split the mappings by changing ';' to '|'
            mappings = mappings.replaceAll(";", "|");

            return mappings;
```

### DynamicRegexReplaceableByCompiledPattern
`replaceFirst()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `impl/src/main/java/org/apache/myfaces/context/servlet/ServletExternalContextImpl.java`
#### Snippet
```java
            {
                webSocketUrl = new URL(scheme, serverName, port, url).toExternalForm();
                webSocketUrl = webSocketUrl.replaceFirst("http", "ws");
                return ((HttpServletResponse) _servletResponse).encodeURL(webSocketUrl);
            }
```

### DynamicRegexReplaceableByCompiledPattern
`matches()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `impl/src/main/java/org/apache/myfaces/component/validate/WholeBeanValidator.java`
#### Snippet
```java
    {
        String validationGroups = getValidationGroups(component);
        if (validationGroups == null || validationGroups.matches(EMPTY_VALIDATION_GROUPS_PATTERN))
        {
            this.validationGroupsArray = DEFAULT_VALIDATION_GROUPS_ARRAY;
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/JavascriptUtils.java`
#### Snippet
```java
    public static String getValidJavascriptNameAsInRI(String origIdentifier)
    {
        return origIdentifier.replaceAll("-", "\\$_");
    }

```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `impl/src/main/java/org/apache/myfaces/renderkit/ErrorPageWriter.java`
#### Snippet
```java
        writer.write(name);
        writer.write("=\"");
        writer.write(value.replaceAll("<", TS));
        writer.write("\"");
    }
```

### DynamicRegexReplaceableByCompiledPattern
`split()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `impl/src/main/java/org/apache/myfaces/renderkit/ErrorPageWriter.java`
#### Snippet
```java
            }
            String str = baos.toString();
            return str.split("@@");
        }
        finally
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `impl/src/main/java/org/apache/myfaces/renderkit/ErrorPageWriter.java`
#### Snippet
```java
        {
            String str = c.toString().trim();
            writer.write(str.replaceAll("<", TS));
        }
        else
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `impl/src/main/java/org/apache/myfaces/renderkit/ErrorPageWriter.java`
#### Snippet
```java
            e.printStackTrace(pstr);
        }
        writer.write(str.toString().replaceAll("<", TS));
    }

```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `impl/src/main/java/org/apache/myfaces/renderkit/ErrorPageWriter.java`
#### Snippet
```java
                {
                    writer.write("<tr><td>");
                    writer.write(key.replaceAll("<", TS));
                    writer.write("</td><td>");
                    Object value = entry.getValue();
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `impl/src/main/java/org/apache/myfaces/renderkit/ErrorPageWriter.java`
#### Snippet
```java
                    if (value != null && value.toString() != null)
                    {
                        writer.write(value.toString().replaceAll("<", TS));
                    }
                    else
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `impl/src/main/java/org/apache/myfaces/renderkit/ErrorPageWriter.java`
#### Snippet
```java
                    if (msg != null)
                    {
                        writer.write(msg.replaceAll("<", TS));
                    }
                    else
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `impl/src/main/java/org/apache/myfaces/renderkit/ErrorPageWriter.java`
#### Snippet
```java
        {
            msg = ex.getClass().getName() + " - " + msg;
            writer.write(msg.replaceAll("<", TS));
        }
        else
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `test/src/main/java/org/apache/myfaces/test/el/MockMethodExpression.java`
#### Snippet
```java
            {
                String temp = expression.substring(2, expression.length() - 1)
                        .replaceAll(" ", "");
                List names = new ArrayList();
                while (temp.length() > 0)
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `test/src/main/java/org/apache/myfaces/test/base/junit/runners/TestPerClassLoaderRunner.java`
#### Snippet
```java

            return mf.getMainAttributes().getValue(Attributes.Name.CLASS_PATH)
                    .replaceAll(" ", System.getProperty("path.separator"))
                    .replaceAll("file:/", "");
        }
```

### DynamicRegexReplaceableByCompiledPattern
`replaceAll()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `test/src/main/java/org/apache/myfaces/test/base/junit/runners/TestPerClassLoaderRunner.java`
#### Snippet
```java
            return mf.getMainAttributes().getValue(Attributes.Name.CLASS_PATH)
                    .replaceAll(" ", System.getProperty("path.separator"))
                    .replaceAll("file:/", "");
        }
        catch (MalformedURLException e)
```

### DynamicRegexReplaceableByCompiledPattern
`replaceFirst()` could be replaced with compiled 'java.util.regex.Pattern' construct
in `test/src/main/java/org/apache/myfaces/test/mock/MockExternalContext10.java`
#### Snippet
```java
            {
                url = new URL(scheme, serverName, port, baseUrl).toExternalForm();
                url = url.replaceFirst("http", "ws");
                return url;
            }
```

## RuleId[ruleID=SamePackageImport]
### SamePackageImport
Unnecessary import from the same package `import jakarta.faces.component.UISelectItem;`
in `api/src/main/java/jakarta/faces/component/UISelectOne.java`
#### Snippet
```java
import jakarta.faces.convert.Converter;
import jakarta.faces.model.SelectItem;
import jakarta.faces.component.UISelectItem;
import org.apache.myfaces.buildtools.maven2.plugin.builder.annotation.JSFComponent;
import org.apache.myfaces.buildtools.maven2.plugin.builder.annotation.JSFJspProperty;
```

## RuleId[ruleID=NonProtectedConstructorInAbstractClass]
### NonProtectedConstructorInAbstractClass
Constructor `FlowHandlerFactoryWrapper()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/flow/FlowHandlerFactoryWrapper.java`
#### Snippet
```java

    @Deprecated
    public FlowHandlerFactoryWrapper()
    {
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `FlowHandlerFactoryWrapper()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/flow/FlowHandlerFactoryWrapper.java`
#### Snippet
```java
    }

    public FlowHandlerFactoryWrapper(FlowHandlerFactory delegate)
    {
        this.delegate = delegate;
```

### NonProtectedConstructorInAbstractClass
Constructor `ViewDeclarationLanguageFactory()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/view/ViewDeclarationLanguageFactory.java`
#### Snippet
```java
    }

    public ViewDeclarationLanguageFactory(ViewDeclarationLanguageFactory delegate)
    {
        this.delegate = delegate;
```

### NonProtectedConstructorInAbstractClass
Constructor `ViewDeclarationLanguageFactory()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/view/ViewDeclarationLanguageFactory.java`
#### Snippet
```java
     */
    @Deprecated
    public ViewDeclarationLanguageFactory()
    {
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `FaceletCacheFactory()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/view/facelets/FaceletCacheFactory.java`
#### Snippet
```java
    }

    public FaceletCacheFactory(FaceletCacheFactory delegate)
    {
        this.delegate = delegate;
```

### NonProtectedConstructorInAbstractClass
Constructor `FaceletCacheFactory()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/view/facelets/FaceletCacheFactory.java`
#### Snippet
```java

    @Deprecated
    public FaceletCacheFactory()
    {
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `MetaTagHandler()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/view/facelets/MetaTagHandler.java`
#### Snippet
```java
    private Metadata _mapper;

    public MetaTagHandler(TagConfig config)
    {
        super(config);
```

### NonProtectedConstructorInAbstractClass
Constructor `ViewDeclarationLanguageWrapper()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/view/ViewDeclarationLanguageWrapper.java`
#### Snippet
```java
    }

    public ViewDeclarationLanguageWrapper(ViewDeclarationLanguage delegate)
    {
        this.delegate = delegate;
```

### NonProtectedConstructorInAbstractClass
Constructor `ViewDeclarationLanguageWrapper()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/view/ViewDeclarationLanguageWrapper.java`
#### Snippet
```java

    @Deprecated
    public ViewDeclarationLanguageWrapper()
    {
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `TagHandlerDelegateFactory()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/view/facelets/TagHandlerDelegateFactory.java`
#### Snippet
```java

    @Deprecated
    public TagHandlerDelegateFactory()
    {
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `TagHandlerDelegateFactory()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/view/facelets/TagHandlerDelegateFactory.java`
#### Snippet
```java
    }

    public TagHandlerDelegateFactory(TagHandlerDelegateFactory delegate)
    {
        this.delegate = delegate;
```

### NonProtectedConstructorInAbstractClass
Constructor `FaceletsAttachedObjectHandler()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/view/facelets/FaceletsAttachedObjectHandler.java`
#### Snippet
```java
     * 
     */
    public FaceletsAttachedObjectHandler(TagConfig config)
    {
        super(config);
```

### NonProtectedConstructorInAbstractClass
Constructor `TagAttributes()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/view/facelets/TagAttributes.java`
#### Snippet
```java
     * 
     */
    public TagAttributes()
    {
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `TagHandler()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/view/facelets/TagHandler.java`
#### Snippet
```java
    protected final FaceletHandler nextHandler;

    public TagHandler(TagConfig config)
    {
        this.tagId = config.getTagId();
```

### NonProtectedConstructorInAbstractClass
Constructor `DelegatingMetaTagHandler()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/view/facelets/DelegatingMetaTagHandler.java`
#### Snippet
```java
    private TagAttribute disabled;
    
    public DelegatingMetaTagHandler(TagConfig config)
    {
        super(config);
```

### NonProtectedConstructorInAbstractClass
Constructor `TagAttribute()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/view/facelets/TagAttribute.java`
#### Snippet
```java
public abstract class TagAttribute
{
    public TagAttribute()
    {
        
```

### NonProtectedConstructorInAbstractClass
Constructor `SystemEvent()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/event/SystemEvent.java`
#### Snippet
```java
    private transient FacesContext facesContext;
    
    public SystemEvent(Object source)
    {
        super(source);
```

### NonProtectedConstructorInAbstractClass
Constructor `SystemEvent()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/event/SystemEvent.java`
#### Snippet
```java
    }
    
    public SystemEvent(FacesContext facesContext, Object source)
    {
        super(source);
```

### NonProtectedConstructorInAbstractClass
Constructor `BehaviorEvent()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/event/BehaviorEvent.java`
#### Snippet
```java
    private Behavior _behavior;

    public BehaviorEvent(FacesContext facesContext, UIComponent uiComponent, Behavior behavior)
    {
        super(facesContext, uiComponent);
```

### NonProtectedConstructorInAbstractClass
Constructor `BehaviorEvent()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/event/BehaviorEvent.java`
#### Snippet
```java
    }
    
    public BehaviorEvent(UIComponent component, Behavior behavior) 
    {
        super(component);
```

### NonProtectedConstructorInAbstractClass
Constructor `FacesEvent()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/event/FacesEvent.java`
#### Snippet
```java
    }
    
    public FacesEvent(UIComponent uiComponent)
    {
        super(uiComponent);
```

### NonProtectedConstructorInAbstractClass
Constructor `FacesEvent()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/event/FacesEvent.java`
#### Snippet
```java
    private transient FacesContext facesContext;

    public FacesEvent(FacesContext facesContext, UIComponent uiComponent)
    {
        this(uiComponent);
```

### NonProtectedConstructorInAbstractClass
Constructor `ComponentSystemEvent()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/event/ComponentSystemEvent.java`
#### Snippet
```java
public abstract class ComponentSystemEvent extends SystemEvent
{
    public ComponentSystemEvent(UIComponent component)
    {
        super(component);
```

### NonProtectedConstructorInAbstractClass
Constructor `ComponentSystemEvent()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/event/ComponentSystemEvent.java`
#### Snippet
```java
    }
    
    public ComponentSystemEvent(FacesContext facesContext, UIComponent component)
    {
        super(facesContext, component);
```

### NonProtectedConstructorInAbstractClass
Constructor `RenderKitFactory()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/render/RenderKitFactory.java`
#### Snippet
```java

    @Deprecated
    public RenderKitFactory()
    {
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `RenderKitFactory()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/render/RenderKitFactory.java`
#### Snippet
```java
    }

    public RenderKitFactory(RenderKitFactory delegate)
    {
        this.delegate = delegate;
```

### NonProtectedConstructorInAbstractClass
Constructor `RenderKitWrapper()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/render/RenderKitWrapper.java`
#### Snippet
```java

    @Deprecated
    public RenderKitWrapper()
    {
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `RenderKitWrapper()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/render/RenderKitWrapper.java`
#### Snippet
```java
    }

    public RenderKitWrapper(RenderKit delegate)
    {
        this.delegate = delegate;
```

### NonProtectedConstructorInAbstractClass
Constructor `RenderKit()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/render/RenderKit.java`
#### Snippet
```java
    private HashMap<String, ClientBehaviorRenderer> clientBehaviorRenderers;
    
    public RenderKit()
    {
        this.clientBehaviorRenderers = new HashMap<>();
```

### NonProtectedConstructorInAbstractClass
Constructor `RendererWrapper()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/render/RendererWrapper.java`
#### Snippet
```java
    }

    public RendererWrapper(Renderer delegate)
    {
        this.delegate = delegate;
```

### NonProtectedConstructorInAbstractClass
Constructor `RendererWrapper()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/render/RendererWrapper.java`
#### Snippet
```java

    @Deprecated
    public RendererWrapper()
    {
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `ExceptionHandlerFactory()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/context/ExceptionHandlerFactory.java`
#### Snippet
```java
    }

    public ExceptionHandlerFactory(ExceptionHandlerFactory delegate)
    {
        this.delegate = delegate;
```

### NonProtectedConstructorInAbstractClass
Constructor `ExceptionHandlerFactory()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/context/ExceptionHandlerFactory.java`
#### Snippet
```java

    @Deprecated
    public ExceptionHandlerFactory()
    {
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `ExceptionHandlerWrapper()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/context/ExceptionHandlerWrapper.java`
#### Snippet
```java

    @Deprecated
    public ExceptionHandlerWrapper()
    {
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `ExceptionHandlerWrapper()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/context/ExceptionHandlerWrapper.java`
#### Snippet
```java
    }

    public ExceptionHandlerWrapper(ExceptionHandler delegate)
    {
        this.delegate = delegate;
```

### NonProtectedConstructorInAbstractClass
Constructor `PartialViewContextWrapper()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/context/PartialViewContextWrapper.java`
#### Snippet
```java
    }

    public PartialViewContextWrapper(PartialViewContext delegate)
    {
        this.delegate = delegate;
```

### NonProtectedConstructorInAbstractClass
Constructor `PartialViewContextWrapper()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/context/PartialViewContextWrapper.java`
#### Snippet
```java
    
    @Deprecated
    public PartialViewContextWrapper()
    {
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `FlashFactory()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/context/FlashFactory.java`
#### Snippet
```java

    @Deprecated
    public FlashFactory()
    {
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `FlashFactory()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/context/FlashFactory.java`
#### Snippet
```java
    }

    public FlashFactory(FlashFactory delegate)
    {
        this.delegate = delegate;
```

### NonProtectedConstructorInAbstractClass
Constructor `FacesContextFactory()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/context/FacesContextFactory.java`
#### Snippet
```java

    @Deprecated
    public FacesContextFactory()
    {
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `FacesContextFactory()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/context/FacesContextFactory.java`
#### Snippet
```java
    }

    public FacesContextFactory(FacesContextFactory delegate)
    {
        this.delegate = delegate;
```

### NonProtectedConstructorInAbstractClass
Constructor `ExceptionHandler()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/context/ExceptionHandler.java`
#### Snippet
```java
public abstract class ExceptionHandler implements SystemEventListener
{
    public ExceptionHandler()
    {

```

### NonProtectedConstructorInAbstractClass
Constructor `ExternalContextFactory()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/context/ExternalContextFactory.java`
#### Snippet
```java
    }

    public ExternalContextFactory(ExternalContextFactory delegate)
    {
        this.delegate = delegate;
```

### NonProtectedConstructorInAbstractClass
Constructor `ExternalContextFactory()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/context/ExternalContextFactory.java`
#### Snippet
```java

    @Deprecated
    public ExternalContextFactory()
    {
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `ResponseWriterWrapper()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/context/ResponseWriterWrapper.java`
#### Snippet
```java

    @Deprecated
    public ResponseWriterWrapper()
    {
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `ResponseWriterWrapper()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/context/ResponseWriterWrapper.java`
#### Snippet
```java
    }

    public ResponseWriterWrapper(ResponseWriter delegate)
    {
        this.delegate = delegate;
```

### NonProtectedConstructorInAbstractClass
Constructor `PartialViewContextFactory()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/context/PartialViewContextFactory.java`
#### Snippet
```java
    }

    public PartialViewContextFactory(PartialViewContextFactory delegate)
    {
        this.delegate = delegate;
```

### NonProtectedConstructorInAbstractClass
Constructor `PartialViewContextFactory()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/context/PartialViewContextFactory.java`
#### Snippet
```java

    @Deprecated
    public PartialViewContextFactory()
    {
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `FacesContextWrapper()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/context/FacesContextWrapper.java`
#### Snippet
```java

    @Deprecated
    public FacesContextWrapper()
    {
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `FacesContextWrapper()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/context/FacesContextWrapper.java`
#### Snippet
```java
    }

    public FacesContextWrapper(FacesContext delegate)
    {
        this.delegate = delegate;
```

### NonProtectedConstructorInAbstractClass
Constructor `FlashWrapper()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/context/FlashWrapper.java`
#### Snippet
```java

    @Deprecated
    public FlashWrapper()
    {
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `FlashWrapper()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/context/FlashWrapper.java`
#### Snippet
```java
    }

    public FlashWrapper(Flash delegate)
    {
        this.delegate = delegate;
```

### NonProtectedConstructorInAbstractClass
Constructor `ExternalContextWrapper()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/context/ExternalContextWrapper.java`
#### Snippet
```java
    }

    public ExternalContextWrapper(ExternalContext delegate)
    {
        this.delegate = delegate;
```

### NonProtectedConstructorInAbstractClass
Constructor `ExternalContextWrapper()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/context/ExternalContextWrapper.java`
#### Snippet
```java
    
    @Deprecated
    public ExternalContextWrapper()
    {
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `UIComponent()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/component/UIComponent.java`
#### Snippet
```java
    protected Map<String, ValueExpression> bindings;

    public UIComponent()
    {
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `VisitContextFactory()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/component/visit/VisitContextFactory.java`
#### Snippet
```java

    @Deprecated
    public VisitContextFactory()
    {
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `VisitContextFactory()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/component/visit/VisitContextFactory.java`
#### Snippet
```java
    }

    public VisitContextFactory(VisitContextFactory delegate)
    {
        this.delegate = delegate;
```

### NonProtectedConstructorInAbstractClass
Constructor `VisitContextWrapper()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/component/visit/VisitContextWrapper.java`
#### Snippet
```java

    @Deprecated
    public VisitContextWrapper()
    {
        
```

### NonProtectedConstructorInAbstractClass
Constructor `VisitContextWrapper()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/component/visit/VisitContextWrapper.java`
#### Snippet
```java
    }

    public VisitContextWrapper(VisitContext delegate)
    {
        this.delegate = delegate;
```

### NonProtectedConstructorInAbstractClass
Constructor `UIComponentBase()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/component/UIComponentBase.java`
#### Snippet
```java
    private transient Renderer _cachedRenderer;
    
    public UIComponentBase()
    {
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `SearchExpressionContextFactory()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/component/search/SearchExpressionContextFactory.java`
#### Snippet
```java
    private SearchExpressionContextFactory delegate;

    public SearchExpressionContextFactory(SearchExpressionContextFactory delegate)
    {
        this.delegate = delegate;
```

### NonProtectedConstructorInAbstractClass
Constructor `SearchExpressionHandlerWrapper()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/component/search/SearchExpressionHandlerWrapper.java`
#### Snippet
```java
    private SearchExpressionHandler delegate;

    public SearchExpressionHandlerWrapper(SearchExpressionHandler delegate)
    {
        this.delegate = delegate;
```

### NonProtectedConstructorInAbstractClass
Constructor `LifecycleWrapper()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/lifecycle/LifecycleWrapper.java`
#### Snippet
```java
    }

    public LifecycleWrapper(Lifecycle delegate)
    {
        this.delegate = delegate;
```

### NonProtectedConstructorInAbstractClass
Constructor `LifecycleWrapper()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/lifecycle/LifecycleWrapper.java`
#### Snippet
```java

    @Deprecated
    public LifecycleWrapper()
    {
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `LifecycleFactory()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/lifecycle/LifecycleFactory.java`
#### Snippet
```java
    }

    public LifecycleFactory(LifecycleFactory delegate)
    {
        this.delegate = delegate;
```

### NonProtectedConstructorInAbstractClass
Constructor `LifecycleFactory()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/lifecycle/LifecycleFactory.java`
#### Snippet
```java

    @Deprecated
    public LifecycleFactory()
    {
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `ClientWindowFactory()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/lifecycle/ClientWindowFactory.java`
#### Snippet
```java

    @Deprecated
    public ClientWindowFactory()
    {
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `ClientWindowFactory()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/lifecycle/ClientWindowFactory.java`
#### Snippet
```java
    }

    public ClientWindowFactory(ClientWindowFactory delegate)
    {
        this.delegate = delegate;
```

### NonProtectedConstructorInAbstractClass
Constructor `ClientWindowWrapper()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/lifecycle/ClientWindowWrapper.java`
#### Snippet
```java

    @Deprecated
    public ClientWindowWrapper()
    {
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `ClientWindowWrapper()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/lifecycle/ClientWindowWrapper.java`
#### Snippet
```java
    private ClientWindow delegate;

    public ClientWindowWrapper(ClientWindow delegate)
    {
        this.delegate = delegate;
```

### NonProtectedConstructorInAbstractClass
Constructor `ResourceWrapper()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/application/ResourceWrapper.java`
#### Snippet
```java
    }

    public ResourceWrapper(Resource delegate)
    {
        this.delegate = delegate;
```

### NonProtectedConstructorInAbstractClass
Constructor `ResourceWrapper()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/application/ResourceWrapper.java`
#### Snippet
```java

    @Deprecated
    public ResourceWrapper()
    {
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `ResourceHandlerWrapper()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/application/ResourceHandlerWrapper.java`
#### Snippet
```java

    @Deprecated
    public ResourceHandlerWrapper()
    {
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `ResourceHandlerWrapper()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/application/ResourceHandlerWrapper.java`
#### Snippet
```java
    }

    public ResourceHandlerWrapper(ResourceHandler delegate)
    {
        this.delegate = delegate;
```

### NonProtectedConstructorInAbstractClass
Constructor `NavigationCaseWrapper()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/application/NavigationCaseWrapper.java`
#### Snippet
```java
    
    @Deprecated
    public NavigationCaseWrapper()
    {
        super(null, null, null, null, null, null, false, false);
```

### NonProtectedConstructorInAbstractClass
Constructor `NavigationCaseWrapper()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/application/NavigationCaseWrapper.java`
#### Snippet
```java
    }

    public NavigationCaseWrapper(NavigationCase delegate)
    {
        this();
```

### NonProtectedConstructorInAbstractClass
Constructor `ApplicationFactory()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/application/ApplicationFactory.java`
#### Snippet
```java

    @Deprecated
    public ApplicationFactory()
    {
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `ApplicationFactory()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/application/ApplicationFactory.java`
#### Snippet
```java
    }

    public ApplicationFactory(ApplicationFactory delegate)
    {
        this.delegate = delegate;
```

### NonProtectedConstructorInAbstractClass
Constructor `NavigationHandlerWrapper()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/application/NavigationHandlerWrapper.java`
#### Snippet
```java

    @Deprecated
    public NavigationHandlerWrapper()
    {
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `NavigationHandlerWrapper()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/application/NavigationHandlerWrapper.java`
#### Snippet
```java
    }

    public NavigationHandlerWrapper(NavigationHandler delegate)
    {
        this.delegate = delegate;
```

### NonProtectedConstructorInAbstractClass
Constructor `ViewResource()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/application/ViewResource.java`
#### Snippet
```java
public abstract class ViewResource
{
    public ViewResource()
    {
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `ConfigurableNavigationHandlerWrapper()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/application/ConfigurableNavigationHandlerWrapper.java`
#### Snippet
```java

    @Deprecated
    public ConfigurableNavigationHandlerWrapper()
    {
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `ConfigurableNavigationHandlerWrapper()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/application/ConfigurableNavigationHandlerWrapper.java`
#### Snippet
```java
    }

    public ConfigurableNavigationHandlerWrapper(ConfigurableNavigationHandler delegate)
    {
        this.delegate = delegate;
```

### NonProtectedConstructorInAbstractClass
Constructor `ViewHandlerWrapper()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/application/ViewHandlerWrapper.java`
#### Snippet
```java

    @Deprecated
    public ViewHandlerWrapper()
    {
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `ViewHandlerWrapper()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/application/ViewHandlerWrapper.java`
#### Snippet
```java
    }

    public ViewHandlerWrapper(ViewHandler delegate)
    {
        this.delegate = delegate;
```

### NonProtectedConstructorInAbstractClass
Constructor `StateManagerWrapper()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/application/StateManagerWrapper.java`
#### Snippet
```java
    private StateManager delegate;

    public StateManagerWrapper(StateManager delegate)
    {
        this.delegate = delegate;
```

### NonProtectedConstructorInAbstractClass
Constructor `ConfigurableNavigationHandler()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/application/ConfigurableNavigationHandler.java`
#### Snippet
```java
{

    public ConfigurableNavigationHandler()
    {
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `ApplicationWrapper()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/application/ApplicationWrapper.java`
#### Snippet
```java

    @Deprecated
    public ApplicationWrapper()
    {
        
```

### NonProtectedConstructorInAbstractClass
Constructor `ApplicationWrapper()` of an abstract class should not be declared 'public'
in `api/src/main/java/jakarta/faces/application/ApplicationWrapper.java`
#### Snippet
```java
    }
    
    public ApplicationWrapper(Application delegate)
    {
        this.delegate = delegate;
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractDynamicProducer()` of an abstract class should not be declared 'public'
in `impl/src/main/java/org/apache/myfaces/cdi/util/AbstractDynamicProducer.java`
#### Snippet
```java
    private Function<CreationalContext<T>, T> create;

    public AbstractDynamicProducer(BeanManager beanManager)
    {
        this.beanManager = beanManager;
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractContextualStorageHolder()` of an abstract class should not be declared 'public'
in `impl/src/main/java/org/apache/myfaces/cdi/util/AbstractContextualStorageHolder.java`
#### Snippet
```java
    protected Map<String, T> storageMap;

    public AbstractContextualStorageHolder()
    {
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `FacesConfigurationProviderWrapper()` of an abstract class should not be declared 'public'
in `impl/src/main/java/org/apache/myfaces/spi/FacesConfigurationProviderWrapper.java`
#### Snippet
```java
{

    public FacesConfigurationProviderWrapper()
    {

```

### NonProtectedConstructorInAbstractClass
Constructor `AnnotationProviderWrapper()` of an abstract class should not be declared 'public'
in `impl/src/main/java/org/apache/myfaces/spi/AnnotationProviderWrapper.java`
#### Snippet
```java
    }

    public AnnotationProviderWrapper(AnnotationProvider delegate)
    {
        this.delegate = delegate;
```

### NonProtectedConstructorInAbstractClass
Constructor `AnnotationProviderWrapper()` of an abstract class should not be declared 'public'
in `impl/src/main/java/org/apache/myfaces/spi/AnnotationProviderWrapper.java`
#### Snippet
```java
    private AnnotationProvider delegate;

    public AnnotationProviderWrapper()
    {
        
```

### NonProtectedConstructorInAbstractClass
Constructor `SkipMatchIterator()` of an abstract class should not be declared 'public'
in `impl/src/main/java/org/apache/myfaces/util/lang/SkipMatchIterator.java`
#### Snippet
```java


    public SkipMatchIterator(Iterator<T> delegate)
    {
        this.delegate = delegate;
```

### NonProtectedConstructorInAbstractClass
Constructor `PriorityQueue()` of an abstract class should not be declared 'public'
in `impl/src/main/java/org/apache/myfaces/util/lang/PriorityQueue.java`
#### Snippet
```java
    private final T[] heap;

    public PriorityQueue(int maxSize)
    {
        this(maxSize, true);
```

### NonProtectedConstructorInAbstractClass
Constructor `PriorityQueue()` of an abstract class should not be declared 'public'
in `impl/src/main/java/org/apache/myfaces/util/lang/PriorityQueue.java`
#### Snippet
```java

    @SuppressWarnings("unchecked")
    public PriorityQueue(int maxSize, boolean prepopulate)
    {
        size = 0;
```

### NonProtectedConstructorInAbstractClass
Constructor `ViewMetadataBase()` of an abstract class should not be declared 'public'
in `impl/src/main/java/org/apache/myfaces/view/ViewMetadataBase.java`
#### Snippet
```java
    private String _viewId;

    public ViewMetadataBase(String viewId)
    {
        _viewId = viewId;
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractChunkReader()` of an abstract class should not be declared 'public'
in `impl/src/main/java/org/apache/myfaces/util/lang/StreamCharBuffer.java`
#### Snippet
```java
        private boolean removeAfterReading;

        public AbstractChunkReader(AbstractChunk parentChunk,
                boolean removeAfterReading)
        {
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractChunk()` of an abstract class should not be declared 'public'
in `impl/src/main/java/org/apache/myfaces/util/lang/StreamCharBuffer.java`
#### Snippet
```java
        int writerUsedCounter;

        public AbstractChunk()
        {
            if (hasReaders)
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractTagLibrary()` of an abstract class should not be declared 'public'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/AbstractTagLibrary.java`
#### Snippet
```java
    }
    
    public AbstractTagLibrary(String namespace, String jcpNamespace, String sunNamespace)
    {
        _namespace = namespace;
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractTagLibrary()` of an abstract class should not be declared 'public'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/AbstractTagLibrary.java`
#### Snippet
```java
    }
    
    public AbstractTagLibrary(String namespace, String jcpNamespace)
    {
        this(namespace, jcpNamespace, null);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractTagLibrary()` of an abstract class should not be declared 'public'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/AbstractTagLibrary.java`
#### Snippet
```java
    private Boolean _strictJsf2FaceletsCompatibility;

    public AbstractTagLibrary(String namespace)
    {
        this(namespace, null, null);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractHtmlLibrary()` of an abstract class should not be declared 'public'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/html/AbstractHtmlLibrary.java`
#### Snippet
```java
    }    

    public AbstractHtmlLibrary(String namespace, String jcpNamespace, String sunNamespace)
    {
        super(namespace, jcpNamespace, sunNamespace);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractHtmlLibrary()` of an abstract class should not be declared 'public'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/html/AbstractHtmlLibrary.java`
#### Snippet
```java
{

    public AbstractHtmlLibrary(String namespace)
    {
        super(namespace);
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractHtmlLibrary()` of an abstract class should not be declared 'public'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/html/AbstractHtmlLibrary.java`
#### Snippet
```java
    }
    
    public AbstractHtmlLibrary(String namespace, String jcpNamespace)
    {
        super(namespace, jcpNamespace);
```

### NonProtectedConstructorInAbstractClass
Constructor `AttachedObjectTargetHandler()` of an abstract class should not be declared 'public'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/AttachedObjectTargetHandler.java`
#### Snippet
```java
    private AttachedObjectTarget _target;

    public AttachedObjectTargetHandler(TagConfig config)
    {
        super(config);
```

### NonProtectedConstructorInAbstractClass
Constructor `Compiler()` of an abstract class should not be declared 'public'
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/Compiler.java`
#### Snippet
```java
    private Collection<FaceletsProcessing> faceletsProcessingConfigurations;

    public Compiler()
    {

```

### NonProtectedConstructorInAbstractClass
Constructor `ServletExternalContextImplBase()` of an abstract class should not be declared 'public'
in `impl/src/main/java/org/apache/myfaces/context/servlet/ServletExternalContextImplBase.java`
#### Snippet
```java

    
    public ServletExternalContextImplBase(ServletContext servletContext)
    {
        _servletContext = servletContext;
```

### NonProtectedConstructorInAbstractClass
Constructor `ContractResourceLoader()` of an abstract class should not be declared 'public'
in `impl/src/main/java/org/apache/myfaces/resource/ContractResourceLoader.java`
#### Snippet
```java
    public static final String VERSION_INVALID = "INVALID";
    
    public ContractResourceLoader(String prefix)
    {
        super(prefix);
```

### NonProtectedConstructorInAbstractClass
Constructor `FacesContextImplBase()` of an abstract class should not be declared 'public'
in `impl/src/main/java/org/apache/myfaces/context/servlet/FacesContextImplBase.java`
#### Snippet
```java
    }
    
    public FacesContextImplBase(final ExternalContext externalContext,
            final ExternalContext defaultExternalContext,
            final ApplicationFactory applicationFactory,
```

### NonProtectedConstructorInAbstractClass
Constructor `FacesContextImplBase()` of an abstract class should not be declared 'public'
in `impl/src/main/java/org/apache/myfaces/context/servlet/FacesContextImplBase.java`
#### Snippet
```java
     * Calls FacesContext.setCurrentInstance(this);
     */
    public FacesContextImplBase(final ExternalContext externalContext,
            final ExternalContext defaultExternalContext)
    {
```

### NonProtectedConstructorInAbstractClass
Constructor `ContractResourceLoaderWrapper()` of an abstract class should not be declared 'public'
in `impl/src/main/java/org/apache/myfaces/resource/ContractResourceLoaderWrapper.java`
#### Snippet
```java
{

    public ContractResourceLoaderWrapper()
    {
        super(null);
```

### NonProtectedConstructorInAbstractClass
Constructor `ResourceLoaderWrapper()` of an abstract class should not be declared 'public'
in `impl/src/main/java/org/apache/myfaces/resource/ResourceLoaderWrapper.java`
#### Snippet
```java
public abstract class ResourceLoaderWrapper extends ResourceLoader implements FacesWrapper<ResourceLoader>
{
    public ResourceLoaderWrapper()
    {
        super(null);
```

### NonProtectedConstructorInAbstractClass
Constructor `ResourceLoader()` of an abstract class should not be declared 'public'
in `impl/src/main/java/org/apache/myfaces/resource/ResourceLoader.java`
#### Snippet
```java
    private ConcurrentLRUCache<Object, Boolean> resourceExistsCache;
    
    public ResourceLoader(String prefix)
    {
        this.prefix = prefix;
```

### NonProtectedConstructorInAbstractClass
Constructor `HtmlLinkRendererBase()` of an abstract class should not be declared 'public'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlLinkRendererBase.java`
#### Snippet
```java
    private MyfacesConfig myfacesConfig;
    
    public HtmlLinkRendererBase()
    {
        myfacesConfig = MyfacesConfig.getCurrentInstance();
```

### NonProtectedConstructorInAbstractClass
Constructor `SessionViewStorageFactory()` of an abstract class should not be declared 'public'
in `impl/src/main/java/org/apache/myfaces/application/viewstate/SessionViewStorageFactory.java`
#### Snippet
```java
    private KeyFactory<K> keyFactory;

    public SessionViewStorageFactory(KeyFactory<K> keyFactory)
    {
        this.keyFactory = keyFactory;
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractJsfTestCase()` of an abstract class should not be declared 'public'
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractJsfTestCase.java`
#### Snippet
```java
     * <p>Construct a new instance of this test case.</p>
     */
    public AbstractJsfTestCase()
    {
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractJsfConfigurableMultipleRequestsTestCase()` of an abstract class should not be declared 'public'
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractJsfConfigurableMultipleRequestsTestCase.java`
#### Snippet
```java
     * <p>Construct a new instance of this test case.</p>
     */
    public AbstractJsfConfigurableMultipleRequestsTestCase()
    {
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractJsfConfigurableMockTestCase()` of an abstract class should not be declared 'public'
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractJsfConfigurableMockTestCase.java`
#### Snippet
```java
     * <p>Construct a new instance of this test case.</p>
     */
    public AbstractJsfConfigurableMockTestCase()
    {
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractViewControllerTestCase()` of an abstract class should not be declared 'public'
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractViewControllerTestCase.java`
#### Snippet
```java
     * <p>Construct a new instance of this test case.</p>
     */
    public AbstractViewControllerTestCase()
    {
        super();
```

### NonProtectedConstructorInAbstractClass
Constructor `AbstractHtmlUnitTestCase()` of an abstract class should not be declared 'public'
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractHtmlUnitTestCase.java`
#### Snippet
```java
     * <p>Construct a new instance of this test case.</p>
     */
    public AbstractHtmlUnitTestCase()
    {
    }
```

### NonProtectedConstructorInAbstractClass
Constructor `MockExternalContext12()` of an abstract class should not be declared 'public'
in `test/src/main/java/org/apache/myfaces/test/mock/MockExternalContext12.java`
#### Snippet
```java
    // ------------------------------------------------------------ Constructors

    public MockExternalContext12(ServletContext context,
            HttpServletRequest request, HttpServletResponse response)
    {
```

### NonProtectedConstructorInAbstractClass
Constructor `MockExternalContext20()` of an abstract class should not be declared 'public'
in `test/src/main/java/org/apache/myfaces/test/mock/MockExternalContext20.java`
#### Snippet
```java
    // ------------------------------------------------------------ Constructors

    public MockExternalContext20(ServletContext context,
            HttpServletRequest request, HttpServletResponse response)
    {
```

### NonProtectedConstructorInAbstractClass
Constructor `MockApplication12()` of an abstract class should not be declared 'public'
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication12.java`
#### Snippet
```java
     * <p>Construct a default instance.</p>
     */
    public MockApplication12()
    {

```

### NonProtectedConstructorInAbstractClass
Constructor `MockExternalContext10()` of an abstract class should not be declared 'public'
in `test/src/main/java/org/apache/myfaces/test/mock/MockExternalContext10.java`
#### Snippet
```java
     * @param response <code>HttpServletResponse</code> for this request
     */
    public MockExternalContext10(ServletContext context,
            HttpServletRequest request, HttpServletResponse response)
    {
```

### NonProtectedConstructorInAbstractClass
Constructor `MockApplication22()` of an abstract class should not be declared 'public'
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication22.java`
#### Snippet
```java
    // ------------------------------------------------------------ Constructors

    public MockApplication22()
    {
        super();
```

### NonProtectedConstructorInAbstractClass
Constructor `MockFacesContext10()` of an abstract class should not be declared 'public'
in `test/src/main/java/org/apache/myfaces/test/mock/MockFacesContext10.java`
#### Snippet
```java
    }

    public MockFacesContext10(ExternalContext externalContext)
    {
        setExternalContext(externalContext);
```

### NonProtectedConstructorInAbstractClass
Constructor `MockFacesContext10()` of an abstract class should not be declared 'public'
in `test/src/main/java/org/apache/myfaces/test/mock/MockFacesContext10.java`
#### Snippet
```java
    }

    public MockFacesContext10(ExternalContext externalContext, Lifecycle lifecycle)
    {
        this(externalContext);
```

### NonProtectedConstructorInAbstractClass
Constructor `MockFacesContext10()` of an abstract class should not be declared 'public'
in `test/src/main/java/org/apache/myfaces/test/mock/MockFacesContext10.java`
#### Snippet
```java
    // ------------------------------------------------------------ Constructors

    public MockFacesContext10()
    {
        super();
```

### NonProtectedConstructorInAbstractClass
Constructor `MockApplication20()` of an abstract class should not be declared 'public'
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication20.java`
#### Snippet
```java
    // ------------------------------------------------------------ Constructors

    public MockApplication20()
    {
        super();
```

### NonProtectedConstructorInAbstractClass
Constructor `MockFacesContext20()` of an abstract class should not be declared 'public'
in `test/src/main/java/org/apache/myfaces/test/mock/MockFacesContext20.java`
#### Snippet
```java
    }

    public MockFacesContext20(ExternalContext externalContext,
            Lifecycle lifecycle)
    {
```

### NonProtectedConstructorInAbstractClass
Constructor `MockFacesContext20()` of an abstract class should not be declared 'public'
in `test/src/main/java/org/apache/myfaces/test/mock/MockFacesContext20.java`
#### Snippet
```java
    }

    public MockFacesContext20(ExternalContext externalContext)
    {
        super(externalContext);
```

### NonProtectedConstructorInAbstractClass
Constructor `MockFacesContext20()` of an abstract class should not be declared 'public'
in `test/src/main/java/org/apache/myfaces/test/mock/MockFacesContext20.java`
#### Snippet
```java
    // ------------------------------------------------------------ Constructors

    public MockFacesContext20()
    {
        super();
```

### NonProtectedConstructorInAbstractClass
Constructor `MockFacesContext12()` of an abstract class should not be declared 'public'
in `test/src/main/java/org/apache/myfaces/test/mock/MockFacesContext12.java`
#### Snippet
```java
    }

    public MockFacesContext12(ExternalContext externalContext,
            Lifecycle lifecycle)
    {
```

### NonProtectedConstructorInAbstractClass
Constructor `MockFacesContext12()` of an abstract class should not be declared 'public'
in `test/src/main/java/org/apache/myfaces/test/mock/MockFacesContext12.java`
#### Snippet
```java
    // ------------------------------------------------------------ Constructors

    public MockFacesContext12()
    {
        super();
```

### NonProtectedConstructorInAbstractClass
Constructor `MockFacesContext12()` of an abstract class should not be declared 'public'
in `test/src/main/java/org/apache/myfaces/test/mock/MockFacesContext12.java`
#### Snippet
```java
    }

    public MockFacesContext12(ExternalContext externalContext)
    {
        super(externalContext);
```

### NonProtectedConstructorInAbstractClass
Constructor `MockApplication10()` of an abstract class should not be declared 'public'
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication10.java`
#### Snippet
```java
     * <p>Construct a default instance.</p>
     */
    public MockApplication10()
    {

```

### NonProtectedConstructorInAbstractClass
Constructor `MockResourceLoader()` of an abstract class should not be declared 'public'
in `test/src/main/java/org/apache/myfaces/test/mock/resource/MockResourceLoader.java`
#### Snippet
```java
    private String _prefix;

    public MockResourceLoader(String prefix)
    {
        _prefix = prefix;
```

## RuleId[ruleID=JavaReflectionInvocation]
### JavaReflectionInvocation
Single-item array is expected
in `test/src/main/java/org/apache/myfaces/test/mock/MockFacesContextFactory.java`
#### Snippet
```java
        {
            externalContext = (MockExternalContext) mecConstructor
                    .newInstance(new Object[] { context, request, response });
        }
        catch (RuntimeException e)
```

## RuleId[ruleID=AssignmentToMethodParameter]
### AssignmentToMethodParameter
Assignment to method parameter `valueExpression`
in `api/src/main/java/org/apache/myfaces/core/api/shared/ValueReferenceResolver.java`
#### Snippet
```java
        while (valueReference != null && valueReference.getBase() instanceof CompositeComponentExpressionHolder)
        {
            valueExpression = ((CompositeComponentExpressionHolder) valueReference.getBase())
                                  .getExpression((String) valueReference.getProperty());
            if (valueExpression == null)
```

### AssignmentToMethodParameter
Assignment to method parameter `component`
in `api/src/main/java/org/apache/myfaces/core/api/shared/ComponentUtils.java`
#### Snippet
```java
                return component;
            }
            component = parent;
        }
    }
```

### AssignmentToMethodParameter
Assignment to method parameter `locale`
in `api/src/main/java/org/apache/myfaces/core/api/shared/MessageUtils.java`
#### Snippet
```java
        if(locale == null)
        {
            locale = Locale.getDefault();
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `attribute`
in `api/src/main/java/org/apache/myfaces/core/api/shared/SharedRendererUtils.java`
#### Snippet
```java
        {
            // get the value of the ValueExpression
            attribute = ((ValueExpression) attribute).getValue(facesContext.getELContext());
        }
        // ... String that is a fully qualified Java class name
```

### AssignmentToMethodParameter
Assignment to method parameter `type`
in `api/src/main/java/org/apache/myfaces/core/api/shared/lang/ClassUtils.java`
#### Snippet
```java
        if (type.indexOf('.') == -1)
        {
            type = "java.lang." + type;
        }
        return classForName(type);
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `api/src/main/java/jakarta/faces/convert/ByteConverter.java`
#### Snippet
```java
        }
        
        value = value.trim();
        if (value.length() < 1)
        {
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `api/src/main/java/jakarta/faces/convert/FloatConverter.java`
#### Snippet
```java
        }
        
        value = value.trim();
        if (value.length() < 1)
        {
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `api/src/main/java/jakarta/faces/convert/CharacterConverter.java`
#### Snippet
```java
        }
        
        value = value.trim();
        if (value.length() < 1)
        {
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `api/src/main/java/jakarta/faces/convert/EnumConverter.java`
#### Snippet
```java
            return null;
        }
        value = value.trim();
        if (value.length() == 0)
        {
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `api/src/main/java/jakarta/faces/convert/LongConverter.java`
#### Snippet
```java
        }
        
        value = value.trim();
        if (value.length() < 1)
        {
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `api/src/main/java/jakarta/faces/convert/BigIntegerConverter.java`
#### Snippet
```java
        }
        
        value = value.trim();
        if (value.length() < 1)
        {
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `api/src/main/java/jakarta/faces/convert/BigDecimalConverter.java`
#### Snippet
```java
        }
        
        value = value.trim();
        if (value.length() < 1)
        {
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `api/src/main/java/jakarta/faces/convert/ShortConverter.java`
#### Snippet
```java
        }
        
        value = value.trim();
        if (value.length() < 1)
        {
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `api/src/main/java/jakarta/faces/convert/DoubleConverter.java`
#### Snippet
```java
            }

            value = sbVal.toString();
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `api/src/main/java/jakarta/faces/convert/DoubleConverter.java`
#### Snippet
```java
        }
        
        value = value.trim();
        if (value.length() < 1)
        {
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `api/src/main/java/jakarta/faces/convert/DoubleConverter.java`
#### Snippet
```java
        try
        {
            value = fixLocale(facesContext, value);
            return Double.valueOf(value);
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `api/src/main/java/jakarta/faces/convert/IntegerConverter.java`
#### Snippet
```java
        }
        
        value = value.trim();
        if (value.length() < 1)
        {
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `api/src/main/java/jakarta/faces/convert/BooleanConverter.java`
#### Snippet
```java
        }
        
        value = value.trim();
        if (value.length() < 1)
        {
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `api/src/main/java/jakarta/faces/convert/NumberConverter.java`
#### Snippet
```java
        }
        
        value = value.trim();
        if (value.length() < 1)
        {
```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `api/src/main/java/jakarta/faces/convert/NumberConverter.java`
#### Snippet
```java
            dfs.setGroupingSeparator(' ');
            df.setDecimalFormatSymbols(dfs);
            value = value.replace('\u00a0', ' ');
            changed = true;
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `component`
in `api/src/main/java/jakarta/faces/component/UIComponent.java`
#### Snippet
```java
        if (component == null)
        {
            component = this;
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `value`
in `api/src/main/java/jakarta/faces/convert/DateTimeConverter.java`
#### Snippet
```java
        if (value != null)
        {
            value = value.trim();
            if (value.length() > 0)
            {
```

### AssignmentToMethodParameter
Assignment to method parameter `location`
in `api/src/main/java/jakarta/faces/component/_ComponentChildrenList.java`
#### Snippet
```java
        while (it.hasNext())
        {
            add(location++, it.next());
        }
        return !collection.isEmpty();
```

### AssignmentToMethodParameter
Assignment to method parameter `oldValue`
in `api/src/main/java/jakarta/faces/component/UIInput.java`
#### Snippet
```java
        if (oldValue != null && oldValue.getClass().isArray() && Object[].class.isAssignableFrom(oldValue.getClass()))
        {
            oldValue = Arrays.deepToString((Object[]) oldValue);
        }
        if (newValue != null && newValue.getClass().isArray() && Object[].class.isAssignableFrom(newValue.getClass()))
```

### AssignmentToMethodParameter
Assignment to method parameter `newValue`
in `api/src/main/java/jakarta/faces/component/UIInput.java`
#### Snippet
```java
        if (newValue != null && newValue.getClass().isArray() && Object[].class.isAssignableFrom(newValue.getClass()))
        {
            newValue = Arrays.deepToString((Object[]) newValue);
        }
        
```

### AssignmentToMethodParameter
Assignment to method parameter `expr`
in `api/src/main/java/jakarta/faces/component/UIComponentBase.java`
#### Snippet
```java
        {
            findBase = ComponentUtils.findRootComponent(this);
            expr = expr.substring(1);
        }
        else
```

### AssignmentToMethodParameter
Assignment to method parameter `target`
in `api/src/main/java/jakarta/faces/component/UIViewRoot.java`
#### Snippet
```java
        {
            // Look for a target attribute on the component
            target = (String)componentResource.getAttributes().get("target");

            // If there is no target attribute
```

### AssignmentToMethodParameter
Assignment to method parameter `target`
in `api/src/main/java/jakarta/faces/component/UIViewRoot.java`
#### Snippet
```java
            {
                // Set target to be the default value head
                target = "head";
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `target`
in `api/src/main/java/jakarta/faces/component/UIViewRoot.java`
#### Snippet
```java
        {
            // Look for a target attribute on the component
            target = (String)componentResource.getAttributes().get("target");

            // If there is no target attribute, set target to be the default value head
```

### AssignmentToMethodParameter
Assignment to method parameter `target`
in `api/src/main/java/jakarta/faces/component/UIViewRoot.java`
#### Snippet
```java
            if (target == null)
            {
                target = "head";
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `stateMap`
in `api/src/main/java/jakarta/faces/component/UIData.java`
#### Snippet
```java
                        ? child.getFacetsAndChildren()
                        : child.getChildren().iterator();
                stateMap = saveFullDescendantComponentStates(facesContext, stateMap, childsIterator, true);
                Object state = child.saveState(facesContext);
                if (state != null)
```

### AssignmentToMethodParameter
Assignment to method parameter `stateMap`
in `api/src/main/java/jakarta/faces/component/UIData.java`
#### Snippet
```java
                    if (stateMap == null)
                    {
                        stateMap = new HashMap<>();
                    }
                    stateMap.put(child.getClientId(facesContext), state);
```

### AssignmentToMethodParameter
Assignment to method parameter `childStates`
in `api/src/main/java/jakarta/faces/component/UIData.java`
#### Snippet
```java
                        ? child.getFacetsAndChildren()
                        : child.getChildren().iterator();
                childStates = saveTransientDescendantComponentStates(facesContext, childStates, childsIterator, true);
                Object state = child.saveTransientState(facesContext);
                if (state != null)
```

### AssignmentToMethodParameter
Assignment to method parameter `childStates`
in `api/src/main/java/jakarta/faces/component/UIData.java`
#### Snippet
```java
                    if (childStates == null)
                    {
                        childStates = new HashMap<>();
                    }
                    childStates.put(child.getClientId(facesContext), state);
```

### AssignmentToMethodParameter
Assignment to method parameter `facesContext`
in `impl/src/main/java/org/apache/myfaces/cdi/util/AbstractContextualStorageHolder.java`
#### Snippet
```java
        if (facesContext == null)
        {
            facesContext = FacesContext.getCurrentInstance();
        }
        
```

### AssignmentToMethodParameter
Assignment to method parameter `facesContext`
in `impl/src/main/java/org/apache/myfaces/cdi/util/AbstractContextualStorageHolder.java`
#### Snippet
```java
            ExternalContext externalContext = new StartupServletExternalContextImpl(servletContext, false);
            ExceptionHandler exceptionHandler = new ExceptionHandlerImpl();
            facesContext = new StartupFacesContextImpl(externalContext, externalContext, exceptionHandler, false);
            tempFacesContext = true;
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `metadata`
in `impl/src/main/java/org/apache/myfaces/spi/impl/NoInjectionAnnotationInjectionProvider.java`
#### Snippet
```java
            ClassLoader cl, Map<Class,Method[]> metadata)
    {
        metadata = (Map<Class,Method[]>) declaredMethodBeans.get(cl);
        if (metadata == null)
        {
```

### AssignmentToMethodParameter
Assignment to method parameter `metadata`
in `impl/src/main/java/org/apache/myfaces/spi/impl/NoInjectionAnnotationInjectionProvider.java`
#### Snippet
```java
        if (metadata == null)
        {
            metadata = new HashMap<Class,Method[]>();
            declaredMethodBeans.put(cl, metadata);
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `response`
in `impl/src/main/java/org/apache/myfaces/util/ExternalContextUtils.java`
#### Snippet
```java
            {
                // unwrap
                response = ((ServletResponseWrapper) response).getResponse();
            }
            // no more possibilities to find a HttpServletResponse
```

### AssignmentToMethodParameter
Assignment to method parameter `path`
in `impl/src/main/java/org/apache/myfaces/util/UrlPatternMatcher.java`
#### Snippet
```java
        if (StringUtils.isEmpty(path))
        {
            path = "/";
        }
        if (StringUtils.isEmpty(pattern))
```

### AssignmentToMethodParameter
Assignment to method parameter `pattern`
in `impl/src/main/java/org/apache/myfaces/util/UrlPatternMatcher.java`
#### Snippet
```java
        if (StringUtils.isEmpty(pattern))
        {
            pattern = "/";
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `pattern`
in `impl/src/main/java/org/apache/myfaces/util/UrlPatternMatcher.java`
#### Snippet
```java
        if (pattern.startsWith("/") && pattern.endsWith("/*"))
        {
            pattern = pattern.substring(0, pattern.length() - 2);
            if (pattern.length() == 0)
            {
```

### AssignmentToMethodParameter
Assignment to method parameter `path`
in `impl/src/main/java/org/apache/myfaces/util/UrlPatternMatcher.java`
#### Snippet
```java
            if (path.endsWith("/"))
            {
                path = path.substring(0, path.length() - 1);
            }
            while (true)
```

### AssignmentToMethodParameter
Assignment to method parameter `path`
in `impl/src/main/java/org/apache/myfaces/util/UrlPatternMatcher.java`
#### Snippet
```java
                    break;
                }
                path = path.substring(0, slash);
            }
            return false;
```

### AssignmentToMethodParameter
Assignment to method parameter `name`
in `impl/src/main/java/org/apache/myfaces/util/DebugUtils.java`
#### Snippet
```java
        if (name.startsWith("jakarta.faces.webapp.UIComponentTag."))
        {
            name = name.substring("jakarta.faces.webapp.UIComponentTag.".length());
        }
        stream.print(' ');
```

### AssignmentToMethodParameter
Assignment to method parameter `toArray`
in `impl/src/main/java/org/apache/myfaces/util/lang/ArrayUtils.java`
#### Snippet
```java
        if (totalLen > Array.getLength(toArray))
        {
            toArray = Array.newInstance(toArray.getClass().getComponentType(), totalLen);
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `len`
in `impl/src/main/java/org/apache/myfaces/util/lang/FastByteArrayInputStream.java`
#### Snippet
```java
        if ((pos + len) > count)
        {
            len = count - pos;
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `n`
in `impl/src/main/java/org/apache/myfaces/util/lang/FastByteArrayInputStream.java`
#### Snippet
```java
        if ((pos + n) > count)
        {
            n = count - pos;
        }
        if (n < 0)
```

### AssignmentToMethodParameter
Assignment to method parameter `key`
in `impl/src/main/java/org/apache/myfaces/util/lang/SubKeyMap.java`
#### Snippet
```java
    public V get(Object key)
    {
        key = _getBaseKey(key);
        return (V) _base.get(key);
    }
```

### AssignmentToMethodParameter
Assignment to method parameter `key`
in `impl/src/main/java/org/apache/myfaces/util/lang/SubKeyMap.java`
#### Snippet
```java
    public V put(String key, V value)
    {
        key = _getBaseKey(key);
        return (V) _base.put(key, value);
    }
```

### AssignmentToMethodParameter
Assignment to method parameter `key`
in `impl/src/main/java/org/apache/myfaces/util/lang/SubKeyMap.java`
#### Snippet
```java
    public V remove(Object key)
    {
        key = _getBaseKey(key);
        return (V) _base.remove(key);
    }
```

### AssignmentToMethodParameter
Assignment to method parameter `viewId`
in `impl/src/main/java/org/apache/myfaces/view/ViewDeclarationLanguageBase.java`
#### Snippet
```java
        try
        {
            viewId = calculateViewId(context, viewId);
            
            Application application = context.getApplication();
```

### AssignmentToMethodParameter
Assignment to method parameter `path`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletsVDLUtils.java`
#### Snippet
```java
        if (path == null || path.length() == 0)
        {
            path = "*";
        }
        if (pattern == null || pattern.length() == 0)
```

### AssignmentToMethodParameter
Assignment to method parameter `pattern`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletsVDLUtils.java`
#### Snippet
```java
        if (pattern == null || pattern.length() == 0)
        {
            pattern = "*";
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `pattern`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletsVDLUtils.java`
#### Snippet
```java
        if (pattern.startsWith("/") && pattern.endsWith("/*"))
        {
            pattern = pattern.substring(0, pattern.length() - 2);
            if (pattern.length() == 0)
            {
```

### AssignmentToMethodParameter
Assignment to method parameter `path`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletsVDLUtils.java`
#### Snippet
```java
            if (path.endsWith("/"))
            {
                path = path.substring(0, path.length() - 1);
            }
            while (true)
```

### AssignmentToMethodParameter
Assignment to method parameter `path`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletsVDLUtils.java`
#### Snippet
```java
                    break;
                }
                path = path.substring(0, slash);
            }
            return false;
```

### AssignmentToMethodParameter
Assignment to method parameter `mappings`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguageStrategy.java`
#### Snippet
```java

        // Get rid of spaces
        mappings = mappings.replaceAll("\\s", "");

        // Escape '.'
```

### AssignmentToMethodParameter
Assignment to method parameter `mappings`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguageStrategy.java`
#### Snippet
```java

        // Escape '.'
        mappings = mappings.replaceAll("\\.", "\\\\.");

        // Change '*' to '.*' to represent any match
```

### AssignmentToMethodParameter
Assignment to method parameter `mappings`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguageStrategy.java`
#### Snippet
```java

        // Change '*' to '.*' to represent any match
        mappings = mappings.replaceAll("\\*", ".*");

        // Split the mappings by changing ';' to '|'
```

### AssignmentToMethodParameter
Assignment to method parameter `mappings`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguageStrategy.java`
#### Snippet
```java

        // Split the mappings by changing ';' to '|'
        mappings = mappings.replaceAll(";", "|");

        return mappings;
```

### AssignmentToMethodParameter
Assignment to method parameter `ids`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FullStateManagementStrategy.java`
#### Snippet
```java
        if (component instanceof NamingContainer)
        {
            ids = new HashSet<>();
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `oldValue`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/DebugPhaseListener.java`
#### Snippet
```java
        if (oldValue != null && oldValue.getClass().isArray())
        {
            oldValue = Arrays.deepToString((Object[]) oldValue);
        }
        if (newValue != null && newValue.getClass().isArray())
```

### AssignmentToMethodParameter
Assignment to method parameter `newValue`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/DebugPhaseListener.java`
#### Snippet
```java
        if (newValue != null && newValue.getClass().isArray())
        {
            newValue = Arrays.deepToString((Object[]) newValue);
        }
        
```

### AssignmentToMethodParameter
Assignment to method parameter `s`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/fn/JstlFunction.java`
#### Snippet
```java
        if (s < 0)
        {
            s = 0;
        }
        if (e >= v.length())
```

### AssignmentToMethodParameter
Assignment to method parameter `e`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/fn/JstlFunction.java`
#### Snippet
```java
        if (e >= v.length())
        {
            e = v.length();
        }
        if (e < s)
```

### AssignmentToMethodParameter
Assignment to method parameter `b`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/fn/JstlFunction.java`
#### Snippet
```java
        if (b == null)
        {
            b = "";
        }
        
```

### AssignmentToMethodParameter
Assignment to method parameter `view`
in `impl/src/main/java/org/apache/myfaces/view/facelets/PartialStateManagementStrategy.java`
#### Snippet
```java
                            if (addedState.length == 2)
                            {
                                view = (UIViewRoot) internalRestoreTreeStructure((TreeStructComponent) addedState[0]);
                                view.processRestoreState(context, addedState[1]);
                                break;
```

### AssignmentToMethodParameter
Assignment to method parameter `validatorClassName`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ComponentTagHandlerDelegate.java`
#### Snippet
```java
            // note that normally we can use this instance later anyway!
            enclosingValidator = context.getApplication().createValidator(validatorId);
            validatorClassName = enclosingValidator.getClass().getName();
        }
        
```

### AssignmentToMethodParameter
Assignment to method parameter `parent`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ComponentTagHandlerDelegate.java`
#### Snippet
```java
                {
                    // Replace parent with the relocated parent.
                    parent = c.getParent();
                    // Since we changed the parent, the facetName becomes invalid, because it points
                    // to the component before relocation. We need to find the right facetName (if any) so we can
```

### AssignmentToMethodParameter
Assignment to method parameter `mappings`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeResourceLibrary.java`
#### Snippet
```java

        // Get rid of spaces
        mappings = mappings.replaceAll("\\s", "");

        // Escape '.'
```

### AssignmentToMethodParameter
Assignment to method parameter `mappings`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeResourceLibrary.java`
#### Snippet
```java

        // Escape '.'
        mappings = mappings.replaceAll("\\.", "\\\\.");

        // Change '*' to '.*' to represent any match
```

### AssignmentToMethodParameter
Assignment to method parameter `mappings`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeResourceLibrary.java`
#### Snippet
```java

        // Change '*' to '.*' to represent any match
        mappings = mappings.replaceAll("\\*", ".*");

        // Split the mappings by changing ';' to '|'
```

### AssignmentToMethodParameter
Assignment to method parameter `mappings`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeResourceLibrary.java`
#### Snippet
```java

        // Split the mappings by changing ';' to '|'
        mappings = mappings.replaceAll(";", "|");

        return mappings;
```

### AssignmentToMethodParameter
Assignment to method parameter `radix`
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/SectionUniqueIdCounter.java`
#### Snippet
```java
        if (radix < Character.MIN_RADIX || radix > Character.MAX_RADIX)
        {
            radix = 10;
        }
        if (l == 0)
```

### AssignmentToMethodParameter
Assignment to method parameter `l`
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/SectionUniqueIdCounter.java`
#### Snippet
```java
            j = -l;
        }
        while ((l /= radix) != 0)
        {
            count++;
```

### AssignmentToMethodParameter
Assignment to method parameter `bufferConversion`
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/SectionUniqueIdCounter.java`
#### Snippet
```java
        if (bufferConversion.length < count)
        {
            bufferConversion = new char[count];
        }
        int finalCount = count;
```

### AssignmentToMethodParameter
Assignment to method parameter `mappings`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TagLibraryConfig.java`
#### Snippet
```java

            // Get rid of spaces
            mappings = mappings.replaceAll("\\s", "");

            // Escape '.'
```

### AssignmentToMethodParameter
Assignment to method parameter `mappings`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TagLibraryConfig.java`
#### Snippet
```java

            // Escape '.'
            mappings = mappings.replaceAll("\\.", "\\\\.");

            // Change '*' to '.*' to represent any match
```

### AssignmentToMethodParameter
Assignment to method parameter `mappings`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TagLibraryConfig.java`
#### Snippet
```java

            // Change '*' to '.*' to represent any match
            mappings = mappings.replaceAll("\\*", ".*");

            // Split the mappings by changing ';' to '|'
```

### AssignmentToMethodParameter
Assignment to method parameter `mappings`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TagLibraryConfig.java`
#### Snippet
```java

            // Split the mappings by changing ';' to '|'
            mappings = mappings.replaceAll(";", "|");

            return mappings;
```

### AssignmentToMethodParameter
Assignment to method parameter `prefix`
in `impl/src/main/java/org/apache/myfaces/view/facelets/util/Classpath.java`
#### Snippet
```java
            String[] split = prefix.split("/");

            prefix = _join(split, true);

            String end = _join(split, false);
```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `impl/src/main/java/org/apache/myfaces/view/facelets/util/Classpath.java`
#### Snippet
```java
                return;
            }
            url = new URL(urlString);

            _searchFromURL(result, prefix, suffix, url);
```

### AssignmentToMethodParameter
Assignment to method parameter `size`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TextUnit.java`
#### Snippet
```java
                        instructionBuffer.remove(i);
                        i--;
                        size--;
                    }
                }
```

### AssignmentToMethodParameter
Assignment to method parameter `systemId`
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigEntityResolver.java`
#### Snippet
```java
                if (systemId.startsWith("file:"))
                {
                    systemId = systemId.substring(7); // remove file://
                }
                stream = _externalContext.getResourceAsStream(systemId);
```

### AssignmentToMethodParameter
Assignment to method parameter `instance`
in `impl/src/main/java/org/apache/myfaces/config/FacesConfigurator.java`
#### Snippet
```java
            if (instance instanceof FacesWrapper)
            {
                instance = ((FacesWrapper)instance).getWrapped();
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `facesContext`
in `impl/src/main/java/org/apache/myfaces/context/SwitchAjaxExceptionHandlerWrapperImpl.java`
#### Snippet
```java
        if (_isAjaxRequest == null)
        {
            facesContext = (facesContext == null) ? FacesContext.getCurrentInstance() : facesContext;
            PartialViewContext pvc = facesContext.getPartialViewContext();
            if (pvc == null)
```

### AssignmentToMethodParameter
Assignment to method parameter `throwable`
in `impl/src/main/java/org/apache/myfaces/context/ExceptionHandlerImpl.java`
#### Snippet
```java
            }
            
            throwable = throwable.getCause();
        }
        
```

### AssignmentToMethodParameter
Assignment to method parameter `throwable`
in `impl/src/main/java/org/apache/myfaces/context/AjaxExceptionHandlerImpl.java`
#### Snippet
```java
            }
            
            throwable = throwable.getCause();
        }
        
```

### AssignmentToMethodParameter
Assignment to method parameter `facesContext`
in `impl/src/main/java/org/apache/myfaces/context/MyFacesExceptionHandlerWrapperImpl.java`
#### Snippet
```java
            if (facesContext == null)
            {
                facesContext = FacesContext.getCurrentInstance();
            }
            WebConfigProvider webConfigProvider = WebConfigProviderFactory.getWebConfigProviderFactory(
```

### AssignmentToMethodParameter
Assignment to method parameter `path`
in `impl/src/main/java/org/apache/myfaces/resource/ExternalContextContractResourceLoader.java`
#### Snippet
```java
        if (getPrefix() != null)
        {
            path = getPrefix() + '/' + path;
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `path`
in `impl/src/main/java/org/apache/myfaces/resource/ExternalContextContractResourceLoader.java`
#### Snippet
```java
        String libraryVersion = null;
        Set<String> libraryPaths = this.getResourcePaths(contractName, path);
        path = getPrefix() + '/' + path;
        if (null != libraryPaths && !libraryPaths.isEmpty())
        {
```

### AssignmentToMethodParameter
Assignment to method parameter `updatedComponents`
in `impl/src/main/java/org/apache/myfaces/context/servlet/PartialViewContextImpl.java`
#### Snippet
```java
                        if (updatedComponents == null)
                        {
                            updatedComponents = new ArrayList<UIComponent>();
                        }
                        updatedComponents.add(component);
```

### AssignmentToMethodParameter
Assignment to method parameter `path`
in `impl/src/main/java/org/apache/myfaces/resource/ExternalContextResourceLoader.java`
#### Snippet
```java
        if (getPrefix() != null)
        {
            path = getPrefix() + '/' + path;
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `path`
in `impl/src/main/java/org/apache/myfaces/resource/ExternalContextResourceLoader.java`
#### Snippet
```java
        String libraryVersion = null;
        Set<String> libraryPaths = this.getResourcePaths(path);
        path = getPrefix() + '/' + path;
        if (null != libraryPaths && !libraryPaths.isEmpty())
        {
```

### AssignmentToMethodParameter
Assignment to method parameter `basePath`
in `impl/src/main/java/org/apache/myfaces/resource/ClassLoaderResourceLoaderIterator.java`
#### Snippet
```java
            if (basePath.endsWith("/"))
            {
                basePath = basePath.substring(0, basePath.length()-1);
            }

```

### AssignmentToMethodParameter
Assignment to method parameter `url`
in `impl/src/main/java/org/apache/myfaces/resource/ClassLoaderResourceLoaderIterator.java`
#### Snippet
```java
            else if (isJarResourceProtocol(url.getProtocol()))
            {
                url = getClassLoader().getResource(basePath);

                if (url != null)
```

### AssignmentToMethodParameter
Assignment to method parameter `baseUrl`
in `impl/src/main/java/org/apache/myfaces/context/servlet/ServletExternalContextImpl.java`
#### Snippet
```java
        {
            fragment = baseUrl.substring(index+1);
            baseUrl = baseUrl.substring(0,index);
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `baseUrl`
in `impl/src/main/java/org/apache/myfaces/context/servlet/ServletExternalContextImpl.java`
#### Snippet
```java
        {
            queryString = baseUrl.substring(index + 1);
            baseUrl = baseUrl.substring(0, index);
            String[] nameValuePairs = queryString.split(URL_PARAM_SEPERATOR);
            for (int i = 0; i < nameValuePairs.length; i++)
```

### AssignmentToMethodParameter
Assignment to method parameter `windowId`
in `impl/src/main/java/org/apache/myfaces/lifecycle/clientwindow/CODIClientSideWindow.java`
#### Snippet
```java
            if (windowId == null)
            {
                windowId = UNINITIALIZED_WINDOW_ID_VALUE;
            }

```

### AssignmentToMethodParameter
Assignment to method parameter `expression`
in `impl/src/main/java/org/apache/myfaces/component/search/SearchExpressionHandlerImpl.java`
#### Snippet
```java
        if (expression == null)
        {
            expression = "";
        }
        else
```

### AssignmentToMethodParameter
Assignment to method parameter `expression`
in `impl/src/main/java/org/apache/myfaces/component/search/SearchExpressionHandlerImpl.java`
#### Snippet
```java
        else
        {
            expression = expression.trim();
        }
        
```

### AssignmentToMethodParameter
Assignment to method parameter `expressions`
in `impl/src/main/java/org/apache/myfaces/component/search/SearchExpressionHandlerImpl.java`
#### Snippet
```java
        if (expressions == null)
        {
            expressions = "";
        }
        else
```

### AssignmentToMethodParameter
Assignment to method parameter `expressions`
in `impl/src/main/java/org/apache/myfaces/component/search/SearchExpressionHandlerImpl.java`
#### Snippet
```java
        else
        {
            expressions = expressions.trim();
        }
        
```

### AssignmentToMethodParameter
Assignment to method parameter `topExpression`
in `impl/src/main/java/org/apache/myfaces/component/search/SearchExpressionHandlerImpl.java`
#### Snippet
```java
        if (topExpression == null)
        {
            topExpression = "";
        }
        else
```

### AssignmentToMethodParameter
Assignment to method parameter `topExpression`
in `impl/src/main/java/org/apache/myfaces/component/search/SearchExpressionHandlerImpl.java`
#### Snippet
```java
        else
        {
            topExpression = topExpression.trim();
        }
        
```

### AssignmentToMethodParameter
Assignment to method parameter `topExpression`
in `impl/src/main/java/org/apache/myfaces/component/search/SearchExpressionHandlerImpl.java`
#### Snippet
```java
        }

        topExpression = topExpression.trim();
        
        FacesContext facesContext = searchExpressionContext.getFacesContext();
```

### AssignmentToMethodParameter
Assignment to method parameter `expression`
in `impl/src/main/java/org/apache/myfaces/component/search/SearchExpressionHandlerImpl.java`
#### Snippet
```java
        if (expression == null)
        {
            expression = "";
        }
        else
```

### AssignmentToMethodParameter
Assignment to method parameter `expression`
in `impl/src/main/java/org/apache/myfaces/component/search/SearchExpressionHandlerImpl.java`
#### Snippet
```java
        else
        {
            expression = expression.trim();
        }
        
```

### AssignmentToMethodParameter
Assignment to method parameter `topExpression`
in `impl/src/main/java/org/apache/myfaces/component/search/SearchExpressionHandlerImpl.java`
#### Snippet
```java
        }

        topExpression = topExpression.trim();
        
        FacesContext facesContext = searchExpressionContext.getFacesContext();
```

### AssignmentToMethodParameter
Assignment to method parameter `expressions`
in `impl/src/main/java/org/apache/myfaces/component/search/SearchExpressionHandlerImpl.java`
#### Snippet
```java
        if (expressions == null)
        {
            expressions = "";
        }
        else
```

### AssignmentToMethodParameter
Assignment to method parameter `expressions`
in `impl/src/main/java/org/apache/myfaces/component/search/SearchExpressionHandlerImpl.java`
#### Snippet
```java
        else
        {
            expressions = expressions.trim();
        }
        
```

### AssignmentToMethodParameter
Assignment to method parameter `component`
in `impl/src/main/java/org/apache/myfaces/component/search/SearchExpressionHandlerImpl.java`
#### Snippet
```java
            }

            component = component.getParent();
        } while (component != null);
        return null;
```

### AssignmentToMethodParameter
Assignment to method parameter `characterEncoding`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlRenderKitImpl.java`
#### Snippet
```java
        if (characterEncoding == null)
        {
            characterEncoding = ContentTypeUtils.DEFAULT_CHAR_ENCODING;
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `submittedValue`
in `impl/src/main/java/org/apache/myfaces/renderkit/RendererUtils.java`
#### Snippet
```java
        if (submittedValue != null && !(submittedValue instanceof String))
        {
            submittedValue = submittedValue.toString();
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `converter`
in `impl/src/main/java/org/apache/myfaces/renderkit/RendererUtils.java`
#### Snippet
```java
        if (converter != null)
        {
            converter = new PassThroughAsStringConverter(converter);
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `submittedValue`
in `impl/src/main/java/org/apache/myfaces/renderkit/RendererUtils.java`
#### Snippet
```java
        {
            //Replace "" by null value
            submittedValue = null;
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `wrapSpan`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessageRendererBase.java`
#### Snippet
```java
            behaviors = ((ClientBehaviorHolder) message).getClientBehaviors();
            // If there is a behavior registered, force wrapSpan
            wrapSpan = wrapSpan || !behaviors.isEmpty();
        }
        
```

### AssignmentToMethodParameter
Assignment to method parameter `itemNum`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlCheckboxRendererBase.java`
#### Snippet
```java
            for (SelectItem groupSelectItem : selectItems)
            {
                itemNum = renderGroupOrItemCheckbox(facesContext, selectMany, groupSelectItem, useSubmittedValues,
                                                    lookupSet, converter, usingTable, itemNum);
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `itemNum`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlCheckboxRendererBase.java`
#### Snippet
```java
            
            // we rendered one checkbox --> increment itemNum
            itemNum++;
        }
        
```

### AssignmentToMethodParameter
Assignment to method parameter `firstParameter`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlLinkRendererBase.java`
#### Snippet
```java
            Object value = param.getValue();
            addParameterToHref(name, value, hrefBuf, firstParameter, charEncoding, myfacesConfig.isStrictXhtmlLinks());
            firstParameter = false;
        }
    }
```

### AssignmentToMethodParameter
Assignment to method parameter `resource`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/ResourceUtils.java`
#### Snippet
```java
            else if (resource instanceof FacesWrapper)
            {
                resource = (Resource) ((FacesWrapper)resource).getWrapped();
            }
            else
```

### AssignmentToMethodParameter
Assignment to method parameter `resource`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/ResourceUtils.java`
#### Snippet
```java
            else
            {
                resource = null;
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `itemNum`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlRadioRendererBase.java`
#### Snippet
```java
            for (SelectItem groupSelectItem : selectItems)
            { 
                itemNum = renderGroupOrItemRadio(facesContext, selectOne, groupSelectItem, currentValue, 
                                                 converter, usingTable, itemNum);
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `itemNum`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlRadioRendererBase.java`
#### Snippet
```java
            
            // we rendered one radio --> increment itemNum
            itemNum++;
        }
        return itemNum;
```

### AssignmentToMethodParameter
Assignment to method parameter `styleClass`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTableRendererBase.java`
#### Snippet
```java
                if (isHeader && column.getHeaderClass()!=null)
                {
                    styleClass = column.getHeaderClass();
                }
                else if (!isHeader && column.getFooterClass()!=null)
```

### AssignmentToMethodParameter
Assignment to method parameter `styleClass`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTableRendererBase.java`
#### Snippet
```java
                else if (!isHeader && column.getFooterClass()!=null)
                {
                    styleClass = column.getFooterClass();
                }
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `styleClass`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTableRendererBase.java`
#### Snippet
```java
                    if (headerClass != null)
                    {
                        styleClass = (String) headerClass;
                    }
                }
```

### AssignmentToMethodParameter
Assignment to method parameter `styleClass`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTableRendererBase.java`
#### Snippet
```java
                    if (footerClass != null)
                    {
                        styleClass = (String) footerClass;
                    }
                }
```

### AssignmentToMethodParameter
Assignment to method parameter `offset`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/HTMLEncoder.java`
#### Snippet
```java
            return;
        }
        offset = Math.max(0, offset);
        int realLength = Math.min(length, string.length - offset);

```

### AssignmentToMethodParameter
Assignment to method parameter `sourceBaseType`
in `impl/src/main/java/org/apache/myfaces/application/ApplicationImplEventManager.java`
#### Snippet
```java
        if (sourceBaseType == null)
        {
            sourceBaseType = source.getClass();
        }
        
```

### AssignmentToMethodParameter
Assignment to method parameter `event`
in `impl/src/main/java/org/apache/myfaces/application/ApplicationImplEventManager.java`
#### Snippet
```java
                    if (event == null)
                    {
                        event = createEvent(systemEventClass, facesContext, source);
                    }

```

### AssignmentToMethodParameter
Assignment to method parameter `event`
in `impl/src/main/java/org/apache/myfaces/application/ApplicationImplEventManager.java`
#### Snippet
```java
                if (event == null)
                {
                    event = createEvent(systemEventClass, facesContext, source);
                }

```

### AssignmentToMethodParameter
Assignment to method parameter `event`
in `impl/src/main/java/org/apache/myfaces/application/ApplicationImplEventManager.java`
#### Snippet
```java
                if (event == null)
                {
                    event = createEvent(systemEventClass, facesContext, source);
                }

```

### AssignmentToMethodParameter
Assignment to method parameter `execute`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/AjaxScriptBuilder.java`
#### Snippet
```java
                    // set the clientId of the component so the behavior can be decoded later,
                    // otherwise the behavior will fail
                    execute = component.getClientId(context);
                }
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `prefix`
in `impl/src/main/java/org/apache/myfaces/application/ViewIdSupport.java`
#### Snippet
```java
            // in order to prevent an infinite loop in uri.startsWith(-emptyString-).
            // Furthermore a prefix of "//" is just another double slash prevention.
            prefix = "//";
        }
        else
```

### AssignmentToMethodParameter
Assignment to method parameter `prefix`
in `impl/src/main/java/org/apache/myfaces/application/ViewIdSupport.java`
#### Snippet
```java
        {
            // need to make sure its really /faces/* and not /facesPage.xhtml
            prefix = prefix + '/'; 
        }
        
```

### AssignmentToMethodParameter
Assignment to method parameter `target`
in `impl/src/main/java/org/apache/myfaces/renderkit/ErrorPageWriter.java`
#### Snippet
```java
                        }

                        target = parent;
                        parent = target.getParent();
                    }
```

### AssignmentToMethodParameter
Assignment to method parameter `ex`
in `impl/src/main/java/org/apache/myfaces/renderkit/ErrorPageWriter.java`
#### Snippet
```java
        while (ex.getCause() != null)
        {
            ex = ex.getCause();
            if (ex instanceof ContextAware)
            {
```

### AssignmentToMethodParameter
Assignment to method parameter `state`
in `impl/src/main/java/org/apache/myfaces/application/viewstate/SerializedViewCollection.java`
#### Snippet
```java
        if (state == null)
        {
            state = EMPTY_STATES;
        }
        else if (state instanceof Object[] &&
```

### AssignmentToMethodParameter
Assignment to method parameter `state`
in `impl/src/main/java/org/apache/myfaces/application/viewstate/SerializedViewCollection.java`
#### Snippet
```java
            // The generated state can be considered zero, set it as null
            // into the map.
            state = null;
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `previousRestoredKey`
in `impl/src/main/java/org/apache/myfaces/application/viewstate/SerializedViewCollection.java`
#### Snippet
```java
                    // set the precence. In that way, we ensure the precedence map will always
                    // have valid keys.
                    previousRestoredKey = null;
                }
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `key`
in `impl/src/main/java/org/apache/myfaces/application/viewstate/SerializedViewCollection.java`
#### Snippet
```java
        while (_keys.size() > views)
        {
            key = _keys.remove(0);
            if (maxCount != null && maxCount > 0)
            {
```

### AssignmentToMethodParameter
Assignment to method parameter `resourceName`
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
            // does not have any meaning (with and without should point to the 
            // same resource).
            resourceName = resourceName.substring(1);
        }        
        if (!ResourceValidationUtils.isValidResourceName(resourceName))
```

### AssignmentToMethodParameter
Assignment to method parameter `contentType`
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
        {
            //Resolve contentType using ExternalContext.getMimeType
            contentType = facesContext.getExternalContext().getMimeType(resourceName);
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `resourceName`
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
            // does not have any meaning (with and without should point to the 
            // same resource).
            resourceName = resourceName.substring(1);
        }
        
```

### AssignmentToMethodParameter
Assignment to method parameter `initParam`
in `impl/src/main/java/org/apache/myfaces/application/viewstate/StateUtils.java`
#### Snippet
```java
        if (initParam == null)
        {
            initParam = DEFAULT_ALGORITHM;
        }
        
```

### AssignmentToMethodParameter
Assignment to method parameter `initParam`
in `impl/src/main/java/org/apache/myfaces/application/viewstate/StateUtils.java`
#### Snippet
```java
        if (initParam == null)
        {
            initParam = DEFAULT_MAC_ALGORITHM;
        }
        
```

### AssignmentToMethodParameter
Assignment to method parameter `t`
in `test/src/main/java/org/apache/myfaces/test/mock/MockExceptionHandler.java`
#### Snippet
```java
            }

            t = t.getCause();
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `baseUrl`
in `test/src/main/java/org/apache/myfaces/test/mock/MockExternalContext20.java`
#### Snippet
```java
        {
            fragment = baseUrl.substring(index + 1);
            baseUrl = baseUrl.substring(0, index);
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `baseUrl`
in `test/src/main/java/org/apache/myfaces/test/mock/MockExternalContext20.java`
#### Snippet
```java
        {
            queryString = baseUrl.substring(index + 1);
            baseUrl = baseUrl.substring(0, index);
            String[] nameValuePairs = queryString.split(URL_PARAM_SEPERATOR);
            for (int i = 0; i < nameValuePairs.length; i++)
```

### AssignmentToMethodParameter
Assignment to method parameter `viewId`
in `test/src/main/java/org/apache/myfaces/test/mock/MockDefaultViewDeclarationLanguage.java`
#### Snippet
```java
            try
            {
                viewId = calculateViewId(context, viewId);
                
                Application application = context.getApplication();
```

### AssignmentToMethodParameter
Assignment to method parameter `submitting`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/ClientBehaviorRendererUtils.java`
#### Snippet
```java
            if (!submitting)
            {
                submitting = clientBehavior.getHints().contains(ClientBehaviorHint.SUBMITTING);
            }
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `event`
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication20.java`
#### Snippet
```java
                if (constructor != null)
                {
                    event = constructor.newInstance(source);
                }

```

### AssignmentToMethodParameter
Assignment to method parameter `event`
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication20.java`
#### Snippet
```java
                    // construct the event, passing source as the argument to the one-argument constructor that takes
                    // an Object. This same event instance must be passed to all listener instances.
                    event = _createEvent(systemEventClass, source, event);

                    // Call SystemEvent.isAppropriateListener(jakarta.faces.event.FacesListener), passing the listener
```

### AssignmentToMethodParameter
Assignment to method parameter `event`
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication20.java`
#### Snippet
```java
            if (source != null && _sourceClassMap != null)
            {
                event = _traverseListenerList(_sourceClassMap.get(classSource),
                        systemEventClass, source, event);
            }
```

### AssignmentToMethodParameter
Assignment to method parameter `key`
in `test/src/main/java/org/apache/myfaces/test/mock/MockSubKeyMap.java`
#### Snippet
```java
    public V put(String key, V value)
    {
        key = _getBaseKey(key);
        return (V) _base.put(key, value);
    }
```

### AssignmentToMethodParameter
Assignment to method parameter `key`
in `test/src/main/java/org/apache/myfaces/test/mock/MockSubKeyMap.java`
#### Snippet
```java
    public V get(Object key)
    {
        key = _getBaseKey(key);
        return (V) _base.get(key);
    }
```

### AssignmentToMethodParameter
Assignment to method parameter `key`
in `test/src/main/java/org/apache/myfaces/test/mock/MockSubKeyMap.java`
#### Snippet
```java
    public V remove(Object key)
    {
        key = _getBaseKey(key);
        return (V) _base.remove(key);
    }
```

### AssignmentToMethodParameter
Assignment to method parameter `path`
in `test/src/main/java/org/apache/myfaces/test/mock/resource/MockExternalContextResourceLoader.java`
#### Snippet
```java
        if (getPrefix() != null)
        {
            path = getPrefix() + '/' + path;
        }

```

### AssignmentToMethodParameter
Assignment to method parameter `path`
in `test/src/main/java/org/apache/myfaces/test/mock/resource/MockExternalContextResourceLoader.java`
#### Snippet
```java
        String libraryVersion = null;
        Set<String> libraryPaths = this.getResourcePaths(path);
        path = getPrefix() + '/' + path;
        if (null != libraryPaths && !libraryPaths.isEmpty())
        {
```

### AssignmentToMethodParameter
Assignment to method parameter `contentType`
in `test/src/main/java/org/apache/myfaces/test/mock/resource/MockResourceHandler.java`
#### Snippet
```java
        {
            //Resolve contentType using ExternalContext.getMimeType
            contentType = FacesContext.getCurrentInstance()
                    .getExternalContext().getMimeType(resourceName);
        }
```

### AssignmentToMethodParameter
Assignment to method parameter `startElementWritten`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/HtmlRendererUtils.java`
#### Snippet
```java
            {
                writer.startElement(elementName, component);
                startElementWritten = true;
            }
            renderHTMLAttribute(writer, attrName, attrName, value);
```

## RuleId[ruleID=SynchronizationOnLocalVariableOrMethodParameter]
### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `factoryClassNames`
in `api/src/main/java/jakarta/faces/FactoryFinder.java`
#### Snippet
```java
        }

        synchronized (factoryClassNames)
        {
            List<String> classNameList = factoryClassNames.computeIfAbsent(factoryName, k -> new ArrayList<>());
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `factoryClassNames`
in `api/src/main/java/jakarta/faces/FactoryFinder.java`
#### Snippet
```java
        Object factory;
        Object injectionProvider;
        synchronized (factoryClassNames)
        {
            beanEntryStorage = (List) factoryMap.computeIfAbsent(INJECTED_BEAN_STORAGE_KEY,
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `factoryClassNames`
in `api/src/main/java/jakarta/faces/FactoryFinder.java`
#### Snippet
```java
        {
            injectionProvider = getInjectionProvider();
            synchronized (factoryClassNames)
            {
                factoryMap.put(INJECTION_PROVIDER_INSTANCE, injectionProvider);
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `factoryClassNames`
in `api/src/main/java/jakarta/faces/FactoryFinder.java`
#### Snippet
```java
            classNames.iterator(), classLoader, injectionProvider, beanEntryStorage);

        synchronized (factoryClassNames)
        {
            // check if someone else already installed the factory
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `instanceInfo`
in `impl/src/main/java/org/apache/myfaces/cdi/util/ContextualStorage.java`
#### Snippet
```java
            }

            synchronized (instanceInfo)
            {
                T instance = instanceInfo.getContextualInstance();
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `declaredFieldBeansMap`
in `impl/src/main/java/org/apache/myfaces/spi/impl/ResourceAnnotationInjectionProvider.java`
#### Snippet
```java
        {
            fields = clazz.getDeclaredFields();
            synchronized(declaredFieldBeansMap)
            {
                declaredFieldBeansMap.put(clazz, fields);
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `declaredMethodBeansMap`
in `impl/src/main/java/org/apache/myfaces/spi/impl/NoInjectionAnnotationInjectionProvider.java`
#### Snippet
```java
            methods = clazz.getDeclaredMethods();
            Map<Class,Method[]> declaredMethodBeansMap = getDeclaredMethodBeansMap();
            synchronized(declaredMethodBeansMap)
            {
                declaredMethodBeansMap.put(clazz, methods);
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `declaredMethodBeansMap`
in `impl/src/main/java/org/apache/myfaces/spi/impl/NoInjectionAnnotationInjectionProvider.java`
#### Snippet
```java
        {
            methods = clazz.getDeclaredMethods();
            synchronized(declaredMethodBeansMap)
            {
                declaredMethodBeansMap.put(clazz, methods);
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on method parameter `session`
in `impl/src/main/java/org/apache/myfaces/push/cdi/WebsocketSessionManager.java`
#### Snippet
```java
            if (isTomcatWebSocketBombed(session, e))
            {
                synchronized (session)
                {
                    send(session, text, results, retries + 1);
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `str`
in `impl/src/main/java/org/apache/myfaces/util/lang/StringCharArrayAccessor.java`
#### Snippet
```java
            // (JSR-133/JMM, "9.1.1 Post-Construction Modification of Final Fields")
            // it was a bit unclear for me if this could ever happen in a single thread
            synchronized (str)
            {
                valueField.set(str, charBuf);
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `str`
in `impl/src/main/java/org/apache/myfaces/util/lang/StringCharArrayAccessor.java`
#### Snippet
```java
                }
            }
            synchronized (str)
            {
                // safety check, just to be sure that setting the final fields went ok
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `sessionObj`
in `impl/src/main/java/org/apache/myfaces/util/token/CsrfSessionTokenFactoryRandom.java`
#### Snippet
```java
        Object sessionObj = externalContext.getSession(true);
        Integer sequence;
        synchronized (sessionObj) // are handled at the same time for the session
        {
            Map<String, Object> map = externalContext.getSessionMap();
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `metadata`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/MetaRulesetImpl.java`
#### Snippet
```java
            }

            synchronized(metadata)
            {
                // Use a synchronized block to ensure proper operation on concurrent use cases.
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `applicationMap`
in `impl/src/main/java/org/apache/myfaces/context/flash/FlashImpl.java`
#### Snippet
```java
            // synchronize the ApplicationMap to ensure that only
            // once instance of FlashImpl is created and stored in it.
            synchronized (applicationMap)
            {
                // check again, because first try was un-synchronized
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `sessionObj`
in `impl/src/main/java/org/apache/myfaces/application/viewstate/KeyFactoryRandom.java`
#### Snippet
```java
        Object sessionObj = externalContext.getSession(true);
        Integer sequence;
        synchronized (sessionObj) // are handled at the same time for the session
        {
            Map<String, Object> map = externalContext.getSessionMap();
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `sessionObj`
in `impl/src/main/java/org/apache/myfaces/application/viewstate/KeyFactorySecureRandom.java`
#### Snippet
```java
        Object sessionObj = externalContext.getSession(true);
        Integer sequence;
        synchronized (sessionObj) // are handled at the same time for the session
        {
            Map<String, Object> map = externalContext.getSessionMap();
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `cases`
in `impl/src/main/java/org/apache/myfaces/application/NavigationHandlerImpl.java`
#### Snippet
```java
            Collections.sort(wildcardPatterns, KeyComparator.INSTANCE);

            synchronized (cases)
            {
                // We do not really need this sychronization at all, but this
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `applicationMap`
in `test/src/main/java/org/apache/myfaces/test/mock/MockFlash.java`
#### Snippet
```java
        Flash flash = (Flash) applicationMap.get(FLASH_INSTANCE);

        synchronized (applicationMap)
        {
            if (flash == null)
```

### SynchronizationOnLocalVariableOrMethodParameter
Synchronization on local variable `session`
in `test/src/main/java/org/apache/myfaces/test/mock/MockFlash.java`
#### Snippet
```java
        // Synchronize on the session object to ensure that
        // we don't ever create two different caches
        synchronized (session)
        {
            map = (Map<String, Object>) external.getSessionMap().get(token);
```

## RuleId[ruleID=ReturnNull]
### ReturnNull
Return of `null`
in `api/src/main/java/org/apache/myfaces/core/api/shared/ComponentUtils.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `api/src/main/java/org/apache/myfaces/core/api/shared/ComponentUtils.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `api/src/main/java/org/apache/myfaces/core/api/shared/ComponentUtils.java`
#### Snippet
```java
        }

        return null;
    }
    
```

### ReturnNull
Return of `null`
in `api/src/main/java/org/apache/myfaces/core/api/shared/ComponentUtils.java`
#### Snippet
```java
        }
        
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `api/src/main/java/org/apache/myfaces/core/api/shared/MessageUtils.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `api/src/main/java/org/apache/myfaces/core/api/shared/MessageUtils.java`
#### Snippet
```java
    {
        String bundleName = facesContext.getApplication().getMessageBundle();
        return bundleName != null ? getBundle(facesContext, locale, bundleName) : null;
    }

```

### ReturnNull
Return of `null`
in `api/src/main/java/org/apache/myfaces/core/api/shared/MessageUtils.java`
#### Snippet
```java
                {
                    facesContext.getExternalContext().log("resource bundle " + bundleName + " could not be found");
                    return null;
                }
            }
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/_FactoryFinderProviderFactory.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `api/src/main/java/org/apache/myfaces/core/api/shared/SharedRendererUtils.java`
#### Snippet
```java
        if (expression == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `api/src/main/java/org/apache/myfaces/core/api/shared/SharedRendererUtils.java`
#### Snippet
```java
        if (valueType == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `api/src/main/java/org/apache/myfaces/core/api/shared/SharedRendererUtils.java`
#### Snippet
```java
        if (Object.class.equals(valueType))
        {
            return null; //There is no converter for Object class
        }

```

### ReturnNull
Return of `null`
in `api/src/main/java/org/apache/myfaces/core/api/shared/SharedRendererUtils.java`
#### Snippet
```java
        {
            log(facesContext, "No Converter for type " + valueType.getName() + " found", e);
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `api/src/main/java/org/apache/myfaces/core/api/shared/SharedRendererUtils.java`
#### Snippet
```java
                if (String.class.equals(selectItemsType))
                {
                    return null;
                }
                
```

### ReturnNull
Return of `null`
in `api/src/main/java/org/apache/myfaces/core/api/shared/lang/PropertyDescriptorUtils.java`
#### Snippet
```java
            if (propertyDescriptors == null || propertyDescriptors.length == 0)
            {
                return null;
            }
            
```

### ReturnNull
Return of `null`
in `api/src/main/java/org/apache/myfaces/core/api/shared/lang/LocaleUtils.java`
#### Snippet
```java
        if (str == null)
        {
            return null;
        }
        int len = str.length();
```

### ReturnNull
Return of `null`
in `api/src/main/java/org/apache/myfaces/core/api/shared/lang/LocaleUtils.java`
#### Snippet
```java
        if (len != 2 && len != 5 && len < 7)
        {
            return null;
        }
        char ch0 = str.charAt(0);
```

### ReturnNull
Return of `null`
in `api/src/main/java/org/apache/myfaces/core/api/shared/lang/LocaleUtils.java`
#### Snippet
```java
        if (ch0 < 'a' || ch0 > 'z' || ch1 < 'a' || ch1 > 'z')
        {
            return null;
        }
        if (len == 2)
```

### ReturnNull
Return of `null`
in `api/src/main/java/org/apache/myfaces/core/api/shared/lang/LocaleUtils.java`
#### Snippet
```java
            if (str.charAt(2) != '_')
            {
                return null;
            }
            char ch3 = str.charAt(3);
```

### ReturnNull
Return of `null`
in `api/src/main/java/org/apache/myfaces/core/api/shared/lang/LocaleUtils.java`
#### Snippet
```java
            if (ch3 < 'A' || ch3 > 'Z' || ch4 < 'A' || ch4 > 'Z')
            {
                return null;
            }
            if (len == 5)
```

### ReturnNull
Return of `null`
in `api/src/main/java/org/apache/myfaces/core/api/shared/lang/LocaleUtils.java`
#### Snippet
```java
                if (str.charAt(5) != '_')
                {
                    return null;
                }
                return new Locale(str.substring(0, 2), str.substring(3, 5), str.substring(6));
```

### ReturnNull
Return of `null`
in `api/src/main/java/org/apache/myfaces/core/api/shared/lang/LocaleUtils.java`
#### Snippet
```java
        if (str == null)
        {
            return null;
        }
        int len = str.length();
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/view/ViewDeclarationLanguage.java`
#### Snippet
```java
                                   Map<String,Object> attributes)
    {
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/view/ViewDeclarationLanguage.java`
#### Snippet
```java
                                                      String viewId)
    {
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `api/src/main/java/org/apache/myfaces/core/api/shared/lang/ClassUtils.java`
#### Snippet
```java
        if (type == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `api/src/main/java/org/apache/myfaces/core/api/shared/lang/ClassUtils.java`
#### Snippet
```java
        if (type == null)
        {
            return null;
        }
        return newInstance(simpleClassForName(type));
```

### ReturnNull
Return of `null`
in `api/src/main/java/org/apache/myfaces/core/api/shared/lang/ClassUtils.java`
#### Snippet
```java
        if (c == null)
        {
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `api/src/main/java/org/apache/myfaces/core/api/shared/lang/ClassUtils.java`
#### Snippet
```java
        if (value == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `api/src/main/java/org/apache/myfaces/core/api/shared/lang/ClassUtils.java`
#### Snippet
```java
        if (s == null)
        {
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `api/src/main/java/org/apache/myfaces/core/api/shared/lang/ClassUtils.java`
#### Snippet
```java
        if (name == null || name.isEmpty())
        {
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `api/src/main/java/org/apache/myfaces/core/api/shared/lang/ClassUtils.java`
#### Snippet
```java
        }
        
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `api/src/main/java/org/apache/myfaces/core/api/shared/lang/ClassUtils.java`
#### Snippet
```java
        if (value == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/view/facelets/BehaviorHandler.java`
#### Snippet
```java
        if (event == null)
        {
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/view/facelets/TagAttributes.java`
#### Snippet
```java
    public Tag getTag()
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/view/facelets/ComponentHandler.java`
#### Snippet
```java
    public UIComponent createComponent(FaceletContext ctx)
    {
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/FactoryFinder.java`
#### Snippet
```java
        {
        }
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/view/facelets/TagAttribute.java`
#### Snippet
```java
    public Tag getTag()
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/model/ScalarDataModel.java`
#### Snippet
```java
        if (_data == null)
        {
            return null;
        }
        if (!isRowAvailable())
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/model/ArrayDataModel.java`
#### Snippet
```java
        if (_data == null)
        {
            return null;
        }
        if (!isRowAvailable())
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/model/ListDataModel.java`
#### Snippet
```java
        if (_data == null)
        {
            return null;
        }
        if (!isRowAvailable())
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/model/CollectionDataModel.java`
#### Snippet
```java
        if (_data == null)
        {
            return null;
        }
        if (!isRowAvailable())
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/render/ResponseStateManager.java`
#### Snippet
```java
    public String getViewState(FacesContext context, Object state)
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/render/ResponseStateManager.java`
#### Snippet
```java
    public String getCryptographicallyStrongTokenFromSession(FacesContext context)
    {
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/render/ResponseStateManager.java`
#### Snippet
```java
    public Object getState(FacesContext context, String viewId)
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/render/ClientBehaviorRenderer.java`
#### Snippet
```java
    {
        // default implementation returns null per the javadoc
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/model/IterableDataModel.java`
#### Snippet
```java
        if (_iterable == null)
        {
            return null;
        }
        if (!isRowAvailable())
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/convert/ByteConverter.java`
#### Snippet
```java
        if (value == null)
        {
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/convert/ByteConverter.java`
#### Snippet
```java
        if (value.length() < 1)
        {
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/convert/FloatConverter.java`
#### Snippet
```java
        if (value == null)
        {
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/convert/FloatConverter.java`
#### Snippet
```java
        if (value.length() < 1)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/convert/CharacterConverter.java`
#### Snippet
```java
        if (value == null)
        {
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/convert/CharacterConverter.java`
#### Snippet
```java
        if (value.length() < 1)
        {
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/convert/EnumConverter.java`
#### Snippet
```java
        if (value == null)
        {
            return null;
        }
        value = value.trim();
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/convert/EnumConverter.java`
#### Snippet
```java
        if (value.length() == 0)
        {
            return null;
        }
        checkTargetClass(facesContext, uiComponent, value);
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/convert/EnumConverter.java`
#### Snippet
```java
            return targetClass;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/convert/LongConverter.java`
#### Snippet
```java
        if (value == null)
        {
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/convert/LongConverter.java`
#### Snippet
```java
        if (value.length() < 1)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/model/ResultSetDataModel.java`
#### Snippet
```java
            if (!containsKey(key))
            {
                return null;
            }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/model/ResultSetDataModel.java`
#### Snippet
```java
        if (_resultSet == null)
        {
            return null;
        }
        else if (!isRowAvailable())
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/convert/BigIntegerConverter.java`
#### Snippet
```java
        if (value == null)
        {
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/convert/BigIntegerConverter.java`
#### Snippet
```java
        if (value.length() < 1)
        {
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/convert/BigDecimalConverter.java`
#### Snippet
```java
        if (value == null)
        {
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/convert/BigDecimalConverter.java`
#### Snippet
```java
        if (value.length() < 1)
        {
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/convert/ShortConverter.java`
#### Snippet
```java
        if (value == null)
        {
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/convert/ShortConverter.java`
#### Snippet
```java
        if (value.length() < 1)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/convert/DoubleConverter.java`
#### Snippet
```java
        if (value == null)
        {
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/convert/DoubleConverter.java`
#### Snippet
```java
        if (value.length() < 1)
        {
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/convert/IntegerConverter.java`
#### Snippet
```java
        if (value == null)
        {
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/convert/IntegerConverter.java`
#### Snippet
```java
        if (value.length() < 1)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/convert/BooleanConverter.java`
#### Snippet
```java
        if (value == null)
        {
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/convert/BooleanConverter.java`
#### Snippet
```java
        if (value.length() < 1)
        {
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/UIOutput.java`
#### Snippet
```java
            {
                //No values
                return null;
            }
            else if (parentSaved != null && nullDelta)
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/UIOutput.java`
#### Snippet
```java
            return (Converter) expression.getValue(getFacesContext().getELContext());
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/convert/NumberConverter.java`
#### Snippet
```java
            return values;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/convert/NumberConverter.java`
#### Snippet
```java
        if (value == null)
        {
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/convert/NumberConverter.java`
#### Snippet
```java
        if (value.length() < 1)
        {
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/convert/NumberConverter.java`
#### Snippet
```java
        }
        
        return null;
    }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/UIForm.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/UIForm.java`
#### Snippet
```java
            return location.toString();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/UIComponent.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/UIComponent.java`
#### Snippet
```java
        } while (component != null);

        return null;
    }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/UIComponent.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/UIComponent.java`
#### Snippet
```java
        if (component == null)
        {
            return null;
        }
        UIComponent parent = component;
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/UIComponent.java`
#### Snippet
```java
            }
        } while (parent != null);
        return null;
    }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/convert/DateTimeConverter.java`
#### Snippet
```java
            return ZonedDateTime::from;
        }
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/convert/DateTimeConverter.java`
#### Snippet
```java
                }
            }
            return null;
        }
        else
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/convert/DateTimeConverter.java`
#### Snippet
```java
            return values;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/convert/DateTimeConverter.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/_UISelectItems.java`
#### Snippet
```java
    public String getVar()
    {
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/_UISelectItems.java`
#### Snippet
```java
    public String getItemDescription()
    {
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/_UISelectItems.java`
#### Snippet
```java
    public Object getItemValue()
    {
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/_UISelectItems.java`
#### Snippet
```java
    public String getItemLabel()
    {
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/UIViewParameter.java`
#### Snippet
```java
        {
            // No value expression, return null.
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/UIViewParameter.java`
#### Snippet
```java
            {
                // No converter, no value, return null.
                return null;
            }
            
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/UIInput.java`
#### Snippet
```java

            //_validatorList instances once is created never changes, we can return null
            return null;
        }
        else
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/UIInput.java`
#### Snippet
```java
            {
                //No values
                return null;
            }
            
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/UISelectMany.java`
#### Snippet
```java
            {
                // unsupported type for iteration
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/UISelectMany.java`
#### Snippet
```java
        else if (submittedValue == null)
        {
            return null;
        }
        else if (submittedValue instanceof String[])
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/_DeltaStateHelper.java`
#### Snippet
```java
    public Object getTransient(Object key)
    {
        return (_transientState == null) ? null : _transientState.get(key);
    }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/_DeltaStateHelper.java`
#### Snippet
```java
            _transientState.clear();
        }
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/_DeltaStateHelper.java`
#### Snippet
```java
            return expression.getValue(_component.getFacesContext().getELContext());
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/_DeltaStateHelper.java`
#### Snippet
```java
        if (serializableMap == null || serializableMap.isEmpty())
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/_DeltaStateHelper.java`
#### Snippet
```java
        if (valueOrKey == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/_DeltaStateHelper.java`
#### Snippet
```java
            return defaultValueSupplier.get();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/_ArrayMap.java`
#### Snippet
```java
        if (array == null)
        {
            return null;
        }
        ArrayList<Object> keyList = new ArrayList<Object>();
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/_ArrayMap.java`
#### Snippet
```java
        if (array == null)
        {
            return null;
        }
        ArrayList<Object> valueList = new ArrayList<Object>();
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/_ArrayMap.java`
#### Snippet
```java
        if (size == 0)
        {
            return null;
        }
        ArrayList<K> keyList = new ArrayList<K>();
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/_ArrayMap.java`
#### Snippet
```java
        {
            // Should never reach here
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/_ArrayMap.java`
#### Snippet
```java
                if (targetKey == null)
                {
                    return null;
                }
                else if (targetKey.equals(key))
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/_ArrayMap.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/_ArrayMap.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/_EventListenerWrapper.java`
#### Snippet
```java
            if ((listenerCapability & LISTENER_TYPE_COMPONENT) != 0)
            {
                return null;
            }
            else if ((listenerCapability & LISTENER_TYPE_RENDERER) != 0)
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/_EventListenerWrapper.java`
#### Snippet
```java
            else if ((listenerCapability & LISTENER_TYPE_RENDERER) != 0)
            {
                return null;
            }
            else
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/_EventListenerWrapper.java`
#### Snippet
```java
                    if (listenerSaved == null)
                    {
                        return null;
                    }
                    return new Object[] { componentClass,
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/_EventListenerWrapper.java`
#### Snippet
```java
                {
                    //This is not necessary, because the instance is considered serializable!
                    return null;
                }
            }
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/_DeltaList.java`
#### Snippet
```java
            if (nullDelta)
            {
                return null;
            }
            return lst;
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/html/_HtmlGraphicImage.java`
#### Snippet
```java
  public String getLibrary()
  {
      return null;
  }
  
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/html/_HtmlGraphicImage.java`
#### Snippet
```java
  public String getName()
  {
      return null;
  }
}
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/UIComponentBase.java`
#### Snippet
```java
            return (String) expression.getValue(getFacesContext().getELContext());
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/UIComponentBase.java`
#### Snippet
```java
        if (attachedObject == null)
        {
            return null;
        }
        // StateHolder interface should take precedence over
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/UIComponentBase.java`
#### Snippet
```java
            if (holder.isTransient())
            {
                return null;
            }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/UIComponentBase.java`
#### Snippet
```java
    public UIComponent getFacet(String name)
    {
        return _facetMap == null ? null : _facetMap.get(name);
    }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/UIComponentBase.java`
#### Snippet
```java
        {
            // consult the transient property of this component. If true, just return null.
            return null;
        }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/UIComponentBase.java`
#### Snippet
```java
                if (nullDelta)
                {
                    return null;
                }
                return stateMap;
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/UIComponentBase.java`
#### Snippet
```java
        else
        {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/UIComponentBase.java`
#### Snippet
```java
            }
            //_facesListeners instances once is created never changes, we can return null
            return null;
        }
        else
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/UIComponentBase.java`
#### Snippet
```java
        if (stateObj == null)
        {
            return null;
        }
        if (stateObj instanceof _AttachedListStateWrapper)
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/UIComponentBase.java`
#### Snippet
```java
    {
        // if a default event exists for a component, this method is overriden thus assume null
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/UIComponentBase.java`
#### Snippet
```java
        if (expr.length() == 0)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/UIComponentBase.java`
#### Snippet
```java
        if (findBase == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/UIComponentBase.java`
#### Snippet
```java
               !((_capabilities & FLAG_IS_RENDERER_TYPE_SET) != 0))
            {
                return null;
            }
            
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/UIComponentBase.java`
#### Snippet
```java
                if (nullDelta)
                {
                    return null;
                }
                return stateMap;
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/UIComponentBase.java`
#### Snippet
```java
        else
        {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/UIComponentBase.java`
#### Snippet
```java
        // must be specified by the implementing component.
        // Returning null will force an error message in addClientBehavior.
        return null;
    }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/UIComponentBase.java`
#### Snippet
```java
        if (rendererType == null)
        {
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/UIComponentBase.java`
#### Snippet
```java
    {
        Location location = (Location) component.getAttributes().get(UIComponent.VIEW_LOCATION_KEY);
        return location == null ? null : location.toString();
    }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/behavior/ClientBehaviorBase.java`
#### Snippet
```java
        }
        
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/behavior/ClientBehaviorBase.java`
#### Snippet
```java
    public String getRendererType()
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/behavior/ClientBehaviorBase.java`
#### Snippet
```java
        
        // Otherwise, this method returns null.
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/behavior/AjaxBehavior.java`
#### Snippet
```java
            {
                //No values
                return null;
            }   
            return new Object[]{parentSaved, stateHelperSaved};
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/behavior/AjaxBehavior.java`
#### Snippet
```java
        else
        {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/behavior/_DeltaList.java`
#### Snippet
```java
            if (nullDelta)
            {
                return null;
            }
            return lst;
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/behavior/BehaviorBase.java`
#### Snippet
```java
        if (attachedObject == null)
        {
            return null;
        }
        // StateHolder interface should take precedence over
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/behavior/BehaviorBase.java`
#### Snippet
```java
            if (holder.isTransient())
            {
                return null;
            }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/behavior/BehaviorBase.java`
#### Snippet
```java
        if (stateObj == null)
        {
            return null;
        }
        if (stateObj instanceof _AttachedListStateWrapper)
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/behavior/BehaviorBase.java`
#### Snippet
```java
            }
            //_behaviorListeners instances once is created never changes, we can return null
            return null;
        }
        else
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/validator/RequiredValidator.java`
#### Snippet
```java
    private String getFor()
    {
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/validator/RequiredValidator.java`
#### Snippet
```java
    private Boolean isDisabled()
    {
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/validator/RegexValidator.java`
#### Snippet
```java
    private Boolean isDisabled()
    {
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/validator/RegexValidator.java`
#### Snippet
```java
    private String getFor()
    {
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/validator/RegexValidator.java`
#### Snippet
```java
            return pattern;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/validator/DoubleRangeValidator.java`
#### Snippet
```java
    private Boolean isDisabled()
    {
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/validator/DoubleRangeValidator.java`
#### Snippet
```java
            return values;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/validator/DoubleRangeValidator.java`
#### Snippet
```java
    private String getFor()
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/validator/LongRangeValidator.java`
#### Snippet
```java
    private Boolean isDisabled()
    {
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/validator/LongRangeValidator.java`
#### Snippet
```java
            return values;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/validator/LongRangeValidator.java`
#### Snippet
```java
    private String getFor()
    {
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/validator/LengthValidator.java`
#### Snippet
```java
    private Boolean isDisabled()
    {
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/validator/LengthValidator.java`
#### Snippet
```java
    private String getFor()
    {
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/validator/LengthValidator.java`
#### Snippet
```java
            return values;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/annotation/FacesConfig.java`
#### Snippet
```java
        public Version version()
        {
            return null; // non binding, so not used
        }
    }
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/application/ResourceHandler.java`
#### Snippet
```java
            String path, int maxDepth, ResourceVisitOption... options)
    {
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/application/ResourceHandler.java`
#### Snippet
```java
    public Resource createResourceFromId(String resourceId)
    {
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/behavior/_DeltaStateHelper.java`
#### Snippet
```java
            return defaultValueSupplier.get();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/behavior/_DeltaStateHelper.java`
#### Snippet
```java
        if (stateObj == null)
        {
            return null;
        }
        if (stateObj instanceof _AttachedListStateWrapper)
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/behavior/_DeltaStateHelper.java`
#### Snippet
```java
            return expression.getValue(_target.getFacesContext().getELContext());
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/behavior/_DeltaStateHelper.java`
#### Snippet
```java
        if (valueOrKey == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/behavior/_DeltaStateHelper.java`
#### Snippet
```java
        if (attachedObject == null)
        {
            return null;
        }
        // StateHolder interface should take precedence over
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/behavior/_DeltaStateHelper.java`
#### Snippet
```java
            if (holder.isTransient())
            {
                return null;
            }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/behavior/_DeltaStateHelper.java`
#### Snippet
```java
        if (serializableMap == null || serializableMap.isEmpty())
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/validator/BeanValidator.java`
#### Snippet
```java
        {
            // default validation groups can be saved as null.
            return null;
        }
        else
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/validator/BeanValidator.java`
#### Snippet
```java
    private String getFor()
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/validator/BeanValidator.java`
#### Snippet
```java
    private Boolean isDisabled()
    {
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/behavior/_AjaxBehaviorDeltaStateHelper.java`
#### Snippet
```java
        if (valueOrKey == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/behavior/_AjaxBehaviorDeltaStateHelper.java`
#### Snippet
```java
            return defaultValueSupplier.get();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/behavior/_AjaxBehaviorDeltaStateHelper.java`
#### Snippet
```java
            return expression.getValue(FacesContext.getCurrentInstance().getELContext());
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/behavior/_AjaxBehaviorDeltaStateHelper.java`
#### Snippet
```java
        if (serializableMap == null || serializableMap.isEmpty())
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/UIViewRoot.java`
#### Snippet
```java
        if (_systemEventListeners == null)
        {
            return null;
        }
        return _systemEventListeners.get(systemEvent);
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/UIViewRoot.java`
#### Snippet
```java
                if (parentSaved == null)
                {
                    return null;
                }
                return new Object[]{parentSaved, null};
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/UIViewRoot.java`
#### Snippet
```java
            {
                //No values
                return null;
            }
            else if (parentSaved == null && _viewScope != null && _viewScope.isEmpty()
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/UIViewRoot.java`
#### Snippet
```java
            {
                //Empty view scope, no values
                return null;
            }
            
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/UIData.java`
#### Snippet
```java
        public Object getWrappedData()
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/UIData.java`
#### Snippet
```java
                if (parentSaved == null &&_rowDeltaStates.isEmpty() && _rowStates.isEmpty())
                {
                    return null;
                }
                else
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/component/UIData.java`
#### Snippet
```java
                if (parentSaved == null &&_rowDeltaStates.isEmpty())
                {
                    return null;
                }
                else
```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/application/Application.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/application/Application.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/application/NavigationCase.java`
#### Snippet
```java
        if (_condition == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/application/NavigationCase.java`
#### Snippet
```java
        if (_toViewId == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/ImportHandlerResolver.java`
#### Snippet
```java
                || IMPORT_HANDLER_RESOLVE_CLASS_METHOD == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/ImportHandlerResolver.java`
#### Snippet
```java
        }            

        return null;
    }
}
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/ResourceResolver.java`
#### Snippet
```java
            final Object base)
    {
        return base == null ? Object.class : null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/ResourceResolver.java`
#### Snippet
```java
            }
        }
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/ResourceResolver.java`
#### Snippet
```java
            final Object property)
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/ResourceResolver.java`
#### Snippet
```java
            final ELContext context, final Object base)
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/EmptyStringToNullELResolver.java`
#### Snippet
```java
    public Class<?> getType(ELContext context, Object base, Object property)
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/EmptyStringToNullELResolver.java`
#### Snippet
```java
    public Iterator<FeatureDescriptor> getFeatureDescriptors(ELContext context, Object base)
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/EmptyStringToNullELResolver.java`
#### Snippet
```java
    public Object getValue(ELContext context, Object base, Object property)
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/implicitobject/SessionScopeImplicitObject.java`
#### Snippet
```java
    public Class<?> getType()
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/LambdaBeanELResolver.java`
#### Snippet
```java
        if (base == null || property == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/LambdaBeanELResolver.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/LambdaBeanELResolver.java`
#### Snippet
```java
        if (base == null || property == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/implicitobject/SessionImplicitObject.java`
#### Snippet
```java
    public Class<?> getType()
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/ScopedAttributeResolver.java`
#### Snippet
```java
        if (facesContext == null)
        {
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/ScopedAttributeResolver.java`
#### Snippet
```java
        if (extContext == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/ScopedAttributeResolver.java`
#### Snippet
```java

        // not found
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/ScopedAttributeResolver.java`
#### Snippet
```java
        if (base != null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/ScopedAttributeResolver.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/ScopedAttributeResolver.java`
#### Snippet
```java
        if (base != null)
        {
            return null;
        }
        if (property == null)
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/ScopedAttributeResolver.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/ScopedAttributeResolver.java`
#### Snippet
```java
        if (base != null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/ScopedAttributeResolver.java`
#### Snippet
```java
        if (base != null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/implicitobject/CookieImplicitObject.java`
#### Snippet
```java
    public Class<?> getType()
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/implicitobject/ParamValuesImplicitObject.java`
#### Snippet
```java
    public Class<?> getType()
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/implicitobject/ViewScopeImplicitObject.java`
#### Snippet
```java
    public Class<?> getType()
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/ResourceBundleResolver.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/ResourceBundleResolver.java`
#### Snippet
```java
        if (base != null)
        {
            return null;
        }
        if (property == null)
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/ResourceBundleResolver.java`
#### Snippet
```java
        if (!(property instanceof String))
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/ResourceBundleResolver.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/ResourceBundleResolver.java`
#### Snippet
```java
        if (base != null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/ResourceBundleResolver.java`
#### Snippet
```java
        if (base != null)
        {
            return null;
        }
        if (property == null)
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/ResourceBundleResolver.java`
#### Snippet
```java
        if (!(property instanceof String))
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/ResourceBundleResolver.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/ResourceBundleResolver.java`
#### Snippet
```java
        if (base != null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/implicitobject/InitParamImplicitObject.java`
#### Snippet
```java
    public Class<?> getType()
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/implicitobject/RequestImplicitObject.java`
#### Snippet
```java
    public Class<?> getType()
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/implicitobject/RequestScopeImplicitObject.java`
#### Snippet
```java
    public Class<?> getType()
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/FlashELResolver.java`
#### Snippet
```java
        if (!(property instanceof String))
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/FlashELResolver.java`
#### Snippet
```java
                if (facesContext == null)
                {
                    return null;
                }
                ExternalContext externalContext = facesContext.getExternalContext();
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/FlashELResolver.java`
#### Snippet
```java
                if (externalContext == null)
                {
                    return null;
                }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/FlashELResolver.java`
#### Snippet
```java
            if (facesContext == null)
            {
                return null;
            }
            ExternalContext externalContext = facesContext.getExternalContext();
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/FlashELResolver.java`
#### Snippet
```java
            if (externalContext == null)
            {
                return null;
            }
            Flash flash = (Flash) base;
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/FlashELResolver.java`
#### Snippet
```java
            }
        }
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/FlashELResolver.java`
#### Snippet
```java
        if (!(property instanceof String))
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/FlashELResolver.java`
#### Snippet
```java
            context.setPropertyResolved(true);
            Object obj = ((Flash) base).get(property);
            return (obj != null) ? obj.getClass() : null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/FlashELResolver.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/FlashELResolver.java`
#### Snippet
```java
        if (base == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/FlashELResolver.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/implicitobject/ParamImplicitObject.java`
#### Snippet
```java
    public Class<?> getType()
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/implicitobject/ViewImplicitObject.java`
#### Snippet
```java
    public Class<?> getType()
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/implicitobject/FacesContextImplicitObject.java`
#### Snippet
```java
    public Class<?> getType()
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/implicitobject/ResourceImplicitObject.java`
#### Snippet
```java
    public Class<?> getType()
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/ImportConstantsELResolver.java`
#### Snippet
```java
        if (base != null)
        {
            return null;
        }
        if (property == null)
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/implicitobject/HeaderValuesImplicitObject.java`
#### Snippet
```java
    public Class<?> getType()
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/ImportConstantsELResolver.java`
#### Snippet
```java
        if (!(property instanceof String))
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/ImportConstantsELResolver.java`
#### Snippet
```java
        if (facesContext == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/implicitobject/ApplicationImplicitObject.java`
#### Snippet
```java
    public Class<?> getType()
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/ImportConstantsELResolver.java`
#### Snippet
```java
        if (viewRoot == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/ImportConstantsELResolver.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/ImportConstantsELResolver.java`
#### Snippet
```java
    public Class<?> getCommonPropertyType(final ELContext context, final Object base)
    {
        return base == null ? Object.class : null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/ImportConstantsELResolver.java`
#### Snippet
```java
            throws NullPointerException, PropertyNotFoundException, ELException
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/ImportConstantsELResolver.java`
#### Snippet
```java
    public Iterator<FeatureDescriptor> getFeatureDescriptors(final ELContext context, final Object base)
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/implicitobject/HeaderImplicitObject.java`
#### Snippet
```java
    public Class<?> getType()
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/implicitobject/ComponentImplicitObject.java`
#### Snippet
```java
    public Class<?> getType()
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/implicitobject/FlowScopeImplicitObject.java`
#### Snippet
```java
    public Class<?> getType()
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/implicitobject/ApplicationScopeImplicitObject.java`
#### Snippet
```java
    public Class<?> getType()
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/implicitobject/CompositeComponentImplicitObject.java`
#### Snippet
```java
    public Class<?> getType()
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/CompositeComponentELResolver.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/CompositeComponentELResolver.java`
#### Snippet
```java
    {
        // Per the spec, do nothing.
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/CompositeComponentELResolver.java`
#### Snippet
```java
            {
                valueExpression.setValue(FacesContext.getCurrentInstance().getELContext(), value);
                return null;
            }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/CompositeComponentELResolver.java`
#### Snippet
```java
        // Otherwise, spec says to do nothing (return null).

        return null;
    }
    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/CompositeComponentELResolver.java`
#### Snippet
```java
            if (facesContext == null)
            {
                return null;
            }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/CompositeComponentELResolver.java`
#### Snippet
```java

        // Per the spec, return null.
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/cdi/FacesScopeContext.java`
#### Snippet
```java
            throw new IllegalStateException("FacesContext cannot be found when resolving bean " +bean.toString());
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/cdi/util/AbstractContext.java`
#### Snippet
```java
        if (storage == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/cdi/util/AbstractContext.java`
#### Snippet
```java
        if (contextualInstanceInfo == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/implicitobject/ImplicitObjectResolver.java`
#### Snippet
```java
        if (base != null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/implicitobject/ImplicitObjectResolver.java`
#### Snippet
```java
        if (base != null)
        {
            return null;
        }
        if (property == null)
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/implicitobject/ImplicitObjectResolver.java`
#### Snippet
```java
        if (!(property instanceof String))
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/implicitobject/ImplicitObjectResolver.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/implicitobject/ImplicitObjectResolver.java`
#### Snippet
```java
        if (base != null)
        {
            return null;
        }
        if (property == null)
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/implicitobject/ImplicitObjectResolver.java`
#### Snippet
```java
        if (!(property instanceof String))
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/implicitobject/ImplicitObjectResolver.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/el/resolver/implicitobject/ImplicitObjectResolver.java`
#### Snippet
```java
        if (base != null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/cdi/view/ViewScopeContext.java`
#### Snippet
```java
        if (storage == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/cdi/view/ViewScopeContext.java`
#### Snippet
```java
        if (contextualInstanceInfo == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/cdi/view/ViewScopeContext.java`
#### Snippet
```java
            return getStorageHolder(facesContext).getContextualStorage(viewScopeId, createIfNotExist);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/cdi/view/ViewScopeContext.java`
#### Snippet
```java
            return id;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/cdi/view/ViewScopeCDIMap.java`
#### Snippet
```java
            ContextualInstanceInfo<?> info = getCreationalContextInstances().get(key);

            return (Object) (info == null ? null : info.getContextualInstance());
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/cdi/view/ViewScopeCDIMap.java`
#### Snippet
```java
    {
        ContextualInstanceInfo info = getCreationalContextInstances().remove(key);
        return info == null ? null : info.getContextualInstance();
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/cdi/view/ViewScopeCDIMap.java`
#### Snippet
```java
    {
        ContextualInstanceInfo<?> info = getCreationalContextInstances().get(key);
        return info == null ? null : info.getContextualInstance();
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/cdi/util/AbstractContextualStorageHolder.java`
#### Snippet
```java
                || facesContext.getExternalContext().getSession(false) == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/cdi/util/AbstractContextualStorageHolder.java`
#### Snippet
```java
        if (beanManager == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/cdi/util/AbstractContextualStorageHolder.java`
#### Snippet
```java
        if (!CDIUtils.isSessionScopeActive(beanManager))
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/cdi/util/AbstractContextualStorageHolder.java`
#### Snippet
```java
            if (!create)
            {
                return null;
            }
            
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/cdi/view/ViewTransientScopeContext.java`
#### Snippet
```java
            throw new IllegalStateException("FacesContext cannot be found when resolving bean " +bean.toString());
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/cdi/util/CDIUtils.java`
#### Snippet
```java
        if (beans == null || beans.isEmpty())
        {
            return null;
        }
        return resolveInstance(bm, beans, clazz);
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/cdi/util/CDIUtils.java`
#### Snippet
```java
        {
            Bean<T> bean = get(beanManager, beanClass, qualifiers);
            return bean == null ? null : get(beanManager, bean, beanClass, create);
        }
        catch (ContextNotActiveException e)
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/cdi/util/CDIUtils.java`
#### Snippet
```java
        catch (ContextNotActiveException e)
        {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/cdi/util/CDIUtils.java`
#### Snippet
```java
            Bean<T> bean = (Bean<T>) beanManager.resolve(beans);

            return bean == null ? null : get(beanManager, bean, type, create);
        }
        catch (ContextNotActiveException e)
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/cdi/util/CDIUtils.java`
#### Snippet
```java
        catch (ContextNotActiveException e)
        {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/cdi/model/FacesDataModelManager.java`
#### Snippet
```java
        if (facesDataModels == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/cdi/model/FacesDataModelManager.java`
#### Snippet
```java
            }
        }
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/cdi/wrapper/FacesClientBehaviorCDIWrapper.java`
#### Snippet
```java
            return values;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/cdi/wrapper/FacesBehaviorCDIWrapper.java`
#### Snippet
```java
            return values;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/cdi/wrapper/FacesConverterCDIWrapper.java`
#### Snippet
```java
            return values;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/cdi/wrapper/FacesValidatorCDIWrapper.java`
#### Snippet
```java
            return values;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/cdi/clientwindow/ClientWindowScopeContext.java`
#### Snippet
```java
            throw new IllegalStateException("FacesContext cannot be found when resolving bean " + bean.toString());
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/spi/StateCacheProvider.java`
#### Snippet
```java
    public StateCacheProvider getWrapped()
    {
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/spi/StateCacheProviderFactory.java`
#### Snippet
```java
    public StateCacheProviderFactory getWrapped()
    {
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/spi/AnnotationProvider.java`
#### Snippet
```java
    public AnnotationProvider getWrapped()
    {
        return null;
    }    
}
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/spi/ServiceProviderFinderFactory.java`
#### Snippet
```java
            }
        }
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/spi/ServiceProviderFinder.java`
#### Snippet
```java
    {

        return null;
    }
}
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/spi/impl/CDIAnnotationDelegateInjectionProvider.java`
#### Snippet
```java
            return delegate.inject(instance);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/spi/impl/Tomcat7AnnotationInjectionProvider.java`
#### Snippet
```java
        if (context == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/spi/impl/Tomcat7AnnotationInjectionProvider.java`
#### Snippet
```java
        if (extCtx == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/spi/impl/Tomcat7AnnotationInjectionProvider.java`
#### Snippet
```java
    public Object inject(Object instance) throws InjectionProviderException
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/spi/impl/NoAnnotationInjectionProvider.java`
#### Snippet
```java
    {
        // No op
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/spi/impl/NoInjectionAnnotationInjectionProvider.java`
#### Snippet
```java
            throw new InjectionProviderException(ex);
        }
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/spi/impl/DefaultServiceProviderFinder.java`
#### Snippet
```java
        catch (IOException e)
        {
            return null;
        }
        if (null != profiles && profiles.hasMoreElements())
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/flow/FlowUtils.java`
#### Snippet
```java
            return FlowUtils.getFlowMapKey(facesContext, flow);
        }
        return null;
    }    
    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/flow/cdi/DefaultCDIFacesFlowProvider.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/flow/cdi/DefaultCDIFacesFlowProvider.java`
#### Snippet
```java
                Logger.getLogger(DefaultCDIFacesFlowProvider.class.getName()).log(Level.INFO,
                        "CDI BeanManager not found");
                return null;
            }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/flow/cdi/FlowScopeMap.java`
#### Snippet
```java
        if (storageHolder == null)
        {
            return null;
        }
        return storageHolder.getFlowScopeMap(beanManager, currentClientWindowFlowId, create);
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/flow/cdi/FlowScopeMap.java`
#### Snippet
```java
    {
        Map<Object, Object> wrapped = getWrapped(false);
        return wrapped == null ? null : wrapped.remove(key);
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/flow/cdi/FlowScopeMap.java`
#### Snippet
```java
    {
        Map<Object, Object> wrapped = getWrapped(false);
        return wrapped == null ? null : wrapped.get(key);
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/flow/impl/DefaultFacesFlowProvider.java`
#### Snippet
```java
    {
        //Without CDI there is no @FlowDefinition annotations to scan for
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/flow/cdi/FlowScopeContextualStorageHolder.java`
#### Snippet
```java
        if (contextualStorage == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/flow/cdi/FlowScopeContextualStorageHolder.java`
#### Snippet
```java
        if (info == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/flow/impl/FlowScopeMap.java`
#### Snippet
```java
        if (map == null)
        {
            return null;
        }
        return map.get(key);
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/flow/impl/FlowScopeMap.java`
#### Snippet
```java
        if (map == null)
        {
            return null;
        }
        return map.remove(key);
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/flow/FlowImpl.java`
#### Snippet
```java
            }
        }
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/push/Json.java`
#### Snippet
```java
        if (string == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/push/cdi/WebsocketScopeManager.java`
#### Snippet
```java
            {
                // Always generate a connection
                return null;
            }
            String token = null;
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/push/cdi/WebsocketScopeManager.java`
#### Snippet
```java
                return value;
            }
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/push/cdi/WebsocketScopeManager.java`
#### Snippet
```java
                }
            }
            return null;
        } 
    }
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/flow/cdi/FlowScopeContext.java`
#### Snippet
```java
            return (T) contextualInstanceInfo.getContextualInstance();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/util/ExternalContextUtils.java`
#### Snippet
```java
            break;
        }
        return null; // not found
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/util/ExternalContextUtils.java`
#### Snippet
```java
        }

        return null;
    }
    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/util/WebXmlParser.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/util/WebXmlParser.java`
#### Snippet
```java
            if (url == null)
            {
                return null;
            }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/util/WebXmlParser.java`
#### Snippet
```java
            if (is == null)
            {
                return null;
            }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/flow/NavigationCaseImpl.java`
#### Snippet
```java
        if (_toViewId == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/flow/NavigationCaseImpl.java`
#### Snippet
```java
        if (_condition == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/util/lang/SkipMatchIterator.java`
#### Snippet
```java
                else
                {
                    return null;
                }
            }
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/util/IllegalXmlCharacterFilterWriter.java`
#### Snippet
```java
        if (str == null)
        {
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/util/IllegalXmlCharacterFilterWriter.java`
#### Snippet
```java
        if (cbuf == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/flow/FlowHandlerImpl.java`
#### Snippet
```java
        if (session == null)
        {
            return null;
        }
        ClientWindow clientWindow = context.getExternalContext().getClientWindow();
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/flow/FlowHandlerImpl.java`
#### Snippet
```java
        if (clientWindow == null)
        {
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/flow/FlowHandlerImpl.java`
#### Snippet
```java
        if (info == null)
        {
            return null;
        }
        FlowReference flowReference = info.getFlowReference();
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/flow/FlowHandlerImpl.java`
#### Snippet
```java
        if (currentFlowStack == null)
        {
            return null;
        }
        return currentFlowStack.size() > 0 ? currentFlowStack.remove(currentFlowStack.size()-1) : null;
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/flow/FlowHandlerImpl.java`
#### Snippet
```java
            return null;
        }
        return currentFlowStack.size() > 0 ? currentFlowStack.remove(currentFlowStack.size()-1) : null;
    }
    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/flow/FlowHandlerImpl.java`
#### Snippet
```java
                return info;
            }
            return null;
        }
        else
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/flow/FlowHandlerImpl.java`
#### Snippet
```java
            if (currentFlowStack == null)
            {
                return null;
            }
            return currentFlowStack.size() > 0 ? 
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/flow/FlowHandlerImpl.java`
#### Snippet
```java
            }
            return currentFlowStack.size() > 0 ? 
                currentFlowStack.get(currentFlowStack.size()-1) : null;
        }
    }
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/flow/FlowHandlerImpl.java`
#### Snippet
```java
        if (session == null)
        {
            return null;
        }
        ClientWindow clientWindow = context.getExternalContext().getClientWindow();
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/flow/FlowHandlerImpl.java`
#### Snippet
```java
        if (clientWindow == null)
        {
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/flow/FlowHandlerImpl.java`
#### Snippet
```java
            return _flowMapById.get(id);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/util/lang/ArrayUtils.java`
#### Snippet
```java
        if (commonComponentType == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/util/lang/PriorityQueue.java`
#### Snippet
```java
    protected T getSentinelObject()
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/util/lang/PriorityQueue.java`
#### Snippet
```java
        {
            add(element);
            return null;
        }
        else if (size > 0 && !lessThan(element, heap[1]))
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/util/lang/PriorityQueue.java`
#### Snippet
```java
        else
        {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/util/lang/FilenameUtils.java`
#### Snippet
```java
        if (filename == null)
        {
            return null;
        }
        final int len = getPrefixLength(filename);
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/util/lang/FilenameUtils.java`
#### Snippet
```java
        if (len < 0)
        {
            return null;
        }
        if (len > filename.length())
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/util/lang/FilenameUtils.java`
#### Snippet
```java
        if (path == null)
        {
            return null;
        }
        if (isSystemWindows())
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/util/lang/FilenameUtils.java`
#### Snippet
```java
        if (filename == null)
        {
            return null;
        }
        final int prefix = getPrefixLength(filename);
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/util/lang/FilenameUtils.java`
#### Snippet
```java
        if (prefix < 0)
        {
            return null;
        }
        final int index = indexOfLastSeparator(filename);
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/util/lang/FilenameUtils.java`
#### Snippet
```java
        if (filename == null)
        {
            return null;
        }
        final int index = indexOfExtension(filename);
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/util/lang/FilenameUtils.java`
#### Snippet
```java
        if (filename == null)
        {
            return null;
        }
        final int index = indexOfExtension(filename);
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/util/lang/FilenameUtils.java`
#### Snippet
```java
        if (prefix < 0)
        {
            return null;
        }
        if (prefix > 0)
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/util/lang/FilenameUtils.java`
#### Snippet
```java
        if (basePath == null)
        {
            return null;
        }
        final int len = basePath.length();
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/util/lang/FilenameUtils.java`
#### Snippet
```java
        if (filename == null)
        {
            return null;
        }
        final int prefix = getPrefixLength(filename);
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/util/lang/FilenameUtils.java`
#### Snippet
```java
        if (prefix < 0)
        {
            return null;
        }
        if (prefix >= filename.length())
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/util/lang/FilenameUtils.java`
#### Snippet
```java
        if (filename == null)
        {
            return null;
        }
        final int index = indexOfLastSeparator(filename);
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/util/lang/FilenameUtils.java`
#### Snippet
```java
        if (filename == null)
        {
            return null;
        }
        int size = filename.length();
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/util/lang/FilenameUtils.java`
#### Snippet
```java
        if (prefix < 0)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/util/lang/FilenameUtils.java`
#### Snippet
```java
                if (i == prefix + 2)
                {
                    return null;
                }
                if (i == size - 1)
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/util/lang/ClassUtils.java`
#### Snippet
```java
        }
        
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/ViewDeclarationLanguageBase.java`
#### Snippet
```java
            
            // TODO: VALIDATE - Spec is silent on the return value when an error was sent
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/ServletViewResponseWrapper.java`
#### Snippet
```java
            return _charArrayWriter.toString();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/ViewDeclarationLanguageFactoryImpl.java`
#### Snippet
```java
        }
        
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/AbstractFaceletContext.java`
#### Snippet
```java
    public PageContext popPageContext()
    {
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/AbstractFaceletContext.java`
#### Snippet
```java
    public TemplateContext getTemplateContext()
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/AbstractFaceletContext.java`
#### Snippet
```java
    public PageContext getPageContext()
    {
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/util/lang/StringUtils.java`
#### Snippet
```java
        if (strings == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/util/lang/StringUtils.java`
#### Snippet
```java
        if (str == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/util/lang/StringUtils.java`
#### Snippet
```java
        if (str == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletCompositionContext.java`
#### Snippet
```java
    public String generateUniqueId()
    {
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletCompositionContext.java`
#### Snippet
```java
    public List<String> getUniqueIdList()
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletCompositionContext.java`
#### Snippet
```java
    public String getUniqueIdFromIterator()
    {
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletCompositionContext.java`
#### Snippet
```java
    public String generateUniqueComponentId()
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletCompositionContext.java`
#### Snippet
```java
    public String startComponentUniqueIdSection(String base)
    {
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletCompositionContext.java`
#### Snippet
```java
    public String startComponentUniqueIdSection()
    {
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguageStrategy.java`
#### Snippet
```java
        if (mappings == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguageStrategy.java`
#### Snippet
```java
        if (mappings.length() == 0)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FullStateManagementStrategy.java`
#### Snippet
```java
        if (viewRoot.isTransient())
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FullStateManagementStrategy.java`
#### Snippet
```java
        if (uiViewRoot.isTransient())
        {
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FullStateManagementStrategy.java`
#### Snippet
```java
            if (uiViewRoot.isTransient())
            {
                return null;
            }
    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FullStateManagementStrategy.java`
#### Snippet
```java
        if (viewRoot.isTransient())
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/util/lang/StreamCharBuffer.java`
#### Snippet
```java
        public ChunkReader next()
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/CompositeComponentELUtils.java`
#### Snippet
```java
        {
            // Cannot found any component, because we don't have any reference!
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/CompositeComponentELUtils.java`
#### Snippet
```java
        
        // not found
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/CompositeComponentELUtils.java`
#### Snippet
```java
        {
            // Cannot found any component, because we don't have any reference!
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/CompositeComponentELUtils.java`
#### Snippet
```java
        
        // not found
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/CompositeComponentELUtils.java`
#### Snippet
```java
            else
            {
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/CompositeComponentELUtils.java`
#### Snippet
```java
            else
            {
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/CompositeComponentELUtils.java`
#### Snippet
```java
            currentComponent = UIComponent.getCompositeComponentParent(currentComponent);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/util/lang/ConcurrentLRUCache.java`
#### Snippet
```java
            {
                add(element);
                return null;
            }
            else if (size() > 0
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/util/lang/ConcurrentLRUCache.java`
#### Snippet
```java
                stats.missCounter.incrementAndGet();
            }
            return null;
        }
        if (islive)
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/util/lang/ConcurrentLRUCache.java`
#### Snippet
```java
            return cacheEntry.value;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/util/lang/ConcurrentLRUCache.java`
#### Snippet
```java
        if (val == null)
        {
            return null;
        }
        CacheEntry<K, V> e = new CacheEntry<K, V>(key, val,
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/util/lang/ConcurrentLRUCache.java`
#### Snippet
```java
            }
        }
        return oldCacheEntry == null ? null : oldCacheEntry.value;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ValueExpressionMethodExpression.java`
#### Snippet
```java
            return me.getMethodInfo(context);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ValueExpressionMethodExpression.java`
#### Snippet
```java
            return me.invoke(context, params);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ValueExpressionMethodExpression.java`
#### Snippet
```java
        else
        {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/RedirectMethodExpressionValueExpressionValueChangeListener.java`
#### Snippet
```java
        else
        {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/RedirectMethodExpressionValueExpressionValidator.java`
#### Snippet
```java
        else
        {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/DefaultFunctionMapper.java`
#### Snippet
```java
        }
        
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/RedirectMethodExpressionValueExpressionActionListener.java`
#### Snippet
```java
        else
        {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/ViewPoolProcessor.java`
#### Snippet
```java
            }
        }
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/ViewPoolProcessor.java`
#### Snippet
```java
            // Stateless views cannot be pooled, because we are reusing
            // state saving algorithm for that.
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/ViewPoolProcessor.java`
#### Snippet
```java
        {
            // view pool not enabled for this view.
            return null;
        }
        else
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/CompositeTagLibrary.java`
#### Snippet
```java
            }
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/CompositeTagLibrary.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/TagHandlerUtils.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/TagAttributesImpl.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/MetadataTargetImpl.java`
#### Snippet
```java
        }
        
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/MetadataTargetImpl.java`
#### Snippet
```java
        if (propertyDescriptors == null)
        {
            return null;
        }
        return propertyDescriptors.get(name);
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/MetadataTargetImpl.java`
#### Snippet
```java
        }
        
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/MetadataTargetImpl.java`
#### Snippet
```java
        }
        
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ComponentTagDeclarationLibrary.java`
#### Snippet
```java
    public Method createFunction(String ns, String name)
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ComponentTagDeclarationLibrary.java`
#### Snippet
```java
        }
        
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/LambdaMetadataTargetImpl.java`
#### Snippet
```java
        if (lpd == null)
        {
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/LambdaMetadataTargetImpl.java`
#### Snippet
```java
        if (lpd == null)
        {
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/LambdaMetadataTargetImpl.java`
#### Snippet
```java
        if (lpd == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/LambdaMetadataTargetImpl.java`
#### Snippet
```java
        else
        {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/LambdaMetadataTargetImpl.java`
#### Snippet
```java
        if (lpd == null)
        {
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/LambdaMetadataTargetImpl.java`
#### Snippet
```java
        if (lpd == null)
        {
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/LambdaMetadataTargetImpl.java`
#### Snippet
```java
        if (lpd == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/BeanPropertyTagRule.java`
#### Snippet
```java
        }

        return null;
    }
    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/MethodRule.java`
#### Snippet
```java
        if (false == name.equals(this.methodName))
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/MethodRule.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ELText.java`
#### Snippet
```java
        public Class<?> getExpectedType()
        {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ELText.java`
#### Snippet
```java
            }

            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ELText.java`
#### Snippet
```java
        public Object getValue(ELContext context)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ELText.java`
#### Snippet
```java
        if (text == null || text.isEmpty())
        {
            return null;
        }
        else if (text.size() == 1)
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ELText.java`
#### Snippet
```java
            }

            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ELText.java`
#### Snippet
```java
        if (text == null || text.isEmpty())
        {
            return null;
        }
        else if (text.size() == 1)
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ELText.java`
#### Snippet
```java
        public Class<?> getType(ELContext context)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/UIDebug.java`
#### Snippet
```java
        }
        
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/AbstractTagLibrary.java`
#### Snippet
```java
    public Method createFunction(String ns, String name)
    {
        return containsNamespace(ns) ? _functions.get(name) : null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/AbstractTagLibrary.java`
#### Snippet
```java
        }
        
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/fn/JstlFnLibrary.java`
#### Snippet
```java
        }
        
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/fn/JstlFnLibrary.java`
#### Snippet
```java
    public TagHandler createTagHandler(String ns, String localName, TagConfig tag) throws FacesException
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/MappedValueExpression.java`
#### Snippet
```java

        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/MappedValueExpression.java`
#### Snippet
```java
            return context.getELResolver().getType(context, base, key);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/PartialStateManagementStrategy.java`
#### Snippet
```java
        {
            //No state could be restored, return null causing ViewExpiredException
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/PartialStateManagementStrategy.java`
#### Snippet
```java
                    if (view == null && context.getResponseComplete())
                    {
                        return null;
                    }
                }
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/PartialStateManagementStrategy.java`
#### Snippet
```java
        {
            // Not much that can be done.
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/IndexedValueExpression.java`
#### Snippet
```java
            return context.getELResolver().getType(context, base, i);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/IndexedValueExpression.java`
#### Snippet
```java
            return context.getELResolver().getValue(context, base, i);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/PassthroughRuleImpl.java`
#### Snippet
```java
            }
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ElementNameRule.java`
#### Snippet
```java
            }
        }
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ActionSourceRule.java`
#### Snippet
```java
        }
        
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ComponentRule.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ValueHolderRule.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/FaceletState.java`
#### Snippet
```java
        if (bindingsMap == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/FaceletState.java`
#### Snippet
```java
        if (bindings == null)
        {
            return null;
        }
        return bindings.get(key);
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/FaceletState.java`
#### Snippet
```java
        if(stateMap == null)
        {
            return null;
        }
        return stateMap.get(key);
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/FaceletState.java`
#### Snippet
```java
                return values;
            }
            return null;
        }
        else
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/PartialMethodExpressionValidator.java`
#### Snippet
```java
        if (initialStateMarked())
        {
            return null;
        }
        return super.saveState(context);
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/BehaviorTagHandlerDelegate.java`
#### Snippet
```java
        if (forAttribute == null)
        {
            return null;
        }
        else
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/PartialMethodExpressionActionListener.java`
#### Snippet
```java
        if (initialStateMarked())
        {
            return null;
        }
        return super.saveState(context);
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ConverterTagHandlerDelegate.java`
#### Snippet
```java
        if (forAttribute == null)
        {
            return null;
        }
        else
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/EditableValueHolderRule.java`
#### Snippet
```java

        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/PartialMethodExpressionValueChangeListener.java`
#### Snippet
```java
        if (initialStateMarked())
        {
            return null;
        }
        return super.saveState(context);
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ValidatorTagHandlerDelegate.java`
#### Snippet
```java
        if (forAttribute == null)
        {
            return null;
        }
        else
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ValidatorTagHandlerDelegate.java`
#### Snippet
```java
        if (attribute == null)
        {
            return null;
        }
        else
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/LegacyForEachHandler.java`
#### Snippet
```java
            return this.varStatus.getValue(ctx);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/LegacyForEachHandler.java`
#### Snippet
```java
            return pctx.getAttributes().put(name, null);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/LegacyForEachHandler.java`
#### Snippet
```java
        if (src == null)
        {
            return null;
        }
        else if (src instanceof Collection)
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/LegacyForEachHandler.java`
#### Snippet
```java
            return this.var.getValue(ctx);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/ValidateDelegateHandler.java`
#### Snippet
```java
        if (validatorId == null)
        {
            return null;
        }
        return validatorId.getValue(ctx);
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/SetPropertyActionListenerHandler.java`
#### Snippet
```java
        if (forAttribute == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/ValueChangeListenerHandler.java`
#### Snippet
```java
        if (forAttribute == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/PhaseListenerHandler.java`
#### Snippet
```java
            if (facesContext == null)
            {
                return null;
            }
            
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguage.java`
#### Snippet
```java
                    {
                        sendSourceNotFound(context, getViewId());
                        return null;
                    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguage.java`
#### Snippet
```java
        Assert.notNull(componentResource, "componentResource");

        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguage.java`
#### Snippet
```java
            if (componentFacelet == null)
            {
                return null;
            }
            // Create a temporal component base class where all components will be put, but we are only
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguage.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguage.java`
#### Snippet
```java
            // in UIDebug.debugRequest() and facesContext.responseComplete()
            // has been called.
            return null;
        }
        else
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/ActionListenerHandler.java`
#### Snippet
```java
        if (forAttribute == null)
        {
            return null;
        }
        else
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/ConvertDelegateHandler.java`
#### Snippet
```java
        if (converterId == null)
        {
            return null;
        }
        return converterId.getValue(ctx);
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ComponentSupport.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ComponentSupport.java`
#### Snippet
```java
        }

        return null;
    }
    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ComponentSupport.java`
#### Snippet
```java
            }
        }
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ComponentSupport.java`
#### Snippet
```java
            }
        }
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ComponentSupport.java`
#### Snippet
```java
            }
        }
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ComponentSupport.java`
#### Snippet
```java
                }
            }
            return null;
        }
        return null;
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ComponentSupport.java`
#### Snippet
```java
            return null;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ComponentSupport.java`
#### Snippet
```java
            }
        }
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/EventHandler.java`
#### Snippet
```java
            // If the listener was marked, no need to save anything, because 
            // this object is immutable after that.
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/ResetValuesActionListenerHandler.java`
#### Snippet
```java
        if (forAttribute == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/AjaxHandler.java`
#### Snippet
```java
    public String getFor()
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/AjaxHandler.java`
#### Snippet
```java
            if (initialStateMarked())
            {
                return null;
            }
            return _expr;
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/AjaxHandler.java`
#### Snippet
```java
        if (_event == null)
        {
            return null;
        }
        else
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/AttachedObjectTargetImpl.java`
#### Snippet
```java
            return (String) _name.getValue(FacesContext.getCurrentInstance().getELContext());
        }        
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeMetadataTargetImpl.java`
#### Snippet
```java
        }
        
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeMetadataTargetImpl.java`
#### Snippet
```java
        }
        
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeMetadataTargetImpl.java`
#### Snippet
```java
        }
        
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/RetargetMethodExpressionRule.java`
#### Snippet
```java
            }
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/html/DefaultTagDecorator.java`
#### Snippet
```java
                     }
                 }
                return null;
            }
            else
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/html/DefaultTagDecorator.java`
#### Snippet
```java
        {
            // Return null, so the outer CompositeTagDecorator can process the tag.
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/html/DefaultTagDecorator.java`
#### Snippet
```java
                return NO_MATCH_SELECTOR.decorate(tag, convertTagAttributes(tag));
            }
            return null;
        }
        else
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/ClientBehaviorRedirectEventComponentWrapper.java`
#### Snippet
```java
    protected FacesListener[] getFacesListeners(Class clazz)
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/ClientBehaviorRedirectEventComponentWrapper.java`
#### Snippet
```java
            else
            {
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/ClientBehaviorRedirectEventComponentWrapper.java`
#### Snippet
```java
    protected Renderer getRenderer(FacesContext context)
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeComponentRule.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/ClientBehaviorAttachedObjectTargetImpl.java`
#### Snippet
```java
            return (String) _name.getValue(FacesContext.getCurrentInstance().getELContext());
        }        
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/ClientBehaviorAttachedObjectTargetImpl.java`
#### Snippet
```java
            return (String) _event.getValue(FacesContext.getCurrentInstance().getELContext());
        }        
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeResourceLibrary.java`
#### Snippet
```java
    {
        // Composite component tag library does not suport functions
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeResourceLibrary.java`
#### Snippet
```java
        if (mappings == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeResourceLibrary.java`
#### Snippet
```java
        if (mappings.length() == 0)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeResourceLibrary.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFaceletContext.java`
#### Snippet
```java
            return itc;
        }
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFaceletContext.java`
#### Snippet
```java
            return itc;
        }
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFaceletContext.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFaceletContext.java`
#### Snippet
```java
            return _ajaxHandlerStack.iterator();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/pool/ViewPoolFactory.java`
#### Snippet
```java
    public ViewPoolFactory getWrapped()
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/pool/impl/ViewPoolFactoryImpl.java`
#### Snippet
```java
            if (enableViewPool == null)
            {
                return null;
            }
            return defaultViewPool;
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/pool/impl/ViewPoolFactoryImpl.java`
#### Snippet
```java
            return defaultViewPool;
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/ForEachHandler.java`
#### Snippet
```java
            return this.var.getValue(ctx);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/ForEachHandler.java`
#### Snippet
```java
            return pctx.getAttributes().put(name, null);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/ForEachHandler.java`
#### Snippet
```java
            return this.varStatus.getValue(ctx);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/ForEachHandler.java`
#### Snippet
```java
        if (src == null)
        {
            return null;
        }
        else if (src instanceof Collection)
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/TemplateContextImpl.java`
#### Snippet
```java
                }
            }
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/TemplateContextImpl.java`
#### Snippet
```java
                }
            }
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/TemplateContextImpl.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFaceletFactory.java`
#### Snippet
```java
            _baseUrl = Optional.ofNullable(resource == null ? null : resource.getURL());
        }
        return _baseUrl.isPresent() ? _baseUrl.get() : null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFaceletFactory.java`
#### Snippet
```java
            return resource.getURL();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/pool/impl/ViewPoolImpl.java`
#### Snippet
```java
            return map.get(key);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/pool/impl/ViewPoolImpl.java`
#### Snippet
```java
        if (q == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/pool/impl/ViewPoolImpl.java`
#### Snippet
```java
        if (entry == null)
        {
            return null;
        }
        do
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/pool/impl/ViewPoolImpl.java`
#### Snippet
```java
        }
        while (entry != null);
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/pool/impl/ViewPoolImpl.java`
#### Snippet
```java
        if (map == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/pool/impl/ViewPoolImpl.java`
#### Snippet
```java
        if (q == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/pool/impl/ViewPoolImpl.java`
#### Snippet
```java
            entry = q.poll();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/pool/impl/ViewPoolImpl.java`
#### Snippet
```java
        if (q == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/pool/impl/ViewPoolImpl.java`
#### Snippet
```java
        if (entry == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/pool/impl/ViewPoolImpl.java`
#### Snippet
```java
        } while (entry != null);

        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/FaceletCompositionContextImpl.java`
#### Snippet
```java
        if (_methodExpressionsTargeted == null)
        {
            return null;
        }
        Map<String, Object> map = _methodExpressionsTargeted.get(targetedComponent);
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/FaceletCompositionContextImpl.java`
#### Snippet
```java
            return map.remove(attributeName);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/FaceletCompositionContextImpl.java`
#### Snippet
```java
            return _uniqueIdsIterator.next();
        }
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/FaceletCompositionContextImpl.java`
#### Snippet
```java
            return _enclosingValidatorIdsStack.iterator(); 
        }
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/FaceletCompositionContextImpl.java`
#### Snippet
```java
            return _compositeComponentStack.peek();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/FaceletCompositionContextImpl.java`
#### Snippet
```java
        if (_attachedObjectHandlers == null)
        {
            return null;
        }
        return _attachedObjectHandlers.get(compositeComponentParent);
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/FaceletCompositionContextImpl.java`
#### Snippet
```java
            return _uniqueIdVendorStack.peek();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/NamespaceManager.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TagLibraryConfig.java`
#### Snippet
```java
            if (mappings == null)
            {
                return null;
            }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TagLibraryConfig.java`
#### Snippet
```java
            if (mappings.length() == 0)
            {
                return null;
            }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/CompilationManager.java`
#### Snippet
```java
            return (CompilationUnit) this.units.peek();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/Compiler.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/NamespaceHandler.java`
#### Snippet
```java
            return this.library.createFunction(uri, localName);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/util/Classpath.java`
#### Snippet
```java
            {
                // excluded URL found, ignore it
                return null;
            }
            return new JarFile(jarFileUrl);
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/util/Classpath.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/util/Classpath.java`
#### Snippet
```java
        catch (Throwable t)
        {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/component/RepeatStatus.java`
#### Snippet
```java
        if (end == -1)
        {
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/component/RepeatStatus.java`
#### Snippet
```java
        if (begin == -1)
        {
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/component/RepeatStatus.java`
#### Snippet
```java
        if (step == -1)
        {
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/UILeaf.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/UILeaf.java`
#### Snippet
```java
    public UIComponent findComponent(String id)
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/UILeaf.java`
#### Snippet
```java
    public String getRendererType()
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/UILeaf.java`
#### Snippet
```java
    public Object processSaveState(FacesContext faces)
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/UILeaf.java`
#### Snippet
```java
    protected FacesListener[] getFacesListeners(Class faces)
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/UILeaf.java`
#### Snippet
```java
    public UIComponent getFacet(String name)
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/UILeaf.java`
#### Snippet
```java
            return location.toString();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/UILeaf.java`
#### Snippet
```java
            return _markCreated;
        }
        return (_attributes == null) ? null : _attributes.get(key);
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/UILeaf.java`
#### Snippet
```java
    public Object saveState(FacesContext faces)
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/UILeaf.java`
#### Snippet
```java
            _markCreated = null;
        }
         return (_attributes == null) ? null : _attributes.remove(key);
    }
         
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/UILeaf.java`
#### Snippet
```java
    protected Renderer getRenderer(FacesContext faces)
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/UILeaf.java`
#### Snippet
```java
    public ValueExpression getValueExpression(String name)
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TagLibraryConfigUnmarshallerImpl.java`
#### Snippet
```java
                return new InputSource(url.toExternalForm());
            }
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/config/ConfigFilesXmlValidationUtils.java`
#### Snippet
```java
        if (stream == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/config/ConfigFilesXmlValidationUtils.java`
#### Snippet
```java
        if (stream == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/config/ConfigFilesXmlValidationUtils.java`
#### Snippet
```java
        public String getStringData()
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/config/ConfigFilesXmlValidationUtils.java`
#### Snippet
```java
                        ClassUtils.getResourceAsStream("org/apache/myfaces/resource/xml.xsd"));
            }
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/config/ConfigFilesXmlValidationUtils.java`
#### Snippet
```java
        public String getEncoding()
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/config/ConfigFilesXmlValidationUtils.java`
#### Snippet
```java
        public Reader getCharacterStream()
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigEntityResolver.java`
#### Snippet
```java
        if (stream == null)
        {
            return null;
        }
        InputSource is = new InputSource(stream);
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/SAXCompiler.java`
#### Snippet
```java
                    AccessController.doPrivileged((PrivilegedExceptionAction) () -> {
                        parser.parse(finalInputStream, handler);
                        return null;
                    });
                }
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/config/DefaultFacesConfigurationProvider.java`
#### Snippet
```java
            {
                log.severe("Faces config resource " + systemId + " not found");
                return null;
            }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/config/DefaultFacesConfigurationProvider.java`
#### Snippet
```java
            }

            return null;
        }
        catch (IOException | SAXException e)
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/component/UIRepeat.java`
#### Snippet
```java
                if (parentSaved == null /*&&_rowDeltaStates.isEmpty()*/ && _rowStates.isEmpty())
                {
                    return null;
                }
                else
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/view/facelets/component/UIRepeat.java`
#### Snippet
```java
                if (parentSaved == null)
                {
                    return null;
                }
                else
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/config/FacesConfigurator.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/config/DefaultFacesConfigurationMerger.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/config/DefaultFacesConfigurationMerger.java`
#### Snippet
```java
                            {
                                //Cyclic reference
                                return null;
                            }
                        }
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/config/DefaultFacesConfigurationMerger.java`
#### Snippet
```java
                            {
                                //Cyclic reference
                                return null;
                            }
                        }
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/config/impl/element/FacesConfigImpl.java`
#### Snippet
```java
            }
        }
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/config/element/FacesConfigData.java`
#### Snippet
```java
    public FaceletsProcessing getFaceletsProcessingConfiguration(String fileExtension)
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigDispenserImpl.java`
#### Snippet
```java
            return localeConfig.getDefaultLocale();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigDispenserImpl.java`
#### Snippet
```java
    {
        Component component = components.get(componentType);
        return component == null ? null : component.getComponentClass();
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/webapp/FacesInitializerFactory.java`
#### Snippet
```java
            }
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        }

        return null;
    }
    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        if (node == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        }

        return StringUtils.isBlank(textContent) ? null : textContent;
    }
}
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/context/ExceptionHandlerImpl.java`
#### Snippet
```java
        }
        
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/context/AjaxExceptionHandlerImpl.java`
#### Snippet
```java
        }
        
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/context/RequestViewContext.java`
#### Snippet
```java
        }

        return null;
    }
    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/context/servlet/SessionMap.java`
#### Snippet
```java
    {
        HttpSession httpSession = httpRequest.getSession(false);
        return httpSession == null ? null : httpSession.getAttribute(key);
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/config/annotation/DefaultAnnotationProvider.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/webapp/FacesInitializerImpl.java`
#### Snippet
```java
            // ignore
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/webapp/FacesInitializerImpl.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/webapp/FacesInitializerImpl.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/context/servlet/StartupFacesContextImpl.java`
#### Snippet
```java
    public Lifecycle getLifecycle()
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/config/annotation/AnnotationConfigurator.java`
#### Snippet
```java
            return createFacesConfig(map);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/resource/ResourceMeta.java`
#### Snippet
```java
    public String getContractName()
    {
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/context/servlet/CookieMap.java`
#### Snippet
```java
        if (cookies == null)
        {
            return null;
        }
        for (int i = 0, len = cookies.length; i < len; i++)
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/context/servlet/CookieMap.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/resource/ContractResourceLoader.java`
#### Snippet
```java
    public String getLibraryVersion(String path)
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/resource/ContractResourceLoader.java`
#### Snippet
```java
    public String getResourceVersion(String path)
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/resource/ContractResourceLoader.java`
#### Snippet
```java
            String libraryVersion, String resourceName, String resourceVersion)
    {
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/context/servlet/FacesContextImplBase.java`
#### Snippet
```java
        if (viewRoot == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/context/servlet/FacesContextImplBase.java`
#### Snippet
```java
        if (renderKitId == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/context/servlet/StartupServletExternalContextImpl.java`
#### Snippet
```java
            throw new UnsupportedOperationException(EXCEPTION_TEXT + _getTime());
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/context/servlet/StartupServletExternalContextImpl.java`
#### Snippet
```java
    public Object getResponse()
    {
        return null;
        //throw new UnsupportedOperationException(EXCEPTION_TEXT + _getTime());
    }
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/context/servlet/StartupServletExternalContextImpl.java`
#### Snippet
```java
    public Object getRequest()
    {
        return null;
        //throw new UnsupportedOperationException(EXCEPTION_TEXT + _getTime());
    }
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/context/servlet/StartupServletExternalContextImpl.java`
#### Snippet
```java
            throw new UnsupportedOperationException(EXCEPTION_TEXT + _getTime());
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/resource/ExternalContextContractResourceLoader.java`
#### Snippet
```java
        catch (MalformedURLException e)
        {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/resource/RootExternalContextResourceLoader.java`
#### Snippet
```java
        catch (MalformedURLException e)
        {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/resource/RootExternalContextResourceLoader.java`
#### Snippet
```java
    public String getLibraryVersion(String path)
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/resource/RootExternalContextResourceLoader.java`
#### Snippet
```java
    public String getResourceVersion(String path)
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/resource/RootExternalContextResourceLoader.java`
#### Snippet
```java
            // the webapp root folder cannot be localized, 
            // but if the resource lives in /contracts/[contract-name] it can be.
            return null;
        }
        return getResourceURL(resourceMeta.getResourceIdentifier());
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/context/flash/FlashImpl.java`
#### Snippet
```java
        if (session == null && !create)
        {
            return null;
        }
        if (session == null && externalContext.getResponse() instanceof HttpServletResponse)
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/context/flash/FlashImpl.java`
#### Snippet
```java
            if (response.isCommitted())
            {
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/context/flash/FlashImpl.java`
#### Snippet
```java
        if (key == null)
        {
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/resource/ExternalContextResourceLoaderIterator.java`
#### Snippet
```java
        if (stack == null || stack.isEmpty())
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/resource/ExternalContextResourceLoaderIterator.java`
#### Snippet
```java
            return path;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/resource/TempDirFileCacheResourceLoader.java`
#### Snippet
```java
        else
        {
            return null;
        }
    }    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/resource/TempDirFileCacheResourceLoader.java`
#### Snippet
```java
        else
        {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/context/servlet/PartialViewContextImpl.java`
#### Snippet
```java
        if (mode == null)
        {
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/resource/ClassLoaderContractResourceLoader.java`
#### Snippet
```java
    public String getLibraryVersion(String path, String contractName)
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/resource/ClassLoaderContractResourceLoader.java`
#### Snippet
```java
    public String getResourceVersion(String path, String contractName)
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/resource/ResourceLoaderUtils.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/resource/ClassLoaderResourceLoader.java`
#### Snippet
```java
    public String getResourceVersion(String path)
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/resource/ClassLoaderResourceLoader.java`
#### Snippet
```java
    public String getLibraryVersion(String path)
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/resource/ClassLoaderResourceLoader.java`
#### Snippet
```java
        if (resourceMeta == null)
        {
            return null;
        }
        return getResourceURL(resourceMeta.getResourceIdentifier());
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/resource/ExternalContextResourceLoader.java`
#### Snippet
```java
        catch (MalformedURLException e)
        {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/resource/TempDirFileCacheContractResourceLoader.java`
#### Snippet
```java
        else
        {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/resource/TempDirFileCacheContractResourceLoader.java`
#### Snippet
```java
        else
        {
            return null;
        }
    }    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/resource/ResourceHandlerCache.java`
#### Snippet
```java
        if (!isResourceHandlerCacheEnabled())
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/resource/ResourceHandlerCache.java`
#### Snippet
```java
        if (!isResourceHandlerCacheEnabled())
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/resource/ResourceHandlerCache.java`
#### Snippet
```java
        if (!isResourceHandlerCacheEnabled())
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/resource/ResourceHandlerCache.java`
#### Snippet
```java
        if (!isResourceHandlerCacheEnabled())
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/resource/ResourceHandlerCache.java`
#### Snippet
```java
        if (!isResourceHandlerCacheEnabled())
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/resource/ResourceLoader.java`
#### Snippet
```java
            String path, int maxDepth, ResourceVisitOption... options)
    {
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/resource/InternalClassLoaderResourceLoader.java`
#### Snippet
```java
    public String getLibraryVersion(String path)
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/resource/InternalClassLoaderResourceLoader.java`
#### Snippet
```java
    public String getResourceVersion(String path)
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/resource/InternalClassLoaderResourceLoader.java`
#### Snippet
```java
                        ResourceUtils.FACES_MINIMAL_JS_RESOURCE_NAME, false);
            }
            return null;
        }
        else if (jakartaFacesLib && !_developmentStage && !_jsfMode.equals(ResourceUtils.JSF_MYFACES_JSFJS_NORMAL) &&
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/resource/InternalClassLoaderResourceLoader.java`
#### Snippet
```java
        else
        {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/resource/ClassLoaderResourceLoaderIterator.java`
#### Snippet
```java
            return delegate.next();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/resource/ClassLoaderResourceLoaderIterator.java`
#### Snippet
```java
            if (stack == null || stack.isEmpty())
            {
                return null;
            }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/resource/ClassLoaderResourceLoaderIterator.java`
#### Snippet
```java
            }

            return null;
        }
        
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/lifecycle/clientwindow/ClientWindowFactoryImpl.java`
#### Snippet
```java
        {
            //No need to do anything
            return null;
        }
        else
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/lifecycle/clientwindow/ClientWindowFactoryImpl.java`
#### Snippet
```java
            }

            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/component/visit/PartialVisitContext.java`
#### Snippet
```java
        if (id != null && !_ids.contains(id))
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/component/visit/PartialVisitContext.java`
#### Snippet
```java
        assert (clientId != null);

        return _clientIds.contains(clientId) ? clientId : null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/component/validate/WholeBeanValidator.java`
#### Snippet
```java
        }

        return null;
    }    
    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/lifecycle/clientwindow/CODIClientSideWindow.java`
#### Snippet
```java
            return windowId;
        }
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/lifecycle/clientwindow/CODIClientSideWindow.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlAjaxBehaviorRenderer.java`
#### Snippet
```java
        if (ajaxBehavior.isDisabled())
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/component/search/SearchExpressionHandlerImpl.java`
#### Snippet
```java
        if (StringUtils.isBlank(value))
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/component/search/SearchExpressionHandlerImpl.java`
#### Snippet
```java
                if (isHintSet(searchExpressionContext, SearchExpressionHint.IGNORE_NO_RESULT))
                {
                    return null;
                }
                else
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/component/search/SearchExpressionHandlerImpl.java`
#### Snippet
```java
            component = component.getParent();
        } while (component != null);
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlResponseStateManager.java`
#### Snippet
```java
        if(encodedState==null || (((String) encodedState).length() == 0))
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlResponseStateManager.java`
#### Snippet
```java
        if (savedState == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlResponseStateManager.java`
#### Snippet
```java
        if (baseState == null)
        {
            return null;
        }
        if (facesContext.getViewRoot().isTransient())
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlResponseStateManager.java`
#### Snippet
```java
        if (facesContext.getViewRoot().isTransient())
        {
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/renderkit/RendererUtils.java`
#### Snippet
```java
        if (submittedValue == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/renderkit/RendererUtils.java`
#### Snippet
```java
        if (ve == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/renderkit/RendererUtils.java`
#### Snippet
```java
        if (valueType == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/renderkit/RendererUtils.java`
#### Snippet
```java
        if (String.class.equals(arrayComponentType))
        {
            return null; //No converter needed for String type
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/renderkit/RendererUtils.java`
#### Snippet
```java
                    "No Converter for type " + arrayComponentType.getName() + " found",
                    e);
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/renderkit/RendererUtils.java`
#### Snippet
```java
            return value.toString();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/renderkit/RendererUtils.java`
#### Snippet
```java
        if (submittedValues == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/renderkit/RendererUtils.java`
#### Snippet
```java
                if (value == null)
                {
                    return null;
                }
                
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/renderkit/RendererUtils.java`
#### Snippet
```java
        if (expression == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/renderkit/RendererUtils.java`
#### Snippet
```java
        if (valueType == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/renderkit/RendererUtils.java`
#### Snippet
```java
        if (Object.class.equals(valueType))
        {
            return null; //There is no converter for Object class
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/renderkit/RendererUtils.java`
#### Snippet
```java
        {
            log.log(Level.SEVERE, "No Converter for type " + valueType.getName() + " found", e);
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/renderkit/RendererUtils.java`
#### Snippet
```java
        if (o == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/renderkit/RendererUtils.java`
#### Snippet
```java
        if (uri.length() == 0)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlCheckboxRendererBase.java`
#### Snippet
```java
            return HtmlRendererUtils.findUIOutputConverterFailSafe(facesContext, component);
        }
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMenuRendererBase.java`
#### Snippet
```java
            return HtmlRendererUtils.findUIOutputConverterFailSafe(facesContext, component);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlListboxRendererBase.java`
#### Snippet
```java
            return HtmlRendererUtils.findUIOutputConverterFailSafe(facesContext, component);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlInputFileRendererBase.java`
#### Snippet
```java
            if (isEmpty(part))
            {
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlRenderer.java`
#### Snippet
```java
        if (component.getChildCount() == 0)
        {
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/ResourceUtils.java`
#### Snippet
```java
            }
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/HttpPartWrapper.java`
#### Snippet
```java
    public Object saveState(FacesContext context)
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTableRendererBase.java`
#### Snippet
```java
    protected UIComponent getNewspaperTableSpacer(UIComponent component)
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTableRendererBase.java`
#### Snippet
```java
            if(!hasRowStyle())
            {
                return null;
            }
            return _rowStyle[idx % _rowStyle.length];
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTableRendererBase.java`
#### Snippet
```java
            if(!hasColumnStyle())
            {
                return null;
            }
            //return _columnStyle[idx % _columnStyle.length];
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTableRendererBase.java`
#### Snippet
```java
                return _columnStyle[idx];
            }
            return null;   
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/OutcomeTargetUtils.java`
#### Snippet
```java
            log.warning("Could not determine NavigationCase for UIOutcomeTarget component "
                    + ComponentUtils.getPathToComponent(component) + " with outcome " + outcome);
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/HTMLEncoder.java`
#### Snippet
```java
        catch (IOException e)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/application/ViewIdSupport.java`
#### Snippet
```java
        if (rawViewId == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/application/ViewIdSupport.java`
#### Snippet
```java
        if (viewId != null && checkViewExists)
        {
            return isViewExistent(context, viewId) ? viewId : null;
        }
        
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/application/ViewIdSupport.java`
#### Snippet
```java

        //Otherwise return null.
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/application/ViewResourceIterator.java`
#### Snippet
```java
        }

        return null;
    }
}
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/application/ViewHandlerImpl.java`
#### Snippet
```java
            // If there is no VDL that can handle the view, throw 404 response.
            sendSourceNotFound(context, viewId);
            return null;
        }
        return vdl.createView(context,calculatedViewId);
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/application/ViewHandlerImpl.java`
#### Snippet
```java
            // If there is no VDL that can handle the view, throw 404 response.
            sendSourceNotFound(context, viewId);
            return null;
            
        }
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/renderkit/ErrorPageWriter.java`
#### Snippet
```java
            return location.toString();
        }
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/renderkit/ErrorPageWriter.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/renderkit/ErrorPageWriter.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/application/FacesServletMappingUtils.java`
#### Snippet
```java
        }
        
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/application/FacesServletMappingUtils.java`
#### Snippet
```java
        }
        
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/application/FacesServletMappingUtils.java`
#### Snippet
```java
        }

        return null;
    }
    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/application/viewstate/KeyFactoryRandom.java`
#### Snippet
```java
            // ViewExpiredException
        }
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/application/viewstate/StateTokenProcessorServerSide.java`
#### Snippet
```java
            // Should not happen, because ResponseStateManager.isStateless(context,viewId) should
            // catch it first
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/application/viewstate/StateCacheUtils.java`
#### Snippet
```java
                }
            }
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/application/viewstate/SerializedViewCollection.java`
#### Snippet
```java
        {
            // Remember inside the state map null is stored as an empty array.
            return null;
        }
        return value;
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/application/viewstate/SerializedViewCollection.java`
#### Snippet
```java
            return _lastWindowKeys.get(id);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/application/viewstate/StateTokenProcessorClientSide.java`
#### Snippet
```java
            // Should not happen, because ResponseStateManager.isStateless(context,viewId) should
            // catch it first
            return null;
        }
        Object savedStateObject = StateUtils.reconstruct((String)token, facesContext.getExternalContext());
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/application/viewstate/StateCacheClientSide.java`
#### Snippet
```java
            {
                //If no timestamp, state is invalid.
                return null;
            }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/application/viewstate/StateCacheClientSide.java`
#### Snippet
```java
            {
                //expire
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/application/viewstate/StateCacheClientSide.java`
#### Snippet
```java
        {
            //invalid viewId, expire
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/application/viewstate/StateCacheClientSide.java`
#### Snippet
```java
            {
                // Remember inside the state null is stored as an empty array.
                return null;
            }
            
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
        if (resourceName.length() == 0)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
        if (!ResourceValidationUtils.isValidResourceName(resourceName))
        {
            return null;
        }
        if (libraryName != null && !ResourceValidationUtils.isValidLibraryName(
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
                libraryName, isAllowSlashesLibraryName()))
        {
            return null;
        }
        FacesContext facesContext = FacesContext.getCurrentInstance();
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
        if (!ResourceValidationUtils.isValidViewResource(resourceName))
        {
            return null;
        }
        final String localePrefix = getLocalePrefixForLocateResource(facesContext);
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
        if (resourceName == null)
        {
            return null;
        }
        if (!ResourceValidationUtils.isValidResourceName(resourceName))
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
        if (!ResourceValidationUtils.isValidResourceName(resourceName))
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
                && !ResourceValidationUtils.isValidLibraryName(libraryName, isAllowSlashesLibraryName()))
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
            !ResourceValidationUtils.isValidContractName(contractName))
        {
            return null;
        }
        return contractName;
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
            return ResourceUtils.DEFAULT_STYLESHEET_RENDERER_TYPE;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
        if (resourceName == null)
        {
            return null;
        }
        if (!ResourceValidationUtils.isValidResourceName(resourceName))
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
        if (!ResourceValidationUtils.isValidResourceName(resourceName))
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
                libraryName, isAllowSlashesLibraryName()))
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
        }

        return null;
    }
    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
        if (!ResourceValidationUtils.isValidResourceId(resourceId))
        {
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
                if (!ResourceValidationUtils.isValidLocalePrefix(localePrefix))
                {
                    return null;
                }
                return localePrefix;
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/application/viewstate/KeyFactorySecureRandom.java`
#### Snippet
```java
            // ViewExpiredException
        }
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/application/viewstate/StateUtils.java`
#### Snippet
```java
                log.log(Level.FINE, "View State cannot be reconstructed", e);
            }
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/el/FacesScopedAttributeELResolver.java`
#### Snippet
```java
        if (base != null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/el/FacesScopedAttributeELResolver.java`
#### Snippet
```java
        if (base != null)
        {
            return null;
        }
        if (property == null)
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/el/FacesScopedAttributeELResolver.java`
#### Snippet
```java
        }

        return null;

    }
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/el/FacesScopedAttributeELResolver.java`
#### Snippet
```java
        if (base != null)
        {
            return null;
        }
        if (property == null)
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/el/FacesScopedAttributeELResolver.java`
#### Snippet
```java
        if (base != null)
        {
            return null;
        }
        else
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/application/viewstate/StateCacheServerSide.java`
#### Snippet
```java

        return (serverStateId == null)
                ? null
                : getSerializedViewFromSession(facesContext, viewId, serverStateId);
    }
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/application/viewstate/StateCacheServerSide.java`
#### Snippet
```java
            {
                log.log(Level.SEVERE, "Exiting serializeView - Could not serialize state: " + e.getMessage(), e);
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/application/viewstate/StateCacheServerSide.java`
#### Snippet
```java
            return sessionViewStorageFactory.getKeyFactory().decode((String) state);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/application/viewstate/StateCacheServerSide.java`
#### Snippet
```java
            {
                log.log(Level.SEVERE, "Exiting deserializeView - Could not deserialize state: " + e.getMessage(), e);
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/application/viewstate/StateCacheServerSide.java`
#### Snippet
```java
        {
            log.severe("Exiting deserializeView - this method should not be called with a null-state.");
            return null;
        }
        else
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/application/viewstate/StateCacheServerSide.java`
#### Snippet
```java
            log.severe("Exiting deserializeView - this method should not be called with a state of type : "
                       + state.getClass());
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/el/FacesVariableResolverChainWrapper.java`
#### Snippet
```java
        if (base != null)
        {
            return null;
        }
        else
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/el/FacesVariableResolverChainWrapper.java`
#### Snippet
```java
    {

        return null;

    }
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/el/FacesVariableResolverChainWrapper.java`
#### Snippet
```java
            throw new PropertyNotFoundException("No property specified");
        }
        return null;

    }
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/el/ReservedWordsELResolver.java`
#### Snippet
```java
            return VALUES.get(property);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/el/ReservedWordsELResolver.java`
#### Snippet
```java
    public Class<?> getCommonPropertyType(ELContext context, Object base)
    {
        return base == null ? String.class : null;
    }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/el/ReservedWordsELResolver.java`
#### Snippet
```java
            context.setPropertyResolved(true);
            Object value = VALUES.get(property);
            return value == null ? null : value.getClass();
        }
        return null;
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/el/ReservedWordsELResolver.java`
#### Snippet
```java
            return value == null ? null : value.getClass();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/el/MockExpressionFactory.java`
#### Snippet
```java
            if (object == null || "".equals(object))
            {
                return null;
            }
            if (targetType.isAssignableFrom(object.getClass()))
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/el/MockExpressionFactory.java`
#### Snippet
```java
        if (object == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/el/FacesResourceBundleELResolver.java`
#### Snippet
```java
        if (base != null)
        {
            return null;
        }
        else
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/el/FacesResourceBundleELResolver.java`
#### Snippet
```java
        if (base != null)
        {
            return null;
        }
        if (property == null)
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/el/FacesResourceBundleELResolver.java`
#### Snippet
```java
            return bundle;
        }
        return null;

    }
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/el/FacesResourceBundleELResolver.java`
#### Snippet
```java
        if (base != null)
        {
            return null;
        }
        if (property == null)
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/el/FacesResourceBundleELResolver.java`
#### Snippet
```java
            return ResourceBundle.class;
        }
        return null;

    }
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/el/FacesResourceBundleELResolver.java`
#### Snippet
```java
        if (base != null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/el/ExpressionTokenizer.java`
#### Snippet
```java
            pos.setIndex(start);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/el/MockCompositeValueExpression.java`
#### Snippet
```java
            else
            {
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/el/MockCompositeValueExpression.java`
#### Snippet
```java
        {
        case 0:
            return null;
        case 1:
            return valueExpressionChain.get(0).getType(context);
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/el/FacesImplicitObjectELResolver.java`
#### Snippet
```java
        if (base != null)
        {
            return null;
        }
        if (property == null)
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/el/FacesImplicitObjectELResolver.java`
#### Snippet
```java
            }
        }
        return null;

    }
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/el/FacesImplicitObjectELResolver.java`
#### Snippet
```java
        if (base != null)
        {
            return null;
        }
        else
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/el/FacesImplicitObjectELResolver.java`
#### Snippet
```java
        if (base != null)
        {
            return null;
        }
        if (property == null)
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/el/FacesImplicitObjectELResolver.java`
#### Snippet
```java
        }

        return null;

    }
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/el/FacesImplicitObjectELResolver.java`
#### Snippet
```java
        if (base != null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/application/ApplicationImpl.java`
#### Snippet
```java
        if (obj == null)
        {
            return null;    //object for this id wasn't found on the map
        }
        
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/application/ApplicationImpl.java`
#### Snippet
```java
        //object stored in the map for this id is an invalid type.  remove it and return null
        classMap.remove(id);
        return null;        
    }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/application/ApplicationImpl.java`
#### Snippet
```java
        Class<?> superClazz = targetClass.getSuperclass();

        return superClazz != null ? internalCreateConverter(superClazz) : null;

    }
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/application/ApplicationImpl.java`
#### Snippet
```java
        public String getFamily()
        {
            return null;
        }
    };
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/application/ApplicationImpl.java`
#### Snippet
```java
        if (bundleName == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/application/ApplicationImpl.java`
#### Snippet
```java
    {
        ResourceBundle bundle = _runtimeConfig.getResourceBundle(name);
        return bundle != null ? bundle.getBaseName() : null;
    }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractHtmlUnitTestCase.java`
#### Snippet
```java
            }
        }
        return (null);

    }
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractHtmlUnitTestCase.java`
#### Snippet
```java
            }
        }
        return (null);

    }
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractHtmlUnitTestCase.java`
#### Snippet
```java
        catch (ElementNotFoundException e)
        {
            return (null);
        }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractHtmlUnitTestCase.java`
#### Snippet
```java
            }
        }
        return (null);

    }
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/_SessionMap.java`
#### Snippet
```java
    {
        final HttpSession httpSession = _getSession();
        return (httpSession == null) ? null : httpSession.getAttribute(key);
    }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/base/junit/runners/TestPerClassLoaderRunner.java`
#### Snippet
```java
        if (!archive.exists())
        {
            return null;
        }
        ZipFile zipFile = null;
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/base/junit/runners/TestPerClassLoaderRunner.java`
#### Snippet
```java
        catch (IOException io)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/base/junit/runners/TestPerClassLoaderRunner.java`
#### Snippet
```java
        if (entry == null)
        {
            return null;
        }
        try
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/base/junit/runners/TestPerClassLoaderRunner.java`
#### Snippet
```java
                    e);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockExceptionHandler.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpSession.java`
#### Snippet
```java
            return ((MockServletContext)this.servletContext).getWebContainer();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/base/junit/runners/TestClassLoader.java`
#### Snippet
```java
            return getClassData(file);
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/base/junit/runners/TestClassLoader.java`
#### Snippet
```java
        if (!archive.exists())
        {
            return null;
        }
        try
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/base/junit/runners/TestClassLoader.java`
#### Snippet
```java
        catch (IOException io)
        {
            return null;
        }
        ZipEntry entry = zipFile.getEntry(fileName);
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/base/junit/runners/TestClassLoader.java`
#### Snippet
```java
        if (entry == null)
        {
            return null;
        }
        int size = (int) entry.getSize();
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/base/junit/runners/TestClassLoader.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/base/junit/runners/TestClassLoader.java`
#### Snippet
```java
        {
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockPortletRequest.java`
#### Snippet
```java
        else
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockPortletRequest.java`
#### Snippet
```java
        else
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockPortletContext.java`
#### Snippet
```java
        {
        }
        return null;

    }
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockPortletContext.java`
#### Snippet
```java
            else
            {
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockPortletContext.java`
#### Snippet
```java
        else
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockPortletContext.java`
#### Snippet
```java
        if (documentRoot == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockPortletContext.java`
#### Snippet
```java
        if (!node.exists())
        {
            return null;
        }
        if (!node.isDirectory())
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockPortletContext.java`
#### Snippet
```java
        if (!node.isDirectory())
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/_Hex.java`
#### Snippet
```java
        if (string == null)
        {
            return null;
        }
        try
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockPortletContext.java`
#### Snippet
```java
        if (files == null)
        {
            return null;
        }
        for (int i = 0; i < files.length; i++)
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockPortletContext.java`
#### Snippet
```java
        else
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockPortletContext.java`
#### Snippet
```java
        if (period < 0)
        {
            return null;
        }
        String extension = path.substring(period + 1);
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication12.java`
#### Snippet
```java
            if (newName == null)
            {
                return null;
            }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication12.java`
#### Snippet
```java
            catch (MissingResourceException exc)
            {
                return null;
            }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockFlowHandler.java`
#### Snippet
```java
    public String getLastDisplayedViewId(FacesContext context)
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockFlowHandler.java`
#### Snippet
```java
    public Flow getCurrentFlow(FacesContext context)
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockFlowHandler.java`
#### Snippet
```java
    public Flow getFlow(FacesContext context, String definingDocumentId, String id)
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockFlash.java`
#### Snippet
```java
        if (key == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockClientWindow.java`
#### Snippet
```java
            return windowId;
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockDefaultViewDeclarationLanguage.java`
#### Snippet
```java
            throw new RuntimeException();
        }
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockDefaultViewDeclarationLanguage.java`
#### Snippet
```java
            Resource componentResource)
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockDefaultViewDeclarationLanguage.java`
#### Snippet
```java
    public ViewMetadata getViewMetadata(FacesContext context, String viewId)
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockDefaultViewDeclarationLanguage.java`
#### Snippet
```java
            Resource componentResource)
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockDefaultViewDeclarationLanguage.java`
#### Snippet
```java
            FacesContext context, String viewId)
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockDefaultViewDeclarationLanguage.java`
#### Snippet
```java
                
                // TODO: VALIDATE - Spec is silent on the return value when an error was sent
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockFacesContext10.java`
#### Snippet
```java
        if (vr == null)
        {
            return null;
        }
        String renderKitId = vr.getRenderKitId();
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockFacesContext10.java`
#### Snippet
```java
        if (renderKitId == null)
        {
            return null;
        }
        RenderKitFactory rkFactory = (RenderKitFactory) FactoryFinder
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockClientWindowFactory.java`
#### Snippet
```java
        {
            //No need to do anything
            return null;
        }
        else
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockClientWindowFactory.java`
#### Snippet
```java
            else
            {
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockServletContext.java`
#### Snippet
```java
            @Override public String getDomain()
            {
                return null;
            }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockServletContext.java`
#### Snippet
```java
        else
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockServletContext.java`
#### Snippet
```java
            return documentRoot.toURI();
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockServletContext.java`
#### Snippet
```java
            @Override public String getComment()
            {
                return null;
            }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockServletContext.java`
#### Snippet
```java
            @Override public String getName()
            {
                return null;
            }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockServletContext.java`
#### Snippet
```java
            //No op
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockServletContext.java`
#### Snippet
```java
            else
            {
                return null;
            }
        }
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockServletContext.java`
#### Snippet
```java
        else
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockServletContext.java`
#### Snippet
```java
            @Override public String getAttribute(String s)
            {
                return null;
            }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockServletContext.java`
#### Snippet
```java
        if (documentRoot == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockServletContext.java`
#### Snippet
```java
        if (!node.exists())
        {
            return null;
        }
        if (!node.isDirectory())
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockServletContext.java`
#### Snippet
```java
        if (!node.isDirectory())
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockServletContext.java`
#### Snippet
```java
        if (files == null)
        {
            return null;
        }
        for (int i = 0; i < files.length; i++)
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockServletContext.java`
#### Snippet
```java
            @Override public String getPath()
            {
                return null;
            }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockServletContext.java`
#### Snippet
```java
                {
                    this.log(e.getMessage());
                    return null;
                }
            }
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockServletContext.java`
#### Snippet
```java

        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockServletContext.java`
#### Snippet
```java
        if (period < 0)
        {
            return null;
        }
        String extension = path.substring(period + 1);
```

### ReturnNull
Return of `null`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/ClientBehaviorRendererUtils.java`
#### Snippet
```java
        if (params == null || params.isEmpty())
        {
            return null;
        }
        
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletRequest.java`
#### Snippet
```java
        else
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletRequest.java`
#### Snippet
```java
        else
        {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletRequest.java`
#### Snippet
```java
        else
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletRequest.java`
#### Snippet
```java
            }
        }
        return null;

    }
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletRequest.java`
#### Snippet
```java
            return ((MockServletContext)this.servletContext).getWebContainer();
        }
        return null;
    }
    
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockResponseWriter.java`
#### Snippet
```java
                else
                {
                    return null;
                }
            }
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockResponseWriter.java`
#### Snippet
```java

        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletResponse.java`
#### Snippet
```java
            }
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/_CookieMap.java`
#### Snippet
```java
        if (cookies == null)
        {
            return null;
        }
        for (int i = 0, len = cookies.length; i < len; i++)
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/_CookieMap.java`
#### Snippet
```java
        }

        return null;
    }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockResponseStateManager.java`
#### Snippet
```java
        if (state == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockResponseStateManager.java`
#### Snippet
```java
        if (savedState == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockResponseStateManager.java`
#### Snippet
```java
        if (savedState == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockResponseStateManager.java`
#### Snippet
```java
        if (savedState == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockResponseStateManager.java`
#### Snippet
```java
        if (encodedState == null || (((String) encodedState).length() == 0))
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockResponseStateManager.java`
#### Snippet
```java
        if (savedState == null)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockResponseStateManager.java`
#### Snippet
```java
        if (restoredViewId == null)
        {
            return null;
        }
        return savedState;
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockFacesContext.java`
#### Snippet
```java
    public Lifecycle getLifecycle()
    {
        return null;
    }
}
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication10.java`
#### Snippet
```java
        if (converterClass == null)
        {
            return null;
        }
        try
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication10.java`
#### Snippet
```java
        if (converterClass == null)
        {
            return null;
        }
        try
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/_ELText.java`
#### Snippet
```java
        public Class<?> getType(ELContext context)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/_ELText.java`
#### Snippet
```java
            }

            return null;
        }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/_ELText.java`
#### Snippet
```java
        public Class<?> getExpectedType()
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/_ELText.java`
#### Snippet
```java
        public Object getValue(ELContext context)
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/_ELText.java`
#### Snippet
```java
        if (text.size() == 0)
        {
            return null;
        }
        else if (text.size() == 1)
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/resource/MockSimpleResource.java`
#### Snippet
```java
        catch (MalformedURLException e)
        {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/resource/MockClassLoaderResourceLoader.java`
#### Snippet
```java
    public String getResourceVersion(String path)
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/resource/MockClassLoaderResourceLoader.java`
#### Snippet
```java
    public String getLibraryVersion(String path)
    {
        return null;
    }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/resource/MockExternalContextResourceLoader.java`
#### Snippet
```java
        catch (MalformedURLException e)
        {
            return null;
        }
    }
```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/resource/MockSimpleResourceHandler.java`
#### Snippet
```java
            return "jakarta.faces.resource.Stylesheet";
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/resource/MockResourceHandler.java`
#### Snippet
```java
            return "jakarta.faces.resource.Stylesheet";
        }
        return null;
    }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/visit/PartialVisitContext.java`
#### Snippet
```java
        if ((id != null) && !_ids.contains(id))
        {
            return null;
        }

```

### ReturnNull
Return of `null`
in `test/src/main/java/org/apache/myfaces/test/mock/visit/PartialVisitContext.java`
#### Snippet
```java
        assert (clientId != null);

        return _clientIds.contains(clientId) ? clientId : null;
    }

```

### ReturnNull
Return of `null`
in `api/src/main/java/jakarta/faces/application/ViewHandler.java`
#### Snippet
```java
        // because they don't provide any ViewDeclarationLanguage,
        // but in the default implementation (ViewHandlerImpl) we return vdlFactory.getViewDeclarationLanguage(viewId)
        return null;
    }

```

## RuleId[ruleID=UnnecessaryLocalVariable]
### UnnecessaryLocalVariable
Local variable `injectionProvider` is redundant
in `api/src/main/java/jakarta/faces/FactoryFinder.java`
#### Snippet
```java
                    _FactoryFinderProviderFactory.INJECTION_PROVIDER_FACTORY_GET_INSTANCE_METHOD
                        .invoke(_FactoryFinderProviderFactory.INJECTION_PROVIDER_CLASS);
                Object injectionProvider = 
                    _FactoryFinderProviderFactory.INJECTION_PROVIDER_FACTORY_GET_INJECTION_PROVIDER_METHOD
                        .invoke(injectionProviderFactory, facesContext.getExternalContext());
```

### UnnecessaryLocalVariable
Local variable `facesContext` is redundant
in `api/src/main/java/jakarta/faces/webapp/FacesServlet.java`
#### Snippet
```java
    private FacesContext prepareFacesContext(ServletRequest request, ServletResponse response)
    {
        FacesContext facesContext =
                _facesContextFactory.getFacesContext(_servletConfig.getServletContext(), request, response, _lifecycle);
        return facesContext;
```

### UnnecessaryLocalVariable
Local variable `serializableMap` is redundant
in `api/src/main/java/jakarta/faces/component/_DeltaStateHelper.java`
#### Snippet
```java
            Map<Serializable, Object> targetMap)
    {
        Map serializableMap = sourceMap;
        Map.Entry<Serializable, Object> entry;

```

### UnnecessaryLocalVariable
Local variable `value` is redundant
in `impl/src/main/java/org/apache/myfaces/el/resolver/FlashELResolver.java`
#### Snippet
```java
                flash.keep(strProperty);
                //Obtain the value on requestMap if any
                Object value = externalContext.getRequestMap().get(strProperty);
                return value;
            }
```

### UnnecessaryLocalVariable
Local variable `ve` is redundant
in `impl/src/main/java/org/apache/myfaces/el/resolver/CompositeComponentELResolver.java`
#### Snippet
```java
        public ValueExpression getExpression(String name)
        {
            ValueExpression ve = component.getValueExpression(name);
            return ve;
        }
```

### UnnecessaryLocalVariable
Local variable `hash` is redundant
in `impl/src/main/java/org/apache/myfaces/cdi/util/ParameterizedTypeImpl.java`
#### Snippet
```java
    public int hashCode()
    {
        int hash = 7;
        return hash;
    }
```

### UnnecessaryLocalVariable
Local variable `instance` is redundant
in `impl/src/main/java/org/apache/myfaces/cdi/util/CDIUtils.java`
#### Snippet
```java
        Bean<?> bean = bm.resolve(beans);
        CreationalContext<?> cc = bm.createCreationalContext(bean);
        T instance = (T) bm.getReference(bean, type, cc);
        return instance;

```

### UnnecessaryLocalVariable
Local variable `currentFlowStack` is redundant
in `impl/src/main/java/org/apache/myfaces/flow/FlowHandlerImpl.java`
#### Snippet
```java
        Map<String, Object> sessionMap = context.getExternalContext().getSessionMap();
        String currentFlowMapKey = CURRENT_FLOW_STACK + clientWindow.getId();
        List<_FlowContextualInfo> currentFlowStack = (List<_FlowContextualInfo>) sessionMap.get(currentFlowMapKey);
        return currentFlowStack;
    }
```

### UnnecessaryLocalVariable
Local variable `currentFlowStack` is redundant
in `impl/src/main/java/org/apache/myfaces/flow/FlowHandlerImpl.java`
#### Snippet
```java
        Map<Object, Object> attributesMap = context.getAttributes();
        String currentFlowMapKey = stackKey + clientWindow.getId();
        List<_FlowContextualInfo> currentFlowStack = (List<_FlowContextualInfo>) attributesMap.get(currentFlowMapKey);
        return currentFlowStack;
    }
```

### UnnecessaryLocalVariable
Local variable `info` is redundant
in `impl/src/main/java/org/apache/myfaces/flow/FlowHandlerImpl.java`
#### Snippet
```java
            if (returnFlowList != null && !returnFlowList.isEmpty())
            {
                _FlowContextualInfo info = returnFlowList.get(returnFlowList.size()-1);
                return info;
            }
```

### UnnecessaryLocalVariable
Local variable `len` is redundant
in `impl/src/main/java/org/apache/myfaces/util/lang/StreamCharBuffer.java`
#### Snippet
```java
                        || csq instanceof StringBuilder)
                {
                    int len = end - start;
                    int charsLeft = len;
                    int currentOffset = start;
```

### UnnecessaryLocalVariable
Local variable `ta` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ELText.java`
#### Snippet
```java
        else
        {
            ELText[] ta = (ELText[]) text.toArray(new ELText[text.size()]);
            return ta;
        }
```

### UnnecessaryLocalVariable
Local variable `parent` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/PartialStateManagementStrategy.java`
#### Snippet
```java
                    // in the component tree, so what we need to do as a fallback is replace the
                    // component in the spot with the restored version.
                    UIComponent parent = target;
                    if (parent.getChildCount() > 0)
                    {
```

### UnnecessaryLocalVariable
Local variable `beanInfo` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeComponentDefinitionTagHandler.java`
#### Snippet
```java
    {
        BeanDescriptor descriptor = new BeanDescriptor(parent.getClass());
        CompositeComponentBeanInfo beanInfo = new CompositeComponentBeanInfo(descriptor);
        return beanInfo;
    }
```

### UnnecessaryLocalVariable
Local variable `metadata` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeMetaRulesetImpl.java`
#### Snippet
```java
        Map<String, Object> applicationMap = facesContext.getExternalContext().getApplicationMap();

        Map<String, MetadataTarget> metadata = (Map<String, MetadataTarget>) applicationMap.computeIfAbsent(
                METADATA_KEY, k -> new HashMap<>());

```

### UnnecessaryLocalVariable
Local variable `f` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFaceletFactory.java`
#### Snippet
```java
        {
            Compiler.CompilerResult result = _compiler.compileCompositeComponentMetadata(url, alias);
            DefaultFacelet f = new DefaultFacelet(this, _compiler.createExpressionFactory(), url, alias,
                    alias, result.getFaceletHandler(), true, viewUniqueIdsCacheEnabled, result.getDoctype());
            return f;
```

### UnnecessaryLocalVariable
Local variable `f` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFaceletFactory.java`
#### Snippet
```java
        {
            Compiler.CompilerResult result = _compiler.compile(url, alias);
            DefaultFacelet f = new DefaultFacelet(this, _compiler.createExpressionFactory(), url, alias, alias,
                    result.getFaceletHandler(), viewUniqueIdsCacheEnabled, result.getDoctype());
            return f;
```

### UnnecessaryLocalVariable
Local variable `f` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFaceletFactory.java`
#### Snippet
```java
        {
            Compiler.CompilerResult result = _compiler.compileViewMetadata(url, alias);
            DefaultFacelet f = new DefaultFacelet(this, _compiler.createExpressionFactory(), url, alias, 
                    faceletId, result.getFaceletHandler(), viewUniqueIdsCacheEnabled, result.getDoctype());
            return f;
```

### UnnecessaryLocalVariable
Local variable `size` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/FaceletCompositionContextImpl.java`
#### Snippet
```java
            if (childCount > 0)
            {
                for (int i = 0, size = childCount; i < size; i++)
                {
                    UIComponent child = component.getChildren().get(i);
```

### UnnecessaryLocalVariable
Local variable `urlArray` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/util/Classpath.java`
#### Snippet
```java
        _searchResource(all, loader, prefix + "MANIFEST.MF", prefix, suffix);

        URL[] urlArray = (URL[]) all.toArray(new URL[all.size()]);

        return urlArray;
```

### UnnecessaryLocalVariable
Local variable `alias` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/SAXCompiler.java`
#### Snippet
```java
        String taglibURI, String tagName, Map<String, Object> attributes)
    {
        String alias = tagName;
        CompilationManager mngr = new CompilationManager(alias, this, getDefaultFaceletsProcessingInstructions());
        String prefix = "oamf"; // The prefix is only a logical name.
```

### UnnecessaryLocalVariable
Local variable `contextSpecifiedList` is redundant
in `impl/src/main/java/org/apache/myfaces/config/FacesConfigurator.java`
#### Snippet
```java
        if (directoryPaths != null)
        {
            List<String> contextSpecifiedList = configFilesList;
            for (String dirPath : directoryPaths)
            {
```

### UnnecessaryLocalVariable
Local variable `clone` is redundant
in `impl/src/main/java/org/apache/myfaces/context/RequestViewContext.java`
#### Snippet
```java
    public static RequestViewContext newInstance(RequestViewMetadata rvm)
    {
        RequestViewContext clone = new RequestViewContext(rvm.cloneInstance());
        return clone;
    }
```

### UnnecessaryLocalVariable
Local variable `beanManager` is redundant
in `impl/src/main/java/org/apache/myfaces/webapp/FacesInitializerImpl.java`
#### Snippet
```java

                Method getBeanManagerMethod = cdiClass.getMethod("getBeanManager");
                Object beanManager = getBeanManagerMethod.invoke(cdi);
                return beanManager;
            }
```

### UnnecessaryLocalVariable
Local variable `path` is redundant
in `impl/src/main/java/org/apache/myfaces/resource/ClassLoaderResourceLoaderIterator.java`
#### Snippet
```java
            {
                // Calculate name based on url, basePath.
                String path = calculatePath(file);
                return path;
            }
```

### UnnecessaryLocalVariable
Local variable `splittedExpressions` is redundant
in `impl/src/main/java/org/apache/myfaces/component/search/SearchExpressionHandlerImpl.java`
#### Snippet
```java
    {
        // split expressions by blank or comma (and ignore blank and commas inside brackets)
        String[] splittedExpressions = split(context, expressions, EXPRESSION_SEPARATOR_CHARS);
        return splittedExpressions;
    }
```

### UnnecessaryLocalVariable
Local variable `id` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlResponseStateManager.java`
#### Snippet
```java
        facesContext.getAttributes().put(VIEW_STATE_COUNTER, count);

        String id = facesContext.getViewRoot().getContainerClientId(facesContext) + 
            separator + ResponseStateManager.VIEW_STATE_PARAM + separator + count;
        return id;
```

### UnnecessaryLocalVariable
Local variable `savedStateObject` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlResponseStateManager.java`
#### Snippet
```java
        }

        Object savedStateObject = getStateCache(facesContext).getStateTokenProcessor(facesContext)
                .decode(facesContext, (String)encodedState);
        
```

### UnnecessaryLocalVariable
Local variable `id` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlResponseStateManager.java`
#### Snippet
```java
        facesContext.getAttributes().put(CLIENT_WINDOW_COUNTER, count);

        String id = facesContext.getViewRoot().getContainerClientId(facesContext) + 
            separator + ResponseStateManager.CLIENT_WINDOW_PARAM + separator + count;
        return id;
```

### UnnecessaryLocalVariable
Local variable `newWriter` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/html/EarlyFlushHtmlResponseWriterImpl.java`
#### Snippet
```java
    public ResponseWriter cloneWithWriter(Writer writer)
    {
        EarlyFlushHtmlResponseWriterImpl newWriter
                = new EarlyFlushHtmlResponseWriterImpl(writer, getContentType(), getCharacterEncoding(), 
                        getWrapScriptContentWithXmlCommentTag(), getWriterContentTypeMode());
```

### UnnecessaryLocalVariable
Local variable `value` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/RendererUtils.java`
#### Snippet
```java
    private static Object getValue(UIComponent component)
    {
        Object value = ((ValueHolder) component).getValue();
        return value;
    }
```

### UnnecessaryLocalVariable
Local variable `newWriter` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlResponseWriterImpl.java`
#### Snippet
```java
    public ResponseWriter cloneWithWriter(Writer writer)
    {
        HtmlResponseWriterImpl newWriter = new HtmlResponseWriterImpl(writer, getContentType(),
                getCharacterEncoding(), _wrapScriptContentWithXmlCommentTag, _writerContentTypeMode);
        return newWriter;
```

### UnnecessaryLocalVariable
Local variable `url` is redundant
in `impl/src/main/java/org/apache/myfaces/application/ViewHandlerImpl.java`
#### Snippet
```java
    public String getWebsocketURL(FacesContext context, String channelAndToken)
    {
        String url = context.getExternalContext().getRequestContextPath() + 
                PushContext.URI_PREFIX + "/" + channelAndToken;
        return url;
```

### UnnecessaryLocalVariable
Local variable `tokenEncoded` is redundant
in `impl/src/main/java/org/apache/myfaces/application/viewstate/StateTokenProcessorServerSide.java`
#### Snippet
```java
            byte[] tokenBytes = token.getBytes(StateUtils.ZIP_CHARSET);
            byte[] tokenBytesEncoded = StateUtils.encode(tokenBytes);
            String tokenEncoded = new String(tokenBytesEncoded, StateUtils.ZIP_CHARSET);

            return tokenEncoded;
```

### UnnecessaryLocalVariable
Local variable `tokenDecoded` is redundant
in `impl/src/main/java/org/apache/myfaces/application/viewstate/StateTokenProcessorServerSide.java`
#### Snippet
```java
            byte[] tokenBytes = token.getBytes(StateUtils.ZIP_CHARSET);
            byte[] tokenBytesDecoded = StateUtils.decode(tokenBytes);
            String tokenDecoded = new String(tokenBytesDecoded, StateUtils.ZIP_CHARSET);

            return tokenDecoded;
```

### UnnecessaryLocalVariable
Local variable `savedStateObject` is redundant
in `impl/src/main/java/org/apache/myfaces/application/viewstate/StateTokenProcessorClientSide.java`
#### Snippet
```java
            return null;
        }
        Object savedStateObject = StateUtils.reconstruct((String)token, facesContext.getExternalContext());
        return savedStateObject;
    }
```

### UnnecessaryLocalVariable
Local variable `serializedState` is redundant
in `impl/src/main/java/org/apache/myfaces/application/viewstate/StateTokenProcessorClientSide.java`
#### Snippet
```java
            return STATELESS_TOKEN;
        }
        String serializedState = StateUtils.construct(savedStateObject, facesContext.getExternalContext());
        return serializedState;
    }
```

### UnnecessaryLocalVariable
Local variable `viewId` is redundant
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
        protected boolean match(String value)
        {
            String viewId = (String) value;
            ViewDeclarationLanguage vdl = facesContext.getApplication().getViewHandler()
                    .getViewDeclarationLanguage(facesContext, viewId);
```

### UnnecessaryLocalVariable
Local variable `bais` is redundant
in `impl/src/main/java/org/apache/myfaces/application/viewstate/StateCacheServerSide.java`
#### Snippet
```java
            try
            {
                ByteArrayInputStream bais = new ByteArrayInputStream((byte[]) state);

                InputStream is = bais;
```

### UnnecessaryLocalVariable
Local variable `url` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockViewHandler.java`
#### Snippet
```java
    public String getWebsocketURL(FacesContext context, String channelAndToken)
    {
        String url = context.getExternalContext().getRequestContextPath() + 
                PushContext.URI_PREFIX + "/"+channelAndToken;
        return url;
```

### UnnecessaryLocalVariable
Local variable `returnValue` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockFlash.java`
#### Snippet
```java
        else
        {
            Object returnValue = getCurrentPhaseMap().put(key, value);
            return returnValue;
        }
```

### UnnecessaryLocalVariable
Local variable `it` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockExternalContext10.java`
#### Snippet
```java
    {
        final Enumeration enumer = request.getParameterNames();
        Iterator it = new Iterator()
        {
            public boolean hasNext()
```

### UnnecessaryLocalVariable
Local variable `cl` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/resource/MockSimpleResourceHandler.java`
#### Snippet
```java
            try
            {
                ClassLoader cl = AccessController.doPrivileged(
                        (PrivilegedExceptionAction<ClassLoader>) () -> Thread.currentThread().getContextClassLoader());
                return cl;
```

### UnnecessaryLocalVariable
Local variable `cl` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/resource/MockResourceHandler.java`
#### Snippet
```java
            try
            {
                ClassLoader cl = AccessController.doPrivileged(
                        (PrivilegedExceptionAction<ClassLoader>) () -> Thread.currentThread().getContextClassLoader());
                return cl;
```

## RuleId[ruleID=UseCompareMethod]
### UseCompareMethod
Expression can be replaced with 'Integer.compare'
in `impl/src/main/java/org/apache/myfaces/config/util/Vertex.java`
#### Snippet
```java
        int orderInd;

        if (m_order < o.m_order)
        {
            orderInd = -1;
```

## RuleId[ruleID=PointlessBooleanExpression]
### PointlessBooleanExpression
`scoreBoard[j] == false` can be simplified to '!scoreBoard\[j\]'
in `api/src/main/java/jakarta/faces/component/UISelectMany.java`
#### Snippet
```java
            for (int j = 0; j < length; j++)
            {
                if (scoreBoard[j] == false)
                {
                    Object v = Array.get(value, j);
```

### PointlessBooleanExpression
`scoreBoard[j] == false` can be simplified to '!scoreBoard\[j\]'
in `api/src/main/java/jakarta/faces/component/UISelectMany.java`
#### Snippet
```java
            {
                Object v = itValue.next();
                if (scoreBoard[j] == false)
                {
                    if ((p == null && v == null) || (p != null && v != null && p.equals(v)))
```

### PointlessBooleanExpression
`scoreBoard[j] == false` can be simplified to '!scoreBoard\[j\]'
in `api/src/main/java/jakarta/faces/component/UISelectMany.java`
#### Snippet
```java
            for (int j = 0; j < length; j++)
            {
                if (scoreBoard[j] == false)
                {
                    Object v = value[j];
```

### PointlessBooleanExpression
`surrogateResolved == true` can be simplified to 'surrogateResolved'
in `impl/src/main/java/org/apache/myfaces/util/IllegalXmlCharacterFilterWriter.java`
#### Snippet
```java
        for (int i = off; i < to; i++)
        {
            if (surrogateResolved == true)
            {
                surrogateResolved = false;
```

### PointlessBooleanExpression
`surrogateResolved == true` can be simplified to 'surrogateResolved'
in `impl/src/main/java/org/apache/myfaces/util/IllegalXmlCharacterFilterWriter.java`
#### Snippet
```java
        for (int i = off; i < to; i++)
        {
            if (surrogateResolved == true)
            {
                surrogateResolved = false;
```

### PointlessBooleanExpression
`isSeparator(filename.charAt(2)) == false` can be simplified to '!isSeparator(filename.charAt(2))'
in `impl/src/main/java/org/apache/myfaces/util/lang/FilenameUtils.java`
#### Snippet
```java
                if (ch0 >= 'A' && ch0 <= 'Z')
                {
                    if (len == 2 || isSeparator(filename.charAt(2)) == false)
                    {
                        return 2;
```

### PointlessBooleanExpression
`false == name.equals(this.methodName)` can be simplified to '!name.equals(this.methodName)'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/MethodRule.java`
#### Snippet
```java
    public Metadata applyRule(String name, TagAttribute attribute, MetadataTarget meta)
    {
        if (false == name.equals(this.methodName))
        {
            return null;
```

### PointlessBooleanExpression
`resourceExists == false` can be simplified to '!resourceExists'
in `impl/src/main/java/org/apache/myfaces/application/ViewIdSupport.java`
#### Snippet
```java
                    resourceExists = vdl.viewExists(facesContext, viewId);
                    // Check if it might be a programmatic java-built view (MYFACES-4477)
                    if(resourceExists == null || resourceExists == false)
                    {
                        if (ExternalSpecifications.isCDIAvailable(facesContext.getExternalContext()))
```

## RuleId[ruleID=UseBulkOperation]
### UseBulkOperation
Iteration can be replaced with bulk 'Map.putAll()' call
in `impl/src/main/java/org/apache/myfaces/view/facelets/ViewPoolProcessor.java`
#### Snippet
```java
                        for (Map.Entry<String, Object> entry2 : viewScopeMap.entrySet())
                        {
                            newViewScopeMap.put(entry2.getKey(), entry2.getValue());
                        }
                    }
```

## RuleId[ruleID=AssignmentToCatchBlockParameter]
### AssignmentToCatchBlockParameter
Assignment to 'catch' block parameter `e`
in `api/src/main/java/jakarta/faces/component/UIViewRoot.java`
#### Snippet
```java
                if (ape != null)
                {
                    e = ape;
                }
                ExceptionQueuedEventContext exceptionContext 
```

## RuleId[ruleID=StringEquality]
### StringEquality
String values are compared using `==`, not 'equals()'
in `impl/src/main/java/org/apache/myfaces/lifecycle/RenderResponseExecutor.java`
#### Snippet
```java
                newViewId = root.getViewId();
                
                isNotSameRoot = !( (newViewId == null ? newViewId == viewId : newViewId.equals(viewId) ) && 
                        previousRoot.equals(root) ); 
                
```

## RuleId[ruleID=LoopStatementsThatDontLoop]
### LoopStatementsThatDontLoop
`while` statement does not loop
in `impl/src/main/java/org/apache/myfaces/util/ExternalContextUtils.java`
#### Snippet
```java
    {
        // unwrap the response until we find a HttpServletResponse
        while (response != null)
        {
            if (response instanceof HttpServletResponse)
```

### LoopStatementsThatDontLoop
`do` statement does not loop
in `impl/src/main/java/org/apache/myfaces/util/lang/SkipMatchIterator.java`
#### Snippet
```java
    {
        T value = this.value;
        do 
        {
            if (value == null)
```

## RuleId[ruleID=SuspiciousSystemArraycopy]
### SuspiciousSystemArraycopy
`arr1` is not of an array type
in `impl/src/main/java/org/apache/myfaces/util/lang/ArrayUtils.java`
#### Snippet
```java
            commonClass(arr1.getClass().getComponentType(), arr2.getClass().getComponentType());
        Object newArray = Array.newInstance(commonComponentType, len1 + len2);
        System.arraycopy(arr1, 0, newArray, 0, len1);
        System.arraycopy(arr2, 0, newArray, len1, len2);

```

### SuspiciousSystemArraycopy
`newArray` is not of an array type
in `impl/src/main/java/org/apache/myfaces/util/lang/ArrayUtils.java`
#### Snippet
```java
            commonClass(arr1.getClass().getComponentType(), arr2.getClass().getComponentType());
        Object newArray = Array.newInstance(commonComponentType, len1 + len2);
        System.arraycopy(arr1, 0, newArray, 0, len1);
        System.arraycopy(arr2, 0, newArray, len1, len2);

```

### SuspiciousSystemArraycopy
`arr2` is not of an array type
in `impl/src/main/java/org/apache/myfaces/util/lang/ArrayUtils.java`
#### Snippet
```java
        Object newArray = Array.newInstance(commonComponentType, len1 + len2);
        System.arraycopy(arr1, 0, newArray, 0, len1);
        System.arraycopy(arr2, 0, newArray, len1, len2);

        return newArray;
```

### SuspiciousSystemArraycopy
`newArray` is not of an array type
in `impl/src/main/java/org/apache/myfaces/util/lang/ArrayUtils.java`
#### Snippet
```java
        Object newArray = Array.newInstance(commonComponentType, len1 + len2);
        System.arraycopy(arr1, 0, newArray, 0, len1);
        System.arraycopy(arr2, 0, newArray, len1, len2);

        return newArray;
```

### SuspiciousSystemArraycopy
`arr` is not of an array type
in `impl/src/main/java/org/apache/myfaces/util/lang/ArrayUtils.java`
#### Snippet
```java
                if (arrayLen > 0)
                {
                    System.arraycopy(arr, 0, toArray, offset, arrayLen);
                    offset += arrayLen;
                }
```

### SuspiciousSystemArraycopy
`toArray` is not of an array type
in `impl/src/main/java/org/apache/myfaces/util/lang/ArrayUtils.java`
#### Snippet
```java
                if (arrayLen > 0)
                {
                    System.arraycopy(arr, 0, toArray, offset, arrayLen);
                    offset += arrayLen;
                }
```

## RuleId[ruleID=StaticCallOnSubclass]
### StaticCallOnSubclass
Static method `javaDefaultTypeToClass()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/el/resolver/CompositeComponentELResolver.java`
#### Snippet
```java
                    try
                    {
                        type = ClassUtils.javaDefaultTypeToClass((String)type);
                    }
                    catch (ClassNotFoundException e)
```

### StaticCallOnSubclass
Static method `simpleJavaTypeToClass()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/cdi/managedproperty/ManagedPropertyProducer.java`
#### Snippet
```java
        {
            // need to use simpleJavaTypeToClass to support Arrays and primitive types
            beanClass = ClassUtils.simpleJavaTypeToClass(typeInfo.getType().getTypeName());
        }

```

### StaticCallOnSubclass
Static method `newInstance()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/spi/ServiceProviderFinderFactory.java`
#### Snippet
```java
                
                // create instance and return it
                return (ServiceProviderFinder) ClassUtils.newInstance(clazz);
            }
            catch (ClassNotFoundException cnfe)
```

### StaticCallOnSubclass
Static method `getContextClassLoader()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/spi/impl/Tomcat7AnnotationInjectionProvider.java`
#### Snippet
```java
        if (instanceManager != null)
        {
            instanceManagers.put(ClassUtils.getContextClassLoader(), instanceManager);
        }

```

### StaticCallOnSubclass
Static method `getContextClassLoader()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/spi/impl/Tomcat7AnnotationInjectionProvider.java`
#### Snippet
```java
    public void postConstruct(Object instance, Object creationMetaData) throws InjectionProviderException
    {
        InstanceManager manager = instanceManagers.get(ClassUtils.getContextClassLoader());
        if (manager == null)
        {
```

### StaticCallOnSubclass
Static method `getContextClassLoader()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/spi/impl/Tomcat7AnnotationInjectionProvider.java`
#### Snippet
```java
    public void preDestroy(Object instance, Object creationMetaData) throws InjectionProviderException
    {
        InstanceManager manager = instanceManagers.get(ClassUtils.getContextClassLoader());

        if (manager != null)
```

### StaticCallOnSubclass
Static method `newInstance()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/spi/impl/SpiUtils.java`
#### Snippet
```java
                        " found :" + classList.toString() + ". Setting up the first one found.");
            }
            return ClassUtils.newInstance(classList.get(0));
        }
        return ClassUtils.newInstance(defaultImpl);
```

### StaticCallOnSubclass
Static method `newInstance()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/spi/impl/SpiUtils.java`
#### Snippet
```java
            return ClassUtils.newInstance(classList.get(0));
        }
        return ClassUtils.newInstance(defaultImpl);
    }

```

### StaticCallOnSubclass
Static method `getContextClassLoader()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/spi/impl/ResourceAnnotationInjectionProvider.java`
#### Snippet
```java
    private static Map<Class,Field[]> getDeclaredFieldBeansMap()
    {
        ClassLoader cl = ClassUtils.getContextClassLoader();
        
        Map<Class,Field[]> metadata = declaredFieldBeans.get(cl);
```

### StaticCallOnSubclass
Static method `getContextClassLoader()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/spi/impl/NoInjectionAnnotationInjectionProvider.java`
#### Snippet
```java
    private static Map<Class,Method[]> getDeclaredMethodBeansMap()
    {
        ClassLoader cl = ClassUtils.getContextClassLoader();
        
        Map<Class,Method[]> metadata = (Map<Class,Method[]>) declaredMethodBeans.get(cl);
```

### StaticCallOnSubclass
Static method `getContextClassLoader()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/spi/impl/DefaultServiceProviderFinder.java`
#### Snippet
```java
        try
        {
            profiles = ClassUtils.getContextClassLoader().getResources(META_INF_SERVICES + spiClass);
        }
        catch (IOException e)
```

### StaticCallOnSubclass
Static method `newInstance()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/spi/impl/DefaultInjectionProviderFactory.java`
#### Snippet
```java
        try
        {
            return ClassUtils.newInstance(clazz, new Class<?>[]{ ExternalContext.class }, externalContext);
        }
        catch (NoSuchMethodException e)
```

### StaticCallOnSubclass
Static method `newInstance()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/spi/impl/DefaultInjectionProviderFactory.java`
#### Snippet
```java
        catch (NoSuchMethodException e)
        {
            return ClassUtils.newInstance(clazz);
        }
    }
```

### StaticCallOnSubclass
Static method `getContextClassLoader()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/util/WebXmlParser.java`
#### Snippet
```java
        try
        {
            Enumeration<URL> webFragments = ClassUtils.getContextClassLoader()
                    .getResources("META-INF/web-fragment.xml");
            while (webFragments.hasMoreElements())
```

### StaticCallOnSubclass
Static method `getCurrentLoader()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/util/WebXmlParser.java`
#### Snippet
```java
            {
                // Quarkus
                webXml = toDocument(ClassUtils.getCurrentLoader(WebXmlParser.class).getResource("META-INF/web.xml"));
            }
            
```

### StaticCallOnSubclass
Static method `getContextClassLoader()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/util/MyFacesObjectInputStream.java`
#### Snippet
```java
        {
            // Try WebApp ClassLoader first
            return Proxy.getProxyClass(ClassUtils.getContextClassLoader(), cinterfaces);
        }
        catch (Exception ex)
```

### StaticCallOnSubclass
Static method `getCurrentLoader()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/util/MessageUtils.java`
#### Snippet
```java
                if (bundleControl == null)
                {
                    bundle = ResourceBundle.getBundle(bundleName, locale, ClassUtils.getCurrentLoader(bundleName));
                }
                else
```

### StaticCallOnSubclass
Static method `getCurrentLoader()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/util/MessageUtils.java`
#### Snippet
```java
                else
                {
                    bundle = ResourceBundle.getBundle(bundleName, locale, ClassUtils.getCurrentLoader(bundleName),
                            bundleControl);
                }
```

### StaticCallOnSubclass
Static method `getCurrentLoader()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/util/MessageUtils.java`
#### Snippet
```java
                {
                    bundle = ResourceBundle.getBundle(DEFAULT_BUNDLE, locale,
                            ClassUtils.getCurrentLoader(DEFAULT_BUNDLE));
                }
                else
```

### StaticCallOnSubclass
Static method `getCurrentLoader()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/util/MessageUtils.java`
#### Snippet
```java
                {
                    bundle = ResourceBundle.getBundle(DEFAULT_BUNDLE, locale,
                            ClassUtils.getCurrentLoader(DEFAULT_BUNDLE), bundleControl);
                }

```

### StaticCallOnSubclass
Static method `getCurrentLoader()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/util/MessageUtils.java`
#### Snippet
```java
                if (bundleControl == null)
                {
                    bundle = ResourceBundle.getBundle(bundleName, locale, ClassUtils.getCurrentLoader(bundleName));
                }
                else
```

### StaticCallOnSubclass
Static method `getCurrentLoader()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/util/MessageUtils.java`
#### Snippet
```java
                else
                {
                    bundle = ResourceBundle.getBundle(bundleName, locale, ClassUtils.getCurrentLoader(bundleName),
                            bundleControl);
                }
```

### StaticCallOnSubclass
Static method `getCurrentLoader()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/util/MessageUtils.java`
#### Snippet
```java
                {
                    bundle = ResourceBundle.getBundle(bundleBaseName, locale,
                            ClassUtils.getCurrentLoader(bundleBaseName));
                }
                else
```

### StaticCallOnSubclass
Static method `getCurrentLoader()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/util/MessageUtils.java`
#### Snippet
```java
                {
                    bundle = ResourceBundle.getBundle(bundleBaseName, locale,
                            ClassUtils.getCurrentLoader(bundleBaseName), bundleControl);
                }

```

### StaticCallOnSubclass
Static method `getCurrentLoader()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/util/MessageUtils.java`
#### Snippet
```java
                {
                    bundle = ResourceBundle.getBundle(DEFAULT_BUNDLE, locale, 
                            ClassUtils.getCurrentLoader(DEFAULT_BUNDLE));
                }
                else
```

### StaticCallOnSubclass
Static method `getCurrentLoader()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/util/MessageUtils.java`
#### Snippet
```java
                {
                    bundle = ResourceBundle.getBundle(DEFAULT_BUNDLE, locale, 
                            ClassUtils.getCurrentLoader(DEFAULT_BUNDLE), bundleControl);
                }

```

### StaticCallOnSubclass
Static method `newInstance()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/util/lang/ClassUtils.java`
#### Snippet
```java
            {
                // nothing to decorate
                current = (T) ClassUtils.newInstance(implClass);
            }
            else
```

### StaticCallOnSubclass
Static method `newInstance()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/util/lang/ClassUtils.java`
#### Snippet
```java
                {
                    // no decorator pattern support
                    newCurrent = (T) ClassUtils.newInstance(implClass);
                }
                
```

### StaticCallOnSubclass
Static method `forName()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/DefaultFunctionMapper.java`
#### Snippet
```java
                try
                {
                    Class<?> t = ClassUtils.forName(_owner);
                    Class<?>[] p = ClassUtils.toTypeArray(_types);
                    _m = t.getMethod(_name, p);
```

### StaticCallOnSubclass
Static method `toTypeArray()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/DefaultFunctionMapper.java`
#### Snippet
```java
                {
                    Class<?> t = ClassUtils.forName(_owner);
                    Class<?>[] p = ClassUtils.toTypeArray(_types);
                    _m = t.getMethod(_name, p);
                }
```

### StaticCallOnSubclass
Static method `toTypeNameArray()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/DefaultFunctionMapper.java`
#### Snippet
```java
            out.writeUTF(_m.getDeclaringClass().getName());
            out.writeUTF(_m.getName());
            out.writeObject(ClassUtils.toTypeNameArray(this._m.getParameterTypes()));
        }

```

### StaticCallOnSubclass
Static method `getContextClassLoader()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/MetaRulesetImpl.java`
#### Snippet
```java
    private static Map<String, MetadataTarget> getMetaData()
    {
        ClassLoader cl = ClassUtils.getContextClassLoader();
        
        Map<String, MetadataTarget> metadata = (Map<String, MetadataTarget>)
```

### StaticCallOnSubclass
Static method `getContextClassLoader()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/MetaRulesetImpl.java`
#### Snippet
```java
    public static void clearMetadataTargetCache()
    {
        metadata.remove(ClassUtils.getContextClassLoader());
    }

```

### StaticCallOnSubclass
Static method `newInstance()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/view/facelets/PartialStateManagementStrategy.java`
#### Snippet
```java
        String compClass = treeStructComp.getComponentClass();
        String compId = treeStructComp.getComponentId();
        UIComponent component = (UIComponent)ClassUtils.newInstance(compClass);
        component.setId(compId);

```

### StaticCallOnSubclass
Static method `getContextClassLoader()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/LoadBundleHandler.java`
#### Snippet
```java
            
            String name = this.basename.getValue(ctx);
            ClassLoader cl = ClassUtils.getContextClassLoader();
            Locale locale = root != null && root.getLocale() != null
                    ? root.getLocale()
```

### StaticCallOnSubclass
Static method `forName()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/ValueChangeListenerHandler.java`
#### Snippet
```java
                try
                {
                    ClassUtils.forName(type.getValue());
                }
                catch (ClassNotFoundException e)
```

### StaticCallOnSubclass
Static method `forName()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/ValueChangeListenerHandler.java`
#### Snippet
```java
                try
                {
                    instance = (ValueChangeListener) ClassUtils.forName(this.type).newInstance();
                }
                catch (Exception e)
```

### StaticCallOnSubclass
Static method `forName()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/PhaseListenerHandler.java`
#### Snippet
```java
                try
                {
                    ClassUtils.forName(type.getValue());
                }
                catch (ClassNotFoundException e)
```

### StaticCallOnSubclass
Static method `forName()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/PhaseListenerHandler.java`
#### Snippet
```java
                try
                {
                    instance = (PhaseListener) ClassUtils.forName(this.type).newInstance();
                }
                catch (Exception e)
```

### StaticCallOnSubclass
Static method `forName()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/ActionListenerHandler.java`
#### Snippet
```java
                try
                {
                    instance = (ActionListener) ClassUtils.forName(this.type).newInstance();
                }
                catch (Exception e)
```

### StaticCallOnSubclass
Static method `forName()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/ActionListenerHandler.java`
#### Snippet
```java
                try
                {
                    ClassUtils.forName(type.getValue());
                }
                catch (ClassNotFoundException e)
```

### StaticCallOnSubclass
Static method `forName()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/EventHandler.java`
#### Snippet
```java
            try
            {
                eventClass = ClassUtils.forName(value);
                if (type.isLiteral())
                {
```

### StaticCallOnSubclass
Static method `javaDefaultTypeToClass()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeMetadataTargetImpl.java`
#### Snippet
```java
                    try
                    {
                        type = ClassUtils.javaDefaultTypeToClass((String)type);
                    }
                    catch (ClassNotFoundException e)
```

### StaticCallOnSubclass
Static method `javaDefaultTypeToClass()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/AttributeHandler.java`
#### Snippet
```java
                        try
                        {
                            clazz = ClassUtils.javaDefaultTypeToClass(type);
                        }
                        catch (ClassNotFoundException e)
```

### StaticCallOnSubclass
Static method `javaDefaultTypeToClass()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/view/facelets/util/FaceletsViewDeclarationLanguageUtils.java`
#### Snippet
```java
        try
        {
            return ClassUtils.javaDefaultTypeToClass(signature.substring(0, end));
        }
        catch (ClassNotFoundException e)
```

### StaticCallOnSubclass
Static method `javaDefaultTypeToClass()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/view/facelets/util/FaceletsViewDeclarationLanguageUtils.java`
#### Snippet
```java
                try
                {
                    params.add(ClassUtils.javaDefaultTypeToClass(arg.trim()));
                }
                catch (ClassNotFoundException e)
```

### StaticCallOnSubclass
Static method `getTimeInstance()` declared in class 'java.text.DateFormat' but referenced via subclass 'java.text.SimpleDateFormat'
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFacelet.java`
#### Snippet
```java
                            if (log.isLoggable(Level.INFO))
                            {
                                DateFormat df = SimpleDateFormat.getTimeInstance();
                                log.info("Facelet[" + _alias + "] was modified @ "
                                        + df.format(new Date(_createTime)) + ", flushing component applied @ "
```

### StaticCallOnSubclass
Static method `getTimeInstance()` declared in class 'java.text.DateFormat' but referenced via subclass 'java.text.SimpleDateFormat'
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFacelet.java`
#### Snippet
```java
                            if (log.isLoggable(Level.INFO))
                            {
                                DateFormat df = SimpleDateFormat.getTimeInstance();
                                log.info("Facelet[" + _alias + "] was modified @ "
                                        + df.format(new Date(_createTime)) + ", flushing component applied @ "
```

### StaticCallOnSubclass
Static method `getCurrentLoader()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/DefaultFaceletConfigResourceProvider.java`
#### Snippet
```java
 
        //Scan files inside META-INF ending with .faces-config.xml
        URL[] urls = Classpath.search(ClassUtils.getCurrentLoader(this), META_INF_PREFIX, FACELET_TAGLIB_SUFFIX);
        Collections.addAll(urlSet, urls);

```

### StaticCallOnSubclass
Static method `forName()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/FaceletsCompilerSupport.java`
#### Snippet
```java
                try
                {
                    compiler.addTagDecorator((TagDecorator) ClassUtils.forName(decorator).newInstance());
                    if (log.isLoggable(Level.FINE))
                    {
```

### StaticCallOnSubclass
Static method `forName()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TagLibraryConfig.java`
#### Snippet
```java
    private static <T> Class<? extends T> createClass(Class<T> type, String name) throws Exception
    {
        Class<? extends T> factory = (Class<? extends T>) ClassUtils.forName(name);
        if (!type.isAssignableFrom(factory))
        {
```

### StaticCallOnSubclass
Static method `forName()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TagLibraryConfig.java`
#### Snippet
```java
                        for (int i = 0; i < pc.length; i++)
                        {
                            pc[i] = ClassUtils.forName(ps[i].trim());
                        }
                    }
```

### StaticCallOnSubclass
Static method `forName()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/Compiler.java`
#### Snippet
```java
            try
            {
                return ClassUtils.forName(type).newInstance();
            }
            catch (Throwable t)
```

### StaticCallOnSubclass
Static method `getContextClassLoader()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/view/facelets/util/Classpath.java`
#### Snippet
```java
    public static URL[] search(String prefix, String suffix) throws IOException
    {
        return search(ClassUtils.getContextClassLoader(), prefix, suffix);
    }

```

### StaticCallOnSubclass
Static method `getCurrentLoader()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/config/DefaultFacesConfigResourceProvider.java`
#### Snippet
```java
        List<URL> urlSet = new ArrayList<>();
        
        ClassLoader cl = ClassUtils.getCurrentLoader(this);
        
        //This usually happens when maven-jetty-plugin is used
```

### StaticCallOnSubclass
Static method `getResources()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/config/LogMetaInfUtils.java`
#### Snippet
```java
            Map<String, List<JarInfo>> libs = new HashMap<>(30);

            Iterator<URL> it = ClassUtils.getResources("META-INF/MANIFEST.MF", LogMetaInfUtils.class).iterator();
            while (it.hasNext())
            {
```

### StaticCallOnSubclass
Static method `newInstance()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/config/FacesConfigurator.java`
#### Snippet
```java
                {
                    PhaseListener listener = (PhaseListener)
                            ClassUtils.newInstance(listenerClassName, PhaseListener.class);
                    _callInjectAndPostConstruct(listener);
                    lifecycle.addPhaseListener(listener);
```

### StaticCallOnSubclass
Static method `newInstance()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/config/FacesConfigurator.java`
#### Snippet
```java
                        try
                        {
                            renderer = (jakarta.faces.render.Renderer) ClassUtils.newInstance(
                                element.getRendererClass());
                        }
```

### StaticCallOnSubclass
Static method `newInstance()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/config/FacesConfigurator.java`
#### Snippet
```java
                    jakarta.faces.render.ClientBehaviorRenderer behaviorRenderer
                            = (jakarta.faces.render.ClientBehaviorRenderer)
                            ClassUtils.newInstance(clientBehaviorRenderer.getRendererClass());

                    renderKit.addClientBehaviorRenderer(clientBehaviorRenderer.getRendererType(), behaviorRenderer);
```

### StaticCallOnSubclass
Static method `newInstance()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/config/FacesConfigurator.java`
#### Snippet
```java
            try
            {
                serialFactory = (SerialFactory) ClassUtils.newInstance(serialProvider);

            }
```

### StaticCallOnSubclass
Static method `newInstance()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/config/FacesConfigurator.java`
#### Snippet
```java

                jakarta.faces.event.SystemEventListener listener = (jakarta.faces.event.SystemEventListener) 
                        ClassUtils.newInstance(systemEventListener.getSystemEventListenerClass());
                _callInjectAndPostConstruct(listener);
                if (systemEventListener.getSourceClass() != null && systemEventListener.getSourceClass().length() > 0)
```

### StaticCallOnSubclass
Static method `newInstance()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/config/FacesConfigurator.java`
#### Snippet
```java
        for (String className : dispenser.getElResolvers())
        {
            ELResolver elResolver = (ELResolver) ClassUtils.newInstance(className, ELResolver.class);
            _callInjectAndPostConstruct(elResolver);
            runtimeConfig.addFacesConfigElResolver(elResolver);
```

### StaticCallOnSubclass
Static method `newInstance()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/config/FacesConfigurator.java`
#### Snippet
```java
        for (String className : dispenser.getSearchKeywordResolvers())
        {
            SearchKeywordResolver searchKeywordResolver = (SearchKeywordResolver) ClassUtils.newInstance(
                    className, SearchKeywordResolver.class);
            
```

### StaticCallOnSubclass
Static method `newInstance()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/config/FacesConfigurator.java`
#### Snippet
```java
            {
                Class<Comparator<ELResolver>> clazz = ClassUtils.classForName(elResolverComparatorClass);
                Comparator<ELResolver> comparator = ClassUtils.newInstance(clazz);
                runtimeConfig.setELResolverComparator(comparator);
            }
```

### StaticCallOnSubclass
Static method `newInstance()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/config/FacesConfigurator.java`
#### Snippet
```java
            {
                Class<?> clazz = ClassUtils.classForName(elResolverPredicateClass);
                Object elResolverPredicate = ClassUtils.newInstance(clazz);
                if (elResolverPredicate instanceof Predicate)
                {
```

### StaticCallOnSubclass
Static method `newInstance()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/webapp/FacesInitializerFactory.java`
#### Snippet
```java
                
                // create instance and return it
                return (FacesInitializer) ClassUtils.newInstance(clazz);
            }
            catch (ClassNotFoundException cnfe)
```

### StaticCallOnSubclass
Static method `getContextClassLoader()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/config/annotation/PackageInfo.java`
#### Snippet
```java
            path = pckgname.replace('.', '/');

            cld = ClassUtils.getContextClassLoader();
            if (cld == null)
            {
```

### StaticCallOnSubclass
Static method `newInstance()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/config/webparameters/MyfacesConfig.java`
#### Snippet
```java
        if (StringUtils.isNotBlank(resourceBundleControl))
        {
            cfg.resourceBundleControl = (ResourceBundle.Control) ClassUtils.newInstance(resourceBundleControl);
        }
        
```

### StaticCallOnSubclass
Static method `getContextClassLoader()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/config/annotation/DefaultAnnotationProvider.java`
#### Snippet
```java
    {
        // Accumulate and return a list of classes in this JAR file
        ClassLoader loader = ClassUtils.getContextClassLoader();
        if (loader == null)
        {
```

### StaticCallOnSubclass
Static method `getCurrentLoader()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/config/annotation/DefaultAnnotationProvider.java`
#### Snippet
```java
    private void webClasses(ExternalContext externalContext, String prefix, List<Class<?>> list)
    {
        ClassLoader loader = ClassUtils.getCurrentLoader(this);

        Set<String> paths = externalContext.getResourcePaths(prefix);
```

### StaticCallOnSubclass
Static method `getCurrentLoader()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/config/annotation/DefaultAnnotationProvider.java`
#### Snippet
```java
    public Set<URL> getBaseUrls(ExternalContext context) throws IOException
    {
        ClassLoader cl = ClassUtils.getCurrentLoader(this);

        Set<URL> urlSet = new HashSet<>();
```

### StaticCallOnSubclass
Static method `getContextClassLoader()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/webapp/FacesInitializerImpl.java`
#### Snippet
```java
            try
            {
                Class pluginClass = ClassUtils.getContextClassLoader().loadClass(pluginEntry);
                if (pluginClass == null)
                {
```

### StaticCallOnSubclass
Static method `getContextClassLoader()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/webapp/FacesInitializerImpl.java`
#### Snippet
```java
    {
        ServiceLoader<StartupListener> loader = ServiceLoader.load(StartupListener.class,
                ClassUtils.getContextClassLoader());

        Iterator<StartupListener> it = (Iterator<StartupListener>) loader.iterator();
```

### StaticCallOnSubclass
Static method `getContextClassLoader()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/webapp/FacesInitializerImpl.java`
#### Snippet
```java
        try
        {
            ClassLoader cl = ClassUtils.getContextClassLoader();
            if (cl == null)
            {
```

### StaticCallOnSubclass
Static method `getCurrentLoader()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/resource/DefaultResourceLibraryContractsProvider.java`
#### Snippet
```java
        Set<String> contracts = new HashSet<>();

        URL[] urls = Classpath.search(ClassUtils.getCurrentLoader(this),
                META_INF_CONTRACTS_PREFIX,
                META_INF_CONTRACTS_SUFFIX);
```

### StaticCallOnSubclass
Static method `getContextClassLoader()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/resource/ClassLoaderContractResourceLoader.java`
#### Snippet
```java
    protected ClassLoader getClassLoader()
    {
        return ClassUtils.getContextClassLoader();
    }

```

### StaticCallOnSubclass
Static method `getContextClassLoader()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/resource/ClassLoaderResourceLoader.java`
#### Snippet
```java
    protected ClassLoader getClassLoader()
    {
        return ClassUtils.getContextClassLoader();
    }

```

### StaticCallOnSubclass
Static method `getContextClassLoader()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/resource/InternalClassLoaderResourceLoader.java`
#### Snippet
```java
    protected ClassLoader getClassLoader()
    {
        return ClassUtils.getContextClassLoader();
    }

```

### StaticCallOnSubclass
Static method `getContextClassLoader()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/resource/ClassLoaderResourceLoaderIterator.java`
#### Snippet
```java
    protected ClassLoader getClassLoader()
    {
        return ClassUtils.getContextClassLoader();
    }
 
```

### StaticCallOnSubclass
Static method `newInstance()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlRenderKitImpl.java`
#### Snippet
```java
            if (delegate == null)
            {
                delegate = (Renderer) ClassUtils.newInstance(
                    ClassUtils.simpleClassForName(rendererClass));
            }
```

### StaticCallOnSubclass
Static method `newInstance()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/application/TreeStructureManager.java`
#### Snippet
```java
        String compClass = treeStructComp.getComponentClass();
        String compId = treeStructComp.getComponentId();
        UIComponent component = (UIComponent)ClassUtils.newInstance(compClass);
        component.setId(compId);

```

### StaticCallOnSubclass
Static method `getContextClassLoader()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/renderkit/ErrorPageWriter.java`
#### Snippet
```java
    private static String[] splitTemplate(String rsc) throws IOException
    {
        InputStream is = ClassUtils.getContextClassLoader().getResourceAsStream(rsc);
        if (is == null)
        {
```

### StaticCallOnSubclass
Static method `getContextClassLoader()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
                if (bundleControl == null)
                {
                    bundle = ResourceBundle.getBundle(bundleName, locale, ClassUtils.getContextClassLoader());
                }
                else
```

### StaticCallOnSubclass
Static method `getContextClassLoader()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
                else
                {
                    bundle = ResourceBundle.getBundle(bundleName, locale, ClassUtils.getContextClassLoader(),
                            bundleControl);
                }
```

### StaticCallOnSubclass
Static method `simpleJavaTypeToClass()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/application/NavigationHandlerImpl.java`
#### Snippet
```java
                parameters[i] = param.getValue().getValue(facesContext.getELContext());
                clazzes[i] = param.getName() != null ? 
                    ClassUtils.simpleJavaTypeToClass(param.getName()) :
                    (parameters[i] == null ? String.class : parameters[i].getClass());
            }
```

### StaticCallOnSubclass
Static method `convertToType()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/application/ApplicationImpl.java`
#### Snippet
```java
                    }

                    Object convertedValue = ClassUtils.convertToType(property.getDefaultValue(), pd.getPropertyType());
                    if (pd instanceof LambdaPropertyDescriptor)
                    {
```

### StaticCallOnSubclass
Static method `newInstance()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/application/ApplicationImpl.java`
#### Snippet
```java
                String className = name.substring(0, name.lastIndexOf('.'));

                component = (UIComponent)ClassUtils.newInstance(className);
            }
            else
```

### StaticCallOnSubclass
Static method `getContextClassLoader()` declared in class 'org.apache.myfaces.core.api.shared.lang.ClassUtils' but referenced via subclass 'org.apache.myfaces.util.lang.ClassUtils'
in `impl/src/main/java/org/apache/myfaces/application/ApplicationImpl.java`
#### Snippet
```java
        try
        {
            return getResourceBundle(bundleName, locale, ClassUtils.getContextClassLoader());
        }
        catch (MissingResourceException e)
```

## RuleId[ruleID=UnnecessaryStringEscape]
### UnnecessaryStringEscape
`\'` is unnecessarily escaped
in `impl/src/main/java/org/apache/myfaces/lifecycle/clientwindow/CODIClientSideWindow.java`
#### Snippet
```java
        // DO NOT REMOVE THE FOLLOWING LINES!
        url = url.replace("\"", "");
        url = url.replace("\'", "");

        return url;
```

## RuleId[ruleID=ObsoleteCollection]
### ObsoleteCollection
Obsolete collection type `Stack` used
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/CompilationManager.java`
#### Snippet
```java

        // our compilationunit stack
        this.units = new Stack<CompilationUnit>();
        this.units.push(new CompilationUnit());
        
```

### ObsoleteCollection
Obsolete collection type `Stack` used
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/CompilationManager.java`
#### Snippet
```java
    private final NamespaceManager namespaceManager;

    private final Stack<CompilationUnit> units;

    private int tagId;
```

### ObsoleteCollection
Obsolete collection type `Stack` used
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TextUnit.java`
#### Snippet
```java
        this.textBuffer = new StringBuffer();
        this.instructionBuffer = new ArrayList<Instruction>();
        this.tags = new Stack<Tag>();
        this.children = new ArrayList<Object>();
        this.startTagOpen = false;
```

### ObsoleteCollection
Obsolete collection type `Stack` used
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TextUnit.java`
#### Snippet
```java
    private final StringBuffer textBuffer;
    private final List<Instruction> instructionBuffer;
    private final Stack<Tag> tags;
    private final List<Object> children;
    private boolean startTagOpen;
```

### ObsoleteCollection
Obsolete collection type `Vector` used
in `test/src/main/java/org/apache/myfaces/test/base/junit/runners/TestPerClassLoaderRunner.java`
#### Snippet
```java
        // We also need to set useSystemClassLoader=true in the POM so that we gets a jar with the classpath in it.
        String booterClassPath = System.getProperty("java.class.path");
        Vector<String> pathItems = null;
        if (booterClassPath != null)
        {
```

### ObsoleteCollection
Obsolete collection type `Vector` used
in `test/src/main/java/org/apache/myfaces/test/base/junit/runners/TestPerClassLoaderRunner.java`
#### Snippet
```java
     * @return the vector< string>
     */
    private static Vector<String> scanPath(String classPath)
    {
        String separator = System.getProperty("path.separator");
```

### ObsoleteCollection
Obsolete collection type `Vector` used
in `test/src/main/java/org/apache/myfaces/test/base/junit/runners/TestPerClassLoaderRunner.java`
#### Snippet
```java
    {
        String separator = System.getProperty("path.separator");
        Vector<String> pathItems = new Vector<String>(10);
        StringTokenizer st = new StringTokenizer(classPath, separator);
        while (st.hasMoreTokens())
```

### ObsoleteCollection
Obsolete collection type `Vector` used
in `test/src/main/java/org/apache/myfaces/test/base/junit/runners/TestPerClassLoaderRunner.java`
#### Snippet
```java
    {
        String separator = System.getProperty("path.separator");
        Vector<String> pathItems = new Vector<String>(10);
        StringTokenizer st = new StringTokenizer(classPath, separator);
        while (st.hasMoreTokens())
```

### ObsoleteCollection
Obsolete collection type `Vector` used
in `test/src/main/java/org/apache/myfaces/test/base/junit/runners/TestClassLoader.java`
#### Snippet
```java
    static final String EXCLUDED_FILE = "excluded.properties";
    /** excluded paths */
    private Vector<String> fExcluded;

    /**
```

### ObsoleteCollection
Obsolete collection type `Vector` used
in `test/src/main/java/org/apache/myfaces/test/base/junit/runners/TestClassLoader.java`
#### Snippet
```java
    private void readExcludedPackages()
    {
        fExcluded = new Vector<String>(10);
        for (int i = 0; i < defaultExclusions.length; i++)
        {
```

### ObsoleteCollection
Obsolete collection type `Vector` used
in `test/src/main/java/org/apache/myfaces/test/base/junit/runners/TestClassLoader.java`
#### Snippet
```java
    {
        String separator = System.getProperty("path.separator");
        fPathItems = new Vector<String>(10);
        StringTokenizer st = new StringTokenizer(classPath, separator);
        while (st.hasMoreTokens())
```

### ObsoleteCollection
Obsolete collection type `Vector` used
in `test/src/main/java/org/apache/myfaces/test/base/junit/runners/TestClassLoader.java`
#### Snippet
```java
{
    /** scanned class path */
    private Vector<String> fPathItems;
    /** default excluded paths */
    private String[] defaultExclusions = { "junit.framework.",
```

### ObsoleteCollection
Obsolete collection type `Hashtable` used
in `test/src/main/java/org/apache/myfaces/test/mock/MockPortletContext.java`
#### Snippet
```java
    private File documentRoot = null;
    private Hashtable mimeTypes = new Hashtable();
    private Hashtable parameters = new Hashtable();

    // -------------------------------------------------- PortletContext Methods
```

### ObsoleteCollection
Obsolete collection type `Hashtable` used
in `test/src/main/java/org/apache/myfaces/test/mock/MockPortletContext.java`
#### Snippet
```java
    private File documentRoot = null;
    private Hashtable mimeTypes = new Hashtable();
    private Hashtable parameters = new Hashtable();

    // -------------------------------------------------- PortletContext Methods
```

### ObsoleteCollection
Obsolete collection type `Hashtable` used
in `test/src/main/java/org/apache/myfaces/test/mock/MockPortletContext.java`
#### Snippet
```java
    // ------------------------------------------------------ Instance Variables

    private Hashtable attributes = new Hashtable();
    private File documentRoot = null;
    private Hashtable mimeTypes = new Hashtable();
```

### ObsoleteCollection
Obsolete collection type `Hashtable` used
in `test/src/main/java/org/apache/myfaces/test/mock/MockPortletContext.java`
#### Snippet
```java
    // ------------------------------------------------------ Instance Variables

    private Hashtable attributes = new Hashtable();
    private File documentRoot = null;
    private Hashtable mimeTypes = new Hashtable();
```

### ObsoleteCollection
Obsolete collection type `Hashtable` used
in `test/src/main/java/org/apache/myfaces/test/mock/MockPortletContext.java`
#### Snippet
```java
    private Hashtable attributes = new Hashtable();
    private File documentRoot = null;
    private Hashtable mimeTypes = new Hashtable();
    private Hashtable parameters = new Hashtable();

```

### ObsoleteCollection
Obsolete collection type `Hashtable` used
in `test/src/main/java/org/apache/myfaces/test/mock/MockPortletContext.java`
#### Snippet
```java
    private Hashtable attributes = new Hashtable();
    private File documentRoot = null;
    private Hashtable mimeTypes = new Hashtable();
    private Hashtable parameters = new Hashtable();

```

### ObsoleteCollection
Obsolete collection type `Hashtable` used
in `test/src/main/java/org/apache/myfaces/test/mock/MockServletContext.java`
#### Snippet
```java
    // ------------------------------------------------------ Instance Variables

    private Hashtable attributes = new Hashtable();
    private File documentRoot = null;
    private Hashtable mimeTypes = new Hashtable();
```

### ObsoleteCollection
Obsolete collection type `Hashtable` used
in `test/src/main/java/org/apache/myfaces/test/mock/MockServletContext.java`
#### Snippet
```java
    // ------------------------------------------------------ Instance Variables

    private Hashtable attributes = new Hashtable();
    private File documentRoot = null;
    private Hashtable mimeTypes = new Hashtable();
```

### ObsoleteCollection
Obsolete collection type `Hashtable` used
in `test/src/main/java/org/apache/myfaces/test/mock/MockServletContext.java`
#### Snippet
```java
    private Hashtable attributes = new Hashtable();
    private File documentRoot = null;
    private Hashtable mimeTypes = new Hashtable();
    private Hashtable parameters = new Hashtable();
    private List attributeListeners = new ArrayList();
```

### ObsoleteCollection
Obsolete collection type `Hashtable` used
in `test/src/main/java/org/apache/myfaces/test/mock/MockServletContext.java`
#### Snippet
```java
    private Hashtable attributes = new Hashtable();
    private File documentRoot = null;
    private Hashtable mimeTypes = new Hashtable();
    private Hashtable parameters = new Hashtable();
    private List attributeListeners = new ArrayList();
```

### ObsoleteCollection
Obsolete collection type `Hashtable` used
in `test/src/main/java/org/apache/myfaces/test/mock/MockServletContext.java`
#### Snippet
```java
    private File documentRoot = null;
    private Hashtable mimeTypes = new Hashtable();
    private Hashtable parameters = new Hashtable();
    private List attributeListeners = new ArrayList();
    private MockWebContainer webContainer;
```

### ObsoleteCollection
Obsolete collection type `Hashtable` used
in `test/src/main/java/org/apache/myfaces/test/mock/MockServletContext.java`
#### Snippet
```java
    private File documentRoot = null;
    private Hashtable mimeTypes = new Hashtable();
    private Hashtable parameters = new Hashtable();
    private List attributeListeners = new ArrayList();
    private MockWebContainer webContainer;
```

### ObsoleteCollection
Obsolete collection type `Hashtable` used
in `test/src/main/java/org/apache/myfaces/test/mock/MockServletConfig.java`
#### Snippet
```java

    private ServletContext context;
    private Hashtable parameters = new Hashtable();

    // --------------------------------------------------- ServletConfig Methods
```

### ObsoleteCollection
Obsolete collection type `Hashtable` used
in `test/src/main/java/org/apache/myfaces/test/mock/MockServletConfig.java`
#### Snippet
```java

    private ServletContext context;
    private Hashtable parameters = new Hashtable();

    // --------------------------------------------------- ServletConfig Methods
```

### ObsoleteCollection
Obsolete collection type `Vector` used
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletRequest.java`
#### Snippet
```java

        String match = name + ':';
        Vector values = new Vector();
        Iterator headers = this.headers.iterator();
        while (headers.hasNext())
```

### ObsoleteCollection
Obsolete collection type `Vector` used
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletRequest.java`
#### Snippet
```java

        String match = name + ':';
        Vector values = new Vector();
        Iterator headers = this.headers.iterator();
        while (headers.hasNext())
```

### ObsoleteCollection
Obsolete collection type `Vector` used
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletRequest.java`
#### Snippet
```java
    private ServletInputStream inputStream = null;
    private List cookies = new ArrayList();
    private Vector locales = null;
    private String method = null;
    private List<String> roles = null;
```

### ObsoleteCollection
Obsolete collection type `Vector` used
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletRequest.java`
#### Snippet
```java
    {

        Vector values = new Vector();
        Iterator headers = this.headers.iterator();
        while (headers.hasNext())
```

### ObsoleteCollection
Obsolete collection type `Vector` used
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletRequest.java`
#### Snippet
```java
    {

        Vector values = new Vector();
        Iterator headers = this.headers.iterator();
        while (headers.hasNext())
```

### ObsoleteCollection
Obsolete collection type `Vector` used
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletRequest.java`
#### Snippet
```java
        if (this.locales == null)
        {
            locales = new Vector(Arrays.asList(Locale.getAvailableLocales()));
        }
        return this.locales.elements();
```

## RuleId[ruleID=RedundantArrayCreation]
### RedundantArrayCreation
Redundant array creation for calling varargs method
in `api/src/main/java/jakarta/faces/_FactoryFinderProviderFactory.java`
#### Snippet
```java
            {
                factoryFinderProviderGetFactoryMethod = factoryFinderProviderClass.getMethod("getFactory",
                        new Class[]{String.class});
                factoryFinderProviderSetFactoryMethod = factoryFinderProviderClass.getMethod("setFactory",
                        new Class[]{String.class, String.class});
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `api/src/main/java/jakarta/faces/_FactoryFinderProviderFactory.java`
#### Snippet
```java
                        new Class[]{String.class});
                factoryFinderProviderSetFactoryMethod = factoryFinderProviderClass.getMethod("setFactory",
                        new Class[]{String.class, String.class});
                factoryFinderProviderReleaseFactoriesMethod = factoryFinderProviderClass.getMethod
                        ("releaseFactories", null);
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `api/src/main/java/jakarta/faces/FactoryFinder.java`
#### Snippet
```java
                    try
                    {
                        Constructor<?> delegationConstructor = implClass.getConstructor(new Class[] { interfaceClass });
                        // impl class supports decorator pattern,
                        try
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `api/src/main/java/jakarta/faces/FactoryFinder.java`
#### Snippet
```java
                        {
                            // create new decorator wrapping current
                            current = delegationConstructor.newInstance(new Object[] { current });
                            injectAndPostConstruct(injectionProvider, current, injectedBeanStorage);
                        }
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `api/src/main/java/jakarta/faces/component/_ComponentAttributesMap.java`
#### Snippet
```java
            else
            {
                writeMethod.invoke(_component, new Object[]{value});
            }
        }
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `api/src/main/java/jakarta/faces/component/UIViewRoot.java`
#### Snippet
```java
                    = ClassUtils.classForName("org.apache.myfaces.context.RequestViewContext");
            REQUEST_VIEW_CONTEXT_GET_INSTANCE = REQUEST_VIEW_CONTEXT_CLASS.getMethod("getCurrentInstance",
                    new Class[] { FacesContext.class });
            REQUEST_VIEW_CONTEXT_SET_RENDER_TARGET = REQUEST_VIEW_CONTEXT_CLASS.getMethod("setRenderTarget",
                    new Class[] { String.class, boolean.class, UIComponent.class });
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `api/src/main/java/jakarta/faces/component/UIViewRoot.java`
#### Snippet
```java
                    new Class[] { FacesContext.class });
            REQUEST_VIEW_CONTEXT_SET_RENDER_TARGET = REQUEST_VIEW_CONTEXT_CLASS.getMethod("setRenderTarget",
                    new Class[] { String.class, boolean.class, UIComponent.class });
        }
        catch (Exception e)
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `api/src/main/java/jakarta/faces/component/UIData.java`
#### Snippet
```java
            {
                createDataModelMethod = dataModelBuilderClass.getMethod("createDataModel",
                        new Class[]{FacesContext.class, Class.class, Object.class});
            }
        }
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `impl/src/main/java/org/apache/myfaces/el/resolver/ImportHandlerResolver.java`
#### Snippet
```java
            {
                importHandlerResolveClassMethod = 
                    importHandlerClass.getDeclaredMethod("resolveClass", new Class[] {String.class});
                elClassConstructor = elClass.getConstructor(Class.class);
            }
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `impl/src/main/java/org/apache/myfaces/util/lang/ClassUtils.java`
#### Snippet
```java
                Constructor<? extends T> wrapperConstructor
                        = extendedInterfaceWrapperClass.getConstructor(
                                new Class[] {interfaceClass, extendedInterfaceClass});
                current = wrapperConstructor.newInstance(new Object[] {newCurrent, defaultObject});
            }
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `impl/src/main/java/org/apache/myfaces/util/lang/ClassUtils.java`
#### Snippet
```java
                        = extendedInterfaceWrapperClass.getConstructor(
                                new Class[] {interfaceClass, extendedInterfaceClass});
                current = wrapperConstructor.newInstance(new Object[] {newCurrent, defaultObject});
            }
            catch (NoSuchMethodException | InstantiationException | IllegalAccessException
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `impl/src/main/java/org/apache/myfaces/util/lang/ClassUtils.java`
#### Snippet
```java
                        {
                            delegationConstructor = 
                                    implClass.getConstructor(new Class[] {extendedInterfaceClass});
                        }
                        catch (NoSuchMethodException mnfe)
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `impl/src/main/java/org/apache/myfaces/util/lang/ClassUtils.java`
#### Snippet
```java
                        // try to find the constructor with the "normal" interfaceClass
                        delegationConstructor = 
                                implClass.getConstructor(new Class[] {interfaceClass});
                    }
                    // impl class supports decorator pattern at this point
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `impl/src/main/java/org/apache/myfaces/util/lang/ClassUtils.java`
#### Snippet
```java
                    {
                        // create new decorator wrapping current
                        newCurrent = delegationConstructor.newInstance(new Object[] { current });
                    }
                    catch (InstantiationException | IllegalAccessException | InvocationTargetException e)
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ComponentTagDeclarationLibrary.java`
#### Snippet
```java
            {
                ComponentConfig ccfg = new ComponentConfigWrapper(cfg, componentType, renderType);
                return constructor.newInstance(new Object[] { ccfg });
            }
            catch (InvocationTargetException e)
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/BeanPropertyTagRule.java`
#### Snippet
```java
                if (method != null)
                {
                    method.invoke(instance, new Object[] { attribute.getObject(ctx, propertyType) });
                }
                else if (function != null)
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/MethodRule.java`
#### Snippet
```java
                if (method != null)
                {
                    method.invoke(instance, new Object[] { expr });
                }
                else if (function != null)
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/AbstractTagLibrary.java`
#### Snippet
```java
            try
            {
                return handlerType.getConstructor(CONSTRUCTOR_SIG).newInstance(new Object[] { cfg });
            }
            catch (InvocationTargetException ite)
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/AbstractTagLibrary.java`
#### Snippet
```java
            {
                ConverterConfig ccfg = new ConverterConfigWrapper(cfg, converterId);
                return constructor.newInstance(new Object[] { ccfg });
            }
            catch (InvocationTargetException e)
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/AbstractTagLibrary.java`
#### Snippet
```java
            {
                BehaviorConfig bcfg = new BehaviorConfigWrapper(cfg,this.behaviorId);
                return constructor.newInstance(new Object[] { bcfg });
            }
            catch (InvocationTargetException e)
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/AbstractTagLibrary.java`
#### Snippet
```java
            {
                ValidatorConfig ccfg = new ValidatorConfigWrapper(cfg, validatorId);
                return constructor.newInstance(new Object[] { ccfg });
            }
            catch (InvocationTargetException e)
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/AbstractTagLibrary.java`
#### Snippet
```java
            {
                ComponentConfig ccfg = new ComponentConfigWrapper(cfg, componentType, renderType);
                return constructor.newInstance(new Object[] { ccfg });
            }
            catch (InvocationTargetException e)
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/JavascriptUtils.java`
#### Snippet
```java

    private static final Set RESERVED_WORDS =
        new HashSet(Arrays.asList(new String[]{
            "abstract",
            "boolean",
            "break",
            "byte",
            "case",
            "catch",
            "char",
            "class",
            "const",
            "continue",
            "default",
            "delete",
            "do",
            "double",
            "else",
            "export",
            "extends",
            "false",
            "final",
            "finally",
            "float",
            "for",
            "function",
            "goto",
            "if",
            "implements",
            "in",
            "instanceof",
            "int",
            "long",
            "native",
            "new",
            "null",
            "package",
            "private",
            "protected",
            "public",
            "return",
            "short",
            "static",
            "super",
            "switch",
            "synchronized",
            "this",
            "throw",
            "throws",
            "transient",
            "true",
            "try",
            "typeof",
            "var",
            "void",
            "while",
            "with"
        }));

    /**Don't use this function - except when compatibility with the RI is a must,
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `impl/src/main/java/org/apache/myfaces/renderkit/ErrorPageWriter.java`
#### Snippet
```java
        try
        {
            Method m = Throwable.class.getMethod("initCause", new Class[] { Throwable.class });
            m.invoke(th, new Object[] { cause });
            return true;
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `impl/src/main/java/org/apache/myfaces/renderkit/ErrorPageWriter.java`
#### Snippet
```java
        {
            Method m = Throwable.class.getMethod("initCause", new Class[] { Throwable.class });
            m.invoke(th, new Object[] { cause });
            return true;
        }
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `impl/src/main/java/org/apache/myfaces/application/ApplicationImpl.java`
#### Snippet
```java
                            // See Faces 1.2 javadoc for Converter
                            Constructor<? extends Converter> constructor = converterClass
                                    .getConstructor(new Class[] { Class.class });

                            converter = constructor.newInstance(new Object[] { targetClass });
```

### RedundantArrayCreation
Redundant array creation for calling varargs method
in `impl/src/main/java/org/apache/myfaces/application/ApplicationImpl.java`
#### Snippet
```java
                                    .getConstructor(new Class[] { Class.class });

                            converter = constructor.newInstance(new Object[] { targetClass });
                        }
                        catch (Exception e)
```

## RuleId[ruleID=DuplicateBranchesInSwitch]
### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `impl/src/main/java/org/apache/myfaces/config/annotation/ClassByteCodeAnnotationFilter.java`
#### Snippet
```java
                case CP_INFO_STRING: //ignore
                    //u2 string_index
                    in.readUnsignedShort();
                    break;
                case CP_INFO_INTEGER: //ignore
```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `impl/src/main/java/org/apache/myfaces/config/annotation/ClassByteCodeAnnotationFilter.java`
#### Snippet
```java
                    //u2 name_index
                    //u2 descriptor_index
                    in.readUnsignedShort();
                    in.readUnsignedShort();
                    break;

```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `impl/src/main/java/org/apache/myfaces/config/annotation/ClassByteCodeAnnotationFilter.java`
#### Snippet
```java
                case CP_INFO_METHOD_TYPE:       // Ignore
                    // u2 descriptor_index
                    in.readUnsignedShort();
                    break;

```

### DuplicateBranchesInSwitch
Duplicate branch in 'switch'
in `impl/src/main/java/org/apache/myfaces/config/annotation/ClassByteCodeAnnotationFilter.java`
#### Snippet
```java
                    // u2 bootstrap_method_attr_index;
                    // u2 name_and_type_index;
                    in.readUnsignedShort();
                    in.readUnsignedShort();
                    break;

```

## RuleId[ruleID=RegExpSimplifiable]
### RegExpSimplifiable
`[^\\w]` can be simplified to '\\W'
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/CompositeComponentELUtils.java`
#### Snippet
```java
     * A regular expression used to determine if cc is used in an expression String.
     */
    public static final Pattern CC_EXPRESSION_REGEX = Pattern.compile(".*[^\\w\\.]cc[^\\w].*");
    
    /**
```

### RegExpSimplifiable
`[^\\w]` can be simplified to '\\W'
in `impl/src/main/java/org/apache/myfaces/resource/ResourceELUtils.java`
#### Snippet
```java
    // TODO: check this expression, maybe we can make it simpler, because "resource" implicit object
    // cannot be preceded by anything.
    public static final Pattern RESOURCE_EXPRESSION_REGEX = Pattern.compile(".*[^\\w\\.]resource[^\\w].*");
    
    private static final String RESOURCE = "resource";
```

### RegExpSimplifiable
`\\p{Digit}` can be simplified to '\\d'
in `impl/src/main/java/org/apache/myfaces/resource/ExternalContextContractResourceLoader.java`
#### Snippet
```java
     * Used on getLibraryVersion to filter resource directories
     **/
    protected static final Pattern VERSION_CHECKER = Pattern.compile("/\\p{Digit}+(_\\p{Digit}*)*/");

    /**
```

### RegExpSimplifiable
`\\p{Digit}` can be simplified to '\\d'
in `impl/src/main/java/org/apache/myfaces/resource/ExternalContextContractResourceLoader.java`
#### Snippet
```java
     * Used on getLibraryVersion to filter resource directories
     **/
    protected static final Pattern VERSION_CHECKER = Pattern.compile("/\\p{Digit}+(_\\p{Digit}*)*/");

    /**
```

### RegExpSimplifiable
`\\p{Digit}` can be simplified to '\\d'
in `impl/src/main/java/org/apache/myfaces/resource/ExternalContextContractResourceLoader.java`
#### Snippet
```java
     * Used on getResourceVersion to filter resources
     **/
    protected static final Pattern RESOURCE_VERSION_CHECKER = Pattern.compile("/\\p{Digit}+(_\\p{Digit}*)*\\..*");

    public ExternalContextContractResourceLoader(String prefix)
```

### RegExpSimplifiable
`\\p{Digit}` can be simplified to '\\d'
in `impl/src/main/java/org/apache/myfaces/resource/ExternalContextContractResourceLoader.java`
#### Snippet
```java
     * Used on getResourceVersion to filter resources
     **/
    protected static final Pattern RESOURCE_VERSION_CHECKER = Pattern.compile("/\\p{Digit}+(_\\p{Digit}*)*\\..*");

    public ExternalContextContractResourceLoader(String prefix)
```

### RegExpSimplifiable
`\\p{Digit}` can be simplified to '\\d'
in `impl/src/main/java/org/apache/myfaces/resource/ExternalContextResourceLoader.java`
#### Snippet
```java
     * Used on getLibraryVersion to filter resource directories
     **/
    protected static final Pattern VERSION_CHECKER = Pattern.compile("/\\p{Digit}+(_\\p{Digit}*)*/");

    /**
```

### RegExpSimplifiable
`\\p{Digit}` can be simplified to '\\d'
in `impl/src/main/java/org/apache/myfaces/resource/ExternalContextResourceLoader.java`
#### Snippet
```java
     * Used on getLibraryVersion to filter resource directories
     **/
    protected static final Pattern VERSION_CHECKER = Pattern.compile("/\\p{Digit}+(_\\p{Digit}*)*/");

    /**
```

### RegExpSimplifiable
`\\p{Digit}` can be simplified to '\\d'
in `impl/src/main/java/org/apache/myfaces/resource/ExternalContextResourceLoader.java`
#### Snippet
```java
     * Used on getResourceVersion to filter resources
     **/
    protected static final Pattern RESOURCE_VERSION_CHECKER = Pattern.compile("/\\p{Digit}+(_\\p{Digit}*)*\\..*");

    public ExternalContextResourceLoader(String prefix)
```

### RegExpSimplifiable
`\\p{Digit}` can be simplified to '\\d'
in `impl/src/main/java/org/apache/myfaces/resource/ExternalContextResourceLoader.java`
#### Snippet
```java
     * Used on getResourceVersion to filter resources
     **/
    protected static final Pattern RESOURCE_VERSION_CHECKER = Pattern.compile("/\\p{Digit}+(_\\p{Digit}*)*\\..*");

    public ExternalContextResourceLoader(String prefix)
```

### RegExpSimplifiable
`\\p{Digit}` can be simplified to '\\d'
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
    private static final Logger log = Logger.getLogger(ResourceHandlerImpl.class.getName());

    public static final Pattern LIBRARY_VERSION_CHECKER = Pattern.compile("\\p{Digit}+(_\\p{Digit}*)*");
    public static final Pattern RESOURCE_VERSION_CHECKER = Pattern.compile("\\p{Digit}+(_\\p{Digit}*)*\\..*");    
    
```

### RegExpSimplifiable
`\\p{Digit}` can be simplified to '\\d'
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
    private static final Logger log = Logger.getLogger(ResourceHandlerImpl.class.getName());

    public static final Pattern LIBRARY_VERSION_CHECKER = Pattern.compile("\\p{Digit}+(_\\p{Digit}*)*");
    public static final Pattern RESOURCE_VERSION_CHECKER = Pattern.compile("\\p{Digit}+(_\\p{Digit}*)*\\..*");    
    
```

### RegExpSimplifiable
`\\p{Digit}` can be simplified to '\\d'
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java

    public static final Pattern LIBRARY_VERSION_CHECKER = Pattern.compile("\\p{Digit}+(_\\p{Digit}*)*");
    public static final Pattern RESOURCE_VERSION_CHECKER = Pattern.compile("\\p{Digit}+(_\\p{Digit}*)*\\..*");    
    
    public final static String RENDERED_RESOURCES_SET = "org.apache.myfaces.RENDERED_RESOURCES_SET";
```

### RegExpSimplifiable
`\\p{Digit}` can be simplified to '\\d'
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java

    public static final Pattern LIBRARY_VERSION_CHECKER = Pattern.compile("\\p{Digit}+(_\\p{Digit}*)*");
    public static final Pattern RESOURCE_VERSION_CHECKER = Pattern.compile("\\p{Digit}+(_\\p{Digit}*)*\\..*");    
    
    public final static String RENDERED_RESOURCES_SET = "org.apache.myfaces.RENDERED_RESOURCES_SET";
```

### RegExpSimplifiable
`\\p{Digit}` can be simplified to '\\d'
in `test/src/main/java/org/apache/myfaces/test/mock/resource/MockExternalContextResourceLoader.java`
#### Snippet
```java
     **/
    protected static final Pattern RESOURCE_VERSION_CHECKER = Pattern
            .compile("/\\p{Digit}+(_\\p{Digit}*)*\\..*");

    public MockExternalContextResourceLoader(String prefix)
```

### RegExpSimplifiable
`\\p{Digit}` can be simplified to '\\d'
in `test/src/main/java/org/apache/myfaces/test/mock/resource/MockExternalContextResourceLoader.java`
#### Snippet
```java
     **/
    protected static final Pattern RESOURCE_VERSION_CHECKER = Pattern
            .compile("/\\p{Digit}+(_\\p{Digit}*)*\\..*");

    public MockExternalContextResourceLoader(String prefix)
```

### RegExpSimplifiable
`\\p{Digit}` can be simplified to '\\d'
in `test/src/main/java/org/apache/myfaces/test/mock/resource/MockExternalContextResourceLoader.java`
#### Snippet
```java
     **/
    protected static final Pattern VERSION_CHECKER = Pattern
            .compile("/\\p{Digit}+(_\\p{Digit}*)*/");

    /**
```

### RegExpSimplifiable
`\\p{Digit}` can be simplified to '\\d'
in `test/src/main/java/org/apache/myfaces/test/mock/resource/MockExternalContextResourceLoader.java`
#### Snippet
```java
     **/
    protected static final Pattern VERSION_CHECKER = Pattern
            .compile("/\\p{Digit}+(_\\p{Digit}*)*/");

    /**
```

### RegExpSimplifiable
`\\p{Digit}` can be simplified to '\\d'
in `test/src/main/java/org/apache/myfaces/test/mock/resource/MockSimpleResourceHandler.java`
#### Snippet
```java
     */
    protected static final Pattern VERSION_CHECKER = Pattern
            .compile("/\\p{Digit}+(_\\p{Digit}*)*/");

    /**
```

### RegExpSimplifiable
`\\p{Digit}` can be simplified to '\\d'
in `test/src/main/java/org/apache/myfaces/test/mock/resource/MockSimpleResourceHandler.java`
#### Snippet
```java
     */
    protected static final Pattern VERSION_CHECKER = Pattern
            .compile("/\\p{Digit}+(_\\p{Digit}*)*/");

    /**
```

### RegExpSimplifiable
`\\p{Digit}` can be simplified to '\\d'
in `test/src/main/java/org/apache/myfaces/test/mock/resource/MockSimpleResourceHandler.java`
#### Snippet
```java
     */
    protected static final Pattern RESOURCE_VERSION_CHECKER = Pattern
            .compile("/\\p{Digit}+(_\\p{Digit}*)*\\..*");

    private File _documentRoot;
```

### RegExpSimplifiable
`\\p{Digit}` can be simplified to '\\d'
in `test/src/main/java/org/apache/myfaces/test/mock/resource/MockSimpleResourceHandler.java`
#### Snippet
```java
     */
    protected static final Pattern RESOURCE_VERSION_CHECKER = Pattern
            .compile("/\\p{Digit}+(_\\p{Digit}*)*\\..*");

    private File _documentRoot;
```

## RuleId[ruleID=StringBufferReplaceableByString]
### StringBufferReplaceableByString
`StringBuilder buffer` can be replaced with 'String'
in `api/src/main/java/jakarta/faces/webapp/FacesServlet.java`
#### Snippet
```java
        if (pathInfo != null && (pathInfo.startsWith("/WEB-INF") || pathInfo.startsWith("/META-INF")))
        {
            StringBuilder buffer = new StringBuilder();

            buffer.append(" Someone is trying to access a secure resource : ").append(pathInfo);
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `impl/src/main/java/org/apache/myfaces/flow/FlowImpl.java`
#### Snippet
```java
        // and the expected id cannot be null.
        String windowId = curWindow.getId();
        StringBuilder sb = new StringBuilder( id.length() + 1 + windowId.length() );
        sb.append(windowId).append('_').append(documentId).append('_').append(id);
        return sb.toString();
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/SAXCompiler.java`
#### Snippet
```java
                && !unit.getFaceletsProcessingInstructions().isConsumeProcessingInstructions())
            {
                StringBuilder sb = new StringBuilder(64);
                sb.append("<?").append(target).append(' ').append(data).append("?>\n");
                this.unit.writeInstruction(sb.toString(), createLocation());
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/SAXCompiler.java`
#### Snippet
```java
            if (inDocument && inMetadata && !unit.getFaceletsProcessingInstructions().isConsumeProcessingInstructions())
            {
                StringBuilder sb = new StringBuilder(64);
                sb.append("<?").append(target).append(' ').append(data).append("?>\n");
                unit.writeInstruction(sb.toString(), createLocation());
```

### StringBufferReplaceableByString
`StringBuilder sb` can be replaced with 'String'
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/SAXCompiler.java`
#### Snippet
```java
            if (this.inDocument && !this.unit.getFaceletsProcessingInstructions().isConsumeProcessingInstructions())
            {
                StringBuilder sb = new StringBuilder(64);
                sb.append("<?").append(target).append(' ').append(data).append("?>\n");
                this.unit.writeInstruction(sb.toString(), createLocation());
```

### StringBufferReplaceableByString
`StringBuilder finalUrl` can be replaced with 'String'
in `impl/src/main/java/org/apache/myfaces/lifecycle/clientwindow/CODIClientSideWindow.java`
#### Snippet
```java
                                       boolean addRequestParameter, boolean addPageParameter, boolean encodeValues)
    {
        StringBuilder finalUrl = new StringBuilder(url);
        boolean existingParameters = url.contains("?");
        boolean urlContainsWindowId = url.contains(ResponseStateManager.CLIENT_WINDOW_URL_PARAM + '=');
```

### StringBufferReplaceableByString
`StringBuffer buffer` can be replaced with 'String'
in `test/src/main/java/org/apache/myfaces/test/base/junit/runners/NamedRunner.java`
#### Snippet
```java
    private static String getKey()
    {
        StringBuffer buffer = new StringBuffer();
        buffer.append(PREFIX_KEY).append(Thread.currentThread().getName());
        return buffer.toString();
```

## RuleId[ruleID=NonShortCircuitBoolean]
### NonShortCircuitBoolean
Non-short-circuit boolean expression `span |= HtmlRendererUtils.renderHTMLAttributeWithOptionalStartElement( writer, messa...`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessageRendererBase.java`
#### Snippet
```java
        }

        span |= HtmlRendererUtils.renderHTMLAttributeWithOptionalStartElement(
                writer, message, HTML.SPAN_ELEM, HTML.TITLE_ATTR, title, span);
        if (renderStyleAndStyleClass)
        {
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `span |= HtmlRendererUtils.renderHTMLAttributeWithOptionalStartElement( writer, m...`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessageRendererBase.java`
#### Snippet
```java
        if (renderStyleAndStyleClass)
        {
            span |= HtmlRendererUtils.renderHTMLAttributeWithOptionalStartElement(
                    writer, message, HTML.SPAN_ELEM, HTML.STYLE_ATTR, style, span);
            span |= HtmlRendererUtils.renderHTMLAttributeWithOptionalStartElement(
                    writer, message, HTML.SPAN_ELEM, HTML.STYLE_CLASS_ATTR, styleClass, span);
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `span |= HtmlRendererUtils.renderHTMLAttributeWithOptionalStartElement( writer, m...`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessageRendererBase.java`
#### Snippet
```java
            span |= HtmlRendererUtils.renderHTMLAttributeWithOptionalStartElement(
                    writer, message, HTML.SPAN_ELEM, HTML.STYLE_ATTR, style, span);
            span |= HtmlRendererUtils.renderHTMLAttributeWithOptionalStartElement(
                    writer, message, HTML.SPAN_ELEM, HTML.STYLE_CLASS_ATTR, styleClass, span);
            // Remember if renderStyleAndStyleClass is true, it means style and styleClass
            // are rendered on the outer tag, and in that sense, role attribute should
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `span |= HtmlRendererUtils.renderHTMLAttributeWithOptionalStartElement( writer, m...`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessageRendererBase.java`
#### Snippet
```java
            // are rendered on the outer tag, and in that sense, role attribute should
            // be rendered there too.
            span |= HtmlRendererUtils.renderHTMLAttributeWithOptionalStartElement(
                    writer, message, HTML.ROLE_ATTR, HTML.ROLE_ATTR, 
                    message.getAttributes().get(HTML.ROLE_ATTR), span);
        }

```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `(obj != null) & (obj instanceof ValueExpression)`
in `test/src/main/java/org/apache/myfaces/test/el/MockVariableValueExpression.java`
#### Snippet
```java
    {

        if ((obj != null) & (obj instanceof ValueExpression))
        {
            return instance.toString().equals(
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `(obj != null) & (obj instanceof ValueExpression)`
in `test/src/main/java/org/apache/myfaces/test/el/MockValueExpression.java`
#### Snippet
```java
    {

        if ((obj != null) & (obj instanceof ValueExpression))
        {
            return expression.equals(((ValueExpression) obj)
```

### NonShortCircuitBoolean
Non-short-circuit boolean expression `(obj != null) & (obj instanceof MethodExpression)`
in `test/src/main/java/org/apache/myfaces/test/el/MockMethodExpression.java`
#### Snippet
```java
    {

        if ((obj != null) & (obj instanceof MethodExpression))
        {
            return expression.equals(((MethodExpression) obj)
```

## RuleId[ruleID=UnnecessaryReturn]
### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `api/src/main/java/jakarta/faces/component/UIViewAction.java`
#### Snippet
```java
                if (context.getResponseComplete())
                {
                    return;
                }
                else
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `api/src/main/java/jakarta/faces/component/behavior/BehaviorBase.java`
#### Snippet
```java
        if (state == null)
        {
            return;
        }
        else if (state instanceof _AttachedDeltaWrapper)
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/ExtensionHandler.java`
#### Snippet
```java
                log.severe("Cannot find composite bean descriptor UIComponent.BEANINFO_KEY ");
            }
            return;
        }
    }
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `impl/src/main/java/org/apache/myfaces/context/MyFacesExceptionHandlerWrapperImpl.java`
#### Snippet
```java
                throw e;
            }
            return;
        }
        else
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlScriptRenderer.java`
#### Snippet
```java
                    " not found in call to ResourceHandler.createResource." +
                    " It will be silenty ignored.");
            return;
        }
        else
```

### UnnecessaryReturn
`return` is unnecessary as the last statement in a 'void' method
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlStylesheetRenderer.java`
#### Snippet
```java
                    " not found in call to ResourceHandler.createResource."+
                    " It will be silenty ignored.");
            return;
        }
        else
```

## RuleId[ruleID=AbstractClassNeverImplemented]
### AbstractClassNeverImplemented
Abstract class `FlowHandlerFactoryWrapper` has no concrete subclass
in `api/src/main/java/jakarta/faces/flow/FlowHandlerFactoryWrapper.java`
#### Snippet
```java
 * @since 2.2
 */
public abstract class FlowHandlerFactoryWrapper extends FlowHandlerFactory
    implements FacesWrapper<FlowHandlerFactory>
{
```

### AbstractClassNeverImplemented
Abstract class `ViewDeclarationLanguageWrapper` has no concrete subclass
in `api/src/main/java/jakarta/faces/view/ViewDeclarationLanguageWrapper.java`
#### Snippet
```java
 * @since 2.2
 */
public abstract class ViewDeclarationLanguageWrapper extends ViewDeclarationLanguage 
    implements FacesWrapper<ViewDeclarationLanguage>
{
```

### AbstractClassNeverImplemented
Abstract class `ActionListenerWrapper` has no concrete subclass
in `api/src/main/java/jakarta/faces/event/ActionListenerWrapper.java`
#### Snippet
```java
import jakarta.faces.FacesWrapper;

public abstract class ActionListenerWrapper implements ActionListener, FacesWrapper<ActionListener>
{

```

### AbstractClassNeverImplemented
Abstract class `RenderKitWrapper` has no concrete subclass
in `api/src/main/java/jakarta/faces/render/RenderKitWrapper.java`
#### Snippet
```java
 * @since 2.0
 */
public abstract class RenderKitWrapper extends RenderKit implements FacesWrapper<RenderKit>
{
    private RenderKit delegate;
```

### AbstractClassNeverImplemented
Abstract class `PartialViewContextWrapper` has no concrete subclass
in `api/src/main/java/jakarta/faces/context/PartialViewContextWrapper.java`
#### Snippet
```java
 * @since 2.0
 */
public abstract class PartialViewContextWrapper extends PartialViewContext implements FacesWrapper<PartialViewContext>
{
    private PartialViewContext delegate;
```

### AbstractClassNeverImplemented
Abstract class `FlashWrapper` has no concrete subclass
in `api/src/main/java/jakarta/faces/context/FlashWrapper.java`
#### Snippet
```java
 * @since 2.2
 */
public abstract class FlashWrapper extends Flash implements FacesWrapper<Flash>
{
    private Flash delegate;
```

### AbstractClassNeverImplemented
Abstract class `ExternalContextWrapper` has no concrete subclass
in `api/src/main/java/jakarta/faces/context/ExternalContextWrapper.java`
#### Snippet
```java
 * @since 2.0
 */
public abstract class ExternalContextWrapper extends ExternalContext implements FacesWrapper<ExternalContext>
{

```

### AbstractClassNeverImplemented
Abstract class `_UIMessage` has no concrete subclass
in `api/src/main/java/jakarta/faces/component/_UIMessage.java`
#### Snippet
```java
 */
@JSFComponent(clazz = "jakarta.faces.component.UIMessage", template = true, defaultRendererType = "jakarta.faces.Message")
abstract class _UIMessage extends UIComponentBase
{

```

### AbstractClassNeverImplemented
Abstract class `_UISelectItems` has no concrete subclass
in `api/src/main/java/jakarta/faces/component/_UISelectItems.java`
#### Snippet
```java
              name = "f:selectItems",
              bodyContent = "empty")
abstract class _UISelectItems extends UIComponentBase
{

```

### AbstractClassNeverImplemented
Abstract class `_UISelectItemGroup` has no concrete subclass
in `api/src/main/java/jakarta/faces/component/_UISelectItemGroup.java`
#### Snippet
```java
              name = "f:selectItemGroup",
              bodyContent = "empty")
abstract class _UISelectItemGroup extends UISelectItem
{
    static public final String COMPONENT_FAMILY = "jakarta.faces.SelectItem";
```

### AbstractClassNeverImplemented
Abstract class `_UISelectItem` has no concrete subclass
in `api/src/main/java/jakarta/faces/component/_UISelectItem.java`
#### Snippet
```java
@JSFComponent(clazz = "jakarta.faces.component.UISelectItem", template = true, name = "f:selectItem",
        bodyContent = "empty")
abstract class _UISelectItem extends UIComponentBase
{

```

### AbstractClassNeverImplemented
Abstract class `_UIWebsocket` has no concrete subclass
in `api/src/main/java/jakarta/faces/component/_UIWebsocket.java`
#### Snippet
```java
        implementz = "jakarta.faces.component.behavior.ClientBehaviorHolder",
        bodyContent = "empty")
abstract class _UIWebsocket extends UIComponentBase
{
    
```

### AbstractClassNeverImplemented
Abstract class `_UIMessages` has no concrete subclass
in `api/src/main/java/jakarta/faces/component/_UIMessages.java`
#### Snippet
```java
@JSFComponent(clazz = "jakarta.faces.component.UIMessages", template=true,
        defaultRendererType = "jakarta.faces.Messages")
abstract class _UIMessages extends UIComponentBase
{

```

### AbstractClassNeverImplemented
Abstract class `_UISelectItemGroups` has no concrete subclass
in `api/src/main/java/jakarta/faces/component/_UISelectItemGroups.java`
#### Snippet
```java
              name = "f:selectItemGroups",
              bodyContent = "empty")
abstract class _UISelectItemGroups extends UISelectItems
{

```

### AbstractClassNeverImplemented
Abstract class `_HtmlPanelGrid` has no concrete subclass
in `api/src/main/java/jakarta/faces/component/html/_HtmlPanelGrid.java`
#### Snippet
```java
        defaultRendererType = "jakarta.faces.Grid",
        implementz = "jakarta.faces.component.behavior.ClientBehaviorHolder")
abstract class _HtmlPanelGrid extends UIPanel implements _EventProperties,
_StyleProperties, _UniversalProperties, _RoleProperty
{
```

### AbstractClassNeverImplemented
Abstract class `_HtmlSelectOneMenu` has no concrete subclass
in `api/src/main/java/jakarta/faces/component/html/_HtmlSelectOneMenu.java`
#### Snippet
```java
        implementz = "jakarta.faces.component.behavior.ClientBehaviorHolder",
        defaultEventName = "valueChange")
abstract class _HtmlSelectOneMenu extends UISelectOne implements
    _AccesskeyProperty, _UniversalProperties, _DisabledReadonlyProperties,
    _FocusBlurProperties, _ChangeProperty, _EventProperties,
```

### AbstractClassNeverImplemented
Abstract class `_UIParameter` has no concrete subclass
in `api/src/main/java/jakarta/faces/component/_UIParameter.java`
#### Snippet
```java
 */
@JSFComponent(clazz = "jakarta.faces.component.UIParameter", template = true, name = "f:param")
abstract class _UIParameter extends UIComponentBase
{

```

### AbstractClassNeverImplemented
Abstract class `_HtmlBody` has no concrete subclass
in `api/src/main/java/jakarta/faces/component/html/_HtmlBody.java`
#### Snippet
```java
        defaultRendererType = "jakarta.faces.Body", template = true,
        implementz = "jakarta.faces.component.behavior.ClientBehaviorHolder")
abstract class _HtmlBody extends UIOutput 
    implements _EventProperties, _UniversalProperties, _StyleProperties, _RoleProperty
{
```

### AbstractClassNeverImplemented
Abstract class `_HtmlSelectOneListbox` has no concrete subclass
in `api/src/main/java/jakarta/faces/component/html/_HtmlSelectOneListbox.java`
#### Snippet
```java
        implementz = "jakarta.faces.component.behavior.ClientBehaviorHolder",
        defaultEventName = "valueChange")
abstract class _HtmlSelectOneListbox extends UISelectOne implements
    _AccesskeyProperty, _UniversalProperties, _DisabledReadonlyProperties,
    _FocusBlurProperties, _ChangeProperty, _EventProperties,
```

### AbstractClassNeverImplemented
Abstract class `_HtmlOutcomeTargetButton` has no concrete subclass
in `api/src/main/java/jakarta/faces/component/html/_HtmlOutcomeTargetButton.java`
#### Snippet
```java
        implementz = "jakarta.faces.component.behavior.ClientBehaviorHolder",
        defaultRendererType = "jakarta.faces.Button")
abstract class _HtmlOutcomeTargetButton extends UIOutcomeTarget implements _FocusBlurProperties,
_EventProperties, _StyleProperties, _UniversalProperties, _AccesskeyProperty,
_TabindexProperty, _AltProperty, _RoleProperty
```

### AbstractClassNeverImplemented
Abstract class `_HtmlCommandScript` has no concrete subclass
in `api/src/main/java/jakarta/faces/component/html/_HtmlCommandScript.java`
#### Snippet
```java
        defaultRendererType = "jakarta.faces.Script",
        defaultEventName = "action")
abstract class _HtmlCommandScript extends UICommand
{
    static public final String COMPONENT_FAMILY =
```

### AbstractClassNeverImplemented
Abstract class `_HtmlForm` has no concrete subclass
in `api/src/main/java/jakarta/faces/component/html/_HtmlForm.java`
#### Snippet
```java
        defaultRendererType = "jakarta.faces.Form",
        implementz = "jakarta.faces.component.behavior.ClientBehaviorHolder")
abstract class _HtmlForm extends UIForm
    implements _EventProperties, _UniversalProperties, _StyleProperties, _RoleProperty
{
```

### AbstractClassNeverImplemented
Abstract class `_HtmlGraphicImage` has no concrete subclass
in `api/src/main/java/jakarta/faces/component/html/_HtmlGraphicImage.java`
#### Snippet
```java
        defaultRendererType = "jakarta.faces.Image",
        implementz = "jakarta.faces.component.behavior.ClientBehaviorHolder")
abstract class _HtmlGraphicImage extends UIGraphic 
    implements _EventProperties,
    _StyleProperties, _UniversalProperties, _AltProperty, _RoleProperty
```

### AbstractClassNeverImplemented
Abstract class `_HtmlInputSecret` has no concrete subclass
in `api/src/main/java/jakarta/faces/component/html/_HtmlInputSecret.java`
#### Snippet
```java
        implementz = "jakarta.faces.component.behavior.ClientBehaviorHolder",
        defaultEventName = "valueChange")
abstract class _HtmlInputSecret extends UIInput implements _AccesskeyProperty,
    _AltProperty, _UniversalProperties, _FocusBlurProperties, _EventProperties,
    _StyleProperties, _TabindexProperty, _ChangeProperty, _SelectProperty,
```

### AbstractClassNeverImplemented
Abstract class `_HtmlCommandLink` has no concrete subclass
in `api/src/main/java/jakarta/faces/component/html/_HtmlCommandLink.java`
#### Snippet
```java
        implementz = "jakarta.faces.component.behavior.ClientBehaviorHolder",
        defaultEventName = "action")
abstract class _HtmlCommandLink extends UICommand
    implements _EventProperties, _UniversalProperties, _StyleProperties,
    _FocusBlurProperties, _AccesskeyProperty, _TabindexProperty,
```

### AbstractClassNeverImplemented
Abstract class `_HtmlInputText` has no concrete subclass
in `api/src/main/java/jakarta/faces/component/html/_HtmlInputText.java`
#### Snippet
```java
    implementz = "jakarta.faces.component.behavior.ClientBehaviorHolder",
    defaultEventName = "valueChange")
abstract class _HtmlInputText extends UIInput implements _AccesskeyProperty,
    _AltProperty, _UniversalProperties, _DisabledReadonlyProperties,
    _FocusBlurProperties, _ChangeProperty, _SelectProperty, 
```

### AbstractClassNeverImplemented
Abstract class `_HtmlColumn` has no concrete subclass
in `api/src/main/java/jakarta/faces/component/html/_HtmlColumn.java`
#### Snippet
```java
        clazz = "jakarta.faces.component.html.HtmlColumn",template=true,
        tagClass = "org.apache.myfaces.taglib.html.HtmlColumnTag")
abstract class _HtmlColumn extends UIColumn
{

```

### AbstractClassNeverImplemented
Abstract class `_HtmlPanelGroup` has no concrete subclass
in `api/src/main/java/jakarta/faces/component/html/_HtmlPanelGroup.java`
#### Snippet
```java
        defaultRendererType = "jakarta.faces.Group",
        implementz = "jakarta.faces.component.behavior.ClientBehaviorHolder")
abstract class _HtmlPanelGroup extends UIPanel implements _StyleProperties, _EventProperties
{

```

### AbstractClassNeverImplemented
Abstract class `_HtmlDoctype` has no concrete subclass
in `api/src/main/java/jakarta/faces/component/html/_HtmlDoctype.java`
#### Snippet
```java
        template=true,
        implementz="jakarta.faces.component.Doctype")
abstract class _HtmlDoctype extends UIOutput
{

```

### AbstractClassNeverImplemented
Abstract class `_HtmlSelectOneRadio` has no concrete subclass
in `api/src/main/java/jakarta/faces/component/html/_HtmlSelectOneRadio.java`
#### Snippet
```java
        implementz = "jakarta.faces.component.behavior.ClientBehaviorHolder",
        defaultEventName = "valueChange")
abstract class _HtmlSelectOneRadio extends UISelectOne implements
    _AccesskeyProperty, _UniversalProperties, _DisabledReadonlyProperties,
    _FocusBlurProperties, _ChangeProperty, _SelectProperty,
```

### AbstractClassNeverImplemented
Abstract class `_HtmlInputTextarea` has no concrete subclass
in `api/src/main/java/jakarta/faces/component/html/_HtmlInputTextarea.java`
#### Snippet
```java
    implementz = "jakarta.faces.component.behavior.ClientBehaviorHolder",
    defaultEventName = "valueChange")
abstract class _HtmlInputTextarea extends UIInput implements _AccesskeyProperty,
    _UniversalProperties, _FocusBlurProperties, _ChangeProperty,
    _SelectProperty, _EventProperties, _StyleProperties, 
```

### AbstractClassNeverImplemented
Abstract class `_HtmlCommandButton` has no concrete subclass
in `api/src/main/java/jakarta/faces/component/html/_HtmlCommandButton.java`
#### Snippet
```java
        implementz = "jakarta.faces.component.behavior.ClientBehaviorHolder",
        defaultEventName = "action")
abstract class _HtmlCommandButton extends UICommand implements _FocusBlurProperties, 
    _EventProperties, _StyleProperties, _UniversalProperties,
    _AccesskeyProperty, _TabindexProperty, _AltProperty, 
```

### AbstractClassNeverImplemented
Abstract class `_HtmlSelectManyCheckbox` has no concrete subclass
in `api/src/main/java/jakarta/faces/component/html/_HtmlSelectManyCheckbox.java`
#### Snippet
```java
        implementz = "jakarta.faces.component.behavior.ClientBehaviorHolder",
        defaultEventName = "valueChange")
abstract class _HtmlSelectManyCheckbox extends UISelectMany implements 
    _AccesskeyProperty, _UniversalProperties, _FocusBlurProperties,
    _ChangeProperty, _SelectProperty, _EventProperties,
```

### AbstractClassNeverImplemented
Abstract class `_HtmlOutputLink` has no concrete subclass
in `api/src/main/java/jakarta/faces/component/html/_HtmlOutputLink.java`
#### Snippet
```java
        defaultRendererType = "jakarta.faces.Link",
        implementz = "jakarta.faces.component.behavior.ClientBehaviorHolder")
abstract class _HtmlOutputLink extends UIOutput implements _AccesskeyProperty,
_UniversalProperties, _FocusBlurProperties, _EventProperties, _StyleProperties,
_TabindexProperty, _LinkProperties, _RoleProperty
```

### AbstractClassNeverImplemented
Abstract class `_HtmlSelectManyMenu` has no concrete subclass
in `api/src/main/java/jakarta/faces/component/html/_HtmlSelectManyMenu.java`
#### Snippet
```java
        implementz = "jakarta.faces.component.behavior.ClientBehaviorHolder",
        defaultEventName = "valueChange")
abstract class _HtmlSelectManyMenu extends UISelectMany implements
    _AccesskeyProperty, _UniversalProperties, _DisabledReadonlyProperties,
    _FocusBlurProperties, _ChangeProperty, _EventProperties,
```

### AbstractClassNeverImplemented
Abstract class `_HtmlHead` has no concrete subclass
in `api/src/main/java/jakarta/faces/component/html/_HtmlHead.java`
#### Snippet
```java
@JSFComponent(name="h:head", clazz = "jakarta.faces.component.html.HtmlHead",
        defaultRendererType="jakarta.faces.Head",template=true)
abstract class _HtmlHead extends UIOutput 
{

```

### AbstractClassNeverImplemented
Abstract class `_HtmlSelectBooleanCheckbox` has no concrete subclass
in `api/src/main/java/jakarta/faces/component/html/_HtmlSelectBooleanCheckbox.java`
#### Snippet
```java
        implementz = "jakarta.faces.component.behavior.ClientBehaviorHolder",
        defaultEventName = "valueChange")
abstract class _HtmlSelectBooleanCheckbox extends UISelectBoolean implements
    _AccesskeyProperty, _UniversalProperties, _DisabledReadonlyProperties,
    _FocusBlurProperties, _ChangeProperty, _SelectProperty, 
```

### AbstractClassNeverImplemented
Abstract class `_HtmlOutcomeTargetLink` has no concrete subclass
in `api/src/main/java/jakarta/faces/component/html/_HtmlOutcomeTargetLink.java`
#### Snippet
```java
        implementz = "jakarta.faces.component.behavior.ClientBehaviorHolder",
        defaultRendererType = "jakarta.faces.Link")
abstract class _HtmlOutcomeTargetLink extends UIOutcomeTarget implements _FocusBlurProperties,
_EventProperties, _StyleProperties, _UniversalProperties, _AccesskeyProperty,
_TabindexProperty, _LinkProperties, _RoleProperty
```

### AbstractClassNeverImplemented
Abstract class `_HtmlDataTable` has no concrete subclass
in `api/src/main/java/jakarta/faces/component/html/_HtmlDataTable.java`
#### Snippet
```java
        defaultRendererType = "jakarta.faces.Table",
        implementz = "jakarta.faces.component.behavior.ClientBehaviorHolder")
abstract class _HtmlDataTable extends UIData
    implements _EventProperties, _StyleProperties, _UniversalProperties, _RoleProperty
{
```

### AbstractClassNeverImplemented
Abstract class `_HtmlMessage` has no concrete subclass
in `api/src/main/java/jakarta/faces/component/html/_HtmlMessage.java`
#### Snippet
```java
        tagClass = "org.apache.myfaces.taglib.html.HtmlMessageTag",
        defaultRendererType = "jakarta.faces.Message")
abstract class _HtmlMessage extends UIMessage implements _StyleProperties, 
    _MessageProperties, _UniversalProperties, _RoleProperty
{
```

### AbstractClassNeverImplemented
Abstract class `_HtmlInputFile` has no concrete subclass
in `api/src/main/java/jakarta/faces/component/html/_HtmlInputFile.java`
#### Snippet
```java
    defaultEventName = "valueChange"
)
abstract class _HtmlInputFile extends UIInput implements _AccesskeyProperty,
    _AltProperty, _UniversalProperties, _DisabledReadonlyProperties,
    _FocusBlurProperties, _ChangeProperty, _SelectProperty,
```

### AbstractClassNeverImplemented
Abstract class `_HtmlOutputFormat` has no concrete subclass
in `api/src/main/java/jakarta/faces/component/html/_HtmlOutputFormat.java`
#### Snippet
```java
        tagClass = "org.apache.myfaces.taglib.html.HtmlOutputFormatTag",
        defaultRendererType = "jakarta.faces.Format")
abstract class _HtmlOutputFormat extends UIOutput implements _StyleProperties, 
    _EscapeProperty, _TitleProperty, _UniversalProperties, _RoleProperty
{
```

### AbstractClassNeverImplemented
Abstract class `_HtmlOutputText` has no concrete subclass
in `api/src/main/java/jakarta/faces/component/html/_HtmlOutputText.java`
#### Snippet
```java
        tagClass = "org.apache.myfaces.taglib.html.HtmlOutputTextTag",
        defaultRendererType = "jakarta.faces.Text")
abstract class _HtmlOutputText extends UIOutput implements _EscapeProperty, 
_StyleProperties, _UniversalProperties, _RoleProperty
{
```

### AbstractClassNeverImplemented
Abstract class `_HtmlMessages` has no concrete subclass
in `api/src/main/java/jakarta/faces/component/html/_HtmlMessages.java`
#### Snippet
```java
        tagClass = "org.apache.myfaces.taglib.html.HtmlMessagesTag",
        defaultRendererType = "jakarta.faces.Messages")
abstract class _HtmlMessages extends UIMessages implements _StyleProperties, 
_MessageProperties, _UniversalProperties, _RoleProperty
{
```

### AbstractClassNeverImplemented
Abstract class `_HtmlSelectManyListbox` has no concrete subclass
in `api/src/main/java/jakarta/faces/component/html/_HtmlSelectManyListbox.java`
#### Snippet
```java
        implementz = "jakarta.faces.component.behavior.ClientBehaviorHolder",
        defaultEventName = "valueChange")
abstract class _HtmlSelectManyListbox extends UISelectMany implements 
    _AccesskeyProperty, _UniversalProperties, _DisabledReadonlyProperties,
    _FocusBlurProperties, _ChangeProperty, _EventProperties,
```

### AbstractClassNeverImplemented
Abstract class `_HtmlOutputLabel` has no concrete subclass
in `api/src/main/java/jakarta/faces/component/html/_HtmlOutputLabel.java`
#### Snippet
```java
        defaultRendererType = "jakarta.faces.Label",
        implementz = "jakarta.faces.component.behavior.ClientBehaviorHolder")
abstract class _HtmlOutputLabel extends UIOutput implements _FocusBlurProperties,
_EventProperties, _StyleProperties, _UniversalProperties, _AccesskeyProperty,
_TabindexProperty, _EscapeProperty, _RoleProperty
```

### AbstractClassNeverImplemented
Abstract class `VisitContextWrapper` has no concrete subclass
in `api/src/main/java/jakarta/faces/component/visit/VisitContextWrapper.java`
#### Snippet
```java
 * @since 2.0
 */
public abstract class VisitContextWrapper extends VisitContext implements FacesWrapper<VisitContext>
{
    private VisitContext delegate;
```

### AbstractClassNeverImplemented
Abstract class `SearchExpressionHandlerWrapper` has no concrete subclass
in `api/src/main/java/jakarta/faces/component/search/SearchExpressionHandlerWrapper.java`
#### Snippet
```java
 *
 */
public abstract class SearchExpressionHandlerWrapper extends SearchExpressionHandler
        implements FacesWrapper<SearchExpressionHandler>
{
```

### AbstractClassNeverImplemented
Abstract class `LifecycleWrapper` has no concrete subclass
in `api/src/main/java/jakarta/faces/lifecycle/LifecycleWrapper.java`
#### Snippet
```java
import jakarta.faces.event.PhaseListener;

public abstract class LifecycleWrapper extends Lifecycle implements FacesWrapper<Lifecycle>
{
    private Lifecycle delegate;
```

### AbstractClassNeverImplemented
Abstract class `ClientWindowWrapper` has no concrete subclass
in `api/src/main/java/jakarta/faces/lifecycle/ClientWindowWrapper.java`
#### Snippet
```java
 * @since 2.2
 */
public abstract class ClientWindowWrapper extends ClientWindow 
    implements FacesWrapper<ClientWindow>
{
```

### AbstractClassNeverImplemented
Abstract class `ResourceWrapper` has no concrete subclass
in `api/src/main/java/jakarta/faces/application/ResourceWrapper.java`
#### Snippet
```java
 * @since 2.0
 */
public abstract class ResourceWrapper extends Resource
    implements FacesWrapper<Resource>
{
```

### AbstractClassNeverImplemented
Abstract class `ResourceHandlerWrapper` has no concrete subclass
in `api/src/main/java/jakarta/faces/application/ResourceHandlerWrapper.java`
#### Snippet
```java
 * @since 2.0
 */
public abstract class ResourceHandlerWrapper extends ResourceHandler
    implements FacesWrapper<ResourceHandler>
{
```

### AbstractClassNeverImplemented
Abstract class `NavigationHandlerWrapper` has no concrete subclass
in `api/src/main/java/jakarta/faces/application/NavigationHandlerWrapper.java`
#### Snippet
```java
 * @since 2.2
 */
public abstract class NavigationHandlerWrapper extends NavigationHandler
    implements FacesWrapper<NavigationHandler>
{
```

### AbstractClassNeverImplemented
Abstract class `ApplicationConfigurationPopulator` has no concrete subclass
in `api/src/main/java/jakarta/faces/application/ApplicationConfigurationPopulator.java`
#### Snippet
```java
import org.w3c.dom.Document;

public abstract class ApplicationConfigurationPopulator
{
    
```

### AbstractClassNeverImplemented
Abstract class `ConfigurableNavigationHandlerWrapper` has no concrete subclass
in `api/src/main/java/jakarta/faces/application/ConfigurableNavigationHandlerWrapper.java`
#### Snippet
```java
 * @since 2.2
 */
public abstract class ConfigurableNavigationHandlerWrapper extends ConfigurableNavigationHandler
    implements FacesWrapper<ConfigurableNavigationHandler>
{
```

### AbstractClassNeverImplemented
Abstract class `ViewHandlerWrapper` has no concrete subclass
in `api/src/main/java/jakarta/faces/application/ViewHandlerWrapper.java`
#### Snippet
```java
 * see Javadoc of <a href="http://java.sun.com/j2ee/javaserverfaces/1.2/docs/api/index.html">Faces Specification</a>
 */
public abstract class ViewHandlerWrapper extends ViewHandler
    implements FacesWrapper<ViewHandler>
{
```

### AbstractClassNeverImplemented
Abstract class `StateManagerWrapper` has no concrete subclass
in `api/src/main/java/jakarta/faces/application/StateManagerWrapper.java`
#### Snippet
```java
 * see Javadoc of <a href="http://java.sun.com/j2ee/javaserverfaces/1.2/docs/api/index.html">Faces Specification</a>
 */
public abstract class StateManagerWrapper extends StateManager implements FacesWrapper<StateManager>
{
    private StateManager delegate;
```

### AbstractClassNeverImplemented
Abstract class `AbstractContext` has no concrete subclass
in `impl/src/main/java/org/apache/myfaces/cdi/util/AbstractContext.java`
#### Snippet
```java
 * NOTE: Taken from Apache DeltaSpike
 */
public abstract class AbstractContext implements Context
{
    /**
```

### AbstractClassNeverImplemented
Abstract class `FacesConfigurationProviderWrapper` has no concrete subclass
in `impl/src/main/java/org/apache/myfaces/spi/FacesConfigurationProviderWrapper.java`
#### Snippet
```java
 * @since 2.0.3
 */
public abstract class FacesConfigurationProviderWrapper 
    extends FacesConfigurationProvider 
    implements FacesWrapper<FacesConfigurationProvider>
```

### AbstractClassNeverImplemented
Abstract class `AnnotationProviderWrapper` has no concrete subclass
in `impl/src/main/java/org/apache/myfaces/spi/AnnotationProviderWrapper.java`
#### Snippet
```java
 * @author Leonardo Uribe 
 */
public abstract class AnnotationProviderWrapper extends AnnotationProvider implements FacesWrapper<AnnotationProvider>
{
    private AnnotationProvider delegate;
```

### AbstractClassNeverImplemented
Abstract class `_WebsocketInit` has no concrete subclass
in `impl/src/main/java/org/apache/myfaces/push/_WebsocketInit.java`
#### Snippet
```java
        type = "org.apache.myfaces.WebsocketInit",
        defaultRendererType="org.apache.myfaces.WebsocketInit")
public abstract class _WebsocketInit extends UIOutput
{
    static public final String ATTRIBUTE_COMPONENT_ID = _WebsocketInit.class.getName() + "ComponentId";
```

### AbstractClassNeverImplemented
Abstract class `AbstractWebsocketComponent` has no concrete subclass
in `impl/src/main/java/org/apache/myfaces/push/AbstractWebsocketComponent.java`
#### Snippet
```java
        type = "org.apache.myfaces.WebsocketComponent",
        defaultRendererType="org.apache.myfaces.WebsocketComponent")
public abstract class AbstractWebsocketComponent extends UIOutput implements ClientBehaviorHolder
{
    private static final Collection<String> EVERY_EVENT = Collections.unmodifiableList(new ArrayList<String>()
```

### AbstractClassNeverImplemented
Abstract class `_Fragment` has no concrete subclass
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/_Fragment.java`
#### Snippet
```java
        configExcluded=true,
        defaultRendererType="jakarta.faces.resource.Script")
abstract class _Fragment extends UIComponentBase
{
    public final static String COMPONENT_TYPE = "facelets.ui.ComponentRef";
```

### AbstractClassNeverImplemented
Abstract class `_Component` has no concrete subclass
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/_Component.java`
#### Snippet
```java
        configExcluded=true,
        defaultRendererType="jakarta.faces.resource.Script")
abstract class _Component extends UIComponentBase
{
    public final static String COMPONENT_TYPE = "facelets.ui.ComponentRef";
```

### AbstractClassNeverImplemented
Abstract class `_HtmlOutputStylesheet` has no concrete subclass
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/html/_HtmlOutputStylesheet.java`
#### Snippet
```java
 */
@JSFComponent(configExcluded = true, defaultRendererType = "jakarta.faces.resource.Stylesheet")
abstract class _HtmlOutputStylesheet extends UIOutput
{

```

### AbstractClassNeverImplemented
Abstract class `_HtmlOutputScript` has no concrete subclass
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/html/_HtmlOutputScript.java`
#### Snippet
```java
 */
@JSFComponent(configExcluded = true, defaultRendererType = "jakarta.faces.resource.Script")
abstract class _HtmlOutputScript extends UIOutput
{

```

### AbstractClassNeverImplemented
Abstract class `_JsfElement` has no concrete subclass
in `impl/src/main/java/org/apache/myfaces/view/facelets/component/_JsfElement.java`
#### Snippet
```java
    defaultEventName = "click",
    defaultRendererType="jakarta.faces.passthrough.Element")
abstract class _JsfElement extends UIPanel implements 
    _EventProperties, _FocusBlurProperties, _ChangeSelectProperties, _StyleProperties
{
```

### AbstractClassNeverImplemented
Abstract class `UnicodeEncoder` has no concrete subclass
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/UnicodeEncoder.java`
#### Snippet
```java
 * 
 */
public abstract class UnicodeEncoder
{
    /**
```

### AbstractClassNeverImplemented
Abstract class `HTMLEncoder` has no concrete subclass
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/HTMLEncoder.java`
#### Snippet
```java
 * Converts Strings so that they can be used within HTML-Code.
 */
public abstract class HTMLEncoder
{
    private static final String SB_ENCODE = HTMLEncoder.class.getName() + "#SB_ENCODE";
```

### AbstractClassNeverImplemented
Abstract class `AbstractViewControllerTestCase` has no concrete subclass
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractViewControllerTestCase.java`
#### Snippet
```java
 * @since 1.0.0
 */
public abstract class AbstractViewControllerTestCase extends
        AbstractJsfTestCase
{
```

### AbstractClassNeverImplemented
Abstract class `AbstractHtmlUnitTestCase` has no concrete subclass
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractHtmlUnitTestCase.java`
#### Snippet
```java
 * @since 1.0.0
 */
public abstract class AbstractHtmlUnitTestCase
{

```

## RuleId[ruleID=BoundedWildcard]
### BoundedWildcard
Can generalize to `? extends SelectItem`
in `api/src/main/java/org/apache/myfaces/core/api/shared/SelectItemsUtil.java`
#### Snippet
```java
    public static boolean matchValue(FacesContext context,
            UIComponent uiComponent, Object value,
            Iterator<SelectItem> iterator, Converter converter)
    {
        while (iterator.hasNext())
```

### BoundedWildcard
Can generalize to `? extends SelectItem`
in `api/src/main/java/org/apache/myfaces/core/api/shared/SelectItemsUtil.java`
#### Snippet
```java
    public static boolean isNoSelectionOption(FacesContext context,
            UIComponent uiComponent, Object value,
            Iterator<SelectItem> iterator, Converter converter)
    {
        while (iterator.hasNext())
```

### BoundedWildcard
Can generalize to `? extends SelectItem`
in `api/src/main/java/org/apache/myfaces/core/api/shared/SharedRendererUtils.java`
#### Snippet
```java
     * @return The first suitable Converter for the given SelectItems or null.
     */
    public static Converter getSelectItemsValueConverter(Iterator<SelectItem> iterator, FacesContext facesContext)
    {
        // Attention!
```

### BoundedWildcard
Can generalize to `? super String`
in `api/src/main/java/jakarta/faces/model/ResultSetDataModel.java`
#### Snippet
```java
        private int _currentIndex;

        public WrapResultSetMap(Comparator<String> comparator) throws SQLException
        {
            super(comparator);
```

### BoundedWildcard
Can generalize to `? extends Map.Entry`
in `api/src/main/java/jakarta/faces/component/_ComponentFacetMap.java`
#### Snippet
```java
        private V _currentEntryValue;
        
        public ComponentFacetKeyIterator(Iterator<Map.Entry<String, V>> it)
        {
            _delegate = it;
```

### BoundedWildcard
Can generalize to `? extends Map.Entry`
in `api/src/main/java/jakarta/faces/component/_ComponentFacetMap.java`
#### Snippet
```java
        private V _currentEntryValue;
        
        public ComponentFacetValueIterator(Iterator<Map.Entry<String, V>> it)
        {
            _delegate = it;
```

### BoundedWildcard
Can generalize to `? extends Map.Entry`
in `api/src/main/java/jakarta/faces/component/_ComponentFacetMap.java`
#### Snippet
```java
        private V _currentEntryValue;
        
        public ComponentFacetEntryIterator(Iterator<Map.Entry<String, V>> it)
        {
            _delegate = it;
```

### BoundedWildcard
Can generalize to `? super Serializable`
in `api/src/main/java/jakarta/faces/component/_DeltaStateHelper.java`
#### Snippet
```java
    
    private static void copyMap(FacesContext context, Map<Serializable, Object> sourceMap,
            Map<Serializable, Object> targetMap)
    {
        Map serializableMap = sourceMap;
```

### BoundedWildcard
Can generalize to `? super Serializable`
in `api/src/main/java/jakarta/faces/component/_DeltaStateHelper.java`
#### Snippet
```java

    private static Object _removeValueOrKeyFromMap(
            Map<Serializable, Object> stateMap, Serializable key, Object valueOrKey, boolean delta)
    {
        if (valueOrKey == null)
```

### BoundedWildcard
Can generalize to `? extends FacesMessage`
in `api/src/main/java/jakarta/faces/validator/ValidatorException.java`
#### Snippet
```java
    }
    
    private static String facesMessagesToString(Collection<FacesMessage> messages)
    {
        if (messages == null || messages.isEmpty())
```

### BoundedWildcard
Can generalize to `? super Serializable`
in `api/src/main/java/jakarta/faces/component/behavior/_DeltaStateHelper.java`
#### Snippet
```java

    private static Object _removeValueOrKeyFromMap(
            Map<Serializable, Object> stateMap, Serializable key, Object valueOrKey, boolean delta)
    {
        if (valueOrKey == null)
```

### BoundedWildcard
Can generalize to `? super Serializable`
in `api/src/main/java/jakarta/faces/component/behavior/_AjaxBehaviorDeltaStateHelper.java`
#### Snippet
```java

    private static Object _removeValueOrKeyFromMap(
            Map<Serializable, Object> stateMap, Serializable key, Object valueOrKey, boolean delta)
    {
        if (valueOrKey == null)
```

### BoundedWildcard
Can generalize to `? super FacesEvent`
in `api/src/main/java/jakarta/faces/component/UIViewRoot.java`
#### Snippet
```java
    private boolean _broadcastAll(FacesContext context,
                               List<? extends FacesEvent> events,
                               Collection<FacesEvent> eventsAborted)
    {
        assert events != null;
```

### BoundedWildcard
Can generalize to `? extends UIComponent`
in `api/src/main/java/jakarta/faces/component/UIData.java`
#### Snippet
```java
    
    private Map<String,Object> saveFullDescendantComponentStates(FacesContext facesContext, Map<String,Object> stateMap,
            Iterator<UIComponent> childIterator, boolean saveChildFacets)
    {
        while (childIterator.hasNext())
```

### BoundedWildcard
Can generalize to `? extends UIComponent`
in `api/src/main/java/jakarta/faces/component/UIData.java`
#### Snippet
```java
    private Map<String, Object> saveTransientDescendantComponentStates(FacesContext facesContext,
                                                                       Map<String, Object> childStates,
                                                                       Iterator<UIComponent> childIterator,
                                                                       boolean saveChildFacets)
    {
```

### BoundedWildcard
Can generalize to `? extends UIComponent`
in `api/src/main/java/jakarta/faces/component/UIData.java`
#### Snippet
```java
    
    private void restoreFullDescendantComponentDeltaStates(FacesContext facesContext,
            Iterator<UIComponent> childIterator, Map<String, Object> state, Object initialState,
            boolean restoreChildFacets)
    {
```

### BoundedWildcard
Can generalize to `? extends UIComponent`
in `api/src/main/java/jakarta/faces/component/UIData.java`
#### Snippet
```java

    private Collection<Object[]> saveDescendantInitialComponentStates(FacesContext facesContext,
            Iterator<UIComponent> childIterator, boolean saveChildFacets)
    {
        Collection<Object[]> childStates = null;
```

### BoundedWildcard
Can generalize to `? extends UIComponent`
in `api/src/main/java/jakarta/faces/component/UIData.java`
#### Snippet
```java

    private void restoreFullDescendantComponentStates(FacesContext facesContext,
            Iterator<UIComponent> childIterator, Object initialState,
            boolean restoreChildFacets)
    {
```

### BoundedWildcard
Can generalize to `? extends UIComponent`
in `api/src/main/java/jakarta/faces/component/UIData.java`
#### Snippet
```java
    
    private void restoreTransientDescendantComponentStates(FacesContext facesContext,
                                                           Iterator<UIComponent> childIterator,
                                                           Map<String, Object> state,
                                                           boolean restoreChildFacets)
```

### BoundedWildcard
Can generalize to `? extends ELResolver`
in `impl/src/main/java/org/apache/myfaces/el/resolver/CompositeELResolver.java`
#### Snippet
```java
        private Iterator<FeatureDescriptor> _currentFDIter;

        public CompositeIterator(final ELContext context, final Object base, final Iterator<ELResolver> elResolvers)
        {
            _context = context;
```

### BoundedWildcard
Can generalize to `? super ELResolver`
in `impl/src/main/java/org/apache/myfaces/el/ELResolverBuilder.java`
#### Snippet
```java
     * @param resolvers
     */
    protected void addFromRuntimeConfig(List<ELResolver> resolvers)
    {
        if (runtimeConfig.getFacesConfigElResolvers() != null)
```

### BoundedWildcard
Can generalize to `? extends ELResolver`
in `impl/src/main/java/org/apache/myfaces/el/ELResolverBuilder.java`
#### Snippet
```java
     * @since 1.2.10, 2.0.2
     */
    protected void sortELResolvers(List<ELResolver> resolvers)
    {
        if (runtimeConfig.getELResolverComparator() != null)
```

### BoundedWildcard
Can generalize to `? super FeatureDescriptor`
in `impl/src/main/java/org/apache/myfaces/el/resolver/ScopedAttributeResolver.java`
#### Snippet
```java

    // side effect: modifies the list
    private static void addDescriptorsToList(final List<FeatureDescriptor> descriptorList,
                                             final Map<String, Object> scopeMap)
    {
```

### BoundedWildcard
Can generalize to `? extends ImplicitObject`
in `impl/src/main/java/org/apache/myfaces/el/resolver/implicitobject/ImplicitObjectResolver.java`
#### Snippet
```java
    }

    private ImplicitObjectResolver(Map<String, ImplicitObject> implicitObjects)
    {
        this();
```

### BoundedWildcard
Can generalize to `? super CreationalContext`
in `impl/src/main/java/org/apache/myfaces/cdi/util/AbstractDynamicProducer.java`
#### Snippet
```java
    }

    public AbstractDynamicProducer<T> create(Function<CreationalContext<T>, T> create)
    {
        this.create = create;
```

### BoundedWildcard
Can generalize to `? extends T`
in `impl/src/main/java/org/apache/myfaces/cdi/util/AbstractDynamicProducer.java`
#### Snippet
```java
    }

    public AbstractDynamicProducer<T> create(Function<CreationalContext<T>, T> create)
    {
        this.create = create;
```

### BoundedWildcard
Can generalize to `? extends List`
in `impl/src/main/java/org/apache/myfaces/spi/impl/DefaultServiceProviderFinder.java`
#### Snippet
```java

    @Override
    public void initKnownServiceProviderMapInfo(ExternalContext ectx, Map<String, List<String>> map)
    {
        knownServicesMap = map;
```

### BoundedWildcard
Can generalize to `? extends NavigationCase`
in `impl/src/main/java/org/apache/myfaces/flow/FlowImpl.java`
#### Snippet
```java
    }
    
    public void addNavigationCases(String fromViewId, Set<NavigationCase> navigationCases)
    {
        checkInitialized();
```

### BoundedWildcard
Can generalize to `? extends Parameter`
in `impl/src/main/java/org/apache/myfaces/flow/builder/MethodCallBuilderImpl.java`
#### Snippet
```java

    @Override
    public MethodCallBuilder parameters(List<Parameter> parameters)
    {
        for (Parameter p : parameters)
```

### BoundedWildcard
Can generalize to `? extends FlowReference`
in `impl/src/main/java/org/apache/myfaces/flow/cdi/FlowScopeContext.java`
#### Snippet
```java

    public FlowScopeContext(BeanManager beanManager, 
        Map<Class, FlowReference> flowBeanReferences)
    {
        this.beanManager = beanManager;
```

### BoundedWildcard
Can generalize to `? extends S`
in `impl/src/main/java/org/apache/myfaces/push/cdi/PushContextImpl.java`
#### Snippet
```java

    @Override
    public <S extends Serializable> Map<S, Set<Future<Void>>> send(Object message, Collection<S> users)
    {

```

### BoundedWildcard
Can generalize to `? super Future`
in `impl/src/main/java/org/apache/myfaces/push/cdi/WebsocketSessionManager.java`
#### Snippet
```java
            + " Consider rate limiting sending push messages. For example, once every 500ms.";    
    
    private void send(Session session, String text, Set<Future<Void>> results, int retries)
    {
        try
```

### BoundedWildcard
Can generalize to `? extends T`
in `impl/src/main/java/org/apache/myfaces/util/lang/SkipMatchIterator.java`
#### Snippet
```java


    public SkipMatchIterator(Iterator<T> delegate)
    {
        this.delegate = delegate;
```

### BoundedWildcard
Can generalize to `? extends _FlowContextualInfo`
in `impl/src/main/java/org/apache/myfaces/flow/FlowHandlerImpl.java`
#### Snippet
```java
    
    private void traverseDependantFlows(FlowReference sourceFlowReference, 
        int index, List<_FlowContextualInfo> currentFlowStack, List<_FlowContextualInfo> flowsToRemove)
    {
        if (index < currentFlowStack.size())
```

### BoundedWildcard
Can generalize to `? super _FlowContextualInfo`
in `impl/src/main/java/org/apache/myfaces/flow/FlowHandlerImpl.java`
#### Snippet
```java
    
    private void traverseDependantFlows(FlowReference sourceFlowReference, 
        int index, List<_FlowContextualInfo> currentFlowStack, List<_FlowContextualInfo> flowsToRemove)
    {
        if (index < currentFlowStack.size())
```

### BoundedWildcard
Can generalize to `? super Map.Entry`
in `impl/src/main/java/org/apache/myfaces/util/lang/LRULinkedHashMap.java`
#### Snippet
```java
    }

    public LRULinkedHashMap(int capacity, SerializableConsumer<Map.Entry<K, V>> expiredCallback)
    {
        this(capacity);
```

### BoundedWildcard
Can generalize to `? extends T`
in `impl/src/main/java/org/apache/myfaces/util/lang/Lazy.java`
#### Snippet
```java
    private volatile Supplier<T> init;

    public Lazy(Supplier<T> init)
    {
        this.init = init;
```

### BoundedWildcard
Can generalize to `? extends T`
in `impl/src/main/java/org/apache/myfaces/util/lang/Lazy.java`
#### Snippet
```java
    }
    
    public synchronized void reset(Supplier<T> init)
    {
        this.init = init;
```

### BoundedWildcard
Can generalize to `? super String`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FullStateManagementStrategy.java`
#### Snippet
```java
    private static void checkForDuplicateIds(FacesContext context,
                                             UIComponent component,
                                             Set<String> ids)
    {
        String id = component.getId();
```

### BoundedWildcard
Can generalize to `? super K`
in `impl/src/main/java/org/apache/myfaces/util/lang/ConcurrentLRUCache.java`
#### Snippet
```java
            int acceptableWatermark, int initialSize, boolean runCleanupThread,
            boolean runNewThreadForCleanup,
            EvictionListener<K, V> evictionListener)
    {
        if (upperWaterMark < 1)
```

### BoundedWildcard
Can generalize to `? super V`
in `impl/src/main/java/org/apache/myfaces/util/lang/ConcurrentLRUCache.java`
#### Snippet
```java
            int acceptableWatermark, int initialSize, boolean runCleanupThread,
            boolean runNewThreadForCleanup,
            EvictionListener<K, V> evictionListener)
    {
        if (upperWaterMark < 1)
```

### BoundedWildcard
Can generalize to `? extends ResourceDependency`
in `impl/src/main/java/org/apache/myfaces/view/facelets/ViewPoolProcessor.java`
#### Snippet
```java
    
    private void clearComponentResourceContainer(final FacesContext context, UIComponent component,
        List<ResourceDependency> resourceDependencyList)
    {
        //Scan children
```

### BoundedWildcard
Can generalize to `? super String`
in `impl/src/main/java/org/apache/myfaces/view/facelets/PartialStateManagementStrategy.java`
#### Snippet
```java
    }

    private void saveStateOnMapVisitTree(final FacesContext facesContext, final Map<String,Object> states,
            final UIViewRoot uiViewRoot)
    {
```

### BoundedWildcard
Can generalize to `? super String`
in `impl/src/main/java/org/apache/myfaces/view/facelets/PartialStateManagementStrategy.java`
#### Snippet
```java
        private boolean removeAddedComponents;
        
        public SaveStateAndResetViewCallback(UIViewRoot view, Map<String, Object> states, boolean forceHardReset)
        {
            this.states = states;
```

### BoundedWildcard
Can generalize to `? extends AttachedObjectHandler`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguage.java`
#### Snippet
```java
    @SuppressWarnings("unchecked")
    public void retargetAttachedObjects(FacesContext context,
                                        UIComponent topLevelComponent, List<AttachedObjectHandler> handlerList)
    {
        Assert.notNull(context, "context");
```

### BoundedWildcard
Can generalize to `? extends UIComponent`
in `impl/src/main/java/org/apache/myfaces/view/facelets/util/FaceletsViewDeclarationLanguageUtils.java`
#### Snippet
```java

    private static void markRenderedResources(FacesContext facesContext, UIViewRoot view, 
            List<UIComponent> componentResources)
    {
        if (componentResources != null)
```

### BoundedWildcard
Can generalize to `? super URL`
in `impl/src/main/java/org/apache/myfaces/view/facelets/util/Classpath.java`
#### Snippet
```java
    }

    private static void _searchJar(ClassLoader loader, Set<URL> result, JarFile file, String prefix, String suffix)
            throws IOException
    {
```

### BoundedWildcard
Can generalize to `? super URL`
in `impl/src/main/java/org/apache/myfaces/view/facelets/util/Classpath.java`
#### Snippet
```java
     *             for any error
     */
    private static void _searchFromURL(Set<URL> result, String prefix, String suffix, URL url) throws IOException
    {
        boolean done = false;
```

### BoundedWildcard
Can generalize to `? super URL`
in `impl/src/main/java/org/apache/myfaces/view/facelets/util/Classpath.java`
#### Snippet
```java
    }

    private static boolean _searchDir(Set<URL> result, File dir, String suffix) throws IOException
    {
        boolean dirExists = false;
```

### BoundedWildcard
Can generalize to `? super String`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/CheckDuplicateIdFaceletUtils.java`
#### Snippet
```java
    }
    
    private static void checkIds(FacesContext context, UIComponent component, Set<String> existingIds)
    {
        if (component == null)
```

### BoundedWildcard
Can generalize to `? super String`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/CheckDuplicateIdFaceletUtils.java`
#### Snippet
```java

    private static void checkIdsStatefulComponents(FacesContext context, UIComponent component,
            Set<String> existingIds)
    {
        if (component == null)
```

### BoundedWildcard
Can generalize to `? extends List`
in `impl/src/main/java/org/apache/myfaces/config/LogMetaInfUtils.java`
#### Snippet
```java
    }

    private static void logArtifact(String artifactId, Map<String, List<JarInfo>> libs)
    {
        List<JarInfo> versions = libs.get(artifactId);
```

### BoundedWildcard
Can generalize to `? super String`
in `impl/src/main/java/org/apache/myfaces/config/LogMetaInfUtils.java`
#### Snippet
```java
    }

    protected static void addJarInfo(Map<String, List<JarInfo>> libs, URL url)
    {
        Matcher matcher = REGEX_LIBRARY_PATTERN.matcher(url.toString());
```

### BoundedWildcard
Can generalize to `? super String`
in `impl/src/main/java/org/apache/myfaces/config/FacesConfigValidator.java`
#### Snippet
```java
    }
    
    private static void validateNavRule(NavigationRule navRule, List<String> list, ExternalContext ctx)
    {
        String fromId = navRule.getFromViewId();
```

### BoundedWildcard
Can generalize to `? extends FacesConfig`
in `impl/src/main/java/org/apache/myfaces/config/DefaultFacesConfigurationMerger.java`
#### Snippet
```java
    }

    private FacesConfig getFacesConfig(List<FacesConfig> appConfigResources, String name)
    {
        for (FacesConfig cfg: appConfigResources)
```

### BoundedWildcard
Can generalize to `? extends FacesConfig`
in `impl/src/main/java/org/apache/myfaces/config/DefaultFacesConfigurationMerger.java`
#### Snippet
```java
     * @return
     */
    protected List<FacesConfig> sortRelativeOrderingList(List<FacesConfig> preOrderedList)
    {
        List<FacesConfig> sortedList = new ArrayList<>();
```

### BoundedWildcard
Can generalize to `? extends FacesConfig`
in `impl/src/main/java/org/apache/myfaces/config/DefaultFacesConfigurationMerger.java`
#### Snippet
```java
    }

    private void resolveConflicts(final List<FacesConfig> appConfigResources, int index, boolean[] visitedSlots,
            List<String> nameBeforeStack, List<String> nameAfterStack, List<FacesConfig> postOrderedList,
            List<FacesConfig> othersList, boolean indexReferenced) throws FacesException
```

### BoundedWildcard
Can generalize to `? super String`
in `impl/src/main/java/org/apache/myfaces/config/DefaultFacesConfigurationMerger.java`
#### Snippet
```java

    private void resolveConflicts(final List<FacesConfig> appConfigResources, int index, boolean[] visitedSlots,
            List<String> nameBeforeStack, List<String> nameAfterStack, List<FacesConfig> postOrderedList,
            List<FacesConfig> othersList, boolean indexReferenced) throws FacesException
    {
```

### BoundedWildcard
Can generalize to `? super String`
in `impl/src/main/java/org/apache/myfaces/config/DefaultFacesConfigurationMerger.java`
#### Snippet
```java

    private void resolveConflicts(final List<FacesConfig> appConfigResources, int index, boolean[] visitedSlots,
            List<String> nameBeforeStack, List<String> nameAfterStack, List<FacesConfig> postOrderedList,
            List<FacesConfig> othersList, boolean indexReferenced) throws FacesException
    {
```

### BoundedWildcard
Can generalize to `? super FacesConfig`
in `impl/src/main/java/org/apache/myfaces/config/DefaultFacesConfigurationMerger.java`
#### Snippet
```java
    private void resolveConflicts(final List<FacesConfig> appConfigResources, int index, boolean[] visitedSlots,
            List<String> nameBeforeStack, List<String> nameAfterStack, List<FacesConfig> postOrderedList,
            List<FacesConfig> othersList, boolean indexReferenced) throws FacesException
    {
        FacesConfig facesConfig = appConfigResources.get(index);
```

### BoundedWildcard
Can generalize to `? extends FacesConfig`
in `impl/src/main/java/org/apache/myfaces/config/DefaultFacesConfigurationMerger.java`
#### Snippet
```java
     * @throws FacesException
     */
    protected List<FacesConfig> applySortingAlgorithm(List<FacesConfig> appConfigResources) throws FacesException
    {
        //0. Convert the references into a graph
```

### BoundedWildcard
Can generalize to `? extends FacesConfig`
in `impl/src/main/java/org/apache/myfaces/config/DefaultFacesConfigurationMerger.java`
#### Snippet
```java
     * @return
     */
    protected List<FacesConfig> getPostOrderedList(final List<FacesConfig> appConfigResources) throws FacesException
    {

```

### BoundedWildcard
Can generalize to `? extends OrderSlot`
in `impl/src/main/java/org/apache/myfaces/config/DefaultFacesConfigurationMerger.java`
#### Snippet
```java
    }

    private boolean containsResourceInSlot(List<OrderSlot> slots, String name)
    {
        for (OrderSlot slot: slots)
```

### BoundedWildcard
Can generalize to `? super Vertex`
in `impl/src/main/java/org/apache/myfaces/config/util/DirectedAcyclicGraphVerifier.java`
#### Snippet
```java
     * @param vertices Existing list of vertices.
     */
    private static <T> void addDependencies(final Vertex<T> vertex, final List<Vertex<T>> vertices)
    {
        if (!vertices.contains(vertex))
```

### BoundedWildcard
Can generalize to `? super Class`
in `impl/src/main/java/org/apache/myfaces/config/annotation/PackageInfo.java`
#### Snippet
```java
     * @param className Name of the class to be loaded
     */
    protected static void loadClass(List<Class> classes, ClassLoader cld, String className)
    {
        try
```

### BoundedWildcard
Can generalize to `? super Node`
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
    }
    
    protected void onChild(String name, Node node, Consumer<Node> val)
    {
        if (node.getChildNodes() != null)
```

### BoundedWildcard
Can generalize to `? super String`
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
    
    
    protected void onAttribute(String name, Node node, Consumer<String> val)
    {
        if (node instanceof Element)
```

### BoundedWildcard
Can generalize to `? super Node`
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
    }
    
    protected void forEachChild(Node node, Consumer<Node> val)
    {
        if (node.getChildNodes() != null)
```

### BoundedWildcard
Can generalize to `? super Class`
in `impl/src/main/java/org/apache/myfaces/config/annotation/DefaultAnnotationProvider.java`
#### Snippet
```java

    
    private void processClass(Map<Class<? extends Annotation>,Set<Class<?>>> map, Class<?> clazz)
    {
        Annotation[] annotations = clazz.getAnnotations();
```

### BoundedWildcard
Can generalize to `? extends Set`>
in `impl/src/main/java/org/apache/myfaces/config/annotation/AnnotationConfigurator.java`
#### Snippet
```java
     * @return
     */
    protected FacesConfigImpl createFacesConfig(Map<Class<? extends Annotation>, Set<Class<?>>> map)
    {
        FacesConfigImpl facesConfig = new FacesConfigImpl();
```

### BoundedWildcard
Can generalize to `? super UIComponent`
in `impl/src/main/java/org/apache/myfaces/context/servlet/PartialViewContextImpl.java`
#### Snippet
```java
    
    private void processRenderResource(FacesContext facesContext, PartialResponseWriter writer, RequestViewContext rvc, 
            List<UIComponent> updatedComponents, String target) throws IOException
    {
        if (rvc.isRenderTarget(target))
```

### BoundedWildcard
Can generalize to `? extends E`
in `impl/src/main/java/org/apache/myfaces/component/visit/PartialVisitContext.java`
#### Snippet
```java
        private E _current = null;
        
        private IteratorProxy(Iterator<E> wrapped)
        {
            _wrapped = wrapped;
```

### BoundedWildcard
Can generalize to `? extends List`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlLinkRendererBase.java`
#### Snippet
```java
    }

    private boolean hasSubmittingBehavior(Map<String, List<ClientBehavior>> clientBehaviors, String eventName)
    {
        List<ClientBehavior> eventBehaviors = clientBehaviors.get(eventName);
```

### BoundedWildcard
Can generalize to `? extends EventInfo`
in `impl/src/main/java/org/apache/myfaces/application/ApplicationImplEventManager.java`
#### Snippet
```java
    }
    
    protected SystemEvent processGlobalListeners(FacesContext facesContext, List<EventInfo> eventInfos,
            Class<? extends SystemEvent> systemEventClass, Object source, SystemEvent event, Class<?> sourceBaseType)
    {
```

### BoundedWildcard
Can generalize to `? extends UIComponent`
in `impl/src/main/java/org/apache/myfaces/renderkit/ErrorPageWriter.java`
#### Snippet
```java
    }

    private static List<String> getErrorId(Collection<UIComponent> components, Throwable... exs)
    {
        List<String> list = null;
```

### BoundedWildcard
Can generalize to `? super String`
in `impl/src/main/java/org/apache/myfaces/application/viewstate/SerializedViewCollection.java`
#### Snippet
```java
    public synchronized void put(FacesContext context, Object state, 
        SerializedViewKey key, SerializedViewKey previousRestoredKey, String viewScopeId,
        Consumer<String> destroyCallback)
    {
        if (state == null)
```

### BoundedWildcard
Can generalize to `? extends Flow`
in `impl/src/main/java/org/apache/myfaces/application/NavigationHandlerImpl.java`
#### Snippet
```java
    
    private Flow calculateTargetFlow(FacesContext facesContext, String outcome, 
        FlowHandler flowHandler, List<Flow> activeFlows, String toFlowDocumentId)
    {
        Flow targetFlow = null;
```

### BoundedWildcard
Can generalize to `? super String`
in `impl/src/main/java/org/apache/myfaces/application/ApplicationImpl.java`
#### Snippet
```java
     * @return
     */
    private <T> Class<? extends T> getObjectFromClassMap(String id, Map<String, Object> classMap)
    {
        Object obj = classMap.get(id);
```

### BoundedWildcard
Can generalize to `? extends List`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/ClientBehaviorRendererUtils.java`
#### Snippet
```java
            UIComponent uiComponent,
            String sourceId, String eventName,
            Map<String, List<ClientBehavior>> clientBehaviors,
            JavascriptContext target,
            Collection<ClientBehaviorContext.Parameter> params)
```

### BoundedWildcard
Can generalize to `? super String`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/ClientBehaviorRendererUtils.java`
#### Snippet
```java
    }
    
    private static void addFunction(FacesContext facesContext, String function, List<String> functions,
            MyfacesConfig config)
    {
```

### BoundedWildcard
Can generalize to `? super SystemEventListener`
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication20.java`
#### Snippet
```java

        private void addListenerNoDuplicate(
                List<SystemEventListener> listeners,
                SystemEventListener listener)
        {
```

### BoundedWildcard
Can generalize to `? extends E`
in `test/src/main/java/org/apache/myfaces/test/mock/visit/PartialVisitContext.java`
#### Snippet
```java
    private class IteratorProxy<E extends String> implements Iterator<E>
    {
        private IteratorProxy(Iterator<E> wrapped)
        {
            _wrapped = wrapped;
```

## RuleId[ruleID=MissortedModifiers]
### MissortedModifiers
Missorted modifiers `public @Nonbinding`
in `api/src/main/java/jakarta/faces/push/Push.java`
#### Snippet
```java
public @interface Push
{
    public @Nonbinding String channel() default "";
    
    /*
```

### MissortedModifiers
Missorted modifiers `abstract public`
in `api/src/main/java/jakarta/faces/model/DataModel.java`
#### Snippet
```java
     *                 or may be a value between 0 and getRowCount()-1. 
     */
    abstract public void setRowIndex(int rowIndex);

    /**
```

### MissortedModifiers
Missorted modifiers `abstract public`
in `api/src/main/java/jakarta/faces/model/DataModel.java`
#### Snippet
```java
     * is not within the range of the current wrappedData property.
     */
    abstract public E getRowData();

    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `api/src/main/java/jakarta/faces/model/DataModel.java`
#### Snippet
```java
public abstract class DataModel<E> implements Iterable<E>
{
    private final static DataModelListener[] EMPTY_DATA_MODEL_LISTENER = new DataModelListener[]{};

    private List<DataModelListener> _listeners;
```

### MissortedModifiers
Missorted modifiers `abstract public`
in `api/src/main/java/jakarta/faces/model/DataModel.java`
#### Snippet
```java
     * @return true if a call to getRowData will return a valid object. false otherwise.
     */
    abstract public boolean isRowAvailable();
    
    /**
```

### MissortedModifiers
Missorted modifiers `abstract public`
in `api/src/main/java/jakarta/faces/model/DataModel.java`
#### Snippet
```java
     * @param data The object to be wrapped.
     */
    abstract public void setWrappedData(Object data);
    
    private class DataModelIterator implements Iterator<E>
```

### MissortedModifiers
Missorted modifiers `abstract public`
in `api/src/main/java/jakarta/faces/model/DataModel.java`
#### Snippet
```java
     * @return The current row index.
     */
    abstract public int getRowIndex();

    /**
```

### MissortedModifiers
Missorted modifiers `abstract public`
in `api/src/main/java/jakarta/faces/model/DataModel.java`
#### Snippet
```java
     * @return the wrapped object.
     */
    abstract public Object getWrappedData();

    /**
```

### MissortedModifiers
Missorted modifiers `abstract public`
in `api/src/main/java/jakarta/faces/model/DataModel.java`
#### Snippet
```java
     * @return the number of rows available.
     */
    abstract public int getRowCount();

    /**
```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/_UIMessage.java`
#### Snippet
```java
{

    static public final String COMPONENT_FAMILY =
      "jakarta.faces.Message";
    static public final String COMPONENT_TYPE =
```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/_UIMessage.java`
#### Snippet
```java
    static public final String COMPONENT_FAMILY =
      "jakarta.faces.Message";
    static public final String COMPONENT_TYPE =
      "jakarta.faces.Message";

```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/UIImportConstants.java`
#### Snippet
```java
{
    
    static public final String COMPONENT_FAMILY = "jakarta.faces.ImportConstants";
    static public final String COMPONENT_TYPE = "jakarta.faces.ImportConstants";

```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/UIImportConstants.java`
#### Snippet
```java
    
    static public final String COMPONENT_FAMILY = "jakarta.faces.ImportConstants";
    static public final String COMPONENT_TYPE = "jakarta.faces.ImportConstants";


```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/_UISelectItems.java`
#### Snippet
```java
{

    static public final String COMPONENT_FAMILY =
        "jakarta.faces.SelectItems";
    static public final String COMPONENT_TYPE =
```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/_UISelectItems.java`
#### Snippet
```java
    static public final String COMPONENT_FAMILY =
        "jakarta.faces.SelectItems";
    static public final String COMPONENT_TYPE =
        "jakarta.faces.SelectItems";

```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/UIPanel.java`
#### Snippet
```java
{
    static public final String COMPONENT_FAMILY = "jakarta.faces.Panel";
    static public final String COMPONENT_TYPE = "jakarta.faces.Panel";

    /**
```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/UIPanel.java`
#### Snippet
```java
public class UIPanel extends UIComponentBase
{
    static public final String COMPONENT_FAMILY = "jakarta.faces.Panel";
    static public final String COMPONENT_TYPE = "jakarta.faces.Panel";

```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/_UISelectItemGroup.java`
#### Snippet
```java
abstract class _UISelectItemGroup extends UISelectItem
{
    static public final String COMPONENT_FAMILY = "jakarta.faces.SelectItem";
    static public final String COMPONENT_TYPE = "jakarta.faces.SelectItemGroup";

```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/_UISelectItemGroup.java`
#### Snippet
```java
{
    static public final String COMPONENT_FAMILY = "jakarta.faces.SelectItem";
    static public final String COMPONENT_TYPE = "jakarta.faces.SelectItemGroup";

    @Override
```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/_UISelectItem.java`
#### Snippet
```java
{

    static public final String COMPONENT_FAMILY = "jakarta.faces.SelectItem";
    static public final String COMPONENT_TYPE = "jakarta.faces.SelectItem";

```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/_UISelectItem.java`
#### Snippet
```java

    static public final String COMPONENT_FAMILY = "jakarta.faces.SelectItem";
    static public final String COMPONENT_TYPE = "jakarta.faces.SelectItem";

    /**
```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/_UIWebsocket.java`
#### Snippet
```java
{
    
    static public final String COMPONENT_FAMILY = "jakarta.faces.Script";
    static public final String COMPONENT_TYPE = "jakarta.faces.Websocket";

```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/_UIWebsocket.java`
#### Snippet
```java
    
    static public final String COMPONENT_FAMILY = "jakarta.faces.Script";
    static public final String COMPONENT_TYPE = "jakarta.faces.Websocket";

    private static final Collection<String> EVERY_EVENT = Collections.unmodifiableList(new java.util.ArrayList<String>()
```

### MissortedModifiers
Missorted modifiers `final static`
in `api/src/main/java/jakarta/faces/component/UIInput.java`
#### Snippet
```java
    private static final String DEBUG_INFO_KEY = "org.apache.myfaces.debug.DEBUG_INFO";
    
    private final static String BEAN_BEFORE_JSF_PROPERTY = "oam.beanBeforeJsf";

    private static final Validator[] EMPTY_VALIDATOR_ARRAY = new Validator[0];
```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/_UIMessages.java`
#### Snippet
```java
{

    static public final String COMPONENT_FAMILY =
      "jakarta.faces.Messages";
    static public final String COMPONENT_TYPE =
```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/_UIMessages.java`
#### Snippet
```java
    static public final String COMPONENT_FAMILY =
      "jakarta.faces.Messages";
    static public final String COMPONENT_TYPE =
      "jakarta.faces.Messages";

```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/_ArrayMap.java`
#### Snippet
```java
     * new array if necessary.
     */
    static public Object[] remove(Object[] array, Object key)
    {
        return remove(array, key, true);
```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/_ArrayMap.java`
#### Snippet
```java
     * by object identity, then by object equality.
     */
    static public Object get(Object[] array, Object key)
    {
        Object o = getByIdentity(array, key);
```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/_ArrayMap.java`
#### Snippet
```java
     * new array if necessary.
     */
    static public Object[] remove(Object[] array, Object key, boolean reallocate)
    {
        if (array != null)
```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/_ArrayMap.java`
#### Snippet
```java
     * new array if necessary.
     */
    static public Object[] put(Object[] array, Object key, Object value)
    {
        if (array != null)
```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/_ArrayMap.java`
#### Snippet
```java
     * only object equality.
     */
    static public Object getByEquality(Object[] array, Object key)
    {
        if (array != null)
```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/_ArrayMap.java`
#### Snippet
```java
     * only object identity.
     */
    static public Object getByIdentity(Object[] array, Object key)
    {
        if (array != null)
```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/_UISelectItemGroups.java`
#### Snippet
```java

    static public final String COMPONENT_FAMILY = "jakarta.faces.SelectItems";
    static public final String COMPONENT_TYPE = "jakarta.faces.SelectItemGroups";

    @Override
```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/_UISelectItemGroups.java`
#### Snippet
```java
{

    static public final String COMPONENT_FAMILY = "jakarta.faces.SelectItems";
    static public final String COMPONENT_TYPE = "jakarta.faces.SelectItemGroups";

```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlPanelGrid.java`
#### Snippet
```java
{

  static public final String COMPONENT_FAMILY =
    "jakarta.faces.Panel";
  static public final String COMPONENT_TYPE =
```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlPanelGrid.java`
#### Snippet
```java
  static public final String COMPONENT_FAMILY =
    "jakarta.faces.Panel";
  static public final String COMPONENT_TYPE =
    "jakarta.faces.HtmlPanelGrid";

```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlSelectOneMenu.java`
#### Snippet
```java
  static public final String COMPONENT_FAMILY =
    "jakarta.faces.SelectOne";
  static public final String COMPONENT_TYPE =
    "jakarta.faces.HtmlSelectOneMenu";

```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlSelectOneMenu.java`
#### Snippet
```java
{

  static public final String COMPONENT_FAMILY =
    "jakarta.faces.SelectOne";
  static public final String COMPONENT_TYPE =
```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/_UIParameter.java`
#### Snippet
```java

    static public final String COMPONENT_FAMILY = "jakarta.faces.Parameter";
    static public final String COMPONENT_TYPE = "jakarta.faces.Parameter";

    /**
```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/_UIParameter.java`
#### Snippet
```java
{

    static public final String COMPONENT_FAMILY = "jakarta.faces.Parameter";
    static public final String COMPONENT_TYPE = "jakarta.faces.Parameter";

```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlBody.java`
#### Snippet
```java
{

  static public final String COMPONENT_FAMILY = "jakarta.faces.Output";
  static public final String COMPONENT_TYPE = "jakarta.faces.OutputBody";

```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlBody.java`
#### Snippet
```java

  static public final String COMPONENT_FAMILY = "jakarta.faces.Output";
  static public final String COMPONENT_TYPE = "jakarta.faces.OutputBody";

  /**
```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/HtmlInputHidden.java`
#### Snippet
```java
  static public final String COMPONENT_FAMILY =
    "jakarta.faces.Input";
  static public final String COMPONENT_TYPE =
    "jakarta.faces.HtmlInputHidden";

```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/HtmlInputHidden.java`
#### Snippet
```java
{

  static public final String COMPONENT_FAMILY =
    "jakarta.faces.Input";
  static public final String COMPONENT_TYPE =
```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlSelectOneListbox.java`
#### Snippet
```java
{

  static public final String COMPONENT_FAMILY =
    "jakarta.faces.SelectOne";
  static public final String COMPONENT_TYPE =
```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlSelectOneListbox.java`
#### Snippet
```java
  static public final String COMPONENT_FAMILY =
    "jakarta.faces.SelectOne";
  static public final String COMPONENT_TYPE =
    "jakarta.faces.HtmlSelectOneListbox";

```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlCommandScript.java`
#### Snippet
```java
    static public final String COMPONENT_FAMILY =
        "jakarta.faces.Command";
    static public final String COMPONENT_TYPE =
        "jakarta.faces.HtmlCommandScript";

```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlOutcomeTargetButton.java`
#### Snippet
```java
  static public final String COMPONENT_FAMILY =
    "jakarta.faces.OutcomeTarget";
  static public final String COMPONENT_TYPE =
    "jakarta.faces.HtmlOutcomeTargetButton";

```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlOutcomeTargetButton.java`
#### Snippet
```java
{

  static public final String COMPONENT_FAMILY =
    "jakarta.faces.OutcomeTarget";
  static public final String COMPONENT_TYPE =
```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlCommandScript.java`
#### Snippet
```java
abstract class _HtmlCommandScript extends UICommand
{
    static public final String COMPONENT_FAMILY =
        "jakarta.faces.Command";
    static public final String COMPONENT_TYPE =
```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlForm.java`
#### Snippet
```java
{

  static public final String COMPONENT_FAMILY =
    "jakarta.faces.Form";
  static public final String COMPONENT_TYPE =
```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlForm.java`
#### Snippet
```java
  static public final String COMPONENT_FAMILY =
    "jakarta.faces.Form";
  static public final String COMPONENT_TYPE =
    "jakarta.faces.HtmlForm";

```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlGraphicImage.java`
#### Snippet
```java
{

  static public final String COMPONENT_FAMILY =
    "jakarta.faces.Graphic";
  static public final String COMPONENT_TYPE =
```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlGraphicImage.java`
#### Snippet
```java
  static public final String COMPONENT_FAMILY =
    "jakarta.faces.Graphic";
  static public final String COMPONENT_TYPE =
    "jakarta.faces.HtmlGraphicImage";

```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlInputSecret.java`
#### Snippet
```java
{

  static public final String COMPONENT_FAMILY =
    "jakarta.faces.Input";
  static public final String COMPONENT_TYPE =
```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlInputSecret.java`
#### Snippet
```java
  static public final String COMPONENT_FAMILY =
    "jakarta.faces.Input";
  static public final String COMPONENT_TYPE =
    "jakarta.faces.HtmlInputSecret";

```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlCommandLink.java`
#### Snippet
```java
{

  static public final String COMPONENT_FAMILY =
    "jakarta.faces.Command";
  static public final String COMPONENT_TYPE =
```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlCommandLink.java`
#### Snippet
```java
  static public final String COMPONENT_FAMILY =
    "jakarta.faces.Command";
  static public final String COMPONENT_TYPE =
    "jakarta.faces.HtmlCommandLink";

```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlInputText.java`
#### Snippet
```java
{

    static public final String COMPONENT_FAMILY = "jakarta.faces.Input";
    static public final String COMPONENT_TYPE = "jakarta.faces.HtmlInputText";

```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlInputText.java`
#### Snippet
```java

    static public final String COMPONENT_FAMILY = "jakarta.faces.Input";
    static public final String COMPONENT_TYPE = "jakarta.faces.HtmlInputText";

    /**
```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlColumn.java`
#### Snippet
```java
{

  static public final String COMPONENT_FAMILY = "jakarta.faces.Column";
  static public final String COMPONENT_TYPE = "jakarta.faces.HtmlColumn";

```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlColumn.java`
#### Snippet
```java

  static public final String COMPONENT_FAMILY = "jakarta.faces.Column";
  static public final String COMPONENT_TYPE = "jakarta.faces.HtmlColumn";

  /**
```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlPanelGroup.java`
#### Snippet
```java
  static public final String COMPONENT_FAMILY =
    "jakarta.faces.Panel";
  static public final String COMPONENT_TYPE =
    "jakarta.faces.HtmlPanelGroup";

```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlPanelGroup.java`
#### Snippet
```java
{

  static public final String COMPONENT_FAMILY =
    "jakarta.faces.Panel";
  static public final String COMPONENT_TYPE =
```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlDoctype.java`
#### Snippet
```java

    static public final String COMPONENT_FAMILY = "jakarta.faces.Output";
    static public final String COMPONENT_TYPE = "jakarta.faces.OutputDoctype";

    @JSFProperty
```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlDoctype.java`
#### Snippet
```java
{

    static public final String COMPONENT_FAMILY = "jakarta.faces.Output";
    static public final String COMPONENT_TYPE = "jakarta.faces.OutputDoctype";

```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlSelectOneRadio.java`
#### Snippet
```java
  static public final String COMPONENT_FAMILY =
    "jakarta.faces.SelectOne";
  static public final String COMPONENT_TYPE =
    "jakarta.faces.HtmlSelectOneRadio";

```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlSelectOneRadio.java`
#### Snippet
```java
{

  static public final String COMPONENT_FAMILY =
    "jakarta.faces.SelectOne";
  static public final String COMPONENT_TYPE =
```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlInputTextarea.java`
#### Snippet
```java
{

    static public final String COMPONENT_FAMILY = "jakarta.faces.Input";
    static public final String COMPONENT_TYPE = "jakarta.faces.HtmlInputTextarea";

```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlInputTextarea.java`
#### Snippet
```java

    static public final String COMPONENT_FAMILY = "jakarta.faces.Input";
    static public final String COMPONENT_TYPE = "jakarta.faces.HtmlInputTextarea";

    /**
```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlCommandButton.java`
#### Snippet
```java

    static public final String COMPONENT_FAMILY = "jakarta.faces.Command";
    static public final String COMPONENT_TYPE = "jakarta.faces.HtmlCommandButton";

    /**
```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlCommandButton.java`
#### Snippet
```java
{

    static public final String COMPONENT_FAMILY = "jakarta.faces.Command";
    static public final String COMPONENT_TYPE = "jakarta.faces.HtmlCommandButton";

```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlSelectManyCheckbox.java`
#### Snippet
```java
  static public final String COMPONENT_FAMILY =
    "jakarta.faces.SelectMany";
  static public final String COMPONENT_TYPE =
    "jakarta.faces.HtmlSelectManyCheckbox";

```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlSelectManyCheckbox.java`
#### Snippet
```java
{

  static public final String COMPONENT_FAMILY =
    "jakarta.faces.SelectMany";
  static public final String COMPONENT_TYPE =
```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlOutputLink.java`
#### Snippet
```java
  static public final String COMPONENT_FAMILY =
    "jakarta.faces.Output";
  static public final String COMPONENT_TYPE =
    "jakarta.faces.HtmlOutputLink";

```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlOutputLink.java`
#### Snippet
```java
{

  static public final String COMPONENT_FAMILY =
    "jakarta.faces.Output";
  static public final String COMPONENT_TYPE =
```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlSelectManyMenu.java`
#### Snippet
```java
  static public final String COMPONENT_FAMILY =
    "jakarta.faces.SelectMany";
  static public final String COMPONENT_TYPE =
    "jakarta.faces.HtmlSelectManyMenu";

```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlSelectManyMenu.java`
#### Snippet
```java
{

  static public final String COMPONENT_FAMILY =
    "jakarta.faces.SelectMany";
  static public final String COMPONENT_TYPE =
```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlHead.java`
#### Snippet
```java
{

  static public final String COMPONENT_FAMILY = "jakarta.faces.Output";
  static public final String COMPONENT_TYPE = "jakarta.faces.OutputHead";

```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlHead.java`
#### Snippet
```java

  static public final String COMPONENT_FAMILY = "jakarta.faces.Output";
  static public final String COMPONENT_TYPE = "jakarta.faces.OutputHead";

  /**
```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlSelectBooleanCheckbox.java`
#### Snippet
```java
{

  static public final String COMPONENT_FAMILY =
    "jakarta.faces.SelectBoolean";
  static public final String COMPONENT_TYPE =
```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlOutcomeTargetLink.java`
#### Snippet
```java
{

  static public final String COMPONENT_FAMILY =
    "jakarta.faces.OutcomeTarget";
  static public final String COMPONENT_TYPE =
```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlSelectBooleanCheckbox.java`
#### Snippet
```java
  static public final String COMPONENT_FAMILY =
    "jakarta.faces.SelectBoolean";
  static public final String COMPONENT_TYPE =
    "jakarta.faces.HtmlSelectBooleanCheckbox";

```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlOutcomeTargetLink.java`
#### Snippet
```java
  static public final String COMPONENT_FAMILY =
    "jakarta.faces.OutcomeTarget";
  static public final String COMPONENT_TYPE =
    "jakarta.faces.HtmlOutcomeTargetLink";

```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlDataTable.java`
#### Snippet
```java
  static public final String COMPONENT_FAMILY =
    "jakarta.faces.Data";
  static public final String COMPONENT_TYPE =
    "jakarta.faces.HtmlDataTable";

```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlDataTable.java`
#### Snippet
```java
{

  static public final String COMPONENT_FAMILY =
    "jakarta.faces.Data";
  static public final String COMPONENT_TYPE =
```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlMessage.java`
#### Snippet
```java
  static public final String COMPONENT_FAMILY =
    "jakarta.faces.Message";
  static public final String COMPONENT_TYPE =
    "jakarta.faces.HtmlMessage";

```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlMessage.java`
#### Snippet
```java
{

  static public final String COMPONENT_FAMILY =
    "jakarta.faces.Message";
  static public final String COMPONENT_TYPE =
```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlInputFile.java`
#### Snippet
```java
{

    static public final String COMPONENT_FAMILY = "jakarta.faces.Input";
    static public final String COMPONENT_TYPE = "jakarta.faces.HtmlInputFile";

```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlInputFile.java`
#### Snippet
```java

    static public final String COMPONENT_FAMILY = "jakarta.faces.Input";
    static public final String COMPONENT_TYPE = "jakarta.faces.HtmlInputFile";

    /**
```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlOutputFormat.java`
#### Snippet
```java
  static public final String COMPONENT_FAMILY =
    "jakarta.faces.Output";
  static public final String COMPONENT_TYPE =
    "jakarta.faces.HtmlOutputFormat";

```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlOutputFormat.java`
#### Snippet
```java
{

  static public final String COMPONENT_FAMILY =
    "jakarta.faces.Output";
  static public final String COMPONENT_TYPE =
```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlOutputText.java`
#### Snippet
```java
{

  static public final String COMPONENT_FAMILY =
    "jakarta.faces.Output";
  static public final String COMPONENT_TYPE =
```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlOutputText.java`
#### Snippet
```java
  static public final String COMPONENT_FAMILY =
    "jakarta.faces.Output";
  static public final String COMPONENT_TYPE =
    "jakarta.faces.HtmlOutputText";

```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlMessages.java`
#### Snippet
```java
{

  static public final String COMPONENT_FAMILY =
    "jakarta.faces.Messages";
  static public final String COMPONENT_TYPE =
```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlMessages.java`
#### Snippet
```java
  static public final String COMPONENT_FAMILY =
    "jakarta.faces.Messages";
  static public final String COMPONENT_TYPE =
    "jakarta.faces.HtmlMessages";

```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlSelectManyListbox.java`
#### Snippet
```java
  static public final String COMPONENT_FAMILY =
    "jakarta.faces.SelectMany";
  static public final String COMPONENT_TYPE =
    "jakarta.faces.HtmlSelectManyListbox";

```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlSelectManyListbox.java`
#### Snippet
```java
{

  static public final String COMPONENT_FAMILY =
    "jakarta.faces.SelectMany";
  static public final String COMPONENT_TYPE =
```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlOutputLabel.java`
#### Snippet
```java
  static public final String COMPONENT_FAMILY =
    "jakarta.faces.Output";
  static public final String COMPONENT_TYPE =
    "jakarta.faces.HtmlOutputLabel";

```

### MissortedModifiers
Missorted modifiers `static public`
in `api/src/main/java/jakarta/faces/component/html/_HtmlOutputLabel.java`
#### Snippet
```java
{

  static public final String COMPONENT_FAMILY =
    "jakarta.faces.Output";
  static public final String COMPONENT_TYPE =
```

### MissortedModifiers
Missorted modifiers `final static`
in `api/src/main/java/jakarta/faces/component/UIComponentBase.java`
#### Snippet
```java
    private int _capabilities = 0;
    private final static int FLAG_IS_RENDERER_TYPE_SET = 1;
    private final static int FLAG_ADDED_BY_HANDLER = 2;
    private final static int FLAG_FACET_CREATED_UIPANEL = 4;
    private final static int FLAG_PASSTHROUGH_ATTRIBUTE_MAP_SET = 8;
```

### MissortedModifiers
Missorted modifiers `final static`
in `api/src/main/java/jakarta/faces/component/UIComponentBase.java`
#### Snippet
```java
    private final static int FLAG_ADDED_BY_HANDLER = 2;
    private final static int FLAG_FACET_CREATED_UIPANEL = 4;
    private final static int FLAG_PASSTHROUGH_ATTRIBUTE_MAP_SET = 8;

    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `api/src/main/java/jakarta/faces/component/UIComponentBase.java`
#### Snippet
```java
    private final static int FLAG_IS_RENDERER_TYPE_SET = 1;
    private final static int FLAG_ADDED_BY_HANDLER = 2;
    private final static int FLAG_FACET_CREATED_UIPANEL = 4;
    private final static int FLAG_PASSTHROUGH_ATTRIBUTE_MAP_SET = 8;

```

### MissortedModifiers
Missorted modifiers `final static`
in `api/src/main/java/jakarta/faces/component/UIComponentBase.java`
#### Snippet
```java
    
    private int _capabilities = 0;
    private final static int FLAG_IS_RENDERER_TYPE_SET = 1;
    private final static int FLAG_ADDED_BY_HANDLER = 2;
    private final static int FLAG_FACET_CREATED_UIPANEL = 4;
```

### MissortedModifiers
Missorted modifiers `final static`
in `api/src/main/java/jakarta/faces/component/_ComponentAttributesMap.java`
#### Snippet
```java
    public final static String FACET_CREATED_UIPANEL_MARKER = "oam.vf.createdUIPanel";
    
    private final static String COMPONENT_ADDED_BY_HANDLER_MARKER = "oam.vf.addedByHandler";
    
    public static final String PROPERTY_DESCRIPTOR_MAP_KEY = "oam.cc.beanInfo.PDM";
```

### MissortedModifiers
Missorted modifiers `final static`
in `api/src/main/java/jakarta/faces/component/_ComponentAttributesMap.java`
#### Snippet
```java
    private static final Object[] EMPTY_ARGS = new Object[0];
    
    private final static String MARK_CREATED = "oam.vf.MARK_ID";
    
    private final static String FACET_NAME_KEY = "facelets.FACET_NAME";
```

### MissortedModifiers
Missorted modifiers `final static`
in `api/src/main/java/jakarta/faces/component/_ComponentAttributesMap.java`
#### Snippet
```java
     * put() operations.
     */
    private final static int MIN_LENGHT_CHECK = MARK_CREATED.length();

    // The component that is read/written via this map.
```

### MissortedModifiers
Missorted modifiers `final static`
in `api/src/main/java/jakarta/faces/component/_ComponentAttributesMap.java`
#### Snippet
```java
    private final static String MARK_CREATED = "oam.vf.MARK_ID";
    
    private final static String FACET_NAME_KEY = "facelets.FACET_NAME";
    
    public final static String FACET_CREATED_UIPANEL_MARKER = "oam.vf.createdUIPanel";
```

### MissortedModifiers
Missorted modifiers `final static`
in `api/src/main/java/jakarta/faces/component/_ComponentAttributesMap.java`
#### Snippet
```java
    private final static String FACET_NAME_KEY = "facelets.FACET_NAME";
    
    public final static String FACET_CREATED_UIPANEL_MARKER = "oam.vf.createdUIPanel";
    
    private final static String COMPONENT_ADDED_BY_HANDLER_MARKER = "oam.vf.addedByHandler";
```

### MissortedModifiers
Missorted modifiers `final static`
in `api/src/main/java/jakarta/faces/annotation/View.java`
#### Snippet
```java
     *
     */
    public final static class Literal extends AnnotationLiteral<View> implements View
    {
        public static final Literal INSTANCE = of("");
```

### MissortedModifiers
Missorted modifiers `final static`
in `api/src/main/java/jakarta/faces/application/ResourceHandler.java`
#### Snippet
```java
    public static final String FACES_SCRIPT_LIBRARY_NAME = "jakarta.faces";        

    private final static String RENDERED_RESOURCES_SET = "org.apache.myfaces.RENDERED_RESOURCES_SET";

    public abstract Resource createResource(String resourceName);
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/el/resolver/FlashELResolver.java`
#### Snippet
```java
    private final static String FLASH = "flash";

    private final static String KEEP = "keep";

    private final static String NOW = "now";
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/el/resolver/FlashELResolver.java`
#### Snippet
```java
    private final static String KEEP = "keep";

    private final static String NOW = "now";

    public FlashELResolver()
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/el/resolver/FlashELResolver.java`
#### Snippet
```java
{

    private final static String FLASH = "flash";

    private final static String KEEP = "keep";
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/spi/ServiceProviderFinderFactory.java`
#### Snippet
```java
{

    private final static String SERVICE_PROVIDER_KEY = "org.apache.myfaces.spi.ServiceProviderFinder";
    
    /**
```

### MissortedModifiers
Missorted modifiers `volatile static`
in `impl/src/main/java/org/apache/myfaces/spi/impl/ResourceAnnotationInjectionProvider.java`
#### Snippet
```java
     * the webapp is undeployed just by removing the Map for the current ClassLoader. 
     */
    private volatile static WeakHashMap<ClassLoader, Map<Class,Field[]> > declaredFieldBeans = new WeakHashMap<>();

    protected Context context;
```

### MissortedModifiers
Missorted modifiers `volatile static`
in `impl/src/main/java/org/apache/myfaces/spi/impl/NoInjectionAnnotationInjectionProvider.java`
#### Snippet
```java
     * the webapp is undeployed just by removing the Map for the current ClassLoader. 
     */
    private volatile static WeakHashMap<ClassLoader, Map<Class,Method[]> > declaredMethodBeans = 
            new WeakHashMap<ClassLoader, Map<Class, Method[]>>();

```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/flow/cdi/DefaultCDIFacesFlowProvider.java`
#### Snippet
```java
public class DefaultCDIFacesFlowProvider extends FacesFlowProvider
{
    private final static String CURRENT_FLOW_SCOPE_MAP = "oam.flow.SCOPE_MAP";
    private final static char SEPARATOR_CHAR = '.';

```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/flow/cdi/DefaultCDIFacesFlowProvider.java`
#### Snippet
```java
{
    private final static String CURRENT_FLOW_SCOPE_MAP = "oam.flow.SCOPE_MAP";
    private final static char SEPARATOR_CHAR = '.';

    private List<Flow> flows;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/flow/impl/DefaultFacesFlowProvider.java`
#### Snippet
```java
    static final char SEPARATOR_CHAR = '.';
    
    private final static String CURRENT_FLOW_SCOPE_MAP = "oam.flow.SCOPE_MAP";

    @Override
```

### MissortedModifiers
Missorted modifiers `static public`
in `impl/src/main/java/org/apache/myfaces/push/HtmlBufferResponseWriterWrapper.java`
#### Snippet
```java
     * @return A properly initialized writer which stores the output in a buffer; writer is wrapped.
     */
    static public HtmlBufferResponseWriterWrapper getInstance(ResponseWriter initialWriter)
    {
        FastWriter bufferWriter = new FastWriter();
```

### MissortedModifiers
Missorted modifiers `static public`
in `impl/src/main/java/org/apache/myfaces/push/_WebsocketInit.java`
#### Snippet
```java
public abstract class _WebsocketInit extends UIOutput
{
    static public final String ATTRIBUTE_COMPONENT_ID = _WebsocketInit.class.getName() + "ComponentId";
    
    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/push/WebsocketSessionClusterSerializedRestore.java`
#### Snippet
```java
public class WebsocketSessionClusterSerializedRestore implements Externalizable
{
    public final static String WEBSOCKET_SESSION_SERIALIZED_RESTORE = "oam.websocket.SR";
    
    private String channelToken;
```

### MissortedModifiers
Missorted modifiers `static abstract`
in `impl/src/main/java/org/apache/myfaces/push/cdi/WebsocketScopeManager.java`
#### Snippet
```java
    }
    
    public static abstract class AbstractUserScope extends AbstractScope
    {
        protected Map<String, WebsocketChannelMetadata> tokens = new ConcurrentHashMap<>(2, 1f);
```

### MissortedModifiers
Missorted modifiers `static abstract`
in `impl/src/main/java/org/apache/myfaces/push/cdi/WebsocketScopeManager.java`
#### Snippet
```java
    

    public static abstract class AbstractScope implements Serializable
    {
        /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/flow/FlowHandlerImpl.java`
#### Snippet
```java
    
    private final static String RETURN_MODE = "oam.flow.RETURN_MODE";
    private final static String FLOW_RETURN_STACK = "oam.flow.RETURN_STACK.";
    private final static String CURRENT_FLOW_REQUEST_STACK = "oam.flow.REQUEST_STACK.";
    
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/flow/FlowHandlerImpl.java`
#### Snippet
```java
    private final static String RETURN_MODE = "oam.flow.RETURN_MODE";
    private final static String FLOW_RETURN_STACK = "oam.flow.RETURN_STACK.";
    private final static String CURRENT_FLOW_REQUEST_STACK = "oam.flow.REQUEST_STACK.";
    
    private Map<String, Map<String, Flow>> _flowMapByDocumentId;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/flow/FlowHandlerImpl.java`
#### Snippet
```java
{
    private final static String CURRENT_FLOW_STACK = "oam.flow.STACK.";
    private final static String ROOT_LAST_VIEW_ID = "oam.flow.ROOT_LAST_VIEW_ID.";
    
    private final static String RETURN_MODE = "oam.flow.RETURN_MODE";
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/flow/FlowHandlerImpl.java`
#### Snippet
```java
    private final static String ROOT_LAST_VIEW_ID = "oam.flow.ROOT_LAST_VIEW_ID.";
    
    private final static String RETURN_MODE = "oam.flow.RETURN_MODE";
    private final static String FLOW_RETURN_STACK = "oam.flow.RETURN_STACK.";
    private final static String CURRENT_FLOW_REQUEST_STACK = "oam.flow.REQUEST_STACK.";
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/flow/FlowHandlerImpl.java`
#### Snippet
```java
public class FlowHandlerImpl extends FlowHandler implements SystemEventListener
{
    private final static String CURRENT_FLOW_STACK = "oam.flow.STACK.";
    private final static String ROOT_LAST_VIEW_ID = "oam.flow.ROOT_LAST_VIEW_ID.";
    
```

### MissortedModifiers
Missorted modifiers `abstract protected`
in `impl/src/main/java/org/apache/myfaces/util/lang/AbstractThreadSafeAttributeMap.java`
#### Snippet
```java
    abstract protected void removeAttribute(String key);

    abstract protected Enumeration<String> getAttributeNames();

    private abstract class AbstractAttributeSet<E> extends AbstractSet<E>
```

### MissortedModifiers
Missorted modifiers `abstract protected`
in `impl/src/main/java/org/apache/myfaces/util/lang/AbstractThreadSafeAttributeMap.java`
#### Snippet
```java
    }

    abstract protected V getAttribute(String key);

    abstract protected void setAttribute(String key, V value);
```

### MissortedModifiers
Missorted modifiers `abstract protected`
in `impl/src/main/java/org/apache/myfaces/util/lang/AbstractThreadSafeAttributeMap.java`
#### Snippet
```java
    abstract protected void setAttribute(String key, V value);

    abstract protected void removeAttribute(String key);

    abstract protected Enumeration<String> getAttributeNames();
```

### MissortedModifiers
Missorted modifiers `abstract protected`
in `impl/src/main/java/org/apache/myfaces/util/lang/AbstractThreadSafeAttributeMap.java`
#### Snippet
```java
    abstract protected V getAttribute(String key);

    abstract protected void setAttribute(String key, V value);

    abstract protected void removeAttribute(String key);
```

### MissortedModifiers
Missorted modifiers `static public`
in `impl/src/main/java/org/apache/myfaces/util/lang/StringCharArrayAccessor.java`
#### Snippet
```java
     * @throws IOException If an I/O error occurs
     */
    static public void writeStringAsCharArray(Writer writer, String str, int off, int len) throws IOException
    {
        if (!enabled)
```

### MissortedModifiers
Missorted modifiers `static public`
in `impl/src/main/java/org/apache/myfaces/util/lang/StringCharArrayAccessor.java`
#### Snippet
```java
     * @throws IOException If an I/O error occurs
     */
    static public void writeStringAsCharArray(Writer writer, String str) throws IOException
    {
        writeStringAsCharArray(writer, str, 0, str.length());
```

### MissortedModifiers
Missorted modifiers `static public`
in `impl/src/main/java/org/apache/myfaces/util/lang/StringCharArrayAccessor.java`
#### Snippet
```java
    }

    static public boolean isEnabled()
    {
        return enabled;
```

### MissortedModifiers
Missorted modifiers `abstract protected`
in `impl/src/main/java/org/apache/myfaces/util/lang/AbstractAttributeMap.java`
#### Snippet
```java
    abstract protected V getAttribute(String key);

    abstract protected void setAttribute(String key, V value);

    abstract protected void removeAttribute(String key);
```

### MissortedModifiers
Missorted modifiers `abstract protected`
in `impl/src/main/java/org/apache/myfaces/util/lang/AbstractAttributeMap.java`
#### Snippet
```java
    }

    abstract protected V getAttribute(String key);

    abstract protected void setAttribute(String key, V value);
```

### MissortedModifiers
Missorted modifiers `abstract protected`
in `impl/src/main/java/org/apache/myfaces/util/lang/AbstractAttributeMap.java`
#### Snippet
```java
    abstract protected void setAttribute(String key, V value);

    abstract protected void removeAttribute(String key);

    abstract protected Enumeration<String> getAttributeNames();
```

### MissortedModifiers
Missorted modifiers `abstract protected`
in `impl/src/main/java/org/apache/myfaces/util/lang/AbstractAttributeMap.java`
#### Snippet
```java
    abstract protected void removeAttribute(String key);

    abstract protected Enumeration<String> getAttributeNames();

    private abstract class AbstractAttributeSet<E> extends AbstractSet<E>
```

### MissortedModifiers
Missorted modifiers `static private`
in `impl/src/main/java/org/apache/myfaces/util/lang/SubKeyMap.java`
#### Snippet
```java
    }

    static private boolean _equals(Object a, Object b)
    {
        if (a == null)
```

### MissortedModifiers
Missorted modifiers `static public`
in `impl/src/main/java/org/apache/myfaces/view/facelets/StateWriter.java`
#### Snippet
```java
    private boolean writtenStateWithoutWrapper;

    static public StateWriter getCurrentInstance()
    {
        FacesContext facesContext = FacesContext.getCurrentInstance();
```

### MissortedModifiers
Missorted modifiers `static public`
in `impl/src/main/java/org/apache/myfaces/view/facelets/StateWriter.java`
#### Snippet
```java
    }
        
    static public StateWriter getCurrentInstance(FacesContext facesContext)
    {
        return (StateWriter)facesContext.getAttributes().get(CURRENT_WRITER_KEY);
```

### MissortedModifiers
Missorted modifiers `abstract public`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletCompositionContext.java`
#### Snippet
```java
 * @version $Revision$ $Date$
 */
abstract public class FaceletCompositionContext
{
    static protected final String FACELET_COMPOSITION_CONTEXT_KEY = "oam.facelets.FACELET_COMPOSITION_CONTEXT";
```

### MissortedModifiers
Missorted modifiers `static public`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletCompositionContext.java`
#### Snippet
```java
    }
    
    static public FaceletCompositionContext getCurrentInstance(FacesContext ctx)
    {
        return (FaceletCompositionContext) ctx.getAttributes().get(FACELET_COMPOSITION_CONTEXT_KEY);
```

### MissortedModifiers
Missorted modifiers `static protected`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletCompositionContext.java`
#### Snippet
```java
abstract public class FaceletCompositionContext
{
    static protected final String FACELET_COMPOSITION_CONTEXT_KEY = "oam.facelets.FACELET_COMPOSITION_CONTEXT";

    protected FaceletCompositionContext()
```

### MissortedModifiers
Missorted modifiers `static public`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletCompositionContext.java`
#### Snippet
```java
    }
    
    static public FaceletCompositionContext getCurrentInstance(FaceletContext ctx)
    {
        if (ctx instanceof AbstractFaceletContext)
```

### MissortedModifiers
Missorted modifiers `static public`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletCompositionContext.java`
#### Snippet
```java
    }
    
    static public FaceletCompositionContext getCurrentInstance()
    {
        return (FaceletCompositionContext)
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletFactory.java`
#### Snippet
```java
    private static ThreadLocal<FaceletFactory> instance = new ThreadLocal<FaceletFactory>();

    public final static String LAST_RESOURCE_RESOLVED = "oam.facelets.LAST_RESOURCE_RESOLVED";

    /**
```

### MissortedModifiers
Missorted modifiers `static abstract`
in `impl/src/main/java/org/apache/myfaces/util/lang/StreamCharBuffer.java`
#### Snippet
```java

    // keep read state in this class
    static abstract class ChunkReader
    {
        public abstract int read(char[] ch, int off, int len)
```

### MissortedModifiers
Missorted modifiers `final public`
in `impl/src/main/java/org/apache/myfaces/util/lang/StreamCharBuffer.java`
#### Snippet
```java
     */

    final public class StreamCharBufferReader extends Reader
    {
        boolean eofException = false;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/ViewPoolProcessor.java`
#### Snippet
```java
     * partially (an &lt;view-pool-mapping&gt; entry inside &lt;faces-config-extension&gt;).
     */
    private final static String INSTANCE = "oam.ViewPoolProcessor";
    
    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/ViewPoolProcessor.java`
#### Snippet
```java
     * into the pool directly.
     */
    public final static String FORCE_HARD_RESET = "oam.ViewPool.forceHardReset";
    
    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/ViewPoolProcessor.java`
#### Snippet
```java
     * (reset and check) reset is required in the call to saveState().
     */
    public final static String RESET_SAVE_STATE_MODE_KEY ="oam.view.resetSaveStateMode";
    
    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/ViewPoolProcessor.java`
#### Snippet
```java
     * Flag to indicate that dispose this view on navigation is valid.
     */
    public final static String DISPOSE_VIEW_NAVIGATION = "oam.ViewPool.disposeViewOnNavigation";
    
    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/ViewPoolProcessor.java`
#### Snippet
```java
     * UIViewRoot attribute to enable/disable the view for use pooling.
     */
    public final static String ENABLE_VIEW_POOL = "oamEnableViewPool";
    
    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/TagAttributesImpl.java`
#### Snippet
```java
public final class TagAttributesImpl extends TagAttributes
{
    private final static TagAttribute[] EMPTY = new TagAttribute[0];

    private final TagAttribute[] attributes;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ComponentTagDeclarationLibrary.java`
#### Snippet
```java
    private static class UserComponentHandlerFactory implements TagHandlerFactory
    {
        private final static Class<?>[] CONS_SIG = new Class[] { ComponentConfig.class };

        protected final String componentType;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/MetaRulesetImpl.java`
#### Snippet
```java
    }

    private final static TagAttribute[] EMPTY = new TagAttribute[0];
    
    private final Map<String, TagAttribute> _attributes;
```

### MissortedModifiers
Missorted modifiers `volatile static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/MetaRulesetImpl.java`
#### Snippet
```java
     * the webapp is undeployed just by removing the Map for the current ClassLoader. 
     */
    private volatile static WeakHashMap<ClassLoader, Map<String, MetadataTarget>> metadata
            = new WeakHashMap<ClassLoader, Map<String, MetadataTarget>>();

```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/MetaRulesetImpl.java`
#### Snippet
```java
public final class MetaRulesetImpl extends MetaRuleset
{
    private final static Logger log = Logger.getLogger(MetaRulesetImpl.class.getName());

    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/BeanPropertyTagRule.java`
#### Snippet
```java
public final class BeanPropertyTagRule extends MetaRule
{
    public final static BeanPropertyTagRule INSTANCE = new BeanPropertyTagRule();

    @Override
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/BeanPropertyTagRule.java`
#### Snippet
```java
    }

    final static class DynamicPropertyMetadata extends Metadata
    {
        private final Class<?> propertyType;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/BeanPropertyTagRule.java`
#### Snippet
```java
    }
    
    final static class LiteralPropertyMetadata extends Metadata
    {
        private final Class<?> propertyType;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/_Fragment.java`
#### Snippet
```java
{
    public final static String COMPONENT_TYPE = "facelets.ui.ComponentRef";
    public final static String COMPONENT_FAMILY = "facelets";


```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/_Fragment.java`
#### Snippet
```java
abstract class _Fragment extends UIComponentBase
{
    public final static String COMPONENT_TYPE = "facelets.ui.ComponentRef";
    public final static String COMPONENT_FAMILY = "facelets";

```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/UILibrary.java`
#### Snippet
```java
public final class UILibrary extends AbstractTagLibrary
{
    public final static String NAMESPACE = "jakarta.faces.facelets";
    public final static String JCP_NAMESPACE = "http://xmlns.jcp.org/jsf/facelets";
    public final static String SUN_NAMESPACE = "http://java.sun.com/jsf/facelets";
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/UILibrary.java`
#### Snippet
```java
{
    public final static String NAMESPACE = "jakarta.faces.facelets";
    public final static String JCP_NAMESPACE = "http://xmlns.jcp.org/jsf/facelets";
    public final static String SUN_NAMESPACE = "http://java.sun.com/jsf/facelets";

```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/UILibrary.java`
#### Snippet
```java
    public final static String SUN_NAMESPACE = "http://java.sun.com/jsf/facelets";

    public final static UILibrary INSTANCE = new UILibrary();

    public UILibrary()
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/UILibrary.java`
#### Snippet
```java
    public final static String NAMESPACE = "jakarta.faces.facelets";
    public final static String JCP_NAMESPACE = "http://xmlns.jcp.org/jsf/facelets";
    public final static String SUN_NAMESPACE = "http://java.sun.com/jsf/facelets";

    public final static UILibrary INSTANCE = new UILibrary();
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ELText.java`
#### Snippet
```java
        //Just like TagAttributeImpl
        private final static int EL_CC = 2;
        private final static int EL_RESOURCE = 8;
        
        private final ValueExpression ve;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ELText.java`
#### Snippet
```java
    {
        //Just like TagAttributeImpl
        private final static int EL_CC = 2;
        private final static int EL_RESOURCE = 8;
        
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/LegacyUILibrary.java`
#### Snippet
```java
public final class LegacyUILibrary extends AbstractTagLibrary
{
    public final static String NAMESPACE = "jakarta.faces.facelets";
    public final static String JCP_NAMESPACE = "http://xmlns.jcp.org/jsf/facelets";
    public final static String SUN_NAMESPACE = "http://java.sun.com/jsf/facelets";
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/LegacyUILibrary.java`
#### Snippet
```java
    public final static String SUN_NAMESPACE = "http://java.sun.com/jsf/facelets";

    public final static LegacyUILibrary INSTANCE = new LegacyUILibrary();

    public LegacyUILibrary()
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/LegacyUILibrary.java`
#### Snippet
```java
    public final static String NAMESPACE = "jakarta.faces.facelets";
    public final static String JCP_NAMESPACE = "http://xmlns.jcp.org/jsf/facelets";
    public final static String SUN_NAMESPACE = "http://java.sun.com/jsf/facelets";

    public final static LegacyUILibrary INSTANCE = new LegacyUILibrary();
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/LegacyUILibrary.java`
#### Snippet
```java
{
    public final static String NAMESPACE = "jakarta.faces.facelets";
    public final static String JCP_NAMESPACE = "http://xmlns.jcp.org/jsf/facelets";
    public final static String SUN_NAMESPACE = "http://java.sun.com/jsf/facelets";

```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/ComponentRef.java`
#### Snippet
```java

    public final static String COMPONENT_TYPE = "facelets.ui.ComponentRef";
    public final static String COMPONENT_FAMILY = "facelets";

    public ComponentRef()
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/ComponentRef.java`
#### Snippet
```java
{

    public final static String COMPONENT_TYPE = "facelets.ui.ComponentRef";
    public final static String COMPONENT_FAMILY = "facelets";

```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/LegacyCompositionHandler.java`
#### Snippet
```java
    private static final Logger log = Logger.getLogger(CompositionHandler.class.getName());

    public final static String NAME = "composition";

    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/TagAttributeImpl.java`
#### Snippet
```java
    private final static int EL_CC_ATTR_ME = 4;
    
    private final static int EL_RESOURCE = 8;
    
    private final int capabilities;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/TagAttributeImpl.java`
#### Snippet
```java
    private final static int EL_CC = 2;
    
    private final static int EL_CC_ATTR_ME = 4;
    
    private final static int EL_RESOURCE = 8;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/TagAttributeImpl.java`
#### Snippet
```java
{

    private final static int EL_LITERAL = 1;
   
    private final static int EL_CC = 2;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/TagAttributeImpl.java`
#### Snippet
```java
    private final static int EL_LITERAL = 1;
   
    private final static int EL_CC = 2;
    
    private final static int EL_CC_ATTR_ME = 4;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/CompositionHandler.java`
#### Snippet
```java
    private static final Logger log = Logger.getLogger(CompositionHandler.class.getName());

    public final static String NAME = "composition";

    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/_Component.java`
#### Snippet
```java
abstract class _Component extends UIComponentBase
{
    public final static String COMPONENT_TYPE = "facelets.ui.ComponentRef";
    public final static String COMPONENT_FAMILY = "facelets";

```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/_Component.java`
#### Snippet
```java
{
    public final static String COMPONENT_TYPE = "facelets.ui.ComponentRef";
    public final static String COMPONENT_FAMILY = "facelets";


```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/ComponentRefHandler.java`
#### Snippet
```java
{

    public final static String NAME = "component";

    public ComponentRefHandler(ComponentConfig config)
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/AbstractTagLibrary.java`
#### Snippet
```java
    private static class UserComponentHandlerFactory implements TagHandlerFactory
    {
        private final static Class<?>[] CONS_SIG = new Class[] { ComponentConfig.class };

        protected final String componentType;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/AbstractTagLibrary.java`
#### Snippet
```java
    private static class UserValidatorHandlerFactory implements TagHandlerFactory
    {
        private final static Class<?>[] CONS_SIG = new Class[] { ValidatorConfig.class };

        protected final String validatorId;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/AbstractTagLibrary.java`
#### Snippet
```java
    private static class HandlerFactory implements TagHandlerFactory
    {
        private final static Class<?>[] CONSTRUCTOR_SIG = new Class[]{TagConfig.class};

        protected final Class<? extends TagHandler> handlerType;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/AbstractTagLibrary.java`
#### Snippet
```java
    private static class UserBehaviorHandlerFactory implements TagHandlerFactory
    {
        private final static Class<?>[] CONS_SIG = new Class[] { BehaviorConfig.class };

        protected final String behaviorId;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/AbstractTagLibrary.java`
#### Snippet
```java
    private static class UserConverterHandlerFactory implements TagHandlerFactory
    {
        private final static Class<?>[] CONS_SIG = new Class[] { ConverterConfig.class };

        protected final String converterId;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/fn/JstlFnLibrary.java`
#### Snippet
```java
public class JstlFnLibrary implements TagLibrary
{
    public final static String NAMESPACE = "jakarta.tags.functions";
    public final static String JCP_NAMESPACE = "http://xmlns.jcp.org/jsp/jstl/functions";
    public final static String SUN_NAMESPACE = "http://java.sun.com/jsp/jstl/functions";
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/fn/JstlFnLibrary.java`
#### Snippet
```java
{
    public final static String NAMESPACE = "jakarta.tags.functions";
    public final static String JCP_NAMESPACE = "http://xmlns.jcp.org/jsp/jstl/functions";
    public final static String SUN_NAMESPACE = "http://java.sun.com/jsp/jstl/functions";

```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/fn/JstlFnLibrary.java`
#### Snippet
```java
    public final static String NAMESPACE = "jakarta.tags.functions";
    public final static String JCP_NAMESPACE = "http://xmlns.jcp.org/jsp/jstl/functions";
    public final static String SUN_NAMESPACE = "http://java.sun.com/jsp/jstl/functions";

    private final Map<String, Method> fns = new HashMap<>();
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/MappedValueExpression.java`
#### Snippet
```java
public final class MappedValueExpression extends ValueExpression
{
    private final static class Entry implements Map.Entry, Serializable
    {
        private final Map src;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/LegacyJstlCoreLibrary.java`
#### Snippet
```java
    public final static String NAMESPACE = "jakarta.tags.core";
    public final static String JCP_NAMESPACE = "http://xmlns.jcp.org/jsp/jstl/core";
    public final static String SUN_NAMESPACE = "http://java.sun.com/jsp/jstl/core";

    // This namespace was used in the early stages of Faces 2.0 development, but later
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/LegacyJstlCoreLibrary.java`
#### Snippet
```java
public final class LegacyJstlCoreLibrary extends AbstractTagLibrary
{
    public final static String NAMESPACE = "jakarta.tags.core";
    public final static String JCP_NAMESPACE = "http://xmlns.jcp.org/jsp/jstl/core";
    public final static String SUN_NAMESPACE = "http://java.sun.com/jsp/jstl/core";
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/LegacyJstlCoreLibrary.java`
#### Snippet
```java
    public final static String ALTERNATIVE_NAMESPACE = "http://java.sun.com/jstl/core";

    public final static LegacyJstlCoreLibrary INSTANCE = new LegacyJstlCoreLibrary();

    public LegacyJstlCoreLibrary()
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/LegacyJstlCoreLibrary.java`
#### Snippet
```java
    // uses the old syntax and it is too hard to fix them, so in Faces 2.2 it was 
    // decided to add this namespace as an alternative synonym.
    public final static String ALTERNATIVE_NAMESPACE = "http://java.sun.com/jstl/core";

    public final static LegacyJstlCoreLibrary INSTANCE = new LegacyJstlCoreLibrary();
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/LegacyJstlCoreLibrary.java`
#### Snippet
```java
{
    public final static String NAMESPACE = "jakarta.tags.core";
    public final static String JCP_NAMESPACE = "http://xmlns.jcp.org/jsp/jstl/core";
    public final static String SUN_NAMESPACE = "http://java.sun.com/jsp/jstl/core";

```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/JstlCoreLibrary.java`
#### Snippet
```java
    public final static String ALTERNATIVE_NAMESPACE = "http://java.sun.com/jstl/core";

    public final static JstlCoreLibrary INSTANCE = new JstlCoreLibrary();

    public JstlCoreLibrary()
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/JstlCoreLibrary.java`
#### Snippet
```java
public final class JstlCoreLibrary extends AbstractTagLibrary
{
    public final static String NAMESPACE = "jakarta.tags.core";
    public final static String JCP_NAMESPACE = "http://xmlns.jcp.org/jsp/jstl/core";
    public final static String SUN_NAMESPACE = "http://java.sun.com/jsp/jstl/core";
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/JstlCoreLibrary.java`
#### Snippet
```java
    public final static String NAMESPACE = "jakarta.tags.core";
    public final static String JCP_NAMESPACE = "http://xmlns.jcp.org/jsp/jstl/core";
    public final static String SUN_NAMESPACE = "http://java.sun.com/jsp/jstl/core";

    // This namespace was used in the early stages of Faces 2.0 development, but later
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/JstlCoreLibrary.java`
#### Snippet
```java
    // uses the old syntax and it is too hard to fix them, so in Faces 2.2 it was 
    // decided to add this namespace as an alternative synonym.
    public final static String ALTERNATIVE_NAMESPACE = "http://java.sun.com/jstl/core";

    public final static JstlCoreLibrary INSTANCE = new JstlCoreLibrary();
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/JstlCoreLibrary.java`
#### Snippet
```java
{
    public final static String NAMESPACE = "jakarta.tags.core";
    public final static String JCP_NAMESPACE = "http://xmlns.jcp.org/jsp/jstl/core";
    public final static String SUN_NAMESPACE = "http://java.sun.com/jsp/jstl/core";

```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/JsfLibrary.java`
#### Snippet
```java
    public final static String NAMESPACE = "jakarta.faces";
    public final static String JCP_NAMESPACE = "http://xmlns.jcp.org/jsf";
    public final static String SUN_NAMESPACE = "http://java.sun.com/jsf";

    public final static JsfLibrary INSTANCE = new JsfLibrary();
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/JsfLibrary.java`
#### Snippet
```java
    public final static String SUN_NAMESPACE = "http://java.sun.com/jsf";

    public final static JsfLibrary INSTANCE = new JsfLibrary();

    public JsfLibrary()
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/JsfLibrary.java`
#### Snippet
```java
{
    public final static String NAMESPACE = "jakarta.faces";
    public final static String JCP_NAMESPACE = "http://xmlns.jcp.org/jsf";
    public final static String SUN_NAMESPACE = "http://java.sun.com/jsf";

```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/JsfLibrary.java`
#### Snippet
```java
public final class JsfLibrary extends AbstractTagLibrary
{
    public final static String NAMESPACE = "jakarta.faces";
    public final static String JCP_NAMESPACE = "http://xmlns.jcp.org/jsf";
    public final static String SUN_NAMESPACE = "http://java.sun.com/jsf";
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/PassthroughRuleImpl.java`
#### Snippet
```java
{

    final static class LiteralAttributeMetadata extends Metadata
    {
        private final String _name;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/PassthroughRuleImpl.java`
#### Snippet
```java
    }

    final static class ValueExpressionMetadata extends Metadata
    {
        private final String _name;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/PassthroughRuleImpl.java`
#### Snippet
```java
    }

    public final static PassthroughRuleImpl INSTANCE = new PassthroughRuleImpl();

    public PassthroughRuleImpl()
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ElementNameRule.java`
#### Snippet
```java
{
    
    public final static ElementNameRule INSTANCE = new ElementNameRule();
    
    @Override
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ActionSourceRule.java`
#### Snippet
```java
    }

    final static class ActionListenerMapper2 extends Metadata
    {
        private final TagAttribute _attr;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ActionSourceRule.java`
#### Snippet
```java
    }
    
    final static class ActionMapper2 extends Metadata
    {
        private final TagAttribute _attr;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ActionSourceRule.java`
#### Snippet
```java
public final class ActionSourceRule extends MetaRule
{
    public final static Class<?>[] ACTION_SIG = new Class[0];

    public final static Class<?>[] ACTION_LISTENER_SIG = new Class<?>[] { ActionEvent.class };
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ActionSourceRule.java`
#### Snippet
```java
    public final static Class<?>[] ACTION_LISTENER_SIG = new Class<?>[] { ActionEvent.class };

    final static class ActionMapper extends Metadata
    {
        private final TagAttribute attr;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ActionSourceRule.java`
#### Snippet
```java
    public final static Class<?>[] ACTION_SIG = new Class[0];

    public final static Class<?>[] ACTION_LISTENER_SIG = new Class<?>[] { ActionEvent.class };

    final static class ActionMapper extends Metadata
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ActionSourceRule.java`
#### Snippet
```java
    }

    public final static ActionSourceRule INSTANCE = new ActionSourceRule();

    public ActionSourceRule()
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ActionSourceRule.java`
#### Snippet
```java
    }

    final static class ActionListenerMapper extends Metadata
    {
        private final TagAttribute attr;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ComponentRule.java`
#### Snippet
```java
    private final static Logger log = Logger.getLogger(ComponentRule.class.getName());

    public final static ComponentRule INSTANCE = new ComponentRule();

    public ComponentRule()
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ComponentRule.java`
#### Snippet
```java
    }

    private final static Logger log = Logger.getLogger(ComponentRule.class.getName());

    public final static ComponentRule INSTANCE = new ComponentRule();
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ComponentRule.java`
#### Snippet
```java
    }

    final static class ValueExpressionMetadata extends Metadata
    {
        private final String _name;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ValueHolderRule.java`
#### Snippet
```java
    }

    final static class LiteralValueMetadata extends Metadata
    {
        private final String value;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ValueHolderRule.java`
#### Snippet
```java
    }

    public final static ValueHolderRule INSTANCE = new ValueHolderRule();

    @Override
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ValueHolderRule.java`
#### Snippet
```java
    }

    final static class DynamicValueExpressionMetadata extends Metadata
    {
        private final TagAttribute attr;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ValueHolderRule.java`
#### Snippet
```java
{

    final static class LiteralConverterMetadata extends Metadata
    {
        private final String converterId;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ValueHolderRule.java`
#### Snippet
```java
    }

    final static class DynamicConverterMetadata2 extends Metadata
    {
        private final TagAttribute attr;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/PassThroughLibrary.java`
#### Snippet
```java
    public final static String SUN_NAMESPACE = "http://java.sun.com/jsf/passthrough";

    public final static PassThroughLibrary INSTANCE = new PassThroughLibrary();
    
    public PassThroughLibrary()
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/PassThroughLibrary.java`
#### Snippet
```java
public class PassThroughLibrary extends AbstractTagLibrary
{
    public final static String NAMESPACE = "jakarta.faces.passthrough";
    public final static String JCP_NAMESPACE = "http://xmlns.jcp.org/jsf/passthrough";
    public final static String SUN_NAMESPACE = "http://java.sun.com/jsf/passthrough";
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/PassThroughLibrary.java`
#### Snippet
```java
    public final static String NAMESPACE = "jakarta.faces.passthrough";
    public final static String JCP_NAMESPACE = "http://xmlns.jcp.org/jsf/passthrough";
    public final static String SUN_NAMESPACE = "http://java.sun.com/jsf/passthrough";

    public final static PassThroughLibrary INSTANCE = new PassThroughLibrary();
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/PassThroughLibrary.java`
#### Snippet
```java
{
    public final static String NAMESPACE = "jakarta.faces.passthrough";
    public final static String JCP_NAMESPACE = "http://xmlns.jcp.org/jsf/passthrough";
    public final static String SUN_NAMESPACE = "http://java.sun.com/jsf/passthrough";

```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/EditableValueHolderRule.java`
#### Snippet
```java
    private final static Class<?>[] VALUECHANGE_SIG = new Class[] { ValueChangeEvent.class };
    
    private final static Class<?>[] EMPTY_CLASS_ARRAY = new Class[0];

    public final static EditableValueHolderRule INSTANCE = new EditableValueHolderRule();
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/EditableValueHolderRule.java`
#### Snippet
```java
    }

    private final static Class<?>[] VALIDATOR_SIG = new Class[] { FacesContext.class, UIComponent.class, Object.class };

    private final static Class<?>[] VALUECHANGE_SIG = new Class[] { ValueChangeEvent.class };
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/EditableValueHolderRule.java`
#### Snippet
```java
    private final static Class<?>[] VALIDATOR_SIG = new Class[] { FacesContext.class, UIComponent.class, Object.class };

    private final static Class<?>[] VALUECHANGE_SIG = new Class[] { ValueChangeEvent.class };
    
    private final static Class<?>[] EMPTY_CLASS_ARRAY = new Class[0];
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/EditableValueHolderRule.java`
#### Snippet
```java
    private final static Class<?>[] EMPTY_CLASS_ARRAY = new Class[0];

    public final static EditableValueHolderRule INSTANCE = new EditableValueHolderRule();

    @Override
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/EditableValueHolderRule.java`
#### Snippet
```java
    }

    final static class ValidatorExpressionMetadata extends Metadata
    {
        private final TagAttribute attr;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/EditableValueHolderRule.java`
#### Snippet
```java
    }

    final static class ValueChangedExpressionMetadata extends Metadata
    {
        private final TagAttribute attr;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/EditableValueHolderRule.java`
#### Snippet
```java
{

    final static class LiteralValidatorMetadata extends Metadata
    {
        private final String validatorId;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ValidatorTagHandlerDelegate.java`
#### Snippet
```java
            = "org.apache.myfaces.validator.VALIDATOR_ID_EXCLUSION_LIST";
    
    private final static String BEAN_BEFORE_JSF_PROPERTY = "oam.beanBeforeJsf";
    
    private ValidatorHandler _delegate;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ValidatorTagHandlerDelegate.java`
#### Snippet
```java
     * this key on the parent UIComponent.
     */
    public final static String VALIDATOR_ID_EXCLUSION_LIST_KEY
            = "org.apache.myfaces.validator.VALIDATOR_ID_EXCLUSION_LIST";
    
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/LoadBundleHandler.java`
#### Snippet
```java
    private final static class ResourceBundleMap implements Map<String, String>
    {
        private final static class ResourceEntry implements Map.Entry<String, String>
        {
            protected final String key;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/LoadBundleHandler.java`
#### Snippet
```java
{

    private final static class ResourceBundleMap implements Map<String, String>
    {
        private final static class ResourceEntry implements Map.Entry<String, String>
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/ViewHandler.java`
#### Snippet
```java
{

    private final static Class<?>[] LISTENER_SIG = new Class<?>[] { PhaseEvent.class };

    @JSFFaceletAttribute
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ComponentTagHandlerDelegate.java`
#### Snippet
```java
public class ComponentTagHandlerDelegate extends TagHandlerDelegate
{
    private final static Logger log = Logger.getLogger(ComponentTagHandlerDelegate.class.getName());

    private final ComponentHandler _delegate;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/PhaseListenerHandler.java`
#### Snippet
```java
{

    private final static class LazyPhaseListener implements PhaseListener, Serializable
    {
        private static final long serialVersionUID = -6496143057319213401L;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/CoreLibrary.java`
#### Snippet
```java
public final class CoreLibrary extends AbstractTagLibrary
{
    public final static String NAMESPACE = "jakarta.faces.core";
    public final static String JCP_NAMESPACE = "http://xmlns.jcp.org/jsf/core";
    public final static String SUN_NAMESPACE = "http://java.sun.com/jsf/core";
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/CoreLibrary.java`
#### Snippet
```java
    public final static String NAMESPACE = "jakarta.faces.core";
    public final static String JCP_NAMESPACE = "http://xmlns.jcp.org/jsf/core";
    public final static String SUN_NAMESPACE = "http://java.sun.com/jsf/core";

    public final static CoreLibrary INSTANCE = new CoreLibrary();
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/CoreLibrary.java`
#### Snippet
```java
{
    public final static String NAMESPACE = "jakarta.faces.core";
    public final static String JCP_NAMESPACE = "http://xmlns.jcp.org/jsf/core";
    public final static String SUN_NAMESPACE = "http://java.sun.com/jsf/core";

```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/CoreLibrary.java`
#### Snippet
```java
    public final static String SUN_NAMESPACE = "http://java.sun.com/jsf/core";

    public final static CoreLibrary INSTANCE = new CoreLibrary();

    public CoreLibrary()
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguage.java`
#### Snippet
```java
    public final static String DYN_WRAPPER = "oam.vf.DYN_WRAPPER";
    
    public final static String GEN_MARK_ID = "oam.vf.GEN_MARK_ID";
    //END CONSTANTS SET ON BUILD VIEW

```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguage.java`
#### Snippet
```java

    //BEGIN CONSTANTS SET ON BUILD VIEW
    public final static String BUILDING_VIEW_METADATA = "org.apache.myfaces.BUILDING_VIEW_METADATA";

    public final static String REFRESHING_TRANSIENT_BUILD = "org.apache.myfaces.REFRESHING_TRANSIENT_BUILD";
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguage.java`
#### Snippet
```java
    public final static String MARK_INITIAL_STATE_KEY = "org.apache.myfaces.MARK_INITIAL_STATE";
    
    public final static String IS_BUILDING_INITIAL_STATE_KEY_ALIAS
            = "jakarta.faces.view.ViewDeclarationLanguage.IS_BUILDING_INITIAL_STATE";

```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguage.java`
#### Snippet
```java
     * and indicate which one is the response encoding on getResponseEncoding(FacesContext, String) method.
     */
    public final static String PARAM_ENCODING = "facelets.Encoding";


```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguage.java`
#### Snippet
```java
    public final static long DEFAULT_REFRESH_PERIOD_PRODUCTION = -1;

    public final static String DEFAULT_CHARACTER_ENCODING = "UTF-8";

    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguage.java`
#### Snippet
```java
            = new Class[]{FacesContext.class, UIComponent.class, Object.class};

    public final static long DEFAULT_REFRESH_PERIOD = 0;
    public final static long DEFAULT_REFRESH_PERIOD_PRODUCTION = -1;

```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguage.java`
#### Snippet
```java
    public final static String REFRESH_TRANSIENT_BUILD_ON_PSS = "org.apache.myfaces.REFRESH_TRANSIENT_BUILD_ON_PSS";

    public final static String USING_PSS_ON_THIS_VIEW = "org.apache.myfaces.USING_PSS_ON_THIS_VIEW";

    public final static String REMOVING_COMPONENTS_BUILD = "org.apache.myfaces.REMOVING_COMPONENTS_BUILD";
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguage.java`
#### Snippet
```java
            = "jakarta.faces.view.ViewDeclarationLanguage.IS_BUILDING_INITIAL_STATE";

    public final static String CLEAN_TRANSIENT_BUILD_ON_RESTORE
            = "org.apache.myfaces.CLEAN_TRANSIENT_BUILD_ON_RESTORE";

```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguage.java`
#### Snippet
```java
    public final static String BUILDING_VIEW_METADATA = "org.apache.myfaces.BUILDING_VIEW_METADATA";

    public final static String REFRESHING_TRANSIENT_BUILD = "org.apache.myfaces.REFRESHING_TRANSIENT_BUILD";

    public final static String REFRESH_TRANSIENT_BUILD_ON_PSS = "org.apache.myfaces.REFRESH_TRANSIENT_BUILD_ON_PSS";
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguage.java`
#### Snippet
```java
            = "org.apache.myfaces.CLEAN_TRANSIENT_BUILD_ON_RESTORE";

    private final static String STATE_KEY = "<!--@@JSF_FORM_STATE_MARKER@@-->";

    private final static int STATE_KEY_LEN = STATE_KEY.length();
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguage.java`
#### Snippet
```java
     * after component instances are populated. 
     */
    public final static String MARK_INITIAL_STATE_KEY = "org.apache.myfaces.MARK_INITIAL_STATE";
    
    public final static String IS_BUILDING_INITIAL_STATE_KEY_ALIAS
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguage.java`
#### Snippet
```java
     * Key used to cache component ids for the counter
     */
    public final static String CACHED_COMPONENT_IDS = "oam.CACHED_COMPONENT_IDS"; 
    
    private static final String ASTERISK = "*";
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguage.java`
#### Snippet
```java
    public final static String REFRESHING_TRANSIENT_BUILD = "org.apache.myfaces.REFRESHING_TRANSIENT_BUILD";

    public final static String REFRESH_TRANSIENT_BUILD_ON_PSS = "org.apache.myfaces.REFRESH_TRANSIENT_BUILD_ON_PSS";

    public final static String USING_PSS_ON_THIS_VIEW = "org.apache.myfaces.USING_PSS_ON_THIS_VIEW";
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguage.java`
#### Snippet
```java

    public final static long DEFAULT_REFRESH_PERIOD = 0;
    public final static long DEFAULT_REFRESH_PERIOD_PRODUCTION = -1;

    public final static String DEFAULT_CHARACTER_ENCODING = "UTF-8";
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguage.java`
#### Snippet
```java
    public final static String USING_PSS_ON_THIS_VIEW = "org.apache.myfaces.USING_PSS_ON_THIS_VIEW";

    public final static String REMOVING_COMPONENTS_BUILD = "org.apache.myfaces.REMOVING_COMPONENTS_BUILD";
    
    public final static String DYN_WRAPPER = "oam.vf.DYN_WRAPPER";
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguage.java`
#### Snippet
```java
    public final static String REMOVING_COMPONENTS_BUILD = "org.apache.myfaces.REMOVING_COMPONENTS_BUILD";
    
    public final static String DYN_WRAPPER = "oam.vf.DYN_WRAPPER";
    
    public final static String GEN_MARK_ID = "oam.vf.GEN_MARK_ID";
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguage.java`
#### Snippet
```java
    private final static String STATE_KEY = "<!--@@JSF_FORM_STATE_MARKER@@-->";

    private final static int STATE_KEY_LEN = STATE_KEY.length();
    
    private static final String SERIALIZED_VIEW_REQUEST_ATTR = 
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/ActionListenerHandler.java`
#### Snippet
```java
{

    private final static class LazyActionListener implements ActionListener, Serializable
    {
        private static final long serialVersionUID = -9202120013153262119L;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ComponentSupport.java`
#### Snippet
```java
     * not register it when a remove happens.
     */
    public final static String COMPONENT_ADDED_BY_HANDLER_MARKER = "oam.vf.addedByHandler";

    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ComponentSupport.java`
#### Snippet
```java
     * The key under the facelet state map is stored
     */
    public final static String FACELET_STATE_INSTANCE = "oam.FACELET_STATE_INSTANCE";

    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ComponentSupport.java`
#### Snippet
```java
    }

    public final static String MARK_CREATED = "oam.vf.MARK_ID";
    
    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ComponentSupport.java`
#### Snippet
```java
     * This constant is duplicate in jakarta.faces.webapp.UIComponentClassicTagBase
     */
    public final static String FACET_CREATED_UIPANEL_MARKER = "oam.vf.createdUIPanel";

    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/html/HtmlLibrary.java`
#### Snippet
```java
{
    public final static String NAMESPACE = "jakarta.faces.html";
    public final static String JCP_NAMESPACE = "http://xmlns.jcp.org/jsf/html";
    public final static String SUN_NAMESPACE = "http://java.sun.com/jsf/html";

```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/html/HtmlLibrary.java`
#### Snippet
```java
public final class HtmlLibrary extends AbstractHtmlLibrary
{
    public final static String NAMESPACE = "jakarta.faces.html";
    public final static String JCP_NAMESPACE = "http://xmlns.jcp.org/jsf/html";
    public final static String SUN_NAMESPACE = "http://java.sun.com/jsf/html";
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/html/HtmlLibrary.java`
#### Snippet
```java
    public final static String SUN_NAMESPACE = "http://java.sun.com/jsf/html";

    public final static HtmlLibrary INSTANCE = new HtmlLibrary();

    public HtmlLibrary()
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/html/HtmlLibrary.java`
#### Snippet
```java
    public final static String NAMESPACE = "jakarta.faces.html";
    public final static String JCP_NAMESPACE = "http://xmlns.jcp.org/jsf/html";
    public final static String SUN_NAMESPACE = "http://java.sun.com/jsf/html";

    public final static HtmlLibrary INSTANCE = new HtmlLibrary();
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/ResetValuesActionListenerHandler.java`
#### Snippet
```java
    }
    
    private final static class LiteralResetValuesActionListener implements ActionListener, Serializable
    {
        private static final long serialVersionUID = -9200000013153262119L;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/ResetValuesActionListenerHandler.java`
#### Snippet
```java
    }
    
    private final static class ResetValuesActionListener implements ActionListener, Serializable
    {
        private static final long serialVersionUID = -9200000013153262119L;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/AjaxHandler.java`
#### Snippet
```java
     * Wraps a method expression in a AjaxBehaviorListener
     */
    public final static class AjaxBehaviorListenerImpl implements AjaxBehaviorListener, PartialStateHolder
    {
        private MethodExpression _expr;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/AjaxHandler.java`
#### Snippet
```java
     * working this code for next views to be built.
     */
    public final static String FACES_JS_DYNAMICALLY_ADDED
            = "org.apache.myfaces.FACES_JS_DYNAMICALLY_ADDED";

```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/AjaxHandler.java`
#### Snippet
```java
{

    public final static Class<?>[] AJAX_BEHAVIOR_LISTENER_SIG = new Class<?>[] { AjaxBehaviorEvent.class };
    
    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/RetargetMethodExpressionRule.java`
#### Snippet
```java
    }

    public final static RetargetMethodExpressionRule INSTANCE = new RetargetMethodExpressionRule();

    public RetargetMethodExpressionRule()
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/RetargetMethodExpressionRule.java`
#### Snippet
```java
final class RetargetMethodExpressionRule extends MetaRule
{
    final static class RetargetValueExpressionMapper extends Metadata
    {
        private final TagAttribute _attr;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeLibrary.java`
#### Snippet
```java
public class CompositeLibrary extends AbstractTagLibrary
{
    public final static String NAMESPACE = "jakarta.faces.composite";
    public final static String JCP_NAMESPACE = "http://xmlns.jcp.org/jsf/composite";
    public final static String SUN_NAMESPACE = "http://java.sun.com/jsf/composite";
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeLibrary.java`
#### Snippet
```java
    public final static String NAMESPACE = "jakarta.faces.composite";
    public final static String JCP_NAMESPACE = "http://xmlns.jcp.org/jsf/composite";
    public final static String SUN_NAMESPACE = "http://java.sun.com/jsf/composite";
    
    public CompositeLibrary()
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeLibrary.java`
#### Snippet
```java
{
    public final static String NAMESPACE = "jakarta.faces.composite";
    public final static String JCP_NAMESPACE = "http://xmlns.jcp.org/jsf/composite";
    public final static String SUN_NAMESPACE = "http://java.sun.com/jsf/composite";
    
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/ImplementationHandler.java`
#### Snippet
```java
    private static final Logger log = Logger.getLogger(ImplementationHandler.class.getName());
    
    public final static String NAME = "implementation";

    public ImplementationHandler(TagConfig config)
```

### MissortedModifiers
Missorted modifiers `static private`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/html/DefaultTagDecorator.java`
#### Snippet
```java
    };
    
    static private final Object[] L_NAMES = new Object[]
    {
      "label", new Object[]{new TagSelectorImpl(null, "h:outputLabel")},
```

### MissortedModifiers
Missorted modifiers `static private`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/html/DefaultTagDecorator.java`
#### Snippet
```java
    static private final Object[][] LOCAL_NAME_ARR = new Object[256][];
    
    static private final Object[] A_NAMES = new Object[]
    {
      "a", new Object[]
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/html/DefaultTagDecorator.java`
#### Snippet
```java
public class DefaultTagDecorator implements TagDecorator
{
    public final static String XHTML_NAMESPACE = "http://www.w3.org/1999/xhtml";
    private final static String EMPTY_NAMESPACE = "";
    
```

### MissortedModifiers
Missorted modifiers `static private`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/html/DefaultTagDecorator.java`
#### Snippet
```java
     * Fast array for lookup of local names to be inspected. 
     */
    static private final Object[][] LOCAL_NAME_ARR = new Object[256][];
    
    static private final Object[] A_NAMES = new Object[]
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/html/DefaultTagDecorator.java`
#### Snippet
```java
{
    public final static String XHTML_NAMESPACE = "http://www.w3.org/1999/xhtml";
    private final static String EMPTY_NAMESPACE = "";
    
    private final static String P_ELEMENTNAME = "p:" + Renderer.PASSTHROUGH_RENDERER_LOCALNAME_KEY;
```

### MissortedModifiers
Missorted modifiers `static private`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/html/DefaultTagDecorator.java`
#### Snippet
```java
    };
    
    static private final Object[] H_NAMES = new Object[]
    {
      "head", new Object[]{new TagSelectorImpl(null, "h:head")}
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/html/DefaultTagDecorator.java`
#### Snippet
```java
    private final static String EMPTY_NAMESPACE = "";
    
    private final static String P_ELEMENTNAME = "p:" + Renderer.PASSTHROUGH_RENDERER_LOCALNAME_KEY;
    
    /**
```

### MissortedModifiers
Missorted modifiers `static private`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/html/DefaultTagDecorator.java`
#### Snippet
```java
    };
    
    static private final Object[] F_NAMES = new Object[]
    {
      "form", new Object[]{new TagSelectorImpl(null, "h:form")}
```

### MissortedModifiers
Missorted modifiers `static private`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/html/DefaultTagDecorator.java`
#### Snippet
```java
    };

    static private final Object[] S_NAMES = new Object[]
    {
      "script", new Object[]{new TagSelectorImpl(null, "h:outputScript")},
```

### MissortedModifiers
Missorted modifiers `static private`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/html/DefaultTagDecorator.java`
#### Snippet
```java
    };

    static private final Object[] B_NAMES = new Object[]
    {
      "body",   new Object[]{new TagSelectorImpl(null, "h:body")},
```

### MissortedModifiers
Missorted modifiers `static private`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/html/DefaultTagDecorator.java`
#### Snippet
```java
    };
    
    static private final Object[] T_NAMES = new Object[]
    {
      "textarea", new Object[]{new TagSelectorImpl(null, "h:inputTextarea")}
```

### MissortedModifiers
Missorted modifiers `static private`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/html/DefaultTagDecorator.java`
#### Snippet
```java
    };
    
    static private final Object[] I_NAMES = new Object[]
    {
      "img", new Object[]{new TagSelectorImpl(null, "h:graphicImage")},
```

### MissortedModifiers
Missorted modifiers `static abstract`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/html/DefaultTagDecorator.java`
#### Snippet
```java
    }
    
    private static abstract class TagSelector
    {
        public abstract TagDecoratorExecutor getExecutorIfApplies(Tag tag);
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeMetaRulesetImpl.java`
#### Snippet
```java
public class CompositeMetaRulesetImpl extends MetaRuleset
{
    private final static Logger log = Logger.getLogger(CompositeMetadataTargetImpl.class.getName());
    
    private static final String METADATA_KEY = CompositeMetaRulesetImpl.class.getName() + ".METADATA";
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeComponentRule.java`
#### Snippet
```java
    private final static Logger log = Logger.getLogger(CompositeComponentRule.class.getName());

    public final static CompositeComponentRule INSTANCE = new CompositeComponentRule();

    public CompositeComponentRule()
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeComponentRule.java`
#### Snippet
```java
    }

    final static class ValueExpressionMetadata extends Metadata
    {
        private final String _name;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeComponentRule.java`
#### Snippet
```java
    }

    private final static Logger log = Logger.getLogger(CompositeComponentRule.class.getName());

    public final static CompositeComponentRule INSTANCE = new CompositeComponentRule();
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/InterfaceHandler.java`
#### Snippet
```java
    private static final Logger log = Logger.getLogger(InterfaceHandler.class.getName());
    
    public final static String NAME = "interface";
    
    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeResourceLibrary.java`
#### Snippet
```java
    public final static String NAMESPACE_PREFIX = "jakarta.faces.composite/"; // Forward slash added under MYFACES-4484
    public final static String JCP_NAMESPACE_PREFIX = "http://xmlns.jcp.org/jsf/composite/";
    public final static String SUN_NAMESPACE_PREFIX = "http://java.sun.com/jsf/composite/";
    
    private final ResourceHandler _resourceHandler;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeResourceLibrary.java`
#### Snippet
```java
{
    public final static String NAMESPACE_PREFIX = "jakarta.faces.composite/"; // Forward slash added under MYFACES-4484
    public final static String JCP_NAMESPACE_PREFIX = "http://xmlns.jcp.org/jsf/composite/";
    public final static String SUN_NAMESPACE_PREFIX = "http://java.sun.com/jsf/composite/";
    
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeResourceLibrary.java`
#### Snippet
```java
public class CompositeResourceLibrary implements TagLibrary
{
    public final static String NAMESPACE_PREFIX = "jakarta.faces.composite/"; // Forward slash added under MYFACES-4484
    public final static String JCP_NAMESPACE_PREFIX = "http://xmlns.jcp.org/jsf/composite/";
    public final static String SUN_NAMESPACE_PREFIX = "http://java.sun.com/jsf/composite/";
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFaceletContext.java`
#### Snippet
```java
    }
    
    private final static class CompositeComponentTemplateManager extends TemplateManager implements TemplateClient
    {
        private final AbstractFacelet _owner;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/TemplateContextImpl.java`
#### Snippet
```java
    }
    
    private final static class TemplateManagerImpl extends TemplateManager implements TemplateClient
    {
        private final AbstractFacelet _owner;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFacelet.java`
#### Snippet
```java
    private static final Logger log = Logger.getLogger(DefaultFacelet.class.getName());

    private final static String APPLIED_KEY = "org.apache.myfaces.view.facelets.APPLIED";

    private final String _alias;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/CompilationUnit.java`
#### Snippet
```java
{

    protected final static FaceletHandler LEAF = new FaceletHandler()
    {
        @Override
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/NamespaceManager.java`
#### Snippet
```java
{

    private final static class NS
    {
        public final String prefix;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TagLibraryConfig.java`
#### Snippet
```java
public final class TagLibraryConfig
{
    protected final static Logger log = Logger.getLogger(TagLibraryConfig.class.getName());

    private static class TagLibraryImpl extends AbstractTagLibrary
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/CompilationManager.java`
#### Snippet
```java
{

    private final static Logger log = Logger.getLogger(CompilationManager.class.getName());

    private final Compiler compiler;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/Compiler.java`
#### Snippet
```java
    protected final static Logger log = Logger.getLogger(Compiler.class.getName());

    public final static String EXPRESSION_FACTORY = "compiler.ExpressionFactory";

    private static final TagLibrary EMPTY_LIBRARY = new CompositeTagLibrary(new TagLibrary[0]);
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/Compiler.java`
#### Snippet
```java
{

    protected final static Logger log = Logger.getLogger(Compiler.class.getName());

    public final static String EXPRESSION_FACTORY = "compiler.ExpressionFactory";
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/LiteralXMLInstruction.java`
#### Snippet
```java
{

    private final static char[] STOP = new char[0];

    private final char[] instruction;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/XMLInstruction.java`
#### Snippet
```java
public class XMLInstruction implements Instruction
{
    private final static char[] STOP = new char[0];

    private final ELText _text;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TextUnit.java`
#### Snippet
```java
     * @return 
     */
    final static int compressSpaces(List<Instruction> instructionBuffer, int size)
    {
        boolean addleftspace = true;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TextUnit.java`
#### Snippet
```java
    }
    
    final static String compressELText(String text)
    {
        //int firstCharLocation = getFirstTextCharLocationIgnoringSpacesTabsAndCarriageReturn(text);
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TextUnit.java`
#### Snippet
```java
    }

    private final static String trimRight(String s)
    {
        int i = s.length() - 1;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/config/ConfigFilesXmlValidationUtils.java`
#### Snippet
```java
    private final static String FACES_CONFIG_SCHEMA_PATH_30 = "org/apache/myfaces/resource/web-facesconfig_3_0.xsd";
    private final static String FACES_CONFIG_SCHEMA_PATH_40 = "org/apache/myfaces/resource/web-facesconfig_4_0.xsd";
    private final static String FACES_TAGLIB_SCHEMA_PATH = "org/apache/myfaces/resource/web-facelettaglibrary_2_0.xsd";

    public static class LSInputImpl implements LSInput
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/config/ConfigFilesXmlValidationUtils.java`
#### Snippet
```java
    private final static String FACES_CONFIG_SCHEMA_PATH_23 = "org/apache/myfaces/resource/web-facesconfig_2_3.xsd";
    private final static String FACES_CONFIG_SCHEMA_PATH_30 = "org/apache/myfaces/resource/web-facesconfig_3_0.xsd";
    private final static String FACES_CONFIG_SCHEMA_PATH_40 = "org/apache/myfaces/resource/web-facesconfig_4_0.xsd";
    private final static String FACES_TAGLIB_SCHEMA_PATH = "org/apache/myfaces/resource/web-facelettaglibrary_2_0.xsd";

```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/config/ConfigFilesXmlValidationUtils.java`
#### Snippet
```java
{
    public final static LSResourceResolver LS_RESOURCE_RESOLVER = new ValidatorLSResourceResolver();
    public final static ErrorHandler VALIDATION_ERROR_HANDLER = new ValidationErrorHandler();

    private final static Logger log = Logger.getLogger(ConfigFilesXmlValidationUtils.class.getName());
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/config/ConfigFilesXmlValidationUtils.java`
#### Snippet
```java
    private final static Logger log = Logger.getLogger(ConfigFilesXmlValidationUtils.class.getName());

    private final static String FACES_CONFIG_SCHEMA_PATH_12 = "org/apache/myfaces/resource/web-facesconfig_1_2.xsd";
    private final static String FACES_CONFIG_SCHEMA_PATH_20 = "org/apache/myfaces/resource/web-facesconfig_2_0.xsd";
    private final static String FACES_CONFIG_SCHEMA_PATH_21 = "org/apache/myfaces/resource/web-facesconfig_2_1.xsd";
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/config/ConfigFilesXmlValidationUtils.java`
#### Snippet
```java
    private final static String FACES_CONFIG_SCHEMA_PATH_21 = "org/apache/myfaces/resource/web-facesconfig_2_1.xsd";
    private final static String FACES_CONFIG_SCHEMA_PATH_22 = "org/apache/myfaces/resource/web-facesconfig_2_2.xsd";
    private final static String FACES_CONFIG_SCHEMA_PATH_23 = "org/apache/myfaces/resource/web-facesconfig_2_3.xsd";
    private final static String FACES_CONFIG_SCHEMA_PATH_30 = "org/apache/myfaces/resource/web-facesconfig_3_0.xsd";
    private final static String FACES_CONFIG_SCHEMA_PATH_40 = "org/apache/myfaces/resource/web-facesconfig_4_0.xsd";
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/config/ConfigFilesXmlValidationUtils.java`
#### Snippet
```java
    private final static String FACES_CONFIG_SCHEMA_PATH_20 = "org/apache/myfaces/resource/web-facesconfig_2_0.xsd";
    private final static String FACES_CONFIG_SCHEMA_PATH_21 = "org/apache/myfaces/resource/web-facesconfig_2_1.xsd";
    private final static String FACES_CONFIG_SCHEMA_PATH_22 = "org/apache/myfaces/resource/web-facesconfig_2_2.xsd";
    private final static String FACES_CONFIG_SCHEMA_PATH_23 = "org/apache/myfaces/resource/web-facesconfig_2_3.xsd";
    private final static String FACES_CONFIG_SCHEMA_PATH_30 = "org/apache/myfaces/resource/web-facesconfig_3_0.xsd";
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/config/ConfigFilesXmlValidationUtils.java`
#### Snippet
```java
    private final static String FACES_CONFIG_SCHEMA_PATH_12 = "org/apache/myfaces/resource/web-facesconfig_1_2.xsd";
    private final static String FACES_CONFIG_SCHEMA_PATH_20 = "org/apache/myfaces/resource/web-facesconfig_2_0.xsd";
    private final static String FACES_CONFIG_SCHEMA_PATH_21 = "org/apache/myfaces/resource/web-facesconfig_2_1.xsd";
    private final static String FACES_CONFIG_SCHEMA_PATH_22 = "org/apache/myfaces/resource/web-facesconfig_2_2.xsd";
    private final static String FACES_CONFIG_SCHEMA_PATH_23 = "org/apache/myfaces/resource/web-facesconfig_2_3.xsd";
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/config/ConfigFilesXmlValidationUtils.java`
#### Snippet
```java
    public final static ErrorHandler VALIDATION_ERROR_HANDLER = new ValidationErrorHandler();

    private final static Logger log = Logger.getLogger(ConfigFilesXmlValidationUtils.class.getName());

    private final static String FACES_CONFIG_SCHEMA_PATH_12 = "org/apache/myfaces/resource/web-facesconfig_1_2.xsd";
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/config/ConfigFilesXmlValidationUtils.java`
#### Snippet
```java
public class ConfigFilesXmlValidationUtils
{
    public final static LSResourceResolver LS_RESOURCE_RESOLVER = new ValidatorLSResourceResolver();
    public final static ErrorHandler VALIDATION_ERROR_HANDLER = new ValidationErrorHandler();

```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/config/ConfigFilesXmlValidationUtils.java`
#### Snippet
```java
    private final static String FACES_CONFIG_SCHEMA_PATH_22 = "org/apache/myfaces/resource/web-facesconfig_2_2.xsd";
    private final static String FACES_CONFIG_SCHEMA_PATH_23 = "org/apache/myfaces/resource/web-facesconfig_2_3.xsd";
    private final static String FACES_CONFIG_SCHEMA_PATH_30 = "org/apache/myfaces/resource/web-facesconfig_3_0.xsd";
    private final static String FACES_CONFIG_SCHEMA_PATH_40 = "org/apache/myfaces/resource/web-facesconfig_4_0.xsd";
    private final static String FACES_TAGLIB_SCHEMA_PATH = "org/apache/myfaces/resource/web-facelettaglibrary_2_0.xsd";
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/config/ConfigFilesXmlValidationUtils.java`
#### Snippet
```java

    private final static String FACES_CONFIG_SCHEMA_PATH_12 = "org/apache/myfaces/resource/web-facesconfig_1_2.xsd";
    private final static String FACES_CONFIG_SCHEMA_PATH_20 = "org/apache/myfaces/resource/web-facesconfig_2_0.xsd";
    private final static String FACES_CONFIG_SCHEMA_PATH_21 = "org/apache/myfaces/resource/web-facesconfig_2_1.xsd";
    private final static String FACES_CONFIG_SCHEMA_PATH_22 = "org/apache/myfaces/resource/web-facesconfig_2_2.xsd";
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/SAXCompiler.java`
#### Snippet
```java
     * see https://issues.apache.org/jira/browse/MYFACES-4281
     */
    private final static List<String> SKIPPED_NAMESPACES = Arrays.asList("http://www.w3.org/1998/Math/MathML",
            "http://www.w3.org/2000/svg","http://www.w3.org/1999/xlink");

```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/SAXCompiler.java`
#### Snippet
```java
{

    private final static Pattern XML_DECLARATION = Pattern
            .compile("^<\\?xml.+?version=['\"](.+?)['\"](.+?encoding=['\"]((.+?))['\"])?.*?\\?>");

```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/component/UIRepeat.java`
#### Snippet
```java
    private static final String STRING_BUILDER_KEY = UIRepeat.class.getName() + ".SHARED_STRING_BUILDER";

    private final static DataModel<?> EMPTY_MODEL = new ListDataModel<Object>(Collections.emptyList());
    
    private static final Class<Object[]> OBJECT_ARRAY_CLASS = Object[].class;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/view/facelets/component/UIRepeat.java`
#### Snippet
```java

    // from RI
    private final static class SavedState implements Serializable
    {
        private boolean _localValueSet;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/config/webparameters/MyfacesConfig.java`
#### Snippet
```java
    public static final String STRICT_JSF_2_ORIGIN_HEADER_APP_PATH = 
            "org.apache.myfaces.STRICT_JSF_2_ORIGIN_HEADER_APP_PATH";
    private final static boolean STRICT_JSF_2_ORIGIN_HEADER_APP_PATH_DEFAULT = false;

    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/config/webparameters/MyfacesConfig.java`
#### Snippet
```java
    public final static String BEAN_BEFORE_JSF_VALIDATION
            = "org.apache.myfaces.validator.BEAN_BEFORE_JSF_VALIDATION";
    private final static boolean BEAN_BEFORE_JSF_VALIDATION_DEFAULT = false;

    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/config/webparameters/MyfacesConfig.java`
#### Snippet
```java
    @JSFWebConfigParam(since="2.0.11,2.1.5", expectedValues="text/html, application/xhtml+xml", 
            defaultValue="text/html", group="render")
    public final static String DEFAULT_RESPONSE_WRITER_CONTENT_TYPE_MODE = 
        "org.apache.myfaces.DEFAULT_RESPONSE_WRITER_CONTENT_TYPE_MODE";
    private final static String DEFAULT_RESPONSE_WRITER_CONTENT_TYPE_MODE_DEFAULT = "text/html";
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/config/webparameters/MyfacesConfig.java`
#### Snippet
```java
    public final static String DEFAULT_RESPONSE_WRITER_CONTENT_TYPE_MODE = 
        "org.apache.myfaces.DEFAULT_RESPONSE_WRITER_CONTENT_TYPE_MODE";
    private final static String DEFAULT_RESPONSE_WRITER_CONTENT_TYPE_MODE_DEFAULT = "text/html";

    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/config/webparameters/MyfacesConfig.java`
#### Snippet
```java
    @JSFWebConfigParam(since="2.0.1", expectedValues="true, false, on, off, yes, no", defaultValue="false",
            ignoreUpperLowerCase=true, group="render")
    public final static String WRAP_SCRIPT_CONTENT_WITH_XML_COMMENT_TAG = 
        "org.apache.myfaces.WRAP_SCRIPT_CONTENT_WITH_XML_COMMENT_TAG";
    private final static boolean WRAP_SCRIPT_CONTENT_WITH_XML_COMMENT_TAG_DEFAULT = false;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/config/webparameters/MyfacesConfig.java`
#### Snippet
```java
    @JSFWebConfigParam(since="2.0", defaultValue="auto", expectedValues="true,false,auto", tags="performance", 
            ignoreUpperLowerCase=true, group="state")
    public final static String REFRESH_TRANSIENT_BUILD_ON_PSS = 
        "org.apache.myfaces.REFRESH_TRANSIENT_BUILD_ON_PSS"; 
    private final static String REFRESH_TRANSIENT_BUILD_ON_PSS_DEFAULT = "auto";
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/config/webparameters/MyfacesConfig.java`
#### Snippet
```java
    public final static String WRAP_SCRIPT_CONTENT_WITH_XML_COMMENT_TAG = 
        "org.apache.myfaces.WRAP_SCRIPT_CONTENT_WITH_XML_COMMENT_TAG";
    private final static boolean WRAP_SCRIPT_CONTENT_WITH_XML_COMMENT_TAG_DEFAULT = false;
    
    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/config/webparameters/MyfacesConfig.java`
#### Snippet
```java
    public static final String ALWAYS_FORCE_SESSION_CREATION = 
            "org.apache.myfaces.ALWAYS_FORCE_SESSION_CREATION";
    protected final static boolean ALWAYS_FORCE_SESSION_CREATION_DEFAULT = false;
    
    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/config/webparameters/MyfacesConfig.java`
#### Snippet
```java
    public static final String RENDER_FORM_VIEW_STATE_AT_BEGIN =
            "org.apache.myfaces.RENDER_FORM_VIEW_STATE_AT_BEGIN";
    private final static boolean RENDER_FORM_VIEW_STATE_AT_BEGIN_DEFAULT = false;
    
    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/config/webparameters/MyfacesConfig.java`
#### Snippet
```java
    @JSFWebConfigParam(since="2.0", expectedValues="true, false, on, off, yes, no", ignoreUpperLowerCase=true)
    public final static String VALIDATE_XML = "org.apache.myfaces.VALIDATE_XML";
    private final static boolean VALIDATE_XML_DEFAULT = false;
    
    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/config/webparameters/MyfacesConfig.java`
#### Snippet
```java
    
    @JSFWebConfigParam(since = "2.1", defaultValue = "false", expectedValues = "true, false", tags = "performance")
    public final static String MARK_INITIAL_STATE_WHEN_APPLY_BUILD_VIEW
            = "org.apache.myfaces.MARK_INITIAL_STATE_WHEN_APPLY_BUILD_VIEW";
    private static final boolean MARK_INITIAL_STATE_WHEN_APPLY_BUILD_VIEW_DEFAULT = false;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/config/webparameters/MyfacesConfig.java`
#### Snippet
```java
    public static final String STRICT_JSF_2_FACELETS_COMPATIBILITY = 
            "org.apache.myfaces.STRICT_JSF_2_FACELETS_COMPATIBILITY";
    private final static boolean STRICT_JSF_2_FACELETS_COMPATIBILITY_DEFAULT = false;    
    
    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/config/webparameters/MyfacesConfig.java`
#### Snippet
```java
    @JSFWebConfigParam(since="2.2.9", defaultValue="false", expectedValues="true,false", group="EL")
    public static final String SUPPORT_EL_3_IMPORT_HANDLER = "org.apache.myfaces.SUPPORT_EL_3_IMPORT_HANDLER";
    private final static boolean SUPPORT_EL_3_IMPORT_HANDLER_DEFAULT = false;

    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/config/webparameters/MyfacesConfig.java`
#### Snippet
```java
     */
    @JSFWebConfigParam(defaultValue="false", expectedValues="true, false", since = "2.2.10", group="validation")
    public final static String BEAN_BEFORE_JSF_VALIDATION
            = "org.apache.myfaces.validator.BEAN_BEFORE_JSF_VALIDATION";
    private final static boolean BEAN_BEFORE_JSF_VALIDATION_DEFAULT = false;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/config/webparameters/MyfacesConfig.java`
#### Snippet
```java
     */
    @JSFWebConfigParam(since="2.0", expectedValues="true, false, on, off, yes, no", ignoreUpperLowerCase=true)
    public final static String VALIDATE_XML = "org.apache.myfaces.VALIDATE_XML";
    private final static boolean VALIDATE_XML_DEFAULT = false;
    
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/config/webparameters/MyfacesConfig.java`
#### Snippet
```java
     */
    @JSFWebConfigParam(since="2.0.8")
    public final static String DEBUG_PHASE_LISTENER = "org.apache.myfaces.DEBUG_PHASE_LISTENER";
    private final static boolean DEBUG_PHASE_LISTENER_DEFAULT = false;
    
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/config/webparameters/MyfacesConfig.java`
#### Snippet
```java
    @JSFWebConfigParam(since="2.0", defaultValue="false", expectedValues="true, false, on, off, yes, no", 
            tags="performance", ignoreUpperLowerCase=true, group="state")
    public final static String REFRESH_TRANSIENT_BUILD_ON_PSS_PRESERVE_STATE = 
        "org.apache.myfaces.REFRESH_TRANSIENT_BUILD_ON_PSS_PRESERVE_STATE";
    private final static boolean REFRESH_TRANSIENT_BUILD_ON_PSS_PRESERVE_STATE_DEFAULT = false;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/config/webparameters/MyfacesConfig.java`
#### Snippet
```java
     */
    @JSFWebConfigParam(since="2.0.10", expectedValues="true, false", defaultValue="false", group="EL")
    public final static String STRICT_JSF_2_CC_EL_RESOLVER = 
        "org.apache.myfaces.STRICT_JSF_2_CC_EL_RESOLVER";
    private final static boolean STRICT_JSF_2_CC_EL_RESOLVER_DEFAULT = false;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/config/webparameters/MyfacesConfig.java`
#### Snippet
```java
    public final static String REFRESH_TRANSIENT_BUILD_ON_PSS = 
        "org.apache.myfaces.REFRESH_TRANSIENT_BUILD_ON_PSS"; 
    private final static String REFRESH_TRANSIENT_BUILD_ON_PSS_DEFAULT = "auto";

    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/config/webparameters/MyfacesConfig.java`
#### Snippet
```java
    public final static String STRICT_JSF_2_CC_EL_RESOLVER = 
        "org.apache.myfaces.STRICT_JSF_2_CC_EL_RESOLVER";
    private final static boolean STRICT_JSF_2_CC_EL_RESOLVER_DEFAULT = false;
    
    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/config/webparameters/MyfacesConfig.java`
#### Snippet
```java
    public final static String REFRESH_TRANSIENT_BUILD_ON_PSS_PRESERVE_STATE = 
        "org.apache.myfaces.REFRESH_TRANSIENT_BUILD_ON_PSS_PRESERVE_STATE";
    private final static boolean REFRESH_TRANSIENT_BUILD_ON_PSS_PRESERVE_STATE_DEFAULT = false;
    
    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/config/webparameters/MyfacesConfig.java`
#### Snippet
```java
    public static final String STRICT_JSF_2_VIEW_NOT_FOUND = 
            "org.apache.myfaces.STRICT_JSF_2_VIEW_NOT_FOUND";
    private final static boolean STRICT_JSF_2_VIEW_NOT_FOUND_DEFAULT = false;

    @JSFWebConfigParam(defaultValue = "false", since = "2.2.0", expectedValues="true, false", group="render",
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/config/webparameters/MyfacesConfig.java`
#### Snippet
```java
    @JSFWebConfigParam(since="2.0.8")
    public final static String DEBUG_PHASE_LISTENER = "org.apache.myfaces.DEBUG_PHASE_LISTENER";
    private final static boolean DEBUG_PHASE_LISTENER_DEFAULT = false;
    
    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/config/annotation/AnnotationConfigurator.java`
#### Snippet
```java

    /* Added under MYFACES-4493 for Namespace Backward Compatibility */
    private final static String JCP_NAMESPACE_FACES_COMPONENT = "http://xmlns.jcp.org/jsf/component";
    
    public AnnotationConfigurator()
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/resource/ResourceImpl.java`
#### Snippet
```java
{
    protected final static String JAKARTA_FACES_LIBRARY_NAME = "jakarta.faces";
    protected final static String FACES_JS_RESOURCE_NAME = "faces.js";


```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/resource/ResourceImpl.java`
#### Snippet
```java
public class ResourceImpl extends Resource implements ContractResource
{
    protected final static String JAKARTA_FACES_LIBRARY_NAME = "jakarta.faces";
    protected final static String FACES_JS_RESOURCE_NAME = "faces.js";

```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/resource/TempDirFileCacheResourceLoader.java`
#### Snippet
```java
    public final static boolean INIT_PARAM_TEMPORAL_RESOURCEHANDLER_CACHE_ENABLED_DEFAULT = false;
    
    public final static String TEMP_FILES_LOCK_MAP = "oam.rh.TEMP_FILES_LOCK_MAP";
    
    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/resource/TempDirFileCacheResourceLoader.java`
#### Snippet
```java
     */
    @JSFWebConfigParam(since="2.1.11", expectedValues="true, false", defaultValue="false")
    public final static String INIT_PARAM_TEMPORAL_RESOURCEHANDLER_CACHE_ENABLED = 
        "org.apache.myfaces.TEMPORAL_RESOURCEHANDLER_CACHE_ENABLED";
    public final static boolean INIT_PARAM_TEMPORAL_RESOURCEHANDLER_CACHE_ENABLED_DEFAULT = false;
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/resource/TempDirFileCacheResourceLoader.java`
#### Snippet
```java
    public final static String INIT_PARAM_TEMPORAL_RESOURCEHANDLER_CACHE_ENABLED = 
        "org.apache.myfaces.TEMPORAL_RESOURCEHANDLER_CACHE_ENABLED";
    public final static boolean INIT_PARAM_TEMPORAL_RESOURCEHANDLER_CACHE_ENABLED_DEFAULT = false;
    
    public final static String TEMP_FILES_LOCK_MAP = "oam.rh.TEMP_FILES_LOCK_MAP";
```

### MissortedModifiers
Missorted modifiers `static public`
in `impl/src/main/java/org/apache/myfaces/component/ComponentResourceContainer.java`
#### Snippet
```java
    static public final String COMPONENT_FAMILY =
        "jakarta.faces.Panel";
    static public final String COMPONENT_TYPE =
        "jakarta.faces.ComponentResourceContainer";

```

### MissortedModifiers
Missorted modifiers `static public`
in `impl/src/main/java/org/apache/myfaces/component/ComponentResourceContainer.java`
#### Snippet
```java
public class ComponentResourceContainer extends UIPanel
{
    static public final String COMPONENT_FAMILY =
        "jakarta.faces.Panel";
    static public final String COMPONENT_TYPE =
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/resource/TempDirFileCacheContractResourceLoader.java`
#### Snippet
```java
{
    
    public final static String TEMP_FILES_LOCK_MAP = "oam.rh.con.TEMP_FILES_LOCK_MAP";
    
    /**
```

### MissortedModifiers
Missorted modifiers `static public`
in `impl/src/main/java/org/apache/myfaces/component/validate/ValidateWholeBeanComponent.java`
#### Snippet
```java
    static public final String COMPONENT_FAMILY =
        "jakarta.faces.Input";
    static public final String COMPONENT_TYPE =
        "org.apache.myfaces.component.validate.ValidateWholeBean";

```

### MissortedModifiers
Missorted modifiers `static public`
in `impl/src/main/java/org/apache/myfaces/component/validate/ValidateWholeBeanComponent.java`
#### Snippet
```java
public class ValidateWholeBeanComponent extends UIInput
{
    static public final String COMPONENT_FAMILY =
        "jakarta.faces.Input";
    static public final String COMPONENT_TYPE =
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlHeadRenderer.java`
#### Snippet
```java
    private final static String PROFILE_ATTR = "profile";

    private final static String[] HEAD_PASSTHROUGH_ATTRIBUTES = { HTML.DIR_ATTR,
            HTML.LANG_ATTR, PROFILE_ATTR};

```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlHeadRenderer.java`
#### Snippet
```java
public class HtmlHeadRenderer extends Renderer
{
    private final static String PROFILE_ATTR = "profile";

    private final static String[] HEAD_PASSTHROUGH_ATTRIBUTES = { HTML.DIR_ATTR,
```

### MissortedModifiers
Missorted modifiers `synchronized private`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlRenderKitImpl.java`
#### Snippet
```java
     * @param renderer
     */
    synchronized private void _put(String componentFamily, String rendererType, Renderer renderer)
    {
        Map <String,Renderer> familyRendererMap = _renderers.get(componentFamily);
```

### MissortedModifiers
Missorted modifiers `static public`
in `impl/src/main/java/org/apache/myfaces/renderkit/RendererUtils.java`
#### Snippet
```java
     * Coerces an object into a resource URI, calling the view-handler.
     */
    static public String toResourceUri(FacesContext facesContext, Object o)
    {
        if (o == null)
```

### MissortedModifiers
Missorted modifiers `static private`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlResponseWriterImpl.java`
#### Snippet
```java
    };

    static private final String[] I_NAMES = new String[]
    {
      "img",
```

### MissortedModifiers
Missorted modifiers `static private`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlResponseWriterImpl.java`
#### Snippet
```java
    };

    static private final String[] L_NAMES = new String[]
    {
      "link",
```

### MissortedModifiers
Missorted modifiers `static private`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlResponseWriterImpl.java`
#### Snippet
```java
    };

    static private final String[] H_NAMES = new String[]
    {
      "hr",
```

### MissortedModifiers
Missorted modifiers `static private`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlResponseWriterImpl.java`
#### Snippet
```java
    };

    static private final String[] B_NAMES = new String[]
    {
      "br",
```

### MissortedModifiers
Missorted modifiers `static private`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlResponseWriterImpl.java`
#### Snippet
```java
    };

    static private final String[] P_NAMES = new String[]
    {
      "param",
```

### MissortedModifiers
Missorted modifiers `static private`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlResponseWriterImpl.java`
#### Snippet
```java
    static private final String[][] EMPTY_ELEMENT_ARR = new String[256][];

    static private final String[] A_NAMES = new String[]
    {
      "area",
```

### MissortedModifiers
Missorted modifiers `static private`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlResponseWriterImpl.java`
#### Snippet
```java
    };

    static private final String[] F_NAMES = new String[]
    {
      "frame",
```

### MissortedModifiers
Missorted modifiers `static private`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlResponseWriterImpl.java`
#### Snippet
```java
    };

    static private final String[] M_NAMES = new String[]
    {
      "meta",
```

### MissortedModifiers
Missorted modifiers `static private`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlResponseWriterImpl.java`
#### Snippet
```java
    };

    static private final String[] E_NAMES = new String[]
    {
      "embed",
```

### MissortedModifiers
Missorted modifiers `static private`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlResponseWriterImpl.java`
#### Snippet
```java
    private static final String COMMENT_END = "\n-->";

    static private final String[][] EMPTY_ELEMENT_ARR = new String[256][];

    static private final String[] A_NAMES = new String[]
```

### MissortedModifiers
Missorted modifiers `static private`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlResponseWriterImpl.java`
#### Snippet
```java
    };

    static private final String[] C_NAMES = new String[]
    {
      "col",
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/ResourceUtils.java`
#### Snippet
```java
    public final static String FACES_MYFACES_JSFJS_I18N = "faces-i18n.js";

    private final static String RENDERED_FACES_JS = "org.apache.myfaces.RENDERED_FACES_JS";

    public static final String JAKARTA_FACES_OUTPUT_COMPONENT_TYPE = "jakarta.faces.Output";
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/ResourceUtils.java`
#### Snippet
```java
    
    public final static String FACES_UNCOMPRESSED_JS_RESOURCE_NAME = "faces-uncompressed.js";
    public final static String FACES_UNCOMPRESSED_FULL_JS_RESOURCE_NAME = "faces-uncompressed-full.js";
    public final static String FACES_MINIMAL_JS_RESOURCE_NAME = "faces-minimal.js";
    public final static String FACES_MYFACES_JSFJS_I18N = "faces-i18n.js";
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/ResourceUtils.java`
#### Snippet
```java
    public final static String JSF_MYFACES_JSFJS_NORMAL = "normal";
    
    public final static String FACES_UNCOMPRESSED_JS_RESOURCE_NAME = "faces-uncompressed.js";
    public final static String FACES_UNCOMPRESSED_FULL_JS_RESOURCE_NAME = "faces-uncompressed-full.js";
    public final static String FACES_MINIMAL_JS_RESOURCE_NAME = "faces-minimal.js";
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/ResourceUtils.java`
#### Snippet
```java
    public final static String MYFACES_LIBRARY_NAME = "org.apache.myfaces";

    public final static String JSF_MYFACES_JSFJS_MINIMAL = "minimal";
    public final static String JSF_MYFACES_JSFJS_NORMAL = "normal";
    
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/ResourceUtils.java`
#### Snippet
```java
    public final static String FACES_UNCOMPRESSED_FULL_JS_RESOURCE_NAME = "faces-uncompressed-full.js";
    public final static String FACES_MINIMAL_JS_RESOURCE_NAME = "faces-minimal.js";
    public final static String FACES_MYFACES_JSFJS_I18N = "faces-i18n.js";

    private final static String RENDERED_FACES_JS = "org.apache.myfaces.RENDERED_FACES_JS";
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/ResourceUtils.java`
#### Snippet
```java
public class ResourceUtils
{
    public final static String MYFACES_LIBRARY_NAME = "org.apache.myfaces";

    public final static String JSF_MYFACES_JSFJS_MINIMAL = "minimal";
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/ResourceUtils.java`
#### Snippet
```java
    public final static String FACES_UNCOMPRESSED_JS_RESOURCE_NAME = "faces-uncompressed.js";
    public final static String FACES_UNCOMPRESSED_FULL_JS_RESOURCE_NAME = "faces-uncompressed-full.js";
    public final static String FACES_MINIMAL_JS_RESOURCE_NAME = "faces-minimal.js";
    public final static String FACES_MYFACES_JSFJS_I18N = "faces-i18n.js";

```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/ResourceUtils.java`
#### Snippet
```java

    public final static String JSF_MYFACES_JSFJS_MINIMAL = "minimal";
    public final static String JSF_MYFACES_JSFJS_NORMAL = "normal";
    
    public final static String FACES_UNCOMPRESSED_JS_RESOURCE_NAME = "faces-uncompressed.js";
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/renderkit/ErrorPageWriter.java`
#### Snippet
```java
    private final static String[] IGNORE = new String[] { "parent", "rendererType" };

    private final static String[] ALWAYS_WRITE = new String[] { "class", "clientId" };

    /**
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/renderkit/ErrorPageWriter.java`
#### Snippet
```java
    private static final Pattern REGEX_PATTERN = Pattern.compile(".*?\\Q,Id:\\E\\s*(\\S+)\\s*\\].*?");

    private final static String[] IGNORE = new String[] { "parent", "rendererType" };

    private final static String[] ALWAYS_WRITE = new String[] { "class", "clientId" };
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/renderkit/ErrorPageWriter.java`
#### Snippet
```java
    private static final Logger log = Logger.getLogger(ErrorPageWriter.class.getName());

    private final static String TS = "&lt;";

    private static final String ERROR_TEMPLATE = "META-INF/rsc/myfaces-dev-error.xml";
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
    public static final Pattern RESOURCE_VERSION_CHECKER = Pattern.compile("\\p{Digit}+(_\\p{Digit}*)*\\..*");    
    
    public final static String RENDERED_RESOURCES_SET = "org.apache.myfaces.RENDERED_RESOURCES_SET";

    private static final String SHARED_STRING_BUILDER = ResourceHandlerImpl.class.getName() + ".SHARED_STRING_BUILDER";
```

### MissortedModifiers
Missorted modifiers `final static`
in `impl/src/main/java/org/apache/myfaces/application/ApplicationImpl.java`
#### Snippet
```java

    // the name for the system property which specifies the current ProjectStage (see MYFACES-2545 for details)
    public final static String PROJECT_STAGE_SYSTEM_PROPERTY_NAME = "faces.PROJECT_STAGE";

    /**
```

### MissortedModifiers
Missorted modifiers `abstract protected`
in `test/src/main/java/org/apache/myfaces/test/mock/_AbstractAttributeMap.java`
#### Snippet
```java
    abstract protected V getAttribute(String key);

    abstract protected void setAttribute(String key, V value);

    abstract protected void removeAttribute(String key);
```

### MissortedModifiers
Missorted modifiers `abstract protected`
in `test/src/main/java/org/apache/myfaces/test/mock/_AbstractAttributeMap.java`
#### Snippet
```java
    abstract protected void setAttribute(String key, V value);

    abstract protected void removeAttribute(String key);

    abstract protected Enumeration<String> getAttributeNames();
```

### MissortedModifiers
Missorted modifiers `abstract protected`
in `test/src/main/java/org/apache/myfaces/test/mock/_AbstractAttributeMap.java`
#### Snippet
```java
    abstract protected void removeAttribute(String key);

    abstract protected Enumeration<String> getAttributeNames();

    private abstract class AbstractAttributeSet<E> extends AbstractSet<E>
```

### MissortedModifiers
Missorted modifiers `abstract protected`
in `test/src/main/java/org/apache/myfaces/test/mock/_AbstractAttributeMap.java`
#### Snippet
```java
    }

    abstract protected V getAttribute(String key);

    abstract protected void setAttribute(String key, V value);
```

### MissortedModifiers
Missorted modifiers `static protected`
in `test/src/main/java/org/apache/myfaces/test/mock/MockFlash.java`
#### Snippet
```java
            + ".CURRENTMAP.KEY";

    static protected final String FLASH_POSTBACK_MAP_CACHE = MockFlash.class
            .getName()
            + ".POSTBACKMAP.CACHE";
```

### MissortedModifiers
Missorted modifiers `static protected`
in `test/src/main/java/org/apache/myfaces/test/mock/MockFlash.java`
#### Snippet
```java
     * Key on app map to keep current instance
     */
    static protected final String FLASH_INSTANCE = MockFlash.class.getName()
            + ".INSTANCE";

```

### MissortedModifiers
Missorted modifiers `static protected`
in `test/src/main/java/org/apache/myfaces/test/mock/MockFlash.java`
#### Snippet
```java
            + ".POSTBACKMAP.CACHE";

    static protected final String FLASH_POSTBACK_MAP_KEY = MockFlash.class
            .getName()
            + ".POSTBACKMAP.KEY";
```

### MissortedModifiers
Missorted modifiers `static protected`
in `test/src/main/java/org/apache/myfaces/test/mock/MockFlash.java`
#### Snippet
```java
     * Key used to check if there is the current request will be or was redirected
     */
    static protected final String FLASH_REDIRECT = MockFlash.class.getName()
            + ".REDIRECT";

```

### MissortedModifiers
Missorted modifiers `static protected`
in `test/src/main/java/org/apache/myfaces/test/mock/MockFlash.java`
#### Snippet
```java
            + ".SCOPE";

    static protected final String FLASH_CURRENT_MAP_CACHE = MockFlash.class
            .getName()
            + ".CURRENTMAP.CACHE";
```

### MissortedModifiers
Missorted modifiers `static protected`
in `test/src/main/java/org/apache/myfaces/test/mock/MockFlash.java`
#### Snippet
```java
            + ".CURRENTMAP.CACHE";

    static protected final String FLASH_CURRENT_MAP_KEY = MockFlash.class
            .getName()
            + ".CURRENTMAP.KEY";
```

### MissortedModifiers
Missorted modifiers `static protected`
in `test/src/main/java/org/apache/myfaces/test/mock/MockFlash.java`
#### Snippet
```java
            + ".KEEP_MESSAGES";

    static protected final String FLASH_KEEP_MESSAGES_LIST = "KEEPMESSAGESLIST";

    /**
```

### MissortedModifiers
Missorted modifiers `static protected`
in `test/src/main/java/org/apache/myfaces/test/mock/MockFlash.java`
#### Snippet
```java
     * Used when post-redirect-get pattern is used)
     */
    static protected final String FLASH_KEEP_MESSAGES = MockFlash.class
            .getName()
            + ".KEEP_MESSAGES";
```

### MissortedModifiers
Missorted modifiers `static private`
in `test/src/main/java/org/apache/myfaces/test/mock/MockFlash.java`
#### Snippet
```java
            + ".POSTBACKMAP.KEY";

    static private final char SEPARATOR_CHAR = '.';

    // the current token value
```

### MissortedModifiers
Missorted modifiers `static protected`
in `test/src/main/java/org/apache/myfaces/test/mock/MockFlash.java`
#### Snippet
```java
     * Session map prefix to flash maps
     */
    static protected final String FLASH_SCOPE_CACHE = MockFlash.class.getName()
            + ".SCOPE";

```

### MissortedModifiers
Missorted modifiers `static private`
in `test/src/main/java/org/apache/myfaces/test/mock/MockSubKeyMap.java`
#### Snippet
```java
    }

    static private boolean _equals(Object a, Object b)
    {
        if (a == null)
```

## RuleId[ruleID=AnonymousHasLambdaAlternative]
### AnonymousHasLambdaAlternative
Anonymous new ThreadLocal() can be replaced with ThreadLocal.withInitial()
in `impl/src/main/java/org/apache/myfaces/util/lang/ThreadsafeXorShiftRandom.java`
#### Snippet
```java
    }

    private static final ThreadLocal<XorShiftRandom> randomThreadLocal = new ThreadLocal<XorShiftRandom>()
    {
        @Override
        protected XorShiftRandom initialValue()
```

### AnonymousHasLambdaAlternative
Anonymous new Thread() can be replaced with new Thread(() -\> {...})
in `impl/src/main/java/org/apache/myfaces/util/lang/ConcurrentLRUCache.java`
#### Snippet
```java
            if (newThreadForCleanup)
            {
                new Thread()
                {
                    @Override
                    public void run()
```

## RuleId[ruleID=IfStatementMissingBreakInLoop]
### IfStatementMissingBreakInLoop
Loop can be terminated after condition is met
in `impl/src/main/java/org/apache/myfaces/component/search/SearchExpressionHandlerImpl.java`
#### Snippet
```java
                for (char separator : separators)
                {
                    if (c == separator)
                    {
                        isSeparator = true;
```

### IfStatementMissingBreakInLoop
Loop can be terminated after condition is met
in `impl/src/main/java/org/apache/myfaces/application/viewstate/StateUtils.java`
#### Snippet
```java
            for (int i = 0; i < signedDigestHash.length; i++)
            {
                if (signedDigestHash[i] != secure[secure.length - macLenght + i])
                {
                    isMacEqual = false;
```

## RuleId[ruleID=InstanceofIncompatibleInterface]
### InstanceofIncompatibleInterface
'instanceof' incompatible interface `FacesWrapper`
in `impl/src/main/java/org/apache/myfaces/flow/FlowHandlerImpl.java`
#### Snippet
```java
                break;
            }
            else if (flowHandler instanceof FacesWrapper)
            {
                flowHandler = ((FacesWrapper<FlowHandler>)flowHandler).getWrapped();
```

### InstanceofIncompatibleInterface
'instanceof' incompatible interface `FacesWrapper`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ComponentTagHandlerDelegate.java`
#### Snippet
```java
                found = true;
            }
            else if (handler instanceof FacesWrapper)
            {
                handler = ((FacesWrapper<? extends ComponentHandler>)handler).getWrapped();
```

### InstanceofIncompatibleInterface
'instanceof' incompatible interface `FacesWrapper`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ComponentTagHandlerDelegate.java`
#### Snippet
```java
                found = true;
            }
            else if (handler instanceof FacesWrapper)
            {
                handler = ((FacesWrapper<? extends ComponentHandler>)handler).getWrapped();
```

### InstanceofIncompatibleInterface
'instanceof' incompatible interface `ClientBehaviorHolder`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlFormRendererBase.java`
#### Snippet
```java
        writer.writeURIAttribute(HTML.ACTION_ATTR, encodedActionURL, null);
        
        if (htmlForm instanceof ClientBehaviorHolder)
        {
            behaviors = ((ClientBehaviorHolder) htmlForm).getClientBehaviors();
```

### InstanceofIncompatibleInterface
'instanceof' incompatible interface `ClientBehaviorHolder`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlCheckboxRendererBase.java`
#### Snippet
```java
        
        Map<String, List<ClientBehavior>> behaviors = null;
        if (selectMany instanceof ClientBehaviorHolder)
        {
            behaviors = ((ClientBehaviorHolder) selectMany).getClientBehaviors();
```

### InstanceofIncompatibleInterface
'instanceof' incompatible interface `ClientBehaviorHolder`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlLinkRendererBase.java`
#### Snippet
```java
            facesContext.getAttributes().put(END_LINK_OUTCOME_AS_SPAN, Boolean.TRUE);
            writer.startElement(HTML.SPAN_ELEM, output);
            if (output instanceof ClientBehaviorHolder)
            {
                behaviors = ((ClientBehaviorHolder) output).getClientBehaviors();
```

### InstanceofIncompatibleInterface
'instanceof' incompatible interface `ClientBehaviorHolder`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlLinkRendererBase.java`
#### Snippet
```java
            writer.startElement(HTML.ANCHOR_ELEM, output);
            writer.writeURIAttribute(HTML.HREF_ATTR, targetHref, null);
            if (output instanceof ClientBehaviorHolder)
            {
                behaviors = ((ClientBehaviorHolder) output).getClientBehaviors();
```

### InstanceofIncompatibleInterface
'instanceof' incompatible interface `ClientBehaviorHolder`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlRadioRendererBase.java`
#### Snippet
```java
        
        Map<String, List<ClientBehavior>> behaviors = null;
        if (uiComponent instanceof ClientBehaviorHolder)
        {
            behaviors = ((ClientBehaviorHolder) uiComponent).getClientBehaviors();
```

### InstanceofIncompatibleInterface
'instanceof' incompatible interface `FacesWrapper`
in `impl/src/main/java/org/apache/myfaces/application/viewstate/StateCacheUtils.java`
#### Snippet
```java
                    return (MyfacesResponseStateManager) rsm1;
                }
                if (rsm1 instanceof FacesWrapper)
                {
                    rsm1 = ((FacesWrapper<? extends ResponseStateManager>) rsm1).getWrapped();
```

### InstanceofIncompatibleInterface
'instanceof' incompatible interface `FacesWrapper`
in `impl/src/main/java/org/apache/myfaces/application/viewstate/StateCacheUtils.java`
#### Snippet
```java
                    return true;
                }
                if (rsm1 instanceof FacesWrapper)
                {
                    rsm1 = ((FacesWrapper<? extends ResponseStateManager>) rsm1).getWrapped();
```

## RuleId[ruleID=DefaultAnnotationParam]
### DefaultAnnotationParam
Redundant default parameter value assignment
in `api/src/main/java/jakarta/faces/component/UIColumn.java`
#### Snippet
```java
     * component. The id is not necessarily unique across all components in the current view.
     */
    @JSFProperty(literalOnly=true, rtexprvalue=false)
    @Override
    public String getId()
```

## RuleId[ruleID=IfStatementWithIdenticalBranches]
### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ELText.java`
#### Snippet
```java
                        }
                        vlen = findVarLength(ca, i);
                        if (ctx != null && fact != null)
                        {
                            ve = fact.createValueExpression(ctx, new String(ca, i, vlen), String.class);
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ELText.java`
#### Snippet
```java
                        }
                        vlen = findVarLength(ca, i);
                        if (ctx != null && fact != null)
                        {
                            ve = fact.createValueExpression(ctx, new String(ca, i, vlen), String.class);
```

### IfStatementWithIdenticalBranches
Common part can be extracted removing branch
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/html/HtmlOutputScriptHandler.java`
#### Snippet
```java
        //Script with no target and no relocation is possible
        UIComponent c = ComponentSupport.findChildByTagId(parent, id);
        if (c == null)
        {
            UIViewRoot root = ComponentSupport.getViewRoot(ctx, parent);
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeComponentResourceTagHandler.java`
#### Snippet
```java
                    }
                    UIComponent facet = innerCompositeComponent.getFacet(name);
                    if (facet != null)
                    {
                        // Insert facet
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeComponentResourceTagHandler.java`
#### Snippet
```java
            FaceletHandler handler = _facetHandlersMap.get(name);

            if (handler != null)
            {
                AbstractFaceletContext actx = (AbstractFaceletContext) ctx;
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFaceletContext.java`
#### Snippet
```java
        _fnMapper = _ctx.getFunctionMapper();
        _varMapper = _ctx.getVariableMapper();
        if (_varMapper == null)
        {
            _defaultVarMapper = new DefaultVariableMapper();
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFaceletContext.java`
#### Snippet
```java
        
        String uniqueIdFromIterator = getFaceletCompositionContext().getUniqueIdFromIterator();
        if (uniqueIdFromIterator == null)
        {
            getFaceletCompositionContext().generateUniqueId(_uniqueIdBuilder);
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `impl/src/main/java/org/apache/myfaces/resource/ClassLoaderContractResourceLoader.java`
#### Snippet
```java

        String prefix = getPrefix();
        if (prefix != null && !prefix.isEmpty())
        {
            String name = prefix + '/' + resourceMeta.getContractName() + '/' + resourceMeta.getResourceIdentifier();
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `impl/src/main/java/org/apache/myfaces/resource/ClassLoaderContractResourceLoader.java`
#### Snippet
```java

        String prefix = getPrefix();
        if (prefix != null && !prefix.isEmpty())
        {
            String name = prefix + '/' + resourceMeta.getContractName() + '/' + resourceMeta.getResourceIdentifier();
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `impl/src/main/java/org/apache/myfaces/resource/ClassLoaderResourceLoader.java`
#### Snippet
```java

        String prefix = getPrefix();
        if (prefix != null && !prefix.isEmpty())
        {
            String name = prefix + '/' + resourceId;
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `impl/src/main/java/org/apache/myfaces/resource/ClassLoaderResourceLoader.java`
#### Snippet
```java

        String prefix = getPrefix();
        if (prefix != null && !prefix.isEmpty())
        {
            String name = prefix + '/' + resourceMeta.getResourceIdentifier();
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `impl/src/main/java/org/apache/myfaces/resource/InternalClassLoaderResourceLoader.java`
#### Snippet
```java

        String prefix = getPrefix();
        if (prefix != null && !prefix.isEmpty())
        {
            String name = prefix + '/' + resourceId;
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `impl/src/main/java/org/apache/myfaces/resource/InternalClassLoaderResourceLoader.java`
#### Snippet
```java

        String prefix = getPrefix();
        if (prefix != null && !prefix.isEmpty())
        {
            String name = prefix + '/' + resourceMeta.getResourceIdentifier();
```

### IfStatementWithIdenticalBranches
Common part can be extracted removing branch
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlRadioRendererBase.java`
#### Snippet
```java
                UISelectOne targetSelectOneRadio = ((UISelectOne) target);
                String targetGroup = targetSelectOneRadio.getGroup();
                if (group.equals(targetGroup))
                {
                    selectOneRadios.add(targetSelectOneRadio);
```

### IfStatementWithIdenticalBranches
Common parts with variables can be extracted from 'if'
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
                libraryVersion = resourceLoader.getLibraryVersion(pathToLib);

                if (null != libraryVersion)
                {
                    String pathToResource = localePrefix + '/'
```

### IfStatementWithIdenticalBranches
Common parts with variables can be extracted from 'if'
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
                libraryVersion = resourceLoader.getLibraryVersion(libraryName);

                if (null != libraryVersion)
                {
                    String pathToResource = libraryName + '/' + libraryVersion + '/' + resourceName;
```

### IfStatementWithIdenticalBranches
Common parts with variables can be extracted from 'if'
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
                libraryVersion = resourceLoader.getLibraryVersion(pathToLib, contractName);

                if (null != libraryVersion)
                {
                    String pathToResource = localePrefix + '/'
```

### IfStatementWithIdenticalBranches
Common parts with variables can be extracted from 'if'
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
                libraryVersion = resourceLoader.getLibraryVersion(libraryName, contractName);

                if (null != libraryVersion)
                {
                    String pathToResource = libraryName + '/' + libraryVersion + '/' + resourceName;
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `impl/src/main/java/org/apache/myfaces/application/NavigationHandlerImpl.java`
#### Snippet
```java
                        // Third case: if only <from-action> specified, match against action.
                        // Caveat: if <if> is available, evaluate.  If not, only match if outcome is not null.
                        if (cazeIf != null)
                        {
                            if (ifMatches)
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `impl/src/main/java/org/apache/myfaces/application/NavigationHandlerImpl.java`
#### Snippet
```java
            _developmentStage = FacesContext.getCurrentInstance().isProjectStage(ProjectStage.Development);
        }
        if (!Boolean.TRUE.equals(_developmentStage))
        {
            if (_navigationCases == null)
```

### IfStatementWithIdenticalBranches
Common part can be extracted from 'if'
in `impl/src/main/java/org/apache/myfaces/application/NavigationHandlerImpl.java`
#### Snippet
```java
                                continue;
                            }
                            if (!complete && flowNode instanceof ViewNode)
                            {
                                ViewNode viewNode = (ViewNode) flowNode;
```

### IfStatementWithIdenticalBranches
Common parts with variables can be extracted from 'if'
in `test/src/main/java/org/apache/myfaces/test/mock/resource/MockResourceHandler.java`
#### Snippet
```java
                libraryVersion = resourceLoader.getLibraryVersion(pathToLib);

                if (null != libraryVersion)
                {
                    String pathToResource = localePrefix + '/' + libraryName
```

### IfStatementWithIdenticalBranches
Common parts with variables can be extracted from 'if'
in `test/src/main/java/org/apache/myfaces/test/mock/resource/MockResourceHandler.java`
#### Snippet
```java
                libraryVersion = resourceLoader.getLibraryVersion(libraryName);

                if (null != libraryVersion)
                {
                    String pathToResource = (libraryName + '/' + libraryVersion
```

## RuleId[ruleID=SimplifyOptionalCallChains]
### SimplifyOptionalCallChains
Can be replaced with 'isEmpty()'
in `impl/src/main/java/org/apache/myfaces/push/cdi/WebsocketSessionManager.java`
#### Snippet
```java
                sessions.stream().filter(p -> Objects.equals(p.get(), session)).findFirst();

        if (!referenceOptional.isPresent())
        {
            return sessions.add(new SoftReference<>(session));
```

## RuleId[ruleID=UnnecessarySuperQualifier]
### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `api/src/main/java/jakarta/faces/view/facelets/DelegatingMetaTagHandler.java`
#### Snippet
```java
    public TagAttribute getTagAttribute(String localName)
    {
        return super.getAttribute(localName);
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `api/src/main/java/jakarta/faces/event/PostRestoreStateEvent.java`
#### Snippet
```java
    public void setComponent(UIComponent newComponent)
    {
        super.source = newComponent;
    }
}
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `api/src/main/java/jakarta/faces/component/UIInput.java`
#### Snippet
```java
        if (isLocalValueSet())
        {
            return super.getLocalValue();
        }
        return super.getValue();
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `api/src/main/java/jakarta/faces/component/_DeltaList.java`
#### Snippet
```java
        _initialStateMarked = true;

        int size = super.size();
        for (int i = 0; i < size; i++)
        {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `api/src/main/java/jakarta/faces/component/_DeltaList.java`
#### Snippet
```java
        for (int i = 0; i < size; i++)
        {
            T value = super.get(i);
            if (value instanceof PartialStateHolder)
            {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `api/src/main/java/jakarta/faces/component/_DeltaList.java`
#### Snippet
```java
    public Object saveState(FacesContext context)
    {
        int size = super.size();
        if (initialStateMarked())
        {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `api/src/main/java/jakarta/faces/component/_DeltaList.java`
#### Snippet
```java
                for (int i = 0; i < size; i++)
                {
                    Object value = super.get(i);
                    if (value instanceof PartialStateHolder)
                    {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `api/src/main/java/jakarta/faces/component/_DeltaList.java`
#### Snippet
```java
                for (int i = 0; i < size; i++)
                {
                    lst[i] = UIComponentBase.saveAttachedState(context, super.get(i));
                }
                return lst;
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `api/src/main/java/jakarta/faces/component/_DeltaList.java`
#### Snippet
```java
            _initialStateMarked = false;

            for (int i = 0; i < super.size(); i++)
            {
                T value = super.get(i);
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `api/src/main/java/jakarta/faces/component/_DeltaList.java`
#### Snippet
```java
            for (int i = 0; i < super.size(); i++)
            {
                T value = super.get(i);
                if (value instanceof PartialStateHolder)
                {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `api/src/main/java/jakarta/faces/component/_DeltaList.java`
#### Snippet
```java
                {
                    //Delta
                    ((StateHolder)super.get(j)).restoreState(context,
                            ((_AttachedDeltaWrapper) lst[i]).getWrappedStateObject());
                    j++;
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `api/src/main/java/jakarta/faces/component/_DeltaList.java`
#### Snippet
```java
            
            super.clear();
            super.ensureCapacity(lst.length);

            for (int i = 0; i < lst.length; i++)
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `api/src/main/java/jakarta/faces/component/behavior/AjaxBehavior.java`
#### Snippet
```java
    public void addAjaxBehaviorListener(AjaxBehaviorListener listener) 
    {
        super.addBehaviorListener(listener);
    }
    
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `api/src/main/java/jakarta/faces/component/behavior/_DeltaList.java`
#### Snippet
```java
    public Object saveState(FacesContext context)
    {
        int size = super.size();
        if (initialStateMarked())
        {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `api/src/main/java/jakarta/faces/component/behavior/_DeltaList.java`
#### Snippet
```java
                for (int i = 0; i < size; i++)
                {
                    Object value = super.get(i);
                    if (value instanceof PartialStateHolder)
                    {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `api/src/main/java/jakarta/faces/component/behavior/_DeltaList.java`
#### Snippet
```java
                for (int i = 0; i < size; i++)
                {
                    lst[i] = UIComponentBase.saveAttachedState(context, super.get(i));
                }
                return lst;
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `api/src/main/java/jakarta/faces/component/behavior/_DeltaList.java`
#### Snippet
```java
        _initialStateMarked = true;

        int size = super.size();
        for (int i = 0; i < size; i++)
        {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `api/src/main/java/jakarta/faces/component/behavior/_DeltaList.java`
#### Snippet
```java
        for (int i = 0; i < size; i++)
        {
            T value = super.get(i);
            if (value instanceof PartialStateHolder)
            {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `api/src/main/java/jakarta/faces/component/behavior/_DeltaList.java`
#### Snippet
```java
            _initialStateMarked = false;

            for (int i = 0; i < super.size(); i++)
            {
                T value = super.get(i);
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `api/src/main/java/jakarta/faces/component/behavior/_DeltaList.java`
#### Snippet
```java
            for (int i = 0; i < super.size(); i++)
            {
                T value = super.get(i);
                if (value instanceof PartialStateHolder)
                {
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `api/src/main/java/jakarta/faces/component/behavior/_DeltaList.java`
#### Snippet
```java
                {
                    //Delta
                    ((StateHolder)super.get(j)).restoreState(context,
                            ((_AttachedDeltaWrapper) lst[i]).getWrappedStateObject());
                    j++;
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `api/src/main/java/jakarta/faces/component/behavior/_DeltaList.java`
#### Snippet
```java
            
            super.clear();
            super.ensureCapacity(lst.length);
            
            for (int i = 0; i < lst.length; i++)
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `impl/src/main/java/org/apache/myfaces/cdi/FacesArtifactFlowMapProducer.java`
#### Snippet
```java
        super(beanManager);
        
        super.name("flowScope")
                .scope(FlowScoped.class)
                .qualifiers(new FlowMapAnnotationLiteral())
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `impl/src/main/java/org/apache/myfaces/cdi/managedproperty/ManagedPropertyProducer.java`
#### Snippet
```java
        }

        super.id(typeInfo.getType() + "_" + typeInfo.getExpression())
                .scope(Dependent.class)
                .qualifiers(new ManagedPropertyAnnotationLiteral(typeInfo.getExpression()))
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletDynamicComponentRefreshTransientBuildEvent.java`
#### Snippet
```java
    public void setComponent(UIComponent newComponent)
    {
        super.source = newComponent;
    }

```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `impl/src/main/java/org/apache/myfaces/view/facelets/DynamicComponentRefreshTransientBuildEvent.java`
#### Snippet
```java
    public void setComponent(UIComponent newComponent)
    {
        super.source = newComponent;
    }
}
```

### UnnecessarySuperQualifier
Qualifier `super` is unnecessary in this context
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/html/AbstractHtmlLibrary.java`
#### Snippet
```java
    public void addHtmlComponent(String name, String componentType, String rendererType)
    {
        super.addComponent(name, componentType, rendererType, HtmlComponentHandler.class);
    }

```

## RuleId[ruleID=JUnitMalformedDeclaration]
### JUnitMalformedDeclaration
Method `tearDown` annotated with '@After' should be public
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractHtmlUnitTestCase.java`
#### Snippet
```java
     */
    @After
    protected void tearDown() throws Exception
    {

```

### JUnitMalformedDeclaration
Method `setUp` annotated with '@Before' should be public
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractHtmlUnitTestCase.java`
#### Snippet
```java
     */
    @Before
    protected void setUp() throws Exception
    {

```

## RuleId[ruleID=CodeBlock2Expr]
### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/cdi/clientwindow/ClientWindowScopeContextualStorageHolder.java`
#### Snippet
```java
                MyfacesConfig.getCurrentInstance(facesContext).getNumberOfClientWindows();
        clientWindowExpirationStack = new LRULinkedHashMap<>(numberOfClientWindowsInSession, (eldest) ->
        {
            destroyAll(FacesContext.getCurrentInstance(), eldest.getKey());
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/flow/cdi/FlowScopeContextualStorageHolder.java`
#### Snippet
```java
                MyfacesConfig.getCurrentInstance(facesContext).getNumberOfClientWindows();
        clientWindowExpirationStack = new LRULinkedHashMap<>(numberOfClientWindowsInSession, (eldest) ->
        {
            clearFlowMap(FacesContext.getCurrentInstance(), eldest.getKey());
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        ComponentImpl obj = new ComponentImpl();

        onChild("component-type", node, (n) -> { obj.setComponentType(getTextContent(n)); });
        onChild("component-class", node, (n) -> { obj.setComponentClass(getTextContent(n)); });

```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java

        onChild("component-type", node, (n) -> { obj.setComponentType(getTextContent(n)); });
        onChild("component-class", node, (n) -> { obj.setComponentClass(getTextContent(n)); });

        onChild("attribute", node, (n) -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onChild("attribute", node, (n) -> {
            AttributeImpl a = new AttributeImpl();
            onChild("description", n, (cn) -> { a.addDescription(getTextContent(cn)); });
            onChild("display-name", n, (cn) -> { a.addDisplayName(getTextContent(cn)); });
            onChild("icon", n, (cn) -> { a.addIcon(getTextContent(cn)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            AttributeImpl a = new AttributeImpl();
            onChild("description", n, (cn) -> { a.addDescription(getTextContent(cn)); });
            onChild("display-name", n, (cn) -> { a.addDisplayName(getTextContent(cn)); });
            onChild("icon", n, (cn) -> { a.addIcon(getTextContent(cn)); });
            onChild("attribute-name", n, (cn) -> { a.setAttributeName(getTextContent(cn)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            onChild("description", n, (cn) -> { a.addDescription(getTextContent(cn)); });
            onChild("display-name", n, (cn) -> { a.addDisplayName(getTextContent(cn)); });
            onChild("icon", n, (cn) -> { a.addIcon(getTextContent(cn)); });
            onChild("attribute-name", n, (cn) -> { a.setAttributeName(getTextContent(cn)); });
            onChild("attribute-class", n, (cn) -> { a.setAttributeClass(getTextContent(cn)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            onChild("display-name", n, (cn) -> { a.addDisplayName(getTextContent(cn)); });
            onChild("icon", n, (cn) -> { a.addIcon(getTextContent(cn)); });
            onChild("attribute-name", n, (cn) -> { a.setAttributeName(getTextContent(cn)); });
            onChild("attribute-class", n, (cn) -> { a.setAttributeClass(getTextContent(cn)); });
            onChild("default-value", n, (cn) -> { a.setDefaultValue(getTextContent(cn)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            onChild("icon", n, (cn) -> { a.addIcon(getTextContent(cn)); });
            onChild("attribute-name", n, (cn) -> { a.setAttributeName(getTextContent(cn)); });
            onChild("attribute-class", n, (cn) -> { a.setAttributeClass(getTextContent(cn)); });
            onChild("default-value", n, (cn) -> { a.setDefaultValue(getTextContent(cn)); });
            onChild("suggested-value", n, (cn) -> { a.setSuggestedValue(getTextContent(cn)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            onChild("attribute-name", n, (cn) -> { a.setAttributeName(getTextContent(cn)); });
            onChild("attribute-class", n, (cn) -> { a.setAttributeClass(getTextContent(cn)); });
            onChild("default-value", n, (cn) -> { a.setDefaultValue(getTextContent(cn)); });
            onChild("suggested-value", n, (cn) -> { a.setSuggestedValue(getTextContent(cn)); });
            onChild("attribute-extension", n, (cn) -> { a.addAttributeExtension(getTextContent(cn)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            onChild("attribute-class", n, (cn) -> { a.setAttributeClass(getTextContent(cn)); });
            onChild("default-value", n, (cn) -> { a.setDefaultValue(getTextContent(cn)); });
            onChild("suggested-value", n, (cn) -> { a.setSuggestedValue(getTextContent(cn)); });
            onChild("attribute-extension", n, (cn) -> { a.addAttributeExtension(getTextContent(cn)); });
            obj.addAttribute(a);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            onChild("default-value", n, (cn) -> { a.setDefaultValue(getTextContent(cn)); });
            onChild("suggested-value", n, (cn) -> { a.setSuggestedValue(getTextContent(cn)); });
            onChild("attribute-extension", n, (cn) -> { a.addAttributeExtension(getTextContent(cn)); });
            obj.addAttribute(a);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onChild("property", node, (n) -> {
            PropertyImpl p = new PropertyImpl();
            onChild("description", n, (cn) -> { p.addDescription(getTextContent(cn)); });
            onChild("display-name", n, (cn) -> { p.addDisplayName(getTextContent(cn)); });
            onChild("icon", n, (cn) -> { p.addIcon(getTextContent(cn)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            PropertyImpl p = new PropertyImpl();
            onChild("description", n, (cn) -> { p.addDescription(getTextContent(cn)); });
            onChild("display-name", n, (cn) -> { p.addDisplayName(getTextContent(cn)); });
            onChild("icon", n, (cn) -> { p.addIcon(getTextContent(cn)); });
            onChild("property-name", n, (cn) -> { p.setPropertyName(getTextContent(cn)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            onChild("description", n, (cn) -> { p.addDescription(getTextContent(cn)); });
            onChild("display-name", n, (cn) -> { p.addDisplayName(getTextContent(cn)); });
            onChild("icon", n, (cn) -> { p.addIcon(getTextContent(cn)); });
            onChild("property-name", n, (cn) -> { p.setPropertyName(getTextContent(cn)); });
            onChild("property-class", n, (cn) -> { p.setPropertyClass(getTextContent(cn)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            onChild("display-name", n, (cn) -> { p.addDisplayName(getTextContent(cn)); });
            onChild("icon", n, (cn) -> { p.addIcon(getTextContent(cn)); });
            onChild("property-name", n, (cn) -> { p.setPropertyName(getTextContent(cn)); });
            onChild("property-class", n, (cn) -> { p.setPropertyClass(getTextContent(cn)); });
            onChild("default-value", n, (cn) -> { p.setDefaultValue(getTextContent(cn)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            onChild("icon", n, (cn) -> { p.addIcon(getTextContent(cn)); });
            onChild("property-name", n, (cn) -> { p.setPropertyName(getTextContent(cn)); });
            onChild("property-class", n, (cn) -> { p.setPropertyClass(getTextContent(cn)); });
            onChild("default-value", n, (cn) -> { p.setDefaultValue(getTextContent(cn)); });
            onChild("suggested-value", n, (cn) -> { p.setSuggestedValue(getTextContent(cn)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            onChild("property-name", n, (cn) -> { p.setPropertyName(getTextContent(cn)); });
            onChild("property-class", n, (cn) -> { p.setPropertyClass(getTextContent(cn)); });
            onChild("default-value", n, (cn) -> { p.setDefaultValue(getTextContent(cn)); });
            onChild("suggested-value", n, (cn) -> { p.setSuggestedValue(getTextContent(cn)); });
            onChild("property-extension", n, (cn) -> { p.addPropertyExtension(getTextContent(cn)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            onChild("property-class", n, (cn) -> { p.setPropertyClass(getTextContent(cn)); });
            onChild("default-value", n, (cn) -> { p.setDefaultValue(getTextContent(cn)); });
            onChild("suggested-value", n, (cn) -> { p.setSuggestedValue(getTextContent(cn)); });
            onChild("property-extension", n, (cn) -> { p.addPropertyExtension(getTextContent(cn)); });
            obj.addProperty(p);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            onChild("default-value", n, (cn) -> { p.setDefaultValue(getTextContent(cn)); });
            onChild("suggested-value", n, (cn) -> { p.setSuggestedValue(getTextContent(cn)); });
            onChild("property-extension", n, (cn) -> { p.addPropertyExtension(getTextContent(cn)); });
            obj.addProperty(p);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        FactoryImpl obj = new FactoryImpl();
        
        onChild("application-factory", node, (n) -> { obj.addApplicationFactory(getTextContent(n)); });
        onChild("faces-context-factory", node, (n) -> { obj.addFacesContextFactory(getTextContent(n)); });
        onChild("lifecycle-factory", node, (n) -> { obj.addLifecycleFactory(getTextContent(n)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        
        onChild("application-factory", node, (n) -> { obj.addApplicationFactory(getTextContent(n)); });
        onChild("faces-context-factory", node, (n) -> { obj.addFacesContextFactory(getTextContent(n)); });
        onChild("lifecycle-factory", node, (n) -> { obj.addLifecycleFactory(getTextContent(n)); });
        onChild("render-kit-factory", node, (n) -> { obj.addRenderkitFactory(getTextContent(n)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onChild("application-factory", node, (n) -> { obj.addApplicationFactory(getTextContent(n)); });
        onChild("faces-context-factory", node, (n) -> { obj.addFacesContextFactory(getTextContent(n)); });
        onChild("lifecycle-factory", node, (n) -> { obj.addLifecycleFactory(getTextContent(n)); });
        onChild("render-kit-factory", node, (n) -> { obj.addRenderkitFactory(getTextContent(n)); });
        onChild("exception-handler-factory", node, (n) -> { obj.addExceptionHandlerFactory(getTextContent(n)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onChild("faces-context-factory", node, (n) -> { obj.addFacesContextFactory(getTextContent(n)); });
        onChild("lifecycle-factory", node, (n) -> { obj.addLifecycleFactory(getTextContent(n)); });
        onChild("render-kit-factory", node, (n) -> { obj.addRenderkitFactory(getTextContent(n)); });
        onChild("exception-handler-factory", node, (n) -> { obj.addExceptionHandlerFactory(getTextContent(n)); });
        onChild("external-context-factory", node, (n) -> { obj.addExternalContextFactory(getTextContent(n)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onChild("lifecycle-factory", node, (n) -> { obj.addLifecycleFactory(getTextContent(n)); });
        onChild("render-kit-factory", node, (n) -> { obj.addRenderkitFactory(getTextContent(n)); });
        onChild("exception-handler-factory", node, (n) -> { obj.addExceptionHandlerFactory(getTextContent(n)); });
        onChild("external-context-factory", node, (n) -> { obj.addExternalContextFactory(getTextContent(n)); });
        onChild("view-declaration-language-factory", node, (n) -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onChild("render-kit-factory", node, (n) -> { obj.addRenderkitFactory(getTextContent(n)); });
        onChild("exception-handler-factory", node, (n) -> { obj.addExceptionHandlerFactory(getTextContent(n)); });
        onChild("external-context-factory", node, (n) -> { obj.addExternalContextFactory(getTextContent(n)); });
        onChild("view-declaration-language-factory", node, (n) -> {
            obj.addViewDeclarationLanguageFactory(getTextContent(n));
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onChild("exception-handler-factory", node, (n) -> { obj.addExceptionHandlerFactory(getTextContent(n)); });
        onChild("external-context-factory", node, (n) -> { obj.addExternalContextFactory(getTextContent(n)); });
        onChild("view-declaration-language-factory", node, (n) -> {
            obj.addViewDeclarationLanguageFactory(getTextContent(n));
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            obj.addViewDeclarationLanguageFactory(getTextContent(n));
        });
        onChild("partial-view-context-factory", node, (n) -> {
            obj.addPartialViewContextFactory(getTextContent(n));
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            obj.addPartialViewContextFactory(getTextContent(n));
        });
        onChild("tag-handler-delegate-factory", node, (n) -> {
            obj.addTagHandlerDelegateFactory(getTextContent(n));
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            obj.addTagHandlerDelegateFactory(getTextContent(n));
        });
        onChild("visit-context-factory", node, (n) -> { obj.addVisitContextFactory(getTextContent(n)); });
        onChild("search-expression-context-factory", node, (n) -> {
            obj.addSearchExpressionContextFactory(getTextContent(n));
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        });
        onChild("visit-context-factory", node, (n) -> { obj.addVisitContextFactory(getTextContent(n)); });
        onChild("search-expression-context-factory", node, (n) -> {
            obj.addSearchExpressionContextFactory(getTextContent(n));
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            obj.addSearchExpressionContextFactory(getTextContent(n));
        });
        onChild("facelet-cache-factory", node, (n) -> { obj.addFaceletCacheFactory(getTextContent(n)); });
        onChild("flash-factory", node, (n) -> { obj.addFlashFactory(getTextContent(n)); });
        onChild("flow-handler-factory", node, (n) -> { obj.addFlowHandlerFactory(getTextContent(n)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        });
        onChild("facelet-cache-factory", node, (n) -> { obj.addFaceletCacheFactory(getTextContent(n)); });
        onChild("flash-factory", node, (n) -> { obj.addFlashFactory(getTextContent(n)); });
        onChild("flow-handler-factory", node, (n) -> { obj.addFlowHandlerFactory(getTextContent(n)); });
        onChild("client-window-factory", node, (n) -> { obj.addClientWindowFactory(getTextContent(n)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onChild("facelet-cache-factory", node, (n) -> { obj.addFaceletCacheFactory(getTextContent(n)); });
        onChild("flash-factory", node, (n) -> { obj.addFlashFactory(getTextContent(n)); });
        onChild("flow-handler-factory", node, (n) -> { obj.addFlowHandlerFactory(getTextContent(n)); });
        onChild("client-window-factory", node, (n) -> { obj.addClientWindowFactory(getTextContent(n)); });
        
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onChild("flash-factory", node, (n) -> { obj.addFlashFactory(getTextContent(n)); });
        onChild("flow-handler-factory", node, (n) -> { obj.addFlowHandlerFactory(getTextContent(n)); });
        onChild("client-window-factory", node, (n) -> { obj.addClientWindowFactory(getTextContent(n)); });
        
        return obj;
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        
        onChild("before", node, (n) -> {
            onChild("name", n, (cn) -> { 
                obj.addBeforeSlot(new FacesConfigNameSlotImpl(getTextContent(cn)));
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
                obj.addBeforeSlot(new FacesConfigNameSlotImpl(getTextContent(cn)));
            });
            onChild("others", n, (cn) -> {
                obj.addBeforeSlot(new ConfigOthersSlotImpl());
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java

        onChild("after", node, (n) -> {
            onChild("name", n, (cn) -> { 
                obj.addAfterSlot(new FacesConfigNameSlotImpl(getTextContent(cn)));
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
                obj.addAfterSlot(new FacesConfigNameSlotImpl(getTextContent(cn)));
            });
            onChild("others", n, (cn) -> {
                obj.addAfterSlot(new ConfigOthersSlotImpl());
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        ApplicationImpl obj = new ApplicationImpl();
        
        onChild("action-listener", node, (n) -> { obj.addActionListener(getTextContent(n)); });
        onChild("message-bundle", node, (n) -> { obj.addMessageBundle(getTextContent(n)); });
        onChild("navigation-handler", node, (n) -> { obj.addNavigationHandler(getTextContent(n)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        
        onChild("action-listener", node, (n) -> { obj.addActionListener(getTextContent(n)); });
        onChild("message-bundle", node, (n) -> { obj.addMessageBundle(getTextContent(n)); });
        onChild("navigation-handler", node, (n) -> { obj.addNavigationHandler(getTextContent(n)); });
        onChild("view-handler", node, (n) -> { obj.addViewHandler(getTextContent(n)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onChild("action-listener", node, (n) -> { obj.addActionListener(getTextContent(n)); });
        onChild("message-bundle", node, (n) -> { obj.addMessageBundle(getTextContent(n)); });
        onChild("navigation-handler", node, (n) -> { obj.addNavigationHandler(getTextContent(n)); });
        onChild("view-handler", node, (n) -> { obj.addViewHandler(getTextContent(n)); });
        onChild("state-manager", node, (n) -> { obj.addStateManager(getTextContent(n)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onChild("message-bundle", node, (n) -> { obj.addMessageBundle(getTextContent(n)); });
        onChild("navigation-handler", node, (n) -> { obj.addNavigationHandler(getTextContent(n)); });
        onChild("view-handler", node, (n) -> { obj.addViewHandler(getTextContent(n)); });
        onChild("state-manager", node, (n) -> { obj.addStateManager(getTextContent(n)); });
        onChild("property-resolver", node, (n) -> { obj.addPropertyResolver(getTextContent(n)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onChild("navigation-handler", node, (n) -> { obj.addNavigationHandler(getTextContent(n)); });
        onChild("view-handler", node, (n) -> { obj.addViewHandler(getTextContent(n)); });
        onChild("state-manager", node, (n) -> { obj.addStateManager(getTextContent(n)); });
        onChild("property-resolver", node, (n) -> { obj.addPropertyResolver(getTextContent(n)); });
        onChild("variable-resolver", node, (n) -> { obj.addVariableResolver(getTextContent(n)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onChild("view-handler", node, (n) -> { obj.addViewHandler(getTextContent(n)); });
        onChild("state-manager", node, (n) -> { obj.addStateManager(getTextContent(n)); });
        onChild("property-resolver", node, (n) -> { obj.addPropertyResolver(getTextContent(n)); });
        onChild("variable-resolver", node, (n) -> { obj.addVariableResolver(getTextContent(n)); });
        onChild("el-resolver", node, (n) -> { obj.addElResolver(getTextContent(n)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onChild("state-manager", node, (n) -> { obj.addStateManager(getTextContent(n)); });
        onChild("property-resolver", node, (n) -> { obj.addPropertyResolver(getTextContent(n)); });
        onChild("variable-resolver", node, (n) -> { obj.addVariableResolver(getTextContent(n)); });
        onChild("el-resolver", node, (n) -> { obj.addElResolver(getTextContent(n)); });
        onChild("resource-handler", node, (n) -> { obj.addResourceHandler(getTextContent(n)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onChild("property-resolver", node, (n) -> { obj.addPropertyResolver(getTextContent(n)); });
        onChild("variable-resolver", node, (n) -> { obj.addVariableResolver(getTextContent(n)); });
        onChild("el-resolver", node, (n) -> { obj.addElResolver(getTextContent(n)); });
        onChild("resource-handler", node, (n) -> { obj.addResourceHandler(getTextContent(n)); });
        onChild("default-render-kit-id", node, (n) -> { obj.addDefaultRenderkitId(getTextContent(n)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onChild("variable-resolver", node, (n) -> { obj.addVariableResolver(getTextContent(n)); });
        onChild("el-resolver", node, (n) -> { obj.addElResolver(getTextContent(n)); });
        onChild("resource-handler", node, (n) -> { obj.addResourceHandler(getTextContent(n)); });
        onChild("default-render-kit-id", node, (n) -> { obj.addDefaultRenderkitId(getTextContent(n)); });
        onChild("search-expression-handler", node, (n) -> { obj.addSearchExpressionHandler(getTextContent(n)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onChild("el-resolver", node, (n) -> { obj.addElResolver(getTextContent(n)); });
        onChild("resource-handler", node, (n) -> { obj.addResourceHandler(getTextContent(n)); });
        onChild("default-render-kit-id", node, (n) -> { obj.addDefaultRenderkitId(getTextContent(n)); });
        onChild("search-expression-handler", node, (n) -> { obj.addSearchExpressionHandler(getTextContent(n)); });
        onChild("search-keyword-resolver", node, (n) -> { obj.addSearchKeywordResolver(getTextContent(n)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onChild("resource-handler", node, (n) -> { obj.addResourceHandler(getTextContent(n)); });
        onChild("default-render-kit-id", node, (n) -> { obj.addDefaultRenderkitId(getTextContent(n)); });
        onChild("search-expression-handler", node, (n) -> { obj.addSearchExpressionHandler(getTextContent(n)); });
        onChild("search-keyword-resolver", node, (n) -> { obj.addSearchKeywordResolver(getTextContent(n)); });

```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onChild("default-render-kit-id", node, (n) -> { obj.addDefaultRenderkitId(getTextContent(n)); });
        onChild("search-expression-handler", node, (n) -> { obj.addSearchExpressionHandler(getTextContent(n)); });
        onChild("search-keyword-resolver", node, (n) -> { obj.addSearchKeywordResolver(getTextContent(n)); });

        onChild("default-validators", node, (n) -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onChild("default-validators", node, (n) -> {
            obj.setDefaultValidatorsPresent();
            onChild("validator-id", n, (cn) -> {
                obj.addDefaultValidatorId(getTextContent(cn));
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onChild("locale-config", node, (n) -> {
            LocaleConfigImpl lc = new LocaleConfigImpl();
            onChild("default-locale", n, (cn) -> { lc.setDefaultLocale(getTextContent(cn)); });
            onChild("supported-locale", n, (cn) -> { lc.addSupportedLocale(getTextContent(cn)); });
            obj.addLocaleConfig(lc);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            LocaleConfigImpl lc = new LocaleConfigImpl();
            onChild("default-locale", n, (cn) -> { lc.setDefaultLocale(getTextContent(cn)); });
            onChild("supported-locale", n, (cn) -> { lc.addSupportedLocale(getTextContent(cn)); });
            obj.addLocaleConfig(lc);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onChild("resource-bundle", node, (n) -> {
            ResourceBundleImpl rb = new ResourceBundleImpl();
            onChild("base-name", n, (cn) -> { rb.setBaseName(getTextContent(cn)); });
            onChild("var", n, (cn) -> { rb.setVar(getTextContent(cn)); });
            onChild("display-name", n, (cn) -> { rb.setDisplayName(getTextContent(cn)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            ResourceBundleImpl rb = new ResourceBundleImpl();
            onChild("base-name", n, (cn) -> { rb.setBaseName(getTextContent(cn)); });
            onChild("var", n, (cn) -> { rb.setVar(getTextContent(cn)); });
            onChild("display-name", n, (cn) -> { rb.setDisplayName(getTextContent(cn)); });
            obj.addResourceBundle(rb);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            onChild("base-name", n, (cn) -> { rb.setBaseName(getTextContent(cn)); });
            onChild("var", n, (cn) -> { rb.setVar(getTextContent(cn)); });
            onChild("display-name", n, (cn) -> { rb.setDisplayName(getTextContent(cn)); });
            obj.addResourceBundle(rb);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onChild("system-event-listener", node, (n) -> {
            SystemEventListenerImpl sel = new SystemEventListenerImpl();
            onChild("system-event-listener-class", n, (cn) -> {
                sel.setSystemEventListenerClass(getTextContent(cn));
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
                sel.setSystemEventListenerClass(getTextContent(cn));
            });
            onChild("system-event-class", n, (cn) -> {
                sel.setSystemEventClass(getTextContent(cn));
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
                sel.setSystemEventClass(getTextContent(cn));
            });
            onChild("source-class", n, (cn) -> {
                sel.setSourceClass(getTextContent(cn));
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        });

        onChild("resource-library-contracts", node, (n) -> {
            onChild("contract-mapping", n, (cn) -> {
                ContractMappingImpl cm = new ContractMappingImpl();
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            onChild("contract-mapping", n, (cn) -> {
                ContractMappingImpl cm = new ContractMappingImpl();
                onChild("url-pattern", cn, (ccn) -> {
                    cm.addUrlPattern(getTextContent(ccn));
                });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
                    cm.addUrlPattern(getTextContent(ccn));
                });
                onChild("contracts", cn, (ccn) -> {
                    cm.addContract(getTextContent(ccn));
                });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        NavigationCaseImpl obj = new NavigationCaseImpl();

        onChild("from-action", node, (n) -> { obj.setFromAction(getTextContent(n)); });
        onChild("from-outcome", node, (n) -> { obj.setFromOutcome(getTextContent(n)); });
        onChild("if", node, (n) -> { obj.setIf(getTextContent(n)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java

        onChild("from-action", node, (n) -> { obj.setFromAction(getTextContent(n)); });
        onChild("from-outcome", node, (n) -> { obj.setFromOutcome(getTextContent(n)); });
        onChild("if", node, (n) -> { obj.setIf(getTextContent(n)); });
        onChild("to-view-id", node, (n) -> { obj.setToViewId(getTextContent(n)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onChild("from-action", node, (n) -> { obj.setFromAction(getTextContent(n)); });
        onChild("from-outcome", node, (n) -> { obj.setFromOutcome(getTextContent(n)); });
        onChild("if", node, (n) -> { obj.setIf(getTextContent(n)); });
        onChild("to-view-id", node, (n) -> { obj.setToViewId(getTextContent(n)); });
       
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onChild("from-outcome", node, (n) -> { obj.setFromOutcome(getTextContent(n)); });
        onChild("if", node, (n) -> { obj.setIf(getTextContent(n)); });
        onChild("to-view-id", node, (n) -> { obj.setToViewId(getTextContent(n)); });
       
        onChild("redirect", node, (n) -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onChild("redirect", node, (n) -> {
            RedirectImpl r = new RedirectImpl();
            onChild("include-view-params", n, (cn) -> { r.setIncludeViewParams("true"); });
            onChild("view-param", n, (cn) -> {
                ViewParamImpl vp = new ViewParamImpl();
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            onChild("view-param", n, (cn) -> {
                ViewParamImpl vp = new ViewParamImpl();
                onChild("name", cn, (ccn) -> { vp.setName(getTextContent(ccn)); });
                onChild("value", cn, (ccn) -> { vp.setValue(getTextContent(ccn)); });
                r.addViewParam(vp);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
                ViewParamImpl vp = new ViewParamImpl();
                onChild("name", cn, (ccn) -> { vp.setName(getTextContent(ccn)); });
                onChild("value", cn, (ccn) -> { vp.setValue(getTextContent(ccn)); });
                r.addViewParam(vp);
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            onChild("redirect-param", n, (cn) -> {
                ViewParamImpl vp = new ViewParamImpl();
                onChild("name", cn, (ccn) -> { vp.setName(getTextContent(ccn)); });
                onChild("value", cn, (ccn) -> { vp.setValue(getTextContent(ccn)); });
                r.addViewParam(vp);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
                ViewParamImpl vp = new ViewParamImpl();
                onChild("name", cn, (ccn) -> { vp.setName(getTextContent(ccn)); });
                onChild("value", cn, (ccn) -> { vp.setValue(getTextContent(ccn)); });
                r.addViewParam(vp);
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        FacesFlowDefinitionImpl obj = new FacesFlowDefinitionImpl();
        
        onAttribute("id", node, (v) -> { obj.setId(v); });
        onChild("start-node", node, (n) -> { obj.setStartNode(getTextContent(n)); });
        onChild("initializer", node, (n) -> { obj.setInitializer(getTextContent(n)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        
        onAttribute("id", node, (v) -> { obj.setId(v); });
        onChild("start-node", node, (n) -> { obj.setStartNode(getTextContent(n)); });
        onChild("initializer", node, (n) -> { obj.setInitializer(getTextContent(n)); });
        onChild("finalizer", node, (n) -> { obj.setFinalizer(getTextContent(n)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onAttribute("id", node, (v) -> { obj.setId(v); });
        onChild("start-node", node, (n) -> { obj.setStartNode(getTextContent(n)); });
        onChild("initializer", node, (n) -> { obj.setInitializer(getTextContent(n)); });
        onChild("finalizer", node, (n) -> { obj.setFinalizer(getTextContent(n)); });
        
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onChild("start-node", node, (n) -> { obj.setStartNode(getTextContent(n)); });
        onChild("initializer", node, (n) -> { obj.setInitializer(getTextContent(n)); });
        onChild("finalizer", node, (n) -> { obj.setFinalizer(getTextContent(n)); });
        
        onChild("view", node, (n) -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onChild("view", node, (n) -> {
            FacesFlowViewImpl ffv = new FacesFlowViewImpl();
            onAttribute("id", n, (v) -> { ffv.setId(v); });
            onChild("vdl-document", n, (cn) -> { ffv.setVdlDocument(getTextContent(cn)); });
            obj.addView(ffv);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            FacesFlowViewImpl ffv = new FacesFlowViewImpl();
            onAttribute("id", n, (v) -> { ffv.setId(v); });
            onChild("vdl-document", n, (cn) -> { ffv.setVdlDocument(getTextContent(cn)); });
            obj.addView(ffv);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onChild("switch", node, (n) -> {
            FacesFlowSwitchImpl ffs = new FacesFlowSwitchImpl();
            onAttribute("id", n, (v) -> { ffs.setId(v); });
            onChild("case", n, (cn) -> { ffs.addNavigationCase(processNavigationCase(cn)); });
            onChild("default-outcome", n, (cn) -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            FacesFlowSwitchImpl ffs = new FacesFlowSwitchImpl();
            onAttribute("id", n, (v) -> { ffs.setId(v); });
            onChild("case", n, (cn) -> { ffs.addNavigationCase(processNavigationCase(cn)); });
            onChild("default-outcome", n, (cn) -> {
                NavigationCaseImpl nc = new NavigationCaseImpl();
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onChild("flow-return", node, (n) -> {
            FacesFlowReturnImpl ffr = new FacesFlowReturnImpl();
            onAttribute("id", n, (v) -> { ffr.setId(v); });
            onChild("from-outcome", n, (cn) -> {
                NavigationCaseImpl nc = new NavigationCaseImpl();
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onChild("navigation-rule", node, (n) -> {
            NavigationRuleImpl nr = new NavigationRuleImpl();
            onChild("from-view-id", n, (cn) -> { nr.setFromViewId(getTextContent(cn)); });
            onChild("navigation-case", n, (cn) -> { nr.addNavigationCase(processNavigationCase(cn)); });
            obj.addNavigationRule(nr);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            NavigationRuleImpl nr = new NavigationRuleImpl();
            onChild("from-view-id", n, (cn) -> { nr.setFromViewId(getTextContent(cn)); });
            onChild("navigation-case", n, (cn) -> { nr.addNavigationCase(processNavigationCase(cn)); });
            obj.addNavigationRule(nr);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onChild("flow-call", node, (n) -> {
            FacesFlowCallImpl ffc = new FacesFlowCallImpl();
            onAttribute("id", n, (v) -> { ffc.setId(v); });
            onChild("flow-reference", n, (cn) -> {
                FacesFlowReferenceImpl ffr = new FacesFlowReferenceImpl();
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            onChild("flow-reference", n, (cn) -> {
                FacesFlowReferenceImpl ffr = new FacesFlowReferenceImpl();
                onChild("flow-document-id", cn, (ccn) -> { ffr.setFlowDocumentId(getTextContent(ccn)); });
                onChild("flow-id", cn, (ccn) -> { ffr.setFlowId(getTextContent(ccn)); });
                ffc.setFlowReference(ffr);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
                FacesFlowReferenceImpl ffr = new FacesFlowReferenceImpl();
                onChild("flow-document-id", cn, (ccn) -> { ffr.setFlowDocumentId(getTextContent(ccn)); });
                onChild("flow-id", cn, (ccn) -> { ffr.setFlowId(getTextContent(ccn)); });
                ffc.setFlowReference(ffr);
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            onChild("outbound-parameter", n, (cn) -> {
                FacesFlowParameterImpl ffp = new FacesFlowParameterImpl();
                onChild("name", cn, (ccn) -> { ffp.setName(getTextContent(ccn)); });
                onChild("value", cn, (ccn) -> { ffp.setValue(getTextContent(ccn)); });
                ffc.addOutboundParameter(ffp);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
                FacesFlowParameterImpl ffp = new FacesFlowParameterImpl();
                onChild("name", cn, (ccn) -> { ffp.setName(getTextContent(ccn)); });
                onChild("value", cn, (ccn) -> { ffp.setValue(getTextContent(ccn)); });
                ffc.addOutboundParameter(ffp);
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onChild("method-call", node, (n) -> {
            FacesFlowMethodCallImpl ffmc = new FacesFlowMethodCallImpl();
            onAttribute("id", n, (v) -> { ffmc.setId(v); });
            onChild("method", n, (cn) -> { ffmc.setMethod(getTextContent(cn)); });
            onChild("default-outcome", n, (cn) -> { ffmc.setDefaultOutcome(getTextContent(cn)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            FacesFlowMethodCallImpl ffmc = new FacesFlowMethodCallImpl();
            onAttribute("id", n, (v) -> { ffmc.setId(v); });
            onChild("method", n, (cn) -> { ffmc.setMethod(getTextContent(cn)); });
            onChild("default-outcome", n, (cn) -> { ffmc.setDefaultOutcome(getTextContent(cn)); });
            onChild("parameter", n, (cn) -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            onAttribute("id", n, (v) -> { ffmc.setId(v); });
            onChild("method", n, (cn) -> { ffmc.setMethod(getTextContent(cn)); });
            onChild("default-outcome", n, (cn) -> { ffmc.setDefaultOutcome(getTextContent(cn)); });
            onChild("parameter", n, (cn) -> {
                FacesFlowMethodParameterImpl ffmp = new FacesFlowMethodParameterImpl();
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            onChild("parameter", n, (cn) -> {
                FacesFlowMethodParameterImpl ffmp = new FacesFlowMethodParameterImpl();
                onChild("class", cn, (ccn) -> { ffmp.setClassName(getTextContent(ccn)); });
                onChild("value", cn, (ccn) -> { ffmp.setValue(getTextContent(ccn)); });
                ffmc.addParameter(ffmp);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
                FacesFlowMethodParameterImpl ffmp = new FacesFlowMethodParameterImpl();
                onChild("class", cn, (ccn) -> { ffmp.setClassName(getTextContent(ccn)); });
                onChild("value", cn, (ccn) -> { ffmp.setValue(getTextContent(ccn)); });
                ffmc.addParameter(ffmp);
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onChild("inbound-parameter", node, (n) -> {
            FacesFlowParameterImpl ffp = new FacesFlowParameterImpl();
            onChild("name", n, (cn) -> { ffp.setName(getTextContent(cn)); });
            onChild("value", n, (cn) -> { ffp.setValue(getTextContent(cn)); });
            obj.addInboundParameter(ffp);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            FacesFlowParameterImpl ffp = new FacesFlowParameterImpl();
            onChild("name", n, (cn) -> { ffp.setName(getTextContent(cn)); });
            onChild("value", n, (cn) -> { ffp.setValue(getTextContent(cn)); });
            obj.addInboundParameter(ffp);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            
            onAttribute("metadata-complete", document.getDocumentElement(),
                    (v) -> { facesConfig.setMetadataComplete(v); });
            onAttribute("version", document.getDocumentElement(),
                    (v) -> { facesConfig.setVersion(v); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
                    (v) -> { facesConfig.setMetadataComplete(v); });
            onAttribute("version", document.getDocumentElement(),
                    (v) -> { facesConfig.setVersion(v); });

            onChild("name", document.getDocumentElement(), 
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java

            onChild("name", document.getDocumentElement(), 
                    (n) -> { facesConfig.setName(getTextContent(n)); });
            onChild("ordering", document.getDocumentElement(), 
                    (n) -> { facesConfig.setOrdering(processOrdering(n)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
                    (n) -> { facesConfig.setName(getTextContent(n)); });
            onChild("ordering", document.getDocumentElement(), 
                    (n) -> { facesConfig.setOrdering(processOrdering(n)); });
            onChild("absolute-ordering", document.getDocumentElement(), 
                    (n) -> { facesConfig.setAbsoluteOrdering(processAbsoluteOrdering(n)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
                    (n) -> { facesConfig.setOrdering(processOrdering(n)); });
            onChild("absolute-ordering", document.getDocumentElement(), 
                    (n) -> { facesConfig.setAbsoluteOrdering(processAbsoluteOrdering(n)); });
            onChild("application", document.getDocumentElement(), 
                    (n) -> { facesConfig.addApplication(processApplication(n)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
                    (n) -> { facesConfig.setAbsoluteOrdering(processAbsoluteOrdering(n)); });
            onChild("application", document.getDocumentElement(), 
                    (n) -> { facesConfig.addApplication(processApplication(n)); });
            onChild("factory", document.getDocumentElement(), 
                    (n) -> { facesConfig.addFactory(processFactory(n)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
                    (n) -> { facesConfig.addApplication(processApplication(n)); });
            onChild("factory", document.getDocumentElement(), 
                    (n) -> { facesConfig.addFactory(processFactory(n)); });
            onChild("component", document.getDocumentElement(),
                    (n) -> { facesConfig.addComponent(processComponent(n)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
                    (n) -> { facesConfig.addFactory(processFactory(n)); });
            onChild("component", document.getDocumentElement(),
                    (n) -> { facesConfig.addComponent(processComponent(n)); });
            onChild("lifecycle", document.getDocumentElement(), (n) -> {
                onChild("phase-listener", n, (cn) -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            onChild("component", document.getDocumentElement(),
                    (n) -> { facesConfig.addComponent(processComponent(n)); });
            onChild("lifecycle", document.getDocumentElement(), (n) -> {
                onChild("phase-listener", n, (cn) -> {
                    facesConfig.addLifecyclePhaseListener(getTextContent(cn)); 
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
                    (n) -> { facesConfig.addComponent(processComponent(n)); });
            onChild("lifecycle", document.getDocumentElement(), (n) -> {
                onChild("phase-listener", n, (cn) -> {
                    facesConfig.addLifecyclePhaseListener(getTextContent(cn)); 
                });  
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
                });  
            });
            onChild("validator", document.getDocumentElement(), (n) -> {
                facesConfig.addValidator(
                        firstChildTextContent("validator-id", n),
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            });
            onChild("render-kit", document.getDocumentElement(),
                    (n) -> { facesConfig.addRenderKit(processRenderKit(n)); });
            onChild("behavior", document.getDocumentElement(),
                    (n) -> { facesConfig.addBehavior(processBehavior(n)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
                    (n) -> { facesConfig.addRenderKit(processRenderKit(n)); });
            onChild("behavior", document.getDocumentElement(),
                    (n) -> { facesConfig.addBehavior(processBehavior(n)); });
            onChild("converter", document.getDocumentElement(),
                    (n) -> { facesConfig.addConverter(processConverter(n)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
                    (n) -> { facesConfig.addBehavior(processBehavior(n)); });
            onChild("converter", document.getDocumentElement(),
                    (n) -> { facesConfig.addConverter(processConverter(n)); });
            onChild("protected-views", document.getDocumentElement(), (n) -> {
                onChild("url-pattern", n, (cn) -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            onChild("converter", document.getDocumentElement(),
                    (n) -> { facesConfig.addConverter(processConverter(n)); });
            onChild("protected-views", document.getDocumentElement(), (n) -> {
                onChild("url-pattern", n, (cn) -> {
                    facesConfig.addProtectedViewUrlPattern(getTextContent(cn)); 
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
                    (n) -> { facesConfig.addConverter(processConverter(n)); });
            onChild("protected-views", document.getDocumentElement(), (n) -> {
                onChild("url-pattern", n, (cn) -> {
                    facesConfig.addProtectedViewUrlPattern(getTextContent(cn)); 
                });  
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            });
            onChild("faces-config-extension", document.getDocumentElement(),
                    (n) -> { facesConfig.addFacesConfigExtension(processFacesConfigExtension(n)); });
            onChild("navigation-rule", document.getDocumentElement(),
                    (n) -> { facesConfig.addNavigationRule(processNavigationRule(n)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
                    (n) -> { facesConfig.addFacesConfigExtension(processFacesConfigExtension(n)); });
            onChild("navigation-rule", document.getDocumentElement(),
                    (n) -> { facesConfig.addNavigationRule(processNavigationRule(n)); });
            onChild("flow-definition", document.getDocumentElement(),
                    (n) -> { facesConfig.addFacesFlowDefinition(processFlowDefinition(n)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
                    (n) -> { facesConfig.addNavigationRule(processNavigationRule(n)); });
            onChild("flow-definition", document.getDocumentElement(),
                    (n) -> { facesConfig.addFacesFlowDefinition(processFlowDefinition(n)); });
            
        }
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onChild("facelets-processing", node, (n) -> {
            FaceletsProcessingImpl fp = new FaceletsProcessingImpl();
            onChild("file-extension", n, (cn) -> { fp.setFileExtension(getTextContent(cn)); });
            onChild("process-as", n, (cn) -> { fp.setProcessAs(getTextContent(cn)); });
            onChild("oam-compress-spaces", n, (cn) -> { fp.setOamCompressSpaces(getTextContent(cn)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            FaceletsProcessingImpl fp = new FaceletsProcessingImpl();
            onChild("file-extension", n, (cn) -> { fp.setFileExtension(getTextContent(cn)); });
            onChild("process-as", n, (cn) -> { fp.setProcessAs(getTextContent(cn)); });
            onChild("oam-compress-spaces", n, (cn) -> { fp.setOamCompressSpaces(getTextContent(cn)); });
            obj.addFaceletsProcessing(fp);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            onChild("file-extension", n, (cn) -> { fp.setFileExtension(getTextContent(cn)); });
            onChild("process-as", n, (cn) -> { fp.setProcessAs(getTextContent(cn)); });
            onChild("oam-compress-spaces", n, (cn) -> { fp.setOamCompressSpaces(getTextContent(cn)); });
            obj.addFaceletsProcessing(fp);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onChild("view-pool-mapping", node, (n) -> {
            ViewPoolMappingImpl vpm = new ViewPoolMappingImpl();
            onChild("url-pattern", n, (cn) -> { vpm.setUrlPattern(getTextContent(cn)); });
            onChild("parameter", n, (cn) -> {
                ViewPoolParameterImpl vpp = new ViewPoolParameterImpl();
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            onChild("parameter", n, (cn) -> {
                ViewPoolParameterImpl vpp = new ViewPoolParameterImpl();
                onChild("name", cn, (ccn) -> { vpp.setName(getTextContent(ccn)); });
                onChild("value", cn, (ccn) -> { vpp.setValue(getTextContent(ccn)); });
                vpm.addParameter(vpp);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
                ViewPoolParameterImpl vpp = new ViewPoolParameterImpl();
                onChild("name", cn, (ccn) -> { vpp.setName(getTextContent(ccn)); });
                onChild("value", cn, (ccn) -> { vpp.setValue(getTextContent(ccn)); });
                vpm.addParameter(vpp);
            });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onChild("facelets-template-mapping", node, (n) -> {
            FaceletsTemplateMappingImpl ftm = new FaceletsTemplateMappingImpl();
            onChild("url-pattern", n, (cn) -> { ftm.setUrlPattern(getTextContent(cn)); });
            obj.addFaceletsTemplateMapping(ftm);
        });  
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        BehaviorImpl obj = new BehaviorImpl();

        onChild("behavior-id", node, (n) -> { obj.setBehaviorId(getTextContent(n)); });
        onChild("behavior-class", node, (n) -> { obj.setBehaviorClass(getTextContent(n)); });
                
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java

        onChild("behavior-id", node, (n) -> { obj.setBehaviorId(getTextContent(n)); });
        onChild("behavior-class", node, (n) -> { obj.setBehaviorClass(getTextContent(n)); });
                
        onChild("attribute", node, (n) -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onChild("attribute", node, (n) -> {
            AttributeImpl a = new AttributeImpl();
            onChild("description", n, (cn) -> { a.addDescription(getTextContent(cn)); });
            onChild("display-name", n, (cn) -> { a.addDisplayName(getTextContent(cn)); });
            onChild("icon", n, (cn) -> { a.addIcon(getTextContent(cn)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            AttributeImpl a = new AttributeImpl();
            onChild("description", n, (cn) -> { a.addDescription(getTextContent(cn)); });
            onChild("display-name", n, (cn) -> { a.addDisplayName(getTextContent(cn)); });
            onChild("icon", n, (cn) -> { a.addIcon(getTextContent(cn)); });
            onChild("attribute-name", n, (cn) -> { a.setAttributeName(getTextContent(cn)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            onChild("description", n, (cn) -> { a.addDescription(getTextContent(cn)); });
            onChild("display-name", n, (cn) -> { a.addDisplayName(getTextContent(cn)); });
            onChild("icon", n, (cn) -> { a.addIcon(getTextContent(cn)); });
            onChild("attribute-name", n, (cn) -> { a.setAttributeName(getTextContent(cn)); });
            onChild("attribute-class", n, (cn) -> { a.setAttributeClass(getTextContent(cn)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            onChild("display-name", n, (cn) -> { a.addDisplayName(getTextContent(cn)); });
            onChild("icon", n, (cn) -> { a.addIcon(getTextContent(cn)); });
            onChild("attribute-name", n, (cn) -> { a.setAttributeName(getTextContent(cn)); });
            onChild("attribute-class", n, (cn) -> { a.setAttributeClass(getTextContent(cn)); });
            onChild("default-value", n, (cn) -> { a.setDefaultValue(getTextContent(cn)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            onChild("icon", n, (cn) -> { a.addIcon(getTextContent(cn)); });
            onChild("attribute-name", n, (cn) -> { a.setAttributeName(getTextContent(cn)); });
            onChild("attribute-class", n, (cn) -> { a.setAttributeClass(getTextContent(cn)); });
            onChild("default-value", n, (cn) -> { a.setDefaultValue(getTextContent(cn)); });
            onChild("suggested-value", n, (cn) -> { a.setSuggestedValue(getTextContent(cn)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            onChild("attribute-name", n, (cn) -> { a.setAttributeName(getTextContent(cn)); });
            onChild("attribute-class", n, (cn) -> { a.setAttributeClass(getTextContent(cn)); });
            onChild("default-value", n, (cn) -> { a.setDefaultValue(getTextContent(cn)); });
            onChild("suggested-value", n, (cn) -> { a.setSuggestedValue(getTextContent(cn)); });
            onChild("attribute-extension", n, (cn) -> { a.addAttributeExtension(getTextContent(cn)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            onChild("attribute-class", n, (cn) -> { a.setAttributeClass(getTextContent(cn)); });
            onChild("default-value", n, (cn) -> { a.setDefaultValue(getTextContent(cn)); });
            onChild("suggested-value", n, (cn) -> { a.setSuggestedValue(getTextContent(cn)); });
            onChild("attribute-extension", n, (cn) -> { a.addAttributeExtension(getTextContent(cn)); });
            obj.addAttribute(a);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            onChild("default-value", n, (cn) -> { a.setDefaultValue(getTextContent(cn)); });
            onChild("suggested-value", n, (cn) -> { a.setSuggestedValue(getTextContent(cn)); });
            onChild("attribute-extension", n, (cn) -> { a.addAttributeExtension(getTextContent(cn)); });
            obj.addAttribute(a);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onChild("property", node, (n) -> {
            PropertyImpl p = new PropertyImpl();
            onChild("description", n, (cn) -> { p.addDescription(getTextContent(cn)); });
            onChild("display-name", n, (cn) -> { p.addDisplayName(getTextContent(cn)); });
            onChild("icon", n, (cn) -> { p.addIcon(getTextContent(cn)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            PropertyImpl p = new PropertyImpl();
            onChild("description", n, (cn) -> { p.addDescription(getTextContent(cn)); });
            onChild("display-name", n, (cn) -> { p.addDisplayName(getTextContent(cn)); });
            onChild("icon", n, (cn) -> { p.addIcon(getTextContent(cn)); });
            onChild("property-name", n, (cn) -> { p.setPropertyName(getTextContent(cn)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            onChild("description", n, (cn) -> { p.addDescription(getTextContent(cn)); });
            onChild("display-name", n, (cn) -> { p.addDisplayName(getTextContent(cn)); });
            onChild("icon", n, (cn) -> { p.addIcon(getTextContent(cn)); });
            onChild("property-name", n, (cn) -> { p.setPropertyName(getTextContent(cn)); });
            onChild("property-class", n, (cn) -> { p.setPropertyClass(getTextContent(cn)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            onChild("display-name", n, (cn) -> { p.addDisplayName(getTextContent(cn)); });
            onChild("icon", n, (cn) -> { p.addIcon(getTextContent(cn)); });
            onChild("property-name", n, (cn) -> { p.setPropertyName(getTextContent(cn)); });
            onChild("property-class", n, (cn) -> { p.setPropertyClass(getTextContent(cn)); });
            onChild("default-value", n, (cn) -> { p.setDefaultValue(getTextContent(cn)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            onChild("icon", n, (cn) -> { p.addIcon(getTextContent(cn)); });
            onChild("property-name", n, (cn) -> { p.setPropertyName(getTextContent(cn)); });
            onChild("property-class", n, (cn) -> { p.setPropertyClass(getTextContent(cn)); });
            onChild("default-value", n, (cn) -> { p.setDefaultValue(getTextContent(cn)); });
            onChild("suggested-value", n, (cn) -> { p.setSuggestedValue(getTextContent(cn)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            onChild("property-name", n, (cn) -> { p.setPropertyName(getTextContent(cn)); });
            onChild("property-class", n, (cn) -> { p.setPropertyClass(getTextContent(cn)); });
            onChild("default-value", n, (cn) -> { p.setDefaultValue(getTextContent(cn)); });
            onChild("suggested-value", n, (cn) -> { p.setSuggestedValue(getTextContent(cn)); });
            onChild("property-extension", n, (cn) -> { p.addPropertyExtension(getTextContent(cn)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            onChild("property-class", n, (cn) -> { p.setPropertyClass(getTextContent(cn)); });
            onChild("default-value", n, (cn) -> { p.setDefaultValue(getTextContent(cn)); });
            onChild("suggested-value", n, (cn) -> { p.setSuggestedValue(getTextContent(cn)); });
            onChild("property-extension", n, (cn) -> { p.addPropertyExtension(getTextContent(cn)); });
            obj.addProperty(p);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            onChild("default-value", n, (cn) -> { p.setDefaultValue(getTextContent(cn)); });
            onChild("suggested-value", n, (cn) -> { p.setSuggestedValue(getTextContent(cn)); });
            onChild("property-extension", n, (cn) -> { p.addPropertyExtension(getTextContent(cn)); });
            obj.addProperty(p);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        ConverterImpl obj = new ConverterImpl();

        onChild("converter-id", node, (n) -> { obj.setConverterId(getTextContent(n)); });
        onChild("converter-for-class", node, (n) -> { obj.setForClass(getTextContent(n)); });
        onChild("converter-class", node, (n) -> { obj.setConverterClass(getTextContent(n)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java

        onChild("converter-id", node, (n) -> { obj.setConverterId(getTextContent(n)); });
        onChild("converter-for-class", node, (n) -> { obj.setForClass(getTextContent(n)); });
        onChild("converter-class", node, (n) -> { obj.setConverterClass(getTextContent(n)); });
                
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onChild("converter-id", node, (n) -> { obj.setConverterId(getTextContent(n)); });
        onChild("converter-for-class", node, (n) -> { obj.setForClass(getTextContent(n)); });
        onChild("converter-class", node, (n) -> { obj.setConverterClass(getTextContent(n)); });
                
        onChild("attribute", node, (n) -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onChild("attribute", node, (n) -> {
            AttributeImpl a = new AttributeImpl();
            onChild("description", n, (cn) -> { a.addDescription(getTextContent(cn)); });
            onChild("display-name", n, (cn) -> { a.addDisplayName(getTextContent(cn)); });
            onChild("icon", n, (cn) -> { a.addIcon(getTextContent(cn)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            AttributeImpl a = new AttributeImpl();
            onChild("description", n, (cn) -> { a.addDescription(getTextContent(cn)); });
            onChild("display-name", n, (cn) -> { a.addDisplayName(getTextContent(cn)); });
            onChild("icon", n, (cn) -> { a.addIcon(getTextContent(cn)); });
            onChild("attribute-name", n, (cn) -> { a.setAttributeName(getTextContent(cn)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            onChild("description", n, (cn) -> { a.addDescription(getTextContent(cn)); });
            onChild("display-name", n, (cn) -> { a.addDisplayName(getTextContent(cn)); });
            onChild("icon", n, (cn) -> { a.addIcon(getTextContent(cn)); });
            onChild("attribute-name", n, (cn) -> { a.setAttributeName(getTextContent(cn)); });
            onChild("attribute-class", n, (cn) -> { a.setAttributeClass(getTextContent(cn)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            onChild("display-name", n, (cn) -> { a.addDisplayName(getTextContent(cn)); });
            onChild("icon", n, (cn) -> { a.addIcon(getTextContent(cn)); });
            onChild("attribute-name", n, (cn) -> { a.setAttributeName(getTextContent(cn)); });
            onChild("attribute-class", n, (cn) -> { a.setAttributeClass(getTextContent(cn)); });
            onChild("default-value", n, (cn) -> { a.setDefaultValue(getTextContent(cn)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            onChild("icon", n, (cn) -> { a.addIcon(getTextContent(cn)); });
            onChild("attribute-name", n, (cn) -> { a.setAttributeName(getTextContent(cn)); });
            onChild("attribute-class", n, (cn) -> { a.setAttributeClass(getTextContent(cn)); });
            onChild("default-value", n, (cn) -> { a.setDefaultValue(getTextContent(cn)); });
            onChild("suggested-value", n, (cn) -> { a.setSuggestedValue(getTextContent(cn)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            onChild("attribute-name", n, (cn) -> { a.setAttributeName(getTextContent(cn)); });
            onChild("attribute-class", n, (cn) -> { a.setAttributeClass(getTextContent(cn)); });
            onChild("default-value", n, (cn) -> { a.setDefaultValue(getTextContent(cn)); });
            onChild("suggested-value", n, (cn) -> { a.setSuggestedValue(getTextContent(cn)); });
            onChild("attribute-extension", n, (cn) -> { a.addAttributeExtension(getTextContent(cn)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            onChild("attribute-class", n, (cn) -> { a.setAttributeClass(getTextContent(cn)); });
            onChild("default-value", n, (cn) -> { a.setDefaultValue(getTextContent(cn)); });
            onChild("suggested-value", n, (cn) -> { a.setSuggestedValue(getTextContent(cn)); });
            onChild("attribute-extension", n, (cn) -> { a.addAttributeExtension(getTextContent(cn)); });
            obj.addAttribute(a);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            onChild("default-value", n, (cn) -> { a.setDefaultValue(getTextContent(cn)); });
            onChild("suggested-value", n, (cn) -> { a.setSuggestedValue(getTextContent(cn)); });
            onChild("attribute-extension", n, (cn) -> { a.addAttributeExtension(getTextContent(cn)); });
            obj.addAttribute(a);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onChild("property", node, (n) -> {
            PropertyImpl p = new PropertyImpl();
            onChild("description", n, (cn) -> { p.addDescription(getTextContent(cn)); });
            onChild("display-name", n, (cn) -> { p.addDisplayName(getTextContent(cn)); });
            onChild("icon", n, (cn) -> { p.addIcon(getTextContent(cn)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            PropertyImpl p = new PropertyImpl();
            onChild("description", n, (cn) -> { p.addDescription(getTextContent(cn)); });
            onChild("display-name", n, (cn) -> { p.addDisplayName(getTextContent(cn)); });
            onChild("icon", n, (cn) -> { p.addIcon(getTextContent(cn)); });
            onChild("property-name", n, (cn) -> { p.setPropertyName(getTextContent(cn)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            onChild("description", n, (cn) -> { p.addDescription(getTextContent(cn)); });
            onChild("display-name", n, (cn) -> { p.addDisplayName(getTextContent(cn)); });
            onChild("icon", n, (cn) -> { p.addIcon(getTextContent(cn)); });
            onChild("property-name", n, (cn) -> { p.setPropertyName(getTextContent(cn)); });
            onChild("property-class", n, (cn) -> { p.setPropertyClass(getTextContent(cn)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            onChild("display-name", n, (cn) -> { p.addDisplayName(getTextContent(cn)); });
            onChild("icon", n, (cn) -> { p.addIcon(getTextContent(cn)); });
            onChild("property-name", n, (cn) -> { p.setPropertyName(getTextContent(cn)); });
            onChild("property-class", n, (cn) -> { p.setPropertyClass(getTextContent(cn)); });
            onChild("default-value", n, (cn) -> { p.setDefaultValue(getTextContent(cn)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            onChild("icon", n, (cn) -> { p.addIcon(getTextContent(cn)); });
            onChild("property-name", n, (cn) -> { p.setPropertyName(getTextContent(cn)); });
            onChild("property-class", n, (cn) -> { p.setPropertyClass(getTextContent(cn)); });
            onChild("default-value", n, (cn) -> { p.setDefaultValue(getTextContent(cn)); });
            onChild("suggested-value", n, (cn) -> { p.setSuggestedValue(getTextContent(cn)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            onChild("property-name", n, (cn) -> { p.setPropertyName(getTextContent(cn)); });
            onChild("property-class", n, (cn) -> { p.setPropertyClass(getTextContent(cn)); });
            onChild("default-value", n, (cn) -> { p.setDefaultValue(getTextContent(cn)); });
            onChild("suggested-value", n, (cn) -> { p.setSuggestedValue(getTextContent(cn)); });
            onChild("property-extension", n, (cn) -> { p.addPropertyExtension(getTextContent(cn)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            onChild("property-class", n, (cn) -> { p.setPropertyClass(getTextContent(cn)); });
            onChild("default-value", n, (cn) -> { p.setDefaultValue(getTextContent(cn)); });
            onChild("suggested-value", n, (cn) -> { p.setSuggestedValue(getTextContent(cn)); });
            onChild("property-extension", n, (cn) -> { p.addPropertyExtension(getTextContent(cn)); });
            obj.addProperty(p);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            onChild("default-value", n, (cn) -> { p.setDefaultValue(getTextContent(cn)); });
            onChild("suggested-value", n, (cn) -> { p.setSuggestedValue(getTextContent(cn)); });
            onChild("property-extension", n, (cn) -> { p.addPropertyExtension(getTextContent(cn)); });
            obj.addProperty(p);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        RenderKitImpl obj = new RenderKitImpl();

        onChild("render-kit-id", node, (n) -> { obj.setId(getTextContent(n)); });
        onChild("render-kit-class", node, (n) -> { obj.addRenderKitClass(getTextContent(n)); });

```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java

        onChild("render-kit-id", node, (n) -> { obj.setId(getTextContent(n)); });
        onChild("render-kit-class", node, (n) -> { obj.addRenderKitClass(getTextContent(n)); });

        onChild("renderer", node, (n) -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onChild("renderer", node, (n) -> {
            RendererImpl r = new RendererImpl();
            onChild("component-family", n, (cn) -> { r.setComponentFamily(getTextContent(cn)); });
            onChild("renderer-type", n, (cn) -> { r.setRendererType(getTextContent(cn)); });
            onChild("renderer-class", n, (cn) -> { r.setRendererClass(getTextContent(cn)); });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            RendererImpl r = new RendererImpl();
            onChild("component-family", n, (cn) -> { r.setComponentFamily(getTextContent(cn)); });
            onChild("renderer-type", n, (cn) -> { r.setRendererType(getTextContent(cn)); });
            onChild("renderer-class", n, (cn) -> { r.setRendererClass(getTextContent(cn)); });
            obj.addRenderer(r);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            onChild("component-family", n, (cn) -> { r.setComponentFamily(getTextContent(cn)); });
            onChild("renderer-type", n, (cn) -> { r.setRendererType(getTextContent(cn)); });
            onChild("renderer-class", n, (cn) -> { r.setRendererClass(getTextContent(cn)); });
            obj.addRenderer(r);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onChild("client-behavior-renderer", node, (n) -> {
            ClientBehaviorRendererImpl r = new ClientBehaviorRendererImpl();
            onChild("client-behavior-renderer-type", n, (cn) -> { r.setRendererType(getTextContent(cn)); });
            onChild("client-behavior-renderer-class", n, (cn) -> { r.setRendererClass(getTextContent(cn)); });
            obj.addClientBehaviorRenderer(r);
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            ClientBehaviorRendererImpl r = new ClientBehaviorRendererImpl();
            onChild("client-behavior-renderer-type", n, (cn) -> { r.setRendererType(getTextContent(cn)); });
            onChild("client-behavior-renderer-class", n, (cn) -> { r.setRendererClass(getTextContent(cn)); });
            obj.addClientBehaviorRenderer(r);
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        NavigationRuleImpl obj = new NavigationRuleImpl();
        
        onChild("from-view-id", node, (n) -> { obj.setFromViewId(getTextContent(n)); });
                
        onChild("navigation-case", node, (n) -> {
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onChild("from-view-id", node, (n) -> { obj.setFromViewId(getTextContent(n)); });
                
        onChild("navigation-case", node, (n) -> {
            obj.addNavigationCase(processNavigationCase(n));
        });
```

### CodeBlock2Expr
Statement lambda can be replaced with expression lambda
in `impl/src/main/java/org/apache/myfaces/webapp/FacesInitializerImpl.java`
#### Snippet
```java
        {
            facesContext.getApplication().getViewHandler().getViews(facesContext, "/", 
                    ViewVisitOption.RETURN_AS_MINIMAL_IMPLICIT_OUTCOME).forEach(s -> {
                        facesServletRegistration.addMapping(s);
                    });
```

## RuleId[ruleID=EmptyMethod]
### EmptyMethod
Method only calls its super
in `api/src/main/java/jakarta/faces/FacesException.java`
#### Snippet
```java

    @Override
    public Throwable getCause()
    {
        return super.getCause();
```

### EmptyMethod
Method only calls its super
in `api/src/main/java/jakarta/faces/component/UIColumn.java`
#### Snippet
```java
    @JSFProperty(literalOnly=true, rtexprvalue=false)
    @Override
    public String getId()
    {
        return super.getId();
```

### EmptyMethod
Method only calls its super
in `api/src/main/java/jakarta/faces/component/UINamingContainer.java`
#### Snippet
```java
    @JSFProperty(deferredValueType="java.lang.Boolean")
    @Override
    public boolean isRendered()
    {
        return super.isRendered();
```

### EmptyMethod
Method only calls its super
in `api/src/main/java/jakarta/faces/component/UISelectBoolean.java`
#### Snippet
```java
    @JSFProperty(deferredValueType="java.lang.Boolean")
    @Override
    public Object getValue()
    {
        return super.getValue();
```

### EmptyMethod
Method only calls its super
in `api/src/main/java/jakarta/faces/component/UIViewParameter.java`
#### Snippet
```java
    @JSFProperty(tagExcluded=true)
    @Override
    public boolean isRendered()
    {
        return super.isRendered();
```

### EmptyMethod
Method only calls its super
in `api/src/main/java/jakarta/faces/component/_ComponentChildrenList.java`
#### Snippet
```java

    @Override
    public int size()
    {
        return super.size();
```

### EmptyMethod
Method only calls its super
in `api/src/main/java/jakarta/faces/component/html/_HtmlBody.java`
#### Snippet
```java
  @JSFProperty(tagExcluded=true)
  @Override
  public Converter getConverter()
  {
    return super.getConverter();
```

### EmptyMethod
Method only calls its super
in `api/src/main/java/jakarta/faces/component/html/_HtmlBody.java`
#### Snippet
```java
  @JSFProperty(tagExcluded=true)
  @Override
  public Object getValue()
  {
    return super.getValue();
```

### EmptyMethod
Method only calls its super
in `api/src/main/java/jakarta/faces/component/html/_HtmlBody.java`
#### Snippet
```java
  @JSFProperty(tagExcluded=true)
  @Override
  public String getId()
  {
    return super.getId();
```

### EmptyMethod
Method only calls its super
in `api/src/main/java/jakarta/faces/component/html/_HtmlBody.java`
#### Snippet
```java
  @JSFProperty(tagExcluded=true)
  @Override
  public boolean isRendered()
  {
    return super.isRendered();
```

### EmptyMethod
Method only calls its super
in `api/src/main/java/jakarta/faces/component/html/_HtmlColumn.java`
#### Snippet
```java
  @JSFExclude
  @Override
  public boolean isRendered()
  {
      return super.isRendered();
```

### EmptyMethod
Method only calls its super
in `api/src/main/java/jakarta/faces/component/html/_HtmlHead.java`
#### Snippet
```java
  @JSFProperty(tagExcluded=true)
  @Override
  public Converter getConverter()
  {
    return super.getConverter();
```

### EmptyMethod
Method only calls its super
in `api/src/main/java/jakarta/faces/component/html/_HtmlHead.java`
#### Snippet
```java
  @JSFProperty(tagExcluded=true)
  @Override
  public Object getValue()
  {
    return super.getValue();
```

### EmptyMethod
Method only calls its super
in `api/src/main/java/jakarta/faces/component/html/_HtmlHead.java`
#### Snippet
```java
  @JSFProperty(tagExcluded=true)
  @Override
  public String getId()
  {
    return super.getId();
```

### EmptyMethod
Method only calls its super
in `api/src/main/java/jakarta/faces/component/html/_HtmlHead.java`
#### Snippet
```java
  @JSFProperty(tagExcluded=true)
  @Override
  public boolean isRendered()
  {
    return super.isRendered();
```

### EmptyMethod
All implementations of this method are empty
in `api/src/main/java/jakarta/faces/component/html/_LinkProperties.java`
#### Snippet
```java
    @JSFProperty
    (defaultValue = "false")
    public abstract boolean isDisabled();

}
```

### EmptyMethod
The method is empty
in `api/src/main/java/jakarta/faces/view/facelets/TagAttribute.java`
#### Snippet
```java
     * @param tag 
     */
    public void setTag(Tag tag)
    {
    }
```

### EmptyMethod
The method is empty
in `api/src/main/java/jakarta/faces/view/facelets/TagAttributes.java`
#### Snippet
```java
     * @param tag 
     */
    public void setTag(Tag tag)
    {
    }
```

### EmptyMethod
The method is empty
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlLabelRenderer.java`
#### Snippet
```java
    }

    protected void encodeAfterStart(FacesContext facesContext, ResponseWriter writer, UIComponent uiComponent)
        throws IOException
    {
```

### EmptyMethod
The method is empty
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlLabelRenderer.java`
#### Snippet
```java
    }

    protected void encodeBefore(FacesContext facesContext, ResponseWriter writer, UIComponent uiComponent)
        throws IOException
    {
```

### EmptyMethod
The method is empty
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlLabelRenderer.java`
#### Snippet
```java
    }

    protected void encodeBeforeEnd(FacesContext facesContext, ResponseWriter writer, UIComponent uiComponent)
        throws IOException
    {
```

### EmptyMethod
The method is empty
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlLabelRenderer.java`
#### Snippet
```java
    }

    protected void encodeAfter(FacesContext facesContext, ResponseWriter writer, UIComponent uiComponent)
        throws IOException
    {
```

### EmptyMethod
The method is empty
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlFormRendererBase.java`
#### Snippet
```java
     * encodeBegin method
     */
    protected void beforeFormElementsStart(FacesContext facesContext, UIComponent component)
            throws IOException
    {
```

### EmptyMethod
The method is empty
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlFormRendererBase.java`
#### Snippet
```java
     * encodeBegin method
     */
    protected void afterFormElementsStart(FacesContext facesContext, UIComponent component)
            throws IOException
    {
```

### EmptyMethod
The method is empty
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlFormRendererBase.java`
#### Snippet
```java
     * encodeEnd method
     */
    protected void beforeFormElementsEnd(FacesContext facesContext, UIComponent component)
            throws IOException
    {
```

### EmptyMethod
The method is empty
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTableRendererBase.java`
#### Snippet
```java
     * @param uiData the <code>UIData</code> being rendered.
     */
    protected void beforeTable(FacesContext facesContext, UIData uiData) throws IOException
    {
    }
```

### EmptyMethod
The method is empty
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTableRendererBase.java`
#### Snippet
```java
     * @param uiData the <code>UIData</code> being rendered.
     */
    protected void beforeRow(FacesContext facesContext, UIData uiData) throws IOException
    {
    }
```

### EmptyMethod
The method is empty
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTableRendererBase.java`
#### Snippet
```java
     * @param uiData the <code>UIData</code> being rendered.
     */
    protected void afterRow(FacesContext facesContext, UIData uiData) throws IOException
    {
    }
```

### EmptyMethod
The method is empty
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTableRendererBase.java`
#### Snippet
```java
     * @param columnIndex the index of the currenly rendered column
     */
    protected void beforeColumn(FacesContext facesContext, UIData uiData, int columnIndex) throws IOException
    {        
    }
```

### EmptyMethod
The method is empty
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTableRendererBase.java`
#### Snippet
```java
     * @param columnIndex the index of the currenly rendered column
     */
    protected void afterColumn(FacesContext facesContext, UIData uiData, int columnIndex) throws IOException
    {        
    }
```

### EmptyMethod
The method is empty
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTableRendererBase.java`
#### Snippet
```java
     * @param columnIndex the index of the currenly rendered column
     */
    protected void beforeColumnHeaderOrFooter(FacesContext facesContext, UIData uiData, boolean header,
            int columnIndex) throws IOException
    {         
```

### EmptyMethod
The method is empty
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTableRendererBase.java`
#### Snippet
```java
     * @param columnIndex the index of the currenly rendered column
     */
    protected void afterColumnHeaderOrFooter(FacesContext facesContext, UIData uiData, boolean header,
            int columnIndex) throws IOException
    {         
```

### EmptyMethod
The method is empty
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTableRendererBase.java`
#### Snippet
```java
     * @param uiData the <code>UIData</code> being rendered.
     */
    protected void inBodyStart(FacesContext facesContext, UIData uiData) throws IOException
    {
    }
```

### EmptyMethod
The method is empty
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTableRendererBase.java`
#### Snippet
```java
     * @param uiData the <code>UIData</code> being rendered.
     */
    protected void afterBody(FacesContext facesContext, UIData uiData) throws IOException
    {
    }
```

### EmptyMethod
The method is empty
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTableRendererBase.java`
#### Snippet
```java
     * @param uiData the <code>UIData</code> being rendered.
     */
    protected void afterTable(FacesContext facesContext, UIData uiData) throws IOException
    {
    }
```

### EmptyMethod
The method is empty
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTextRendererBase.java`
#### Snippet
```java

    @Deprecated
    protected void renderInput(FacesContext facesContext, UIComponent component)
        throws IOException
    {
```

### EmptyMethod
All implementations of this method are empty
in `impl/src/main/java/org/apache/myfaces/spi/InjectionProviderFactory.java`
#### Snippet
```java
    public abstract InjectionProvider getInjectionProvider(ExternalContext externalContext);

    public abstract void release();

}
```

### EmptyMethod
Method only calls its super
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/ComponentRef.java`
#### Snippet
```java
    @JSFProperty(tagExcluded=true)
    @Override
    public boolean isRendered()
    {
        return super.isRendered();
```

### EmptyMethod
Method only calls its super
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/UIDebug.java`
#### Snippet
```java
    @JSFProperty(tagExcluded=true)
    @Override
    public String getId()
    {
        return super.getId();
```

### EmptyMethod
Method only calls its super
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/_Component.java`
#### Snippet
```java
    @JSFProperty(tagExcluded=true)
    @Override
    public boolean isRendered()
    {
        return super.isRendered();
```

### EmptyMethod
Method only calls its super
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/_Fragment.java`
#### Snippet
```java
    @JSFProperty(tagExcluded=true)
    @Override
    public boolean isRendered()
    {
        return super.isRendered();
```

### EmptyMethod
Method only calls its super
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletRequest.java`
#### Snippet
```java

    @Override
    public HttpServletMapping getHttpServletMapping()
    {
        return HttpServletRequest.super.getHttpServletMapping();
```

### EmptyMethod
Method only calls its super
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletRequest.java`
#### Snippet
```java

    @Override
    public PushBuilder newPushBuilder()
    {
        return HttpServletRequest.super.newPushBuilder();
```

### EmptyMethod
Method only calls its super
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletRequest.java`
#### Snippet
```java

    @Override
    public Map<String, String> getTrailerFields()
    {
        return HttpServletRequest.super.getTrailerFields();
```

### EmptyMethod
Method only calls its super
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletRequest.java`
#### Snippet
```java

    @Override
    public boolean isTrailerFieldsReady()
    {
        return HttpServletRequest.super.isTrailerFieldsReady();
```

## RuleId[ruleID=RedundantFieldInitialization]
### RedundantFieldInitialization
Field initialization to `false` is redundant
in `api/src/main/java/jakarta/faces/FactoryFinder.java`
#### Snippet
```java
    private static Object factoryFinderProviderFactoryInstance;
    
    private static volatile boolean initialized = false;
    
    private static void initializeFactoryFinderProviderFactory()
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `api/src/main/java/jakarta/faces/event/MethodExpressionValueChangeListener.java`
#### Snippet
```java
    private MethodExpression methodExpressionOneArg;
    private MethodExpression methodExpressionZeroArg;
    private boolean isTransient = false;

    /** Creates a new instance of MethodExpressionValueChangeListener */
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `api/src/main/java/jakarta/faces/event/MethodExpressionActionListener.java`
#### Snippet
```java
    private MethodExpression methodExpressionOneArg;
    private MethodExpression methodExpressionZeroArg;
    private boolean isTransient = false;

    /** Creates a new instance of MethodExpressionActionListener */
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `api/src/main/java/jakarta/faces/model/DataModel.java`
#### Snippet
```java

    private List<DataModelListener> _listeners;
    private DataModelListener[] _cachedListenersArray = null;

    public void addDataModelListener(DataModelListener listener)
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `api/src/main/java/jakarta/faces/model/DataModel.java`
#### Snippet
```java
    private class DataModelIterator implements Iterator<E>
    {
        private int nextRowIndex = 0;
        
        public DataModelIterator()
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `api/src/main/java/jakarta/faces/convert/EnumConverter.java`
#### Snippet
```java
    }
    
    private boolean _initialStateMarked = false;

    @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `api/src/main/java/jakarta/faces/convert/EnumConverter.java`
#### Snippet
```java
    private Class targetClass;

    private boolean isTransient = false;

    /** Creates a new instance of EnumConverter */
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `api/src/main/java/jakarta/faces/model/ResultSetDataModel.java`
#### Snippet
```java
    private static class WrapResultSetKeysIterator implements Iterator<String>
    {
        private Iterator<String> _keyIterator = null;

        public WrapResultSetKeysIterator(WrapResultSetMap map)
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `api/src/main/java/jakarta/faces/model/ResultSetDataModel.java`
#### Snippet
```java
     * The MetaData of the ResultSet being wrapped by this DataModel.
     */
    private ResultSetMetaData _resultSetMetadata = null;

    /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `api/src/main/java/jakarta/faces/model/ResultSetDataModel.java`
#### Snippet
```java
     * The ResultSet being wrapped by this DataModel.
     */
    private ResultSet _resultSet = null;

    /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `api/src/main/java/jakarta/faces/model/ResultSetDataModel.java`
#### Snippet
```java
    {
        private WrapResultSetMap _wrapMap = null;
        private Iterator<String> _keyIterator = null;

        public WrapResultSetEntriesIterator(WrapResultSetMap wrapMap)
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `api/src/main/java/jakarta/faces/model/ResultSetDataModel.java`
#### Snippet
```java
     * Indicator for an updated row at the current position.
     */
    private boolean _currentRowUpdated = false;

    public ResultSetDataModel()
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `api/src/main/java/jakarta/faces/model/ResultSetDataModel.java`
#### Snippet
```java
    private static class WrapResultSetEntriesIterator implements Iterator<Map.Entry<String, Object>>
    {
        private WrapResultSetMap _wrapMap = null;
        private Iterator<String> _keyIterator = null;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `api/src/main/java/jakarta/faces/convert/NumberConverter.java`
#### Snippet
```java
    }
    
    private boolean _initialStateMarked = false;

    @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `api/src/main/java/jakarta/faces/convert/NumberConverter.java`
#### Snippet
```java
    private String _type = "number";
    private boolean _groupingUsed = true;
    private boolean _integerOnly = false;
    private boolean _transient;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `api/src/main/java/jakarta/faces/component/UIComponent.java`
#### Snippet
```java
     */
    private transient Map<String, String> _resourceBundleMap = null;
    private boolean _inView = false;
    private _DeltaStateHelper _stateHelper = null;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `api/src/main/java/jakarta/faces/component/UIComponent.java`
#### Snippet
```java
     * to be implemented from here and internally it depends from this property.
     */
    private boolean _initialStateMarked = false;

    /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `api/src/main/java/jakarta/faces/component/UIComponent.java`
#### Snippet
```java
    private transient Map<String, String> _resourceBundleMap = null;
    private boolean _inView = false;
    private _DeltaStateHelper _stateHelper = null;

    /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `api/src/main/java/jakarta/faces/component/UIComponent.java`
#### Snippet
```java
     * transient is used).
     */
    private transient Map<String, String> _resourceBundleMap = null;
    private boolean _inView = false;
    private _DeltaStateHelper _stateHelper = null;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `api/src/main/java/jakarta/faces/convert/DateTimeConverter.java`
#### Snippet
```java
    }
    
    private boolean _initialStateMarked = false;

    @Override
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `api/src/main/java/jakarta/faces/component/_ComponentFacetMap.java`
#### Snippet
```java
    private UIComponent _component;
    private Map<String, V> _map = new _ArrayMap<>(0,5);
    private Set<Entry<String, V>> _entrySet = null;
    private Set<String> _keySet = null;
    private Collection<V> _valueCollection = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `api/src/main/java/jakarta/faces/component/_ComponentFacetMap.java`
#### Snippet
```java
    private Set<Entry<String, V>> _entrySet = null;
    private Set<String> _keySet = null;
    private Collection<V> _valueCollection = null;

    _ComponentFacetMap(UIComponent component)
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `api/src/main/java/jakarta/faces/component/_ComponentFacetMap.java`
#### Snippet
```java
    private Map<String, V> _map = new _ArrayMap<>(0,5);
    private Set<Entry<String, V>> _entrySet = null;
    private Set<String> _keySet = null;
    private Collection<V> _valueCollection = null;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `api/src/main/java/jakarta/faces/component/UISelectOne.java`
#### Snippet
```java
    public static final String INVALID_MESSAGE_ID = "jakarta.faces.component.UISelectOne.INVALID";

    private boolean selectItemValueFound = false;

    public UISelectOne()
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `api/src/main/java/jakarta/faces/component/_DeltaStateHelper.java`
#### Snippet
```java
    private Object[] _initialState;

    private boolean _transient = false;

    /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `api/src/main/java/jakarta/faces/component/UIComponentBase.java`
#### Snippet
```java
    private String _clientId = null;
    private String _id = null;
    private UIComponent _parent = null;
    private boolean _transient = false;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `api/src/main/java/jakarta/faces/component/UIComponentBase.java`
#### Snippet
```java
    private _DeltaList<FacesListener> _facesListeners = null;
    private String _clientId = null;
    private String _id = null;
    private UIComponent _parent = null;
    private boolean _transient = false;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `api/src/main/java/jakarta/faces/component/UIComponentBase.java`
#### Snippet
```java
    private Map<String, UIComponent> _facetMap = null;
    private _DeltaList<FacesListener> _facesListeners = null;
    private String _clientId = null;
    private String _id = null;
    private UIComponent _parent = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `api/src/main/java/jakarta/faces/component/UIComponentBase.java`
#### Snippet
```java
    static final int RESET_MODE_HARD = 2;

    private _ComponentAttributesMap _attributesMap = null;
    private _PassThroughAttributesMap _passthroughAttributesMap = null;
    private List<UIComponent> _childrenList = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `api/src/main/java/jakarta/faces/component/UIComponentBase.java`
#### Snippet
```java
     *  
     */
    private Map<String, List<ClientBehavior>> _behaviorsMap = null;
    private transient Map<String, List<ClientBehavior>> _unmodifiableBehaviorsMap = null;
    
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `api/src/main/java/jakarta/faces/component/UIComponentBase.java`
#### Snippet
```java
    private List<UIComponent> _childrenList = null;
    private Map<String, UIComponent> _facetMap = null;
    private _DeltaList<FacesListener> _facesListeners = null;
    private String _clientId = null;
    private String _id = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `api/src/main/java/jakarta/faces/component/UIComponentBase.java`
#### Snippet
```java

    private _ComponentAttributesMap _attributesMap = null;
    private _PassThroughAttributesMap _passthroughAttributesMap = null;
    private List<UIComponent> _childrenList = null;
    private Map<String, UIComponent> _facetMap = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `api/src/main/java/jakarta/faces/component/UIComponentBase.java`
#### Snippet
```java
    private _ComponentAttributesMap _attributesMap = null;
    private _PassThroughAttributesMap _passthroughAttributesMap = null;
    private List<UIComponent> _childrenList = null;
    private Map<String, UIComponent> _facetMap = null;
    private _DeltaList<FacesListener> _facesListeners = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `api/src/main/java/jakarta/faces/component/UIComponentBase.java`
#### Snippet
```java
     */
    private Map<String, List<ClientBehavior>> _behaviorsMap = null;
    private transient Map<String, List<ClientBehavior>> _unmodifiableBehaviorsMap = null;
    
    private transient FacesContext _facesContext;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `api/src/main/java/jakarta/faces/component/UIComponentBase.java`
#### Snippet
```java
    private String _id = null;
    private UIComponent _parent = null;
    private boolean _transient = false;

    private String _rendererType;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `api/src/main/java/jakarta/faces/component/UIComponentBase.java`
#### Snippet
```java
    private String _facetName;
    
    private int _capabilities = 0;
    private final static int FLAG_IS_RENDERER_TYPE_SET = 1;
    private final static int FLAG_ADDED_BY_HANDLER = 2;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `api/src/main/java/jakarta/faces/component/UIComponentBase.java`
#### Snippet
```java
    private _PassThroughAttributesMap _passthroughAttributesMap = null;
    private List<UIComponent> _childrenList = null;
    private Map<String, UIComponent> _facetMap = null;
    private _DeltaList<FacesListener> _facesListeners = null;
    private String _clientId = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `api/src/main/java/jakarta/faces/component/_ComponentAttributesMap.java`
#### Snippet
```java
    // introspection on the associated UIComponent. Don't serialize this as
    // it can always be recreated when needed.
    private transient Map<String, ? extends PropertyDescriptorWrapper> _propertyDescriptorMap = null;

    private boolean _isCompositeComponent;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `api/src/main/java/jakarta/faces/component/behavior/AjaxBehavior.java`
#### Snippet
```java

    //To enable delta state saving we need this one
    private _DeltaStateHelper<AjaxBehavior> _stateHelper = null;

    public AjaxBehavior() 
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `api/src/main/java/jakarta/faces/validator/MethodExpressionValidator.java`
#### Snippet
```java
    private MethodExpression methodExpression;

    private boolean isTransient = false;

    /** Creates a new instance of MethodExpressionValidator */
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `api/src/main/java/jakarta/faces/validator/RegexValidator.java`
#### Snippet
```java
    private String pattern;

    private boolean _transient = false;
    private boolean _initialStateMarked = false;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `api/src/main/java/jakarta/faces/validator/RegexValidator.java`
#### Snippet
```java

    private boolean _transient = false;
    private boolean _initialStateMarked = false;

    // VALIDATE
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `api/src/main/java/jakarta/faces/validator/DoubleRangeValidator.java`
#### Snippet
```java
    private Double _maximum = null;
    private boolean _transient = false;
    private boolean _initialStateMarked = false;

    // CONSTRUCTORS
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `api/src/main/java/jakarta/faces/validator/DoubleRangeValidator.java`
#### Snippet
```java
    
    private Double _minimum = null;
    private Double _maximum = null;
    private boolean _transient = false;
    private boolean _initialStateMarked = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `api/src/main/java/jakarta/faces/validator/DoubleRangeValidator.java`
#### Snippet
```java
    private Double _minimum = null;
    private Double _maximum = null;
    private boolean _transient = false;
    private boolean _initialStateMarked = false;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `api/src/main/java/jakarta/faces/validator/DoubleRangeValidator.java`
#### Snippet
```java
    public static final String NOT_IN_RANGE_MESSAGE_ID = "jakarta.faces.validator.DoubleRangeValidator.NOT_IN_RANGE";
    
    private Double _minimum = null;
    private Double _maximum = null;
    private boolean _transient = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `api/src/main/java/jakarta/faces/validator/LongRangeValidator.java`
#### Snippet
```java
    private Long _maximum = null;
    private boolean _transient = false;
    private boolean _initialStateMarked = false;

    // CONSTRUCTORS
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `api/src/main/java/jakarta/faces/validator/LongRangeValidator.java`
#### Snippet
```java
    
    private Long _minimum = null;
    private Long _maximum = null;
    private boolean _transient = false;
    private boolean _initialStateMarked = false;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `api/src/main/java/jakarta/faces/validator/LongRangeValidator.java`
#### Snippet
```java
    public static final String NOT_IN_RANGE_MESSAGE_ID = "jakarta.faces.validator.LongRangeValidator.NOT_IN_RANGE";
    
    private Long _minimum = null;
    private Long _maximum = null;
    private boolean _transient = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `api/src/main/java/jakarta/faces/validator/LongRangeValidator.java`
#### Snippet
```java
    private Long _minimum = null;
    private Long _maximum = null;
    private boolean _transient = false;
    private boolean _initialStateMarked = false;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `api/src/main/java/jakarta/faces/validator/LengthValidator.java`
#### Snippet
```java
    public static final String     VALIDATOR_ID        = "jakarta.faces.Length";

    private Integer _minimum = null;
    private Integer _maximum = null;
    private boolean _transient = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `api/src/main/java/jakarta/faces/validator/LengthValidator.java`
#### Snippet
```java
    private Integer _maximum = null;
    private boolean _transient = false;
    private boolean _initialStateMarked = false;

    // CONSTRUCTORS
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `api/src/main/java/jakarta/faces/validator/LengthValidator.java`
#### Snippet
```java

    private Integer _minimum = null;
    private Integer _maximum = null;
    private boolean _transient = false;
    private boolean _initialStateMarked = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `api/src/main/java/jakarta/faces/validator/LengthValidator.java`
#### Snippet
```java
    private Integer _minimum = null;
    private Integer _maximum = null;
    private boolean _transient = false;
    private boolean _initialStateMarked = false;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `api/src/main/java/jakarta/faces/component/behavior/_DeltaStateHelper.java`
#### Snippet
```java
    private Map<Serializable, Object> _deltas;

    private boolean _transient = false;

    public _DeltaStateHelper(A target)
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `api/src/main/java/jakarta/faces/validator/BeanValidator.java`
#### Snippet
```java
    private boolean isTransient = false;

    private boolean _initialStateMarked = false;

    /**
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `api/src/main/java/jakarta/faces/validator/BeanValidator.java`
#### Snippet
```java
    private Class<?>[] validationGroupsArray;

    private boolean isTransient = false;

    private boolean _initialStateMarked = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `api/src/main/java/jakarta/faces/component/behavior/_AjaxBehaviorDeltaStateHelper.java`
#### Snippet
```java
    private Map<Serializable, Object> _deltas;

    private boolean _transient = false;

    public _AjaxBehaviorDeltaStateHelper(A component)
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `api/src/main/java/jakarta/faces/component/UIViewRoot.java`
#### Snippet
```java
     * whatever without change UIViewRoot implementation.
     */
    private static Class<?> VIEW_SCOPE_PROXY_MAP_CLASS = null;
    
    private static Class<?> REQUEST_VIEW_CONTEXT_CLASS = null;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `api/src/main/java/jakarta/faces/component/UIViewRoot.java`
#### Snippet
```java
     */
    private Map<String, Object> _viewScope;
    private transient boolean _restoreViewScopeStateCalled = false;

    private transient Lifecycle _lifecycle = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `api/src/main/java/jakarta/faces/component/UIViewRoot.java`
#### Snippet
```java
            = "jakarta.faces.VIEWROOT_PHASE_LISTENER_QUEUES_EXCEPTIONS";

    private transient Logger logger = null;

    private static final PhaseProcessor APPLY_REQUEST_VALUES_PROCESSOR = new ApplyRequestValuesPhaseProcessor();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `api/src/main/java/jakarta/faces/component/UIViewRoot.java`
#### Snippet
```java
    private static Class<?> VIEW_SCOPE_PROXY_MAP_CLASS = null;
    
    private static Class<?> REQUEST_VIEW_CONTEXT_CLASS = null;
    private static Method REQUEST_VIEW_CONTEXT_GET_INSTANCE = null;
    private static Method REQUEST_VIEW_CONTEXT_SET_RENDER_TARGET = null;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `api/src/main/java/jakarta/faces/component/UIViewRoot.java`
#### Snippet
```java
    private static final String SKIP_VIEW_MAP_SAVE_STATE = "oam.viewPool.SKIP_VIEW_MAP_SAVE_STATE";
    
    private transient int _resetSaveStateMode = 0;
    private transient boolean _resourceDependencyUniqueId;
    private transient Map<String,Object> _attributesMap;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `api/src/main/java/jakarta/faces/component/UIViewRoot.java`
#### Snippet
```java
    private transient boolean _restoreViewScopeStateCalled = false;

    private transient Lifecycle _lifecycle = null;
    
    private HashMap<Class<? extends SystemEvent>, List<SystemEventListener>> _systemEventListeners;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `api/src/main/java/jakarta/faces/component/UIViewRoot.java`
#### Snippet
```java
    
    private static Class<?> REQUEST_VIEW_CONTEXT_CLASS = null;
    private static Method REQUEST_VIEW_CONTEXT_GET_INSTANCE = null;
    private static Method REQUEST_VIEW_CONTEXT_SET_RENDER_TARGET = null;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `api/src/main/java/jakarta/faces/component/UIViewRoot.java`
#### Snippet
```java
    private static Class<?> REQUEST_VIEW_CONTEXT_CLASS = null;
    private static Method REQUEST_VIEW_CONTEXT_GET_INSTANCE = null;
    private static Method REQUEST_VIEW_CONTEXT_SET_RENDER_TARGET = null;

    static
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `api/src/main/java/jakarta/faces/application/StateManager.java`
#### Snippet
```java
                       tags="performance", group="state")
    public static final String PARTIAL_STATE_SAVING_PARAM_NAME = "jakarta.faces.PARTIAL_STATE_SAVING";
    private Boolean _savingStateInClient = null;

    public static final String IS_BUILDING_INITIAL_STATE = "jakarta.faces.IS_BUILDING_INITIAL_STATE";
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `api/src/main/java/jakarta/faces/component/UIData.java`
#### Snippet
```java
    private boolean _isValidChilds = true;

    private Object _initialDescendantComponentState = null;
    
    private Object _initialDescendantFullComponentState = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `api/src/main/java/jakarta/faces/component/UIData.java`
#### Snippet
```java
    private Object _initialDescendantComponentState = null;
    
    private Object _initialDescendantFullComponentState = null;

    private static class FacesEventWrapper extends FacesEvent
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `impl/src/main/java/org/apache/myfaces/el/TracingELResolverPhaseListener.java`
#### Snippet
```java
    private static class TracingInfo
    {
        int getValue = 0;
        int getValueHits = 0;
        int getType = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `impl/src/main/java/org/apache/myfaces/el/TracingELResolverPhaseListener.java`
#### Snippet
```java
        int getValue = 0;
        int getValueHits = 0;
        int getType = 0;
        int getTypeHits = 0;
    }
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `impl/src/main/java/org/apache/myfaces/el/TracingELResolverPhaseListener.java`
#### Snippet
```java
        int getValueHits = 0;
        int getType = 0;
        int getTypeHits = 0;
    }

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `impl/src/main/java/org/apache/myfaces/el/TracingELResolverPhaseListener.java`
#### Snippet
```java
    {
        int getValue = 0;
        int getValueHits = 0;
        int getType = 0;
        int getTypeHits = 0;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/cdi/config/FacesConfigBeanHolder.java`
#### Snippet
```java
public class FacesConfigBeanHolder
{
    private FacesConfig.Version facesConfigVersion = null;

    public FacesConfig.Version getFacesConfigVersion()
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/cdi/model/FacesDataModelManager.java`
#### Snippet
```java
public class FacesDataModelManager
{
    private volatile Map<Class<?>, Class<? extends DataModel>> facesDataModels = null;

    public Map<Class<?>, Class<? extends DataModel>> getFacesDataModels()
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/cdi/wrapper/FacesClientBehaviorCDIWrapper.java`
#### Snippet
```java
    private String behaviorId;
    private boolean _transient;
    private boolean _initialStateMarked = false;

    public FacesClientBehaviorCDIWrapper()
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/cdi/wrapper/FacesBehaviorCDIWrapper.java`
#### Snippet
```java
    private String behaviorId;
    private boolean _transient;
    private boolean _initialStateMarked = false;

    public FacesBehaviorCDIWrapper()
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/cdi/wrapper/FacesConverterCDIWrapper.java`
#### Snippet
```java
    }

    private boolean _initialStateMarked = false;

    @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/cdi/wrapper/FacesValidatorCDIWrapper.java`
#### Snippet
```java
    private String validatorId;
    private boolean _transient;
    private boolean _initialStateMarked = false;

    public FacesValidatorCDIWrapper()
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/spi/FactoryFinderProviderFactory.java`
#### Snippet
```java
public abstract class FactoryFinderProviderFactory
{
    private static volatile FactoryFinderProviderFactory instance = null;
    
    /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/spi/impl/Tomcat7AnnotationInjectionProvider.java`
#### Snippet
```java
{

    private WeakHashMap<ClassLoader, InstanceManager> instanceManagers = null;

    public Tomcat7AnnotationInjectionProvider(ExternalContext externalContext)
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/spi/impl/DefaultServiceProviderFinder.java`
#### Snippet
```java
    private static final String META_INF_SERVICES = "META-INF/services/";
    
    private Map<String, List<String>> knownServicesMap = null;

    protected Set<URL> getURLs(String spiClass)
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/flow/cdi/FlowScopeMap.java`
#### Snippet
```java
    private String currentClientWindowFlowId;
    private FlowScopeContextualStorageHolder storageHolder;
    private boolean beanHolderInitialized = false;

    public FlowScopeMap(BeanManager beanManager, String currentClientWindowFlowId)
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/push/WebsocketSessionClusterSerializedRestore.java`
#### Snippet
```java
    private String channelToken;
    
    private boolean deserialized = false;

    public WebsocketSessionClusterSerializedRestore()
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/util/lang/StringCharArrayAccessor.java`
#### Snippet
```java

    static volatile boolean enabled = !Boolean.getBoolean("oam.stringchararrayaccessor.disabled");
    static volatile boolean jdk7String = false;
    
    static Field valueField;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/util/lang/FastByteArrayInputStream.java`
#### Snippet
```java
     * Our byte buffer
     */
    protected byte[] buf = null;

    /**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `impl/src/main/java/org/apache/myfaces/util/lang/FastByteArrayInputStream.java`
#### Snippet
```java
     * Number of bytes that we can read from the buffer
     */
    protected int count = 0;

    /**
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `impl/src/main/java/org/apache/myfaces/util/lang/FastByteArrayInputStream.java`
#### Snippet
```java
     * Number of bytes that have been read from the buffer
     */
    protected int pos = 0;

    public FastByteArrayInputStream(byte[] buf)
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/util/lang/FastByteArrayOutputStream.java`
#### Snippet
```java
public class FastByteArrayOutputStream extends OutputStream
{
    protected byte[] buf = null;
    protected int size = 0;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `impl/src/main/java/org/apache/myfaces/util/lang/FastByteArrayOutputStream.java`
#### Snippet
```java
{
    protected byte[] buf = null;
    protected int size = 0;

    public FastByteArrayOutputStream()
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/util/token/SessionIdGenerator.java`
#### Snippet
```java
     * specified, an instance of {@link SecureRandom} will be generated.
     */
    private String secureRandomClass = null;
    
    /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/util/token/SessionIdGenerator.java`
#### Snippet
```java
     * SecureRandom} instances will be created using platform defaults.
     */
    private String secureRandomProvider = null;
    
    /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/SwitchableWriter.java`
#### Snippet
```java
class SwitchableWriter extends Writer
{
    Writer _delegate = null;
    ResponseSwitch _responseSwitch = null;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/SwitchableWriter.java`
#### Snippet
```java
{
    Writer _delegate = null;
    ResponseSwitch _responseSwitch = null;

    public SwitchableWriter(Writer delegate, ResponseSwitch responseSwitch)
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/SwitchableOutputStream.java`
#### Snippet
```java

    ServletOutputStream _delegate = null;
    ResponseSwitch _responseSwitch = null;

    public SwitchableOutputStream(ServletOutputStream delegate, ResponseSwitch responseSwitch)
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/SwitchableOutputStream.java`
#### Snippet
```java
{

    ServletOutputStream _delegate = null;
    ResponseSwitch _responseSwitch = null;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/ViewDeclarationLanguageFactoryImpl.java`
#### Snippet
```java
    private volatile ViewDeclarationLanguageStrategy[] _supportedLanguages;
    
    private volatile List<ViewDeclarationLanguage> _supportedVDLs = null;

    public ViewDeclarationLanguageFactoryImpl()
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguageBase.java`
#### Snippet
```java
    private VisitContextFactory _visitContextFactory = null;
    
    private RenderKitFactory _renderKitFactory = null;

    protected VisitContextFactory getVisitContextFactory()
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguageBase.java`
#### Snippet
```java
    private static final String DYNAMIC_COMPONENT_NEEDS_REFRESH = "oam.vf.DC_NEEDS_REFRESH";
    
    private VisitContextFactory _visitContextFactory = null;
    
    private RenderKitFactory _renderKitFactory = null;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/util/lang/StreamCharBuffer.java`
#### Snippet
```java
    int allocatedBufferIdSequence = 0;
    int readerCount = 0;
    boolean hasReaders = false;

    public StreamCharBuffer()
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `impl/src/main/java/org/apache/myfaces/util/lang/StreamCharBuffer.java`
#### Snippet
```java
    {
        boolean eofException = false;
        int eofReachedCounter = 0;
        ChunkReader chunkReader;
        ChunkReader lastChunkReader;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/util/lang/StreamCharBuffer.java`
#### Snippet
```java
    final public class StreamCharBufferReader extends Reader
    {
        boolean eofException = false;
        int eofReachedCounter = 0;
        ChunkReader chunkReader;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/util/lang/StreamCharBuffer.java`
#### Snippet
```java
    public final class StreamCharBufferWriter extends Writer
    {
        boolean closed = false;
        int writerUsedCounter = 0;
        boolean increaseCounter = true;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `impl/src/main/java/org/apache/myfaces/util/lang/StreamCharBuffer.java`
#### Snippet
```java
    private Set<SoftReference<StreamCharBufferKey>> parentBuffers;
    int allocatedBufferIdSequence = 0;
    int readerCount = 0;
    boolean hasReaders = false;

```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `impl/src/main/java/org/apache/myfaces/util/lang/StreamCharBuffer.java`
#### Snippet
```java
    {
        char[] buf;
        int count = 0;

        public FixedCharArrayWriter(int fixedSize)
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `impl/src/main/java/org/apache/myfaces/util/lang/StreamCharBuffer.java`
#### Snippet
```java

    private Set<SoftReference<StreamCharBufferKey>> parentBuffers;
    int allocatedBufferIdSequence = 0;
    int readerCount = 0;
    boolean hasReaders = false;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `impl/src/main/java/org/apache/myfaces/util/lang/StreamCharBuffer.java`
#### Snippet
```java
        private char[] buffer;
        private int used = 0;
        private int chunkStart = 0;

        public AllocatedBuffer(int size)
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `impl/src/main/java/org/apache/myfaces/util/lang/StreamCharBuffer.java`
#### Snippet
```java
    {
        boolean closed = false;
        int writerUsedCounter = 0;
        boolean increaseCounter = true;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/util/lang/StreamCharBuffer.java`
#### Snippet
```java
    private Writer connectedWritersWriter;

    boolean preferSubChunkWhenWritingToOtherBuffer = false;

    private AllocatedBuffer allocBuffer;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `impl/src/main/java/org/apache/myfaces/util/lang/StreamCharBuffer.java`
#### Snippet
```java
        private int size;
        private char[] buffer;
        private int used = 0;
        private int chunkStart = 0;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/util/lang/ConcurrentLRUCache.java`
#### Snippet
```java
        private WeakReference<ConcurrentLRUCache> cache;

        private boolean stop = false;

        public CleanupThread(ConcurrentLRUCache c)
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `impl/src/main/java/org/apache/myfaces/util/lang/ConcurrentLRUCache.java`
#### Snippet
```java
        V value;
        volatile long lastAccessed = 0;
        long lastAccessedCopy = 0;

        public CacheEntry(K key, V value, long lastAccessed)
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/util/lang/ConcurrentLRUCache.java`
#### Snippet
```java
    private final int lowerWaterMark;
    private final ReentrantLock markAndSweepLock = new ReentrantLock(true);
    private boolean isCleaning = false; // not volatile... piggybacked on other volatile vars
    private final boolean newThreadForCleanup;
    private volatile boolean islive = true;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `impl/src/main/java/org/apache/myfaces/util/lang/ConcurrentLRUCache.java`
#### Snippet
```java
        K key;
        V value;
        volatile long lastAccessed = 0;
        long lastAccessedCopy = 0;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/util/lang/ConcurrentLRUCache.java`
#### Snippet
```java
    }

    private boolean isDestroyed = false;

    public void destroy()
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `impl/src/main/java/org/apache/myfaces/util/lang/ConcurrentLRUCache.java`
#### Snippet
```java
    private final Stats stats = new Stats();
    private final int acceptableWaterMark;
    private long oldestEntry = 0; // not volatile, only accessed in the cleaning method
    private final EvictionListener<K, V> evictionListener;
    private CleanupThread cleanupThread;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/DefaultFunctionMapper.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;

    private Map<String, Function> _functions = null;

    /*
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/DebugPhaseListener.java`
#### Snippet
```java
    }
    
    private boolean _afterPhase = false;
    private PhaseId _currentPhase;
    private DebugVisitCallback _visitCallback = new DebugVisitCallback();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/PartialStateManagementStrategy.java`
#### Snippet
```java
        private String _componentClass;
        private String _componentId;
        private TreeStructComponent[] _children = null; // Array of children
        private Object[] _facets = null; // Array of Array-tuples with Facetname and TreeStructComponent

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/PartialStateManagementStrategy.java`
#### Snippet
```java
    private RenderKitFactory _renderKitFactory = null;
    
    private VisitContextFactory _visitContextFactory = null;
    
    private String checkIdsProductionMode;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/PartialStateManagementStrategy.java`
#### Snippet
```java
        private String _componentId;
        private TreeStructComponent[] _children = null; // Array of children
        private Object[] _facets = null; // Array of Array-tuples with Facetname and TreeStructComponent

        TreeStructComponent(String componentClass, String componentId)
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/PartialStateManagementStrategy.java`
#### Snippet
```java
    private ViewDeclarationLanguageFactory _vdlFactory;
    
    private RenderKitFactory _renderKitFactory = null;
    
    private VisitContextFactory _visitContextFactory = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/PageContextImpl.java`
#### Snippet
```java
{
    
    private Map<String, ValueExpression> _attributes = null;
    
    private boolean _isCacheELExpressions;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/TemplateContextImpl.java`
#### Snippet
```java
        private Set<String> _names;
        private final PageContext _pageContext;
        private Map<String, ValueExpression> _parameters = null;
        private Set<String> _knownParameters;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/FaceletCompositionContextImpl.java`
#### Snippet
```java
    private List<Integer> _dynamicOldDeletionLevel;
    
    private VisitContextFactory _visitContextFactory = null;
    private UIViewRoot _viewRoot = null;
    private MyfacesConfig myfacesConfig;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/FaceletCompositionContextImpl.java`
#### Snippet
```java
    
    private VisitContextFactory _visitContextFactory = null;
    private UIViewRoot _viewRoot = null;
    private MyfacesConfig myfacesConfig;
    
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/FaceletCompositionContextImpl.java`
#### Snippet
```java
    private boolean _oldRefreshingTransientBuild;
    private boolean _dynamicComponentTopLevel;
    private int _dynamicComponentSection = 0;
    
    private List<Integer> _dynamicOldDeletionLevel;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/Compiler.java`
#### Snippet
```java

    private boolean validating = false;
    private boolean trimmingWhitespace = false;
    private boolean trimmingComments = false;
    private final List<TagLibrary> libraries = new ArrayList<>();
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/Compiler.java`
#### Snippet
```java
    private boolean validating = false;
    private boolean trimmingWhitespace = false;
    private boolean trimmingComments = false;
    private final List<TagLibrary> libraries = new ArrayList<>();
    private final List<TagDecorator> decorators = new ArrayList<>();
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/Compiler.java`
#### Snippet
```java
    private static final TagLibrary EMPTY_LIBRARY = new CompositeTagLibrary(new TagLibrary[0]);

    private boolean validating = false;
    private boolean trimmingWhitespace = false;
    private boolean trimmingComments = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/Compiler.java`
#### Snippet
```java
    private final List<TagDecorator> decorators = new ArrayList<>();
    private final Map<String, String> features = new HashMap<>();
    private boolean developmentProjectStage = false;
    private Collection<FaceletsProcessing> faceletsProcessingConfigurations;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/UILeaf.java`
#### Snippet
```java
            = UILeaf.class.getName() + ".SHARED_STRING_BUILDER";
    
    private String _clientId = null;
    
    private String _id = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/UILeaf.java`
#### Snippet
```java
    //-------------- START ATTRIBUTE MAP IMPLEMENTATION ----------------

    private Map<String, Object> _attributes = null;
    private String _markCreated = null;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/UILeaf.java`
#### Snippet
```java
    private String _clientId = null;
    
    private String _id = null;

    @Override
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/UILeaf.java`
#### Snippet
```java

    private Map<String, Object> _attributes = null;
    private String _markCreated = null;

    public void setMarkCreated(String markCreated)
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigEntityResolver.java`
#### Snippet
```java
            = "org.apache.myfaces.resource".replace('.', '/') + "/web-facesconfig_1_1.dtd";

    private ExternalContext _externalContext = null;

    public FacesConfigEntityResolver(ExternalContext context)
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/SAXCompiler.java`
#### Snippet
```java
        
        private boolean consumingCDATA = false;
        private boolean swallowCDATAContent = false;

        public CompilationHandler(CompilationManager unit, String alias)
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/SAXCompiler.java`
#### Snippet
```java

        private boolean consumingCDATA = false;
        private boolean swallowCDATAContent = false;

        public CompositeComponentMetadataHandler(CompilationManager unit, String alias)
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/SAXCompiler.java`
#### Snippet
```java
        private boolean inCompositeInterface = false;
        
        private boolean inCompositeImplementation = false;

        private boolean consumingCDATA = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/SAXCompiler.java`
#### Snippet
```java
        private final String alias;

        private boolean inDocument = false;

        private Locator locator;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/SAXCompiler.java`
#### Snippet
```java
        
        private boolean inMetadata = false;
        private int uiRemoveCount = 0;
        
        private boolean consumingCDATA = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/SAXCompiler.java`
#### Snippet
```java
        private final CompilationManager unit;
        
        private boolean consumingCDATA = false;
        private boolean swallowCDATAContent = false;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/SAXCompiler.java`
#### Snippet
```java
        
        private boolean consumingCDATA = false;
        private boolean swallowCDATAContent = false;

        public ViewMetadataHandler(CompilationManager unit, String alias)
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/SAXCompiler.java`
#### Snippet
```java
        private final CompilationManager unit;
        
        private boolean inMetadata = false;
        private int uiRemoveCount = 0;
        
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/SAXCompiler.java`
#### Snippet
```java
        private final String alias;

        private boolean inDocument = false;

        private Locator locator;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/SAXCompiler.java`
#### Snippet
```java
        private int uiRemoveCount = 0;
        
        private boolean consumingCDATA = false;
        private boolean swallowCDATAContent = false;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/SAXCompiler.java`
#### Snippet
```java
        private final String alias;

        private boolean inDocument = false;

        private Locator locator;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/SAXCompiler.java`
#### Snippet
```java
        private boolean inCompositeImplementation = false;

        private boolean consumingCDATA = false;
        private boolean swallowCDATAContent = false;

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/SAXCompiler.java`
#### Snippet
```java
        private final CompilationManager unit;
        
        private boolean inCompositeInterface = false;
        
        private boolean inCompositeImplementation = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/config/RuntimeConfig.java`
#### Snippet
```java

    private final Collection<NavigationRule> _navigationRules = new ArrayList<>();
    private boolean _navigationRulesChanged = false;
    private final Map<String, ResourceBundle> _resourceBundles = new HashMap<>();
    
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ApplicationImpl.java`
#### Snippet
```java
    private List<String> searchExpressionHandler;
    
    private boolean defaultValidatorsPresent = false;
    
    public void addActionListener(String listener)
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ConverterImpl.java`
#### Snippet
```java
    private String converterClass;
    private List<org.apache.myfaces.config.element.Property> properties = null;
    private List<org.apache.myfaces.config.element.Attribute> attributes = null;

    @Override
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ConverterImpl.java`
#### Snippet
```java
    private String forClass;
    private String converterClass;
    private List<org.apache.myfaces.config.element.Property> properties = null;
    private List<org.apache.myfaces.config.element.Attribute> attributes = null;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/component/UIRepeat.java`
#### Snippet
```java
    private static final Object[] LEAF_NO_STATE = new Object[]{null,null};
    
    private Object _initialDescendantComponentState = null;

    // Holds for each row the states of the child components of this UIData.
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/view/facelets/component/UIRepeat.java`
#### Snippet
```java
    private boolean _isValidChilds = true;
    
    private boolean _emptyModel = false;

    private int _end = -1;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ComponentImpl.java`
#### Snippet
```java
    private String componentType;
    private String componentClass;
    private List<org.apache.myfaces.config.element.Property> properties = null;
    private List<org.apache.myfaces.config.element.Attribute> attributes = null;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ComponentImpl.java`
#### Snippet
```java
    private String componentClass;
    private List<org.apache.myfaces.config.element.Property> properties = null;
    private List<org.apache.myfaces.config.element.Attribute> attributes = null;

    public ComponentImpl()
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/context/PartialResponseWriterImpl.java`
#### Snippet
```java

    ResponseWriter _cdataDoubleBufferWriter = null;
    Writer _doubleBuffer = null;
    List<StackEntry> _nestingStack = new ArrayList<StackEntry>(4);

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/context/PartialResponseWriterImpl.java`
#### Snippet
```java
    }

    ResponseWriter _cdataDoubleBufferWriter = null;
    Writer _doubleBuffer = null;
    List<StackEntry> _nestingStack = new ArrayList<StackEntry>(4);
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/context/RequestViewContext.java`
#### Snippet
```java
    
    private Map<String, Boolean> renderTargetMap = null;
    private Map<String, List<UIComponent>> renderTargetMapComponents = null;
    
    public RequestViewContext()
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/context/RequestViewContext.java`
#### Snippet
```java
    private RequestViewMetadata requestViewMetadata;
    
    private Map<String, Boolean> renderTargetMap = null;
    private Map<String, List<UIComponent>> renderTargetMapComponents = null;
    
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/config/webparameters/MyfacesConfig.java`
#### Snippet
```java
    private boolean wrapTagExceptionsAsContextAware = WRAP_TAG_EXCEPTIONS_AS_CONTEXT_AWARE_DEFAULT;
    private boolean resourceCacheLastModified = RESOURCE_CACHE_LAST_MODIFIED_DEFAULT;
    private boolean logWebContextParams = false;
    private int websocketMaxConnections = WEBSOCKET_MAX_CONNECTIONS_DEFAULT;
    private boolean renderClientBehaviorScriptsAsString = RENDER_CLIENTBEHAVIOR_SCRIPTS_AS_STRING_DEFAULT;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/config/webparameters/MyfacesConfig.java`
#### Snippet
```java
            = RANDOM_KEY_IN_VIEW_STATE_SESSION_TOKEN_SECURE_RANDOM_ALGORITHM_DEFAULT;
    private String randomKeyInCsrfSessionToken = RANDOM_KEY_IN_CSRF_SESSION_TOKEN_DEFAULT;
    private boolean serializeStateInSession = false;
    private boolean compressStateInSession = COMPRESS_STATE_IN_SESSION_DEFAULT;
    private boolean useFlashScopePurgeViewsInSession = USE_FLASH_SCOPE_PURGE_VIEWS_IN_SESSION_DEFAULT;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/context/servlet/FacesContextImpl.java`
#### Snippet
```java
    
    private PartialViewContextFactory _partialViewContextFactory = null;
    private RenderKitFactory _renderKitFactory = null;
    private Lifecycle _lifecycle = null;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/context/servlet/FacesContextImpl.java`
#### Snippet
```java
    private PhaseId _currentPhaseId;
    private ResponseStream _responseStream = null;
    private ResponseWriter _responseWriter = null;
    private FacesMessage.Severity _maximumSeverity = null;
    private boolean _renderResponse = false;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/context/servlet/FacesContextImpl.java`
#### Snippet
```java
    // ~ Instance fields ----------------------------------------------------------------------------
    
    private Map<String, List<FacesMessage>> _messages = null;
    private List<FacesMessage> _orderedMessages = null;
    private PhaseId _currentPhaseId;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/context/servlet/FacesContextImpl.java`
#### Snippet
```java
    private ResponseStream _responseStream = null;
    private ResponseWriter _responseWriter = null;
    private FacesMessage.Severity _maximumSeverity = null;
    private boolean _renderResponse = false;
    private boolean _responseComplete = false;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/context/servlet/FacesContextImpl.java`
#### Snippet
```java
    private List<FacesMessage> _orderedMessages = null;
    private PhaseId _currentPhaseId;
    private ResponseStream _responseStream = null;
    private ResponseWriter _responseWriter = null;
    private FacesMessage.Severity _maximumSeverity = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/context/servlet/FacesContextImpl.java`
#### Snippet
```java
    private boolean _validationFailed = false;
    private PartialViewContext _partialViewContext = null;
    private ReleasableFacesContextFactory _facesContextFactory = null;
    
    private PartialViewContextFactory _partialViewContextFactory = null;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/context/servlet/FacesContextImpl.java`
#### Snippet
```java
    private ResponseWriter _responseWriter = null;
    private FacesMessage.Severity _maximumSeverity = null;
    private boolean _renderResponse = false;
    private boolean _responseComplete = false;
    private boolean _validationFailed = false;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/context/servlet/FacesContextImpl.java`
#### Snippet
```java
    private PartialViewContextFactory _partialViewContextFactory = null;
    private RenderKitFactory _renderKitFactory = null;
    private Lifecycle _lifecycle = null;

    // ~ Constructors -------------------------------------------------------------------------------
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/context/servlet/FacesContextImpl.java`
#### Snippet
```java
    private boolean _responseComplete = false;
    private boolean _validationFailed = false;
    private PartialViewContext _partialViewContext = null;
    private ReleasableFacesContextFactory _facesContextFactory = null;
    
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/context/servlet/FacesContextImpl.java`
#### Snippet
```java
    private boolean _renderResponse = false;
    private boolean _responseComplete = false;
    private boolean _validationFailed = false;
    private PartialViewContext _partialViewContext = null;
    private ReleasableFacesContextFactory _facesContextFactory = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/context/servlet/FacesContextImpl.java`
#### Snippet
```java
    
    private Map<String, List<FacesMessage>> _messages = null;
    private List<FacesMessage> _orderedMessages = null;
    private PhaseId _currentPhaseId;
    private ResponseStream _responseStream = null;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/context/servlet/FacesContextImpl.java`
#### Snippet
```java
    private FacesMessage.Severity _maximumSeverity = null;
    private boolean _renderResponse = false;
    private boolean _responseComplete = false;
    private boolean _validationFailed = false;
    private PartialViewContext _partialViewContext = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/context/servlet/FacesContextImpl.java`
#### Snippet
```java
    private ReleasableFacesContextFactory _facesContextFactory = null;
    
    private PartialViewContextFactory _partialViewContextFactory = null;
    private RenderKitFactory _renderKitFactory = null;
    private Lifecycle _lifecycle = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/context/servlet/FacesContextImplBase.java`
#### Snippet
```java
    protected boolean _released = false;
    
    private ApplicationFactory _applicationFactory = null;
    
    private List<String> _resourceLibraryContracts;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/context/servlet/FacesContextImplBase.java`
#### Snippet
```java
    
    // Variables used to cache values
    private RenderKit _cachedRenderKit = null;
    private String _cachedRenderKitId = null;
    
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/context/servlet/FacesContextImplBase.java`
#### Snippet
```java
    private String _cachedRenderKitId = null;
    
    protected boolean _released = false;
    
    private ApplicationFactory _applicationFactory = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/context/servlet/FacesContextImplBase.java`
#### Snippet
```java
    private RenderKitFactory _renderKitFactory;
    private ELContext _elContext;
    private Map<Object, Object> _attributes = null;
    private boolean _processingEvents = true;
    private ExceptionHandler _exceptionHandler = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/context/servlet/FacesContextImplBase.java`
#### Snippet
```java
    private Map<Object, Object> _attributes = null;
    private boolean _processingEvents = true;
    private ExceptionHandler _exceptionHandler = null;
    
    // Variables used to cache values
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/context/servlet/FacesContextImplBase.java`
#### Snippet
```java
    // Variables used to cache values
    private RenderKit _cachedRenderKit = null;
    private String _cachedRenderKitId = null;
    
    protected boolean _released = false;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/resource/AliasResourceMetaImpl.java`
#### Snippet
```java
    private final boolean _couldContainValueExpressions;
    
    private String resourceIdentifier = null;

    public AliasResourceMetaImpl(String prefix, String libraryName, String libraryVersion,
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/resource/RootExternalContextResourceLoader.java`
#### Snippet
```java
    private String contractsDirectory = null;
    
    private String resourcesDirectory = null;

    public RootExternalContextResourceLoader()
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/resource/RootExternalContextResourceLoader.java`
#### Snippet
```java
    private static final String RESOURCES = "resources";
    
    private String contractsDirectory = null;
    
    private String resourcesDirectory = null;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/resource/TempDirFileCacheResourceLoader.java`
#### Snippet
```java
    {
        
        public volatile boolean created = false;
        
        public FileProducer()
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/context/servlet/PartialViewContextImpl.java`
#### Snippet
```java
    private Collection<String> _renderClientIds = null;
    // Values that need to be saved because exists a setXX method 
    private Boolean _partialRequest = null;
    private Boolean _renderAll = null;
    private PartialResponseWriter _partialResponseWriter = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/context/servlet/PartialViewContextImpl.java`
#### Snippet
```java
    // many times and the result does not change during the life time
    // of this object.
    private Boolean _ajaxRequest = null;

    private Collection<String> _executeClientIds = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/context/servlet/PartialViewContextImpl.java`
#### Snippet
```java
    // Values that need to be saved because exists a setXX method 
    private Boolean _partialRequest = null;
    private Boolean _renderAll = null;
    private PartialResponseWriter _partialResponseWriter = null;
    private VisitContextFactory visitContextFactory = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/context/servlet/PartialViewContextImpl.java`
#### Snippet
```java
    private Boolean _renderAll = null;
    private PartialResponseWriter _partialResponseWriter = null;
    private VisitContextFactory visitContextFactory = null;
    private Boolean _resetValues = null;
    private List<String> _evalScripts = new ArrayList<>();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/context/servlet/PartialViewContextImpl.java`
#### Snippet
```java
    private Boolean _partialRequest = null;
    private Boolean _renderAll = null;
    private PartialResponseWriter _partialResponseWriter = null;
    private VisitContextFactory visitContextFactory = null;
    private Boolean _resetValues = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/context/servlet/PartialViewContextImpl.java`
#### Snippet
```java
    private Boolean _ajaxRequest = null;

    private Collection<String> _executeClientIds = null;
    private Collection<String> _renderClientIds = null;
    // Values that need to be saved because exists a setXX method 
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/context/servlet/PartialViewContextImpl.java`
#### Snippet
```java
            EnumSet.of(VisitHint.EXECUTE_LIFECYCLE, VisitHint.SKIP_UNRENDERED));

    private FacesContext context = null;
    private boolean _released = false;
    // Cached values, since their parent methods could be called
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/context/servlet/PartialViewContextImpl.java`
#### Snippet
```java
    private PartialResponseWriter _partialResponseWriter = null;
    private VisitContextFactory visitContextFactory = null;
    private Boolean _resetValues = null;
    private List<String> _evalScripts = new ArrayList<>();

```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/context/servlet/PartialViewContextImpl.java`
#### Snippet
```java

    private FacesContext context = null;
    private boolean _released = false;
    // Cached values, since their parent methods could be called
    // many times and the result does not change during the life time
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/context/servlet/PartialViewContextImpl.java`
#### Snippet
```java

    private Collection<String> _executeClientIds = null;
    private Collection<String> _renderClientIds = null;
    // Values that need to be saved because exists a setXX method 
    private Boolean _partialRequest = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/resource/ResourceMetaImpl.java`
#### Snippet
```java
    
    private Long lastModified;
    private String resourceIdentifier = null;

    public ResourceMetaImpl(String prefix, String libraryName, String libraryVersion,
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/resource/TempDirFileCacheContractResourceLoader.java`
#### Snippet
```java
    public static class FileProducer 
    {
        public volatile boolean created = false;
        
        public FileProducer()
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/resource/ResourceHandlerCache.java`
#### Snippet
```java

    private volatile ConcurrentLRUCache<Object, ResourceValue> _resourceCacheMap = null;
    private volatile ConcurrentLRUCache<Object, ResourceValue> _viewResourceCacheMap = null;
    private volatile ConcurrentLRUCache<Object, Boolean> _libraryExistsCacheMap = null;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/resource/ResourceHandlerCache.java`
#### Snippet
```java
    private boolean _resourceCacheEnabled;

    private volatile ConcurrentLRUCache<Object, ResourceValue> _resourceCacheMap = null;
    private volatile ConcurrentLRUCache<Object, ResourceValue> _viewResourceCacheMap = null;
    private volatile ConcurrentLRUCache<Object, Boolean> _libraryExistsCacheMap = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/resource/ResourceHandlerCache.java`
#### Snippet
```java
    private volatile ConcurrentLRUCache<Object, ResourceValue> _resourceCacheMap = null;
    private volatile ConcurrentLRUCache<Object, ResourceValue> _viewResourceCacheMap = null;
    private volatile ConcurrentLRUCache<Object, Boolean> _libraryExistsCacheMap = null;

    public ResourceHandlerCache()
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/resource/ResourceLoader.java`
#### Snippet
```java
    }
    
    private Comparator<String> _versionComparator = null;

    protected Comparator<String> getVersionComparator()
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/resource/ClassLoaderResourceLoaderIterator.java`
#### Snippet
```java
        private Deque<String> stack = new LinkedList<String>();
        
        Iterator<String> iterator = null;

        public JarDepthIterator(URL directory, String basePath, int maxDepth, ResourceVisitOption... options)
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/resource/ClassLoaderResourceLoaderIterator.java`
#### Snippet
```java
public class ClassLoaderResourceLoaderIterator implements Iterator<String>
{
    private Iterator<String> delegate = null;

    public ClassLoaderResourceLoaderIterator(URL url, String basePath, 
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/lifecycle/ApplyRequestValuesExecutor.java`
#### Snippet
```java
{
    private ViewPoolProcessor viewPoolProcessor;
    private boolean initialized = false;

    @Override
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/lifecycle/RestoreViewSupport.java`
#### Snippet
```java
    private static final Logger log = Logger.getLogger(RestoreViewSupport.class.getName());

    private RenderKitFactory renderKitFactory = null;
    private VisitContextFactory visitContextFactory = null;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/lifecycle/RestoreViewSupport.java`
#### Snippet
```java

    private RenderKitFactory renderKitFactory = null;
    private VisitContextFactory visitContextFactory = null;

    public RestoreViewSupport(FacesContext facesContext)
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/lifecycle/InvokeApplicationExecutor.java`
#### Snippet
```java
{
    private ViewPoolProcessor viewPoolProcessor;
    private boolean initialized = false;
    
    @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/lifecycle/LifecycleImpl.java`
#### Snippet
```java
     * note in this case the semantic of the variable must be preserved.
     */
    private volatile boolean _firstRequestProcessed = false;
    
    public LifecycleImpl()
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/lifecycle/clientwindow/ClientConfig.java`
#### Snippet
```java

    /** lazily initiated via {@link #getUserAgent(jakarta.faces.context.FacesContext)} */
    private volatile String userAgent = null;

    /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/lifecycle/clientwindow/ClientConfig.java`
#### Snippet
```java
    public static final String COOKIE_NAME_NOSCRIPT_ENABLED = "mfNoScriptEnabled";

    private volatile Boolean javaScriptEnabled = null;

    protected String windowHandlerHtml;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/component/visit/PartialVisitContext.java`
#### Snippet
```java
    {
        private final Iterator<E> _wrapped;
        private E _current = null;
        
        private IteratorProxy(Iterator<E> wrapped)
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/lifecycle/clientwindow/CODIClientSideWindow.java`
#### Snippet
```java
    private String windowId;
    
    private String unparsedWindowHandlerHtml = null;
    
    private Map<String,String> queryParamsMap;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/lifecycle/RestoreViewExecutor.java`
#### Snippet
```java
    private Boolean _viewNotFoundCheck;
    
    private RenderKitFactory _renderKitFactory = null;
    
    @Override
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/component/search/SearchExpressionHandlerImpl.java`
#### Snippet
```java
    private static class CollectClientIdsCallback implements ContextCallback
    {
        private List<String> clientIds = null;

        @Override
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/component/search/SearchExpressionHandlerImpl.java`
#### Snippet
```java
    private static class CollectClientIdCallback implements ContextCallback
    {
        private String clientId = null;

        @Override
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlResponseWriterImpl.java`
#### Snippet
```java
    
    private String _startElementName;
    private boolean _isInsideScript = false;
    private boolean _isStyle = false;
    private Boolean _isTextArea;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlResponseWriterImpl.java`
#### Snippet
```java
    private String _startElementName;
    private boolean _isInsideScript = false;
    private boolean _isStyle = false;
    private Boolean _isTextArea;
    private UIComponent _startElementUIComponent;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessagesRendererBase.java`
#### Snippet
```java
        private Iterator _globalMessagesIterator;
        private Iterator _clientIdsWithMessagesIterator;
        private Iterator _componentMessagesIterator = null;
        private String _clientId = null;
        private boolean _redisplay;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessagesRendererBase.java`
#### Snippet
```java
        private Iterator _clientIdsWithMessagesIterator;
        private Iterator _componentMessagesIterator = null;
        private String _clientId = null;
        private boolean _redisplay;
        private Object _next;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/JavascriptContext.java`
#### Snippet
```java
    private long currentIndentationLevel;
    private StringBuilder buffer = new StringBuilder();
    private boolean prettyPrint = false;
    /**
     * automatic formatting will render
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/application/ViewResourceIterator.java`
#### Snippet
```java
    private Deque<ResourceLoader> stack = new LinkedList<ResourceLoader>();
    private Deque<String> basePathStack = new LinkedList<String>();
    private Iterator<String> currentIterator = null;
    private String origBasePath;
    private int maxDepth;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/application/ApplicationFactoryImpl.java`
#### Snippet
```java
    private Application _application;
    
    private boolean _myfacesInstanceAddedToApplicationMap = false;

    public ApplicationFactoryImpl()
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/application/TreeStructureManager.java`
#### Snippet
```java
        private String _componentId;
        private TreeStructComponent[] _children = null;    // Array of children
        private Object[] _facets = null;            // Array of Array-tuples with Facetname and TreeStructComponent

        TreeStructComponent(String componentClass, String componentId)
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/application/TreeStructureManager.java`
#### Snippet
```java
        private String _componentClass;
        private String _componentId;
        private TreeStructComponent[] _children = null;    // Array of children
        private Object[] _facets = null;            // Array of Array-tuples with Facetname and TreeStructComponent

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/application/_FlowNavigationStructure.java`
#### Snippet
```java
    private String _id;
    
    private Map<String, Set<NavigationCase>> _navigationCases = null;
    private List<_WildcardPattern> _wildcardKeys = new ArrayList<_WildcardPattern>();

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/application/viewstate/SerializedViewCollection.java`
#### Snippet
```java

    private final Map<SerializedViewKey, SerializedViewKey> _precedence = new HashMap<>();
    private Map<String, SerializedViewKey> _lastWindowKeys = null;

    public void put(FacesContext context, Object state, SerializedViewKey key, SerializedViewKey previousRestoredKey)
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/application/viewstate/SerializedViewCollection.java`
#### Snippet
```java
     */
    private HashMap<SerializedViewKey, String> _viewScopeIds = null;
    private HashMap<String, Integer> _viewScopeIdCounts = null;

    private final Map<SerializedViewKey, SerializedViewKey> _precedence = new HashMap<>();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/application/viewstate/SerializedViewCollection.java`
#### Snippet
```java
     * in the right time.
     */
    private HashMap<SerializedViewKey, String> _viewScopeIds = null;
    private HashMap<String, Integer> _viewScopeIdCounts = null;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
    private int _resourceBufferSize = -1;
    private String[] _excludedResourceExtensions;
    private Set<String> _viewSuffixes = null;

    @Override
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `impl/src/main/java/org/apache/myfaces/application/NavigationHandlerImpl.java`
#### Snippet
```java
        private List<FlowCallNode> targetFlowCallNodes;
        private List<Flow> currentFlows;
        private int returnCount = 0;

        public NavigationContext()
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/application/NavigationHandlerImpl.java`
#### Snippet
```java
    private static final String ASTERISK = "*";

    private Map<String, Set<NavigationCase>> _navigationCases = null;
    private List<_WildcardPattern> _wildcardPatterns = new ArrayList<>();
    private Boolean _developmentStage;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/el/MockVariableValueExpression.java`
#### Snippet
```java
     * <p>The variable instance being wrapped by this expression.</p>
     */
    private Object instance = null;

    // ------------------------------------------------------ Expression Methods
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/el/MockVariableValueExpression.java`
#### Snippet
```java
     * <p>The expected result type for <code>getValue()</code> calls.</p>
     */
    private Class expectedType = null;

    /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/el/MockValueExpression.java`
#### Snippet
```java
     * <p>The expected result type for <code>getValue()</code> calls.</p>
     */
    private Class expectedType = null;

    /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/el/MockValueExpression.java`
#### Snippet
```java
     * <p>The original expression string used to create this expression.</p>
     */
    private String expression = null;

    // ------------------------------------------------------ Expression Methods
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/el/MockValueExpression.java`
#### Snippet
```java
     * <p>The parsed elements of this expression.</p>
     */
    private String[] elements = null;

    /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/el/MockMethodExpression.java`
#### Snippet
```java
     * <p>The original expression string used to create this expression.</p>
     */
    private String expression = null;

    /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/el/MockMethodExpression.java`
#### Snippet
```java
     * <p>The method signature of the method to be called.</p>
     */
    private Class[] signature = null;

    // ------------------------------------------------------ Expression Methods
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/el/MockMethodExpression.java`
#### Snippet
```java
     * <p>The parsed elements of this expression.</p>
     */
    private String[] elements = null;

    /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/el/MockMethodExpression.java`
#### Snippet
```java
     * <p>The expected result type for <code>getValue()</code> calls.</p>
     */
    private Class expectedType = null;

    /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `impl/src/main/java/org/apache/myfaces/application/ApplicationImpl.java`
#### Snippet
```java
    private final Map<String, String> _defaultValidatorsIds = new HashMap<>();
    
    private volatile Map<String, String> _cachedDefaultValidatorsIds = null;
    
    private final Map<String, Object> _behaviorClassMap = new ConcurrentHashMap<>();
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/application/ApplicationImpl.java`
#### Snippet
```java
    
    /** Value of jakarta.faces.DATETIMECONVERTER_DEFAULT_TIMEZONE_IS_SYSTEM_TIMEZONE parameter */
    private boolean _dateTimeConverterDefaultTimeZoneIsSystemTimeZone = false; 
    
    private SearchExpressionHandler _searchExpressionHandler;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `impl/src/main/java/org/apache/myfaces/application/ApplicationImpl.java`
#### Snippet
```java
    private ProjectStage _projectStage;

    private volatile boolean _firstRequestProcessed = false;
    
    // MYFACES-3442 If HashMap or other non thread-safe structure is used, it is
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractJsfTestCase.java`
#### Snippet
```java
    protected MockHttpServletResponse response = null;
    protected MockServletContext servletContext = null;
    protected MockHttpSession session = null;

    // Thread context class loader saved and restored after each test
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractJsfTestCase.java`
#### Snippet
```java

    // Thread context class loader saved and restored after each test
    private ClassLoader threadContextClassLoader = null;
    private boolean classLoaderSet = false;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractJsfTestCase.java`
#### Snippet
```java
    protected MockExternalContext externalContext = null;
    protected MockFacesContext facesContext = null;
    protected MockFacesContextFactory facesContextFactory = null;
    protected MockLifecycle lifecycle = null;
    protected MockLifecycleFactory lifecycleFactory = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractJsfTestCase.java`
#### Snippet
```java
    protected MockFacesContext facesContext = null;
    protected MockFacesContextFactory facesContextFactory = null;
    protected MockLifecycle lifecycle = null;
    protected MockLifecycleFactory lifecycleFactory = null;
    protected MockRenderKit renderKit = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractJsfTestCase.java`
#### Snippet
```java
    protected MockHttpServletRequest request = null;
    protected MockHttpServletResponse response = null;
    protected MockServletContext servletContext = null;
    protected MockHttpSession session = null;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractJsfTestCase.java`
#### Snippet
```java
    protected MockLifecycleFactory lifecycleFactory = null;
    protected MockRenderKit renderKit = null;
    protected MockHttpServletRequest request = null;
    protected MockHttpServletResponse response = null;
    protected MockServletContext servletContext = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractJsfTestCase.java`
#### Snippet
```java
    protected MockApplication application = null;
    protected MockServletConfig config = null;
    protected MockExternalContext externalContext = null;
    protected MockFacesContext facesContext = null;
    protected MockFacesContextFactory facesContextFactory = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractJsfTestCase.java`
#### Snippet
```java
    protected MockLifecycle lifecycle = null;
    protected MockLifecycleFactory lifecycleFactory = null;
    protected MockRenderKit renderKit = null;
    protected MockHttpServletRequest request = null;
    protected MockHttpServletResponse response = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractJsfTestCase.java`
#### Snippet
```java
    protected MockFacesContextFactory facesContextFactory = null;
    protected MockLifecycle lifecycle = null;
    protected MockLifecycleFactory lifecycleFactory = null;
    protected MockRenderKit renderKit = null;
    protected MockHttpServletRequest request = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractJsfTestCase.java`
#### Snippet
```java
    // Mock object instances for our tests
    protected MockApplication application = null;
    protected MockServletConfig config = null;
    protected MockExternalContext externalContext = null;
    protected MockFacesContext facesContext = null;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractJsfTestCase.java`
#### Snippet
```java
    // Thread context class loader saved and restored after each test
    private ClassLoader threadContextClassLoader = null;
    private boolean classLoaderSet = false;

}
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractJsfTestCase.java`
#### Snippet
```java
    protected MockServletConfig config = null;
    protected MockExternalContext externalContext = null;
    protected MockFacesContext facesContext = null;
    protected MockFacesContextFactory facesContextFactory = null;
    protected MockLifecycle lifecycle = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractJsfTestCase.java`
#### Snippet
```java
    protected MockRenderKit renderKit = null;
    protected MockHttpServletRequest request = null;
    protected MockHttpServletResponse response = null;
    protected MockServletContext servletContext = null;
    protected MockHttpSession session = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractJsfTestCase.java`
#### Snippet
```java

    // Mock object instances for our tests
    protected MockApplication application = null;
    protected MockServletConfig config = null;
    protected MockExternalContext externalContext = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractJsfConfigurableMultipleRequestsTestCase.java`
#### Snippet
```java
    protected MockHttpServletRequest request = null;
    protected MockHttpServletResponse response = null;
    protected MockServletContext servletContext = null;
    protected MockHttpSession session = null;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractJsfConfigurableMockTestCase.java`
#### Snippet
```java

    // Mock object instances for our tests
    protected Application application = null;
    protected MockServletConfig config = null;
    protected ExternalContext externalContext = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractJsfConfigurableMockTestCase.java`
#### Snippet
```java
    protected RenderKit renderKit = null;
    protected MockHttpServletRequest request = null;
    protected MockHttpServletResponse response = null;
    protected MockServletContext servletContext = null;
    protected MockHttpSession session = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractJsfConfigurableMultipleRequestsTestCase.java`
#### Snippet
```java
    protected FacesContext facesContext = null;
    protected FacesContextFactory facesContextFactory = null;
    protected Lifecycle lifecycle = null;
    protected LifecycleFactory lifecycleFactory = null;
    protected RenderKit renderKit = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractJsfConfigurableMultipleRequestsTestCase.java`
#### Snippet
```java
    protected Application application = null;
    protected MockServletConfig config = null;
    protected ExternalContext externalContext = null;
    protected FacesContext facesContext = null;
    protected FacesContextFactory facesContextFactory = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractJsfConfigurableMultipleRequestsTestCase.java`
#### Snippet
```java
    protected MockServletConfig config = null;
    protected ExternalContext externalContext = null;
    protected FacesContext facesContext = null;
    protected FacesContextFactory facesContextFactory = null;
    protected Lifecycle lifecycle = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractJsfConfigurableMultipleRequestsTestCase.java`
#### Snippet
```java

    // Thread context class loader saved and restored after each test
    private ClassLoader threadContextClassLoader = null;
    private boolean classLoaderSet = false;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractJsfConfigurableMultipleRequestsTestCase.java`
#### Snippet
```java

    // Mock object instances for our tests
    protected Application application = null;
    protected MockServletConfig config = null;
    protected ExternalContext externalContext = null;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractJsfConfigurableMultipleRequestsTestCase.java`
#### Snippet
```java
    // Thread context class loader saved and restored after each test
    private ClassLoader threadContextClassLoader = null;
    private boolean classLoaderSet = false;

}
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractJsfConfigurableMultipleRequestsTestCase.java`
#### Snippet
```java
    protected FacesContextFactory facesContextFactory = null;
    protected Lifecycle lifecycle = null;
    protected LifecycleFactory lifecycleFactory = null;
    protected RenderKit renderKit = null;
    protected MockHttpServletRequest request = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractJsfConfigurableMultipleRequestsTestCase.java`
#### Snippet
```java
    protected Lifecycle lifecycle = null;
    protected LifecycleFactory lifecycleFactory = null;
    protected RenderKit renderKit = null;
    protected MockHttpServletRequest request = null;
    protected MockHttpServletResponse response = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractJsfConfigurableMultipleRequestsTestCase.java`
#### Snippet
```java
    protected RenderKit renderKit = null;
    protected MockHttpServletRequest request = null;
    protected MockHttpServletResponse response = null;
    protected MockServletContext servletContext = null;
    protected MockHttpSession session = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractJsfConfigurableMultipleRequestsTestCase.java`
#### Snippet
```java
    protected MockHttpServletResponse response = null;
    protected MockServletContext servletContext = null;
    protected MockHttpSession session = null;

    // Thread context class loader saved and restored after each test
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractJsfConfigurableMultipleRequestsTestCase.java`
#### Snippet
```java
    // Mock object instances for our tests
    protected Application application = null;
    protected MockServletConfig config = null;
    protected ExternalContext externalContext = null;
    protected FacesContext facesContext = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractJsfConfigurableMultipleRequestsTestCase.java`
#### Snippet
```java
    protected ExternalContext externalContext = null;
    protected FacesContext facesContext = null;
    protected FacesContextFactory facesContextFactory = null;
    protected Lifecycle lifecycle = null;
    protected LifecycleFactory lifecycleFactory = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractJsfConfigurableMultipleRequestsTestCase.java`
#### Snippet
```java
    protected LifecycleFactory lifecycleFactory = null;
    protected RenderKit renderKit = null;
    protected MockHttpServletRequest request = null;
    protected MockHttpServletResponse response = null;
    protected MockServletContext servletContext = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractJsfConfigurableMockTestCase.java`
#### Snippet
```java
    protected MockHttpServletResponse response = null;
    protected MockServletContext servletContext = null;
    protected MockHttpSession session = null;

    // Thread context class loader saved and restored after each test
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractJsfConfigurableMockTestCase.java`
#### Snippet
```java
    protected MockHttpServletRequest request = null;
    protected MockHttpServletResponse response = null;
    protected MockServletContext servletContext = null;
    protected MockHttpSession session = null;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractJsfConfigurableMockTestCase.java`
#### Snippet
```java
    protected MockServletConfig config = null;
    protected ExternalContext externalContext = null;
    protected FacesContext facesContext = null;
    protected FacesContextFactory facesContextFactory = null;
    protected Lifecycle lifecycle = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractJsfConfigurableMockTestCase.java`
#### Snippet
```java

    // Thread context class loader saved and restored after each test
    private ClassLoader threadContextClassLoader = null;
    private boolean classLoaderSet = false;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractJsfConfigurableMockTestCase.java`
#### Snippet
```java
    protected LifecycleFactory lifecycleFactory = null;
    protected RenderKit renderKit = null;
    protected MockHttpServletRequest request = null;
    protected MockHttpServletResponse response = null;
    protected MockServletContext servletContext = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractJsfConfigurableMockTestCase.java`
#### Snippet
```java
    protected Lifecycle lifecycle = null;
    protected LifecycleFactory lifecycleFactory = null;
    protected RenderKit renderKit = null;
    protected MockHttpServletRequest request = null;
    protected MockHttpServletResponse response = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractJsfConfigurableMockTestCase.java`
#### Snippet
```java
    protected FacesContextFactory facesContextFactory = null;
    protected Lifecycle lifecycle = null;
    protected LifecycleFactory lifecycleFactory = null;
    protected RenderKit renderKit = null;
    protected MockHttpServletRequest request = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractJsfConfigurableMockTestCase.java`
#### Snippet
```java
    protected FacesContext facesContext = null;
    protected FacesContextFactory facesContextFactory = null;
    protected Lifecycle lifecycle = null;
    protected LifecycleFactory lifecycleFactory = null;
    protected RenderKit renderKit = null;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractJsfConfigurableMockTestCase.java`
#### Snippet
```java
    // Thread context class loader saved and restored after each test
    private ClassLoader threadContextClassLoader = null;
    private boolean classLoaderSet = false;

}
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractJsfConfigurableMockTestCase.java`
#### Snippet
```java
    protected ExternalContext externalContext = null;
    protected FacesContext facesContext = null;
    protected FacesContextFactory facesContextFactory = null;
    protected Lifecycle lifecycle = null;
    protected LifecycleFactory lifecycleFactory = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractJsfConfigurableMockTestCase.java`
#### Snippet
```java
    protected Application application = null;
    protected MockServletConfig config = null;
    protected ExternalContext externalContext = null;
    protected FacesContext facesContext = null;
    protected FacesContextFactory facesContextFactory = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractJsfConfigurableMockTestCase.java`
#### Snippet
```java
    // Mock object instances for our tests
    protected Application application = null;
    protected MockServletConfig config = null;
    protected ExternalContext externalContext = null;
    protected FacesContext facesContext = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractHtmlUnitTestCase.java`
#### Snippet
```java
     * each test case.</p>
     */
    protected URL url = null;

    /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractHtmlUnitTestCase.java`
#### Snippet
```java
     * <p>The most recently retrieved page from the server.</p>
     */
    protected HtmlPage page = null;

    /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractHtmlUnitTestCase.java`
#### Snippet
```java
     * <p>The web client for this test case.</p>
     */
    protected WebClient webClient = null;

    // ------------------------------------------------------ Test Setup Methods
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockPrincipal.java`
#### Snippet
```java
     * <p>The name for this Principal intance.</p>
     */
    private String name = null;

    // ----------------------------------------------------- Mock Object Methods
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `test/src/main/java/org/apache/myfaces/test/base/junit/runners/TestPerClassLoaderRunner.java`
#### Snippet
```java
    // The classpath is needed because the custom class loader looks there to find the classes.
    private static String classPath;
    private static boolean classPathDetermined = false;

    // Some data related to the class from the custom class loader.
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpSession.java`
#### Snippet
```java
    private String id = "123";
    private ServletContext servletContext = null;
    private boolean invalid = false;
    private MockWebContainer webContainer;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpSession.java`
#### Snippet
```java
    private HashMap attributes = new HashMap();
    private String id = "123";
    private ServletContext servletContext = null;
    private boolean invalid = false;
    private MockWebContainer webContainer;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockPortletSession.java`
#### Snippet
```java
    private Map applicationAttributes = new HashMap();
    private String id = "123";
    private PortletContext portletContext = null;

    // ---------------------------------------------------------- Public Methods
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockServletOutputStream.java`
#### Snippet
```java
     * <p>The internal buffer we use to capture output.</p>
     */
    private ByteArrayOutputStream baos = null;

    // --------------------------------------------- ServletOutputStream Methods
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockedJsfTestContainer.java`
#### Snippet
```java
    protected MockHttpSession lastSession = null;
    protected MockServletContext servletContext = null;
    private MockWebContainer webContainer = null;

}
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockedJsfTestContainer.java`
#### Snippet
```java
    protected MockRenderKit renderKit = null;
    protected MockHttpServletRequest request = null;
    protected boolean requestInitializedCalled = false;
    protected MockHttpServletResponse response = null;
    protected MockHttpSession lastSession = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockedJsfTestContainer.java`
#### Snippet
```java

    // Mock object instances for our tests
    protected MockApplication application = null;
    protected MockServletConfig config = null;
    protected MockExternalContext externalContext = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockedJsfTestContainer.java`
#### Snippet
```java
    protected boolean requestInitializedCalled = false;
    protected MockHttpServletResponse response = null;
    protected MockHttpSession lastSession = null;
    protected MockServletContext servletContext = null;
    private MockWebContainer webContainer = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockedJsfTestContainer.java`
#### Snippet
```java
    protected MockLifecycleFactory lifecycleFactory = null;
    protected MockRenderKit renderKit = null;
    protected MockHttpServletRequest request = null;
    protected boolean requestInitializedCalled = false;
    protected MockHttpServletResponse response = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockedJsfTestContainer.java`
#### Snippet
```java
    protected MockExternalContext externalContext = null;
    protected MockFacesContext facesContext = null;
    protected MockFacesContextFactory facesContextFactory = null;
    protected MockLifecycle lifecycle = null;
    protected MockLifecycleFactory lifecycleFactory = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockedJsfTestContainer.java`
#### Snippet
```java
    protected MockHttpServletResponse response = null;
    protected MockHttpSession lastSession = null;
    protected MockServletContext servletContext = null;
    private MockWebContainer webContainer = null;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockedJsfTestContainer.java`
#### Snippet
```java
    protected MockFacesContext facesContext = null;
    protected MockFacesContextFactory facesContextFactory = null;
    protected MockLifecycle lifecycle = null;
    protected MockLifecycleFactory lifecycleFactory = null;
    protected MockRenderKit renderKit = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockedJsfTestContainer.java`
#### Snippet
```java
    protected MockApplication application = null;
    protected MockServletConfig config = null;
    protected MockExternalContext externalContext = null;
    protected MockFacesContext facesContext = null;
    protected MockFacesContextFactory facesContextFactory = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockedJsfTestContainer.java`
#### Snippet
```java
    protected MockLifecycle lifecycle = null;
    protected MockLifecycleFactory lifecycleFactory = null;
    protected MockRenderKit renderKit = null;
    protected MockHttpServletRequest request = null;
    protected boolean requestInitializedCalled = false;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockedJsfTestContainer.java`
#### Snippet
```java
    protected MockServletConfig config = null;
    protected MockExternalContext externalContext = null;
    protected MockFacesContext facesContext = null;
    protected MockFacesContextFactory facesContextFactory = null;
    protected MockLifecycle lifecycle = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockedJsfTestContainer.java`
#### Snippet
```java
    protected MockHttpServletRequest request = null;
    protected boolean requestInitializedCalled = false;
    protected MockHttpServletResponse response = null;
    protected MockHttpSession lastSession = null;
    protected MockServletContext servletContext = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockedJsfTestContainer.java`
#### Snippet
```java
    // Mock object instances for our tests
    protected MockApplication application = null;
    protected MockServletConfig config = null;
    protected MockExternalContext externalContext = null;
    protected MockFacesContext facesContext = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockedJsfTestContainer.java`
#### Snippet
```java
    protected MockFacesContextFactory facesContextFactory = null;
    protected MockLifecycle lifecycle = null;
    protected MockLifecycleFactory lifecycleFactory = null;
    protected MockRenderKit renderKit = null;
    protected MockHttpServletRequest request = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockPortletRequest.java`
#### Snippet
```java
    private Map attributes = new HashMap();
    private String contextPath = null;
    private Locale locale = null;
    private Map parameters = new HashMap();
    private Principal principal = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockPortletRequest.java`
#### Snippet
```java
    private Locale locale = null;
    private Map parameters = new HashMap();
    private Principal principal = null;
    private PortletSession session = null;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockPortletRequest.java`
#### Snippet
```java
    private Map parameters = new HashMap();
    private Principal principal = null;
    private PortletSession session = null;

    // -------------------------------------------------- PortletRequest Methods
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockPortletRequest.java`
#### Snippet
```java

    private Map attributes = new HashMap();
    private String contextPath = null;
    private Locale locale = null;
    private Map parameters = new HashMap();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockPortletContext.java`
#### Snippet
```java

    private Hashtable attributes = new Hashtable();
    private File documentRoot = null;
    private Hashtable mimeTypes = new Hashtable();
    private Hashtable parameters = new Hashtable();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplicationFactory.java`
#### Snippet
```java
     * this factory.</p>
     */
    private Application application = null;

    // --------------------------------------------- AppolicationFactory Methods
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockFacesContextFactory.java`
#### Snippet
```java
     * taking an <code>ExternalContext</code> and <code>Lifecycle</code>.</p>
     */
    private Constructor constructor = null;

    /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication12.java`
#### Snippet
```java
     * <p>Expression factory for this instance.</p>
     */
    private ExpressionFactory expressionFactory = null;

    /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication12.java`
#### Snippet
```java
     * This value is lazily instantiated.</p>
     */
    private ELResolver resolver = null;

    /**
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockFacesContext10.java`
#### Snippet
```java
    private boolean renderResponse = false;
    private boolean responseComplete = false;
    private ResponseStream responseStream = null;
    private ResponseWriter responseWriter = null;
    private UIViewRoot viewRoot = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockFacesContext10.java`
#### Snippet
```java

    private Application application = null;
    private ExternalContext externalContext = null;
    private Lifecycle lifecycle = null;
    protected Map messages = new HashMap(); // needs to be accessed in subclass MockFacesContext20
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockFacesContext10.java`
#### Snippet
```java
    private boolean responseComplete = false;
    private ResponseStream responseStream = null;
    private ResponseWriter responseWriter = null;
    private UIViewRoot viewRoot = null;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockFacesContext10.java`
#### Snippet
```java
    private ResponseStream responseStream = null;
    private ResponseWriter responseWriter = null;
    private UIViewRoot viewRoot = null;

    // ---------------------------------------------------- FacesContext Methods
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockFacesContext10.java`
#### Snippet
```java
    protected Map messages = new HashMap(); // needs to be accessed in subclass MockFacesContext20
    private boolean renderResponse = false;
    private boolean responseComplete = false;
    private ResponseStream responseStream = null;
    private ResponseWriter responseWriter = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockFacesContext10.java`
#### Snippet
```java
    private Application application = null;
    private ExternalContext externalContext = null;
    private Lifecycle lifecycle = null;
    protected Map messages = new HashMap(); // needs to be accessed in subclass MockFacesContext20
    private boolean renderResponse = false;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockFacesContext10.java`
#### Snippet
```java
    private Lifecycle lifecycle = null;
    protected Map messages = new HashMap(); // needs to be accessed in subclass MockFacesContext20
    private boolean renderResponse = false;
    private boolean responseComplete = false;
    private ResponseStream responseStream = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockFacesContext10.java`
#### Snippet
```java
    // ------------------------------------------------------ Instance Variables

    private Application application = null;
    private ExternalContext externalContext = null;
    private Lifecycle lifecycle = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockPartialViewContext.java`
#### Snippet
```java
    private Collection<String> _renderClientIds = null;
    private Boolean _partialRequest = null;
    private Boolean _renderAll = null;
    private PartialResponseWriter _partialResponseWriter = null;
    private List<String> _evalScripts = new ArrayList<String>();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockPartialViewContext.java`
#### Snippet
```java
    private Collection<String> _executeClientIds = null;
    private Collection<String> _renderClientIds = null;
    private Boolean _partialRequest = null;
    private Boolean _renderAll = null;
    private PartialResponseWriter _partialResponseWriter = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockPartialViewContext.java`
#### Snippet
```java
    private static final String SOURCE_PARAM_NAME = "jakarta.faces.source";
    private FacesContext _facesContext = null;
    private Boolean _ajaxRequest = null;
    private Collection<String> _executeClientIds = null;
    private Collection<String> _renderClientIds = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockPartialViewContext.java`
#### Snippet
```java
    private FacesContext _facesContext = null;
    private Boolean _ajaxRequest = null;
    private Collection<String> _executeClientIds = null;
    private Collection<String> _renderClientIds = null;
    private Boolean _partialRequest = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockPartialViewContext.java`
#### Snippet
```java
    private Boolean _ajaxRequest = null;
    private Collection<String> _executeClientIds = null;
    private Collection<String> _renderClientIds = null;
    private Boolean _partialRequest = null;
    private Boolean _renderAll = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockPartialViewContext.java`
#### Snippet
```java
    private Boolean _partialRequest = null;
    private Boolean _renderAll = null;
    private PartialResponseWriter _partialResponseWriter = null;
    private List<String> _evalScripts = new ArrayList<String>();

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockPartialViewContext.java`
#### Snippet
```java
    private static final String PARTIAL_PROCESS = "partial/process";
    private static final String SOURCE_PARAM_NAME = "jakarta.faces.source";
    private FacesContext _facesContext = null;
    private Boolean _ajaxRequest = null;
    private Collection<String> _executeClientIds = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockPrintWriter.java`
#### Snippet
```java
     * <p>The writer we will use for buffering.</p>
     */
    private CharArrayWriter caw = null;

    // ----------------------------------------------------- PrintWriter Methods
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockServletContext.java`
#### Snippet
```java

    private Hashtable attributes = new Hashtable();
    private File documentRoot = null;
    private Hashtable mimeTypes = new Hashtable();
    private Hashtable parameters = new Hashtable();
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockFacesContext20.java`
#### Snippet
```java
    private PartialViewContext _partialViewContext = null;
    private Map<Object, Object> attributes;
    private boolean _validationFailed = false;

    // ----------------------------------------------------- Mock Object Methods   
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockFacesContext20.java`
#### Snippet
```java

    private boolean _processingEvents = true;
    private ExceptionHandler _exceptionHandler = null;
    private PhaseId _currentPhaseId = PhaseId.RESTORE_VIEW;
    private boolean _postback;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockFacesContext20.java`
#### Snippet
```java
    private PhaseId _currentPhaseId = PhaseId.RESTORE_VIEW;
    private boolean _postback;
    private PartialViewContext _partialViewContext = null;
    private Map<Object, Object> attributes;
    private boolean _validationFailed = false;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletRequest.java`
#### Snippet
```java
    private List attributeListeners = new ArrayList();
    private HashMap attributes = new HashMap();
    private String contextPath = null;
    private List headers = new ArrayList();
    private Locale locale = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletRequest.java`
#### Snippet
```java
    private String pathInfo = null;
    private Principal principal = null;
    private String queryString = null;
    private ServletContext servletContext = null;
    private String servletPath = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletRequest.java`
#### Snippet
```java
    private Vector locales = null;
    private String method = null;
    private List<String> roles = null;

    // ---------------------------------------------- HttpServletRequest Methods
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletRequest.java`
#### Snippet
```java
    private List cookies = new ArrayList();
    private Vector locales = null;
    private String method = null;
    private List<String> roles = null;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletRequest.java`
#### Snippet
```java
    private String servletPath = null;
    private HttpSession session = null;
    private String characterEncoding = null;
    private ServletInputStream inputStream = null;
    private List cookies = new ArrayList();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletRequest.java`
#### Snippet
```java
    private ServletInputStream inputStream = null;
    private List cookies = new ArrayList();
    private Vector locales = null;
    private String method = null;
    private List<String> roles = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletRequest.java`
#### Snippet
```java
    private String queryString = null;
    private ServletContext servletContext = null;
    private String servletPath = null;
    private HttpSession session = null;
    private String characterEncoding = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletRequest.java`
#### Snippet
```java
    private String contextPath = null;
    private List headers = new ArrayList();
    private Locale locale = null;
    private HashMap parameters = new HashMap();
    private String pathInfo = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletRequest.java`
#### Snippet
```java
    private HttpSession session = null;
    private String characterEncoding = null;
    private ServletInputStream inputStream = null;
    private List cookies = new ArrayList();
    private Vector locales = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletRequest.java`
#### Snippet
```java
    private ServletContext servletContext = null;
    private String servletPath = null;
    private HttpSession session = null;
    private String characterEncoding = null;
    private ServletInputStream inputStream = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletRequest.java`
#### Snippet
```java
    private Principal principal = null;
    private String queryString = null;
    private ServletContext servletContext = null;
    private String servletPath = null;
    private HttpSession session = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletRequest.java`
#### Snippet
```java
    private HashMap parameters = new HashMap();
    private String pathInfo = null;
    private Principal principal = null;
    private String queryString = null;
    private ServletContext servletContext = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletRequest.java`
#### Snippet
```java
    private Locale locale = null;
    private HashMap parameters = new HashMap();
    private String pathInfo = null;
    private Principal principal = null;
    private String queryString = null;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockResponseWriter.java`
#### Snippet
```java
    private String characterEncoding = null;
    private String contentType = "text/html";
    private boolean open = false; // Is an element currently open?
    private UIComponent component;
    private Writer writer = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockResponseWriter.java`
#### Snippet
```java
    private boolean open = false; // Is an element currently open?
    private UIComponent component;
    private Writer writer = null;

    // ----------------------------------------------------- Mock Object Methods
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockResponseWriter.java`
#### Snippet
```java
    // ------------------------------------------------------ Instance Variables

    private String characterEncoding = null;
    private String contentType = "text/html";
    private boolean open = false; // Is an element currently open?
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletResponse.java`
#### Snippet
```java
    private int status = HttpServletResponse.SC_OK;
    private ServletOutputStream stream = null;
    private PrintWriter writer = null;

    private boolean committed = false;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletResponse.java`
#### Snippet
```java

    private boolean committed = false;
    private long contentLength = 0;
    private int bufferSize = 0;
    private Locale locale = Locale.getDefault();
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletResponse.java`
#### Snippet
```java
    private String message = null;
    private int status = HttpServletResponse.SC_OK;
    private ServletOutputStream stream = null;
    private PrintWriter writer = null;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletResponse.java`
#### Snippet
```java
    private String contentType = "text/html";
    private List headers = new ArrayList();
    private String message = null;
    private int status = HttpServletResponse.SC_OK;
    private ServletOutputStream stream = null;
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletResponse.java`
#### Snippet
```java
    private PrintWriter writer = null;

    private boolean committed = false;
    private long contentLength = 0;
    private int bufferSize = 0;
```

### RedundantFieldInitialization
Field initialization to `0` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletResponse.java`
#### Snippet
```java
    private boolean committed = false;
    private long contentLength = 0;
    private int bufferSize = 0;
    private Locale locale = Locale.getDefault();
    private Map<String, Cookie> cookies = new HashMap<String, Cookie>(4);
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockFacesContext12.java`
#### Snippet
```java
    // ------------------------------------------------------ Instance Variables

    private ELContext elContext = null;

    // ---------------------------------------------------- FacesContext Methods
```

### RedundantFieldInitialization
Field initialization to `false` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockFacesContext.java`
#### Snippet
```java
    private List<String> _resourceLibraryContracts;
    private Character _separatorChar;
    protected boolean _released = false;
    
    // ----------------------------------------------------- Mock Object Methods   
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication10.java`
#### Snippet
```java
    private Map components = null;
    private Map converters = null; // By id
    private Map converters1 = null; // By type
    private Locale defaultLocale = null;
    private String defaultRenderKitId = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication10.java`
#### Snippet
```java
    private String defaultRenderKitId = null;
    private String messageBundle = null;
    private NavigationHandler navigationHandler = null;
    private StateManager stateManager = null;
    private Collection supportedLocales = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication10.java`
#### Snippet
```java
    private Locale defaultLocale = null;
    private String defaultRenderKitId = null;
    private String messageBundle = null;
    private NavigationHandler navigationHandler = null;
    private StateManager stateManager = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication10.java`
#### Snippet
```java
    private Map converters1 = null; // By type
    private Locale defaultLocale = null;
    private String defaultRenderKitId = null;
    private String messageBundle = null;
    private NavigationHandler navigationHandler = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication10.java`
#### Snippet
```java
    private StateManager stateManager = null;
    private Collection supportedLocales = null;
    private Map validators = null;
    private ViewHandler viewHandler = null;

```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication10.java`
#### Snippet
```java
    // ------------------------------------------------------ Instance Variables

    private ActionListener actionListener = null;
    private Map components = null;
    private Map converters = null; // By id
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication10.java`
#### Snippet
```java

    private ActionListener actionListener = null;
    private Map components = null;
    private Map converters = null; // By id
    private Map converters1 = null; // By type
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication10.java`
#### Snippet
```java
    private NavigationHandler navigationHandler = null;
    private StateManager stateManager = null;
    private Collection supportedLocales = null;
    private Map validators = null;
    private ViewHandler viewHandler = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication10.java`
#### Snippet
```java
    private Collection supportedLocales = null;
    private Map validators = null;
    private ViewHandler viewHandler = null;

    // ----------------------------------------------------- Application Methods
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication10.java`
#### Snippet
```java
    private Map converters = null; // By id
    private Map converters1 = null; // By type
    private Locale defaultLocale = null;
    private String defaultRenderKitId = null;
    private String messageBundle = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication10.java`
#### Snippet
```java
    private ActionListener actionListener = null;
    private Map components = null;
    private Map converters = null; // By id
    private Map converters1 = null; // By type
    private Locale defaultLocale = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication10.java`
#### Snippet
```java
    private String messageBundle = null;
    private NavigationHandler navigationHandler = null;
    private StateManager stateManager = null;
    private Collection supportedLocales = null;
    private Map validators = null;
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/resource/MockResourceLoader.java`
#### Snippet
```java
    public abstract boolean libraryExists(String libraryName);

    private Comparator<String> _versionComparator = null;

    protected Comparator<String> getVersionComparator()
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/lifecycle/MockLifecycleFactory.java`
#### Snippet
```java
     * <p>The set of Lifecycle instances registered with us.</p>
     */
    private Map lifecycles = null;

    // ------------------------------------------------ LifecycleFactory Methods
```

### RedundantFieldInitialization
Field initialization to `null` is redundant
in `test/src/main/java/org/apache/myfaces/test/mock/visit/PartialVisitContext.java`
#### Snippet
```java
        private final Iterator<E> _wrapped;

        private E _current = null;
    }

```

## RuleId[ruleID=RedundantImplements]
### RedundantImplements
Redundant interface declaration `Serializable`
in `api/src/main/java/jakarta/faces/component/_ComponentChildrenList.java`
#### Snippet
```java
import org.apache.myfaces.core.api.shared.lang.Assert;

class _ComponentChildrenList extends ArrayList<UIComponent> implements Serializable
{
    private static final long serialVersionUID = -6775078929331154224L;
```

### RedundantImplements
Redundant interface declaration `TransientStateHolder`
in `api/src/main/java/jakarta/faces/component/_DeltaStateHelper.java`
#### Snippet
```java
 * </p>
 */
class _DeltaStateHelper implements StateHelper, TransientStateHelper, TransientStateHolder
{

```

### RedundantImplements
Redundant interface declaration `List`
in `api/src/main/java/jakarta/faces/component/_DeltaList.java`
#### Snippet
```java
 * is necessary to provide convenient methods to track deltas.
 */
class _DeltaList<T> extends ArrayList<T> implements List<T>, PartialStateHolder, RandomAccess
{
    private static Object[] emptyObjectArray = new Object[]{};
```

### RedundantImplements
Redundant interface declaration `RandomAccess`
in `api/src/main/java/jakarta/faces/component/_DeltaList.java`
#### Snippet
```java
 * is necessary to provide convenient methods to track deltas.
 */
class _DeltaList<T> extends ArrayList<T> implements List<T>, PartialStateHolder, RandomAccess
{
    private static Object[] emptyObjectArray = new Object[]{};
```

### RedundantImplements
Redundant interface declaration `List`
in `api/src/main/java/jakarta/faces/component/behavior/_DeltaList.java`
#### Snippet
```java
 * is necessary to provide convenient methods to track deltas.
 */
class _DeltaList<T> extends ArrayList<T> implements List<T>, PartialStateHolder, RandomAccess
{
    private static Object[] emptyObjectArray = new Object[]{};
```

### RedundantImplements
Redundant interface declaration `RandomAccess`
in `api/src/main/java/jakarta/faces/component/behavior/_DeltaList.java`
#### Snippet
```java
 * is necessary to provide convenient methods to track deltas.
 */
class _DeltaList<T> extends ArrayList<T> implements List<T>, PartialStateHolder, RandomAccess
{
    private static Object[] emptyObjectArray = new Object[]{};
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/cdi/view/ViewScopeContextualStorageHolder.java`
#### Snippet
```java
public class ViewScopeContextualStorageHolder
        extends AbstractContextualStorageHolder<ViewScopeContextualStorage>
        implements Serializable
{    
    private static final Random RANDOM_GENERATOR = new Random();
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/cdi/clientwindow/ClientWindowScopeContextualStorageHolder.java`
#### Snippet
```java
public class ClientWindowScopeContextualStorageHolder
        extends AbstractContextualStorageHolder<ContextualStorage>
        implements Serializable
{
    private LRULinkedHashMap<String, String> clientWindowExpirationStack;
```

### RedundantImplements
Redundant interface declaration `FacesWrapper`
in `impl/src/main/java/org/apache/myfaces/spi/StateCacheProviderWrapper.java`
#### Snippet
```java
 *
 */
public class StateCacheProviderWrapper extends StateCacheProvider implements FacesWrapper<StateCacheProvider>
{
    
```

### RedundantImplements
Redundant interface declaration `FacesWrapper`
in `impl/src/main/java/org/apache/myfaces/spi/AnnotationProviderWrapper.java`
#### Snippet
```java
 * @author Leonardo Uribe 
 */
public abstract class AnnotationProviderWrapper extends AnnotationProvider implements FacesWrapper<AnnotationProvider>
{
    private AnnotationProvider delegate;
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/flow/cdi/FlowScopeContextualStorageHolder.java`
#### Snippet
```java
public class FlowScopeContextualStorageHolder 
        extends AbstractContextualStorageHolder<ContextualStorage>
        implements Serializable
{
    /**
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/push/cdi/WebsocketScopeManager.java`
#### Snippet
```java
    */
    @ViewScoped
    public static class ViewScope extends AbstractUserScope implements Serializable
    {
        @Inject private WebsocketScopeManager scopeManager;
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/push/cdi/WebsocketScopeManager.java`
#### Snippet
```java
     */
    @SessionScoped
    public static class SessionScope extends AbstractUserScope implements Serializable
    {
        @Inject private WebsocketSessionManager sessionManager;
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/util/lang/LRULinkedHashMap.java`
#### Snippet
```java
import java.util.Map;

public class LRULinkedHashMap<K, V> extends LinkedHashMap<K, V> implements Serializable
{
    private static final float DEFAULT_LOAD_FACTOR = 0.75f;
```

### RedundantImplements
Redundant interface declaration `FacesWrapper`
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ResourceLocationValueExpression.java`
#### Snippet
```java
 * @author Leonardo Uribe 
 */
public class ResourceLocationValueExpression extends LocationValueExpression implements FacesWrapper<ValueExpression>
{
    
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/PhaseListenerHandler.java`
#### Snippet
```java
{

    private final static class LazyPhaseListener implements PhaseListener, Serializable
    {
        private static final long serialVersionUID = -6496143057319213401L;
```

### RedundantImplements
Redundant interface declaration `TemplateClient`
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFaceletContext.java`
#### Snippet
```java
    }
    
    private final static class CompositeComponentTemplateManager extends TemplateManager implements TemplateClient
    {
        private final AbstractFacelet _owner;
```

### RedundantImplements
Redundant interface declaration `TemplateClient`
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/TemplateContextImpl.java`
#### Snippet
```java
    }
    
    private final static class TemplateManagerImpl extends TemplateManager implements TemplateClient
    {
        private final AbstractFacelet _owner;
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/config/impl/element/AbsoluteOrderingImpl.java`
#### Snippet
```java
import java.util.List;

public class AbsoluteOrderingImpl extends org.apache.myfaces.config.element.AbsoluteOrdering implements Serializable
{
    private List<org.apache.myfaces.config.element.OrderSlot> orderList;
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/config/impl/element/PropertyImpl.java`
#### Snippet
```java
 *
 */
public class PropertyImpl extends org.apache.myfaces.config.element.Property implements Serializable
{
    private List<String> description;
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/config/impl/element/AttributeImpl.java`
#### Snippet
```java
 * @version $Revision$ $Date$
 */
public class AttributeImpl extends org.apache.myfaces.config.element.Attribute implements Serializable
{
    private List<String> description;
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/config/impl/element/FacesConfigNameSlotImpl.java`
#### Snippet
```java

public class FacesConfigNameSlotImpl extends org.apache.myfaces.config.element.FacesConfigNameSlot
    implements Serializable
{
    private String name;
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/config/impl/element/FactoryImpl.java`
#### Snippet
```java
 * @author <a href="mailto:oliver@rossmueller.com">Oliver Rossmueller</a>
 */
public class FactoryImpl extends org.apache.myfaces.config.element.Factory implements Serializable
{
    private List<String> applicationFactories;
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ViewParamImpl.java`
#### Snippet
```java
import java.io.Serializable;

public class ViewParamImpl extends org.apache.myfaces.config.element.ViewParam implements Serializable
{
    private String name;
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ResourceBundleImpl.java`
#### Snippet
```java
 * @version $Revision$ $Date$
 */
public class ResourceBundleImpl extends org.apache.myfaces.config.element.ResourceBundle implements Serializable
{
    private String baseName;
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/config/impl/element/NamedEventImpl.java`
#### Snippet
```java
import java.io.Serializable;

public class NamedEventImpl extends org.apache.myfaces.config.element.NamedEvent implements Serializable
{
    private String shortName;
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/config/impl/element/BehaviorImpl.java`
#### Snippet
```java
 * Implementation of model for &lt;behavior&gt; element.
 */
public class BehaviorImpl extends org.apache.myfaces.config.element.Behavior implements Serializable
{
    private String behaviorClass;
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/config/impl/element/OrderingImpl.java`
#### Snippet
```java
import java.util.List;

public class OrderingImpl extends org.apache.myfaces.config.element.Ordering implements Serializable
{
    private List<org.apache.myfaces.config.element.OrderSlot> beforeList;
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/config/impl/element/NavigationCaseImpl.java`
#### Snippet
```java
 * @author <a href="mailto:oliver@rossmueller.com">Oliver Rossmueller</a>
 */
public class NavigationCaseImpl extends org.apache.myfaces.config.element.NavigationCase implements Serializable
{    
    private String fromAction;
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ApplicationImpl.java`
#### Snippet
```java
 * @author <a href="mailto:oliver@rossmueller.com">Oliver Rossmueller</a>
 */
public class ApplicationImpl extends org.apache.myfaces.config.element.Application implements Serializable
{
    private List<String> actionListener;
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ConverterImpl.java`
#### Snippet
```java
 * @author <a href="mailto:oliver@rossmueller.com">Oliver Rossmueller</a>
 */
public class ConverterImpl extends org.apache.myfaces.config.element.Converter implements Serializable
{
    private String converterId;
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ViewPoolMappingImpl.java`
#### Snippet
```java
import org.apache.myfaces.config.element.ViewPoolParameter;

public class ViewPoolMappingImpl extends ViewPoolMapping implements Serializable
{
    private String urlPattern;
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/config/impl/element/SystemEventListenerImpl.java`
#### Snippet
```java

public class SystemEventListenerImpl extends org.apache.myfaces.config.element.SystemEventListener
    implements Serializable
{
    private String systemEventListenerClass;
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ComponentTagDeclarationImpl.java`
#### Snippet
```java
 * @author lu4242
 */
public class ComponentTagDeclarationImpl extends ComponentTagDeclaration implements Serializable
{
    private String componentType;
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/config/impl/element/FaceletsTemplateMappingImpl.java`
#### Snippet
```java
import org.apache.myfaces.config.element.FaceletsTemplateMapping;

public class FaceletsTemplateMappingImpl extends FaceletsTemplateMapping implements Serializable
{
    private String urlPattern;
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/config/impl/element/LocaleConfigImpl.java`
#### Snippet
```java
 * @author <a href="mailto:oliver@rossmueller.com">Oliver Rossmueller</a>
 */
public class LocaleConfigImpl extends org.apache.myfaces.config.element.LocaleConfig implements Serializable
{
    private String defaultLocale;
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/config/impl/element/RendererImpl.java`
#### Snippet
```java
 * @author <a href="mailto:oliver@rossmueller.com">Oliver Rossmueller</a>
 */
public class RendererImpl extends org.apache.myfaces.config.element.Renderer implements Serializable
{
    private String componentFamily;
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ConfigOthersSlotImpl.java`
#### Snippet
```java
import java.io.Serializable;

public class ConfigOthersSlotImpl extends org.apache.myfaces.config.element.ConfigOthersSlot implements Serializable
{

```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/config/impl/element/RenderKitImpl.java`
#### Snippet
```java
 * @author <a href="mailto:oliver@rossmueller.com">Oliver Rossmueller</a>
 */
public class RenderKitImpl extends org.apache.myfaces.config.element.RenderKit implements Serializable
{
    private String id;
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ComponentImpl.java`
#### Snippet
```java
 * @author <a href="mailto:oliver@rossmueller.com">Oliver Rossmueller</a>
 */
public class ComponentImpl extends org.apache.myfaces.config.element.Component implements Serializable
{
    private String componentType;
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/config/impl/element/NavigationRuleImpl.java`
#### Snippet
```java
 * @author <a href="mailto:oliver@rossmueller.com">Oliver Rossmueller</a>
 */
public class NavigationRuleImpl extends org.apache.myfaces.config.element.NavigationRule implements Serializable
{
    private String fromViewId;
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/config/impl/element/RedirectImpl.java`
#### Snippet
```java
import java.util.Map;

public class RedirectImpl extends org.apache.myfaces.config.element.Redirect implements Serializable
{
    private Map<String,List<String>> viewParams;
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ClientBehaviorRendererImpl.java`
#### Snippet
```java
public class ClientBehaviorRendererImpl
        extends org.apache.myfaces.config.element.ClientBehaviorRenderer
        implements Serializable
{
    private String rendererType;
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/config/impl/element/OrderSlotImpl.java`
#### Snippet
```java
import java.io.Serializable;

public class OrderSlotImpl extends org.apache.myfaces.config.element.OrderSlot implements Serializable
{

```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/config/impl/element/facelets/FaceletSourceTagImpl.java`
#### Snippet
```java
 *
 */
public class FaceletSourceTagImpl extends FaceletSourceTag implements Serializable
{
    private String source;
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/config/impl/element/facelets/FaceletHandlerTagImpl.java`
#### Snippet
```java
import org.apache.myfaces.config.element.facelets.FaceletHandlerTag;

public class FaceletHandlerTagImpl extends FaceletHandlerTag implements Serializable
{
    private String handlerClass;
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/config/impl/element/facelets/FaceletComponentTagImpl.java`
#### Snippet
```java
import org.apache.myfaces.config.element.facelets.FaceletComponentTag;

public class FaceletComponentTagImpl extends FaceletComponentTag implements Serializable
{
    private String componentType;
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/config/impl/element/facelets/FaceletTagImpl.java`
#### Snippet
```java
import org.apache.myfaces.config.element.facelets.FaceletTagDefinition;

public class FaceletTagImpl extends FaceletTag implements Serializable
{
    private String name;
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/config/impl/element/facelets/FaceletTagLibraryImpl.java`
#### Snippet
```java
import org.apache.myfaces.config.element.facelets.FaceletTagLibrary;

public class FaceletTagLibraryImpl extends FaceletTagLibrary implements Serializable
{
    private String namespace;
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/config/impl/element/facelets/FaceletFunctionImpl.java`
#### Snippet
```java
import org.apache.myfaces.config.element.facelets.FaceletFunction;

public class FaceletFunctionImpl extends FaceletFunction implements Serializable
{
    private String functionName;
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/config/impl/element/facelets/FaceletBehaviorTagImpl.java`
#### Snippet
```java
import org.apache.myfaces.config.element.facelets.FaceletBehaviorTag;

public class FaceletBehaviorTagImpl extends FaceletBehaviorTag implements Serializable
{
    private String behaviorId;
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/config/impl/element/facelets/FaceletValidatorTagImpl.java`
#### Snippet
```java
import org.apache.myfaces.config.element.facelets.FaceletValidatorTag;

public class FaceletValidatorTagImpl extends FaceletValidatorTag implements Serializable
{
    private String validatorId;
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/config/impl/element/facelets/FaceletConverterTagImpl.java`
#### Snippet
```java
import org.apache.myfaces.config.element.facelets.FaceletConverterTag;

public class FaceletConverterTagImpl extends FaceletConverterTag implements Serializable
{
    private String converterId;
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/config/element/FacesConfigNameSlot.java`
#### Snippet
```java
import java.io.Serializable;

public abstract class FacesConfigNameSlot extends OrderSlot implements Serializable
{
    public abstract String getName();
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/config/impl/element/FacesConfigImpl.java`
#### Snippet
```java
 * @author <a href="mailto:oliver@rossmueller.com">Oliver Rossmueller</a>
 */
public class FacesConfigImpl extends org.apache.myfaces.config.element.FacesConfig implements Serializable
{
    private static final long serialVersionUID = 1L;
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/config/element/ConfigOthersSlot.java`
#### Snippet
```java
 * @since 2.0.3
 */
public abstract class ConfigOthersSlot extends OrderSlot implements Serializable
{

```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/config/element/facelets/FaceletSourceTag.java`
#### Snippet
```java
 *
 */
public abstract class FaceletSourceTag extends FaceletTagDefinition implements Serializable
{
    public abstract String getSource();
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/config/element/facelets/FaceletComponentTag.java`
#### Snippet
```java
 *
 */
public abstract class FaceletComponentTag extends FaceletTagDefinition implements Serializable
{
    public abstract String getComponentType();
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/config/element/facelets/FaceletValidatorTag.java`
#### Snippet
```java
 *
 */
public abstract class FaceletValidatorTag extends FaceletTagDefinition implements Serializable
{
    public abstract String getValidatorId();
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/config/element/facelets/FaceletHandlerTag.java`
#### Snippet
```java
 *
 */
public abstract class FaceletHandlerTag extends FaceletTagDefinition implements Serializable
{
    public abstract String getHandlerClass();
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/config/element/facelets/FaceletBehaviorTag.java`
#### Snippet
```java
 *
 */
public abstract class FaceletBehaviorTag extends FaceletTagDefinition implements Serializable
{
    public abstract String getBehaviorId();
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/config/element/facelets/FaceletConverterTag.java`
#### Snippet
```java
 *
 */
public abstract class FaceletConverterTag extends FaceletTagDefinition implements Serializable
{
    public abstract String getConverterId();
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/application/viewstate/SerializedViewKeyIntInt.java`
#### Snippet
```java
 * and the sequenceId is a numeric value.
 */
class SerializedViewKeyIntInt extends SerializedViewKey implements Serializable
{
    private static final long serialVersionUID = -1170697124386063642L;
```

### RedundantImplements
Redundant interface declaration `Serializable`
in `impl/src/main/java/org/apache/myfaces/application/viewstate/SerializedViewKeyIntByteArray.java`
#### Snippet
```java
 * and the sequenceId is a string value.
 */
class SerializedViewKeyIntByteArray extends SerializedViewKey implements Serializable
{
    final int _viewId;
```

## RuleId[ruleID=StringBufferReplaceableByStringBuilder]
### StringBufferReplaceableByStringBuilder
`StringBuffer buffer` may be declared as 'StringBuilder'
in `test/src/main/java/org/apache/myfaces/test/base/junit/runners/NamedRunner.java`
#### Snippet
```java
    private static String getKey()
    {
        StringBuffer buffer = new StringBuffer();
        buffer.append(PREFIX_KEY).append(Thread.currentThread().getName());
        return buffer.toString();
```

### StringBufferReplaceableByStringBuilder
`StringBuffer sb` may be declared as 'StringBuilder'
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletRequest.java`
#### Snippet
```java
    {

        StringBuffer sb = new StringBuffer();
        if (contextPath != null)
        {
```

### StringBufferReplaceableByStringBuilder
`StringBuffer sb` may be declared as 'StringBuilder'
in `test/src/main/java/org/apache/myfaces/test/mock/_ELText.java`
#### Snippet
```java
        public String toString()
        {
            StringBuffer sb = new StringBuffer();
            for (int i = 0; i < this.txt.length; i++)
            {
```

### StringBufferReplaceableByStringBuilder
`StringBuffer sb` may be declared as 'StringBuilder'
in `test/src/main/java/org/apache/myfaces/test/mock/_ELText.java`
#### Snippet
```java
        public String toString(ELContext ctx)
        {
            StringBuffer sb = new StringBuffer();
            for (int i = 0; i < this.txt.length; i++)
            {
```

### StringBufferReplaceableByStringBuilder
`StringBuffer buff` may be declared as 'StringBuilder'
in `test/src/main/java/org/apache/myfaces/test/mock/_ELText.java`
#### Snippet
```java
        int vlen = 0;

        StringBuffer buff = new StringBuffer(128);
        List<_ELText> text = new ArrayList<_ELText>();
        _ELText t = null;
```

## RuleId[ruleID=NonStaticFinalLogger]
### NonStaticFinalLogger
Non-constant logger field `log`
in `api/src/main/java/jakarta/faces/component/UIComponentBase.java`
#### Snippet
```java
public abstract class UIComponentBase extends UIComponent
{
    private static Logger log = Logger.getLogger(UIComponentBase.class.getName());

    private static final String _STRING_BUILDER_KEY
```

### NonStaticFinalLogger
Non-constant logger field `logger`
in `api/src/main/java/jakarta/faces/component/UIViewRoot.java`
#### Snippet
```java
            = "jakarta.faces.VIEWROOT_PHASE_LISTENER_QUEUES_EXCEPTIONS";

    private transient Logger logger = null;

    private static final PhaseProcessor APPLY_REQUEST_VALUES_PROCESSOR = new ApplyRequestValuesPhaseProcessor();
```

### NonStaticFinalLogger
Non-constant logger field `log`
in `impl/src/main/java/org/apache/myfaces/spi/impl/DefaultInjectionProviderFactory.java`
#### Snippet
```java
public class DefaultInjectionProviderFactory extends InjectionProviderFactory
{
    private static Logger log = Logger.getLogger(DefaultInjectionProviderFactory.class.getName());


```

### NonStaticFinalLogger
Non-constant logger field `log`
in `impl/src/main/java/org/apache/myfaces/util/MessageUtils.java`
#### Snippet
```java
    private static final String DETAIL_SUFFIX = "_detail";

    private static Logger log = Logger.getLogger(MessageUtils.class.getName());

    /**
```

### NonStaticFinalLogger
Non-constant logger field `log`
in `impl/src/main/java/org/apache/myfaces/util/token/SessionIdGenerator.java`
#### Snippet
```java
{

    private static Logger log = Logger.getLogger(SessionIdGenerator.class.getName());

    /**
```

## RuleId[ruleID=FuseStreamOperations]
### FuseStreamOperations
Stream may be extended replacing unmodifiableList
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlInputFileRendererBase.java`
#### Snippet
```java
            return Collections.unmodifiableList(parts.stream()
                        .filter(part -> !isEmpty(part))
                        .collect(Collectors.toList()));
        }
        return submittedValue;
```

## RuleId[ruleID=NonFinalFieldOfException]
### NonFinalFieldOfException
Non-final field `_facesMessage` of exception class
in `api/src/main/java/jakarta/faces/convert/ConverterException.java`
#### Snippet
```java
    private static final long serialVersionUID = 8668056061177480197L;
    // FIELDS
    private FacesMessage _facesMessage;

    // CONSTRUCTORS
```

### NonFinalFieldOfException
Non-final field `_facesMessage` of exception class
in `api/src/main/java/jakarta/faces/component/UpdateModelException.java`
#### Snippet
```java
public class UpdateModelException extends FacesException
{
    private FacesMessage _facesMessage;
    
    public UpdateModelException(FacesMessage facesMessage, Throwable cause)
```

### NonFinalFieldOfException
Non-final field `_facesMessages` of exception class
in `api/src/main/java/jakarta/faces/validator/ValidatorException.java`
#### Snippet
```java
    private static final long serialVersionUID = 5965885122446047949L;
    private FacesMessage _facesMessage;
    private Collection<FacesMessage> _facesMessages;

    public ValidatorException(Collection<FacesMessage> messages)
```

### NonFinalFieldOfException
Non-final field `_facesMessage` of exception class
in `api/src/main/java/jakarta/faces/validator/ValidatorException.java`
#### Snippet
```java
{
    private static final long serialVersionUID = 5965885122446047949L;
    private FacesMessage _facesMessage;
    private Collection<FacesMessage> _facesMessages;

```

### NonFinalFieldOfException
Non-final field `viewId` of exception class
in `api/src/main/java/jakarta/faces/application/ViewExpiredException.java`
#### Snippet
```java
{

    private String viewId;

    public ViewExpiredException()
```

### NonFinalFieldOfException
Non-final field `location` of exception class
in `impl/src/main/java/org/apache/myfaces/view/facelets/LocationAwareFacesException.java`
#### Snippet
```java
public class LocationAwareFacesException extends FacesException implements LocationAware
{
    private Location location;
    
    public LocationAwareFacesException()
```

### NonFinalFieldOfException
Non-final field `_delegate` of exception class
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ContextAwarePropertyNotWritableException.java`
#### Snippet
```java
{

    private ContextAwareExceptionWrapper _delegate;

    public ContextAwarePropertyNotWritableException(Location location,
```

### NonFinalFieldOfException
Non-final field `_delegate` of exception class
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ContextAwareException.java`
#### Snippet
```java
    private static final long serialVersionUID = 7239367504106332588L;

    private ContextAwareExceptionWrapper _delegate;

    public ContextAwareException(Location location, String expressionString,
```

### NonFinalFieldOfException
Non-final field `_delegate` of exception class
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ContextAwareMethodNotFoundException.java`
#### Snippet
```java
    private static final long serialVersionUID = -8172862923048615707L;

    private ContextAwareExceptionWrapper _delegate;


```

### NonFinalFieldOfException
Non-final field `_delegate` of exception class
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ContextAwarePropertyNotFoundException.java`
#### Snippet
```java
    private static final long serialVersionUID = -4194177998555929451L;

    private ContextAwareExceptionWrapper _delegate;

    public ContextAwarePropertyNotFoundException(Location location,
```

### NonFinalFieldOfException
Non-final field `_delegate` of exception class
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ContextAwareELException.java`
#### Snippet
```java
    private static final long serialVersionUID = -2231893442274827689L;

    private ContextAwareExceptionWrapper _delegate;

    public ContextAwareELException(Location location, String expressionString,
```

## RuleId[ruleID=ZeroLengthArrayInitialization]
### ZeroLengthArrayInitialization
Allocation of zero length array
in `api/src/main/java/jakarta/faces/component/_DeltaList.java`
#### Snippet
```java
class _DeltaList<T> extends ArrayList<T> implements List<T>, PartialStateHolder, RandomAccess
{
    private static Object[] emptyObjectArray = new Object[]{};

    private boolean _initialStateMarked;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `api/src/main/java/jakarta/faces/component/behavior/_DeltaList.java`
#### Snippet
```java
class _DeltaList<T> extends ArrayList<T> implements List<T>, PartialStateHolder, RandomAccess
{
    private static Object[] emptyObjectArray = new Object[]{};

    private boolean _initialStateMarked;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `impl/src/main/java/org/apache/myfaces/util/lang/StreamCharBuffer.java`
#### Snippet
```java
        if (currentSize == 0)
        {
            return new char[0];
        }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TagLibraryConfig.java`
#### Snippet
```java
                    if (ps.length == 1 && "".equals(ps[0]))
                    {
                        pc = new Class[0];
                    }
                    else
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `impl/src/main/java/org/apache/myfaces/config/FacesConfigurator.java`
#### Snippet
```java
{
    private final Class<?>[] NO_PARAMETER_TYPES = new Class[]{};
    private final Object[] NO_PARAMETERS = new Object[]{};

    private static final Logger log = Logger.getLogger(FacesConfigurator.class.getName());
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `impl/src/main/java/org/apache/myfaces/config/FacesConfigurator.java`
#### Snippet
```java
public class FacesConfigurator
{
    private final Class<?>[] NO_PARAMETER_TYPES = new Class[]{};
    private final Object[] NO_PARAMETERS = new Object[]{};

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `impl/src/main/java/org/apache/myfaces/config/webparameters/MyfacesConfig.java`
#### Snippet
```java
    private boolean markInitialStateWhenApplyBuildView = MARK_INITIAL_STATE_WHEN_APPLY_BUILD_VIEW_DEFAULT;
    private String[] viewSuffix = new String[] { ViewHandler.DEFAULT_SUFFIX };
    private String[] faceletsViewMappings = new String[] {};
    private String faceletsViewSuffix = ViewHandler.DEFAULT_FACELETS_SUFFIX;
    private ELExpressionCacheMode elExpressionCacheMode;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `impl/src/main/java/org/apache/myfaces/application/FacesServletMappingUtils.java`
#### Snippet
```java
            if (mappings == null)
            {
                mappings = new String[]{ };
            }
        }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `test/src/main/java/org/apache/myfaces/test/el/MockValueExpression.java`
#### Snippet
```java
        if (isLiteralText())
        {
            elements = new String[0];
            return;
        }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `test/src/main/java/org/apache/myfaces/test/el/MockMethodExpression.java`
#### Snippet
```java
        if (isLiteralText())
        {
            elements = new String[0];
            return;
        }
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractJsfTestCase.java`
#### Snippet
```java
        Thread.currentThread()
                .setContextClassLoader(
                        new URLClassLoader(new URL[0], this.getClass()
                                .getClassLoader()));
        classLoaderSet = true;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractJsfConfigurableMultipleRequestsTestCase.java`
#### Snippet
```java
        Thread.currentThread()
                .setContextClassLoader(
                        new URLClassLoader(new URL[0], this.getClass()
                                .getClassLoader()));
        classLoaderSet = true;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `test/src/main/java/org/apache/myfaces/test/base/junit/AbstractJsfConfigurableMockTestCase.java`
#### Snippet
```java
        Thread.currentThread()
                .setContextClassLoader(
                        new URLClassLoader(new URL[0], this.getClass()
                                .getClassLoader()));
        classLoaderSet = true;
```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `test/src/main/java/org/apache/myfaces/test/mock/MockServletInputStream.java`
#### Snippet
```java
    public MockServletInputStream()
    {
        this.source = new ByteArrayInputStream(new byte[0]);
    }

```

### ZeroLengthArrayInitialization
Allocation of zero length array
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/HtmlRendererUtils.java`
#### Snippet
```java
               So in fact, there must be component value at this location, but for listboxes, comboboxes etc.
               the submitted value is not posted if no item is selected. */
            ((EditableValueHolder) component).setSubmittedValue(new String[] {});
        }
    }
```

## RuleId[ruleID=RedundantUnmodifiable]
### RedundantUnmodifiable
Redundant usage of the 'unmodifiableList' wrapper
in `impl/src/main/java/org/apache/myfaces/context/servlet/FacesContextImpl.java`
#### Snippet
```java
        if (_messages == null)
        {
            return Collections.unmodifiableList(Collections.<FacesMessage>emptyList());
        }
        
```

### RedundantUnmodifiable
Redundant usage of the 'unmodifiableList' wrapper
in `impl/src/main/java/org/apache/myfaces/context/servlet/FacesContextImpl.java`
#### Snippet
```java
        if (_messages == null || !_messages.containsKey(clientId))
        {
            return Collections.unmodifiableList(Collections.<FacesMessage>emptyList());
        }
        
```

### RedundantUnmodifiable
Redundant usage of the 'unmodifiableList' wrapper
in `test/src/main/java/org/apache/myfaces/test/mock/MockFacesContext20.java`
#### Snippet
```java
        if (messages == null)
        {
            return Collections.unmodifiableList(Collections
                    .<FacesMessage> emptyList());
        }
```

### RedundantUnmodifiable
Redundant usage of the 'unmodifiableList' wrapper
in `test/src/main/java/org/apache/myfaces/test/mock/MockFacesContext20.java`
#### Snippet
```java
        if (messages == null || !messages.containsKey(clientId))
        {
            return Collections.unmodifiableList(Collections
                    .<FacesMessage> emptyList());
        }
```

### RedundantUnmodifiable
Redundant usage of the 'unmodifiableSet' wrapper
in `api/src/main/java/jakarta/faces/application/ViewHandler.java`
#### Snippet
```java
    {
        Set<String> set = Collections.emptySet();
        return Collections.unmodifiableSet(set);
    }
    
```

## RuleId[ruleID=NullArgumentToVariableArgMethod]
### NullArgumentToVariableArgMethod
Confusing argument `null`, unclear if a varargs or non-varargs call is desired
in `api/src/main/java/jakarta/faces/_FactoryFinderProviderFactory.java`
#### Snippet
```java
            try
            {
                return FACTORY_FINDER_PROVIDER_GET_INSTANCE_METHOD.invoke(FACTORY_FINDER_PROVIDER_FACTORY_CLASS, null);
            }
            catch (Exception e)
```

### NullArgumentToVariableArgMethod
Confusing argument `null`, unclear if a varargs or non-varargs call is desired
in `api/src/main/java/jakarta/faces/_FactoryFinderProviderFactory.java`
#### Snippet
```java
            {
                factoryFinderproviderFactoryGetMethod = factoryFinderFactoryClass.getMethod
                        ("getInstance", null);
                factoryFinderproviderFactoryGetFactoryFinderMethod = factoryFinderFactoryClass
                        .getMethod("getFactoryFinderProvider", null);
```

### NullArgumentToVariableArgMethod
Confusing argument `null`, unclear if a varargs or non-varargs call is desired
in `api/src/main/java/jakarta/faces/_FactoryFinderProviderFactory.java`
#### Snippet
```java
                        ("getInstance", null);
                factoryFinderproviderFactoryGetFactoryFinderMethod = factoryFinderFactoryClass
                        .getMethod("getFactoryFinderProvider", null);
            }

```

### NullArgumentToVariableArgMethod
Confusing argument `null`, unclear if a varargs or non-varargs call is desired
in `api/src/main/java/jakarta/faces/_FactoryFinderProviderFactory.java`
#### Snippet
```java
                        new Class[]{String.class, String.class});
                factoryFinderProviderReleaseFactoriesMethod = factoryFinderProviderClass.getMethod
                        ("releaseFactories", null);
            }
            
```

### NullArgumentToVariableArgMethod
Confusing argument `null`, unclear if a varargs or non-varargs call is desired
in `api/src/main/java/jakarta/faces/_FactoryFinderProviderFactory.java`
#### Snippet
```java
            {
                injectionProviderFactoryGetInstanceMethod = injectionProviderFactoryClass.
                    getMethod("getInjectionProviderFactory", null);
                injectionProviderFactoryGetInjectionProviderMethod = injectionProviderFactoryClass.
                    getMethod("getInjectionProvider", ExternalContext.class);
```

### NullArgumentToVariableArgMethod
Confusing argument `null`, unclear if a varargs or non-varargs call is desired
in `api/src/main/java/jakarta/faces/FactoryFinder.java`
#### Snippet
```java
                Object ffp = _FactoryFinderProviderFactory
                        .FACTORY_FINDER_PROVIDER_FACTORY_GET_FACTORY_FINDER_METHOD
                        .invoke(factoryFinderProviderFactoryInstance, null);
                
                //Call getFactory method and pass the params
```

### NullArgumentToVariableArgMethod
Confusing argument `null`, unclear if a varargs or non-varargs call is desired
in `api/src/main/java/jakarta/faces/FactoryFinder.java`
#### Snippet
```java
                
                //Call getFactory method and pass the params
                _FactoryFinderProviderFactory.FACTORY_FINDER_PROVIDER_RELEASE_FACTORIES_METHOD.invoke(ffp, null);
            }
            catch (InvocationTargetException e)
```

### NullArgumentToVariableArgMethod
Confusing argument `null`, unclear if a varargs or non-varargs call is desired
in `api/src/main/java/jakarta/faces/FactoryFinder.java`
#### Snippet
```java
                Object ffp = _FactoryFinderProviderFactory
                        .FACTORY_FINDER_PROVIDER_FACTORY_GET_FACTORY_FINDER_METHOD
                        .invoke(factoryFinderProviderFactoryInstance, null);
                
                //Call getFactory method and pass the params
```

### NullArgumentToVariableArgMethod
Confusing argument `null`, unclear if a varargs or non-varargs call is desired
in `api/src/main/java/jakarta/faces/FactoryFinder.java`
#### Snippet
```java
                Object ffp = _FactoryFinderProviderFactory
                        .FACTORY_FINDER_PROVIDER_FACTORY_GET_FACTORY_FINDER_METHOD
                        .invoke(factoryFinderProviderFactoryInstance, null);
                
                //Call getFactory method and pass the params
```

### NullArgumentToVariableArgMethod
Confusing argument `null`, unclear if a varargs or non-varargs call is desired
in `impl/src/main/java/org/apache/myfaces/renderkit/ErrorPageWriter.java`
#### Snippet
```java
                            else
                            {
                                v = m.invoke(c, null);
                                if (v != null)
                                {
```

## RuleId[ruleID=NonExceptionNameEndsWithException]
### NonExceptionNameEndsWithException
Non-exception class name `DefaultContextAwareELException` ends with 'Exception'
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/DefaultContextAwareELException.java`
#### Snippet
```java
 * @author martinkoci
 */
public class DefaultContextAwareELException implements ContextAwareExceptionWrapper, Serializable
{
    private static final long serialVersionUID = 1L;
```

## RuleId[ruleID=TypeParameterExtendsObject]
### TypeParameterExtendsObject
Type parameter `T` explicitly extends 'java.lang.Object'
in `api/src/main/java/org/apache/myfaces/core/api/shared/lang/PropertyDescriptorUtils.java`
#### Snippet
```java

    @FunctionalInterface
    public interface ObjFloatConsumer<T extends Object>
    {
        public void accept(T t, float i);
```

### TypeParameterExtendsObject
Type parameter `T` explicitly extends 'java.lang.Object'
in `api/src/main/java/org/apache/myfaces/core/api/shared/lang/PropertyDescriptorUtils.java`
#### Snippet
```java

    @FunctionalInterface
    public interface ObjByteConsumer<T extends Object>
    {
        public void accept(T t, byte i);
```

### TypeParameterExtendsObject
Type parameter `T` explicitly extends 'java.lang.Object'
in `api/src/main/java/org/apache/myfaces/core/api/shared/lang/PropertyDescriptorUtils.java`
#### Snippet
```java

    @FunctionalInterface
    public interface ObjBooleanConsumer<T extends Object>
    {
        public void accept(T t, boolean i);
```

### TypeParameterExtendsObject
Type parameter `T` explicitly extends 'java.lang.Object'
in `api/src/main/java/org/apache/myfaces/core/api/shared/lang/PropertyDescriptorUtils.java`
#### Snippet
```java

    @FunctionalInterface
    public interface ObjShortConsumer<T extends Object>
    {
        public void accept(T t, short i);
```

### TypeParameterExtendsObject
Type parameter `T` explicitly extends 'java.lang.Object'
in `api/src/main/java/org/apache/myfaces/core/api/shared/lang/PropertyDescriptorUtils.java`
#### Snippet
```java

    @FunctionalInterface
    public interface ObjCharConsumer<T extends Object>
    {
        public void accept(T t, char i);
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `api/src/main/java/jakarta/faces/context/FlashWrapper.java`
#### Snippet
```java

    @Override
    public void putAll(Map<? extends String, ? extends Object> m)
    {
        getWrapped().putAll(m);
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `api/src/main/java/jakarta/faces/component/_ViewAttributeMap.java`
#### Snippet
```java

    @Override
    public void putAll(Map<? extends String, ? extends Object> m)
    {
        _delegate.putAll(m);
```

### TypeParameterExtendsObject
Type parameter `T` explicitly extends 'java.lang.Object'
in `impl/src/main/java/org/apache/myfaces/el/resolver/EmptyStringToNullELResolver.java`
#### Snippet
```java

    @Override
    public <T extends Object> T convertToType(ELContext context, Object value, Class<T> targetType)
    {
        if (value == null && targetType == String.class)
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `impl/src/main/java/org/apache/myfaces/el/resolver/CompositeComponentELResolver.java`
#### Snippet
```java

        @Override
        public void putAll(Map<? extends String, ? extends Object> m)
        {
            for (Entry<? extends String, ? extends Object> entry : m.entrySet())
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `impl/src/main/java/org/apache/myfaces/el/resolver/CompositeComponentELResolver.java`
#### Snippet
```java
        public void putAll(Map<? extends String, ? extends Object> m)
        {
            for (Entry<? extends String, ? extends Object> entry : m.entrySet())
            {
                put(entry.getKey(), entry.getValue());
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `impl/src/main/java/org/apache/myfaces/cdi/view/ViewScopeCDIMap.java`
#### Snippet
```java

    @Override
    public void putAll(Map<? extends String, ? extends Object> m)
    {
        for (Map.Entry<? extends String, ? extends Object> entry : m.entrySet())
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `impl/src/main/java/org/apache/myfaces/cdi/view/ViewScopeCDIMap.java`
#### Snippet
```java
    public void putAll(Map<? extends String, ? extends Object> m)
    {
        for (Map.Entry<? extends String, ? extends Object> entry : m.entrySet())
        {
            put(entry.getKey(), entry.getValue());
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `impl/src/main/java/org/apache/myfaces/flow/impl/FlowScopeMap.java`
#### Snippet
```java

    @Override
    public void putAll(Map<? extends Object, ? extends Object> m)
    {
        getWrapped(true).putAll(m);
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `impl/src/main/java/org/apache/myfaces/flow/impl/FlowScopeMap.java`
#### Snippet
```java

    @Override
    public void putAll(Map<? extends Object, ? extends Object> m)
    {
        getWrapped(true).putAll(m);
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `impl/src/main/java/org/apache/myfaces/view/ViewScopeProxyMap.java`
#### Snippet
```java

    @Override
    public void putAll(Map<? extends String, ? extends Object> m)
    {
        getDelegate().putAll(m);
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/UILeaf.java`
#### Snippet
```java

    @Override
    public void putAll(Map<? extends String, ? extends Object> t)
    {
        for (Map.Entry<? extends String, ? extends Object> entry : t.entrySet())
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/UILeaf.java`
#### Snippet
```java
    public void putAll(Map<? extends String, ? extends Object> t)
    {
        for (Map.Entry<? extends String, ? extends Object> entry : t.entrySet())
        {
            put(entry.getKey(), entry.getValue());
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `impl/src/main/java/org/apache/myfaces/context/servlet/SessionMap.java`
#### Snippet
```java

    @Override
    public void putAll(final Map<? extends String, ? extends Object> t)
    {
        throw new UnsupportedOperationException();
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `impl/src/main/java/org/apache/myfaces/context/servlet/ApplicationMap.java`
#### Snippet
```java

    @Override
    public void putAll(final Map<? extends String, ? extends Object> t)
    {
        throw new UnsupportedOperationException();
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `impl/src/main/java/org/apache/myfaces/context/servlet/RequestMap.java`
#### Snippet
```java

    @Override
    public void putAll(final Map<? extends String, ? extends Object> t)
    {
        throw new UnsupportedOperationException();
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `impl/src/main/java/org/apache/myfaces/context/servlet/CookieMap.java`
#### Snippet
```java

    @Override
    public void putAll(final Map<? extends String, ? extends Object> t)
    {
        throw new UnsupportedOperationException();
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `impl/src/main/java/org/apache/myfaces/context/flash/FlashImpl.java`
#### Snippet
```java

    @Override
    public void putAll(Map<? extends String, ? extends Object> m)
    {
        _checkFlashScopeDisabled();
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `impl/src/main/java/org/apache/myfaces/renderkit/ErrorPageWriter.java`
#### Snippet
```java
    }

    private static void writeVariables(Writer writer, Map<String, ? extends Object> vars, String caption)
            throws IOException
    {
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `test/src/main/java/org/apache/myfaces/test/mock/_SessionMap.java`
#### Snippet
```java

    @Override
    public void putAll(final Map<? extends String, ? extends Object> t)
    {
        throw new UnsupportedOperationException();
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `test/src/main/java/org/apache/myfaces/test/mock/MockFlash.java`
#### Snippet
```java
    }

    public void putAll(Map<? extends String, ? extends Object> m)
    {
        getCurrentPhaseMap().putAll(m);
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `test/src/main/java/org/apache/myfaces/test/mock/_RequestMap.java`
#### Snippet
```java

    @Override
    public void putAll(final Map<? extends String, ? extends Object> t)
    {
        throw new UnsupportedOperationException();
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `test/src/main/java/org/apache/myfaces/test/mock/_ApplicationMap.java`
#### Snippet
```java

    @Override
    public void putAll(final Map<? extends String, ? extends Object> t)
    {
        throw new UnsupportedOperationException();
```

### TypeParameterExtendsObject
Wildcard type argument `?` explicitly extends 'java.lang.Object'
in `test/src/main/java/org/apache/myfaces/test/mock/_CookieMap.java`
#### Snippet
```java

    @Override
    public void putAll(final Map<? extends String, ? extends Object> t)
    {
        throw new UnsupportedOperationException();
```

## RuleId[ruleID=ConstantValue]
### ConstantValue
Condition `Class.forName("jakarta.validation.Validation") != null` is always `true`
in `api/src/main/java/org/apache/myfaces/core/api/shared/ExternalSpecifications.java`
#### Snippet
```java
                try
                {
                    beanValidationAvailable = (Class.forName("jakarta.validation.Validation") != null);
                }
                catch(ClassNotFoundException e)
```

### ConstantValue
Condition `!(child instanceof UISelectItems)` is always `true` when reached
in `api/src/main/java/org/apache/myfaces/core/api/shared/SelectItemsIterator.java`
#### Snippet
```java
            // classes are found. If there is no more elements
            // that conform this condition, just return false.
            while (!(child instanceof UISelectItem) && !(child instanceof UISelectItems))
            {
                // Try to skip it
```

### ConstantValue
Condition `child instanceof UISelectItems` is always `false` when reached
in `api/src/main/java/org/apache/myfaces/core/api/shared/SelectItemsIterator.java`
#### Snippet
```java
            // classes are found. If there is no more elements
            // that conform this condition, just return false.
            while (!(child instanceof UISelectItem) && !(child instanceof UISelectItems))
            {
                // Try to skip it
```

### ConstantValue
Value `child` is always 'null'
in `api/src/main/java/org/apache/myfaces/core/api/shared/SelectItemsIterator.java`
#### Snippet
```java
            // classes are found. If there is no more elements
            // that conform this condition, just return false.
            while (!(child instanceof UISelectItem) && !(child instanceof UISelectItems))
            {
                // Try to skip it
```

### ConstantValue
Condition `child instanceof UISelectItem` is always `true`
in `api/src/main/java/org/apache/myfaces/core/api/shared/SelectItemsIterator.java`
#### Snippet
```java
                }
            }
            if (child instanceof UISelectItem)
            {
                UISelectItem uiSelectItem = (UISelectItem) child;
```

### ConstantValue
Condition `child instanceof UISelectItem` is always `false`
in `api/src/main/java/org/apache/myfaces/core/api/shared/SelectItemsUtil.java`
#### Snippet
```java
                createSelectItems(context, uiSelectItems, uiSelectItems.getValue(), SelectItem::new, items::add);
            }
            else if (child instanceof UISelectItem)
            {
                items.add(createSelectItem(child, null, SelectItem::new));
```

### ConstantValue
Value `child` is always 'null'
in `api/src/main/java/org/apache/myfaces/core/api/shared/SelectItemsUtil.java`
#### Snippet
```java
                createSelectItems(context, uiSelectItems, uiSelectItems.getValue(), SelectItem::new, items::add);
            }
            else if (child instanceof UISelectItem)
            {
                items.add(createSelectItem(child, null, SelectItem::new));
```

### ConstantValue
Condition `factoryFinderFactoryClass != null` is always `true`
in `api/src/main/java/jakarta/faces/_FactoryFinderProviderFactory.java`
#### Snippet
```java
        {
            factoryFinderFactoryClass = ClassUtils.classForName(FACTORY_FINDER_PROVIDER_FACTORY_CLASS_NAME);
            if (factoryFinderFactoryClass != null)
            {
                factoryFinderproviderFactoryGetMethod = factoryFinderFactoryClass.getMethod
```

### ConstantValue
Condition `factoryFinderProviderClass != null` is always `true`
in `api/src/main/java/jakarta/faces/_FactoryFinderProviderFactory.java`
#### Snippet
```java

            factoryFinderProviderClass = ClassUtils.classForName(FACTORY_FINDER_PROVIDER_CLASS_NAME);
            if (factoryFinderProviderClass != null)
            {
                factoryFinderProviderGetFactoryMethod = factoryFinderProviderClass.getMethod("getFactory",
```

### ConstantValue
Condition `injectionProviderFactoryClass != null` is always `true`
in `api/src/main/java/jakarta/faces/_FactoryFinderProviderFactory.java`
#### Snippet
```java
            injectionProviderFactoryClass = ClassUtils.classForName(INJECTION_PROVIDER_FACTORY_CLASS_NAME);
            
            if (injectionProviderFactoryClass != null)
            {
                injectionProviderFactoryGetInstanceMethod = injectionProviderFactoryClass.
```

### ConstantValue
Condition `injectionProviderClass != null` is always `true`
in `api/src/main/java/jakarta/faces/_FactoryFinderProviderFactory.java`
#### Snippet
```java
            injectionProviderClass = ClassUtils.classForName(INJECTION_PROVIDER_CLASS_NAME);
            
            if (injectionProviderClass != null)
            {
                injectionProviderInjectMethod = injectionProviderClass.
```

### ConstantValue
Condition `uiComponent == null` is always `false`
in `api/src/main/java/jakarta/faces/event/FacesEvent.java`
#### Snippet
```java
    {
        super(uiComponent);
        if (uiComponent == null)
        {
            throw new IllegalArgumentException("uiComponent");
```

### ConstantValue
Condition `oldRowIndex > rowIndex` is always `true`
in `api/src/main/java/jakarta/faces/model/IterableDataModel.java`
#### Snippet
```java
            // No change
        }
        else if (oldRowIndex > rowIndex)
        {
            // Retrieve it from cache
```

### ConstantValue
Value `found` is always 'true'
in `api/src/main/java/jakarta/faces/component/UIComponent.java`
#### Snippet
```java
                    throw new FacesException(e);
                }
                return found;
            }
            // Searching for this component's children/facets
```

### ConstantValue
Condition `_bundle.getObject(key.toString()) != null` is always `true`
in `api/src/main/java/jakarta/faces/component/_BundleMap.java`
#### Snippet
```java
        try
        {
            return _bundle.getObject(key.toString()) != null;
        }
        catch (MissingResourceException e)
```

### ConstantValue
Condition `_validatorList == null` is always `false`
in `api/src/main/java/jakarta/faces/component/UIInput.java`
#### Snippet
```java
            else
            {
                return _validatorList == null
                        ? EMPTY_VALIDATOR_ARRAY
                        : _validatorList.toArray(new Validator[_validatorList.size()]);
```

### ConstantValue
Condition `!(value instanceof Serializable)` is always `false`
in `api/src/main/java/jakarta/faces/component/_DeltaStateHelper.java`
#### Snippet
```java
                targetMap.put(key, UIComponentBase.restoreAttachedState(context, savedValue));
            }
            else if (!(value instanceof Serializable))
            {
                Object newInstance;
```

### ConstantValue
Condition `value instanceof Serializable` is always `true`
in `api/src/main/java/jakarta/faces/component/_DeltaStateHelper.java`
#### Snippet
```java
                targetMap.put(key, UIComponentBase.restoreAttachedState(context, savedValue));
            }
            else if (!(value instanceof Serializable))
            {
                Object newInstance;
```

### ConstantValue
Condition `cause != null` is always `true`
in `api/src/main/java/jakarta/faces/validator/MethodExpressionValidator.java`
#### Snippet
```java
                do
                {
                    if (cause != null && cause instanceof ValidatorException)
                    {
                        vex = (ValidatorException) cause;
```

### ConstantValue
Condition `_viewScope != null` is always `true`
in `api/src/main/java/jakarta/faces/component/UIViewRoot.java`
#### Snippet
```java
                return null;
            }
            else if (parentSaved == null && _viewScope != null && _viewScope.isEmpty()
                && !(_viewScope instanceof StateHolder) )
            {
```

### ConstantValue
Condition `cause != null` is always `true`
in `api/src/main/java/jakarta/faces/component/UIViewRoot.java`
#### Snippet
```java
                do
                {
                    if (cause != null && cause instanceof AbortProcessingException)
                    {
                        ape = (AbortProcessingException) cause;
```

### ConstantValue
Condition `isBuildingInitialState` is always `true` when reached
in `api/src/main/java/jakarta/faces/component/UIViewRoot.java`
#### Snippet
```java
                
                boolean isPostAddToViewEventAfterBuildInitialState =
                        !isBuildingInitialState || (isBuildingInitialState && isRefreshTransientBuild);
                if (isPostAddToViewEventAfterBuildInitialState)
                {
```

### ConstantValue
Condition `locale instanceof String` is always `false`
in `api/src/main/java/jakarta/faces/component/UIViewRoot.java`
#### Snippet
```java
                return (Locale)locale;
            }
            else if (locale instanceof String)
            {
                return LocaleUtils.toLocale((String)locale);
```

### ConstantValue
Value `locale` is always 'null'
in `api/src/main/java/jakarta/faces/component/UIViewRoot.java`
#### Snippet
```java
                return (Locale)locale;
            }
            else if (locale instanceof String)
            {
                return LocaleUtils.toLocale((String)locale);
```

### ConstantValue
Value `returnValue` is always 'true'
in `api/src/main/java/jakarta/faces/component/UIData.java`
#### Snippet
```java
            if (returnValue)
            {
                return returnValue;
            }
            
```

### ConstantValue
Condition `dataModelBuilderClass != null` is always `true`
in `api/src/main/java/jakarta/faces/component/UIData.java`
#### Snippet
```java
        {
            dataModelBuilderClass = ClassUtils.classForName(FACES_DATA_MODEL_MANAGER_CLASS_NAME);
            if (dataModelBuilderClass != null)
            {
                createDataModelMethod = dataModelBuilderClass.getMethod("createDataModel",
```

### ConstantValue
Condition `skipIterationHint` is always `false`
in `api/src/main/java/jakarta/faces/component/UIData.java`
#### Snippet
```java
                            }

                            if (skipIterationHint)
                            {
                                // If SKIP_ITERATION is enabled, do not take into account rows.
```

### ConstantValue
Condition `value != null` is always `true` when reached
in `api/src/main/java/jakarta/faces/component/UIData.java`
#### Snippet
```java
        {
            DataModel dataModel = null;
            if (FACES_DATA_MODEL_MANAGER_CLASS != null && value != null)
            {
                try
```

### ConstantValue
Condition `value instanceof Collection` is always `false`
in `api/src/main/java/jakarta/faces/component/UIData.java`
#### Snippet
```java
                    return new IterableDataModel<>(((Map<?, ?>) value).entrySet());
                }
                else if (value instanceof Collection)
                {
                    return new CollectionDataModel((Collection) value);
```

### ConstantValue
Condition `elClass != null` is always `true`
in `impl/src/main/java/org/apache/myfaces/el/resolver/ImportHandlerResolver.java`
#### Snippet
```java
            importHandlerClass = ClassUtils.classForName("jakarta.el.ImportHandler");
            getImportHandlerMethod = ELContext.class.getMethod("getImportHandler");
            if (elClass != null && importHandlerClass != null) 
            {
                importHandlerResolveClassMethod = 
```

### ConstantValue
Condition `elClass != null && importHandlerClass != null` is always `true`
in `impl/src/main/java/org/apache/myfaces/el/resolver/ImportHandlerResolver.java`
#### Snippet
```java
            importHandlerClass = ClassUtils.classForName("jakarta.el.ImportHandler");
            getImportHandlerMethod = ELContext.class.getMethod("getImportHandler");
            if (elClass != null && importHandlerClass != null) 
            {
                importHandlerResolveClassMethod = 
```

### ConstantValue
Condition `importHandlerClass != null` is always `true` when reached
in `impl/src/main/java/org/apache/myfaces/el/resolver/ImportHandlerResolver.java`
#### Snippet
```java
            importHandlerClass = ClassUtils.classForName("jakarta.el.ImportHandler");
            getImportHandlerMethod = ELContext.class.getMethod("getImportHandler");
            if (elClass != null && importHandlerClass != null) 
            {
                importHandlerResolveClassMethod = 
```

### ConstantValue
Condition `version.ordinal() >= FacesConfig.Version.JSF_2_3.ordinal()` is always `true`
in `impl/src/main/java/org/apache/myfaces/el/ELResolverBuilder.java`
#### Snippet
```java
                    return false;
                }
                else if (version.ordinal() >= FacesConfig.Version.JSF_2_3.ordinal())
                {
                    return true;
```

### ConstantValue
Condition `base == null` is always `true`
in `impl/src/main/java/org/apache/myfaces/el/resolver/ResourceBundleResolver.java`
#### Snippet
```java
        }

        if ((base == null) && (property == null))
        {
            throw new PropertyNotFoundException();
```

### ConstantValue
Condition `importConstantsMap != null` is always `true`
in `impl/src/main/java/org/apache/myfaces/el/resolver/ImportConstantsELResolver.java`
#### Snippet
```java
        }

        if (importConstantsMap != null && !importConstantsMap.isEmpty())
        {
            String type = importConstantsMap.get((String)property);
```

### ConstantValue
Value `value` is always 'true'
in `impl/src/main/java/org/apache/myfaces/el/resolver/CompositeComponentELResolver.java`
#### Snippet
```java
            if (value)
            {
                return value;
            }
 
```

### ConstantValue
Value `oldValue` is always 'null'
in `impl/src/main/java/org/apache/myfaces/cdi/view/ViewScopeCDIMap.java`
#### Snippet
```java
                getCreationalContextInstances().put(key, info);
            }
            return oldValue;
        }
    }
```

### ConstantValue
Condition `c != null` is always `true`
in `impl/src/main/java/org/apache/myfaces/spi/impl/Tomcat7AnnotationInjectionProvider.java`
#### Snippet
```java
        {
            Class c = ClassUtils.classForName("org.apache.tomcat.InstanceManager");
            if (c != null)
            {
                // Tomcat 7 Available, check CDI integration. If there is no CDI available,
```

### ConstantValue
Value `metadata` is always 'null'
in `impl/src/main/java/org/apache/myfaces/spi/impl/NoInjectionAnnotationInjectionProvider.java`
#### Snippet
```java
            synchronized (declaredMethodBeans)
            {
                metadata = createDeclaredMethodBeansMap(cl, metadata);
            }
        }
```

### ConstantValue
Condition `rd != null` is always `true`
in `impl/src/main/java/org/apache/myfaces/spi/impl/DefaultServiceProviderFinder.java`
#### Snippet
```java
                        finally
                        {
                            if (rd != null)
                            {
                                rd.close();
```

### ConstantValue
Condition `ClassUtils.classForName("jakarta.validation.Validation") != null` is always `true`
in `impl/src/main/java/org/apache/myfaces/util/ExternalSpecifications.java`
#### Snippet
```java
            try
            {
                available = ClassUtils.classForName("jakarta.validation.Validation") != null;
            }
            catch(ClassNotFoundException e)
```

### ConstantValue
Condition `ClassUtils.classForName("jakarta.servlet.http.PushBuilder") != null` is always `true`
in `impl/src/main/java/org/apache/myfaces/util/ExternalSpecifications.java`
#### Snippet
```java
        try
        {
            available = ClassUtils.classForName("jakarta.servlet.http.PushBuilder") != null
                    && HttpServletRequest.class.getMethod("newPushBuilder", (Class[]) null) != null;
        }
```

### ConstantValue
Condition `ClassUtils.classForName("jakarta.servlet.http.PushBuilder") != null && HttpServl...` is always `true`
in `impl/src/main/java/org/apache/myfaces/util/ExternalSpecifications.java`
#### Snippet
```java
        try
        {
            available = ClassUtils.classForName("jakarta.servlet.http.PushBuilder") != null
                    && HttpServletRequest.class.getMethod("newPushBuilder", (Class[]) null) != null;
        }
        catch (Throwable t)
```

### ConstantValue
Condition `HttpServletRequest.class.getMethod("newPushBuilder", (Class[]) null) != null` is always `true` when reached
in `impl/src/main/java/org/apache/myfaces/util/ExternalSpecifications.java`
#### Snippet
```java
        {
            available = ClassUtils.classForName("jakarta.servlet.http.PushBuilder") != null
                    && HttpServletRequest.class.getMethod("newPushBuilder", (Class[]) null) != null;
        }
        catch (Throwable t)
```

### ConstantValue
Condition `ClassUtils.classForName("jakarta.el.StaticFieldELResolver") != null` is always `true`
in `impl/src/main/java/org/apache/myfaces/util/ExternalSpecifications.java`
#### Snippet
```java
        try
        {
            available = ClassUtils.classForName("jakarta.el.StaticFieldELResolver") != null ;
        }
        catch (Throwable t)
```

### ConstantValue
Condition `ClassUtils.classForName("jakarta.enterprise.inject.spi.BeanManager") != null` is always `true`
in `impl/src/main/java/org/apache/myfaces/util/ExternalSpecifications.java`
#### Snippet
```java
        try
        {
            available = ClassUtils.classForName("jakarta.enterprise.inject.spi.BeanManager") != null;
        }
        catch (Throwable t)
```

### ConstantValue
Condition `!outboundCallNodeProcessed` is always `true`
in `impl/src/main/java/org/apache/myfaces/flow/FlowHandlerImpl.java`
#### Snippet
```java
            // Entering a flow
            Map<String, Object> outboundParameters = doBeforeEnterFlow(context, 
                targetFlow, !outboundCallNodeProcessed ? outboundCallNode : null);
            outboundCallNodeProcessed = true;
            pushFlowReference(context, clientWindow, 
```

### ConstantValue
Value `outboundCallNodeProcessed` is always 'false'
in `impl/src/main/java/org/apache/myfaces/flow/FlowHandlerImpl.java`
#### Snippet
```java
            // Entering a flow
            Map<String, Object> outboundParameters = doBeforeEnterFlow(context, 
                targetFlow, !outboundCallNodeProcessed ? outboundCallNode : null);
            outboundCallNodeProcessed = true;
            pushFlowReference(context, clientWindow, 
```

### ConstantValue
Value `parentFlowReference` is always 'null'
in `impl/src/main/java/org/apache/myfaces/flow/FlowHandlerImpl.java`
#### Snippet
```java
            pushFlowReference(context, clientWindow, 
                    new FlowReference(targetFlow.getDefiningDocumentId(), targetFlow.getId()), 
                    toViewId, parentFlowReference);
            doAfterEnterFlow(context, targetFlow, outboundParameters);
        }
```

### ConstantValue
Condition `!outboundCallNodeProcessed` is always `true`
in `impl/src/main/java/org/apache/myfaces/flow/FlowHandlerImpl.java`
#### Snippet
```java
                    // targetFlow is not on the stack, so it is flow call.
                    Map<String, Object> outboundParameters = doBeforeEnterFlow(context,
                        targetFlow, !outboundCallNodeProcessed ? outboundCallNode : null);
                    outboundCallNodeProcessed = true;
                    pushFlowReference(context, clientWindow, 
```

### ConstantValue
Value `outboundCallNodeProcessed` is always 'false'
in `impl/src/main/java/org/apache/myfaces/flow/FlowHandlerImpl.java`
#### Snippet
```java
                    // targetFlow is not on the stack, so it is flow call.
                    Map<String, Object> outboundParameters = doBeforeEnterFlow(context,
                        targetFlow, !outboundCallNodeProcessed ? outboundCallNode : null);
                    outboundCallNodeProcessed = true;
                    pushFlowReference(context, clientWindow, 
```

### ConstantValue
Value `outboundCallNode` is always 'null'
in `impl/src/main/java/org/apache/myfaces/flow/FlowHandlerImpl.java`
#### Snippet
```java
                    // targetFlow is not on the stack, so it is flow call.
                    Map<String, Object> outboundParameters = doBeforeEnterFlow(context,
                        targetFlow, !outboundCallNodeProcessed ? outboundCallNode : null);
                    outboundCallNodeProcessed = true;
                    pushFlowReference(context, clientWindow, 
```

### ConstantValue
Value `parentFlowReference` is always 'null'
in `impl/src/main/java/org/apache/myfaces/flow/FlowHandlerImpl.java`
#### Snippet
```java
                    pushFlowReference(context, clientWindow, 
                            new FlowReference(targetFlow.getDefiningDocumentId(), targetFlow.getId()), toViewId,
                            parentFlowReference);
                    doAfterEnterFlow(context, targetFlow, outboundParameters);
                }
```

### ConstantValue
Condition `!outboundCallNodeProcessed` is always `true`
in `impl/src/main/java/org/apache/myfaces/flow/FlowHandlerImpl.java`
#### Snippet
```java
                // case just enter into targetFlow
                Map<String, Object> outboundParameters = doBeforeEnterFlow(context, 
                    targetFlow, !outboundCallNodeProcessed ? outboundCallNode : null);
                outboundCallNodeProcessed = true;
                pushFlowReference(context, clientWindow, 
```

### ConstantValue
Value `outboundCallNodeProcessed` is always 'false'
in `impl/src/main/java/org/apache/myfaces/flow/FlowHandlerImpl.java`
#### Snippet
```java
                // case just enter into targetFlow
                Map<String, Object> outboundParameters = doBeforeEnterFlow(context, 
                    targetFlow, !outboundCallNodeProcessed ? outboundCallNode : null);
                outboundCallNodeProcessed = true;
                pushFlowReference(context, clientWindow, 
```

### ConstantValue
Value `locale` is always 'null'
in `impl/src/main/java/org/apache/myfaces/util/MessageUtils.java`
#### Snippet
```java
        if(null == locale)
        {
            throw new NullPointerException(" locale " + locale);
        }
        FacesMessage message = getMessageFromBundle(bundleBaseName, context, locale, messageId, params);
```

### ConstantValue
Condition `message != null` is always `true`
in `impl/src/main/java/org/apache/myfaces/util/MessageUtils.java`
#### Snippet
```java
        }
        FacesMessage message = getMessageFromBundle(bundleBaseName, context, locale, messageId, params);
        if(message != null)
        {
            return message;
```

### ConstantValue
Value `locale` is always 'null'
in `impl/src/main/java/org/apache/myfaces/util/MessageUtils.java`
#### Snippet
```java
        if(null == locale)
        {
            throw new NullPointerException(" locale " + locale);
        }
        FacesMessage message = getMessage(locale, messageId, params);
```

### ConstantValue
Condition `message != null` is always `true`
in `impl/src/main/java/org/apache/myfaces/util/MessageUtils.java`
#### Snippet
```java
        }
        FacesMessage message = getMessage(locale, messageId, params);
        if(message != null)
        {
            return message;
```

### ConstantValue
Condition `locale != null` is always `true` when reached
in `impl/src/main/java/org/apache/myfaces/util/MessageUtils.java`
#### Snippet
```java
        
        String detailMessageId = messageId + DETAIL_SUFFIX;
        if (params != null && locale != null)
        {
            try
```

### ConstantValue
Value `params` is always 'null'
in `impl/src/main/java/org/apache/myfaces/util/MessageUtils.java`
#### Snippet
```java
        else
        {
            summary = substituteParams(locale, summary, params);
            try
            {
```

### ConstantValue
Value `params` is always 'null'
in `impl/src/main/java/org/apache/myfaces/util/MessageUtils.java`
#### Snippet
```java
                if (bundle.containsKey(detailMessageId))
                {
                    detail = substituteParams(locale, bundle.getString(detailMessageId), params);
                }
            }
```

### ConstantValue
Condition `locale != null` is always `true` when reached
in `impl/src/main/java/org/apache/myfaces/util/MessageUtils.java`
#### Snippet
```java
        
        String detailMessageId = messageId + DETAIL_SUFFIX;
        if (params != null && locale != null)
        {
            try
```

### ConstantValue
Value `params` is always 'null'
in `impl/src/main/java/org/apache/myfaces/util/MessageUtils.java`
#### Snippet
```java
        else
        {
            summary = substituteParams(locale, summary, params);
            try
            {
```

### ConstantValue
Value `params` is always 'null'
in `impl/src/main/java/org/apache/myfaces/util/MessageUtils.java`
#### Snippet
```java
                if (bundle.containsKey(detailMessageId))
                {
                    detail = substituteParams(locale, bundle.getString(detailMessageId), params);
                }
            }
```

### ConstantValue
Condition `viewRoot != null` is always `true`
in `impl/src/main/java/org/apache/myfaces/view/ViewDeclarationLanguageBase.java`
#### Snippet
```java
                viewRoot = TreeStructureManager.restoreTreeStructure(((Object[]) stateArray[0])[0]);

                if (viewRoot != null)
                {
                    context.setViewRoot(viewRoot);
```

### ConstantValue
Condition `uiViewRoot != null` is always `true`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FullStateManagementStrategy.java`
#### Snippet
```java
            uiViewRoot = TreeStructureManager.restoreTreeStructure(((Object[]) stateArray[0])[0]);

            if (uiViewRoot != null)
            {
                context.setViewRoot(uiViewRoot);
```

### ConstantValue
Condition `csq instanceof StringBuilder` is always `true`
in `impl/src/main/java/org/apache/myfaces/util/lang/StreamCharBuffer.java`
#### Snippet
```java
                                    currentOffset, writeChars);
                        }
                        else if (csq instanceof StringBuilder)
                        {
                            allocBuffer.writeStringBuilder((StringBuilder) csq,
```

### ConstantValue
Condition `child != null` is always `true`
in `impl/src/main/java/org/apache/myfaces/view/facelets/ViewPoolProcessor.java`
#### Snippet
```java
                UIComponent child = component.getChildren().get(i);
                String id = (String) child.getAttributes().get(ComponentSupport.MARK_CREATED);
                if (child != null && child.isTransient() && id == null)
                {
                    //Remove both transient not facelets bound components
```

### ConstantValue
Condition `child != null` is always `true`
in `impl/src/main/java/org/apache/myfaces/view/facelets/ViewPoolProcessor.java`
#### Snippet
```java
                    UIComponent child = root.getChildren().get(i);
                    boolean containsFaceletId = child.getAttributes().containsKey(ComponentSupport.MARK_CREATED);
                    if (child != null && child.isTransient() && !containsFaceletId)
                    {
                        //Transient and not bound to facelets tag, remove it!.
```

### ConstantValue
Condition `this.value == null` is always `false`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/LegacySetHandler.java`
#### Snippet
```java
        {
            // Check attributes
            if (this.target == null || this.property == null || this.value == null)
            {
                throw new TagException(
```

### ConstantValue
Condition `this.value == null` is always `false`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/SetHandler.java`
#### Snippet
```java
        {
            // Check attributes
            if (this.target == null || this.property == null || this.value == null)
            {
                throw new TagException(
```

### ConstantValue
Condition `view != null` is always `true`
in `impl/src/main/java/org/apache/myfaces/view/facelets/PartialStateManagementStrategy.java`
#### Snippet
```java
            view = (UIViewRoot) internalRestoreTreeStructure((TreeStructComponent)fullState[0]);

            if (view != null)
            {
                context.setViewRoot (view);
```

### ConstantValue
Condition `!containsFaceletState` is always `true` when reached
in `impl/src/main/java/org/apache/myfaces/view/facelets/PartialStateManagementStrategy.java`
#### Snippet
```java
                    // Check if there is only one component state
                    // and that state is UIViewRoot instance (for example when using ViewScope)
                    if ((states.size() == 1 && !containsFaceletState)
                            || (states.size() == 2 && containsFaceletState))
                    {
```

### ConstantValue
Value `containsFaceletState` is always 'false'
in `impl/src/main/java/org/apache/myfaces/view/facelets/PartialStateManagementStrategy.java`
#### Snippet
```java
                    // Check if there is only one component state
                    // and that state is UIViewRoot instance (for example when using ViewScope)
                    if ((states.size() == 1 && !containsFaceletState)
                            || (states.size() == 2 && containsFaceletState))
                    {
```

### ConstantValue
Value `v` is always 'null'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/LegacyForEachHandler.java`
#### Snippet
```java
                        else
                        {
                            pctx.getAttributes().remove(v);
                        }
                        if (vs != null)
```

### ConstantValue
Value `vs` is always 'null'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/LegacyForEachHandler.java`
#### Snippet
```java
                        else
                        {
                            pctx.getAttributes().remove(vs);
                        }
                    }
```

### ConstantValue
Condition `FaceletViewDeclarationLanguageBase.isDynamicComponentNeedsRefresh(ctx.getFacesContext())` is always `false`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ComponentTagHandlerDelegate.java`
#### Snippet
```java
                                          ComponentState.REMOVE_ADD);
                    
                    if (FaceletViewDeclarationLanguageBase.isDynamicComponentNeedsRefresh(ctx.getFacesContext()))
                    {
                        FaceletViewDeclarationLanguageBase.resetDynamicComponentNeedsRefreshFlag(
```

### ConstantValue
Condition `parent != null` is always `true`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ComponentTagHandlerDelegate.java`
#### Snippet
```java
            {
                facesContext.setProcessingEvents(false);
                if (_relocatableResourceHandler != null && parent != null && !parent.equals(c.getParent()))
                {
                    // Replace parent with the relocated parent.
```

### ConstantValue
Condition `required != null` is always `true`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguage.java`
#### Snippet
```java
                                }

                                if (required != null && required)
                                {
                                    if (log.isLoggable(Level.SEVERE))
```

### ConstantValue
Condition `FaceletViewDeclarationLanguageBase.isDynamicComponentNeedsRefresh(context)` is always `false`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguage.java`
#### Snippet
```java
                    requiresFaceletDynamicRefresh = true;
                }
                else if (FaceletViewDeclarationLanguageBase.isDynamicComponentNeedsRefresh(context))
                {
                    FaceletViewDeclarationLanguageBase.activateDynamicComponentRefreshTransientBuild(context);
```

### ConstantValue
Condition `requiresDynamicRefresh` is always `false`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguage.java`
#### Snippet
```java
                        RefreshDynamicComponentListener(taglibURI, tagName, attributes, baseKey));
                }
                if (requiresDynamicRefresh)
                {
                    createdComponent.subscribeToEvent(DynamicComponentRefreshTransientBuildEvent.class, new 
```

### ConstantValue
Condition `view != null && FaceletViewDeclarationLanguageBase.isDynamicComponentRefreshTransie...` is always `false`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguage.java`
#### Snippet
```java
        if (isFilledView(context, view))
        {
            if (view != null && 
                FaceletViewDeclarationLanguageBase.isDynamicComponentRefreshTransientBuildActive(context, view))
            {
                // don't return
```

### ConstantValue
Condition `FaceletViewDeclarationLanguageBase.isDynamicComponentRefreshTransientBuildActive(context, view)` is always `false` when reached
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguage.java`
#### Snippet
```java
        {
            if (view != null && 
                FaceletViewDeclarationLanguageBase.isDynamicComponentRefreshTransientBuildActive(context, view))
            {
                // don't return
```

### ConstantValue
Condition `eventClass == PreRenderViewEvent.class` is always `true`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/EventHandler.java`
#### Snippet
```java
                parentCompositeComponent.subscribeToEvent(PostAddToViewEvent.class, 
                        new SubscribeEventListener(eventClass, methodExpOneArg, methodExpZeroArg,
                                                   (eventClass == PreRenderViewEvent.class) ? null : parent));
            }
            else
```

### ConstantValue
Condition `required != null` is always `true`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeComponentResourceTagHandler.java`
#### Snippet
```java
                    } 
                    
                    if (required != null && required)
                    {
                        Object attrValue = this.tag.getAttributes().get (propertyDescriptor.getName());
```

### ConstantValue
Condition `FaceletViewDeclarationLanguageBase.isDynamicComponentNeedsRefresh(facesContext)` is always `false`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CreateDynamicCompositeComponentListener.java`
#### Snippet
```java
                }
                
                if (FaceletViewDeclarationLanguageBase.isDynamicComponentNeedsRefresh(facesContext))
                {
                    FaceletCompositionContext fcc = FaceletCompositionContext.getCurrentInstance(facesContext);
```

### ConstantValue
Value `v` is always 'null'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/ForEachHandler.java`
#### Snippet
```java
        else
        {
            pctx.getAttributes().remove(v);
        }
        if (vs != null)
```

### ConstantValue
Value `vs` is always 'null'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/ForEachHandler.java`
#### Snippet
```java
        else
        {
            pctx.getAttributes().remove(vs);
        }
    }
```

### ConstantValue
Condition `url != null` is always `true`
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFaceletFactory.java`
#### Snippet
```java
        {
            url = resolveURL(facesContext, getBaseUrl(), uri);
            if (url != null)
            {
                ViewResource viewResource = (ViewResource) facesContext.getAttributes().get(
```

### ConstantValue
Condition `url != null` is always `true`
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFaceletFactory.java`
#### Snippet
```java
            ViewResource viewResource = (ViewResource) facesContext.getAttributes().get(
                FaceletFactory.LAST_RESOURCE_RESOLVED);            
            if (url != null)
            {
                if (viewResource != null)
```

### ConstantValue
Condition `url != null` is always `true`
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFaceletFactory.java`
#### Snippet
```java
            ViewResource viewResource = (ViewResource) facesContext.getAttributes().get(
                FaceletFactory.LAST_RESOURCE_RESOLVED);
            if (url != null)
            {
                if (viewResource != null)
```

### ConstantValue
Condition `url != null` is always `true`
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFacelet.java`
#### Snippet
```java
        {
            url = _factory.resolveURL(facesContext, _src, path);
            if (url != null)
            {
                ViewResource viewResource = (ViewResource) facesContext.getAttributes().get(
```

### ConstantValue
Condition `id == null` is always `true`
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/FaceletCompositionContextImpl.java`
#### Snippet
```java
            removeComponentForDeletion(id);
        }
        else if (id == null
                 && Boolean.TRUE.equals(component.getAttributes().get(ComponentSupport.FACET_CREATED_UIPANEL_MARKER)))
        {
```

### ConstantValue
Condition `compositeComponentResource != null` is always `true`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TagLibraryConfig.java`
#### Snippet
```java
                            _resourceHandler.createResource(resourceName, _compositeLibraryName));
                        
                        if (compositeComponentResource != null)
                        {
                            ComponentConfig componentConfig = new ComponentConfigWrapper(tag,
```

### ConstantValue
Condition `instructionType == 2` is always `true`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TextUnit.java`
#### Snippet
```java
                        instructionBuffer.set(i, new LiteralTextInstruction(newText));
                    }
                    else if (instructionType == 2)
                    {
                        instructionBuffer.set(i, new LiteralNonExcapedTextInstruction(newText));
```

### ConstantValue
Condition `s != null` is always `true`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TextUnit.java`
#### Snippet
```java
                    // First check if the text contains EL before build something, and if contains 
                    // an EL expression, compress it before build the ELText.
                    if (s != null && s.length() > 0)
                    {
                        if (ELText.isLiteral(s))
```

### ConstantValue
Condition `s != null && s.length() > 0` is always `true`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TextUnit.java`
#### Snippet
```java
                    // First check if the text contains EL before build something, and if contains 
                    // an EL expression, compress it before build the ELText.
                    if (s != null && s.length() > 0)
                    {
                        if (ELText.isLiteral(s))
```

### ConstantValue
Condition `s.length() > 0` is always `true` when reached
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TextUnit.java`
#### Snippet
```java
                    // First check if the text contains EL before build something, and if contains 
                    // an EL expression, compress it before build the ELText.
                    if (s != null && s.length() > 0)
                    {
                        if (ELText.isLiteral(s))
```

### ConstantValue
Value `filePath` is always 'null'
in `impl/src/main/java/org/apache/myfaces/config/FacesConfigValidator.java`
#### Snippet
```java
            if(fromId != null && ! "*".equals(fromId) && filePath == null)
            {
                list.add("File for navigation 'from id' does not exist " + filePath);
            }            
        }
```

### ConstantValue
Value `toViewPath` is always 'null'
in `impl/src/main/java/org/apache/myfaces/config/FacesConfigValidator.java`
#### Snippet
```java
                if(toViewPath == null)
                {
                    list.add("File for navigation 'to id' does not exist " + toViewPath);
                }
            }
```

### ConstantValue
Condition `consumingCDATA` is always `true` when reached
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/SAXCompiler.java`
#### Snippet
```java
        public void characters(char[] ch, int start, int length) throws SAXException
        {
            if (this.inDocument && (!consumingCDATA || (consumingCDATA && !swallowCDATAContent)))
            {
                this.unit.writeText(new String(ch, start, length), createLocation());
```

### ConstantValue
Condition `consumingCDATA` is always `true` when reached
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/SAXCompiler.java`
#### Snippet
```java
        public void characters(char[] ch, int start, int length) throws SAXException
        {
            if (this.inDocument && inMetadata && (!consumingCDATA || (consumingCDATA && !swallowCDATAContent)))
            {
                this.unit.writeText(new String(ch, start, length), createLocation());
```

### ConstantValue
Condition `consumingCDATA` is always `true` when reached
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/SAXCompiler.java`
#### Snippet
```java
        {
            if (this.inDocument && inCompositeInterface && 
                    (!consumingCDATA || (consumingCDATA && !swallowCDATAContent)))
            {
                this.unit.writeText(new String(ch, start, length), createLocation());
```

### ConstantValue
Condition `value instanceof Collection` is always `false`
in `impl/src/main/java/org/apache/myfaces/view/facelets/component/UIRepeat.java`
#### Snippet
```java
                    return new IterableDataModel<>(((Map<?, ?>) value).entrySet());
                }
                else if (value instanceof Collection)
                {
                    return new CollectionDataModel((Collection) value);
```

### ConstantValue
Condition `step > 0 && (end < begin)` is always `false`
in `impl/src/main/java/org/apache/myfaces/view/facelets/component/UIRepeat.java`
#### Snippet
```java
        if (_emptyModel)
        {
            if (step > 0 && (end < begin))
            {
                throw new LocationAwareFacesException("on empty models, end cannot be less than begin " +
```

### ConstantValue
Condition `end < begin` is always `false` when reached
in `impl/src/main/java/org/apache/myfaces/view/facelets/component/UIRepeat.java`
#### Snippet
```java
        if (_emptyModel)
        {
            if (step > 0 && (end < begin))
            {
                throw new LocationAwareFacesException("on empty models, end cannot be less than begin " +
```

### ConstantValue
Value `returnValue` is always 'true'
in `impl/src/main/java/org/apache/myfaces/view/facelets/component/UIRepeat.java`
#### Snippet
```java
            if (returnValue)
            {
                return returnValue;
            }
            
```

### ConstantValue
Condition `!isEnableDefaultWindowMode(facesContext)` is always `true`
in `impl/src/main/java/org/apache/myfaces/config/FacesConfigurator.java`
#### Snippet
```java
    public static void enableDefaultWindowMode(FacesContext facesContext)
    {
        if (!isEnableDefaultWindowMode(facesContext))
        {
            String windowMode = WebConfigParamUtils.getStringInitParameter(
```

### ConstantValue
Condition `node.getChildNodes() != null` is always `true`
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
    protected String firstChildTextContent(String name, Node node)
    {
        if (node.getChildNodes() != null)
        {
            for (int i = 0; i < node.getChildNodes().getLength(); i++)
```

### ConstantValue
Condition `node.getChildNodes() != null` is always `true`
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
    protected void onChild(String name, Node node, Consumer<Node> val)
    {
        if (node.getChildNodes() != null)
        {
            for (int i = 0; i < node.getChildNodes().getLength(); i++)
```

### ConstantValue
Condition `node.getChildNodes() != null` is always `true`
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
    protected void forEachChild(Node node, Consumer<Node> val)
    {
        if (node.getChildNodes() != null)
        {
            for (int i = 0; i < node.getChildNodes().getLength(); i++)
```

### ConstantValue
Condition `firstExternalContextInstance == null` is always `true`
in `impl/src/main/java/org/apache/myfaces/context/FacesContextFactoryImpl.java`
#### Snippet
```java
            externalContextFirstInstance.setAccessible(true);

            if (firstExternalContextInstance == null)
            {
                firstExternalContextInstance = 
```

### ConstantValue
Condition `cfg.numberOfSequentialViewsInSession == null` is always `false`
in `impl/src/main/java/org/apache/myfaces/config/webparameters/MyfacesConfig.java`
#### Snippet
```java
            cfg.numberOfSequentialViewsInSession = getInt(extCtx, NUMBER_OF_SEQUENTIAL_VIEWS_IN_SESSION,
                    NUMBER_OF_SEQUENTIAL_VIEWS_IN_SESSION_DEFAULT);
            if (cfg.numberOfSequentialViewsInSession == null
                    || cfg.numberOfSequentialViewsInSession < 0)
            {
```

### ConstantValue
Condition `cfg.numberOfViewsInSession == null` is always `false`
in `impl/src/main/java/org/apache/myfaces/config/webparameters/MyfacesConfig.java`
#### Snippet
```java
            cfg.numberOfViewsInSession = getInt(extCtx, NUMBER_OF_VIEWS_IN_SESSION,
                    NUMBER_OF_VIEWS_IN_SESSION_DEFAULT);
            if (cfg.numberOfViewsInSession == null || cfg.numberOfViewsInSession <= 0)
            {
                Logger.getLogger(MyfacesConfig.class.getName()).severe(
```

### ConstantValue
Condition `c >=0x10000` is always `false`
in `impl/src/main/java/org/apache/myfaces/resource/ResourceValidationUtils.java`
#### Snippet
```java
                 (c >=0x2070 && c <=0x218F) || (c >=0x2C00 && c <=0x2FEF) || 
                 (c >=0x3001 && c <=0xD7FF) || (c >=0xF900 && c <=0xFDCF) ||
                 (c >=0xFDF0 && c <=0xFFFD) || (c >=0x10000 && c <=0xEFFFF) ||
                 (c == '-') || (c >='0' && c <='9') || c == 0xB7 || (c >=0x300 && c <=0x36F) || 
                 (c >=0x203F && c <=0x2040) || (allowSlash && c == '/') ||
```

### ConstantValue
Condition `c >=0x10000 && c <=0xEFFFF` is always `false`
in `impl/src/main/java/org/apache/myfaces/resource/ResourceValidationUtils.java`
#### Snippet
```java
                 (c >=0x2070 && c <=0x218F) || (c >=0x2C00 && c <=0x2FEF) || 
                 (c >=0x3001 && c <=0xD7FF) || (c >=0xF900 && c <=0xFDCF) ||
                 (c >=0xFDF0 && c <=0xFFFD) || (c >=0x10000 && c <=0xEFFFF) ||
                 (c == '-') || (c >='0' && c <='9') || c == 0xB7 || (c >=0x300 && c <=0x36F) || 
                 (c >=0x203F && c <=0x2040) || (allowSlash && c == '/') ||
```

### ConstantValue
Condition `c >=0x10000` is always `false`
in `impl/src/main/java/org/apache/myfaces/resource/ResourceValidationUtils.java`
#### Snippet
```java
                 (c >=0x2070 && c <=0x218F) || (c >=0x2C00 && c <=0x2FEF) || 
                 (c >=0x3001 && c <=0xD7FF) || (c >=0xF900 && c <=0xFDCF) ||
                 (c >=0xFDF0 && c <=0xFFFD) || (c >=0x10000 && c <=0xEFFFF) ||
                 c == '-' || (c >='0' && c <='9') || c == 0xB7 || (c >=0x300 && c <=0x36F) || 
                 (c >=0x203F && c <=0x2040) || (allowSlash && c == '/')
```

### ConstantValue
Condition `c >=0x10000 && c <=0xEFFFF` is always `false`
in `impl/src/main/java/org/apache/myfaces/resource/ResourceValidationUtils.java`
#### Snippet
```java
                 (c >=0x2070 && c <=0x218F) || (c >=0x2C00 && c <=0x2FEF) || 
                 (c >=0x3001 && c <=0xD7FF) || (c >=0xF900 && c <=0xFDCF) ||
                 (c >=0xFDF0 && c <=0xFFFD) || (c >=0x10000 && c <=0xEFFFF) ||
                 c == '-' || (c >='0' && c <='9') || c == 0xB7 || (c >=0x300 && c <=0x36F) || 
                 (c >=0x203F && c <=0x2040) || (allowSlash && c == '/')
```

### ConstantValue
Value `exists` is always 'false'
in `impl/src/main/java/org/apache/myfaces/resource/ResourceLoader.java`
#### Snippet
```java
                if (!exists)
                {
                    resourceExistsCache.put(resourceMeta, exists);
                }
            }
```

### ConstantValue
Value `newViewId` is always 'null'
in `impl/src/main/java/org/apache/myfaces/lifecycle/RenderResponseExecutor.java`
#### Snippet
```java
                newViewId = root.getViewId();
                
                isNotSameRoot = !( (newViewId == null ? newViewId == viewId : newViewId.equals(viewId) ) && 
                        previousRoot.equals(root) ); 
                
```

### ConstantValue
Condition `FacesConfigurator.isEnableDefaultWindowMode(context)` is always `false`
in `impl/src/main/java/org/apache/myfaces/lifecycle/clientwindow/ClientWindowFactoryImpl.java`
#### Snippet
```java
        if (windowMode == null)
        {
            if (FacesConfigurator.isEnableDefaultWindowMode(context))
            {
                String defaultWindowMode = WebConfigParamUtils.getStringInitParameter(
```

### ConstantValue
Condition `copyConstructor != null` is always `true`
in `impl/src/main/java/org/apache/myfaces/component/validate/WholeBeanValidator.java`
#### Snippet
```java
            {
                Constructor<?> copyConstructor = clazz.getConstructor(clazz);
                if (copyConstructor != null)
                {
                    copy = copyConstructor.newInstance(base);
```

### ConstantValue
Condition `viewRoot != null` is always `true`
in `impl/src/main/java/org/apache/myfaces/lifecycle/RestoreViewExecutor.java`
#### Snippet
```java
                    facesContext.renderResponse();
                }
                else if (viewRoot != null && !ViewMetadata.hasMetadata(viewRoot))
                {
                    facesContext.renderResponse();
```

### ConstantValue
Condition `valid` is always `true`
in `impl/src/main/java/org/apache/myfaces/lifecycle/RestoreViewExecutor.java`
#### Snippet
```java
                        if (referer != null)
                        {
                            valid = valid && checkRefererOrOriginHeader(facesContext, viewHandler, referer);
                        }
                        String origin = facesContext.getExternalContext().getRequestHeaderMap().get("Origin");
```

### ConstantValue
Condition `valid` is always `true`
in `impl/src/main/java/org/apache/myfaces/lifecycle/RestoreViewExecutor.java`
#### Snippet
```java
                        if (valid && origin != null)
                        {
                            valid = valid && checkRefererOrOriginHeader(facesContext, viewHandler, origin);
                        }
                    }
```

### ConstantValue
Condition `target == null` is always `true`
in `impl/src/main/java/org/apache/myfaces/component/search/SearchExpressionHandlerImpl.java`
#### Snippet
```java
            // At this point if the algorithm hasn't returned and the topExpression does not have any separator char
            // we need to do the search backward using findComponent.
            if (target == null 
                    && searchExpressionContext.getSource() == previous 
                    && (topExpression.indexOf(separatorChar) == -1) )
```

### ConstantValue
Value `libraryName` is always 'null'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlScriptRenderer.java`
#### Snippet
```java
        if (libraryName == null)
        {
            if (ResourceUtils.isRenderedScript(facesContext, libraryName, resourceName))
            {
                //Resource already founded
```

### ConstantValue
Condition `validParams != null` is always `true`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlFormatRenderer.java`
#### Snippet
```java
            List<UIParameter> validParams = HtmlRendererUtils.getValidUIParameterChildren(
                    facesContext, htmlOutputFormat.getChildren(), false, false, false);
            if (validParams != null && !validParams.isEmpty())
            {
                List<Object> argsList = new ArrayList<>(validParams.size());
```

### ConstantValue
Value `component` is always 'null'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlFormatRenderer.java`
#### Snippet
```java
        else
        {
            escape = AttributeUtils.getBooleanAttribute(component, ComponentAttrs.ESCAPE_ATTR, true);
        }
        if (text != null)
```

### ConstantValue
Condition `text != null` is always `true`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlFormatRenderer.java`
#### Snippet
```java
            escape = AttributeUtils.getBooleanAttribute(component, ComponentAttrs.ESCAPE_ATTR, true);
        }
        if (text != null)
        {
            ResponseWriter writer = facesContext.getResponseWriter();
```

### ConstantValue
Value `libraryName` is always 'null'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlStylesheetRenderer.java`
#### Snippet
```java
        if (libraryName == null)
        {
            if (ResourceUtils.isRenderedStylesheet(facesContext, libraryName, resourceName))
            {
                //Resource already founded
```

### ConstantValue
Condition `uiComponent instanceof HtmlOutputLabel` is always `true`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlLabelRenderer.java`
#### Snippet
```java
            {
                boolean escape;
                if (uiComponent instanceof HtmlOutputLabel)
                {
                    escape = ((HtmlOutputLabel) uiComponent).isEscape();
```

### ConstantValue
Value `component` is always 'null'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTextRendererBase.java`
#### Snippet
```java
        }

        return AttributeUtils.getBooleanAttribute(component, HTML.DISABLED_ATTR, false);
    }

```

### ConstantValue
Condition `component instanceof HtmlOutputText` is always `true`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTextRendererBase.java`
#### Snippet
```java
        
        boolean escape;
        if (component instanceof HtmlOutputText)
        {
            escape = ((HtmlOutputText) component).isEscape();
```

### ConstantValue
Condition `!(title == null && tooltip)` is always `true` when reached
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessageRendererBase.java`
#### Snippet
```java
        }

        if (showSummary && !(title == null && tooltip))
        {
            writer.writeText(summary, null);
```

### ConstantValue
Condition `title == null && tooltip` is always `false` when reached
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessageRendererBase.java`
#### Snippet
```java
        }

        if (showSummary && !(title == null && tooltip))
        {
            writer.writeText(summary, null);
```

### ConstantValue
Condition `tooltip` is always `false` when reached
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessageRendererBase.java`
#### Snippet
```java
        }

        if (showSummary && !(title == null && tooltip))
        {
            writer.writeText(summary, null);
```

### ConstantValue
Value `component` is always 'null'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessageRendererBase.java`
#### Snippet
```java
        }

        return AttributeUtils.getBooleanAttribute(component, ComponentAttrs.TOOLTIP_ATTR, false);
    }

```

### ConstantValue
Value `component` is always 'null'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessageRendererBase.java`
#### Snippet
```java
        }

        return AttributeUtils.getBooleanAttribute(component, ComponentAttrs.REDISPLAY_ATTR, true);
    }

```

### ConstantValue
Value `component` is always 'null'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessageRendererBase.java`
#### Snippet
```java
        }

        return AttributeUtils.getBooleanAttribute(component, ComponentAttrs.SHOW_SUMMARY_ATTR, false);
    }

```

### ConstantValue
Value `component` is always 'null'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessageRendererBase.java`
#### Snippet
```java
        }

        return AttributeUtils.getBooleanAttribute(component, ComponentAttrs.SHOW_DETAIL_ATTR, false);
    }
    
```

### ConstantValue
Condition `component instanceof HtmlPanelGrid` is always `true`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlGridRendererBase.java`
#### Snippet
```java
        writer.startElement(header ? HTML.TH_ELEM : HTML.TD_ELEM, null); // component);

        String styleClass = (component instanceof HtmlPanelGrid)
            ? (header
                ? ((HtmlPanelGrid)component).getHeaderClass()
```

### ConstantValue
Condition `component instanceof HtmlSelectManyCheckbox` is always `false`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlCheckboxRendererBase.java`
#### Snippet
```java
            return ((HtmlSelectBooleanCheckbox) component).isDisabled();
        }
        else if (component instanceof HtmlSelectManyCheckbox)
        {
            return ((HtmlSelectManyCheckbox) component).isDisabled();
```

### ConstantValue
Value `component` is always 'null'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlCheckboxRendererBase.java`
#### Snippet
```java
            return ((HtmlSelectBooleanCheckbox) component).isDisabled();
        }
        else if (component instanceof HtmlSelectManyCheckbox)
        {
            return ((HtmlSelectManyCheckbox) component).isDisabled();
```

### ConstantValue
Value `component` is always 'null'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlCheckboxRendererBase.java`
#### Snippet
```java
        }

        return AttributeUtils.getBooleanAttribute(component, HTML.DISABLED_ATTR, false);
    }

```

### ConstantValue
Condition `uiComponent instanceof HtmlSelectOneMenu` is always `false`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMenuRendererBase.java`
#### Snippet
```java
            return ((HtmlSelectManyMenu) uiComponent).isDisabled();
        }
        else if (uiComponent instanceof HtmlSelectOneMenu)
        {
            return ((HtmlSelectOneMenu) uiComponent).isDisabled();
```

### ConstantValue
Value `uiComponent` is always 'null'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMenuRendererBase.java`
#### Snippet
```java
            return ((HtmlSelectManyMenu) uiComponent).isDisabled();
        }
        else if (uiComponent instanceof HtmlSelectOneMenu)
        {
            return ((HtmlSelectOneMenu) uiComponent).isDisabled();
```

### ConstantValue
Value `uiComponent` is always 'null'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMenuRendererBase.java`
#### Snippet
```java
        }

        return AttributeUtils.getBooleanAttribute(uiComponent, HTML.DISABLED_ATTR, false);
    }

```

### ConstantValue
Condition `uiComponent instanceof HtmlSelectOneListbox` is always `true`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlListboxRendererBase.java`
#### Snippet
```java
                                            size, getConverter(facesContext, uiComponent));
        }
        else if (uiComponent instanceof HtmlSelectOneListbox)
        {
            renderListbox(facesContext,
```

### ConstantValue
Condition `uiComponent instanceof HtmlSelectOneListbox` is always `false`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlListboxRendererBase.java`
#### Snippet
```java
            return ((HtmlSelectManyListbox)uiComponent).isDisabled();
        }
        else if (uiComponent instanceof HtmlSelectOneListbox)
        {
            return ((HtmlSelectOneListbox)uiComponent).isDisabled();
```

### ConstantValue
Value `uiComponent` is always 'null'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlListboxRendererBase.java`
#### Snippet
```java
            return ((HtmlSelectManyListbox)uiComponent).isDisabled();
        }
        else if (uiComponent instanceof HtmlSelectOneListbox)
        {
            return ((HtmlSelectOneListbox)uiComponent).isDisabled();
```

### ConstantValue
Value `uiComponent` is always 'null'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlListboxRendererBase.java`
#### Snippet
```java
        }

        return AttributeUtils.getBooleanAttribute(uiComponent, HTML.DISABLED_ATTR, false);
    }

```

### ConstantValue
Condition `uiComponent instanceof HtmlInputSecret` is always `true`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlSecretRendererBase.java`
#### Snippet
```java

        boolean isRedisplay;
        if (uiComponent instanceof HtmlInputSecret)
        {
            isRedisplay = ((HtmlInputSecret)uiComponent).isRedisplay();
```

### ConstantValue
Condition `uiComponent instanceof ClientBehaviorHolder` is always `true`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlSecretRendererBase.java`
#### Snippet
```java
        }

        if (uiComponent instanceof ClientBehaviorHolder)
        {
            Map<String, List<ClientBehavior>> behaviors = ((ClientBehaviorHolder) uiComponent).getClientBehaviors();
```

### ConstantValue
Value `uiComponent` is always 'null'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlSecretRendererBase.java`
#### Snippet
```java
        }

        return AttributeUtils.getBooleanAttribute(uiComponent, HTML.DISABLED_ATTR, false);
    }

```

### ConstantValue
Value `clientId` is always 'null'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessagesRendererBase.java`
#### Snippet
```java
                log.severe("Could not render Message. Unable to find component '" 
                        + forAttr + "' (calling findComponent on component '" 
                        + clientId
                        + "'). If the provided id was correct, wrap the message and its " 
                        + "component into an h:panelGroup or h:panelGrid.");
```

### ConstantValue
Value `component` is always 'null'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessagesRendererBase.java`
#### Snippet
```java
        }

        return AttributeUtils.getBooleanAttribute(component, ComponentAttrs.REDISPLAY_ATTR, true);
        
    }
```

### ConstantValue
Value `component` is always 'null'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessagesRendererBase.java`
#### Snippet
```java
        else
        {
            return AttributeUtils.getBooleanAttribute(component, ComponentAttrs.TOOLTIP_ATTR, false);
        }
    }
```

### ConstantValue
Value `component` is always 'null'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessagesRendererBase.java`
#### Snippet
```java
        else
        {
            return AttributeUtils.getBooleanAttribute(component, ComponentAttrs.SHOW_SUMMARY_ATTR, false);
        }
    }
```

### ConstantValue
Value `component` is always 'null'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessagesRendererBase.java`
#### Snippet
```java
        else
        {
            return AttributeUtils.getBooleanAttribute(component, ComponentAttrs.GLOBAL_ONLY_ATTR, false);
        }
    }
```

### ConstantValue
Value `component` is always 'null'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessagesRendererBase.java`
#### Snippet
```java
        else
        {
            return AttributeUtils.getBooleanAttribute(component, ComponentAttrs.SHOW_DETAIL_ATTR, false);
        }
    }
```

### ConstantValue
Condition `uiComponent instanceof ClientBehaviorHolder` is always `true`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlOutcomeTargetButtonRendererBase.java`
#### Snippet
```java
            long commonPropertiesMarked = CommonHtmlAttributesUtil.getMarkedAttributes(uiComponent);
            
            if (behaviors != null && !behaviors.isEmpty() && uiComponent instanceof ClientBehaviorHolder)
            {
                HtmlRendererUtils.renderBehaviorizedEventHandlersWithoutOnclick(
```

### ConstantValue
Condition `form instanceof HtmlForm` is always `true` when reached
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlInputFileRendererBase.java`
#### Snippet
```java
        {
            UIForm form = ComponentUtils.findClosest(UIForm.class, component);
            if (form != null && form instanceof HtmlForm)
            {
                String content = ((HtmlForm) form).getEnctype();
```

### ConstantValue
Value `uiComponent` is always 'null'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTextareaRendererBase.java`
#### Snippet
```java
        }

        return AttributeUtils.getBooleanAttribute(uiComponent, HTML.DISABLED_ATTR, false);
    }

```

### ConstantValue
Condition `selectItemInfo != null` is always `true`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/SelectItemsUtils.java`
#### Snippet
```java
                Object oldRequestMapVarValue = null;
                String var = null;
                if (selectItemInfo != null && selectItemInfo.getComponent() instanceof UISelectItems)
                {
                    var = (String) selectItemInfo.getComponent().getAttributes().get(ComponentAttrs.VAR_ATTR);
```

### ConstantValue
Condition `component instanceof HtmlCommandLink` is always `true`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlLinkRendererBase.java`
#### Snippet
```java
                    // do not render the script using faces.util.chain, but for now it is ok.
                    String commandOnclick;
                    if (component instanceof HtmlCommandLink)
                    {
                        commandOnclick = ((HtmlCommandLink)component).getOnclick();
```

### ConstantValue
Condition `output instanceof HtmlOutputLink` is always `true`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlLinkRendererBase.java`
#### Snippet
```java
            // check for the fragement attribute
            String fragmentAttr = null;
            if (output instanceof HtmlOutputLink)
            {
                fragmentAttr = ((HtmlOutputLink) output).getFragment();
```

### ConstantValue
Condition `output instanceof ClientBehaviorHolder` is always `true`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlLinkRendererBase.java`
#### Snippet
```java
            writer.startElement(HTML.ANCHOR_ELEM, output);
            writer.writeURIAttribute(HTML.HREF_ATTR, href, null);
            if (output instanceof ClientBehaviorHolder)
            {
                behaviors = ((ClientBehaviorHolder) output).getClientBehaviors();
```

### ConstantValue
Condition `selectItemList == null` is always `false`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlRadioRendererBase.java`
#### Snippet
```java
                        List<SelectItem> selectItemList = RendererUtils.getSelectItemList(first,
                                context.getFacesContext());
                        if (selectItemList == null || selectItemList.isEmpty())
                        {
                            throw new FacesException("UISelectOne with id=\"" + first.getId()
```

### ConstantValue
Condition `selectItemList != null` is always `true`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlRadioRendererBase.java`
#### Snippet
```java

            List selectItemList = RendererUtils.getSelectItemList(selectOne, facesContext);
            if (selectItemList != null && !selectItemList.isEmpty())
            {
                Converter converter = HtmlRendererUtils.findUIOutputConverterFailSafe(facesContext, selectOne);
```

### ConstantValue
Condition `uiComponent instanceof HtmlSelectOneRadio` is always `true`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlRadioRendererBase.java`
#### Snippet
```java
        writer.writeAttribute(HTML.TYPE_ATTR, HTML.INPUT_TYPE_RADIO, null);
        
        String group = uiComponent instanceof HtmlSelectOneRadio ? ((HtmlSelectOneRadio) uiComponent).getGroup() : null;
        if (group != null && !group.isEmpty())
        {
```

### ConstantValue
Condition `uiComponent instanceof ClientBehaviorHolder` is always `true`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlRadioRendererBase.java`
#### Snippet
```java
        
        Map<String, List<ClientBehavior>> behaviors = null;
        if (uiComponent instanceof ClientBehaviorHolder)
        {
            behaviors = ((ClientBehaviorHolder) uiComponent).getClientBehaviors();
```

### ConstantValue
Value `uiComponent` is always 'null'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlRadioRendererBase.java`
#### Snippet
```java
        }

        return AttributeUtils.getBooleanAttribute(uiComponent, HTML.DISABLED_ATTR, false);
    }

```

### ConstantValue
Value `itemStrValue` is always 'null'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlRadioRendererBase.java`
#### Snippet
```java
                    facesContext, selectOne, converter, selectItem.getValue());
            boolean itemChecked = (itemStrValue == null) ? 
                    itemStrValue == currentValue : 
                    "".equals(itemStrValue) ? 
                            (currentValue == null || itemStrValue.equals(currentValue)) : 
```

### ConstantValue
Condition `uiComponent instanceof HtmlColumn` is always `true`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTableRendererBase.java`
#### Snippet
```java
        {
            // allow column to override style class, new in Faces 1.2
            if (uiComponent instanceof HtmlColumn)
            {
                HtmlColumn column = (HtmlColumn)uiComponent;
```

### ConstantValue
Condition `component instanceof HtmlColumn` is always `true`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTableRendererBase.java`
#### Snippet
```java
        }
        String styleClass = null;
        if (component instanceof HtmlColumn) 
        {
            styleClass = ((HtmlColumn) component).getStyleClass();
```

### ConstantValue
Condition `validParams != null` is always `true` when reached
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlButtonRendererBase.java`
#### Snippet
```java
                facesContext, getChildren(uiComponent), false, false);

        if (form != null && (validParams != null && !validParams.isEmpty()))
        {
            HtmlRendererUtils.renderFormSubmitScript(facesContext);
```

### ConstantValue
Condition `validParams != null` is always `true` when reached
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlButtonRendererBase.java`
#### Snippet
```java
        String commandOnclick = (String)uiComponent.getAttributes().get(HTML.ONCLICK_ATTR);
        
        if (commandOnclick != null && (validParams != null && !validParams.isEmpty()))
        {
            ResourceUtils.renderDefaultJsfJsInlineIfNecessary(facesContext, writer);
```

### ConstantValue
Value `uiComponent` is always 'null'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlButtonRendererBase.java`
#### Snippet
```java
        }
        return AttributeUtils.getBooleanAttribute(
                uiComponent, HTML.READONLY_ATTR, false);
    }

```

### ConstantValue
Value `uiComponent` is always 'null'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlButtonRendererBase.java`
#### Snippet
```java
        }

        return AttributeUtils.getBooleanAttribute(uiComponent, HTML.DISABLED_ATTR, false);
    }

```

### ConstantValue
Condition `endLoop` is always `false`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/HTMLEncoder.java`
#### Snippet
```java
                }
            }
            if (endLoop)
            {
                break;
```

### ConstantValue
Condition `app != null` is always `false`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/HTMLEncoder.java`
#### Snippet
```java
            }
                        
            if (app != null)
            {
                if (start < i)
```

### ConstantValue
Value `event` is always 'null'
in `impl/src/main/java/org/apache/myfaces/application/ApplicationImplEventManager.java`
#### Snippet
```java
                List<SystemEventListener> listeners =
                        ((SystemEventListenerHolder) source).getListenersForEventClass(systemEventClass);
                event = processComponentAttachedListeners(facesContext, listeners, systemEventClass, source, event);
            }

```

### ConstantValue
Condition `resourceExists == null` is always `false`
in `impl/src/main/java/org/apache/myfaces/application/ViewIdSupport.java`
#### Snippet
```java
                    resourceExists = vdl.viewExists(facesContext, viewId);
                    // Check if it might be a programmatic java-built view (MYFACES-4477)
                    if(resourceExists == null || resourceExists == false)
                    {
                        if (ExternalSpecifications.isCDIAvailable(facesContext.getExternalContext()))
```

### ConstantValue
Value `writeChildren` is always 'true'
in `impl/src/main/java/org/apache/myfaces/renderkit/ErrorPageWriter.java`
#### Snippet
```java
                    UIComponent child = c.getChildren().get(i);
                    writer.write("<dd>");
                    writeComponent(faces, writer, child, highlightId, writeChildren);
                    writer.write("</dd>");
                }
```

### ConstantValue
Condition `config!=null` is always `true`
in `impl/src/main/java/org/apache/myfaces/renderkit/ErrorPageWriter.java`
#### Snippet
```java
        }
        MyfacesConfig config = MyfacesConfig.getCurrentInstance(faces);
        if(config!=null && !config.isFlashScopeDisabled() && ctx.getFlash() != null)
        {
            writeVariables(writer, ctx.getFlash(), "Flash Attributes");
```

### ConstantValue
Condition `servletRegistrations != null` is always `true`
in `impl/src/main/java/org/apache/myfaces/application/FacesServletMappingUtils.java`
#### Snippet
```java
            List<ServletRegistrationInfo> servletRegistrations =
                    getServletRegistrations(facesContext, servletContext);
            if (servletRegistrations  != null)
            {
                FacesServletMapping facesExtensionMapping = null;
```

### ConstantValue
Value `pathInfo` is always 'null'
in `impl/src/main/java/org/apache/myfaces/application/FacesServletMappingUtils.java`
#### Snippet
```java
                        // we assume that it's an exact match using prefix-based mapping.
                        return createMappingFromServletRegistration(facesContext, 
                                (ServletContext)context, servletPath, pathInfo, allowExactMapping);
                    }
                }
```

### ConstantValue
Condition `mappings == null` is always `false`
in `impl/src/main/java/org/apache/myfaces/application/FacesServletMappingUtils.java`
#### Snippet
```java
            Collection<String> mappingsCollection = registration.getMappings();
            mappings = mappingsCollection.toArray(new String[mappingsCollection.size()]);
            if (mappings == null)
            {
                mappings = new String[]{ };
```

### ConstantValue
Condition `resourceName == null` is always `false`
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java

        //Check libraryName and resourceName
        if (resourceName == null)
        {
            return null;
```

### ConstantValue
Condition `resourceName != null` is always `true`
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
            libraryVersion = null;
        }
        if (resourceName != null && resourceName.length() == 0)
        {
            resourceName = null;
```

### ConstantValue
Condition `resourceBasePath != null` is always `true`
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
                    .substring(ResourceHandler.RESOURCE_IDENTIFIER.length() + 1);

            if (resourceBasePath != null && !ResourceValidationUtils.isValidResourceName(resourceName))
            {
                httpServletResponse.setStatus(HttpServletResponse.SC_NOT_FOUND);
```

### ConstantValue
Condition `resourceName == null` is always `false`
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java

        //Check libraryName and resourceName
        if (resourceName == null)
        {
            return null;
```

### ConstantValue
Condition `resourceName != null` is always `true`
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
            libraryVersion = null;
        }
        if (resourceName != null && resourceName.length() == 0)
        {
            resourceName = null;
```

### ConstantValue
Condition `!(secretKey instanceof SecretKey)` is always `false`
in `impl/src/main/java/org/apache/myfaces/application/viewstate/StateUtils.java`
#### Snippet
```java
        }
        
        if (!(secretKey instanceof SecretKey))
        {
            throw new ClassCastException("Did not find an instance of SecretKey "
```

### ConstantValue
Condition `secretKey instanceof SecretKey` is always `true`
in `impl/src/main/java/org/apache/myfaces/application/viewstate/StateUtils.java`
#### Snippet
```java
        }
        
        if (!(secretKey instanceof SecretKey))
        {
            throw new ClassCastException("Did not find an instance of SecretKey "
```

### ConstantValue
Condition `!(secretKey instanceof SecretKey)` is always `false`
in `impl/src/main/java/org/apache/myfaces/application/viewstate/StateUtils.java`
#### Snippet
```java
        }
        
        if (!(secretKey instanceof SecretKey))
        {
            throw new ClassCastException("Did not find an instance of SecretKey "
```

### ConstantValue
Condition `secretKey instanceof SecretKey` is always `true`
in `impl/src/main/java/org/apache/myfaces/application/viewstate/StateUtils.java`
#### Snippet
```java
        }
        
        if (!(secretKey instanceof SecretKey))
        {
            throw new ClassCastException("Did not find an instance of SecretKey "
```

### ConstantValue
Value `navigationCase` is always 'null'
in `impl/src/main/java/org/apache/myfaces/application/NavigationHandlerImpl.java`
#### Snippet
```java
        if (flowNavigationStructure == null)
        {
            return navigationCase;
        }
        if (viewId != null)
```

### ConstantValue
Condition `UIViewAction.isProcessingBroadcast(facesContext)` is always `false`
in `impl/src/main/java/org/apache/myfaces/application/NavigationHandlerImpl.java`
#### Snippet
```java
            }
            boolean isViewActionProcessingBroadcastAndRequiresRedirect = false;
            if (UIViewAction.isProcessingBroadcast(facesContext))
            {
                // f:viewAction tag always triggers a redirect to enforce execution of 
```

### ConstantValue
Condition `isViewActionProcessingBroadcastAndRequiresRedirect` is always `false` when reached
in `impl/src/main/java/org/apache/myfaces/application/NavigationHandlerImpl.java`
#### Snippet
```java
                }
            }
            if (navigationCase.isRedirect() || isViewActionProcessingBroadcastAndRequiresRedirect)
            { 
                // Need to add the FlowHandler parameters here.
```

### ConstantValue
Condition `targetFlow != null` is always `true` when reached
in `impl/src/main/java/org/apache/myfaces/application/NavigationHandlerImpl.java`
#### Snippet
```java
                    // Ensure that at least one has a value and check for equality
                    if ((currentFlow != null && !currentFlow.equals(targetFlow)) ||
                                    (targetFlow != null && !targetFlow.equals(currentFlow)))
                    {
                        // MYFACES-4504: check isEmpty()
```

### ConstantValue
Condition `currentFlow != null` is always `true` when reached
in `impl/src/main/java/org/apache/myfaces/application/NavigationHandlerImpl.java`
#### Snippet
```java
            // If is necessary to enter a flow or there is a current
            // flow and it is necessary to check a flow node
            if (startFlow || (checkFlowNode && currentFlow != null))
            {
                boolean complete = false;
```

### ConstantValue
Condition `startFlow || checkFlowNode` is always `true` when reached
in `impl/src/main/java/org/apache/myfaces/application/NavigationHandlerImpl.java`
#### Snippet
```java
                boolean checkNavCase = true;

                while (!complete && (startFlow || checkFlowNode))
                {
                    if (startFlow)
```

### ConstantValue
Condition `checkFlowNode` is always `true` when reached
in `impl/src/main/java/org/apache/myfaces/application/NavigationHandlerImpl.java`
#### Snippet
```java
                boolean checkNavCase = true;

                while (!complete && (startFlow || checkFlowNode))
                {
                    if (startFlow)
```

### ConstantValue
Condition `checkFlowNode` is always `true`
in `impl/src/main/java/org/apache/myfaces/application/NavigationHandlerImpl.java`
#### Snippet
```java
                        startFlow = false;
                    }
                    if (checkFlowNode)
                    {
                        FlowNode flowNode = currentFlow.getNode(outcomeToGo);
```

### ConstantValue
Condition `!complete` is always `true`
in `impl/src/main/java/org/apache/myfaces/application/NavigationHandlerImpl.java`
#### Snippet
```java
                        {
                            checkNavCase = true;
                            if (!complete && flowNode instanceof SwitchNode)
                            {
                                outcomeToGo = calculateSwitchOutcome(facesContext, (SwitchNode) flowNode);
```

### ConstantValue
Value `complete` is always 'false'
in `impl/src/main/java/org/apache/myfaces/application/NavigationHandlerImpl.java`
#### Snippet
```java
                        {
                            checkNavCase = true;
                            if (!complete && flowNode instanceof SwitchNode)
                            {
                                outcomeToGo = calculateSwitchOutcome(facesContext, (SwitchNode) flowNode);
```

### ConstantValue
Condition `!complete` is always `true`
in `impl/src/main/java/org/apache/myfaces/application/NavigationHandlerImpl.java`
#### Snippet
```java
                                continue;
                            }
                            if (!complete && flowNode instanceof FlowCallNode)
                            {
                                // "... If the node is a FlowCallNode, save it aside as facesFlowCallNode. ..."
```

### ConstantValue
Value `complete` is always 'false'
in `impl/src/main/java/org/apache/myfaces/application/NavigationHandlerImpl.java`
#### Snippet
```java
                                continue;
                            }
                            if (!complete && flowNode instanceof FlowCallNode)
                            {
                                // "... If the node is a FlowCallNode, save it aside as facesFlowCallNode. ..."
```

### ConstantValue
Condition `interfaces != null` is always `true`
in `impl/src/main/java/org/apache/myfaces/application/ApplicationImpl.java`
#### Snippet
```java
        {
            final Class<?>[] interfaces = targetClass.getInterfaces();
            if (interfaces != null)
            {
                for (int i = 0, len = interfaces.length; i < len; i++)
```

### ConstantValue
Condition `dependencyList != null` is always `true`
in `impl/src/main/java/org/apache/myfaces/application/ApplicationImpl.java`
#### Snippet
```java
 
        // resource dependencies were found through inspection or from cache, handle them
        if (dependencyList != null && !dependencyList.isEmpty()) 
        {
            for (int i = 0, size = dependencyList.size(); i < size; i++)
```

### ConstantValue
Condition `dependencyList != null` is always `true`
in `impl/src/main/java/org/apache/myfaces/application/ApplicationImpl.java`
#### Snippet
```java
        
        //if we're in production and the list is not yet cached, store it
        if(isProduction && !isCachedList && dependencyList != null)   
        {
            // Note at this point listenerForList cannot be null, but just let dependencyList != null
```

### ConstantValue
Condition `c == null` is always `true`
in `test/src/main/java/org/apache/myfaces/test/base/junit/runners/TestClassLoader.java`
#### Snippet
```java
            }
        }
        if (c == null)
        {
            byte[] data = lookupClassData(name);
```

### ConstantValue
Condition `data == null` is always `false`
in `test/src/main/java/org/apache/myfaces/test/base/junit/runners/TestClassLoader.java`
#### Snippet
```java
        {
            byte[] data = lookupClassData(name);
            if (data == null)
            {
                throw new ClassNotFoundException();
```

### ConstantValue
Value `redirect` is always 'true'
in `test/src/main/java/org/apache/myfaces/test/mock/MockFlash.java`
#### Snippet
```java
                    cookie.setValue(null);
                    httpResponse.addCookie(cookie);
                    requestMap.put(FLASH_REDIRECT, redirect);
                }
                else
```

### ConstantValue
Value `redirect` is always 'true'
in `test/src/main/java/org/apache/myfaces/test/mock/MockFlash.java`
#### Snippet
```java
            // pass it between request. One strategy is use a cookie that is never sent
            // to the client. Other alternative is use the session map.
            externalContext.getSessionMap().put(FLASH_REDIRECT, redirect);
        }
        requestMap.put(FLASH_REDIRECT, redirect);
```

### ConstantValue
Value `keepMessages` is always 'true'
in `test/src/main/java/org/apache/myfaces/test/mock/MockFlash.java`
#### Snippet
```java
                    cookie.setValue(null);
                    httpResponse.addCookie(cookie);
                    requestMap.put(FLASH_KEEP_MESSAGES, keepMessages);
                }
                else
```

### ConstantValue
Value `keepMessages` is always 'true'
in `test/src/main/java/org/apache/myfaces/test/mock/MockFlash.java`
#### Snippet
```java
        {
            externalContext.getSessionMap().put(FLASH_KEEP_MESSAGES,
                    keepMessages);
        }
        requestMap.put(FLASH_KEEP_MESSAGES, keepMessages);
```

### ConstantValue
Condition `port == 0` is always `false`
in `test/src/main/java/org/apache/myfaces/test/mock/MockExternalContext10.java`
#### Snippet
```java
    {
        Integer port = 8080;
        port = (port == 0) ? null : port;
        if (port != null && 
            !port.equals(request.getServerPort()))
```

### ConstantValue
Condition `port != null` is always `true`
in `test/src/main/java/org/apache/myfaces/test/mock/MockExternalContext10.java`
#### Snippet
```java
        Integer port = 8080;
        port = (port == 0) ? null : port;
        if (port != null && 
            !port.equals(request.getServerPort()))
        {
```

### ConstantValue
Value `event` is always 'null'
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication20.java`
#### Snippet
```java
                event = _traverseListenerList(holder
                        .getListenersForEventClass(systemEventClass),
                        systemEventClass, source, event);
            }
            
```

### ConstantValue
Condition `dependencyList == null` is always `true`
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication20.java`
#### Snippet
```java
        List<ResourceDependency> dependencyList = null;
        
        if(dependencyList == null)  //not in production or the class hasn't been inspected yet
        {   
            ResourceDependency dependency = inspectedClass.getAnnotation(ResourceDependency.class);
```

### ConstantValue
Condition `listenerForList == null` is always `true`
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication20.java`
#### Snippet
```java
        List<ListenerFor> listenerForList = null;
        
        if(listenerForList == null) //not in production or the class hasn't been inspected yet
        {
            ListenerFor listener = inspectedClass.getAnnotation(ListenerFor.class);
```

### ConstantValue
Result of `value.booleanValue()` is always 'true'
in `test/src/main/java/org/apache/myfaces/test/mock/resource/MockSimpleResourceHandler.java`
#### Snippet
```java
        {
            //return the saved value
            return value.booleanValue();
        }
        else
```

### ConstantValue
Condition `cbl.size() == 1` is always `true` when reached
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/CommonHtmlEventsUtil.java`
#### Snippet
```java
        }

        if (cbl.size() > 1 || (cbl.size() == 1 && attributeValue != null))
        {
            return HtmlRendererUtils.renderHTMLStringAttribute(writer, componentProperty, htmlAttrName,
```

### ConstantValue
Condition `cbl.size() == 1` is always `true` when reached
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/HtmlRendererUtils.java`
#### Snippet
```java
            return renderHTMLAttribute(writer, componentProperty, htmlAttrName, attributeValue);
        }
        if (cbl.size() > 1 || (cbl.size() == 1 && attributeValue != null))
        {
            return renderHTMLAttribute(writer, componentProperty, htmlAttrName,
```

### ConstantValue
Condition `component instanceof HtmlPanelGrid` is always `true`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/HtmlRendererUtils.java`
#### Snippet
```java
        String captionClass;
        String captionStyle;
        if (component instanceof HtmlPanelGrid)
        {
            HtmlPanelGrid panelGrid = (HtmlPanelGrid) component;
```

### ConstantValue
Value `partialOrBehaviorSubmit` is always 'true'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/HtmlRendererUtils.java`
#### Snippet
```java
            if (partialOrBehaviorSubmit)
            {
                return partialOrBehaviorSubmit;
            }
        }
```

## RuleId[ruleID=MethodOverridesStaticMethod]
### MethodOverridesStaticMethod
Method `getResourceAsStream()` tries to override a static method of a superclass
in `impl/src/main/java/org/apache/myfaces/util/lang/ClassUtils.java`
#### Snippet
```java
    // @Override MYFACES-4449: Methods that call ClassUtils.class.getClassLoader() need to be here
    //           as well as in the API ClassUtils so that the correct ClassLoader is used.
    public static InputStream getResourceAsStream(String resource)
    {
        InputStream stream = getContextClassLoader().getResourceAsStream(resource);
```

### MethodOverridesStaticMethod
Method `simpleClassForName()` tries to override a static method of a superclass
in `impl/src/main/java/org/apache/myfaces/util/lang/ClassUtils.java`
#### Snippet
```java
    // @Override MYFACES-4449: Methods that call ClassUtils.class.getClassLoader() need to be here
    //           as well as in the API ClassUtils so that the correct ClassLoader is used.
    public static Class simpleClassForName(String type)
    {
        return simpleClassForName(type, true);
```

### MethodOverridesStaticMethod
Method `simpleClassForName()` tries to override a static method of a superclass
in `impl/src/main/java/org/apache/myfaces/util/lang/ClassUtils.java`
#### Snippet
```java
    // @Override MYFACES-4449: Methods that call ClassUtils.class.getClassLoader() need to be here
    //           as well as in the API ClassUtils so that the correct ClassLoader is used.
    public static Class simpleClassForName(String type, boolean logException)
    {
        Class returnClass = null;
```

### MethodOverridesStaticMethod
Method `classForName()` tries to override a static method of a superclass
in `impl/src/main/java/org/apache/myfaces/util/lang/ClassUtils.java`
#### Snippet
```java
    // @Override MYFACES-4449: Methods that call ClassUtils.class.getClassLoader() need to be here
    //           as well as in the API ClassUtils so that the correct ClassLoader is used.
    public static <T> Class<T> classForName(String type) throws ClassNotFoundException
    {
        Assert.notNull(type, "type");
```

### MethodOverridesStaticMethod
Method `getResource()` tries to override a static method of a superclass
in `impl/src/main/java/org/apache/myfaces/util/lang/ClassUtils.java`
#### Snippet
```java
    // @Override MYFACES-4449: Methods that call ClassUtils.class.getClassLoader() need to be here
    //           as well as in the API ClassUtils so that the correct ClassLoader is used.
    public static URL getResource(String resource)
    {
        URL url = getContextClassLoader().getResource(resource);
```

### MethodOverridesStaticMethod
Method `getStyleAndStyleClass()` tries to override a static method of a superclass
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessagesRendererBase.java`
#### Snippet
```java


    public static String[] getStyleAndStyleClass(UIComponent messages,
                                             FacesMessage.Severity severity)
    {
```

### MethodOverridesStaticMethod
Method `setCurrentInstance()` tries to override a static method of a superclass
in `test/src/main/java/org/apache/myfaces/test/mock/MockFacesContext10.java`
#### Snippet
```java
     * @param facesContext The new FacesContext
     */
    public static void setCurrentInstance(FacesContext facesContext)
    {

```

## RuleId[ruleID=StringConcatenationInsideStringBufferAppend]
### StringConcatenationInsideStringBufferAppend
String concatenation as argument to `StringBuilder.append()` call
in `test/src/main/java/org/apache/myfaces/test/mock/MockExternalContext20.java`
#### Snippet
```java
        if (fragment != null)
        {
            newUrl.append(URL_FRAGMENT_SEPERATOR + fragment);
        }

```

## RuleId[ruleID=IOResource]
### IOResource
'MyFacesObjectInputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `impl/src/main/java/org/apache/myfaces/component/validate/WholeBeanValidator.java`
#### Snippet
```java
            }

            ObjectInputStream ois = new MyFacesObjectInputStream(baos.getInputStream());
            try 
            {
```

### IOResource
'MyFacesObjectInputStream' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `impl/src/main/java/org/apache/myfaces/application/viewstate/StateCacheServerSide.java`
#### Snippet
```java
                try
                {
                    final ObjectInputStream in = new MyFacesObjectInputStream(is);
                    ois = in;
                    Object object = null;
```

### IOResource
'ZipFile' should be opened in front of a 'try' block and closed in the corresponding 'finally' block
in `test/src/main/java/org/apache/myfaces/test/base/junit/runners/TestPerClassLoaderRunner.java`
#### Snippet
```java
        try
        {
            zipFile = new ZipFile(archive);
        }
        catch (IOException io)
```

## RuleId[ruleID=OptionalIsPresent]
### OptionalIsPresent
Can be replaced with single expression in functional style
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFaceletFactory.java`
#### Snippet
```java
            _baseUrl = Optional.ofNullable(resource == null ? null : resource.getURL());
        }
        return _baseUrl.isPresent() ? _baseUrl.get() : null;
    }

```

## RuleId[ruleID=RedundantOperationOnEmptyContainer]
### RedundantOperationOnEmptyContainer
Collection `emptyList` is always empty
in `api/src/main/java/jakarta/faces/render/RenderKit.java`
#### Snippet
```java
        List<String> emptyList = Collections.emptyList();

        return emptyList.iterator();
    }

```

### RedundantOperationOnEmptyContainer
Collection `emptyList` is always empty
in `api/src/main/java/jakarta/faces/render/RenderKit.java`
#### Snippet
```java
        List<String> emptyList = Collections.emptyList();

        return emptyList.iterator();
    }

```

### RedundantOperationOnEmptyContainer
Collection `Collections.EMPTY_LIST` is always empty
in `api/src/main/java/jakarta/faces/application/Application.java`
#### Snippet
```java
        // to keep compatiblity with previous jsf 2.0 Application
        // instances
        return Collections.EMPTY_LIST.iterator();
    }

```

## RuleId[ruleID=FieldMayBeStatic]
### FieldMayBeStatic
Field `WARNING_TOMCAT_WEB_SOCKET_BOMBED` may be 'static'
in `impl/src/main/java/org/apache/myfaces/push/cdi/WebsocketSessionManager.java`
#### Snippet
```java
    }

    private final String WARNING_TOMCAT_WEB_SOCKET_BOMBED =
            "Tomcat cannot handle concurrent push messages. A push message has been sent only after %s retries."
            + " Consider rate limiting sending push messages. For example, once every 500ms.";    
```

## RuleId[ruleID=RedundantLengthCheck]
### RedundantLengthCheck
Redundant array length check
in `api/src/main/java/jakarta/faces/validator/BeanValidator.java`
#### Snippet
```java
        // instance has validation groups other than or in addition to the Default group
        boolean containsOtherValidationGroup = false;
        if (validationGroupsArray != null && validationGroupsArray.length > 0)
        {
            for (Class<?> clazz : validationGroupsArray)
```

### RedundantLengthCheck
Redundant array length check
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TextUnit.java`
#### Snippet
```java

        TagAttribute[] attrs = tag.getAttributes().getAll();
        if (attrs.length > 0)
        {
            for (TagAttribute attr : attrs)
```

### RedundantLengthCheck
Redundant array length check
in `impl/src/main/java/org/apache/myfaces/application/ViewIdSupport.java`
#### Snippet
```java
            String candidateViewId = builder.toString();
            
            if (config.getFaceletsViewMappings() != null && config.getFaceletsViewMappings().length > 0 )
            {
                for (String mapping : config.getFaceletsViewMappings())
```

## RuleId[ruleID=UtilityClassWithoutPrivateConstructor]
### UtilityClassWithoutPrivateConstructor
Class `BeanValidationUtils` has only 'static' members, and lacks a 'private' constructor
in `api/src/main/java/org/apache/myfaces/core/api/shared/BeanValidationUtils.java`
#### Snippet
```java
 * @since 2.0
 */
public final class BeanValidationUtils
{
    public static boolean isBeanValidator(Validator v)
```

### UtilityClassWithoutPrivateConstructor
Class `ValueReferenceResolver` has only 'static' members, and lacks a 'private' constructor
in `api/src/main/java/org/apache/myfaces/core/api/shared/ValueReferenceResolver.java`
#### Snippet
```java
import jakarta.faces.el.CompositeComponentExpressionHolder;

public class ValueReferenceResolver
{
    /**
```

### UtilityClassWithoutPrivateConstructor
Class `CommonHtmlEvents` has only 'static' members, and lacks a 'private' constructor
in `api/src/main/java/org/apache/myfaces/core/api/shared/CommonHtmlEvents.java`
#### Snippet
```java
import jakarta.faces.component.UIComponent;

public class CommonHtmlEvents
{
    public static final String EVENTS_MARKED = "oam.EVENTS_MARKED";
```

### UtilityClassWithoutPrivateConstructor
Class `AttributeUtils` has only 'static' members, and lacks a 'private' constructor
in `api/src/main/java/org/apache/myfaces/core/api/shared/AttributeUtils.java`
#### Snippet
```java
import jakarta.faces.component.UIComponent;

public class AttributeUtils
{
    public static boolean getBooleanAttribute(UIComponent component,
```

### UtilityClassWithoutPrivateConstructor
Class `CommonHtmlAttributes` has only 'static' members, and lacks a 'private' constructor
in `api/src/main/java/org/apache/myfaces/core/api/shared/CommonHtmlAttributes.java`
#### Snippet
```java
 * This is a list of the most common properties used by a Faces html component, organized by interfaces.
 */
public class CommonHtmlAttributes
{
    public static final String ATTRIBUTES_MARKED = "oam.ATTRIBUTES_MARKED";
```

### UtilityClassWithoutPrivateConstructor
Class `CollectionUtils` has only 'static' members, and lacks a 'private' constructor
in `api/src/main/java/org/apache/myfaces/core/api/shared/lang/CollectionUtils.java`
#### Snippet
```java
import java.util.stream.Stream;

public class CollectionUtils
{
    public static void forEach(Object value, Consumer<Object> callback)
```

### UtilityClassWithoutPrivateConstructor
Class `SharedStringBuilder` has only 'static' members, and lacks a 'private' constructor
in `api/src/main/java/org/apache/myfaces/core/api/shared/lang/SharedStringBuilder.java`
#### Snippet
```java
 *
 */
public class SharedStringBuilder
{
    public static StringBuilder get(String stringBuilderKey)
```

### UtilityClassWithoutPrivateConstructor
Class `MessageUtils` has only 'static' members, and lacks a 'private' constructor
in `api/src/main/java/org/apache/myfaces/core/api/shared/MessageUtils.java`
#### Snippet
```java
import org.apache.myfaces.core.api.shared.lang.ClassUtils;

public class MessageUtils
{
    private static final String DETAIL_SUFFIX = "_detail";
```

### UtilityClassWithoutPrivateConstructor
Class `SelectItemsUtil` has only 'static' members, and lacks a 'private' constructor
in `api/src/main/java/org/apache/myfaces/core/api/shared/SelectItemsUtil.java`
#### Snippet
```java
import org.apache.myfaces.core.api.shared.lang.CollectionUtils;

public class SelectItemsUtil
{
    public static final String ATTR_ITEM_VALUE = "itemValue";
```

### UtilityClassWithoutPrivateConstructor
Class `_FactoryFinderProviderFactory` has only 'static' members, and lacks a 'private' constructor
in `api/src/main/java/jakarta/faces/_FactoryFinderProviderFactory.java`
#### Snippet
```java
 * @since 2.0.5
 */
class _FactoryFinderProviderFactory
{

```

### UtilityClassWithoutPrivateConstructor
Class `SharedRendererUtils` has only 'static' members, and lacks a 'private' constructor
in `api/src/main/java/org/apache/myfaces/core/api/shared/SharedRendererUtils.java`
#### Snippet
```java
import org.apache.myfaces.core.api.shared.lang.Assert;

public class SharedRendererUtils
{
    static final String COLLECTION_TYPE_KEY = "collectionType";
```

### UtilityClassWithoutPrivateConstructor
Class `PropertyDescriptorUtils` has only 'static' members, and lacks a 'private' constructor
in `api/src/main/java/org/apache/myfaces/core/api/shared/lang/PropertyDescriptorUtils.java`
#### Snippet
```java
import org.apache.myfaces.buildtools.maven2.plugin.builder.annotation.JSFWebConfigParam;

public class PropertyDescriptorUtils
{
    private static final Logger LOG = Logger.getLogger(PropertyDescriptorUtils.class.getName());
```

### UtilityClassWithoutPrivateConstructor
Class `LocaleUtils` has only 'static' members, and lacks a 'private' constructor
in `api/src/main/java/org/apache/myfaces/core/api/shared/lang/LocaleUtils.java`
#### Snippet
```java
 * @version $Id$
 */
public class LocaleUtils
{

```

### UtilityClassWithoutPrivateConstructor
Class `Assert` has only 'static' members, and lacks a 'private' constructor
in `api/src/main/java/org/apache/myfaces/core/api/shared/lang/Assert.java`
#### Snippet
```java
package org.apache.myfaces.core.api.shared.lang;

public class Assert
{
    public static void notNull(Object value)
```

### UtilityClassWithoutPrivateConstructor
Class `FactoryFinder` has only 'static' members, and lacks a 'private' constructor
in `api/src/main/java/jakarta/faces/FactoryFinder.java`
#### Snippet
```java
 * see Javadoc of <a href="http://java.sun.com/javaee/javaserverfaces/1.2/docs/api/index.html">Faces Specification</a>
 */
public final class FactoryFinder
{
    public static final String APPLICATION_FACTORY = "jakarta.faces.application.ApplicationFactory";
```

### UtilityClassWithoutPrivateConstructor
Class `_NavigationUtils` has only 'static' members, and lacks a 'private' constructor
in `api/src/main/java/jakarta/faces/application/_NavigationUtils.java`
#### Snippet
```java
import jakarta.faces.context.FacesContext;

class _NavigationUtils
{
    /**
```

### UtilityClassWithoutPrivateConstructor
Class `CDIUtils` has only 'static' members, and lacks a 'private' constructor
in `impl/src/main/java/org/apache/myfaces/cdi/util/CDIUtils.java`
#### Snippet
```java
 * Lookup code for Contextual Instances.
 */
public class CDIUtils
{
    public static BeanManager getBeanManager(FacesContext facesContext)
```

### UtilityClassWithoutPrivateConstructor
Class `ServiceProviderFinderFactory` has only 'static' members, and lacks a 'private' constructor
in `impl/src/main/java/org/apache/myfaces/spi/ServiceProviderFinderFactory.java`
#### Snippet
```java
 *
 */
public class ServiceProviderFinderFactory
{

```

### UtilityClassWithoutPrivateConstructor
Class `SpiUtils` has only 'static' members, and lacks a 'private' constructor
in `impl/src/main/java/org/apache/myfaces/spi/impl/SpiUtils.java`
#### Snippet
```java
 * @author Leonardo Uribe
 */
public final class SpiUtils
{
    public static Object build(ExternalContext ectx, Class spiClass, Class defaultImpl)
```

### UtilityClassWithoutPrivateConstructor
Class `FlowUtils` has only 'static' members, and lacks a 'private' constructor
in `impl/src/main/java/org/apache/myfaces/flow/FlowUtils.java`
#### Snippet
```java
import jakarta.faces.flow.Flow;

public class FlowUtils
{
    public static String getFlowMapKey(FacesContext facesContext, FlowReference flowReference)
```

### UtilityClassWithoutPrivateConstructor
Class `WebsocketFacesInit` has only 'static' members, and lacks a 'private' constructor
in `impl/src/main/java/org/apache/myfaces/push/WebsocketFacesInit.java`
#### Snippet
```java
 * 
 */
public class WebsocketFacesInit
{
    public static void init(ExternalContext context)
```

### UtilityClassWithoutPrivateConstructor
Class `NavigationUtils` has only 'static' members, and lacks a 'private' constructor
in `impl/src/main/java/org/apache/myfaces/util/NavigationUtils.java`
#### Snippet
```java
 * @author lu4242
 */
public final class NavigationUtils
{
    public static Set<NavigationCase> convertNavigationCasesToAPI(NavigationRule rule)
```

### UtilityClassWithoutPrivateConstructor
Class `CommentUtils` has only 'static' members, and lacks a 'private' constructor
in `impl/src/main/java/org/apache/myfaces/util/CommentUtils.java`
#### Snippet
```java
 * "style" tags by HtmlResponseWriterImpl.
 */
public class CommentUtils
{
    public static final String INLINE_SCRIPT_COMMENT = "//";
```

### UtilityClassWithoutPrivateConstructor
Class `UrlPatternMatcher` has only 'static' members, and lacks a 'private' constructor
in `impl/src/main/java/org/apache/myfaces/util/UrlPatternMatcher.java`
#### Snippet
```java
import org.apache.myfaces.util.lang.StringUtils;

public class UrlPatternMatcher
{
    /**
```

### UtilityClassWithoutPrivateConstructor
Class `WebConfigParamUtils` has only 'static' members, and lacks a 'private' constructor
in `impl/src/main/java/org/apache/myfaces/util/WebConfigParamUtils.java`
#### Snippet
```java
 * @since 2.0.10 (4.0.4 in shared, 1.0.1 in commons)
 */
public final class WebConfigParamUtils
{
    /**
```

### UtilityClassWithoutPrivateConstructor
Class `Hex` has only 'static' members, and lacks a 'private' constructor
in `impl/src/main/java/org/apache/myfaces/util/lang/Hex.java`
#### Snippet
```java

// minimal version of org.apache.commons.codec.binary.Hex to avoid commons-codec or JAXB DatatypeConverter dependency.
public class Hex
{
    /**
```

### UtilityClassWithoutPrivateConstructor
Class `ArrayUtils` has only 'static' members, and lacks a 'private' constructor
in `impl/src/main/java/org/apache/myfaces/util/lang/ArrayUtils.java`
#### Snippet
```java
 * Utility class for managing arrays
 */
public class ArrayUtils
{
    public static final Object[] EMPTY_OBJECT_ARRAY = new Object[0];
```

### UtilityClassWithoutPrivateConstructor
Class `HashMapUtils` has only 'static' members, and lacks a 'private' constructor
in `impl/src/main/java/org/apache/myfaces/util/lang/HashMapUtils.java`
#### Snippet
```java


public class HashMapUtils
{
    //~ Constructors -------------------------------------------------------------------------------
```

### UtilityClassWithoutPrivateConstructor
Class `FilenameUtils` has only 'static' members, and lacks a 'private' constructor
in `impl/src/main/java/org/apache/myfaces/util/lang/FilenameUtils.java`
#### Snippet
```java
 * @since 1.1
 */
public class FilenameUtils
{

```

### UtilityClassWithoutPrivateConstructor
Class `FaceletsVDLUtils` has only 'static' members, and lacks a 'private' constructor
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletsVDLUtils.java`
#### Snippet
```java
 * @author lu4242
 */
public class FaceletsVDLUtils
{
    /**
```

### UtilityClassWithoutPrivateConstructor
Class `ComponentSupport` has only 'static' members, and lacks a 'private' constructor
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ComponentSupport.java`
#### Snippet
```java
 * @version $Id$
 */
public final class ComponentSupport
{
    private static final Logger LOG = Logger.getLogger(ComponentSupport.class.getName());
```

### UtilityClassWithoutPrivateConstructor
Class `FaceletsTemplateMappingUtils` has only 'static' members, and lacks a 'private' constructor
in `impl/src/main/java/org/apache/myfaces/view/facelets/util/FaceletsTemplateMappingUtils.java`
#### Snippet
```java
import org.apache.myfaces.util.UrlPatternMatcher;

public class FaceletsTemplateMappingUtils
{
    public static final boolean matchTemplate(RuntimeConfig runtimeConfig, String path)
```

### UtilityClassWithoutPrivateConstructor
Class `FaceletsViewDeclarationLanguageUtils` has only 'static' members, and lacks a 'private' constructor
in `impl/src/main/java/org/apache/myfaces/view/facelets/util/FaceletsViewDeclarationLanguageUtils.java`
#### Snippet
```java
 * @author Leonardo Uribe
 */
public class FaceletsViewDeclarationLanguageUtils
{
    public static Class getReturnType(String signature)
```

### UtilityClassWithoutPrivateConstructor
Class `TagLibraryConfig` has only 'static' members, and lacks a 'private' constructor
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TagLibraryConfig.java`
#### Snippet
```java
 * @version $Id$
 */
public final class TagLibraryConfig
{
    protected final static Logger log = Logger.getLogger(TagLibraryConfig.class.getName());
```

### UtilityClassWithoutPrivateConstructor
Class `CheckDuplicateIdFaceletUtils` has only 'static' members, and lacks a 'private' constructor
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/CheckDuplicateIdFaceletUtils.java`
#### Snippet
```java
 * @author Leonardo Uribe
 */
public final class CheckDuplicateIdFaceletUtils
{
    
```

### UtilityClassWithoutPrivateConstructor
Class `TagLibraryConfigUnmarshallerImpl` has only 'static' members, and lacks a 'private' constructor
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TagLibraryConfigUnmarshallerImpl.java`
#### Snippet
```java
 *
 */
public class TagLibraryConfigUnmarshallerImpl
{
    private static final Logger log = Logger.getLogger(TagLibraryConfigUnmarshallerImpl.class.getName());
```

### UtilityClassWithoutPrivateConstructor
Class `LogMetaInfUtils` has only 'static' members, and lacks a 'private' constructor
in `impl/src/main/java/org/apache/myfaces/config/LogMetaInfUtils.java`
#### Snippet
```java
import java.util.regex.Pattern;

class LogMetaInfUtils
{

```

### UtilityClassWithoutPrivateConstructor
Class `ConfigFilesXmlValidationUtils` has only 'static' members, and lacks a 'private' constructor
in `impl/src/main/java/org/apache/myfaces/config/ConfigFilesXmlValidationUtils.java`
#### Snippet
```java
import org.xml.sax.helpers.DefaultHandler;

public class ConfigFilesXmlValidationUtils
{
    public final static LSResourceResolver LS_RESOURCE_RESOLVER = new ValidatorLSResourceResolver();
```

### UtilityClassWithoutPrivateConstructor
Class `DirectedAcyclicGraphVerifier` has only 'static' members, and lacks a 'private' constructor
in `impl/src/main/java/org/apache/myfaces/config/util/DirectedAcyclicGraphVerifier.java`
#### Snippet
```java
 * @version CVS $ Revision: 1.1 $
 */
public class DirectedAcyclicGraphVerifier
{
    /**
```

### UtilityClassWithoutPrivateConstructor
Class `ClassByteCodeAnnotationFilter` has only 'static' members, and lacks a 'private' constructor
in `impl/src/main/java/org/apache/myfaces/config/annotation/ClassByteCodeAnnotationFilter.java`
#### Snippet
```java
 * @version $Revision$ $Date$
 */
class ClassByteCodeAnnotationFilter
{
    private static final Logger log = Logger.getLogger(ClassByteCodeAnnotationFilter.class.getName());
```

### UtilityClassWithoutPrivateConstructor
Class `FacesInitializerFactory` has only 'static' members, and lacks a 'private' constructor
in `impl/src/main/java/org/apache/myfaces/webapp/FacesInitializerFactory.java`
#### Snippet
```java
 * @version $Revision$ $Date$
 */
public class FacesInitializerFactory
{   
    /**
```

### UtilityClassWithoutPrivateConstructor
Class `PackageInfo` has only 'static' members, and lacks a 'private' constructor
in `impl/src/main/java/org/apache/myfaces/config/annotation/PackageInfo.java`
#### Snippet
```java
 * @version $Revision$ $Date$
 */
class PackageInfo
{
    private static final Logger LOG = Logger.getLogger(PackageInfo.class.getName());
```

### UtilityClassWithoutPrivateConstructor
Class `ResourceELUtils` has only 'static' members, and lacks a 'private' constructor
in `impl/src/main/java/org/apache/myfaces/resource/ResourceELUtils.java`
#### Snippet
```java
 * Utility class when used in EL Expressions --&gt; #{resource}
 */
public class ResourceELUtils
{

```

### UtilityClassWithoutPrivateConstructor
Class `ResourceLoaderUtils` has only 'static' members, and lacks a 'private' constructor
in `impl/src/main/java/org/apache/myfaces/resource/ResourceLoaderUtils.java`
#### Snippet
```java
import org.apache.myfaces.util.lang.StringUtils;

public class ResourceLoaderUtils
{
    private static final DateTimeFormatter HTTP_RESPONSE_DATE_HEADER =
```

### UtilityClassWithoutPrivateConstructor
Class `ResourceValidationUtils` has only 'static' members, and lacks a 'private' constructor
in `impl/src/main/java/org/apache/myfaces/resource/ResourceValidationUtils.java`
#### Snippet
```java
package org.apache.myfaces.resource;

public class ResourceValidationUtils
{
    public static boolean isValidResourceName(String resourceName)
```

### UtilityClassWithoutPrivateConstructor
Class `MyFacesVisitHints` has only 'static' members, and lacks a 'private' constructor
in `impl/src/main/java/org/apache/myfaces/component/visit/MyFacesVisitHints.java`
#### Snippet
```java


public class MyFacesVisitHints
{
    public static final String SKIP_ITERATION_HINT = "jakarta.faces.visit.SKIP_ITERATION";
```

### UtilityClassWithoutPrivateConstructor
Class `MyFacesSearchExpressionHints` has only 'static' members, and lacks a 'private' constructor
in `impl/src/main/java/org/apache/myfaces/component/search/MyFacesSearchExpressionHints.java`
#### Snippet
```java
import jakarta.faces.component.search.SearchExpressionHint;

public class MyFacesSearchExpressionHints
{
    public static final Set<SearchExpressionHint> SET_IGNORE_NO_RESULT =
```

### UtilityClassWithoutPrivateConstructor
Class `ContentTypeUtils` has only 'static' members, and lacks a 'private' constructor
in `impl/src/main/java/org/apache/myfaces/renderkit/ContentTypeUtils.java`
#### Snippet
```java
import org.apache.myfaces.util.lang.StringUtils;

public class ContentTypeUtils
{
    public static final String HTML_CONTENT_TYPE = "text/html";
```

### UtilityClassWithoutPrivateConstructor
Class `CommonHtmlAttributesUtil` has only 'static' members, and lacks a 'private' constructor
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/CommonHtmlAttributesUtil.java`
#### Snippet
```java
import org.apache.myfaces.core.api.shared.CommonHtmlAttributes;

public final class CommonHtmlAttributesUtil
{
    public static long getMarkedAttributes(UIComponent component)
```

### UtilityClassWithoutPrivateConstructor
Class `SelectItemsUtils` has only 'static' members, and lacks a 'private' constructor
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/SelectItemsUtils.java`
#### Snippet
```java
 * Utility methods to manipulate SelectItem/SelectItems
 */
public class SelectItemsUtils
{
    private static final char TABULATOR = '\t';
```

### UtilityClassWithoutPrivateConstructor
Class `ResourceUtils` has only 'static' members, and lacks a 'private' constructor
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/ResourceUtils.java`
#### Snippet
```java
import org.apache.myfaces.resource.ContractResource;

public class ResourceUtils
{
    public final static String MYFACES_LIBRARY_NAME = "org.apache.myfaces";
```

### UtilityClassWithoutPrivateConstructor
Class `HtmlJavaScriptUtils` has only 'static' members, and lacks a 'private' constructor
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/HtmlJavaScriptUtils.java`
#### Snippet
```java
import jakarta.faces.context.ResponseWriter;

public final class HtmlJavaScriptUtils
{
    private static final Logger log = Logger.getLogger(HtmlJavaScriptUtils.class.getName());
```

### UtilityClassWithoutPrivateConstructor
Class `OutcomeTargetUtils` has only 'static' members, and lacks a 'private' constructor
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/OutcomeTargetUtils.java`
#### Snippet
```java
 * Utility methods for OutcomeTarget components.
 */
public class OutcomeTargetUtils
{
    
```

### UtilityClassWithoutPrivateConstructor
Class `AjaxScriptBuilder` has only 'static' members, and lacks a 'private' constructor
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/AjaxScriptBuilder.java`
#### Snippet
```java

// CHECKSTYLE:OFF
public class AjaxScriptBuilder
{    
    public static void build(FacesContext context,
```

### UtilityClassWithoutPrivateConstructor
Class `ErrorPageWriter` has only 'static' members, and lacks a 'private' constructor
in `impl/src/main/java/org/apache/myfaces/renderkit/ErrorPageWriter.java`
#### Snippet
```java
 * @author Jakob Korherr (refactored and moved here from jakarta.faces.webapp._ErrorPageWriter)
 */
public final class ErrorPageWriter
{

```

### UtilityClassWithoutPrivateConstructor
Class `FacesServletMappingUtils` has only 'static' members, and lacks a 'private' constructor
in `impl/src/main/java/org/apache/myfaces/application/FacesServletMappingUtils.java`
#### Snippet
```java
import org.apache.myfaces.webapp.DelegatedFacesServlet;

public class FacesServletMappingUtils
{
    private static final String FACES_SERVLET_REGISTRATION = "org.apache.myfaces.FACES_SERVLET_REGISTRATION";
```

### UtilityClassWithoutPrivateConstructor
Class `StateCacheUtils` has only 'static' members, and lacks a 'private' constructor
in `impl/src/main/java/org/apache/myfaces/application/viewstate/StateCacheUtils.java`
#### Snippet
```java
import org.apache.myfaces.renderkit.MyfacesResponseStateManager;

public class StateCacheUtils
{
    public static boolean isMyFacesResponseStateManager(ResponseStateManager rsm)
```

### UtilityClassWithoutPrivateConstructor
Class `ClientBehaviorRendererUtils` has only 'static' members, and lacks a 'private' constructor
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/ClientBehaviorRendererUtils.java`
#### Snippet
```java
import org.apache.myfaces.util.lang.StringUtils;

public class ClientBehaviorRendererUtils
{
    private static final String SB_ESCAPE_JS_FOR_CHAIN = ClientBehaviorRendererUtils.class.getName()
```

### UtilityClassWithoutPrivateConstructor
Class `CommonHtmlEventsUtil` has only 'static' members, and lacks a 'private' constructor
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/CommonHtmlEventsUtil.java`
#### Snippet
```java
import org.apache.myfaces.renderkit.RendererUtils;

public class CommonHtmlEventsUtil
{
    public static long getMarkedEvents(UIComponent component)
```

## RuleId[ruleID=DataFlowIssue]
### DataFlowIssue
Variable is already assigned to this value
in `api/src/main/java/org/apache/myfaces/core/api/shared/MessageUtils.java`
#### Snippet
```java
                if (detail != null)
                {
                    summary = null;
                }
                else
```

### DataFlowIssue
Variable is already assigned to this value
in `api/src/main/java/org/apache/myfaces/core/api/shared/MessageUtils.java`
#### Snippet
```java
                    if (detail != null)
                    {
                        summary = null;
                    }
                    else
```

### DataFlowIssue
Condition `child instanceof UISelectItem` is redundant and can be replaced with a null check
in `api/src/main/java/org/apache/myfaces/core/api/shared/SelectItemsIterator.java`
#### Snippet
```java
            // classes are found. If there is no more elements
            // that conform this condition, just return false.
            while (!(child instanceof UISelectItem) && !(child instanceof UISelectItems))
            {
                // Try to skip it
```

### DataFlowIssue
Condition `child instanceof UISelectItems` is redundant and can be replaced with a null check
in `api/src/main/java/org/apache/myfaces/core/api/shared/SelectItemsUtil.java`
#### Snippet
```java
        {
            UIComponent child = component.getChildren().get(i);
            if (child instanceof UISelectItems)
            {
                UISelectItems uiSelectItems = (UISelectItems) child;
```

### DataFlowIssue
Casting `targetForConvertedValues` to `Collection` may produce `ClassCastException`
in `api/src/main/java/org/apache/myfaces/core/api/shared/SharedRendererUtils.java`
#### Snippet
```java
            else
            {
                ((Collection) targetForConvertedValues).add(value);
            }
        }
```

### DataFlowIssue
Casting `value` to `Enum` may produce `ClassCastException`
in `api/src/main/java/jakarta/faces/convert/EnumConverter.java`
#### Snippet
```java
        if (targetClass.isInstance(value))
        {
            return ((Enum<?>) value).name();
        }
        
```

### DataFlowIssue
Method invocation `hashCode` may produce `NullPointerException`
in `api/src/main/java/jakarta/faces/model/ResultSetDataModel.java`
#### Snippet
```java
            int result;
            result = (_entryKey != null ? _entryKey.hashCode() : 0);
            result = 29 * result + (_wrapMap.get(_entryKey) != null ? _wrapMap.get(_entryKey).hashCode() : 0);
            return result;
        }
```

### DataFlowIssue
Variable is already assigned to this value
in `api/src/main/java/jakarta/faces/component/UIOutput.java`
#### Snippet
```java
                {
                    nullDelta = false;
                    converterSaved = null;
                }
            }
```

### DataFlowIssue
Method invocation `withLocale` may produce `NullPointerException`
in `api/src/main/java/jakarta/faces/convert/DateTimeConverter.java`
#### Snippet
```java
            if (locale != null)
            {
                formatter = formatter.withLocale(locale);
            }
        }
```

### DataFlowIssue
Condition `component instanceof UISelectItem` is redundant and can be replaced with a null check
in `api/src/main/java/jakarta/faces/component/UISelectOne.java`
#### Snippet
```java
                        {
                            UIComponent component = iter.next();
                            if (component instanceof UISelectItem) 
                            {
                                UISelectItem item = (UISelectItem) component;
```

### DataFlowIssue
Variable is already assigned to this value
in `api/src/main/java/jakarta/faces/component/_DeltaStateHelper.java`
#### Snippet
```java
            else if (value == null && !_fullState.containsKey(key))
            {
                returnValue = null;
            }
            else
```

### DataFlowIssue
Method invocation `getRenderer` may produce `NullPointerException`
in `api/src/main/java/jakarta/faces/component/_EventListenerWrapper.java`
#### Snippet
```java
            else if ((listenerCapability & LISTENER_TYPE_RENDERER) != 0)
            {
                Renderer renderer = _component.getRenderer(context);
                Integer i = (Integer) values[1];
                if (i != null && i >= 0)
```

### DataFlowIssue
Method invocation `getRenderer` may produce `NullPointerException`
in `api/src/main/java/jakarta/faces/component/_EventListenerWrapper.java`
#### Snippet
```java
                {
                    UIComponent componentRef = _component != null ? _component : getCurrentComponent(context);
                    Renderer renderer = componentRef.getRenderer(context);
                    int i = 0;
                    while (renderer != null && !renderer.getClass().equals(listener.getClass()))
```

### DataFlowIssue
Variable is already assigned to this value
in `api/src/main/java/jakarta/faces/component/behavior/_DeltaStateHelper.java`
#### Snippet
```java
            else if (value == null && !_fullState.containsKey(key))
            {
                returnValue = null;
            }
            else
```

### DataFlowIssue
Condition `locale instanceof Locale` is redundant and can be replaced with a null check
in `api/src/main/java/jakarta/faces/component/UIViewRoot.java`
#### Snippet
```java
            locale = getFacesContext().getApplication().getViewHandler().calculateLocale(getFacesContext());

            if (locale instanceof Locale)
            {
                return (Locale)locale;
```

### DataFlowIssue
Method invocation `getRenderKit` may produce `NullPointerException`
in `api/src/main/java/jakarta/faces/application/StateManager.java`
#### Snippet
```java
        }

        return context.getRenderKit().getResponseStateManager().getViewState(context, savedView);
    }

```

### DataFlowIssue
Method invocation `getAttributes` may produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/el/resolver/ResourceResolver.java`
#### Snippet
```java
                            UIComponent cc = CompositeComponentELUtils.
                                    getCompositeComponentBasedOnLocation(facesContext, location);
                            Resource ccResource = (Resource) cc.getAttributes().get(
                                    Resource.COMPONENT_RESOURCE_KEY); 
                            libraryName = ccResource.getLibraryName();
```

### DataFlowIssue
Method invocation `getRequestMap` may produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/el/resolver/ScopedAttributeResolver.java`
#### Snippet
```java
        else
        {
            externalContext(context).getRequestMap().put((String)property, value);
        }

```

### DataFlowIssue
Method invocation `getRequestMap` may produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/el/resolver/ScopedAttributeResolver.java`
#### Snippet
```java
        final List<FeatureDescriptor> descriptorList = new ArrayList<FeatureDescriptor>();
        final ExternalContext extContext = externalContext(context);
        addDescriptorsToList(descriptorList, extContext.getRequestMap());
        addDescriptorsToList(descriptorList, extContext.getSessionMap());
        addDescriptorsToList(descriptorList, extContext.getApplicationMap());
```

### DataFlowIssue
Method invocation `release` may produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/cdi/util/AbstractContextualStorageHolder.java`
#### Snippet
```java
                finally
                {
                    facesContext.release();
                }
            }
```

### DataFlowIssue
Method invocation `put` may produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/flow/cdi/FlowScopeMap.java`
#### Snippet
```java
    {
        Map<Object, Object> wrapped = getWrapped(true);
        return wrapped.put(key, value);
    }

```

### DataFlowIssue
Method invocation `putAll` may produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/flow/cdi/FlowScopeMap.java`
#### Snippet
```java
    {
        Map<Object, Object> wrapped = getWrapped(true);
        wrapped.putAll(m);
    }

```

### DataFlowIssue
Casting `responseWriter` to `HtmlBufferResponseWriterWrapper` may produce `ClassCastException`
in `impl/src/main/java/org/apache/myfaces/push/WebsocketComponentRenderer.java`
#### Snippet
```java
            
            HtmlBufferResponseWriterWrapper htmlBufferResponseWritter =
                    (HtmlBufferResponseWriterWrapper) responseWriter;
            init.getUIWebsocketMarkupList().add(htmlBufferResponseWritter.toString());

```

### DataFlowIssue
Method invocation `toString` may produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/push/WebsocketComponentRenderer.java`
#### Snippet
```java
            HtmlBufferResponseWriterWrapper htmlBufferResponseWritter =
                    (HtmlBufferResponseWriterWrapper) responseWriter;
            init.getUIWebsocketMarkupList().add(htmlBufferResponseWritter.toString());

            facesContext.setResponseWriter(htmlBufferResponseWritter.getInitialWriter());
```

### DataFlowIssue
Method invocation `stream` may produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/push/cdi/WebsocketSessionManager.java`
#### Snippet
```java
        }
        Optional<Reference<Session>> referenceOptional =
                sessions.stream().filter(p -> Objects.equals(p.get(), session)).findFirst();

        if (!referenceOptional.isPresent())
```

### DataFlowIssue
Method invocation `isOpen` may produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/push/cdi/WebsocketSessionManager.java`
#### Snippet
```java
                        {
                            Session session = sessionRef.get();
                            if (session.isOpen())
                            {
                                send(session, json, results, 0);
```

### DataFlowIssue
Method invocation `isOpen` may produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/push/cdi/WebsocketSessionManager.java`
#### Snippet
```java
                                referenceCollection
                                        .stream()
                                        .filter(p -> p.get() != null && p.get().isOpen())
                                        .distinct()
                                        .collect(Collectors.toCollection(ConcurrentLinkedQueue::new));
```

### DataFlowIssue
Variable is already assigned to this value
in `impl/src/main/java/org/apache/myfaces/util/lang/SkipMatchIterator.java`
#### Snippet
```java
                if (hasNext())
                {
                    value = null;
                    return this.value;
                }
```

### DataFlowIssue
Method invocation `getCurrentFlow` may produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/flow/FlowHandlerImpl.java`
#### Snippet
```java
        {
            // Use the standard form
            Flow curFlow = fh.getCurrentFlow(facesContext);
            if (curFlow != null)
            {
```

### DataFlowIssue
Argument `context` might be null
in `impl/src/main/java/org/apache/myfaces/util/MessageUtils.java`
#### Snippet
```java
            locale = Locale.getDefault();
        }
        return getMessageFromBundle(bundleBaseName, context , locale, messageId, params);
    }

```

### DataFlowIssue
Method invocation `saveState` may produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/view/facelets/FullStateManagementStrategy.java`
#### Snippet
```java
            {
                StateCacheUtils.getMyFacesResponseStateManager(responseStateManager).
                        saveState(facesContext, serializedView);
            }
    
```

### DataFlowIssue
Variable is already assigned to this value
in `impl/src/main/java/org/apache/myfaces/util/lang/StreamCharBuffer.java`
#### Snippet
```java
            else
            {
                spaceLeft = 0;
            }
        }
```

### DataFlowIssue
Variable is already assigned to this value
in `impl/src/main/java/org/apache/myfaces/util/lang/StreamCharBuffer.java`
#### Snippet
```java
                    else
                    {
                        available = 0;
                    }
                }
```

### DataFlowIssue
Variable is already assigned to this value
in `impl/src/main/java/org/apache/myfaces/util/lang/ConcurrentLRUCache.java`
#### Snippet
```java
            long oldestEntry = this.oldestEntry;
            isCleaning = true;
            this.oldestEntry = oldestEntry; // volatile write to make isCleaning visible

            long timeCurrent = stats.accessCounter.get();
```

### DataFlowIssue
Dereference of `otherEntry` may produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/util/lang/ConcurrentLRUCache.java`
#### Snippet
```java
                            CacheEntry otherEntry = (CacheEntry) queue.pop();
                            newOldestEntry = Math
                                    .min(otherEntry.lastAccessedCopy,
                                            newOldestEntry);
                        }
```

### DataFlowIssue
Method invocation `invoke` may produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/RedirectMethodExpressionValueExpressionValueChangeListener.java`
#### Snippet
```java
    {
        ELContext elContext = event.getFacesContext().getELContext();
        getMethodExpression(elContext).invoke(elContext, new Object[]{event});
    }

```

### DataFlowIssue
Method invocation `invoke` may produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/RedirectMethodExpressionValueExpressionValidator.java`
#### Snippet
```java
    {
        ELContext elContext = context.getELContext();
        getMethodExpression(elContext).invoke(elContext, new Object[]{context,component,value});
    }

```

### DataFlowIssue
Method invocation `invoke` may produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/RedirectMethodExpressionValueExpressionActionListener.java`
#### Snippet
```java
    {
        ELContext elContext = actionEvent.getFacesContext().getELContext();
        getMethodExpression(elContext).invoke(elContext, new Object[]{actionEvent});
    }

```

### DataFlowIssue
Method invocation `getChildCount` may produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/view/facelets/ViewPoolProcessor.java`
#### Snippet
```java
                else
                {
                    if (child.getChildCount() > 0 || !child.getFacets().isEmpty())
                    {
                        clearTransientAndNonFaceletComponents(context, child);
```

### DataFlowIssue
Method invocation `getChildCount` may produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/view/facelets/ViewPoolProcessor.java`
#### Snippet
```java
                else
                {
                    if (fc.getChildCount() > 0 || !fc.getFacets().isEmpty())
                    {
                        clearTransientAndNonFaceletComponents(context, fc);
```

### DataFlowIssue
Method invocation `getChildCount` may produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/view/facelets/ViewPoolProcessor.java`
#### Snippet
```java
                else
                {
                    if (child.getChildCount() > 0 || !child.getFacets().isEmpty())
                    {
                        clearTransientAndNonFaceletComponents(context, child);
```

### DataFlowIssue
Argument `in` might be null
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ELText.java`
#### Snippet
```java
        else
        {
            return parse(null, null, in, location);
        }
    }
```

### DataFlowIssue
Method invocation `toString` may produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ELText.java`
#### Snippet
```java
        }

        return parse(fact, ctx, in, null).toString(ctx);
    }

```

### DataFlowIssue
Method invocation `saveState` may produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/view/facelets/PartialStateManagementStrategy.java`
#### Snippet
```java
                {
                    StateCacheUtils.getMyFacesResponseStateManager(responseStateManager).
                            saveState(context, serializedView);
                }
            }
```

### DataFlowIssue
Variable is already assigned to this value
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ComponentSupport.java`
#### Snippet
```java
            if (map == null)
            {
                value = null;
            }
            else
```

### DataFlowIssue
Method invocation `addActionListener` may produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/ResetValuesActionListenerHandler.java`
#### Snippet
```java
                            : null);
        }
        as.addActionListener(listener);
    }

```

### DataFlowIssue
Argument `contracts` might be null
in `impl/src/main/java/org/apache/myfaces/view/facelets/pool/impl/MetadataViewKeyImpl.java`
#### Snippet
```java
    public String[] getContracts()
    {
        return Arrays.copyOf(contracts, contracts.length);
    }

```

### DataFlowIssue
Method invocation `addChild` may produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/CompilationManager.java`
#### Snippet
```java
            if (interfaceCompilationUnit != null)
            {
                this.currentUnit().addChild(interfaceCompilationUnit);
                interfaceCompilationUnit = null;
            }
```

### DataFlowIssue
Method invocation `addMessage` may produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/CompilationManager.java`
#### Snippet
```java
                if (isPrefixed)
                {
                    unit.addMessage(FacesMessage.SEVERITY_WARN,
                            "Warning: The page "+alias+" declares namespace "+qname[0]+ 
                            " and uses the tag " + qName + " , but no TagLibrary associated to namespace.", 
```

### DataFlowIssue
Method invocation `startTag` may produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/CompilationManager.java`
#### Snippet
```java
            }
            
            unit.startTag(t);
        }
    }
```

### DataFlowIssue
Method invocation `write` may produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/CompilationManager.java`
#### Snippet
```java
            this.startUnit(unit);
        }
        unit.write(value);
    }

```

### DataFlowIssue
Method invocation `writeComment` may produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/CompilationManager.java`
#### Snippet
```java
        }

        unit.writeComment(text);
    }

```

### DataFlowIssue
Method invocation `writeInstruction` may produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/CompilationManager.java`
#### Snippet
```java
            this.startUnit(unit);
        }
        unit.writeInstruction(value);
    }
    
```

### DataFlowIssue
Method invocation `setNamespace` may produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/CompilationManager.java`
#### Snippet
```java
            this.startUnit(unit);
        }
        unit.setNamespace(prefix, uri);
    }

```

### DataFlowIssue
Method invocation `addChild` may produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/CompilationManager.java`
#### Snippet
```java
        }

        this.currentUnit().addChild(unit);
        this.units.push(unit);
    }
```

### DataFlowIssue
Method invocation `isEmpty` may produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/view/facelets/component/JsfElementRenderer.java`
#### Snippet
```java
        writer.startElement(elementName, component);

        if (!behaviors.isEmpty())
        {
            HtmlRendererUtils.writeIdAndName(writer, component, facesContext);
```

### DataFlowIssue
Variable is already assigned to this value
in `impl/src/main/java/org/apache/myfaces/config/DefaultFacesConfigurationMerger.java`
#### Snippet
```java
            //assume false if no faces-config.xml was found
            //metadata-complete can only be specified in faces-config.xml per the Faces 2.0 schema
            metadataComplete = false;
        }

```

### DataFlowIssue
Method invocation `isExtensionMapping` may produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/resource/ResourceImpl.java`
#### Snippet
```java
            
            String path = "";
            if (mapping.isExtensionMapping())
            {
                path = _resourceHandlerSupport.getResourceIdentifier() + '/' + 
```

### DataFlowIssue
Dereference of `children` may produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/resource/TempDirFileCacheResourceLoader.java`
#### Snippet
```java
        {
            String[] children = dir.list();
            for (int i = 0; i < children.length; i++)
            {
                boolean success = deleteDir(new File(dir, children[i]));
```

### DataFlowIssue
Method invocation `getClientId` may produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/context/servlet/PartialViewContextImpl.java`
#### Snippet
```java
            try
            {
                writer.startUpdate(target.getClientId(_facesContext));
                inUpdate = true;
                target.encodeAll(_facesContext);
```

### DataFlowIssue
Method invocation `substring` may produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/resource/ResourceMetaImpl.java`
#### Snippet
```java
                }
                builder.append(_resourceVersion);
                builder.append(_resourceName.substring(_resourceName.lastIndexOf('.')));
                firstSlashAdded = true;
            }
```

### DataFlowIssue
Dereference of `children` may produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/resource/TempDirFileCacheContractResourceLoader.java`
#### Snippet
```java
        {
            String[] children = dir.list();
            for (int i = 0; i < children.length; i++)
            {
                boolean success = deleteDir(new File(dir, children[i]));
```

### DataFlowIssue
Argument `list` might be null
in `impl/src/main/java/org/apache/myfaces/resource/ClassLoaderResourceLoaderIterator.java`
#### Snippet
```java
                    {
                        File[] list = file.listFiles();
                        stack.addAll(Arrays.asList(list));
                    }
                    if (!stack.isEmpty())
```

### DataFlowIssue
Argument `list` might be null
in `impl/src/main/java/org/apache/myfaces/resource/ClassLoaderResourceLoaderIterator.java`
#### Snippet
```java
                    {
                        File[] list = file.listFiles();
                        stack.addAll(Arrays.asList(list));
                    }
                    if (!stack.isEmpty())
```

### DataFlowIssue
Variable is already assigned to this value
in `impl/src/main/java/org/apache/myfaces/lifecycle/RestoreViewExecutor.java`
#### Snippet
```java
            else
            {
                servletPathIndex = -1;
                pathInfoIndex = (Math.max(appContextPathIndex, 0)) + appContextPath.length();
            }
```

### DataFlowIssue
Method invocation `length` may produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/lifecycle/RestoreViewExecutor.java`
#### Snippet
```java
            {
                servletPathIndex = -1;
                pathInfoIndex = (Math.max(appContextPathIndex, 0)) + appContextPath.length();
            }

```

### DataFlowIssue
Condition `component instanceof HtmlOutputFormat` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlFormatRenderer.java`
#### Snippet
```java
        String text = getOutputFormatText(facesContext, component);
        boolean escape;
        if (component instanceof HtmlOutputFormat)
        {
            escape = ((HtmlOutputFormat) component).isEscape();
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlFormatRenderer.java`
#### Snippet
```java
        else
        {
            escape = AttributeUtils.getBooleanAttribute(component, ComponentAttrs.ESCAPE_ATTR, true);
        }
        if (text != null)
```

### DataFlowIssue
Condition `component instanceof HtmlOutputLabel` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlLabelRenderer.java`
#### Snippet
```java
    protected String getFor(UIComponent component)
    {
        if (component instanceof HtmlOutputLabel)
        {
            return ((HtmlOutputLabel)component).getFor();
```

### DataFlowIssue
Method invocation `getAttributes` will produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlLabelRenderer.java`
#### Snippet
```java
        }

        return (String) component.getAttributes().get(ComponentAttrs.FOR_ATTR);

    }
```

### DataFlowIssue
Condition `component instanceof HtmlInputText` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTextRendererBase.java`
#### Snippet
```java
    protected boolean isAutocompleteOff(FacesContext facesContext, UIComponent component)
    {
        if (component instanceof HtmlInputText)
        {
            String autocomplete = ((HtmlInputText)component).getAutocomplete();
```

### DataFlowIssue
Condition `input instanceof HtmlInputText` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTextRendererBase.java`
#### Snippet
```java
    protected boolean isValidLength(FacesContext facesContext, UIInput input)
    {
        if (input instanceof HtmlInputText)
        {
            int maxlength = ((HtmlInputText)input).getMaxlength();
```

### DataFlowIssue
Condition `component instanceof HtmlInputText` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTextRendererBase.java`
#### Snippet
```java
    protected boolean isDisabled(FacesContext facesContext, UIComponent component)
    {
        if (component instanceof HtmlInputText)
        {
            return ((HtmlInputText)component).isDisabled();
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTextRendererBase.java`
#### Snippet
```java
        }

        return AttributeUtils.getBooleanAttribute(component, HTML.DISABLED_ATTR, false);
    }

```

### DataFlowIssue
Condition `component instanceof HtmlInputText` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTextRendererBase.java`
#### Snippet
```java
    {
        String type = null;
        if (component instanceof HtmlInputText)
        {
            type = ((HtmlInputText)component).getType();
```

### DataFlowIssue
Condition `component instanceof HtmlMessage` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessageRendererBase.java`
#### Snippet
```java
    protected boolean isTooltip(UIComponent component)
    {
        if (component instanceof HtmlMessage)
        {
            return ((HtmlMessage) component).isTooltip();
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessageRendererBase.java`
#### Snippet
```java
        }

        return AttributeUtils.getBooleanAttribute(component, ComponentAttrs.TOOLTIP_ATTR, false);
    }

```

### DataFlowIssue
Condition `component instanceof UIMessage` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessageRendererBase.java`
#### Snippet
```java
    protected String getFor(UIComponent component)
    {
        if (component instanceof UIMessage)
        {
            return ((UIMessage) component).getFor();
```

### DataFlowIssue
Method invocation `getAttributes` will produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessageRendererBase.java`
#### Snippet
```java
        }
 
        return (String) component.getAttributes().get(ComponentAttrs.FOR_ATTR);
        
    }
```

### DataFlowIssue
Condition `component instanceof UIMessage` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessageRendererBase.java`
#### Snippet
```java
    protected boolean isRedisplay(UIComponent component)
    {
        if (component instanceof UIMessage)
        {
            return ((UIMessage) component).isRedisplay();
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessageRendererBase.java`
#### Snippet
```java
        }

        return AttributeUtils.getBooleanAttribute(component, ComponentAttrs.REDISPLAY_ATTR, true);
    }

```

### DataFlowIssue
Condition `message instanceof HtmlMessage` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessageRendererBase.java`
#### Snippet
```java
        String style = null;
        String styleClass = null;
        if (message instanceof HtmlMessage)
        {
            if (severity == FacesMessage.SEVERITY_INFO)
```

### DataFlowIssue
Method invocation `getAttributes` will produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessageRendererBase.java`
#### Snippet
```java
        else
        {
            Map<String, Object> attr = message.getAttributes();
            if (severity == FacesMessage.SEVERITY_INFO)
            {
```

### DataFlowIssue
Condition `component instanceof HtmlMessage` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessageRendererBase.java`
#### Snippet
```java
    protected String getTitle(UIComponent component)
    {
        if (component instanceof HtmlMessage)
        {
            return ((HtmlMessage) component).getTitle();
```

### DataFlowIssue
Method invocation `getAttributes` will produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessageRendererBase.java`
#### Snippet
```java
        }

        return (String) component.getAttributes().get(ComponentAttrs.TITLE_ATTR);
        
    }
```

### DataFlowIssue
Condition `component instanceof UIMessage` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessageRendererBase.java`
#### Snippet
```java
    protected boolean isShowSummary(UIComponent component)
    {
        if (component instanceof UIMessage)
        {
            return ((UIMessage) component).isShowSummary();
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessageRendererBase.java`
#### Snippet
```java
        }

        return AttributeUtils.getBooleanAttribute(component, ComponentAttrs.SHOW_SUMMARY_ATTR, false);
    }

```

### DataFlowIssue
Condition `component instanceof UIMessage` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessageRendererBase.java`
#### Snippet
```java
    protected boolean isShowDetail(UIComponent component)
    {
        if (component instanceof UIMessage)
        {
            return ((UIMessage) component).isShowDetail();
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessageRendererBase.java`
#### Snippet
```java
        }

        return AttributeUtils.getBooleanAttribute(component, ComponentAttrs.SHOW_DETAIL_ATTR, false);
    }
    
```

### DataFlowIssue
Condition `component instanceof HtmlPanelGrid` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlGridRendererBase.java`
#### Snippet
```java
        String columnClasses;
        String rowClasses;
        if (component instanceof HtmlPanelGrid)
        {
            columnClasses = ((HtmlPanelGrid)component).getColumnClasses();
```

### DataFlowIssue
Method invocation `getAttributes` will produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlGridRendererBase.java`
#### Snippet
```java
        else
        {
            columnClasses = (String)component.getAttributes().get(ComponentAttrs.COLUMN_CLASSES_ATTR);
            rowClasses = (String)component.getAttributes().get(ComponentAttrs.ROW_CLASSES_ATTR);
        }
```

### DataFlowIssue
Condition `component instanceof HtmlPanelGrid` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlGridRendererBase.java`
#### Snippet
```java

        int columns;
        if (component instanceof HtmlPanelGrid)
        {
            columns = ((HtmlPanelGrid)component).getColumns();
```

### DataFlowIssue
Method invocation `getAttributes` will produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlGridRendererBase.java`
#### Snippet
```java
        else
        {
            Integer i = (Integer)component.getAttributes().get(ComponentAttrs.COLUMNS_ATTR);
            columns = i != null ? i : 0;
        }
```

### DataFlowIssue
Method invocation `isEmpty` may produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlGridRendererBase.java`
#### Snippet
```java
        if (component instanceof ClientBehaviorHolder)
        {
            if (!behaviors.isEmpty())
            {
                HtmlRendererUtils.writeIdAndName(writer, component, facesContext);
```

### DataFlowIssue
Condition `component instanceof HtmlSelectBooleanCheckbox` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlCheckboxRendererBase.java`
#### Snippet
```java
    protected boolean isDisabled(FacesContext facesContext, UIComponent component)
    {
        if (component instanceof HtmlSelectBooleanCheckbox)
        {
            return ((HtmlSelectBooleanCheckbox) component).isDisabled();
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlCheckboxRendererBase.java`
#### Snippet
```java
        }

        return AttributeUtils.getBooleanAttribute(component, HTML.DISABLED_ATTR, false);
    }

```

### DataFlowIssue
Condition `selectMany instanceof HtmlSelectManyCheckbox` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlCheckboxRendererBase.java`
#### Snippet
```java
    protected String getLayout(UISelectMany selectMany)
    {
        if (selectMany instanceof HtmlSelectManyCheckbox)
        {
            return ((HtmlSelectManyCheckbox) selectMany).getLayout();
```

### DataFlowIssue
Method invocation `getAttributes` will produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlCheckboxRendererBase.java`
#### Snippet
```java
        } 
        
        return (String) selectMany.getAttributes().get(ComponentAttrs.LAYOUT_ATTR);
    }

```

### DataFlowIssue
Condition `uiComponent instanceof HtmlSelectManyMenu` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMenuRendererBase.java`
#### Snippet
```java
    protected boolean isDisabled(FacesContext facesContext, UIComponent uiComponent)
    {
        if (uiComponent instanceof HtmlSelectManyMenu)
        {
            return ((HtmlSelectManyMenu) uiComponent).isDisabled();
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMenuRendererBase.java`
#### Snippet
```java
        }

        return AttributeUtils.getBooleanAttribute(uiComponent, HTML.DISABLED_ATTR, false);
    }

```

### DataFlowIssue
Condition `uiComponent instanceof HtmlSelectManyListbox` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlListboxRendererBase.java`
#### Snippet
```java
    protected boolean isDisabled(FacesContext facesContext, UIComponent uiComponent)
    {
        if (uiComponent instanceof HtmlSelectManyListbox)
        {
            return ((HtmlSelectManyListbox)uiComponent).isDisabled();
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlListboxRendererBase.java`
#### Snippet
```java
        }

        return AttributeUtils.getBooleanAttribute(uiComponent, HTML.DISABLED_ATTR, false);
    }

```

### DataFlowIssue
Condition `uiComponent instanceof HtmlInputSecret` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlSecretRendererBase.java`
#### Snippet
```java
    protected boolean isDisabled(FacesContext facesContext, UIComponent uiComponent)
    {
        if (uiComponent instanceof HtmlInputSecret)
        {
            return ((HtmlInputSecret)uiComponent).isDisabled();
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlSecretRendererBase.java`
#### Snippet
```java
        }

        return AttributeUtils.getBooleanAttribute(uiComponent, HTML.DISABLED_ATTR, false);
    }

```

### DataFlowIssue
Condition `component instanceof HtmlInputSecret` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlSecretRendererBase.java`
#### Snippet
```java
    protected boolean isAutocompleteOff(FacesContext facesContext, UIComponent component)
    {
        if (component instanceof HtmlInputSecret)
        {
            String autocomplete = ((HtmlInputSecret)component).getAutocomplete();
```

### DataFlowIssue
Condition `component instanceof HtmlMessages` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessagesRendererBase.java`
#### Snippet
```java
    protected String getLayout(UIComponent component)
    {
        if (component instanceof HtmlMessages)
        {
            return ((HtmlMessages)component).getLayout();
```

### DataFlowIssue
Method invocation `getAttributes` will produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessagesRendererBase.java`
#### Snippet
```java
        else
        {
            return (String)component.getAttributes().get(ComponentAttrs.LAYOUT_ATTR);
        }
    }
```

### DataFlowIssue
Condition `messages instanceof HtmlMessages` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessagesRendererBase.java`
#### Snippet
```java
    {
        String style = null;
        if (messages instanceof HtmlMessages)
        {
            style = ((HtmlMessages)messages).getStyle();
```

### DataFlowIssue
Method invocation `getAttributes` will produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessagesRendererBase.java`
#### Snippet
```java
        else
        {
            style = (String) messages.getAttributes().get(ComponentAttrs.STYLE_ATTR);
        }
        return style;
```

### DataFlowIssue
Condition `component instanceof UIMessages` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessagesRendererBase.java`
#### Snippet
```java
    protected boolean isRedisplay(UIComponent component)
    {
        if (component instanceof UIMessages)
        {
            return ((UIMessages) component).isRedisplay();
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessagesRendererBase.java`
#### Snippet
```java
        }

        return AttributeUtils.getBooleanAttribute(component, ComponentAttrs.REDISPLAY_ATTR, true);
        
    }
```

### DataFlowIssue
Condition `component instanceof UIMessages` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessagesRendererBase.java`
#### Snippet
```java
    protected String getFor(UIComponent component)
    {
        if (component instanceof UIMessages)
        {
            return ((UIMessages) component).getFor();
```

### DataFlowIssue
Method invocation `getAttributes` will produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessagesRendererBase.java`
#### Snippet
```java
        }
 
        return (String) component.getAttributes().get(ComponentAttrs.FOR_ATTR); 
    }

```

### DataFlowIssue
Condition `component instanceof HtmlMessages` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessagesRendererBase.java`
#### Snippet
```java
    protected boolean isTooltip(UIComponent component)
    {
        if (component instanceof HtmlMessages)
        {
            return ((HtmlMessages)component).isTooltip();
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessagesRendererBase.java`
#### Snippet
```java
        else
        {
            return AttributeUtils.getBooleanAttribute(component, ComponentAttrs.TOOLTIP_ATTR, false);
        }
    }
```

### DataFlowIssue
Condition `messages instanceof HtmlMessages` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessagesRendererBase.java`
#### Snippet
```java
    {
        String styleClass = null;
        if (messages instanceof HtmlMessages)
        {
            styleClass = ((HtmlMessages)messages).getStyleClass();
```

### DataFlowIssue
Method invocation `getAttributes` will produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessagesRendererBase.java`
#### Snippet
```java
        else
        {
            styleClass = (String) messages.getAttributes().get(ComponentAttrs.STYLE_CLASS_ATTR);
        }
        return styleClass;
```

### DataFlowIssue
Condition `component instanceof HtmlMessages` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessagesRendererBase.java`
#### Snippet
```java
    protected String getTitle(UIComponent component)
    {
        if (component instanceof HtmlMessages)
        {
            return ((HtmlMessages)component).getTitle();
```

### DataFlowIssue
Method invocation `getAttributes` will produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessagesRendererBase.java`
#### Snippet
```java
        else
        {
            return (String)component.getAttributes().get(ComponentAttrs.TITLE_ATTR);
        }
    }
```

### DataFlowIssue
Condition `component instanceof UIMessages` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessagesRendererBase.java`
#### Snippet
```java
    protected boolean isShowSummary(UIComponent component)
    {
        if (component instanceof UIMessages)
        {
            return ((UIMessages)component).isShowSummary();
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessagesRendererBase.java`
#### Snippet
```java
        else
        {
            return AttributeUtils.getBooleanAttribute(component, ComponentAttrs.SHOW_SUMMARY_ATTR, false);
        }
    }
```

### DataFlowIssue
Condition `messages instanceof HtmlMessages` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessagesRendererBase.java`
#### Snippet
```java
        String style = null;
        String styleClass = null;
        if (messages instanceof HtmlMessages)
        {
            if (severity == FacesMessage.SEVERITY_INFO)
```

### DataFlowIssue
Method invocation `getAttributes` will produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessagesRendererBase.java`
#### Snippet
```java
        else
        {
            Map attr = messages.getAttributes();
            if (severity == FacesMessage.SEVERITY_INFO)
            {
```

### DataFlowIssue
Condition `component instanceof UIMessages` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessagesRendererBase.java`
#### Snippet
```java
    protected boolean isGlobalOnly(UIComponent component)
    {
        if (component instanceof UIMessages)
        {
            return ((UIMessages)component).isGlobalOnly();
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessagesRendererBase.java`
#### Snippet
```java
        else
        {
            return AttributeUtils.getBooleanAttribute(component, ComponentAttrs.GLOBAL_ONLY_ATTR, false);
        }
    }
```

### DataFlowIssue
Condition `component instanceof UIMessages` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessagesRendererBase.java`
#### Snippet
```java
    protected boolean isShowDetail(UIComponent component)
    {
        if (component instanceof UIMessages)
        {
            return ((UIMessages)component).isShowDetail();
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlMessagesRendererBase.java`
#### Snippet
```java
        else
        {
            return AttributeUtils.getBooleanAttribute(component, ComponentAttrs.SHOW_DETAIL_ATTR, false);
        }
    }
```

### DataFlowIssue
Method invocation `isEmpty` may produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlGroupRendererBase.java`
#### Snippet
```java
        }

        if (!behaviors.isEmpty() || shouldRenderId(context, component))
        {
            span = true;
```

### DataFlowIssue
Condition `uiComponent instanceof HtmlOutcomeTargetButton` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlOutcomeTargetButtonRendererBase.java`
#### Snippet
```java
    private String getImage(UIComponent uiComponent)
    {
        if (uiComponent instanceof HtmlOutcomeTargetButton)
        {
            return ((HtmlOutcomeTargetButton) uiComponent).getImage();
```

### DataFlowIssue
Method invocation `getAttributes` will produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlOutcomeTargetButtonRendererBase.java`
#### Snippet
```java
            return ((HtmlOutcomeTargetButton) uiComponent).getImage();
        }
        return (String) uiComponent.getAttributes().get(ComponentAttrs.IMAGE_ATTR);
    }

```

### DataFlowIssue
Condition `uiComponent instanceof HtmlInputTextarea` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTextareaRendererBase.java`
#### Snippet
```java
    protected boolean isDisabled(FacesContext facesContext, UIComponent uiComponent)
    {
        if (uiComponent instanceof HtmlInputTextarea)
        {
            return ((HtmlInputTextarea)uiComponent).isDisabled();
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTextareaRendererBase.java`
#### Snippet
```java
        }

        return AttributeUtils.getBooleanAttribute(uiComponent, HTML.DISABLED_ATTR, false);
    }

```

### DataFlowIssue
Condition `selectItemInfo.getComponent() instanceof UISelectItems` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/SelectItemsUtils.java`
#### Snippet
```java
                Object oldRequestMapVarValue = null;
                String var = null;
                if (selectItemInfo != null && selectItemInfo.getComponent() instanceof UISelectItems)
                {
                    var = (String) selectItemInfo.getComponent().getAttributes().get(ComponentAttrs.VAR_ATTR);
```

### DataFlowIssue
Condition `component instanceof HtmlCommandLink` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlLinkRendererBase.java`
#### Snippet
```java

        String commandOnclick;
        if (component instanceof HtmlCommandLink)
        {
            commandOnclick = ((HtmlCommandLink)component).getOnclick();
```

### DataFlowIssue
Method invocation `getAttributes` will produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlLinkRendererBase.java`
#### Snippet
```java
        else
        {
            commandOnclick = (String)component.getAttributes().get(HTML.ONCLICK_ATTR);
        }
        if (commandOnclick != null)
```

### DataFlowIssue
Condition `component instanceof HtmlCommandLink` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlLinkRendererBase.java`
#### Snippet
```java
        // for performance reason: double check for the target attribute
        String target;
        if (component instanceof HtmlCommandLink)
        {
            target = ((HtmlCommandLink) component).getTarget();
```

### DataFlowIssue
Method invocation `getAttributes` will produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlLinkRendererBase.java`
#### Snippet
```java
        else
        {
            target = (String) component.getAttributes().get(HTML.TARGET_ATTR);
        }
        return target;
```

### DataFlowIssue
Condition `link instanceof HtmlCommandLink` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlLinkRendererBase.java`
#### Snippet
```java
    protected String getStyle(FacesContext facesContext, UIComponent link)
    {
        if (link instanceof HtmlCommandLink)
        {
            return ((HtmlCommandLink)link).getStyle();
```

### DataFlowIssue
Method invocation `getAttributes` will produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlLinkRendererBase.java`
#### Snippet
```java
        }

        return (String)link.getAttributes().get(HTML.STYLE_ATTR);

    }
```

### DataFlowIssue
Condition `link instanceof HtmlCommandLink` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlLinkRendererBase.java`
#### Snippet
```java
    protected String getStyleClass(FacesContext facesContext, UIComponent link)
    {
        if (link instanceof HtmlCommandLink)
        {
            return ((HtmlCommandLink)link).getStyleClass();
```

### DataFlowIssue
Method invocation `getAttributes` will produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlLinkRendererBase.java`
#### Snippet
```java
        }

        return (String)link.getAttributes().get(HTML.STYLE_CLASS_ATTR);

    }
```

### DataFlowIssue
Condition `component instanceof HtmlCommandLink` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlLinkRendererBase.java`
#### Snippet
```java
    {
        String commandOnclick;
        if (component instanceof HtmlCommandLink)
        {
            commandOnclick = ((HtmlCommandLink)component).getOnclick();
```

### DataFlowIssue
Method invocation `getAttributes` will produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlLinkRendererBase.java`
#### Snippet
```java
        else
        {
            commandOnclick = (String)component.getAttributes().get(HTML.ONCLICK_ATTR);
        }

```

### DataFlowIssue
Condition `selectOne instanceof HtmlSelectOneRadio` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlRadioRendererBase.java`
#### Snippet
```java
        }
        
        String group = selectOne instanceof HtmlSelectOneRadio ? ((HtmlSelectOneRadio) selectOne).getGroup() : null;
        if (group != null && !group.isEmpty())
        {
```

### DataFlowIssue
Method invocation `visitTree` may produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlRadioRendererBase.java`
#### Snippet
```java
                UIForm form = ComponentUtils.findClosest(UIForm.class, uiComponent);
                GetSelectItemListCallback callback = new GetSelectItemListCallback(selectOne, group);
                form.visitTree(
                        VisitContext.createVisitContext(facesContext, null, FIND_SELECT_LIST_HINTS),
                        callback);                
```

### DataFlowIssue
Method invocation `getClientId` may produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlRadioRendererBase.java`
#### Snippet
```java
        {
            UIForm form = ComponentUtils.findClosest(UIForm.class, uiComponent);
            writer.writeAttribute(HTML.NAME_ATTR, form.getClientId(facesContext) +
                    facesContext.getNamingContainerSeparatorChar() + group, null);
        }
```

### DataFlowIssue
Condition `selectOne instanceof HtmlSelectOneRadio` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlRadioRendererBase.java`
#### Snippet
```java
    protected String getStyleClass(UISelectOne selectOne)
     {
         if (selectOne instanceof HtmlSelectOneRadio)
         {
             return ((HtmlSelectOneRadio)selectOne).getStyleClass();
```

### DataFlowIssue
Method invocation `getAttributes` will produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlRadioRendererBase.java`
#### Snippet
```java
         }

         return (String)selectOne.getAttributes().get(ComponentAttrs.STYLE_CLASS_ATTR);
     }

```

### DataFlowIssue
Condition `uiComponent instanceof HtmlSelectOneRadio` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlRadioRendererBase.java`
#### Snippet
```java
    protected boolean isDisabled(FacesContext facesContext, UIComponent uiComponent)
    {
        if (uiComponent instanceof HtmlSelectOneRadio)
        {
            return ((HtmlSelectOneRadio)uiComponent).isDisabled();
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlRadioRendererBase.java`
#### Snippet
```java
        }

        return AttributeUtils.getBooleanAttribute(uiComponent, HTML.DISABLED_ATTR, false);
    }

```

### DataFlowIssue
Condition `selectOne instanceof HtmlSelectOneRadio` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlRadioRendererBase.java`
#### Snippet
```java
    protected String getLayout(UIComponent selectOne)
    {
        if (selectOne instanceof HtmlSelectOneRadio)
        {
            return ((HtmlSelectOneRadio)selectOne).getLayout();
```

### DataFlowIssue
Method invocation `getAttributes` will produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlRadioRendererBase.java`
#### Snippet
```java
        }

        return (String)selectOne.getAttributes().get(ComponentAttrs.LAYOUT_ATTR);
    }

```

### DataFlowIssue
Condition `uiData instanceof HtmlDataTable` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTableRendererBase.java`
#### Snippet
```java
    {
        String rowClass = null;
        if (uiData instanceof HtmlDataTable)
        {
            rowClass = ((HtmlDataTable) uiData).getRowClass();
```

### DataFlowIssue
Condition `component instanceof HtmlDataTable` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTableRendererBase.java`
#### Snippet
```java
    protected static String getHeaderClass(UIComponent component)
    {
        if (component instanceof HtmlDataTable)
        {
            return ((HtmlDataTable) component).getHeaderClass();
```

### DataFlowIssue
Method invocation `getAttributes` will produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTableRendererBase.java`
#### Snippet
```java
        else
        {
            return (String) component.getAttributes().get(ComponentAttrs.HEADER_CLASS_ATTR);
        }
    }
```

### DataFlowIssue
Condition `component instanceof HtmlDataTable` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTableRendererBase.java`
#### Snippet
```java
    protected static String getFooterClass(UIComponent component)
    {
        if (component instanceof HtmlDataTable)
        {
            return ((HtmlDataTable) component).getFooterClass();
```

### DataFlowIssue
Method invocation `getAttributes` will produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTableRendererBase.java`
#### Snippet
```java
        else
        {
            return (String) component.getAttributes().get(ComponentAttrs.FOOTER_CLASS_ATTR);
        }
    }
```

### DataFlowIssue
Condition `uiData instanceof HtmlDataTable` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTableRendererBase.java`
#### Snippet
```java
        String rowClasses;
        String columnClasses;
        if(uiData instanceof HtmlDataTable) 
        {
            rowClasses = ((HtmlDataTable)uiData).getRowClasses();
```

### DataFlowIssue
Method invocation `getAttributes` will produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTableRendererBase.java`
#### Snippet
```java
        else
        {
            rowClasses = (String)uiData.getAttributes().get(ComponentAttrs.ROW_CLASSES_ATTR);
            columnClasses = (String)uiData.getAttributes().get(ComponentAttrs.COLUMN_CLASSES_ATTR);
        }
```

### DataFlowIssue
Condition `uiComponent instanceof HtmlCommandButton` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlButtonRendererBase.java`
#### Snippet
```java
    protected boolean isReadonly(FacesContext facesContext, UIComponent uiComponent)
    {
        if (uiComponent instanceof HtmlCommandButton)
        {
            return ((HtmlCommandButton)uiComponent).isReadonly();
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlButtonRendererBase.java`
#### Snippet
```java
        }
        return AttributeUtils.getBooleanAttribute(
                uiComponent, HTML.READONLY_ATTR, false);
    }

```

### DataFlowIssue
Condition `uiComponent instanceof HtmlCommandButton` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlButtonRendererBase.java`
#### Snippet
```java
    protected boolean isDisabled(FacesContext facesContext, UIComponent uiComponent)
    {
        if (uiComponent instanceof HtmlCommandButton)
        {
            return ((HtmlCommandButton) uiComponent).isDisabled();
```

### DataFlowIssue
Passing `null` argument to parameter annotated as @NotNull
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlButtonRendererBase.java`
#### Snippet
```java
        }

        return AttributeUtils.getBooleanAttribute(uiComponent, HTML.DISABLED_ATTR, false);
    }

```

### DataFlowIssue
Condition `component instanceof HtmlCommandLink` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlButtonRendererBase.java`
#### Snippet
```java
        // for performance reason: double check for the target attribute
        String target;
        if (component instanceof HtmlCommandLink)
        {
            target = ((HtmlCommandLink) component).getTarget();
```

### DataFlowIssue
Method invocation `getAttributes` will produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlButtonRendererBase.java`
#### Snippet
```java
        else
        {
            target = (String) component.getAttributes().get(HTML.TARGET_ATTR);
        }
        return target;
```

### DataFlowIssue
Condition `uiComponent instanceof HtmlCommandButton` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlButtonRendererBase.java`
#### Snippet
```java
    private String getType(UIComponent uiComponent)
    {
        if (uiComponent instanceof HtmlCommandButton)
        {
            return ((HtmlCommandButton)uiComponent).getType();
```

### DataFlowIssue
Method invocation `getAttributes` will produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlButtonRendererBase.java`
#### Snippet
```java
            return ((HtmlCommandButton)uiComponent).getType();
        }
        return (String)uiComponent.getAttributes().get(ComponentAttrs.TYPE_ATTR);
    }

```

### DataFlowIssue
Condition `child instanceof UIParameter` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/OutcomeTargetUtils.java`
#### Snippet
```java
            {
                UIComponent child = children.get(i);
                if (child instanceof UIParameter)
                {
                    UIParameter param = (UIParameter) child;
```

### DataFlowIssue
Condition `component instanceof HtmlCommandScript` is redundant and can be replaced with a null check
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/AjaxScriptBuilder.java`
#### Snippet
```java
    {
    // CHECKSTYLE:ON
        HtmlCommandScript commandScript = (component instanceof HtmlCommandScript)
                ? (HtmlCommandScript) component
                : null;
```

### DataFlowIssue
Method invocation `isWriteStateAfterRenderViewRequired` may produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/application/ViewHandlerImpl.java`
#### Snippet
```java
        {
            if (StateCacheUtils.getMyFacesResponseStateManager(responseStateManager).
                    isWriteStateAfterRenderViewRequired(context))
            {
                // Only write state marker if javascript view state is disabled
```

### DataFlowIssue
Method invocation `isWriteStateAfterRenderViewRequired` may produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/application/ViewHandlerImpl.java`
#### Snippet
```java
            if (StateCacheUtils.isMyFacesResponseStateManager(rsm))
            {
                if (StateCacheUtils.getMyFacesResponseStateManager(rsm).isWriteStateAfterRenderViewRequired(context))
                {
                    stateWriter.writingState();
```

### DataFlowIssue
Method invocation `getTargetFlows` may produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/application/NavigationHandlerImpl.java`
#### Snippet
```java
        if (navigationContext != null
                && navigationContext.getSourceFlows() != null
                || (navigationContext.getTargetFlows() != null && !navigationContext.getTargetFlows().isEmpty()))
        {
            FlowHandler flowHandler = facesContext.getApplication().getFlowHandler();
```

### DataFlowIssue
Casting `object` to `Boolean` may produce `ClassCastException`
in `test/src/main/java/org/apache/myfaces/test/el/MockExpressionFactory.java`
#### Snippet
```java
                    || (object.getClass() == Boolean.TYPE))
            {
                return (Boolean) object;
            }
            else if (object instanceof String)
```

### DataFlowIssue
Casting `object` to `Character` may produce `ClassCastException`
in `test/src/main/java/org/apache/myfaces/test/el/MockExpressionFactory.java`
#### Snippet
```java
                    || (object.getClass() == Character.TYPE))
            {
                return (Character) object;
            }
            throw new IllegalArgumentException("Cannot convert " + object
```

### DataFlowIssue
Method invocation `getClass` may produce `NullPointerException`
in `test/src/main/java/org/apache/myfaces/test/el/MockMethodExpression.java`
#### Snippet
```java
        try
        {
            Method method = base.getClass().getMethod(
                    elements[elements.length - 1], signature);
            Object result = method.invoke(base, params);
```

### DataFlowIssue
Method invocation `getAnnotation` may produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/application/ApplicationImpl.java`
#### Snippet
```java
                if (!_cdiManagedConverterMap.containsKey(converterClass))
                {
                    FacesConverter annotation = converterClass.getAnnotation(FacesConverter.class);
                    if (annotation != null && annotation.managed())
                    {
```

### DataFlowIssue
Argument `converterClass` might be null
in `impl/src/main/java/org/apache/myfaces/application/ApplicationImpl.java`
#### Snippet
```java
                    converter = new FacesConverterCDIWrapper(converterClass, targetClass, null);
                    
                    setConverterProperties(converterClass, ((FacesWrapper<Converter>)converter).getWrapped());
                }
                else
```

### DataFlowIssue
Method invocation `getConstructor` may produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/application/ApplicationImpl.java`
#### Snippet
```java
                            // See Faces 1.2 javadoc for Converter
                            Constructor<? extends Converter> constructor = converterClass
                                    .getConstructor(new Class[] { Class.class });

                            converter = constructor.newInstance(new Object[] { targetClass });
```

### DataFlowIssue
Argument `converterClass` might be null
in `impl/src/main/java/org/apache/myfaces/application/ApplicationImpl.java`
#### Snippet
```java
                    {
                        // use no-arg constructor
                        converter = createConverterInstance(converterClass);
                    }
                    
```

### DataFlowIssue
Method invocation `getName` may produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/application/ApplicationImpl.java`
#### Snippet
```java
            synchronized(_defaultValidatorsIds)
            {
                _defaultValidatorsIds.put(validatorId, validatorClass.getName());
                _cachedDefaultValidatorsIds = null;
            }
```

### DataFlowIssue
Variable is already assigned to this value
in `test/src/main/java/org/apache/myfaces/test/mock/MockExternalContext10.java`
#### Snippet
```java
    {
        Integer port = 8080;
        port = (port == 0) ? null : port;
        if (port != null && 
            !port.equals(request.getServerPort()))
```

### DataFlowIssue
Method invocation `toString` may produce `NullPointerException`
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication20.java`
#### Snippet
```java
            if (name != null && name.length() > 0)
            {
                name = _ELText.parse(getExpressionFactory(), context.getELContext(), name).toString(
                    context.getELContext());
            }
```

### DataFlowIssue
Method invocation `toString` may produce `NullPointerException`
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication20.java`
#### Snippet
```java
            if (library != null && library.length() > 0)
            {
                library = _ELText.parse(getExpressionFactory(), context.getELContext(), library).toString(
                    context.getELContext());
                // If library is non-null, store it under the key "library".
```

### DataFlowIssue
Method invocation `toString` may produce `NullPointerException`
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication20.java`
#### Snippet
```java
            if (target != null && target.length() > 0)
            {
                target = _ELText.parse(getExpressionFactory(), context.getELContext(), target).toString(
                    context.getELContext());
                // If target is non-null, store it under the key "target".
```

### DataFlowIssue
Method invocation `toString` may produce `NullPointerException`
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication20.java`
#### Snippet
```java
            if (name != null && name.length() > 0)
            {
                name = _ELText.parse(getExpressionFactory(), context.getELContext(), name).toString(
                    context.getELContext());
            }
```

### DataFlowIssue
Method invocation `toString` may produce `NullPointerException`
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication20.java`
#### Snippet
```java
            if (library != null && library.length() > 0)
            {
                library = _ELText.parse(getExpressionFactory(), context.getELContext(), library).toString(
                    context.getELContext());
                // If library is non-null, store it under the key "library".
```

### DataFlowIssue
Method invocation `toString` may produce `NullPointerException`
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication20.java`
#### Snippet
```java
            if (target != null && target.length() > 0)
            {
                target = _ELText.parse(getExpressionFactory(), context.getELContext(), target).toString(
                    context.getELContext());
                // If target is non-null, store it under the key "target".
```

### DataFlowIssue
Method invocation `substring` may produce `NullPointerException`
in `test/src/main/java/org/apache/myfaces/test/mock/resource/MockResourceMeta.java`
#### Snippet
```java
            }
            builder.append(_resourceVersion);
            builder.append(_resourceName.substring(_resourceName
                    .lastIndexOf('.')));
            firstSlashAdded = true;
```

### DataFlowIssue
Method invocation `substring` may produce `NullPointerException`
in `test/src/main/java/org/apache/myfaces/test/mock/resource/MockSimpleResource.java`
#### Snippet
```java
            }
            builder.append(_resourceVersion);
            builder.append(_resourceName.substring(_resourceName
                    .lastIndexOf('.')));
        }
```

### DataFlowIssue
Variable is already assigned to this value
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/HtmlRendererUtils.java`
#### Snippet
```java
            if (uiComponent instanceof UISelectMany)
            {
                isSelectOne = false;
                selectItemList = RendererUtils.getSelectItemList((UISelectMany) uiComponent, facesContext);
                converter = findUISelectManyConverterFailsafe(facesContext, uiComponent, considerValueType);
```

### DataFlowIssue
Argument `selectItemList` might be null
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/HtmlRendererUtils.java`
#### Snippet
```java

            renderSelectOptionsAsText(facesContext, uiComponent, converter,
                    lookupSet, selectItemList, isSelectOne);

            // bug #970747: force separate end tag
```

### DataFlowIssue
Method invocation `get` may produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/HtmlRendererUtils.java`
#### Snippet
```java
                return renderHTMLStringAttribute(
                        writer, componentProperty, htmlAttrName,
                        cb2.get(0).getScript(ClientBehaviorContext
                                        .createClientBehaviorContext(
                                                facesContext, component,
```

### DataFlowIssue
Method invocation `getClientId` may produce `NullPointerException`
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/HtmlRendererUtils.java`
#### Snippet
```java
            {
                UIForm form = ComponentUtils.findClosest(UIForm.class, component);
                String fullGroupId = form.getClientId(facesContext) +
                        facesContext.getNamingContainerSeparatorChar() + group;
                if (paramMap.containsKey(fullGroupId))
```

## RuleId[ruleID=ParameterCanBeLocal]
### ParameterCanBeLocal
Parameter can be converted to a local variable
in `impl/src/main/java/org/apache/myfaces/spi/impl/NoInjectionAnnotationInjectionProvider.java`
#### Snippet
```java
    
    private static Map<Class,Method[]> createDeclaredMethodBeansMap(
            ClassLoader cl, Map<Class,Method[]> metadata)
    {
        metadata = (Map<Class,Method[]>) declaredMethodBeans.get(cl);
```

## RuleId[ruleID=DeprecatedIsStillUsed]
### DeprecatedIsStillUsed
Deprecated member 'FaceletCacheFactory' is still used
in `api/src/main/java/jakarta/faces/view/facelets/FaceletCacheFactory.java`
#### Snippet
```java

    @Deprecated
    public FaceletCacheFactory()
    {
    }
```

### DeprecatedIsStillUsed
Deprecated member 'TagHandlerDelegateFactory' is still used
in `api/src/main/java/jakarta/faces/view/facelets/TagHandlerDelegateFactory.java`
#### Snippet
```java

    @Deprecated
    public TagHandlerDelegateFactory()
    {
    }
```

### DeprecatedIsStillUsed
Deprecated member 'RendererWrapper' is still used
in `api/src/main/java/jakarta/faces/render/RendererWrapper.java`
#### Snippet
```java

    @Deprecated
    public RendererWrapper()
    {
    }
```

### DeprecatedIsStillUsed
Deprecated member 'ExceptionHandlerFactory' is still used
in `api/src/main/java/jakarta/faces/context/ExceptionHandlerFactory.java`
#### Snippet
```java

    @Deprecated
    public ExceptionHandlerFactory()
    {
    }
```

### DeprecatedIsStillUsed
Deprecated member 'ExceptionHandlerWrapper' is still used
in `api/src/main/java/jakarta/faces/context/ExceptionHandlerWrapper.java`
#### Snippet
```java

    @Deprecated
    public ExceptionHandlerWrapper()
    {
    }
```

### DeprecatedIsStillUsed
Deprecated member 'FlashFactory' is still used
in `api/src/main/java/jakarta/faces/context/FlashFactory.java`
#### Snippet
```java

    @Deprecated
    public FlashFactory()
    {
    }
```

### DeprecatedIsStillUsed
Deprecated member 'ExternalContextFactory' is still used
in `api/src/main/java/jakarta/faces/context/ExternalContextFactory.java`
#### Snippet
```java

    @Deprecated
    public ExternalContextFactory()
    {
    }
```

### DeprecatedIsStillUsed
Deprecated member 'PartialViewContextFactory' is still used
in `api/src/main/java/jakarta/faces/context/PartialViewContextFactory.java`
#### Snippet
```java

    @Deprecated
    public PartialViewContextFactory()
    {
    }
```

### DeprecatedIsStillUsed
Deprecated member 'FacesContextWrapper' is still used
in `api/src/main/java/jakarta/faces/context/FacesContextWrapper.java`
#### Snippet
```java

    @Deprecated
    public FacesContextWrapper()
    {
    }
```

### DeprecatedIsStillUsed
Deprecated member 'ClientWindowFactory' is still used
in `api/src/main/java/jakarta/faces/lifecycle/ClientWindowFactory.java`
#### Snippet
```java

    @Deprecated
    public ClientWindowFactory()
    {
    }
```

### DeprecatedIsStillUsed
Deprecated member 'NavigationCaseWrapper' is still used
in `api/src/main/java/jakarta/faces/application/NavigationCaseWrapper.java`
#### Snippet
```java
    
    @Deprecated
    public NavigationCaseWrapper()
    {
        super(null, null, null, null, null, null, false, false);
```

### DeprecatedIsStillUsed
Deprecated member 'ApplicationWrapper' is still used
in `api/src/main/java/jakarta/faces/application/ApplicationWrapper.java`
#### Snippet
```java

    @Deprecated
    public ApplicationWrapper()
    {
        
```

### DeprecatedIsStillUsed
Deprecated member 'ConvertDateTimeHandler' is still used
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/ConvertDateTimeHandler.java`
#### Snippet
```java
        bodyContent = "empty", 
        converterClass="jakarta.faces.convert.DateTimeConverter")
public final class ConvertDateTimeHandler extends ConverterHandler
{
    private final TagAttribute dateStyle;
```

### DeprecatedIsStillUsed
Deprecated member 'DEFAULT_SUFFIX_PARAM_NAME' is still used
in `api/src/main/java/jakarta/faces/application/ViewHandler.java`
#### Snippet
```java
    @JSFWebConfigParam(defaultValue=".xhtml", since="1.1", group="viewhandler", deprecated = true)
    @Deprecated(since = "4.0")
    public static final String DEFAULT_SUFFIX_PARAM_NAME = "jakarta.faces.DEFAULT_SUFFIX";
    
    /**
```

### DeprecatedIsStillUsed
Deprecated member 'DEFAULT_SUFFIX' is still used
in `api/src/main/java/jakarta/faces/application/ViewHandler.java`
#### Snippet
```java
    public static final String DEFAULT_FACELETS_SUFFIX = ".xhtml";
    @Deprecated(since = "4.0")
    public static final String DEFAULT_SUFFIX = ".xhtml";
    
    /**
```

## RuleId[ruleID=Convert2MethodRef]
### Convert2MethodRef
Lambda can be replaced with method reference
in `api/src/main/java/jakarta/faces/FactoryFinder.java`
#### Snippet
```java
            {
                classLoader = (ClassLoader) AccessController.doPrivileged(
                        (PrivilegedExceptionAction) () -> FactoryFinder.class.getClassLoader());
            }
            else
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `impl/src/main/java/org/apache/myfaces/el/ELResolverBuilder.java`
#### Snippet
```java

        return resolvers.stream()
                .map(r -> new TracingELResolverWrapper(r))
                .collect(Collectors.toList()); 
    }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `impl/src/main/java/org/apache/myfaces/spi/SerialFactory.java`
#### Snippet
```java
                if (System.getSecurityManager() != null)
                {
                    return AccessController.doPrivileged((PrivilegedExceptionAction) () -> ois.readObject());
                }

```

### Convert2MethodRef
Lambda can be replaced with method reference
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguage.java`
#### Snippet
```java
            Arrays.binarySearch(options, ViewVisitOption.RETURN_AS_MINIMAL_IMPLICIT_OUTCOME) >= 0)
        {
            stream = stream.map(f -> strategy.getMinimalImplicitOutcome(f));
        }
        return stream;
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFaceletFactory.java`
#### Snippet
```java
        _faceletCache = (FaceletCache<Facelet>) cacheFactory.getFaceletCache();
        
        FaceletCache.MemberFactory<Facelet> faceletFactory = (URL url) -> _createFacelet(url);
        FaceletCache.MemberFactory<Facelet> viewMetadataFaceletFactory = (URL url) -> _createViewMetadataFacelet(url);
        
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFaceletFactory.java`
#### Snippet
```java
        
        FaceletCache.MemberFactory<Facelet> faceletFactory = (URL url) -> _createFacelet(url);
        FaceletCache.MemberFactory<Facelet> viewMetadataFaceletFactory = (URL url) -> _createViewMetadataFacelet(url);
        
        if (_faceletCache instanceof AbstractFaceletCache)
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFaceletFactory.java`
#### Snippet
```java
            
            FaceletCache.MemberFactory<Facelet> compositeComponentMetadataFaceletFactory = 
                (URL url) -> _createCompositeComponentMetadataFacelet(url);

            try
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/FaceletCompositionContextImpl.java`
#### Snippet
```java
        _relocatableResourceForDeletion = new HashMap<>();
        _deletionLevel = -1;
        _sectionUniqueIdCounter = new Lazy(() -> new SectionUniqueIdCounter());
        //Cached at facelet view
        myfacesConfig = MyfacesConfig.getCurrentInstance(facesContext);
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `impl/src/main/java/org/apache/myfaces/view/facelets/util/Classpath.java`
#### Snippet
```java
        {
            final File finalDir = dir;
            dirExists = (Boolean) AccessController.doPrivileged((PrivilegedAction) () -> finalDir.exists());
        }  
        else
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        FacesFlowDefinitionImpl obj = new FacesFlowDefinitionImpl();
        
        onAttribute("id", node, (v) -> { obj.setId(v); });
        onChild("start-node", node, (n) -> { obj.setStartNode(getTextContent(n)); });
        onChild("initializer", node, (n) -> { obj.setInitializer(getTextContent(n)); });
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onChild("view", node, (n) -> {
            FacesFlowViewImpl ffv = new FacesFlowViewImpl();
            onAttribute("id", n, (v) -> { ffv.setId(v); });
            onChild("vdl-document", n, (cn) -> { ffv.setVdlDocument(getTextContent(cn)); });
            obj.addView(ffv);
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onChild("switch", node, (n) -> {
            FacesFlowSwitchImpl ffs = new FacesFlowSwitchImpl();
            onAttribute("id", n, (v) -> { ffs.setId(v); });
            onChild("case", n, (cn) -> { ffs.addNavigationCase(processNavigationCase(cn)); });
            onChild("default-outcome", n, (cn) -> {
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onChild("flow-return", node, (n) -> {
            FacesFlowReturnImpl ffr = new FacesFlowReturnImpl();
            onAttribute("id", n, (v) -> { ffr.setId(v); });
            onChild("from-outcome", n, (cn) -> {
                NavigationCaseImpl nc = new NavigationCaseImpl();
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onChild("flow-call", node, (n) -> {
            FacesFlowCallImpl ffc = new FacesFlowCallImpl();
            onAttribute("id", n, (v) -> { ffc.setId(v); });
            onChild("flow-reference", n, (cn) -> {
                FacesFlowReferenceImpl ffr = new FacesFlowReferenceImpl();
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        onChild("method-call", node, (n) -> {
            FacesFlowMethodCallImpl ffmc = new FacesFlowMethodCallImpl();
            onAttribute("id", n, (v) -> { ffmc.setId(v); });
            onChild("method", n, (cn) -> { ffmc.setMethod(getTextContent(cn)); });
            onChild("default-outcome", n, (cn) -> { ffmc.setDefaultOutcome(getTextContent(cn)); });
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
            
            onAttribute("metadata-complete", document.getDocumentElement(),
                    (v) -> { facesConfig.setMetadataComplete(v); });
            onAttribute("version", document.getDocumentElement(),
                    (v) -> { facesConfig.setVersion(v); });
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
                    (v) -> { facesConfig.setMetadataComplete(v); });
            onAttribute("version", document.getDocumentElement(),
                    (v) -> { facesConfig.setVersion(v); });

            onChild("name", document.getDocumentElement(), 
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `impl/src/main/java/org/apache/myfaces/webapp/FacesInitializerImpl.java`
#### Snippet
```java
            facesContext.getApplication().getViewHandler().getViews(facesContext, "/", 
                    ViewVisitOption.RETURN_AS_MINIMAL_IMPLICIT_OUTCOME).forEach(s -> {
                        facesServletRegistration.addMapping(s);
                    });
        }
```

### Convert2MethodRef
Lambda can be replaced with method reference
in `impl/src/main/java/org/apache/myfaces/application/viewstate/StateCacheServerSide.java`
#### Snippet
```java
                    if (System.getSecurityManager() != null) 
                    {
                        object = AccessController.doPrivileged((PrivilegedExceptionAction) () -> in.readObject());
                    }
                    else
```

## RuleId[ruleID=RedundantCollectionOperation]
### RedundantCollectionOperation
`contains` can be replaced with 'Map.containsKey()'
in `api/src/main/java/jakarta/faces/component/_ComponentFacetMap.java`
#### Snippet
```java
        public boolean contains(Object o)
        {
            return _map.keySet().contains(o);
        }

```

### RedundantCollectionOperation
`contains` can be replaced with 'Map.containsValue()'
in `api/src/main/java/jakarta/faces/component/_ComponentFacetMap.java`
#### Snippet
```java
        public boolean contains(Object o)
        {
            return _map.values().contains(o);
        }

```

### RedundantCollectionOperation
`iterator` can be replaced with 'Collections.emptyIterator()'
in `api/src/main/java/jakarta/faces/component/UISelectMany.java`
#### Snippet
```java
        if (convertedValue == null)
        {
            return Collections.emptyList().iterator();
        }
        else
```

### RedundantCollectionOperation
Unnecessary 'contains()' check
in `impl/src/main/java/org/apache/myfaces/component/search/SearchExpressionHandlerImpl.java`
#### Snippet
```java
    {
        // already available
        if (!searchExpressionContext.getExpressionHints().contains(hint))
        {
            searchExpressionContext.getExpressionHints().add(hint);
```

### RedundantCollectionOperation
`iterator` can be replaced with 'Collections.emptyIterator()'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlRenderKitImpl.java`
#### Snippet
```java

        //If the specified componentFamily is not known to this RenderKit implementation, return an empty Iterator
        return Collections.<String>emptySet().iterator();
    }

```

### RedundantCollectionOperation
Unnecessary 'containsKey()' check
in `test/src/main/java/org/apache/myfaces/test/mock/MockPortletSession.java`
#### Snippet
```java
                    + " not recognized");
        }
        if (attributes.containsKey(name))
        {
            attributes.remove(name);
```

### RedundantCollectionOperation
Unnecessary 'containsKey()' check
in `test/src/main/java/org/apache/myfaces/test/mock/MockPortletRequest.java`
#### Snippet
```java
    {

        if (attributes.containsKey(name))
        {
            attributes.remove(name);
```

### RedundantCollectionOperation
Unnecessary 'containsKey()' check
in `test/src/main/java/org/apache/myfaces/test/mock/MockPortletContext.java`
#### Snippet
```java
    {

        if (attributes.containsKey(name))
        {
            attributes.remove(name);
```

## RuleId[ruleID=NonSerializableFieldInSerializableClass]
### NonSerializableFieldInSerializableClass
Non-serializable field 'args' in a Serializable class
in `api/src/main/java/org/apache/myfaces/core/api/shared/ParametrizableFacesMessage.java`
#### Snippet
```java
    private static final long serialVersionUID = 7792947730961657948L;

    private final Object[] args;
    private String evaluatedDetail;
    private String evaluatedSummary;
```

### NonSerializableFieldInSerializableClass
Non-serializable field '_phaseId' in a Serializable class
in `api/src/main/java/jakarta/faces/event/PhaseEvent.java`
#### Snippet
```java
    // FIELDS
    private FacesContext _facesContext;
    private PhaseId _phaseId;

    // CONSTRUCTORS
```

### NonSerializableFieldInSerializableClass
Non-serializable field '_oldValue' in a Serializable class
in `api/src/main/java/jakarta/faces/event/ValueChangeEvent.java`
#### Snippet
```java
    private static final long serialVersionUID = -2490528664421353795L;
    // FIELDS
    private Object _oldValue;
    private Object _newValue;

```

### NonSerializableFieldInSerializableClass
Non-serializable field '_newValue' in a Serializable class
in `api/src/main/java/jakarta/faces/event/ValueChangeEvent.java`
#### Snippet
```java
    // FIELDS
    private Object _oldValue;
    private Object _newValue;

    public ValueChangeEvent(FacesContext facesContext, UIComponent uiComponent, Object oldValue, Object newValue)
```

### NonSerializableFieldInSerializableClass
Non-serializable field '_phaseId' in a Serializable class
in `api/src/main/java/jakarta/faces/event/FacesEvent.java`
#### Snippet
```java
public abstract class FacesEvent extends EventObject
{
    private PhaseId _phaseId;
    
    private transient FacesContext facesContext;
```

### NonSerializableFieldInSerializableClass
Non-serializable field '_data' in a Serializable class
in `api/src/main/java/jakarta/faces/model/DataModelEvent.java`
#### Snippet
```java
    // FIELDS
    private int _index;
    private Object _data;


```

### NonSerializableFieldInSerializableClass
Non-serializable field '_value' in a Serializable class
in `api/src/main/java/jakarta/faces/model/SelectItem.java`
#### Snippet
```java
    private static final long serialVersionUID = 8841094741464512226L;
    // FIELDS
    private Object _value;
    private String _label;
    private String _description;
```

### NonSerializableFieldInSerializableClass
Non-serializable field '_wrappedStateObject' in a Serializable class
in `api/src/main/java/jakarta/faces/component/_AttachedStateWrapper.java`
#### Snippet
```java
    private static final long serialVersionUID = 4948301780259917764L;
    private Class<?> _class;
    private Object _wrappedStateObject;

    /**
```

### NonSerializableFieldInSerializableClass
Non-serializable field '_wrappedStateObject' in a Serializable class
in `api/src/main/java/jakarta/faces/component/_AttachedDeltaWrapper.java`
#### Snippet
```java
    private static final long serialVersionUID = 4732389964367986402L;

    private Object _wrappedStateObject;

    /**
```

### NonSerializableFieldInSerializableClass
Non-serializable field '_root' in a Serializable class
in `api/src/main/java/jakarta/faces/component/_ViewAttributeMap.java`
#### Snippet
```java
    
    private Map<String, Object> _delegate;
    private UIViewRoot _root;

    public _ViewAttributeMap(UIViewRoot root, Map<String, Object> delegate)
```

### NonSerializableFieldInSerializableClass
Non-serializable field '_wrappedStateObject' in a Serializable class
in `api/src/main/java/jakarta/faces/component/behavior/_AttachedDeltaWrapper.java`
#### Snippet
```java
    private static final long serialVersionUID = 4732389964367986402L;

    private Object _wrappedStateObject;

    /**
```

### NonSerializableFieldInSerializableClass
Non-serializable field '_wrappedStateObject' in a Serializable class
in `api/src/main/java/jakarta/faces/component/behavior/_AttachedStateWrapper.java`
#### Snippet
```java
    private static final long serialVersionUID = 4948301780259917764L;
    private Class<?> _class;
    private Object _wrappedStateObject;

    /**
```

### NonSerializableFieldInSerializableClass
Non-serializable field '_submittedValue' in a Serializable class
in `api/src/main/java/jakarta/faces/component/UIData.java`
#### Snippet
```java
        private final boolean _localValueSet;
        private final boolean _valid;
        private final Object _submittedValue;

        public EditableValueHolderState(EditableValueHolder evh)
```

### NonSerializableFieldInSerializableClass
Non-serializable field '_value' in a Serializable class
in `api/src/main/java/jakarta/faces/component/UIData.java`
#### Snippet
```java
    private static class EditableValueHolderState implements Serializable
    {
        private final Object _value;
        private final boolean _localValueSet;
        private final boolean _valid;
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'applicationContextBean' in a Serializable class
in `impl/src/main/java/org/apache/myfaces/cdi/util/AbstractContextualStorageHolder.java`
#### Snippet
```java
{
    @Inject
    protected FacesApplicationArtifactHolder applicationContextBean;
    
    @Inject
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'sessionManager' in a Serializable class
in `impl/src/main/java/org/apache/myfaces/push/cdi/WebsocketScopeManager.java`
#### Snippet
```java
    {
        @Inject private WebsocketScopeManager scopeManager;
        @Inject private WebsocketSessionManager sessionManager;
        
        /*
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'scopeManager' in a Serializable class
in `impl/src/main/java/org/apache/myfaces/push/cdi/WebsocketScopeManager.java`
#### Snippet
```java
    public static class ViewScope extends AbstractUserScope implements Serializable
    {
        @Inject private WebsocketScopeManager scopeManager;
        @Inject private WebsocketSessionManager sessionManager;
        
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'sessionManager' in a Serializable class
in `impl/src/main/java/org/apache/myfaces/push/cdi/WebsocketScopeManager.java`
#### Snippet
```java
    public static class SessionScope extends AbstractUserScope implements Serializable
    {
        @Inject private WebsocketSessionManager sessionManager;
        
        @PreDestroy
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'scopeManager' in a Serializable class
in `impl/src/main/java/org/apache/myfaces/push/cdi/PushContextImpl.java`
#### Snippet
```java
    private final String channel;
    private final BeanManager beanManager;
    private final WebsocketScopeManager scopeManager;
    private final WebsocketSessionManager sessionManager;

```

### NonSerializableFieldInSerializableClass
Non-serializable field 'sessionManager' in a Serializable class
in `impl/src/main/java/org/apache/myfaces/push/cdi/PushContextImpl.java`
#### Snippet
```java
    private final BeanManager beanManager;
    private final WebsocketScopeManager scopeManager;
    private final WebsocketSessionManager sessionManager;

    public PushContextImpl(String channel, BeanManager beanManager)
```

### NonSerializableFieldInSerializableClass
Non-serializable field '_args' in a Serializable class
in `impl/src/main/java/org/apache/myfaces/util/ParametrizableFacesMessage.java`
#### Snippet
```java
    private static final long serialVersionUID = 7792947730961657948L;

    private final Object[] _args;
    private String _evaluatedDetail;
    private String _evaluatedSummary;
```

### NonSerializableFieldInSerializableClass
Non-serializable field '_wrappedStateObject' in a Serializable class
in `impl/src/main/java/org/apache/myfaces/view/facelets/AttachedFullStateWrapper.java`
#### Snippet
```java
{
    private static final long serialVersionUID = -138360308769803541L;
    private Object _wrappedStateObject;

    /**
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'bufferKey' in a Serializable class
in `impl/src/main/java/org/apache/myfaces/util/lang/StreamCharBuffer.java`
#### Snippet
```java
    private int totalCharsInDynamicChunks;
    private int sizeAtLeast;
    private StreamCharBufferKey bufferKey = new StreamCharBufferKey();
    private Map<StreamCharBufferKey, StreamCharBufferSubChunk> dynamicChunkMap;

```

### NonSerializableFieldInSerializableClass
Non-serializable field 'writer' in a Serializable class
in `impl/src/main/java/org/apache/myfaces/util/lang/StreamCharBuffer.java`
#### Snippet
```java
    private int totalChunkSize;

    private final StreamCharBufferWriter writer;
    private List<ConnectedWriter> connectedWriters;
    private Writer connectedWritersWriter;
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'allocBuffer' in a Serializable class
in `impl/src/main/java/org/apache/myfaces/util/lang/StreamCharBuffer.java`
#### Snippet
```java
    boolean preferSubChunkWhenWritingToOtherBuffer = false;

    private AllocatedBuffer allocBuffer;
    private AbstractChunk firstChunk;
    private AbstractChunk lastChunk;
```

### NonSerializableFieldInSerializableClass
Non-serializable field '_currentPhase' in a Serializable class
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/DebugPhaseListener.java`
#### Snippet
```java
    
    private boolean _afterPhase = false;
    private PhaseId _currentPhase;
    private DebugVisitCallback _visitCallback = new DebugVisitCallback();

```

### NonSerializableFieldInSerializableClass
Non-serializable field '_visitCallback' in a Serializable class
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/DebugPhaseListener.java`
#### Snippet
```java
    private boolean _afterPhase = false;
    private PhaseId _currentPhase;
    private DebugVisitCallback _visitCallback = new DebugVisitCallback();

    @Override
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'value' in a Serializable class
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/IterationStatus.java`
#### Snippet
```java
    private final Integer end;
    private final Integer step;
    private final Object value;

    public IterationStatus(boolean first, boolean last, int index, Integer begin, Integer end,
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'key' in a Serializable class
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/MappedValueExpression.java`
#### Snippet
```java
    {
        private final Map src;
        private final Object key;

        public Entry(Map src, Object key)
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'key' in a Serializable class
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/MappedValueExpression.java`
#### Snippet
```java
    private static final long serialVersionUID = 1L;

    private final Object key;
    private final ValueExpression orig;

```

### NonSerializableFieldInSerializableClass
Non-serializable field '_facets' in a Serializable class
in `impl/src/main/java/org/apache/myfaces/view/facelets/PartialStateManagementStrategy.java`
#### Snippet
```java
        private String _componentId;
        private TreeStructComponent[] _children = null; // Array of children
        private Object[] _facets = null; // Array of Array-tuples with Facetname and TreeStructComponent

        TreeStructComponent(String componentClass, String componentId)
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'value' in a Serializable class
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/IteratedValueExpression.java`
#### Snippet
```java

    private ValueExpression orig;
    private Object value;

    public IteratedValueExpression(ValueExpression orig, Object value)
```

### NonSerializableFieldInSerializableClass
Non-serializable field '_descriptor' in a Serializable class
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeComponentBeanInfo.java`
#### Snippet
```java
     * key is used by other handlers inside the inner map for this BeanDescriptor.
     */
    private BeanDescriptor _descriptor;
    
    /**
```

### NonSerializableFieldInSerializableClass
Non-serializable field '_propertyDescriptorsArray' in a Serializable class
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeComponentBeanInfo.java`
#### Snippet
```java
    private static final PropertyDescriptor[] EMPTY_PROPERTY_DESCRIPTOR_ARRAY = new PropertyDescriptor[0];
    
    private PropertyDescriptor[] _propertyDescriptorsArray;
    
    private Map<String, PropertyDescriptor> _propertyDescriptorsMap;
```

### NonSerializableFieldInSerializableClass
Non-serializable field '_submittedValue' in a Serializable class
in `impl/src/main/java/org/apache/myfaces/view/facelets/component/UIRepeat.java`
#### Snippet
```java
    {
        private boolean _localValueSet;
        private Object _submittedValue;
        private boolean _valid = true;
        private Object _value;
```

### NonSerializableFieldInSerializableClass
Non-serializable field '_value' in a Serializable class
in `impl/src/main/java/org/apache/myfaces/view/facelets/component/UIRepeat.java`
#### Snippet
```java
        private Object _submittedValue;
        private boolean _valid = true;
        private Object _value;

        private static final long serialVersionUID = 2920252657338389849L;
```

### NonSerializableFieldInSerializableClass
Non-serializable field '_facets' in a Serializable class
in `impl/src/main/java/org/apache/myfaces/application/TreeStructureManager.java`
#### Snippet
```java
        private String _componentId;
        private TreeStructComponent[] _children = null;    // Array of children
        private Object[] _facets = null;            // Array of Array-tuples with Facetname and TreeStructComponent

        TreeStructComponent(String componentClass, String componentId)
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'instance' in a Serializable class
in `test/src/main/java/org/apache/myfaces/test/el/MockVariableValueExpression.java`
#### Snippet
```java
     * <p>The variable instance being wrapped by this expression.</p>
     */
    private Object instance = null;

    // ------------------------------------------------------ Expression Methods
```

### NonSerializableFieldInSerializableClass
Non-serializable field 'clientId' in a Serializable class
in `test/src/main/java/org/apache/myfaces/test/mock/MockFlash.java`
#### Snippet
```java
    private static class MessageEntry implements Serializable
    {
        private final Object clientId;
        private final Object message;

```

### NonSerializableFieldInSerializableClass
Non-serializable field 'message' in a Serializable class
in `test/src/main/java/org/apache/myfaces/test/mock/MockFlash.java`
#### Snippet
```java
    {
        private final Object clientId;
        private final Object message;

        public MessageEntry(Object clientId, Object message)
```

## RuleId[ruleID=CatchMayIgnoreException]
### CatchMayIgnoreException
Empty `catch` block
in `api/src/main/java/org/apache/myfaces/core/api/shared/SelectItemsUtil.java`
#### Snippet
```java
                }
            }
            catch (IllegalArgumentException e)
            {

```

### CatchMayIgnoreException
Empty `catch` block
in `api/src/main/java/org/apache/myfaces/core/api/shared/SelectItemsUtil.java`
#### Snippet
```java

            }
            catch (Exception e)
            {

```

### CatchMayIgnoreException
Empty `catch` block
in `api/src/main/java/org/apache/myfaces/core/api/shared/lang/PropertyDescriptorUtils.java`
#### Snippet
```java
                    MethodHandles.Lookup.class);
        }
        catch (Exception e)
        {
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `api/src/main/java/jakarta/faces/FactoryFinder.java`
#### Snippet
```java
            }
        }
        catch (Exception e)
        {
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `impl/src/main/java/org/apache/myfaces/el/DefaultELResolverBuilder.java`
#### Snippet
```java
                list.add((ELResolver) STATIC_FIELD_EL_RESOLVER_CLASS.newInstance());
            } 
            catch (IllegalAccessException
                    | IllegalArgumentException
                    | InvocationTargetException
```

### CatchMayIgnoreException
'catch' parameter named `ignore` is used
in `impl/src/main/java/org/apache/myfaces/el/resolver/ImportConstantsELResolver.java`
#### Snippet
```java
                    return toClass(new StringBuilder(type).replace(i, i + 1, "$").toString());
                } 
                catch (Exception ignore)
                {
                    Logger.getLogger(ImportConstantsELResolver.class.getName()).log(
```

### CatchMayIgnoreException
Empty `catch` block
in `impl/src/main/java/org/apache/myfaces/util/lang/ConcurrentLRUCache.java`
#### Snippet
```java
                        this.wait();
                    }
                    catch (InterruptedException e)
                    {
                    }
```

### CatchMayIgnoreException
Empty `catch` block
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ComponentSupport.java`
#### Snippet
```java
                SET_CACHED_FACES_CONTEXT.invoke(component, context);
            }
            catch (IllegalAccessException | IllegalArgumentException | InvocationTargetException ex)
            {
            }
```

### CatchMayIgnoreException
Empty `catch` block
in `impl/src/main/java/org/apache/myfaces/config/DefaultFacesConfigurationProvider.java`
#### Snippet
```java
                                }
                            }
                            catch (MalformedURLException ex)
                            {
                            }
```

### CatchMayIgnoreException
Empty `catch` block
in `impl/src/main/java/org/apache/myfaces/config/DefaultFacesConfigurationProvider.java`
#### Snippet
```java
                        }
                    }
                    catch (MalformedURLException ex)
                    {
                    }
```

### CatchMayIgnoreException
Empty `catch` block
in `impl/src/main/java/org/apache/myfaces/config/webparameters/MyfacesConfig.java`
#### Snippet
```java
            return Integer.parseInt(value);
        }
        catch (NumberFormatException e)
        {
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `impl/src/main/java/org/apache/myfaces/config/webparameters/MyfacesConfig.java`
#### Snippet
```java
            return Long.parseLong(value);
        }
        catch (NumberFormatException e)
        {
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `test/src/main/java/org/apache/myfaces/test/base/junit/runners/TestClassLoader.java`
#### Snippet
```java
                is.close();
            }
            catch (IOException e)
            {
            }
```

### CatchMayIgnoreException
Empty `catch` block
in `test/src/main/java/org/apache/myfaces/test/base/junit/runners/TestClassLoader.java`
#### Snippet
```java
            return data;
        }
        catch (IOException e)
        {
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `test/src/main/java/org/apache/myfaces/test/base/junit/runners/TestClassLoader.java`
#### Snippet
```java
                }
            }
            catch (IOException e)
            {
            }
```

### CatchMayIgnoreException
Empty `catch` block
in `test/src/main/java/org/apache/myfaces/test/base/junit/runners/TestClassLoader.java`
#### Snippet
```java

        }
        catch (IOException e)
        {
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `test/src/main/java/org/apache/myfaces/test/mock/MockPortletContext.java`
#### Snippet
```java
            }
        }
        catch (Exception e)
        {
        }
```

### CatchMayIgnoreException
Empty `catch` block
in `test/src/main/java/org/apache/myfaces/test/mock/MockApplication20.java`
#### Snippet
```java
                    componentClass = classForName(fqcn);
                }
                catch (ClassNotFoundException e)
                {
                }
```

### CatchMayIgnoreException
Empty `catch` block
in `test/src/main/java/org/apache/myfaces/test/mock/MockResponseStateManager.java`
#### Snippet
```java
                    s.close();
                }
                catch (IOException e)
                {
                }
```

### CatchMayIgnoreException
Empty `catch` block
in `test/src/main/java/org/apache/myfaces/test/mock/MockResponseStateManager.java`
#### Snippet
```java
                    input.close();
                }
                catch (IOException e)
                {
                }
```

### CatchMayIgnoreException
Empty `catch` block
in `test/src/main/java/org/apache/myfaces/test/mock/MockResponseStateManager.java`
#### Snippet
```java
                    oos.close();
                }
                catch (IOException e)
                {
                }
```

### CatchMayIgnoreException
Empty `catch` block
in `test/src/main/java/org/apache/myfaces/test/mock/MockResponseStateManager.java`
#### Snippet
```java
                    baos.close();
                }
                catch (IOException e)
                {
                }
```

## RuleId[ruleID=MismatchedJavadocCode]
### MismatchedJavadocCode
Method is specified to return list but the return type is array
in `api/src/main/java/jakarta/faces/view/facelets/TagAttributes.java`
#### Snippet
```java
     * A list of Namespaces found in this set
     * 
     * @return a list of Namespaces found in this set
     */
    public abstract String[] getNamespaces();
```

### MismatchedJavadocCode
Method is specified to return list but the return type is array
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/TagAttributesImpl.java`
#### Snippet
```java
     * A list of Namespaces found in this set
     * 
     * @return a list of Namespaces found in this set
     */
    @Override
```

## RuleId[ruleID=AssignmentToForLoopParameter]
### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `api/src/main/java/jakarta/faces/component/UIViewRoot.java`
#### Snippet
```java
                _events.remove(i);
                size--;
                i--;
            }
            else if (event.getPhaseId().equals(phaseId))
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `api/src/main/java/jakarta/faces/component/UIViewRoot.java`
#### Snippet
```java
                _events.remove(i);
                size--;
                i--;
            }
        }
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `j`
in `impl/src/main/java/org/apache/myfaces/util/lang/Hex.java`
#### Snippet
```java
        {
            int f = toDigit(data[j], j) << 4;
            j++;
            f = f | toDigit(data[j], j);
            j++;
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `j`
in `impl/src/main/java/org/apache/myfaces/util/lang/Hex.java`
#### Snippet
```java
            j++;
            f = f | toDigit(data[j], j);
            j++;
            out[i] = (byte) (f & 0xFF);
        }
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `j`
in `impl/src/main/java/org/apache/myfaces/util/lang/Hex.java`
#### Snippet
```java
        for (int i = 0, j = 0; i < l; i++)
        {
            out[j++] = toDigits[(0xF0 & data[i]) >>> 4];
            out[j++] = toDigits[0x0F & data[i]];
        }
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `j`
in `impl/src/main/java/org/apache/myfaces/util/lang/Hex.java`
#### Snippet
```java
        {
            out[j++] = toDigits[(0xF0 & data[i]) >>> 4];
            out[j++] = toDigits[0x0F & data[i]];
        }
        return out;
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `offset`
in `impl/src/main/java/org/apache/myfaces/util/lang/ArrayUtils.java`
#### Snippet
```java
                {
                    System.arraycopy(arr, 0, toArray, offset, arrayLen);
                    offset += arrayLen;
                }
            }
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `impl/src/main/java/org/apache/myfaces/util/lang/FilenameUtils.java`
#### Snippet
```java
                System.arraycopy(array, i, array, i - 1, size - i);
                size--;
                i--;
            }
        }
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `impl/src/main/java/org/apache/myfaces/util/lang/FilenameUtils.java`
#### Snippet
```java
                System.arraycopy(array, i + 1, array, i - 1, size - i);
                size -= 2;
                i--;
            }
        }
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `impl/src/main/java/org/apache/myfaces/util/lang/FilenameUtils.java`
#### Snippet
```java
                        System.arraycopy(array, i + 1, array, j + 1, size - i);
                        size -= i - j;
                        i = j + 1;
                        continue outer;
                    }
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `impl/src/main/java/org/apache/myfaces/util/lang/FilenameUtils.java`
#### Snippet
```java
                System.arraycopy(array, i + 1, array, prefix, size - i);
                size -= i + 1 - prefix;
                i = prefix + 1;
            }
        }
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `pos`
in `impl/src/main/java/org/apache/myfaces/util/lang/StringUtils.java`
#### Snippet
```java
            while ((pos < len) && (str.charAt(pos) == quote))
            {
                pos = str.indexOf(quote, pos + 1) + 1;

                // pos == 0 is not found (-1 returned by indexOf + 1)
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `pos`
in `impl/src/main/java/org/apache/myfaces/util/lang/StringUtils.java`
#### Snippet
```java
            else
            {
                pos = str.indexOf(separator, pos);
                if (pos < 0)
                {
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `pos`
in `impl/src/main/java/org/apache/myfaces/util/lang/StringUtils.java`
#### Snippet
```java
            while (str.charAt(pos) == quote)
            {
                pos = str.indexOf(quote, pos + 1) + 1;
            }

```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `pos`
in `impl/src/main/java/org/apache/myfaces/util/lang/StringUtils.java`
#### Snippet
```java
            {
                quoted     = false;
                pos        = str.indexOf(separator, pos);
            }

```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `pos`
in `impl/src/main/java/org/apache/myfaces/util/lang/StringUtils.java`
#### Snippet
```java
            while ((pos < len) && (str.charAt(pos) == quote))
            {
                pos = str.indexOf(quote, pos + 1) + 1;

                if (pos == 0)
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `pos`
in `impl/src/main/java/org/apache/myfaces/util/lang/StringUtils.java`
#### Snippet
```java
            {
                quoted = false;
                pos = str.indexOf(separator, pos);
                if (pos < 0)
                {
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `pos`
in `impl/src/main/java/org/apache/myfaces/util/lang/StringUtils.java`
#### Snippet
```java
                if (pos < 0)
                {
                    pos = len;
                }
            }
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `impl/src/main/java/org/apache/myfaces/view/facelets/ViewPoolProcessor.java`
#### Snippet
```java
                {
                    component.getChildren().remove(i);
                    i--;
                    childCount--;
                }
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `impl/src/main/java/org/apache/myfaces/view/facelets/ViewPoolProcessor.java`
#### Snippet
```java
                    //Transient and not bound to facelets tag, remove it!.
                    component.getChildren().remove(i);
                    i--;
                    childCount--;
                }
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `impl/src/main/java/org/apache/myfaces/view/facelets/ViewPoolProcessor.java`
#### Snippet
```java
                        //Dynamically added or moved, remove it!
                        component.getChildren().remove(i);
                        i--;
                        childCount--;

```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `impl/src/main/java/org/apache/myfaces/view/facelets/ViewPoolProcessor.java`
#### Snippet
```java
                            {
                                component.getChildren().remove(i);
                                i--;
                                childCount--;
                            }
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `impl/src/main/java/org/apache/myfaces/view/facelets/ViewPoolProcessor.java`
#### Snippet
```java
                        // Non facelets component, remove it!.
                        component.getChildren().remove(i);
                        i--;
                        childCount--;
                    }
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `impl/src/main/java/org/apache/myfaces/view/facelets/ViewPoolProcessor.java`
#### Snippet
```java
                {
                    root.getChildren().remove(i);
                    i--;
                    childCount--;
                }
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `impl/src/main/java/org/apache/myfaces/view/facelets/ViewPoolProcessor.java`
#### Snippet
```java
                    //Remove both transient not facelets bound components
                    component.getChildren().remove(i);
                    i--;
                    childCount--;
                }
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `impl/src/main/java/org/apache/myfaces/view/facelets/ViewPoolProcessor.java`
#### Snippet
```java
                            //Remove it, because for this dynamic state it it does not exists.
                            component.getChildren().remove(i);
                            i--;
                            childCount--;
                        }
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `impl/src/main/java/org/apache/myfaces/view/facelets/ViewPoolProcessor.java`
#### Snippet
```java
                        //Transient and not bound to facelets tag, remove it!.
                        root.getChildren().remove(i);
                        i--;
                        childCount--;
                    }
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `impl/src/main/java/org/apache/myfaces/view/facelets/ViewPoolProcessor.java`
#### Snippet
```java
                            //Dynamically added or moved, remove it!
                            root.getChildren().remove(i);
                            i--;
                            childCount--;

```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `impl/src/main/java/org/apache/myfaces/view/facelets/ViewPoolProcessor.java`
#### Snippet
```java
                                {
                                    root.getChildren().remove(i);
                                    i--;
                                    childCount--;
                                }
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `impl/src/main/java/org/apache/myfaces/view/facelets/ViewPoolProcessor.java`
#### Snippet
```java
                            // Non facelets component, remove it!.
                            root.getChildren().remove(i);
                            i--;
                            childCount--;
                        }
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/FaceletCompositionContextImpl.java`
#### Snippet
```java
                {
                    component.getChildren().remove(i);
                    i--;
                    childCount--;
                }
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/FaceletCompositionContextImpl.java`
#### Snippet
```java
                            {
                                fc.getChildren().remove(i);
                                i--;
                                size--;
                            }
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `size`
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/FaceletCompositionContextImpl.java`
#### Snippet
```java
                                fc.getChildren().remove(i);
                                i--;
                                size--;
                            }
                        }
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/FaceletCompositionContextImpl.java`
#### Snippet
```java
                            {
                                fc.getChildren().remove(i);
                                i--;
                                childCount--;
                            }
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TextUnit.java`
#### Snippet
```java
                    {
                        instructionBuffer.remove(i);
                        i--;
                        size--;
                    }
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `impl/src/main/java/org/apache/myfaces/config/annotation/ClassByteCodeAnnotationFilter.java`
#### Snippet
```java
                    in.readInt();
                    // this tag takes two entries in the constants pool
                    i++;
                    break;
                case CP_INFO_NAME_AND_TYPE: //ignore
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `i`
in `impl/src/main/java/org/apache/myfaces/config/annotation/ClassByteCodeAnnotationFilter.java`
#### Snippet
```java
                        log.warning("Unknown tag in constants pool: " + tag);
                    }
                    i = constantsPoolCount;
                    break;
            }
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `j`
in `test/src/main/java/org/apache/myfaces/test/mock/_Hex.java`
#### Snippet
```java
        for (int i = 0, j = 0; i < l; i++)
        {
            out[j++] = toDigits[(0xF0 & data[i]) >>> 4];
            out[j++] = toDigits[0x0F & data[i]];
        }
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `j`
in `test/src/main/java/org/apache/myfaces/test/mock/_Hex.java`
#### Snippet
```java
        {
            out[j++] = toDigits[(0xF0 & data[i]) >>> 4];
            out[j++] = toDigits[0x0F & data[i]];
        }
        return out;
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `j`
in `test/src/main/java/org/apache/myfaces/test/mock/_Hex.java`
#### Snippet
```java
        {
            int f = toDigit(data[j], j) << 4;
            j++;
            f = f | toDigit(data[j], j);
            j++;
```

### AssignmentToForLoopParameter
Assignment to for-loop parameter `j`
in `test/src/main/java/org/apache/myfaces/test/mock/_Hex.java`
#### Snippet
```java
            j++;
            f = f | toDigit(data[j], j);
            j++;
            out[i] = (byte) (f & 0xFF);
        }
```

## RuleId[ruleID=UnnecessaryToStringCall]
### UnnecessaryToStringCall
Unnecessary `toString()` call
in `api/src/main/java/org/apache/myfaces/core/api/shared/ComponentUtils.java`
#### Snippet
```java
        intBuf.append(']');

        buf.insert(0, intBuf.toString());

        getPathToComponent(component.getParent(), buf);
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `impl/src/main/java/org/apache/myfaces/spi/impl/SpiUtils.java`
#### Snippet
```java
            {
                getLogger().log(Level.WARNING, "More than one SPI interface for " + spiClass.getName() +
                        " found :" + classList.toString() + ". Setting up the first one found.");
            }
            return ClassUtils.newInstance(classList.get(0));
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `impl/src/main/java/org/apache/myfaces/push/Json.java`
#### Snippet
```java
        else if (object instanceof Boolean || object instanceof Number)
        {
            builder.append(object.toString());
        }
        else if (object instanceof CharSequence)
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `impl/src/main/java/org/apache/myfaces/flow/cdi/FlowScopeContext.java`
#### Snippet
```java
            {
                throw new IllegalStateException("Flow "+ reference.getId()+
                    " cannot be found when resolving bean " +bean.toString());
            }
        }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `impl/src/main/java/org/apache/myfaces/flow/cdi/FlowScopeContext.java`
#### Snippet
```java
            {
                throw new IllegalStateException("Flow " + reference.getId()+
                    " cannot be found when resolving bean " + bean.toString());
            }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `impl/src/main/java/org/apache/myfaces/flow/cdi/FlowScopeContext.java`
#### Snippet
```java
            if (flow == null)
            {
                throw new IllegalStateException(bean.toString() + "cannot be created because flow "
                        + reference.getId() + " is not registered");
            }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `impl/src/main/java/org/apache/myfaces/flow/cdi/FlowScopeContext.java`
#### Snippet
```java
            if (!flowHandler.isActive(facesContext, flow.getDefiningDocumentId(), flow.getId())) 
            {
                throw new IllegalStateException(bean.toString() + "cannot be created if flow "
                        + reference.getId() + " is not active");
            }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `impl/src/main/java/org/apache/myfaces/util/ConverterUtils.java`
#### Snippet
```java
            catch (NumberFormatException e)
            {
                throw new IllegalArgumentException("Cannot convert " + value.toString() + " to double");
            }
        }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `impl/src/main/java/org/apache/myfaces/util/ConverterUtils.java`
#### Snippet
```java
            catch (NumberFormatException e)
            {
                throw new IllegalArgumentException("Cannot convert " + value.toString() + " to long");
            }
        }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `impl/src/main/java/org/apache/myfaces/util/ConverterUtils.java`
#### Snippet
```java
            catch (NumberFormatException e)
            {
                throw new IllegalArgumentException("Cannot convert " + value.toString() + " to int");
            }
        }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `impl/src/main/java/org/apache/myfaces/util/ConverterUtils.java`
#### Snippet
```java
            catch (Exception e)
            {
                throw new IllegalArgumentException("Cannot convert " + value.toString() + " to boolean");
            }
        }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `impl/src/main/java/org/apache/myfaces/util/DebugUtils.java`
#### Snippet
```java
            else
            {
                stream.print(value.toString());
            }
        }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `impl/src/main/java/org/apache/myfaces/util/lang/StreamCharBuffer.java`
#### Snippet
```java
    public String plus(Object value)
    {
        return toString() + String.valueOf(value);
    }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `impl/src/main/java/org/apache/myfaces/util/lang/StreamCharBuffer.java`
#### Snippet
```java
    public String plus(String value)
    {
        return toString() + value;
    }

```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/BehaviorTagHandlerDelegate.java`
#### Snippet
```java
            {
                throw new TagAttributeException(_delegate.getEvent(),
                        "eventName could not be defined for client behavior "+ behavior.toString());
            }
            else if (!cvh.getEventNames().contains(eventName))
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/EventHandler.java`
#### Snippet
```java
            
            throw new FacesException("The event name '" + value + "' is mapped to more than one " +
                " event class: " + classNames.toString());
        }
        else
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `impl/src/main/java/org/apache/myfaces/view/facelets/util/Classpath.java`
#### Snippet
```java
                        {
                            bytes.flip();
                            buffer.append(UTF8.decode(bytes).toString());
                            bytes.clear();
                        }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/CheckDuplicateIdFaceletUtils.java`
#### Snippet
```java
            if (location != null)
            {
                message += " The first component is " + location.toString();
            }
            else
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `impl/src/main/java/org/apache/myfaces/resource/ClassLoaderResourceLoaderIterator.java`
#### Snippet
```java
                    if (log.isLoggable(Level.WARNING))
                    {
                        log.log(Level.WARNING, "url "+url.toString()+" cannot be translated to uri: "
                                +e.getMessage(), e);
                    }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `impl/src/main/java/org/apache/myfaces/lifecycle/RenderResponseExecutor.java`
#### Snippet
```java
                    log.log(Level.WARNING, "There are some unhandled FacesMessages, " +
                            "this means not every FacesMessage had a chance to be rendered.\n" +
                            "These unhandled FacesMessages are: " + builder.toString());
                }
            }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/AjaxScriptBuilder.java`
#### Snippet
```java
        {
            builder.append('\'');
            builder.append(value.toString());
            builder.append('\'');
        }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/AjaxScriptBuilder.java`
#### Snippet
```java
        else
        {
            builder.append(value.toString());
        }
    }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `impl/src/main/java/org/apache/myfaces/application/ApplicationImpl.java`
#### Snippet
```java
            catch (Exception e)
            {
                log.log(Level.SEVERE, "Could not instantiate converter " + converterClassOrClassName.toString(), e);
                throw new FacesException("Could not instantiate converter: " + converterClassOrClassName.toString(), e);
            }
```

### UnnecessaryToStringCall
Unnecessary `toString()` call
in `impl/src/main/java/org/apache/myfaces/application/ApplicationImpl.java`
#### Snippet
```java
            {
                log.log(Level.SEVERE, "Could not instantiate converter " + converterClassOrClassName.toString(), e);
                throw new FacesException("Could not instantiate converter: " + converterClassOrClassName.toString(), e);
            }
        }
```

## RuleId[ruleID=Java9CollectionFactory]
### Java9CollectionFactory
Can be replaced with 'Set.copyOf()' call
in `api/src/main/java/org/apache/myfaces/core/api/shared/lang/LocaleUtils.java`
#### Snippet
```java
            List<Locale> list = new ArrayList<>(Arrays.asList(Locale.getAvailableLocales()));  // extra safe
            AVAILABLE_LOCALE_LIST = Collections.unmodifiableList(list);
            AVAILABLE_LOCALE_SET = Collections.unmodifiableSet(new HashSet<>(availableLocaleList()));
        }
    }
```

### Java9CollectionFactory
Can be replaced with 'Set.of()' call
in `impl/src/main/java/org/apache/myfaces/config/annotation/DefaultAnnotationProvider.java`
#### Snippet
```java
        //bcan.add("Ljakarta/faces/event/ListenersFor;");
        bcan.add("Ljakarta/faces/render/FacesBehaviorRenderer;");
        JSF_ANNOTATION_NAMES = Collections.unmodifiableSet(bcan);

        Set<Class<? extends Annotation>> ancl = new HashSet<>(10, 1f);
```

### Java9CollectionFactory
Can be replaced with 'Set.of()' call
in `impl/src/main/java/org/apache/myfaces/config/annotation/DefaultAnnotationProvider.java`
#### Snippet
```java
        ancl.add(NamedEvent.class);
        ancl.add(FacesBehaviorRenderer.class);
        JSF_ANNOTATION_CLASSES = Collections.unmodifiableSet(ancl);
    }

```

### Java9CollectionFactory
Can be replaced with 'Map.of()' call
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTextRendererBase.java`
#### Snippet
```java
        map.put("reset", "<h:commandButton type=\"reset\">");
        map.put("button", "<h:commandButton type=\"button\"> or <h:button>");
        return Collections.unmodifiableMap(map);
    }

```

## RuleId[ruleID=InnerClassMayBeStatic]
### InnerClassMayBeStatic
Inner class `RedirectBuilder` may be 'static'
in `api/src/main/java/jakarta/faces/flow/builder/NavigationCaseBuilder.java`
#### Snippet
```java
    public abstract NavigationCaseBuilder.RedirectBuilder redirect();
    
    public abstract class RedirectBuilder
    {
        public abstract NavigationCaseBuilder.RedirectBuilder parameter(String name,
```

### InnerClassMayBeStatic
Inner class `CompositeComponentAttributesMapWrapper` may be 'static'
in `impl/src/main/java/org/apache/myfaces/el/resolver/CompositeComponentELResolver.java`
#### Snippet
```java

    // Wrapper map for composite component attributes.  Follows spec, section 5.6.2.2, table 5-11.
    private final class CompositeComponentAttributesMapWrapper 
            implements CompositeComponentExpressionHolder, Map<String, Object>
    {
```

### InnerClassMayBeStatic
Inner class `FlowMapAnnotationLiteral` may be 'static'
in `impl/src/main/java/org/apache/myfaces/cdi/FacesArtifactFlowMapProducer.java`
#### Snippet
```java
public class FacesArtifactFlowMapProducer extends AbstractDynamicProducer
{
    class FlowMapAnnotationLiteral extends AnnotationLiteral<FlowMap> implements FlowMap
    {
        private static final long serialVersionUID = -8623640277155878657L;
```

### InnerClassMayBeStatic
Inner class `ManagedPropertyAnnotationLiteral` may be 'static'
in `impl/src/main/java/org/apache/myfaces/cdi/managedproperty/ManagedPropertyProducer.java`
#### Snippet
```java
public class ManagedPropertyProducer extends AbstractDynamicProducer<Object>
{
    class ManagedPropertyAnnotationLiteral extends AnnotationLiteral<ManagedProperty> implements ManagedProperty
    {
        private static final long serialVersionUID = 1L;
```

### InnerClassMayBeStatic
Inner class `UserChannelKey` may be 'static'
in `impl/src/main/java/org/apache/myfaces/push/cdi/WebsocketSessionManager.java`
#### Snippet
```java


    private class UserChannelKey implements Serializable
    {

```

### InnerClassMayBeStatic
Inner class `MethodExpressionMetadata` may be 'static'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/MethodRule.java`
#### Snippet
```java
    }

    private class MethodExpressionMetadata extends Metadata
    {
        private final Method method;
```

### InnerClassMayBeStatic
Inner class `LiteralAttributeMetadata` may be 'static'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ComponentRule.java`
#### Snippet
```java
{

    final class LiteralAttributeMetadata extends Metadata
    {
        private final String _name;
```

### InnerClassMayBeStatic
Inner class `TypedLiteralAttributeMetadata` may be 'static'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeComponentRule.java`
#### Snippet
```java
    }
    
    final class TypedLiteralAttributeMetadata extends Metadata
    {
        private final String _name;
```

### InnerClassMayBeStatic
Inner class `LiteralAttributeMetadata` may be 'static'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeComponentRule.java`
#### Snippet
```java
{

    final class LiteralAttributeMetadata extends Metadata
    {
        private final String _name;
```

### InnerClassMayBeStatic
Inner class `StackEntry` may be 'static'
in `impl/src/main/java/org/apache/myfaces/context/PartialResponseWriterImpl.java`
#### Snippet
```java
{

    class StackEntry
    {
        ResponseWriter writer;
```

### InnerClassMayBeStatic
Inner class `PhaseAwareVisitCallback` may be 'static'
in `impl/src/main/java/org/apache/myfaces/context/servlet/PartialViewContextImpl.java`
#### Snippet
```java
    }

    private class PhaseAwareVisitCallback implements VisitCallback
    {
        private PhaseId _phaseId;
```

### InnerClassMayBeStatic
Inner class `LocalesIterator` may be 'static'
in `test/src/main/java/org/apache/myfaces/test/mock/MockExternalContext10.java`
#### Snippet
```java
     * of Locales for the current request.</p>
     */
    private class LocalesIterator implements Iterator
    {

```

### InnerClassMayBeStatic
Inner class `VersionComparator` may be 'static'
in `test/src/main/java/org/apache/myfaces/test/mock/resource/MockResourceLoader.java`
#### Snippet
```java
    }

    public class VersionComparator implements Comparator<String>
    {
        @Override
```

## RuleId[ruleID=StringEqualsEmptyString]
### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `impl/src/main/java/org/apache/myfaces/flow/FlowHandlerImpl.java`
#### Snippet
```java
                                calledFlowDocumentId, 
                                outboundCallNode.getCalledFlowId(context));
                            if (targetFlow == null && !"".equals(calledFlowDocumentId))
                            {
                                targetFlow = flowHandler.getFlow(context, "", 
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `impl/src/main/java/org/apache/myfaces/flow/FlowHandlerImpl.java`
#### Snippet
```java
                                    calledFlowDocumentId, 
                                    outboundCallNode.getCalledFlowId(context));
                                if (targetFlow == null && !"".equals(calledFlowDocumentId))
                                {
                                    targetFlow = flowHandler.getFlow(context, "", 
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `impl/src/main/java/org/apache/myfaces/flow/FlowHandlerImpl.java`
#### Snippet
```java
                                    calledFlowDocumentId, 
                                    navCase.getFromOutcome());
                                if (targetFlow == null && !"".equals(calledFlowDocumentId))
                                {
                                    targetFlow = flowHandler.getFlow(context, "", 
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/DefaultFunctionMapper.java`
#### Snippet
```java
        {
            _prefix = in.readUTF();
            if ("".equals(_prefix))
            {
                _prefix = null;
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/html/DefaultTagDecorator.java`
#### Snippet
```java
                             }
                         }
                         else if ("".equals(attributeNS) && attr.getNamespace() == null)
                         {
                             // if namespace is empty match
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/html/DefaultTagDecorator.java`
#### Snippet
```java
                             return this;
                         }
                         else if ("".equals(attributeNS) && attr.getNamespace() == null)
                         {
                             // if namespace is empty match
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlRadioRendererBase.java`
#### Snippet
```java
            boolean itemChecked = (itemStrValue == null) ? 
                    itemStrValue == currentValue : 
                    "".equals(itemStrValue) ? 
                            (currentValue == null || itemStrValue.equals(currentValue)) : 
                            itemStrValue.equals(currentValue);
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `test/src/main/java/org/apache/myfaces/test/mock/MockPartialViewContext.java`
#### Snippet
```java

            if (executeMode != null
                    && !"".equals(executeMode)
                    &&
                    //!PartialViewContext.NO_PARTIAL_PHASE_CLIENT_IDS.equals(executeMode) &&
```

### StringEqualsEmptyString
`equals("")` can be replaced with 'isEmpty()'
in `test/src/main/java/org/apache/myfaces/test/mock/MockPartialViewContext.java`
#### Snippet
```java

            if (renderMode != null
                    && !"".equals(renderMode)
                    &&
                    //!PartialViewContext.NO_PARTIAL_PHASE_CLIENT_IDS.equals(renderMode) &&
```

## RuleId[ruleID=UnnecessaryBoxing]
### UnnecessaryBoxing
Redundant boxing, `Integer.parseInt()` call can be used instead
in `api/src/main/java/org/apache/myfaces/core/api/shared/AttributeUtils.java`
#### Snippet
```java
        // This makes the following code work: <tag attribute="true" />,
        // otherwise you would have to write <tag attribute="#{true}" />.
        return Integer.valueOf(value.toString());
    }
}
```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `api/src/main/java/org/apache/myfaces/core/api/shared/AttributeUtils.java`
#### Snippet
```java
        // This makes the following code work: <tag attribute="true" />,
        // otherwise you would have to write <tag attribute="#{true}" />.
        return Boolean.valueOf(value.toString());
    }

```

### UnnecessaryBoxing
Unnecessary boxing
in `api/src/main/java/jakarta/faces/convert/CharacterConverter.java`
#### Snippet
```java
        try
        {
            return Character.valueOf(value.charAt(0));
        }
        catch(Exception e)
```

### UnnecessaryBoxing
Unnecessary boxing
in `api/src/main/java/jakarta/faces/convert/NumberConverter.java`
#### Snippet
```java
            values[1] = _currencySymbol;
            values[2] = _locale;
            values[3] = _maxFractionDigitsSet ? Integer.valueOf(_maxFractionDigits) : null;
            values[4] = _maxIntegerDigitsSet ? Integer.valueOf(_maxIntegerDigits) : null;
            values[5] = _minFractionDigitsSet ? Integer.valueOf(_minFractionDigits) : null;
```

### UnnecessaryBoxing
Unnecessary boxing
in `api/src/main/java/jakarta/faces/convert/NumberConverter.java`
#### Snippet
```java
            values[2] = _locale;
            values[3] = _maxFractionDigitsSet ? Integer.valueOf(_maxFractionDigits) : null;
            values[4] = _maxIntegerDigitsSet ? Integer.valueOf(_maxIntegerDigits) : null;
            values[5] = _minFractionDigitsSet ? Integer.valueOf(_minFractionDigits) : null;
            values[6] = _minIntegerDigitsSet ? Integer.valueOf(_minIntegerDigits) : null;
```

### UnnecessaryBoxing
Unnecessary boxing
in `api/src/main/java/jakarta/faces/convert/NumberConverter.java`
#### Snippet
```java
            values[3] = _maxFractionDigitsSet ? Integer.valueOf(_maxFractionDigits) : null;
            values[4] = _maxIntegerDigitsSet ? Integer.valueOf(_maxIntegerDigits) : null;
            values[5] = _minFractionDigitsSet ? Integer.valueOf(_minFractionDigits) : null;
            values[6] = _minIntegerDigitsSet ? Integer.valueOf(_minIntegerDigits) : null;
            values[7] = _pattern;
```

### UnnecessaryBoxing
Unnecessary boxing
in `api/src/main/java/jakarta/faces/convert/NumberConverter.java`
#### Snippet
```java
            values[4] = _maxIntegerDigitsSet ? Integer.valueOf(_maxIntegerDigits) : null;
            values[5] = _minFractionDigitsSet ? Integer.valueOf(_minFractionDigits) : null;
            values[6] = _minIntegerDigitsSet ? Integer.valueOf(_minIntegerDigits) : null;
            values[7] = _pattern;
            values[8] = _type;
```

### UnnecessaryBoxing
Unnecessary boxing
in `api/src/main/java/jakarta/faces/validator/DoubleRangeValidator.java`
#### Snippet
```java
    public DoubleRangeValidator(double maximum, double minimum)
    {
        _maximum = new Double(maximum);
        _minimum = new Double(minimum);
    }
```

### UnnecessaryBoxing
Unnecessary boxing
in `api/src/main/java/jakarta/faces/validator/DoubleRangeValidator.java`
#### Snippet
```java
    {
        _maximum = new Double(maximum);
        _minimum = new Double(minimum);
    }

```

### UnnecessaryBoxing
Unnecessary boxing
in `api/src/main/java/jakarta/faces/validator/DoubleRangeValidator.java`
#### Snippet
```java
    public DoubleRangeValidator(double maximum)
    {
        _maximum = new Double(maximum);
    }

```

### UnnecessaryBoxing
Unnecessary boxing
in `api/src/main/java/jakarta/faces/validator/DoubleRangeValidator.java`
#### Snippet
```java
    public void setMaximum(double maximum)
    {
        _maximum = Double.valueOf(maximum);
        clearInitialState();
    }
```

### UnnecessaryBoxing
Unnecessary boxing
in `api/src/main/java/jakarta/faces/validator/DoubleRangeValidator.java`
#### Snippet
```java
    public void setMinimum(double minimum)
    {
        _minimum = Double.valueOf(minimum);
        clearInitialState();
    }
```

### UnnecessaryBoxing
Unnecessary boxing
in `api/src/main/java/jakarta/faces/validator/LongRangeValidator.java`
#### Snippet
```java
                              long minimum)
    {
        _maximum = Long.valueOf(maximum);
        _minimum = Long.valueOf(minimum);
    }
```

### UnnecessaryBoxing
Unnecessary boxing
in `api/src/main/java/jakarta/faces/validator/LongRangeValidator.java`
#### Snippet
```java
    {
        _maximum = Long.valueOf(maximum);
        _minimum = Long.valueOf(minimum);
    }

```

### UnnecessaryBoxing
Unnecessary boxing
in `api/src/main/java/jakarta/faces/validator/LongRangeValidator.java`
#### Snippet
```java
    public LongRangeValidator(long maximum)
    {
        _maximum = Long.valueOf(maximum);
    }

```

### UnnecessaryBoxing
Unnecessary boxing
in `api/src/main/java/jakarta/faces/validator/LongRangeValidator.java`
#### Snippet
```java
    public void setMaximum(long maximum)
    {
        _maximum = Long.valueOf(maximum);
        clearInitialState();
    }
```

### UnnecessaryBoxing
Unnecessary boxing
in `api/src/main/java/jakarta/faces/validator/LongRangeValidator.java`
#### Snippet
```java
    public void setMinimum(long minimum)
    {
        _minimum = Long.valueOf(minimum);
        clearInitialState();
    }
```

### UnnecessaryBoxing
Unnecessary boxing
in `api/src/main/java/jakarta/faces/validator/LengthValidator.java`
#### Snippet
```java
    public void setMinimum(int minimum)
    {
        _minimum = Integer.valueOf(minimum);
        clearInitialState();
    }
```

### UnnecessaryBoxing
Unnecessary boxing
in `api/src/main/java/jakarta/faces/validator/LengthValidator.java`
#### Snippet
```java
    public void setMaximum(int maximum)
    {
        _maximum = Integer.valueOf(maximum);
        clearInitialState();
    }
```

### UnnecessaryBoxing
Unnecessary boxing
in `api/src/main/java/jakarta/faces/application/NavigationCase.java`
#### Snippet
```java
    {
        return hash(_fromViewId) << 4 ^ hash(_fromAction) << 8 ^ hash(_fromOutcome) << 12 ^ hash(_condition) << 16
                ^ hash(_toViewId) << 20 ^ hash(Boolean.valueOf(_redirect)) << 24
                ^ hash(Boolean.valueOf(_includeViewParams));
    }
```

### UnnecessaryBoxing
Unnecessary boxing
in `api/src/main/java/jakarta/faces/application/NavigationCase.java`
#### Snippet
```java
        return hash(_fromViewId) << 4 ^ hash(_fromAction) << 8 ^ hash(_fromOutcome) << 12 ^ hash(_condition) << 16
                ^ hash(_toViewId) << 20 ^ hash(Boolean.valueOf(_redirect)) << 24
                ^ hash(Boolean.valueOf(_includeViewParams));
    }

```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `impl/src/main/java/org/apache/myfaces/util/NavigationUtils.java`
#### Snippet
```java
                if (includeViewParamsAttribute != null)
                {
                    includeViewParams = Boolean.valueOf(includeViewParamsAttribute);
                }
                apiCases.add(new NavigationCase(rule.getFromViewId(),
```

### UnnecessaryBoxing
Unnecessary boxing
in `impl/src/main/java/org/apache/myfaces/util/token/CsrfSessionTokenFactoryRandom.java`
#### Snippet
```java
            if (sequence == null || sequence == Integer.MAX_VALUE)
            {
                sequence = Integer.valueOf(1);
            }
            else
```

### UnnecessaryBoxing
Unnecessary boxing
in `impl/src/main/java/org/apache/myfaces/util/token/CsrfSessionTokenFactoryRandom.java`
#### Snippet
```java
            else
            {
                sequence = Integer.valueOf(sequence.intValue() + 1);
            }
            map.put(RendererUtils.SEQUENCE_PARAM, sequence);
```

### UnnecessaryBoxing
Unnecessary boxing
in `impl/src/main/java/org/apache/myfaces/util/token/SessionIdGenerator.java`
#### Snippet
```java
            {
                log.info("Creation of SecureRandom instance for session ID generation using ["
                        +result.getAlgorithm()+"] took ["+Long.valueOf(t2 - t1)+"] milliseconds.");
            }
        }
```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/TagAttributeImpl.java`
#### Snippet
```java
        if ((this.capabilities & EL_LITERAL) != 0)
        {
            return Boolean.valueOf(this.value);
        }
        else
```

### UnnecessaryBoxing
Unnecessary boxing
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/LegacyForEachHandler.java`
#### Snippet
```java
        int e = this.getEnd(ctx);
        int m = this.getStep(ctx);
        Integer sO = this.begin != null ? Integer.valueOf(s) : null;
        Integer eO = this.end != null ? Integer.valueOf(e) : null;
        Integer mO = this.step != null ? Integer.valueOf(m) : null;
```

### UnnecessaryBoxing
Unnecessary boxing
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/LegacyForEachHandler.java`
#### Snippet
```java
        int m = this.getStep(ctx);
        Integer sO = this.begin != null ? Integer.valueOf(s) : null;
        Integer eO = this.end != null ? Integer.valueOf(e) : null;
        Integer mO = this.step != null ? Integer.valueOf(m) : null;

```

### UnnecessaryBoxing
Unnecessary boxing
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/LegacyForEachHandler.java`
#### Snippet
```java
        Integer sO = this.begin != null ? Integer.valueOf(s) : null;
        Integer eO = this.end != null ? Integer.valueOf(e) : null;
        Integer mO = this.step != null ? Integer.valueOf(m) : null;

        boolean t = this.getTransient(ctx);
```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeTagAttributeUtils.java`
#### Snippet
```java
        if (expert != null)
        {
            descriptor.setExpert(Boolean.valueOf(expert.getValue()));
        }
        if (hidden != null)
```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeTagAttributeUtils.java`
#### Snippet
```java
        if (hidden != null)
        {
            descriptor.setHidden(Boolean.valueOf(hidden.getValue()));
        }
        if (preferred != null)
```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeTagAttributeUtils.java`
#### Snippet
```java
        if (preferred != null)
        {
            descriptor.setPreferred(Boolean.valueOf(preferred.getValue()));
        }
    }
```

### UnnecessaryBoxing
Unnecessary boxing
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/ForEachHandler.java`
#### Snippet
```java
        int e = this.getEnd(ctx);
        int m = this.getStep(ctx);
        Integer sO = this.begin != null ? Integer.valueOf(s) : null;
        Integer eO = this.end != null ? Integer.valueOf(e) : null;
        Integer mO = this.step != null ? Integer.valueOf(m) : null;
```

### UnnecessaryBoxing
Unnecessary boxing
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/ForEachHandler.java`
#### Snippet
```java
        int m = this.getStep(ctx);
        Integer sO = this.begin != null ? Integer.valueOf(s) : null;
        Integer eO = this.end != null ? Integer.valueOf(e) : null;
        Integer mO = this.step != null ? Integer.valueOf(m) : null;
        boolean t = this.getTransient(ctx);
```

### UnnecessaryBoxing
Unnecessary boxing
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/ForEachHandler.java`
#### Snippet
```java
        Integer sO = this.begin != null ? Integer.valueOf(s) : null;
        Integer eO = this.end != null ? Integer.valueOf(e) : null;
        Integer mO = this.step != null ? Integer.valueOf(m) : null;
        boolean t = this.getTransient(ctx);

```

### UnnecessaryBoxing
Unnecessary boxing
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/ForEachHandler.java`
#### Snippet
```java
        int e = this.getEnd(ctx);
        int m = this.getStep(ctx);
        Integer sO = this.begin != null ? Integer.valueOf(s) : null;
        Integer eO = this.end != null ? Integer.valueOf(e) : null;
        Integer mO = this.step != null ? Integer.valueOf(m) : null;
```

### UnnecessaryBoxing
Unnecessary boxing
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/ForEachHandler.java`
#### Snippet
```java
        int m = this.getStep(ctx);
        Integer sO = this.begin != null ? Integer.valueOf(s) : null;
        Integer eO = this.end != null ? Integer.valueOf(e) : null;
        Integer mO = this.step != null ? Integer.valueOf(m) : null;
        boolean t = this.getTransient(ctx);
```

### UnnecessaryBoxing
Unnecessary boxing
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/ForEachHandler.java`
#### Snippet
```java
        Integer sO = this.begin != null ? Integer.valueOf(s) : null;
        Integer eO = this.end != null ? Integer.valueOf(e) : null;
        Integer mO = this.step != null ? Integer.valueOf(m) : null;
        boolean t = this.getTransient(ctx);

```

### UnnecessaryBoxing
Unnecessary boxing
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/ForEachHandler.java`
#### Snippet
```java
        int e = this.getEnd(ctx);
        int m = this.getStep(ctx);
        Integer sO = this.begin != null ? Integer.valueOf(s) : null;
        Integer eO = this.end != null ? Integer.valueOf(e) : null;
        Integer mO = this.step != null ? Integer.valueOf(m) : null;
```

### UnnecessaryBoxing
Unnecessary boxing
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/ForEachHandler.java`
#### Snippet
```java
        int m = this.getStep(ctx);
        Integer sO = this.begin != null ? Integer.valueOf(s) : null;
        Integer eO = this.end != null ? Integer.valueOf(e) : null;
        Integer mO = this.step != null ? Integer.valueOf(m) : null;
        boolean t = this.getTransient(ctx);
```

### UnnecessaryBoxing
Unnecessary boxing
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/ForEachHandler.java`
#### Snippet
```java
        Integer sO = this.begin != null ? Integer.valueOf(s) : null;
        Integer eO = this.end != null ? Integer.valueOf(e) : null;
        Integer mO = this.step != null ? Integer.valueOf(m) : null;
        boolean t = this.getTransient(ctx);
        IterationState iterationState = new IterationState();
```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `impl/src/main/java/org/apache/myfaces/view/facelets/pool/impl/ViewPoolImpl.java`
#### Snippet
```java
                parameters.get(INIT_PARAM_VIEW_POOL_DEFERRED_NAVIGATION) :
                "false");
        deferredNavigation = Boolean.valueOf(deferredNavigationVal);
        
        staticStructureViewMetadataMap = new ConcurrentHashMap<>();
```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/SAXCompiler.java`
#### Snippet
```java
            {
                processAs = entry.getProcessAs();
                compressSpaces = Boolean.valueOf(entry.getOamCompressSpaces());
                break;
            }
```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `impl/src/main/java/org/apache/myfaces/config/DefaultFacesConfigurationMerger.java`
#### Snippet
```java
        if(webAppFacesConfig != null)
        {
            metadataComplete = Boolean.valueOf(webAppFacesConfig.getMetadataComplete());
        }
        else
```

### UnnecessaryBoxing
Redundant boxing, `Integer.parseInt()` call can be used instead
in `impl/src/main/java/org/apache/myfaces/resource/ResourceLoader.java`
#### Snippet
```java
                else
                {
                    n1 = Integer.valueOf(o1.substring(0, i1));
                    o1 = o1.substring(i1 + 1);
                }
```

### UnnecessaryBoxing
Redundant boxing, `Integer.parseInt()` call can be used instead
in `impl/src/main/java/org/apache/myfaces/resource/ResourceLoader.java`
#### Snippet
```java
                    {
                        p2 = false;
                        n2 = Integer.valueOf(o2);
                        o2 = "";
                    }
```

### UnnecessaryBoxing
Redundant boxing, `Integer.parseInt()` call can be used instead
in `impl/src/main/java/org/apache/myfaces/resource/ResourceLoader.java`
#### Snippet
```java
                else
                {
                    n2 = Integer.valueOf(o2.substring(0, i2));
                    o2 = o2.substring(i2 + 1);
                }
```

### UnnecessaryBoxing
Unnecessary boxing
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlResponseStateManager.java`
#### Snippet
```java
        if (count == null)
        {
            count = Integer.valueOf(1);
        }
        else
```

### UnnecessaryBoxing
Unnecessary boxing
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlResponseStateManager.java`
#### Snippet
```java
        if (count == null)
        {
            count = Integer.valueOf(1);
        }
        else
```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTextareaRendererBase.java`
#### Snippet
```java
            if (addNewLineAtStart instanceof String)
            {
                addNewLineAtStartBoolean = Boolean.valueOf((String)addNewLineAtStart);
            }
            else if (addNewLineAtStart instanceof Boolean)
```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/SelectItemsUtils.java`
#### Snippet
```java
        if (obj instanceof String)
        {
            return Boolean.valueOf((String) obj);
        }
        if (!(obj instanceof Boolean))
```

### UnnecessaryBoxing
Unnecessary boxing
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTableRendererBase.java`
#### Snippet
```java
        if (colspan > 1)
        {
            writer.writeAttribute(HTML.COLSPAN_ATTR, Integer.valueOf(colspan), null);
        }
        if (headerStyleClass != null)
```

### UnnecessaryBoxing
Unnecessary boxing
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTableRendererBase.java`
#### Snippet
```java
        if (colspan > 1)
        {
            writer.writeAttribute(HTML.COLSPAN_ATTR, Integer.valueOf(colspan), null);
        }
        if (footerStyleClass != null)
```

### UnnecessaryBoxing
Unnecessary boxing
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlTableRendererBase.java`
#### Snippet
```java
        if (totalColumns > 0)
        {
            writer.writeAttribute(HTML.COLSPAN_ATTR, Integer.valueOf(totalColumns), null);
        }
        if (styleClass != null)
```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `impl/src/main/java/org/apache/myfaces/application/NavigationHandlerImpl.java`
#### Snippet
```java
                if (includeViewParamsAttribute != null)
                {
                    includeViewParams = Boolean.valueOf(includeViewParamsAttribute);
                }
                apiCases.add(new NavigationCase(rule.getFromViewId(),configCase.getFromAction(),
```

### UnnecessaryBoxing
Unnecessary boxing
in `test/src/main/java/org/apache/myfaces/test/el/MockExpressionFactory.java`
#### Snippet
```java
            if (object == null)
            {
                return new Character((char) 0);
            }
            else if ("".equals(object))
```

### UnnecessaryBoxing
Unnecessary boxing
in `test/src/main/java/org/apache/myfaces/test/el/MockExpressionFactory.java`
#### Snippet
```java
            else if ("".equals(object))
            {
                return new Character((char) 0);
            }
            else if (object instanceof String)
```

### UnnecessaryBoxing
Unnecessary boxing
in `test/src/main/java/org/apache/myfaces/test/el/MockExpressionFactory.java`
#### Snippet
```java
            else if (object instanceof String)
            {
                return new Character(((String) object).charAt(0));
            }
            else if (isNumeric(object.getClass()))
```

### UnnecessaryBoxing
Unnecessary boxing
in `test/src/main/java/org/apache/myfaces/test/el/MockExpressionFactory.java`
#### Snippet
```java
            else if (isNumeric(object.getClass()))
            {
                return new Character((char) ((Number) object).shortValue());
            }
            else if ((object instanceof Character)
```

### UnnecessaryBoxing
Unnecessary boxing
in `test/src/main/java/org/apache/myfaces/test/el/MockExpressionFactory.java`
#### Snippet
```java
        if ((type == Byte.TYPE) || (type == Byte.class))
        {
            return new Byte(value.byteValue());
        }
        else if ((type == Double.TYPE) || (type == Double.class))
```

### UnnecessaryBoxing
Unnecessary boxing
in `test/src/main/java/org/apache/myfaces/test/el/MockExpressionFactory.java`
#### Snippet
```java
        else if ((type == Double.TYPE) || (type == Double.class))
        {
            return new Double(value.doubleValue());
        }
        else if ((type == Float.TYPE) || (type == Float.class))
```

### UnnecessaryBoxing
Unnecessary boxing
in `test/src/main/java/org/apache/myfaces/test/el/MockExpressionFactory.java`
#### Snippet
```java
        else if ((type == Float.TYPE) || (type == Float.class))
        {
            return new Float(value.floatValue());
        }
        else if ((type == Integer.TYPE) || (type == Integer.class))
```

### UnnecessaryBoxing
Unnecessary boxing
in `test/src/main/java/org/apache/myfaces/test/el/MockExpressionFactory.java`
#### Snippet
```java
        else if ((type == Integer.TYPE) || (type == Integer.class))
        {
            return new Integer(value.intValue());
        }
        else if ((type == Long.TYPE) || (type == Long.class))
```

### UnnecessaryBoxing
Unnecessary boxing
in `test/src/main/java/org/apache/myfaces/test/el/MockExpressionFactory.java`
#### Snippet
```java
        else if ((type == Long.TYPE) || (type == Long.class))
        {
            return new Long(value.longValue());
        }
        else if ((type == Short.TYPE) || (type == Short.class))
```

### UnnecessaryBoxing
Unnecessary boxing
in `test/src/main/java/org/apache/myfaces/test/el/MockExpressionFactory.java`
#### Snippet
```java
        else if ((type == Short.TYPE) || (type == Short.class))
        {
            return new Short(value.shortValue());
        }
        else if (type == BigDecimal.class)
```

### UnnecessaryBoxing
Unnecessary boxing
in `test/src/main/java/org/apache/myfaces/test/el/MockExpressionFactory.java`
#### Snippet
```java
     * <p>Literal numeric value for zero.</p>
     */
    private static final Integer ZERO = new Integer(0);

    // ----------------------------------------------------- Mock Object Methods
```

### UnnecessaryBoxing
Redundant boxing, `Integer.parseInt()` call can be used instead
in `test/src/main/java/org/apache/myfaces/test/mock/resource/MockResourceLoader.java`
#### Snippet
```java
                else
                {
                    n1 = Integer.valueOf(o1.substring(0, i1));
                    o1 = o1.substring(i1 + 1);
                }
```

### UnnecessaryBoxing
Redundant boxing, `Integer.parseInt()` call can be used instead
in `test/src/main/java/org/apache/myfaces/test/mock/resource/MockResourceLoader.java`
#### Snippet
```java
                    {
                        p2 = false;
                        n2 = Integer.valueOf(o2);
                        o2 = "";
                    }
```

### UnnecessaryBoxing
Redundant boxing, `Integer.parseInt()` call can be used instead
in `test/src/main/java/org/apache/myfaces/test/mock/resource/MockResourceLoader.java`
#### Snippet
```java
                else
                {
                    n2 = Integer.valueOf(o2.substring(0, i2));
                    o2 = o2.substring(i2 + 1);
                }
```

### UnnecessaryBoxing
Redundant boxing, `Integer.parseInt()` call can be used instead
in `test/src/main/java/org/apache/myfaces/test/mock/resource/MockSimpleResourceHandler.java`
#### Snippet
```java
            else
            {
                n1 = Integer.valueOf(o1.substring(0, i1));
                o1 = o1.substring(i1 + 1);
            }
```

### UnnecessaryBoxing
Redundant boxing, `Integer.parseInt()` call can be used instead
in `test/src/main/java/org/apache/myfaces/test/mock/resource/MockSimpleResourceHandler.java`
#### Snippet
```java
                {
                    p2 = false;
                    n2 = Integer.valueOf(o2);
                    o2 = "";
                }
```

### UnnecessaryBoxing
Redundant boxing, `Integer.parseInt()` call can be used instead
in `test/src/main/java/org/apache/myfaces/test/mock/resource/MockSimpleResourceHandler.java`
#### Snippet
```java
            else
            {
                n2 = Integer.valueOf(o2.substring(0, i2));
                o2 = o2.substring(i2 + 1);
            }
```

### UnnecessaryBoxing
Redundant boxing, `Boolean.parseBoolean()` call can be used instead
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/HtmlRendererUtils.java`
#### Snippet
```java
        if (obj instanceof String)
        {
            return Boolean.valueOf((String) obj);
        }
        if (!(obj instanceof Boolean))
```

## RuleId[ruleID=SynchronizeOnNonFinalField]
### SynchronizeOnNonFinalField
Synchronization on a non-final field `registeredFactoryNames`
in `api/src/main/java/jakarta/faces/FactoryFinder.java`
#### Snippet
```java
        ClassLoader classLoader = ClassUtils.getContextClassLoader();
        Map<String, List<String>> factoryClassNames = null;
        synchronized (registeredFactoryNames)
        {
            Map<String, Object> factories = FactoryFinder.factories.get(classLoader);
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `registeredFactoryNames`
in `api/src/main/java/jakarta/faces/FactoryFinder.java`
#### Snippet
```java
        Map<String, Object> factoryMap = null;

        synchronized (registeredFactoryNames)
        {
            factoryClassNames = registeredFactoryNames.get(classLoader);
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `registeredFactoryNames`
in `api/src/main/java/jakarta/faces/FactoryFinder.java`
#### Snippet
```java
        Map<String, Object> factoryMap;
        // This code must be synchronized
        synchronized (registeredFactoryNames)
        {
            factoryMap = factories.remove(classLoader);
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `clientWindowExpirationStack`
in `impl/src/main/java/org/apache/myfaces/cdi/clientwindow/ClientWindowScopeContextualStorageHolder.java`
#### Snippet
```java
        if (clientWindow != null && clientWindow.getId() != null)
        {
            synchronized (clientWindowExpirationStack)
            {
                clientWindowExpirationStack.remove(clientWindow.getId());
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `declaredFieldBeans`
in `impl/src/main/java/org/apache/myfaces/spi/impl/ResourceAnnotationInjectionProvider.java`
#### Snippet
```java
            // Ensure thread-safe put over _metadata, and only create one map
            // per classloader to hold metadata.
            synchronized (declaredFieldBeans)
            {
                metadata = (Map<Class,Field[]>) declaredFieldBeans.get(cl);
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `declaredMethodBeans`
in `impl/src/main/java/org/apache/myfaces/spi/impl/NoInjectionAnnotationInjectionProvider.java`
#### Snippet
```java
            // Ensure thread-safe put over _metadata, and only create one map
            // per classloader to hold metadata.
            synchronized (declaredMethodBeans)
            {
                metadata = createDeclaredMethodBeansMap(cl, metadata);
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `clientWindowExpirationStack`
in `impl/src/main/java/org/apache/myfaces/flow/cdi/FlowScopeContextualStorageHolder.java`
#### Snippet
```java
        if (cw != null && cw.getId() != null)
        {
            synchronized (clientWindowExpirationStack)
            {
                clientWindowExpirationStack.remove(cw.getId());
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `sessionMap`
in `impl/src/main/java/org/apache/myfaces/push/cdi/WebsocketSessionManager.java`
#### Snippet
```java
                = new ConcurrentLRUCache<>((size * 4 + 3) / 3, size);
        
        synchronized (sessionMap)
        {
            if (sessionMap.isInitialized())
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `MetaRulesetImpl.metadata`
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/MetaRulesetImpl.java`
#### Snippet
```java
            // Ensure thread-safe put over _metadata, and only create one map
            // per classloader to hold metadata.
            synchronized (MetaRulesetImpl.metadata)
            {
                metadata = MetaRulesetImpl.metadata.computeIfAbsent(cl,
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `_elContextListeners`
in `impl/src/main/java/org/apache/myfaces/application/ApplicationImpl.java`
#### Snippet
```java
    public final void removeELContextListener(final ELContextListener listener)
    {
        synchronized (_elContextListeners)
        {
            _elContextListeners.remove(listener);
```

### SynchronizeOnNonFinalField
Synchronization on a non-final field `_elContextListeners`
in `impl/src/main/java/org/apache/myfaces/application/ApplicationImpl.java`
#### Snippet
```java
    public final void addELContextListener(final ELContextListener listener)
    {
        synchronized (_elContextListeners)
        {
            _elContextListeners.add(listener);
```

## RuleId[ruleID=MissingDeprecatedAnnotation]
### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/ConvertDateTimeHandler.java`
#### Snippet
```java
        bodyContent = "empty", 
        converterClass="jakarta.faces.convert.DateTimeConverter")
public final class ConvertDateTimeHandler extends ConverterHandler
{
    private final TagAttribute dateStyle;
```

### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletResponse.java`
#### Snippet
```java
     *  an instance of <code>MockPrintWriter</code> for you
     */
    public void setWriter(PrintWriter writer)
    {
        this.writer = writer;
```

### MissingDeprecatedAnnotation
Missing '@Deprecated' annotation
in `test/src/main/java/org/apache/myfaces/test/mock/MockHttpServletResponse.java`
#### Snippet
```java
     *  return an instance of <code>MockServletOutputStream</code> for you
     */
    public void setOutputStream(ServletOutputStream stream)
    {
        this.stream = stream;
```

## RuleId[ruleID=ConditionCoveredByFurtherCondition]
### ConditionCoveredByFurtherCondition
Condition 'label != null' covered by subsequent condition 'label instanceof String'
in `api/src/main/java/org/apache/myfaces/core/api/shared/MessageUtils.java`
#### Snippet
```java
        Object label = component.getAttributes().get("label");
        ValueExpression expression = null;
        if (label != null && 
            label instanceof String && ((String)label).length() == 0 )
        {
```

### ConditionCoveredByFurtherCondition
Condition '!(child instanceof UISelectItem)' covered by subsequent condition '!(child instanceof UISelectItems)'
in `api/src/main/java/org/apache/myfaces/core/api/shared/SelectItemsIterator.java`
#### Snippet
```java
            // classes are found. If there is no more elements
            // that conform this condition, just return false.
            while (!(child instanceof UISelectItem) && !(child instanceof UISelectItems))
            {
                // Try to skip it
```

### ConditionCoveredByFurtherCondition
Condition 'value != null' covered by subsequent condition 'value instanceof UIComponent'
in `api/src/main/java/jakarta/faces/component/_ComponentFacetMap.java`
#### Snippet
```java
                    Object value = ((Map.Entry<String, V>) o).getValue();
                    
                    if (value != null && value instanceof UIComponent)
                    {
                        ((UIComponent) value).setParent(null);
```

### ConditionCoveredByFurtherCondition
Condition 'event != null' covered by subsequent condition 'event instanceof ActionEvent'
in `api/src/main/java/jakarta/faces/component/UICommand.java`
#### Snippet
```java
    public void queueEvent(FacesEvent event)
    {
        if (event != null && event instanceof ActionEvent)
        {
            UIComponent component = event.getComponent();
```

### ConditionCoveredByFurtherCondition
Condition 'helper != null' covered by subsequent condition 'helper instanceof _DeltaStateHelper'
in `api/src/main/java/jakarta/faces/component/UIInput.java`
#### Snippet
```java
    {
        StateHelper helper = getStateHelper(false);
        if (helper != null && helper instanceof _DeltaStateHelper)
        {
            ((_DeltaStateHelper)helper).markPropertyInInitialState(INITIAL_STATE_PROPERTIES);
```

### ConditionCoveredByFurtherCondition
Condition 'value != null' covered by subsequent condition 'value instanceof ValueExpression'
in `api/src/main/java/jakarta/faces/component/_ComponentAttributesMap.java`
#### Snippet
```java
                            // here, because in the PropertyDescriptor the default values are
                            // always stored as (Tag-)ValueExpressions.
                            if (value != null && value instanceof ValueExpression)
                            {
                                return ((ValueExpression) value).getValue(_component.getFacesContext().getELContext());
```

### ConditionCoveredByFurtherCondition
Condition 'base != null' covered by subsequent condition 'base instanceof ResourceHandler'
in `impl/src/main/java/org/apache/myfaces/el/resolver/ResourceResolver.java`
#### Snippet
```java
            final Object property)
    {
        if (base != null && property != null && base instanceof ResourceHandler)
        {
            String reference = (String) property;
```

### ConditionCoveredByFurtherCondition
Condition 'obj != null' covered by subsequent condition 'obj instanceof ValueExpression'
in `impl/src/main/java/org/apache/myfaces/el/resolver/CompositeComponentELResolver.java`
#### Snippet
```java
            // here, because in the PropertyDescriptor the default values are
            // always stored as (Tag-)ValueExpressions.
            if (obj != null && obj instanceof ValueExpression)
            {
                return ((ValueExpression) obj).getValue(FacesContext.getCurrentInstance().getELContext());
```

### ConditionCoveredByFurtherCondition
Condition 'base != null' covered by subsequent condition 'base instanceof UIComponent'
in `impl/src/main/java/org/apache/myfaces/el/resolver/CompositeComponentELResolver.java`
#### Snippet
```java
        // component.  Property must be a String.

        if (base != null
                && base instanceof UIComponent
                && property != null
```

### ConditionCoveredByFurtherCondition
Condition 'base != null' covered by subsequent condition 'base instanceof CompositeComponentAttributesMapWrapper'
in `impl/src/main/java/org/apache/myfaces/el/resolver/CompositeComponentELResolver.java`
#### Snippet
```java
    public Class<?> getType(ELContext context, Object base, Object property)
    {
        if (base != null
                && property != null
                && base instanceof CompositeComponentAttributesMapWrapper
```

### ConditionCoveredByFurtherCondition
Condition 'property != null' covered by subsequent condition 'property instanceof String'
in `impl/src/main/java/org/apache/myfaces/el/resolver/CompositeComponentELResolver.java`
#### Snippet
```java
    {
        if (base != null
                && property != null
                && base instanceof CompositeComponentAttributesMapWrapper
                && property instanceof String)
```

### ConditionCoveredByFurtherCondition
Condition 'node != null' covered by subsequent condition 'node instanceof FlowCallNode'
in `impl/src/main/java/org/apache/myfaces/flow/FlowHandlerImpl.java`
#### Snippet
```java
                    {
                        node = activeFlow != null ? activeFlow.getNode(flowIdRequestParam) : null;
                        if (node != null && node instanceof FlowCallNode)
                        {
                            outboundCallNode = (FlowCallNode) node;
```

### ConditionCoveredByFurtherCondition
Condition 'node != null' covered by subsequent condition 'node instanceof FlowCallNode'
in `impl/src/main/java/org/apache/myfaces/flow/FlowHandlerImpl.java`
#### Snippet
```java
                            currentFlow = flowHandler.getCurrentFlow(context);
                            node = currentFlow.getNode(navCase.getFromOutcome());
                            if (node != null && node instanceof FlowCallNode)
                            {
                                outboundCallNode = (FlowCallNode) node;
```

### ConditionCoveredByFurtherCondition
Condition 'label != null' covered by subsequent condition 'label instanceof String'
in `impl/src/main/java/org/apache/myfaces/util/MessageUtils.java`
#### Snippet
```java
        Object label = component.getAttributes().get("label");
        ValueExpression expression = null;
        if (label != null && 
            label instanceof String && ((String)label).length() == 0 )
        {
```

### ConditionCoveredByFurtherCondition
Condition 'meOrVe != null' covered by subsequent condition 'meOrVe instanceof ValueExpression'
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ValueExpressionMethodExpression.java`
#### Snippet
```java
        else if (meOrVe instanceof ValueExpression)
        {
            while (meOrVe != null && meOrVe instanceof ValueExpression)
            {
                meOrVe = ((ValueExpression)meOrVe).getValue(context);
```

### ConditionCoveredByFurtherCondition
Condition 'meOrVe != null' covered by subsequent condition 'meOrVe instanceof ValueExpression'
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/RedirectMethodExpressionValueExpressionValueChangeListener.java`
#### Snippet
```java
        else if (meOrVe instanceof ValueExpression)
        {
            while (meOrVe != null && meOrVe instanceof ValueExpression)
            {
                meOrVe = ((ValueExpression)meOrVe).getValue(context);
```

### ConditionCoveredByFurtherCondition
Condition 'meOrVe != null' covered by subsequent condition 'meOrVe instanceof ValueExpression'
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/RedirectMethodExpressionValueExpressionValidator.java`
#### Snippet
```java
        else if (meOrVe instanceof ValueExpression)
        {
            while (meOrVe != null && meOrVe instanceof ValueExpression)
            {
                meOrVe = ((ValueExpression)meOrVe).getValue(context);
```

### ConditionCoveredByFurtherCondition
Condition 'meOrVe != null' covered by subsequent condition 'meOrVe instanceof ValueExpression'
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/RedirectMethodExpressionValueExpressionActionListener.java`
#### Snippet
```java
        else if (meOrVe instanceof ValueExpression)
        {
            while (meOrVe != null && meOrVe instanceof ValueExpression)
            {
                meOrVe = ((ValueExpression)meOrVe).getValue(context);
```

### ConditionCoveredByFurtherCondition
Condition 'currentTarget instanceof EditableValueHolderAttachedObjectTarget \&\& currentHandler instanceof Editab ...' covered by subsequent condition 'currentTarget instanceof ValueHolderAttachedObjectTarget \&\& currentHandler instanceof ValueHolderAtt ...'
in `impl/src/main/java/org/apache/myfaces/view/facelets/FaceletViewDeclarationLanguage.java`
#### Snippet
```java
                        ((currentTarget instanceof ActionSource2AttachedObjectTarget &&
                                currentHandler instanceof ActionSource2AttachedObjectHandler) ||
                                (currentTarget instanceof EditableValueHolderAttachedObjectTarget &&
                                        currentHandler instanceof EditableValueHolderAttachedObjectHandler) ||
                                (currentTarget instanceof ValueHolderAttachedObjectTarget &&
                                        currentHandler instanceof ValueHolderAttachedObjectHandler)))
```

### ConditionCoveredByFurtherCondition
Condition 'requestType != null' covered by subsequent condition 'PARTIAL_AJAX.equals(...)'
in `impl/src/main/java/org/apache/myfaces/context/servlet/PartialViewContextImpl.java`
#### Snippet
```java
        {
            String requestType = context.getExternalContext().getRequestHeaderMap().get(FACES_REQUEST);
            _ajaxRequest = (requestType != null && PARTIAL_AJAX.equals(requestType));
            String reqParmamterPartialAjax = context.getExternalContext()
                    .getRequestParameterMap().get(PARTIAL_AJAX_REQ);
```

### ConditionCoveredByFurtherCondition
Condition 'requestType != null' covered by subsequent condition 'PARTIAL_PROCESS.equals(...)'
in `impl/src/main/java/org/apache/myfaces/context/servlet/PartialViewContextImpl.java`
#### Snippet
```java
        {
            String requestType = context.getExternalContext().getRequestHeaderMap().get(FACES_REQUEST);
            _partialRequest = (requestType != null && PARTIAL_PROCESS.equals(requestType));
        }
        return _partialRequest || isAjaxRequest();
```

### ConditionCoveredByFurtherCondition
Condition 'libraryName != null' covered by subsequent condition 'ResourceHandler.FACES_SCRIPT_LIBRARY_NAME.equals(...)'
in `impl/src/main/java/org/apache/myfaces/resource/InternalClassLoaderResourceLoader.java`
#### Snippet
```java
    {
        //handle faces.js
        final boolean jakartaFacesLib = libraryName != null &&
                ResourceHandler.FACES_SCRIPT_LIBRARY_NAME.equals(libraryName);
        final boolean jakartaFaces = jakartaFacesLib &&
```

### ConditionCoveredByFurtherCondition
Condition 'noscript != null' covered by subsequent condition '"true".equals(...)'
in `impl/src/main/java/org/apache/myfaces/lifecycle/clientwindow/CODIClientSideWindow.java`
#### Snippet
```java
        String noscript = externalContext.getRequestParameterMap().get(NOSCRIPT_PARAMETER);

        return (noscript != null && "true".equals(noscript));
    }

```

### ConditionCoveredByFurtherCondition
Condition 'submittedValue != null' covered by subsequent condition '"".equals(...)'
in `impl/src/main/java/org/apache/myfaces/renderkit/RendererUtils.java`
#### Snippet
```java
        //[  Permit the passing of a null value to SelectItem.setValue()  ]
        //If submittedValue == "" then convert to null.
        if (submittedValue != null && "".equals(submittedValue))
        {
            //Replace "" by null value
```

### ConditionCoveredByFurtherCondition
Condition 'form != null' covered by subsequent condition 'form instanceof HtmlForm'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlInputFileRendererBase.java`
#### Snippet
```java
        {
            UIForm form = ComponentUtils.findClosest(UIForm.class, component);
            if (form != null && form instanceof HtmlForm)
            {
                String content = ((HtmlForm) form).getEnctype();
```

### ConditionCoveredByFurtherCondition
Condition 'resourceVersion != null' covered by subsequent condition 'ResourceLoader.VERSION_INVALID.equals(...)'
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
            resourceVersion = resourceLoader
                    .getResourceVersion(localePrefix + '/'+ resourceName);
            if (!(resourceVersion != null && ResourceLoader.VERSION_INVALID.equals(resourceVersion)))
            {
                resourceMeta = resourceLoader.createResourceMeta(localePrefix, null, null,
```

### ConditionCoveredByFurtherCondition
Condition 'resourceVersion != null' covered by subsequent condition 'ResourceLoader.VERSION_INVALID.equals(...)'
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
        {
            resourceVersion = resourceLoader.getResourceVersion(resourceName);
            if (!(resourceVersion != null && ResourceLoader.VERSION_INVALID.equals(resourceVersion)))
            {
                resourceMeta = resourceLoader.createResourceMeta(null, null, null, resourceName, resourceVersion);
```

### ConditionCoveredByFurtherCondition
Condition 'resourceVersion != null' covered by subsequent condition 'ResourceLoader.VERSION_INVALID.equals(...)'
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
            resourceVersion = resourceLoader
                    .getResourceVersion(localePrefix + '/'+ resourceName, contractName);
            if (!(resourceVersion != null && ResourceLoader.VERSION_INVALID.equals(resourceVersion)))
            {
                resourceMeta = resourceLoader.createResourceMeta(localePrefix, null, null,
```

### ConditionCoveredByFurtherCondition
Condition 'resourceVersion != null' covered by subsequent condition 'ResourceLoader.VERSION_INVALID.equals(...)'
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
        {
            resourceVersion = resourceLoader.getResourceVersion(resourceName, contractName);
            if (!(resourceVersion != null && ResourceLoader.VERSION_INVALID.equals(resourceVersion)))
            {
                resourceMeta = resourceLoader.createResourceMeta(null, null, null,
```

### ConditionCoveredByFurtherCondition
Condition 'resourceVersion != null' covered by subsequent condition 'ResourceLoader.VERSION_INVALID.equals(...)'
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
                }

                if (!(resourceVersion != null && ResourceLoader.VERSION_INVALID.equals(resourceVersion)))
                {
                    resourceId = resourceLoader.createResourceMeta(localePrefix, libraryName,
```

### ConditionCoveredByFurtherCondition
Condition 'resourceVersion != null' covered by subsequent condition 'ResourceLoader.VERSION_INVALID.equals(...)'
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
            {
                resourceVersion = resourceLoader.getResourceVersion(localePrefix + '/'+ resourceName);
                if (!(resourceVersion != null && ResourceLoader.VERSION_INVALID.equals(resourceVersion)))
                {               
                    resourceId = resourceLoader.createResourceMeta(localePrefix, null, null,
```

### ConditionCoveredByFurtherCondition
Condition 'resourceVersion != null' covered by subsequent condition 'ResourceLoader.VERSION_INVALID.equals(...)'
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
                }

                if (!(resourceVersion != null && ResourceLoader.VERSION_INVALID.equals(resourceVersion)))
                {               
                    resourceId = resourceLoader.createResourceMeta(null, libraryName,
```

### ConditionCoveredByFurtherCondition
Condition 'resourceVersion != null' covered by subsequent condition 'ResourceLoader.VERSION_INVALID.equals(...)'
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
                resourceVersion = resourceLoader.getResourceVersion(resourceName);
                
                if (!(resourceVersion != null && ResourceLoader.VERSION_INVALID.equals(resourceVersion)))
                {               
                    resourceId = resourceLoader.createResourceMeta(null, null, null,
```

### ConditionCoveredByFurtherCondition
Condition 'resourceVersion != null' covered by subsequent condition 'ResourceLoader.VERSION_INVALID.equals(...)'
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
                }

                if (!(resourceVersion != null && ResourceLoader.VERSION_INVALID.equals(resourceVersion)))
                {
                    resourceId = resourceLoader.createResourceMeta(localePrefix, libraryName,
```

### ConditionCoveredByFurtherCondition
Condition 'resourceVersion != null' covered by subsequent condition 'ResourceLoader.VERSION_INVALID.equals(...)'
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
            {
                resourceVersion = resourceLoader.getResourceVersion(localePrefix + '/'+ resourceName, contractName);
                if (!(resourceVersion != null && ResourceLoader.VERSION_INVALID.equals(resourceVersion)))
                {               
                    resourceId = resourceLoader.createResourceMeta(localePrefix, null, null,
```

### ConditionCoveredByFurtherCondition
Condition 'resourceVersion != null' covered by subsequent condition 'ResourceLoader.VERSION_INVALID.equals(...)'
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
                }

                if (!(resourceVersion != null && ResourceLoader.VERSION_INVALID.equals(resourceVersion)))
                {               
                    resourceId = resourceLoader.createResourceMeta(null, libraryName,
```

### ConditionCoveredByFurtherCondition
Condition 'resourceVersion != null' covered by subsequent condition 'ResourceLoader.VERSION_INVALID.equals(...)'
in `impl/src/main/java/org/apache/myfaces/application/ResourceHandlerImpl.java`
#### Snippet
```java
                resourceVersion = resourceLoader.getResourceVersion(resourceName, contractName);
                
                if (!(resourceVersion != null && ResourceLoader.VERSION_INVALID.equals(resourceVersion)))
                {               
                    resourceId = resourceLoader.createResourceMeta(null, null, null,
```

### ConditionCoveredByFurtherCondition
Condition 'actionRef != null' covered by subsequent condition 'cazeActionRef.equals(...)'
in `impl/src/main/java/org/apache/myfaces/application/NavigationHandlerImpl.java`
#### Snippet
```java
                if (cazeOutcome != null)
                {
                    if (actionRef != null && outcome != null
                            && cazeActionRef.equals(actionRef) && cazeOutcome.equals(outcome))
                    {
```

### ConditionCoveredByFurtherCondition
Condition 'requestType != null' covered by subsequent condition 'PARTIAL_PROCESS.equals(...)'
in `test/src/main/java/org/apache/myfaces/test/mock/MockPartialViewContext.java`
#### Snippet
```java
            String requestType = _facesContext.getExternalContext()
                    .getRequestHeaderMap().get(FACES_REQUEST);
            _partialRequest = (requestType != null && PARTIAL_PROCESS
                    .equals(requestType));
        }
```

### ConditionCoveredByFurtherCondition
Condition 'requestType != null' covered by subsequent condition 'PARTIAL_AJAX.equals(...)'
in `test/src/main/java/org/apache/myfaces/test/mock/MockPartialViewContext.java`
#### Snippet
```java
            String requestType = _facesContext.getExternalContext()
                    .getRequestHeaderMap().get(FACES_REQUEST);
            _ajaxRequest = (requestType != null && PARTIAL_AJAX
                    .equals(requestType));
        }
```

### ConditionCoveredByFurtherCondition
Condition 'resourceVersion != null' covered by subsequent condition 'MockResourceLoader.VERSION_INVALID.equals(...)'
in `test/src/main/java/org/apache/myfaces/test/mock/resource/MockResourceHandler.java`
#### Snippet
```java
                }

                if (!(resourceVersion != null && MockResourceLoader.VERSION_INVALID
                        .equals(resourceVersion)))
                {
```

### ConditionCoveredByFurtherCondition
Condition 'resourceVersion != null' covered by subsequent condition 'MockResourceLoader.VERSION_INVALID.equals(...)'
in `test/src/main/java/org/apache/myfaces/test/mock/resource/MockResourceHandler.java`
#### Snippet
```java
                resourceVersion = resourceLoader
                        .getResourceVersion(localePrefix + '/' + resourceName);
                if (!(resourceVersion != null && MockResourceLoader.VERSION_INVALID
                        .equals(resourceVersion)))
                {
```

### ConditionCoveredByFurtherCondition
Condition 'resourceVersion != null' covered by subsequent condition 'MockResourceLoader.VERSION_INVALID.equals(...)'
in `test/src/main/java/org/apache/myfaces/test/mock/resource/MockResourceHandler.java`
#### Snippet
```java
                }

                if (!(resourceVersion != null && MockResourceLoader.VERSION_INVALID
                        .equals(resourceVersion)))
                {
```

### ConditionCoveredByFurtherCondition
Condition 'resourceVersion != null' covered by subsequent condition 'MockResourceLoader.VERSION_INVALID.equals(...)'
in `test/src/main/java/org/apache/myfaces/test/mock/resource/MockResourceHandler.java`
#### Snippet
```java
                        .getResourceVersion(resourceName);

                if (!(resourceVersion != null && MockResourceLoader.VERSION_INVALID
                        .equals(resourceVersion)))
                {
```

## RuleId[ruleID=UnnecessaryFullyQualifiedName]
### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.context` is unnecessary and can be removed
in `api/src/main/java/org/apache/myfaces/core/api/shared/ComponentUtils.java`
#### Snippet
```java
    /**
     * Call
     * {@link UIComponent#pushComponentToEL(jakarta.faces.context.FacesContext, jakarta.faces.component.UIComponent)},
     * reads the isRendered property, call {@link UIComponent#popComponentFromEL} and returns the value of isRendered.
     */
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.component` is unnecessary and can be removed
in `api/src/main/java/org/apache/myfaces/core/api/shared/ComponentUtils.java`
#### Snippet
```java
    /**
     * Call
     * {@link UIComponent#pushComponentToEL(jakarta.faces.context.FacesContext, jakarta.faces.component.UIComponent)},
     * reads the isRendered property, call {@link UIComponent#popComponentFromEL} and returns the value of isRendered.
     */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/org/apache/myfaces/core/api/shared/SelectItemsUtil.java`
#### Snippet
```java
            try
            {
                if (value instanceof java.lang.Enum)
                {
                    // Values from an enum are a special case. There is one syntax were the
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.el` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/flow/MethodCallNode.java`
#### Snippet
```java
    public abstract jakarta.el.MethodExpression getMethodExpression();
    
    public abstract jakarta.el.ValueExpression getOutcome();
    
    public abstract List<Parameter> getParameters();
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.el` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/flow/MethodCallNode.java`
#### Snippet
```java
public abstract class MethodCallNode extends FlowNode
{
    public abstract jakarta.el.MethodExpression getMethodExpression();
    
    public abstract jakarta.el.ValueExpression getOutcome();
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/flow/FlowHandler.java`
#### Snippet
```java
    public static final String TO_FLOW_DOCUMENT_ID_REQUEST_PARAM_NAME = "jftfdi";

    public abstract java.util.Map<java.lang.Object, java.lang.Object> getCurrentFlowScope();

    public abstract Flow getFlow(FacesContext context,
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/flow/FlowHandler.java`
#### Snippet
```java
    public static final String TO_FLOW_DOCUMENT_ID_REQUEST_PARAM_NAME = "jftfdi";

    public abstract java.util.Map<java.lang.Object, java.lang.Object> getCurrentFlowScope();

    public abstract Flow getFlow(FacesContext context,
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/flow/FlowHandler.java`
#### Snippet
```java
    public static final String TO_FLOW_DOCUMENT_ID_REQUEST_PARAM_NAME = "jftfdi";

    public abstract java.util.Map<java.lang.Object, java.lang.Object> getCurrentFlowScope();

    public abstract Flow getFlow(FacesContext context,
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.el` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/flow/Parameter.java`
#### Snippet
```java
    public abstract String getName();
    
    public abstract jakarta.el.ValueExpression getValue();
    
}
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.el` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/flow/Flow.java`
#### Snippet
```java
    public abstract Map<String,Parameter> getInboundParameters();
    
    public abstract jakarta.el.MethodExpression getInitializer();
    
    public abstract jakarta.el.MethodExpression getFinalizer();
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/flow/Flow.java`
#### Snippet
```java
    public abstract String getStartNodeId();
    
    public abstract java.util.Map<java.lang.String,java.util.Set<NavigationCase>> getNavigationCases();
    
}
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/flow/Flow.java`
#### Snippet
```java
    public abstract String getStartNodeId();
    
    public abstract java.util.Map<java.lang.String,java.util.Set<NavigationCase>> getNavigationCases();
    
}
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/flow/Flow.java`
#### Snippet
```java
    public abstract String getStartNodeId();
    
    public abstract java.util.Map<java.lang.String,java.util.Set<NavigationCase>> getNavigationCases();
    
}
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.el` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/flow/Flow.java`
#### Snippet
```java
    public abstract jakarta.el.MethodExpression getInitializer();
    
    public abstract jakarta.el.MethodExpression getFinalizer();
    
    public abstract FlowCallNode getFlowCall(Flow targetFlow);
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.el` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/flow/builder/SwitchCaseBuilder.java`
#### Snippet
```java
    public abstract SwitchCaseBuilder condition(String expression);
    
    public abstract SwitchCaseBuilder condition(jakarta.el.ValueExpression expression);
    
    public abstract SwitchCaseBuilder fromOutcome(String outcome);
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.el` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/flow/builder/FlowBuilder.java`
#### Snippet
```java

    public abstract FlowBuilder inboundParameter(String name,
        jakarta.el.ValueExpression value);

    public abstract FlowBuilder inboundParameter(String name,
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.el` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/flow/builder/FlowBuilder.java`
#### Snippet
```java
    public abstract FlowBuilder initializer(String methodExpression);

    public abstract FlowBuilder finalizer(jakarta.el.MethodExpression methodExpression);

    public abstract FlowBuilder finalizer(String methodExpression);
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.el` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/flow/builder/FlowBuilder.java`
#### Snippet
```java
    public abstract FlowCallBuilder flowCallNode(String flowCallNodeId);

    public abstract FlowBuilder initializer(jakarta.el.MethodExpression methodExpression);

    public abstract FlowBuilder initializer(String methodExpression);
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.el` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/flow/builder/ReturnBuilder.java`
#### Snippet
```java
    public abstract ReturnBuilder fromOutcome(String outcome);
    
    public abstract ReturnBuilder fromOutcome(jakarta.el.ValueExpression outcome);
    
    public abstract ReturnBuilder markAsStartNode();
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.el` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/flow/builder/MethodCallBuilder.java`
#### Snippet
```java
    public abstract MethodCallBuilder defaultOutcome(String outcome);

    public abstract MethodCallBuilder defaultOutcome(jakarta.el.ValueExpression outcome);

    public abstract MethodCallBuilder markAsStartNode();
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.el` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/flow/builder/MethodCallBuilder.java`
#### Snippet
```java
{

    public abstract MethodCallBuilder expression(jakarta.el.MethodExpression me);

    public abstract MethodCallBuilder expression(String methodExpression);
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.el` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/flow/builder/NavigationCaseBuilder.java`
#### Snippet
```java
    public abstract NavigationCaseBuilder condition(String condition);
    
    public abstract NavigationCaseBuilder condition(jakarta.el.ValueExpression condition);
    
    public abstract NavigationCaseBuilder.RedirectBuilder redirect();
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.el` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/flow/builder/SwitchBuilder.java`
#### Snippet
```java
    public abstract SwitchCaseBuilder defaultOutcome(String outcome);
    
    public abstract SwitchCaseBuilder defaultOutcome(jakarta.el.ValueExpression outcome);
    
    public abstract SwitchBuilder markAsStartNode();
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.el` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/flow/builder/FlowCallBuilder.java`
#### Snippet
```java

    public abstract FlowCallBuilder outboundParameter(String name,
        jakarta.el.ValueExpression value);

    public abstract FlowCallBuilder outboundParameter(String name,
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/view/ViewDeclarationLanguageFactory.java`
#### Snippet
```java
     * @return 
     */
    public java.util.List<ViewDeclarationLanguage> getAllViewDeclarationLanguages()
    {
        if (getWrapped() == null)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/view/ViewDeclarationLanguage.java`
#### Snippet
```java
     * @return 
     */
    public Stream<java.lang.String> getViews(FacesContext facesContext, String path, ViewVisitOption... options)
    {
        return getViews(facesContext, path, Integer.MAX_VALUE, options);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/view/ViewDeclarationLanguage.java`
#### Snippet
```java
     * @return 
     */
    public Stream<java.lang.String> getViews(FacesContext facesContext, String path, 
            int maxDepth, ViewVisitOption... options)
    {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/event/PhaseListener.java`
#### Snippet
```java
 * see Javadoc of <a href="http://java.sun.com/javaee/javaserverfaces/1.2/docs/api/index.html">Faces Specification</a>
 */
public interface PhaseListener extends java.util.EventListener, java.io.Serializable
{
    default void afterPhase(PhaseEvent event)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/event/PhaseListener.java`
#### Snippet
```java
 * see Javadoc of <a href="http://java.sun.com/javaee/javaserverfaces/1.2/docs/api/index.html">Faces Specification</a>
 */
public interface PhaseListener extends java.util.EventListener, java.io.Serializable
{
    default void afterPhase(PhaseEvent event)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/view/facelets/TagAttribute.java`
#### Snippet
```java

    /**
     * If literal, return {@link Boolean#getBoolean(java.lang.String) Boolean.getBoolean(java.lang.String)} passing our
     * value, otherwise call {@link #getObject(FaceletContext, Class) getObject(FaceletContext, Class)}.
     * 
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/view/facelets/TagAttribute.java`
#### Snippet
```java
     * value, otherwise call {@link #getObject(FaceletContext, Class) getObject(FaceletContext, Class)}.
     * 
     * @see Boolean#getBoolean(java.lang.String)
     * @see #getObject(FaceletContext, Class)
     * @param ctx
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/view/facelets/TagAttribute.java`
#### Snippet
```java
     * Create a MethodExpression, using this attribute's value as the expression String.
     * 
     * @see jakarta.el.ExpressionFactory#createMethodExpression(jakarta.el.ELContext, java.lang.String, java.lang.Class,
     *      java.lang.Class[])
     * @see MethodExpression
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/view/facelets/TagAttribute.java`
#### Snippet
```java
     * Create a MethodExpression, using this attribute's value as the expression String.
     * 
     * @see jakarta.el.ExpressionFactory#createMethodExpression(jakarta.el.ELContext, java.lang.String, java.lang.Class,
     *      java.lang.Class[])
     * @see MethodExpression
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/view/facelets/TagAttribute.java`
#### Snippet
```java
     * 
     * @see jakarta.el.ExpressionFactory#createMethodExpression(jakarta.el.ELContext, java.lang.String, java.lang.Class,
     *      java.lang.Class[])
     * @see MethodExpression
     * @param ctx
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/view/facelets/TagAttribute.java`
#### Snippet
```java

    /**
     * If literal, call {@link Integer#parseInt(java.lang.String) Integer.parseInt(String)}, otherwise call
     * {@link #getObject(FaceletContext, Class) getObject(FaceletContext, Class)}.
     * 
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/view/facelets/TagAttribute.java`
#### Snippet
```java
     * {@link #getObject(FaceletContext, Class) getObject(FaceletContext, Class)}.
     * 
     * @see Integer#parseInt(java.lang.String)
     * @see #getObject(FaceletContext, Class)
     * @param ctx
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/view/facelets/TagAttribute.java`
#### Snippet
```java
     * and evaluate it.
     * 
     * @see jakarta.el.ExpressionFactory#coerceToType(java.lang.Object, java.lang.Class)
     * @see jakarta.el.ExpressionFactory#createValueExpression(jakarta.el.ELContext, java.lang.String, java.lang.Class)
     * @see ValueExpression
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/view/facelets/TagAttribute.java`
#### Snippet
```java
     * and evaluate it.
     * 
     * @see jakarta.el.ExpressionFactory#coerceToType(java.lang.Object, java.lang.Class)
     * @see jakarta.el.ExpressionFactory#createValueExpression(jakarta.el.ELContext, java.lang.String, java.lang.Class)
     * @see ValueExpression
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/view/facelets/TagAttribute.java`
#### Snippet
```java
     * 
     * @see jakarta.el.ExpressionFactory#coerceToType(java.lang.Object, java.lang.Class)
     * @see jakarta.el.ExpressionFactory#createValueExpression(jakarta.el.ELContext, java.lang.String, java.lang.Class)
     * @see ValueExpression
     * @param ctx
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/view/facelets/TagAttribute.java`
#### Snippet
```java
     * 
     * @see jakarta.el.ExpressionFactory#coerceToType(java.lang.Object, java.lang.Class)
     * @see jakarta.el.ExpressionFactory#createValueExpression(jakarta.el.ELContext, java.lang.String, java.lang.Class)
     * @see ValueExpression
     * @param ctx
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/view/facelets/TagAttribute.java`
#### Snippet
```java
     * Create a ValueExpression, using this attribute's literal value and the passed expected type.
     * 
     * @see jakarta.el.ExpressionFactory#createValueExpression(jakarta.el.ELContext, java.lang.String, java.lang.Class)
     * @see ValueExpression
     * @param ctx
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/view/facelets/TagAttribute.java`
#### Snippet
```java
     * Create a ValueExpression, using this attribute's literal value and the passed expected type.
     * 
     * @see jakarta.el.ExpressionFactory#createValueExpression(jakarta.el.ELContext, java.lang.String, java.lang.Class)
     * @see ValueExpression
     * @param ctx
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.event` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/event/ValueChangeListener.java`
#### Snippet
```java
public interface ValueChangeListener extends FacesListener
{
    public void processValueChange(jakarta.faces.event.ValueChangeEvent event) throws AbortProcessingException;
}

```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/event/WebsocketEvent.java`
#### Snippet
```java
    }

    public <S extends java.io.Serializable> S getUser()
    {
        return (S) user;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/model/DataModelEvent.java`
#### Snippet
```java
 * see Javadoc of <a href="http://java.sun.com/javaee/javaserverfaces/1.2/docs/api/index.html">Faces Specification</a>
 */
public class DataModelEvent extends java.util.EventObject
{
    private static final long serialVersionUID = 1823115573192262656L;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/model/FacesDataModel.java`
#### Snippet
```java
public @interface FacesDataModel
{
    public java.lang.Class<?> forClass() default Object.class;

    /**
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/model/DataModelListener.java`
#### Snippet
```java
 * see Javadoc of <a href="http://java.sun.com/javaee/javaserverfaces/1.2/docs/api/index.html">Faces Specification</a>
 */
public interface DataModelListener extends java.util.EventListener
{
    void rowSelected(DataModelEvent event);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/render/FacesBehaviorRenderer.java`
#### Snippet
```java
     * The value of this annotation attribute is taken to be the <i>renderer-type</i> which, in combination with
     * componentFamily can be used to obtain a reference to an instance of this {@link Renderer} by calling
     * {@link RenderKit#getRenderer(java.lang.String, java.lang.String)}.
     */
    public String rendererType();
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/render/FacesBehaviorRenderer.java`
#### Snippet
```java
     * The value of this annotation attribute is taken to be the <i>renderer-type</i> which, in combination with
     * componentFamily can be used to obtain a reference to an instance of this {@link Renderer} by calling
     * {@link RenderKit#getRenderer(java.lang.String, java.lang.String)}.
     */
    public String rendererType();
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/render/FacesRenderer.java`
#### Snippet
```java
     * The value of this annotation attribute is taken to be the <i>renderer-type</i> which, in combination with
     * {@link #componentFamily()} can be used to obtain a reference to an instance of this {@link Renderer} by calling
     * {@link RenderKit#getRenderer(java.lang.String, java.lang.String)}.
     */
    public String rendererType();
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/render/FacesRenderer.java`
#### Snippet
```java
     * The value of this annotation attribute is taken to be the <i>renderer-type</i> which, in combination with
     * {@link #componentFamily()} can be used to obtain a reference to an instance of this {@link Renderer} by calling
     * {@link RenderKit#getRenderer(java.lang.String, java.lang.String)}.
     */
    public String rendererType();
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/render/FacesRenderer.java`
#### Snippet
```java
     * The value of this annotation attribute is taken to be the <i>component-family</i> which, in combination with
     * {@link #rendererType()} can be used to obtain a reference to an instance of this {@link Renderer} by calling
     * {@link RenderKit#getRenderer(java.lang.String, java.lang.String)}.
     */
    public String componentFamily();
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/render/FacesRenderer.java`
#### Snippet
```java
     * The value of this annotation attribute is taken to be the <i>component-family</i> which, in combination with
     * {@link #rendererType()} can be used to obtain a reference to an instance of this {@link Renderer} by calling
     * {@link RenderKit#getRenderer(java.lang.String, java.lang.String)}.
     */
    public String componentFamily();
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/context/PartialViewContextWrapper.java`
#### Snippet
```java
 
    @Override
    public List<java.lang.String> getEvalScripts()
    {
        return getWrapped().getEvalScripts();
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/context/PartialViewContext.java`
#### Snippet
```java
     * @since 2.2
     */
    public static final java.lang.String RESET_VALUES_PARAM_NAME = "jakarta.faces.partial.resetValues";
    
    /**
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/context/PartialViewContext.java`
#### Snippet
```java
    * @since 2.3
    */
    public abstract java.util.List<String> getEvalScripts();
    
    /**
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.convert` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/component/UIOutput.java`
#### Snippet
```java
            {
                //Full
                _converter = (jakarta.faces.convert.Converter) restoreAttachedState(facesContext,values[1]);
            }
        }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/component/TransientStateHelper.java`
#### Snippet
```java
 * 
 * <p>The values retrieved or saved through 
 * {@link #getTransient(java.lang.Object) } or 
 * {@link #putTransient(java.lang.Object, java.lang.Object) } 
 * will not be preserved between requests.</p>
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/component/TransientStateHelper.java`
#### Snippet
```java
 * <p>The values retrieved or saved through 
 * {@link #getTransient(java.lang.Object) } or 
 * {@link #putTransient(java.lang.Object, java.lang.Object) } 
 * will not be preserved between requests.</p>
 * 
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/component/TransientStateHelper.java`
#### Snippet
```java
 * <p>The values retrieved or saved through 
 * {@link #getTransient(java.lang.Object) } or 
 * {@link #putTransient(java.lang.Object, java.lang.Object) } 
 * will not be preserved between requests.</p>
 * 
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/component/TransientStateHelper.java`
#### Snippet
```java
    
    /**
     * <p>Performs the same logic as {@link #getTransient(java.lang.Object)} but if no value is found, this
     * will return the specified <code>defaultValue</code></p>

```

### UnnecessaryFullyQualifiedName
Qualifier `java.security` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/context/ExternalContext.java`
#### Snippet
```java
    public abstract Map<String, Object> getSessionMap();

    public abstract java.security.Principal getUserPrincipal();

    /**
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/context/ExternalContext.java`
#### Snippet
```java
    public abstract void log(String message, Throwable exception);

    public abstract void redirect(String url) throws java.io.IOException;

    /**
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/context/ExternalContext.java`
#### Snippet
```java
     * @throws java.io.UnsupportedEncodingException
     */
    public void setRequestCharacterEncoding(java.lang.String encoding)
            throws java.io.UnsupportedEncodingException
    {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/context/ExternalContext.java`
#### Snippet
```java
     */
    public void setRequestCharacterEncoding(java.lang.String encoding)
            throws java.io.UnsupportedEncodingException
    {
        ExternalContext ctx = _MyFacesExternalContextHelper.firstInstance.get();
```

### UnnecessaryFullyQualifiedName
Qualifier `java.net` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/context/ExternalContext.java`
#### Snippet
```java
    public abstract String getRequestServletPath();

    public abstract java.net.URL getResource(String path) throws java.net.MalformedURLException;

    public abstract java.io.InputStream getResourceAsStream(String path);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.net` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/context/ExternalContext.java`
#### Snippet
```java
    public abstract String getRequestServletPath();

    public abstract java.net.URL getResource(String path) throws java.net.MalformedURLException;

    public abstract java.io.InputStream getResourceAsStream(String path);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/context/ExternalContext.java`
#### Snippet
```java
    public abstract java.net.URL getResource(String path) throws java.net.MalformedURLException;

    public abstract java.io.InputStream getResourceAsStream(String path);

    public abstract Set<String> getResourcePaths(String path);
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/component/UIComponent.java`
#### Snippet
```java
     *            Implementation of the <code>ContextCallback</code> to be called
     * @return has component been found ?
     * @throws jakarta.faces.FacesException
     */
    public boolean invokeOnComponent(FacesContext context, String clientId, ContextCallback callback)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/component/UIComponent.java`
#### Snippet
```java
    public abstract void processUpdates(FacesContext context);

    public abstract java.lang.Object processSaveState(FacesContext context);

    public void subscribeToEvent(Class<? extends SystemEvent> eventClass,
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.model` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/component/_UISelectItemGroup.java`
#### Snippet
```java
    public Object getValue() {
        FacesContext context = getFacesContext();
        jakarta.faces.model.SelectItemGroup group = org.apache.myfaces.core.api.shared.SelectItemsUtil.createSelectItem(this,null,
                jakarta.faces.model.SelectItemGroup::new);
        group.setSelectItems(org.apache.myfaces.core.api.shared.SelectItemsUtil.collectSelectItems(context, this));
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.core.api.shared` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/component/_UISelectItemGroup.java`
#### Snippet
```java
    public Object getValue() {
        FacesContext context = getFacesContext();
        jakarta.faces.model.SelectItemGroup group = org.apache.myfaces.core.api.shared.SelectItemsUtil.createSelectItem(this,null,
                jakarta.faces.model.SelectItemGroup::new);
        group.setSelectItems(org.apache.myfaces.core.api.shared.SelectItemsUtil.collectSelectItems(context, this));
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.model` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/component/_UISelectItemGroup.java`
#### Snippet
```java
        FacesContext context = getFacesContext();
        jakarta.faces.model.SelectItemGroup group = org.apache.myfaces.core.api.shared.SelectItemsUtil.createSelectItem(this,null,
                jakarta.faces.model.SelectItemGroup::new);
        group.setSelectItems(org.apache.myfaces.core.api.shared.SelectItemsUtil.collectSelectItems(context, this));
        return group;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.core.api.shared` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/component/_UISelectItemGroup.java`
#### Snippet
```java
        jakarta.faces.model.SelectItemGroup group = org.apache.myfaces.core.api.shared.SelectItemsUtil.createSelectItem(this,null,
                jakarta.faces.model.SelectItemGroup::new);
        group.setSelectItems(org.apache.myfaces.core.api.shared.SelectItemsUtil.collectSelectItems(context, this));
        return group;
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/component/_ComponentFacetMap.java`
#### Snippet
```java

        @Override
        public java.util.Map.Entry<String, V> next()
        {
            java.util.Map.Entry<String, V> next = _delegate.next(); 
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/component/_ComponentFacetMap.java`
#### Snippet
```java
        public java.util.Map.Entry<String, V> next()
        {
            java.util.Map.Entry<String, V> next = _delegate.next(); 
            _currentEntryValue = next.getValue();
            return new ComponentFacetEntry(next);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/component/_ComponentFacetMap.java`
#### Snippet
```java
    private class ComponentFacetEntry implements Map.Entry<String, V>
    {
        private java.util.Map.Entry<String, V> _entry;
        
        public ComponentFacetEntry(java.util.Map.Entry<String, V> entry)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/component/_ComponentFacetMap.java`
#### Snippet
```java

        @Override
        public Iterator<java.util.Map.Entry<String, V>> iterator()
        {
            return new ComponentFacetEntryIterator(_map.entrySet().iterator());
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/component/_ComponentFacetMap.java`
#### Snippet
```java

        @Override
        public boolean addAll(Collection<? extends java.util.Map.Entry<String, V>> c)
        {
            // Add over the entry set is not allowed, because this should be done
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/component/_ComponentFacetMap.java`
#### Snippet
```java

        @Override
        public boolean add(java.util.Map.Entry<String, V> o)
        {
            // Add over the entry set is not allowed, because this should be done
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/component/_ComponentFacetMap.java`
#### Snippet
```java
        private java.util.Map.Entry<String, V> _entry;
        
        public ComponentFacetEntry(java.util.Map.Entry<String, V> entry)
        {
            _entry = entry;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/component/_ComponentFacetMap.java`
#### Snippet
```java
        public V next()
        {
            java.util.Map.Entry<String, V> next = _delegate.next(); 
            _currentEntryValue = next.getValue();
            return next.getValue();
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/component/_ComponentFacetMap.java`
#### Snippet
```java
        public String next()
        {
            java.util.Map.Entry<String, V> next = _delegate.next(); 
            _currentEntryValue = next.getValue();
            return next.getKey();
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.context` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/component/StateHolder.java`
#### Snippet
```java
    public java.lang.Object saveState(jakarta.faces.context.FacesContext context);

    public void restoreState(jakarta.faces.context.FacesContext context,
                             java.lang.Object state);

```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/component/StateHolder.java`
#### Snippet
```java

    public void restoreState(jakarta.faces.context.FacesContext context,
                             java.lang.Object state);

    public boolean isTransient();
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/component/StateHolder.java`
#### Snippet
```java
public interface StateHolder
{
    public java.lang.Object saveState(jakarta.faces.context.FacesContext context);

    public void restoreState(jakarta.faces.context.FacesContext context,
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.context` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/component/StateHolder.java`
#### Snippet
```java
public interface StateHolder
{
    public java.lang.Object saveState(jakarta.faces.context.FacesContext context);

    public void restoreState(jakarta.faces.context.FacesContext context,
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/component/_UIWebsocket.java`
#### Snippet
```java
    static public final String COMPONENT_TYPE = "jakarta.faces.Websocket";

    private static final Collection<String> EVERY_EVENT = Collections.unmodifiableList(new java.util.ArrayList<String>()
    {
        @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/component/UIInput.java`
#### Snippet
```java
        else if (value.getClass().isArray())
        {
            if (java.lang.reflect.Array.getLength(value) <= 0)
            {
                return true;
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.context` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/component/TransientStateHolder.java`
#### Snippet
```java
     * @since 2.1
     */
    public void restoreTransientState(jakarta.faces.context.FacesContext context,
                             java.lang.Object state);

```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/component/TransientStateHolder.java`
#### Snippet
```java
     */
    public void restoreTransientState(jakarta.faces.context.FacesContext context,
                             java.lang.Object state);

}
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/component/TransientStateHolder.java`
#### Snippet
```java
     * @since 2.1
     */
    public java.lang.Object saveTransientState(jakarta.faces.context.FacesContext context);

    /**
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.context` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/component/TransientStateHolder.java`
#### Snippet
```java
     * @since 2.1
     */
    public java.lang.Object saveTransientState(jakarta.faces.context.FacesContext context);

    /**
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.component` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/component/UISelectOne.java`
#### Snippet
```java
     * the child SelectItem objects. If this is not true, a validation error is reported.
     * 
     * @see jakarta.faces.component.UIInput#validateValue(jakarta.faces.context.FacesContext,java.lang.Object)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.context` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/component/UISelectOne.java`
#### Snippet
```java
     * the child SelectItem objects. If this is not true, a validation error is reported.
     * 
     * @see jakarta.faces.component.UIInput#validateValue(jakarta.faces.context.FacesContext,java.lang.Object)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/component/UISelectOne.java`
#### Snippet
```java
     * the child SelectItem objects. If this is not true, a validation error is reported.
     * 
     * @see jakarta.faces.component.UIInput#validateValue(jakarta.faces.context.FacesContext,java.lang.Object)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.component` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/component/UISelectOne.java`
#### Snippet
```java
     * the submitted value exists in any of the select items.
     *
     * @see jakarta.faces.component.UIInput#processValidators(jakarta.faces.context.FacesContext)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.context` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/component/UISelectOne.java`
#### Snippet
```java
     * the submitted value exists in any of the select items.
     *
     * @see jakarta.faces.component.UIInput#processValidators(jakarta.faces.context.FacesContext)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.event` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/component/ActionSource.java`
#### Snippet
```java
    public jakarta.faces.event.ActionListener[] getActionListeners();

    public void removeActionListener(jakarta.faces.event.ActionListener listener);
}

```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.event` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/component/ActionSource.java`
#### Snippet
```java
    public void addActionListener(jakarta.faces.event.ActionListener listener);

    public jakarta.faces.event.ActionListener[] getActionListeners();

    public void removeActionListener(jakarta.faces.event.ActionListener listener);
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.event` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/component/ActionSource.java`
#### Snippet
```java
    public void setImmediate(boolean immediate);

    public void addActionListener(jakarta.faces.event.ActionListener listener);

    public jakarta.faces.event.ActionListener[] getActionListeners();
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/component/_UISelectItemGroups.java`
#### Snippet
```java
    public Object getValue() {
        FacesContext context = getFacesContext();
        java.util.List<jakarta.faces.model.SelectItemGroup> groups = new java.util.ArrayList<>();

        org.apache.myfaces.core.api.shared.SelectItemsUtil.createSelectItems(context,
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.model` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/component/_UISelectItemGroups.java`
#### Snippet
```java
    public Object getValue() {
        FacesContext context = getFacesContext();
        java.util.List<jakarta.faces.model.SelectItemGroup> groups = new java.util.ArrayList<>();

        org.apache.myfaces.core.api.shared.SelectItemsUtil.createSelectItems(context,
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/component/_UISelectItemGroups.java`
#### Snippet
```java
    public Object getValue() {
        FacesContext context = getFacesContext();
        java.util.List<jakarta.faces.model.SelectItemGroup> groups = new java.util.ArrayList<>();

        org.apache.myfaces.core.api.shared.SelectItemsUtil.createSelectItems(context,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.core.api.shared` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/component/_UISelectItemGroups.java`
#### Snippet
```java
        java.util.List<jakarta.faces.model.SelectItemGroup> groups = new java.util.ArrayList<>();

        org.apache.myfaces.core.api.shared.SelectItemsUtil.createSelectItems(context,
                this,
                super.getValue(),
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.model` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/component/_UISelectItemGroups.java`
#### Snippet
```java
                this,
                super.getValue(),
                jakarta.faces.model.SelectItemGroup::new,
                selectItemGroup ->
        {
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.core.api.shared` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/component/_UISelectItemGroups.java`
#### Snippet
```java
        {
            selectItemGroup.setSelectItems(
                    org.apache.myfaces.core.api.shared.SelectItemsUtil.collectSelectItems(context, this));
            groups.add(selectItemGroup);
        });
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.application` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/component/html/_HtmlInputFile.java`
#### Snippet
```java
            {
                String requiredMessage = getRequiredMessage();
                context.addMessage(this.getClientId(context), new jakarta.faces.application.FacesMessage(
                        jakarta.faces.application.FacesMessage.SEVERITY_ERROR,
                    requiredMessage, requiredMessage));
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.application` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/component/html/_HtmlInputFile.java`
#### Snippet
```java
                String requiredMessage = getRequiredMessage();
                context.addMessage(this.getClientId(context), new jakarta.faces.application.FacesMessage(
                        jakarta.faces.application.FacesMessage.SEVERITY_ERROR,
                    requiredMessage, requiredMessage));
            }
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/component/html/_HtmlInputFile.java`
#### Snippet
```java
            }
        }
        else if (value instanceof java.util.Collection)
        {
            if ( ((java.util.Collection)value).isEmpty())
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/component/html/_HtmlInputFile.java`
#### Snippet
```java
        else if (value instanceof java.util.Collection)
        {
            if ( ((java.util.Collection)value).isEmpty())
            {
                return true;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/component/html/_HtmlInputFile.java`
#### Snippet
```java
        else if (value.getClass().isArray())
        {
            if (java.lang.reflect.Array.getLength(value) <= 0)
            {
                return true;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/component/html/_HtmlInputFile.java`
#### Snippet
```java
            }
        }
        else if (value instanceof java.util.Map)
        {
            if ( ((java.util.Map)value).isEmpty())
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/component/html/_HtmlInputFile.java`
#### Snippet
```java
        else if (value instanceof java.util.Map)
        {
            if ( ((java.util.Map)value).isEmpty())
            {
                return true;
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet.http` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/component/html/_HtmlInputFile.java`
#### Snippet
```java
            }
        }
        else if (value instanceof jakarta.servlet.http.Part) 
        {
            if (((jakarta.servlet.http.Part)value).getSize() <= 0) 
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet.http` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/component/html/_HtmlInputFile.java`
#### Snippet
```java
        else if (value instanceof jakarta.servlet.http.Part) 
        {
            if (((jakarta.servlet.http.Part)value).getSize() <= 0) 
            {
                return true;
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.event` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/lifecycle/Lifecycle.java`
#### Snippet
```java
            throws FacesException;

    public abstract jakarta.faces.event.PhaseListener[] getPhaseListeners();

    public abstract void removePhaseListener(jakarta.faces.event.PhaseListener listener);
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.event` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/lifecycle/Lifecycle.java`
#### Snippet
```java
    public abstract jakarta.faces.event.PhaseListener[] getPhaseListeners();

    public abstract void removePhaseListener(jakarta.faces.event.PhaseListener listener);

    public abstract void render(jakarta.faces.context.FacesContext context)
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.context` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/lifecycle/Lifecycle.java`
#### Snippet
```java
    public abstract void addPhaseListener(jakarta.faces.event.PhaseListener listener);

    public abstract void execute(jakarta.faces.context.FacesContext context)
            throws FacesException;

```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.context` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/lifecycle/Lifecycle.java`
#### Snippet
```java
    public abstract void removePhaseListener(jakarta.faces.event.PhaseListener listener);

    public abstract void render(jakarta.faces.context.FacesContext context)
            throws FacesException;
    
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.event` is unnecessary, and can be replaced with an import
in `api/src/main/java/jakarta/faces/lifecycle/Lifecycle.java`
#### Snippet
```java
public abstract class Lifecycle
{
    public abstract void addPhaseListener(jakarta.faces.event.PhaseListener listener);

    public abstract void execute(jakarta.faces.context.FacesContext context)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/lifecycle/LifecycleFactory.java`
#### Snippet
```java
    FacesWrapper<LifecycleFactory>
{
    public static final java.lang.String DEFAULT_LIFECYCLE = "DEFAULT";

    private LifecycleFactory delegate;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util.regex` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/validator/RegexValidator.java`
#### Snippet
```java
    /**
     * This message ID is used when the pattern is not a valid regular expression, according
     * to the rules as defined in class {@link java.util.regex.Pattern}
     */
    public static final String MATCH_EXCEPTION_MESSAGE_ID = "jakarta.faces.validator.RegexValidator.MATCH_EXCEPTION";
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.validator` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/validator/RegexValidator.java`
#### Snippet
```java
/**
 * <p>
 *   <strong>RegexValidator</strong> is a {@link jakarta.faces.validator.Validator}
 *   that checks the value of the corresponding component against specified
 *   pattern using Java regular expression syntax.
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util.regex` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/validator/RegexValidator.java`
#### Snippet
```java
 *
 *   The regular expression syntax accepted by the RegexValidator class is
 *   same as mentioned in class {@link java.util.regex.Pattern} in package
 *   <code>java.util.regex</code>.
 * </p>
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.validator` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/validator/RegexValidator.java`
#### Snippet
```java
 *   <li>
 *     If no pattern has been set, or pattern resolves to <code>null</code> or an
 *     empty String, throw a {@link jakarta.faces.validator.ValidatorException}
 *     with a {@link #PATTERN_NOT_SET_MESSAGE_ID} message.
 *   </li>
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util.regex` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/validator/RegexValidator.java`
#### Snippet
```java
 *   <li>
 *     If pattern is not a valid regular expression, according to the rules as defined
 *     in class {@link java.util.regex.Pattern}, throw a {@link ValidatorException}
 *     with a (@link #MATCH_EXCEPTION_MESSAGE_ID} message.
 *   </li>
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.validator` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/validator/RegexValidator.java`
#### Snippet
```java
 *   <li>
 *     If a <code>pattern</code> property has been configured on this
 *     {@link jakarta.faces.validator.Validator}, check the passed value against this pattern.
 *     If value does not match pattern throw a {@link ValidatorException}
 *     containing a {@link #NOT_MATCHED_MESSAGE_ID} message.
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/validator/FacesValidator.java`
#### Snippet
```java
     * The value of this annotation attribute is taken to be the <i>validator-id</i> with which instances of this class
     * of component can be instantiated by calling 
     * {@link jakarta.faces.application.Application#createValidator(java.lang.String)}.
     */
    public String value() default "";
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/validator/FacesValidator.java`
#### Snippet
```java
    /**
     * If it is true, the validator is added to default list calling  
     * {@link jakarta.faces.application.Application#addDefaultValidatorId(java.lang.String)}.
     */
    boolean isDefault() default false;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/application/ResourceHandler.java`
#### Snippet
```java
     * @return 
     */
    public Stream<java.lang.String> getViewResources(FacesContext facesContext, 
            String path, int maxDepth, ResourceVisitOption... options)
    {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/application/ResourceHandler.java`
#### Snippet
```java
    }
    
    public boolean isResourceURL(java.lang.String url)
    {
        Assert.notNull(url);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/application/ResourceHandler.java`
#### Snippet
```java
     * @return 
     */
    public Stream<java.lang.String> getViewResources(
            FacesContext facesContext, String path, ResourceVisitOption... options)
    {
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.validator` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/validator/BeanValidator.java`
#### Snippet
```java
/**
 * <p>
 * <strong>BeanValidator</strong> is a {@link jakarta.faces.validator.Validator}
 * that doesn't do any validation itself, but delegates validation logic to
 * Bean Validation.
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/component/UIViewRoot.java`
#### Snippet
```java
     * @param clientIds 
     */
    public void resetValues(FacesContext context, java.util.Collection<java.lang.String> clientIds)    
    {
        VisitContext visitContext = (VisitContext) VisitContext.createVisitContext(context, clientIds, null);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/component/UIViewRoot.java`
#### Snippet
```java
     * @param clientIds 
     */
    public void resetValues(FacesContext context, java.util.Collection<java.lang.String> clientIds)    
    {
        VisitContext visitContext = (VisitContext) VisitContext.createVisitContext(context, clientIds, null);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/application/StateManager.java`
#### Snippet
```java
    @JSFWebConfigParam(since="2.2", group="state", tags="performance", 
            defaultValue="false", expectedValues="true,false")
    public static final java.lang.String SERIALIZE_SERVER_STATE_PARAM_NAME = "jakarta.faces.SERIALIZE_SERVER_STATE";

    /**
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.component` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/component/UIData.java`
#### Snippet
```java

    /**
     * @see jakarta.faces.component.UIComponentBase#encodeEnd(jakarta.faces.context.FacesContext)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.context` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/component/UIData.java`
#### Snippet
```java

    /**
     * @see jakarta.faces.component.UIComponentBase#encodeEnd(jakarta.faces.context.FacesContext)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/application/ConfigurableNavigationHandler.java`
#### Snippet
```java
     */
    public NavigationCase getNavigationCase(FacesContext context,
                                        java.lang.String fromAction,
                                        java.lang.String outcome,
                                        java.lang.String toFlowDocumentId)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/application/ConfigurableNavigationHandler.java`
#### Snippet
```java
    public NavigationCase getNavigationCase(FacesContext context,
                                        java.lang.String fromAction,
                                        java.lang.String outcome,
                                        java.lang.String toFlowDocumentId)
    {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/application/ConfigurableNavigationHandler.java`
#### Snippet
```java
                                        java.lang.String fromAction,
                                        java.lang.String outcome,
                                        java.lang.String toFlowDocumentId)
    {
        return getNavigationCase(context, fromAction, outcome);
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.el` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/application/Application.java`
#### Snippet
```java
     * <p>
     * 
     * @throws jakarta.el.ELException
     */
    public <T> T evaluateExpressionGet(FacesContext context, String expression, Class<? extends T> expectedType)
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.context` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/application/Application.java`
#### Snippet
```java
     * Return the <code>ExpressionFactory</code> instance for this application.
     * This instance is used by the convenience method
     * <code>{@link #evaluateExpressionGet(jakarta.faces.context.FacesContext, java.lang.String, java.lang.Class)}.
     * </code>
     * </p>
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/application/Application.java`
#### Snippet
```java
     * Return the <code>ExpressionFactory</code> instance for this application.
     * This instance is used by the convenience method
     * <code>{@link #evaluateExpressionGet(jakarta.faces.context.FacesContext, java.lang.String, java.lang.Class)}.
     * </code>
     * </p>
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/application/Application.java`
#### Snippet
```java
     * Return the <code>ExpressionFactory</code> instance for this application.
     * This instance is used by the convenience method
     * <code>{@link #evaluateExpressionGet(jakarta.faces.context.FacesContext, java.lang.String, java.lang.Class)}.
     * </code>
     * </p>
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/application/Application.java`
#### Snippet
```java
     * <code>source</code> has been installed, and should return immediately once such a determination has been made.
     * The implementation of <code>publishEvent</code> must honor the requirements stated in
     * <code>{@link #subscribeToEvent(java.lang.Class, java.lang.Class,
     *                                               jakarta.faces.event.SystemEventListener)}</code>
     * <p>
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/application/Application.java`
#### Snippet
```java
     * <code>source</code> has been installed, and should return immediately once such a determination has been made.
     * The implementation of <code>publishEvent</code> must honor the requirements stated in
     * <code>{@link #subscribeToEvent(java.lang.Class, java.lang.Class,
     *                                               jakarta.faces.event.SystemEventListener)}</code>
     * <p>
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.event` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/application/Application.java`
#### Snippet
```java
     * The implementation of <code>publishEvent</code> must honor the requirements stated in
     * <code>{@link #subscribeToEvent(java.lang.Class, java.lang.Class,
     *                                               jakarta.faces.event.SystemEventListener)}</code>
     * <p>
     * <p>
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/application/Application.java`
#### Snippet
```java
     * <li>If the <code>source</code> argument implements
     * <code>{@link jakarta.faces.event.SystemEventListenerHolder}</code>, call
     * <code>{@link jakarta.faces.event.SystemEventListenerHolder#getListenersForEventClass(java.lang.Class)}</code>
     * on it, passing the
     * <code>systemEventClass</code> argument. If the list is not empty, perform algorithm
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/application/Application.java`
#### Snippet
```java
     * 
     * <li>If any <code>Application</code> level listeners have been installed by previous calls to <code>{@link
     * #subscribeToEvent(java.lang.Class, java.lang.Class, SystemEventListener)}</code>, perform algorithm
     * <code>traverseListenerList</code> on the list.</li>
     * 
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/application/Application.java`
#### Snippet
```java
     * 
     * <li>If any <code>Application</code> level listeners have been installed by previous calls to <code>{@link
     * #subscribeToEvent(java.lang.Class, java.lang.Class, SystemEventListener)}</code>, perform algorithm
     * <code>traverseListenerList</code> on the list.</li>
     * 
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/application/Application.java`
#### Snippet
```java
     * 
     * <li>If any <code>Application</code> level listeners have been installed by previous calls to
     * <code>{@link #subscribeToEvent(java.lang.Class, SystemEventListener)}</code>, perform algorithm
     * <code>traverseListenerList</code> on the list.</li>
     * </ul>
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/application/Application.java`
#### Snippet
```java
     * <ul>
     * <li>Call
     * <code>{@link SystemEventListener#isListenerForSource(java.lang.Object)}</code>, passing the <code>source</code>
     * argument. If this returns <code>false</code>, take no action on the listener.</li>
     * 
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.el` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/application/Application.java`
#### Snippet
```java
     * 
     * <li>Any <code>ELResolver</code> instances added by calls to
     * <code>{@link #addELResolver(jakarta.el.ELResolver)}</code>.</li>
     * 
     * <li>The default implementation throws <code>UnsupportedOperationException</code> and is provided for the sole
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/el/resolver/CompositeComponentELResolver.java`
#### Snippet
```java

        @Override
        public Set<java.util.Map.Entry<String, Object>> entrySet()
        {
            return originalMap.entrySet();
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.enterprise.inject` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/cdi/util/AnyLiteral.java`
#### Snippet
```java

/**
 * Literal for the {@link jakarta.enterprise.inject.Any} annotation.
 * 
 * NOTE: Taken from Apache DeltaSpike
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.enterprise.inject` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/cdi/util/DefaultLiteral.java`
#### Snippet
```java

/**
 * Literal for the {@link jakarta.enterprise.inject.Default} annotation.
 */
public class DefaultLiteral extends AnnotationLiteral<Default> implements Default
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.enterprise.context.spi` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/cdi/util/ContextualInstanceInfo.java`
#### Snippet
```java
     * properly destroying the contextual instance via
     * {@link jakarta.enterprise.context.spi.Contextual#destroy(Object,
     *      jakarta.enterprise.context.spi.CreationalContext)}
     */
    private CreationalContext<T> creationalContext;
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.enterprise.context.spi` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/cdi/util/ContextualStorage.java`
#### Snippet
```java
    /**
     * Restores the Bean from its beanKey.
     * @see #getBeanKey(jakarta.enterprise.context.spi.Contextual)
     */
    public Contextual<?> getBean(Object beanKey)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.security` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/spi/FacesConfigurationMergerFactory.java`
#### Snippet
```java
                final ExternalContext ectx = ctx;
                factory = (FacesConfigurationMergerFactory) AccessController.doPrivileged(
                        (java.security.PrivilegedExceptionAction) () -> SpiUtils.build(ectx,
                                FacesConfigurationMergerFactory.class,
                                DefaultFacesConfigurationMergerFactory.class));
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.naming` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/spi/impl/ResourceAnnotationInjectionProvider.java`
#### Snippet
```java
     * Inject resources in specified method.
     */
    protected static void lookupMethodResource(javax.naming.Context context,
            Object instance, Method method, String name)
            throws NamingException, IllegalAccessException, InvocationTargetException
```

### UnnecessaryFullyQualifiedName
Qualifier `javax.naming` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/spi/impl/ResourceAnnotationInjectionProvider.java`
#### Snippet
```java
     * Inject resources in specified field.
     */
    protected static void lookupFieldResource(javax.naming.Context context,
            Object instance, Field field, String name)
            throws NamingException, IllegalAccessException
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/push/_WebsocketInit.java`
#### Snippet
```java
     * List that stores the rendered markup of previous UIWebsocket instances.
     */
    private transient List<String> websocketComponentMarkupList = new java.util.ArrayList<String>();
    
    public List<String> getUIWebsocketMarkupList()
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/util/MessageUtils.java`
#### Snippet
```java
 *
 * @see FacesMessage
 * @see java.util.ResourceBundle
 */
public final class MessageUtils
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/util/lang/FilenameUtils.java`
#### Snippet
```java
 * <p>
 * <b>NOTE</b>: You may be able to avoid using this class entirely simply by
 * using JDK {@link java.io.File File} objects and the two argument constructor
 * {@link java.io.File#File(java.io.File, java.lang.String) File(File,String)}.
 * <p>
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/util/lang/FilenameUtils.java`
#### Snippet
```java
 * <b>NOTE</b>: You may be able to avoid using this class entirely simply by
 * using JDK {@link java.io.File File} objects and the two argument constructor
 * {@link java.io.File#File(java.io.File, java.lang.String) File(File,String)}.
 * <p>
 * Most methods on this class are designed to work the same on both Unix and Windows.
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/util/lang/FilenameUtils.java`
#### Snippet
```java
 * <b>NOTE</b>: You may be able to avoid using this class entirely simply by
 * using JDK {@link java.io.File File} objects and the two argument constructor
 * {@link java.io.File#File(java.io.File, java.lang.String) File(File,String)}.
 * <p>
 * Most methods on this class are designed to work the same on both Unix and Windows.
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/util/lang/FilenameUtils.java`
#### Snippet
```java
 * <b>NOTE</b>: You may be able to avoid using this class entirely simply by
 * using JDK {@link java.io.File File} objects and the two argument constructor
 * {@link java.io.File#File(java.io.File, java.lang.String) File(File,String)}.
 * <p>
 * Most methods on this class are designed to work the same on both Unix and Windows.
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.util.lang` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/util/lang/StringUtils.java`
#### Snippet
```java
        if (str == null || len == 0)
        {
            return org.apache.myfaces.util.lang.ArrayUtils.EMPTY_STRING_ARRAY;
        }

```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/util/lang/StreamCharBuffer.java`
#### Snippet
```java
     * cached value and the data read from the buffer.
     *
     * @see java.lang.Object#toString()
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/util/lang/StreamCharBuffer.java`
#### Snippet
```java
     * Uses String's hashCode to support compatibility with String instances in maps, sets, etc.
     *
     * @see java.lang.Object#hashCode()
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/util/lang/StreamCharBuffer.java`
#### Snippet
```java
     * in maps, sets, etc.
     *
     * @see java.lang.Object#equals(java.lang.Object)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/util/lang/StreamCharBuffer.java`
#### Snippet
```java
     * in maps, sets, etc.
     *
     * @see java.lang.Object#equals(java.lang.Object)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.el` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ContextAwarePropertyNotWritableException.java`
#### Snippet
```java
 */
public class ContextAwarePropertyNotWritableException
        extends jakarta.el.PropertyNotWritableException
        implements ContextAwareExceptionWrapper
{
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.el` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/CompositeFunctionMapper.java`
#### Snippet
```java

    /**
     * @see jakarta.el.FunctionMapper#resolveFunction(java.lang.String, java.lang.String)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/CompositeFunctionMapper.java`
#### Snippet
```java

    /**
     * @see jakarta.el.FunctionMapper#resolveFunction(java.lang.String, java.lang.String)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/CompositeFunctionMapper.java`
#### Snippet
```java

    /**
     * @see jakarta.el.FunctionMapper#resolveFunction(java.lang.String, java.lang.String)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.el` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/CompositeFunctionMapper.java`
#### Snippet
```java
 * <code>null</code>.
 * 
 * @see jakarta.el.FunctionMapper
 * @see java.lang.reflect.Method
 * 
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/CompositeFunctionMapper.java`
#### Snippet
```java
 * 
 * @see jakarta.el.FunctionMapper
 * @see java.lang.reflect.Method
 * 
 * @author Jacob Hookom
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.el` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/DefaultVariableMapper.java`
#### Snippet
```java
    
    /**
     * @see jakarta.el.VariableMapper#resolveVariable(java.lang.String)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/DefaultVariableMapper.java`
#### Snippet
```java
    
    /**
     * @see jakarta.el.VariableMapper#resolveVariable(java.lang.String)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.el` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/DefaultVariableMapper.java`
#### Snippet
```java
 * Default instance of a VariableMapper backed by a Map
 * 
 * @see jakarta.el.VariableMapper
 * @see jakarta.el.ValueExpression
 * @see java.util.Map
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.el` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/DefaultVariableMapper.java`
#### Snippet
```java
 * 
 * @see jakarta.el.VariableMapper
 * @see jakarta.el.ValueExpression
 * @see java.util.Map
 * 
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/DefaultVariableMapper.java`
#### Snippet
```java
 * @see jakarta.el.VariableMapper
 * @see jakarta.el.ValueExpression
 * @see java.util.Map
 * 
 * @author Jacob Hookom
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.el` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/DefaultVariableMapper.java`
#### Snippet
```java

    /**
     * @see jakarta.el.VariableMapper#setVariable(java.lang.String, jakarta.el.ValueExpression)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/DefaultVariableMapper.java`
#### Snippet
```java

    /**
     * @see jakarta.el.VariableMapper#setVariable(java.lang.String, jakarta.el.ValueExpression)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.el` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/DefaultVariableMapper.java`
#### Snippet
```java

    /**
     * @see jakarta.el.VariableMapper#setVariable(java.lang.String, jakarta.el.ValueExpression)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.el` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ContextAwarePropertyNotFoundException.java`
#### Snippet
```java
 */
public class ContextAwarePropertyNotFoundException
        extends jakarta.el.PropertyNotFoundException
        implements ContextAwareExceptionWrapper
{
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang.reflect` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/DefaultFunctionMapper.java`
#### Snippet
```java
 * Default implementation of the FunctionMapper
 * 
 * @see java.lang.reflect.Method
 * @see jakarta.el.FunctionMapper
 * 
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.el` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/DefaultFunctionMapper.java`
#### Snippet
```java
 * 
 * @see java.lang.reflect.Method
 * @see jakarta.el.FunctionMapper
 * 
 * @author Jacob Hookom
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.el` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/VariableMapperWrapper.java`
#### Snippet
```java
     * First tries to resolve agains the inner Map, then the wrapped ValueExpression.
     * 
     * @see jakarta.el.VariableMapper#resolveVariable(java.lang.String)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/VariableMapperWrapper.java`
#### Snippet
```java
     * First tries to resolve agains the inner Map, then the wrapped ValueExpression.
     * 
     * @see jakarta.el.VariableMapper#resolveVariable(java.lang.String)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.el` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/VariableMapperWrapper.java`
#### Snippet
```java
     * Set the ValueExpression on the inner Map instance.
     * 
     * @see jakarta.el.VariableMapper#setVariable(java.lang.String, jakarta.el.ValueExpression)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/VariableMapperWrapper.java`
#### Snippet
```java
     * Set the ValueExpression on the inner Map instance.
     * 
     * @see jakarta.el.VariableMapper#setVariable(java.lang.String, jakarta.el.ValueExpression)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.el` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/VariableMapperWrapper.java`
#### Snippet
```java
     * Set the ValueExpression on the inner Map instance.
     * 
     * @see jakarta.el.VariableMapper#setVariable(java.lang.String, jakarta.el.ValueExpression)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/VariableMapperWrapper.java`
#### Snippet
```java

/**
 * Utility class for wrapping another VariableMapper with a new context, represented by a {@link java.util.Map Map}.
 * Modifications occur to the Map instance, but resolve against the wrapped VariableMapper if the Map doesn't contain
 * the ValueExpression requested.
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.view.facelets` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/CompositeTagDecorator.java`
#### Snippet
```java
     * null.
     * 
     * @see jakarta.faces.view.facelets.TagDecorator#decorate(jakarta.faces.view.facelets.Tag)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.view.facelets` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/CompositeTagDecorator.java`
#### Snippet
```java
     * null.
     * 
     * @see jakarta.faces.view.facelets.TagDecorator#decorate(jakarta.faces.view.facelets.Tag)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.view.facelets` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/LegacyUserTagHandler.java`
#### Snippet
```java
     * @see TagAttribute#getValueExpression(FaceletContext, Class)
     * @see VariableMapper
     * @see jakarta.faces.view.facelets.FaceletHandler#apply(jakarta.faces.view.facelets.FaceletContext, jakarta.faces.component.UIComponent)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.component` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/LegacyUserTagHandler.java`
#### Snippet
```java
     * @see TagAttribute#getValueExpression(FaceletContext, Class)
     * @see VariableMapper
     * @see jakarta.faces.view.facelets.FaceletHandler#apply(jakarta.faces.view.facelets.FaceletContext, jakarta.faces.component.UIComponent)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.view.facelets` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/UserTagHandler.java`
#### Snippet
```java
     * @see TagAttribute#getValueExpression(FaceletContext, Class)
     * @see jakarta.el.VariableMapper
     * @see jakarta.faces.view.facelets.FaceletHandler#apply(jakarta.faces.view.facelets.FaceletContext,
     *        jakarta.faces.component.UIComponent)
     */
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.component` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/UserTagHandler.java`
#### Snippet
```java
     * @see jakarta.el.VariableMapper
     * @see jakarta.faces.view.facelets.FaceletHandler#apply(jakarta.faces.view.facelets.FaceletContext,
     *        jakarta.faces.component.UIComponent)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.view.facelets` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/TagHandlerUtils.java`
#### Snippet
```java
            found.add((FaceletHandler) nextHandler);
        }
        else if (nextHandler instanceof jakarta.faces.view.facelets.CompositeFaceletHandler)
        {
            for (FaceletHandler handler :
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.view.facelets` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/TagHandlerUtils.java`
#### Snippet
```java
        {
            for (FaceletHandler handler :
                    ((jakarta.faces.view.facelets.CompositeFaceletHandler)nextHandler).getHandlers())
            {
                if (type1.isAssignableFrom(handler.getClass()) || type2.isAssignableFrom(handler.getClass()))
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.view.facelets` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/TagHandlerUtils.java`
#### Snippet
```java
            return (T) nextHandler;
        }
        else if (nextHandler instanceof jakarta.faces.view.facelets.CompositeFaceletHandler)
        {
            for (FaceletHandler handler :
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.view.facelets` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/TagHandlerUtils.java`
#### Snippet
```java
        {
            for (FaceletHandler handler :
                    ((jakarta.faces.view.facelets.CompositeFaceletHandler)nextHandler).getHandlers())
            {
                if (type.isAssignableFrom(handler.getClass()))
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.view.facelets` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/TagHandlerUtils.java`
#### Snippet
```java
            found.add((FaceletHandler) nextHandler);
        }
        else if (nextHandler instanceof jakarta.faces.view.facelets.CompositeFaceletHandler)
        {
            for (FaceletHandler handler :
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.view.facelets` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/TagHandlerUtils.java`
#### Snippet
```java
        {
            for (FaceletHandler handler :
                    ((jakarta.faces.view.facelets.CompositeFaceletHandler) nextHandler).getHandlers())
            {
                isAssignable = false;
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.view.facelets` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/TagHandlerUtils.java`
#### Snippet
```java
            found.add((T) nextHandler);
        }
        else if (nextHandler instanceof jakarta.faces.view.facelets.CompositeFaceletHandler)
        {
            for (FaceletHandler handler :
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.view.facelets` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/TagHandlerUtils.java`
#### Snippet
```java
        {
            for (FaceletHandler handler :
                    ((jakarta.faces.view.facelets.CompositeFaceletHandler)nextHandler).getHandlers())
            {
                if (type.isAssignableFrom(handler.getClass()))
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.view.facelets` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ComponentTagDeclarationLibrary.java`
#### Snippet
```java
        {
            ComponentConfig ccfg = new ComponentConfigWrapper(cfg, this.componentType, this.renderType);
            return new jakarta.faces.view.facelets.ComponentHandler(ccfg);
        }
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.el` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ELText.java`
#### Snippet
```java
     *            The location
     * @return ELText that can be re-applied later
     * @throws jakarta.el.ELException
     */
    public static ELText parse(ExpressionFactory fact, ELContext ctx, String in, Location location) throws ELException
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.view.facelets.el` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ELText.java`
#### Snippet
```java
    /**
     * Factory method for creating an unvalidated ELText instance. NOTE: All expressions in the passed String are
     * treated as {@link org.apache.myfaces.view.facelets.el.LocationValueExpression}.
     * 
     * @param in
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.el` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ELText.java`
#### Snippet
```java
     *            The location
     * @return ELText instance that knows if the String was literal or not
     * @throws jakarta.el.ELException
     */
    public static ELText parse(String in, Location location) throws ELException
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/TagAttributeImpl.java`
#### Snippet
```java

    /**
     * If literal, return {@link Boolean#getBoolean(java.lang.String) Boolean.getBoolean(java.lang.String)} passing our
     * value, otherwise call {@link #getObject(FaceletContext, Class) getObject(FaceletContext, Class)}.
     * 
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/TagAttributeImpl.java`
#### Snippet
```java

    /**
     * If literal, call {@link Integer#parseInt(java.lang.String) Integer.parseInt(String)}, otherwise call
     * {@link #getObject(FaceletContext, Class) getObject(FaceletContext, Class)}.
     * 
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.view.facelets` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/JsfElementHandler.java`
#### Snippet
```java
        name = "jsf:element",
        componentClass = "org.apache.myfaces.view.facelets.component.JsfElement")
public class JsfElementHandler extends jakarta.faces.view.facelets.ComponentHandler
{

```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/LoadBundleHandler.java`
#### Snippet
```java
                }
            }
            catch (java.util.MissingResourceException mre)
            {
                // NOOP
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.view.facelets` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/ViewHandler.java`
#### Snippet
```java
     * See taglib documentation.
     * 
     * @see jakarta.faces.view.facelets.FaceletHandler#apply(jakarta.faces.view.facelets.FaceletContext, jakarta.faces.component.UIComponent)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.component` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/core/ViewHandler.java`
#### Snippet
```java
     * See taglib documentation.
     * 
     * @see jakarta.faces.view.facelets.FaceletHandler#apply(jakarta.faces.view.facelets.FaceletContext, jakarta.faces.component.UIComponent)
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.validation.groups` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ComponentTagHandlerDelegate.java`
#### Snippet
```java
                            // no validationGroups on the stack
                            // --> set the default validationGroup
                            validationGroups = jakarta.validation.groups.Default.class.getName();
                        //}
                        beanValidator.setValidationGroups(validationGroups);
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.validation.groups` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/ComponentTagHandlerDelegate.java`
#### Snippet
```java
                    // no validationGroups on the stack
                    // --> set the default validationGroup
                    validationGroups = jakarta.validation.groups.Default.class.getName();
                //}
                beanValidator.setValidationGroups(validationGroups);
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.view.facelets` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeComponentResourceTagHandler.java`
#### Snippet
```java
        List<String> insertFacetList = (List<String>) beanDescriptor.getValue(InsertFacetHandler.INSERT_FACET_USED);
        
        if (nextHandler instanceof jakarta.faces.view.facelets.CompositeFaceletHandler)
        {
            for (FaceletHandler handler :
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.view.facelets` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeComponentResourceTagHandler.java`
#### Snippet
```java
        {
            for (FaceletHandler handler :
                    ((jakarta.faces.view.facelets.CompositeFaceletHandler)nextHandler).getHandlers())
            {
                if (handler instanceof jakarta.faces.view.facelets.FacetHandler)
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.view.facelets` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeComponentResourceTagHandler.java`
#### Snippet
```java
                else if (insertChildrenUsed)
                {
                    if (!(handler instanceof jakarta.faces.view.facelets.ComponentHandler ||
                            handler instanceof ComponentContainerHandler ||
                            handler instanceof TextHandler))
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.view.facelets` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeComponentResourceTagHandler.java`
#### Snippet
```java
            else if (insertChildrenUsed)
            {
                if (!(nextHandler instanceof jakarta.faces.view.facelets.ComponentHandler ||
                      nextHandler instanceof ComponentContainerHandler ||
                      nextHandler instanceof TextHandler))
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.view.facelets` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/composite/CompositeComponentResourceTagHandler.java`
#### Snippet
```java
                                                        InsertFacetHandler.class);
        _componentHandlers = TagHandlerUtils.findNextByType(nextHandler,
                jakarta.faces.view.facelets.ComponentHandler.class,
                ComponentContainerHandler.class, TextHandler.class);
        _dynamicCompositeComponent = false;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFaceletFactory.java`
#### Snippet
```java
    /**
     * Resolves a path based on the passed URL. If the path starts with '/', then resolve the path against
     * {@link jakarta.faces.context.ExternalContext#getResource(java.lang.String)
     * jakarta.faces.context.ExternalContext#getResource(java.lang.String)}. Otherwise create a new URL via
     * {@link URL#URL(java.net.URL, java.lang.String) URL(URL, String)}.
```

### UnnecessaryFullyQualifiedName
Qualifier `java.net` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFaceletFactory.java`
#### Snippet
```java
     * {@link jakarta.faces.context.ExternalContext#getResource(java.lang.String)
     * jakarta.faces.context.ExternalContext#getResource(java.lang.String)}. Otherwise create a new URL via
     * {@link URL#URL(java.net.URL, java.lang.String) URL(URL, String)}.
     * 
     * @param source
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFaceletFactory.java`
#### Snippet
```java
     * {@link jakarta.faces.context.ExternalContext#getResource(java.lang.String)
     * jakarta.faces.context.ExternalContext#getResource(java.lang.String)}. Otherwise create a new URL via
     * {@link URL#URL(java.net.URL, java.lang.String) URL(URL, String)}.
     * 
     * @param source
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.view.facelets` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFacelet.java`
#### Snippet
```java
     * Used for delegation by the DefaultFaceletContext. First pulls the URL from {@link #getRelativePath(String)
     * getRelativePath(String)}, then calls
     * {@link #include(org.apache.myfaces.view.facelets.AbstractFaceletContext,
     * jakarta.faces.component.UIComponent, java.net.URL)}.
     * 
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.component` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFacelet.java`
#### Snippet
```java
     * getRelativePath(String)}, then calls
     * {@link #include(org.apache.myfaces.view.facelets.AbstractFaceletContext,
     * jakarta.faces.component.UIComponent, java.net.URL)}.
     * 
     * @see FaceletContext#includeFacelet(UIComponent, String)
```

### UnnecessaryFullyQualifiedName
Qualifier `java.net` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFacelet.java`
#### Snippet
```java
     * getRelativePath(String)}, then calls
     * {@link #include(org.apache.myfaces.view.facelets.AbstractFaceletContext,
     * jakarta.faces.component.UIComponent, java.net.URL)}.
     * 
     * @see FaceletContext#includeFacelet(UIComponent, String)
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.view.facelets` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/CompilationUnit.java`
#### Snippet
```java
            fh[i] = ((CompilationUnit) this.children.get(i)).createFaceletHandler();
        }
        return new jakarta.faces.view.facelets.CompositeFaceletHandler(fh);
    }

```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.view.facelets.tag` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TagLibraryConfig.java`
#### Snippet
```java

/**
 * Handles creating a {@link org.apache.myfaces.view.facelets.tag.TagLibrary TagLibrary}
 * from a {@link java.net.URL URL} source.
 * 
```

### UnnecessaryFullyQualifiedName
Qualifier `java.net` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TagLibraryConfig.java`
#### Snippet
```java
/**
 * Handles creating a {@link org.apache.myfaces.view.facelets.tag.TagLibrary TagLibrary}
 * from a {@link java.net.URL URL} source.
 * 
 * @author Jacob Hookom
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.view.facelets.tag.faces.core` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/RefreshDynamicComponentListener.java`
#### Snippet
```java
                if (facetName != null)
                {
                    parent.getAttributes().put(org.apache.myfaces.view.facelets.tag.faces.core.FacetHandler.KEY, 
                            facetName);
                }            
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.view.facelets.tag.faces.core` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/RefreshDynamicComponentListener.java`
#### Snippet
```java
                if (facetName != null)
                {
                    parent.getAttributes().remove(org.apache.myfaces.view.facelets.tag.faces.core.FacetHandler.KEY);
                }
            }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/FacesConfigDispenser.java`
#### Snippet
```java
     * @param facesConfig unmarshalled faces config object
     */
    public abstract void feed(org.apache.myfaces.config.element.FacesConfig facesConfig);

    /**
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/FacesFlowParameterImpl.java`
#### Snippet
```java
 * @author Leonardo Uribe
 */
public class FacesFlowParameterImpl extends org.apache.myfaces.config.element.FacesFlowParameter
{
    private String name;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/AbsoluteOrderingImpl.java`
#### Snippet
```java
public class AbsoluteOrderingImpl extends org.apache.myfaces.config.element.AbsoluteOrdering implements Serializable
{
    private List<org.apache.myfaces.config.element.OrderSlot> orderList;
    
    public void addOrderSlot(org.apache.myfaces.config.element.OrderSlot slot)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/AbsoluteOrderingImpl.java`
#### Snippet
```java
import java.util.List;

public class AbsoluteOrderingImpl extends org.apache.myfaces.config.element.AbsoluteOrdering implements Serializable
{
    private List<org.apache.myfaces.config.element.OrderSlot> orderList;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/AbsoluteOrderingImpl.java`
#### Snippet
```java

    @Override
    public List<org.apache.myfaces.config.element.OrderSlot> getOrderList()
    {
        if (orderList == null)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/AbsoluteOrderingImpl.java`
#### Snippet
```java
    private List<org.apache.myfaces.config.element.OrderSlot> orderList;
    
    public void addOrderSlot(org.apache.myfaces.config.element.OrderSlot slot)
    {
        if (orderList == null)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.impl.element` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/config/DefaultFacesConfigurationProvider.java`
#### Snippet
```java
        try
        {
            org.apache.myfaces.config.impl.element.FacesConfigImpl facesConfig
                    = new org.apache.myfaces.config.impl.element.FacesConfigImpl();
            org.apache.myfaces.config.impl.element.FactoryImpl factory
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.impl.element` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/config/DefaultFacesConfigurationProvider.java`
#### Snippet
```java
        {
            org.apache.myfaces.config.impl.element.FacesConfigImpl facesConfig
                    = new org.apache.myfaces.config.impl.element.FacesConfigImpl();
            org.apache.myfaces.config.impl.element.FactoryImpl factory
                    = new org.apache.myfaces.config.impl.element.FactoryImpl();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.impl.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/DefaultFacesConfigurationProvider.java`
#### Snippet
```java
            org.apache.myfaces.config.impl.element.FacesConfigImpl facesConfig
                    = new org.apache.myfaces.config.impl.element.FacesConfigImpl();
            org.apache.myfaces.config.impl.element.FactoryImpl factory
                    = new org.apache.myfaces.config.impl.element.FactoryImpl();
            
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.impl.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/DefaultFacesConfigurationProvider.java`
#### Snippet
```java
                    = new org.apache.myfaces.config.impl.element.FacesConfigImpl();
            org.apache.myfaces.config.impl.element.FactoryImpl factory
                    = new org.apache.myfaces.config.impl.element.FactoryImpl();
            
            facesConfig.addFactory(factory);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.impl.element` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/config/DefaultFacesConfigurationProvider.java`
#### Snippet
```java
                    if (tl != null)
                    {
                        org.apache.myfaces.config.impl.element.FacesConfigImpl config = 
                            new org.apache.myfaces.config.impl.element.FacesConfigImpl();
                        config.addFaceletTagLibrary(tl);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.impl.element` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/config/DefaultFacesConfigurationProvider.java`
#### Snippet
```java
                    {
                        org.apache.myfaces.config.impl.element.FacesConfigImpl config = 
                            new org.apache.myfaces.config.impl.element.FacesConfigImpl();
                        config.addFaceletTagLibrary(tl);
                        facesConfigFilesList.add(config);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.impl.element` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/config/DefaultFacesConfigurationProvider.java`
#### Snippet
```java
                    if (tl != null)
                    {
                        org.apache.myfaces.config.impl.element.FacesConfigImpl config = 
                            new org.apache.myfaces.config.impl.element.FacesConfigImpl();
                        config.addFaceletTagLibrary(tl);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.impl.element` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/config/DefaultFacesConfigurationProvider.java`
#### Snippet
```java
                    {
                        org.apache.myfaces.config.impl.element.FacesConfigImpl config = 
                            new org.apache.myfaces.config.impl.element.FacesConfigImpl();
                        config.addFaceletTagLibrary(tl);
                        facesConfigFilesList.add(config);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/FacesFlowDefinitionImpl.java`
#### Snippet
```java
 * @author Leonardo Uribe
 */
public class FacesFlowDefinitionImpl extends org.apache.myfaces.config.element.FacesFlowDefinition
{
    private String definingDocumentId;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/RuntimeConfig.java`
#### Snippet
```java
    }
    
    public org.apache.myfaces.config.element.Converter getConverterConfiguration(String converterClassName)
    {
        return (org.apache.myfaces.config.element.Converter)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/RuntimeConfig.java`
#### Snippet
```java
    public org.apache.myfaces.config.element.Converter getConverterConfiguration(String converterClassName)
    {
        return (org.apache.myfaces.config.element.Converter)
                _converterClassNameToConfigurationMap.get(converterClassName);
    }
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/config/RuntimeConfig.java`
#### Snippet
```java
     * Return the navigation rules that can be used by the NavigationHandler implementation.
     * 
     * @return a Collection of {@link org.apache.myfaces.config.element.NavigationRule NavigationRule}s
     */
    public Collection<NavigationRule> getNavigationRules()
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/RuntimeConfig.java`
#### Snippet
```java
    private Predicate<ELResolver> _elResolverPredicate;

    private final Map<String, org.apache.myfaces.config.element.Converter> _converterClassNameToConfigurationMap =
        new ConcurrentHashMap<>();
    
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/RuntimeConfig.java`
#### Snippet
```java
    
    public final void addConverterConfiguration(final String converterClassName,
            final org.apache.myfaces.config.element.Converter configuration)
    {
        Assert.notEmpty(converterClassName, "converterClassName");
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/PropertyImpl.java`
#### Snippet
```java
 *
 */
public class PropertyImpl extends org.apache.myfaces.config.element.Property implements Serializable
{
    private List<String> description;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/FaceletsProcessingImpl.java`
#### Snippet
```java
 * @since 2.1.0
 */
public class FaceletsProcessingImpl extends org.apache.myfaces.config.element.FaceletsProcessing 
{
    private static final long serialVersionUID = 7692451499973040255L;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/AttributeImpl.java`
#### Snippet
```java
 * @version $Revision$ $Date$
 */
public class AttributeImpl extends org.apache.myfaces.config.element.Attribute implements Serializable
{
    private List<String> description;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/FacesConfigNameSlotImpl.java`
#### Snippet
```java
import java.io.Serializable;

public class FacesConfigNameSlotImpl extends org.apache.myfaces.config.element.FacesConfigNameSlot
    implements Serializable
{
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/FactoryImpl.java`
#### Snippet
```java
 * @author <a href="mailto:oliver@rossmueller.com">Oliver Rossmueller</a>
 */
public class FactoryImpl extends org.apache.myfaces.config.element.Factory implements Serializable
{
    private List<String> applicationFactories;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ViewParamImpl.java`
#### Snippet
```java
import java.io.Serializable;

public class ViewParamImpl extends org.apache.myfaces.config.element.ViewParam implements Serializable
{
    private String name;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ResourceBundleImpl.java`
#### Snippet
```java
 * @version $Revision$ $Date$
 */
public class ResourceBundleImpl extends org.apache.myfaces.config.element.ResourceBundle implements Serializable
{
    private String baseName;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/NamedEventImpl.java`
#### Snippet
```java
import java.io.Serializable;

public class NamedEventImpl extends org.apache.myfaces.config.element.NamedEvent implements Serializable
{
    private String shortName;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/BehaviorImpl.java`
#### Snippet
```java
 * Implementation of model for &lt;behavior&gt; element.
 */
public class BehaviorImpl extends org.apache.myfaces.config.element.Behavior implements Serializable
{
    private String behaviorClass;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/FacesConfigExtensionImpl.java`
#### Snippet
```java
    private static final long serialVersionUID = 7624583794474223864L;
    
    private List<org.apache.myfaces.config.element.FaceletsProcessing> _faceletsProcessingList;
    
    private List<ViewPoolMapping> viewPoolMappings;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/FacesConfigExtensionImpl.java`
#### Snippet
```java
    }
    
    public void addFaceletsProcessing(org.apache.myfaces.config.element.FaceletsProcessing elem)
    {
        if (_faceletsProcessingList == null)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/FacesConfigExtensionImpl.java`
#### Snippet
```java
 * @since 2.1.0
 */
public class FacesConfigExtensionImpl extends org.apache.myfaces.config.element.FacesConfigExtension
{
    private static final long serialVersionUID = 7624583794474223864L;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/FacesConfigExtensionImpl.java`
#### Snippet
```java
    
    @Override
    public List<org.apache.myfaces.config.element.FaceletsProcessing> getFaceletsProcessingList()
    {
        if (_faceletsProcessingList == null)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/OrderingImpl.java`
#### Snippet
```java
    
    @Override
    public List<org.apache.myfaces.config.element.OrderSlot> getAfterList()
    {
        if (afterList == null)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/OrderingImpl.java`
#### Snippet
```java
    }
    
    public void addAfterSlot(org.apache.myfaces.config.element.OrderSlot slot)
    {
        if (afterList == null)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/OrderingImpl.java`
#### Snippet
```java
    
    @Override
    public List<org.apache.myfaces.config.element.OrderSlot> getBeforeList()
    {
        if (beforeList == null)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/OrderingImpl.java`
#### Snippet
```java
import java.util.List;

public class OrderingImpl extends org.apache.myfaces.config.element.Ordering implements Serializable
{
    private List<org.apache.myfaces.config.element.OrderSlot> beforeList;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/OrderingImpl.java`
#### Snippet
```java
{
    private List<org.apache.myfaces.config.element.OrderSlot> beforeList;
    private List<org.apache.myfaces.config.element.OrderSlot> afterList;

    public void addBeforeSlot(org.apache.myfaces.config.element.OrderSlot slot)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/OrderingImpl.java`
#### Snippet
```java
    private List<org.apache.myfaces.config.element.OrderSlot> afterList;

    public void addBeforeSlot(org.apache.myfaces.config.element.OrderSlot slot)
    {
        if (beforeList == null)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/OrderingImpl.java`
#### Snippet
```java
public class OrderingImpl extends org.apache.myfaces.config.element.Ordering implements Serializable
{
    private List<org.apache.myfaces.config.element.OrderSlot> beforeList;
    private List<org.apache.myfaces.config.element.OrderSlot> afterList;

```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/NavigationCaseImpl.java`
#### Snippet
```java

    @Override
    public org.apache.myfaces.config.element.Redirect getRedirect()
    {
        return redirect;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/NavigationCaseImpl.java`
#### Snippet
```java
 * @author <a href="mailto:oliver@rossmueller.com">Oliver Rossmueller</a>
 */
public class NavigationCaseImpl extends org.apache.myfaces.config.element.NavigationCase implements Serializable
{    
    private String fromAction;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ApplicationImpl.java`
#### Snippet
```java

    @Override
    public List<org.apache.myfaces.config.element.LocaleConfig> getLocaleConfig()
    {
        if (localeConfig == null)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/NavigationCaseImpl.java`
#### Snippet
```java
    }

    public void setRedirect(org.apache.myfaces.config.element.Redirect redirect)
    {
        this.redirect = redirect;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ApplicationImpl.java`
#### Snippet
```java
    }
    
    public void addSystemEventListener(org.apache.myfaces.config.element.SystemEventListener systemEventListener)
    {
        if (systemEventListeners == null)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/NavigationCaseImpl.java`
#### Snippet
```java
    private String ifValue;
    private String toViewId;
    private org.apache.myfaces.config.element.Redirect redirect;

    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ApplicationImpl.java`
#### Snippet
```java
    
    @Override
    public List<org.apache.myfaces.config.element.SystemEventListener> getSystemEventListeners()
    {
        if (systemEventListeners == null)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ApplicationImpl.java`
#### Snippet
```java
 * @author <a href="mailto:oliver@rossmueller.com">Oliver Rossmueller</a>
 */
public class ApplicationImpl extends org.apache.myfaces.config.element.Application implements Serializable
{
    private List<String> actionListener;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ApplicationImpl.java`
#### Snippet
```java
    private List<String> propertyResolver;
    private List<String> variableResolver;
    private List<org.apache.myfaces.config.element.LocaleConfig> localeConfig;
    private List<String> elResolver;
    private List<org.apache.myfaces.config.element.ResourceBundle> resourceBundle;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ApplicationImpl.java`
#### Snippet
```java
    private List<org.apache.myfaces.config.element.LocaleConfig> localeConfig;
    private List<String> elResolver;
    private List<org.apache.myfaces.config.element.ResourceBundle> resourceBundle;
    private List<org.apache.myfaces.config.element.SystemEventListener> systemEventListeners;
    private List<ContractMapping> resourceLibraryContractMappings;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ApplicationImpl.java`
#### Snippet
```java

    @Override
    public List<org.apache.myfaces.config.element.ResourceBundle> getResourceBundle()
    {
        if (resourceBundle == null)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ApplicationImpl.java`
#### Snippet
```java
    private List<String> elResolver;
    private List<org.apache.myfaces.config.element.ResourceBundle> resourceBundle;
    private List<org.apache.myfaces.config.element.SystemEventListener> systemEventListeners;
    private List<ContractMapping> resourceLibraryContractMappings;
    private List<String> searchKeywordResolver;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ApplicationImpl.java`
#### Snippet
```java
    }

    public void addLocaleConfig(org.apache.myfaces.config.element.LocaleConfig config)
    {
        if (localeConfig == null)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ApplicationImpl.java`
#### Snippet
```java
    }

    public void addResourceBundle(org.apache.myfaces.config.element.ResourceBundle bundle)
    {
        if (resourceBundle == null)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ConverterImpl.java`
#### Snippet
```java
    }

    public void addProperty(org.apache.myfaces.config.element.Property value)
    {
        if (properties == null)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ConverterImpl.java`
#### Snippet
```java
    private String converterClass;
    private List<org.apache.myfaces.config.element.Property> properties = null;
    private List<org.apache.myfaces.config.element.Attribute> attributes = null;

    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ConverterImpl.java`
#### Snippet
```java

    @Override
    public List<? extends org.apache.myfaces.config.element.Attribute> getAttributes()
    {
        if (attributes == null)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ConverterImpl.java`
#### Snippet
```java

    @Override
    public List<? extends org.apache.myfaces.config.element.Property> getProperties()
    {
        if (properties == null)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ConverterImpl.java`
#### Snippet
```java
 * @author <a href="mailto:oliver@rossmueller.com">Oliver Rossmueller</a>
 */
public class ConverterImpl extends org.apache.myfaces.config.element.Converter implements Serializable
{
    private String converterId;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ConverterImpl.java`
#### Snippet
```java
    }
    
    public void addAttribute(org.apache.myfaces.config.element.Attribute value)
    {
        if (attributes == null)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ConverterImpl.java`
#### Snippet
```java
    private String forClass;
    private String converterClass;
    private List<org.apache.myfaces.config.element.Property> properties = null;
    private List<org.apache.myfaces.config.element.Attribute> attributes = null;

```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/SystemEventListenerImpl.java`
#### Snippet
```java
import java.io.Serializable;

public class SystemEventListenerImpl extends org.apache.myfaces.config.element.SystemEventListener
    implements Serializable
{
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/FacesFlowReturnImpl.java`
#### Snippet
```java
 * @author Leonardo Uribe
 */
public class FacesFlowReturnImpl extends org.apache.myfaces.config.element.FacesFlowReturn
{
    private String id;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/LocaleConfigImpl.java`
#### Snippet
```java
 * @author <a href="mailto:oliver@rossmueller.com">Oliver Rossmueller</a>
 */
public class LocaleConfigImpl extends org.apache.myfaces.config.element.LocaleConfig implements Serializable
{
    private String defaultLocale;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/FacesFlowViewImpl.java`
#### Snippet
```java
 * @author Leonardo Uribe
 */
public class FacesFlowViewImpl extends org.apache.myfaces.config.element.FacesFlowView
{
    private String id;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/RendererImpl.java`
#### Snippet
```java
 * @author <a href="mailto:oliver@rossmueller.com">Oliver Rossmueller</a>
 */
public class RendererImpl extends org.apache.myfaces.config.element.Renderer implements Serializable
{
    private String componentFamily;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/FacesFlowSwitchImpl.java`
#### Snippet
```java
 * @author Leonardo Uribe
 */
public class FacesFlowSwitchImpl extends org.apache.myfaces.config.element.FacesFlowSwitch
{
    private String _id;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ConfigOthersSlotImpl.java`
#### Snippet
```java
import java.io.Serializable;

public class ConfigOthersSlotImpl extends org.apache.myfaces.config.element.ConfigOthersSlot implements Serializable
{

```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/RenderKitImpl.java`
#### Snippet
```java
    
    @Override
    public List<org.apache.myfaces.config.element.Renderer> getRenderer()
    {
        if (renderer == null)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/RenderKitImpl.java`
#### Snippet
```java

    @Override
    public List<org.apache.myfaces.config.element.ClientBehaviorRenderer> getClientBehaviorRenderers ()
    {
        if (clientBehaviorRenderers == null)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/RenderKitImpl.java`
#### Snippet
```java
    private List<String> renderKitClasses;
    private List<org.apache.myfaces.config.element.Renderer> renderer;
    private List<org.apache.myfaces.config.element.ClientBehaviorRenderer> clientBehaviorRenderers;
    
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/RenderKitImpl.java`
#### Snippet
```java

    @Override
    public void merge(org.apache.myfaces.config.element.RenderKit renderKit)
    {
        if (!renderKit.getRenderKitClasses().isEmpty())
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/RenderKitImpl.java`
#### Snippet
```java
    private String id;
    private List<String> renderKitClasses;
    private List<org.apache.myfaces.config.element.Renderer> renderer;
    private List<org.apache.myfaces.config.element.ClientBehaviorRenderer> clientBehaviorRenderers;
    
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/RenderKitImpl.java`
#### Snippet
```java
    }

    public void addClientBehaviorRenderer(org.apache.myfaces.config.element.ClientBehaviorRenderer renderer)
    {
        if (clientBehaviorRenderers == null)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/RenderKitImpl.java`
#### Snippet
```java
 * @author <a href="mailto:oliver@rossmueller.com">Oliver Rossmueller</a>
 */
public class RenderKitImpl extends org.apache.myfaces.config.element.RenderKit implements Serializable
{
    private String id;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/RenderKitImpl.java`
#### Snippet
```java
    }
    
    public void addRenderer(org.apache.myfaces.config.element.Renderer value)
    {
        if (renderer == null)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ComponentImpl.java`
#### Snippet
```java
    }

    public void addProperty(org.apache.myfaces.config.element.Property value)
    {
        if (properties == null)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ComponentImpl.java`
#### Snippet
```java
 * @author <a href="mailto:oliver@rossmueller.com">Oliver Rossmueller</a>
 */
public class ComponentImpl extends org.apache.myfaces.config.element.Component implements Serializable
{
    private String componentType;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ComponentImpl.java`
#### Snippet
```java

    @Override
    public List<? extends org.apache.myfaces.config.element.Attribute> getAttributes()
    {
        if (attributes == null)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ComponentImpl.java`
#### Snippet
```java
    private String componentType;
    private String componentClass;
    private List<org.apache.myfaces.config.element.Property> properties = null;
    private List<org.apache.myfaces.config.element.Attribute> attributes = null;

```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ComponentImpl.java`
#### Snippet
```java

    @Override
    public List<? extends org.apache.myfaces.config.element.Property> getProperties()
    {
        if (properties == null)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ComponentImpl.java`
#### Snippet
```java
    private String componentClass;
    private List<org.apache.myfaces.config.element.Property> properties = null;
    private List<org.apache.myfaces.config.element.Attribute> attributes = null;

    public ComponentImpl()
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ComponentImpl.java`
#### Snippet
```java
    }
    
    public void addAttribute(org.apache.myfaces.config.element.Attribute value)
    {
        if (attributes == null)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/NavigationRuleImpl.java`
#### Snippet
```java

    @Override
    public List<org.apache.myfaces.config.element.NavigationCase> getNavigationCases()
    {
        if (navigationCases == null)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/NavigationRuleImpl.java`
#### Snippet
```java
    }

    public void addNavigationCase(org.apache.myfaces.config.element.NavigationCase value)
    {
        if (navigationCases == null)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/NavigationRuleImpl.java`
#### Snippet
```java
 * @author <a href="mailto:oliver@rossmueller.com">Oliver Rossmueller</a>
 */
public class NavigationRuleImpl extends org.apache.myfaces.config.element.NavigationRule implements Serializable
{
    private String fromViewId;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/NavigationRuleImpl.java`
#### Snippet
```java
{
    private String fromViewId;
    private List<org.apache.myfaces.config.element.NavigationCase> navigationCases;

    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/RedirectImpl.java`
#### Snippet
```java
import java.util.Map;

public class RedirectImpl extends org.apache.myfaces.config.element.Redirect implements Serializable
{
    private Map<String,List<String>> viewParams;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/FacesFlowMethodCallImpl.java`
#### Snippet
```java
 * @author Leonardo Uribe
 */
public class FacesFlowMethodCallImpl extends org.apache.myfaces.config.element.FacesFlowMethodCall
{
    private String id;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ClientBehaviorRendererImpl.java`
#### Snippet
```java
 */
public class ClientBehaviorRendererImpl
        extends org.apache.myfaces.config.element.ClientBehaviorRenderer
        implements Serializable
{
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/OrderSlotImpl.java`
#### Snippet
```java
import java.io.Serializable;

public class OrderSlotImpl extends org.apache.myfaces.config.element.OrderSlot implements Serializable
{

```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/config/impl/element/FacesConfigImpl.java`
#### Snippet
```java
    }

    public void setOrdering(org.apache.myfaces.config.element.Ordering ordering)
    {
        this.ordering = ordering;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/config/impl/element/FacesConfigImpl.java`
#### Snippet
```java

    @Override
    public org.apache.myfaces.config.element.Ordering getOrdering()
    {
        return ordering;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/config/impl/element/FacesConfigImpl.java`
#### Snippet
```java

    @Override
    public List<org.apache.myfaces.config.element.FacesConfigExtension> getFacesConfigExtensions()
    {
        if (facesConfigExtensions == null)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/config/impl/element/FacesConfigImpl.java`
#### Snippet
```java
    
    @Override
    public org.apache.myfaces.config.element.AbsoluteOrdering getAbsoluteOrdering()
    {
        return absoluteOrdering;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/config/impl/element/FacesConfigImpl.java`
#### Snippet
```java
    }

    public void setAbsoluteOrdering(org.apache.myfaces.config.element.AbsoluteOrdering absoluteOrdering)
    {
        this.absoluteOrdering = absoluteOrdering;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/element/FacesConfigImpl.java`
#### Snippet
```java
 * @author <a href="mailto:oliver@rossmueller.com">Oliver Rossmueller</a>
 */
public class FacesConfigImpl extends org.apache.myfaces.config.element.FacesConfig implements Serializable
{
    private static final long serialVersionUID = 1L;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/config/element/NavigationRule.java`
#### Snippet
```java

    /**
     * @return a Collection of {@link org.apache.myfaces.config.element.NavigationCase}s
     */
    public abstract List<? extends NavigationCase> getNavigationCases();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/config/element/FacesConfigData.java`
#### Snippet
```java

    /**
     * @return Iterator over {@link org.apache.myfaces.config.element.ClientBehaviorRenderer ClientBehaviorRenderer}s
     *         for the given renderKitId
     */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/config/element/FacesConfigData.java`
#### Snippet
```java
    
    /**
     * @return Iterator over {@link org.apache.myfaces.config.element.Renderer Renderer}s for the given renderKitId
     */
    public abstract Collection<Renderer> getRenderers(String renderKitId);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/config/element/FacesConfigData.java`
#### Snippet
```java

    /**
     * @return Iterator over {@link org.apache.myfaces.config.element.NavigationRule NavigationRule}s
     */
    public abstract Collection<NavigationRule> getNavigationRules();
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigDispenserImpl.java`
#### Snippet
```java
    
    /**
     * @return Collection over {@link org.apache.myfaces.config.element.Renderer Renderer}s for the given renderKitId
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigDispenserImpl.java`
#### Snippet
```java
    /**
     * @return Iterator over
     * {@link org.apache.myfaces.config.element.ClientBehaviorRenderer ClientBehaviorRenderer}s
     * for the given renderKitId
     */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigDispenserImpl.java`
#### Snippet
```java

    /**
     * @return Collection over {@link org.apache.myfaces.config.element.NavigationRule NavigationRule}s
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
    private void postProcessFacesConfig(String systemId, FacesConfigImpl config)
    {
        for (org.apache.myfaces.config.element.Application application : config.getApplications())
        {
            for (org.apache.myfaces.config.element.LocaleConfig localeConfig : application.getLocaleConfig())
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/impl/FacesConfigUnmarshallerImpl.java`
#### Snippet
```java
        for (org.apache.myfaces.config.element.Application application : config.getApplications())
        {
            for (org.apache.myfaces.config.element.LocaleConfig localeConfig : application.getLocaleConfig())
            {
                if (!localeConfig.getSupportedLocales().contains(localeConfig.getDefaultLocale()))
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/config/webparameters/MyfacesConfig.java`
#### Snippet
```java
     */
    @JSFWebConfigParam(since = "2.3")
    public static final java.lang.String WEBSOCKET_ENDPOINT_PORT = "jakarta.faces.WEBSOCKET_ENDPOINT_PORT";

    @JSFWebConfigParam(defaultValue = "300000")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/config/webparameters/MyfacesConfig.java`
#### Snippet
```java
    
    /**
     * Defines the {@link java.util.ResourceBundle.Control} to use for all
     * {@link java.util.ResourceBundle#getBundle(java.lang.String)} calls.
     */
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/config/webparameters/MyfacesConfig.java`
#### Snippet
```java
    /**
     * Defines the {@link java.util.ResourceBundle.Control} to use for all
     * {@link java.util.ResourceBundle#getBundle(java.lang.String)} calls.
     */
    @JSFWebConfigParam(since="2.3-next")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/config/webparameters/MyfacesConfig.java`
#### Snippet
```java
    /**
     * Defines the {@link java.util.ResourceBundle.Control} to use for all
     * {@link java.util.ResourceBundle#getBundle(java.lang.String)} calls.
     */
    @JSFWebConfigParam(since="2.3-next")
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.component.behavior` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/annotation/AnnotationConfigurator.java`
#### Snippet
```java
            {
                // Can only apply @FacesBehavior to Behavior implementors.
                if (!jakarta.faces.component.behavior.Behavior.class.isAssignableFrom(clazz))
                {
                    // Just log this.  We'll catch it later in the runtime.
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.impl.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/annotation/AnnotationConfigurator.java`
#### Snippet
```java
                }

                org.apache.myfaces.config.impl.element.RenderKitImpl renderKit =
                        (org.apache.myfaces.config.impl.element.RenderKitImpl)
                                facesConfig.getRenderKit(renderKitId);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.impl.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/annotation/AnnotationConfigurator.java`
#### Snippet
```java

                org.apache.myfaces.config.impl.element.RenderKitImpl renderKit =
                        (org.apache.myfaces.config.impl.element.RenderKitImpl)
                                facesConfig.getRenderKit(renderKitId);
                if (renderKit == null)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.impl.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/annotation/AnnotationConfigurator.java`
#### Snippet
```java
                if (renderKit == null)
                {
                    renderKit = new org.apache.myfaces.config.impl.element.RenderKitImpl();
                    renderKit.setId(renderKitId);
                    facesConfig.addRenderKit(renderKit);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.impl.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/annotation/AnnotationConfigurator.java`
#### Snippet
```java
                }

                org.apache.myfaces.config.impl.element.ClientBehaviorRendererImpl cbr =
                        new org.apache.myfaces.config.impl.element.ClientBehaviorRendererImpl();
                cbr.setRendererType(facesBehaviorRenderer.rendererType());
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.impl.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/annotation/AnnotationConfigurator.java`
#### Snippet
```java

                org.apache.myfaces.config.impl.element.ClientBehaviorRendererImpl cbr =
                        new org.apache.myfaces.config.impl.element.ClientBehaviorRendererImpl();
                cbr.setRendererType(facesBehaviorRenderer.rendererType());
                cbr.setRendererClass(clazz.getName());
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.impl.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/annotation/AnnotationConfigurator.java`
#### Snippet
```java
                // we need to get access to this info later and can't from the dispenser (it's not a
                // singleton).
                org.apache.myfaces.config.impl.element.NamedEventImpl namedEventConfig =
                        new org.apache.myfaces.config.impl.element.NamedEventImpl();
                namedEventConfig.setEventClass(clazz.getName());
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.impl.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/annotation/AnnotationConfigurator.java`
#### Snippet
```java
                // singleton).
                org.apache.myfaces.config.impl.element.NamedEventImpl namedEventConfig =
                        new org.apache.myfaces.config.impl.element.NamedEventImpl();
                namedEventConfig.setEventClass(clazz.getName());
                namedEventConfig.setShortName(namedEvent.shortName());
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.impl.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/annotation/AnnotationConfigurator.java`
#### Snippet
```java
                    }

                    org.apache.myfaces.config.impl.element.RenderKitImpl renderKit =
                            (org.apache.myfaces.config.impl.element.RenderKitImpl)
                                    facesConfig.getRenderKit(renderKitId);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.impl.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/annotation/AnnotationConfigurator.java`
#### Snippet
```java

                    org.apache.myfaces.config.impl.element.RenderKitImpl renderKit =
                            (org.apache.myfaces.config.impl.element.RenderKitImpl)
                                    facesConfig.getRenderKit(renderKitId);
                    if (renderKit == null)
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.impl.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/annotation/AnnotationConfigurator.java`
#### Snippet
```java
                    if (renderKit == null)
                    {
                        renderKit = new org.apache.myfaces.config.impl.element.RenderKitImpl();
                        renderKit.setId(renderKitId);
                        facesConfig.addRenderKit(renderKit);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.impl.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/annotation/AnnotationConfigurator.java`
#### Snippet
```java
                    }

                    org.apache.myfaces.config.impl.element.RendererImpl renderer =
                            new org.apache.myfaces.config.impl.element.RendererImpl();
                    renderer.setComponentFamily(rend.componentFamily());
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.config.impl.element` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/config/annotation/AnnotationConfigurator.java`
#### Snippet
```java

                    org.apache.myfaces.config.impl.element.RendererImpl renderer =
                            new org.apache.myfaces.config.impl.element.RendererImpl();
                    renderer.setComponentFamily(rend.componentFamily());
                    renderer.setRendererClass(clazz.getName());
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.component` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/config/annotation/AnnotationConfigurator.java`
#### Snippet
```java
 *
 * <ul>
 * <li>{@link jakarta.faces.component.FacesComponent}</li>
 * <li>{@link jakarta.faces.convert.FacesConverter}</li>
 * <li>{@link jakarta.faces.validator.FacesValidator}</li>
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.convert` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/config/annotation/AnnotationConfigurator.java`
#### Snippet
```java
 * <ul>
 * <li>{@link jakarta.faces.component.FacesComponent}</li>
 * <li>{@link jakarta.faces.convert.FacesConverter}</li>
 * <li>{@link jakarta.faces.validator.FacesValidator}</li>
 * <li>{@link jakarta.faces.render.FacesRenderer}</li>
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.validator` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/config/annotation/AnnotationConfigurator.java`
#### Snippet
```java
 * <li>{@link jakarta.faces.component.FacesComponent}</li>
 * <li>{@link jakarta.faces.convert.FacesConverter}</li>
 * <li>{@link jakarta.faces.validator.FacesValidator}</li>
 * <li>{@link jakarta.faces.render.FacesRenderer}</li>
 * <li>{@link jakarta.faces.render.FacesBehaviorRenderer}</li>
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.render` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/config/annotation/AnnotationConfigurator.java`
#### Snippet
```java
 * <li>{@link jakarta.faces.convert.FacesConverter}</li>
 * <li>{@link jakarta.faces.validator.FacesValidator}</li>
 * <li>{@link jakarta.faces.render.FacesRenderer}</li>
 * <li>{@link jakarta.faces.render.FacesBehaviorRenderer}</li>
 * </ul>
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.render` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/config/annotation/AnnotationConfigurator.java`
#### Snippet
```java
 * <li>{@link jakarta.faces.validator.FacesValidator}</li>
 * <li>{@link jakarta.faces.render.FacesRenderer}</li>
 * <li>{@link jakarta.faces.render.FacesBehaviorRenderer}</li>
 * </ul>
 * <p>
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.context` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/context/servlet/FacesContextImplBase.java`
#### Snippet
```java
    /**
     * Returns a mutable map of attributes associated with this faces context when
     * {@link jakarta.faces.context.FacesContext#release()} is called the map must be cleared!
     * 
     * Note this map is not associated with the request map the request map still is accessible via the
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.context` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/context/servlet/FacesContextImplBase.java`
#### Snippet
```java
     * 
     * Note this map is not associated with the request map the request map still is accessible via the
     * {@link jakarta.faces.context.ExternalContext#getRequestMap()} method!
     * 
     * Also the scope is different to the request map, this map has the scope of the context, and is cleared once the
```

### UnnecessaryFullyQualifiedName
Qualifier `java.util` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/context/flash/FlashImpl.java`
#### Snippet
```java

    @Override
    public Set<java.util.Map.Entry<String, Object>> entrySet()
    {
        _checkFlashScopeDisabled();
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/context/servlet/ServletExternalContextImpl.java`
#### Snippet
```java
     * @since Faces 1.2
     * @param encoding
     * @throws java.io.UnsupportedEncodingException
     */
    @Override
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/context/servlet/ServletExternalContextImpl.java`
#### Snippet
```java
     */
    @Override
    public void setRequestCharacterEncoding(final java.lang.String encoding) throws java.io.UnsupportedEncodingException
    {

```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/context/servlet/ServletExternalContextImpl.java`
#### Snippet
```java
     */
    @Override
    public void setRequestCharacterEncoding(final java.lang.String encoding) throws java.io.UnsupportedEncodingException
    {

```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.servlet.http` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/context/servlet/ServletExternalContextImpl.java`
#### Snippet
```java
        Map<Object, Object> attributes = facesContext.getAttributes();

        jakarta.servlet.http.PushBuilder pushBuilder = null;
        
        if (!attributes.containsKey(PUSH_SUPPORTED))
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/context/servlet/ServletExternalContextImpl.java`
#### Snippet
```java
     */
    @Override
    public void setRequest(final java.lang.Object request)
    {
        this._servletRequest = (ServletRequest) request;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/context/servlet/ServletExternalContextImpl.java`
#### Snippet
```java
     */
    @Override
    public void setResponse(final java.lang.Object response)
    {
        this._servletResponse = (ServletResponse) response;
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/context/servlet/ServletExternalContextImpl.java`
#### Snippet
```java
     */
    @Override
    public void setResponseCharacterEncoding(final java.lang.String encoding)
    {
        this._servletResponse.setCharacterEncoding(encoding);
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.context` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/lifecycle/clientwindow/ClientConfig.java`
#### Snippet
```java
     *        windowhandler.html page should get rendered first. By returning <code>false</code>
     *        the requested page will get rendered intermediately.
     * @see #getUserAgent(jakarta.faces.context.FacesContext) for determining the UserAgent
     */
    public boolean isClientSideWindowHandlerRequest(FacesContext facesContext)
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.context` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/lifecycle/clientwindow/ClientConfig.java`
#### Snippet
```java
    protected String windowHandlerHtml;

    /** lazily initiated via {@link #getUserAgent(jakarta.faces.context.FacesContext)} */
    private volatile String userAgent = null;

```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.util.lang` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/component/visit/PartialVisitContext.java`
#### Snippet
```java
        // Finally, populate the clientIds collection.  This has the
        // side effect of populating all of the other collections.
        org.apache.myfaces.util.lang.ArrayUtils.addAll(_clientIds, clientIds);
        //_clientIds.addAll(clientIdSet);

```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.context` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/renderkit/RendererUtils.java`
#### Snippet
```java
      * creates {@link Resource} and returns it's path suitable for rendering.
      * If component doesn't have name/library gets value for attribute named <code>attributeName</code> 
      * returns it processed with {@link #toResourceUri(jakarta.faces.context.FacesContext, java.lang.Object)}
      *       
      * @param facesContext a {@link FacesContext}
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/renderkit/RendererUtils.java`
#### Snippet
```java
      * creates {@link Resource} and returns it's path suitable for rendering.
      * If component doesn't have name/library gets value for attribute named <code>attributeName</code> 
      * returns it processed with {@link #toResourceUri(jakarta.faces.context.FacesContext, java.lang.Object)}
      *       
      * @param facesContext a {@link FacesContext}
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.util.lang` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlGridRendererBase.java`
#### Snippet
```java

        String[] rowClassesArray = (rowClasses == null)
            ? org.apache.myfaces.util.lang.ArrayUtils.EMPTY_STRING_ARRAY
            : StringUtils.trim(StringUtils.splitShortString(rowClasses, ','));
        int rowClassesCount = rowClassesArray.length;
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.renderkit` is unnecessary, and can be replaced with an import
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/SelectItemsUtils.java`
#### Snippet
```java
            else
            {
                String itemStrValue = org.apache.myfaces.renderkit.RendererUtils
                        .getConvertedStringValue(context, component, converter,
                                selectItem);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.util.lang` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/HTML.java`
#### Snippet
```java
            UNIVERSAL_ATTRIBUTES);
    String[] ANCHOR_PASSTHROUGH_ATTRIBUTES_WITHOUT_STYLE_AND_EVENTS =
        (String[]) org.apache.myfaces.util.lang.ArrayUtils.concat(
            ANCHOR_ATTRIBUTES,
            UNIVERSAL_ATTRIBUTES_WITHOUT_STYLE);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.util.lang` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/HTML.java`
#### Snippet
```java
            COMMON_FIELD_EVENT_ATTRIBUTES);
    String[] COMMON_FIELD_PASSTROUGH_ATTRIBUTES_WITHOUT_DISABLED_AND_ONFOCUS =
        (String[]) org.apache.myfaces.util.lang.ArrayUtils.concat(
            COMMON_PASSTROUGH_ATTRIBUTES,
            COMMON_FIELD_ATTRIBUTES_WITHOUT_DISABLED,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.util.lang` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/HTML.java`
#### Snippet
```java
    // Common Attributes
    String[] COMMON_PASSTROUGH_ATTRIBUTES =
        (String[]) org.apache.myfaces.util.lang.ArrayUtils.concat(
            EVENT_HANDLER_ATTRIBUTES,
            UNIVERSAL_ATTRIBUTES);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.util.lang` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/HTML.java`
#### Snippet
```java
            UNIVERSAL_ATTRIBUTES);
    String[] COMMON_PASSTROUGH_ATTRIBUTES_WITHOUT_STYLE =
        (String[]) org.apache.myfaces.util.lang.ArrayUtils.concat(
            EVENT_HANDLER_ATTRIBUTES,
            UNIVERSAL_ATTRIBUTES_WITHOUT_STYLE);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.util.lang` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/HTML.java`
#### Snippet
```java
            COMMON_PASSTROUGH_ATTRIBUTES);
    String[] LABEL_PASSTHROUGH_ATTRIBUTES_WITHOUT_EVENTS =
        (String[]) org.apache.myfaces.util.lang.ArrayUtils.concat(
            LABEL_ATTRIBUTES_WITHOUT_EVENTS,
            UNIVERSAL_ATTRIBUTES);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.util.lang` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/HTML.java`
#### Snippet
```java

    String[] INPUT_PASSTHROUGH_ATTRIBUTES_WITHOUT_DISABLED_AND_ONFOCUS_AND_ONCLICK =
        (String[]) org.apache.myfaces.util.lang.ArrayUtils.concat(
                INPUT_ATTRIBUTES,
                COMMON_FIELD_PASSTROUGH_ATTRIBUTES_WITHOUT_DISABLED_AND_ONFOCUS_AND_ONCLICK);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.util.lang` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/HTML.java`
#### Snippet
```java
    };
    String[] BUTTON_PASSTHROUGH_ATTRIBUTES_WITHOUT_DISABLED =
        (String[]) org.apache.myfaces.util.lang.ArrayUtils.concat(
            BUTTON_ATTRIBUTES,
            COMMON_FIELD_PASSTROUGH_ATTRIBUTES_WITHOUT_DISABLED);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.util.lang` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/HTML.java`
#### Snippet
```java
            COMMON_FIELD_EVENT_ATTRIBUTES_WITHOUT_ONSELECT_AND_ONCHANGE);
    String[] ANCHOR_PASSTHROUGH_ATTRIBUTES_WITHOUT_ONCLICK_WITHOUT_STYLE =
        (String[]) org.apache.myfaces.util.lang.ArrayUtils.concat(
            ANCHOR_ATTRIBUTES,
            COMMON_PASSTROUGH_ATTRIBUTES_WITHOUT_ONCLICK_WITHOUT_STYLE,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.util.lang` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/HTML.java`
#### Snippet
```java
            COMMON_FIELD_EVENT_ATTRIBUTES);
    String[] COMMON_PASSTROUGH_ATTRIBUTES_WITHOUT_ONMOUSEOVER_AND_ONMOUSEOUT =
        (String[]) org.apache.myfaces.util.lang.ArrayUtils.concat(
            EVENT_HANDLER_ATTRIBUTES_WITHOUT_ONMOUSEOVER_AND_ONMOUSEOUT,
            UNIVERSAL_ATTRIBUTES);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.util.lang` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/HTML.java`
#### Snippet
```java

    String[] MESSAGE_PASSTHROUGH_ATTRIBUTES_WITHOUT_TITLE_STYLE_AND_STYLE_CLASS =
        (String[]) org.apache.myfaces.util.lang.ArrayUtils.concat(
            new String[] {DIR_ATTR, LANG_ATTR},
            EVENT_HANDLER_ATTRIBUTES);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.util.lang` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/HTML.java`
#### Snippet
```java
            COMMON_FIELD_EVENT_ATTRIBUTES_WITHOUT_ONFOCUS);
    String[] COMMON_FIELD_PASSTROUGH_ATTRIBUTES_WITHOUT_DISABLED_AND_ONCLICK =
        (String[]) org.apache.myfaces.util.lang.ArrayUtils.concat(
            COMMON_PASSTROUGH_ATTRIBUTES_WITHOUT_ONCLICK,
            COMMON_FIELD_ATTRIBUTES_WITHOUT_DISABLED,
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.util.lang` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/HTML.java`
#### Snippet
```java
    };
    String[] EVENT_HANDLER_ATTRIBUTES =
            (String[]) org.apache.myfaces.util.lang.ArrayUtils.concat(
                EVENT_HANDLER_ATTRIBUTES_WITHOUT_ONCLICK,
                new String[] {ONCLICK_ATTR});
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.util.lang` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/HTML.java`
#### Snippet
```java
            COMMON_FIELD_PASSTROUGH_ATTRIBUTES_WITHOUT_DISABLED_AND_ONCLICK);
    String[] BUTTON_PASSTHROUGH_ATTRIBUTES_WITHOUT_DISABLED_AND_EVENTS =
        (String[]) org.apache.myfaces.util.lang.ArrayUtils.concat(
            BUTTON_ATTRIBUTES,
            COMMON_FIELD_PASSTROUGH_ATTRIBUTES_WITHOUT_DISABLED_AND_EVENTS);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.util.lang` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/HTML.java`
#### Snippet
```java
            COMMON_FIELD_PASSTROUGH_ATTRIBUTES_WITHOUT_DISABLED);
    String[] BUTTON_PASSTHROUGH_ATTRIBUTES_WITHOUT_DISABLED_AND_ONCLICK =
        (String[]) org.apache.myfaces.util.lang.ArrayUtils.concat(
            BUTTON_ATTRIBUTES,
            COMMON_FIELD_PASSTROUGH_ATTRIBUTES_WITHOUT_DISABLED_AND_ONCLICK);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.util.lang` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/HTML.java`
#### Snippet
```java
    };
    String[] UNIVERSAL_ATTRIBUTES =
            (String[]) org.apache.myfaces.util.lang.ArrayUtils.concat(
                UNIVERSAL_ATTRIBUTES_WITHOUT_STYLE,
                new String[] {STYLE_ATTR, STYLE_CLASS_ATTR});
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.util.lang` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/HTML.java`
#### Snippet
```java
    };
    String[] LABEL_PASSTHROUGH_ATTRIBUTES =
        (String[]) org.apache.myfaces.util.lang.ArrayUtils.concat(
            LABEL_ATTRIBUTES,
            COMMON_PASSTROUGH_ATTRIBUTES);
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.util.lang` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/HTML.java`
#### Snippet
```java
            COMMON_FIELD_EVENT_ATTRIBUTES_WITHOUT_ONSELECT_AND_ONCHANGE);
    String[] ANCHOR_PASSTHROUGH_ATTRIBUTES_WITHOUT_STYLE =
        (String[]) org.apache.myfaces.util.lang.ArrayUtils.concat(
            ANCHOR_ATTRIBUTES,
            COMMON_PASSTROUGH_ATTRIBUTES_WITHOUT_STYLE,
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.context` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/application/ViewIdSupport.java`
#### Snippet
```java
     * <ul>
     * <li>lookup the viewid from the request attribute "jakarta.servlet.include.path_info"
     * <li>if null lookup the value for viewid by {@link jakarta.faces.context.ExternalContext#getRequestPathInfo()}
     * <li>if null lookup the value for viewid from the request attribute "jakarta.servlet.include.servlet_path"
     * <li>if null lookup the value for viewid by {@link jakarta.faces.context.ExternalContext#getRequestServletPath()}
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.context` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/application/ViewIdSupport.java`
#### Snippet
```java
     * <li>if null lookup the value for viewid by {@link jakarta.faces.context.ExternalContext#getRequestPathInfo()}
     * <li>if null lookup the value for viewid from the request attribute "jakarta.servlet.include.servlet_path"
     * <li>if null lookup the value for viewid by {@link jakarta.faces.context.ExternalContext#getRequestServletPath()}
     * <li>if null throw a {@link jakarta.faces.FacesException}
     * </ul>
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/application/ViewIdSupport.java`
#### Snippet
```java
     * <li>if null lookup the value for viewid from the request attribute "jakarta.servlet.include.servlet_path"
     * <li>if null lookup the value for viewid by {@link jakarta.faces.context.ExternalContext#getRequestServletPath()}
     * <li>if null throw a {@link jakarta.faces.FacesException}
     * </ul>
     */
```

### UnnecessaryFullyQualifiedName
Qualifier `org.apache.myfaces.spi` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/application/viewstate/StateUtils.java`
#### Snippet
```java
     * Defines the factory class name using for serialize/deserialize the view state returned 
     * by state manager into a byte array. The expected class must implement
     * {@link org.apache.myfaces.spi.SerialFactory} interface.
     */
    @JSFWebConfigParam(name="org.apache.myfaces.SERIAL_FACTORY", since="1.1",group="state",tags="performance")
```

### UnnecessaryFullyQualifiedName
Qualifier `java.security` is unnecessary, and can be replaced with an import
in `test/src/main/java/org/apache/myfaces/test/mock/MockExternalContext10.java`
#### Snippet
```java

    /** {@inheritDoc} */
    public java.security.Principal getUserPrincipal()
    {
        return request.getUserPrincipal();
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.el` is unnecessary and can be removed
in `test/src/main/java/org/apache/myfaces/test/mock/_ELText.java`
#### Snippet
```java
     *            String to parse
     * @return ELText that can be re-applied later
     * @throws jakarta.el.ELException
     */
    public static _ELText parse(ExpressionFactory fact, ELContext ctx, String in) throws ELException
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.el` is unnecessary and can be removed
in `test/src/main/java/org/apache/myfaces/test/mock/_ELText.java`
#### Snippet
```java
     *            String to parse
     * @return ELText instance that knows if the String was literal or not
     * @throws jakarta.el.ELException
     */
    public static _ELText parse(String in) throws ELException
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/application/ViewHandler.java`
#### Snippet
```java
     */
    @JSFWebConfigParam(since = "2.0", alias = "facelets.SKIP_COMMENTS")
    public static final java.lang.String FACELETS_SKIP_COMMENTS_PARAM_NAME = "jakarta.faces.FACELETS_SKIP_COMMENTS";
    
    /**
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/application/ViewHandler.java`
#### Snippet
```java
            desc = "Set of .taglib.xml files, separated by ';' that should be loaded by facelet engine.",
            alias = "facelets.LIBRARIES")
    public static final java.lang.String FACELETS_LIBRARIES_PARAM_NAME = "jakarta.faces.FACELETS_LIBRARIES";
    
    /**
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/application/ViewHandler.java`
#### Snippet
```java
     * @return 
     */
    public Stream<java.lang.String> getViews(FacesContext facesContext, String path, ViewVisitOption... options)
    {
        return getViews(facesContext, path, Integer.MAX_VALUE, options);
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/application/ViewHandler.java`
#### Snippet
```java
    @JSFWebConfigParam(since = "2.0", defaultValue = "-1", alias = "facelets.REFRESH_PERIOD",
            classType = "java.lang.Long", tags = "performance")
    public static final java.lang.String FACELETS_REFRESH_PERIOD_PARAM_NAME = "jakarta.faces.FACELETS_REFRESH_PERIOD";

    /**
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/application/ViewHandler.java`
#### Snippet
```java
     */
    @JSFWebConfigParam(since = "2.0", alias = "facelets.DECORATORS")
    public static final java.lang.String FACELETS_DECORATORS_PARAM_NAME = "jakarta.faces.FACELETS_DECORATORS";
    
    /**
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/application/ViewHandler.java`
#### Snippet
```java
     * @return 
     */
    public Stream<java.lang.String> getViews(FacesContext facesContext, String path, 
            int maxDepth, ViewVisitOption... options)
    {
```

### UnnecessaryFullyQualifiedName
Qualifier `java.lang` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/application/ViewHandler.java`
#### Snippet
```java
            desc = "Define the default buffer size value passed to ExternalContext.setResponseBufferResponse() and in "
                   + "a servlet environment to HttpServletResponse.setBufferSize()")
    public static final java.lang.String FACELETS_BUFFER_SIZE_PARAM_NAME = "jakarta.faces.FACELETS_BUFFER_SIZE";
    
    /**
```

### UnnecessaryFullyQualifiedName
Qualifier `jakarta.faces.application` is unnecessary and can be removed
in `api/src/main/java/jakarta/faces/application/ViewHandler.java`
#### Snippet
```java
     * example, HTTP requests can specify the "accept-language" header.
     * <p>
     * Method {@link jakarta.faces.application.Application#getSupportedLocales()} defines what locales this Faces
     * application is capable of supporting.
     * <p>
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/HtmlRendererUtils.java`
#### Snippet
```java
    /**
     * @return true, if an attribute was written
     * @throws java.io.IOException
     */
    public static boolean renderHTMLAttributes(ResponseWriter writer,
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/HtmlRendererUtils.java`
#### Snippet
```java
    /**
     * @return true, if the attribute was written
     * @throws java.io.IOException
     */
    public static boolean renderHTMLAttribute(ResponseWriter writer,
```

### UnnecessaryFullyQualifiedName
Qualifier `java.io` is unnecessary and can be removed
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/HtmlRendererUtils.java`
#### Snippet
```java
    /**
     * @return true, if the attribute was written
     * @throws java.io.IOException
     */
    public static boolean renderHTMLAttribute(ResponseWriter writer,
```

## RuleId[ruleID=ThrowablePrintStackTrace]
### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/DefaultFunctionMapper.java`
#### Snippet
```java
                catch (Exception e)
                {
                    e.printStackTrace();
                }
            }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `impl/src/main/java/org/apache/myfaces/config/DefaultFacesConfigurationMerger.java`
#### Snippet
```java
        catch (CyclicDependencyException e)
        {
            e.printStackTrace();
        }

```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `test/src/main/java/org/apache/myfaces/test/mock/MockPortletContext.java`
#### Snippet
```java

        System.out.println(message);
        exception.printStackTrace();

    }
```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `test/src/main/java/org/apache/myfaces/test/mock/MockServletContext.java`
#### Snippet
```java
    {
        System.out.println(message);
        exception.printStackTrace();
    }

```

### ThrowablePrintStackTrace
Call to `printStackTrace()` should probably be replaced with more robust logging
in `test/src/main/java/org/apache/myfaces/test/mock/MockServletContext.java`
#### Snippet
```java
    {
        System.out.println(message);
        exception.printStackTrace();
    }

```

## RuleId[ruleID=RegExpSingleCharAlternation]
### RegExpSingleCharAlternation
Single character alternation in RegExp
in `impl/src/main/resources/org/apache/myfaces/resource/web-facesconfig_1_2.xsd`
#### Snippet
```java
    <xsd:simpleType name="faces-config-localeType">
        <xsd:restriction base="xsd:string">
            <xsd:pattern value="[a-z]{2}(_|-)?([\p{L}\-\p{Nd}]{2})?"/>
        </xsd:restriction>
    </xsd:simpleType>
```

### RegExpSingleCharAlternation
Single character alternation in RegExp
in `impl/src/main/resources/org/apache/myfaces/resource/web-facesconfig_3_0.xsd`
#### Snippet
```java
    <xsd:simpleType name="faces-config-localeType">
        <xsd:restriction base="xsd:string">
            <xsd:pattern value="[a-z]{2}(_|-)?([\p{L}\-\p{Nd}]{2})?"/>
        </xsd:restriction>
    </xsd:simpleType>
```

### RegExpSingleCharAlternation
Single character alternation in RegExp
in `impl/src/main/resources/org/apache/myfaces/resource/web-facesconfig_2_0.xsd`
#### Snippet
```java
    <xsd:simpleType name="faces-config-localeType">
        <xsd:restriction base="xsd:string">
            <xsd:pattern value="[a-z]{2}(_|-)?([\p{L}\-\p{Nd}]{2})?"/>
        </xsd:restriction>
    </xsd:simpleType>
```

### RegExpSingleCharAlternation
Single character alternation in RegExp
in `impl/src/main/resources/org/apache/myfaces/resource/web-facesconfig_2_3.xsd`
#### Snippet
```java
    <xsd:simpleType name="faces-config-localeType">
        <xsd:restriction base="xsd:string">
            <xsd:pattern value="[a-z]{2}(_|-)?([\p{L}\-\p{Nd}]{2})?"/>
        </xsd:restriction>
    </xsd:simpleType>
```

### RegExpSingleCharAlternation
Single character alternation in RegExp
in `impl/src/main/resources/org/apache/myfaces/resource/web-facesconfig_4_0.xsd`
#### Snippet
```java
    <xsd:simpleType name="faces-config-localeType">
        <xsd:restriction base="xsd:string">
            <xsd:pattern value="[a-z]{2}(_|-)?([\p{L}\-\p{Nd}]{2})?"/>
        </xsd:restriction>
    </xsd:simpleType>
```

### RegExpSingleCharAlternation
Single character alternation in RegExp
in `impl/src/main/resources/org/apache/myfaces/resource/web-facesconfig_2_2.xsd`
#### Snippet
```java
    <xsd:simpleType name="faces-config-localeType">
        <xsd:restriction base="xsd:string">
            <xsd:pattern value="[a-z]{2}(_|-)?([\p{L}\-\p{Nd}]{2})?"/>
        </xsd:restriction>
    </xsd:simpleType>
```

### RegExpSingleCharAlternation
Single character alternation in RegExp
in `impl/src/main/resources/org/apache/myfaces/resource/web-facesconfig_2_1.xsd`
#### Snippet
```java
    <xsd:simpleType name="faces-config-localeType">
        <xsd:restriction base="xsd:string">
            <xsd:pattern value="[a-z]{2}(_|-)?([\p{L}\-\p{Nd}]{2})?"/>
        </xsd:restriction>
    </xsd:simpleType>
```

## RuleId[ruleID=ReplaceNullCheck]
### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `api/src/main/java/jakarta/faces/FactoryFinder.java`
#### Snippet
```java
                    throw (FacesException) targetException;
                }
                else if (targetException == null)
                {
                    throw new FacesException(e);
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `api/src/main/java/jakarta/faces/FactoryFinder.java`
#### Snippet
```java
                    throw (IllegalStateException) targetException;
                }
                else if (targetException == null)
                {
                    throw new FacesException(e);
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `api/src/main/java/jakarta/faces/FactoryFinder.java`
#### Snippet
```java
                    throw (IllegalArgumentException) targetException;
                }
                else if (targetException == null)
                {
                    throw new FacesException(e);
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/cdi/model/FacesDataModelManager.java`
#### Snippet
```java
    public Map<Class<?>, Class<? extends DataModel>> getFacesDataModels()
    {
        if (facesDataModels == null)
        {
            return Collections.emptyMap();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/flow/cdi/FlowScopeContextualStorageHolder.java`
#### Snippet
```java
        String baseKey = cw.getId();
        List<String> activeFlowKeys = activeFlowMapKeys.get(baseKey);
        if (activeFlowKeys == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/TagAttributeImpl.java`
#### Snippet
```java
        {
            Boolean result = ((Boolean) this.getObject(ctx, Boolean.class));
            if(result == null)
            {
                return false;
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/AbsoluteOrderingImpl.java`
#### Snippet
```java
    public List<org.apache.myfaces.config.element.OrderSlot> getOrderList()
    {
        if (orderList == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/FacesFlowDefinitionImpl.java`
#### Snippet
```java
    public List<FacesFlowCall> getFlowCallList()
    {
        if (flowCallList == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/FacesFlowDefinitionImpl.java`
#### Snippet
```java
    public List<NavigationRule> getNavigationRuleList()
    {
        if (navigationRuleList == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/FacesFlowDefinitionImpl.java`
#### Snippet
```java
    public List<FacesFlowView> getViewList()
    {
        if (viewList == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/FacesFlowDefinitionImpl.java`
#### Snippet
```java
    public List<FacesFlowParameter> getInboundParameterList()
    {
        if (inboundParameterList == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/FacesFlowDefinitionImpl.java`
#### Snippet
```java
    public List<FacesFlowMethodCall> getMethodCallList()
    {
        if (methodCallList == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/FacesFlowDefinitionImpl.java`
#### Snippet
```java
    public List<FacesFlowReturn> getReturnList()
    {
        if (returnList == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/FacesFlowDefinitionImpl.java`
#### Snippet
```java
    public List<FacesFlowSwitch> getSwitchList()
    {
        if (switchList == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/PropertyImpl.java`
#### Snippet
```java
    public Collection<? extends String> getDisplayNames()
    {
        if (displayName == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/PropertyImpl.java`
#### Snippet
```java
    public Collection<? extends String> getDescriptions()
    {
        if (description == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/PropertyImpl.java`
#### Snippet
```java
    public Collection<? extends String> getPropertyExtensions()
    {
        if (propertyExtensions == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/AttributeImpl.java`
#### Snippet
```java
    public Collection<? extends String> getDescriptions()
    {
        if (description == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/AttributeImpl.java`
#### Snippet
```java
    public Collection<? extends String> getAttributeExtensions()
    {
        if (attributeExtensions == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/AttributeImpl.java`
#### Snippet
```java
    public Collection<? extends String> getDisplayNames()
    {
        if (displayName == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/FactoryImpl.java`
#### Snippet
```java
    public List<String> getApplicationFactory()
    {
        if (applicationFactories == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/FactoryImpl.java`
#### Snippet
```java
    public List<String> getSearchExpressionContextFactory()
    {
        if (searchExpressionContextFactories == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/FactoryImpl.java`
#### Snippet
```java
    public List<String> getExceptionHandlerFactory()
    {
        if (exceptionHandlerFactories == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/FactoryImpl.java`
#### Snippet
```java
    public List<String> getRenderkitFactory()
    {
        if (renderKitFactories == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/FactoryImpl.java`
#### Snippet
```java
    public List<String> getClientWindowFactory()
    {
        if (clientWindowFactories == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/FactoryImpl.java`
#### Snippet
```java
    public List<String> getViewDeclarationLanguageFactory()
    {
        if (viewDeclarationLanguageFactories == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/FactoryImpl.java`
#### Snippet
```java
    public List<String> getLifecycleFactory()
    {
        if (lifecycleFactories == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/FactoryImpl.java`
#### Snippet
```java
    public List<String> getFaceletCacheFactory()
    {
        if (faceletCacheFactories == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/FactoryImpl.java`
#### Snippet
```java
    public List<String> getPartialViewContextFactory()
    {
        if (partialViewContextFactories == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/FactoryImpl.java`
#### Snippet
```java
    public List<String> getFlowHandlerFactory()
    {
        if (flowHandlerFactories == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/FactoryImpl.java`
#### Snippet
```java
    public List<String> getFlashFactory()
    {
        if (flashFactories == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/FactoryImpl.java`
#### Snippet
```java
    public List<String> getFacesContextFactory()
    {
        if (facesContextFactories == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/FactoryImpl.java`
#### Snippet
```java
    public List<String> getExternalContextFactory()
    {
        if (externalContextFactories == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/FactoryImpl.java`
#### Snippet
```java
    public List<String> getVisitContextFactory()
    {
        if (visitContextFactories == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/FactoryImpl.java`
#### Snippet
```java
    public List<String> getTagHandlerDelegateFactory()
    {
        if (tagHandlerDelegateFactories == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/BehaviorImpl.java`
#### Snippet
```java
    public Collection<AttributeImpl> getAttributes()
    {
        if (attributes == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/BehaviorImpl.java`
#### Snippet
```java
    public Collection<PropertyImpl> getProperties()
    {
        if (properties == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/FacesConfigExtensionImpl.java`
#### Snippet
```java
    public List<org.apache.myfaces.config.element.FaceletsProcessing> getFaceletsProcessingList()
    {
        if (_faceletsProcessingList == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/OrderingImpl.java`
#### Snippet
```java
    public List<org.apache.myfaces.config.element.OrderSlot> getAfterList()
    {
        if (afterList == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/OrderingImpl.java`
#### Snippet
```java
    public List<org.apache.myfaces.config.element.OrderSlot> getBeforeList()
    {
        if (beforeList == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ApplicationImpl.java`
#### Snippet
```java
    public List<org.apache.myfaces.config.element.LocaleConfig> getLocaleConfig()
    {
        if (localeConfig == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ApplicationImpl.java`
#### Snippet
```java
    public List<String> getResourceHandler()
    {
        if (resourceHandler == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ApplicationImpl.java`
#### Snippet
```java
    public List<String> getPropertyResolver()
    {
        if (propertyResolver == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ApplicationImpl.java`
#### Snippet
```java
    public List<String> getDefaultValidatorIds()
    {
        if (defaultValidatorIds == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ApplicationImpl.java`
#### Snippet
```java
    public List<String> getViewHandler()
    {
        if (viewHandler == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ApplicationImpl.java`
#### Snippet
```java
    public List<String> getElResolver()
    {
        if (elResolver == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ApplicationImpl.java`
#### Snippet
```java
    public List<String> getDefaultRenderkitId()
    {
        if (defaultRenderkitId == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ApplicationImpl.java`
#### Snippet
```java
    public List<org.apache.myfaces.config.element.ResourceBundle> getResourceBundle()
    {
        if (resourceBundle == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ApplicationImpl.java`
#### Snippet
```java
    public List<String> getSearchExpressionHandler()
    {
        if (searchExpressionHandler == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ApplicationImpl.java`
#### Snippet
```java
    public List<ContractMapping> getResourceLibraryContractMappings()
    {
        if (resourceLibraryContractMappings == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ApplicationImpl.java`
#### Snippet
```java
    public List<String> getSearchKeywordResolver()
    {
        if (searchKeywordResolver == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ApplicationImpl.java`
#### Snippet
```java
    public List<org.apache.myfaces.config.element.SystemEventListener> getSystemEventListeners()
    {
        if (systemEventListeners == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ApplicationImpl.java`
#### Snippet
```java
    public List<String> getActionListener()
    {
        if (actionListener == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ApplicationImpl.java`
#### Snippet
```java
    public List<String> getStateManager()
    {
        if (stateManager == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ApplicationImpl.java`
#### Snippet
```java
    public List<String> getMessageBundle()
    {
        if (messageBundle == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ApplicationImpl.java`
#### Snippet
```java
    public List<String> getVariableResolver()
    {
        if (variableResolver == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ApplicationImpl.java`
#### Snippet
```java
    public List<String> getNavigationHandler()
    {
        if (navigationHandler == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ConverterImpl.java`
#### Snippet
```java
    public List<? extends org.apache.myfaces.config.element.Attribute> getAttributes()
    {
        if (attributes == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ConverterImpl.java`
#### Snippet
```java
    public List<? extends org.apache.myfaces.config.element.Property> getProperties()
    {
        if (properties == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ViewPoolMappingImpl.java`
#### Snippet
```java
    public List<ViewPoolParameter> getParameterList()
    {
        if (parameterList == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/FacesFlowCallImpl.java`
#### Snippet
```java
    public List<FacesFlowParameter> getOutboundParameterList()
    {
        if (outboundParameterList == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/LocaleConfigImpl.java`
#### Snippet
```java
    public List<String> getSupportedLocales()
    {
        if (supportedLocales == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/FacesFlowSwitchImpl.java`
#### Snippet
```java
    public List<NavigationCase> getNavigationCaseList()
    {
        if (navigationCaseList == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/RenderKitImpl.java`
#### Snippet
```java
    public List<org.apache.myfaces.config.element.Renderer> getRenderer()
    {
        if (renderer == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/RenderKitImpl.java`
#### Snippet
```java
    public List<org.apache.myfaces.config.element.ClientBehaviorRenderer> getClientBehaviorRenderers ()
    {
        if (clientBehaviorRenderers == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/RenderKitImpl.java`
#### Snippet
```java
    public List<String> getRenderKitClasses()
    {
        if (renderKitClasses == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ComponentImpl.java`
#### Snippet
```java
    public List<? extends org.apache.myfaces.config.element.Attribute> getAttributes()
    {
        if (attributes == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/ComponentImpl.java`
#### Snippet
```java
    public List<? extends org.apache.myfaces.config.element.Property> getProperties()
    {
        if (properties == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/NavigationRuleImpl.java`
#### Snippet
```java
    public List<org.apache.myfaces.config.element.NavigationCase> getNavigationCases()
    {
        if (navigationCases == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/RedirectImpl.java`
#### Snippet
```java
    public Map<String, List<String>> getViewParams()
    {
        if (viewParams == null)
        {
            return Collections.emptyMap();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/FacesFlowMethodCallImpl.java`
#### Snippet
```java
    public List<FacesFlowMethodParameter> getParameterList()
    {
        if (parameterList == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/config/impl/element/facelets/FaceletTagLibraryImpl.java`
#### Snippet
```java
    public List<FaceletFunction> getFunctions()
    {
        if (functions == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/context/servlet/FacesContextImplBase.java`
#### Snippet
```java
        assertNotReleased();
        
        if (_resourceLibraryContracts == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/lifecycle/clientwindow/CODIClientSideWindow.java`
#### Snippet
```java
    {
        String requestToken = externalContext.getRequestParameterMap().get(CODI_REQUEST_TOKEN);
        if (requestToken != null)
        {
            return requestToken;
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/SelectItemsUtils.java`
#### Snippet
```java
                
                writer.startElement(HTML.OPTION_ELEM, selectItemInfo.getComponent());
                if (itemStrValue != null)
                {
                    writer.writeAttribute(HTML.VALUE_ATTR, itemStrValue, null);
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlRadioRendererBase.java`
#### Snippet
```java
        if (group != null && group.length() > 0)
        {
            if (value != null)
            {
                writer.writeAttribute(HTML.VALUE_ATTR, 
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlRadioRendererBase.java`
#### Snippet
```java
        else
        {
            if (value != null)
            {
                writer.writeAttribute(HTML.VALUE_ATTR, value, null);
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElseGet()'
in `impl/src/main/java/org/apache/myfaces/application/FacesServletMappingUtils.java`
#### Snippet
```java
                    return facesPrefixMapping;
                }
                else if (facesExtensionMapping != null)
                {
                    return facesExtensionMapping;
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `test/src/main/java/org/apache/myfaces/test/mock/MockClientWindow.java`
#### Snippet
```java
        String windowId = calculateWindowId(context);

        if (windowId != null)
        {
            // Store the current windowId.
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `test/src/main/java/org/apache/myfaces/test/mock/MockFacesContext.java`
#### Snippet
```java
    public List<String> getResourceLibraryContracts()
    {
        if (_resourceLibraryContracts == null)
        {
            return Collections.emptyList();
```

### ReplaceNullCheck
'if' statement can be replaced with call to 'Objects.requireNonNullElse()'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/util/HtmlRendererUtils.java`
#### Snippet
```java
                writer.write(TABULATOR);
                writer.startElement(HTML.OPTION_ELEM, null); // component);
                if (itemStrValue != null)
                {
                    writer.writeAttribute(HTML.VALUE_ATTR, itemStrValue, null);
```

## RuleId[ruleID=Java8MapApi]
### Java8MapApi
Can be replaced with single 'Map.putIfAbsent' method call
in `api/src/main/java/jakarta/faces/FactoryFinder.java`
#### Snippet
```java
        {
            // check if someone else already installed the factory
            if (factoryMap.get(factoryName) == null)
            {
                factoryMap.put(factoryName, factory);
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `api/src/main/java/jakarta/faces/component/UIViewRoot.java`
#### Snippet
```java
        
        listeners = _systemEventListeners.get(systemEvent);
        if (listeners == null)
        {
            listeners = new ArrayList<SystemEventListener>();
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/ui/DebugPhaseListener.java`
#### Snippet
```java
        Map<String, List<Object[]>> debugInfo = getDebugInfoMap(clientId);
        List<Object[]> fieldDebugInfo = debugInfo.get(field);
        if (fieldDebugInfo == null)
        {
            // no field debug-infos yet, create them and store it in the Map
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/faces/FaceletState.java`
#### Snippet
```java

        Map<String, ValueExpression> bindings = bindingsMap.get(uniqueId);
        if (bindings == null)
        {
            bindings = new HashMap<>();
```

### Java8MapApi
Can be replaced with single 'Map.getOrDefault' method call
in `impl/src/main/java/org/apache/myfaces/view/facelets/pool/impl/ViewPoolImpl.java`
#### Snippet
```java
        String entryMode = WebConfigParamUtils.getStringInitParameter(facesContext.getExternalContext(),
                INIT_PARAM_VIEW_POOL_ENTRY_MODE,
                parameters.containsKey(INIT_PARAM_VIEW_POOL_ENTRY_MODE) ?
                parameters.get(INIT_PARAM_VIEW_POOL_ENTRY_MODE) :
                INIT_PARAM_VIEW_POOL_ENTRY_MODE_DEFAULT);
```

### Java8MapApi
Can be replaced with single 'Map.getOrDefault' method call
in `impl/src/main/java/org/apache/myfaces/view/facelets/pool/impl/ViewPoolImpl.java`
#### Snippet
```java
        String deferredNavigationVal = WebConfigParamUtils.getStringInitParameter(facesContext.getExternalContext(),
                INIT_PARAM_VIEW_POOL_DEFERRED_NAVIGATION,
                parameters.containsKey(INIT_PARAM_VIEW_POOL_DEFERRED_NAVIGATION) ?
                parameters.get(INIT_PARAM_VIEW_POOL_DEFERRED_NAVIGATION) :
                "false");
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `impl/src/main/java/org/apache/myfaces/context/servlet/ServletExternalContextImpl.java`
#### Snippet
```java

                List<String> values = paramMap.get(currentName);
                if (values == null)
                {
                    values = new ArrayList<>(1);
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `impl/src/main/java/org/apache/myfaces/component/visit/PartialVisitContext.java`
#### Snippet
```java
                // stash it away in our map
                Collection<String> c = _subtreeClientIds.get(namingContainerClientId);
                if (c == null)
                {
                    c = new ArrayList<>(5);
```

### Java8MapApi
Can be replaced with single 'Map.putIfAbsent' method call
in `impl/src/main/java/org/apache/myfaces/renderkit/html/base/HtmlFormRendererBase.java`
#### Snippet
```java
        }

        if (facesContext.getExternalContext().getRequestMap().get(
                getScrollHiddenInputName(facesContext, form)) == null)
        {
            facesContext.getExternalContext().getRequestMap().put(getScrollHiddenInputName(
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `impl/src/main/java/org/apache/myfaces/application/NavigationHandlerImpl.java`
#### Snippet
```java
                        }
                        List<String> paramValues = params.get(splitParam[0]);
                        if (paramValues == null)
                        {
                            paramValues = new ArrayList<>(5);
```

### Java8MapApi
Can be replaced with single 'Map.computeIfAbsent' method call
in `test/src/main/java/org/apache/myfaces/test/mock/visit/PartialVisitContext.java`
#### Snippet
```java
                        .get(namingContainerClientId);

                if (c == null)
                {
                    // TODO: smarter initial size?
```

## RuleId[ruleID=Convert2Lambda]
### Convert2Lambda
Anonymous new Comparator() can be replaced with lambda
in `impl/src/main/java/org/apache/myfaces/config/DefaultFacesConfigurationMerger.java`
#### Snippet
```java
        }
        Collections.sort(appFilteredConfigResources,
                new Comparator<FacesConfig>()
                {
                    @Override
                    public int compare(FacesConfig o1, FacesConfig o2)
```

### Convert2Lambda
Anonymous new FileFilter() can be replaced with lambda
in `impl/src/main/java/org/apache/myfaces/config/annotation/PackageInfo.java`
#### Snippet
```java
            final File base, final ClassLoader cld, final String pckgname)
    {
        base.listFiles(new FileFilter()
        {
            @Override
            public boolean accept(File file)
```

## RuleId[ruleID=UnnecessaryContinue]
### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `api/src/main/java/jakarta/faces/model/IterableDataModel.java`
#### Snippet
```java
                {
                    // Trying to move to a non existent row
                    continue;
                }
            }
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `impl/src/main/java/org/apache/myfaces/util/ParametrizableFacesMessage.java`
#### Snippet
```java
            if (_args[i] == null)
            {
                continue;
            }
            else if (_args[i] instanceof ValueExpression)
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `impl/src/main/java/org/apache/myfaces/view/facelets/util/Classpath.java`
#### Snippet
```java
                // but not META-INF/, some classloaders may incorrectly report that META-INF/
                // exists and we'll end up here.  Just ignore this case.
                continue;
            }
        }
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TextUnit.java`
#### Snippet
```java
                    if (Character.isWhitespace(c))
                    {
                        continue;
                    }
                    else
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TextUnit.java`
#### Snippet
```java
            if (Character.isWhitespace(c))
            {
                continue;
            }
            else
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `impl/src/main/java/org/apache/myfaces/view/facelets/compiler/TextUnit.java`
#### Snippet
```java
            if (Character.isWhitespace(text.charAt(i)))
            {
                continue;
            }
            else
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `impl/src/main/java/org/apache/myfaces/resource/ResourceValidationUtils.java`
#### Snippet
```java
            if ( (c >='A' && c <='Z') || c == '_' || (c >='a' && c <='z') || (c >='0' && c <='9') )
            {
                continue;
            }
            else
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `impl/src/main/java/org/apache/myfaces/resource/ResourceValidationUtils.java`
#### Snippet
```java
                 (c == '@') || (c == '[') || (c == ']' ) || (c == '{') || (c == '}'))
            {
                continue;
            }
            else if (c == '.')
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `impl/src/main/java/org/apache/myfaces/resource/ResourceValidationUtils.java`
#### Snippet
```java
                    }
                }
                continue;
            }
            else
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `impl/src/main/java/org/apache/myfaces/resource/ResourceValidationUtils.java`
#### Snippet
```java
                 )
            {
                continue;
            }
            else if (c == '.')
```

### UnnecessaryContinue
`continue` is unnecessary as the last statement in a loop
in `impl/src/main/java/org/apache/myfaces/resource/ResourceValidationUtils.java`
#### Snippet
```java
                    }
                }
                continue;
            }
            else
```

## RuleId[ruleID=ThreadStartInConstruction]
### ThreadStartInConstruction
Call to `start()` during object construction
in `impl/src/main/java/org/apache/myfaces/util/lang/ConcurrentLRUCache.java`
#### Snippet
```java
        {
            cleanupThread = new CleanupThread(this);
            cleanupThread.start();
        }
    }
```

## RuleId[ruleID=EqualsWhichDoesntCheckParameterClass]
### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `api/src/main/java/jakarta/faces/component/_ComponentFacetMap.java`
#### Snippet
```java

        @Override
        public boolean equals(Object obj)
        {
            return _map.keySet().equals(obj);
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `api/src/main/java/jakarta/faces/component/_ComponentFacetMap.java`
#### Snippet
```java

        @Override
        public boolean equals(Object obj)
        {
            return _map.values().equals(obj);
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `api/src/main/java/jakarta/faces/component/_ComponentFacetMap.java`
#### Snippet
```java

        @Override
        public boolean equals(Object obj)
        {
            return _entry.equals(obj);
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `api/src/main/java/jakarta/faces/component/_ComponentFacetMap.java`
#### Snippet
```java

        @Override
        public boolean equals(Object obj)
        {
            return _map.entrySet().equals(obj);
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `api/src/main/java/jakarta/faces/component/_PassThroughAttributesMap.java`
#### Snippet
```java
    
    @Override
    public boolean equals(Object obj)
    {
        return getUnderlyingMap().equals(obj);
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `api/src/main/java/jakarta/faces/component/_ViewAttributeMap.java`
#### Snippet
```java

    @Override
    public boolean equals(Object o)
    {
        return _delegate.equals(o);
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `api/src/main/java/jakarta/faces/component/_ComponentAttributesMap.java`
#### Snippet
```java
     */
    @Override
    public boolean equals(Object obj)
    {
        return getUnderlyingMap().equals(obj);
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `api/src/main/java/jakarta/faces/application/NavigationCaseWrapper.java`
#### Snippet
```java

    @Override
    public boolean equals(Object o)
    {
        return getWrapped().equals(o);
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ResourceLocationValueExpression.java`
#### Snippet
```java

    @Override
    public boolean equals(Object obj)
    {
        return delegate.equals(obj);
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `impl/src/main/java/org/apache/myfaces/util/lang/ConcurrentLRUCache.java`
#### Snippet
```java

        @Override
        public boolean equals(Object obj)
        {
            return value.equals(obj);
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/TagMethodExpression.java`
#### Snippet
```java

    @Override
    public boolean equals(Object obj)
    {
        return this.orig.equals(obj);
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ContextAwareTagMethodExpression.java`
#### Snippet
```java

    @Override
    public boolean equals(Object obj)
    {
        return _wrapped.equals(obj);
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/LocationMethodExpression.java`
#### Snippet
```java

    @Override
    public boolean equals(Object obj)
    {
        return delegate.equals(obj);
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/TagValueExpression.java`
#### Snippet
```java
    
    @Override
    public boolean equals(Object obj)
    {
        return this.orig.equals(obj);
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/LocationValueExpression.java`
#### Snippet
```java

    @Override
    public boolean equals(Object obj)
    {
        return delegate.equals(obj);
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/CacheableValueExpressionWrapper.java`
#### Snippet
```java

    @Override
    public boolean equals(Object obj)
    {
        return delegate.equals(obj);
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `impl/src/main/java/org/apache/myfaces/view/facelets/el/ContextAwareTagValueExpression.java`
#### Snippet
```java
    
    @Override
    public boolean equals(Object obj)
    {
        return wrapped.equals(obj);
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/MappedValueExpression.java`
#### Snippet
```java

    @Override
    public boolean equals(Object obj)
    {
        return this.orig.equals(obj);
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/IndexedValueExpression.java`
#### Snippet
```java

    @Override
    public boolean equals(Object obj)
    {
        return this.orig.equals(obj);
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/IterationStatusExpression.java`
#### Snippet
```java

    @Override
    public boolean equals(Object obj)
    {
        return this.status.equals(obj);
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `impl/src/main/java/org/apache/myfaces/view/facelets/tag/jstl/core/IteratedValueExpression.java`
#### Snippet
```java

    @Override
    public boolean equals(Object obj)
    {
        return this.orig.equals(obj);
```

### EqualsWhichDoesntCheckParameterClass
`equals()` should check the class of its parameter
in `impl/src/main/java/org/apache/myfaces/view/facelets/impl/DefaultFaceletContext.java`
#### Snippet
```java

        @Override
        public boolean equals(Object o)
        {
            return this._owner == o || this._target == o;
```

## RuleId[ruleID=CastCanBeRemovedNarrowingVariableType]
### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'value' to 'UIComponent'
in `api/src/main/java/jakarta/faces/component/_ComponentFacetMap.java`
#### Snippet
```java
                    if (value != null && value instanceof UIComponent)
                    {
                        ((UIComponent) value).setParent(null);
                    }
                }
```

### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'name' to 'String'
in `impl/src/main/java/org/apache/myfaces/el/resolver/ScopedAttributeResolver.java`
#### Snippet
```java
        for (Object name : scopeMap.keySet())
        {
            String strName = (String)name;
            Class<?> runtimeType = scopeMap.get(strName).getClass();
            descriptorList.add(makeDescriptor(strName, runtimeType));
```

### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'pathObject' to 'String'
in `impl/src/main/java/org/apache/myfaces/config/annotation/DefaultAnnotationProvider.java`
#### Snippet
```java
            for (Object pathObject : paths)
            {
                path = (String) pathObject;
                if (path.endsWith("/"))
                {
```

### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'encodedState' to 'String'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlResponseStateManager.java`
#### Snippet
```java
        Object encodedState = 
            facesContext.getExternalContext().getRequestParameterMap().get(ResponseStateManager.VIEW_STATE_PARAM);
        if(encodedState==null || (((String) encodedState).length() == 0))
        {
            return null;
```

### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'encodedState' to 'String'
in `impl/src/main/java/org/apache/myfaces/renderkit/html/HtmlResponseStateManager.java`
#### Snippet
```java

        Object savedStateObject = getStateCache(facesContext).getStateTokenProcessor(facesContext)
                .decode(facesContext, (String)encodedState);
        
        return savedStateObject;
```

### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'secretKey' to 'SecretKey'
in `impl/src/main/java/org/apache/myfaces/application/viewstate/StateUtils.java`
#### Snippet
```java
        }

        return (SecretKey) secretKey;
    }

```

### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'secretKey' to 'SecretKey'
in `impl/src/main/java/org/apache/myfaces/application/viewstate/StateUtils.java`
#### Snippet
```java
        }

        return (SecretKey) secretKey;
    }

```

### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'clazz' to 'Class'
in `impl/src/main/java/org/apache/myfaces/application/ApplicationImpl.java`
#### Snippet
```java
            Class<?> clazz = ClassUtils.simpleClassForName((String)obj);
            classMap.put(id, clazz);
            return (Class<? extends T>)clazz;
        }
        
```

### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'encodedState' to 'String'
in `test/src/main/java/org/apache/myfaces/test/mock/MockResponseStateManager.java`
#### Snippet
```java
        Object encodedState = facesContext.getExternalContext()
                .getRequestParameterMap().get(VIEW_STATE_PARAM);
        if (encodedState == null || (((String) encodedState).length() == 0))
        {
            return null;
```

### CastCanBeRemovedNarrowingVariableType
Cast may be removed by changing the type of 'encodedState' to 'String'
in `test/src/main/java/org/apache/myfaces/test/mock/MockResponseStateManager.java`
#### Snippet
```java
        {
            savedState = (Object[]) reconstruct(facesContext,
                    (String) encodedState);
        }
        catch (IOException e)
```

